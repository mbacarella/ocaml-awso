(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and profileId =
         flag "profile-id" (required string) ~doc:"STRING ResourceId"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_profile
           (Values.AssociateProfileRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name
              ~profileId ~resourceId ())
           (Some Values.AssociateProfileResponse.to_json)
           (Some Values.AssociateProfileResponse.error_to_json)])
let associate_resource_to_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceProperties =
         flag "resource-properties" (optional string)
           ~doc:"STRING ResourceProperties"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and profileId =
         flag "profile-id" (required string) ~doc:"STRING ResourceId"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_resource_to_profile
           (Values.AssociateResourceToProfileRequest.make ?resourceProperties
              ~name ~profileId ~resourceArn ())
           (Some Values.AssociateResourceToProfileResponse.to_json)
           (Some Values.AssociateResourceToProfileResponse.error_to_json)])
let create_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING CreatorRequestId"
       and name = flag "name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_profile
           (Values.CreateProfileRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~clientToken
              ~name ()) (Some Values.CreateProfileResponse.to_json)
           (Some Values.CreateProfileResponse.error_to_json)])
let delete_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileId =
         flag "profile-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_profile (Values.DeleteProfileRequest.make ~profileId ())
           (Some Values.DeleteProfileResponse.to_json)
           (Some Values.DeleteProfileResponse.error_to_json)])
let disassociate_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileId =
         flag "profile-id" (required string) ~doc:"STRING ResourceId"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_profile
           (Values.DisassociateProfileRequest.make ~profileId ~resourceId ())
           (Some Values.DisassociateProfileResponse.to_json)
           (Some Values.DisassociateProfileResponse.error_to_json)])
let disassociate_resource_from_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileId =
         flag "profile-id" (required string) ~doc:"STRING ResourceId"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_resource_from_profile
           (Values.DisassociateResourceFromProfileRequest.make ~profileId
              ~resourceArn ())
           (Some Values.DisassociateResourceFromProfileResponse.to_json)
           (Some Values.DisassociateResourceFromProfileResponse.error_to_json)])
let get_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileId =
         flag "profile-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_profile (Values.GetProfileRequest.make ~profileId ())
           (Some Values.GetProfileResponse.to_json)
           (Some Values.GetProfileResponse.error_to_json)])
let get_profile_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileAssociationId =
         flag "profile-association-id" (required string)
           ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_profile_association
           (Values.GetProfileAssociationRequest.make ~profileAssociationId ())
           (Some Values.GetProfileAssociationResponse.to_json)
           (Some Values.GetProfileAssociationResponse.error_to_json)])
let get_profile_resource_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileResourceAssociationId =
         flag "profile-resource-association-id" (required string)
           ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_profile_resource_association
           (Values.GetProfileResourceAssociationRequest.make
              ~profileResourceAssociationId ())
           (Some Values.GetProfileResourceAssociationResponse.to_json)
           (Some Values.GetProfileResourceAssociationResponse.error_to_json)])
let list_profile_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and profileId =
         flag "profile-id" (optional string) ~doc:"STRING ResourceId"
       and resourceId =
         flag "resource-id" (optional string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_profile_associations
           (Values.ListProfileAssociationsRequest.make ?maxResults ?nextToken
              ?profileId ?resourceId ())
           (Some Values.ListProfileAssociationsResponse.to_json)
           (Some Values.ListProfileAssociationsResponse.error_to_json)])
let list_profile_resource_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and resourceType =
         flag "resource-type" (optional string) ~doc:"STRING String"
       and profileId =
         flag "profile-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_profile_resource_associations
           (Values.ListProfileResourceAssociationsRequest.make ?maxResults
              ?nextToken ?resourceType ~profileId ())
           (Some Values.ListProfileResourceAssociationsResponse.to_json)
           (Some Values.ListProfileResourceAssociationsResponse.error_to_json)])
let list_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_profiles
           (Values.ListProfilesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListProfilesResponse.to_json)
           (Some Values.ListProfilesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_profile_resource_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING Name"
       and resourceProperties =
         flag "resource-properties" (optional string)
           ~doc:"STRING ResourceProperties"
       and profileResourceAssociationId =
         flag "profile-resource-association-id" (required string)
           ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_profile_resource_association
           (Values.UpdateProfileResourceAssociationRequest.make ?name
              ?resourceProperties ~profileResourceAssociationId ())
           (Some Values.UpdateProfileResourceAssociationResponse.to_json)
           (Some
              Values.UpdateProfileResourceAssociationResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-profile", associate_profile);
    ("associate-resource-to-profile", associate_resource_to_profile);
    ("create-profile", create_profile);
    ("delete-profile", delete_profile);
    ("disassociate-profile", disassociate_profile);
    ("disassociate-resource-from-profile",
      disassociate_resource_from_profile);
    ("get-profile", get_profile);
    ("get-profile-association", get_profile_association);
    ("get-profile-resource-association", get_profile_resource_association);
    ("list-profile-associations", list_profile_associations);
    ("list-profile-resource-associations",
      list_profile_resource_associations);
    ("list-profiles", list_profiles);
    ("list-tags-for-resource", list_tags_for_resource);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-profile-resource-association",
      update_profile_resource_association)]
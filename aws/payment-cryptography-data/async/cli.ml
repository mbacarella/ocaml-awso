(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let decrypt_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and wrappedKey =
         flag "wrapped-key" (optional json_arg) ~doc:"JSON WrappedKey"
       and keyIdentifier =
         flag "key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType"
       and cipherText =
         flag "cipher-text" (required string) ~doc:"STRING CipherTextType"
       and decryptionAttributes =
         flag "decryption-attributes" (required json_arg)
           ~doc:"JSON EncryptionDecryptionAttributes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.decrypt_data
           (Values.DecryptDataInput.make
              ?wrappedKey:(Option.map ~f:Values.WrappedKey.of_json wrappedKey)
              ~keyIdentifier ~cipherText
              ~decryptionAttributes:(Values.EncryptionDecryptionAttributes.of_json
                                       decryptionAttributes) ())
           (Some Values.DecryptDataOutput.to_json)
           (Some Values.DecryptDataOutput.error_to_json)])
let encrypt_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and wrappedKey =
         flag "wrapped-key" (optional json_arg) ~doc:"JSON WrappedKey"
       and keyIdentifier =
         flag "key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType"
       and plainText =
         flag "plain-text" (required string) ~doc:"STRING PlainTextType"
       and encryptionAttributes =
         flag "encryption-attributes" (required json_arg)
           ~doc:"JSON EncryptionDecryptionAttributes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.encrypt_data
           (Values.EncryptDataInput.make
              ?wrappedKey:(Option.map ~f:Values.WrappedKey.of_json wrappedKey)
              ~keyIdentifier ~plainText
              ~encryptionAttributes:(Values.EncryptionDecryptionAttributes.of_json
                                       encryptionAttributes) ())
           (Some Values.EncryptDataOutput.to_json)
           (Some Values.EncryptDataOutput.error_to_json)])
let generate_as2805_kek_validation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyIdentifier =
         flag "key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType"
       and kekValidationType =
         flag "kek-validation-type" (required json_arg)
           ~doc:"JSON As2805KekValidationType"
       and randomKeySendVariantMask =
         flag "random-key-send-variant-mask" (required json_arg)
           ~doc:"JSON RandomKeySendVariantMask" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.generate_as2805_kek_validation
           (Values.GenerateAs2805KekValidationInput.make ~keyIdentifier
              ~kekValidationType:(Values.As2805KekValidationType.of_json
                                    kekValidationType)
              ~randomKeySendVariantMask:(Values.RandomKeySendVariantMask.of_json
                                           randomKeySendVariantMask) ())
           (Some Values.GenerateAs2805KekValidationOutput.to_json)
           (Some Values.GenerateAs2805KekValidationOutput.error_to_json)])
let generate_card_validation_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and validationDataLength =
         flag "validation-data-length" (optional int)
           ~doc:"INT IntegerRangeBetween3And5Type"
       and keyIdentifier =
         flag "key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType"
       and primaryAccountNumber =
         flag "primary-account-number" (required string)
           ~doc:"STRING PrimaryAccountNumberType"
       and generationAttributes =
         flag "generation-attributes" (required json_arg)
           ~doc:"JSON CardGenerationAttributes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.generate_card_validation_data
           (Values.GenerateCardValidationDataInput.make ?validationDataLength
              ~keyIdentifier ~primaryAccountNumber
              ~generationAttributes:(Values.CardGenerationAttributes.of_json
                                       generationAttributes) ())
           (Some Values.GenerateCardValidationDataOutput.to_json)
           (Some Values.GenerateCardValidationDataOutput.error_to_json)])
let generate_mac =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and macLength =
         flag "mac-length" (optional int)
           ~doc:"INT IntegerRangeBetween4And16"
       and keyIdentifier =
         flag "key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType"
       and messageData =
         flag "message-data" (required string) ~doc:"STRING MessageDataType"
       and generationAttributes =
         flag "generation-attributes" (required json_arg)
           ~doc:"JSON MacAttributes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.generate_mac
           (Values.GenerateMacInput.make ?macLength ~keyIdentifier
              ~messageData
              ~generationAttributes:(Values.MacAttributes.of_json
                                       generationAttributes) ())
           (Some Values.GenerateMacOutput.to_json)
           (Some Values.GenerateMacOutput.error_to_json)])
let generate_mac_emv_pin_change =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and newPinPekIdentifier =
         flag "new-pin-pek-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType"
       and newEncryptedPinBlock =
         flag "new-encrypted-pin-block" (required string)
           ~doc:"STRING PinBlockLengthEquals16"
       and pinBlockFormat =
         flag "pin-block-format" (required json_arg)
           ~doc:"JSON PinBlockFormatForEmvPinChange"
       and secureMessagingIntegrityKeyIdentifier =
         flag "secure-messaging-integrity-key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType"
       and secureMessagingConfidentialityKeyIdentifier =
         flag "secure-messaging-confidentiality-key-identifier"
           (required string) ~doc:"STRING KeyArnOrKeyAliasType"
       and messageData =
         flag "message-data" (required string)
           ~doc:"STRING CommandMessageDataType"
       and derivationMethodAttributes =
         flag "derivation-method-attributes" (required json_arg)
           ~doc:"JSON DerivationMethodAttributes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.generate_mac_emv_pin_change
           (Values.GenerateMacEmvPinChangeInput.make ~newPinPekIdentifier
              ~newEncryptedPinBlock
              ~pinBlockFormat:(Values.PinBlockFormatForEmvPinChange.of_json
                                 pinBlockFormat)
              ~secureMessagingIntegrityKeyIdentifier
              ~secureMessagingConfidentialityKeyIdentifier ~messageData
              ~derivationMethodAttributes:(Values.DerivationMethodAttributes.of_json
                                             derivationMethodAttributes) ())
           (Some Values.GenerateMacEmvPinChangeOutput.to_json)
           (Some Values.GenerateMacEmvPinChangeOutput.error_to_json)])
let generate_pin_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pinDataLength =
         flag "pin-data-length" (optional int)
           ~doc:"INT IntegerRangeBetween4And12"
       and primaryAccountNumber =
         flag "primary-account-number" (optional string)
           ~doc:"STRING PrimaryAccountNumberType"
       and encryptionWrappedKey =
         flag "encryption-wrapped-key" (optional json_arg)
           ~doc:"JSON WrappedKey"
       and generationKeyIdentifier =
         flag "generation-key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType"
       and encryptionKeyIdentifier =
         flag "encryption-key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType"
       and generationAttributes =
         flag "generation-attributes" (required json_arg)
           ~doc:"JSON PinGenerationAttributes"
       and pinBlockFormat =
         flag "pin-block-format" (required json_arg)
           ~doc:"JSON PinBlockFormatForPinData" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.generate_pin_data
           (Values.GeneratePinDataInput.make ?pinDataLength
              ?primaryAccountNumber
              ?encryptionWrappedKey:(Option.map ~f:Values.WrappedKey.of_json
                                       encryptionWrappedKey)
              ~generationKeyIdentifier ~encryptionKeyIdentifier
              ~generationAttributes:(Values.PinGenerationAttributes.of_json
                                       generationAttributes)
              ~pinBlockFormat:(Values.PinBlockFormatForPinData.of_json
                                 pinBlockFormat) ())
           (Some Values.GeneratePinDataOutput.to_json)
           (Some Values.GeneratePinDataOutput.error_to_json)])
let re_encrypt_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and incomingWrappedKey =
         flag "incoming-wrapped-key" (optional json_arg)
           ~doc:"JSON WrappedKey"
       and outgoingWrappedKey =
         flag "outgoing-wrapped-key" (optional json_arg)
           ~doc:"JSON WrappedKey"
       and incomingKeyIdentifier =
         flag "incoming-key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType"
       and outgoingKeyIdentifier =
         flag "outgoing-key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType"
       and cipherText =
         flag "cipher-text" (required string) ~doc:"STRING CipherTextType"
       and incomingEncryptionAttributes =
         flag "incoming-encryption-attributes" (required json_arg)
           ~doc:"JSON ReEncryptionAttributes"
       and outgoingEncryptionAttributes =
         flag "outgoing-encryption-attributes" (required json_arg)
           ~doc:"JSON ReEncryptionAttributes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.re_encrypt_data
           (Values.ReEncryptDataInput.make
              ?incomingWrappedKey:(Option.map ~f:Values.WrappedKey.of_json
                                     incomingWrappedKey)
              ?outgoingWrappedKey:(Option.map ~f:Values.WrappedKey.of_json
                                     outgoingWrappedKey)
              ~incomingKeyIdentifier ~outgoingKeyIdentifier ~cipherText
              ~incomingEncryptionAttributes:(Values.ReEncryptionAttributes.of_json
                                               incomingEncryptionAttributes)
              ~outgoingEncryptionAttributes:(Values.ReEncryptionAttributes.of_json
                                               outgoingEncryptionAttributes)
              ()) (Some Values.ReEncryptDataOutput.to_json)
           (Some Values.ReEncryptDataOutput.error_to_json)])
let translate_key_material =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyCheckValueAlgorithm =
         flag "key-check-value-algorithm" (optional json_arg)
           ~doc:"JSON KeyCheckValueAlgorithm"
       and incomingKeyMaterial =
         flag "incoming-key-material" (required json_arg)
           ~doc:"JSON IncomingKeyMaterial"
       and outgoingKeyMaterial =
         flag "outgoing-key-material" (required json_arg)
           ~doc:"JSON OutgoingKeyMaterial" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.translate_key_material
           (Values.TranslateKeyMaterialInput.make
              ?keyCheckValueAlgorithm:(Option.map
                                         ~f:Values.KeyCheckValueAlgorithm.of_json
                                         keyCheckValueAlgorithm)
              ~incomingKeyMaterial:(Values.IncomingKeyMaterial.of_json
                                      incomingKeyMaterial)
              ~outgoingKeyMaterial:(Values.OutgoingKeyMaterial.of_json
                                      outgoingKeyMaterial) ())
           (Some Values.TranslateKeyMaterialOutput.to_json)
           (Some Values.TranslateKeyMaterialOutput.error_to_json)])
let translate_pin_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and incomingDukptAttributes =
         flag "incoming-dukpt-attributes" (optional json_arg)
           ~doc:"JSON DukptDerivationAttributes"
       and outgoingDukptAttributes =
         flag "outgoing-dukpt-attributes" (optional json_arg)
           ~doc:"JSON DukptDerivationAttributes"
       and incomingWrappedKey =
         flag "incoming-wrapped-key" (optional json_arg)
           ~doc:"JSON WrappedKey"
       and outgoingWrappedKey =
         flag "outgoing-wrapped-key" (optional json_arg)
           ~doc:"JSON WrappedKey"
       and incomingAs2805Attributes =
         flag "incoming-as2805-attributes" (optional json_arg)
           ~doc:"JSON As2805PekDerivationAttributes"
       and incomingKeyIdentifier =
         flag "incoming-key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType"
       and outgoingKeyIdentifier =
         flag "outgoing-key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType"
       and incomingTranslationAttributes =
         flag "incoming-translation-attributes" (required json_arg)
           ~doc:"JSON TranslationIsoFormats"
       and outgoingTranslationAttributes =
         flag "outgoing-translation-attributes" (required json_arg)
           ~doc:"JSON TranslationIsoFormats"
       and encryptedPinBlock =
         flag "encrypted-pin-block" (required string)
           ~doc:"STRING HexEvenLengthBetween16And32" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.translate_pin_data
           (Values.TranslatePinDataInput.make
              ?incomingDukptAttributes:(Option.map
                                          ~f:Values.DukptDerivationAttributes.of_json
                                          incomingDukptAttributes)
              ?outgoingDukptAttributes:(Option.map
                                          ~f:Values.DukptDerivationAttributes.of_json
                                          outgoingDukptAttributes)
              ?incomingWrappedKey:(Option.map ~f:Values.WrappedKey.of_json
                                     incomingWrappedKey)
              ?outgoingWrappedKey:(Option.map ~f:Values.WrappedKey.of_json
                                     outgoingWrappedKey)
              ?incomingAs2805Attributes:(Option.map
                                           ~f:Values.As2805PekDerivationAttributes.of_json
                                           incomingAs2805Attributes)
              ~incomingKeyIdentifier ~outgoingKeyIdentifier
              ~incomingTranslationAttributes:(Values.TranslationIsoFormats.of_json
                                                incomingTranslationAttributes)
              ~outgoingTranslationAttributes:(Values.TranslationIsoFormats.of_json
                                                outgoingTranslationAttributes)
              ~encryptedPinBlock ())
           (Some Values.TranslatePinDataOutput.to_json)
           (Some Values.TranslatePinDataOutput.error_to_json)])
let verify_auth_request_cryptogram =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authResponseAttributes =
         flag "auth-response-attributes" (optional json_arg)
           ~doc:"JSON CryptogramAuthResponse"
       and keyIdentifier =
         flag "key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType"
       and transactionData =
         flag "transaction-data" (required string)
           ~doc:"STRING TransactionDataType"
       and authRequestCryptogram =
         flag "auth-request-cryptogram" (required string)
           ~doc:"STRING AuthRequestCryptogramType"
       and majorKeyDerivationMode =
         flag "major-key-derivation-mode" (required json_arg)
           ~doc:"JSON MajorKeyDerivationMode"
       and sessionKeyDerivationAttributes =
         flag "session-key-derivation-attributes" (required json_arg)
           ~doc:"JSON SessionKeyDerivation" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.verify_auth_request_cryptogram
           (Values.VerifyAuthRequestCryptogramInput.make
              ?authResponseAttributes:(Option.map
                                         ~f:Values.CryptogramAuthResponse.of_json
                                         authResponseAttributes)
              ~keyIdentifier ~transactionData ~authRequestCryptogram
              ~majorKeyDerivationMode:(Values.MajorKeyDerivationMode.of_json
                                         majorKeyDerivationMode)
              ~sessionKeyDerivationAttributes:(Values.SessionKeyDerivation.of_json
                                                 sessionKeyDerivationAttributes)
              ()) (Some Values.VerifyAuthRequestCryptogramOutput.to_json)
           (Some Values.VerifyAuthRequestCryptogramOutput.error_to_json)])
let verify_card_validation_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyIdentifier =
         flag "key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType"
       and primaryAccountNumber =
         flag "primary-account-number" (required string)
           ~doc:"STRING PrimaryAccountNumberType"
       and verificationAttributes =
         flag "verification-attributes" (required json_arg)
           ~doc:"JSON CardVerificationAttributes"
       and validationData =
         flag "validation-data" (required string)
           ~doc:"STRING ValidationDataType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.verify_card_validation_data
           (Values.VerifyCardValidationDataInput.make ~keyIdentifier
              ~primaryAccountNumber
              ~verificationAttributes:(Values.CardVerificationAttributes.of_json
                                         verificationAttributes)
              ~validationData ())
           (Some Values.VerifyCardValidationDataOutput.to_json)
           (Some Values.VerifyCardValidationDataOutput.error_to_json)])
let verify_mac =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and macLength =
         flag "mac-length" (optional int)
           ~doc:"INT IntegerRangeBetween4And16"
       and keyIdentifier =
         flag "key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType"
       and messageData =
         flag "message-data" (required string) ~doc:"STRING MessageDataType"
       and mac = flag "mac" (required string) ~doc:"STRING MacType"
       and verificationAttributes =
         flag "verification-attributes" (required json_arg)
           ~doc:"JSON MacAttributes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.verify_mac
           (Values.VerifyMacInput.make ?macLength ~keyIdentifier ~messageData
              ~mac
              ~verificationAttributes:(Values.MacAttributes.of_json
                                         verificationAttributes) ())
           (Some Values.VerifyMacOutput.to_json)
           (Some Values.VerifyMacOutput.error_to_json)])
let verify_pin_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and primaryAccountNumber =
         flag "primary-account-number" (optional string)
           ~doc:"STRING PrimaryAccountNumberType"
       and pinDataLength =
         flag "pin-data-length" (optional int)
           ~doc:"INT IntegerRangeBetween4And12"
       and dukptAttributes =
         flag "dukpt-attributes" (optional json_arg)
           ~doc:"JSON DukptAttributes"
       and encryptionWrappedKey =
         flag "encryption-wrapped-key" (optional json_arg)
           ~doc:"JSON WrappedKey"
       and verificationKeyIdentifier =
         flag "verification-key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType"
       and encryptionKeyIdentifier =
         flag "encryption-key-identifier" (required string)
           ~doc:"STRING KeyArnOrKeyAliasType"
       and verificationAttributes =
         flag "verification-attributes" (required json_arg)
           ~doc:"JSON PinVerificationAttributes"
       and encryptedPinBlock =
         flag "encrypted-pin-block" (required string)
           ~doc:"STRING EncryptedPinBlockType"
       and pinBlockFormat =
         flag "pin-block-format" (required json_arg)
           ~doc:"JSON PinBlockFormatForPinData" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.verify_pin_data
           (Values.VerifyPinDataInput.make ?primaryAccountNumber
              ?pinDataLength
              ?dukptAttributes:(Option.map ~f:Values.DukptAttributes.of_json
                                  dukptAttributes)
              ?encryptionWrappedKey:(Option.map ~f:Values.WrappedKey.of_json
                                       encryptionWrappedKey)
              ~verificationKeyIdentifier ~encryptionKeyIdentifier
              ~verificationAttributes:(Values.PinVerificationAttributes.of_json
                                         verificationAttributes)
              ~encryptedPinBlock
              ~pinBlockFormat:(Values.PinBlockFormatForPinData.of_json
                                 pinBlockFormat) ())
           (Some Values.VerifyPinDataOutput.to_json)
           (Some Values.VerifyPinDataOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("decrypt-data", decrypt_data);
    ("encrypt-data", encrypt_data);
    ("generate-as2805-kek-validation", generate_as2805_kek_validation);
    ("generate-card-validation-data", generate_card_validation_data);
    ("generate-mac", generate_mac);
    ("generate-mac-emv-pin-change", generate_mac_emv_pin_change);
    ("generate-pin-data", generate_pin_data);
    ("re-encrypt-data", re_encrypt_data);
    ("translate-key-material", translate_key_material);
    ("translate-pin-data", translate_pin_data);
    ("verify-auth-request-cryptogram", verify_auth_request_cryptogram);
    ("verify-card-validation-data", verify_card_validation_data);
    ("verify-mac", verify_mac);
    ("verify-pin-data", verify_pin_data)]
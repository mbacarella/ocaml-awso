(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_get_named_query =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namedQueryIds =
         flag "named-query-ids" (required json_arg)
           ~doc:"JSON NamedQueryIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_named_query
           (Values.BatchGetNamedQueryInput.make
              ~namedQueryIds:(Values.NamedQueryIdList.of_json namedQueryIds)
              ()) (Some Values.BatchGetNamedQueryOutput.to_json)
           (Some Values.BatchGetNamedQueryOutput.error_to_json)])
let batch_get_query_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queryExecutionIds =
         flag "query-execution-ids" (required json_arg)
           ~doc:"JSON QueryExecutionIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_query_execution
           (Values.BatchGetQueryExecutionInput.make
              ~queryExecutionIds:(Values.QueryExecutionIdList.of_json
                                    queryExecutionIds) ())
           (Some Values.BatchGetQueryExecutionOutput.to_json)
           (Some Values.BatchGetQueryExecutionOutput.error_to_json)])
let create_data_catalog =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionString"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON ParametersMap"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name =
         flag "name" (required string) ~doc:"STRING CatalogNameString"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON DataCatalogType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_catalog
           (Values.CreateDataCatalogInput.make ?description
              ?parameters:(Option.map ~f:Values.ParametersMap.of_json
                             parameters)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name
              ~type_:(Values.DataCatalogType.of_json type_) ())
           (Some Values.CreateDataCatalogOutput.to_json)
           (Some Values.CreateDataCatalogOutput.error_to_json)])
let create_named_query =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionString"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and workGroup =
         flag "work-group" (optional string) ~doc:"STRING WorkGroupName"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and database =
         flag "database" (required string) ~doc:"STRING DatabaseString"
       and queryString =
         flag "query-string" (required string) ~doc:"STRING QueryString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_named_query
           (Values.CreateNamedQueryInput.make ?description
              ?clientRequestToken ?workGroup ~name ~database ~queryString ())
           (Some Values.CreateNamedQueryOutput.to_json)
           (Some Values.CreateNamedQueryOutput.error_to_json)])
let create_prepared_statement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionString"
       and statementName =
         flag "statement-name" (required string) ~doc:"STRING StatementName"
       and workGroup =
         flag "work-group" (required string) ~doc:"STRING WorkGroupName"
       and queryStatement =
         flag "query-statement" (required string) ~doc:"STRING QueryString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_prepared_statement
           (Values.CreatePreparedStatementInput.make ?description
              ~statementName ~workGroup ~queryStatement ())
           (Some Values.CreatePreparedStatementOutput.to_json)
           (Some Values.CreatePreparedStatementOutput.error_to_json)])
let create_work_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON WorkGroupConfiguration"
       and description =
         flag "description" (optional string)
           ~doc:"STRING WorkGroupDescriptionString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name = flag "name" (required string) ~doc:"STRING WorkGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_work_group
           (Values.CreateWorkGroupInput.make
              ?configuration:(Option.map
                                ~f:Values.WorkGroupConfiguration.of_json
                                configuration) ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name ())
           (Some Values.CreateWorkGroupOutput.to_json)
           (Some Values.CreateWorkGroupOutput.error_to_json)])
let delete_data_catalog =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING CatalogNameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_data_catalog
           (Values.DeleteDataCatalogInput.make ~name ())
           (Some Values.DeleteDataCatalogOutput.to_json)
           (Some Values.DeleteDataCatalogOutput.error_to_json)])
let delete_named_query =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namedQueryId =
         flag "named-query-id" (required string) ~doc:"STRING NamedQueryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_named_query
           (Values.DeleteNamedQueryInput.make ~namedQueryId ())
           (Some Values.DeleteNamedQueryOutput.to_json)
           (Some Values.DeleteNamedQueryOutput.error_to_json)])
let delete_prepared_statement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and statementName =
         flag "statement-name" (required string) ~doc:"STRING StatementName"
       and workGroup =
         flag "work-group" (required string) ~doc:"STRING WorkGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_prepared_statement
           (Values.DeletePreparedStatementInput.make ~statementName
              ~workGroup ())
           (Some Values.DeletePreparedStatementOutput.to_json)
           (Some Values.DeletePreparedStatementOutput.error_to_json)])
let delete_work_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and recursiveDeleteOption =
         flag "recursive-delete-option" (optional bool)
           ~doc:"BOOL BoxedBoolean"
       and workGroup =
         flag "work-group" (required string) ~doc:"STRING WorkGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_work_group
           (Values.DeleteWorkGroupInput.make ?recursiveDeleteOption
              ~workGroup ()) (Some Values.DeleteWorkGroupOutput.to_json)
           (Some Values.DeleteWorkGroupOutput.error_to_json)])
let get_data_catalog =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING CatalogNameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_catalog (Values.GetDataCatalogInput.make ~name ())
           (Some Values.GetDataCatalogOutput.to_json)
           (Some Values.GetDataCatalogOutput.error_to_json)])
let get_database =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogName =
         flag "catalog-name" (required string)
           ~doc:"STRING CatalogNameString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_database
           (Values.GetDatabaseInput.make ~catalogName ~databaseName ())
           (Some Values.GetDatabaseOutput.to_json)
           (Some Values.GetDatabaseOutput.error_to_json)])
let get_named_query =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namedQueryId =
         flag "named-query-id" (required string) ~doc:"STRING NamedQueryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_named_query
           (Values.GetNamedQueryInput.make ~namedQueryId ())
           (Some Values.GetNamedQueryOutput.to_json)
           (Some Values.GetNamedQueryOutput.error_to_json)])
let get_prepared_statement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and statementName =
         flag "statement-name" (required string) ~doc:"STRING StatementName"
       and workGroup =
         flag "work-group" (required string) ~doc:"STRING WorkGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_prepared_statement
           (Values.GetPreparedStatementInput.make ~statementName ~workGroup
              ()) (Some Values.GetPreparedStatementOutput.to_json)
           (Some Values.GetPreparedStatementOutput.error_to_json)])
let get_query_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queryExecutionId =
         flag "query-execution-id" (required string)
           ~doc:"STRING QueryExecutionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_query_execution
           (Values.GetQueryExecutionInput.make ~queryExecutionId ())
           (Some Values.GetQueryExecutionOutput.to_json)
           (Some Values.GetQueryExecutionOutput.error_to_json)])
let get_query_results =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxQueryResults"
       and queryExecutionId =
         flag "query-execution-id" (required string)
           ~doc:"STRING QueryExecutionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_query_results
           (Values.GetQueryResultsInput.make ?nextToken ?maxResults
              ~queryExecutionId ())
           (Some Values.GetQueryResultsOutput.to_json)
           (Some Values.GetQueryResultsOutput.error_to_json)])
let get_table_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogName =
         flag "catalog-name" (required string)
           ~doc:"STRING CatalogNameString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_table_metadata
           (Values.GetTableMetadataInput.make ~catalogName ~databaseName
              ~tableName ()) (Some Values.GetTableMetadataOutput.to_json)
           (Some Values.GetTableMetadataOutput.error_to_json)])
let get_work_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workGroup =
         flag "work-group" (required string) ~doc:"STRING WorkGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_work_group (Values.GetWorkGroupInput.make ~workGroup ())
           (Some Values.GetWorkGroupOutput.to_json)
           (Some Values.GetWorkGroupOutput.error_to_json)])
let list_data_catalogs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxDataCatalogsCount" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_catalogs
           (Values.ListDataCatalogsInput.make ?nextToken ?maxResults ())
           (Some Values.ListDataCatalogsOutput.to_json)
           (Some Values.ListDataCatalogsOutput.error_to_json)])
let list_databases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxDatabasesCount"
       and catalogName =
         flag "catalog-name" (required string)
           ~doc:"STRING CatalogNameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_databases
           (Values.ListDatabasesInput.make ?nextToken ?maxResults
              ~catalogName ()) (Some Values.ListDatabasesOutput.to_json)
           (Some Values.ListDatabasesOutput.error_to_json)])
let list_engine_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxEngineVersionsCount" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_engine_versions
           (Values.ListEngineVersionsInput.make ?nextToken ?maxResults ())
           (Some Values.ListEngineVersionsOutput.to_json)
           (Some Values.ListEngineVersionsOutput.error_to_json)])
let list_named_queries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxNamedQueriesCount"
       and workGroup =
         flag "work-group" (optional string) ~doc:"STRING WorkGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_named_queries
           (Values.ListNamedQueriesInput.make ?nextToken ?maxResults
              ?workGroup ()) (Some Values.ListNamedQueriesOutput.to_json)
           (Some Values.ListNamedQueriesOutput.error_to_json)])
let list_prepared_statements =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxPreparedStatementsCount"
       and workGroup =
         flag "work-group" (required string) ~doc:"STRING WorkGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_prepared_statements
           (Values.ListPreparedStatementsInput.make ?nextToken ?maxResults
              ~workGroup ())
           (Some Values.ListPreparedStatementsOutput.to_json)
           (Some Values.ListPreparedStatementsOutput.error_to_json)])
let list_query_executions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxQueryExecutionsCount"
       and workGroup =
         flag "work-group" (optional string) ~doc:"STRING WorkGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_query_executions
           (Values.ListQueryExecutionsInput.make ?nextToken ?maxResults
              ?workGroup ()) (Some Values.ListQueryExecutionsOutput.to_json)
           (Some Values.ListQueryExecutionsOutput.error_to_json)])
let list_table_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expression =
         flag "expression" (optional string) ~doc:"STRING ExpressionString"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxTableMetadataCount"
       and catalogName =
         flag "catalog-name" (required string)
           ~doc:"STRING CatalogNameString"
       and databaseName =
         flag "database-name" (required string) ~doc:"STRING NameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_table_metadata
           (Values.ListTableMetadataInput.make ?expression ?nextToken
              ?maxResults ~catalogName ~databaseName ())
           (Some Values.ListTableMetadataOutput.to_json)
           (Some Values.ListTableMetadataOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxTagsCount"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ?nextToken ?maxResults
              ~resourceARN ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let list_work_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxWorkGroupsCount" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_work_groups
           (Values.ListWorkGroupsInput.make ?nextToken ?maxResults ())
           (Some Values.ListWorkGroupsOutput.to_json)
           (Some Values.ListWorkGroupsOutput.error_to_json)])
let start_query_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING IdempotencyToken"
       and queryExecutionContext =
         flag "query-execution-context" (optional json_arg)
           ~doc:"JSON QueryExecutionContext"
       and resultConfiguration =
         flag "result-configuration" (optional json_arg)
           ~doc:"JSON ResultConfiguration"
       and workGroup =
         flag "work-group" (optional string) ~doc:"STRING WorkGroupName"
       and queryString =
         flag "query-string" (required string) ~doc:"STRING QueryString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_query_execution
           (Values.StartQueryExecutionInput.make ?clientRequestToken
              ?queryExecutionContext:(Option.map
                                        ~f:Values.QueryExecutionContext.of_json
                                        queryExecutionContext)
              ?resultConfiguration:(Option.map
                                      ~f:Values.ResultConfiguration.of_json
                                      resultConfiguration) ?workGroup
              ~queryString ())
           (Some Values.StartQueryExecutionOutput.to_json)
           (Some Values.StartQueryExecutionOutput.error_to_json)])
let stop_query_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queryExecutionId =
         flag "query-execution-id" (required string)
           ~doc:"STRING QueryExecutionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_query_execution
           (Values.StopQueryExecutionInput.make ~queryExecutionId ())
           (Some Values.StopQueryExecutionOutput.to_json)
           (Some Values.StopQueryExecutionOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceOutput.to_json)
           (Some Values.TagResourceOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceOutput.to_json)
           (Some Values.UntagResourceOutput.error_to_json)])
let update_data_catalog =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionString"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON ParametersMap"
       and name =
         flag "name" (required string) ~doc:"STRING CatalogNameString"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON DataCatalogType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_data_catalog
           (Values.UpdateDataCatalogInput.make ?description
              ?parameters:(Option.map ~f:Values.ParametersMap.of_json
                             parameters) ~name
              ~type_:(Values.DataCatalogType.of_json type_) ())
           (Some Values.UpdateDataCatalogOutput.to_json)
           (Some Values.UpdateDataCatalogOutput.error_to_json)])
let update_named_query =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING NamedQueryDescriptionString"
       and namedQueryId =
         flag "named-query-id" (required string) ~doc:"STRING NamedQueryId"
       and name = flag "name" (required string) ~doc:"STRING NameString"
       and queryString =
         flag "query-string" (required string) ~doc:"STRING QueryString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_named_query
           (Values.UpdateNamedQueryInput.make ?description ~namedQueryId
              ~name ~queryString ())
           (Some Values.UpdateNamedQueryOutput.to_json)
           (Some Values.UpdateNamedQueryOutput.error_to_json)])
let update_prepared_statement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionString"
       and statementName =
         flag "statement-name" (required string) ~doc:"STRING StatementName"
       and workGroup =
         flag "work-group" (required string) ~doc:"STRING WorkGroupName"
       and queryStatement =
         flag "query-statement" (required string) ~doc:"STRING QueryString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_prepared_statement
           (Values.UpdatePreparedStatementInput.make ?description
              ~statementName ~workGroup ~queryStatement ())
           (Some Values.UpdatePreparedStatementOutput.to_json)
           (Some Values.UpdatePreparedStatementOutput.error_to_json)])
let update_work_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING WorkGroupDescriptionString"
       and configurationUpdates =
         flag "configuration-updates" (optional json_arg)
           ~doc:"JSON WorkGroupConfigurationUpdates"
       and state =
         flag "state" (optional json_arg) ~doc:"JSON WorkGroupState"
       and workGroup =
         flag "work-group" (required string) ~doc:"STRING WorkGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_work_group
           (Values.UpdateWorkGroupInput.make ?description
              ?configurationUpdates:(Option.map
                                       ~f:Values.WorkGroupConfigurationUpdates.of_json
                                       configurationUpdates)
              ?state:(Option.map ~f:Values.WorkGroupState.of_json state)
              ~workGroup ()) (Some Values.UpdateWorkGroupOutput.to_json)
           (Some Values.UpdateWorkGroupOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("batch-get-named-query", batch_get_named_query);
    ("batch-get-query-execution", batch_get_query_execution);
    ("create-data-catalog", create_data_catalog);
    ("create-named-query", create_named_query);
    ("create-prepared-statement", create_prepared_statement);
    ("create-work-group", create_work_group);
    ("delete-data-catalog", delete_data_catalog);
    ("delete-named-query", delete_named_query);
    ("delete-prepared-statement", delete_prepared_statement);
    ("delete-work-group", delete_work_group);
    ("get-data-catalog", get_data_catalog);
    ("get-database", get_database);
    ("get-named-query", get_named_query);
    ("get-prepared-statement", get_prepared_statement);
    ("get-query-execution", get_query_execution);
    ("get-query-results", get_query_results);
    ("get-table-metadata", get_table_metadata);
    ("get-work-group", get_work_group);
    ("list-data-catalogs", list_data_catalogs);
    ("list-databases", list_databases);
    ("list-engine-versions", list_engine_versions);
    ("list-named-queries", list_named_queries);
    ("list-prepared-statements", list_prepared_statements);
    ("list-query-executions", list_query_executions);
    ("list-table-metadata", list_table_metadata);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-work-groups", list_work_groups);
    ("start-query-execution", start_query_execution);
    ("stop-query-execution", stop_query_execution);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-data-catalog", update_data_catalog);
    ("update-named-query", update_named_query);
    ("update-prepared-statement", update_prepared_statement);
    ("update-work-group", update_work_group)]
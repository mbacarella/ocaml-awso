(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let convert_recovery_point_to_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and retentionPeriod =
         flag "retention-period" (optional int) ~doc:"INT Integer"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and recoveryPointId =
         flag "recovery-point-id" (required string) ~doc:"STRING String"
       and snapshotName =
         flag "snapshot-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.convert_recovery_point_to_snapshot
           (Values.ConvertRecoveryPointToSnapshotRequest.make
              ?retentionPeriod
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~recoveryPointId ~snapshotName ())
           (Some Values.ConvertRecoveryPointToSnapshotResponse.to_json)
           (Some Values.ConvertRecoveryPointToSnapshotResponse.error_to_json)])
let create_custom_domain_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customDomainCertificateArn =
         flag "custom-domain-certificate-arn" (required string)
           ~doc:"STRING CustomDomainCertificateArnString"
       and customDomainName =
         flag "custom-domain-name" (required string)
           ~doc:"STRING CustomDomainName"
       and workgroupName =
         flag "workgroup-name" (required string) ~doc:"STRING WorkgroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_custom_domain_association
           (Values.CreateCustomDomainAssociationRequest.make
              ~customDomainCertificateArn ~customDomainName ~workgroupName ())
           (Some Values.CreateCustomDomainAssociationResponse.to_json)
           (Some Values.CreateCustomDomainAssociationResponse.error_to_json)])
let create_endpoint_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ownerAccount =
         flag "owner-account" (optional string) ~doc:"STRING OwnerAccount"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON VpcSecurityGroupIdList"
       and endpointName =
         flag "endpoint-name" (required string) ~doc:"STRING String"
       and subnetIds =
         flag "subnet-ids" (required json_arg) ~doc:"JSON SubnetIdList"
       and workgroupName =
         flag "workgroup-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_endpoint_access
           (Values.CreateEndpointAccessRequest.make ?ownerAccount
              ?vpcSecurityGroupIds:(Option.map
                                      ~f:Values.VpcSecurityGroupIdList.of_json
                                      vpcSecurityGroupIds) ~endpointName
              ~subnetIds:(Values.SubnetIdList.of_json subnetIds)
              ~workgroupName ())
           (Some Values.CreateEndpointAccessResponse.to_json)
           (Some Values.CreateEndpointAccessResponse.error_to_json)])
let create_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and adminPasswordSecretKmsKeyId =
         flag "admin-password-secret-kms-key-id" (optional string)
           ~doc:"STRING KmsKeyId"
       and adminUserPassword =
         flag "admin-user-password" (optional string)
           ~doc:"STRING DbPassword"
       and adminUsername =
         flag "admin-username" (optional string) ~doc:"STRING DbUser"
       and dbName = flag "db-name" (optional string) ~doc:"STRING String"
       and defaultIamRoleArn =
         flag "default-iam-role-arn" (optional string) ~doc:"STRING String"
       and iamRoles =
         flag "iam-roles" (optional json_arg) ~doc:"JSON IamRoleArnList"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and logExports =
         flag "log-exports" (optional json_arg) ~doc:"JSON LogExportList"
       and manageAdminPassword =
         flag "manage-admin-password" (optional bool) ~doc:"BOOL Boolean"
       and redshiftIdcApplicationArn =
         flag "redshift-idc-application-arn" (optional string)
           ~doc:"STRING RedshiftIdcApplicationArn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and namespaceName =
         flag "namespace-name" (required string) ~doc:"STRING NamespaceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_namespace
           (Values.CreateNamespaceRequest.make ?adminPasswordSecretKmsKeyId
              ?adminUserPassword ?adminUsername ?dbName ?defaultIamRoleArn
              ?iamRoles:(Option.map ~f:Values.IamRoleArnList.of_json iamRoles)
              ?kmsKeyId
              ?logExports:(Option.map ~f:Values.LogExportList.of_json
                             logExports) ?manageAdminPassword
              ?redshiftIdcApplicationArn
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~namespaceName ())
           (Some Values.CreateNamespaceResponse.to_json)
           (Some Values.CreateNamespaceResponse.error_to_json)])
let create_reservation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and capacity = flag "capacity" (required int) ~doc:"INT Capacity"
       and offeringId =
         flag "offering-id" (required string)
           ~doc:"STRING CreateReservationRequestOfferingIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_reservation
           (Values.CreateReservationRequest.make ?clientToken ~capacity
              ~offeringId ()) (Some Values.CreateReservationResponse.to_json)
           (Some Values.CreateReservationResponse.error_to_json)])
let create_scheduled_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and enabled = flag "enabled" (optional bool) ~doc:"BOOL Boolean"
       and endTime =
         flag "end-time" (optional json_arg) ~doc:"JSON Timestamp"
       and scheduledActionDescription =
         flag "scheduled-action-description" (optional string)
           ~doc:"STRING String"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON Timestamp"
       and namespaceName =
         flag "namespace-name" (required string) ~doc:"STRING NamespaceName"
       and roleArn =
         flag "role-arn" (required string) ~doc:"STRING IamRoleArn"
       and schedule =
         flag "schedule" (required json_arg) ~doc:"JSON Schedule"
       and scheduledActionName =
         flag "scheduled-action-name" (required string)
           ~doc:"STRING ScheduledActionName"
       and targetAction =
         flag "target-action" (required json_arg) ~doc:"JSON TargetAction" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_scheduled_action
           (Values.CreateScheduledActionRequest.make ?enabled
              ?endTime:(Option.map ~f:Values.Timestamp.of_json endTime)
              ?scheduledActionDescription
              ?startTime:(Option.map ~f:Values.Timestamp.of_json startTime)
              ~namespaceName ~roleArn
              ~schedule:(Values.Schedule.of_json schedule)
              ~scheduledActionName
              ~targetAction:(Values.TargetAction.of_json targetAction) ())
           (Some Values.CreateScheduledActionResponse.to_json)
           (Some Values.CreateScheduledActionResponse.error_to_json)])
let create_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and retentionPeriod =
         flag "retention-period" (optional int) ~doc:"INT Integer"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and namespaceName =
         flag "namespace-name" (required string) ~doc:"STRING String"
       and snapshotName =
         flag "snapshot-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_snapshot
           (Values.CreateSnapshotRequest.make ?retentionPeriod
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~namespaceName ~snapshotName ())
           (Some Values.CreateSnapshotResponse.to_json)
           (Some Values.CreateSnapshotResponse.error_to_json)])
let create_snapshot_copy_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and destinationKmsKeyId =
         flag "destination-kms-key-id" (optional string)
           ~doc:"STRING KmsKeyId"
       and snapshotRetentionPeriod =
         flag "snapshot-retention-period" (optional int) ~doc:"INT Integer"
       and destinationRegion =
         flag "destination-region" (required string) ~doc:"STRING String"
       and namespaceName =
         flag "namespace-name" (required string) ~doc:"STRING NamespaceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_snapshot_copy_configuration
           (Values.CreateSnapshotCopyConfigurationRequest.make
              ?destinationKmsKeyId ?snapshotRetentionPeriod
              ~destinationRegion ~namespaceName ())
           (Some Values.CreateSnapshotCopyConfigurationResponse.to_json)
           (Some Values.CreateSnapshotCopyConfigurationResponse.error_to_json)])
let create_usage_limit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and breachAction =
         flag "breach-action" (optional json_arg)
           ~doc:"JSON UsageLimitBreachAction"
       and period =
         flag "period" (optional json_arg) ~doc:"JSON UsageLimitPeriod"
       and amount = flag "amount" (required json_arg) ~doc:"JSON Long"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and usageType =
         flag "usage-type" (required json_arg)
           ~doc:"JSON UsageLimitUsageType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_usage_limit
           (Values.CreateUsageLimitRequest.make
              ?breachAction:(Option.map
                               ~f:Values.UsageLimitBreachAction.of_json
                               breachAction)
              ?period:(Option.map ~f:Values.UsageLimitPeriod.of_json period)
              ~amount:(Values.Long.of_json amount) ~resourceArn
              ~usageType:(Values.UsageLimitUsageType.of_json usageType) ())
           (Some Values.CreateUsageLimitResponse.to_json)
           (Some Values.CreateUsageLimitResponse.error_to_json)])
let create_workgroup =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and baseCapacity =
         flag "base-capacity" (optional int) ~doc:"INT Integer"
       and configParameters =
         flag "config-parameters" (optional json_arg)
           ~doc:"JSON ConfigParameterList"
       and enhancedVpcRouting =
         flag "enhanced-vpc-routing" (optional bool) ~doc:"BOOL Boolean"
       and extraComputeForAutomaticOptimization =
         flag "extra-compute-for-automatic-optimization" (optional bool)
           ~doc:"BOOL Boolean"
       and ipAddressType =
         flag "ip-address-type" (optional string) ~doc:"STRING IpAddressType"
       and maxCapacity =
         flag "max-capacity" (optional int) ~doc:"INT Integer"
       and port = flag "port" (optional int) ~doc:"INT Integer"
       and pricePerformanceTarget =
         flag "price-performance-target" (optional json_arg)
           ~doc:"JSON PerformanceTarget"
       and publiclyAccessible =
         flag "publicly-accessible" (optional bool) ~doc:"BOOL Boolean"
       and securityGroupIds =
         flag "security-group-ids" (optional json_arg)
           ~doc:"JSON SecurityGroupIdList"
       and subnetIds =
         flag "subnet-ids" (optional json_arg) ~doc:"JSON SubnetIdList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and trackName =
         flag "track-name" (optional string) ~doc:"STRING TrackName"
       and namespaceName =
         flag "namespace-name" (required string) ~doc:"STRING NamespaceName"
       and workgroupName =
         flag "workgroup-name" (required string) ~doc:"STRING WorkgroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_workgroup
           (Values.CreateWorkgroupRequest.make ?baseCapacity
              ?configParameters:(Option.map
                                   ~f:Values.ConfigParameterList.of_json
                                   configParameters) ?enhancedVpcRouting
              ?extraComputeForAutomaticOptimization ?ipAddressType
              ?maxCapacity ?port
              ?pricePerformanceTarget:(Option.map
                                         ~f:Values.PerformanceTarget.of_json
                                         pricePerformanceTarget)
              ?publiclyAccessible
              ?securityGroupIds:(Option.map
                                   ~f:Values.SecurityGroupIdList.of_json
                                   securityGroupIds)
              ?subnetIds:(Option.map ~f:Values.SubnetIdList.of_json subnetIds)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?trackName
              ~namespaceName ~workgroupName ())
           (Some Values.CreateWorkgroupResponse.to_json)
           (Some Values.CreateWorkgroupResponse.error_to_json)])
let delete_custom_domain_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customDomainName =
         flag "custom-domain-name" (required string)
           ~doc:"STRING CustomDomainName"
       and workgroupName =
         flag "workgroup-name" (required string) ~doc:"STRING WorkgroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_custom_domain_association
           (Values.DeleteCustomDomainAssociationRequest.make
              ~customDomainName ~workgroupName ())
           (Some Values.DeleteCustomDomainAssociationResponse.to_json)
           (Some Values.DeleteCustomDomainAssociationResponse.error_to_json)])
let delete_endpoint_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointName =
         flag "endpoint-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_endpoint_access
           (Values.DeleteEndpointAccessRequest.make ~endpointName ())
           (Some Values.DeleteEndpointAccessResponse.to_json)
           (Some Values.DeleteEndpointAccessResponse.error_to_json)])
let delete_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and finalSnapshotName =
         flag "final-snapshot-name" (optional string) ~doc:"STRING String"
       and finalSnapshotRetentionPeriod =
         flag "final-snapshot-retention-period" (optional int)
           ~doc:"INT Integer"
       and namespaceName =
         flag "namespace-name" (required string) ~doc:"STRING NamespaceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_namespace
           (Values.DeleteNamespaceRequest.make ?finalSnapshotName
              ?finalSnapshotRetentionPeriod ~namespaceName ())
           (Some Values.DeleteNamespaceResponse.to_json)
           (Some Values.DeleteNamespaceResponse.error_to_json)])
let delete_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_policy
           (Values.DeleteResourcePolicyRequest.make ~resourceArn ())
           (Some Values.DeleteResourcePolicyResponse.to_json)
           (Some Values.DeleteResourcePolicyResponse.error_to_json)])
let delete_scheduled_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scheduledActionName =
         flag "scheduled-action-name" (required string)
           ~doc:"STRING ScheduledActionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_scheduled_action
           (Values.DeleteScheduledActionRequest.make ~scheduledActionName ())
           (Some Values.DeleteScheduledActionResponse.to_json)
           (Some Values.DeleteScheduledActionResponse.error_to_json)])
let delete_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snapshotName =
         flag "snapshot-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_snapshot
           (Values.DeleteSnapshotRequest.make ~snapshotName ())
           (Some Values.DeleteSnapshotResponse.to_json)
           (Some Values.DeleteSnapshotResponse.error_to_json)])
let delete_snapshot_copy_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snapshotCopyConfigurationId =
         flag "snapshot-copy-configuration-id" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_snapshot_copy_configuration
           (Values.DeleteSnapshotCopyConfigurationRequest.make
              ~snapshotCopyConfigurationId ())
           (Some Values.DeleteSnapshotCopyConfigurationResponse.to_json)
           (Some Values.DeleteSnapshotCopyConfigurationResponse.error_to_json)])
let delete_usage_limit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and usageLimitId =
         flag "usage-limit-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_usage_limit
           (Values.DeleteUsageLimitRequest.make ~usageLimitId ())
           (Some Values.DeleteUsageLimitResponse.to_json)
           (Some Values.DeleteUsageLimitResponse.error_to_json)])
let delete_workgroup =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workgroupName =
         flag "workgroup-name" (required string) ~doc:"STRING WorkgroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_workgroup
           (Values.DeleteWorkgroupRequest.make ~workgroupName ())
           (Some Values.DeleteWorkgroupResponse.to_json)
           (Some Values.DeleteWorkgroupResponse.error_to_json)])
let get_credentials =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customDomainName =
         flag "custom-domain-name" (optional string)
           ~doc:"STRING CustomDomainName"
       and dbName = flag "db-name" (optional string) ~doc:"STRING DbName"
       and durationSeconds =
         flag "duration-seconds" (optional int) ~doc:"INT Integer"
       and workgroupName =
         flag "workgroup-name" (optional string) ~doc:"STRING WorkgroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_credentials
           (Values.GetCredentialsRequest.make ?customDomainName ?dbName
              ?durationSeconds ?workgroupName ())
           (Some Values.GetCredentialsResponse.to_json)
           (Some Values.GetCredentialsResponse.error_to_json)])
let get_custom_domain_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customDomainName =
         flag "custom-domain-name" (required string)
           ~doc:"STRING CustomDomainName"
       and workgroupName =
         flag "workgroup-name" (required string) ~doc:"STRING WorkgroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_custom_domain_association
           (Values.GetCustomDomainAssociationRequest.make ~customDomainName
              ~workgroupName ())
           (Some Values.GetCustomDomainAssociationResponse.to_json)
           (Some Values.GetCustomDomainAssociationResponse.error_to_json)])
let get_endpoint_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointName =
         flag "endpoint-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_endpoint_access
           (Values.GetEndpointAccessRequest.make ~endpointName ())
           (Some Values.GetEndpointAccessResponse.to_json)
           (Some Values.GetEndpointAccessResponse.error_to_json)])
let get_identity_center_auth_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workgroupNames =
         flag "workgroup-names" (required json_arg)
           ~doc:"JSON WorkgroupNameList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_identity_center_auth_token
           (Values.GetIdentityCenterAuthTokenRequest.make
              ~workgroupNames:(Values.WorkgroupNameList.of_json
                                 workgroupNames) ())
           (Some Values.GetIdentityCenterAuthTokenResponse.to_json)
           (Some Values.GetIdentityCenterAuthTokenResponse.error_to_json)])
let get_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namespaceName =
         flag "namespace-name" (required string) ~doc:"STRING NamespaceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_namespace
           (Values.GetNamespaceRequest.make ~namespaceName ())
           (Some Values.GetNamespaceResponse.to_json)
           (Some Values.GetNamespaceResponse.error_to_json)])
let get_recovery_point =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and recoveryPointId =
         flag "recovery-point-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_recovery_point
           (Values.GetRecoveryPointRequest.make ~recoveryPointId ())
           (Some Values.GetRecoveryPointResponse.to_json)
           (Some Values.GetRecoveryPointResponse.error_to_json)])
let get_reservation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reservationId =
         flag "reservation-id" (required string)
           ~doc:"STRING GetReservationRequestReservationIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_reservation
           (Values.GetReservationRequest.make ~reservationId ())
           (Some Values.GetReservationResponse.to_json)
           (Some Values.GetReservationResponse.error_to_json)])
let get_reservation_offering =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and offeringId =
         flag "offering-id" (required string)
           ~doc:"STRING GetReservationOfferingRequestOfferingIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_reservation_offering
           (Values.GetReservationOfferingRequest.make ~offeringId ())
           (Some Values.GetReservationOfferingResponse.to_json)
           (Some Values.GetReservationOfferingResponse.error_to_json)])
let get_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_policy
           (Values.GetResourcePolicyRequest.make ~resourceArn ())
           (Some Values.GetResourcePolicyResponse.to_json)
           (Some Values.GetResourcePolicyResponse.error_to_json)])
let get_scheduled_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scheduledActionName =
         flag "scheduled-action-name" (required string)
           ~doc:"STRING ScheduledActionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_scheduled_action
           (Values.GetScheduledActionRequest.make ~scheduledActionName ())
           (Some Values.GetScheduledActionResponse.to_json)
           (Some Values.GetScheduledActionResponse.error_to_json)])
let get_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ownerAccount =
         flag "owner-account" (optional string) ~doc:"STRING String"
       and snapshotArn =
         flag "snapshot-arn" (optional string) ~doc:"STRING String"
       and snapshotName =
         flag "snapshot-name" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_snapshot
           (Values.GetSnapshotRequest.make ?ownerAccount ?snapshotArn
              ?snapshotName ()) (Some Values.GetSnapshotResponse.to_json)
           (Some Values.GetSnapshotResponse.error_to_json)])
let get_table_restore_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableRestoreRequestId =
         flag "table-restore-request-id" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_table_restore_status
           (Values.GetTableRestoreStatusRequest.make ~tableRestoreRequestId
              ()) (Some Values.GetTableRestoreStatusResponse.to_json)
           (Some Values.GetTableRestoreStatusResponse.error_to_json)])
let get_track =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trackName =
         flag "track-name" (required string) ~doc:"STRING TrackName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_track (Values.GetTrackRequest.make ~trackName ())
           (Some Values.GetTrackResponse.to_json)
           (Some Values.GetTrackResponse.error_to_json)])
let get_usage_limit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and usageLimitId =
         flag "usage-limit-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_usage_limit
           (Values.GetUsageLimitRequest.make ~usageLimitId ())
           (Some Values.GetUsageLimitResponse.to_json)
           (Some Values.GetUsageLimitResponse.error_to_json)])
let get_workgroup =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workgroupName =
         flag "workgroup-name" (required string) ~doc:"STRING WorkgroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_workgroup
           (Values.GetWorkgroupRequest.make ~workgroupName ())
           (Some Values.GetWorkgroupResponse.to_json)
           (Some Values.GetWorkgroupResponse.error_to_json)])
let list_custom_domain_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customDomainCertificateArn =
         flag "custom-domain-certificate-arn" (optional string)
           ~doc:"STRING CustomDomainCertificateArnString"
       and customDomainName =
         flag "custom-domain-name" (optional string)
           ~doc:"STRING CustomDomainName"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListCustomDomainAssociationsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_custom_domain_associations
           (Values.ListCustomDomainAssociationsRequest.make
              ?customDomainCertificateArn ?customDomainName ?maxResults
              ?nextToken ())
           (Some Values.ListCustomDomainAssociationsResponse.to_json)
           (Some Values.ListCustomDomainAssociationsResponse.error_to_json)])
let list_endpoint_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListEndpointAccessRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and ownerAccount =
         flag "owner-account" (optional string) ~doc:"STRING OwnerAccount"
       and vpcId = flag "vpc-id" (optional string) ~doc:"STRING String"
       and workgroupName =
         flag "workgroup-name" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_endpoint_access
           (Values.ListEndpointAccessRequest.make ?maxResults ?nextToken
              ?ownerAccount ?vpcId ?workgroupName ())
           (Some Values.ListEndpointAccessResponse.to_json)
           (Some Values.ListEndpointAccessResponse.error_to_json)])
let list_managed_workgroups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListManagedWorkgroupsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and sourceArn =
         flag "source-arn" (optional string) ~doc:"STRING SourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_managed_workgroups
           (Values.ListManagedWorkgroupsRequest.make ?maxResults ?nextToken
              ?sourceArn ())
           (Some Values.ListManagedWorkgroupsResponse.to_json)
           (Some Values.ListManagedWorkgroupsResponse.error_to_json)])
let list_namespaces =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListNamespacesRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_namespaces
           (Values.ListNamespacesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListNamespacesResponse.to_json)
           (Some Values.ListNamespacesResponse.error_to_json)])
let list_recovery_points =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endTime =
         flag "end-time" (optional json_arg) ~doc:"JSON Timestamp"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListRecoveryPointsRequestMaxResultsInteger"
       and namespaceArn =
         flag "namespace-arn" (optional string) ~doc:"STRING String"
       and namespaceName =
         flag "namespace-name" (optional string) ~doc:"STRING NamespaceName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_recovery_points
           (Values.ListRecoveryPointsRequest.make
              ?endTime:(Option.map ~f:Values.Timestamp.of_json endTime)
              ?maxResults ?namespaceArn ?namespaceName ?nextToken
              ?startTime:(Option.map ~f:Values.Timestamp.of_json startTime)
              ()) (Some Values.ListRecoveryPointsResponse.to_json)
           (Some Values.ListRecoveryPointsResponse.error_to_json)])
let list_reservation_offerings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListReservationOfferingsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_reservation_offerings
           (Values.ListReservationOfferingsRequest.make ?maxResults
              ?nextToken ())
           (Some Values.ListReservationOfferingsResponse.to_json)
           (Some Values.ListReservationOfferingsResponse.error_to_json)])
let list_reservations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListReservationsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_reservations
           (Values.ListReservationsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListReservationsResponse.to_json)
           (Some Values.ListReservationsResponse.error_to_json)])
let list_scheduled_actions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListScheduledActionsRequestMaxResultsInteger"
       and namespaceName =
         flag "namespace-name" (optional string) ~doc:"STRING NamespaceName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_scheduled_actions
           (Values.ListScheduledActionsRequest.make ?maxResults
              ?namespaceName ?nextToken ())
           (Some Values.ListScheduledActionsResponse.to_json)
           (Some Values.ListScheduledActionsResponse.error_to_json)])
let list_snapshot_copy_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListSnapshotCopyConfigurationsRequestMaxResultsInteger"
       and namespaceName =
         flag "namespace-name" (optional string) ~doc:"STRING NamespaceName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_snapshot_copy_configurations
           (Values.ListSnapshotCopyConfigurationsRequest.make ?maxResults
              ?namespaceName ?nextToken ())
           (Some Values.ListSnapshotCopyConfigurationsResponse.to_json)
           (Some Values.ListSnapshotCopyConfigurationsResponse.error_to_json)])
let list_snapshots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endTime =
         flag "end-time" (optional json_arg) ~doc:"JSON Timestamp"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListSnapshotsRequestMaxResultsInteger"
       and namespaceArn =
         flag "namespace-arn" (optional string) ~doc:"STRING String"
       and namespaceName =
         flag "namespace-name" (optional string) ~doc:"STRING String"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and ownerAccount =
         flag "owner-account" (optional string) ~doc:"STRING String"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_snapshots
           (Values.ListSnapshotsRequest.make
              ?endTime:(Option.map ~f:Values.Timestamp.of_json endTime)
              ?maxResults ?namespaceArn ?namespaceName ?nextToken
              ?ownerAccount
              ?startTime:(Option.map ~f:Values.Timestamp.of_json startTime)
              ()) (Some Values.ListSnapshotsResponse.to_json)
           (Some Values.ListSnapshotsResponse.error_to_json)])
let list_table_restore_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListTableRestoreStatusRequestMaxResultsInteger"
       and namespaceName =
         flag "namespace-name" (optional string) ~doc:"STRING String"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and workgroupName =
         flag "workgroup-name" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_table_restore_status
           (Values.ListTableRestoreStatusRequest.make ?maxResults
              ?namespaceName ?nextToken ?workgroupName ())
           (Some Values.ListTableRestoreStatusResponse.to_json)
           (Some Values.ListTableRestoreStatusResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_tracks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListTracksRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tracks
           (Values.ListTracksRequest.make ?maxResults ?nextToken ())
           (Some Values.ListTracksResponse.to_json)
           (Some Values.ListTracksResponse.error_to_json)])
let list_usage_limits =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListUsageLimitsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and resourceArn =
         flag "resource-arn" (optional string) ~doc:"STRING String"
       and usageType =
         flag "usage-type" (optional json_arg)
           ~doc:"JSON UsageLimitUsageType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_usage_limits
           (Values.ListUsageLimitsRequest.make ?maxResults ?nextToken
              ?resourceArn
              ?usageType:(Option.map ~f:Values.UsageLimitUsageType.of_json
                            usageType) ())
           (Some Values.ListUsageLimitsResponse.to_json)
           (Some Values.ListUsageLimitsResponse.error_to_json)])
let list_workgroups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListWorkgroupsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and ownerAccount =
         flag "owner-account" (optional string) ~doc:"STRING OwnerAccount" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_workgroups
           (Values.ListWorkgroupsRequest.make ?maxResults ?nextToken
              ?ownerAccount ()) (Some Values.ListWorkgroupsResponse.to_json)
           (Some Values.ListWorkgroupsResponse.error_to_json)])
let put_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policy = flag "policy" (required string) ~doc:"STRING String"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_resource_policy
           (Values.PutResourcePolicyRequest.make ~policy ~resourceArn ())
           (Some Values.PutResourcePolicyResponse.to_json)
           (Some Values.PutResourcePolicyResponse.error_to_json)])
let restore_from_recovery_point =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namespaceName =
         flag "namespace-name" (required string) ~doc:"STRING NamespaceName"
       and recoveryPointId =
         flag "recovery-point-id" (required string) ~doc:"STRING String"
       and workgroupName =
         flag "workgroup-name" (required string) ~doc:"STRING WorkgroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_from_recovery_point
           (Values.RestoreFromRecoveryPointRequest.make ~namespaceName
              ~recoveryPointId ~workgroupName ())
           (Some Values.RestoreFromRecoveryPointResponse.to_json)
           (Some Values.RestoreFromRecoveryPointResponse.error_to_json)])
let restore_from_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and adminPasswordSecretKmsKeyId =
         flag "admin-password-secret-kms-key-id" (optional string)
           ~doc:"STRING KmsKeyId"
       and manageAdminPassword =
         flag "manage-admin-password" (optional bool) ~doc:"BOOL Boolean"
       and ownerAccount =
         flag "owner-account" (optional string) ~doc:"STRING String"
       and snapshotArn =
         flag "snapshot-arn" (optional string) ~doc:"STRING String"
       and snapshotName =
         flag "snapshot-name" (optional string) ~doc:"STRING String"
       and namespaceName =
         flag "namespace-name" (required string) ~doc:"STRING NamespaceName"
       and workgroupName =
         flag "workgroup-name" (required string) ~doc:"STRING WorkgroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_from_snapshot
           (Values.RestoreFromSnapshotRequest.make
              ?adminPasswordSecretKmsKeyId ?manageAdminPassword ?ownerAccount
              ?snapshotArn ?snapshotName ~namespaceName ~workgroupName ())
           (Some Values.RestoreFromSnapshotResponse.to_json)
           (Some Values.RestoreFromSnapshotResponse.error_to_json)])
let restore_table_from_recovery_point =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and activateCaseSensitiveIdentifier =
         flag "activate-case-sensitive-identifier" (optional bool)
           ~doc:"BOOL Boolean"
       and sourceSchemaName =
         flag "source-schema-name" (optional string) ~doc:"STRING String"
       and targetDatabaseName =
         flag "target-database-name" (optional string) ~doc:"STRING String"
       and targetSchemaName =
         flag "target-schema-name" (optional string) ~doc:"STRING String"
       and namespaceName =
         flag "namespace-name" (required string) ~doc:"STRING String"
       and newTableName =
         flag "new-table-name" (required string) ~doc:"STRING String"
       and recoveryPointId =
         flag "recovery-point-id" (required string) ~doc:"STRING String"
       and sourceDatabaseName =
         flag "source-database-name" (required string) ~doc:"STRING String"
       and sourceTableName =
         flag "source-table-name" (required string) ~doc:"STRING String"
       and workgroupName =
         flag "workgroup-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_table_from_recovery_point
           (Values.RestoreTableFromRecoveryPointRequest.make
              ?activateCaseSensitiveIdentifier ?sourceSchemaName
              ?targetDatabaseName ?targetSchemaName ~namespaceName
              ~newTableName ~recoveryPointId ~sourceDatabaseName
              ~sourceTableName ~workgroupName ())
           (Some Values.RestoreTableFromRecoveryPointResponse.to_json)
           (Some Values.RestoreTableFromRecoveryPointResponse.error_to_json)])
let restore_table_from_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and activateCaseSensitiveIdentifier =
         flag "activate-case-sensitive-identifier" (optional bool)
           ~doc:"BOOL Boolean"
       and sourceSchemaName =
         flag "source-schema-name" (optional string) ~doc:"STRING String"
       and targetDatabaseName =
         flag "target-database-name" (optional string) ~doc:"STRING String"
       and targetSchemaName =
         flag "target-schema-name" (optional string) ~doc:"STRING String"
       and namespaceName =
         flag "namespace-name" (required string) ~doc:"STRING String"
       and newTableName =
         flag "new-table-name" (required string) ~doc:"STRING String"
       and snapshotName =
         flag "snapshot-name" (required string) ~doc:"STRING String"
       and sourceDatabaseName =
         flag "source-database-name" (required string) ~doc:"STRING String"
       and sourceTableName =
         flag "source-table-name" (required string) ~doc:"STRING String"
       and workgroupName =
         flag "workgroup-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_table_from_snapshot
           (Values.RestoreTableFromSnapshotRequest.make
              ?activateCaseSensitiveIdentifier ?sourceSchemaName
              ?targetDatabaseName ?targetSchemaName ~namespaceName
              ~newTableName ~snapshotName ~sourceDatabaseName
              ~sourceTableName ~workgroupName ())
           (Some Values.RestoreTableFromSnapshotResponse.to_json)
           (Some Values.RestoreTableFromSnapshotResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_custom_domain_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customDomainCertificateArn =
         flag "custom-domain-certificate-arn" (required string)
           ~doc:"STRING CustomDomainCertificateArnString"
       and customDomainName =
         flag "custom-domain-name" (required string)
           ~doc:"STRING CustomDomainName"
       and workgroupName =
         flag "workgroup-name" (required string) ~doc:"STRING WorkgroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_custom_domain_association
           (Values.UpdateCustomDomainAssociationRequest.make
              ~customDomainCertificateArn ~customDomainName ~workgroupName ())
           (Some Values.UpdateCustomDomainAssociationResponse.to_json)
           (Some Values.UpdateCustomDomainAssociationResponse.error_to_json)])
let update_endpoint_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON VpcSecurityGroupIdList"
       and endpointName =
         flag "endpoint-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_endpoint_access
           (Values.UpdateEndpointAccessRequest.make
              ?vpcSecurityGroupIds:(Option.map
                                      ~f:Values.VpcSecurityGroupIdList.of_json
                                      vpcSecurityGroupIds) ~endpointName ())
           (Some Values.UpdateEndpointAccessResponse.to_json)
           (Some Values.UpdateEndpointAccessResponse.error_to_json)])
let update_lakehouse_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogName =
         flag "catalog-name" (optional string)
           ~doc:"STRING CatalogNameString"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and lakehouseIdcApplicationArn =
         flag "lakehouse-idc-application-arn" (optional string)
           ~doc:"STRING String"
       and lakehouseIdcRegistration =
         flag "lakehouse-idc-registration" (optional json_arg)
           ~doc:"JSON LakehouseIdcRegistration"
       and lakehouseRegistration =
         flag "lakehouse-registration" (optional json_arg)
           ~doc:"JSON LakehouseRegistration"
       and namespaceName =
         flag "namespace-name" (required string) ~doc:"STRING NamespaceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_lakehouse_configuration
           (Values.UpdateLakehouseConfigurationRequest.make ?catalogName
              ?dryRun ?lakehouseIdcApplicationArn
              ?lakehouseIdcRegistration:(Option.map
                                           ~f:Values.LakehouseIdcRegistration.of_json
                                           lakehouseIdcRegistration)
              ?lakehouseRegistration:(Option.map
                                        ~f:Values.LakehouseRegistration.of_json
                                        lakehouseRegistration) ~namespaceName
              ()) (Some Values.UpdateLakehouseConfigurationResponse.to_json)
           (Some Values.UpdateLakehouseConfigurationResponse.error_to_json)])
let update_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and adminPasswordSecretKmsKeyId =
         flag "admin-password-secret-kms-key-id" (optional string)
           ~doc:"STRING KmsKeyId"
       and adminUserPassword =
         flag "admin-user-password" (optional string)
           ~doc:"STRING DbPassword"
       and adminUsername =
         flag "admin-username" (optional string) ~doc:"STRING DbUser"
       and defaultIamRoleArn =
         flag "default-iam-role-arn" (optional string) ~doc:"STRING String"
       and iamRoles =
         flag "iam-roles" (optional json_arg) ~doc:"JSON IamRoleArnList"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and logExports =
         flag "log-exports" (optional json_arg) ~doc:"JSON LogExportList"
       and manageAdminPassword =
         flag "manage-admin-password" (optional bool) ~doc:"BOOL Boolean"
       and namespaceName =
         flag "namespace-name" (required string) ~doc:"STRING NamespaceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_namespace
           (Values.UpdateNamespaceRequest.make ?adminPasswordSecretKmsKeyId
              ?adminUserPassword ?adminUsername ?defaultIamRoleArn
              ?iamRoles:(Option.map ~f:Values.IamRoleArnList.of_json iamRoles)
              ?kmsKeyId
              ?logExports:(Option.map ~f:Values.LogExportList.of_json
                             logExports) ?manageAdminPassword ~namespaceName
              ()) (Some Values.UpdateNamespaceResponse.to_json)
           (Some Values.UpdateNamespaceResponse.error_to_json)])
let update_scheduled_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and enabled = flag "enabled" (optional bool) ~doc:"BOOL Boolean"
       and endTime =
         flag "end-time" (optional json_arg) ~doc:"JSON Timestamp"
       and roleArn =
         flag "role-arn" (optional string) ~doc:"STRING IamRoleArn"
       and schedule =
         flag "schedule" (optional json_arg) ~doc:"JSON Schedule"
       and scheduledActionDescription =
         flag "scheduled-action-description" (optional string)
           ~doc:"STRING String"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON Timestamp"
       and targetAction =
         flag "target-action" (optional json_arg) ~doc:"JSON TargetAction"
       and scheduledActionName =
         flag "scheduled-action-name" (required string)
           ~doc:"STRING ScheduledActionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_scheduled_action
           (Values.UpdateScheduledActionRequest.make ?enabled
              ?endTime:(Option.map ~f:Values.Timestamp.of_json endTime)
              ?roleArn
              ?schedule:(Option.map ~f:Values.Schedule.of_json schedule)
              ?scheduledActionDescription
              ?startTime:(Option.map ~f:Values.Timestamp.of_json startTime)
              ?targetAction:(Option.map ~f:Values.TargetAction.of_json
                               targetAction) ~scheduledActionName ())
           (Some Values.UpdateScheduledActionResponse.to_json)
           (Some Values.UpdateScheduledActionResponse.error_to_json)])
let update_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and retentionPeriod =
         flag "retention-period" (optional int) ~doc:"INT Integer"
       and snapshotName =
         flag "snapshot-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_snapshot
           (Values.UpdateSnapshotRequest.make ?retentionPeriod ~snapshotName
              ()) (Some Values.UpdateSnapshotResponse.to_json)
           (Some Values.UpdateSnapshotResponse.error_to_json)])
let update_snapshot_copy_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snapshotRetentionPeriod =
         flag "snapshot-retention-period" (optional int) ~doc:"INT Integer"
       and snapshotCopyConfigurationId =
         flag "snapshot-copy-configuration-id" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_snapshot_copy_configuration
           (Values.UpdateSnapshotCopyConfigurationRequest.make
              ?snapshotRetentionPeriod ~snapshotCopyConfigurationId ())
           (Some Values.UpdateSnapshotCopyConfigurationResponse.to_json)
           (Some Values.UpdateSnapshotCopyConfigurationResponse.error_to_json)])
let update_usage_limit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and amount = flag "amount" (optional json_arg) ~doc:"JSON Long"
       and breachAction =
         flag "breach-action" (optional json_arg)
           ~doc:"JSON UsageLimitBreachAction"
       and usageLimitId =
         flag "usage-limit-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_usage_limit
           (Values.UpdateUsageLimitRequest.make
              ?amount:(Option.map ~f:Values.Long.of_json amount)
              ?breachAction:(Option.map
                               ~f:Values.UsageLimitBreachAction.of_json
                               breachAction) ~usageLimitId ())
           (Some Values.UpdateUsageLimitResponse.to_json)
           (Some Values.UpdateUsageLimitResponse.error_to_json)])
let update_workgroup =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and baseCapacity =
         flag "base-capacity" (optional int) ~doc:"INT Integer"
       and configParameters =
         flag "config-parameters" (optional json_arg)
           ~doc:"JSON ConfigParameterList"
       and enhancedVpcRouting =
         flag "enhanced-vpc-routing" (optional bool) ~doc:"BOOL Boolean"
       and extraComputeForAutomaticOptimization =
         flag "extra-compute-for-automatic-optimization" (optional bool)
           ~doc:"BOOL Boolean"
       and ipAddressType =
         flag "ip-address-type" (optional string) ~doc:"STRING IpAddressType"
       and maxCapacity =
         flag "max-capacity" (optional int) ~doc:"INT Integer"
       and port = flag "port" (optional int) ~doc:"INT Integer"
       and pricePerformanceTarget =
         flag "price-performance-target" (optional json_arg)
           ~doc:"JSON PerformanceTarget"
       and publiclyAccessible =
         flag "publicly-accessible" (optional bool) ~doc:"BOOL Boolean"
       and securityGroupIds =
         flag "security-group-ids" (optional json_arg)
           ~doc:"JSON SecurityGroupIdList"
       and subnetIds =
         flag "subnet-ids" (optional json_arg) ~doc:"JSON SubnetIdList"
       and trackName =
         flag "track-name" (optional string) ~doc:"STRING TrackName"
       and workgroupName =
         flag "workgroup-name" (required string) ~doc:"STRING WorkgroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_workgroup
           (Values.UpdateWorkgroupRequest.make ?baseCapacity
              ?configParameters:(Option.map
                                   ~f:Values.ConfigParameterList.of_json
                                   configParameters) ?enhancedVpcRouting
              ?extraComputeForAutomaticOptimization ?ipAddressType
              ?maxCapacity ?port
              ?pricePerformanceTarget:(Option.map
                                         ~f:Values.PerformanceTarget.of_json
                                         pricePerformanceTarget)
              ?publiclyAccessible
              ?securityGroupIds:(Option.map
                                   ~f:Values.SecurityGroupIdList.of_json
                                   securityGroupIds)
              ?subnetIds:(Option.map ~f:Values.SubnetIdList.of_json subnetIds)
              ?trackName ~workgroupName ())
           (Some Values.UpdateWorkgroupResponse.to_json)
           (Some Values.UpdateWorkgroupResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("convert-recovery-point-to-snapshot",
       convert_recovery_point_to_snapshot);
    ("create-custom-domain-association", create_custom_domain_association);
    ("create-endpoint-access", create_endpoint_access);
    ("create-namespace", create_namespace);
    ("create-reservation", create_reservation);
    ("create-scheduled-action", create_scheduled_action);
    ("create-snapshot", create_snapshot);
    ("create-snapshot-copy-configuration",
      create_snapshot_copy_configuration);
    ("create-usage-limit", create_usage_limit);
    ("create-workgroup", create_workgroup);
    ("delete-custom-domain-association", delete_custom_domain_association);
    ("delete-endpoint-access", delete_endpoint_access);
    ("delete-namespace", delete_namespace);
    ("delete-resource-policy", delete_resource_policy);
    ("delete-scheduled-action", delete_scheduled_action);
    ("delete-snapshot", delete_snapshot);
    ("delete-snapshot-copy-configuration",
      delete_snapshot_copy_configuration);
    ("delete-usage-limit", delete_usage_limit);
    ("delete-workgroup", delete_workgroup);
    ("get-credentials", get_credentials);
    ("get-custom-domain-association", get_custom_domain_association);
    ("get-endpoint-access", get_endpoint_access);
    ("get-identity-center-auth-token", get_identity_center_auth_token);
    ("get-namespace", get_namespace);
    ("get-recovery-point", get_recovery_point);
    ("get-reservation", get_reservation);
    ("get-reservation-offering", get_reservation_offering);
    ("get-resource-policy", get_resource_policy);
    ("get-scheduled-action", get_scheduled_action);
    ("get-snapshot", get_snapshot);
    ("get-table-restore-status", get_table_restore_status);
    ("get-track", get_track);
    ("get-usage-limit", get_usage_limit);
    ("get-workgroup", get_workgroup);
    ("list-custom-domain-associations", list_custom_domain_associations);
    ("list-endpoint-access", list_endpoint_access);
    ("list-managed-workgroups", list_managed_workgroups);
    ("list-namespaces", list_namespaces);
    ("list-recovery-points", list_recovery_points);
    ("list-reservation-offerings", list_reservation_offerings);
    ("list-reservations", list_reservations);
    ("list-scheduled-actions", list_scheduled_actions);
    ("list-snapshot-copy-configurations", list_snapshot_copy_configurations);
    ("list-snapshots", list_snapshots);
    ("list-table-restore-status", list_table_restore_status);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-tracks", list_tracks);
    ("list-usage-limits", list_usage_limits);
    ("list-workgroups", list_workgroups);
    ("put-resource-policy", put_resource_policy);
    ("restore-from-recovery-point", restore_from_recovery_point);
    ("restore-from-snapshot", restore_from_snapshot);
    ("restore-table-from-recovery-point", restore_table_from_recovery_point);
    ("restore-table-from-snapshot", restore_table_from_snapshot);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-custom-domain-association", update_custom_domain_association);
    ("update-endpoint-access", update_endpoint_access);
    ("update-lakehouse-configuration", update_lakehouse_configuration);
    ("update-namespace", update_namespace);
    ("update-scheduled-action", update_scheduled_action);
    ("update-snapshot", update_snapshot);
    ("update-snapshot-copy-configuration",
      update_snapshot_copy_configuration);
    ("update-usage-limit", update_usage_limit);
    ("update-workgroup", update_workgroup)]
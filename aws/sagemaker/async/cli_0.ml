(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and associationType =
         flag "association-type" (optional json_arg)
           ~doc:"JSON AssociationEdgeType"
       and sourceArn =
         flag "source-arn" (required string)
           ~doc:"STRING AssociationEntityArn"
       and destinationArn =
         flag "destination-arn" (required string)
           ~doc:"STRING AssociationEntityArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_association
           (Values.AddAssociationRequest.make
              ?associationType:(Option.map
                                  ~f:Values.AssociationEdgeType.of_json
                                  associationType) ~sourceArn ~destinationArn
              ()) (Some Values.AddAssociationResponse.to_json)
           (Some Values.AddAssociationResponse.error_to_json)])
let add_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_tags
           (Values.AddTagsInput.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.AddTagsOutput.to_json)
           (Some Values.AddTagsOutput.error_to_json)])
let associate_trial_component =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trialComponentName =
         flag "trial-component-name" (required string)
           ~doc:"STRING ExperimentEntityName"
       and trialName =
         flag "trial-name" (required string)
           ~doc:"STRING ExperimentEntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_trial_component
           (Values.AssociateTrialComponentRequest.make ~trialComponentName
              ~trialName ())
           (Some Values.AssociateTrialComponentResponse.to_json)
           (Some Values.AssociateTrialComponentResponse.error_to_json)])
let batch_describe_model_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelPackageArnList =
         flag "model-package-arn-list" (required json_arg)
           ~doc:"JSON ModelPackageArnList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_describe_model_package
           (Values.BatchDescribeModelPackageInput.make
              ~modelPackageArnList:(Values.ModelPackageArnList.of_json
                                      modelPackageArnList) ())
           (Some Values.BatchDescribeModelPackageOutput.to_json)
           (Some Values.BatchDescribeModelPackageOutput.error_to_json)])
let create_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ExperimentDescription"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON ActionStatus"
       and properties =
         flag "properties" (optional json_arg)
           ~doc:"JSON LineageEntityParameters"
       and metadataProperties =
         flag "metadata-properties" (optional json_arg)
           ~doc:"JSON MetadataProperties"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and actionName =
         flag "action-name" (required string)
           ~doc:"STRING ExperimentEntityName"
       and source =
         flag "source" (required json_arg) ~doc:"JSON ActionSource"
       and actionType =
         flag "action-type" (required string) ~doc:"STRING String256" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_action
           (Values.CreateActionRequest.make ?description
              ?status:(Option.map ~f:Values.ActionStatus.of_json status)
              ?properties:(Option.map
                             ~f:Values.LineageEntityParameters.of_json
                             properties)
              ?metadataProperties:(Option.map
                                     ~f:Values.MetadataProperties.of_json
                                     metadataProperties)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~actionName
              ~source:(Values.ActionSource.of_json source) ~actionType ())
           (Some Values.CreateActionResponse.to_json)
           (Some Values.CreateActionResponse.error_to_json)])
let create_algorithm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and algorithmDescription =
         flag "algorithm-description" (optional string)
           ~doc:"STRING EntityDescription"
       and inferenceSpecification =
         flag "inference-specification" (optional json_arg)
           ~doc:"JSON InferenceSpecification"
       and validationSpecification =
         flag "validation-specification" (optional json_arg)
           ~doc:"JSON AlgorithmValidationSpecification"
       and certifyForMarketplace =
         flag "certify-for-marketplace" (optional bool)
           ~doc:"BOOL CertifyForMarketplace"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and algorithmName =
         flag "algorithm-name" (required string) ~doc:"STRING EntityName"
       and trainingSpecification =
         flag "training-specification" (required json_arg)
           ~doc:"JSON TrainingSpecification" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_algorithm
           (Values.CreateAlgorithmInput.make ?algorithmDescription
              ?inferenceSpecification:(Option.map
                                         ~f:Values.InferenceSpecification.of_json
                                         inferenceSpecification)
              ?validationSpecification:(Option.map
                                          ~f:Values.AlgorithmValidationSpecification.of_json
                                          validationSpecification)
              ?certifyForMarketplace
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~algorithmName
              ~trainingSpecification:(Values.TrainingSpecification.of_json
                                        trainingSpecification) ())
           (Some Values.CreateAlgorithmOutput.to_json)
           (Some Values.CreateAlgorithmOutput.error_to_json)])
let create_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and resourceSpec =
         flag "resource-spec" (optional json_arg) ~doc:"JSON ResourceSpec"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and userProfileName =
         flag "user-profile-name" (required string)
           ~doc:"STRING UserProfileName"
       and appType = flag "app-type" (required json_arg) ~doc:"JSON AppType"
       and appName = flag "app-name" (required string) ~doc:"STRING AppName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_app
           (Values.CreateAppRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?resourceSpec:(Option.map ~f:Values.ResourceSpec.of_json
                               resourceSpec) ~domainId ~userProfileName
              ~appType:(Values.AppType.of_json appType) ~appName ())
           (Some Values.CreateAppResponse.to_json)
           (Some Values.CreateAppResponse.error_to_json)])
let create_app_image_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and kernelGatewayImageConfig =
         flag "kernel-gateway-image-config" (optional json_arg)
           ~doc:"JSON KernelGatewayImageConfig"
       and appImageConfigName =
         flag "app-image-config-name" (required string)
           ~doc:"STRING AppImageConfigName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_app_image_config
           (Values.CreateAppImageConfigRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?kernelGatewayImageConfig:(Option.map
                                           ~f:Values.KernelGatewayImageConfig.of_json
                                           kernelGatewayImageConfig)
              ~appImageConfigName ())
           (Some Values.CreateAppImageConfigResponse.to_json)
           (Some Values.CreateAppImageConfigResponse.error_to_json)])
let create_artifact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and artifactName =
         flag "artifact-name" (optional string)
           ~doc:"STRING ExperimentEntityName"
       and properties =
         flag "properties" (optional json_arg)
           ~doc:"JSON LineageEntityParameters"
       and metadataProperties =
         flag "metadata-properties" (optional json_arg)
           ~doc:"JSON MetadataProperties"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and source =
         flag "source" (required json_arg) ~doc:"JSON ArtifactSource"
       and artifactType =
         flag "artifact-type" (required string) ~doc:"STRING String256" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_artifact
           (Values.CreateArtifactRequest.make ?artifactName
              ?properties:(Option.map
                             ~f:Values.LineageEntityParameters.of_json
                             properties)
              ?metadataProperties:(Option.map
                                     ~f:Values.MetadataProperties.of_json
                                     metadataProperties)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~source:(Values.ArtifactSource.of_json source) ~artifactType ())
           (Some Values.CreateArtifactResponse.to_json)
           (Some Values.CreateArtifactResponse.error_to_json)])
let create_auto_m_l_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and problemType =
         flag "problem-type" (optional json_arg) ~doc:"JSON ProblemType"
       and autoMLJobObjective =
         flag "auto-m-l-job-objective" (optional json_arg)
           ~doc:"JSON AutoMLJobObjective"
       and autoMLJobConfig =
         flag "auto-m-l-job-config" (optional json_arg)
           ~doc:"JSON AutoMLJobConfig"
       and generateCandidateDefinitionsOnly =
         flag "generate-candidate-definitions-only" (optional bool)
           ~doc:"BOOL GenerateCandidateDefinitionsOnly"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and modelDeployConfig =
         flag "model-deploy-config" (optional json_arg)
           ~doc:"JSON ModelDeployConfig"
       and autoMLJobName =
         flag "auto-m-l-job-name" (required string)
           ~doc:"STRING AutoMLJobName"
       and inputDataConfig =
         flag "input-data-config" (required json_arg)
           ~doc:"JSON AutoMLInputDataConfig"
       and outputDataConfig =
         flag "output-data-config" (required json_arg)
           ~doc:"JSON AutoMLOutputDataConfig"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_auto_m_l_job
           (Values.CreateAutoMLJobRequest.make
              ?problemType:(Option.map ~f:Values.ProblemType.of_json
                              problemType)
              ?autoMLJobObjective:(Option.map
                                     ~f:Values.AutoMLJobObjective.of_json
                                     autoMLJobObjective)
              ?autoMLJobConfig:(Option.map ~f:Values.AutoMLJobConfig.of_json
                                  autoMLJobConfig)
              ?generateCandidateDefinitionsOnly
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?modelDeployConfig:(Option.map
                                    ~f:Values.ModelDeployConfig.of_json
                                    modelDeployConfig) ~autoMLJobName
              ~inputDataConfig:(Values.AutoMLInputDataConfig.of_json
                                  inputDataConfig)
              ~outputDataConfig:(Values.AutoMLOutputDataConfig.of_json
                                   outputDataConfig) ~roleArn ())
           (Some Values.CreateAutoMLJobResponse.to_json)
           (Some Values.CreateAutoMLJobResponse.error_to_json)])
let create_code_repository =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and codeRepositoryName =
         flag "code-repository-name" (required string)
           ~doc:"STRING EntityName"
       and gitConfig =
         flag "git-config" (required json_arg) ~doc:"JSON GitConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_code_repository
           (Values.CreateCodeRepositoryInput.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~codeRepositoryName
              ~gitConfig:(Values.GitConfig.of_json gitConfig) ())
           (Some Values.CreateCodeRepositoryOutput.to_json)
           (Some Values.CreateCodeRepositoryOutput.error_to_json)])
let create_compilation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelPackageVersionArn =
         flag "model-package-version-arn" (optional string)
           ~doc:"STRING ModelPackageArn"
       and inputConfig =
         flag "input-config" (optional json_arg) ~doc:"JSON InputConfig"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON NeoVpcConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and compilationJobName =
         flag "compilation-job-name" (required string)
           ~doc:"STRING EntityName"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn"
       and outputConfig =
         flag "output-config" (required json_arg) ~doc:"JSON OutputConfig"
       and stoppingCondition =
         flag "stopping-condition" (required json_arg)
           ~doc:"JSON StoppingCondition" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_compilation_job
           (Values.CreateCompilationJobRequest.make ?modelPackageVersionArn
              ?inputConfig:(Option.map ~f:Values.InputConfig.of_json
                              inputConfig)
              ?vpcConfig:(Option.map ~f:Values.NeoVpcConfig.of_json vpcConfig)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~compilationJobName ~roleArn
              ~outputConfig:(Values.OutputConfig.of_json outputConfig)
              ~stoppingCondition:(Values.StoppingCondition.of_json
                                    stoppingCondition) ())
           (Some Values.CreateCompilationJobResponse.to_json)
           (Some Values.CreateCompilationJobResponse.error_to_json)])
let create_context =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ExperimentDescription"
       and properties =
         flag "properties" (optional json_arg)
           ~doc:"JSON LineageEntityParameters"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and contextName =
         flag "context-name" (required string)
           ~doc:"STRING ExperimentEntityName"
       and source =
         flag "source" (required json_arg) ~doc:"JSON ContextSource"
       and contextType =
         flag "context-type" (required string) ~doc:"STRING String256" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_context
           (Values.CreateContextRequest.make ?description
              ?properties:(Option.map
                             ~f:Values.LineageEntityParameters.of_json
                             properties)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~contextName
              ~source:(Values.ContextSource.of_json source) ~contextType ())
           (Some Values.CreateContextResponse.to_json)
           (Some Values.CreateContextResponse.error_to_json)])
let create_data_quality_job_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataQualityBaselineConfig =
         flag "data-quality-baseline-config" (optional json_arg)
           ~doc:"JSON DataQualityBaselineConfig"
       and networkConfig =
         flag "network-config" (optional json_arg)
           ~doc:"JSON MonitoringNetworkConfig"
       and stoppingCondition =
         flag "stopping-condition" (optional json_arg)
           ~doc:"JSON MonitoringStoppingCondition"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and jobDefinitionName =
         flag "job-definition-name" (required string)
           ~doc:"STRING MonitoringJobDefinitionName"
       and dataQualityAppSpecification =
         flag "data-quality-app-specification" (required json_arg)
           ~doc:"JSON DataQualityAppSpecification"
       and dataQualityJobInput =
         flag "data-quality-job-input" (required json_arg)
           ~doc:"JSON DataQualityJobInput"
       and dataQualityJobOutputConfig =
         flag "data-quality-job-output-config" (required json_arg)
           ~doc:"JSON MonitoringOutputConfig"
       and jobResources =
         flag "job-resources" (required json_arg)
           ~doc:"JSON MonitoringResources"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_quality_job_definition
           (Values.CreateDataQualityJobDefinitionRequest.make
              ?dataQualityBaselineConfig:(Option.map
                                            ~f:Values.DataQualityBaselineConfig.of_json
                                            dataQualityBaselineConfig)
              ?networkConfig:(Option.map
                                ~f:Values.MonitoringNetworkConfig.of_json
                                networkConfig)
              ?stoppingCondition:(Option.map
                                    ~f:Values.MonitoringStoppingCondition.of_json
                                    stoppingCondition)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~jobDefinitionName
              ~dataQualityAppSpecification:(Values.DataQualityAppSpecification.of_json
                                              dataQualityAppSpecification)
              ~dataQualityJobInput:(Values.DataQualityJobInput.of_json
                                      dataQualityJobInput)
              ~dataQualityJobOutputConfig:(Values.MonitoringOutputConfig.of_json
                                             dataQualityJobOutputConfig)
              ~jobResources:(Values.MonitoringResources.of_json jobResources)
              ~roleArn ())
           (Some Values.CreateDataQualityJobDefinitionResponse.to_json)
           (Some Values.CreateDataQualityJobDefinitionResponse.error_to_json)])
let create_device_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and description =
         flag "description" (optional string)
           ~doc:"STRING DeviceFleetDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and enableIotRoleAlias =
         flag "enable-iot-role-alias" (optional bool)
           ~doc:"BOOL EnableIotRoleAlias"
       and deviceFleetName =
         flag "device-fleet-name" (required string) ~doc:"STRING EntityName"
       and outputConfig =
         flag "output-config" (required json_arg)
           ~doc:"JSON EdgeOutputConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_device_fleet
           (Values.CreateDeviceFleetRequest.make ?roleArn ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?enableIotRoleAlias ~deviceFleetName
              ~outputConfig:(Values.EdgeOutputConfig.of_json outputConfig) ())
           None None])
let create_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and appNetworkAccessType =
         flag "app-network-access-type" (optional json_arg)
           ~doc:"JSON AppNetworkAccessType"
       and homeEfsFileSystemKmsKeyId =
         flag "home-efs-file-system-kms-key-id" (optional string)
           ~doc:"STRING KmsKeyId"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and appSecurityGroupManagement =
         flag "app-security-group-management" (optional json_arg)
           ~doc:"JSON AppSecurityGroupManagement"
       and domainSettings =
         flag "domain-settings" (optional json_arg)
           ~doc:"JSON DomainSettings"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and authMode =
         flag "auth-mode" (required json_arg) ~doc:"JSON AuthMode"
       and defaultUserSettings =
         flag "default-user-settings" (required json_arg)
           ~doc:"JSON UserSettings"
       and subnetIds =
         flag "subnet-ids" (required json_arg) ~doc:"JSON Subnets"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_domain
           (Values.CreateDomainRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?appNetworkAccessType:(Option.map
                                       ~f:Values.AppNetworkAccessType.of_json
                                       appNetworkAccessType)
              ?homeEfsFileSystemKmsKeyId ?kmsKeyId
              ?appSecurityGroupManagement:(Option.map
                                             ~f:Values.AppSecurityGroupManagement.of_json
                                             appSecurityGroupManagement)
              ?domainSettings:(Option.map ~f:Values.DomainSettings.of_json
                                 domainSettings) ~domainName
              ~authMode:(Values.AuthMode.of_json authMode)
              ~defaultUserSettings:(Values.UserSettings.of_json
                                      defaultUserSettings)
              ~subnetIds:(Values.Subnets.of_json subnetIds) ~vpcId ())
           (Some Values.CreateDomainResponse.to_json)
           (Some Values.CreateDomainResponse.error_to_json)])
let create_edge_packaging_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceKey =
         flag "resource-key" (optional string) ~doc:"STRING KmsKeyId"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and edgePackagingJobName =
         flag "edge-packaging-job-name" (required string)
           ~doc:"STRING EntityName"
       and compilationJobName =
         flag "compilation-job-name" (required string)
           ~doc:"STRING EntityName"
       and modelName =
         flag "model-name" (required string) ~doc:"STRING EntityName"
       and modelVersion =
         flag "model-version" (required string) ~doc:"STRING EdgeVersion"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn"
       and outputConfig =
         flag "output-config" (required json_arg)
           ~doc:"JSON EdgeOutputConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_edge_packaging_job
           (Values.CreateEdgePackagingJobRequest.make ?resourceKey
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~edgePackagingJobName ~compilationJobName ~modelName
              ~modelVersion ~roleArn
              ~outputConfig:(Values.EdgeOutputConfig.of_json outputConfig) ())
           None None])
let create_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deploymentConfig =
         flag "deployment-config" (optional json_arg)
           ~doc:"JSON DeploymentConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and endpointName =
         flag "endpoint-name" (required string) ~doc:"STRING EndpointName"
       and endpointConfigName =
         flag "endpoint-config-name" (required string)
           ~doc:"STRING EndpointConfigName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_endpoint
           (Values.CreateEndpointInput.make
              ?deploymentConfig:(Option.map
                                   ~f:Values.DeploymentConfig.of_json
                                   deploymentConfig)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~endpointName
              ~endpointConfigName ())
           (Some Values.CreateEndpointOutput.to_json)
           (Some Values.CreateEndpointOutput.error_to_json)])
let create_endpoint_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataCaptureConfig =
         flag "data-capture-config" (optional json_arg)
           ~doc:"JSON DataCaptureConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and asyncInferenceConfig =
         flag "async-inference-config" (optional json_arg)
           ~doc:"JSON AsyncInferenceConfig"
       and endpointConfigName =
         flag "endpoint-config-name" (required string)
           ~doc:"STRING EndpointConfigName"
       and productionVariants =
         flag "production-variants" (required json_arg)
           ~doc:"JSON ProductionVariantList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_endpoint_config
           (Values.CreateEndpointConfigInput.make
              ?dataCaptureConfig:(Option.map
                                    ~f:Values.DataCaptureConfig.of_json
                                    dataCaptureConfig)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?kmsKeyId
              ?asyncInferenceConfig:(Option.map
                                       ~f:Values.AsyncInferenceConfig.of_json
                                       asyncInferenceConfig)
              ~endpointConfigName
              ~productionVariants:(Values.ProductionVariantList.of_json
                                     productionVariants) ())
           (Some Values.CreateEndpointConfigOutput.to_json)
           (Some Values.CreateEndpointConfigOutput.error_to_json)])
let create_experiment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayName =
         flag "display-name" (optional string)
           ~doc:"STRING ExperimentEntityName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ExperimentDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and experimentName =
         flag "experiment-name" (required string)
           ~doc:"STRING ExperimentEntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_experiment
           (Values.CreateExperimentRequest.make ?displayName ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~experimentName ())
           (Some Values.CreateExperimentResponse.to_json)
           (Some Values.CreateExperimentResponse.error_to_json)])
let create_feature_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and onlineStoreConfig =
         flag "online-store-config" (optional json_arg)
           ~doc:"JSON OnlineStoreConfig"
       and offlineStoreConfig =
         flag "offline-store-config" (optional json_arg)
           ~doc:"JSON OfflineStoreConfig"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and featureGroupName =
         flag "feature-group-name" (required string)
           ~doc:"STRING FeatureGroupName"
       and recordIdentifierFeatureName =
         flag "record-identifier-feature-name" (required string)
           ~doc:"STRING FeatureName"
       and eventTimeFeatureName =
         flag "event-time-feature-name" (required string)
           ~doc:"STRING FeatureName"
       and featureDefinitions =
         flag "feature-definitions" (required json_arg)
           ~doc:"JSON FeatureDefinitions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_feature_group
           (Values.CreateFeatureGroupRequest.make
              ?onlineStoreConfig:(Option.map
                                    ~f:Values.OnlineStoreConfig.of_json
                                    onlineStoreConfig)
              ?offlineStoreConfig:(Option.map
                                     ~f:Values.OfflineStoreConfig.of_json
                                     offlineStoreConfig) ?roleArn
              ?description ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~featureGroupName ~recordIdentifierFeatureName
              ~eventTimeFeatureName
              ~featureDefinitions:(Values.FeatureDefinitions.of_json
                                     featureDefinitions) ())
           (Some Values.CreateFeatureGroupResponse.to_json)
           (Some Values.CreateFeatureGroupResponse.error_to_json)])
let create_flow_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and humanLoopRequestSource =
         flag "human-loop-request-source" (optional json_arg)
           ~doc:"JSON HumanLoopRequestSource"
       and humanLoopActivationConfig =
         flag "human-loop-activation-config" (optional json_arg)
           ~doc:"JSON HumanLoopActivationConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and flowDefinitionName =
         flag "flow-definition-name" (required string)
           ~doc:"STRING FlowDefinitionName"
       and humanLoopConfig =
         flag "human-loop-config" (required json_arg)
           ~doc:"JSON HumanLoopConfig"
       and outputConfig =
         flag "output-config" (required json_arg)
           ~doc:"JSON FlowDefinitionOutputConfig"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_flow_definition
           (Values.CreateFlowDefinitionRequest.make
              ?humanLoopRequestSource:(Option.map
                                         ~f:Values.HumanLoopRequestSource.of_json
                                         humanLoopRequestSource)
              ?humanLoopActivationConfig:(Option.map
                                            ~f:Values.HumanLoopActivationConfig.of_json
                                            humanLoopActivationConfig)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~flowDefinitionName
              ~humanLoopConfig:(Values.HumanLoopConfig.of_json
                                  humanLoopConfig)
              ~outputConfig:(Values.FlowDefinitionOutputConfig.of_json
                               outputConfig) ~roleArn ())
           (Some Values.CreateFlowDefinitionResponse.to_json)
           (Some Values.CreateFlowDefinitionResponse.error_to_json)])
let create_human_task_ui =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and humanTaskUiName =
         flag "human-task-ui-name" (required string)
           ~doc:"STRING HumanTaskUiName"
       and uiTemplate =
         flag "ui-template" (required json_arg) ~doc:"JSON UiTemplate" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_human_task_ui
           (Values.CreateHumanTaskUiRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~humanTaskUiName
              ~uiTemplate:(Values.UiTemplate.of_json uiTemplate) ())
           (Some Values.CreateHumanTaskUiResponse.to_json)
           (Some Values.CreateHumanTaskUiResponse.error_to_json)])
let create_hyper_parameter_tuning_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trainingJobDefinition =
         flag "training-job-definition" (optional json_arg)
           ~doc:"JSON HyperParameterTrainingJobDefinition"
       and trainingJobDefinitions =
         flag "training-job-definitions" (optional json_arg)
           ~doc:"JSON HyperParameterTrainingJobDefinitions"
       and warmStartConfig =
         flag "warm-start-config" (optional json_arg)
           ~doc:"JSON HyperParameterTuningJobWarmStartConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and hyperParameterTuningJobName =
         flag "hyper-parameter-tuning-job-name" (required string)
           ~doc:"STRING HyperParameterTuningJobName"
       and hyperParameterTuningJobConfig =
         flag "hyper-parameter-tuning-job-config" (required json_arg)
           ~doc:"JSON HyperParameterTuningJobConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_hyper_parameter_tuning_job
           (Values.CreateHyperParameterTuningJobRequest.make
              ?trainingJobDefinition:(Option.map
                                        ~f:Values.HyperParameterTrainingJobDefinition.of_json
                                        trainingJobDefinition)
              ?trainingJobDefinitions:(Option.map
                                         ~f:Values.HyperParameterTrainingJobDefinitions.of_json
                                         trainingJobDefinitions)
              ?warmStartConfig:(Option.map
                                  ~f:Values.HyperParameterTuningJobWarmStartConfig.of_json
                                  warmStartConfig)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~hyperParameterTuningJobName
              ~hyperParameterTuningJobConfig:(Values.HyperParameterTuningJobConfig.of_json
                                                hyperParameterTuningJobConfig)
              ()) (Some Values.CreateHyperParameterTuningJobResponse.to_json)
           (Some Values.CreateHyperParameterTuningJobResponse.error_to_json)])
let create_image =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING ImageDescription"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING ImageDisplayName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and imageName =
         flag "image-name" (required string) ~doc:"STRING ImageName"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_image
           (Values.CreateImageRequest.make ?description ?displayName
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~imageName
              ~roleArn ()) (Some Values.CreateImageResponse.to_json)
           (Some Values.CreateImageResponse.error_to_json)])
let create_image_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and baseImage =
         flag "base-image" (required string) ~doc:"STRING ImageBaseImage"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken"
       and imageName =
         flag "image-name" (required string) ~doc:"STRING ImageName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_image_version
           (Values.CreateImageVersionRequest.make ~baseImage ~clientToken
              ~imageName ()) (Some Values.CreateImageVersionResponse.to_json)
           (Some Values.CreateImageVersionResponse.error_to_json)])
let create_inference_recommendations_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobDescription =
         flag "job-description" (optional string)
           ~doc:"STRING RecommendationJobDescription"
       and stoppingConditions =
         flag "stopping-conditions" (optional json_arg)
           ~doc:"JSON RecommendationJobStoppingConditions"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and jobName =
         flag "job-name" (required string)
           ~doc:"STRING RecommendationJobName"
       and jobType =
         flag "job-type" (required json_arg)
           ~doc:"JSON RecommendationJobType"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn"
       and inputConfig =
         flag "input-config" (required json_arg)
           ~doc:"JSON RecommendationJobInputConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_inference_recommendations_job
           (Values.CreateInferenceRecommendationsJobRequest.make
              ?jobDescription
              ?stoppingConditions:(Option.map
                                     ~f:Values.RecommendationJobStoppingConditions.of_json
                                     stoppingConditions)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~jobName
              ~jobType:(Values.RecommendationJobType.of_json jobType)
              ~roleArn
              ~inputConfig:(Values.RecommendationJobInputConfig.of_json
                              inputConfig) ())
           (Some Values.CreateInferenceRecommendationsJobResponse.to_json)
           (Some
              Values.CreateInferenceRecommendationsJobResponse.error_to_json)])
let create_labeling_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and labelCategoryConfigS3Uri =
         flag "label-category-config-s3-uri" (optional string)
           ~doc:"STRING S3Uri"
       and stoppingConditions =
         flag "stopping-conditions" (optional json_arg)
           ~doc:"JSON LabelingJobStoppingConditions"
       and labelingJobAlgorithmsConfig =
         flag "labeling-job-algorithms-config" (optional json_arg)
           ~doc:"JSON LabelingJobAlgorithmsConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and labelingJobName =
         flag "labeling-job-name" (required string)
           ~doc:"STRING LabelingJobName"
       and labelAttributeName =
         flag "label-attribute-name" (required string)
           ~doc:"STRING LabelAttributeName"
       and inputConfig =
         flag "input-config" (required json_arg)
           ~doc:"JSON LabelingJobInputConfig"
       and outputConfig =
         flag "output-config" (required json_arg)
           ~doc:"JSON LabelingJobOutputConfig"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn"
       and humanTaskConfig =
         flag "human-task-config" (required json_arg)
           ~doc:"JSON HumanTaskConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_labeling_job
           (Values.CreateLabelingJobRequest.make ?labelCategoryConfigS3Uri
              ?stoppingConditions:(Option.map
                                     ~f:Values.LabelingJobStoppingConditions.of_json
                                     stoppingConditions)
              ?labelingJobAlgorithmsConfig:(Option.map
                                              ~f:Values.LabelingJobAlgorithmsConfig.of_json
                                              labelingJobAlgorithmsConfig)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~labelingJobName ~labelAttributeName
              ~inputConfig:(Values.LabelingJobInputConfig.of_json inputConfig)
              ~outputConfig:(Values.LabelingJobOutputConfig.of_json
                               outputConfig) ~roleArn
              ~humanTaskConfig:(Values.HumanTaskConfig.of_json
                                  humanTaskConfig) ())
           (Some Values.CreateLabelingJobResponse.to_json)
           (Some Values.CreateLabelingJobResponse.error_to_json)])
let create_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and primaryContainer =
         flag "primary-container" (optional json_arg)
           ~doc:"JSON ContainerDefinition"
       and containers =
         flag "containers" (optional json_arg)
           ~doc:"JSON ContainerDefinitionList"
       and inferenceExecutionConfig =
         flag "inference-execution-config" (optional json_arg)
           ~doc:"JSON InferenceExecutionConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON VpcConfig"
       and enableNetworkIsolation =
         flag "enable-network-isolation" (optional bool) ~doc:"BOOL Boolean"
       and modelName =
         flag "model-name" (required string) ~doc:"STRING ModelName"
       and executionRoleArn =
         flag "execution-role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_model
           (Values.CreateModelInput.make
              ?primaryContainer:(Option.map
                                   ~f:Values.ContainerDefinition.of_json
                                   primaryContainer)
              ?containers:(Option.map
                             ~f:Values.ContainerDefinitionList.of_json
                             containers)
              ?inferenceExecutionConfig:(Option.map
                                           ~f:Values.InferenceExecutionConfig.of_json
                                           inferenceExecutionConfig)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?vpcConfig:(Option.map ~f:Values.VpcConfig.of_json vpcConfig)
              ?enableNetworkIsolation ~modelName ~executionRoleArn ())
           (Some Values.CreateModelOutput.to_json)
           (Some Values.CreateModelOutput.error_to_json)])
let create_model_bias_job_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelBiasBaselineConfig =
         flag "model-bias-baseline-config" (optional json_arg)
           ~doc:"JSON ModelBiasBaselineConfig"
       and networkConfig =
         flag "network-config" (optional json_arg)
           ~doc:"JSON MonitoringNetworkConfig"
       and stoppingCondition =
         flag "stopping-condition" (optional json_arg)
           ~doc:"JSON MonitoringStoppingCondition"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and jobDefinitionName =
         flag "job-definition-name" (required string)
           ~doc:"STRING MonitoringJobDefinitionName"
       and modelBiasAppSpecification =
         flag "model-bias-app-specification" (required json_arg)
           ~doc:"JSON ModelBiasAppSpecification"
       and modelBiasJobInput =
         flag "model-bias-job-input" (required json_arg)
           ~doc:"JSON ModelBiasJobInput"
       and modelBiasJobOutputConfig =
         flag "model-bias-job-output-config" (required json_arg)
           ~doc:"JSON MonitoringOutputConfig"
       and jobResources =
         flag "job-resources" (required json_arg)
           ~doc:"JSON MonitoringResources"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_model_bias_job_definition
           (Values.CreateModelBiasJobDefinitionRequest.make
              ?modelBiasBaselineConfig:(Option.map
                                          ~f:Values.ModelBiasBaselineConfig.of_json
                                          modelBiasBaselineConfig)
              ?networkConfig:(Option.map
                                ~f:Values.MonitoringNetworkConfig.of_json
                                networkConfig)
              ?stoppingCondition:(Option.map
                                    ~f:Values.MonitoringStoppingCondition.of_json
                                    stoppingCondition)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~jobDefinitionName
              ~modelBiasAppSpecification:(Values.ModelBiasAppSpecification.of_json
                                            modelBiasAppSpecification)
              ~modelBiasJobInput:(Values.ModelBiasJobInput.of_json
                                    modelBiasJobInput)
              ~modelBiasJobOutputConfig:(Values.MonitoringOutputConfig.of_json
                                           modelBiasJobOutputConfig)
              ~jobResources:(Values.MonitoringResources.of_json jobResources)
              ~roleArn ())
           (Some Values.CreateModelBiasJobDefinitionResponse.to_json)
           (Some Values.CreateModelBiasJobDefinitionResponse.error_to_json)])
let create_model_explainability_job_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelExplainabilityBaselineConfig =
         flag "model-explainability-baseline-config" (optional json_arg)
           ~doc:"JSON ModelExplainabilityBaselineConfig"
       and networkConfig =
         flag "network-config" (optional json_arg)
           ~doc:"JSON MonitoringNetworkConfig"
       and stoppingCondition =
         flag "stopping-condition" (optional json_arg)
           ~doc:"JSON MonitoringStoppingCondition"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and jobDefinitionName =
         flag "job-definition-name" (required string)
           ~doc:"STRING MonitoringJobDefinitionName"
       and modelExplainabilityAppSpecification =
         flag "model-explainability-app-specification" (required json_arg)
           ~doc:"JSON ModelExplainabilityAppSpecification"
       and modelExplainabilityJobInput =
         flag "model-explainability-job-input" (required json_arg)
           ~doc:"JSON ModelExplainabilityJobInput"
       and modelExplainabilityJobOutputConfig =
         flag "model-explainability-job-output-config" (required json_arg)
           ~doc:"JSON MonitoringOutputConfig"
       and jobResources =
         flag "job-resources" (required json_arg)
           ~doc:"JSON MonitoringResources"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_model_explainability_job_definition
           (Values.CreateModelExplainabilityJobDefinitionRequest.make
              ?modelExplainabilityBaselineConfig:(Option.map
                                                    ~f:Values.ModelExplainabilityBaselineConfig.of_json
                                                    modelExplainabilityBaselineConfig)
              ?networkConfig:(Option.map
                                ~f:Values.MonitoringNetworkConfig.of_json
                                networkConfig)
              ?stoppingCondition:(Option.map
                                    ~f:Values.MonitoringStoppingCondition.of_json
                                    stoppingCondition)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~jobDefinitionName
              ~modelExplainabilityAppSpecification:(Values.ModelExplainabilityAppSpecification.of_json
                                                      modelExplainabilityAppSpecification)
              ~modelExplainabilityJobInput:(Values.ModelExplainabilityJobInput.of_json
                                              modelExplainabilityJobInput)
              ~modelExplainabilityJobOutputConfig:(Values.MonitoringOutputConfig.of_json
                                                     modelExplainabilityJobOutputConfig)
              ~jobResources:(Values.MonitoringResources.of_json jobResources)
              ~roleArn ())
           (Some
              Values.CreateModelExplainabilityJobDefinitionResponse.to_json)
           (Some
              Values.CreateModelExplainabilityJobDefinitionResponse.error_to_json)])
let create_model_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelPackageName =
         flag "model-package-name" (optional string) ~doc:"STRING EntityName"
       and modelPackageGroupName =
         flag "model-package-group-name" (optional string)
           ~doc:"STRING ArnOrName"
       and modelPackageDescription =
         flag "model-package-description" (optional string)
           ~doc:"STRING EntityDescription"
       and inferenceSpecification =
         flag "inference-specification" (optional json_arg)
           ~doc:"JSON InferenceSpecification"
       and validationSpecification =
         flag "validation-specification" (optional json_arg)
           ~doc:"JSON ModelPackageValidationSpecification"
       and sourceAlgorithmSpecification =
         flag "source-algorithm-specification" (optional json_arg)
           ~doc:"JSON SourceAlgorithmSpecification"
       and certifyForMarketplace =
         flag "certify-for-marketplace" (optional bool)
           ~doc:"BOOL CertifyForMarketplace"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and modelApprovalStatus =
         flag "model-approval-status" (optional json_arg)
           ~doc:"JSON ModelApprovalStatus"
       and metadataProperties =
         flag "metadata-properties" (optional json_arg)
           ~doc:"JSON MetadataProperties"
       and modelMetrics =
         flag "model-metrics" (optional json_arg) ~doc:"JSON ModelMetrics"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and customerMetadataProperties =
         flag "customer-metadata-properties" (optional json_arg)
           ~doc:"JSON CustomerMetadataMap"
       and driftCheckBaselines =
         flag "drift-check-baselines" (optional json_arg)
           ~doc:"JSON DriftCheckBaselines"
       and domain = flag "domain" (optional string) ~doc:"STRING String"
       and task = flag "task" (optional string) ~doc:"STRING String"
       and samplePayloadUrl =
         flag "sample-payload-url" (optional string) ~doc:"STRING S3Uri"
       and additionalInferenceSpecifications =
         flag "additional-inference-specifications" (optional json_arg)
           ~doc:"JSON AdditionalInferenceSpecifications" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_model_package
           (Values.CreateModelPackageInput.make ?modelPackageName
              ?modelPackageGroupName ?modelPackageDescription
              ?inferenceSpecification:(Option.map
                                         ~f:Values.InferenceSpecification.of_json
                                         inferenceSpecification)
              ?validationSpecification:(Option.map
                                          ~f:Values.ModelPackageValidationSpecification.of_json
                                          validationSpecification)
              ?sourceAlgorithmSpecification:(Option.map
                                               ~f:Values.SourceAlgorithmSpecification.of_json
                                               sourceAlgorithmSpecification)
              ?certifyForMarketplace
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?modelApprovalStatus:(Option.map
                                      ~f:Values.ModelApprovalStatus.of_json
                                      modelApprovalStatus)
              ?metadataProperties:(Option.map
                                     ~f:Values.MetadataProperties.of_json
                                     metadataProperties)
              ?modelMetrics:(Option.map ~f:Values.ModelMetrics.of_json
                               modelMetrics) ?clientToken
              ?customerMetadataProperties:(Option.map
                                             ~f:Values.CustomerMetadataMap.of_json
                                             customerMetadataProperties)
              ?driftCheckBaselines:(Option.map
                                      ~f:Values.DriftCheckBaselines.of_json
                                      driftCheckBaselines) ?domain ?task
              ?samplePayloadUrl
              ?additionalInferenceSpecifications:(Option.map
                                                    ~f:Values.AdditionalInferenceSpecifications.of_json
                                                    additionalInferenceSpecifications)
              ()) (Some Values.CreateModelPackageOutput.to_json)
           (Some Values.CreateModelPackageOutput.error_to_json)])
let create_model_package_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelPackageGroupDescription =
         flag "model-package-group-description" (optional string)
           ~doc:"STRING EntityDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and modelPackageGroupName =
         flag "model-package-group-name" (required string)
           ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_model_package_group
           (Values.CreateModelPackageGroupInput.make
              ?modelPackageGroupDescription
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~modelPackageGroupName ())
           (Some Values.CreateModelPackageGroupOutput.to_json)
           (Some Values.CreateModelPackageGroupOutput.error_to_json)])
let create_model_quality_job_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelQualityBaselineConfig =
         flag "model-quality-baseline-config" (optional json_arg)
           ~doc:"JSON ModelQualityBaselineConfig"
       and networkConfig =
         flag "network-config" (optional json_arg)
           ~doc:"JSON MonitoringNetworkConfig"
       and stoppingCondition =
         flag "stopping-condition" (optional json_arg)
           ~doc:"JSON MonitoringStoppingCondition"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and jobDefinitionName =
         flag "job-definition-name" (required string)
           ~doc:"STRING MonitoringJobDefinitionName"
       and modelQualityAppSpecification =
         flag "model-quality-app-specification" (required json_arg)
           ~doc:"JSON ModelQualityAppSpecification"
       and modelQualityJobInput =
         flag "model-quality-job-input" (required json_arg)
           ~doc:"JSON ModelQualityJobInput"
       and modelQualityJobOutputConfig =
         flag "model-quality-job-output-config" (required json_arg)
           ~doc:"JSON MonitoringOutputConfig"
       and jobResources =
         flag "job-resources" (required json_arg)
           ~doc:"JSON MonitoringResources"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_model_quality_job_definition
           (Values.CreateModelQualityJobDefinitionRequest.make
              ?modelQualityBaselineConfig:(Option.map
                                             ~f:Values.ModelQualityBaselineConfig.of_json
                                             modelQualityBaselineConfig)
              ?networkConfig:(Option.map
                                ~f:Values.MonitoringNetworkConfig.of_json
                                networkConfig)
              ?stoppingCondition:(Option.map
                                    ~f:Values.MonitoringStoppingCondition.of_json
                                    stoppingCondition)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~jobDefinitionName
              ~modelQualityAppSpecification:(Values.ModelQualityAppSpecification.of_json
                                               modelQualityAppSpecification)
              ~modelQualityJobInput:(Values.ModelQualityJobInput.of_json
                                       modelQualityJobInput)
              ~modelQualityJobOutputConfig:(Values.MonitoringOutputConfig.of_json
                                              modelQualityJobOutputConfig)
              ~jobResources:(Values.MonitoringResources.of_json jobResources)
              ~roleArn ())
           (Some Values.CreateModelQualityJobDefinitionResponse.to_json)
           (Some Values.CreateModelQualityJobDefinitionResponse.error_to_json)])
let create_monitoring_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and monitoringScheduleName =
         flag "monitoring-schedule-name" (required string)
           ~doc:"STRING MonitoringScheduleName"
       and monitoringScheduleConfig =
         flag "monitoring-schedule-config" (required json_arg)
           ~doc:"JSON MonitoringScheduleConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_monitoring_schedule
           (Values.CreateMonitoringScheduleRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~monitoringScheduleName
              ~monitoringScheduleConfig:(Values.MonitoringScheduleConfig.of_json
                                           monitoringScheduleConfig) ())
           (Some Values.CreateMonitoringScheduleResponse.to_json)
           (Some Values.CreateMonitoringScheduleResponse.error_to_json)])
let create_notebook_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subnetId =
         flag "subnet-id" (optional string) ~doc:"STRING SubnetId"
       and securityGroupIds =
         flag "security-group-ids" (optional json_arg)
           ~doc:"JSON SecurityGroupIds"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and lifecycleConfigName =
         flag "lifecycle-config-name" (optional string)
           ~doc:"STRING NotebookInstanceLifecycleConfigName"
       and directInternetAccess =
         flag "direct-internet-access" (optional json_arg)
           ~doc:"JSON DirectInternetAccess"
       and volumeSizeInGB =
         flag "volume-size-in-g-b" (optional int)
           ~doc:"INT NotebookInstanceVolumeSizeInGB"
       and acceleratorTypes =
         flag "accelerator-types" (optional json_arg)
           ~doc:"JSON NotebookInstanceAcceleratorTypes"
       and defaultCodeRepository =
         flag "default-code-repository" (optional string)
           ~doc:"STRING CodeRepositoryNameOrUrl"
       and additionalCodeRepositories =
         flag "additional-code-repositories" (optional json_arg)
           ~doc:"JSON AdditionalCodeRepositoryNamesOrUrls"
       and rootAccess =
         flag "root-access" (optional json_arg) ~doc:"JSON RootAccess"
       and platformIdentifier =
         flag "platform-identifier" (optional string)
           ~doc:"STRING PlatformIdentifier"
       and notebookInstanceName =
         flag "notebook-instance-name" (required string)
           ~doc:"STRING NotebookInstanceName"
       and instanceType =
         flag "instance-type" (required json_arg) ~doc:"JSON InstanceType"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_notebook_instance
           (Values.CreateNotebookInstanceInput.make ?subnetId
              ?securityGroupIds:(Option.map
                                   ~f:Values.SecurityGroupIds.of_json
                                   securityGroupIds) ?kmsKeyId
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?lifecycleConfigName
              ?directInternetAccess:(Option.map
                                       ~f:Values.DirectInternetAccess.of_json
                                       directInternetAccess) ?volumeSizeInGB
              ?acceleratorTypes:(Option.map
                                   ~f:Values.NotebookInstanceAcceleratorTypes.of_json
                                   acceleratorTypes) ?defaultCodeRepository
              ?additionalCodeRepositories:(Option.map
                                             ~f:Values.AdditionalCodeRepositoryNamesOrUrls.of_json
                                             additionalCodeRepositories)
              ?rootAccess:(Option.map ~f:Values.RootAccess.of_json rootAccess)
              ?platformIdentifier ~notebookInstanceName
              ~instanceType:(Values.InstanceType.of_json instanceType)
              ~roleArn ()) (Some Values.CreateNotebookInstanceOutput.to_json)
           (Some Values.CreateNotebookInstanceOutput.error_to_json)])
let create_notebook_instance_lifecycle_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and onCreate =
         flag "on-create" (optional json_arg)
           ~doc:"JSON NotebookInstanceLifecycleConfigList"
       and onStart =
         flag "on-start" (optional json_arg)
           ~doc:"JSON NotebookInstanceLifecycleConfigList"
       and notebookInstanceLifecycleConfigName =
         flag "notebook-instance-lifecycle-config-name" (required string)
           ~doc:"STRING NotebookInstanceLifecycleConfigName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_notebook_instance_lifecycle_config
           (Values.CreateNotebookInstanceLifecycleConfigInput.make
              ?onCreate:(Option.map
                           ~f:Values.NotebookInstanceLifecycleConfigList.of_json
                           onCreate)
              ?onStart:(Option.map
                          ~f:Values.NotebookInstanceLifecycleConfigList.of_json
                          onStart) ~notebookInstanceLifecycleConfigName ())
           (Some Values.CreateNotebookInstanceLifecycleConfigOutput.to_json)
           (Some
              Values.CreateNotebookInstanceLifecycleConfigOutput.error_to_json)])
let create_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineDisplayName =
         flag "pipeline-display-name" (optional string)
           ~doc:"STRING PipelineName"
       and pipelineDefinition =
         flag "pipeline-definition" (optional string)
           ~doc:"STRING PipelineDefinition"
       and pipelineDefinitionS3Location =
         flag "pipeline-definition-s3-location" (optional json_arg)
           ~doc:"JSON PipelineDefinitionS3Location"
       and pipelineDescription =
         flag "pipeline-description" (optional string)
           ~doc:"STRING PipelineDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and parallelismConfiguration =
         flag "parallelism-configuration" (optional json_arg)
           ~doc:"JSON ParallelismConfiguration"
       and pipelineName =
         flag "pipeline-name" (required string) ~doc:"STRING PipelineName"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING IdempotencyToken"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_pipeline
           (Values.CreatePipelineRequest.make ?pipelineDisplayName
              ?pipelineDefinition
              ?pipelineDefinitionS3Location:(Option.map
                                               ~f:Values.PipelineDefinitionS3Location.of_json
                                               pipelineDefinitionS3Location)
              ?pipelineDescription
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?parallelismConfiguration:(Option.map
                                           ~f:Values.ParallelismConfiguration.of_json
                                           parallelismConfiguration)
              ~pipelineName ~clientRequestToken ~roleArn ())
           (Some Values.CreatePipelineResponse.to_json)
           (Some Values.CreatePipelineResponse.error_to_json)])
let create_presigned_domain_url =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sessionExpirationDurationInSeconds =
         flag "session-expiration-duration-in-seconds" (optional int)
           ~doc:"INT SessionExpirationDurationInSeconds"
       and expiresInSeconds =
         flag "expires-in-seconds" (optional int) ~doc:"INT ExpiresInSeconds"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and userProfileName =
         flag "user-profile-name" (required string)
           ~doc:"STRING UserProfileName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_presigned_domain_url
           (Values.CreatePresignedDomainUrlRequest.make
              ?sessionExpirationDurationInSeconds ?expiresInSeconds ~domainId
              ~userProfileName ())
           (Some Values.CreatePresignedDomainUrlResponse.to_json)
           (Some Values.CreatePresignedDomainUrlResponse.error_to_json)])
let create_presigned_notebook_instance_url =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sessionExpirationDurationInSeconds =
         flag "session-expiration-duration-in-seconds" (optional int)
           ~doc:"INT SessionExpirationDurationInSeconds"
       and notebookInstanceName =
         flag "notebook-instance-name" (required string)
           ~doc:"STRING NotebookInstanceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_presigned_notebook_instance_url
           (Values.CreatePresignedNotebookInstanceUrlInput.make
              ?sessionExpirationDurationInSeconds ~notebookInstanceName ())
           (Some Values.CreatePresignedNotebookInstanceUrlOutput.to_json)
           (Some
              Values.CreatePresignedNotebookInstanceUrlOutput.error_to_json)])
let create_processing_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and processingInputs =
         flag "processing-inputs" (optional json_arg)
           ~doc:"JSON ProcessingInputs"
       and processingOutputConfig =
         flag "processing-output-config" (optional json_arg)
           ~doc:"JSON ProcessingOutputConfig"
       and stoppingCondition =
         flag "stopping-condition" (optional json_arg)
           ~doc:"JSON ProcessingStoppingCondition"
       and environment =
         flag "environment" (optional json_arg)
           ~doc:"JSON ProcessingEnvironmentMap"
       and networkConfig =
         flag "network-config" (optional json_arg) ~doc:"JSON NetworkConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and experimentConfig =
         flag "experiment-config" (optional json_arg)
           ~doc:"JSON ExperimentConfig"
       and processingJobName =
         flag "processing-job-name" (required string)
           ~doc:"STRING ProcessingJobName"
       and processingResources =
         flag "processing-resources" (required json_arg)
           ~doc:"JSON ProcessingResources"
       and appSpecification =
         flag "app-specification" (required json_arg)
           ~doc:"JSON AppSpecification"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_processing_job
           (Values.CreateProcessingJobRequest.make
              ?processingInputs:(Option.map
                                   ~f:Values.ProcessingInputs.of_json
                                   processingInputs)
              ?processingOutputConfig:(Option.map
                                         ~f:Values.ProcessingOutputConfig.of_json
                                         processingOutputConfig)
              ?stoppingCondition:(Option.map
                                    ~f:Values.ProcessingStoppingCondition.of_json
                                    stoppingCondition)
              ?environment:(Option.map
                              ~f:Values.ProcessingEnvironmentMap.of_json
                              environment)
              ?networkConfig:(Option.map ~f:Values.NetworkConfig.of_json
                                networkConfig)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?experimentConfig:(Option.map
                                   ~f:Values.ExperimentConfig.of_json
                                   experimentConfig) ~processingJobName
              ~processingResources:(Values.ProcessingResources.of_json
                                      processingResources)
              ~appSpecification:(Values.AppSpecification.of_json
                                   appSpecification) ~roleArn ())
           (Some Values.CreateProcessingJobResponse.to_json)
           (Some Values.CreateProcessingJobResponse.error_to_json)])
let create_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectDescription =
         flag "project-description" (optional string)
           ~doc:"STRING EntityDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and projectName =
         flag "project-name" (required string)
           ~doc:"STRING ProjectEntityName"
       and serviceCatalogProvisioningDetails =
         flag "service-catalog-provisioning-details" (required json_arg)
           ~doc:"JSON ServiceCatalogProvisioningDetails" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_project
           (Values.CreateProjectInput.make ?projectDescription
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~projectName
              ~serviceCatalogProvisioningDetails:(Values.ServiceCatalogProvisioningDetails.of_json
                                                    serviceCatalogProvisioningDetails)
              ()) (Some Values.CreateProjectOutput.to_json)
           (Some Values.CreateProjectOutput.error_to_json)])
let create_studio_lifecycle_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and studioLifecycleConfigName =
         flag "studio-lifecycle-config-name" (required string)
           ~doc:"STRING StudioLifecycleConfigName"
       and studioLifecycleConfigContent =
         flag "studio-lifecycle-config-content" (required string)
           ~doc:"STRING StudioLifecycleConfigContent"
       and studioLifecycleConfigAppType =
         flag "studio-lifecycle-config-app-type" (required json_arg)
           ~doc:"JSON StudioLifecycleConfigAppType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_studio_lifecycle_config
           (Values.CreateStudioLifecycleConfigRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~studioLifecycleConfigName ~studioLifecycleConfigContent
              ~studioLifecycleConfigAppType:(Values.StudioLifecycleConfigAppType.of_json
                                               studioLifecycleConfigAppType)
              ()) (Some Values.CreateStudioLifecycleConfigResponse.to_json)
           (Some Values.CreateStudioLifecycleConfigResponse.error_to_json)])
let create_training_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hyperParameters =
         flag "hyper-parameters" (optional json_arg)
           ~doc:"JSON HyperParameters"
       and inputDataConfig =
         flag "input-data-config" (optional json_arg)
           ~doc:"JSON InputDataConfig"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON VpcConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and enableNetworkIsolation =
         flag "enable-network-isolation" (optional bool) ~doc:"BOOL Boolean"
       and enableInterContainerTrafficEncryption =
         flag "enable-inter-container-traffic-encryption" (optional bool)
           ~doc:"BOOL Boolean"
       and enableManagedSpotTraining =
         flag "enable-managed-spot-training" (optional bool)
           ~doc:"BOOL Boolean"
       and checkpointConfig =
         flag "checkpoint-config" (optional json_arg)
           ~doc:"JSON CheckpointConfig"
       and debugHookConfig =
         flag "debug-hook-config" (optional json_arg)
           ~doc:"JSON DebugHookConfig"
       and debugRuleConfigurations =
         flag "debug-rule-configurations" (optional json_arg)
           ~doc:"JSON DebugRuleConfigurations"
       and tensorBoardOutputConfig =
         flag "tensor-board-output-config" (optional json_arg)
           ~doc:"JSON TensorBoardOutputConfig"
       and experimentConfig =
         flag "experiment-config" (optional json_arg)
           ~doc:"JSON ExperimentConfig"
       and profilerConfig =
         flag "profiler-config" (optional json_arg)
           ~doc:"JSON ProfilerConfig"
       and profilerRuleConfigurations =
         flag "profiler-rule-configurations" (optional json_arg)
           ~doc:"JSON ProfilerRuleConfigurations"
       and environment =
         flag "environment" (optional json_arg)
           ~doc:"JSON TrainingEnvironmentMap"
       and retryStrategy =
         flag "retry-strategy" (optional json_arg) ~doc:"JSON RetryStrategy"
       and trainingJobName =
         flag "training-job-name" (required string)
           ~doc:"STRING TrainingJobName"
       and algorithmSpecification =
         flag "algorithm-specification" (required json_arg)
           ~doc:"JSON AlgorithmSpecification"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn"
       and outputDataConfig =
         flag "output-data-config" (required json_arg)
           ~doc:"JSON OutputDataConfig"
       and resourceConfig =
         flag "resource-config" (required json_arg)
           ~doc:"JSON ResourceConfig"
       and stoppingCondition =
         flag "stopping-condition" (required json_arg)
           ~doc:"JSON StoppingCondition" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_training_job
           (Values.CreateTrainingJobRequest.make
              ?hyperParameters:(Option.map ~f:Values.HyperParameters.of_json
                                  hyperParameters)
              ?inputDataConfig:(Option.map ~f:Values.InputDataConfig.of_json
                                  inputDataConfig)
              ?vpcConfig:(Option.map ~f:Values.VpcConfig.of_json vpcConfig)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?enableNetworkIsolation ?enableInterContainerTrafficEncryption
              ?enableManagedSpotTraining
              ?checkpointConfig:(Option.map
                                   ~f:Values.CheckpointConfig.of_json
                                   checkpointConfig)
              ?debugHookConfig:(Option.map ~f:Values.DebugHookConfig.of_json
                                  debugHookConfig)
              ?debugRuleConfigurations:(Option.map
                                          ~f:Values.DebugRuleConfigurations.of_json
                                          debugRuleConfigurations)
              ?tensorBoardOutputConfig:(Option.map
                                          ~f:Values.TensorBoardOutputConfig.of_json
                                          tensorBoardOutputConfig)
              ?experimentConfig:(Option.map
                                   ~f:Values.ExperimentConfig.of_json
                                   experimentConfig)
              ?profilerConfig:(Option.map ~f:Values.ProfilerConfig.of_json
                                 profilerConfig)
              ?profilerRuleConfigurations:(Option.map
                                             ~f:Values.ProfilerRuleConfigurations.of_json
                                             profilerRuleConfigurations)
              ?environment:(Option.map
                              ~f:Values.TrainingEnvironmentMap.of_json
                              environment)
              ?retryStrategy:(Option.map ~f:Values.RetryStrategy.of_json
                                retryStrategy) ~trainingJobName
              ~algorithmSpecification:(Values.AlgorithmSpecification.of_json
                                         algorithmSpecification) ~roleArn
              ~outputDataConfig:(Values.OutputDataConfig.of_json
                                   outputDataConfig)
              ~resourceConfig:(Values.ResourceConfig.of_json resourceConfig)
              ~stoppingCondition:(Values.StoppingCondition.of_json
                                    stoppingCondition) ())
           (Some Values.CreateTrainingJobResponse.to_json)
           (Some Values.CreateTrainingJobResponse.error_to_json)])
let create_transform_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxConcurrentTransforms =
         flag "max-concurrent-transforms" (optional int)
           ~doc:"INT MaxConcurrentTransforms"
       and modelClientConfig =
         flag "model-client-config" (optional json_arg)
           ~doc:"JSON ModelClientConfig"
       and maxPayloadInMB =
         flag "max-payload-in-m-b" (optional int) ~doc:"INT MaxPayloadInMB"
       and batchStrategy =
         flag "batch-strategy" (optional json_arg) ~doc:"JSON BatchStrategy"
       and environment =
         flag "environment" (optional json_arg)
           ~doc:"JSON TransformEnvironmentMap"
       and dataProcessing =
         flag "data-processing" (optional json_arg)
           ~doc:"JSON DataProcessing"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and experimentConfig =
         flag "experiment-config" (optional json_arg)
           ~doc:"JSON ExperimentConfig"
       and transformJobName =
         flag "transform-job-name" (required string)
           ~doc:"STRING TransformJobName"
       and modelName =
         flag "model-name" (required string) ~doc:"STRING ModelName"
       and transformInput =
         flag "transform-input" (required json_arg)
           ~doc:"JSON TransformInput"
       and transformOutput =
         flag "transform-output" (required json_arg)
           ~doc:"JSON TransformOutput"
       and transformResources =
         flag "transform-resources" (required json_arg)
           ~doc:"JSON TransformResources" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_transform_job
           (Values.CreateTransformJobRequest.make ?maxConcurrentTransforms
              ?modelClientConfig:(Option.map
                                    ~f:Values.ModelClientConfig.of_json
                                    modelClientConfig) ?maxPayloadInMB
              ?batchStrategy:(Option.map ~f:Values.BatchStrategy.of_json
                                batchStrategy)
              ?environment:(Option.map
                              ~f:Values.TransformEnvironmentMap.of_json
                              environment)
              ?dataProcessing:(Option.map ~f:Values.DataProcessing.of_json
                                 dataProcessing)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?experimentConfig:(Option.map
                                   ~f:Values.ExperimentConfig.of_json
                                   experimentConfig) ~transformJobName
              ~modelName
              ~transformInput:(Values.TransformInput.of_json transformInput)
              ~transformOutput:(Values.TransformOutput.of_json
                                  transformOutput)
              ~transformResources:(Values.TransformResources.of_json
                                     transformResources) ())
           (Some Values.CreateTransformJobResponse.to_json)
           (Some Values.CreateTransformJobResponse.error_to_json)])
let create_trial =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayName =
         flag "display-name" (optional string)
           ~doc:"STRING ExperimentEntityName"
       and metadataProperties =
         flag "metadata-properties" (optional json_arg)
           ~doc:"JSON MetadataProperties"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and trialName =
         flag "trial-name" (required string)
           ~doc:"STRING ExperimentEntityName"
       and experimentName =
         flag "experiment-name" (required string)
           ~doc:"STRING ExperimentEntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_trial
           (Values.CreateTrialRequest.make ?displayName
              ?metadataProperties:(Option.map
                                     ~f:Values.MetadataProperties.of_json
                                     metadataProperties)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~trialName
              ~experimentName ()) (Some Values.CreateTrialResponse.to_json)
           (Some Values.CreateTrialResponse.error_to_json)])
let create_trial_component =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayName =
         flag "display-name" (optional string)
           ~doc:"STRING ExperimentEntityName"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON TrialComponentStatus"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (optional json_arg) ~doc:"JSON Timestamp"
       and parameters =
         flag "parameters" (optional json_arg)
           ~doc:"JSON TrialComponentParameters"
       and inputArtifacts =
         flag "input-artifacts" (optional json_arg)
           ~doc:"JSON TrialComponentArtifacts"
       and outputArtifacts =
         flag "output-artifacts" (optional json_arg)
           ~doc:"JSON TrialComponentArtifacts"
       and metadataProperties =
         flag "metadata-properties" (optional json_arg)
           ~doc:"JSON MetadataProperties"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and trialComponentName =
         flag "trial-component-name" (required string)
           ~doc:"STRING ExperimentEntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_trial_component
           (Values.CreateTrialComponentRequest.make ?displayName
              ?status:(Option.map ~f:Values.TrialComponentStatus.of_json
                         status)
              ?startTime:(Option.map ~f:Values.Timestamp.of_json startTime)
              ?endTime:(Option.map ~f:Values.Timestamp.of_json endTime)
              ?parameters:(Option.map
                             ~f:Values.TrialComponentParameters.of_json
                             parameters)
              ?inputArtifacts:(Option.map
                                 ~f:Values.TrialComponentArtifacts.of_json
                                 inputArtifacts)
              ?outputArtifacts:(Option.map
                                  ~f:Values.TrialComponentArtifacts.of_json
                                  outputArtifacts)
              ?metadataProperties:(Option.map
                                     ~f:Values.MetadataProperties.of_json
                                     metadataProperties)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~trialComponentName ())
           (Some Values.CreateTrialComponentResponse.to_json)
           (Some Values.CreateTrialComponentResponse.error_to_json)])
let create_user_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and singleSignOnUserIdentifier =
         flag "single-sign-on-user-identifier" (optional string)
           ~doc:"STRING SingleSignOnUserIdentifier"
       and singleSignOnUserValue =
         flag "single-sign-on-user-value" (optional string)
           ~doc:"STRING String256"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and userSettings =
         flag "user-settings" (optional json_arg) ~doc:"JSON UserSettings"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and userProfileName =
         flag "user-profile-name" (required string)
           ~doc:"STRING UserProfileName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_user_profile
           (Values.CreateUserProfileRequest.make ?singleSignOnUserIdentifier
              ?singleSignOnUserValue
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?userSettings:(Option.map ~f:Values.UserSettings.of_json
                               userSettings) ~domainId ~userProfileName ())
           (Some Values.CreateUserProfileResponse.to_json)
           (Some Values.CreateUserProfileResponse.error_to_json)])
let create_workforce =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cognitoConfig =
         flag "cognito-config" (optional json_arg) ~doc:"JSON CognitoConfig"
       and oidcConfig =
         flag "oidc-config" (optional json_arg) ~doc:"JSON OidcConfig"
       and sourceIpConfig =
         flag "source-ip-config" (optional json_arg)
           ~doc:"JSON SourceIpConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and workforceName =
         flag "workforce-name" (required string) ~doc:"STRING WorkforceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_workforce
           (Values.CreateWorkforceRequest.make
              ?cognitoConfig:(Option.map ~f:Values.CognitoConfig.of_json
                                cognitoConfig)
              ?oidcConfig:(Option.map ~f:Values.OidcConfig.of_json oidcConfig)
              ?sourceIpConfig:(Option.map ~f:Values.SourceIpConfig.of_json
                                 sourceIpConfig)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~workforceName ())
           (Some Values.CreateWorkforceResponse.to_json)
           (Some Values.CreateWorkforceResponse.error_to_json)])
let create_workteam =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workforceName =
         flag "workforce-name" (optional string) ~doc:"STRING WorkforceName"
       and notificationConfiguration =
         flag "notification-configuration" (optional json_arg)
           ~doc:"JSON NotificationConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and workteamName =
         flag "workteam-name" (required string) ~doc:"STRING WorkteamName"
       and memberDefinitions =
         flag "member-definitions" (required json_arg)
           ~doc:"JSON MemberDefinitions"
       and description =
         flag "description" (required string) ~doc:"STRING String200" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_workteam
           (Values.CreateWorkteamRequest.make ?workforceName
              ?notificationConfiguration:(Option.map
                                            ~f:Values.NotificationConfiguration.of_json
                                            notificationConfiguration)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~workteamName
              ~memberDefinitions:(Values.MemberDefinitions.of_json
                                    memberDefinitions) ~description ())
           (Some Values.CreateWorkteamResponse.to_json)
           (Some Values.CreateWorkteamResponse.error_to_json)])
let delete_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and actionName =
         flag "action-name" (required string)
           ~doc:"STRING ExperimentEntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_action (Values.DeleteActionRequest.make ~actionName ())
           (Some Values.DeleteActionResponse.to_json)
           (Some Values.DeleteActionResponse.error_to_json)])
let delete_algorithm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and algorithmName =
         flag "algorithm-name" (required string) ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_algorithm
           (Values.DeleteAlgorithmInput.make ~algorithmName ()) None None])
let delete_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and userProfileName =
         flag "user-profile-name" (required string)
           ~doc:"STRING UserProfileName"
       and appType = flag "app-type" (required json_arg) ~doc:"JSON AppType"
       and appName = flag "app-name" (required string) ~doc:"STRING AppName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_app
           (Values.DeleteAppRequest.make ~domainId ~userProfileName
              ~appType:(Values.AppType.of_json appType) ~appName ()) None
           None])
let delete_app_image_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appImageConfigName =
         flag "app-image-config-name" (required string)
           ~doc:"STRING AppImageConfigName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_app_image_config
           (Values.DeleteAppImageConfigRequest.make ~appImageConfigName ())
           None None])
let delete_artifact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and artifactArn =
         flag "artifact-arn" (optional string) ~doc:"STRING ArtifactArn"
       and source =
         flag "source" (optional json_arg) ~doc:"JSON ArtifactSource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_artifact
           (Values.DeleteArtifactRequest.make ?artifactArn
              ?source:(Option.map ~f:Values.ArtifactSource.of_json source) ())
           (Some Values.DeleteArtifactResponse.to_json)
           (Some Values.DeleteArtifactResponse.error_to_json)])
let delete_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceArn =
         flag "source-arn" (required string)
           ~doc:"STRING AssociationEntityArn"
       and destinationArn =
         flag "destination-arn" (required string)
           ~doc:"STRING AssociationEntityArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_association
           (Values.DeleteAssociationRequest.make ~sourceArn ~destinationArn
              ()) (Some Values.DeleteAssociationResponse.to_json)
           (Some Values.DeleteAssociationResponse.error_to_json)])
let delete_code_repository =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and codeRepositoryName =
         flag "code-repository-name" (required string)
           ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_code_repository
           (Values.DeleteCodeRepositoryInput.make ~codeRepositoryName ())
           None None])
let delete_context =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contextName =
         flag "context-name" (required string)
           ~doc:"STRING ExperimentEntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_context
           (Values.DeleteContextRequest.make ~contextName ())
           (Some Values.DeleteContextResponse.to_json)
           (Some Values.DeleteContextResponse.error_to_json)])
let delete_data_quality_job_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobDefinitionName =
         flag "job-definition-name" (required string)
           ~doc:"STRING MonitoringJobDefinitionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_data_quality_job_definition
           (Values.DeleteDataQualityJobDefinitionRequest.make
              ~jobDefinitionName ()) None None])
let delete_device_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceFleetName =
         flag "device-fleet-name" (required string) ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_device_fleet
           (Values.DeleteDeviceFleetRequest.make ~deviceFleetName ()) None
           None])
let delete_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and retentionPolicy =
         flag "retention-policy" (optional json_arg)
           ~doc:"JSON RetentionPolicy"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_domain
           (Values.DeleteDomainRequest.make
              ?retentionPolicy:(Option.map ~f:Values.RetentionPolicy.of_json
                                  retentionPolicy) ~domainId ()) None None])
let delete_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointName =
         flag "endpoint-name" (required string) ~doc:"STRING EndpointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_endpoint
           (Values.DeleteEndpointInput.make ~endpointName ()) None None])
let delete_endpoint_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointConfigName =
         flag "endpoint-config-name" (required string)
           ~doc:"STRING EndpointConfigName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_endpoint_config
           (Values.DeleteEndpointConfigInput.make ~endpointConfigName ())
           None None])
let delete_experiment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and experimentName =
         flag "experiment-name" (required string)
           ~doc:"STRING ExperimentEntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_experiment
           (Values.DeleteExperimentRequest.make ~experimentName ())
           (Some Values.DeleteExperimentResponse.to_json)
           (Some Values.DeleteExperimentResponse.error_to_json)])
let delete_feature_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and featureGroupName =
         flag "feature-group-name" (required string)
           ~doc:"STRING FeatureGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_feature_group
           (Values.DeleteFeatureGroupRequest.make ~featureGroupName ()) None
           None])
let delete_flow_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowDefinitionName =
         flag "flow-definition-name" (required string)
           ~doc:"STRING FlowDefinitionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_flow_definition
           (Values.DeleteFlowDefinitionRequest.make ~flowDefinitionName ())
           (Some Values.DeleteFlowDefinitionResponse.to_json)
           (Some Values.DeleteFlowDefinitionResponse.error_to_json)])
let delete_human_task_ui =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and humanTaskUiName =
         flag "human-task-ui-name" (required string)
           ~doc:"STRING HumanTaskUiName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_human_task_ui
           (Values.DeleteHumanTaskUiRequest.make ~humanTaskUiName ())
           (Some Values.DeleteHumanTaskUiResponse.to_json)
           (Some Values.DeleteHumanTaskUiResponse.error_to_json)])
let delete_image =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and imageName =
         flag "image-name" (required string) ~doc:"STRING ImageName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_image (Values.DeleteImageRequest.make ~imageName ())
           (Some Values.DeleteImageResponse.to_json)
           (Some Values.DeleteImageResponse.error_to_json)])
let delete_image_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and imageName =
         flag "image-name" (required string) ~doc:"STRING ImageName"
       and version =
         flag "version" (required int) ~doc:"INT ImageVersionNumber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_image_version
           (Values.DeleteImageVersionRequest.make ~imageName ~version ())
           (Some Values.DeleteImageVersionResponse.to_json)
           (Some Values.DeleteImageVersionResponse.error_to_json)])
let delete_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelName =
         flag "model-name" (required string) ~doc:"STRING ModelName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_model (Values.DeleteModelInput.make ~modelName ()) None
           None])
let delete_model_bias_job_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobDefinitionName =
         flag "job-definition-name" (required string)
           ~doc:"STRING MonitoringJobDefinitionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_model_bias_job_definition
           (Values.DeleteModelBiasJobDefinitionRequest.make
              ~jobDefinitionName ()) None None])
let delete_model_explainability_job_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobDefinitionName =
         flag "job-definition-name" (required string)
           ~doc:"STRING MonitoringJobDefinitionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_model_explainability_job_definition
           (Values.DeleteModelExplainabilityJobDefinitionRequest.make
              ~jobDefinitionName ()) None None])
let delete_model_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelPackageName =
         flag "model-package-name" (required string)
           ~doc:"STRING VersionedArnOrName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_model_package
           (Values.DeleteModelPackageInput.make ~modelPackageName ()) None
           None])
let delete_model_package_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelPackageGroupName =
         flag "model-package-group-name" (required string)
           ~doc:"STRING ArnOrName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_model_package_group
           (Values.DeleteModelPackageGroupInput.make ~modelPackageGroupName
              ()) None None])
let delete_model_package_group_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelPackageGroupName =
         flag "model-package-group-name" (required string)
           ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_model_package_group_policy
           (Values.DeleteModelPackageGroupPolicyInput.make
              ~modelPackageGroupName ()) None None])
let delete_model_quality_job_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobDefinitionName =
         flag "job-definition-name" (required string)
           ~doc:"STRING MonitoringJobDefinitionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_model_quality_job_definition
           (Values.DeleteModelQualityJobDefinitionRequest.make
              ~jobDefinitionName ()) None None])
let delete_monitoring_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and monitoringScheduleName =
         flag "monitoring-schedule-name" (required string)
           ~doc:"STRING MonitoringScheduleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_monitoring_schedule
           (Values.DeleteMonitoringScheduleRequest.make
              ~monitoringScheduleName ()) None None])
let delete_notebook_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and notebookInstanceName =
         flag "notebook-instance-name" (required string)
           ~doc:"STRING NotebookInstanceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_notebook_instance
           (Values.DeleteNotebookInstanceInput.make ~notebookInstanceName ())
           None None])
let delete_notebook_instance_lifecycle_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and notebookInstanceLifecycleConfigName =
         flag "notebook-instance-lifecycle-config-name" (required string)
           ~doc:"STRING NotebookInstanceLifecycleConfigName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_notebook_instance_lifecycle_config
           (Values.DeleteNotebookInstanceLifecycleConfigInput.make
              ~notebookInstanceLifecycleConfigName ()) None None])
let delete_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineName =
         flag "pipeline-name" (required string) ~doc:"STRING PipelineName"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING IdempotencyToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_pipeline
           (Values.DeletePipelineRequest.make ~pipelineName
              ~clientRequestToken ())
           (Some Values.DeletePipelineResponse.to_json)
           (Some Values.DeletePipelineResponse.error_to_json)])
let delete_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectName =
         flag "project-name" (required string)
           ~doc:"STRING ProjectEntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_project (Values.DeleteProjectInput.make ~projectName ())
           None None])
let delete_studio_lifecycle_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and studioLifecycleConfigName =
         flag "studio-lifecycle-config-name" (required string)
           ~doc:"STRING StudioLifecycleConfigName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_studio_lifecycle_config
           (Values.DeleteStudioLifecycleConfigRequest.make
              ~studioLifecycleConfigName ()) None None])
let delete_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_tags
           (Values.DeleteTagsInput.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.DeleteTagsOutput.to_json)
           (Some Values.DeleteTagsOutput.error_to_json)])
let delete_trial =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trialName =
         flag "trial-name" (required string)
           ~doc:"STRING ExperimentEntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_trial (Values.DeleteTrialRequest.make ~trialName ())
           (Some Values.DeleteTrialResponse.to_json)
           (Some Values.DeleteTrialResponse.error_to_json)])
let delete_trial_component =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trialComponentName =
         flag "trial-component-name" (required string)
           ~doc:"STRING ExperimentEntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_trial_component
           (Values.DeleteTrialComponentRequest.make ~trialComponentName ())
           (Some Values.DeleteTrialComponentResponse.to_json)
           (Some Values.DeleteTrialComponentResponse.error_to_json)])
let delete_user_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and userProfileName =
         flag "user-profile-name" (required string)
           ~doc:"STRING UserProfileName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_user_profile
           (Values.DeleteUserProfileRequest.make ~domainId ~userProfileName
              ()) None None])
let delete_workforce =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workforceName =
         flag "workforce-name" (required string) ~doc:"STRING WorkforceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_workforce
           (Values.DeleteWorkforceRequest.make ~workforceName ())
           (Some Values.DeleteWorkforceResponse.to_json)
           (Some Values.DeleteWorkforceResponse.error_to_json)])
let delete_workteam =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workteamName =
         flag "workteam-name" (required string) ~doc:"STRING WorkteamName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_workteam
           (Values.DeleteWorkteamRequest.make ~workteamName ())
           (Some Values.DeleteWorkteamResponse.to_json)
           (Some Values.DeleteWorkteamResponse.error_to_json)])
let deregister_devices =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceFleetName =
         flag "device-fleet-name" (required string) ~doc:"STRING EntityName"
       and deviceNames =
         flag "device-names" (required json_arg) ~doc:"JSON DeviceNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_devices
           (Values.DeregisterDevicesRequest.make ~deviceFleetName
              ~deviceNames:(Values.DeviceNames.of_json deviceNames) ()) None
           None])
let describe_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and actionName =
         flag "action-name" (required string)
           ~doc:"STRING ExperimentEntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_action
           (Values.DescribeActionRequest.make ~actionName ())
           (Some Values.DescribeActionResponse.to_json)
           (Some Values.DescribeActionResponse.error_to_json)])
let describe_algorithm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and algorithmName =
         flag "algorithm-name" (required string) ~doc:"STRING ArnOrName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_algorithm
           (Values.DescribeAlgorithmInput.make ~algorithmName ())
           (Some Values.DescribeAlgorithmOutput.to_json)
           (Some Values.DescribeAlgorithmOutput.error_to_json)])
let describe_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId"
       and userProfileName =
         flag "user-profile-name" (required string)
           ~doc:"STRING UserProfileName"
       and appType = flag "app-type" (required json_arg) ~doc:"JSON AppType"
       and appName = flag "app-name" (required string) ~doc:"STRING AppName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_app
           (Values.DescribeAppRequest.make ~domainId ~userProfileName
              ~appType:(Values.AppType.of_json appType) ~appName ())
           (Some Values.DescribeAppResponse.to_json)
           (Some Values.DescribeAppResponse.error_to_json)])
let describe_app_image_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appImageConfigName =
         flag "app-image-config-name" (required string)
           ~doc:"STRING AppImageConfigName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_app_image_config
           (Values.DescribeAppImageConfigRequest.make ~appImageConfigName ())
           (Some Values.DescribeAppImageConfigResponse.to_json)
           (Some Values.DescribeAppImageConfigResponse.error_to_json)])
let describe_artifact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and artifactArn =
         flag "artifact-arn" (required string) ~doc:"STRING ArtifactArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_artifact
           (Values.DescribeArtifactRequest.make ~artifactArn ())
           (Some Values.DescribeArtifactResponse.to_json)
           (Some Values.DescribeArtifactResponse.error_to_json)])
let describe_auto_m_l_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoMLJobName =
         flag "auto-m-l-job-name" (required string)
           ~doc:"STRING AutoMLJobName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_auto_m_l_job
           (Values.DescribeAutoMLJobRequest.make ~autoMLJobName ())
           (Some Values.DescribeAutoMLJobResponse.to_json)
           (Some Values.DescribeAutoMLJobResponse.error_to_json)])
let describe_code_repository =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and codeRepositoryName =
         flag "code-repository-name" (required string)
           ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_code_repository
           (Values.DescribeCodeRepositoryInput.make ~codeRepositoryName ())
           (Some Values.DescribeCodeRepositoryOutput.to_json)
           (Some Values.DescribeCodeRepositoryOutput.error_to_json)])
let describe_compilation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and compilationJobName =
         flag "compilation-job-name" (required string)
           ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_compilation_job
           (Values.DescribeCompilationJobRequest.make ~compilationJobName ())
           (Some Values.DescribeCompilationJobResponse.to_json)
           (Some Values.DescribeCompilationJobResponse.error_to_json)])
let describe_context =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contextName =
         flag "context-name" (required string)
           ~doc:"STRING ExperimentEntityNameOrArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_context
           (Values.DescribeContextRequest.make ~contextName ())
           (Some Values.DescribeContextResponse.to_json)
           (Some Values.DescribeContextResponse.error_to_json)])
let describe_data_quality_job_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobDefinitionName =
         flag "job-definition-name" (required string)
           ~doc:"STRING MonitoringJobDefinitionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_data_quality_job_definition
           (Values.DescribeDataQualityJobDefinitionRequest.make
              ~jobDefinitionName ())
           (Some Values.DescribeDataQualityJobDefinitionResponse.to_json)
           (Some
              Values.DescribeDataQualityJobDefinitionResponse.error_to_json)])
let describe_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and deviceName =
         flag "device-name" (required string) ~doc:"STRING EntityName"
       and deviceFleetName =
         flag "device-fleet-name" (required string) ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_device
           (Values.DescribeDeviceRequest.make ?nextToken ~deviceName
              ~deviceFleetName ())
           (Some Values.DescribeDeviceResponse.to_json)
           (Some Values.DescribeDeviceResponse.error_to_json)])
let describe_device_fleet =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceFleetName =
         flag "device-fleet-name" (required string) ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_device_fleet
           (Values.DescribeDeviceFleetRequest.make ~deviceFleetName ())
           (Some Values.DescribeDeviceFleetResponse.to_json)
           (Some Values.DescribeDeviceFleetResponse.error_to_json)])
let describe_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainId =
         flag "domain-id" (required string) ~doc:"STRING DomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_domain
           (Values.DescribeDomainRequest.make ~domainId ())
           (Some Values.DescribeDomainResponse.to_json)
           (Some Values.DescribeDomainResponse.error_to_json)])
let describe_edge_packaging_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and edgePackagingJobName =
         flag "edge-packaging-job-name" (required string)
           ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_edge_packaging_job
           (Values.DescribeEdgePackagingJobRequest.make ~edgePackagingJobName
              ()) (Some Values.DescribeEdgePackagingJobResponse.to_json)
           (Some Values.DescribeEdgePackagingJobResponse.error_to_json)])
let describe_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointName =
         flag "endpoint-name" (required string) ~doc:"STRING EndpointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_endpoint
           (Values.DescribeEndpointInput.make ~endpointName ())
           (Some Values.DescribeEndpointOutput.to_json)
           (Some Values.DescribeEndpointOutput.error_to_json)])
let describe_endpoint_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointConfigName =
         flag "endpoint-config-name" (required string)
           ~doc:"STRING EndpointConfigName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_endpoint_config
           (Values.DescribeEndpointConfigInput.make ~endpointConfigName ())
           (Some Values.DescribeEndpointConfigOutput.to_json)
           (Some Values.DescribeEndpointConfigOutput.error_to_json)])
let describe_experiment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and experimentName =
         flag "experiment-name" (required string)
           ~doc:"STRING ExperimentEntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_experiment
           (Values.DescribeExperimentRequest.make ~experimentName ())
           (Some Values.DescribeExperimentResponse.to_json)
           (Some Values.DescribeExperimentResponse.error_to_json)])
let describe_feature_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and featureGroupName =
         flag "feature-group-name" (required string)
           ~doc:"STRING FeatureGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_feature_group
           (Values.DescribeFeatureGroupRequest.make ?nextToken
              ~featureGroupName ())
           (Some Values.DescribeFeatureGroupResponse.to_json)
           (Some Values.DescribeFeatureGroupResponse.error_to_json)])
let describe_flow_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowDefinitionName =
         flag "flow-definition-name" (required string)
           ~doc:"STRING FlowDefinitionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_flow_definition
           (Values.DescribeFlowDefinitionRequest.make ~flowDefinitionName ())
           (Some Values.DescribeFlowDefinitionResponse.to_json)
           (Some Values.DescribeFlowDefinitionResponse.error_to_json)])
let describe_human_task_ui =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and humanTaskUiName =
         flag "human-task-ui-name" (required string)
           ~doc:"STRING HumanTaskUiName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_human_task_ui
           (Values.DescribeHumanTaskUiRequest.make ~humanTaskUiName ())
           (Some Values.DescribeHumanTaskUiResponse.to_json)
           (Some Values.DescribeHumanTaskUiResponse.error_to_json)])
let describe_hyper_parameter_tuning_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hyperParameterTuningJobName =
         flag "hyper-parameter-tuning-job-name" (required string)
           ~doc:"STRING HyperParameterTuningJobName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_hyper_parameter_tuning_job
           (Values.DescribeHyperParameterTuningJobRequest.make
              ~hyperParameterTuningJobName ())
           (Some Values.DescribeHyperParameterTuningJobResponse.to_json)
           (Some Values.DescribeHyperParameterTuningJobResponse.error_to_json)])
let describe_image =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and imageName =
         flag "image-name" (required string) ~doc:"STRING ImageName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_image (Values.DescribeImageRequest.make ~imageName ())
           (Some Values.DescribeImageResponse.to_json)
           (Some Values.DescribeImageResponse.error_to_json)])
let describe_image_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and version =
         flag "version" (optional int) ~doc:"INT ImageVersionNumber"
       and imageName =
         flag "image-name" (required string) ~doc:"STRING ImageName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_image_version
           (Values.DescribeImageVersionRequest.make ?version ~imageName ())
           (Some Values.DescribeImageVersionResponse.to_json)
           (Some Values.DescribeImageVersionResponse.error_to_json)])
let describe_inference_recommendations_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobName =
         flag "job-name" (required string)
           ~doc:"STRING RecommendationJobName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_inference_recommendations_job
           (Values.DescribeInferenceRecommendationsJobRequest.make ~jobName
              ())
           (Some Values.DescribeInferenceRecommendationsJobResponse.to_json)
           (Some
              Values.DescribeInferenceRecommendationsJobResponse.error_to_json)])
let describe_labeling_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and labelingJobName =
         flag "labeling-job-name" (required string)
           ~doc:"STRING LabelingJobName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_labeling_job
           (Values.DescribeLabelingJobRequest.make ~labelingJobName ())
           (Some Values.DescribeLabelingJobResponse.to_json)
           (Some Values.DescribeLabelingJobResponse.error_to_json)])
let describe_lineage_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and lineageGroupName =
         flag "lineage-group-name" (required string)
           ~doc:"STRING ExperimentEntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_lineage_group
           (Values.DescribeLineageGroupRequest.make ~lineageGroupName ())
           (Some Values.DescribeLineageGroupResponse.to_json)
           (Some Values.DescribeLineageGroupResponse.error_to_json)])
let describe_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelName =
         flag "model-name" (required string) ~doc:"STRING ModelName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_model (Values.DescribeModelInput.make ~modelName ())
           (Some Values.DescribeModelOutput.to_json)
           (Some Values.DescribeModelOutput.error_to_json)])
let describe_model_bias_job_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobDefinitionName =
         flag "job-definition-name" (required string)
           ~doc:"STRING MonitoringJobDefinitionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_model_bias_job_definition
           (Values.DescribeModelBiasJobDefinitionRequest.make
              ~jobDefinitionName ())
           (Some Values.DescribeModelBiasJobDefinitionResponse.to_json)
           (Some Values.DescribeModelBiasJobDefinitionResponse.error_to_json)])
let describe_model_explainability_job_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobDefinitionName =
         flag "job-definition-name" (required string)
           ~doc:"STRING MonitoringJobDefinitionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_model_explainability_job_definition
           (Values.DescribeModelExplainabilityJobDefinitionRequest.make
              ~jobDefinitionName ())
           (Some
              Values.DescribeModelExplainabilityJobDefinitionResponse.to_json)
           (Some
              Values.DescribeModelExplainabilityJobDefinitionResponse.error_to_json)])
let describe_model_package =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelPackageName =
         flag "model-package-name" (required string)
           ~doc:"STRING VersionedArnOrName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_model_package
           (Values.DescribeModelPackageInput.make ~modelPackageName ())
           (Some Values.DescribeModelPackageOutput.to_json)
           (Some Values.DescribeModelPackageOutput.error_to_json)])
let describe_model_package_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelPackageGroupName =
         flag "model-package-group-name" (required string)
           ~doc:"STRING ArnOrName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_model_package_group
           (Values.DescribeModelPackageGroupInput.make ~modelPackageGroupName
              ()) (Some Values.DescribeModelPackageGroupOutput.to_json)
           (Some Values.DescribeModelPackageGroupOutput.error_to_json)])
let describe_model_quality_job_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobDefinitionName =
         flag "job-definition-name" (required string)
           ~doc:"STRING MonitoringJobDefinitionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_model_quality_job_definition
           (Values.DescribeModelQualityJobDefinitionRequest.make
              ~jobDefinitionName ())
           (Some Values.DescribeModelQualityJobDefinitionResponse.to_json)
           (Some
              Values.DescribeModelQualityJobDefinitionResponse.error_to_json)])
let describe_monitoring_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and monitoringScheduleName =
         flag "monitoring-schedule-name" (required string)
           ~doc:"STRING MonitoringScheduleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_monitoring_schedule
           (Values.DescribeMonitoringScheduleRequest.make
              ~monitoringScheduleName ())
           (Some Values.DescribeMonitoringScheduleResponse.to_json)
           (Some Values.DescribeMonitoringScheduleResponse.error_to_json)])
let describe_notebook_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and notebookInstanceName =
         flag "notebook-instance-name" (required string)
           ~doc:"STRING NotebookInstanceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_notebook_instance
           (Values.DescribeNotebookInstanceInput.make ~notebookInstanceName
              ()) (Some Values.DescribeNotebookInstanceOutput.to_json)
           (Some Values.DescribeNotebookInstanceOutput.error_to_json)])
let describe_notebook_instance_lifecycle_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and notebookInstanceLifecycleConfigName =
         flag "notebook-instance-lifecycle-config-name" (required string)
           ~doc:"STRING NotebookInstanceLifecycleConfigName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_notebook_instance_lifecycle_config
           (Values.DescribeNotebookInstanceLifecycleConfigInput.make
              ~notebookInstanceLifecycleConfigName ())
           (Some Values.DescribeNotebookInstanceLifecycleConfigOutput.to_json)
           (Some
              Values.DescribeNotebookInstanceLifecycleConfigOutput.error_to_json)])
let describe_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineName =
         flag "pipeline-name" (required string) ~doc:"STRING PipelineName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_pipeline
           (Values.DescribePipelineRequest.make ~pipelineName ())
           (Some Values.DescribePipelineResponse.to_json)
           (Some Values.DescribePipelineResponse.error_to_json)])
let describe_pipeline_definition_for_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pipelineExecutionArn =
         flag "pipeline-execution-arn" (required string)
           ~doc:"STRING PipelineExecutionArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_pipeline_definition_for_execution
           (Values.DescribePipelineDefinitionForExecutionRequest.make
              ~pipelineExecutionArn ())
           (Some
              Values.DescribePipelineDefinitionForExecutionResponse.to_json)
           (Some
              Values.DescribePipelineDefinitionForExecutionResponse.error_to_json)])
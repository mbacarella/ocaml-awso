(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | BatchGetSecretValue: (BatchGetSecretValueRequest.t,
  BatchGetSecretValueResponse.t, BatchGetSecretValueResponse.error) t 
  | CancelRotateSecret: (CancelRotateSecretRequest.t,
  CancelRotateSecretResponse.t, CancelRotateSecretResponse.error) t 
  | CreateSecret: (CreateSecretRequest.t, CreateSecretResponse.t,
  CreateSecretResponse.error) t 
  | DeleteResourcePolicy: (DeleteResourcePolicyRequest.t,
  DeleteResourcePolicyResponse.t, DeleteResourcePolicyResponse.error) t 
  | DeleteSecret: (DeleteSecretRequest.t, DeleteSecretResponse.t,
  DeleteSecretResponse.error) t 
  | DescribeSecret: (DescribeSecretRequest.t, DescribeSecretResponse.t,
  DescribeSecretResponse.error) t 
  | GetRandomPassword: (GetRandomPasswordRequest.t,
  GetRandomPasswordResponse.t, GetRandomPasswordResponse.error) t 
  | GetResourcePolicy: (GetResourcePolicyRequest.t,
  GetResourcePolicyResponse.t, GetResourcePolicyResponse.error) t 
  | GetSecretValue: (GetSecretValueRequest.t, GetSecretValueResponse.t,
  GetSecretValueResponse.error) t 
  | ListSecretVersionIds: (ListSecretVersionIdsRequest.t,
  ListSecretVersionIdsResponse.t, ListSecretVersionIdsResponse.error) t 
  | ListSecrets: (ListSecretsRequest.t, ListSecretsResponse.t,
  ListSecretsResponse.error) t 
  | PutResourcePolicy: (PutResourcePolicyRequest.t,
  PutResourcePolicyResponse.t, PutResourcePolicyResponse.error) t 
  | PutSecretValue: (PutSecretValueRequest.t, PutSecretValueResponse.t,
  PutSecretValueResponse.error) t 
  | RemoveRegionsFromReplication: (RemoveRegionsFromReplicationRequest.t,
  RemoveRegionsFromReplicationResponse.t,
  RemoveRegionsFromReplicationResponse.error) t 
  | ReplicateSecretToRegions: (ReplicateSecretToRegionsRequest.t,
  ReplicateSecretToRegionsResponse.t, ReplicateSecretToRegionsResponse.error)
  t 
  | RestoreSecret: (RestoreSecretRequest.t, RestoreSecretResponse.t,
  RestoreSecretResponse.error) t 
  | RotateSecret: (RotateSecretRequest.t, RotateSecretResponse.t,
  RotateSecretResponse.error) t 
  | StopReplicationToReplica: (StopReplicationToReplicaRequest.t,
  StopReplicationToReplicaResponse.t, StopReplicationToReplicaResponse.error)
  t 
  | TagResource: (TagResourceRequest.t, unit, unit) t 
  | UntagResource: (UntagResourceRequest.t, unit, unit) t 
  | UpdateSecret: (UpdateSecretRequest.t, UpdateSecretResponse.t,
  UpdateSecretResponse.error) t 
  | UpdateSecretVersionStage: (UpdateSecretVersionStageRequest.t,
  UpdateSecretVersionStageResponse.t, UpdateSecretVersionStageResponse.error)
  t 
  | ValidateResourcePolicy: (ValidateResourcePolicyRequest.t,
  ValidateResourcePolicyResponse.t, ValidateResourcePolicyResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | BatchGetSecretValue -> `POST
  | CancelRotateSecret -> `POST
  | CreateSecret -> `POST
  | DeleteResourcePolicy -> `POST
  | DeleteSecret -> `POST
  | DescribeSecret -> `POST
  | GetRandomPassword -> `POST
  | GetResourcePolicy -> `POST
  | GetSecretValue -> `POST
  | ListSecretVersionIds -> `POST
  | ListSecrets -> `POST
  | PutResourcePolicy -> `POST
  | PutSecretValue -> `POST
  | RemoveRegionsFromReplication -> `POST
  | ReplicateSecretToRegions -> `POST
  | RestoreSecret -> `POST
  | RotateSecret -> `POST
  | StopReplicationToReplica -> `POST
  | TagResource -> `POST
  | UntagResource -> `POST
  | UpdateSecret -> `POST
  | UpdateSecretVersionStage -> `POST
  | ValidateResourcePolicy -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | BatchGetSecretValue -> (Format.kasprintf Uri.of_string) "/"
      | CancelRotateSecret -> (Format.kasprintf Uri.of_string) "/"
      | CreateSecret -> (Format.kasprintf Uri.of_string) "/"
      | DeleteResourcePolicy -> (Format.kasprintf Uri.of_string) "/"
      | DeleteSecret -> (Format.kasprintf Uri.of_string) "/"
      | DescribeSecret -> (Format.kasprintf Uri.of_string) "/"
      | GetRandomPassword -> (Format.kasprintf Uri.of_string) "/"
      | GetResourcePolicy -> (Format.kasprintf Uri.of_string) "/"
      | GetSecretValue -> (Format.kasprintf Uri.of_string) "/"
      | ListSecretVersionIds -> (Format.kasprintf Uri.of_string) "/"
      | ListSecrets -> (Format.kasprintf Uri.of_string) "/"
      | PutResourcePolicy -> (Format.kasprintf Uri.of_string) "/"
      | PutSecretValue -> (Format.kasprintf Uri.of_string) "/"
      | RemoveRegionsFromReplication -> (Format.kasprintf Uri.of_string) "/"
      | ReplicateSecretToRegions -> (Format.kasprintf Uri.of_string) "/"
      | RestoreSecret -> (Format.kasprintf Uri.of_string) "/"
      | RotateSecret -> (Format.kasprintf Uri.of_string) "/"
      | StopReplicationToReplica -> (Format.kasprintf Uri.of_string) "/"
      | TagResource -> (Format.kasprintf Uri.of_string) "/"
      | UntagResource -> (Format.kasprintf Uri.of_string) "/"
      | UpdateSecret -> (Format.kasprintf Uri.of_string) "/"
      | UpdateSecretVersionStage -> (Format.kasprintf Uri.of_string) "/"
      | ValidateResourcePolicy -> (Format.kasprintf Uri.of_string) "/")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  match endp with
  | BatchGetSecretValue ->
      let json = BatchGetSecretValueRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "secretsmanager.BatchGetSecretValue")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | CancelRotateSecret ->
      let json = CancelRotateSecretRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "secretsmanager.CancelRotateSecret")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | CreateSecret ->
      let json = CreateSecretRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "secretsmanager.CreateSecret")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | DeleteResourcePolicy ->
      let json = DeleteResourcePolicyRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "secretsmanager.DeleteResourcePolicy")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | DeleteSecret ->
      let json = DeleteSecretRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "secretsmanager.DeleteSecret")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | DescribeSecret ->
      let json = DescribeSecretRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "secretsmanager.DescribeSecret")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | GetRandomPassword ->
      let json = GetRandomPasswordRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "secretsmanager.GetRandomPassword")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | GetResourcePolicy ->
      let json = GetResourcePolicyRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "secretsmanager.GetResourcePolicy")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | GetSecretValue ->
      let json = GetSecretValueRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "secretsmanager.GetSecretValue")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListSecretVersionIds ->
      let json = ListSecretVersionIdsRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "secretsmanager.ListSecretVersionIds")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListSecrets ->
      let json = ListSecretsRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "secretsmanager.ListSecrets")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | PutResourcePolicy ->
      let json = PutResourcePolicyRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "secretsmanager.PutResourcePolicy")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | PutSecretValue ->
      let json = PutSecretValueRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "secretsmanager.PutSecretValue")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | RemoveRegionsFromReplication ->
      let json = RemoveRegionsFromReplicationRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "secretsmanager.RemoveRegionsFromReplication")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ReplicateSecretToRegions ->
      let json = ReplicateSecretToRegionsRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "secretsmanager.ReplicateSecretToRegions")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | RestoreSecret ->
      let json = RestoreSecretRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "secretsmanager.RestoreSecret")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | RotateSecret ->
      let json = RotateSecretRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "secretsmanager.RotateSecret")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | StopReplicationToReplica ->
      let json = StopReplicationToReplicaRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "secretsmanager.StopReplicationToReplica")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | TagResource ->
      let json = TagResourceRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "secretsmanager.TagResource")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | UntagResource ->
      let json = UntagResourceRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "secretsmanager.UntagResource")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | UpdateSecret ->
      let json = UpdateSecretRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "secretsmanager.UpdateSecret")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | UpdateSecretVersionStage ->
      let json = UpdateSecretVersionStageRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "secretsmanager.UpdateSecretVersionStage")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ValidateResourcePolicy ->
      let json = ValidateResourcePolicyRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target", "secretsmanager.ValidateResourcePolicy")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type = None }) in
    match (error_of_json, ((code >= 400) && (code <= 599))) with
    | (Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        (match json |> (Yojson.Safe.Util.member "__type") with
         | `String error_type -> error_of_json error_type json
         | `Null -> bail ()
         | _ ->
             failwith
               (sprintf "Error '__type' did not have string type: %s" body))
    | (None, _) | (_, false) -> bail () in
  let _ = parse_aws_error in
  let _ = resp in
  match endpoint with
  | BatchGetSecretValue ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (BatchGetSecretValueResponse.of_json json)
      else
        Error
          (parse_aws_error (Some BatchGetSecretValueResponse.error_of_json))
  | CancelRotateSecret ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (CancelRotateSecretResponse.of_json json)
      else
        Error
          (parse_aws_error (Some CancelRotateSecretResponse.error_of_json))
  | CreateSecret ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (CreateSecretResponse.of_json json)
      else Error (parse_aws_error (Some CreateSecretResponse.error_of_json))
  | DeleteResourcePolicy ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (DeleteResourcePolicyResponse.of_json json)
      else
        Error
          (parse_aws_error (Some DeleteResourcePolicyResponse.error_of_json))
  | DeleteSecret ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (DeleteSecretResponse.of_json json)
      else Error (parse_aws_error (Some DeleteSecretResponse.error_of_json))
  | DescribeSecret ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (DescribeSecretResponse.of_json json)
      else
        Error (parse_aws_error (Some DescribeSecretResponse.error_of_json))
  | GetRandomPassword ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (GetRandomPasswordResponse.of_json json)
      else
        Error
          (parse_aws_error (Some GetRandomPasswordResponse.error_of_json))
  | GetResourcePolicy ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (GetResourcePolicyResponse.of_json json)
      else
        Error
          (parse_aws_error (Some GetResourcePolicyResponse.error_of_json))
  | GetSecretValue ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (GetSecretValueResponse.of_json json)
      else
        Error (parse_aws_error (Some GetSecretValueResponse.error_of_json))
  | ListSecretVersionIds ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ListSecretVersionIdsResponse.of_json json)
      else
        Error
          (parse_aws_error (Some ListSecretVersionIdsResponse.error_of_json))
  | ListSecrets ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ListSecretsResponse.of_json json)
      else Error (parse_aws_error (Some ListSecretsResponse.error_of_json))
  | PutResourcePolicy ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (PutResourcePolicyResponse.of_json json)
      else
        Error
          (parse_aws_error (Some PutResourcePolicyResponse.error_of_json))
  | PutSecretValue ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (PutSecretValueResponse.of_json json)
      else
        Error (parse_aws_error (Some PutSecretValueResponse.error_of_json))
  | RemoveRegionsFromReplication ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (RemoveRegionsFromReplicationResponse.of_json json)
      else
        Error
          (parse_aws_error
             (Some RemoveRegionsFromReplicationResponse.error_of_json))
  | ReplicateSecretToRegions ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ReplicateSecretToRegionsResponse.of_json json)
      else
        Error
          (parse_aws_error
             (Some ReplicateSecretToRegionsResponse.error_of_json))
  | RestoreSecret ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (RestoreSecretResponse.of_json json)
      else Error (parse_aws_error (Some RestoreSecretResponse.error_of_json))
  | RotateSecret ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (RotateSecretResponse.of_json json)
      else Error (parse_aws_error (Some RotateSecretResponse.error_of_json))
  | StopReplicationToReplica ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (StopReplicationToReplicaResponse.of_json json)
      else
        Error
          (parse_aws_error
             (Some StopReplicationToReplicaResponse.error_of_json))
  | TagResource -> if is_success then Ok () else Error (parse_aws_error None)
  | UntagResource ->
      if is_success then Ok () else Error (parse_aws_error None)
  | UpdateSecret ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (UpdateSecretResponse.of_json json)
      else Error (parse_aws_error (Some UpdateSecretResponse.error_of_json))
  | UpdateSecretVersionStage ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (UpdateSecretVersionStageResponse.of_json json)
      else
        Error
          (parse_aws_error
             (Some UpdateSecretVersionStageResponse.error_of_json))
  | ValidateResourcePolicy ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ValidateResourcePolicyResponse.of_json json)
      else
        Error
          (parse_aws_error
             (Some ValidateResourcePolicyResponse.error_of_json))
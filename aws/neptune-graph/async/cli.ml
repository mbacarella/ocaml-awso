(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let cancel_export_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskIdentifier =
         flag "task-identifier" (required string) ~doc:"STRING ExportTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_export_task
           (Values.CancelExportTaskInput.make ~taskIdentifier ())
           (Some Values.CancelExportTaskOutput.to_json)
           (Some Values.CancelExportTaskOutput.error_to_json)])
let cancel_import_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskIdentifier =
         flag "task-identifier" (required string) ~doc:"STRING TaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_import_task
           (Values.CancelImportTaskInput.make ~taskIdentifier ())
           (Some Values.CancelImportTaskOutput.to_json)
           (Some Values.CancelImportTaskOutput.error_to_json)])
let cancel_query =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and graphIdentifier =
         flag "graph-identifier" (required string)
           ~doc:"STRING GraphIdentifier"
       and queryId = flag "query-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_query
           (Values.CancelQueryInput.make ~graphIdentifier ~queryId ()) None
           None])
let create_graph =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and publicConnectivity =
         flag "public-connectivity" (optional bool) ~doc:"BOOL Boolean"
       and kmsKeyIdentifier =
         flag "kms-key-identifier" (optional string) ~doc:"STRING KmsKeyArn"
       and vectorSearchConfiguration =
         flag "vector-search-configuration" (optional json_arg)
           ~doc:"JSON VectorSearchConfiguration"
       and replicaCount =
         flag "replica-count" (optional int) ~doc:"INT ReplicaCount"
       and deletionProtection =
         flag "deletion-protection" (optional bool) ~doc:"BOOL Boolean"
       and graphName =
         flag "graph-name" (required string) ~doc:"STRING GraphName"
       and provisionedMemory =
         flag "provisioned-memory" (required int)
           ~doc:"INT ProvisionedMemory" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_graph
           (Values.CreateGraphInput.make
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?publicConnectivity ?kmsKeyIdentifier
              ?vectorSearchConfiguration:(Option.map
                                            ~f:Values.VectorSearchConfiguration.of_json
                                            vectorSearchConfiguration)
              ?replicaCount ?deletionProtection ~graphName ~provisionedMemory
              ()) (Some Values.CreateGraphOutput.to_json)
           (Some Values.CreateGraphOutput.error_to_json)])
let create_graph_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and graphIdentifier =
         flag "graph-identifier" (required string)
           ~doc:"STRING GraphIdentifier"
       and snapshotName =
         flag "snapshot-name" (required string) ~doc:"STRING SnapshotName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_graph_snapshot
           (Values.CreateGraphSnapshotInput.make
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~graphIdentifier ~snapshotName ())
           (Some Values.CreateGraphSnapshotOutput.to_json)
           (Some Values.CreateGraphSnapshotOutput.error_to_json)])
let create_graph_using_import_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and publicConnectivity =
         flag "public-connectivity" (optional bool) ~doc:"BOOL Boolean"
       and kmsKeyIdentifier =
         flag "kms-key-identifier" (optional string) ~doc:"STRING KmsKeyArn"
       and vectorSearchConfiguration =
         flag "vector-search-configuration" (optional json_arg)
           ~doc:"JSON VectorSearchConfiguration"
       and replicaCount =
         flag "replica-count" (optional int) ~doc:"INT ReplicaCount"
       and deletionProtection =
         flag "deletion-protection" (optional bool) ~doc:"BOOL Boolean"
       and importOptions =
         flag "import-options" (optional json_arg) ~doc:"JSON ImportOptions"
       and maxProvisionedMemory =
         flag "max-provisioned-memory" (optional int)
           ~doc:"INT ProvisionedMemory"
       and minProvisionedMemory =
         flag "min-provisioned-memory" (optional int)
           ~doc:"INT ProvisionedMemory"
       and failOnError =
         flag "fail-on-error" (optional bool) ~doc:"BOOL Boolean"
       and format = flag "format" (optional json_arg) ~doc:"JSON Format"
       and parquetType =
         flag "parquet-type" (optional json_arg) ~doc:"JSON ParquetType"
       and blankNodeHandling =
         flag "blank-node-handling" (optional json_arg)
           ~doc:"JSON BlankNodeHandling"
       and graphName =
         flag "graph-name" (required string) ~doc:"STRING GraphName"
       and source = flag "source" (required string) ~doc:"STRING String"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_graph_using_import_task
           (Values.CreateGraphUsingImportTaskInput.make
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?publicConnectivity ?kmsKeyIdentifier
              ?vectorSearchConfiguration:(Option.map
                                            ~f:Values.VectorSearchConfiguration.of_json
                                            vectorSearchConfiguration)
              ?replicaCount ?deletionProtection
              ?importOptions:(Option.map ~f:Values.ImportOptions.of_json
                                importOptions) ?maxProvisionedMemory
              ?minProvisionedMemory ?failOnError
              ?format:(Option.map ~f:Values.Format_.of_json format)
              ?parquetType:(Option.map ~f:Values.ParquetType.of_json
                              parquetType)
              ?blankNodeHandling:(Option.map
                                    ~f:Values.BlankNodeHandling.of_json
                                    blankNodeHandling) ~graphName ~source
              ~roleArn ())
           (Some Values.CreateGraphUsingImportTaskOutput.to_json)
           (Some Values.CreateGraphUsingImportTaskOutput.error_to_json)])
let create_private_graph_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vpcId = flag "vpc-id" (optional string) ~doc:"STRING VpcId"
       and subnetIds =
         flag "subnet-ids" (optional json_arg) ~doc:"JSON SubnetIds"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON SecurityGroupIds"
       and graphIdentifier =
         flag "graph-identifier" (required string)
           ~doc:"STRING GraphIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_private_graph_endpoint
           (Values.CreatePrivateGraphEndpointInput.make ?vpcId
              ?subnetIds:(Option.map ~f:Values.SubnetIds.of_json subnetIds)
              ?vpcSecurityGroupIds:(Option.map
                                      ~f:Values.SecurityGroupIds.of_json
                                      vpcSecurityGroupIds) ~graphIdentifier
              ()) (Some Values.CreatePrivateGraphEndpointOutput.to_json)
           (Some Values.CreatePrivateGraphEndpointOutput.error_to_json)])
let delete_graph =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and graphIdentifier =
         flag "graph-identifier" (required string)
           ~doc:"STRING GraphIdentifier"
       and skipSnapshot =
         flag "skip-snapshot" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_graph
           (Values.DeleteGraphInput.make ~graphIdentifier ~skipSnapshot ())
           (Some Values.DeleteGraphOutput.to_json)
           (Some Values.DeleteGraphOutput.error_to_json)])
let delete_graph_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snapshotIdentifier =
         flag "snapshot-identifier" (required string)
           ~doc:"STRING SnapshotIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_graph_snapshot
           (Values.DeleteGraphSnapshotInput.make ~snapshotIdentifier ())
           (Some Values.DeleteGraphSnapshotOutput.to_json)
           (Some Values.DeleteGraphSnapshotOutput.error_to_json)])
let delete_private_graph_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and graphIdentifier =
         flag "graph-identifier" (required string)
           ~doc:"STRING GraphIdentifier"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_private_graph_endpoint
           (Values.DeletePrivateGraphEndpointInput.make ~graphIdentifier
              ~vpcId ())
           (Some Values.DeletePrivateGraphEndpointOutput.to_json)
           (Some Values.DeletePrivateGraphEndpointOutput.error_to_json)])
let execute_query =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON DocumentValuedMap"
       and planCache =
         flag "plan-cache" (optional json_arg) ~doc:"JSON PlanCacheType"
       and explainMode =
         flag "explain-mode" (optional json_arg) ~doc:"JSON ExplainMode"
       and queryTimeoutMilliseconds =
         flag "query-timeout-milliseconds" (optional int) ~doc:"INT Integer"
       and graphIdentifier =
         flag "graph-identifier" (required string)
           ~doc:"STRING GraphIdentifier"
       and queryString =
         flag "query-string" (required string) ~doc:"STRING String"
       and language =
         flag "language" (required json_arg) ~doc:"JSON QueryLanguage" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.execute_query
           (Values.ExecuteQueryInput.make
              ?parameters:(Option.map ~f:Values.DocumentValuedMap.of_json
                             parameters)
              ?planCache:(Option.map ~f:Values.PlanCacheType.of_json
                            planCache)
              ?explainMode:(Option.map ~f:Values.ExplainMode.of_json
                              explainMode) ?queryTimeoutMilliseconds
              ~graphIdentifier ~queryString
              ~language:(Values.QueryLanguage.of_json language) ())
           (Some Values.ExecuteQueryOutput.to_json)
           (Some Values.ExecuteQueryOutput.error_to_json)])
let get_export_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskIdentifier =
         flag "task-identifier" (required string) ~doc:"STRING ExportTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_export_task
           (Values.GetExportTaskInput.make ~taskIdentifier ())
           (Some Values.GetExportTaskOutput.to_json)
           (Some Values.GetExportTaskOutput.error_to_json)])
let get_graph =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and graphIdentifier =
         flag "graph-identifier" (required string)
           ~doc:"STRING GraphIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_graph (Values.GetGraphInput.make ~graphIdentifier ())
           (Some Values.GetGraphOutput.to_json)
           (Some Values.GetGraphOutput.error_to_json)])
let get_graph_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snapshotIdentifier =
         flag "snapshot-identifier" (required string)
           ~doc:"STRING SnapshotIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_graph_snapshot
           (Values.GetGraphSnapshotInput.make ~snapshotIdentifier ())
           (Some Values.GetGraphSnapshotOutput.to_json)
           (Some Values.GetGraphSnapshotOutput.error_to_json)])
let get_graph_summary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mode =
         flag "mode" (optional json_arg) ~doc:"JSON GraphSummaryMode"
       and graphIdentifier =
         flag "graph-identifier" (required string)
           ~doc:"STRING GraphIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_graph_summary
           (Values.GetGraphSummaryInput.make
              ?mode:(Option.map ~f:Values.GraphSummaryMode.of_json mode)
              ~graphIdentifier ())
           (Some Values.GetGraphSummaryOutput.to_json)
           (Some Values.GetGraphSummaryOutput.error_to_json)])
let get_import_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskIdentifier =
         flag "task-identifier" (required string) ~doc:"STRING TaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_import_task
           (Values.GetImportTaskInput.make ~taskIdentifier ())
           (Some Values.GetImportTaskOutput.to_json)
           (Some Values.GetImportTaskOutput.error_to_json)])
let get_private_graph_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and graphIdentifier =
         flag "graph-identifier" (required string)
           ~doc:"STRING GraphIdentifier"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_private_graph_endpoint
           (Values.GetPrivateGraphEndpointInput.make ~graphIdentifier ~vpcId
              ()) (Some Values.GetPrivateGraphEndpointOutput.to_json)
           (Some Values.GetPrivateGraphEndpointOutput.error_to_json)])
let get_query =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and graphIdentifier =
         flag "graph-identifier" (required string)
           ~doc:"STRING GraphIdentifier"
       and queryId = flag "query-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_query
           (Values.GetQueryInput.make ~graphIdentifier ~queryId ())
           (Some Values.GetQueryOutput.to_json)
           (Some Values.GetQueryOutput.error_to_json)])
let list_export_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and graphIdentifier =
         flag "graph-identifier" (optional string)
           ~doc:"STRING GraphIdentifier"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_export_tasks
           (Values.ListExportTasksInput.make ?graphIdentifier ?nextToken
              ?maxResults ()) (Some Values.ListExportTasksOutput.to_json)
           (Some Values.ListExportTasksOutput.error_to_json)])
let list_graph_snapshots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and graphIdentifier =
         flag "graph-identifier" (optional string)
           ~doc:"STRING GraphIdentifier"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_graph_snapshots
           (Values.ListGraphSnapshotsInput.make ?graphIdentifier ?nextToken
              ?maxResults ()) (Some Values.ListGraphSnapshotsOutput.to_json)
           (Some Values.ListGraphSnapshotsOutput.error_to_json)])
let list_graphs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_graphs
           (Values.ListGraphsInput.make ?nextToken ?maxResults ())
           (Some Values.ListGraphsOutput.to_json)
           (Some Values.ListGraphsOutput.error_to_json)])
let list_import_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_import_tasks
           (Values.ListImportTasksInput.make ?nextToken ?maxResults ())
           (Some Values.ListImportTasksOutput.to_json)
           (Some Values.ListImportTasksOutput.error_to_json)])
let list_private_graph_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and graphIdentifier =
         flag "graph-identifier" (required string)
           ~doc:"STRING GraphIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_private_graph_endpoints
           (Values.ListPrivateGraphEndpointsInput.make ?nextToken ?maxResults
              ~graphIdentifier ())
           (Some Values.ListPrivateGraphEndpointsOutput.to_json)
           (Some Values.ListPrivateGraphEndpointsOutput.error_to_json)])
let list_queries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and state =
         flag "state" (optional json_arg) ~doc:"JSON QueryStateInput"
       and graphIdentifier =
         flag "graph-identifier" (required string)
           ~doc:"STRING GraphIdentifier"
       and maxResults = flag "max-results" (required int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_queries
           (Values.ListQueriesInput.make
              ?state:(Option.map ~f:Values.QueryStateInput.of_json state)
              ~graphIdentifier ~maxResults ())
           (Some Values.ListQueriesOutput.to_json)
           (Some Values.ListQueriesOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let reset_graph =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and graphIdentifier =
         flag "graph-identifier" (required string)
           ~doc:"STRING GraphIdentifier"
       and skipSnapshot =
         flag "skip-snapshot" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reset_graph
           (Values.ResetGraphInput.make ~graphIdentifier ~skipSnapshot ())
           (Some Values.ResetGraphOutput.to_json)
           (Some Values.ResetGraphOutput.error_to_json)])
let restore_graph_from_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and provisionedMemory =
         flag "provisioned-memory" (optional int)
           ~doc:"INT ProvisionedMemory"
       and deletionProtection =
         flag "deletion-protection" (optional bool) ~doc:"BOOL Boolean"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and replicaCount =
         flag "replica-count" (optional int) ~doc:"INT ReplicaCount"
       and publicConnectivity =
         flag "public-connectivity" (optional bool) ~doc:"BOOL Boolean"
       and snapshotIdentifier =
         flag "snapshot-identifier" (required string)
           ~doc:"STRING SnapshotIdentifier"
       and graphName =
         flag "graph-name" (required string) ~doc:"STRING GraphName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_graph_from_snapshot
           (Values.RestoreGraphFromSnapshotInput.make ?provisionedMemory
              ?deletionProtection
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ?replicaCount
              ?publicConnectivity ~snapshotIdentifier ~graphName ())
           (Some Values.RestoreGraphFromSnapshotOutput.to_json)
           (Some Values.RestoreGraphFromSnapshotOutput.error_to_json)])
let start_export_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parquetType =
         flag "parquet-type" (optional json_arg) ~doc:"JSON ParquetType"
       and exportFilter =
         flag "export-filter" (optional json_arg) ~doc:"JSON ExportFilter"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and graphIdentifier =
         flag "graph-identifier" (required string)
           ~doc:"STRING GraphIdentifier"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn"
       and format =
         flag "format" (required json_arg) ~doc:"JSON ExportFormat"
       and destination =
         flag "destination" (required string)
           ~doc:"STRING StartExportTaskInputDestinationString"
       and kmsKeyIdentifier =
         flag "kms-key-identifier" (required string) ~doc:"STRING KmsKeyArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_export_task
           (Values.StartExportTaskInput.make
              ?parquetType:(Option.map ~f:Values.ParquetType.of_json
                              parquetType)
              ?exportFilter:(Option.map ~f:Values.ExportFilter.of_json
                               exportFilter)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~graphIdentifier ~roleArn
              ~format:(Values.ExportFormat.of_json format) ~destination
              ~kmsKeyIdentifier ())
           (Some Values.StartExportTaskOutput.to_json)
           (Some Values.StartExportTaskOutput.error_to_json)])
let start_graph =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and graphIdentifier =
         flag "graph-identifier" (required string)
           ~doc:"STRING GraphIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_graph (Values.StartGraphInput.make ~graphIdentifier ())
           (Some Values.StartGraphOutput.to_json)
           (Some Values.StartGraphOutput.error_to_json)])
let start_import_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and importOptions =
         flag "import-options" (optional json_arg) ~doc:"JSON ImportOptions"
       and failOnError =
         flag "fail-on-error" (optional bool) ~doc:"BOOL Boolean"
       and format = flag "format" (optional json_arg) ~doc:"JSON Format"
       and parquetType =
         flag "parquet-type" (optional json_arg) ~doc:"JSON ParquetType"
       and blankNodeHandling =
         flag "blank-node-handling" (optional json_arg)
           ~doc:"JSON BlankNodeHandling"
       and source = flag "source" (required string) ~doc:"STRING String"
       and graphIdentifier =
         flag "graph-identifier" (required string)
           ~doc:"STRING GraphIdentifier"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_import_task
           (Values.StartImportTaskInput.make
              ?importOptions:(Option.map ~f:Values.ImportOptions.of_json
                                importOptions) ?failOnError
              ?format:(Option.map ~f:Values.Format_.of_json format)
              ?parquetType:(Option.map ~f:Values.ParquetType.of_json
                              parquetType)
              ?blankNodeHandling:(Option.map
                                    ~f:Values.BlankNodeHandling.of_json
                                    blankNodeHandling) ~source
              ~graphIdentifier ~roleArn ())
           (Some Values.StartImportTaskOutput.to_json)
           (Some Values.StartImportTaskOutput.error_to_json)])
let stop_graph =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and graphIdentifier =
         flag "graph-identifier" (required string)
           ~doc:"STRING GraphIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_graph (Values.StopGraphInput.make ~graphIdentifier ())
           (Some Values.StopGraphOutput.to_json)
           (Some Values.StopGraphOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceOutput.to_json)
           (Some Values.TagResourceOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceOutput.to_json)
           (Some Values.UntagResourceOutput.error_to_json)])
let update_graph =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and publicConnectivity =
         flag "public-connectivity" (optional bool) ~doc:"BOOL Boolean"
       and provisionedMemory =
         flag "provisioned-memory" (optional int)
           ~doc:"INT ProvisionedMemory"
       and deletionProtection =
         flag "deletion-protection" (optional bool) ~doc:"BOOL Boolean"
       and graphIdentifier =
         flag "graph-identifier" (required string)
           ~doc:"STRING GraphIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_graph
           (Values.UpdateGraphInput.make ?publicConnectivity
              ?provisionedMemory ?deletionProtection ~graphIdentifier ())
           (Some Values.UpdateGraphOutput.to_json)
           (Some Values.UpdateGraphOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("cancel-export-task", cancel_export_task);
    ("cancel-import-task", cancel_import_task);
    ("cancel-query", cancel_query);
    ("create-graph", create_graph);
    ("create-graph-snapshot", create_graph_snapshot);
    ("create-graph-using-import-task", create_graph_using_import_task);
    ("create-private-graph-endpoint", create_private_graph_endpoint);
    ("delete-graph", delete_graph);
    ("delete-graph-snapshot", delete_graph_snapshot);
    ("delete-private-graph-endpoint", delete_private_graph_endpoint);
    ("execute-query", execute_query);
    ("get-export-task", get_export_task);
    ("get-graph", get_graph);
    ("get-graph-snapshot", get_graph_snapshot);
    ("get-graph-summary", get_graph_summary);
    ("get-import-task", get_import_task);
    ("get-private-graph-endpoint", get_private_graph_endpoint);
    ("get-query", get_query);
    ("list-export-tasks", list_export_tasks);
    ("list-graph-snapshots", list_graph_snapshots);
    ("list-graphs", list_graphs);
    ("list-import-tasks", list_import_tasks);
    ("list-private-graph-endpoints", list_private_graph_endpoints);
    ("list-queries", list_queries);
    ("list-tags-for-resource", list_tags_for_resource);
    ("reset-graph", reset_graph);
    ("restore-graph-from-snapshot", restore_graph_from_snapshot);
    ("start-export-task", start_export_task);
    ("start-graph", start_graph);
    ("start-import-task", start_import_task);
    ("stop-graph", stop_graph);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-graph", update_graph)]
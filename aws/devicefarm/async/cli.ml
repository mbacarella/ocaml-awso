(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_device_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Message"
       and maxDevices = flag "max-devices" (optional int) ~doc:"INT Integer"
       and projectArn =
         flag "project-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and rules = flag "rules" (required json_arg) ~doc:"JSON Rules" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_device_pool
           (Values.CreateDevicePoolRequest.make ?description ?maxDevices
              ~projectArn ~name ~rules:(Values.Rules.of_json rules) ())
           (Some Values.CreateDevicePoolResult.to_json)
           (Some Values.CreateDevicePoolResult.error_to_json)])
let create_instance_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Message"
       and packageCleanup =
         flag "package-cleanup" (optional bool) ~doc:"BOOL Boolean"
       and excludeAppPackagesFromCleanup =
         flag "exclude-app-packages-from-cleanup" (optional json_arg)
           ~doc:"JSON PackageIds"
       and rebootAfterUse =
         flag "reboot-after-use" (optional bool) ~doc:"BOOL Boolean"
       and name = flag "name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_instance_profile
           (Values.CreateInstanceProfileRequest.make ?description
              ?packageCleanup
              ?excludeAppPackagesFromCleanup:(Option.map
                                                ~f:Values.PackageIds.of_json
                                                excludeAppPackagesFromCleanup)
              ?rebootAfterUse ~name ())
           (Some Values.CreateInstanceProfileResult.to_json)
           (Some Values.CreateInstanceProfileResult.error_to_json)])
let create_network_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Message"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON NetworkProfileType"
       and uplinkBandwidthBits =
         flag "uplink-bandwidth-bits" (optional json_arg) ~doc:"JSON Long"
       and downlinkBandwidthBits =
         flag "downlink-bandwidth-bits" (optional json_arg) ~doc:"JSON Long"
       and uplinkDelayMs =
         flag "uplink-delay-ms" (optional json_arg) ~doc:"JSON Long"
       and downlinkDelayMs =
         flag "downlink-delay-ms" (optional json_arg) ~doc:"JSON Long"
       and uplinkJitterMs =
         flag "uplink-jitter-ms" (optional json_arg) ~doc:"JSON Long"
       and downlinkJitterMs =
         flag "downlink-jitter-ms" (optional json_arg) ~doc:"JSON Long"
       and uplinkLossPercent =
         flag "uplink-loss-percent" (optional int) ~doc:"INT PercentInteger"
       and downlinkLossPercent =
         flag "downlink-loss-percent" (optional int)
           ~doc:"INT PercentInteger"
       and projectArn =
         flag "project-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and name = flag "name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_network_profile
           (Values.CreateNetworkProfileRequest.make ?description
              ?type_:(Option.map ~f:Values.NetworkProfileType.of_json type_)
              ?uplinkBandwidthBits:(Option.map ~f:Values.Long.of_json
                                      uplinkBandwidthBits)
              ?downlinkBandwidthBits:(Option.map ~f:Values.Long.of_json
                                        downlinkBandwidthBits)
              ?uplinkDelayMs:(Option.map ~f:Values.Long.of_json uplinkDelayMs)
              ?downlinkDelayMs:(Option.map ~f:Values.Long.of_json
                                  downlinkDelayMs)
              ?uplinkJitterMs:(Option.map ~f:Values.Long.of_json
                                 uplinkJitterMs)
              ?downlinkJitterMs:(Option.map ~f:Values.Long.of_json
                                   downlinkJitterMs) ?uplinkLossPercent
              ?downlinkLossPercent ~projectArn ~name ())
           (Some Values.CreateNetworkProfileResult.to_json)
           (Some Values.CreateNetworkProfileResult.error_to_json)])
let create_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and defaultJobTimeoutMinutes =
         flag "default-job-timeout-minutes" (optional int)
           ~doc:"INT JobTimeoutMinutes"
       and name = flag "name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_project
           (Values.CreateProjectRequest.make ?defaultJobTimeoutMinutes ~name
              ()) (Some Values.CreateProjectResult.to_json)
           (Some Values.CreateProjectResult.error_to_json)])
let create_remote_access_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceArn =
         flag "instance-arn" (optional string)
           ~doc:"STRING AmazonResourceName"
       and sshPublicKey =
         flag "ssh-public-key" (optional string) ~doc:"STRING SshPublicKey"
       and remoteDebugEnabled =
         flag "remote-debug-enabled" (optional bool) ~doc:"BOOL Boolean"
       and remoteRecordEnabled =
         flag "remote-record-enabled" (optional bool) ~doc:"BOOL Boolean"
       and remoteRecordAppArn =
         flag "remote-record-app-arn" (optional string)
           ~doc:"STRING AmazonResourceName"
       and name = flag "name" (optional string) ~doc:"STRING Name"
       and clientId =
         flag "client-id" (optional string) ~doc:"STRING ClientId"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON CreateRemoteAccessSessionConfiguration"
       and interactionMode =
         flag "interaction-mode" (optional json_arg)
           ~doc:"JSON InteractionMode"
       and skipAppResign =
         flag "skip-app-resign" (optional bool) ~doc:"BOOL Boolean"
       and projectArn =
         flag "project-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and deviceArn =
         flag "device-arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_remote_access_session
           (Values.CreateRemoteAccessSessionRequest.make ?instanceArn
              ?sshPublicKey ?remoteDebugEnabled ?remoteRecordEnabled
              ?remoteRecordAppArn ?name ?clientId
              ?configuration:(Option.map
                                ~f:Values.CreateRemoteAccessSessionConfiguration.of_json
                                configuration)
              ?interactionMode:(Option.map ~f:Values.InteractionMode.of_json
                                  interactionMode) ?skipAppResign ~projectArn
              ~deviceArn ())
           (Some Values.CreateRemoteAccessSessionResult.to_json)
           (Some Values.CreateRemoteAccessSessionResult.error_to_json)])
let create_test_grid_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON TestGridVpcConfig"
       and name = flag "name" (required string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_test_grid_project
           (Values.CreateTestGridProjectRequest.make ?description
              ?vpcConfig:(Option.map ~f:Values.TestGridVpcConfig.of_json
                            vpcConfig) ~name ())
           (Some Values.CreateTestGridProjectResult.to_json)
           (Some Values.CreateTestGridProjectResult.error_to_json)])
let create_test_grid_url =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectArn =
         flag "project-arn" (required string) ~doc:"STRING DeviceFarmArn"
       and expiresInSeconds =
         flag "expires-in-seconds" (required int)
           ~doc:"INT TestGridUrlExpiresInSecondsInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_test_grid_url
           (Values.CreateTestGridUrlRequest.make ~projectArn
              ~expiresInSeconds ())
           (Some Values.CreateTestGridUrlResult.to_json)
           (Some Values.CreateTestGridUrlResult.error_to_json)])
let create_upload =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentType =
         flag "content-type" (optional string) ~doc:"STRING ContentType"
       and projectArn =
         flag "project-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON UploadType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_upload
           (Values.CreateUploadRequest.make ?contentType ~projectArn ~name
              ~type_:(Values.UploadType.of_json type_) ())
           (Some Values.CreateUploadResult.to_json)
           (Some Values.CreateUploadResult.error_to_json)])
let create_v_p_c_e_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vpceConfigurationDescription =
         flag "vpce-configuration-description" (optional string)
           ~doc:"STRING VPCEConfigurationDescription"
       and vpceConfigurationName =
         flag "vpce-configuration-name" (required string)
           ~doc:"STRING VPCEConfigurationName"
       and vpceServiceName =
         flag "vpce-service-name" (required string)
           ~doc:"STRING VPCEServiceName"
       and serviceDnsName =
         flag "service-dns-name" (required string)
           ~doc:"STRING ServiceDnsName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_v_p_c_e_configuration
           (Values.CreateVPCEConfigurationRequest.make
              ?vpceConfigurationDescription ~vpceConfigurationName
              ~vpceServiceName ~serviceDnsName ())
           (Some Values.CreateVPCEConfigurationResult.to_json)
           (Some Values.CreateVPCEConfigurationResult.error_to_json)])
let delete_device_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_device_pool
           (Values.DeleteDevicePoolRequest.make ~arn ())
           (Some Values.DeleteDevicePoolResult.to_json)
           (Some Values.DeleteDevicePoolResult.error_to_json)])
let delete_instance_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_instance_profile
           (Values.DeleteInstanceProfileRequest.make ~arn ())
           (Some Values.DeleteInstanceProfileResult.to_json)
           (Some Values.DeleteInstanceProfileResult.error_to_json)])
let delete_network_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_network_profile
           (Values.DeleteNetworkProfileRequest.make ~arn ())
           (Some Values.DeleteNetworkProfileResult.to_json)
           (Some Values.DeleteNetworkProfileResult.error_to_json)])
let delete_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_project (Values.DeleteProjectRequest.make ~arn ())
           (Some Values.DeleteProjectResult.to_json)
           (Some Values.DeleteProjectResult.error_to_json)])
let delete_remote_access_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_remote_access_session
           (Values.DeleteRemoteAccessSessionRequest.make ~arn ())
           (Some Values.DeleteRemoteAccessSessionResult.to_json)
           (Some Values.DeleteRemoteAccessSessionResult.error_to_json)])
let delete_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_run (Values.DeleteRunRequest.make ~arn ())
           (Some Values.DeleteRunResult.to_json)
           (Some Values.DeleteRunResult.error_to_json)])
let delete_test_grid_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectArn =
         flag "project-arn" (required string) ~doc:"STRING DeviceFarmArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_test_grid_project
           (Values.DeleteTestGridProjectRequest.make ~projectArn ())
           (Some Values.DeleteTestGridProjectResult.to_json)
           (Some Values.DeleteTestGridProjectResult.error_to_json)])
let delete_upload =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_upload (Values.DeleteUploadRequest.make ~arn ())
           (Some Values.DeleteUploadResult.to_json)
           (Some Values.DeleteUploadResult.error_to_json)])
let delete_v_p_c_e_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_v_p_c_e_configuration
           (Values.DeleteVPCEConfigurationRequest.make ~arn ())
           (Some Values.DeleteVPCEConfigurationResult.to_json)
           (Some Values.DeleteVPCEConfigurationResult.error_to_json)])
let get_account_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_account_settings (Values.GetAccountSettingsRequest.make ())
           (Some Values.GetAccountSettingsResult.to_json)
           (Some Values.GetAccountSettingsResult.error_to_json)])
let get_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_device (Values.GetDeviceRequest.make ~arn ())
           (Some Values.GetDeviceResult.to_json)
           (Some Values.GetDeviceResult.error_to_json)])
let get_device_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_device_instance
           (Values.GetDeviceInstanceRequest.make ~arn ())
           (Some Values.GetDeviceInstanceResult.to_json)
           (Some Values.GetDeviceInstanceResult.error_to_json)])
let get_device_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_device_pool (Values.GetDevicePoolRequest.make ~arn ())
           (Some Values.GetDevicePoolResult.to_json)
           (Some Values.GetDevicePoolResult.error_to_json)])
let get_device_pool_compatibility =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appArn =
         flag "app-arn" (optional string) ~doc:"STRING AmazonResourceName"
       and testType =
         flag "test-type" (optional json_arg) ~doc:"JSON TestType"
       and test = flag "test" (optional json_arg) ~doc:"JSON ScheduleRunTest"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON ScheduleRunConfiguration"
       and devicePoolArn =
         flag "device-pool-arn" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_device_pool_compatibility
           (Values.GetDevicePoolCompatibilityRequest.make ?appArn
              ?testType:(Option.map ~f:Values.TestType.of_json testType)
              ?test:(Option.map ~f:Values.ScheduleRunTest.of_json test)
              ?configuration:(Option.map
                                ~f:Values.ScheduleRunConfiguration.of_json
                                configuration) ~devicePoolArn ())
           (Some Values.GetDevicePoolCompatibilityResult.to_json)
           (Some Values.GetDevicePoolCompatibilityResult.error_to_json)])
let get_instance_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_instance_profile
           (Values.GetInstanceProfileRequest.make ~arn ())
           (Some Values.GetInstanceProfileResult.to_json)
           (Some Values.GetInstanceProfileResult.error_to_json)])
let get_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_job (Values.GetJobRequest.make ~arn ())
           (Some Values.GetJobResult.to_json)
           (Some Values.GetJobResult.error_to_json)])
let get_network_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_network_profile
           (Values.GetNetworkProfileRequest.make ~arn ())
           (Some Values.GetNetworkProfileResult.to_json)
           (Some Values.GetNetworkProfileResult.error_to_json)])
let get_offering_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_offering_status
           (Values.GetOfferingStatusRequest.make ?nextToken ())
           (Some Values.GetOfferingStatusResult.to_json)
           (Some Values.GetOfferingStatusResult.error_to_json)])
let get_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_project (Values.GetProjectRequest.make ~arn ())
           (Some Values.GetProjectResult.to_json)
           (Some Values.GetProjectResult.error_to_json)])
let get_remote_access_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_remote_access_session
           (Values.GetRemoteAccessSessionRequest.make ~arn ())
           (Some Values.GetRemoteAccessSessionResult.to_json)
           (Some Values.GetRemoteAccessSessionResult.error_to_json)])
let get_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_run (Values.GetRunRequest.make ~arn ())
           (Some Values.GetRunResult.to_json)
           (Some Values.GetRunResult.error_to_json)])
let get_suite =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_suite (Values.GetSuiteRequest.make ~arn ())
           (Some Values.GetSuiteResult.to_json)
           (Some Values.GetSuiteResult.error_to_json)])
let get_test =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_test (Values.GetTestRequest.make ~arn ())
           (Some Values.GetTestResult.to_json)
           (Some Values.GetTestResult.error_to_json)])
let get_test_grid_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectArn =
         flag "project-arn" (required string) ~doc:"STRING DeviceFarmArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_test_grid_project
           (Values.GetTestGridProjectRequest.make ~projectArn ())
           (Some Values.GetTestGridProjectResult.to_json)
           (Some Values.GetTestGridProjectResult.error_to_json)])
let get_test_grid_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectArn =
         flag "project-arn" (optional string) ~doc:"STRING DeviceFarmArn"
       and sessionId =
         flag "session-id" (optional string) ~doc:"STRING ResourceId"
       and sessionArn =
         flag "session-arn" (optional string) ~doc:"STRING DeviceFarmArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_test_grid_session
           (Values.GetTestGridSessionRequest.make ?projectArn ?sessionId
              ?sessionArn ()) (Some Values.GetTestGridSessionResult.to_json)
           (Some Values.GetTestGridSessionResult.error_to_json)])
let get_upload =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_upload (Values.GetUploadRequest.make ~arn ())
           (Some Values.GetUploadResult.to_json)
           (Some Values.GetUploadResult.error_to_json)])
let get_v_p_c_e_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_v_p_c_e_configuration
           (Values.GetVPCEConfigurationRequest.make ~arn ())
           (Some Values.GetVPCEConfigurationResult.to_json)
           (Some Values.GetVPCEConfigurationResult.error_to_json)])
let install_to_remote_access_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and remoteAccessSessionArn =
         flag "remote-access-session-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and appArn =
         flag "app-arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.install_to_remote_access_session
           (Values.InstallToRemoteAccessSessionRequest.make
              ~remoteAccessSessionArn ~appArn ())
           (Some Values.InstallToRemoteAccessSessionResult.to_json)
           (Some Values.InstallToRemoteAccessSessionResult.error_to_json)])
let list_artifacts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON ArtifactCategory" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_artifacts
           (Values.ListArtifactsRequest.make ?nextToken ~arn
              ~type_:(Values.ArtifactCategory.of_json type_) ())
           (Some Values.ListArtifactsResult.to_json)
           (Some Values.ListArtifactsResult.error_to_json)])
let list_device_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_device_instances
           (Values.ListDeviceInstancesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListDeviceInstancesResult.to_json)
           (Some Values.ListDeviceInstancesResult.error_to_json)])
let list_device_pools =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON DevicePoolType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_device_pools
           (Values.ListDevicePoolsRequest.make
              ?type_:(Option.map ~f:Values.DevicePoolType.of_json type_)
              ?nextToken ~arn ()) (Some Values.ListDevicePoolsResult.to_json)
           (Some Values.ListDevicePoolsResult.error_to_json)])
let list_devices =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (optional string) ~doc:"STRING AmazonResourceName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON DeviceFilters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_devices
           (Values.ListDevicesRequest.make ?arn ?nextToken
              ?filters:(Option.map ~f:Values.DeviceFilters.of_json filters)
              ()) (Some Values.ListDevicesResult.to_json)
           (Some Values.ListDevicesResult.error_to_json)])
let list_instance_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_instance_profiles
           (Values.ListInstanceProfilesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListInstanceProfilesResult.to_json)
           (Some Values.ListInstanceProfilesResult.error_to_json)])
let list_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_jobs (Values.ListJobsRequest.make ?nextToken ~arn ())
           (Some Values.ListJobsResult.to_json)
           (Some Values.ListJobsResult.error_to_json)])
let list_network_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON NetworkProfileType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_network_profiles
           (Values.ListNetworkProfilesRequest.make
              ?type_:(Option.map ~f:Values.NetworkProfileType.of_json type_)
              ?nextToken ~arn ())
           (Some Values.ListNetworkProfilesResult.to_json)
           (Some Values.ListNetworkProfilesResult.error_to_json)])
let list_offering_promotions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_offering_promotions
           (Values.ListOfferingPromotionsRequest.make ?nextToken ())
           (Some Values.ListOfferingPromotionsResult.to_json)
           (Some Values.ListOfferingPromotionsResult.error_to_json)])
let list_offering_transactions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_offering_transactions
           (Values.ListOfferingTransactionsRequest.make ?nextToken ())
           (Some Values.ListOfferingTransactionsResult.to_json)
           (Some Values.ListOfferingTransactionsResult.error_to_json)])
let list_offerings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_offerings (Values.ListOfferingsRequest.make ?nextToken ())
           (Some Values.ListOfferingsResult.to_json)
           (Some Values.ListOfferingsResult.error_to_json)])
let list_projects =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (optional string) ~doc:"STRING AmazonResourceName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_projects
           (Values.ListProjectsRequest.make ?arn ?nextToken ())
           (Some Values.ListProjectsResult.to_json)
           (Some Values.ListProjectsResult.error_to_json)])
let list_remote_access_sessions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_remote_access_sessions
           (Values.ListRemoteAccessSessionsRequest.make ?nextToken ~arn ())
           (Some Values.ListRemoteAccessSessionsResult.to_json)
           (Some Values.ListRemoteAccessSessionsResult.error_to_json)])
let list_runs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_runs (Values.ListRunsRequest.make ?nextToken ~arn ())
           (Some Values.ListRunsResult.to_json)
           (Some Values.ListRunsResult.error_to_json)])
let list_samples =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_samples
           (Values.ListSamplesRequest.make ?nextToken ~arn ())
           (Some Values.ListSamplesResult.to_json)
           (Some Values.ListSamplesResult.error_to_json)])
let list_suites =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_suites (Values.ListSuitesRequest.make ?nextToken ~arn ())
           (Some Values.ListSuitesResult.to_json)
           (Some Values.ListSuitesResult.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING DeviceFarmArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_test_grid_projects =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResult =
         flag "max-result" (optional int) ~doc:"INT MaxPageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_test_grid_projects
           (Values.ListTestGridProjectsRequest.make ?maxResult ?nextToken ())
           (Some Values.ListTestGridProjectsResult.to_json)
           (Some Values.ListTestGridProjectsResult.error_to_json)])
let list_test_grid_session_actions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResult =
         flag "max-result" (optional int) ~doc:"INT MaxPageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and sessionArn =
         flag "session-arn" (required string) ~doc:"STRING DeviceFarmArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_test_grid_session_actions
           (Values.ListTestGridSessionActionsRequest.make ?maxResult
              ?nextToken ~sessionArn ())
           (Some Values.ListTestGridSessionActionsResult.to_json)
           (Some Values.ListTestGridSessionActionsResult.error_to_json)])
let list_test_grid_session_artifacts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ =
         flag "type-" (optional json_arg)
           ~doc:"JSON TestGridSessionArtifactCategory"
       and maxResult =
         flag "max-result" (optional int) ~doc:"INT MaxPageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and sessionArn =
         flag "session-arn" (required string) ~doc:"STRING DeviceFarmArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_test_grid_session_artifacts
           (Values.ListTestGridSessionArtifactsRequest.make
              ?type_:(Option.map
                        ~f:Values.TestGridSessionArtifactCategory.of_json
                        type_) ?maxResult ?nextToken ~sessionArn ())
           (Some Values.ListTestGridSessionArtifactsResult.to_json)
           (Some Values.ListTestGridSessionArtifactsResult.error_to_json)])
let list_test_grid_sessions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON TestGridSessionStatus"
       and creationTimeAfter =
         flag "creation-time-after" (optional json_arg) ~doc:"JSON DateTime"
       and creationTimeBefore =
         flag "creation-time-before" (optional json_arg) ~doc:"JSON DateTime"
       and endTimeAfter =
         flag "end-time-after" (optional json_arg) ~doc:"JSON DateTime"
       and endTimeBefore =
         flag "end-time-before" (optional json_arg) ~doc:"JSON DateTime"
       and maxResult =
         flag "max-result" (optional int) ~doc:"INT MaxPageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and projectArn =
         flag "project-arn" (required string) ~doc:"STRING DeviceFarmArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_test_grid_sessions
           (Values.ListTestGridSessionsRequest.make
              ?status:(Option.map ~f:Values.TestGridSessionStatus.of_json
                         status)
              ?creationTimeAfter:(Option.map ~f:Values.DateTime.of_json
                                    creationTimeAfter)
              ?creationTimeBefore:(Option.map ~f:Values.DateTime.of_json
                                     creationTimeBefore)
              ?endTimeAfter:(Option.map ~f:Values.DateTime.of_json
                               endTimeAfter)
              ?endTimeBefore:(Option.map ~f:Values.DateTime.of_json
                                endTimeBefore) ?maxResult ?nextToken
              ~projectArn ())
           (Some Values.ListTestGridSessionsResult.to_json)
           (Some Values.ListTestGridSessionsResult.error_to_json)])
let list_tests =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tests (Values.ListTestsRequest.make ?nextToken ~arn ())
           (Some Values.ListTestsResult.to_json)
           (Some Values.ListTestsResult.error_to_json)])
let list_unique_problems =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_unique_problems
           (Values.ListUniqueProblemsRequest.make ?nextToken ~arn ())
           (Some Values.ListUniqueProblemsResult.to_json)
           (Some Values.ListUniqueProblemsResult.error_to_json)])
let list_uploads =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON UploadType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_uploads
           (Values.ListUploadsRequest.make
              ?type_:(Option.map ~f:Values.UploadType.of_json type_)
              ?nextToken ~arn ()) (Some Values.ListUploadsResult.to_json)
           (Some Values.ListUploadsResult.error_to_json)])
let list_v_p_c_e_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_v_p_c_e_configurations
           (Values.ListVPCEConfigurationsRequest.make ?maxResults ?nextToken
              ()) (Some Values.ListVPCEConfigurationsResult.to_json)
           (Some Values.ListVPCEConfigurationsResult.error_to_json)])
let purchase_offering =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and offeringPromotionId =
         flag "offering-promotion-id" (optional string)
           ~doc:"STRING OfferingPromotionIdentifier"
       and offeringId =
         flag "offering-id" (required string)
           ~doc:"STRING OfferingIdentifier"
       and quantity = flag "quantity" (required int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.purchase_offering
           (Values.PurchaseOfferingRequest.make ?offeringPromotionId
              ~offeringId ~quantity ())
           (Some Values.PurchaseOfferingResult.to_json)
           (Some Values.PurchaseOfferingResult.error_to_json)])
let renew_offering =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and offeringId =
         flag "offering-id" (required string)
           ~doc:"STRING OfferingIdentifier"
       and quantity = flag "quantity" (required int) ~doc:"INT Integer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.renew_offering
           (Values.RenewOfferingRequest.make ~offeringId ~quantity ())
           (Some Values.RenewOfferingResult.to_json)
           (Some Values.RenewOfferingResult.error_to_json)])
let schedule_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appArn =
         flag "app-arn" (optional string) ~doc:"STRING AmazonResourceName"
       and devicePoolArn =
         flag "device-pool-arn" (optional string)
           ~doc:"STRING AmazonResourceName"
       and deviceSelectionConfiguration =
         flag "device-selection-configuration" (optional json_arg)
           ~doc:"JSON DeviceSelectionConfiguration"
       and name = flag "name" (optional string) ~doc:"STRING Name"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON ScheduleRunConfiguration"
       and executionConfiguration =
         flag "execution-configuration" (optional json_arg)
           ~doc:"JSON ExecutionConfiguration"
       and projectArn =
         flag "project-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and test = flag "test" (required json_arg) ~doc:"JSON ScheduleRunTest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.schedule_run
           (Values.ScheduleRunRequest.make ?appArn ?devicePoolArn
              ?deviceSelectionConfiguration:(Option.map
                                               ~f:Values.DeviceSelectionConfiguration.of_json
                                               deviceSelectionConfiguration)
              ?name
              ?configuration:(Option.map
                                ~f:Values.ScheduleRunConfiguration.of_json
                                configuration)
              ?executionConfiguration:(Option.map
                                         ~f:Values.ExecutionConfiguration.of_json
                                         executionConfiguration) ~projectArn
              ~test:(Values.ScheduleRunTest.of_json test) ())
           (Some Values.ScheduleRunResult.to_json)
           (Some Values.ScheduleRunResult.error_to_json)])
let stop_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_job (Values.StopJobRequest.make ~arn ())
           (Some Values.StopJobResult.to_json)
           (Some Values.StopJobResult.error_to_json)])
let stop_remote_access_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_remote_access_session
           (Values.StopRemoteAccessSessionRequest.make ~arn ())
           (Some Values.StopRemoteAccessSessionResult.to_json)
           (Some Values.StopRemoteAccessSessionResult.error_to_json)])
let stop_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_run (Values.StopRunRequest.make ~arn ())
           (Some Values.StopRunResult.to_json)
           (Some Values.StopRunResult.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING DeviceFarmArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING DeviceFarmArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_device_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileArn =
         flag "profile-arn" (optional string)
           ~doc:"STRING AmazonResourceName"
       and labels =
         flag "labels" (optional json_arg) ~doc:"JSON InstanceLabels"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_device_instance
           (Values.UpdateDeviceInstanceRequest.make ?profileArn
              ?labels:(Option.map ~f:Values.InstanceLabels.of_json labels)
              ~arn ()) (Some Values.UpdateDeviceInstanceResult.to_json)
           (Some Values.UpdateDeviceInstanceResult.error_to_json)])
let update_device_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING Name"
       and description =
         flag "description" (optional string) ~doc:"STRING Message"
       and rules = flag "rules" (optional json_arg) ~doc:"JSON Rules"
       and maxDevices = flag "max-devices" (optional int) ~doc:"INT Integer"
       and clearMaxDevices =
         flag "clear-max-devices" (optional bool) ~doc:"BOOL Boolean"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_device_pool
           (Values.UpdateDevicePoolRequest.make ?name ?description
              ?rules:(Option.map ~f:Values.Rules.of_json rules) ?maxDevices
              ?clearMaxDevices ~arn ())
           (Some Values.UpdateDevicePoolResult.to_json)
           (Some Values.UpdateDevicePoolResult.error_to_json)])
let update_instance_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING Name"
       and description =
         flag "description" (optional string) ~doc:"STRING Message"
       and packageCleanup =
         flag "package-cleanup" (optional bool) ~doc:"BOOL Boolean"
       and excludeAppPackagesFromCleanup =
         flag "exclude-app-packages-from-cleanup" (optional json_arg)
           ~doc:"JSON PackageIds"
       and rebootAfterUse =
         flag "reboot-after-use" (optional bool) ~doc:"BOOL Boolean"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_instance_profile
           (Values.UpdateInstanceProfileRequest.make ?name ?description
              ?packageCleanup
              ?excludeAppPackagesFromCleanup:(Option.map
                                                ~f:Values.PackageIds.of_json
                                                excludeAppPackagesFromCleanup)
              ?rebootAfterUse ~arn ())
           (Some Values.UpdateInstanceProfileResult.to_json)
           (Some Values.UpdateInstanceProfileResult.error_to_json)])
let update_network_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING Name"
       and description =
         flag "description" (optional string) ~doc:"STRING Message"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON NetworkProfileType"
       and uplinkBandwidthBits =
         flag "uplink-bandwidth-bits" (optional json_arg) ~doc:"JSON Long"
       and downlinkBandwidthBits =
         flag "downlink-bandwidth-bits" (optional json_arg) ~doc:"JSON Long"
       and uplinkDelayMs =
         flag "uplink-delay-ms" (optional json_arg) ~doc:"JSON Long"
       and downlinkDelayMs =
         flag "downlink-delay-ms" (optional json_arg) ~doc:"JSON Long"
       and uplinkJitterMs =
         flag "uplink-jitter-ms" (optional json_arg) ~doc:"JSON Long"
       and downlinkJitterMs =
         flag "downlink-jitter-ms" (optional json_arg) ~doc:"JSON Long"
       and uplinkLossPercent =
         flag "uplink-loss-percent" (optional int) ~doc:"INT PercentInteger"
       and downlinkLossPercent =
         flag "downlink-loss-percent" (optional int)
           ~doc:"INT PercentInteger"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_network_profile
           (Values.UpdateNetworkProfileRequest.make ?name ?description
              ?type_:(Option.map ~f:Values.NetworkProfileType.of_json type_)
              ?uplinkBandwidthBits:(Option.map ~f:Values.Long.of_json
                                      uplinkBandwidthBits)
              ?downlinkBandwidthBits:(Option.map ~f:Values.Long.of_json
                                        downlinkBandwidthBits)
              ?uplinkDelayMs:(Option.map ~f:Values.Long.of_json uplinkDelayMs)
              ?downlinkDelayMs:(Option.map ~f:Values.Long.of_json
                                  downlinkDelayMs)
              ?uplinkJitterMs:(Option.map ~f:Values.Long.of_json
                                 uplinkJitterMs)
              ?downlinkJitterMs:(Option.map ~f:Values.Long.of_json
                                   downlinkJitterMs) ?uplinkLossPercent
              ?downlinkLossPercent ~arn ())
           (Some Values.UpdateNetworkProfileResult.to_json)
           (Some Values.UpdateNetworkProfileResult.error_to_json)])
let update_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING Name"
       and defaultJobTimeoutMinutes =
         flag "default-job-timeout-minutes" (optional int)
           ~doc:"INT JobTimeoutMinutes"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_project
           (Values.UpdateProjectRequest.make ?name ?defaultJobTimeoutMinutes
              ~arn ()) (Some Values.UpdateProjectResult.to_json)
           (Some Values.UpdateProjectResult.error_to_json)])
let update_test_grid_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING ResourceName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON TestGridVpcConfig"
       and projectArn =
         flag "project-arn" (required string) ~doc:"STRING DeviceFarmArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_test_grid_project
           (Values.UpdateTestGridProjectRequest.make ?name ?description
              ?vpcConfig:(Option.map ~f:Values.TestGridVpcConfig.of_json
                            vpcConfig) ~projectArn ())
           (Some Values.UpdateTestGridProjectResult.to_json)
           (Some Values.UpdateTestGridProjectResult.error_to_json)])
let update_upload =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING Name"
       and contentType =
         flag "content-type" (optional string) ~doc:"STRING ContentType"
       and editContent =
         flag "edit-content" (optional bool) ~doc:"BOOL Boolean"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_upload
           (Values.UpdateUploadRequest.make ?name ?contentType ?editContent
              ~arn ()) (Some Values.UpdateUploadResult.to_json)
           (Some Values.UpdateUploadResult.error_to_json)])
let update_v_p_c_e_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vpceConfigurationName =
         flag "vpce-configuration-name" (optional string)
           ~doc:"STRING VPCEConfigurationName"
       and vpceServiceName =
         flag "vpce-service-name" (optional string)
           ~doc:"STRING VPCEServiceName"
       and serviceDnsName =
         flag "service-dns-name" (optional string)
           ~doc:"STRING ServiceDnsName"
       and vpceConfigurationDescription =
         flag "vpce-configuration-description" (optional string)
           ~doc:"STRING VPCEConfigurationDescription"
       and arn =
         flag "arn" (required string) ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_v_p_c_e_configuration
           (Values.UpdateVPCEConfigurationRequest.make ?vpceConfigurationName
              ?vpceServiceName ?serviceDnsName ?vpceConfigurationDescription
              ~arn ()) (Some Values.UpdateVPCEConfigurationResult.to_json)
           (Some Values.UpdateVPCEConfigurationResult.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-device-pool", create_device_pool);
    ("create-instance-profile", create_instance_profile);
    ("create-network-profile", create_network_profile);
    ("create-project", create_project);
    ("create-remote-access-session", create_remote_access_session);
    ("create-test-grid-project", create_test_grid_project);
    ("create-test-grid-url", create_test_grid_url);
    ("create-upload", create_upload);
    ("create-v-p-c-e-configuration", create_v_p_c_e_configuration);
    ("delete-device-pool", delete_device_pool);
    ("delete-instance-profile", delete_instance_profile);
    ("delete-network-profile", delete_network_profile);
    ("delete-project", delete_project);
    ("delete-remote-access-session", delete_remote_access_session);
    ("delete-run", delete_run);
    ("delete-test-grid-project", delete_test_grid_project);
    ("delete-upload", delete_upload);
    ("delete-v-p-c-e-configuration", delete_v_p_c_e_configuration);
    ("get-account-settings", get_account_settings);
    ("get-device", get_device);
    ("get-device-instance", get_device_instance);
    ("get-device-pool", get_device_pool);
    ("get-device-pool-compatibility", get_device_pool_compatibility);
    ("get-instance-profile", get_instance_profile);
    ("get-job", get_job);
    ("get-network-profile", get_network_profile);
    ("get-offering-status", get_offering_status);
    ("get-project", get_project);
    ("get-remote-access-session", get_remote_access_session);
    ("get-run", get_run);
    ("get-suite", get_suite);
    ("get-test", get_test);
    ("get-test-grid-project", get_test_grid_project);
    ("get-test-grid-session", get_test_grid_session);
    ("get-upload", get_upload);
    ("get-v-p-c-e-configuration", get_v_p_c_e_configuration);
    ("install-to-remote-access-session", install_to_remote_access_session);
    ("list-artifacts", list_artifacts);
    ("list-device-instances", list_device_instances);
    ("list-device-pools", list_device_pools);
    ("list-devices", list_devices);
    ("list-instance-profiles", list_instance_profiles);
    ("list-jobs", list_jobs);
    ("list-network-profiles", list_network_profiles);
    ("list-offering-promotions", list_offering_promotions);
    ("list-offering-transactions", list_offering_transactions);
    ("list-offerings", list_offerings);
    ("list-projects", list_projects);
    ("list-remote-access-sessions", list_remote_access_sessions);
    ("list-runs", list_runs);
    ("list-samples", list_samples);
    ("list-suites", list_suites);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-test-grid-projects", list_test_grid_projects);
    ("list-test-grid-session-actions", list_test_grid_session_actions);
    ("list-test-grid-session-artifacts", list_test_grid_session_artifacts);
    ("list-test-grid-sessions", list_test_grid_sessions);
    ("list-tests", list_tests);
    ("list-unique-problems", list_unique_problems);
    ("list-uploads", list_uploads);
    ("list-v-p-c-e-configurations", list_v_p_c_e_configurations);
    ("purchase-offering", purchase_offering);
    ("renew-offering", renew_offering);
    ("schedule-run", schedule_run);
    ("stop-job", stop_job);
    ("stop-remote-access-session", stop_remote_access_session);
    ("stop-run", stop_run);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-device-instance", update_device_instance);
    ("update-device-pool", update_device_pool);
    ("update-instance-profile", update_instance_profile);
    ("update-network-profile", update_network_profile);
    ("update-project", update_project);
    ("update-test-grid-project", update_test_grid_project);
    ("update-upload", update_upload);
    ("update-v-p-c-e-configuration", update_v_p_c_e_configuration)]
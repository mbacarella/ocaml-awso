(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.devicefarm
let apiVersion = "2015-06-23"
let endpointPrefix = "devicefarm"
let serviceFullName = "AWS Device Farm"
let signatureVersion = "v4"
let protocol = "json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let targetPrefix = "DeviceFarm_20150623"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module AmazonResourceName =
  struct
    type nonrec t = string
    let context_ = "AmazonResourceName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:32) >>=
             (fun () ->
                (check_string_max i ~max:1011) >>=
                  (fun () -> check_pattern i ~pattern:"^arn:.+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AmazonResourceName" j
    let to_json = simple_to_json to_value
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module Message =
  struct
    type nonrec t = string
    let context_ = "Message"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:16384) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Message" j
    let to_json = simple_to_json to_value
  end
module Name =
  struct
    type nonrec t = string
    let context_ = "Name"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Name" j
    let to_json = simple_to_json to_value
  end
module PackageIds =
  struct
    type nonrec t = String_.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"PackageIds" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module InstanceLabels =
  struct
    type nonrec t = String_.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"InstanceLabels" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module InstanceProfile =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the instance profile."];
      packageCleanup: Boolean.t option
        [@ocaml.doc
          "When set to true, Device Farm removes app packages after a test run. The default value is false for private devices."];
      excludeAppPackagesFromCleanup: PackageIds.t option
        [@ocaml.doc
          "An array of strings containing the list of app packages that should not be cleaned up from the device after a test run completes. The list of packages is considered only if you set packageCleanup to true."];
      rebootAfterUse: Boolean.t option
        [@ocaml.doc
          "When set to true, Device Farm reboots the instance after a test run. The default value is true."];
      name: Name.t option [@ocaml.doc "The name of the instance profile."];
      description: Message.t option
        [@ocaml.doc "The description of the instance profile."]}
    let make ?arn =
      fun ?packageCleanup ->
        fun ?excludeAppPackagesFromCleanup ->
          fun ?rebootAfterUse ->
            fun ?name ->
              fun ?description ->
                fun () ->
                  {
                    arn;
                    packageCleanup;
                    excludeAppPackagesFromCleanup;
                    rebootAfterUse;
                    name;
                    description
                  }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:AmazonResourceName.to_value));
        ("packageCleanup", (Option.map x.packageCleanup ~f:Boolean.to_value));
        ("excludeAppPackagesFromCleanup",
          (Option.map x.excludeAppPackagesFromCleanup ~f:PackageIds.to_value));
        ("rebootAfterUse", (Option.map x.rebootAfterUse ~f:Boolean.to_value));
        ("name", (Option.map x.name ~f:Name.to_value));
        ("description", (Option.map x.description ~f:Message.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let description =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "description") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      let rebootAfterUse =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "rebootAfterUse") in
      let excludeAppPackagesFromCleanup =
        (Option.map ~f:PackageIds.of_xml)
          (Xml.child xml_arg0 "excludeAppPackagesFromCleanup") in
      let packageCleanup =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "packageCleanup") in
      let arn =
        (Option.map ~f:AmazonResourceName.of_xml) (Xml.child xml_arg0 "arn") in
      make ?description ?name ?rebootAfterUse ?excludeAppPackagesFromCleanup
        ?packageCleanup ?arn ()
    let of_json json =
      let description = field_map json "description" Message.of_json in
      let name = field_map json "name" Name.of_json in
      let rebootAfterUse = field_map json "rebootAfterUse" Boolean.of_json in
      let excludeAppPackagesFromCleanup =
        field_map json "excludeAppPackagesFromCleanup" PackageIds.of_json in
      let packageCleanup = field_map json "packageCleanup" Boolean.of_json in
      let arn = field_map json "arn" AmazonResourceName.of_json in
      make ?description ?name ?rebootAfterUse ?excludeAppPackagesFromCleanup
        ?packageCleanup ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the instance profile."]
module InstanceStatus =
  struct
    type nonrec t =
      | IN_USE 
      | PREPARING 
      | AVAILABLE 
      | NOT_AVAILABLE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | IN_USE -> "IN_USE"
      | PREPARING -> "PREPARING"
      | AVAILABLE -> "AVAILABLE"
      | NOT_AVAILABLE -> "NOT_AVAILABLE"
      | Non_static_id s -> s
    let of_string =
      function
      | "IN_USE" -> IN_USE
      | "PREPARING" -> PREPARING
      | "AVAILABLE" -> AVAILABLE
      | "NOT_AVAILABLE" -> NOT_AVAILABLE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration InstanceStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"InstanceStatus" j)
    let to_json = simple_to_json to_value
  end
module Double =
  struct
    type nonrec t = float
    let make i = i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module DeviceInstance =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the device instance."];
      deviceArn: AmazonResourceName.t option
        [@ocaml.doc "The ARN of the device."];
      labels: InstanceLabels.t option
        [@ocaml.doc "An array of strings that describe the device instance."];
      status: InstanceStatus.t option
        [@ocaml.doc
          "The status of the device instance. Valid values are listed here."];
      udid: String_.t option
        [@ocaml.doc "Unique device identifier for the device instance."];
      instanceProfile: InstanceProfile.t option
        [@ocaml.doc
          "A object that contains information about the instance profile."]}
    let make ?arn =
      fun ?deviceArn ->
        fun ?labels ->
          fun ?status ->
            fun ?udid ->
              fun ?instanceProfile ->
                fun () ->
                  { arn; deviceArn; labels; status; udid; instanceProfile }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:AmazonResourceName.to_value));
        ("deviceArn",
          (Option.map x.deviceArn ~f:AmazonResourceName.to_value));
        ("labels", (Option.map x.labels ~f:InstanceLabels.to_value));
        ("status", (Option.map x.status ~f:InstanceStatus.to_value));
        ("udid", (Option.map x.udid ~f:String_.to_value));
        ("instanceProfile",
          (Option.map x.instanceProfile ~f:InstanceProfile.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceProfile =
        (Option.map ~f:InstanceProfile.of_xml)
          (Xml.child xml_arg0 "instanceProfile") in
      let udid = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "udid") in
      let status =
        (Option.map ~f:InstanceStatus.of_xml) (Xml.child xml_arg0 "status") in
      let labels =
        (Option.map ~f:InstanceLabels.of_xml) (Xml.child xml_arg0 "labels") in
      let deviceArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "deviceArn") in
      let arn =
        (Option.map ~f:AmazonResourceName.of_xml) (Xml.child xml_arg0 "arn") in
      make ?instanceProfile ?udid ?status ?labels ?deviceArn ?arn ()
    let of_json json =
      let instanceProfile =
        field_map json "instanceProfile" InstanceProfile.of_json in
      let udid = field_map json "udid" String_.of_json in
      let status = field_map json "status" InstanceStatus.of_json in
      let labels = field_map json "labels" InstanceLabels.of_json in
      let deviceArn = field_map json "deviceArn" AmazonResourceName.of_json in
      let arn = field_map json "arn" AmazonResourceName.of_json in
      make ?instanceProfile ?udid ?status ?labels ?deviceArn ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the device instance."]
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module CurrencyCode =
  struct
    type nonrec t =
      | USD 
      | Non_static_id of string 
    let make i = i
    let to_string = function | USD -> "USD" | Non_static_id s -> s
    let of_string = function | "USD" -> USD | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration CurrencyCode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"CurrencyCode" j)
    let to_json = simple_to_json to_value
  end
module CPU =
  struct
    type nonrec t =
      {
      frequency: String_.t option [@ocaml.doc "The CPU's frequency."];
      architecture: String_.t option
        [@ocaml.doc "The CPU's architecture (for example, x86 or ARM)."];
      clock: Double.t option
        [@ocaml.doc
          "The clock speed of the device's CPU, expressed in hertz (Hz). For example, a 1.2 GHz CPU is expressed as 1200000000."]}
    let make ?frequency =
      fun ?architecture ->
        fun ?clock -> fun () -> { frequency; architecture; clock }
    let to_value x =
      structure_to_value
        [("frequency", (Option.map x.frequency ~f:String_.to_value));
        ("architecture", (Option.map x.architecture ~f:String_.to_value));
        ("clock", (Option.map x.clock ~f:Double.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clock = (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "clock") in
      let architecture =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "architecture") in
      let frequency =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "frequency") in
      make ?clock ?architecture ?frequency ()
    let of_json json =
      let clock = field_map json "clock" Double.of_json in
      let architecture = field_map json "architecture" String_.of_json in
      let frequency = field_map json "frequency" String_.of_json in
      make ?clock ?architecture ?frequency ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the amount of CPU that an app is using on a physical device. Does not represent system-wide CPU usage."]
module DeviceAvailability =
  struct
    type nonrec t =
      | TEMPORARY_NOT_AVAILABLE 
      | BUSY 
      | AVAILABLE 
      | HIGHLY_AVAILABLE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | TEMPORARY_NOT_AVAILABLE -> "TEMPORARY_NOT_AVAILABLE"
      | BUSY -> "BUSY"
      | AVAILABLE -> "AVAILABLE"
      | HIGHLY_AVAILABLE -> "HIGHLY_AVAILABLE"
      | Non_static_id s -> s
    let of_string =
      function
      | "TEMPORARY_NOT_AVAILABLE" -> TEMPORARY_NOT_AVAILABLE
      | "BUSY" -> BUSY
      | "AVAILABLE" -> AVAILABLE
      | "HIGHLY_AVAILABLE" -> HIGHLY_AVAILABLE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration DeviceAvailability" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"DeviceAvailability" j)
    let to_json = simple_to_json to_value
  end
module DeviceFormFactor =
  struct
    type nonrec t =
      | PHONE 
      | TABLET 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | PHONE -> "PHONE" | TABLET -> "TABLET" | Non_static_id s -> s
    let of_string =
      function | "PHONE" -> PHONE | "TABLET" -> TABLET | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration DeviceFormFactor" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"DeviceFormFactor" j)
    let to_json = simple_to_json to_value
  end
module DeviceInstances =
  struct
    type nonrec t = DeviceInstance.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:DeviceInstance.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DeviceInstance.of_xml)
    let of_json j =
      list_of_json ~kind:"DeviceInstances" ~of_json:DeviceInstance.of_json j
    let to_json v = composed_to_json to_value v
  end
module DevicePlatform =
  struct
    type nonrec t =
      | ANDROID 
      | IOS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | ANDROID -> "ANDROID" | IOS -> "IOS" | Non_static_id s -> s
    let of_string =
      function | "ANDROID" -> ANDROID | "IOS" -> IOS | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration DevicePlatform" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"DevicePlatform" j)
    let to_json = simple_to_json to_value
  end
module Long =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module Resolution =
  struct
    type nonrec t =
      {
      width: Integer.t option
        [@ocaml.doc "The screen resolution's width, expressed in pixels."];
      height: Integer.t option
        [@ocaml.doc "The screen resolution's height, expressed in pixels."]}
    let make ?width = fun ?height -> fun () -> { width; height }
    let to_value x =
      structure_to_value
        [("width", (Option.map x.width ~f:Integer.to_value));
        ("height", (Option.map x.height ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let height =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "height") in
      let width = (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "width") in
      make ?height ?width ()
    let of_json json =
      let height = field_map json "height" Integer.of_json in
      let width = field_map json "width" Integer.of_json in
      make ?height ?width ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the screen resolution of a device in height and width, expressed in pixels."]
module MonetaryAmount =
  struct
    type nonrec t =
      {
      amount: Double.t option
        [@ocaml.doc "The numerical amount of an offering or transaction."];
      currencyCode: CurrencyCode.t option
        [@ocaml.doc
          "The currency code of a monetary amount. For example, USD means U.S. dollars."]}
    let make ?amount =
      fun ?currencyCode -> fun () -> { amount; currencyCode }
    let to_value x =
      structure_to_value
        [("amount", (Option.map x.amount ~f:Double.to_value));
        ("currencyCode",
          (Option.map x.currencyCode ~f:CurrencyCode.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let currencyCode =
        (Option.map ~f:CurrencyCode.of_xml)
          (Xml.child xml_arg0 "currencyCode") in
      let amount =
        (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "amount") in
      make ?currencyCode ?amount ()
    let of_json json =
      let currencyCode = field_map json "currencyCode" CurrencyCode.of_json in
      let amount = field_map json "amount" Double.of_json in
      make ?currencyCode ?amount ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A number that represents the monetary amount for an offering or transaction."]
module RecurringChargeFrequency =
  struct
    type nonrec t =
      | MONTHLY 
      | Non_static_id of string 
    let make i = i
    let to_string = function | MONTHLY -> "MONTHLY" | Non_static_id s -> s
    let of_string = function | "MONTHLY" -> MONTHLY | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration RecurringChargeFrequency" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"RecurringChargeFrequency" j)
    let to_json = simple_to_json to_value
  end
module Device =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t option [@ocaml.doc "The device's ARN."];
      name: Name.t option [@ocaml.doc "The device's display name."];
      manufacturer: String_.t option
        [@ocaml.doc "The device's manufacturer name."];
      model: String_.t option [@ocaml.doc "The device's model name."];
      modelId: String_.t option [@ocaml.doc "The device's model ID."];
      formFactor: DeviceFormFactor.t option
        [@ocaml.doc
          "The device's form factor. Allowed values include: PHONE TABLET"];
      platform: DevicePlatform.t option
        [@ocaml.doc
          "The device's platform. Allowed values include: ANDROID IOS"];
      os: String_.t option [@ocaml.doc "The device's operating system type."];
      cpu: CPU.t option [@ocaml.doc "Information about the device's CPU."];
      resolution: Resolution.t option
        [@ocaml.doc "The resolution of the device."];
      heapSize: Long.t option
        [@ocaml.doc "The device's heap size, expressed in bytes."];
      memory: Long.t option
        [@ocaml.doc "The device's total memory size, expressed in bytes."];
      image: String_.t option [@ocaml.doc "The device's image name."];
      carrier: String_.t option [@ocaml.doc "The device's carrier."];
      radio: String_.t option [@ocaml.doc "The device's radio."];
      remoteAccessEnabled: Boolean.t option
        [@ocaml.doc
          "Specifies whether remote access has been enabled for the specified device."];
      remoteDebugEnabled: Boolean.t option
        [@ocaml.doc
          "This flag is set to true if remote debugging is enabled for the device. Remote debugging is no longer supported."];
      fleetType: String_.t option
        [@ocaml.doc
          "The type of fleet to which this device belongs. Possible values are PRIVATE and PUBLIC."];
      fleetName: String_.t option
        [@ocaml.doc "The name of the fleet to which this device belongs."];
      instances: DeviceInstances.t option
        [@ocaml.doc "The instances that belong to this device."];
      availability: DeviceAvailability.t option
        [@ocaml.doc
          "Indicates how likely a device is available for a test run. Currently available in the ListDevices and GetDevice API methods."]}
    let make ?arn =
      fun ?name ->
        fun ?manufacturer ->
          fun ?model ->
            fun ?modelId ->
              fun ?formFactor ->
                fun ?platform ->
                  fun ?os ->
                    fun ?cpu ->
                      fun ?resolution ->
                        fun ?heapSize ->
                          fun ?memory ->
                            fun ?image ->
                              fun ?carrier ->
                                fun ?radio ->
                                  fun ?remoteAccessEnabled ->
                                    fun ?remoteDebugEnabled ->
                                      fun ?fleetType ->
                                        fun ?fleetName ->
                                          fun ?instances ->
                                            fun ?availability ->
                                              fun () ->
                                                {
                                                  arn;
                                                  name;
                                                  manufacturer;
                                                  model;
                                                  modelId;
                                                  formFactor;
                                                  platform;
                                                  os;
                                                  cpu;
                                                  resolution;
                                                  heapSize;
                                                  memory;
                                                  image;
                                                  carrier;
                                                  radio;
                                                  remoteAccessEnabled;
                                                  remoteDebugEnabled;
                                                  fleetType;
                                                  fleetName;
                                                  instances;
                                                  availability
                                                }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:AmazonResourceName.to_value));
        ("name", (Option.map x.name ~f:Name.to_value));
        ("manufacturer", (Option.map x.manufacturer ~f:String_.to_value));
        ("model", (Option.map x.model ~f:String_.to_value));
        ("modelId", (Option.map x.modelId ~f:String_.to_value));
        ("formFactor",
          (Option.map x.formFactor ~f:DeviceFormFactor.to_value));
        ("platform", (Option.map x.platform ~f:DevicePlatform.to_value));
        ("os", (Option.map x.os ~f:String_.to_value));
        ("cpu", (Option.map x.cpu ~f:CPU.to_value));
        ("resolution", (Option.map x.resolution ~f:Resolution.to_value));
        ("heapSize", (Option.map x.heapSize ~f:Long.to_value));
        ("memory", (Option.map x.memory ~f:Long.to_value));
        ("image", (Option.map x.image ~f:String_.to_value));
        ("carrier", (Option.map x.carrier ~f:String_.to_value));
        ("radio", (Option.map x.radio ~f:String_.to_value));
        ("remoteAccessEnabled",
          (Option.map x.remoteAccessEnabled ~f:Boolean.to_value));
        ("remoteDebugEnabled",
          (Option.map x.remoteDebugEnabled ~f:Boolean.to_value));
        ("fleetType", (Option.map x.fleetType ~f:String_.to_value));
        ("fleetName", (Option.map x.fleetName ~f:String_.to_value));
        ("instances", (Option.map x.instances ~f:DeviceInstances.to_value));
        ("availability",
          (Option.map x.availability ~f:DeviceAvailability.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let availability =
        (Option.map ~f:DeviceAvailability.of_xml)
          (Xml.child xml_arg0 "availability") in
      let instances =
        (Option.map ~f:DeviceInstances.of_xml)
          (Xml.child xml_arg0 "instances") in
      let fleetName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "fleetName") in
      let fleetType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "fleetType") in
      let remoteDebugEnabled =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "remoteDebugEnabled") in
      let remoteAccessEnabled =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "remoteAccessEnabled") in
      let radio = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "radio") in
      let carrier =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "carrier") in
      let image = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "image") in
      let memory = (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "memory") in
      let heapSize =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "heapSize") in
      let resolution =
        (Option.map ~f:Resolution.of_xml) (Xml.child xml_arg0 "resolution") in
      let cpu = (Option.map ~f:CPU.of_xml) (Xml.child xml_arg0 "cpu") in
      let os = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "os") in
      let platform =
        (Option.map ~f:DevicePlatform.of_xml) (Xml.child xml_arg0 "platform") in
      let formFactor =
        (Option.map ~f:DeviceFormFactor.of_xml)
          (Xml.child xml_arg0 "formFactor") in
      let modelId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "modelId") in
      let model = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "model") in
      let manufacturer =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "manufacturer") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      let arn =
        (Option.map ~f:AmazonResourceName.of_xml) (Xml.child xml_arg0 "arn") in
      make ?availability ?instances ?fleetName ?fleetType ?remoteDebugEnabled
        ?remoteAccessEnabled ?radio ?carrier ?image ?memory ?heapSize
        ?resolution ?cpu ?os ?platform ?formFactor ?modelId ?model
        ?manufacturer ?name ?arn ()
    let of_json json =
      let availability =
        field_map json "availability" DeviceAvailability.of_json in
      let instances = field_map json "instances" DeviceInstances.of_json in
      let fleetName = field_map json "fleetName" String_.of_json in
      let fleetType = field_map json "fleetType" String_.of_json in
      let remoteDebugEnabled =
        field_map json "remoteDebugEnabled" Boolean.of_json in
      let remoteAccessEnabled =
        field_map json "remoteAccessEnabled" Boolean.of_json in
      let radio = field_map json "radio" String_.of_json in
      let carrier = field_map json "carrier" String_.of_json in
      let image = field_map json "image" String_.of_json in
      let memory = field_map json "memory" Long.of_json in
      let heapSize = field_map json "heapSize" Long.of_json in
      let resolution = field_map json "resolution" Resolution.of_json in
      let cpu = field_map json "cpu" CPU.of_json in
      let os = field_map json "os" String_.of_json in
      let platform = field_map json "platform" DevicePlatform.of_json in
      let formFactor = field_map json "formFactor" DeviceFormFactor.of_json in
      let modelId = field_map json "modelId" String_.of_json in
      let model = field_map json "model" String_.of_json in
      let manufacturer = field_map json "manufacturer" String_.of_json in
      let name = field_map json "name" Name.of_json in
      let arn = field_map json "arn" AmazonResourceName.of_json in
      make ?availability ?instances ?fleetName ?fleetType ?remoteDebugEnabled
        ?remoteAccessEnabled ?radio ?carrier ?image ?memory ?heapSize
        ?resolution ?cpu ?os ?platform ?formFactor ?modelId ?model
        ?manufacturer ?name ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a device type that an app is tested against."]
module ExecutionResult =
  struct
    type nonrec t =
      | PENDING 
      | PASSED 
      | WARNED 
      | FAILED 
      | SKIPPED 
      | ERRORED 
      | STOPPED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PENDING -> "PENDING"
      | PASSED -> "PASSED"
      | WARNED -> "WARNED"
      | FAILED -> "FAILED"
      | SKIPPED -> "SKIPPED"
      | ERRORED -> "ERRORED"
      | STOPPED -> "STOPPED"
      | Non_static_id s -> s
    let of_string =
      function
      | "PENDING" -> PENDING
      | "PASSED" -> PASSED
      | "WARNED" -> WARNED
      | "FAILED" -> FAILED
      | "SKIPPED" -> SKIPPED
      | "ERRORED" -> ERRORED
      | "STOPPED" -> STOPPED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ExecutionResult" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ExecutionResult" j)
    let to_json = simple_to_json to_value
  end
module ProblemDetail =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t option
        [@ocaml.doc "The problem detail's ARN."];
      name: Name.t option [@ocaml.doc "The problem detail's name."]}
    let make ?arn = fun ?name -> fun () -> { arn; name }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:AmazonResourceName.to_value));
        ("name", (Option.map x.name ~f:Name.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      let arn =
        (Option.map ~f:AmazonResourceName.of_xml) (Xml.child xml_arg0 "arn") in
      make ?name ?arn ()
    let of_json json =
      let name = field_map json "name" Name.of_json in
      let arn = field_map json "arn" AmazonResourceName.of_json in
      make ?name ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a problem detail."]
module DeviceFilterAttribute =
  struct
    type nonrec t =
      | ARN 
      | PLATFORM 
      | OS_VERSION 
      | MODEL 
      | AVAILABILITY 
      | FORM_FACTOR 
      | MANUFACTURER 
      | REMOTE_ACCESS_ENABLED 
      | REMOTE_DEBUG_ENABLED 
      | INSTANCE_ARN 
      | INSTANCE_LABELS 
      | FLEET_TYPE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ARN -> "ARN"
      | PLATFORM -> "PLATFORM"
      | OS_VERSION -> "OS_VERSION"
      | MODEL -> "MODEL"
      | AVAILABILITY -> "AVAILABILITY"
      | FORM_FACTOR -> "FORM_FACTOR"
      | MANUFACTURER -> "MANUFACTURER"
      | REMOTE_ACCESS_ENABLED -> "REMOTE_ACCESS_ENABLED"
      | REMOTE_DEBUG_ENABLED -> "REMOTE_DEBUG_ENABLED"
      | INSTANCE_ARN -> "INSTANCE_ARN"
      | INSTANCE_LABELS -> "INSTANCE_LABELS"
      | FLEET_TYPE -> "FLEET_TYPE"
      | Non_static_id s -> s
    let of_string =
      function
      | "ARN" -> ARN
      | "PLATFORM" -> PLATFORM
      | "OS_VERSION" -> OS_VERSION
      | "MODEL" -> MODEL
      | "AVAILABILITY" -> AVAILABILITY
      | "FORM_FACTOR" -> FORM_FACTOR
      | "MANUFACTURER" -> MANUFACTURER
      | "REMOTE_ACCESS_ENABLED" -> REMOTE_ACCESS_ENABLED
      | "REMOTE_DEBUG_ENABLED" -> REMOTE_DEBUG_ENABLED
      | "INSTANCE_ARN" -> INSTANCE_ARN
      | "INSTANCE_LABELS" -> INSTANCE_LABELS
      | "FLEET_TYPE" -> FLEET_TYPE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration DeviceFilterAttribute" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"DeviceFilterAttribute" j)
    let to_json = simple_to_json to_value
  end
module DeviceFilterValues =
  struct
    type nonrec t = String_.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"DeviceFilterValues" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module RuleOperator =
  struct
    type nonrec t =
      | EQUALS 
      | LESS_THAN 
      | LESS_THAN_OR_EQUALS 
      | GREATER_THAN 
      | GREATER_THAN_OR_EQUALS 
      | IN 
      | NOT_IN 
      | CONTAINS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | EQUALS -> "EQUALS"
      | LESS_THAN -> "LESS_THAN"
      | LESS_THAN_OR_EQUALS -> "LESS_THAN_OR_EQUALS"
      | GREATER_THAN -> "GREATER_THAN"
      | GREATER_THAN_OR_EQUALS -> "GREATER_THAN_OR_EQUALS"
      | IN -> "IN"
      | NOT_IN -> "NOT_IN"
      | CONTAINS -> "CONTAINS"
      | Non_static_id s -> s
    let of_string =
      function
      | "EQUALS" -> EQUALS
      | "LESS_THAN" -> LESS_THAN
      | "LESS_THAN_OR_EQUALS" -> LESS_THAN_OR_EQUALS
      | "GREATER_THAN" -> GREATER_THAN
      | "GREATER_THAN_OR_EQUALS" -> GREATER_THAN_OR_EQUALS
      | "IN" -> IN
      | "NOT_IN" -> NOT_IN
      | "CONTAINS" -> CONTAINS
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration RuleOperator" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"RuleOperator" j)
    let to_json = simple_to_json to_value
  end
module RecurringCharge =
  struct
    type nonrec t =
      {
      cost: MonetaryAmount.t option
        [@ocaml.doc "The cost of the recurring charge."];
      frequency: RecurringChargeFrequency.t option
        [@ocaml.doc "The frequency in which charges recur."]}
    let make ?cost = fun ?frequency -> fun () -> { cost; frequency }
    let to_value x =
      structure_to_value
        [("cost", (Option.map x.cost ~f:MonetaryAmount.to_value));
        ("frequency",
          (Option.map x.frequency ~f:RecurringChargeFrequency.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let frequency =
        (Option.map ~f:RecurringChargeFrequency.of_xml)
          (Xml.child xml_arg0 "frequency") in
      let cost =
        (Option.map ~f:MonetaryAmount.of_xml) (Xml.child xml_arg0 "cost") in
      make ?frequency ?cost ()
    let of_json json =
      let frequency =
        field_map json "frequency" RecurringChargeFrequency.of_json in
      let cost = field_map json "cost" MonetaryAmount.of_json in
      make ?frequency ?cost ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies whether charges for devices are recurring."]
module Problem =
  struct
    type nonrec t =
      {
      run: ProblemDetail.t option
        [@ocaml.doc "Information about the associated run."];
      job: ProblemDetail.t option
        [@ocaml.doc "Information about the associated job."];
      suite: ProblemDetail.t option
        [@ocaml.doc "Information about the associated suite."];
      test: ProblemDetail.t option
        [@ocaml.doc "Information about the associated test."];
      device: Device.t option
        [@ocaml.doc "Information about the associated device."];
      result: ExecutionResult.t option
        [@ocaml.doc
          "The problem's result. Allowed values include: PENDING PASSED WARNED FAILED SKIPPED ERRORED STOPPED"];
      message: Message.t option
        [@ocaml.doc "A message about the problem's result."]}
    let make ?run =
      fun ?job ->
        fun ?suite ->
          fun ?test ->
            fun ?device ->
              fun ?result ->
                fun ?message ->
                  fun () ->
                    { run; job; suite; test; device; result; message }
    let to_value x =
      structure_to_value
        [("run", (Option.map x.run ~f:ProblemDetail.to_value));
        ("job", (Option.map x.job ~f:ProblemDetail.to_value));
        ("suite", (Option.map x.suite ~f:ProblemDetail.to_value));
        ("test", (Option.map x.test ~f:ProblemDetail.to_value));
        ("device", (Option.map x.device ~f:Device.to_value));
        ("result", (Option.map x.result ~f:ExecutionResult.to_value));
        ("message", (Option.map x.message ~f:Message.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "message") in
      let result =
        (Option.map ~f:ExecutionResult.of_xml) (Xml.child xml_arg0 "result") in
      let device =
        (Option.map ~f:Device.of_xml) (Xml.child xml_arg0 "device") in
      let test =
        (Option.map ~f:ProblemDetail.of_xml) (Xml.child xml_arg0 "test") in
      let suite =
        (Option.map ~f:ProblemDetail.of_xml) (Xml.child xml_arg0 "suite") in
      let job =
        (Option.map ~f:ProblemDetail.of_xml) (Xml.child xml_arg0 "job") in
      let run =
        (Option.map ~f:ProblemDetail.of_xml) (Xml.child xml_arg0 "run") in
      make ?message ?result ?device ?test ?suite ?job ?run ()
    let of_json json =
      let message = field_map json "message" Message.of_json in
      let result = field_map json "result" ExecutionResult.of_json in
      let device = field_map json "device" Device.of_json in
      let test = field_map json "test" ProblemDetail.of_json in
      let suite = field_map json "suite" ProblemDetail.of_json in
      let job = field_map json "job" ProblemDetail.of_json in
      let run = field_map json "run" ProblemDetail.of_json in
      make ?message ?result ?device ?test ?suite ?job ?run ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a specific warning or failure."]
module NonEmptyString =
  struct
    type nonrec t = string
    let context_ = "NonEmptyString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:4096) >>=
                  (fun () -> check_pattern i ~pattern:".*\\S.*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NonEmptyString" j
    let to_json = simple_to_json to_value
  end
module DeviceFilter =
  struct
    type nonrec t =
      {
      attribute: DeviceFilterAttribute.t
        [@ocaml.doc
          "The aspect of a device such as platform or model used as the selection criteria in a device filter. The supported operators for each attribute are provided in the following list. ARN The Amazon Resource Name (ARN) of the device (for example, arn:aws:devicefarm:us-west-2::device:12345Example). Supported operators: EQUALS, IN, NOT_IN PLATFORM The device platform. Valid values are ANDROID or IOS. Supported operators: EQUALS OS_VERSION The operating system version (for example, 10.3.2). Supported operators: EQUALS, GREATER_THAN, GREATER_THAN_OR_EQUALS, IN, LESS_THAN, LESS_THAN_OR_EQUALS, NOT_IN MODEL The device model (for example, iPad 5th Gen). Supported operators: CONTAINS, EQUALS, IN, NOT_IN AVAILABILITY The current availability of the device. Valid values are AVAILABLE, HIGHLY_AVAILABLE, BUSY, or TEMPORARY_NOT_AVAILABLE. Supported operators: EQUALS FORM_FACTOR The device form factor. Valid values are PHONE or TABLET. Supported operators: EQUALS MANUFACTURER The device manufacturer (for example, Apple). Supported operators: EQUALS, IN, NOT_IN REMOTE_ACCESS_ENABLED Whether the device is enabled for remote access. Valid values are TRUE or FALSE. Supported operators: EQUALS REMOTE_DEBUG_ENABLED Whether the device is enabled for remote debugging. Valid values are TRUE or FALSE. Supported operators: EQUALS Because remote debugging is no longer supported, this filter is ignored. INSTANCE_ARN The Amazon Resource Name (ARN) of the device instance. Supported operators: EQUALS, IN, NOT_IN INSTANCE_LABELS The label of the device instance. Supported operators: CONTAINS FLEET_TYPE The fleet type. Valid values are PUBLIC or PRIVATE. Supported operators: EQUALS"];
      operator: RuleOperator.t
        [@ocaml.doc
          "Specifies how Device Farm compares the filter's attribute to the value. See the attribute descriptions."];
      values: DeviceFilterValues.t
        [@ocaml.doc
          "An array of one or more filter values used in a device filter. Operator Values The IN and NOT_IN operators can take a values array that has more than one element. The other operators require an array with a single element. Attribute Values The PLATFORM attribute can be set to ANDROID or IOS. The AVAILABILITY attribute can be set to AVAILABLE, HIGHLY_AVAILABLE, BUSY, or TEMPORARY_NOT_AVAILABLE. The FORM_FACTOR attribute can be set to PHONE or TABLET. The FLEET_TYPE attribute can be set to PUBLIC or PRIVATE."]}
    let context_ = "DeviceFilter"
    let make ~attribute =
      fun ~operator ->
        fun ~values -> fun () -> { attribute; operator; values }
    let to_value x =
      structure_to_value
        [("attribute", (Some (DeviceFilterAttribute.to_value x.attribute)));
        ("operator", (Some (RuleOperator.to_value x.operator)));
        ("values", (Some (DeviceFilterValues.to_value x.values)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let values =
        DeviceFilterValues.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "values") in
      let operator =
        RuleOperator.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "operator") in
      let attribute =
        DeviceFilterAttribute.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "attribute") in
      make ~values ~operator ~attribute ()
    let of_json json =
      let values = field_map_exn json "values" DeviceFilterValues.of_json in
      let operator = field_map_exn json "operator" RuleOperator.of_json in
      let attribute =
        field_map_exn json "attribute" DeviceFilterAttribute.of_json in
      make ~values ~operator ~attribute ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a device filter used to select a set of devices to be included in a test run. This data structure is passed in as the deviceSelectionConfiguration parameter to ScheduleRun. For an example of the JSON request syntax, see ScheduleRun. It is also passed in as the filters parameter to ListDevices. For an example of the JSON request syntax, see ListDevices."]
module OfferingIdentifier =
  struct
    type nonrec t = string
    let context_ = "OfferingIdentifier"
    let make i =
      let open Result in ok_or_failwith (check_string_min i ~min:32); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"OfferingIdentifier" j
    let to_json = simple_to_json to_value
  end
module OfferingType =
  struct
    type nonrec t =
      | RECURRING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | RECURRING -> "RECURRING" | Non_static_id s -> s
    let of_string =
      function | "RECURRING" -> RECURRING | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration OfferingType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"OfferingType" j)
    let to_json = simple_to_json to_value
  end
module RecurringCharges =
  struct
    type nonrec t = RecurringCharge.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:RecurringCharge.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RecurringCharge.of_xml)
    let of_json j =
      list_of_json ~kind:"RecurringCharges" ~of_json:RecurringCharge.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module DeviceAttribute =
  struct
    type nonrec t =
      | ARN 
      | PLATFORM 
      | FORM_FACTOR 
      | MANUFACTURER 
      | REMOTE_ACCESS_ENABLED 
      | REMOTE_DEBUG_ENABLED 
      | APPIUM_VERSION 
      | INSTANCE_ARN 
      | INSTANCE_LABELS 
      | FLEET_TYPE 
      | OS_VERSION 
      | MODEL 
      | AVAILABILITY 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ARN -> "ARN"
      | PLATFORM -> "PLATFORM"
      | FORM_FACTOR -> "FORM_FACTOR"
      | MANUFACTURER -> "MANUFACTURER"
      | REMOTE_ACCESS_ENABLED -> "REMOTE_ACCESS_ENABLED"
      | REMOTE_DEBUG_ENABLED -> "REMOTE_DEBUG_ENABLED"
      | APPIUM_VERSION -> "APPIUM_VERSION"
      | INSTANCE_ARN -> "INSTANCE_ARN"
      | INSTANCE_LABELS -> "INSTANCE_LABELS"
      | FLEET_TYPE -> "FLEET_TYPE"
      | OS_VERSION -> "OS_VERSION"
      | MODEL -> "MODEL"
      | AVAILABILITY -> "AVAILABILITY"
      | Non_static_id s -> s
    let of_string =
      function
      | "ARN" -> ARN
      | "PLATFORM" -> PLATFORM
      | "FORM_FACTOR" -> FORM_FACTOR
      | "MANUFACTURER" -> MANUFACTURER
      | "REMOTE_ACCESS_ENABLED" -> REMOTE_ACCESS_ENABLED
      | "REMOTE_DEBUG_ENABLED" -> REMOTE_DEBUG_ENABLED
      | "APPIUM_VERSION" -> APPIUM_VERSION
      | "INSTANCE_ARN" -> INSTANCE_ARN
      | "INSTANCE_LABELS" -> INSTANCE_LABELS
      | "FLEET_TYPE" -> FLEET_TYPE
      | "OS_VERSION" -> OS_VERSION
      | "MODEL" -> MODEL
      | "AVAILABILITY" -> AVAILABILITY
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration DeviceAttribute" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"DeviceAttribute" j)
    let to_json = simple_to_json to_value
  end
module Problems =
  struct
    type nonrec t = Problem.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Problem.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Problem.of_xml)
    let of_json j = list_of_json ~kind:"Problems" ~of_json:Problem.of_json j
    let to_json v = composed_to_json to_value v
  end
module SecurityGroupIds =
  struct
    type nonrec t = NonEmptyString.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:5) >>= (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:NonEmptyString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NonEmptyString.of_xml)
    let of_json j =
      list_of_json ~kind:"SecurityGroupIds" ~of_json:NonEmptyString.of_json j
    let to_json v = composed_to_json to_value v
  end
module SubnetIds =
  struct
    type nonrec t = NonEmptyString.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:8) >>= (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:NonEmptyString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NonEmptyString.of_xml)
    let of_json j =
      list_of_json ~kind:"SubnetIds" ~of_json:NonEmptyString.of_json j
    let to_json v = composed_to_json to_value v
  end
module AndroidPaths =
  struct
    type nonrec t = String_.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"AndroidPaths" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module DeviceHostPaths =
  struct
    type nonrec t = String_.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"DeviceHostPaths" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module IosPaths =
  struct
    type nonrec t = String_.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j = list_of_json ~kind:"IosPaths" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module DeviceFilters =
  struct
    type nonrec t = DeviceFilter.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:DeviceFilter.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DeviceFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"DeviceFilters" ~of_json:DeviceFilter.of_json j
    let to_json v = composed_to_json to_value v
  end
module NetworkProfileType =
  struct
    type nonrec t =
      | CURATED 
      | PRIVATE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CURATED -> "CURATED"
      | PRIVATE -> "PRIVATE"
      | Non_static_id s -> s
    let of_string =
      function
      | "CURATED" -> CURATED
      | "PRIVATE" -> PRIVATE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration NetworkProfileType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"NetworkProfileType" j)
    let to_json = simple_to_json to_value
  end
module PercentInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for PercentInteger" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module DateTime =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module Offering =
  struct
    type nonrec t =
      {
      id: OfferingIdentifier.t option
        [@ocaml.doc "The ID that corresponds to a device offering."];
      description: Message.t option
        [@ocaml.doc "A string that describes the offering."];
      type_: OfferingType.t option
        [@ocaml.doc
          "The type of offering (for example, RECURRING) for a device."];
      platform: DevicePlatform.t option
        [@ocaml.doc
          "The platform of the device (for example, ANDROID or IOS)."];
      recurringCharges: RecurringCharges.t option
        [@ocaml.doc
          "Specifies whether there are recurring charges for the offering."]}
    let make ?id =
      fun ?description ->
        fun ?type_ ->
          fun ?platform ->
            fun ?recurringCharges ->
              fun () ->
                { id; description; type_; platform; recurringCharges }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:OfferingIdentifier.to_value));
        ("description", (Option.map x.description ~f:Message.to_value));
        ("type", (Option.map x.type_ ~f:OfferingType.to_value));
        ("platform", (Option.map x.platform ~f:DevicePlatform.to_value));
        ("recurringCharges",
          (Option.map x.recurringCharges ~f:RecurringCharges.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recurringCharges =
        (Option.map ~f:RecurringCharges.of_xml)
          (Xml.child xml_arg0 "recurringCharges") in
      let platform =
        (Option.map ~f:DevicePlatform.of_xml) (Xml.child xml_arg0 "platform") in
      let type_ =
        (Option.map ~f:OfferingType.of_xml) (Xml.child xml_arg0 "type") in
      let description =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "description") in
      let id =
        (Option.map ~f:OfferingIdentifier.of_xml) (Xml.child xml_arg0 "id") in
      make ?recurringCharges ?platform ?type_ ?description ?id ()
    let of_json json =
      let recurringCharges =
        field_map json "recurringCharges" RecurringCharges.of_json in
      let platform = field_map json "platform" DevicePlatform.of_json in
      let type_ = field_map json "type" OfferingType.of_json in
      let description = field_map json "description" Message.of_json in
      let id = field_map json "id" OfferingIdentifier.of_json in
      make ?recurringCharges ?platform ?type_ ?description ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the metadata of a device offering."]
module OfferingTransactionType =
  struct
    type nonrec t =
      | PURCHASE 
      | RENEW 
      | SYSTEM 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PURCHASE -> "PURCHASE"
      | RENEW -> "RENEW"
      | SYSTEM -> "SYSTEM"
      | Non_static_id s -> s
    let of_string =
      function
      | "PURCHASE" -> PURCHASE
      | "RENEW" -> RENEW
      | "SYSTEM" -> SYSTEM
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration OfferingTransactionType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"OfferingTransactionType" j)
    let to_json = simple_to_json to_value
  end
module Rule =
  struct
    type nonrec t =
      {
      attribute: DeviceAttribute.t option
        [@ocaml.doc
          "The rule's stringified attribute. For example, specify the value as \"\\\"abc\\\"\". The supported operators for each attribute are provided in the following list. APPIUM_VERSION The Appium version for the test. Supported operators: CONTAINS ARN The Amazon Resource Name (ARN) of the device (for example, arn:aws:devicefarm:us-west-2::device:12345Example. Supported operators: EQUALS, IN, NOT_IN AVAILABILITY The current availability of the device. Valid values are AVAILABLE, HIGHLY_AVAILABLE, BUSY, or TEMPORARY_NOT_AVAILABLE. Supported operators: EQUALS FLEET_TYPE The fleet type. Valid values are PUBLIC or PRIVATE. Supported operators: EQUALS FORM_FACTOR The device form factor. Valid values are PHONE or TABLET. Supported operators: EQUALS, IN, NOT_IN INSTANCE_ARN The Amazon Resource Name (ARN) of the device instance. Supported operators: IN, NOT_IN INSTANCE_LABELS The label of the device instance. Supported operators: CONTAINS MANUFACTURER The device manufacturer (for example, Apple). Supported operators: EQUALS, IN, NOT_IN MODEL The device model, such as Apple iPad Air 2 or Google Pixel. Supported operators: CONTAINS, EQUALS, IN, NOT_IN OS_VERSION The operating system version (for example, 10.3.2). Supported operators: EQUALS, GREATER_THAN, GREATER_THAN_OR_EQUALS, IN, LESS_THAN, LESS_THAN_OR_EQUALS, NOT_IN PLATFORM The device platform. Valid values are ANDROID or IOS. Supported operators: EQUALS, IN, NOT_IN REMOTE_ACCESS_ENABLED Whether the device is enabled for remote access. Valid values are TRUE or FALSE. Supported operators: EQUALS REMOTE_DEBUG_ENABLED Whether the device is enabled for remote debugging. Valid values are TRUE or FALSE. Supported operators: EQUALS Because remote debugging is no longer supported, this filter is ignored."];
      operator: RuleOperator.t option
        [@ocaml.doc
          "Specifies how Device Farm compares the rule's attribute to the value. For the operators that are supported by each attribute, see the attribute descriptions."];
      value: String_.t option [@ocaml.doc "The rule's value."]}
    let make ?attribute =
      fun ?operator -> fun ?value -> fun () -> { attribute; operator; value }
    let to_value x =
      structure_to_value
        [("attribute", (Option.map x.attribute ~f:DeviceAttribute.to_value));
        ("operator", (Option.map x.operator ~f:RuleOperator.to_value));
        ("value", (Option.map x.value ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "value") in
      let operator =
        (Option.map ~f:RuleOperator.of_xml) (Xml.child xml_arg0 "operator") in
      let attribute =
        (Option.map ~f:DeviceAttribute.of_xml)
          (Xml.child xml_arg0 "attribute") in
      make ?value ?operator ?attribute ()
    let of_json json =
      let value = field_map json "value" String_.of_json in
      let operator = field_map json "operator" RuleOperator.of_json in
      let attribute = field_map json "attribute" DeviceAttribute.of_json in
      make ?value ?operator ?attribute ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a condition for a device pool."]
module IncompatibilityMessage =
  struct
    type nonrec t =
      {
      message: Message.t option
        [@ocaml.doc "A message about the incompatibility."];
      type_: DeviceAttribute.t option
        [@ocaml.doc
          "The type of incompatibility. Allowed values include: ARN FORM_FACTOR (for example, phone or tablet) MANUFACTURER PLATFORM (for example, Android or iOS) REMOTE_ACCESS_ENABLED APPIUM_VERSION"]}
    let make ?message = fun ?type_ -> fun () -> { message; type_ }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:Message.to_value));
        ("type", (Option.map x.type_ ~f:DeviceAttribute.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:DeviceAttribute.of_xml) (Xml.child xml_arg0 "type") in
      let message =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "message") in
      make ?type_ ?message ()
    let of_json json =
      let type_ = field_map json "type" DeviceAttribute.of_json in
      let message = field_map json "message" Message.of_json in
      make ?type_ ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents information about incompatibility."]
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module ServiceDnsName =
  struct
    type nonrec t = string
    let context_ = "ServiceDnsName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ServiceDnsName" j
    let to_json = simple_to_json to_value
  end
module VPCEConfigurationDescription =
  struct
    type nonrec t = string
    let context_ = "VPCEConfigurationDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"VPCEConfigurationDescription" j
    let to_json = simple_to_json to_value
  end
module VPCEConfigurationName =
  struct
    type nonrec t = string
    let context_ = "VPCEConfigurationName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"VPCEConfigurationName" j
    let to_json = simple_to_json to_value
  end
module VPCEServiceName =
  struct
    type nonrec t = string
    let context_ = "VPCEServiceName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"VPCEServiceName" j
    let to_json = simple_to_json to_value
  end
module ContentType =
  struct
    type nonrec t = string
    let context_ = "ContentType"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ContentType" j
    let to_json = simple_to_json to_value
  end
module Metadata =
  struct
    type nonrec t = string
    let context_ = "Metadata"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:8192) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Metadata" j
    let to_json = simple_to_json to_value
  end
module SensitiveURL =
  struct
    type nonrec t = string
    let context_ = "SensitiveURL"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SensitiveURL" j
    let to_json = simple_to_json to_value
  end
module UploadCategory =
  struct
    type nonrec t =
      | CURATED 
      | PRIVATE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CURATED -> "CURATED"
      | PRIVATE -> "PRIVATE"
      | Non_static_id s -> s
    let of_string =
      function
      | "CURATED" -> CURATED
      | "PRIVATE" -> PRIVATE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration UploadCategory" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"UploadCategory" j)
    let to_json = simple_to_json to_value
  end
module UploadStatus =
  struct
    type nonrec t =
      | INITIALIZED 
      | PROCESSING 
      | SUCCEEDED 
      | FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | INITIALIZED -> "INITIALIZED"
      | PROCESSING -> "PROCESSING"
      | SUCCEEDED -> "SUCCEEDED"
      | FAILED -> "FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "INITIALIZED" -> INITIALIZED
      | "PROCESSING" -> PROCESSING
      | "SUCCEEDED" -> SUCCEEDED
      | "FAILED" -> FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration UploadStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"UploadStatus" j)
    let to_json = simple_to_json to_value
  end
module UploadType =
  struct
    type nonrec t =
      | ANDROID_APP 
      | IOS_APP 
      | WEB_APP 
      | EXTERNAL_DATA 
      | APPIUM_JAVA_JUNIT_TEST_PACKAGE 
      | APPIUM_JAVA_TESTNG_TEST_PACKAGE 
      | APPIUM_PYTHON_TEST_PACKAGE 
      | APPIUM_NODE_TEST_PACKAGE 
      | APPIUM_RUBY_TEST_PACKAGE 
      | APPIUM_WEB_JAVA_JUNIT_TEST_PACKAGE 
      | APPIUM_WEB_JAVA_TESTNG_TEST_PACKAGE 
      | APPIUM_WEB_PYTHON_TEST_PACKAGE 
      | APPIUM_WEB_NODE_TEST_PACKAGE 
      | APPIUM_WEB_RUBY_TEST_PACKAGE 
      | CALABASH_TEST_PACKAGE 
      | INSTRUMENTATION_TEST_PACKAGE 
      | UIAUTOMATION_TEST_PACKAGE 
      | UIAUTOMATOR_TEST_PACKAGE 
      | XCTEST_TEST_PACKAGE 
      | XCTEST_UI_TEST_PACKAGE 
      | APPIUM_JAVA_JUNIT_TEST_SPEC 
      | APPIUM_JAVA_TESTNG_TEST_SPEC 
      | APPIUM_PYTHON_TEST_SPEC 
      | APPIUM_NODE_TEST_SPEC 
      | APPIUM_RUBY_TEST_SPEC 
      | APPIUM_WEB_JAVA_JUNIT_TEST_SPEC 
      | APPIUM_WEB_JAVA_TESTNG_TEST_SPEC 
      | APPIUM_WEB_PYTHON_TEST_SPEC 
      | APPIUM_WEB_NODE_TEST_SPEC 
      | APPIUM_WEB_RUBY_TEST_SPEC 
      | INSTRUMENTATION_TEST_SPEC 
      | XCTEST_UI_TEST_SPEC 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ANDROID_APP -> "ANDROID_APP"
      | IOS_APP -> "IOS_APP"
      | WEB_APP -> "WEB_APP"
      | EXTERNAL_DATA -> "EXTERNAL_DATA"
      | APPIUM_JAVA_JUNIT_TEST_PACKAGE -> "APPIUM_JAVA_JUNIT_TEST_PACKAGE"
      | APPIUM_JAVA_TESTNG_TEST_PACKAGE -> "APPIUM_JAVA_TESTNG_TEST_PACKAGE"
      | APPIUM_PYTHON_TEST_PACKAGE -> "APPIUM_PYTHON_TEST_PACKAGE"
      | APPIUM_NODE_TEST_PACKAGE -> "APPIUM_NODE_TEST_PACKAGE"
      | APPIUM_RUBY_TEST_PACKAGE -> "APPIUM_RUBY_TEST_PACKAGE"
      | APPIUM_WEB_JAVA_JUNIT_TEST_PACKAGE ->
          "APPIUM_WEB_JAVA_JUNIT_TEST_PACKAGE"
      | APPIUM_WEB_JAVA_TESTNG_TEST_PACKAGE ->
          "APPIUM_WEB_JAVA_TESTNG_TEST_PACKAGE"
      | APPIUM_WEB_PYTHON_TEST_PACKAGE -> "APPIUM_WEB_PYTHON_TEST_PACKAGE"
      | APPIUM_WEB_NODE_TEST_PACKAGE -> "APPIUM_WEB_NODE_TEST_PACKAGE"
      | APPIUM_WEB_RUBY_TEST_PACKAGE -> "APPIUM_WEB_RUBY_TEST_PACKAGE"
      | CALABASH_TEST_PACKAGE -> "CALABASH_TEST_PACKAGE"
      | INSTRUMENTATION_TEST_PACKAGE -> "INSTRUMENTATION_TEST_PACKAGE"
      | UIAUTOMATION_TEST_PACKAGE -> "UIAUTOMATION_TEST_PACKAGE"
      | UIAUTOMATOR_TEST_PACKAGE -> "UIAUTOMATOR_TEST_PACKAGE"
      | XCTEST_TEST_PACKAGE -> "XCTEST_TEST_PACKAGE"
      | XCTEST_UI_TEST_PACKAGE -> "XCTEST_UI_TEST_PACKAGE"
      | APPIUM_JAVA_JUNIT_TEST_SPEC -> "APPIUM_JAVA_JUNIT_TEST_SPEC"
      | APPIUM_JAVA_TESTNG_TEST_SPEC -> "APPIUM_JAVA_TESTNG_TEST_SPEC"
      | APPIUM_PYTHON_TEST_SPEC -> "APPIUM_PYTHON_TEST_SPEC"
      | APPIUM_NODE_TEST_SPEC -> "APPIUM_NODE_TEST_SPEC"
      | APPIUM_RUBY_TEST_SPEC -> "APPIUM_RUBY_TEST_SPEC"
      | APPIUM_WEB_JAVA_JUNIT_TEST_SPEC -> "APPIUM_WEB_JAVA_JUNIT_TEST_SPEC"
      | APPIUM_WEB_JAVA_TESTNG_TEST_SPEC ->
          "APPIUM_WEB_JAVA_TESTNG_TEST_SPEC"
      | APPIUM_WEB_PYTHON_TEST_SPEC -> "APPIUM_WEB_PYTHON_TEST_SPEC"
      | APPIUM_WEB_NODE_TEST_SPEC -> "APPIUM_WEB_NODE_TEST_SPEC"
      | APPIUM_WEB_RUBY_TEST_SPEC -> "APPIUM_WEB_RUBY_TEST_SPEC"
      | INSTRUMENTATION_TEST_SPEC -> "INSTRUMENTATION_TEST_SPEC"
      | XCTEST_UI_TEST_SPEC -> "XCTEST_UI_TEST_SPEC"
      | Non_static_id s -> s
    let of_string =
      function
      | "ANDROID_APP" -> ANDROID_APP
      | "IOS_APP" -> IOS_APP
      | "WEB_APP" -> WEB_APP
      | "EXTERNAL_DATA" -> EXTERNAL_DATA
      | "APPIUM_JAVA_JUNIT_TEST_PACKAGE" -> APPIUM_JAVA_JUNIT_TEST_PACKAGE
      | "APPIUM_JAVA_TESTNG_TEST_PACKAGE" -> APPIUM_JAVA_TESTNG_TEST_PACKAGE
      | "APPIUM_PYTHON_TEST_PACKAGE" -> APPIUM_PYTHON_TEST_PACKAGE
      | "APPIUM_NODE_TEST_PACKAGE" -> APPIUM_NODE_TEST_PACKAGE
      | "APPIUM_RUBY_TEST_PACKAGE" -> APPIUM_RUBY_TEST_PACKAGE
      | "APPIUM_WEB_JAVA_JUNIT_TEST_PACKAGE" ->
          APPIUM_WEB_JAVA_JUNIT_TEST_PACKAGE
      | "APPIUM_WEB_JAVA_TESTNG_TEST_PACKAGE" ->
          APPIUM_WEB_JAVA_TESTNG_TEST_PACKAGE
      | "APPIUM_WEB_PYTHON_TEST_PACKAGE" -> APPIUM_WEB_PYTHON_TEST_PACKAGE
      | "APPIUM_WEB_NODE_TEST_PACKAGE" -> APPIUM_WEB_NODE_TEST_PACKAGE
      | "APPIUM_WEB_RUBY_TEST_PACKAGE" -> APPIUM_WEB_RUBY_TEST_PACKAGE
      | "CALABASH_TEST_PACKAGE" -> CALABASH_TEST_PACKAGE
      | "INSTRUMENTATION_TEST_PACKAGE" -> INSTRUMENTATION_TEST_PACKAGE
      | "UIAUTOMATION_TEST_PACKAGE" -> UIAUTOMATION_TEST_PACKAGE
      | "UIAUTOMATOR_TEST_PACKAGE" -> UIAUTOMATOR_TEST_PACKAGE
      | "XCTEST_TEST_PACKAGE" -> XCTEST_TEST_PACKAGE
      | "XCTEST_UI_TEST_PACKAGE" -> XCTEST_UI_TEST_PACKAGE
      | "APPIUM_JAVA_JUNIT_TEST_SPEC" -> APPIUM_JAVA_JUNIT_TEST_SPEC
      | "APPIUM_JAVA_TESTNG_TEST_SPEC" -> APPIUM_JAVA_TESTNG_TEST_SPEC
      | "APPIUM_PYTHON_TEST_SPEC" -> APPIUM_PYTHON_TEST_SPEC
      | "APPIUM_NODE_TEST_SPEC" -> APPIUM_NODE_TEST_SPEC
      | "APPIUM_RUBY_TEST_SPEC" -> APPIUM_RUBY_TEST_SPEC
      | "APPIUM_WEB_JAVA_JUNIT_TEST_SPEC" -> APPIUM_WEB_JAVA_JUNIT_TEST_SPEC
      | "APPIUM_WEB_JAVA_TESTNG_TEST_SPEC" ->
          APPIUM_WEB_JAVA_TESTNG_TEST_SPEC
      | "APPIUM_WEB_PYTHON_TEST_SPEC" -> APPIUM_WEB_PYTHON_TEST_SPEC
      | "APPIUM_WEB_NODE_TEST_SPEC" -> APPIUM_WEB_NODE_TEST_SPEC
      | "APPIUM_WEB_RUBY_TEST_SPEC" -> APPIUM_WEB_RUBY_TEST_SPEC
      | "INSTRUMENTATION_TEST_SPEC" -> INSTRUMENTATION_TEST_SPEC
      | "XCTEST_UI_TEST_SPEC" -> XCTEST_UI_TEST_SPEC
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration UploadType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"UploadType" j)
    let to_json = simple_to_json to_value
  end
module UniqueProblem =
  struct
    type nonrec t =
      {
      message: Message.t option
        [@ocaml.doc "A message about the unique problems' result."];
      problems: Problems.t option
        [@ocaml.doc "Information about the problems."]}
    let make ?message = fun ?problems -> fun () -> { message; problems }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:Message.to_value));
        ("problems", (Option.map x.problems ~f:Problems.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let problems =
        (Option.map ~f:Problems.of_xml) (Xml.child xml_arg0 "problems") in
      let message =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "message") in
      make ?problems ?message ()
    let of_json json =
      let problems = field_map json "problems" Problems.of_json in
      let message = field_map json "message" Message.of_json in
      make ?problems ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A collection of one or more problems, grouped by their result."]
module Counters =
  struct
    type nonrec t =
      {
      total: Integer.t option [@ocaml.doc "The total number of entities."];
      passed: Integer.t option [@ocaml.doc "The number of passed entities."];
      failed: Integer.t option [@ocaml.doc "The number of failed entities."];
      warned: Integer.t option [@ocaml.doc "The number of warned entities."];
      errored: Integer.t option
        [@ocaml.doc "The number of errored entities."];
      stopped: Integer.t option
        [@ocaml.doc "The number of stopped entities."];
      skipped: Integer.t option
        [@ocaml.doc "The number of skipped entities."]}
    let make ?total =
      fun ?passed ->
        fun ?failed ->
          fun ?warned ->
            fun ?errored ->
              fun ?stopped ->
                fun ?skipped ->
                  fun () ->
                    {
                      total;
                      passed;
                      failed;
                      warned;
                      errored;
                      stopped;
                      skipped
                    }
    let to_value x =
      structure_to_value
        [("total", (Option.map x.total ~f:Integer.to_value));
        ("passed", (Option.map x.passed ~f:Integer.to_value));
        ("failed", (Option.map x.failed ~f:Integer.to_value));
        ("warned", (Option.map x.warned ~f:Integer.to_value));
        ("errored", (Option.map x.errored ~f:Integer.to_value));
        ("stopped", (Option.map x.stopped ~f:Integer.to_value));
        ("skipped", (Option.map x.skipped ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let skipped =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "skipped") in
      let stopped =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "stopped") in
      let errored =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "errored") in
      let warned =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "warned") in
      let failed =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "failed") in
      let passed =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "passed") in
      let total = (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "total") in
      make ?skipped ?stopped ?errored ?warned ?failed ?passed ?total ()
    let of_json json =
      let skipped = field_map json "skipped" Integer.of_json in
      let stopped = field_map json "stopped" Integer.of_json in
      let errored = field_map json "errored" Integer.of_json in
      let warned = field_map json "warned" Integer.of_json in
      let failed = field_map json "failed" Integer.of_json in
      let passed = field_map json "passed" Integer.of_json in
      let total = field_map json "total" Integer.of_json in
      make ?skipped ?stopped ?errored ?warned ?failed ?passed ?total ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents entity counters."]
module DeviceMinutes =
  struct
    type nonrec t =
      {
      total: Double.t option
        [@ocaml.doc
          "When specified, represents the total minutes used by the resource to run tests."];
      metered: Double.t option
        [@ocaml.doc
          "When specified, represents only the sum of metered minutes used by the resource to run tests."];
      unmetered: Double.t option
        [@ocaml.doc
          "When specified, represents only the sum of unmetered minutes used by the resource to run tests."]}
    let make ?total =
      fun ?metered ->
        fun ?unmetered -> fun () -> { total; metered; unmetered }
    let to_value x =
      structure_to_value
        [("total", (Option.map x.total ~f:Double.to_value));
        ("metered", (Option.map x.metered ~f:Double.to_value));
        ("unmetered", (Option.map x.unmetered ~f:Double.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unmetered =
        (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "unmetered") in
      let metered =
        (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "metered") in
      let total = (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "total") in
      make ?unmetered ?metered ?total ()
    let of_json json =
      let unmetered = field_map json "unmetered" Double.of_json in
      let metered = field_map json "metered" Double.of_json in
      let total = field_map json "total" Double.of_json in
      make ?unmetered ?metered ?total ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the total (metered or unmetered) minutes used by the resource to run tests. Contains the sum of minutes consumed by all children."]
module ExecutionStatus =
  struct
    type nonrec t =
      | PENDING 
      | PENDING_CONCURRENCY 
      | PENDING_DEVICE 
      | PROCESSING 
      | SCHEDULING 
      | PREPARING 
      | RUNNING 
      | COMPLETED 
      | STOPPING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PENDING -> "PENDING"
      | PENDING_CONCURRENCY -> "PENDING_CONCURRENCY"
      | PENDING_DEVICE -> "PENDING_DEVICE"
      | PROCESSING -> "PROCESSING"
      | SCHEDULING -> "SCHEDULING"
      | PREPARING -> "PREPARING"
      | RUNNING -> "RUNNING"
      | COMPLETED -> "COMPLETED"
      | STOPPING -> "STOPPING"
      | Non_static_id s -> s
    let of_string =
      function
      | "PENDING" -> PENDING
      | "PENDING_CONCURRENCY" -> PENDING_CONCURRENCY
      | "PENDING_DEVICE" -> PENDING_DEVICE
      | "PROCESSING" -> PROCESSING
      | "SCHEDULING" -> SCHEDULING
      | "PREPARING" -> PREPARING
      | "RUNNING" -> RUNNING
      | "COMPLETED" -> COMPLETED
      | "STOPPING" -> STOPPING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ExecutionStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ExecutionStatus" j)
    let to_json = simple_to_json to_value
  end
module TestType =
  struct
    type nonrec t =
      | BUILTIN_FUZZ 
      | BUILTIN_EXPLORER 
      | WEB_PERFORMANCE_PROFILE 
      | APPIUM_JAVA_JUNIT 
      | APPIUM_JAVA_TESTNG 
      | APPIUM_PYTHON 
      | APPIUM_NODE 
      | APPIUM_RUBY 
      | APPIUM_WEB_JAVA_JUNIT 
      | APPIUM_WEB_JAVA_TESTNG 
      | APPIUM_WEB_PYTHON 
      | APPIUM_WEB_NODE 
      | APPIUM_WEB_RUBY 
      | CALABASH 
      | INSTRUMENTATION 
      | UIAUTOMATION 
      | UIAUTOMATOR 
      | XCTEST 
      | XCTEST_UI 
      | REMOTE_ACCESS_RECORD 
      | REMOTE_ACCESS_REPLAY 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | BUILTIN_FUZZ -> "BUILTIN_FUZZ"
      | BUILTIN_EXPLORER -> "BUILTIN_EXPLORER"
      | WEB_PERFORMANCE_PROFILE -> "WEB_PERFORMANCE_PROFILE"
      | APPIUM_JAVA_JUNIT -> "APPIUM_JAVA_JUNIT"
      | APPIUM_JAVA_TESTNG -> "APPIUM_JAVA_TESTNG"
      | APPIUM_PYTHON -> "APPIUM_PYTHON"
      | APPIUM_NODE -> "APPIUM_NODE"
      | APPIUM_RUBY -> "APPIUM_RUBY"
      | APPIUM_WEB_JAVA_JUNIT -> "APPIUM_WEB_JAVA_JUNIT"
      | APPIUM_WEB_JAVA_TESTNG -> "APPIUM_WEB_JAVA_TESTNG"
      | APPIUM_WEB_PYTHON -> "APPIUM_WEB_PYTHON"
      | APPIUM_WEB_NODE -> "APPIUM_WEB_NODE"
      | APPIUM_WEB_RUBY -> "APPIUM_WEB_RUBY"
      | CALABASH -> "CALABASH"
      | INSTRUMENTATION -> "INSTRUMENTATION"
      | UIAUTOMATION -> "UIAUTOMATION"
      | UIAUTOMATOR -> "UIAUTOMATOR"
      | XCTEST -> "XCTEST"
      | XCTEST_UI -> "XCTEST_UI"
      | REMOTE_ACCESS_RECORD -> "REMOTE_ACCESS_RECORD"
      | REMOTE_ACCESS_REPLAY -> "REMOTE_ACCESS_REPLAY"
      | Non_static_id s -> s
    let of_string =
      function
      | "BUILTIN_FUZZ" -> BUILTIN_FUZZ
      | "BUILTIN_EXPLORER" -> BUILTIN_EXPLORER
      | "WEB_PERFORMANCE_PROFILE" -> WEB_PERFORMANCE_PROFILE
      | "APPIUM_JAVA_JUNIT" -> APPIUM_JAVA_JUNIT
      | "APPIUM_JAVA_TESTNG" -> APPIUM_JAVA_TESTNG
      | "APPIUM_PYTHON" -> APPIUM_PYTHON
      | "APPIUM_NODE" -> APPIUM_NODE
      | "APPIUM_RUBY" -> APPIUM_RUBY
      | "APPIUM_WEB_JAVA_JUNIT" -> APPIUM_WEB_JAVA_JUNIT
      | "APPIUM_WEB_JAVA_TESTNG" -> APPIUM_WEB_JAVA_TESTNG
      | "APPIUM_WEB_PYTHON" -> APPIUM_WEB_PYTHON
      | "APPIUM_WEB_NODE" -> APPIUM_WEB_NODE
      | "APPIUM_WEB_RUBY" -> APPIUM_WEB_RUBY
      | "CALABASH" -> CALABASH
      | "INSTRUMENTATION" -> INSTRUMENTATION
      | "UIAUTOMATION" -> UIAUTOMATION
      | "UIAUTOMATOR" -> UIAUTOMATOR
      | "XCTEST" -> XCTEST
      | "XCTEST_UI" -> XCTEST_UI
      | "REMOTE_ACCESS_RECORD" -> REMOTE_ACCESS_RECORD
      | "REMOTE_ACCESS_REPLAY" -> REMOTE_ACCESS_REPLAY
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration TestType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"TestType" j)
    let to_json = simple_to_json to_value
  end
module DeviceFarmArn =
  struct
    type nonrec t = string
    let context_ = "DeviceFarmArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:32) >>=
             (fun () ->
                (check_string_max i ~max:1011) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^arn:aws:devicefarm:.+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DeviceFarmArn" j
    let to_json = simple_to_json to_value
  end
module TestGridSessionStatus =
  struct
    type nonrec t =
      | ACTIVE 
      | CLOSED 
      | ERRORED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTIVE -> "ACTIVE"
      | CLOSED -> "CLOSED"
      | ERRORED -> "ERRORED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTIVE" -> ACTIVE
      | "CLOSED" -> CLOSED
      | "ERRORED" -> ERRORED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration TestGridSessionStatus" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"TestGridSessionStatus" j)
    let to_json = simple_to_json to_value
  end
module SensitiveString =
  struct
    type nonrec t = string
    let context_ = "SensitiveString"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SensitiveString" j
    let to_json = simple_to_json to_value
  end
module TestGridSessionArtifactType =
  struct
    type nonrec t =
      | UNKNOWN 
      | VIDEO 
      | SELENIUM_LOG 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | UNKNOWN -> "UNKNOWN"
      | VIDEO -> "VIDEO"
      | SELENIUM_LOG -> "SELENIUM_LOG"
      | Non_static_id s -> s
    let of_string =
      function
      | "UNKNOWN" -> UNKNOWN
      | "VIDEO" -> VIDEO
      | "SELENIUM_LOG" -> SELENIUM_LOG
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration TestGridSessionArtifactType"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"TestGridSessionArtifactType" j)
    let to_json = simple_to_json to_value
  end
module TestGridVpcConfig =
  struct
    type nonrec t =
      {
      securityGroupIds: SecurityGroupIds.t
        [@ocaml.doc "A list of VPC security group IDs in your Amazon VPC."];
      subnetIds: SubnetIds.t
        [@ocaml.doc "A list of VPC subnet IDs in your Amazon VPC."];
      vpcId: NonEmptyString.t [@ocaml.doc "The ID of the Amazon VPC."]}
    let context_ = "TestGridVpcConfig"
    let make ~securityGroupIds =
      fun ~subnetIds ->
        fun ~vpcId -> fun () -> { securityGroupIds; subnetIds; vpcId }
    let to_value x =
      structure_to_value
        [("securityGroupIds",
           (Some (SecurityGroupIds.to_value x.securityGroupIds)));
        ("subnetIds", (Some (SubnetIds.to_value x.subnetIds)));
        ("vpcId", (Some (NonEmptyString.to_value x.vpcId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcId =
        NonEmptyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "vpcId") in
      let subnetIds =
        SubnetIds.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "subnetIds") in
      let securityGroupIds =
        SecurityGroupIds.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "securityGroupIds") in
      make ~vpcId ~subnetIds ~securityGroupIds ()
    let of_json json =
      let vpcId = field_map_exn json "vpcId" NonEmptyString.of_json in
      let subnetIds = field_map_exn json "subnetIds" SubnetIds.of_json in
      let securityGroupIds =
        field_map_exn json "securityGroupIds" SecurityGroupIds.of_json in
      make ~vpcId ~subnetIds ~securityGroupIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The VPC security groups and subnets that are attached to a project."]
module SampleType =
  struct
    type nonrec t =
      | CPU 
      | MEMORY 
      | THREADS 
      | RX_RATE 
      | TX_RATE 
      | RX 
      | TX 
      | NATIVE_FRAMES 
      | NATIVE_FPS 
      | NATIVE_MIN_DRAWTIME 
      | NATIVE_AVG_DRAWTIME 
      | NATIVE_MAX_DRAWTIME 
      | OPENGL_FRAMES 
      | OPENGL_FPS 
      | OPENGL_MIN_DRAWTIME 
      | OPENGL_AVG_DRAWTIME 
      | OPENGL_MAX_DRAWTIME 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CPU -> "CPU"
      | MEMORY -> "MEMORY"
      | THREADS -> "THREADS"
      | RX_RATE -> "RX_RATE"
      | TX_RATE -> "TX_RATE"
      | RX -> "RX"
      | TX -> "TX"
      | NATIVE_FRAMES -> "NATIVE_FRAMES"
      | NATIVE_FPS -> "NATIVE_FPS"
      | NATIVE_MIN_DRAWTIME -> "NATIVE_MIN_DRAWTIME"
      | NATIVE_AVG_DRAWTIME -> "NATIVE_AVG_DRAWTIME"
      | NATIVE_MAX_DRAWTIME -> "NATIVE_MAX_DRAWTIME"
      | OPENGL_FRAMES -> "OPENGL_FRAMES"
      | OPENGL_FPS -> "OPENGL_FPS"
      | OPENGL_MIN_DRAWTIME -> "OPENGL_MIN_DRAWTIME"
      | OPENGL_AVG_DRAWTIME -> "OPENGL_AVG_DRAWTIME"
      | OPENGL_MAX_DRAWTIME -> "OPENGL_MAX_DRAWTIME"
      | Non_static_id s -> s
    let of_string =
      function
      | "CPU" -> CPU
      | "MEMORY" -> MEMORY
      | "THREADS" -> THREADS
      | "RX_RATE" -> RX_RATE
      | "TX_RATE" -> TX_RATE
      | "RX" -> RX
      | "TX" -> TX
      | "NATIVE_FRAMES" -> NATIVE_FRAMES
      | "NATIVE_FPS" -> NATIVE_FPS
      | "NATIVE_MIN_DRAWTIME" -> NATIVE_MIN_DRAWTIME
      | "NATIVE_AVG_DRAWTIME" -> NATIVE_AVG_DRAWTIME
      | "NATIVE_MAX_DRAWTIME" -> NATIVE_MAX_DRAWTIME
      | "OPENGL_FRAMES" -> OPENGL_FRAMES
      | "OPENGL_FPS" -> OPENGL_FPS
      | "OPENGL_MIN_DRAWTIME" -> OPENGL_MIN_DRAWTIME
      | "OPENGL_AVG_DRAWTIME" -> OPENGL_AVG_DRAWTIME
      | "OPENGL_MAX_DRAWTIME" -> OPENGL_MAX_DRAWTIME
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration SampleType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SampleType" j)
    let to_json = simple_to_json to_value
  end
module URL =
  struct
    type nonrec t = string
    let context_ = "URL"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"URL" j
    let to_json = simple_to_json to_value
  end
module BillingMethod =
  struct
    type nonrec t =
      | METERED 
      | UNMETERED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | METERED -> "METERED"
      | UNMETERED -> "UNMETERED"
      | Non_static_id s -> s
    let of_string =
      function
      | "METERED" -> METERED
      | "UNMETERED" -> UNMETERED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration BillingMethod" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"BillingMethod" j)
    let to_json = simple_to_json to_value
  end
module CustomerArtifactPaths =
  struct
    type nonrec t =
      {
      iosPaths: IosPaths.t option
        [@ocaml.doc
          "Comma-separated list of paths on the iOS device where the artifacts generated by the customer's tests are pulled from."];
      androidPaths: AndroidPaths.t option
        [@ocaml.doc
          "Comma-separated list of paths on the Android device where the artifacts generated by the customer's tests are pulled from."];
      deviceHostPaths: DeviceHostPaths.t option
        [@ocaml.doc
          "Comma-separated list of paths in the test execution environment where the artifacts generated by the customer's tests are pulled from."]}
    let make ?iosPaths =
      fun ?androidPaths ->
        fun ?deviceHostPaths ->
          fun () -> { iosPaths; androidPaths; deviceHostPaths }
    let to_value x =
      structure_to_value
        [("iosPaths", (Option.map x.iosPaths ~f:IosPaths.to_value));
        ("androidPaths",
          (Option.map x.androidPaths ~f:AndroidPaths.to_value));
        ("deviceHostPaths",
          (Option.map x.deviceHostPaths ~f:DeviceHostPaths.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deviceHostPaths =
        (Option.map ~f:DeviceHostPaths.of_xml)
          (Xml.child xml_arg0 "deviceHostPaths") in
      let androidPaths =
        (Option.map ~f:AndroidPaths.of_xml)
          (Xml.child xml_arg0 "androidPaths") in
      let iosPaths =
        (Option.map ~f:IosPaths.of_xml) (Xml.child xml_arg0 "iosPaths") in
      make ?deviceHostPaths ?androidPaths ?iosPaths ()
    let of_json json =
      let deviceHostPaths =
        field_map json "deviceHostPaths" DeviceHostPaths.of_json in
      let androidPaths = field_map json "androidPaths" AndroidPaths.of_json in
      let iosPaths = field_map json "iosPaths" IosPaths.of_json in
      make ?deviceHostPaths ?androidPaths ?iosPaths ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A JSON object that specifies the paths where the artifacts generated by the customer's tests, on the device or in the test environment, are pulled from. Specify deviceHostPaths and optionally specify either iosPaths or androidPaths. For web app tests, you can specify both iosPaths and androidPaths."]
module DeviceSelectionResult =
  struct
    type nonrec t =
      {
      filters: DeviceFilters.t option
        [@ocaml.doc "The filters in a device selection result."];
      matchedDevicesCount: Integer.t option
        [@ocaml.doc
          "The number of devices that matched the device filter selection criteria."];
      maxDevices: Integer.t option
        [@ocaml.doc
          "The maximum number of devices to be selected by a device filter and included in a test run."]}
    let make ?filters =
      fun ?matchedDevicesCount ->
        fun ?maxDevices ->
          fun () -> { filters; matchedDevicesCount; maxDevices }
    let to_value x =
      structure_to_value
        [("filters", (Option.map x.filters ~f:DeviceFilters.to_value));
        ("matchedDevicesCount",
          (Option.map x.matchedDevicesCount ~f:Integer.to_value));
        ("maxDevices", (Option.map x.maxDevices ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxDevices =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxDevices") in
      let matchedDevicesCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "matchedDevicesCount") in
      let filters =
        (Option.map ~f:DeviceFilters.of_xml) (Xml.child xml_arg0 "filters") in
      make ?maxDevices ?matchedDevicesCount ?filters ()
    let of_json json =
      let maxDevices = field_map json "maxDevices" Integer.of_json in
      let matchedDevicesCount =
        field_map json "matchedDevicesCount" Integer.of_json in
      let filters = field_map json "filters" DeviceFilters.of_json in
      make ?maxDevices ?matchedDevicesCount ?filters ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the run results requested by the device selection configuration and how many devices were returned. For an example of the JSON response syntax, see ScheduleRun."]
module ExecutionResultCode =
  struct
    type nonrec t =
      | PARSING_FAILED 
      | VPC_ENDPOINT_SETUP_FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PARSING_FAILED -> "PARSING_FAILED"
      | VPC_ENDPOINT_SETUP_FAILED -> "VPC_ENDPOINT_SETUP_FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "PARSING_FAILED" -> PARSING_FAILED
      | "VPC_ENDPOINT_SETUP_FAILED" -> VPC_ENDPOINT_SETUP_FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ExecutionResultCode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ExecutionResultCode" j)
    let to_json = simple_to_json to_value
  end
module JobTimeoutMinutes =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for JobTimeoutMinutes" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Location =
  struct
    type nonrec t =
      {
      latitude: Double.t [@ocaml.doc "The latitude."];
      longitude: Double.t [@ocaml.doc "The longitude."]}
    let context_ = "Location"
    let make ~latitude = fun ~longitude -> fun () -> { latitude; longitude }
    let to_value x =
      structure_to_value
        [("latitude", (Some (Double.to_value x.latitude)));
        ("longitude", (Some (Double.to_value x.longitude)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let longitude =
        Double.of_xml (Xml.child_exn ~context:context_ xml_arg0 "longitude") in
      let latitude =
        Double.of_xml (Xml.child_exn ~context:context_ xml_arg0 "latitude") in
      make ~longitude ~latitude ()
    let of_json json =
      let longitude = field_map_exn json "longitude" Double.of_json in
      let latitude = field_map_exn json "latitude" Double.of_json in
      make ~longitude ~latitude ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a latitude and longitude pair, expressed in geographic coordinate system degrees (for example, 47.6204, -122.3491). Elevation is currently not supported."]
module NetworkProfile =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the network profile."];
      name: Name.t option [@ocaml.doc "The name of the network profile."];
      description: Message.t option
        [@ocaml.doc "The description of the network profile."];
      type_: NetworkProfileType.t option
        [@ocaml.doc
          "The type of network profile. Valid values are listed here."];
      uplinkBandwidthBits: Long.t option
        [@ocaml.doc
          "The data throughput rate in bits per second, as an integer from 0 to 104857600."];
      downlinkBandwidthBits: Long.t option
        [@ocaml.doc
          "The data throughput rate in bits per second, as an integer from 0 to 104857600."];
      uplinkDelayMs: Long.t option
        [@ocaml.doc
          "Delay time for all packets to destination in milliseconds as an integer from 0 to 2000."];
      downlinkDelayMs: Long.t option
        [@ocaml.doc
          "Delay time for all packets to destination in milliseconds as an integer from 0 to 2000."];
      uplinkJitterMs: Long.t option
        [@ocaml.doc
          "Time variation in the delay of received packets in milliseconds as an integer from 0 to 2000."];
      downlinkJitterMs: Long.t option
        [@ocaml.doc
          "Time variation in the delay of received packets in milliseconds as an integer from 0 to 2000."];
      uplinkLossPercent: PercentInteger.t option
        [@ocaml.doc
          "Proportion of transmitted packets that fail to arrive from 0 to 100 percent."];
      downlinkLossPercent: PercentInteger.t option
        [@ocaml.doc
          "Proportion of received packets that fail to arrive from 0 to 100 percent."]}
    let make ?arn =
      fun ?name ->
        fun ?description ->
          fun ?type_ ->
            fun ?uplinkBandwidthBits ->
              fun ?downlinkBandwidthBits ->
                fun ?uplinkDelayMs ->
                  fun ?downlinkDelayMs ->
                    fun ?uplinkJitterMs ->
                      fun ?downlinkJitterMs ->
                        fun ?uplinkLossPercent ->
                          fun ?downlinkLossPercent ->
                            fun () ->
                              {
                                arn;
                                name;
                                description;
                                type_;
                                uplinkBandwidthBits;
                                downlinkBandwidthBits;
                                uplinkDelayMs;
                                downlinkDelayMs;
                                uplinkJitterMs;
                                downlinkJitterMs;
                                uplinkLossPercent;
                                downlinkLossPercent
                              }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:AmazonResourceName.to_value));
        ("name", (Option.map x.name ~f:Name.to_value));
        ("description", (Option.map x.description ~f:Message.to_value));
        ("type", (Option.map x.type_ ~f:NetworkProfileType.to_value));
        ("uplinkBandwidthBits",
          (Option.map x.uplinkBandwidthBits ~f:Long.to_value));
        ("downlinkBandwidthBits",
          (Option.map x.downlinkBandwidthBits ~f:Long.to_value));
        ("uplinkDelayMs", (Option.map x.uplinkDelayMs ~f:Long.to_value));
        ("downlinkDelayMs", (Option.map x.downlinkDelayMs ~f:Long.to_value));
        ("uplinkJitterMs", (Option.map x.uplinkJitterMs ~f:Long.to_value));
        ("downlinkJitterMs",
          (Option.map x.downlinkJitterMs ~f:Long.to_value));
        ("uplinkLossPercent",
          (Option.map x.uplinkLossPercent ~f:PercentInteger.to_value));
        ("downlinkLossPercent",
          (Option.map x.downlinkLossPercent ~f:PercentInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let downlinkLossPercent =
        (Option.map ~f:PercentInteger.of_xml)
          (Xml.child xml_arg0 "downlinkLossPercent") in
      let uplinkLossPercent =
        (Option.map ~f:PercentInteger.of_xml)
          (Xml.child xml_arg0 "uplinkLossPercent") in
      let downlinkJitterMs =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "downlinkJitterMs") in
      let uplinkJitterMs =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "uplinkJitterMs") in
      let downlinkDelayMs =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "downlinkDelayMs") in
      let uplinkDelayMs =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "uplinkDelayMs") in
      let downlinkBandwidthBits =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "downlinkBandwidthBits") in
      let uplinkBandwidthBits =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "uplinkBandwidthBits") in
      let type_ =
        (Option.map ~f:NetworkProfileType.of_xml) (Xml.child xml_arg0 "type") in
      let description =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "description") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      let arn =
        (Option.map ~f:AmazonResourceName.of_xml) (Xml.child xml_arg0 "arn") in
      make ?downlinkLossPercent ?uplinkLossPercent ?downlinkJitterMs
        ?uplinkJitterMs ?downlinkDelayMs ?uplinkDelayMs
        ?downlinkBandwidthBits ?uplinkBandwidthBits ?type_ ?description ?name
        ?arn ()
    let of_json json =
      let downlinkLossPercent =
        field_map json "downlinkLossPercent" PercentInteger.of_json in
      let uplinkLossPercent =
        field_map json "uplinkLossPercent" PercentInteger.of_json in
      let downlinkJitterMs = field_map json "downlinkJitterMs" Long.of_json in
      let uplinkJitterMs = field_map json "uplinkJitterMs" Long.of_json in
      let downlinkDelayMs = field_map json "downlinkDelayMs" Long.of_json in
      let uplinkDelayMs = field_map json "uplinkDelayMs" Long.of_json in
      let downlinkBandwidthBits =
        field_map json "downlinkBandwidthBits" Long.of_json in
      let uplinkBandwidthBits =
        field_map json "uplinkBandwidthBits" Long.of_json in
      let type_ = field_map json "type" NetworkProfileType.of_json in
      let description = field_map json "description" Message.of_json in
      let name = field_map json "name" Name.of_json in
      let arn = field_map json "arn" AmazonResourceName.of_json in
      make ?downlinkLossPercent ?uplinkLossPercent ?downlinkJitterMs
        ?uplinkJitterMs ?downlinkDelayMs ?uplinkDelayMs
        ?downlinkBandwidthBits ?uplinkBandwidthBits ?type_ ?description ?name
        ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An array of settings that describes characteristics of a network profile."]
module Radios =
  struct
    type nonrec t =
      {
      wifi: Boolean.t option
        [@ocaml.doc
          "True if Wi-Fi is enabled at the beginning of the test. Otherwise, false."];
      bluetooth: Boolean.t option
        [@ocaml.doc
          "True if Bluetooth is enabled at the beginning of the test. Otherwise, false."];
      nfc: Boolean.t option
        [@ocaml.doc
          "True if NFC is enabled at the beginning of the test. Otherwise, false."];
      gps: Boolean.t option
        [@ocaml.doc
          "True if GPS is enabled at the beginning of the test. Otherwise, false."]}
    let make ?wifi =
      fun ?bluetooth ->
        fun ?nfc -> fun ?gps -> fun () -> { wifi; bluetooth; nfc; gps }
    let to_value x =
      structure_to_value
        [("wifi", (Option.map x.wifi ~f:Boolean.to_value));
        ("bluetooth", (Option.map x.bluetooth ~f:Boolean.to_value));
        ("nfc", (Option.map x.nfc ~f:Boolean.to_value));
        ("gps", (Option.map x.gps ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let gps = (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "gps") in
      let nfc = (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "nfc") in
      let bluetooth =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "bluetooth") in
      let wifi = (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "wifi") in
      make ?gps ?nfc ?bluetooth ?wifi ()
    let of_json json =
      let gps = field_map json "gps" Boolean.of_json in
      let nfc = field_map json "nfc" Boolean.of_json in
      let bluetooth = field_map json "bluetooth" Boolean.of_json in
      let wifi = field_map json "wifi" Boolean.of_json in
      make ?gps ?nfc ?bluetooth ?wifi ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the set of radios and their states on a device. Examples of radios include Wi-Fi, GPS, Bluetooth, and NFC."]
module SkipAppResign =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module ClientId =
  struct
    type nonrec t = string
    let context_ = "ClientId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ClientId" j
    let to_json = simple_to_json to_value
  end
module HostAddress =
  struct
    type nonrec t = string
    let context_ = "HostAddress"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:1024); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"HostAddress" j
    let to_json = simple_to_json to_value
  end
module InteractionMode =
  struct
    type nonrec t =
      | INTERACTIVE 
      | NO_VIDEO 
      | VIDEO_ONLY 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | INTERACTIVE -> "INTERACTIVE"
      | NO_VIDEO -> "NO_VIDEO"
      | VIDEO_ONLY -> "VIDEO_ONLY"
      | Non_static_id s -> s
    let of_string =
      function
      | "INTERACTIVE" -> INTERACTIVE
      | "NO_VIDEO" -> NO_VIDEO
      | "VIDEO_ONLY" -> VIDEO_ONLY
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration InteractionMode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"InteractionMode" j)
    let to_json = simple_to_json to_value
  end
module OfferingPromotionIdentifier =
  struct
    type nonrec t = string
    let context_ = "OfferingPromotionIdentifier"
    let make i =
      let open Result in ok_or_failwith (check_string_min i ~min:4); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"OfferingPromotionIdentifier" j
    let to_json = simple_to_json to_value
  end
module OfferingStatus =
  struct
    type nonrec t =
      {
      type_: OfferingTransactionType.t option
        [@ocaml.doc "The type specified for the offering status."];
      offering: Offering.t option
        [@ocaml.doc "Represents the metadata of an offering status."];
      quantity: Integer.t option
        [@ocaml.doc "The number of available devices in the offering."];
      effectiveOn: DateTime.t option
        [@ocaml.doc "The date on which the offering is effective."]}
    let make ?type_ =
      fun ?offering ->
        fun ?quantity ->
          fun ?effectiveOn ->
            fun () -> { type_; offering; quantity; effectiveOn }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:OfferingTransactionType.to_value));
        ("offering", (Option.map x.offering ~f:Offering.to_value));
        ("quantity", (Option.map x.quantity ~f:Integer.to_value));
        ("effectiveOn", (Option.map x.effectiveOn ~f:DateTime.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let effectiveOn =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "effectiveOn") in
      let quantity =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "quantity") in
      let offering =
        (Option.map ~f:Offering.of_xml) (Xml.child xml_arg0 "offering") in
      let type_ =
        (Option.map ~f:OfferingTransactionType.of_xml)
          (Xml.child xml_arg0 "type") in
      make ?effectiveOn ?quantity ?offering ?type_ ()
    let of_json json =
      let effectiveOn = field_map json "effectiveOn" DateTime.of_json in
      let quantity = field_map json "quantity" Integer.of_json in
      let offering = field_map json "offering" Offering.of_json in
      let type_ = field_map json "type" OfferingTransactionType.of_json in
      make ?effectiveOn ?quantity ?offering ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The status of the offering."]
module TransactionIdentifier =
  struct
    type nonrec t = string
    let context_ = "TransactionIdentifier"
    let make i =
      let open Result in ok_or_failwith (check_string_min i ~min:32); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TransactionIdentifier" j
    let to_json = simple_to_json to_value
  end
module VideoCapture =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module DevicePoolType =
  struct
    type nonrec t =
      | CURATED 
      | PRIVATE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CURATED -> "CURATED"
      | PRIVATE -> "PRIVATE"
      | Non_static_id s -> s
    let of_string =
      function
      | "CURATED" -> CURATED
      | "PRIVATE" -> PRIVATE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration DevicePoolType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"DevicePoolType" j)
    let to_json = simple_to_json to_value
  end
module Rules =
  struct
    type nonrec t = Rule.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Rule.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Rule.of_xml)
    let of_json j = list_of_json ~kind:"Rules" ~of_json:Rule.of_json j
    let to_json v = composed_to_json to_value v
  end
module ArtifactType =
  struct
    type nonrec t =
      | UNKNOWN 
      | SCREENSHOT 
      | DEVICE_LOG 
      | MESSAGE_LOG 
      | VIDEO_LOG 
      | RESULT_LOG 
      | SERVICE_LOG 
      | WEBKIT_LOG 
      | INSTRUMENTATION_OUTPUT 
      | EXERCISER_MONKEY_OUTPUT 
      | CALABASH_JSON_OUTPUT 
      | CALABASH_PRETTY_OUTPUT 
      | CALABASH_STANDARD_OUTPUT 
      | CALABASH_JAVA_XML_OUTPUT 
      | AUTOMATION_OUTPUT 
      | APPIUM_SERVER_OUTPUT 
      | APPIUM_JAVA_OUTPUT 
      | APPIUM_JAVA_XML_OUTPUT 
      | APPIUM_PYTHON_OUTPUT 
      | APPIUM_PYTHON_XML_OUTPUT 
      | EXPLORER_EVENT_LOG 
      | EXPLORER_SUMMARY_LOG 
      | APPLICATION_CRASH_REPORT 
      | XCTEST_LOG 
      | VIDEO 
      | CUSTOMER_ARTIFACT 
      | CUSTOMER_ARTIFACT_LOG 
      | TESTSPEC_OUTPUT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | UNKNOWN -> "UNKNOWN"
      | SCREENSHOT -> "SCREENSHOT"
      | DEVICE_LOG -> "DEVICE_LOG"
      | MESSAGE_LOG -> "MESSAGE_LOG"
      | VIDEO_LOG -> "VIDEO_LOG"
      | RESULT_LOG -> "RESULT_LOG"
      | SERVICE_LOG -> "SERVICE_LOG"
      | WEBKIT_LOG -> "WEBKIT_LOG"
      | INSTRUMENTATION_OUTPUT -> "INSTRUMENTATION_OUTPUT"
      | EXERCISER_MONKEY_OUTPUT -> "EXERCISER_MONKEY_OUTPUT"
      | CALABASH_JSON_OUTPUT -> "CALABASH_JSON_OUTPUT"
      | CALABASH_PRETTY_OUTPUT -> "CALABASH_PRETTY_OUTPUT"
      | CALABASH_STANDARD_OUTPUT -> "CALABASH_STANDARD_OUTPUT"
      | CALABASH_JAVA_XML_OUTPUT -> "CALABASH_JAVA_XML_OUTPUT"
      | AUTOMATION_OUTPUT -> "AUTOMATION_OUTPUT"
      | APPIUM_SERVER_OUTPUT -> "APPIUM_SERVER_OUTPUT"
      | APPIUM_JAVA_OUTPUT -> "APPIUM_JAVA_OUTPUT"
      | APPIUM_JAVA_XML_OUTPUT -> "APPIUM_JAVA_XML_OUTPUT"
      | APPIUM_PYTHON_OUTPUT -> "APPIUM_PYTHON_OUTPUT"
      | APPIUM_PYTHON_XML_OUTPUT -> "APPIUM_PYTHON_XML_OUTPUT"
      | EXPLORER_EVENT_LOG -> "EXPLORER_EVENT_LOG"
      | EXPLORER_SUMMARY_LOG -> "EXPLORER_SUMMARY_LOG"
      | APPLICATION_CRASH_REPORT -> "APPLICATION_CRASH_REPORT"
      | XCTEST_LOG -> "XCTEST_LOG"
      | VIDEO -> "VIDEO"
      | CUSTOMER_ARTIFACT -> "CUSTOMER_ARTIFACT"
      | CUSTOMER_ARTIFACT_LOG -> "CUSTOMER_ARTIFACT_LOG"
      | TESTSPEC_OUTPUT -> "TESTSPEC_OUTPUT"
      | Non_static_id s -> s
    let of_string =
      function
      | "UNKNOWN" -> UNKNOWN
      | "SCREENSHOT" -> SCREENSHOT
      | "DEVICE_LOG" -> DEVICE_LOG
      | "MESSAGE_LOG" -> MESSAGE_LOG
      | "VIDEO_LOG" -> VIDEO_LOG
      | "RESULT_LOG" -> RESULT_LOG
      | "SERVICE_LOG" -> SERVICE_LOG
      | "WEBKIT_LOG" -> WEBKIT_LOG
      | "INSTRUMENTATION_OUTPUT" -> INSTRUMENTATION_OUTPUT
      | "EXERCISER_MONKEY_OUTPUT" -> EXERCISER_MONKEY_OUTPUT
      | "CALABASH_JSON_OUTPUT" -> CALABASH_JSON_OUTPUT
      | "CALABASH_PRETTY_OUTPUT" -> CALABASH_PRETTY_OUTPUT
      | "CALABASH_STANDARD_OUTPUT" -> CALABASH_STANDARD_OUTPUT
      | "CALABASH_JAVA_XML_OUTPUT" -> CALABASH_JAVA_XML_OUTPUT
      | "AUTOMATION_OUTPUT" -> AUTOMATION_OUTPUT
      | "APPIUM_SERVER_OUTPUT" -> APPIUM_SERVER_OUTPUT
      | "APPIUM_JAVA_OUTPUT" -> APPIUM_JAVA_OUTPUT
      | "APPIUM_JAVA_XML_OUTPUT" -> APPIUM_JAVA_XML_OUTPUT
      | "APPIUM_PYTHON_OUTPUT" -> APPIUM_PYTHON_OUTPUT
      | "APPIUM_PYTHON_XML_OUTPUT" -> APPIUM_PYTHON_XML_OUTPUT
      | "EXPLORER_EVENT_LOG" -> EXPLORER_EVENT_LOG
      | "EXPLORER_SUMMARY_LOG" -> EXPLORER_SUMMARY_LOG
      | "APPLICATION_CRASH_REPORT" -> APPLICATION_CRASH_REPORT
      | "XCTEST_LOG" -> XCTEST_LOG
      | "VIDEO" -> VIDEO
      | "CUSTOMER_ARTIFACT" -> CUSTOMER_ARTIFACT
      | "CUSTOMER_ARTIFACT_LOG" -> CUSTOMER_ARTIFACT_LOG
      | "TESTSPEC_OUTPUT" -> TESTSPEC_OUTPUT
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ArtifactType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ArtifactType" j)
    let to_json = simple_to_json to_value
  end
module IncompatibilityMessages =
  struct
    type nonrec t = IncompatibilityMessage.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:IncompatibilityMessage.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:IncompatibilityMessage.of_xml)
    let of_json j =
      list_of_json ~kind:"IncompatibilityMessages"
        ~of_json:IncompatibilityMessage.of_json j
    let to_json v = composed_to_json to_value v
  end
module ExceptionMessage =
  struct
    type nonrec t = string
    let context_ = "ExceptionMessage"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExceptionMessage" j
    let to_json = simple_to_json to_value
  end
module Tag =
  struct
    type nonrec t =
      {
      key: TagKey.t
        [@ocaml.doc
          "One part of a key-value pair that makes up a tag. A key is a general label that acts like a category for more specific tag values."];
      value: TagValue.t
        [@ocaml.doc
          "The optional part of a key-value pair that makes up a tag. A value acts as a descriptor in a tag category (key)."]}
    let context_ = "Tag"
    let make ~key = fun ~value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("Key", (Some (TagKey.to_value x.key)));
        ("Value", (Some (TagValue.to_value x.value)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        TagValue.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Value") in
      let key =
        TagKey.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Key") in
      make ~value ~key ()
    let of_json json =
      let value = field_map_exn json "Value" TagValue.of_json in
      let key = field_map_exn json "Key" TagKey.of_json in
      make ~value ~key ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The metadata that you apply to a resource to help you categorize and organize it. Each tag consists of a key and an optional value, both of which you define. Tag keys can have a maximum character length of 128 characters. Tag values can have a maximum length of 256 characters."]
module AccountsCleanup =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module AppPackagesCleanup =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module AmazonResourceNames =
  struct
    type nonrec t = AmazonResourceName.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AmazonResourceName.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AmazonResourceName.of_xml)
    let of_json j =
      list_of_json ~kind:"AmazonResourceNames"
        ~of_json:AmazonResourceName.of_json j
    let to_json v = composed_to_json to_value v
  end
module Filter =
  struct
    type nonrec t = string
    let context_ = "Filter"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:8192) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Filter" j
    let to_json = simple_to_json to_value
  end
module TestParameters =
  struct
    type nonrec t = (String_.t * String_.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((String_.of_string chopped),
                              (String_.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x -> (String_.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module VPCEConfiguration =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the VPC endpoint configuration."];
      vpceConfigurationName: VPCEConfigurationName.t option
        [@ocaml.doc
          "The friendly name you give to your VPC endpoint configuration to manage your configurations more easily."];
      vpceServiceName: VPCEServiceName.t option
        [@ocaml.doc
          "The name of the VPC endpoint service running in your AWS account that you want Device Farm to test."];
      serviceDnsName: ServiceDnsName.t option
        [@ocaml.doc
          "The DNS name that maps to the private IP address of the service you want to access."];
      vpceConfigurationDescription: VPCEConfigurationDescription.t option
        [@ocaml.doc
          "An optional description that provides details about your VPC endpoint configuration."]}
    let make ?arn =
      fun ?vpceConfigurationName ->
        fun ?vpceServiceName ->
          fun ?serviceDnsName ->
            fun ?vpceConfigurationDescription ->
              fun () ->
                {
                  arn;
                  vpceConfigurationName;
                  vpceServiceName;
                  serviceDnsName;
                  vpceConfigurationDescription
                }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:AmazonResourceName.to_value));
        ("vpceConfigurationName",
          (Option.map x.vpceConfigurationName
             ~f:VPCEConfigurationName.to_value));
        ("vpceServiceName",
          (Option.map x.vpceServiceName ~f:VPCEServiceName.to_value));
        ("serviceDnsName",
          (Option.map x.serviceDnsName ~f:ServiceDnsName.to_value));
        ("vpceConfigurationDescription",
          (Option.map x.vpceConfigurationDescription
             ~f:VPCEConfigurationDescription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpceConfigurationDescription =
        (Option.map ~f:VPCEConfigurationDescription.of_xml)
          (Xml.child xml_arg0 "vpceConfigurationDescription") in
      let serviceDnsName =
        (Option.map ~f:ServiceDnsName.of_xml)
          (Xml.child xml_arg0 "serviceDnsName") in
      let vpceServiceName =
        (Option.map ~f:VPCEServiceName.of_xml)
          (Xml.child xml_arg0 "vpceServiceName") in
      let vpceConfigurationName =
        (Option.map ~f:VPCEConfigurationName.of_xml)
          (Xml.child xml_arg0 "vpceConfigurationName") in
      let arn =
        (Option.map ~f:AmazonResourceName.of_xml) (Xml.child xml_arg0 "arn") in
      make ?vpceConfigurationDescription ?serviceDnsName ?vpceServiceName
        ?vpceConfigurationName ?arn ()
    let of_json json =
      let vpceConfigurationDescription =
        field_map json "vpceConfigurationDescription"
          VPCEConfigurationDescription.of_json in
      let serviceDnsName =
        field_map json "serviceDnsName" ServiceDnsName.of_json in
      let vpceServiceName =
        field_map json "vpceServiceName" VPCEServiceName.of_json in
      let vpceConfigurationName =
        field_map json "vpceConfigurationName" VPCEConfigurationName.of_json in
      let arn = field_map json "arn" AmazonResourceName.of_json in
      make ?vpceConfigurationDescription ?serviceDnsName ?vpceServiceName
        ?vpceConfigurationName ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents an Amazon Virtual Private Cloud (VPC) endpoint configuration."]
module Upload =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t option [@ocaml.doc "The upload's ARN."];
      name: Name.t option [@ocaml.doc "The upload's file name."];
      created: DateTime.t option [@ocaml.doc "When the upload was created."];
      type_: UploadType.t option
        [@ocaml.doc
          "The upload's type. Must be one of the following values: ANDROID_APP IOS_APP WEB_APP EXTERNAL_DATA APPIUM_JAVA_JUNIT_TEST_PACKAGE APPIUM_JAVA_TESTNG_TEST_PACKAGE APPIUM_PYTHON_TEST_PACKAGE APPIUM_NODE_TEST_PACKAGE APPIUM_RUBY_TEST_PACKAGE APPIUM_WEB_JAVA_JUNIT_TEST_PACKAGE APPIUM_WEB_JAVA_TESTNG_TEST_PACKAGE APPIUM_WEB_PYTHON_TEST_PACKAGE APPIUM_WEB_NODE_TEST_PACKAGE APPIUM_WEB_RUBY_TEST_PACKAGE CALABASH_TEST_PACKAGE INSTRUMENTATION_TEST_PACKAGE UIAUTOMATION_TEST_PACKAGE UIAUTOMATOR_TEST_PACKAGE XCTEST_TEST_PACKAGE XCTEST_UI_TEST_PACKAGE APPIUM_JAVA_JUNIT_TEST_SPEC APPIUM_JAVA_TESTNG_TEST_SPEC APPIUM_PYTHON_TEST_SPEC APPIUM_NODE_TEST_SPEC APPIUM_RUBY_TEST_SPEC APPIUM_WEB_JAVA_JUNIT_TEST_SPEC APPIUM_WEB_JAVA_TESTNG_TEST_SPEC APPIUM_WEB_PYTHON_TEST_SPEC APPIUM_WEB_NODE_TEST_SPEC APPIUM_WEB_RUBY_TEST_SPEC INSTRUMENTATION_TEST_SPEC XCTEST_UI_TEST_SPEC"];
      status: UploadStatus.t option
        [@ocaml.doc
          "The upload's status. Must be one of the following values: FAILED INITIALIZED PROCESSING SUCCEEDED"];
      url: SensitiveURL.t option
        [@ocaml.doc
          "The presigned Amazon S3 URL that was used to store a file using a PUT request."];
      metadata: Metadata.t option
        [@ocaml.doc
          "The upload's metadata. For example, for Android, this contains information that is parsed from the manifest and is displayed in the AWS Device Farm console after the associated app is uploaded."];
      contentType: ContentType.t option
        [@ocaml.doc
          "The upload's content type (for example, application/octet-stream)."];
      message: Message.t option
        [@ocaml.doc "A message about the upload's result."];
      category: UploadCategory.t option
        [@ocaml.doc
          "The upload's category. Allowed values include: CURATED: An upload managed by AWS Device Farm. PRIVATE: An upload managed by the AWS Device Farm customer."]}
    let make ?arn =
      fun ?name ->
        fun ?created ->
          fun ?type_ ->
            fun ?status ->
              fun ?url ->
                fun ?metadata ->
                  fun ?contentType ->
                    fun ?message ->
                      fun ?category ->
                        fun () ->
                          {
                            arn;
                            name;
                            created;
                            type_;
                            status;
                            url;
                            metadata;
                            contentType;
                            message;
                            category
                          }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:AmazonResourceName.to_value));
        ("name", (Option.map x.name ~f:Name.to_value));
        ("created", (Option.map x.created ~f:DateTime.to_value));
        ("type", (Option.map x.type_ ~f:UploadType.to_value));
        ("status", (Option.map x.status ~f:UploadStatus.to_value));
        ("url", (Option.map x.url ~f:SensitiveURL.to_value));
        ("metadata", (Option.map x.metadata ~f:Metadata.to_value));
        ("contentType", (Option.map x.contentType ~f:ContentType.to_value));
        ("message", (Option.map x.message ~f:Message.to_value));
        ("category", (Option.map x.category ~f:UploadCategory.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let category =
        (Option.map ~f:UploadCategory.of_xml) (Xml.child xml_arg0 "category") in
      let message =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "message") in
      let contentType =
        (Option.map ~f:ContentType.of_xml) (Xml.child xml_arg0 "contentType") in
      let metadata =
        (Option.map ~f:Metadata.of_xml) (Xml.child xml_arg0 "metadata") in
      let url =
        (Option.map ~f:SensitiveURL.of_xml) (Xml.child xml_arg0 "url") in
      let status =
        (Option.map ~f:UploadStatus.of_xml) (Xml.child xml_arg0 "status") in
      let type_ =
        (Option.map ~f:UploadType.of_xml) (Xml.child xml_arg0 "type") in
      let created =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "created") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      let arn =
        (Option.map ~f:AmazonResourceName.of_xml) (Xml.child xml_arg0 "arn") in
      make ?category ?message ?contentType ?metadata ?url ?status ?type_
        ?created ?name ?arn ()
    let of_json json =
      let category = field_map json "category" UploadCategory.of_json in
      let message = field_map json "message" Message.of_json in
      let contentType = field_map json "contentType" ContentType.of_json in
      let metadata = field_map json "metadata" Metadata.of_json in
      let url = field_map json "url" SensitiveURL.of_json in
      let status = field_map json "status" UploadStatus.of_json in
      let type_ = field_map json "type" UploadType.of_json in
      let created = field_map json "created" DateTime.of_json in
      let name = field_map json "name" Name.of_json in
      let arn = field_map json "arn" AmazonResourceName.of_json in
      make ?category ?message ?contentType ?metadata ?url ?status ?type_
        ?created ?name ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An app or a set of one or more tests to upload or that have been uploaded."]
module UniqueProblems =
  struct
    type nonrec t = UniqueProblem.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:UniqueProblem.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:UniqueProblem.of_xml)
    let of_json j =
      list_of_json ~kind:"UniqueProblems" ~of_json:UniqueProblem.of_json j
    let to_json v = composed_to_json to_value v
  end
module Test =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t option [@ocaml.doc "The test's ARN."];
      name: Name.t option [@ocaml.doc "The test's name."];
      type_: TestType.t option
        [@ocaml.doc
          "The test's type. Must be one of the following values: BUILTIN_FUZZ BUILTIN_EXPLORER For Android, an app explorer that traverses an Android app, interacting with it and capturing screenshots at the same time. APPIUM_JAVA_JUNIT APPIUM_JAVA_TESTNG APPIUM_PYTHON APPIUM_NODE APPIUM_RUBY APPIUM_WEB_JAVA_JUNIT APPIUM_WEB_JAVA_TESTNG APPIUM_WEB_PYTHON APPIUM_WEB_NODE APPIUM_WEB_RUBY CALABASH INSTRUMENTATION UIAUTOMATION UIAUTOMATOR XCTEST XCTEST_UI"];
      created: DateTime.t option [@ocaml.doc "When the test was created."];
      status: ExecutionStatus.t option
        [@ocaml.doc
          "The test's status. Allowed values include: PENDING PENDING_CONCURRENCY PENDING_DEVICE PROCESSING SCHEDULING PREPARING RUNNING COMPLETED STOPPING"];
      result: ExecutionResult.t option
        [@ocaml.doc
          "The test's result. Allowed values include: PENDING PASSED WARNED FAILED SKIPPED ERRORED STOPPED"];
      started: DateTime.t option [@ocaml.doc "The test's start time."];
      stopped: DateTime.t option [@ocaml.doc "The test's stop time."];
      counters: Counters.t option [@ocaml.doc "The test's result counters."];
      message: Message.t option
        [@ocaml.doc "A message about the test's result."];
      deviceMinutes: DeviceMinutes.t option
        [@ocaml.doc
          "Represents the total (metered or unmetered) minutes used by the test."]}
    let make ?arn =
      fun ?name ->
        fun ?type_ ->
          fun ?created ->
            fun ?status ->
              fun ?result ->
                fun ?started ->
                  fun ?stopped ->
                    fun ?counters ->
                      fun ?message ->
                        fun ?deviceMinutes ->
                          fun () ->
                            {
                              arn;
                              name;
                              type_;
                              created;
                              status;
                              result;
                              started;
                              stopped;
                              counters;
                              message;
                              deviceMinutes
                            }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:AmazonResourceName.to_value));
        ("name", (Option.map x.name ~f:Name.to_value));
        ("type", (Option.map x.type_ ~f:TestType.to_value));
        ("created", (Option.map x.created ~f:DateTime.to_value));
        ("status", (Option.map x.status ~f:ExecutionStatus.to_value));
        ("result", (Option.map x.result ~f:ExecutionResult.to_value));
        ("started", (Option.map x.started ~f:DateTime.to_value));
        ("stopped", (Option.map x.stopped ~f:DateTime.to_value));
        ("counters", (Option.map x.counters ~f:Counters.to_value));
        ("message", (Option.map x.message ~f:Message.to_value));
        ("deviceMinutes",
          (Option.map x.deviceMinutes ~f:DeviceMinutes.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deviceMinutes =
        (Option.map ~f:DeviceMinutes.of_xml)
          (Xml.child xml_arg0 "deviceMinutes") in
      let message =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "message") in
      let counters =
        (Option.map ~f:Counters.of_xml) (Xml.child xml_arg0 "counters") in
      let stopped =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "stopped") in
      let started =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "started") in
      let result =
        (Option.map ~f:ExecutionResult.of_xml) (Xml.child xml_arg0 "result") in
      let status =
        (Option.map ~f:ExecutionStatus.of_xml) (Xml.child xml_arg0 "status") in
      let created =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "created") in
      let type_ = (Option.map ~f:TestType.of_xml) (Xml.child xml_arg0 "type") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      let arn =
        (Option.map ~f:AmazonResourceName.of_xml) (Xml.child xml_arg0 "arn") in
      make ?deviceMinutes ?message ?counters ?stopped ?started ?result
        ?status ?created ?type_ ?name ?arn ()
    let of_json json =
      let deviceMinutes =
        field_map json "deviceMinutes" DeviceMinutes.of_json in
      let message = field_map json "message" Message.of_json in
      let counters = field_map json "counters" Counters.of_json in
      let stopped = field_map json "stopped" DateTime.of_json in
      let started = field_map json "started" DateTime.of_json in
      let result = field_map json "result" ExecutionResult.of_json in
      let status = field_map json "status" ExecutionStatus.of_json in
      let created = field_map json "created" DateTime.of_json in
      let type_ = field_map json "type" TestType.of_json in
      let name = field_map json "name" Name.of_json in
      let arn = field_map json "arn" AmazonResourceName.of_json in
      make ?deviceMinutes ?message ?counters ?stopped ?started ?result
        ?status ?created ?type_ ?name ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a condition that is evaluated."]
module TestGridSession =
  struct
    type nonrec t =
      {
      arn: DeviceFarmArn.t option [@ocaml.doc "The ARN of the session."];
      status: TestGridSessionStatus.t option
        [@ocaml.doc "The state of the session."];
      created: DateTime.t option
        [@ocaml.doc "The time that the session was started."];
      ended: DateTime.t option [@ocaml.doc "The time the session ended."];
      billingMinutes: Double.t option
        [@ocaml.doc
          "The number of billed minutes that were used for this session."];
      seleniumProperties: String_.t option
        [@ocaml.doc
          "A JSON object of options and parameters passed to the Selenium WebDriver."]}
    let make ?arn =
      fun ?status ->
        fun ?created ->
          fun ?ended ->
            fun ?billingMinutes ->
              fun ?seleniumProperties ->
                fun () ->
                  {
                    arn;
                    status;
                    created;
                    ended;
                    billingMinutes;
                    seleniumProperties
                  }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:DeviceFarmArn.to_value));
        ("status", (Option.map x.status ~f:TestGridSessionStatus.to_value));
        ("created", (Option.map x.created ~f:DateTime.to_value));
        ("ended", (Option.map x.ended ~f:DateTime.to_value));
        ("billingMinutes", (Option.map x.billingMinutes ~f:Double.to_value));
        ("seleniumProperties",
          (Option.map x.seleniumProperties ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let seleniumProperties =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "seleniumProperties") in
      let billingMinutes =
        (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "billingMinutes") in
      let ended =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "ended") in
      let created =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "created") in
      let status =
        (Option.map ~f:TestGridSessionStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let arn =
        (Option.map ~f:DeviceFarmArn.of_xml) (Xml.child xml_arg0 "arn") in
      make ?seleniumProperties ?billingMinutes ?ended ?created ?status ?arn
        ()
    let of_json json =
      let seleniumProperties =
        field_map json "seleniumProperties" String_.of_json in
      let billingMinutes = field_map json "billingMinutes" Double.of_json in
      let ended = field_map json "ended" DateTime.of_json in
      let created = field_map json "created" DateTime.of_json in
      let status = field_map json "status" TestGridSessionStatus.of_json in
      let arn = field_map json "arn" DeviceFarmArn.of_json in
      make ?seleniumProperties ?billingMinutes ?ended ?created ?status ?arn
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A TestGridSession is a single instance of a browser launched from the URL provided by a call to CreateTestGridUrl."]
module TestGridSessionArtifact =
  struct
    type nonrec t =
      {
      filename: String_.t option
        [@ocaml.doc "The file name of the artifact."];
      type_: TestGridSessionArtifactType.t option
        [@ocaml.doc "The kind of artifact."];
      url: SensitiveString.t option
        [@ocaml.doc "A semi-stable URL to the content of the object."]}
    let make ?filename =
      fun ?type_ -> fun ?url -> fun () -> { filename; type_; url }
    let to_value x =
      structure_to_value
        [("filename", (Option.map x.filename ~f:String_.to_value));
        ("type",
          (Option.map x.type_ ~f:TestGridSessionArtifactType.to_value));
        ("url", (Option.map x.url ~f:SensitiveString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let url =
        (Option.map ~f:SensitiveString.of_xml) (Xml.child xml_arg0 "url") in
      let type_ =
        (Option.map ~f:TestGridSessionArtifactType.of_xml)
          (Xml.child xml_arg0 "type") in
      let filename =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "filename") in
      make ?url ?type_ ?filename ()
    let of_json json =
      let url = field_map json "url" SensitiveString.of_json in
      let type_ = field_map json "type" TestGridSessionArtifactType.of_json in
      let filename = field_map json "filename" String_.of_json in
      make ?url ?type_ ?filename ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Artifacts are video and other files that are produced in the process of running a browser in an automated context. Video elements might be broken up into multiple artifacts as they grow in size during creation."]
module TestGridSessionAction =
  struct
    type nonrec t =
      {
      action: String_.t option
        [@ocaml.doc "The action taken by the session."];
      started: DateTime.t option
        [@ocaml.doc "The time that the session invoked the action."];
      duration: Long.t option
        [@ocaml.doc
          "The time, in milliseconds, that the action took to complete in the browser."];
      statusCode: String_.t option
        [@ocaml.doc
          "HTTP status code returned to the browser when the action was taken."];
      requestMethod: String_.t option
        [@ocaml.doc "HTTP method that the browser used to make the request."]}
    let make ?action =
      fun ?started ->
        fun ?duration ->
          fun ?statusCode ->
            fun ?requestMethod ->
              fun () ->
                { action; started; duration; statusCode; requestMethod }
    let to_value x =
      structure_to_value
        [("action", (Option.map x.action ~f:String_.to_value));
        ("started", (Option.map x.started ~f:DateTime.to_value));
        ("duration", (Option.map x.duration ~f:Long.to_value));
        ("statusCode", (Option.map x.statusCode ~f:String_.to_value));
        ("requestMethod", (Option.map x.requestMethod ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let requestMethod =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "requestMethod") in
      let statusCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "statusCode") in
      let duration =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "duration") in
      let started =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "started") in
      let action =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "action") in
      make ?requestMethod ?statusCode ?duration ?started ?action ()
    let of_json json =
      let requestMethod = field_map json "requestMethod" String_.of_json in
      let statusCode = field_map json "statusCode" String_.of_json in
      let duration = field_map json "duration" Long.of_json in
      let started = field_map json "started" DateTime.of_json in
      let action = field_map json "action" String_.of_json in
      make ?requestMethod ?statusCode ?duration ?started ?action ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An action taken by a TestGridSession browser instance."]
module TestGridProject =
  struct
    type nonrec t =
      {
      arn: DeviceFarmArn.t option [@ocaml.doc "The ARN for the project."];
      name: String_.t option
        [@ocaml.doc "A human-readable name for the project."];
      description: String_.t option
        [@ocaml.doc "A human-readable description for the project."];
      vpcConfig: TestGridVpcConfig.t option
        [@ocaml.doc
          "The VPC security groups and subnets that are attached to a project."];
      created: DateTime.t option [@ocaml.doc "When the project was created."]}
    let make ?arn =
      fun ?name ->
        fun ?description ->
          fun ?vpcConfig ->
            fun ?created ->
              fun () -> { arn; name; description; vpcConfig; created }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:DeviceFarmArn.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("description", (Option.map x.description ~f:String_.to_value));
        ("vpcConfig", (Option.map x.vpcConfig ~f:TestGridVpcConfig.to_value));
        ("created", (Option.map x.created ~f:DateTime.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let created =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "created") in
      let vpcConfig =
        (Option.map ~f:TestGridVpcConfig.of_xml)
          (Xml.child xml_arg0 "vpcConfig") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let arn =
        (Option.map ~f:DeviceFarmArn.of_xml) (Xml.child xml_arg0 "arn") in
      make ?created ?vpcConfig ?description ?name ?arn ()
    let of_json json =
      let created = field_map json "created" DateTime.of_json in
      let vpcConfig = field_map json "vpcConfig" TestGridVpcConfig.of_json in
      let description = field_map json "description" String_.of_json in
      let name = field_map json "name" String_.of_json in
      let arn = field_map json "arn" DeviceFarmArn.of_json in
      make ?created ?vpcConfig ?description ?name ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A Selenium testing project. Projects are used to collect and collate sessions."]
module Suite =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t option [@ocaml.doc "The suite's ARN."];
      name: Name.t option [@ocaml.doc "The suite's name."];
      type_: TestType.t option
        [@ocaml.doc
          "The suite's type. Must be one of the following values: BUILTIN_FUZZ BUILTIN_EXPLORER Only available for Android; an app explorer that traverses an Android app, interacting with it and capturing screenshots at the same time. APPIUM_JAVA_JUNIT APPIUM_JAVA_TESTNG APPIUM_PYTHON APPIUM_NODE APPIUM_RUBY APPIUM_WEB_JAVA_JUNIT APPIUM_WEB_JAVA_TESTNG APPIUM_WEB_PYTHON APPIUM_WEB_NODE APPIUM_WEB_RUBY CALABASH INSTRUMENTATION UIAUTOMATION UIAUTOMATOR XCTEST XCTEST_UI"];
      created: DateTime.t option [@ocaml.doc "When the suite was created."];
      status: ExecutionStatus.t option
        [@ocaml.doc
          "The suite's status. Allowed values include: PENDING PENDING_CONCURRENCY PENDING_DEVICE PROCESSING SCHEDULING PREPARING RUNNING COMPLETED STOPPING"];
      result: ExecutionResult.t option
        [@ocaml.doc
          "The suite's result. Allowed values include: PENDING PASSED WARNED FAILED SKIPPED ERRORED STOPPED"];
      started: DateTime.t option [@ocaml.doc "The suite's start time."];
      stopped: DateTime.t option [@ocaml.doc "The suite's stop time."];
      counters: Counters.t option [@ocaml.doc "The suite's result counters."];
      message: Message.t option
        [@ocaml.doc "A message about the suite's result."];
      deviceMinutes: DeviceMinutes.t option
        [@ocaml.doc
          "Represents the total (metered or unmetered) minutes used by the test suite."]}
    let make ?arn =
      fun ?name ->
        fun ?type_ ->
          fun ?created ->
            fun ?status ->
              fun ?result ->
                fun ?started ->
                  fun ?stopped ->
                    fun ?counters ->
                      fun ?message ->
                        fun ?deviceMinutes ->
                          fun () ->
                            {
                              arn;
                              name;
                              type_;
                              created;
                              status;
                              result;
                              started;
                              stopped;
                              counters;
                              message;
                              deviceMinutes
                            }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:AmazonResourceName.to_value));
        ("name", (Option.map x.name ~f:Name.to_value));
        ("type", (Option.map x.type_ ~f:TestType.to_value));
        ("created", (Option.map x.created ~f:DateTime.to_value));
        ("status", (Option.map x.status ~f:ExecutionStatus.to_value));
        ("result", (Option.map x.result ~f:ExecutionResult.to_value));
        ("started", (Option.map x.started ~f:DateTime.to_value));
        ("stopped", (Option.map x.stopped ~f:DateTime.to_value));
        ("counters", (Option.map x.counters ~f:Counters.to_value));
        ("message", (Option.map x.message ~f:Message.to_value));
        ("deviceMinutes",
          (Option.map x.deviceMinutes ~f:DeviceMinutes.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deviceMinutes =
        (Option.map ~f:DeviceMinutes.of_xml)
          (Xml.child xml_arg0 "deviceMinutes") in
      let message =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "message") in
      let counters =
        (Option.map ~f:Counters.of_xml) (Xml.child xml_arg0 "counters") in
      let stopped =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "stopped") in
      let started =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "started") in
      let result =
        (Option.map ~f:ExecutionResult.of_xml) (Xml.child xml_arg0 "result") in
      let status =
        (Option.map ~f:ExecutionStatus.of_xml) (Xml.child xml_arg0 "status") in
      let created =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "created") in
      let type_ = (Option.map ~f:TestType.of_xml) (Xml.child xml_arg0 "type") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      let arn =
        (Option.map ~f:AmazonResourceName.of_xml) (Xml.child xml_arg0 "arn") in
      make ?deviceMinutes ?message ?counters ?stopped ?started ?result
        ?status ?created ?type_ ?name ?arn ()
    let of_json json =
      let deviceMinutes =
        field_map json "deviceMinutes" DeviceMinutes.of_json in
      let message = field_map json "message" Message.of_json in
      let counters = field_map json "counters" Counters.of_json in
      let stopped = field_map json "stopped" DateTime.of_json in
      let started = field_map json "started" DateTime.of_json in
      let result = field_map json "result" ExecutionResult.of_json in
      let status = field_map json "status" ExecutionStatus.of_json in
      let created = field_map json "created" DateTime.of_json in
      let type_ = field_map json "type" TestType.of_json in
      let name = field_map json "name" Name.of_json in
      let arn = field_map json "arn" AmazonResourceName.of_json in
      make ?deviceMinutes ?message ?counters ?stopped ?started ?result
        ?status ?created ?type_ ?name ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a collection of one or more tests."]
module Sample =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t option [@ocaml.doc "The sample's ARN."];
      type_: SampleType.t option
        [@ocaml.doc
          "The sample's type. Must be one of the following values: CPU: A CPU sample type. This is expressed as the app processing CPU time (including child processes) as reported by process, as a percentage. MEMORY: A memory usage sample type. This is expressed as the total proportional set size of an app process, in kilobytes. NATIVE_AVG_DRAWTIME NATIVE_FPS NATIVE_FRAMES NATIVE_MAX_DRAWTIME NATIVE_MIN_DRAWTIME OPENGL_AVG_DRAWTIME OPENGL_FPS OPENGL_FRAMES OPENGL_MAX_DRAWTIME OPENGL_MIN_DRAWTIME RX RX_RATE: The total number of bytes per second (TCP and UDP) that are sent, by app process. THREADS: A threads sample type. This is expressed as the total number of threads per app process. TX TX_RATE: The total number of bytes per second (TCP and UDP) that are received, by app process."];
      url: URL.t option
        [@ocaml.doc
          "The presigned Amazon S3 URL that can be used with a GET request to download the sample's file."]}
    let make ?arn = fun ?type_ -> fun ?url -> fun () -> { arn; type_; url }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:AmazonResourceName.to_value));
        ("type", (Option.map x.type_ ~f:SampleType.to_value));
        ("url", (Option.map x.url ~f:URL.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let url = (Option.map ~f:URL.of_xml) (Xml.child xml_arg0 "url") in
      let type_ =
        (Option.map ~f:SampleType.of_xml) (Xml.child xml_arg0 "type") in
      let arn =
        (Option.map ~f:AmazonResourceName.of_xml) (Xml.child xml_arg0 "arn") in
      make ?url ?type_ ?arn ()
    let of_json json =
      let url = field_map json "url" URL.of_json in
      let type_ = field_map json "type" SampleType.of_json in
      let arn = field_map json "arn" AmazonResourceName.of_json in
      make ?url ?type_ ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a sample of performance data."]
module Run =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t option [@ocaml.doc "The run's ARN."];
      name: Name.t option [@ocaml.doc "The run's name."];
      type_: TestType.t option
        [@ocaml.doc
          "The run's type. Must be one of the following values: BUILTIN_FUZZ BUILTIN_EXPLORER For Android, an app explorer that traverses an Android app, interacting with it and capturing screenshots at the same time. APPIUM_JAVA_JUNIT APPIUM_JAVA_TESTNG APPIUM_PYTHON APPIUM_NODE APPIUM_RUBY APPIUM_WEB_JAVA_JUNIT APPIUM_WEB_JAVA_TESTNG APPIUM_WEB_PYTHON APPIUM_WEB_NODE APPIUM_WEB_RUBY CALABASH INSTRUMENTATION UIAUTOMATION UIAUTOMATOR XCTEST XCTEST_UI"];
      platform: DevicePlatform.t option
        [@ocaml.doc
          "The run's platform. Allowed values include: ANDROID IOS"];
      created: DateTime.t option [@ocaml.doc "When the run was created."];
      status: ExecutionStatus.t option
        [@ocaml.doc
          "The run's status. Allowed values include: PENDING PENDING_CONCURRENCY PENDING_DEVICE PROCESSING SCHEDULING PREPARING RUNNING COMPLETED STOPPING"];
      result: ExecutionResult.t option
        [@ocaml.doc
          "The run's result. Allowed values include: PENDING PASSED WARNED FAILED SKIPPED ERRORED STOPPED"];
      started: DateTime.t option [@ocaml.doc "The run's start time."];
      stopped: DateTime.t option [@ocaml.doc "The run's stop time."];
      counters: Counters.t option [@ocaml.doc "The run's result counters."];
      message: Message.t option
        [@ocaml.doc "A message about the run's result."];
      totalJobs: Integer.t option
        [@ocaml.doc "The total number of jobs for the run."];
      completedJobs: Integer.t option
        [@ocaml.doc "The total number of completed jobs."];
      billingMethod: BillingMethod.t option
        [@ocaml.doc
          "Specifies the billing method for a test run: metered or unmetered. If the parameter is not specified, the default value is metered. If you have unmetered device slots, you must set this to unmetered to use them. Otherwise, the run is counted toward metered device minutes."];
      deviceMinutes: DeviceMinutes.t option
        [@ocaml.doc
          "Represents the total (metered or unmetered) minutes used by the test run."];
      networkProfile: NetworkProfile.t option
        [@ocaml.doc "The network profile being used for a test run."];
      parsingResultUrl: String_.t option
        [@ocaml.doc
          "Read-only URL for an object in an S3 bucket where you can get the parsing results of the test package. If the test package doesn't parse, the reason why it doesn't parse appears in the file that this URL points to."];
      resultCode: ExecutionResultCode.t option
        [@ocaml.doc
          "Supporting field for the result field. Set only if result is SKIPPED. PARSING_FAILED if the result is skipped because of test package parsing failure."];
      seed: Integer.t option
        [@ocaml.doc
          "For fuzz tests, this is a seed to use for randomizing the UI fuzz test. Using the same seed value between tests ensures identical event sequences."];
      appUpload: AmazonResourceName.t option
        [@ocaml.doc "An app to upload or that has been uploaded."];
      eventCount: Integer.t option
        [@ocaml.doc
          "For fuzz tests, this is the number of events, between 1 and 10000, that the UI fuzz test should perform."];
      jobTimeoutMinutes: JobTimeoutMinutes.t option
        [@ocaml.doc
          "The number of minutes the job executes before it times out."];
      devicePoolArn: AmazonResourceName.t option
        [@ocaml.doc "The ARN of the device pool for the run."];
      locale: String_.t option
        [@ocaml.doc "Information about the locale that is used for the run."];
      radios: Radios.t option
        [@ocaml.doc "Information about the radio states for the run."];
      location: Location.t option
        [@ocaml.doc
          "Information about the location that is used for the run."];
      customerArtifactPaths: CustomerArtifactPaths.t option
        [@ocaml.doc "Output CustomerArtifactPaths object for the test run."];
      webUrl: String_.t option
        [@ocaml.doc
          "The Device Farm console URL for the recording of the run."];
      skipAppResign: SkipAppResign.t option
        [@ocaml.doc
          "When set to true, for private devices, Device Farm does not sign your app again. For public devices, Device Farm always signs your apps again. For more information about how Device Farm re-signs your apps, see Do you modify my app? in the AWS Device Farm FAQs."];
      testSpecArn: AmazonResourceName.t option
        [@ocaml.doc
          "The ARN of the YAML-formatted test specification for the run."];
      deviceSelectionResult: DeviceSelectionResult.t option
        [@ocaml.doc
          "The results of a device filter used to select the devices for a test run."]}
    let make ?arn =
      fun ?name ->
        fun ?type_ ->
          fun ?platform ->
            fun ?created ->
              fun ?status ->
                fun ?result ->
                  fun ?started ->
                    fun ?stopped ->
                      fun ?counters ->
                        fun ?message ->
                          fun ?totalJobs ->
                            fun ?completedJobs ->
                              fun ?billingMethod ->
                                fun ?deviceMinutes ->
                                  fun ?networkProfile ->
                                    fun ?parsingResultUrl ->
                                      fun ?resultCode ->
                                        fun ?seed ->
                                          fun ?appUpload ->
                                            fun ?eventCount ->
                                              fun ?jobTimeoutMinutes ->
                                                fun ?devicePoolArn ->
                                                  fun ?locale ->
                                                    fun ?radios ->
                                                      fun ?location ->
                                                        fun
                                                          ?customerArtifactPaths
                                                          ->
                                                          fun ?webUrl ->
                                                            fun
                                                              ?skipAppResign
                                                              ->
                                                              fun
                                                                ?testSpecArn
                                                                ->
                                                                fun
                                                                  ?deviceSelectionResult
                                                                  ->
                                                                  fun () ->
                                                                    {
                                                                    arn;
                                                                    name;
                                                                    type_;
                                                                    platform;
                                                                    created;
                                                                    status;
                                                                    result;
                                                                    started;
                                                                    stopped;
                                                                    counters;
                                                                    message;
                                                                    totalJobs;
                                                                    completedJobs;
                                                                    billingMethod;
                                                                    deviceMinutes;
                                                                    networkProfile;
                                                                    parsingResultUrl;
                                                                    resultCode;
                                                                    seed;
                                                                    appUpload;
                                                                    eventCount;
                                                                    jobTimeoutMinutes;
                                                                    devicePoolArn;
                                                                    locale;
                                                                    radios;
                                                                    location;
                                                                    customerArtifactPaths;
                                                                    webUrl;
                                                                    skipAppResign;
                                                                    testSpecArn;
                                                                    deviceSelectionResult
                                                                    }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:AmazonResourceName.to_value));
        ("name", (Option.map x.name ~f:Name.to_value));
        ("type", (Option.map x.type_ ~f:TestType.to_value));
        ("platform", (Option.map x.platform ~f:DevicePlatform.to_value));
        ("created", (Option.map x.created ~f:DateTime.to_value));
        ("status", (Option.map x.status ~f:ExecutionStatus.to_value));
        ("result", (Option.map x.result ~f:ExecutionResult.to_value));
        ("started", (Option.map x.started ~f:DateTime.to_value));
        ("stopped", (Option.map x.stopped ~f:DateTime.to_value));
        ("counters", (Option.map x.counters ~f:Counters.to_value));
        ("message", (Option.map x.message ~f:Message.to_value));
        ("totalJobs", (Option.map x.totalJobs ~f:Integer.to_value));
        ("completedJobs", (Option.map x.completedJobs ~f:Integer.to_value));
        ("billingMethod",
          (Option.map x.billingMethod ~f:BillingMethod.to_value));
        ("deviceMinutes",
          (Option.map x.deviceMinutes ~f:DeviceMinutes.to_value));
        ("networkProfile",
          (Option.map x.networkProfile ~f:NetworkProfile.to_value));
        ("parsingResultUrl",
          (Option.map x.parsingResultUrl ~f:String_.to_value));
        ("resultCode",
          (Option.map x.resultCode ~f:ExecutionResultCode.to_value));
        ("seed", (Option.map x.seed ~f:Integer.to_value));
        ("appUpload",
          (Option.map x.appUpload ~f:AmazonResourceName.to_value));
        ("eventCount", (Option.map x.eventCount ~f:Integer.to_value));
        ("jobTimeoutMinutes",
          (Option.map x.jobTimeoutMinutes ~f:JobTimeoutMinutes.to_value));
        ("devicePoolArn",
          (Option.map x.devicePoolArn ~f:AmazonResourceName.to_value));
        ("locale", (Option.map x.locale ~f:String_.to_value));
        ("radios", (Option.map x.radios ~f:Radios.to_value));
        ("location", (Option.map x.location ~f:Location.to_value));
        ("customerArtifactPaths",
          (Option.map x.customerArtifactPaths
             ~f:CustomerArtifactPaths.to_value));
        ("webUrl", (Option.map x.webUrl ~f:String_.to_value));
        ("skipAppResign",
          (Option.map x.skipAppResign ~f:SkipAppResign.to_value));
        ("testSpecArn",
          (Option.map x.testSpecArn ~f:AmazonResourceName.to_value));
        ("deviceSelectionResult",
          (Option.map x.deviceSelectionResult
             ~f:DeviceSelectionResult.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deviceSelectionResult =
        (Option.map ~f:DeviceSelectionResult.of_xml)
          (Xml.child xml_arg0 "deviceSelectionResult") in
      let testSpecArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "testSpecArn") in
      let skipAppResign =
        (Option.map ~f:SkipAppResign.of_xml)
          (Xml.child xml_arg0 "skipAppResign") in
      let webUrl =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "webUrl") in
      let customerArtifactPaths =
        (Option.map ~f:CustomerArtifactPaths.of_xml)
          (Xml.child xml_arg0 "customerArtifactPaths") in
      let location =
        (Option.map ~f:Location.of_xml) (Xml.child xml_arg0 "location") in
      let radios =
        (Option.map ~f:Radios.of_xml) (Xml.child xml_arg0 "radios") in
      let locale =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "locale") in
      let devicePoolArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "devicePoolArn") in
      let jobTimeoutMinutes =
        (Option.map ~f:JobTimeoutMinutes.of_xml)
          (Xml.child xml_arg0 "jobTimeoutMinutes") in
      let eventCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "eventCount") in
      let appUpload =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "appUpload") in
      let seed = (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "seed") in
      let resultCode =
        (Option.map ~f:ExecutionResultCode.of_xml)
          (Xml.child xml_arg0 "resultCode") in
      let parsingResultUrl =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "parsingResultUrl") in
      let networkProfile =
        (Option.map ~f:NetworkProfile.of_xml)
          (Xml.child xml_arg0 "networkProfile") in
      let deviceMinutes =
        (Option.map ~f:DeviceMinutes.of_xml)
          (Xml.child xml_arg0 "deviceMinutes") in
      let billingMethod =
        (Option.map ~f:BillingMethod.of_xml)
          (Xml.child xml_arg0 "billingMethod") in
      let completedJobs =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "completedJobs") in
      let totalJobs =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "totalJobs") in
      let message =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "message") in
      let counters =
        (Option.map ~f:Counters.of_xml) (Xml.child xml_arg0 "counters") in
      let stopped =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "stopped") in
      let started =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "started") in
      let result =
        (Option.map ~f:ExecutionResult.of_xml) (Xml.child xml_arg0 "result") in
      let status =
        (Option.map ~f:ExecutionStatus.of_xml) (Xml.child xml_arg0 "status") in
      let created =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "created") in
      let platform =
        (Option.map ~f:DevicePlatform.of_xml) (Xml.child xml_arg0 "platform") in
      let type_ = (Option.map ~f:TestType.of_xml) (Xml.child xml_arg0 "type") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      let arn =
        (Option.map ~f:AmazonResourceName.of_xml) (Xml.child xml_arg0 "arn") in
      make ?deviceSelectionResult ?testSpecArn ?skipAppResign ?webUrl
        ?customerArtifactPaths ?location ?radios ?locale ?devicePoolArn
        ?jobTimeoutMinutes ?eventCount ?appUpload ?seed ?resultCode
        ?parsingResultUrl ?networkProfile ?deviceMinutes ?billingMethod
        ?completedJobs ?totalJobs ?message ?counters ?stopped ?started
        ?result ?status ?created ?platform ?type_ ?name ?arn ()
    let of_json json =
      let deviceSelectionResult =
        field_map json "deviceSelectionResult" DeviceSelectionResult.of_json in
      let testSpecArn =
        field_map json "testSpecArn" AmazonResourceName.of_json in
      let skipAppResign =
        field_map json "skipAppResign" SkipAppResign.of_json in
      let webUrl = field_map json "webUrl" String_.of_json in
      let customerArtifactPaths =
        field_map json "customerArtifactPaths" CustomerArtifactPaths.of_json in
      let location = field_map json "location" Location.of_json in
      let radios = field_map json "radios" Radios.of_json in
      let locale = field_map json "locale" String_.of_json in
      let devicePoolArn =
        field_map json "devicePoolArn" AmazonResourceName.of_json in
      let jobTimeoutMinutes =
        field_map json "jobTimeoutMinutes" JobTimeoutMinutes.of_json in
      let eventCount = field_map json "eventCount" Integer.of_json in
      let appUpload = field_map json "appUpload" AmazonResourceName.of_json in
      let seed = field_map json "seed" Integer.of_json in
      let resultCode =
        field_map json "resultCode" ExecutionResultCode.of_json in
      let parsingResultUrl =
        field_map json "parsingResultUrl" String_.of_json in
      let networkProfile =
        field_map json "networkProfile" NetworkProfile.of_json in
      let deviceMinutes =
        field_map json "deviceMinutes" DeviceMinutes.of_json in
      let billingMethod =
        field_map json "billingMethod" BillingMethod.of_json in
      let completedJobs = field_map json "completedJobs" Integer.of_json in
      let totalJobs = field_map json "totalJobs" Integer.of_json in
      let message = field_map json "message" Message.of_json in
      let counters = field_map json "counters" Counters.of_json in
      let stopped = field_map json "stopped" DateTime.of_json in
      let started = field_map json "started" DateTime.of_json in
      let result = field_map json "result" ExecutionResult.of_json in
      let status = field_map json "status" ExecutionStatus.of_json in
      let created = field_map json "created" DateTime.of_json in
      let platform = field_map json "platform" DevicePlatform.of_json in
      let type_ = field_map json "type" TestType.of_json in
      let name = field_map json "name" Name.of_json in
      let arn = field_map json "arn" AmazonResourceName.of_json in
      make ?deviceSelectionResult ?testSpecArn ?skipAppResign ?webUrl
        ?customerArtifactPaths ?location ?radios ?locale ?devicePoolArn
        ?jobTimeoutMinutes ?eventCount ?appUpload ?seed ?resultCode
        ?parsingResultUrl ?networkProfile ?deviceMinutes ?billingMethod
        ?completedJobs ?totalJobs ?message ?counters ?stopped ?started
        ?result ?status ?created ?platform ?type_ ?name ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a test run on a set of devices with a given app package, test parameters, and so on."]
module RemoteAccessSession =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the remote access session."];
      name: Name.t option
        [@ocaml.doc "The name of the remote access session."];
      created: DateTime.t option
        [@ocaml.doc
          "The date and time the remote access session was created."];
      status: ExecutionStatus.t option
        [@ocaml.doc
          "The status of the remote access session. Can be any of the following: PENDING. PENDING_CONCURRENCY. PENDING_DEVICE. PROCESSING. SCHEDULING. PREPARING. RUNNING. COMPLETED. STOPPING."];
      result: ExecutionResult.t option
        [@ocaml.doc
          "The result of the remote access session. Can be any of the following: PENDING. PASSED. WARNED. FAILED. SKIPPED. ERRORED. STOPPED."];
      message: Message.t option
        [@ocaml.doc "A message about the remote access session."];
      started: DateTime.t option
        [@ocaml.doc
          "The date and time the remote access session was started."];
      stopped: DateTime.t option
        [@ocaml.doc
          "The date and time the remote access session was stopped."];
      device: Device.t option
        [@ocaml.doc
          "The device (phone or tablet) used in the remote access session."];
      instanceArn: AmazonResourceName.t option
        [@ocaml.doc "The ARN of the instance."];
      remoteDebugEnabled: Boolean.t option
        [@ocaml.doc
          "This flag is set to true if remote debugging is enabled for the remote access session. Remote debugging is no longer supported."];
      remoteRecordEnabled: Boolean.t option
        [@ocaml.doc
          "This flag is set to true if remote recording is enabled for the remote access session."];
      remoteRecordAppArn: AmazonResourceName.t option
        [@ocaml.doc
          "The ARN for the app to be recorded in the remote access session."];
      hostAddress: HostAddress.t option
        [@ocaml.doc
          "IP address of the EC2 host where you need to connect to remotely debug devices. Only returned if remote debugging is enabled for the remote access session. Remote debugging is no longer supported."];
      clientId: ClientId.t option
        [@ocaml.doc
          "Unique identifier of your client for the remote access session. Only returned if remote debugging is enabled for the remote access session. Remote debugging is no longer supported."];
      billingMethod: BillingMethod.t option
        [@ocaml.doc
          "The billing method of the remote access session. Possible values include METERED or UNMETERED. For more information about metered devices, see AWS Device Farm terminology."];
      deviceMinutes: DeviceMinutes.t option
        [@ocaml.doc
          "The number of minutes a device is used in a remote access session (including setup and teardown minutes)."];
      endpoint: String_.t option
        [@ocaml.doc "The endpoint for the remote access sesssion."];
      deviceUdid: String_.t option
        [@ocaml.doc
          "Unique device identifier for the remote device. Only returned if remote debugging is enabled for the remote access session. Remote debugging is no longer supported."];
      interactionMode: InteractionMode.t option
        [@ocaml.doc
          "The interaction mode of the remote access session. Valid values are: INTERACTIVE: You can interact with the iOS device by viewing, touching, and rotating the screen. You cannot run XCUITest framework-based tests in this mode. NO_VIDEO: You are connected to the device, but cannot interact with it or view the screen. This mode has the fastest test execution speed. You can run XCUITest framework-based tests in this mode. VIDEO_ONLY: You can view the screen, but cannot touch or rotate it. You can run XCUITest framework-based tests and watch the screen in this mode."];
      skipAppResign: SkipAppResign.t option
        [@ocaml.doc
          "When set to true, for private devices, Device Farm does not sign your app again. For public devices, Device Farm always signs your apps again. For more information about how Device Farm re-signs your apps, see Do you modify my app? in the AWS Device Farm FAQs."]}
    let make ?arn =
      fun ?name ->
        fun ?created ->
          fun ?status ->
            fun ?result ->
              fun ?message ->
                fun ?started ->
                  fun ?stopped ->
                    fun ?device ->
                      fun ?instanceArn ->
                        fun ?remoteDebugEnabled ->
                          fun ?remoteRecordEnabled ->
                            fun ?remoteRecordAppArn ->
                              fun ?hostAddress ->
                                fun ?clientId ->
                                  fun ?billingMethod ->
                                    fun ?deviceMinutes ->
                                      fun ?endpoint ->
                                        fun ?deviceUdid ->
                                          fun ?interactionMode ->
                                            fun ?skipAppResign ->
                                              fun () ->
                                                {
                                                  arn;
                                                  name;
                                                  created;
                                                  status;
                                                  result;
                                                  message;
                                                  started;
                                                  stopped;
                                                  device;
                                                  instanceArn;
                                                  remoteDebugEnabled;
                                                  remoteRecordEnabled;
                                                  remoteRecordAppArn;
                                                  hostAddress;
                                                  clientId;
                                                  billingMethod;
                                                  deviceMinutes;
                                                  endpoint;
                                                  deviceUdid;
                                                  interactionMode;
                                                  skipAppResign
                                                }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:AmazonResourceName.to_value));
        ("name", (Option.map x.name ~f:Name.to_value));
        ("created", (Option.map x.created ~f:DateTime.to_value));
        ("status", (Option.map x.status ~f:ExecutionStatus.to_value));
        ("result", (Option.map x.result ~f:ExecutionResult.to_value));
        ("message", (Option.map x.message ~f:Message.to_value));
        ("started", (Option.map x.started ~f:DateTime.to_value));
        ("stopped", (Option.map x.stopped ~f:DateTime.to_value));
        ("device", (Option.map x.device ~f:Device.to_value));
        ("instanceArn",
          (Option.map x.instanceArn ~f:AmazonResourceName.to_value));
        ("remoteDebugEnabled",
          (Option.map x.remoteDebugEnabled ~f:Boolean.to_value));
        ("remoteRecordEnabled",
          (Option.map x.remoteRecordEnabled ~f:Boolean.to_value));
        ("remoteRecordAppArn",
          (Option.map x.remoteRecordAppArn ~f:AmazonResourceName.to_value));
        ("hostAddress", (Option.map x.hostAddress ~f:HostAddress.to_value));
        ("clientId", (Option.map x.clientId ~f:ClientId.to_value));
        ("billingMethod",
          (Option.map x.billingMethod ~f:BillingMethod.to_value));
        ("deviceMinutes",
          (Option.map x.deviceMinutes ~f:DeviceMinutes.to_value));
        ("endpoint", (Option.map x.endpoint ~f:String_.to_value));
        ("deviceUdid", (Option.map x.deviceUdid ~f:String_.to_value));
        ("interactionMode",
          (Option.map x.interactionMode ~f:InteractionMode.to_value));
        ("skipAppResign",
          (Option.map x.skipAppResign ~f:SkipAppResign.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let skipAppResign =
        (Option.map ~f:SkipAppResign.of_xml)
          (Xml.child xml_arg0 "skipAppResign") in
      let interactionMode =
        (Option.map ~f:InteractionMode.of_xml)
          (Xml.child xml_arg0 "interactionMode") in
      let deviceUdid =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "deviceUdid") in
      let endpoint =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "endpoint") in
      let deviceMinutes =
        (Option.map ~f:DeviceMinutes.of_xml)
          (Xml.child xml_arg0 "deviceMinutes") in
      let billingMethod =
        (Option.map ~f:BillingMethod.of_xml)
          (Xml.child xml_arg0 "billingMethod") in
      let clientId =
        (Option.map ~f:ClientId.of_xml) (Xml.child xml_arg0 "clientId") in
      let hostAddress =
        (Option.map ~f:HostAddress.of_xml) (Xml.child xml_arg0 "hostAddress") in
      let remoteRecordAppArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "remoteRecordAppArn") in
      let remoteRecordEnabled =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "remoteRecordEnabled") in
      let remoteDebugEnabled =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "remoteDebugEnabled") in
      let instanceArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "instanceArn") in
      let device =
        (Option.map ~f:Device.of_xml) (Xml.child xml_arg0 "device") in
      let stopped =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "stopped") in
      let started =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "started") in
      let message =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "message") in
      let result =
        (Option.map ~f:ExecutionResult.of_xml) (Xml.child xml_arg0 "result") in
      let status =
        (Option.map ~f:ExecutionStatus.of_xml) (Xml.child xml_arg0 "status") in
      let created =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "created") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      let arn =
        (Option.map ~f:AmazonResourceName.of_xml) (Xml.child xml_arg0 "arn") in
      make ?skipAppResign ?interactionMode ?deviceUdid ?endpoint
        ?deviceMinutes ?billingMethod ?clientId ?hostAddress
        ?remoteRecordAppArn ?remoteRecordEnabled ?remoteDebugEnabled
        ?instanceArn ?device ?stopped ?started ?message ?result ?status
        ?created ?name ?arn ()
    let of_json json =
      let skipAppResign =
        field_map json "skipAppResign" SkipAppResign.of_json in
      let interactionMode =
        field_map json "interactionMode" InteractionMode.of_json in
      let deviceUdid = field_map json "deviceUdid" String_.of_json in
      let endpoint = field_map json "endpoint" String_.of_json in
      let deviceMinutes =
        field_map json "deviceMinutes" DeviceMinutes.of_json in
      let billingMethod =
        field_map json "billingMethod" BillingMethod.of_json in
      let clientId = field_map json "clientId" ClientId.of_json in
      let hostAddress = field_map json "hostAddress" HostAddress.of_json in
      let remoteRecordAppArn =
        field_map json "remoteRecordAppArn" AmazonResourceName.of_json in
      let remoteRecordEnabled =
        field_map json "remoteRecordEnabled" Boolean.of_json in
      let remoteDebugEnabled =
        field_map json "remoteDebugEnabled" Boolean.of_json in
      let instanceArn =
        field_map json "instanceArn" AmazonResourceName.of_json in
      let device = field_map json "device" Device.of_json in
      let stopped = field_map json "stopped" DateTime.of_json in
      let started = field_map json "started" DateTime.of_json in
      let message = field_map json "message" Message.of_json in
      let result = field_map json "result" ExecutionResult.of_json in
      let status = field_map json "status" ExecutionStatus.of_json in
      let created = field_map json "created" DateTime.of_json in
      let name = field_map json "name" Name.of_json in
      let arn = field_map json "arn" AmazonResourceName.of_json in
      make ?skipAppResign ?interactionMode ?deviceUdid ?endpoint
        ?deviceMinutes ?billingMethod ?clientId ?hostAddress
        ?remoteRecordAppArn ?remoteRecordEnabled ?remoteDebugEnabled
        ?instanceArn ?device ?stopped ?started ?message ?result ?status
        ?created ?name ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents information about the remote access session."]
module Project =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t option [@ocaml.doc "The project's ARN."];
      name: Name.t option [@ocaml.doc "The project's name."];
      defaultJobTimeoutMinutes: JobTimeoutMinutes.t option
        [@ocaml.doc
          "The default number of minutes (at the project level) a test run executes before it times out. The default value is 150 minutes."];
      created: DateTime.t option [@ocaml.doc "When the project was created."]}
    let make ?arn =
      fun ?name ->
        fun ?defaultJobTimeoutMinutes ->
          fun ?created ->
            fun () -> { arn; name; defaultJobTimeoutMinutes; created }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:AmazonResourceName.to_value));
        ("name", (Option.map x.name ~f:Name.to_value));
        ("defaultJobTimeoutMinutes",
          (Option.map x.defaultJobTimeoutMinutes
             ~f:JobTimeoutMinutes.to_value));
        ("created", (Option.map x.created ~f:DateTime.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let created =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "created") in
      let defaultJobTimeoutMinutes =
        (Option.map ~f:JobTimeoutMinutes.of_xml)
          (Xml.child xml_arg0 "defaultJobTimeoutMinutes") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      let arn =
        (Option.map ~f:AmazonResourceName.of_xml) (Xml.child xml_arg0 "arn") in
      make ?created ?defaultJobTimeoutMinutes ?name ?arn ()
    let of_json json =
      let created = field_map json "created" DateTime.of_json in
      let defaultJobTimeoutMinutes =
        field_map json "defaultJobTimeoutMinutes" JobTimeoutMinutes.of_json in
      let name = field_map json "name" Name.of_json in
      let arn = field_map json "arn" AmazonResourceName.of_json in
      make ?created ?defaultJobTimeoutMinutes ?name ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents an operating-system neutral workspace for running and managing tests."]
module OfferingTransaction =
  struct
    type nonrec t =
      {
      offeringStatus: OfferingStatus.t option
        [@ocaml.doc "The status of an offering transaction."];
      transactionId: TransactionIdentifier.t option
        [@ocaml.doc "The transaction ID of the offering transaction."];
      offeringPromotionId: OfferingPromotionIdentifier.t option
        [@ocaml.doc
          "The ID that corresponds to a device offering promotion."];
      createdOn: DateTime.t option
        [@ocaml.doc "The date on which an offering transaction was created."];
      cost: MonetaryAmount.t option
        [@ocaml.doc "The cost of an offering transaction."]}
    let make ?offeringStatus =
      fun ?transactionId ->
        fun ?offeringPromotionId ->
          fun ?createdOn ->
            fun ?cost ->
              fun () ->
                {
                  offeringStatus;
                  transactionId;
                  offeringPromotionId;
                  createdOn;
                  cost
                }
    let to_value x =
      structure_to_value
        [("offeringStatus",
           (Option.map x.offeringStatus ~f:OfferingStatus.to_value));
        ("transactionId",
          (Option.map x.transactionId ~f:TransactionIdentifier.to_value));
        ("offeringPromotionId",
          (Option.map x.offeringPromotionId
             ~f:OfferingPromotionIdentifier.to_value));
        ("createdOn", (Option.map x.createdOn ~f:DateTime.to_value));
        ("cost", (Option.map x.cost ~f:MonetaryAmount.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cost =
        (Option.map ~f:MonetaryAmount.of_xml) (Xml.child xml_arg0 "cost") in
      let createdOn =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "createdOn") in
      let offeringPromotionId =
        (Option.map ~f:OfferingPromotionIdentifier.of_xml)
          (Xml.child xml_arg0 "offeringPromotionId") in
      let transactionId =
        (Option.map ~f:TransactionIdentifier.of_xml)
          (Xml.child xml_arg0 "transactionId") in
      let offeringStatus =
        (Option.map ~f:OfferingStatus.of_xml)
          (Xml.child xml_arg0 "offeringStatus") in
      make ?cost ?createdOn ?offeringPromotionId ?transactionId
        ?offeringStatus ()
    let of_json json =
      let cost = field_map json "cost" MonetaryAmount.of_json in
      let createdOn = field_map json "createdOn" DateTime.of_json in
      let offeringPromotionId =
        field_map json "offeringPromotionId"
          OfferingPromotionIdentifier.of_json in
      let transactionId =
        field_map json "transactionId" TransactionIdentifier.of_json in
      let offeringStatus =
        field_map json "offeringStatus" OfferingStatus.of_json in
      make ?cost ?createdOn ?offeringPromotionId ?transactionId
        ?offeringStatus ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the metadata of an offering transaction."]
module OfferingPromotion =
  struct
    type nonrec t =
      {
      id: OfferingPromotionIdentifier.t option
        [@ocaml.doc "The ID of the offering promotion."];
      description: Message.t option
        [@ocaml.doc "A string that describes the offering promotion."]}
    let make ?id = fun ?description -> fun () -> { id; description }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:OfferingPromotionIdentifier.to_value));
        ("description", (Option.map x.description ~f:Message.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let description =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "description") in
      let id =
        (Option.map ~f:OfferingPromotionIdentifier.of_xml)
          (Xml.child xml_arg0 "id") in
      make ?description ?id ()
    let of_json json =
      let description = field_map json "description" Message.of_json in
      let id = field_map json "id" OfferingPromotionIdentifier.of_json in
      make ?description ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents information about an offering promotion."]
module Job =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t option [@ocaml.doc "The job's ARN."];
      name: Name.t option [@ocaml.doc "The job's name."];
      type_: TestType.t option
        [@ocaml.doc
          "The job's type. Allowed values include the following: BUILTIN_FUZZ BUILTIN_EXPLORER. For Android, an app explorer that traverses an Android app, interacting with it and capturing screenshots at the same time. APPIUM_JAVA_JUNIT APPIUM_JAVA_TESTNG APPIUM_PYTHON APPIUM_NODE APPIUM_RUBY APPIUM_WEB_JAVA_JUNIT APPIUM_WEB_JAVA_TESTNG APPIUM_WEB_PYTHON APPIUM_WEB_NODE APPIUM_WEB_RUBY CALABASH INSTRUMENTATION UIAUTOMATION UIAUTOMATOR XCTEST XCTEST_UI"];
      created: DateTime.t option [@ocaml.doc "When the job was created."];
      status: ExecutionStatus.t option
        [@ocaml.doc
          "The job's status. Allowed values include: PENDING PENDING_CONCURRENCY PENDING_DEVICE PROCESSING SCHEDULING PREPARING RUNNING COMPLETED STOPPING"];
      result: ExecutionResult.t option
        [@ocaml.doc
          "The job's result. Allowed values include: PENDING PASSED WARNED FAILED SKIPPED ERRORED STOPPED"];
      started: DateTime.t option [@ocaml.doc "The job's start time."];
      stopped: DateTime.t option [@ocaml.doc "The job's stop time."];
      counters: Counters.t option [@ocaml.doc "The job's result counters."];
      message: Message.t option
        [@ocaml.doc "A message about the job's result."];
      device: Device.t option [@ocaml.doc "The device (phone or tablet)."];
      instanceArn: AmazonResourceName.t option
        [@ocaml.doc "The ARN of the instance."];
      deviceMinutes: DeviceMinutes.t option
        [@ocaml.doc
          "Represents the total (metered or unmetered) minutes used by the job."];
      videoEndpoint: String_.t option
        [@ocaml.doc "The endpoint for streaming device video."];
      videoCapture: VideoCapture.t option
        [@ocaml.doc
          "This value is set to true if video capture is enabled. Otherwise, it is set to false."]}
    let make ?arn =
      fun ?name ->
        fun ?type_ ->
          fun ?created ->
            fun ?status ->
              fun ?result ->
                fun ?started ->
                  fun ?stopped ->
                    fun ?counters ->
                      fun ?message ->
                        fun ?device ->
                          fun ?instanceArn ->
                            fun ?deviceMinutes ->
                              fun ?videoEndpoint ->
                                fun ?videoCapture ->
                                  fun () ->
                                    {
                                      arn;
                                      name;
                                      type_;
                                      created;
                                      status;
                                      result;
                                      started;
                                      stopped;
                                      counters;
                                      message;
                                      device;
                                      instanceArn;
                                      deviceMinutes;
                                      videoEndpoint;
                                      videoCapture
                                    }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:AmazonResourceName.to_value));
        ("name", (Option.map x.name ~f:Name.to_value));
        ("type", (Option.map x.type_ ~f:TestType.to_value));
        ("created", (Option.map x.created ~f:DateTime.to_value));
        ("status", (Option.map x.status ~f:ExecutionStatus.to_value));
        ("result", (Option.map x.result ~f:ExecutionResult.to_value));
        ("started", (Option.map x.started ~f:DateTime.to_value));
        ("stopped", (Option.map x.stopped ~f:DateTime.to_value));
        ("counters", (Option.map x.counters ~f:Counters.to_value));
        ("message", (Option.map x.message ~f:Message.to_value));
        ("device", (Option.map x.device ~f:Device.to_value));
        ("instanceArn",
          (Option.map x.instanceArn ~f:AmazonResourceName.to_value));
        ("deviceMinutes",
          (Option.map x.deviceMinutes ~f:DeviceMinutes.to_value));
        ("videoEndpoint", (Option.map x.videoEndpoint ~f:String_.to_value));
        ("videoCapture",
          (Option.map x.videoCapture ~f:VideoCapture.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let videoCapture =
        (Option.map ~f:VideoCapture.of_xml)
          (Xml.child xml_arg0 "videoCapture") in
      let videoEndpoint =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "videoEndpoint") in
      let deviceMinutes =
        (Option.map ~f:DeviceMinutes.of_xml)
          (Xml.child xml_arg0 "deviceMinutes") in
      let instanceArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "instanceArn") in
      let device =
        (Option.map ~f:Device.of_xml) (Xml.child xml_arg0 "device") in
      let message =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "message") in
      let counters =
        (Option.map ~f:Counters.of_xml) (Xml.child xml_arg0 "counters") in
      let stopped =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "stopped") in
      let started =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "started") in
      let result =
        (Option.map ~f:ExecutionResult.of_xml) (Xml.child xml_arg0 "result") in
      let status =
        (Option.map ~f:ExecutionStatus.of_xml) (Xml.child xml_arg0 "status") in
      let created =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "created") in
      let type_ = (Option.map ~f:TestType.of_xml) (Xml.child xml_arg0 "type") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      let arn =
        (Option.map ~f:AmazonResourceName.of_xml) (Xml.child xml_arg0 "arn") in
      make ?videoCapture ?videoEndpoint ?deviceMinutes ?instanceArn ?device
        ?message ?counters ?stopped ?started ?result ?status ?created ?type_
        ?name ?arn ()
    let of_json json =
      let videoCapture = field_map json "videoCapture" VideoCapture.of_json in
      let videoEndpoint = field_map json "videoEndpoint" String_.of_json in
      let deviceMinutes =
        field_map json "deviceMinutes" DeviceMinutes.of_json in
      let instanceArn =
        field_map json "instanceArn" AmazonResourceName.of_json in
      let device = field_map json "device" Device.of_json in
      let message = field_map json "message" Message.of_json in
      let counters = field_map json "counters" Counters.of_json in
      let stopped = field_map json "stopped" DateTime.of_json in
      let started = field_map json "started" DateTime.of_json in
      let result = field_map json "result" ExecutionResult.of_json in
      let status = field_map json "status" ExecutionStatus.of_json in
      let created = field_map json "created" DateTime.of_json in
      let type_ = field_map json "type" TestType.of_json in
      let name = field_map json "name" Name.of_json in
      let arn = field_map json "arn" AmazonResourceName.of_json in
      make ?videoCapture ?videoEndpoint ?deviceMinutes ?instanceArn ?device
        ?message ?counters ?stopped ?started ?result ?status ?created ?type_
        ?name ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a device."]
module DevicePool =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t option [@ocaml.doc "The device pool's ARN."];
      name: Name.t option [@ocaml.doc "The device pool's name."];
      description: Message.t option
        [@ocaml.doc "The device pool's description."];
      type_: DevicePoolType.t option
        [@ocaml.doc
          "The device pool's type. Allowed values include: CURATED: A device pool that is created and managed by AWS Device Farm. PRIVATE: A device pool that is created and managed by the device pool developer."];
      rules: Rules.t option
        [@ocaml.doc "Information about the device pool's rules."];
      maxDevices: Integer.t option
        [@ocaml.doc
          "The number of devices that Device Farm can add to your device pool. Device Farm adds devices that are available and meet the criteria that you assign for the rules parameter. Depending on how many devices meet these constraints, your device pool might contain fewer devices than the value for this parameter. By specifying the maximum number of devices, you can control the costs that you incur by running tests."]}
    let make ?arn =
      fun ?name ->
        fun ?description ->
          fun ?type_ ->
            fun ?rules ->
              fun ?maxDevices ->
                fun () ->
                  { arn; name; description; type_; rules; maxDevices }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:AmazonResourceName.to_value));
        ("name", (Option.map x.name ~f:Name.to_value));
        ("description", (Option.map x.description ~f:Message.to_value));
        ("type", (Option.map x.type_ ~f:DevicePoolType.to_value));
        ("rules", (Option.map x.rules ~f:Rules.to_value));
        ("maxDevices", (Option.map x.maxDevices ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxDevices =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxDevices") in
      let rules = (Option.map ~f:Rules.of_xml) (Xml.child xml_arg0 "rules") in
      let type_ =
        (Option.map ~f:DevicePoolType.of_xml) (Xml.child xml_arg0 "type") in
      let description =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "description") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      let arn =
        (Option.map ~f:AmazonResourceName.of_xml) (Xml.child xml_arg0 "arn") in
      make ?maxDevices ?rules ?type_ ?description ?name ?arn ()
    let of_json json =
      let maxDevices = field_map json "maxDevices" Integer.of_json in
      let rules = field_map json "rules" Rules.of_json in
      let type_ = field_map json "type" DevicePoolType.of_json in
      let description = field_map json "description" Message.of_json in
      let name = field_map json "name" Name.of_json in
      let arn = field_map json "arn" AmazonResourceName.of_json in
      make ?maxDevices ?rules ?type_ ?description ?name ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a collection of device types."]
module Artifact =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t option [@ocaml.doc "The artifact's ARN."];
      name: Name.t option [@ocaml.doc "The artifact's name."];
      type_: ArtifactType.t option
        [@ocaml.doc
          "The artifact's type. Allowed values include the following: UNKNOWN SCREENSHOT DEVICE_LOG MESSAGE_LOG VIDEO_LOG RESULT_LOG SERVICE_LOG WEBKIT_LOG INSTRUMENTATION_OUTPUT EXERCISER_MONKEY_OUTPUT: the artifact (log) generated by an Android fuzz test. CALABASH_JSON_OUTPUT CALABASH_PRETTY_OUTPUT CALABASH_STANDARD_OUTPUT CALABASH_JAVA_XML_OUTPUT AUTOMATION_OUTPUT APPIUM_SERVER_OUTPUT APPIUM_JAVA_OUTPUT APPIUM_JAVA_XML_OUTPUT APPIUM_PYTHON_OUTPUT APPIUM_PYTHON_XML_OUTPUT EXPLORER_EVENT_LOG EXPLORER_SUMMARY_LOG APPLICATION_CRASH_REPORT XCTEST_LOG VIDEO CUSTOMER_ARTIFACT CUSTOMER_ARTIFACT_LOG TESTSPEC_OUTPUT"];
      extension: String_.t option
        [@ocaml.doc "The artifact's file extension."];
      url: URL.t option
        [@ocaml.doc
          "The presigned Amazon S3 URL that can be used with a GET request to download the artifact's file."]}
    let make ?arn =
      fun ?name ->
        fun ?type_ ->
          fun ?extension ->
            fun ?url -> fun () -> { arn; name; type_; extension; url }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:AmazonResourceName.to_value));
        ("name", (Option.map x.name ~f:Name.to_value));
        ("type", (Option.map x.type_ ~f:ArtifactType.to_value));
        ("extension", (Option.map x.extension ~f:String_.to_value));
        ("url", (Option.map x.url ~f:URL.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let url = (Option.map ~f:URL.of_xml) (Xml.child xml_arg0 "url") in
      let extension =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "extension") in
      let type_ =
        (Option.map ~f:ArtifactType.of_xml) (Xml.child xml_arg0 "type") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      let arn =
        (Option.map ~f:AmazonResourceName.of_xml) (Xml.child xml_arg0 "arn") in
      make ?url ?extension ?type_ ?name ?arn ()
    let of_json json =
      let url = field_map json "url" URL.of_json in
      let extension = field_map json "extension" String_.of_json in
      let type_ = field_map json "type" ArtifactType.of_json in
      let name = field_map json "name" Name.of_json in
      let arn = field_map json "arn" AmazonResourceName.of_json in
      make ?url ?extension ?type_ ?name ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the output of a test. Examples of artifacts include logs and screenshots."]
module DevicePoolCompatibilityResult =
  struct
    type nonrec t =
      {
      device: Device.t option
        [@ocaml.doc
          "The device (phone or tablet) to return information about."];
      compatible: Boolean.t option
        [@ocaml.doc
          "Whether the result was compatible with the device pool."];
      incompatibilityMessages: IncompatibilityMessages.t option
        [@ocaml.doc "Information about the compatibility."]}
    let make ?device =
      fun ?compatible ->
        fun ?incompatibilityMessages ->
          fun () -> { device; compatible; incompatibilityMessages }
    let to_value x =
      structure_to_value
        [("device", (Option.map x.device ~f:Device.to_value));
        ("compatible", (Option.map x.compatible ~f:Boolean.to_value));
        ("incompatibilityMessages",
          (Option.map x.incompatibilityMessages
             ~f:IncompatibilityMessages.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let incompatibilityMessages =
        (Option.map ~f:IncompatibilityMessages.of_xml)
          (Xml.child xml_arg0 "incompatibilityMessages") in
      let compatible =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "compatible") in
      let device =
        (Option.map ~f:Device.of_xml) (Xml.child xml_arg0 "device") in
      make ?incompatibilityMessages ?compatible ?device ()
    let of_json json =
      let incompatibilityMessages =
        field_map json "incompatibilityMessages"
          IncompatibilityMessages.of_json in
      let compatible = field_map json "compatible" Boolean.of_json in
      let device = field_map json "device" Device.of_json in
      make ?incompatibilityMessages ?compatible ?device ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a device pool compatibility result."]
module AWSAccountNumber =
  struct
    type nonrec t = string
    let context_ = "AWSAccountNumber"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:16) >>=
             (fun () -> check_string_min i ~min:2));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AWSAccountNumber" j
    let to_json = simple_to_json to_value
  end
module MaxSlotMap =
  struct
    type nonrec t = (String_.t * Integer.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((String_.of_string chopped),
                              (Integer.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x -> (Integer.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:Integer.of_json j
    let to_json v = composed_to_json to_value v
  end
module PurchasedDevicesMap =
  struct
    type nonrec t = (DevicePlatform.t * Integer.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((DevicePlatform.of_string chopped),
                              (Integer.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (DevicePlatform.to_value x) |>
                    (fun x -> (Integer.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:DevicePlatform.of_string
        ~of_json:Integer.of_json j
    let to_json v = composed_to_json to_value v
  end
module TrialMinutes =
  struct
    type nonrec t =
      {
      total: Double.t option
        [@ocaml.doc
          "The total number of free trial minutes that the account started with."];
      remaining: Double.t option
        [@ocaml.doc
          "The number of free trial minutes remaining in the account."]}
    let make ?total = fun ?remaining -> fun () -> { total; remaining }
    let to_value x =
      structure_to_value
        [("total", (Option.map x.total ~f:Double.to_value));
        ("remaining", (Option.map x.remaining ~f:Double.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let remaining =
        (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "remaining") in
      let total = (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "total") in
      make ?remaining ?total ()
    let of_json json =
      let remaining = field_map json "remaining" Double.of_json in
      let total = field_map json "total" Double.of_json in
      make ?remaining ?total ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents information about free trial device minutes for an AWS account."]
module ArgumentException =
  struct
    type nonrec t =
      {
      message: Message.t option
        [@ocaml.doc "Any additional information about the exception."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:Message.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" Message.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An invalid argument was specified."]
module InvalidOperationException =
  struct
    type nonrec t = {
      message: Message.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:Message.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" Message.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "There was an error with the update request, or you do not have sufficient permissions to update this VPC endpoint configuration."]
module NotFoundException =
  struct
    type nonrec t =
      {
      message: Message.t option
        [@ocaml.doc "Any additional information about the exception."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:Message.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" Message.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The specified entity was not found."]
module ServiceAccountException =
  struct
    type nonrec t =
      {
      message: Message.t option
        [@ocaml.doc "Any additional information about the exception."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:Message.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" Message.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "There was a problem with the service account."]
module LimitExceededException =
  struct
    type nonrec t =
      {
      message: Message.t option
        [@ocaml.doc "Any additional information about the exception."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:Message.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" Message.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A limit was exceeded."]
module InternalServiceException =
  struct
    type nonrec t = {
      message: Message.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:Message.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" Message.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An internal exception was raised in the service. Contact aws-devicefarm-support@amazon.com if you see this error."]
module ResourceDescription =
  struct
    type nonrec t = string
    let context_ = "ResourceDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () -> check_pattern i ~pattern:".*\\S.*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceDescription" j
    let to_json = simple_to_json to_value
  end
module ResourceName =
  struct
    type nonrec t = string
    let context_ = "ResourceName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:".*\\S.*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceName" j
    let to_json = simple_to_json to_value
  end
module TagOperationException =
  struct
    type nonrec t =
      {
      message: ExceptionMessage.t option ;
      resourceName: AmazonResourceName.t option }
    let make ?message =
      fun ?resourceName -> fun () -> { message; resourceName }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ExceptionMessage.to_value));
        ("resourceName",
          (Option.map x.resourceName ~f:AmazonResourceName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceName =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "resourceName") in
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "message") in
      make ?resourceName ?message ()
    let of_json json =
      let resourceName =
        field_map json "resourceName" AmazonResourceName.of_json in
      let message = field_map json "message" ExceptionMessage.of_json in
      make ?resourceName ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The operation was not successful. Try again."]
module TagKeyList =
  struct
    type nonrec t = TagKey.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:150); i
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeyList" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module TagPolicyException =
  struct
    type nonrec t =
      {
      message: ExceptionMessage.t option ;
      resourceName: AmazonResourceName.t option }
    let make ?message =
      fun ?resourceName -> fun () -> { message; resourceName }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ExceptionMessage.to_value));
        ("resourceName",
          (Option.map x.resourceName ~f:AmazonResourceName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceName =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "resourceName") in
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "message") in
      make ?resourceName ?message ()
    let of_json json =
      let resourceName =
        field_map json "resourceName" AmazonResourceName.of_json in
      let message = field_map json "message" ExceptionMessage.of_json in
      make ?resourceName ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request doesn't comply with the AWS Identity and Access Management (IAM) tag policy. Correct your request and then retry it."]
module TooManyTagsException =
  struct
    type nonrec t =
      {
      message: ExceptionMessage.t option ;
      resourceName: AmazonResourceName.t option }
    let make ?message =
      fun ?resourceName -> fun () -> { message; resourceName }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ExceptionMessage.to_value));
        ("resourceName",
          (Option.map x.resourceName ~f:AmazonResourceName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceName =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "resourceName") in
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "message") in
      make ?resourceName ?message ()
    let of_json json =
      let resourceName =
        field_map json "resourceName" AmazonResourceName.of_json in
      let message = field_map json "message" ExceptionMessage.of_json in
      make ?resourceName ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The list of tags on the repository is over the limit. The maximum number of tags that can be applied to a repository is 50."]
module TagList =
  struct
    type nonrec t = Tag.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:150); i
    let to_value xs =
      (xs |> (List.map ~f:Tag.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tag.of_xml)
    let of_json j = list_of_json ~kind:"TagList" ~of_json:Tag.of_json j
    let to_json v = composed_to_json to_value v
  end
module IdempotencyException =
  struct
    type nonrec t =
      {
      message: Message.t option
        [@ocaml.doc "Any additional information about the exception."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:Message.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" Message.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An entity with the same name already exists."]
module DeviceSelectionConfiguration =
  struct
    type nonrec t =
      {
      filters: DeviceFilters.t
        [@ocaml.doc
          "Used to dynamically select a set of devices for a test run. A filter is made up of an attribute, an operator, and one or more values. Attribute The aspect of a device such as platform or model used as the selection criteria in a device filter. Allowed values include: ARN: The Amazon Resource Name (ARN) of the device (for example, arn:aws:devicefarm:us-west-2::device:12345Example). PLATFORM: The device platform. Valid values are ANDROID or IOS. OS_VERSION: The operating system version (for example, 10.3.2). MODEL: The device model (for example, iPad 5th Gen). AVAILABILITY: The current availability of the device. Valid values are AVAILABLE, HIGHLY_AVAILABLE, BUSY, or TEMPORARY_NOT_AVAILABLE. FORM_FACTOR: The device form factor. Valid values are PHONE or TABLET. MANUFACTURER: The device manufacturer (for example, Apple). REMOTE_ACCESS_ENABLED: Whether the device is enabled for remote access. Valid values are TRUE or FALSE. REMOTE_DEBUG_ENABLED: Whether the device is enabled for remote debugging. Valid values are TRUE or FALSE. Because remote debugging is no longer supported, this filter is ignored. INSTANCE_ARN: The Amazon Resource Name (ARN) of the device instance. INSTANCE_LABELS: The label of the device instance. FLEET_TYPE: The fleet type. Valid values are PUBLIC or PRIVATE. Operator The filter operator. The EQUALS operator is available for every attribute except INSTANCE_LABELS. The CONTAINS operator is available for the INSTANCE_LABELS and MODEL attributes. The IN and NOT_IN operators are available for the ARN, OS_VERSION, MODEL, MANUFACTURER, and INSTANCE_ARN attributes. The LESS_THAN, GREATER_THAN, LESS_THAN_OR_EQUALS, and GREATER_THAN_OR_EQUALS operators are also available for the OS_VERSION attribute. Values An array of one or more filter values. Operator Values The IN and NOT_IN operators can take a values array that has more than one element. The other operators require an array with a single element. Attribute Values The PLATFORM attribute can be set to ANDROID or IOS. The AVAILABILITY attribute can be set to AVAILABLE, HIGHLY_AVAILABLE, BUSY, or TEMPORARY_NOT_AVAILABLE. The FORM_FACTOR attribute can be set to PHONE or TABLET. The FLEET_TYPE attribute can be set to PUBLIC or PRIVATE."];
      maxDevices: Integer.t
        [@ocaml.doc
          "The maximum number of devices to be included in a test run."]}
    let context_ = "DeviceSelectionConfiguration"
    let make ~filters = fun ~maxDevices -> fun () -> { filters; maxDevices }
    let to_value x =
      structure_to_value
        [("filters", (Some (DeviceFilters.to_value x.filters)));
        ("maxDevices", (Some (Integer.to_value x.maxDevices)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxDevices =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "maxDevices") in
      let filters =
        DeviceFilters.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "filters") in
      make ~maxDevices ~filters ()
    let of_json json =
      let maxDevices = field_map_exn json "maxDevices" Integer.of_json in
      let filters = field_map_exn json "filters" DeviceFilters.of_json in
      make ~maxDevices ~filters ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the device filters used in a test run and the maximum number of devices to be included in the run. It is passed in as the deviceSelectionConfiguration request parameter in ScheduleRun."]
module ExecutionConfiguration =
  struct
    type nonrec t =
      {
      jobTimeoutMinutes: JobTimeoutMinutes.t option
        [@ocaml.doc
          "The number of minutes a test run executes before it times out."];
      accountsCleanup: AccountsCleanup.t option
        [@ocaml.doc
          "True if account cleanup is enabled at the beginning of the test. Otherwise, false."];
      appPackagesCleanup: AppPackagesCleanup.t option
        [@ocaml.doc
          "True if app package cleanup is enabled at the beginning of the test. Otherwise, false."];
      videoCapture: VideoCapture.t option
        [@ocaml.doc
          "Set to true to enable video capture. Otherwise, set to false. The default is true."];
      skipAppResign: SkipAppResign.t option
        [@ocaml.doc
          "When set to true, for private devices, Device Farm does not sign your app again. For public devices, Device Farm always signs your apps again. For more information about how Device Farm re-signs your apps, see Do you modify my app? in the AWS Device Farm FAQs."]}
    let make ?jobTimeoutMinutes =
      fun ?accountsCleanup ->
        fun ?appPackagesCleanup ->
          fun ?videoCapture ->
            fun ?skipAppResign ->
              fun () ->
                {
                  jobTimeoutMinutes;
                  accountsCleanup;
                  appPackagesCleanup;
                  videoCapture;
                  skipAppResign
                }
    let to_value x =
      structure_to_value
        [("jobTimeoutMinutes",
           (Option.map x.jobTimeoutMinutes ~f:JobTimeoutMinutes.to_value));
        ("accountsCleanup",
          (Option.map x.accountsCleanup ~f:AccountsCleanup.to_value));
        ("appPackagesCleanup",
          (Option.map x.appPackagesCleanup ~f:AppPackagesCleanup.to_value));
        ("videoCapture",
          (Option.map x.videoCapture ~f:VideoCapture.to_value));
        ("skipAppResign",
          (Option.map x.skipAppResign ~f:SkipAppResign.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let skipAppResign =
        (Option.map ~f:SkipAppResign.of_xml)
          (Xml.child xml_arg0 "skipAppResign") in
      let videoCapture =
        (Option.map ~f:VideoCapture.of_xml)
          (Xml.child xml_arg0 "videoCapture") in
      let appPackagesCleanup =
        (Option.map ~f:AppPackagesCleanup.of_xml)
          (Xml.child xml_arg0 "appPackagesCleanup") in
      let accountsCleanup =
        (Option.map ~f:AccountsCleanup.of_xml)
          (Xml.child xml_arg0 "accountsCleanup") in
      let jobTimeoutMinutes =
        (Option.map ~f:JobTimeoutMinutes.of_xml)
          (Xml.child xml_arg0 "jobTimeoutMinutes") in
      make ?skipAppResign ?videoCapture ?appPackagesCleanup ?accountsCleanup
        ?jobTimeoutMinutes ()
    let of_json json =
      let skipAppResign =
        field_map json "skipAppResign" SkipAppResign.of_json in
      let videoCapture = field_map json "videoCapture" VideoCapture.of_json in
      let appPackagesCleanup =
        field_map json "appPackagesCleanup" AppPackagesCleanup.of_json in
      let accountsCleanup =
        field_map json "accountsCleanup" AccountsCleanup.of_json in
      let jobTimeoutMinutes =
        field_map json "jobTimeoutMinutes" JobTimeoutMinutes.of_json in
      make ?skipAppResign ?videoCapture ?appPackagesCleanup ?accountsCleanup
        ?jobTimeoutMinutes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents configuration information about a test run, such as the execution timeout (in minutes)."]
module ScheduleRunConfiguration =
  struct
    type nonrec t =
      {
      extraDataPackageArn: AmazonResourceName.t option
        [@ocaml.doc
          "The ARN of the extra data for the run. The extra data is a .zip file that AWS Device Farm extracts to external data for Android or the app's sandbox for iOS."];
      networkProfileArn: AmazonResourceName.t option
        [@ocaml.doc "Reserved for internal use."];
      locale: String_.t option
        [@ocaml.doc "Information about the locale that is used for the run."];
      location: Location.t option
        [@ocaml.doc
          "Information about the location that is used for the run."];
      vpceConfigurationArns: AmazonResourceNames.t option
        [@ocaml.doc "An array of ARNs for your VPC endpoint configurations."];
      customerArtifactPaths: CustomerArtifactPaths.t option
        [@ocaml.doc
          "Input CustomerArtifactPaths object for the scheduled run configuration."];
      radios: Radios.t option
        [@ocaml.doc "Information about the radio states for the run."];
      auxiliaryApps: AmazonResourceNames.t option
        [@ocaml.doc
          "A list of upload ARNs for app packages to be installed with your app."];
      billingMethod: BillingMethod.t option
        [@ocaml.doc
          "Specifies the billing method for a test run: metered or unmetered. If the parameter is not specified, the default value is metered. If you have purchased unmetered device slots, you must set this parameter to unmetered to make use of them. Otherwise, your run counts against your metered time."]}
    let make ?extraDataPackageArn =
      fun ?networkProfileArn ->
        fun ?locale ->
          fun ?location ->
            fun ?vpceConfigurationArns ->
              fun ?customerArtifactPaths ->
                fun ?radios ->
                  fun ?auxiliaryApps ->
                    fun ?billingMethod ->
                      fun () ->
                        {
                          extraDataPackageArn;
                          networkProfileArn;
                          locale;
                          location;
                          vpceConfigurationArns;
                          customerArtifactPaths;
                          radios;
                          auxiliaryApps;
                          billingMethod
                        }
    let to_value x =
      structure_to_value
        [("extraDataPackageArn",
           (Option.map x.extraDataPackageArn ~f:AmazonResourceName.to_value));
        ("networkProfileArn",
          (Option.map x.networkProfileArn ~f:AmazonResourceName.to_value));
        ("locale", (Option.map x.locale ~f:String_.to_value));
        ("location", (Option.map x.location ~f:Location.to_value));
        ("vpceConfigurationArns",
          (Option.map x.vpceConfigurationArns ~f:AmazonResourceNames.to_value));
        ("customerArtifactPaths",
          (Option.map x.customerArtifactPaths
             ~f:CustomerArtifactPaths.to_value));
        ("radios", (Option.map x.radios ~f:Radios.to_value));
        ("auxiliaryApps",
          (Option.map x.auxiliaryApps ~f:AmazonResourceNames.to_value));
        ("billingMethod",
          (Option.map x.billingMethod ~f:BillingMethod.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let billingMethod =
        (Option.map ~f:BillingMethod.of_xml)
          (Xml.child xml_arg0 "billingMethod") in
      let auxiliaryApps =
        (Option.map ~f:AmazonResourceNames.of_xml)
          (Xml.child xml_arg0 "auxiliaryApps") in
      let radios =
        (Option.map ~f:Radios.of_xml) (Xml.child xml_arg0 "radios") in
      let customerArtifactPaths =
        (Option.map ~f:CustomerArtifactPaths.of_xml)
          (Xml.child xml_arg0 "customerArtifactPaths") in
      let vpceConfigurationArns =
        (Option.map ~f:AmazonResourceNames.of_xml)
          (Xml.child xml_arg0 "vpceConfigurationArns") in
      let location =
        (Option.map ~f:Location.of_xml) (Xml.child xml_arg0 "location") in
      let locale =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "locale") in
      let networkProfileArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "networkProfileArn") in
      let extraDataPackageArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "extraDataPackageArn") in
      make ?billingMethod ?auxiliaryApps ?radios ?customerArtifactPaths
        ?vpceConfigurationArns ?location ?locale ?networkProfileArn
        ?extraDataPackageArn ()
    let of_json json =
      let billingMethod =
        field_map json "billingMethod" BillingMethod.of_json in
      let auxiliaryApps =
        field_map json "auxiliaryApps" AmazonResourceNames.of_json in
      let radios = field_map json "radios" Radios.of_json in
      let customerArtifactPaths =
        field_map json "customerArtifactPaths" CustomerArtifactPaths.of_json in
      let vpceConfigurationArns =
        field_map json "vpceConfigurationArns" AmazonResourceNames.of_json in
      let location = field_map json "location" Location.of_json in
      let locale = field_map json "locale" String_.of_json in
      let networkProfileArn =
        field_map json "networkProfileArn" AmazonResourceName.of_json in
      let extraDataPackageArn =
        field_map json "extraDataPackageArn" AmazonResourceName.of_json in
      make ?billingMethod ?auxiliaryApps ?radios ?customerArtifactPaths
        ?vpceConfigurationArns ?location ?locale ?networkProfileArn
        ?extraDataPackageArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the settings for a run. Includes things like location, radio states, auxiliary apps, and network profiles."]
module ScheduleRunTest =
  struct
    type nonrec t =
      {
      type_: TestType.t
        [@ocaml.doc
          "The test's type. Must be one of the following values: BUILTIN_FUZZ BUILTIN_EXPLORER. For Android, an app explorer that traverses an Android app, interacting with it and capturing screenshots at the same time. APPIUM_JAVA_JUNIT APPIUM_JAVA_TESTNG APPIUM_PYTHON APPIUM_NODE APPIUM_RUBY APPIUM_WEB_JAVA_JUNIT APPIUM_WEB_JAVA_TESTNG APPIUM_WEB_PYTHON APPIUM_WEB_NODE APPIUM_WEB_RUBY CALABASH INSTRUMENTATION UIAUTOMATION UIAUTOMATOR XCTEST XCTEST_UI"];
      testPackageArn: AmazonResourceName.t option
        [@ocaml.doc "The ARN of the uploaded test to be run."];
      testSpecArn: AmazonResourceName.t option
        [@ocaml.doc "The ARN of the YAML-formatted test specification."];
      filter: Filter.t option [@ocaml.doc "The test's filter."];
      parameters: TestParameters.t option
        [@ocaml.doc
          "The test's parameters, such as test framework parameters and fixture settings. Parameters are represented by name-value pairs of strings. For all tests: app_performance_monitoring: Performance monitoring is enabled by default. Set this parameter to false to disable it. For Calabash tests: profile: A cucumber profile (for example, my_profile_name). tags: You can limit execution to features or scenarios that have (or don't have) certain tags (for example, @smoke or @smoke,~@wip). For Appium tests (all types): appium_version: The Appium version. Currently supported values are 1.6.5 (and later), latest, and default. latest runs the latest Appium version supported by Device Farm (1.9.1). For default, Device Farm selects a compatible version of Appium for the device. The current behavior is to run 1.7.2 on Android devices and iOS 9 and earlier and 1.7.2 for iOS 10 and later. This behavior is subject to change. For fuzz tests (Android only): event_count: The number of events, between 1 and 10000, that the UI fuzz test should perform. throttle: The time, in ms, between 0 and 1000, that the UI fuzz test should wait between events. seed: A seed to use for randomizing the UI fuzz test. Using the same seed value between tests ensures identical event sequences. For Explorer tests: username: A user name to use if the Explorer encounters a login form. If not supplied, no user name is inserted. password: A password to use if the Explorer encounters a login form. If not supplied, no password is inserted. For Instrumentation: filter: A test filter string. Examples: Running a single test case: com.android.abc.Test1 Running a single test: com.android.abc.Test1#smoke Running multiple tests: com.android.abc.Test1,com.android.abc.Test2 For XCTest and XCTestUI: filter: A test filter string. Examples: Running a single test class: LoginTests Running a multiple test classes: LoginTests,SmokeTests Running a single test: LoginTests/testValid Running multiple tests: LoginTests/testValid,LoginTests/testInvalid For UIAutomator: filter: A test filter string. Examples: Running a single test case: com.android.abc.Test1 Running a single test: com.android.abc.Test1#smoke Running multiple tests: com.android.abc.Test1,com.android.abc.Test2"]}
    let context_ = "ScheduleRunTest"
    let make ?testPackageArn =
      fun ?testSpecArn ->
        fun ?filter ->
          fun ?parameters ->
            fun ~type_ ->
              fun () ->
                { testPackageArn; testSpecArn; filter; parameters; type_ }
    let to_value x =
      structure_to_value
        [("type", (Some (TestType.to_value x.type_)));
        ("testPackageArn",
          (Option.map x.testPackageArn ~f:AmazonResourceName.to_value));
        ("testSpecArn",
          (Option.map x.testSpecArn ~f:AmazonResourceName.to_value));
        ("filter", (Option.map x.filter ~f:Filter.to_value));
        ("parameters", (Option.map x.parameters ~f:TestParameters.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let parameters =
        (Option.map ~f:TestParameters.of_xml)
          (Xml.child xml_arg0 "parameters") in
      let filter =
        (Option.map ~f:Filter.of_xml) (Xml.child xml_arg0 "filter") in
      let testSpecArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "testSpecArn") in
      let testPackageArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "testPackageArn") in
      let type_ =
        TestType.of_xml (Xml.child_exn ~context:context_ xml_arg0 "type") in
      make ?parameters ?filter ?testSpecArn ?testPackageArn ~type_ ()
    let of_json json =
      let parameters = field_map json "parameters" TestParameters.of_json in
      let filter = field_map json "filter" Filter.of_json in
      let testSpecArn =
        field_map json "testSpecArn" AmazonResourceName.of_json in
      let testPackageArn =
        field_map json "testPackageArn" AmazonResourceName.of_json in
      let type_ = field_map_exn json "type" TestType.of_json in
      make ?parameters ?filter ?testSpecArn ?testPackageArn ~type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents test settings. This data structure is passed in as the test parameter to ScheduleRun. For an example of the JSON request syntax, see ScheduleRun."]
module NotEligibleException =
  struct
    type nonrec t =
      {
      message: Message.t option
        [@ocaml.doc "The HTTP response code of a Not Eligible exception."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:Message.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" Message.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Exception gets thrown when a user is not eligible to perform the specified transaction."]
module PaginationToken =
  struct
    type nonrec t = string
    let context_ = "PaginationToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:4));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PaginationToken" j
    let to_json = simple_to_json to_value
  end
module VPCEConfigurations =
  struct
    type nonrec t = VPCEConfiguration.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:VPCEConfiguration.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:VPCEConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"VPCEConfigurations"
        ~of_json:VPCEConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module Uploads =
  struct
    type nonrec t = Upload.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Upload.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Upload.of_xml)
    let of_json j = list_of_json ~kind:"Uploads" ~of_json:Upload.of_json j
    let to_json v = composed_to_json to_value v
  end
module UniqueProblemsByExecutionResultMap =
  struct
    type nonrec t = (ExecutionResult.t * UniqueProblems.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types ExecutionResult UniqueProblems"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ExecutionResult.to_value x) |>
                    (fun x ->
                       (UniqueProblems.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ExecutionResult.of_string
        ~of_json:UniqueProblems.of_json j
    let to_json v = composed_to_json to_value v
  end
module Tests =
  struct
    type nonrec t = Test.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Test.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Test.of_xml)
    let of_json j = list_of_json ~kind:"Tests" ~of_json:Test.of_json j
    let to_json v = composed_to_json to_value v
  end
module TestGridSessions =
  struct
    type nonrec t = TestGridSession.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TestGridSession.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TestGridSession.of_xml)
    let of_json j =
      list_of_json ~kind:"TestGridSessions" ~of_json:TestGridSession.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module MaxPageSize =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1000) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxPageSize" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module TestGridSessionArtifacts =
  struct
    type nonrec t = TestGridSessionArtifact.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TestGridSessionArtifact.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TestGridSessionArtifact.of_xml)
    let of_json j =
      list_of_json ~kind:"TestGridSessionArtifacts"
        ~of_json:TestGridSessionArtifact.of_json j
    let to_json v = composed_to_json to_value v
  end
module TestGridSessionArtifactCategory =
  struct
    type nonrec t =
      | VIDEO 
      | LOG 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | VIDEO -> "VIDEO" | LOG -> "LOG" | Non_static_id s -> s
    let of_string =
      function | "VIDEO" -> VIDEO | "LOG" -> LOG | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration TestGridSessionArtifactCategory"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"TestGridSessionArtifactCategory" j)
    let to_json = simple_to_json to_value
  end
module TestGridSessionActions =
  struct
    type nonrec t = TestGridSessionAction.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TestGridSessionAction.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TestGridSessionAction.of_xml)
    let of_json j =
      list_of_json ~kind:"TestGridSessionActions"
        ~of_json:TestGridSessionAction.of_json j
    let to_json v = composed_to_json to_value v
  end
module TestGridProjects =
  struct
    type nonrec t = TestGridProject.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TestGridProject.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TestGridProject.of_xml)
    let of_json j =
      list_of_json ~kind:"TestGridProjects" ~of_json:TestGridProject.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module Suites =
  struct
    type nonrec t = Suite.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Suite.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Suite.of_xml)
    let of_json j = list_of_json ~kind:"Suites" ~of_json:Suite.of_json j
    let to_json v = composed_to_json to_value v
  end
module Samples =
  struct
    type nonrec t = Sample.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Sample.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Sample.of_xml)
    let of_json j = list_of_json ~kind:"Samples" ~of_json:Sample.of_json j
    let to_json v = composed_to_json to_value v
  end
module Runs =
  struct
    type nonrec t = Run.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Run.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Run.of_xml)
    let of_json j = list_of_json ~kind:"Runs" ~of_json:Run.of_json j
    let to_json v = composed_to_json to_value v
  end
module RemoteAccessSessions =
  struct
    type nonrec t = RemoteAccessSession.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:RemoteAccessSession.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RemoteAccessSession.of_xml)
    let of_json j =
      list_of_json ~kind:"RemoteAccessSessions"
        ~of_json:RemoteAccessSession.of_json j
    let to_json v = composed_to_json to_value v
  end
module Projects =
  struct
    type nonrec t = Project.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Project.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Project.of_xml)
    let of_json j = list_of_json ~kind:"Projects" ~of_json:Project.of_json j
    let to_json v = composed_to_json to_value v
  end
module Offerings =
  struct
    type nonrec t = Offering.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Offering.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Offering.of_xml)
    let of_json j =
      list_of_json ~kind:"Offerings" ~of_json:Offering.of_json j
    let to_json v = composed_to_json to_value v
  end
module OfferingTransactions =
  struct
    type nonrec t = OfferingTransaction.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:OfferingTransaction.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:OfferingTransaction.of_xml)
    let of_json j =
      list_of_json ~kind:"OfferingTransactions"
        ~of_json:OfferingTransaction.of_json j
    let to_json v = composed_to_json to_value v
  end
module OfferingPromotions =
  struct
    type nonrec t = OfferingPromotion.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:OfferingPromotion.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:OfferingPromotion.of_xml)
    let of_json j =
      list_of_json ~kind:"OfferingPromotions"
        ~of_json:OfferingPromotion.of_json j
    let to_json v = composed_to_json to_value v
  end
module NetworkProfiles =
  struct
    type nonrec t = NetworkProfile.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:NetworkProfile.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NetworkProfile.of_xml)
    let of_json j =
      list_of_json ~kind:"NetworkProfiles" ~of_json:NetworkProfile.of_json j
    let to_json v = composed_to_json to_value v
  end
module Jobs =
  struct
    type nonrec t = Job.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Job.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Job.of_xml)
    let of_json j = list_of_json ~kind:"Jobs" ~of_json:Job.of_json j
    let to_json v = composed_to_json to_value v
  end
module InstanceProfiles =
  struct
    type nonrec t = InstanceProfile.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:InstanceProfile.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InstanceProfile.of_xml)
    let of_json j =
      list_of_json ~kind:"InstanceProfiles" ~of_json:InstanceProfile.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module Devices =
  struct
    type nonrec t = Device.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Device.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Device.of_xml)
    let of_json j = list_of_json ~kind:"Devices" ~of_json:Device.of_json j
    let to_json v = composed_to_json to_value v
  end
module DevicePools =
  struct
    type nonrec t = DevicePool.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:DevicePool.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DevicePool.of_xml)
    let of_json j =
      list_of_json ~kind:"DevicePools" ~of_json:DevicePool.of_json j
    let to_json v = composed_to_json to_value v
  end
module Artifacts =
  struct
    type nonrec t = Artifact.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Artifact.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Artifact.of_xml)
    let of_json j =
      list_of_json ~kind:"Artifacts" ~of_json:Artifact.of_json j
    let to_json v = composed_to_json to_value v
  end
module ArtifactCategory =
  struct
    type nonrec t =
      | SCREENSHOT 
      | FILE 
      | LOG 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SCREENSHOT -> "SCREENSHOT"
      | FILE -> "FILE"
      | LOG -> "LOG"
      | Non_static_id s -> s
    let of_string =
      function
      | "SCREENSHOT" -> SCREENSHOT
      | "FILE" -> FILE
      | "LOG" -> LOG
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ArtifactCategory" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ArtifactCategory" j)
    let to_json = simple_to_json to_value
  end
module ResourceId =
  struct
    type nonrec t = string
    let context_ = "ResourceId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () -> check_pattern i ~pattern:".*\\S.*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceId" j
    let to_json = simple_to_json to_value
  end
module OfferingStatusMap =
  struct
    type nonrec t = (OfferingIdentifier.t * OfferingStatus.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types OfferingIdentifier OfferingStatus"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (OfferingIdentifier.to_value x) |>
                    (fun x ->
                       (OfferingStatus.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:OfferingIdentifier.of_string
        ~of_json:OfferingStatus.of_json j
    let to_json v = composed_to_json to_value v
  end
module DevicePoolCompatibilityResults =
  struct
    type nonrec t = DevicePoolCompatibilityResult.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:DevicePoolCompatibilityResult.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DevicePoolCompatibilityResult.of_xml)
    let of_json j =
      list_of_json ~kind:"DevicePoolCompatibilityResults"
        ~of_json:DevicePoolCompatibilityResult.of_json j
    let to_json v = composed_to_json to_value v
  end
module AccountSettings =
  struct
    type nonrec t =
      {
      awsAccountNumber: AWSAccountNumber.t option
        [@ocaml.doc
          "The AWS account number specified in the AccountSettings container."];
      unmeteredDevices: PurchasedDevicesMap.t option
        [@ocaml.doc
          "Returns the unmetered devices you have purchased or want to purchase."];
      unmeteredRemoteAccessDevices: PurchasedDevicesMap.t option
        [@ocaml.doc
          "Returns the unmetered remote access devices you have purchased or want to purchase."];
      maxJobTimeoutMinutes: JobTimeoutMinutes.t option
        [@ocaml.doc
          "The maximum number of minutes a test run executes before it times out."];
      trialMinutes: TrialMinutes.t option
        [@ocaml.doc
          "Information about an AWS account's usage of free trial device minutes."];
      maxSlots: MaxSlotMap.t option
        [@ocaml.doc
          "The maximum number of device slots that the AWS account can purchase. Each maximum is expressed as an offering-id:number pair, where the offering-id represents one of the IDs returned by the ListOfferings command."];
      defaultJobTimeoutMinutes: JobTimeoutMinutes.t option
        [@ocaml.doc
          "The default number of minutes (at the account level) a test run executes before it times out. The default value is 150 minutes."];
      skipAppResign: SkipAppResign.t option
        [@ocaml.doc
          "When set to true, for private devices, Device Farm does not sign your app again. For public devices, Device Farm always signs your apps again. For more information about how Device Farm re-signs your apps, see Do you modify my app? in the AWS Device Farm FAQs."]}
    let make ?awsAccountNumber =
      fun ?unmeteredDevices ->
        fun ?unmeteredRemoteAccessDevices ->
          fun ?maxJobTimeoutMinutes ->
            fun ?trialMinutes ->
              fun ?maxSlots ->
                fun ?defaultJobTimeoutMinutes ->
                  fun ?skipAppResign ->
                    fun () ->
                      {
                        awsAccountNumber;
                        unmeteredDevices;
                        unmeteredRemoteAccessDevices;
                        maxJobTimeoutMinutes;
                        trialMinutes;
                        maxSlots;
                        defaultJobTimeoutMinutes;
                        skipAppResign
                      }
    let to_value x =
      structure_to_value
        [("awsAccountNumber",
           (Option.map x.awsAccountNumber ~f:AWSAccountNumber.to_value));
        ("unmeteredDevices",
          (Option.map x.unmeteredDevices ~f:PurchasedDevicesMap.to_value));
        ("unmeteredRemoteAccessDevices",
          (Option.map x.unmeteredRemoteAccessDevices
             ~f:PurchasedDevicesMap.to_value));
        ("maxJobTimeoutMinutes",
          (Option.map x.maxJobTimeoutMinutes ~f:JobTimeoutMinutes.to_value));
        ("trialMinutes",
          (Option.map x.trialMinutes ~f:TrialMinutes.to_value));
        ("maxSlots", (Option.map x.maxSlots ~f:MaxSlotMap.to_value));
        ("defaultJobTimeoutMinutes",
          (Option.map x.defaultJobTimeoutMinutes
             ~f:JobTimeoutMinutes.to_value));
        ("skipAppResign",
          (Option.map x.skipAppResign ~f:SkipAppResign.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let skipAppResign =
        (Option.map ~f:SkipAppResign.of_xml)
          (Xml.child xml_arg0 "skipAppResign") in
      let defaultJobTimeoutMinutes =
        (Option.map ~f:JobTimeoutMinutes.of_xml)
          (Xml.child xml_arg0 "defaultJobTimeoutMinutes") in
      let maxSlots =
        (Option.map ~f:MaxSlotMap.of_xml) (Xml.child xml_arg0 "maxSlots") in
      let trialMinutes =
        (Option.map ~f:TrialMinutes.of_xml)
          (Xml.child xml_arg0 "trialMinutes") in
      let maxJobTimeoutMinutes =
        (Option.map ~f:JobTimeoutMinutes.of_xml)
          (Xml.child xml_arg0 "maxJobTimeoutMinutes") in
      let unmeteredRemoteAccessDevices =
        (Option.map ~f:PurchasedDevicesMap.of_xml)
          (Xml.child xml_arg0 "unmeteredRemoteAccessDevices") in
      let unmeteredDevices =
        (Option.map ~f:PurchasedDevicesMap.of_xml)
          (Xml.child xml_arg0 "unmeteredDevices") in
      let awsAccountNumber =
        (Option.map ~f:AWSAccountNumber.of_xml)
          (Xml.child xml_arg0 "awsAccountNumber") in
      make ?skipAppResign ?defaultJobTimeoutMinutes ?maxSlots ?trialMinutes
        ?maxJobTimeoutMinutes ?unmeteredRemoteAccessDevices ?unmeteredDevices
        ?awsAccountNumber ()
    let of_json json =
      let skipAppResign =
        field_map json "skipAppResign" SkipAppResign.of_json in
      let defaultJobTimeoutMinutes =
        field_map json "defaultJobTimeoutMinutes" JobTimeoutMinutes.of_json in
      let maxSlots = field_map json "maxSlots" MaxSlotMap.of_json in
      let trialMinutes = field_map json "trialMinutes" TrialMinutes.of_json in
      let maxJobTimeoutMinutes =
        field_map json "maxJobTimeoutMinutes" JobTimeoutMinutes.of_json in
      let unmeteredRemoteAccessDevices =
        field_map json "unmeteredRemoteAccessDevices"
          PurchasedDevicesMap.of_json in
      let unmeteredDevices =
        field_map json "unmeteredDevices" PurchasedDevicesMap.of_json in
      let awsAccountNumber =
        field_map json "awsAccountNumber" AWSAccountNumber.of_json in
      make ?skipAppResign ?defaultJobTimeoutMinutes ?maxSlots ?trialMinutes
        ?maxJobTimeoutMinutes ?unmeteredRemoteAccessDevices ?unmeteredDevices
        ?awsAccountNumber ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A container for account-level settings in AWS Device Farm."]
module CannotDeleteException =
  struct
    type nonrec t = {
      message: Message.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:Message.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" Message.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The requested object could not be deleted."]
module TestGridUrlExpiresInSecondsInput =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:86400) >>=
             (fun () -> check_int_min i ~min:60));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for TestGridUrlExpiresInSecondsInput" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module CreateRemoteAccessSessionConfiguration =
  struct
    type nonrec t =
      {
      billingMethod: BillingMethod.t option
        [@ocaml.doc "The billing method for the remote access session."];
      vpceConfigurationArns: AmazonResourceNames.t option
        [@ocaml.doc
          "An array of ARNs included in the VPC endpoint configuration."]}
    let make ?billingMethod =
      fun ?vpceConfigurationArns ->
        fun () -> { billingMethod; vpceConfigurationArns }
    let to_value x =
      structure_to_value
        [("billingMethod",
           (Option.map x.billingMethod ~f:BillingMethod.to_value));
        ("vpceConfigurationArns",
          (Option.map x.vpceConfigurationArns ~f:AmazonResourceNames.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpceConfigurationArns =
        (Option.map ~f:AmazonResourceNames.of_xml)
          (Xml.child xml_arg0 "vpceConfigurationArns") in
      let billingMethod =
        (Option.map ~f:BillingMethod.of_xml)
          (Xml.child xml_arg0 "billingMethod") in
      make ?vpceConfigurationArns ?billingMethod ()
    let of_json json =
      let vpceConfigurationArns =
        field_map json "vpceConfigurationArns" AmazonResourceNames.of_json in
      let billingMethod =
        field_map json "billingMethod" BillingMethod.of_json in
      make ?vpceConfigurationArns ?billingMethod ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration settings for a remote access session, including billing method."]
module SshPublicKey =
  struct
    type nonrec t = string
    let context_ = "SshPublicKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:8192) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SshPublicKey" j
    let to_json = simple_to_json to_value
  end
module UpdateVPCEConfigurationResult =
  struct
    type nonrec t =
      {
      vpceConfiguration: VPCEConfiguration.t option
        [@ocaml.doc
          "An object that contains information about your VPC endpoint configuration."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `InvalidOperationException of InvalidOperationException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?vpceConfiguration = fun () -> { vpceConfiguration }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `InvalidOperationException e ->
          `Assoc
            [("error", (`String "InvalidOperationException"));
            ("details", (InvalidOperationException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpceConfiguration",
           (Option.map x.vpceConfiguration ~f:VPCEConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpceConfiguration =
        (Option.map ~f:VPCEConfiguration.of_xml)
          (Xml.child xml_arg0 "vpceConfiguration") in
      make ?vpceConfiguration ()
    let of_json json =
      let vpceConfiguration =
        field_map json "vpceConfiguration" VPCEConfiguration.of_json in
      make ?vpceConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates information about an Amazon Virtual Private Cloud (VPC) endpoint configuration."]
module UpdateVPCEConfigurationRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the VPC endpoint configuration you want to update."];
      vpceConfigurationName: VPCEConfigurationName.t option
        [@ocaml.doc
          "The friendly name you give to your VPC endpoint configuration to manage your configurations more easily."];
      vpceServiceName: VPCEServiceName.t option
        [@ocaml.doc
          "The name of the VPC endpoint service running in your AWS account that you want Device Farm to test."];
      serviceDnsName: ServiceDnsName.t option
        [@ocaml.doc
          "The DNS (domain) name used to connect to your private service in your VPC. The DNS name must not already be in use on the internet."];
      vpceConfigurationDescription: VPCEConfigurationDescription.t option
        [@ocaml.doc
          "An optional description that provides details about your VPC endpoint configuration."]}
    let context_ = "UpdateVPCEConfigurationRequest"
    let make ?vpceConfigurationName =
      fun ?vpceServiceName ->
        fun ?serviceDnsName ->
          fun ?vpceConfigurationDescription ->
            fun ~arn ->
              fun () ->
                {
                  vpceConfigurationName;
                  vpceServiceName;
                  serviceDnsName;
                  vpceConfigurationDescription;
                  arn
                }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)));
        ("vpceConfigurationName",
          (Option.map x.vpceConfigurationName
             ~f:VPCEConfigurationName.to_value));
        ("vpceServiceName",
          (Option.map x.vpceServiceName ~f:VPCEServiceName.to_value));
        ("serviceDnsName",
          (Option.map x.serviceDnsName ~f:ServiceDnsName.to_value));
        ("vpceConfigurationDescription",
          (Option.map x.vpceConfigurationDescription
             ~f:VPCEConfigurationDescription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpceConfigurationDescription =
        (Option.map ~f:VPCEConfigurationDescription.of_xml)
          (Xml.child xml_arg0 "vpceConfigurationDescription") in
      let serviceDnsName =
        (Option.map ~f:ServiceDnsName.of_xml)
          (Xml.child xml_arg0 "serviceDnsName") in
      let vpceServiceName =
        (Option.map ~f:VPCEServiceName.of_xml)
          (Xml.child xml_arg0 "vpceServiceName") in
      let vpceConfigurationName =
        (Option.map ~f:VPCEConfigurationName.of_xml)
          (Xml.child xml_arg0 "vpceConfigurationName") in
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ?vpceConfigurationDescription ?serviceDnsName ?vpceServiceName
        ?vpceConfigurationName ~arn ()
    let of_json json =
      let vpceConfigurationDescription =
        field_map json "vpceConfigurationDescription"
          VPCEConfigurationDescription.of_json in
      let serviceDnsName =
        field_map json "serviceDnsName" ServiceDnsName.of_json in
      let vpceServiceName =
        field_map json "vpceServiceName" VPCEServiceName.of_json in
      let vpceConfigurationName =
        field_map json "vpceConfigurationName" VPCEConfigurationName.of_json in
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ?vpceConfigurationDescription ?serviceDnsName ?vpceServiceName
        ?vpceConfigurationName ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates information about an Amazon Virtual Private Cloud (VPC) endpoint configuration."]
module UpdateUploadResult =
  struct
    type nonrec t =
      {
      upload: Upload.t option
        [@ocaml.doc "A test spec uploaded to Device Farm."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?upload = fun () -> { upload }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("upload", (Option.map x.upload ~f:Upload.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let upload =
        (Option.map ~f:Upload.of_xml) (Xml.child xml_arg0 "upload") in
      make ?upload ()
    let of_json json =
      let upload = field_map json "upload" Upload.of_json in make ?upload ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates an uploaded test spec."]
module UpdateUploadRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the uploaded test spec."];
      name: Name.t option
        [@ocaml.doc
          "The upload's test spec file name. The name must not contain any forward slashes (/). The test spec file name must end with the .yaml or .yml file extension."];
      contentType: ContentType.t option
        [@ocaml.doc
          "The upload's content type (for example, application/x-yaml)."];
      editContent: Boolean.t option
        [@ocaml.doc
          "Set to true if the YAML file has changed and must be updated. Otherwise, set to false."]}
    let context_ = "UpdateUploadRequest"
    let make ?name =
      fun ?contentType ->
        fun ?editContent ->
          fun ~arn -> fun () -> { name; contentType; editContent; arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)));
        ("name", (Option.map x.name ~f:Name.to_value));
        ("contentType", (Option.map x.contentType ~f:ContentType.to_value));
        ("editContent", (Option.map x.editContent ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let editContent =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "editContent") in
      let contentType =
        (Option.map ~f:ContentType.of_xml) (Xml.child xml_arg0 "contentType") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ?editContent ?contentType ?name ~arn ()
    let of_json json =
      let editContent = field_map json "editContent" Boolean.of_json in
      let contentType = field_map json "contentType" ContentType.of_json in
      let name = field_map json "name" Name.of_json in
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ?editContent ?contentType ?name ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates an uploaded test spec."]
module UpdateTestGridProjectResult =
  struct
    type nonrec t =
      {
      testGridProject: TestGridProject.t option
        [@ocaml.doc "The project, including updated information."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?testGridProject = fun () -> { testGridProject }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("testGridProject",
           (Option.map x.testGridProject ~f:TestGridProject.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let testGridProject =
        (Option.map ~f:TestGridProject.of_xml)
          (Xml.child xml_arg0 "testGridProject") in
      make ?testGridProject ()
    let of_json json =
      let testGridProject =
        field_map json "testGridProject" TestGridProject.of_json in
      make ?testGridProject ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Change details of a project."]
module UpdateTestGridProjectRequest =
  struct
    type nonrec t =
      {
      projectArn: DeviceFarmArn.t
        [@ocaml.doc "ARN of the project to update."];
      name: ResourceName.t option
        [@ocaml.doc "Human-readable name for the project."];
      description: ResourceDescription.t option
        [@ocaml.doc "Human-readable description for the project."];
      vpcConfig: TestGridVpcConfig.t option
        [@ocaml.doc
          "The VPC security groups and subnets that are attached to a project."]}
    let context_ = "UpdateTestGridProjectRequest"
    let make ?name =
      fun ?description ->
        fun ?vpcConfig ->
          fun ~projectArn ->
            fun () -> { name; description; vpcConfig; projectArn }
    let to_value x =
      structure_to_value
        [("projectArn", (Some (DeviceFarmArn.to_value x.projectArn)));
        ("name", (Option.map x.name ~f:ResourceName.to_value));
        ("description",
          (Option.map x.description ~f:ResourceDescription.to_value));
        ("vpcConfig", (Option.map x.vpcConfig ~f:TestGridVpcConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcConfig =
        (Option.map ~f:TestGridVpcConfig.of_xml)
          (Xml.child xml_arg0 "vpcConfig") in
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        (Option.map ~f:ResourceName.of_xml) (Xml.child xml_arg0 "name") in
      let projectArn =
        DeviceFarmArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "projectArn") in
      make ?vpcConfig ?description ?name ~projectArn ()
    let of_json json =
      let vpcConfig = field_map json "vpcConfig" TestGridVpcConfig.of_json in
      let description =
        field_map json "description" ResourceDescription.of_json in
      let name = field_map json "name" ResourceName.of_json in
      let projectArn = field_map_exn json "projectArn" DeviceFarmArn.of_json in
      make ?vpcConfig ?description ?name ~projectArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Change details of a project."]
module UpdateProjectResult =
  struct
    type nonrec t =
      {
      project: Project.t option [@ocaml.doc "The project to update."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?project = fun () -> { project }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("project", (Option.map x.project ~f:Project.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let project =
        (Option.map ~f:Project.of_xml) (Xml.child xml_arg0 "project") in
      make ?project ()
    let of_json json =
      let project = field_map json "project" Project.of_json in
      make ?project ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of an update project request."]
module UpdateProjectRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the project whose name to update."];
      name: Name.t option
        [@ocaml.doc
          "A string that represents the new name of the project that you are updating."];
      defaultJobTimeoutMinutes: JobTimeoutMinutes.t option
        [@ocaml.doc
          "The number of minutes a test run in the project executes before it times out."]}
    let context_ = "UpdateProjectRequest"
    let make ?name =
      fun ?defaultJobTimeoutMinutes ->
        fun ~arn -> fun () -> { name; defaultJobTimeoutMinutes; arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)));
        ("name", (Option.map x.name ~f:Name.to_value));
        ("defaultJobTimeoutMinutes",
          (Option.map x.defaultJobTimeoutMinutes
             ~f:JobTimeoutMinutes.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let defaultJobTimeoutMinutes =
        (Option.map ~f:JobTimeoutMinutes.of_xml)
          (Xml.child xml_arg0 "defaultJobTimeoutMinutes") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ?defaultJobTimeoutMinutes ?name ~arn ()
    let of_json json =
      let defaultJobTimeoutMinutes =
        field_map json "defaultJobTimeoutMinutes" JobTimeoutMinutes.of_json in
      let name = field_map json "name" Name.of_json in
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ?defaultJobTimeoutMinutes ?name ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a request to the update project operation."]
module UpdateNetworkProfileResult =
  struct
    type nonrec t =
      {
      networkProfile: NetworkProfile.t option
        [@ocaml.doc "A list of the available network profiles."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?networkProfile = fun () -> { networkProfile }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkProfile",
           (Option.map x.networkProfile ~f:NetworkProfile.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkProfile =
        (Option.map ~f:NetworkProfile.of_xml)
          (Xml.child xml_arg0 "networkProfile") in
      make ?networkProfile ()
    let of_json json =
      let networkProfile =
        field_map json "networkProfile" NetworkProfile.of_json in
      make ?networkProfile ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates the network profile."]
module UpdateNetworkProfileRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the project for which you want to update network profile settings."];
      name: Name.t option
        [@ocaml.doc
          "The name of the network profile about which you are returning information."];
      description: Message.t option
        [@ocaml.doc
          "The description of the network profile about which you are returning information."];
      type_: NetworkProfileType.t option
        [@ocaml.doc
          "The type of network profile to return information about. Valid values are listed here."];
      uplinkBandwidthBits: Long.t option
        [@ocaml.doc
          "The data throughput rate in bits per second, as an integer from 0 to 104857600."];
      downlinkBandwidthBits: Long.t option
        [@ocaml.doc
          "The data throughput rate in bits per second, as an integer from 0 to 104857600."];
      uplinkDelayMs: Long.t option
        [@ocaml.doc
          "Delay time for all packets to destination in milliseconds as an integer from 0 to 2000."];
      downlinkDelayMs: Long.t option
        [@ocaml.doc
          "Delay time for all packets to destination in milliseconds as an integer from 0 to 2000."];
      uplinkJitterMs: Long.t option
        [@ocaml.doc
          "Time variation in the delay of received packets in milliseconds as an integer from 0 to 2000."];
      downlinkJitterMs: Long.t option
        [@ocaml.doc
          "Time variation in the delay of received packets in milliseconds as an integer from 0 to 2000."];
      uplinkLossPercent: PercentInteger.t option
        [@ocaml.doc
          "Proportion of transmitted packets that fail to arrive from 0 to 100 percent."];
      downlinkLossPercent: PercentInteger.t option
        [@ocaml.doc
          "Proportion of received packets that fail to arrive from 0 to 100 percent."]}
    let context_ = "UpdateNetworkProfileRequest"
    let make ?name =
      fun ?description ->
        fun ?type_ ->
          fun ?uplinkBandwidthBits ->
            fun ?downlinkBandwidthBits ->
              fun ?uplinkDelayMs ->
                fun ?downlinkDelayMs ->
                  fun ?uplinkJitterMs ->
                    fun ?downlinkJitterMs ->
                      fun ?uplinkLossPercent ->
                        fun ?downlinkLossPercent ->
                          fun ~arn ->
                            fun () ->
                              {
                                name;
                                description;
                                type_;
                                uplinkBandwidthBits;
                                downlinkBandwidthBits;
                                uplinkDelayMs;
                                downlinkDelayMs;
                                uplinkJitterMs;
                                downlinkJitterMs;
                                uplinkLossPercent;
                                downlinkLossPercent;
                                arn
                              }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)));
        ("name", (Option.map x.name ~f:Name.to_value));
        ("description", (Option.map x.description ~f:Message.to_value));
        ("type", (Option.map x.type_ ~f:NetworkProfileType.to_value));
        ("uplinkBandwidthBits",
          (Option.map x.uplinkBandwidthBits ~f:Long.to_value));
        ("downlinkBandwidthBits",
          (Option.map x.downlinkBandwidthBits ~f:Long.to_value));
        ("uplinkDelayMs", (Option.map x.uplinkDelayMs ~f:Long.to_value));
        ("downlinkDelayMs", (Option.map x.downlinkDelayMs ~f:Long.to_value));
        ("uplinkJitterMs", (Option.map x.uplinkJitterMs ~f:Long.to_value));
        ("downlinkJitterMs",
          (Option.map x.downlinkJitterMs ~f:Long.to_value));
        ("uplinkLossPercent",
          (Option.map x.uplinkLossPercent ~f:PercentInteger.to_value));
        ("downlinkLossPercent",
          (Option.map x.downlinkLossPercent ~f:PercentInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let downlinkLossPercent =
        (Option.map ~f:PercentInteger.of_xml)
          (Xml.child xml_arg0 "downlinkLossPercent") in
      let uplinkLossPercent =
        (Option.map ~f:PercentInteger.of_xml)
          (Xml.child xml_arg0 "uplinkLossPercent") in
      let downlinkJitterMs =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "downlinkJitterMs") in
      let uplinkJitterMs =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "uplinkJitterMs") in
      let downlinkDelayMs =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "downlinkDelayMs") in
      let uplinkDelayMs =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "uplinkDelayMs") in
      let downlinkBandwidthBits =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "downlinkBandwidthBits") in
      let uplinkBandwidthBits =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "uplinkBandwidthBits") in
      let type_ =
        (Option.map ~f:NetworkProfileType.of_xml) (Xml.child xml_arg0 "type") in
      let description =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "description") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ?downlinkLossPercent ?uplinkLossPercent ?downlinkJitterMs
        ?uplinkJitterMs ?downlinkDelayMs ?uplinkDelayMs
        ?downlinkBandwidthBits ?uplinkBandwidthBits ?type_ ?description ?name
        ~arn ()
    let of_json json =
      let downlinkLossPercent =
        field_map json "downlinkLossPercent" PercentInteger.of_json in
      let uplinkLossPercent =
        field_map json "uplinkLossPercent" PercentInteger.of_json in
      let downlinkJitterMs = field_map json "downlinkJitterMs" Long.of_json in
      let uplinkJitterMs = field_map json "uplinkJitterMs" Long.of_json in
      let downlinkDelayMs = field_map json "downlinkDelayMs" Long.of_json in
      let uplinkDelayMs = field_map json "uplinkDelayMs" Long.of_json in
      let downlinkBandwidthBits =
        field_map json "downlinkBandwidthBits" Long.of_json in
      let uplinkBandwidthBits =
        field_map json "uplinkBandwidthBits" Long.of_json in
      let type_ = field_map json "type" NetworkProfileType.of_json in
      let description = field_map json "description" Message.of_json in
      let name = field_map json "name" Name.of_json in
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ?downlinkLossPercent ?uplinkLossPercent ?downlinkJitterMs
        ?uplinkJitterMs ?downlinkDelayMs ?uplinkDelayMs
        ?downlinkBandwidthBits ?uplinkBandwidthBits ?type_ ?description ?name
        ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates the network profile."]
module UpdateInstanceProfileResult =
  struct
    type nonrec t =
      {
      instanceProfile: InstanceProfile.t option
        [@ocaml.doc
          "An object that contains information about your instance profile."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?instanceProfile = fun () -> { instanceProfile }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceProfile",
           (Option.map x.instanceProfile ~f:InstanceProfile.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceProfile =
        (Option.map ~f:InstanceProfile.of_xml)
          (Xml.child xml_arg0 "instanceProfile") in
      make ?instanceProfile ()
    let of_json json =
      let instanceProfile =
        field_map json "instanceProfile" InstanceProfile.of_json in
      make ?instanceProfile ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates information about an existing private device instance profile."]
module UpdateInstanceProfileRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the instance profile."];
      name: Name.t option
        [@ocaml.doc "The updated name for your instance profile."];
      description: Message.t option
        [@ocaml.doc "The updated description for your instance profile."];
      packageCleanup: Boolean.t option
        [@ocaml.doc
          "The updated choice for whether you want to specify package cleanup. The default value is false for private devices."];
      excludeAppPackagesFromCleanup: PackageIds.t option
        [@ocaml.doc
          "An array of strings that specifies the list of app packages that should not be cleaned up from the device after a test run is over. The list of packages is only considered if you set packageCleanup to true."];
      rebootAfterUse: Boolean.t option
        [@ocaml.doc
          "The updated choice for whether you want to reboot the device after use. The default value is true."]}
    let context_ = "UpdateInstanceProfileRequest"
    let make ?name =
      fun ?description ->
        fun ?packageCleanup ->
          fun ?excludeAppPackagesFromCleanup ->
            fun ?rebootAfterUse ->
              fun ~arn ->
                fun () ->
                  {
                    name;
                    description;
                    packageCleanup;
                    excludeAppPackagesFromCleanup;
                    rebootAfterUse;
                    arn
                  }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)));
        ("name", (Option.map x.name ~f:Name.to_value));
        ("description", (Option.map x.description ~f:Message.to_value));
        ("packageCleanup", (Option.map x.packageCleanup ~f:Boolean.to_value));
        ("excludeAppPackagesFromCleanup",
          (Option.map x.excludeAppPackagesFromCleanup ~f:PackageIds.to_value));
        ("rebootAfterUse", (Option.map x.rebootAfterUse ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let rebootAfterUse =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "rebootAfterUse") in
      let excludeAppPackagesFromCleanup =
        (Option.map ~f:PackageIds.of_xml)
          (Xml.child xml_arg0 "excludeAppPackagesFromCleanup") in
      let packageCleanup =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "packageCleanup") in
      let description =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "description") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ?rebootAfterUse ?excludeAppPackagesFromCleanup ?packageCleanup
        ?description ?name ~arn ()
    let of_json json =
      let rebootAfterUse = field_map json "rebootAfterUse" Boolean.of_json in
      let excludeAppPackagesFromCleanup =
        field_map json "excludeAppPackagesFromCleanup" PackageIds.of_json in
      let packageCleanup = field_map json "packageCleanup" Boolean.of_json in
      let description = field_map json "description" Message.of_json in
      let name = field_map json "name" Name.of_json in
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ?rebootAfterUse ?excludeAppPackagesFromCleanup ?packageCleanup
        ?description ?name ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates information about an existing private device instance profile."]
module UpdateDevicePoolResult =
  struct
    type nonrec t =
      {
      devicePool: DevicePool.t option
        [@ocaml.doc "The device pool you just updated."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?devicePool = fun () -> { devicePool }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("devicePool", (Option.map x.devicePool ~f:DevicePool.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let devicePool =
        (Option.map ~f:DevicePool.of_xml) (Xml.child xml_arg0 "devicePool") in
      make ?devicePool ()
    let of_json json =
      let devicePool = field_map json "devicePool" DevicePool.of_json in
      make ?devicePool ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of an update device pool request."]
module UpdateDevicePoolRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Device Farm device pool to update."];
      name: Name.t option
        [@ocaml.doc
          "A string that represents the name of the device pool to update."];
      description: Message.t option
        [@ocaml.doc "A description of the device pool to update."];
      rules: Rules.t option
        [@ocaml.doc
          "Represents the rules to modify for the device pool. Updating rules is optional. If you update rules for your request, the update replaces the existing rules."];
      maxDevices: Integer.t option
        [@ocaml.doc
          "The number of devices that Device Farm can add to your device pool. Device Farm adds devices that are available and that meet the criteria that you assign for the rules parameter. Depending on how many devices meet these constraints, your device pool might contain fewer devices than the value for this parameter. By specifying the maximum number of devices, you can control the costs that you incur by running tests. If you use this parameter in your request, you cannot use the clearMaxDevices parameter in the same request."];
      clearMaxDevices: Boolean.t option
        [@ocaml.doc
          "Sets whether the maxDevices parameter applies to your device pool. If you set this parameter to true, the maxDevices parameter does not apply, and Device Farm does not limit the number of devices that it adds to your device pool. In this case, Device Farm adds all available devices that meet the criteria specified in the rules parameter. If you use this parameter in your request, you cannot use the maxDevices parameter in the same request."]}
    let context_ = "UpdateDevicePoolRequest"
    let make ?name =
      fun ?description ->
        fun ?rules ->
          fun ?maxDevices ->
            fun ?clearMaxDevices ->
              fun ~arn ->
                fun () ->
                  {
                    name;
                    description;
                    rules;
                    maxDevices;
                    clearMaxDevices;
                    arn
                  }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)));
        ("name", (Option.map x.name ~f:Name.to_value));
        ("description", (Option.map x.description ~f:Message.to_value));
        ("rules", (Option.map x.rules ~f:Rules.to_value));
        ("maxDevices", (Option.map x.maxDevices ~f:Integer.to_value));
        ("clearMaxDevices",
          (Option.map x.clearMaxDevices ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clearMaxDevices =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "clearMaxDevices") in
      let maxDevices =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxDevices") in
      let rules = (Option.map ~f:Rules.of_xml) (Xml.child xml_arg0 "rules") in
      let description =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "description") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ?clearMaxDevices ?maxDevices ?rules ?description ?name ~arn ()
    let of_json json =
      let clearMaxDevices = field_map json "clearMaxDevices" Boolean.of_json in
      let maxDevices = field_map json "maxDevices" Integer.of_json in
      let rules = field_map json "rules" Rules.of_json in
      let description = field_map json "description" Message.of_json in
      let name = field_map json "name" Name.of_json in
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ?clearMaxDevices ?maxDevices ?rules ?description ?name ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to the update device pool operation."]
module UpdateDeviceInstanceResult =
  struct
    type nonrec t =
      {
      deviceInstance: DeviceInstance.t option
        [@ocaml.doc
          "An object that contains information about your device instance."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?deviceInstance = fun () -> { deviceInstance }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("deviceInstance",
           (Option.map x.deviceInstance ~f:DeviceInstance.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deviceInstance =
        (Option.map ~f:DeviceInstance.of_xml)
          (Xml.child xml_arg0 "deviceInstance") in
      make ?deviceInstance ()
    let of_json json =
      let deviceInstance =
        field_map json "deviceInstance" DeviceInstance.of_json in
      make ?deviceInstance ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates information about a private device instance."]
module UpdateDeviceInstanceRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the device instance."];
      profileArn: AmazonResourceName.t option
        [@ocaml.doc
          "The ARN of the profile that you want to associate with the device instance."];
      labels: InstanceLabels.t option
        [@ocaml.doc
          "An array of strings that you want to associate with the device instance."]}
    let context_ = "UpdateDeviceInstanceRequest"
    let make ?profileArn =
      fun ?labels -> fun ~arn -> fun () -> { profileArn; labels; arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)));
        ("profileArn",
          (Option.map x.profileArn ~f:AmazonResourceName.to_value));
        ("labels", (Option.map x.labels ~f:InstanceLabels.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let labels =
        (Option.map ~f:InstanceLabels.of_xml) (Xml.child xml_arg0 "labels") in
      let profileArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "profileArn") in
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ?labels ?profileArn ~arn ()
    let of_json json =
      let labels = field_map json "labels" InstanceLabels.of_json in
      let profileArn = field_map json "profileArn" AmazonResourceName.of_json in
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ?labels ?profileArn ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates information about a private device instance."]
module UntagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `NotFoundException of NotFoundException.t 
      | `TagOperationException of TagOperationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TagOperationException" ->
          `TagOperationException (TagOperationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TagOperationException" ->
          `TagOperationException (TagOperationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TagOperationException e ->
          `Assoc
            [("error", (`String "TagOperationException"));
            ("details", (TagOperationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified tags from a resource."]
module UntagResourceRequest =
  struct
    type nonrec t =
      {
      resourceARN: DeviceFarmArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the resource or resources from which to delete tags. You can associate tags with the following Device Farm resources: PROJECT, RUN, NETWORK_PROFILE, INSTANCE_PROFILE, DEVICE_INSTANCE, SESSION, DEVICE_POOL, DEVICE, and VPCE_CONFIGURATION."];
      tagKeys: TagKeyList.t
        [@ocaml.doc "The keys of the tags to be removed."]}
    let context_ = "UntagResourceRequest"
    let make ~resourceARN =
      fun ~tagKeys -> fun () -> { resourceARN; tagKeys }
    let to_value x =
      structure_to_value
        [("ResourceARN", (Some (DeviceFarmArn.to_value x.resourceARN)));
        ("TagKeys", (Some (TagKeyList.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TagKeys") in
      let resourceARN =
        DeviceFarmArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceARN") in
      make ~tagKeys ~resourceARN ()
    let of_json json =
      let tagKeys = field_map_exn json "TagKeys" TagKeyList.of_json in
      let resourceARN =
        field_map_exn json "ResourceARN" DeviceFarmArn.of_json in
      make ~tagKeys ~resourceARN ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified tags from a resource."]
module TagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `NotFoundException of NotFoundException.t 
      | `TagOperationException of TagOperationException.t 
      | `TagPolicyException of TagPolicyException.t 
      | `TooManyTagsException of TooManyTagsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TagOperationException" ->
          `TagOperationException (TagOperationException.of_json json)
      | "TagPolicyException" ->
          `TagPolicyException (TagPolicyException.of_json json)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TagOperationException" ->
          `TagOperationException (TagOperationException.of_xml xml)
      | "TagPolicyException" ->
          `TagPolicyException (TagPolicyException.of_xml xml)
      | "TooManyTagsException" ->
          `TooManyTagsException (TooManyTagsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TagOperationException e ->
          `Assoc
            [("error", (`String "TagOperationException"));
            ("details", (TagOperationException.to_json e))]
      | `TagPolicyException e ->
          `Assoc
            [("error", (`String "TagPolicyException"));
            ("details", (TagPolicyException.to_json e))]
      | `TooManyTagsException e ->
          `Assoc
            [("error", (`String "TooManyTagsException"));
            ("details", (TooManyTagsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates the specified tags to a resource with the specified resourceArn. If existing tags on a resource are not specified in the request parameters, they are not changed. When a resource is deleted, the tags associated with that resource are also deleted."]
module TagResourceRequest =
  struct
    type nonrec t =
      {
      resourceARN: DeviceFarmArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the resource or resources to which to add tags. You can associate tags with the following Device Farm resources: PROJECT, RUN, NETWORK_PROFILE, INSTANCE_PROFILE, DEVICE_INSTANCE, SESSION, DEVICE_POOL, DEVICE, and VPCE_CONFIGURATION."];
      tags: TagList.t
        [@ocaml.doc
          "The tags to add to the resource. A tag is an array of key-value pairs. Tag keys can have a maximum character length of 128 characters. Tag values can have a maximum length of 256 characters."]}
    let context_ = "TagResourceRequest"
    let make ~resourceARN = fun ~tags -> fun () -> { resourceARN; tags }
    let to_value x =
      structure_to_value
        [("ResourceARN", (Some (DeviceFarmArn.to_value x.resourceARN)));
        ("Tags", (Some (TagList.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagList.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Tags") in
      let resourceARN =
        DeviceFarmArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceARN") in
      make ~tags ~resourceARN ()
    let of_json json =
      let tags = field_map_exn json "Tags" TagList.of_json in
      let resourceARN =
        field_map_exn json "ResourceARN" DeviceFarmArn.of_json in
      make ~tags ~resourceARN ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates the specified tags to a resource with the specified resourceArn. If existing tags on a resource are not specified in the request parameters, they are not changed. When a resource is deleted, the tags associated with that resource are also deleted."]
module StopRunResult =
  struct
    type nonrec t =
      {
      run: Run.t option [@ocaml.doc "The run that was stopped."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?run = fun () -> { run }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("run", (Option.map x.run ~f:Run.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let run = (Option.map ~f:Run.of_xml) (Xml.child xml_arg0 "run") in
      make ?run ()
    let of_json json =
      let run = field_map json "run" Run.of_json in make ?run ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the results of your stop run attempt."]
module StopRunRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc
          "Represents the Amazon Resource Name (ARN) of the Device Farm run to stop."]}
    let context_ = "StopRunRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_json json =
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the request to stop a specific run."]
module StopRemoteAccessSessionResult =
  struct
    type nonrec t =
      {
      remoteAccessSession: RemoteAccessSession.t option
        [@ocaml.doc
          "A container that represents the metadata from the service about the remote access session you are stopping."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?remoteAccessSession = fun () -> { remoteAccessSession }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("remoteAccessSession",
           (Option.map x.remoteAccessSession ~f:RemoteAccessSession.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let remoteAccessSession =
        (Option.map ~f:RemoteAccessSession.of_xml)
          (Xml.child xml_arg0 "remoteAccessSession") in
      make ?remoteAccessSession ()
    let of_json json =
      let remoteAccessSession =
        field_map json "remoteAccessSession" RemoteAccessSession.of_json in
      make ?remoteAccessSession ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the response from the server that describes the remote access session when AWS Device Farm stops the session."]
module StopRemoteAccessSessionRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the remote access session to stop."]}
    let context_ = "StopRemoteAccessSessionRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_json json =
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the request to stop the remote access session."]
module StopJobResult =
  struct
    type nonrec t =
      {
      job: Job.t option [@ocaml.doc "The job that was stopped."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?job = fun () -> { job }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("job", (Option.map x.job ~f:Job.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let job = (Option.map ~f:Job.of_xml) (Xml.child xml_arg0 "job") in
      make ?job ()
    let of_json json =
      let job = field_map json "job" Job.of_json in make ?job ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Initiates a stop request for the current job. AWS Device Farm immediately stops the job on the device where tests have not started. You are not billed for this device. On the device where tests have started, setup suite and teardown suite tests run to completion on the device. You are billed for setup, teardown, and any tests that were in progress or already completed."]
module StopJobRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc
          "Represents the Amazon Resource Name (ARN) of the Device Farm job to stop."]}
    let context_ = "StopJobRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_json json =
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Initiates a stop request for the current job. AWS Device Farm immediately stops the job on the device where tests have not started. You are not billed for this device. On the device where tests have started, setup suite and teardown suite tests run to completion on the device. You are billed for setup, teardown, and any tests that were in progress or already completed."]
module ScheduleRunResult =
  struct
    type nonrec t =
      {
      run: Run.t option [@ocaml.doc "Information about the scheduled run."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `IdempotencyException of IdempotencyException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?run = fun () -> { run }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "IdempotencyException" ->
          `IdempotencyException (IdempotencyException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "IdempotencyException" ->
          `IdempotencyException (IdempotencyException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `IdempotencyException e ->
          `Assoc
            [("error", (`String "IdempotencyException"));
            ("details", (IdempotencyException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("run", (Option.map x.run ~f:Run.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let run = (Option.map ~f:Run.of_xml) (Xml.child xml_arg0 "run") in
      make ?run ()
    let of_json json =
      let run = field_map json "run" Run.of_json in make ?run ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a schedule run request."]
module ScheduleRunRequest =
  struct
    type nonrec t =
      {
      projectArn: AmazonResourceName.t
        [@ocaml.doc "The ARN of the project for the run to be scheduled."];
      appArn: AmazonResourceName.t option
        [@ocaml.doc
          "The ARN of an application package to run tests against, created with CreateUpload. See ListUploads."];
      devicePoolArn: AmazonResourceName.t option
        [@ocaml.doc
          "The ARN of the device pool for the run to be scheduled."];
      deviceSelectionConfiguration: DeviceSelectionConfiguration.t option
        [@ocaml.doc
          "The filter criteria used to dynamically select a set of devices for a test run and the maximum number of devices to be included in the run. Either devicePoolArn or deviceSelectionConfiguration is required in a request."];
      name: Name.t option
        [@ocaml.doc "The name for the run to be scheduled."];
      test: ScheduleRunTest.t
        [@ocaml.doc
          "Information about the test for the run to be scheduled."];
      configuration: ScheduleRunConfiguration.t option
        [@ocaml.doc
          "Information about the settings for the run to be scheduled."];
      executionConfiguration: ExecutionConfiguration.t option
        [@ocaml.doc
          "Specifies configuration information about a test run, such as the execution timeout (in minutes)."]}
    let context_ = "ScheduleRunRequest"
    let make ?appArn =
      fun ?devicePoolArn ->
        fun ?deviceSelectionConfiguration ->
          fun ?name ->
            fun ?configuration ->
              fun ?executionConfiguration ->
                fun ~projectArn ->
                  fun ~test ->
                    fun () ->
                      {
                        appArn;
                        devicePoolArn;
                        deviceSelectionConfiguration;
                        name;
                        configuration;
                        executionConfiguration;
                        projectArn;
                        test
                      }
    let to_value x =
      structure_to_value
        [("projectArn", (Some (AmazonResourceName.to_value x.projectArn)));
        ("appArn", (Option.map x.appArn ~f:AmazonResourceName.to_value));
        ("devicePoolArn",
          (Option.map x.devicePoolArn ~f:AmazonResourceName.to_value));
        ("deviceSelectionConfiguration",
          (Option.map x.deviceSelectionConfiguration
             ~f:DeviceSelectionConfiguration.to_value));
        ("name", (Option.map x.name ~f:Name.to_value));
        ("test", (Some (ScheduleRunTest.to_value x.test)));
        ("configuration",
          (Option.map x.configuration ~f:ScheduleRunConfiguration.to_value));
        ("executionConfiguration",
          (Option.map x.executionConfiguration
             ~f:ExecutionConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let executionConfiguration =
        (Option.map ~f:ExecutionConfiguration.of_xml)
          (Xml.child xml_arg0 "executionConfiguration") in
      let configuration =
        (Option.map ~f:ScheduleRunConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      let test =
        ScheduleRunTest.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "test") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      let deviceSelectionConfiguration =
        (Option.map ~f:DeviceSelectionConfiguration.of_xml)
          (Xml.child xml_arg0 "deviceSelectionConfiguration") in
      let devicePoolArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "devicePoolArn") in
      let appArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "appArn") in
      let projectArn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "projectArn") in
      make ?executionConfiguration ?configuration ~test ?name
        ?deviceSelectionConfiguration ?devicePoolArn ?appArn ~projectArn ()
    let of_json json =
      let executionConfiguration =
        field_map json "executionConfiguration"
          ExecutionConfiguration.of_json in
      let configuration =
        field_map json "configuration" ScheduleRunConfiguration.of_json in
      let test = field_map_exn json "test" ScheduleRunTest.of_json in
      let name = field_map json "name" Name.of_json in
      let deviceSelectionConfiguration =
        field_map json "deviceSelectionConfiguration"
          DeviceSelectionConfiguration.of_json in
      let devicePoolArn =
        field_map json "devicePoolArn" AmazonResourceName.of_json in
      let appArn = field_map json "appArn" AmazonResourceName.of_json in
      let projectArn =
        field_map_exn json "projectArn" AmazonResourceName.of_json in
      make ?executionConfiguration ?configuration ~test ?name
        ?deviceSelectionConfiguration ?devicePoolArn ?appArn ~projectArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a request to the schedule run operation."]
module RenewOfferingResult =
  struct
    type nonrec t =
      {
      offeringTransaction: OfferingTransaction.t option
        [@ocaml.doc
          "Represents the status of the offering transaction for the renewal."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotEligibleException of NotEligibleException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?offeringTransaction = fun () -> { offeringTransaction }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotEligibleException" ->
          `NotEligibleException (NotEligibleException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotEligibleException" ->
          `NotEligibleException (NotEligibleException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotEligibleException e ->
          `Assoc
            [("error", (`String "NotEligibleException"));
            ("details", (NotEligibleException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("offeringTransaction",
           (Option.map x.offeringTransaction ~f:OfferingTransaction.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let offeringTransaction =
        (Option.map ~f:OfferingTransaction.of_xml)
          (Xml.child xml_arg0 "offeringTransaction") in
      make ?offeringTransaction ()
    let of_json json =
      let offeringTransaction =
        field_map json "offeringTransaction" OfferingTransaction.of_json in
      make ?offeringTransaction ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The result of a renewal offering."]
module RenewOfferingRequest =
  struct
    type nonrec t =
      {
      offeringId: OfferingIdentifier.t
        [@ocaml.doc "The ID of a request to renew an offering."];
      quantity: Integer.t
        [@ocaml.doc "The quantity requested in an offering renewal."]}
    let context_ = "RenewOfferingRequest"
    let make ~offeringId =
      fun ~quantity -> fun () -> { offeringId; quantity }
    let to_value x =
      structure_to_value
        [("offeringId", (Some (OfferingIdentifier.to_value x.offeringId)));
        ("quantity", (Some (Integer.to_value x.quantity)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let quantity =
        Integer.of_xml (Xml.child_exn ~context:context_ xml_arg0 "quantity") in
      let offeringId =
        OfferingIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "offeringId") in
      make ~quantity ~offeringId ()
    let of_json json =
      let quantity = field_map_exn json "quantity" Integer.of_json in
      let offeringId =
        field_map_exn json "offeringId" OfferingIdentifier.of_json in
      make ~quantity ~offeringId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A request that represents an offering renewal."]
module PurchaseOfferingResult =
  struct
    type nonrec t =
      {
      offeringTransaction: OfferingTransaction.t option
        [@ocaml.doc
          "Represents the offering transaction for the purchase result."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotEligibleException of NotEligibleException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?offeringTransaction = fun () -> { offeringTransaction }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotEligibleException" ->
          `NotEligibleException (NotEligibleException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotEligibleException" ->
          `NotEligibleException (NotEligibleException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotEligibleException e ->
          `Assoc
            [("error", (`String "NotEligibleException"));
            ("details", (NotEligibleException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("offeringTransaction",
           (Option.map x.offeringTransaction ~f:OfferingTransaction.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let offeringTransaction =
        (Option.map ~f:OfferingTransaction.of_xml)
          (Xml.child xml_arg0 "offeringTransaction") in
      make ?offeringTransaction ()
    let of_json json =
      let offeringTransaction =
        field_map json "offeringTransaction" OfferingTransaction.of_json in
      make ?offeringTransaction ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The result of the purchase offering (for example, success or failure)."]
module PurchaseOfferingRequest =
  struct
    type nonrec t =
      {
      offeringId: OfferingIdentifier.t [@ocaml.doc "The ID of the offering."];
      quantity: Integer.t
        [@ocaml.doc
          "The number of device slots to purchase in an offering request."];
      offeringPromotionId: OfferingPromotionIdentifier.t option
        [@ocaml.doc
          "The ID of the offering promotion to be applied to the purchase."]}
    let context_ = "PurchaseOfferingRequest"
    let make ?offeringPromotionId =
      fun ~offeringId ->
        fun ~quantity ->
          fun () -> { offeringPromotionId; offeringId; quantity }
    let to_value x =
      structure_to_value
        [("offeringId", (Some (OfferingIdentifier.to_value x.offeringId)));
        ("quantity", (Some (Integer.to_value x.quantity)));
        ("offeringPromotionId",
          (Option.map x.offeringPromotionId
             ~f:OfferingPromotionIdentifier.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let offeringPromotionId =
        (Option.map ~f:OfferingPromotionIdentifier.of_xml)
          (Xml.child xml_arg0 "offeringPromotionId") in
      let quantity =
        Integer.of_xml (Xml.child_exn ~context:context_ xml_arg0 "quantity") in
      let offeringId =
        OfferingIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "offeringId") in
      make ?offeringPromotionId ~quantity ~offeringId ()
    let of_json json =
      let offeringPromotionId =
        field_map json "offeringPromotionId"
          OfferingPromotionIdentifier.of_json in
      let quantity = field_map_exn json "quantity" Integer.of_json in
      let offeringId =
        field_map_exn json "offeringId" OfferingIdentifier.of_json in
      make ?offeringPromotionId ~quantity ~offeringId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a request for a purchase offering."]
module ListVPCEConfigurationsResult =
  struct
    type nonrec t =
      {
      vpceConfigurations: VPCEConfigurations.t option
        [@ocaml.doc
          "An array of VPCEConfiguration objects that contain information about your VPC endpoint configuration."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?vpceConfigurations =
      fun ?nextToken -> fun () -> { vpceConfigurations; nextToken }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpceConfigurations",
           (Option.map x.vpceConfigurations ~f:VPCEConfigurations.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let vpceConfigurations =
        (Option.map ~f:VPCEConfigurations.of_xml)
          (Xml.child xml_arg0 "vpceConfigurations") in
      make ?nextToken ?vpceConfigurations ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let vpceConfigurations =
        field_map json "vpceConfigurations" VPCEConfigurations.of_json in
      make ?nextToken ?vpceConfigurations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about all Amazon Virtual Private Cloud (VPC) endpoint configurations in the AWS account."]
module ListVPCEConfigurationsRequest =
  struct
    type nonrec t =
      {
      maxResults: Integer.t option
        [@ocaml.doc
          "An integer that specifies the maximum number of items you want to return in the API response."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    let make ?maxResults =
      fun ?nextToken -> fun () -> { maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("maxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxResults") in
      make ?nextToken ?maxResults ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let maxResults = field_map json "maxResults" Integer.of_json in
      make ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about all Amazon Virtual Private Cloud (VPC) endpoint configurations in the AWS account."]
module ListUploadsResult =
  struct
    type nonrec t =
      {
      uploads: Uploads.t option [@ocaml.doc "Information about the uploads."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "If the number of items that are returned is significantly large, this is an identifier that is also returned. It can be used in a subsequent call to this operation to return the next set of items in the list."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?uploads = fun ?nextToken -> fun () -> { uploads; nextToken }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("uploads", (Option.map x.uploads ~f:Uploads.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let uploads =
        (Option.map ~f:Uploads.of_xml) (Xml.child xml_arg0 "uploads") in
      make ?nextToken ?uploads ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let uploads = field_map json "uploads" Uploads.of_json in
      make ?nextToken ?uploads ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a list uploads request."]
module ListUploadsRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the project for which you want to list uploads."];
      type_: UploadType.t option
        [@ocaml.doc
          "The type of upload. Must be one of the following values: ANDROID_APP IOS_APP WEB_APP EXTERNAL_DATA APPIUM_JAVA_JUNIT_TEST_PACKAGE APPIUM_JAVA_TESTNG_TEST_PACKAGE APPIUM_PYTHON_TEST_PACKAGE APPIUM_NODE_TEST_PACKAGE APPIUM_RUBY_TEST_PACKAGE APPIUM_WEB_JAVA_JUNIT_TEST_PACKAGE APPIUM_WEB_JAVA_TESTNG_TEST_PACKAGE APPIUM_WEB_PYTHON_TEST_PACKAGE APPIUM_WEB_NODE_TEST_PACKAGE APPIUM_WEB_RUBY_TEST_PACKAGE CALABASH_TEST_PACKAGE INSTRUMENTATION_TEST_PACKAGE UIAUTOMATION_TEST_PACKAGE UIAUTOMATOR_TEST_PACKAGE XCTEST_TEST_PACKAGE XCTEST_UI_TEST_PACKAGE APPIUM_JAVA_JUNIT_TEST_SPEC APPIUM_JAVA_TESTNG_TEST_SPEC APPIUM_PYTHON_TEST_SPEC APPIUM_NODE_TEST_SPEC APPIUM_RUBY_TEST_SPEC APPIUM_WEB_JAVA_JUNIT_TEST_SPEC APPIUM_WEB_JAVA_TESTNG_TEST_SPEC APPIUM_WEB_PYTHON_TEST_SPEC APPIUM_WEB_NODE_TEST_SPEC APPIUM_WEB_RUBY_TEST_SPEC INSTRUMENTATION_TEST_SPEC XCTEST_UI_TEST_SPEC"];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    let context_ = "ListUploadsRequest"
    let make ?type_ =
      fun ?nextToken -> fun ~arn -> fun () -> { type_; nextToken; arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)));
        ("type", (Option.map x.type_ ~f:UploadType.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let type_ =
        (Option.map ~f:UploadType.of_xml) (Xml.child xml_arg0 "type") in
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ?nextToken ?type_ ~arn ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let type_ = field_map json "type" UploadType.of_json in
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ?nextToken ?type_ ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a request to the list uploads operation."]
module ListUniqueProblemsResult =
  struct
    type nonrec t =
      {
      uniqueProblems: UniqueProblemsByExecutionResultMap.t option
        [@ocaml.doc
          "Information about the unique problems. Allowed values include: PENDING PASSED WARNED FAILED SKIPPED ERRORED STOPPED"];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "If the number of items that are returned is significantly large, this is an identifier that is also returned. It can be used in a subsequent call to this operation to return the next set of items in the list."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?uniqueProblems =
      fun ?nextToken -> fun () -> { uniqueProblems; nextToken }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("uniqueProblems",
           (Option.map x.uniqueProblems
              ~f:UniqueProblemsByExecutionResultMap.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let uniqueProblems =
        (Option.map ~f:UniqueProblemsByExecutionResultMap.of_xml)
          (Xml.child xml_arg0 "uniqueProblems") in
      make ?nextToken ?uniqueProblems ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let uniqueProblems =
        field_map json "uniqueProblems"
          UniqueProblemsByExecutionResultMap.of_json in
      make ?nextToken ?uniqueProblems ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a list unique problems request."]
module ListUniqueProblemsRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t [@ocaml.doc "The unique problems' ARNs."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    let context_ = "ListUniqueProblemsRequest"
    let make ?nextToken = fun ~arn -> fun () -> { nextToken; arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ?nextToken ~arn ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ?nextToken ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to the list unique problems operation."]
module ListTestsResult =
  struct
    type nonrec t =
      {
      tests: Tests.t option [@ocaml.doc "Information about the tests."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "If the number of items that are returned is significantly large, this is an identifier that is also returned. It can be used in a subsequent call to this operation to return the next set of items in the list."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tests = fun ?nextToken -> fun () -> { tests; nextToken }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("tests", (Option.map x.tests ~f:Tests.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let tests = (Option.map ~f:Tests.of_xml) (Xml.child xml_arg0 "tests") in
      make ?nextToken ?tests ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let tests = field_map json "tests" Tests.of_json in
      make ?nextToken ?tests ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a list tests request."]
module ListTestsRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc "The test suite's Amazon Resource Name (ARN)."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    let context_ = "ListTestsRequest"
    let make ?nextToken = fun ~arn -> fun () -> { nextToken; arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ?nextToken ~arn ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ?nextToken ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a request to the list tests operation."]
module ListTestGridSessionsResult =
  struct
    type nonrec t =
      {
      testGridSessions: TestGridSessions.t option
        [@ocaml.doc
          "The sessions that match the criteria in a ListTestGridSessionsRequest."];
      nextToken: PaginationToken.t option [@ocaml.doc "Pagination token."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `NotFoundException of NotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?testGridSessions =
      fun ?nextToken -> fun () -> { testGridSessions; nextToken }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("testGridSessions",
           (Option.map x.testGridSessions ~f:TestGridSessions.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let testGridSessions =
        (Option.map ~f:TestGridSessions.of_xml)
          (Xml.child xml_arg0 "testGridSessions") in
      make ?nextToken ?testGridSessions ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let testGridSessions =
        field_map json "testGridSessions" TestGridSessions.of_json in
      make ?nextToken ?testGridSessions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves a list of sessions for a TestGridProject."]
module ListTestGridSessionsRequest =
  struct
    type nonrec t =
      {
      projectArn: DeviceFarmArn.t [@ocaml.doc "ARN of a TestGridProject."];
      status: TestGridSessionStatus.t option
        [@ocaml.doc "Return only sessions in this state."];
      creationTimeAfter: DateTime.t option
        [@ocaml.doc "Return only sessions created after this time."];
      creationTimeBefore: DateTime.t option
        [@ocaml.doc "Return only sessions created before this time."];
      endTimeAfter: DateTime.t option
        [@ocaml.doc "Return only sessions that ended after this time."];
      endTimeBefore: DateTime.t option
        [@ocaml.doc "Return only sessions that ended before this time."];
      maxResult: MaxPageSize.t option
        [@ocaml.doc "Return only this many results at a time."];
      nextToken: PaginationToken.t option [@ocaml.doc "Pagination token."]}
    let context_ = "ListTestGridSessionsRequest"
    let make ?status =
      fun ?creationTimeAfter ->
        fun ?creationTimeBefore ->
          fun ?endTimeAfter ->
            fun ?endTimeBefore ->
              fun ?maxResult ->
                fun ?nextToken ->
                  fun ~projectArn ->
                    fun () ->
                      {
                        status;
                        creationTimeAfter;
                        creationTimeBefore;
                        endTimeAfter;
                        endTimeBefore;
                        maxResult;
                        nextToken;
                        projectArn
                      }
    let to_value x =
      structure_to_value
        [("projectArn", (Some (DeviceFarmArn.to_value x.projectArn)));
        ("status", (Option.map x.status ~f:TestGridSessionStatus.to_value));
        ("creationTimeAfter",
          (Option.map x.creationTimeAfter ~f:DateTime.to_value));
        ("creationTimeBefore",
          (Option.map x.creationTimeBefore ~f:DateTime.to_value));
        ("endTimeAfter", (Option.map x.endTimeAfter ~f:DateTime.to_value));
        ("endTimeBefore", (Option.map x.endTimeBefore ~f:DateTime.to_value));
        ("maxResult", (Option.map x.maxResult ~f:MaxPageSize.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let maxResult =
        (Option.map ~f:MaxPageSize.of_xml) (Xml.child xml_arg0 "maxResult") in
      let endTimeBefore =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "endTimeBefore") in
      let endTimeAfter =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "endTimeAfter") in
      let creationTimeBefore =
        (Option.map ~f:DateTime.of_xml)
          (Xml.child xml_arg0 "creationTimeBefore") in
      let creationTimeAfter =
        (Option.map ~f:DateTime.of_xml)
          (Xml.child xml_arg0 "creationTimeAfter") in
      let status =
        (Option.map ~f:TestGridSessionStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let projectArn =
        DeviceFarmArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "projectArn") in
      make ?nextToken ?maxResult ?endTimeBefore ?endTimeAfter
        ?creationTimeBefore ?creationTimeAfter ?status ~projectArn ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let maxResult = field_map json "maxResult" MaxPageSize.of_json in
      let endTimeBefore = field_map json "endTimeBefore" DateTime.of_json in
      let endTimeAfter = field_map json "endTimeAfter" DateTime.of_json in
      let creationTimeBefore =
        field_map json "creationTimeBefore" DateTime.of_json in
      let creationTimeAfter =
        field_map json "creationTimeAfter" DateTime.of_json in
      let status = field_map json "status" TestGridSessionStatus.of_json in
      let projectArn = field_map_exn json "projectArn" DeviceFarmArn.of_json in
      make ?nextToken ?maxResult ?endTimeBefore ?endTimeAfter
        ?creationTimeBefore ?creationTimeAfter ?status ~projectArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves a list of sessions for a TestGridProject."]
module ListTestGridSessionArtifactsResult =
  struct
    type nonrec t =
      {
      artifacts: TestGridSessionArtifacts.t option
        [@ocaml.doc
          "A list of test grid session artifacts for a TestGridSession."];
      nextToken: PaginationToken.t option [@ocaml.doc "Pagination token."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `NotFoundException of NotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?artifacts =
      fun ?nextToken -> fun () -> { artifacts; nextToken }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("artifacts",
           (Option.map x.artifacts ~f:TestGridSessionArtifacts.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let artifacts =
        (Option.map ~f:TestGridSessionArtifacts.of_xml)
          (Xml.child xml_arg0 "artifacts") in
      make ?nextToken ?artifacts ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let artifacts =
        field_map json "artifacts" TestGridSessionArtifacts.of_json in
      make ?nextToken ?artifacts ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a list of artifacts created during the session."]
module ListTestGridSessionArtifactsRequest =
  struct
    type nonrec t =
      {
      sessionArn: DeviceFarmArn.t
        [@ocaml.doc "The ARN of a TestGridSession."];
      type_: TestGridSessionArtifactCategory.t option
        [@ocaml.doc "Limit results to a specified type of artifact."];
      maxResult: MaxPageSize.t option
        [@ocaml.doc
          "The maximum number of results to be returned by a request."];
      nextToken: PaginationToken.t option [@ocaml.doc "Pagination token."]}
    let context_ = "ListTestGridSessionArtifactsRequest"
    let make ?type_ =
      fun ?maxResult ->
        fun ?nextToken ->
          fun ~sessionArn ->
            fun () -> { type_; maxResult; nextToken; sessionArn }
    let to_value x =
      structure_to_value
        [("sessionArn", (Some (DeviceFarmArn.to_value x.sessionArn)));
        ("type",
          (Option.map x.type_ ~f:TestGridSessionArtifactCategory.to_value));
        ("maxResult", (Option.map x.maxResult ~f:MaxPageSize.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let maxResult =
        (Option.map ~f:MaxPageSize.of_xml) (Xml.child xml_arg0 "maxResult") in
      let type_ =
        (Option.map ~f:TestGridSessionArtifactCategory.of_xml)
          (Xml.child xml_arg0 "type") in
      let sessionArn =
        DeviceFarmArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "sessionArn") in
      make ?nextToken ?maxResult ?type_ ~sessionArn ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let maxResult = field_map json "maxResult" MaxPageSize.of_json in
      let type_ =
        field_map json "type" TestGridSessionArtifactCategory.of_json in
      let sessionArn = field_map_exn json "sessionArn" DeviceFarmArn.of_json in
      make ?nextToken ?maxResult ?type_ ~sessionArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a list of artifacts created during the session."]
module ListTestGridSessionActionsResult =
  struct
    type nonrec t =
      {
      actions: TestGridSessionActions.t option
        [@ocaml.doc "The action taken by the session."];
      nextToken: PaginationToken.t option [@ocaml.doc "Pagination token."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `NotFoundException of NotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?actions = fun ?nextToken -> fun () -> { actions; nextToken }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("actions",
           (Option.map x.actions ~f:TestGridSessionActions.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let actions =
        (Option.map ~f:TestGridSessionActions.of_xml)
          (Xml.child xml_arg0 "actions") in
      make ?nextToken ?actions ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let actions = field_map json "actions" TestGridSessionActions.of_json in
      make ?nextToken ?actions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of the actions taken in a TestGridSession."]
module ListTestGridSessionActionsRequest =
  struct
    type nonrec t =
      {
      sessionArn: DeviceFarmArn.t
        [@ocaml.doc "The ARN of the session to retrieve."];
      maxResult: MaxPageSize.t option
        [@ocaml.doc "The maximum number of sessions to return per response."];
      nextToken: PaginationToken.t option [@ocaml.doc "Pagination token."]}
    let context_ = "ListTestGridSessionActionsRequest"
    let make ?maxResult =
      fun ?nextToken ->
        fun ~sessionArn -> fun () -> { maxResult; nextToken; sessionArn }
    let to_value x =
      structure_to_value
        [("sessionArn", (Some (DeviceFarmArn.to_value x.sessionArn)));
        ("maxResult", (Option.map x.maxResult ~f:MaxPageSize.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let maxResult =
        (Option.map ~f:MaxPageSize.of_xml) (Xml.child xml_arg0 "maxResult") in
      let sessionArn =
        DeviceFarmArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "sessionArn") in
      make ?nextToken ?maxResult ~sessionArn ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let maxResult = field_map json "maxResult" MaxPageSize.of_json in
      let sessionArn = field_map_exn json "sessionArn" DeviceFarmArn.of_json in
      make ?nextToken ?maxResult ~sessionArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of the actions taken in a TestGridSession."]
module ListTestGridProjectsResult =
  struct
    type nonrec t =
      {
      testGridProjects: TestGridProjects.t option
        [@ocaml.doc
          "The list of TestGridProjects, based on a ListTestGridProjectsRequest."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "Used for pagination. Pass into ListTestGridProjects to get more results in a paginated request."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?testGridProjects =
      fun ?nextToken -> fun () -> { testGridProjects; nextToken }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("testGridProjects",
           (Option.map x.testGridProjects ~f:TestGridProjects.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let testGridProjects =
        (Option.map ~f:TestGridProjects.of_xml)
          (Xml.child xml_arg0 "testGridProjects") in
      make ?nextToken ?testGridProjects ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let testGridProjects =
        field_map json "testGridProjects" TestGridProjects.of_json in
      make ?nextToken ?testGridProjects ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a list of all Selenium testing projects in your account."]
module ListTestGridProjectsRequest =
  struct
    type nonrec t =
      {
      maxResult: MaxPageSize.t option
        [@ocaml.doc "Return no more than this number of results."];
      nextToken: PaginationToken.t option
        [@ocaml.doc "From a response, used to continue a paginated listing."]}
    let make ?maxResult =
      fun ?nextToken -> fun () -> { maxResult; nextToken }
    let to_value x =
      structure_to_value
        [("maxResult", (Option.map x.maxResult ~f:MaxPageSize.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let maxResult =
        (Option.map ~f:MaxPageSize.of_xml) (Xml.child xml_arg0 "maxResult") in
      make ?nextToken ?maxResult ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let maxResult = field_map json "maxResult" MaxPageSize.of_json in
      make ?nextToken ?maxResult ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets a list of all Selenium testing projects in your account."]
module ListTagsForResourceResponse =
  struct
    type nonrec t =
      {
      tags: TagList.t option
        [@ocaml.doc
          "The tags to add to the resource. A tag is an array of key-value pairs. Tag keys can have a maximum character length of 128 characters. Tag values can have a maximum length of 256 characters."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `NotFoundException of NotFoundException.t 
      | `TagOperationException of TagOperationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "TagOperationException" ->
          `TagOperationException (TagOperationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "TagOperationException" ->
          `TagOperationException (TagOperationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `TagOperationException e ->
          `Assoc
            [("error", (`String "TagOperationException"));
            ("details", (TagOperationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("Tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      make ?tags ()
    let of_json json =
      let tags = field_map json "Tags" TagList.of_json in make ?tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "List the tags for an AWS Device Farm resource."]
module ListTagsForResourceRequest =
  struct
    type nonrec t =
      {
      resourceARN: DeviceFarmArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the resource or resources for which to list tags. You can associate tags with the following Device Farm resources: PROJECT, RUN, NETWORK_PROFILE, INSTANCE_PROFILE, DEVICE_INSTANCE, SESSION, DEVICE_POOL, DEVICE, and VPCE_CONFIGURATION."]}
    let context_ = "ListTagsForResourceRequest"
    let make ~resourceARN = fun () -> { resourceARN }
    let to_value x =
      structure_to_value
        [("ResourceARN", (Some (DeviceFarmArn.to_value x.resourceARN)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceARN =
        DeviceFarmArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceARN") in
      make ~resourceARN ()
    let of_json json =
      let resourceARN =
        field_map_exn json "ResourceARN" DeviceFarmArn.of_json in
      make ~resourceARN ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "List the tags for an AWS Device Farm resource."]
module ListSuitesResult =
  struct
    type nonrec t =
      {
      suites: Suites.t option [@ocaml.doc "Information about the suites."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "If the number of items that are returned is significantly large, this is an identifier that is also returned. It can be used in a subsequent call to this operation to return the next set of items in the list."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?suites = fun ?nextToken -> fun () -> { suites; nextToken }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("suites", (Option.map x.suites ~f:Suites.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let suites =
        (Option.map ~f:Suites.of_xml) (Xml.child xml_arg0 "suites") in
      make ?nextToken ?suites ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let suites = field_map json "suites" Suites.of_json in
      make ?nextToken ?suites ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a list suites request."]
module ListSuitesRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc "The job's Amazon Resource Name (ARN)."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    let context_ = "ListSuitesRequest"
    let make ?nextToken = fun ~arn -> fun () -> { nextToken; arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ?nextToken ~arn ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ?nextToken ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a request to the list suites operation."]
module ListSamplesResult =
  struct
    type nonrec t =
      {
      samples: Samples.t option [@ocaml.doc "Information about the samples."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "If the number of items that are returned is significantly large, this is an identifier that is also returned. It can be used in a subsequent call to this operation to return the next set of items in the list."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?samples = fun ?nextToken -> fun () -> { samples; nextToken }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("samples", (Option.map x.samples ~f:Samples.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let samples =
        (Option.map ~f:Samples.of_xml) (Xml.child xml_arg0 "samples") in
      make ?nextToken ?samples ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let samples = field_map json "samples" Samples.of_json in
      make ?nextToken ?samples ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a list samples request."]
module ListSamplesRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the job used to list samples."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    let context_ = "ListSamplesRequest"
    let make ?nextToken = fun ~arn -> fun () -> { nextToken; arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ?nextToken ~arn ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ?nextToken ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a request to the list samples operation."]
module ListRunsResult =
  struct
    type nonrec t =
      {
      runs: Runs.t option [@ocaml.doc "Information about the runs."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "If the number of items that are returned is significantly large, this is an identifier that is also returned. It can be used in a subsequent call to this operation to return the next set of items in the list."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?runs = fun ?nextToken -> fun () -> { runs; nextToken }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("runs", (Option.map x.runs ~f:Runs.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let runs = (Option.map ~f:Runs.of_xml) (Xml.child xml_arg0 "runs") in
      make ?nextToken ?runs ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let runs = field_map json "runs" Runs.of_json in
      make ?nextToken ?runs ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a list runs request."]
module ListRunsRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the project for which you want to list runs."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    let context_ = "ListRunsRequest"
    let make ?nextToken = fun ~arn -> fun () -> { nextToken; arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ?nextToken ~arn ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ?nextToken ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a request to the list runs operation."]
module ListRemoteAccessSessionsResult =
  struct
    type nonrec t =
      {
      remoteAccessSessions: RemoteAccessSessions.t option
        [@ocaml.doc
          "A container that represents the metadata from the service about each remote access session you are requesting."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?remoteAccessSessions =
      fun ?nextToken -> fun () -> { remoteAccessSessions; nextToken }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("remoteAccessSessions",
           (Option.map x.remoteAccessSessions
              ~f:RemoteAccessSessions.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let remoteAccessSessions =
        (Option.map ~f:RemoteAccessSessions.of_xml)
          (Xml.child xml_arg0 "remoteAccessSessions") in
      make ?nextToken ?remoteAccessSessions ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let remoteAccessSessions =
        field_map json "remoteAccessSessions" RemoteAccessSessions.of_json in
      make ?nextToken ?remoteAccessSessions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the response from the server after AWS Device Farm makes a request to return information about the remote access session."]
module ListRemoteAccessSessionsRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the project about which you are requesting information."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    let context_ = "ListRemoteAccessSessionsRequest"
    let make ?nextToken = fun ~arn -> fun () -> { nextToken; arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ?nextToken ~arn ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ?nextToken ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the request to return information about the remote access session."]
module ListProjectsResult =
  struct
    type nonrec t =
      {
      projects: Projects.t option
        [@ocaml.doc "Information about the projects."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "If the number of items that are returned is significantly large, this is an identifier that is also returned. It can be used in a subsequent call to this operation to return the next set of items in the list."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?projects = fun ?nextToken -> fun () -> { projects; nextToken }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("projects", (Option.map x.projects ~f:Projects.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let projects =
        (Option.map ~f:Projects.of_xml) (Xml.child xml_arg0 "projects") in
      make ?nextToken ?projects ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let projects = field_map json "projects" Projects.of_json in
      make ?nextToken ?projects ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a list projects request."]
module ListProjectsRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t option
        [@ocaml.doc
          "Optional. If no Amazon Resource Name (ARN) is specified, then AWS Device Farm returns a list of all projects for the AWS account. You can also specify a project ARN."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    let make ?arn = fun ?nextToken -> fun () -> { arn; nextToken }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:AmazonResourceName.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let arn =
        (Option.map ~f:AmazonResourceName.of_xml) (Xml.child xml_arg0 "arn") in
      make ?nextToken ?arn ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let arn = field_map json "arn" AmazonResourceName.of_json in
      make ?nextToken ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a request to the list projects operation."]
module ListOfferingsResult =
  struct
    type nonrec t =
      {
      offerings: Offerings.t option
        [@ocaml.doc "A value that represents the list offering results."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotEligibleException of NotEligibleException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?offerings =
      fun ?nextToken -> fun () -> { offerings; nextToken }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotEligibleException" ->
          `NotEligibleException (NotEligibleException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotEligibleException" ->
          `NotEligibleException (NotEligibleException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotEligibleException e ->
          `Assoc
            [("error", (`String "NotEligibleException"));
            ("details", (NotEligibleException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("offerings", (Option.map x.offerings ~f:Offerings.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let offerings =
        (Option.map ~f:Offerings.of_xml) (Xml.child xml_arg0 "offerings") in
      make ?nextToken ?offerings ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let offerings = field_map json "offerings" Offerings.of_json in
      make ?nextToken ?offerings ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the return values of the list of offerings."]
module ListOfferingsRequest =
  struct
    type nonrec t =
      {
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    let make ?nextToken = fun () -> { nextToken }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      make ?nextToken ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      make ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the request to list all offerings."]
module ListOfferingTransactionsResult =
  struct
    type nonrec t =
      {
      offeringTransactions: OfferingTransactions.t option
        [@ocaml.doc
          "The audit log of subscriptions you have purchased and modified through AWS Device Farm."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotEligibleException of NotEligibleException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?offeringTransactions =
      fun ?nextToken -> fun () -> { offeringTransactions; nextToken }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotEligibleException" ->
          `NotEligibleException (NotEligibleException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotEligibleException" ->
          `NotEligibleException (NotEligibleException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotEligibleException e ->
          `Assoc
            [("error", (`String "NotEligibleException"));
            ("details", (NotEligibleException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("offeringTransactions",
           (Option.map x.offeringTransactions
              ~f:OfferingTransactions.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let offeringTransactions =
        (Option.map ~f:OfferingTransactions.of_xml)
          (Xml.child xml_arg0 "offeringTransactions") in
      make ?nextToken ?offeringTransactions ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let offeringTransactions =
        field_map json "offeringTransactions" OfferingTransactions.of_json in
      make ?nextToken ?offeringTransactions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns the transaction log of the specified offerings."]
module ListOfferingTransactionsRequest =
  struct
    type nonrec t =
      {
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    let make ?nextToken = fun () -> { nextToken }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      make ?nextToken ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      make ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the request to list the offering transaction history."]
module ListOfferingPromotionsResult =
  struct
    type nonrec t =
      {
      offeringPromotions: OfferingPromotions.t option
        [@ocaml.doc "Information about the offering promotions."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier to be used in the next call to this operation, to return the next set of items in the list."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotEligibleException of NotEligibleException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?offeringPromotions =
      fun ?nextToken -> fun () -> { offeringPromotions; nextToken }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotEligibleException" ->
          `NotEligibleException (NotEligibleException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotEligibleException" ->
          `NotEligibleException (NotEligibleException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotEligibleException e ->
          `Assoc
            [("error", (`String "NotEligibleException"));
            ("details", (NotEligibleException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("offeringPromotions",
           (Option.map x.offeringPromotions ~f:OfferingPromotions.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let offeringPromotions =
        (Option.map ~f:OfferingPromotions.of_xml)
          (Xml.child xml_arg0 "offeringPromotions") in
      make ?nextToken ?offeringPromotions ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let offeringPromotions =
        field_map json "offeringPromotions" OfferingPromotions.of_json in
      make ?nextToken ?offeringPromotions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of offering promotions. Each offering promotion record contains the ID and description of the promotion. The API returns a NotEligible error if the caller is not permitted to invoke the operation. Contact aws-devicefarm-support@amazon.com if you must be able to invoke this operation."]
module ListOfferingPromotionsRequest =
  struct
    type nonrec t =
      {
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    let make ?nextToken = fun () -> { nextToken }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      make ?nextToken ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      make ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of offering promotions. Each offering promotion record contains the ID and description of the promotion. The API returns a NotEligible error if the caller is not permitted to invoke the operation. Contact aws-devicefarm-support@amazon.com if you must be able to invoke this operation."]
module ListNetworkProfilesResult =
  struct
    type nonrec t =
      {
      networkProfiles: NetworkProfiles.t option
        [@ocaml.doc "A list of the available network profiles."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?networkProfiles =
      fun ?nextToken -> fun () -> { networkProfiles; nextToken }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkProfiles",
           (Option.map x.networkProfiles ~f:NetworkProfiles.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let networkProfiles =
        (Option.map ~f:NetworkProfiles.of_xml)
          (Xml.child xml_arg0 "networkProfiles") in
      make ?nextToken ?networkProfiles ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let networkProfiles =
        field_map json "networkProfiles" NetworkProfiles.of_json in
      make ?nextToken ?networkProfiles ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns the list of available network profiles."]
module ListNetworkProfilesRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the project for which you want to list network profiles."];
      type_: NetworkProfileType.t option
        [@ocaml.doc
          "The type of network profile to return information about. Valid values are listed here."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    let context_ = "ListNetworkProfilesRequest"
    let make ?type_ =
      fun ?nextToken -> fun ~arn -> fun () -> { type_; nextToken; arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)));
        ("type", (Option.map x.type_ ~f:NetworkProfileType.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let type_ =
        (Option.map ~f:NetworkProfileType.of_xml) (Xml.child xml_arg0 "type") in
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ?nextToken ?type_ ~arn ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let type_ = field_map json "type" NetworkProfileType.of_json in
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ?nextToken ?type_ ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns the list of available network profiles."]
module ListJobsResult =
  struct
    type nonrec t =
      {
      jobs: Jobs.t option [@ocaml.doc "Information about the jobs."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "If the number of items that are returned is significantly large, this is an identifier that is also returned. It can be used in a subsequent call to this operation to return the next set of items in the list."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?jobs = fun ?nextToken -> fun () -> { jobs; nextToken }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("jobs", (Option.map x.jobs ~f:Jobs.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let jobs = (Option.map ~f:Jobs.of_xml) (Xml.child xml_arg0 "jobs") in
      make ?nextToken ?jobs ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let jobs = field_map json "jobs" Jobs.of_json in
      make ?nextToken ?jobs ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a list jobs request."]
module ListJobsRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc "The run's Amazon Resource Name (ARN)."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    let context_ = "ListJobsRequest"
    let make ?nextToken = fun ~arn -> fun () -> { nextToken; arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ?nextToken ~arn ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ?nextToken ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a request to the list jobs operation."]
module ListInstanceProfilesResult =
  struct
    type nonrec t =
      {
      instanceProfiles: InstanceProfiles.t option
        [@ocaml.doc
          "An object that contains information about your instance profiles."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that can be used in the next call to this operation to return the next set of items in the list."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?instanceProfiles =
      fun ?nextToken -> fun () -> { instanceProfiles; nextToken }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceProfiles",
           (Option.map x.instanceProfiles ~f:InstanceProfiles.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let instanceProfiles =
        (Option.map ~f:InstanceProfiles.of_xml)
          (Xml.child xml_arg0 "instanceProfiles") in
      make ?nextToken ?instanceProfiles ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let instanceProfiles =
        field_map json "instanceProfiles" InstanceProfiles.of_json in
      make ?nextToken ?instanceProfiles ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about all the instance profiles in an AWS account."]
module ListInstanceProfilesRequest =
  struct
    type nonrec t =
      {
      maxResults: Integer.t option
        [@ocaml.doc
          "An integer that specifies the maximum number of items you want to return in the API response."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    let make ?maxResults =
      fun ?nextToken -> fun () -> { maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("maxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxResults") in
      make ?nextToken ?maxResults ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let maxResults = field_map json "maxResults" Integer.of_json in
      make ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about all the instance profiles in an AWS account."]
module ListDevicesResult =
  struct
    type nonrec t =
      {
      devices: Devices.t option [@ocaml.doc "Information about the devices."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "If the number of items that are returned is significantly large, this is an identifier that is also returned. It can be used in a subsequent call to this operation to return the next set of items in the list."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?devices = fun ?nextToken -> fun () -> { devices; nextToken }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("devices", (Option.map x.devices ~f:Devices.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let devices =
        (Option.map ~f:Devices.of_xml) (Xml.child xml_arg0 "devices") in
      make ?nextToken ?devices ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let devices = field_map json "devices" Devices.of_json in
      make ?nextToken ?devices ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a list devices operation."]
module ListDevicesRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the project."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."];
      filters: DeviceFilters.t option
        [@ocaml.doc
          "Used to select a set of devices. A filter is made up of an attribute, an operator, and one or more values. Attribute: The aspect of a device such as platform or model used as the selection criteria in a device filter. Allowed values include: ARN: The Amazon Resource Name (ARN) of the device (for example, arn:aws:devicefarm:us-west-2::device:12345Example). PLATFORM: The device platform. Valid values are ANDROID or IOS. OS_VERSION: The operating system version (for example, 10.3.2). MODEL: The device model (for example, iPad 5th Gen). AVAILABILITY: The current availability of the device. Valid values are AVAILABLE, HIGHLY_AVAILABLE, BUSY, or TEMPORARY_NOT_AVAILABLE. FORM_FACTOR: The device form factor. Valid values are PHONE or TABLET. MANUFACTURER: The device manufacturer (for example, Apple). REMOTE_ACCESS_ENABLED: Whether the device is enabled for remote access. Valid values are TRUE or FALSE. REMOTE_DEBUG_ENABLED: Whether the device is enabled for remote debugging. Valid values are TRUE or FALSE. Because remote debugging is no longer supported, this attribute is ignored. INSTANCE_ARN: The Amazon Resource Name (ARN) of the device instance. INSTANCE_LABELS: The label of the device instance. FLEET_TYPE: The fleet type. Valid values are PUBLIC or PRIVATE. Operator: The filter operator. The EQUALS operator is available for every attribute except INSTANCE_LABELS. The CONTAINS operator is available for the INSTANCE_LABELS and MODEL attributes. The IN and NOT_IN operators are available for the ARN, OS_VERSION, MODEL, MANUFACTURER, and INSTANCE_ARN attributes. The LESS_THAN, GREATER_THAN, LESS_THAN_OR_EQUALS, and GREATER_THAN_OR_EQUALS operators are also available for the OS_VERSION attribute. Values: An array of one or more filter values. The IN and NOT_IN operators take a values array that has one or more elements. The other operators require an array with a single element. In a request, the AVAILABILITY attribute takes the following values: AVAILABLE, HIGHLY_AVAILABLE, BUSY, or TEMPORARY_NOT_AVAILABLE."]}
    let make ?arn =
      fun ?nextToken -> fun ?filters -> fun () -> { arn; nextToken; filters }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:AmazonResourceName.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value));
        ("filters", (Option.map x.filters ~f:DeviceFilters.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let filters =
        (Option.map ~f:DeviceFilters.of_xml) (Xml.child xml_arg0 "filters") in
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let arn =
        (Option.map ~f:AmazonResourceName.of_xml) (Xml.child xml_arg0 "arn") in
      make ?filters ?nextToken ?arn ()
    let of_json json =
      let filters = field_map json "filters" DeviceFilters.of_json in
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let arn = field_map json "arn" AmazonResourceName.of_json in
      make ?filters ?nextToken ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a list devices request."]
module ListDevicePoolsResult =
  struct
    type nonrec t =
      {
      devicePools: DevicePools.t option
        [@ocaml.doc "Information about the device pools."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "If the number of items that are returned is significantly large, this is an identifier that is also returned. It can be used in a subsequent call to this operation to return the next set of items in the list."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?devicePools =
      fun ?nextToken -> fun () -> { devicePools; nextToken }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("devicePools", (Option.map x.devicePools ~f:DevicePools.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let devicePools =
        (Option.map ~f:DevicePools.of_xml) (Xml.child xml_arg0 "devicePools") in
      make ?nextToken ?devicePools ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let devicePools = field_map json "devicePools" DevicePools.of_json in
      make ?nextToken ?devicePools ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a list device pools request."]
module ListDevicePoolsRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t [@ocaml.doc "The project ARN."];
      type_: DevicePoolType.t option
        [@ocaml.doc
          "The device pools' type. Allowed values include: CURATED: A device pool that is created and managed by AWS Device Farm. PRIVATE: A device pool that is created and managed by the device pool developer."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    let context_ = "ListDevicePoolsRequest"
    let make ?type_ =
      fun ?nextToken -> fun ~arn -> fun () -> { type_; nextToken; arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)));
        ("type", (Option.map x.type_ ~f:DevicePoolType.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let type_ =
        (Option.map ~f:DevicePoolType.of_xml) (Xml.child xml_arg0 "type") in
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ?nextToken ?type_ ~arn ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let type_ = field_map json "type" DevicePoolType.of_json in
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ?nextToken ?type_ ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a list device pools request."]
module ListDeviceInstancesResult =
  struct
    type nonrec t =
      {
      deviceInstances: DeviceInstances.t option
        [@ocaml.doc
          "An object that contains information about your device instances."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that can be used in the next call to this operation to return the next set of items in the list."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?deviceInstances =
      fun ?nextToken -> fun () -> { deviceInstances; nextToken }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("deviceInstances",
           (Option.map x.deviceInstances ~f:DeviceInstances.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let deviceInstances =
        (Option.map ~f:DeviceInstances.of_xml)
          (Xml.child xml_arg0 "deviceInstances") in
      make ?nextToken ?deviceInstances ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let deviceInstances =
        field_map json "deviceInstances" DeviceInstances.of_json in
      make ?nextToken ?deviceInstances ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about the private device instances associated with one or more AWS accounts."]
module ListDeviceInstancesRequest =
  struct
    type nonrec t =
      {
      maxResults: Integer.t option
        [@ocaml.doc
          "An integer that specifies the maximum number of items you want to return in the API response."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    let make ?maxResults =
      fun ?nextToken -> fun () -> { maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("maxResults", (Option.map x.maxResults ~f:Integer.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxResults") in
      make ?nextToken ?maxResults ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let maxResults = field_map json "maxResults" Integer.of_json in
      make ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about the private device instances associated with one or more AWS accounts."]
module ListArtifactsResult =
  struct
    type nonrec t =
      {
      artifacts: Artifacts.t option
        [@ocaml.doc "Information about the artifacts."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "If the number of items that are returned is significantly large, this is an identifier that is also returned. It can be used in a subsequent call to this operation to return the next set of items in the list."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?artifacts =
      fun ?nextToken -> fun () -> { artifacts; nextToken }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("artifacts", (Option.map x.artifacts ~f:Artifacts.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let artifacts =
        (Option.map ~f:Artifacts.of_xml) (Xml.child xml_arg0 "artifacts") in
      make ?nextToken ?artifacts ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let artifacts = field_map json "artifacts" Artifacts.of_json in
      make ?nextToken ?artifacts ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a list artifacts operation."]
module ListArtifactsRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc "The run, job, suite, or test ARN."];
      type_: ArtifactCategory.t
        [@ocaml.doc
          "The artifacts' type. Allowed values include: FILE LOG SCREENSHOT"];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    let context_ = "ListArtifactsRequest"
    let make ?nextToken =
      fun ~arn -> fun ~type_ -> fun () -> { nextToken; arn; type_ }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)));
        ("type", (Some (ArtifactCategory.to_value x.type_)));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let type_ =
        ArtifactCategory.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "type") in
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ?nextToken ~type_ ~arn ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let type_ = field_map_exn json "type" ArtifactCategory.of_json in
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ?nextToken ~type_ ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a request to the list artifacts operation."]
module InstallToRemoteAccessSessionResult =
  struct
    type nonrec t =
      {
      appUpload: Upload.t option
        [@ocaml.doc "An app to upload or that has been uploaded."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?appUpload = fun () -> { appUpload }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("appUpload", (Option.map x.appUpload ~f:Upload.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let appUpload =
        (Option.map ~f:Upload.of_xml) (Xml.child xml_arg0 "appUpload") in
      make ?appUpload ()
    let of_json json =
      let appUpload = field_map json "appUpload" Upload.of_json in
      make ?appUpload ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the response from the server after AWS Device Farm makes a request to install to a remote access session."]
module InstallToRemoteAccessSessionRequest =
  struct
    type nonrec t =
      {
      remoteAccessSessionArn: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the remote access session about which you are requesting information."];
      appArn: AmazonResourceName.t
        [@ocaml.doc
          "The ARN of the app about which you are requesting information."]}
    let context_ = "InstallToRemoteAccessSessionRequest"
    let make ~remoteAccessSessionArn =
      fun ~appArn -> fun () -> { remoteAccessSessionArn; appArn }
    let to_value x =
      structure_to_value
        [("remoteAccessSessionArn",
           (Some (AmazonResourceName.to_value x.remoteAccessSessionArn)));
        ("appArn", (Some (AmazonResourceName.to_value x.appArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let appArn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "appArn") in
      let remoteAccessSessionArn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "remoteAccessSessionArn") in
      make ~appArn ~remoteAccessSessionArn ()
    let of_json json =
      let appArn = field_map_exn json "appArn" AmazonResourceName.of_json in
      let remoteAccessSessionArn =
        field_map_exn json "remoteAccessSessionArn"
          AmazonResourceName.of_json in
      make ~appArn ~remoteAccessSessionArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the request to install an Android application (in .apk format) or an iOS application (in .ipa format) as part of a remote access session."]
module GetVPCEConfigurationResult =
  struct
    type nonrec t =
      {
      vpceConfiguration: VPCEConfiguration.t option
        [@ocaml.doc
          "An object that contains information about your VPC endpoint configuration."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?vpceConfiguration = fun () -> { vpceConfiguration }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpceConfiguration",
           (Option.map x.vpceConfiguration ~f:VPCEConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpceConfiguration =
        (Option.map ~f:VPCEConfiguration.of_xml)
          (Xml.child xml_arg0 "vpceConfiguration") in
      make ?vpceConfiguration ()
    let of_json json =
      let vpceConfiguration =
        field_map json "vpceConfiguration" VPCEConfiguration.of_json in
      make ?vpceConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about the configuration settings for your Amazon Virtual Private Cloud (VPC) endpoint."]
module GetVPCEConfigurationRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the VPC endpoint configuration you want to describe."]}
    let context_ = "GetVPCEConfigurationRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_json json =
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about the configuration settings for your Amazon Virtual Private Cloud (VPC) endpoint."]
module GetUploadResult =
  struct
    type nonrec t =
      {
      upload: Upload.t option
        [@ocaml.doc
          "An app or a set of one or more tests to upload or that have been uploaded."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?upload = fun () -> { upload }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("upload", (Option.map x.upload ~f:Upload.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let upload =
        (Option.map ~f:Upload.of_xml) (Xml.child xml_arg0 "upload") in
      make ?upload ()
    let of_json json =
      let upload = field_map json "upload" Upload.of_json in make ?upload ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a get upload request."]
module GetUploadRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t [@ocaml.doc "The upload's ARN."]}
    let context_ = "GetUploadRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_json json =
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a request to the get upload operation."]
module GetTestResult =
  struct
    type nonrec t =
      {
      test: Test.t option [@ocaml.doc "A test condition that is evaluated."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?test = fun () -> { test }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("test", (Option.map x.test ~f:Test.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let test = (Option.map ~f:Test.of_xml) (Xml.child xml_arg0 "test") in
      make ?test ()
    let of_json json =
      let test = field_map json "test" Test.of_json in make ?test ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a get test request."]
module GetTestRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t [@ocaml.doc "The test's ARN."]}
    let context_ = "GetTestRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_json json =
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a request to the get test operation."]
module GetTestGridSessionResult =
  struct
    type nonrec t =
      {
      testGridSession: TestGridSession.t option
        [@ocaml.doc "The TestGridSession that was requested."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `NotFoundException of NotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?testGridSession = fun () -> { testGridSession }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("testGridSession",
           (Option.map x.testGridSession ~f:TestGridSession.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let testGridSession =
        (Option.map ~f:TestGridSession.of_xml)
          (Xml.child xml_arg0 "testGridSession") in
      make ?testGridSession ()
    let of_json json =
      let testGridSession =
        field_map json "testGridSession" TestGridSession.of_json in
      make ?testGridSession ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A session is an instance of a browser created through a RemoteWebDriver with the URL from CreateTestGridUrlResult$url. You can use the following to look up sessions: The session ARN (GetTestGridSessionRequest$sessionArn). The project ARN and a session ID (GetTestGridSessionRequest$projectArn and GetTestGridSessionRequest$sessionId)."]
module GetTestGridSessionRequest =
  struct
    type nonrec t =
      {
      projectArn: DeviceFarmArn.t option
        [@ocaml.doc
          "The ARN for the project that this session belongs to. See CreateTestGridProject and ListTestGridProjects."];
      sessionId: ResourceId.t option
        [@ocaml.doc "An ID associated with this session."];
      sessionArn: DeviceFarmArn.t option
        [@ocaml.doc "An ARN that uniquely identifies a TestGridSession."]}
    let make ?projectArn =
      fun ?sessionId ->
        fun ?sessionArn -> fun () -> { projectArn; sessionId; sessionArn }
    let to_value x =
      structure_to_value
        [("projectArn", (Option.map x.projectArn ~f:DeviceFarmArn.to_value));
        ("sessionId", (Option.map x.sessionId ~f:ResourceId.to_value));
        ("sessionArn", (Option.map x.sessionArn ~f:DeviceFarmArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionArn =
        (Option.map ~f:DeviceFarmArn.of_xml)
          (Xml.child xml_arg0 "sessionArn") in
      let sessionId =
        (Option.map ~f:ResourceId.of_xml) (Xml.child xml_arg0 "sessionId") in
      let projectArn =
        (Option.map ~f:DeviceFarmArn.of_xml)
          (Xml.child xml_arg0 "projectArn") in
      make ?sessionArn ?sessionId ?projectArn ()
    let of_json json =
      let sessionArn = field_map json "sessionArn" DeviceFarmArn.of_json in
      let sessionId = field_map json "sessionId" ResourceId.of_json in
      let projectArn = field_map json "projectArn" DeviceFarmArn.of_json in
      make ?sessionArn ?sessionId ?projectArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A session is an instance of a browser created through a RemoteWebDriver with the URL from CreateTestGridUrlResult$url. You can use the following to look up sessions: The session ARN (GetTestGridSessionRequest$sessionArn). The project ARN and a session ID (GetTestGridSessionRequest$projectArn and GetTestGridSessionRequest$sessionId)."]
module GetTestGridProjectResult =
  struct
    type nonrec t =
      {
      testGridProject: TestGridProject.t option
        [@ocaml.doc "A TestGridProject."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `NotFoundException of NotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?testGridProject = fun () -> { testGridProject }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("testGridProject",
           (Option.map x.testGridProject ~f:TestGridProject.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let testGridProject =
        (Option.map ~f:TestGridProject.of_xml)
          (Xml.child xml_arg0 "testGridProject") in
      make ?testGridProject ()
    let of_json json =
      let testGridProject =
        field_map json "testGridProject" TestGridProject.of_json in
      make ?testGridProject ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves information about a Selenium testing project."]
module GetTestGridProjectRequest =
  struct
    type nonrec t =
      {
      projectArn: DeviceFarmArn.t
        [@ocaml.doc
          "The ARN of the Selenium testing project, from either CreateTestGridProject or ListTestGridProjects."]}
    let context_ = "GetTestGridProjectRequest"
    let make ~projectArn = fun () -> { projectArn }
    let to_value x =
      structure_to_value
        [("projectArn", (Some (DeviceFarmArn.to_value x.projectArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let projectArn =
        DeviceFarmArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "projectArn") in
      make ~projectArn ()
    let of_json json =
      let projectArn = field_map_exn json "projectArn" DeviceFarmArn.of_json in
      make ~projectArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves information about a Selenium testing project."]
module GetSuiteResult =
  struct
    type nonrec t =
      {
      suite: Suite.t option [@ocaml.doc "A collection of one or more tests."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?suite = fun () -> { suite }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("suite", (Option.map x.suite ~f:Suite.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let suite = (Option.map ~f:Suite.of_xml) (Xml.child xml_arg0 "suite") in
      make ?suite ()
    let of_json json =
      let suite = field_map json "suite" Suite.of_json in make ?suite ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a get suite request."]
module GetSuiteRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t [@ocaml.doc "The suite's ARN."]}
    let context_ = "GetSuiteRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_json json =
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a request to the get suite operation."]
module GetRunResult =
  struct
    type nonrec t =
      {
      run: Run.t option [@ocaml.doc "The run to get results from."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?run = fun () -> { run }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("run", (Option.map x.run ~f:Run.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let run = (Option.map ~f:Run.of_xml) (Xml.child xml_arg0 "run") in
      make ?run ()
    let of_json json =
      let run = field_map json "run" Run.of_json in make ?run ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a get run request."]
module GetRunRequest =
  struct
    type nonrec t = {
      arn: AmazonResourceName.t [@ocaml.doc "The run's ARN."]}
    let context_ = "GetRunRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_json json =
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a request to the get run operation."]
module GetRemoteAccessSessionResult =
  struct
    type nonrec t =
      {
      remoteAccessSession: RemoteAccessSession.t option
        [@ocaml.doc
          "A container that lists detailed information about the remote access session."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?remoteAccessSession = fun () -> { remoteAccessSession }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("remoteAccessSession",
           (Option.map x.remoteAccessSession ~f:RemoteAccessSession.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let remoteAccessSession =
        (Option.map ~f:RemoteAccessSession.of_xml)
          (Xml.child xml_arg0 "remoteAccessSession") in
      make ?remoteAccessSession ()
    let of_json json =
      let remoteAccessSession =
        field_map json "remoteAccessSession" RemoteAccessSession.of_json in
      make ?remoteAccessSession ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the response from the server that lists detailed information about the remote access session."]
module GetRemoteAccessSessionRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the remote access session about which you want to get session information."]}
    let context_ = "GetRemoteAccessSessionRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_json json =
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the request to get information about the specified remote access session."]
module GetProjectResult =
  struct
    type nonrec t =
      {
      project: Project.t option
        [@ocaml.doc "The project to get information about."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?project = fun () -> { project }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("project", (Option.map x.project ~f:Project.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let project =
        (Option.map ~f:Project.of_xml) (Xml.child xml_arg0 "project") in
      make ?project ()
    let of_json json =
      let project = field_map json "project" Project.of_json in
      make ?project ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a get project request."]
module GetProjectRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t [@ocaml.doc "The project's ARN."]}
    let context_ = "GetProjectRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_json json =
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a request to the get project operation."]
module GetOfferingStatusResult =
  struct
    type nonrec t =
      {
      current: OfferingStatusMap.t option
        [@ocaml.doc
          "When specified, gets the offering status for the current period."];
      nextPeriod: OfferingStatusMap.t option
        [@ocaml.doc
          "When specified, gets the offering status for the next period."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotEligibleException of NotEligibleException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?current =
      fun ?nextPeriod ->
        fun ?nextToken -> fun () -> { current; nextPeriod; nextToken }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotEligibleException" ->
          `NotEligibleException (NotEligibleException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotEligibleException" ->
          `NotEligibleException (NotEligibleException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotEligibleException e ->
          `Assoc
            [("error", (`String "NotEligibleException"));
            ("details", (NotEligibleException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("current", (Option.map x.current ~f:OfferingStatusMap.to_value));
        ("nextPeriod",
          (Option.map x.nextPeriod ~f:OfferingStatusMap.to_value));
        ("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      let nextPeriod =
        (Option.map ~f:OfferingStatusMap.of_xml)
          (Xml.child xml_arg0 "nextPeriod") in
      let current =
        (Option.map ~f:OfferingStatusMap.of_xml)
          (Xml.child xml_arg0 "current") in
      make ?nextToken ?nextPeriod ?current ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      let nextPeriod = field_map json "nextPeriod" OfferingStatusMap.of_json in
      let current = field_map json "current" OfferingStatusMap.of_json in
      make ?nextToken ?nextPeriod ?current ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns the status result for a device offering."]
module GetOfferingStatusRequest =
  struct
    type nonrec t =
      {
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list."]}
    let make ?nextToken = fun () -> { nextToken }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nextToken") in
      make ?nextToken ()
    let of_json json =
      let nextToken = field_map json "nextToken" PaginationToken.of_json in
      make ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the request to retrieve the offering status for the specified customer or account."]
module GetNetworkProfileResult =
  struct
    type nonrec t =
      {
      networkProfile: NetworkProfile.t option
        [@ocaml.doc "The network profile."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?networkProfile = fun () -> { networkProfile }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkProfile",
           (Option.map x.networkProfile ~f:NetworkProfile.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkProfile =
        (Option.map ~f:NetworkProfile.of_xml)
          (Xml.child xml_arg0 "networkProfile") in
      make ?networkProfile ()
    let of_json json =
      let networkProfile =
        field_map json "networkProfile" NetworkProfile.of_json in
      make ?networkProfile ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns information about a network profile."]
module GetNetworkProfileRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc
          "The ARN of the network profile to return information about."]}
    let context_ = "GetNetworkProfileRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_json json =
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns information about a network profile."]
module GetJobResult =
  struct
    type nonrec t =
      {
      job: Job.t option
        [@ocaml.doc
          "An object that contains information about the requested job."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?job = fun () -> { job }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("job", (Option.map x.job ~f:Job.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let job = (Option.map ~f:Job.of_xml) (Xml.child xml_arg0 "job") in
      make ?job ()
    let of_json json =
      let job = field_map json "job" Job.of_json in make ?job ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a get job request."]
module GetJobRequest =
  struct
    type nonrec t = {
      arn: AmazonResourceName.t [@ocaml.doc "The job's ARN."]}
    let context_ = "GetJobRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_json json =
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a request to the get job operation."]
module GetInstanceProfileResult =
  struct
    type nonrec t =
      {
      instanceProfile: InstanceProfile.t option
        [@ocaml.doc
          "An object that contains information about an instance profile."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?instanceProfile = fun () -> { instanceProfile }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceProfile",
           (Option.map x.instanceProfile ~f:InstanceProfile.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceProfile =
        (Option.map ~f:InstanceProfile.of_xml)
          (Xml.child xml_arg0 "instanceProfile") in
      make ?instanceProfile ()
    let of_json json =
      let instanceProfile =
        field_map json "instanceProfile" InstanceProfile.of_json in
      make ?instanceProfile ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about the specified instance profile."]
module GetInstanceProfileRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of an instance profile."]}
    let context_ = "GetInstanceProfileRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_json json =
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about the specified instance profile."]
module GetDeviceResult =
  struct
    type nonrec t =
      {
      device: Device.t option
        [@ocaml.doc
          "An object that contains information about the requested device."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?device = fun () -> { device }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("device", (Option.map x.device ~f:Device.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let device =
        (Option.map ~f:Device.of_xml) (Xml.child xml_arg0 "device") in
      make ?device ()
    let of_json json =
      let device = field_map json "device" Device.of_json in make ?device ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a get device request."]
module GetDeviceRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t [@ocaml.doc "The device type's ARN."]}
    let context_ = "GetDeviceRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_json json =
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a request to the get device request."]
module GetDevicePoolResult =
  struct
    type nonrec t =
      {
      devicePool: DevicePool.t option
        [@ocaml.doc
          "An object that contains information about the requested device pool."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?devicePool = fun () -> { devicePool }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("devicePool", (Option.map x.devicePool ~f:DevicePool.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let devicePool =
        (Option.map ~f:DevicePool.of_xml) (Xml.child xml_arg0 "devicePool") in
      make ?devicePool ()
    let of_json json =
      let devicePool = field_map json "devicePool" DevicePool.of_json in
      make ?devicePool ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a get device pool request."]
module GetDevicePoolRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t [@ocaml.doc "The device pool's ARN."]}
    let context_ = "GetDevicePoolRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_json json =
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a request to the get device pool operation."]
module GetDevicePoolCompatibilityResult =
  struct
    type nonrec t =
      {
      compatibleDevices: DevicePoolCompatibilityResults.t option
        [@ocaml.doc "Information about compatible devices."];
      incompatibleDevices: DevicePoolCompatibilityResults.t option
        [@ocaml.doc "Information about incompatible devices."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?compatibleDevices =
      fun ?incompatibleDevices ->
        fun () -> { compatibleDevices; incompatibleDevices }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("compatibleDevices",
           (Option.map x.compatibleDevices
              ~f:DevicePoolCompatibilityResults.to_value));
        ("incompatibleDevices",
          (Option.map x.incompatibleDevices
             ~f:DevicePoolCompatibilityResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let incompatibleDevices =
        (Option.map ~f:DevicePoolCompatibilityResults.of_xml)
          (Xml.child xml_arg0 "incompatibleDevices") in
      let compatibleDevices =
        (Option.map ~f:DevicePoolCompatibilityResults.of_xml)
          (Xml.child xml_arg0 "compatibleDevices") in
      make ?incompatibleDevices ?compatibleDevices ()
    let of_json json =
      let incompatibleDevices =
        field_map json "incompatibleDevices"
          DevicePoolCompatibilityResults.of_json in
      let compatibleDevices =
        field_map json "compatibleDevices"
          DevicePoolCompatibilityResults.of_json in
      make ?incompatibleDevices ?compatibleDevices ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the result of describe device pool compatibility request."]
module GetDevicePoolCompatibilityRequest =
  struct
    type nonrec t =
      {
      devicePoolArn: AmazonResourceName.t
        [@ocaml.doc "The device pool's ARN."];
      appArn: AmazonResourceName.t option
        [@ocaml.doc
          "The ARN of the app that is associated with the specified device pool."];
      testType: TestType.t option
        [@ocaml.doc
          "The test type for the specified device pool. Allowed values include the following: BUILTIN_FUZZ. BUILTIN_EXPLORER. For Android, an app explorer that traverses an Android app, interacting with it and capturing screenshots at the same time. APPIUM_JAVA_JUNIT. APPIUM_JAVA_TESTNG. APPIUM_PYTHON. APPIUM_NODE. APPIUM_RUBY. APPIUM_WEB_JAVA_JUNIT. APPIUM_WEB_JAVA_TESTNG. APPIUM_WEB_PYTHON. APPIUM_WEB_NODE. APPIUM_WEB_RUBY. CALABASH. INSTRUMENTATION. UIAUTOMATION. UIAUTOMATOR. XCTEST. XCTEST_UI."];
      test: ScheduleRunTest.t option
        [@ocaml.doc
          "Information about the uploaded test to be run against the device pool."];
      configuration: ScheduleRunConfiguration.t option
        [@ocaml.doc
          "An object that contains information about the settings for a run."]}
    let context_ = "GetDevicePoolCompatibilityRequest"
    let make ?appArn =
      fun ?testType ->
        fun ?test ->
          fun ?configuration ->
            fun ~devicePoolArn ->
              fun () ->
                { appArn; testType; test; configuration; devicePoolArn }
    let to_value x =
      structure_to_value
        [("devicePoolArn",
           (Some (AmazonResourceName.to_value x.devicePoolArn)));
        ("appArn", (Option.map x.appArn ~f:AmazonResourceName.to_value));
        ("testType", (Option.map x.testType ~f:TestType.to_value));
        ("test", (Option.map x.test ~f:ScheduleRunTest.to_value));
        ("configuration",
          (Option.map x.configuration ~f:ScheduleRunConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configuration =
        (Option.map ~f:ScheduleRunConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      let test =
        (Option.map ~f:ScheduleRunTest.of_xml) (Xml.child xml_arg0 "test") in
      let testType =
        (Option.map ~f:TestType.of_xml) (Xml.child xml_arg0 "testType") in
      let appArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "appArn") in
      let devicePoolArn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "devicePoolArn") in
      make ?configuration ?test ?testType ?appArn ~devicePoolArn ()
    let of_json json =
      let configuration =
        field_map json "configuration" ScheduleRunConfiguration.of_json in
      let test = field_map json "test" ScheduleRunTest.of_json in
      let testType = field_map json "testType" TestType.of_json in
      let appArn = field_map json "appArn" AmazonResourceName.of_json in
      let devicePoolArn =
        field_map_exn json "devicePoolArn" AmazonResourceName.of_json in
      make ?configuration ?test ?testType ?appArn ~devicePoolArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to the get device pool compatibility operation."]
module GetDeviceInstanceResult =
  struct
    type nonrec t =
      {
      deviceInstance: DeviceInstance.t option
        [@ocaml.doc
          "An object that contains information about your device instance."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?deviceInstance = fun () -> { deviceInstance }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("deviceInstance",
           (Option.map x.deviceInstance ~f:DeviceInstance.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deviceInstance =
        (Option.map ~f:DeviceInstance.of_xml)
          (Xml.child xml_arg0 "deviceInstance") in
      make ?deviceInstance ()
    let of_json json =
      let deviceInstance =
        field_map json "deviceInstance" DeviceInstance.of_json in
      make ?deviceInstance ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about a device instance that belongs to a private device fleet."]
module GetDeviceInstanceRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the instance you're requesting information about."]}
    let context_ = "GetDeviceInstanceRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_json json =
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about a device instance that belongs to a private device fleet."]
module GetAccountSettingsResult =
  struct
    type nonrec t =
      {
      accountSettings: AccountSettings.t option
        [@ocaml.doc "The account settings."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?accountSettings = fun () -> { accountSettings }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("accountSettings",
           (Option.map x.accountSettings ~f:AccountSettings.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let accountSettings =
        (Option.map ~f:AccountSettings.of_xml)
          (Xml.child xml_arg0 "accountSettings") in
      make ?accountSettings ()
    let of_json json =
      let accountSettings =
        field_map json "accountSettings" AccountSettings.of_json in
      make ?accountSettings ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the account settings return values from the GetAccountSettings request."]
module GetAccountSettingsRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the request sent to retrieve the account settings."]
module DeleteVPCEConfigurationResult =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `InvalidOperationException of InvalidOperationException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `InvalidOperationException e ->
          `Assoc
            [("error", (`String "InvalidOperationException"));
            ("details", (InvalidOperationException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a configuration for your Amazon Virtual Private Cloud (VPC) endpoint."]
module DeleteVPCEConfigurationRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the VPC endpoint configuration you want to delete."]}
    let context_ = "DeleteVPCEConfigurationRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_json json =
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a configuration for your Amazon Virtual Private Cloud (VPC) endpoint."]
module DeleteUploadResult =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a delete upload request."]
module DeleteUploadRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc
          "Represents the Amazon Resource Name (ARN) of the Device Farm upload to delete."]}
    let context_ = "DeleteUploadRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_json json =
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a request to the delete upload operation."]
module DeleteTestGridProjectResult =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `CannotDeleteException of CannotDeleteException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `NotFoundException of NotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "CannotDeleteException" ->
          `CannotDeleteException (CannotDeleteException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "CannotDeleteException" ->
          `CannotDeleteException (CannotDeleteException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `CannotDeleteException e ->
          `Assoc
            [("error", (`String "CannotDeleteException"));
            ("details", (CannotDeleteException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a Selenium testing project and all content generated under it. You cannot undo this operation. You cannot delete a project if it has active sessions."]
module DeleteTestGridProjectRequest =
  struct
    type nonrec t =
      {
      projectArn: DeviceFarmArn.t
        [@ocaml.doc
          "The ARN of the project to delete, from CreateTestGridProject or ListTestGridProjects."]}
    let context_ = "DeleteTestGridProjectRequest"
    let make ~projectArn = fun () -> { projectArn }
    let to_value x =
      structure_to_value
        [("projectArn", (Some (DeviceFarmArn.to_value x.projectArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let projectArn =
        DeviceFarmArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "projectArn") in
      make ~projectArn ()
    let of_json json =
      let projectArn = field_map_exn json "projectArn" DeviceFarmArn.of_json in
      make ~projectArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a Selenium testing project and all content generated under it. You cannot undo this operation. You cannot delete a project if it has active sessions."]
module DeleteRunResult =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a delete run request."]
module DeleteRunRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc "The Amazon Resource Name (ARN) for the run to delete."]}
    let context_ = "DeleteRunRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_json json =
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a request to the delete run operation."]
module DeleteRemoteAccessSessionResult =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The response from the server when a request is made to delete the remote access session."]
module DeleteRemoteAccessSessionRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the session for which you want to delete remote access."]}
    let context_ = "DeleteRemoteAccessSessionRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_json json =
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the request to delete the specified remote access session."]
module DeleteProjectResult =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a delete project request."]
module DeleteProjectRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc
          "Represents the Amazon Resource Name (ARN) of the Device Farm project to delete."]}
    let context_ = "DeleteProjectRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_json json =
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a request to the delete project operation."]
module DeleteNetworkProfileResult =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a network profile."]
module DeleteNetworkProfileRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc "The ARN of the network profile to delete."]}
    let context_ = "DeleteNetworkProfileRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_json json =
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a network profile."]
module DeleteInstanceProfileResult =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a profile that can be applied to one or more private device instances."]
module DeleteInstanceProfileRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the instance profile you are requesting to delete."]}
    let context_ = "DeleteInstanceProfileRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_json json =
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a profile that can be applied to one or more private device instances."]
module DeleteDevicePoolResult =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a delete device pool request."]
module DeleteDevicePoolRequest =
  struct
    type nonrec t =
      {
      arn: AmazonResourceName.t
        [@ocaml.doc
          "Represents the Amazon Resource Name (ARN) of the Device Farm device pool to delete."]}
    let context_ = "DeleteDevicePoolRequest"
    let make ~arn = fun () -> { arn }
    let to_value x =
      structure_to_value
        [("arn", (Some (AmazonResourceName.to_value x.arn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "arn") in
      make ~arn ()
    let of_json json =
      let arn = field_map_exn json "arn" AmazonResourceName.of_json in
      make ~arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to the delete device pool operation."]
module CreateVPCEConfigurationResult =
  struct
    type nonrec t =
      {
      vpceConfiguration: VPCEConfiguration.t option
        [@ocaml.doc
          "An object that contains information about your VPC endpoint configuration."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?vpceConfiguration = fun () -> { vpceConfiguration }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vpceConfiguration",
           (Option.map x.vpceConfiguration ~f:VPCEConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpceConfiguration =
        (Option.map ~f:VPCEConfiguration.of_xml)
          (Xml.child xml_arg0 "vpceConfiguration") in
      make ?vpceConfiguration ()
    let of_json json =
      let vpceConfiguration =
        field_map json "vpceConfiguration" VPCEConfiguration.of_json in
      make ?vpceConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a configuration record in Device Farm for your Amazon Virtual Private Cloud (VPC) endpoint."]
module CreateVPCEConfigurationRequest =
  struct
    type nonrec t =
      {
      vpceConfigurationName: VPCEConfigurationName.t
        [@ocaml.doc
          "The friendly name you give to your VPC endpoint configuration, to manage your configurations more easily."];
      vpceServiceName: VPCEServiceName.t
        [@ocaml.doc
          "The name of the VPC endpoint service running in your AWS account that you want Device Farm to test."];
      serviceDnsName: ServiceDnsName.t
        [@ocaml.doc
          "The DNS name of the service running in your VPC that you want Device Farm to test."];
      vpceConfigurationDescription: VPCEConfigurationDescription.t option
        [@ocaml.doc
          "An optional description that provides details about your VPC endpoint configuration."]}
    let context_ = "CreateVPCEConfigurationRequest"
    let make ?vpceConfigurationDescription =
      fun ~vpceConfigurationName ->
        fun ~vpceServiceName ->
          fun ~serviceDnsName ->
            fun () ->
              {
                vpceConfigurationDescription;
                vpceConfigurationName;
                vpceServiceName;
                serviceDnsName
              }
    let to_value x =
      structure_to_value
        [("vpceConfigurationName",
           (Some (VPCEConfigurationName.to_value x.vpceConfigurationName)));
        ("vpceServiceName",
          (Some (VPCEServiceName.to_value x.vpceServiceName)));
        ("serviceDnsName", (Some (ServiceDnsName.to_value x.serviceDnsName)));
        ("vpceConfigurationDescription",
          (Option.map x.vpceConfigurationDescription
             ~f:VPCEConfigurationDescription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpceConfigurationDescription =
        (Option.map ~f:VPCEConfigurationDescription.of_xml)
          (Xml.child xml_arg0 "vpceConfigurationDescription") in
      let serviceDnsName =
        ServiceDnsName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "serviceDnsName") in
      let vpceServiceName =
        VPCEServiceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "vpceServiceName") in
      let vpceConfigurationName =
        VPCEConfigurationName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "vpceConfigurationName") in
      make ?vpceConfigurationDescription ~serviceDnsName ~vpceServiceName
        ~vpceConfigurationName ()
    let of_json json =
      let vpceConfigurationDescription =
        field_map json "vpceConfigurationDescription"
          VPCEConfigurationDescription.of_json in
      let serviceDnsName =
        field_map_exn json "serviceDnsName" ServiceDnsName.of_json in
      let vpceServiceName =
        field_map_exn json "vpceServiceName" VPCEServiceName.of_json in
      let vpceConfigurationName =
        field_map_exn json "vpceConfigurationName"
          VPCEConfigurationName.of_json in
      make ?vpceConfigurationDescription ~serviceDnsName ~vpceServiceName
        ~vpceConfigurationName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a configuration record in Device Farm for your Amazon Virtual Private Cloud (VPC) endpoint."]
module CreateUploadResult =
  struct
    type nonrec t =
      {
      upload: Upload.t option [@ocaml.doc "The newly created upload."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?upload = fun () -> { upload }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("upload", (Option.map x.upload ~f:Upload.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let upload =
        (Option.map ~f:Upload.of_xml) (Xml.child xml_arg0 "upload") in
      make ?upload ()
    let of_json json =
      let upload = field_map json "upload" Upload.of_json in make ?upload ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a create upload request."]
module CreateUploadRequest =
  struct
    type nonrec t =
      {
      projectArn: AmazonResourceName.t
        [@ocaml.doc "The ARN of the project for the upload."];
      name: Name.t
        [@ocaml.doc
          "The upload's file name. The name should not contain any forward slashes (/). If you are uploading an iOS app, the file name must end with the .ipa extension. If you are uploading an Android app, the file name must end with the .apk extension. For all others, the file name must end with the .zip file extension."];
      type_: UploadType.t
        [@ocaml.doc
          "The upload's upload type. Must be one of the following values: ANDROID_APP IOS_APP WEB_APP EXTERNAL_DATA APPIUM_JAVA_JUNIT_TEST_PACKAGE APPIUM_JAVA_TESTNG_TEST_PACKAGE APPIUM_PYTHON_TEST_PACKAGE APPIUM_NODE_TEST_PACKAGE APPIUM_RUBY_TEST_PACKAGE APPIUM_WEB_JAVA_JUNIT_TEST_PACKAGE APPIUM_WEB_JAVA_TESTNG_TEST_PACKAGE APPIUM_WEB_PYTHON_TEST_PACKAGE APPIUM_WEB_NODE_TEST_PACKAGE APPIUM_WEB_RUBY_TEST_PACKAGE CALABASH_TEST_PACKAGE INSTRUMENTATION_TEST_PACKAGE UIAUTOMATION_TEST_PACKAGE UIAUTOMATOR_TEST_PACKAGE XCTEST_TEST_PACKAGE XCTEST_UI_TEST_PACKAGE APPIUM_JAVA_JUNIT_TEST_SPEC APPIUM_JAVA_TESTNG_TEST_SPEC APPIUM_PYTHON_TEST_SPEC APPIUM_NODE_TEST_SPEC APPIUM_RUBY_TEST_SPEC APPIUM_WEB_JAVA_JUNIT_TEST_SPEC APPIUM_WEB_JAVA_TESTNG_TEST_SPEC APPIUM_WEB_PYTHON_TEST_SPEC APPIUM_WEB_NODE_TEST_SPEC APPIUM_WEB_RUBY_TEST_SPEC INSTRUMENTATION_TEST_SPEC XCTEST_UI_TEST_SPEC If you call CreateUpload with WEB_APP specified, AWS Device Farm throws an ArgumentException error."];
      contentType: ContentType.t option
        [@ocaml.doc
          "The upload's content type (for example, application/octet-stream)."]}
    let context_ = "CreateUploadRequest"
    let make ?contentType =
      fun ~projectArn ->
        fun ~name ->
          fun ~type_ -> fun () -> { contentType; projectArn; name; type_ }
    let to_value x =
      structure_to_value
        [("projectArn", (Some (AmazonResourceName.to_value x.projectArn)));
        ("name", (Some (Name.to_value x.name)));
        ("type", (Some (UploadType.to_value x.type_)));
        ("contentType", (Option.map x.contentType ~f:ContentType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let contentType =
        (Option.map ~f:ContentType.of_xml) (Xml.child xml_arg0 "contentType") in
      let type_ =
        UploadType.of_xml (Xml.child_exn ~context:context_ xml_arg0 "type") in
      let name =
        Name.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      let projectArn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "projectArn") in
      make ?contentType ~type_ ~name ~projectArn ()
    let of_json json =
      let contentType = field_map json "contentType" ContentType.of_json in
      let type_ = field_map_exn json "type" UploadType.of_json in
      let name = field_map_exn json "name" Name.of_json in
      let projectArn =
        field_map_exn json "projectArn" AmazonResourceName.of_json in
      make ?contentType ~type_ ~name ~projectArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a request to the create upload operation."]
module CreateTestGridUrlResult =
  struct
    type nonrec t =
      {
      url: SensitiveString.t option
        [@ocaml.doc
          "A signed URL, expiring in CreateTestGridUrlRequest$expiresInSeconds seconds, to be passed to a RemoteWebDriver."];
      expires: DateTime.t option
        [@ocaml.doc
          "The number of seconds the URL from CreateTestGridUrlResult$url stays active."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `NotFoundException of NotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?url = fun ?expires -> fun () -> { url; expires }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("url", (Option.map x.url ~f:SensitiveString.to_value));
        ("expires", (Option.map x.expires ~f:DateTime.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let expires =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "expires") in
      let url =
        (Option.map ~f:SensitiveString.of_xml) (Xml.child xml_arg0 "url") in
      make ?expires ?url ()
    let of_json json =
      let expires = field_map json "expires" DateTime.of_json in
      let url = field_map json "url" SensitiveString.of_json in
      make ?expires ?url ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a signed, short-term URL that can be passed to a Selenium RemoteWebDriver constructor."]
module CreateTestGridUrlRequest =
  struct
    type nonrec t =
      {
      projectArn: DeviceFarmArn.t
        [@ocaml.doc
          "ARN (from CreateTestGridProject or ListTestGridProjects) to associate with the short-term URL."];
      expiresInSeconds: TestGridUrlExpiresInSecondsInput.t
        [@ocaml.doc "Lifetime, in seconds, of the URL."]}
    let context_ = "CreateTestGridUrlRequest"
    let make ~projectArn =
      fun ~expiresInSeconds -> fun () -> { projectArn; expiresInSeconds }
    let to_value x =
      structure_to_value
        [("projectArn", (Some (DeviceFarmArn.to_value x.projectArn)));
        ("expiresInSeconds",
          (Some
             (TestGridUrlExpiresInSecondsInput.to_value x.expiresInSeconds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let expiresInSeconds =
        TestGridUrlExpiresInSecondsInput.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "expiresInSeconds") in
      let projectArn =
        DeviceFarmArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "projectArn") in
      make ~expiresInSeconds ~projectArn ()
    let of_json json =
      let expiresInSeconds =
        field_map_exn json "expiresInSeconds"
          TestGridUrlExpiresInSecondsInput.of_json in
      let projectArn = field_map_exn json "projectArn" DeviceFarmArn.of_json in
      make ~expiresInSeconds ~projectArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a signed, short-term URL that can be passed to a Selenium RemoteWebDriver constructor."]
module CreateTestGridProjectResult =
  struct
    type nonrec t =
      {
      testGridProject: TestGridProject.t option
        [@ocaml.doc "ARN of the Selenium testing project that was created."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `InternalServiceException of InternalServiceException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?testGridProject = fun () -> { testGridProject }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "InternalServiceException" ->
          `InternalServiceException (InternalServiceException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `InternalServiceException e ->
          `Assoc
            [("error", (`String "InternalServiceException"));
            ("details", (InternalServiceException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("testGridProject",
           (Option.map x.testGridProject ~f:TestGridProject.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let testGridProject =
        (Option.map ~f:TestGridProject.of_xml)
          (Xml.child xml_arg0 "testGridProject") in
      make ?testGridProject ()
    let of_json json =
      let testGridProject =
        field_map json "testGridProject" TestGridProject.of_json in
      make ?testGridProject ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a Selenium testing project. Projects are used to track TestGridSession instances."]
module CreateTestGridProjectRequest =
  struct
    type nonrec t =
      {
      name: ResourceName.t
        [@ocaml.doc "Human-readable name of the Selenium testing project."];
      description: ResourceDescription.t option
        [@ocaml.doc "Human-readable description of the project."];
      vpcConfig: TestGridVpcConfig.t option
        [@ocaml.doc
          "The VPC security groups and subnets that are attached to a project."]}
    let context_ = "CreateTestGridProjectRequest"
    let make ?description =
      fun ?vpcConfig ->
        fun ~name -> fun () -> { description; vpcConfig; name }
    let to_value x =
      structure_to_value
        [("name", (Some (ResourceName.to_value x.name)));
        ("description",
          (Option.map x.description ~f:ResourceDescription.to_value));
        ("vpcConfig", (Option.map x.vpcConfig ~f:TestGridVpcConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vpcConfig =
        (Option.map ~f:TestGridVpcConfig.of_xml)
          (Xml.child xml_arg0 "vpcConfig") in
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        ResourceName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?vpcConfig ?description ~name ()
    let of_json json =
      let vpcConfig = field_map json "vpcConfig" TestGridVpcConfig.of_json in
      let description =
        field_map json "description" ResourceDescription.of_json in
      let name = field_map_exn json "name" ResourceName.of_json in
      make ?vpcConfig ?description ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a Selenium testing project. Projects are used to track TestGridSession instances."]
module CreateRemoteAccessSessionResult =
  struct
    type nonrec t =
      {
      remoteAccessSession: RemoteAccessSession.t option
        [@ocaml.doc
          "A container that describes the remote access session when the request to create a remote access session is sent."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?remoteAccessSession = fun () -> { remoteAccessSession }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("remoteAccessSession",
           (Option.map x.remoteAccessSession ~f:RemoteAccessSession.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let remoteAccessSession =
        (Option.map ~f:RemoteAccessSession.of_xml)
          (Xml.child xml_arg0 "remoteAccessSession") in
      make ?remoteAccessSession ()
    let of_json json =
      let remoteAccessSession =
        field_map json "remoteAccessSession" RemoteAccessSession.of_json in
      make ?remoteAccessSession ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents the server response from a request to create a remote access session."]
module CreateRemoteAccessSessionRequest =
  struct
    type nonrec t =
      {
      projectArn: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the project for which you want to create a remote access session."];
      deviceArn: AmazonResourceName.t
        [@ocaml.doc
          "The ARN of the device for which you want to create a remote access session."];
      instanceArn: AmazonResourceName.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the device instance for which you want to create a remote access session."];
      sshPublicKey: SshPublicKey.t option
        [@ocaml.doc
          "Ignored. The public key of the ssh key pair you want to use for connecting to remote devices in your remote debugging session. This key is required only if remoteDebugEnabled is set to true. Remote debugging is no longer supported."];
      remoteDebugEnabled: Boolean.t option
        [@ocaml.doc
          "Set to true if you want to access devices remotely for debugging in your remote access session. Remote debugging is no longer supported."];
      remoteRecordEnabled: Boolean.t option
        [@ocaml.doc
          "Set to true to enable remote recording for the remote access session."];
      remoteRecordAppArn: AmazonResourceName.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) for the app to be recorded in the remote access session."];
      name: Name.t option
        [@ocaml.doc "The name of the remote access session to create."];
      clientId: ClientId.t option
        [@ocaml.doc
          "Unique identifier for the client. If you want access to multiple devices on the same client, you should pass the same clientId value in each call to CreateRemoteAccessSession. This identifier is required only if remoteDebugEnabled is set to true. Remote debugging is no longer supported."];
      configuration: CreateRemoteAccessSessionConfiguration.t option
        [@ocaml.doc
          "The configuration information for the remote access session request."];
      interactionMode: InteractionMode.t option
        [@ocaml.doc
          "The interaction mode of the remote access session. Valid values are: INTERACTIVE: You can interact with the iOS device by viewing, touching, and rotating the screen. You cannot run XCUITest framework-based tests in this mode. NO_VIDEO: You are connected to the device, but cannot interact with it or view the screen. This mode has the fastest test execution speed. You can run XCUITest framework-based tests in this mode. VIDEO_ONLY: You can view the screen, but cannot touch or rotate it. You can run XCUITest framework-based tests and watch the screen in this mode."];
      skipAppResign: Boolean.t option
        [@ocaml.doc
          "When set to true, for private devices, Device Farm does not sign your app again. For public devices, Device Farm always signs your apps again. For more information on how Device Farm modifies your uploads during tests, see Do you modify my app?"]}
    let context_ = "CreateRemoteAccessSessionRequest"
    let make ?instanceArn =
      fun ?sshPublicKey ->
        fun ?remoteDebugEnabled ->
          fun ?remoteRecordEnabled ->
            fun ?remoteRecordAppArn ->
              fun ?name ->
                fun ?clientId ->
                  fun ?configuration ->
                    fun ?interactionMode ->
                      fun ?skipAppResign ->
                        fun ~projectArn ->
                          fun ~deviceArn ->
                            fun () ->
                              {
                                instanceArn;
                                sshPublicKey;
                                remoteDebugEnabled;
                                remoteRecordEnabled;
                                remoteRecordAppArn;
                                name;
                                clientId;
                                configuration;
                                interactionMode;
                                skipAppResign;
                                projectArn;
                                deviceArn
                              }
    let to_value x =
      structure_to_value
        [("projectArn", (Some (AmazonResourceName.to_value x.projectArn)));
        ("deviceArn", (Some (AmazonResourceName.to_value x.deviceArn)));
        ("instanceArn",
          (Option.map x.instanceArn ~f:AmazonResourceName.to_value));
        ("sshPublicKey",
          (Option.map x.sshPublicKey ~f:SshPublicKey.to_value));
        ("remoteDebugEnabled",
          (Option.map x.remoteDebugEnabled ~f:Boolean.to_value));
        ("remoteRecordEnabled",
          (Option.map x.remoteRecordEnabled ~f:Boolean.to_value));
        ("remoteRecordAppArn",
          (Option.map x.remoteRecordAppArn ~f:AmazonResourceName.to_value));
        ("name", (Option.map x.name ~f:Name.to_value));
        ("clientId", (Option.map x.clientId ~f:ClientId.to_value));
        ("configuration",
          (Option.map x.configuration
             ~f:CreateRemoteAccessSessionConfiguration.to_value));
        ("interactionMode",
          (Option.map x.interactionMode ~f:InteractionMode.to_value));
        ("skipAppResign", (Option.map x.skipAppResign ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let skipAppResign =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "skipAppResign") in
      let interactionMode =
        (Option.map ~f:InteractionMode.of_xml)
          (Xml.child xml_arg0 "interactionMode") in
      let configuration =
        (Option.map ~f:CreateRemoteAccessSessionConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      let clientId =
        (Option.map ~f:ClientId.of_xml) (Xml.child xml_arg0 "clientId") in
      let name = (Option.map ~f:Name.of_xml) (Xml.child xml_arg0 "name") in
      let remoteRecordAppArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "remoteRecordAppArn") in
      let remoteRecordEnabled =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "remoteRecordEnabled") in
      let remoteDebugEnabled =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "remoteDebugEnabled") in
      let sshPublicKey =
        (Option.map ~f:SshPublicKey.of_xml)
          (Xml.child xml_arg0 "sshPublicKey") in
      let instanceArn =
        (Option.map ~f:AmazonResourceName.of_xml)
          (Xml.child xml_arg0 "instanceArn") in
      let deviceArn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "deviceArn") in
      let projectArn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "projectArn") in
      make ?skipAppResign ?interactionMode ?configuration ?clientId ?name
        ?remoteRecordAppArn ?remoteRecordEnabled ?remoteDebugEnabled
        ?sshPublicKey ?instanceArn ~deviceArn ~projectArn ()
    let of_json json =
      let skipAppResign = field_map json "skipAppResign" Boolean.of_json in
      let interactionMode =
        field_map json "interactionMode" InteractionMode.of_json in
      let configuration =
        field_map json "configuration"
          CreateRemoteAccessSessionConfiguration.of_json in
      let clientId = field_map json "clientId" ClientId.of_json in
      let name = field_map json "name" Name.of_json in
      let remoteRecordAppArn =
        field_map json "remoteRecordAppArn" AmazonResourceName.of_json in
      let remoteRecordEnabled =
        field_map json "remoteRecordEnabled" Boolean.of_json in
      let remoteDebugEnabled =
        field_map json "remoteDebugEnabled" Boolean.of_json in
      let sshPublicKey = field_map json "sshPublicKey" SshPublicKey.of_json in
      let instanceArn =
        field_map json "instanceArn" AmazonResourceName.of_json in
      let deviceArn =
        field_map_exn json "deviceArn" AmazonResourceName.of_json in
      let projectArn =
        field_map_exn json "projectArn" AmazonResourceName.of_json in
      make ?skipAppResign ?interactionMode ?configuration ?clientId ?name
        ?remoteRecordAppArn ?remoteRecordEnabled ?remoteDebugEnabled
        ?sshPublicKey ?instanceArn ~deviceArn ~projectArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates and submits a request to start a remote access session."]
module CreateProjectResult =
  struct
    type nonrec t =
      {
      project: Project.t option [@ocaml.doc "The newly created project."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `TagOperationException of TagOperationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?project = fun () -> { project }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | "TagOperationException" ->
          `TagOperationException (TagOperationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | "TagOperationException" ->
          `TagOperationException (TagOperationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `TagOperationException e ->
          `Assoc
            [("error", (`String "TagOperationException"));
            ("details", (TagOperationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("project", (Option.map x.project ~f:Project.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let project =
        (Option.map ~f:Project.of_xml) (Xml.child xml_arg0 "project") in
      make ?project ()
    let of_json json =
      let project = field_map json "project" Project.of_json in
      make ?project ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a create project request."]
module CreateProjectRequest =
  struct
    type nonrec t =
      {
      name: Name.t [@ocaml.doc "The project's name."];
      defaultJobTimeoutMinutes: JobTimeoutMinutes.t option
        [@ocaml.doc
          "Sets the execution timeout value (in minutes) for a project. All test runs in this project use the specified execution timeout value unless overridden when scheduling a run."]}
    let context_ = "CreateProjectRequest"
    let make ?defaultJobTimeoutMinutes =
      fun ~name -> fun () -> { defaultJobTimeoutMinutes; name }
    let to_value x =
      structure_to_value
        [("name", (Some (Name.to_value x.name)));
        ("defaultJobTimeoutMinutes",
          (Option.map x.defaultJobTimeoutMinutes
             ~f:JobTimeoutMinutes.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let defaultJobTimeoutMinutes =
        (Option.map ~f:JobTimeoutMinutes.of_xml)
          (Xml.child xml_arg0 "defaultJobTimeoutMinutes") in
      let name =
        Name.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?defaultJobTimeoutMinutes ~name ()
    let of_json json =
      let defaultJobTimeoutMinutes =
        field_map json "defaultJobTimeoutMinutes" JobTimeoutMinutes.of_json in
      let name = field_map_exn json "name" Name.of_json in
      make ?defaultJobTimeoutMinutes ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents a request to the create project operation."]
module CreateNetworkProfileResult =
  struct
    type nonrec t =
      {
      networkProfile: NetworkProfile.t option
        [@ocaml.doc
          "The network profile that is returned by the create network profile request."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?networkProfile = fun () -> { networkProfile }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("networkProfile",
           (Option.map x.networkProfile ~f:NetworkProfile.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let networkProfile =
        (Option.map ~f:NetworkProfile.of_xml)
          (Xml.child xml_arg0 "networkProfile") in
      make ?networkProfile ()
    let of_json json =
      let networkProfile =
        field_map json "networkProfile" NetworkProfile.of_json in
      make ?networkProfile ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Creates a network profile."]
module CreateNetworkProfileRequest =
  struct
    type nonrec t =
      {
      projectArn: AmazonResourceName.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the project for which you want to create a network profile."];
      name: Name.t [@ocaml.doc "The name for the new network profile."];
      description: Message.t option
        [@ocaml.doc "The description of the network profile."];
      type_: NetworkProfileType.t option
        [@ocaml.doc
          "The type of network profile to create. Valid values are listed here."];
      uplinkBandwidthBits: Long.t option
        [@ocaml.doc
          "The data throughput rate in bits per second, as an integer from 0 to 104857600."];
      downlinkBandwidthBits: Long.t option
        [@ocaml.doc
          "The data throughput rate in bits per second, as an integer from 0 to 104857600."];
      uplinkDelayMs: Long.t option
        [@ocaml.doc
          "Delay time for all packets to destination in milliseconds as an integer from 0 to 2000."];
      downlinkDelayMs: Long.t option
        [@ocaml.doc
          "Delay time for all packets to destination in milliseconds as an integer from 0 to 2000."];
      uplinkJitterMs: Long.t option
        [@ocaml.doc
          "Time variation in the delay of received packets in milliseconds as an integer from 0 to 2000."];
      downlinkJitterMs: Long.t option
        [@ocaml.doc
          "Time variation in the delay of received packets in milliseconds as an integer from 0 to 2000."];
      uplinkLossPercent: PercentInteger.t option
        [@ocaml.doc
          "Proportion of transmitted packets that fail to arrive from 0 to 100 percent."];
      downlinkLossPercent: PercentInteger.t option
        [@ocaml.doc
          "Proportion of received packets that fail to arrive from 0 to 100 percent."]}
    let context_ = "CreateNetworkProfileRequest"
    let make ?description =
      fun ?type_ ->
        fun ?uplinkBandwidthBits ->
          fun ?downlinkBandwidthBits ->
            fun ?uplinkDelayMs ->
              fun ?downlinkDelayMs ->
                fun ?uplinkJitterMs ->
                  fun ?downlinkJitterMs ->
                    fun ?uplinkLossPercent ->
                      fun ?downlinkLossPercent ->
                        fun ~projectArn ->
                          fun ~name ->
                            fun () ->
                              {
                                description;
                                type_;
                                uplinkBandwidthBits;
                                downlinkBandwidthBits;
                                uplinkDelayMs;
                                downlinkDelayMs;
                                uplinkJitterMs;
                                downlinkJitterMs;
                                uplinkLossPercent;
                                downlinkLossPercent;
                                projectArn;
                                name
                              }
    let to_value x =
      structure_to_value
        [("projectArn", (Some (AmazonResourceName.to_value x.projectArn)));
        ("name", (Some (Name.to_value x.name)));
        ("description", (Option.map x.description ~f:Message.to_value));
        ("type", (Option.map x.type_ ~f:NetworkProfileType.to_value));
        ("uplinkBandwidthBits",
          (Option.map x.uplinkBandwidthBits ~f:Long.to_value));
        ("downlinkBandwidthBits",
          (Option.map x.downlinkBandwidthBits ~f:Long.to_value));
        ("uplinkDelayMs", (Option.map x.uplinkDelayMs ~f:Long.to_value));
        ("downlinkDelayMs", (Option.map x.downlinkDelayMs ~f:Long.to_value));
        ("uplinkJitterMs", (Option.map x.uplinkJitterMs ~f:Long.to_value));
        ("downlinkJitterMs",
          (Option.map x.downlinkJitterMs ~f:Long.to_value));
        ("uplinkLossPercent",
          (Option.map x.uplinkLossPercent ~f:PercentInteger.to_value));
        ("downlinkLossPercent",
          (Option.map x.downlinkLossPercent ~f:PercentInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let downlinkLossPercent =
        (Option.map ~f:PercentInteger.of_xml)
          (Xml.child xml_arg0 "downlinkLossPercent") in
      let uplinkLossPercent =
        (Option.map ~f:PercentInteger.of_xml)
          (Xml.child xml_arg0 "uplinkLossPercent") in
      let downlinkJitterMs =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "downlinkJitterMs") in
      let uplinkJitterMs =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "uplinkJitterMs") in
      let downlinkDelayMs =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "downlinkDelayMs") in
      let uplinkDelayMs =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "uplinkDelayMs") in
      let downlinkBandwidthBits =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "downlinkBandwidthBits") in
      let uplinkBandwidthBits =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "uplinkBandwidthBits") in
      let type_ =
        (Option.map ~f:NetworkProfileType.of_xml) (Xml.child xml_arg0 "type") in
      let description =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "description") in
      let name =
        Name.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      let projectArn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "projectArn") in
      make ?downlinkLossPercent ?uplinkLossPercent ?downlinkJitterMs
        ?uplinkJitterMs ?downlinkDelayMs ?uplinkDelayMs
        ?downlinkBandwidthBits ?uplinkBandwidthBits ?type_ ?description ~name
        ~projectArn ()
    let of_json json =
      let downlinkLossPercent =
        field_map json "downlinkLossPercent" PercentInteger.of_json in
      let uplinkLossPercent =
        field_map json "uplinkLossPercent" PercentInteger.of_json in
      let downlinkJitterMs = field_map json "downlinkJitterMs" Long.of_json in
      let uplinkJitterMs = field_map json "uplinkJitterMs" Long.of_json in
      let downlinkDelayMs = field_map json "downlinkDelayMs" Long.of_json in
      let uplinkDelayMs = field_map json "uplinkDelayMs" Long.of_json in
      let downlinkBandwidthBits =
        field_map json "downlinkBandwidthBits" Long.of_json in
      let uplinkBandwidthBits =
        field_map json "uplinkBandwidthBits" Long.of_json in
      let type_ = field_map json "type" NetworkProfileType.of_json in
      let description = field_map json "description" Message.of_json in
      let name = field_map_exn json "name" Name.of_json in
      let projectArn =
        field_map_exn json "projectArn" AmazonResourceName.of_json in
      make ?downlinkLossPercent ?uplinkLossPercent ?downlinkJitterMs
        ?uplinkJitterMs ?downlinkDelayMs ?uplinkDelayMs
        ?downlinkBandwidthBits ?uplinkBandwidthBits ?type_ ?description ~name
        ~projectArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Creates a network profile."]
module CreateInstanceProfileResult =
  struct
    type nonrec t =
      {
      instanceProfile: InstanceProfile.t option
        [@ocaml.doc
          "An object that contains information about your instance profile."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?instanceProfile = fun () -> { instanceProfile }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instanceProfile",
           (Option.map x.instanceProfile ~f:InstanceProfile.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceProfile =
        (Option.map ~f:InstanceProfile.of_xml)
          (Xml.child xml_arg0 "instanceProfile") in
      make ?instanceProfile ()
    let of_json json =
      let instanceProfile =
        field_map json "instanceProfile" InstanceProfile.of_json in
      make ?instanceProfile ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a profile that can be applied to one or more private fleet device instances."]
module CreateInstanceProfileRequest =
  struct
    type nonrec t =
      {
      name: Name.t [@ocaml.doc "The name of your instance profile."];
      description: Message.t option
        [@ocaml.doc "The description of your instance profile."];
      packageCleanup: Boolean.t option
        [@ocaml.doc
          "When set to true, Device Farm removes app packages after a test run. The default value is false for private devices."];
      excludeAppPackagesFromCleanup: PackageIds.t option
        [@ocaml.doc
          "An array of strings that specifies the list of app packages that should not be cleaned up from the device after a test run. The list of packages is considered only if you set packageCleanup to true."];
      rebootAfterUse: Boolean.t option
        [@ocaml.doc
          "When set to true, Device Farm reboots the instance after a test run. The default value is true."]}
    let context_ = "CreateInstanceProfileRequest"
    let make ?description =
      fun ?packageCleanup ->
        fun ?excludeAppPackagesFromCleanup ->
          fun ?rebootAfterUse ->
            fun ~name ->
              fun () ->
                {
                  description;
                  packageCleanup;
                  excludeAppPackagesFromCleanup;
                  rebootAfterUse;
                  name
                }
    let to_value x =
      structure_to_value
        [("name", (Some (Name.to_value x.name)));
        ("description", (Option.map x.description ~f:Message.to_value));
        ("packageCleanup", (Option.map x.packageCleanup ~f:Boolean.to_value));
        ("excludeAppPackagesFromCleanup",
          (Option.map x.excludeAppPackagesFromCleanup ~f:PackageIds.to_value));
        ("rebootAfterUse", (Option.map x.rebootAfterUse ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let rebootAfterUse =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "rebootAfterUse") in
      let excludeAppPackagesFromCleanup =
        (Option.map ~f:PackageIds.of_xml)
          (Xml.child xml_arg0 "excludeAppPackagesFromCleanup") in
      let packageCleanup =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "packageCleanup") in
      let description =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "description") in
      let name =
        Name.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?rebootAfterUse ?excludeAppPackagesFromCleanup ?packageCleanup
        ?description ~name ()
    let of_json json =
      let rebootAfterUse = field_map json "rebootAfterUse" Boolean.of_json in
      let excludeAppPackagesFromCleanup =
        field_map json "excludeAppPackagesFromCleanup" PackageIds.of_json in
      let packageCleanup = field_map json "packageCleanup" Boolean.of_json in
      let description = field_map json "description" Message.of_json in
      let name = field_map_exn json "name" Name.of_json in
      make ?rebootAfterUse ?excludeAppPackagesFromCleanup ?packageCleanup
        ?description ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a profile that can be applied to one or more private fleet device instances."]
module CreateDevicePoolResult =
  struct
    type nonrec t =
      {
      devicePool: DevicePool.t option
        [@ocaml.doc "The newly created device pool."]}
    type nonrec error =
      [ `ArgumentException of ArgumentException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceAccountException of ServiceAccountException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?devicePool = fun () -> { devicePool }
    let error_of_json name json =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ArgumentException" ->
          `ArgumentException (ArgumentException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceAccountException" ->
          `ServiceAccountException (ServiceAccountException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ArgumentException e ->
          `Assoc
            [("error", (`String "ArgumentException"));
            ("details", (ArgumentException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceAccountException e ->
          `Assoc
            [("error", (`String "ServiceAccountException"));
            ("details", (ServiceAccountException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("devicePool", (Option.map x.devicePool ~f:DevicePool.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let devicePool =
        (Option.map ~f:DevicePool.of_xml) (Xml.child xml_arg0 "devicePool") in
      make ?devicePool ()
    let of_json json =
      let devicePool = field_map json "devicePool" DevicePool.of_json in
      make ?devicePool ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Represents the result of a create device pool request."]
module CreateDevicePoolRequest =
  struct
    type nonrec t =
      {
      projectArn: AmazonResourceName.t
        [@ocaml.doc "The ARN of the project for the device pool."];
      name: Name.t [@ocaml.doc "The device pool's name."];
      description: Message.t option
        [@ocaml.doc "The device pool's description."];
      rules: Rules.t [@ocaml.doc "The device pool's rules."];
      maxDevices: Integer.t option
        [@ocaml.doc
          "The number of devices that Device Farm can add to your device pool. Device Farm adds devices that are available and meet the criteria that you assign for the rules parameter. Depending on how many devices meet these constraints, your device pool might contain fewer devices than the value for this parameter. By specifying the maximum number of devices, you can control the costs that you incur by running tests."]}
    let context_ = "CreateDevicePoolRequest"
    let make ?description =
      fun ?maxDevices ->
        fun ~projectArn ->
          fun ~name ->
            fun ~rules ->
              fun () -> { description; maxDevices; projectArn; name; rules }
    let to_value x =
      structure_to_value
        [("projectArn", (Some (AmazonResourceName.to_value x.projectArn)));
        ("name", (Some (Name.to_value x.name)));
        ("description", (Option.map x.description ~f:Message.to_value));
        ("rules", (Some (Rules.to_value x.rules)));
        ("maxDevices", (Option.map x.maxDevices ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxDevices =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "maxDevices") in
      let rules =
        Rules.of_xml (Xml.child_exn ~context:context_ xml_arg0 "rules") in
      let description =
        (Option.map ~f:Message.of_xml) (Xml.child xml_arg0 "description") in
      let name =
        Name.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      let projectArn =
        AmazonResourceName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "projectArn") in
      make ?maxDevices ~rules ?description ~name ~projectArn ()
    let of_json json =
      let maxDevices = field_map json "maxDevices" Integer.of_json in
      let rules = field_map_exn json "rules" Rules.of_json in
      let description = field_map json "description" Message.of_json in
      let name = field_map_exn json "name" Name.of_json in
      let projectArn =
        field_map_exn json "projectArn" AmazonResourceName.of_json in
      make ?maxDevices ~rules ?description ~name ~projectArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a request to the create device pool operation."]
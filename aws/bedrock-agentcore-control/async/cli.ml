(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_agent_runtime =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and authorizerConfiguration =
         flag "authorizer-configuration" (optional json_arg)
           ~doc:"JSON AuthorizerConfiguration"
       and requestHeaderConfiguration =
         flag "request-header-configuration" (optional json_arg)
           ~doc:"JSON RequestHeaderConfiguration"
       and protocolConfiguration =
         flag "protocol-configuration" (optional json_arg)
           ~doc:"JSON ProtocolConfiguration"
       and lifecycleConfiguration =
         flag "lifecycle-configuration" (optional json_arg)
           ~doc:"JSON LifecycleConfiguration"
       and environmentVariables =
         flag "environment-variables" (optional json_arg)
           ~doc:"JSON EnvironmentVariablesMap"
       and filesystemConfigurations =
         flag "filesystem-configurations" (optional json_arg)
           ~doc:"JSON FilesystemConfigurations"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and agentRuntimeName =
         flag "agent-runtime-name" (required string)
           ~doc:"STRING AgentRuntimeName"
       and agentRuntimeArtifact =
         flag "agent-runtime-artifact" (required json_arg)
           ~doc:"JSON AgentRuntimeArtifact"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn"
       and networkConfiguration =
         flag "network-configuration" (required json_arg)
           ~doc:"JSON NetworkConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_agent_runtime
           (Values.CreateAgentRuntimeRequest.make ?clientToken ?description
              ?authorizerConfiguration:(Option.map
                                          ~f:Values.AuthorizerConfiguration.of_json
                                          authorizerConfiguration)
              ?requestHeaderConfiguration:(Option.map
                                             ~f:Values.RequestHeaderConfiguration.of_json
                                             requestHeaderConfiguration)
              ?protocolConfiguration:(Option.map
                                        ~f:Values.ProtocolConfiguration.of_json
                                        protocolConfiguration)
              ?lifecycleConfiguration:(Option.map
                                         ~f:Values.LifecycleConfiguration.of_json
                                         lifecycleConfiguration)
              ?environmentVariables:(Option.map
                                       ~f:Values.EnvironmentVariablesMap.of_json
                                       environmentVariables)
              ?filesystemConfigurations:(Option.map
                                           ~f:Values.FilesystemConfigurations.of_json
                                           filesystemConfigurations)
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ~agentRuntimeName
              ~agentRuntimeArtifact:(Values.AgentRuntimeArtifact.of_json
                                       agentRuntimeArtifact) ~roleArn
              ~networkConfiguration:(Values.NetworkConfiguration.of_json
                                       networkConfiguration) ())
           (Some Values.CreateAgentRuntimeResponse.to_json)
           (Some Values.CreateAgentRuntimeResponse.error_to_json)])
let create_agent_runtime_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentRuntimeVersion =
         flag "agent-runtime-version" (optional string)
           ~doc:"STRING AgentRuntimeVersion"
       and description =
         flag "description" (optional string)
           ~doc:"STRING AgentEndpointDescription"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and agentRuntimeId =
         flag "agent-runtime-id" (required string)
           ~doc:"STRING AgentRuntimeId"
       and name = flag "name" (required string) ~doc:"STRING EndpointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_agent_runtime_endpoint
           (Values.CreateAgentRuntimeEndpointRequest.make
              ?agentRuntimeVersion ?description ?clientToken
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ~agentRuntimeId ~name ())
           (Some Values.CreateAgentRuntimeEndpointResponse.to_json)
           (Some Values.CreateAgentRuntimeEndpointResponse.error_to_json)])
let create_api_key_credential_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and name =
         flag "name" (required string) ~doc:"STRING CredentialProviderName"
       and apiKey = flag "api-key" (required string) ~doc:"STRING ApiKeyType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_api_key_credential_provider
           (Values.CreateApiKeyCredentialProviderRequest.make
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~name ~apiKey
              ())
           (Some Values.CreateApiKeyCredentialProviderResponse.to_json)
           (Some Values.CreateApiKeyCredentialProviderResponse.error_to_json)])
let create_browser =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and executionRoleArn =
         flag "execution-role-arn" (optional string) ~doc:"STRING RoleArn"
       and recording =
         flag "recording" (optional json_arg) ~doc:"JSON RecordingConfig"
       and browserSigning =
         flag "browser-signing" (optional json_arg)
           ~doc:"JSON BrowserSigningConfigInput"
       and enterprisePolicies =
         flag "enterprise-policies" (optional json_arg)
           ~doc:"JSON BrowserEnterprisePolicies"
       and certificates =
         flag "certificates" (optional json_arg) ~doc:"JSON Certificates"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and name = flag "name" (required string) ~doc:"STRING SandboxName"
       and networkConfiguration =
         flag "network-configuration" (required json_arg)
           ~doc:"JSON BrowserNetworkConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_browser
           (Values.CreateBrowserRequest.make ?description ?executionRoleArn
              ?recording:(Option.map ~f:Values.RecordingConfig.of_json
                            recording)
              ?browserSigning:(Option.map
                                 ~f:Values.BrowserSigningConfigInput.of_json
                                 browserSigning)
              ?enterprisePolicies:(Option.map
                                     ~f:Values.BrowserEnterprisePolicies.of_json
                                     enterprisePolicies)
              ?certificates:(Option.map ~f:Values.Certificates.of_json
                               certificates) ?clientToken
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~name
              ~networkConfiguration:(Values.BrowserNetworkConfiguration.of_json
                                       networkConfiguration) ())
           (Some Values.CreateBrowserResponse.to_json)
           (Some Values.CreateBrowserResponse.error_to_json)])
let create_browser_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and name =
         flag "name" (required string) ~doc:"STRING BrowserProfileName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_browser_profile
           (Values.CreateBrowserProfileRequest.make ?description ?clientToken
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~name ())
           (Some Values.CreateBrowserProfileResponse.to_json)
           (Some Values.CreateBrowserProfileResponse.error_to_json)])
let create_code_interpreter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and executionRoleArn =
         flag "execution-role-arn" (optional string) ~doc:"STRING RoleArn"
       and certificates =
         flag "certificates" (optional json_arg) ~doc:"JSON Certificates"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and name = flag "name" (required string) ~doc:"STRING SandboxName"
       and networkConfiguration =
         flag "network-configuration" (required json_arg)
           ~doc:"JSON CodeInterpreterNetworkConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_code_interpreter
           (Values.CreateCodeInterpreterRequest.make ?description
              ?executionRoleArn
              ?certificates:(Option.map ~f:Values.Certificates.of_json
                               certificates) ?clientToken
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~name
              ~networkConfiguration:(Values.CodeInterpreterNetworkConfiguration.of_json
                                       networkConfiguration) ())
           (Some Values.CreateCodeInterpreterResponse.to_json)
           (Some Values.CreateCodeInterpreterResponse.error_to_json)])
let create_configuration_bundle =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ConfigurationBundleDescription"
       and branchName =
         flag "branch-name" (optional string) ~doc:"STRING BranchName"
       and commitMessage =
         flag "commit-message" (optional string)
           ~doc:"STRING CreateConfigurationBundleRequestCommitMessageString"
       and createdBy =
         flag "created-by" (optional json_arg)
           ~doc:"JSON VersionCreatedBySource"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and bundleName =
         flag "bundle-name" (required string)
           ~doc:"STRING ConfigurationBundleName"
       and components =
         flag "components" (required json_arg)
           ~doc:"JSON ComponentConfigurationMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_configuration_bundle
           (Values.CreateConfigurationBundleRequest.make ?clientToken
              ?description ?branchName ?commitMessage
              ?createdBy:(Option.map ~f:Values.VersionCreatedBySource.of_json
                            createdBy)
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~bundleName
              ~components:(Values.ComponentConfigurationMap.of_json
                             components) ())
           (Some Values.CreateConfigurationBundleResponse.to_json)
           (Some Values.CreateConfigurationBundleResponse.error_to_json)])
let create_evaluator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string)
           ~doc:"STRING EvaluatorDescription"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and evaluatorName =
         flag "evaluator-name" (required string)
           ~doc:"STRING CustomEvaluatorName"
       and evaluatorConfig =
         flag "evaluator-config" (required json_arg)
           ~doc:"JSON EvaluatorConfig"
       and level =
         flag "level" (required json_arg) ~doc:"JSON EvaluatorLevel" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_evaluator
           (Values.CreateEvaluatorRequest.make ?clientToken ?description
              ?kmsKeyArn ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ~evaluatorName
              ~evaluatorConfig:(Values.EvaluatorConfig.of_json
                                  evaluatorConfig)
              ~level:(Values.EvaluatorLevel.of_json level) ())
           (Some Values.CreateEvaluatorResponse.to_json)
           (Some Values.CreateEvaluatorResponse.error_to_json)])
let create_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING GatewayDescription"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and protocolType =
         flag "protocol-type" (optional json_arg)
           ~doc:"JSON GatewayProtocolType"
       and protocolConfiguration =
         flag "protocol-configuration" (optional json_arg)
           ~doc:"JSON GatewayProtocolConfiguration"
       and authorizerConfiguration =
         flag "authorizer-configuration" (optional json_arg)
           ~doc:"JSON AuthorizerConfiguration"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and interceptorConfigurations =
         flag "interceptor-configurations" (optional json_arg)
           ~doc:"JSON GatewayInterceptorConfigurations"
       and policyEngineConfiguration =
         flag "policy-engine-configuration" (optional json_arg)
           ~doc:"JSON GatewayPolicyEngineConfiguration"
       and exceptionLevel =
         flag "exception-level" (optional json_arg)
           ~doc:"JSON ExceptionLevel"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and name = flag "name" (required string) ~doc:"STRING GatewayName"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn"
       and authorizerType =
         flag "authorizer-type" (required json_arg)
           ~doc:"JSON AuthorizerType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_gateway
           (Values.CreateGatewayRequest.make ?description ?clientToken
              ?protocolType:(Option.map ~f:Values.GatewayProtocolType.of_json
                               protocolType)
              ?protocolConfiguration:(Option.map
                                        ~f:Values.GatewayProtocolConfiguration.of_json
                                        protocolConfiguration)
              ?authorizerConfiguration:(Option.map
                                          ~f:Values.AuthorizerConfiguration.of_json
                                          authorizerConfiguration) ?kmsKeyArn
              ?interceptorConfigurations:(Option.map
                                            ~f:Values.GatewayInterceptorConfigurations.of_json
                                            interceptorConfigurations)
              ?policyEngineConfiguration:(Option.map
                                            ~f:Values.GatewayPolicyEngineConfiguration.of_json
                                            policyEngineConfiguration)
              ?exceptionLevel:(Option.map ~f:Values.ExceptionLevel.of_json
                                 exceptionLevel)
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~name
              ~roleArn
              ~authorizerType:(Values.AuthorizerType.of_json authorizerType)
              ()) (Some Values.CreateGatewayResponse.to_json)
           (Some Values.CreateGatewayResponse.error_to_json)])
let create_gateway_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and conditions =
         flag "conditions" (optional json_arg) ~doc:"JSON Conditions"
       and description =
         flag "description" (optional string)
           ~doc:"STRING GatewayRuleDescription"
       and gatewayIdentifier =
         flag "gateway-identifier" (required string)
           ~doc:"STRING GatewayIdentifier"
       and priority =
         flag "priority" (required int) ~doc:"INT GatewayRulePriority"
       and actions = flag "actions" (required json_arg) ~doc:"JSON Actions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_gateway_rule
           (Values.CreateGatewayRuleRequest.make ?clientToken
              ?conditions:(Option.map ~f:Values.Conditions.of_json conditions)
              ?description ~gatewayIdentifier ~priority
              ~actions:(Values.Actions.of_json actions) ())
           (Some Values.CreateGatewayRuleResponse.to_json)
           (Some Values.CreateGatewayRuleResponse.error_to_json)])
let create_gateway_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING TargetDescription"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and credentialProviderConfigurations =
         flag "credential-provider-configurations" (optional json_arg)
           ~doc:"JSON CredentialProviderConfigurations"
       and metadataConfiguration =
         flag "metadata-configuration" (optional json_arg)
           ~doc:"JSON MetadataConfiguration"
       and privateEndpoint =
         flag "private-endpoint" (optional json_arg)
           ~doc:"JSON PrivateEndpoint"
       and gatewayIdentifier =
         flag "gateway-identifier" (required string)
           ~doc:"STRING GatewayIdentifier"
       and name = flag "name" (required string) ~doc:"STRING TargetName"
       and targetConfiguration =
         flag "target-configuration" (required json_arg)
           ~doc:"JSON TargetConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_gateway_target
           (Values.CreateGatewayTargetRequest.make ?description ?clientToken
              ?credentialProviderConfigurations:(Option.map
                                                   ~f:Values.CredentialProviderConfigurations.of_json
                                                   credentialProviderConfigurations)
              ?metadataConfiguration:(Option.map
                                        ~f:Values.MetadataConfiguration.of_json
                                        metadataConfiguration)
              ?privateEndpoint:(Option.map ~f:Values.PrivateEndpoint.of_json
                                  privateEndpoint) ~gatewayIdentifier ~name
              ~targetConfiguration:(Values.TargetConfiguration.of_json
                                      targetConfiguration) ())
           (Some Values.CreateGatewayTargetResponse.to_json)
           (Some Values.CreateGatewayTargetResponse.error_to_json)])
let create_harness =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and environment =
         flag "environment" (optional json_arg)
           ~doc:"JSON HarnessEnvironmentProviderRequest"
       and environmentArtifact =
         flag "environment-artifact" (optional json_arg)
           ~doc:"JSON HarnessEnvironmentArtifact"
       and environmentVariables =
         flag "environment-variables" (optional json_arg)
           ~doc:"JSON EnvironmentVariablesMap"
       and authorizerConfiguration =
         flag "authorizer-configuration" (optional json_arg)
           ~doc:"JSON AuthorizerConfiguration"
       and model =
         flag "model" (optional json_arg)
           ~doc:"JSON HarnessModelConfiguration"
       and systemPrompt =
         flag "system-prompt" (optional json_arg)
           ~doc:"JSON HarnessSystemPrompt"
       and tools = flag "tools" (optional json_arg) ~doc:"JSON HarnessTools"
       and skills =
         flag "skills" (optional json_arg) ~doc:"JSON HarnessSkills"
       and allowedTools =
         flag "allowed-tools" (optional json_arg)
           ~doc:"JSON HarnessAllowedTools"
       and memory =
         flag "memory" (optional json_arg)
           ~doc:"JSON HarnessMemoryConfiguration"
       and truncation =
         flag "truncation" (optional json_arg)
           ~doc:"JSON HarnessTruncationConfiguration"
       and maxIterations =
         flag "max-iterations" (optional int) ~doc:"INT Integer"
       and maxTokens = flag "max-tokens" (optional int) ~doc:"INT Integer"
       and timeoutSeconds =
         flag "timeout-seconds" (optional int) ~doc:"INT Integer"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and harnessName =
         flag "harness-name" (required string) ~doc:"STRING HarnessName"
       and executionRoleArn =
         flag "execution-role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_harness
           (Values.CreateHarnessRequest.make ?clientToken
              ?environment:(Option.map
                              ~f:Values.HarnessEnvironmentProviderRequest.of_json
                              environment)
              ?environmentArtifact:(Option.map
                                      ~f:Values.HarnessEnvironmentArtifact.of_json
                                      environmentArtifact)
              ?environmentVariables:(Option.map
                                       ~f:Values.EnvironmentVariablesMap.of_json
                                       environmentVariables)
              ?authorizerConfiguration:(Option.map
                                          ~f:Values.AuthorizerConfiguration.of_json
                                          authorizerConfiguration)
              ?model:(Option.map ~f:Values.HarnessModelConfiguration.of_json
                        model)
              ?systemPrompt:(Option.map ~f:Values.HarnessSystemPrompt.of_json
                               systemPrompt)
              ?tools:(Option.map ~f:Values.HarnessTools.of_json tools)
              ?skills:(Option.map ~f:Values.HarnessSkills.of_json skills)
              ?allowedTools:(Option.map ~f:Values.HarnessAllowedTools.of_json
                               allowedTools)
              ?memory:(Option.map
                         ~f:Values.HarnessMemoryConfiguration.of_json memory)
              ?truncation:(Option.map
                             ~f:Values.HarnessTruncationConfiguration.of_json
                             truncation) ?maxIterations ?maxTokens
              ?timeoutSeconds
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~harnessName
              ~executionRoleArn ())
           (Some Values.CreateHarnessResponse.to_json)
           (Some Values.CreateHarnessResponse.error_to_json)])
let create_memory =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING CreateMemoryInputClientTokenString"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and encryptionKeyArn =
         flag "encryption-key-arn" (optional string) ~doc:"STRING Arn"
       and memoryExecutionRoleArn =
         flag "memory-execution-role-arn" (optional string) ~doc:"STRING Arn"
       and memoryStrategies =
         flag "memory-strategies" (optional json_arg)
           ~doc:"JSON MemoryStrategyInputList"
       and indexedKeys =
         flag "indexed-keys" (optional json_arg) ~doc:"JSON IndexedKeysList"
       and streamDeliveryResources =
         flag "stream-delivery-resources" (optional json_arg)
           ~doc:"JSON StreamDeliveryResources"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and eventExpiryDuration =
         flag "event-expiry-duration" (required int)
           ~doc:"INT CreateMemoryInputEventExpiryDurationInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_memory
           (Values.CreateMemoryInput.make ?clientToken ?description
              ?encryptionKeyArn ?memoryExecutionRoleArn
              ?memoryStrategies:(Option.map
                                   ~f:Values.MemoryStrategyInputList.of_json
                                   memoryStrategies)
              ?indexedKeys:(Option.map ~f:Values.IndexedKeysList.of_json
                              indexedKeys)
              ?streamDeliveryResources:(Option.map
                                          ~f:Values.StreamDeliveryResources.of_json
                                          streamDeliveryResources)
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~name
              ~eventExpiryDuration ())
           (Some Values.CreateMemoryOutput.to_json)
           (Some Values.CreateMemoryOutput.error_to_json)])
let create_oauth2_credential_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and name =
         flag "name" (required string) ~doc:"STRING CredentialProviderName"
       and credentialProviderVendor =
         flag "credential-provider-vendor" (required json_arg)
           ~doc:"JSON CredentialProviderVendorType"
       and oauth2ProviderConfigInput =
         flag "oauth2-provider-config-input" (required json_arg)
           ~doc:"JSON Oauth2ProviderConfigInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_oauth2_credential_provider
           (Values.CreateOauth2CredentialProviderRequest.make
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~name
              ~credentialProviderVendor:(Values.CredentialProviderVendorType.of_json
                                           credentialProviderVendor)
              ~oauth2ProviderConfigInput:(Values.Oauth2ProviderConfigInput.of_json
                                            oauth2ProviderConfigInput) ())
           (Some Values.CreateOauth2CredentialProviderResponse.to_json)
           (Some Values.CreateOauth2CredentialProviderResponse.error_to_json)])
let create_online_evaluation_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string)
           ~doc:"STRING EvaluationConfigDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and onlineEvaluationConfigName =
         flag "online-evaluation-config-name" (required string)
           ~doc:"STRING EvaluationConfigName"
       and rule = flag "rule" (required json_arg) ~doc:"JSON Rule"
       and dataSourceConfig =
         flag "data-source-config" (required json_arg)
           ~doc:"JSON DataSourceConfig"
       and evaluators =
         flag "evaluators" (required json_arg) ~doc:"JSON EvaluatorList"
       and evaluationExecutionRoleArn =
         flag "evaluation-execution-role-arn" (required string)
           ~doc:"STRING RoleArn"
       and enableOnCreate =
         flag "enable-on-create" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_online_evaluation_config
           (Values.CreateOnlineEvaluationConfigRequest.make ?clientToken
              ?description ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ~onlineEvaluationConfigName ~rule:(Values.Rule.of_json rule)
              ~dataSourceConfig:(Values.DataSourceConfig.of_json
                                   dataSourceConfig)
              ~evaluators:(Values.EvaluatorList.of_json evaluators)
              ~evaluationExecutionRoleArn ~enableOnCreate ())
           (Some Values.CreateOnlineEvaluationConfigResponse.to_json)
           (Some Values.CreateOnlineEvaluationConfigResponse.error_to_json)])
let create_payment_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING PaymentsDescription"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and paymentManagerId =
         flag "payment-manager-id" (required string)
           ~doc:"STRING PaymentManagerId"
       and name =
         flag "name" (required string) ~doc:"STRING PaymentConnectorName"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON PaymentConnectorType"
       and credentialProviderConfigurations =
         flag "credential-provider-configurations" (required json_arg)
           ~doc:"JSON CredentialsProviderConfigurations" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_payment_connector
           (Values.CreatePaymentConnectorRequest.make ?description
              ?clientToken ~paymentManagerId ~name
              ~type_:(Values.PaymentConnectorType.of_json type_)
              ~credentialProviderConfigurations:(Values.CredentialsProviderConfigurations.of_json
                                                   credentialProviderConfigurations)
              ()) (Some Values.CreatePaymentConnectorResponse.to_json)
           (Some Values.CreatePaymentConnectorResponse.error_to_json)])
let create_payment_credential_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and name =
         flag "name" (required string) ~doc:"STRING CredentialProviderName"
       and credentialProviderVendor =
         flag "credential-provider-vendor" (required json_arg)
           ~doc:"JSON PaymentCredentialProviderVendorType"
       and providerConfigurationInput =
         flag "provider-configuration-input" (required json_arg)
           ~doc:"JSON PaymentProviderConfigurationInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_payment_credential_provider
           (Values.CreatePaymentCredentialProviderRequest.make
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~name
              ~credentialProviderVendor:(Values.PaymentCredentialProviderVendorType.of_json
                                           credentialProviderVendor)
              ~providerConfigurationInput:(Values.PaymentProviderConfigurationInput.of_json
                                             providerConfigurationInput) ())
           (Some Values.CreatePaymentCredentialProviderResponse.to_json)
           (Some Values.CreatePaymentCredentialProviderResponse.error_to_json)])
let create_payment_manager =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING PaymentsDescription"
       and authorizerConfiguration =
         flag "authorizer-configuration" (optional json_arg)
           ~doc:"JSON AuthorizerConfiguration"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and name =
         flag "name" (required string) ~doc:"STRING PaymentManagerName"
       and authorizerType =
         flag "authorizer-type" (required json_arg)
           ~doc:"JSON PaymentsAuthorizerType"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_payment_manager
           (Values.CreatePaymentManagerRequest.make ?description
              ?authorizerConfiguration:(Option.map
                                          ~f:Values.AuthorizerConfiguration.of_json
                                          authorizerConfiguration)
              ?clientToken ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ~name
              ~authorizerType:(Values.PaymentsAuthorizerType.of_json
                                 authorizerType) ~roleArn ())
           (Some Values.CreatePaymentManagerResponse.to_json)
           (Some Values.CreatePaymentManagerResponse.error_to_json)])
let create_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and validationMode =
         flag "validation-mode" (optional json_arg)
           ~doc:"JSON PolicyValidationMode"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and name = flag "name" (required string) ~doc:"STRING PolicyName"
       and definition =
         flag "definition" (required json_arg) ~doc:"JSON PolicyDefinition"
       and policyEngineId =
         flag "policy-engine-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_policy
           (Values.CreatePolicyRequest.make ?description
              ?validationMode:(Option.map
                                 ~f:Values.PolicyValidationMode.of_json
                                 validationMode) ?clientToken ~name
              ~definition:(Values.PolicyDefinition.of_json definition)
              ~policyEngineId ()) (Some Values.CreatePolicyResponse.to_json)
           (Some Values.CreatePolicyResponse.error_to_json)])
let create_policy_engine =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and encryptionKeyArn =
         flag "encryption-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and name =
         flag "name" (required string) ~doc:"STRING PolicyEngineName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_policy_engine
           (Values.CreatePolicyEngineRequest.make ?description ?clientToken
              ?encryptionKeyArn
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~name ())
           (Some Values.CreatePolicyEngineResponse.to_json)
           (Some Values.CreatePolicyEngineResponse.error_to_json)])
let create_registry =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and authorizerType =
         flag "authorizer-type" (optional json_arg)
           ~doc:"JSON RegistryAuthorizerType"
       and authorizerConfiguration =
         flag "authorizer-configuration" (optional json_arg)
           ~doc:"JSON AuthorizerConfiguration"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and approvalConfiguration =
         flag "approval-configuration" (optional json_arg)
           ~doc:"JSON ApprovalConfiguration"
       and name = flag "name" (required string) ~doc:"STRING RegistryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_registry
           (Values.CreateRegistryRequest.make ?description
              ?authorizerType:(Option.map
                                 ~f:Values.RegistryAuthorizerType.of_json
                                 authorizerType)
              ?authorizerConfiguration:(Option.map
                                          ~f:Values.AuthorizerConfiguration.of_json
                                          authorizerConfiguration)
              ?clientToken
              ?approvalConfiguration:(Option.map
                                        ~f:Values.ApprovalConfiguration.of_json
                                        approvalConfiguration) ~name ())
           (Some Values.CreateRegistryResponse.to_json)
           (Some Values.CreateRegistryResponse.error_to_json)])
let create_registry_record =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and descriptors =
         flag "descriptors" (optional json_arg) ~doc:"JSON Descriptors"
       and recordVersion =
         flag "record-version" (optional string)
           ~doc:"STRING RegistryRecordVersion"
       and synchronizationType =
         flag "synchronization-type" (optional json_arg)
           ~doc:"JSON SynchronizationType"
       and synchronizationConfiguration =
         flag "synchronization-configuration" (optional json_arg)
           ~doc:"JSON SynchronizationConfiguration"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and registryId =
         flag "registry-id" (required string)
           ~doc:"STRING RegistryIdentifier"
       and name =
         flag "name" (required string) ~doc:"STRING RegistryRecordName"
       and descriptorType =
         flag "descriptor-type" (required json_arg)
           ~doc:"JSON DescriptorType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_registry_record
           (Values.CreateRegistryRecordRequest.make ?description
              ?descriptors:(Option.map ~f:Values.Descriptors.of_json
                              descriptors) ?recordVersion
              ?synchronizationType:(Option.map
                                      ~f:Values.SynchronizationType.of_json
                                      synchronizationType)
              ?synchronizationConfiguration:(Option.map
                                               ~f:Values.SynchronizationConfiguration.of_json
                                               synchronizationConfiguration)
              ?clientToken ~registryId ~name
              ~descriptorType:(Values.DescriptorType.of_json descriptorType)
              ()) (Some Values.CreateRegistryRecordResponse.to_json)
           (Some Values.CreateRegistryRecordResponse.error_to_json)])
let create_workload_identity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and allowedResourceOauth2ReturnUrls =
         flag "allowed-resource-oauth2-return-urls" (optional json_arg)
           ~doc:"JSON ResourceOauth2ReturnUrlListType"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and name =
         flag "name" (required string) ~doc:"STRING WorkloadIdentityNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_workload_identity
           (Values.CreateWorkloadIdentityRequest.make
              ?allowedResourceOauth2ReturnUrls:(Option.map
                                                  ~f:Values.ResourceOauth2ReturnUrlListType.of_json
                                                  allowedResourceOauth2ReturnUrls)
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~name ())
           (Some Values.CreateWorkloadIdentityResponse.to_json)
           (Some Values.CreateWorkloadIdentityResponse.error_to_json)])
let delete_agent_runtime =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and agentRuntimeId =
         flag "agent-runtime-id" (required string)
           ~doc:"STRING AgentRuntimeId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_agent_runtime
           (Values.DeleteAgentRuntimeRequest.make ?clientToken
              ~agentRuntimeId ())
           (Some Values.DeleteAgentRuntimeResponse.to_json)
           (Some Values.DeleteAgentRuntimeResponse.error_to_json)])
let delete_agent_runtime_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and agentRuntimeId =
         flag "agent-runtime-id" (required string)
           ~doc:"STRING AgentRuntimeId"
       and endpointName =
         flag "endpoint-name" (required string) ~doc:"STRING EndpointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_agent_runtime_endpoint
           (Values.DeleteAgentRuntimeEndpointRequest.make ?clientToken
              ~agentRuntimeId ~endpointName ())
           (Some Values.DeleteAgentRuntimeEndpointResponse.to_json)
           (Some Values.DeleteAgentRuntimeEndpointResponse.error_to_json)])
let delete_api_key_credential_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING CredentialProviderName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_api_key_credential_provider
           (Values.DeleteApiKeyCredentialProviderRequest.make ~name ())
           (Some Values.DeleteApiKeyCredentialProviderResponse.to_json)
           (Some Values.DeleteApiKeyCredentialProviderResponse.error_to_json)])
let delete_browser =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and browserId =
         flag "browser-id" (required string) ~doc:"STRING BrowserId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_browser
           (Values.DeleteBrowserRequest.make ?clientToken ~browserId ())
           (Some Values.DeleteBrowserResponse.to_json)
           (Some Values.DeleteBrowserResponse.error_to_json)])
let delete_browser_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and profileId =
         flag "profile-id" (required string) ~doc:"STRING BrowserProfileId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_browser_profile
           (Values.DeleteBrowserProfileRequest.make ?clientToken ~profileId
              ()) (Some Values.DeleteBrowserProfileResponse.to_json)
           (Some Values.DeleteBrowserProfileResponse.error_to_json)])
let delete_code_interpreter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and codeInterpreterId =
         flag "code-interpreter-id" (required string)
           ~doc:"STRING CodeInterpreterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_code_interpreter
           (Values.DeleteCodeInterpreterRequest.make ?clientToken
              ~codeInterpreterId ())
           (Some Values.DeleteCodeInterpreterResponse.to_json)
           (Some Values.DeleteCodeInterpreterResponse.error_to_json)])
let delete_configuration_bundle =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bundleId =
         flag "bundle-id" (required string)
           ~doc:"STRING ConfigurationBundleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_configuration_bundle
           (Values.DeleteConfigurationBundleRequest.make ~bundleId ())
           (Some Values.DeleteConfigurationBundleResponse.to_json)
           (Some Values.DeleteConfigurationBundleResponse.error_to_json)])
let delete_evaluator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and evaluatorId =
         flag "evaluator-id" (required string) ~doc:"STRING EvaluatorId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_evaluator
           (Values.DeleteEvaluatorRequest.make ~evaluatorId ())
           (Some Values.DeleteEvaluatorResponse.to_json)
           (Some Values.DeleteEvaluatorResponse.error_to_json)])
let delete_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayIdentifier =
         flag "gateway-identifier" (required string)
           ~doc:"STRING GatewayIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_gateway
           (Values.DeleteGatewayRequest.make ~gatewayIdentifier ())
           (Some Values.DeleteGatewayResponse.to_json)
           (Some Values.DeleteGatewayResponse.error_to_json)])
let delete_gateway_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayIdentifier =
         flag "gateway-identifier" (required string)
           ~doc:"STRING GatewayIdentifier"
       and ruleId =
         flag "rule-id" (required string) ~doc:"STRING GatewayRuleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_gateway_rule
           (Values.DeleteGatewayRuleRequest.make ~gatewayIdentifier ~ruleId
              ()) (Some Values.DeleteGatewayRuleResponse.to_json)
           (Some Values.DeleteGatewayRuleResponse.error_to_json)])
let delete_gateway_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayIdentifier =
         flag "gateway-identifier" (required string)
           ~doc:"STRING GatewayIdentifier"
       and targetId =
         flag "target-id" (required string) ~doc:"STRING TargetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_gateway_target
           (Values.DeleteGatewayTargetRequest.make ~gatewayIdentifier
              ~targetId ()) (Some Values.DeleteGatewayTargetResponse.to_json)
           (Some Values.DeleteGatewayTargetResponse.error_to_json)])
let delete_harness =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and harnessId =
         flag "harness-id" (required string) ~doc:"STRING HarnessId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_harness
           (Values.DeleteHarnessRequest.make ?clientToken ~harnessId ())
           (Some Values.DeleteHarnessResponse.to_json)
           (Some Values.DeleteHarnessResponse.error_to_json)])
let delete_memory =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING DeleteMemoryInputClientTokenString"
       and memoryId =
         flag "memory-id" (required string) ~doc:"STRING MemoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_memory
           (Values.DeleteMemoryInput.make ?clientToken ~memoryId ())
           (Some Values.DeleteMemoryOutput.to_json)
           (Some Values.DeleteMemoryOutput.error_to_json)])
let delete_oauth2_credential_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING CredentialProviderName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_oauth2_credential_provider
           (Values.DeleteOauth2CredentialProviderRequest.make ~name ())
           (Some Values.DeleteOauth2CredentialProviderResponse.to_json)
           (Some Values.DeleteOauth2CredentialProviderResponse.error_to_json)])
let delete_online_evaluation_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and onlineEvaluationConfigId =
         flag "online-evaluation-config-id" (required string)
           ~doc:"STRING OnlineEvaluationConfigId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_online_evaluation_config
           (Values.DeleteOnlineEvaluationConfigRequest.make
              ~onlineEvaluationConfigId ())
           (Some Values.DeleteOnlineEvaluationConfigResponse.to_json)
           (Some Values.DeleteOnlineEvaluationConfigResponse.error_to_json)])
let delete_payment_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and paymentManagerId =
         flag "payment-manager-id" (required string)
           ~doc:"STRING PaymentManagerId"
       and paymentConnectorId =
         flag "payment-connector-id" (required string)
           ~doc:"STRING PaymentConnectorId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_payment_connector
           (Values.DeletePaymentConnectorRequest.make ?clientToken
              ~paymentManagerId ~paymentConnectorId ())
           (Some Values.DeletePaymentConnectorResponse.to_json)
           (Some Values.DeletePaymentConnectorResponse.error_to_json)])
let delete_payment_credential_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING CredentialProviderName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_payment_credential_provider
           (Values.DeletePaymentCredentialProviderRequest.make ~name ())
           (Some Values.DeletePaymentCredentialProviderResponse.to_json)
           (Some Values.DeletePaymentCredentialProviderResponse.error_to_json)])
let delete_payment_manager =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and paymentManagerId =
         flag "payment-manager-id" (required string)
           ~doc:"STRING PaymentManagerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_payment_manager
           (Values.DeletePaymentManagerRequest.make ?clientToken
              ~paymentManagerId ())
           (Some Values.DeletePaymentManagerResponse.to_json)
           (Some Values.DeletePaymentManagerResponse.error_to_json)])
let delete_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyEngineId =
         flag "policy-engine-id" (required string) ~doc:"STRING ResourceId"
       and policyId =
         flag "policy-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_policy
           (Values.DeletePolicyRequest.make ~policyEngineId ~policyId ())
           (Some Values.DeletePolicyResponse.to_json)
           (Some Values.DeletePolicyResponse.error_to_json)])
let delete_policy_engine =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyEngineId =
         flag "policy-engine-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_policy_engine
           (Values.DeletePolicyEngineRequest.make ~policyEngineId ())
           (Some Values.DeletePolicyEngineResponse.to_json)
           (Some Values.DeletePolicyEngineResponse.error_to_json)])
let delete_registry =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (required string)
           ~doc:"STRING RegistryIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_registry
           (Values.DeleteRegistryRequest.make ~registryId ())
           (Some Values.DeleteRegistryResponse.to_json)
           (Some Values.DeleteRegistryResponse.error_to_json)])
let delete_registry_record =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (required string)
           ~doc:"STRING RegistryIdentifier"
       and recordId =
         flag "record-id" (required string) ~doc:"STRING RecordIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_registry_record
           (Values.DeleteRegistryRecordRequest.make ~registryId ~recordId ())
           (Some Values.DeleteRegistryRecordResponse.to_json)
           (Some Values.DeleteRegistryRecordResponse.error_to_json)])
let delete_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING BedrockAgentcoreResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_policy
           (Values.DeleteResourcePolicyRequest.make ~resourceArn ())
           (Some Values.DeleteResourcePolicyResponse.to_json)
           (Some Values.DeleteResourcePolicyResponse.error_to_json)])
let delete_workload_identity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING WorkloadIdentityNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_workload_identity
           (Values.DeleteWorkloadIdentityRequest.make ~name ())
           (Some Values.DeleteWorkloadIdentityResponse.to_json)
           (Some Values.DeleteWorkloadIdentityResponse.error_to_json)])
let get_agent_runtime =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentRuntimeVersion =
         flag "agent-runtime-version" (optional string)
           ~doc:"STRING AgentRuntimeVersion"
       and agentRuntimeId =
         flag "agent-runtime-id" (required string)
           ~doc:"STRING AgentRuntimeId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_agent_runtime
           (Values.GetAgentRuntimeRequest.make ?agentRuntimeVersion
              ~agentRuntimeId ())
           (Some Values.GetAgentRuntimeResponse.to_json)
           (Some Values.GetAgentRuntimeResponse.error_to_json)])
let get_agent_runtime_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentRuntimeId =
         flag "agent-runtime-id" (required string)
           ~doc:"STRING AgentRuntimeId"
       and endpointName =
         flag "endpoint-name" (required string) ~doc:"STRING EndpointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_agent_runtime_endpoint
           (Values.GetAgentRuntimeEndpointRequest.make ~agentRuntimeId
              ~endpointName ())
           (Some Values.GetAgentRuntimeEndpointResponse.to_json)
           (Some Values.GetAgentRuntimeEndpointResponse.error_to_json)])
let get_api_key_credential_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING CredentialProviderName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_api_key_credential_provider
           (Values.GetApiKeyCredentialProviderRequest.make ~name ())
           (Some Values.GetApiKeyCredentialProviderResponse.to_json)
           (Some Values.GetApiKeyCredentialProviderResponse.error_to_json)])
let get_browser =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and browserId =
         flag "browser-id" (required string) ~doc:"STRING BrowserId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_browser (Values.GetBrowserRequest.make ~browserId ())
           (Some Values.GetBrowserResponse.to_json)
           (Some Values.GetBrowserResponse.error_to_json)])
let get_browser_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileId =
         flag "profile-id" (required string) ~doc:"STRING BrowserProfileId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_browser_profile
           (Values.GetBrowserProfileRequest.make ~profileId ())
           (Some Values.GetBrowserProfileResponse.to_json)
           (Some Values.GetBrowserProfileResponse.error_to_json)])
let get_code_interpreter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and codeInterpreterId =
         flag "code-interpreter-id" (required string)
           ~doc:"STRING CodeInterpreterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_code_interpreter
           (Values.GetCodeInterpreterRequest.make ~codeInterpreterId ())
           (Some Values.GetCodeInterpreterResponse.to_json)
           (Some Values.GetCodeInterpreterResponse.error_to_json)])
let get_configuration_bundle =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and branchName =
         flag "branch-name" (optional string) ~doc:"STRING BranchName"
       and bundleId =
         flag "bundle-id" (required string)
           ~doc:"STRING ConfigurationBundleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_configuration_bundle
           (Values.GetConfigurationBundleRequest.make ?branchName ~bundleId
              ()) (Some Values.GetConfigurationBundleResponse.to_json)
           (Some Values.GetConfigurationBundleResponse.error_to_json)])
let get_configuration_bundle_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bundleId =
         flag "bundle-id" (required string)
           ~doc:"STRING ConfigurationBundleId"
       and versionId =
         flag "version-id" (required string)
           ~doc:"STRING ConfigurationBundleVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_configuration_bundle_version
           (Values.GetConfigurationBundleVersionRequest.make ~bundleId
              ~versionId ())
           (Some Values.GetConfigurationBundleVersionResponse.to_json)
           (Some Values.GetConfigurationBundleVersionResponse.error_to_json)])
let get_evaluator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includedData =
         flag "included-data" (optional json_arg) ~doc:"JSON IncludedData"
       and evaluatorId =
         flag "evaluator-id" (required string) ~doc:"STRING EvaluatorId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_evaluator
           (Values.GetEvaluatorRequest.make
              ?includedData:(Option.map ~f:Values.IncludedData.of_json
                               includedData) ~evaluatorId ())
           (Some Values.GetEvaluatorResponse.to_json)
           (Some Values.GetEvaluatorResponse.error_to_json)])
let get_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayIdentifier =
         flag "gateway-identifier" (required string)
           ~doc:"STRING GatewayIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_gateway
           (Values.GetGatewayRequest.make ~gatewayIdentifier ())
           (Some Values.GetGatewayResponse.to_json)
           (Some Values.GetGatewayResponse.error_to_json)])
let get_gateway_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayIdentifier =
         flag "gateway-identifier" (required string)
           ~doc:"STRING GatewayIdentifier"
       and ruleId =
         flag "rule-id" (required string) ~doc:"STRING GatewayRuleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_gateway_rule
           (Values.GetGatewayRuleRequest.make ~gatewayIdentifier ~ruleId ())
           (Some Values.GetGatewayRuleResponse.to_json)
           (Some Values.GetGatewayRuleResponse.error_to_json)])
let get_gateway_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayIdentifier =
         flag "gateway-identifier" (required string)
           ~doc:"STRING GatewayIdentifier"
       and targetId =
         flag "target-id" (required string) ~doc:"STRING TargetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_gateway_target
           (Values.GetGatewayTargetRequest.make ~gatewayIdentifier ~targetId
              ()) (Some Values.GetGatewayTargetResponse.to_json)
           (Some Values.GetGatewayTargetResponse.error_to_json)])
let get_harness =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and harnessId =
         flag "harness-id" (required string) ~doc:"STRING HarnessId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_harness (Values.GetHarnessRequest.make ~harnessId ())
           (Some Values.GetHarnessResponse.to_json)
           (Some Values.GetHarnessResponse.error_to_json)])
let get_memory =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and view = flag "view" (optional json_arg) ~doc:"JSON MemoryView"
       and memoryId =
         flag "memory-id" (required string) ~doc:"STRING MemoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_memory
           (Values.GetMemoryInput.make
              ?view:(Option.map ~f:Values.MemoryView.of_json view) ~memoryId
              ()) (Some Values.GetMemoryOutput.to_json)
           (Some Values.GetMemoryOutput.error_to_json)])
let get_oauth2_credential_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING CredentialProviderName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_oauth2_credential_provider
           (Values.GetOauth2CredentialProviderRequest.make ~name ())
           (Some Values.GetOauth2CredentialProviderResponse.to_json)
           (Some Values.GetOauth2CredentialProviderResponse.error_to_json)])
let get_online_evaluation_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and onlineEvaluationConfigId =
         flag "online-evaluation-config-id" (required string)
           ~doc:"STRING OnlineEvaluationConfigId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_online_evaluation_config
           (Values.GetOnlineEvaluationConfigRequest.make
              ~onlineEvaluationConfigId ())
           (Some Values.GetOnlineEvaluationConfigResponse.to_json)
           (Some Values.GetOnlineEvaluationConfigResponse.error_to_json)])
let get_payment_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and paymentManagerId =
         flag "payment-manager-id" (required string)
           ~doc:"STRING PaymentManagerId"
       and paymentConnectorId =
         flag "payment-connector-id" (required string)
           ~doc:"STRING PaymentConnectorId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_payment_connector
           (Values.GetPaymentConnectorRequest.make ~paymentManagerId
              ~paymentConnectorId ())
           (Some Values.GetPaymentConnectorResponse.to_json)
           (Some Values.GetPaymentConnectorResponse.error_to_json)])
let get_payment_credential_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING CredentialProviderName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_payment_credential_provider
           (Values.GetPaymentCredentialProviderRequest.make ~name ())
           (Some Values.GetPaymentCredentialProviderResponse.to_json)
           (Some Values.GetPaymentCredentialProviderResponse.error_to_json)])
let get_payment_manager =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and paymentManagerId =
         flag "payment-manager-id" (required string)
           ~doc:"STRING PaymentManagerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_payment_manager
           (Values.GetPaymentManagerRequest.make ~paymentManagerId ())
           (Some Values.GetPaymentManagerResponse.to_json)
           (Some Values.GetPaymentManagerResponse.error_to_json)])
let get_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyEngineId =
         flag "policy-engine-id" (required string) ~doc:"STRING ResourceId"
       and policyId =
         flag "policy-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_policy
           (Values.GetPolicyRequest.make ~policyEngineId ~policyId ())
           (Some Values.GetPolicyResponse.to_json)
           (Some Values.GetPolicyResponse.error_to_json)])
let get_policy_engine =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyEngineId =
         flag "policy-engine-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_policy_engine
           (Values.GetPolicyEngineRequest.make ~policyEngineId ())
           (Some Values.GetPolicyEngineResponse.to_json)
           (Some Values.GetPolicyEngineResponse.error_to_json)])
let get_policy_engine_summary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyEngineId =
         flag "policy-engine-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_policy_engine_summary
           (Values.GetPolicyEngineSummaryRequest.make ~policyEngineId ())
           (Some Values.GetPolicyEngineSummaryResponse.to_json)
           (Some Values.GetPolicyEngineSummaryResponse.error_to_json)])
let get_policy_generation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyGenerationId =
         flag "policy-generation-id" (required string)
           ~doc:"STRING ResourceId"
       and policyEngineId =
         flag "policy-engine-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_policy_generation
           (Values.GetPolicyGenerationRequest.make ~policyGenerationId
              ~policyEngineId ())
           (Some Values.GetPolicyGenerationResponse.to_json)
           (Some Values.GetPolicyGenerationResponse.error_to_json)])
let get_policy_generation_summary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyGenerationId =
         flag "policy-generation-id" (required string)
           ~doc:"STRING ResourceId"
       and policyEngineId =
         flag "policy-engine-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_policy_generation_summary
           (Values.GetPolicyGenerationSummaryRequest.make ~policyGenerationId
              ~policyEngineId ())
           (Some Values.GetPolicyGenerationSummaryResponse.to_json)
           (Some Values.GetPolicyGenerationSummaryResponse.error_to_json)])
let get_policy_summary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyEngineId =
         flag "policy-engine-id" (required string) ~doc:"STRING ResourceId"
       and policyId =
         flag "policy-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_policy_summary
           (Values.GetPolicySummaryRequest.make ~policyEngineId ~policyId ())
           (Some Values.GetPolicySummaryResponse.to_json)
           (Some Values.GetPolicySummaryResponse.error_to_json)])
let get_registry =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (required string)
           ~doc:"STRING RegistryIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_registry (Values.GetRegistryRequest.make ~registryId ())
           (Some Values.GetRegistryResponse.to_json)
           (Some Values.GetRegistryResponse.error_to_json)])
let get_registry_record =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (required string)
           ~doc:"STRING RegistryIdentifier"
       and recordId =
         flag "record-id" (required string) ~doc:"STRING RecordIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_registry_record
           (Values.GetRegistryRecordRequest.make ~registryId ~recordId ())
           (Some Values.GetRegistryRecordResponse.to_json)
           (Some Values.GetRegistryRecordResponse.error_to_json)])
let get_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING BedrockAgentcoreResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_policy
           (Values.GetResourcePolicyRequest.make ~resourceArn ())
           (Some Values.GetResourcePolicyResponse.to_json)
           (Some Values.GetResourcePolicyResponse.error_to_json)])
let get_token_vault =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tokenVaultId =
         flag "token-vault-id" (optional string)
           ~doc:"STRING TokenVaultIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_token_vault
           (Values.GetTokenVaultRequest.make ?tokenVaultId ())
           (Some Values.GetTokenVaultResponse.to_json)
           (Some Values.GetTokenVaultResponse.error_to_json)])
let get_workload_identity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING WorkloadIdentityNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_workload_identity
           (Values.GetWorkloadIdentityRequest.make ~name ())
           (Some Values.GetWorkloadIdentityResponse.to_json)
           (Some Values.GetWorkloadIdentityResponse.error_to_json)])
let list_agent_runtime_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and agentRuntimeId =
         flag "agent-runtime-id" (required string)
           ~doc:"STRING AgentRuntimeId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_agent_runtime_endpoints
           (Values.ListAgentRuntimeEndpointsRequest.make ?maxResults
              ?nextToken ~agentRuntimeId ())
           (Some Values.ListAgentRuntimeEndpointsResponse.to_json)
           (Some Values.ListAgentRuntimeEndpointsResponse.error_to_json)])
let list_agent_runtime_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and agentRuntimeId =
         flag "agent-runtime-id" (required string)
           ~doc:"STRING AgentRuntimeId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_agent_runtime_versions
           (Values.ListAgentRuntimeVersionsRequest.make ?maxResults
              ?nextToken ~agentRuntimeId ())
           (Some Values.ListAgentRuntimeVersionsResponse.to_json)
           (Some Values.ListAgentRuntimeVersionsResponse.error_to_json)])
let list_agent_runtimes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_agent_runtimes
           (Values.ListAgentRuntimesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListAgentRuntimesResponse.to_json)
           (Some Values.ListAgentRuntimesResponse.error_to_json)])
let list_api_key_credential_providers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_api_key_credential_providers
           (Values.ListApiKeyCredentialProvidersRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListApiKeyCredentialProvidersResponse.to_json)
           (Some Values.ListApiKeyCredentialProvidersResponse.error_to_json)])
let list_browser_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and name =
         flag "name" (optional string) ~doc:"STRING BrowserProfileName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_browser_profiles
           (Values.ListBrowserProfilesRequest.make ?maxResults ?nextToken
              ?name ()) (Some Values.ListBrowserProfilesResponse.to_json)
           (Some Values.ListBrowserProfilesResponse.error_to_json)])
let list_browsers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON ResourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_browsers
           (Values.ListBrowsersRequest.make ?maxResults ?nextToken
              ?type_:(Option.map ~f:Values.ResourceType.of_json type_) ())
           (Some Values.ListBrowsersResponse.to_json)
           (Some Values.ListBrowsersResponse.error_to_json)])
let list_code_interpreters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON ResourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_code_interpreters
           (Values.ListCodeInterpretersRequest.make ?maxResults ?nextToken
              ?type_:(Option.map ~f:Values.ResourceType.of_json type_) ())
           (Some Values.ListCodeInterpretersResponse.to_json)
           (Some Values.ListCodeInterpretersResponse.error_to_json)])
let list_configuration_bundle_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListConfigurationBundleVersionsRequestMaxResultsInteger"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON VersionFilter"
       and bundleId =
         flag "bundle-id" (required string)
           ~doc:"STRING ConfigurationBundleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_configuration_bundle_versions
           (Values.ListConfigurationBundleVersionsRequest.make ?nextToken
              ?maxResults
              ?filter:(Option.map ~f:Values.VersionFilter.of_json filter)
              ~bundleId ())
           (Some Values.ListConfigurationBundleVersionsResponse.to_json)
           (Some Values.ListConfigurationBundleVersionsResponse.error_to_json)])
let list_configuration_bundles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListConfigurationBundlesRequestMaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_configuration_bundles
           (Values.ListConfigurationBundlesRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListConfigurationBundlesResponse.to_json)
           (Some Values.ListConfigurationBundlesResponse.error_to_json)])
let list_evaluators =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListEvaluatorsRequestMaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_evaluators
           (Values.ListEvaluatorsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListEvaluatorsResponse.to_json)
           (Some Values.ListEvaluatorsResponse.error_to_json)])
let list_gateway_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT GatewayRuleMaxResults"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING GatewayRuleNextToken"
       and gatewayIdentifier =
         flag "gateway-identifier" (required string)
           ~doc:"STRING GatewayIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_gateway_rules
           (Values.ListGatewayRulesRequest.make ?maxResults ?nextToken
              ~gatewayIdentifier ())
           (Some Values.ListGatewayRulesResponse.to_json)
           (Some Values.ListGatewayRulesResponse.error_to_json)])
let list_gateway_targets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT TargetMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING TargetNextToken"
       and gatewayIdentifier =
         flag "gateway-identifier" (required string)
           ~doc:"STRING GatewayIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_gateway_targets
           (Values.ListGatewayTargetsRequest.make ?maxResults ?nextToken
              ~gatewayIdentifier ())
           (Some Values.ListGatewayTargetsResponse.to_json)
           (Some Values.ListGatewayTargetsResponse.error_to_json)])
let list_gateways =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT GatewayMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING GatewayNextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_gateways
           (Values.ListGatewaysRequest.make ?maxResults ?nextToken ())
           (Some Values.ListGatewaysResponse.to_json)
           (Some Values.ListGatewaysResponse.error_to_json)])
let list_harnesses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_harnesses
           (Values.ListHarnessesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListHarnessesResponse.to_json)
           (Some Values.ListHarnessesResponse.error_to_json)])
let list_memories =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListMemoriesInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_memories
           (Values.ListMemoriesInput.make ?maxResults ?nextToken ())
           (Some Values.ListMemoriesOutput.to_json)
           (Some Values.ListMemoriesOutput.error_to_json)])
let list_oauth2_credential_providers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListOauth2CredentialProvidersRequestMaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_oauth2_credential_providers
           (Values.ListOauth2CredentialProvidersRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListOauth2CredentialProvidersResponse.to_json)
           (Some Values.ListOauth2CredentialProvidersResponse.error_to_json)])
let list_online_evaluation_configs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListOnlineEvaluationConfigsRequestMaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_online_evaluation_configs
           (Values.ListOnlineEvaluationConfigsRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListOnlineEvaluationConfigsResponse.to_json)
           (Some Values.ListOnlineEvaluationConfigsResponse.error_to_json)])
let list_payment_connectors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and paymentManagerId =
         flag "payment-manager-id" (required string)
           ~doc:"STRING PaymentManagerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_payment_connectors
           (Values.ListPaymentConnectorsRequest.make ?maxResults ?nextToken
              ~paymentManagerId ())
           (Some Values.ListPaymentConnectorsResponse.to_json)
           (Some Values.ListPaymentConnectorsResponse.error_to_json)])
let list_payment_credential_providers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListPaymentCredentialProvidersRequestMaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_payment_credential_providers
           (Values.ListPaymentCredentialProvidersRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListPaymentCredentialProvidersResponse.to_json)
           (Some Values.ListPaymentCredentialProvidersResponse.error_to_json)])
let list_payment_managers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_payment_managers
           (Values.ListPaymentManagersRequest.make ?maxResults ?nextToken ())
           (Some Values.ListPaymentManagersResponse.to_json)
           (Some Values.ListPaymentManagersResponse.error_to_json)])
let list_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and targetResourceScope =
         flag "target-resource-scope" (optional string)
           ~doc:"STRING BedrockAgentcoreResourceArn"
       and policyEngineId =
         flag "policy-engine-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_policies
           (Values.ListPoliciesRequest.make ?nextToken ?maxResults
              ?targetResourceScope ~policyEngineId ())
           (Some Values.ListPoliciesResponse.to_json)
           (Some Values.ListPoliciesResponse.error_to_json)])
let list_policy_engine_summaries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_policy_engine_summaries
           (Values.ListPolicyEngineSummariesRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListPolicyEngineSummariesResponse.to_json)
           (Some Values.ListPolicyEngineSummariesResponse.error_to_json)])
let list_policy_engines =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_policy_engines
           (Values.ListPolicyEnginesRequest.make ?nextToken ?maxResults ())
           (Some Values.ListPolicyEnginesResponse.to_json)
           (Some Values.ListPolicyEnginesResponse.error_to_json)])
let list_policy_generation_assets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and policyGenerationId =
         flag "policy-generation-id" (required string)
           ~doc:"STRING ResourceId"
       and policyEngineId =
         flag "policy-engine-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_policy_generation_assets
           (Values.ListPolicyGenerationAssetsRequest.make ?nextToken
              ?maxResults ~policyGenerationId ~policyEngineId ())
           (Some Values.ListPolicyGenerationAssetsResponse.to_json)
           (Some Values.ListPolicyGenerationAssetsResponse.error_to_json)])
let list_policy_generation_summaries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and policyEngineId =
         flag "policy-engine-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_policy_generation_summaries
           (Values.ListPolicyGenerationSummariesRequest.make ?nextToken
              ?maxResults ~policyEngineId ())
           (Some Values.ListPolicyGenerationSummariesResponse.to_json)
           (Some Values.ListPolicyGenerationSummariesResponse.error_to_json)])
let list_policy_generations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and policyEngineId =
         flag "policy-engine-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_policy_generations
           (Values.ListPolicyGenerationsRequest.make ?nextToken ?maxResults
              ~policyEngineId ())
           (Some Values.ListPolicyGenerationsResponse.to_json)
           (Some Values.ListPolicyGenerationsResponse.error_to_json)])
let list_policy_summaries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and targetResourceScope =
         flag "target-resource-scope" (optional string)
           ~doc:"STRING BedrockAgentcoreResourceArn"
       and policyEngineId =
         flag "policy-engine-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_policy_summaries
           (Values.ListPolicySummariesRequest.make ?nextToken ?maxResults
              ?targetResourceScope ~policyEngineId ())
           (Some Values.ListPolicySummariesResponse.to_json)
           (Some Values.ListPolicySummariesResponse.error_to_json)])
let list_registries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON RegistryStatus"
       and authorizerType =
         flag "authorizer-type" (optional json_arg)
           ~doc:"JSON RegistryAuthorizerType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_registries
           (Values.ListRegistriesRequest.make ?maxResults ?nextToken
              ?status:(Option.map ~f:Values.RegistryStatus.of_json status)
              ?authorizerType:(Option.map
                                 ~f:Values.RegistryAuthorizerType.of_json
                                 authorizerType) ())
           (Some Values.ListRegistriesResponse.to_json)
           (Some Values.ListRegistriesResponse.error_to_json)])
let list_registry_records =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and name =
         flag "name" (optional string) ~doc:"STRING RegistryRecordName"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON RegistryRecordStatus"
       and descriptorType =
         flag "descriptor-type" (optional json_arg)
           ~doc:"JSON DescriptorType"
       and registryId =
         flag "registry-id" (required string)
           ~doc:"STRING RegistryIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_registry_records
           (Values.ListRegistryRecordsRequest.make ?maxResults ?nextToken
              ?name
              ?status:(Option.map ~f:Values.RegistryRecordStatus.of_json
                         status)
              ?descriptorType:(Option.map ~f:Values.DescriptorType.of_json
                                 descriptorType) ~registryId ())
           (Some Values.ListRegistryRecordsResponse.to_json)
           (Some Values.ListRegistryRecordsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING TaggableResourcesArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_workload_identities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListWorkloadIdentitiesRequestMaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_workload_identities
           (Values.ListWorkloadIdentitiesRequest.make ?nextToken ?maxResults
              ()) (Some Values.ListWorkloadIdentitiesResponse.to_json)
           (Some Values.ListWorkloadIdentitiesResponse.error_to_json)])
let put_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING BedrockAgentcoreResourceArn"
       and policy =
         flag "policy" (required string) ~doc:"STRING ResourcePolicyBody" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_resource_policy
           (Values.PutResourcePolicyRequest.make ~resourceArn ~policy ())
           (Some Values.PutResourcePolicyResponse.to_json)
           (Some Values.PutResourcePolicyResponse.error_to_json)])
let set_token_vault_c_m_k =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tokenVaultId =
         flag "token-vault-id" (optional string)
           ~doc:"STRING TokenVaultIdType"
       and kmsConfiguration =
         flag "kms-configuration" (required json_arg)
           ~doc:"JSON KmsConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_token_vault_c_m_k
           (Values.SetTokenVaultCMKRequest.make ?tokenVaultId
              ~kmsConfiguration:(Values.KmsConfiguration.of_json
                                   kmsConfiguration) ())
           (Some Values.SetTokenVaultCMKResponse.to_json)
           (Some Values.SetTokenVaultCMKResponse.error_to_json)])
let start_policy_generation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and policyEngineId =
         flag "policy-engine-id" (required string) ~doc:"STRING ResourceId"
       and resource =
         flag "resource" (required json_arg) ~doc:"JSON Resource"
       and content = flag "content" (required json_arg) ~doc:"JSON Content"
       and name =
         flag "name" (required string) ~doc:"STRING PolicyGenerationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_policy_generation
           (Values.StartPolicyGenerationRequest.make ?clientToken
              ~policyEngineId ~resource:(Values.Resource.of_json resource)
              ~content:(Values.Content.of_json content) ~name ())
           (Some Values.StartPolicyGenerationResponse.to_json)
           (Some Values.StartPolicyGenerationResponse.error_to_json)])
let submit_registry_record_for_approval =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (required string)
           ~doc:"STRING RegistryIdentifier"
       and recordId =
         flag "record-id" (required string) ~doc:"STRING RecordIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.submit_registry_record_for_approval
           (Values.SubmitRegistryRecordForApprovalRequest.make ~registryId
              ~recordId ())
           (Some Values.SubmitRegistryRecordForApprovalResponse.to_json)
           (Some Values.SubmitRegistryRecordForApprovalResponse.error_to_json)])
let synchronize_gateway_targets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayIdentifier =
         flag "gateway-identifier" (required string)
           ~doc:"STRING GatewayIdentifier"
       and targetIdList =
         flag "target-id-list" (required json_arg) ~doc:"JSON TargetIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.synchronize_gateway_targets
           (Values.SynchronizeGatewayTargetsRequest.make ~gatewayIdentifier
              ~targetIdList:(Values.TargetIdList.of_json targetIdList) ())
           (Some Values.SynchronizeGatewayTargetsResponse.to_json)
           (Some Values.SynchronizeGatewayTargetsResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING TaggableResourcesArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagsMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING TaggableResourcesArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_agent_runtime =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and authorizerConfiguration =
         flag "authorizer-configuration" (optional json_arg)
           ~doc:"JSON AuthorizerConfiguration"
       and requestHeaderConfiguration =
         flag "request-header-configuration" (optional json_arg)
           ~doc:"JSON RequestHeaderConfiguration"
       and protocolConfiguration =
         flag "protocol-configuration" (optional json_arg)
           ~doc:"JSON ProtocolConfiguration"
       and lifecycleConfiguration =
         flag "lifecycle-configuration" (optional json_arg)
           ~doc:"JSON LifecycleConfiguration"
       and metadataConfiguration =
         flag "metadata-configuration" (optional json_arg)
           ~doc:"JSON RuntimeMetadataConfiguration"
       and environmentVariables =
         flag "environment-variables" (optional json_arg)
           ~doc:"JSON EnvironmentVariablesMap"
       and filesystemConfigurations =
         flag "filesystem-configurations" (optional json_arg)
           ~doc:"JSON FilesystemConfigurations"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and agentRuntimeId =
         flag "agent-runtime-id" (required string)
           ~doc:"STRING AgentRuntimeId"
       and agentRuntimeArtifact =
         flag "agent-runtime-artifact" (required json_arg)
           ~doc:"JSON AgentRuntimeArtifact"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn"
       and networkConfiguration =
         flag "network-configuration" (required json_arg)
           ~doc:"JSON NetworkConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_agent_runtime
           (Values.UpdateAgentRuntimeRequest.make ?description
              ?authorizerConfiguration:(Option.map
                                          ~f:Values.AuthorizerConfiguration.of_json
                                          authorizerConfiguration)
              ?requestHeaderConfiguration:(Option.map
                                             ~f:Values.RequestHeaderConfiguration.of_json
                                             requestHeaderConfiguration)
              ?protocolConfiguration:(Option.map
                                        ~f:Values.ProtocolConfiguration.of_json
                                        protocolConfiguration)
              ?lifecycleConfiguration:(Option.map
                                         ~f:Values.LifecycleConfiguration.of_json
                                         lifecycleConfiguration)
              ?metadataConfiguration:(Option.map
                                        ~f:Values.RuntimeMetadataConfiguration.of_json
                                        metadataConfiguration)
              ?environmentVariables:(Option.map
                                       ~f:Values.EnvironmentVariablesMap.of_json
                                       environmentVariables)
              ?filesystemConfigurations:(Option.map
                                           ~f:Values.FilesystemConfigurations.of_json
                                           filesystemConfigurations)
              ?clientToken ~agentRuntimeId
              ~agentRuntimeArtifact:(Values.AgentRuntimeArtifact.of_json
                                       agentRuntimeArtifact) ~roleArn
              ~networkConfiguration:(Values.NetworkConfiguration.of_json
                                       networkConfiguration) ())
           (Some Values.UpdateAgentRuntimeResponse.to_json)
           (Some Values.UpdateAgentRuntimeResponse.error_to_json)])
let update_agent_runtime_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentRuntimeVersion =
         flag "agent-runtime-version" (optional string)
           ~doc:"STRING AgentRuntimeVersion"
       and description =
         flag "description" (optional string)
           ~doc:"STRING AgentEndpointDescription"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and agentRuntimeId =
         flag "agent-runtime-id" (required string)
           ~doc:"STRING AgentRuntimeId"
       and endpointName =
         flag "endpoint-name" (required string) ~doc:"STRING EndpointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_agent_runtime_endpoint
           (Values.UpdateAgentRuntimeEndpointRequest.make
              ?agentRuntimeVersion ?description ?clientToken ~agentRuntimeId
              ~endpointName ())
           (Some Values.UpdateAgentRuntimeEndpointResponse.to_json)
           (Some Values.UpdateAgentRuntimeEndpointResponse.error_to_json)])
let update_api_key_credential_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING CredentialProviderName"
       and apiKey = flag "api-key" (required string) ~doc:"STRING ApiKeyType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_api_key_credential_provider
           (Values.UpdateApiKeyCredentialProviderRequest.make ~name ~apiKey
              ())
           (Some Values.UpdateApiKeyCredentialProviderResponse.to_json)
           (Some Values.UpdateApiKeyCredentialProviderResponse.error_to_json)])
let update_configuration_bundle =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and bundleName =
         flag "bundle-name" (optional string)
           ~doc:"STRING ConfigurationBundleName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ConfigurationBundleDescription"
       and components =
         flag "components" (optional json_arg)
           ~doc:"JSON ComponentConfigurationMap"
       and parentVersionIds =
         flag "parent-version-ids" (optional json_arg)
           ~doc:"JSON ConfigurationBundleVersionList"
       and branchName =
         flag "branch-name" (optional string) ~doc:"STRING BranchName"
       and commitMessage =
         flag "commit-message" (optional string)
           ~doc:"STRING UpdateConfigurationBundleRequestCommitMessageString"
       and createdBy =
         flag "created-by" (optional json_arg)
           ~doc:"JSON VersionCreatedBySource"
       and bundleId =
         flag "bundle-id" (required string)
           ~doc:"STRING ConfigurationBundleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_configuration_bundle
           (Values.UpdateConfigurationBundleRequest.make ?clientToken
              ?bundleName ?description
              ?components:(Option.map
                             ~f:Values.ComponentConfigurationMap.of_json
                             components)
              ?parentVersionIds:(Option.map
                                   ~f:Values.ConfigurationBundleVersionList.of_json
                                   parentVersionIds) ?branchName
              ?commitMessage
              ?createdBy:(Option.map ~f:Values.VersionCreatedBySource.of_json
                            createdBy) ~bundleId ())
           (Some Values.UpdateConfigurationBundleResponse.to_json)
           (Some Values.UpdateConfigurationBundleResponse.error_to_json)])
let update_evaluator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string)
           ~doc:"STRING EvaluatorDescription"
       and evaluatorConfig =
         flag "evaluator-config" (optional json_arg)
           ~doc:"JSON EvaluatorConfig"
       and level =
         flag "level" (optional json_arg) ~doc:"JSON EvaluatorLevel"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and evaluatorId =
         flag "evaluator-id" (required string) ~doc:"STRING EvaluatorId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_evaluator
           (Values.UpdateEvaluatorRequest.make ?clientToken ?description
              ?evaluatorConfig:(Option.map ~f:Values.EvaluatorConfig.of_json
                                  evaluatorConfig)
              ?level:(Option.map ~f:Values.EvaluatorLevel.of_json level)
              ?kmsKeyArn ~evaluatorId ())
           (Some Values.UpdateEvaluatorResponse.to_json)
           (Some Values.UpdateEvaluatorResponse.error_to_json)])
let update_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING GatewayDescription"
       and protocolType =
         flag "protocol-type" (optional json_arg)
           ~doc:"JSON GatewayProtocolType"
       and protocolConfiguration =
         flag "protocol-configuration" (optional json_arg)
           ~doc:"JSON GatewayProtocolConfiguration"
       and authorizerConfiguration =
         flag "authorizer-configuration" (optional json_arg)
           ~doc:"JSON AuthorizerConfiguration"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and interceptorConfigurations =
         flag "interceptor-configurations" (optional json_arg)
           ~doc:"JSON GatewayInterceptorConfigurations"
       and policyEngineConfiguration =
         flag "policy-engine-configuration" (optional json_arg)
           ~doc:"JSON GatewayPolicyEngineConfiguration"
       and exceptionLevel =
         flag "exception-level" (optional json_arg)
           ~doc:"JSON ExceptionLevel"
       and gatewayIdentifier =
         flag "gateway-identifier" (required string)
           ~doc:"STRING GatewayIdentifier"
       and name = flag "name" (required string) ~doc:"STRING GatewayName"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn"
       and authorizerType =
         flag "authorizer-type" (required json_arg)
           ~doc:"JSON AuthorizerType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_gateway
           (Values.UpdateGatewayRequest.make ?description
              ?protocolType:(Option.map ~f:Values.GatewayProtocolType.of_json
                               protocolType)
              ?protocolConfiguration:(Option.map
                                        ~f:Values.GatewayProtocolConfiguration.of_json
                                        protocolConfiguration)
              ?authorizerConfiguration:(Option.map
                                          ~f:Values.AuthorizerConfiguration.of_json
                                          authorizerConfiguration) ?kmsKeyArn
              ?interceptorConfigurations:(Option.map
                                            ~f:Values.GatewayInterceptorConfigurations.of_json
                                            interceptorConfigurations)
              ?policyEngineConfiguration:(Option.map
                                            ~f:Values.GatewayPolicyEngineConfiguration.of_json
                                            policyEngineConfiguration)
              ?exceptionLevel:(Option.map ~f:Values.ExceptionLevel.of_json
                                 exceptionLevel) ~gatewayIdentifier ~name
              ~roleArn
              ~authorizerType:(Values.AuthorizerType.of_json authorizerType)
              ()) (Some Values.UpdateGatewayResponse.to_json)
           (Some Values.UpdateGatewayResponse.error_to_json)])
let update_gateway_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and priority =
         flag "priority" (optional int) ~doc:"INT GatewayRulePriority"
       and conditions =
         flag "conditions" (optional json_arg) ~doc:"JSON Conditions"
       and actions = flag "actions" (optional json_arg) ~doc:"JSON Actions"
       and description =
         flag "description" (optional string)
           ~doc:"STRING GatewayRuleDescription"
       and gatewayIdentifier =
         flag "gateway-identifier" (required string)
           ~doc:"STRING GatewayIdentifier"
       and ruleId =
         flag "rule-id" (required string) ~doc:"STRING GatewayRuleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_gateway_rule
           (Values.UpdateGatewayRuleRequest.make ?priority
              ?conditions:(Option.map ~f:Values.Conditions.of_json conditions)
              ?actions:(Option.map ~f:Values.Actions.of_json actions)
              ?description ~gatewayIdentifier ~ruleId ())
           (Some Values.UpdateGatewayRuleResponse.to_json)
           (Some Values.UpdateGatewayRuleResponse.error_to_json)])
let update_gateway_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING TargetDescription"
       and credentialProviderConfigurations =
         flag "credential-provider-configurations" (optional json_arg)
           ~doc:"JSON CredentialProviderConfigurations"
       and metadataConfiguration =
         flag "metadata-configuration" (optional json_arg)
           ~doc:"JSON MetadataConfiguration"
       and privateEndpoint =
         flag "private-endpoint" (optional json_arg)
           ~doc:"JSON PrivateEndpoint"
       and gatewayIdentifier =
         flag "gateway-identifier" (required string)
           ~doc:"STRING GatewayIdentifier"
       and targetId =
         flag "target-id" (required string) ~doc:"STRING TargetId"
       and name = flag "name" (required string) ~doc:"STRING TargetName"
       and targetConfiguration =
         flag "target-configuration" (required json_arg)
           ~doc:"JSON TargetConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_gateway_target
           (Values.UpdateGatewayTargetRequest.make ?description
              ?credentialProviderConfigurations:(Option.map
                                                   ~f:Values.CredentialProviderConfigurations.of_json
                                                   credentialProviderConfigurations)
              ?metadataConfiguration:(Option.map
                                        ~f:Values.MetadataConfiguration.of_json
                                        metadataConfiguration)
              ?privateEndpoint:(Option.map ~f:Values.PrivateEndpoint.of_json
                                  privateEndpoint) ~gatewayIdentifier
              ~targetId ~name
              ~targetConfiguration:(Values.TargetConfiguration.of_json
                                      targetConfiguration) ())
           (Some Values.UpdateGatewayTargetResponse.to_json)
           (Some Values.UpdateGatewayTargetResponse.error_to_json)])
let update_harness =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and executionRoleArn =
         flag "execution-role-arn" (optional string) ~doc:"STRING RoleArn"
       and environment =
         flag "environment" (optional json_arg)
           ~doc:"JSON HarnessEnvironmentProviderRequest"
       and environmentArtifact =
         flag "environment-artifact" (optional json_arg)
           ~doc:"JSON UpdatedHarnessEnvironmentArtifact"
       and environmentVariables =
         flag "environment-variables" (optional json_arg)
           ~doc:"JSON EnvironmentVariablesMap"
       and authorizerConfiguration =
         flag "authorizer-configuration" (optional json_arg)
           ~doc:"JSON UpdatedAuthorizerConfiguration"
       and model =
         flag "model" (optional json_arg)
           ~doc:"JSON HarnessModelConfiguration"
       and systemPrompt =
         flag "system-prompt" (optional json_arg)
           ~doc:"JSON HarnessSystemPrompt"
       and tools = flag "tools" (optional json_arg) ~doc:"JSON HarnessTools"
       and skills =
         flag "skills" (optional json_arg) ~doc:"JSON HarnessSkills"
       and allowedTools =
         flag "allowed-tools" (optional json_arg)
           ~doc:"JSON HarnessAllowedTools"
       and memory =
         flag "memory" (optional json_arg)
           ~doc:"JSON UpdatedHarnessMemoryConfiguration"
       and truncation =
         flag "truncation" (optional json_arg)
           ~doc:"JSON HarnessTruncationConfiguration"
       and maxIterations =
         flag "max-iterations" (optional int) ~doc:"INT Integer"
       and maxTokens = flag "max-tokens" (optional int) ~doc:"INT Integer"
       and timeoutSeconds =
         flag "timeout-seconds" (optional int) ~doc:"INT Integer"
       and harnessId =
         flag "harness-id" (required string) ~doc:"STRING HarnessId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_harness
           (Values.UpdateHarnessRequest.make ?clientToken ?executionRoleArn
              ?environment:(Option.map
                              ~f:Values.HarnessEnvironmentProviderRequest.of_json
                              environment)
              ?environmentArtifact:(Option.map
                                      ~f:Values.UpdatedHarnessEnvironmentArtifact.of_json
                                      environmentArtifact)
              ?environmentVariables:(Option.map
                                       ~f:Values.EnvironmentVariablesMap.of_json
                                       environmentVariables)
              ?authorizerConfiguration:(Option.map
                                          ~f:Values.UpdatedAuthorizerConfiguration.of_json
                                          authorizerConfiguration)
              ?model:(Option.map ~f:Values.HarnessModelConfiguration.of_json
                        model)
              ?systemPrompt:(Option.map ~f:Values.HarnessSystemPrompt.of_json
                               systemPrompt)
              ?tools:(Option.map ~f:Values.HarnessTools.of_json tools)
              ?skills:(Option.map ~f:Values.HarnessSkills.of_json skills)
              ?allowedTools:(Option.map ~f:Values.HarnessAllowedTools.of_json
                               allowedTools)
              ?memory:(Option.map
                         ~f:Values.UpdatedHarnessMemoryConfiguration.of_json
                         memory)
              ?truncation:(Option.map
                             ~f:Values.HarnessTruncationConfiguration.of_json
                             truncation) ?maxIterations ?maxTokens
              ?timeoutSeconds ~harnessId ())
           (Some Values.UpdateHarnessResponse.to_json)
           (Some Values.UpdateHarnessResponse.error_to_json)])
let update_memory =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING UpdateMemoryInputClientTokenString"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and eventExpiryDuration =
         flag "event-expiry-duration" (optional int)
           ~doc:"INT UpdateMemoryInputEventExpiryDurationInteger"
       and memoryExecutionRoleArn =
         flag "memory-execution-role-arn" (optional string) ~doc:"STRING Arn"
       and memoryStrategies =
         flag "memory-strategies" (optional json_arg)
           ~doc:"JSON ModifyMemoryStrategies"
       and addIndexedKeys =
         flag "add-indexed-keys" (optional json_arg)
           ~doc:"JSON IndexedKeysList"
       and streamDeliveryResources =
         flag "stream-delivery-resources" (optional json_arg)
           ~doc:"JSON StreamDeliveryResources"
       and memoryId =
         flag "memory-id" (required string) ~doc:"STRING MemoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_memory
           (Values.UpdateMemoryInput.make ?clientToken ?description
              ?eventExpiryDuration ?memoryExecutionRoleArn
              ?memoryStrategies:(Option.map
                                   ~f:Values.ModifyMemoryStrategies.of_json
                                   memoryStrategies)
              ?addIndexedKeys:(Option.map ~f:Values.IndexedKeysList.of_json
                                 addIndexedKeys)
              ?streamDeliveryResources:(Option.map
                                          ~f:Values.StreamDeliveryResources.of_json
                                          streamDeliveryResources) ~memoryId
              ()) (Some Values.UpdateMemoryOutput.to_json)
           (Some Values.UpdateMemoryOutput.error_to_json)])
let update_oauth2_credential_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING CredentialProviderName"
       and credentialProviderVendor =
         flag "credential-provider-vendor" (required json_arg)
           ~doc:"JSON CredentialProviderVendorType"
       and oauth2ProviderConfigInput =
         flag "oauth2-provider-config-input" (required json_arg)
           ~doc:"JSON Oauth2ProviderConfigInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_oauth2_credential_provider
           (Values.UpdateOauth2CredentialProviderRequest.make ~name
              ~credentialProviderVendor:(Values.CredentialProviderVendorType.of_json
                                           credentialProviderVendor)
              ~oauth2ProviderConfigInput:(Values.Oauth2ProviderConfigInput.of_json
                                            oauth2ProviderConfigInput) ())
           (Some Values.UpdateOauth2CredentialProviderResponse.to_json)
           (Some Values.UpdateOauth2CredentialProviderResponse.error_to_json)])
let update_online_evaluation_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string)
           ~doc:"STRING EvaluationConfigDescription"
       and rule = flag "rule" (optional json_arg) ~doc:"JSON Rule"
       and dataSourceConfig =
         flag "data-source-config" (optional json_arg)
           ~doc:"JSON DataSourceConfig"
       and evaluators =
         flag "evaluators" (optional json_arg) ~doc:"JSON EvaluatorList"
       and evaluationExecutionRoleArn =
         flag "evaluation-execution-role-arn" (optional string)
           ~doc:"STRING RoleArn"
       and executionStatus =
         flag "execution-status" (optional json_arg)
           ~doc:"JSON OnlineEvaluationExecutionStatus"
       and onlineEvaluationConfigId =
         flag "online-evaluation-config-id" (required string)
           ~doc:"STRING OnlineEvaluationConfigId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_online_evaluation_config
           (Values.UpdateOnlineEvaluationConfigRequest.make ?clientToken
              ?description ?rule:(Option.map ~f:Values.Rule.of_json rule)
              ?dataSourceConfig:(Option.map
                                   ~f:Values.DataSourceConfig.of_json
                                   dataSourceConfig)
              ?evaluators:(Option.map ~f:Values.EvaluatorList.of_json
                             evaluators) ?evaluationExecutionRoleArn
              ?executionStatus:(Option.map
                                  ~f:Values.OnlineEvaluationExecutionStatus.of_json
                                  executionStatus) ~onlineEvaluationConfigId
              ()) (Some Values.UpdateOnlineEvaluationConfigResponse.to_json)
           (Some Values.UpdateOnlineEvaluationConfigResponse.error_to_json)])
let update_payment_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING PaymentsDescription"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON PaymentConnectorType"
       and credentialProviderConfigurations =
         flag "credential-provider-configurations" (optional json_arg)
           ~doc:"JSON CredentialsProviderConfigurations"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and paymentManagerId =
         flag "payment-manager-id" (required string)
           ~doc:"STRING PaymentManagerId"
       and paymentConnectorId =
         flag "payment-connector-id" (required string)
           ~doc:"STRING PaymentConnectorId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_payment_connector
           (Values.UpdatePaymentConnectorRequest.make ?description
              ?type_:(Option.map ~f:Values.PaymentConnectorType.of_json type_)
              ?credentialProviderConfigurations:(Option.map
                                                   ~f:Values.CredentialsProviderConfigurations.of_json
                                                   credentialProviderConfigurations)
              ?clientToken ~paymentManagerId ~paymentConnectorId ())
           (Some Values.UpdatePaymentConnectorResponse.to_json)
           (Some Values.UpdatePaymentConnectorResponse.error_to_json)])
let update_payment_credential_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING CredentialProviderName"
       and credentialProviderVendor =
         flag "credential-provider-vendor" (required json_arg)
           ~doc:"JSON PaymentCredentialProviderVendorType"
       and providerConfigurationInput =
         flag "provider-configuration-input" (required json_arg)
           ~doc:"JSON PaymentProviderConfigurationInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_payment_credential_provider
           (Values.UpdatePaymentCredentialProviderRequest.make ~name
              ~credentialProviderVendor:(Values.PaymentCredentialProviderVendorType.of_json
                                           credentialProviderVendor)
              ~providerConfigurationInput:(Values.PaymentProviderConfigurationInput.of_json
                                             providerConfigurationInput) ())
           (Some Values.UpdatePaymentCredentialProviderResponse.to_json)
           (Some Values.UpdatePaymentCredentialProviderResponse.error_to_json)])
let update_payment_manager =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING PaymentsDescription"
       and authorizerType =
         flag "authorizer-type" (optional json_arg)
           ~doc:"JSON PaymentsAuthorizerType"
       and authorizerConfiguration =
         flag "authorizer-configuration" (optional json_arg)
           ~doc:"JSON AuthorizerConfiguration"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and paymentManagerId =
         flag "payment-manager-id" (required string)
           ~doc:"STRING PaymentManagerId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_payment_manager
           (Values.UpdatePaymentManagerRequest.make ?description
              ?authorizerType:(Option.map
                                 ~f:Values.PaymentsAuthorizerType.of_json
                                 authorizerType)
              ?authorizerConfiguration:(Option.map
                                          ~f:Values.AuthorizerConfiguration.of_json
                                          authorizerConfiguration) ?roleArn
              ?clientToken ~paymentManagerId ())
           (Some Values.UpdatePaymentManagerResponse.to_json)
           (Some Values.UpdatePaymentManagerResponse.error_to_json)])
let update_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional json_arg)
           ~doc:"JSON UpdatedDescription"
       and definition =
         flag "definition" (optional json_arg) ~doc:"JSON PolicyDefinition"
       and validationMode =
         flag "validation-mode" (optional json_arg)
           ~doc:"JSON PolicyValidationMode"
       and policyEngineId =
         flag "policy-engine-id" (required string) ~doc:"STRING ResourceId"
       and policyId =
         flag "policy-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_policy
           (Values.UpdatePolicyRequest.make
              ?description:(Option.map ~f:Values.UpdatedDescription.of_json
                              description)
              ?definition:(Option.map ~f:Values.PolicyDefinition.of_json
                             definition)
              ?validationMode:(Option.map
                                 ~f:Values.PolicyValidationMode.of_json
                                 validationMode) ~policyEngineId ~policyId ())
           (Some Values.UpdatePolicyResponse.to_json)
           (Some Values.UpdatePolicyResponse.error_to_json)])
let update_policy_engine =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional json_arg)
           ~doc:"JSON UpdatedDescription"
       and policyEngineId =
         flag "policy-engine-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_policy_engine
           (Values.UpdatePolicyEngineRequest.make
              ?description:(Option.map ~f:Values.UpdatedDescription.of_json
                              description) ~policyEngineId ())
           (Some Values.UpdatePolicyEngineResponse.to_json)
           (Some Values.UpdatePolicyEngineResponse.error_to_json)])
let update_registry =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING RegistryName"
       and description =
         flag "description" (optional json_arg)
           ~doc:"JSON UpdatedDescription"
       and authorizerConfiguration =
         flag "authorizer-configuration" (optional json_arg)
           ~doc:"JSON UpdatedAuthorizerConfiguration"
       and approvalConfiguration =
         flag "approval-configuration" (optional json_arg)
           ~doc:"JSON UpdatedApprovalConfiguration"
       and registryId =
         flag "registry-id" (required string)
           ~doc:"STRING RegistryIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_registry
           (Values.UpdateRegistryRequest.make ?name
              ?description:(Option.map ~f:Values.UpdatedDescription.of_json
                              description)
              ?authorizerConfiguration:(Option.map
                                          ~f:Values.UpdatedAuthorizerConfiguration.of_json
                                          authorizerConfiguration)
              ?approvalConfiguration:(Option.map
                                        ~f:Values.UpdatedApprovalConfiguration.of_json
                                        approvalConfiguration) ~registryId ())
           (Some Values.UpdateRegistryResponse.to_json)
           (Some Values.UpdateRegistryResponse.error_to_json)])
let update_registry_record =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING RegistryRecordName"
       and description =
         flag "description" (optional json_arg)
           ~doc:"JSON UpdatedDescription"
       and descriptorType =
         flag "descriptor-type" (optional json_arg)
           ~doc:"JSON DescriptorType"
       and descriptors =
         flag "descriptors" (optional json_arg)
           ~doc:"JSON UpdatedDescriptors"
       and recordVersion =
         flag "record-version" (optional string)
           ~doc:"STRING RegistryRecordVersion"
       and synchronizationType =
         flag "synchronization-type" (optional json_arg)
           ~doc:"JSON UpdatedSynchronizationType"
       and synchronizationConfiguration =
         flag "synchronization-configuration" (optional json_arg)
           ~doc:"JSON UpdatedSynchronizationConfiguration"
       and triggerSynchronization =
         flag "trigger-synchronization" (optional bool) ~doc:"BOOL Boolean"
       and registryId =
         flag "registry-id" (required string)
           ~doc:"STRING RegistryIdentifier"
       and recordId =
         flag "record-id" (required string) ~doc:"STRING RecordIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_registry_record
           (Values.UpdateRegistryRecordRequest.make ?name
              ?description:(Option.map ~f:Values.UpdatedDescription.of_json
                              description)
              ?descriptorType:(Option.map ~f:Values.DescriptorType.of_json
                                 descriptorType)
              ?descriptors:(Option.map ~f:Values.UpdatedDescriptors.of_json
                              descriptors) ?recordVersion
              ?synchronizationType:(Option.map
                                      ~f:Values.UpdatedSynchronizationType.of_json
                                      synchronizationType)
              ?synchronizationConfiguration:(Option.map
                                               ~f:Values.UpdatedSynchronizationConfiguration.of_json
                                               synchronizationConfiguration)
              ?triggerSynchronization ~registryId ~recordId ())
           (Some Values.UpdateRegistryRecordResponse.to_json)
           (Some Values.UpdateRegistryRecordResponse.error_to_json)])
let update_registry_record_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and registryId =
         flag "registry-id" (required string)
           ~doc:"STRING RegistryIdentifier"
       and recordId =
         flag "record-id" (required string) ~doc:"STRING RecordIdentifier"
       and status =
         flag "status" (required json_arg) ~doc:"JSON RegistryRecordStatus"
       and statusReason =
         flag "status-reason" (required string)
           ~doc:"STRING UpdateRegistryRecordStatusRequestStatusReasonString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_registry_record_status
           (Values.UpdateRegistryRecordStatusRequest.make ~registryId
              ~recordId ~status:(Values.RegistryRecordStatus.of_json status)
              ~statusReason ())
           (Some Values.UpdateRegistryRecordStatusResponse.to_json)
           (Some Values.UpdateRegistryRecordStatusResponse.error_to_json)])
let update_workload_identity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and allowedResourceOauth2ReturnUrls =
         flag "allowed-resource-oauth2-return-urls" (optional json_arg)
           ~doc:"JSON ResourceOauth2ReturnUrlListType"
       and name =
         flag "name" (required string) ~doc:"STRING WorkloadIdentityNameType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_workload_identity
           (Values.UpdateWorkloadIdentityRequest.make
              ?allowedResourceOauth2ReturnUrls:(Option.map
                                                  ~f:Values.ResourceOauth2ReturnUrlListType.of_json
                                                  allowedResourceOauth2ReturnUrls)
              ~name ()) (Some Values.UpdateWorkloadIdentityResponse.to_json)
           (Some Values.UpdateWorkloadIdentityResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-agent-runtime", create_agent_runtime);
    ("create-agent-runtime-endpoint", create_agent_runtime_endpoint);
    ("create-api-key-credential-provider",
      create_api_key_credential_provider);
    ("create-browser", create_browser);
    ("create-browser-profile", create_browser_profile);
    ("create-code-interpreter", create_code_interpreter);
    ("create-configuration-bundle", create_configuration_bundle);
    ("create-evaluator", create_evaluator);
    ("create-gateway", create_gateway);
    ("create-gateway-rule", create_gateway_rule);
    ("create-gateway-target", create_gateway_target);
    ("create-harness", create_harness);
    ("create-memory", create_memory);
    ("create-oauth2-credential-provider", create_oauth2_credential_provider);
    ("create-online-evaluation-config", create_online_evaluation_config);
    ("create-payment-connector", create_payment_connector);
    ("create-payment-credential-provider",
      create_payment_credential_provider);
    ("create-payment-manager", create_payment_manager);
    ("create-policy", create_policy);
    ("create-policy-engine", create_policy_engine);
    ("create-registry", create_registry);
    ("create-registry-record", create_registry_record);
    ("create-workload-identity", create_workload_identity);
    ("delete-agent-runtime", delete_agent_runtime);
    ("delete-agent-runtime-endpoint", delete_agent_runtime_endpoint);
    ("delete-api-key-credential-provider",
      delete_api_key_credential_provider);
    ("delete-browser", delete_browser);
    ("delete-browser-profile", delete_browser_profile);
    ("delete-code-interpreter", delete_code_interpreter);
    ("delete-configuration-bundle", delete_configuration_bundle);
    ("delete-evaluator", delete_evaluator);
    ("delete-gateway", delete_gateway);
    ("delete-gateway-rule", delete_gateway_rule);
    ("delete-gateway-target", delete_gateway_target);
    ("delete-harness", delete_harness);
    ("delete-memory", delete_memory);
    ("delete-oauth2-credential-provider", delete_oauth2_credential_provider);
    ("delete-online-evaluation-config", delete_online_evaluation_config);
    ("delete-payment-connector", delete_payment_connector);
    ("delete-payment-credential-provider",
      delete_payment_credential_provider);
    ("delete-payment-manager", delete_payment_manager);
    ("delete-policy", delete_policy);
    ("delete-policy-engine", delete_policy_engine);
    ("delete-registry", delete_registry);
    ("delete-registry-record", delete_registry_record);
    ("delete-resource-policy", delete_resource_policy);
    ("delete-workload-identity", delete_workload_identity);
    ("get-agent-runtime", get_agent_runtime);
    ("get-agent-runtime-endpoint", get_agent_runtime_endpoint);
    ("get-api-key-credential-provider", get_api_key_credential_provider);
    ("get-browser", get_browser);
    ("get-browser-profile", get_browser_profile);
    ("get-code-interpreter", get_code_interpreter);
    ("get-configuration-bundle", get_configuration_bundle);
    ("get-configuration-bundle-version", get_configuration_bundle_version);
    ("get-evaluator", get_evaluator);
    ("get-gateway", get_gateway);
    ("get-gateway-rule", get_gateway_rule);
    ("get-gateway-target", get_gateway_target);
    ("get-harness", get_harness);
    ("get-memory", get_memory);
    ("get-oauth2-credential-provider", get_oauth2_credential_provider);
    ("get-online-evaluation-config", get_online_evaluation_config);
    ("get-payment-connector", get_payment_connector);
    ("get-payment-credential-provider", get_payment_credential_provider);
    ("get-payment-manager", get_payment_manager);
    ("get-policy", get_policy);
    ("get-policy-engine", get_policy_engine);
    ("get-policy-engine-summary", get_policy_engine_summary);
    ("get-policy-generation", get_policy_generation);
    ("get-policy-generation-summary", get_policy_generation_summary);
    ("get-policy-summary", get_policy_summary);
    ("get-registry", get_registry);
    ("get-registry-record", get_registry_record);
    ("get-resource-policy", get_resource_policy);
    ("get-token-vault", get_token_vault);
    ("get-workload-identity", get_workload_identity);
    ("list-agent-runtime-endpoints", list_agent_runtime_endpoints);
    ("list-agent-runtime-versions", list_agent_runtime_versions);
    ("list-agent-runtimes", list_agent_runtimes);
    ("list-api-key-credential-providers", list_api_key_credential_providers);
    ("list-browser-profiles", list_browser_profiles);
    ("list-browsers", list_browsers);
    ("list-code-interpreters", list_code_interpreters);
    ("list-configuration-bundle-versions",
      list_configuration_bundle_versions);
    ("list-configuration-bundles", list_configuration_bundles);
    ("list-evaluators", list_evaluators);
    ("list-gateway-rules", list_gateway_rules);
    ("list-gateway-targets", list_gateway_targets);
    ("list-gateways", list_gateways);
    ("list-harnesses", list_harnesses);
    ("list-memories", list_memories);
    ("list-oauth2-credential-providers", list_oauth2_credential_providers);
    ("list-online-evaluation-configs", list_online_evaluation_configs);
    ("list-payment-connectors", list_payment_connectors);
    ("list-payment-credential-providers", list_payment_credential_providers);
    ("list-payment-managers", list_payment_managers);
    ("list-policies", list_policies);
    ("list-policy-engine-summaries", list_policy_engine_summaries);
    ("list-policy-engines", list_policy_engines);
    ("list-policy-generation-assets", list_policy_generation_assets);
    ("list-policy-generation-summaries", list_policy_generation_summaries);
    ("list-policy-generations", list_policy_generations);
    ("list-policy-summaries", list_policy_summaries);
    ("list-registries", list_registries);
    ("list-registry-records", list_registry_records);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-workload-identities", list_workload_identities);
    ("put-resource-policy", put_resource_policy);
    ("set-token-vault-c-m-k", set_token_vault_c_m_k);
    ("start-policy-generation", start_policy_generation);
    ("submit-registry-record-for-approval",
      submit_registry_record_for_approval);
    ("synchronize-gateway-targets", synchronize_gateway_targets);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-agent-runtime", update_agent_runtime);
    ("update-agent-runtime-endpoint", update_agent_runtime_endpoint);
    ("update-api-key-credential-provider",
      update_api_key_credential_provider);
    ("update-configuration-bundle", update_configuration_bundle);
    ("update-evaluator", update_evaluator);
    ("update-gateway", update_gateway);
    ("update-gateway-rule", update_gateway_rule);
    ("update-gateway-target", update_gateway_target);
    ("update-harness", update_harness);
    ("update-memory", update_memory);
    ("update-oauth2-credential-provider", update_oauth2_credential_provider);
    ("update-online-evaluation-config", update_online_evaluation_config);
    ("update-payment-connector", update_payment_connector);
    ("update-payment-credential-provider",
      update_payment_credential_provider);
    ("update-payment-manager", update_payment_manager);
    ("update-policy", update_policy);
    ("update-policy-engine", update_policy_engine);
    ("update-registry", update_registry);
    ("update-registry-record", update_registry_record);
    ("update-registry-record-status", update_registry_record_status);
    ("update-workload-identity", update_workload_identity)]
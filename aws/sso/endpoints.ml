(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | GetRoleCredentials: (GetRoleCredentialsRequest.t,
  GetRoleCredentialsResponse.t, GetRoleCredentialsResponse.error) t 
  | ListAccountRoles: (ListAccountRolesRequest.t, ListAccountRolesResponse.t,
  ListAccountRolesResponse.error) t 
  | ListAccounts: (ListAccountsRequest.t, ListAccountsResponse.t,
  ListAccountsResponse.error) t 
  | Logout: (LogoutRequest.t, unit, unit) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | GetRoleCredentials -> `GET
  | ListAccountRoles -> `GET
  | ListAccounts -> `GET
  | Logout -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | GetRoleCredentials ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/federation/credentials")
            (List.filter_opt
               [Some ("role_name", (RoleNameType.to_header x.roleName));
               Some ("account_id", (AccountIdType.to_header x.accountId))])
      | ListAccountRoles ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/assignment/roles")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("next_token", (NextTokenType.to_header v)))
                  x.nextToken;
               Option.map
                 ~f:(fun v -> ("max_result", (MaxResultType.to_header v)))
                 x.maxResults;
               Some ("account_id", (AccountIdType.to_header x.accountId))])
      | ListAccounts ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/assignment/accounts")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("next_token", (NextTokenType.to_header v)))
                  x.nextToken;
               Option.map
                 ~f:(fun v -> ("max_result", (MaxResultType.to_header v)))
                 x.maxResults])
      | Logout -> (Format.kasprintf Uri.of_string) "/logout")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | GetRoleCredentials ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Some
                   ("x-amz-sso_bearer_token",
                     (AccessTokenType.to_header
                        req.GetRoleCredentialsRequest.accessToken))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map (List.filter_opt [])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListAccountRoles ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Some
                   ("x-amz-sso_bearer_token",
                     (AccessTokenType.to_header
                        req.ListAccountRolesRequest.accessToken))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map (List.filter_opt [])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListAccounts ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Some
                   ("x-amz-sso_bearer_token",
                     (AccessTokenType.to_header
                        req.ListAccountsRequest.accessToken))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map (List.filter_opt [])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | Logout ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Some
                   ("x-amz-sso_bearer_token",
                     (AccessTokenType.to_header req.LogoutRequest.accessToken))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map (List.filter_opt [])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : (Awso.Http.Response.t, Awso.Http.Io.Error.call) result) :
  (o, [ `AWS of e  | `Transport of Awso.Http.Io.Error.call ]) result=
  let handle_error err error_of_json =
    match err with
    | `Too_many_redirects -> Error (`Transport `Too_many_redirects)
    | `Bad_response
        { Awso.Http.Io.Error.code = code; body; x_amzn_error_type } ->
        let generic_error () =
          Error
            (`Transport
               (`Bad_response
                  { Awso.Http.Io.Error.code = code; body; x_amzn_error_type })) in
        (match (x_amzn_error_type, error_of_json,
                 ((code >= 400) && (code <= 599)))
         with
         | (Some error_type, Some error_of_json, true) ->
             let json = Yojson.Safe.from_string body in
             Error (`AWS (error_of_json error_type json))
         | (None, Some error_of_json, true) ->
             (try
                let json = Yojson.Safe.from_string body in
                match json |> (Yojson.Safe.Util.member "__type") with
                | `String error_type ->
                    let error_type =
                      match String.lsplit2 error_type ~on:'#' with
                      | Some (_, s) -> s
                      | None -> error_type in
                    Error (`AWS (error_of_json error_type json))
                | `Null -> generic_error ()
                | _ ->
                    failwithf "Error '__type' did not have string type: %s"
                      body ()
              with | _ -> generic_error ())
         | (None, _, _) | (_, None, _) | (_, _, false) -> generic_error ()) in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = resp in
  let _ = handle_error in
  let _ = response_to_json in
  match endpoint with
  | GetRoleCredentials ->
      (match resp with
       | Error err ->
           handle_error err (Some GetRoleCredentialsResponse.error_of_json)
       | Ok resp ->
           Ok (GetRoleCredentialsResponse.of_json (response_to_json resp)))
  | ListAccountRoles ->
      (match resp with
       | Error err ->
           handle_error err (Some ListAccountRolesResponse.error_of_json)
       | Ok resp ->
           Ok (ListAccountRolesResponse.of_json (response_to_json resp)))
  | ListAccounts ->
      (match resp with
       | Error err ->
           handle_error err (Some ListAccountsResponse.error_of_json)
       | Ok resp -> Ok (ListAccountsResponse.of_json (response_to_json resp)))
  | Logout -> Ok ()
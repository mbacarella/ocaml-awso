(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_access_grants_identity_center =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and identityCenterArn =
         flag "identity-center-arn" (required string)
           ~doc:"STRING IdentityCenterArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_access_grants_identity_center
           (Values.AssociateAccessGrantsIdentityCenterRequest.make ~accountId
              ~identityCenterArn ()) None None])
let create_access_grant =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessGrantsLocationConfiguration =
         flag "access-grants-location-configuration" (optional json_arg)
           ~doc:"JSON AccessGrantsLocationConfiguration"
       and applicationArn =
         flag "application-arn" (optional string)
           ~doc:"STRING IdentityCenterApplicationArn"
       and s3PrefixType =
         flag "s3-prefix-type" (optional json_arg) ~doc:"JSON S3PrefixType"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and accessGrantsLocationId =
         flag "access-grants-location-id" (required string)
           ~doc:"STRING AccessGrantsLocationId"
       and grantee = flag "grantee" (required json_arg) ~doc:"JSON Grantee"
       and permission =
         flag "permission" (required json_arg) ~doc:"JSON Permission" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_access_grant
           (Values.CreateAccessGrantRequest.make
              ?accessGrantsLocationConfiguration:(Option.map
                                                    ~f:Values.AccessGrantsLocationConfiguration.of_json
                                                    accessGrantsLocationConfiguration)
              ?applicationArn
              ?s3PrefixType:(Option.map ~f:Values.S3PrefixType.of_json
                               s3PrefixType)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~accountId
              ~accessGrantsLocationId
              ~grantee:(Values.Grantee.of_json grantee)
              ~permission:(Values.Permission.of_json permission) ())
           (Some Values.CreateAccessGrantResult.to_json)
           (Some Values.CreateAccessGrantResult.error_to_json)])
let create_access_grants_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityCenterArn =
         flag "identity-center-arn" (optional string)
           ~doc:"STRING IdentityCenterArn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_access_grants_instance
           (Values.CreateAccessGrantsInstanceRequest.make ?identityCenterArn
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~accountId ())
           (Some Values.CreateAccessGrantsInstanceResult.to_json)
           (Some Values.CreateAccessGrantsInstanceResult.error_to_json)])
let create_access_grants_location =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and locationScope =
         flag "location-scope" (required string) ~doc:"STRING S3Prefix"
       and iAMRoleArn =
         flag "i-a-m-role-arn" (required string) ~doc:"STRING IAMRoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_access_grants_location
           (Values.CreateAccessGrantsLocationRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~accountId
              ~locationScope ~iAMRoleArn ())
           (Some Values.CreateAccessGrantsLocationResult.to_json)
           (Some Values.CreateAccessGrantsLocationResult.error_to_json)])
let create_access_point =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vpcConfiguration =
         flag "vpc-configuration" (optional json_arg)
           ~doc:"JSON VpcConfiguration"
       and publicAccessBlockConfiguration =
         flag "public-access-block-configuration" (optional json_arg)
           ~doc:"JSON PublicAccessBlockConfiguration"
       and bucketAccountId =
         flag "bucket-account-id" (optional string) ~doc:"STRING AccountId"
       and scope = flag "scope" (optional json_arg) ~doc:"JSON Scope"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and name = flag "name" (required string) ~doc:"STRING AccessPointName"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_access_point
           (Values.CreateAccessPointRequest.make
              ?vpcConfiguration:(Option.map
                                   ~f:Values.VpcConfiguration.of_json
                                   vpcConfiguration)
              ?publicAccessBlockConfiguration:(Option.map
                                                 ~f:Values.PublicAccessBlockConfiguration.of_json
                                                 publicAccessBlockConfiguration)
              ?bucketAccountId
              ?scope:(Option.map ~f:Values.Scope.of_json scope)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~accountId
              ~name ~bucket ()) (Some Values.CreateAccessPointResult.to_json)
           (Some Values.CreateAccessPointResult.error_to_json)])
let create_access_point_for_object_lambda =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and name =
         flag "name" (required string)
           ~doc:"STRING ObjectLambdaAccessPointName"
       and configuration =
         flag "configuration" (required json_arg)
           ~doc:"JSON ObjectLambdaConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_access_point_for_object_lambda
           (Values.CreateAccessPointForObjectLambdaRequest.make ~accountId
              ~name
              ~configuration:(Values.ObjectLambdaConfiguration.of_json
                                configuration) ())
           (Some Values.CreateAccessPointForObjectLambdaResult.to_json)
           (Some Values.CreateAccessPointForObjectLambdaResult.error_to_json)])
let create_bucket =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aCL = flag "a-c-l" (optional json_arg) ~doc:"JSON BucketCannedACL"
       and createBucketConfiguration =
         flag "create-bucket-configuration" (optional json_arg)
           ~doc:"JSON CreateBucketConfiguration"
       and grantFullControl =
         flag "grant-full-control" (optional string)
           ~doc:"STRING GrantFullControl"
       and grantRead =
         flag "grant-read" (optional string) ~doc:"STRING GrantRead"
       and grantReadACP =
         flag "grant-read-a-c-p" (optional string) ~doc:"STRING GrantReadACP"
       and grantWrite =
         flag "grant-write" (optional string) ~doc:"STRING GrantWrite"
       and grantWriteACP =
         flag "grant-write-a-c-p" (optional string)
           ~doc:"STRING GrantWriteACP"
       and objectLockEnabledForBucket =
         flag "object-lock-enabled-for-bucket" (optional bool)
           ~doc:"BOOL ObjectLockEnabledForBucket"
       and outpostId =
         flag "outpost-id" (optional string)
           ~doc:"STRING NonEmptyMaxLength64String"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_bucket
           (Values.CreateBucketRequest.make
              ?aCL:(Option.map ~f:Values.BucketCannedACL.of_json aCL)
              ?createBucketConfiguration:(Option.map
                                            ~f:Values.CreateBucketConfiguration.of_json
                                            createBucketConfiguration)
              ?grantFullControl ?grantRead ?grantReadACP ?grantWrite
              ?grantWriteACP ?objectLockEnabledForBucket ?outpostId ~bucket
              ()) (Some Values.CreateBucketResult.to_json)
           (Some Values.CreateBucketResult.error_to_json)])
let create_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and confirmationRequired =
         flag "confirmation-required" (optional bool)
           ~doc:"BOOL ConfirmationRequired"
       and manifest =
         flag "manifest" (optional json_arg) ~doc:"JSON JobManifest"
       and description =
         flag "description" (optional string)
           ~doc:"STRING NonEmptyMaxLength256String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON S3TagSet"
       and manifestGenerator =
         flag "manifest-generator" (optional json_arg)
           ~doc:"JSON JobManifestGenerator"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and operation =
         flag "operation" (required json_arg) ~doc:"JSON JobOperation"
       and report = flag "report" (required json_arg) ~doc:"JSON JobReport"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING NonEmptyMaxLength64String"
       and priority = flag "priority" (required int) ~doc:"INT JobPriority"
       and roleArn =
         flag "role-arn" (required string) ~doc:"STRING IAMRoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_job
           (Values.CreateJobRequest.make ?confirmationRequired
              ?manifest:(Option.map ~f:Values.JobManifest.of_json manifest)
              ?description ?tags:(Option.map ~f:Values.S3TagSet.of_json tags)
              ?manifestGenerator:(Option.map
                                    ~f:Values.JobManifestGenerator.of_json
                                    manifestGenerator) ~accountId
              ~operation:(Values.JobOperation.of_json operation)
              ~report:(Values.JobReport.of_json report) ~clientRequestToken
              ~priority ~roleArn ()) (Some Values.CreateJobResult.to_json)
           (Some Values.CreateJobResult.error_to_json)])
let create_multi_region_access_point =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and clientToken =
         flag "client-token" (required string)
           ~doc:"STRING MultiRegionAccessPointClientToken"
       and details =
         flag "details" (required json_arg)
           ~doc:"JSON CreateMultiRegionAccessPointInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_multi_region_access_point
           (Values.CreateMultiRegionAccessPointRequest.make ~accountId
              ~clientToken
              ~details:(Values.CreateMultiRegionAccessPointInput.of_json
                          details) ())
           (Some Values.CreateMultiRegionAccessPointResult.to_json)
           (Some Values.CreateMultiRegionAccessPointResult.error_to_json)])
let create_storage_lens_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and storageLensGroup =
         flag "storage-lens-group" (required json_arg)
           ~doc:"JSON StorageLensGroup" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_storage_lens_group
           (Values.CreateStorageLensGroupRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~accountId
              ~storageLensGroup:(Values.StorageLensGroup.of_json
                                   storageLensGroup) ()) None None])
let delete_access_grant =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and accessGrantId =
         flag "access-grant-id" (required string) ~doc:"STRING AccessGrantId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_access_grant
           (Values.DeleteAccessGrantRequest.make ~accountId ~accessGrantId ())
           None None])
let delete_access_grants_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_access_grants_instance
           (Values.DeleteAccessGrantsInstanceRequest.make ~accountId ()) None
           None])
let delete_access_grants_instance_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_access_grants_instance_resource_policy
           (Values.DeleteAccessGrantsInstanceResourcePolicyRequest.make
              ~accountId ()) None None])
let delete_access_grants_location =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and accessGrantsLocationId =
         flag "access-grants-location-id" (required string)
           ~doc:"STRING AccessGrantsLocationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_access_grants_location
           (Values.DeleteAccessGrantsLocationRequest.make ~accountId
              ~accessGrantsLocationId ()) None None])
let delete_access_point =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and name = flag "name" (required string) ~doc:"STRING AccessPointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_access_point
           (Values.DeleteAccessPointRequest.make ~accountId ~name ()) None
           None])
let delete_access_point_for_object_lambda =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and name =
         flag "name" (required string)
           ~doc:"STRING ObjectLambdaAccessPointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_access_point_for_object_lambda
           (Values.DeleteAccessPointForObjectLambdaRequest.make ~accountId
              ~name ()) None None])
let delete_access_point_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and name = flag "name" (required string) ~doc:"STRING AccessPointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_access_point_policy
           (Values.DeleteAccessPointPolicyRequest.make ~accountId ~name ())
           None None])
let delete_access_point_policy_for_object_lambda =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and name =
         flag "name" (required string)
           ~doc:"STRING ObjectLambdaAccessPointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_access_point_policy_for_object_lambda
           (Values.DeleteAccessPointPolicyForObjectLambdaRequest.make
              ~accountId ~name ()) None None])
let delete_access_point_scope =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and name = flag "name" (required string) ~doc:"STRING AccessPointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_access_point_scope
           (Values.DeleteAccessPointScopeRequest.make ~accountId ~name ())
           None None])
let delete_bucket =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bucket
           (Values.DeleteBucketRequest.make ~accountId ~bucket ()) None None])
let delete_bucket_lifecycle_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bucket_lifecycle_configuration
           (Values.DeleteBucketLifecycleConfigurationRequest.make ~accountId
              ~bucket ()) None None])
let delete_bucket_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bucket_policy
           (Values.DeleteBucketPolicyRequest.make ~accountId ~bucket ()) None
           None])
let delete_bucket_replication =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bucket_replication
           (Values.DeleteBucketReplicationRequest.make ~accountId ~bucket ())
           None None])
let delete_bucket_tagging =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_bucket_tagging
           (Values.DeleteBucketTaggingRequest.make ~accountId ~bucket ())
           None None])
let delete_job_tagging =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_job_tagging
           (Values.DeleteJobTaggingRequest.make ~accountId ~jobId ())
           (Some Values.DeleteJobTaggingResult.to_json)
           (Some Values.DeleteJobTaggingResult.error_to_json)])
let delete_multi_region_access_point =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and clientToken =
         flag "client-token" (required string)
           ~doc:"STRING MultiRegionAccessPointClientToken"
       and details =
         flag "details" (required json_arg)
           ~doc:"JSON DeleteMultiRegionAccessPointInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_multi_region_access_point
           (Values.DeleteMultiRegionAccessPointRequest.make ~accountId
              ~clientToken
              ~details:(Values.DeleteMultiRegionAccessPointInput.of_json
                          details) ())
           (Some Values.DeleteMultiRegionAccessPointResult.to_json)
           (Some Values.DeleteMultiRegionAccessPointResult.error_to_json)])
let delete_public_access_block =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_public_access_block
           (Values.DeletePublicAccessBlockRequest.make ~accountId ()) None
           None])
let delete_storage_lens_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configId =
         flag "config-id" (required string) ~doc:"STRING ConfigId"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_storage_lens_configuration
           (Values.DeleteStorageLensConfigurationRequest.make ~configId
              ~accountId ()) None None])
let delete_storage_lens_configuration_tagging =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configId =
         flag "config-id" (required string) ~doc:"STRING ConfigId"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_storage_lens_configuration_tagging
           (Values.DeleteStorageLensConfigurationTaggingRequest.make
              ~configId ~accountId ())
           (Some Values.DeleteStorageLensConfigurationTaggingResult.to_json)
           (Some
              Values.DeleteStorageLensConfigurationTaggingResult.error_to_json)])
let delete_storage_lens_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING StorageLensGroupName"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_storage_lens_group
           (Values.DeleteStorageLensGroupRequest.make ~name ~accountId ())
           None None])
let describe_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_job
           (Values.DescribeJobRequest.make ~accountId ~jobId ())
           (Some Values.DescribeJobResult.to_json)
           (Some Values.DescribeJobResult.error_to_json)])
let describe_multi_region_access_point_operation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and requestTokenARN =
         flag "request-token-a-r-n" (required string)
           ~doc:"STRING AsyncRequestTokenARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_multi_region_access_point_operation
           (Values.DescribeMultiRegionAccessPointOperationRequest.make
              ~accountId ~requestTokenARN ())
           (Some Values.DescribeMultiRegionAccessPointOperationResult.to_json)
           (Some
              Values.DescribeMultiRegionAccessPointOperationResult.error_to_json)])
let dissociate_access_grants_identity_center =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.dissociate_access_grants_identity_center
           (Values.DissociateAccessGrantsIdentityCenterRequest.make
              ~accountId ()) None None])
let get_access_grant =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and accessGrantId =
         flag "access-grant-id" (required string) ~doc:"STRING AccessGrantId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_access_grant
           (Values.GetAccessGrantRequest.make ~accountId ~accessGrantId ())
           (Some Values.GetAccessGrantResult.to_json)
           (Some Values.GetAccessGrantResult.error_to_json)])
let get_access_grants_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_access_grants_instance
           (Values.GetAccessGrantsInstanceRequest.make ~accountId ())
           (Some Values.GetAccessGrantsInstanceResult.to_json)
           (Some Values.GetAccessGrantsInstanceResult.error_to_json)])
let get_access_grants_instance_for_prefix =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and s3Prefix =
         flag "s3-prefix" (required string) ~doc:"STRING S3Prefix" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_access_grants_instance_for_prefix
           (Values.GetAccessGrantsInstanceForPrefixRequest.make ~accountId
              ~s3Prefix ())
           (Some Values.GetAccessGrantsInstanceForPrefixResult.to_json)
           (Some Values.GetAccessGrantsInstanceForPrefixResult.error_to_json)])
let get_access_grants_instance_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_access_grants_instance_resource_policy
           (Values.GetAccessGrantsInstanceResourcePolicyRequest.make
              ~accountId ())
           (Some Values.GetAccessGrantsInstanceResourcePolicyResult.to_json)
           (Some
              Values.GetAccessGrantsInstanceResourcePolicyResult.error_to_json)])
let get_access_grants_location =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and accessGrantsLocationId =
         flag "access-grants-location-id" (required string)
           ~doc:"STRING AccessGrantsLocationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_access_grants_location
           (Values.GetAccessGrantsLocationRequest.make ~accountId
              ~accessGrantsLocationId ())
           (Some Values.GetAccessGrantsLocationResult.to_json)
           (Some Values.GetAccessGrantsLocationResult.error_to_json)])
let get_access_point =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and name = flag "name" (required string) ~doc:"STRING AccessPointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_access_point
           (Values.GetAccessPointRequest.make ~accountId ~name ())
           (Some Values.GetAccessPointResult.to_json)
           (Some Values.GetAccessPointResult.error_to_json)])
let get_access_point_configuration_for_object_lambda =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and name =
         flag "name" (required string)
           ~doc:"STRING ObjectLambdaAccessPointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_access_point_configuration_for_object_lambda
           (Values.GetAccessPointConfigurationForObjectLambdaRequest.make
              ~accountId ~name ())
           (Some
              Values.GetAccessPointConfigurationForObjectLambdaResult.to_json)
           (Some
              Values.GetAccessPointConfigurationForObjectLambdaResult.error_to_json)])
let get_access_point_for_object_lambda =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and name =
         flag "name" (required string)
           ~doc:"STRING ObjectLambdaAccessPointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_access_point_for_object_lambda
           (Values.GetAccessPointForObjectLambdaRequest.make ~accountId ~name
              ()) (Some Values.GetAccessPointForObjectLambdaResult.to_json)
           (Some Values.GetAccessPointForObjectLambdaResult.error_to_json)])
let get_access_point_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and name = flag "name" (required string) ~doc:"STRING AccessPointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_access_point_policy
           (Values.GetAccessPointPolicyRequest.make ~accountId ~name ())
           (Some Values.GetAccessPointPolicyResult.to_json)
           (Some Values.GetAccessPointPolicyResult.error_to_json)])
let get_access_point_policy_for_object_lambda =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and name =
         flag "name" (required string)
           ~doc:"STRING ObjectLambdaAccessPointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_access_point_policy_for_object_lambda
           (Values.GetAccessPointPolicyForObjectLambdaRequest.make ~accountId
              ~name ())
           (Some Values.GetAccessPointPolicyForObjectLambdaResult.to_json)
           (Some
              Values.GetAccessPointPolicyForObjectLambdaResult.error_to_json)])
let get_access_point_policy_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and name = flag "name" (required string) ~doc:"STRING AccessPointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_access_point_policy_status
           (Values.GetAccessPointPolicyStatusRequest.make ~accountId ~name ())
           (Some Values.GetAccessPointPolicyStatusResult.to_json)
           (Some Values.GetAccessPointPolicyStatusResult.error_to_json)])
let get_access_point_policy_status_for_object_lambda =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and name =
         flag "name" (required string)
           ~doc:"STRING ObjectLambdaAccessPointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_access_point_policy_status_for_object_lambda
           (Values.GetAccessPointPolicyStatusForObjectLambdaRequest.make
              ~accountId ~name ())
           (Some
              Values.GetAccessPointPolicyStatusForObjectLambdaResult.to_json)
           (Some
              Values.GetAccessPointPolicyStatusForObjectLambdaResult.error_to_json)])
let get_access_point_scope =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and name = flag "name" (required string) ~doc:"STRING AccessPointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_access_point_scope
           (Values.GetAccessPointScopeRequest.make ~accountId ~name ())
           (Some Values.GetAccessPointScopeResult.to_json)
           (Some Values.GetAccessPointScopeResult.error_to_json)])
let get_bucket =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket (Values.GetBucketRequest.make ~accountId ~bucket ())
           (Some Values.GetBucketResult.to_json)
           (Some Values.GetBucketResult.error_to_json)])
let get_bucket_lifecycle_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_lifecycle_configuration
           (Values.GetBucketLifecycleConfigurationRequest.make ~accountId
              ~bucket ())
           (Some Values.GetBucketLifecycleConfigurationResult.to_json)
           (Some Values.GetBucketLifecycleConfigurationResult.error_to_json)])
let get_bucket_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_policy
           (Values.GetBucketPolicyRequest.make ~accountId ~bucket ())
           (Some Values.GetBucketPolicyResult.to_json)
           (Some Values.GetBucketPolicyResult.error_to_json)])
let get_bucket_replication =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_replication
           (Values.GetBucketReplicationRequest.make ~accountId ~bucket ())
           (Some Values.GetBucketReplicationResult.to_json)
           (Some Values.GetBucketReplicationResult.error_to_json)])
let get_bucket_tagging =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_tagging
           (Values.GetBucketTaggingRequest.make ~accountId ~bucket ())
           (Some Values.GetBucketTaggingResult.to_json)
           (Some Values.GetBucketTaggingResult.error_to_json)])
let get_bucket_versioning =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_bucket_versioning
           (Values.GetBucketVersioningRequest.make ~accountId ~bucket ())
           (Some Values.GetBucketVersioningResult.to_json)
           (Some Values.GetBucketVersioningResult.error_to_json)])
let get_data_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and durationSeconds =
         flag "duration-seconds" (optional int) ~doc:"INT DurationSeconds"
       and privilege =
         flag "privilege" (optional json_arg) ~doc:"JSON Privilege"
       and targetType =
         flag "target-type" (optional json_arg) ~doc:"JSON S3PrefixType"
       and auditContext =
         flag "audit-context" (optional string) ~doc:"STRING AuditContext"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and target = flag "target" (required string) ~doc:"STRING S3Prefix"
       and permission =
         flag "permission" (required json_arg) ~doc:"JSON Permission" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_access
           (Values.GetDataAccessRequest.make ?durationSeconds
              ?privilege:(Option.map ~f:Values.Privilege.of_json privilege)
              ?targetType:(Option.map ~f:Values.S3PrefixType.of_json
                             targetType) ?auditContext ~accountId ~target
              ~permission:(Values.Permission.of_json permission) ())
           (Some Values.GetDataAccessResult.to_json)
           (Some Values.GetDataAccessResult.error_to_json)])
let get_job_tagging =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_job_tagging
           (Values.GetJobTaggingRequest.make ~accountId ~jobId ())
           (Some Values.GetJobTaggingResult.to_json)
           (Some Values.GetJobTaggingResult.error_to_json)])
let get_multi_region_access_point =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and name =
         flag "name" (required string)
           ~doc:"STRING MultiRegionAccessPointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_multi_region_access_point
           (Values.GetMultiRegionAccessPointRequest.make ~accountId ~name ())
           (Some Values.GetMultiRegionAccessPointResult.to_json)
           (Some Values.GetMultiRegionAccessPointResult.error_to_json)])
let get_multi_region_access_point_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and name =
         flag "name" (required string)
           ~doc:"STRING MultiRegionAccessPointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_multi_region_access_point_policy
           (Values.GetMultiRegionAccessPointPolicyRequest.make ~accountId
              ~name ())
           (Some Values.GetMultiRegionAccessPointPolicyResult.to_json)
           (Some Values.GetMultiRegionAccessPointPolicyResult.error_to_json)])
let get_multi_region_access_point_policy_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and name =
         flag "name" (required string)
           ~doc:"STRING MultiRegionAccessPointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_multi_region_access_point_policy_status
           (Values.GetMultiRegionAccessPointPolicyStatusRequest.make
              ~accountId ~name ())
           (Some Values.GetMultiRegionAccessPointPolicyStatusResult.to_json)
           (Some
              Values.GetMultiRegionAccessPointPolicyStatusResult.error_to_json)])
let get_multi_region_access_point_routes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and mrap =
         flag "mrap" (required string) ~doc:"STRING MultiRegionAccessPointId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_multi_region_access_point_routes
           (Values.GetMultiRegionAccessPointRoutesRequest.make ~accountId
              ~mrap ())
           (Some Values.GetMultiRegionAccessPointRoutesResult.to_json)
           (Some Values.GetMultiRegionAccessPointRoutesResult.error_to_json)])
let get_public_access_block =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_public_access_block
           (Values.GetPublicAccessBlockRequest.make ~accountId ())
           (Some Values.GetPublicAccessBlockOutput.to_json)
           (Some Values.GetPublicAccessBlockOutput.error_to_json)])
let get_storage_lens_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configId =
         flag "config-id" (required string) ~doc:"STRING ConfigId"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_storage_lens_configuration
           (Values.GetStorageLensConfigurationRequest.make ~configId
              ~accountId ())
           (Some Values.GetStorageLensConfigurationResult.to_json)
           (Some Values.GetStorageLensConfigurationResult.error_to_json)])
let get_storage_lens_configuration_tagging =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configId =
         flag "config-id" (required string) ~doc:"STRING ConfigId"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_storage_lens_configuration_tagging
           (Values.GetStorageLensConfigurationTaggingRequest.make ~configId
              ~accountId ())
           (Some Values.GetStorageLensConfigurationTaggingResult.to_json)
           (Some
              Values.GetStorageLensConfigurationTaggingResult.error_to_json)])
let get_storage_lens_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING StorageLensGroupName"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_storage_lens_group
           (Values.GetStorageLensGroupRequest.make ~name ~accountId ())
           (Some Values.GetStorageLensGroupResult.to_json)
           (Some Values.GetStorageLensGroupResult.error_to_json)])
let list_access_grants =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING ContinuationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and granteeType =
         flag "grantee-type" (optional json_arg) ~doc:"JSON GranteeType"
       and granteeIdentifier =
         flag "grantee-identifier" (optional string)
           ~doc:"STRING GranteeIdentifier"
       and permission =
         flag "permission" (optional json_arg) ~doc:"JSON Permission"
       and grantScope =
         flag "grant-scope" (optional string) ~doc:"STRING S3Prefix"
       and applicationArn =
         flag "application-arn" (optional string)
           ~doc:"STRING IdentityCenterApplicationArn"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_access_grants
           (Values.ListAccessGrantsRequest.make ?nextToken ?maxResults
              ?granteeType:(Option.map ~f:Values.GranteeType.of_json
                              granteeType) ?granteeIdentifier
              ?permission:(Option.map ~f:Values.Permission.of_json permission)
              ?grantScope ?applicationArn ~accountId ())
           (Some Values.ListAccessGrantsResult.to_json)
           (Some Values.ListAccessGrantsResult.error_to_json)])
let list_access_grants_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING ContinuationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_access_grants_instances
           (Values.ListAccessGrantsInstancesRequest.make ?nextToken
              ?maxResults ~accountId ())
           (Some Values.ListAccessGrantsInstancesResult.to_json)
           (Some Values.ListAccessGrantsInstancesResult.error_to_json)])
let list_access_grants_locations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING ContinuationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and locationScope =
         flag "location-scope" (optional string) ~doc:"STRING S3Prefix"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_access_grants_locations
           (Values.ListAccessGrantsLocationsRequest.make ?nextToken
              ?maxResults ?locationScope ~accountId ())
           (Some Values.ListAccessGrantsLocationsResult.to_json)
           (Some Values.ListAccessGrantsLocationsResult.error_to_json)])
let list_access_points =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bucket = flag "bucket" (optional string) ~doc:"STRING BucketName"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING NonEmptyMaxLength1024String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and dataSourceId =
         flag "data-source-id" (optional string) ~doc:"STRING DataSourceId"
       and dataSourceType =
         flag "data-source-type" (optional string)
           ~doc:"STRING DataSourceType"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_access_points
           (Values.ListAccessPointsRequest.make ?bucket ?nextToken
              ?maxResults ?dataSourceId ?dataSourceType ~accountId ())
           (Some Values.ListAccessPointsResult.to_json)
           (Some Values.ListAccessPointsResult.error_to_json)])
let list_access_points_for_directory_buckets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryBucket =
         flag "directory-bucket" (optional string) ~doc:"STRING BucketName"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING NonEmptyMaxLength1024String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_access_points_for_directory_buckets
           (Values.ListAccessPointsForDirectoryBucketsRequest.make
              ?directoryBucket ?nextToken ?maxResults ~accountId ())
           (Some Values.ListAccessPointsForDirectoryBucketsResult.to_json)
           (Some
              Values.ListAccessPointsForDirectoryBucketsResult.error_to_json)])
let list_access_points_for_object_lambda =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING NonEmptyMaxLength1024String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_access_points_for_object_lambda
           (Values.ListAccessPointsForObjectLambdaRequest.make ?nextToken
              ?maxResults ~accountId ())
           (Some Values.ListAccessPointsForObjectLambdaResult.to_json)
           (Some Values.ListAccessPointsForObjectLambdaResult.error_to_json)])
let list_caller_access_grants =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and grantScope =
         flag "grant-scope" (optional string) ~doc:"STRING S3Prefix"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING ContinuationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and allowedByApplication =
         flag "allowed-by-application" (optional bool) ~doc:"BOOL Boolean"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_caller_access_grants
           (Values.ListCallerAccessGrantsRequest.make ?grantScope ?nextToken
              ?maxResults ?allowedByApplication ~accountId ())
           (Some Values.ListCallerAccessGrantsResult.to_json)
           (Some Values.ListCallerAccessGrantsResult.error_to_json)])
let list_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobStatuses =
         flag "job-statuses" (optional json_arg) ~doc:"JSON JobStatusList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING StringForNextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_jobs
           (Values.ListJobsRequest.make
              ?jobStatuses:(Option.map ~f:Values.JobStatusList.of_json
                              jobStatuses) ?nextToken ?maxResults ~accountId
              ()) (Some Values.ListJobsResult.to_json)
           (Some Values.ListJobsResult.error_to_json)])
let list_multi_region_access_points =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING NonEmptyMaxLength1024String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_multi_region_access_points
           (Values.ListMultiRegionAccessPointsRequest.make ?nextToken
              ?maxResults ~accountId ())
           (Some Values.ListMultiRegionAccessPointsResult.to_json)
           (Some Values.ListMultiRegionAccessPointsResult.error_to_json)])
let list_regional_buckets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING NonEmptyMaxLength1024String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and outpostId =
         flag "outpost-id" (optional string)
           ~doc:"STRING NonEmptyMaxLength64String"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_regional_buckets
           (Values.ListRegionalBucketsRequest.make ?nextToken ?maxResults
              ?outpostId ~accountId ())
           (Some Values.ListRegionalBucketsResult.to_json)
           (Some Values.ListRegionalBucketsResult.error_to_json)])
let list_storage_lens_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING ContinuationToken"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_storage_lens_configurations
           (Values.ListStorageLensConfigurationsRequest.make ?nextToken
              ~accountId ())
           (Some Values.ListStorageLensConfigurationsResult.to_json)
           (Some Values.ListStorageLensConfigurationsResult.error_to_json)])
let list_storage_lens_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING ContinuationToken"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_storage_lens_groups
           (Values.ListStorageLensGroupsRequest.make ?nextToken ~accountId ())
           (Some Values.ListStorageLensGroupsResult.to_json)
           (Some Values.ListStorageLensGroupsResult.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING S3ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~accountId ~resourceArn ())
           (Some Values.ListTagsForResourceResult.to_json)
           (Some Values.ListTagsForResourceResult.error_to_json)])
let put_access_grants_instance_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organization =
         flag "organization" (optional string) ~doc:"STRING Organization"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and policy =
         flag "policy" (required string) ~doc:"STRING PolicyDocument" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_access_grants_instance_resource_policy
           (Values.PutAccessGrantsInstanceResourcePolicyRequest.make
              ?organization ~accountId ~policy ())
           (Some Values.PutAccessGrantsInstanceResourcePolicyResult.to_json)
           (Some
              Values.PutAccessGrantsInstanceResourcePolicyResult.error_to_json)])
let put_access_point_configuration_for_object_lambda =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and name =
         flag "name" (required string)
           ~doc:"STRING ObjectLambdaAccessPointName"
       and configuration =
         flag "configuration" (required json_arg)
           ~doc:"JSON ObjectLambdaConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_access_point_configuration_for_object_lambda
           (Values.PutAccessPointConfigurationForObjectLambdaRequest.make
              ~accountId ~name
              ~configuration:(Values.ObjectLambdaConfiguration.of_json
                                configuration) ()) None None])
let put_access_point_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and name = flag "name" (required string) ~doc:"STRING AccessPointName"
       and policy = flag "policy" (required string) ~doc:"STRING Policy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_access_point_policy
           (Values.PutAccessPointPolicyRequest.make ~accountId ~name ~policy
              ()) None None])
let put_access_point_policy_for_object_lambda =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and name =
         flag "name" (required string)
           ~doc:"STRING ObjectLambdaAccessPointName"
       and policy =
         flag "policy" (required string) ~doc:"STRING ObjectLambdaPolicy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_access_point_policy_for_object_lambda
           (Values.PutAccessPointPolicyForObjectLambdaRequest.make ~accountId
              ~name ~policy ()) None None])
let put_access_point_scope =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and name = flag "name" (required string) ~doc:"STRING AccessPointName"
       and scope = flag "scope" (required json_arg) ~doc:"JSON Scope" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_access_point_scope
           (Values.PutAccessPointScopeRequest.make ~accountId ~name
              ~scope:(Values.Scope.of_json scope) ()) None None])
let put_bucket_lifecycle_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and lifecycleConfiguration =
         flag "lifecycle-configuration" (optional json_arg)
           ~doc:"JSON LifecycleConfiguration"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_bucket_lifecycle_configuration
           (Values.PutBucketLifecycleConfigurationRequest.make
              ?lifecycleConfiguration:(Option.map
                                         ~f:Values.LifecycleConfiguration.of_json
                                         lifecycleConfiguration) ~accountId
              ~bucket ()) None None])
let put_bucket_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and confirmRemoveSelfBucketAccess =
         flag "confirm-remove-self-bucket-access" (optional bool)
           ~doc:"BOOL ConfirmRemoveSelfBucketAccess"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and policy = flag "policy" (required string) ~doc:"STRING Policy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_bucket_policy
           (Values.PutBucketPolicyRequest.make ?confirmRemoveSelfBucketAccess
              ~accountId ~bucket ~policy ()) None None])
let put_bucket_replication =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and replicationConfiguration =
         flag "replication-configuration" (required json_arg)
           ~doc:"JSON ReplicationConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_bucket_replication
           (Values.PutBucketReplicationRequest.make ~accountId ~bucket
              ~replicationConfiguration:(Values.ReplicationConfiguration.of_json
                                           replicationConfiguration) ()) None
           None])
let put_bucket_tagging =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and tagging = flag "tagging" (required json_arg) ~doc:"JSON Tagging" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_bucket_tagging
           (Values.PutBucketTaggingRequest.make ~accountId ~bucket
              ~tagging:(Values.Tagging.of_json tagging) ()) None None])
let put_bucket_versioning =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mFA = flag "m-f-a" (optional string) ~doc:"STRING MFA"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and bucket = flag "bucket" (required string) ~doc:"STRING BucketName"
       and versioningConfiguration =
         flag "versioning-configuration" (required json_arg)
           ~doc:"JSON VersioningConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_bucket_versioning
           (Values.PutBucketVersioningRequest.make ?mFA ~accountId ~bucket
              ~versioningConfiguration:(Values.VersioningConfiguration.of_json
                                          versioningConfiguration) ()) None
           None])
let put_job_tagging =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId"
       and tags = flag "tags" (required json_arg) ~doc:"JSON S3TagSet" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_job_tagging
           (Values.PutJobTaggingRequest.make ~accountId ~jobId
              ~tags:(Values.S3TagSet.of_json tags) ())
           (Some Values.PutJobTaggingResult.to_json)
           (Some Values.PutJobTaggingResult.error_to_json)])
let put_multi_region_access_point_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and clientToken =
         flag "client-token" (required string)
           ~doc:"STRING MultiRegionAccessPointClientToken"
       and details =
         flag "details" (required json_arg)
           ~doc:"JSON PutMultiRegionAccessPointPolicyInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_multi_region_access_point_policy
           (Values.PutMultiRegionAccessPointPolicyRequest.make ~accountId
              ~clientToken
              ~details:(Values.PutMultiRegionAccessPointPolicyInput.of_json
                          details) ())
           (Some Values.PutMultiRegionAccessPointPolicyResult.to_json)
           (Some Values.PutMultiRegionAccessPointPolicyResult.error_to_json)])
let put_public_access_block =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and publicAccessBlockConfiguration =
         flag "public-access-block-configuration" (required json_arg)
           ~doc:"JSON PublicAccessBlockConfiguration"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_public_access_block
           (Values.PutPublicAccessBlockRequest.make
              ~publicAccessBlockConfiguration:(Values.PublicAccessBlockConfiguration.of_json
                                                 publicAccessBlockConfiguration)
              ~accountId ()) None None])
let put_storage_lens_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON StorageLensTags"
       and configId =
         flag "config-id" (required string) ~doc:"STRING ConfigId"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and storageLensConfiguration =
         flag "storage-lens-configuration" (required json_arg)
           ~doc:"JSON StorageLensConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_storage_lens_configuration
           (Values.PutStorageLensConfigurationRequest.make
              ?tags:(Option.map ~f:Values.StorageLensTags.of_json tags)
              ~configId ~accountId
              ~storageLensConfiguration:(Values.StorageLensConfiguration.of_json
                                           storageLensConfiguration) ()) None
           None])
let put_storage_lens_configuration_tagging =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configId =
         flag "config-id" (required string) ~doc:"STRING ConfigId"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and tags = flag "tags" (required json_arg) ~doc:"JSON StorageLensTags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_storage_lens_configuration_tagging
           (Values.PutStorageLensConfigurationTaggingRequest.make ~configId
              ~accountId ~tags:(Values.StorageLensTags.of_json tags) ())
           (Some Values.PutStorageLensConfigurationTaggingResult.to_json)
           (Some
              Values.PutStorageLensConfigurationTaggingResult.error_to_json)])
let submit_multi_region_access_point_routes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and mrap =
         flag "mrap" (required string) ~doc:"STRING MultiRegionAccessPointId"
       and routeUpdates =
         flag "route-updates" (required json_arg) ~doc:"JSON RouteList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.submit_multi_region_access_point_routes
           (Values.SubmitMultiRegionAccessPointRoutesRequest.make ~accountId
              ~mrap ~routeUpdates:(Values.RouteList.of_json routeUpdates) ())
           (Some Values.SubmitMultiRegionAccessPointRoutesResult.to_json)
           (Some
              Values.SubmitMultiRegionAccessPointRoutesResult.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING S3ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~accountId ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResult.to_json)
           (Some Values.TagResourceResult.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING S3ResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~accountId ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResult.to_json)
           (Some Values.UntagResourceResult.error_to_json)])
let update_access_grants_location =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and accessGrantsLocationId =
         flag "access-grants-location-id" (required string)
           ~doc:"STRING AccessGrantsLocationId"
       and iAMRoleArn =
         flag "i-a-m-role-arn" (required string) ~doc:"STRING IAMRoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_access_grants_location
           (Values.UpdateAccessGrantsLocationRequest.make ~accountId
              ~accessGrantsLocationId ~iAMRoleArn ())
           (Some Values.UpdateAccessGrantsLocationResult.to_json)
           (Some Values.UpdateAccessGrantsLocationResult.error_to_json)])
let update_job_priority =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId"
       and priority = flag "priority" (required int) ~doc:"INT JobPriority" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_job_priority
           (Values.UpdateJobPriorityRequest.make ~accountId ~jobId ~priority
              ()) (Some Values.UpdateJobPriorityResult.to_json)
           (Some Values.UpdateJobPriorityResult.error_to_json)])
let update_job_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and statusUpdateReason =
         flag "status-update-reason" (optional string)
           ~doc:"STRING JobStatusUpdateReason"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId"
       and requestedJobStatus =
         flag "requested-job-status" (required json_arg)
           ~doc:"JSON RequestedJobStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_job_status
           (Values.UpdateJobStatusRequest.make ?statusUpdateReason ~accountId
              ~jobId
              ~requestedJobStatus:(Values.RequestedJobStatus.of_json
                                     requestedJobStatus) ())
           (Some Values.UpdateJobStatusResult.to_json)
           (Some Values.UpdateJobStatusResult.error_to_json)])
let update_storage_lens_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING StorageLensGroupName"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId"
       and storageLensGroup =
         flag "storage-lens-group" (required json_arg)
           ~doc:"JSON StorageLensGroup" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_storage_lens_group
           (Values.UpdateStorageLensGroupRequest.make ~name ~accountId
              ~storageLensGroup:(Values.StorageLensGroup.of_json
                                   storageLensGroup) ()) None None])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-access-grants-identity-center",
       associate_access_grants_identity_center);
    ("create-access-grant", create_access_grant);
    ("create-access-grants-instance", create_access_grants_instance);
    ("create-access-grants-location", create_access_grants_location);
    ("create-access-point", create_access_point);
    ("create-access-point-for-object-lambda",
      create_access_point_for_object_lambda);
    ("create-bucket", create_bucket);
    ("create-job", create_job);
    ("create-multi-region-access-point", create_multi_region_access_point);
    ("create-storage-lens-group", create_storage_lens_group);
    ("delete-access-grant", delete_access_grant);
    ("delete-access-grants-instance", delete_access_grants_instance);
    ("delete-access-grants-instance-resource-policy",
      delete_access_grants_instance_resource_policy);
    ("delete-access-grants-location", delete_access_grants_location);
    ("delete-access-point", delete_access_point);
    ("delete-access-point-for-object-lambda",
      delete_access_point_for_object_lambda);
    ("delete-access-point-policy", delete_access_point_policy);
    ("delete-access-point-policy-for-object-lambda",
      delete_access_point_policy_for_object_lambda);
    ("delete-access-point-scope", delete_access_point_scope);
    ("delete-bucket", delete_bucket);
    ("delete-bucket-lifecycle-configuration",
      delete_bucket_lifecycle_configuration);
    ("delete-bucket-policy", delete_bucket_policy);
    ("delete-bucket-replication", delete_bucket_replication);
    ("delete-bucket-tagging", delete_bucket_tagging);
    ("delete-job-tagging", delete_job_tagging);
    ("delete-multi-region-access-point", delete_multi_region_access_point);
    ("delete-public-access-block", delete_public_access_block);
    ("delete-storage-lens-configuration", delete_storage_lens_configuration);
    ("delete-storage-lens-configuration-tagging",
      delete_storage_lens_configuration_tagging);
    ("delete-storage-lens-group", delete_storage_lens_group);
    ("describe-job", describe_job);
    ("describe-multi-region-access-point-operation",
      describe_multi_region_access_point_operation);
    ("dissociate-access-grants-identity-center",
      dissociate_access_grants_identity_center);
    ("get-access-grant", get_access_grant);
    ("get-access-grants-instance", get_access_grants_instance);
    ("get-access-grants-instance-for-prefix",
      get_access_grants_instance_for_prefix);
    ("get-access-grants-instance-resource-policy",
      get_access_grants_instance_resource_policy);
    ("get-access-grants-location", get_access_grants_location);
    ("get-access-point", get_access_point);
    ("get-access-point-configuration-for-object-lambda",
      get_access_point_configuration_for_object_lambda);
    ("get-access-point-for-object-lambda",
      get_access_point_for_object_lambda);
    ("get-access-point-policy", get_access_point_policy);
    ("get-access-point-policy-for-object-lambda",
      get_access_point_policy_for_object_lambda);
    ("get-access-point-policy-status", get_access_point_policy_status);
    ("get-access-point-policy-status-for-object-lambda",
      get_access_point_policy_status_for_object_lambda);
    ("get-access-point-scope", get_access_point_scope);
    ("get-bucket", get_bucket);
    ("get-bucket-lifecycle-configuration",
      get_bucket_lifecycle_configuration);
    ("get-bucket-policy", get_bucket_policy);
    ("get-bucket-replication", get_bucket_replication);
    ("get-bucket-tagging", get_bucket_tagging);
    ("get-bucket-versioning", get_bucket_versioning);
    ("get-data-access", get_data_access);
    ("get-job-tagging", get_job_tagging);
    ("get-multi-region-access-point", get_multi_region_access_point);
    ("get-multi-region-access-point-policy",
      get_multi_region_access_point_policy);
    ("get-multi-region-access-point-policy-status",
      get_multi_region_access_point_policy_status);
    ("get-multi-region-access-point-routes",
      get_multi_region_access_point_routes);
    ("get-public-access-block", get_public_access_block);
    ("get-storage-lens-configuration", get_storage_lens_configuration);
    ("get-storage-lens-configuration-tagging",
      get_storage_lens_configuration_tagging);
    ("get-storage-lens-group", get_storage_lens_group);
    ("list-access-grants", list_access_grants);
    ("list-access-grants-instances", list_access_grants_instances);
    ("list-access-grants-locations", list_access_grants_locations);
    ("list-access-points", list_access_points);
    ("list-access-points-for-directory-buckets",
      list_access_points_for_directory_buckets);
    ("list-access-points-for-object-lambda",
      list_access_points_for_object_lambda);
    ("list-caller-access-grants", list_caller_access_grants);
    ("list-jobs", list_jobs);
    ("list-multi-region-access-points", list_multi_region_access_points);
    ("list-regional-buckets", list_regional_buckets);
    ("list-storage-lens-configurations", list_storage_lens_configurations);
    ("list-storage-lens-groups", list_storage_lens_groups);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-access-grants-instance-resource-policy",
      put_access_grants_instance_resource_policy);
    ("put-access-point-configuration-for-object-lambda",
      put_access_point_configuration_for_object_lambda);
    ("put-access-point-policy", put_access_point_policy);
    ("put-access-point-policy-for-object-lambda",
      put_access_point_policy_for_object_lambda);
    ("put-access-point-scope", put_access_point_scope);
    ("put-bucket-lifecycle-configuration",
      put_bucket_lifecycle_configuration);
    ("put-bucket-policy", put_bucket_policy);
    ("put-bucket-replication", put_bucket_replication);
    ("put-bucket-tagging", put_bucket_tagging);
    ("put-bucket-versioning", put_bucket_versioning);
    ("put-job-tagging", put_job_tagging);
    ("put-multi-region-access-point-policy",
      put_multi_region_access_point_policy);
    ("put-public-access-block", put_public_access_block);
    ("put-storage-lens-configuration", put_storage_lens_configuration);
    ("put-storage-lens-configuration-tagging",
      put_storage_lens_configuration_tagging);
    ("submit-multi-region-access-point-routes",
      submit_multi_region_access_point_routes);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-access-grants-location", update_access_grants_location);
    ("update-job-priority", update_job_priority);
    ("update-job-status", update_job_status);
    ("update-storage-lens-group", update_storage_lens_group)]
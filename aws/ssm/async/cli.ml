(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_tags_to_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceType =
         flag "resource-type" (required json_arg)
           ~doc:"JSON ResourceTypeForTagging"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceId"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_tags_to_resource
           (Values.AddTagsToResourceRequest.make
              ~resourceType:(Values.ResourceTypeForTagging.of_json
                               resourceType) ~resourceId
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.AddTagsToResourceResult.to_json)
           (Some Values.AddTagsToResourceResult.error_to_json)])
let associate_ops_item_related_item =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and opsItemId =
         flag "ops-item-id" (required string) ~doc:"STRING OpsItemId"
       and associationType =
         flag "association-type" (required string)
           ~doc:"STRING OpsItemRelatedItemAssociationType"
       and resourceType =
         flag "resource-type" (required string)
           ~doc:"STRING OpsItemRelatedItemAssociationResourceType"
       and resourceUri =
         flag "resource-uri" (required string)
           ~doc:"STRING OpsItemRelatedItemAssociationResourceUri" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_ops_item_related_item
           (Values.AssociateOpsItemRelatedItemRequest.make ~opsItemId
              ~associationType ~resourceType ~resourceUri ())
           (Some Values.AssociateOpsItemRelatedItemResponse.to_json)
           (Some Values.AssociateOpsItemRelatedItemResponse.error_to_json)])
let cancel_command =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceIds =
         flag "instance-ids" (optional json_arg) ~doc:"JSON InstanceIdList"
       and commandId =
         flag "command-id" (required string) ~doc:"STRING CommandId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_command
           (Values.CancelCommandRequest.make
              ?instanceIds:(Option.map ~f:Values.InstanceIdList.of_json
                              instanceIds) ~commandId ())
           (Some Values.CancelCommandResult.to_json)
           (Some Values.CancelCommandResult.error_to_json)])
let cancel_maintenance_window_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and windowExecutionId =
         flag "window-execution-id" (required string)
           ~doc:"STRING MaintenanceWindowExecutionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_maintenance_window_execution
           (Values.CancelMaintenanceWindowExecutionRequest.make
              ~windowExecutionId ())
           (Some Values.CancelMaintenanceWindowExecutionResult.to_json)
           (Some Values.CancelMaintenanceWindowExecutionResult.error_to_json)])
let create_activation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ActivationDescription"
       and defaultInstanceName =
         flag "default-instance-name" (optional string)
           ~doc:"STRING DefaultInstanceName"
       and registrationLimit =
         flag "registration-limit" (optional int)
           ~doc:"INT RegistrationLimit"
       and expirationDate =
         flag "expiration-date" (optional json_arg)
           ~doc:"JSON ExpirationDate"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and registrationMetadata =
         flag "registration-metadata" (optional json_arg)
           ~doc:"JSON RegistrationMetadataList"
       and iamRole = flag "iam-role" (required string) ~doc:"STRING IamRole" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_activation
           (Values.CreateActivationRequest.make ?description
              ?defaultInstanceName ?registrationLimit
              ?expirationDate:(Option.map ~f:Values.ExpirationDate.of_json
                                 expirationDate)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?registrationMetadata:(Option.map
                                       ~f:Values.RegistrationMetadataList.of_json
                                       registrationMetadata) ~iamRole ())
           (Some Values.CreateActivationResult.to_json)
           (Some Values.CreateActivationResult.error_to_json)])
let create_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and documentVersion =
         flag "document-version" (optional string)
           ~doc:"STRING DocumentVersion"
       and instanceId =
         flag "instance-id" (optional string) ~doc:"STRING InstanceId"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON Parameters"
       and targets = flag "targets" (optional json_arg) ~doc:"JSON Targets"
       and scheduleExpression =
         flag "schedule-expression" (optional string)
           ~doc:"STRING ScheduleExpression"
       and outputLocation =
         flag "output-location" (optional json_arg)
           ~doc:"JSON InstanceAssociationOutputLocation"
       and associationName =
         flag "association-name" (optional string)
           ~doc:"STRING AssociationName"
       and automationTargetParameterName =
         flag "automation-target-parameter-name" (optional string)
           ~doc:"STRING AutomationTargetParameterName"
       and maxErrors =
         flag "max-errors" (optional string) ~doc:"STRING MaxErrors"
       and maxConcurrency =
         flag "max-concurrency" (optional string)
           ~doc:"STRING MaxConcurrency"
       and complianceSeverity =
         flag "compliance-severity" (optional json_arg)
           ~doc:"JSON AssociationComplianceSeverity"
       and syncCompliance =
         flag "sync-compliance" (optional json_arg)
           ~doc:"JSON AssociationSyncCompliance"
       and applyOnlyAtCronInterval =
         flag "apply-only-at-cron-interval" (optional bool)
           ~doc:"BOOL ApplyOnlyAtCronInterval"
       and calendarNames =
         flag "calendar-names" (optional json_arg)
           ~doc:"JSON CalendarNameOrARNList"
       and targetLocations =
         flag "target-locations" (optional json_arg)
           ~doc:"JSON TargetLocations"
       and name = flag "name" (required string) ~doc:"STRING DocumentARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_association
           (Values.CreateAssociationRequest.make ?documentVersion ?instanceId
              ?parameters:(Option.map ~f:Values.Parameters.of_json parameters)
              ?targets:(Option.map ~f:Values.Targets.of_json targets)
              ?scheduleExpression
              ?outputLocation:(Option.map
                                 ~f:Values.InstanceAssociationOutputLocation.of_json
                                 outputLocation) ?associationName
              ?automationTargetParameterName ?maxErrors ?maxConcurrency
              ?complianceSeverity:(Option.map
                                     ~f:Values.AssociationComplianceSeverity.of_json
                                     complianceSeverity)
              ?syncCompliance:(Option.map
                                 ~f:Values.AssociationSyncCompliance.of_json
                                 syncCompliance) ?applyOnlyAtCronInterval
              ?calendarNames:(Option.map
                                ~f:Values.CalendarNameOrARNList.of_json
                                calendarNames)
              ?targetLocations:(Option.map ~f:Values.TargetLocations.of_json
                                  targetLocations) ~name ())
           (Some Values.CreateAssociationResult.to_json)
           (Some Values.CreateAssociationResult.error_to_json)])
let create_association_batch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and entries =
         flag "entries" (required json_arg)
           ~doc:"JSON CreateAssociationBatchRequestEntries" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_association_batch
           (Values.CreateAssociationBatchRequest.make
              ~entries:(Values.CreateAssociationBatchRequestEntries.of_json
                          entries) ())
           (Some Values.CreateAssociationBatchResult.to_json)
           (Some Values.CreateAssociationBatchResult.error_to_json)])
let create_document =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and requires =
         flag "requires" (optional json_arg) ~doc:"JSON DocumentRequiresList"
       and attachments =
         flag "attachments" (optional json_arg)
           ~doc:"JSON AttachmentsSourceList"
       and displayName =
         flag "display-name" (optional string)
           ~doc:"STRING DocumentDisplayName"
       and versionName =
         flag "version-name" (optional string)
           ~doc:"STRING DocumentVersionName"
       and documentType =
         flag "document-type" (optional json_arg) ~doc:"JSON DocumentType"
       and documentFormat =
         flag "document-format" (optional json_arg)
           ~doc:"JSON DocumentFormat"
       and targetType =
         flag "target-type" (optional string) ~doc:"STRING TargetType"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and content =
         flag "content" (required string) ~doc:"STRING DocumentContent"
       and name = flag "name" (required string) ~doc:"STRING DocumentName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_document
           (Values.CreateDocumentRequest.make
              ?requires:(Option.map ~f:Values.DocumentRequiresList.of_json
                           requires)
              ?attachments:(Option.map
                              ~f:Values.AttachmentsSourceList.of_json
                              attachments) ?displayName ?versionName
              ?documentType:(Option.map ~f:Values.DocumentType.of_json
                               documentType)
              ?documentFormat:(Option.map ~f:Values.DocumentFormat.of_json
                                 documentFormat) ?targetType
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~content
              ~name ()) (Some Values.CreateDocumentResult.to_json)
           (Some Values.CreateDocumentResult.error_to_json)])
let create_maintenance_window =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING MaintenanceWindowDescription"
       and startDate =
         flag "start-date" (optional string)
           ~doc:"STRING MaintenanceWindowStringDateTime"
       and endDate =
         flag "end-date" (optional string)
           ~doc:"STRING MaintenanceWindowStringDateTime"
       and scheduleTimezone =
         flag "schedule-timezone" (optional string)
           ~doc:"STRING MaintenanceWindowTimezone"
       and scheduleOffset =
         flag "schedule-offset" (optional int)
           ~doc:"INT MaintenanceWindowOffset"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name =
         flag "name" (required string) ~doc:"STRING MaintenanceWindowName"
       and schedule =
         flag "schedule" (required string)
           ~doc:"STRING MaintenanceWindowSchedule"
       and duration =
         flag "duration" (required int)
           ~doc:"INT MaintenanceWindowDurationHours"
       and cutoff =
         flag "cutoff" (required int) ~doc:"INT MaintenanceWindowCutoff"
       and allowUnassociatedTargets =
         flag "allow-unassociated-targets" (required bool)
           ~doc:"BOOL MaintenanceWindowAllowUnassociatedTargets" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_maintenance_window
           (Values.CreateMaintenanceWindowRequest.make ?description
              ?startDate ?endDate ?scheduleTimezone ?scheduleOffset
              ?clientToken ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~name ~schedule ~duration ~cutoff ~allowUnassociatedTargets ())
           (Some Values.CreateMaintenanceWindowResult.to_json)
           (Some Values.CreateMaintenanceWindowResult.error_to_json)])
let create_ops_item =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and opsItemType =
         flag "ops-item-type" (optional string) ~doc:"STRING OpsItemType"
       and operationalData =
         flag "operational-data" (optional json_arg)
           ~doc:"JSON OpsItemOperationalData"
       and notifications =
         flag "notifications" (optional json_arg)
           ~doc:"JSON OpsItemNotifications"
       and priority =
         flag "priority" (optional int) ~doc:"INT OpsItemPriority"
       and relatedOpsItems =
         flag "related-ops-items" (optional json_arg)
           ~doc:"JSON RelatedOpsItems"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and category =
         flag "category" (optional string) ~doc:"STRING OpsItemCategory"
       and severity =
         flag "severity" (optional string) ~doc:"STRING OpsItemSeverity"
       and actualStartTime =
         flag "actual-start-time" (optional json_arg) ~doc:"JSON DateTime"
       and actualEndTime =
         flag "actual-end-time" (optional json_arg) ~doc:"JSON DateTime"
       and plannedStartTime =
         flag "planned-start-time" (optional json_arg) ~doc:"JSON DateTime"
       and plannedEndTime =
         flag "planned-end-time" (optional json_arg) ~doc:"JSON DateTime"
       and description =
         flag "description" (required string)
           ~doc:"STRING OpsItemDescription"
       and source =
         flag "source" (required string) ~doc:"STRING OpsItemSource"
       and title = flag "title" (required string) ~doc:"STRING OpsItemTitle" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_ops_item
           (Values.CreateOpsItemRequest.make ?opsItemType
              ?operationalData:(Option.map
                                  ~f:Values.OpsItemOperationalData.of_json
                                  operationalData)
              ?notifications:(Option.map
                                ~f:Values.OpsItemNotifications.of_json
                                notifications) ?priority
              ?relatedOpsItems:(Option.map ~f:Values.RelatedOpsItems.of_json
                                  relatedOpsItems)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?category
              ?severity
              ?actualStartTime:(Option.map ~f:Values.DateTime.of_json
                                  actualStartTime)
              ?actualEndTime:(Option.map ~f:Values.DateTime.of_json
                                actualEndTime)
              ?plannedStartTime:(Option.map ~f:Values.DateTime.of_json
                                   plannedStartTime)
              ?plannedEndTime:(Option.map ~f:Values.DateTime.of_json
                                 plannedEndTime) ~description ~source ~title
              ()) (Some Values.CreateOpsItemResponse.to_json)
           (Some Values.CreateOpsItemResponse.error_to_json)])
let create_ops_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metadata =
         flag "metadata" (optional json_arg) ~doc:"JSON MetadataMap"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and resourceId =
         flag "resource-id" (required string)
           ~doc:"STRING OpsMetadataResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_ops_metadata
           (Values.CreateOpsMetadataRequest.make
              ?metadata:(Option.map ~f:Values.MetadataMap.of_json metadata)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~resourceId
              ()) (Some Values.CreateOpsMetadataResult.to_json)
           (Some Values.CreateOpsMetadataResult.error_to_json)])
let create_patch_baseline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and operatingSystem =
         flag "operating-system" (optional json_arg)
           ~doc:"JSON OperatingSystem"
       and globalFilters =
         flag "global-filters" (optional json_arg)
           ~doc:"JSON PatchFilterGroup"
       and approvalRules =
         flag "approval-rules" (optional json_arg) ~doc:"JSON PatchRuleGroup"
       and approvedPatches =
         flag "approved-patches" (optional json_arg) ~doc:"JSON PatchIdList"
       and approvedPatchesComplianceLevel =
         flag "approved-patches-compliance-level" (optional json_arg)
           ~doc:"JSON PatchComplianceLevel"
       and approvedPatchesEnableNonSecurity =
         flag "approved-patches-enable-non-security" (optional bool)
           ~doc:"BOOL Boolean"
       and rejectedPatches =
         flag "rejected-patches" (optional json_arg) ~doc:"JSON PatchIdList"
       and rejectedPatchesAction =
         flag "rejected-patches-action" (optional json_arg)
           ~doc:"JSON PatchAction"
       and description =
         flag "description" (optional string)
           ~doc:"STRING BaselineDescription"
       and sources =
         flag "sources" (optional json_arg) ~doc:"JSON PatchSourceList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name = flag "name" (required string) ~doc:"STRING BaselineName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_patch_baseline
           (Values.CreatePatchBaselineRequest.make
              ?operatingSystem:(Option.map ~f:Values.OperatingSystem.of_json
                                  operatingSystem)
              ?globalFilters:(Option.map ~f:Values.PatchFilterGroup.of_json
                                globalFilters)
              ?approvalRules:(Option.map ~f:Values.PatchRuleGroup.of_json
                                approvalRules)
              ?approvedPatches:(Option.map ~f:Values.PatchIdList.of_json
                                  approvedPatches)
              ?approvedPatchesComplianceLevel:(Option.map
                                                 ~f:Values.PatchComplianceLevel.of_json
                                                 approvedPatchesComplianceLevel)
              ?approvedPatchesEnableNonSecurity
              ?rejectedPatches:(Option.map ~f:Values.PatchIdList.of_json
                                  rejectedPatches)
              ?rejectedPatchesAction:(Option.map
                                        ~f:Values.PatchAction.of_json
                                        rejectedPatchesAction) ?description
              ?sources:(Option.map ~f:Values.PatchSourceList.of_json sources)
              ?clientToken ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~name ()) (Some Values.CreatePatchBaselineResult.to_json)
           (Some Values.CreatePatchBaselineResult.error_to_json)])
let create_resource_data_sync =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and s3Destination =
         flag "s3-destination" (optional json_arg)
           ~doc:"JSON ResourceDataSyncS3Destination"
       and syncType =
         flag "sync-type" (optional string)
           ~doc:"STRING ResourceDataSyncType"
       and syncSource =
         flag "sync-source" (optional json_arg)
           ~doc:"JSON ResourceDataSyncSource"
       and syncName =
         flag "sync-name" (required string)
           ~doc:"STRING ResourceDataSyncName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_resource_data_sync
           (Values.CreateResourceDataSyncRequest.make
              ?s3Destination:(Option.map
                                ~f:Values.ResourceDataSyncS3Destination.of_json
                                s3Destination) ?syncType
              ?syncSource:(Option.map
                             ~f:Values.ResourceDataSyncSource.of_json
                             syncSource) ~syncName ())
           (Some Values.CreateResourceDataSyncResult.to_json)
           (Some Values.CreateResourceDataSyncResult.error_to_json)])
let delete_activation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and activationId =
         flag "activation-id" (required string) ~doc:"STRING ActivationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_activation
           (Values.DeleteActivationRequest.make ~activationId ())
           (Some Values.DeleteActivationResult.to_json)
           (Some Values.DeleteActivationResult.error_to_json)])
let delete_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING DocumentARN"
       and instanceId =
         flag "instance-id" (optional string) ~doc:"STRING InstanceId"
       and associationId =
         flag "association-id" (optional string) ~doc:"STRING AssociationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_association
           (Values.DeleteAssociationRequest.make ?name ?instanceId
              ?associationId ())
           (Some Values.DeleteAssociationResult.to_json)
           (Some Values.DeleteAssociationResult.error_to_json)])
let delete_document =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and documentVersion =
         flag "document-version" (optional string)
           ~doc:"STRING DocumentVersion"
       and versionName =
         flag "version-name" (optional string)
           ~doc:"STRING DocumentVersionName"
       and force = flag "force" (optional bool) ~doc:"BOOL Boolean"
       and name = flag "name" (required string) ~doc:"STRING DocumentName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_document
           (Values.DeleteDocumentRequest.make ?documentVersion ?versionName
              ?force ~name ()) (Some Values.DeleteDocumentResult.to_json)
           (Some Values.DeleteDocumentResult.error_to_json)])
let delete_inventory =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and schemaDeleteOption =
         flag "schema-delete-option" (optional json_arg)
           ~doc:"JSON InventorySchemaDeleteOption"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL DryRun"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING UUID"
       and typeName =
         flag "type-name" (required string)
           ~doc:"STRING InventoryItemTypeName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_inventory
           (Values.DeleteInventoryRequest.make
              ?schemaDeleteOption:(Option.map
                                     ~f:Values.InventorySchemaDeleteOption.of_json
                                     schemaDeleteOption) ?dryRun ?clientToken
              ~typeName ()) (Some Values.DeleteInventoryResult.to_json)
           (Some Values.DeleteInventoryResult.error_to_json)])
let delete_maintenance_window =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and windowId =
         flag "window-id" (required string) ~doc:"STRING MaintenanceWindowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_maintenance_window
           (Values.DeleteMaintenanceWindowRequest.make ~windowId ())
           (Some Values.DeleteMaintenanceWindowResult.to_json)
           (Some Values.DeleteMaintenanceWindowResult.error_to_json)])
let delete_ops_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and opsMetadataArn =
         flag "ops-metadata-arn" (required string)
           ~doc:"STRING OpsMetadataArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_ops_metadata
           (Values.DeleteOpsMetadataRequest.make ~opsMetadataArn ())
           (Some Values.DeleteOpsMetadataResult.to_json)
           (Some Values.DeleteOpsMetadataResult.error_to_json)])
let delete_parameter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING PSParameterName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_parameter (Values.DeleteParameterRequest.make ~name ())
           (Some Values.DeleteParameterResult.to_json)
           (Some Values.DeleteParameterResult.error_to_json)])
let delete_parameters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and names =
         flag "names" (required json_arg) ~doc:"JSON ParameterNameList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_parameters
           (Values.DeleteParametersRequest.make
              ~names:(Values.ParameterNameList.of_json names) ())
           (Some Values.DeleteParametersResult.to_json)
           (Some Values.DeleteParametersResult.error_to_json)])
let delete_patch_baseline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and baselineId =
         flag "baseline-id" (required string) ~doc:"STRING BaselineId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_patch_baseline
           (Values.DeletePatchBaselineRequest.make ~baselineId ())
           (Some Values.DeletePatchBaselineResult.to_json)
           (Some Values.DeletePatchBaselineResult.error_to_json)])
let delete_resource_data_sync =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and syncType =
         flag "sync-type" (optional string)
           ~doc:"STRING ResourceDataSyncType"
       and syncName =
         flag "sync-name" (required string)
           ~doc:"STRING ResourceDataSyncName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_data_sync
           (Values.DeleteResourceDataSyncRequest.make ?syncType ~syncName ())
           (Some Values.DeleteResourceDataSyncResult.to_json)
           (Some Values.DeleteResourceDataSyncResult.error_to_json)])
let deregister_managed_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING ManagedInstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_managed_instance
           (Values.DeregisterManagedInstanceRequest.make ~instanceId ())
           (Some Values.DeregisterManagedInstanceResult.to_json)
           (Some Values.DeregisterManagedInstanceResult.error_to_json)])
let deregister_patch_baseline_for_patch_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and baselineId =
         flag "baseline-id" (required string) ~doc:"STRING BaselineId"
       and patchGroup =
         flag "patch-group" (required string) ~doc:"STRING PatchGroup" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_patch_baseline_for_patch_group
           (Values.DeregisterPatchBaselineForPatchGroupRequest.make
              ~baselineId ~patchGroup ())
           (Some Values.DeregisterPatchBaselineForPatchGroupResult.to_json)
           (Some
              Values.DeregisterPatchBaselineForPatchGroupResult.error_to_json)])
let deregister_target_from_maintenance_window =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and safe = flag "safe" (optional bool) ~doc:"BOOL Boolean"
       and windowId =
         flag "window-id" (required string) ~doc:"STRING MaintenanceWindowId"
       and windowTargetId =
         flag "window-target-id" (required string)
           ~doc:"STRING MaintenanceWindowTargetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_target_from_maintenance_window
           (Values.DeregisterTargetFromMaintenanceWindowRequest.make ?safe
              ~windowId ~windowTargetId ())
           (Some Values.DeregisterTargetFromMaintenanceWindowResult.to_json)
           (Some
              Values.DeregisterTargetFromMaintenanceWindowResult.error_to_json)])
let deregister_task_from_maintenance_window =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and windowId =
         flag "window-id" (required string) ~doc:"STRING MaintenanceWindowId"
       and windowTaskId =
         flag "window-task-id" (required string)
           ~doc:"STRING MaintenanceWindowTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_task_from_maintenance_window
           (Values.DeregisterTaskFromMaintenanceWindowRequest.make ~windowId
              ~windowTaskId ())
           (Some Values.DeregisterTaskFromMaintenanceWindowResult.to_json)
           (Some
              Values.DeregisterTaskFromMaintenanceWindowResult.error_to_json)])
let describe_activations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON DescribeActivationsFilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_activations
           (Values.DescribeActivationsRequest.make
              ?filters:(Option.map
                          ~f:Values.DescribeActivationsFilterList.of_json
                          filters) ?maxResults ?nextToken ())
           (Some Values.DescribeActivationsResult.to_json)
           (Some Values.DescribeActivationsResult.error_to_json)])
let describe_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING DocumentARN"
       and instanceId =
         flag "instance-id" (optional string) ~doc:"STRING InstanceId"
       and associationId =
         flag "association-id" (optional string) ~doc:"STRING AssociationId"
       and associationVersion =
         flag "association-version" (optional string)
           ~doc:"STRING AssociationVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_association
           (Values.DescribeAssociationRequest.make ?name ?instanceId
              ?associationId ?associationVersion ())
           (Some Values.DescribeAssociationResult.to_json)
           (Some Values.DescribeAssociationResult.error_to_json)])
let describe_association_execution_targets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON AssociationExecutionTargetsFilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and associationId =
         flag "association-id" (required string) ~doc:"STRING AssociationId"
       and executionId =
         flag "execution-id" (required string)
           ~doc:"STRING AssociationExecutionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_association_execution_targets
           (Values.DescribeAssociationExecutionTargetsRequest.make
              ?filters:(Option.map
                          ~f:Values.AssociationExecutionTargetsFilterList.of_json
                          filters) ?maxResults ?nextToken ~associationId
              ~executionId ())
           (Some Values.DescribeAssociationExecutionTargetsResult.to_json)
           (Some
              Values.DescribeAssociationExecutionTargetsResult.error_to_json)])
let describe_association_executions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON AssociationExecutionFilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and associationId =
         flag "association-id" (required string) ~doc:"STRING AssociationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_association_executions
           (Values.DescribeAssociationExecutionsRequest.make
              ?filters:(Option.map
                          ~f:Values.AssociationExecutionFilterList.of_json
                          filters) ?maxResults ?nextToken ~associationId ())
           (Some Values.DescribeAssociationExecutionsResult.to_json)
           (Some Values.DescribeAssociationExecutionsResult.error_to_json)])
let describe_automation_executions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON AutomationExecutionFilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_automation_executions
           (Values.DescribeAutomationExecutionsRequest.make
              ?filters:(Option.map
                          ~f:Values.AutomationExecutionFilterList.of_json
                          filters) ?maxResults ?nextToken ())
           (Some Values.DescribeAutomationExecutionsResult.to_json)
           (Some Values.DescribeAutomationExecutionsResult.error_to_json)])
let describe_automation_step_executions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON StepExecutionFilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and reverseOrder =
         flag "reverse-order" (optional bool) ~doc:"BOOL Boolean"
       and automationExecutionId =
         flag "automation-execution-id" (required string)
           ~doc:"STRING AutomationExecutionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_automation_step_executions
           (Values.DescribeAutomationStepExecutionsRequest.make
              ?filters:(Option.map ~f:Values.StepExecutionFilterList.of_json
                          filters) ?nextToken ?maxResults ?reverseOrder
              ~automationExecutionId ())
           (Some Values.DescribeAutomationStepExecutionsResult.to_json)
           (Some Values.DescribeAutomationStepExecutionsResult.error_to_json)])
let describe_available_patches =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON PatchOrchestratorFilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT PatchBaselineMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_available_patches
           (Values.DescribeAvailablePatchesRequest.make
              ?filters:(Option.map
                          ~f:Values.PatchOrchestratorFilterList.of_json
                          filters) ?maxResults ?nextToken ())
           (Some Values.DescribeAvailablePatchesResult.to_json)
           (Some Values.DescribeAvailablePatchesResult.error_to_json)])
let describe_document =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and documentVersion =
         flag "document-version" (optional string)
           ~doc:"STRING DocumentVersion"
       and versionName =
         flag "version-name" (optional string)
           ~doc:"STRING DocumentVersionName"
       and name = flag "name" (required string) ~doc:"STRING DocumentARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_document
           (Values.DescribeDocumentRequest.make ?documentVersion ?versionName
              ~name ()) (Some Values.DescribeDocumentResult.to_json)
           (Some Values.DescribeDocumentResult.error_to_json)])
let describe_document_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DocumentPermissionMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and name = flag "name" (required string) ~doc:"STRING DocumentName"
       and permissionType =
         flag "permission-type" (required json_arg)
           ~doc:"JSON DocumentPermissionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_document_permission
           (Values.DescribeDocumentPermissionRequest.make ?maxResults
              ?nextToken ~name
              ~permissionType:(Values.DocumentPermissionType.of_json
                                 permissionType) ())
           (Some Values.DescribeDocumentPermissionResponse.to_json)
           (Some Values.DescribeDocumentPermissionResponse.error_to_json)])
let describe_effective_instance_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT EffectiveInstanceAssociationMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_effective_instance_associations
           (Values.DescribeEffectiveInstanceAssociationsRequest.make
              ?maxResults ?nextToken ~instanceId ())
           (Some Values.DescribeEffectiveInstanceAssociationsResult.to_json)
           (Some
              Values.DescribeEffectiveInstanceAssociationsResult.error_to_json)])
let describe_effective_patches_for_patch_baseline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT PatchBaselineMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and baselineId =
         flag "baseline-id" (required string) ~doc:"STRING BaselineId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_effective_patches_for_patch_baseline
           (Values.DescribeEffectivePatchesForPatchBaselineRequest.make
              ?maxResults ?nextToken ~baselineId ())
           (Some
              Values.DescribeEffectivePatchesForPatchBaselineResult.to_json)
           (Some
              Values.DescribeEffectivePatchesForPatchBaselineResult.error_to_json)])
let describe_instance_associations_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instance_associations_status
           (Values.DescribeInstanceAssociationsStatusRequest.make ?maxResults
              ?nextToken ~instanceId ())
           (Some Values.DescribeInstanceAssociationsStatusResult.to_json)
           (Some
              Values.DescribeInstanceAssociationsStatusResult.error_to_json)])
let describe_instance_information =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceInformationFilterList =
         flag "instance-information-filter-list" (optional json_arg)
           ~doc:"JSON InstanceInformationFilterList"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON InstanceInformationStringFilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsEC2Compatible"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instance_information
           (Values.DescribeInstanceInformationRequest.make
              ?instanceInformationFilterList:(Option.map
                                                ~f:Values.InstanceInformationFilterList.of_json
                                                instanceInformationFilterList)
              ?filters:(Option.map
                          ~f:Values.InstanceInformationStringFilterList.of_json
                          filters) ?maxResults ?nextToken ())
           (Some Values.DescribeInstanceInformationResult.to_json)
           (Some Values.DescribeInstanceInformationResult.error_to_json)])
let describe_instance_patch_states =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT PatchComplianceMaxResults"
       and instanceIds =
         flag "instance-ids" (required json_arg) ~doc:"JSON InstanceIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instance_patch_states
           (Values.DescribeInstancePatchStatesRequest.make ?nextToken
              ?maxResults
              ~instanceIds:(Values.InstanceIdList.of_json instanceIds) ())
           (Some Values.DescribeInstancePatchStatesResult.to_json)
           (Some Values.DescribeInstancePatchStatesResult.error_to_json)])
let describe_instance_patch_states_for_patch_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON InstancePatchStateFilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT PatchComplianceMaxResults"
       and patchGroup =
         flag "patch-group" (required string) ~doc:"STRING PatchGroup" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instance_patch_states_for_patch_group
           (Values.DescribeInstancePatchStatesForPatchGroupRequest.make
              ?filters:(Option.map
                          ~f:Values.InstancePatchStateFilterList.of_json
                          filters) ?nextToken ?maxResults ~patchGroup ())
           (Some
              Values.DescribeInstancePatchStatesForPatchGroupResult.to_json)
           (Some
              Values.DescribeInstancePatchStatesForPatchGroupResult.error_to_json)])
let describe_instance_patches =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON PatchOrchestratorFilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT PatchComplianceMaxResults"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_instance_patches
           (Values.DescribeInstancePatchesRequest.make
              ?filters:(Option.map
                          ~f:Values.PatchOrchestratorFilterList.of_json
                          filters) ?nextToken ?maxResults ~instanceId ())
           (Some Values.DescribeInstancePatchesResult.to_json)
           (Some Values.DescribeInstancePatchesResult.error_to_json)])
let describe_inventory_deletions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deletionId =
         flag "deletion-id" (optional string) ~doc:"STRING UUID"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_inventory_deletions
           (Values.DescribeInventoryDeletionsRequest.make ?deletionId
              ?nextToken ?maxResults ())
           (Some Values.DescribeInventoryDeletionsResult.to_json)
           (Some Values.DescribeInventoryDeletionsResult.error_to_json)])
let describe_maintenance_window_execution_task_invocations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON MaintenanceWindowFilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaintenanceWindowMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and windowExecutionId =
         flag "window-execution-id" (required string)
           ~doc:"STRING MaintenanceWindowExecutionId"
       and taskId =
         flag "task-id" (required string)
           ~doc:"STRING MaintenanceWindowExecutionTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_maintenance_window_execution_task_invocations
           (Values.DescribeMaintenanceWindowExecutionTaskInvocationsRequest.make
              ?filters:(Option.map
                          ~f:Values.MaintenanceWindowFilterList.of_json
                          filters) ?maxResults ?nextToken ~windowExecutionId
              ~taskId ())
           (Some
              Values.DescribeMaintenanceWindowExecutionTaskInvocationsResult.to_json)
           (Some
              Values.DescribeMaintenanceWindowExecutionTaskInvocationsResult.error_to_json)])
let describe_maintenance_window_execution_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON MaintenanceWindowFilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaintenanceWindowMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and windowExecutionId =
         flag "window-execution-id" (required string)
           ~doc:"STRING MaintenanceWindowExecutionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_maintenance_window_execution_tasks
           (Values.DescribeMaintenanceWindowExecutionTasksRequest.make
              ?filters:(Option.map
                          ~f:Values.MaintenanceWindowFilterList.of_json
                          filters) ?maxResults ?nextToken ~windowExecutionId
              ())
           (Some Values.DescribeMaintenanceWindowExecutionTasksResult.to_json)
           (Some
              Values.DescribeMaintenanceWindowExecutionTasksResult.error_to_json)])
let describe_maintenance_window_executions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON MaintenanceWindowFilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaintenanceWindowMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and windowId =
         flag "window-id" (required string) ~doc:"STRING MaintenanceWindowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_maintenance_window_executions
           (Values.DescribeMaintenanceWindowExecutionsRequest.make
              ?filters:(Option.map
                          ~f:Values.MaintenanceWindowFilterList.of_json
                          filters) ?maxResults ?nextToken ~windowId ())
           (Some Values.DescribeMaintenanceWindowExecutionsResult.to_json)
           (Some
              Values.DescribeMaintenanceWindowExecutionsResult.error_to_json)])
let describe_maintenance_window_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and windowId =
         flag "window-id" (optional string) ~doc:"STRING MaintenanceWindowId"
       and targets = flag "targets" (optional json_arg) ~doc:"JSON Targets"
       and resourceType =
         flag "resource-type" (optional json_arg)
           ~doc:"JSON MaintenanceWindowResourceType"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON PatchOrchestratorFilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaintenanceWindowSearchMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_maintenance_window_schedule
           (Values.DescribeMaintenanceWindowScheduleRequest.make ?windowId
              ?targets:(Option.map ~f:Values.Targets.of_json targets)
              ?resourceType:(Option.map
                               ~f:Values.MaintenanceWindowResourceType.of_json
                               resourceType)
              ?filters:(Option.map
                          ~f:Values.PatchOrchestratorFilterList.of_json
                          filters) ?maxResults ?nextToken ())
           (Some Values.DescribeMaintenanceWindowScheduleResult.to_json)
           (Some Values.DescribeMaintenanceWindowScheduleResult.error_to_json)])
let describe_maintenance_window_targets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON MaintenanceWindowFilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaintenanceWindowMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and windowId =
         flag "window-id" (required string) ~doc:"STRING MaintenanceWindowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_maintenance_window_targets
           (Values.DescribeMaintenanceWindowTargetsRequest.make
              ?filters:(Option.map
                          ~f:Values.MaintenanceWindowFilterList.of_json
                          filters) ?maxResults ?nextToken ~windowId ())
           (Some Values.DescribeMaintenanceWindowTargetsResult.to_json)
           (Some Values.DescribeMaintenanceWindowTargetsResult.error_to_json)])
let describe_maintenance_window_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON MaintenanceWindowFilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaintenanceWindowMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and windowId =
         flag "window-id" (required string) ~doc:"STRING MaintenanceWindowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_maintenance_window_tasks
           (Values.DescribeMaintenanceWindowTasksRequest.make
              ?filters:(Option.map
                          ~f:Values.MaintenanceWindowFilterList.of_json
                          filters) ?maxResults ?nextToken ~windowId ())
           (Some Values.DescribeMaintenanceWindowTasksResult.to_json)
           (Some Values.DescribeMaintenanceWindowTasksResult.error_to_json)])
let describe_maintenance_windows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON MaintenanceWindowFilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaintenanceWindowMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_maintenance_windows
           (Values.DescribeMaintenanceWindowsRequest.make
              ?filters:(Option.map
                          ~f:Values.MaintenanceWindowFilterList.of_json
                          filters) ?maxResults ?nextToken ())
           (Some Values.DescribeMaintenanceWindowsResult.to_json)
           (Some Values.DescribeMaintenanceWindowsResult.error_to_json)])
let describe_maintenance_windows_for_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaintenanceWindowSearchMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and targets = flag "targets" (required json_arg) ~doc:"JSON Targets"
       and resourceType =
         flag "resource-type" (required json_arg)
           ~doc:"JSON MaintenanceWindowResourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_maintenance_windows_for_target
           (Values.DescribeMaintenanceWindowsForTargetRequest.make
              ?maxResults ?nextToken
              ~targets:(Values.Targets.of_json targets)
              ~resourceType:(Values.MaintenanceWindowResourceType.of_json
                               resourceType) ())
           (Some Values.DescribeMaintenanceWindowsForTargetResult.to_json)
           (Some
              Values.DescribeMaintenanceWindowsForTargetResult.error_to_json)])
let describe_ops_items =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and opsItemFilters =
         flag "ops-item-filters" (optional json_arg)
           ~doc:"JSON OpsItemFilters"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT OpsItemMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_ops_items
           (Values.DescribeOpsItemsRequest.make
              ?opsItemFilters:(Option.map ~f:Values.OpsItemFilters.of_json
                                 opsItemFilters) ?maxResults ?nextToken ())
           (Some Values.DescribeOpsItemsResponse.to_json)
           (Some Values.DescribeOpsItemsResponse.error_to_json)])
let describe_parameters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON ParametersFilterList"
       and parameterFilters =
         flag "parameter-filters" (optional json_arg)
           ~doc:"JSON ParameterStringFilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_parameters
           (Values.DescribeParametersRequest.make
              ?filters:(Option.map ~f:Values.ParametersFilterList.of_json
                          filters)
              ?parameterFilters:(Option.map
                                   ~f:Values.ParameterStringFilterList.of_json
                                   parameterFilters) ?maxResults ?nextToken
              ()) (Some Values.DescribeParametersResult.to_json)
           (Some Values.DescribeParametersResult.error_to_json)])
let describe_patch_baselines =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON PatchOrchestratorFilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT PatchBaselineMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_patch_baselines
           (Values.DescribePatchBaselinesRequest.make
              ?filters:(Option.map
                          ~f:Values.PatchOrchestratorFilterList.of_json
                          filters) ?maxResults ?nextToken ())
           (Some Values.DescribePatchBaselinesResult.to_json)
           (Some Values.DescribePatchBaselinesResult.error_to_json)])
let describe_patch_group_state =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and patchGroup =
         flag "patch-group" (required string) ~doc:"STRING PatchGroup" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_patch_group_state
           (Values.DescribePatchGroupStateRequest.make ~patchGroup ())
           (Some Values.DescribePatchGroupStateResult.to_json)
           (Some Values.DescribePatchGroupStateResult.error_to_json)])
let describe_patch_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT PatchBaselineMaxResults"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON PatchOrchestratorFilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_patch_groups
           (Values.DescribePatchGroupsRequest.make ?maxResults
              ?filters:(Option.map
                          ~f:Values.PatchOrchestratorFilterList.of_json
                          filters) ?nextToken ())
           (Some Values.DescribePatchGroupsResult.to_json)
           (Some Values.DescribePatchGroupsResult.error_to_json)])
let describe_patch_properties =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and patchSet =
         flag "patch-set" (optional json_arg) ~doc:"JSON PatchSet"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and operatingSystem =
         flag "operating-system" (required json_arg)
           ~doc:"JSON OperatingSystem"
       and property =
         flag "property" (required json_arg) ~doc:"JSON PatchProperty" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_patch_properties
           (Values.DescribePatchPropertiesRequest.make
              ?patchSet:(Option.map ~f:Values.PatchSet.of_json patchSet)
              ?maxResults ?nextToken
              ~operatingSystem:(Values.OperatingSystem.of_json
                                  operatingSystem)
              ~property:(Values.PatchProperty.of_json property) ())
           (Some Values.DescribePatchPropertiesResult.to_json)
           (Some Values.DescribePatchPropertiesResult.error_to_json)])
let describe_sessions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT SessionMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON SessionFilterList"
       and state = flag "state" (required json_arg) ~doc:"JSON SessionState" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_sessions
           (Values.DescribeSessionsRequest.make ?maxResults ?nextToken
              ?filters:(Option.map ~f:Values.SessionFilterList.of_json
                          filters) ~state:(Values.SessionState.of_json state)
              ()) (Some Values.DescribeSessionsResponse.to_json)
           (Some Values.DescribeSessionsResponse.error_to_json)])
let disassociate_ops_item_related_item =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and opsItemId =
         flag "ops-item-id" (required string) ~doc:"STRING OpsItemId"
       and associationId =
         flag "association-id" (required string)
           ~doc:"STRING OpsItemRelatedItemAssociationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_ops_item_related_item
           (Values.DisassociateOpsItemRelatedItemRequest.make ~opsItemId
              ~associationId ())
           (Some Values.DisassociateOpsItemRelatedItemResponse.to_json)
           (Some Values.DisassociateOpsItemRelatedItemResponse.error_to_json)])
let get_automation_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and automationExecutionId =
         flag "automation-execution-id" (required string)
           ~doc:"STRING AutomationExecutionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_automation_execution
           (Values.GetAutomationExecutionRequest.make ~automationExecutionId
              ()) (Some Values.GetAutomationExecutionResult.to_json)
           (Some Values.GetAutomationExecutionResult.error_to_json)])
let get_calendar_state =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and atTime =
         flag "at-time" (optional string) ~doc:"STRING ISO8601String"
       and calendarNames =
         flag "calendar-names" (required json_arg)
           ~doc:"JSON CalendarNameOrARNList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_calendar_state
           (Values.GetCalendarStateRequest.make ?atTime
              ~calendarNames:(Values.CalendarNameOrARNList.of_json
                                calendarNames) ())
           (Some Values.GetCalendarStateResponse.to_json)
           (Some Values.GetCalendarStateResponse.error_to_json)])
let get_command_invocation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pluginName =
         flag "plugin-name" (optional string) ~doc:"STRING CommandPluginName"
       and commandId =
         flag "command-id" (required string) ~doc:"STRING CommandId"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_command_invocation
           (Values.GetCommandInvocationRequest.make ?pluginName ~commandId
              ~instanceId ())
           (Some Values.GetCommandInvocationResult.to_json)
           (Some Values.GetCommandInvocationResult.error_to_json)])
let get_connection_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and target =
         flag "target" (required string) ~doc:"STRING SessionTarget" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_connection_status
           (Values.GetConnectionStatusRequest.make ~target ())
           (Some Values.GetConnectionStatusResponse.to_json)
           (Some Values.GetConnectionStatusResponse.error_to_json)])
let get_default_patch_baseline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and operatingSystem =
         flag "operating-system" (optional json_arg)
           ~doc:"JSON OperatingSystem" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_default_patch_baseline
           (Values.GetDefaultPatchBaselineRequest.make
              ?operatingSystem:(Option.map ~f:Values.OperatingSystem.of_json
                                  operatingSystem) ())
           (Some Values.GetDefaultPatchBaselineResult.to_json)
           (Some Values.GetDefaultPatchBaselineResult.error_to_json)])
let get_deployable_patch_snapshot_for_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and baselineOverride =
         flag "baseline-override" (optional json_arg)
           ~doc:"JSON BaselineOverride"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and snapshotId =
         flag "snapshot-id" (required string) ~doc:"STRING SnapshotId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_deployable_patch_snapshot_for_instance
           (Values.GetDeployablePatchSnapshotForInstanceRequest.make
              ?baselineOverride:(Option.map
                                   ~f:Values.BaselineOverride.of_json
                                   baselineOverride) ~instanceId ~snapshotId
              ())
           (Some Values.GetDeployablePatchSnapshotForInstanceResult.to_json)
           (Some
              Values.GetDeployablePatchSnapshotForInstanceResult.error_to_json)])
let get_document =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionName =
         flag "version-name" (optional string)
           ~doc:"STRING DocumentVersionName"
       and documentVersion =
         flag "document-version" (optional string)
           ~doc:"STRING DocumentVersion"
       and documentFormat =
         flag "document-format" (optional json_arg)
           ~doc:"JSON DocumentFormat"
       and name = flag "name" (required string) ~doc:"STRING DocumentARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_document
           (Values.GetDocumentRequest.make ?versionName ?documentVersion
              ?documentFormat:(Option.map ~f:Values.DocumentFormat.of_json
                                 documentFormat) ~name ())
           (Some Values.GetDocumentResult.to_json)
           (Some Values.GetDocumentResult.error_to_json)])
let get_inventory =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON InventoryFilterList"
       and aggregators =
         flag "aggregators" (optional json_arg)
           ~doc:"JSON InventoryAggregatorList"
       and resultAttributes =
         flag "result-attributes" (optional json_arg)
           ~doc:"JSON ResultAttributeList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_inventory
           (Values.GetInventoryRequest.make
              ?filters:(Option.map ~f:Values.InventoryFilterList.of_json
                          filters)
              ?aggregators:(Option.map
                              ~f:Values.InventoryAggregatorList.of_json
                              aggregators)
              ?resultAttributes:(Option.map
                                   ~f:Values.ResultAttributeList.of_json
                                   resultAttributes) ?nextToken ?maxResults
              ()) (Some Values.GetInventoryResult.to_json)
           (Some Values.GetInventoryResult.error_to_json)])
let get_inventory_schema =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and typeName =
         flag "type-name" (optional string)
           ~doc:"STRING InventoryItemTypeNameFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT GetInventorySchemaMaxResults"
       and aggregator =
         flag "aggregator" (optional bool) ~doc:"BOOL AggregatorSchemaOnly"
       and subType =
         flag "sub-type" (optional bool) ~doc:"BOOL IsSubTypeSchema" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_inventory_schema
           (Values.GetInventorySchemaRequest.make ?typeName ?nextToken
              ?maxResults ?aggregator ?subType ())
           (Some Values.GetInventorySchemaResult.to_json)
           (Some Values.GetInventorySchemaResult.error_to_json)])
let get_maintenance_window =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and windowId =
         flag "window-id" (required string) ~doc:"STRING MaintenanceWindowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_maintenance_window
           (Values.GetMaintenanceWindowRequest.make ~windowId ())
           (Some Values.GetMaintenanceWindowResult.to_json)
           (Some Values.GetMaintenanceWindowResult.error_to_json)])
let get_maintenance_window_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and windowExecutionId =
         flag "window-execution-id" (required string)
           ~doc:"STRING MaintenanceWindowExecutionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_maintenance_window_execution
           (Values.GetMaintenanceWindowExecutionRequest.make
              ~windowExecutionId ())
           (Some Values.GetMaintenanceWindowExecutionResult.to_json)
           (Some Values.GetMaintenanceWindowExecutionResult.error_to_json)])
let get_maintenance_window_execution_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and windowExecutionId =
         flag "window-execution-id" (required string)
           ~doc:"STRING MaintenanceWindowExecutionId"
       and taskId =
         flag "task-id" (required string)
           ~doc:"STRING MaintenanceWindowExecutionTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_maintenance_window_execution_task
           (Values.GetMaintenanceWindowExecutionTaskRequest.make
              ~windowExecutionId ~taskId ())
           (Some Values.GetMaintenanceWindowExecutionTaskResult.to_json)
           (Some Values.GetMaintenanceWindowExecutionTaskResult.error_to_json)])
let get_maintenance_window_execution_task_invocation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and windowExecutionId =
         flag "window-execution-id" (required string)
           ~doc:"STRING MaintenanceWindowExecutionId"
       and taskId =
         flag "task-id" (required string)
           ~doc:"STRING MaintenanceWindowExecutionTaskId"
       and invocationId =
         flag "invocation-id" (required string)
           ~doc:"STRING MaintenanceWindowExecutionTaskInvocationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_maintenance_window_execution_task_invocation
           (Values.GetMaintenanceWindowExecutionTaskInvocationRequest.make
              ~windowExecutionId ~taskId ~invocationId ())
           (Some
              Values.GetMaintenanceWindowExecutionTaskInvocationResult.to_json)
           (Some
              Values.GetMaintenanceWindowExecutionTaskInvocationResult.error_to_json)])
let get_maintenance_window_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and windowId =
         flag "window-id" (required string) ~doc:"STRING MaintenanceWindowId"
       and windowTaskId =
         flag "window-task-id" (required string)
           ~doc:"STRING MaintenanceWindowTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_maintenance_window_task
           (Values.GetMaintenanceWindowTaskRequest.make ~windowId
              ~windowTaskId ())
           (Some Values.GetMaintenanceWindowTaskResult.to_json)
           (Some Values.GetMaintenanceWindowTaskResult.error_to_json)])
let get_ops_item =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and opsItemId =
         flag "ops-item-id" (required string) ~doc:"STRING OpsItemId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_ops_item (Values.GetOpsItemRequest.make ~opsItemId ())
           (Some Values.GetOpsItemResponse.to_json)
           (Some Values.GetOpsItemResponse.error_to_json)])
let get_ops_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT GetOpsMetadataMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and opsMetadataArn =
         flag "ops-metadata-arn" (required string)
           ~doc:"STRING OpsMetadataArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_ops_metadata
           (Values.GetOpsMetadataRequest.make ?maxResults ?nextToken
              ~opsMetadataArn ()) (Some Values.GetOpsMetadataResult.to_json)
           (Some Values.GetOpsMetadataResult.error_to_json)])
let get_ops_summary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and syncName =
         flag "sync-name" (optional string)
           ~doc:"STRING ResourceDataSyncName"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON OpsFilterList"
       and aggregators =
         flag "aggregators" (optional json_arg) ~doc:"JSON OpsAggregatorList"
       and resultAttributes =
         flag "result-attributes" (optional json_arg)
           ~doc:"JSON OpsResultAttributeList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_ops_summary
           (Values.GetOpsSummaryRequest.make ?syncName
              ?filters:(Option.map ~f:Values.OpsFilterList.of_json filters)
              ?aggregators:(Option.map ~f:Values.OpsAggregatorList.of_json
                              aggregators)
              ?resultAttributes:(Option.map
                                   ~f:Values.OpsResultAttributeList.of_json
                                   resultAttributes) ?nextToken ?maxResults
              ()) (Some Values.GetOpsSummaryResult.to_json)
           (Some Values.GetOpsSummaryResult.error_to_json)])
let get_parameter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and withDecryption =
         flag "with-decryption" (optional bool) ~doc:"BOOL Boolean"
       and name = flag "name" (required string) ~doc:"STRING PSParameterName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_parameter
           (Values.GetParameterRequest.make ?withDecryption ~name ())
           (Some Values.GetParameterResult.to_json)
           (Some Values.GetParameterResult.error_to_json)])
let get_parameter_history =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and withDecryption =
         flag "with-decryption" (optional bool) ~doc:"BOOL Boolean"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and name = flag "name" (required string) ~doc:"STRING PSParameterName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_parameter_history
           (Values.GetParameterHistoryRequest.make ?withDecryption
              ?maxResults ?nextToken ~name ())
           (Some Values.GetParameterHistoryResult.to_json)
           (Some Values.GetParameterHistoryResult.error_to_json)])
let get_parameters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and withDecryption =
         flag "with-decryption" (optional bool) ~doc:"BOOL Boolean"
       and names =
         flag "names" (required json_arg) ~doc:"JSON ParameterNameList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_parameters
           (Values.GetParametersRequest.make ?withDecryption
              ~names:(Values.ParameterNameList.of_json names) ())
           (Some Values.GetParametersResult.to_json)
           (Some Values.GetParametersResult.error_to_json)])
let get_parameters_by_path =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and recursive = flag "recursive" (optional bool) ~doc:"BOOL Boolean"
       and parameterFilters =
         flag "parameter-filters" (optional json_arg)
           ~doc:"JSON ParameterStringFilterList"
       and withDecryption =
         flag "with-decryption" (optional bool) ~doc:"BOOL Boolean"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT GetParametersByPathMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and path = flag "path" (required string) ~doc:"STRING PSParameterName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_parameters_by_path
           (Values.GetParametersByPathRequest.make ?recursive
              ?parameterFilters:(Option.map
                                   ~f:Values.ParameterStringFilterList.of_json
                                   parameterFilters) ?withDecryption
              ?maxResults ?nextToken ~path ())
           (Some Values.GetParametersByPathResult.to_json)
           (Some Values.GetParametersByPathResult.error_to_json)])
let get_patch_baseline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and baselineId =
         flag "baseline-id" (required string) ~doc:"STRING BaselineId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_patch_baseline
           (Values.GetPatchBaselineRequest.make ~baselineId ())
           (Some Values.GetPatchBaselineResult.to_json)
           (Some Values.GetPatchBaselineResult.error_to_json)])
let get_patch_baseline_for_patch_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and operatingSystem =
         flag "operating-system" (optional json_arg)
           ~doc:"JSON OperatingSystem"
       and patchGroup =
         flag "patch-group" (required string) ~doc:"STRING PatchGroup" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_patch_baseline_for_patch_group
           (Values.GetPatchBaselineForPatchGroupRequest.make
              ?operatingSystem:(Option.map ~f:Values.OperatingSystem.of_json
                                  operatingSystem) ~patchGroup ())
           (Some Values.GetPatchBaselineForPatchGroupResult.to_json)
           (Some Values.GetPatchBaselineForPatchGroupResult.error_to_json)])
let get_service_setting =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and settingId =
         flag "setting-id" (required string) ~doc:"STRING ServiceSettingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service_setting
           (Values.GetServiceSettingRequest.make ~settingId ())
           (Some Values.GetServiceSettingResult.to_json)
           (Some Values.GetServiceSettingResult.error_to_json)])
let label_parameter_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parameterVersion =
         flag "parameter-version" (optional json_arg)
           ~doc:"JSON PSParameterVersion"
       and name = flag "name" (required string) ~doc:"STRING PSParameterName"
       and labels =
         flag "labels" (required json_arg) ~doc:"JSON ParameterLabelList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.label_parameter_version
           (Values.LabelParameterVersionRequest.make
              ?parameterVersion:(Option.map
                                   ~f:Values.PSParameterVersion.of_json
                                   parameterVersion) ~name
              ~labels:(Values.ParameterLabelList.of_json labels) ())
           (Some Values.LabelParameterVersionResult.to_json)
           (Some Values.LabelParameterVersionResult.error_to_json)])
let list_association_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and associationId =
         flag "association-id" (required string) ~doc:"STRING AssociationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_association_versions
           (Values.ListAssociationVersionsRequest.make ?maxResults ?nextToken
              ~associationId ())
           (Some Values.ListAssociationVersionsResult.to_json)
           (Some Values.ListAssociationVersionsResult.error_to_json)])
let list_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and associationFilterList =
         flag "association-filter-list" (optional json_arg)
           ~doc:"JSON AssociationFilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_associations
           (Values.ListAssociationsRequest.make
              ?associationFilterList:(Option.map
                                        ~f:Values.AssociationFilterList.of_json
                                        associationFilterList) ?maxResults
              ?nextToken ()) (Some Values.ListAssociationsResult.to_json)
           (Some Values.ListAssociationsResult.error_to_json)])
let list_command_invocations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and commandId =
         flag "command-id" (optional string) ~doc:"STRING CommandId"
       and instanceId =
         flag "instance-id" (optional string) ~doc:"STRING InstanceId"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT CommandMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON CommandFilterList"
       and details = flag "details" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_command_invocations
           (Values.ListCommandInvocationsRequest.make ?commandId ?instanceId
              ?maxResults ?nextToken
              ?filters:(Option.map ~f:Values.CommandFilterList.of_json
                          filters) ?details ())
           (Some Values.ListCommandInvocationsResult.to_json)
           (Some Values.ListCommandInvocationsResult.error_to_json)])
let list_commands =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and commandId =
         flag "command-id" (optional string) ~doc:"STRING CommandId"
       and instanceId =
         flag "instance-id" (optional string) ~doc:"STRING InstanceId"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT CommandMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON CommandFilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_commands
           (Values.ListCommandsRequest.make ?commandId ?instanceId
              ?maxResults ?nextToken
              ?filters:(Option.map ~f:Values.CommandFilterList.of_json
                          filters) ())
           (Some Values.ListCommandsResult.to_json)
           (Some Values.ListCommandsResult.error_to_json)])
let list_compliance_items =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON ComplianceStringFilterList"
       and resourceIds =
         flag "resource-ids" (optional json_arg)
           ~doc:"JSON ComplianceResourceIdList"
       and resourceTypes =
         flag "resource-types" (optional json_arg)
           ~doc:"JSON ComplianceResourceTypeList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_compliance_items
           (Values.ListComplianceItemsRequest.make
              ?filters:(Option.map
                          ~f:Values.ComplianceStringFilterList.of_json
                          filters)
              ?resourceIds:(Option.map
                              ~f:Values.ComplianceResourceIdList.of_json
                              resourceIds)
              ?resourceTypes:(Option.map
                                ~f:Values.ComplianceResourceTypeList.of_json
                                resourceTypes) ?nextToken ?maxResults ())
           (Some Values.ListComplianceItemsResult.to_json)
           (Some Values.ListComplianceItemsResult.error_to_json)])
let list_compliance_summaries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON ComplianceStringFilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_compliance_summaries
           (Values.ListComplianceSummariesRequest.make
              ?filters:(Option.map
                          ~f:Values.ComplianceStringFilterList.of_json
                          filters) ?nextToken ?maxResults ())
           (Some Values.ListComplianceSummariesResult.to_json)
           (Some Values.ListComplianceSummariesResult.error_to_json)])
let list_document_metadata_history =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and documentVersion =
         flag "document-version" (optional string)
           ~doc:"STRING DocumentVersion"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and name = flag "name" (required string) ~doc:"STRING DocumentName"
       and metadata =
         flag "metadata" (required json_arg) ~doc:"JSON DocumentMetadataEnum" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_document_metadata_history
           (Values.ListDocumentMetadataHistoryRequest.make ?documentVersion
              ?nextToken ?maxResults ~name
              ~metadata:(Values.DocumentMetadataEnum.of_json metadata) ())
           (Some Values.ListDocumentMetadataHistoryResponse.to_json)
           (Some Values.ListDocumentMetadataHistoryResponse.error_to_json)])
let list_document_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and name = flag "name" (required string) ~doc:"STRING DocumentARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_document_versions
           (Values.ListDocumentVersionsRequest.make ?maxResults ?nextToken
              ~name ()) (Some Values.ListDocumentVersionsResult.to_json)
           (Some Values.ListDocumentVersionsResult.error_to_json)])
let list_documents =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and documentFilterList =
         flag "document-filter-list" (optional json_arg)
           ~doc:"JSON DocumentFilterList"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON DocumentKeyValuesFilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_documents
           (Values.ListDocumentsRequest.make
              ?documentFilterList:(Option.map
                                     ~f:Values.DocumentFilterList.of_json
                                     documentFilterList)
              ?filters:(Option.map
                          ~f:Values.DocumentKeyValuesFilterList.of_json
                          filters) ?maxResults ?nextToken ())
           (Some Values.ListDocumentsResult.to_json)
           (Some Values.ListDocumentsResult.error_to_json)])
let list_inventory_entries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON InventoryFilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and typeName =
         flag "type-name" (required string)
           ~doc:"STRING InventoryItemTypeName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_inventory_entries
           (Values.ListInventoryEntriesRequest.make
              ?filters:(Option.map ~f:Values.InventoryFilterList.of_json
                          filters) ?nextToken ?maxResults ~instanceId
              ~typeName ()) (Some Values.ListInventoryEntriesResult.to_json)
           (Some Values.ListInventoryEntriesResult.error_to_json)])
let list_ops_item_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON OpsItemEventFilters"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT OpsItemEventMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_ops_item_events
           (Values.ListOpsItemEventsRequest.make
              ?filters:(Option.map ~f:Values.OpsItemEventFilters.of_json
                          filters) ?maxResults ?nextToken ())
           (Some Values.ListOpsItemEventsResponse.to_json)
           (Some Values.ListOpsItemEventsResponse.error_to_json)])
let list_ops_item_related_items =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and opsItemId =
         flag "ops-item-id" (optional string) ~doc:"STRING OpsItemId"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON OpsItemRelatedItemsFilters"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT OpsItemRelatedItemsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_ops_item_related_items
           (Values.ListOpsItemRelatedItemsRequest.make ?opsItemId
              ?filters:(Option.map
                          ~f:Values.OpsItemRelatedItemsFilters.of_json
                          filters) ?maxResults ?nextToken ())
           (Some Values.ListOpsItemRelatedItemsResponse.to_json)
           (Some Values.ListOpsItemRelatedItemsResponse.error_to_json)])
let list_ops_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON OpsMetadataFilterList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListOpsMetadataMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_ops_metadata
           (Values.ListOpsMetadataRequest.make
              ?filters:(Option.map ~f:Values.OpsMetadataFilterList.of_json
                          filters) ?maxResults ?nextToken ())
           (Some Values.ListOpsMetadataResult.to_json)
           (Some Values.ListOpsMetadataResult.error_to_json)])
let list_resource_compliance_summaries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON ComplianceStringFilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resource_compliance_summaries
           (Values.ListResourceComplianceSummariesRequest.make
              ?filters:(Option.map
                          ~f:Values.ComplianceStringFilterList.of_json
                          filters) ?nextToken ?maxResults ())
           (Some Values.ListResourceComplianceSummariesResult.to_json)
           (Some Values.ListResourceComplianceSummariesResult.error_to_json)])
let list_resource_data_sync =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and syncType =
         flag "sync-type" (optional string)
           ~doc:"STRING ResourceDataSyncType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resource_data_sync
           (Values.ListResourceDataSyncRequest.make ?syncType ?nextToken
              ?maxResults ())
           (Some Values.ListResourceDataSyncResult.to_json)
           (Some Values.ListResourceDataSyncResult.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceType =
         flag "resource-type" (required json_arg)
           ~doc:"JSON ResourceTypeForTagging"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make
              ~resourceType:(Values.ResourceTypeForTagging.of_json
                               resourceType) ~resourceId ())
           (Some Values.ListTagsForResourceResult.to_json)
           (Some Values.ListTagsForResourceResult.error_to_json)])
let modify_document_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIdsToAdd =
         flag "account-ids-to-add" (optional json_arg)
           ~doc:"JSON AccountIdList"
       and accountIdsToRemove =
         flag "account-ids-to-remove" (optional json_arg)
           ~doc:"JSON AccountIdList"
       and sharedDocumentVersion =
         flag "shared-document-version" (optional string)
           ~doc:"STRING SharedDocumentVersion"
       and name = flag "name" (required string) ~doc:"STRING DocumentName"
       and permissionType =
         flag "permission-type" (required json_arg)
           ~doc:"JSON DocumentPermissionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_document_permission
           (Values.ModifyDocumentPermissionRequest.make
              ?accountIdsToAdd:(Option.map ~f:Values.AccountIdList.of_json
                                  accountIdsToAdd)
              ?accountIdsToRemove:(Option.map ~f:Values.AccountIdList.of_json
                                     accountIdsToRemove)
              ?sharedDocumentVersion ~name
              ~permissionType:(Values.DocumentPermissionType.of_json
                                 permissionType) ())
           (Some Values.ModifyDocumentPermissionResponse.to_json)
           (Some Values.ModifyDocumentPermissionResponse.error_to_json)])
let put_compliance_items =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and itemContentHash =
         flag "item-content-hash" (optional string)
           ~doc:"STRING ComplianceItemContentHash"
       and uploadType =
         flag "upload-type" (optional json_arg)
           ~doc:"JSON ComplianceUploadType"
       and resourceId =
         flag "resource-id" (required string)
           ~doc:"STRING ComplianceResourceId"
       and resourceType =
         flag "resource-type" (required string)
           ~doc:"STRING ComplianceResourceType"
       and complianceType =
         flag "compliance-type" (required string)
           ~doc:"STRING ComplianceTypeName"
       and executionSummary =
         flag "execution-summary" (required json_arg)
           ~doc:"JSON ComplianceExecutionSummary"
       and items =
         flag "items" (required json_arg) ~doc:"JSON ComplianceItemEntryList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_compliance_items
           (Values.PutComplianceItemsRequest.make ?itemContentHash
              ?uploadType:(Option.map ~f:Values.ComplianceUploadType.of_json
                             uploadType) ~resourceId ~resourceType
              ~complianceType
              ~executionSummary:(Values.ComplianceExecutionSummary.of_json
                                   executionSummary)
              ~items:(Values.ComplianceItemEntryList.of_json items) ())
           (Some Values.PutComplianceItemsResult.to_json)
           (Some Values.PutComplianceItemsResult.error_to_json)])
let put_inventory =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and items =
         flag "items" (required json_arg) ~doc:"JSON InventoryItemList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_inventory
           (Values.PutInventoryRequest.make ~instanceId
              ~items:(Values.InventoryItemList.of_json items) ())
           (Some Values.PutInventoryResult.to_json)
           (Some Values.PutInventoryResult.error_to_json)])
let put_parameter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ParameterDescription"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON ParameterType"
       and keyId =
         flag "key-id" (optional string) ~doc:"STRING ParameterKeyId"
       and overwrite = flag "overwrite" (optional bool) ~doc:"BOOL Boolean"
       and allowedPattern =
         flag "allowed-pattern" (optional string)
           ~doc:"STRING AllowedPattern"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and tier = flag "tier" (optional json_arg) ~doc:"JSON ParameterTier"
       and policies =
         flag "policies" (optional string) ~doc:"STRING ParameterPolicies"
       and dataType =
         flag "data-type" (optional string) ~doc:"STRING ParameterDataType"
       and name = flag "name" (required string) ~doc:"STRING PSParameterName"
       and value =
         flag "value" (required string) ~doc:"STRING PSParameterValue" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_parameter
           (Values.PutParameterRequest.make ?description
              ?type_:(Option.map ~f:Values.ParameterType.of_json type_)
              ?keyId ?overwrite ?allowedPattern
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?tier:(Option.map ~f:Values.ParameterTier.of_json tier)
              ?policies ?dataType ~name ~value ())
           (Some Values.PutParameterResult.to_json)
           (Some Values.PutParameterResult.error_to_json)])
let register_default_patch_baseline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and baselineId =
         flag "baseline-id" (required string) ~doc:"STRING BaselineId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_default_patch_baseline
           (Values.RegisterDefaultPatchBaselineRequest.make ~baselineId ())
           (Some Values.RegisterDefaultPatchBaselineResult.to_json)
           (Some Values.RegisterDefaultPatchBaselineResult.error_to_json)])
let register_patch_baseline_for_patch_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and baselineId =
         flag "baseline-id" (required string) ~doc:"STRING BaselineId"
       and patchGroup =
         flag "patch-group" (required string) ~doc:"STRING PatchGroup" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_patch_baseline_for_patch_group
           (Values.RegisterPatchBaselineForPatchGroupRequest.make ~baselineId
              ~patchGroup ())
           (Some Values.RegisterPatchBaselineForPatchGroupResult.to_json)
           (Some
              Values.RegisterPatchBaselineForPatchGroupResult.error_to_json)])
let register_target_with_maintenance_window =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ownerInformation =
         flag "owner-information" (optional string)
           ~doc:"STRING OwnerInformation"
       and name =
         flag "name" (optional string) ~doc:"STRING MaintenanceWindowName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING MaintenanceWindowDescription"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and windowId =
         flag "window-id" (required string) ~doc:"STRING MaintenanceWindowId"
       and resourceType =
         flag "resource-type" (required json_arg)
           ~doc:"JSON MaintenanceWindowResourceType"
       and targets = flag "targets" (required json_arg) ~doc:"JSON Targets" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_target_with_maintenance_window
           (Values.RegisterTargetWithMaintenanceWindowRequest.make
              ?ownerInformation ?name ?description ?clientToken ~windowId
              ~resourceType:(Values.MaintenanceWindowResourceType.of_json
                               resourceType)
              ~targets:(Values.Targets.of_json targets) ())
           (Some Values.RegisterTargetWithMaintenanceWindowResult.to_json)
           (Some
              Values.RegisterTargetWithMaintenanceWindowResult.error_to_json)])
let register_task_with_maintenance_window =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targets = flag "targets" (optional json_arg) ~doc:"JSON Targets"
       and serviceRoleArn =
         flag "service-role-arn" (optional string) ~doc:"STRING ServiceRole"
       and taskParameters =
         flag "task-parameters" (optional json_arg)
           ~doc:"JSON MaintenanceWindowTaskParameters"
       and taskInvocationParameters =
         flag "task-invocation-parameters" (optional json_arg)
           ~doc:"JSON MaintenanceWindowTaskInvocationParameters"
       and priority =
         flag "priority" (optional int)
           ~doc:"INT MaintenanceWindowTaskPriority"
       and maxConcurrency =
         flag "max-concurrency" (optional string)
           ~doc:"STRING MaxConcurrency"
       and maxErrors =
         flag "max-errors" (optional string) ~doc:"STRING MaxErrors"
       and loggingInfo =
         flag "logging-info" (optional json_arg) ~doc:"JSON LoggingInfo"
       and name =
         flag "name" (optional string) ~doc:"STRING MaintenanceWindowName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING MaintenanceWindowDescription"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and cutoffBehavior =
         flag "cutoff-behavior" (optional json_arg)
           ~doc:"JSON MaintenanceWindowTaskCutoffBehavior"
       and windowId =
         flag "window-id" (required string) ~doc:"STRING MaintenanceWindowId"
       and taskArn =
         flag "task-arn" (required string)
           ~doc:"STRING MaintenanceWindowTaskArn"
       and taskType =
         flag "task-type" (required json_arg)
           ~doc:"JSON MaintenanceWindowTaskType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_task_with_maintenance_window
           (Values.RegisterTaskWithMaintenanceWindowRequest.make
              ?targets:(Option.map ~f:Values.Targets.of_json targets)
              ?serviceRoleArn
              ?taskParameters:(Option.map
                                 ~f:Values.MaintenanceWindowTaskParameters.of_json
                                 taskParameters)
              ?taskInvocationParameters:(Option.map
                                           ~f:Values.MaintenanceWindowTaskInvocationParameters.of_json
                                           taskInvocationParameters)
              ?priority ?maxConcurrency ?maxErrors
              ?loggingInfo:(Option.map ~f:Values.LoggingInfo.of_json
                              loggingInfo) ?name ?description ?clientToken
              ?cutoffBehavior:(Option.map
                                 ~f:Values.MaintenanceWindowTaskCutoffBehavior.of_json
                                 cutoffBehavior) ~windowId ~taskArn
              ~taskType:(Values.MaintenanceWindowTaskType.of_json taskType)
              ())
           (Some Values.RegisterTaskWithMaintenanceWindowResult.to_json)
           (Some Values.RegisterTaskWithMaintenanceWindowResult.error_to_json)])
let remove_tags_from_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceType =
         flag "resource-type" (required json_arg)
           ~doc:"JSON ResourceTypeForTagging"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceId"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON KeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_tags_from_resource
           (Values.RemoveTagsFromResourceRequest.make
              ~resourceType:(Values.ResourceTypeForTagging.of_json
                               resourceType) ~resourceId
              ~tagKeys:(Values.KeyList.of_json tagKeys) ())
           (Some Values.RemoveTagsFromResourceResult.to_json)
           (Some Values.RemoveTagsFromResourceResult.error_to_json)])
let reset_service_setting =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and settingId =
         flag "setting-id" (required string) ~doc:"STRING ServiceSettingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reset_service_setting
           (Values.ResetServiceSettingRequest.make ~settingId ())
           (Some Values.ResetServiceSettingResult.to_json)
           (Some Values.ResetServiceSettingResult.error_to_json)])
let resume_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING SessionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.resume_session (Values.ResumeSessionRequest.make ~sessionId ())
           (Some Values.ResumeSessionResponse.to_json)
           (Some Values.ResumeSessionResponse.error_to_json)])
let send_automation_signal =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and payload =
         flag "payload" (optional json_arg)
           ~doc:"JSON AutomationParameterMap"
       and automationExecutionId =
         flag "automation-execution-id" (required string)
           ~doc:"STRING AutomationExecutionId"
       and signalType =
         flag "signal-type" (required json_arg) ~doc:"JSON SignalType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_automation_signal
           (Values.SendAutomationSignalRequest.make
              ?payload:(Option.map ~f:Values.AutomationParameterMap.of_json
                          payload) ~automationExecutionId
              ~signalType:(Values.SignalType.of_json signalType) ())
           (Some Values.SendAutomationSignalResult.to_json)
           (Some Values.SendAutomationSignalResult.error_to_json)])
let send_command =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceIds =
         flag "instance-ids" (optional json_arg) ~doc:"JSON InstanceIdList"
       and targets = flag "targets" (optional json_arg) ~doc:"JSON Targets"
       and documentVersion =
         flag "document-version" (optional string)
           ~doc:"STRING DocumentVersion"
       and documentHash =
         flag "document-hash" (optional string) ~doc:"STRING DocumentHash"
       and documentHashType =
         flag "document-hash-type" (optional json_arg)
           ~doc:"JSON DocumentHashType"
       and timeoutSeconds =
         flag "timeout-seconds" (optional int) ~doc:"INT TimeoutSeconds"
       and comment = flag "comment" (optional string) ~doc:"STRING Comment"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON Parameters"
       and outputS3Region =
         flag "output-s3-region" (optional string) ~doc:"STRING S3Region"
       and outputS3BucketName =
         flag "output-s3-bucket-name" (optional string)
           ~doc:"STRING S3BucketName"
       and outputS3KeyPrefix =
         flag "output-s3-key-prefix" (optional string)
           ~doc:"STRING S3KeyPrefix"
       and maxConcurrency =
         flag "max-concurrency" (optional string)
           ~doc:"STRING MaxConcurrency"
       and maxErrors =
         flag "max-errors" (optional string) ~doc:"STRING MaxErrors"
       and serviceRoleArn =
         flag "service-role-arn" (optional string) ~doc:"STRING ServiceRole"
       and notificationConfig =
         flag "notification-config" (optional json_arg)
           ~doc:"JSON NotificationConfig"
       and cloudWatchOutputConfig =
         flag "cloud-watch-output-config" (optional json_arg)
           ~doc:"JSON CloudWatchOutputConfig"
       and documentName =
         flag "document-name" (required string) ~doc:"STRING DocumentARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_command
           (Values.SendCommandRequest.make
              ?instanceIds:(Option.map ~f:Values.InstanceIdList.of_json
                              instanceIds)
              ?targets:(Option.map ~f:Values.Targets.of_json targets)
              ?documentVersion ?documentHash
              ?documentHashType:(Option.map
                                   ~f:Values.DocumentHashType.of_json
                                   documentHashType) ?timeoutSeconds ?comment
              ?parameters:(Option.map ~f:Values.Parameters.of_json parameters)
              ?outputS3Region ?outputS3BucketName ?outputS3KeyPrefix
              ?maxConcurrency ?maxErrors ?serviceRoleArn
              ?notificationConfig:(Option.map
                                     ~f:Values.NotificationConfig.of_json
                                     notificationConfig)
              ?cloudWatchOutputConfig:(Option.map
                                         ~f:Values.CloudWatchOutputConfig.of_json
                                         cloudWatchOutputConfig)
              ~documentName ()) (Some Values.SendCommandResult.to_json)
           (Some Values.SendCommandResult.error_to_json)])
let start_associations_once =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and associationIds =
         flag "association-ids" (required json_arg)
           ~doc:"JSON AssociationIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_associations_once
           (Values.StartAssociationsOnceRequest.make
              ~associationIds:(Values.AssociationIdList.of_json
                                 associationIds) ())
           (Some Values.StartAssociationsOnceResult.to_json)
           (Some Values.StartAssociationsOnceResult.error_to_json)])
let start_automation_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and documentVersion =
         flag "document-version" (optional string)
           ~doc:"STRING DocumentVersion"
       and parameters =
         flag "parameters" (optional json_arg)
           ~doc:"JSON AutomationParameterMap"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING IdempotencyToken"
       and mode = flag "mode" (optional json_arg) ~doc:"JSON ExecutionMode"
       and targetParameterName =
         flag "target-parameter-name" (optional string)
           ~doc:"STRING AutomationParameterKey"
       and targets = flag "targets" (optional json_arg) ~doc:"JSON Targets"
       and targetMaps =
         flag "target-maps" (optional json_arg) ~doc:"JSON TargetMaps"
       and maxConcurrency =
         flag "max-concurrency" (optional string)
           ~doc:"STRING MaxConcurrency"
       and maxErrors =
         flag "max-errors" (optional string) ~doc:"STRING MaxErrors"
       and targetLocations =
         flag "target-locations" (optional json_arg)
           ~doc:"JSON TargetLocations"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and documentName =
         flag "document-name" (required string) ~doc:"STRING DocumentARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_automation_execution
           (Values.StartAutomationExecutionRequest.make ?documentVersion
              ?parameters:(Option.map
                             ~f:Values.AutomationParameterMap.of_json
                             parameters) ?clientToken
              ?mode:(Option.map ~f:Values.ExecutionMode.of_json mode)
              ?targetParameterName
              ?targets:(Option.map ~f:Values.Targets.of_json targets)
              ?targetMaps:(Option.map ~f:Values.TargetMaps.of_json targetMaps)
              ?maxConcurrency ?maxErrors
              ?targetLocations:(Option.map ~f:Values.TargetLocations.of_json
                                  targetLocations)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~documentName
              ()) (Some Values.StartAutomationExecutionResult.to_json)
           (Some Values.StartAutomationExecutionResult.error_to_json)])
let start_change_request_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scheduledTime =
         flag "scheduled-time" (optional json_arg) ~doc:"JSON DateTime"
       and documentVersion =
         flag "document-version" (optional string)
           ~doc:"STRING DocumentVersion"
       and parameters =
         flag "parameters" (optional json_arg)
           ~doc:"JSON AutomationParameterMap"
       and changeRequestName =
         flag "change-request-name" (optional string)
           ~doc:"STRING ChangeRequestName"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING IdempotencyToken"
       and autoApprove =
         flag "auto-approve" (optional bool) ~doc:"BOOL Boolean"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and scheduledEndTime =
         flag "scheduled-end-time" (optional json_arg) ~doc:"JSON DateTime"
       and changeDetails =
         flag "change-details" (optional string)
           ~doc:"STRING ChangeDetailsValue"
       and documentName =
         flag "document-name" (required string) ~doc:"STRING DocumentARN"
       and runbooks =
         flag "runbooks" (required json_arg) ~doc:"JSON Runbooks" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_change_request_execution
           (Values.StartChangeRequestExecutionRequest.make
              ?scheduledTime:(Option.map ~f:Values.DateTime.of_json
                                scheduledTime) ?documentVersion
              ?parameters:(Option.map
                             ~f:Values.AutomationParameterMap.of_json
                             parameters) ?changeRequestName ?clientToken
              ?autoApprove ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?scheduledEndTime:(Option.map ~f:Values.DateTime.of_json
                                   scheduledEndTime) ?changeDetails
              ~documentName ~runbooks:(Values.Runbooks.of_json runbooks) ())
           (Some Values.StartChangeRequestExecutionResult.to_json)
           (Some Values.StartChangeRequestExecutionResult.error_to_json)])
let start_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and documentName =
         flag "document-name" (optional string) ~doc:"STRING DocumentARN"
       and reason =
         flag "reason" (optional string) ~doc:"STRING SessionReason"
       and parameters =
         flag "parameters" (optional json_arg)
           ~doc:"JSON SessionManagerParameters"
       and target =
         flag "target" (required string) ~doc:"STRING SessionTarget" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_session
           (Values.StartSessionRequest.make ?documentName ?reason
              ?parameters:(Option.map
                             ~f:Values.SessionManagerParameters.of_json
                             parameters) ~target ())
           (Some Values.StartSessionResponse.to_json)
           (Some Values.StartSessionResponse.error_to_json)])
let stop_automation_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON StopType"
       and automationExecutionId =
         flag "automation-execution-id" (required string)
           ~doc:"STRING AutomationExecutionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_automation_execution
           (Values.StopAutomationExecutionRequest.make
              ?type_:(Option.map ~f:Values.StopType.of_json type_)
              ~automationExecutionId ())
           (Some Values.StopAutomationExecutionResult.to_json)
           (Some Values.StopAutomationExecutionResult.error_to_json)])
let terminate_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING SessionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.terminate_session
           (Values.TerminateSessionRequest.make ~sessionId ())
           (Some Values.TerminateSessionResponse.to_json)
           (Some Values.TerminateSessionResponse.error_to_json)])
let unlabel_parameter_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING PSParameterName"
       and parameterVersion =
         flag "parameter-version" (required json_arg)
           ~doc:"JSON PSParameterVersion"
       and labels =
         flag "labels" (required json_arg) ~doc:"JSON ParameterLabelList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.unlabel_parameter_version
           (Values.UnlabelParameterVersionRequest.make ~name
              ~parameterVersion:(Values.PSParameterVersion.of_json
                                   parameterVersion)
              ~labels:(Values.ParameterLabelList.of_json labels) ())
           (Some Values.UnlabelParameterVersionResult.to_json)
           (Some Values.UnlabelParameterVersionResult.error_to_json)])
let update_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON Parameters"
       and documentVersion =
         flag "document-version" (optional string)
           ~doc:"STRING DocumentVersion"
       and scheduleExpression =
         flag "schedule-expression" (optional string)
           ~doc:"STRING ScheduleExpression"
       and outputLocation =
         flag "output-location" (optional json_arg)
           ~doc:"JSON InstanceAssociationOutputLocation"
       and name = flag "name" (optional string) ~doc:"STRING DocumentARN"
       and targets = flag "targets" (optional json_arg) ~doc:"JSON Targets"
       and associationName =
         flag "association-name" (optional string)
           ~doc:"STRING AssociationName"
       and associationVersion =
         flag "association-version" (optional string)
           ~doc:"STRING AssociationVersion"
       and automationTargetParameterName =
         flag "automation-target-parameter-name" (optional string)
           ~doc:"STRING AutomationTargetParameterName"
       and maxErrors =
         flag "max-errors" (optional string) ~doc:"STRING MaxErrors"
       and maxConcurrency =
         flag "max-concurrency" (optional string)
           ~doc:"STRING MaxConcurrency"
       and complianceSeverity =
         flag "compliance-severity" (optional json_arg)
           ~doc:"JSON AssociationComplianceSeverity"
       and syncCompliance =
         flag "sync-compliance" (optional json_arg)
           ~doc:"JSON AssociationSyncCompliance"
       and applyOnlyAtCronInterval =
         flag "apply-only-at-cron-interval" (optional bool)
           ~doc:"BOOL ApplyOnlyAtCronInterval"
       and calendarNames =
         flag "calendar-names" (optional json_arg)
           ~doc:"JSON CalendarNameOrARNList"
       and targetLocations =
         flag "target-locations" (optional json_arg)
           ~doc:"JSON TargetLocations"
       and associationId =
         flag "association-id" (required string) ~doc:"STRING AssociationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_association
           (Values.UpdateAssociationRequest.make
              ?parameters:(Option.map ~f:Values.Parameters.of_json parameters)
              ?documentVersion ?scheduleExpression
              ?outputLocation:(Option.map
                                 ~f:Values.InstanceAssociationOutputLocation.of_json
                                 outputLocation) ?name
              ?targets:(Option.map ~f:Values.Targets.of_json targets)
              ?associationName ?associationVersion
              ?automationTargetParameterName ?maxErrors ?maxConcurrency
              ?complianceSeverity:(Option.map
                                     ~f:Values.AssociationComplianceSeverity.of_json
                                     complianceSeverity)
              ?syncCompliance:(Option.map
                                 ~f:Values.AssociationSyncCompliance.of_json
                                 syncCompliance) ?applyOnlyAtCronInterval
              ?calendarNames:(Option.map
                                ~f:Values.CalendarNameOrARNList.of_json
                                calendarNames)
              ?targetLocations:(Option.map ~f:Values.TargetLocations.of_json
                                  targetLocations) ~associationId ())
           (Some Values.UpdateAssociationResult.to_json)
           (Some Values.UpdateAssociationResult.error_to_json)])
let update_association_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING DocumentARN"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and associationStatus =
         flag "association-status" (required json_arg)
           ~doc:"JSON AssociationStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_association_status
           (Values.UpdateAssociationStatusRequest.make ~name ~instanceId
              ~associationStatus:(Values.AssociationStatus.of_json
                                    associationStatus) ())
           (Some Values.UpdateAssociationStatusResult.to_json)
           (Some Values.UpdateAssociationStatusResult.error_to_json)])
let update_document =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attachments =
         flag "attachments" (optional json_arg)
           ~doc:"JSON AttachmentsSourceList"
       and displayName =
         flag "display-name" (optional string)
           ~doc:"STRING DocumentDisplayName"
       and versionName =
         flag "version-name" (optional string)
           ~doc:"STRING DocumentVersionName"
       and documentVersion =
         flag "document-version" (optional string)
           ~doc:"STRING DocumentVersion"
       and documentFormat =
         flag "document-format" (optional json_arg)
           ~doc:"JSON DocumentFormat"
       and targetType =
         flag "target-type" (optional string) ~doc:"STRING TargetType"
       and content =
         flag "content" (required string) ~doc:"STRING DocumentContent"
       and name = flag "name" (required string) ~doc:"STRING DocumentName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_document
           (Values.UpdateDocumentRequest.make
              ?attachments:(Option.map
                              ~f:Values.AttachmentsSourceList.of_json
                              attachments) ?displayName ?versionName
              ?documentVersion
              ?documentFormat:(Option.map ~f:Values.DocumentFormat.of_json
                                 documentFormat) ?targetType ~content ~name
              ()) (Some Values.UpdateDocumentResult.to_json)
           (Some Values.UpdateDocumentResult.error_to_json)])
let update_document_default_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING DocumentName"
       and documentVersion =
         flag "document-version" (required string)
           ~doc:"STRING DocumentVersionNumber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_document_default_version
           (Values.UpdateDocumentDefaultVersionRequest.make ~name
              ~documentVersion ())
           (Some Values.UpdateDocumentDefaultVersionResult.to_json)
           (Some Values.UpdateDocumentDefaultVersionResult.error_to_json)])
let update_document_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and documentVersion =
         flag "document-version" (optional string)
           ~doc:"STRING DocumentVersion"
       and name = flag "name" (required string) ~doc:"STRING DocumentName"
       and documentReviews =
         flag "document-reviews" (required json_arg)
           ~doc:"JSON DocumentReviews" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_document_metadata
           (Values.UpdateDocumentMetadataRequest.make ?documentVersion ~name
              ~documentReviews:(Values.DocumentReviews.of_json
                                  documentReviews) ())
           (Some Values.UpdateDocumentMetadataResponse.to_json)
           (Some Values.UpdateDocumentMetadataResponse.error_to_json)])
let update_maintenance_window =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING MaintenanceWindowName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING MaintenanceWindowDescription"
       and startDate =
         flag "start-date" (optional string)
           ~doc:"STRING MaintenanceWindowStringDateTime"
       and endDate =
         flag "end-date" (optional string)
           ~doc:"STRING MaintenanceWindowStringDateTime"
       and schedule =
         flag "schedule" (optional string)
           ~doc:"STRING MaintenanceWindowSchedule"
       and scheduleTimezone =
         flag "schedule-timezone" (optional string)
           ~doc:"STRING MaintenanceWindowTimezone"
       and scheduleOffset =
         flag "schedule-offset" (optional int)
           ~doc:"INT MaintenanceWindowOffset"
       and duration =
         flag "duration" (optional int)
           ~doc:"INT MaintenanceWindowDurationHours"
       and cutoff =
         flag "cutoff" (optional int) ~doc:"INT MaintenanceWindowCutoff"
       and allowUnassociatedTargets =
         flag "allow-unassociated-targets" (optional bool)
           ~doc:"BOOL MaintenanceWindowAllowUnassociatedTargets"
       and enabled =
         flag "enabled" (optional bool) ~doc:"BOOL MaintenanceWindowEnabled"
       and replace = flag "replace" (optional bool) ~doc:"BOOL Boolean"
       and windowId =
         flag "window-id" (required string) ~doc:"STRING MaintenanceWindowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_maintenance_window
           (Values.UpdateMaintenanceWindowRequest.make ?name ?description
              ?startDate ?endDate ?schedule ?scheduleTimezone ?scheduleOffset
              ?duration ?cutoff ?allowUnassociatedTargets ?enabled ?replace
              ~windowId ())
           (Some Values.UpdateMaintenanceWindowResult.to_json)
           (Some Values.UpdateMaintenanceWindowResult.error_to_json)])
let update_maintenance_window_target =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targets = flag "targets" (optional json_arg) ~doc:"JSON Targets"
       and ownerInformation =
         flag "owner-information" (optional string)
           ~doc:"STRING OwnerInformation"
       and name =
         flag "name" (optional string) ~doc:"STRING MaintenanceWindowName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING MaintenanceWindowDescription"
       and replace = flag "replace" (optional bool) ~doc:"BOOL Boolean"
       and windowId =
         flag "window-id" (required string) ~doc:"STRING MaintenanceWindowId"
       and windowTargetId =
         flag "window-target-id" (required string)
           ~doc:"STRING MaintenanceWindowTargetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_maintenance_window_target
           (Values.UpdateMaintenanceWindowTargetRequest.make
              ?targets:(Option.map ~f:Values.Targets.of_json targets)
              ?ownerInformation ?name ?description ?replace ~windowId
              ~windowTargetId ())
           (Some Values.UpdateMaintenanceWindowTargetResult.to_json)
           (Some Values.UpdateMaintenanceWindowTargetResult.error_to_json)])
let update_maintenance_window_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targets = flag "targets" (optional json_arg) ~doc:"JSON Targets"
       and taskArn =
         flag "task-arn" (optional string)
           ~doc:"STRING MaintenanceWindowTaskArn"
       and serviceRoleArn =
         flag "service-role-arn" (optional string) ~doc:"STRING ServiceRole"
       and taskParameters =
         flag "task-parameters" (optional json_arg)
           ~doc:"JSON MaintenanceWindowTaskParameters"
       and taskInvocationParameters =
         flag "task-invocation-parameters" (optional json_arg)
           ~doc:"JSON MaintenanceWindowTaskInvocationParameters"
       and priority =
         flag "priority" (optional int)
           ~doc:"INT MaintenanceWindowTaskPriority"
       and maxConcurrency =
         flag "max-concurrency" (optional string)
           ~doc:"STRING MaxConcurrency"
       and maxErrors =
         flag "max-errors" (optional string) ~doc:"STRING MaxErrors"
       and loggingInfo =
         flag "logging-info" (optional json_arg) ~doc:"JSON LoggingInfo"
       and name =
         flag "name" (optional string) ~doc:"STRING MaintenanceWindowName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING MaintenanceWindowDescription"
       and replace = flag "replace" (optional bool) ~doc:"BOOL Boolean"
       and cutoffBehavior =
         flag "cutoff-behavior" (optional json_arg)
           ~doc:"JSON MaintenanceWindowTaskCutoffBehavior"
       and windowId =
         flag "window-id" (required string) ~doc:"STRING MaintenanceWindowId"
       and windowTaskId =
         flag "window-task-id" (required string)
           ~doc:"STRING MaintenanceWindowTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_maintenance_window_task
           (Values.UpdateMaintenanceWindowTaskRequest.make
              ?targets:(Option.map ~f:Values.Targets.of_json targets)
              ?taskArn ?serviceRoleArn
              ?taskParameters:(Option.map
                                 ~f:Values.MaintenanceWindowTaskParameters.of_json
                                 taskParameters)
              ?taskInvocationParameters:(Option.map
                                           ~f:Values.MaintenanceWindowTaskInvocationParameters.of_json
                                           taskInvocationParameters)
              ?priority ?maxConcurrency ?maxErrors
              ?loggingInfo:(Option.map ~f:Values.LoggingInfo.of_json
                              loggingInfo) ?name ?description ?replace
              ?cutoffBehavior:(Option.map
                                 ~f:Values.MaintenanceWindowTaskCutoffBehavior.of_json
                                 cutoffBehavior) ~windowId ~windowTaskId ())
           (Some Values.UpdateMaintenanceWindowTaskResult.to_json)
           (Some Values.UpdateMaintenanceWindowTaskResult.error_to_json)])
let update_managed_instance_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING ManagedInstanceId"
       and iamRole = flag "iam-role" (required string) ~doc:"STRING IamRole" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_managed_instance_role
           (Values.UpdateManagedInstanceRoleRequest.make ~instanceId ~iamRole
              ()) (Some Values.UpdateManagedInstanceRoleResult.to_json)
           (Some Values.UpdateManagedInstanceRoleResult.error_to_json)])
let update_ops_item =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING OpsItemDescription"
       and operationalData =
         flag "operational-data" (optional json_arg)
           ~doc:"JSON OpsItemOperationalData"
       and operationalDataToDelete =
         flag "operational-data-to-delete" (optional json_arg)
           ~doc:"JSON OpsItemOpsDataKeysList"
       and notifications =
         flag "notifications" (optional json_arg)
           ~doc:"JSON OpsItemNotifications"
       and priority =
         flag "priority" (optional int) ~doc:"INT OpsItemPriority"
       and relatedOpsItems =
         flag "related-ops-items" (optional json_arg)
           ~doc:"JSON RelatedOpsItems"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON OpsItemStatus"
       and title = flag "title" (optional string) ~doc:"STRING OpsItemTitle"
       and category =
         flag "category" (optional string) ~doc:"STRING OpsItemCategory"
       and severity =
         flag "severity" (optional string) ~doc:"STRING OpsItemSeverity"
       and actualStartTime =
         flag "actual-start-time" (optional json_arg) ~doc:"JSON DateTime"
       and actualEndTime =
         flag "actual-end-time" (optional json_arg) ~doc:"JSON DateTime"
       and plannedStartTime =
         flag "planned-start-time" (optional json_arg) ~doc:"JSON DateTime"
       and plannedEndTime =
         flag "planned-end-time" (optional json_arg) ~doc:"JSON DateTime"
       and opsItemId =
         flag "ops-item-id" (required string) ~doc:"STRING OpsItemId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_ops_item
           (Values.UpdateOpsItemRequest.make ?description
              ?operationalData:(Option.map
                                  ~f:Values.OpsItemOperationalData.of_json
                                  operationalData)
              ?operationalDataToDelete:(Option.map
                                          ~f:Values.OpsItemOpsDataKeysList.of_json
                                          operationalDataToDelete)
              ?notifications:(Option.map
                                ~f:Values.OpsItemNotifications.of_json
                                notifications) ?priority
              ?relatedOpsItems:(Option.map ~f:Values.RelatedOpsItems.of_json
                                  relatedOpsItems)
              ?status:(Option.map ~f:Values.OpsItemStatus.of_json status)
              ?title ?category ?severity
              ?actualStartTime:(Option.map ~f:Values.DateTime.of_json
                                  actualStartTime)
              ?actualEndTime:(Option.map ~f:Values.DateTime.of_json
                                actualEndTime)
              ?plannedStartTime:(Option.map ~f:Values.DateTime.of_json
                                   plannedStartTime)
              ?plannedEndTime:(Option.map ~f:Values.DateTime.of_json
                                 plannedEndTime) ~opsItemId ())
           (Some Values.UpdateOpsItemResponse.to_json)
           (Some Values.UpdateOpsItemResponse.error_to_json)])
let update_ops_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metadataToUpdate =
         flag "metadata-to-update" (optional json_arg)
           ~doc:"JSON MetadataMap"
       and keysToDelete =
         flag "keys-to-delete" (optional json_arg)
           ~doc:"JSON MetadataKeysToDeleteList"
       and opsMetadataArn =
         flag "ops-metadata-arn" (required string)
           ~doc:"STRING OpsMetadataArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_ops_metadata
           (Values.UpdateOpsMetadataRequest.make
              ?metadataToUpdate:(Option.map ~f:Values.MetadataMap.of_json
                                   metadataToUpdate)
              ?keysToDelete:(Option.map
                               ~f:Values.MetadataKeysToDeleteList.of_json
                               keysToDelete) ~opsMetadataArn ())
           (Some Values.UpdateOpsMetadataResult.to_json)
           (Some Values.UpdateOpsMetadataResult.error_to_json)])
let update_patch_baseline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING BaselineName"
       and globalFilters =
         flag "global-filters" (optional json_arg)
           ~doc:"JSON PatchFilterGroup"
       and approvalRules =
         flag "approval-rules" (optional json_arg) ~doc:"JSON PatchRuleGroup"
       and approvedPatches =
         flag "approved-patches" (optional json_arg) ~doc:"JSON PatchIdList"
       and approvedPatchesComplianceLevel =
         flag "approved-patches-compliance-level" (optional json_arg)
           ~doc:"JSON PatchComplianceLevel"
       and approvedPatchesEnableNonSecurity =
         flag "approved-patches-enable-non-security" (optional bool)
           ~doc:"BOOL Boolean"
       and rejectedPatches =
         flag "rejected-patches" (optional json_arg) ~doc:"JSON PatchIdList"
       and rejectedPatchesAction =
         flag "rejected-patches-action" (optional json_arg)
           ~doc:"JSON PatchAction"
       and description =
         flag "description" (optional string)
           ~doc:"STRING BaselineDescription"
       and sources =
         flag "sources" (optional json_arg) ~doc:"JSON PatchSourceList"
       and replace = flag "replace" (optional bool) ~doc:"BOOL Boolean"
       and baselineId =
         flag "baseline-id" (required string) ~doc:"STRING BaselineId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_patch_baseline
           (Values.UpdatePatchBaselineRequest.make ?name
              ?globalFilters:(Option.map ~f:Values.PatchFilterGroup.of_json
                                globalFilters)
              ?approvalRules:(Option.map ~f:Values.PatchRuleGroup.of_json
                                approvalRules)
              ?approvedPatches:(Option.map ~f:Values.PatchIdList.of_json
                                  approvedPatches)
              ?approvedPatchesComplianceLevel:(Option.map
                                                 ~f:Values.PatchComplianceLevel.of_json
                                                 approvedPatchesComplianceLevel)
              ?approvedPatchesEnableNonSecurity
              ?rejectedPatches:(Option.map ~f:Values.PatchIdList.of_json
                                  rejectedPatches)
              ?rejectedPatchesAction:(Option.map
                                        ~f:Values.PatchAction.of_json
                                        rejectedPatchesAction) ?description
              ?sources:(Option.map ~f:Values.PatchSourceList.of_json sources)
              ?replace ~baselineId ())
           (Some Values.UpdatePatchBaselineResult.to_json)
           (Some Values.UpdatePatchBaselineResult.error_to_json)])
let update_resource_data_sync =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and syncName =
         flag "sync-name" (required string)
           ~doc:"STRING ResourceDataSyncName"
       and syncType =
         flag "sync-type" (required string)
           ~doc:"STRING ResourceDataSyncType"
       and syncSource =
         flag "sync-source" (required json_arg)
           ~doc:"JSON ResourceDataSyncSource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_resource_data_sync
           (Values.UpdateResourceDataSyncRequest.make ~syncName ~syncType
              ~syncSource:(Values.ResourceDataSyncSource.of_json syncSource)
              ()) (Some Values.UpdateResourceDataSyncResult.to_json)
           (Some Values.UpdateResourceDataSyncResult.error_to_json)])
let update_service_setting =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and settingId =
         flag "setting-id" (required string) ~doc:"STRING ServiceSettingId"
       and settingValue =
         flag "setting-value" (required string)
           ~doc:"STRING ServiceSettingValue" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_service_setting
           (Values.UpdateServiceSettingRequest.make ~settingId ~settingValue
              ()) (Some Values.UpdateServiceSettingResult.to_json)
           (Some Values.UpdateServiceSettingResult.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("add-tags-to-resource", add_tags_to_resource);
    ("associate-ops-item-related-item", associate_ops_item_related_item);
    ("cancel-command", cancel_command);
    ("cancel-maintenance-window-execution",
      cancel_maintenance_window_execution);
    ("create-activation", create_activation);
    ("create-association", create_association);
    ("create-association-batch", create_association_batch);
    ("create-document", create_document);
    ("create-maintenance-window", create_maintenance_window);
    ("create-ops-item", create_ops_item);
    ("create-ops-metadata", create_ops_metadata);
    ("create-patch-baseline", create_patch_baseline);
    ("create-resource-data-sync", create_resource_data_sync);
    ("delete-activation", delete_activation);
    ("delete-association", delete_association);
    ("delete-document", delete_document);
    ("delete-inventory", delete_inventory);
    ("delete-maintenance-window", delete_maintenance_window);
    ("delete-ops-metadata", delete_ops_metadata);
    ("delete-parameter", delete_parameter);
    ("delete-parameters", delete_parameters);
    ("delete-patch-baseline", delete_patch_baseline);
    ("delete-resource-data-sync", delete_resource_data_sync);
    ("deregister-managed-instance", deregister_managed_instance);
    ("deregister-patch-baseline-for-patch-group",
      deregister_patch_baseline_for_patch_group);
    ("deregister-target-from-maintenance-window",
      deregister_target_from_maintenance_window);
    ("deregister-task-from-maintenance-window",
      deregister_task_from_maintenance_window);
    ("describe-activations", describe_activations);
    ("describe-association", describe_association);
    ("describe-association-execution-targets",
      describe_association_execution_targets);
    ("describe-association-executions", describe_association_executions);
    ("describe-automation-executions", describe_automation_executions);
    ("describe-automation-step-executions",
      describe_automation_step_executions);
    ("describe-available-patches", describe_available_patches);
    ("describe-document", describe_document);
    ("describe-document-permission", describe_document_permission);
    ("describe-effective-instance-associations",
      describe_effective_instance_associations);
    ("describe-effective-patches-for-patch-baseline",
      describe_effective_patches_for_patch_baseline);
    ("describe-instance-associations-status",
      describe_instance_associations_status);
    ("describe-instance-information", describe_instance_information);
    ("describe-instance-patch-states", describe_instance_patch_states);
    ("describe-instance-patch-states-for-patch-group",
      describe_instance_patch_states_for_patch_group);
    ("describe-instance-patches", describe_instance_patches);
    ("describe-inventory-deletions", describe_inventory_deletions);
    ("describe-maintenance-window-execution-task-invocations",
      describe_maintenance_window_execution_task_invocations);
    ("describe-maintenance-window-execution-tasks",
      describe_maintenance_window_execution_tasks);
    ("describe-maintenance-window-executions",
      describe_maintenance_window_executions);
    ("describe-maintenance-window-schedule",
      describe_maintenance_window_schedule);
    ("describe-maintenance-window-targets",
      describe_maintenance_window_targets);
    ("describe-maintenance-window-tasks", describe_maintenance_window_tasks);
    ("describe-maintenance-windows", describe_maintenance_windows);
    ("describe-maintenance-windows-for-target",
      describe_maintenance_windows_for_target);
    ("describe-ops-items", describe_ops_items);
    ("describe-parameters", describe_parameters);
    ("describe-patch-baselines", describe_patch_baselines);
    ("describe-patch-group-state", describe_patch_group_state);
    ("describe-patch-groups", describe_patch_groups);
    ("describe-patch-properties", describe_patch_properties);
    ("describe-sessions", describe_sessions);
    ("disassociate-ops-item-related-item",
      disassociate_ops_item_related_item);
    ("get-automation-execution", get_automation_execution);
    ("get-calendar-state", get_calendar_state);
    ("get-command-invocation", get_command_invocation);
    ("get-connection-status", get_connection_status);
    ("get-default-patch-baseline", get_default_patch_baseline);
    ("get-deployable-patch-snapshot-for-instance",
      get_deployable_patch_snapshot_for_instance);
    ("get-document", get_document);
    ("get-inventory", get_inventory);
    ("get-inventory-schema", get_inventory_schema);
    ("get-maintenance-window", get_maintenance_window);
    ("get-maintenance-window-execution", get_maintenance_window_execution);
    ("get-maintenance-window-execution-task",
      get_maintenance_window_execution_task);
    ("get-maintenance-window-execution-task-invocation",
      get_maintenance_window_execution_task_invocation);
    ("get-maintenance-window-task", get_maintenance_window_task);
    ("get-ops-item", get_ops_item);
    ("get-ops-metadata", get_ops_metadata);
    ("get-ops-summary", get_ops_summary);
    ("get-parameter", get_parameter);
    ("get-parameter-history", get_parameter_history);
    ("get-parameters", get_parameters);
    ("get-parameters-by-path", get_parameters_by_path);
    ("get-patch-baseline", get_patch_baseline);
    ("get-patch-baseline-for-patch-group",
      get_patch_baseline_for_patch_group);
    ("get-service-setting", get_service_setting);
    ("label-parameter-version", label_parameter_version);
    ("list-association-versions", list_association_versions);
    ("list-associations", list_associations);
    ("list-command-invocations", list_command_invocations);
    ("list-commands", list_commands);
    ("list-compliance-items", list_compliance_items);
    ("list-compliance-summaries", list_compliance_summaries);
    ("list-document-metadata-history", list_document_metadata_history);
    ("list-document-versions", list_document_versions);
    ("list-documents", list_documents);
    ("list-inventory-entries", list_inventory_entries);
    ("list-ops-item-events", list_ops_item_events);
    ("list-ops-item-related-items", list_ops_item_related_items);
    ("list-ops-metadata", list_ops_metadata);
    ("list-resource-compliance-summaries",
      list_resource_compliance_summaries);
    ("list-resource-data-sync", list_resource_data_sync);
    ("list-tags-for-resource", list_tags_for_resource);
    ("modify-document-permission", modify_document_permission);
    ("put-compliance-items", put_compliance_items);
    ("put-inventory", put_inventory);
    ("put-parameter", put_parameter);
    ("register-default-patch-baseline", register_default_patch_baseline);
    ("register-patch-baseline-for-patch-group",
      register_patch_baseline_for_patch_group);
    ("register-target-with-maintenance-window",
      register_target_with_maintenance_window);
    ("register-task-with-maintenance-window",
      register_task_with_maintenance_window);
    ("remove-tags-from-resource", remove_tags_from_resource);
    ("reset-service-setting", reset_service_setting);
    ("resume-session", resume_session);
    ("send-automation-signal", send_automation_signal);
    ("send-command", send_command);
    ("start-associations-once", start_associations_once);
    ("start-automation-execution", start_automation_execution);
    ("start-change-request-execution", start_change_request_execution);
    ("start-session", start_session);
    ("stop-automation-execution", stop_automation_execution);
    ("terminate-session", terminate_session);
    ("unlabel-parameter-version", unlabel_parameter_version);
    ("update-association", update_association);
    ("update-association-status", update_association_status);
    ("update-document", update_document);
    ("update-document-default-version", update_document_default_version);
    ("update-document-metadata", update_document_metadata);
    ("update-maintenance-window", update_maintenance_window);
    ("update-maintenance-window-target", update_maintenance_window_target);
    ("update-maintenance-window-task", update_maintenance_window_task);
    ("update-managed-instance-role", update_managed_instance_role);
    ("update-ops-item", update_ops_item);
    ("update-ops-metadata", update_ops_metadata);
    ("update-patch-baseline", update_patch_baseline);
    ("update-resource-data-sync", update_resource_data_sync);
    ("update-service-setting", update_service_setting)]
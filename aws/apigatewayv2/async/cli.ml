(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_api =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiKeySelectionExpression =
         flag "api-key-selection-expression" (optional string)
           ~doc:"STRING SelectionExpression"
       and corsConfiguration =
         flag "cors-configuration" (optional json_arg) ~doc:"JSON Cors"
       and credentialsArn =
         flag "credentials-arn" (optional string) ~doc:"STRING Arn"
       and description =
         flag "description" (optional string)
           ~doc:"STRING StringWithLengthBetween0And1024"
       and disableSchemaValidation =
         flag "disable-schema-validation" (optional bool)
           ~doc:"BOOL __boolean"
       and disableExecuteApiEndpoint =
         flag "disable-execute-api-endpoint" (optional bool)
           ~doc:"BOOL __boolean"
       and ipAddressType =
         flag "ip-address-type" (optional json_arg) ~doc:"JSON IpAddressType"
       and routeKey =
         flag "route-key" (optional string) ~doc:"STRING SelectionKey"
       and routeSelectionExpression =
         flag "route-selection-expression" (optional string)
           ~doc:"STRING SelectionExpression"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and target =
         flag "target" (optional string)
           ~doc:"STRING UriWithLengthBetween1And2048"
       and version =
         flag "version" (optional string)
           ~doc:"STRING StringWithLengthBetween1And64"
       and name =
         flag "name" (required string)
           ~doc:"STRING StringWithLengthBetween1And128"
       and protocolType =
         flag "protocol-type" (required json_arg) ~doc:"JSON ProtocolType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_api
           (Values.CreateApiRequest.make ?apiKeySelectionExpression
              ?corsConfiguration:(Option.map ~f:Values.Cors.of_json
                                    corsConfiguration) ?credentialsArn
              ?description ?disableSchemaValidation
              ?disableExecuteApiEndpoint
              ?ipAddressType:(Option.map ~f:Values.IpAddressType.of_json
                                ipAddressType) ?routeKey
              ?routeSelectionExpression
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ?target ?version
              ~name ~protocolType:(Values.ProtocolType.of_json protocolType)
              ()) (Some Values.CreateApiResponse.to_json)
           (Some Values.CreateApiResponse.error_to_json)])
let create_api_mapping =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiMappingKey =
         flag "api-mapping-key" (optional string) ~doc:"STRING SelectionKey"
       and apiId = flag "api-id" (required string) ~doc:"STRING Id"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING __string"
       and stage =
         flag "stage" (required string)
           ~doc:"STRING StringWithLengthBetween1And128" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_api_mapping
           (Values.CreateApiMappingRequest.make ?apiMappingKey ~apiId
              ~domainName ~stage ())
           (Some Values.CreateApiMappingResponse.to_json)
           (Some Values.CreateApiMappingResponse.error_to_json)])
let create_authorizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authorizerCredentialsArn =
         flag "authorizer-credentials-arn" (optional string)
           ~doc:"STRING Arn"
       and authorizerPayloadFormatVersion =
         flag "authorizer-payload-format-version" (optional string)
           ~doc:"STRING StringWithLengthBetween1And64"
       and authorizerResultTtlInSeconds =
         flag "authorizer-result-ttl-in-seconds" (optional int)
           ~doc:"INT IntegerWithLengthBetween0And3600"
       and authorizerUri =
         flag "authorizer-uri" (optional string)
           ~doc:"STRING UriWithLengthBetween1And2048"
       and enableSimpleResponses =
         flag "enable-simple-responses" (optional bool) ~doc:"BOOL __boolean"
       and identityValidationExpression =
         flag "identity-validation-expression" (optional string)
           ~doc:"STRING StringWithLengthBetween0And1024"
       and jwtConfiguration =
         flag "jwt-configuration" (optional json_arg)
           ~doc:"JSON JWTConfiguration"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and authorizerType =
         flag "authorizer-type" (required json_arg)
           ~doc:"JSON AuthorizerType"
       and identitySource =
         flag "identity-source" (required json_arg)
           ~doc:"JSON IdentitySourceList"
       and name =
         flag "name" (required string)
           ~doc:"STRING StringWithLengthBetween1And128" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_authorizer
           (Values.CreateAuthorizerRequest.make ?authorizerCredentialsArn
              ?authorizerPayloadFormatVersion ?authorizerResultTtlInSeconds
              ?authorizerUri ?enableSimpleResponses
              ?identityValidationExpression
              ?jwtConfiguration:(Option.map
                                   ~f:Values.JWTConfiguration.of_json
                                   jwtConfiguration) ~apiId
              ~authorizerType:(Values.AuthorizerType.of_json authorizerType)
              ~identitySource:(Values.IdentitySourceList.of_json
                                 identitySource) ~name ())
           (Some Values.CreateAuthorizerResponse.to_json)
           (Some Values.CreateAuthorizerResponse.error_to_json)])
let create_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING StringWithLengthBetween0And1024"
       and stageName =
         flag "stage-name" (optional string)
           ~doc:"STRING StringWithLengthBetween1And128"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_deployment
           (Values.CreateDeploymentRequest.make ?description ?stageName
              ~apiId ()) (Some Values.CreateDeploymentResponse.to_json)
           (Some Values.CreateDeploymentResponse.error_to_json)])
let create_domain_name =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainNameConfigurations =
         flag "domain-name-configurations" (optional json_arg)
           ~doc:"JSON DomainNameConfigurations"
       and mutualTlsAuthentication =
         flag "mutual-tls-authentication" (optional json_arg)
           ~doc:"JSON MutualTlsAuthenticationInput"
       and routingMode =
         flag "routing-mode" (optional json_arg) ~doc:"JSON RoutingMode"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and domainName =
         flag "domain-name" (required string)
           ~doc:"STRING StringWithLengthBetween1And512" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_domain_name
           (Values.CreateDomainNameRequest.make
              ?domainNameConfigurations:(Option.map
                                           ~f:Values.DomainNameConfigurations.of_json
                                           domainNameConfigurations)
              ?mutualTlsAuthentication:(Option.map
                                          ~f:Values.MutualTlsAuthenticationInput.of_json
                                          mutualTlsAuthentication)
              ?routingMode:(Option.map ~f:Values.RoutingMode.of_json
                              routingMode)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~domainName ())
           (Some Values.CreateDomainNameResponse.to_json)
           (Some Values.CreateDomainNameResponse.error_to_json)])
let create_integration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectionId =
         flag "connection-id" (optional string)
           ~doc:"STRING StringWithLengthBetween1And1024"
       and connectionType =
         flag "connection-type" (optional json_arg)
           ~doc:"JSON ConnectionType"
       and contentHandlingStrategy =
         flag "content-handling-strategy" (optional json_arg)
           ~doc:"JSON ContentHandlingStrategy"
       and credentialsArn =
         flag "credentials-arn" (optional string) ~doc:"STRING Arn"
       and description =
         flag "description" (optional string)
           ~doc:"STRING StringWithLengthBetween0And1024"
       and integrationMethod =
         flag "integration-method" (optional string)
           ~doc:"STRING StringWithLengthBetween1And64"
       and integrationSubtype =
         flag "integration-subtype" (optional string)
           ~doc:"STRING StringWithLengthBetween1And128"
       and integrationUri =
         flag "integration-uri" (optional string)
           ~doc:"STRING UriWithLengthBetween1And2048"
       and passthroughBehavior =
         flag "passthrough-behavior" (optional json_arg)
           ~doc:"JSON PassthroughBehavior"
       and payloadFormatVersion =
         flag "payload-format-version" (optional string)
           ~doc:"STRING StringWithLengthBetween1And64"
       and requestParameters =
         flag "request-parameters" (optional json_arg)
           ~doc:"JSON IntegrationParameters"
       and requestTemplates =
         flag "request-templates" (optional json_arg) ~doc:"JSON TemplateMap"
       and responseParameters =
         flag "response-parameters" (optional json_arg)
           ~doc:"JSON ResponseParameters"
       and templateSelectionExpression =
         flag "template-selection-expression" (optional string)
           ~doc:"STRING SelectionExpression"
       and timeoutInMillis =
         flag "timeout-in-millis" (optional int)
           ~doc:"INT IntegerWithLengthBetween50And30000"
       and tlsConfig =
         flag "tls-config" (optional json_arg) ~doc:"JSON TlsConfigInput"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and integrationType =
         flag "integration-type" (required json_arg)
           ~doc:"JSON IntegrationType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_integration
           (Values.CreateIntegrationRequest.make ?connectionId
              ?connectionType:(Option.map ~f:Values.ConnectionType.of_json
                                 connectionType)
              ?contentHandlingStrategy:(Option.map
                                          ~f:Values.ContentHandlingStrategy.of_json
                                          contentHandlingStrategy)
              ?credentialsArn ?description ?integrationMethod
              ?integrationSubtype ?integrationUri
              ?passthroughBehavior:(Option.map
                                      ~f:Values.PassthroughBehavior.of_json
                                      passthroughBehavior)
              ?payloadFormatVersion
              ?requestParameters:(Option.map
                                    ~f:Values.IntegrationParameters.of_json
                                    requestParameters)
              ?requestTemplates:(Option.map ~f:Values.TemplateMap.of_json
                                   requestTemplates)
              ?responseParameters:(Option.map
                                     ~f:Values.ResponseParameters.of_json
                                     responseParameters)
              ?templateSelectionExpression ?timeoutInMillis
              ?tlsConfig:(Option.map ~f:Values.TlsConfigInput.of_json
                            tlsConfig) ~apiId
              ~integrationType:(Values.IntegrationType.of_json
                                  integrationType) ())
           (Some Values.CreateIntegrationResult.to_json)
           (Some Values.CreateIntegrationResult.error_to_json)])
let create_integration_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentHandlingStrategy =
         flag "content-handling-strategy" (optional json_arg)
           ~doc:"JSON ContentHandlingStrategy"
       and responseParameters =
         flag "response-parameters" (optional json_arg)
           ~doc:"JSON IntegrationParameters"
       and responseTemplates =
         flag "response-templates" (optional json_arg)
           ~doc:"JSON TemplateMap"
       and templateSelectionExpression =
         flag "template-selection-expression" (optional string)
           ~doc:"STRING SelectionExpression"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and integrationId =
         flag "integration-id" (required string) ~doc:"STRING __string"
       and integrationResponseKey =
         flag "integration-response-key" (required string)
           ~doc:"STRING SelectionKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_integration_response
           (Values.CreateIntegrationResponseRequest.make
              ?contentHandlingStrategy:(Option.map
                                          ~f:Values.ContentHandlingStrategy.of_json
                                          contentHandlingStrategy)
              ?responseParameters:(Option.map
                                     ~f:Values.IntegrationParameters.of_json
                                     responseParameters)
              ?responseTemplates:(Option.map ~f:Values.TemplateMap.of_json
                                    responseTemplates)
              ?templateSelectionExpression ~apiId ~integrationId
              ~integrationResponseKey ())
           (Some Values.CreateIntegrationResponseResponse.to_json)
           (Some Values.CreateIntegrationResponseResponse.error_to_json)])
let create_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentType =
         flag "content-type" (optional string)
           ~doc:"STRING StringWithLengthBetween1And256"
       and description =
         flag "description" (optional string)
           ~doc:"STRING StringWithLengthBetween0And1024"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and name =
         flag "name" (required string)
           ~doc:"STRING StringWithLengthBetween1And128"
       and schema =
         flag "schema" (required string)
           ~doc:"STRING StringWithLengthBetween0And32K" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_model
           (Values.CreateModelRequest.make ?contentType ?description ~apiId
              ~name ~schema ()) (Some Values.CreateModelResponse.to_json)
           (Some Values.CreateModelResponse.error_to_json)])
let create_portal =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includedPortalProductArns =
         flag "included-portal-product-arns" (optional json_arg)
           ~doc:"JSON __listOf__stringMin20Max2048"
       and logoUri =
         flag "logo-uri" (optional string) ~doc:"STRING __stringMin0Max1092"
       and rumAppMonitorName =
         flag "rum-app-monitor-name" (optional string)
           ~doc:"STRING __stringMin0Max255"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and authorization =
         flag "authorization" (required json_arg) ~doc:"JSON Authorization"
       and endpointConfiguration =
         flag "endpoint-configuration" (required json_arg)
           ~doc:"JSON EndpointConfigurationRequest"
       and portalContent =
         flag "portal-content" (required json_arg) ~doc:"JSON PortalContent" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_portal
           (Values.CreatePortalRequest.make
              ?includedPortalProductArns:(Option.map
                                            ~f:Values.Zz__listOf__stringMin20Max2048.of_json
                                            includedPortalProductArns)
              ?logoUri ?rumAppMonitorName
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~authorization:(Values.Authorization.of_json authorization)
              ~endpointConfiguration:(Values.EndpointConfigurationRequest.of_json
                                        endpointConfiguration)
              ~portalContent:(Values.PortalContent.of_json portalContent) ())
           (Some Values.CreatePortalResponse.to_json)
           (Some Values.CreatePortalResponse.error_to_json)])
let create_portal_product =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING __stringMin0Max1024"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and displayName =
         flag "display-name" (required string)
           ~doc:"STRING __stringMin1Max255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_portal_product
           (Values.CreatePortalProductRequest.make ?description
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~displayName ())
           (Some Values.CreatePortalProductResponse.to_json)
           (Some Values.CreatePortalProductResponse.error_to_json)])
let create_product_page =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayContent =
         flag "display-content" (required json_arg)
           ~doc:"JSON DisplayContent"
       and portalProductId =
         flag "portal-product-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_product_page
           (Values.CreateProductPageRequest.make
              ~displayContent:(Values.DisplayContent.of_json displayContent)
              ~portalProductId ())
           (Some Values.CreateProductPageResponse.to_json)
           (Some Values.CreateProductPageResponse.error_to_json)])
let create_product_rest_endpoint_page =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayContent =
         flag "display-content" (optional json_arg)
           ~doc:"JSON EndpointDisplayContent"
       and tryItState =
         flag "try-it-state" (optional json_arg) ~doc:"JSON TryItState"
       and portalProductId =
         flag "portal-product-id" (required string) ~doc:"STRING __string"
       and restEndpointIdentifier =
         flag "rest-endpoint-identifier" (required json_arg)
           ~doc:"JSON RestEndpointIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_product_rest_endpoint_page
           (Values.CreateProductRestEndpointPageRequest.make
              ?displayContent:(Option.map
                                 ~f:Values.EndpointDisplayContent.of_json
                                 displayContent)
              ?tryItState:(Option.map ~f:Values.TryItState.of_json tryItState)
              ~portalProductId
              ~restEndpointIdentifier:(Values.RestEndpointIdentifier.of_json
                                         restEndpointIdentifier) ())
           (Some Values.CreateProductRestEndpointPageResponse.to_json)
           (Some Values.CreateProductRestEndpointPageResponse.error_to_json)])
let create_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiKeyRequired =
         flag "api-key-required" (optional bool) ~doc:"BOOL __boolean"
       and authorizationScopes =
         flag "authorization-scopes" (optional json_arg)
           ~doc:"JSON AuthorizationScopes"
       and authorizationType =
         flag "authorization-type" (optional json_arg)
           ~doc:"JSON AuthorizationType"
       and authorizerId =
         flag "authorizer-id" (optional string) ~doc:"STRING Id"
       and modelSelectionExpression =
         flag "model-selection-expression" (optional string)
           ~doc:"STRING SelectionExpression"
       and operationName =
         flag "operation-name" (optional string)
           ~doc:"STRING StringWithLengthBetween1And64"
       and requestModels =
         flag "request-models" (optional json_arg) ~doc:"JSON RouteModels"
       and requestParameters =
         flag "request-parameters" (optional json_arg)
           ~doc:"JSON RouteParameters"
       and routeResponseSelectionExpression =
         flag "route-response-selection-expression" (optional string)
           ~doc:"STRING SelectionExpression"
       and target =
         flag "target" (optional string)
           ~doc:"STRING StringWithLengthBetween1And128"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and routeKey =
         flag "route-key" (required string) ~doc:"STRING SelectionKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_route
           (Values.CreateRouteRequest.make ?apiKeyRequired
              ?authorizationScopes:(Option.map
                                      ~f:Values.AuthorizationScopes.of_json
                                      authorizationScopes)
              ?authorizationType:(Option.map
                                    ~f:Values.AuthorizationType.of_json
                                    authorizationType) ?authorizerId
              ?modelSelectionExpression ?operationName
              ?requestModels:(Option.map ~f:Values.RouteModels.of_json
                                requestModels)
              ?requestParameters:(Option.map
                                    ~f:Values.RouteParameters.of_json
                                    requestParameters)
              ?routeResponseSelectionExpression ?target ~apiId ~routeKey ())
           (Some Values.CreateRouteResult.to_json)
           (Some Values.CreateRouteResult.error_to_json)])
let create_route_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelSelectionExpression =
         flag "model-selection-expression" (optional string)
           ~doc:"STRING SelectionExpression"
       and responseModels =
         flag "response-models" (optional json_arg) ~doc:"JSON RouteModels"
       and responseParameters =
         flag "response-parameters" (optional json_arg)
           ~doc:"JSON RouteParameters"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and routeId = flag "route-id" (required string) ~doc:"STRING __string"
       and routeResponseKey =
         flag "route-response-key" (required string)
           ~doc:"STRING SelectionKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_route_response
           (Values.CreateRouteResponseRequest.make ?modelSelectionExpression
              ?responseModels:(Option.map ~f:Values.RouteModels.of_json
                                 responseModels)
              ?responseParameters:(Option.map
                                     ~f:Values.RouteParameters.of_json
                                     responseParameters) ~apiId ~routeId
              ~routeResponseKey ())
           (Some Values.CreateRouteResponseResponse.to_json)
           (Some Values.CreateRouteResponseResponse.error_to_json)])
let create_routing_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainNameId =
         flag "domain-name-id" (optional string) ~doc:"STRING __string"
       and actions =
         flag "actions" (required json_arg)
           ~doc:"JSON __listOfRoutingRuleAction"
       and conditions =
         flag "conditions" (required json_arg)
           ~doc:"JSON __listOfRoutingRuleCondition"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING __string"
       and priority =
         flag "priority" (required int) ~doc:"INT RoutingRulePriority" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_routing_rule
           (Values.CreateRoutingRuleRequest.make ?domainNameId
              ~actions:(Values.Zz__listOfRoutingRuleAction.of_json actions)
              ~conditions:(Values.Zz__listOfRoutingRuleCondition.of_json
                             conditions) ~domainName ~priority ())
           (Some Values.CreateRoutingRuleResponse.to_json)
           (Some Values.CreateRoutingRuleResponse.error_to_json)])
let create_stage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessLogSettings =
         flag "access-log-settings" (optional json_arg)
           ~doc:"JSON AccessLogSettings"
       and autoDeploy =
         flag "auto-deploy" (optional bool) ~doc:"BOOL __boolean"
       and clientCertificateId =
         flag "client-certificate-id" (optional string) ~doc:"STRING Id"
       and defaultRouteSettings =
         flag "default-route-settings" (optional json_arg)
           ~doc:"JSON RouteSettings"
       and deploymentId =
         flag "deployment-id" (optional string) ~doc:"STRING Id"
       and description =
         flag "description" (optional string)
           ~doc:"STRING StringWithLengthBetween0And1024"
       and routeSettings =
         flag "route-settings" (optional json_arg)
           ~doc:"JSON RouteSettingsMap"
       and stageVariables =
         flag "stage-variables" (optional json_arg)
           ~doc:"JSON StageVariablesMap"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and stageName =
         flag "stage-name" (required string)
           ~doc:"STRING StringWithLengthBetween1And128" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_stage
           (Values.CreateStageRequest.make
              ?accessLogSettings:(Option.map
                                    ~f:Values.AccessLogSettings.of_json
                                    accessLogSettings) ?autoDeploy
              ?clientCertificateId
              ?defaultRouteSettings:(Option.map
                                       ~f:Values.RouteSettings.of_json
                                       defaultRouteSettings) ?deploymentId
              ?description
              ?routeSettings:(Option.map ~f:Values.RouteSettingsMap.of_json
                                routeSettings)
              ?stageVariables:(Option.map ~f:Values.StageVariablesMap.of_json
                                 stageVariables)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~apiId
              ~stageName ()) (Some Values.CreateStageResponse.to_json)
           (Some Values.CreateStageResponse.error_to_json)])
let create_vpc_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and securityGroupIds =
         flag "security-group-ids" (optional json_arg)
           ~doc:"JSON SecurityGroupIdList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name =
         flag "name" (required string)
           ~doc:"STRING StringWithLengthBetween1And128"
       and subnetIds =
         flag "subnet-ids" (required json_arg) ~doc:"JSON SubnetIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_vpc_link
           (Values.CreateVpcLinkRequest.make
              ?securityGroupIds:(Option.map
                                   ~f:Values.SecurityGroupIdList.of_json
                                   securityGroupIds)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~name
              ~subnetIds:(Values.SubnetIdList.of_json subnetIds) ())
           (Some Values.CreateVpcLinkResponse.to_json)
           (Some Values.CreateVpcLinkResponse.error_to_json)])
let delete_access_log_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_access_log_settings
           (Values.DeleteAccessLogSettingsRequest.make ~apiId ~stageName ())
           None None])
let delete_api =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_api (Values.DeleteApiRequest.make ~apiId ()) None None])
let delete_api_mapping =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiMappingId =
         flag "api-mapping-id" (required string) ~doc:"STRING __string"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_api_mapping
           (Values.DeleteApiMappingRequest.make ~apiMappingId ~domainName ())
           None None])
let delete_authorizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and authorizerId =
         flag "authorizer-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_authorizer
           (Values.DeleteAuthorizerRequest.make ~apiId ~authorizerId ()) None
           None])
let delete_cors_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cors_configuration
           (Values.DeleteCorsConfigurationRequest.make ~apiId ()) None None])
let delete_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and deploymentId =
         flag "deployment-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_deployment
           (Values.DeleteDeploymentRequest.make ~apiId ~deploymentId ()) None
           None])
let delete_domain_name =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_domain_name
           (Values.DeleteDomainNameRequest.make ~domainName ()) None None])
let delete_integration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and integrationId =
         flag "integration-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_integration
           (Values.DeleteIntegrationRequest.make ~apiId ~integrationId ())
           None None])
let delete_integration_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and integrationId =
         flag "integration-id" (required string) ~doc:"STRING __string"
       and integrationResponseId =
         flag "integration-response-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_integration_response
           (Values.DeleteIntegrationResponseRequest.make ~apiId
              ~integrationId ~integrationResponseId ()) None None])
let delete_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and modelId = flag "model-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_model
           (Values.DeleteModelRequest.make ~apiId ~modelId ()) None None])
let delete_portal =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and portalId =
         flag "portal-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_portal (Values.DeletePortalRequest.make ~portalId ())
           None None])
let delete_portal_product =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and portalProductId =
         flag "portal-product-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_portal_product
           (Values.DeletePortalProductRequest.make ~portalProductId ()) None
           None])
let delete_portal_product_sharing_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and portalProductId =
         flag "portal-product-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_portal_product_sharing_policy
           (Values.DeletePortalProductSharingPolicyRequest.make
              ~portalProductId ()) None None])
let delete_product_page =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and portalProductId =
         flag "portal-product-id" (required string) ~doc:"STRING __string"
       and productPageId =
         flag "product-page-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_product_page
           (Values.DeleteProductPageRequest.make ~portalProductId
              ~productPageId ()) None None])
let delete_product_rest_endpoint_page =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and portalProductId =
         flag "portal-product-id" (required string) ~doc:"STRING __string"
       and productRestEndpointPageId =
         flag "product-rest-endpoint-page-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_product_rest_endpoint_page
           (Values.DeleteProductRestEndpointPageRequest.make ~portalProductId
              ~productRestEndpointPageId ()) None None])
let delete_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and routeId = flag "route-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_route
           (Values.DeleteRouteRequest.make ~apiId ~routeId ()) None None])
let delete_route_request_parameter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and requestParameterKey =
         flag "request-parameter-key" (required string)
           ~doc:"STRING __string"
       and routeId = flag "route-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_route_request_parameter
           (Values.DeleteRouteRequestParameterRequest.make ~apiId
              ~requestParameterKey ~routeId ()) None None])
let delete_route_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and routeId = flag "route-id" (required string) ~doc:"STRING __string"
       and routeResponseId =
         flag "route-response-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_route_response
           (Values.DeleteRouteResponseRequest.make ~apiId ~routeId
              ~routeResponseId ()) None None])
let delete_route_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and routeKey =
         flag "route-key" (required string) ~doc:"STRING __string"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_route_settings
           (Values.DeleteRouteSettingsRequest.make ~apiId ~routeKey
              ~stageName ()) None None])
let delete_routing_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainNameId =
         flag "domain-name-id" (optional string) ~doc:"STRING __string"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING __string"
       and routingRuleId =
         flag "routing-rule-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_routing_rule
           (Values.DeleteRoutingRuleRequest.make ?domainNameId ~domainName
              ~routingRuleId ()) None None])
let delete_stage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_stage
           (Values.DeleteStageRequest.make ~apiId ~stageName ()) None None])
let delete_vpc_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vpcLinkId =
         flag "vpc-link-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vpc_link
           (Values.DeleteVpcLinkRequest.make ~vpcLinkId ())
           (Some Values.DeleteVpcLinkResponse.to_json)
           (Some Values.DeleteVpcLinkResponse.error_to_json)])
let export_api =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and exportVersion =
         flag "export-version" (optional string) ~doc:"STRING __string"
       and includeExtensions =
         flag "include-extensions" (optional bool) ~doc:"BOOL __boolean"
       and stageName =
         flag "stage-name" (optional string) ~doc:"STRING __string"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and outputType =
         flag "output-type" (required string) ~doc:"STRING __string"
       and specification =
         flag "specification" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.export_api
           (Values.ExportApiRequest.make ?exportVersion ?includeExtensions
              ?stageName ~apiId ~outputType ~specification ())
           (Some Values.ExportApiResponse.to_json)
           (Some Values.ExportApiResponse.error_to_json)])
let disable_portal =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and portalId =
         flag "portal-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_portal (Values.DisablePortalRequest.make ~portalId ())
           None None])
let reset_authorizers_cache =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reset_authorizers_cache
           (Values.ResetAuthorizersCacheRequest.make ~apiId ~stageName ())
           None None])
let get_api =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_api (Values.GetApiRequest.make ~apiId ())
           (Some Values.GetApiResponse.to_json)
           (Some Values.GetApiResponse.error_to_json)])
let get_api_mapping =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiMappingId =
         flag "api-mapping-id" (required string) ~doc:"STRING __string"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_api_mapping
           (Values.GetApiMappingRequest.make ~apiMappingId ~domainName ())
           (Some Values.GetApiMappingResponse.to_json)
           (Some Values.GetApiMappingResponse.error_to_json)])
let get_api_mappings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_api_mappings
           (Values.GetApiMappingsRequest.make ?maxResults ?nextToken
              ~domainName ()) (Some Values.GetApiMappingsResponse.to_json)
           (Some Values.GetApiMappingsResponse.error_to_json)])
let get_apis =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_apis (Values.GetApisRequest.make ?maxResults ?nextToken ())
           (Some Values.GetApisResponse.to_json)
           (Some Values.GetApisResponse.error_to_json)])
let get_authorizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and authorizerId =
         flag "authorizer-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_authorizer
           (Values.GetAuthorizerRequest.make ~apiId ~authorizerId ())
           (Some Values.GetAuthorizerResponse.to_json)
           (Some Values.GetAuthorizerResponse.error_to_json)])
let get_authorizers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_authorizers
           (Values.GetAuthorizersRequest.make ?maxResults ?nextToken ~apiId
              ()) (Some Values.GetAuthorizersResponse.to_json)
           (Some Values.GetAuthorizersResponse.error_to_json)])
let get_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and deploymentId =
         flag "deployment-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_deployment
           (Values.GetDeploymentRequest.make ~apiId ~deploymentId ())
           (Some Values.GetDeploymentResponse.to_json)
           (Some Values.GetDeploymentResponse.error_to_json)])
let get_deployments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_deployments
           (Values.GetDeploymentsRequest.make ?maxResults ?nextToken ~apiId
              ()) (Some Values.GetDeploymentsResponse.to_json)
           (Some Values.GetDeploymentsResponse.error_to_json)])
let get_domain_name =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_domain_name
           (Values.GetDomainNameRequest.make ~domainName ())
           (Some Values.GetDomainNameResponse.to_json)
           (Some Values.GetDomainNameResponse.error_to_json)])
let get_domain_names =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_domain_names
           (Values.GetDomainNamesRequest.make ?maxResults ?nextToken ())
           (Some Values.GetDomainNamesResponse.to_json)
           (Some Values.GetDomainNamesResponse.error_to_json)])
let get_integration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and integrationId =
         flag "integration-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_integration
           (Values.GetIntegrationRequest.make ~apiId ~integrationId ())
           (Some Values.GetIntegrationResult.to_json)
           (Some Values.GetIntegrationResult.error_to_json)])
let get_integration_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and integrationId =
         flag "integration-id" (required string) ~doc:"STRING __string"
       and integrationResponseId =
         flag "integration-response-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_integration_response
           (Values.GetIntegrationResponseRequest.make ~apiId ~integrationId
              ~integrationResponseId ())
           (Some Values.GetIntegrationResponseResponse.to_json)
           (Some Values.GetIntegrationResponseResponse.error_to_json)])
let get_integration_responses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and integrationId =
         flag "integration-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_integration_responses
           (Values.GetIntegrationResponsesRequest.make ?maxResults ?nextToken
              ~apiId ~integrationId ())
           (Some Values.GetIntegrationResponsesResponse.to_json)
           (Some Values.GetIntegrationResponsesResponse.error_to_json)])
let get_integrations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_integrations
           (Values.GetIntegrationsRequest.make ?maxResults ?nextToken ~apiId
              ()) (Some Values.GetIntegrationsResponse.to_json)
           (Some Values.GetIntegrationsResponse.error_to_json)])
let get_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and modelId = flag "model-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_model (Values.GetModelRequest.make ~apiId ~modelId ())
           (Some Values.GetModelResponse.to_json)
           (Some Values.GetModelResponse.error_to_json)])
let get_model_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and modelId = flag "model-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_model_template
           (Values.GetModelTemplateRequest.make ~apiId ~modelId ())
           (Some Values.GetModelTemplateResponse.to_json)
           (Some Values.GetModelTemplateResponse.error_to_json)])
let get_models =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_models
           (Values.GetModelsRequest.make ?maxResults ?nextToken ~apiId ())
           (Some Values.GetModelsResponse.to_json)
           (Some Values.GetModelsResponse.error_to_json)])
let get_portal =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and portalId =
         flag "portal-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_portal (Values.GetPortalRequest.make ~portalId ())
           (Some Values.GetPortalResponse.to_json)
           (Some Values.GetPortalResponse.error_to_json)])
let get_portal_product =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceOwnerAccountId =
         flag "resource-owner-account-id" (optional string)
           ~doc:"STRING __string"
       and portalProductId =
         flag "portal-product-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_portal_product
           (Values.GetPortalProductRequest.make ?resourceOwnerAccountId
              ~portalProductId ())
           (Some Values.GetPortalProductResponse.to_json)
           (Some Values.GetPortalProductResponse.error_to_json)])
let get_portal_product_sharing_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and portalProductId =
         flag "portal-product-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_portal_product_sharing_policy
           (Values.GetPortalProductSharingPolicyRequest.make ~portalProductId
              ()) (Some Values.GetPortalProductSharingPolicyResponse.to_json)
           (Some Values.GetPortalProductSharingPolicyResponse.error_to_json)])
let get_product_page =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceOwnerAccountId =
         flag "resource-owner-account-id" (optional string)
           ~doc:"STRING __string"
       and portalProductId =
         flag "portal-product-id" (required string) ~doc:"STRING __string"
       and productPageId =
         flag "product-page-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_product_page
           (Values.GetProductPageRequest.make ?resourceOwnerAccountId
              ~portalProductId ~productPageId ())
           (Some Values.GetProductPageResponse.to_json)
           (Some Values.GetProductPageResponse.error_to_json)])
let get_product_rest_endpoint_page =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeRawDisplayContent =
         flag "include-raw-display-content" (optional string)
           ~doc:"STRING __string"
       and resourceOwnerAccountId =
         flag "resource-owner-account-id" (optional string)
           ~doc:"STRING __string"
       and portalProductId =
         flag "portal-product-id" (required string) ~doc:"STRING __string"
       and productRestEndpointPageId =
         flag "product-rest-endpoint-page-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_product_rest_endpoint_page
           (Values.GetProductRestEndpointPageRequest.make
              ?includeRawDisplayContent ?resourceOwnerAccountId
              ~portalProductId ~productRestEndpointPageId ())
           (Some Values.GetProductRestEndpointPageResponse.to_json)
           (Some Values.GetProductRestEndpointPageResponse.error_to_json)])
let get_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and routeId = flag "route-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_route (Values.GetRouteRequest.make ~apiId ~routeId ())
           (Some Values.GetRouteResult.to_json)
           (Some Values.GetRouteResult.error_to_json)])
let get_route_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and routeId = flag "route-id" (required string) ~doc:"STRING __string"
       and routeResponseId =
         flag "route-response-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_route_response
           (Values.GetRouteResponseRequest.make ~apiId ~routeId
              ~routeResponseId ())
           (Some Values.GetRouteResponseResponse.to_json)
           (Some Values.GetRouteResponseResponse.error_to_json)])
let get_route_responses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and routeId = flag "route-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_route_responses
           (Values.GetRouteResponsesRequest.make ?maxResults ?nextToken
              ~apiId ~routeId ())
           (Some Values.GetRouteResponsesResponse.to_json)
           (Some Values.GetRouteResponsesResponse.error_to_json)])
let get_routes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_routes
           (Values.GetRoutesRequest.make ?maxResults ?nextToken ~apiId ())
           (Some Values.GetRoutesResponse.to_json)
           (Some Values.GetRoutesResponse.error_to_json)])
let get_routing_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainNameId =
         flag "domain-name-id" (optional string) ~doc:"STRING __string"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING __string"
       and routingRuleId =
         flag "routing-rule-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_routing_rule
           (Values.GetRoutingRuleRequest.make ?domainNameId ~domainName
              ~routingRuleId ()) (Some Values.GetRoutingRuleResponse.to_json)
           (Some Values.GetRoutingRuleResponse.error_to_json)])
let list_routing_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainNameId =
         flag "domain-name-id" (optional string) ~doc:"STRING __string"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_routing_rules
           (Values.ListRoutingRulesRequest.make ?domainNameId ?maxResults
              ?nextToken ~domainName ())
           (Some Values.ListRoutingRulesResponse.to_json)
           (Some Values.ListRoutingRulesResponse.error_to_json)])
let get_stage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_stage (Values.GetStageRequest.make ~apiId ~stageName ())
           (Some Values.GetStageResponse.to_json)
           (Some Values.GetStageResponse.error_to_json)])
let get_stages =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_stages
           (Values.GetStagesRequest.make ?maxResults ?nextToken ~apiId ())
           (Some Values.GetStagesResponse.to_json)
           (Some Values.GetStagesResponse.error_to_json)])
let get_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_tags (Values.GetTagsRequest.make ~resourceArn ())
           (Some Values.GetTagsResponse.to_json)
           (Some Values.GetTagsResponse.error_to_json)])
let get_vpc_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vpcLinkId =
         flag "vpc-link-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_vpc_link (Values.GetVpcLinkRequest.make ~vpcLinkId ())
           (Some Values.GetVpcLinkResponse.to_json)
           (Some Values.GetVpcLinkResponse.error_to_json)])
let get_vpc_links =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_vpc_links
           (Values.GetVpcLinksRequest.make ?maxResults ?nextToken ())
           (Some Values.GetVpcLinksResponse.to_json)
           (Some Values.GetVpcLinksResponse.error_to_json)])
let import_api =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and basepath =
         flag "basepath" (optional string) ~doc:"STRING __string"
       and failOnWarnings =
         flag "fail-on-warnings" (optional bool) ~doc:"BOOL __boolean"
       and body = flag "body" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_api
           (Values.ImportApiRequest.make ?basepath ?failOnWarnings ~body ())
           (Some Values.ImportApiResponse.to_json)
           (Some Values.ImportApiResponse.error_to_json)])
let list_portal_products =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and resourceOwner =
         flag "resource-owner" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_portal_products
           (Values.ListPortalProductsRequest.make ?maxResults ?nextToken
              ?resourceOwner ())
           (Some Values.ListPortalProductsResponse.to_json)
           (Some Values.ListPortalProductsResponse.error_to_json)])
let list_portals =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_portals
           (Values.ListPortalsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListPortalsResponse.to_json)
           (Some Values.ListPortalsResponse.error_to_json)])
let list_product_pages =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and resourceOwnerAccountId =
         flag "resource-owner-account-id" (optional string)
           ~doc:"STRING __string"
       and portalProductId =
         flag "portal-product-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_product_pages
           (Values.ListProductPagesRequest.make ?maxResults ?nextToken
              ?resourceOwnerAccountId ~portalProductId ())
           (Some Values.ListProductPagesResponse.to_json)
           (Some Values.ListProductPagesResponse.error_to_json)])
let list_product_rest_endpoint_pages =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING __string"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and resourceOwnerAccountId =
         flag "resource-owner-account-id" (optional string)
           ~doc:"STRING __string"
       and portalProductId =
         flag "portal-product-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_product_rest_endpoint_pages
           (Values.ListProductRestEndpointPagesRequest.make ?maxResults
              ?nextToken ?resourceOwnerAccountId ~portalProductId ())
           (Some Values.ListProductRestEndpointPagesResponse.to_json)
           (Some Values.ListProductRestEndpointPagesResponse.error_to_json)])
let preview_portal =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and portalId =
         flag "portal-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.preview_portal (Values.PreviewPortalRequest.make ~portalId ())
           (Some Values.PreviewPortalResponse.to_json)
           (Some Values.PreviewPortalResponse.error_to_json)])
let publish_portal =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING __stringMin0Max1024"
       and portalId =
         flag "portal-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.publish_portal
           (Values.PublishPortalRequest.make ?description ~portalId ())
           (Some Values.PublishPortalResponse.to_json)
           (Some Values.PublishPortalResponse.error_to_json)])
let put_portal_product_sharing_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyDocument =
         flag "policy-document" (required string)
           ~doc:"STRING __stringMin1Max307200"
       and portalProductId =
         flag "portal-product-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_portal_product_sharing_policy
           (Values.PutPortalProductSharingPolicyRequest.make ~policyDocument
              ~portalProductId ())
           (Some Values.PutPortalProductSharingPolicyResponse.to_json)
           (Some Values.PutPortalProductSharingPolicyResponse.error_to_json)])
let put_routing_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainNameId =
         flag "domain-name-id" (optional string) ~doc:"STRING __string"
       and actions =
         flag "actions" (required json_arg)
           ~doc:"JSON __listOfRoutingRuleAction"
       and conditions =
         flag "conditions" (required json_arg)
           ~doc:"JSON __listOfRoutingRuleCondition"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING __string"
       and priority =
         flag "priority" (required int) ~doc:"INT RoutingRulePriority"
       and routingRuleId =
         flag "routing-rule-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_routing_rule
           (Values.PutRoutingRuleRequest.make ?domainNameId
              ~actions:(Values.Zz__listOfRoutingRuleAction.of_json actions)
              ~conditions:(Values.Zz__listOfRoutingRuleCondition.of_json
                             conditions) ~domainName ~priority ~routingRuleId
              ()) (Some Values.PutRoutingRuleResponse.to_json)
           (Some Values.PutRoutingRuleResponse.error_to_json)])
let reimport_api =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and basepath =
         flag "basepath" (optional string) ~doc:"STRING __string"
       and failOnWarnings =
         flag "fail-on-warnings" (optional bool) ~doc:"BOOL __boolean"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and body = flag "body" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reimport_api
           (Values.ReimportApiRequest.make ?basepath ?failOnWarnings ~apiId
              ~body ()) (Some Values.ReimportApiResponse.to_json)
           (Some Values.ReimportApiResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~resourceArn ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON __listOf__string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.Zz__listOf__string.of_json tagKeys) ()) None
           None])
let update_api =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiKeySelectionExpression =
         flag "api-key-selection-expression" (optional string)
           ~doc:"STRING SelectionExpression"
       and corsConfiguration =
         flag "cors-configuration" (optional json_arg) ~doc:"JSON Cors"
       and credentialsArn =
         flag "credentials-arn" (optional string) ~doc:"STRING Arn"
       and description =
         flag "description" (optional string)
           ~doc:"STRING StringWithLengthBetween0And1024"
       and disableSchemaValidation =
         flag "disable-schema-validation" (optional bool)
           ~doc:"BOOL __boolean"
       and disableExecuteApiEndpoint =
         flag "disable-execute-api-endpoint" (optional bool)
           ~doc:"BOOL __boolean"
       and ipAddressType =
         flag "ip-address-type" (optional json_arg) ~doc:"JSON IpAddressType"
       and name =
         flag "name" (optional string)
           ~doc:"STRING StringWithLengthBetween1And128"
       and routeKey =
         flag "route-key" (optional string) ~doc:"STRING SelectionKey"
       and routeSelectionExpression =
         flag "route-selection-expression" (optional string)
           ~doc:"STRING SelectionExpression"
       and target =
         flag "target" (optional string)
           ~doc:"STRING UriWithLengthBetween1And2048"
       and version =
         flag "version" (optional string)
           ~doc:"STRING StringWithLengthBetween1And64"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_api
           (Values.UpdateApiRequest.make ?apiKeySelectionExpression
              ?corsConfiguration:(Option.map ~f:Values.Cors.of_json
                                    corsConfiguration) ?credentialsArn
              ?description ?disableSchemaValidation
              ?disableExecuteApiEndpoint
              ?ipAddressType:(Option.map ~f:Values.IpAddressType.of_json
                                ipAddressType) ?name ?routeKey
              ?routeSelectionExpression ?target ?version ~apiId ())
           (Some Values.UpdateApiResponse.to_json)
           (Some Values.UpdateApiResponse.error_to_json)])
let update_api_mapping =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiMappingKey =
         flag "api-mapping-key" (optional string) ~doc:"STRING SelectionKey"
       and stage =
         flag "stage" (optional string)
           ~doc:"STRING StringWithLengthBetween1And128"
       and apiId = flag "api-id" (required string) ~doc:"STRING Id"
       and apiMappingId =
         flag "api-mapping-id" (required string) ~doc:"STRING __string"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_api_mapping
           (Values.UpdateApiMappingRequest.make ?apiMappingKey ?stage ~apiId
              ~apiMappingId ~domainName ())
           (Some Values.UpdateApiMappingResponse.to_json)
           (Some Values.UpdateApiMappingResponse.error_to_json)])
let update_authorizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authorizerCredentialsArn =
         flag "authorizer-credentials-arn" (optional string)
           ~doc:"STRING Arn"
       and authorizerPayloadFormatVersion =
         flag "authorizer-payload-format-version" (optional string)
           ~doc:"STRING StringWithLengthBetween1And64"
       and authorizerResultTtlInSeconds =
         flag "authorizer-result-ttl-in-seconds" (optional int)
           ~doc:"INT IntegerWithLengthBetween0And3600"
       and authorizerType =
         flag "authorizer-type" (optional json_arg)
           ~doc:"JSON AuthorizerType"
       and authorizerUri =
         flag "authorizer-uri" (optional string)
           ~doc:"STRING UriWithLengthBetween1And2048"
       and enableSimpleResponses =
         flag "enable-simple-responses" (optional bool) ~doc:"BOOL __boolean"
       and identitySource =
         flag "identity-source" (optional json_arg)
           ~doc:"JSON IdentitySourceList"
       and identityValidationExpression =
         flag "identity-validation-expression" (optional string)
           ~doc:"STRING StringWithLengthBetween0And1024"
       and jwtConfiguration =
         flag "jwt-configuration" (optional json_arg)
           ~doc:"JSON JWTConfiguration"
       and name =
         flag "name" (optional string)
           ~doc:"STRING StringWithLengthBetween1And128"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and authorizerId =
         flag "authorizer-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_authorizer
           (Values.UpdateAuthorizerRequest.make ?authorizerCredentialsArn
              ?authorizerPayloadFormatVersion ?authorizerResultTtlInSeconds
              ?authorizerType:(Option.map ~f:Values.AuthorizerType.of_json
                                 authorizerType) ?authorizerUri
              ?enableSimpleResponses
              ?identitySource:(Option.map
                                 ~f:Values.IdentitySourceList.of_json
                                 identitySource)
              ?identityValidationExpression
              ?jwtConfiguration:(Option.map
                                   ~f:Values.JWTConfiguration.of_json
                                   jwtConfiguration) ?name ~apiId
              ~authorizerId ())
           (Some Values.UpdateAuthorizerResponse.to_json)
           (Some Values.UpdateAuthorizerResponse.error_to_json)])
let update_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING StringWithLengthBetween0And1024"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and deploymentId =
         flag "deployment-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_deployment
           (Values.UpdateDeploymentRequest.make ?description ~apiId
              ~deploymentId ())
           (Some Values.UpdateDeploymentResponse.to_json)
           (Some Values.UpdateDeploymentResponse.error_to_json)])
let update_domain_name =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainNameConfigurations =
         flag "domain-name-configurations" (optional json_arg)
           ~doc:"JSON DomainNameConfigurations"
       and mutualTlsAuthentication =
         flag "mutual-tls-authentication" (optional json_arg)
           ~doc:"JSON MutualTlsAuthenticationInput"
       and routingMode =
         flag "routing-mode" (optional json_arg) ~doc:"JSON RoutingMode"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_domain_name
           (Values.UpdateDomainNameRequest.make
              ?domainNameConfigurations:(Option.map
                                           ~f:Values.DomainNameConfigurations.of_json
                                           domainNameConfigurations)
              ?mutualTlsAuthentication:(Option.map
                                          ~f:Values.MutualTlsAuthenticationInput.of_json
                                          mutualTlsAuthentication)
              ?routingMode:(Option.map ~f:Values.RoutingMode.of_json
                              routingMode) ~domainName ())
           (Some Values.UpdateDomainNameResponse.to_json)
           (Some Values.UpdateDomainNameResponse.error_to_json)])
let update_integration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectionId =
         flag "connection-id" (optional string)
           ~doc:"STRING StringWithLengthBetween1And1024"
       and connectionType =
         flag "connection-type" (optional json_arg)
           ~doc:"JSON ConnectionType"
       and contentHandlingStrategy =
         flag "content-handling-strategy" (optional json_arg)
           ~doc:"JSON ContentHandlingStrategy"
       and credentialsArn =
         flag "credentials-arn" (optional string) ~doc:"STRING Arn"
       and description =
         flag "description" (optional string)
           ~doc:"STRING StringWithLengthBetween0And1024"
       and integrationMethod =
         flag "integration-method" (optional string)
           ~doc:"STRING StringWithLengthBetween1And64"
       and integrationSubtype =
         flag "integration-subtype" (optional string)
           ~doc:"STRING StringWithLengthBetween1And128"
       and integrationType =
         flag "integration-type" (optional json_arg)
           ~doc:"JSON IntegrationType"
       and integrationUri =
         flag "integration-uri" (optional string)
           ~doc:"STRING UriWithLengthBetween1And2048"
       and passthroughBehavior =
         flag "passthrough-behavior" (optional json_arg)
           ~doc:"JSON PassthroughBehavior"
       and payloadFormatVersion =
         flag "payload-format-version" (optional string)
           ~doc:"STRING StringWithLengthBetween1And64"
       and requestParameters =
         flag "request-parameters" (optional json_arg)
           ~doc:"JSON IntegrationParameters"
       and requestTemplates =
         flag "request-templates" (optional json_arg) ~doc:"JSON TemplateMap"
       and responseParameters =
         flag "response-parameters" (optional json_arg)
           ~doc:"JSON ResponseParameters"
       and templateSelectionExpression =
         flag "template-selection-expression" (optional string)
           ~doc:"STRING SelectionExpression"
       and timeoutInMillis =
         flag "timeout-in-millis" (optional int)
           ~doc:"INT IntegerWithLengthBetween50And30000"
       and tlsConfig =
         flag "tls-config" (optional json_arg) ~doc:"JSON TlsConfigInput"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and integrationId =
         flag "integration-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_integration
           (Values.UpdateIntegrationRequest.make ?connectionId
              ?connectionType:(Option.map ~f:Values.ConnectionType.of_json
                                 connectionType)
              ?contentHandlingStrategy:(Option.map
                                          ~f:Values.ContentHandlingStrategy.of_json
                                          contentHandlingStrategy)
              ?credentialsArn ?description ?integrationMethod
              ?integrationSubtype
              ?integrationType:(Option.map ~f:Values.IntegrationType.of_json
                                  integrationType) ?integrationUri
              ?passthroughBehavior:(Option.map
                                      ~f:Values.PassthroughBehavior.of_json
                                      passthroughBehavior)
              ?payloadFormatVersion
              ?requestParameters:(Option.map
                                    ~f:Values.IntegrationParameters.of_json
                                    requestParameters)
              ?requestTemplates:(Option.map ~f:Values.TemplateMap.of_json
                                   requestTemplates)
              ?responseParameters:(Option.map
                                     ~f:Values.ResponseParameters.of_json
                                     responseParameters)
              ?templateSelectionExpression ?timeoutInMillis
              ?tlsConfig:(Option.map ~f:Values.TlsConfigInput.of_json
                            tlsConfig) ~apiId ~integrationId ())
           (Some Values.UpdateIntegrationResult.to_json)
           (Some Values.UpdateIntegrationResult.error_to_json)])
let update_integration_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentHandlingStrategy =
         flag "content-handling-strategy" (optional json_arg)
           ~doc:"JSON ContentHandlingStrategy"
       and integrationResponseKey =
         flag "integration-response-key" (optional string)
           ~doc:"STRING SelectionKey"
       and responseParameters =
         flag "response-parameters" (optional json_arg)
           ~doc:"JSON IntegrationParameters"
       and responseTemplates =
         flag "response-templates" (optional json_arg)
           ~doc:"JSON TemplateMap"
       and templateSelectionExpression =
         flag "template-selection-expression" (optional string)
           ~doc:"STRING SelectionExpression"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and integrationId =
         flag "integration-id" (required string) ~doc:"STRING __string"
       and integrationResponseId =
         flag "integration-response-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_integration_response
           (Values.UpdateIntegrationResponseRequest.make
              ?contentHandlingStrategy:(Option.map
                                          ~f:Values.ContentHandlingStrategy.of_json
                                          contentHandlingStrategy)
              ?integrationResponseKey
              ?responseParameters:(Option.map
                                     ~f:Values.IntegrationParameters.of_json
                                     responseParameters)
              ?responseTemplates:(Option.map ~f:Values.TemplateMap.of_json
                                    responseTemplates)
              ?templateSelectionExpression ~apiId ~integrationId
              ~integrationResponseId ())
           (Some Values.UpdateIntegrationResponseResponse.to_json)
           (Some Values.UpdateIntegrationResponseResponse.error_to_json)])
let update_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentType =
         flag "content-type" (optional string)
           ~doc:"STRING StringWithLengthBetween1And256"
       and description =
         flag "description" (optional string)
           ~doc:"STRING StringWithLengthBetween0And1024"
       and name =
         flag "name" (optional string)
           ~doc:"STRING StringWithLengthBetween1And128"
       and schema =
         flag "schema" (optional string)
           ~doc:"STRING StringWithLengthBetween0And32K"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and modelId = flag "model-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_model
           (Values.UpdateModelRequest.make ?contentType ?description ?name
              ?schema ~apiId ~modelId ())
           (Some Values.UpdateModelResponse.to_json)
           (Some Values.UpdateModelResponse.error_to_json)])
let update_portal =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authorization =
         flag "authorization" (optional json_arg) ~doc:"JSON Authorization"
       and endpointConfiguration =
         flag "endpoint-configuration" (optional json_arg)
           ~doc:"JSON EndpointConfigurationRequest"
       and includedPortalProductArns =
         flag "included-portal-product-arns" (optional json_arg)
           ~doc:"JSON __listOf__stringMin20Max2048"
       and logoUri =
         flag "logo-uri" (optional string) ~doc:"STRING __stringMin0Max1092"
       and portalContent =
         flag "portal-content" (optional json_arg) ~doc:"JSON PortalContent"
       and rumAppMonitorName =
         flag "rum-app-monitor-name" (optional string)
           ~doc:"STRING __stringMin0Max255"
       and portalId =
         flag "portal-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_portal
           (Values.UpdatePortalRequest.make
              ?authorization:(Option.map ~f:Values.Authorization.of_json
                                authorization)
              ?endpointConfiguration:(Option.map
                                        ~f:Values.EndpointConfigurationRequest.of_json
                                        endpointConfiguration)
              ?includedPortalProductArns:(Option.map
                                            ~f:Values.Zz__listOf__stringMin20Max2048.of_json
                                            includedPortalProductArns)
              ?logoUri
              ?portalContent:(Option.map ~f:Values.PortalContent.of_json
                                portalContent) ?rumAppMonitorName ~portalId
              ()) (Some Values.UpdatePortalResponse.to_json)
           (Some Values.UpdatePortalResponse.error_to_json)])
let update_portal_product =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING __stringMin0Max1024"
       and displayName =
         flag "display-name" (optional string)
           ~doc:"STRING __stringMin1Max255"
       and displayOrder =
         flag "display-order" (optional json_arg) ~doc:"JSON DisplayOrder"
       and portalProductId =
         flag "portal-product-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_portal_product
           (Values.UpdatePortalProductRequest.make ?description ?displayName
              ?displayOrder:(Option.map ~f:Values.DisplayOrder.of_json
                               displayOrder) ~portalProductId ())
           (Some Values.UpdatePortalProductResponse.to_json)
           (Some Values.UpdatePortalProductResponse.error_to_json)])
let update_product_page =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayContent =
         flag "display-content" (optional json_arg)
           ~doc:"JSON DisplayContent"
       and portalProductId =
         flag "portal-product-id" (required string) ~doc:"STRING __string"
       and productPageId =
         flag "product-page-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_product_page
           (Values.UpdateProductPageRequest.make
              ?displayContent:(Option.map ~f:Values.DisplayContent.of_json
                                 displayContent) ~portalProductId
              ~productPageId ())
           (Some Values.UpdateProductPageResponse.to_json)
           (Some Values.UpdateProductPageResponse.error_to_json)])
let update_product_rest_endpoint_page =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayContent =
         flag "display-content" (optional json_arg)
           ~doc:"JSON EndpointDisplayContent"
       and tryItState =
         flag "try-it-state" (optional json_arg) ~doc:"JSON TryItState"
       and portalProductId =
         flag "portal-product-id" (required string) ~doc:"STRING __string"
       and productRestEndpointPageId =
         flag "product-rest-endpoint-page-id" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_product_rest_endpoint_page
           (Values.UpdateProductRestEndpointPageRequest.make
              ?displayContent:(Option.map
                                 ~f:Values.EndpointDisplayContent.of_json
                                 displayContent)
              ?tryItState:(Option.map ~f:Values.TryItState.of_json tryItState)
              ~portalProductId ~productRestEndpointPageId ())
           (Some Values.UpdateProductRestEndpointPageResponse.to_json)
           (Some Values.UpdateProductRestEndpointPageResponse.error_to_json)])
let update_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiKeyRequired =
         flag "api-key-required" (optional bool) ~doc:"BOOL __boolean"
       and authorizationScopes =
         flag "authorization-scopes" (optional json_arg)
           ~doc:"JSON AuthorizationScopes"
       and authorizationType =
         flag "authorization-type" (optional json_arg)
           ~doc:"JSON AuthorizationType"
       and authorizerId =
         flag "authorizer-id" (optional string) ~doc:"STRING Id"
       and modelSelectionExpression =
         flag "model-selection-expression" (optional string)
           ~doc:"STRING SelectionExpression"
       and operationName =
         flag "operation-name" (optional string)
           ~doc:"STRING StringWithLengthBetween1And64"
       and requestModels =
         flag "request-models" (optional json_arg) ~doc:"JSON RouteModels"
       and requestParameters =
         flag "request-parameters" (optional json_arg)
           ~doc:"JSON RouteParameters"
       and routeKey =
         flag "route-key" (optional string) ~doc:"STRING SelectionKey"
       and routeResponseSelectionExpression =
         flag "route-response-selection-expression" (optional string)
           ~doc:"STRING SelectionExpression"
       and target =
         flag "target" (optional string)
           ~doc:"STRING StringWithLengthBetween1And128"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and routeId = flag "route-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_route
           (Values.UpdateRouteRequest.make ?apiKeyRequired
              ?authorizationScopes:(Option.map
                                      ~f:Values.AuthorizationScopes.of_json
                                      authorizationScopes)
              ?authorizationType:(Option.map
                                    ~f:Values.AuthorizationType.of_json
                                    authorizationType) ?authorizerId
              ?modelSelectionExpression ?operationName
              ?requestModels:(Option.map ~f:Values.RouteModels.of_json
                                requestModels)
              ?requestParameters:(Option.map
                                    ~f:Values.RouteParameters.of_json
                                    requestParameters) ?routeKey
              ?routeResponseSelectionExpression ?target ~apiId ~routeId ())
           (Some Values.UpdateRouteResult.to_json)
           (Some Values.UpdateRouteResult.error_to_json)])
let update_route_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelSelectionExpression =
         flag "model-selection-expression" (optional string)
           ~doc:"STRING SelectionExpression"
       and responseModels =
         flag "response-models" (optional json_arg) ~doc:"JSON RouteModels"
       and responseParameters =
         flag "response-parameters" (optional json_arg)
           ~doc:"JSON RouteParameters"
       and routeResponseKey =
         flag "route-response-key" (optional string)
           ~doc:"STRING SelectionKey"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and routeId = flag "route-id" (required string) ~doc:"STRING __string"
       and routeResponseId =
         flag "route-response-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_route_response
           (Values.UpdateRouteResponseRequest.make ?modelSelectionExpression
              ?responseModels:(Option.map ~f:Values.RouteModels.of_json
                                 responseModels)
              ?responseParameters:(Option.map
                                     ~f:Values.RouteParameters.of_json
                                     responseParameters) ?routeResponseKey
              ~apiId ~routeId ~routeResponseId ())
           (Some Values.UpdateRouteResponseResponse.to_json)
           (Some Values.UpdateRouteResponseResponse.error_to_json)])
let update_stage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessLogSettings =
         flag "access-log-settings" (optional json_arg)
           ~doc:"JSON AccessLogSettings"
       and autoDeploy =
         flag "auto-deploy" (optional bool) ~doc:"BOOL __boolean"
       and clientCertificateId =
         flag "client-certificate-id" (optional string) ~doc:"STRING Id"
       and defaultRouteSettings =
         flag "default-route-settings" (optional json_arg)
           ~doc:"JSON RouteSettings"
       and deploymentId =
         flag "deployment-id" (optional string) ~doc:"STRING Id"
       and description =
         flag "description" (optional string)
           ~doc:"STRING StringWithLengthBetween0And1024"
       and routeSettings =
         flag "route-settings" (optional json_arg)
           ~doc:"JSON RouteSettingsMap"
       and stageVariables =
         flag "stage-variables" (optional json_arg)
           ~doc:"JSON StageVariablesMap"
       and apiId = flag "api-id" (required string) ~doc:"STRING __string"
       and stageName =
         flag "stage-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_stage
           (Values.UpdateStageRequest.make
              ?accessLogSettings:(Option.map
                                    ~f:Values.AccessLogSettings.of_json
                                    accessLogSettings) ?autoDeploy
              ?clientCertificateId
              ?defaultRouteSettings:(Option.map
                                       ~f:Values.RouteSettings.of_json
                                       defaultRouteSettings) ?deploymentId
              ?description
              ?routeSettings:(Option.map ~f:Values.RouteSettingsMap.of_json
                                routeSettings)
              ?stageVariables:(Option.map ~f:Values.StageVariablesMap.of_json
                                 stageVariables) ~apiId ~stageName ())
           (Some Values.UpdateStageResponse.to_json)
           (Some Values.UpdateStageResponse.error_to_json)])
let update_vpc_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string)
           ~doc:"STRING StringWithLengthBetween1And128"
       and vpcLinkId =
         flag "vpc-link-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_vpc_link
           (Values.UpdateVpcLinkRequest.make ?name ~vpcLinkId ())
           (Some Values.UpdateVpcLinkResponse.to_json)
           (Some Values.UpdateVpcLinkResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-api", create_api);
    ("create-api-mapping", create_api_mapping);
    ("create-authorizer", create_authorizer);
    ("create-deployment", create_deployment);
    ("create-domain-name", create_domain_name);
    ("create-integration", create_integration);
    ("create-integration-response", create_integration_response);
    ("create-model", create_model);
    ("create-portal", create_portal);
    ("create-portal-product", create_portal_product);
    ("create-product-page", create_product_page);
    ("create-product-rest-endpoint-page", create_product_rest_endpoint_page);
    ("create-route", create_route);
    ("create-route-response", create_route_response);
    ("create-routing-rule", create_routing_rule);
    ("create-stage", create_stage);
    ("create-vpc-link", create_vpc_link);
    ("delete-access-log-settings", delete_access_log_settings);
    ("delete-api", delete_api);
    ("delete-api-mapping", delete_api_mapping);
    ("delete-authorizer", delete_authorizer);
    ("delete-cors-configuration", delete_cors_configuration);
    ("delete-deployment", delete_deployment);
    ("delete-domain-name", delete_domain_name);
    ("delete-integration", delete_integration);
    ("delete-integration-response", delete_integration_response);
    ("delete-model", delete_model);
    ("delete-portal", delete_portal);
    ("delete-portal-product", delete_portal_product);
    ("delete-portal-product-sharing-policy",
      delete_portal_product_sharing_policy);
    ("delete-product-page", delete_product_page);
    ("delete-product-rest-endpoint-page", delete_product_rest_endpoint_page);
    ("delete-route", delete_route);
    ("delete-route-request-parameter", delete_route_request_parameter);
    ("delete-route-response", delete_route_response);
    ("delete-route-settings", delete_route_settings);
    ("delete-routing-rule", delete_routing_rule);
    ("delete-stage", delete_stage);
    ("delete-vpc-link", delete_vpc_link);
    ("export-api", export_api);
    ("disable-portal", disable_portal);
    ("reset-authorizers-cache", reset_authorizers_cache);
    ("get-api", get_api);
    ("get-api-mapping", get_api_mapping);
    ("get-api-mappings", get_api_mappings);
    ("get-apis", get_apis);
    ("get-authorizer", get_authorizer);
    ("get-authorizers", get_authorizers);
    ("get-deployment", get_deployment);
    ("get-deployments", get_deployments);
    ("get-domain-name", get_domain_name);
    ("get-domain-names", get_domain_names);
    ("get-integration", get_integration);
    ("get-integration-response", get_integration_response);
    ("get-integration-responses", get_integration_responses);
    ("get-integrations", get_integrations);
    ("get-model", get_model);
    ("get-model-template", get_model_template);
    ("get-models", get_models);
    ("get-portal", get_portal);
    ("get-portal-product", get_portal_product);
    ("get-portal-product-sharing-policy", get_portal_product_sharing_policy);
    ("get-product-page", get_product_page);
    ("get-product-rest-endpoint-page", get_product_rest_endpoint_page);
    ("get-route", get_route);
    ("get-route-response", get_route_response);
    ("get-route-responses", get_route_responses);
    ("get-routes", get_routes);
    ("get-routing-rule", get_routing_rule);
    ("list-routing-rules", list_routing_rules);
    ("get-stage", get_stage);
    ("get-stages", get_stages);
    ("get-tags", get_tags);
    ("get-vpc-link", get_vpc_link);
    ("get-vpc-links", get_vpc_links);
    ("import-api", import_api);
    ("list-portal-products", list_portal_products);
    ("list-portals", list_portals);
    ("list-product-pages", list_product_pages);
    ("list-product-rest-endpoint-pages", list_product_rest_endpoint_pages);
    ("preview-portal", preview_portal);
    ("publish-portal", publish_portal);
    ("put-portal-product-sharing-policy", put_portal_product_sharing_policy);
    ("put-routing-rule", put_routing_rule);
    ("reimport-api", reimport_api);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-api", update_api);
    ("update-api-mapping", update_api_mapping);
    ("update-authorizer", update_authorizer);
    ("update-deployment", update_deployment);
    ("update-domain-name", update_domain_name);
    ("update-integration", update_integration);
    ("update-integration-response", update_integration_response);
    ("update-model", update_model);
    ("update-portal", update_portal);
    ("update-portal-product", update_portal_product);
    ("update-product-page", update_product_page);
    ("update-product-rest-endpoint-page", update_product_rest_endpoint_page);
    ("update-route", update_route);
    ("update-route-response", update_route_response);
    ("update-stage", update_stage);
    ("update-vpc-link", update_vpc_link)]
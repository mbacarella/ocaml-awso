(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let delete_resource_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and actionType =
         flag "action-type" (optional json_arg)
           ~doc:"JSON PermissionActionType"
       and sourceResourceArn =
         flag "source-resource-arn" (optional string) ~doc:"STRING Arn"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_permission
           (Values.DeleteResourcePermissionInput.make
              ?actionType:(Option.map ~f:Values.PermissionActionType.of_json
                             actionType) ?sourceResourceArn ~resourceArn ())
           (Some Values.DeleteResourcePermissionOutput.to_json)
           (Some Values.DeleteResourcePermissionOutput.error_to_json)])
let deregister_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_application
           (Values.DeregisterApplicationInput.make ~applicationId ())
           (Some Values.DeregisterApplicationOutput.to_json)
           (Some Values.DeregisterApplicationOutput.error_to_json)])
let get_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (optional string) ~doc:"STRING ApplicationId"
       and applicationArn =
         flag "application-arn" (optional string) ~doc:"STRING SsmSapArn"
       and appRegistryArn =
         flag "app-registry-arn" (optional string)
           ~doc:"STRING AppRegistryArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_application
           (Values.GetApplicationInput.make ?applicationId ?applicationArn
              ?appRegistryArn ()) (Some Values.GetApplicationOutput.to_json)
           (Some Values.GetApplicationOutput.error_to_json)])
let get_component =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and componentId =
         flag "component-id" (required string) ~doc:"STRING ComponentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_component
           (Values.GetComponentInput.make ~applicationId ~componentId ())
           (Some Values.GetComponentOutput.to_json)
           (Some Values.GetComponentOutput.error_to_json)])
let get_configuration_check_operation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and operationId =
         flag "operation-id" (required string) ~doc:"STRING OperationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_configuration_check_operation
           (Values.GetConfigurationCheckOperationInput.make ~operationId ())
           (Some Values.GetConfigurationCheckOperationOutput.to_json)
           (Some Values.GetConfigurationCheckOperationOutput.error_to_json)])
let get_database =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (optional string) ~doc:"STRING ApplicationId"
       and componentId =
         flag "component-id" (optional string) ~doc:"STRING ComponentId"
       and databaseId =
         flag "database-id" (optional string) ~doc:"STRING DatabaseId"
       and databaseArn =
         flag "database-arn" (optional string) ~doc:"STRING SsmSapArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_database
           (Values.GetDatabaseInput.make ?applicationId ?componentId
              ?databaseId ?databaseArn ())
           (Some Values.GetDatabaseOutput.to_json)
           (Some Values.GetDatabaseOutput.error_to_json)])
let get_operation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and operationId =
         flag "operation-id" (required string) ~doc:"STRING OperationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_operation (Values.GetOperationInput.make ~operationId ())
           (Some Values.GetOperationOutput.to_json)
           (Some Values.GetOperationOutput.error_to_json)])
let get_resource_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and actionType =
         flag "action-type" (optional json_arg)
           ~doc:"JSON PermissionActionType"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_permission
           (Values.GetResourcePermissionInput.make
              ?actionType:(Option.map ~f:Values.PermissionActionType.of_json
                             actionType) ~resourceArn ())
           (Some Values.GetResourcePermissionOutput.to_json)
           (Some Values.GetResourcePermissionOutput.error_to_json)])
let list_applications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_applications
           (Values.ListApplicationsInput.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.FilterList.of_json filters) ())
           (Some Values.ListApplicationsOutput.to_json)
           (Some Values.ListApplicationsOutput.error_to_json)])
let list_components =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (optional string) ~doc:"STRING ApplicationId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_components
           (Values.ListComponentsInput.make ?applicationId ?nextToken
              ?maxResults ()) (Some Values.ListComponentsOutput.to_json)
           (Some Values.ListComponentsOutput.error_to_json)])
let list_configuration_check_definitions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_configuration_check_definitions
           (Values.ListConfigurationCheckDefinitionsInput.make ?maxResults
              ?nextToken ())
           (Some Values.ListConfigurationCheckDefinitionsOutput.to_json)
           (Some Values.ListConfigurationCheckDefinitionsOutput.error_to_json)])
let list_configuration_check_operations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and listMode =
         flag "list-mode" (optional json_arg)
           ~doc:"JSON ConfigurationCheckOperationListingMode"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_configuration_check_operations
           (Values.ListConfigurationCheckOperationsInput.make
              ?listMode:(Option.map
                           ~f:Values.ConfigurationCheckOperationListingMode.of_json
                           listMode) ?maxResults ?nextToken
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ~applicationId ())
           (Some Values.ListConfigurationCheckOperationsOutput.to_json)
           (Some Values.ListConfigurationCheckOperationsOutput.error_to_json)])
let list_databases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (optional string) ~doc:"STRING ApplicationId"
       and componentId =
         flag "component-id" (optional string) ~doc:"STRING ComponentId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_databases
           (Values.ListDatabasesInput.make ?applicationId ?componentId
              ?nextToken ?maxResults ())
           (Some Values.ListDatabasesOutput.to_json)
           (Some Values.ListDatabasesOutput.error_to_json)])
let list_operation_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and operationId =
         flag "operation-id" (required string) ~doc:"STRING OperationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_operation_events
           (Values.ListOperationEventsInput.make ?maxResults ?nextToken
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ~operationId ())
           (Some Values.ListOperationEventsOutput.to_json)
           (Some Values.ListOperationEventsOutput.error_to_json)])
let list_operations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_operations
           (Values.ListOperationsInput.make ?maxResults ?nextToken
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ~applicationId ()) (Some Values.ListOperationsOutput.to_json)
           (Some Values.ListOperationsOutput.error_to_json)])
let list_sub_check_results =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and operationId =
         flag "operation-id" (required string) ~doc:"STRING OperationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_sub_check_results
           (Values.ListSubCheckResultsInput.make ?maxResults ?nextToken
              ~operationId ())
           (Some Values.ListSubCheckResultsOutput.to_json)
           (Some Values.ListSubCheckResultsOutput.error_to_json)])
let list_sub_check_rule_results =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and subCheckResultId =
         flag "sub-check-result-id" (required string)
           ~doc:"STRING SubCheckResultId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_sub_check_rule_results
           (Values.ListSubCheckRuleResultsInput.make ?maxResults ?nextToken
              ~subCheckResultId ())
           (Some Values.ListSubCheckRuleResultsOutput.to_json)
           (Some Values.ListSubCheckRuleResultsOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING SsmSapArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_resource_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and actionType =
         flag "action-type" (required json_arg)
           ~doc:"JSON PermissionActionType"
       and sourceResourceArn =
         flag "source-resource-arn" (required string) ~doc:"STRING Arn"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_resource_permission
           (Values.PutResourcePermissionInput.make
              ~actionType:(Values.PermissionActionType.of_json actionType)
              ~sourceResourceArn ~resourceArn ())
           (Some Values.PutResourcePermissionOutput.to_json)
           (Some Values.PutResourcePermissionOutput.error_to_json)])
let register_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sapInstanceNumber =
         flag "sap-instance-number" (optional string)
           ~doc:"STRING SAPInstanceNumber"
       and sid = flag "sid" (optional string) ~doc:"STRING SID"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and credentials =
         flag "credentials" (optional json_arg)
           ~doc:"JSON ApplicationCredentialList"
       and databaseArn =
         flag "database-arn" (optional string) ~doc:"STRING SsmSapArn"
       and componentsInfo =
         flag "components-info" (optional json_arg)
           ~doc:"JSON ComponentInfoList"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and applicationType =
         flag "application-type" (required json_arg)
           ~doc:"JSON ApplicationType"
       and instances =
         flag "instances" (required json_arg) ~doc:"JSON InstanceList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_application
           (Values.RegisterApplicationInput.make ?sapInstanceNumber ?sid
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?credentials:(Option.map
                              ~f:Values.ApplicationCredentialList.of_json
                              credentials) ?databaseArn
              ?componentsInfo:(Option.map ~f:Values.ComponentInfoList.of_json
                                 componentsInfo) ~applicationId
              ~applicationType:(Values.ApplicationType.of_json
                                  applicationType)
              ~instances:(Values.InstanceList.of_json instances) ())
           (Some Values.RegisterApplicationOutput.to_json)
           (Some Values.RegisterApplicationOutput.error_to_json)])
let start_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_application
           (Values.StartApplicationInput.make ~applicationId ())
           (Some Values.StartApplicationOutput.to_json)
           (Some Values.StartApplicationOutput.error_to_json)])
let start_application_refresh =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_application_refresh
           (Values.StartApplicationRefreshInput.make ~applicationId ())
           (Some Values.StartApplicationRefreshOutput.to_json)
           (Some Values.StartApplicationRefreshOutput.error_to_json)])
let start_configuration_checks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationCheckIds =
         flag "configuration-check-ids" (optional json_arg)
           ~doc:"JSON ConfigurationCheckTypeList"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_configuration_checks
           (Values.StartConfigurationChecksInput.make
              ?configurationCheckIds:(Option.map
                                        ~f:Values.ConfigurationCheckTypeList.of_json
                                        configurationCheckIds) ~applicationId
              ()) (Some Values.StartConfigurationChecksOutput.to_json)
           (Some Values.StartConfigurationChecksOutput.error_to_json)])
let stop_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stopConnectedEntity =
         flag "stop-connected-entity" (optional json_arg)
           ~doc:"JSON ConnectedEntityType"
       and includeEc2InstanceShutdown =
         flag "include-ec2-instance-shutdown" (optional bool)
           ~doc:"BOOL Boolean"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_application
           (Values.StopApplicationInput.make
              ?stopConnectedEntity:(Option.map
                                      ~f:Values.ConnectedEntityType.of_json
                                      stopConnectedEntity)
              ?includeEc2InstanceShutdown ~applicationId ())
           (Some Values.StopApplicationOutput.to_json)
           (Some Values.StopApplicationOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING SsmSapArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING SsmSapArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_application_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and credentialsToAddOrUpdate =
         flag "credentials-to-add-or-update" (optional json_arg)
           ~doc:"JSON ApplicationCredentialList"
       and credentialsToRemove =
         flag "credentials-to-remove" (optional json_arg)
           ~doc:"JSON ApplicationCredentialList"
       and backint =
         flag "backint" (optional json_arg) ~doc:"JSON BackintConfig"
       and databaseArn =
         flag "database-arn" (optional string) ~doc:"STRING SsmSapArn"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_application_settings
           (Values.UpdateApplicationSettingsInput.make
              ?credentialsToAddOrUpdate:(Option.map
                                           ~f:Values.ApplicationCredentialList.of_json
                                           credentialsToAddOrUpdate)
              ?credentialsToRemove:(Option.map
                                      ~f:Values.ApplicationCredentialList.of_json
                                      credentialsToRemove)
              ?backint:(Option.map ~f:Values.BackintConfig.of_json backint)
              ?databaseArn ~applicationId ())
           (Some Values.UpdateApplicationSettingsOutput.to_json)
           (Some Values.UpdateApplicationSettingsOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("delete-resource-permission", delete_resource_permission);
    ("deregister-application", deregister_application);
    ("get-application", get_application);
    ("get-component", get_component);
    ("get-configuration-check-operation", get_configuration_check_operation);
    ("get-database", get_database);
    ("get-operation", get_operation);
    ("get-resource-permission", get_resource_permission);
    ("list-applications", list_applications);
    ("list-components", list_components);
    ("list-configuration-check-definitions",
      list_configuration_check_definitions);
    ("list-configuration-check-operations",
      list_configuration_check_operations);
    ("list-databases", list_databases);
    ("list-operation-events", list_operation_events);
    ("list-operations", list_operations);
    ("list-sub-check-results", list_sub_check_results);
    ("list-sub-check-rule-results", list_sub_check_rule_results);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-resource-permission", put_resource_permission);
    ("register-application", register_application);
    ("start-application", start_application);
    ("start-application-refresh", start_application_refresh);
    ("start-configuration-checks", start_configuration_checks);
    ("stop-application", stop_application);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-application-settings", update_application_settings)]
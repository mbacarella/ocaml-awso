(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | DeleteResourcePermission: (DeleteResourcePermissionInput.t,
  DeleteResourcePermissionOutput.t, DeleteResourcePermissionOutput.error) t 
  | DeregisterApplication: (DeregisterApplicationInput.t,
  DeregisterApplicationOutput.t, DeregisterApplicationOutput.error) t 
  | GetApplication: (GetApplicationInput.t, GetApplicationOutput.t,
  GetApplicationOutput.error) t 
  | GetComponent: (GetComponentInput.t, GetComponentOutput.t,
  GetComponentOutput.error) t 
  | GetConfigurationCheckOperation: (GetConfigurationCheckOperationInput.t,
  GetConfigurationCheckOperationOutput.t,
  GetConfigurationCheckOperationOutput.error) t 
  | GetDatabase: (GetDatabaseInput.t, GetDatabaseOutput.t,
  GetDatabaseOutput.error) t 
  | GetOperation: (GetOperationInput.t, GetOperationOutput.t,
  GetOperationOutput.error) t 
  | GetResourcePermission: (GetResourcePermissionInput.t,
  GetResourcePermissionOutput.t, GetResourcePermissionOutput.error) t 
  | ListApplications: (ListApplicationsInput.t, ListApplicationsOutput.t,
  ListApplicationsOutput.error) t 
  | ListComponents: (ListComponentsInput.t, ListComponentsOutput.t,
  ListComponentsOutput.error) t 
  | ListConfigurationCheckDefinitions:
  (ListConfigurationCheckDefinitionsInput.t,
  ListConfigurationCheckDefinitionsOutput.t,
  ListConfigurationCheckDefinitionsOutput.error) t 
  | ListConfigurationCheckOperations:
  (ListConfigurationCheckOperationsInput.t,
  ListConfigurationCheckOperationsOutput.t,
  ListConfigurationCheckOperationsOutput.error) t 
  | ListDatabases: (ListDatabasesInput.t, ListDatabasesOutput.t,
  ListDatabasesOutput.error) t 
  | ListOperationEvents: (ListOperationEventsInput.t,
  ListOperationEventsOutput.t, ListOperationEventsOutput.error) t 
  | ListOperations: (ListOperationsInput.t, ListOperationsOutput.t,
  ListOperationsOutput.error) t 
  | ListSubCheckResults: (ListSubCheckResultsInput.t,
  ListSubCheckResultsOutput.t, ListSubCheckResultsOutput.error) t 
  | ListSubCheckRuleResults: (ListSubCheckRuleResultsInput.t,
  ListSubCheckRuleResultsOutput.t, ListSubCheckRuleResultsOutput.error) t 
  | ListTagsForResource: (ListTagsForResourceRequest.t,
  ListTagsForResourceResponse.t, ListTagsForResourceResponse.error) t 
  | PutResourcePermission: (PutResourcePermissionInput.t,
  PutResourcePermissionOutput.t, PutResourcePermissionOutput.error) t 
  | RegisterApplication: (RegisterApplicationInput.t,
  RegisterApplicationOutput.t, RegisterApplicationOutput.error) t 
  | StartApplication: (StartApplicationInput.t, StartApplicationOutput.t,
  StartApplicationOutput.error) t 
  | StartApplicationRefresh: (StartApplicationRefreshInput.t,
  StartApplicationRefreshOutput.t, StartApplicationRefreshOutput.error) t 
  | StartConfigurationChecks: (StartConfigurationChecksInput.t,
  StartConfigurationChecksOutput.t, StartConfigurationChecksOutput.error) t 
  | StopApplication: (StopApplicationInput.t, StopApplicationOutput.t,
  StopApplicationOutput.error) t 
  | TagResource: (TagResourceRequest.t, TagResourceResponse.t,
  TagResourceResponse.error) t 
  | UntagResource: (UntagResourceRequest.t, UntagResourceResponse.t,
  UntagResourceResponse.error) t 
  | UpdateApplicationSettings: (UpdateApplicationSettingsInput.t,
  UpdateApplicationSettingsOutput.t, UpdateApplicationSettingsOutput.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | DeleteResourcePermission -> `POST
  | DeregisterApplication -> `POST
  | GetApplication -> `POST
  | GetComponent -> `POST
  | GetConfigurationCheckOperation -> `POST
  | GetDatabase -> `POST
  | GetOperation -> `POST
  | GetResourcePermission -> `POST
  | ListApplications -> `POST
  | ListComponents -> `POST
  | ListConfigurationCheckDefinitions -> `POST
  | ListConfigurationCheckOperations -> `POST
  | ListDatabases -> `POST
  | ListOperationEvents -> `POST
  | ListOperations -> `POST
  | ListSubCheckResults -> `POST
  | ListSubCheckRuleResults -> `POST
  | ListTagsForResource -> `GET
  | PutResourcePermission -> `POST
  | RegisterApplication -> `POST
  | StartApplication -> `POST
  | StartApplicationRefresh -> `POST
  | StartConfigurationChecks -> `POST
  | StopApplication -> `POST
  | TagResource -> `POST
  | UntagResource -> `DELETE
  | UpdateApplicationSettings -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | DeleteResourcePermission ->
          (Format.kasprintf Uri.of_string) "/delete-resource-permission"
      | DeregisterApplication ->
          (Format.kasprintf Uri.of_string) "/deregister-application"
      | GetApplication -> (Format.kasprintf Uri.of_string) "/get-application"
      | GetComponent -> (Format.kasprintf Uri.of_string) "/get-component"
      | GetConfigurationCheckOperation ->
          (Format.kasprintf Uri.of_string)
            "/get-configuration-check-operation"
      | GetDatabase -> (Format.kasprintf Uri.of_string) "/get-database"
      | GetOperation -> (Format.kasprintf Uri.of_string) "/get-operation"
      | GetResourcePermission ->
          (Format.kasprintf Uri.of_string) "/get-resource-permission"
      | ListApplications ->
          (Format.kasprintf Uri.of_string) "/list-applications"
      | ListComponents -> (Format.kasprintf Uri.of_string) "/list-components"
      | ListConfigurationCheckDefinitions ->
          (Format.kasprintf Uri.of_string)
            "/list-configuration-check-definitions"
      | ListConfigurationCheckOperations ->
          (Format.kasprintf Uri.of_string)
            "/list-configuration-check-operations"
      | ListDatabases -> (Format.kasprintf Uri.of_string) "/list-databases"
      | ListOperationEvents ->
          (Format.kasprintf Uri.of_string) "/list-operation-events"
      | ListOperations -> (Format.kasprintf Uri.of_string) "/list-operations"
      | ListSubCheckResults ->
          (Format.kasprintf Uri.of_string) "/list-sub-check-results"
      | ListSubCheckRuleResults ->
          (Format.kasprintf Uri.of_string) "/list-sub-check-rule-results"
      | ListTagsForResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (SsmSapArn.to_header x.ListTagsForResourceRequest.resourceArn)
      | PutResourcePermission ->
          (Format.kasprintf Uri.of_string) "/put-resource-permission"
      | RegisterApplication ->
          (Format.kasprintf Uri.of_string) "/register-application"
      | StartApplication ->
          (Format.kasprintf Uri.of_string) "/start-application"
      | StartApplicationRefresh ->
          (Format.kasprintf Uri.of_string) "/start-application-refresh"
      | StartConfigurationChecks ->
          (Format.kasprintf Uri.of_string) "/start-configuration-checks"
      | StopApplication ->
          (Format.kasprintf Uri.of_string) "/stop-application"
      | TagResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (SsmSapArn.to_header x.TagResourceRequest.resourceArn)
      | UntagResource ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/tags/%s"
               (SsmSapArn.to_header x.UntagResourceRequest.resourceArn))
            (List.filter_opt
               [Some ("tagKeys", (TagKeyList.to_header x.tagKeys))])
      | UpdateApplicationSettings ->
          (Format.kasprintf Uri.of_string) "/update-application-settings")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | DeleteResourcePermission ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.DeleteResourcePermissionInput.actionType
                         ~f:(fun x ->
                               ("ActionType",
                                 (PermissionActionType.to_value x)));
                      Option.map
                        req.DeleteResourcePermissionInput.sourceResourceArn
                        ~f:(fun x -> ("SourceResourceArn", (Arn.to_value x)));
                      Some
                        ("ResourceArn",
                          (Arn.to_value
                             req.DeleteResourcePermissionInput.resourceArn))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeregisterApplication ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("ApplicationId",
                           (ApplicationId.to_value
                              req.DeregisterApplicationInput.applicationId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetApplication ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.GetApplicationInput.applicationId
                         ~f:(fun x ->
                               ("ApplicationId", (ApplicationId.to_value x)));
                      Option.map req.GetApplicationInput.applicationArn
                        ~f:(fun x ->
                              ("ApplicationArn", (SsmSapArn.to_value x)));
                      Option.map req.GetApplicationInput.appRegistryArn
                        ~f:(fun x ->
                              ("AppRegistryArn", (AppRegistryArn.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetComponent ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("ApplicationId",
                           (ApplicationId.to_value
                              req.GetComponentInput.applicationId));
                      Some
                        ("ComponentId",
                          (ComponentId.to_value
                             req.GetComponentInput.componentId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetConfigurationCheckOperation ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("OperationId",
                           (OperationId.to_value
                              req.GetConfigurationCheckOperationInput.operationId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetDatabase ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.GetDatabaseInput.applicationId
                         ~f:(fun x ->
                               ("ApplicationId", (ApplicationId.to_value x)));
                      Option.map req.GetDatabaseInput.componentId
                        ~f:(fun x ->
                              ("ComponentId", (ComponentId.to_value x)));
                      Option.map req.GetDatabaseInput.databaseId
                        ~f:(fun x -> ("DatabaseId", (DatabaseId.to_value x)));
                      Option.map req.GetDatabaseInput.databaseArn
                        ~f:(fun x -> ("DatabaseArn", (SsmSapArn.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetOperation ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("OperationId",
                           (OperationId.to_value
                              req.GetOperationInput.operationId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetResourcePermission ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.GetResourcePermissionInput.actionType
                         ~f:(fun x ->
                               ("ActionType",
                                 (PermissionActionType.to_value x)));
                      Some
                        ("ResourceArn",
                          (Arn.to_value
                             req.GetResourcePermissionInput.resourceArn))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListApplications ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListApplicationsInput.nextToken
                         ~f:(fun x -> ("NextToken", (NextToken.to_value x)));
                      Option.map req.ListApplicationsInput.maxResults
                        ~f:(fun x -> ("MaxResults", (MaxResults.to_value x)));
                      Option.map req.ListApplicationsInput.filters
                        ~f:(fun x -> ("Filters", (FilterList.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListComponents ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListComponentsInput.applicationId
                         ~f:(fun x ->
                               ("ApplicationId", (ApplicationId.to_value x)));
                      Option.map req.ListComponentsInput.nextToken
                        ~f:(fun x -> ("NextToken", (NextToken.to_value x)));
                      Option.map req.ListComponentsInput.maxResults
                        ~f:(fun x -> ("MaxResults", (MaxResults.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListConfigurationCheckDefinitions ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.ListConfigurationCheckDefinitionsInput.maxResults
                         ~f:(fun x -> ("MaxResults", (MaxResults.to_value x)));
                      Option.map
                        req.ListConfigurationCheckDefinitionsInput.nextToken
                        ~f:(fun x -> ("NextToken", (NextToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListConfigurationCheckOperations ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("ApplicationId",
                           (ApplicationId.to_value
                              req.ListConfigurationCheckOperationsInput.applicationId));
                      Option.map
                        req.ListConfigurationCheckOperationsInput.listMode
                        ~f:(fun x ->
                              ("ListMode",
                                (ConfigurationCheckOperationListingMode.to_value
                                   x)));
                      Option.map
                        req.ListConfigurationCheckOperationsInput.maxResults
                        ~f:(fun x -> ("MaxResults", (MaxResults.to_value x)));
                      Option.map
                        req.ListConfigurationCheckOperationsInput.nextToken
                        ~f:(fun x -> ("NextToken", (NextToken.to_value x)));
                      Option.map
                        req.ListConfigurationCheckOperationsInput.filters
                        ~f:(fun x -> ("Filters", (FilterList.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListDatabases ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListDatabasesInput.applicationId
                         ~f:(fun x ->
                               ("ApplicationId", (ApplicationId.to_value x)));
                      Option.map req.ListDatabasesInput.componentId
                        ~f:(fun x ->
                              ("ComponentId", (ComponentId.to_value x)));
                      Option.map req.ListDatabasesInput.nextToken
                        ~f:(fun x -> ("NextToken", (NextToken.to_value x)));
                      Option.map req.ListDatabasesInput.maxResults
                        ~f:(fun x -> ("MaxResults", (MaxResults.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListOperationEvents ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("OperationId",
                           (OperationId.to_value
                              req.ListOperationEventsInput.operationId));
                      Option.map req.ListOperationEventsInput.maxResults
                        ~f:(fun x -> ("MaxResults", (MaxResults.to_value x)));
                      Option.map req.ListOperationEventsInput.nextToken
                        ~f:(fun x -> ("NextToken", (NextToken.to_value x)));
                      Option.map req.ListOperationEventsInput.filters
                        ~f:(fun x -> ("Filters", (FilterList.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListOperations ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("ApplicationId",
                           (ApplicationId.to_value
                              req.ListOperationsInput.applicationId));
                      Option.map req.ListOperationsInput.maxResults
                        ~f:(fun x -> ("MaxResults", (MaxResults.to_value x)));
                      Option.map req.ListOperationsInput.nextToken
                        ~f:(fun x -> ("NextToken", (NextToken.to_value x)));
                      Option.map req.ListOperationsInput.filters
                        ~f:(fun x -> ("Filters", (FilterList.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListSubCheckResults ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("OperationId",
                           (OperationId.to_value
                              req.ListSubCheckResultsInput.operationId));
                      Option.map req.ListSubCheckResultsInput.maxResults
                        ~f:(fun x -> ("MaxResults", (MaxResults.to_value x)));
                      Option.map req.ListSubCheckResultsInput.nextToken
                        ~f:(fun x -> ("NextToken", (NextToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListSubCheckRuleResults ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("SubCheckResultId",
                           (SubCheckResultId.to_value
                              req.ListSubCheckRuleResultsInput.subCheckResultId));
                      Option.map req.ListSubCheckRuleResultsInput.maxResults
                        ~f:(fun x -> ("MaxResults", (MaxResults.to_value x)));
                      Option.map req.ListSubCheckRuleResultsInput.nextToken
                        ~f:(fun x -> ("NextToken", (NextToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTagsForResource ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | PutResourcePermission ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("ActionType",
                           (PermissionActionType.to_value
                              req.PutResourcePermissionInput.actionType));
                      Some
                        ("SourceResourceArn",
                          (Arn.to_value
                             req.PutResourcePermissionInput.sourceResourceArn));
                      Some
                        ("ResourceArn",
                          (Arn.to_value
                             req.PutResourcePermissionInput.resourceArn))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | RegisterApplication ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("ApplicationId",
                           (ApplicationId.to_value
                              req.RegisterApplicationInput.applicationId));
                      Some
                        ("ApplicationType",
                          (ApplicationType.to_value
                             req.RegisterApplicationInput.applicationType));
                      Some
                        ("Instances",
                          (InstanceList.to_value
                             req.RegisterApplicationInput.instances));
                      Option.map
                        req.RegisterApplicationInput.sapInstanceNumber
                        ~f:(fun x ->
                              ("SapInstanceNumber",
                                (SAPInstanceNumber.to_value x)));
                      Option.map req.RegisterApplicationInput.sid
                        ~f:(fun x -> ("Sid", (SID.to_value x)));
                      Option.map req.RegisterApplicationInput.tags
                        ~f:(fun x -> ("Tags", (TagMap.to_value x)));
                      Option.map req.RegisterApplicationInput.credentials
                        ~f:(fun x ->
                              ("Credentials",
                                (ApplicationCredentialList.to_value x)));
                      Option.map req.RegisterApplicationInput.databaseArn
                        ~f:(fun x -> ("DatabaseArn", (SsmSapArn.to_value x)));
                      Option.map req.RegisterApplicationInput.componentsInfo
                        ~f:(fun x ->
                              ("ComponentsInfo",
                                (ComponentInfoList.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StartApplication ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("ApplicationId",
                           (ApplicationId.to_value
                              req.StartApplicationInput.applicationId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StartApplicationRefresh ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("ApplicationId",
                           (ApplicationId.to_value
                              req.StartApplicationRefreshInput.applicationId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StartConfigurationChecks ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("ApplicationId",
                           (ApplicationId.to_value
                              req.StartConfigurationChecksInput.applicationId));
                      Option.map
                        req.StartConfigurationChecksInput.configurationCheckIds
                        ~f:(fun x ->
                              ("ConfigurationCheckIds",
                                (ConfigurationCheckTypeList.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StopApplication ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("ApplicationId",
                           (ApplicationId.to_value
                              req.StopApplicationInput.applicationId));
                      Option.map req.StopApplicationInput.stopConnectedEntity
                        ~f:(fun x ->
                              ("StopConnectedEntity",
                                (ConnectedEntityType.to_value x)));
                      Option.map
                        req.StopApplicationInput.includeEc2InstanceShutdown
                        ~f:(fun x ->
                              ("IncludeEc2InstanceShutdown",
                                (Boolean.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | TagResource ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("tags",
                           (TagMap.to_value req.TagResourceRequest.tags))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UntagResource -> Awso.Http.Request.make (method_of_endpoint endp)
  | UpdateApplicationSettings ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("ApplicationId",
                           (ApplicationId.to_value
                              req.UpdateApplicationSettingsInput.applicationId));
                      Option.map
                        req.UpdateApplicationSettingsInput.credentialsToAddOrUpdate
                        ~f:(fun x ->
                              ("CredentialsToAddOrUpdate",
                                (ApplicationCredentialList.to_value x)));
                      Option.map
                        req.UpdateApplicationSettingsInput.credentialsToRemove
                        ~f:(fun x ->
                              ("CredentialsToRemove",
                                (ApplicationCredentialList.to_value x)));
                      Option.map req.UpdateApplicationSettingsInput.backint
                        ~f:(fun x -> ("Backint", (BackintConfig.to_value x)));
                      Option.map
                        req.UpdateApplicationSettingsInput.databaseArn
                        ~f:(fun x -> ("DatabaseArn", (SsmSapArn.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | DeleteResourcePermission ->
      if is_success
      then
        Ok (DeleteResourcePermissionOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some DeleteResourcePermissionOutput.error_of_json))
  | DeregisterApplication ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DeregisterApplicationOutput.of_header_and_body (headers, ()))
      else
        Error
          (parse_aws_error (Some DeregisterApplicationOutput.error_of_json))
  | GetApplication ->
      if is_success
      then Ok (GetApplicationOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetApplicationOutput.error_of_json))
  | GetComponent ->
      if is_success
      then Ok (GetComponentOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetComponentOutput.error_of_json))
  | GetConfigurationCheckOperation ->
      if is_success
      then
        Ok
          (GetConfigurationCheckOperationOutput.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some GetConfigurationCheckOperationOutput.error_of_json))
  | GetDatabase ->
      if is_success
      then Ok (GetDatabaseOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetDatabaseOutput.error_of_json))
  | GetOperation ->
      if is_success
      then Ok (GetOperationOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetOperationOutput.error_of_json))
  | GetResourcePermission ->
      if is_success
      then Ok (GetResourcePermissionOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some GetResourcePermissionOutput.error_of_json))
  | ListApplications ->
      if is_success
      then Ok (ListApplicationsOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some ListApplicationsOutput.error_of_json))
  | ListComponents ->
      if is_success
      then Ok (ListComponentsOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListComponentsOutput.error_of_json))
  | ListConfigurationCheckDefinitions ->
      if is_success
      then
        Ok
          (ListConfigurationCheckDefinitionsOutput.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some ListConfigurationCheckDefinitionsOutput.error_of_json))
  | ListConfigurationCheckOperations ->
      if is_success
      then
        Ok
          (ListConfigurationCheckOperationsOutput.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some ListConfigurationCheckOperationsOutput.error_of_json))
  | ListDatabases ->
      if is_success
      then Ok (ListDatabasesOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListDatabasesOutput.error_of_json))
  | ListOperationEvents ->
      if is_success
      then Ok (ListOperationEventsOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListOperationEventsOutput.error_of_json))
  | ListOperations ->
      if is_success
      then Ok (ListOperationsOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListOperationsOutput.error_of_json))
  | ListSubCheckResults ->
      if is_success
      then Ok (ListSubCheckResultsOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListSubCheckResultsOutput.error_of_json))
  | ListSubCheckRuleResults ->
      if is_success
      then Ok (ListSubCheckRuleResultsOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListSubCheckRuleResultsOutput.error_of_json))
  | ListTagsForResource ->
      if is_success
      then Ok (ListTagsForResourceResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListTagsForResourceResponse.error_of_json))
  | PutResourcePermission ->
      if is_success
      then Ok (PutResourcePermissionOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some PutResourcePermissionOutput.error_of_json))
  | RegisterApplication ->
      if is_success
      then Ok (RegisterApplicationOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some RegisterApplicationOutput.error_of_json))
  | StartApplication ->
      if is_success
      then Ok (StartApplicationOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some StartApplicationOutput.error_of_json))
  | StartApplicationRefresh ->
      if is_success
      then Ok (StartApplicationRefreshOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some StartApplicationRefreshOutput.error_of_json))
  | StartConfigurationChecks ->
      if is_success
      then
        Ok (StartConfigurationChecksOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some StartConfigurationChecksOutput.error_of_json))
  | StopApplication ->
      if is_success
      then Ok (StopApplicationOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some StopApplicationOutput.error_of_json))
  | TagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (TagResourceResponse.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some TagResourceResponse.error_of_json))
  | UntagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (UntagResourceResponse.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some UntagResourceResponse.error_of_json))
  | UpdateApplicationSettings ->
      if is_success
      then
        Ok (UpdateApplicationSettingsOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some UpdateApplicationSettingsOutput.error_of_json))
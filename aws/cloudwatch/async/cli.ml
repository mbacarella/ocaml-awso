(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let delete_alarm_mute_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and alarmMuteRuleName =
         flag "alarm-mute-rule-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_alarm_mute_rule
           (Values.DeleteAlarmMuteRuleInput.make ~alarmMuteRuleName ()) None
           None])
let delete_alarms =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and alarmNames =
         flag "alarm-names" (required json_arg) ~doc:"JSON AlarmNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_alarms
           (Values.DeleteAlarmsInput.make
              ~alarmNames:(Values.AlarmNames.of_json alarmNames) ()) None
           None])
let delete_anomaly_detector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namespace =
         flag "namespace" (optional string) ~doc:"STRING Namespace"
       and metricName =
         flag "metric-name" (optional string) ~doc:"STRING MetricName"
       and dimensions =
         flag "dimensions" (optional json_arg) ~doc:"JSON Dimensions"
       and stat =
         flag "stat" (optional string)
           ~doc:"STRING AnomalyDetectorMetricStat"
       and singleMetricAnomalyDetector =
         flag "single-metric-anomaly-detector" (optional json_arg)
           ~doc:"JSON SingleMetricAnomalyDetector"
       and metricMathAnomalyDetector =
         flag "metric-math-anomaly-detector" (optional json_arg)
           ~doc:"JSON MetricMathAnomalyDetector" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_anomaly_detector
           (Values.DeleteAnomalyDetectorInput.make ?namespace ?metricName
              ?dimensions:(Option.map ~f:Values.Dimensions.of_json dimensions)
              ?stat
              ?singleMetricAnomalyDetector:(Option.map
                                              ~f:Values.SingleMetricAnomalyDetector.of_json
                                              singleMetricAnomalyDetector)
              ?metricMathAnomalyDetector:(Option.map
                                            ~f:Values.MetricMathAnomalyDetector.of_json
                                            metricMathAnomalyDetector) ())
           (Some Values.DeleteAnomalyDetectorOutput.to_json)
           (Some Values.DeleteAnomalyDetectorOutput.error_to_json)])
let delete_dashboards =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dashboardNames =
         flag "dashboard-names" (required json_arg)
           ~doc:"JSON DashboardNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_dashboards
           (Values.DeleteDashboardsInput.make
              ~dashboardNames:(Values.DashboardNames.of_json dashboardNames)
              ()) (Some Values.DeleteDashboardsOutput.to_json)
           (Some Values.DeleteDashboardsOutput.error_to_json)])
let delete_insight_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleNames =
         flag "rule-names" (required json_arg) ~doc:"JSON InsightRuleNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_insight_rules
           (Values.DeleteInsightRulesInput.make
              ~ruleNames:(Values.InsightRuleNames.of_json ruleNames) ())
           (Some Values.DeleteInsightRulesOutput.to_json)
           (Some Values.DeleteInsightRulesOutput.error_to_json)])
let delete_metric_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING MetricStreamName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_metric_stream
           (Values.DeleteMetricStreamInput.make ~name ())
           (Some Values.DeleteMetricStreamOutput.to_json)
           (Some Values.DeleteMetricStreamOutput.error_to_json)])
let describe_alarm_contributors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and alarmName =
         flag "alarm-name" (required string) ~doc:"STRING AlarmName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_alarm_contributors
           (Values.DescribeAlarmContributorsInput.make ?nextToken ~alarmName
              ()) (Some Values.DescribeAlarmContributorsOutput.to_json)
           (Some Values.DescribeAlarmContributorsOutput.error_to_json)])
let describe_alarm_history =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and alarmName =
         flag "alarm-name" (optional string) ~doc:"STRING AlarmName"
       and alarmContributorId =
         flag "alarm-contributor-id" (optional string)
           ~doc:"STRING ContributorId"
       and alarmTypes =
         flag "alarm-types" (optional json_arg) ~doc:"JSON AlarmTypes"
       and historyItemType =
         flag "history-item-type" (optional json_arg)
           ~doc:"JSON HistoryItemType"
       and startDate =
         flag "start-date" (optional json_arg) ~doc:"JSON Timestamp"
       and endDate =
         flag "end-date" (optional json_arg) ~doc:"JSON Timestamp"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT MaxRecords"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and scanBy = flag "scan-by" (optional json_arg) ~doc:"JSON ScanBy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_alarm_history
           (Values.DescribeAlarmHistoryInput.make ?alarmName
              ?alarmContributorId
              ?alarmTypes:(Option.map ~f:Values.AlarmTypes.of_json alarmTypes)
              ?historyItemType:(Option.map ~f:Values.HistoryItemType.of_json
                                  historyItemType)
              ?startDate:(Option.map ~f:Values.Timestamp.of_json startDate)
              ?endDate:(Option.map ~f:Values.Timestamp.of_json endDate)
              ?maxRecords ?nextToken
              ?scanBy:(Option.map ~f:Values.ScanBy.of_json scanBy) ())
           (Some Values.DescribeAlarmHistoryOutput.to_json)
           (Some Values.DescribeAlarmHistoryOutput.error_to_json)])
let describe_alarms =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and alarmNames =
         flag "alarm-names" (optional json_arg) ~doc:"JSON AlarmNames"
       and alarmNamePrefix =
         flag "alarm-name-prefix" (optional string)
           ~doc:"STRING AlarmNamePrefix"
       and alarmTypes =
         flag "alarm-types" (optional json_arg) ~doc:"JSON AlarmTypes"
       and childrenOfAlarmName =
         flag "children-of-alarm-name" (optional string)
           ~doc:"STRING AlarmName"
       and parentsOfAlarmName =
         flag "parents-of-alarm-name" (optional string)
           ~doc:"STRING AlarmName"
       and stateValue =
         flag "state-value" (optional json_arg) ~doc:"JSON StateValue"
       and actionPrefix =
         flag "action-prefix" (optional string) ~doc:"STRING ActionPrefix"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT MaxRecords"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_alarms
           (Values.DescribeAlarmsInput.make
              ?alarmNames:(Option.map ~f:Values.AlarmNames.of_json alarmNames)
              ?alarmNamePrefix
              ?alarmTypes:(Option.map ~f:Values.AlarmTypes.of_json alarmTypes)
              ?childrenOfAlarmName ?parentsOfAlarmName
              ?stateValue:(Option.map ~f:Values.StateValue.of_json stateValue)
              ?actionPrefix ?maxRecords ?nextToken ())
           (Some Values.DescribeAlarmsOutput.to_json)
           (Some Values.DescribeAlarmsOutput.error_to_json)])
let describe_alarms_for_metric =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and statistic =
         flag "statistic" (optional json_arg) ~doc:"JSON Statistic"
       and extendedStatistic =
         flag "extended-statistic" (optional string)
           ~doc:"STRING ExtendedStatistic"
       and dimensions =
         flag "dimensions" (optional json_arg) ~doc:"JSON Dimensions"
       and period = flag "period" (optional int) ~doc:"INT Period"
       and unit = flag "unit" (optional json_arg) ~doc:"JSON StandardUnit"
       and metricName =
         flag "metric-name" (required string) ~doc:"STRING MetricName"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_alarms_for_metric
           (Values.DescribeAlarmsForMetricInput.make
              ?statistic:(Option.map ~f:Values.Statistic.of_json statistic)
              ?extendedStatistic
              ?dimensions:(Option.map ~f:Values.Dimensions.of_json dimensions)
              ?period ?unit:(Option.map ~f:Values.StandardUnit.of_json unit)
              ~metricName ~namespace ())
           (Some Values.DescribeAlarmsForMetricOutput.to_json)
           (Some Values.DescribeAlarmsForMetricOutput.error_to_json)])
let describe_anomaly_detectors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxReturnedResultsCount"
       and namespace =
         flag "namespace" (optional string) ~doc:"STRING Namespace"
       and metricName =
         flag "metric-name" (optional string) ~doc:"STRING MetricName"
       and dimensions =
         flag "dimensions" (optional json_arg) ~doc:"JSON Dimensions"
       and anomalyDetectorTypes =
         flag "anomaly-detector-types" (optional json_arg)
           ~doc:"JSON AnomalyDetectorTypes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_anomaly_detectors
           (Values.DescribeAnomalyDetectorsInput.make ?nextToken ?maxResults
              ?namespace ?metricName
              ?dimensions:(Option.map ~f:Values.Dimensions.of_json dimensions)
              ?anomalyDetectorTypes:(Option.map
                                       ~f:Values.AnomalyDetectorTypes.of_json
                                       anomalyDetectorTypes) ())
           (Some Values.DescribeAnomalyDetectorsOutput.to_json)
           (Some Values.DescribeAnomalyDetectorsOutput.error_to_json)])
let describe_insight_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT InsightRuleMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_insight_rules
           (Values.DescribeInsightRulesInput.make ?nextToken ?maxResults ())
           (Some Values.DescribeInsightRulesOutput.to_json)
           (Some Values.DescribeInsightRulesOutput.error_to_json)])
let disable_alarm_actions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and alarmNames =
         flag "alarm-names" (required json_arg) ~doc:"JSON AlarmNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_alarm_actions
           (Values.DisableAlarmActionsInput.make
              ~alarmNames:(Values.AlarmNames.of_json alarmNames) ()) None
           None])
let disable_insight_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleNames =
         flag "rule-names" (required json_arg) ~doc:"JSON InsightRuleNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_insight_rules
           (Values.DisableInsightRulesInput.make
              ~ruleNames:(Values.InsightRuleNames.of_json ruleNames) ())
           (Some Values.DisableInsightRulesOutput.to_json)
           (Some Values.DisableInsightRulesOutput.error_to_json)])
let enable_alarm_actions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and alarmNames =
         flag "alarm-names" (required json_arg) ~doc:"JSON AlarmNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_alarm_actions
           (Values.EnableAlarmActionsInput.make
              ~alarmNames:(Values.AlarmNames.of_json alarmNames) ()) None
           None])
let enable_insight_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleNames =
         flag "rule-names" (required json_arg) ~doc:"JSON InsightRuleNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_insight_rules
           (Values.EnableInsightRulesInput.make
              ~ruleNames:(Values.InsightRuleNames.of_json ruleNames) ())
           (Some Values.EnableInsightRulesOutput.to_json)
           (Some Values.EnableInsightRulesOutput.error_to_json)])
let get_alarm_mute_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and alarmMuteRuleName =
         flag "alarm-mute-rule-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_alarm_mute_rule
           (Values.GetAlarmMuteRuleInput.make ~alarmMuteRuleName ())
           (Some Values.GetAlarmMuteRuleOutput.to_json)
           (Some Values.GetAlarmMuteRuleOutput.error_to_json)])
let get_dashboard =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dashboardName =
         flag "dashboard-name" (required string) ~doc:"STRING DashboardName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_dashboard (Values.GetDashboardInput.make ~dashboardName ())
           (Some Values.GetDashboardOutput.to_json)
           (Some Values.GetDashboardOutput.error_to_json)])
let get_insight_rule_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxContributorCount =
         flag "max-contributor-count" (optional int)
           ~doc:"INT InsightRuleUnboundInteger"
       and metrics =
         flag "metrics" (optional json_arg) ~doc:"JSON InsightRuleMetricList"
       and orderBy =
         flag "order-by" (optional string) ~doc:"STRING InsightRuleOrderBy"
       and ruleName =
         flag "rule-name" (required string) ~doc:"STRING InsightRuleName"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON Timestamp"
       and period = flag "period" (required int) ~doc:"INT Period" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_insight_rule_report
           (Values.GetInsightRuleReportInput.make ?maxContributorCount
              ?metrics:(Option.map ~f:Values.InsightRuleMetricList.of_json
                          metrics) ?orderBy ~ruleName
              ~startTime:(Values.Timestamp.of_json startTime)
              ~endTime:(Values.Timestamp.of_json endTime) ~period ())
           (Some Values.GetInsightRuleReportOutput.to_json)
           (Some Values.GetInsightRuleReportOutput.error_to_json)])
let get_metric_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and scanBy = flag "scan-by" (optional json_arg) ~doc:"JSON ScanBy"
       and maxDatapoints =
         flag "max-datapoints" (optional int)
           ~doc:"INT GetMetricDataMaxDatapoints"
       and labelOptions =
         flag "label-options" (optional json_arg) ~doc:"JSON LabelOptions"
       and metricDataQueries =
         flag "metric-data-queries" (required json_arg)
           ~doc:"JSON MetricDataQueries"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_metric_data
           (Values.GetMetricDataInput.make ?nextToken
              ?scanBy:(Option.map ~f:Values.ScanBy.of_json scanBy)
              ?maxDatapoints
              ?labelOptions:(Option.map ~f:Values.LabelOptions.of_json
                               labelOptions)
              ~metricDataQueries:(Values.MetricDataQueries.of_json
                                    metricDataQueries)
              ~startTime:(Values.Timestamp.of_json startTime)
              ~endTime:(Values.Timestamp.of_json endTime) ())
           (Some Values.GetMetricDataOutput.to_json)
           (Some Values.GetMetricDataOutput.error_to_json)])
let get_metric_statistics =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dimensions =
         flag "dimensions" (optional json_arg) ~doc:"JSON Dimensions"
       and statistics =
         flag "statistics" (optional json_arg) ~doc:"JSON Statistics"
       and extendedStatistics =
         flag "extended-statistics" (optional json_arg)
           ~doc:"JSON ExtendedStatistics"
       and unit = flag "unit" (optional json_arg) ~doc:"JSON StandardUnit"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace"
       and metricName =
         flag "metric-name" (required string) ~doc:"STRING MetricName"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON Timestamp"
       and period = flag "period" (required int) ~doc:"INT Period" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_metric_statistics
           (Values.GetMetricStatisticsInput.make
              ?dimensions:(Option.map ~f:Values.Dimensions.of_json dimensions)
              ?statistics:(Option.map ~f:Values.Statistics.of_json statistics)
              ?extendedStatistics:(Option.map
                                     ~f:Values.ExtendedStatistics.of_json
                                     extendedStatistics)
              ?unit:(Option.map ~f:Values.StandardUnit.of_json unit)
              ~namespace ~metricName
              ~startTime:(Values.Timestamp.of_json startTime)
              ~endTime:(Values.Timestamp.of_json endTime) ~period ())
           (Some Values.GetMetricStatisticsOutput.to_json)
           (Some Values.GetMetricStatisticsOutput.error_to_json)])
let get_metric_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING MetricStreamName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_metric_stream (Values.GetMetricStreamInput.make ~name ())
           (Some Values.GetMetricStreamOutput.to_json)
           (Some Values.GetMetricStreamOutput.error_to_json)])
let get_metric_widget_image =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and outputFormat =
         flag "output-format" (optional string) ~doc:"STRING OutputFormat"
       and metricWidget =
         flag "metric-widget" (required string) ~doc:"STRING MetricWidget" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_metric_widget_image
           (Values.GetMetricWidgetImageInput.make ?outputFormat ~metricWidget
              ()) (Some Values.GetMetricWidgetImageOutput.to_json)
           (Some Values.GetMetricWidgetImageOutput.error_to_json)])
let get_o_tel_enrichment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_o_tel_enrichment (Values.GetOTelEnrichmentInput.make ())
           (Some Values.GetOTelEnrichmentOutput.to_json)
           (Some Values.GetOTelEnrichmentOutput.error_to_json)])
let list_alarm_mute_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and alarmName = flag "alarm-name" (optional string) ~doc:"STRING Name"
       and statuses =
         flag "statuses" (optional json_arg)
           ~doc:"JSON AlarmMuteRuleStatuses"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT MaxRecords"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_alarm_mute_rules
           (Values.ListAlarmMuteRulesInput.make ?alarmName
              ?statuses:(Option.map ~f:Values.AlarmMuteRuleStatuses.of_json
                           statuses) ?maxRecords ?nextToken ())
           (Some Values.ListAlarmMuteRulesOutput.to_json)
           (Some Values.ListAlarmMuteRulesOutput.error_to_json)])
let list_dashboards =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dashboardNamePrefix =
         flag "dashboard-name-prefix" (optional string)
           ~doc:"STRING DashboardNamePrefix"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_dashboards
           (Values.ListDashboardsInput.make ?dashboardNamePrefix ?nextToken
              ()) (Some Values.ListDashboardsOutput.to_json)
           (Some Values.ListDashboardsOutput.error_to_json)])
let list_managed_insight_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT InsightRuleMaxResults"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_managed_insight_rules
           (Values.ListManagedInsightRulesInput.make ?nextToken ?maxResults
              ~resourceARN ())
           (Some Values.ListManagedInsightRulesOutput.to_json)
           (Some Values.ListManagedInsightRulesOutput.error_to_json)])
let list_metric_streams =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListMetricStreamsMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_metric_streams
           (Values.ListMetricStreamsInput.make ?nextToken ?maxResults ())
           (Some Values.ListMetricStreamsOutput.to_json)
           (Some Values.ListMetricStreamsOutput.error_to_json)])
let list_metrics =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namespace =
         flag "namespace" (optional string) ~doc:"STRING Namespace"
       and metricName =
         flag "metric-name" (optional string) ~doc:"STRING MetricName"
       and dimensions =
         flag "dimensions" (optional json_arg) ~doc:"JSON DimensionFilters"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and recentlyActive =
         flag "recently-active" (optional json_arg)
           ~doc:"JSON RecentlyActive"
       and includeLinkedAccounts =
         flag "include-linked-accounts" (optional bool)
           ~doc:"BOOL IncludeLinkedAccounts"
       and owningAccount =
         flag "owning-account" (optional string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_metrics
           (Values.ListMetricsInput.make ?namespace ?metricName
              ?dimensions:(Option.map ~f:Values.DimensionFilters.of_json
                             dimensions) ?nextToken
              ?recentlyActive:(Option.map ~f:Values.RecentlyActive.of_json
                                 recentlyActive) ?includeLinkedAccounts
              ?owningAccount ()) (Some Values.ListMetricsOutput.to_json)
           (Some Values.ListMetricsOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~resourceARN ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let put_alarm_mute_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING AlarmDescription"
       and muteTargets =
         flag "mute-targets" (optional json_arg) ~doc:"JSON MuteTargets"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and startDate =
         flag "start-date" (optional json_arg) ~doc:"JSON Timestamp"
       and expireDate =
         flag "expire-date" (optional json_arg) ~doc:"JSON Timestamp"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and rule = flag "rule" (required json_arg) ~doc:"JSON Rule" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_alarm_mute_rule
           (Values.PutAlarmMuteRuleInput.make ?description
              ?muteTargets:(Option.map ~f:Values.MuteTargets.of_json
                              muteTargets)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?startDate:(Option.map ~f:Values.Timestamp.of_json startDate)
              ?expireDate:(Option.map ~f:Values.Timestamp.of_json expireDate)
              ~name ~rule:(Values.Rule.of_json rule) ()) None None])
let put_anomaly_detector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namespace =
         flag "namespace" (optional string) ~doc:"STRING Namespace"
       and metricName =
         flag "metric-name" (optional string) ~doc:"STRING MetricName"
       and dimensions =
         flag "dimensions" (optional json_arg) ~doc:"JSON Dimensions"
       and stat =
         flag "stat" (optional string)
           ~doc:"STRING AnomalyDetectorMetricStat"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON AnomalyDetectorConfiguration"
       and metricCharacteristics =
         flag "metric-characteristics" (optional json_arg)
           ~doc:"JSON MetricCharacteristics"
       and singleMetricAnomalyDetector =
         flag "single-metric-anomaly-detector" (optional json_arg)
           ~doc:"JSON SingleMetricAnomalyDetector"
       and metricMathAnomalyDetector =
         flag "metric-math-anomaly-detector" (optional json_arg)
           ~doc:"JSON MetricMathAnomalyDetector" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_anomaly_detector
           (Values.PutAnomalyDetectorInput.make ?namespace ?metricName
              ?dimensions:(Option.map ~f:Values.Dimensions.of_json dimensions)
              ?stat
              ?configuration:(Option.map
                                ~f:Values.AnomalyDetectorConfiguration.of_json
                                configuration)
              ?metricCharacteristics:(Option.map
                                        ~f:Values.MetricCharacteristics.of_json
                                        metricCharacteristics)
              ?singleMetricAnomalyDetector:(Option.map
                                              ~f:Values.SingleMetricAnomalyDetector.of_json
                                              singleMetricAnomalyDetector)
              ?metricMathAnomalyDetector:(Option.map
                                            ~f:Values.MetricMathAnomalyDetector.of_json
                                            metricMathAnomalyDetector) ())
           (Some Values.PutAnomalyDetectorOutput.to_json)
           (Some Values.PutAnomalyDetectorOutput.error_to_json)])
let put_composite_alarm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and actionsEnabled =
         flag "actions-enabled" (optional bool) ~doc:"BOOL ActionsEnabled"
       and alarmActions =
         flag "alarm-actions" (optional json_arg) ~doc:"JSON ResourceList"
       and alarmDescription =
         flag "alarm-description" (optional string)
           ~doc:"STRING AlarmDescription"
       and insufficientDataActions =
         flag "insufficient-data-actions" (optional json_arg)
           ~doc:"JSON ResourceList"
       and oKActions =
         flag "o-k-actions" (optional json_arg) ~doc:"JSON ResourceList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and actionsSuppressor =
         flag "actions-suppressor" (optional string) ~doc:"STRING AlarmArn"
       and actionsSuppressorWaitPeriod =
         flag "actions-suppressor-wait-period" (optional int)
           ~doc:"INT SuppressorPeriod"
       and actionsSuppressorExtensionPeriod =
         flag "actions-suppressor-extension-period" (optional int)
           ~doc:"INT SuppressorPeriod"
       and alarmName =
         flag "alarm-name" (required string) ~doc:"STRING AlarmName"
       and alarmRule =
         flag "alarm-rule" (required string) ~doc:"STRING AlarmRule" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_composite_alarm
           (Values.PutCompositeAlarmInput.make ?actionsEnabled
              ?alarmActions:(Option.map ~f:Values.ResourceList.of_json
                               alarmActions) ?alarmDescription
              ?insufficientDataActions:(Option.map
                                          ~f:Values.ResourceList.of_json
                                          insufficientDataActions)
              ?oKActions:(Option.map ~f:Values.ResourceList.of_json oKActions)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?actionsSuppressor ?actionsSuppressorWaitPeriod
              ?actionsSuppressorExtensionPeriod ~alarmName ~alarmRule ())
           None None])
let put_dashboard =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and dashboardName =
         flag "dashboard-name" (required string) ~doc:"STRING DashboardName"
       and dashboardBody =
         flag "dashboard-body" (required string) ~doc:"STRING DashboardBody" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_dashboard
           (Values.PutDashboardInput.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~dashboardName ~dashboardBody ())
           (Some Values.PutDashboardOutput.to_json)
           (Some Values.PutDashboardOutput.error_to_json)])
let put_insight_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleState =
         flag "rule-state" (optional string) ~doc:"STRING InsightRuleState"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and applyOnTransformedLogs =
         flag "apply-on-transformed-logs" (optional bool)
           ~doc:"BOOL InsightRuleOnTransformedLogs"
       and ruleName =
         flag "rule-name" (required string) ~doc:"STRING InsightRuleName"
       and ruleDefinition =
         flag "rule-definition" (required string)
           ~doc:"STRING InsightRuleDefinition" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_insight_rule
           (Values.PutInsightRuleInput.make ?ruleState
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?applyOnTransformedLogs ~ruleName ~ruleDefinition ())
           (Some Values.PutInsightRuleOutput.to_json)
           (Some Values.PutInsightRuleOutput.error_to_json)])
let put_managed_insight_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and managedRules =
         flag "managed-rules" (required json_arg) ~doc:"JSON ManagedRules" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_managed_insight_rules
           (Values.PutManagedInsightRulesInput.make
              ~managedRules:(Values.ManagedRules.of_json managedRules) ())
           (Some Values.PutManagedInsightRulesOutput.to_json)
           (Some Values.PutManagedInsightRulesOutput.error_to_json)])
let put_metric_alarm =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and alarmDescription =
         flag "alarm-description" (optional string)
           ~doc:"STRING AlarmDescription"
       and actionsEnabled =
         flag "actions-enabled" (optional bool) ~doc:"BOOL ActionsEnabled"
       and oKActions =
         flag "o-k-actions" (optional json_arg) ~doc:"JSON ResourceList"
       and alarmActions =
         flag "alarm-actions" (optional json_arg) ~doc:"JSON ResourceList"
       and insufficientDataActions =
         flag "insufficient-data-actions" (optional json_arg)
           ~doc:"JSON ResourceList"
       and metricName =
         flag "metric-name" (optional string) ~doc:"STRING MetricName"
       and namespace =
         flag "namespace" (optional string) ~doc:"STRING Namespace"
       and statistic =
         flag "statistic" (optional json_arg) ~doc:"JSON Statistic"
       and extendedStatistic =
         flag "extended-statistic" (optional string)
           ~doc:"STRING ExtendedStatistic"
       and dimensions =
         flag "dimensions" (optional json_arg) ~doc:"JSON Dimensions"
       and period = flag "period" (optional int) ~doc:"INT Period"
       and unit = flag "unit" (optional json_arg) ~doc:"JSON StandardUnit"
       and evaluationPeriods =
         flag "evaluation-periods" (optional int)
           ~doc:"INT EvaluationPeriods"
       and datapointsToAlarm =
         flag "datapoints-to-alarm" (optional int)
           ~doc:"INT DatapointsToAlarm"
       and threshold =
         flag "threshold" (optional float) ~doc:"FLOAT Threshold"
       and comparisonOperator =
         flag "comparison-operator" (optional json_arg)
           ~doc:"JSON ComparisonOperator"
       and treatMissingData =
         flag "treat-missing-data" (optional string)
           ~doc:"STRING TreatMissingData"
       and evaluateLowSampleCountPercentile =
         flag "evaluate-low-sample-count-percentile" (optional string)
           ~doc:"STRING EvaluateLowSampleCountPercentile"
       and metrics =
         flag "metrics" (optional json_arg) ~doc:"JSON MetricDataQueries"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and thresholdMetricId =
         flag "threshold-metric-id" (optional string) ~doc:"STRING MetricId"
       and evaluationCriteria =
         flag "evaluation-criteria" (optional json_arg)
           ~doc:"JSON EvaluationCriteria"
       and evaluationInterval =
         flag "evaluation-interval" (optional int)
           ~doc:"INT EvaluationInterval"
       and alarmName =
         flag "alarm-name" (required string) ~doc:"STRING AlarmName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_metric_alarm
           (Values.PutMetricAlarmInput.make ?alarmDescription ?actionsEnabled
              ?oKActions:(Option.map ~f:Values.ResourceList.of_json oKActions)
              ?alarmActions:(Option.map ~f:Values.ResourceList.of_json
                               alarmActions)
              ?insufficientDataActions:(Option.map
                                          ~f:Values.ResourceList.of_json
                                          insufficientDataActions)
              ?metricName ?namespace
              ?statistic:(Option.map ~f:Values.Statistic.of_json statistic)
              ?extendedStatistic
              ?dimensions:(Option.map ~f:Values.Dimensions.of_json dimensions)
              ?period ?unit:(Option.map ~f:Values.StandardUnit.of_json unit)
              ?evaluationPeriods ?datapointsToAlarm ?threshold
              ?comparisonOperator:(Option.map
                                     ~f:Values.ComparisonOperator.of_json
                                     comparisonOperator) ?treatMissingData
              ?evaluateLowSampleCountPercentile
              ?metrics:(Option.map ~f:Values.MetricDataQueries.of_json
                          metrics)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?thresholdMetricId
              ?evaluationCriteria:(Option.map
                                     ~f:Values.EvaluationCriteria.of_json
                                     evaluationCriteria) ?evaluationInterval
              ~alarmName ()) None None])
let put_metric_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metricData =
         flag "metric-data" (optional json_arg) ~doc:"JSON MetricData"
       and entityMetricData =
         flag "entity-metric-data" (optional json_arg)
           ~doc:"JSON EntityMetricDataList"
       and strictEntityValidation =
         flag "strict-entity-validation" (optional bool)
           ~doc:"BOOL StrictEntityValidation"
       and namespace =
         flag "namespace" (required string) ~doc:"STRING Namespace" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_metric_data
           (Values.PutMetricDataInput.make
              ?metricData:(Option.map ~f:Values.MetricData.of_json metricData)
              ?entityMetricData:(Option.map
                                   ~f:Values.EntityMetricDataList.of_json
                                   entityMetricData) ?strictEntityValidation
              ~namespace ()) None None])
let put_metric_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeFilters =
         flag "include-filters" (optional json_arg)
           ~doc:"JSON MetricStreamFilters"
       and excludeFilters =
         flag "exclude-filters" (optional json_arg)
           ~doc:"JSON MetricStreamFilters"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and statisticsConfigurations =
         flag "statistics-configurations" (optional json_arg)
           ~doc:"JSON MetricStreamStatisticsConfigurations"
       and includeLinkedAccountsMetrics =
         flag "include-linked-accounts-metrics" (optional bool)
           ~doc:"BOOL IncludeLinkedAccountsMetrics"
       and name =
         flag "name" (required string) ~doc:"STRING MetricStreamName"
       and firehoseArn =
         flag "firehose-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and roleArn =
         flag "role-arn" (required string) ~doc:"STRING AmazonResourceName"
       and outputFormat =
         flag "output-format" (required json_arg)
           ~doc:"JSON MetricStreamOutputFormat" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_metric_stream
           (Values.PutMetricStreamInput.make
              ?includeFilters:(Option.map
                                 ~f:Values.MetricStreamFilters.of_json
                                 includeFilters)
              ?excludeFilters:(Option.map
                                 ~f:Values.MetricStreamFilters.of_json
                                 excludeFilters)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?statisticsConfigurations:(Option.map
                                           ~f:Values.MetricStreamStatisticsConfigurations.of_json
                                           statisticsConfigurations)
              ?includeLinkedAccountsMetrics ~name ~firehoseArn ~roleArn
              ~outputFormat:(Values.MetricStreamOutputFormat.of_json
                               outputFormat) ())
           (Some Values.PutMetricStreamOutput.to_json)
           (Some Values.PutMetricStreamOutput.error_to_json)])
let set_alarm_state =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stateReasonData =
         flag "state-reason-data" (optional string)
           ~doc:"STRING StateReasonData"
       and alarmName =
         flag "alarm-name" (required string) ~doc:"STRING AlarmName"
       and stateValue =
         flag "state-value" (required json_arg) ~doc:"JSON StateValue"
       and stateReason =
         flag "state-reason" (required string) ~doc:"STRING StateReason" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_alarm_state
           (Values.SetAlarmStateInput.make ?stateReasonData ~alarmName
              ~stateValue:(Values.StateValue.of_json stateValue) ~stateReason
              ()) None None])
let start_metric_streams =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and names =
         flag "names" (required json_arg) ~doc:"JSON MetricStreamNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_metric_streams
           (Values.StartMetricStreamsInput.make
              ~names:(Values.MetricStreamNames.of_json names) ())
           (Some Values.StartMetricStreamsOutput.to_json)
           (Some Values.StartMetricStreamsOutput.error_to_json)])
let start_o_tel_enrichment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_o_tel_enrichment
           (Values.StartOTelEnrichmentInput.make ())
           (Some Values.StartOTelEnrichmentOutput.to_json)
           (Some Values.StartOTelEnrichmentOutput.error_to_json)])
let stop_metric_streams =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and names =
         flag "names" (required json_arg) ~doc:"JSON MetricStreamNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_metric_streams
           (Values.StopMetricStreamsInput.make
              ~names:(Values.MetricStreamNames.of_json names) ())
           (Some Values.StopMetricStreamsOutput.to_json)
           (Some Values.StopMetricStreamsOutput.error_to_json)])
let stop_o_tel_enrichment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_o_tel_enrichment (Values.StopOTelEnrichmentInput.make ())
           (Some Values.StopOTelEnrichmentOutput.to_json)
           (Some Values.StopOTelEnrichmentOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceOutput.to_json)
           (Some Values.TagResourceOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceOutput.to_json)
           (Some Values.UntagResourceOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("delete-alarm-mute-rule", delete_alarm_mute_rule);
    ("delete-alarms", delete_alarms);
    ("delete-anomaly-detector", delete_anomaly_detector);
    ("delete-dashboards", delete_dashboards);
    ("delete-insight-rules", delete_insight_rules);
    ("delete-metric-stream", delete_metric_stream);
    ("describe-alarm-contributors", describe_alarm_contributors);
    ("describe-alarm-history", describe_alarm_history);
    ("describe-alarms", describe_alarms);
    ("describe-alarms-for-metric", describe_alarms_for_metric);
    ("describe-anomaly-detectors", describe_anomaly_detectors);
    ("describe-insight-rules", describe_insight_rules);
    ("disable-alarm-actions", disable_alarm_actions);
    ("disable-insight-rules", disable_insight_rules);
    ("enable-alarm-actions", enable_alarm_actions);
    ("enable-insight-rules", enable_insight_rules);
    ("get-alarm-mute-rule", get_alarm_mute_rule);
    ("get-dashboard", get_dashboard);
    ("get-insight-rule-report", get_insight_rule_report);
    ("get-metric-data", get_metric_data);
    ("get-metric-statistics", get_metric_statistics);
    ("get-metric-stream", get_metric_stream);
    ("get-metric-widget-image", get_metric_widget_image);
    ("get-o-tel-enrichment", get_o_tel_enrichment);
    ("list-alarm-mute-rules", list_alarm_mute_rules);
    ("list-dashboards", list_dashboards);
    ("list-managed-insight-rules", list_managed_insight_rules);
    ("list-metric-streams", list_metric_streams);
    ("list-metrics", list_metrics);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-alarm-mute-rule", put_alarm_mute_rule);
    ("put-anomaly-detector", put_anomaly_detector);
    ("put-composite-alarm", put_composite_alarm);
    ("put-dashboard", put_dashboard);
    ("put-insight-rule", put_insight_rule);
    ("put-managed-insight-rules", put_managed_insight_rules);
    ("put-metric-alarm", put_metric_alarm);
    ("put-metric-data", put_metric_data);
    ("put-metric-stream", put_metric_stream);
    ("set-alarm-state", set_alarm_state);
    ("start-metric-streams", start_metric_streams);
    ("start-o-tel-enrichment", start_o_tel_enrichment);
    ("stop-metric-streams", stop_metric_streams);
    ("stop-o-tel-enrichment", stop_o_tel_enrichment);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_broker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationStrategy =
         flag "authentication-strategy" (optional json_arg)
           ~doc:"JSON AuthenticationStrategy"
       and autoMinorVersionUpgrade =
         flag "auto-minor-version-upgrade" (optional bool)
           ~doc:"BOOL __boolean"
       and configuration =
         flag "configuration" (optional json_arg) ~doc:"JSON ConfigurationId"
       and creatorRequestId =
         flag "creator-request-id" (optional string) ~doc:"STRING __string"
       and encryptionOptions =
         flag "encryption-options" (optional json_arg)
           ~doc:"JSON EncryptionOptions"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING __string"
       and ldapServerMetadata =
         flag "ldap-server-metadata" (optional json_arg)
           ~doc:"JSON LdapServerMetadataInput"
       and logs = flag "logs" (optional json_arg) ~doc:"JSON Logs"
       and maintenanceWindowStartTime =
         flag "maintenance-window-start-time" (optional json_arg)
           ~doc:"JSON WeeklyStartTime"
       and securityGroups =
         flag "security-groups" (optional json_arg)
           ~doc:"JSON __listOf__string"
       and storageType =
         flag "storage-type" (optional json_arg)
           ~doc:"JSON BrokerStorageType"
       and subnetIds =
         flag "subnet-ids" (optional json_arg) ~doc:"JSON __listOf__string"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON __mapOf__string"
       and users = flag "users" (optional json_arg) ~doc:"JSON __listOfUser"
       and dataReplicationMode =
         flag "data-replication-mode" (optional json_arg)
           ~doc:"JSON DataReplicationMode"
       and dataReplicationPrimaryBrokerArn =
         flag "data-replication-primary-broker-arn" (optional string)
           ~doc:"STRING __string"
       and brokerName =
         flag "broker-name" (required string) ~doc:"STRING __string"
       and deploymentMode =
         flag "deployment-mode" (required json_arg)
           ~doc:"JSON DeploymentMode"
       and engineType =
         flag "engine-type" (required json_arg) ~doc:"JSON EngineType"
       and hostInstanceType =
         flag "host-instance-type" (required string) ~doc:"STRING __string"
       and publiclyAccessible =
         flag "publicly-accessible" (required bool) ~doc:"BOOL __boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_broker
           (Values.CreateBrokerRequest.make
              ?authenticationStrategy:(Option.map
                                         ~f:Values.AuthenticationStrategy.of_json
                                         authenticationStrategy)
              ?autoMinorVersionUpgrade
              ?configuration:(Option.map ~f:Values.ConfigurationId.of_json
                                configuration) ?creatorRequestId
              ?encryptionOptions:(Option.map
                                    ~f:Values.EncryptionOptions.of_json
                                    encryptionOptions) ?engineVersion
              ?ldapServerMetadata:(Option.map
                                     ~f:Values.LdapServerMetadataInput.of_json
                                     ldapServerMetadata)
              ?logs:(Option.map ~f:Values.Logs.of_json logs)
              ?maintenanceWindowStartTime:(Option.map
                                             ~f:Values.WeeklyStartTime.of_json
                                             maintenanceWindowStartTime)
              ?securityGroups:(Option.map
                                 ~f:Values.Zz__listOf__string.of_json
                                 securityGroups)
              ?storageType:(Option.map ~f:Values.BrokerStorageType.of_json
                              storageType)
              ?subnetIds:(Option.map ~f:Values.Zz__listOf__string.of_json
                            subnetIds)
              ?tags:(Option.map ~f:Values.Zz__mapOf__string.of_json tags)
              ?users:(Option.map ~f:Values.Zz__listOfUser.of_json users)
              ?dataReplicationMode:(Option.map
                                      ~f:Values.DataReplicationMode.of_json
                                      dataReplicationMode)
              ?dataReplicationPrimaryBrokerArn ~brokerName
              ~deploymentMode:(Values.DeploymentMode.of_json deploymentMode)
              ~engineType:(Values.EngineType.of_json engineType)
              ~hostInstanceType ~publiclyAccessible ())
           (Some Values.CreateBrokerResponse.to_json)
           (Some Values.CreateBrokerResponse.error_to_json)])
let create_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationStrategy =
         flag "authentication-strategy" (optional json_arg)
           ~doc:"JSON AuthenticationStrategy"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING __string"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON __mapOf__string"
       and engineType =
         flag "engine-type" (required json_arg) ~doc:"JSON EngineType"
       and name = flag "name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_configuration
           (Values.CreateConfigurationRequest.make
              ?authenticationStrategy:(Option.map
                                         ~f:Values.AuthenticationStrategy.of_json
                                         authenticationStrategy)
              ?engineVersion
              ?tags:(Option.map ~f:Values.Zz__mapOf__string.of_json tags)
              ~engineType:(Values.EngineType.of_json engineType) ~name ())
           (Some Values.CreateConfigurationResponse.to_json)
           (Some Values.CreateConfigurationResponse.error_to_json)])
let create_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON __mapOf__string"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_tags
           (Values.CreateTagsRequest.make
              ?tags:(Option.map ~f:Values.Zz__mapOf__string.of_json tags)
              ~resourceArn ()) None None])
let create_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and consoleAccess =
         flag "console-access" (optional bool) ~doc:"BOOL __boolean"
       and groups =
         flag "groups" (optional json_arg) ~doc:"JSON __listOf__string"
       and replicationUser =
         flag "replication-user" (optional bool) ~doc:"BOOL __boolean"
       and brokerId =
         flag "broker-id" (required string) ~doc:"STRING __string"
       and password =
         flag "password" (required string) ~doc:"STRING __string"
       and username =
         flag "username" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_user
           (Values.CreateUserRequest.make ?consoleAccess
              ?groups:(Option.map ~f:Values.Zz__listOf__string.of_json groups)
              ?replicationUser ~brokerId ~password ~username ())
           (Some Values.CreateUserResponse.to_json)
           (Some Values.CreateUserResponse.error_to_json)])
let delete_broker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and brokerId =
         flag "broker-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_broker (Values.DeleteBrokerRequest.make ~brokerId ())
           (Some Values.DeleteBrokerResponse.to_json)
           (Some Values.DeleteBrokerResponse.error_to_json)])
let delete_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationId =
         flag "configuration-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_configuration
           (Values.DeleteConfigurationRequest.make ~configurationId ())
           (Some Values.DeleteConfigurationResponse.to_json)
           (Some Values.DeleteConfigurationResponse.error_to_json)])
let delete_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON __listOf__string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_tags
           (Values.DeleteTagsRequest.make ~resourceArn
              ~tagKeys:(Values.Zz__listOf__string.of_json tagKeys) ()) None
           None])
let delete_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and brokerId =
         flag "broker-id" (required string) ~doc:"STRING __string"
       and username =
         flag "username" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_user
           (Values.DeleteUserRequest.make ~brokerId ~username ())
           (Some Values.DeleteUserResponse.to_json)
           (Some Values.DeleteUserResponse.error_to_json)])
let describe_broker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and brokerId =
         flag "broker-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_broker
           (Values.DescribeBrokerRequest.make ~brokerId ())
           (Some Values.DescribeBrokerResponse.to_json)
           (Some Values.DescribeBrokerResponse.error_to_json)])
let describe_broker_engine_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and engineType =
         flag "engine-type" (optional string) ~doc:"STRING __string"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_broker_engine_types
           (Values.DescribeBrokerEngineTypesRequest.make ?engineType
              ?maxResults ?nextToken ())
           (Some Values.DescribeBrokerEngineTypesResponse.to_json)
           (Some Values.DescribeBrokerEngineTypesResponse.error_to_json)])
let describe_broker_instance_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and engineType =
         flag "engine-type" (optional string) ~doc:"STRING __string"
       and hostInstanceType =
         flag "host-instance-type" (optional string) ~doc:"STRING __string"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and storageType =
         flag "storage-type" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_broker_instance_options
           (Values.DescribeBrokerInstanceOptionsRequest.make ?engineType
              ?hostInstanceType ?maxResults ?nextToken ?storageType ())
           (Some Values.DescribeBrokerInstanceOptionsResponse.to_json)
           (Some Values.DescribeBrokerInstanceOptionsResponse.error_to_json)])
let describe_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationId =
         flag "configuration-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_configuration
           (Values.DescribeConfigurationRequest.make ~configurationId ())
           (Some Values.DescribeConfigurationResponse.to_json)
           (Some Values.DescribeConfigurationResponse.error_to_json)])
let describe_configuration_revision =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationId =
         flag "configuration-id" (required string) ~doc:"STRING __string"
       and configurationRevision =
         flag "configuration-revision" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_configuration_revision
           (Values.DescribeConfigurationRevisionRequest.make ~configurationId
              ~configurationRevision ())
           (Some Values.DescribeConfigurationRevisionResponse.to_json)
           (Some Values.DescribeConfigurationRevisionResponse.error_to_json)])
let describe_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and brokerId =
         flag "broker-id" (required string) ~doc:"STRING __string"
       and username =
         flag "username" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_user
           (Values.DescribeUserRequest.make ~brokerId ~username ())
           (Some Values.DescribeUserResponse.to_json)
           (Some Values.DescribeUserResponse.error_to_json)])
let list_brokers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_brokers
           (Values.ListBrokersRequest.make ?maxResults ?nextToken ())
           (Some Values.ListBrokersResponse.to_json)
           (Some Values.ListBrokersResponse.error_to_json)])
let list_configuration_revisions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and configurationId =
         flag "configuration-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_configuration_revisions
           (Values.ListConfigurationRevisionsRequest.make ?maxResults
              ?nextToken ~configurationId ())
           (Some Values.ListConfigurationRevisionsResponse.to_json)
           (Some Values.ListConfigurationRevisionsResponse.error_to_json)])
let list_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_configurations
           (Values.ListConfigurationsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListConfigurationsResponse.to_json)
           (Some Values.ListConfigurationsResponse.error_to_json)])
let list_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags (Values.ListTagsRequest.make ~resourceArn ())
           (Some Values.ListTagsResponse.to_json)
           (Some Values.ListTagsResponse.error_to_json)])
let list_users =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and brokerId =
         flag "broker-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_users
           (Values.ListUsersRequest.make ?maxResults ?nextToken ~brokerId ())
           (Some Values.ListUsersResponse.to_json)
           (Some Values.ListUsersResponse.error_to_json)])
let promote =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and brokerId =
         flag "broker-id" (required string) ~doc:"STRING __string"
       and mode = flag "mode" (required json_arg) ~doc:"JSON PromoteMode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.promote
           (Values.PromoteRequest.make ~brokerId
              ~mode:(Values.PromoteMode.of_json mode) ())
           (Some Values.PromoteResponse.to_json)
           (Some Values.PromoteResponse.error_to_json)])
let reboot_broker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and brokerId =
         flag "broker-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reboot_broker (Values.RebootBrokerRequest.make ~brokerId ())
           (Some Values.RebootBrokerResponse.to_json)
           (Some Values.RebootBrokerResponse.error_to_json)])
let update_broker =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationStrategy =
         flag "authentication-strategy" (optional json_arg)
           ~doc:"JSON AuthenticationStrategy"
       and autoMinorVersionUpgrade =
         flag "auto-minor-version-upgrade" (optional bool)
           ~doc:"BOOL __boolean"
       and configuration =
         flag "configuration" (optional json_arg) ~doc:"JSON ConfigurationId"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING __string"
       and hostInstanceType =
         flag "host-instance-type" (optional string) ~doc:"STRING __string"
       and ldapServerMetadata =
         flag "ldap-server-metadata" (optional json_arg)
           ~doc:"JSON LdapServerMetadataInput"
       and logs = flag "logs" (optional json_arg) ~doc:"JSON Logs"
       and maintenanceWindowStartTime =
         flag "maintenance-window-start-time" (optional json_arg)
           ~doc:"JSON WeeklyStartTime"
       and securityGroups =
         flag "security-groups" (optional json_arg)
           ~doc:"JSON __listOf__string"
       and dataReplicationMode =
         flag "data-replication-mode" (optional json_arg)
           ~doc:"JSON DataReplicationMode"
       and brokerId =
         flag "broker-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_broker
           (Values.UpdateBrokerRequest.make
              ?authenticationStrategy:(Option.map
                                         ~f:Values.AuthenticationStrategy.of_json
                                         authenticationStrategy)
              ?autoMinorVersionUpgrade
              ?configuration:(Option.map ~f:Values.ConfigurationId.of_json
                                configuration) ?engineVersion
              ?hostInstanceType
              ?ldapServerMetadata:(Option.map
                                     ~f:Values.LdapServerMetadataInput.of_json
                                     ldapServerMetadata)
              ?logs:(Option.map ~f:Values.Logs.of_json logs)
              ?maintenanceWindowStartTime:(Option.map
                                             ~f:Values.WeeklyStartTime.of_json
                                             maintenanceWindowStartTime)
              ?securityGroups:(Option.map
                                 ~f:Values.Zz__listOf__string.of_json
                                 securityGroups)
              ?dataReplicationMode:(Option.map
                                      ~f:Values.DataReplicationMode.of_json
                                      dataReplicationMode) ~brokerId ())
           (Some Values.UpdateBrokerResponse.to_json)
           (Some Values.UpdateBrokerResponse.error_to_json)])
let update_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING __string"
       and configurationId =
         flag "configuration-id" (required string) ~doc:"STRING __string"
       and data = flag "data" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_configuration
           (Values.UpdateConfigurationRequest.make ?description
              ~configurationId ~data ())
           (Some Values.UpdateConfigurationResponse.to_json)
           (Some Values.UpdateConfigurationResponse.error_to_json)])
let update_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and consoleAccess =
         flag "console-access" (optional bool) ~doc:"BOOL __boolean"
       and groups =
         flag "groups" (optional json_arg) ~doc:"JSON __listOf__string"
       and password =
         flag "password" (optional string) ~doc:"STRING __string"
       and replicationUser =
         flag "replication-user" (optional bool) ~doc:"BOOL __boolean"
       and brokerId =
         flag "broker-id" (required string) ~doc:"STRING __string"
       and username =
         flag "username" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user
           (Values.UpdateUserRequest.make ?consoleAccess
              ?groups:(Option.map ~f:Values.Zz__listOf__string.of_json groups)
              ?password ?replicationUser ~brokerId ~username ())
           (Some Values.UpdateUserResponse.to_json)
           (Some Values.UpdateUserResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-broker", create_broker);
    ("create-configuration", create_configuration);
    ("create-tags", create_tags);
    ("create-user", create_user);
    ("delete-broker", delete_broker);
    ("delete-configuration", delete_configuration);
    ("delete-tags", delete_tags);
    ("delete-user", delete_user);
    ("describe-broker", describe_broker);
    ("describe-broker-engine-types", describe_broker_engine_types);
    ("describe-broker-instance-options", describe_broker_instance_options);
    ("describe-configuration", describe_configuration);
    ("describe-configuration-revision", describe_configuration_revision);
    ("describe-user", describe_user);
    ("list-brokers", list_brokers);
    ("list-configuration-revisions", list_configuration_revisions);
    ("list-configurations", list_configurations);
    ("list-tags", list_tags);
    ("list-users", list_users);
    ("promote", promote);
    ("reboot-broker", reboot_broker);
    ("update-broker", update_broker);
    ("update-configuration", update_configuration);
    ("update-user", update_user)]
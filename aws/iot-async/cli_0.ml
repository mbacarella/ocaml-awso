(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let accept_certificate_transfer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and setAsActive =
         flag "set-as-active" (optional bool) ~doc:"BOOL SetAsActive"
       and certificateId =
         flag "certificate-id" (required string) ~doc:"STRING CertificateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_certificate_transfer
           (Values.AcceptCertificateTransferRequest.make ?setAsActive
              ~certificateId ()) None None])
let add_thing_to_billing_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and billingGroupName =
         flag "billing-group-name" (optional string)
           ~doc:"STRING BillingGroupName"
       and billingGroupArn =
         flag "billing-group-arn" (optional string)
           ~doc:"STRING BillingGroupArn"
       and thingName =
         flag "thing-name" (optional string) ~doc:"STRING ThingName"
       and thingArn =
         flag "thing-arn" (optional string) ~doc:"STRING ThingArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_thing_to_billing_group
           (Values.AddThingToBillingGroupRequest.make ?billingGroupName
              ?billingGroupArn ?thingName ?thingArn ())
           (Some Values.AddThingToBillingGroupResponse.to_json)
           (Some Values.AddThingToBillingGroupResponse.error_to_json)])
let add_thing_to_thing_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thingGroupName =
         flag "thing-group-name" (optional string)
           ~doc:"STRING ThingGroupName"
       and thingGroupArn =
         flag "thing-group-arn" (optional string) ~doc:"STRING ThingGroupArn"
       and thingName =
         flag "thing-name" (optional string) ~doc:"STRING ThingName"
       and thingArn =
         flag "thing-arn" (optional string) ~doc:"STRING ThingArn"
       and overrideDynamicGroups =
         flag "override-dynamic-groups" (optional bool)
           ~doc:"BOOL OverrideDynamicGroups" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_thing_to_thing_group
           (Values.AddThingToThingGroupRequest.make ?thingGroupName
              ?thingGroupArn ?thingName ?thingArn ?overrideDynamicGroups ())
           (Some Values.AddThingToThingGroupResponse.to_json)
           (Some Values.AddThingToThingGroupResponse.error_to_json)])
let associate_targets_with_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and comment = flag "comment" (optional string) ~doc:"STRING Comment"
       and namespaceId =
         flag "namespace-id" (optional string) ~doc:"STRING NamespaceId"
       and targets =
         flag "targets" (required json_arg) ~doc:"JSON JobTargets"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_targets_with_job
           (Values.AssociateTargetsWithJobRequest.make ?comment ?namespaceId
              ~targets:(Values.JobTargets.of_json targets) ~jobId ())
           (Some Values.AssociateTargetsWithJobResponse.to_json)
           (Some Values.AssociateTargetsWithJobResponse.error_to_json)])
let attach_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING PolicyName"
       and target =
         flag "target" (required string) ~doc:"STRING PolicyTarget" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_policy
           (Values.AttachPolicyRequest.make ~policyName ~target ()) None None])
let attach_principal_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING PolicyName"
       and principal =
         flag "principal" (required string) ~doc:"STRING Principal" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_principal_policy
           (Values.AttachPrincipalPolicyRequest.make ~policyName ~principal
              ()) None None])
let attach_security_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and securityProfileName =
         flag "security-profile-name" (required string)
           ~doc:"STRING SecurityProfileName"
       and securityProfileTargetArn =
         flag "security-profile-target-arn" (required string)
           ~doc:"STRING SecurityProfileTargetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_security_profile
           (Values.AttachSecurityProfileRequest.make ~securityProfileName
              ~securityProfileTargetArn ())
           (Some Values.AttachSecurityProfileResponse.to_json)
           (Some Values.AttachSecurityProfileResponse.error_to_json)])
let attach_thing_principal =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thingName =
         flag "thing-name" (required string) ~doc:"STRING ThingName"
       and principal =
         flag "principal" (required string) ~doc:"STRING Principal" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.attach_thing_principal
           (Values.AttachThingPrincipalRequest.make ~thingName ~principal ())
           (Some Values.AttachThingPrincipalResponse.to_json)
           (Some Values.AttachThingPrincipalResponse.error_to_json)])
let cancel_audit_mitigation_actions_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskId =
         flag "task-id" (required string)
           ~doc:"STRING MitigationActionsTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_audit_mitigation_actions_task
           (Values.CancelAuditMitigationActionsTaskRequest.make ~taskId ())
           (Some Values.CancelAuditMitigationActionsTaskResponse.to_json)
           (Some
              Values.CancelAuditMitigationActionsTaskResponse.error_to_json)])
let cancel_audit_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskId =
         flag "task-id" (required string) ~doc:"STRING AuditTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_audit_task
           (Values.CancelAuditTaskRequest.make ~taskId ())
           (Some Values.CancelAuditTaskResponse.to_json)
           (Some Values.CancelAuditTaskResponse.error_to_json)])
let cancel_certificate_transfer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateId =
         flag "certificate-id" (required string) ~doc:"STRING CertificateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_certificate_transfer
           (Values.CancelCertificateTransferRequest.make ~certificateId ())
           None None])
let cancel_detect_mitigation_actions_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskId =
         flag "task-id" (required string)
           ~doc:"STRING MitigationActionsTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_detect_mitigation_actions_task
           (Values.CancelDetectMitigationActionsTaskRequest.make ~taskId ())
           (Some Values.CancelDetectMitigationActionsTaskResponse.to_json)
           (Some
              Values.CancelDetectMitigationActionsTaskResponse.error_to_json)])
let cancel_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reasonCode =
         flag "reason-code" (optional string) ~doc:"STRING ReasonCode"
       and comment = flag "comment" (optional string) ~doc:"STRING Comment"
       and force = flag "force" (optional bool) ~doc:"BOOL ForceFlag"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_job
           (Values.CancelJobRequest.make ?reasonCode ?comment ?force ~jobId
              ()) (Some Values.CancelJobResponse.to_json)
           (Some Values.CancelJobResponse.error_to_json)])
let cancel_job_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and force = flag "force" (optional bool) ~doc:"BOOL ForceFlag"
       and expectedVersion =
         flag "expected-version" (optional json_arg)
           ~doc:"JSON ExpectedVersion"
       and statusDetails =
         flag "status-details" (optional json_arg) ~doc:"JSON DetailsMap"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId"
       and thingName =
         flag "thing-name" (required string) ~doc:"STRING ThingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_job_execution
           (Values.CancelJobExecutionRequest.make ?force
              ?expectedVersion:(Option.map ~f:Values.ExpectedVersion.of_json
                                  expectedVersion)
              ?statusDetails:(Option.map ~f:Values.DetailsMap.of_json
                                statusDetails) ~jobId ~thingName ()) None
           None])
let clear_default_authorizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.clear_default_authorizer
           (Values.ClearDefaultAuthorizerRequest.make ())
           (Some Values.ClearDefaultAuthorizerResponse.to_json)
           (Some Values.ClearDefaultAuthorizerResponse.error_to_json)])
let confirm_topic_rule_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and confirmationToken =
         flag "confirmation-token" (required string)
           ~doc:"STRING ConfirmationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.confirm_topic_rule_destination
           (Values.ConfirmTopicRuleDestinationRequest.make ~confirmationToken
              ()) (Some Values.ConfirmTopicRuleDestinationResponse.to_json)
           (Some Values.ConfirmTopicRuleDestinationResponse.error_to_json)])
let create_audit_suppression =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expirationDate =
         flag "expiration-date" (optional json_arg) ~doc:"JSON Timestamp"
       and suppressIndefinitely =
         flag "suppress-indefinitely" (optional bool)
           ~doc:"BOOL SuppressIndefinitely"
       and description =
         flag "description" (optional string) ~doc:"STRING AuditDescription"
       and checkName =
         flag "check-name" (required string) ~doc:"STRING AuditCheckName"
       and resourceIdentifier =
         flag "resource-identifier" (required json_arg)
           ~doc:"JSON ResourceIdentifier"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_audit_suppression
           (Values.CreateAuditSuppressionRequest.make
              ?expirationDate:(Option.map ~f:Values.Timestamp.of_json
                                 expirationDate) ?suppressIndefinitely
              ?description ~checkName
              ~resourceIdentifier:(Values.ResourceIdentifier.of_json
                                     resourceIdentifier) ~clientRequestToken
              ()) (Some Values.CreateAuditSuppressionResponse.to_json)
           (Some Values.CreateAuditSuppressionResponse.error_to_json)])
let create_authorizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tokenKeyName =
         flag "token-key-name" (optional string) ~doc:"STRING TokenKeyName"
       and tokenSigningPublicKeys =
         flag "token-signing-public-keys" (optional json_arg)
           ~doc:"JSON PublicKeyMap"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON AuthorizerStatus"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and signingDisabled =
         flag "signing-disabled" (optional bool) ~doc:"BOOL BooleanKey"
       and enableCachingForHttp =
         flag "enable-caching-for-http" (optional bool)
           ~doc:"BOOL EnableCachingForHttp"
       and authorizerName =
         flag "authorizer-name" (required string)
           ~doc:"STRING AuthorizerName"
       and authorizerFunctionArn =
         flag "authorizer-function-arn" (required string)
           ~doc:"STRING AuthorizerFunctionArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_authorizer
           (Values.CreateAuthorizerRequest.make ?tokenKeyName
              ?tokenSigningPublicKeys:(Option.map
                                         ~f:Values.PublicKeyMap.of_json
                                         tokenSigningPublicKeys)
              ?status:(Option.map ~f:Values.AuthorizerStatus.of_json status)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?signingDisabled ?enableCachingForHttp ~authorizerName
              ~authorizerFunctionArn ())
           (Some Values.CreateAuthorizerResponse.to_json)
           (Some Values.CreateAuthorizerResponse.error_to_json)])
let create_billing_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and billingGroupProperties =
         flag "billing-group-properties" (optional json_arg)
           ~doc:"JSON BillingGroupProperties"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and billingGroupName =
         flag "billing-group-name" (required string)
           ~doc:"STRING BillingGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_billing_group
           (Values.CreateBillingGroupRequest.make
              ?billingGroupProperties:(Option.map
                                         ~f:Values.BillingGroupProperties.of_json
                                         billingGroupProperties)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~billingGroupName ())
           (Some Values.CreateBillingGroupResponse.to_json)
           (Some Values.CreateBillingGroupResponse.error_to_json)])
let create_certificate_from_csr =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and setAsActive =
         flag "set-as-active" (optional bool) ~doc:"BOOL SetAsActive"
       and certificateSigningRequest =
         flag "certificate-signing-request" (required string)
           ~doc:"STRING CertificateSigningRequest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_certificate_from_csr
           (Values.CreateCertificateFromCsrRequest.make ?setAsActive
              ~certificateSigningRequest ())
           (Some Values.CreateCertificateFromCsrResponse.to_json)
           (Some Values.CreateCertificateFromCsrResponse.error_to_json)])
let create_custom_metric =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayName =
         flag "display-name" (optional string)
           ~doc:"STRING CustomMetricDisplayName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and metricName =
         flag "metric-name" (required string) ~doc:"STRING MetricName"
       and metricType =
         flag "metric-type" (required json_arg) ~doc:"JSON CustomMetricType"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_custom_metric
           (Values.CreateCustomMetricRequest.make ?displayName
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~metricName
              ~metricType:(Values.CustomMetricType.of_json metricType)
              ~clientRequestToken ())
           (Some Values.CreateCustomMetricResponse.to_json)
           (Some Values.CreateCustomMetricResponse.error_to_json)])
let create_dimension =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name = flag "name" (required string) ~doc:"STRING DimensionName"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON DimensionType"
       and stringValues =
         flag "string-values" (required json_arg)
           ~doc:"JSON DimensionStringValues"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_dimension
           (Values.CreateDimensionRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name
              ~type_:(Values.DimensionType.of_json type_)
              ~stringValues:(Values.DimensionStringValues.of_json
                               stringValues) ~clientRequestToken ())
           (Some Values.CreateDimensionResponse.to_json)
           (Some Values.CreateDimensionResponse.error_to_json)])
let create_domain_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (optional string) ~doc:"STRING DomainName"
       and serverCertificateArns =
         flag "server-certificate-arns" (optional json_arg)
           ~doc:"JSON ServerCertificateArns"
       and validationCertificateArn =
         flag "validation-certificate-arn" (optional string)
           ~doc:"STRING AcmCertificateArn"
       and authorizerConfig =
         flag "authorizer-config" (optional json_arg)
           ~doc:"JSON AuthorizerConfig"
       and serviceType =
         flag "service-type" (optional json_arg) ~doc:"JSON ServiceType"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and domainConfigurationName =
         flag "domain-configuration-name" (required string)
           ~doc:"STRING DomainConfigurationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_domain_configuration
           (Values.CreateDomainConfigurationRequest.make ?domainName
              ?serverCertificateArns:(Option.map
                                        ~f:Values.ServerCertificateArns.of_json
                                        serverCertificateArns)
              ?validationCertificateArn
              ?authorizerConfig:(Option.map
                                   ~f:Values.AuthorizerConfig.of_json
                                   authorizerConfig)
              ?serviceType:(Option.map ~f:Values.ServiceType.of_json
                              serviceType)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~domainConfigurationName ())
           (Some Values.CreateDomainConfigurationResponse.to_json)
           (Some Values.CreateDomainConfigurationResponse.error_to_json)])
let create_dynamic_thing_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thingGroupProperties =
         flag "thing-group-properties" (optional json_arg)
           ~doc:"JSON ThingGroupProperties"
       and indexName =
         flag "index-name" (optional string) ~doc:"STRING IndexName"
       and queryVersion =
         flag "query-version" (optional string) ~doc:"STRING QueryVersion"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and thingGroupName =
         flag "thing-group-name" (required string)
           ~doc:"STRING ThingGroupName"
       and queryString =
         flag "query-string" (required string) ~doc:"STRING QueryString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_dynamic_thing_group
           (Values.CreateDynamicThingGroupRequest.make
              ?thingGroupProperties:(Option.map
                                       ~f:Values.ThingGroupProperties.of_json
                                       thingGroupProperties) ?indexName
              ?queryVersion ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~thingGroupName ~queryString ())
           (Some Values.CreateDynamicThingGroupResponse.to_json)
           (Some Values.CreateDynamicThingGroupResponse.error_to_json)])
let create_fleet_metric =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING FleetMetricDescription"
       and queryVersion =
         flag "query-version" (optional string) ~doc:"STRING QueryVersion"
       and indexName =
         flag "index-name" (optional string) ~doc:"STRING IndexName"
       and unit = flag "unit" (optional json_arg) ~doc:"JSON FleetMetricUnit"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and metricName =
         flag "metric-name" (required string) ~doc:"STRING FleetMetricName"
       and queryString =
         flag "query-string" (required string) ~doc:"STRING QueryString"
       and aggregationType =
         flag "aggregation-type" (required json_arg)
           ~doc:"JSON AggregationType"
       and period = flag "period" (required int) ~doc:"INT FleetMetricPeriod"
       and aggregationField =
         flag "aggregation-field" (required string)
           ~doc:"STRING AggregationField" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_fleet_metric
           (Values.CreateFleetMetricRequest.make ?description ?queryVersion
              ?indexName
              ?unit:(Option.map ~f:Values.FleetMetricUnit.of_json unit)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~metricName
              ~queryString
              ~aggregationType:(Values.AggregationType.of_json
                                  aggregationType) ~period ~aggregationField
              ()) (Some Values.CreateFleetMetricResponse.to_json)
           (Some Values.CreateFleetMetricResponse.error_to_json)])
let create_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and documentSource =
         flag "document-source" (optional string)
           ~doc:"STRING JobDocumentSource"
       and document =
         flag "document" (optional string) ~doc:"STRING JobDocument"
       and description =
         flag "description" (optional string) ~doc:"STRING JobDescription"
       and presignedUrlConfig =
         flag "presigned-url-config" (optional json_arg)
           ~doc:"JSON PresignedUrlConfig"
       and targetSelection =
         flag "target-selection" (optional json_arg)
           ~doc:"JSON TargetSelection"
       and jobExecutionsRolloutConfig =
         flag "job-executions-rollout-config" (optional json_arg)
           ~doc:"JSON JobExecutionsRolloutConfig"
       and abortConfig =
         flag "abort-config" (optional json_arg) ~doc:"JSON AbortConfig"
       and timeoutConfig =
         flag "timeout-config" (optional json_arg) ~doc:"JSON TimeoutConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and namespaceId =
         flag "namespace-id" (optional string) ~doc:"STRING NamespaceId"
       and jobTemplateArn =
         flag "job-template-arn" (optional string)
           ~doc:"STRING JobTemplateArn"
       and jobExecutionsRetryConfig =
         flag "job-executions-retry-config" (optional json_arg)
           ~doc:"JSON JobExecutionsRetryConfig"
       and documentParameters =
         flag "document-parameters" (optional json_arg)
           ~doc:"JSON ParameterMap"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId"
       and targets =
         flag "targets" (required json_arg) ~doc:"JSON JobTargets" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_job
           (Values.CreateJobRequest.make ?documentSource ?document
              ?description
              ?presignedUrlConfig:(Option.map
                                     ~f:Values.PresignedUrlConfig.of_json
                                     presignedUrlConfig)
              ?targetSelection:(Option.map ~f:Values.TargetSelection.of_json
                                  targetSelection)
              ?jobExecutionsRolloutConfig:(Option.map
                                             ~f:Values.JobExecutionsRolloutConfig.of_json
                                             jobExecutionsRolloutConfig)
              ?abortConfig:(Option.map ~f:Values.AbortConfig.of_json
                              abortConfig)
              ?timeoutConfig:(Option.map ~f:Values.TimeoutConfig.of_json
                                timeoutConfig)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?namespaceId
              ?jobTemplateArn
              ?jobExecutionsRetryConfig:(Option.map
                                           ~f:Values.JobExecutionsRetryConfig.of_json
                                           jobExecutionsRetryConfig)
              ?documentParameters:(Option.map ~f:Values.ParameterMap.of_json
                                     documentParameters) ~jobId
              ~targets:(Values.JobTargets.of_json targets) ())
           (Some Values.CreateJobResponse.to_json)
           (Some Values.CreateJobResponse.error_to_json)])
let create_job_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobArn = flag "job-arn" (optional string) ~doc:"STRING JobArn"
       and documentSource =
         flag "document-source" (optional string)
           ~doc:"STRING JobDocumentSource"
       and document =
         flag "document" (optional string) ~doc:"STRING JobDocument"
       and presignedUrlConfig =
         flag "presigned-url-config" (optional json_arg)
           ~doc:"JSON PresignedUrlConfig"
       and jobExecutionsRolloutConfig =
         flag "job-executions-rollout-config" (optional json_arg)
           ~doc:"JSON JobExecutionsRolloutConfig"
       and abortConfig =
         flag "abort-config" (optional json_arg) ~doc:"JSON AbortConfig"
       and timeoutConfig =
         flag "timeout-config" (optional json_arg) ~doc:"JSON TimeoutConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and jobExecutionsRetryConfig =
         flag "job-executions-retry-config" (optional json_arg)
           ~doc:"JSON JobExecutionsRetryConfig"
       and jobTemplateId =
         flag "job-template-id" (required string) ~doc:"STRING JobTemplateId"
       and description =
         flag "description" (required string) ~doc:"STRING JobDescription" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_job_template
           (Values.CreateJobTemplateRequest.make ?jobArn ?documentSource
              ?document
              ?presignedUrlConfig:(Option.map
                                     ~f:Values.PresignedUrlConfig.of_json
                                     presignedUrlConfig)
              ?jobExecutionsRolloutConfig:(Option.map
                                             ~f:Values.JobExecutionsRolloutConfig.of_json
                                             jobExecutionsRolloutConfig)
              ?abortConfig:(Option.map ~f:Values.AbortConfig.of_json
                              abortConfig)
              ?timeoutConfig:(Option.map ~f:Values.TimeoutConfig.of_json
                                timeoutConfig)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?jobExecutionsRetryConfig:(Option.map
                                           ~f:Values.JobExecutionsRetryConfig.of_json
                                           jobExecutionsRetryConfig)
              ~jobTemplateId ~description ())
           (Some Values.CreateJobTemplateResponse.to_json)
           (Some Values.CreateJobTemplateResponse.error_to_json)])
let create_keys_and_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and setAsActive =
         flag "set-as-active" (optional bool) ~doc:"BOOL SetAsActive" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_keys_and_certificate
           (Values.CreateKeysAndCertificateRequest.make ?setAsActive ())
           (Some Values.CreateKeysAndCertificateResponse.to_json)
           (Some Values.CreateKeysAndCertificateResponse.error_to_json)])
let create_mitigation_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and actionName =
         flag "action-name" (required string)
           ~doc:"STRING MitigationActionName"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn"
       and actionParams =
         flag "action-params" (required json_arg)
           ~doc:"JSON MitigationActionParams" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_mitigation_action
           (Values.CreateMitigationActionRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~actionName
              ~roleArn
              ~actionParams:(Values.MitigationActionParams.of_json
                               actionParams) ())
           (Some Values.CreateMitigationActionResponse.to_json)
           (Some Values.CreateMitigationActionResponse.error_to_json)])
let create_o_t_a_update =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING OTAUpdateDescription"
       and protocols =
         flag "protocols" (optional json_arg) ~doc:"JSON Protocols"
       and targetSelection =
         flag "target-selection" (optional json_arg)
           ~doc:"JSON TargetSelection"
       and awsJobExecutionsRolloutConfig =
         flag "aws-job-executions-rollout-config" (optional json_arg)
           ~doc:"JSON AwsJobExecutionsRolloutConfig"
       and awsJobPresignedUrlConfig =
         flag "aws-job-presigned-url-config" (optional json_arg)
           ~doc:"JSON AwsJobPresignedUrlConfig"
       and awsJobAbortConfig =
         flag "aws-job-abort-config" (optional json_arg)
           ~doc:"JSON AwsJobAbortConfig"
       and awsJobTimeoutConfig =
         flag "aws-job-timeout-config" (optional json_arg)
           ~doc:"JSON AwsJobTimeoutConfig"
       and additionalParameters =
         flag "additional-parameters" (optional json_arg)
           ~doc:"JSON AdditionalParameterMap"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and otaUpdateId =
         flag "ota-update-id" (required string) ~doc:"STRING OTAUpdateId"
       and targets = flag "targets" (required json_arg) ~doc:"JSON Targets"
       and files =
         flag "files" (required json_arg) ~doc:"JSON OTAUpdateFiles"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_o_t_a_update
           (Values.CreateOTAUpdateRequest.make ?description
              ?protocols:(Option.map ~f:Values.Protocols.of_json protocols)
              ?targetSelection:(Option.map ~f:Values.TargetSelection.of_json
                                  targetSelection)
              ?awsJobExecutionsRolloutConfig:(Option.map
                                                ~f:Values.AwsJobExecutionsRolloutConfig.of_json
                                                awsJobExecutionsRolloutConfig)
              ?awsJobPresignedUrlConfig:(Option.map
                                           ~f:Values.AwsJobPresignedUrlConfig.of_json
                                           awsJobPresignedUrlConfig)
              ?awsJobAbortConfig:(Option.map
                                    ~f:Values.AwsJobAbortConfig.of_json
                                    awsJobAbortConfig)
              ?awsJobTimeoutConfig:(Option.map
                                      ~f:Values.AwsJobTimeoutConfig.of_json
                                      awsJobTimeoutConfig)
              ?additionalParameters:(Option.map
                                       ~f:Values.AdditionalParameterMap.of_json
                                       additionalParameters)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~otaUpdateId
              ~targets:(Values.Targets.of_json targets)
              ~files:(Values.OTAUpdateFiles.of_json files) ~roleArn ())
           (Some Values.CreateOTAUpdateResponse.to_json)
           (Some Values.CreateOTAUpdateResponse.error_to_json)])
let create_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING PolicyName"
       and policyDocument =
         flag "policy-document" (required string)
           ~doc:"STRING PolicyDocument" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_policy
           (Values.CreatePolicyRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~policyName
              ~policyDocument ()) (Some Values.CreatePolicyResponse.to_json)
           (Some Values.CreatePolicyResponse.error_to_json)])
let create_policy_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and setAsDefault =
         flag "set-as-default" (optional bool) ~doc:"BOOL SetAsDefault"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING PolicyName"
       and policyDocument =
         flag "policy-document" (required string)
           ~doc:"STRING PolicyDocument" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_policy_version
           (Values.CreatePolicyVersionRequest.make ?setAsDefault ~policyName
              ~policyDocument ())
           (Some Values.CreatePolicyVersionResponse.to_json)
           (Some Values.CreatePolicyVersionResponse.error_to_json)])
let create_provisioning_claim =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING TemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_provisioning_claim
           (Values.CreateProvisioningClaimRequest.make ~templateName ())
           (Some Values.CreateProvisioningClaimResponse.to_json)
           (Some Values.CreateProvisioningClaimResponse.error_to_json)])
let create_provisioning_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING TemplateDescription"
       and enabled = flag "enabled" (optional bool) ~doc:"BOOL Enabled"
       and preProvisioningHook =
         flag "pre-provisioning-hook" (optional json_arg)
           ~doc:"JSON ProvisioningHook"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING TemplateName"
       and templateBody =
         flag "template-body" (required string) ~doc:"STRING TemplateBody"
       and provisioningRoleArn =
         flag "provisioning-role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_provisioning_template
           (Values.CreateProvisioningTemplateRequest.make ?description
              ?enabled
              ?preProvisioningHook:(Option.map
                                      ~f:Values.ProvisioningHook.of_json
                                      preProvisioningHook)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~templateName
              ~templateBody ~provisioningRoleArn ())
           (Some Values.CreateProvisioningTemplateResponse.to_json)
           (Some Values.CreateProvisioningTemplateResponse.error_to_json)])
let create_provisioning_template_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and setAsDefault =
         flag "set-as-default" (optional bool) ~doc:"BOOL SetAsDefault"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING TemplateName"
       and templateBody =
         flag "template-body" (required string) ~doc:"STRING TemplateBody" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_provisioning_template_version
           (Values.CreateProvisioningTemplateVersionRequest.make
              ?setAsDefault ~templateName ~templateBody ())
           (Some Values.CreateProvisioningTemplateVersionResponse.to_json)
           (Some
              Values.CreateProvisioningTemplateVersionResponse.error_to_json)])
let create_role_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and credentialDurationSeconds =
         flag "credential-duration-seconds" (optional int)
           ~doc:"INT CredentialDurationSeconds"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and roleAlias =
         flag "role-alias" (required string) ~doc:"STRING RoleAlias"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_role_alias
           (Values.CreateRoleAliasRequest.make ?credentialDurationSeconds
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~roleAlias
              ~roleArn ()) (Some Values.CreateRoleAliasResponse.to_json)
           (Some Values.CreateRoleAliasResponse.error_to_json)])
let create_scheduled_audit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dayOfMonth =
         flag "day-of-month" (optional string) ~doc:"STRING DayOfMonth"
       and dayOfWeek =
         flag "day-of-week" (optional json_arg) ~doc:"JSON DayOfWeek"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and frequency =
         flag "frequency" (required json_arg) ~doc:"JSON AuditFrequency"
       and targetCheckNames =
         flag "target-check-names" (required json_arg)
           ~doc:"JSON TargetAuditCheckNames"
       and scheduledAuditName =
         flag "scheduled-audit-name" (required string)
           ~doc:"STRING ScheduledAuditName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_scheduled_audit
           (Values.CreateScheduledAuditRequest.make ?dayOfMonth
              ?dayOfWeek:(Option.map ~f:Values.DayOfWeek.of_json dayOfWeek)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~frequency:(Values.AuditFrequency.of_json frequency)
              ~targetCheckNames:(Values.TargetAuditCheckNames.of_json
                                   targetCheckNames) ~scheduledAuditName ())
           (Some Values.CreateScheduledAuditResponse.to_json)
           (Some Values.CreateScheduledAuditResponse.error_to_json)])
let create_security_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and securityProfileDescription =
         flag "security-profile-description" (optional string)
           ~doc:"STRING SecurityProfileDescription"
       and behaviors =
         flag "behaviors" (optional json_arg) ~doc:"JSON Behaviors"
       and alertTargets =
         flag "alert-targets" (optional json_arg) ~doc:"JSON AlertTargets"
       and additionalMetricsToRetain =
         flag "additional-metrics-to-retain" (optional json_arg)
           ~doc:"JSON AdditionalMetricsToRetainList"
       and additionalMetricsToRetainV2 =
         flag "additional-metrics-to-retain-v2" (optional json_arg)
           ~doc:"JSON AdditionalMetricsToRetainV2List"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and securityProfileName =
         flag "security-profile-name" (required string)
           ~doc:"STRING SecurityProfileName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_security_profile
           (Values.CreateSecurityProfileRequest.make
              ?securityProfileDescription
              ?behaviors:(Option.map ~f:Values.Behaviors.of_json behaviors)
              ?alertTargets:(Option.map ~f:Values.AlertTargets.of_json
                               alertTargets)
              ?additionalMetricsToRetain:(Option.map
                                            ~f:Values.AdditionalMetricsToRetainList.of_json
                                            additionalMetricsToRetain)
              ?additionalMetricsToRetainV2:(Option.map
                                              ~f:Values.AdditionalMetricsToRetainV2List.of_json
                                              additionalMetricsToRetainV2)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~securityProfileName ())
           (Some Values.CreateSecurityProfileResponse.to_json)
           (Some Values.CreateSecurityProfileResponse.error_to_json)])
let create_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING StreamDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and streamId =
         flag "stream-id" (required string) ~doc:"STRING StreamId"
       and files = flag "files" (required json_arg) ~doc:"JSON StreamFiles"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_stream
           (Values.CreateStreamRequest.make ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~streamId
              ~files:(Values.StreamFiles.of_json files) ~roleArn ())
           (Some Values.CreateStreamResponse.to_json)
           (Some Values.CreateStreamResponse.error_to_json)])
let create_thing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thingTypeName =
         flag "thing-type-name" (optional string) ~doc:"STRING ThingTypeName"
       and attributePayload =
         flag "attribute-payload" (optional json_arg)
           ~doc:"JSON AttributePayload"
       and billingGroupName =
         flag "billing-group-name" (optional string)
           ~doc:"STRING BillingGroupName"
       and thingName =
         flag "thing-name" (required string) ~doc:"STRING ThingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_thing
           (Values.CreateThingRequest.make ?thingTypeName
              ?attributePayload:(Option.map
                                   ~f:Values.AttributePayload.of_json
                                   attributePayload) ?billingGroupName
              ~thingName ()) (Some Values.CreateThingResponse.to_json)
           (Some Values.CreateThingResponse.error_to_json)])
let create_thing_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parentGroupName =
         flag "parent-group-name" (optional string)
           ~doc:"STRING ThingGroupName"
       and thingGroupProperties =
         flag "thing-group-properties" (optional json_arg)
           ~doc:"JSON ThingGroupProperties"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and thingGroupName =
         flag "thing-group-name" (required string)
           ~doc:"STRING ThingGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_thing_group
           (Values.CreateThingGroupRequest.make ?parentGroupName
              ?thingGroupProperties:(Option.map
                                       ~f:Values.ThingGroupProperties.of_json
                                       thingGroupProperties)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~thingGroupName ())
           (Some Values.CreateThingGroupResponse.to_json)
           (Some Values.CreateThingGroupResponse.error_to_json)])
let create_thing_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thingTypeProperties =
         flag "thing-type-properties" (optional json_arg)
           ~doc:"JSON ThingTypeProperties"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and thingTypeName =
         flag "thing-type-name" (required string) ~doc:"STRING ThingTypeName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_thing_type
           (Values.CreateThingTypeRequest.make
              ?thingTypeProperties:(Option.map
                                      ~f:Values.ThingTypeProperties.of_json
                                      thingTypeProperties)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~thingTypeName ())
           (Some Values.CreateThingTypeResponse.to_json)
           (Some Values.CreateThingTypeResponse.error_to_json)])
let create_topic_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional string) ~doc:"STRING String"
       and ruleName =
         flag "rule-name" (required string) ~doc:"STRING RuleName"
       and topicRulePayload =
         flag "topic-rule-payload" (required json_arg)
           ~doc:"JSON TopicRulePayload" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_topic_rule
           (Values.CreateTopicRuleRequest.make ?tags ~ruleName
              ~topicRulePayload:(Values.TopicRulePayload.of_json
                                   topicRulePayload) ()) None None])
let create_topic_rule_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and destinationConfiguration =
         flag "destination-configuration" (required json_arg)
           ~doc:"JSON TopicRuleDestinationConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_topic_rule_destination
           (Values.CreateTopicRuleDestinationRequest.make
              ~destinationConfiguration:(Values.TopicRuleDestinationConfiguration.of_json
                                           destinationConfiguration) ())
           (Some Values.CreateTopicRuleDestinationResponse.to_json)
           (Some Values.CreateTopicRuleDestinationResponse.error_to_json)])
let delete_account_audit_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deleteScheduledAudits =
         flag "delete-scheduled-audits" (optional bool)
           ~doc:"BOOL DeleteScheduledAudits" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_account_audit_configuration
           (Values.DeleteAccountAuditConfigurationRequest.make
              ?deleteScheduledAudits ())
           (Some Values.DeleteAccountAuditConfigurationResponse.to_json)
           (Some Values.DeleteAccountAuditConfigurationResponse.error_to_json)])
let delete_audit_suppression =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and checkName =
         flag "check-name" (required string) ~doc:"STRING AuditCheckName"
       and resourceIdentifier =
         flag "resource-identifier" (required json_arg)
           ~doc:"JSON ResourceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_audit_suppression
           (Values.DeleteAuditSuppressionRequest.make ~checkName
              ~resourceIdentifier:(Values.ResourceIdentifier.of_json
                                     resourceIdentifier) ())
           (Some Values.DeleteAuditSuppressionResponse.to_json)
           (Some Values.DeleteAuditSuppressionResponse.error_to_json)])
let delete_authorizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authorizerName =
         flag "authorizer-name" (required string)
           ~doc:"STRING AuthorizerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_authorizer
           (Values.DeleteAuthorizerRequest.make ~authorizerName ())
           (Some Values.DeleteAuthorizerResponse.to_json)
           (Some Values.DeleteAuthorizerResponse.error_to_json)])
let delete_billing_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedVersion =
         flag "expected-version" (optional json_arg)
           ~doc:"JSON OptionalVersion"
       and billingGroupName =
         flag "billing-group-name" (required string)
           ~doc:"STRING BillingGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_billing_group
           (Values.DeleteBillingGroupRequest.make
              ?expectedVersion:(Option.map ~f:Values.OptionalVersion.of_json
                                  expectedVersion) ~billingGroupName ())
           (Some Values.DeleteBillingGroupResponse.to_json)
           (Some Values.DeleteBillingGroupResponse.error_to_json)])
let delete_c_a_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateId =
         flag "certificate-id" (required string) ~doc:"STRING CertificateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_c_a_certificate
           (Values.DeleteCACertificateRequest.make ~certificateId ())
           (Some Values.DeleteCACertificateResponse.to_json)
           (Some Values.DeleteCACertificateResponse.error_to_json)])
let delete_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forceDelete =
         flag "force-delete" (optional bool) ~doc:"BOOL ForceDelete"
       and certificateId =
         flag "certificate-id" (required string) ~doc:"STRING CertificateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_certificate
           (Values.DeleteCertificateRequest.make ?forceDelete ~certificateId
              ()) None None])
let delete_custom_metric =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metricName =
         flag "metric-name" (required string) ~doc:"STRING MetricName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_custom_metric
           (Values.DeleteCustomMetricRequest.make ~metricName ())
           (Some Values.DeleteCustomMetricResponse.to_json)
           (Some Values.DeleteCustomMetricResponse.error_to_json)])
let delete_dimension =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING DimensionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_dimension (Values.DeleteDimensionRequest.make ~name ())
           (Some Values.DeleteDimensionResponse.to_json)
           (Some Values.DeleteDimensionResponse.error_to_json)])
let delete_domain_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainConfigurationName =
         flag "domain-configuration-name" (required string)
           ~doc:"STRING DomainConfigurationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_domain_configuration
           (Values.DeleteDomainConfigurationRequest.make
              ~domainConfigurationName ())
           (Some Values.DeleteDomainConfigurationResponse.to_json)
           (Some Values.DeleteDomainConfigurationResponse.error_to_json)])
let delete_dynamic_thing_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedVersion =
         flag "expected-version" (optional json_arg)
           ~doc:"JSON OptionalVersion"
       and thingGroupName =
         flag "thing-group-name" (required string)
           ~doc:"STRING ThingGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_dynamic_thing_group
           (Values.DeleteDynamicThingGroupRequest.make
              ?expectedVersion:(Option.map ~f:Values.OptionalVersion.of_json
                                  expectedVersion) ~thingGroupName ())
           (Some Values.DeleteDynamicThingGroupResponse.to_json)
           (Some Values.DeleteDynamicThingGroupResponse.error_to_json)])
let delete_fleet_metric =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedVersion =
         flag "expected-version" (optional json_arg)
           ~doc:"JSON OptionalVersion"
       and metricName =
         flag "metric-name" (required string) ~doc:"STRING FleetMetricName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_fleet_metric
           (Values.DeleteFleetMetricRequest.make
              ?expectedVersion:(Option.map ~f:Values.OptionalVersion.of_json
                                  expectedVersion) ~metricName ()) None None])
let delete_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and force = flag "force" (optional bool) ~doc:"BOOL ForceFlag"
       and namespaceId =
         flag "namespace-id" (optional string) ~doc:"STRING NamespaceId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_job
           (Values.DeleteJobRequest.make ?force ?namespaceId ~jobId ()) None
           None])
let delete_job_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and force = flag "force" (optional bool) ~doc:"BOOL ForceFlag"
       and namespaceId =
         flag "namespace-id" (optional string) ~doc:"STRING NamespaceId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId"
       and thingName =
         flag "thing-name" (required string) ~doc:"STRING ThingName"
       and executionNumber =
         flag "execution-number" (required json_arg)
           ~doc:"JSON ExecutionNumber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_job_execution
           (Values.DeleteJobExecutionRequest.make ?force ?namespaceId ~jobId
              ~thingName
              ~executionNumber:(Values.ExecutionNumber.of_json
                                  executionNumber) ()) None None])
let delete_job_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobTemplateId =
         flag "job-template-id" (required string) ~doc:"STRING JobTemplateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_job_template
           (Values.DeleteJobTemplateRequest.make ~jobTemplateId ()) None None])
let delete_mitigation_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and actionName =
         flag "action-name" (required string)
           ~doc:"STRING MitigationActionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_mitigation_action
           (Values.DeleteMitigationActionRequest.make ~actionName ())
           (Some Values.DeleteMitigationActionResponse.to_json)
           (Some Values.DeleteMitigationActionResponse.error_to_json)])
let delete_o_t_a_update =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deleteStream =
         flag "delete-stream" (optional bool) ~doc:"BOOL DeleteStream"
       and forceDeleteAWSJob =
         flag "force-delete-a-w-s-job" (optional bool)
           ~doc:"BOOL ForceDeleteAWSJob"
       and otaUpdateId =
         flag "ota-update-id" (required string) ~doc:"STRING OTAUpdateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_o_t_a_update
           (Values.DeleteOTAUpdateRequest.make ?deleteStream
              ?forceDeleteAWSJob ~otaUpdateId ())
           (Some Values.DeleteOTAUpdateResponse.to_json)
           (Some Values.DeleteOTAUpdateResponse.error_to_json)])
let delete_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING PolicyName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_policy (Values.DeletePolicyRequest.make ~policyName ())
           None None])
let delete_policy_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING PolicyName"
       and policyVersionId =
         flag "policy-version-id" (required string)
           ~doc:"STRING PolicyVersionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_policy_version
           (Values.DeletePolicyVersionRequest.make ~policyName
              ~policyVersionId ()) None None])
let delete_provisioning_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING TemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_provisioning_template
           (Values.DeleteProvisioningTemplateRequest.make ~templateName ())
           (Some Values.DeleteProvisioningTemplateResponse.to_json)
           (Some Values.DeleteProvisioningTemplateResponse.error_to_json)])
let delete_provisioning_template_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING TemplateName"
       and versionId =
         flag "version-id" (required int) ~doc:"INT TemplateVersionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_provisioning_template_version
           (Values.DeleteProvisioningTemplateVersionRequest.make
              ~templateName ~versionId ())
           (Some Values.DeleteProvisioningTemplateVersionResponse.to_json)
           (Some
              Values.DeleteProvisioningTemplateVersionResponse.error_to_json)])
let delete_registration_code =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_registration_code
           (Values.DeleteRegistrationCodeRequest.make ())
           (Some Values.DeleteRegistrationCodeResponse.to_json)
           (Some Values.DeleteRegistrationCodeResponse.error_to_json)])
let delete_role_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleAlias =
         flag "role-alias" (required string) ~doc:"STRING RoleAlias" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_role_alias
           (Values.DeleteRoleAliasRequest.make ~roleAlias ())
           (Some Values.DeleteRoleAliasResponse.to_json)
           (Some Values.DeleteRoleAliasResponse.error_to_json)])
let delete_scheduled_audit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scheduledAuditName =
         flag "scheduled-audit-name" (required string)
           ~doc:"STRING ScheduledAuditName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_scheduled_audit
           (Values.DeleteScheduledAuditRequest.make ~scheduledAuditName ())
           (Some Values.DeleteScheduledAuditResponse.to_json)
           (Some Values.DeleteScheduledAuditResponse.error_to_json)])
let delete_security_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedVersion =
         flag "expected-version" (optional json_arg)
           ~doc:"JSON OptionalVersion"
       and securityProfileName =
         flag "security-profile-name" (required string)
           ~doc:"STRING SecurityProfileName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_security_profile
           (Values.DeleteSecurityProfileRequest.make
              ?expectedVersion:(Option.map ~f:Values.OptionalVersion.of_json
                                  expectedVersion) ~securityProfileName ())
           (Some Values.DeleteSecurityProfileResponse.to_json)
           (Some Values.DeleteSecurityProfileResponse.error_to_json)])
let delete_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and streamId =
         flag "stream-id" (required string) ~doc:"STRING StreamId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_stream (Values.DeleteStreamRequest.make ~streamId ())
           (Some Values.DeleteStreamResponse.to_json)
           (Some Values.DeleteStreamResponse.error_to_json)])
let delete_thing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedVersion =
         flag "expected-version" (optional json_arg)
           ~doc:"JSON OptionalVersion"
       and thingName =
         flag "thing-name" (required string) ~doc:"STRING ThingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_thing
           (Values.DeleteThingRequest.make
              ?expectedVersion:(Option.map ~f:Values.OptionalVersion.of_json
                                  expectedVersion) ~thingName ())
           (Some Values.DeleteThingResponse.to_json)
           (Some Values.DeleteThingResponse.error_to_json)])
let delete_thing_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedVersion =
         flag "expected-version" (optional json_arg)
           ~doc:"JSON OptionalVersion"
       and thingGroupName =
         flag "thing-group-name" (required string)
           ~doc:"STRING ThingGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_thing_group
           (Values.DeleteThingGroupRequest.make
              ?expectedVersion:(Option.map ~f:Values.OptionalVersion.of_json
                                  expectedVersion) ~thingGroupName ())
           (Some Values.DeleteThingGroupResponse.to_json)
           (Some Values.DeleteThingGroupResponse.error_to_json)])
let delete_thing_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thingTypeName =
         flag "thing-type-name" (required string) ~doc:"STRING ThingTypeName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_thing_type
           (Values.DeleteThingTypeRequest.make ~thingTypeName ())
           (Some Values.DeleteThingTypeResponse.to_json)
           (Some Values.DeleteThingTypeResponse.error_to_json)])
let delete_topic_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleName =
         flag "rule-name" (required string) ~doc:"STRING RuleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_topic_rule
           (Values.DeleteTopicRuleRequest.make ~ruleName ()) None None])
let delete_topic_rule_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING AwsArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_topic_rule_destination
           (Values.DeleteTopicRuleDestinationRequest.make ~arn ())
           (Some Values.DeleteTopicRuleDestinationResponse.to_json)
           (Some Values.DeleteTopicRuleDestinationResponse.error_to_json)])
let delete_v2_logging_level =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetType =
         flag "target-type" (required json_arg) ~doc:"JSON LogTargetType"
       and targetName =
         flag "target-name" (required string) ~doc:"STRING LogTargetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_v2_logging_level
           (Values.DeleteV2LoggingLevelRequest.make
              ~targetType:(Values.LogTargetType.of_json targetType)
              ~targetName ()) None None])
let deprecate_thing_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and undoDeprecate =
         flag "undo-deprecate" (optional bool) ~doc:"BOOL UndoDeprecate"
       and thingTypeName =
         flag "thing-type-name" (required string) ~doc:"STRING ThingTypeName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deprecate_thing_type
           (Values.DeprecateThingTypeRequest.make ?undoDeprecate
              ~thingTypeName ())
           (Some Values.DeprecateThingTypeResponse.to_json)
           (Some Values.DeprecateThingTypeResponse.error_to_json)])
let describe_account_audit_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_account_audit_configuration
           (Values.DescribeAccountAuditConfigurationRequest.make ())
           (Some Values.DescribeAccountAuditConfigurationResponse.to_json)
           (Some
              Values.DescribeAccountAuditConfigurationResponse.error_to_json)])
let describe_audit_finding =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and findingId =
         flag "finding-id" (required string) ~doc:"STRING FindingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_audit_finding
           (Values.DescribeAuditFindingRequest.make ~findingId ())
           (Some Values.DescribeAuditFindingResponse.to_json)
           (Some Values.DescribeAuditFindingResponse.error_to_json)])
let describe_audit_mitigation_actions_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskId =
         flag "task-id" (required string)
           ~doc:"STRING MitigationActionsTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_audit_mitigation_actions_task
           (Values.DescribeAuditMitigationActionsTaskRequest.make ~taskId ())
           (Some Values.DescribeAuditMitigationActionsTaskResponse.to_json)
           (Some
              Values.DescribeAuditMitigationActionsTaskResponse.error_to_json)])
let describe_audit_suppression =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and checkName =
         flag "check-name" (required string) ~doc:"STRING AuditCheckName"
       and resourceIdentifier =
         flag "resource-identifier" (required json_arg)
           ~doc:"JSON ResourceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_audit_suppression
           (Values.DescribeAuditSuppressionRequest.make ~checkName
              ~resourceIdentifier:(Values.ResourceIdentifier.of_json
                                     resourceIdentifier) ())
           (Some Values.DescribeAuditSuppressionResponse.to_json)
           (Some Values.DescribeAuditSuppressionResponse.error_to_json)])
let describe_audit_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskId =
         flag "task-id" (required string) ~doc:"STRING AuditTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_audit_task
           (Values.DescribeAuditTaskRequest.make ~taskId ())
           (Some Values.DescribeAuditTaskResponse.to_json)
           (Some Values.DescribeAuditTaskResponse.error_to_json)])
let describe_authorizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authorizerName =
         flag "authorizer-name" (required string)
           ~doc:"STRING AuthorizerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_authorizer
           (Values.DescribeAuthorizerRequest.make ~authorizerName ())
           (Some Values.DescribeAuthorizerResponse.to_json)
           (Some Values.DescribeAuthorizerResponse.error_to_json)])
let describe_billing_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and billingGroupName =
         flag "billing-group-name" (required string)
           ~doc:"STRING BillingGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_billing_group
           (Values.DescribeBillingGroupRequest.make ~billingGroupName ())
           (Some Values.DescribeBillingGroupResponse.to_json)
           (Some Values.DescribeBillingGroupResponse.error_to_json)])
let describe_c_a_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateId =
         flag "certificate-id" (required string) ~doc:"STRING CertificateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_c_a_certificate
           (Values.DescribeCACertificateRequest.make ~certificateId ())
           (Some Values.DescribeCACertificateResponse.to_json)
           (Some Values.DescribeCACertificateResponse.error_to_json)])
let describe_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateId =
         flag "certificate-id" (required string) ~doc:"STRING CertificateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_certificate
           (Values.DescribeCertificateRequest.make ~certificateId ())
           (Some Values.DescribeCertificateResponse.to_json)
           (Some Values.DescribeCertificateResponse.error_to_json)])
let describe_custom_metric =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metricName =
         flag "metric-name" (required string) ~doc:"STRING MetricName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_custom_metric
           (Values.DescribeCustomMetricRequest.make ~metricName ())
           (Some Values.DescribeCustomMetricResponse.to_json)
           (Some Values.DescribeCustomMetricResponse.error_to_json)])
let describe_default_authorizer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_default_authorizer
           (Values.DescribeDefaultAuthorizerRequest.make ())
           (Some Values.DescribeDefaultAuthorizerResponse.to_json)
           (Some Values.DescribeDefaultAuthorizerResponse.error_to_json)])
let describe_detect_mitigation_actions_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskId =
         flag "task-id" (required string)
           ~doc:"STRING MitigationActionsTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_detect_mitigation_actions_task
           (Values.DescribeDetectMitigationActionsTaskRequest.make ~taskId ())
           (Some Values.DescribeDetectMitigationActionsTaskResponse.to_json)
           (Some
              Values.DescribeDetectMitigationActionsTaskResponse.error_to_json)])
let describe_dimension =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING DimensionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_dimension
           (Values.DescribeDimensionRequest.make ~name ())
           (Some Values.DescribeDimensionResponse.to_json)
           (Some Values.DescribeDimensionResponse.error_to_json)])
let describe_domain_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainConfigurationName =
         flag "domain-configuration-name" (required string)
           ~doc:"STRING ReservedDomainConfigurationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_domain_configuration
           (Values.DescribeDomainConfigurationRequest.make
              ~domainConfigurationName ())
           (Some Values.DescribeDomainConfigurationResponse.to_json)
           (Some Values.DescribeDomainConfigurationResponse.error_to_json)])
let describe_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointType =
         flag "endpoint-type" (optional string) ~doc:"STRING EndpointType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_endpoint
           (Values.DescribeEndpointRequest.make ?endpointType ())
           (Some Values.DescribeEndpointResponse.to_json)
           (Some Values.DescribeEndpointResponse.error_to_json)])
let describe_event_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_event_configurations
           (Values.DescribeEventConfigurationsRequest.make ())
           (Some Values.DescribeEventConfigurationsResponse.to_json)
           (Some Values.DescribeEventConfigurationsResponse.error_to_json)])
let describe_fleet_metric =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and metricName =
         flag "metric-name" (required string) ~doc:"STRING FleetMetricName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_fleet_metric
           (Values.DescribeFleetMetricRequest.make ~metricName ())
           (Some Values.DescribeFleetMetricResponse.to_json)
           (Some Values.DescribeFleetMetricResponse.error_to_json)])
let describe_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and indexName =
         flag "index-name" (required string) ~doc:"STRING IndexName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_index (Values.DescribeIndexRequest.make ~indexName ())
           (Some Values.DescribeIndexResponse.to_json)
           (Some Values.DescribeIndexResponse.error_to_json)])
let describe_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_job (Values.DescribeJobRequest.make ~jobId ())
           (Some Values.DescribeJobResponse.to_json)
           (Some Values.DescribeJobResponse.error_to_json)])
let describe_job_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and executionNumber =
         flag "execution-number" (optional json_arg)
           ~doc:"JSON ExecutionNumber"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId"
       and thingName =
         flag "thing-name" (required string) ~doc:"STRING ThingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_job_execution
           (Values.DescribeJobExecutionRequest.make
              ?executionNumber:(Option.map ~f:Values.ExecutionNumber.of_json
                                  executionNumber) ~jobId ~thingName ())
           (Some Values.DescribeJobExecutionResponse.to_json)
           (Some Values.DescribeJobExecutionResponse.error_to_json)])
let describe_job_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobTemplateId =
         flag "job-template-id" (required string) ~doc:"STRING JobTemplateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_job_template
           (Values.DescribeJobTemplateRequest.make ~jobTemplateId ())
           (Some Values.DescribeJobTemplateResponse.to_json)
           (Some Values.DescribeJobTemplateResponse.error_to_json)])
let describe_managed_job_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateVersion =
         flag "template-version" (optional string)
           ~doc:"STRING ManagedTemplateVersion"
       and templateName =
         flag "template-name" (required string)
           ~doc:"STRING ManagedJobTemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_managed_job_template
           (Values.DescribeManagedJobTemplateRequest.make ?templateVersion
              ~templateName ())
           (Some Values.DescribeManagedJobTemplateResponse.to_json)
           (Some Values.DescribeManagedJobTemplateResponse.error_to_json)])
let describe_mitigation_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and actionName =
         flag "action-name" (required string)
           ~doc:"STRING MitigationActionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_mitigation_action
           (Values.DescribeMitigationActionRequest.make ~actionName ())
           (Some Values.DescribeMitigationActionResponse.to_json)
           (Some Values.DescribeMitigationActionResponse.error_to_json)])
let describe_provisioning_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING TemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_provisioning_template
           (Values.DescribeProvisioningTemplateRequest.make ~templateName ())
           (Some Values.DescribeProvisioningTemplateResponse.to_json)
           (Some Values.DescribeProvisioningTemplateResponse.error_to_json)])
let describe_provisioning_template_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (required string) ~doc:"STRING TemplateName"
       and versionId =
         flag "version-id" (required int) ~doc:"INT TemplateVersionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_provisioning_template_version
           (Values.DescribeProvisioningTemplateVersionRequest.make
              ~templateName ~versionId ())
           (Some Values.DescribeProvisioningTemplateVersionResponse.to_json)
           (Some
              Values.DescribeProvisioningTemplateVersionResponse.error_to_json)])
let describe_role_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleAlias =
         flag "role-alias" (required string) ~doc:"STRING RoleAlias" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_role_alias
           (Values.DescribeRoleAliasRequest.make ~roleAlias ())
           (Some Values.DescribeRoleAliasResponse.to_json)
           (Some Values.DescribeRoleAliasResponse.error_to_json)])
let describe_scheduled_audit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scheduledAuditName =
         flag "scheduled-audit-name" (required string)
           ~doc:"STRING ScheduledAuditName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_scheduled_audit
           (Values.DescribeScheduledAuditRequest.make ~scheduledAuditName ())
           (Some Values.DescribeScheduledAuditResponse.to_json)
           (Some Values.DescribeScheduledAuditResponse.error_to_json)])
let describe_security_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and securityProfileName =
         flag "security-profile-name" (required string)
           ~doc:"STRING SecurityProfileName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_security_profile
           (Values.DescribeSecurityProfileRequest.make ~securityProfileName
              ()) (Some Values.DescribeSecurityProfileResponse.to_json)
           (Some Values.DescribeSecurityProfileResponse.error_to_json)])
let describe_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and streamId =
         flag "stream-id" (required string) ~doc:"STRING StreamId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_stream
           (Values.DescribeStreamRequest.make ~streamId ())
           (Some Values.DescribeStreamResponse.to_json)
           (Some Values.DescribeStreamResponse.error_to_json)])
let describe_thing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thingName =
         flag "thing-name" (required string) ~doc:"STRING ThingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_thing (Values.DescribeThingRequest.make ~thingName ())
           (Some Values.DescribeThingResponse.to_json)
           (Some Values.DescribeThingResponse.error_to_json)])
let describe_thing_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thingGroupName =
         flag "thing-group-name" (required string)
           ~doc:"STRING ThingGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_thing_group
           (Values.DescribeThingGroupRequest.make ~thingGroupName ())
           (Some Values.DescribeThingGroupResponse.to_json)
           (Some Values.DescribeThingGroupResponse.error_to_json)])
let describe_thing_registration_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskId = flag "task-id" (required string) ~doc:"STRING TaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_thing_registration_task
           (Values.DescribeThingRegistrationTaskRequest.make ~taskId ())
           (Some Values.DescribeThingRegistrationTaskResponse.to_json)
           (Some Values.DescribeThingRegistrationTaskResponse.error_to_json)])
let describe_thing_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thingTypeName =
         flag "thing-type-name" (required string) ~doc:"STRING ThingTypeName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_thing_type
           (Values.DescribeThingTypeRequest.make ~thingTypeName ())
           (Some Values.DescribeThingTypeResponse.to_json)
           (Some Values.DescribeThingTypeResponse.error_to_json)])
let detach_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING PolicyName"
       and target =
         flag "target" (required string) ~doc:"STRING PolicyTarget" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detach_policy
           (Values.DetachPolicyRequest.make ~policyName ~target ()) None None])
let detach_principal_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING PolicyName"
       and principal =
         flag "principal" (required string) ~doc:"STRING Principal" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detach_principal_policy
           (Values.DetachPrincipalPolicyRequest.make ~policyName ~principal
              ()) None None])
let detach_security_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and securityProfileName =
         flag "security-profile-name" (required string)
           ~doc:"STRING SecurityProfileName"
       and securityProfileTargetArn =
         flag "security-profile-target-arn" (required string)
           ~doc:"STRING SecurityProfileTargetArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detach_security_profile
           (Values.DetachSecurityProfileRequest.make ~securityProfileName
              ~securityProfileTargetArn ())
           (Some Values.DetachSecurityProfileResponse.to_json)
           (Some Values.DetachSecurityProfileResponse.error_to_json)])
let detach_thing_principal =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thingName =
         flag "thing-name" (required string) ~doc:"STRING ThingName"
       and principal =
         flag "principal" (required string) ~doc:"STRING Principal" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.detach_thing_principal
           (Values.DetachThingPrincipalRequest.make ~thingName ~principal ())
           (Some Values.DetachThingPrincipalResponse.to_json)
           (Some Values.DetachThingPrincipalResponse.error_to_json)])
let disable_topic_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleName =
         flag "rule-name" (required string) ~doc:"STRING RuleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_topic_rule
           (Values.DisableTopicRuleRequest.make ~ruleName ()) None None])
let enable_topic_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleName =
         flag "rule-name" (required string) ~doc:"STRING RuleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_topic_rule
           (Values.EnableTopicRuleRequest.make ~ruleName ()) None None])
let get_behavior_model_training_summaries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and securityProfileName =
         flag "security-profile-name" (optional string)
           ~doc:"STRING SecurityProfileName"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT TinyMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_behavior_model_training_summaries
           (Values.GetBehaviorModelTrainingSummariesRequest.make
              ?securityProfileName ?maxResults ?nextToken ())
           (Some Values.GetBehaviorModelTrainingSummariesResponse.to_json)
           (Some
              Values.GetBehaviorModelTrainingSummariesResponse.error_to_json)])
let get_buckets_aggregation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and indexName =
         flag "index-name" (optional string) ~doc:"STRING IndexName"
       and queryVersion =
         flag "query-version" (optional string) ~doc:"STRING QueryVersion"
       and queryString =
         flag "query-string" (required string) ~doc:"STRING QueryString"
       and aggregationField =
         flag "aggregation-field" (required string)
           ~doc:"STRING AggregationField"
       and bucketsAggregationType =
         flag "buckets-aggregation-type" (required json_arg)
           ~doc:"JSON BucketsAggregationType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_buckets_aggregation
           (Values.GetBucketsAggregationRequest.make ?indexName ?queryVersion
              ~queryString ~aggregationField
              ~bucketsAggregationType:(Values.BucketsAggregationType.of_json
                                         bucketsAggregationType) ())
           (Some Values.GetBucketsAggregationResponse.to_json)
           (Some Values.GetBucketsAggregationResponse.error_to_json)])
let get_cardinality =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and indexName =
         flag "index-name" (optional string) ~doc:"STRING IndexName"
       and aggregationField =
         flag "aggregation-field" (optional string)
           ~doc:"STRING AggregationField"
       and queryVersion =
         flag "query-version" (optional string) ~doc:"STRING QueryVersion"
       and queryString =
         flag "query-string" (required string) ~doc:"STRING QueryString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_cardinality
           (Values.GetCardinalityRequest.make ?indexName ?aggregationField
              ?queryVersion ~queryString ())
           (Some Values.GetCardinalityResponse.to_json)
           (Some Values.GetCardinalityResponse.error_to_json)])
let get_effective_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and principal =
         flag "principal" (optional string) ~doc:"STRING Principal"
       and cognitoIdentityPoolId =
         flag "cognito-identity-pool-id" (optional string)
           ~doc:"STRING CognitoIdentityPoolId"
       and thingName =
         flag "thing-name" (optional string) ~doc:"STRING ThingName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_effective_policies
           (Values.GetEffectivePoliciesRequest.make ?principal
              ?cognitoIdentityPoolId ?thingName ())
           (Some Values.GetEffectivePoliciesResponse.to_json)
           (Some Values.GetEffectivePoliciesResponse.error_to_json)])
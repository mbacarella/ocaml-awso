(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_configuration_manager =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING CreateConfigurationManagerInputDescriptionString"
       and name =
         flag "name" (optional string)
           ~doc:"STRING CreateConfigurationManagerInputNameString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and configurationDefinitions =
         flag "configuration-definitions" (required json_arg)
           ~doc:"JSON ConfigurationDefinitionsInputList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_configuration_manager
           (Values.CreateConfigurationManagerInput.make ?description ?name
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ~configurationDefinitions:(Values.ConfigurationDefinitionsInputList.of_json
                                           configurationDefinitions) ())
           (Some Values.CreateConfigurationManagerOutput.to_json)
           (Some Values.CreateConfigurationManagerOutput.error_to_json)])
let delete_configuration_manager =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and managerArn =
         flag "manager-arn" (required string)
           ~doc:"STRING DeleteConfigurationManagerInputManagerArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_configuration_manager
           (Values.DeleteConfigurationManagerInput.make ~managerArn ()) None
           None])
let get_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationId =
         flag "configuration-id" (required string)
           ~doc:"STRING GetConfigurationInputConfigurationIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_configuration
           (Values.GetConfigurationInput.make ~configurationId ())
           (Some Values.GetConfigurationOutput.to_json)
           (Some Values.GetConfigurationOutput.error_to_json)])
let get_configuration_manager =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and managerArn =
         flag "manager-arn" (required string)
           ~doc:"STRING GetConfigurationManagerInputManagerArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_configuration_manager
           (Values.GetConfigurationManagerInput.make ~managerArn ())
           (Some Values.GetConfigurationManagerOutput.to_json)
           (Some Values.GetConfigurationManagerOutput.error_to_json)])
let get_service_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service_settings (Fn.id ())
           (Some Values.GetServiceSettingsOutput.to_json)
           (Some Values.GetServiceSettingsOutput.error_to_json)])
let list_configuration_managers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FiltersList"
       and maxItems =
         flag "max-items" (optional int)
           ~doc:"INT ListConfigurationManagersInputMaxItemsInteger"
       and startingToken =
         flag "starting-token" (optional string)
           ~doc:"STRING ListConfigurationManagersInputStartingTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_configuration_managers
           (Values.ListConfigurationManagersInput.make
              ?filters:(Option.map ~f:Values.FiltersList.of_json filters)
              ?maxItems ?startingToken ())
           (Some Values.ListConfigurationManagersOutput.to_json)
           (Some Values.ListConfigurationManagersOutput.error_to_json)])
let list_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationDefinitionId =
         flag "configuration-definition-id" (optional string)
           ~doc:"STRING ListConfigurationsInputConfigurationDefinitionIdString"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FiltersList"
       and managerArn =
         flag "manager-arn" (optional string)
           ~doc:"STRING ListConfigurationsInputManagerArnString"
       and maxItems =
         flag "max-items" (optional int)
           ~doc:"INT ListConfigurationsInputMaxItemsInteger"
       and startingToken =
         flag "starting-token" (optional string)
           ~doc:"STRING ListConfigurationsInputStartingTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_configurations
           (Values.ListConfigurationsInput.make ?configurationDefinitionId
              ?filters:(Option.map ~f:Values.FiltersList.of_json filters)
              ?managerArn ?maxItems ?startingToken ())
           (Some Values.ListConfigurationsOutput.to_json)
           (Some Values.ListConfigurationsOutput.error_to_json)])
let list_quick_setup_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_quick_setup_types (Fn.id ())
           (Some Values.ListQuickSetupTypesOutput.to_json)
           (Some Values.ListQuickSetupTypesOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.TagsMap.of_json tags) ()) None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ()) None None])
let update_configuration_definition =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and localDeploymentAdministrationRoleArn =
         flag "local-deployment-administration-role-arn" (optional string)
           ~doc:"STRING IAMRoleArn"
       and localDeploymentExecutionRoleName =
         flag "local-deployment-execution-role-name" (optional string)
           ~doc:"STRING UpdateConfigurationDefinitionInputLocalDeploymentExecutionRoleNameString"
       and parameters =
         flag "parameters" (optional json_arg)
           ~doc:"JSON ConfigurationParametersMap"
       and typeVersion =
         flag "type-version" (optional string)
           ~doc:"STRING UpdateConfigurationDefinitionInputTypeVersionString"
       and id =
         flag "id" (required string)
           ~doc:"STRING UpdateConfigurationDefinitionInputIdString"
       and managerArn =
         flag "manager-arn" (required string)
           ~doc:"STRING UpdateConfigurationDefinitionInputManagerArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_configuration_definition
           (Values.UpdateConfigurationDefinitionInput.make
              ?localDeploymentAdministrationRoleArn
              ?localDeploymentExecutionRoleName
              ?parameters:(Option.map
                             ~f:Values.ConfigurationParametersMap.of_json
                             parameters) ?typeVersion ~id ~managerArn ())
           None None])
let update_configuration_manager =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING UpdateConfigurationManagerInputDescriptionString"
       and name =
         flag "name" (optional string)
           ~doc:"STRING UpdateConfigurationManagerInputNameString"
       and managerArn =
         flag "manager-arn" (required string)
           ~doc:"STRING UpdateConfigurationManagerInputManagerArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_configuration_manager
           (Values.UpdateConfigurationManagerInput.make ?description ?name
              ~managerArn ()) None None])
let update_service_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and explorerEnablingRoleArn =
         flag "explorer-enabling-role-arn" (optional string)
           ~doc:"STRING IAMRoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_service_settings
           (Values.UpdateServiceSettingsInput.make ?explorerEnablingRoleArn
              ()) None None])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-configuration-manager", create_configuration_manager);
    ("delete-configuration-manager", delete_configuration_manager);
    ("get-configuration", get_configuration);
    ("get-configuration-manager", get_configuration_manager);
    ("get-service-settings", get_service_settings);
    ("list-configuration-managers", list_configuration_managers);
    ("list-configurations", list_configurations);
    ("list-quick-setup-types", list_quick_setup_types);
    ("list-tags-for-resource", list_tags_for_resource);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-configuration-definition", update_configuration_definition);
    ("update-configuration-manager", update_configuration_manager);
    ("update-service-settings", update_service_settings)]
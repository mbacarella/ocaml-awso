(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_auto_predictor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forecastHorizon =
         flag "forecast-horizon" (optional int) ~doc:"INT Integer"
       and forecastTypes =
         flag "forecast-types" (optional json_arg) ~doc:"JSON ForecastTypes"
       and forecastDimensions =
         flag "forecast-dimensions" (optional json_arg)
           ~doc:"JSON ForecastDimensions"
       and forecastFrequency =
         flag "forecast-frequency" (optional string) ~doc:"STRING Frequency"
       and dataConfig =
         flag "data-config" (optional json_arg) ~doc:"JSON DataConfig"
       and encryptionConfig =
         flag "encryption-config" (optional json_arg)
           ~doc:"JSON EncryptionConfig"
       and referencePredictorArn =
         flag "reference-predictor-arn" (optional string) ~doc:"STRING Arn"
       and optimizationMetric =
         flag "optimization-metric" (optional json_arg)
           ~doc:"JSON OptimizationMetric"
       and explainPredictor =
         flag "explain-predictor" (optional bool) ~doc:"BOOL Boolean"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and monitorConfig =
         flag "monitor-config" (optional json_arg) ~doc:"JSON MonitorConfig"
       and timeAlignmentBoundary =
         flag "time-alignment-boundary" (optional json_arg)
           ~doc:"JSON TimeAlignmentBoundary"
       and predictorName =
         flag "predictor-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_auto_predictor
           (Values.CreateAutoPredictorRequest.make ?forecastHorizon
              ?forecastTypes:(Option.map ~f:Values.ForecastTypes.of_json
                                forecastTypes)
              ?forecastDimensions:(Option.map
                                     ~f:Values.ForecastDimensions.of_json
                                     forecastDimensions) ?forecastFrequency
              ?dataConfig:(Option.map ~f:Values.DataConfig.of_json dataConfig)
              ?encryptionConfig:(Option.map
                                   ~f:Values.EncryptionConfig.of_json
                                   encryptionConfig) ?referencePredictorArn
              ?optimizationMetric:(Option.map
                                     ~f:Values.OptimizationMetric.of_json
                                     optimizationMetric) ?explainPredictor
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?monitorConfig:(Option.map ~f:Values.MonitorConfig.of_json
                                monitorConfig)
              ?timeAlignmentBoundary:(Option.map
                                        ~f:Values.TimeAlignmentBoundary.of_json
                                        timeAlignmentBoundary) ~predictorName
              ()) (Some Values.CreateAutoPredictorResponse.to_json)
           (Some Values.CreateAutoPredictorResponse.error_to_json)])
let create_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataFrequency =
         flag "data-frequency" (optional string) ~doc:"STRING Frequency"
       and encryptionConfig =
         flag "encryption-config" (optional json_arg)
           ~doc:"JSON EncryptionConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and datasetName =
         flag "dataset-name" (required string) ~doc:"STRING Name"
       and domain = flag "domain" (required json_arg) ~doc:"JSON Domain"
       and datasetType =
         flag "dataset-type" (required json_arg) ~doc:"JSON DatasetType"
       and schema = flag "schema" (required json_arg) ~doc:"JSON Schema" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_dataset
           (Values.CreateDatasetRequest.make ?dataFrequency
              ?encryptionConfig:(Option.map
                                   ~f:Values.EncryptionConfig.of_json
                                   encryptionConfig)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~datasetName
              ~domain:(Values.Domain.of_json domain)
              ~datasetType:(Values.DatasetType.of_json datasetType)
              ~schema:(Values.Schema.of_json schema) ())
           (Some Values.CreateDatasetResponse.to_json)
           (Some Values.CreateDatasetResponse.error_to_json)])
let create_dataset_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetArns =
         flag "dataset-arns" (optional json_arg) ~doc:"JSON ArnList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and datasetGroupName =
         flag "dataset-group-name" (required string) ~doc:"STRING Name"
       and domain = flag "domain" (required json_arg) ~doc:"JSON Domain" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_dataset_group
           (Values.CreateDatasetGroupRequest.make
              ?datasetArns:(Option.map ~f:Values.ArnList.of_json datasetArns)
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~datasetGroupName ~domain:(Values.Domain.of_json domain) ())
           (Some Values.CreateDatasetGroupResponse.to_json)
           (Some Values.CreateDatasetGroupResponse.error_to_json)])
let create_dataset_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and timestampFormat =
         flag "timestamp-format" (optional string)
           ~doc:"STRING TimestampFormat"
       and timeZone =
         flag "time-zone" (optional string) ~doc:"STRING TimeZone"
       and useGeolocationForTimeZone =
         flag "use-geolocation-for-time-zone" (optional bool)
           ~doc:"BOOL UseGeolocationForTimeZone"
       and geolocationFormat =
         flag "geolocation-format" (optional string)
           ~doc:"STRING GeolocationFormat"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and format = flag "format" (optional string) ~doc:"STRING Format"
       and importMode =
         flag "import-mode" (optional json_arg) ~doc:"JSON ImportMode"
       and datasetImportJobName =
         flag "dataset-import-job-name" (required string) ~doc:"STRING Name"
       and datasetArn =
         flag "dataset-arn" (required string) ~doc:"STRING Arn"
       and dataSource =
         flag "data-source" (required json_arg) ~doc:"JSON DataSource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_dataset_import_job
           (Values.CreateDatasetImportJobRequest.make ?timestampFormat
              ?timeZone ?useGeolocationForTimeZone ?geolocationFormat
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ?format
              ?importMode:(Option.map ~f:Values.ImportMode.of_json importMode)
              ~datasetImportJobName ~datasetArn
              ~dataSource:(Values.DataSource.of_json dataSource) ())
           (Some Values.CreateDatasetImportJobResponse.to_json)
           (Some Values.CreateDatasetImportJobResponse.error_to_json)])
let create_explainability =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataSource =
         flag "data-source" (optional json_arg) ~doc:"JSON DataSource"
       and schema = flag "schema" (optional json_arg) ~doc:"JSON Schema"
       and enableVisualization =
         flag "enable-visualization" (optional bool) ~doc:"BOOL Boolean"
       and startDateTime =
         flag "start-date-time" (optional string) ~doc:"STRING LocalDateTime"
       and endDateTime =
         flag "end-date-time" (optional string) ~doc:"STRING LocalDateTime"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and explainabilityName =
         flag "explainability-name" (required string) ~doc:"STRING Name"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and explainabilityConfig =
         flag "explainability-config" (required json_arg)
           ~doc:"JSON ExplainabilityConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_explainability
           (Values.CreateExplainabilityRequest.make
              ?dataSource:(Option.map ~f:Values.DataSource.of_json dataSource)
              ?schema:(Option.map ~f:Values.Schema.of_json schema)
              ?enableVisualization ?startDateTime ?endDateTime
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~explainabilityName ~resourceArn
              ~explainabilityConfig:(Values.ExplainabilityConfig.of_json
                                       explainabilityConfig) ())
           (Some Values.CreateExplainabilityResponse.to_json)
           (Some Values.CreateExplainabilityResponse.error_to_json)])
let create_explainability_export =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and format = flag "format" (optional string) ~doc:"STRING Format"
       and explainabilityExportName =
         flag "explainability-export-name" (required string)
           ~doc:"STRING Name"
       and explainabilityArn =
         flag "explainability-arn" (required string) ~doc:"STRING Arn"
       and destination =
         flag "destination" (required json_arg) ~doc:"JSON DataDestination" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_explainability_export
           (Values.CreateExplainabilityExportRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ?format
              ~explainabilityExportName ~explainabilityArn
              ~destination:(Values.DataDestination.of_json destination) ())
           (Some Values.CreateExplainabilityExportResponse.to_json)
           (Some Values.CreateExplainabilityExportResponse.error_to_json)])
let create_forecast =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forecastTypes =
         flag "forecast-types" (optional json_arg) ~doc:"JSON ForecastTypes"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and timeSeriesSelector =
         flag "time-series-selector" (optional json_arg)
           ~doc:"JSON TimeSeriesSelector"
       and forecastName =
         flag "forecast-name" (required string) ~doc:"STRING Name"
       and predictorArn =
         flag "predictor-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_forecast
           (Values.CreateForecastRequest.make
              ?forecastTypes:(Option.map ~f:Values.ForecastTypes.of_json
                                forecastTypes)
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?timeSeriesSelector:(Option.map
                                     ~f:Values.TimeSeriesSelector.of_json
                                     timeSeriesSelector) ~forecastName
              ~predictorArn ()) (Some Values.CreateForecastResponse.to_json)
           (Some Values.CreateForecastResponse.error_to_json)])
let create_forecast_export_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and format = flag "format" (optional string) ~doc:"STRING Format"
       and forecastExportJobName =
         flag "forecast-export-job-name" (required string) ~doc:"STRING Name"
       and forecastArn =
         flag "forecast-arn" (required string) ~doc:"STRING Arn"
       and destination =
         flag "destination" (required json_arg) ~doc:"JSON DataDestination" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_forecast_export_job
           (Values.CreateForecastExportJobRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ?format
              ~forecastExportJobName ~forecastArn
              ~destination:(Values.DataDestination.of_json destination) ())
           (Some Values.CreateForecastExportJobResponse.to_json)
           (Some Values.CreateForecastExportJobResponse.error_to_json)])
let create_monitor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and monitorName =
         flag "monitor-name" (required string) ~doc:"STRING Name"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_monitor
           (Values.CreateMonitorRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~monitorName
              ~resourceArn ()) (Some Values.CreateMonitorResponse.to_json)
           (Some Values.CreateMonitorResponse.error_to_json)])
let create_predictor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and algorithmArn =
         flag "algorithm-arn" (optional string) ~doc:"STRING Arn"
       and forecastTypes =
         flag "forecast-types" (optional json_arg) ~doc:"JSON ForecastTypes"
       and performAutoML =
         flag "perform-auto-m-l" (optional bool) ~doc:"BOOL Boolean"
       and autoMLOverrideStrategy =
         flag "auto-m-l-override-strategy" (optional json_arg)
           ~doc:"JSON AutoMLOverrideStrategy"
       and performHPO =
         flag "perform-h-p-o" (optional bool) ~doc:"BOOL Boolean"
       and trainingParameters =
         flag "training-parameters" (optional json_arg)
           ~doc:"JSON TrainingParameters"
       and evaluationParameters =
         flag "evaluation-parameters" (optional json_arg)
           ~doc:"JSON EvaluationParameters"
       and hPOConfig =
         flag "h-p-o-config" (optional json_arg)
           ~doc:"JSON HyperParameterTuningJobConfig"
       and encryptionConfig =
         flag "encryption-config" (optional json_arg)
           ~doc:"JSON EncryptionConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and optimizationMetric =
         flag "optimization-metric" (optional json_arg)
           ~doc:"JSON OptimizationMetric"
       and predictorName =
         flag "predictor-name" (required string) ~doc:"STRING Name"
       and forecastHorizon =
         flag "forecast-horizon" (required int) ~doc:"INT Integer"
       and inputDataConfig =
         flag "input-data-config" (required json_arg)
           ~doc:"JSON InputDataConfig"
       and featurizationConfig =
         flag "featurization-config" (required json_arg)
           ~doc:"JSON FeaturizationConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_predictor
           (Values.CreatePredictorRequest.make ?algorithmArn
              ?forecastTypes:(Option.map ~f:Values.ForecastTypes.of_json
                                forecastTypes) ?performAutoML
              ?autoMLOverrideStrategy:(Option.map
                                         ~f:Values.AutoMLOverrideStrategy.of_json
                                         autoMLOverrideStrategy) ?performHPO
              ?trainingParameters:(Option.map
                                     ~f:Values.TrainingParameters.of_json
                                     trainingParameters)
              ?evaluationParameters:(Option.map
                                       ~f:Values.EvaluationParameters.of_json
                                       evaluationParameters)
              ?hPOConfig:(Option.map
                            ~f:Values.HyperParameterTuningJobConfig.of_json
                            hPOConfig)
              ?encryptionConfig:(Option.map
                                   ~f:Values.EncryptionConfig.of_json
                                   encryptionConfig)
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?optimizationMetric:(Option.map
                                     ~f:Values.OptimizationMetric.of_json
                                     optimizationMetric) ~predictorName
              ~forecastHorizon
              ~inputDataConfig:(Values.InputDataConfig.of_json
                                  inputDataConfig)
              ~featurizationConfig:(Values.FeaturizationConfig.of_json
                                      featurizationConfig) ())
           (Some Values.CreatePredictorResponse.to_json)
           (Some Values.CreatePredictorResponse.error_to_json)])
let create_predictor_backtest_export_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and format = flag "format" (optional string) ~doc:"STRING Format"
       and predictorBacktestExportJobName =
         flag "predictor-backtest-export-job-name" (required string)
           ~doc:"STRING Name"
       and predictorArn =
         flag "predictor-arn" (required string) ~doc:"STRING Arn"
       and destination =
         flag "destination" (required json_arg) ~doc:"JSON DataDestination" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_predictor_backtest_export_job
           (Values.CreatePredictorBacktestExportJobRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ?format
              ~predictorBacktestExportJobName ~predictorArn
              ~destination:(Values.DataDestination.of_json destination) ())
           (Some Values.CreatePredictorBacktestExportJobResponse.to_json)
           (Some
              Values.CreatePredictorBacktestExportJobResponse.error_to_json)])
let create_what_if_analysis =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and timeSeriesSelector =
         flag "time-series-selector" (optional json_arg)
           ~doc:"JSON TimeSeriesSelector"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and whatIfAnalysisName =
         flag "what-if-analysis-name" (required string) ~doc:"STRING Name"
       and forecastArn =
         flag "forecast-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_what_if_analysis
           (Values.CreateWhatIfAnalysisRequest.make
              ?timeSeriesSelector:(Option.map
                                     ~f:Values.TimeSeriesSelector.of_json
                                     timeSeriesSelector)
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~whatIfAnalysisName ~forecastArn ())
           (Some Values.CreateWhatIfAnalysisResponse.to_json)
           (Some Values.CreateWhatIfAnalysisResponse.error_to_json)])
let create_what_if_forecast =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and timeSeriesTransformations =
         flag "time-series-transformations" (optional json_arg)
           ~doc:"JSON TimeSeriesTransformations"
       and timeSeriesReplacementsDataSource =
         flag "time-series-replacements-data-source" (optional json_arg)
           ~doc:"JSON TimeSeriesReplacementsDataSource"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and whatIfForecastName =
         flag "what-if-forecast-name" (required string) ~doc:"STRING Name"
       and whatIfAnalysisArn =
         flag "what-if-analysis-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_what_if_forecast
           (Values.CreateWhatIfForecastRequest.make
              ?timeSeriesTransformations:(Option.map
                                            ~f:Values.TimeSeriesTransformations.of_json
                                            timeSeriesTransformations)
              ?timeSeriesReplacementsDataSource:(Option.map
                                                   ~f:Values.TimeSeriesReplacementsDataSource.of_json
                                                   timeSeriesReplacementsDataSource)
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~whatIfForecastName ~whatIfAnalysisArn ())
           (Some Values.CreateWhatIfForecastResponse.to_json)
           (Some Values.CreateWhatIfForecastResponse.error_to_json)])
let create_what_if_forecast_export =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and format = flag "format" (optional string) ~doc:"STRING Format"
       and whatIfForecastExportName =
         flag "what-if-forecast-export-name" (required string)
           ~doc:"STRING Name"
       and whatIfForecastArns =
         flag "what-if-forecast-arns" (required json_arg)
           ~doc:"JSON WhatIfForecastArnListForExport"
       and destination =
         flag "destination" (required json_arg) ~doc:"JSON DataDestination" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_what_if_forecast_export
           (Values.CreateWhatIfForecastExportRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ?format
              ~whatIfForecastExportName
              ~whatIfForecastArns:(Values.WhatIfForecastArnListForExport.of_json
                                     whatIfForecastArns)
              ~destination:(Values.DataDestination.of_json destination) ())
           (Some Values.CreateWhatIfForecastExportResponse.to_json)
           (Some Values.CreateWhatIfForecastExportResponse.error_to_json)])
let delete_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetArn =
         flag "dataset-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_dataset
           (Values.DeleteDatasetRequest.make ~datasetArn ()) None None])
let delete_dataset_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetGroupArn =
         flag "dataset-group-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_dataset_group
           (Values.DeleteDatasetGroupRequest.make ~datasetGroupArn ()) None
           None])
let delete_dataset_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetImportJobArn =
         flag "dataset-import-job-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_dataset_import_job
           (Values.DeleteDatasetImportJobRequest.make ~datasetImportJobArn ())
           None None])
let delete_explainability =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and explainabilityArn =
         flag "explainability-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_explainability
           (Values.DeleteExplainabilityRequest.make ~explainabilityArn ())
           None None])
let delete_explainability_export =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and explainabilityExportArn =
         flag "explainability-export-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_explainability_export
           (Values.DeleteExplainabilityExportRequest.make
              ~explainabilityExportArn ()) None None])
let delete_forecast =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forecastArn =
         flag "forecast-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_forecast
           (Values.DeleteForecastRequest.make ~forecastArn ()) None None])
let delete_forecast_export_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forecastExportJobArn =
         flag "forecast-export-job-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_forecast_export_job
           (Values.DeleteForecastExportJobRequest.make ~forecastExportJobArn
              ()) None None])
let delete_monitor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and monitorArn =
         flag "monitor-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_monitor
           (Values.DeleteMonitorRequest.make ~monitorArn ()) None None])
let delete_predictor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and predictorArn =
         flag "predictor-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_predictor
           (Values.DeletePredictorRequest.make ~predictorArn ()) None None])
let delete_predictor_backtest_export_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and predictorBacktestExportJobArn =
         flag "predictor-backtest-export-job-arn" (required string)
           ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_predictor_backtest_export_job
           (Values.DeletePredictorBacktestExportJobRequest.make
              ~predictorBacktestExportJobArn ()) None None])
let delete_resource_tree =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_tree
           (Values.DeleteResourceTreeRequest.make ~resourceArn ()) None None])
let delete_what_if_analysis =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and whatIfAnalysisArn =
         flag "what-if-analysis-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_what_if_analysis
           (Values.DeleteWhatIfAnalysisRequest.make ~whatIfAnalysisArn ())
           None None])
let delete_what_if_forecast =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and whatIfForecastArn =
         flag "what-if-forecast-arn" (required string) ~doc:"STRING LongArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_what_if_forecast
           (Values.DeleteWhatIfForecastRequest.make ~whatIfForecastArn ())
           None None])
let delete_what_if_forecast_export =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and whatIfForecastExportArn =
         flag "what-if-forecast-export-arn" (required string)
           ~doc:"STRING LongArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_what_if_forecast_export
           (Values.DeleteWhatIfForecastExportRequest.make
              ~whatIfForecastExportArn ()) None None])
let describe_auto_predictor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and predictorArn =
         flag "predictor-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_auto_predictor
           (Values.DescribeAutoPredictorRequest.make ~predictorArn ())
           (Some Values.DescribeAutoPredictorResponse.to_json)
           (Some Values.DescribeAutoPredictorResponse.error_to_json)])
let describe_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetArn =
         flag "dataset-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_dataset
           (Values.DescribeDatasetRequest.make ~datasetArn ())
           (Some Values.DescribeDatasetResponse.to_json)
           (Some Values.DescribeDatasetResponse.error_to_json)])
let describe_dataset_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetGroupArn =
         flag "dataset-group-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_dataset_group
           (Values.DescribeDatasetGroupRequest.make ~datasetGroupArn ())
           (Some Values.DescribeDatasetGroupResponse.to_json)
           (Some Values.DescribeDatasetGroupResponse.error_to_json)])
let describe_dataset_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetImportJobArn =
         flag "dataset-import-job-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_dataset_import_job
           (Values.DescribeDatasetImportJobRequest.make ~datasetImportJobArn
              ()) (Some Values.DescribeDatasetImportJobResponse.to_json)
           (Some Values.DescribeDatasetImportJobResponse.error_to_json)])
let describe_explainability =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and explainabilityArn =
         flag "explainability-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_explainability
           (Values.DescribeExplainabilityRequest.make ~explainabilityArn ())
           (Some Values.DescribeExplainabilityResponse.to_json)
           (Some Values.DescribeExplainabilityResponse.error_to_json)])
let describe_explainability_export =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and explainabilityExportArn =
         flag "explainability-export-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_explainability_export
           (Values.DescribeExplainabilityExportRequest.make
              ~explainabilityExportArn ())
           (Some Values.DescribeExplainabilityExportResponse.to_json)
           (Some Values.DescribeExplainabilityExportResponse.error_to_json)])
let describe_forecast =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forecastArn =
         flag "forecast-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_forecast
           (Values.DescribeForecastRequest.make ~forecastArn ())
           (Some Values.DescribeForecastResponse.to_json)
           (Some Values.DescribeForecastResponse.error_to_json)])
let describe_forecast_export_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forecastExportJobArn =
         flag "forecast-export-job-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_forecast_export_job
           (Values.DescribeForecastExportJobRequest.make
              ~forecastExportJobArn ())
           (Some Values.DescribeForecastExportJobResponse.to_json)
           (Some Values.DescribeForecastExportJobResponse.error_to_json)])
let describe_monitor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and monitorArn =
         flag "monitor-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_monitor
           (Values.DescribeMonitorRequest.make ~monitorArn ())
           (Some Values.DescribeMonitorResponse.to_json)
           (Some Values.DescribeMonitorResponse.error_to_json)])
let describe_predictor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and predictorArn =
         flag "predictor-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_predictor
           (Values.DescribePredictorRequest.make ~predictorArn ())
           (Some Values.DescribePredictorResponse.to_json)
           (Some Values.DescribePredictorResponse.error_to_json)])
let describe_predictor_backtest_export_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and predictorBacktestExportJobArn =
         flag "predictor-backtest-export-job-arn" (required string)
           ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_predictor_backtest_export_job
           (Values.DescribePredictorBacktestExportJobRequest.make
              ~predictorBacktestExportJobArn ())
           (Some Values.DescribePredictorBacktestExportJobResponse.to_json)
           (Some
              Values.DescribePredictorBacktestExportJobResponse.error_to_json)])
let describe_what_if_analysis =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and whatIfAnalysisArn =
         flag "what-if-analysis-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_what_if_analysis
           (Values.DescribeWhatIfAnalysisRequest.make ~whatIfAnalysisArn ())
           (Some Values.DescribeWhatIfAnalysisResponse.to_json)
           (Some Values.DescribeWhatIfAnalysisResponse.error_to_json)])
let describe_what_if_forecast =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and whatIfForecastArn =
         flag "what-if-forecast-arn" (required string) ~doc:"STRING LongArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_what_if_forecast
           (Values.DescribeWhatIfForecastRequest.make ~whatIfForecastArn ())
           (Some Values.DescribeWhatIfForecastResponse.to_json)
           (Some Values.DescribeWhatIfForecastResponse.error_to_json)])
let describe_what_if_forecast_export =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and whatIfForecastExportArn =
         flag "what-if-forecast-export-arn" (required string)
           ~doc:"STRING LongArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_what_if_forecast_export
           (Values.DescribeWhatIfForecastExportRequest.make
              ~whatIfForecastExportArn ())
           (Some Values.DescribeWhatIfForecastExportResponse.to_json)
           (Some Values.DescribeWhatIfForecastExportResponse.error_to_json)])
let get_accuracy_metrics =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and predictorArn =
         flag "predictor-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_accuracy_metrics
           (Values.GetAccuracyMetricsRequest.make ~predictorArn ())
           (Some Values.GetAccuracyMetricsResponse.to_json)
           (Some Values.GetAccuracyMetricsResponse.error_to_json)])
let list_dataset_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_dataset_groups
           (Values.ListDatasetGroupsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListDatasetGroupsResponse.to_json)
           (Some Values.ListDatasetGroupsResponse.error_to_json)])
let list_dataset_import_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_dataset_import_jobs
           (Values.ListDatasetImportJobsRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.Filters.of_json filters) ())
           (Some Values.ListDatasetImportJobsResponse.to_json)
           (Some Values.ListDatasetImportJobsResponse.error_to_json)])
let list_datasets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_datasets
           (Values.ListDatasetsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListDatasetsResponse.to_json)
           (Some Values.ListDatasetsResponse.error_to_json)])
let list_explainabilities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_explainabilities
           (Values.ListExplainabilitiesRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.Filters.of_json filters) ())
           (Some Values.ListExplainabilitiesResponse.to_json)
           (Some Values.ListExplainabilitiesResponse.error_to_json)])
let list_explainability_exports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_explainability_exports
           (Values.ListExplainabilityExportsRequest.make ?nextToken
              ?maxResults
              ?filters:(Option.map ~f:Values.Filters.of_json filters) ())
           (Some Values.ListExplainabilityExportsResponse.to_json)
           (Some Values.ListExplainabilityExportsResponse.error_to_json)])
let list_forecast_export_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_forecast_export_jobs
           (Values.ListForecastExportJobsRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.Filters.of_json filters) ())
           (Some Values.ListForecastExportJobsResponse.to_json)
           (Some Values.ListForecastExportJobsResponse.error_to_json)])
let list_forecasts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_forecasts
           (Values.ListForecastsRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.Filters.of_json filters) ())
           (Some Values.ListForecastsResponse.to_json)
           (Some Values.ListForecastsResponse.error_to_json)])
let list_monitor_evaluations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters"
       and monitorArn =
         flag "monitor-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_monitor_evaluations
           (Values.ListMonitorEvaluationsRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.Filters.of_json filters)
              ~monitorArn ())
           (Some Values.ListMonitorEvaluationsResponse.to_json)
           (Some Values.ListMonitorEvaluationsResponse.error_to_json)])
let list_monitors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_monitors
           (Values.ListMonitorsRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.Filters.of_json filters) ())
           (Some Values.ListMonitorsResponse.to_json)
           (Some Values.ListMonitorsResponse.error_to_json)])
let list_predictor_backtest_export_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_predictor_backtest_export_jobs
           (Values.ListPredictorBacktestExportJobsRequest.make ?nextToken
              ?maxResults
              ?filters:(Option.map ~f:Values.Filters.of_json filters) ())
           (Some Values.ListPredictorBacktestExportJobsResponse.to_json)
           (Some Values.ListPredictorBacktestExportJobsResponse.error_to_json)])
let list_predictors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_predictors
           (Values.ListPredictorsRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.Filters.of_json filters) ())
           (Some Values.ListPredictorsResponse.to_json)
           (Some Values.ListPredictorsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_what_if_analyses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_what_if_analyses
           (Values.ListWhatIfAnalysesRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.Filters.of_json filters) ())
           (Some Values.ListWhatIfAnalysesResponse.to_json)
           (Some Values.ListWhatIfAnalysesResponse.error_to_json)])
let list_what_if_forecast_exports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_what_if_forecast_exports
           (Values.ListWhatIfForecastExportsRequest.make ?nextToken
              ?maxResults
              ?filters:(Option.map ~f:Values.Filters.of_json filters) ())
           (Some Values.ListWhatIfForecastExportsResponse.to_json)
           (Some Values.ListWhatIfForecastExportsResponse.error_to_json)])
let list_what_if_forecasts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_what_if_forecasts
           (Values.ListWhatIfForecastsRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.Filters.of_json filters) ())
           (Some Values.ListWhatIfForecastsResponse.to_json)
           (Some Values.ListWhatIfForecastsResponse.error_to_json)])
let resume_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.resume_resource
           (Values.ResumeResourceRequest.make ~resourceArn ()) None None])
let stop_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_resource (Values.StopResourceRequest.make ~resourceArn ())
           None None])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_dataset_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetGroupArn =
         flag "dataset-group-arn" (required string) ~doc:"STRING Arn"
       and datasetArns =
         flag "dataset-arns" (required json_arg) ~doc:"JSON ArnList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_dataset_group
           (Values.UpdateDatasetGroupRequest.make ~datasetGroupArn
              ~datasetArns:(Values.ArnList.of_json datasetArns) ())
           (Some Values.UpdateDatasetGroupResponse.to_json)
           (Some Values.UpdateDatasetGroupResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-auto-predictor", create_auto_predictor);
    ("create-dataset", create_dataset);
    ("create-dataset-group", create_dataset_group);
    ("create-dataset-import-job", create_dataset_import_job);
    ("create-explainability", create_explainability);
    ("create-explainability-export", create_explainability_export);
    ("create-forecast", create_forecast);
    ("create-forecast-export-job", create_forecast_export_job);
    ("create-monitor", create_monitor);
    ("create-predictor", create_predictor);
    ("create-predictor-backtest-export-job",
      create_predictor_backtest_export_job);
    ("create-what-if-analysis", create_what_if_analysis);
    ("create-what-if-forecast", create_what_if_forecast);
    ("create-what-if-forecast-export", create_what_if_forecast_export);
    ("delete-dataset", delete_dataset);
    ("delete-dataset-group", delete_dataset_group);
    ("delete-dataset-import-job", delete_dataset_import_job);
    ("delete-explainability", delete_explainability);
    ("delete-explainability-export", delete_explainability_export);
    ("delete-forecast", delete_forecast);
    ("delete-forecast-export-job", delete_forecast_export_job);
    ("delete-monitor", delete_monitor);
    ("delete-predictor", delete_predictor);
    ("delete-predictor-backtest-export-job",
      delete_predictor_backtest_export_job);
    ("delete-resource-tree", delete_resource_tree);
    ("delete-what-if-analysis", delete_what_if_analysis);
    ("delete-what-if-forecast", delete_what_if_forecast);
    ("delete-what-if-forecast-export", delete_what_if_forecast_export);
    ("describe-auto-predictor", describe_auto_predictor);
    ("describe-dataset", describe_dataset);
    ("describe-dataset-group", describe_dataset_group);
    ("describe-dataset-import-job", describe_dataset_import_job);
    ("describe-explainability", describe_explainability);
    ("describe-explainability-export", describe_explainability_export);
    ("describe-forecast", describe_forecast);
    ("describe-forecast-export-job", describe_forecast_export_job);
    ("describe-monitor", describe_monitor);
    ("describe-predictor", describe_predictor);
    ("describe-predictor-backtest-export-job",
      describe_predictor_backtest_export_job);
    ("describe-what-if-analysis", describe_what_if_analysis);
    ("describe-what-if-forecast", describe_what_if_forecast);
    ("describe-what-if-forecast-export", describe_what_if_forecast_export);
    ("get-accuracy-metrics", get_accuracy_metrics);
    ("list-dataset-groups", list_dataset_groups);
    ("list-dataset-import-jobs", list_dataset_import_jobs);
    ("list-datasets", list_datasets);
    ("list-explainabilities", list_explainabilities);
    ("list-explainability-exports", list_explainability_exports);
    ("list-forecast-export-jobs", list_forecast_export_jobs);
    ("list-forecasts", list_forecasts);
    ("list-monitor-evaluations", list_monitor_evaluations);
    ("list-monitors", list_monitors);
    ("list-predictor-backtest-export-jobs",
      list_predictor_backtest_export_jobs);
    ("list-predictors", list_predictors);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-what-if-analyses", list_what_if_analyses);
    ("list-what-if-forecast-exports", list_what_if_forecast_exports);
    ("list-what-if-forecasts", list_what_if_forecasts);
    ("resume-resource", resume_resource);
    ("stop-resource", stop_resource);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-dataset-group", update_dataset_group)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING String"
       and tagsList =
         flag "tags-list" (required json_arg) ~doc:"JSON TagsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_tags
           (Values.AddTagsRequest.make ~resourceId
              ~tagsList:(Values.TagsList.of_json tagsList) ())
           (Some Values.AddTagsResponse.to_json)
           (Some Values.AddTagsResponse.error_to_json)])
let cancel_query =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventDataStore =
         flag "event-data-store" (required string)
           ~doc:"STRING EventDataStoreArn"
       and queryId = flag "query-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_query
           (Values.CancelQueryRequest.make ~eventDataStore ~queryId ())
           (Some Values.CancelQueryResponse.to_json)
           (Some Values.CancelQueryResponse.error_to_json)])
let create_event_data_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and advancedEventSelectors =
         flag "advanced-event-selectors" (optional json_arg)
           ~doc:"JSON AdvancedEventSelectors"
       and multiRegionEnabled =
         flag "multi-region-enabled" (optional bool) ~doc:"BOOL Boolean"
       and organizationEnabled =
         flag "organization-enabled" (optional bool) ~doc:"BOOL Boolean"
       and retentionPeriod =
         flag "retention-period" (optional int) ~doc:"INT RetentionPeriod"
       and terminationProtectionEnabled =
         flag "termination-protection-enabled" (optional bool)
           ~doc:"BOOL TerminationProtectionEnabled"
       and tagsList =
         flag "tags-list" (optional json_arg) ~doc:"JSON TagsList"
       and name =
         flag "name" (required string) ~doc:"STRING EventDataStoreName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_event_data_store
           (Values.CreateEventDataStoreRequest.make
              ?advancedEventSelectors:(Option.map
                                         ~f:Values.AdvancedEventSelectors.of_json
                                         advancedEventSelectors)
              ?multiRegionEnabled ?organizationEnabled ?retentionPeriod
              ?terminationProtectionEnabled
              ?tagsList:(Option.map ~f:Values.TagsList.of_json tagsList)
              ~name ()) (Some Values.CreateEventDataStoreResponse.to_json)
           (Some Values.CreateEventDataStoreResponse.error_to_json)])
let create_trail =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and s3KeyPrefix =
         flag "s3-key-prefix" (optional string) ~doc:"STRING String"
       and snsTopicName =
         flag "sns-topic-name" (optional string) ~doc:"STRING String"
       and includeGlobalServiceEvents =
         flag "include-global-service-events" (optional bool)
           ~doc:"BOOL Boolean"
       and isMultiRegionTrail =
         flag "is-multi-region-trail" (optional bool) ~doc:"BOOL Boolean"
       and enableLogFileValidation =
         flag "enable-log-file-validation" (optional bool)
           ~doc:"BOOL Boolean"
       and cloudWatchLogsLogGroupArn =
         flag "cloud-watch-logs-log-group-arn" (optional string)
           ~doc:"STRING String"
       and cloudWatchLogsRoleArn =
         flag "cloud-watch-logs-role-arn" (optional string)
           ~doc:"STRING String"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and isOrganizationTrail =
         flag "is-organization-trail" (optional bool) ~doc:"BOOL Boolean"
       and tagsList =
         flag "tags-list" (optional json_arg) ~doc:"JSON TagsList"
       and name = flag "name" (required string) ~doc:"STRING String"
       and s3BucketName =
         flag "s3-bucket-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_trail
           (Values.CreateTrailRequest.make ?s3KeyPrefix ?snsTopicName
              ?includeGlobalServiceEvents ?isMultiRegionTrail
              ?enableLogFileValidation ?cloudWatchLogsLogGroupArn
              ?cloudWatchLogsRoleArn ?kmsKeyId ?isOrganizationTrail
              ?tagsList:(Option.map ~f:Values.TagsList.of_json tagsList)
              ~name ~s3BucketName ())
           (Some Values.CreateTrailResponse.to_json)
           (Some Values.CreateTrailResponse.error_to_json)])
let delete_event_data_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventDataStore =
         flag "event-data-store" (required string)
           ~doc:"STRING EventDataStoreArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_event_data_store
           (Values.DeleteEventDataStoreRequest.make ~eventDataStore ())
           (Some Values.DeleteEventDataStoreResponse.to_json)
           (Some Values.DeleteEventDataStoreResponse.error_to_json)])
let delete_trail =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_trail (Values.DeleteTrailRequest.make ~name ())
           (Some Values.DeleteTrailResponse.to_json)
           (Some Values.DeleteTrailResponse.error_to_json)])
let describe_query =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventDataStore =
         flag "event-data-store" (required string)
           ~doc:"STRING EventDataStoreArn"
       and queryId = flag "query-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_query
           (Values.DescribeQueryRequest.make ~eventDataStore ~queryId ())
           (Some Values.DescribeQueryResponse.to_json)
           (Some Values.DescribeQueryResponse.error_to_json)])
let describe_trails =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trailNameList =
         flag "trail-name-list" (optional json_arg) ~doc:"JSON TrailNameList"
       and includeShadowTrails =
         flag "include-shadow-trails" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_trails
           (Values.DescribeTrailsRequest.make
              ?trailNameList:(Option.map ~f:Values.TrailNameList.of_json
                                trailNameList) ?includeShadowTrails ())
           (Some Values.DescribeTrailsResponse.to_json)
           (Some Values.DescribeTrailsResponse.error_to_json)])
let get_event_data_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventDataStore =
         flag "event-data-store" (required string)
           ~doc:"STRING EventDataStoreArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_event_data_store
           (Values.GetEventDataStoreRequest.make ~eventDataStore ())
           (Some Values.GetEventDataStoreResponse.to_json)
           (Some Values.GetEventDataStoreResponse.error_to_json)])
let get_event_selectors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trailName =
         flag "trail-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_event_selectors
           (Values.GetEventSelectorsRequest.make ~trailName ())
           (Some Values.GetEventSelectorsResponse.to_json)
           (Some Values.GetEventSelectorsResponse.error_to_json)])
let get_insight_selectors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trailName =
         flag "trail-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_insight_selectors
           (Values.GetInsightSelectorsRequest.make ~trailName ())
           (Some Values.GetInsightSelectorsResponse.to_json)
           (Some Values.GetInsightSelectorsResponse.error_to_json)])
let get_query_results =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxQueryResults =
         flag "max-query-results" (optional int) ~doc:"INT MaxQueryResults"
       and eventDataStore =
         flag "event-data-store" (required string)
           ~doc:"STRING EventDataStoreArn"
       and queryId = flag "query-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_query_results
           (Values.GetQueryResultsRequest.make ?nextToken ?maxQueryResults
              ~eventDataStore ~queryId ())
           (Some Values.GetQueryResultsResponse.to_json)
           (Some Values.GetQueryResultsResponse.error_to_json)])
let get_trail =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_trail (Values.GetTrailRequest.make ~name ())
           (Some Values.GetTrailResponse.to_json)
           (Some Values.GetTrailResponse.error_to_json)])
let get_trail_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_trail_status (Values.GetTrailStatusRequest.make ~name ())
           (Some Values.GetTrailStatusResponse.to_json)
           (Some Values.GetTrailStatusResponse.error_to_json)])
let list_event_data_stores =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListEventDataStoresMaxResultsCount" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_event_data_stores
           (Values.ListEventDataStoresRequest.make ?nextToken ?maxResults ())
           (Some Values.ListEventDataStoresResponse.to_json)
           (Some Values.ListEventDataStoresResponse.error_to_json)])
let list_public_keys =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and startTime = flag "start-time" (optional json_arg) ~doc:"JSON Date"
       and endTime = flag "end-time" (optional json_arg) ~doc:"JSON Date"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_public_keys
           (Values.ListPublicKeysRequest.make
              ?startTime:(Option.map ~f:Values.Date.of_json startTime)
              ?endTime:(Option.map ~f:Values.Date.of_json endTime) ?nextToken
              ()) (Some Values.ListPublicKeysResponse.to_json)
           (Some Values.ListPublicKeysResponse.error_to_json)])
let list_queries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListQueriesMaxResultsCount"
       and startTime = flag "start-time" (optional json_arg) ~doc:"JSON Date"
       and endTime = flag "end-time" (optional json_arg) ~doc:"JSON Date"
       and queryStatus =
         flag "query-status" (optional json_arg) ~doc:"JSON QueryStatus"
       and eventDataStore =
         flag "event-data-store" (required string)
           ~doc:"STRING EventDataStoreArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_queries
           (Values.ListQueriesRequest.make ?nextToken ?maxResults
              ?startTime:(Option.map ~f:Values.Date.of_json startTime)
              ?endTime:(Option.map ~f:Values.Date.of_json endTime)
              ?queryStatus:(Option.map ~f:Values.QueryStatus.of_json
                              queryStatus) ~eventDataStore ())
           (Some Values.ListQueriesResponse.to_json)
           (Some Values.ListQueriesResponse.error_to_json)])
let list_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and resourceIdList =
         flag "resource-id-list" (required json_arg)
           ~doc:"JSON ResourceIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags
           (Values.ListTagsRequest.make ?nextToken
              ~resourceIdList:(Values.ResourceIdList.of_json resourceIdList)
              ()) (Some Values.ListTagsResponse.to_json)
           (Some Values.ListTagsResponse.error_to_json)])
let list_trails =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_trails (Values.ListTrailsRequest.make ?nextToken ())
           (Some Values.ListTrailsResponse.to_json)
           (Some Values.ListTrailsResponse.error_to_json)])
let lookup_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and lookupAttributes =
         flag "lookup-attributes" (optional json_arg)
           ~doc:"JSON LookupAttributesList"
       and startTime = flag "start-time" (optional json_arg) ~doc:"JSON Date"
       and endTime = flag "end-time" (optional json_arg) ~doc:"JSON Date"
       and eventCategory =
         flag "event-category" (optional json_arg) ~doc:"JSON EventCategory"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.lookup_events
           (Values.LookupEventsRequest.make
              ?lookupAttributes:(Option.map
                                   ~f:Values.LookupAttributesList.of_json
                                   lookupAttributes)
              ?startTime:(Option.map ~f:Values.Date.of_json startTime)
              ?endTime:(Option.map ~f:Values.Date.of_json endTime)
              ?eventCategory:(Option.map ~f:Values.EventCategory.of_json
                                eventCategory) ?maxResults ?nextToken ())
           (Some Values.LookupEventsResponse.to_json)
           (Some Values.LookupEventsResponse.error_to_json)])
let put_event_selectors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventSelectors =
         flag "event-selectors" (optional json_arg)
           ~doc:"JSON EventSelectors"
       and advancedEventSelectors =
         flag "advanced-event-selectors" (optional json_arg)
           ~doc:"JSON AdvancedEventSelectors"
       and trailName =
         flag "trail-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_event_selectors
           (Values.PutEventSelectorsRequest.make
              ?eventSelectors:(Option.map ~f:Values.EventSelectors.of_json
                                 eventSelectors)
              ?advancedEventSelectors:(Option.map
                                         ~f:Values.AdvancedEventSelectors.of_json
                                         advancedEventSelectors) ~trailName
              ()) (Some Values.PutEventSelectorsResponse.to_json)
           (Some Values.PutEventSelectorsResponse.error_to_json)])
let put_insight_selectors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trailName =
         flag "trail-name" (required string) ~doc:"STRING String"
       and insightSelectors =
         flag "insight-selectors" (required json_arg)
           ~doc:"JSON InsightSelectors" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_insight_selectors
           (Values.PutInsightSelectorsRequest.make ~trailName
              ~insightSelectors:(Values.InsightSelectors.of_json
                                   insightSelectors) ())
           (Some Values.PutInsightSelectorsResponse.to_json)
           (Some Values.PutInsightSelectorsResponse.error_to_json)])
let remove_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING String"
       and tagsList =
         flag "tags-list" (required json_arg) ~doc:"JSON TagsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_tags
           (Values.RemoveTagsRequest.make ~resourceId
              ~tagsList:(Values.TagsList.of_json tagsList) ())
           (Some Values.RemoveTagsResponse.to_json)
           (Some Values.RemoveTagsResponse.error_to_json)])
let restore_event_data_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventDataStore =
         flag "event-data-store" (required string)
           ~doc:"STRING EventDataStoreArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_event_data_store
           (Values.RestoreEventDataStoreRequest.make ~eventDataStore ())
           (Some Values.RestoreEventDataStoreResponse.to_json)
           (Some Values.RestoreEventDataStoreResponse.error_to_json)])
let start_logging =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_logging (Values.StartLoggingRequest.make ~name ())
           (Some Values.StartLoggingResponse.to_json)
           (Some Values.StartLoggingResponse.error_to_json)])
let start_query =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queryStatement =
         flag "query-statement" (required string)
           ~doc:"STRING QueryStatement" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_query (Values.StartQueryRequest.make ~queryStatement ())
           (Some Values.StartQueryResponse.to_json)
           (Some Values.StartQueryResponse.error_to_json)])
let stop_logging =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_logging (Values.StopLoggingRequest.make ~name ())
           (Some Values.StopLoggingResponse.to_json)
           (Some Values.StopLoggingResponse.error_to_json)])
let update_event_data_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING EventDataStoreName"
       and advancedEventSelectors =
         flag "advanced-event-selectors" (optional json_arg)
           ~doc:"JSON AdvancedEventSelectors"
       and multiRegionEnabled =
         flag "multi-region-enabled" (optional bool) ~doc:"BOOL Boolean"
       and organizationEnabled =
         flag "organization-enabled" (optional bool) ~doc:"BOOL Boolean"
       and retentionPeriod =
         flag "retention-period" (optional int) ~doc:"INT RetentionPeriod"
       and terminationProtectionEnabled =
         flag "termination-protection-enabled" (optional bool)
           ~doc:"BOOL TerminationProtectionEnabled"
       and eventDataStore =
         flag "event-data-store" (required string)
           ~doc:"STRING EventDataStoreArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_event_data_store
           (Values.UpdateEventDataStoreRequest.make ?name
              ?advancedEventSelectors:(Option.map
                                         ~f:Values.AdvancedEventSelectors.of_json
                                         advancedEventSelectors)
              ?multiRegionEnabled ?organizationEnabled ?retentionPeriod
              ?terminationProtectionEnabled ~eventDataStore ())
           (Some Values.UpdateEventDataStoreResponse.to_json)
           (Some Values.UpdateEventDataStoreResponse.error_to_json)])
let update_trail =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and s3BucketName =
         flag "s3-bucket-name" (optional string) ~doc:"STRING String"
       and s3KeyPrefix =
         flag "s3-key-prefix" (optional string) ~doc:"STRING String"
       and snsTopicName =
         flag "sns-topic-name" (optional string) ~doc:"STRING String"
       and includeGlobalServiceEvents =
         flag "include-global-service-events" (optional bool)
           ~doc:"BOOL Boolean"
       and isMultiRegionTrail =
         flag "is-multi-region-trail" (optional bool) ~doc:"BOOL Boolean"
       and enableLogFileValidation =
         flag "enable-log-file-validation" (optional bool)
           ~doc:"BOOL Boolean"
       and cloudWatchLogsLogGroupArn =
         flag "cloud-watch-logs-log-group-arn" (optional string)
           ~doc:"STRING String"
       and cloudWatchLogsRoleArn =
         flag "cloud-watch-logs-role-arn" (optional string)
           ~doc:"STRING String"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and isOrganizationTrail =
         flag "is-organization-trail" (optional bool) ~doc:"BOOL Boolean"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_trail
           (Values.UpdateTrailRequest.make ?s3BucketName ?s3KeyPrefix
              ?snsTopicName ?includeGlobalServiceEvents ?isMultiRegionTrail
              ?enableLogFileValidation ?cloudWatchLogsLogGroupArn
              ?cloudWatchLogsRoleArn ?kmsKeyId ?isOrganizationTrail ~name ())
           (Some Values.UpdateTrailResponse.to_json)
           (Some Values.UpdateTrailResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("add-tags", add_tags);
    ("cancel-query", cancel_query);
    ("create-event-data-store", create_event_data_store);
    ("create-trail", create_trail);
    ("delete-event-data-store", delete_event_data_store);
    ("delete-trail", delete_trail);
    ("describe-query", describe_query);
    ("describe-trails", describe_trails);
    ("get-event-data-store", get_event_data_store);
    ("get-event-selectors", get_event_selectors);
    ("get-insight-selectors", get_insight_selectors);
    ("get-query-results", get_query_results);
    ("get-trail", get_trail);
    ("get-trail-status", get_trail_status);
    ("list-event-data-stores", list_event_data_stores);
    ("list-public-keys", list_public_keys);
    ("list-queries", list_queries);
    ("list-tags", list_tags);
    ("list-trails", list_trails);
    ("lookup-events", lookup_events);
    ("put-event-selectors", put_event_selectors);
    ("put-insight-selectors", put_insight_selectors);
    ("remove-tags", remove_tags);
    ("restore-event-data-store", restore_event_data_store);
    ("start-logging", start_logging);
    ("start-query", start_query);
    ("stop-logging", stop_logging);
    ("update-event-data-store", update_event_data_store);
    ("update-trail", update_trail)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let get_routing_control_state =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and routingControlArn =
         flag "routing-control-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_routing_control_state
           (Values.GetRoutingControlStateRequest.make ~routingControlArn ())
           (Some Values.GetRoutingControlStateResponse.to_json)
           (Some Values.GetRoutingControlStateResponse.error_to_json)])
let list_routing_controls =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and controlPanelArn =
         flag "control-panel-arn" (optional string) ~doc:"STRING Arn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PageToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_routing_controls
           (Values.ListRoutingControlsRequest.make ?controlPanelArn
              ?nextToken ?maxResults ())
           (Some Values.ListRoutingControlsResponse.to_json)
           (Some Values.ListRoutingControlsResponse.error_to_json)])
let update_routing_control_state =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and safetyRulesToOverride =
         flag "safety-rules-to-override" (optional json_arg) ~doc:"JSON Arns"
       and routingControlArn =
         flag "routing-control-arn" (required string) ~doc:"STRING Arn"
       and routingControlState =
         flag "routing-control-state" (required json_arg)
           ~doc:"JSON RoutingControlState" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_routing_control_state
           (Values.UpdateRoutingControlStateRequest.make
              ?safetyRulesToOverride:(Option.map ~f:Values.Arns.of_json
                                        safetyRulesToOverride)
              ~routingControlArn
              ~routingControlState:(Values.RoutingControlState.of_json
                                      routingControlState) ())
           (Some Values.UpdateRoutingControlStateResponse.to_json)
           (Some Values.UpdateRoutingControlStateResponse.error_to_json)])
let update_routing_control_states =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and safetyRulesToOverride =
         flag "safety-rules-to-override" (optional json_arg) ~doc:"JSON Arns"
       and updateRoutingControlStateEntries =
         flag "update-routing-control-state-entries" (required json_arg)
           ~doc:"JSON UpdateRoutingControlStateEntries" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_routing_control_states
           (Values.UpdateRoutingControlStatesRequest.make
              ?safetyRulesToOverride:(Option.map ~f:Values.Arns.of_json
                                        safetyRulesToOverride)
              ~updateRoutingControlStateEntries:(Values.UpdateRoutingControlStateEntries.of_json
                                                   updateRoutingControlStateEntries)
              ()) (Some Values.UpdateRoutingControlStatesResponse.to_json)
           (Some Values.UpdateRoutingControlStatesResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("get-routing-control-state", get_routing_control_state);
    ("list-routing-controls", list_routing_controls);
    ("update-routing-control-state", update_routing_control_state);
    ("update-routing-control-states", update_routing_control_states)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_campaign =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelSubtypeConfig =
         flag "channel-subtype-config" (optional json_arg)
           ~doc:"JSON ChannelSubtypeConfig"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON ExternalCampaignType"
       and source = flag "source" (optional json_arg) ~doc:"JSON Source"
       and connectCampaignFlowArn =
         flag "connect-campaign-flow-arn" (optional string) ~doc:"STRING Arn"
       and schedule =
         flag "schedule" (optional json_arg) ~doc:"JSON Schedule"
       and entryLimitsConfig =
         flag "entry-limits-config" (optional json_arg)
           ~doc:"JSON EntryLimitsConfig"
       and communicationTimeConfig =
         flag "communication-time-config" (optional json_arg)
           ~doc:"JSON CommunicationTimeConfig"
       and communicationLimitsOverride =
         flag "communication-limits-override" (optional json_arg)
           ~doc:"JSON CommunicationLimitsConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name = flag "name" (required string) ~doc:"STRING CampaignName"
       and connectInstanceId =
         flag "connect-instance-id" (required string)
           ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_campaign
           (Values.CreateCampaignRequest.make
              ?channelSubtypeConfig:(Option.map
                                       ~f:Values.ChannelSubtypeConfig.of_json
                                       channelSubtypeConfig)
              ?type_:(Option.map ~f:Values.ExternalCampaignType.of_json type_)
              ?source:(Option.map ~f:Values.Source.of_json source)
              ?connectCampaignFlowArn
              ?schedule:(Option.map ~f:Values.Schedule.of_json schedule)
              ?entryLimitsConfig:(Option.map
                                    ~f:Values.EntryLimitsConfig.of_json
                                    entryLimitsConfig)
              ?communicationTimeConfig:(Option.map
                                          ~f:Values.CommunicationTimeConfig.of_json
                                          communicationTimeConfig)
              ?communicationLimitsOverride:(Option.map
                                              ~f:Values.CommunicationLimitsConfig.of_json
                                              communicationLimitsOverride)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name
              ~connectInstanceId ())
           (Some Values.CreateCampaignResponse.to_json)
           (Some Values.CreateCampaignResponse.error_to_json)])
let delete_campaign =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING CampaignId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_campaign (Values.DeleteCampaignRequest.make ~id ()) None
           None])
let delete_campaign_channel_subtype_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING CampaignId"
       and channelSubtype =
         flag "channel-subtype" (required json_arg)
           ~doc:"JSON ChannelSubtype" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_campaign_channel_subtype_config
           (Values.DeleteCampaignChannelSubtypeConfigRequest.make ~id
              ~channelSubtype:(Values.ChannelSubtype.of_json channelSubtype)
              ()) None None])
let delete_campaign_communication_limits =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING CampaignId"
       and config =
         flag "config" (required json_arg)
           ~doc:"JSON CommunicationLimitsConfigType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_campaign_communication_limits
           (Values.DeleteCampaignCommunicationLimitsRequest.make ~id
              ~config:(Values.CommunicationLimitsConfigType.of_json config)
              ()) None None])
let delete_campaign_communication_time =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING CampaignId"
       and config =
         flag "config" (required json_arg)
           ~doc:"JSON CommunicationTimeConfigType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_campaign_communication_time
           (Values.DeleteCampaignCommunicationTimeRequest.make ~id
              ~config:(Values.CommunicationTimeConfigType.of_json config) ())
           None None])
let delete_campaign_entry_limits =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING CampaignId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_campaign_entry_limits
           (Values.DeleteCampaignEntryLimitsRequest.make ~id ()) None None])
let delete_connect_instance_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and campaignDeletionPolicy =
         flag "campaign-deletion-policy" (optional json_arg)
           ~doc:"JSON CampaignDeletionPolicy"
       and connectInstanceId =
         flag "connect-instance-id" (required string)
           ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_connect_instance_config
           (Values.DeleteConnectInstanceConfigRequest.make
              ?campaignDeletionPolicy:(Option.map
                                         ~f:Values.CampaignDeletionPolicy.of_json
                                         campaignDeletionPolicy)
              ~connectInstanceId ()) None None])
let delete_connect_instance_integration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectInstanceId =
         flag "connect-instance-id" (required string)
           ~doc:"STRING InstanceId"
       and integrationIdentifier =
         flag "integration-identifier" (required json_arg)
           ~doc:"JSON IntegrationIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_connect_instance_integration
           (Values.DeleteConnectInstanceIntegrationRequest.make
              ~connectInstanceId
              ~integrationIdentifier:(Values.IntegrationIdentifier.of_json
                                        integrationIdentifier) ()) None None])
let delete_instance_onboarding_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectInstanceId =
         flag "connect-instance-id" (required string)
           ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_instance_onboarding_job
           (Values.DeleteInstanceOnboardingJobRequest.make ~connectInstanceId
              ()) None None])
let describe_campaign =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING CampaignId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_campaign (Values.DescribeCampaignRequest.make ~id ())
           (Some Values.DescribeCampaignResponse.to_json)
           (Some Values.DescribeCampaignResponse.error_to_json)])
let get_campaign_state =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING CampaignId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_campaign_state (Values.GetCampaignStateRequest.make ~id ())
           (Some Values.GetCampaignStateResponse.to_json)
           (Some Values.GetCampaignStateResponse.error_to_json)])
let get_campaign_state_batch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and campaignIds =
         flag "campaign-ids" (required json_arg)
           ~doc:"JSON GetCampaignStateBatchRequestCampaignIdsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_campaign_state_batch
           (Values.GetCampaignStateBatchRequest.make
              ~campaignIds:(Values.GetCampaignStateBatchRequestCampaignIdsList.of_json
                              campaignIds) ())
           (Some Values.GetCampaignStateBatchResponse.to_json)
           (Some Values.GetCampaignStateBatchResponse.error_to_json)])
let get_connect_instance_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectInstanceId =
         flag "connect-instance-id" (required string)
           ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_connect_instance_config
           (Values.GetConnectInstanceConfigRequest.make ~connectInstanceId ())
           (Some Values.GetConnectInstanceConfigResponse.to_json)
           (Some Values.GetConnectInstanceConfigResponse.error_to_json)])
let get_instance_communication_limits =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectInstanceId =
         flag "connect-instance-id" (required string)
           ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_instance_communication_limits
           (Values.GetInstanceCommunicationLimitsRequest.make
              ~connectInstanceId ())
           (Some Values.GetInstanceCommunicationLimitsResponse.to_json)
           (Some Values.GetInstanceCommunicationLimitsResponse.error_to_json)])
let get_instance_onboarding_job_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectInstanceId =
         flag "connect-instance-id" (required string)
           ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_instance_onboarding_job_status
           (Values.GetInstanceOnboardingJobStatusRequest.make
              ~connectInstanceId ())
           (Some Values.GetInstanceOnboardingJobStatusResponse.to_json)
           (Some Values.GetInstanceOnboardingJobStatusResponse.error_to_json)])
let list_campaigns =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON CampaignFilters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_campaigns
           (Values.ListCampaignsRequest.make ?maxResults ?nextToken
              ?filters:(Option.map ~f:Values.CampaignFilters.of_json filters)
              ()) (Some Values.ListCampaignsResponse.to_json)
           (Some Values.ListCampaignsResponse.error_to_json)])
let list_connect_instance_integrations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and connectInstanceId =
         flag "connect-instance-id" (required string)
           ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_connect_instance_integrations
           (Values.ListConnectInstanceIntegrationsRequest.make ?maxResults
              ?nextToken ~connectInstanceId ())
           (Some Values.ListConnectInstanceIntegrationsResponse.to_json)
           (Some Values.ListConnectInstanceIntegrationsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~arn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let pause_campaign =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING CampaignId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.pause_campaign (Values.PauseCampaignRequest.make ~id ()) None
           None])
let put_connect_instance_integration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectInstanceId =
         flag "connect-instance-id" (required string)
           ~doc:"STRING InstanceId"
       and integrationConfig =
         flag "integration-config" (required json_arg)
           ~doc:"JSON IntegrationConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_connect_instance_integration
           (Values.PutConnectInstanceIntegrationRequest.make
              ~connectInstanceId
              ~integrationConfig:(Values.IntegrationConfig.of_json
                                    integrationConfig) ()) None None])
let put_instance_communication_limits =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectInstanceId =
         flag "connect-instance-id" (required string)
           ~doc:"STRING InstanceId"
       and communicationLimitsConfig =
         flag "communication-limits-config" (required json_arg)
           ~doc:"JSON InstanceCommunicationLimitsConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_instance_communication_limits
           (Values.PutInstanceCommunicationLimitsRequest.make
              ~connectInstanceId
              ~communicationLimitsConfig:(Values.InstanceCommunicationLimitsConfig.of_json
                                            communicationLimitsConfig) ())
           None None])
let put_outbound_request_batch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING CampaignId"
       and outboundRequests =
         flag "outbound-requests" (required json_arg)
           ~doc:"JSON OutboundRequestList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_outbound_request_batch
           (Values.PutOutboundRequestBatchRequest.make ~id
              ~outboundRequests:(Values.OutboundRequestList.of_json
                                   outboundRequests) ())
           (Some Values.PutOutboundRequestBatchResponse.to_json)
           (Some Values.PutOutboundRequestBatchResponse.error_to_json)])
let put_profile_outbound_request_batch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING CampaignId"
       and profileOutboundRequests =
         flag "profile-outbound-requests" (required json_arg)
           ~doc:"JSON ProfileOutboundRequestList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_profile_outbound_request_batch
           (Values.PutProfileOutboundRequestBatchRequest.make ~id
              ~profileOutboundRequests:(Values.ProfileOutboundRequestList.of_json
                                          profileOutboundRequests) ())
           (Some Values.PutProfileOutboundRequestBatchResponse.to_json)
           (Some Values.PutProfileOutboundRequestBatchResponse.error_to_json)])
let resume_campaign =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING CampaignId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.resume_campaign (Values.ResumeCampaignRequest.make ~id ()) None
           None])
let start_campaign =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING CampaignId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_campaign (Values.StartCampaignRequest.make ~id ()) None
           None])
let start_instance_onboarding_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectInstanceId =
         flag "connect-instance-id" (required string)
           ~doc:"STRING InstanceId"
       and encryptionConfig =
         flag "encryption-config" (required json_arg)
           ~doc:"JSON EncryptionConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_instance_onboarding_job
           (Values.StartInstanceOnboardingJobRequest.make ~connectInstanceId
              ~encryptionConfig:(Values.EncryptionConfig.of_json
                                   encryptionConfig) ())
           (Some Values.StartInstanceOnboardingJobResponse.to_json)
           (Some Values.StartInstanceOnboardingJobResponse.error_to_json)])
let stop_campaign =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING CampaignId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_campaign (Values.StopCampaignRequest.make ~id ()) None
           None])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~arn
              ~tags:(Values.TagMap.of_json tags) ()) None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~arn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ()) None None])
let update_campaign_channel_subtype_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING CampaignId"
       and channelSubtypeConfig =
         flag "channel-subtype-config" (required json_arg)
           ~doc:"JSON ChannelSubtypeConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_campaign_channel_subtype_config
           (Values.UpdateCampaignChannelSubtypeConfigRequest.make ~id
              ~channelSubtypeConfig:(Values.ChannelSubtypeConfig.of_json
                                       channelSubtypeConfig) ()) None None])
let update_campaign_communication_limits =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING CampaignId"
       and communicationLimitsOverride =
         flag "communication-limits-override" (required json_arg)
           ~doc:"JSON CommunicationLimitsConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_campaign_communication_limits
           (Values.UpdateCampaignCommunicationLimitsRequest.make ~id
              ~communicationLimitsOverride:(Values.CommunicationLimitsConfig.of_json
                                              communicationLimitsOverride) ())
           None None])
let update_campaign_communication_time =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING CampaignId"
       and communicationTimeConfig =
         flag "communication-time-config" (required json_arg)
           ~doc:"JSON CommunicationTimeConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_campaign_communication_time
           (Values.UpdateCampaignCommunicationTimeRequest.make ~id
              ~communicationTimeConfig:(Values.CommunicationTimeConfig.of_json
                                          communicationTimeConfig) ()) None
           None])
let update_campaign_entry_limits =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING CampaignId"
       and entryLimitsConfig =
         flag "entry-limits-config" (required json_arg)
           ~doc:"JSON EntryLimitsConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_campaign_entry_limits
           (Values.UpdateCampaignEntryLimitsRequest.make ~id
              ~entryLimitsConfig:(Values.EntryLimitsConfig.of_json
                                    entryLimitsConfig) ()) None None])
let update_campaign_flow_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING CampaignId"
       and connectCampaignFlowArn =
         flag "connect-campaign-flow-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_campaign_flow_association
           (Values.UpdateCampaignFlowAssociationRequest.make ~id
              ~connectCampaignFlowArn ()) None None])
let update_campaign_name =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING CampaignId"
       and name = flag "name" (required string) ~doc:"STRING CampaignName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_campaign_name
           (Values.UpdateCampaignNameRequest.make ~id ~name ()) None None])
let update_campaign_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING CampaignId"
       and schedule =
         flag "schedule" (required json_arg) ~doc:"JSON Schedule" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_campaign_schedule
           (Values.UpdateCampaignScheduleRequest.make ~id
              ~schedule:(Values.Schedule.of_json schedule) ()) None None])
let update_campaign_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING CampaignId"
       and source = flag "source" (required json_arg) ~doc:"JSON Source" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_campaign_source
           (Values.UpdateCampaignSourceRequest.make ~id
              ~source:(Values.Source.of_json source) ()) None None])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-campaign", create_campaign);
    ("delete-campaign", delete_campaign);
    ("delete-campaign-channel-subtype-config",
      delete_campaign_channel_subtype_config);
    ("delete-campaign-communication-limits",
      delete_campaign_communication_limits);
    ("delete-campaign-communication-time",
      delete_campaign_communication_time);
    ("delete-campaign-entry-limits", delete_campaign_entry_limits);
    ("delete-connect-instance-config", delete_connect_instance_config);
    ("delete-connect-instance-integration",
      delete_connect_instance_integration);
    ("delete-instance-onboarding-job", delete_instance_onboarding_job);
    ("describe-campaign", describe_campaign);
    ("get-campaign-state", get_campaign_state);
    ("get-campaign-state-batch", get_campaign_state_batch);
    ("get-connect-instance-config", get_connect_instance_config);
    ("get-instance-communication-limits", get_instance_communication_limits);
    ("get-instance-onboarding-job-status",
      get_instance_onboarding_job_status);
    ("list-campaigns", list_campaigns);
    ("list-connect-instance-integrations",
      list_connect_instance_integrations);
    ("list-tags-for-resource", list_tags_for_resource);
    ("pause-campaign", pause_campaign);
    ("put-connect-instance-integration", put_connect_instance_integration);
    ("put-instance-communication-limits", put_instance_communication_limits);
    ("put-outbound-request-batch", put_outbound_request_batch);
    ("put-profile-outbound-request-batch",
      put_profile_outbound_request_batch);
    ("resume-campaign", resume_campaign);
    ("start-campaign", start_campaign);
    ("start-instance-onboarding-job", start_instance_onboarding_job);
    ("stop-campaign", stop_campaign);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-campaign-channel-subtype-config",
      update_campaign_channel_subtype_config);
    ("update-campaign-communication-limits",
      update_campaign_communication_limits);
    ("update-campaign-communication-time",
      update_campaign_communication_time);
    ("update-campaign-entry-limits", update_campaign_entry_limits);
    ("update-campaign-flow-association", update_campaign_flow_association);
    ("update-campaign-name", update_campaign_name);
    ("update-campaign-schedule", update_campaign_schedule);
    ("update-campaign-source", update_campaign_source)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_member_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and memberAccountId =
         flag "member-account-id" (required string)
           ~doc:"STRING AWSAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_member_account
           (Values.AssociateMemberAccountRequest.make ~memberAccountId ())
           None None])
let associate_s3_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and memberAccountId =
         flag "member-account-id" (optional string)
           ~doc:"STRING AWSAccountId"
       and s3Resources =
         flag "s3-resources" (required json_arg)
           ~doc:"JSON S3ResourcesClassification" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_s3_resources
           (Values.AssociateS3ResourcesRequest.make ?memberAccountId
              ~s3Resources:(Values.S3ResourcesClassification.of_json
                              s3Resources) ())
           (Some Values.AssociateS3ResourcesResult.to_json)
           (Some Values.AssociateS3ResourcesResult.error_to_json)])
let disassociate_member_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and memberAccountId =
         flag "member-account-id" (required string)
           ~doc:"STRING AWSAccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_member_account
           (Values.DisassociateMemberAccountRequest.make ~memberAccountId ())
           None None])
let disassociate_s3_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and memberAccountId =
         flag "member-account-id" (optional string)
           ~doc:"STRING AWSAccountId"
       and associatedS3Resources =
         flag "associated-s3-resources" (required json_arg)
           ~doc:"JSON S3Resources" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_s3_resources
           (Values.DisassociateS3ResourcesRequest.make ?memberAccountId
              ~associatedS3Resources:(Values.S3Resources.of_json
                                        associatedS3Resources) ())
           (Some Values.DisassociateS3ResourcesResult.to_json)
           (Some Values.DisassociateS3ResourcesResult.error_to_json)])
let list_member_accounts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_member_accounts
           (Values.ListMemberAccountsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListMemberAccountsResult.to_json)
           (Some Values.ListMemberAccountsResult.error_to_json)])
let list_s3_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and memberAccountId =
         flag "member-account-id" (optional string)
           ~doc:"STRING AWSAccountId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_s3_resources
           (Values.ListS3ResourcesRequest.make ?memberAccountId ?nextToken
              ?maxResults ()) (Some Values.ListS3ResourcesResult.to_json)
           (Some Values.ListS3ResourcesResult.error_to_json)])
let update_s3_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and memberAccountId =
         flag "member-account-id" (optional string)
           ~doc:"STRING AWSAccountId"
       and s3ResourcesUpdate =
         flag "s3-resources-update" (required json_arg)
           ~doc:"JSON S3ResourcesClassificationUpdate" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_s3_resources
           (Values.UpdateS3ResourcesRequest.make ?memberAccountId
              ~s3ResourcesUpdate:(Values.S3ResourcesClassificationUpdate.of_json
                                    s3ResourcesUpdate) ())
           (Some Values.UpdateS3ResourcesResult.to_json)
           (Some Values.UpdateS3ResourcesResult.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-member-account", associate_member_account);
    ("associate-s3-resources", associate_s3_resources);
    ("disassociate-member-account", disassociate_member_account);
    ("disassociate-s3-resources", disassociate_s3_resources);
    ("list-member-accounts", list_member_accounts);
    ("list-s3-resources", list_s3_resources);
    ("update-s3-resources", update_s3_resources)]
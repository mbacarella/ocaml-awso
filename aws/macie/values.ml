(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.macie
let apiVersion = "2017-12-19"
let endpointPrefix = "macie"
let serviceFullName = "Amazon Macie"
let signatureVersion = "v4"
let protocol = "json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let targetPrefix = "MacieService"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module BucketName =
  struct
    type nonrec t = string
    let context_ = "BucketName"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:500); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BucketName" j
    let to_json = simple_to_json to_value
  end
module Prefix =
  struct
    type nonrec t = string
    let context_ = "Prefix"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:10000); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Prefix" j
    let to_json = simple_to_json to_value
  end
module S3ContinuousClassificationType =
  struct
    type nonrec t =
      | FULL 
      | Non_static_id of string 
    let make i = i
    let to_string = function | FULL -> "FULL" | Non_static_id s -> s
    let of_string = function | "FULL" -> FULL | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration S3ContinuousClassificationType"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"S3ContinuousClassificationType" j)
    let to_json = simple_to_json to_value
  end
module S3OneTimeClassificationType =
  struct
    type nonrec t =
      | FULL 
      | NONE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | FULL -> "FULL" | NONE -> "NONE" | Non_static_id s -> s
    let of_string =
      function | "FULL" -> FULL | "NONE" -> NONE | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration S3OneTimeClassificationType"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"S3OneTimeClassificationType" j)
    let to_json = simple_to_json to_value
  end
module ErrorCode =
  struct
    type nonrec t = string
    let context_ = "ErrorCode"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:10); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorCode" j
    let to_json = simple_to_json to_value
  end
module ExceptionMessage =
  struct
    type nonrec t = string
    let context_ = "ExceptionMessage"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:10000); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExceptionMessage" j
    let to_json = simple_to_json to_value
  end
module S3Resource =
  struct
    type nonrec t = {
      bucketName: BucketName.t ;
      prefix: Prefix.t option }
    let context_ = "S3Resource"
    let make ?prefix = fun ~bucketName -> fun () -> { prefix; bucketName }
    let to_value x =
      structure_to_value
        [("bucketName", (Some (BucketName.to_value x.bucketName)));
        ("prefix", (Option.map x.prefix ~f:Prefix.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let prefix =
        (Option.map ~f:Prefix.of_xml) (Xml.child xml_arg0 "prefix") in
      let bucketName =
        BucketName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "bucketName") in
      make ?prefix ~bucketName ()
    let of_json json =
      let prefix = field_map json "prefix" Prefix.of_json in
      let bucketName = field_map_exn json "bucketName" BucketName.of_json in
      make ?prefix ~bucketName ()
    let to_json v = composed_to_json to_value v
  end
module ClassificationTypeUpdate =
  struct
    type nonrec t =
      {
      oneTime: S3OneTimeClassificationType.t option ;
      continuous: S3ContinuousClassificationType.t option }
    let make ?oneTime = fun ?continuous -> fun () -> { oneTime; continuous }
    let to_value x =
      structure_to_value
        [("oneTime",
           (Option.map x.oneTime ~f:S3OneTimeClassificationType.to_value));
        ("continuous",
          (Option.map x.continuous ~f:S3ContinuousClassificationType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let continuous =
        (Option.map ~f:S3ContinuousClassificationType.of_xml)
          (Xml.child xml_arg0 "continuous") in
      let oneTime =
        (Option.map ~f:S3OneTimeClassificationType.of_xml)
          (Xml.child xml_arg0 "oneTime") in
      make ?continuous ?oneTime ()
    let of_json json =
      let continuous =
        field_map json "continuous" S3ContinuousClassificationType.of_json in
      let oneTime =
        field_map json "oneTime" S3OneTimeClassificationType.of_json in
      make ?continuous ?oneTime ()
    let to_json v = composed_to_json to_value v
  end
module ClassificationType =
  struct
    type nonrec t =
      {
      oneTime: S3OneTimeClassificationType.t ;
      continuous: S3ContinuousClassificationType.t }
    let context_ = "ClassificationType"
    let make ~oneTime = fun ~continuous -> fun () -> { oneTime; continuous }
    let to_value x =
      structure_to_value
        [("oneTime", (Some (S3OneTimeClassificationType.to_value x.oneTime)));
        ("continuous",
          (Some (S3ContinuousClassificationType.to_value x.continuous)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let continuous =
        S3ContinuousClassificationType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "continuous") in
      let oneTime =
        S3OneTimeClassificationType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "oneTime") in
      make ~continuous ~oneTime ()
    let of_json json =
      let continuous =
        field_map_exn json "continuous"
          S3ContinuousClassificationType.of_json in
      let oneTime =
        field_map_exn json "oneTime" S3OneTimeClassificationType.of_json in
      make ~continuous ~oneTime ()
    let to_json v = composed_to_json to_value v
  end
module AWSAccountId =
  struct
    type nonrec t = string
    let context_ = "AWSAccountId"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[0-9]{12}"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AWSAccountId" j
    let to_json = simple_to_json to_value
  end
module ResourceType =
  struct
    type nonrec t = string
    let context_ = "ResourceType"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:1000); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceType" j
    let to_json = simple_to_json to_value
  end
module FailedS3Resource =
  struct
    type nonrec t =
      {
      failedItem: S3Resource.t option ;
      errorCode: ErrorCode.t option ;
      errorMessage: ExceptionMessage.t option }
    let make ?failedItem =
      fun ?errorCode ->
        fun ?errorMessage ->
          fun () -> { failedItem; errorCode; errorMessage }
    let to_value x =
      structure_to_value
        [("failedItem", (Option.map x.failedItem ~f:S3Resource.to_value));
        ("errorCode", (Option.map x.errorCode ~f:ErrorCode.to_value));
        ("errorMessage",
          (Option.map x.errorMessage ~f:ExceptionMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errorMessage =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "errorMessage") in
      let errorCode =
        (Option.map ~f:ErrorCode.of_xml) (Xml.child xml_arg0 "errorCode") in
      let failedItem =
        (Option.map ~f:S3Resource.of_xml) (Xml.child xml_arg0 "failedItem") in
      make ?errorMessage ?errorCode ?failedItem ()
    let of_json json =
      let errorMessage =
        field_map json "errorMessage" ExceptionMessage.of_json in
      let errorCode = field_map json "errorCode" ErrorCode.of_json in
      let failedItem = field_map json "failedItem" S3Resource.of_json in
      make ?errorMessage ?errorCode ?failedItem ()
    let to_json v = composed_to_json to_value v
  end
module FieldName =
  struct
    type nonrec t = string
    let context_ = "FieldName"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:1000); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FieldName" j
    let to_json = simple_to_json to_value
  end
module S3ResourceClassificationUpdate =
  struct
    type nonrec t =
      {
      bucketName: BucketName.t ;
      prefix: Prefix.t option ;
      classificationTypeUpdate: ClassificationTypeUpdate.t }
    let context_ = "S3ResourceClassificationUpdate"
    let make ?prefix =
      fun ~bucketName ->
        fun ~classificationTypeUpdate ->
          fun () -> { prefix; bucketName; classificationTypeUpdate }
    let to_value x =
      structure_to_value
        [("bucketName", (Some (BucketName.to_value x.bucketName)));
        ("prefix", (Option.map x.prefix ~f:Prefix.to_value));
        ("classificationTypeUpdate",
          (Some
             (ClassificationTypeUpdate.to_value x.classificationTypeUpdate)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let classificationTypeUpdate =
        ClassificationTypeUpdate.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "classificationTypeUpdate") in
      let prefix =
        (Option.map ~f:Prefix.of_xml) (Xml.child xml_arg0 "prefix") in
      let bucketName =
        BucketName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "bucketName") in
      make ~classificationTypeUpdate ?prefix ~bucketName ()
    let of_json json =
      let classificationTypeUpdate =
        field_map_exn json "classificationTypeUpdate"
          ClassificationTypeUpdate.of_json in
      let prefix = field_map json "prefix" Prefix.of_json in
      let bucketName = field_map_exn json "bucketName" BucketName.of_json in
      make ~classificationTypeUpdate ?prefix ~bucketName ()
    let to_json v = composed_to_json to_value v
  end
module S3ResourceClassification =
  struct
    type nonrec t =
      {
      bucketName: BucketName.t ;
      prefix: Prefix.t option ;
      classificationType: ClassificationType.t }
    let context_ = "S3ResourceClassification"
    let make ?prefix =
      fun ~bucketName ->
        fun ~classificationType ->
          fun () -> { prefix; bucketName; classificationType }
    let to_value x =
      structure_to_value
        [("bucketName", (Some (BucketName.to_value x.bucketName)));
        ("prefix", (Option.map x.prefix ~f:Prefix.to_value));
        ("classificationType",
          (Some (ClassificationType.to_value x.classificationType)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let classificationType =
        ClassificationType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "classificationType") in
      let prefix =
        (Option.map ~f:Prefix.of_xml) (Xml.child xml_arg0 "prefix") in
      let bucketName =
        BucketName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "bucketName") in
      make ~classificationType ?prefix ~bucketName ()
    let of_json json =
      let classificationType =
        field_map_exn json "classificationType" ClassificationType.of_json in
      let prefix = field_map json "prefix" Prefix.of_json in
      let bucketName = field_map_exn json "bucketName" BucketName.of_json in
      make ~classificationType ?prefix ~bucketName ()
    let to_json v = composed_to_json to_value v
  end
module MemberAccount =
  struct
    type nonrec t = {
      accountId: AWSAccountId.t option }
    let make ?accountId = fun () -> { accountId }
    let to_value x =
      structure_to_value
        [("accountId", (Option.map x.accountId ~f:AWSAccountId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let accountId =
        (Option.map ~f:AWSAccountId.of_xml) (Xml.child xml_arg0 "accountId") in
      make ?accountId ()
    let of_json json =
      let accountId = field_map json "accountId" AWSAccountId.of_json in
      make ?accountId ()
    let to_json v = composed_to_json to_value v
  end
module AccessDeniedException =
  struct
    type nonrec t =
      {
      message: ExceptionMessage.t option ;
      resourceType: ResourceType.t option }
    let make ?message =
      fun ?resourceType -> fun () -> { message; resourceType }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ExceptionMessage.to_value));
        ("resourceType",
          (Option.map x.resourceType ~f:ResourceType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "resourceType") in
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "message") in
      make ?resourceType ?message ()
    let of_json json =
      let resourceType = field_map json "resourceType" ResourceType.of_json in
      let message = field_map json "message" ExceptionMessage.of_json in
      make ?resourceType ?message ()
    let to_json v = composed_to_json to_value v
  end
module FailedS3Resources =
  struct
    type nonrec t = FailedS3Resource.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:FailedS3Resource.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FailedS3Resource.of_xml)
    let of_json j =
      list_of_json ~kind:"FailedS3Resources"
        ~of_json:FailedS3Resource.of_json j
    let to_json v = composed_to_json to_value v
  end
module InternalException =
  struct
    type nonrec t =
      {
      errorCode: ErrorCode.t option ;
      message: ExceptionMessage.t option }
    let make ?errorCode = fun ?message -> fun () -> { errorCode; message }
    let to_value x =
      structure_to_value
        [("errorCode", (Option.map x.errorCode ~f:ErrorCode.to_value));
        ("message", (Option.map x.message ~f:ExceptionMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "message") in
      let errorCode =
        (Option.map ~f:ErrorCode.of_xml) (Xml.child xml_arg0 "errorCode") in
      make ?message ?errorCode ()
    let of_json json =
      let message = field_map json "message" ExceptionMessage.of_json in
      let errorCode = field_map json "errorCode" ErrorCode.of_json in
      make ?message ?errorCode ()
    let to_json v = composed_to_json to_value v
  end
module InvalidInputException =
  struct
    type nonrec t =
      {
      errorCode: ErrorCode.t option ;
      message: ExceptionMessage.t option ;
      fieldName: FieldName.t option }
    let make ?errorCode =
      fun ?message ->
        fun ?fieldName -> fun () -> { errorCode; message; fieldName }
    let to_value x =
      structure_to_value
        [("errorCode", (Option.map x.errorCode ~f:ErrorCode.to_value));
        ("message", (Option.map x.message ~f:ExceptionMessage.to_value));
        ("fieldName", (Option.map x.fieldName ~f:FieldName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fieldName =
        (Option.map ~f:FieldName.of_xml) (Xml.child xml_arg0 "fieldName") in
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "message") in
      let errorCode =
        (Option.map ~f:ErrorCode.of_xml) (Xml.child xml_arg0 "errorCode") in
      make ?fieldName ?message ?errorCode ()
    let of_json json =
      let fieldName = field_map json "fieldName" FieldName.of_json in
      let message = field_map json "message" ExceptionMessage.of_json in
      let errorCode = field_map json "errorCode" ErrorCode.of_json in
      make ?fieldName ?message ?errorCode ()
    let to_json v = composed_to_json to_value v
  end
module S3ResourcesClassificationUpdate =
  struct
    type nonrec t = S3ResourceClassificationUpdate.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:S3ResourceClassificationUpdate.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:S3ResourceClassificationUpdate.of_xml)
    let of_json j =
      list_of_json ~kind:"S3ResourcesClassificationUpdate"
        ~of_json:S3ResourceClassificationUpdate.of_json j
    let to_json v = composed_to_json to_value v
  end
module NextToken =
  struct
    type nonrec t = string
    let context_ = "NextToken"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:500); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NextToken" j
    let to_json = simple_to_json to_value
  end
module S3ResourcesClassification =
  struct
    type nonrec t = S3ResourceClassification.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:S3ResourceClassification.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:S3ResourceClassification.of_xml)
    let of_json j =
      list_of_json ~kind:"S3ResourcesClassification"
        ~of_json:S3ResourceClassification.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_max i ~max:250); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module MemberAccounts =
  struct
    type nonrec t = MemberAccount.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:MemberAccount.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MemberAccount.of_xml)
    let of_json j =
      list_of_json ~kind:"MemberAccounts" ~of_json:MemberAccount.of_json j
    let to_json v = composed_to_json to_value v
  end
module S3Resources =
  struct
    type nonrec t = S3Resource.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:S3Resource.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:S3Resource.of_xml)
    let of_json j =
      list_of_json ~kind:"S3Resources" ~of_json:S3Resource.of_json j
    let to_json v = composed_to_json to_value v
  end
module LimitExceededException =
  struct
    type nonrec t =
      {
      errorCode: ErrorCode.t option ;
      message: ExceptionMessage.t option ;
      resourceType: ResourceType.t option }
    let make ?errorCode =
      fun ?message ->
        fun ?resourceType -> fun () -> { errorCode; message; resourceType }
    let to_value x =
      structure_to_value
        [("errorCode", (Option.map x.errorCode ~f:ErrorCode.to_value));
        ("message", (Option.map x.message ~f:ExceptionMessage.to_value));
        ("resourceType",
          (Option.map x.resourceType ~f:ResourceType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "resourceType") in
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "message") in
      let errorCode =
        (Option.map ~f:ErrorCode.of_xml) (Xml.child xml_arg0 "errorCode") in
      make ?resourceType ?message ?errorCode ()
    let of_json json =
      let resourceType = field_map json "resourceType" ResourceType.of_json in
      let message = field_map json "message" ExceptionMessage.of_json in
      let errorCode = field_map json "errorCode" ErrorCode.of_json in
      make ?resourceType ?message ?errorCode ()
    let to_json v = composed_to_json to_value v
  end
module UpdateS3ResourcesResult =
  struct
    type nonrec t = {
      failedS3Resources: FailedS3Resources.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalException of InternalException.t 
      | `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?failedS3Resources = fun () -> { failedS3Resources }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("failedS3Resources",
           (Option.map x.failedS3Resources ~f:FailedS3Resources.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failedS3Resources =
        (Option.map ~f:FailedS3Resources.of_xml)
          (Xml.child xml_arg0 "failedS3Resources") in
      make ?failedS3Resources ()
    let of_json json =
      let failedS3Resources =
        field_map json "failedS3Resources" FailedS3Resources.of_json in
      make ?failedS3Resources ()
    let to_json v = composed_to_json to_value v
  end
module UpdateS3ResourcesRequest =
  struct
    type nonrec t =
      {
      memberAccountId: AWSAccountId.t option ;
      s3ResourcesUpdate: S3ResourcesClassificationUpdate.t }
    let context_ = "UpdateS3ResourcesRequest"
    let make ?memberAccountId =
      fun ~s3ResourcesUpdate ->
        fun () -> { memberAccountId; s3ResourcesUpdate }
    let to_value x =
      structure_to_value
        [("memberAccountId",
           (Option.map x.memberAccountId ~f:AWSAccountId.to_value));
        ("s3ResourcesUpdate",
          (Some
             (S3ResourcesClassificationUpdate.to_value x.s3ResourcesUpdate)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3ResourcesUpdate =
        S3ResourcesClassificationUpdate.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "s3ResourcesUpdate") in
      let memberAccountId =
        (Option.map ~f:AWSAccountId.of_xml)
          (Xml.child xml_arg0 "memberAccountId") in
      make ~s3ResourcesUpdate ?memberAccountId ()
    let of_json json =
      let s3ResourcesUpdate =
        field_map_exn json "s3ResourcesUpdate"
          S3ResourcesClassificationUpdate.of_json in
      let memberAccountId =
        field_map json "memberAccountId" AWSAccountId.of_json in
      make ~s3ResourcesUpdate ?memberAccountId ()
    let to_json v = composed_to_json to_value v
  end
module ListS3ResourcesResult =
  struct
    type nonrec t =
      {
      s3Resources: S3ResourcesClassification.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalException of InternalException.t 
      | `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?s3Resources =
      fun ?nextToken -> fun () -> { s3Resources; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("s3Resources",
           (Option.map x.s3Resources ~f:S3ResourcesClassification.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let s3Resources =
        (Option.map ~f:S3ResourcesClassification.of_xml)
          (Xml.child xml_arg0 "s3Resources") in
      make ?nextToken ?s3Resources ()
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let s3Resources =
        field_map json "s3Resources" S3ResourcesClassification.of_json in
      make ?nextToken ?s3Resources ()
    let to_json v = composed_to_json to_value v
  end
module ListS3ResourcesRequest =
  struct
    type nonrec t =
      {
      memberAccountId: AWSAccountId.t option ;
      nextToken: NextToken.t option ;
      maxResults: MaxResults.t option }
    let make ?memberAccountId =
      fun ?nextToken ->
        fun ?maxResults ->
          fun () -> { memberAccountId; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("memberAccountId",
           (Option.map x.memberAccountId ~f:AWSAccountId.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let memberAccountId =
        (Option.map ~f:AWSAccountId.of_xml)
          (Xml.child xml_arg0 "memberAccountId") in
      make ?maxResults ?nextToken ?memberAccountId ()
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let memberAccountId =
        field_map json "memberAccountId" AWSAccountId.of_json in
      make ?maxResults ?nextToken ?memberAccountId ()
    let to_json v = composed_to_json to_value v
  end
module ListMemberAccountsResult =
  struct
    type nonrec t =
      {
      memberAccounts: MemberAccounts.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `InternalException of InternalException.t 
      | `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?memberAccounts =
      fun ?nextToken -> fun () -> { memberAccounts; nextToken }
    let error_of_json name json =
      match name with
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("memberAccounts",
           (Option.map x.memberAccounts ~f:MemberAccounts.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let memberAccounts =
        (Option.map ~f:MemberAccounts.of_xml)
          (Xml.child xml_arg0 "memberAccounts") in
      make ?nextToken ?memberAccounts ()
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let memberAccounts =
        field_map json "memberAccounts" MemberAccounts.of_json in
      make ?nextToken ?memberAccounts ()
    let to_json v = composed_to_json to_value v
  end
module ListMemberAccountsRequest =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option ;
      maxResults: MaxResults.t option }
    let make ?nextToken =
      fun ?maxResults -> fun () -> { nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?maxResults ?nextToken ()
    let of_json json =
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      make ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateS3ResourcesResult =
  struct
    type nonrec t = {
      failedS3Resources: FailedS3Resources.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalException of InternalException.t 
      | `InvalidInputException of InvalidInputException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?failedS3Resources = fun () -> { failedS3Resources }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("failedS3Resources",
           (Option.map x.failedS3Resources ~f:FailedS3Resources.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failedS3Resources =
        (Option.map ~f:FailedS3Resources.of_xml)
          (Xml.child xml_arg0 "failedS3Resources") in
      make ?failedS3Resources ()
    let of_json json =
      let failedS3Resources =
        field_map json "failedS3Resources" FailedS3Resources.of_json in
      make ?failedS3Resources ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateS3ResourcesRequest =
  struct
    type nonrec t =
      {
      memberAccountId: AWSAccountId.t option ;
      associatedS3Resources: S3Resources.t }
    let context_ = "DisassociateS3ResourcesRequest"
    let make ?memberAccountId =
      fun ~associatedS3Resources ->
        fun () -> { memberAccountId; associatedS3Resources }
    let to_value x =
      structure_to_value
        [("memberAccountId",
           (Option.map x.memberAccountId ~f:AWSAccountId.to_value));
        ("associatedS3Resources",
          (Some (S3Resources.to_value x.associatedS3Resources)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let associatedS3Resources =
        S3Resources.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "associatedS3Resources") in
      let memberAccountId =
        (Option.map ~f:AWSAccountId.of_xml)
          (Xml.child xml_arg0 "memberAccountId") in
      make ~associatedS3Resources ?memberAccountId ()
    let of_json json =
      let associatedS3Resources =
        field_map_exn json "associatedS3Resources" S3Resources.of_json in
      let memberAccountId =
        field_map json "memberAccountId" AWSAccountId.of_json in
      make ~associatedS3Resources ?memberAccountId ()
    let to_json v = composed_to_json to_value v
  end
module DisassociateMemberAccountRequest =
  struct
    type nonrec t = {
      memberAccountId: AWSAccountId.t }
    let context_ = "DisassociateMemberAccountRequest"
    let make ~memberAccountId = fun () -> { memberAccountId }
    let to_value x =
      structure_to_value
        [("memberAccountId",
           (Some (AWSAccountId.to_value x.memberAccountId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let memberAccountId =
        AWSAccountId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "memberAccountId") in
      make ~memberAccountId ()
    let of_json json =
      let memberAccountId =
        field_map_exn json "memberAccountId" AWSAccountId.of_json in
      make ~memberAccountId ()
    let to_json v = composed_to_json to_value v
  end
module AssociateS3ResourcesResult =
  struct
    type nonrec t = {
      failedS3Resources: FailedS3Resources.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalException of InternalException.t 
      | `InvalidInputException of InvalidInputException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?failedS3Resources = fun () -> { failedS3Resources }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalException" ->
          `InternalException (InternalException.of_json json)
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalException" ->
          `InternalException (InternalException.of_xml xml)
      | "InvalidInputException" ->
          `InvalidInputException (InvalidInputException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalException e ->
          `Assoc
            [("error", (`String "InternalException"));
            ("details", (InternalException.to_json e))]
      | `InvalidInputException e ->
          `Assoc
            [("error", (`String "InvalidInputException"));
            ("details", (InvalidInputException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("failedS3Resources",
           (Option.map x.failedS3Resources ~f:FailedS3Resources.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failedS3Resources =
        (Option.map ~f:FailedS3Resources.of_xml)
          (Xml.child xml_arg0 "failedS3Resources") in
      make ?failedS3Resources ()
    let of_json json =
      let failedS3Resources =
        field_map json "failedS3Resources" FailedS3Resources.of_json in
      make ?failedS3Resources ()
    let to_json v = composed_to_json to_value v
  end
module AssociateS3ResourcesRequest =
  struct
    type nonrec t =
      {
      memberAccountId: AWSAccountId.t option ;
      s3Resources: S3ResourcesClassification.t }
    let context_ = "AssociateS3ResourcesRequest"
    let make ?memberAccountId =
      fun ~s3Resources -> fun () -> { memberAccountId; s3Resources }
    let to_value x =
      structure_to_value
        [("memberAccountId",
           (Option.map x.memberAccountId ~f:AWSAccountId.to_value));
        ("s3Resources",
          (Some (S3ResourcesClassification.to_value x.s3Resources)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3Resources =
        S3ResourcesClassification.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "s3Resources") in
      let memberAccountId =
        (Option.map ~f:AWSAccountId.of_xml)
          (Xml.child xml_arg0 "memberAccountId") in
      make ~s3Resources ?memberAccountId ()
    let of_json json =
      let s3Resources =
        field_map_exn json "s3Resources" S3ResourcesClassification.of_json in
      let memberAccountId =
        field_map json "memberAccountId" AWSAccountId.of_json in
      make ~s3Resources ?memberAccountId ()
    let to_json v = composed_to_json to_value v
  end
module AssociateMemberAccountRequest =
  struct
    type nonrec t = {
      memberAccountId: AWSAccountId.t }
    let context_ = "AssociateMemberAccountRequest"
    let make ~memberAccountId = fun () -> { memberAccountId }
    let to_value x =
      structure_to_value
        [("memberAccountId",
           (Some (AWSAccountId.to_value x.memberAccountId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let memberAccountId =
        AWSAccountId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "memberAccountId") in
      make ~memberAccountId ()
    let of_json json =
      let memberAccountId =
        field_map_exn json "memberAccountId" AWSAccountId.of_json in
      make ~memberAccountId ()
    let to_json v = composed_to_json to_value v
  end
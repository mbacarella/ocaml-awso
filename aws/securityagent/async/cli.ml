(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_artifact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId"
       and artifactContent =
         flag "artifact-content" (required json_arg) ~doc:"JSON Blob"
       and artifactType =
         flag "artifact-type" (required json_arg) ~doc:"JSON ArtifactType"
       and fileName = flag "file-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_artifact
           (Values.AddArtifactInput.make ~agentSpaceId
              ~artifactContent:(Values.Blob.of_json artifactContent)
              ~artifactType:(Values.ArtifactType.of_json artifactType)
              ~fileName ()) (Some Values.AddArtifactOutput.to_json)
           (Some Values.AddArtifactOutput.error_to_json)])
let batch_delete_code_reviews =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and codeReviewIds =
         flag "code-review-ids" (required json_arg)
           ~doc:"JSON CodeReviewIdList"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_code_reviews
           (Values.BatchDeleteCodeReviewsInput.make
              ~codeReviewIds:(Values.CodeReviewIdList.of_json codeReviewIds)
              ~agentSpaceId ())
           (Some Values.BatchDeleteCodeReviewsOutput.to_json)
           (Some Values.BatchDeleteCodeReviewsOutput.error_to_json)])
let batch_delete_pentests =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pentestIds =
         flag "pentest-ids" (required json_arg) ~doc:"JSON PentestIdList"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_pentests
           (Values.BatchDeletePentestsInput.make
              ~pentestIds:(Values.PentestIdList.of_json pentestIds)
              ~agentSpaceId ())
           (Some Values.BatchDeletePentestsOutput.to_json)
           (Some Values.BatchDeletePentestsOutput.error_to_json)])
let batch_get_agent_spaces =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentSpaceIds =
         flag "agent-space-ids" (required json_arg)
           ~doc:"JSON AgentSpaceIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_agent_spaces
           (Values.BatchGetAgentSpacesInput.make
              ~agentSpaceIds:(Values.AgentSpaceIdList.of_json agentSpaceIds)
              ()) (Some Values.BatchGetAgentSpacesOutput.to_json)
           (Some Values.BatchGetAgentSpacesOutput.error_to_json)])
let batch_get_artifact_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId"
       and artifactIds =
         flag "artifact-ids" (required json_arg) ~doc:"JSON ArtifactIds" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_artifact_metadata
           (Values.BatchGetArtifactMetadataInput.make ~agentSpaceId
              ~artifactIds:(Values.ArtifactIds.of_json artifactIds) ())
           (Some Values.BatchGetArtifactMetadataOutput.to_json)
           (Some Values.BatchGetArtifactMetadataOutput.error_to_json)])
let batch_get_code_review_job_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String"
       and codeReviewJobTaskIds =
         flag "code-review-job-task-ids" (required json_arg)
           ~doc:"JSON TaskIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_code_review_job_tasks
           (Values.BatchGetCodeReviewJobTasksInput.make ~agentSpaceId
              ~codeReviewJobTaskIds:(Values.TaskIdList.of_json
                                       codeReviewJobTaskIds) ())
           (Some Values.BatchGetCodeReviewJobTasksOutput.to_json)
           (Some Values.BatchGetCodeReviewJobTasksOutput.error_to_json)])
let batch_get_code_review_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and codeReviewJobIds =
         flag "code-review-job-ids" (required json_arg)
           ~doc:"JSON CodeReviewJobIdList"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_code_review_jobs
           (Values.BatchGetCodeReviewJobsInput.make
              ~codeReviewJobIds:(Values.CodeReviewJobIdList.of_json
                                   codeReviewJobIds) ~agentSpaceId ())
           (Some Values.BatchGetCodeReviewJobsOutput.to_json)
           (Some Values.BatchGetCodeReviewJobsOutput.error_to_json)])
let batch_get_code_reviews =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and codeReviewIds =
         flag "code-review-ids" (required json_arg)
           ~doc:"JSON CodeReviewIdList"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_code_reviews
           (Values.BatchGetCodeReviewsInput.make
              ~codeReviewIds:(Values.CodeReviewIdList.of_json codeReviewIds)
              ~agentSpaceId ())
           (Some Values.BatchGetCodeReviewsOutput.to_json)
           (Some Values.BatchGetCodeReviewsOutput.error_to_json)])
let batch_get_findings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and findingIds =
         flag "finding-ids" (required json_arg) ~doc:"JSON FindingIdList"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_findings
           (Values.BatchGetFindingsInput.make
              ~findingIds:(Values.FindingIdList.of_json findingIds)
              ~agentSpaceId ()) (Some Values.BatchGetFindingsOutput.to_json)
           (Some Values.BatchGetFindingsOutput.error_to_json)])
let batch_get_pentest_job_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String"
       and taskIds =
         flag "task-ids" (required json_arg) ~doc:"JSON TaskIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_pentest_job_tasks
           (Values.BatchGetPentestJobTasksInput.make ~agentSpaceId
              ~taskIds:(Values.TaskIdList.of_json taskIds) ())
           (Some Values.BatchGetPentestJobTasksOutput.to_json)
           (Some Values.BatchGetPentestJobTasksOutput.error_to_json)])
let batch_get_pentest_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pentestJobIds =
         flag "pentest-job-ids" (required json_arg)
           ~doc:"JSON PentestJobIdList"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_pentest_jobs
           (Values.BatchGetPentestJobsInput.make
              ~pentestJobIds:(Values.PentestJobIdList.of_json pentestJobIds)
              ~agentSpaceId ())
           (Some Values.BatchGetPentestJobsOutput.to_json)
           (Some Values.BatchGetPentestJobsOutput.error_to_json)])
let batch_get_pentests =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pentestIds =
         flag "pentest-ids" (required json_arg) ~doc:"JSON PentestIdList"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_pentests
           (Values.BatchGetPentestsInput.make
              ~pentestIds:(Values.PentestIdList.of_json pentestIds)
              ~agentSpaceId ()) (Some Values.BatchGetPentestsOutput.to_json)
           (Some Values.BatchGetPentestsOutput.error_to_json)])
let batch_get_target_domains =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetDomainIds =
         flag "target-domain-ids" (required json_arg)
           ~doc:"JSON TargetDomainIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_target_domains
           (Values.BatchGetTargetDomainsInput.make
              ~targetDomainIds:(Values.TargetDomainIdList.of_json
                                  targetDomainIds) ())
           (Some Values.BatchGetTargetDomainsOutput.to_json)
           (Some Values.BatchGetTargetDomainsOutput.error_to_json)])
let create_agent_space =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and awsResources =
         flag "aws-resources" (optional json_arg) ~doc:"JSON AWSResources"
       and targetDomainIds =
         flag "target-domain-ids" (optional json_arg)
           ~doc:"JSON TargetDomainIdList"
       and codeReviewSettings =
         flag "code-review-settings" (optional json_arg)
           ~doc:"JSON CodeReviewSettings"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name = flag "name" (required string) ~doc:"STRING AgentName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_agent_space
           (Values.CreateAgentSpaceInput.make ?description
              ?awsResources:(Option.map ~f:Values.AWSResources.of_json
                               awsResources)
              ?targetDomainIds:(Option.map
                                  ~f:Values.TargetDomainIdList.of_json
                                  targetDomainIds)
              ?codeReviewSettings:(Option.map
                                     ~f:Values.CodeReviewSettings.of_json
                                     codeReviewSettings) ?kmsKeyId
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name ())
           (Some Values.CreateAgentSpaceOutput.to_json)
           (Some Values.CreateAgentSpaceOutput.error_to_json)])
let create_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and idcInstanceArn =
         flag "idc-instance-arn" (optional string)
           ~doc:"STRING IdCInstanceArn"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and defaultKmsKeyId =
         flag "default-kms-key-id" (optional string)
           ~doc:"STRING DefaultKmsKeyId"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_application
           (Values.CreateApplicationRequest.make ?idcInstanceArn ?roleArn
              ?defaultKmsKeyId
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ())
           (Some Values.CreateApplicationResponse.to_json)
           (Some Values.CreateApplicationResponse.error_to_json)])
let create_code_review =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceRole =
         flag "service-role" (optional string) ~doc:"STRING ServiceRole"
       and logConfig =
         flag "log-config" (optional json_arg) ~doc:"JSON CloudWatchLog"
       and codeRemediationStrategy =
         flag "code-remediation-strategy" (optional json_arg)
           ~doc:"JSON CodeRemediationStrategy"
       and title = flag "title" (required string) ~doc:"STRING String"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String"
       and assets = flag "assets" (required json_arg) ~doc:"JSON Assets" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_code_review
           (Values.CreateCodeReviewInput.make ?serviceRole
              ?logConfig:(Option.map ~f:Values.CloudWatchLog.of_json
                            logConfig)
              ?codeRemediationStrategy:(Option.map
                                          ~f:Values.CodeRemediationStrategy.of_json
                                          codeRemediationStrategy) ~title
              ~agentSpaceId ~assets:(Values.Assets.of_json assets) ())
           (Some Values.CreateCodeReviewOutput.to_json)
           (Some Values.CreateCodeReviewOutput.error_to_json)])
let create_integration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING KmsKeyId"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and provider =
         flag "provider" (required json_arg) ~doc:"JSON Provider"
       and input = flag "input" (required json_arg) ~doc:"JSON ProviderInput"
       and integrationDisplayName =
         flag "integration-display-name" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_integration
           (Values.CreateIntegrationInput.make ?kmsKeyId
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~provider:(Values.Provider.of_json provider)
              ~input:(Values.ProviderInput.of_json input)
              ~integrationDisplayName ())
           (Some Values.CreateIntegrationOutput.to_json)
           (Some Values.CreateIntegrationOutput.error_to_json)])
let create_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and config =
         flag "config" (optional json_arg) ~doc:"JSON MembershipConfig"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId"
       and membershipId =
         flag "membership-id" (required string) ~doc:"STRING MembershipId"
       and memberType =
         flag "member-type" (required json_arg) ~doc:"JSON MembershipType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_membership
           (Values.CreateMembershipRequest.make
              ?config:(Option.map ~f:Values.MembershipConfig.of_json config)
              ~applicationId ~agentSpaceId ~membershipId
              ~memberType:(Values.MembershipType.of_json memberType) ())
           (Some Values.CreateMembershipResponse.to_json)
           (Some Values.CreateMembershipResponse.error_to_json)])
let create_pentest =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and assets = flag "assets" (optional json_arg) ~doc:"JSON Assets"
       and excludeRiskTypes =
         flag "exclude-risk-types" (optional json_arg)
           ~doc:"JSON RiskTypeList"
       and serviceRole =
         flag "service-role" (optional string) ~doc:"STRING ServiceRole"
       and logConfig =
         flag "log-config" (optional json_arg) ~doc:"JSON CloudWatchLog"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON VpcConfig"
       and networkTrafficConfig =
         flag "network-traffic-config" (optional json_arg)
           ~doc:"JSON NetworkTrafficConfig"
       and codeRemediationStrategy =
         flag "code-remediation-strategy" (optional json_arg)
           ~doc:"JSON CodeRemediationStrategy"
       and title = flag "title" (required string) ~doc:"STRING String"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_pentest
           (Values.CreatePentestInput.make
              ?assets:(Option.map ~f:Values.Assets.of_json assets)
              ?excludeRiskTypes:(Option.map ~f:Values.RiskTypeList.of_json
                                   excludeRiskTypes) ?serviceRole
              ?logConfig:(Option.map ~f:Values.CloudWatchLog.of_json
                            logConfig)
              ?vpcConfig:(Option.map ~f:Values.VpcConfig.of_json vpcConfig)
              ?networkTrafficConfig:(Option.map
                                       ~f:Values.NetworkTrafficConfig.of_json
                                       networkTrafficConfig)
              ?codeRemediationStrategy:(Option.map
                                          ~f:Values.CodeRemediationStrategy.of_json
                                          codeRemediationStrategy) ~title
              ~agentSpaceId ()) (Some Values.CreatePentestOutput.to_json)
           (Some Values.CreatePentestOutput.error_to_json)])
let create_target_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and targetDomainName =
         flag "target-domain-name" (required string) ~doc:"STRING String"
       and verificationMethod =
         flag "verification-method" (required json_arg)
           ~doc:"JSON DomainVerificationMethod" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_target_domain
           (Values.CreateTargetDomainInput.make
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~targetDomainName
              ~verificationMethod:(Values.DomainVerificationMethod.of_json
                                     verificationMethod) ())
           (Some Values.CreateTargetDomainOutput.to_json)
           (Some Values.CreateTargetDomainOutput.error_to_json)])
let delete_agent_space =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_agent_space
           (Values.DeleteAgentSpaceInput.make ~agentSpaceId ())
           (Some Values.DeleteAgentSpaceOutput.to_json)
           (Some Values.DeleteAgentSpaceOutput.error_to_json)])
let delete_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_application
           (Values.DeleteApplicationRequest.make ~applicationId ()) None None])
let delete_artifact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId"
       and artifactId =
         flag "artifact-id" (required string) ~doc:"STRING ArtifactId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_artifact
           (Values.DeleteArtifactInput.make ~agentSpaceId ~artifactId ())
           (Some Values.DeleteArtifactOutput.to_json)
           (Some Values.DeleteArtifactOutput.error_to_json)])
let delete_integration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and integrationId =
         flag "integration-id" (required string) ~doc:"STRING IntegrationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_integration
           (Values.DeleteIntegrationInput.make ~integrationId ())
           (Some Values.DeleteIntegrationOutput.to_json)
           (Some Values.DeleteIntegrationOutput.error_to_json)])
let delete_membership =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and memberType =
         flag "member-type" (optional json_arg) ~doc:"JSON MembershipType"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId"
       and membershipId =
         flag "membership-id" (required string) ~doc:"STRING MembershipId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_membership
           (Values.DeleteMembershipRequest.make
              ?memberType:(Option.map ~f:Values.MembershipType.of_json
                             memberType) ~applicationId ~agentSpaceId
              ~membershipId ())
           (Some Values.DeleteMembershipResponse.to_json)
           (Some Values.DeleteMembershipResponse.error_to_json)])
let delete_target_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetDomainId =
         flag "target-domain-id" (required string)
           ~doc:"STRING TargetDomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_target_domain
           (Values.DeleteTargetDomainInput.make ~targetDomainId ())
           (Some Values.DeleteTargetDomainOutput.to_json)
           (Some Values.DeleteTargetDomainOutput.error_to_json)])
let get_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_application
           (Values.GetApplicationRequest.make ~applicationId ())
           (Some Values.GetApplicationResponse.to_json)
           (Some Values.GetApplicationResponse.error_to_json)])
let get_artifact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId"
       and artifactId =
         flag "artifact-id" (required string) ~doc:"STRING ArtifactId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_artifact
           (Values.GetArtifactInput.make ~agentSpaceId ~artifactId ())
           (Some Values.GetArtifactOutput.to_json)
           (Some Values.GetArtifactOutput.error_to_json)])
let get_integration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and integrationId =
         flag "integration-id" (required string) ~doc:"STRING IntegrationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_integration
           (Values.GetIntegrationInput.make ~integrationId ())
           (Some Values.GetIntegrationOutput.to_json)
           (Some Values.GetIntegrationOutput.error_to_json)])
let initiate_provider_registration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and provider =
         flag "provider" (required json_arg) ~doc:"JSON Provider" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.initiate_provider_registration
           (Values.InitiateProviderRegistrationInput.make
              ~provider:(Values.Provider.of_json provider) ())
           (Some Values.InitiateProviderRegistrationOutput.to_json)
           (Some Values.InitiateProviderRegistrationOutput.error_to_json)])
let list_agent_spaces =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_agent_spaces
           (Values.ListAgentSpacesInput.make ?nextToken ?maxResults ())
           (Some Values.ListAgentSpacesOutput.to_json)
           (Some Values.ListAgentSpacesOutput.error_to_json)])
let list_applications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_applications
           (Values.ListApplicationsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListApplicationsResponse.to_json)
           (Some Values.ListApplicationsResponse.error_to_json)])
let list_artifacts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_artifacts
           (Values.ListArtifactsInput.make ?nextToken ?maxResults
              ~agentSpaceId ()) (Some Values.ListArtifactsOutput.to_json)
           (Some Values.ListArtifactsOutput.error_to_json)])
let list_code_review_job_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and codeReviewJobId =
         flag "code-review-job-id" (optional string) ~doc:"STRING String"
       and stepName =
         flag "step-name" (optional json_arg) ~doc:"JSON StepName"
       and categoryName =
         flag "category-name" (optional string) ~doc:"STRING String"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_code_review_job_tasks
           (Values.ListCodeReviewJobTasksInput.make ?maxResults
              ?codeReviewJobId
              ?stepName:(Option.map ~f:Values.StepName.of_json stepName)
              ?categoryName ?nextToken ~agentSpaceId ())
           (Some Values.ListCodeReviewJobTasksOutput.to_json)
           (Some Values.ListCodeReviewJobTasksOutput.error_to_json)])
let list_code_review_jobs_for_code_review =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and codeReviewId =
         flag "code-review-id" (required string) ~doc:"STRING String"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_code_review_jobs_for_code_review
           (Values.ListCodeReviewJobsForCodeReviewInput.make ?maxResults
              ?nextToken ~codeReviewId ~agentSpaceId ())
           (Some Values.ListCodeReviewJobsForCodeReviewOutput.to_json)
           (Some Values.ListCodeReviewJobsForCodeReviewOutput.error_to_json)])
let list_code_reviews =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_code_reviews
           (Values.ListCodeReviewsInput.make ?maxResults ?nextToken
              ~agentSpaceId ()) (Some Values.ListCodeReviewsOutput.to_json)
           (Some Values.ListCodeReviewsOutput.error_to_json)])
let list_discovered_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and prefix = flag "prefix" (optional string) ~doc:"STRING String"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and pentestJobId =
         flag "pentest-job-id" (required string) ~doc:"STRING String"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_discovered_endpoints
           (Values.ListDiscoveredEndpointsInput.make ?maxResults ?prefix
              ?nextToken ~pentestJobId ~agentSpaceId ())
           (Some Values.ListDiscoveredEndpointsOutput.to_json)
           (Some Values.ListDiscoveredEndpointsOutput.error_to_json)])
let list_findings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and pentestJobId =
         flag "pentest-job-id" (optional string) ~doc:"STRING String"
       and codeReviewJobId =
         flag "code-review-job-id" (optional string) ~doc:"STRING String"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and riskType = flag "risk-type" (optional string) ~doc:"STRING String"
       and riskLevel =
         flag "risk-level" (optional json_arg) ~doc:"JSON RiskLevel"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON FindingStatus"
       and confidence =
         flag "confidence" (optional json_arg) ~doc:"JSON ConfidenceLevel"
       and name = flag "name" (optional string) ~doc:"STRING String"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_findings
           (Values.ListFindingsInput.make ?maxResults ?pentestJobId
              ?codeReviewJobId ?nextToken ?riskType
              ?riskLevel:(Option.map ~f:Values.RiskLevel.of_json riskLevel)
              ?status:(Option.map ~f:Values.FindingStatus.of_json status)
              ?confidence:(Option.map ~f:Values.ConfidenceLevel.of_json
                             confidence) ?name ~agentSpaceId ())
           (Some Values.ListFindingsOutput.to_json)
           (Some Values.ListFindingsOutput.error_to_json)])
let list_integrated_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and integrationId =
         flag "integration-id" (optional string) ~doc:"STRING IntegrationId"
       and resourceType =
         flag "resource-type" (optional json_arg) ~doc:"JSON ResourceType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_integrated_resources
           (Values.ListIntegratedResourcesInput.make ?integrationId
              ?resourceType:(Option.map ~f:Values.ResourceType.of_json
                               resourceType) ?nextToken ?maxResults
              ~agentSpaceId ())
           (Some Values.ListIntegratedResourcesOutput.to_json)
           (Some Values.ListIntegratedResourcesOutput.error_to_json)])
let list_integrations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON IntegrationFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_integrations
           (Values.ListIntegrationsInput.make
              ?filter:(Option.map ~f:Values.IntegrationFilter.of_json filter)
              ?nextToken ?maxResults ())
           (Some Values.ListIntegrationsOutput.to_json)
           (Some Values.ListIntegrationsOutput.error_to_json)])
let list_memberships =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and memberType =
         flag "member-type" (optional json_arg)
           ~doc:"JSON MembershipTypeFilter"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_memberships
           (Values.ListMembershipsRequest.make
              ?memberType:(Option.map ~f:Values.MembershipTypeFilter.of_json
                             memberType) ?maxResults ?nextToken
              ~applicationId ~agentSpaceId ())
           (Some Values.ListMembershipsResponse.to_json)
           (Some Values.ListMembershipsResponse.error_to_json)])
let list_pentest_job_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and pentestJobId =
         flag "pentest-job-id" (optional string) ~doc:"STRING String"
       and stepName =
         flag "step-name" (optional json_arg) ~doc:"JSON StepName"
       and categoryName =
         flag "category-name" (optional string) ~doc:"STRING String"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_pentest_job_tasks
           (Values.ListPentestJobTasksInput.make ?maxResults ?pentestJobId
              ?stepName:(Option.map ~f:Values.StepName.of_json stepName)
              ?categoryName ?nextToken ~agentSpaceId ())
           (Some Values.ListPentestJobTasksOutput.to_json)
           (Some Values.ListPentestJobTasksOutput.error_to_json)])
let list_pentest_jobs_for_pentest =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and pentestId =
         flag "pentest-id" (required string) ~doc:"STRING String"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_pentest_jobs_for_pentest
           (Values.ListPentestJobsForPentestInput.make ?maxResults ?nextToken
              ~pentestId ~agentSpaceId ())
           (Some Values.ListPentestJobsForPentestOutput.to_json)
           (Some Values.ListPentestJobsForPentestOutput.error_to_json)])
let list_pentests =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_pentests
           (Values.ListPentestsInput.make ?maxResults ?nextToken
              ~agentSpaceId ()) (Some Values.ListPentestsOutput.to_json)
           (Some Values.ListPentestsOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let list_target_domains =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_target_domains
           (Values.ListTargetDomainsInput.make ?nextToken ?maxResults ())
           (Some Values.ListTargetDomainsOutput.to_json)
           (Some Values.ListTargetDomainsOutput.error_to_json)])
let start_code_remediation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pentestJobId =
         flag "pentest-job-id" (optional string) ~doc:"STRING String"
       and codeReviewJobId =
         flag "code-review-job-id" (optional string) ~doc:"STRING String"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String"
       and findingIds =
         flag "finding-ids" (required json_arg) ~doc:"JSON FindingIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_code_remediation
           (Values.StartCodeRemediationInput.make ?pentestJobId
              ?codeReviewJobId ~agentSpaceId
              ~findingIds:(Values.FindingIdList.of_json findingIds) ())
           (Some Values.StartCodeRemediationOutput.to_json)
           (Some Values.StartCodeRemediationOutput.error_to_json)])
let start_code_review_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String"
       and codeReviewId =
         flag "code-review-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_code_review_job
           (Values.StartCodeReviewJobInput.make ~agentSpaceId ~codeReviewId
              ()) (Some Values.StartCodeReviewJobOutput.to_json)
           (Some Values.StartCodeReviewJobOutput.error_to_json)])
let start_pentest_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String"
       and pentestId =
         flag "pentest-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_pentest_job
           (Values.StartPentestJobInput.make ~agentSpaceId ~pentestId ())
           (Some Values.StartPentestJobOutput.to_json)
           (Some Values.StartPentestJobOutput.error_to_json)])
let stop_code_review_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String"
       and codeReviewJobId =
         flag "code-review-job-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_code_review_job
           (Values.StopCodeReviewJobInput.make ~agentSpaceId ~codeReviewJobId
              ()) (Some Values.StopCodeReviewJobOutput.to_json)
           (Some Values.StopCodeReviewJobOutput.error_to_json)])
let stop_pentest_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String"
       and pentestJobId =
         flag "pentest-job-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_pentest_job
           (Values.StopPentestJobInput.make ~agentSpaceId ~pentestJobId ())
           (Some Values.StopPentestJobOutput.to_json)
           (Some Values.StopPentestJobOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceOutput.to_json)
           (Some Values.TagResourceOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceOutput.to_json)
           (Some Values.UntagResourceOutput.error_to_json)])
let update_agent_space =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING AgentName"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and awsResources =
         flag "aws-resources" (optional json_arg) ~doc:"JSON AWSResources"
       and targetDomainIds =
         flag "target-domain-ids" (optional json_arg)
           ~doc:"JSON TargetDomainIdList"
       and codeReviewSettings =
         flag "code-review-settings" (optional json_arg)
           ~doc:"JSON CodeReviewSettings"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_agent_space
           (Values.UpdateAgentSpaceInput.make ?name ?description
              ?awsResources:(Option.map ~f:Values.AWSResources.of_json
                               awsResources)
              ?targetDomainIds:(Option.map
                                  ~f:Values.TargetDomainIdList.of_json
                                  targetDomainIds)
              ?codeReviewSettings:(Option.map
                                     ~f:Values.CodeReviewSettings.of_json
                                     codeReviewSettings) ~agentSpaceId ())
           (Some Values.UpdateAgentSpaceOutput.to_json)
           (Some Values.UpdateAgentSpaceOutput.error_to_json)])
let update_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and defaultKmsKeyId =
         flag "default-kms-key-id" (optional string)
           ~doc:"STRING DefaultKmsKeyId"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_application
           (Values.UpdateApplicationRequest.make ?roleArn ?defaultKmsKeyId
              ~applicationId ())
           (Some Values.UpdateApplicationResponse.to_json)
           (Some Values.UpdateApplicationResponse.error_to_json)])
let update_code_review =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and title = flag "title" (optional string) ~doc:"STRING String"
       and assets = flag "assets" (optional json_arg) ~doc:"JSON Assets"
       and serviceRole =
         flag "service-role" (optional string) ~doc:"STRING ServiceRole"
       and logConfig =
         flag "log-config" (optional json_arg) ~doc:"JSON CloudWatchLog"
       and codeRemediationStrategy =
         flag "code-remediation-strategy" (optional json_arg)
           ~doc:"JSON CodeRemediationStrategy"
       and codeReviewId =
         flag "code-review-id" (required string) ~doc:"STRING String"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_code_review
           (Values.UpdateCodeReviewInput.make ?title
              ?assets:(Option.map ~f:Values.Assets.of_json assets)
              ?serviceRole
              ?logConfig:(Option.map ~f:Values.CloudWatchLog.of_json
                            logConfig)
              ?codeRemediationStrategy:(Option.map
                                          ~f:Values.CodeRemediationStrategy.of_json
                                          codeRemediationStrategy)
              ~codeReviewId ~agentSpaceId ())
           (Some Values.UpdateCodeReviewOutput.to_json)
           (Some Values.UpdateCodeReviewOutput.error_to_json)])
let update_finding =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and riskLevel =
         flag "risk-level" (optional json_arg) ~doc:"JSON RiskLevel"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON FindingStatus"
       and findingId =
         flag "finding-id" (required string) ~doc:"STRING String"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_finding
           (Values.UpdateFindingInput.make
              ?riskLevel:(Option.map ~f:Values.RiskLevel.of_json riskLevel)
              ?status:(Option.map ~f:Values.FindingStatus.of_json status)
              ~findingId ~agentSpaceId ())
           (Some Values.UpdateFindingOutput.to_json)
           (Some Values.UpdateFindingOutput.error_to_json)])
let update_integrated_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING AgentSpaceId"
       and integrationId =
         flag "integration-id" (required string) ~doc:"STRING IntegrationId"
       and items =
         flag "items" (required json_arg)
           ~doc:"JSON IntegratedResourceInputItemList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_integrated_resources
           (Values.UpdateIntegratedResourcesInput.make ~agentSpaceId
              ~integrationId
              ~items:(Values.IntegratedResourceInputItemList.of_json items)
              ()) (Some Values.UpdateIntegratedResourcesOutput.to_json)
           (Some Values.UpdateIntegratedResourcesOutput.error_to_json)])
let update_pentest =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and title = flag "title" (optional string) ~doc:"STRING String"
       and assets = flag "assets" (optional json_arg) ~doc:"JSON Assets"
       and excludeRiskTypes =
         flag "exclude-risk-types" (optional json_arg)
           ~doc:"JSON RiskTypeList"
       and serviceRole =
         flag "service-role" (optional string) ~doc:"STRING ServiceRole"
       and logConfig =
         flag "log-config" (optional json_arg) ~doc:"JSON CloudWatchLog"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON VpcConfig"
       and networkTrafficConfig =
         flag "network-traffic-config" (optional json_arg)
           ~doc:"JSON NetworkTrafficConfig"
       and codeRemediationStrategy =
         flag "code-remediation-strategy" (optional json_arg)
           ~doc:"JSON CodeRemediationStrategy"
       and pentestId =
         flag "pentest-id" (required string) ~doc:"STRING String"
       and agentSpaceId =
         flag "agent-space-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_pentest
           (Values.UpdatePentestInput.make ?title
              ?assets:(Option.map ~f:Values.Assets.of_json assets)
              ?excludeRiskTypes:(Option.map ~f:Values.RiskTypeList.of_json
                                   excludeRiskTypes) ?serviceRole
              ?logConfig:(Option.map ~f:Values.CloudWatchLog.of_json
                            logConfig)
              ?vpcConfig:(Option.map ~f:Values.VpcConfig.of_json vpcConfig)
              ?networkTrafficConfig:(Option.map
                                       ~f:Values.NetworkTrafficConfig.of_json
                                       networkTrafficConfig)
              ?codeRemediationStrategy:(Option.map
                                          ~f:Values.CodeRemediationStrategy.of_json
                                          codeRemediationStrategy) ~pentestId
              ~agentSpaceId ()) (Some Values.UpdatePentestOutput.to_json)
           (Some Values.UpdatePentestOutput.error_to_json)])
let update_target_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetDomainId =
         flag "target-domain-id" (required string)
           ~doc:"STRING TargetDomainId"
       and verificationMethod =
         flag "verification-method" (required json_arg)
           ~doc:"JSON DomainVerificationMethod" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_target_domain
           (Values.UpdateTargetDomainInput.make ~targetDomainId
              ~verificationMethod:(Values.DomainVerificationMethod.of_json
                                     verificationMethod) ())
           (Some Values.UpdateTargetDomainOutput.to_json)
           (Some Values.UpdateTargetDomainOutput.error_to_json)])
let verify_target_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetDomainId =
         flag "target-domain-id" (required string)
           ~doc:"STRING TargetDomainId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.verify_target_domain
           (Values.VerifyTargetDomainInput.make ~targetDomainId ())
           (Some Values.VerifyTargetDomainOutput.to_json)
           (Some Values.VerifyTargetDomainOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("add-artifact", add_artifact);
    ("batch-delete-code-reviews", batch_delete_code_reviews);
    ("batch-delete-pentests", batch_delete_pentests);
    ("batch-get-agent-spaces", batch_get_agent_spaces);
    ("batch-get-artifact-metadata", batch_get_artifact_metadata);
    ("batch-get-code-review-job-tasks", batch_get_code_review_job_tasks);
    ("batch-get-code-review-jobs", batch_get_code_review_jobs);
    ("batch-get-code-reviews", batch_get_code_reviews);
    ("batch-get-findings", batch_get_findings);
    ("batch-get-pentest-job-tasks", batch_get_pentest_job_tasks);
    ("batch-get-pentest-jobs", batch_get_pentest_jobs);
    ("batch-get-pentests", batch_get_pentests);
    ("batch-get-target-domains", batch_get_target_domains);
    ("create-agent-space", create_agent_space);
    ("create-application", create_application);
    ("create-code-review", create_code_review);
    ("create-integration", create_integration);
    ("create-membership", create_membership);
    ("create-pentest", create_pentest);
    ("create-target-domain", create_target_domain);
    ("delete-agent-space", delete_agent_space);
    ("delete-application", delete_application);
    ("delete-artifact", delete_artifact);
    ("delete-integration", delete_integration);
    ("delete-membership", delete_membership);
    ("delete-target-domain", delete_target_domain);
    ("get-application", get_application);
    ("get-artifact", get_artifact);
    ("get-integration", get_integration);
    ("initiate-provider-registration", initiate_provider_registration);
    ("list-agent-spaces", list_agent_spaces);
    ("list-applications", list_applications);
    ("list-artifacts", list_artifacts);
    ("list-code-review-job-tasks", list_code_review_job_tasks);
    ("list-code-review-jobs-for-code-review",
      list_code_review_jobs_for_code_review);
    ("list-code-reviews", list_code_reviews);
    ("list-discovered-endpoints", list_discovered_endpoints);
    ("list-findings", list_findings);
    ("list-integrated-resources", list_integrated_resources);
    ("list-integrations", list_integrations);
    ("list-memberships", list_memberships);
    ("list-pentest-job-tasks", list_pentest_job_tasks);
    ("list-pentest-jobs-for-pentest", list_pentest_jobs_for_pentest);
    ("list-pentests", list_pentests);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-target-domains", list_target_domains);
    ("start-code-remediation", start_code_remediation);
    ("start-code-review-job", start_code_review_job);
    ("start-pentest-job", start_pentest_job);
    ("stop-code-review-job", stop_code_review_job);
    ("stop-pentest-job", stop_pentest_job);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-agent-space", update_agent_space);
    ("update-application", update_application);
    ("update-code-review", update_code_review);
    ("update-finding", update_finding);
    ("update-integrated-resources", update_integrated_resources);
    ("update-pentest", update_pentest);
    ("update-target-domain", update_target_domain);
    ("verify-target-domain", verify_target_domain)]
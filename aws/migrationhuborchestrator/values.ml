(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.migrationhuborchestrator
let apiVersion = "2021-08-28"
let endpointPrefix = "migrationhub-orchestrator"
let serviceFullName = "AWS Migration Hub Orchestrator"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module MaxStringValue =
  struct
    type nonrec t = string
    let context_ = "MaxStringValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MaxStringValue" j
    let to_json = simple_to_json to_value
  end
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module MaxStringList =
  struct
    type nonrec t = MaxStringValue.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:MaxStringValue.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MaxStringValue.of_xml)
    let of_json j =
      list_of_json ~kind:"MaxStringList" ~of_json:MaxStringValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module StringListMember =
  struct
    type nonrec t = string
    let context_ = "StringListMember"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:500) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StringListMember" j
    let to_json = simple_to_json to_value
  end
module StringMapKey =
  struct
    type nonrec t = string
    let context_ = "StringMapKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9-_ ()]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StringMapKey" j
    let to_json = simple_to_json to_value
  end
module StringMapValue =
  struct
    type nonrec t = string
    let context_ = "StringMapValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StringMapValue" j
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module S3Key =
  struct
    type nonrec t = string
    let context_ = "S3Key"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"S3Key" j
    let to_json = simple_to_json to_value
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module DataType =
  struct
    type nonrec t =
      | STRING 
      | INTEGER 
      | STRINGLIST 
      | STRINGMAP 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | STRING -> "STRING"
      | INTEGER -> "INTEGER"
      | STRINGLIST -> "STRINGLIST"
      | STRINGMAP -> "STRINGMAP"
      | Non_static_id s -> s
    let of_string =
      function
      | "STRING" -> STRING
      | "INTEGER" -> INTEGER
      | "STRINGLIST" -> STRINGLIST
      | "STRINGMAP" -> STRINGMAP
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration DataType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"DataType" j)
    let to_json = simple_to_json to_value
  end
module WorkflowStepOutputName =
  struct
    type nonrec t = string
    let context_ = "WorkflowStepOutputName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[-a-zA-Z0-9_.+]+[-a-zA-Z0-9_.+ ]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"WorkflowStepOutputName" j
    let to_json = simple_to_json to_value
  end
module WorkflowStepOutputUnion =
  struct
    type nonrec t =
      {
      integerValue: Integer.t option [@ocaml.doc "The integer value."];
      stringValue: MaxStringValue.t option [@ocaml.doc "The string value."];
      listOfStringValue: MaxStringList.t option
        [@ocaml.doc "The list of string value."]}
    let make ?integerValue =
      fun ?stringValue ->
        fun ?listOfStringValue ->
          fun () -> { integerValue; stringValue; listOfStringValue }
    let to_value x =
      structure_to_value
        [("integerValue", (Option.map x.integerValue ~f:Integer.to_value));
        ("stringValue",
          (Option.map x.stringValue ~f:MaxStringValue.to_value));
        ("listOfStringValue",
          (Option.map x.listOfStringValue ~f:MaxStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let listOfStringValue =
        (Option.map ~f:MaxStringList.of_xml)
          (Xml.child xml_arg0 "listOfStringValue") in
      let stringValue =
        (Option.map ~f:MaxStringValue.of_xml)
          (Xml.child xml_arg0 "stringValue") in
      let integerValue =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "integerValue") in
      make ?listOfStringValue ?stringValue ?integerValue ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let listOfStringValue =
        field_map json__ "listOfStringValue" MaxStringList.of_json in
      let stringValue = field_map json__ "stringValue" MaxStringValue.of_json in
      let integerValue = field_map json__ "integerValue" Integer.of_json in
      make ?listOfStringValue ?stringValue ?integerValue ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A structure to hold multiple values of an output."]
module StringList =
  struct
    type nonrec t = StringListMember.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:StringListMember.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:StringListMember.of_xml)
    let of_json j =
      list_of_json ~kind:"StringList" ~of_json:StringListMember.of_json j
    let to_json v = composed_to_json to_value v
  end
module StringMap =
  struct
    type nonrec t = (StringMapKey.t * StringMapValue.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((StringMapKey.of_string chopped),
                              (StringMapValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (StringMapKey.to_value x) |>
                    (fun x ->
                       (StringMapValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:StringMapKey.of_string
        ~of_json:StringMapValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module StringValue =
  struct
    type nonrec t = string
    let context_ = "StringValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StringValue" j
    let to_json = simple_to_json to_value
  end
module Owner =
  struct
    type nonrec t =
      | AWS_MANAGED 
      | CUSTOM 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | AWS_MANAGED -> "AWS_MANAGED"
      | CUSTOM -> "CUSTOM"
      | Non_static_id s -> s
    let of_string =
      function
      | "AWS_MANAGED" -> AWS_MANAGED
      | "CUSTOM" -> CUSTOM
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Owner" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Owner" j)
    let to_json = simple_to_json to_value
  end
module StepActionType =
  struct
    type nonrec t =
      | MANUAL 
      | AUTOMATED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | MANUAL -> "MANUAL"
      | AUTOMATED -> "AUTOMATED"
      | Non_static_id s -> s
    let of_string =
      function
      | "MANUAL" -> MANUAL
      | "AUTOMATED" -> AUTOMATED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration StepActionType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"StepActionType" j)
    let to_json = simple_to_json to_value
  end
module StepStatus =
  struct
    type nonrec t =
      | AWAITING_DEPENDENCIES 
      | SKIPPED 
      | READY 
      | IN_PROGRESS 
      | COMPLETED 
      | FAILED 
      | PAUSED 
      | USER_ATTENTION_REQUIRED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | AWAITING_DEPENDENCIES -> "AWAITING_DEPENDENCIES"
      | SKIPPED -> "SKIPPED"
      | READY -> "READY"
      | IN_PROGRESS -> "IN_PROGRESS"
      | COMPLETED -> "COMPLETED"
      | FAILED -> "FAILED"
      | PAUSED -> "PAUSED"
      | USER_ATTENTION_REQUIRED -> "USER_ATTENTION_REQUIRED"
      | Non_static_id s -> s
    let of_string =
      function
      | "AWAITING_DEPENDENCIES" -> AWAITING_DEPENDENCIES
      | "SKIPPED" -> SKIPPED
      | "READY" -> READY
      | "IN_PROGRESS" -> IN_PROGRESS
      | "COMPLETED" -> COMPLETED
      | "FAILED" -> FAILED
      | "PAUSED" -> PAUSED
      | "USER_ATTENTION_REQUIRED" -> USER_ATTENTION_REQUIRED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration StepStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"StepStatus" j)
    let to_json = simple_to_json to_value
  end
module StepGroupStatus =
  struct
    type nonrec t =
      | AWAITING_DEPENDENCIES 
      | READY 
      | IN_PROGRESS 
      | COMPLETED 
      | FAILED 
      | PAUSED 
      | PAUSING 
      | USER_ATTENTION_REQUIRED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | AWAITING_DEPENDENCIES -> "AWAITING_DEPENDENCIES"
      | READY -> "READY"
      | IN_PROGRESS -> "IN_PROGRESS"
      | COMPLETED -> "COMPLETED"
      | FAILED -> "FAILED"
      | PAUSED -> "PAUSED"
      | PAUSING -> "PAUSING"
      | USER_ATTENTION_REQUIRED -> "USER_ATTENTION_REQUIRED"
      | Non_static_id s -> s
    let of_string =
      function
      | "AWAITING_DEPENDENCIES" -> AWAITING_DEPENDENCIES
      | "READY" -> READY
      | "IN_PROGRESS" -> IN_PROGRESS
      | "COMPLETED" -> COMPLETED
      | "FAILED" -> FAILED
      | "PAUSED" -> PAUSED
      | "PAUSING" -> PAUSING
      | "USER_ATTENTION_REQUIRED" -> USER_ATTENTION_REQUIRED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration StepGroupStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"StepGroupStatus" j)
    let to_json = simple_to_json to_value
  end
module TargetType =
  struct
    type nonrec t =
      | SINGLE 
      | ALL 
      | NONE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SINGLE -> "SINGLE"
      | ALL -> "ALL"
      | NONE -> "NONE"
      | Non_static_id s -> s
    let of_string =
      function
      | "SINGLE" -> SINGLE
      | "ALL" -> ALL
      | "NONE" -> NONE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration TargetType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"TargetType" j)
    let to_json = simple_to_json to_value
  end
module IPAddress =
  struct
    type nonrec t = string
    let context_ = "IPAddress"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:15) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"(([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])\\.){3}([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"IPAddress" j
    let to_json = simple_to_json to_value
  end
module PluginHealth =
  struct
    type nonrec t =
      | HEALTHY 
      | UNHEALTHY 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | HEALTHY -> "HEALTHY"
      | UNHEALTHY -> "UNHEALTHY"
      | Non_static_id s -> s
    let of_string =
      function
      | "HEALTHY" -> HEALTHY
      | "UNHEALTHY" -> UNHEALTHY
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration PluginHealth" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"PluginHealth" j)
    let to_json = simple_to_json to_value
  end
module PluginId =
  struct
    type nonrec t = string
    let context_ = "PluginId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:60) >>=
                  (fun () -> check_pattern i ~pattern:".*\\S.*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PluginId" j
    let to_json = simple_to_json to_value
  end
module PluginVersion =
  struct
    type nonrec t = string
    let context_ = "PluginVersion"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () -> check_pattern i ~pattern:".*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PluginVersion" j
    let to_json = simple_to_json to_value
  end
module MigrationWorkflowId =
  struct
    type nonrec t = string
    let context_ = "MigrationWorkflowId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MigrationWorkflowId" j
    let to_json = simple_to_json to_value
  end
module MigrationWorkflowStatusEnum =
  struct
    type nonrec t =
      | CREATING 
      | NOT_STARTED 
      | CREATION_FAILED 
      | STARTING 
      | IN_PROGRESS 
      | WORKFLOW_FAILED 
      | PAUSED 
      | PAUSING 
      | PAUSING_FAILED 
      | USER_ATTENTION_REQUIRED 
      | DELETING 
      | DELETION_FAILED 
      | DELETED 
      | COMPLETED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CREATING -> "CREATING"
      | NOT_STARTED -> "NOT_STARTED"
      | CREATION_FAILED -> "CREATION_FAILED"
      | STARTING -> "STARTING"
      | IN_PROGRESS -> "IN_PROGRESS"
      | WORKFLOW_FAILED -> "WORKFLOW_FAILED"
      | PAUSED -> "PAUSED"
      | PAUSING -> "PAUSING"
      | PAUSING_FAILED -> "PAUSING_FAILED"
      | USER_ATTENTION_REQUIRED -> "USER_ATTENTION_REQUIRED"
      | DELETING -> "DELETING"
      | DELETION_FAILED -> "DELETION_FAILED"
      | DELETED -> "DELETED"
      | COMPLETED -> "COMPLETED"
      | Non_static_id s -> s
    let of_string =
      function
      | "CREATING" -> CREATING
      | "NOT_STARTED" -> NOT_STARTED
      | "CREATION_FAILED" -> CREATION_FAILED
      | "STARTING" -> STARTING
      | "IN_PROGRESS" -> IN_PROGRESS
      | "WORKFLOW_FAILED" -> WORKFLOW_FAILED
      | "PAUSED" -> PAUSED
      | "PAUSING" -> PAUSING
      | "PAUSING_FAILED" -> PAUSING_FAILED
      | "USER_ATTENTION_REQUIRED" -> USER_ATTENTION_REQUIRED
      | "DELETING" -> DELETING
      | "DELETION_FAILED" -> DELETION_FAILED
      | "DELETED" -> DELETED
      | "COMPLETED" -> COMPLETED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration MigrationWorkflowStatusEnum"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"MigrationWorkflowStatusEnum" j)
    let to_json = simple_to_json to_value
  end
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module TemplateInputName =
  struct
    type nonrec t = string
    let context_ = "TemplateInputName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[-a-zA-Z0-9_.+]+[-a-zA-Z0-9_.+ ]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TemplateInputName" j
    let to_json = simple_to_json to_value
  end
module PlatformCommand =
  struct
    type nonrec t =
      {
      linux: String_.t option [@ocaml.doc "Command for Linux."];
      windows: String_.t option [@ocaml.doc "Command for Windows."]}
    let make ?linux = fun ?windows -> fun () -> { linux; windows }
    let to_value x =
      structure_to_value
        [("linux", (Option.map x.linux ~f:String_.to_value));
        ("windows", (Option.map x.windows ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let windows =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "windows") in
      let linux = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "linux") in
      make ?windows ?linux ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let windows = field_map json__ "windows" String_.of_json in
      let linux = field_map json__ "linux" String_.of_json in
      make ?windows ?linux ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Command to be run on a particular operating system."]
module PlatformScriptKey =
  struct
    type nonrec t =
      {
      linux: S3Key.t option [@ocaml.doc "The script location for Linux."];
      windows: S3Key.t option [@ocaml.doc "The script location for Windows."]}
    let make ?linux = fun ?windows -> fun () -> { linux; windows }
    let to_value x =
      structure_to_value
        [("linux", (Option.map x.linux ~f:S3Key.to_value));
        ("windows", (Option.map x.windows ~f:S3Key.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let windows =
        (Option.map ~f:S3Key.of_xml) (Xml.child xml_arg0 "windows") in
      let linux = (Option.map ~f:S3Key.of_xml) (Xml.child xml_arg0 "linux") in
      make ?windows ?linux ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let windows = field_map json__ "windows" S3Key.of_json in
      let linux = field_map json__ "linux" S3Key.of_json in
      make ?windows ?linux ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The script location for a particular operating system."]
module RunEnvironment =
  struct
    type nonrec t =
      | AWS 
      | ONPREMISE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | AWS -> "AWS"
      | ONPREMISE -> "ONPREMISE"
      | Non_static_id s -> s
    let of_string =
      function
      | "AWS" -> AWS
      | "ONPREMISE" -> ONPREMISE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration RunEnvironment" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"RunEnvironment" j)
    let to_json = simple_to_json to_value
  end
module S3Bucket =
  struct
    type nonrec t = string
    let context_ = "S3Bucket"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:63) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[0-9a-z]+[0-9a-z\\.\\-]*[0-9a-z]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"S3Bucket" j
    let to_json = simple_to_json to_value
  end
module WorkflowStepOutput =
  struct
    type nonrec t =
      {
      name: WorkflowStepOutputName.t option
        [@ocaml.doc "The name of the step."];
      dataType: DataType.t option [@ocaml.doc "The data type of the output."];
      required: Boolean.t option
        [@ocaml.doc "Determine if an output is required from a step."];
      value: WorkflowStepOutputUnion.t option
        [@ocaml.doc "The value of the output."]}
    let make ?name =
      fun ?dataType ->
        fun ?required ->
          fun ?value -> fun () -> { name; dataType; required; value }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:WorkflowStepOutputName.to_value));
        ("dataType", (Option.map x.dataType ~f:DataType.to_value));
        ("required", (Option.map x.required ~f:Boolean.to_value));
        ("value", (Option.map x.value ~f:WorkflowStepOutputUnion.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:WorkflowStepOutputUnion.of_xml)
          (Xml.child xml_arg0 "value") in
      let required =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "required") in
      let dataType =
        (Option.map ~f:DataType.of_xml) (Xml.child xml_arg0 "dataType") in
      let name =
        (Option.map ~f:WorkflowStepOutputName.of_xml)
          (Xml.child xml_arg0 "name") in
      make ?value ?required ?dataType ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let value = field_map json__ "value" WorkflowStepOutputUnion.of_json in
      let required = field_map json__ "required" Boolean.of_json in
      let dataType = field_map json__ "dataType" DataType.of_json in
      let name = field_map json__ "name" WorkflowStepOutputName.of_json in
      make ?value ?required ?dataType ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The output of a step."]
module Tool =
  struct
    type nonrec t =
      {
      name: String_.t option [@ocaml.doc "The name of an AWS service."];
      url: String_.t option [@ocaml.doc "The URL of an AWS service."]}
    let make ?name = fun ?url -> fun () -> { name; url }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:String_.to_value));
        ("url", (Option.map x.url ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let url = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "url") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      make ?url ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let url = field_map json__ "url" String_.of_json in
      let name = field_map json__ "name" String_.of_json in
      make ?url ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "List of AWS services utilized in a migration workflow."]
module StepInput =
  struct
    type nonrec t =
      {
      integerValue: Integer.t option [@ocaml.doc "The value of the integer."];
      stringValue: StringValue.t option [@ocaml.doc "String value."];
      listOfStringsValue: StringList.t option
        [@ocaml.doc "List of string values."];
      mapOfStringValue: StringMap.t option
        [@ocaml.doc "Map of string values."]}
    let make ?integerValue =
      fun ?stringValue ->
        fun ?listOfStringsValue ->
          fun ?mapOfStringValue ->
            fun () ->
              {
                integerValue;
                stringValue;
                listOfStringsValue;
                mapOfStringValue
              }
    let to_value x =
      structure_to_value
        [("integerValue", (Option.map x.integerValue ~f:Integer.to_value));
        ("stringValue", (Option.map x.stringValue ~f:StringValue.to_value));
        ("listOfStringsValue",
          (Option.map x.listOfStringsValue ~f:StringList.to_value));
        ("mapOfStringValue",
          (Option.map x.mapOfStringValue ~f:StringMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let mapOfStringValue =
        (Option.map ~f:StringMap.of_xml)
          (Xml.child xml_arg0 "mapOfStringValue") in
      let listOfStringsValue =
        (Option.map ~f:StringList.of_xml)
          (Xml.child xml_arg0 "listOfStringsValue") in
      let stringValue =
        (Option.map ~f:StringValue.of_xml) (Xml.child xml_arg0 "stringValue") in
      let integerValue =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "integerValue") in
      make ?mapOfStringValue ?listOfStringsValue ?stringValue ?integerValue
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let mapOfStringValue =
        field_map json__ "mapOfStringValue" StringMap.of_json in
      let listOfStringsValue =
        field_map json__ "listOfStringsValue" StringList.of_json in
      let stringValue = field_map json__ "stringValue" StringValue.of_json in
      let integerValue = field_map json__ "integerValue" Integer.of_json in
      make ?mapOfStringValue ?listOfStringsValue ?stringValue ?integerValue
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A map of key value pairs that is generated when you create a migration workflow. The key value pairs will differ based on your selection of the template."]
module StepInputParametersKey =
  struct
    type nonrec t = string
    let context_ = "StepInputParametersKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9-_ ()]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StepInputParametersKey" j
    let to_json = simple_to_json to_value
  end
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i ~pattern:"(?!aws:)[a-zA-Z+-=._:/]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module WorkflowStepSummary =
  struct
    type nonrec t =
      {
      stepId: String_.t option [@ocaml.doc "The ID of the step."];
      name: String_.t option [@ocaml.doc "The name of the step."];
      stepActionType: StepActionType.t option
        [@ocaml.doc
          "The action type of the step. You must run and update the status of a manual step for the workflow to continue after the completion of the step."];
      owner: Owner.t option [@ocaml.doc "The owner of the step."];
      previous: StringList.t option [@ocaml.doc "The previous step."];
      next: StringList.t option [@ocaml.doc "The next step."];
      status: StepStatus.t option [@ocaml.doc "The status of the step."];
      statusMessage: String_.t option
        [@ocaml.doc "The status message of the migration workflow."];
      noOfSrvCompleted: Integer.t option
        [@ocaml.doc "The number of servers that have been migrated."];
      noOfSrvFailed: Integer.t option
        [@ocaml.doc "The number of servers that have failed to migrate."];
      totalNoOfSrv: Integer.t option
        [@ocaml.doc "The total number of servers that have been migrated."];
      description: String_.t option
        [@ocaml.doc "The description of the step."];
      scriptLocation: String_.t option
        [@ocaml.doc "The location of the script."]}
    let make ?stepId =
      fun ?name ->
        fun ?stepActionType ->
          fun ?owner ->
            fun ?previous ->
              fun ?next ->
                fun ?status ->
                  fun ?statusMessage ->
                    fun ?noOfSrvCompleted ->
                      fun ?noOfSrvFailed ->
                        fun ?totalNoOfSrv ->
                          fun ?description ->
                            fun ?scriptLocation ->
                              fun () ->
                                {
                                  stepId;
                                  name;
                                  stepActionType;
                                  owner;
                                  previous;
                                  next;
                                  status;
                                  statusMessage;
                                  noOfSrvCompleted;
                                  noOfSrvFailed;
                                  totalNoOfSrv;
                                  description;
                                  scriptLocation
                                }
    let to_value x =
      structure_to_value
        [("stepId", (Option.map x.stepId ~f:String_.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("stepActionType",
          (Option.map x.stepActionType ~f:StepActionType.to_value));
        ("owner", (Option.map x.owner ~f:Owner.to_value));
        ("previous", (Option.map x.previous ~f:StringList.to_value));
        ("next", (Option.map x.next ~f:StringList.to_value));
        ("status", (Option.map x.status ~f:StepStatus.to_value));
        ("statusMessage", (Option.map x.statusMessage ~f:String_.to_value));
        ("noOfSrvCompleted",
          (Option.map x.noOfSrvCompleted ~f:Integer.to_value));
        ("noOfSrvFailed", (Option.map x.noOfSrvFailed ~f:Integer.to_value));
        ("totalNoOfSrv", (Option.map x.totalNoOfSrv ~f:Integer.to_value));
        ("description", (Option.map x.description ~f:String_.to_value));
        ("scriptLocation", (Option.map x.scriptLocation ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scriptLocation =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "scriptLocation") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      let totalNoOfSrv =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "totalNoOfSrv") in
      let noOfSrvFailed =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "noOfSrvFailed") in
      let noOfSrvCompleted =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "noOfSrvCompleted") in
      let statusMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "statusMessage") in
      let status =
        (Option.map ~f:StepStatus.of_xml) (Xml.child xml_arg0 "status") in
      let next =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "next") in
      let previous =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "previous") in
      let owner = (Option.map ~f:Owner.of_xml) (Xml.child xml_arg0 "owner") in
      let stepActionType =
        (Option.map ~f:StepActionType.of_xml)
          (Xml.child xml_arg0 "stepActionType") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let stepId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "stepId") in
      make ?scriptLocation ?description ?totalNoOfSrv ?noOfSrvFailed
        ?noOfSrvCompleted ?statusMessage ?status ?next ?previous ?owner
        ?stepActionType ?name ?stepId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let scriptLocation = field_map json__ "scriptLocation" String_.of_json in
      let description = field_map json__ "description" String_.of_json in
      let totalNoOfSrv = field_map json__ "totalNoOfSrv" Integer.of_json in
      let noOfSrvFailed = field_map json__ "noOfSrvFailed" Integer.of_json in
      let noOfSrvCompleted =
        field_map json__ "noOfSrvCompleted" Integer.of_json in
      let statusMessage = field_map json__ "statusMessage" String_.of_json in
      let status = field_map json__ "status" StepStatus.of_json in
      let next = field_map json__ "next" StringList.of_json in
      let previous = field_map json__ "previous" StringList.of_json in
      let owner = field_map json__ "owner" Owner.of_json in
      let stepActionType =
        field_map json__ "stepActionType" StepActionType.of_json in
      let name = field_map json__ "name" String_.of_json in
      let stepId = field_map json__ "stepId" String_.of_json in
      make ?scriptLocation ?description ?totalNoOfSrv ?noOfSrvFailed
        ?noOfSrvCompleted ?statusMessage ?status ?next ?previous ?owner
        ?stepActionType ?name ?stepId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The summary of the step in a migration workflow."]
module WorkflowStepGroupSummary =
  struct
    type nonrec t =
      {
      id: String_.t option [@ocaml.doc "The ID of the step group."];
      name: String_.t option [@ocaml.doc "The name of the step group."];
      owner: Owner.t option [@ocaml.doc "The owner of the step group."];
      status: StepGroupStatus.t option
        [@ocaml.doc "The status of the step group."];
      previous: StringList.t option [@ocaml.doc "The previous step group."];
      next: StringList.t option [@ocaml.doc "The next step group."]}
    let make ?id =
      fun ?name ->
        fun ?owner ->
          fun ?status ->
            fun ?previous ->
              fun ?next ->
                fun () -> { id; name; owner; status; previous; next }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:String_.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("owner", (Option.map x.owner ~f:Owner.to_value));
        ("status", (Option.map x.status ~f:StepGroupStatus.to_value));
        ("previous", (Option.map x.previous ~f:StringList.to_value));
        ("next", (Option.map x.next ~f:StringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let next =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "next") in
      let previous =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "previous") in
      let status =
        (Option.map ~f:StepGroupStatus.of_xml) (Xml.child xml_arg0 "status") in
      let owner = (Option.map ~f:Owner.of_xml) (Xml.child xml_arg0 "owner") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let id = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "id") in
      make ?next ?previous ?status ?owner ?name ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let next = field_map json__ "next" StringList.of_json in
      let previous = field_map json__ "previous" StringList.of_json in
      let status = field_map json__ "status" StepGroupStatus.of_json in
      let owner = field_map json__ "owner" Owner.of_json in
      let name = field_map json__ "name" String_.of_json in
      let id = field_map json__ "id" String_.of_json in
      make ?next ?previous ?status ?owner ?name ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The summary of a step group in a workflow."]
module TemplateStepSummary =
  struct
    type nonrec t =
      {
      id: String_.t option [@ocaml.doc "The ID of the step."];
      stepGroupId: String_.t option [@ocaml.doc "The ID of the step group."];
      templateId: String_.t option [@ocaml.doc "The ID of the template."];
      name: String_.t option [@ocaml.doc "The name of the step."];
      stepActionType: StepActionType.t option
        [@ocaml.doc
          "The action type of the step. You must run and update the status of a manual step for the workflow to continue after the completion of the step."];
      targetType: TargetType.t option
        [@ocaml.doc "The servers on which to run the script."];
      owner: Owner.t option [@ocaml.doc "The owner of the step."];
      previous: StringList.t option [@ocaml.doc "The previous step."];
      next: StringList.t option [@ocaml.doc "The next step."]}
    let make ?id =
      fun ?stepGroupId ->
        fun ?templateId ->
          fun ?name ->
            fun ?stepActionType ->
              fun ?targetType ->
                fun ?owner ->
                  fun ?previous ->
                    fun ?next ->
                      fun () ->
                        {
                          id;
                          stepGroupId;
                          templateId;
                          name;
                          stepActionType;
                          targetType;
                          owner;
                          previous;
                          next
                        }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:String_.to_value));
        ("stepGroupId", (Option.map x.stepGroupId ~f:String_.to_value));
        ("templateId", (Option.map x.templateId ~f:String_.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("stepActionType",
          (Option.map x.stepActionType ~f:StepActionType.to_value));
        ("targetType", (Option.map x.targetType ~f:TargetType.to_value));
        ("owner", (Option.map x.owner ~f:Owner.to_value));
        ("previous", (Option.map x.previous ~f:StringList.to_value));
        ("next", (Option.map x.next ~f:StringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let next =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "next") in
      let previous =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "previous") in
      let owner = (Option.map ~f:Owner.of_xml) (Xml.child xml_arg0 "owner") in
      let targetType =
        (Option.map ~f:TargetType.of_xml) (Xml.child xml_arg0 "targetType") in
      let stepActionType =
        (Option.map ~f:StepActionType.of_xml)
          (Xml.child xml_arg0 "stepActionType") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let templateId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "templateId") in
      let stepGroupId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "stepGroupId") in
      let id = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "id") in
      make ?next ?previous ?owner ?targetType ?stepActionType ?name
        ?templateId ?stepGroupId ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let next = field_map json__ "next" StringList.of_json in
      let previous = field_map json__ "previous" StringList.of_json in
      let owner = field_map json__ "owner" Owner.of_json in
      let targetType = field_map json__ "targetType" TargetType.of_json in
      let stepActionType =
        field_map json__ "stepActionType" StepActionType.of_json in
      let name = field_map json__ "name" String_.of_json in
      let templateId = field_map json__ "templateId" String_.of_json in
      let stepGroupId = field_map json__ "stepGroupId" String_.of_json in
      let id = field_map json__ "id" String_.of_json in
      make ?next ?previous ?owner ?targetType ?stepActionType ?name
        ?templateId ?stepGroupId ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The summary of the step."]
module TemplateStepGroupSummary =
  struct
    type nonrec t =
      {
      id: String_.t option [@ocaml.doc "The ID of the step group."];
      name: String_.t option [@ocaml.doc "The name of the step group."];
      previous: StringList.t option [@ocaml.doc "The previous step group."];
      next: StringList.t option [@ocaml.doc "The next step group."]}
    let make ?id =
      fun ?name ->
        fun ?previous -> fun ?next -> fun () -> { id; name; previous; next }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:String_.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("previous", (Option.map x.previous ~f:StringList.to_value));
        ("next", (Option.map x.next ~f:StringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let next =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "next") in
      let previous =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "previous") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let id = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "id") in
      make ?next ?previous ?name ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let next = field_map json__ "next" StringList.of_json in
      let previous = field_map json__ "previous" StringList.of_json in
      let name = field_map json__ "name" String_.of_json in
      let id = field_map json__ "id" String_.of_json in
      make ?next ?previous ?name ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The summary of the step group in the template."]
module PluginSummary =
  struct
    type nonrec t =
      {
      pluginId: PluginId.t option [@ocaml.doc "The ID of the plugin."];
      hostname: String_.t option [@ocaml.doc "The name of the host."];
      status: PluginHealth.t option [@ocaml.doc "The status of the plugin."];
      ipAddress: IPAddress.t option
        [@ocaml.doc "The IP address at which the plugin is located."];
      version: PluginVersion.t option
        [@ocaml.doc "The version of the plugin."];
      registeredTime: String_.t option
        [@ocaml.doc "The time at which the plugin was registered."]}
    let make ?pluginId =
      fun ?hostname ->
        fun ?status ->
          fun ?ipAddress ->
            fun ?version ->
              fun ?registeredTime ->
                fun () ->
                  {
                    pluginId;
                    hostname;
                    status;
                    ipAddress;
                    version;
                    registeredTime
                  }
    let to_value x =
      structure_to_value
        [("pluginId", (Option.map x.pluginId ~f:PluginId.to_value));
        ("hostname", (Option.map x.hostname ~f:String_.to_value));
        ("status", (Option.map x.status ~f:PluginHealth.to_value));
        ("ipAddress", (Option.map x.ipAddress ~f:IPAddress.to_value));
        ("version", (Option.map x.version ~f:PluginVersion.to_value));
        ("registeredTime", (Option.map x.registeredTime ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let registeredTime =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "registeredTime") in
      let version =
        (Option.map ~f:PluginVersion.of_xml) (Xml.child xml_arg0 "version") in
      let ipAddress =
        (Option.map ~f:IPAddress.of_xml) (Xml.child xml_arg0 "ipAddress") in
      let status =
        (Option.map ~f:PluginHealth.of_xml) (Xml.child xml_arg0 "status") in
      let hostname =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "hostname") in
      let pluginId =
        (Option.map ~f:PluginId.of_xml) (Xml.child xml_arg0 "pluginId") in
      make ?registeredTime ?version ?ipAddress ?status ?hostname ?pluginId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let registeredTime = field_map json__ "registeredTime" String_.of_json in
      let version = field_map json__ "version" PluginVersion.of_json in
      let ipAddress = field_map json__ "ipAddress" IPAddress.of_json in
      let status = field_map json__ "status" PluginHealth.of_json in
      let hostname = field_map json__ "hostname" String_.of_json in
      let pluginId = field_map json__ "pluginId" PluginId.of_json in
      make ?registeredTime ?version ?ipAddress ?status ?hostname ?pluginId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The summary of the Migration Hub Orchestrator plugin."]
module MigrationWorkflowSummary =
  struct
    type nonrec t =
      {
      id: MigrationWorkflowId.t option
        [@ocaml.doc "The ID of the migration workflow."];
      name: String_.t option
        [@ocaml.doc "The name of the migration workflow."];
      templateId: String_.t option [@ocaml.doc "The ID of the template."];
      adsApplicationConfigurationName: String_.t option
        [@ocaml.doc
          "The name of the application configured in Application Discovery Service."];
      status: MigrationWorkflowStatusEnum.t option
        [@ocaml.doc "The status of the migration workflow."];
      creationTime: Timestamp.t option
        [@ocaml.doc "The time at which the migration workflow was created."];
      endTime: Timestamp.t option
        [@ocaml.doc "The time at which the migration workflow ended."];
      statusMessage: String_.t option
        [@ocaml.doc "The status message of the migration workflow."];
      completedSteps: Integer.t option
        [@ocaml.doc "The steps completed in the migration workflow."];
      totalSteps: Integer.t option
        [@ocaml.doc "All the steps in a migration workflow."]}
    let make ?id =
      fun ?name ->
        fun ?templateId ->
          fun ?adsApplicationConfigurationName ->
            fun ?status ->
              fun ?creationTime ->
                fun ?endTime ->
                  fun ?statusMessage ->
                    fun ?completedSteps ->
                      fun ?totalSteps ->
                        fun () ->
                          {
                            id;
                            name;
                            templateId;
                            adsApplicationConfigurationName;
                            status;
                            creationTime;
                            endTime;
                            statusMessage;
                            completedSteps;
                            totalSteps
                          }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:MigrationWorkflowId.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("templateId", (Option.map x.templateId ~f:String_.to_value));
        ("adsApplicationConfigurationName",
          (Option.map x.adsApplicationConfigurationName ~f:String_.to_value));
        ("status",
          (Option.map x.status ~f:MigrationWorkflowStatusEnum.to_value));
        ("creationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("endTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("statusMessage", (Option.map x.statusMessage ~f:String_.to_value));
        ("completedSteps", (Option.map x.completedSteps ~f:Integer.to_value));
        ("totalSteps", (Option.map x.totalSteps ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let totalSteps =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "totalSteps") in
      let completedSteps =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "completedSteps") in
      let statusMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "statusMessage") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "endTime") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "creationTime") in
      let status =
        (Option.map ~f:MigrationWorkflowStatusEnum.of_xml)
          (Xml.child xml_arg0 "status") in
      let adsApplicationConfigurationName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "adsApplicationConfigurationName") in
      let templateId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "templateId") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let id =
        (Option.map ~f:MigrationWorkflowId.of_xml) (Xml.child xml_arg0 "id") in
      make ?totalSteps ?completedSteps ?statusMessage ?endTime ?creationTime
        ?status ?adsApplicationConfigurationName ?templateId ?name ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let totalSteps = field_map json__ "totalSteps" Integer.of_json in
      let completedSteps = field_map json__ "completedSteps" Integer.of_json in
      let statusMessage = field_map json__ "statusMessage" String_.of_json in
      let endTime = field_map json__ "endTime" Timestamp.of_json in
      let creationTime = field_map json__ "creationTime" Timestamp.of_json in
      let status =
        field_map json__ "status" MigrationWorkflowStatusEnum.of_json in
      let adsApplicationConfigurationName =
        field_map json__ "adsApplicationConfigurationName" String_.of_json in
      let templateId = field_map json__ "templateId" String_.of_json in
      let name = field_map json__ "name" String_.of_json in
      let id = field_map json__ "id" MigrationWorkflowId.of_json in
      make ?totalSteps ?completedSteps ?statusMessage ?endTime ?creationTime
        ?status ?adsApplicationConfigurationName ?templateId ?name ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The summary of a migration workflow."]
module TemplateSummary =
  struct
    type nonrec t =
      {
      id: String_.t option [@ocaml.doc "The ID of the template."];
      name: String_.t option [@ocaml.doc "The name of the template."];
      arn: String_.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the template."];
      description: String_.t option
        [@ocaml.doc "The description of the template."]}
    let make ?id =
      fun ?name ->
        fun ?arn ->
          fun ?description -> fun () -> { id; name; arn; description }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:String_.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("arn", (Option.map x.arn ~f:String_.to_value));
        ("description", (Option.map x.description ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "arn") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let id = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "id") in
      make ?description ?arn ?name ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let description = field_map json__ "description" String_.of_json in
      let arn = field_map json__ "arn" String_.of_json in
      let name = field_map json__ "name" String_.of_json in
      let id = field_map json__ "id" String_.of_json in
      make ?description ?arn ?name ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The summary of the template."]
module StepOutput =
  struct
    type nonrec t =
      {
      name: String_.t option [@ocaml.doc "The name of the step."];
      dataType: DataType.t option
        [@ocaml.doc "The data type of the step output."];
      required: Boolean.t option
        [@ocaml.doc "Determine if an output is required from a step."]}
    let make ?name =
      fun ?dataType ->
        fun ?required -> fun () -> { name; dataType; required }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:String_.to_value));
        ("dataType", (Option.map x.dataType ~f:DataType.to_value));
        ("required", (Option.map x.required ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let required =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "required") in
      let dataType =
        (Option.map ~f:DataType.of_xml) (Xml.child xml_arg0 "dataType") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      make ?required ?dataType ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let required = field_map json__ "required" Boolean.of_json in
      let dataType = field_map json__ "dataType" DataType.of_json in
      let name = field_map json__ "name" String_.of_json in
      make ?required ?dataType ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The output of the step."]
module TemplateInput =
  struct
    type nonrec t =
      {
      inputName: TemplateInputName.t option
        [@ocaml.doc "The name of the template."];
      dataType: DataType.t option
        [@ocaml.doc "The data type of the template input."];
      required: Boolean.t option
        [@ocaml.doc "Determine if an input is required from the template."]}
    let make ?inputName =
      fun ?dataType ->
        fun ?required -> fun () -> { inputName; dataType; required }
    let to_value x =
      structure_to_value
        [("inputName",
           (Option.map x.inputName ~f:TemplateInputName.to_value));
        ("dataType", (Option.map x.dataType ~f:DataType.to_value));
        ("required", (Option.map x.required ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let required =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "required") in
      let dataType =
        (Option.map ~f:DataType.of_xml) (Xml.child xml_arg0 "dataType") in
      let inputName =
        (Option.map ~f:TemplateInputName.of_xml)
          (Xml.child xml_arg0 "inputName") in
      make ?required ?dataType ?inputName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let required = field_map json__ "required" Boolean.of_json in
      let dataType = field_map json__ "dataType" DataType.of_json in
      let inputName = field_map json__ "inputName" TemplateInputName.of_json in
      make ?required ?dataType ?inputName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The input parameters of a template."]
module AccessDeniedException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You do not have sufficient access to perform this action."]
module InternalServerException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An internal error has occurred."]
module StepId =
  struct
    type nonrec t = string
    let context_ = "StepId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StepId" j
    let to_json = simple_to_json to_value
  end
module ThrottlingException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The request was denied due to request throttling."]
module ValidationException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The input fails to satisfy the constraints specified by an AWS service."]
module StepDescription =
  struct
    type nonrec t = string
    let context_ = "StepDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:500) >>=
                  (fun () -> check_pattern i ~pattern:"[-a-zA-Z0-9_.+, ]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StepDescription" j
    let to_json = simple_to_json to_value
  end
module StepGroupId =
  struct
    type nonrec t = string
    let context_ = "StepGroupId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StepGroupId" j
    let to_json = simple_to_json to_value
  end
module StepName =
  struct
    type nonrec t = string
    let context_ = "StepName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[-a-zA-Z0-9_.+]+[-a-zA-Z0-9_.+ ]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StepName" j
    let to_json = simple_to_json to_value
  end
module WorkflowStepAutomationConfiguration =
  struct
    type nonrec t =
      {
      scriptLocationS3Bucket: S3Bucket.t option
        [@ocaml.doc "The Amazon S3 bucket where the script is located."];
      scriptLocationS3Key: PlatformScriptKey.t option
        [@ocaml.doc "The Amazon S3 key for the script location."];
      command: PlatformCommand.t option
        [@ocaml.doc "The command required to run the script."];
      runEnvironment: RunEnvironment.t option
        [@ocaml.doc "The source or target environment."];
      targetType: TargetType.t option
        [@ocaml.doc "The servers on which to run the script."]}
    let make ?scriptLocationS3Bucket =
      fun ?scriptLocationS3Key ->
        fun ?command ->
          fun ?runEnvironment ->
            fun ?targetType ->
              fun () ->
                {
                  scriptLocationS3Bucket;
                  scriptLocationS3Key;
                  command;
                  runEnvironment;
                  targetType
                }
    let to_value x =
      structure_to_value
        [("scriptLocationS3Bucket",
           (Option.map x.scriptLocationS3Bucket ~f:S3Bucket.to_value));
        ("scriptLocationS3Key",
          (Option.map x.scriptLocationS3Key ~f:PlatformScriptKey.to_value));
        ("command", (Option.map x.command ~f:PlatformCommand.to_value));
        ("runEnvironment",
          (Option.map x.runEnvironment ~f:RunEnvironment.to_value));
        ("targetType", (Option.map x.targetType ~f:TargetType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetType =
        (Option.map ~f:TargetType.of_xml) (Xml.child xml_arg0 "targetType") in
      let runEnvironment =
        (Option.map ~f:RunEnvironment.of_xml)
          (Xml.child xml_arg0 "runEnvironment") in
      let command =
        (Option.map ~f:PlatformCommand.of_xml) (Xml.child xml_arg0 "command") in
      let scriptLocationS3Key =
        (Option.map ~f:PlatformScriptKey.of_xml)
          (Xml.child xml_arg0 "scriptLocationS3Key") in
      let scriptLocationS3Bucket =
        (Option.map ~f:S3Bucket.of_xml)
          (Xml.child xml_arg0 "scriptLocationS3Bucket") in
      make ?targetType ?runEnvironment ?command ?scriptLocationS3Key
        ?scriptLocationS3Bucket ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let targetType = field_map json__ "targetType" TargetType.of_json in
      let runEnvironment =
        field_map json__ "runEnvironment" RunEnvironment.of_json in
      let command = field_map json__ "command" PlatformCommand.of_json in
      let scriptLocationS3Key =
        field_map json__ "scriptLocationS3Key" PlatformScriptKey.of_json in
      let scriptLocationS3Bucket =
        field_map json__ "scriptLocationS3Bucket" S3Bucket.of_json in
      make ?targetType ?runEnvironment ?command ?scriptLocationS3Key
        ?scriptLocationS3Bucket ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The custom script to run tests on source or target environments."]
module WorkflowStepOutputList =
  struct
    type nonrec t = WorkflowStepOutput.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:WorkflowStepOutput.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:WorkflowStepOutput.of_xml)
    let of_json j =
      list_of_json ~kind:"WorkflowStepOutputList"
        ~of_json:WorkflowStepOutput.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceNotFoundException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The resource is not available."]
module ToolsList =
  struct
    type nonrec t = Tool.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Tool.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tool.of_xml)
    let of_json j = list_of_json ~kind:"ToolsList" ~of_json:Tool.of_json j
    let to_json v = composed_to_json to_value v
  end
module StepGroupDescription =
  struct
    type nonrec t = string
    let context_ = "StepGroupDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:500) >>=
                  (fun () -> check_pattern i ~pattern:"[-a-zA-Z0-9_.+, ]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StepGroupDescription" j
    let to_json = simple_to_json to_value
  end
module StepGroupName =
  struct
    type nonrec t = string
    let context_ = "StepGroupName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[-a-zA-Z0-9_.+]+[-a-zA-Z0-9_.+ ]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StepGroupName" j
    let to_json = simple_to_json to_value
  end
module ClientToken =
  struct
    type nonrec t = string
    let context_ = "ClientToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:"[-a-zA-Z0-9]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ClientToken" j
    let to_json = simple_to_json to_value
  end
module TemplateId =
  struct
    type nonrec t = string
    let context_ = "TemplateId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[-a-zA-Z0-9_.+]+[-a-zA-Z0-9_.+ ]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TemplateId" j
    let to_json = simple_to_json to_value
  end
module UpdateTemplateRequestTemplateDescriptionString =
  struct
    type nonrec t = string
    let context_ = "UpdateTemplateRequestTemplateDescriptionString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:250) >>=
                  (fun () -> check_pattern i ~pattern:".*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"UpdateTemplateRequestTemplateDescriptionString" j
    let to_json = simple_to_json to_value
  end
module UpdateTemplateRequestTemplateNameString =
  struct
    type nonrec t = string
    let context_ = "UpdateTemplateRequestTemplateNameString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () -> check_pattern i ~pattern:"[ a-zA-Z0-9]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"UpdateTemplateRequestTemplateNameString" j
    let to_json = simple_to_json to_value
  end
module StepInputParameters =
  struct
    type nonrec t = (StepInputParametersKey.t * StepInput.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            let (_ : string) = v in
                            let (_ : string) = chopped in
                            failwith
                              "no of_header for complex types StepInputParametersKey StepInput"))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (StepInputParametersKey.to_value x) |>
                    (fun x -> (StepInput.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:StepInputParametersKey.of_string
        ~of_json:StepInput.of_json j
    let to_json v = composed_to_json to_value v
  end
module UpdateMigrationWorkflowRequestDescriptionString =
  struct
    type nonrec t = string
    let context_ = "UpdateMigrationWorkflowRequestDescriptionString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:500) >>=
                  (fun () -> check_pattern i ~pattern:"[-a-zA-Z0-9_.+, ]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"UpdateMigrationWorkflowRequestDescriptionString"
        j
    let to_json = simple_to_json to_value
  end
module UpdateMigrationWorkflowRequestNameString =
  struct
    type nonrec t = string
    let context_ = "UpdateMigrationWorkflowRequestNameString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[-a-zA-Z0-9_.+]+[-a-zA-Z0-9_.+ ]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"UpdateMigrationWorkflowRequestNameString" j
    let to_json = simple_to_json to_value
  end
module ResourceArn =
  struct
    type nonrec t = string
    let context_ = "ResourceArn"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i
             ~pattern:"arn:aws:migrationhub-orchestrator:[a-z0-9-]+:[0-9]+:(template|workflow)/[.]*");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceArn" j
    let to_json = simple_to_json to_value
  end
module TagKeyList =
  struct
    type nonrec t = TagKey.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeyList" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module TagMap =
  struct
    type nonrec t = (TagKey.t * TagValue.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((TagKey.of_string chopped),
                              (TagValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (TagKey.to_value x) |>
                    (fun x -> (TagValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:TagKey.of_string
        ~of_json:TagValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module NextToken =
  struct
    type nonrec t = string
    let context_ = "NextToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () -> check_pattern i ~pattern:".*\\S.*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NextToken" j
    let to_json = simple_to_json to_value
  end
module WorkflowStepsSummaryList =
  struct
    type nonrec t = WorkflowStepSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:WorkflowStepSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:WorkflowStepSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"WorkflowStepsSummaryList"
        ~of_json:WorkflowStepSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module WorkflowStepGroupsSummaryList =
  struct
    type nonrec t = WorkflowStepGroupSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:WorkflowStepGroupSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:WorkflowStepGroupSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"WorkflowStepGroupsSummaryList"
        ~of_json:WorkflowStepGroupSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module TemplateStepSummaryList =
  struct
    type nonrec t = TemplateStepSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TemplateStepSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TemplateStepSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"TemplateStepSummaryList"
        ~of_json:TemplateStepSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module TemplateStepGroupSummaryList =
  struct
    type nonrec t = TemplateStepGroupSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TemplateStepGroupSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TemplateStepGroupSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"TemplateStepGroupSummaryList"
        ~of_json:TemplateStepGroupSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module PluginSummaries =
  struct
    type nonrec t = PluginSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:PluginSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PluginSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"PluginSummaries" ~of_json:PluginSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module MigrationWorkflowSummaryList =
  struct
    type nonrec t = MigrationWorkflowSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:MigrationWorkflowSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MigrationWorkflowSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"MigrationWorkflowSummaryList"
        ~of_json:MigrationWorkflowSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ApplicationConfigurationName =
  struct
    type nonrec t = string
    let context_ = "ApplicationConfigurationName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[-a-zA-Z0-9_.+]+[-a-zA-Z0-9_.+ ]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ApplicationConfigurationName" j
    let to_json = simple_to_json to_value
  end
module TemplateSummaryList =
  struct
    type nonrec t = TemplateSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TemplateSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TemplateSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"TemplateSummaryList"
        ~of_json:TemplateSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module TemplateName =
  struct
    type nonrec t = string
    let context_ = "TemplateName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[-a-zA-Z0-9_.+]+[-a-zA-Z0-9_.+ ]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TemplateName" j
    let to_json = simple_to_json to_value
  end
module GetWorkflowStepResponseOutputsList =
  struct
    type nonrec t = WorkflowStepOutput.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:5) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:WorkflowStepOutput.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:WorkflowStepOutput.of_xml)
    let of_json j =
      list_of_json ~kind:"GetWorkflowStepResponseOutputsList"
        ~of_json:WorkflowStepOutput.of_json j
    let to_json v = composed_to_json to_value v
  end
module StepAutomationConfiguration =
  struct
    type nonrec t =
      {
      scriptLocationS3Bucket: String_.t option
        [@ocaml.doc "The Amazon S3 bucket where the script is located."];
      scriptLocationS3Key: PlatformScriptKey.t option
        [@ocaml.doc "The Amazon S3 key for the script location."];
      command: PlatformCommand.t option
        [@ocaml.doc "The command to run the script."];
      runEnvironment: RunEnvironment.t option
        [@ocaml.doc "The source or target environment."];
      targetType: TargetType.t option
        [@ocaml.doc "The servers on which to run the script."]}
    let make ?scriptLocationS3Bucket =
      fun ?scriptLocationS3Key ->
        fun ?command ->
          fun ?runEnvironment ->
            fun ?targetType ->
              fun () ->
                {
                  scriptLocationS3Bucket;
                  scriptLocationS3Key;
                  command;
                  runEnvironment;
                  targetType
                }
    let to_value x =
      structure_to_value
        [("scriptLocationS3Bucket",
           (Option.map x.scriptLocationS3Bucket ~f:String_.to_value));
        ("scriptLocationS3Key",
          (Option.map x.scriptLocationS3Key ~f:PlatformScriptKey.to_value));
        ("command", (Option.map x.command ~f:PlatformCommand.to_value));
        ("runEnvironment",
          (Option.map x.runEnvironment ~f:RunEnvironment.to_value));
        ("targetType", (Option.map x.targetType ~f:TargetType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetType =
        (Option.map ~f:TargetType.of_xml) (Xml.child xml_arg0 "targetType") in
      let runEnvironment =
        (Option.map ~f:RunEnvironment.of_xml)
          (Xml.child xml_arg0 "runEnvironment") in
      let command =
        (Option.map ~f:PlatformCommand.of_xml) (Xml.child xml_arg0 "command") in
      let scriptLocationS3Key =
        (Option.map ~f:PlatformScriptKey.of_xml)
          (Xml.child xml_arg0 "scriptLocationS3Key") in
      let scriptLocationS3Bucket =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "scriptLocationS3Bucket") in
      make ?targetType ?runEnvironment ?command ?scriptLocationS3Key
        ?scriptLocationS3Bucket ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let targetType = field_map json__ "targetType" TargetType.of_json in
      let runEnvironment =
        field_map json__ "runEnvironment" RunEnvironment.of_json in
      let command = field_map json__ "command" PlatformCommand.of_json in
      let scriptLocationS3Key =
        field_map json__ "scriptLocationS3Key" PlatformScriptKey.of_json in
      let scriptLocationS3Bucket =
        field_map json__ "scriptLocationS3Bucket" String_.of_json in
      make ?targetType ?runEnvironment ?command ?scriptLocationS3Key
        ?scriptLocationS3Bucket ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The custom script to run tests on source or target environments."]
module StepOutputList =
  struct
    type nonrec t = StepOutput.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:StepOutput.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:StepOutput.of_xml)
    let of_json j =
      list_of_json ~kind:"StepOutputList" ~of_json:StepOutput.of_json j
    let to_json v = composed_to_json to_value v
  end
module TemplateInputList =
  struct
    type nonrec t = TemplateInput.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TemplateInput.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TemplateInput.of_xml)
    let of_json j =
      list_of_json ~kind:"TemplateInputList" ~of_json:TemplateInput.of_json j
    let to_json v = composed_to_json to_value v
  end
module TemplateStatus =
  struct
    type nonrec t =
      | CREATED 
      | READY 
      | PENDING_CREATION 
      | CREATING 
      | CREATION_FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CREATED -> "CREATED"
      | READY -> "READY"
      | PENDING_CREATION -> "PENDING_CREATION"
      | CREATING -> "CREATING"
      | CREATION_FAILED -> "CREATION_FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "CREATED" -> CREATED
      | "READY" -> READY
      | "PENDING_CREATION" -> PENDING_CREATION
      | "CREATING" -> CREATING
      | "CREATION_FAILED" -> CREATION_FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration TemplateStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"TemplateStatus" j)
    let to_json = simple_to_json to_value
  end
module MigrationWorkflowDescription =
  struct
    type nonrec t = string
    let context_ = "MigrationWorkflowDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:500) >>=
                  (fun () -> check_pattern i ~pattern:"[-a-zA-Z0-9_.+, ]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MigrationWorkflowDescription" j
    let to_json = simple_to_json to_value
  end
module MigrationWorkflowName =
  struct
    type nonrec t = string
    let context_ = "MigrationWorkflowName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[-a-zA-Z0-9_.+]+[-a-zA-Z0-9_.+ ]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MigrationWorkflowName" j
    let to_json = simple_to_json to_value
  end
module ConflictException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This exception is thrown when an attempt to update or delete a resource would cause an inconsistent state."]
module CreateTemplateRequestTemplateDescriptionString =
  struct
    type nonrec t = string
    let context_ = "CreateTemplateRequestTemplateDescriptionString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:250) >>=
                  (fun () -> check_pattern i ~pattern:".*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"CreateTemplateRequestTemplateDescriptionString" j
    let to_json = simple_to_json to_value
  end
module CreateTemplateRequestTemplateNameString =
  struct
    type nonrec t = string
    let context_ = "CreateTemplateRequestTemplateNameString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () -> check_pattern i ~pattern:"[ a-zA-Z0-9]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"CreateTemplateRequestTemplateNameString" j
    let to_json = simple_to_json to_value
  end
module TemplateSource =
  struct
    type nonrec t =
      {
      workflowId: MigrationWorkflowId.t option
        [@ocaml.doc
          "The ID of the workflow from the source migration workflow template."]}
    let make ?workflowId = fun () -> { workflowId }
    let to_value x =
      structure_to_value
        [("workflowId",
           (Option.map x.workflowId ~f:MigrationWorkflowId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let workflowId =
        (Option.map ~f:MigrationWorkflowId.of_xml)
          (Xml.child xml_arg0 "workflowId") in
      make ?workflowId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let workflowId =
        field_map json__ "workflowId" MigrationWorkflowId.of_json in
      make ?workflowId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The migration workflow template used as the source for the new template."]
module CreateMigrationWorkflowRequestApplicationConfigurationIdString =
  struct
    type nonrec t = string
    let context_ =
      "CreateMigrationWorkflowRequestApplicationConfigurationIdString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () -> check_pattern i ~pattern:"[-a-zA-Z0-9_.+]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json
        ~kind:"CreateMigrationWorkflowRequestApplicationConfigurationIdString"
        j
    let to_json = simple_to_json to_value
  end
module CreateMigrationWorkflowRequestDescriptionString =
  struct
    type nonrec t = string
    let context_ = "CreateMigrationWorkflowRequestDescriptionString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:500) >>=
                  (fun () -> check_pattern i ~pattern:"[-a-zA-Z0-9_.+, ]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"CreateMigrationWorkflowRequestDescriptionString"
        j
    let to_json = simple_to_json to_value
  end
module CreateMigrationWorkflowRequestNameString =
  struct
    type nonrec t = string
    let context_ = "CreateMigrationWorkflowRequestNameString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[-a-zA-Z0-9_.+]+[-a-zA-Z0-9_.+ ]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"CreateMigrationWorkflowRequestNameString" j
    let to_json = simple_to_json to_value
  end
module CreateMigrationWorkflowRequestTemplateIdString =
  struct
    type nonrec t = string
    let context_ = "CreateMigrationWorkflowRequestTemplateIdString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[-a-zA-Z0-9_.+]+[-a-zA-Z0-9_.+ ]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"CreateMigrationWorkflowRequestTemplateIdString" j
    let to_json = simple_to_json to_value
  end
module UpdateWorkflowStepResponse =
  struct
    type nonrec t =
      {
      id: StepId.t option [@ocaml.doc "The ID of the step."];
      stepGroupId: String_.t option [@ocaml.doc "The ID of the step group."];
      workflowId: String_.t option
        [@ocaml.doc "The ID of the migration workflow."];
      name: String_.t option [@ocaml.doc "The name of the step."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?id =
      fun ?stepGroupId ->
        fun ?workflowId ->
          fun ?name -> fun () -> { id; stepGroupId; workflowId; name }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:StepId.to_value));
        ("stepGroupId", (Option.map x.stepGroupId ~f:String_.to_value));
        ("workflowId", (Option.map x.workflowId ~f:String_.to_value));
        ("name", (Option.map x.name ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let workflowId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "workflowId") in
      let stepGroupId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "stepGroupId") in
      let id = (Option.map ~f:StepId.of_xml) (Xml.child xml_arg0 "id") in
      make ?name ?workflowId ?stepGroupId ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let name = field_map json__ "name" String_.of_json in
      let workflowId = field_map json__ "workflowId" String_.of_json in
      let stepGroupId = field_map json__ "stepGroupId" String_.of_json in
      let id = field_map json__ "id" StepId.of_json in
      make ?name ?workflowId ?stepGroupId ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Update a step in a migration workflow."]
module UpdateWorkflowStepRequest =
  struct
    type nonrec t =
      {
      id: StepId.t [@ocaml.doc "The ID of the step."];
      stepGroupId: StepGroupId.t [@ocaml.doc "The ID of the step group."];
      workflowId: MigrationWorkflowId.t
        [@ocaml.doc "The ID of the migration workflow."];
      name: StepName.t option [@ocaml.doc "The name of the step."];
      description: StepDescription.t option
        [@ocaml.doc "The description of the step."];
      stepActionType: StepActionType.t option
        [@ocaml.doc
          "The action type of the step. You must run and update the status of a manual step for the workflow to continue after the completion of the step."];
      workflowStepAutomationConfiguration:
        WorkflowStepAutomationConfiguration.t option
        [@ocaml.doc
          "The custom script to run tests on the source and target environments."];
      stepTarget: StringList.t option
        [@ocaml.doc "The servers on which a step will be run."];
      outputs: WorkflowStepOutputList.t option
        [@ocaml.doc "The outputs of a step."];
      previous: StringList.t option [@ocaml.doc "The previous step."];
      next: StringList.t option [@ocaml.doc "The next step."];
      status: StepStatus.t option [@ocaml.doc "The status of the step."]}
    let context_ = "UpdateWorkflowStepRequest"
    let make ?name =
      fun ?description ->
        fun ?stepActionType ->
          fun ?workflowStepAutomationConfiguration ->
            fun ?stepTarget ->
              fun ?outputs ->
                fun ?previous ->
                  fun ?next ->
                    fun ?status ->
                      fun ~id ->
                        fun ~stepGroupId ->
                          fun ~workflowId ->
                            fun () ->
                              {
                                name;
                                description;
                                stepActionType;
                                workflowStepAutomationConfiguration;
                                stepTarget;
                                outputs;
                                previous;
                                next;
                                status;
                                id;
                                stepGroupId;
                                workflowId
                              }
    let to_value x =
      structure_to_value
        [("id", (Some (StepId.to_value x.id)));
        ("stepGroupId", (Some (StepGroupId.to_value x.stepGroupId)));
        ("workflowId", (Some (MigrationWorkflowId.to_value x.workflowId)));
        ("name", (Option.map x.name ~f:StepName.to_value));
        ("description",
          (Option.map x.description ~f:StepDescription.to_value));
        ("stepActionType",
          (Option.map x.stepActionType ~f:StepActionType.to_value));
        ("workflowStepAutomationConfiguration",
          (Option.map x.workflowStepAutomationConfiguration
             ~f:WorkflowStepAutomationConfiguration.to_value));
        ("stepTarget", (Option.map x.stepTarget ~f:StringList.to_value));
        ("outputs",
          (Option.map x.outputs ~f:WorkflowStepOutputList.to_value));
        ("previous", (Option.map x.previous ~f:StringList.to_value));
        ("next", (Option.map x.next ~f:StringList.to_value));
        ("status", (Option.map x.status ~f:StepStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:StepStatus.of_xml) (Xml.child xml_arg0 "status") in
      let next =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "next") in
      let previous =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "previous") in
      let outputs =
        (Option.map ~f:WorkflowStepOutputList.of_xml)
          (Xml.child xml_arg0 "outputs") in
      let stepTarget =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "stepTarget") in
      let workflowStepAutomationConfiguration =
        (Option.map ~f:WorkflowStepAutomationConfiguration.of_xml)
          (Xml.child xml_arg0 "workflowStepAutomationConfiguration") in
      let stepActionType =
        (Option.map ~f:StepActionType.of_xml)
          (Xml.child xml_arg0 "stepActionType") in
      let description =
        (Option.map ~f:StepDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let name = (Option.map ~f:StepName.of_xml) (Xml.child xml_arg0 "name") in
      let workflowId =
        MigrationWorkflowId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "workflowId") in
      let stepGroupId =
        StepGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "stepGroupId") in
      let id = StepId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ?status ?next ?previous ?outputs ?stepTarget
        ?workflowStepAutomationConfiguration ?stepActionType ?description
        ?name ~workflowId ~stepGroupId ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "status" StepStatus.of_json in
      let next = field_map json__ "next" StringList.of_json in
      let previous = field_map json__ "previous" StringList.of_json in
      let outputs = field_map json__ "outputs" WorkflowStepOutputList.of_json in
      let stepTarget = field_map json__ "stepTarget" StringList.of_json in
      let workflowStepAutomationConfiguration =
        field_map json__ "workflowStepAutomationConfiguration"
          WorkflowStepAutomationConfiguration.of_json in
      let stepActionType =
        field_map json__ "stepActionType" StepActionType.of_json in
      let description =
        field_map json__ "description" StepDescription.of_json in
      let name = field_map json__ "name" StepName.of_json in
      let workflowId =
        field_map_exn json__ "workflowId" MigrationWorkflowId.of_json in
      let stepGroupId =
        field_map_exn json__ "stepGroupId" StepGroupId.of_json in
      let id = field_map_exn json__ "id" StepId.of_json in
      make ?status ?next ?previous ?outputs ?stepTarget
        ?workflowStepAutomationConfiguration ?stepActionType ?description
        ?name ~workflowId ~stepGroupId ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Update a step in a migration workflow."]
module UpdateWorkflowStepGroupResponse =
  struct
    type nonrec t =
      {
      workflowId: String_.t option
        [@ocaml.doc "The ID of the migration workflow."];
      name: String_.t option [@ocaml.doc "The name of the step group."];
      id: String_.t option [@ocaml.doc "The ID of the step group."];
      description: String_.t option
        [@ocaml.doc "The description of the step group."];
      tools: ToolsList.t option
        [@ocaml.doc "List of AWS services utilized in a migration workflow."];
      next: StringList.t option [@ocaml.doc "The next step group."];
      previous: StringList.t option [@ocaml.doc "The previous step group."];
      lastModifiedTime: Timestamp.t option
        [@ocaml.doc "The time at which the step group was last modified."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?workflowId =
      fun ?name ->
        fun ?id ->
          fun ?description ->
            fun ?tools ->
              fun ?next ->
                fun ?previous ->
                  fun ?lastModifiedTime ->
                    fun () ->
                      {
                        workflowId;
                        name;
                        id;
                        description;
                        tools;
                        next;
                        previous;
                        lastModifiedTime
                      }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("workflowId", (Option.map x.workflowId ~f:String_.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("id", (Option.map x.id ~f:String_.to_value));
        ("description", (Option.map x.description ~f:String_.to_value));
        ("tools", (Option.map x.tools ~f:ToolsList.to_value));
        ("next", (Option.map x.next ~f:StringList.to_value));
        ("previous", (Option.map x.previous ~f:StringList.to_value));
        ("lastModifiedTime",
          (Option.map x.lastModifiedTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastModifiedTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "lastModifiedTime") in
      let previous =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "previous") in
      let next =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "next") in
      let tools =
        (Option.map ~f:ToolsList.of_xml) (Xml.child xml_arg0 "tools") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      let id = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "id") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let workflowId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "workflowId") in
      make ?lastModifiedTime ?previous ?next ?tools ?description ?id ?name
        ?workflowId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let lastModifiedTime =
        field_map json__ "lastModifiedTime" Timestamp.of_json in
      let previous = field_map json__ "previous" StringList.of_json in
      let next = field_map json__ "next" StringList.of_json in
      let tools = field_map json__ "tools" ToolsList.of_json in
      let description = field_map json__ "description" String_.of_json in
      let id = field_map json__ "id" String_.of_json in
      let name = field_map json__ "name" String_.of_json in
      let workflowId = field_map json__ "workflowId" String_.of_json in
      make ?lastModifiedTime ?previous ?next ?tools ?description ?id ?name
        ?workflowId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Update the step group in a migration workflow."]
module UpdateWorkflowStepGroupRequest =
  struct
    type nonrec t =
      {
      workflowId: MigrationWorkflowId.t
        [@ocaml.doc "The ID of the migration workflow."];
      id: StepGroupId.t [@ocaml.doc "The ID of the step group."];
      name: StepGroupName.t option [@ocaml.doc "The name of the step group."];
      description: StepGroupDescription.t option
        [@ocaml.doc "The description of the step group."];
      next: StringList.t option [@ocaml.doc "The next step group."];
      previous: StringList.t option [@ocaml.doc "The previous step group."]}
    let context_ = "UpdateWorkflowStepGroupRequest"
    let make ?name =
      fun ?description ->
        fun ?next ->
          fun ?previous ->
            fun ~workflowId ->
              fun ~id ->
                fun () ->
                  { name; description; next; previous; workflowId; id }
    let to_value x =
      structure_to_value
        [("workflowId", (Some (MigrationWorkflowId.to_value x.workflowId)));
        ("id", (Some (StepGroupId.to_value x.id)));
        ("name", (Option.map x.name ~f:StepGroupName.to_value));
        ("description",
          (Option.map x.description ~f:StepGroupDescription.to_value));
        ("next", (Option.map x.next ~f:StringList.to_value));
        ("previous", (Option.map x.previous ~f:StringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let previous =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "previous") in
      let next =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "next") in
      let description =
        (Option.map ~f:StepGroupDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        (Option.map ~f:StepGroupName.of_xml) (Xml.child xml_arg0 "name") in
      let id =
        StepGroupId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      let workflowId =
        MigrationWorkflowId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "workflowId") in
      make ?previous ?next ?description ?name ~id ~workflowId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let previous = field_map json__ "previous" StringList.of_json in
      let next = field_map json__ "next" StringList.of_json in
      let description =
        field_map json__ "description" StepGroupDescription.of_json in
      let name = field_map json__ "name" StepGroupName.of_json in
      let id = field_map_exn json__ "id" StepGroupId.of_json in
      let workflowId =
        field_map_exn json__ "workflowId" MigrationWorkflowId.of_json in
      make ?previous ?next ?description ?name ~id ~workflowId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Update the step group in a migration workflow."]
module UpdateTemplateResponse =
  struct
    type nonrec t =
      {
      templateId: String_.t option
        [@ocaml.doc
          "The ID of the migration workflow template being updated."];
      templateArn: String_.t option
        [@ocaml.doc
          "The ARN of the migration workflow template being updated. The format for an Migration Hub Orchestrator template ARN is arn:aws:migrationhub-orchestrator:region:account:template/template-abcd1234. For more information about ARNs, see Amazon Resource Names (ARNs) in the AWS General Reference."];
      tags: StringMap.t option
        [@ocaml.doc "The tags added to the migration workflow template."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?templateId =
      fun ?templateArn ->
        fun ?tags -> fun () -> { templateId; templateArn; tags }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("templateId", (Option.map x.templateId ~f:String_.to_value));
        ("templateArn", (Option.map x.templateArn ~f:String_.to_value));
        ("tags", (Option.map x.tags ~f:StringMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:StringMap.of_xml) (Xml.child xml_arg0 "tags") in
      let templateArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "templateArn") in
      let templateId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "templateId") in
      make ?tags ?templateArn ?templateId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" StringMap.of_json in
      let templateArn = field_map json__ "templateArn" String_.of_json in
      let templateId = field_map json__ "templateId" String_.of_json in
      make ?tags ?templateArn ?templateId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates a migration workflow template."]
module UpdateTemplateRequest =
  struct
    type nonrec t =
      {
      id: TemplateId.t
        [@ocaml.doc
          "The ID of the request to update a migration workflow template."];
      templateName: UpdateTemplateRequestTemplateNameString.t option
        [@ocaml.doc "The name of the migration workflow template to update."];
      templateDescription:
        UpdateTemplateRequestTemplateDescriptionString.t option
        [@ocaml.doc
          "The description of the migration workflow template to update."];
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique, case-sensitive identifier that you provide to ensure the idempotency of the request."]}
    let context_ = "UpdateTemplateRequest"
    let make ?templateName =
      fun ?templateDescription ->
        fun ?clientToken ->
          fun ~id ->
            fun () -> { templateName; templateDescription; clientToken; id }
    let to_value x =
      structure_to_value
        [("id", (Some (TemplateId.to_value x.id)));
        ("templateName",
          (Option.map x.templateName
             ~f:UpdateTemplateRequestTemplateNameString.to_value));
        ("templateDescription",
          (Option.map x.templateDescription
             ~f:UpdateTemplateRequestTemplateDescriptionString.to_value));
        ("clientToken", (Option.map x.clientToken ~f:ClientToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "clientToken") in
      let templateDescription =
        (Option.map ~f:UpdateTemplateRequestTemplateDescriptionString.of_xml)
          (Xml.child xml_arg0 "templateDescription") in
      let templateName =
        (Option.map ~f:UpdateTemplateRequestTemplateNameString.of_xml)
          (Xml.child xml_arg0 "templateName") in
      let id =
        TemplateId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ?clientToken ?templateDescription ?templateName ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "clientToken" ClientToken.of_json in
      let templateDescription =
        field_map json__ "templateDescription"
          UpdateTemplateRequestTemplateDescriptionString.of_json in
      let templateName =
        field_map json__ "templateName"
          UpdateTemplateRequestTemplateNameString.of_json in
      let id = field_map_exn json__ "id" TemplateId.of_json in
      make ?clientToken ?templateDescription ?templateName ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates a migration workflow template."]
module UpdateMigrationWorkflowResponse =
  struct
    type nonrec t =
      {
      id: MigrationWorkflowId.t option
        [@ocaml.doc "The ID of the migration workflow."];
      arn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the migration workflow."];
      name: String_.t option
        [@ocaml.doc "The name of the migration workflow."];
      description: String_.t option
        [@ocaml.doc "The description of the migration workflow."];
      templateId: String_.t option [@ocaml.doc "The ID of the template."];
      adsApplicationConfigurationId: String_.t option
        [@ocaml.doc
          "The ID of the application configured in Application Discovery Service."];
      workflowInputs: StepInputParameters.t option
        [@ocaml.doc "The inputs required to update a migration workflow."];
      stepTargets: StringList.t option
        [@ocaml.doc "The servers on which a step will be run."];
      status: MigrationWorkflowStatusEnum.t option
        [@ocaml.doc "The status of the migration workflow."];
      creationTime: Timestamp.t option
        [@ocaml.doc "The time at which the migration workflow was created."];
      lastModifiedTime: Timestamp.t option
        [@ocaml.doc
          "The time at which the migration workflow was last modified."];
      tags: StringMap.t option
        [@ocaml.doc "The tags added to the migration workflow."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?id =
      fun ?arn ->
        fun ?name ->
          fun ?description ->
            fun ?templateId ->
              fun ?adsApplicationConfigurationId ->
                fun ?workflowInputs ->
                  fun ?stepTargets ->
                    fun ?status ->
                      fun ?creationTime ->
                        fun ?lastModifiedTime ->
                          fun ?tags ->
                            fun () ->
                              {
                                id;
                                arn;
                                name;
                                description;
                                templateId;
                                adsApplicationConfigurationId;
                                workflowInputs;
                                stepTargets;
                                status;
                                creationTime;
                                lastModifiedTime;
                                tags
                              }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:MigrationWorkflowId.to_value));
        ("arn", (Option.map x.arn ~f:String_.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("description", (Option.map x.description ~f:String_.to_value));
        ("templateId", (Option.map x.templateId ~f:String_.to_value));
        ("adsApplicationConfigurationId",
          (Option.map x.adsApplicationConfigurationId ~f:String_.to_value));
        ("workflowInputs",
          (Option.map x.workflowInputs ~f:StepInputParameters.to_value));
        ("stepTargets", (Option.map x.stepTargets ~f:StringList.to_value));
        ("status",
          (Option.map x.status ~f:MigrationWorkflowStatusEnum.to_value));
        ("creationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("lastModifiedTime",
          (Option.map x.lastModifiedTime ~f:Timestamp.to_value));
        ("tags", (Option.map x.tags ~f:StringMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:StringMap.of_xml) (Xml.child xml_arg0 "tags") in
      let lastModifiedTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "lastModifiedTime") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "creationTime") in
      let status =
        (Option.map ~f:MigrationWorkflowStatusEnum.of_xml)
          (Xml.child xml_arg0 "status") in
      let stepTargets =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "stepTargets") in
      let workflowInputs =
        (Option.map ~f:StepInputParameters.of_xml)
          (Xml.child xml_arg0 "workflowInputs") in
      let adsApplicationConfigurationId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "adsApplicationConfigurationId") in
      let templateId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "templateId") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "arn") in
      let id =
        (Option.map ~f:MigrationWorkflowId.of_xml) (Xml.child xml_arg0 "id") in
      make ?tags ?lastModifiedTime ?creationTime ?status ?stepTargets
        ?workflowInputs ?adsApplicationConfigurationId ?templateId
        ?description ?name ?arn ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" StringMap.of_json in
      let lastModifiedTime =
        field_map json__ "lastModifiedTime" Timestamp.of_json in
      let creationTime = field_map json__ "creationTime" Timestamp.of_json in
      let status =
        field_map json__ "status" MigrationWorkflowStatusEnum.of_json in
      let stepTargets = field_map json__ "stepTargets" StringList.of_json in
      let workflowInputs =
        field_map json__ "workflowInputs" StepInputParameters.of_json in
      let adsApplicationConfigurationId =
        field_map json__ "adsApplicationConfigurationId" String_.of_json in
      let templateId = field_map json__ "templateId" String_.of_json in
      let description = field_map json__ "description" String_.of_json in
      let name = field_map json__ "name" String_.of_json in
      let arn = field_map json__ "arn" String_.of_json in
      let id = field_map json__ "id" MigrationWorkflowId.of_json in
      make ?tags ?lastModifiedTime ?creationTime ?status ?stepTargets
        ?workflowInputs ?adsApplicationConfigurationId ?templateId
        ?description ?name ?arn ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Update a migration workflow."]
module UpdateMigrationWorkflowRequest =
  struct
    type nonrec t =
      {
      id: MigrationWorkflowId.t
        [@ocaml.doc "The ID of the migration workflow."];
      name: UpdateMigrationWorkflowRequestNameString.t option
        [@ocaml.doc "The name of the migration workflow."];
      description: UpdateMigrationWorkflowRequestDescriptionString.t option
        [@ocaml.doc "The description of the migration workflow."];
      inputParameters: StepInputParameters.t option
        [@ocaml.doc
          "The input parameters required to update a migration workflow."];
      stepTargets: StringList.t option
        [@ocaml.doc "The servers on which a step will be run."]}
    let context_ = "UpdateMigrationWorkflowRequest"
    let make ?name =
      fun ?description ->
        fun ?inputParameters ->
          fun ?stepTargets ->
            fun ~id ->
              fun () ->
                { name; description; inputParameters; stepTargets; id }
    let to_value x =
      structure_to_value
        [("id", (Some (MigrationWorkflowId.to_value x.id)));
        ("name",
          (Option.map x.name
             ~f:UpdateMigrationWorkflowRequestNameString.to_value));
        ("description",
          (Option.map x.description
             ~f:UpdateMigrationWorkflowRequestDescriptionString.to_value));
        ("inputParameters",
          (Option.map x.inputParameters ~f:StepInputParameters.to_value));
        ("stepTargets", (Option.map x.stepTargets ~f:StringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stepTargets =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "stepTargets") in
      let inputParameters =
        (Option.map ~f:StepInputParameters.of_xml)
          (Xml.child xml_arg0 "inputParameters") in
      let description =
        (Option.map ~f:UpdateMigrationWorkflowRequestDescriptionString.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        (Option.map ~f:UpdateMigrationWorkflowRequestNameString.of_xml)
          (Xml.child xml_arg0 "name") in
      let id =
        MigrationWorkflowId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ?stepTargets ?inputParameters ?description ?name ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let stepTargets = field_map json__ "stepTargets" StringList.of_json in
      let inputParameters =
        field_map json__ "inputParameters" StepInputParameters.of_json in
      let description =
        field_map json__ "description"
          UpdateMigrationWorkflowRequestDescriptionString.of_json in
      let name =
        field_map json__ "name"
          UpdateMigrationWorkflowRequestNameString.of_json in
      let id = field_map_exn json__ "id" MigrationWorkflowId.of_json in
      make ?stepTargets ?inputParameters ?description ?name ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Update a migration workflow."]
module UntagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the tags for a resource."]
module UntagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: ResourceArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the resource from which you want to remove tags."];
      tagKeys: TagKeyList.t
        [@ocaml.doc
          "One or more tag keys. Specify only the tag keys, not the tag values."]}
    let context_ = "UntagResourceRequest"
    let make ~resourceArn =
      fun ~tagKeys -> fun () -> { resourceArn; tagKeys }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (ResourceArn.to_value x.resourceArn)));
        ("tagKeys", (Some (TagKeyList.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "tagKeys") in
      let resourceArn =
        ResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tagKeys ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagKeys = field_map_exn json__ "tagKeys" TagKeyList.of_json in
      let resourceArn =
        field_map_exn json__ "resourceArn" ResourceArn.of_json in
      make ~tagKeys ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the tags for a resource."]
module TagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Tag a resource by specifying its Amazon Resource Name (ARN)."]
module TagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: ResourceArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the resource to which you want to add tags."];
      tags: TagMap.t
        [@ocaml.doc
          "A collection of labels, in the form of key:value pairs, that apply to this resource."]}
    let context_ = "TagResourceRequest"
    let make ~resourceArn = fun ~tags -> fun () -> { resourceArn; tags }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (ResourceArn.to_value x.resourceArn)));
        ("tags", (Some (TagMap.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagMap.of_xml (Xml.child_exn ~context:context_ xml_arg0 "tags") in
      let resourceArn =
        ResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tags ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map_exn json__ "tags" TagMap.of_json in
      let resourceArn =
        field_map_exn json__ "resourceArn" ResourceArn.of_json in
      make ~tags ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Tag a resource by specifying its Amazon Resource Name (ARN)."]
module StopMigrationWorkflowResponse =
  struct
    type nonrec t =
      {
      id: MigrationWorkflowId.t option
        [@ocaml.doc "The ID of the migration workflow."];
      arn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the migration workflow."];
      status: MigrationWorkflowStatusEnum.t option
        [@ocaml.doc "The status of the migration workflow."];
      statusMessage: String_.t option
        [@ocaml.doc "The status message of the migration workflow."];
      lastStopTime: Timestamp.t option
        [@ocaml.doc "The time at which the migration workflow was stopped."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?id =
      fun ?arn ->
        fun ?status ->
          fun ?statusMessage ->
            fun ?lastStopTime ->
              fun () -> { id; arn; status; statusMessage; lastStopTime }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:MigrationWorkflowId.to_value));
        ("arn", (Option.map x.arn ~f:String_.to_value));
        ("status",
          (Option.map x.status ~f:MigrationWorkflowStatusEnum.to_value));
        ("statusMessage", (Option.map x.statusMessage ~f:String_.to_value));
        ("lastStopTime", (Option.map x.lastStopTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastStopTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastStopTime") in
      let statusMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "statusMessage") in
      let status =
        (Option.map ~f:MigrationWorkflowStatusEnum.of_xml)
          (Xml.child xml_arg0 "status") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "arn") in
      let id =
        (Option.map ~f:MigrationWorkflowId.of_xml) (Xml.child xml_arg0 "id") in
      make ?lastStopTime ?statusMessage ?status ?arn ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let lastStopTime = field_map json__ "lastStopTime" Timestamp.of_json in
      let statusMessage = field_map json__ "statusMessage" String_.of_json in
      let status =
        field_map json__ "status" MigrationWorkflowStatusEnum.of_json in
      let arn = field_map json__ "arn" String_.of_json in
      let id = field_map json__ "id" MigrationWorkflowId.of_json in
      make ?lastStopTime ?statusMessage ?status ?arn ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Stop an ongoing migration workflow."]
module StopMigrationWorkflowRequest =
  struct
    type nonrec t =
      {
      id: MigrationWorkflowId.t
        [@ocaml.doc "The ID of the migration workflow."]}
    let context_ = "StopMigrationWorkflowRequest"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value [("id", (Some (MigrationWorkflowId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        MigrationWorkflowId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let id = field_map_exn json__ "id" MigrationWorkflowId.of_json in
      make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Stop an ongoing migration workflow."]
module StartMigrationWorkflowResponse =
  struct
    type nonrec t =
      {
      id: MigrationWorkflowId.t option
        [@ocaml.doc "The ID of the migration workflow."];
      arn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the migration workflow."];
      status: MigrationWorkflowStatusEnum.t option
        [@ocaml.doc "The status of the migration workflow."];
      statusMessage: String_.t option
        [@ocaml.doc "The status message of the migration workflow."];
      lastStartTime: Timestamp.t option
        [@ocaml.doc
          "The time at which the migration workflow was last started."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?id =
      fun ?arn ->
        fun ?status ->
          fun ?statusMessage ->
            fun ?lastStartTime ->
              fun () -> { id; arn; status; statusMessage; lastStartTime }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:MigrationWorkflowId.to_value));
        ("arn", (Option.map x.arn ~f:String_.to_value));
        ("status",
          (Option.map x.status ~f:MigrationWorkflowStatusEnum.to_value));
        ("statusMessage", (Option.map x.statusMessage ~f:String_.to_value));
        ("lastStartTime", (Option.map x.lastStartTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastStartTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastStartTime") in
      let statusMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "statusMessage") in
      let status =
        (Option.map ~f:MigrationWorkflowStatusEnum.of_xml)
          (Xml.child xml_arg0 "status") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "arn") in
      let id =
        (Option.map ~f:MigrationWorkflowId.of_xml) (Xml.child xml_arg0 "id") in
      make ?lastStartTime ?statusMessage ?status ?arn ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let lastStartTime = field_map json__ "lastStartTime" Timestamp.of_json in
      let statusMessage = field_map json__ "statusMessage" String_.of_json in
      let status =
        field_map json__ "status" MigrationWorkflowStatusEnum.of_json in
      let arn = field_map json__ "arn" String_.of_json in
      let id = field_map json__ "id" MigrationWorkflowId.of_json in
      make ?lastStartTime ?statusMessage ?status ?arn ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Start a migration workflow."]
module StartMigrationWorkflowRequest =
  struct
    type nonrec t =
      {
      id: MigrationWorkflowId.t
        [@ocaml.doc "The ID of the migration workflow."]}
    let context_ = "StartMigrationWorkflowRequest"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value [("id", (Some (MigrationWorkflowId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        MigrationWorkflowId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let id = field_map_exn json__ "id" MigrationWorkflowId.of_json in
      make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Start a migration workflow."]
module RetryWorkflowStepResponse =
  struct
    type nonrec t =
      {
      stepGroupId: String_.t option [@ocaml.doc "The ID of the step group."];
      workflowId: String_.t option
        [@ocaml.doc "The ID of the migration workflow."];
      id: String_.t option [@ocaml.doc "The ID of the step."];
      status: StepStatus.t option [@ocaml.doc "The status of the step."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?stepGroupId =
      fun ?workflowId ->
        fun ?id ->
          fun ?status -> fun () -> { stepGroupId; workflowId; id; status }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("stepGroupId", (Option.map x.stepGroupId ~f:String_.to_value));
        ("workflowId", (Option.map x.workflowId ~f:String_.to_value));
        ("id", (Option.map x.id ~f:String_.to_value));
        ("status", (Option.map x.status ~f:StepStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:StepStatus.of_xml) (Xml.child xml_arg0 "status") in
      let id = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "id") in
      let workflowId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "workflowId") in
      let stepGroupId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "stepGroupId") in
      make ?status ?id ?workflowId ?stepGroupId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "status" StepStatus.of_json in
      let id = field_map json__ "id" String_.of_json in
      let workflowId = field_map json__ "workflowId" String_.of_json in
      let stepGroupId = field_map json__ "stepGroupId" String_.of_json in
      make ?status ?id ?workflowId ?stepGroupId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retry a failed step in a migration workflow."]
module RetryWorkflowStepRequest =
  struct
    type nonrec t =
      {
      workflowId: MigrationWorkflowId.t
        [@ocaml.doc "The ID of the migration workflow."];
      stepGroupId: StepGroupId.t [@ocaml.doc "The ID of the step group."];
      id: StepId.t [@ocaml.doc "The ID of the step."]}
    let context_ = "RetryWorkflowStepRequest"
    let make ~workflowId =
      fun ~stepGroupId ->
        fun ~id -> fun () -> { workflowId; stepGroupId; id }
    let to_value x =
      structure_to_value
        [("workflowId", (Some (MigrationWorkflowId.to_value x.workflowId)));
        ("stepGroupId", (Some (StepGroupId.to_value x.stepGroupId)));
        ("id", (Some (StepId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id = StepId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      let stepGroupId =
        StepGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "stepGroupId") in
      let workflowId =
        MigrationWorkflowId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "workflowId") in
      make ~id ~stepGroupId ~workflowId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let id = field_map_exn json__ "id" StepId.of_json in
      let stepGroupId =
        field_map_exn json__ "stepGroupId" StepGroupId.of_json in
      let workflowId =
        field_map_exn json__ "workflowId" MigrationWorkflowId.of_json in
      make ~id ~stepGroupId ~workflowId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retry a failed step in a migration workflow."]
module ListWorkflowStepsResponse =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option [@ocaml.doc "The pagination token."];
      workflowStepsSummary: WorkflowStepsSummaryList.t option
        [@ocaml.doc "The summary of steps in a migration workflow."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?workflowStepsSummary ->
        fun () -> { nextToken; workflowStepsSummary }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("workflowStepsSummary",
          (Option.map x.workflowStepsSummary
             ~f:WorkflowStepsSummaryList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let workflowStepsSummary =
        (Option.map ~f:WorkflowStepsSummaryList.of_xml)
          (Xml.child xml_arg0 "workflowStepsSummary") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?workflowStepsSummary ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let workflowStepsSummary =
        field_map json__ "workflowStepsSummary"
          WorkflowStepsSummaryList.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      make ?workflowStepsSummary ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "List the steps in a workflow."]
module ListWorkflowStepsRequest =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option [@ocaml.doc "The pagination token."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of results that can be returned."];
      workflowId: MigrationWorkflowId.t
        [@ocaml.doc "The ID of the migration workflow."];
      stepGroupId: StepGroupId.t [@ocaml.doc "The ID of the step group."]}
    let context_ = "ListWorkflowStepsRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ~workflowId ->
          fun ~stepGroupId ->
            fun () -> { nextToken; maxResults; workflowId; stepGroupId }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("workflowId", (Some (MigrationWorkflowId.to_value x.workflowId)));
        ("stepGroupId", (Some (StepGroupId.to_value x.stepGroupId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stepGroupId =
        StepGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "stepGroupId") in
      let workflowId =
        MigrationWorkflowId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "workflowId") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ~stepGroupId ~workflowId ?maxResults ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let stepGroupId =
        field_map_exn json__ "stepGroupId" StepGroupId.of_json in
      let workflowId =
        field_map_exn json__ "workflowId" MigrationWorkflowId.of_json in
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      make ~stepGroupId ~workflowId ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "List the steps in a workflow."]
module ListWorkflowStepGroupsResponse =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option [@ocaml.doc "The pagination token."];
      workflowStepGroupsSummary: WorkflowStepGroupsSummaryList.t option
        [@ocaml.doc "The summary of step groups in a migration workflow."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?workflowStepGroupsSummary ->
        fun () -> { nextToken; workflowStepGroupsSummary }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("workflowStepGroupsSummary",
          (Option.map x.workflowStepGroupsSummary
             ~f:WorkflowStepGroupsSummaryList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let workflowStepGroupsSummary =
        (Option.map ~f:WorkflowStepGroupsSummaryList.of_xml)
          (Xml.child xml_arg0 "workflowStepGroupsSummary") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?workflowStepGroupsSummary ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let workflowStepGroupsSummary =
        field_map json__ "workflowStepGroupsSummary"
          WorkflowStepGroupsSummaryList.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      make ?workflowStepGroupsSummary ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "List the step groups in a migration workflow."]
module ListWorkflowStepGroupsRequest =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option [@ocaml.doc "The pagination token."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of results that can be returned."];
      workflowId: MigrationWorkflowId.t
        [@ocaml.doc "The ID of the migration workflow."]}
    let context_ = "ListWorkflowStepGroupsRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ~workflowId -> fun () -> { nextToken; maxResults; workflowId }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("workflowId", (Some (MigrationWorkflowId.to_value x.workflowId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let workflowId =
        MigrationWorkflowId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "workflowId") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ~workflowId ?maxResults ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let workflowId =
        field_map_exn json__ "workflowId" MigrationWorkflowId.of_json in
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      make ~workflowId ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "List the step groups in a migration workflow."]
module ListTemplateStepsResponse =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option [@ocaml.doc "The pagination token."];
      templateStepSummaryList: TemplateStepSummaryList.t option
        [@ocaml.doc "The list of summaries of steps in a template."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?templateStepSummaryList ->
        fun () -> { nextToken; templateStepSummaryList }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("templateStepSummaryList",
          (Option.map x.templateStepSummaryList
             ~f:TemplateStepSummaryList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let templateStepSummaryList =
        (Option.map ~f:TemplateStepSummaryList.of_xml)
          (Xml.child xml_arg0 "templateStepSummaryList") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?templateStepSummaryList ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let templateStepSummaryList =
        field_map json__ "templateStepSummaryList"
          TemplateStepSummaryList.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      make ?templateStepSummaryList ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "List the steps in a template."]
module ListTemplateStepsRequest =
  struct
    type nonrec t =
      {
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of results that can be returned."];
      nextToken: NextToken.t option [@ocaml.doc "The pagination token."];
      templateId: TemplateId.t [@ocaml.doc "The ID of the template."];
      stepGroupId: StepGroupId.t [@ocaml.doc "The ID of the step group."]}
    let context_ = "ListTemplateStepsRequest"
    let make ?maxResults =
      fun ?nextToken ->
        fun ~templateId ->
          fun ~stepGroupId ->
            fun () -> { maxResults; nextToken; templateId; stepGroupId }
    let to_value x =
      structure_to_value
        [("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("templateId", (Some (TemplateId.to_value x.templateId)));
        ("stepGroupId", (Some (StepGroupId.to_value x.stepGroupId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stepGroupId =
        StepGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "stepGroupId") in
      let templateId =
        TemplateId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "templateId") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      make ~stepGroupId ~templateId ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let stepGroupId =
        field_map_exn json__ "stepGroupId" StepGroupId.of_json in
      let templateId = field_map_exn json__ "templateId" TemplateId.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      make ~stepGroupId ~templateId ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "List the steps in a template."]
module ListTemplateStepGroupsResponse =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option [@ocaml.doc "The pagination token."];
      templateStepGroupSummary: TemplateStepGroupSummaryList.t option
        [@ocaml.doc "The summary of the step group in the template."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?templateStepGroupSummary ->
        fun () -> { nextToken; templateStepGroupSummary }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("templateStepGroupSummary",
          (Option.map x.templateStepGroupSummary
             ~f:TemplateStepGroupSummaryList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let templateStepGroupSummary =
        (Option.map ~f:TemplateStepGroupSummaryList.of_xml)
          (Xml.child xml_arg0 "templateStepGroupSummary") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?templateStepGroupSummary ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let templateStepGroupSummary =
        field_map json__ "templateStepGroupSummary"
          TemplateStepGroupSummaryList.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      make ?templateStepGroupSummary ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "List the step groups in a template."]
module ListTemplateStepGroupsRequest =
  struct
    type nonrec t =
      {
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of results that can be returned."];
      nextToken: NextToken.t option [@ocaml.doc "The pagination token."];
      templateId: TemplateId.t [@ocaml.doc "The ID of the template."]}
    let context_ = "ListTemplateStepGroupsRequest"
    let make ?maxResults =
      fun ?nextToken ->
        fun ~templateId -> fun () -> { maxResults; nextToken; templateId }
    let to_value x =
      structure_to_value
        [("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("templateId", (Some (TemplateId.to_value x.templateId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let templateId =
        TemplateId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "templateId") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      make ~templateId ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let templateId = field_map_exn json__ "templateId" TemplateId.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      make ~templateId ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "List the step groups in a template."]
module ListTagsForResourceResponse =
  struct
    type nonrec t =
      {
      tags: TagMap.t option [@ocaml.doc "The tags added to a resource."]}
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      make ?tags ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in make ?tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "List the tags added to a resource."]
module ListTagsForResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: ResourceArn.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the resource."]}
    let context_ = "ListTagsForResourceRequest"
    let make ~resourceArn = fun () -> { resourceArn }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (ResourceArn.to_value x.resourceArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        ResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceArn =
        field_map_exn json__ "resourceArn" ResourceArn.of_json in
      make ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "List the tags added to a resource."]
module ListPluginsResponse =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option [@ocaml.doc "The pagination token."];
      plugins: PluginSummaries.t option
        [@ocaml.doc "Migration Hub Orchestrator plugins."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken = fun ?plugins -> fun () -> { nextToken; plugins }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("plugins", (Option.map x.plugins ~f:PluginSummaries.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let plugins =
        (Option.map ~f:PluginSummaries.of_xml) (Xml.child xml_arg0 "plugins") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?plugins ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let plugins = field_map json__ "plugins" PluginSummaries.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      make ?plugins ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "List AWS Migration Hub Orchestrator plugins."]
module ListPluginsRequest =
  struct
    type nonrec t =
      {
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of plugins that can be returned."];
      nextToken: NextToken.t option [@ocaml.doc "The pagination token."]}
    let make ?maxResults =
      fun ?nextToken -> fun () -> { maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      make ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      make ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "List AWS Migration Hub Orchestrator plugins."]
module ListMigrationWorkflowsResponse =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option [@ocaml.doc "The pagination token."];
      migrationWorkflowSummary: MigrationWorkflowSummaryList.t option
        [@ocaml.doc "The summary of the migration workflow."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?migrationWorkflowSummary ->
        fun () -> { nextToken; migrationWorkflowSummary }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("migrationWorkflowSummary",
          (Option.map x.migrationWorkflowSummary
             ~f:MigrationWorkflowSummaryList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let migrationWorkflowSummary =
        (Option.map ~f:MigrationWorkflowSummaryList.of_xml)
          (Xml.child xml_arg0 "migrationWorkflowSummary") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?migrationWorkflowSummary ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let migrationWorkflowSummary =
        field_map json__ "migrationWorkflowSummary"
          MigrationWorkflowSummaryList.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      make ?migrationWorkflowSummary ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "List the migration workflows."]
module ListMigrationWorkflowsRequest =
  struct
    type nonrec t =
      {
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of results that can be returned."];
      nextToken: NextToken.t option [@ocaml.doc "The pagination token."];
      templateId: TemplateId.t option [@ocaml.doc "The ID of the template."];
      adsApplicationConfigurationName: ApplicationConfigurationName.t option
        [@ocaml.doc
          "The name of the application configured in Application Discovery Service."];
      status: MigrationWorkflowStatusEnum.t option
        [@ocaml.doc "The status of the migration workflow."];
      name: String_.t option
        [@ocaml.doc "The name of the migration workflow."]}
    let make ?maxResults =
      fun ?nextToken ->
        fun ?templateId ->
          fun ?adsApplicationConfigurationName ->
            fun ?status ->
              fun ?name ->
                fun () ->
                  {
                    maxResults;
                    nextToken;
                    templateId;
                    adsApplicationConfigurationName;
                    status;
                    name
                  }
    let to_value x =
      structure_to_value
        [("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("templateId", (Option.map x.templateId ~f:TemplateId.to_value));
        ("adsApplicationConfigurationName",
          (Option.map x.adsApplicationConfigurationName
             ~f:ApplicationConfigurationName.to_value));
        ("status",
          (Option.map x.status ~f:MigrationWorkflowStatusEnum.to_value));
        ("name", (Option.map x.name ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let status =
        (Option.map ~f:MigrationWorkflowStatusEnum.of_xml)
          (Xml.child xml_arg0 "status") in
      let adsApplicationConfigurationName =
        (Option.map ~f:ApplicationConfigurationName.of_xml)
          (Xml.child xml_arg0 "adsApplicationConfigurationName") in
      let templateId =
        (Option.map ~f:TemplateId.of_xml) (Xml.child xml_arg0 "templateId") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      make ?name ?status ?adsApplicationConfigurationName ?templateId
        ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let name = field_map json__ "name" String_.of_json in
      let status =
        field_map json__ "status" MigrationWorkflowStatusEnum.of_json in
      let adsApplicationConfigurationName =
        field_map json__ "adsApplicationConfigurationName"
          ApplicationConfigurationName.of_json in
      let templateId = field_map json__ "templateId" TemplateId.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      make ?name ?status ?adsApplicationConfigurationName ?templateId
        ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "List the migration workflows."]
module ListMigrationWorkflowTemplatesResponse =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option [@ocaml.doc "The pagination token."];
      templateSummary: TemplateSummaryList.t option
        [@ocaml.doc "The summary of the template."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?templateSummary -> fun () -> { nextToken; templateSummary }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("templateSummary",
          (Option.map x.templateSummary ~f:TemplateSummaryList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let templateSummary =
        (Option.map ~f:TemplateSummaryList.of_xml)
          (Xml.child xml_arg0 "templateSummary") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?templateSummary ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let templateSummary =
        field_map json__ "templateSummary" TemplateSummaryList.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      make ?templateSummary ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "List the templates available in Migration Hub Orchestrator to create a migration workflow."]
module ListMigrationWorkflowTemplatesRequest =
  struct
    type nonrec t =
      {
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of results that can be returned."];
      nextToken: NextToken.t option [@ocaml.doc "The pagination token."];
      name: TemplateName.t option [@ocaml.doc "The name of the template."]}
    let make ?maxResults =
      fun ?nextToken ->
        fun ?name -> fun () -> { maxResults; nextToken; name }
    let to_value x =
      structure_to_value
        [("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("name", (Option.map x.name ~f:TemplateName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        (Option.map ~f:TemplateName.of_xml) (Xml.child xml_arg0 "name") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      make ?name ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let name = field_map json__ "name" TemplateName.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      make ?name ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "List the templates available in Migration Hub Orchestrator to create a migration workflow."]
module GetWorkflowStepResponse =
  struct
    type nonrec t =
      {
      name: String_.t option [@ocaml.doc "The name of the step."];
      stepGroupId: String_.t option [@ocaml.doc "The ID of the step group."];
      workflowId: String_.t option
        [@ocaml.doc "The ID of the migration workflow."];
      stepId: String_.t option [@ocaml.doc "The ID of the step."];
      description: String_.t option
        [@ocaml.doc "The description of the step."];
      stepActionType: StepActionType.t option
        [@ocaml.doc
          "The action type of the step. You must run and update the status of a manual step for the workflow to continue after the completion of the step."];
      owner: Owner.t option [@ocaml.doc "The owner of the step."];
      workflowStepAutomationConfiguration:
        WorkflowStepAutomationConfiguration.t option
        [@ocaml.doc
          "The custom script to run tests on source or target environments."];
      stepTarget: StringList.t option
        [@ocaml.doc "The servers on which a step will be run."];
      outputs: GetWorkflowStepResponseOutputsList.t option
        [@ocaml.doc "The outputs of the step."];
      previous: StringList.t option [@ocaml.doc "The previous step."];
      next: StringList.t option [@ocaml.doc "The next step."];
      status: StepStatus.t option [@ocaml.doc "The status of the step."];
      statusMessage: String_.t option
        [@ocaml.doc "The status message of the migration workflow."];
      scriptOutputLocation: String_.t option
        [@ocaml.doc "The output location of the script."];
      creationTime: Timestamp.t option
        [@ocaml.doc "The time at which the step was created."];
      lastStartTime: Timestamp.t option
        [@ocaml.doc "The time at which the workflow was last started."];
      endTime: Timestamp.t option
        [@ocaml.doc "The time at which the step ended."];
      noOfSrvCompleted: Integer.t option
        [@ocaml.doc "The number of servers that have been migrated."];
      noOfSrvFailed: Integer.t option
        [@ocaml.doc "The number of servers that have failed to migrate."];
      totalNoOfSrv: Integer.t option
        [@ocaml.doc "The total number of servers that have been migrated."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?name =
      fun ?stepGroupId ->
        fun ?workflowId ->
          fun ?stepId ->
            fun ?description ->
              fun ?stepActionType ->
                fun ?owner ->
                  fun ?workflowStepAutomationConfiguration ->
                    fun ?stepTarget ->
                      fun ?outputs ->
                        fun ?previous ->
                          fun ?next ->
                            fun ?status ->
                              fun ?statusMessage ->
                                fun ?scriptOutputLocation ->
                                  fun ?creationTime ->
                                    fun ?lastStartTime ->
                                      fun ?endTime ->
                                        fun ?noOfSrvCompleted ->
                                          fun ?noOfSrvFailed ->
                                            fun ?totalNoOfSrv ->
                                              fun () ->
                                                {
                                                  name;
                                                  stepGroupId;
                                                  workflowId;
                                                  stepId;
                                                  description;
                                                  stepActionType;
                                                  owner;
                                                  workflowStepAutomationConfiguration;
                                                  stepTarget;
                                                  outputs;
                                                  previous;
                                                  next;
                                                  status;
                                                  statusMessage;
                                                  scriptOutputLocation;
                                                  creationTime;
                                                  lastStartTime;
                                                  endTime;
                                                  noOfSrvCompleted;
                                                  noOfSrvFailed;
                                                  totalNoOfSrv
                                                }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:String_.to_value));
        ("stepGroupId", (Option.map x.stepGroupId ~f:String_.to_value));
        ("workflowId", (Option.map x.workflowId ~f:String_.to_value));
        ("stepId", (Option.map x.stepId ~f:String_.to_value));
        ("description", (Option.map x.description ~f:String_.to_value));
        ("stepActionType",
          (Option.map x.stepActionType ~f:StepActionType.to_value));
        ("owner", (Option.map x.owner ~f:Owner.to_value));
        ("workflowStepAutomationConfiguration",
          (Option.map x.workflowStepAutomationConfiguration
             ~f:WorkflowStepAutomationConfiguration.to_value));
        ("stepTarget", (Option.map x.stepTarget ~f:StringList.to_value));
        ("outputs",
          (Option.map x.outputs
             ~f:GetWorkflowStepResponseOutputsList.to_value));
        ("previous", (Option.map x.previous ~f:StringList.to_value));
        ("next", (Option.map x.next ~f:StringList.to_value));
        ("status", (Option.map x.status ~f:StepStatus.to_value));
        ("statusMessage", (Option.map x.statusMessage ~f:String_.to_value));
        ("scriptOutputLocation",
          (Option.map x.scriptOutputLocation ~f:String_.to_value));
        ("creationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("lastStartTime", (Option.map x.lastStartTime ~f:Timestamp.to_value));
        ("endTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("noOfSrvCompleted",
          (Option.map x.noOfSrvCompleted ~f:Integer.to_value));
        ("noOfSrvFailed", (Option.map x.noOfSrvFailed ~f:Integer.to_value));
        ("totalNoOfSrv", (Option.map x.totalNoOfSrv ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let totalNoOfSrv =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "totalNoOfSrv") in
      let noOfSrvFailed =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "noOfSrvFailed") in
      let noOfSrvCompleted =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "noOfSrvCompleted") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "endTime") in
      let lastStartTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastStartTime") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "creationTime") in
      let scriptOutputLocation =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "scriptOutputLocation") in
      let statusMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "statusMessage") in
      let status =
        (Option.map ~f:StepStatus.of_xml) (Xml.child xml_arg0 "status") in
      let next =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "next") in
      let previous =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "previous") in
      let outputs =
        (Option.map ~f:GetWorkflowStepResponseOutputsList.of_xml)
          (Xml.child xml_arg0 "outputs") in
      let stepTarget =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "stepTarget") in
      let workflowStepAutomationConfiguration =
        (Option.map ~f:WorkflowStepAutomationConfiguration.of_xml)
          (Xml.child xml_arg0 "workflowStepAutomationConfiguration") in
      let owner = (Option.map ~f:Owner.of_xml) (Xml.child xml_arg0 "owner") in
      let stepActionType =
        (Option.map ~f:StepActionType.of_xml)
          (Xml.child xml_arg0 "stepActionType") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      let stepId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "stepId") in
      let workflowId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "workflowId") in
      let stepGroupId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "stepGroupId") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      make ?totalNoOfSrv ?noOfSrvFailed ?noOfSrvCompleted ?endTime
        ?lastStartTime ?creationTime ?scriptOutputLocation ?statusMessage
        ?status ?next ?previous ?outputs ?stepTarget
        ?workflowStepAutomationConfiguration ?owner ?stepActionType
        ?description ?stepId ?workflowId ?stepGroupId ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let totalNoOfSrv = field_map json__ "totalNoOfSrv" Integer.of_json in
      let noOfSrvFailed = field_map json__ "noOfSrvFailed" Integer.of_json in
      let noOfSrvCompleted =
        field_map json__ "noOfSrvCompleted" Integer.of_json in
      let endTime = field_map json__ "endTime" Timestamp.of_json in
      let lastStartTime = field_map json__ "lastStartTime" Timestamp.of_json in
      let creationTime = field_map json__ "creationTime" Timestamp.of_json in
      let scriptOutputLocation =
        field_map json__ "scriptOutputLocation" String_.of_json in
      let statusMessage = field_map json__ "statusMessage" String_.of_json in
      let status = field_map json__ "status" StepStatus.of_json in
      let next = field_map json__ "next" StringList.of_json in
      let previous = field_map json__ "previous" StringList.of_json in
      let outputs =
        field_map json__ "outputs" GetWorkflowStepResponseOutputsList.of_json in
      let stepTarget = field_map json__ "stepTarget" StringList.of_json in
      let workflowStepAutomationConfiguration =
        field_map json__ "workflowStepAutomationConfiguration"
          WorkflowStepAutomationConfiguration.of_json in
      let owner = field_map json__ "owner" Owner.of_json in
      let stepActionType =
        field_map json__ "stepActionType" StepActionType.of_json in
      let description = field_map json__ "description" String_.of_json in
      let stepId = field_map json__ "stepId" String_.of_json in
      let workflowId = field_map json__ "workflowId" String_.of_json in
      let stepGroupId = field_map json__ "stepGroupId" String_.of_json in
      let name = field_map json__ "name" String_.of_json in
      make ?totalNoOfSrv ?noOfSrvFailed ?noOfSrvCompleted ?endTime
        ?lastStartTime ?creationTime ?scriptOutputLocation ?statusMessage
        ?status ?next ?previous ?outputs ?stepTarget
        ?workflowStepAutomationConfiguration ?owner ?stepActionType
        ?description ?stepId ?workflowId ?stepGroupId ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Get a step in the migration workflow."]
module GetWorkflowStepRequest =
  struct
    type nonrec t =
      {
      workflowId: MigrationWorkflowId.t
        [@ocaml.doc "The ID of the migration workflow."];
      stepGroupId: StepGroupId.t [@ocaml.doc "The ID of the step group."];
      id: StepId.t [@ocaml.doc "The ID of the step."]}
    let context_ = "GetWorkflowStepRequest"
    let make ~workflowId =
      fun ~stepGroupId ->
        fun ~id -> fun () -> { workflowId; stepGroupId; id }
    let to_value x =
      structure_to_value
        [("workflowId", (Some (MigrationWorkflowId.to_value x.workflowId)));
        ("stepGroupId", (Some (StepGroupId.to_value x.stepGroupId)));
        ("id", (Some (StepId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id = StepId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      let stepGroupId =
        StepGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "stepGroupId") in
      let workflowId =
        MigrationWorkflowId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "workflowId") in
      make ~id ~stepGroupId ~workflowId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let id = field_map_exn json__ "id" StepId.of_json in
      let stepGroupId =
        field_map_exn json__ "stepGroupId" StepGroupId.of_json in
      let workflowId =
        field_map_exn json__ "workflowId" MigrationWorkflowId.of_json in
      make ~id ~stepGroupId ~workflowId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Get a step in the migration workflow."]
module GetWorkflowStepGroupResponse =
  struct
    type nonrec t =
      {
      id: StepGroupId.t option [@ocaml.doc "The ID of the step group."];
      workflowId: String_.t option
        [@ocaml.doc "The ID of the migration workflow."];
      name: String_.t option [@ocaml.doc "The name of the step group."];
      description: String_.t option
        [@ocaml.doc "The description of the step group."];
      status: StepGroupStatus.t option
        [@ocaml.doc "The status of the step group."];
      owner: Owner.t option [@ocaml.doc "The owner of the step group."];
      creationTime: Timestamp.t option
        [@ocaml.doc "The time at which the step group was created."];
      lastModifiedTime: Timestamp.t option
        [@ocaml.doc "The time at which the step group was last modified."];
      endTime: Timestamp.t option
        [@ocaml.doc "The time at which the step group ended."];
      tools: ToolsList.t option
        [@ocaml.doc "List of AWS services utilized in a migration workflow."];
      previous: StringList.t option [@ocaml.doc "The previous step group."];
      next: StringList.t option [@ocaml.doc "The next step group."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?id =
      fun ?workflowId ->
        fun ?name ->
          fun ?description ->
            fun ?status ->
              fun ?owner ->
                fun ?creationTime ->
                  fun ?lastModifiedTime ->
                    fun ?endTime ->
                      fun ?tools ->
                        fun ?previous ->
                          fun ?next ->
                            fun () ->
                              {
                                id;
                                workflowId;
                                name;
                                description;
                                status;
                                owner;
                                creationTime;
                                lastModifiedTime;
                                endTime;
                                tools;
                                previous;
                                next
                              }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:StepGroupId.to_value));
        ("workflowId", (Option.map x.workflowId ~f:String_.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("description", (Option.map x.description ~f:String_.to_value));
        ("status", (Option.map x.status ~f:StepGroupStatus.to_value));
        ("owner", (Option.map x.owner ~f:Owner.to_value));
        ("creationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("lastModifiedTime",
          (Option.map x.lastModifiedTime ~f:Timestamp.to_value));
        ("endTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("tools", (Option.map x.tools ~f:ToolsList.to_value));
        ("previous", (Option.map x.previous ~f:StringList.to_value));
        ("next", (Option.map x.next ~f:StringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let next =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "next") in
      let previous =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "previous") in
      let tools =
        (Option.map ~f:ToolsList.of_xml) (Xml.child xml_arg0 "tools") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "endTime") in
      let lastModifiedTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "lastModifiedTime") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "creationTime") in
      let owner = (Option.map ~f:Owner.of_xml) (Xml.child xml_arg0 "owner") in
      let status =
        (Option.map ~f:StepGroupStatus.of_xml) (Xml.child xml_arg0 "status") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let workflowId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "workflowId") in
      let id = (Option.map ~f:StepGroupId.of_xml) (Xml.child xml_arg0 "id") in
      make ?next ?previous ?tools ?endTime ?lastModifiedTime ?creationTime
        ?owner ?status ?description ?name ?workflowId ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let next = field_map json__ "next" StringList.of_json in
      let previous = field_map json__ "previous" StringList.of_json in
      let tools = field_map json__ "tools" ToolsList.of_json in
      let endTime = field_map json__ "endTime" Timestamp.of_json in
      let lastModifiedTime =
        field_map json__ "lastModifiedTime" Timestamp.of_json in
      let creationTime = field_map json__ "creationTime" Timestamp.of_json in
      let owner = field_map json__ "owner" Owner.of_json in
      let status = field_map json__ "status" StepGroupStatus.of_json in
      let description = field_map json__ "description" String_.of_json in
      let name = field_map json__ "name" String_.of_json in
      let workflowId = field_map json__ "workflowId" String_.of_json in
      let id = field_map json__ "id" StepGroupId.of_json in
      make ?next ?previous ?tools ?endTime ?lastModifiedTime ?creationTime
        ?owner ?status ?description ?name ?workflowId ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Get the step group of a migration workflow."]
module GetWorkflowStepGroupRequest =
  struct
    type nonrec t =
      {
      id: StepGroupId.t [@ocaml.doc "The ID of the step group."];
      workflowId: MigrationWorkflowId.t
        [@ocaml.doc "The ID of the migration workflow."]}
    let context_ = "GetWorkflowStepGroupRequest"
    let make ~id = fun ~workflowId -> fun () -> { id; workflowId }
    let to_value x =
      structure_to_value
        [("id", (Some (StepGroupId.to_value x.id)));
        ("workflowId", (Some (MigrationWorkflowId.to_value x.workflowId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let workflowId =
        MigrationWorkflowId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "workflowId") in
      let id =
        StepGroupId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~workflowId ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let workflowId =
        field_map_exn json__ "workflowId" MigrationWorkflowId.of_json in
      let id = field_map_exn json__ "id" StepGroupId.of_json in
      make ~workflowId ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Get the step group of a migration workflow."]
module GetTemplateStepResponse =
  struct
    type nonrec t =
      {
      id: StepId.t option [@ocaml.doc "The ID of the step."];
      stepGroupId: StepGroupId.t option
        [@ocaml.doc "The ID of the step group."];
      templateId: TemplateId.t option [@ocaml.doc "The ID of the template."];
      name: String_.t option [@ocaml.doc "The name of the step."];
      description: String_.t option
        [@ocaml.doc "The description of the step."];
      stepActionType: StepActionType.t option
        [@ocaml.doc
          "The action type of the step. You must run and update the status of a manual step for the workflow to continue after the completion of the step."];
      creationTime: String_.t option
        [@ocaml.doc "The time at which the step was created."];
      previous: StringList.t option [@ocaml.doc "The previous step."];
      next: StringList.t option [@ocaml.doc "The next step."];
      outputs: StepOutputList.t option
        [@ocaml.doc "The outputs of the step."];
      stepAutomationConfiguration: StepAutomationConfiguration.t option
        [@ocaml.doc
          "The custom script to run tests on source or target environments."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?id =
      fun ?stepGroupId ->
        fun ?templateId ->
          fun ?name ->
            fun ?description ->
              fun ?stepActionType ->
                fun ?creationTime ->
                  fun ?previous ->
                    fun ?next ->
                      fun ?outputs ->
                        fun ?stepAutomationConfiguration ->
                          fun () ->
                            {
                              id;
                              stepGroupId;
                              templateId;
                              name;
                              description;
                              stepActionType;
                              creationTime;
                              previous;
                              next;
                              outputs;
                              stepAutomationConfiguration
                            }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:StepId.to_value));
        ("stepGroupId", (Option.map x.stepGroupId ~f:StepGroupId.to_value));
        ("templateId", (Option.map x.templateId ~f:TemplateId.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("description", (Option.map x.description ~f:String_.to_value));
        ("stepActionType",
          (Option.map x.stepActionType ~f:StepActionType.to_value));
        ("creationTime", (Option.map x.creationTime ~f:String_.to_value));
        ("previous", (Option.map x.previous ~f:StringList.to_value));
        ("next", (Option.map x.next ~f:StringList.to_value));
        ("outputs", (Option.map x.outputs ~f:StepOutputList.to_value));
        ("stepAutomationConfiguration",
          (Option.map x.stepAutomationConfiguration
             ~f:StepAutomationConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stepAutomationConfiguration =
        (Option.map ~f:StepAutomationConfiguration.of_xml)
          (Xml.child xml_arg0 "stepAutomationConfiguration") in
      let outputs =
        (Option.map ~f:StepOutputList.of_xml) (Xml.child xml_arg0 "outputs") in
      let next =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "next") in
      let previous =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "previous") in
      let creationTime =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "creationTime") in
      let stepActionType =
        (Option.map ~f:StepActionType.of_xml)
          (Xml.child xml_arg0 "stepActionType") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let templateId =
        (Option.map ~f:TemplateId.of_xml) (Xml.child xml_arg0 "templateId") in
      let stepGroupId =
        (Option.map ~f:StepGroupId.of_xml) (Xml.child xml_arg0 "stepGroupId") in
      let id = (Option.map ~f:StepId.of_xml) (Xml.child xml_arg0 "id") in
      make ?stepAutomationConfiguration ?outputs ?next ?previous
        ?creationTime ?stepActionType ?description ?name ?templateId
        ?stepGroupId ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let stepAutomationConfiguration =
        field_map json__ "stepAutomationConfiguration"
          StepAutomationConfiguration.of_json in
      let outputs = field_map json__ "outputs" StepOutputList.of_json in
      let next = field_map json__ "next" StringList.of_json in
      let previous = field_map json__ "previous" StringList.of_json in
      let creationTime = field_map json__ "creationTime" String_.of_json in
      let stepActionType =
        field_map json__ "stepActionType" StepActionType.of_json in
      let description = field_map json__ "description" String_.of_json in
      let name = field_map json__ "name" String_.of_json in
      let templateId = field_map json__ "templateId" TemplateId.of_json in
      let stepGroupId = field_map json__ "stepGroupId" StepGroupId.of_json in
      let id = field_map json__ "id" StepId.of_json in
      make ?stepAutomationConfiguration ?outputs ?next ?previous
        ?creationTime ?stepActionType ?description ?name ?templateId
        ?stepGroupId ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Get a specific step in a template."]
module GetTemplateStepRequest =
  struct
    type nonrec t =
      {
      id: StepId.t [@ocaml.doc "The ID of the step."];
      templateId: TemplateId.t [@ocaml.doc "The ID of the template."];
      stepGroupId: StepGroupId.t [@ocaml.doc "The ID of the step group."]}
    let context_ = "GetTemplateStepRequest"
    let make ~id =
      fun ~templateId ->
        fun ~stepGroupId -> fun () -> { id; templateId; stepGroupId }
    let to_value x =
      structure_to_value
        [("id", (Some (StepId.to_value x.id)));
        ("templateId", (Some (TemplateId.to_value x.templateId)));
        ("stepGroupId", (Some (StepGroupId.to_value x.stepGroupId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stepGroupId =
        StepGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "stepGroupId") in
      let templateId =
        TemplateId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "templateId") in
      let id = StepId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~stepGroupId ~templateId ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let stepGroupId =
        field_map_exn json__ "stepGroupId" StepGroupId.of_json in
      let templateId = field_map_exn json__ "templateId" TemplateId.of_json in
      let id = field_map_exn json__ "id" StepId.of_json in
      make ~stepGroupId ~templateId ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Get a specific step in a template."]
module GetTemplateStepGroupResponse =
  struct
    type nonrec t =
      {
      templateId: String_.t option [@ocaml.doc "The ID of the template."];
      id: String_.t option [@ocaml.doc "The ID of the step group."];
      name: String_.t option [@ocaml.doc "The name of the step group."];
      description: String_.t option
        [@ocaml.doc "The description of the step group."];
      status: StepGroupStatus.t option
        [@ocaml.doc "The status of the step group."];
      creationTime: Timestamp.t option
        [@ocaml.doc "The time at which the step group was created."];
      lastModifiedTime: Timestamp.t option
        [@ocaml.doc "The time at which the step group was last modified."];
      tools: ToolsList.t option
        [@ocaml.doc "List of AWS services utilized in a migration workflow."];
      previous: StringList.t option [@ocaml.doc "The previous step group."];
      next: StringList.t option [@ocaml.doc "The next step group."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?templateId =
      fun ?id ->
        fun ?name ->
          fun ?description ->
            fun ?status ->
              fun ?creationTime ->
                fun ?lastModifiedTime ->
                  fun ?tools ->
                    fun ?previous ->
                      fun ?next ->
                        fun () ->
                          {
                            templateId;
                            id;
                            name;
                            description;
                            status;
                            creationTime;
                            lastModifiedTime;
                            tools;
                            previous;
                            next
                          }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("templateId", (Option.map x.templateId ~f:String_.to_value));
        ("id", (Option.map x.id ~f:String_.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("description", (Option.map x.description ~f:String_.to_value));
        ("status", (Option.map x.status ~f:StepGroupStatus.to_value));
        ("creationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("lastModifiedTime",
          (Option.map x.lastModifiedTime ~f:Timestamp.to_value));
        ("tools", (Option.map x.tools ~f:ToolsList.to_value));
        ("previous", (Option.map x.previous ~f:StringList.to_value));
        ("next", (Option.map x.next ~f:StringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let next =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "next") in
      let previous =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "previous") in
      let tools =
        (Option.map ~f:ToolsList.of_xml) (Xml.child xml_arg0 "tools") in
      let lastModifiedTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "lastModifiedTime") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "creationTime") in
      let status =
        (Option.map ~f:StepGroupStatus.of_xml) (Xml.child xml_arg0 "status") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let id = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "id") in
      let templateId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "templateId") in
      make ?next ?previous ?tools ?lastModifiedTime ?creationTime ?status
        ?description ?name ?id ?templateId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let next = field_map json__ "next" StringList.of_json in
      let previous = field_map json__ "previous" StringList.of_json in
      let tools = field_map json__ "tools" ToolsList.of_json in
      let lastModifiedTime =
        field_map json__ "lastModifiedTime" Timestamp.of_json in
      let creationTime = field_map json__ "creationTime" Timestamp.of_json in
      let status = field_map json__ "status" StepGroupStatus.of_json in
      let description = field_map json__ "description" String_.of_json in
      let name = field_map json__ "name" String_.of_json in
      let id = field_map json__ "id" String_.of_json in
      let templateId = field_map json__ "templateId" String_.of_json in
      make ?next ?previous ?tools ?lastModifiedTime ?creationTime ?status
        ?description ?name ?id ?templateId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Get a step group in a template."]
module GetTemplateStepGroupRequest =
  struct
    type nonrec t =
      {
      templateId: TemplateId.t [@ocaml.doc "The ID of the template."];
      id: StepGroupId.t [@ocaml.doc "The ID of the step group."]}
    let context_ = "GetTemplateStepGroupRequest"
    let make ~templateId = fun ~id -> fun () -> { templateId; id }
    let to_value x =
      structure_to_value
        [("templateId", (Some (TemplateId.to_value x.templateId)));
        ("id", (Some (StepGroupId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        StepGroupId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      let templateId =
        TemplateId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "templateId") in
      make ~id ~templateId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let id = field_map_exn json__ "id" StepGroupId.of_json in
      let templateId = field_map_exn json__ "templateId" TemplateId.of_json in
      make ~id ~templateId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Get a step group in a template."]
module GetMigrationWorkflowTemplateResponse =
  struct
    type nonrec t =
      {
      id: String_.t option [@ocaml.doc "The ID of the template."];
      templateArn: String_.t option
        [@ocaml.doc
          ">The Amazon Resource Name (ARN) of the migration workflow template. The format for an Migration Hub Orchestrator template ARN is arn:aws:migrationhub-orchestrator:region:account:template/template-abcd1234. For more information about ARNs, see Amazon Resource Names (ARNs) in the AWS General Reference."];
      name: String_.t option [@ocaml.doc "The name of the template."];
      description: String_.t option
        [@ocaml.doc "The time at which the template was last created."];
      inputs: TemplateInputList.t option
        [@ocaml.doc
          "The inputs provided for the creation of the migration workflow."];
      tools: ToolsList.t option
        [@ocaml.doc "List of AWS services utilized in a migration workflow."];
      creationTime: Timestamp.t option
        [@ocaml.doc "The time at which the template was last created."];
      owner: String_.t option
        [@ocaml.doc "The owner of the migration workflow template."];
      status: TemplateStatus.t option
        [@ocaml.doc "The status of the template."];
      statusMessage: String_.t option
        [@ocaml.doc
          "The status message of retrieving migration workflow templates."];
      templateClass: String_.t option
        [@ocaml.doc
          "The class of the migration workflow template. The available template classes are: A2C MGN SAP_MULTI SQL_EC2 SQL_RDS VMIE"];
      tags: StringMap.t option
        [@ocaml.doc "The tags added to the migration workflow template."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?id =
      fun ?templateArn ->
        fun ?name ->
          fun ?description ->
            fun ?inputs ->
              fun ?tools ->
                fun ?creationTime ->
                  fun ?owner ->
                    fun ?status ->
                      fun ?statusMessage ->
                        fun ?templateClass ->
                          fun ?tags ->
                            fun () ->
                              {
                                id;
                                templateArn;
                                name;
                                description;
                                inputs;
                                tools;
                                creationTime;
                                owner;
                                status;
                                statusMessage;
                                templateClass;
                                tags
                              }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:String_.to_value));
        ("templateArn", (Option.map x.templateArn ~f:String_.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("description", (Option.map x.description ~f:String_.to_value));
        ("inputs", (Option.map x.inputs ~f:TemplateInputList.to_value));
        ("tools", (Option.map x.tools ~f:ToolsList.to_value));
        ("creationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("owner", (Option.map x.owner ~f:String_.to_value));
        ("status", (Option.map x.status ~f:TemplateStatus.to_value));
        ("statusMessage", (Option.map x.statusMessage ~f:String_.to_value));
        ("templateClass", (Option.map x.templateClass ~f:String_.to_value));
        ("tags", (Option.map x.tags ~f:StringMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:StringMap.of_xml) (Xml.child xml_arg0 "tags") in
      let templateClass =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "templateClass") in
      let statusMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "statusMessage") in
      let status =
        (Option.map ~f:TemplateStatus.of_xml) (Xml.child xml_arg0 "status") in
      let owner = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "owner") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "creationTime") in
      let tools =
        (Option.map ~f:ToolsList.of_xml) (Xml.child xml_arg0 "tools") in
      let inputs =
        (Option.map ~f:TemplateInputList.of_xml)
          (Xml.child xml_arg0 "inputs") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let templateArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "templateArn") in
      let id = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "id") in
      make ?tags ?templateClass ?statusMessage ?status ?owner ?creationTime
        ?tools ?inputs ?description ?name ?templateArn ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" StringMap.of_json in
      let templateClass = field_map json__ "templateClass" String_.of_json in
      let statusMessage = field_map json__ "statusMessage" String_.of_json in
      let status = field_map json__ "status" TemplateStatus.of_json in
      let owner = field_map json__ "owner" String_.of_json in
      let creationTime = field_map json__ "creationTime" Timestamp.of_json in
      let tools = field_map json__ "tools" ToolsList.of_json in
      let inputs = field_map json__ "inputs" TemplateInputList.of_json in
      let description = field_map json__ "description" String_.of_json in
      let name = field_map json__ "name" String_.of_json in
      let templateArn = field_map json__ "templateArn" String_.of_json in
      let id = field_map json__ "id" String_.of_json in
      make ?tags ?templateClass ?statusMessage ?status ?owner ?creationTime
        ?tools ?inputs ?description ?name ?templateArn ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Get the template you want to use for creating a migration workflow."]
module GetMigrationWorkflowTemplateRequest =
  struct
    type nonrec t = {
      id: TemplateId.t [@ocaml.doc "The ID of the template."]}
    let context_ = "GetMigrationWorkflowTemplateRequest"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value [("id", (Some (TemplateId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        TemplateId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let id = field_map_exn json__ "id" TemplateId.of_json in make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Get the template you want to use for creating a migration workflow."]
module GetMigrationWorkflowResponse =
  struct
    type nonrec t =
      {
      id: MigrationWorkflowId.t option
        [@ocaml.doc "The ID of the migration workflow."];
      arn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the migration workflow."];
      name: String_.t option
        [@ocaml.doc "The name of the migration workflow."];
      description: String_.t option
        [@ocaml.doc "The description of the migration workflow."];
      templateId: String_.t option [@ocaml.doc "The ID of the template."];
      adsApplicationConfigurationId: String_.t option
        [@ocaml.doc
          "The configuration ID of the application configured in Application Discovery Service."];
      adsApplicationName: String_.t option
        [@ocaml.doc
          "The name of the application configured in Application Discovery Service."];
      status: MigrationWorkflowStatusEnum.t option
        [@ocaml.doc "The status of the migration workflow."];
      statusMessage: String_.t option
        [@ocaml.doc "The status message of the migration workflow."];
      creationTime: Timestamp.t option
        [@ocaml.doc "The time at which the migration workflow was created."];
      lastStartTime: Timestamp.t option
        [@ocaml.doc
          "The time at which the migration workflow was last started."];
      lastStopTime: Timestamp.t option
        [@ocaml.doc
          "The time at which the migration workflow was last stopped."];
      lastModifiedTime: Timestamp.t option
        [@ocaml.doc
          "The time at which the migration workflow was last modified."];
      endTime: Timestamp.t option
        [@ocaml.doc "The time at which the migration workflow ended."];
      tools: ToolsList.t option
        [@ocaml.doc "List of AWS services utilized in a migration workflow."];
      totalSteps: Integer.t option
        [@ocaml.doc "The total number of steps in the migration workflow."];
      completedSteps: Integer.t option
        [@ocaml.doc
          "Get a list of completed steps in the migration workflow."];
      workflowInputs: StepInputParameters.t option
        [@ocaml.doc
          "The inputs required for creating the migration workflow."];
      tags: StringMap.t option
        [@ocaml.doc "The tags added to the migration workflow."];
      workflowBucket: String_.t option
        [@ocaml.doc
          "The Amazon S3 bucket where the migration logs are stored."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?id =
      fun ?arn ->
        fun ?name ->
          fun ?description ->
            fun ?templateId ->
              fun ?adsApplicationConfigurationId ->
                fun ?adsApplicationName ->
                  fun ?status ->
                    fun ?statusMessage ->
                      fun ?creationTime ->
                        fun ?lastStartTime ->
                          fun ?lastStopTime ->
                            fun ?lastModifiedTime ->
                              fun ?endTime ->
                                fun ?tools ->
                                  fun ?totalSteps ->
                                    fun ?completedSteps ->
                                      fun ?workflowInputs ->
                                        fun ?tags ->
                                          fun ?workflowBucket ->
                                            fun () ->
                                              {
                                                id;
                                                arn;
                                                name;
                                                description;
                                                templateId;
                                                adsApplicationConfigurationId;
                                                adsApplicationName;
                                                status;
                                                statusMessage;
                                                creationTime;
                                                lastStartTime;
                                                lastStopTime;
                                                lastModifiedTime;
                                                endTime;
                                                tools;
                                                totalSteps;
                                                completedSteps;
                                                workflowInputs;
                                                tags;
                                                workflowBucket
                                              }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:MigrationWorkflowId.to_value));
        ("arn", (Option.map x.arn ~f:String_.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("description", (Option.map x.description ~f:String_.to_value));
        ("templateId", (Option.map x.templateId ~f:String_.to_value));
        ("adsApplicationConfigurationId",
          (Option.map x.adsApplicationConfigurationId ~f:String_.to_value));
        ("adsApplicationName",
          (Option.map x.adsApplicationName ~f:String_.to_value));
        ("status",
          (Option.map x.status ~f:MigrationWorkflowStatusEnum.to_value));
        ("statusMessage", (Option.map x.statusMessage ~f:String_.to_value));
        ("creationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("lastStartTime", (Option.map x.lastStartTime ~f:Timestamp.to_value));
        ("lastStopTime", (Option.map x.lastStopTime ~f:Timestamp.to_value));
        ("lastModifiedTime",
          (Option.map x.lastModifiedTime ~f:Timestamp.to_value));
        ("endTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("tools", (Option.map x.tools ~f:ToolsList.to_value));
        ("totalSteps", (Option.map x.totalSteps ~f:Integer.to_value));
        ("completedSteps", (Option.map x.completedSteps ~f:Integer.to_value));
        ("workflowInputs",
          (Option.map x.workflowInputs ~f:StepInputParameters.to_value));
        ("tags", (Option.map x.tags ~f:StringMap.to_value));
        ("workflowBucket", (Option.map x.workflowBucket ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let workflowBucket =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "workflowBucket") in
      let tags = (Option.map ~f:StringMap.of_xml) (Xml.child xml_arg0 "tags") in
      let workflowInputs =
        (Option.map ~f:StepInputParameters.of_xml)
          (Xml.child xml_arg0 "workflowInputs") in
      let completedSteps =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "completedSteps") in
      let totalSteps =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "totalSteps") in
      let tools =
        (Option.map ~f:ToolsList.of_xml) (Xml.child xml_arg0 "tools") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "endTime") in
      let lastModifiedTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "lastModifiedTime") in
      let lastStopTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastStopTime") in
      let lastStartTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastStartTime") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "creationTime") in
      let statusMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "statusMessage") in
      let status =
        (Option.map ~f:MigrationWorkflowStatusEnum.of_xml)
          (Xml.child xml_arg0 "status") in
      let adsApplicationName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "adsApplicationName") in
      let adsApplicationConfigurationId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "adsApplicationConfigurationId") in
      let templateId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "templateId") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "arn") in
      let id =
        (Option.map ~f:MigrationWorkflowId.of_xml) (Xml.child xml_arg0 "id") in
      make ?workflowBucket ?tags ?workflowInputs ?completedSteps ?totalSteps
        ?tools ?endTime ?lastModifiedTime ?lastStopTime ?lastStartTime
        ?creationTime ?statusMessage ?status ?adsApplicationName
        ?adsApplicationConfigurationId ?templateId ?description ?name ?arn
        ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let workflowBucket = field_map json__ "workflowBucket" String_.of_json in
      let tags = field_map json__ "tags" StringMap.of_json in
      let workflowInputs =
        field_map json__ "workflowInputs" StepInputParameters.of_json in
      let completedSteps = field_map json__ "completedSteps" Integer.of_json in
      let totalSteps = field_map json__ "totalSteps" Integer.of_json in
      let tools = field_map json__ "tools" ToolsList.of_json in
      let endTime = field_map json__ "endTime" Timestamp.of_json in
      let lastModifiedTime =
        field_map json__ "lastModifiedTime" Timestamp.of_json in
      let lastStopTime = field_map json__ "lastStopTime" Timestamp.of_json in
      let lastStartTime = field_map json__ "lastStartTime" Timestamp.of_json in
      let creationTime = field_map json__ "creationTime" Timestamp.of_json in
      let statusMessage = field_map json__ "statusMessage" String_.of_json in
      let status =
        field_map json__ "status" MigrationWorkflowStatusEnum.of_json in
      let adsApplicationName =
        field_map json__ "adsApplicationName" String_.of_json in
      let adsApplicationConfigurationId =
        field_map json__ "adsApplicationConfigurationId" String_.of_json in
      let templateId = field_map json__ "templateId" String_.of_json in
      let description = field_map json__ "description" String_.of_json in
      let name = field_map json__ "name" String_.of_json in
      let arn = field_map json__ "arn" String_.of_json in
      let id = field_map json__ "id" MigrationWorkflowId.of_json in
      make ?workflowBucket ?tags ?workflowInputs ?completedSteps ?totalSteps
        ?tools ?endTime ?lastModifiedTime ?lastStopTime ?lastStartTime
        ?creationTime ?statusMessage ?status ?adsApplicationName
        ?adsApplicationConfigurationId ?templateId ?description ?name ?arn
        ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Get migration workflow."]
module GetMigrationWorkflowRequest =
  struct
    type nonrec t =
      {
      id: MigrationWorkflowId.t
        [@ocaml.doc "The ID of the migration workflow."]}
    let context_ = "GetMigrationWorkflowRequest"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value [("id", (Some (MigrationWorkflowId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        MigrationWorkflowId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let id = field_map_exn json__ "id" MigrationWorkflowId.of_json in
      make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Get migration workflow."]
module DeleteWorkflowStepResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Delete a step in a migration workflow. Pause the workflow to delete a running step."]
module DeleteWorkflowStepRequest =
  struct
    type nonrec t =
      {
      id: StepId.t [@ocaml.doc "The ID of the step you want to delete."];
      stepGroupId: StepGroupId.t
        [@ocaml.doc
          "The ID of the step group that contains the step you want to delete."];
      workflowId: MigrationWorkflowId.t
        [@ocaml.doc "The ID of the migration workflow."]}
    let context_ = "DeleteWorkflowStepRequest"
    let make ~id =
      fun ~stepGroupId ->
        fun ~workflowId -> fun () -> { id; stepGroupId; workflowId }
    let to_value x =
      structure_to_value
        [("id", (Some (StepId.to_value x.id)));
        ("stepGroupId", (Some (StepGroupId.to_value x.stepGroupId)));
        ("workflowId", (Some (MigrationWorkflowId.to_value x.workflowId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let workflowId =
        MigrationWorkflowId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "workflowId") in
      let stepGroupId =
        StepGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "stepGroupId") in
      let id = StepId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~workflowId ~stepGroupId ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let workflowId =
        field_map_exn json__ "workflowId" MigrationWorkflowId.of_json in
      let stepGroupId =
        field_map_exn json__ "stepGroupId" StepGroupId.of_json in
      let id = field_map_exn json__ "id" StepId.of_json in
      make ~workflowId ~stepGroupId ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Delete a step in a migration workflow. Pause the workflow to delete a running step."]
module DeleteWorkflowStepGroupResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Delete a step group in a migration workflow."]
module DeleteWorkflowStepGroupRequest =
  struct
    type nonrec t =
      {
      workflowId: MigrationWorkflowId.t
        [@ocaml.doc "The ID of the migration workflow."];
      id: StepGroupId.t
        [@ocaml.doc "The ID of the step group you want to delete."]}
    let context_ = "DeleteWorkflowStepGroupRequest"
    let make ~workflowId = fun ~id -> fun () -> { workflowId; id }
    let to_value x =
      structure_to_value
        [("workflowId", (Some (MigrationWorkflowId.to_value x.workflowId)));
        ("id", (Some (StepGroupId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        StepGroupId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      let workflowId =
        MigrationWorkflowId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "workflowId") in
      make ~id ~workflowId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let id = field_map_exn json__ "id" StepGroupId.of_json in
      let workflowId =
        field_map_exn json__ "workflowId" MigrationWorkflowId.of_json in
      make ~id ~workflowId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Delete a step group in a migration workflow."]
module DeleteTemplateResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a migration workflow template."]
module DeleteTemplateRequest =
  struct
    type nonrec t =
      {
      id: TemplateId.t
        [@ocaml.doc
          "The ID of the request to delete a migration workflow template."]}
    let context_ = "DeleteTemplateRequest"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value [("id", (Some (TemplateId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        TemplateId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let id = field_map_exn json__ "id" TemplateId.of_json in make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes a migration workflow template."]
module DeleteMigrationWorkflowResponse =
  struct
    type nonrec t =
      {
      id: MigrationWorkflowId.t option
        [@ocaml.doc "The ID of the migration workflow."];
      arn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the migration workflow."];
      status: MigrationWorkflowStatusEnum.t option
        [@ocaml.doc "The status of the migration workflow."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?id = fun ?arn -> fun ?status -> fun () -> { id; arn; status }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:MigrationWorkflowId.to_value));
        ("arn", (Option.map x.arn ~f:String_.to_value));
        ("status",
          (Option.map x.status ~f:MigrationWorkflowStatusEnum.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:MigrationWorkflowStatusEnum.of_xml)
          (Xml.child xml_arg0 "status") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "arn") in
      let id =
        (Option.map ~f:MigrationWorkflowId.of_xml) (Xml.child xml_arg0 "id") in
      make ?status ?arn ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status =
        field_map json__ "status" MigrationWorkflowStatusEnum.of_json in
      let arn = field_map json__ "arn" String_.of_json in
      let id = field_map json__ "id" MigrationWorkflowId.of_json in
      make ?status ?arn ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Delete a migration workflow. You must pause a running workflow in Migration Hub Orchestrator console to delete it."]
module DeleteMigrationWorkflowRequest =
  struct
    type nonrec t =
      {
      id: MigrationWorkflowId.t
        [@ocaml.doc "The ID of the migration workflow you want to delete."]}
    let context_ = "DeleteMigrationWorkflowRequest"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value [("id", (Some (MigrationWorkflowId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        MigrationWorkflowId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let id = field_map_exn json__ "id" MigrationWorkflowId.of_json in
      make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Delete a migration workflow. You must pause a running workflow in Migration Hub Orchestrator console to delete it."]
module CreateWorkflowStepResponse =
  struct
    type nonrec t =
      {
      id: String_.t option [@ocaml.doc "The ID of the step."];
      stepGroupId: String_.t option [@ocaml.doc "The ID of the step group."];
      workflowId: String_.t option
        [@ocaml.doc "The ID of the migration workflow."];
      name: String_.t option [@ocaml.doc "The name of the step."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?id =
      fun ?stepGroupId ->
        fun ?workflowId ->
          fun ?name -> fun () -> { id; stepGroupId; workflowId; name }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:String_.to_value));
        ("stepGroupId", (Option.map x.stepGroupId ~f:String_.to_value));
        ("workflowId", (Option.map x.workflowId ~f:String_.to_value));
        ("name", (Option.map x.name ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let workflowId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "workflowId") in
      let stepGroupId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "stepGroupId") in
      let id = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "id") in
      make ?name ?workflowId ?stepGroupId ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let name = field_map json__ "name" String_.of_json in
      let workflowId = field_map json__ "workflowId" String_.of_json in
      let stepGroupId = field_map json__ "stepGroupId" String_.of_json in
      let id = field_map json__ "id" String_.of_json in
      make ?name ?workflowId ?stepGroupId ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Create a step in the migration workflow."]
module CreateWorkflowStepRequest =
  struct
    type nonrec t =
      {
      name: MigrationWorkflowName.t [@ocaml.doc "The name of the step."];
      stepGroupId: StepGroupId.t [@ocaml.doc "The ID of the step group."];
      workflowId: MigrationWorkflowId.t
        [@ocaml.doc "The ID of the migration workflow."];
      stepActionType: StepActionType.t
        [@ocaml.doc
          "The action type of the step. You must run and update the status of a manual step for the workflow to continue after the completion of the step."];
      description: MigrationWorkflowDescription.t option
        [@ocaml.doc "The description of the step."];
      workflowStepAutomationConfiguration:
        WorkflowStepAutomationConfiguration.t option
        [@ocaml.doc
          "The custom script to run tests on source or target environments."];
      stepTarget: StringList.t option
        [@ocaml.doc "The servers on which a step will be run."];
      outputs: WorkflowStepOutputList.t option
        [@ocaml.doc "The key value pairs added for the expected output."];
      previous: StringList.t option [@ocaml.doc "The previous step."];
      next: StringList.t option [@ocaml.doc "The next step."]}
    let context_ = "CreateWorkflowStepRequest"
    let make ?description =
      fun ?workflowStepAutomationConfiguration ->
        fun ?stepTarget ->
          fun ?outputs ->
            fun ?previous ->
              fun ?next ->
                fun ~name ->
                  fun ~stepGroupId ->
                    fun ~workflowId ->
                      fun ~stepActionType ->
                        fun () ->
                          {
                            description;
                            workflowStepAutomationConfiguration;
                            stepTarget;
                            outputs;
                            previous;
                            next;
                            name;
                            stepGroupId;
                            workflowId;
                            stepActionType
                          }
    let to_value x =
      structure_to_value
        [("name", (Some (MigrationWorkflowName.to_value x.name)));
        ("stepGroupId", (Some (StepGroupId.to_value x.stepGroupId)));
        ("workflowId", (Some (MigrationWorkflowId.to_value x.workflowId)));
        ("stepActionType", (Some (StepActionType.to_value x.stepActionType)));
        ("description",
          (Option.map x.description ~f:MigrationWorkflowDescription.to_value));
        ("workflowStepAutomationConfiguration",
          (Option.map x.workflowStepAutomationConfiguration
             ~f:WorkflowStepAutomationConfiguration.to_value));
        ("stepTarget", (Option.map x.stepTarget ~f:StringList.to_value));
        ("outputs",
          (Option.map x.outputs ~f:WorkflowStepOutputList.to_value));
        ("previous", (Option.map x.previous ~f:StringList.to_value));
        ("next", (Option.map x.next ~f:StringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let next =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "next") in
      let previous =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "previous") in
      let outputs =
        (Option.map ~f:WorkflowStepOutputList.of_xml)
          (Xml.child xml_arg0 "outputs") in
      let stepTarget =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "stepTarget") in
      let workflowStepAutomationConfiguration =
        (Option.map ~f:WorkflowStepAutomationConfiguration.of_xml)
          (Xml.child xml_arg0 "workflowStepAutomationConfiguration") in
      let description =
        (Option.map ~f:MigrationWorkflowDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let stepActionType =
        StepActionType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "stepActionType") in
      let workflowId =
        MigrationWorkflowId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "workflowId") in
      let stepGroupId =
        StepGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "stepGroupId") in
      let name =
        MigrationWorkflowName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?next ?previous ?outputs ?stepTarget
        ?workflowStepAutomationConfiguration ?description ~stepActionType
        ~workflowId ~stepGroupId ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let next = field_map json__ "next" StringList.of_json in
      let previous = field_map json__ "previous" StringList.of_json in
      let outputs = field_map json__ "outputs" WorkflowStepOutputList.of_json in
      let stepTarget = field_map json__ "stepTarget" StringList.of_json in
      let workflowStepAutomationConfiguration =
        field_map json__ "workflowStepAutomationConfiguration"
          WorkflowStepAutomationConfiguration.of_json in
      let description =
        field_map json__ "description" MigrationWorkflowDescription.of_json in
      let stepActionType =
        field_map_exn json__ "stepActionType" StepActionType.of_json in
      let workflowId =
        field_map_exn json__ "workflowId" MigrationWorkflowId.of_json in
      let stepGroupId =
        field_map_exn json__ "stepGroupId" StepGroupId.of_json in
      let name = field_map_exn json__ "name" MigrationWorkflowName.of_json in
      make ?next ?previous ?outputs ?stepTarget
        ?workflowStepAutomationConfiguration ?description ~stepActionType
        ~workflowId ~stepGroupId ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Create a step in the migration workflow."]
module CreateWorkflowStepGroupResponse =
  struct
    type nonrec t =
      {
      workflowId: String_.t option
        [@ocaml.doc
          "The ID of the migration workflow that contains the step group."];
      name: String_.t option [@ocaml.doc "The name of the step group."];
      id: String_.t option [@ocaml.doc "The ID of the step group."];
      description: String_.t option
        [@ocaml.doc "The description of the step group."];
      tools: ToolsList.t option
        [@ocaml.doc "List of AWS services utilized in a migration workflow."];
      next: StringList.t option [@ocaml.doc "The next step group."];
      previous: StringList.t option [@ocaml.doc "The previous step group."];
      creationTime: Timestamp.t option
        [@ocaml.doc "The time at which the step group is created."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?workflowId =
      fun ?name ->
        fun ?id ->
          fun ?description ->
            fun ?tools ->
              fun ?next ->
                fun ?previous ->
                  fun ?creationTime ->
                    fun () ->
                      {
                        workflowId;
                        name;
                        id;
                        description;
                        tools;
                        next;
                        previous;
                        creationTime
                      }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("workflowId", (Option.map x.workflowId ~f:String_.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("id", (Option.map x.id ~f:String_.to_value));
        ("description", (Option.map x.description ~f:String_.to_value));
        ("tools", (Option.map x.tools ~f:ToolsList.to_value));
        ("next", (Option.map x.next ~f:StringList.to_value));
        ("previous", (Option.map x.previous ~f:StringList.to_value));
        ("creationTime", (Option.map x.creationTime ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "creationTime") in
      let previous =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "previous") in
      let next =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "next") in
      let tools =
        (Option.map ~f:ToolsList.of_xml) (Xml.child xml_arg0 "tools") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      let id = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "id") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let workflowId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "workflowId") in
      make ?creationTime ?previous ?next ?tools ?description ?id ?name
        ?workflowId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let creationTime = field_map json__ "creationTime" Timestamp.of_json in
      let previous = field_map json__ "previous" StringList.of_json in
      let next = field_map json__ "next" StringList.of_json in
      let tools = field_map json__ "tools" ToolsList.of_json in
      let description = field_map json__ "description" String_.of_json in
      let id = field_map json__ "id" String_.of_json in
      let name = field_map json__ "name" String_.of_json in
      let workflowId = field_map json__ "workflowId" String_.of_json in
      make ?creationTime ?previous ?next ?tools ?description ?id ?name
        ?workflowId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Create a step group in a migration workflow."]
module CreateWorkflowStepGroupRequest =
  struct
    type nonrec t =
      {
      workflowId: MigrationWorkflowId.t
        [@ocaml.doc
          "The ID of the migration workflow that will contain the step group."];
      name: StepGroupName.t [@ocaml.doc "The name of the step group."];
      description: StepGroupDescription.t option
        [@ocaml.doc "The description of the step group."];
      next: StringList.t option [@ocaml.doc "The next step group."];
      previous: StringList.t option [@ocaml.doc "The previous step group."]}
    let context_ = "CreateWorkflowStepGroupRequest"
    let make ?description =
      fun ?next ->
        fun ?previous ->
          fun ~workflowId ->
            fun ~name ->
              fun () -> { description; next; previous; workflowId; name }
    let to_value x =
      structure_to_value
        [("workflowId", (Some (MigrationWorkflowId.to_value x.workflowId)));
        ("name", (Some (StepGroupName.to_value x.name)));
        ("description",
          (Option.map x.description ~f:StepGroupDescription.to_value));
        ("next", (Option.map x.next ~f:StringList.to_value));
        ("previous", (Option.map x.previous ~f:StringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let previous =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "previous") in
      let next =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "next") in
      let description =
        (Option.map ~f:StepGroupDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        StepGroupName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "name") in
      let workflowId =
        MigrationWorkflowId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "workflowId") in
      make ?previous ?next ?description ~name ~workflowId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let previous = field_map json__ "previous" StringList.of_json in
      let next = field_map json__ "next" StringList.of_json in
      let description =
        field_map json__ "description" StepGroupDescription.of_json in
      let name = field_map_exn json__ "name" StepGroupName.of_json in
      let workflowId =
        field_map_exn json__ "workflowId" MigrationWorkflowId.of_json in
      make ?previous ?next ?description ~name ~workflowId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Create a step group in a migration workflow."]
module CreateTemplateResponse =
  struct
    type nonrec t =
      {
      templateId: String_.t option
        [@ocaml.doc "The ID of the migration workflow template."];
      templateArn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the migration workflow template. The format for an Migration Hub Orchestrator template ARN is arn:aws:migrationhub-orchestrator:region:account:template/template-abcd1234. For more information about ARNs, see Amazon Resource Names (ARNs) in the AWS General Reference."];
      tags: StringMap.t option
        [@ocaml.doc "The tags added to the migration workflow template."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?templateId =
      fun ?templateArn ->
        fun ?tags -> fun () -> { templateId; templateArn; tags }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("templateId", (Option.map x.templateId ~f:String_.to_value));
        ("templateArn", (Option.map x.templateArn ~f:String_.to_value));
        ("tags", (Option.map x.tags ~f:StringMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:StringMap.of_xml) (Xml.child xml_arg0 "tags") in
      let templateArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "templateArn") in
      let templateId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "templateId") in
      make ?tags ?templateArn ?templateId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" StringMap.of_json in
      let templateArn = field_map json__ "templateArn" String_.of_json in
      let templateId = field_map json__ "templateId" String_.of_json in
      make ?tags ?templateArn ?templateId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Creates a migration workflow template."]
module CreateTemplateRequest =
  struct
    type nonrec t =
      {
      templateName: CreateTemplateRequestTemplateNameString.t
        [@ocaml.doc "The name of the migration workflow template."];
      templateDescription:
        CreateTemplateRequestTemplateDescriptionString.t option
        [@ocaml.doc "A description of the migration workflow template."];
      templateSource: TemplateSource.t
        [@ocaml.doc "The source of the migration workflow template."];
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. For more information, see Idempotency in the Smithy documentation."];
      tags: TagMap.t option
        [@ocaml.doc "The tags to add to the migration workflow template."]}
    let context_ = "CreateTemplateRequest"
    let make ?templateDescription =
      fun ?clientToken ->
        fun ?tags ->
          fun ~templateName ->
            fun ~templateSource ->
              fun () ->
                {
                  templateDescription;
                  clientToken;
                  tags;
                  templateName;
                  templateSource
                }
    let to_value x =
      structure_to_value
        [("templateName",
           (Some
              (CreateTemplateRequestTemplateNameString.to_value
                 x.templateName)));
        ("templateDescription",
          (Option.map x.templateDescription
             ~f:CreateTemplateRequestTemplateDescriptionString.to_value));
        ("templateSource", (Some (TemplateSource.to_value x.templateSource)));
        ("clientToken", (Option.map x.clientToken ~f:ClientToken.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "clientToken") in
      let templateSource =
        TemplateSource.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "templateSource") in
      let templateDescription =
        (Option.map ~f:CreateTemplateRequestTemplateDescriptionString.of_xml)
          (Xml.child xml_arg0 "templateDescription") in
      let templateName =
        CreateTemplateRequestTemplateNameString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "templateName") in
      make ?tags ?clientToken ~templateSource ?templateDescription
        ~templateName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in
      let clientToken = field_map json__ "clientToken" ClientToken.of_json in
      let templateSource =
        field_map_exn json__ "templateSource" TemplateSource.of_json in
      let templateDescription =
        field_map json__ "templateDescription"
          CreateTemplateRequestTemplateDescriptionString.of_json in
      let templateName =
        field_map_exn json__ "templateName"
          CreateTemplateRequestTemplateNameString.of_json in
      make ?tags ?clientToken ~templateSource ?templateDescription
        ~templateName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Creates a migration workflow template."]
module CreateMigrationWorkflowResponse =
  struct
    type nonrec t =
      {
      id: MigrationWorkflowId.t option
        [@ocaml.doc "The ID of the migration workflow."];
      arn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the migration workflow."];
      name: String_.t option
        [@ocaml.doc "The name of the migration workflow."];
      description: String_.t option
        [@ocaml.doc "The description of the migration workflow."];
      templateId: String_.t option [@ocaml.doc "The ID of the template."];
      adsApplicationConfigurationId: String_.t option
        [@ocaml.doc
          "The configuration ID of the application configured in Application Discovery Service."];
      workflowInputs: StepInputParameters.t option
        [@ocaml.doc "The inputs for creating a migration workflow."];
      stepTargets: StringList.t option
        [@ocaml.doc "The servers on which a step will be run."];
      status: MigrationWorkflowStatusEnum.t option
        [@ocaml.doc "The status of the migration workflow."];
      creationTime: Timestamp.t option
        [@ocaml.doc "The time at which the migration workflow was created."];
      tags: StringMap.t option
        [@ocaml.doc "The tags to add on a migration workflow."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?id =
      fun ?arn ->
        fun ?name ->
          fun ?description ->
            fun ?templateId ->
              fun ?adsApplicationConfigurationId ->
                fun ?workflowInputs ->
                  fun ?stepTargets ->
                    fun ?status ->
                      fun ?creationTime ->
                        fun ?tags ->
                          fun () ->
                            {
                              id;
                              arn;
                              name;
                              description;
                              templateId;
                              adsApplicationConfigurationId;
                              workflowInputs;
                              stepTargets;
                              status;
                              creationTime;
                              tags
                            }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:MigrationWorkflowId.to_value));
        ("arn", (Option.map x.arn ~f:String_.to_value));
        ("name", (Option.map x.name ~f:String_.to_value));
        ("description", (Option.map x.description ~f:String_.to_value));
        ("templateId", (Option.map x.templateId ~f:String_.to_value));
        ("adsApplicationConfigurationId",
          (Option.map x.adsApplicationConfigurationId ~f:String_.to_value));
        ("workflowInputs",
          (Option.map x.workflowInputs ~f:StepInputParameters.to_value));
        ("stepTargets", (Option.map x.stepTargets ~f:StringList.to_value));
        ("status",
          (Option.map x.status ~f:MigrationWorkflowStatusEnum.to_value));
        ("creationTime", (Option.map x.creationTime ~f:Timestamp.to_value));
        ("tags", (Option.map x.tags ~f:StringMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:StringMap.of_xml) (Xml.child xml_arg0 "tags") in
      let creationTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "creationTime") in
      let status =
        (Option.map ~f:MigrationWorkflowStatusEnum.of_xml)
          (Xml.child xml_arg0 "status") in
      let stepTargets =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "stepTargets") in
      let workflowInputs =
        (Option.map ~f:StepInputParameters.of_xml)
          (Xml.child xml_arg0 "workflowInputs") in
      let adsApplicationConfigurationId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "adsApplicationConfigurationId") in
      let templateId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "templateId") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "arn") in
      let id =
        (Option.map ~f:MigrationWorkflowId.of_xml) (Xml.child xml_arg0 "id") in
      make ?tags ?creationTime ?status ?stepTargets ?workflowInputs
        ?adsApplicationConfigurationId ?templateId ?description ?name ?arn
        ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" StringMap.of_json in
      let creationTime = field_map json__ "creationTime" Timestamp.of_json in
      let status =
        field_map json__ "status" MigrationWorkflowStatusEnum.of_json in
      let stepTargets = field_map json__ "stepTargets" StringList.of_json in
      let workflowInputs =
        field_map json__ "workflowInputs" StepInputParameters.of_json in
      let adsApplicationConfigurationId =
        field_map json__ "adsApplicationConfigurationId" String_.of_json in
      let templateId = field_map json__ "templateId" String_.of_json in
      let description = field_map json__ "description" String_.of_json in
      let name = field_map json__ "name" String_.of_json in
      let arn = field_map json__ "arn" String_.of_json in
      let id = field_map json__ "id" MigrationWorkflowId.of_json in
      make ?tags ?creationTime ?status ?stepTargets ?workflowInputs
        ?adsApplicationConfigurationId ?templateId ?description ?name ?arn
        ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Create a workflow to orchestrate your migrations."]
module CreateMigrationWorkflowRequest =
  struct
    type nonrec t =
      {
      name: CreateMigrationWorkflowRequestNameString.t
        [@ocaml.doc "The name of the migration workflow."];
      description: CreateMigrationWorkflowRequestDescriptionString.t option
        [@ocaml.doc "The description of the migration workflow."];
      templateId: CreateMigrationWorkflowRequestTemplateIdString.t
        [@ocaml.doc "The ID of the template."];
      applicationConfigurationId:
        CreateMigrationWorkflowRequestApplicationConfigurationIdString.t
          option
        [@ocaml.doc
          "The configuration ID of the application configured in Application Discovery Service."];
      inputParameters: StepInputParameters.t
        [@ocaml.doc
          "The input parameters required to create a migration workflow."];
      stepTargets: StringList.t option
        [@ocaml.doc "The servers on which a step will be run."];
      tags: StringMap.t option
        [@ocaml.doc "The tags to add on a migration workflow."]}
    let context_ = "CreateMigrationWorkflowRequest"
    let make ?description =
      fun ?applicationConfigurationId ->
        fun ?stepTargets ->
          fun ?tags ->
            fun ~name ->
              fun ~templateId ->
                fun ~inputParameters ->
                  fun () ->
                    {
                      description;
                      applicationConfigurationId;
                      stepTargets;
                      tags;
                      name;
                      templateId;
                      inputParameters
                    }
    let to_value x =
      structure_to_value
        [("name",
           (Some (CreateMigrationWorkflowRequestNameString.to_value x.name)));
        ("description",
          (Option.map x.description
             ~f:CreateMigrationWorkflowRequestDescriptionString.to_value));
        ("templateId",
          (Some
             (CreateMigrationWorkflowRequestTemplateIdString.to_value
                x.templateId)));
        ("applicationConfigurationId",
          (Option.map x.applicationConfigurationId
             ~f:CreateMigrationWorkflowRequestApplicationConfigurationIdString.to_value));
        ("inputParameters",
          (Some (StepInputParameters.to_value x.inputParameters)));
        ("stepTargets", (Option.map x.stepTargets ~f:StringList.to_value));
        ("tags", (Option.map x.tags ~f:StringMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:StringMap.of_xml) (Xml.child xml_arg0 "tags") in
      let stepTargets =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "stepTargets") in
      let inputParameters =
        StepInputParameters.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "inputParameters") in
      let applicationConfigurationId =
        (Option.map
           ~f:CreateMigrationWorkflowRequestApplicationConfigurationIdString.of_xml)
          (Xml.child xml_arg0 "applicationConfigurationId") in
      let templateId =
        CreateMigrationWorkflowRequestTemplateIdString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "templateId") in
      let description =
        (Option.map ~f:CreateMigrationWorkflowRequestDescriptionString.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        CreateMigrationWorkflowRequestNameString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ?tags ?stepTargets ~inputParameters ?applicationConfigurationId
        ~templateId ?description ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" StringMap.of_json in
      let stepTargets = field_map json__ "stepTargets" StringList.of_json in
      let inputParameters =
        field_map_exn json__ "inputParameters" StepInputParameters.of_json in
      let applicationConfigurationId =
        field_map json__ "applicationConfigurationId"
          CreateMigrationWorkflowRequestApplicationConfigurationIdString.of_json in
      let templateId =
        field_map_exn json__ "templateId"
          CreateMigrationWorkflowRequestTemplateIdString.of_json in
      let description =
        field_map json__ "description"
          CreateMigrationWorkflowRequestDescriptionString.of_json in
      let name =
        field_map_exn json__ "name"
          CreateMigrationWorkflowRequestNameString.of_json in
      make ?tags ?stepTargets ~inputParameters ?applicationConfigurationId
        ~templateId ?description ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Create a workflow to orchestrate your migrations."]
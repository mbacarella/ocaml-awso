(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateDescription =
         flag "template-description" (optional string)
           ~doc:"STRING CreateTemplateRequestTemplateDescriptionString"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and templateName =
         flag "template-name" (required string)
           ~doc:"STRING CreateTemplateRequestTemplateNameString"
       and templateSource =
         flag "template-source" (required json_arg)
           ~doc:"JSON TemplateSource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_template
           (Values.CreateTemplateRequest.make ?templateDescription
              ?clientToken ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~templateName
              ~templateSource:(Values.TemplateSource.of_json templateSource)
              ()) (Some Values.CreateTemplateResponse.to_json)
           (Some Values.CreateTemplateResponse.error_to_json)])
let create_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING CreateMigrationWorkflowRequestDescriptionString"
       and applicationConfigurationId =
         flag "application-configuration-id" (optional string)
           ~doc:"STRING CreateMigrationWorkflowRequestApplicationConfigurationIdString"
       and stepTargets =
         flag "step-targets" (optional json_arg) ~doc:"JSON StringList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON StringMap"
       and name =
         flag "name" (required string)
           ~doc:"STRING CreateMigrationWorkflowRequestNameString"
       and templateId =
         flag "template-id" (required string)
           ~doc:"STRING CreateMigrationWorkflowRequestTemplateIdString"
       and inputParameters =
         flag "input-parameters" (required json_arg)
           ~doc:"JSON StepInputParameters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_workflow
           (Values.CreateMigrationWorkflowRequest.make ?description
              ?applicationConfigurationId
              ?stepTargets:(Option.map ~f:Values.StringList.of_json
                              stepTargets)
              ?tags:(Option.map ~f:Values.StringMap.of_json tags) ~name
              ~templateId
              ~inputParameters:(Values.StepInputParameters.of_json
                                  inputParameters) ())
           (Some Values.CreateMigrationWorkflowResponse.to_json)
           (Some Values.CreateMigrationWorkflowResponse.error_to_json)])
let create_workflow_step =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING MigrationWorkflowDescription"
       and workflowStepAutomationConfiguration =
         flag "workflow-step-automation-configuration" (optional json_arg)
           ~doc:"JSON WorkflowStepAutomationConfiguration"
       and stepTarget =
         flag "step-target" (optional json_arg) ~doc:"JSON StringList"
       and outputs =
         flag "outputs" (optional json_arg)
           ~doc:"JSON WorkflowStepOutputList"
       and previous =
         flag "previous" (optional json_arg) ~doc:"JSON StringList"
       and next = flag "next" (optional json_arg) ~doc:"JSON StringList"
       and name =
         flag "name" (required string) ~doc:"STRING MigrationWorkflowName"
       and stepGroupId =
         flag "step-group-id" (required string) ~doc:"STRING StepGroupId"
       and workflowId =
         flag "workflow-id" (required string)
           ~doc:"STRING MigrationWorkflowId"
       and stepActionType =
         flag "step-action-type" (required json_arg)
           ~doc:"JSON StepActionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_workflow_step
           (Values.CreateWorkflowStepRequest.make ?description
              ?workflowStepAutomationConfiguration:(Option.map
                                                      ~f:Values.WorkflowStepAutomationConfiguration.of_json
                                                      workflowStepAutomationConfiguration)
              ?stepTarget:(Option.map ~f:Values.StringList.of_json stepTarget)
              ?outputs:(Option.map ~f:Values.WorkflowStepOutputList.of_json
                          outputs)
              ?previous:(Option.map ~f:Values.StringList.of_json previous)
              ?next:(Option.map ~f:Values.StringList.of_json next) ~name
              ~stepGroupId ~workflowId
              ~stepActionType:(Values.StepActionType.of_json stepActionType)
              ()) (Some Values.CreateWorkflowStepResponse.to_json)
           (Some Values.CreateWorkflowStepResponse.error_to_json)])
let create_workflow_step_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING StepGroupDescription"
       and next = flag "next" (optional json_arg) ~doc:"JSON StringList"
       and previous =
         flag "previous" (optional json_arg) ~doc:"JSON StringList"
       and workflowId =
         flag "workflow-id" (required string)
           ~doc:"STRING MigrationWorkflowId"
       and name = flag "name" (required string) ~doc:"STRING StepGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_workflow_step_group
           (Values.CreateWorkflowStepGroupRequest.make ?description
              ?next:(Option.map ~f:Values.StringList.of_json next)
              ?previous:(Option.map ~f:Values.StringList.of_json previous)
              ~workflowId ~name ())
           (Some Values.CreateWorkflowStepGroupResponse.to_json)
           (Some Values.CreateWorkflowStepGroupResponse.error_to_json)])
let delete_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING TemplateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_template (Values.DeleteTemplateRequest.make ~id ())
           (Some Values.DeleteTemplateResponse.to_json)
           (Some Values.DeleteTemplateResponse.error_to_json)])
let delete_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING MigrationWorkflowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_workflow
           (Values.DeleteMigrationWorkflowRequest.make ~id ())
           (Some Values.DeleteMigrationWorkflowResponse.to_json)
           (Some Values.DeleteMigrationWorkflowResponse.error_to_json)])
let delete_workflow_step =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING StepId"
       and stepGroupId =
         flag "step-group-id" (required string) ~doc:"STRING StepGroupId"
       and workflowId =
         flag "workflow-id" (required string)
           ~doc:"STRING MigrationWorkflowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_workflow_step
           (Values.DeleteWorkflowStepRequest.make ~id ~stepGroupId
              ~workflowId ())
           (Some Values.DeleteWorkflowStepResponse.to_json)
           (Some Values.DeleteWorkflowStepResponse.error_to_json)])
let delete_workflow_step_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workflowId =
         flag "workflow-id" (required string)
           ~doc:"STRING MigrationWorkflowId"
       and id = flag "id" (required string) ~doc:"STRING StepGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_workflow_step_group
           (Values.DeleteWorkflowStepGroupRequest.make ~workflowId ~id ())
           (Some Values.DeleteWorkflowStepGroupResponse.to_json)
           (Some Values.DeleteWorkflowStepGroupResponse.error_to_json)])
let get_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING TemplateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_template
           (Values.GetMigrationWorkflowTemplateRequest.make ~id ())
           (Some Values.GetMigrationWorkflowTemplateResponse.to_json)
           (Some Values.GetMigrationWorkflowTemplateResponse.error_to_json)])
let get_template_step =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING StepId"
       and templateId =
         flag "template-id" (required string) ~doc:"STRING TemplateId"
       and stepGroupId =
         flag "step-group-id" (required string) ~doc:"STRING StepGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_template_step
           (Values.GetTemplateStepRequest.make ~id ~templateId ~stepGroupId
              ()) (Some Values.GetTemplateStepResponse.to_json)
           (Some Values.GetTemplateStepResponse.error_to_json)])
let get_template_step_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateId =
         flag "template-id" (required string) ~doc:"STRING TemplateId"
       and id = flag "id" (required string) ~doc:"STRING StepGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_template_step_group
           (Values.GetTemplateStepGroupRequest.make ~templateId ~id ())
           (Some Values.GetTemplateStepGroupResponse.to_json)
           (Some Values.GetTemplateStepGroupResponse.error_to_json)])
let get_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING MigrationWorkflowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_workflow (Values.GetMigrationWorkflowRequest.make ~id ())
           (Some Values.GetMigrationWorkflowResponse.to_json)
           (Some Values.GetMigrationWorkflowResponse.error_to_json)])
let get_workflow_step =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workflowId =
         flag "workflow-id" (required string)
           ~doc:"STRING MigrationWorkflowId"
       and stepGroupId =
         flag "step-group-id" (required string) ~doc:"STRING StepGroupId"
       and id = flag "id" (required string) ~doc:"STRING StepId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_workflow_step
           (Values.GetWorkflowStepRequest.make ~workflowId ~stepGroupId ~id
              ()) (Some Values.GetWorkflowStepResponse.to_json)
           (Some Values.GetWorkflowStepResponse.error_to_json)])
let get_workflow_step_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING StepGroupId"
       and workflowId =
         flag "workflow-id" (required string)
           ~doc:"STRING MigrationWorkflowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_workflow_step_group
           (Values.GetWorkflowStepGroupRequest.make ~id ~workflowId ())
           (Some Values.GetWorkflowStepGroupResponse.to_json)
           (Some Values.GetWorkflowStepGroupResponse.error_to_json)])
let list_plugins =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_plugins
           (Values.ListPluginsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListPluginsResponse.to_json)
           (Some Values.ListPluginsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_template_step_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and templateId =
         flag "template-id" (required string) ~doc:"STRING TemplateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_template_step_groups
           (Values.ListTemplateStepGroupsRequest.make ?maxResults ?nextToken
              ~templateId ())
           (Some Values.ListTemplateStepGroupsResponse.to_json)
           (Some Values.ListTemplateStepGroupsResponse.error_to_json)])
let list_template_steps =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and templateId =
         flag "template-id" (required string) ~doc:"STRING TemplateId"
       and stepGroupId =
         flag "step-group-id" (required string) ~doc:"STRING StepGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_template_steps
           (Values.ListTemplateStepsRequest.make ?maxResults ?nextToken
              ~templateId ~stepGroupId ())
           (Some Values.ListTemplateStepsResponse.to_json)
           (Some Values.ListTemplateStepsResponse.error_to_json)])
let list_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and name = flag "name" (optional string) ~doc:"STRING TemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_templates
           (Values.ListMigrationWorkflowTemplatesRequest.make ?maxResults
              ?nextToken ?name ())
           (Some Values.ListMigrationWorkflowTemplatesResponse.to_json)
           (Some Values.ListMigrationWorkflowTemplatesResponse.error_to_json)])
let list_workflow_step_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and workflowId =
         flag "workflow-id" (required string)
           ~doc:"STRING MigrationWorkflowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_workflow_step_groups
           (Values.ListWorkflowStepGroupsRequest.make ?nextToken ?maxResults
              ~workflowId ())
           (Some Values.ListWorkflowStepGroupsResponse.to_json)
           (Some Values.ListWorkflowStepGroupsResponse.error_to_json)])
let list_workflow_steps =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and workflowId =
         flag "workflow-id" (required string)
           ~doc:"STRING MigrationWorkflowId"
       and stepGroupId =
         flag "step-group-id" (required string) ~doc:"STRING StepGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_workflow_steps
           (Values.ListWorkflowStepsRequest.make ?nextToken ?maxResults
              ~workflowId ~stepGroupId ())
           (Some Values.ListWorkflowStepsResponse.to_json)
           (Some Values.ListWorkflowStepsResponse.error_to_json)])
let list_workflows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and templateId =
         flag "template-id" (optional string) ~doc:"STRING TemplateId"
       and adsApplicationConfigurationName =
         flag "ads-application-configuration-name" (optional string)
           ~doc:"STRING ApplicationConfigurationName"
       and status =
         flag "status" (optional json_arg)
           ~doc:"JSON MigrationWorkflowStatusEnum"
       and name = flag "name" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_workflows
           (Values.ListMigrationWorkflowsRequest.make ?maxResults ?nextToken
              ?templateId ?adsApplicationConfigurationName
              ?status:(Option.map
                         ~f:Values.MigrationWorkflowStatusEnum.of_json status)
              ?name ()) (Some Values.ListMigrationWorkflowsResponse.to_json)
           (Some Values.ListMigrationWorkflowsResponse.error_to_json)])
let retry_workflow_step =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workflowId =
         flag "workflow-id" (required string)
           ~doc:"STRING MigrationWorkflowId"
       and stepGroupId =
         flag "step-group-id" (required string) ~doc:"STRING StepGroupId"
       and id = flag "id" (required string) ~doc:"STRING StepId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.retry_workflow_step
           (Values.RetryWorkflowStepRequest.make ~workflowId ~stepGroupId ~id
              ()) (Some Values.RetryWorkflowStepResponse.to_json)
           (Some Values.RetryWorkflowStepResponse.error_to_json)])
let start_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING MigrationWorkflowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_workflow
           (Values.StartMigrationWorkflowRequest.make ~id ())
           (Some Values.StartMigrationWorkflowResponse.to_json)
           (Some Values.StartMigrationWorkflowResponse.error_to_json)])
let stop_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING MigrationWorkflowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_workflow (Values.StopMigrationWorkflowRequest.make ~id ())
           (Some Values.StopMigrationWorkflowResponse.to_json)
           (Some Values.StopMigrationWorkflowResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (optional string)
           ~doc:"STRING UpdateTemplateRequestTemplateNameString"
       and templateDescription =
         flag "template-description" (optional string)
           ~doc:"STRING UpdateTemplateRequestTemplateDescriptionString"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and id = flag "id" (required string) ~doc:"STRING TemplateId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_template
           (Values.UpdateTemplateRequest.make ?templateName
              ?templateDescription ?clientToken ~id ())
           (Some Values.UpdateTemplateResponse.to_json)
           (Some Values.UpdateTemplateResponse.error_to_json)])
let update_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string)
           ~doc:"STRING UpdateMigrationWorkflowRequestNameString"
       and description =
         flag "description" (optional string)
           ~doc:"STRING UpdateMigrationWorkflowRequestDescriptionString"
       and inputParameters =
         flag "input-parameters" (optional json_arg)
           ~doc:"JSON StepInputParameters"
       and stepTargets =
         flag "step-targets" (optional json_arg) ~doc:"JSON StringList"
       and id = flag "id" (required string) ~doc:"STRING MigrationWorkflowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_workflow
           (Values.UpdateMigrationWorkflowRequest.make ?name ?description
              ?inputParameters:(Option.map
                                  ~f:Values.StepInputParameters.of_json
                                  inputParameters)
              ?stepTargets:(Option.map ~f:Values.StringList.of_json
                              stepTargets) ~id ())
           (Some Values.UpdateMigrationWorkflowResponse.to_json)
           (Some Values.UpdateMigrationWorkflowResponse.error_to_json)])
let update_workflow_step =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING StepName"
       and description =
         flag "description" (optional string) ~doc:"STRING StepDescription"
       and stepActionType =
         flag "step-action-type" (optional json_arg)
           ~doc:"JSON StepActionType"
       and workflowStepAutomationConfiguration =
         flag "workflow-step-automation-configuration" (optional json_arg)
           ~doc:"JSON WorkflowStepAutomationConfiguration"
       and stepTarget =
         flag "step-target" (optional json_arg) ~doc:"JSON StringList"
       and outputs =
         flag "outputs" (optional json_arg)
           ~doc:"JSON WorkflowStepOutputList"
       and previous =
         flag "previous" (optional json_arg) ~doc:"JSON StringList"
       and next = flag "next" (optional json_arg) ~doc:"JSON StringList"
       and status = flag "status" (optional json_arg) ~doc:"JSON StepStatus"
       and id = flag "id" (required string) ~doc:"STRING StepId"
       and stepGroupId =
         flag "step-group-id" (required string) ~doc:"STRING StepGroupId"
       and workflowId =
         flag "workflow-id" (required string)
           ~doc:"STRING MigrationWorkflowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_workflow_step
           (Values.UpdateWorkflowStepRequest.make ?name ?description
              ?stepActionType:(Option.map ~f:Values.StepActionType.of_json
                                 stepActionType)
              ?workflowStepAutomationConfiguration:(Option.map
                                                      ~f:Values.WorkflowStepAutomationConfiguration.of_json
                                                      workflowStepAutomationConfiguration)
              ?stepTarget:(Option.map ~f:Values.StringList.of_json stepTarget)
              ?outputs:(Option.map ~f:Values.WorkflowStepOutputList.of_json
                          outputs)
              ?previous:(Option.map ~f:Values.StringList.of_json previous)
              ?next:(Option.map ~f:Values.StringList.of_json next)
              ?status:(Option.map ~f:Values.StepStatus.of_json status) ~id
              ~stepGroupId ~workflowId ())
           (Some Values.UpdateWorkflowStepResponse.to_json)
           (Some Values.UpdateWorkflowStepResponse.error_to_json)])
let update_workflow_step_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING StepGroupName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING StepGroupDescription"
       and next = flag "next" (optional json_arg) ~doc:"JSON StringList"
       and previous =
         flag "previous" (optional json_arg) ~doc:"JSON StringList"
       and workflowId =
         flag "workflow-id" (required string)
           ~doc:"STRING MigrationWorkflowId"
       and id = flag "id" (required string) ~doc:"STRING StepGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_workflow_step_group
           (Values.UpdateWorkflowStepGroupRequest.make ?name ?description
              ?next:(Option.map ~f:Values.StringList.of_json next)
              ?previous:(Option.map ~f:Values.StringList.of_json previous)
              ~workflowId ~id ())
           (Some Values.UpdateWorkflowStepGroupResponse.to_json)
           (Some Values.UpdateWorkflowStepGroupResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-template", create_template);
    ("create-workflow", create_workflow);
    ("create-workflow-step", create_workflow_step);
    ("create-workflow-step-group", create_workflow_step_group);
    ("delete-template", delete_template);
    ("delete-workflow", delete_workflow);
    ("delete-workflow-step", delete_workflow_step);
    ("delete-workflow-step-group", delete_workflow_step_group);
    ("get-template", get_template);
    ("get-template-step", get_template_step);
    ("get-template-step-group", get_template_step_group);
    ("get-workflow", get_workflow);
    ("get-workflow-step", get_workflow_step);
    ("get-workflow-step-group", get_workflow_step_group);
    ("list-plugins", list_plugins);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-template-step-groups", list_template_step_groups);
    ("list-template-steps", list_template_steps);
    ("list-templates", list_templates);
    ("list-workflow-step-groups", list_workflow_step_groups);
    ("list-workflow-steps", list_workflow_steps);
    ("list-workflows", list_workflows);
    ("retry-workflow-step", retry_workflow_step);
    ("start-workflow", start_workflow);
    ("stop-workflow", stop_workflow);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-template", update_template);
    ("update-workflow", update_workflow);
    ("update-workflow-step", update_workflow_step);
    ("update-workflow-step-group", update_workflow_step_group)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | BatchGetFindings: (BatchGetFindingsRequest.t, BatchGetFindingsResponse.t,
  BatchGetFindingsResponse.error) t 
  | CreateScan: (CreateScanRequest.t, CreateScanResponse.t,
  CreateScanResponse.error) t 
  | CreateUploadUrl: (CreateUploadUrlRequest.t, CreateUploadUrlResponse.t,
  CreateUploadUrlResponse.error) t 
  | GetAccountConfiguration: (GetAccountConfigurationRequest.t,
  GetAccountConfigurationResponse.t, GetAccountConfigurationResponse.error) t
  
  | GetFindings: (GetFindingsRequest.t, GetFindingsResponse.t,
  GetFindingsResponse.error) t 
  | GetMetricsSummary: (GetMetricsSummaryRequest.t,
  GetMetricsSummaryResponse.t, GetMetricsSummaryResponse.error) t 
  | GetScan: (GetScanRequest.t, GetScanResponse.t, GetScanResponse.error) t 
  | ListFindingsMetrics: (ListFindingsMetricsRequest.t,
  ListFindingsMetricsResponse.t, ListFindingsMetricsResponse.error) t 
  | ListScans: (ListScansRequest.t, ListScansResponse.t,
  ListScansResponse.error) t 
  | ListTagsForResource: (ListTagsForResourceRequest.t,
  ListTagsForResourceResponse.t, ListTagsForResourceResponse.error) t 
  | TagResource: (TagResourceRequest.t, TagResourceResponse.t,
  TagResourceResponse.error) t 
  | UntagResource: (UntagResourceRequest.t, UntagResourceResponse.t,
  UntagResourceResponse.error) t 
  | UpdateAccountConfiguration: (UpdateAccountConfigurationRequest.t,
  UpdateAccountConfigurationResponse.t,
  UpdateAccountConfigurationResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | BatchGetFindings -> `POST
  | CreateScan -> `POST
  | CreateUploadUrl -> `POST
  | GetAccountConfiguration -> `GET
  | GetFindings -> `GET
  | GetMetricsSummary -> `GET
  | GetScan -> `GET
  | ListFindingsMetrics -> `GET
  | ListScans -> `GET
  | ListTagsForResource -> `GET
  | TagResource -> `POST
  | UntagResource -> `DELETE
  | UpdateAccountConfiguration -> `PUT
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | BatchGetFindings ->
          (Format.kasprintf Uri.of_string) "/batchGetFindings"
      | CreateScan -> (Format.kasprintf Uri.of_string) "/scans"
      | CreateUploadUrl -> (Format.kasprintf Uri.of_string) "/uploadUrl"
      | GetAccountConfiguration ->
          (Format.kasprintf Uri.of_string) "/accountConfiguration/get"
      | GetFindings ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/findings/%s"
               (ScanName.to_header x.GetFindingsRequest.scanName))
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("nextToken", (NextToken.to_header v)))
                  x.nextToken;
               Option.map
                 ~f:(fun v ->
                       ("maxResults",
                         (GetFindingsRequestMaxResultsInteger.to_header v)))
                 x.maxResults;
               Option.map ~f:(fun v -> ("status", (Status.to_header v)))
                 x.status])
      | GetMetricsSummary ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/metrics/summary")
            (List.filter_opt [Some ("date", (Timestamp.to_header x.date))])
      | GetScan ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/scans/%s"
               (ScanName.to_header x.GetScanRequest.scanName))
            (List.filter_opt
               [Option.map ~f:(fun v -> ("runId", (Uuid.to_header v)))
                  x.runId])
      | ListFindingsMetrics ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/metrics/findings")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("nextToken", (NextToken.to_header v)))
                  x.nextToken;
               Option.map
                 ~f:(fun v ->
                       ("maxResults",
                         (ListFindingsMetricsRequestMaxResultsInteger.to_header
                            v))) x.maxResults;
               Some ("startDate", (Timestamp.to_header x.startDate));
               Some ("endDate", (Timestamp.to_header x.endDate))])
      | ListScans ->
          Uri.add_query_params' ((Format.kasprintf Uri.of_string) "/scans")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("nextToken", (NextToken.to_header v)))
                  x.nextToken;
               Option.map
                 ~f:(fun v ->
                       ("maxResults",
                         (ListScansRequestMaxResultsInteger.to_header v)))
                 x.maxResults])
      | ListTagsForResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (ScanNameArn.to_header x.ListTagsForResourceRequest.resourceArn)
      | TagResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (ScanNameArn.to_header x.TagResourceRequest.resourceArn)
      | UntagResource ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/tags/%s"
               (ScanNameArn.to_header x.UntagResourceRequest.resourceArn))
            (List.filter_opt
               [Some ("tagKeys", (TagKeyList.to_header x.tagKeys))])
      | UpdateAccountConfiguration ->
          (Format.kasprintf Uri.of_string) "/updateAccountConfiguration")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | BatchGetFindings ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("findingIdentifiers",
                           (FindingIdentifiers.to_value
                              req.BatchGetFindingsRequest.findingIdentifiers))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CreateScan ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.CreateScanRequest.clientToken
                         ~f:(fun x ->
                               ("clientToken", (ClientToken.to_value x)));
                      Some
                        ("resourceId",
                          (ResourceId.to_value
                             req.CreateScanRequest.resourceId));
                      Some
                        ("scanName",
                          (ScanName.to_value req.CreateScanRequest.scanName));
                      Option.map req.CreateScanRequest.scanType
                        ~f:(fun x -> ("scanType", (ScanType.to_value x)));
                      Option.map req.CreateScanRequest.analysisType
                        ~f:(fun x ->
                              ("analysisType", (AnalysisType.to_value x)));
                      Option.map req.CreateScanRequest.tags
                        ~f:(fun x -> ("tags", (TagMap.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CreateUploadUrl ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("scanName",
                           (ScanName.to_value
                              req.CreateUploadUrlRequest.scanName))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetAccountConfiguration ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetFindings ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetMetricsSummary ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetScan ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListFindingsMetrics ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListScans ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTagsForResource ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | TagResource ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("tags",
                           (TagMap.to_value req.TagResourceRequest.tags))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UntagResource -> Awso.Http.Request.make (method_of_endpoint endp)
  | UpdateAccountConfiguration ->
      Awso.Http.Request.make (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | BatchGetFindings ->
      if is_success
      then Ok (BatchGetFindingsResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some BatchGetFindingsResponse.error_of_json))
  | CreateScan ->
      if is_success
      then Ok (CreateScanResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some CreateScanResponse.error_of_json))
  | CreateUploadUrl ->
      if is_success
      then Ok (CreateUploadUrlResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some CreateUploadUrlResponse.error_of_json))
  | GetAccountConfiguration ->
      if is_success
      then
        Ok (GetAccountConfigurationResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some GetAccountConfigurationResponse.error_of_json))
  | GetFindings ->
      if is_success
      then Ok (GetFindingsResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetFindingsResponse.error_of_json))
  | GetMetricsSummary ->
      if is_success
      then Ok (GetMetricsSummaryResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some GetMetricsSummaryResponse.error_of_json))
  | GetScan ->
      if is_success
      then Ok (GetScanResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetScanResponse.error_of_json))
  | ListFindingsMetrics ->
      if is_success
      then Ok (ListFindingsMetricsResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListFindingsMetricsResponse.error_of_json))
  | ListScans ->
      if is_success
      then Ok (ListScansResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListScansResponse.error_of_json))
  | ListTagsForResource ->
      if is_success
      then Ok (ListTagsForResourceResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListTagsForResourceResponse.error_of_json))
  | TagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (TagResourceResponse.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some TagResourceResponse.error_of_json))
  | UntagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (UntagResourceResponse.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some UntagResourceResponse.error_of_json))
  | UpdateAccountConfiguration ->
      if is_success
      then
        Ok
          (UpdateAccountConfigurationResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some UpdateAccountConfigurationResponse.error_of_json))
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.codeguru_security
let apiVersion = "2018-05-10"
let endpointPrefix = "codeguru-security"
let serviceFullName = "Amazon CodeGuru Security"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module CodeLine =
  struct
    type nonrec t =
      {
      number: Integer.t option [@ocaml.doc "The code line number."];
      content: String_.t option
        [@ocaml.doc "The code that contains a vulnerability."]}
    let make ?number = fun ?content -> fun () -> { number; content }
    let to_value x =
      structure_to_value
        [("number", (Option.map x.number ~f:Integer.to_value));
        ("content", (Option.map x.content ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let content =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "content") in
      let number =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "number") in
      make ?content ?number ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let content = field_map json__ "content" String_.of_json in
      let number = field_map json__ "number" Integer.of_json in
      make ?content ?number ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The line of code where a finding was detected."]
module SuggestedFix =
  struct
    type nonrec t =
      {
      description: String_.t option
        [@ocaml.doc
          "A description of the suggested code fix and why it is being suggested."];
      code: String_.t option
        [@ocaml.doc
          "The suggested code fix. If applicable, includes code patch to replace your source code."]}
    let make ?description = fun ?code -> fun () -> { description; code }
    let to_value x =
      structure_to_value
        [("description", (Option.map x.description ~f:String_.to_value));
        ("code", (Option.map x.code ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let code = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "code") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      make ?code ?description ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let code = field_map json__ "code" String_.of_json in
      let description = field_map json__ "description" String_.of_json in
      make ?code ?description ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the suggested code fix to remediate a finding."]
module CodeSnippet =
  struct
    type nonrec t = CodeLine.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:CodeLine.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CodeLine.of_xml)
    let of_json j =
      list_of_json ~kind:"CodeSnippet" ~of_json:CodeLine.of_json j
    let to_json v = composed_to_json to_value v
  end
module Double =
  struct
    type nonrec t = float
    let make i = i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module Recommendation =
  struct
    type nonrec t =
      {
      text: String_.t option
        [@ocaml.doc
          "The recommended course of action to remediate the finding."];
      url: String_.t option
        [@ocaml.doc
          "The URL address to the recommendation for remediating the finding."]}
    let make ?text = fun ?url -> fun () -> { text; url }
    let to_value x =
      structure_to_value
        [("text", (Option.map x.text ~f:String_.to_value));
        ("url", (Option.map x.url ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let url = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "url") in
      let text = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "text") in
      make ?url ?text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let url = field_map json__ "url" String_.of_json in
      let text = field_map json__ "text" String_.of_json in
      make ?url ?text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the recommended course of action to remediate a finding."]
module SuggestedFixes =
  struct
    type nonrec t = SuggestedFix.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:SuggestedFix.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SuggestedFix.of_xml)
    let of_json j =
      list_of_json ~kind:"SuggestedFixes" ~of_json:SuggestedFix.of_json j
    let to_json v = composed_to_json to_value v
  end
module FilePath =
  struct
    type nonrec t =
      {
      name: String_.t option [@ocaml.doc "The name of the file."];
      path: String_.t option
        [@ocaml.doc
          "The path to the resource with the security vulnerability."];
      startLine: Integer.t option
        [@ocaml.doc
          "The first line number of the code snippet where the security vulnerability appears in your code."];
      endLine: Integer.t option
        [@ocaml.doc
          "The last line number of the code snippet where the security vulnerability appears in your code."];
      codeSnippet: CodeSnippet.t option
        [@ocaml.doc
          "A list of CodeLine objects that describe where the security vulnerability appears in your code."]}
    let make ?name =
      fun ?path ->
        fun ?startLine ->
          fun ?endLine ->
            fun ?codeSnippet ->
              fun () -> { name; path; startLine; endLine; codeSnippet }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:String_.to_value));
        ("path", (Option.map x.path ~f:String_.to_value));
        ("startLine", (Option.map x.startLine ~f:Integer.to_value));
        ("endLine", (Option.map x.endLine ~f:Integer.to_value));
        ("codeSnippet", (Option.map x.codeSnippet ~f:CodeSnippet.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let codeSnippet =
        (Option.map ~f:CodeSnippet.of_xml) (Xml.child xml_arg0 "codeSnippet") in
      let endLine =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "endLine") in
      let startLine =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "startLine") in
      let path = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "path") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      make ?codeSnippet ?endLine ?startLine ?path ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let codeSnippet = field_map json__ "codeSnippet" CodeSnippet.of_json in
      let endLine = field_map json__ "endLine" Integer.of_json in
      let startLine = field_map json__ "startLine" Integer.of_json in
      let path = field_map json__ "path" String_.of_json in
      let name = field_map json__ "name" String_.of_json in
      make ?codeSnippet ?endLine ?startLine ?path ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the location of security vulnerabilities that Amazon CodeGuru Security detected in your code."]
module ReferenceUrls =
  struct
    type nonrec t = String_.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"ReferenceUrls" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module RelatedVulnerabilities =
  struct
    type nonrec t = String_.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"RelatedVulnerabilities" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module ValidationExceptionField =
  struct
    type nonrec t =
      {
      name: String_.t option [@ocaml.doc "The name of the exception."];
      message: String_.t option [@ocaml.doc "Describes the exception."]}
    let make ?name = fun ?message -> fun () -> { name; message }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:String_.to_value));
        ("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      make ?message ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      let name = field_map json__ "name" String_.of_json in
      make ?message ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a validation exception."]
module ScanName =
  struct
    type nonrec t = string
    let context_ = "ScanName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:140) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9-_$:.]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ScanName" j
    let to_json = simple_to_json to_value
  end
module ScanNameArn =
  struct
    type nonrec t = string
    let context_ = "ScanNameArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:300) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:aws:codeguru-security:[\\S]+:[\\d]{12}:scans\\/[a-zA-Z0-9-_$:.]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ScanNameArn" j
    let to_json = simple_to_json to_value
  end
module ScanState =
  struct
    type nonrec t =
      | InProgress 
      | Successful 
      | Failed 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | InProgress -> "InProgress"
      | Successful -> "Successful"
      | Failed -> "Failed"
      | Non_static_id s -> s
    let of_string =
      function
      | "InProgress" -> InProgress
      | "Successful" -> Successful
      | "Failed" -> Failed
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ScanState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ScanState" j)
    let to_json = simple_to_json to_value
  end
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module Uuid =
  struct
    type nonrec t = string
    let context_ = "Uuid"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i
             ~pattern:"[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Uuid" j
    let to_json = simple_to_json to_value
  end
module FindingMetricsValuePerSeverity =
  struct
    type nonrec t =
      {
      info: Double.t option
        [@ocaml.doc
          "A numeric value corresponding to an informational finding."];
      low: Double.t option
        [@ocaml.doc
          "A numeric value corresponding to a low severity finding."];
      medium: Double.t option
        [@ocaml.doc
          "A numeric value corresponding to a medium severity finding."];
      high: Double.t option
        [@ocaml.doc
          "A numeric value corresponding to a high severity finding."];
      critical: Double.t option
        [@ocaml.doc "A numeric value corresponding to a critical finding."]}
    let make ?info =
      fun ?low ->
        fun ?medium ->
          fun ?high ->
            fun ?critical -> fun () -> { info; low; medium; high; critical }
    let to_value x =
      structure_to_value
        [("info", (Option.map x.info ~f:Double.to_value));
        ("low", (Option.map x.low ~f:Double.to_value));
        ("medium", (Option.map x.medium ~f:Double.to_value));
        ("high", (Option.map x.high ~f:Double.to_value));
        ("critical", (Option.map x.critical ~f:Double.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let critical =
        (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "critical") in
      let high = (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "high") in
      let medium =
        (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "medium") in
      let low = (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "low") in
      let info = (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "info") in
      make ?critical ?high ?medium ?low ?info ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let critical = field_map json__ "critical" Double.of_json in
      let high = field_map json__ "high" Double.of_json in
      let medium = field_map json__ "medium" Double.of_json in
      let low = field_map json__ "low" Double.of_json in
      let info = field_map json__ "info" Double.of_json in
      make ?critical ?high ?medium ?low ?info ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A numeric value corresponding to the severity of a finding, such as the number of open findings or the average time it takes to close findings of a given severity."]
module CategoryWithFindingNum =
  struct
    type nonrec t =
      {
      categoryName: String_.t option
        [@ocaml.doc
          "The name of the finding category. A finding category is determined by the detector that detected the finding."];
      findingNumber: Integer.t option
        [@ocaml.doc "The number of open findings in the category."]}
    let make ?categoryName =
      fun ?findingNumber -> fun () -> { categoryName; findingNumber }
    let to_value x =
      structure_to_value
        [("categoryName", (Option.map x.categoryName ~f:String_.to_value));
        ("findingNumber", (Option.map x.findingNumber ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let findingNumber =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "findingNumber") in
      let categoryName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "categoryName") in
      make ?findingNumber ?categoryName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let findingNumber = field_map json__ "findingNumber" Integer.of_json in
      let categoryName = field_map json__ "categoryName" String_.of_json in
      make ?findingNumber ?categoryName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a finding category with open findings."]
module ScanNameWithFindingNum =
  struct
    type nonrec t =
      {
      scanName: String_.t option [@ocaml.doc "The name of the scan."];
      findingNumber: Integer.t option
        [@ocaml.doc "The number of findings generated by a scan."]}
    let make ?scanName =
      fun ?findingNumber -> fun () -> { scanName; findingNumber }
    let to_value x =
      structure_to_value
        [("scanName", (Option.map x.scanName ~f:String_.to_value));
        ("findingNumber", (Option.map x.findingNumber ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let findingNumber =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "findingNumber") in
      let scanName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "scanName") in
      make ?findingNumber ?scanName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let findingNumber = field_map json__ "findingNumber" Integer.of_json in
      let scanName = field_map json__ "scanName" String_.of_json in
      make ?findingNumber ?scanName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the number of findings generated by a scan."]
module DetectorTags =
  struct
    type nonrec t = String_.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"DetectorTags" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module Remediation =
  struct
    type nonrec t =
      {
      recommendation: Recommendation.t option
        [@ocaml.doc
          "An object that contains information about the recommended course of action to remediate a finding."];
      suggestedFixes: SuggestedFixes.t option
        [@ocaml.doc
          "A list of SuggestedFix objects. Each object contains information about a suggested code fix to remediate the finding."]}
    let make ?recommendation =
      fun ?suggestedFixes -> fun () -> { recommendation; suggestedFixes }
    let to_value x =
      structure_to_value
        [("recommendation",
           (Option.map x.recommendation ~f:Recommendation.to_value));
        ("suggestedFixes",
          (Option.map x.suggestedFixes ~f:SuggestedFixes.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let suggestedFixes =
        (Option.map ~f:SuggestedFixes.of_xml)
          (Xml.child xml_arg0 "suggestedFixes") in
      let recommendation =
        (Option.map ~f:Recommendation.of_xml)
          (Xml.child xml_arg0 "recommendation") in
      make ?suggestedFixes ?recommendation ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let suggestedFixes =
        field_map json__ "suggestedFixes" SuggestedFixes.of_json in
      let recommendation =
        field_map json__ "recommendation" Recommendation.of_json in
      make ?suggestedFixes ?recommendation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about how to remediate a finding."]
module Resource =
  struct
    type nonrec t =
      {
      id: String_.t option
        [@ocaml.doc "The scanName of the scan that was run on the resource."];
      subResourceId: String_.t option
        [@ocaml.doc "The identifier for a section of the resource."]}
    let make ?id = fun ?subResourceId -> fun () -> { id; subResourceId }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:String_.to_value));
        ("subResourceId", (Option.map x.subResourceId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let subResourceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "subResourceId") in
      let id = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "id") in
      make ?subResourceId ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let subResourceId = field_map json__ "subResourceId" String_.of_json in
      let id = field_map json__ "id" String_.of_json in
      make ?subResourceId ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a resource that contains a finding."]
module Severity =
  struct
    type nonrec t =
      | Critical 
      | High 
      | Medium 
      | Low 
      | Info 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Critical -> "Critical"
      | High -> "High"
      | Medium -> "Medium"
      | Low -> "Low"
      | Info -> "Info"
      | Non_static_id s -> s
    let of_string =
      function
      | "Critical" -> Critical
      | "High" -> High
      | "Medium" -> Medium
      | "Low" -> Low
      | "Info" -> Info
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Severity" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Severity" j)
    let to_json = simple_to_json to_value
  end
module Status =
  struct
    type nonrec t =
      | Closed 
      | Open 
      | All 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Closed -> "Closed"
      | Open -> "Open"
      | All -> "All"
      | Non_static_id s -> s
    let of_string =
      function
      | "Closed" -> Closed
      | "Open" -> Open
      | "All" -> All
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Status" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Status" j)
    let to_json = simple_to_json to_value
  end
module Vulnerability =
  struct
    type nonrec t =
      {
      referenceUrls: ReferenceUrls.t option
        [@ocaml.doc
          "One or more URL addresses that contain details about a vulnerability."];
      relatedVulnerabilities: RelatedVulnerabilities.t option
        [@ocaml.doc
          "One or more vulnerabilities that are related to the vulnerability being described."];
      id: String_.t option
        [@ocaml.doc "The identifier for the vulnerability."];
      filePath: FilePath.t option
        [@ocaml.doc
          "An object that describes the location of the detected security vulnerability in your code."];
      itemCount: Integer.t option
        [@ocaml.doc
          "The number of times the vulnerability appears in your code."]}
    let make ?referenceUrls =
      fun ?relatedVulnerabilities ->
        fun ?id ->
          fun ?filePath ->
            fun ?itemCount ->
              fun () ->
                {
                  referenceUrls;
                  relatedVulnerabilities;
                  id;
                  filePath;
                  itemCount
                }
    let to_value x =
      structure_to_value
        [("referenceUrls",
           (Option.map x.referenceUrls ~f:ReferenceUrls.to_value));
        ("relatedVulnerabilities",
          (Option.map x.relatedVulnerabilities
             ~f:RelatedVulnerabilities.to_value));
        ("id", (Option.map x.id ~f:String_.to_value));
        ("filePath", (Option.map x.filePath ~f:FilePath.to_value));
        ("itemCount", (Option.map x.itemCount ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let itemCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "itemCount") in
      let filePath =
        (Option.map ~f:FilePath.of_xml) (Xml.child xml_arg0 "filePath") in
      let id = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "id") in
      let relatedVulnerabilities =
        (Option.map ~f:RelatedVulnerabilities.of_xml)
          (Xml.child xml_arg0 "relatedVulnerabilities") in
      let referenceUrls =
        (Option.map ~f:ReferenceUrls.of_xml)
          (Xml.child xml_arg0 "referenceUrls") in
      make ?itemCount ?filePath ?id ?relatedVulnerabilities ?referenceUrls ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let itemCount = field_map json__ "itemCount" Integer.of_json in
      let filePath = field_map json__ "filePath" FilePath.of_json in
      let id = field_map json__ "id" String_.of_json in
      let relatedVulnerabilities =
        field_map json__ "relatedVulnerabilities"
          RelatedVulnerabilities.of_json in
      let referenceUrls =
        field_map json__ "referenceUrls" ReferenceUrls.of_json in
      make ?itemCount ?filePath ?id ?relatedVulnerabilities ?referenceUrls ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about a security vulnerability that Amazon CodeGuru Security detected."]
module ErrorCode =
  struct
    type nonrec t =
      | DUPLICATE_IDENTIFIER 
      | ITEM_DOES_NOT_EXIST 
      | INTERNAL_ERROR 
      | INVALID_FINDING_ID 
      | INVALID_SCAN_NAME 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DUPLICATE_IDENTIFIER -> "DUPLICATE_IDENTIFIER"
      | ITEM_DOES_NOT_EXIST -> "ITEM_DOES_NOT_EXIST"
      | INTERNAL_ERROR -> "INTERNAL_ERROR"
      | INVALID_FINDING_ID -> "INVALID_FINDING_ID"
      | INVALID_SCAN_NAME -> "INVALID_SCAN_NAME"
      | Non_static_id s -> s
    let of_string =
      function
      | "DUPLICATE_IDENTIFIER" -> DUPLICATE_IDENTIFIER
      | "ITEM_DOES_NOT_EXIST" -> ITEM_DOES_NOT_EXIST
      | "INTERNAL_ERROR" -> INTERNAL_ERROR
      | "INVALID_FINDING_ID" -> INVALID_FINDING_ID
      | "INVALID_SCAN_NAME" -> INVALID_SCAN_NAME
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ErrorCode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ErrorCode" j)
    let to_json = simple_to_json to_value
  end
module KmsKeyArn =
  struct
    type nonrec t = string
    let context_ = "KmsKeyArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:aws:kms:[\\S]+:[\\d]{12}:key\\/(([a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12})|(mrk-[0-9a-zA-Z]{32}))")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"KmsKeyArn" j
    let to_json = simple_to_json to_value
  end
module ValidationExceptionFieldList =
  struct
    type nonrec t = ValidationExceptionField.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ValidationExceptionField.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ValidationExceptionField.of_xml)
    let of_json j =
      list_of_json ~kind:"ValidationExceptionFieldList"
        ~of_json:ValidationExceptionField.of_json j
    let to_json v = composed_to_json to_value v
  end
module ValidationExceptionReason =
  struct
    type nonrec t =
      | UnknownOperation 
      | CannotParse 
      | FieldValidationFailed 
      | Other 
      | LambdaCodeShaMisMatch 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | UnknownOperation -> "unknownOperation"
      | CannotParse -> "cannotParse"
      | FieldValidationFailed -> "fieldValidationFailed"
      | Other -> "other"
      | LambdaCodeShaMisMatch -> "lambdaCodeShaMisMatch"
      | Non_static_id s -> s
    let of_string =
      function
      | "unknownOperation" -> UnknownOperation
      | "cannotParse" -> CannotParse
      | "fieldValidationFailed" -> FieldValidationFailed
      | "other" -> Other
      | "lambdaCodeShaMisMatch" -> LambdaCodeShaMisMatch
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ValidationExceptionReason" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ValidationExceptionReason" j)
    let to_json = simple_to_json to_value
  end
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module ScanSummary =
  struct
    type nonrec t =
      {
      scanState: ScanState.t option
        [@ocaml.doc
          "The state of the scan. A scan can be In Progress, Complete, or Failed."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The time when the scan was created."];
      updatedAt: Timestamp.t option
        [@ocaml.doc
          "The time the scan was last updated. A scan is updated when it is re-run."];
      scanName: ScanName.t option [@ocaml.doc "The name of the scan."];
      runId: Uuid.t option [@ocaml.doc "The identifier for the scan run."];
      scanNameArn: ScanNameArn.t option
        [@ocaml.doc "The ARN for the scan name."]}
    let make ?scanState =
      fun ?createdAt ->
        fun ?updatedAt ->
          fun ?scanName ->
            fun ?runId ->
              fun ?scanNameArn ->
                fun () ->
                  {
                    scanState;
                    createdAt;
                    updatedAt;
                    scanName;
                    runId;
                    scanNameArn
                  }
    let to_value x =
      structure_to_value
        [("scanState", (Option.map x.scanState ~f:ScanState.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("updatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value));
        ("scanName", (Option.map x.scanName ~f:ScanName.to_value));
        ("runId", (Option.map x.runId ~f:Uuid.to_value));
        ("scanNameArn", (Option.map x.scanNameArn ~f:ScanNameArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scanNameArn =
        (Option.map ~f:ScanNameArn.of_xml) (Xml.child xml_arg0 "scanNameArn") in
      let runId = (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "runId") in
      let scanName =
        (Option.map ~f:ScanName.of_xml) (Xml.child xml_arg0 "scanName") in
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "updatedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let scanState =
        (Option.map ~f:ScanState.of_xml) (Xml.child xml_arg0 "scanState") in
      make ?scanNameArn ?runId ?scanName ?updatedAt ?createdAt ?scanState ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let scanNameArn = field_map json__ "scanNameArn" ScanNameArn.of_json in
      let runId = field_map json__ "runId" Uuid.of_json in
      let scanName = field_map json__ "scanName" ScanName.of_json in
      let updatedAt = field_map json__ "updatedAt" Timestamp.of_json in
      let createdAt = field_map json__ "createdAt" Timestamp.of_json in
      let scanState = field_map json__ "scanState" ScanState.of_json in
      make ?scanNameArn ?runId ?scanName ?updatedAt ?createdAt ?scanState ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about a scan."]
module AccountFindingsMetric =
  struct
    type nonrec t =
      {
      date: Timestamp.t option
        [@ocaml.doc
          "The date from which the findings metrics were retrieved."];
      newFindings: FindingMetricsValuePerSeverity.t option
        [@ocaml.doc
          "The number of new findings of each severity on the specified date."];
      closedFindings: FindingMetricsValuePerSeverity.t option
        [@ocaml.doc
          "The number of closed findings of each severity on the specified date."];
      openFindings: FindingMetricsValuePerSeverity.t option
        [@ocaml.doc
          "The number of open findings of each severity as of the specified date."];
      meanTimeToClose: FindingMetricsValuePerSeverity.t option
        [@ocaml.doc
          "The average time in days it takes to close findings of each severity as of a specified date."]}
    let make ?date =
      fun ?newFindings ->
        fun ?closedFindings ->
          fun ?openFindings ->
            fun ?meanTimeToClose ->
              fun () ->
                {
                  date;
                  newFindings;
                  closedFindings;
                  openFindings;
                  meanTimeToClose
                }
    let to_value x =
      structure_to_value
        [("date", (Option.map x.date ~f:Timestamp.to_value));
        ("newFindings",
          (Option.map x.newFindings
             ~f:FindingMetricsValuePerSeverity.to_value));
        ("closedFindings",
          (Option.map x.closedFindings
             ~f:FindingMetricsValuePerSeverity.to_value));
        ("openFindings",
          (Option.map x.openFindings
             ~f:FindingMetricsValuePerSeverity.to_value));
        ("meanTimeToClose",
          (Option.map x.meanTimeToClose
             ~f:FindingMetricsValuePerSeverity.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let meanTimeToClose =
        (Option.map ~f:FindingMetricsValuePerSeverity.of_xml)
          (Xml.child xml_arg0 "meanTimeToClose") in
      let openFindings =
        (Option.map ~f:FindingMetricsValuePerSeverity.of_xml)
          (Xml.child xml_arg0 "openFindings") in
      let closedFindings =
        (Option.map ~f:FindingMetricsValuePerSeverity.of_xml)
          (Xml.child xml_arg0 "closedFindings") in
      let newFindings =
        (Option.map ~f:FindingMetricsValuePerSeverity.of_xml)
          (Xml.child xml_arg0 "newFindings") in
      let date = (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "date") in
      make ?meanTimeToClose ?openFindings ?closedFindings ?newFindings ?date
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let meanTimeToClose =
        field_map json__ "meanTimeToClose"
          FindingMetricsValuePerSeverity.of_json in
      let openFindings =
        field_map json__ "openFindings"
          FindingMetricsValuePerSeverity.of_json in
      let closedFindings =
        field_map json__ "closedFindings"
          FindingMetricsValuePerSeverity.of_json in
      let newFindings =
        field_map json__ "newFindings" FindingMetricsValuePerSeverity.of_json in
      let date = field_map json__ "date" Timestamp.of_json in
      make ?meanTimeToClose ?openFindings ?closedFindings ?newFindings ?date
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A summary of findings metrics for an account on a specified date."]
module CategoriesWithMostFindings =
  struct
    type nonrec t = CategoryWithFindingNum.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:5) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:CategoryWithFindingNum.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CategoryWithFindingNum.of_xml)
    let of_json j =
      list_of_json ~kind:"CategoriesWithMostFindings"
        ~of_json:CategoryWithFindingNum.of_json j
    let to_json v = composed_to_json to_value v
  end
module ScansWithMostOpenCriticalFindings =
  struct
    type nonrec t = ScanNameWithFindingNum.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:3) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ScanNameWithFindingNum.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ScanNameWithFindingNum.of_xml)
    let of_json j =
      list_of_json ~kind:"ScansWithMostOpenCriticalFindings"
        ~of_json:ScanNameWithFindingNum.of_json j
    let to_json v = composed_to_json to_value v
  end
module ScansWithMostOpenFindings =
  struct
    type nonrec t = ScanNameWithFindingNum.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:3) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ScanNameWithFindingNum.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ScanNameWithFindingNum.of_xml)
    let of_json j =
      list_of_json ~kind:"ScansWithMostOpenFindings"
        ~of_json:ScanNameWithFindingNum.of_json j
    let to_json v = composed_to_json to_value v
  end
module Finding =
  struct
    type nonrec t =
      {
      createdAt: Timestamp.t option
        [@ocaml.doc "The time when the finding was created."];
      description: String_.t option
        [@ocaml.doc "A description of the finding."];
      generatorId: String_.t option
        [@ocaml.doc
          "The identifier for the component that generated a finding such as AmazonCodeGuruSecurity."];
      id: String_.t option [@ocaml.doc "The identifier for a finding."];
      updatedAt: Timestamp.t option
        [@ocaml.doc
          "The time when the finding was last updated. Findings are updated when you remediate them or when the finding code location changes."];
      type_: String_.t option [@ocaml.doc "The type of finding."];
      status: Status.t option
        [@ocaml.doc
          "The status of the finding. A finding status can be open or closed."];
      resource: Resource.t option
        [@ocaml.doc
          "The resource where Amazon CodeGuru Security detected a finding."];
      vulnerability: Vulnerability.t option
        [@ocaml.doc
          "An object that describes the detected security vulnerability."];
      severity: Severity.t option
        [@ocaml.doc
          "The severity of the finding. Severity can be critical, high, medium, low, or informational. For information on severity levels, see Finding severity in the Amazon CodeGuru Security User Guide."];
      remediation: Remediation.t option
        [@ocaml.doc
          "An object that contains the details about how to remediate a finding."];
      title: String_.t option [@ocaml.doc "The title of the finding."];
      detectorTags: DetectorTags.t option
        [@ocaml.doc
          "One or more tags or categorizations that are associated with a detector. These tags are defined by type, programming language, or other classification such as maintainability or consistency."];
      detectorId: String_.t option
        [@ocaml.doc
          "The identifier for the detector that detected the finding in your code. A detector is a defined rule based on industry standards and AWS best practices."];
      detectorName: String_.t option
        [@ocaml.doc
          "The name of the detector that identified the security vulnerability in your code."];
      ruleId: String_.t option
        [@ocaml.doc
          "The identifier for the rule that generated the finding."]}
    let make ?createdAt =
      fun ?description ->
        fun ?generatorId ->
          fun ?id ->
            fun ?updatedAt ->
              fun ?type_ ->
                fun ?status ->
                  fun ?resource ->
                    fun ?vulnerability ->
                      fun ?severity ->
                        fun ?remediation ->
                          fun ?title ->
                            fun ?detectorTags ->
                              fun ?detectorId ->
                                fun ?detectorName ->
                                  fun ?ruleId ->
                                    fun () ->
                                      {
                                        createdAt;
                                        description;
                                        generatorId;
                                        id;
                                        updatedAt;
                                        type_;
                                        status;
                                        resource;
                                        vulnerability;
                                        severity;
                                        remediation;
                                        title;
                                        detectorTags;
                                        detectorId;
                                        detectorName;
                                        ruleId
                                      }
    let to_value x =
      structure_to_value
        [("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("description", (Option.map x.description ~f:String_.to_value));
        ("generatorId", (Option.map x.generatorId ~f:String_.to_value));
        ("id", (Option.map x.id ~f:String_.to_value));
        ("updatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value));
        ("type", (Option.map x.type_ ~f:String_.to_value));
        ("status", (Option.map x.status ~f:Status.to_value));
        ("resource", (Option.map x.resource ~f:Resource.to_value));
        ("vulnerability",
          (Option.map x.vulnerability ~f:Vulnerability.to_value));
        ("severity", (Option.map x.severity ~f:Severity.to_value));
        ("remediation", (Option.map x.remediation ~f:Remediation.to_value));
        ("title", (Option.map x.title ~f:String_.to_value));
        ("detectorTags",
          (Option.map x.detectorTags ~f:DetectorTags.to_value));
        ("detectorId", (Option.map x.detectorId ~f:String_.to_value));
        ("detectorName", (Option.map x.detectorName ~f:String_.to_value));
        ("ruleId", (Option.map x.ruleId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ruleId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ruleId") in
      let detectorName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "detectorName") in
      let detectorId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "detectorId") in
      let detectorTags =
        (Option.map ~f:DetectorTags.of_xml)
          (Xml.child xml_arg0 "detectorTags") in
      let title = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "title") in
      let remediation =
        (Option.map ~f:Remediation.of_xml) (Xml.child xml_arg0 "remediation") in
      let severity =
        (Option.map ~f:Severity.of_xml) (Xml.child xml_arg0 "severity") in
      let vulnerability =
        (Option.map ~f:Vulnerability.of_xml)
          (Xml.child xml_arg0 "vulnerability") in
      let resource =
        (Option.map ~f:Resource.of_xml) (Xml.child xml_arg0 "resource") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "type") in
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "updatedAt") in
      let id = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "id") in
      let generatorId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "generatorId") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      make ?ruleId ?detectorName ?detectorId ?detectorTags ?title
        ?remediation ?severity ?vulnerability ?resource ?status ?type_
        ?updatedAt ?id ?generatorId ?description ?createdAt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let ruleId = field_map json__ "ruleId" String_.of_json in
      let detectorName = field_map json__ "detectorName" String_.of_json in
      let detectorId = field_map json__ "detectorId" String_.of_json in
      let detectorTags = field_map json__ "detectorTags" DetectorTags.of_json in
      let title = field_map json__ "title" String_.of_json in
      let remediation = field_map json__ "remediation" Remediation.of_json in
      let severity = field_map json__ "severity" Severity.of_json in
      let vulnerability =
        field_map json__ "vulnerability" Vulnerability.of_json in
      let resource = field_map json__ "resource" Resource.of_json in
      let status = field_map json__ "status" Status.of_json in
      let type_ = field_map json__ "type" String_.of_json in
      let updatedAt = field_map json__ "updatedAt" Timestamp.of_json in
      let id = field_map json__ "id" String_.of_json in
      let generatorId = field_map json__ "generatorId" String_.of_json in
      let description = field_map json__ "description" String_.of_json in
      let createdAt = field_map json__ "createdAt" Timestamp.of_json in
      make ?ruleId ?detectorName ?detectorId ?detectorTags ?title
        ?remediation ?severity ?vulnerability ?resource ?status ?type_
        ?updatedAt ?id ?generatorId ?description ?createdAt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about a finding that was detected in your code."]
module HeaderKey =
  struct
    type nonrec t = string
    let context_ = "HeaderKey"
    let make i =
      let open Result in ok_or_failwith (check_string_min i ~min:1); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"HeaderKey" j
    let to_json = simple_to_json to_value
  end
module HeaderValue =
  struct
    type nonrec t = string
    let context_ = "HeaderValue"
    let make i =
      let open Result in ok_or_failwith (check_string_min i ~min:1); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"HeaderValue" j
    let to_json = simple_to_json to_value
  end
module BatchGetFindingsError =
  struct
    type nonrec t =
      {
      scanName: ScanName.t option
        [@ocaml.doc "The name of the scan that generated the finding."];
      findingId: String_.t option
        [@ocaml.doc "The finding ID of the finding that was not fetched."];
      errorCode: ErrorCode.t option
        [@ocaml.doc "A code associated with the type of error."];
      message: String_.t option [@ocaml.doc "Describes the error."]}
    let make ?scanName =
      fun ?findingId ->
        fun ?errorCode ->
          fun ?message ->
            fun () -> { scanName; findingId; errorCode; message }
    let to_value x =
      structure_to_value
        [("scanName", (Option.map x.scanName ~f:ScanName.to_value));
        ("findingId", (Option.map x.findingId ~f:String_.to_value));
        ("errorCode", (Option.map x.errorCode ~f:ErrorCode.to_value));
        ("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let errorCode =
        (Option.map ~f:ErrorCode.of_xml) (Xml.child xml_arg0 "errorCode") in
      let findingId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "findingId") in
      let scanName =
        (Option.map ~f:ScanName.of_xml) (Xml.child xml_arg0 "scanName") in
      make ?message ?errorCode ?findingId ?scanName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      let errorCode = field_map json__ "errorCode" ErrorCode.of_json in
      let findingId = field_map json__ "findingId" String_.of_json in
      let scanName = field_map json__ "scanName" ScanName.of_json in
      make ?message ?errorCode ?findingId ?scanName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about the error that caused a finding to fail to be retrieved."]
module FindingIdentifier =
  struct
    type nonrec t =
      {
      scanName: String_.t
        [@ocaml.doc "The name of the scan that generated the finding."];
      findingId: String_.t [@ocaml.doc "The identifier for a finding."]}
    let context_ = "FindingIdentifier"
    let make ~scanName = fun ~findingId -> fun () -> { scanName; findingId }
    let to_value x =
      structure_to_value
        [("scanName", (Some (String_.to_value x.scanName)));
        ("findingId", (Some (String_.to_value x.findingId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let findingId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "findingId") in
      let scanName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "scanName") in
      make ~findingId ~scanName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let findingId = field_map_exn json__ "findingId" String_.of_json in
      let scanName = field_map_exn json__ "scanName" String_.of_json in
      make ~findingId ~scanName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An object that contains information about a finding and the scan that generated it."]
module AccessDeniedException =
  struct
    type nonrec t =
      {
      errorCode: String_.t option
        [@ocaml.doc "The identifier for the error."];
      message: String_.t option [@ocaml.doc "Description of the error."];
      resourceId: String_.t option
        [@ocaml.doc
          "The identifier for the resource you don't have access to."];
      resourceType: String_.t option
        [@ocaml.doc "The type of resource you don't have access to."]}
    let make ?errorCode =
      fun ?message ->
        fun ?resourceId ->
          fun ?resourceType ->
            fun () -> { errorCode; message; resourceId; resourceType }
    let to_value x =
      structure_to_value
        [("errorCode", (Option.map x.errorCode ~f:String_.to_value));
        ("message", (Option.map x.message ~f:String_.to_value));
        ("resourceId", (Option.map x.resourceId ~f:String_.to_value));
        ("resourceType", (Option.map x.resourceType ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "resourceType") in
      let resourceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "resourceId") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let errorCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "errorCode") in
      make ?resourceType ?resourceId ?message ?errorCode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceType = field_map json__ "resourceType" String_.of_json in
      let resourceId = field_map json__ "resourceId" String_.of_json in
      let message = field_map json__ "message" String_.of_json in
      let errorCode = field_map json__ "errorCode" String_.of_json in
      make ?resourceType ?resourceId ?message ?errorCode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You do not have sufficient access to perform this action."]
module EncryptionConfig =
  struct
    type nonrec t =
      {
      kmsKeyArn: KmsKeyArn.t option
        [@ocaml.doc
          "The KMS key ARN that is used for encryption. If an AWS-managed key is used for encryption, returns empty."]}
    let make ?kmsKeyArn = fun () -> { kmsKeyArn }
    let to_value x =
      structure_to_value
        [("kmsKeyArn", (Option.map x.kmsKeyArn ~f:KmsKeyArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let kmsKeyArn =
        (Option.map ~f:KmsKeyArn.of_xml) (Xml.child xml_arg0 "kmsKeyArn") in
      make ?kmsKeyArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let kmsKeyArn = field_map json__ "kmsKeyArn" KmsKeyArn.of_json in
      make ?kmsKeyArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the encryption configuration for an account. Required to call UpdateAccountConfiguration."]
module InternalServerException =
  struct
    type nonrec t =
      {
      error: String_.t option
        [@ocaml.doc "The internal error encountered by the server."];
      message: String_.t option [@ocaml.doc "Description of the error."]}
    let make ?error = fun ?message -> fun () -> { error; message }
    let to_value x =
      structure_to_value
        [("error", (Option.map x.error ~f:String_.to_value));
        ("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let error = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "error") in
      make ?message ?error ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      let error = field_map json__ "error" String_.of_json in
      make ?message ?error ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The server encountered an internal error and is unable to complete the request."]
module ResourceNotFoundException =
  struct
    type nonrec t =
      {
      errorCode: String_.t option
        [@ocaml.doc "The identifier for the error."];
      message: String_.t option [@ocaml.doc "Description of the error."];
      resourceId: String_.t option
        [@ocaml.doc "The identifier for the resource that was not found."];
      resourceType: String_.t option
        [@ocaml.doc "The type of resource that was not found."]}
    let make ?errorCode =
      fun ?message ->
        fun ?resourceId ->
          fun ?resourceType ->
            fun () -> { errorCode; message; resourceId; resourceType }
    let to_value x =
      structure_to_value
        [("errorCode", (Option.map x.errorCode ~f:String_.to_value));
        ("message", (Option.map x.message ~f:String_.to_value));
        ("resourceId", (Option.map x.resourceId ~f:String_.to_value));
        ("resourceType", (Option.map x.resourceType ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "resourceType") in
      let resourceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "resourceId") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let errorCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "errorCode") in
      make ?resourceType ?resourceId ?message ?errorCode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceType = field_map json__ "resourceType" String_.of_json in
      let resourceId = field_map json__ "resourceId" String_.of_json in
      let message = field_map json__ "message" String_.of_json in
      let errorCode = field_map json__ "errorCode" String_.of_json in
      make ?resourceType ?resourceId ?message ?errorCode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The resource specified in the request was not found."]
module ThrottlingException =
  struct
    type nonrec t =
      {
      errorCode: String_.t option
        [@ocaml.doc "The identifier for the error."];
      message: String_.t option [@ocaml.doc "Description of the error."];
      serviceCode: String_.t option
        [@ocaml.doc "The identifier for the originating service."];
      quotaCode: String_.t option
        [@ocaml.doc "The identifier for the originating quota."]}
    let make ?errorCode =
      fun ?message ->
        fun ?serviceCode ->
          fun ?quotaCode ->
            fun () -> { errorCode; message; serviceCode; quotaCode }
    let to_value x =
      structure_to_value
        [("errorCode", (Option.map x.errorCode ~f:String_.to_value));
        ("message", (Option.map x.message ~f:String_.to_value));
        ("serviceCode", (Option.map x.serviceCode ~f:String_.to_value));
        ("quotaCode", (Option.map x.quotaCode ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let quotaCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "quotaCode") in
      let serviceCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "serviceCode") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let errorCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "errorCode") in
      make ?quotaCode ?serviceCode ?message ?errorCode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let quotaCode = field_map json__ "quotaCode" String_.of_json in
      let serviceCode = field_map json__ "serviceCode" String_.of_json in
      let message = field_map json__ "message" String_.of_json in
      let errorCode = field_map json__ "errorCode" String_.of_json in
      make ?quotaCode ?serviceCode ?message ?errorCode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The request was denied due to request throttling."]
module ValidationException =
  struct
    type nonrec t =
      {
      errorCode: String_.t option
        [@ocaml.doc "The identifier for the error."];
      message: String_.t option [@ocaml.doc "Description of the error."];
      reason: ValidationExceptionReason.t option
        [@ocaml.doc "The reason the request failed validation."];
      fieldList: ValidationExceptionFieldList.t option
        [@ocaml.doc "The field that caused the error, if applicable."]}
    let make ?errorCode =
      fun ?message ->
        fun ?reason ->
          fun ?fieldList ->
            fun () -> { errorCode; message; reason; fieldList }
    let to_value x =
      structure_to_value
        [("errorCode", (Option.map x.errorCode ~f:String_.to_value));
        ("message", (Option.map x.message ~f:String_.to_value));
        ("reason",
          (Option.map x.reason ~f:ValidationExceptionReason.to_value));
        ("fieldList",
          (Option.map x.fieldList ~f:ValidationExceptionFieldList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fieldList =
        (Option.map ~f:ValidationExceptionFieldList.of_xml)
          (Xml.child xml_arg0 "fieldList") in
      let reason =
        (Option.map ~f:ValidationExceptionReason.of_xml)
          (Xml.child xml_arg0 "reason") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let errorCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "errorCode") in
      make ?fieldList ?reason ?message ?errorCode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fieldList =
        field_map json__ "fieldList" ValidationExceptionFieldList.of_json in
      let reason =
        field_map json__ "reason" ValidationExceptionReason.of_json in
      let message = field_map json__ "message" String_.of_json in
      let errorCode = field_map json__ "errorCode" String_.of_json in
      make ?fieldList ?reason ?message ?errorCode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The input fails to satisfy the specified constraints."]
module ConflictException =
  struct
    type nonrec t =
      {
      errorCode: String_.t option
        [@ocaml.doc "The identifier for the error."];
      message: String_.t option [@ocaml.doc "Description of the error."];
      resourceId: String_.t option
        [@ocaml.doc
          "The identifier for the service resource associated with the request."];
      resourceType: String_.t option
        [@ocaml.doc "The type of resource associated with the request."]}
    let make ?errorCode =
      fun ?message ->
        fun ?resourceId ->
          fun ?resourceType ->
            fun () -> { errorCode; message; resourceId; resourceType }
    let to_value x =
      structure_to_value
        [("errorCode", (Option.map x.errorCode ~f:String_.to_value));
        ("message", (Option.map x.message ~f:String_.to_value));
        ("resourceId", (Option.map x.resourceId ~f:String_.to_value));
        ("resourceType", (Option.map x.resourceType ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "resourceType") in
      let resourceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "resourceId") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let errorCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "errorCode") in
      make ?resourceType ?resourceId ?message ?errorCode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceType = field_map json__ "resourceType" String_.of_json in
      let resourceId = field_map json__ "resourceId" String_.of_json in
      let message = field_map json__ "message" String_.of_json in
      let errorCode = field_map json__ "errorCode" String_.of_json in
      make ?resourceType ?resourceId ?message ?errorCode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The requested operation would cause a conflict with the current state of a service resource associated with the request. Resolve the conflict before retrying this request."]
module TagKeyList =
  struct
    type nonrec t = TagKey.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeyList" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module TagMap =
  struct
    type nonrec t = (TagKey.t * TagValue.t) list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((TagKey.of_string chopped),
                              (TagValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (TagKey.to_value x) |>
                    (fun x -> (TagValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:TagKey.of_string
        ~of_json:TagValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module NextToken =
  struct
    type nonrec t = string
    let context_ = "NextToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () -> check_pattern i ~pattern:"[\\S]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NextToken" j
    let to_json = simple_to_json to_value
  end
module ScanSummaries =
  struct
    type nonrec t = ScanSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ScanSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ScanSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"ScanSummaries" ~of_json:ScanSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListScansRequestMaxResultsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ListScansRequestMaxResultsInteger" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module FindingsMetricList =
  struct
    type nonrec t = AccountFindingsMetric.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AccountFindingsMetric.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AccountFindingsMetric.of_xml)
    let of_json j =
      list_of_json ~kind:"FindingsMetricList"
        ~of_json:AccountFindingsMetric.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListFindingsMetricsRequestMaxResultsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1000) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ListFindingsMetricsRequestMaxResultsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module AnalysisType =
  struct
    type nonrec t =
      | Security 
      | All 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | Security -> "Security" | All -> "All" | Non_static_id s -> s
    let of_string =
      function | "Security" -> Security | "All" -> All | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration AnalysisType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AnalysisType" j)
    let to_json = simple_to_json to_value
  end
module ErrorMessage =
  struct
    type nonrec t = string
    let context_ = "ErrorMessage"
    let make i =
      let open Result in ok_or_failwith (check_string_min i ~min:1); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorMessage" j
    let to_json = simple_to_json to_value
  end
module Long =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module MetricsSummary =
  struct
    type nonrec t =
      {
      date: Timestamp.t option
        [@ocaml.doc
          "The date from which the metrics summary information was retrieved."];
      openFindings: FindingMetricsValuePerSeverity.t option
        [@ocaml.doc "The number of open findings of each severity."];
      categoriesWithMostFindings: CategoriesWithMostFindings.t option
        [@ocaml.doc
          "A list of CategoryWithFindingNum objects for the top 5 finding categories with the most findings."];
      scansWithMostOpenFindings: ScansWithMostOpenFindings.t option
        [@ocaml.doc
          "A list of ScanNameWithFindingNum objects for the top 3 scans with the most number of open findings."];
      scansWithMostOpenCriticalFindings:
        ScansWithMostOpenCriticalFindings.t option
        [@ocaml.doc
          "A list of ScanNameWithFindingNum objects for the top 3 scans with the most number of open critical findings."]}
    let make ?date =
      fun ?openFindings ->
        fun ?categoriesWithMostFindings ->
          fun ?scansWithMostOpenFindings ->
            fun ?scansWithMostOpenCriticalFindings ->
              fun () ->
                {
                  date;
                  openFindings;
                  categoriesWithMostFindings;
                  scansWithMostOpenFindings;
                  scansWithMostOpenCriticalFindings
                }
    let to_value x =
      structure_to_value
        [("date", (Option.map x.date ~f:Timestamp.to_value));
        ("openFindings",
          (Option.map x.openFindings
             ~f:FindingMetricsValuePerSeverity.to_value));
        ("categoriesWithMostFindings",
          (Option.map x.categoriesWithMostFindings
             ~f:CategoriesWithMostFindings.to_value));
        ("scansWithMostOpenFindings",
          (Option.map x.scansWithMostOpenFindings
             ~f:ScansWithMostOpenFindings.to_value));
        ("scansWithMostOpenCriticalFindings",
          (Option.map x.scansWithMostOpenCriticalFindings
             ~f:ScansWithMostOpenCriticalFindings.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scansWithMostOpenCriticalFindings =
        (Option.map ~f:ScansWithMostOpenCriticalFindings.of_xml)
          (Xml.child xml_arg0 "scansWithMostOpenCriticalFindings") in
      let scansWithMostOpenFindings =
        (Option.map ~f:ScansWithMostOpenFindings.of_xml)
          (Xml.child xml_arg0 "scansWithMostOpenFindings") in
      let categoriesWithMostFindings =
        (Option.map ~f:CategoriesWithMostFindings.of_xml)
          (Xml.child xml_arg0 "categoriesWithMostFindings") in
      let openFindings =
        (Option.map ~f:FindingMetricsValuePerSeverity.of_xml)
          (Xml.child xml_arg0 "openFindings") in
      let date = (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "date") in
      make ?scansWithMostOpenCriticalFindings ?scansWithMostOpenFindings
        ?categoriesWithMostFindings ?openFindings ?date ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let scansWithMostOpenCriticalFindings =
        field_map json__ "scansWithMostOpenCriticalFindings"
          ScansWithMostOpenCriticalFindings.of_json in
      let scansWithMostOpenFindings =
        field_map json__ "scansWithMostOpenFindings"
          ScansWithMostOpenFindings.of_json in
      let categoriesWithMostFindings =
        field_map json__ "categoriesWithMostFindings"
          CategoriesWithMostFindings.of_json in
      let openFindings =
        field_map json__ "openFindings"
          FindingMetricsValuePerSeverity.of_json in
      let date = field_map json__ "date" Timestamp.of_json in
      make ?scansWithMostOpenCriticalFindings ?scansWithMostOpenFindings
        ?categoriesWithMostFindings ?openFindings ?date ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A summary of metrics for an account as of a specified date."]
module Findings =
  struct
    type nonrec t = Finding.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Finding.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Finding.of_xml)
    let of_json j = list_of_json ~kind:"Findings" ~of_json:Finding.of_json j
    let to_json v = composed_to_json to_value v
  end
module GetFindingsRequestMaxResultsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1000) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for GetFindingsRequestMaxResultsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module RequestHeaderMap =
  struct
    type nonrec t = (HeaderKey.t * HeaderValue.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((HeaderKey.of_string chopped),
                              (HeaderValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (HeaderKey.to_value x) |>
                    (fun x -> (HeaderValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:HeaderKey.of_string
        ~of_json:HeaderValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module S3Url =
  struct
    type nonrec t = string
    let context_ = "S3Url"
    let make i =
      let open Result in ok_or_failwith (check_string_min i ~min:1); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"S3Url" j
    let to_json = simple_to_json to_value
  end
module ResourceId =
  struct
    type nonrec t =
      {
      codeArtifactId: Uuid.t option
        [@ocaml.doc
          "The identifier for the code file uploaded to the resource object. Returned by CreateUploadUrl when you upload resources to be scanned."]}
    let make ?codeArtifactId = fun () -> { codeArtifactId }
    let to_value x =
      structure_to_value
        [("codeArtifactId", (Option.map x.codeArtifactId ~f:Uuid.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let codeArtifactId =
        (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "codeArtifactId") in
      make ?codeArtifactId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let codeArtifactId = field_map json__ "codeArtifactId" Uuid.of_json in
      make ?codeArtifactId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The identifier for a resource object that contains resources to scan. Specifying a codeArtifactId is required to create a scan."]
module ClientToken =
  struct
    type nonrec t = string
    let context_ = "ClientToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"[\\S]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ClientToken" j
    let to_json = simple_to_json to_value
  end
module ScanType =
  struct
    type nonrec t =
      | Standard 
      | Express 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Standard -> "Standard"
      | Express -> "Express"
      | Non_static_id s -> s
    let of_string =
      function
      | "Standard" -> Standard
      | "Express" -> Express
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ScanType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ScanType" j)
    let to_json = simple_to_json to_value
  end
module BatchGetFindingsErrors =
  struct
    type nonrec t = BatchGetFindingsError.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:BatchGetFindingsError.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BatchGetFindingsError.of_xml)
    let of_json j =
      list_of_json ~kind:"BatchGetFindingsErrors"
        ~of_json:BatchGetFindingsError.of_json j
    let to_json v = composed_to_json to_value v
  end
module FindingIdentifiers =
  struct
    type nonrec t = FindingIdentifier.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:25) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FindingIdentifier.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FindingIdentifier.of_xml)
    let of_json j =
      list_of_json ~kind:"FindingIdentifiers"
        ~of_json:FindingIdentifier.of_json j
    let to_json v = composed_to_json to_value v
  end
module UpdateAccountConfigurationResponse =
  struct
    type nonrec t =
      {
      encryptionConfig: EncryptionConfig.t option
        [@ocaml.doc
          "An EncryptionConfig object that contains the KMS key ARN that is used for encryption. If you did not specify a customer-managed KMS key in the request, returns empty."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?encryptionConfig = fun () -> { encryptionConfig }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("encryptionConfig",
           (Option.map x.encryptionConfig ~f:EncryptionConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let encryptionConfig =
        (Option.map ~f:EncryptionConfig.of_xml)
          (Xml.child xml_arg0 "encryptionConfig") in
      make ?encryptionConfig ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let encryptionConfig =
        field_map json__ "encryptionConfig" EncryptionConfig.of_json in
      make ?encryptionConfig ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Use to update the encryption configuration for an account."]
module UpdateAccountConfigurationRequest =
  struct
    type nonrec t =
      {
      encryptionConfig: EncryptionConfig.t
        [@ocaml.doc
          "The customer-managed KMS key ARN you want to use for encryption. If not specified, CodeGuru Security will use an AWS-managed key for encryption. If you previously specified a customer-managed KMS key and want CodeGuru Security to use an AWS-managed key for encryption instead, pass nothing."]}
    let context_ = "UpdateAccountConfigurationRequest"
    let make ~encryptionConfig = fun () -> { encryptionConfig }
    let to_value x =
      structure_to_value
        [("encryptionConfig",
           (Some (EncryptionConfig.to_value x.encryptionConfig)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let encryptionConfig =
        EncryptionConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "encryptionConfig") in
      make ~encryptionConfig ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let encryptionConfig =
        field_map_exn json__ "encryptionConfig" EncryptionConfig.of_json in
      make ~encryptionConfig ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Use to update the encryption configuration for an account."]
module UntagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Use to remove one or more tags from an existing scan."]
module UntagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: ScanNameArn.t
        [@ocaml.doc
          "The ARN of the ScanName object. You can retrieve this ARN by calling CreateScan, ListScans, or GetScan."];
      tagKeys: TagKeyList.t
        [@ocaml.doc
          "A list of keys for each tag you want to remove from a scan."]}
    let context_ = "UntagResourceRequest"
    let make ~resourceArn =
      fun ~tagKeys -> fun () -> { resourceArn; tagKeys }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (ScanNameArn.to_value x.resourceArn)));
        ("tagKeys", (Some (TagKeyList.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "tagKeys") in
      let resourceArn =
        ScanNameArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tagKeys ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagKeys = field_map_exn json__ "tagKeys" TagKeyList.of_json in
      let resourceArn =
        field_map_exn json__ "resourceArn" ScanNameArn.of_json in
      make ~tagKeys ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Use to remove one or more tags from an existing scan."]
module TagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Use to add one or more tags to an existing scan."]
module TagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: ScanNameArn.t
        [@ocaml.doc
          "The ARN of the ScanName object. You can retrieve this ARN by calling CreateScan, ListScans, or GetScan."];
      tags: TagMap.t
        [@ocaml.doc
          "An array of key-value pairs used to tag an existing scan. A tag is a custom attribute label with two parts: A tag key. For example, CostCenter, Environment, or Secret. Tag keys are case sensitive. An optional tag value field. For example, 111122223333, Production, or a team name. Omitting the tag value is the same as using an empty string. Tag values are case sensitive."]}
    let context_ = "TagResourceRequest"
    let make ~resourceArn = fun ~tags -> fun () -> { resourceArn; tags }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (ScanNameArn.to_value x.resourceArn)));
        ("tags", (Some (TagMap.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagMap.of_xml (Xml.child_exn ~context:context_ xml_arg0 "tags") in
      let resourceArn =
        ScanNameArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tags ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map_exn json__ "tags" TagMap.of_json in
      let resourceArn =
        field_map_exn json__ "resourceArn" ScanNameArn.of_json in
      make ~tags ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Use to add one or more tags to an existing scan."]
module ListTagsForResourceResponse =
  struct
    type nonrec t =
      {
      tags: TagMap.t option
        [@ocaml.doc
          "An array of key-value pairs used to tag an existing scan. A tag is a custom attribute label with two parts: A tag key. For example, CostCenter, Environment, or Secret. Tag keys are case sensitive. An optional tag value field. For example, 111122223333, Production, or a team name. Omitting the tag value is the same as using an empty string. Tag values are case sensitive."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      make ?tags ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in make ?tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns a list of all tags associated with a scan."]
module ListTagsForResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: ScanNameArn.t
        [@ocaml.doc
          "The ARN of the ScanName object. You can retrieve this ARN by calling CreateScan, ListScans, or GetScan."]}
    let context_ = "ListTagsForResourceRequest"
    let make ~resourceArn = fun () -> { resourceArn }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (ScanNameArn.to_value x.resourceArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        ScanNameArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceArn =
        field_map_exn json__ "resourceArn" ScanNameArn.of_json in
      make ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns a list of all tags associated with a scan."]
module ListScansResponse =
  struct
    type nonrec t =
      {
      summaries: ScanSummaries.t option
        [@ocaml.doc
          "A list of ScanSummary objects with information about all scans in an account."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A pagination token. You can use this in future calls to ListScans to continue listing results after the current page."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?summaries =
      fun ?nextToken -> fun () -> { summaries; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("summaries", (Option.map x.summaries ~f:ScanSummaries.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let summaries =
        (Option.map ~f:ScanSummaries.of_xml) (Xml.child xml_arg0 "summaries") in
      make ?nextToken ?summaries ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let summaries = field_map json__ "summaries" ScanSummaries.of_json in
      make ?nextToken ?summaries ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of all scans in an account. Does not return EXPRESS scans."]
module ListScansRequest =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token to use for paginating results that are returned in the response. Set the value of this parameter to null for the first request. For subsequent calls, use the nextToken value returned from the previous request to continue listing results after the first page."];
      maxResults: ListScansRequestMaxResultsInteger.t option
        [@ocaml.doc
          "The maximum number of results to return in the response. Use this parameter when paginating results. If additional results exist beyond the number you specify, the nextToken element is returned in the response. Use nextToken in a subsequent request to retrieve additional results. If not specified, returns 100 results."]}
    let make ?nextToken =
      fun ?maxResults -> fun () -> { nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults",
          (Option.map x.maxResults
             ~f:ListScansRequestMaxResultsInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:ListScansRequestMaxResultsInteger.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?maxResults ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxResults =
        field_map json__ "maxResults"
          ListScansRequestMaxResultsInteger.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      make ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of all scans in an account. Does not return EXPRESS scans."]
module ListFindingsMetricsResponse =
  struct
    type nonrec t =
      {
      findingsMetrics: FindingsMetricList.t option
        [@ocaml.doc
          "A list of AccountFindingsMetric objects retrieved from the specified time interval."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A pagination token. You can use this in future calls to ListFindingMetrics to continue listing results after the current page."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?findingsMetrics =
      fun ?nextToken -> fun () -> { findingsMetrics; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("findingsMetrics",
           (Option.map x.findingsMetrics ~f:FindingsMetricList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let findingsMetrics =
        (Option.map ~f:FindingsMetricList.of_xml)
          (Xml.child xml_arg0 "findingsMetrics") in
      make ?nextToken ?findingsMetrics ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let findingsMetrics =
        field_map json__ "findingsMetrics" FindingsMetricList.of_json in
      make ?nextToken ?findingsMetrics ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns metrics about all findings in an account within a specified time range."]
module ListFindingsMetricsRequest =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token to use for paginating results that are returned in the response. Set the value of this parameter to null for the first request. For subsequent calls, use the nextToken value returned from the previous request to continue listing results after the first page."];
      maxResults: ListFindingsMetricsRequestMaxResultsInteger.t option
        [@ocaml.doc
          "The maximum number of results to return in the response. Use this parameter when paginating results. If additional results exist beyond the number you specify, the nextToken element is returned in the response. Use nextToken in a subsequent request to retrieve additional results. If not specified, returns 1000 results."];
      startDate: Timestamp.t
        [@ocaml.doc
          "The start date of the interval which you want to retrieve metrics from. Rounds to the nearest day."];
      endDate: Timestamp.t
        [@ocaml.doc
          "The end date of the interval which you want to retrieve metrics from. Round to the nearest day."]}
    let context_ = "ListFindingsMetricsRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ~startDate ->
          fun ~endDate ->
            fun () -> { nextToken; maxResults; startDate; endDate }
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults",
          (Option.map x.maxResults
             ~f:ListFindingsMetricsRequestMaxResultsInteger.to_value));
        ("startDate", (Some (Timestamp.to_value x.startDate)));
        ("endDate", (Some (Timestamp.to_value x.endDate)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let endDate =
        Timestamp.of_xml (Xml.child_exn ~context:context_ xml_arg0 "endDate") in
      let startDate =
        Timestamp.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "startDate") in
      let maxResults =
        (Option.map ~f:ListFindingsMetricsRequestMaxResultsInteger.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ~endDate ~startDate ?maxResults ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let endDate = field_map_exn json__ "endDate" Timestamp.of_json in
      let startDate = field_map_exn json__ "startDate" Timestamp.of_json in
      let maxResults =
        field_map json__ "maxResults"
          ListFindingsMetricsRequestMaxResultsInteger.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      make ~endDate ~startDate ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns metrics about all findings in an account within a specified time range."]
module GetScanResponse =
  struct
    type nonrec t =
      {
      scanName: ScanName.t option [@ocaml.doc "The name of the scan."];
      runId: Uuid.t option
        [@ocaml.doc "UUID that identifies the individual scan run."];
      scanState: ScanState.t option
        [@ocaml.doc
          "The current state of the scan. Returns either InProgress, Successful, or Failed."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The time the scan was created."];
      analysisType: AnalysisType.t option
        [@ocaml.doc
          "The type of analysis CodeGuru Security performed in the scan, either Security or All. The Security type only generates findings related to security. The All type generates both security findings and quality findings."];
      updatedAt: Timestamp.t option
        [@ocaml.doc
          "The time when the scan was last updated. Only available for STANDARD scan types."];
      numberOfRevisions: Long.t option
        [@ocaml.doc
          "The number of times a scan has been re-run on a revised resource."];
      scanNameArn: ScanNameArn.t option
        [@ocaml.doc "The ARN for the scan name."];
      errorMessage: ErrorMessage.t option
        [@ocaml.doc
          "Details about the error that causes a scan to fail to be retrieved."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?scanName =
      fun ?runId ->
        fun ?scanState ->
          fun ?createdAt ->
            fun ?analysisType ->
              fun ?updatedAt ->
                fun ?numberOfRevisions ->
                  fun ?scanNameArn ->
                    fun ?errorMessage ->
                      fun () ->
                        {
                          scanName;
                          runId;
                          scanState;
                          createdAt;
                          analysisType;
                          updatedAt;
                          numberOfRevisions;
                          scanNameArn;
                          errorMessage
                        }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("scanName", (Option.map x.scanName ~f:ScanName.to_value));
        ("runId", (Option.map x.runId ~f:Uuid.to_value));
        ("scanState", (Option.map x.scanState ~f:ScanState.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("analysisType",
          (Option.map x.analysisType ~f:AnalysisType.to_value));
        ("updatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value));
        ("numberOfRevisions",
          (Option.map x.numberOfRevisions ~f:Long.to_value));
        ("scanNameArn", (Option.map x.scanNameArn ~f:ScanNameArn.to_value));
        ("errorMessage",
          (Option.map x.errorMessage ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errorMessage =
        (Option.map ~f:ErrorMessage.of_xml)
          (Xml.child xml_arg0 "errorMessage") in
      let scanNameArn =
        (Option.map ~f:ScanNameArn.of_xml) (Xml.child xml_arg0 "scanNameArn") in
      let numberOfRevisions =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "numberOfRevisions") in
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "updatedAt") in
      let analysisType =
        (Option.map ~f:AnalysisType.of_xml)
          (Xml.child xml_arg0 "analysisType") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let scanState =
        (Option.map ~f:ScanState.of_xml) (Xml.child xml_arg0 "scanState") in
      let runId = (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "runId") in
      let scanName =
        (Option.map ~f:ScanName.of_xml) (Xml.child xml_arg0 "scanName") in
      make ?errorMessage ?scanNameArn ?numberOfRevisions ?updatedAt
        ?analysisType ?createdAt ?scanState ?runId ?scanName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let errorMessage = field_map json__ "errorMessage" ErrorMessage.of_json in
      let scanNameArn = field_map json__ "scanNameArn" ScanNameArn.of_json in
      let numberOfRevisions =
        field_map json__ "numberOfRevisions" Long.of_json in
      let updatedAt = field_map json__ "updatedAt" Timestamp.of_json in
      let analysisType = field_map json__ "analysisType" AnalysisType.of_json in
      let createdAt = field_map json__ "createdAt" Timestamp.of_json in
      let scanState = field_map json__ "scanState" ScanState.of_json in
      let runId = field_map json__ "runId" Uuid.of_json in
      let scanName = field_map json__ "scanName" ScanName.of_json in
      make ?errorMessage ?scanNameArn ?numberOfRevisions ?updatedAt
        ?analysisType ?createdAt ?scanState ?runId ?scanName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns details about a scan, including whether or not a scan has completed."]
module GetScanRequest =
  struct
    type nonrec t =
      {
      scanName: ScanName.t
        [@ocaml.doc "The name of the scan you want to view details about."];
      runId: Uuid.t option
        [@ocaml.doc
          "UUID that identifies the individual scan run you want to view details about. You retrieve this when you call the CreateScan operation. Defaults to the latest scan run if missing."]}
    let context_ = "GetScanRequest"
    let make ?runId = fun ~scanName -> fun () -> { runId; scanName }
    let to_value x =
      structure_to_value
        [("scanName", (Some (ScanName.to_value x.scanName)));
        ("runId", (Option.map x.runId ~f:Uuid.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let runId = (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "runId") in
      let scanName =
        ScanName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "scanName") in
      make ?runId ~scanName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let runId = field_map json__ "runId" Uuid.of_json in
      let scanName = field_map_exn json__ "scanName" ScanName.of_json in
      make ?runId ~scanName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns details about a scan, including whether or not a scan has completed."]
module GetMetricsSummaryResponse =
  struct
    type nonrec t =
      {
      metricsSummary: MetricsSummary.t option
        [@ocaml.doc "The summary metrics from the specified date."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?metricsSummary = fun () -> { metricsSummary }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("metricsSummary",
           (Option.map x.metricsSummary ~f:MetricsSummary.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let metricsSummary =
        (Option.map ~f:MetricsSummary.of_xml)
          (Xml.child xml_arg0 "metricsSummary") in
      make ?metricsSummary ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let metricsSummary =
        field_map json__ "metricsSummary" MetricsSummary.of_json in
      make ?metricsSummary ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a summary of metrics for an account from a specified date, including number of open findings, the categories with most findings, the scans with most open findings, and scans with most open critical findings."]
module GetMetricsSummaryRequest =
  struct
    type nonrec t =
      {
      date: Timestamp.t
        [@ocaml.doc
          "The date you want to retrieve summary metrics from, rounded to the nearest day. The date must be within the past two years."]}
    let context_ = "GetMetricsSummaryRequest"
    let make ~date = fun () -> { date }
    let to_value x =
      structure_to_value [("date", (Some (Timestamp.to_value x.date)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let date =
        Timestamp.of_xml (Xml.child_exn ~context:context_ xml_arg0 "date") in
      make ~date ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let date = field_map_exn json__ "date" Timestamp.of_json in
      make ~date ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a summary of metrics for an account from a specified date, including number of open findings, the categories with most findings, the scans with most open findings, and scans with most open critical findings."]
module GetFindingsResponse =
  struct
    type nonrec t =
      {
      findings: Findings.t option
        [@ocaml.doc "A list of findings generated by the specified scan."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A pagination token. You can use this in future calls to GetFindings to continue listing results after the current page."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?findings = fun ?nextToken -> fun () -> { findings; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("findings", (Option.map x.findings ~f:Findings.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let findings =
        (Option.map ~f:Findings.of_xml) (Xml.child xml_arg0 "findings") in
      make ?nextToken ?findings ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let findings = field_map json__ "findings" Findings.of_json in
      make ?nextToken ?findings ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of all findings generated by a particular scan."]
module GetFindingsRequest =
  struct
    type nonrec t =
      {
      scanName: ScanName.t
        [@ocaml.doc
          "The name of the scan you want to retrieve findings from."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token to use for paginating results that are returned in the response. Set the value of this parameter to null for the first request. For subsequent calls, use the nextToken value returned from the previous request to continue listing results after the first page."];
      maxResults: GetFindingsRequestMaxResultsInteger.t option
        [@ocaml.doc
          "The maximum number of results to return in the response. Use this parameter when paginating results. If additional results exist beyond the number you specify, the nextToken element is returned in the response. Use nextToken in a subsequent request to retrieve additional results. If not specified, returns 1000 results."];
      status: Status.t option
        [@ocaml.doc
          "The status of the findings you want to get. Pass either Open, Closed, or All."]}
    let context_ = "GetFindingsRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ?status ->
          fun ~scanName ->
            fun () -> { nextToken; maxResults; status; scanName }
    let to_value x =
      structure_to_value
        [("scanName", (Some (ScanName.to_value x.scanName)));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults",
          (Option.map x.maxResults
             ~f:GetFindingsRequestMaxResultsInteger.to_value));
        ("status", (Option.map x.status ~f:Status.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "status") in
      let maxResults =
        (Option.map ~f:GetFindingsRequestMaxResultsInteger.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let scanName =
        ScanName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "scanName") in
      make ?status ?maxResults ?nextToken ~scanName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "status" Status.of_json in
      let maxResults =
        field_map json__ "maxResults"
          GetFindingsRequestMaxResultsInteger.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let scanName = field_map_exn json__ "scanName" ScanName.of_json in
      make ?status ?maxResults ?nextToken ~scanName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of all findings generated by a particular scan."]
module GetAccountConfigurationResponse =
  struct
    type nonrec t =
      {
      encryptionConfig: EncryptionConfig.t option
        [@ocaml.doc
          "An EncryptionConfig object that contains the KMS key ARN that is used for encryption. By default, CodeGuru Security uses an AWS-managed key for encryption. To specify your own key, call UpdateAccountConfiguration. If you do not specify a customer-managed key, returns empty."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?encryptionConfig = fun () -> { encryptionConfig }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("encryptionConfig",
           (Option.map x.encryptionConfig ~f:EncryptionConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let encryptionConfig =
        (Option.map ~f:EncryptionConfig.of_xml)
          (Xml.child xml_arg0 "encryptionConfig") in
      make ?encryptionConfig ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let encryptionConfig =
        field_map json__ "encryptionConfig" EncryptionConfig.of_json in
      make ?encryptionConfig ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Use to get the encryption configuration for an account."]
module GetAccountConfigurationRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Use to get the encryption configuration for an account."]
module CreateUploadUrlResponse =
  struct
    type nonrec t =
      {
      s3Url: S3Url.t option
        [@ocaml.doc
          "A pre-signed S3 URL. You can upload the code file you want to scan with the required requestHeaders using any HTTP client."];
      requestHeaders: RequestHeaderMap.t option
        [@ocaml.doc
          "A set of key-value pairs that contain the required headers when uploading your resource."];
      codeArtifactId: Uuid.t option
        [@ocaml.doc
          "The identifier for the uploaded code resource. Pass this to CreateScan to use the uploaded resources."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?s3Url =
      fun ?requestHeaders ->
        fun ?codeArtifactId ->
          fun () -> { s3Url; requestHeaders; codeArtifactId }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("s3Url", (Option.map x.s3Url ~f:S3Url.to_value));
        ("requestHeaders",
          (Option.map x.requestHeaders ~f:RequestHeaderMap.to_value));
        ("codeArtifactId", (Option.map x.codeArtifactId ~f:Uuid.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let codeArtifactId =
        (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "codeArtifactId") in
      let requestHeaders =
        (Option.map ~f:RequestHeaderMap.of_xml)
          (Xml.child xml_arg0 "requestHeaders") in
      let s3Url = (Option.map ~f:S3Url.of_xml) (Xml.child xml_arg0 "s3Url") in
      make ?codeArtifactId ?requestHeaders ?s3Url ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let codeArtifactId = field_map json__ "codeArtifactId" Uuid.of_json in
      let requestHeaders =
        field_map json__ "requestHeaders" RequestHeaderMap.of_json in
      let s3Url = field_map json__ "s3Url" S3Url.of_json in
      make ?codeArtifactId ?requestHeaders ?s3Url ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Generates a pre-signed URL, request headers used to upload a code resource, and code artifact identifier for the uploaded resource. You can upload your code resource to the URL with the request headers using any HTTP client."]
module CreateUploadUrlRequest =
  struct
    type nonrec t =
      {
      scanName: ScanName.t
        [@ocaml.doc
          "The name of the scan that will use the uploaded resource. CodeGuru Security uses the unique scan name to track revisions across multiple scans of the same resource. Use this scanName when you call CreateScan on the code resource you upload to this URL."]}
    let context_ = "CreateUploadUrlRequest"
    let make ~scanName = fun () -> { scanName }
    let to_value x =
      structure_to_value
        [("scanName", (Some (ScanName.to_value x.scanName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scanName =
        ScanName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "scanName") in
      make ~scanName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let scanName = field_map_exn json__ "scanName" ScanName.of_json in
      make ~scanName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Generates a pre-signed URL, request headers used to upload a code resource, and code artifact identifier for the uploaded resource. You can upload your code resource to the URL with the request headers using any HTTP client."]
module CreateScanResponse =
  struct
    type nonrec t =
      {
      scanName: ScanName.t option [@ocaml.doc "The name of the scan."];
      runId: Uuid.t option
        [@ocaml.doc "UUID that identifies the individual scan run."];
      resourceId: ResourceId.t option
        [@ocaml.doc
          "The identifier for the resource object that contains resources that were scanned."];
      scanState: ScanState.t option
        [@ocaml.doc
          "The current state of the scan. Returns either InProgress, Successful, or Failed."];
      scanNameArn: ScanNameArn.t option
        [@ocaml.doc "The ARN for the scan name."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?scanName =
      fun ?runId ->
        fun ?resourceId ->
          fun ?scanState ->
            fun ?scanNameArn ->
              fun () ->
                { scanName; runId; resourceId; scanState; scanNameArn }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("scanName", (Option.map x.scanName ~f:ScanName.to_value));
        ("runId", (Option.map x.runId ~f:Uuid.to_value));
        ("resourceId", (Option.map x.resourceId ~f:ResourceId.to_value));
        ("scanState", (Option.map x.scanState ~f:ScanState.to_value));
        ("scanNameArn", (Option.map x.scanNameArn ~f:ScanNameArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scanNameArn =
        (Option.map ~f:ScanNameArn.of_xml) (Xml.child xml_arg0 "scanNameArn") in
      let scanState =
        (Option.map ~f:ScanState.of_xml) (Xml.child xml_arg0 "scanState") in
      let resourceId =
        (Option.map ~f:ResourceId.of_xml) (Xml.child xml_arg0 "resourceId") in
      let runId = (Option.map ~f:Uuid.of_xml) (Xml.child xml_arg0 "runId") in
      let scanName =
        (Option.map ~f:ScanName.of_xml) (Xml.child xml_arg0 "scanName") in
      make ?scanNameArn ?scanState ?resourceId ?runId ?scanName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let scanNameArn = field_map json__ "scanNameArn" ScanNameArn.of_json in
      let scanState = field_map json__ "scanState" ScanState.of_json in
      let resourceId = field_map json__ "resourceId" ResourceId.of_json in
      let runId = field_map json__ "runId" Uuid.of_json in
      let scanName = field_map json__ "scanName" ScanName.of_json in
      make ?scanNameArn ?scanState ?resourceId ?runId ?scanName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Use to create a scan using code uploaded to an Amazon S3 bucket."]
module CreateScanRequest =
  struct
    type nonrec t =
      {
      clientToken: ClientToken.t option
        [@ocaml.doc
          "The idempotency token for the request. Amazon CodeGuru Security uses this value to prevent the accidental creation of duplicate scans if there are failures and retries."];
      resourceId: ResourceId.t
        [@ocaml.doc "The identifier for the resource object to be scanned."];
      scanName: ScanName.t
        [@ocaml.doc
          "The unique name that CodeGuru Security uses to track revisions across multiple scans of the same resource. Only allowed for a STANDARD scan type."];
      scanType: ScanType.t option
        [@ocaml.doc
          "The type of scan, either Standard or Express. Defaults to Standard type if missing. Express scans run on limited resources and use a limited set of detectors to analyze your code in near-real time. Standard scans have standard resource limits and use the full set of detectors to analyze your code."];
      analysisType: AnalysisType.t option
        [@ocaml.doc
          "The type of analysis you want CodeGuru Security to perform in the scan, either Security or All. The Security type only generates findings related to security. The All type generates both security findings and quality findings. Defaults to Security type if missing."];
      tags: TagMap.t option
        [@ocaml.doc
          "An array of key-value pairs used to tag a scan. A tag is a custom attribute label with two parts: A tag key. For example, CostCenter, Environment, or Secret. Tag keys are case sensitive. An optional tag value field. For example, 111122223333, Production, or a team name. Omitting the tag value is the same as using an empty string. Tag values are case sensitive."]}
    let context_ = "CreateScanRequest"
    let make ?clientToken =
      fun ?scanType ->
        fun ?analysisType ->
          fun ?tags ->
            fun ~resourceId ->
              fun ~scanName ->
                fun () ->
                  {
                    clientToken;
                    scanType;
                    analysisType;
                    tags;
                    resourceId;
                    scanName
                  }
    let to_value x =
      structure_to_value
        [("clientToken", (Option.map x.clientToken ~f:ClientToken.to_value));
        ("resourceId", (Some (ResourceId.to_value x.resourceId)));
        ("scanName", (Some (ScanName.to_value x.scanName)));
        ("scanType", (Option.map x.scanType ~f:ScanType.to_value));
        ("analysisType",
          (Option.map x.analysisType ~f:AnalysisType.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let analysisType =
        (Option.map ~f:AnalysisType.of_xml)
          (Xml.child xml_arg0 "analysisType") in
      let scanType =
        (Option.map ~f:ScanType.of_xml) (Xml.child xml_arg0 "scanType") in
      let scanName =
        ScanName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "scanName") in
      let resourceId =
        ResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceId") in
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "clientToken") in
      make ?tags ?analysisType ?scanType ~scanName ~resourceId ?clientToken
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagMap.of_json in
      let analysisType = field_map json__ "analysisType" AnalysisType.of_json in
      let scanType = field_map json__ "scanType" ScanType.of_json in
      let scanName = field_map_exn json__ "scanName" ScanName.of_json in
      let resourceId = field_map_exn json__ "resourceId" ResourceId.of_json in
      let clientToken = field_map json__ "clientToken" ClientToken.of_json in
      make ?tags ?analysisType ?scanType ~scanName ~resourceId ?clientToken
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Use to create a scan using code uploaded to an Amazon S3 bucket."]
module BatchGetFindingsResponse =
  struct
    type nonrec t =
      {
      findings: Findings.t option
        [@ocaml.doc
          "A list of all findings which were successfully fetched."];
      failedFindings: BatchGetFindingsErrors.t option
        [@ocaml.doc
          "A list of errors for individual findings which were not fetched. Each BatchGetFindingsError contains the scanName, findingId, errorCode and error message."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?findings =
      fun ?failedFindings -> fun () -> { findings; failedFindings }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("findings", (Option.map x.findings ~f:Findings.to_value));
        ("failedFindings",
          (Option.map x.failedFindings ~f:BatchGetFindingsErrors.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let failedFindings =
        (Option.map ~f:BatchGetFindingsErrors.of_xml)
          (Xml.child xml_arg0 "failedFindings") in
      let findings =
        (Option.map ~f:Findings.of_xml) (Xml.child xml_arg0 "findings") in
      make ?failedFindings ?findings ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let failedFindings =
        field_map json__ "failedFindings" BatchGetFindingsErrors.of_json in
      let findings = field_map json__ "findings" Findings.of_json in
      make ?failedFindings ?findings ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of requested findings from standard scans."]
module BatchGetFindingsRequest =
  struct
    type nonrec t =
      {
      findingIdentifiers: FindingIdentifiers.t
        [@ocaml.doc
          "A list of finding identifiers. Each identifier consists of a scanName and a findingId. You retrieve the findingId when you call GetFindings."]}
    let context_ = "BatchGetFindingsRequest"
    let make ~findingIdentifiers = fun () -> { findingIdentifiers }
    let to_value x =
      structure_to_value
        [("findingIdentifiers",
           (Some (FindingIdentifiers.to_value x.findingIdentifiers)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let findingIdentifiers =
        FindingIdentifiers.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "findingIdentifiers") in
      make ~findingIdentifiers ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let findingIdentifiers =
        field_map_exn json__ "findingIdentifiers" FindingIdentifiers.of_json in
      make ~findingIdentifiers ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of requested findings from standard scans."]
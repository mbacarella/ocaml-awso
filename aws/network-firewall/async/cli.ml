(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_firewall_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and updateToken =
         flag "update-token" (optional string) ~doc:"STRING UpdateToken"
       and firewallArn =
         flag "firewall-arn" (optional string) ~doc:"STRING ResourceArn"
       and firewallName =
         flag "firewall-name" (optional string) ~doc:"STRING ResourceName"
       and firewallPolicyArn =
         flag "firewall-policy-arn" (required string)
           ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_firewall_policy
           (Values.AssociateFirewallPolicyRequest.make ?updateToken
              ?firewallArn ?firewallName ~firewallPolicyArn ())
           (Some Values.AssociateFirewallPolicyResponse.to_json)
           (Some Values.AssociateFirewallPolicyResponse.error_to_json)])
let associate_subnets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and updateToken =
         flag "update-token" (optional string) ~doc:"STRING UpdateToken"
       and firewallArn =
         flag "firewall-arn" (optional string) ~doc:"STRING ResourceArn"
       and firewallName =
         flag "firewall-name" (optional string) ~doc:"STRING ResourceName"
       and subnetMappings =
         flag "subnet-mappings" (required json_arg)
           ~doc:"JSON SubnetMappings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_subnets
           (Values.AssociateSubnetsRequest.make ?updateToken ?firewallArn
              ?firewallName
              ~subnetMappings:(Values.SubnetMappings.of_json subnetMappings)
              ()) (Some Values.AssociateSubnetsResponse.to_json)
           (Some Values.AssociateSubnetsResponse.error_to_json)])
let create_firewall =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deleteProtection =
         flag "delete-protection" (optional bool) ~doc:"BOOL Boolean"
       and subnetChangeProtection =
         flag "subnet-change-protection" (optional bool) ~doc:"BOOL Boolean"
       and firewallPolicyChangeProtection =
         flag "firewall-policy-change-protection" (optional bool)
           ~doc:"BOOL Boolean"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and firewallName =
         flag "firewall-name" (required string) ~doc:"STRING ResourceName"
       and firewallPolicyArn =
         flag "firewall-policy-arn" (required string)
           ~doc:"STRING ResourceArn"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId"
       and subnetMappings =
         flag "subnet-mappings" (required json_arg)
           ~doc:"JSON SubnetMappings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_firewall
           (Values.CreateFirewallRequest.make ?deleteProtection
              ?subnetChangeProtection ?firewallPolicyChangeProtection
              ?description ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~firewallName ~firewallPolicyArn ~vpcId
              ~subnetMappings:(Values.SubnetMappings.of_json subnetMappings)
              ()) (Some Values.CreateFirewallResponse.to_json)
           (Some Values.CreateFirewallResponse.error_to_json)])
let create_firewall_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and firewallPolicyName =
         flag "firewall-policy-name" (required string)
           ~doc:"STRING ResourceName"
       and firewallPolicy =
         flag "firewall-policy" (required json_arg)
           ~doc:"JSON FirewallPolicy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_firewall_policy
           (Values.CreateFirewallPolicyRequest.make ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?dryRun
              ~firewallPolicyName
              ~firewallPolicy:(Values.FirewallPolicy.of_json firewallPolicy)
              ()) (Some Values.CreateFirewallPolicyResponse.to_json)
           (Some Values.CreateFirewallPolicyResponse.error_to_json)])
let create_rule_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleGroup =
         flag "rule-group" (optional json_arg) ~doc:"JSON RuleGroup"
       and rules = flag "rules" (optional string) ~doc:"STRING RulesString"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and ruleGroupName =
         flag "rule-group-name" (required string) ~doc:"STRING ResourceName"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON RuleGroupType"
       and capacity = flag "capacity" (required int) ~doc:"INT RuleCapacity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_rule_group
           (Values.CreateRuleGroupRequest.make
              ?ruleGroup:(Option.map ~f:Values.RuleGroup.of_json ruleGroup)
              ?rules ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?dryRun
              ~ruleGroupName ~type_:(Values.RuleGroupType.of_json type_)
              ~capacity ()) (Some Values.CreateRuleGroupResponse.to_json)
           (Some Values.CreateRuleGroupResponse.error_to_json)])
let delete_firewall =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and firewallName =
         flag "firewall-name" (optional string) ~doc:"STRING ResourceName"
       and firewallArn =
         flag "firewall-arn" (optional string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_firewall
           (Values.DeleteFirewallRequest.make ?firewallName ?firewallArn ())
           (Some Values.DeleteFirewallResponse.to_json)
           (Some Values.DeleteFirewallResponse.error_to_json)])
let delete_firewall_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and firewallPolicyName =
         flag "firewall-policy-name" (optional string)
           ~doc:"STRING ResourceName"
       and firewallPolicyArn =
         flag "firewall-policy-arn" (optional string)
           ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_firewall_policy
           (Values.DeleteFirewallPolicyRequest.make ?firewallPolicyName
              ?firewallPolicyArn ())
           (Some Values.DeleteFirewallPolicyResponse.to_json)
           (Some Values.DeleteFirewallPolicyResponse.error_to_json)])
let delete_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_policy
           (Values.DeleteResourcePolicyRequest.make ~resourceArn ())
           (Some Values.DeleteResourcePolicyResponse.to_json)
           (Some Values.DeleteResourcePolicyResponse.error_to_json)])
let delete_rule_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleGroupName =
         flag "rule-group-name" (optional string) ~doc:"STRING ResourceName"
       and ruleGroupArn =
         flag "rule-group-arn" (optional string) ~doc:"STRING ResourceArn"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON RuleGroupType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_rule_group
           (Values.DeleteRuleGroupRequest.make ?ruleGroupName ?ruleGroupArn
              ?type_:(Option.map ~f:Values.RuleGroupType.of_json type_) ())
           (Some Values.DeleteRuleGroupResponse.to_json)
           (Some Values.DeleteRuleGroupResponse.error_to_json)])
let describe_firewall =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and firewallName =
         flag "firewall-name" (optional string) ~doc:"STRING ResourceName"
       and firewallArn =
         flag "firewall-arn" (optional string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_firewall
           (Values.DescribeFirewallRequest.make ?firewallName ?firewallArn ())
           (Some Values.DescribeFirewallResponse.to_json)
           (Some Values.DescribeFirewallResponse.error_to_json)])
let describe_firewall_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and firewallPolicyName =
         flag "firewall-policy-name" (optional string)
           ~doc:"STRING ResourceName"
       and firewallPolicyArn =
         flag "firewall-policy-arn" (optional string)
           ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_firewall_policy
           (Values.DescribeFirewallPolicyRequest.make ?firewallPolicyName
              ?firewallPolicyArn ())
           (Some Values.DescribeFirewallPolicyResponse.to_json)
           (Some Values.DescribeFirewallPolicyResponse.error_to_json)])
let describe_logging_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and firewallArn =
         flag "firewall-arn" (optional string) ~doc:"STRING ResourceArn"
       and firewallName =
         flag "firewall-name" (optional string) ~doc:"STRING ResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_logging_configuration
           (Values.DescribeLoggingConfigurationRequest.make ?firewallArn
              ?firewallName ())
           (Some Values.DescribeLoggingConfigurationResponse.to_json)
           (Some Values.DescribeLoggingConfigurationResponse.error_to_json)])
let describe_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_resource_policy
           (Values.DescribeResourcePolicyRequest.make ~resourceArn ())
           (Some Values.DescribeResourcePolicyResponse.to_json)
           (Some Values.DescribeResourcePolicyResponse.error_to_json)])
let describe_rule_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleGroupName =
         flag "rule-group-name" (optional string) ~doc:"STRING ResourceName"
       and ruleGroupArn =
         flag "rule-group-arn" (optional string) ~doc:"STRING ResourceArn"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON RuleGroupType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_rule_group
           (Values.DescribeRuleGroupRequest.make ?ruleGroupName ?ruleGroupArn
              ?type_:(Option.map ~f:Values.RuleGroupType.of_json type_) ())
           (Some Values.DescribeRuleGroupResponse.to_json)
           (Some Values.DescribeRuleGroupResponse.error_to_json)])
let describe_rule_group_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleGroupName =
         flag "rule-group-name" (optional string) ~doc:"STRING ResourceName"
       and ruleGroupArn =
         flag "rule-group-arn" (optional string) ~doc:"STRING ResourceArn"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON RuleGroupType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_rule_group_metadata
           (Values.DescribeRuleGroupMetadataRequest.make ?ruleGroupName
              ?ruleGroupArn
              ?type_:(Option.map ~f:Values.RuleGroupType.of_json type_) ())
           (Some Values.DescribeRuleGroupMetadataResponse.to_json)
           (Some Values.DescribeRuleGroupMetadataResponse.error_to_json)])
let disassociate_subnets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and updateToken =
         flag "update-token" (optional string) ~doc:"STRING UpdateToken"
       and firewallArn =
         flag "firewall-arn" (optional string) ~doc:"STRING ResourceArn"
       and firewallName =
         flag "firewall-name" (optional string) ~doc:"STRING ResourceName"
       and subnetIds =
         flag "subnet-ids" (required json_arg) ~doc:"JSON AzSubnets" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_subnets
           (Values.DisassociateSubnetsRequest.make ?updateToken ?firewallArn
              ?firewallName ~subnetIds:(Values.AzSubnets.of_json subnetIds)
              ()) (Some Values.DisassociateSubnetsResponse.to_json)
           (Some Values.DisassociateSubnetsResponse.error_to_json)])
let list_firewall_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT PaginationMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_firewall_policies
           (Values.ListFirewallPoliciesRequest.make ?nextToken ?maxResults ())
           (Some Values.ListFirewallPoliciesResponse.to_json)
           (Some Values.ListFirewallPoliciesResponse.error_to_json)])
let list_firewalls =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and vpcIds = flag "vpc-ids" (optional json_arg) ~doc:"JSON VpcIds"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT PaginationMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_firewalls
           (Values.ListFirewallsRequest.make ?nextToken
              ?vpcIds:(Option.map ~f:Values.VpcIds.of_json vpcIds)
              ?maxResults ()) (Some Values.ListFirewallsResponse.to_json)
           (Some Values.ListFirewallsResponse.error_to_json)])
let list_rule_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT PaginationMaxResults"
       and scope =
         flag "scope" (optional json_arg) ~doc:"JSON ResourceManagedStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_rule_groups
           (Values.ListRuleGroupsRequest.make ?nextToken ?maxResults
              ?scope:(Option.map ~f:Values.ResourceManagedStatus.of_json
                        scope) ())
           (Some Values.ListRuleGroupsResponse.to_json)
           (Some Values.ListRuleGroupsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT TagsPaginationMaxResults"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ?nextToken ?maxResults
              ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and policy =
         flag "policy" (required string) ~doc:"STRING PolicyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_resource_policy
           (Values.PutResourcePolicyRequest.make ~resourceArn ~policy ())
           (Some Values.PutResourcePolicyResponse.to_json)
           (Some Values.PutResourcePolicyResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_firewall_delete_protection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and updateToken =
         flag "update-token" (optional string) ~doc:"STRING UpdateToken"
       and firewallArn =
         flag "firewall-arn" (optional string) ~doc:"STRING ResourceArn"
       and firewallName =
         flag "firewall-name" (optional string) ~doc:"STRING ResourceName"
       and deleteProtection =
         flag "delete-protection" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_firewall_delete_protection
           (Values.UpdateFirewallDeleteProtectionRequest.make ?updateToken
              ?firewallArn ?firewallName ~deleteProtection ())
           (Some Values.UpdateFirewallDeleteProtectionResponse.to_json)
           (Some Values.UpdateFirewallDeleteProtectionResponse.error_to_json)])
let update_firewall_description =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and updateToken =
         flag "update-token" (optional string) ~doc:"STRING UpdateToken"
       and firewallArn =
         flag "firewall-arn" (optional string) ~doc:"STRING ResourceArn"
       and firewallName =
         flag "firewall-name" (optional string) ~doc:"STRING ResourceName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_firewall_description
           (Values.UpdateFirewallDescriptionRequest.make ?updateToken
              ?firewallArn ?firewallName ?description ())
           (Some Values.UpdateFirewallDescriptionResponse.to_json)
           (Some Values.UpdateFirewallDescriptionResponse.error_to_json)])
let update_firewall_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and firewallPolicyArn =
         flag "firewall-policy-arn" (optional string)
           ~doc:"STRING ResourceArn"
       and firewallPolicyName =
         flag "firewall-policy-name" (optional string)
           ~doc:"STRING ResourceName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and updateToken =
         flag "update-token" (required string) ~doc:"STRING UpdateToken"
       and firewallPolicy =
         flag "firewall-policy" (required json_arg)
           ~doc:"JSON FirewallPolicy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_firewall_policy
           (Values.UpdateFirewallPolicyRequest.make ?firewallPolicyArn
              ?firewallPolicyName ?description ?dryRun ~updateToken
              ~firewallPolicy:(Values.FirewallPolicy.of_json firewallPolicy)
              ()) (Some Values.UpdateFirewallPolicyResponse.to_json)
           (Some Values.UpdateFirewallPolicyResponse.error_to_json)])
let update_firewall_policy_change_protection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and updateToken =
         flag "update-token" (optional string) ~doc:"STRING UpdateToken"
       and firewallArn =
         flag "firewall-arn" (optional string) ~doc:"STRING ResourceArn"
       and firewallName =
         flag "firewall-name" (optional string) ~doc:"STRING ResourceName"
       and firewallPolicyChangeProtection =
         flag "firewall-policy-change-protection" (required bool)
           ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_firewall_policy_change_protection
           (Values.UpdateFirewallPolicyChangeProtectionRequest.make
              ?updateToken ?firewallArn ?firewallName
              ~firewallPolicyChangeProtection ())
           (Some Values.UpdateFirewallPolicyChangeProtectionResponse.to_json)
           (Some
              Values.UpdateFirewallPolicyChangeProtectionResponse.error_to_json)])
let update_logging_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and firewallArn =
         flag "firewall-arn" (optional string) ~doc:"STRING ResourceArn"
       and firewallName =
         flag "firewall-name" (optional string) ~doc:"STRING ResourceName"
       and loggingConfiguration =
         flag "logging-configuration" (optional json_arg)
           ~doc:"JSON LoggingConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_logging_configuration
           (Values.UpdateLoggingConfigurationRequest.make ?firewallArn
              ?firewallName
              ?loggingConfiguration:(Option.map
                                       ~f:Values.LoggingConfiguration.of_json
                                       loggingConfiguration) ())
           (Some Values.UpdateLoggingConfigurationResponse.to_json)
           (Some Values.UpdateLoggingConfigurationResponse.error_to_json)])
let update_rule_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleGroupArn =
         flag "rule-group-arn" (optional string) ~doc:"STRING ResourceArn"
       and ruleGroupName =
         flag "rule-group-name" (optional string) ~doc:"STRING ResourceName"
       and ruleGroup =
         flag "rule-group" (optional json_arg) ~doc:"JSON RuleGroup"
       and rules = flag "rules" (optional string) ~doc:"STRING RulesString"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON RuleGroupType"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and updateToken =
         flag "update-token" (required string) ~doc:"STRING UpdateToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_rule_group
           (Values.UpdateRuleGroupRequest.make ?ruleGroupArn ?ruleGroupName
              ?ruleGroup:(Option.map ~f:Values.RuleGroup.of_json ruleGroup)
              ?rules
              ?type_:(Option.map ~f:Values.RuleGroupType.of_json type_)
              ?description ?dryRun ~updateToken ())
           (Some Values.UpdateRuleGroupResponse.to_json)
           (Some Values.UpdateRuleGroupResponse.error_to_json)])
let update_subnet_change_protection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and updateToken =
         flag "update-token" (optional string) ~doc:"STRING UpdateToken"
       and firewallArn =
         flag "firewall-arn" (optional string) ~doc:"STRING ResourceArn"
       and firewallName =
         flag "firewall-name" (optional string) ~doc:"STRING ResourceName"
       and subnetChangeProtection =
         flag "subnet-change-protection" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_subnet_change_protection
           (Values.UpdateSubnetChangeProtectionRequest.make ?updateToken
              ?firewallArn ?firewallName ~subnetChangeProtection ())
           (Some Values.UpdateSubnetChangeProtectionResponse.to_json)
           (Some Values.UpdateSubnetChangeProtectionResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-firewall-policy", associate_firewall_policy);
    ("associate-subnets", associate_subnets);
    ("create-firewall", create_firewall);
    ("create-firewall-policy", create_firewall_policy);
    ("create-rule-group", create_rule_group);
    ("delete-firewall", delete_firewall);
    ("delete-firewall-policy", delete_firewall_policy);
    ("delete-resource-policy", delete_resource_policy);
    ("delete-rule-group", delete_rule_group);
    ("describe-firewall", describe_firewall);
    ("describe-firewall-policy", describe_firewall_policy);
    ("describe-logging-configuration", describe_logging_configuration);
    ("describe-resource-policy", describe_resource_policy);
    ("describe-rule-group", describe_rule_group);
    ("describe-rule-group-metadata", describe_rule_group_metadata);
    ("disassociate-subnets", disassociate_subnets);
    ("list-firewall-policies", list_firewall_policies);
    ("list-firewalls", list_firewalls);
    ("list-rule-groups", list_rule_groups);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-resource-policy", put_resource_policy);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-firewall-delete-protection", update_firewall_delete_protection);
    ("update-firewall-description", update_firewall_description);
    ("update-firewall-policy", update_firewall_policy);
    ("update-firewall-policy-change-protection",
      update_firewall_policy_change_protection);
    ("update-logging-configuration", update_logging_configuration);
    ("update-rule-group", update_rule_group);
    ("update-subnet-change-protection", update_subnet_change_protection)]
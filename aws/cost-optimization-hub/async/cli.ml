(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let get_preferences =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_preferences (Values.GetPreferencesRequest.make ())
           (Some Values.GetPreferencesResponse.to_json)
           (Some Values.GetPreferencesResponse.error_to_json)])
let get_recommendation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and recommendationId =
         flag "recommendation-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_recommendation
           (Values.GetRecommendationRequest.make ~recommendationId ())
           (Some Values.GetRecommendationResponse.to_json)
           (Some Values.GetRecommendationResponse.error_to_json)])
let list_efficiency_metrics =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupBy = flag "group-by" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListEfficiencyMetricsRequestMaxResultsInteger"
       and orderBy = flag "order-by" (optional json_arg) ~doc:"JSON OrderBy"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and granularity =
         flag "granularity" (required json_arg) ~doc:"JSON GranularityType"
       and timePeriod =
         flag "time-period" (required json_arg) ~doc:"JSON TimePeriod" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_efficiency_metrics
           (Values.ListEfficiencyMetricsRequest.make ?groupBy ?maxResults
              ?orderBy:(Option.map ~f:Values.OrderBy.of_json orderBy)
              ?nextToken
              ~granularity:(Values.GranularityType.of_json granularity)
              ~timePeriod:(Values.TimePeriod.of_json timePeriod) ())
           (Some Values.ListEfficiencyMetricsResponse.to_json)
           (Some Values.ListEfficiencyMetricsResponse.error_to_json)])
let list_enrollment_statuses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeOrganizationInfo =
         flag "include-organization-info" (optional bool)
           ~doc:"BOOL PrimitiveBoolean"
       and accountId =
         flag "account-id" (optional string) ~doc:"STRING AccountId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_enrollment_statuses
           (Values.ListEnrollmentStatusesRequest.make
              ?includeOrganizationInfo ?accountId ?nextToken ?maxResults ())
           (Some Values.ListEnrollmentStatusesResponse.to_json)
           (Some Values.ListEnrollmentStatusesResponse.error_to_json)])
let list_recommendation_summaries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON Filter"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListRecommendationSummariesRequestMaxResultsInteger"
       and metrics =
         flag "metrics" (optional json_arg) ~doc:"JSON SummaryMetricsList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and groupBy = flag "group-by" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_recommendation_summaries
           (Values.ListRecommendationSummariesRequest.make
              ?filter:(Option.map ~f:Values.Filter.of_json filter)
              ?maxResults
              ?metrics:(Option.map ~f:Values.SummaryMetricsList.of_json
                          metrics) ?nextToken ~groupBy ())
           (Some Values.ListRecommendationSummariesResponse.to_json)
           (Some Values.ListRecommendationSummariesResponse.error_to_json)])
let list_recommendations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON Filter"
       and orderBy = flag "order-by" (optional json_arg) ~doc:"JSON OrderBy"
       and includeAllRecommendations =
         flag "include-all-recommendations" (optional bool)
           ~doc:"BOOL PrimitiveBoolean"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListRecommendationsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_recommendations
           (Values.ListRecommendationsRequest.make
              ?filter:(Option.map ~f:Values.Filter.of_json filter)
              ?orderBy:(Option.map ~f:Values.OrderBy.of_json orderBy)
              ?includeAllRecommendations ?maxResults ?nextToken ())
           (Some Values.ListRecommendationsResponse.to_json)
           (Some Values.ListRecommendationsResponse.error_to_json)])
let update_enrollment_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeMemberAccounts =
         flag "include-member-accounts" (optional bool) ~doc:"BOOL Boolean"
       and status =
         flag "status" (required json_arg) ~doc:"JSON EnrollmentStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_enrollment_status
           (Values.UpdateEnrollmentStatusRequest.make ?includeMemberAccounts
              ~status:(Values.EnrollmentStatus.of_json status) ())
           (Some Values.UpdateEnrollmentStatusResponse.to_json)
           (Some Values.UpdateEnrollmentStatusResponse.error_to_json)])
let update_preferences =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and savingsEstimationMode =
         flag "savings-estimation-mode" (optional json_arg)
           ~doc:"JSON SavingsEstimationMode"
       and memberAccountDiscountVisibility =
         flag "member-account-discount-visibility" (optional json_arg)
           ~doc:"JSON MemberAccountDiscountVisibility"
       and preferredCommitment =
         flag "preferred-commitment" (optional json_arg)
           ~doc:"JSON PreferredCommitment" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_preferences
           (Values.UpdatePreferencesRequest.make
              ?savingsEstimationMode:(Option.map
                                        ~f:Values.SavingsEstimationMode.of_json
                                        savingsEstimationMode)
              ?memberAccountDiscountVisibility:(Option.map
                                                  ~f:Values.MemberAccountDiscountVisibility.of_json
                                                  memberAccountDiscountVisibility)
              ?preferredCommitment:(Option.map
                                      ~f:Values.PreferredCommitment.of_json
                                      preferredCommitment) ())
           (Some Values.UpdatePreferencesResponse.to_json)
           (Some Values.UpdatePreferencesResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("get-preferences", get_preferences);
    ("get-recommendation", get_recommendation);
    ("list-efficiency-metrics", list_efficiency_metrics);
    ("list-enrollment-statuses", list_enrollment_statuses);
    ("list-recommendation-summaries", list_recommendation_summaries);
    ("list-recommendations", list_recommendations);
    ("update-enrollment-status", update_enrollment_status);
    ("update-preferences", update_preferences)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.cost_optimization_hub
let apiVersion = "2022-07-26"
let endpointPrefix = "cost-optimization-hub"
let serviceFullName = "Cost Optimization Hub"
let signatureVersion = "v4"
let protocol = "json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let targetPrefix = "CostOptimizationHubService"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module Double =
  struct
    type nonrec t = float
    let make i = i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module EstimatedDiscounts =
  struct
    type nonrec t =
      {
      savingsPlansDiscount: Double.t option
        [@ocaml.doc "Estimated Savings Plans discounts."];
      reservedInstancesDiscount: Double.t option
        [@ocaml.doc "Estimated reserved instance discounts."];
      otherDiscount: Double.t option
        [@ocaml.doc
          "Estimated other discounts include all discounts that are not itemized. Itemized discounts include reservedInstanceDiscount and savingsPlansDiscount."]}
    let make ?savingsPlansDiscount =
      fun ?reservedInstancesDiscount ->
        fun ?otherDiscount ->
          fun () ->
            { savingsPlansDiscount; reservedInstancesDiscount; otherDiscount
            }
    let to_value x =
      structure_to_value
        [("savingsPlansDiscount",
           (Option.map x.savingsPlansDiscount ~f:Double.to_value));
        ("reservedInstancesDiscount",
          (Option.map x.reservedInstancesDiscount ~f:Double.to_value));
        ("otherDiscount", (Option.map x.otherDiscount ~f:Double.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let otherDiscount =
        (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "otherDiscount") in
      let reservedInstancesDiscount =
        (Option.map ~f:Double.of_xml)
          (Xml.child xml_arg0 "reservedInstancesDiscount") in
      let savingsPlansDiscount =
        (Option.map ~f:Double.of_xml)
          (Xml.child xml_arg0 "savingsPlansDiscount") in
      make ?otherDiscount ?reservedInstancesDiscount ?savingsPlansDiscount ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let otherDiscount = field_map json__ "otherDiscount" Double.of_json in
      let reservedInstancesDiscount =
        field_map json__ "reservedInstancesDiscount" Double.of_json in
      let savingsPlansDiscount =
        field_map json__ "savingsPlansDiscount" Double.of_json in
      make ?otherDiscount ?reservedInstancesDiscount ?savingsPlansDiscount ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Estimated discount details of the current and recommended resource configuration for a recommendation."]
module Usage =
  struct
    type nonrec t =
      {
      usageType: String_.t option [@ocaml.doc "The usage type."];
      usageAmount: Double.t option [@ocaml.doc "The usage amount."];
      operation: String_.t option [@ocaml.doc "The operation value."];
      productCode: String_.t option [@ocaml.doc "The product code."];
      unit: String_.t option [@ocaml.doc "The usage unit."]}
    let make ?usageType =
      fun ?usageAmount ->
        fun ?operation ->
          fun ?productCode ->
            fun ?unit ->
              fun () ->
                { usageType; usageAmount; operation; productCode; unit }
    let to_value x =
      structure_to_value
        [("usageType", (Option.map x.usageType ~f:String_.to_value));
        ("usageAmount", (Option.map x.usageAmount ~f:Double.to_value));
        ("operation", (Option.map x.operation ~f:String_.to_value));
        ("productCode", (Option.map x.productCode ~f:String_.to_value));
        ("unit", (Option.map x.unit ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unit = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "unit") in
      let productCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "productCode") in
      let operation =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "operation") in
      let usageAmount =
        (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "usageAmount") in
      let usageType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "usageType") in
      make ?unit ?productCode ?operation ?usageAmount ?usageType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let unit = field_map json__ "unit" String_.of_json in
      let productCode = field_map json__ "productCode" String_.of_json in
      let operation = field_map json__ "operation" String_.of_json in
      let usageAmount = field_map json__ "usageAmount" Double.of_json in
      let usageType = field_map json__ "usageType" String_.of_json in
      make ?unit ?productCode ?operation ?usageAmount ?usageType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Details about the usage."]
module MixedInstanceConfiguration =
  struct
    type nonrec t =
      {
      type_: String_.t option
        [@ocaml.doc "The instance type of the configuration."]}
    let make ?type_ = fun () -> { type_ }
    let to_value x =
      structure_to_value [("type", (Option.map x.type_ ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "type") in
      make ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" String_.of_json in make ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The configuration for the EC2 Auto Scaling group with mixed instance types."]
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Tag =
  struct
    type nonrec t =
      {
      key: String_.t option
        [@ocaml.doc "The key that's associated with the tag."];
      value: String_.t option
        [@ocaml.doc "The value that's associated with the tag."]}
    let make ?key = fun ?value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("key", (Option.map x.key ~f:String_.to_value));
        ("value", (Option.map x.value ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "value") in
      let key = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "key") in
      make ?value ?key ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let value = field_map json__ "value" String_.of_json in
      let key = field_map json__ "key" String_.of_json in make ?value ?key ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The tag structure that contains a tag key and value."]
module MetricsByTime =
  struct
    type nonrec t =
      {
      score: Double.t option
        [@ocaml.doc
          "The efficiency score for this time period. The score represents a measure of how effectively the cloud resources are being optimized, with higher scores indicating better optimization performance."];
      savings: Double.t option
        [@ocaml.doc
          "The estimated savings amount for this time period, representing the potential cost reduction achieved through optimization recommendations."];
      spend: Double.t option
        [@ocaml.doc "The total spending amount for this time period."];
      timestamp: String_.t option
        [@ocaml.doc
          "The timestamp for this data point. The format depends on the granularity: YYYY-MM-DD for daily metrics, or YYYY-MM for monthly metrics."]}
    let make ?score =
      fun ?savings ->
        fun ?spend ->
          fun ?timestamp -> fun () -> { score; savings; spend; timestamp }
    let to_value x =
      structure_to_value
        [("score", (Option.map x.score ~f:Double.to_value));
        ("savings", (Option.map x.savings ~f:Double.to_value));
        ("spend", (Option.map x.spend ~f:Double.to_value));
        ("timestamp", (Option.map x.timestamp ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timestamp =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "timestamp") in
      let spend = (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "spend") in
      let savings =
        (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "savings") in
      let score = (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "score") in
      make ?timestamp ?spend ?savings ?score ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let timestamp = field_map json__ "timestamp" String_.of_json in
      let spend = field_map json__ "spend" Double.of_json in
      let savings = field_map json__ "savings" Double.of_json in
      let score = field_map json__ "score" Double.of_json in
      make ?timestamp ?spend ?savings ?score ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains efficiency metrics for a specific point in time, including an efficiency score, potential savings, optimizable spend, and timestamp."]
module ResourcePricing =
  struct
    type nonrec t =
      {
      estimatedCostBeforeDiscounts: Double.t option
        [@ocaml.doc
          "The savings estimate using Amazon Web Services public pricing without incorporating any discounts."];
      estimatedNetUnusedAmortizedCommitments: Double.t option
        [@ocaml.doc
          "The estimated net unused amortized commitment for the recommendation."];
      estimatedDiscounts: EstimatedDiscounts.t option
        [@ocaml.doc "The estimated discounts for a recommendation."];
      estimatedCostAfterDiscounts: Double.t option
        [@ocaml.doc
          "The savings estimate incorporating all discounts with Amazon Web Services, such as Reserved Instances and Savings Plans."]}
    let make ?estimatedCostBeforeDiscounts =
      fun ?estimatedNetUnusedAmortizedCommitments ->
        fun ?estimatedDiscounts ->
          fun ?estimatedCostAfterDiscounts ->
            fun () ->
              {
                estimatedCostBeforeDiscounts;
                estimatedNetUnusedAmortizedCommitments;
                estimatedDiscounts;
                estimatedCostAfterDiscounts
              }
    let to_value x =
      structure_to_value
        [("estimatedCostBeforeDiscounts",
           (Option.map x.estimatedCostBeforeDiscounts ~f:Double.to_value));
        ("estimatedNetUnusedAmortizedCommitments",
          (Option.map x.estimatedNetUnusedAmortizedCommitments
             ~f:Double.to_value));
        ("estimatedDiscounts",
          (Option.map x.estimatedDiscounts ~f:EstimatedDiscounts.to_value));
        ("estimatedCostAfterDiscounts",
          (Option.map x.estimatedCostAfterDiscounts ~f:Double.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let estimatedCostAfterDiscounts =
        (Option.map ~f:Double.of_xml)
          (Xml.child xml_arg0 "estimatedCostAfterDiscounts") in
      let estimatedDiscounts =
        (Option.map ~f:EstimatedDiscounts.of_xml)
          (Xml.child xml_arg0 "estimatedDiscounts") in
      let estimatedNetUnusedAmortizedCommitments =
        (Option.map ~f:Double.of_xml)
          (Xml.child xml_arg0 "estimatedNetUnusedAmortizedCommitments") in
      let estimatedCostBeforeDiscounts =
        (Option.map ~f:Double.of_xml)
          (Xml.child xml_arg0 "estimatedCostBeforeDiscounts") in
      make ?estimatedCostAfterDiscounts ?estimatedDiscounts
        ?estimatedNetUnusedAmortizedCommitments ?estimatedCostBeforeDiscounts
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let estimatedCostAfterDiscounts =
        field_map json__ "estimatedCostAfterDiscounts" Double.of_json in
      let estimatedDiscounts =
        field_map json__ "estimatedDiscounts" EstimatedDiscounts.of_json in
      let estimatedNetUnusedAmortizedCommitments =
        field_map json__ "estimatedNetUnusedAmortizedCommitments"
          Double.of_json in
      let estimatedCostBeforeDiscounts =
        field_map json__ "estimatedCostBeforeDiscounts" Double.of_json in
      make ?estimatedCostAfterDiscounts ?estimatedDiscounts
        ?estimatedNetUnusedAmortizedCommitments ?estimatedCostBeforeDiscounts
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains pricing information about the specified resource."]
module UsageList =
  struct
    type nonrec t = Usage.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Usage.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Usage.of_xml)
    let of_json j = list_of_json ~kind:"UsageList" ~of_json:Usage.of_json j
    let to_json v = composed_to_json to_value v
  end
module SavingsPlansPricing =
  struct
    type nonrec t =
      {
      monthlySavingsPlansEligibleCost: Double.t option
        [@ocaml.doc
          "The cost of paying for the recommended Savings Plans monthly."];
      estimatedMonthlyCommitment: Double.t option
        [@ocaml.doc "Estimated monthly commitment for the Savings Plans."];
      savingsPercentage: Double.t option
        [@ocaml.doc
          "Estimated savings as a percentage of your overall costs after buying the Savings Plans."];
      estimatedOnDemandCost: Double.t option
        [@ocaml.doc
          "Estimated On-Demand cost you will pay after buying the Savings Plans."]}
    let make ?monthlySavingsPlansEligibleCost =
      fun ?estimatedMonthlyCommitment ->
        fun ?savingsPercentage ->
          fun ?estimatedOnDemandCost ->
            fun () ->
              {
                monthlySavingsPlansEligibleCost;
                estimatedMonthlyCommitment;
                savingsPercentage;
                estimatedOnDemandCost
              }
    let to_value x =
      structure_to_value
        [("monthlySavingsPlansEligibleCost",
           (Option.map x.monthlySavingsPlansEligibleCost ~f:Double.to_value));
        ("estimatedMonthlyCommitment",
          (Option.map x.estimatedMonthlyCommitment ~f:Double.to_value));
        ("savingsPercentage",
          (Option.map x.savingsPercentage ~f:Double.to_value));
        ("estimatedOnDemandCost",
          (Option.map x.estimatedOnDemandCost ~f:Double.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let estimatedOnDemandCost =
        (Option.map ~f:Double.of_xml)
          (Xml.child xml_arg0 "estimatedOnDemandCost") in
      let savingsPercentage =
        (Option.map ~f:Double.of_xml)
          (Xml.child xml_arg0 "savingsPercentage") in
      let estimatedMonthlyCommitment =
        (Option.map ~f:Double.of_xml)
          (Xml.child xml_arg0 "estimatedMonthlyCommitment") in
      let monthlySavingsPlansEligibleCost =
        (Option.map ~f:Double.of_xml)
          (Xml.child xml_arg0 "monthlySavingsPlansEligibleCost") in
      make ?estimatedOnDemandCost ?savingsPercentage
        ?estimatedMonthlyCommitment ?monthlySavingsPlansEligibleCost ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let estimatedOnDemandCost =
        field_map json__ "estimatedOnDemandCost" Double.of_json in
      let savingsPercentage =
        field_map json__ "savingsPercentage" Double.of_json in
      let estimatedMonthlyCommitment =
        field_map json__ "estimatedMonthlyCommitment" Double.of_json in
      let monthlySavingsPlansEligibleCost =
        field_map json__ "monthlySavingsPlansEligibleCost" Double.of_json in
      make ?estimatedOnDemandCost ?savingsPercentage
        ?estimatedMonthlyCommitment ?monthlySavingsPlansEligibleCost ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Pricing information about a Savings Plans."]
module ReservedInstancesPricing =
  struct
    type nonrec t =
      {
      estimatedOnDemandCost: Double.t option
        [@ocaml.doc
          "The remaining On-Demand cost estimated to not be covered by the recommended reserved instance, over the length of the lookback period."];
      monthlyReservationEligibleCost: Double.t option
        [@ocaml.doc
          "The cost of paying for the recommended reserved instance monthly."];
      savingsPercentage: Double.t option
        [@ocaml.doc
          "The savings percentage relative to the total On-Demand costs that are associated with this instance."];
      estimatedMonthlyAmortizedReservationCost: Double.t option
        [@ocaml.doc
          "The estimated cost of your recurring monthly fees for the recommended reserved instance across the month."]}
    let make ?estimatedOnDemandCost =
      fun ?monthlyReservationEligibleCost ->
        fun ?savingsPercentage ->
          fun ?estimatedMonthlyAmortizedReservationCost ->
            fun () ->
              {
                estimatedOnDemandCost;
                monthlyReservationEligibleCost;
                savingsPercentage;
                estimatedMonthlyAmortizedReservationCost
              }
    let to_value x =
      structure_to_value
        [("estimatedOnDemandCost",
           (Option.map x.estimatedOnDemandCost ~f:Double.to_value));
        ("monthlyReservationEligibleCost",
          (Option.map x.monthlyReservationEligibleCost ~f:Double.to_value));
        ("savingsPercentage",
          (Option.map x.savingsPercentage ~f:Double.to_value));
        ("estimatedMonthlyAmortizedReservationCost",
          (Option.map x.estimatedMonthlyAmortizedReservationCost
             ~f:Double.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let estimatedMonthlyAmortizedReservationCost =
        (Option.map ~f:Double.of_xml)
          (Xml.child xml_arg0 "estimatedMonthlyAmortizedReservationCost") in
      let savingsPercentage =
        (Option.map ~f:Double.of_xml)
          (Xml.child xml_arg0 "savingsPercentage") in
      let monthlyReservationEligibleCost =
        (Option.map ~f:Double.of_xml)
          (Xml.child xml_arg0 "monthlyReservationEligibleCost") in
      let estimatedOnDemandCost =
        (Option.map ~f:Double.of_xml)
          (Xml.child xml_arg0 "estimatedOnDemandCost") in
      make ?estimatedMonthlyAmortizedReservationCost ?savingsPercentage
        ?monthlyReservationEligibleCost ?estimatedOnDemandCost ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let estimatedMonthlyAmortizedReservationCost =
        field_map json__ "estimatedMonthlyAmortizedReservationCost"
          Double.of_json in
      let savingsPercentage =
        field_map json__ "savingsPercentage" Double.of_json in
      let monthlyReservationEligibleCost =
        field_map json__ "monthlyReservationEligibleCost" Double.of_json in
      let estimatedOnDemandCost =
        field_map json__ "estimatedOnDemandCost" Double.of_json in
      make ?estimatedMonthlyAmortizedReservationCost ?savingsPercentage
        ?monthlyReservationEligibleCost ?estimatedOnDemandCost ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Pricing details for your recommended reserved instance."]
module BlockStoragePerformanceConfiguration =
  struct
    type nonrec t =
      {
      iops: Double.t option
        [@ocaml.doc "The number of I/O operations per second."];
      throughput: Double.t option
        [@ocaml.doc "The throughput that the volume supports."]}
    let make ?iops = fun ?throughput -> fun () -> { iops; throughput }
    let to_value x =
      structure_to_value
        [("iops", (Option.map x.iops ~f:Double.to_value));
        ("throughput", (Option.map x.throughput ~f:Double.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let throughput =
        (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "throughput") in
      let iops = (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "iops") in
      make ?throughput ?iops ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let throughput = field_map json__ "throughput" Double.of_json in
      let iops = field_map json__ "iops" Double.of_json in
      make ?throughput ?iops ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the Amazon Elastic Block Store performance configuration of the current and recommended resource configuration for a recommendation."]
module StorageConfiguration =
  struct
    type nonrec t =
      {
      type_: String_.t option [@ocaml.doc "The storage type."];
      sizeInGb: Double.t option [@ocaml.doc "The storage volume."]}
    let make ?type_ = fun ?sizeInGb -> fun () -> { type_; sizeInGb }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:String_.to_value));
        ("sizeInGb", (Option.map x.sizeInGb ~f:Double.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sizeInGb =
        (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "sizeInGb") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "type") in
      make ?sizeInGb ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sizeInGb = field_map json__ "sizeInGb" Double.of_json in
      let type_ = field_map json__ "type" String_.of_json in
      make ?sizeInGb ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The storage configuration used for recommendations."]
module AllocationStrategy =
  struct
    type nonrec t =
      | Prioritized 
      | LowestPrice 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Prioritized -> "Prioritized"
      | LowestPrice -> "LowestPrice"
      | Non_static_id s -> s
    let of_string =
      function
      | "Prioritized" -> Prioritized
      | "LowestPrice" -> LowestPrice
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration AllocationStrategy" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AllocationStrategy" j)
    let to_json = simple_to_json to_value
  end
module Ec2AutoScalingGroupType =
  struct
    type nonrec t =
      | SingleInstanceType 
      | MixedInstanceTypes 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SingleInstanceType -> "SingleInstanceType"
      | MixedInstanceTypes -> "MixedInstanceTypes"
      | Non_static_id s -> s
    let of_string =
      function
      | "SingleInstanceType" -> SingleInstanceType
      | "MixedInstanceTypes" -> MixedInstanceTypes
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration Ec2AutoScalingGroupType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"Ec2AutoScalingGroupType" j)
    let to_json = simple_to_json to_value
  end
module InstanceConfiguration =
  struct
    type nonrec t =
      {
      type_: String_.t option
        [@ocaml.doc "The instance type of the configuration."]}
    let make ?type_ = fun () -> { type_ }
    let to_value x =
      structure_to_value [("type", (Option.map x.type_ ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "type") in
      make ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" String_.of_json in make ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The instance configuration used for recommendations."]
module MixedInstanceConfigurationList =
  struct
    type nonrec t = MixedInstanceConfiguration.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:MixedInstanceConfiguration.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MixedInstanceConfiguration.of_xml)
    let of_json j =
      list_of_json ~kind:"MixedInstanceConfigurationList"
        ~of_json:MixedInstanceConfiguration.of_json j
    let to_json v = composed_to_json to_value v
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module ComputeConfiguration =
  struct
    type nonrec t =
      {
      vCpu: Double.t option
        [@ocaml.doc "The number of vCPU cores in the resource."];
      memorySizeInMB: Integer.t option
        [@ocaml.doc "The memory size of the resource."];
      architecture: String_.t option
        [@ocaml.doc "The architecture of the resource."];
      platform: String_.t option
        [@ocaml.doc
          "The platform of the resource. The platform is the specific combination of operating system, license model, and software on an instance."]}
    let make ?vCpu =
      fun ?memorySizeInMB ->
        fun ?architecture ->
          fun ?platform ->
            fun () -> { vCpu; memorySizeInMB; architecture; platform }
    let to_value x =
      structure_to_value
        [("vCpu", (Option.map x.vCpu ~f:Double.to_value));
        ("memorySizeInMB", (Option.map x.memorySizeInMB ~f:Integer.to_value));
        ("architecture", (Option.map x.architecture ~f:String_.to_value));
        ("platform", (Option.map x.platform ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let platform =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "platform") in
      let architecture =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "architecture") in
      let memorySizeInMB =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "memorySizeInMB") in
      let vCpu = (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "vCpu") in
      make ?platform ?architecture ?memorySizeInMB ?vCpu ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let platform = field_map json__ "platform" String_.of_json in
      let architecture = field_map json__ "architecture" String_.of_json in
      let memorySizeInMB = field_map json__ "memorySizeInMB" Integer.of_json in
      let vCpu = field_map json__ "vCpu" Double.of_json in
      make ?platform ?architecture ?memorySizeInMB ?vCpu ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the performance configuration for compute services such as Amazon EC2, Lambda, and ECS."]
module Long =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module DbInstanceConfiguration =
  struct
    type nonrec t =
      {
      dbInstanceClass: String_.t option
        [@ocaml.doc "The DB instance class of the DB instance."]}
    let make ?dbInstanceClass = fun () -> { dbInstanceClass }
    let to_value x =
      structure_to_value
        [("dbInstanceClass",
           (Option.map x.dbInstanceClass ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dbInstanceClass =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "dbInstanceClass") in
      make ?dbInstanceClass ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dbInstanceClass =
        field_map json__ "dbInstanceClass" String_.of_json in
      make ?dbInstanceClass ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The DB instance configuration used for recommendations."]
module ValidationExceptionDetail =
  struct
    type nonrec t =
      {
      fieldName: String_.t option
        [@ocaml.doc "The field name where the invalid entry was detected."];
      message: String_.t option
        [@ocaml.doc
          "A message with the reason for the validation exception error."]}
    let make ?fieldName = fun ?message -> fun () -> { fieldName; message }
    let to_value x =
      structure_to_value
        [("fieldName", (Option.map x.fieldName ~f:String_.to_value));
        ("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let fieldName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "fieldName") in
      make ?message ?fieldName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      let fieldName = field_map json__ "fieldName" String_.of_json in
      make ?message ?fieldName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The input failed to meet the constraints specified by the Amazon Web Services service in a specified field."]
module Datetime =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module Source =
  struct
    type nonrec t =
      | ComputeOptimizer 
      | CostExplorer 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ComputeOptimizer -> "ComputeOptimizer"
      | CostExplorer -> "CostExplorer"
      | Non_static_id s -> s
    let of_string =
      function
      | "ComputeOptimizer" -> ComputeOptimizer
      | "CostExplorer" -> CostExplorer
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Source" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Source" j)
    let to_json = simple_to_json to_value
  end
module TagList =
  struct
    type nonrec t = Tag.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Tag.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tag.of_xml)
    let of_json j = list_of_json ~kind:"TagList" ~of_json:Tag.of_json j
    let to_json v = composed_to_json to_value v
  end
module AccountId =
  struct
    type nonrec t = string
    let context_ = "AccountId"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[0-9]{12}"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AccountId" j
    let to_json = simple_to_json to_value
  end
module ActionType =
  struct
    type nonrec t =
      | Rightsize 
      | Stop 
      | Upgrade 
      | PurchaseSavingsPlans 
      | PurchaseReservedInstances 
      | MigrateToGraviton 
      | Delete 
      | ScaleIn 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Rightsize -> "Rightsize"
      | Stop -> "Stop"
      | Upgrade -> "Upgrade"
      | PurchaseSavingsPlans -> "PurchaseSavingsPlans"
      | PurchaseReservedInstances -> "PurchaseReservedInstances"
      | MigrateToGraviton -> "MigrateToGraviton"
      | Delete -> "Delete"
      | ScaleIn -> "ScaleIn"
      | Non_static_id s -> s
    let of_string =
      function
      | "Rightsize" -> Rightsize
      | "Stop" -> Stop
      | "Upgrade" -> Upgrade
      | "PurchaseSavingsPlans" -> PurchaseSavingsPlans
      | "PurchaseReservedInstances" -> PurchaseReservedInstances
      | "MigrateToGraviton" -> MigrateToGraviton
      | "Delete" -> Delete
      | "ScaleIn" -> ScaleIn
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ActionType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ActionType" j)
    let to_json = simple_to_json to_value
  end
module ImplementationEffort =
  struct
    type nonrec t =
      | VeryLow 
      | Low 
      | Medium 
      | High 
      | VeryHigh 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | VeryLow -> "VeryLow"
      | Low -> "Low"
      | Medium -> "Medium"
      | High -> "High"
      | VeryHigh -> "VeryHigh"
      | Non_static_id s -> s
    let of_string =
      function
      | "VeryLow" -> VeryLow
      | "Low" -> Low
      | "Medium" -> Medium
      | "High" -> High
      | "VeryHigh" -> VeryHigh
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ImplementationEffort" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ImplementationEffort" j)
    let to_json = simple_to_json to_value
  end
module ResourceType =
  struct
    type nonrec t =
      | Ec2Instance 
      | LambdaFunction 
      | EbsVolume 
      | EcsService 
      | Ec2AutoScalingGroup 
      | Ec2InstanceSavingsPlans 
      | ComputeSavingsPlans 
      | SageMakerSavingsPlans 
      | Ec2ReservedInstances 
      | RdsReservedInstances 
      | OpenSearchReservedInstances 
      | RedshiftReservedInstances 
      | ElastiCacheReservedInstances 
      | RdsDbInstanceStorage 
      | RdsDbInstance 
      | AuroraDbClusterStorage 
      | DynamoDbReservedCapacity 
      | MemoryDbReservedInstances 
      | NatGateway 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Ec2Instance -> "Ec2Instance"
      | LambdaFunction -> "LambdaFunction"
      | EbsVolume -> "EbsVolume"
      | EcsService -> "EcsService"
      | Ec2AutoScalingGroup -> "Ec2AutoScalingGroup"
      | Ec2InstanceSavingsPlans -> "Ec2InstanceSavingsPlans"
      | ComputeSavingsPlans -> "ComputeSavingsPlans"
      | SageMakerSavingsPlans -> "SageMakerSavingsPlans"
      | Ec2ReservedInstances -> "Ec2ReservedInstances"
      | RdsReservedInstances -> "RdsReservedInstances"
      | OpenSearchReservedInstances -> "OpenSearchReservedInstances"
      | RedshiftReservedInstances -> "RedshiftReservedInstances"
      | ElastiCacheReservedInstances -> "ElastiCacheReservedInstances"
      | RdsDbInstanceStorage -> "RdsDbInstanceStorage"
      | RdsDbInstance -> "RdsDbInstance"
      | AuroraDbClusterStorage -> "AuroraDbClusterStorage"
      | DynamoDbReservedCapacity -> "DynamoDbReservedCapacity"
      | MemoryDbReservedInstances -> "MemoryDbReservedInstances"
      | NatGateway -> "NatGateway"
      | Non_static_id s -> s
    let of_string =
      function
      | "Ec2Instance" -> Ec2Instance
      | "LambdaFunction" -> LambdaFunction
      | "EbsVolume" -> EbsVolume
      | "EcsService" -> EcsService
      | "Ec2AutoScalingGroup" -> Ec2AutoScalingGroup
      | "Ec2InstanceSavingsPlans" -> Ec2InstanceSavingsPlans
      | "ComputeSavingsPlans" -> ComputeSavingsPlans
      | "SageMakerSavingsPlans" -> SageMakerSavingsPlans
      | "Ec2ReservedInstances" -> Ec2ReservedInstances
      | "RdsReservedInstances" -> RdsReservedInstances
      | "OpenSearchReservedInstances" -> OpenSearchReservedInstances
      | "RedshiftReservedInstances" -> RedshiftReservedInstances
      | "ElastiCacheReservedInstances" -> ElastiCacheReservedInstances
      | "RdsDbInstanceStorage" -> RdsDbInstanceStorage
      | "RdsDbInstance" -> RdsDbInstance
      | "AuroraDbClusterStorage" -> AuroraDbClusterStorage
      | "DynamoDbReservedCapacity" -> DynamoDbReservedCapacity
      | "MemoryDbReservedInstances" -> MemoryDbReservedInstances
      | "NatGateway" -> NatGateway
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ResourceType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ResourceType" j)
    let to_json = simple_to_json to_value
  end
module EnrollmentStatus =
  struct
    type nonrec t =
      | Active 
      | Inactive 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Active -> "Active"
      | Inactive -> "Inactive"
      | Non_static_id s -> s
    let of_string =
      function
      | "Active" -> Active
      | "Inactive" -> Inactive
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration EnrollmentStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"EnrollmentStatus" j)
    let to_json = simple_to_json to_value
  end
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module MetricsByTimeList =
  struct
    type nonrec t = MetricsByTime.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:MetricsByTime.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MetricsByTime.of_xml)
    let of_json j =
      list_of_json ~kind:"MetricsByTimeList" ~of_json:MetricsByTime.of_json j
    let to_json v = composed_to_json to_value v
  end
module AuroraDbClusterStorageConfiguration =
  struct
    type nonrec t =
      {
      storageType: String_.t option
        [@ocaml.doc
          "The storage type to associate with the Aurora DB cluster."]}
    let make ?storageType = fun () -> { storageType }
    let to_value x =
      structure_to_value
        [("storageType", (Option.map x.storageType ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let storageType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "storageType") in
      make ?storageType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let storageType = field_map json__ "storageType" String_.of_json in
      make ?storageType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The Aurora DB cluster storage configuration used for recommendations."]
module ResourceCostCalculation =
  struct
    type nonrec t =
      {
      usages: UsageList.t option
        [@ocaml.doc "Usage details of the resource recommendation."];
      pricing: ResourcePricing.t option
        [@ocaml.doc "Pricing details of the resource recommendation."]}
    let make ?usages = fun ?pricing -> fun () -> { usages; pricing }
    let to_value x =
      structure_to_value
        [("usages", (Option.map x.usages ~f:UsageList.to_value));
        ("pricing", (Option.map x.pricing ~f:ResourcePricing.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pricing =
        (Option.map ~f:ResourcePricing.of_xml) (Xml.child xml_arg0 "pricing") in
      let usages =
        (Option.map ~f:UsageList.of_xml) (Xml.child xml_arg0 "usages") in
      make ?pricing ?usages ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let pricing = field_map json__ "pricing" ResourcePricing.of_json in
      let usages = field_map json__ "usages" UsageList.of_json in
      make ?pricing ?usages ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Cost impact of the resource recommendation."]
module ComputeSavingsPlansConfiguration =
  struct
    type nonrec t =
      {
      accountScope: String_.t option
        [@ocaml.doc
          "The account scope for which you want recommendations. Amazon Web Services calculates recommendations including the management account and member accounts if the value is set to PAYER. If the value is LINKED, recommendations are calculated for individual member accounts only."];
      term: String_.t option
        [@ocaml.doc "The Savings Plans recommendation term in years."];
      paymentOption: String_.t option
        [@ocaml.doc "The payment option for the commitment."];
      hourlyCommitment: String_.t option
        [@ocaml.doc "The hourly commitment for the Savings Plans type."]}
    let make ?accountScope =
      fun ?term ->
        fun ?paymentOption ->
          fun ?hourlyCommitment ->
            fun () -> { accountScope; term; paymentOption; hourlyCommitment }
    let to_value x =
      structure_to_value
        [("accountScope", (Option.map x.accountScope ~f:String_.to_value));
        ("term", (Option.map x.term ~f:String_.to_value));
        ("paymentOption", (Option.map x.paymentOption ~f:String_.to_value));
        ("hourlyCommitment",
          (Option.map x.hourlyCommitment ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let hourlyCommitment =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "hourlyCommitment") in
      let paymentOption =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "paymentOption") in
      let term = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "term") in
      let accountScope =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "accountScope") in
      make ?hourlyCommitment ?paymentOption ?term ?accountScope ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let hourlyCommitment =
        field_map json__ "hourlyCommitment" String_.of_json in
      let paymentOption = field_map json__ "paymentOption" String_.of_json in
      let term = field_map json__ "term" String_.of_json in
      let accountScope = field_map json__ "accountScope" String_.of_json in
      make ?hourlyCommitment ?paymentOption ?term ?accountScope ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The Compute Savings Plans configuration used for recommendations."]
module SavingsPlansCostCalculation =
  struct
    type nonrec t =
      {
      pricing: SavingsPlansPricing.t option
        [@ocaml.doc "Pricing details of the purchase recommendation."]}
    let make ?pricing = fun () -> { pricing }
    let to_value x =
      structure_to_value
        [("pricing", (Option.map x.pricing ~f:SavingsPlansPricing.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pricing =
        (Option.map ~f:SavingsPlansPricing.of_xml)
          (Xml.child xml_arg0 "pricing") in
      make ?pricing ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let pricing = field_map json__ "pricing" SavingsPlansPricing.of_json in
      make ?pricing ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Cost impact of the purchase recommendation."]
module DynamoDbReservedCapacityConfiguration =
  struct
    type nonrec t =
      {
      accountScope: String_.t option
        [@ocaml.doc "The account scope for which you want recommendations."];
      service: String_.t option
        [@ocaml.doc "The service for which you want recommendations."];
      term: String_.t option
        [@ocaml.doc "The reserved capacity recommendation term in years."];
      paymentOption: String_.t option
        [@ocaml.doc "The payment option for the commitment."];
      reservedInstancesRegion: String_.t option
        [@ocaml.doc "The Amazon Web Services Region of the commitment."];
      upfrontCost: String_.t option
        [@ocaml.doc
          "How much purchasing this reserved capacity costs you upfront."];
      monthlyRecurringCost: String_.t option
        [@ocaml.doc
          "How much purchasing this reserved capacity costs you on a monthly basis."];
      numberOfCapacityUnitsToPurchase: String_.t option
        [@ocaml.doc
          "The number of reserved capacity units that Amazon Web Services recommends that you purchase."];
      capacityUnits: String_.t option
        [@ocaml.doc "The capacity unit of the recommended reservation."]}
    let make ?accountScope =
      fun ?service ->
        fun ?term ->
          fun ?paymentOption ->
            fun ?reservedInstancesRegion ->
              fun ?upfrontCost ->
                fun ?monthlyRecurringCost ->
                  fun ?numberOfCapacityUnitsToPurchase ->
                    fun ?capacityUnits ->
                      fun () ->
                        {
                          accountScope;
                          service;
                          term;
                          paymentOption;
                          reservedInstancesRegion;
                          upfrontCost;
                          monthlyRecurringCost;
                          numberOfCapacityUnitsToPurchase;
                          capacityUnits
                        }
    let to_value x =
      structure_to_value
        [("accountScope", (Option.map x.accountScope ~f:String_.to_value));
        ("service", (Option.map x.service ~f:String_.to_value));
        ("term", (Option.map x.term ~f:String_.to_value));
        ("paymentOption", (Option.map x.paymentOption ~f:String_.to_value));
        ("reservedInstancesRegion",
          (Option.map x.reservedInstancesRegion ~f:String_.to_value));
        ("upfrontCost", (Option.map x.upfrontCost ~f:String_.to_value));
        ("monthlyRecurringCost",
          (Option.map x.monthlyRecurringCost ~f:String_.to_value));
        ("numberOfCapacityUnitsToPurchase",
          (Option.map x.numberOfCapacityUnitsToPurchase ~f:String_.to_value));
        ("capacityUnits", (Option.map x.capacityUnits ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let capacityUnits =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "capacityUnits") in
      let numberOfCapacityUnitsToPurchase =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "numberOfCapacityUnitsToPurchase") in
      let monthlyRecurringCost =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "monthlyRecurringCost") in
      let upfrontCost =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "upfrontCost") in
      let reservedInstancesRegion =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "reservedInstancesRegion") in
      let paymentOption =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "paymentOption") in
      let term = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "term") in
      let service =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "service") in
      let accountScope =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "accountScope") in
      make ?capacityUnits ?numberOfCapacityUnitsToPurchase
        ?monthlyRecurringCost ?upfrontCost ?reservedInstancesRegion
        ?paymentOption ?term ?service ?accountScope ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let capacityUnits = field_map json__ "capacityUnits" String_.of_json in
      let numberOfCapacityUnitsToPurchase =
        field_map json__ "numberOfCapacityUnitsToPurchase" String_.of_json in
      let monthlyRecurringCost =
        field_map json__ "monthlyRecurringCost" String_.of_json in
      let upfrontCost = field_map json__ "upfrontCost" String_.of_json in
      let reservedInstancesRegion =
        field_map json__ "reservedInstancesRegion" String_.of_json in
      let paymentOption = field_map json__ "paymentOption" String_.of_json in
      let term = field_map json__ "term" String_.of_json in
      let service = field_map json__ "service" String_.of_json in
      let accountScope = field_map json__ "accountScope" String_.of_json in
      make ?capacityUnits ?numberOfCapacityUnitsToPurchase
        ?monthlyRecurringCost ?upfrontCost ?reservedInstancesRegion
        ?paymentOption ?term ?service ?accountScope ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The DynamoDB reserved capacity configuration used for recommendations."]
module ReservedInstancesCostCalculation =
  struct
    type nonrec t =
      {
      pricing: ReservedInstancesPricing.t option
        [@ocaml.doc "Pricing details of the purchase recommendation."]}
    let make ?pricing = fun () -> { pricing }
    let to_value x =
      structure_to_value
        [("pricing",
           (Option.map x.pricing ~f:ReservedInstancesPricing.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let pricing =
        (Option.map ~f:ReservedInstancesPricing.of_xml)
          (Xml.child xml_arg0 "pricing") in
      make ?pricing ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let pricing =
        field_map json__ "pricing" ReservedInstancesPricing.of_json in
      make ?pricing ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Cost impact of the purchase recommendation."]
module EbsVolumeConfiguration =
  struct
    type nonrec t =
      {
      storage: StorageConfiguration.t option
        [@ocaml.doc
          "The disk storage of the Amazon Elastic Block Store volume."];
      performance: BlockStoragePerformanceConfiguration.t option
        [@ocaml.doc
          "The Amazon Elastic Block Store performance configuration."];
      attachmentState: String_.t option
        [@ocaml.doc "The Amazon Elastic Block Store attachment state."]}
    let make ?storage =
      fun ?performance ->
        fun ?attachmentState ->
          fun () -> { storage; performance; attachmentState }
    let to_value x =
      structure_to_value
        [("storage", (Option.map x.storage ~f:StorageConfiguration.to_value));
        ("performance",
          (Option.map x.performance
             ~f:BlockStoragePerformanceConfiguration.to_value));
        ("attachmentState",
          (Option.map x.attachmentState ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attachmentState =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "attachmentState") in
      let performance =
        (Option.map ~f:BlockStoragePerformanceConfiguration.of_xml)
          (Xml.child xml_arg0 "performance") in
      let storage =
        (Option.map ~f:StorageConfiguration.of_xml)
          (Xml.child xml_arg0 "storage") in
      make ?attachmentState ?performance ?storage ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let attachmentState =
        field_map json__ "attachmentState" String_.of_json in
      let performance =
        field_map json__ "performance"
          BlockStoragePerformanceConfiguration.of_json in
      let storage = field_map json__ "storage" StorageConfiguration.of_json in
      make ?attachmentState ?performance ?storage ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The Amazon Elastic Block Store volume configuration used for recommendations."]
module Ec2AutoScalingGroupConfiguration =
  struct
    type nonrec t =
      {
      instance: InstanceConfiguration.t option
        [@ocaml.doc
          "Details about the instance for the EC2 Auto Scaling group with a single instance type."];
      mixedInstances: MixedInstanceConfigurationList.t option
        [@ocaml.doc
          "A list of instance types for an EC2 Auto Scaling group with mixed instance types."];
      type_: Ec2AutoScalingGroupType.t option
        [@ocaml.doc
          "The type of EC2 Auto Scaling group, showing whether it consists of a single instance type or mixed instance types."];
      allocationStrategy: AllocationStrategy.t option
        [@ocaml.doc
          "The strategy used for allocating instances, based on a predefined priority order or based on the lowest available price."]}
    let make ?instance =
      fun ?mixedInstances ->
        fun ?type_ ->
          fun ?allocationStrategy ->
            fun () -> { instance; mixedInstances; type_; allocationStrategy }
    let to_value x =
      structure_to_value
        [("instance",
           (Option.map x.instance ~f:InstanceConfiguration.to_value));
        ("mixedInstances",
          (Option.map x.mixedInstances
             ~f:MixedInstanceConfigurationList.to_value));
        ("type", (Option.map x.type_ ~f:Ec2AutoScalingGroupType.to_value));
        ("allocationStrategy",
          (Option.map x.allocationStrategy ~f:AllocationStrategy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let allocationStrategy =
        (Option.map ~f:AllocationStrategy.of_xml)
          (Xml.child xml_arg0 "allocationStrategy") in
      let type_ =
        (Option.map ~f:Ec2AutoScalingGroupType.of_xml)
          (Xml.child xml_arg0 "type") in
      let mixedInstances =
        (Option.map ~f:MixedInstanceConfigurationList.of_xml)
          (Xml.child xml_arg0 "mixedInstances") in
      let instance =
        (Option.map ~f:InstanceConfiguration.of_xml)
          (Xml.child xml_arg0 "instance") in
      make ?allocationStrategy ?type_ ?mixedInstances ?instance ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let allocationStrategy =
        field_map json__ "allocationStrategy" AllocationStrategy.of_json in
      let type_ = field_map json__ "type" Ec2AutoScalingGroupType.of_json in
      let mixedInstances =
        field_map json__ "mixedInstances"
          MixedInstanceConfigurationList.of_json in
      let instance =
        field_map json__ "instance" InstanceConfiguration.of_json in
      make ?allocationStrategy ?type_ ?mixedInstances ?instance ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The EC2 Auto Scaling group configuration used for recommendations."]
module Ec2InstanceConfiguration =
  struct
    type nonrec t =
      {
      instance: InstanceConfiguration.t option
        [@ocaml.doc "Details about the instance."]}
    let make ?instance = fun () -> { instance }
    let to_value x =
      structure_to_value
        [("instance",
           (Option.map x.instance ~f:InstanceConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instance =
        (Option.map ~f:InstanceConfiguration.of_xml)
          (Xml.child xml_arg0 "instance") in
      make ?instance ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instance =
        field_map json__ "instance" InstanceConfiguration.of_json in
      make ?instance ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The EC2 instance configuration used for recommendations."]
module Ec2InstanceSavingsPlansConfiguration =
  struct
    type nonrec t =
      {
      accountScope: String_.t option
        [@ocaml.doc "The account scope for which you want recommendations."];
      term: String_.t option
        [@ocaml.doc "The Savings Plans recommendation term in years."];
      paymentOption: String_.t option
        [@ocaml.doc "The payment option for the commitment."];
      hourlyCommitment: String_.t option
        [@ocaml.doc "The hourly commitment for the Savings Plans type."];
      instanceFamily: String_.t option
        [@ocaml.doc "The instance family of the recommended Savings Plans."];
      savingsPlansRegion: String_.t option
        [@ocaml.doc "The Amazon Web Services Region of the commitment."]}
    let make ?accountScope =
      fun ?term ->
        fun ?paymentOption ->
          fun ?hourlyCommitment ->
            fun ?instanceFamily ->
              fun ?savingsPlansRegion ->
                fun () ->
                  {
                    accountScope;
                    term;
                    paymentOption;
                    hourlyCommitment;
                    instanceFamily;
                    savingsPlansRegion
                  }
    let to_value x =
      structure_to_value
        [("accountScope", (Option.map x.accountScope ~f:String_.to_value));
        ("term", (Option.map x.term ~f:String_.to_value));
        ("paymentOption", (Option.map x.paymentOption ~f:String_.to_value));
        ("hourlyCommitment",
          (Option.map x.hourlyCommitment ~f:String_.to_value));
        ("instanceFamily", (Option.map x.instanceFamily ~f:String_.to_value));
        ("savingsPlansRegion",
          (Option.map x.savingsPlansRegion ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let savingsPlansRegion =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "savingsPlansRegion") in
      let instanceFamily =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "instanceFamily") in
      let hourlyCommitment =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "hourlyCommitment") in
      let paymentOption =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "paymentOption") in
      let term = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "term") in
      let accountScope =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "accountScope") in
      make ?savingsPlansRegion ?instanceFamily ?hourlyCommitment
        ?paymentOption ?term ?accountScope ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let savingsPlansRegion =
        field_map json__ "savingsPlansRegion" String_.of_json in
      let instanceFamily = field_map json__ "instanceFamily" String_.of_json in
      let hourlyCommitment =
        field_map json__ "hourlyCommitment" String_.of_json in
      let paymentOption = field_map json__ "paymentOption" String_.of_json in
      let term = field_map json__ "term" String_.of_json in
      let accountScope = field_map json__ "accountScope" String_.of_json in
      make ?savingsPlansRegion ?instanceFamily ?hourlyCommitment
        ?paymentOption ?term ?accountScope ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The EC2 instance Savings Plans configuration used for recommendations."]
module Ec2ReservedInstancesConfiguration =
  struct
    type nonrec t =
      {
      accountScope: String_.t option
        [@ocaml.doc "The account scope for which you want recommendations."];
      service: String_.t option
        [@ocaml.doc "The service for which you want recommendations."];
      term: String_.t option
        [@ocaml.doc "The reserved instances recommendation term in years."];
      paymentOption: String_.t option
        [@ocaml.doc "The payment option for the commitment."];
      reservedInstancesRegion: String_.t option
        [@ocaml.doc "The Amazon Web Services Region of the commitment."];
      upfrontCost: String_.t option
        [@ocaml.doc "How much purchasing this instance costs you upfront."];
      monthlyRecurringCost: String_.t option
        [@ocaml.doc
          "How much purchasing these reserved instances costs you on a monthly basis."];
      normalizedUnitsToPurchase: String_.t option
        [@ocaml.doc
          "The number of normalized units that Amazon Web Services recommends that you purchase."];
      numberOfInstancesToPurchase: String_.t option
        [@ocaml.doc
          "The number of instances that Amazon Web Services recommends that you purchase."];
      offeringClass: String_.t option
        [@ocaml.doc
          "Indicates whether the recommendation is for standard or convertible reservations."];
      instanceFamily: String_.t option
        [@ocaml.doc "The instance family of the recommended reservation."];
      instanceType: String_.t option
        [@ocaml.doc
          "The type of instance that Amazon Web Services recommends."];
      currentGeneration: String_.t option
        [@ocaml.doc
          "Determines whether the recommendation is for a current generation instance."];
      platform: String_.t option
        [@ocaml.doc
          "The platform of the recommended reservation. The platform is the specific combination of operating system, license model, and software on an instance."];
      tenancy: String_.t option
        [@ocaml.doc
          "Determines whether the recommended reservation is dedicated or shared."];
      sizeFlexEligible: Boolean.t option
        [@ocaml.doc
          "Determines whether the recommendation is size flexible."]}
    let make ?accountScope =
      fun ?service ->
        fun ?term ->
          fun ?paymentOption ->
            fun ?reservedInstancesRegion ->
              fun ?upfrontCost ->
                fun ?monthlyRecurringCost ->
                  fun ?normalizedUnitsToPurchase ->
                    fun ?numberOfInstancesToPurchase ->
                      fun ?offeringClass ->
                        fun ?instanceFamily ->
                          fun ?instanceType ->
                            fun ?currentGeneration ->
                              fun ?platform ->
                                fun ?tenancy ->
                                  fun ?sizeFlexEligible ->
                                    fun () ->
                                      {
                                        accountScope;
                                        service;
                                        term;
                                        paymentOption;
                                        reservedInstancesRegion;
                                        upfrontCost;
                                        monthlyRecurringCost;
                                        normalizedUnitsToPurchase;
                                        numberOfInstancesToPurchase;
                                        offeringClass;
                                        instanceFamily;
                                        instanceType;
                                        currentGeneration;
                                        platform;
                                        tenancy;
                                        sizeFlexEligible
                                      }
    let to_value x =
      structure_to_value
        [("accountScope", (Option.map x.accountScope ~f:String_.to_value));
        ("service", (Option.map x.service ~f:String_.to_value));
        ("term", (Option.map x.term ~f:String_.to_value));
        ("paymentOption", (Option.map x.paymentOption ~f:String_.to_value));
        ("reservedInstancesRegion",
          (Option.map x.reservedInstancesRegion ~f:String_.to_value));
        ("upfrontCost", (Option.map x.upfrontCost ~f:String_.to_value));
        ("monthlyRecurringCost",
          (Option.map x.monthlyRecurringCost ~f:String_.to_value));
        ("normalizedUnitsToPurchase",
          (Option.map x.normalizedUnitsToPurchase ~f:String_.to_value));
        ("numberOfInstancesToPurchase",
          (Option.map x.numberOfInstancesToPurchase ~f:String_.to_value));
        ("offeringClass", (Option.map x.offeringClass ~f:String_.to_value));
        ("instanceFamily", (Option.map x.instanceFamily ~f:String_.to_value));
        ("instanceType", (Option.map x.instanceType ~f:String_.to_value));
        ("currentGeneration",
          (Option.map x.currentGeneration ~f:String_.to_value));
        ("platform", (Option.map x.platform ~f:String_.to_value));
        ("tenancy", (Option.map x.tenancy ~f:String_.to_value));
        ("sizeFlexEligible",
          (Option.map x.sizeFlexEligible ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sizeFlexEligible =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "sizeFlexEligible") in
      let tenancy =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "tenancy") in
      let platform =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "platform") in
      let currentGeneration =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "currentGeneration") in
      let instanceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "instanceType") in
      let instanceFamily =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "instanceFamily") in
      let offeringClass =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "offeringClass") in
      let numberOfInstancesToPurchase =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "numberOfInstancesToPurchase") in
      let normalizedUnitsToPurchase =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "normalizedUnitsToPurchase") in
      let monthlyRecurringCost =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "monthlyRecurringCost") in
      let upfrontCost =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "upfrontCost") in
      let reservedInstancesRegion =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "reservedInstancesRegion") in
      let paymentOption =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "paymentOption") in
      let term = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "term") in
      let service =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "service") in
      let accountScope =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "accountScope") in
      make ?sizeFlexEligible ?tenancy ?platform ?currentGeneration
        ?instanceType ?instanceFamily ?offeringClass
        ?numberOfInstancesToPurchase ?normalizedUnitsToPurchase
        ?monthlyRecurringCost ?upfrontCost ?reservedInstancesRegion
        ?paymentOption ?term ?service ?accountScope ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sizeFlexEligible =
        field_map json__ "sizeFlexEligible" Boolean.of_json in
      let tenancy = field_map json__ "tenancy" String_.of_json in
      let platform = field_map json__ "platform" String_.of_json in
      let currentGeneration =
        field_map json__ "currentGeneration" String_.of_json in
      let instanceType = field_map json__ "instanceType" String_.of_json in
      let instanceFamily = field_map json__ "instanceFamily" String_.of_json in
      let offeringClass = field_map json__ "offeringClass" String_.of_json in
      let numberOfInstancesToPurchase =
        field_map json__ "numberOfInstancesToPurchase" String_.of_json in
      let normalizedUnitsToPurchase =
        field_map json__ "normalizedUnitsToPurchase" String_.of_json in
      let monthlyRecurringCost =
        field_map json__ "monthlyRecurringCost" String_.of_json in
      let upfrontCost = field_map json__ "upfrontCost" String_.of_json in
      let reservedInstancesRegion =
        field_map json__ "reservedInstancesRegion" String_.of_json in
      let paymentOption = field_map json__ "paymentOption" String_.of_json in
      let term = field_map json__ "term" String_.of_json in
      let service = field_map json__ "service" String_.of_json in
      let accountScope = field_map json__ "accountScope" String_.of_json in
      make ?sizeFlexEligible ?tenancy ?platform ?currentGeneration
        ?instanceType ?instanceFamily ?offeringClass
        ?numberOfInstancesToPurchase ?normalizedUnitsToPurchase
        ?monthlyRecurringCost ?upfrontCost ?reservedInstancesRegion
        ?paymentOption ?term ?service ?accountScope ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The EC2 reserved instances configuration used for recommendations."]
module EcsServiceConfiguration =
  struct
    type nonrec t =
      {
      compute: ComputeConfiguration.t option
        [@ocaml.doc "Details about the compute configuration."]}
    let make ?compute = fun () -> { compute }
    let to_value x =
      structure_to_value
        [("compute", (Option.map x.compute ~f:ComputeConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let compute =
        (Option.map ~f:ComputeConfiguration.of_xml)
          (Xml.child xml_arg0 "compute") in
      make ?compute ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let compute = field_map json__ "compute" ComputeConfiguration.of_json in
      make ?compute ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The ECS service configuration used for recommendations."]
module ElastiCacheReservedInstancesConfiguration =
  struct
    type nonrec t =
      {
      accountScope: String_.t option
        [@ocaml.doc "The account scope for which you want recommendations."];
      service: String_.t option
        [@ocaml.doc "The service for which you want recommendations."];
      term: String_.t option
        [@ocaml.doc "The reserved instances recommendation term in years."];
      paymentOption: String_.t option
        [@ocaml.doc "The payment option for the commitment."];
      reservedInstancesRegion: String_.t option
        [@ocaml.doc "The Amazon Web Services Region of the commitment."];
      upfrontCost: String_.t option
        [@ocaml.doc "How much purchasing this instance costs you upfront."];
      monthlyRecurringCost: String_.t option
        [@ocaml.doc
          "How much purchasing these reserved instances costs you on a monthly basis."];
      normalizedUnitsToPurchase: String_.t option
        [@ocaml.doc
          "The number of normalized units that Amazon Web Services recommends that you purchase."];
      numberOfInstancesToPurchase: String_.t option
        [@ocaml.doc
          "The number of instances that Amazon Web Services recommends that you purchase."];
      instanceFamily: String_.t option
        [@ocaml.doc "The instance family of the recommended reservation."];
      instanceType: String_.t option
        [@ocaml.doc
          "The type of instance that Amazon Web Services recommends."];
      currentGeneration: String_.t option
        [@ocaml.doc
          "Determines whether the recommendation is for a current generation instance."];
      sizeFlexEligible: Boolean.t option
        [@ocaml.doc
          "Determines whether the recommendation is size flexible."]}
    let make ?accountScope =
      fun ?service ->
        fun ?term ->
          fun ?paymentOption ->
            fun ?reservedInstancesRegion ->
              fun ?upfrontCost ->
                fun ?monthlyRecurringCost ->
                  fun ?normalizedUnitsToPurchase ->
                    fun ?numberOfInstancesToPurchase ->
                      fun ?instanceFamily ->
                        fun ?instanceType ->
                          fun ?currentGeneration ->
                            fun ?sizeFlexEligible ->
                              fun () ->
                                {
                                  accountScope;
                                  service;
                                  term;
                                  paymentOption;
                                  reservedInstancesRegion;
                                  upfrontCost;
                                  monthlyRecurringCost;
                                  normalizedUnitsToPurchase;
                                  numberOfInstancesToPurchase;
                                  instanceFamily;
                                  instanceType;
                                  currentGeneration;
                                  sizeFlexEligible
                                }
    let to_value x =
      structure_to_value
        [("accountScope", (Option.map x.accountScope ~f:String_.to_value));
        ("service", (Option.map x.service ~f:String_.to_value));
        ("term", (Option.map x.term ~f:String_.to_value));
        ("paymentOption", (Option.map x.paymentOption ~f:String_.to_value));
        ("reservedInstancesRegion",
          (Option.map x.reservedInstancesRegion ~f:String_.to_value));
        ("upfrontCost", (Option.map x.upfrontCost ~f:String_.to_value));
        ("monthlyRecurringCost",
          (Option.map x.monthlyRecurringCost ~f:String_.to_value));
        ("normalizedUnitsToPurchase",
          (Option.map x.normalizedUnitsToPurchase ~f:String_.to_value));
        ("numberOfInstancesToPurchase",
          (Option.map x.numberOfInstancesToPurchase ~f:String_.to_value));
        ("instanceFamily", (Option.map x.instanceFamily ~f:String_.to_value));
        ("instanceType", (Option.map x.instanceType ~f:String_.to_value));
        ("currentGeneration",
          (Option.map x.currentGeneration ~f:String_.to_value));
        ("sizeFlexEligible",
          (Option.map x.sizeFlexEligible ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sizeFlexEligible =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "sizeFlexEligible") in
      let currentGeneration =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "currentGeneration") in
      let instanceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "instanceType") in
      let instanceFamily =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "instanceFamily") in
      let numberOfInstancesToPurchase =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "numberOfInstancesToPurchase") in
      let normalizedUnitsToPurchase =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "normalizedUnitsToPurchase") in
      let monthlyRecurringCost =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "monthlyRecurringCost") in
      let upfrontCost =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "upfrontCost") in
      let reservedInstancesRegion =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "reservedInstancesRegion") in
      let paymentOption =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "paymentOption") in
      let term = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "term") in
      let service =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "service") in
      let accountScope =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "accountScope") in
      make ?sizeFlexEligible ?currentGeneration ?instanceType ?instanceFamily
        ?numberOfInstancesToPurchase ?normalizedUnitsToPurchase
        ?monthlyRecurringCost ?upfrontCost ?reservedInstancesRegion
        ?paymentOption ?term ?service ?accountScope ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sizeFlexEligible =
        field_map json__ "sizeFlexEligible" Boolean.of_json in
      let currentGeneration =
        field_map json__ "currentGeneration" String_.of_json in
      let instanceType = field_map json__ "instanceType" String_.of_json in
      let instanceFamily = field_map json__ "instanceFamily" String_.of_json in
      let numberOfInstancesToPurchase =
        field_map json__ "numberOfInstancesToPurchase" String_.of_json in
      let normalizedUnitsToPurchase =
        field_map json__ "normalizedUnitsToPurchase" String_.of_json in
      let monthlyRecurringCost =
        field_map json__ "monthlyRecurringCost" String_.of_json in
      let upfrontCost = field_map json__ "upfrontCost" String_.of_json in
      let reservedInstancesRegion =
        field_map json__ "reservedInstancesRegion" String_.of_json in
      let paymentOption = field_map json__ "paymentOption" String_.of_json in
      let term = field_map json__ "term" String_.of_json in
      let service = field_map json__ "service" String_.of_json in
      let accountScope = field_map json__ "accountScope" String_.of_json in
      make ?sizeFlexEligible ?currentGeneration ?instanceType ?instanceFamily
        ?numberOfInstancesToPurchase ?normalizedUnitsToPurchase
        ?monthlyRecurringCost ?upfrontCost ?reservedInstancesRegion
        ?paymentOption ?term ?service ?accountScope ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The ElastiCache reserved instances configuration used for recommendations."]
module LambdaFunctionConfiguration =
  struct
    type nonrec t =
      {
      compute: ComputeConfiguration.t option
        [@ocaml.doc "Details about the compute configuration."]}
    let make ?compute = fun () -> { compute }
    let to_value x =
      structure_to_value
        [("compute", (Option.map x.compute ~f:ComputeConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let compute =
        (Option.map ~f:ComputeConfiguration.of_xml)
          (Xml.child xml_arg0 "compute") in
      make ?compute ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let compute = field_map json__ "compute" ComputeConfiguration.of_json in
      make ?compute ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The Lambda function configuration used for recommendations."]
module MemoryDbReservedInstancesConfiguration =
  struct
    type nonrec t =
      {
      accountScope: String_.t option
        [@ocaml.doc "The account scope for which you want recommendations."];
      service: String_.t option
        [@ocaml.doc "The service for which you want recommendations."];
      term: String_.t option
        [@ocaml.doc "The reserved instances recommendation term in years."];
      paymentOption: String_.t option
        [@ocaml.doc "The payment option for the commitment."];
      reservedInstancesRegion: String_.t option
        [@ocaml.doc "The Amazon Web Services Region of the commitment."];
      upfrontCost: String_.t option
        [@ocaml.doc
          "How much purchasing these reserved instances costs you upfront."];
      monthlyRecurringCost: String_.t option
        [@ocaml.doc
          "How much purchasing these reserved instances costs you on a monthly basis."];
      normalizedUnitsToPurchase: String_.t option
        [@ocaml.doc
          "The number of normalized units that Amazon Web Services recommends that you purchase."];
      numberOfInstancesToPurchase: String_.t option
        [@ocaml.doc
          "The number of instances that Amazon Web Services recommends that you purchase."];
      instanceType: String_.t option
        [@ocaml.doc
          "The type of instance that Amazon Web Services recommends."];
      instanceFamily: String_.t option
        [@ocaml.doc "The instance family of the recommended reservation."];
      sizeFlexEligible: Boolean.t option
        [@ocaml.doc
          "Determines whether the recommendation is size flexible."];
      currentGeneration: String_.t option
        [@ocaml.doc
          "Determines whether the recommendation is for a current generation instance."]}
    let make ?accountScope =
      fun ?service ->
        fun ?term ->
          fun ?paymentOption ->
            fun ?reservedInstancesRegion ->
              fun ?upfrontCost ->
                fun ?monthlyRecurringCost ->
                  fun ?normalizedUnitsToPurchase ->
                    fun ?numberOfInstancesToPurchase ->
                      fun ?instanceType ->
                        fun ?instanceFamily ->
                          fun ?sizeFlexEligible ->
                            fun ?currentGeneration ->
                              fun () ->
                                {
                                  accountScope;
                                  service;
                                  term;
                                  paymentOption;
                                  reservedInstancesRegion;
                                  upfrontCost;
                                  monthlyRecurringCost;
                                  normalizedUnitsToPurchase;
                                  numberOfInstancesToPurchase;
                                  instanceType;
                                  instanceFamily;
                                  sizeFlexEligible;
                                  currentGeneration
                                }
    let to_value x =
      structure_to_value
        [("accountScope", (Option.map x.accountScope ~f:String_.to_value));
        ("service", (Option.map x.service ~f:String_.to_value));
        ("term", (Option.map x.term ~f:String_.to_value));
        ("paymentOption", (Option.map x.paymentOption ~f:String_.to_value));
        ("reservedInstancesRegion",
          (Option.map x.reservedInstancesRegion ~f:String_.to_value));
        ("upfrontCost", (Option.map x.upfrontCost ~f:String_.to_value));
        ("monthlyRecurringCost",
          (Option.map x.monthlyRecurringCost ~f:String_.to_value));
        ("normalizedUnitsToPurchase",
          (Option.map x.normalizedUnitsToPurchase ~f:String_.to_value));
        ("numberOfInstancesToPurchase",
          (Option.map x.numberOfInstancesToPurchase ~f:String_.to_value));
        ("instanceType", (Option.map x.instanceType ~f:String_.to_value));
        ("instanceFamily", (Option.map x.instanceFamily ~f:String_.to_value));
        ("sizeFlexEligible",
          (Option.map x.sizeFlexEligible ~f:Boolean.to_value));
        ("currentGeneration",
          (Option.map x.currentGeneration ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let currentGeneration =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "currentGeneration") in
      let sizeFlexEligible =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "sizeFlexEligible") in
      let instanceFamily =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "instanceFamily") in
      let instanceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "instanceType") in
      let numberOfInstancesToPurchase =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "numberOfInstancesToPurchase") in
      let normalizedUnitsToPurchase =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "normalizedUnitsToPurchase") in
      let monthlyRecurringCost =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "monthlyRecurringCost") in
      let upfrontCost =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "upfrontCost") in
      let reservedInstancesRegion =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "reservedInstancesRegion") in
      let paymentOption =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "paymentOption") in
      let term = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "term") in
      let service =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "service") in
      let accountScope =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "accountScope") in
      make ?currentGeneration ?sizeFlexEligible ?instanceFamily ?instanceType
        ?numberOfInstancesToPurchase ?normalizedUnitsToPurchase
        ?monthlyRecurringCost ?upfrontCost ?reservedInstancesRegion
        ?paymentOption ?term ?service ?accountScope ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let currentGeneration =
        field_map json__ "currentGeneration" String_.of_json in
      let sizeFlexEligible =
        field_map json__ "sizeFlexEligible" Boolean.of_json in
      let instanceFamily = field_map json__ "instanceFamily" String_.of_json in
      let instanceType = field_map json__ "instanceType" String_.of_json in
      let numberOfInstancesToPurchase =
        field_map json__ "numberOfInstancesToPurchase" String_.of_json in
      let normalizedUnitsToPurchase =
        field_map json__ "normalizedUnitsToPurchase" String_.of_json in
      let monthlyRecurringCost =
        field_map json__ "monthlyRecurringCost" String_.of_json in
      let upfrontCost = field_map json__ "upfrontCost" String_.of_json in
      let reservedInstancesRegion =
        field_map json__ "reservedInstancesRegion" String_.of_json in
      let paymentOption = field_map json__ "paymentOption" String_.of_json in
      let term = field_map json__ "term" String_.of_json in
      let service = field_map json__ "service" String_.of_json in
      let accountScope = field_map json__ "accountScope" String_.of_json in
      make ?currentGeneration ?sizeFlexEligible ?instanceFamily ?instanceType
        ?numberOfInstancesToPurchase ?normalizedUnitsToPurchase
        ?monthlyRecurringCost ?upfrontCost ?reservedInstancesRegion
        ?paymentOption ?term ?service ?accountScope ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The MemoryDB reserved instances configuration used for recommendations. While the API reference uses \"MemoryDB reserved instances\", the user guide and other documentation refer to them as \"MemoryDB reserved nodes\", as the terms are used interchangeably."]
module NatGatewayConfiguration =
  struct
    type nonrec t =
      {
      activeConnectionCount: Long.t option
        [@ocaml.doc
          "The number of active connections through the NAT Gateway."];
      packetsInFromSource: Long.t option
        [@ocaml.doc
          "The number of packets received from the source through the NAT Gateway."];
      packetsInFromDestination: Long.t option
        [@ocaml.doc
          "The number of packets received from the destination through the NAT Gateway."]}
    let make ?activeConnectionCount =
      fun ?packetsInFromSource ->
        fun ?packetsInFromDestination ->
          fun () ->
            {
              activeConnectionCount;
              packetsInFromSource;
              packetsInFromDestination
            }
    let to_value x =
      structure_to_value
        [("activeConnectionCount",
           (Option.map x.activeConnectionCount ~f:Long.to_value));
        ("packetsInFromSource",
          (Option.map x.packetsInFromSource ~f:Long.to_value));
        ("packetsInFromDestination",
          (Option.map x.packetsInFromDestination ~f:Long.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let packetsInFromDestination =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "packetsInFromDestination") in
      let packetsInFromSource =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "packetsInFromSource") in
      let activeConnectionCount =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "activeConnectionCount") in
      make ?packetsInFromDestination ?packetsInFromSource
        ?activeConnectionCount ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let packetsInFromDestination =
        field_map json__ "packetsInFromDestination" Long.of_json in
      let packetsInFromSource =
        field_map json__ "packetsInFromSource" Long.of_json in
      let activeConnectionCount =
        field_map json__ "activeConnectionCount" Long.of_json in
      make ?packetsInFromDestination ?packetsInFromSource
        ?activeConnectionCount ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The NAT Gateway configuration used for recommendations."]
module OpenSearchReservedInstancesConfiguration =
  struct
    type nonrec t =
      {
      accountScope: String_.t option
        [@ocaml.doc "The account scope for which you want recommendations."];
      service: String_.t option
        [@ocaml.doc "The service for which you want recommendations."];
      term: String_.t option
        [@ocaml.doc "The reserved instances recommendation term in years."];
      paymentOption: String_.t option
        [@ocaml.doc "The payment option for the commitment."];
      reservedInstancesRegion: String_.t option
        [@ocaml.doc "The Amazon Web Services Region of the commitment."];
      upfrontCost: String_.t option
        [@ocaml.doc "How much purchasing this instance costs you upfront."];
      monthlyRecurringCost: String_.t option
        [@ocaml.doc
          "How much purchasing these reserved instances costs you on a monthly basis."];
      normalizedUnitsToPurchase: String_.t option
        [@ocaml.doc
          "The number of normalized units that Amazon Web Services recommends that you purchase."];
      numberOfInstancesToPurchase: String_.t option
        [@ocaml.doc
          "The number of instances that Amazon Web Services recommends that you purchase."];
      instanceType: String_.t option
        [@ocaml.doc
          "The type of instance that Amazon Web Services recommends."];
      currentGeneration: String_.t option
        [@ocaml.doc
          "Determines whether the recommendation is for a current generation instance."];
      sizeFlexEligible: Boolean.t option
        [@ocaml.doc
          "Determines whether the recommendation is size flexible."]}
    let make ?accountScope =
      fun ?service ->
        fun ?term ->
          fun ?paymentOption ->
            fun ?reservedInstancesRegion ->
              fun ?upfrontCost ->
                fun ?monthlyRecurringCost ->
                  fun ?normalizedUnitsToPurchase ->
                    fun ?numberOfInstancesToPurchase ->
                      fun ?instanceType ->
                        fun ?currentGeneration ->
                          fun ?sizeFlexEligible ->
                            fun () ->
                              {
                                accountScope;
                                service;
                                term;
                                paymentOption;
                                reservedInstancesRegion;
                                upfrontCost;
                                monthlyRecurringCost;
                                normalizedUnitsToPurchase;
                                numberOfInstancesToPurchase;
                                instanceType;
                                currentGeneration;
                                sizeFlexEligible
                              }
    let to_value x =
      structure_to_value
        [("accountScope", (Option.map x.accountScope ~f:String_.to_value));
        ("service", (Option.map x.service ~f:String_.to_value));
        ("term", (Option.map x.term ~f:String_.to_value));
        ("paymentOption", (Option.map x.paymentOption ~f:String_.to_value));
        ("reservedInstancesRegion",
          (Option.map x.reservedInstancesRegion ~f:String_.to_value));
        ("upfrontCost", (Option.map x.upfrontCost ~f:String_.to_value));
        ("monthlyRecurringCost",
          (Option.map x.monthlyRecurringCost ~f:String_.to_value));
        ("normalizedUnitsToPurchase",
          (Option.map x.normalizedUnitsToPurchase ~f:String_.to_value));
        ("numberOfInstancesToPurchase",
          (Option.map x.numberOfInstancesToPurchase ~f:String_.to_value));
        ("instanceType", (Option.map x.instanceType ~f:String_.to_value));
        ("currentGeneration",
          (Option.map x.currentGeneration ~f:String_.to_value));
        ("sizeFlexEligible",
          (Option.map x.sizeFlexEligible ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sizeFlexEligible =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "sizeFlexEligible") in
      let currentGeneration =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "currentGeneration") in
      let instanceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "instanceType") in
      let numberOfInstancesToPurchase =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "numberOfInstancesToPurchase") in
      let normalizedUnitsToPurchase =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "normalizedUnitsToPurchase") in
      let monthlyRecurringCost =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "monthlyRecurringCost") in
      let upfrontCost =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "upfrontCost") in
      let reservedInstancesRegion =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "reservedInstancesRegion") in
      let paymentOption =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "paymentOption") in
      let term = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "term") in
      let service =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "service") in
      let accountScope =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "accountScope") in
      make ?sizeFlexEligible ?currentGeneration ?instanceType
        ?numberOfInstancesToPurchase ?normalizedUnitsToPurchase
        ?monthlyRecurringCost ?upfrontCost ?reservedInstancesRegion
        ?paymentOption ?term ?service ?accountScope ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sizeFlexEligible =
        field_map json__ "sizeFlexEligible" Boolean.of_json in
      let currentGeneration =
        field_map json__ "currentGeneration" String_.of_json in
      let instanceType = field_map json__ "instanceType" String_.of_json in
      let numberOfInstancesToPurchase =
        field_map json__ "numberOfInstancesToPurchase" String_.of_json in
      let normalizedUnitsToPurchase =
        field_map json__ "normalizedUnitsToPurchase" String_.of_json in
      let monthlyRecurringCost =
        field_map json__ "monthlyRecurringCost" String_.of_json in
      let upfrontCost = field_map json__ "upfrontCost" String_.of_json in
      let reservedInstancesRegion =
        field_map json__ "reservedInstancesRegion" String_.of_json in
      let paymentOption = field_map json__ "paymentOption" String_.of_json in
      let term = field_map json__ "term" String_.of_json in
      let service = field_map json__ "service" String_.of_json in
      let accountScope = field_map json__ "accountScope" String_.of_json in
      make ?sizeFlexEligible ?currentGeneration ?instanceType
        ?numberOfInstancesToPurchase ?normalizedUnitsToPurchase
        ?monthlyRecurringCost ?upfrontCost ?reservedInstancesRegion
        ?paymentOption ?term ?service ?accountScope ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The OpenSearch reserved instances configuration used for recommendations."]
module RdsDbInstanceConfiguration =
  struct
    type nonrec t =
      {
      instance: DbInstanceConfiguration.t option
        [@ocaml.doc "Details about the instance configuration."]}
    let make ?instance = fun () -> { instance }
    let to_value x =
      structure_to_value
        [("instance",
           (Option.map x.instance ~f:DbInstanceConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instance =
        (Option.map ~f:DbInstanceConfiguration.of_xml)
          (Xml.child xml_arg0 "instance") in
      make ?instance ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let instance =
        field_map json__ "instance" DbInstanceConfiguration.of_json in
      make ?instance ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The Amazon RDS DB instance configuration used for recommendations."]
module RdsDbInstanceStorageConfiguration =
  struct
    type nonrec t =
      {
      storageType: String_.t option
        [@ocaml.doc "The storage type to associate with the DB instance."];
      allocatedStorageInGb: Double.t option
        [@ocaml.doc
          "The new amount of storage in GB to allocate for the DB instance."];
      iops: Double.t option
        [@ocaml.doc
          "The amount of Provisioned IOPS (input/output operations per second) to be initially allocated for the DB instance."];
      storageThroughput: Double.t option
        [@ocaml.doc "The storage throughput for the DB instance."]}
    let make ?storageType =
      fun ?allocatedStorageInGb ->
        fun ?iops ->
          fun ?storageThroughput ->
            fun () ->
              { storageType; allocatedStorageInGb; iops; storageThroughput }
    let to_value x =
      structure_to_value
        [("storageType", (Option.map x.storageType ~f:String_.to_value));
        ("allocatedStorageInGb",
          (Option.map x.allocatedStorageInGb ~f:Double.to_value));
        ("iops", (Option.map x.iops ~f:Double.to_value));
        ("storageThroughput",
          (Option.map x.storageThroughput ~f:Double.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let storageThroughput =
        (Option.map ~f:Double.of_xml)
          (Xml.child xml_arg0 "storageThroughput") in
      let iops = (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "iops") in
      let allocatedStorageInGb =
        (Option.map ~f:Double.of_xml)
          (Xml.child xml_arg0 "allocatedStorageInGb") in
      let storageType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "storageType") in
      make ?storageThroughput ?iops ?allocatedStorageInGb ?storageType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let storageThroughput =
        field_map json__ "storageThroughput" Double.of_json in
      let iops = field_map json__ "iops" Double.of_json in
      let allocatedStorageInGb =
        field_map json__ "allocatedStorageInGb" Double.of_json in
      let storageType = field_map json__ "storageType" String_.of_json in
      make ?storageThroughput ?iops ?allocatedStorageInGb ?storageType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The Amazon RDS DB instance storage configuration used for recommendations."]
module RdsReservedInstancesConfiguration =
  struct
    type nonrec t =
      {
      accountScope: String_.t option
        [@ocaml.doc "The account scope for which you want recommendations."];
      service: String_.t option
        [@ocaml.doc "The service for which you want recommendations."];
      term: String_.t option
        [@ocaml.doc "The reserved instances recommendation term in years."];
      paymentOption: String_.t option
        [@ocaml.doc "The payment option for the commitment."];
      reservedInstancesRegion: String_.t option
        [@ocaml.doc "The Amazon Web Services Region of the commitment."];
      upfrontCost: String_.t option
        [@ocaml.doc "How much purchasing this instance costs you upfront."];
      monthlyRecurringCost: String_.t option
        [@ocaml.doc
          "How much purchasing this instance costs you on a monthly basis."];
      normalizedUnitsToPurchase: String_.t option
        [@ocaml.doc
          "The number of normalized units that Amazon Web Services recommends that you purchase."];
      numberOfInstancesToPurchase: String_.t option
        [@ocaml.doc
          "The number of instances that Amazon Web Services recommends that you purchase."];
      instanceFamily: String_.t option
        [@ocaml.doc "The instance family of the recommended reservation."];
      instanceType: String_.t option
        [@ocaml.doc
          "The type of instance that Amazon Web Services recommends."];
      sizeFlexEligible: Boolean.t option
        [@ocaml.doc
          "Determines whether the recommendation is size flexible."];
      currentGeneration: String_.t option
        [@ocaml.doc
          "Determines whether the recommendation is for a current generation instance."];
      licenseModel: String_.t option
        [@ocaml.doc
          "The license model that the recommended reservation supports."];
      databaseEdition: String_.t option
        [@ocaml.doc
          "The database edition that the recommended reservation supports."];
      databaseEngine: String_.t option
        [@ocaml.doc
          "The database engine that the recommended reservation supports."];
      deploymentOption: String_.t option
        [@ocaml.doc
          "Determines whether the recommendation is for a reservation in a single Availability Zone or a reservation with a backup in a second Availability Zone."]}
    let make ?accountScope =
      fun ?service ->
        fun ?term ->
          fun ?paymentOption ->
            fun ?reservedInstancesRegion ->
              fun ?upfrontCost ->
                fun ?monthlyRecurringCost ->
                  fun ?normalizedUnitsToPurchase ->
                    fun ?numberOfInstancesToPurchase ->
                      fun ?instanceFamily ->
                        fun ?instanceType ->
                          fun ?sizeFlexEligible ->
                            fun ?currentGeneration ->
                              fun ?licenseModel ->
                                fun ?databaseEdition ->
                                  fun ?databaseEngine ->
                                    fun ?deploymentOption ->
                                      fun () ->
                                        {
                                          accountScope;
                                          service;
                                          term;
                                          paymentOption;
                                          reservedInstancesRegion;
                                          upfrontCost;
                                          monthlyRecurringCost;
                                          normalizedUnitsToPurchase;
                                          numberOfInstancesToPurchase;
                                          instanceFamily;
                                          instanceType;
                                          sizeFlexEligible;
                                          currentGeneration;
                                          licenseModel;
                                          databaseEdition;
                                          databaseEngine;
                                          deploymentOption
                                        }
    let to_value x =
      structure_to_value
        [("accountScope", (Option.map x.accountScope ~f:String_.to_value));
        ("service", (Option.map x.service ~f:String_.to_value));
        ("term", (Option.map x.term ~f:String_.to_value));
        ("paymentOption", (Option.map x.paymentOption ~f:String_.to_value));
        ("reservedInstancesRegion",
          (Option.map x.reservedInstancesRegion ~f:String_.to_value));
        ("upfrontCost", (Option.map x.upfrontCost ~f:String_.to_value));
        ("monthlyRecurringCost",
          (Option.map x.monthlyRecurringCost ~f:String_.to_value));
        ("normalizedUnitsToPurchase",
          (Option.map x.normalizedUnitsToPurchase ~f:String_.to_value));
        ("numberOfInstancesToPurchase",
          (Option.map x.numberOfInstancesToPurchase ~f:String_.to_value));
        ("instanceFamily", (Option.map x.instanceFamily ~f:String_.to_value));
        ("instanceType", (Option.map x.instanceType ~f:String_.to_value));
        ("sizeFlexEligible",
          (Option.map x.sizeFlexEligible ~f:Boolean.to_value));
        ("currentGeneration",
          (Option.map x.currentGeneration ~f:String_.to_value));
        ("licenseModel", (Option.map x.licenseModel ~f:String_.to_value));
        ("databaseEdition",
          (Option.map x.databaseEdition ~f:String_.to_value));
        ("databaseEngine", (Option.map x.databaseEngine ~f:String_.to_value));
        ("deploymentOption",
          (Option.map x.deploymentOption ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deploymentOption =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "deploymentOption") in
      let databaseEngine =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "databaseEngine") in
      let databaseEdition =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "databaseEdition") in
      let licenseModel =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "licenseModel") in
      let currentGeneration =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "currentGeneration") in
      let sizeFlexEligible =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "sizeFlexEligible") in
      let instanceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "instanceType") in
      let instanceFamily =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "instanceFamily") in
      let numberOfInstancesToPurchase =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "numberOfInstancesToPurchase") in
      let normalizedUnitsToPurchase =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "normalizedUnitsToPurchase") in
      let monthlyRecurringCost =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "monthlyRecurringCost") in
      let upfrontCost =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "upfrontCost") in
      let reservedInstancesRegion =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "reservedInstancesRegion") in
      let paymentOption =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "paymentOption") in
      let term = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "term") in
      let service =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "service") in
      let accountScope =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "accountScope") in
      make ?deploymentOption ?databaseEngine ?databaseEdition ?licenseModel
        ?currentGeneration ?sizeFlexEligible ?instanceType ?instanceFamily
        ?numberOfInstancesToPurchase ?normalizedUnitsToPurchase
        ?monthlyRecurringCost ?upfrontCost ?reservedInstancesRegion
        ?paymentOption ?term ?service ?accountScope ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let deploymentOption =
        field_map json__ "deploymentOption" String_.of_json in
      let databaseEngine = field_map json__ "databaseEngine" String_.of_json in
      let databaseEdition =
        field_map json__ "databaseEdition" String_.of_json in
      let licenseModel = field_map json__ "licenseModel" String_.of_json in
      let currentGeneration =
        field_map json__ "currentGeneration" String_.of_json in
      let sizeFlexEligible =
        field_map json__ "sizeFlexEligible" Boolean.of_json in
      let instanceType = field_map json__ "instanceType" String_.of_json in
      let instanceFamily = field_map json__ "instanceFamily" String_.of_json in
      let numberOfInstancesToPurchase =
        field_map json__ "numberOfInstancesToPurchase" String_.of_json in
      let normalizedUnitsToPurchase =
        field_map json__ "normalizedUnitsToPurchase" String_.of_json in
      let monthlyRecurringCost =
        field_map json__ "monthlyRecurringCost" String_.of_json in
      let upfrontCost = field_map json__ "upfrontCost" String_.of_json in
      let reservedInstancesRegion =
        field_map json__ "reservedInstancesRegion" String_.of_json in
      let paymentOption = field_map json__ "paymentOption" String_.of_json in
      let term = field_map json__ "term" String_.of_json in
      let service = field_map json__ "service" String_.of_json in
      let accountScope = field_map json__ "accountScope" String_.of_json in
      make ?deploymentOption ?databaseEngine ?databaseEdition ?licenseModel
        ?currentGeneration ?sizeFlexEligible ?instanceType ?instanceFamily
        ?numberOfInstancesToPurchase ?normalizedUnitsToPurchase
        ?monthlyRecurringCost ?upfrontCost ?reservedInstancesRegion
        ?paymentOption ?term ?service ?accountScope ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The RDS reserved instances configuration used for recommendations."]
module RedshiftReservedInstancesConfiguration =
  struct
    type nonrec t =
      {
      accountScope: String_.t option
        [@ocaml.doc "The account scope for which you want recommendations."];
      service: String_.t option
        [@ocaml.doc "The service for which you want recommendations."];
      term: String_.t option
        [@ocaml.doc "The reserved instances recommendation term in years."];
      paymentOption: String_.t option
        [@ocaml.doc "The payment option for the commitment."];
      reservedInstancesRegion: String_.t option
        [@ocaml.doc "The Amazon Web Services Region of the commitment."];
      upfrontCost: String_.t option
        [@ocaml.doc "How much purchasing this instance costs you upfront."];
      monthlyRecurringCost: String_.t option
        [@ocaml.doc
          "How much purchasing these reserved instances costs you on a monthly basis."];
      normalizedUnitsToPurchase: String_.t option
        [@ocaml.doc
          "The number of normalized units that Amazon Web Services recommends that you purchase."];
      numberOfInstancesToPurchase: String_.t option
        [@ocaml.doc
          "The number of instances that Amazon Web Services recommends that you purchase."];
      instanceFamily: String_.t option
        [@ocaml.doc "The instance family of the recommended reservation."];
      instanceType: String_.t option
        [@ocaml.doc
          "The type of instance that Amazon Web Services recommends."];
      sizeFlexEligible: Boolean.t option
        [@ocaml.doc
          "Determines whether the recommendation is size flexible."];
      currentGeneration: String_.t option
        [@ocaml.doc
          "Determines whether the recommendation is for a current generation instance."]}
    let make ?accountScope =
      fun ?service ->
        fun ?term ->
          fun ?paymentOption ->
            fun ?reservedInstancesRegion ->
              fun ?upfrontCost ->
                fun ?monthlyRecurringCost ->
                  fun ?normalizedUnitsToPurchase ->
                    fun ?numberOfInstancesToPurchase ->
                      fun ?instanceFamily ->
                        fun ?instanceType ->
                          fun ?sizeFlexEligible ->
                            fun ?currentGeneration ->
                              fun () ->
                                {
                                  accountScope;
                                  service;
                                  term;
                                  paymentOption;
                                  reservedInstancesRegion;
                                  upfrontCost;
                                  monthlyRecurringCost;
                                  normalizedUnitsToPurchase;
                                  numberOfInstancesToPurchase;
                                  instanceFamily;
                                  instanceType;
                                  sizeFlexEligible;
                                  currentGeneration
                                }
    let to_value x =
      structure_to_value
        [("accountScope", (Option.map x.accountScope ~f:String_.to_value));
        ("service", (Option.map x.service ~f:String_.to_value));
        ("term", (Option.map x.term ~f:String_.to_value));
        ("paymentOption", (Option.map x.paymentOption ~f:String_.to_value));
        ("reservedInstancesRegion",
          (Option.map x.reservedInstancesRegion ~f:String_.to_value));
        ("upfrontCost", (Option.map x.upfrontCost ~f:String_.to_value));
        ("monthlyRecurringCost",
          (Option.map x.monthlyRecurringCost ~f:String_.to_value));
        ("normalizedUnitsToPurchase",
          (Option.map x.normalizedUnitsToPurchase ~f:String_.to_value));
        ("numberOfInstancesToPurchase",
          (Option.map x.numberOfInstancesToPurchase ~f:String_.to_value));
        ("instanceFamily", (Option.map x.instanceFamily ~f:String_.to_value));
        ("instanceType", (Option.map x.instanceType ~f:String_.to_value));
        ("sizeFlexEligible",
          (Option.map x.sizeFlexEligible ~f:Boolean.to_value));
        ("currentGeneration",
          (Option.map x.currentGeneration ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let currentGeneration =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "currentGeneration") in
      let sizeFlexEligible =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "sizeFlexEligible") in
      let instanceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "instanceType") in
      let instanceFamily =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "instanceFamily") in
      let numberOfInstancesToPurchase =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "numberOfInstancesToPurchase") in
      let normalizedUnitsToPurchase =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "normalizedUnitsToPurchase") in
      let monthlyRecurringCost =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "monthlyRecurringCost") in
      let upfrontCost =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "upfrontCost") in
      let reservedInstancesRegion =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "reservedInstancesRegion") in
      let paymentOption =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "paymentOption") in
      let term = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "term") in
      let service =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "service") in
      let accountScope =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "accountScope") in
      make ?currentGeneration ?sizeFlexEligible ?instanceType ?instanceFamily
        ?numberOfInstancesToPurchase ?normalizedUnitsToPurchase
        ?monthlyRecurringCost ?upfrontCost ?reservedInstancesRegion
        ?paymentOption ?term ?service ?accountScope ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let currentGeneration =
        field_map json__ "currentGeneration" String_.of_json in
      let sizeFlexEligible =
        field_map json__ "sizeFlexEligible" Boolean.of_json in
      let instanceType = field_map json__ "instanceType" String_.of_json in
      let instanceFamily = field_map json__ "instanceFamily" String_.of_json in
      let numberOfInstancesToPurchase =
        field_map json__ "numberOfInstancesToPurchase" String_.of_json in
      let normalizedUnitsToPurchase =
        field_map json__ "normalizedUnitsToPurchase" String_.of_json in
      let monthlyRecurringCost =
        field_map json__ "monthlyRecurringCost" String_.of_json in
      let upfrontCost = field_map json__ "upfrontCost" String_.of_json in
      let reservedInstancesRegion =
        field_map json__ "reservedInstancesRegion" String_.of_json in
      let paymentOption = field_map json__ "paymentOption" String_.of_json in
      let term = field_map json__ "term" String_.of_json in
      let service = field_map json__ "service" String_.of_json in
      let accountScope = field_map json__ "accountScope" String_.of_json in
      make ?currentGeneration ?sizeFlexEligible ?instanceType ?instanceFamily
        ?numberOfInstancesToPurchase ?normalizedUnitsToPurchase
        ?monthlyRecurringCost ?upfrontCost ?reservedInstancesRegion
        ?paymentOption ?term ?service ?accountScope ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The Redshift reserved instances configuration used for recommendations."]
module SageMakerSavingsPlansConfiguration =
  struct
    type nonrec t =
      {
      accountScope: String_.t option
        [@ocaml.doc "The account scope for which you want recommendations."];
      term: String_.t option
        [@ocaml.doc "The Savings Plans recommendation term in years."];
      paymentOption: String_.t option
        [@ocaml.doc "The payment option for the commitment."];
      hourlyCommitment: String_.t option
        [@ocaml.doc "The hourly commitment for the Savings Plans type."]}
    let make ?accountScope =
      fun ?term ->
        fun ?paymentOption ->
          fun ?hourlyCommitment ->
            fun () -> { accountScope; term; paymentOption; hourlyCommitment }
    let to_value x =
      structure_to_value
        [("accountScope", (Option.map x.accountScope ~f:String_.to_value));
        ("term", (Option.map x.term ~f:String_.to_value));
        ("paymentOption", (Option.map x.paymentOption ~f:String_.to_value));
        ("hourlyCommitment",
          (Option.map x.hourlyCommitment ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let hourlyCommitment =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "hourlyCommitment") in
      let paymentOption =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "paymentOption") in
      let term = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "term") in
      let accountScope =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "accountScope") in
      make ?hourlyCommitment ?paymentOption ?term ?accountScope ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let hourlyCommitment =
        field_map json__ "hourlyCommitment" String_.of_json in
      let paymentOption = field_map json__ "paymentOption" String_.of_json in
      let term = field_map json__ "term" String_.of_json in
      let accountScope = field_map json__ "accountScope" String_.of_json in
      make ?hourlyCommitment ?paymentOption ?term ?accountScope ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The SageMaker Savings Plans configuration used for recommendations."]
module PaymentOption =
  struct
    type nonrec t =
      | AllUpfront 
      | PartialUpfront 
      | NoUpfront 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | AllUpfront -> "AllUpfront"
      | PartialUpfront -> "PartialUpfront"
      | NoUpfront -> "NoUpfront"
      | Non_static_id s -> s
    let of_string =
      function
      | "AllUpfront" -> AllUpfront
      | "PartialUpfront" -> PartialUpfront
      | "NoUpfront" -> NoUpfront
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration PaymentOption" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"PaymentOption" j)
    let to_json = simple_to_json to_value
  end
module Term =
  struct
    type nonrec t =
      | OneYear 
      | ThreeYears 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | OneYear -> "OneYear"
      | ThreeYears -> "ThreeYears"
      | Non_static_id s -> s
    let of_string =
      function
      | "OneYear" -> OneYear
      | "ThreeYears" -> ThreeYears
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Term" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Term" j)
    let to_json = simple_to_json to_value
  end
module ValidationExceptionDetails =
  struct
    type nonrec t = ValidationExceptionDetail.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ValidationExceptionDetail.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ValidationExceptionDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"ValidationExceptionDetails"
        ~of_json:ValidationExceptionDetail.of_json j
    let to_json v = composed_to_json to_value v
  end
module ValidationExceptionReason =
  struct
    type nonrec t =
      | FieldValidationFailed 
      | Other 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | FieldValidationFailed -> "FieldValidationFailed"
      | Other -> "Other"
      | Non_static_id s -> s
    let of_string =
      function
      | "FieldValidationFailed" -> FieldValidationFailed
      | "Other" -> Other
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ValidationExceptionReason" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ValidationExceptionReason" j)
    let to_json = simple_to_json to_value
  end
module Recommendation =
  struct
    type nonrec t =
      {
      recommendationId: String_.t option
        [@ocaml.doc "The ID for the recommendation."];
      accountId: String_.t option
        [@ocaml.doc "The account to which the recommendation applies."];
      region: String_.t option
        [@ocaml.doc "The Amazon Web Services Region of the resource."];
      resourceId: String_.t option
        [@ocaml.doc "The resource ID for the recommendation."];
      resourceArn: String_.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) for the recommendation."];
      currentResourceType: String_.t option
        [@ocaml.doc "The current resource type."];
      recommendedResourceType: String_.t option
        [@ocaml.doc "The recommended resource type."];
      estimatedMonthlySavings: Double.t option
        [@ocaml.doc
          "The estimated monthly savings amount for the recommendation."];
      estimatedSavingsPercentage: Double.t option
        [@ocaml.doc
          "The estimated savings percentage relative to the total cost over the cost calculation lookback period."];
      estimatedMonthlyCost: Double.t option
        [@ocaml.doc
          "The estimated monthly cost of the current resource. For Reserved Instances and Savings Plans, it refers to the cost for eligible usage."];
      currencyCode: String_.t option
        [@ocaml.doc "The currency code used for the recommendation."];
      implementationEffort: String_.t option
        [@ocaml.doc "The effort required to implement the recommendation."];
      restartNeeded: Boolean.t option
        [@ocaml.doc
          "Whether or not implementing the recommendation requires a restart."];
      actionType: String_.t option
        [@ocaml.doc
          "The type of tasks that can be carried out by this action."];
      rollbackPossible: Boolean.t option
        [@ocaml.doc
          "Whether or not implementing the recommendation can be rolled back."];
      currentResourceSummary: String_.t option
        [@ocaml.doc "Describes the current resource."];
      recommendedResourceSummary: String_.t option
        [@ocaml.doc "Describes the recommended resource."];
      lastRefreshTimestamp: Datetime.t option
        [@ocaml.doc "The time when the recommendation was last generated."];
      recommendationLookbackPeriodInDays: Integer.t option
        [@ocaml.doc
          "The lookback period that's used to generate the recommendation."];
      source: Source.t option
        [@ocaml.doc "The source of the recommendation."];
      tags: TagList.t option
        [@ocaml.doc "A list of tags assigned to the recommendation."]}
    let make ?recommendationId =
      fun ?accountId ->
        fun ?region ->
          fun ?resourceId ->
            fun ?resourceArn ->
              fun ?currentResourceType ->
                fun ?recommendedResourceType ->
                  fun ?estimatedMonthlySavings ->
                    fun ?estimatedSavingsPercentage ->
                      fun ?estimatedMonthlyCost ->
                        fun ?currencyCode ->
                          fun ?implementationEffort ->
                            fun ?restartNeeded ->
                              fun ?actionType ->
                                fun ?rollbackPossible ->
                                  fun ?currentResourceSummary ->
                                    fun ?recommendedResourceSummary ->
                                      fun ?lastRefreshTimestamp ->
                                        fun
                                          ?recommendationLookbackPeriodInDays
                                          ->
                                          fun ?source ->
                                            fun ?tags ->
                                              fun () ->
                                                {
                                                  recommendationId;
                                                  accountId;
                                                  region;
                                                  resourceId;
                                                  resourceArn;
                                                  currentResourceType;
                                                  recommendedResourceType;
                                                  estimatedMonthlySavings;
                                                  estimatedSavingsPercentage;
                                                  estimatedMonthlyCost;
                                                  currencyCode;
                                                  implementationEffort;
                                                  restartNeeded;
                                                  actionType;
                                                  rollbackPossible;
                                                  currentResourceSummary;
                                                  recommendedResourceSummary;
                                                  lastRefreshTimestamp;
                                                  recommendationLookbackPeriodInDays;
                                                  source;
                                                  tags
                                                }
    let to_value x =
      structure_to_value
        [("recommendationId",
           (Option.map x.recommendationId ~f:String_.to_value));
        ("accountId", (Option.map x.accountId ~f:String_.to_value));
        ("region", (Option.map x.region ~f:String_.to_value));
        ("resourceId", (Option.map x.resourceId ~f:String_.to_value));
        ("resourceArn", (Option.map x.resourceArn ~f:String_.to_value));
        ("currentResourceType",
          (Option.map x.currentResourceType ~f:String_.to_value));
        ("recommendedResourceType",
          (Option.map x.recommendedResourceType ~f:String_.to_value));
        ("estimatedMonthlySavings",
          (Option.map x.estimatedMonthlySavings ~f:Double.to_value));
        ("estimatedSavingsPercentage",
          (Option.map x.estimatedSavingsPercentage ~f:Double.to_value));
        ("estimatedMonthlyCost",
          (Option.map x.estimatedMonthlyCost ~f:Double.to_value));
        ("currencyCode", (Option.map x.currencyCode ~f:String_.to_value));
        ("implementationEffort",
          (Option.map x.implementationEffort ~f:String_.to_value));
        ("restartNeeded", (Option.map x.restartNeeded ~f:Boolean.to_value));
        ("actionType", (Option.map x.actionType ~f:String_.to_value));
        ("rollbackPossible",
          (Option.map x.rollbackPossible ~f:Boolean.to_value));
        ("currentResourceSummary",
          (Option.map x.currentResourceSummary ~f:String_.to_value));
        ("recommendedResourceSummary",
          (Option.map x.recommendedResourceSummary ~f:String_.to_value));
        ("lastRefreshTimestamp",
          (Option.map x.lastRefreshTimestamp ~f:Datetime.to_value));
        ("recommendationLookbackPeriodInDays",
          (Option.map x.recommendationLookbackPeriodInDays
             ~f:Integer.to_value));
        ("source", (Option.map x.source ~f:Source.to_value));
        ("tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tags") in
      let source =
        (Option.map ~f:Source.of_xml) (Xml.child xml_arg0 "source") in
      let recommendationLookbackPeriodInDays =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "recommendationLookbackPeriodInDays") in
      let lastRefreshTimestamp =
        (Option.map ~f:Datetime.of_xml)
          (Xml.child xml_arg0 "lastRefreshTimestamp") in
      let recommendedResourceSummary =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "recommendedResourceSummary") in
      let currentResourceSummary =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "currentResourceSummary") in
      let rollbackPossible =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "rollbackPossible") in
      let actionType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "actionType") in
      let restartNeeded =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "restartNeeded") in
      let implementationEffort =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "implementationEffort") in
      let currencyCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "currencyCode") in
      let estimatedMonthlyCost =
        (Option.map ~f:Double.of_xml)
          (Xml.child xml_arg0 "estimatedMonthlyCost") in
      let estimatedSavingsPercentage =
        (Option.map ~f:Double.of_xml)
          (Xml.child xml_arg0 "estimatedSavingsPercentage") in
      let estimatedMonthlySavings =
        (Option.map ~f:Double.of_xml)
          (Xml.child xml_arg0 "estimatedMonthlySavings") in
      let recommendedResourceType =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "recommendedResourceType") in
      let currentResourceType =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "currentResourceType") in
      let resourceArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "resourceArn") in
      let resourceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "resourceId") in
      let region =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "region") in
      let accountId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "accountId") in
      let recommendationId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "recommendationId") in
      make ?tags ?source ?recommendationLookbackPeriodInDays
        ?lastRefreshTimestamp ?recommendedResourceSummary
        ?currentResourceSummary ?rollbackPossible ?actionType ?restartNeeded
        ?implementationEffort ?currencyCode ?estimatedMonthlyCost
        ?estimatedSavingsPercentage ?estimatedMonthlySavings
        ?recommendedResourceType ?currentResourceType ?resourceArn
        ?resourceId ?region ?accountId ?recommendationId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagList.of_json in
      let source = field_map json__ "source" Source.of_json in
      let recommendationLookbackPeriodInDays =
        field_map json__ "recommendationLookbackPeriodInDays" Integer.of_json in
      let lastRefreshTimestamp =
        field_map json__ "lastRefreshTimestamp" Datetime.of_json in
      let recommendedResourceSummary =
        field_map json__ "recommendedResourceSummary" String_.of_json in
      let currentResourceSummary =
        field_map json__ "currentResourceSummary" String_.of_json in
      let rollbackPossible =
        field_map json__ "rollbackPossible" Boolean.of_json in
      let actionType = field_map json__ "actionType" String_.of_json in
      let restartNeeded = field_map json__ "restartNeeded" Boolean.of_json in
      let implementationEffort =
        field_map json__ "implementationEffort" String_.of_json in
      let currencyCode = field_map json__ "currencyCode" String_.of_json in
      let estimatedMonthlyCost =
        field_map json__ "estimatedMonthlyCost" Double.of_json in
      let estimatedSavingsPercentage =
        field_map json__ "estimatedSavingsPercentage" Double.of_json in
      let estimatedMonthlySavings =
        field_map json__ "estimatedMonthlySavings" Double.of_json in
      let recommendedResourceType =
        field_map json__ "recommendedResourceType" String_.of_json in
      let currentResourceType =
        field_map json__ "currentResourceType" String_.of_json in
      let resourceArn = field_map json__ "resourceArn" String_.of_json in
      let resourceId = field_map json__ "resourceId" String_.of_json in
      let region = field_map json__ "region" String_.of_json in
      let accountId = field_map json__ "accountId" String_.of_json in
      let recommendationId =
        field_map json__ "recommendationId" String_.of_json in
      make ?tags ?source ?recommendationLookbackPeriodInDays
        ?lastRefreshTimestamp ?recommendedResourceSummary
        ?currentResourceSummary ?rollbackPossible ?actionType ?restartNeeded
        ?implementationEffort ?currencyCode ?estimatedMonthlyCost
        ?estimatedSavingsPercentage ?estimatedMonthlySavings
        ?recommendedResourceType ?currentResourceType ?resourceArn
        ?resourceId ?region ?accountId ?recommendationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes a recommendation."]
module AccountIdList =
  struct
    type nonrec t = AccountId.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AccountId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AccountId.of_xml)
    let of_json j =
      list_of_json ~kind:"AccountIdList" ~of_json:AccountId.of_json j
    let to_json v = composed_to_json to_value v
  end
module ActionTypeList =
  struct
    type nonrec t = ActionType.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ActionType.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ActionType.of_xml)
    let of_json j =
      list_of_json ~kind:"ActionTypeList" ~of_json:ActionType.of_json j
    let to_json v = composed_to_json to_value v
  end
module ImplementationEffortList =
  struct
    type nonrec t = ImplementationEffort.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ImplementationEffort.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ImplementationEffort.of_xml)
    let of_json j =
      list_of_json ~kind:"ImplementationEffortList"
        ~of_json:ImplementationEffort.of_json j
    let to_json v = composed_to_json to_value v
  end
module RecommendationIdList =
  struct
    type nonrec t = String_.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"RecommendationIdList" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module RegionList =
  struct
    type nonrec t = String_.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"RegionList" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceArnList =
  struct
    type nonrec t = String_.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"ResourceArnList" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceIdList =
  struct
    type nonrec t = String_.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"ResourceIdList" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceTypeList =
  struct
    type nonrec t = ResourceType.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ResourceType.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResourceType.of_xml)
    let of_json j =
      list_of_json ~kind:"ResourceTypeList" ~of_json:ResourceType.of_json j
    let to_json v = composed_to_json to_value v
  end
module Order =
  struct
    type nonrec t =
      | Asc 
      | Desc 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | Asc -> "Asc" | Desc -> "Desc" | Non_static_id s -> s
    let of_string =
      function | "Asc" -> Asc | "Desc" -> Desc | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Order" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Order" j)
    let to_json = simple_to_json to_value
  end
module RecommendationSummary =
  struct
    type nonrec t =
      {
      group: String_.t option [@ocaml.doc "The grouping of recommendations."];
      estimatedMonthlySavings: Double.t option
        [@ocaml.doc
          "The estimated total savings resulting from modifications, on a monthly basis."];
      recommendationCount: Integer.t option
        [@ocaml.doc "The total number of instance recommendations."]}
    let make ?group =
      fun ?estimatedMonthlySavings ->
        fun ?recommendationCount ->
          fun () -> { group; estimatedMonthlySavings; recommendationCount }
    let to_value x =
      structure_to_value
        [("group", (Option.map x.group ~f:String_.to_value));
        ("estimatedMonthlySavings",
          (Option.map x.estimatedMonthlySavings ~f:Double.to_value));
        ("recommendationCount",
          (Option.map x.recommendationCount ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recommendationCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "recommendationCount") in
      let estimatedMonthlySavings =
        (Option.map ~f:Double.of_xml)
          (Xml.child xml_arg0 "estimatedMonthlySavings") in
      let group = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "group") in
      make ?recommendationCount ?estimatedMonthlySavings ?group ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let recommendationCount =
        field_map json__ "recommendationCount" Integer.of_json in
      let estimatedMonthlySavings =
        field_map json__ "estimatedMonthlySavings" Double.of_json in
      let group = field_map json__ "group" String_.of_json in
      make ?recommendationCount ?estimatedMonthlySavings ?group ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The summary of rightsizing recommendations, including de-duped savings from all types of recommendations."]
module SummaryMetrics =
  struct
    type nonrec t =
      | SavingsPercentage 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SavingsPercentage -> "SavingsPercentage"
      | Non_static_id s -> s
    let of_string =
      function
      | "SavingsPercentage" -> SavingsPercentage
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration SummaryMetrics" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SummaryMetrics" j)
    let to_json = simple_to_json to_value
  end
module AccountEnrollmentStatus =
  struct
    type nonrec t =
      {
      accountId: AccountId.t option
        [@ocaml.doc "The Amazon Web Services account ID."];
      status: EnrollmentStatus.t option
        [@ocaml.doc "The account enrollment status."];
      lastUpdatedTimestamp: Timestamp.t option
        [@ocaml.doc
          "The time when the account enrollment status was last updated."];
      createdTimestamp: Timestamp.t option
        [@ocaml.doc
          "The time when the account enrollment status was created."]}
    let make ?accountId =
      fun ?status ->
        fun ?lastUpdatedTimestamp ->
          fun ?createdTimestamp ->
            fun () ->
              { accountId; status; lastUpdatedTimestamp; createdTimestamp }
    let to_value x =
      structure_to_value
        [("accountId", (Option.map x.accountId ~f:AccountId.to_value));
        ("status", (Option.map x.status ~f:EnrollmentStatus.to_value));
        ("lastUpdatedTimestamp",
          (Option.map x.lastUpdatedTimestamp ~f:Timestamp.to_value));
        ("createdTimestamp",
          (Option.map x.createdTimestamp ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let createdTimestamp =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "createdTimestamp") in
      let lastUpdatedTimestamp =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "lastUpdatedTimestamp") in
      let status =
        (Option.map ~f:EnrollmentStatus.of_xml) (Xml.child xml_arg0 "status") in
      let accountId =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "accountId") in
      make ?createdTimestamp ?lastUpdatedTimestamp ?status ?accountId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let createdTimestamp =
        field_map json__ "createdTimestamp" Timestamp.of_json in
      let lastUpdatedTimestamp =
        field_map json__ "lastUpdatedTimestamp" Timestamp.of_json in
      let status = field_map json__ "status" EnrollmentStatus.of_json in
      let accountId = field_map json__ "accountId" AccountId.of_json in
      make ?createdTimestamp ?lastUpdatedTimestamp ?status ?accountId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the enrollment status of an organization's member accounts in Cost Optimization Hub."]
module EfficiencyMetricsByGroup =
  struct
    type nonrec t =
      {
      metricsByTime: MetricsByTimeList.t option
        [@ocaml.doc
          "A list of time-series data points containing efficiency metrics for this group. Each data point includes an efficiency score, estimated savings, spending, and a timestamp corresponding to the specified granularity. This field is null when efficiency metrics cannot be calculated for the group, in which case the message field provides an explanation."];
      group: String_.t option
        [@ocaml.doc
          "The value of the grouping dimension for this set of metrics. For example, if grouped by account ID, this field contains the account ID. If no grouping is specified, this field is empty."];
      message: String_.t option
        [@ocaml.doc
          "An explanation of why efficiency metrics could not be calculated for this group when the metricsByTime field is null. Common reasons include insufficient or inconclusive cost and usage data during the specified time period. This field is null or empty when metrics are successfully calculated."]}
    let make ?metricsByTime =
      fun ?group ->
        fun ?message -> fun () -> { metricsByTime; group; message }
    let to_value x =
      structure_to_value
        [("metricsByTime",
           (Option.map x.metricsByTime ~f:MetricsByTimeList.to_value));
        ("group", (Option.map x.group ~f:String_.to_value));
        ("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let group = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "group") in
      let metricsByTime =
        (Option.map ~f:MetricsByTimeList.of_xml)
          (Xml.child xml_arg0 "metricsByTime") in
      make ?message ?group ?metricsByTime ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      let group = field_map json__ "group" String_.of_json in
      let metricsByTime =
        field_map json__ "metricsByTime" MetricsByTimeList.of_json in
      make ?message ?group ?metricsByTime ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains cost efficiency metrics for a specific group over time. The group is defined by the grouping dimension specified in the request, such as account ID, Amazon Web Services Region."]
module AuroraDbClusterStorage =
  struct
    type nonrec t =
      {
      configuration: AuroraDbClusterStorageConfiguration.t option
        [@ocaml.doc
          "The Aurora DB cluster storage configuration used for recommendations."];
      costCalculation: ResourceCostCalculation.t option }
    let make ?configuration =
      fun ?costCalculation -> fun () -> { configuration; costCalculation }
    let to_value x =
      structure_to_value
        [("configuration",
           (Option.map x.configuration
              ~f:AuroraDbClusterStorageConfiguration.to_value));
        ("costCalculation",
          (Option.map x.costCalculation ~f:ResourceCostCalculation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let costCalculation =
        (Option.map ~f:ResourceCostCalculation.of_xml)
          (Xml.child xml_arg0 "costCalculation") in
      let configuration =
        (Option.map ~f:AuroraDbClusterStorageConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      make ?costCalculation ?configuration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let costCalculation =
        field_map json__ "costCalculation" ResourceCostCalculation.of_json in
      let configuration =
        field_map json__ "configuration"
          AuroraDbClusterStorageConfiguration.of_json in
      make ?costCalculation ?configuration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the details of an Aurora DB cluster storage."]
module ComputeSavingsPlans =
  struct
    type nonrec t =
      {
      configuration: ComputeSavingsPlansConfiguration.t option
        [@ocaml.doc
          "Configuration details of the Compute Savings Plans to purchase."];
      costCalculation: SavingsPlansCostCalculation.t option
        [@ocaml.doc
          "Cost impact of the Savings Plans purchase recommendation."]}
    let make ?configuration =
      fun ?costCalculation -> fun () -> { configuration; costCalculation }
    let to_value x =
      structure_to_value
        [("configuration",
           (Option.map x.configuration
              ~f:ComputeSavingsPlansConfiguration.to_value));
        ("costCalculation",
          (Option.map x.costCalculation
             ~f:SavingsPlansCostCalculation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let costCalculation =
        (Option.map ~f:SavingsPlansCostCalculation.of_xml)
          (Xml.child xml_arg0 "costCalculation") in
      let configuration =
        (Option.map ~f:ComputeSavingsPlansConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      make ?costCalculation ?configuration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let costCalculation =
        field_map json__ "costCalculation"
          SavingsPlansCostCalculation.of_json in
      let configuration =
        field_map json__ "configuration"
          ComputeSavingsPlansConfiguration.of_json in
      make ?costCalculation ?configuration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The Compute Savings Plans recommendation details."]
module DynamoDbReservedCapacity =
  struct
    type nonrec t =
      {
      configuration: DynamoDbReservedCapacityConfiguration.t option
        [@ocaml.doc
          "The DynamoDB reserved capacity configuration used for recommendations."];
      costCalculation: ReservedInstancesCostCalculation.t option }
    let make ?configuration =
      fun ?costCalculation -> fun () -> { configuration; costCalculation }
    let to_value x =
      structure_to_value
        [("configuration",
           (Option.map x.configuration
              ~f:DynamoDbReservedCapacityConfiguration.to_value));
        ("costCalculation",
          (Option.map x.costCalculation
             ~f:ReservedInstancesCostCalculation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let costCalculation =
        (Option.map ~f:ReservedInstancesCostCalculation.of_xml)
          (Xml.child xml_arg0 "costCalculation") in
      let configuration =
        (Option.map ~f:DynamoDbReservedCapacityConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      make ?costCalculation ?configuration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let costCalculation =
        field_map json__ "costCalculation"
          ReservedInstancesCostCalculation.of_json in
      let configuration =
        field_map json__ "configuration"
          DynamoDbReservedCapacityConfiguration.of_json in
      make ?costCalculation ?configuration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The DynamoDB reserved capacity recommendation details."]
module EbsVolume =
  struct
    type nonrec t =
      {
      configuration: EbsVolumeConfiguration.t option
        [@ocaml.doc
          "The Amazon Elastic Block Store volume configuration used for recommendations."];
      costCalculation: ResourceCostCalculation.t option
        [@ocaml.doc "Cost impact of the recommendation."]}
    let make ?configuration =
      fun ?costCalculation -> fun () -> { configuration; costCalculation }
    let to_value x =
      structure_to_value
        [("configuration",
           (Option.map x.configuration ~f:EbsVolumeConfiguration.to_value));
        ("costCalculation",
          (Option.map x.costCalculation ~f:ResourceCostCalculation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let costCalculation =
        (Option.map ~f:ResourceCostCalculation.of_xml)
          (Xml.child xml_arg0 "costCalculation") in
      let configuration =
        (Option.map ~f:EbsVolumeConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      make ?costCalculation ?configuration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let costCalculation =
        field_map json__ "costCalculation" ResourceCostCalculation.of_json in
      let configuration =
        field_map json__ "configuration" EbsVolumeConfiguration.of_json in
      make ?costCalculation ?configuration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the Amazon Elastic Block Store volume configuration of the current and recommended resource configuration for a recommendation."]
module Ec2AutoScalingGroup =
  struct
    type nonrec t =
      {
      configuration: Ec2AutoScalingGroupConfiguration.t option
        [@ocaml.doc
          "The EC2 Auto Scaling group configuration used for recommendations."];
      costCalculation: ResourceCostCalculation.t option
        [@ocaml.doc "Cost impact of the recommendation."]}
    let make ?configuration =
      fun ?costCalculation -> fun () -> { configuration; costCalculation }
    let to_value x =
      structure_to_value
        [("configuration",
           (Option.map x.configuration
              ~f:Ec2AutoScalingGroupConfiguration.to_value));
        ("costCalculation",
          (Option.map x.costCalculation ~f:ResourceCostCalculation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let costCalculation =
        (Option.map ~f:ResourceCostCalculation.of_xml)
          (Xml.child xml_arg0 "costCalculation") in
      let configuration =
        (Option.map ~f:Ec2AutoScalingGroupConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      make ?costCalculation ?configuration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let costCalculation =
        field_map json__ "costCalculation" ResourceCostCalculation.of_json in
      let configuration =
        field_map json__ "configuration"
          Ec2AutoScalingGroupConfiguration.of_json in
      make ?costCalculation ?configuration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The EC2 Auto Scaling group recommendation details."]
module Ec2Instance =
  struct
    type nonrec t =
      {
      configuration: Ec2InstanceConfiguration.t option
        [@ocaml.doc
          "The EC2 instance configuration used for recommendations."];
      costCalculation: ResourceCostCalculation.t option
        [@ocaml.doc "Cost impact of the recommendation."]}
    let make ?configuration =
      fun ?costCalculation -> fun () -> { configuration; costCalculation }
    let to_value x =
      structure_to_value
        [("configuration",
           (Option.map x.configuration ~f:Ec2InstanceConfiguration.to_value));
        ("costCalculation",
          (Option.map x.costCalculation ~f:ResourceCostCalculation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let costCalculation =
        (Option.map ~f:ResourceCostCalculation.of_xml)
          (Xml.child xml_arg0 "costCalculation") in
      let configuration =
        (Option.map ~f:Ec2InstanceConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      make ?costCalculation ?configuration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let costCalculation =
        field_map json__ "costCalculation" ResourceCostCalculation.of_json in
      let configuration =
        field_map json__ "configuration" Ec2InstanceConfiguration.of_json in
      make ?costCalculation ?configuration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the EC2 instance configuration of the current and recommended resource configuration for a recommendation."]
module Ec2InstanceSavingsPlans =
  struct
    type nonrec t =
      {
      configuration: Ec2InstanceSavingsPlansConfiguration.t option
        [@ocaml.doc
          "The EC2 instance Savings Plans configuration used for recommendations."];
      costCalculation: SavingsPlansCostCalculation.t option
        [@ocaml.doc
          "Cost impact of the Savings Plans purchase recommendation."]}
    let make ?configuration =
      fun ?costCalculation -> fun () -> { configuration; costCalculation }
    let to_value x =
      structure_to_value
        [("configuration",
           (Option.map x.configuration
              ~f:Ec2InstanceSavingsPlansConfiguration.to_value));
        ("costCalculation",
          (Option.map x.costCalculation
             ~f:SavingsPlansCostCalculation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let costCalculation =
        (Option.map ~f:SavingsPlansCostCalculation.of_xml)
          (Xml.child xml_arg0 "costCalculation") in
      let configuration =
        (Option.map ~f:Ec2InstanceSavingsPlansConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      make ?costCalculation ?configuration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let costCalculation =
        field_map json__ "costCalculation"
          SavingsPlansCostCalculation.of_json in
      let configuration =
        field_map json__ "configuration"
          Ec2InstanceSavingsPlansConfiguration.of_json in
      make ?costCalculation ?configuration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The EC2 instance Savings Plans recommendation details."]
module Ec2ReservedInstances =
  struct
    type nonrec t =
      {
      configuration: Ec2ReservedInstancesConfiguration.t option
        [@ocaml.doc
          "The EC2 reserved instances configuration used for recommendations."];
      costCalculation: ReservedInstancesCostCalculation.t option
        [@ocaml.doc "Cost impact of the purchase recommendation."]}
    let make ?configuration =
      fun ?costCalculation -> fun () -> { configuration; costCalculation }
    let to_value x =
      structure_to_value
        [("configuration",
           (Option.map x.configuration
              ~f:Ec2ReservedInstancesConfiguration.to_value));
        ("costCalculation",
          (Option.map x.costCalculation
             ~f:ReservedInstancesCostCalculation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let costCalculation =
        (Option.map ~f:ReservedInstancesCostCalculation.of_xml)
          (Xml.child xml_arg0 "costCalculation") in
      let configuration =
        (Option.map ~f:Ec2ReservedInstancesConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      make ?costCalculation ?configuration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let costCalculation =
        field_map json__ "costCalculation"
          ReservedInstancesCostCalculation.of_json in
      let configuration =
        field_map json__ "configuration"
          Ec2ReservedInstancesConfiguration.of_json in
      make ?costCalculation ?configuration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The EC2 reserved instances recommendation details."]
module EcsService =
  struct
    type nonrec t =
      {
      configuration: EcsServiceConfiguration.t option
        [@ocaml.doc
          "The ECS service configuration used for recommendations."];
      costCalculation: ResourceCostCalculation.t option
        [@ocaml.doc "Cost impact of the recommendation."]}
    let make ?configuration =
      fun ?costCalculation -> fun () -> { configuration; costCalculation }
    let to_value x =
      structure_to_value
        [("configuration",
           (Option.map x.configuration ~f:EcsServiceConfiguration.to_value));
        ("costCalculation",
          (Option.map x.costCalculation ~f:ResourceCostCalculation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let costCalculation =
        (Option.map ~f:ResourceCostCalculation.of_xml)
          (Xml.child xml_arg0 "costCalculation") in
      let configuration =
        (Option.map ~f:EcsServiceConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      make ?costCalculation ?configuration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let costCalculation =
        field_map json__ "costCalculation" ResourceCostCalculation.of_json in
      let configuration =
        field_map json__ "configuration" EcsServiceConfiguration.of_json in
      make ?costCalculation ?configuration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The ECS service recommendation details."]
module ElastiCacheReservedInstances =
  struct
    type nonrec t =
      {
      configuration: ElastiCacheReservedInstancesConfiguration.t option
        [@ocaml.doc
          "The ElastiCache reserved instances configuration used for recommendations."];
      costCalculation: ReservedInstancesCostCalculation.t option
        [@ocaml.doc "Cost impact of the purchase recommendation."]}
    let make ?configuration =
      fun ?costCalculation -> fun () -> { configuration; costCalculation }
    let to_value x =
      structure_to_value
        [("configuration",
           (Option.map x.configuration
              ~f:ElastiCacheReservedInstancesConfiguration.to_value));
        ("costCalculation",
          (Option.map x.costCalculation
             ~f:ReservedInstancesCostCalculation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let costCalculation =
        (Option.map ~f:ReservedInstancesCostCalculation.of_xml)
          (Xml.child xml_arg0 "costCalculation") in
      let configuration =
        (Option.map ~f:ElastiCacheReservedInstancesConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      make ?costCalculation ?configuration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let costCalculation =
        field_map json__ "costCalculation"
          ReservedInstancesCostCalculation.of_json in
      let configuration =
        field_map json__ "configuration"
          ElastiCacheReservedInstancesConfiguration.of_json in
      make ?costCalculation ?configuration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The ElastiCache reserved instances recommendation details."]
module LambdaFunction =
  struct
    type nonrec t =
      {
      configuration: LambdaFunctionConfiguration.t option
        [@ocaml.doc
          "The Lambda function configuration used for recommendations."];
      costCalculation: ResourceCostCalculation.t option
        [@ocaml.doc "Cost impact of the recommendation."]}
    let make ?configuration =
      fun ?costCalculation -> fun () -> { configuration; costCalculation }
    let to_value x =
      structure_to_value
        [("configuration",
           (Option.map x.configuration
              ~f:LambdaFunctionConfiguration.to_value));
        ("costCalculation",
          (Option.map x.costCalculation ~f:ResourceCostCalculation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let costCalculation =
        (Option.map ~f:ResourceCostCalculation.of_xml)
          (Xml.child xml_arg0 "costCalculation") in
      let configuration =
        (Option.map ~f:LambdaFunctionConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      make ?costCalculation ?configuration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let costCalculation =
        field_map json__ "costCalculation" ResourceCostCalculation.of_json in
      let configuration =
        field_map json__ "configuration" LambdaFunctionConfiguration.of_json in
      make ?costCalculation ?configuration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The Lambda function recommendation details."]
module MemoryDbReservedInstances =
  struct
    type nonrec t =
      {
      configuration: MemoryDbReservedInstancesConfiguration.t option
        [@ocaml.doc
          "The MemoryDB reserved instances configuration used for recommendations."];
      costCalculation: ReservedInstancesCostCalculation.t option }
    let make ?configuration =
      fun ?costCalculation -> fun () -> { configuration; costCalculation }
    let to_value x =
      structure_to_value
        [("configuration",
           (Option.map x.configuration
              ~f:MemoryDbReservedInstancesConfiguration.to_value));
        ("costCalculation",
          (Option.map x.costCalculation
             ~f:ReservedInstancesCostCalculation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let costCalculation =
        (Option.map ~f:ReservedInstancesCostCalculation.of_xml)
          (Xml.child xml_arg0 "costCalculation") in
      let configuration =
        (Option.map ~f:MemoryDbReservedInstancesConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      make ?costCalculation ?configuration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let costCalculation =
        field_map json__ "costCalculation"
          ReservedInstancesCostCalculation.of_json in
      let configuration =
        field_map json__ "configuration"
          MemoryDbReservedInstancesConfiguration.of_json in
      make ?costCalculation ?configuration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The MemoryDB reserved instances recommendation details. While the API reference uses \"MemoryDB reserved instances\", the user guide and other documentation refer to them as \"MemoryDB reserved nodes\", as the terms are used interchangeably."]
module NatGateway =
  struct
    type nonrec t =
      {
      configuration: NatGatewayConfiguration.t option
        [@ocaml.doc
          "The NAT Gateway configuration used for recommendations."];
      costCalculation: ResourceCostCalculation.t option }
    let make ?configuration =
      fun ?costCalculation -> fun () -> { configuration; costCalculation }
    let to_value x =
      structure_to_value
        [("configuration",
           (Option.map x.configuration ~f:NatGatewayConfiguration.to_value));
        ("costCalculation",
          (Option.map x.costCalculation ~f:ResourceCostCalculation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let costCalculation =
        (Option.map ~f:ResourceCostCalculation.of_xml)
          (Xml.child xml_arg0 "costCalculation") in
      let configuration =
        (Option.map ~f:NatGatewayConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      make ?costCalculation ?configuration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let costCalculation =
        field_map json__ "costCalculation" ResourceCostCalculation.of_json in
      let configuration =
        field_map json__ "configuration" NatGatewayConfiguration.of_json in
      make ?costCalculation ?configuration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The NAT Gateway recommendation details."]
module OpenSearchReservedInstances =
  struct
    type nonrec t =
      {
      configuration: OpenSearchReservedInstancesConfiguration.t option
        [@ocaml.doc
          "The OpenSearch reserved instances configuration used for recommendations."];
      costCalculation: ReservedInstancesCostCalculation.t option
        [@ocaml.doc "Cost impact of the purchase recommendation."]}
    let make ?configuration =
      fun ?costCalculation -> fun () -> { configuration; costCalculation }
    let to_value x =
      structure_to_value
        [("configuration",
           (Option.map x.configuration
              ~f:OpenSearchReservedInstancesConfiguration.to_value));
        ("costCalculation",
          (Option.map x.costCalculation
             ~f:ReservedInstancesCostCalculation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let costCalculation =
        (Option.map ~f:ReservedInstancesCostCalculation.of_xml)
          (Xml.child xml_arg0 "costCalculation") in
      let configuration =
        (Option.map ~f:OpenSearchReservedInstancesConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      make ?costCalculation ?configuration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let costCalculation =
        field_map json__ "costCalculation"
          ReservedInstancesCostCalculation.of_json in
      let configuration =
        field_map json__ "configuration"
          OpenSearchReservedInstancesConfiguration.of_json in
      make ?costCalculation ?configuration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The OpenSearch reserved instances recommendation details."]
module RdsDbInstance =
  struct
    type nonrec t =
      {
      configuration: RdsDbInstanceConfiguration.t option
        [@ocaml.doc
          "The Amazon RDS DB instance configuration used for recommendations."];
      costCalculation: ResourceCostCalculation.t option }
    let make ?configuration =
      fun ?costCalculation -> fun () -> { configuration; costCalculation }
    let to_value x =
      structure_to_value
        [("configuration",
           (Option.map x.configuration ~f:RdsDbInstanceConfiguration.to_value));
        ("costCalculation",
          (Option.map x.costCalculation ~f:ResourceCostCalculation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let costCalculation =
        (Option.map ~f:ResourceCostCalculation.of_xml)
          (Xml.child xml_arg0 "costCalculation") in
      let configuration =
        (Option.map ~f:RdsDbInstanceConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      make ?costCalculation ?configuration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let costCalculation =
        field_map json__ "costCalculation" ResourceCostCalculation.of_json in
      let configuration =
        field_map json__ "configuration" RdsDbInstanceConfiguration.of_json in
      make ?costCalculation ?configuration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains the details of an Amazon RDS DB instance."]
module RdsDbInstanceStorage =
  struct
    type nonrec t =
      {
      configuration: RdsDbInstanceStorageConfiguration.t option
        [@ocaml.doc
          "The Amazon RDS DB instance storage configuration used for recommendations."];
      costCalculation: ResourceCostCalculation.t option }
    let make ?configuration =
      fun ?costCalculation -> fun () -> { configuration; costCalculation }
    let to_value x =
      structure_to_value
        [("configuration",
           (Option.map x.configuration
              ~f:RdsDbInstanceStorageConfiguration.to_value));
        ("costCalculation",
          (Option.map x.costCalculation ~f:ResourceCostCalculation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let costCalculation =
        (Option.map ~f:ResourceCostCalculation.of_xml)
          (Xml.child xml_arg0 "costCalculation") in
      let configuration =
        (Option.map ~f:RdsDbInstanceStorageConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      make ?costCalculation ?configuration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let costCalculation =
        field_map json__ "costCalculation" ResourceCostCalculation.of_json in
      let configuration =
        field_map json__ "configuration"
          RdsDbInstanceStorageConfiguration.of_json in
      make ?costCalculation ?configuration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains the details of an Amazon RDS DB instance storage."]
module RdsReservedInstances =
  struct
    type nonrec t =
      {
      configuration: RdsReservedInstancesConfiguration.t option
        [@ocaml.doc
          "The RDS reserved instances configuration used for recommendations."];
      costCalculation: ReservedInstancesCostCalculation.t option
        [@ocaml.doc "Cost impact of the purchase recommendation."]}
    let make ?configuration =
      fun ?costCalculation -> fun () -> { configuration; costCalculation }
    let to_value x =
      structure_to_value
        [("configuration",
           (Option.map x.configuration
              ~f:RdsReservedInstancesConfiguration.to_value));
        ("costCalculation",
          (Option.map x.costCalculation
             ~f:ReservedInstancesCostCalculation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let costCalculation =
        (Option.map ~f:ReservedInstancesCostCalculation.of_xml)
          (Xml.child xml_arg0 "costCalculation") in
      let configuration =
        (Option.map ~f:RdsReservedInstancesConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      make ?costCalculation ?configuration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let costCalculation =
        field_map json__ "costCalculation"
          ReservedInstancesCostCalculation.of_json in
      let configuration =
        field_map json__ "configuration"
          RdsReservedInstancesConfiguration.of_json in
      make ?costCalculation ?configuration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The RDS reserved instances recommendation details."]
module RedshiftReservedInstances =
  struct
    type nonrec t =
      {
      configuration: RedshiftReservedInstancesConfiguration.t option
        [@ocaml.doc
          "The Redshift reserved instances configuration used for recommendations."];
      costCalculation: ReservedInstancesCostCalculation.t option
        [@ocaml.doc "Cost impact of the purchase recommendation."]}
    let make ?configuration =
      fun ?costCalculation -> fun () -> { configuration; costCalculation }
    let to_value x =
      structure_to_value
        [("configuration",
           (Option.map x.configuration
              ~f:RedshiftReservedInstancesConfiguration.to_value));
        ("costCalculation",
          (Option.map x.costCalculation
             ~f:ReservedInstancesCostCalculation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let costCalculation =
        (Option.map ~f:ReservedInstancesCostCalculation.of_xml)
          (Xml.child xml_arg0 "costCalculation") in
      let configuration =
        (Option.map ~f:RedshiftReservedInstancesConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      make ?costCalculation ?configuration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let costCalculation =
        field_map json__ "costCalculation"
          ReservedInstancesCostCalculation.of_json in
      let configuration =
        field_map json__ "configuration"
          RedshiftReservedInstancesConfiguration.of_json in
      make ?costCalculation ?configuration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The Redshift reserved instances recommendation details."]
module SageMakerSavingsPlans =
  struct
    type nonrec t =
      {
      configuration: SageMakerSavingsPlansConfiguration.t option
        [@ocaml.doc
          "The SageMaker Savings Plans configuration used for recommendations."];
      costCalculation: SavingsPlansCostCalculation.t option
        [@ocaml.doc
          "Cost impact of the Savings Plans purchase recommendation."]}
    let make ?configuration =
      fun ?costCalculation -> fun () -> { configuration; costCalculation }
    let to_value x =
      structure_to_value
        [("configuration",
           (Option.map x.configuration
              ~f:SageMakerSavingsPlansConfiguration.to_value));
        ("costCalculation",
          (Option.map x.costCalculation
             ~f:SavingsPlansCostCalculation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let costCalculation =
        (Option.map ~f:SavingsPlansCostCalculation.of_xml)
          (Xml.child xml_arg0 "costCalculation") in
      let configuration =
        (Option.map ~f:SageMakerSavingsPlansConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      make ?costCalculation ?configuration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let costCalculation =
        field_map json__ "costCalculation"
          SavingsPlansCostCalculation.of_json in
      let configuration =
        field_map json__ "configuration"
          SageMakerSavingsPlansConfiguration.of_json in
      make ?costCalculation ?configuration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The SageMaker Savings Plans recommendation details."]
module AccessDeniedException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You are not authorized to use this operation with the given parameters."]
module InternalServerException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An error on the server occurred during the processing of your request. Try again later."]
module MemberAccountDiscountVisibility =
  struct
    type nonrec t =
      | All 
      | None 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | All -> "All" | None -> "None" | Non_static_id s -> s
    let of_string =
      function | "All" -> All | "None" -> None | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration MemberAccountDiscountVisibility"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"MemberAccountDiscountVisibility" j)
    let to_json = simple_to_json to_value
  end
module PreferredCommitment =
  struct
    type nonrec t =
      {
      term: Term.t option
        [@ocaml.doc
          "The preferred length of the commitment period. If the value is null, it will default to ThreeYears (highest savings) where applicable."];
      paymentOption: PaymentOption.t option
        [@ocaml.doc
          "The preferred upfront payment structure for commitments. If the value is null, it will default to AllUpfront (highest savings) where applicable."]}
    let make ?term = fun ?paymentOption -> fun () -> { term; paymentOption }
    let to_value x =
      structure_to_value
        [("term", (Option.map x.term ~f:Term.to_value));
        ("paymentOption",
          (Option.map x.paymentOption ~f:PaymentOption.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let paymentOption =
        (Option.map ~f:PaymentOption.of_xml)
          (Xml.child xml_arg0 "paymentOption") in
      let term = (Option.map ~f:Term.of_xml) (Xml.child xml_arg0 "term") in
      make ?paymentOption ?term ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let paymentOption =
        field_map json__ "paymentOption" PaymentOption.of_json in
      let term = field_map json__ "term" Term.of_json in
      make ?paymentOption ?term ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The preferred configuration for Reserved Instances and Savings Plans commitment-based discounts, consisting of a payment option and a commitment duration."]
module SavingsEstimationMode =
  struct
    type nonrec t =
      | BeforeDiscounts 
      | AfterDiscounts 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | BeforeDiscounts -> "BeforeDiscounts"
      | AfterDiscounts -> "AfterDiscounts"
      | Non_static_id s -> s
    let of_string =
      function
      | "BeforeDiscounts" -> BeforeDiscounts
      | "AfterDiscounts" -> AfterDiscounts
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration SavingsEstimationMode" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"SavingsEstimationMode" j)
    let to_json = simple_to_json to_value
  end
module ThrottlingException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The request was denied due to request throttling."]
module ValidationException =
  struct
    type nonrec t =
      {
      message: String_.t option ;
      reason: ValidationExceptionReason.t option
        [@ocaml.doc "The reason for the validation exception."];
      fields: ValidationExceptionDetails.t option
        [@ocaml.doc "The list of fields that are invalid."]}
    let make ?message =
      fun ?reason -> fun ?fields -> fun () -> { message; reason; fields }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value));
        ("reason",
          (Option.map x.reason ~f:ValidationExceptionReason.to_value));
        ("fields",
          (Option.map x.fields ~f:ValidationExceptionDetails.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fields =
        (Option.map ~f:ValidationExceptionDetails.of_xml)
          (Xml.child xml_arg0 "fields") in
      let reason =
        (Option.map ~f:ValidationExceptionReason.of_xml)
          (Xml.child xml_arg0 "reason") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?fields ?reason ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fields =
        field_map json__ "fields" ValidationExceptionDetails.of_json in
      let reason =
        field_map json__ "reason" ValidationExceptionReason.of_json in
      let message = field_map json__ "message" String_.of_json in
      make ?fields ?reason ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The input fails to satisfy the constraints specified by an Amazon Web Services service."]
module RecommendationList =
  struct
    type nonrec t = Recommendation.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Recommendation.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Recommendation.of_xml)
    let of_json j =
      list_of_json ~kind:"RecommendationList" ~of_json:Recommendation.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module Filter =
  struct
    type nonrec t =
      {
      restartNeeded: Boolean.t option
        [@ocaml.doc
          "Whether or not implementing the recommendation requires a restart."];
      rollbackPossible: Boolean.t option
        [@ocaml.doc
          "Whether or not implementing the recommendation can be rolled back."];
      implementationEfforts: ImplementationEffortList.t option
        [@ocaml.doc "The effort required to implement the recommendation."];
      accountIds: AccountIdList.t option
        [@ocaml.doc "The account to which the recommendation applies."];
      regions: RegionList.t option
        [@ocaml.doc "The Amazon Web Services Region of the resource."];
      resourceTypes: ResourceTypeList.t option
        [@ocaml.doc "The resource type of the recommendation."];
      actionTypes: ActionTypeList.t option
        [@ocaml.doc
          "The type of action you can take by adopting the recommendation."];
      tags: TagList.t option
        [@ocaml.doc "A list of tags assigned to the recommendation."];
      resourceIds: ResourceIdList.t option
        [@ocaml.doc "The resource ID of the recommendation."];
      resourceArns: ResourceArnList.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the recommendation."];
      recommendationIds: RecommendationIdList.t option
        [@ocaml.doc "The IDs for the recommendations."]}
    let make ?restartNeeded =
      fun ?rollbackPossible ->
        fun ?implementationEfforts ->
          fun ?accountIds ->
            fun ?regions ->
              fun ?resourceTypes ->
                fun ?actionTypes ->
                  fun ?tags ->
                    fun ?resourceIds ->
                      fun ?resourceArns ->
                        fun ?recommendationIds ->
                          fun () ->
                            {
                              restartNeeded;
                              rollbackPossible;
                              implementationEfforts;
                              accountIds;
                              regions;
                              resourceTypes;
                              actionTypes;
                              tags;
                              resourceIds;
                              resourceArns;
                              recommendationIds
                            }
    let to_value x =
      structure_to_value
        [("restartNeeded", (Option.map x.restartNeeded ~f:Boolean.to_value));
        ("rollbackPossible",
          (Option.map x.rollbackPossible ~f:Boolean.to_value));
        ("implementationEfforts",
          (Option.map x.implementationEfforts
             ~f:ImplementationEffortList.to_value));
        ("accountIds", (Option.map x.accountIds ~f:AccountIdList.to_value));
        ("regions", (Option.map x.regions ~f:RegionList.to_value));
        ("resourceTypes",
          (Option.map x.resourceTypes ~f:ResourceTypeList.to_value));
        ("actionTypes",
          (Option.map x.actionTypes ~f:ActionTypeList.to_value));
        ("tags", (Option.map x.tags ~f:TagList.to_value));
        ("resourceIds",
          (Option.map x.resourceIds ~f:ResourceIdList.to_value));
        ("resourceArns",
          (Option.map x.resourceArns ~f:ResourceArnList.to_value));
        ("recommendationIds",
          (Option.map x.recommendationIds ~f:RecommendationIdList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recommendationIds =
        (Option.map ~f:RecommendationIdList.of_xml)
          (Xml.child xml_arg0 "recommendationIds") in
      let resourceArns =
        (Option.map ~f:ResourceArnList.of_xml)
          (Xml.child xml_arg0 "resourceArns") in
      let resourceIds =
        (Option.map ~f:ResourceIdList.of_xml)
          (Xml.child xml_arg0 "resourceIds") in
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tags") in
      let actionTypes =
        (Option.map ~f:ActionTypeList.of_xml)
          (Xml.child xml_arg0 "actionTypes") in
      let resourceTypes =
        (Option.map ~f:ResourceTypeList.of_xml)
          (Xml.child xml_arg0 "resourceTypes") in
      let regions =
        (Option.map ~f:RegionList.of_xml) (Xml.child xml_arg0 "regions") in
      let accountIds =
        (Option.map ~f:AccountIdList.of_xml)
          (Xml.child xml_arg0 "accountIds") in
      let implementationEfforts =
        (Option.map ~f:ImplementationEffortList.of_xml)
          (Xml.child xml_arg0 "implementationEfforts") in
      let rollbackPossible =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "rollbackPossible") in
      let restartNeeded =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "restartNeeded") in
      make ?recommendationIds ?resourceArns ?resourceIds ?tags ?actionTypes
        ?resourceTypes ?regions ?accountIds ?implementationEfforts
        ?rollbackPossible ?restartNeeded ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let recommendationIds =
        field_map json__ "recommendationIds" RecommendationIdList.of_json in
      let resourceArns =
        field_map json__ "resourceArns" ResourceArnList.of_json in
      let resourceIds = field_map json__ "resourceIds" ResourceIdList.of_json in
      let tags = field_map json__ "tags" TagList.of_json in
      let actionTypes = field_map json__ "actionTypes" ActionTypeList.of_json in
      let resourceTypes =
        field_map json__ "resourceTypes" ResourceTypeList.of_json in
      let regions = field_map json__ "regions" RegionList.of_json in
      let accountIds = field_map json__ "accountIds" AccountIdList.of_json in
      let implementationEfforts =
        field_map json__ "implementationEfforts"
          ImplementationEffortList.of_json in
      let rollbackPossible =
        field_map json__ "rollbackPossible" Boolean.of_json in
      let restartNeeded = field_map json__ "restartNeeded" Boolean.of_json in
      make ?recommendationIds ?resourceArns ?resourceIds ?tags ?actionTypes
        ?resourceTypes ?regions ?accountIds ?implementationEfforts
        ?rollbackPossible ?restartNeeded ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a filter that returns a more specific list of recommendations. Filters recommendations by different dimensions."]
module ListRecommendationsRequestMaxResultsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1000) >>= (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ListRecommendationsRequestMaxResultsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module OrderBy =
  struct
    type nonrec t =
      {
      dimension: String_.t option [@ocaml.doc "Sorts by dimension values."];
      order: Order.t option
        [@ocaml.doc "The order that's used to sort the data."]}
    let make ?dimension = fun ?order -> fun () -> { dimension; order }
    let to_value x =
      structure_to_value
        [("dimension", (Option.map x.dimension ~f:String_.to_value));
        ("order", (Option.map x.order ~f:Order.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let order = (Option.map ~f:Order.of_xml) (Xml.child xml_arg0 "order") in
      let dimension =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "dimension") in
      make ?order ?dimension ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let order = field_map json__ "order" Order.of_json in
      let dimension = field_map json__ "dimension" String_.of_json in
      make ?order ?dimension ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Defines how rows will be sorted in the response."]
module PrimitiveBoolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module RecommendationSummariesList =
  struct
    type nonrec t = RecommendationSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:RecommendationSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RecommendationSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"RecommendationSummariesList"
        ~of_json:RecommendationSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module SummaryMetricsResult =
  struct
    type nonrec t =
      {
      savingsPercentage: String_.t option
        [@ocaml.doc
          "The savings percentage based on your Amazon Web Services spend over the past 30 days. Savings percentage is only supported when filtering by Region, account ID, or tags."]}
    let make ?savingsPercentage = fun () -> { savingsPercentage }
    let to_value x =
      structure_to_value
        [("savingsPercentage",
           (Option.map x.savingsPercentage ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let savingsPercentage =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "savingsPercentage") in
      make ?savingsPercentage ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let savingsPercentage =
        field_map json__ "savingsPercentage" String_.of_json in
      make ?savingsPercentage ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The results or descriptions for the additional metrics, based on whether the metrics were or were not requested."]
module ListRecommendationSummariesRequestMaxResultsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1000) >>= (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ListRecommendationSummariesRequestMaxResultsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module SummaryMetricsList =
  struct
    type nonrec t = SummaryMetrics.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:SummaryMetrics.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SummaryMetrics.of_xml)
    let of_json j =
      list_of_json ~kind:"SummaryMetricsList" ~of_json:SummaryMetrics.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module AccountEnrollmentStatuses =
  struct
    type nonrec t = AccountEnrollmentStatus.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AccountEnrollmentStatus.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AccountEnrollmentStatus.of_xml)
    let of_json j =
      list_of_json ~kind:"AccountEnrollmentStatuses"
        ~of_json:AccountEnrollmentStatus.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module EfficiencyMetricsByGroupList =
  struct
    type nonrec t = EfficiencyMetricsByGroup.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:EfficiencyMetricsByGroup.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EfficiencyMetricsByGroup.of_xml)
    let of_json j =
      list_of_json ~kind:"EfficiencyMetricsByGroupList"
        ~of_json:EfficiencyMetricsByGroup.of_json j
    let to_json v = composed_to_json to_value v
  end
module GranularityType =
  struct
    type nonrec t =
      | Daily 
      | Monthly 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Daily -> "Daily"
      | Monthly -> "Monthly"
      | Non_static_id s -> s
    let of_string =
      function
      | "Daily" -> Daily
      | "Monthly" -> Monthly
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration GranularityType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"GranularityType" j)
    let to_json = simple_to_json to_value
  end
module ListEfficiencyMetricsRequestMaxResultsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ListEfficiencyMetricsRequestMaxResultsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module TimePeriod =
  struct
    type nonrec t =
      {
      start: String_.t
        [@ocaml.doc
          "The beginning of the time period (inclusive). Specify the date in ISO 8601 format, such as 2024-01-01."];
      end_: String_.t
        [@ocaml.doc
          "The end of the time period (exclusive). Specify the date in ISO 8601 format, such as 2024-12-31."]}
    let context_ = "TimePeriod"
    let make ~start = fun ~end_ -> fun () -> { start; end_ }
    let to_value x =
      structure_to_value
        [("start", (Some (String_.to_value x.start)));
        ("end", (Some (String_.to_value x.end_)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let end_ =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "end") in
      let start =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "start") in
      make ~end_ ~start ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let end_ = field_map_exn json__ "end" String_.of_json in
      let start = field_map_exn json__ "start" String_.of_json in
      make ~end_ ~start ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies a date range for retrieving efficiency metrics. The start date is inclusive and the end date is exclusive."]
module ResourceDetails =
  struct
    type nonrec t =
      {
      lambdaFunction: LambdaFunction.t option
        [@ocaml.doc "The Lambda function recommendation details."];
      ecsService: EcsService.t option
        [@ocaml.doc "The ECS service recommendation details."];
      ec2Instance: Ec2Instance.t option
        [@ocaml.doc "The EC2 instance recommendation details."];
      ebsVolume: EbsVolume.t option
        [@ocaml.doc
          "The Amazon Elastic Block Store volume recommendation details."];
      ec2AutoScalingGroup: Ec2AutoScalingGroup.t option
        [@ocaml.doc "The EC2 Auto Scaling group recommendation details."];
      ec2ReservedInstances: Ec2ReservedInstances.t option
        [@ocaml.doc "The EC2 reserved instances recommendation details."];
      rdsReservedInstances: RdsReservedInstances.t option
        [@ocaml.doc "The RDS reserved instances recommendation details."];
      elastiCacheReservedInstances: ElastiCacheReservedInstances.t option
        [@ocaml.doc
          "The ElastiCache reserved instances recommendation details."];
      openSearchReservedInstances: OpenSearchReservedInstances.t option
        [@ocaml.doc
          "The OpenSearch reserved instances recommendation details."];
      redshiftReservedInstances: RedshiftReservedInstances.t option
        [@ocaml.doc
          "The Redshift reserved instances recommendation details."];
      ec2InstanceSavingsPlans: Ec2InstanceSavingsPlans.t option
        [@ocaml.doc "The EC2 instance Savings Plans recommendation details."];
      computeSavingsPlans: ComputeSavingsPlans.t option
        [@ocaml.doc "The Compute Savings Plans recommendation details."];
      sageMakerSavingsPlans: SageMakerSavingsPlans.t option
        [@ocaml.doc "The SageMaker AI Savings Plans recommendation details."];
      rdsDbInstance: RdsDbInstance.t option
        [@ocaml.doc "The DB instance recommendation details."];
      rdsDbInstanceStorage: RdsDbInstanceStorage.t option
        [@ocaml.doc "The DB instance storage recommendation details."];
      auroraDbClusterStorage: AuroraDbClusterStorage.t option
        [@ocaml.doc "The Aurora DB cluster storage recommendation details."];
      dynamoDbReservedCapacity: DynamoDbReservedCapacity.t option
        [@ocaml.doc "The DynamoDB reserved capacity recommendation details."];
      memoryDbReservedInstances: MemoryDbReservedInstances.t option
        [@ocaml.doc
          "The MemoryDB reserved instances recommendation details."];
      natGateway: NatGateway.t option
        [@ocaml.doc "The NAT Gateway recommendation details."]}
    let make ?lambdaFunction =
      fun ?ecsService ->
        fun ?ec2Instance ->
          fun ?ebsVolume ->
            fun ?ec2AutoScalingGroup ->
              fun ?ec2ReservedInstances ->
                fun ?rdsReservedInstances ->
                  fun ?elastiCacheReservedInstances ->
                    fun ?openSearchReservedInstances ->
                      fun ?redshiftReservedInstances ->
                        fun ?ec2InstanceSavingsPlans ->
                          fun ?computeSavingsPlans ->
                            fun ?sageMakerSavingsPlans ->
                              fun ?rdsDbInstance ->
                                fun ?rdsDbInstanceStorage ->
                                  fun ?auroraDbClusterStorage ->
                                    fun ?dynamoDbReservedCapacity ->
                                      fun ?memoryDbReservedInstances ->
                                        fun ?natGateway ->
                                          fun () ->
                                            {
                                              lambdaFunction;
                                              ecsService;
                                              ec2Instance;
                                              ebsVolume;
                                              ec2AutoScalingGroup;
                                              ec2ReservedInstances;
                                              rdsReservedInstances;
                                              elastiCacheReservedInstances;
                                              openSearchReservedInstances;
                                              redshiftReservedInstances;
                                              ec2InstanceSavingsPlans;
                                              computeSavingsPlans;
                                              sageMakerSavingsPlans;
                                              rdsDbInstance;
                                              rdsDbInstanceStorage;
                                              auroraDbClusterStorage;
                                              dynamoDbReservedCapacity;
                                              memoryDbReservedInstances;
                                              natGateway
                                            }
    let to_value x =
      structure_to_value
        [("lambdaFunction",
           (Option.map x.lambdaFunction ~f:LambdaFunction.to_value));
        ("ecsService", (Option.map x.ecsService ~f:EcsService.to_value));
        ("ec2Instance", (Option.map x.ec2Instance ~f:Ec2Instance.to_value));
        ("ebsVolume", (Option.map x.ebsVolume ~f:EbsVolume.to_value));
        ("ec2AutoScalingGroup",
          (Option.map x.ec2AutoScalingGroup ~f:Ec2AutoScalingGroup.to_value));
        ("ec2ReservedInstances",
          (Option.map x.ec2ReservedInstances ~f:Ec2ReservedInstances.to_value));
        ("rdsReservedInstances",
          (Option.map x.rdsReservedInstances ~f:RdsReservedInstances.to_value));
        ("elastiCacheReservedInstances",
          (Option.map x.elastiCacheReservedInstances
             ~f:ElastiCacheReservedInstances.to_value));
        ("openSearchReservedInstances",
          (Option.map x.openSearchReservedInstances
             ~f:OpenSearchReservedInstances.to_value));
        ("redshiftReservedInstances",
          (Option.map x.redshiftReservedInstances
             ~f:RedshiftReservedInstances.to_value));
        ("ec2InstanceSavingsPlans",
          (Option.map x.ec2InstanceSavingsPlans
             ~f:Ec2InstanceSavingsPlans.to_value));
        ("computeSavingsPlans",
          (Option.map x.computeSavingsPlans ~f:ComputeSavingsPlans.to_value));
        ("sageMakerSavingsPlans",
          (Option.map x.sageMakerSavingsPlans
             ~f:SageMakerSavingsPlans.to_value));
        ("rdsDbInstance",
          (Option.map x.rdsDbInstance ~f:RdsDbInstance.to_value));
        ("rdsDbInstanceStorage",
          (Option.map x.rdsDbInstanceStorage ~f:RdsDbInstanceStorage.to_value));
        ("auroraDbClusterStorage",
          (Option.map x.auroraDbClusterStorage
             ~f:AuroraDbClusterStorage.to_value));
        ("dynamoDbReservedCapacity",
          (Option.map x.dynamoDbReservedCapacity
             ~f:DynamoDbReservedCapacity.to_value));
        ("memoryDbReservedInstances",
          (Option.map x.memoryDbReservedInstances
             ~f:MemoryDbReservedInstances.to_value));
        ("natGateway", (Option.map x.natGateway ~f:NatGateway.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let natGateway =
        (Option.map ~f:NatGateway.of_xml) (Xml.child xml_arg0 "natGateway") in
      let memoryDbReservedInstances =
        (Option.map ~f:MemoryDbReservedInstances.of_xml)
          (Xml.child xml_arg0 "memoryDbReservedInstances") in
      let dynamoDbReservedCapacity =
        (Option.map ~f:DynamoDbReservedCapacity.of_xml)
          (Xml.child xml_arg0 "dynamoDbReservedCapacity") in
      let auroraDbClusterStorage =
        (Option.map ~f:AuroraDbClusterStorage.of_xml)
          (Xml.child xml_arg0 "auroraDbClusterStorage") in
      let rdsDbInstanceStorage =
        (Option.map ~f:RdsDbInstanceStorage.of_xml)
          (Xml.child xml_arg0 "rdsDbInstanceStorage") in
      let rdsDbInstance =
        (Option.map ~f:RdsDbInstance.of_xml)
          (Xml.child xml_arg0 "rdsDbInstance") in
      let sageMakerSavingsPlans =
        (Option.map ~f:SageMakerSavingsPlans.of_xml)
          (Xml.child xml_arg0 "sageMakerSavingsPlans") in
      let computeSavingsPlans =
        (Option.map ~f:ComputeSavingsPlans.of_xml)
          (Xml.child xml_arg0 "computeSavingsPlans") in
      let ec2InstanceSavingsPlans =
        (Option.map ~f:Ec2InstanceSavingsPlans.of_xml)
          (Xml.child xml_arg0 "ec2InstanceSavingsPlans") in
      let redshiftReservedInstances =
        (Option.map ~f:RedshiftReservedInstances.of_xml)
          (Xml.child xml_arg0 "redshiftReservedInstances") in
      let openSearchReservedInstances =
        (Option.map ~f:OpenSearchReservedInstances.of_xml)
          (Xml.child xml_arg0 "openSearchReservedInstances") in
      let elastiCacheReservedInstances =
        (Option.map ~f:ElastiCacheReservedInstances.of_xml)
          (Xml.child xml_arg0 "elastiCacheReservedInstances") in
      let rdsReservedInstances =
        (Option.map ~f:RdsReservedInstances.of_xml)
          (Xml.child xml_arg0 "rdsReservedInstances") in
      let ec2ReservedInstances =
        (Option.map ~f:Ec2ReservedInstances.of_xml)
          (Xml.child xml_arg0 "ec2ReservedInstances") in
      let ec2AutoScalingGroup =
        (Option.map ~f:Ec2AutoScalingGroup.of_xml)
          (Xml.child xml_arg0 "ec2AutoScalingGroup") in
      let ebsVolume =
        (Option.map ~f:EbsVolume.of_xml) (Xml.child xml_arg0 "ebsVolume") in
      let ec2Instance =
        (Option.map ~f:Ec2Instance.of_xml) (Xml.child xml_arg0 "ec2Instance") in
      let ecsService =
        (Option.map ~f:EcsService.of_xml) (Xml.child xml_arg0 "ecsService") in
      let lambdaFunction =
        (Option.map ~f:LambdaFunction.of_xml)
          (Xml.child xml_arg0 "lambdaFunction") in
      make ?natGateway ?memoryDbReservedInstances ?dynamoDbReservedCapacity
        ?auroraDbClusterStorage ?rdsDbInstanceStorage ?rdsDbInstance
        ?sageMakerSavingsPlans ?computeSavingsPlans ?ec2InstanceSavingsPlans
        ?redshiftReservedInstances ?openSearchReservedInstances
        ?elastiCacheReservedInstances ?rdsReservedInstances
        ?ec2ReservedInstances ?ec2AutoScalingGroup ?ebsVolume ?ec2Instance
        ?ecsService ?lambdaFunction ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let natGateway = field_map json__ "natGateway" NatGateway.of_json in
      let memoryDbReservedInstances =
        field_map json__ "memoryDbReservedInstances"
          MemoryDbReservedInstances.of_json in
      let dynamoDbReservedCapacity =
        field_map json__ "dynamoDbReservedCapacity"
          DynamoDbReservedCapacity.of_json in
      let auroraDbClusterStorage =
        field_map json__ "auroraDbClusterStorage"
          AuroraDbClusterStorage.of_json in
      let rdsDbInstanceStorage =
        field_map json__ "rdsDbInstanceStorage" RdsDbInstanceStorage.of_json in
      let rdsDbInstance =
        field_map json__ "rdsDbInstance" RdsDbInstance.of_json in
      let sageMakerSavingsPlans =
        field_map json__ "sageMakerSavingsPlans"
          SageMakerSavingsPlans.of_json in
      let computeSavingsPlans =
        field_map json__ "computeSavingsPlans" ComputeSavingsPlans.of_json in
      let ec2InstanceSavingsPlans =
        field_map json__ "ec2InstanceSavingsPlans"
          Ec2InstanceSavingsPlans.of_json in
      let redshiftReservedInstances =
        field_map json__ "redshiftReservedInstances"
          RedshiftReservedInstances.of_json in
      let openSearchReservedInstances =
        field_map json__ "openSearchReservedInstances"
          OpenSearchReservedInstances.of_json in
      let elastiCacheReservedInstances =
        field_map json__ "elastiCacheReservedInstances"
          ElastiCacheReservedInstances.of_json in
      let rdsReservedInstances =
        field_map json__ "rdsReservedInstances" RdsReservedInstances.of_json in
      let ec2ReservedInstances =
        field_map json__ "ec2ReservedInstances" Ec2ReservedInstances.of_json in
      let ec2AutoScalingGroup =
        field_map json__ "ec2AutoScalingGroup" Ec2AutoScalingGroup.of_json in
      let ebsVolume = field_map json__ "ebsVolume" EbsVolume.of_json in
      let ec2Instance = field_map json__ "ec2Instance" Ec2Instance.of_json in
      let ecsService = field_map json__ "ecsService" EcsService.of_json in
      let lambdaFunction =
        field_map json__ "lambdaFunction" LambdaFunction.of_json in
      make ?natGateway ?memoryDbReservedInstances ?dynamoDbReservedCapacity
        ?auroraDbClusterStorage ?rdsDbInstanceStorage ?rdsDbInstance
        ?sageMakerSavingsPlans ?computeSavingsPlans ?ec2InstanceSavingsPlans
        ?redshiftReservedInstances ?openSearchReservedInstances
        ?elastiCacheReservedInstances ?rdsReservedInstances
        ?ec2ReservedInstances ?ec2AutoScalingGroup ?ebsVolume ?ec2Instance
        ?ecsService ?lambdaFunction ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains detailed information about the specified resource."]
module ResourceNotFoundException =
  struct
    type nonrec t =
      {
      message: String_.t option ;
      resourceId: String_.t option
        [@ocaml.doc "The identifier of the resource that was not found."]}
    let make ?message = fun ?resourceId -> fun () -> { message; resourceId }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value));
        ("resourceId", (Option.map x.resourceId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "resourceId") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?resourceId ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceId = field_map json__ "resourceId" String_.of_json in
      let message = field_map json__ "message" String_.of_json in
      make ?resourceId ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified Amazon Resource Name (ARN) in the request doesn't exist."]
module UpdatePreferencesResponse =
  struct
    type nonrec t =
      {
      savingsEstimationMode: SavingsEstimationMode.t option
        [@ocaml.doc
          "Shows the status of the \"savings estimation mode\" preference."];
      memberAccountDiscountVisibility:
        MemberAccountDiscountVisibility.t option
        [@ocaml.doc
          "Shows the status of the \"member account discount visibility\" preference."];
      preferredCommitment: PreferredCommitment.t option
        [@ocaml.doc
          "Shows the updated preferences for how Reserved Instances and Savings Plans cost-saving opportunities are prioritized in terms of payment option and term length."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?savingsEstimationMode =
      fun ?memberAccountDiscountVisibility ->
        fun ?preferredCommitment ->
          fun () ->
            {
              savingsEstimationMode;
              memberAccountDiscountVisibility;
              preferredCommitment
            }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("savingsEstimationMode",
           (Option.map x.savingsEstimationMode
              ~f:SavingsEstimationMode.to_value));
        ("memberAccountDiscountVisibility",
          (Option.map x.memberAccountDiscountVisibility
             ~f:MemberAccountDiscountVisibility.to_value));
        ("preferredCommitment",
          (Option.map x.preferredCommitment ~f:PreferredCommitment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let preferredCommitment =
        (Option.map ~f:PreferredCommitment.of_xml)
          (Xml.child xml_arg0 "preferredCommitment") in
      let memberAccountDiscountVisibility =
        (Option.map ~f:MemberAccountDiscountVisibility.of_xml)
          (Xml.child xml_arg0 "memberAccountDiscountVisibility") in
      let savingsEstimationMode =
        (Option.map ~f:SavingsEstimationMode.of_xml)
          (Xml.child xml_arg0 "savingsEstimationMode") in
      make ?preferredCommitment ?memberAccountDiscountVisibility
        ?savingsEstimationMode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let preferredCommitment =
        field_map json__ "preferredCommitment" PreferredCommitment.of_json in
      let memberAccountDiscountVisibility =
        field_map json__ "memberAccountDiscountVisibility"
          MemberAccountDiscountVisibility.of_json in
      let savingsEstimationMode =
        field_map json__ "savingsEstimationMode"
          SavingsEstimationMode.of_json in
      make ?preferredCommitment ?memberAccountDiscountVisibility
        ?savingsEstimationMode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates a set of preferences for an account in order to add account-specific preferences into the service. These preferences impact how the savings associated with recommendations are presented."]
module UpdatePreferencesRequest =
  struct
    type nonrec t =
      {
      savingsEstimationMode: SavingsEstimationMode.t option
        [@ocaml.doc "Sets the \"savings estimation mode\" preference."];
      memberAccountDiscountVisibility:
        MemberAccountDiscountVisibility.t option
        [@ocaml.doc
          "Sets the \"member account discount visibility\" preference."];
      preferredCommitment: PreferredCommitment.t option
        [@ocaml.doc
          "Sets the preferences for how Reserved Instances and Savings Plans cost-saving opportunities are prioritized in terms of payment option and term length."]}
    let make ?savingsEstimationMode =
      fun ?memberAccountDiscountVisibility ->
        fun ?preferredCommitment ->
          fun () ->
            {
              savingsEstimationMode;
              memberAccountDiscountVisibility;
              preferredCommitment
            }
    let to_value x =
      structure_to_value
        [("savingsEstimationMode",
           (Option.map x.savingsEstimationMode
              ~f:SavingsEstimationMode.to_value));
        ("memberAccountDiscountVisibility",
          (Option.map x.memberAccountDiscountVisibility
             ~f:MemberAccountDiscountVisibility.to_value));
        ("preferredCommitment",
          (Option.map x.preferredCommitment ~f:PreferredCommitment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let preferredCommitment =
        (Option.map ~f:PreferredCommitment.of_xml)
          (Xml.child xml_arg0 "preferredCommitment") in
      let memberAccountDiscountVisibility =
        (Option.map ~f:MemberAccountDiscountVisibility.of_xml)
          (Xml.child xml_arg0 "memberAccountDiscountVisibility") in
      let savingsEstimationMode =
        (Option.map ~f:SavingsEstimationMode.of_xml)
          (Xml.child xml_arg0 "savingsEstimationMode") in
      make ?preferredCommitment ?memberAccountDiscountVisibility
        ?savingsEstimationMode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let preferredCommitment =
        field_map json__ "preferredCommitment" PreferredCommitment.of_json in
      let memberAccountDiscountVisibility =
        field_map json__ "memberAccountDiscountVisibility"
          MemberAccountDiscountVisibility.of_json in
      let savingsEstimationMode =
        field_map json__ "savingsEstimationMode"
          SavingsEstimationMode.of_json in
      make ?preferredCommitment ?memberAccountDiscountVisibility
        ?savingsEstimationMode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates a set of preferences for an account in order to add account-specific preferences into the service. These preferences impact how the savings associated with recommendations are presented."]
module UpdateEnrollmentStatusResponse =
  struct
    type nonrec t =
      {
      status: String_.t option
        [@ocaml.doc "The enrollment status of the account."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?status = fun () -> { status }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("status", (Option.map x.status ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "status") in
      make ?status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "status" String_.of_json in
      make ?status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the enrollment (opt in and opt out) status of an account to the Cost Optimization Hub service. If the account is a management account of an organization, this action can also be used to enroll member accounts of the organization. You must have the appropriate permissions to opt in to Cost Optimization Hub and to view its recommendations. When you opt in, Cost Optimization Hub automatically creates a service-linked role in your account to access its data."]
module UpdateEnrollmentStatusRequest =
  struct
    type nonrec t =
      {
      status: EnrollmentStatus.t [@ocaml.doc "Sets the account status."];
      includeMemberAccounts: Boolean.t option
        [@ocaml.doc
          "Indicates whether to enroll member accounts of the organization if the account is the management account or delegated administrator."]}
    let context_ = "UpdateEnrollmentStatusRequest"
    let make ?includeMemberAccounts =
      fun ~status -> fun () -> { includeMemberAccounts; status }
    let to_value x =
      structure_to_value
        [("status", (Some (EnrollmentStatus.to_value x.status)));
        ("includeMemberAccounts",
          (Option.map x.includeMemberAccounts ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let includeMemberAccounts =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "includeMemberAccounts") in
      let status =
        EnrollmentStatus.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "status") in
      make ?includeMemberAccounts ~status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let includeMemberAccounts =
        field_map json__ "includeMemberAccounts" Boolean.of_json in
      let status = field_map_exn json__ "status" EnrollmentStatus.of_json in
      make ?includeMemberAccounts ~status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the enrollment (opt in and opt out) status of an account to the Cost Optimization Hub service. If the account is a management account of an organization, this action can also be used to enroll member accounts of the organization. You must have the appropriate permissions to opt in to Cost Optimization Hub and to view its recommendations. When you opt in, Cost Optimization Hub automatically creates a service-linked role in your account to access its data."]
module ListRecommendationsResponse =
  struct
    type nonrec t =
      {
      items: RecommendationList.t option
        [@ocaml.doc "List of all savings recommendations."];
      nextToken: String_.t option
        [@ocaml.doc "The token to retrieve the next set of results."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?items = fun ?nextToken -> fun () -> { items; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("items", (Option.map x.items ~f:RecommendationList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let items =
        (Option.map ~f:RecommendationList.of_xml)
          (Xml.child xml_arg0 "items") in
      make ?nextToken ?items ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let items = field_map json__ "items" RecommendationList.of_json in
      make ?nextToken ?items ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns a list of recommendations."]
module ListRecommendationsRequest =
  struct
    type nonrec t =
      {
      filter: Filter.t option
        [@ocaml.doc
          "The constraints that you want all returned recommendations to match."];
      orderBy: OrderBy.t option
        [@ocaml.doc "The ordering of recommendations by a dimension."];
      includeAllRecommendations: PrimitiveBoolean.t option
        [@ocaml.doc
          "List of all recommendations for a resource, or a single recommendation if de-duped by resourceId."];
      maxResults: ListRecommendationsRequestMaxResultsInteger.t option
        [@ocaml.doc
          "The maximum number of recommendations that are returned for the request."];
      nextToken: String_.t option
        [@ocaml.doc "The token to retrieve the next set of results."]}
    let make ?filter =
      fun ?orderBy ->
        fun ?includeAllRecommendations ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                {
                  filter;
                  orderBy;
                  includeAllRecommendations;
                  maxResults;
                  nextToken
                }
    let to_value x =
      structure_to_value
        [("filter", (Option.map x.filter ~f:Filter.to_value));
        ("orderBy", (Option.map x.orderBy ~f:OrderBy.to_value));
        ("includeAllRecommendations",
          (Option.map x.includeAllRecommendations
             ~f:PrimitiveBoolean.to_value));
        ("maxResults",
          (Option.map x.maxResults
             ~f:ListRecommendationsRequestMaxResultsInteger.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:ListRecommendationsRequestMaxResultsInteger.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let includeAllRecommendations =
        (Option.map ~f:PrimitiveBoolean.of_xml)
          (Xml.child xml_arg0 "includeAllRecommendations") in
      let orderBy =
        (Option.map ~f:OrderBy.of_xml) (Xml.child xml_arg0 "orderBy") in
      let filter =
        (Option.map ~f:Filter.of_xml) (Xml.child xml_arg0 "filter") in
      make ?nextToken ?maxResults ?includeAllRecommendations ?orderBy ?filter
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let maxResults =
        field_map json__ "maxResults"
          ListRecommendationsRequestMaxResultsInteger.of_json in
      let includeAllRecommendations =
        field_map json__ "includeAllRecommendations" PrimitiveBoolean.of_json in
      let orderBy = field_map json__ "orderBy" OrderBy.of_json in
      let filter = field_map json__ "filter" Filter.of_json in
      make ?nextToken ?maxResults ?includeAllRecommendations ?orderBy ?filter
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns a list of recommendations."]
module ListRecommendationSummariesResponse =
  struct
    type nonrec t =
      {
      estimatedTotalDedupedSavings: Double.t option
        [@ocaml.doc "The total overall savings for the aggregated view."];
      items: RecommendationSummariesList.t option
        [@ocaml.doc "A list of all savings recommendations."];
      groupBy: String_.t option
        [@ocaml.doc "The dimension used to group the recommendations by."];
      currencyCode: String_.t option
        [@ocaml.doc "The currency code used for the recommendation."];
      metrics: SummaryMetricsResult.t option
        [@ocaml.doc
          "The results or descriptions for the additional metrics, based on whether the metrics were or were not requested."];
      nextToken: String_.t option
        [@ocaml.doc "The token to retrieve the next set of results."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?estimatedTotalDedupedSavings =
      fun ?items ->
        fun ?groupBy ->
          fun ?currencyCode ->
            fun ?metrics ->
              fun ?nextToken ->
                fun () ->
                  {
                    estimatedTotalDedupedSavings;
                    items;
                    groupBy;
                    currencyCode;
                    metrics;
                    nextToken
                  }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("estimatedTotalDedupedSavings",
           (Option.map x.estimatedTotalDedupedSavings ~f:Double.to_value));
        ("items",
          (Option.map x.items ~f:RecommendationSummariesList.to_value));
        ("groupBy", (Option.map x.groupBy ~f:String_.to_value));
        ("currencyCode", (Option.map x.currencyCode ~f:String_.to_value));
        ("metrics", (Option.map x.metrics ~f:SummaryMetricsResult.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let metrics =
        (Option.map ~f:SummaryMetricsResult.of_xml)
          (Xml.child xml_arg0 "metrics") in
      let currencyCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "currencyCode") in
      let groupBy =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "groupBy") in
      let items =
        (Option.map ~f:RecommendationSummariesList.of_xml)
          (Xml.child xml_arg0 "items") in
      let estimatedTotalDedupedSavings =
        (Option.map ~f:Double.of_xml)
          (Xml.child xml_arg0 "estimatedTotalDedupedSavings") in
      make ?nextToken ?metrics ?currencyCode ?groupBy ?items
        ?estimatedTotalDedupedSavings ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let metrics = field_map json__ "metrics" SummaryMetricsResult.of_json in
      let currencyCode = field_map json__ "currencyCode" String_.of_json in
      let groupBy = field_map json__ "groupBy" String_.of_json in
      let items =
        field_map json__ "items" RecommendationSummariesList.of_json in
      let estimatedTotalDedupedSavings =
        field_map json__ "estimatedTotalDedupedSavings" Double.of_json in
      make ?nextToken ?metrics ?currencyCode ?groupBy ?items
        ?estimatedTotalDedupedSavings ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a concise representation of savings estimates for resources. Also returns de-duped savings across different types of recommendations. The following filters are not supported for this API: recommendationIds, resourceArns, and resourceIds."]
module ListRecommendationSummariesRequest =
  struct
    type nonrec t =
      {
      filter: Filter.t option ;
      groupBy: String_.t
        [@ocaml.doc "The grouping of recommendations by a dimension."];
      maxResults:
        ListRecommendationSummariesRequestMaxResultsInteger.t option
        [@ocaml.doc
          "The maximum number of recommendations to be returned for the request."];
      metrics: SummaryMetricsList.t option
        [@ocaml.doc
          "Additional metrics to be returned for the request. The only valid value is savingsPercentage."];
      nextToken: String_.t option
        [@ocaml.doc "The token to retrieve the next set of results."]}
    let context_ = "ListRecommendationSummariesRequest"
    let make ?filter =
      fun ?maxResults ->
        fun ?metrics ->
          fun ?nextToken ->
            fun ~groupBy ->
              fun () -> { filter; maxResults; metrics; nextToken; groupBy }
    let to_value x =
      structure_to_value
        [("filter", (Option.map x.filter ~f:Filter.to_value));
        ("groupBy", (Some (String_.to_value x.groupBy)));
        ("maxResults",
          (Option.map x.maxResults
             ~f:ListRecommendationSummariesRequestMaxResultsInteger.to_value));
        ("metrics", (Option.map x.metrics ~f:SummaryMetricsList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let metrics =
        (Option.map ~f:SummaryMetricsList.of_xml)
          (Xml.child xml_arg0 "metrics") in
      let maxResults =
        (Option.map
           ~f:ListRecommendationSummariesRequestMaxResultsInteger.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let groupBy =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "groupBy") in
      let filter =
        (Option.map ~f:Filter.of_xml) (Xml.child xml_arg0 "filter") in
      make ?nextToken ?metrics ?maxResults ~groupBy ?filter ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let metrics = field_map json__ "metrics" SummaryMetricsList.of_json in
      let maxResults =
        field_map json__ "maxResults"
          ListRecommendationSummariesRequestMaxResultsInteger.of_json in
      let groupBy = field_map_exn json__ "groupBy" String_.of_json in
      let filter = field_map json__ "filter" Filter.of_json in
      make ?nextToken ?metrics ?maxResults ~groupBy ?filter ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a concise representation of savings estimates for resources. Also returns de-duped savings across different types of recommendations. The following filters are not supported for this API: recommendationIds, resourceArns, and resourceIds."]
module ListEnrollmentStatusesResponse =
  struct
    type nonrec t =
      {
      items: AccountEnrollmentStatuses.t option
        [@ocaml.doc
          "The enrollment status of a specific account ID, including creation and last updated timestamps."];
      includeMemberAccounts: Boolean.t option
        [@ocaml.doc
          "The enrollment status of all member accounts in the organization if the account is the management account or delegated administrator."];
      nextToken: String_.t option
        [@ocaml.doc "The token to retrieve the next set of results."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?items =
      fun ?includeMemberAccounts ->
        fun ?nextToken ->
          fun () -> { items; includeMemberAccounts; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("items",
           (Option.map x.items ~f:AccountEnrollmentStatuses.to_value));
        ("includeMemberAccounts",
          (Option.map x.includeMemberAccounts ~f:Boolean.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let includeMemberAccounts =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "includeMemberAccounts") in
      let items =
        (Option.map ~f:AccountEnrollmentStatuses.of_xml)
          (Xml.child xml_arg0 "items") in
      make ?nextToken ?includeMemberAccounts ?items ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let includeMemberAccounts =
        field_map json__ "includeMemberAccounts" Boolean.of_json in
      let items = field_map json__ "items" AccountEnrollmentStatuses.of_json in
      make ?nextToken ?includeMemberAccounts ?items ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the enrollment status for an account. It can also return the list of accounts that are enrolled under the organization."]
module ListEnrollmentStatusesRequest =
  struct
    type nonrec t =
      {
      includeOrganizationInfo: PrimitiveBoolean.t option
        [@ocaml.doc
          "Indicates whether to return the enrollment status for the organization."];
      accountId: AccountId.t option
        [@ocaml.doc
          "The account ID of a member account in the organization."];
      nextToken: String_.t option
        [@ocaml.doc "The token to retrieve the next set of results."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of objects that are returned for the request."]}
    let make ?includeOrganizationInfo =
      fun ?accountId ->
        fun ?nextToken ->
          fun ?maxResults ->
            fun () ->
              { includeOrganizationInfo; accountId; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("includeOrganizationInfo",
           (Option.map x.includeOrganizationInfo ~f:PrimitiveBoolean.to_value));
        ("accountId", (Option.map x.accountId ~f:AccountId.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let accountId =
        (Option.map ~f:AccountId.of_xml) (Xml.child xml_arg0 "accountId") in
      let includeOrganizationInfo =
        (Option.map ~f:PrimitiveBoolean.of_xml)
          (Xml.child xml_arg0 "includeOrganizationInfo") in
      make ?maxResults ?nextToken ?accountId ?includeOrganizationInfo ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let accountId = field_map json__ "accountId" AccountId.of_json in
      let includeOrganizationInfo =
        field_map json__ "includeOrganizationInfo" PrimitiveBoolean.of_json in
      make ?maxResults ?nextToken ?accountId ?includeOrganizationInfo ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the enrollment status for an account. It can also return the list of accounts that are enrolled under the organization."]
module ListEfficiencyMetricsResponse =
  struct
    type nonrec t =
      {
      efficiencyMetricsByGroup: EfficiencyMetricsByGroupList.t option
        [@ocaml.doc
          "A list of cost efficiency metrics grouped by the specified dimension. Each group contains time-series data points with cost efficiency, potential savings, and optimzable spend for the specified time period."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to retrieve the next page of results. When this value is present in the response, additional groups are available. Pass this token in the nextToken parameter of a subsequent request to retrieve the next page."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?efficiencyMetricsByGroup =
      fun ?nextToken -> fun () -> { efficiencyMetricsByGroup; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("efficiencyMetricsByGroup",
           (Option.map x.efficiencyMetricsByGroup
              ~f:EfficiencyMetricsByGroupList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let efficiencyMetricsByGroup =
        (Option.map ~f:EfficiencyMetricsByGroupList.of_xml)
          (Xml.child xml_arg0 "efficiencyMetricsByGroup") in
      make ?nextToken ?efficiencyMetricsByGroup ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let efficiencyMetricsByGroup =
        field_map json__ "efficiencyMetricsByGroup"
          EfficiencyMetricsByGroupList.of_json in
      make ?nextToken ?efficiencyMetricsByGroup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns cost efficiency metrics aggregated over time and optionally grouped by a specified dimension. The metrics provide insights into your cost optimization progress by tracking estimated savings, spending, and measures how effectively you're optimizing your Cloud resources. The operation supports both daily and monthly time granularities and allows grouping results by account ID, Amazon Web Services Region. Results are returned as time-series data, enabling you to analyze trends in your cost optimization performance over the specified time period."]
module ListEfficiencyMetricsRequest =
  struct
    type nonrec t =
      {
      groupBy: String_.t option
        [@ocaml.doc
          "The dimension by which to group the cost efficiency metrics. Valid values include account ID, Amazon Web Services Region. When no grouping is specified, metrics are aggregated across all resources in the specified time period."];
      granularity: GranularityType.t
        [@ocaml.doc
          "The time granularity for the cost efficiency metrics. Specify Daily for metrics aggregated by day, or Monthly for metrics aggregated by month."];
      timePeriod: TimePeriod.t
        [@ocaml.doc
          "The time period for which to retrieve the cost efficiency metrics. The start date is inclusive and the end date is exclusive. Dates can be specified in either YYYY-MM-DD format or YYYY-MM format depending on the desired granularity."];
      maxResults: ListEfficiencyMetricsRequestMaxResultsInteger.t option
        [@ocaml.doc
          "The maximum number of groups to return in the response. Valid values range from 0 to 1000. Use in conjunction with nextToken to paginate through results when the total number of groups exceeds this limit."];
      orderBy: OrderBy.t option
        [@ocaml.doc
          "The ordering specification for the results. Defines which dimension to sort by and whether to sort in ascending or descending order."];
      nextToken: String_.t option
        [@ocaml.doc
          "The token to retrieve the next page of results. This value is returned in the response when the number of groups exceeds the specified maxResults value."]}
    let context_ = "ListEfficiencyMetricsRequest"
    let make ?groupBy =
      fun ?maxResults ->
        fun ?orderBy ->
          fun ?nextToken ->
            fun ~granularity ->
              fun ~timePeriod ->
                fun () ->
                  {
                    groupBy;
                    maxResults;
                    orderBy;
                    nextToken;
                    granularity;
                    timePeriod
                  }
    let to_value x =
      structure_to_value
        [("groupBy", (Option.map x.groupBy ~f:String_.to_value));
        ("granularity", (Some (GranularityType.to_value x.granularity)));
        ("timePeriod", (Some (TimePeriod.to_value x.timePeriod)));
        ("maxResults",
          (Option.map x.maxResults
             ~f:ListEfficiencyMetricsRequestMaxResultsInteger.to_value));
        ("orderBy", (Option.map x.orderBy ~f:OrderBy.to_value));
        ("nextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "nextToken") in
      let orderBy =
        (Option.map ~f:OrderBy.of_xml) (Xml.child xml_arg0 "orderBy") in
      let maxResults =
        (Option.map ~f:ListEfficiencyMetricsRequestMaxResultsInteger.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let timePeriod =
        TimePeriod.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "timePeriod") in
      let granularity =
        GranularityType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "granularity") in
      let groupBy =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "groupBy") in
      make ?nextToken ?orderBy ?maxResults ~timePeriod ~granularity ?groupBy
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" String_.of_json in
      let orderBy = field_map json__ "orderBy" OrderBy.of_json in
      let maxResults =
        field_map json__ "maxResults"
          ListEfficiencyMetricsRequestMaxResultsInteger.of_json in
      let timePeriod = field_map_exn json__ "timePeriod" TimePeriod.of_json in
      let granularity =
        field_map_exn json__ "granularity" GranularityType.of_json in
      let groupBy = field_map json__ "groupBy" String_.of_json in
      make ?nextToken ?orderBy ?maxResults ~timePeriod ~granularity ?groupBy
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns cost efficiency metrics aggregated over time and optionally grouped by a specified dimension. The metrics provide insights into your cost optimization progress by tracking estimated savings, spending, and measures how effectively you're optimizing your Cloud resources. The operation supports both daily and monthly time granularities and allows grouping results by account ID, Amazon Web Services Region. Results are returned as time-series data, enabling you to analyze trends in your cost optimization performance over the specified time period."]
module GetRecommendationResponse =
  struct
    type nonrec t =
      {
      recommendationId: String_.t option
        [@ocaml.doc "The ID for the recommendation."];
      resourceId: String_.t option
        [@ocaml.doc
          "The unique identifier for the resource. This is the same as the Amazon Resource Name (ARN), if available."];
      resourceArn: String_.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the resource."];
      accountId: String_.t option
        [@ocaml.doc "The account to which the recommendation applies."];
      currencyCode: String_.t option
        [@ocaml.doc "The currency code used for the recommendation."];
      recommendationLookbackPeriodInDays: Integer.t option
        [@ocaml.doc
          "The lookback period that's used to generate the recommendation."];
      costCalculationLookbackPeriodInDays: Integer.t option
        [@ocaml.doc
          "The lookback period used to calculate cost impact for a recommendation."];
      estimatedSavingsPercentage: Double.t option
        [@ocaml.doc
          "The estimated savings percentage relative to the total cost over the cost calculation lookback period."];
      estimatedSavingsOverCostCalculationLookbackPeriod: Double.t option
        [@ocaml.doc
          "The estimated savings amount over the lookback period used to calculate cost impact for a recommendation."];
      currentResourceType: ResourceType.t option
        [@ocaml.doc "The type of resource."];
      recommendedResourceType: ResourceType.t option
        [@ocaml.doc "The resource type of the recommendation."];
      region: String_.t option
        [@ocaml.doc "The Amazon Web Services Region of the resource."];
      source: Source.t option
        [@ocaml.doc "The source of the recommendation."];
      lastRefreshTimestamp: Datetime.t option
        [@ocaml.doc "The time when the recommendation was last generated."];
      estimatedMonthlySavings: Double.t option
        [@ocaml.doc
          "The estimated monthly savings amount for the recommendation."];
      estimatedMonthlyCost: Double.t option
        [@ocaml.doc
          "The estimated monthly cost of the current resource. For Reserved Instances and Savings Plans, it refers to the cost for eligible usage."];
      implementationEffort: ImplementationEffort.t option
        [@ocaml.doc "The effort required to implement the recommendation."];
      restartNeeded: Boolean.t option
        [@ocaml.doc
          "Whether or not implementing the recommendation requires a restart."];
      actionType: ActionType.t option
        [@ocaml.doc
          "The type of action you can take by adopting the recommendation."];
      rollbackPossible: Boolean.t option
        [@ocaml.doc
          "Whether or not implementing the recommendation can be rolled back."];
      currentResourceDetails: ResourceDetails.t option
        [@ocaml.doc "The details for the resource."];
      recommendedResourceDetails: ResourceDetails.t option
        [@ocaml.doc "The details about the recommended resource."];
      tags: TagList.t option
        [@ocaml.doc
          "A list of tags associated with the resource for which the recommendation exists."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?recommendationId =
      fun ?resourceId ->
        fun ?resourceArn ->
          fun ?accountId ->
            fun ?currencyCode ->
              fun ?recommendationLookbackPeriodInDays ->
                fun ?costCalculationLookbackPeriodInDays ->
                  fun ?estimatedSavingsPercentage ->
                    fun ?estimatedSavingsOverCostCalculationLookbackPeriod ->
                      fun ?currentResourceType ->
                        fun ?recommendedResourceType ->
                          fun ?region ->
                            fun ?source ->
                              fun ?lastRefreshTimestamp ->
                                fun ?estimatedMonthlySavings ->
                                  fun ?estimatedMonthlyCost ->
                                    fun ?implementationEffort ->
                                      fun ?restartNeeded ->
                                        fun ?actionType ->
                                          fun ?rollbackPossible ->
                                            fun ?currentResourceDetails ->
                                              fun ?recommendedResourceDetails
                                                ->
                                                fun ?tags ->
                                                  fun () ->
                                                    {
                                                      recommendationId;
                                                      resourceId;
                                                      resourceArn;
                                                      accountId;
                                                      currencyCode;
                                                      recommendationLookbackPeriodInDays;
                                                      costCalculationLookbackPeriodInDays;
                                                      estimatedSavingsPercentage;
                                                      estimatedSavingsOverCostCalculationLookbackPeriod;
                                                      currentResourceType;
                                                      recommendedResourceType;
                                                      region;
                                                      source;
                                                      lastRefreshTimestamp;
                                                      estimatedMonthlySavings;
                                                      estimatedMonthlyCost;
                                                      implementationEffort;
                                                      restartNeeded;
                                                      actionType;
                                                      rollbackPossible;
                                                      currentResourceDetails;
                                                      recommendedResourceDetails;
                                                      tags
                                                    }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("recommendationId",
           (Option.map x.recommendationId ~f:String_.to_value));
        ("resourceId", (Option.map x.resourceId ~f:String_.to_value));
        ("resourceArn", (Option.map x.resourceArn ~f:String_.to_value));
        ("accountId", (Option.map x.accountId ~f:String_.to_value));
        ("currencyCode", (Option.map x.currencyCode ~f:String_.to_value));
        ("recommendationLookbackPeriodInDays",
          (Option.map x.recommendationLookbackPeriodInDays
             ~f:Integer.to_value));
        ("costCalculationLookbackPeriodInDays",
          (Option.map x.costCalculationLookbackPeriodInDays
             ~f:Integer.to_value));
        ("estimatedSavingsPercentage",
          (Option.map x.estimatedSavingsPercentage ~f:Double.to_value));
        ("estimatedSavingsOverCostCalculationLookbackPeriod",
          (Option.map x.estimatedSavingsOverCostCalculationLookbackPeriod
             ~f:Double.to_value));
        ("currentResourceType",
          (Option.map x.currentResourceType ~f:ResourceType.to_value));
        ("recommendedResourceType",
          (Option.map x.recommendedResourceType ~f:ResourceType.to_value));
        ("region", (Option.map x.region ~f:String_.to_value));
        ("source", (Option.map x.source ~f:Source.to_value));
        ("lastRefreshTimestamp",
          (Option.map x.lastRefreshTimestamp ~f:Datetime.to_value));
        ("estimatedMonthlySavings",
          (Option.map x.estimatedMonthlySavings ~f:Double.to_value));
        ("estimatedMonthlyCost",
          (Option.map x.estimatedMonthlyCost ~f:Double.to_value));
        ("implementationEffort",
          (Option.map x.implementationEffort ~f:ImplementationEffort.to_value));
        ("restartNeeded", (Option.map x.restartNeeded ~f:Boolean.to_value));
        ("actionType", (Option.map x.actionType ~f:ActionType.to_value));
        ("rollbackPossible",
          (Option.map x.rollbackPossible ~f:Boolean.to_value));
        ("currentResourceDetails",
          (Option.map x.currentResourceDetails ~f:ResourceDetails.to_value));
        ("recommendedResourceDetails",
          (Option.map x.recommendedResourceDetails
             ~f:ResourceDetails.to_value));
        ("tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "tags") in
      let recommendedResourceDetails =
        (Option.map ~f:ResourceDetails.of_xml)
          (Xml.child xml_arg0 "recommendedResourceDetails") in
      let currentResourceDetails =
        (Option.map ~f:ResourceDetails.of_xml)
          (Xml.child xml_arg0 "currentResourceDetails") in
      let rollbackPossible =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "rollbackPossible") in
      let actionType =
        (Option.map ~f:ActionType.of_xml) (Xml.child xml_arg0 "actionType") in
      let restartNeeded =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "restartNeeded") in
      let implementationEffort =
        (Option.map ~f:ImplementationEffort.of_xml)
          (Xml.child xml_arg0 "implementationEffort") in
      let estimatedMonthlyCost =
        (Option.map ~f:Double.of_xml)
          (Xml.child xml_arg0 "estimatedMonthlyCost") in
      let estimatedMonthlySavings =
        (Option.map ~f:Double.of_xml)
          (Xml.child xml_arg0 "estimatedMonthlySavings") in
      let lastRefreshTimestamp =
        (Option.map ~f:Datetime.of_xml)
          (Xml.child xml_arg0 "lastRefreshTimestamp") in
      let source =
        (Option.map ~f:Source.of_xml) (Xml.child xml_arg0 "source") in
      let region =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "region") in
      let recommendedResourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "recommendedResourceType") in
      let currentResourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "currentResourceType") in
      let estimatedSavingsOverCostCalculationLookbackPeriod =
        (Option.map ~f:Double.of_xml)
          (Xml.child xml_arg0
             "estimatedSavingsOverCostCalculationLookbackPeriod") in
      let estimatedSavingsPercentage =
        (Option.map ~f:Double.of_xml)
          (Xml.child xml_arg0 "estimatedSavingsPercentage") in
      let costCalculationLookbackPeriodInDays =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "costCalculationLookbackPeriodInDays") in
      let recommendationLookbackPeriodInDays =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "recommendationLookbackPeriodInDays") in
      let currencyCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "currencyCode") in
      let accountId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "accountId") in
      let resourceArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "resourceArn") in
      let resourceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "resourceId") in
      let recommendationId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "recommendationId") in
      make ?tags ?recommendedResourceDetails ?currentResourceDetails
        ?rollbackPossible ?actionType ?restartNeeded ?implementationEffort
        ?estimatedMonthlyCost ?estimatedMonthlySavings ?lastRefreshTimestamp
        ?source ?region ?recommendedResourceType ?currentResourceType
        ?estimatedSavingsOverCostCalculationLookbackPeriod
        ?estimatedSavingsPercentage ?costCalculationLookbackPeriodInDays
        ?recommendationLookbackPeriodInDays ?currencyCode ?accountId
        ?resourceArn ?resourceId ?recommendationId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "tags" TagList.of_json in
      let recommendedResourceDetails =
        field_map json__ "recommendedResourceDetails" ResourceDetails.of_json in
      let currentResourceDetails =
        field_map json__ "currentResourceDetails" ResourceDetails.of_json in
      let rollbackPossible =
        field_map json__ "rollbackPossible" Boolean.of_json in
      let actionType = field_map json__ "actionType" ActionType.of_json in
      let restartNeeded = field_map json__ "restartNeeded" Boolean.of_json in
      let implementationEffort =
        field_map json__ "implementationEffort" ImplementationEffort.of_json in
      let estimatedMonthlyCost =
        field_map json__ "estimatedMonthlyCost" Double.of_json in
      let estimatedMonthlySavings =
        field_map json__ "estimatedMonthlySavings" Double.of_json in
      let lastRefreshTimestamp =
        field_map json__ "lastRefreshTimestamp" Datetime.of_json in
      let source = field_map json__ "source" Source.of_json in
      let region = field_map json__ "region" String_.of_json in
      let recommendedResourceType =
        field_map json__ "recommendedResourceType" ResourceType.of_json in
      let currentResourceType =
        field_map json__ "currentResourceType" ResourceType.of_json in
      let estimatedSavingsOverCostCalculationLookbackPeriod =
        field_map json__ "estimatedSavingsOverCostCalculationLookbackPeriod"
          Double.of_json in
      let estimatedSavingsPercentage =
        field_map json__ "estimatedSavingsPercentage" Double.of_json in
      let costCalculationLookbackPeriodInDays =
        field_map json__ "costCalculationLookbackPeriodInDays"
          Integer.of_json in
      let recommendationLookbackPeriodInDays =
        field_map json__ "recommendationLookbackPeriodInDays" Integer.of_json in
      let currencyCode = field_map json__ "currencyCode" String_.of_json in
      let accountId = field_map json__ "accountId" String_.of_json in
      let resourceArn = field_map json__ "resourceArn" String_.of_json in
      let resourceId = field_map json__ "resourceId" String_.of_json in
      let recommendationId =
        field_map json__ "recommendationId" String_.of_json in
      make ?tags ?recommendedResourceDetails ?currentResourceDetails
        ?rollbackPossible ?actionType ?restartNeeded ?implementationEffort
        ?estimatedMonthlyCost ?estimatedMonthlySavings ?lastRefreshTimestamp
        ?source ?region ?recommendedResourceType ?currentResourceType
        ?estimatedSavingsOverCostCalculationLookbackPeriod
        ?estimatedSavingsPercentage ?costCalculationLookbackPeriodInDays
        ?recommendationLookbackPeriodInDays ?currencyCode ?accountId
        ?resourceArn ?resourceId ?recommendationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns both the current and recommended resource configuration and the estimated cost impact for a recommendation. The recommendationId is only valid for up to a maximum of 24 hours as recommendations are refreshed daily. To retrieve the recommendationId, use the ListRecommendations API."]
module GetRecommendationRequest =
  struct
    type nonrec t =
      {
      recommendationId: String_.t
        [@ocaml.doc "The ID for the recommendation."]}
    let context_ = "GetRecommendationRequest"
    let make ~recommendationId = fun () -> { recommendationId }
    let to_value x =
      structure_to_value
        [("recommendationId", (Some (String_.to_value x.recommendationId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let recommendationId =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "recommendationId") in
      make ~recommendationId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let recommendationId =
        field_map_exn json__ "recommendationId" String_.of_json in
      make ~recommendationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns both the current and recommended resource configuration and the estimated cost impact for a recommendation. The recommendationId is only valid for up to a maximum of 24 hours as recommendations are refreshed daily. To retrieve the recommendationId, use the ListRecommendations API."]
module GetPreferencesResponse =
  struct
    type nonrec t =
      {
      savingsEstimationMode: SavingsEstimationMode.t option
        [@ocaml.doc
          "Retrieves the status of the \"savings estimation mode\" preference."];
      memberAccountDiscountVisibility:
        MemberAccountDiscountVisibility.t option
        [@ocaml.doc
          "Retrieves the status of the \"member account discount visibility\" preference."];
      preferredCommitment: PreferredCommitment.t option
        [@ocaml.doc
          "Retrieves the current preferences for how Reserved Instances and Savings Plans cost-saving opportunities are prioritized in terms of payment option and term length."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?savingsEstimationMode =
      fun ?memberAccountDiscountVisibility ->
        fun ?preferredCommitment ->
          fun () ->
            {
              savingsEstimationMode;
              memberAccountDiscountVisibility;
              preferredCommitment
            }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("savingsEstimationMode",
           (Option.map x.savingsEstimationMode
              ~f:SavingsEstimationMode.to_value));
        ("memberAccountDiscountVisibility",
          (Option.map x.memberAccountDiscountVisibility
             ~f:MemberAccountDiscountVisibility.to_value));
        ("preferredCommitment",
          (Option.map x.preferredCommitment ~f:PreferredCommitment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let preferredCommitment =
        (Option.map ~f:PreferredCommitment.of_xml)
          (Xml.child xml_arg0 "preferredCommitment") in
      let memberAccountDiscountVisibility =
        (Option.map ~f:MemberAccountDiscountVisibility.of_xml)
          (Xml.child xml_arg0 "memberAccountDiscountVisibility") in
      let savingsEstimationMode =
        (Option.map ~f:SavingsEstimationMode.of_xml)
          (Xml.child xml_arg0 "savingsEstimationMode") in
      make ?preferredCommitment ?memberAccountDiscountVisibility
        ?savingsEstimationMode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let preferredCommitment =
        field_map json__ "preferredCommitment" PreferredCommitment.of_json in
      let memberAccountDiscountVisibility =
        field_map json__ "memberAccountDiscountVisibility"
          MemberAccountDiscountVisibility.of_json in
      let savingsEstimationMode =
        field_map json__ "savingsEstimationMode"
          SavingsEstimationMode.of_json in
      make ?preferredCommitment ?memberAccountDiscountVisibility
        ?savingsEstimationMode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a set of preferences for an account in order to add account-specific preferences into the service. These preferences impact how the savings associated with recommendations are presented\226\128\148estimated savings after discounts or estimated savings before discounts, for example."]
module GetPreferencesRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a set of preferences for an account in order to add account-specific preferences into the service. These preferences impact how the savings associated with recommendations are presented\226\128\148estimated savings after discounts or estimated savings before discounts, for example."]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | GetPreferences: (GetPreferencesRequest.t, GetPreferencesResponse.t,
  GetPreferencesResponse.error) t 
  | GetRecommendation: (GetRecommendationRequest.t,
  GetRecommendationResponse.t, GetRecommendationResponse.error) t 
  | ListEfficiencyMetrics: (ListEfficiencyMetricsRequest.t,
  ListEfficiencyMetricsResponse.t, ListEfficiencyMetricsResponse.error) t 
  | ListEnrollmentStatuses: (ListEnrollmentStatusesRequest.t,
  ListEnrollmentStatusesResponse.t, ListEnrollmentStatusesResponse.error) t 
  | ListRecommendationSummaries: (ListRecommendationSummariesRequest.t,
  ListRecommendationSummariesResponse.t,
  ListRecommendationSummariesResponse.error) t 
  | ListRecommendations: (ListRecommendationsRequest.t,
  ListRecommendationsResponse.t, ListRecommendationsResponse.error) t 
  | UpdateEnrollmentStatus: (UpdateEnrollmentStatusRequest.t,
  UpdateEnrollmentStatusResponse.t, UpdateEnrollmentStatusResponse.error) t 
  | UpdatePreferences: (UpdatePreferencesRequest.t,
  UpdatePreferencesResponse.t, UpdatePreferencesResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | GetPreferences -> `POST
  | GetRecommendation -> `POST
  | ListEfficiencyMetrics -> `POST
  | ListEnrollmentStatuses -> `POST
  | ListRecommendationSummaries -> `POST
  | ListRecommendations -> `POST
  | UpdateEnrollmentStatus -> `POST
  | UpdatePreferences -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | GetPreferences -> (Format.kasprintf Uri.of_string) "/"
      | GetRecommendation -> (Format.kasprintf Uri.of_string) "/"
      | ListEfficiencyMetrics -> (Format.kasprintf Uri.of_string) "/"
      | ListEnrollmentStatuses -> (Format.kasprintf Uri.of_string) "/"
      | ListRecommendationSummaries -> (Format.kasprintf Uri.of_string) "/"
      | ListRecommendations -> (Format.kasprintf Uri.of_string) "/"
      | UpdateEnrollmentStatus -> (Format.kasprintf Uri.of_string) "/"
      | UpdatePreferences -> (Format.kasprintf Uri.of_string) "/")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  match endp with
  | GetPreferences ->
      let json = GetPreferencesRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "CostOptimizationHubService.GetPreferences")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | GetRecommendation ->
      let json = GetRecommendationRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "CostOptimizationHubService.GetRecommendation")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListEfficiencyMetrics ->
      let json = ListEfficiencyMetricsRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target",
            "CostOptimizationHubService.ListEfficiencyMetrics")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListEnrollmentStatuses ->
      let json = ListEnrollmentStatusesRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target",
            "CostOptimizationHubService.ListEnrollmentStatuses")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListRecommendationSummaries ->
      let json = ListRecommendationSummariesRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target",
            "CostOptimizationHubService.ListRecommendationSummaries")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListRecommendations ->
      let json = ListRecommendationsRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "CostOptimizationHubService.ListRecommendations")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | UpdateEnrollmentStatus ->
      let json = UpdateEnrollmentStatusRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target",
            "CostOptimizationHubService.UpdateEnrollmentStatus")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | UpdatePreferences ->
      let json = UpdatePreferencesRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "CostOptimizationHubService.UpdatePreferences")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type = None }) in
    match (error_of_json, ((code >= 400) && (code <= 599))) with
    | (Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        (match json |> (Yojson.Safe.Util.member "__type") with
         | `String error_type -> error_of_json error_type json
         | `Null -> bail ()
         | _ ->
             failwith
               (sprintf "Error '__type' did not have string type: %s" body))
    | (None, _) | (_, false) -> bail () in
  let _ = parse_aws_error in
  let _ = resp in
  match endpoint with
  | GetPreferences ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (GetPreferencesResponse.of_json json)
      else
        Error (parse_aws_error (Some GetPreferencesResponse.error_of_json))
  | GetRecommendation ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (GetRecommendationResponse.of_json json)
      else
        Error
          (parse_aws_error (Some GetRecommendationResponse.error_of_json))
  | ListEfficiencyMetrics ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ListEfficiencyMetricsResponse.of_json json)
      else
        Error
          (parse_aws_error (Some ListEfficiencyMetricsResponse.error_of_json))
  | ListEnrollmentStatuses ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ListEnrollmentStatusesResponse.of_json json)
      else
        Error
          (parse_aws_error
             (Some ListEnrollmentStatusesResponse.error_of_json))
  | ListRecommendationSummaries ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ListRecommendationSummariesResponse.of_json json)
      else
        Error
          (parse_aws_error
             (Some ListRecommendationSummariesResponse.error_of_json))
  | ListRecommendations ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ListRecommendationsResponse.of_json json)
      else
        Error
          (parse_aws_error (Some ListRecommendationsResponse.error_of_json))
  | UpdateEnrollmentStatus ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (UpdateEnrollmentStatusResponse.of_json json)
      else
        Error
          (parse_aws_error
             (Some UpdateEnrollmentStatusResponse.error_of_json))
  | UpdatePreferences ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (UpdatePreferencesResponse.of_json json)
      else
        Error
          (parse_aws_error (Some UpdatePreferencesResponse.error_of_json))
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_rescore_execution_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and capacityUnits =
         flag "capacity-units" (optional json_arg)
           ~doc:"JSON CapacityUnitsConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientTokenName"
       and name =
         flag "name" (required string) ~doc:"STRING RescoreExecutionPlanName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_rescore_execution_plan
           (Values.CreateRescoreExecutionPlanRequest.make ?description
              ?capacityUnits:(Option.map
                                ~f:Values.CapacityUnitsConfiguration.of_json
                                capacityUnits)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?clientToken
              ~name ())
           (Some Values.CreateRescoreExecutionPlanResponse.to_json)
           (Some Values.CreateRescoreExecutionPlanResponse.error_to_json)])
let delete_rescore_execution_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id =
         flag "id" (required string) ~doc:"STRING RescoreExecutionPlanId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_rescore_execution_plan
           (Values.DeleteRescoreExecutionPlanRequest.make ~id ()) None None])
let describe_rescore_execution_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id =
         flag "id" (required string) ~doc:"STRING RescoreExecutionPlanId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_rescore_execution_plan
           (Values.DescribeRescoreExecutionPlanRequest.make ~id ())
           (Some Values.DescribeRescoreExecutionPlanResponse.to_json)
           (Some Values.DescribeRescoreExecutionPlanResponse.error_to_json)])
let list_rescore_execution_plans =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListRescoreExecutionPlansRequest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_rescore_execution_plans
           (Values.ListRescoreExecutionPlansRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListRescoreExecutionPlansResponse.to_json)
           (Some Values.ListRescoreExecutionPlansResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let rescore =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and rescoreExecutionPlanId =
         flag "rescore-execution-plan-id" (required string)
           ~doc:"STRING RescoreExecutionPlanId"
       and searchQuery =
         flag "search-query" (required string) ~doc:"STRING SearchQuery"
       and documents =
         flag "documents" (required json_arg) ~doc:"JSON DocumentList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.rescore
           (Values.RescoreRequest.make ~rescoreExecutionPlanId ~searchQuery
              ~documents:(Values.DocumentList.of_json documents) ())
           (Some Values.RescoreResult.to_json)
           (Some Values.RescoreResult.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_rescore_execution_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING RescoreExecutionPlanName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and capacityUnits =
         flag "capacity-units" (optional json_arg)
           ~doc:"JSON CapacityUnitsConfiguration"
       and id =
         flag "id" (required string) ~doc:"STRING RescoreExecutionPlanId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_rescore_execution_plan
           (Values.UpdateRescoreExecutionPlanRequest.make ?name ?description
              ?capacityUnits:(Option.map
                                ~f:Values.CapacityUnitsConfiguration.of_json
                                capacityUnits) ~id ()) None None])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-rescore-execution-plan", create_rescore_execution_plan);
    ("delete-rescore-execution-plan", delete_rescore_execution_plan);
    ("describe-rescore-execution-plan", describe_rescore_execution_plan);
    ("list-rescore-execution-plans", list_rescore_execution_plans);
    ("list-tags-for-resource", list_tags_for_resource);
    ("rescore", rescore);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-rescore-execution-plan", update_rescore_execution_plan)]
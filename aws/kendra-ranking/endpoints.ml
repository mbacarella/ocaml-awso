(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | CreateRescoreExecutionPlan: (CreateRescoreExecutionPlanRequest.t,
  CreateRescoreExecutionPlanResponse.t,
  CreateRescoreExecutionPlanResponse.error) t 
  | DeleteRescoreExecutionPlan: (DeleteRescoreExecutionPlanRequest.t, 
  unit, unit) t 
  | DescribeRescoreExecutionPlan: (DescribeRescoreExecutionPlanRequest.t,
  DescribeRescoreExecutionPlanResponse.t,
  DescribeRescoreExecutionPlanResponse.error) t 
  | ListRescoreExecutionPlans: (ListRescoreExecutionPlansRequest.t,
  ListRescoreExecutionPlansResponse.t,
  ListRescoreExecutionPlansResponse.error) t 
  | ListTagsForResource: (ListTagsForResourceRequest.t,
  ListTagsForResourceResponse.t, ListTagsForResourceResponse.error) t 
  | Rescore: (RescoreRequest.t, RescoreResult.t, RescoreResult.error) t 
  | TagResource: (TagResourceRequest.t, TagResourceResponse.t,
  TagResourceResponse.error) t 
  | UntagResource: (UntagResourceRequest.t, UntagResourceResponse.t,
  UntagResourceResponse.error) t 
  | UpdateRescoreExecutionPlan: (UpdateRescoreExecutionPlanRequest.t, 
  unit, unit) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | CreateRescoreExecutionPlan -> `POST
  | DeleteRescoreExecutionPlan -> `POST
  | DescribeRescoreExecutionPlan -> `POST
  | ListRescoreExecutionPlans -> `POST
  | ListTagsForResource -> `POST
  | Rescore -> `POST
  | TagResource -> `POST
  | UntagResource -> `POST
  | UpdateRescoreExecutionPlan -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | CreateRescoreExecutionPlan -> (Format.kasprintf Uri.of_string) "/"
      | DeleteRescoreExecutionPlan -> (Format.kasprintf Uri.of_string) "/"
      | DescribeRescoreExecutionPlan -> (Format.kasprintf Uri.of_string) "/"
      | ListRescoreExecutionPlans -> (Format.kasprintf Uri.of_string) "/"
      | ListTagsForResource -> (Format.kasprintf Uri.of_string) "/"
      | Rescore -> (Format.kasprintf Uri.of_string) "/"
      | TagResource -> (Format.kasprintf Uri.of_string) "/"
      | UntagResource -> (Format.kasprintf Uri.of_string) "/"
      | UpdateRescoreExecutionPlan -> (Format.kasprintf Uri.of_string) "/")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  match endp with
  | CreateRescoreExecutionPlan ->
      let json = CreateRescoreExecutionPlanRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target",
            "AWSKendraRerankingFrontendService.CreateRescoreExecutionPlan")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | DeleteRescoreExecutionPlan ->
      let json = DeleteRescoreExecutionPlanRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target",
            "AWSKendraRerankingFrontendService.DeleteRescoreExecutionPlan")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | DescribeRescoreExecutionPlan ->
      let json = DescribeRescoreExecutionPlanRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target",
            "AWSKendraRerankingFrontendService.DescribeRescoreExecutionPlan")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListRescoreExecutionPlans ->
      let json = ListRescoreExecutionPlansRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target",
            "AWSKendraRerankingFrontendService.ListRescoreExecutionPlans")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListTagsForResource ->
      let json = ListTagsForResourceRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target",
            "AWSKendraRerankingFrontendService.ListTagsForResource")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | Rescore ->
      let json = RescoreRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "AWSKendraRerankingFrontendService.Rescore")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | TagResource ->
      let json = TagResourceRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "AWSKendraRerankingFrontendService.TagResource")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | UntagResource ->
      let json = UntagResourceRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "AWSKendraRerankingFrontendService.UntagResource")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | UpdateRescoreExecutionPlan ->
      let json = UpdateRescoreExecutionPlanRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target",
            "AWSKendraRerankingFrontendService.UpdateRescoreExecutionPlan")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type = None }) in
    match (error_of_json, ((code >= 400) && (code <= 599))) with
    | (Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        (match json |> (Yojson.Safe.Util.member "__type") with
         | `String error_type -> error_of_json error_type json
         | `Null -> bail ()
         | _ ->
             failwith
               (sprintf "Error '__type' did not have string type: %s" body))
    | (None, _) | (_, false) -> bail () in
  let _ = parse_aws_error in
  let _ = resp in
  match endpoint with
  | CreateRescoreExecutionPlan ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (CreateRescoreExecutionPlanResponse.of_json json)
      else
        Error
          (parse_aws_error
             (Some CreateRescoreExecutionPlanResponse.error_of_json))
  | DeleteRescoreExecutionPlan ->
      if is_success then Ok () else Error (parse_aws_error None)
  | DescribeRescoreExecutionPlan ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (DescribeRescoreExecutionPlanResponse.of_json json)
      else
        Error
          (parse_aws_error
             (Some DescribeRescoreExecutionPlanResponse.error_of_json))
  | ListRescoreExecutionPlans ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ListRescoreExecutionPlansResponse.of_json json)
      else
        Error
          (parse_aws_error
             (Some ListRescoreExecutionPlansResponse.error_of_json))
  | ListTagsForResource ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ListTagsForResourceResponse.of_json json)
      else
        Error
          (parse_aws_error (Some ListTagsForResourceResponse.error_of_json))
  | Rescore ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (RescoreResult.of_json json)
      else Error (parse_aws_error (Some RescoreResult.error_of_json))
  | TagResource ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (TagResourceResponse.of_json json)
      else Error (parse_aws_error (Some TagResourceResponse.error_of_json))
  | UntagResource ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (UntagResourceResponse.of_json json)
      else Error (parse_aws_error (Some UntagResourceResponse.error_of_json))
  | UpdateRescoreExecutionPlan ->
      if is_success then Ok () else Error (parse_aws_error None)
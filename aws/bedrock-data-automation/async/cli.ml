(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let copy_blueprint_stage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and blueprintArn =
         flag "blueprint-arn" (required string) ~doc:"STRING BlueprintArn"
       and sourceStage =
         flag "source-stage" (required json_arg) ~doc:"JSON BlueprintStage"
       and targetStage =
         flag "target-stage" (required json_arg) ~doc:"JSON BlueprintStage" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.copy_blueprint_stage
           (Values.CopyBlueprintStageRequest.make ?clientToken ~blueprintArn
              ~sourceStage:(Values.BlueprintStage.of_json sourceStage)
              ~targetStage:(Values.BlueprintStage.of_json targetStage) ())
           (Some Values.CopyBlueprintStageResponse.to_json)
           (Some Values.CopyBlueprintStageResponse.error_to_json)])
let create_blueprint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and blueprintStage =
         flag "blueprint-stage" (optional json_arg)
           ~doc:"JSON BlueprintStage"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and encryptionConfiguration =
         flag "encryption-configuration" (optional json_arg)
           ~doc:"JSON EncryptionConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and blueprintName =
         flag "blueprint-name" (required string) ~doc:"STRING BlueprintName"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON Type"
       and schema =
         flag "schema" (required string) ~doc:"STRING BlueprintSchema" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_blueprint
           (Values.CreateBlueprintRequest.make
              ?blueprintStage:(Option.map ~f:Values.BlueprintStage.of_json
                                 blueprintStage) ?clientToken
              ?encryptionConfiguration:(Option.map
                                          ~f:Values.EncryptionConfiguration.of_json
                                          encryptionConfiguration)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~blueprintName ~type_:(Values.Type.of_json type_) ~schema ())
           (Some Values.CreateBlueprintResponse.to_json)
           (Some Values.CreateBlueprintResponse.error_to_json)])
let create_blueprint_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and blueprintArn =
         flag "blueprint-arn" (required string) ~doc:"STRING BlueprintArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_blueprint_version
           (Values.CreateBlueprintVersionRequest.make ?clientToken
              ~blueprintArn ())
           (Some Values.CreateBlueprintVersionResponse.to_json)
           (Some Values.CreateBlueprintVersionResponse.error_to_json)])
let create_data_automation_library =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and libraryDescription =
         flag "library-description" (optional string)
           ~doc:"STRING DataAutomationLibraryDescription"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and encryptionConfiguration =
         flag "encryption-configuration" (optional json_arg)
           ~doc:"JSON EncryptionConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and libraryName =
         flag "library-name" (required string)
           ~doc:"STRING DataAutomationLibraryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_automation_library
           (Values.CreateDataAutomationLibraryRequest.make
              ?libraryDescription ?clientToken
              ?encryptionConfiguration:(Option.map
                                          ~f:Values.EncryptionConfiguration.of_json
                                          encryptionConfiguration)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~libraryName
              ()) (Some Values.CreateDataAutomationLibraryResponse.to_json)
           (Some Values.CreateDataAutomationLibraryResponse.error_to_json)])
let create_data_automation_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectDescription =
         flag "project-description" (optional string)
           ~doc:"STRING DataAutomationProjectDescription"
       and projectStage =
         flag "project-stage" (optional json_arg)
           ~doc:"JSON DataAutomationProjectStage"
       and projectType =
         flag "project-type" (optional json_arg)
           ~doc:"JSON DataAutomationProjectType"
       and customOutputConfiguration =
         flag "custom-output-configuration" (optional json_arg)
           ~doc:"JSON CustomOutputConfiguration"
       and overrideConfiguration =
         flag "override-configuration" (optional json_arg)
           ~doc:"JSON OverrideConfiguration"
       and dataAutomationLibraryConfiguration =
         flag "data-automation-library-configuration" (optional json_arg)
           ~doc:"JSON DataAutomationLibraryConfiguration"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and encryptionConfiguration =
         flag "encryption-configuration" (optional json_arg)
           ~doc:"JSON EncryptionConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and projectName =
         flag "project-name" (required string)
           ~doc:"STRING DataAutomationProjectName"
       and standardOutputConfiguration =
         flag "standard-output-configuration" (required json_arg)
           ~doc:"JSON StandardOutputConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_automation_project
           (Values.CreateDataAutomationProjectRequest.make
              ?projectDescription
              ?projectStage:(Option.map
                               ~f:Values.DataAutomationProjectStage.of_json
                               projectStage)
              ?projectType:(Option.map
                              ~f:Values.DataAutomationProjectType.of_json
                              projectType)
              ?customOutputConfiguration:(Option.map
                                            ~f:Values.CustomOutputConfiguration.of_json
                                            customOutputConfiguration)
              ?overrideConfiguration:(Option.map
                                        ~f:Values.OverrideConfiguration.of_json
                                        overrideConfiguration)
              ?dataAutomationLibraryConfiguration:(Option.map
                                                     ~f:Values.DataAutomationLibraryConfiguration.of_json
                                                     dataAutomationLibraryConfiguration)
              ?clientToken
              ?encryptionConfiguration:(Option.map
                                          ~f:Values.EncryptionConfiguration.of_json
                                          encryptionConfiguration)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~projectName
              ~standardOutputConfiguration:(Values.StandardOutputConfiguration.of_json
                                              standardOutputConfiguration) ())
           (Some Values.CreateDataAutomationProjectResponse.to_json)
           (Some Values.CreateDataAutomationProjectResponse.error_to_json)])
let delete_blueprint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and blueprintVersion =
         flag "blueprint-version" (optional string)
           ~doc:"STRING BlueprintVersion"
       and blueprintArn =
         flag "blueprint-arn" (required string) ~doc:"STRING BlueprintArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_blueprint
           (Values.DeleteBlueprintRequest.make ?blueprintVersion
              ~blueprintArn ()) (Some Values.DeleteBlueprintResponse.to_json)
           (Some Values.DeleteBlueprintResponse.error_to_json)])
let delete_data_automation_library =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and libraryArn =
         flag "library-arn" (required string)
           ~doc:"STRING DataAutomationLibraryArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_data_automation_library
           (Values.DeleteDataAutomationLibraryRequest.make ~libraryArn ())
           (Some Values.DeleteDataAutomationLibraryResponse.to_json)
           (Some Values.DeleteDataAutomationLibraryResponse.error_to_json)])
let delete_data_automation_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectArn =
         flag "project-arn" (required string)
           ~doc:"STRING DataAutomationProjectArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_data_automation_project
           (Values.DeleteDataAutomationProjectRequest.make ~projectArn ())
           (Some Values.DeleteDataAutomationProjectResponse.to_json)
           (Some Values.DeleteDataAutomationProjectResponse.error_to_json)])
let get_blueprint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and blueprintVersion =
         flag "blueprint-version" (optional string)
           ~doc:"STRING BlueprintVersion"
       and blueprintStage =
         flag "blueprint-stage" (optional json_arg)
           ~doc:"JSON BlueprintStage"
       and blueprintArn =
         flag "blueprint-arn" (required string) ~doc:"STRING BlueprintArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_blueprint
           (Values.GetBlueprintRequest.make ?blueprintVersion
              ?blueprintStage:(Option.map ~f:Values.BlueprintStage.of_json
                                 blueprintStage) ~blueprintArn ())
           (Some Values.GetBlueprintResponse.to_json)
           (Some Values.GetBlueprintResponse.error_to_json)])
let get_blueprint_optimization_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and invocationArn =
         flag "invocation-arn" (required string)
           ~doc:"STRING BlueprintOptimizationInvocationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_blueprint_optimization_status
           (Values.GetBlueprintOptimizationStatusRequest.make ~invocationArn
              ())
           (Some Values.GetBlueprintOptimizationStatusResponse.to_json)
           (Some Values.GetBlueprintOptimizationStatusResponse.error_to_json)])
let get_data_automation_library =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and libraryArn =
         flag "library-arn" (required string)
           ~doc:"STRING DataAutomationLibraryArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_automation_library
           (Values.GetDataAutomationLibraryRequest.make ~libraryArn ())
           (Some Values.GetDataAutomationLibraryResponse.to_json)
           (Some Values.GetDataAutomationLibraryResponse.error_to_json)])
let get_data_automation_library_entity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and libraryArn =
         flag "library-arn" (required string)
           ~doc:"STRING DataAutomationLibraryArn"
       and entityType =
         flag "entity-type" (required json_arg) ~doc:"JSON EntityType"
       and entityId =
         flag "entity-id" (required string) ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_automation_library_entity
           (Values.GetDataAutomationLibraryEntityRequest.make ~libraryArn
              ~entityType:(Values.EntityType.of_json entityType) ~entityId ())
           (Some Values.GetDataAutomationLibraryEntityResponse.to_json)
           (Some Values.GetDataAutomationLibraryEntityResponse.error_to_json)])
let get_data_automation_library_ingestion_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and libraryArn =
         flag "library-arn" (required string)
           ~doc:"STRING DataAutomationLibraryArn"
       and jobArn =
         flag "job-arn" (required string)
           ~doc:"STRING DataAutomationLibraryIngestionJobArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_automation_library_ingestion_job
           (Values.GetDataAutomationLibraryIngestionJobRequest.make
              ~libraryArn ~jobArn ())
           (Some Values.GetDataAutomationLibraryIngestionJobResponse.to_json)
           (Some
              Values.GetDataAutomationLibraryIngestionJobResponse.error_to_json)])
let get_data_automation_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectStage =
         flag "project-stage" (optional json_arg)
           ~doc:"JSON DataAutomationProjectStage"
       and projectArn =
         flag "project-arn" (required string)
           ~doc:"STRING DataAutomationProjectArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_automation_project
           (Values.GetDataAutomationProjectRequest.make
              ?projectStage:(Option.map
                               ~f:Values.DataAutomationProjectStage.of_json
                               projectStage) ~projectArn ())
           (Some Values.GetDataAutomationProjectResponse.to_json)
           (Some Values.GetDataAutomationProjectResponse.error_to_json)])
let invoke_blueprint_optimization_async =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and encryptionConfiguration =
         flag "encryption-configuration" (optional json_arg)
           ~doc:"JSON EncryptionConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and blueprint =
         flag "blueprint" (required json_arg)
           ~doc:"JSON BlueprintOptimizationObject"
       and samples =
         flag "samples" (required json_arg)
           ~doc:"JSON BlueprintOptimizationSamples"
       and outputConfiguration =
         flag "output-configuration" (required json_arg)
           ~doc:"JSON BlueprintOptimizationOutputConfiguration"
       and dataAutomationProfileArn =
         flag "data-automation-profile-arn" (required string)
           ~doc:"STRING DataAutomationProfileArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.invoke_blueprint_optimization_async
           (Values.InvokeBlueprintOptimizationAsyncRequest.make
              ?encryptionConfiguration:(Option.map
                                          ~f:Values.EncryptionConfiguration.of_json
                                          encryptionConfiguration)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~blueprint:(Values.BlueprintOptimizationObject.of_json
                            blueprint)
              ~samples:(Values.BlueprintOptimizationSamples.of_json samples)
              ~outputConfiguration:(Values.BlueprintOptimizationOutputConfiguration.of_json
                                      outputConfiguration)
              ~dataAutomationProfileArn ())
           (Some Values.InvokeBlueprintOptimizationAsyncResponse.to_json)
           (Some
              Values.InvokeBlueprintOptimizationAsyncResponse.error_to_json)])
let invoke_data_automation_library_ingestion_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and notificationConfiguration =
         flag "notification-configuration" (optional json_arg)
           ~doc:"JSON NotificationConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and libraryArn =
         flag "library-arn" (required string)
           ~doc:"STRING DataAutomationLibraryArn"
       and inputConfiguration =
         flag "input-configuration" (required json_arg)
           ~doc:"JSON InputConfiguration"
       and entityType =
         flag "entity-type" (required json_arg) ~doc:"JSON EntityType"
       and operationType =
         flag "operation-type" (required json_arg)
           ~doc:"JSON LibraryIngestionJobOperationType"
       and outputConfiguration =
         flag "output-configuration" (required json_arg)
           ~doc:"JSON OutputConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.invoke_data_automation_library_ingestion_job
           (Values.InvokeDataAutomationLibraryIngestionJobRequest.make
              ?clientToken
              ?notificationConfiguration:(Option.map
                                            ~f:Values.NotificationConfiguration.of_json
                                            notificationConfiguration)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~libraryArn
              ~inputConfiguration:(Values.InputConfiguration.of_json
                                     inputConfiguration)
              ~entityType:(Values.EntityType.of_json entityType)
              ~operationType:(Values.LibraryIngestionJobOperationType.of_json
                                operationType)
              ~outputConfiguration:(Values.OutputConfiguration.of_json
                                      outputConfiguration) ())
           (Some
              Values.InvokeDataAutomationLibraryIngestionJobResponse.to_json)
           (Some
              Values.InvokeDataAutomationLibraryIngestionJobResponse.error_to_json)])
let list_blueprints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and blueprintArn =
         flag "blueprint-arn" (optional string) ~doc:"STRING BlueprintArn"
       and resourceOwner =
         flag "resource-owner" (optional json_arg) ~doc:"JSON ResourceOwner"
       and blueprintStageFilter =
         flag "blueprint-stage-filter" (optional json_arg)
           ~doc:"JSON BlueprintStageFilter"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and projectFilter =
         flag "project-filter" (optional json_arg)
           ~doc:"JSON DataAutomationProjectFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_blueprints
           (Values.ListBlueprintsRequest.make ?blueprintArn
              ?resourceOwner:(Option.map ~f:Values.ResourceOwner.of_json
                                resourceOwner)
              ?blueprintStageFilter:(Option.map
                                       ~f:Values.BlueprintStageFilter.of_json
                                       blueprintStageFilter) ?maxResults
              ?nextToken
              ?projectFilter:(Option.map
                                ~f:Values.DataAutomationProjectFilter.of_json
                                projectFilter) ())
           (Some Values.ListBlueprintsResponse.to_json)
           (Some Values.ListBlueprintsResponse.error_to_json)])
let list_data_automation_libraries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and projectFilter =
         flag "project-filter" (optional json_arg)
           ~doc:"JSON DataAutomationProjectFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_automation_libraries
           (Values.ListDataAutomationLibrariesRequest.make ?maxResults
              ?nextToken
              ?projectFilter:(Option.map
                                ~f:Values.DataAutomationProjectFilter.of_json
                                projectFilter) ())
           (Some Values.ListDataAutomationLibrariesResponse.to_json)
           (Some Values.ListDataAutomationLibrariesResponse.error_to_json)])
let list_data_automation_library_entities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and libraryArn =
         flag "library-arn" (required string)
           ~doc:"STRING DataAutomationLibraryArn"
       and entityType =
         flag "entity-type" (required json_arg) ~doc:"JSON EntityType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_automation_library_entities
           (Values.ListDataAutomationLibraryEntitiesRequest.make ?maxResults
              ?nextToken ~libraryArn
              ~entityType:(Values.EntityType.of_json entityType) ())
           (Some Values.ListDataAutomationLibraryEntitiesResponse.to_json)
           (Some
              Values.ListDataAutomationLibraryEntitiesResponse.error_to_json)])
let list_data_automation_library_ingestion_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and libraryArn =
         flag "library-arn" (required string)
           ~doc:"STRING DataAutomationLibraryArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_automation_library_ingestion_jobs
           (Values.ListDataAutomationLibraryIngestionJobsRequest.make
              ?maxResults ?nextToken ~libraryArn ())
           (Some
              Values.ListDataAutomationLibraryIngestionJobsResponse.to_json)
           (Some
              Values.ListDataAutomationLibraryIngestionJobsResponse.error_to_json)])
let list_data_automation_projects =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and projectStageFilter =
         flag "project-stage-filter" (optional json_arg)
           ~doc:"JSON DataAutomationProjectStageFilter"
       and blueprintFilter =
         flag "blueprint-filter" (optional json_arg)
           ~doc:"JSON BlueprintFilter"
       and resourceOwner =
         flag "resource-owner" (optional json_arg) ~doc:"JSON ResourceOwner"
       and libraryFilter =
         flag "library-filter" (optional json_arg)
           ~doc:"JSON DataAutomationLibraryFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_automation_projects
           (Values.ListDataAutomationProjectsRequest.make ?maxResults
              ?nextToken
              ?projectStageFilter:(Option.map
                                     ~f:Values.DataAutomationProjectStageFilter.of_json
                                     projectStageFilter)
              ?blueprintFilter:(Option.map ~f:Values.BlueprintFilter.of_json
                                  blueprintFilter)
              ?resourceOwner:(Option.map ~f:Values.ResourceOwner.of_json
                                resourceOwner)
              ?libraryFilter:(Option.map
                                ~f:Values.DataAutomationLibraryFilter.of_json
                                libraryFilter) ())
           (Some Values.ListDataAutomationProjectsResponse.to_json)
           (Some Values.ListDataAutomationProjectsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING TaggableResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING TaggableResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING TaggableResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_blueprint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and blueprintStage =
         flag "blueprint-stage" (optional json_arg)
           ~doc:"JSON BlueprintStage"
       and encryptionConfiguration =
         flag "encryption-configuration" (optional json_arg)
           ~doc:"JSON EncryptionConfiguration"
       and blueprintArn =
         flag "blueprint-arn" (required string) ~doc:"STRING BlueprintArn"
       and schema =
         flag "schema" (required string) ~doc:"STRING BlueprintSchema" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_blueprint
           (Values.UpdateBlueprintRequest.make
              ?blueprintStage:(Option.map ~f:Values.BlueprintStage.of_json
                                 blueprintStage)
              ?encryptionConfiguration:(Option.map
                                          ~f:Values.EncryptionConfiguration.of_json
                                          encryptionConfiguration)
              ~blueprintArn ~schema ())
           (Some Values.UpdateBlueprintResponse.to_json)
           (Some Values.UpdateBlueprintResponse.error_to_json)])
let update_data_automation_library =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and libraryDescription =
         flag "library-description" (optional string)
           ~doc:"STRING DataAutomationLibraryDescription"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and libraryArn =
         flag "library-arn" (required string)
           ~doc:"STRING DataAutomationLibraryArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_data_automation_library
           (Values.UpdateDataAutomationLibraryRequest.make
              ?libraryDescription ?clientToken ~libraryArn ())
           (Some Values.UpdateDataAutomationLibraryResponse.to_json)
           (Some Values.UpdateDataAutomationLibraryResponse.error_to_json)])
let update_data_automation_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectStage =
         flag "project-stage" (optional json_arg)
           ~doc:"JSON DataAutomationProjectStage"
       and projectDescription =
         flag "project-description" (optional string)
           ~doc:"STRING DataAutomationProjectDescription"
       and customOutputConfiguration =
         flag "custom-output-configuration" (optional json_arg)
           ~doc:"JSON CustomOutputConfiguration"
       and overrideConfiguration =
         flag "override-configuration" (optional json_arg)
           ~doc:"JSON OverrideConfiguration"
       and dataAutomationLibraryConfiguration =
         flag "data-automation-library-configuration" (optional json_arg)
           ~doc:"JSON DataAutomationLibraryConfiguration"
       and encryptionConfiguration =
         flag "encryption-configuration" (optional json_arg)
           ~doc:"JSON EncryptionConfiguration"
       and projectArn =
         flag "project-arn" (required string)
           ~doc:"STRING DataAutomationProjectArn"
       and standardOutputConfiguration =
         flag "standard-output-configuration" (required json_arg)
           ~doc:"JSON StandardOutputConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_data_automation_project
           (Values.UpdateDataAutomationProjectRequest.make
              ?projectStage:(Option.map
                               ~f:Values.DataAutomationProjectStage.of_json
                               projectStage) ?projectDescription
              ?customOutputConfiguration:(Option.map
                                            ~f:Values.CustomOutputConfiguration.of_json
                                            customOutputConfiguration)
              ?overrideConfiguration:(Option.map
                                        ~f:Values.OverrideConfiguration.of_json
                                        overrideConfiguration)
              ?dataAutomationLibraryConfiguration:(Option.map
                                                     ~f:Values.DataAutomationLibraryConfiguration.of_json
                                                     dataAutomationLibraryConfiguration)
              ?encryptionConfiguration:(Option.map
                                          ~f:Values.EncryptionConfiguration.of_json
                                          encryptionConfiguration)
              ~projectArn
              ~standardOutputConfiguration:(Values.StandardOutputConfiguration.of_json
                                              standardOutputConfiguration) ())
           (Some Values.UpdateDataAutomationProjectResponse.to_json)
           (Some Values.UpdateDataAutomationProjectResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("copy-blueprint-stage", copy_blueprint_stage);
    ("create-blueprint", create_blueprint);
    ("create-blueprint-version", create_blueprint_version);
    ("create-data-automation-library", create_data_automation_library);
    ("create-data-automation-project", create_data_automation_project);
    ("delete-blueprint", delete_blueprint);
    ("delete-data-automation-library", delete_data_automation_library);
    ("delete-data-automation-project", delete_data_automation_project);
    ("get-blueprint", get_blueprint);
    ("get-blueprint-optimization-status", get_blueprint_optimization_status);
    ("get-data-automation-library", get_data_automation_library);
    ("get-data-automation-library-entity",
      get_data_automation_library_entity);
    ("get-data-automation-library-ingestion-job",
      get_data_automation_library_ingestion_job);
    ("get-data-automation-project", get_data_automation_project);
    ("invoke-blueprint-optimization-async",
      invoke_blueprint_optimization_async);
    ("invoke-data-automation-library-ingestion-job",
      invoke_data_automation_library_ingestion_job);
    ("list-blueprints", list_blueprints);
    ("list-data-automation-libraries", list_data_automation_libraries);
    ("list-data-automation-library-entities",
      list_data_automation_library_entities);
    ("list-data-automation-library-ingestion-jobs",
      list_data_automation_library_ingestion_jobs);
    ("list-data-automation-projects", list_data_automation_projects);
    ("list-tags-for-resource", list_tags_for_resource);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-blueprint", update_blueprint);
    ("update-data-automation-library", update_data_automation_library);
    ("update-data-automation-project", update_data_automation_project)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_scaling_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scalingPlanName =
         flag "scaling-plan-name" (required string)
           ~doc:"STRING ScalingPlanName"
       and applicationSource =
         flag "application-source" (required json_arg)
           ~doc:"JSON ApplicationSource"
       and scalingInstructions =
         flag "scaling-instructions" (required json_arg)
           ~doc:"JSON ScalingInstructions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_scaling_plan
           (Values.CreateScalingPlanRequest.make ~scalingPlanName
              ~applicationSource:(Values.ApplicationSource.of_json
                                    applicationSource)
              ~scalingInstructions:(Values.ScalingInstructions.of_json
                                      scalingInstructions) ())
           (Some Values.CreateScalingPlanResponse.to_json)
           (Some Values.CreateScalingPlanResponse.error_to_json)])
let delete_scaling_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scalingPlanName =
         flag "scaling-plan-name" (required string)
           ~doc:"STRING ScalingPlanName"
       and scalingPlanVersion =
         flag "scaling-plan-version" (required json_arg)
           ~doc:"JSON ScalingPlanVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_scaling_plan
           (Values.DeleteScalingPlanRequest.make ~scalingPlanName
              ~scalingPlanVersion:(Values.ScalingPlanVersion.of_json
                                     scalingPlanVersion) ())
           (Some Values.DeleteScalingPlanResponse.to_json)
           (Some Values.DeleteScalingPlanResponse.error_to_json)])
let describe_scaling_plan_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and scalingPlanName =
         flag "scaling-plan-name" (required string)
           ~doc:"STRING ScalingPlanName"
       and scalingPlanVersion =
         flag "scaling-plan-version" (required json_arg)
           ~doc:"JSON ScalingPlanVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_scaling_plan_resources
           (Values.DescribeScalingPlanResourcesRequest.make ?maxResults
              ?nextToken ~scalingPlanName
              ~scalingPlanVersion:(Values.ScalingPlanVersion.of_json
                                     scalingPlanVersion) ())
           (Some Values.DescribeScalingPlanResourcesResponse.to_json)
           (Some Values.DescribeScalingPlanResourcesResponse.error_to_json)])
let describe_scaling_plans =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scalingPlanNames =
         flag "scaling-plan-names" (optional json_arg)
           ~doc:"JSON ScalingPlanNames"
       and scalingPlanVersion =
         flag "scaling-plan-version" (optional json_arg)
           ~doc:"JSON ScalingPlanVersion"
       and applicationSources =
         flag "application-sources" (optional json_arg)
           ~doc:"JSON ApplicationSources"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_scaling_plans
           (Values.DescribeScalingPlansRequest.make
              ?scalingPlanNames:(Option.map
                                   ~f:Values.ScalingPlanNames.of_json
                                   scalingPlanNames)
              ?scalingPlanVersion:(Option.map
                                     ~f:Values.ScalingPlanVersion.of_json
                                     scalingPlanVersion)
              ?applicationSources:(Option.map
                                     ~f:Values.ApplicationSources.of_json
                                     applicationSources) ?maxResults
              ?nextToken ())
           (Some Values.DescribeScalingPlansResponse.to_json)
           (Some Values.DescribeScalingPlansResponse.error_to_json)])
let get_scaling_plan_resource_forecast_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scalingPlanName =
         flag "scaling-plan-name" (required string)
           ~doc:"STRING ScalingPlanName"
       and scalingPlanVersion =
         flag "scaling-plan-version" (required json_arg)
           ~doc:"JSON ScalingPlanVersion"
       and serviceNamespace =
         flag "service-namespace" (required json_arg)
           ~doc:"JSON ServiceNamespace"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING XmlString"
       and scalableDimension =
         flag "scalable-dimension" (required json_arg)
           ~doc:"JSON ScalableDimension"
       and forecastDataType =
         flag "forecast-data-type" (required json_arg)
           ~doc:"JSON ForecastDataType"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON TimestampType"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON TimestampType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_scaling_plan_resource_forecast_data
           (Values.GetScalingPlanResourceForecastDataRequest.make
              ~scalingPlanName
              ~scalingPlanVersion:(Values.ScalingPlanVersion.of_json
                                     scalingPlanVersion)
              ~serviceNamespace:(Values.ServiceNamespace.of_json
                                   serviceNamespace) ~resourceId
              ~scalableDimension:(Values.ScalableDimension.of_json
                                    scalableDimension)
              ~forecastDataType:(Values.ForecastDataType.of_json
                                   forecastDataType)
              ~startTime:(Values.TimestampType.of_json startTime)
              ~endTime:(Values.TimestampType.of_json endTime) ())
           (Some Values.GetScalingPlanResourceForecastDataResponse.to_json)
           (Some
              Values.GetScalingPlanResourceForecastDataResponse.error_to_json)])
let update_scaling_plan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationSource =
         flag "application-source" (optional json_arg)
           ~doc:"JSON ApplicationSource"
       and scalingInstructions =
         flag "scaling-instructions" (optional json_arg)
           ~doc:"JSON ScalingInstructions"
       and scalingPlanName =
         flag "scaling-plan-name" (required string)
           ~doc:"STRING ScalingPlanName"
       and scalingPlanVersion =
         flag "scaling-plan-version" (required json_arg)
           ~doc:"JSON ScalingPlanVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_scaling_plan
           (Values.UpdateScalingPlanRequest.make
              ?applicationSource:(Option.map
                                    ~f:Values.ApplicationSource.of_json
                                    applicationSource)
              ?scalingInstructions:(Option.map
                                      ~f:Values.ScalingInstructions.of_json
                                      scalingInstructions) ~scalingPlanName
              ~scalingPlanVersion:(Values.ScalingPlanVersion.of_json
                                     scalingPlanVersion) ())
           (Some Values.UpdateScalingPlanResponse.to_json)
           (Some Values.UpdateScalingPlanResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-scaling-plan", create_scaling_plan);
    ("delete-scaling-plan", delete_scaling_plan);
    ("describe-scaling-plan-resources", describe_scaling_plan_resources);
    ("describe-scaling-plans", describe_scaling_plans);
    ("get-scaling-plan-resource-forecast-data",
      get_scaling_plan_resource_forecast_data);
    ("update-scaling-plan", update_scaling_plan)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | CreateScalingPlan: (CreateScalingPlanRequest.t,
  CreateScalingPlanResponse.t, CreateScalingPlanResponse.error) t 
  | DeleteScalingPlan: (DeleteScalingPlanRequest.t,
  DeleteScalingPlanResponse.t, DeleteScalingPlanResponse.error) t 
  | DescribeScalingPlanResources: (DescribeScalingPlanResourcesRequest.t,
  DescribeScalingPlanResourcesResponse.t,
  DescribeScalingPlanResourcesResponse.error) t 
  | DescribeScalingPlans: (DescribeScalingPlansRequest.t,
  DescribeScalingPlansResponse.t, DescribeScalingPlansResponse.error) t 
  | GetScalingPlanResourceForecastData:
  (GetScalingPlanResourceForecastDataRequest.t,
  GetScalingPlanResourceForecastDataResponse.t,
  GetScalingPlanResourceForecastDataResponse.error) t 
  | UpdateScalingPlan: (UpdateScalingPlanRequest.t,
  UpdateScalingPlanResponse.t, UpdateScalingPlanResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | CreateScalingPlan -> `POST
  | DeleteScalingPlan -> `POST
  | DescribeScalingPlanResources -> `POST
  | DescribeScalingPlans -> `POST
  | GetScalingPlanResourceForecastData -> `POST
  | UpdateScalingPlan -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | CreateScalingPlan -> (Format.kasprintf Uri.of_string) "/"
      | DeleteScalingPlan -> (Format.kasprintf Uri.of_string) "/"
      | DescribeScalingPlanResources -> (Format.kasprintf Uri.of_string) "/"
      | DescribeScalingPlans -> (Format.kasprintf Uri.of_string) "/"
      | GetScalingPlanResourceForecastData ->
          (Format.kasprintf Uri.of_string) "/"
      | UpdateScalingPlan -> (Format.kasprintf Uri.of_string) "/")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  match endp with
  | CreateScalingPlan ->
      let json = CreateScalingPlanRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target",
            "AnyScaleScalingPlannerFrontendService.CreateScalingPlan")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | DeleteScalingPlan ->
      let json = DeleteScalingPlanRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target",
            "AnyScaleScalingPlannerFrontendService.DeleteScalingPlan")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | DescribeScalingPlanResources ->
      let json = DescribeScalingPlanResourcesRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target",
            "AnyScaleScalingPlannerFrontendService.DescribeScalingPlanResources")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | DescribeScalingPlans ->
      let json = DescribeScalingPlansRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target",
            "AnyScaleScalingPlannerFrontendService.DescribeScalingPlans")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | GetScalingPlanResourceForecastData ->
      let json = GetScalingPlanResourceForecastDataRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target",
            "AnyScaleScalingPlannerFrontendService.GetScalingPlanResourceForecastData")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | UpdateScalingPlan ->
      let json = UpdateScalingPlanRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.1");
          ("X-Amz-Target",
            "AnyScaleScalingPlannerFrontendService.UpdateScalingPlan")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type = None }) in
    match (error_of_json, ((code >= 400) && (code <= 599))) with
    | (Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        (match json |> (Yojson.Safe.Util.member "__type") with
         | `String error_type -> error_of_json error_type json
         | `Null -> bail ()
         | _ ->
             failwith
               (sprintf "Error '__type' did not have string type: %s" body))
    | (None, _) | (_, false) -> bail () in
  let _ = parse_aws_error in
  let _ = resp in
  match endpoint with
  | CreateScalingPlan ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (CreateScalingPlanResponse.of_json json)
      else
        Error
          (parse_aws_error (Some CreateScalingPlanResponse.error_of_json))
  | DeleteScalingPlan ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (DeleteScalingPlanResponse.of_json json)
      else
        Error
          (parse_aws_error (Some DeleteScalingPlanResponse.error_of_json))
  | DescribeScalingPlanResources ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (DescribeScalingPlanResourcesResponse.of_json json)
      else
        Error
          (parse_aws_error
             (Some DescribeScalingPlanResourcesResponse.error_of_json))
  | DescribeScalingPlans ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (DescribeScalingPlansResponse.of_json json)
      else
        Error
          (parse_aws_error (Some DescribeScalingPlansResponse.error_of_json))
  | GetScalingPlanResourceForecastData ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (GetScalingPlanResourceForecastDataResponse.of_json json)
      else
        Error
          (parse_aws_error
             (Some GetScalingPlanResourceForecastDataResponse.error_of_json))
  | UpdateScalingPlan ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (UpdateScalingPlanResponse.of_json json)
      else
        Error
          (parse_aws_error (Some UpdateScalingPlanResponse.error_of_json))
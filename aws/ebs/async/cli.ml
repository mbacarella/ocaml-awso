(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let complete_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and checksum =
         flag "checksum" (optional string) ~doc:"STRING Checksum"
       and checksumAlgorithm =
         flag "checksum-algorithm" (optional json_arg)
           ~doc:"JSON ChecksumAlgorithm"
       and checksumAggregationMethod =
         flag "checksum-aggregation-method" (optional json_arg)
           ~doc:"JSON ChecksumAggregationMethod"
       and snapshotId =
         flag "snapshot-id" (required string) ~doc:"STRING SnapshotId"
       and changedBlocksCount =
         flag "changed-blocks-count" (required int)
           ~doc:"INT ChangedBlocksCount" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.complete_snapshot
           (Values.CompleteSnapshotRequest.make ?checksum
              ?checksumAlgorithm:(Option.map
                                    ~f:Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm)
              ?checksumAggregationMethod:(Option.map
                                            ~f:Values.ChecksumAggregationMethod.of_json
                                            checksumAggregationMethod)
              ~snapshotId ~changedBlocksCount ())
           (Some Values.CompleteSnapshotResponse.to_json)
           (Some Values.CompleteSnapshotResponse.error_to_json)])
let get_snapshot_block =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snapshotId =
         flag "snapshot-id" (required string) ~doc:"STRING SnapshotId"
       and blockIndex =
         flag "block-index" (required int) ~doc:"INT BlockIndex"
       and blockToken =
         flag "block-token" (required string) ~doc:"STRING BlockToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_snapshot_block
           (Values.GetSnapshotBlockRequest.make ~snapshotId ~blockIndex
              ~blockToken ()) (Some Values.GetSnapshotBlockResponse.to_json)
           (Some Values.GetSnapshotBlockResponse.error_to_json)])
let list_changed_blocks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and firstSnapshotId =
         flag "first-snapshot-id" (optional string) ~doc:"STRING SnapshotId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PageToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and startingBlockIndex =
         flag "starting-block-index" (optional int) ~doc:"INT BlockIndex"
       and secondSnapshotId =
         flag "second-snapshot-id" (required string) ~doc:"STRING SnapshotId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_changed_blocks
           (Values.ListChangedBlocksRequest.make ?firstSnapshotId ?nextToken
              ?maxResults ?startingBlockIndex ~secondSnapshotId ())
           (Some Values.ListChangedBlocksResponse.to_json)
           (Some Values.ListChangedBlocksResponse.error_to_json)])
let list_snapshot_blocks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PageToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and startingBlockIndex =
         flag "starting-block-index" (optional int) ~doc:"INT BlockIndex"
       and snapshotId =
         flag "snapshot-id" (required string) ~doc:"STRING SnapshotId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_snapshot_blocks
           (Values.ListSnapshotBlocksRequest.make ?nextToken ?maxResults
              ?startingBlockIndex ~snapshotId ())
           (Some Values.ListSnapshotBlocksResponse.to_json)
           (Some Values.ListSnapshotBlocksResponse.error_to_json)])
let put_snapshot_block =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and progress = flag "progress" (optional int) ~doc:"INT Progress"
       and snapshotId =
         flag "snapshot-id" (required string) ~doc:"STRING SnapshotId"
       and blockIndex =
         flag "block-index" (required int) ~doc:"INT BlockIndex"
       and blockData =
         flag "block-data" (required json_arg) ~doc:"JSON BlockData"
       and dataLength =
         flag "data-length" (required int) ~doc:"INT DataLength"
       and checksum =
         flag "checksum" (required string) ~doc:"STRING Checksum"
       and checksumAlgorithm =
         flag "checksum-algorithm" (required json_arg)
           ~doc:"JSON ChecksumAlgorithm" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_snapshot_block
           (Values.PutSnapshotBlockRequest.make ?progress ~snapshotId
              ~blockIndex ~blockData:(Values.BlockData.of_json blockData)
              ~dataLength ~checksum
              ~checksumAlgorithm:(Values.ChecksumAlgorithm.of_json
                                    checksumAlgorithm) ())
           (Some Values.PutSnapshotBlockResponse.to_json)
           (Some Values.PutSnapshotBlockResponse.error_to_json)])
let start_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parentSnapshotId =
         flag "parent-snapshot-id" (optional string) ~doc:"STRING SnapshotId"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING IdempotencyToken"
       and encrypted = flag "encrypted" (optional bool) ~doc:"BOOL Boolean"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and timeout = flag "timeout" (optional int) ~doc:"INT Timeout"
       and volumeSize =
         flag "volume-size" (required json_arg) ~doc:"JSON VolumeSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_snapshot
           (Values.StartSnapshotRequest.make ?parentSnapshotId
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ?description
              ?clientToken ?encrypted ?kmsKeyArn ?timeout
              ~volumeSize:(Values.VolumeSize.of_json volumeSize) ())
           (Some Values.StartSnapshotResponse.to_json)
           (Some Values.StartSnapshotResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("complete-snapshot", complete_snapshot);
    ("get-snapshot-block", get_snapshot_block);
    ("list-changed-blocks", list_changed_blocks);
    ("list-snapshot-blocks", list_snapshot_blocks);
    ("put-snapshot-block", put_snapshot_block);
    ("start-snapshot", start_snapshot)]
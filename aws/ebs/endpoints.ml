(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | CompleteSnapshot: (CompleteSnapshotRequest.t, CompleteSnapshotResponse.t,
  CompleteSnapshotResponse.error) t 
  | GetSnapshotBlock: (GetSnapshotBlockRequest.t, GetSnapshotBlockResponse.t,
  GetSnapshotBlockResponse.error) t 
  | ListChangedBlocks: (ListChangedBlocksRequest.t,
  ListChangedBlocksResponse.t, ListChangedBlocksResponse.error) t 
  | ListSnapshotBlocks: (ListSnapshotBlocksRequest.t,
  ListSnapshotBlocksResponse.t, ListSnapshotBlocksResponse.error) t 
  | PutSnapshotBlock: (PutSnapshotBlockRequest.t, PutSnapshotBlockResponse.t,
  PutSnapshotBlockResponse.error) t 
  | StartSnapshot: (StartSnapshotRequest.t, StartSnapshotResponse.t,
  StartSnapshotResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | CompleteSnapshot -> `POST
  | GetSnapshotBlock -> `GET
  | ListChangedBlocks -> `GET
  | ListSnapshotBlocks -> `GET
  | PutSnapshotBlock -> `PUT
  | StartSnapshot -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | CompleteSnapshot ->
          (Format.kasprintf Uri.of_string) "/snapshots/completion/%s"
            (SnapshotId.to_header x.CompleteSnapshotRequest.snapshotId)
      | GetSnapshotBlock ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/snapshots/%s/blocks/%s"
               (SnapshotId.to_header x.GetSnapshotBlockRequest.snapshotId)
               (BlockIndex.to_header x.GetSnapshotBlockRequest.blockIndex))
            (List.filter_opt
               [Some ("blockToken", (BlockToken.to_header x.blockToken))])
      | ListChangedBlocks ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/snapshots/%s/changedblocks"
               (SnapshotId.to_header
                  x.ListChangedBlocksRequest.secondSnapshotId))
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("firstSnapshotId", (SnapshotId.to_header v)))
                  x.firstSnapshotId;
               Option.map
                 ~f:(fun v -> ("pageToken", (PageToken.to_header v)))
                 x.nextToken;
               Option.map
                 ~f:(fun v -> ("maxResults", (MaxResults.to_header v)))
                 x.maxResults;
               Option.map
                 ~f:(fun v ->
                       ("startingBlockIndex", (BlockIndex.to_header v)))
                 x.startingBlockIndex])
      | ListSnapshotBlocks ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/snapshots/%s/blocks"
               (SnapshotId.to_header x.ListSnapshotBlocksRequest.snapshotId))
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("pageToken", (PageToken.to_header v)))
                  x.nextToken;
               Option.map
                 ~f:(fun v -> ("maxResults", (MaxResults.to_header v)))
                 x.maxResults;
               Option.map
                 ~f:(fun v ->
                       ("startingBlockIndex", (BlockIndex.to_header v)))
                 x.startingBlockIndex])
      | PutSnapshotBlock ->
          (Format.kasprintf Uri.of_string) "/snapshots/%s/blocks/%s"
            (SnapshotId.to_header x.PutSnapshotBlockRequest.snapshotId)
            (BlockIndex.to_header x.PutSnapshotBlockRequest.blockIndex)
      | StartSnapshot -> (Format.kasprintf Uri.of_string) "/snapshots")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | CompleteSnapshot ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Some
                   ("x-amz-ChangedBlocksCount",
                     (ChangedBlocksCount.to_header
                        req.CompleteSnapshotRequest.changedBlocksCount));
                Option.map req.CompleteSnapshotRequest.checksum
                  ~f:(fun x -> ("x-amz-Checksum", (Checksum.to_header x)));
                Option.map req.CompleteSnapshotRequest.checksumAlgorithm
                  ~f:(fun x ->
                        ("x-amz-Checksum-Algorithm",
                          (ChecksumAlgorithm.to_header x)));
                Option.map
                  req.CompleteSnapshotRequest.checksumAggregationMethod
                  ~f:(fun x ->
                        ("x-amz-Checksum-Aggregation-Method",
                          (ChecksumAggregationMethod.to_header x)))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map (List.filter_opt [])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetSnapshotBlock ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListChangedBlocks ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListSnapshotBlocks ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | PutSnapshotBlock ->
      let headers =
        Some
          ((List.filter_opt
              [Some
                 ("x-amz-Data-Length",
                   (DataLength.to_header
                      req.PutSnapshotBlockRequest.dataLength));
              Option.map req.PutSnapshotBlockRequest.progress
                ~f:(fun x -> ("x-amz-Progress", (Progress.to_header x)));
              Some
                ("x-amz-Checksum",
                  (Checksum.to_header req.PutSnapshotBlockRequest.checksum));
              Some
                ("x-amz-Checksum-Algorithm",
                  (ChecksumAlgorithm.to_header
                     req.PutSnapshotBlockRequest.checksumAlgorithm))])
             |> Awso.Http.Headers.of_list) in
      let body = BlockData.to_header req.blockData in
      Awso.Http.Request.make ?headers ~body (method_of_endpoint endp)
  | StartSnapshot ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("VolumeSize",
                           (VolumeSize.to_value
                              req.StartSnapshotRequest.volumeSize));
                      Option.map req.StartSnapshotRequest.parentSnapshotId
                        ~f:(fun x ->
                              ("ParentSnapshotId", (SnapshotId.to_value x)));
                      Option.map req.StartSnapshotRequest.tags
                        ~f:(fun x -> ("Tags", (Tags.to_value x)));
                      Option.map req.StartSnapshotRequest.description
                        ~f:(fun x ->
                              ("Description", (Description.to_value x)));
                      Option.map req.StartSnapshotRequest.clientToken
                        ~f:(fun x ->
                              ("ClientToken", (IdempotencyToken.to_value x)));
                      Option.map req.StartSnapshotRequest.encrypted
                        ~f:(fun x -> ("Encrypted", (Boolean.to_value x)));
                      Option.map req.StartSnapshotRequest.kmsKeyArn
                        ~f:(fun x -> ("KmsKeyArn", (KmsKeyArn.to_value x)));
                      Option.map req.StartSnapshotRequest.timeout
                        ~f:(fun x -> ("Timeout", (Timeout.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | CompleteSnapshot ->
      if is_success
      then Ok (CompleteSnapshotResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some CompleteSnapshotResponse.error_of_json))
  | GetSnapshotBlock ->
      if is_success
      then
        let body = BlockData.of_string (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (GetSnapshotBlockResponse.of_header_and_body (headers, body))
      else
        Error (parse_aws_error (Some GetSnapshotBlockResponse.error_of_json))
  | ListChangedBlocks ->
      if is_success
      then Ok (ListChangedBlocksResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListChangedBlocksResponse.error_of_json))
  | ListSnapshotBlocks ->
      if is_success
      then Ok (ListSnapshotBlocksResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListSnapshotBlocksResponse.error_of_json))
  | PutSnapshotBlock ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (PutSnapshotBlockResponse.of_header_and_body (headers, ()))
      else
        Error (parse_aws_error (Some PutSnapshotBlockResponse.error_of_json))
  | StartSnapshot ->
      if is_success
      then Ok (StartSnapshotResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some StartSnapshotResponse.error_of_json))
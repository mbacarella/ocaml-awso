(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.ebs
let apiVersion = "2019-11-02"
let endpointPrefix = "ebs"
let serviceFullName = "Amazon Elastic Block Store"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let serviceAbbreviation = "Amazon EBS"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:127) >>=
             (fun () -> check_pattern i ~pattern:"^[\\S\\s]+$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:255) >>=
             (fun () -> check_pattern i ~pattern:"^[\\S\\s]*$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module BlockIndex =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for BlockIndex" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module BlockToken =
  struct
    type nonrec t = string
    let context_ = "BlockToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_pattern i ~pattern:"^[A-Za-z0-9+/=]+$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BlockToken" j
    let to_json = simple_to_json to_value
  end
module AccessDeniedExceptionReason =
  struct
    type nonrec t =
      | UNAUTHORIZED_ACCOUNT 
      | DEPENDENCY_ACCESS_DENIED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | UNAUTHORIZED_ACCOUNT -> "UNAUTHORIZED_ACCOUNT"
      | DEPENDENCY_ACCESS_DENIED -> "DEPENDENCY_ACCESS_DENIED"
      | Non_static_id s -> s
    let of_string =
      function
      | "UNAUTHORIZED_ACCOUNT" -> UNAUTHORIZED_ACCOUNT
      | "DEPENDENCY_ACCESS_DENIED" -> DEPENDENCY_ACCESS_DENIED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration AccessDeniedExceptionReason"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"AccessDeniedExceptionReason" j)
    let to_json = simple_to_json to_value
  end
module ErrorMessage =
  struct
    type nonrec t = string
    let context_ = "ErrorMessage"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:256); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorMessage" j
    let to_json = simple_to_json to_value
  end
module RequestThrottledExceptionReason =
  struct
    type nonrec t =
      | ACCOUNT_THROTTLED 
      | DEPENDENCY_REQUEST_THROTTLED 
      | RESOURCE_LEVEL_THROTTLE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACCOUNT_THROTTLED -> "ACCOUNT_THROTTLED"
      | DEPENDENCY_REQUEST_THROTTLED -> "DEPENDENCY_REQUEST_THROTTLED"
      | RESOURCE_LEVEL_THROTTLE -> "RESOURCE_LEVEL_THROTTLE"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACCOUNT_THROTTLED" -> ACCOUNT_THROTTLED
      | "DEPENDENCY_REQUEST_THROTTLED" -> DEPENDENCY_REQUEST_THROTTLED
      | "RESOURCE_LEVEL_THROTTLE" -> RESOURCE_LEVEL_THROTTLE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration RequestThrottledExceptionReason"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"RequestThrottledExceptionReason" j)
    let to_json = simple_to_json to_value
  end
module ResourceNotFoundExceptionReason =
  struct
    type nonrec t =
      | SNAPSHOT_NOT_FOUND 
      | DEPENDENCY_RESOURCE_NOT_FOUND 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SNAPSHOT_NOT_FOUND -> "SNAPSHOT_NOT_FOUND"
      | DEPENDENCY_RESOURCE_NOT_FOUND -> "DEPENDENCY_RESOURCE_NOT_FOUND"
      | Non_static_id s -> s
    let of_string =
      function
      | "SNAPSHOT_NOT_FOUND" -> SNAPSHOT_NOT_FOUND
      | "DEPENDENCY_RESOURCE_NOT_FOUND" -> DEPENDENCY_RESOURCE_NOT_FOUND
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ResourceNotFoundExceptionReason"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ResourceNotFoundExceptionReason" j)
    let to_json = simple_to_json to_value
  end
module ServiceQuotaExceededExceptionReason =
  struct
    type nonrec t =
      | DEPENDENCY_SERVICE_QUOTA_EXCEEDED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DEPENDENCY_SERVICE_QUOTA_EXCEEDED ->
          "DEPENDENCY_SERVICE_QUOTA_EXCEEDED"
      | Non_static_id s -> s
    let of_string =
      function
      | "DEPENDENCY_SERVICE_QUOTA_EXCEEDED" ->
          DEPENDENCY_SERVICE_QUOTA_EXCEEDED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml
           ~kind:"enumeration ServiceQuotaExceededExceptionReason" xml_arg0)
    let of_json j =
      of_string
        (string_of_json ~kind:"ServiceQuotaExceededExceptionReason" j)
    let to_json = simple_to_json to_value
  end
module Tag =
  struct
    type nonrec t =
      {
      key: TagKey.t option [@ocaml.doc "The key of the tag."];
      value: TagValue.t option [@ocaml.doc "The value of the tag."]}
    let make ?key = fun ?value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("Key", (Option.map x.key ~f:TagKey.to_value));
        ("Value", (Option.map x.value ~f:TagValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:TagValue.of_xml) (Xml.child xml_arg0 "Value") in
      let key = (Option.map ~f:TagKey.of_xml) (Xml.child xml_arg0 "Key") in
      make ?value ?key ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let value = field_map json "Value" TagValue.of_json in
      let key = field_map json "Key" TagKey.of_json in make ?value ?key ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes a tag."]
module ValidationExceptionReason =
  struct
    type nonrec t =
      | INVALID_CUSTOMER_KEY 
      | INVALID_PAGE_TOKEN 
      | INVALID_BLOCK_TOKEN 
      | INVALID_SNAPSHOT_ID 
      | UNRELATED_SNAPSHOTS 
      | INVALID_BLOCK 
      | INVALID_CONTENT_ENCODING 
      | INVALID_TAG 
      | INVALID_DEPENDENCY_REQUEST 
      | INVALID_PARAMETER_VALUE 
      | INVALID_VOLUME_SIZE 
      | CONFLICTING_BLOCK_UPDATE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | INVALID_CUSTOMER_KEY -> "INVALID_CUSTOMER_KEY"
      | INVALID_PAGE_TOKEN -> "INVALID_PAGE_TOKEN"
      | INVALID_BLOCK_TOKEN -> "INVALID_BLOCK_TOKEN"
      | INVALID_SNAPSHOT_ID -> "INVALID_SNAPSHOT_ID"
      | UNRELATED_SNAPSHOTS -> "UNRELATED_SNAPSHOTS"
      | INVALID_BLOCK -> "INVALID_BLOCK"
      | INVALID_CONTENT_ENCODING -> "INVALID_CONTENT_ENCODING"
      | INVALID_TAG -> "INVALID_TAG"
      | INVALID_DEPENDENCY_REQUEST -> "INVALID_DEPENDENCY_REQUEST"
      | INVALID_PARAMETER_VALUE -> "INVALID_PARAMETER_VALUE"
      | INVALID_VOLUME_SIZE -> "INVALID_VOLUME_SIZE"
      | CONFLICTING_BLOCK_UPDATE -> "CONFLICTING_BLOCK_UPDATE"
      | Non_static_id s -> s
    let of_string =
      function
      | "INVALID_CUSTOMER_KEY" -> INVALID_CUSTOMER_KEY
      | "INVALID_PAGE_TOKEN" -> INVALID_PAGE_TOKEN
      | "INVALID_BLOCK_TOKEN" -> INVALID_BLOCK_TOKEN
      | "INVALID_SNAPSHOT_ID" -> INVALID_SNAPSHOT_ID
      | "UNRELATED_SNAPSHOTS" -> UNRELATED_SNAPSHOTS
      | "INVALID_BLOCK" -> INVALID_BLOCK
      | "INVALID_CONTENT_ENCODING" -> INVALID_CONTENT_ENCODING
      | "INVALID_TAG" -> INVALID_TAG
      | "INVALID_DEPENDENCY_REQUEST" -> INVALID_DEPENDENCY_REQUEST
      | "INVALID_PARAMETER_VALUE" -> INVALID_PARAMETER_VALUE
      | "INVALID_VOLUME_SIZE" -> INVALID_VOLUME_SIZE
      | "CONFLICTING_BLOCK_UPDATE" -> CONFLICTING_BLOCK_UPDATE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ValidationExceptionReason" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ValidationExceptionReason" j)
    let to_json = simple_to_json to_value
  end
module Block =
  struct
    type nonrec t =
      {
      blockIndex: BlockIndex.t option [@ocaml.doc "The block index."];
      blockToken: BlockToken.t option
        [@ocaml.doc "The block token for the block index."]}
    let make ?blockIndex =
      fun ?blockToken -> fun () -> { blockIndex; blockToken }
    let to_value x =
      structure_to_value
        [("BlockIndex", (Option.map x.blockIndex ~f:BlockIndex.to_value));
        ("BlockToken", (Option.map x.blockToken ~f:BlockToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let blockToken =
        (Option.map ~f:BlockToken.of_xml) (Xml.child xml_arg0 "BlockToken") in
      let blockIndex =
        (Option.map ~f:BlockIndex.of_xml) (Xml.child xml_arg0 "BlockIndex") in
      make ?blockToken ?blockIndex ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let blockToken = field_map json "BlockToken" BlockToken.of_json in
      let blockIndex = field_map json "BlockIndex" BlockIndex.of_json in
      make ?blockToken ?blockIndex ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A block of data in an Amazon Elastic Block Store snapshot."]
module ChangedBlock =
  struct
    type nonrec t =
      {
      blockIndex: BlockIndex.t option [@ocaml.doc "The block index."];
      firstBlockToken: BlockToken.t option
        [@ocaml.doc
          "The block token for the block index of the FirstSnapshotId specified in the ListChangedBlocks operation. This value is absent if the first snapshot does not have the changed block that is on the second snapshot."];
      secondBlockToken: BlockToken.t option
        [@ocaml.doc
          "The block token for the block index of the SecondSnapshotId specified in the ListChangedBlocks operation."]}
    let make ?blockIndex =
      fun ?firstBlockToken ->
        fun ?secondBlockToken ->
          fun () -> { blockIndex; firstBlockToken; secondBlockToken }
    let to_value x =
      structure_to_value
        [("BlockIndex", (Option.map x.blockIndex ~f:BlockIndex.to_value));
        ("FirstBlockToken",
          (Option.map x.firstBlockToken ~f:BlockToken.to_value));
        ("SecondBlockToken",
          (Option.map x.secondBlockToken ~f:BlockToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let secondBlockToken =
        (Option.map ~f:BlockToken.of_xml)
          (Xml.child xml_arg0 "SecondBlockToken") in
      let firstBlockToken =
        (Option.map ~f:BlockToken.of_xml)
          (Xml.child xml_arg0 "FirstBlockToken") in
      let blockIndex =
        (Option.map ~f:BlockIndex.of_xml) (Xml.child xml_arg0 "BlockIndex") in
      make ?secondBlockToken ?firstBlockToken ?blockIndex ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let secondBlockToken =
        field_map json "SecondBlockToken" BlockToken.of_json in
      let firstBlockToken =
        field_map json "FirstBlockToken" BlockToken.of_json in
      let blockIndex = field_map json "BlockIndex" BlockIndex.of_json in
      make ?secondBlockToken ?firstBlockToken ?blockIndex ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A block of data in an Amazon Elastic Block Store snapshot that is different from another snapshot of the same volume/snapshot lineage."]
module AccessDeniedException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option ;
      reason: AccessDeniedExceptionReason.t
        [@ocaml.doc "The reason for the exception."]}
    let context_ = "AccessDeniedException"
    let make ?message = fun ~reason -> fun () -> { message; reason }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value));
        ("Reason", (Some (AccessDeniedExceptionReason.to_value x.reason)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reason =
        AccessDeniedExceptionReason.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Reason") in
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ~reason ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let reason =
        field_map_exn json "Reason" AccessDeniedExceptionReason.of_json in
      let message = field_map json "Message" ErrorMessage.of_json in
      make ~reason ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You do not have sufficient access to perform this action."]
module BlockSize =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for BlockSize" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ConcurrentLimitExceededException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You have reached the limit for concurrent API requests. For more information, see Optimizing performance of the EBS direct APIs in the Amazon Elastic Compute Cloud User Guide."]
module ConflictException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request uses the same client token as a previous, but non-identical request."]
module Description =
  struct
    type nonrec t = string
    let context_ = "Description"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:255) >>=
             (fun () -> check_pattern i ~pattern:"^[\\S\\s]+$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Description" j
    let to_json = simple_to_json to_value
  end
module InternalServerException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An internal error has occurred."]
module KmsKeyArn =
  struct
    type nonrec t = string
    let context_ = "KmsKeyArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:aws[a-z\\-]*:kms:.*:[0-9]{12}:key/.*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"KmsKeyArn" j
    let to_json = simple_to_json to_value
  end
module OwnerId =
  struct
    type nonrec t = string
    let context_ = "OwnerId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:24) >>=
                  (fun () -> check_pattern i ~pattern:"\\S+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"OwnerId" j
    let to_json = simple_to_json to_value
  end
module RequestThrottledException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option ;
      reason: RequestThrottledExceptionReason.t option
        [@ocaml.doc "The reason for the exception."]}
    let make ?message = fun ?reason -> fun () -> { message; reason }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value));
        ("Reason",
          (Option.map x.reason ~f:RequestThrottledExceptionReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reason =
        (Option.map ~f:RequestThrottledExceptionReason.of_xml)
          (Xml.child xml_arg0 "Reason") in
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?reason ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let reason =
        field_map json "Reason" RequestThrottledExceptionReason.of_json in
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?reason ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The number of API requests has exceed the maximum allowed API request throttling limit."]
module ResourceNotFoundException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option ;
      reason: ResourceNotFoundExceptionReason.t option
        [@ocaml.doc "The reason for the exception."]}
    let make ?message = fun ?reason -> fun () -> { message; reason }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value));
        ("Reason",
          (Option.map x.reason ~f:ResourceNotFoundExceptionReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reason =
        (Option.map ~f:ResourceNotFoundExceptionReason.of_xml)
          (Xml.child xml_arg0 "Reason") in
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?reason ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let reason =
        field_map json "Reason" ResourceNotFoundExceptionReason.of_json in
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?reason ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The specified resource does not exist."]
module ServiceQuotaExceededException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option ;
      reason: ServiceQuotaExceededExceptionReason.t option
        [@ocaml.doc "The reason for the exception."]}
    let make ?message = fun ?reason -> fun () -> { message; reason }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value));
        ("Reason",
          (Option.map x.reason
             ~f:ServiceQuotaExceededExceptionReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reason =
        (Option.map ~f:ServiceQuotaExceededExceptionReason.of_xml)
          (Xml.child xml_arg0 "Reason") in
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?reason ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let reason =
        field_map json "Reason" ServiceQuotaExceededExceptionReason.of_json in
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?reason ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Your current service quotas do not allow you to perform this action."]
module SnapshotId =
  struct
    type nonrec t = string
    let context_ = "SnapshotId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"^snap-[0-9a-f]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SnapshotId" j
    let to_json = simple_to_json to_value
  end
module Status =
  struct
    type nonrec t =
      | Completed 
      | Pending 
      | Error 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Completed -> "completed"
      | Pending -> "pending"
      | Error -> "error"
      | Non_static_id s -> s
    let of_string =
      function
      | "completed" -> Completed
      | "pending" -> Pending
      | "error" -> Error
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Status" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Status" j)
    let to_json = simple_to_json to_value
  end
module Tags =
  struct
    type nonrec t = Tag.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Tag.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tag.of_xml)
    let of_json j = list_of_json ~kind:"Tags" ~of_json:Tag.of_json j
    let to_json v = composed_to_json to_value v
  end
module TimeStamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module ValidationException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option ;
      reason: ValidationExceptionReason.t option
        [@ocaml.doc "The reason for the validation exception."]}
    let make ?message = fun ?reason -> fun () -> { message; reason }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value));
        ("Reason",
          (Option.map x.reason ~f:ValidationExceptionReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reason =
        (Option.map ~f:ValidationExceptionReason.of_xml)
          (Xml.child xml_arg0 "Reason") in
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?reason ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let reason = field_map json "Reason" ValidationExceptionReason.of_json in
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?reason ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The input fails to satisfy the constraints of the EBS direct APIs."]
module VolumeSize =
  struct
    type nonrec t = Int64.t
    let make i =
      let open Result in ok_or_failwith (check_int64_min i ~min:1L); i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module IdempotencyToken =
  struct
    type nonrec t = string
    let context_ = "IdempotencyToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:255) >>=
             (fun () -> check_pattern i ~pattern:"^[\\S]+$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"IdempotencyToken" j
    let to_json = simple_to_json to_value
  end
module Timeout =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:4320) >>=
             (fun () -> check_int_min i ~min:10));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Timeout" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Checksum =
  struct
    type nonrec t = string
    let context_ = "Checksum"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_pattern i ~pattern:"^[A-Za-z0-9+/=]+$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Checksum" j
    let to_json = simple_to_json to_value
  end
module ChecksumAlgorithm =
  struct
    type nonrec t =
      | SHA256 
      | Non_static_id of string 
    let make i = i
    let to_string = function | SHA256 -> "SHA256" | Non_static_id s -> s
    let of_string = function | "SHA256" -> SHA256 | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ChecksumAlgorithm" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ChecksumAlgorithm" j)
    let to_json = simple_to_json to_value
  end
module BlockData =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module DataLength =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for DataLength" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Progress =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Progress" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Blocks =
  struct
    type nonrec t = Block.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Block.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Block.of_xml)
    let of_json j = list_of_json ~kind:"Blocks" ~of_json:Block.of_json j
    let to_json v = composed_to_json to_value v
  end
module PageToken =
  struct
    type nonrec t = string
    let context_ = "PageToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_pattern i ~pattern:"^[A-Za-z0-9+/=]+$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PageToken" j
    let to_json = simple_to_json to_value
  end
module MaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:10000) >>=
             (fun () -> check_int_min i ~min:100));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ChangedBlocks =
  struct
    type nonrec t = ChangedBlock.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ChangedBlock.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ChangedBlock.of_xml)
    let of_json j =
      list_of_json ~kind:"ChangedBlocks" ~of_json:ChangedBlock.of_json j
    let to_json v = composed_to_json to_value v
  end
module ChangedBlocksCount =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ChangedBlocksCount" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ChecksumAggregationMethod =
  struct
    type nonrec t =
      | LINEAR 
      | Non_static_id of string 
    let make i = i
    let to_string = function | LINEAR -> "LINEAR" | Non_static_id s -> s
    let of_string = function | "LINEAR" -> LINEAR | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ChecksumAggregationMethod" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ChecksumAggregationMethod" j)
    let to_json = simple_to_json to_value
  end
module StartSnapshotResponse =
  struct
    type nonrec t =
      {
      description: Description.t option
        [@ocaml.doc "The description of the snapshot."];
      snapshotId: SnapshotId.t option [@ocaml.doc "The ID of the snapshot."];
      ownerId: OwnerId.t option
        [@ocaml.doc
          "The Amazon Web Services account ID of the snapshot owner."];
      status: Status.t option [@ocaml.doc "The status of the snapshot."];
      startTime: TimeStamp.t option
        [@ocaml.doc "The timestamp when the snapshot was created."];
      volumeSize: VolumeSize.t option
        [@ocaml.doc "The size of the volume, in GiB."];
      blockSize: BlockSize.t option
        [@ocaml.doc "The size of the blocks in the snapshot, in bytes."];
      tags: Tags.t option
        [@ocaml.doc
          "The tags applied to the snapshot. You can specify up to 50 tags per snapshot. For more information, see Tagging your Amazon EC2 resources in the Amazon Elastic Compute Cloud User Guide."];
      parentSnapshotId: SnapshotId.t option
        [@ocaml.doc "The ID of the parent snapshot."];
      kmsKeyArn: KmsKeyArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Key Management Service (KMS) key used to encrypt the snapshot."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConcurrentLimitExceededException of
          ConcurrentLimitExceededException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `RequestThrottledException of RequestThrottledException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?description =
      fun ?snapshotId ->
        fun ?ownerId ->
          fun ?status ->
            fun ?startTime ->
              fun ?volumeSize ->
                fun ?blockSize ->
                  fun ?tags ->
                    fun ?parentSnapshotId ->
                      fun ?kmsKeyArn ->
                        fun () ->
                          {
                            description;
                            snapshotId;
                            ownerId;
                            status;
                            startTime;
                            volumeSize;
                            blockSize;
                            tags;
                            parentSnapshotId;
                            kmsKeyArn
                          }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConcurrentLimitExceededException" ->
          `ConcurrentLimitExceededException
            (ConcurrentLimitExceededException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "RequestThrottledException" ->
          `RequestThrottledException (RequestThrottledException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConcurrentLimitExceededException" ->
          `ConcurrentLimitExceededException
            (ConcurrentLimitExceededException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "RequestThrottledException" ->
          `RequestThrottledException (RequestThrottledException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConcurrentLimitExceededException e ->
          `Assoc
            [("error", (`String "ConcurrentLimitExceededException"));
            ("details", (ConcurrentLimitExceededException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `RequestThrottledException e ->
          `Assoc
            [("error", (`String "RequestThrottledException"));
            ("details", (RequestThrottledException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Description", (Option.map x.description ~f:Description.to_value));
        ("SnapshotId", (Option.map x.snapshotId ~f:SnapshotId.to_value));
        ("OwnerId", (Option.map x.ownerId ~f:OwnerId.to_value));
        ("Status", (Option.map x.status ~f:Status.to_value));
        ("StartTime", (Option.map x.startTime ~f:TimeStamp.to_value));
        ("VolumeSize", (Option.map x.volumeSize ~f:VolumeSize.to_value));
        ("BlockSize", (Option.map x.blockSize ~f:BlockSize.to_value));
        ("Tags", (Option.map x.tags ~f:Tags.to_value));
        ("ParentSnapshotId",
          (Option.map x.parentSnapshotId ~f:SnapshotId.to_value));
        ("KmsKeyArn", (Option.map x.kmsKeyArn ~f:KmsKeyArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let kmsKeyArn =
        (Option.map ~f:KmsKeyArn.of_xml) (Xml.child xml_arg0 "KmsKeyArn") in
      let parentSnapshotId =
        (Option.map ~f:SnapshotId.of_xml)
          (Xml.child xml_arg0 "ParentSnapshotId") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let blockSize =
        (Option.map ~f:BlockSize.of_xml) (Xml.child xml_arg0 "BlockSize") in
      let volumeSize =
        (Option.map ~f:VolumeSize.of_xml) (Xml.child xml_arg0 "VolumeSize") in
      let startTime =
        (Option.map ~f:TimeStamp.of_xml) (Xml.child xml_arg0 "StartTime") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "Status") in
      let ownerId =
        (Option.map ~f:OwnerId.of_xml) (Xml.child xml_arg0 "OwnerId") in
      let snapshotId =
        (Option.map ~f:SnapshotId.of_xml) (Xml.child xml_arg0 "SnapshotId") in
      let description =
        (Option.map ~f:Description.of_xml) (Xml.child xml_arg0 "Description") in
      make ?kmsKeyArn ?parentSnapshotId ?tags ?blockSize ?volumeSize
        ?startTime ?status ?ownerId ?snapshotId ?description ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let kmsKeyArn = field_map json "KmsKeyArn" KmsKeyArn.of_json in
      let parentSnapshotId =
        field_map json "ParentSnapshotId" SnapshotId.of_json in
      let tags = field_map json "Tags" Tags.of_json in
      let blockSize = field_map json "BlockSize" BlockSize.of_json in
      let volumeSize = field_map json "VolumeSize" VolumeSize.of_json in
      let startTime = field_map json "StartTime" TimeStamp.of_json in
      let status = field_map json "Status" Status.of_json in
      let ownerId = field_map json "OwnerId" OwnerId.of_json in
      let snapshotId = field_map json "SnapshotId" SnapshotId.of_json in
      let description = field_map json "Description" Description.of_json in
      make ?kmsKeyArn ?parentSnapshotId ?tags ?blockSize ?volumeSize
        ?startTime ?status ?ownerId ?snapshotId ?description ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new Amazon EBS snapshot. The new snapshot enters the pending state after the request completes. After creating the snapshot, use PutSnapshotBlock to write blocks of data to the snapshot."]
module StartSnapshotRequest =
  struct
    type nonrec t =
      {
      volumeSize: VolumeSize.t
        [@ocaml.doc
          "The size of the volume, in GiB. The maximum size is 65536 GiB (64 TiB)."];
      parentSnapshotId: SnapshotId.t option
        [@ocaml.doc
          "The ID of the parent snapshot. If there is no parent snapshot, or if you are creating the first snapshot for an on-premises volume, omit this parameter. You can't specify ParentSnapshotId and Encrypted in the same request. If you specify both parameters, the request fails with ValidationException. The encryption status of the snapshot depends on the values that you specify for Encrypted, KmsKeyArn, and ParentSnapshotId, and whether your Amazon Web Services account is enabled for encryption by default. For more information, see Using encryption in the Amazon Elastic Compute Cloud User Guide. If you specify an encrypted parent snapshot, you must have permission to use the KMS key that was used to encrypt the parent snapshot. For more information, see Permissions to use Key Management Service keys in the Amazon Elastic Compute Cloud User Guide."];
      tags: Tags.t option [@ocaml.doc "The tags to apply to the snapshot."];
      description: Description.t option
        [@ocaml.doc "A description for the snapshot."];
      clientToken: IdempotencyToken.t option
        [@ocaml.doc
          "A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. Idempotency ensures that an API request completes only once. With an idempotent request, if the original request completes successfully. The subsequent retries with the same client token return the result from the original successful request and they have no additional effect. If you do not specify a client token, one is automatically generated by the Amazon Web Services SDK. For more information, see Idempotency for StartSnapshot API in the Amazon Elastic Compute Cloud User Guide."];
      encrypted: Boolean.t option
        [@ocaml.doc
          "Indicates whether to encrypt the snapshot. You can't specify Encrypted and ParentSnapshotId in the same request. If you specify both parameters, the request fails with ValidationException. The encryption status of the snapshot depends on the values that you specify for Encrypted, KmsKeyArn, and ParentSnapshotId, and whether your Amazon Web Services account is enabled for encryption by default. For more information, see Using encryption in the Amazon Elastic Compute Cloud User Guide. To create an encrypted snapshot, you must have permission to use the KMS key. For more information, see Permissions to use Key Management Service keys in the Amazon Elastic Compute Cloud User Guide."];
      kmsKeyArn: KmsKeyArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Key Management Service (KMS) key to be used to encrypt the snapshot. The encryption status of the snapshot depends on the values that you specify for Encrypted, KmsKeyArn, and ParentSnapshotId, and whether your Amazon Web Services account is enabled for encryption by default. For more information, see Using encryption in the Amazon Elastic Compute Cloud User Guide. To create an encrypted snapshot, you must have permission to use the KMS key. For more information, see Permissions to use Key Management Service keys in the Amazon Elastic Compute Cloud User Guide."];
      timeout: Timeout.t option
        [@ocaml.doc
          "The amount of time (in minutes) after which the snapshot is automatically cancelled if: No blocks are written to the snapshot. The snapshot is not completed after writing the last block of data. If no value is specified, the timeout defaults to 60 minutes."]}
    let context_ = "StartSnapshotRequest"
    let make ?parentSnapshotId =
      fun ?tags ->
        fun ?description ->
          fun ?clientToken ->
            fun ?encrypted ->
              fun ?kmsKeyArn ->
                fun ?timeout ->
                  fun ~volumeSize ->
                    fun () ->
                      {
                        parentSnapshotId;
                        tags;
                        description;
                        clientToken;
                        encrypted;
                        kmsKeyArn;
                        timeout;
                        volumeSize
                      }
    let to_value x =
      structure_to_value
        [("VolumeSize", (Some (VolumeSize.to_value x.volumeSize)));
        ("ParentSnapshotId",
          (Option.map x.parentSnapshotId ~f:SnapshotId.to_value));
        ("Tags", (Option.map x.tags ~f:Tags.to_value));
        ("Description", (Option.map x.description ~f:Description.to_value));
        ("ClientToken",
          (Option.map x.clientToken ~f:IdempotencyToken.to_value));
        ("Encrypted", (Option.map x.encrypted ~f:Boolean.to_value));
        ("KmsKeyArn", (Option.map x.kmsKeyArn ~f:KmsKeyArn.to_value));
        ("Timeout", (Option.map x.timeout ~f:Timeout.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timeout =
        (Option.map ~f:Timeout.of_xml) (Xml.child xml_arg0 "Timeout") in
      let kmsKeyArn =
        (Option.map ~f:KmsKeyArn.of_xml) (Xml.child xml_arg0 "KmsKeyArn") in
      let encrypted =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Encrypted") in
      let clientToken =
        (Option.map ~f:IdempotencyToken.of_xml)
          (Xml.child xml_arg0 "ClientToken") in
      let description =
        (Option.map ~f:Description.of_xml) (Xml.child xml_arg0 "Description") in
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let parentSnapshotId =
        (Option.map ~f:SnapshotId.of_xml)
          (Xml.child xml_arg0 "ParentSnapshotId") in
      let volumeSize =
        VolumeSize.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VolumeSize") in
      make ?timeout ?kmsKeyArn ?encrypted ?clientToken ?description ?tags
        ?parentSnapshotId ~volumeSize ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let timeout = field_map json "Timeout" Timeout.of_json in
      let kmsKeyArn = field_map json "KmsKeyArn" KmsKeyArn.of_json in
      let encrypted = field_map json "Encrypted" Boolean.of_json in
      let clientToken = field_map json "ClientToken" IdempotencyToken.of_json in
      let description = field_map json "Description" Description.of_json in
      let tags = field_map json "Tags" Tags.of_json in
      let parentSnapshotId =
        field_map json "ParentSnapshotId" SnapshotId.of_json in
      let volumeSize = field_map_exn json "VolumeSize" VolumeSize.of_json in
      make ?timeout ?kmsKeyArn ?encrypted ?clientToken ?description ?tags
        ?parentSnapshotId ~volumeSize ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new Amazon EBS snapshot. The new snapshot enters the pending state after the request completes. After creating the snapshot, use PutSnapshotBlock to write blocks of data to the snapshot."]
module PutSnapshotBlockResponse =
  struct
    type nonrec t =
      {
      checksum: Checksum.t option
        [@ocaml.doc
          "The SHA256 checksum generated for the block data by Amazon EBS."];
      checksumAlgorithm: ChecksumAlgorithm.t option
        [@ocaml.doc
          "The algorithm used by Amazon EBS to generate the checksum."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `RequestThrottledException of RequestThrottledException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?checksum =
      fun ?checksumAlgorithm -> fun () -> { checksum; checksumAlgorithm }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "RequestThrottledException" ->
          `RequestThrottledException (RequestThrottledException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "RequestThrottledException" ->
          `RequestThrottledException (RequestThrottledException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `RequestThrottledException e ->
          `Assoc
            [("error", (`String "RequestThrottledException"));
            ("details", (RequestThrottledException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body =
      ((fun (xs, pipe) ->
          make
            ?checksum:(Option.map
                         ((List.Assoc.find ~equal:String.Caseless.equal) xs
                            "x-amz-Checksum") ~f:Checksum.of_string)
            ?checksumAlgorithm:(Option.map
                                  ((List.Assoc.find
                                      ~equal:String.Caseless.equal) xs
                                     "x-amz-Checksum-Algorithm")
                                  ~f:ChecksumAlgorithm.of_string) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("x-amz-Checksum", (Option.map x.checksum ~f:Checksum.to_value));
        ("x-amz-Checksum-Algorithm",
          (Option.map x.checksumAlgorithm ~f:ChecksumAlgorithm.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let checksumAlgorithm =
        (Option.map ~f:ChecksumAlgorithm.of_xml)
          (Xml.child xml_arg0 "x-amz-Checksum-Algorithm") in
      let checksum =
        (Option.map ~f:Checksum.of_xml) (Xml.child xml_arg0 "x-amz-Checksum") in
      make ?checksumAlgorithm ?checksum ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let checksumAlgorithm =
        field_map json "ChecksumAlgorithm" ChecksumAlgorithm.of_json in
      let checksum = field_map json "Checksum" Checksum.of_json in
      make ?checksumAlgorithm ?checksum ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Writes a block of data to a snapshot. If the specified block contains data, the existing data is overwritten. The target snapshot must be in the pending state. Data written to a snapshot must be aligned with 512-KiB sectors."]
module PutSnapshotBlockRequest =
  struct
    type nonrec t =
      {
      snapshotId: SnapshotId.t
        [@ocaml.doc
          "The ID of the snapshot. If the specified snapshot is encrypted, you must have permission to use the KMS key that was used to encrypt the snapshot. For more information, see Using encryption in the Amazon Elastic Compute Cloud User Guide.."];
      blockIndex: BlockIndex.t
        [@ocaml.doc
          "The block index of the block in which to write the data. A block index is a logical index in units of 512 KiB blocks. To identify the block index, divide the logical offset of the data in the logical volume by the block size (logical offset of data/524288). The logical offset of the data must be 512 KiB aligned."];
      blockData: BlockData.t
        [@ocaml.doc
          "The data to write to the block. The block data is not signed as part of the Signature Version 4 signing process. As a result, you must generate and provide a Base64-encoded SHA256 checksum for the block data using the x-amz-Checksum header. Also, you must specify the checksum algorithm using the x-amz-Checksum-Algorithm header. The checksum that you provide is part of the Signature Version 4 signing process. It is validated against a checksum generated by Amazon EBS to ensure the validity and authenticity of the data. If the checksums do not correspond, the request fails. For more information, see Using checksums with the EBS direct APIs in the Amazon Elastic Compute Cloud User Guide."];
      dataLength: DataLength.t
        [@ocaml.doc
          "The size of the data to write to the block, in bytes. Currently, the only supported size is 524288 bytes. Valid values: 524288"];
      progress: Progress.t option
        [@ocaml.doc "The progress of the write process, as a percentage."];
      checksum: Checksum.t
        [@ocaml.doc
          "A Base64-encoded SHA256 checksum of the data. Only SHA256 checksums are supported."];
      checksumAlgorithm: ChecksumAlgorithm.t
        [@ocaml.doc
          "The algorithm used to generate the checksum. Currently, the only supported algorithm is SHA256."]}
    let context_ = "PutSnapshotBlockRequest"
    let make ?progress =
      fun ~snapshotId ->
        fun ~blockIndex ->
          fun ~blockData ->
            fun ~dataLength ->
              fun ~checksum ->
                fun ~checksumAlgorithm ->
                  fun () ->
                    {
                      progress;
                      snapshotId;
                      blockIndex;
                      blockData;
                      dataLength;
                      checksum;
                      checksumAlgorithm
                    }
    let of_header_and_body =
      ((fun (xs, pipe) ->
          make
            ~snapshotId:(SnapshotId.of_string
                           ((List.Assoc.find_exn ~equal:String.Caseless.equal)
                              xs "snapshotId"))
            ~blockIndex:(BlockIndex.of_string
                           ((List.Assoc.find_exn ~equal:String.Caseless.equal)
                              xs "blockIndex")) ~blockData:pipe
            ~dataLength:(DataLength.of_string
                           ((List.Assoc.find_exn ~equal:String.Caseless.equal)
                              xs "x-amz-Data-Length"))
            ?progress:(Option.map
                         ((List.Assoc.find ~equal:String.Caseless.equal) xs
                            "x-amz-Progress") ~f:Progress.of_string)
            ~checksum:(Checksum.of_string
                         ((List.Assoc.find_exn ~equal:String.Caseless.equal)
                            xs "x-amz-Checksum"))
            ~checksumAlgorithm:(ChecksumAlgorithm.of_string
                                  ((List.Assoc.find_exn
                                      ~equal:String.Caseless.equal) xs
                                     "x-amz-Checksum-Algorithm")) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("snapshotId", (Some (SnapshotId.to_value x.snapshotId)));
        ("blockIndex", (Some (BlockIndex.to_value x.blockIndex)));
        ("BlockData", (Some (BlockData.to_value x.blockData)));
        ("x-amz-Data-Length", (Some (DataLength.to_value x.dataLength)));
        ("x-amz-Progress", (Option.map x.progress ~f:Progress.to_value));
        ("x-amz-Checksum", (Some (Checksum.to_value x.checksum)));
        ("x-amz-Checksum-Algorithm",
          (Some (ChecksumAlgorithm.to_value x.checksumAlgorithm)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let checksumAlgorithm =
        ChecksumAlgorithm.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "x-amz-Checksum-Algorithm") in
      let checksum =
        Checksum.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "x-amz-Checksum") in
      let progress =
        (Option.map ~f:Progress.of_xml) (Xml.child xml_arg0 "x-amz-Progress") in
      let dataLength =
        DataLength.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "x-amz-Data-Length") in
      let blockData =
        BlockData.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "BlockData") in
      let blockIndex =
        BlockIndex.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "blockIndex") in
      let snapshotId =
        SnapshotId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "snapshotId") in
      make ~checksumAlgorithm ~checksum ?progress ~dataLength ~blockData
        ~blockIndex ~snapshotId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let checksumAlgorithm =
        field_map_exn json "ChecksumAlgorithm" ChecksumAlgorithm.of_json in
      let checksum = field_map_exn json "Checksum" Checksum.of_json in
      let progress = field_map json "Progress" Progress.of_json in
      let dataLength = field_map_exn json "DataLength" DataLength.of_json in
      let blockData = field_map_exn json "BlockData" BlockData.of_json in
      let blockIndex = field_map_exn json "BlockIndex" BlockIndex.of_json in
      let snapshotId = field_map_exn json "SnapshotId" SnapshotId.of_json in
      make ~checksumAlgorithm ~checksum ?progress ~dataLength ~blockData
        ~blockIndex ~snapshotId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Writes a block of data to a snapshot. If the specified block contains data, the existing data is overwritten. The target snapshot must be in the pending state. Data written to a snapshot must be aligned with 512-KiB sectors."]
module ListSnapshotBlocksResponse =
  struct
    type nonrec t =
      {
      blocks: Blocks.t option
        [@ocaml.doc
          "An array of objects containing information about the blocks."];
      expiryTime: TimeStamp.t option
        [@ocaml.doc "The time when the BlockToken expires."];
      volumeSize: VolumeSize.t option
        [@ocaml.doc "The size of the volume in GB."];
      blockSize: BlockSize.t option
        [@ocaml.doc "The size of the blocks in the snapshot, in bytes."];
      nextToken: PageToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `RequestThrottledException of RequestThrottledException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?blocks =
      fun ?expiryTime ->
        fun ?volumeSize ->
          fun ?blockSize ->
            fun ?nextToken ->
              fun () ->
                { blocks; expiryTime; volumeSize; blockSize; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "RequestThrottledException" ->
          `RequestThrottledException (RequestThrottledException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "RequestThrottledException" ->
          `RequestThrottledException (RequestThrottledException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `RequestThrottledException e ->
          `Assoc
            [("error", (`String "RequestThrottledException"));
            ("details", (RequestThrottledException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Blocks", (Option.map x.blocks ~f:Blocks.to_value));
        ("ExpiryTime", (Option.map x.expiryTime ~f:TimeStamp.to_value));
        ("VolumeSize", (Option.map x.volumeSize ~f:VolumeSize.to_value));
        ("BlockSize", (Option.map x.blockSize ~f:BlockSize.to_value));
        ("NextToken", (Option.map x.nextToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let blockSize =
        (Option.map ~f:BlockSize.of_xml) (Xml.child xml_arg0 "BlockSize") in
      let volumeSize =
        (Option.map ~f:VolumeSize.of_xml) (Xml.child xml_arg0 "VolumeSize") in
      let expiryTime =
        (Option.map ~f:TimeStamp.of_xml) (Xml.child xml_arg0 "ExpiryTime") in
      let blocks =
        (Option.map ~f:Blocks.of_xml) (Xml.child xml_arg0 "Blocks") in
      make ?nextToken ?blockSize ?volumeSize ?expiryTime ?blocks ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" PageToken.of_json in
      let blockSize = field_map json "BlockSize" BlockSize.of_json in
      let volumeSize = field_map json "VolumeSize" VolumeSize.of_json in
      let expiryTime = field_map json "ExpiryTime" TimeStamp.of_json in
      let blocks = field_map json "Blocks" Blocks.of_json in
      make ?nextToken ?blockSize ?volumeSize ?expiryTime ?blocks ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about the blocks in an Amazon Elastic Block Store snapshot."]
module ListSnapshotBlocksRequest =
  struct
    type nonrec t =
      {
      snapshotId: SnapshotId.t
        [@ocaml.doc
          "The ID of the snapshot from which to get block indexes and block tokens."];
      nextToken: PageToken.t option
        [@ocaml.doc
          "The token to request the next page of results. If you specify NextToken, then StartingBlockIndex is ignored."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of blocks to be returned by the request. Even if additional blocks can be retrieved from the snapshot, the request can return less blocks than MaxResults or an empty array of blocks. To retrieve the next set of blocks from the snapshot, make another request with the returned NextToken value. The value of NextToken is null when there are no more blocks to return."];
      startingBlockIndex: BlockIndex.t option
        [@ocaml.doc
          "The block index from which the list should start. The list in the response will start from this block index or the next valid block index in the snapshot. If you specify NextToken, then StartingBlockIndex is ignored."]}
    let context_ = "ListSnapshotBlocksRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ?startingBlockIndex ->
          fun ~snapshotId ->
            fun () ->
              { nextToken; maxResults; startingBlockIndex; snapshotId }
    let to_value x =
      structure_to_value
        [("snapshotId", (Some (SnapshotId.to_value x.snapshotId)));
        ("pageToken", (Option.map x.nextToken ~f:PageToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("startingBlockIndex",
          (Option.map x.startingBlockIndex ~f:BlockIndex.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let startingBlockIndex =
        (Option.map ~f:BlockIndex.of_xml)
          (Xml.child xml_arg0 "startingBlockIndex") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "pageToken") in
      let snapshotId =
        SnapshotId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "snapshotId") in
      make ?startingBlockIndex ?maxResults ?nextToken ~snapshotId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let startingBlockIndex =
        field_map json "StartingBlockIndex" BlockIndex.of_json in
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" PageToken.of_json in
      let snapshotId = field_map_exn json "SnapshotId" SnapshotId.of_json in
      make ?startingBlockIndex ?maxResults ?nextToken ~snapshotId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about the blocks in an Amazon Elastic Block Store snapshot."]
module ListChangedBlocksResponse =
  struct
    type nonrec t =
      {
      changedBlocks: ChangedBlocks.t option
        [@ocaml.doc
          "An array of objects containing information about the changed blocks."];
      expiryTime: TimeStamp.t option
        [@ocaml.doc "The time when the BlockToken expires."];
      volumeSize: VolumeSize.t option
        [@ocaml.doc "The size of the volume in GB."];
      blockSize: BlockSize.t option
        [@ocaml.doc "The size of the blocks in the snapshot, in bytes."];
      nextToken: PageToken.t option
        [@ocaml.doc
          "The token to use to retrieve the next page of results. This value is null when there are no more results to return."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `RequestThrottledException of RequestThrottledException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?changedBlocks =
      fun ?expiryTime ->
        fun ?volumeSize ->
          fun ?blockSize ->
            fun ?nextToken ->
              fun () ->
                { changedBlocks; expiryTime; volumeSize; blockSize; nextToken
                }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "RequestThrottledException" ->
          `RequestThrottledException (RequestThrottledException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "RequestThrottledException" ->
          `RequestThrottledException (RequestThrottledException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `RequestThrottledException e ->
          `Assoc
            [("error", (`String "RequestThrottledException"));
            ("details", (RequestThrottledException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ChangedBlocks",
           (Option.map x.changedBlocks ~f:ChangedBlocks.to_value));
        ("ExpiryTime", (Option.map x.expiryTime ~f:TimeStamp.to_value));
        ("VolumeSize", (Option.map x.volumeSize ~f:VolumeSize.to_value));
        ("BlockSize", (Option.map x.blockSize ~f:BlockSize.to_value));
        ("NextToken", (Option.map x.nextToken ~f:PageToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let blockSize =
        (Option.map ~f:BlockSize.of_xml) (Xml.child xml_arg0 "BlockSize") in
      let volumeSize =
        (Option.map ~f:VolumeSize.of_xml) (Xml.child xml_arg0 "VolumeSize") in
      let expiryTime =
        (Option.map ~f:TimeStamp.of_xml) (Xml.child xml_arg0 "ExpiryTime") in
      let changedBlocks =
        (Option.map ~f:ChangedBlocks.of_xml)
          (Xml.child xml_arg0 "ChangedBlocks") in
      make ?nextToken ?blockSize ?volumeSize ?expiryTime ?changedBlocks ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" PageToken.of_json in
      let blockSize = field_map json "BlockSize" BlockSize.of_json in
      let volumeSize = field_map json "VolumeSize" VolumeSize.of_json in
      let expiryTime = field_map json "ExpiryTime" TimeStamp.of_json in
      let changedBlocks =
        field_map json "ChangedBlocks" ChangedBlocks.of_json in
      make ?nextToken ?blockSize ?volumeSize ?expiryTime ?changedBlocks ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about the blocks that are different between two Amazon Elastic Block Store snapshots of the same volume/snapshot lineage."]
module ListChangedBlocksRequest =
  struct
    type nonrec t =
      {
      firstSnapshotId: SnapshotId.t option
        [@ocaml.doc
          "The ID of the first snapshot to use for the comparison. The FirstSnapshotID parameter must be specified with a SecondSnapshotId parameter; otherwise, an error occurs."];
      secondSnapshotId: SnapshotId.t
        [@ocaml.doc
          "The ID of the second snapshot to use for the comparison. The SecondSnapshotId parameter must be specified with a FirstSnapshotID parameter; otherwise, an error occurs."];
      nextToken: PageToken.t option
        [@ocaml.doc
          "The token to request the next page of results. If you specify NextToken, then StartingBlockIndex is ignored."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of blocks to be returned by the request. Even if additional blocks can be retrieved from the snapshot, the request can return less blocks than MaxResults or an empty array of blocks. To retrieve the next set of blocks from the snapshot, make another request with the returned NextToken value. The value of NextToken is null when there are no more blocks to return."];
      startingBlockIndex: BlockIndex.t option
        [@ocaml.doc
          "The block index from which the comparison should start. The list in the response will start from this block index or the next valid block index in the snapshots. If you specify NextToken, then StartingBlockIndex is ignored."]}
    let context_ = "ListChangedBlocksRequest"
    let make ?firstSnapshotId =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ?startingBlockIndex ->
            fun ~secondSnapshotId ->
              fun () ->
                {
                  firstSnapshotId;
                  nextToken;
                  maxResults;
                  startingBlockIndex;
                  secondSnapshotId
                }
    let to_value x =
      structure_to_value
        [("firstSnapshotId",
           (Option.map x.firstSnapshotId ~f:SnapshotId.to_value));
        ("secondSnapshotId", (Some (SnapshotId.to_value x.secondSnapshotId)));
        ("pageToken", (Option.map x.nextToken ~f:PageToken.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("startingBlockIndex",
          (Option.map x.startingBlockIndex ~f:BlockIndex.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let startingBlockIndex =
        (Option.map ~f:BlockIndex.of_xml)
          (Xml.child xml_arg0 "startingBlockIndex") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:PageToken.of_xml) (Xml.child xml_arg0 "pageToken") in
      let secondSnapshotId =
        SnapshotId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "secondSnapshotId") in
      let firstSnapshotId =
        (Option.map ~f:SnapshotId.of_xml)
          (Xml.child xml_arg0 "firstSnapshotId") in
      make ?startingBlockIndex ?maxResults ?nextToken ~secondSnapshotId
        ?firstSnapshotId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let startingBlockIndex =
        field_map json "StartingBlockIndex" BlockIndex.of_json in
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" PageToken.of_json in
      let secondSnapshotId =
        field_map_exn json "SecondSnapshotId" SnapshotId.of_json in
      let firstSnapshotId =
        field_map json "FirstSnapshotId" SnapshotId.of_json in
      make ?startingBlockIndex ?maxResults ?nextToken ~secondSnapshotId
        ?firstSnapshotId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about the blocks that are different between two Amazon Elastic Block Store snapshots of the same volume/snapshot lineage."]
module GetSnapshotBlockResponse =
  struct
    type nonrec t =
      {
      dataLength: DataLength.t option
        [@ocaml.doc "The size of the data in the block."];
      blockData: BlockData.t option
        [@ocaml.doc "The data content of the block."];
      checksum: Checksum.t option
        [@ocaml.doc
          "The checksum generated for the block, which is Base64 encoded."];
      checksumAlgorithm: ChecksumAlgorithm.t option
        [@ocaml.doc
          "The algorithm used to generate the checksum for the block, such as SHA256."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `RequestThrottledException of RequestThrottledException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?dataLength =
      fun ?blockData ->
        fun ?checksum ->
          fun ?checksumAlgorithm ->
            fun () -> { dataLength; blockData; checksum; checksumAlgorithm }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "RequestThrottledException" ->
          `RequestThrottledException (RequestThrottledException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "RequestThrottledException" ->
          `RequestThrottledException (RequestThrottledException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `RequestThrottledException e ->
          `Assoc
            [("error", (`String "RequestThrottledException"));
            ("details", (RequestThrottledException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body =
      ((fun (xs, pipe) ->
          make
            ?dataLength:(Option.map
                           ((List.Assoc.find ~equal:String.Caseless.equal) xs
                              "x-amz-Data-Length") ~f:DataLength.of_string)
            ?blockData:(Some pipe)
            ?checksum:(Option.map
                         ((List.Assoc.find ~equal:String.Caseless.equal) xs
                            "x-amz-Checksum") ~f:Checksum.of_string)
            ?checksumAlgorithm:(Option.map
                                  ((List.Assoc.find
                                      ~equal:String.Caseless.equal) xs
                                     "x-amz-Checksum-Algorithm")
                                  ~f:ChecksumAlgorithm.of_string) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("x-amz-Data-Length",
           (Option.map x.dataLength ~f:DataLength.to_value));
        ("BlockData", (Option.map x.blockData ~f:BlockData.to_value));
        ("x-amz-Checksum", (Option.map x.checksum ~f:Checksum.to_value));
        ("x-amz-Checksum-Algorithm",
          (Option.map x.checksumAlgorithm ~f:ChecksumAlgorithm.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let checksumAlgorithm =
        (Option.map ~f:ChecksumAlgorithm.of_xml)
          (Xml.child xml_arg0 "x-amz-Checksum-Algorithm") in
      let checksum =
        (Option.map ~f:Checksum.of_xml) (Xml.child xml_arg0 "x-amz-Checksum") in
      let blockData =
        (Option.map ~f:BlockData.of_xml) (Xml.child xml_arg0 "BlockData") in
      let dataLength =
        (Option.map ~f:DataLength.of_xml)
          (Xml.child xml_arg0 "x-amz-Data-Length") in
      make ?checksumAlgorithm ?checksum ?blockData ?dataLength ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let checksumAlgorithm =
        field_map json "ChecksumAlgorithm" ChecksumAlgorithm.of_json in
      let checksum = field_map json "Checksum" Checksum.of_json in
      let blockData = field_map json "BlockData" BlockData.of_json in
      let dataLength = field_map json "DataLength" DataLength.of_json in
      make ?checksumAlgorithm ?checksum ?blockData ?dataLength ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the data in a block in an Amazon Elastic Block Store snapshot."]
module GetSnapshotBlockRequest =
  struct
    type nonrec t =
      {
      snapshotId: SnapshotId.t
        [@ocaml.doc
          "The ID of the snapshot containing the block from which to get data. If the specified snapshot is encrypted, you must have permission to use the KMS key that was used to encrypt the snapshot. For more information, see Using encryption in the Amazon Elastic Compute Cloud User Guide."];
      blockIndex: BlockIndex.t
        [@ocaml.doc
          "The block index of the block in which to read the data. A block index is a logical index in units of 512 KiB blocks. To identify the block index, divide the logical offset of the data in the logical volume by the block size (logical offset of data/524288). The logical offset of the data must be 512 KiB aligned."];
      blockToken: BlockToken.t
        [@ocaml.doc
          "The block token of the block from which to get data. You can obtain the BlockToken by running the ListChangedBlocks or ListSnapshotBlocks operations."]}
    let context_ = "GetSnapshotBlockRequest"
    let make ~snapshotId =
      fun ~blockIndex ->
        fun ~blockToken -> fun () -> { snapshotId; blockIndex; blockToken }
    let to_value x =
      structure_to_value
        [("snapshotId", (Some (SnapshotId.to_value x.snapshotId)));
        ("blockIndex", (Some (BlockIndex.to_value x.blockIndex)));
        ("blockToken", (Some (BlockToken.to_value x.blockToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let blockToken =
        BlockToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "blockToken") in
      let blockIndex =
        BlockIndex.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "blockIndex") in
      let snapshotId =
        SnapshotId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "snapshotId") in
      make ~blockToken ~blockIndex ~snapshotId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let blockToken = field_map_exn json "BlockToken" BlockToken.of_json in
      let blockIndex = field_map_exn json "BlockIndex" BlockIndex.of_json in
      let snapshotId = field_map_exn json "SnapshotId" SnapshotId.of_json in
      make ~blockToken ~blockIndex ~snapshotId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the data in a block in an Amazon Elastic Block Store snapshot."]
module CompleteSnapshotResponse =
  struct
    type nonrec t =
      {
      status: Status.t option [@ocaml.doc "The status of the snapshot."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `RequestThrottledException of RequestThrottledException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?status = fun () -> { status }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "RequestThrottledException" ->
          `RequestThrottledException (RequestThrottledException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "RequestThrottledException" ->
          `RequestThrottledException (RequestThrottledException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `RequestThrottledException e ->
          `Assoc
            [("error", (`String "RequestThrottledException"));
            ("details", (RequestThrottledException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Status", (Option.map x.status ~f:Status.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "Status") in
      make ?status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let status = field_map json "Status" Status.of_json in make ?status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Seals and completes the snapshot after all of the required blocks of data have been written to it. Completing the snapshot changes the status to completed. You cannot write new blocks to a snapshot after it has been completed."]
module CompleteSnapshotRequest =
  struct
    type nonrec t =
      {
      snapshotId: SnapshotId.t [@ocaml.doc "The ID of the snapshot."];
      changedBlocksCount: ChangedBlocksCount.t
        [@ocaml.doc
          "The number of blocks that were written to the snapshot."];
      checksum: Checksum.t option
        [@ocaml.doc
          "An aggregated Base-64 SHA256 checksum based on the checksums of each written block. To generate the aggregated checksum using the linear aggregation method, arrange the checksums for each written block in ascending order of their block index, concatenate them to form a single string, and then generate the checksum on the entire string using the SHA256 algorithm."];
      checksumAlgorithm: ChecksumAlgorithm.t option
        [@ocaml.doc
          "The algorithm used to generate the checksum. Currently, the only supported algorithm is SHA256."];
      checksumAggregationMethod: ChecksumAggregationMethod.t option
        [@ocaml.doc
          "The aggregation method used to generate the checksum. Currently, the only supported aggregation method is LINEAR."]}
    let context_ = "CompleteSnapshotRequest"
    let make ?checksum =
      fun ?checksumAlgorithm ->
        fun ?checksumAggregationMethod ->
          fun ~snapshotId ->
            fun ~changedBlocksCount ->
              fun () ->
                {
                  checksum;
                  checksumAlgorithm;
                  checksumAggregationMethod;
                  snapshotId;
                  changedBlocksCount
                }
    let to_value x =
      structure_to_value
        [("snapshotId", (Some (SnapshotId.to_value x.snapshotId)));
        ("x-amz-ChangedBlocksCount",
          (Some (ChangedBlocksCount.to_value x.changedBlocksCount)));
        ("x-amz-Checksum", (Option.map x.checksum ~f:Checksum.to_value));
        ("x-amz-Checksum-Algorithm",
          (Option.map x.checksumAlgorithm ~f:ChecksumAlgorithm.to_value));
        ("x-amz-Checksum-Aggregation-Method",
          (Option.map x.checksumAggregationMethod
             ~f:ChecksumAggregationMethod.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let checksumAggregationMethod =
        (Option.map ~f:ChecksumAggregationMethod.of_xml)
          (Xml.child xml_arg0 "x-amz-Checksum-Aggregation-Method") in
      let checksumAlgorithm =
        (Option.map ~f:ChecksumAlgorithm.of_xml)
          (Xml.child xml_arg0 "x-amz-Checksum-Algorithm") in
      let checksum =
        (Option.map ~f:Checksum.of_xml) (Xml.child xml_arg0 "x-amz-Checksum") in
      let changedBlocksCount =
        ChangedBlocksCount.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "x-amz-ChangedBlocksCount") in
      let snapshotId =
        SnapshotId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "snapshotId") in
      make ?checksumAggregationMethod ?checksumAlgorithm ?checksum
        ~changedBlocksCount ~snapshotId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let checksumAggregationMethod =
        field_map json "ChecksumAggregationMethod"
          ChecksumAggregationMethod.of_json in
      let checksumAlgorithm =
        field_map json "ChecksumAlgorithm" ChecksumAlgorithm.of_json in
      let checksum = field_map json "Checksum" Checksum.of_json in
      let changedBlocksCount =
        field_map_exn json "ChangedBlocksCount" ChangedBlocksCount.of_json in
      let snapshotId = field_map_exn json "SnapshotId" SnapshotId.of_json in
      make ?checksumAggregationMethod ?checksumAlgorithm ?checksum
        ~changedBlocksCount ~snapshotId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Seals and completes the snapshot after all of the required blocks of data have been written to it. Completing the snapshot changes the status to completed. You cannot write new blocks to a snapshot after it has been completed."]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let clone_backend =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING __string"
       and backendEnvironmentName =
         flag "backend-environment-name" (required string)
           ~doc:"STRING __string"
       and targetEnvironmentName =
         flag "target-environment-name" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.clone_backend
           (Values.CloneBackendRequest.make ~appId ~backendEnvironmentName
              ~targetEnvironmentName ())
           (Some Values.CloneBackendResponse.to_json)
           (Some Values.CloneBackendResponse.error_to_json)])
let create_backend =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceConfig =
         flag "resource-config" (optional json_arg)
           ~doc:"JSON ResourceConfig"
       and resourceName =
         flag "resource-name" (optional string) ~doc:"STRING __string"
       and appId = flag "app-id" (required string) ~doc:"STRING __string"
       and appName = flag "app-name" (required string) ~doc:"STRING __string"
       and backendEnvironmentName =
         flag "backend-environment-name" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_backend
           (Values.CreateBackendRequest.make
              ?resourceConfig:(Option.map ~f:Values.ResourceConfig.of_json
                                 resourceConfig) ?resourceName ~appId
              ~appName ~backendEnvironmentName ())
           (Some Values.CreateBackendResponse.to_json)
           (Some Values.CreateBackendResponse.error_to_json)])
let create_backend_a_p_i =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING __string"
       and backendEnvironmentName =
         flag "backend-environment-name" (required string)
           ~doc:"STRING __string"
       and resourceConfig =
         flag "resource-config" (required json_arg)
           ~doc:"JSON BackendAPIResourceConfig"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_backend_a_p_i
           (Values.CreateBackendAPIRequest.make ~appId
              ~backendEnvironmentName
              ~resourceConfig:(Values.BackendAPIResourceConfig.of_json
                                 resourceConfig) ~resourceName ())
           (Some Values.CreateBackendAPIResponse.to_json)
           (Some Values.CreateBackendAPIResponse.error_to_json)])
let create_backend_auth =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING __string"
       and backendEnvironmentName =
         flag "backend-environment-name" (required string)
           ~doc:"STRING __string"
       and resourceConfig =
         flag "resource-config" (required json_arg)
           ~doc:"JSON CreateBackendAuthResourceConfig"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_backend_auth
           (Values.CreateBackendAuthRequest.make ~appId
              ~backendEnvironmentName
              ~resourceConfig:(Values.CreateBackendAuthResourceConfig.of_json
                                 resourceConfig) ~resourceName ())
           (Some Values.CreateBackendAuthResponse.to_json)
           (Some Values.CreateBackendAuthResponse.error_to_json)])
let create_backend_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and backendManagerAppId =
         flag "backend-manager-app-id" (optional string)
           ~doc:"STRING __string"
       and appId = flag "app-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_backend_config
           (Values.CreateBackendConfigRequest.make ?backendManagerAppId
              ~appId ()) (Some Values.CreateBackendConfigResponse.to_json)
           (Some Values.CreateBackendConfigResponse.error_to_json)])
let create_backend_storage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING __string"
       and backendEnvironmentName =
         flag "backend-environment-name" (required string)
           ~doc:"STRING __string"
       and resourceConfig =
         flag "resource-config" (required json_arg)
           ~doc:"JSON CreateBackendStorageResourceConfig"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_backend_storage
           (Values.CreateBackendStorageRequest.make ~appId
              ~backendEnvironmentName
              ~resourceConfig:(Values.CreateBackendStorageResourceConfig.of_json
                                 resourceConfig) ~resourceName ())
           (Some Values.CreateBackendStorageResponse.to_json)
           (Some Values.CreateBackendStorageResponse.error_to_json)])
let create_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_token (Values.CreateTokenRequest.make ~appId ())
           (Some Values.CreateTokenResponse.to_json)
           (Some Values.CreateTokenResponse.error_to_json)])
let delete_backend =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING __string"
       and backendEnvironmentName =
         flag "backend-environment-name" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_backend
           (Values.DeleteBackendRequest.make ~appId ~backendEnvironmentName
              ()) (Some Values.DeleteBackendResponse.to_json)
           (Some Values.DeleteBackendResponse.error_to_json)])
let delete_backend_a_p_i =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceConfig =
         flag "resource-config" (optional json_arg)
           ~doc:"JSON BackendAPIResourceConfig"
       and appId = flag "app-id" (required string) ~doc:"STRING __string"
       and backendEnvironmentName =
         flag "backend-environment-name" (required string)
           ~doc:"STRING __string"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_backend_a_p_i
           (Values.DeleteBackendAPIRequest.make
              ?resourceConfig:(Option.map
                                 ~f:Values.BackendAPIResourceConfig.of_json
                                 resourceConfig) ~appId
              ~backendEnvironmentName ~resourceName ())
           (Some Values.DeleteBackendAPIResponse.to_json)
           (Some Values.DeleteBackendAPIResponse.error_to_json)])
let delete_backend_auth =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING __string"
       and backendEnvironmentName =
         flag "backend-environment-name" (required string)
           ~doc:"STRING __string"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_backend_auth
           (Values.DeleteBackendAuthRequest.make ~appId
              ~backendEnvironmentName ~resourceName ())
           (Some Values.DeleteBackendAuthResponse.to_json)
           (Some Values.DeleteBackendAuthResponse.error_to_json)])
let delete_backend_storage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING __string"
       and backendEnvironmentName =
         flag "backend-environment-name" (required string)
           ~doc:"STRING __string"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING __string"
       and serviceName =
         flag "service-name" (required json_arg) ~doc:"JSON ServiceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_backend_storage
           (Values.DeleteBackendStorageRequest.make ~appId
              ~backendEnvironmentName ~resourceName
              ~serviceName:(Values.ServiceName.of_json serviceName) ())
           (Some Values.DeleteBackendStorageResponse.to_json)
           (Some Values.DeleteBackendStorageResponse.error_to_json)])
let delete_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING __string"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_token
           (Values.DeleteTokenRequest.make ~appId ~sessionId ())
           (Some Values.DeleteTokenResponse.to_json)
           (Some Values.DeleteTokenResponse.error_to_json)])
let generate_backend_a_p_i_models =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING __string"
       and backendEnvironmentName =
         flag "backend-environment-name" (required string)
           ~doc:"STRING __string"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.generate_backend_a_p_i_models
           (Values.GenerateBackendAPIModelsRequest.make ~appId
              ~backendEnvironmentName ~resourceName ())
           (Some Values.GenerateBackendAPIModelsResponse.to_json)
           (Some Values.GenerateBackendAPIModelsResponse.error_to_json)])
let get_backend =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and backendEnvironmentName =
         flag "backend-environment-name" (optional string)
           ~doc:"STRING __string"
       and appId = flag "app-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_backend
           (Values.GetBackendRequest.make ?backendEnvironmentName ~appId ())
           (Some Values.GetBackendResponse.to_json)
           (Some Values.GetBackendResponse.error_to_json)])
let get_backend_a_p_i =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceConfig =
         flag "resource-config" (optional json_arg)
           ~doc:"JSON BackendAPIResourceConfig"
       and appId = flag "app-id" (required string) ~doc:"STRING __string"
       and backendEnvironmentName =
         flag "backend-environment-name" (required string)
           ~doc:"STRING __string"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_backend_a_p_i
           (Values.GetBackendAPIRequest.make
              ?resourceConfig:(Option.map
                                 ~f:Values.BackendAPIResourceConfig.of_json
                                 resourceConfig) ~appId
              ~backendEnvironmentName ~resourceName ())
           (Some Values.GetBackendAPIResponse.to_json)
           (Some Values.GetBackendAPIResponse.error_to_json)])
let get_backend_a_p_i_models =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING __string"
       and backendEnvironmentName =
         flag "backend-environment-name" (required string)
           ~doc:"STRING __string"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_backend_a_p_i_models
           (Values.GetBackendAPIModelsRequest.make ~appId
              ~backendEnvironmentName ~resourceName ())
           (Some Values.GetBackendAPIModelsResponse.to_json)
           (Some Values.GetBackendAPIModelsResponse.error_to_json)])
let get_backend_auth =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING __string"
       and backendEnvironmentName =
         flag "backend-environment-name" (required string)
           ~doc:"STRING __string"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_backend_auth
           (Values.GetBackendAuthRequest.make ~appId ~backendEnvironmentName
              ~resourceName ()) (Some Values.GetBackendAuthResponse.to_json)
           (Some Values.GetBackendAuthResponse.error_to_json)])
let get_backend_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING __string"
       and backendEnvironmentName =
         flag "backend-environment-name" (required string)
           ~doc:"STRING __string"
       and jobId = flag "job-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_backend_job
           (Values.GetBackendJobRequest.make ~appId ~backendEnvironmentName
              ~jobId ()) (Some Values.GetBackendJobResponse.to_json)
           (Some Values.GetBackendJobResponse.error_to_json)])
let get_backend_storage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING __string"
       and backendEnvironmentName =
         flag "backend-environment-name" (required string)
           ~doc:"STRING __string"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_backend_storage
           (Values.GetBackendStorageRequest.make ~appId
              ~backendEnvironmentName ~resourceName ())
           (Some Values.GetBackendStorageResponse.to_json)
           (Some Values.GetBackendStorageResponse.error_to_json)])
let get_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING __string"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_token (Values.GetTokenRequest.make ~appId ~sessionId ())
           (Some Values.GetTokenResponse.to_json)
           (Some Values.GetTokenResponse.error_to_json)])
let import_backend_auth =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityPoolId =
         flag "identity-pool-id" (optional string) ~doc:"STRING __string"
       and appId = flag "app-id" (required string) ~doc:"STRING __string"
       and backendEnvironmentName =
         flag "backend-environment-name" (required string)
           ~doc:"STRING __string"
       and nativeClientId =
         flag "native-client-id" (required string) ~doc:"STRING __string"
       and userPoolId =
         flag "user-pool-id" (required string) ~doc:"STRING __string"
       and webClientId =
         flag "web-client-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_backend_auth
           (Values.ImportBackendAuthRequest.make ?identityPoolId ~appId
              ~backendEnvironmentName ~nativeClientId ~userPoolId
              ~webClientId ())
           (Some Values.ImportBackendAuthResponse.to_json)
           (Some Values.ImportBackendAuthResponse.error_to_json)])
let import_backend_storage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bucketName =
         flag "bucket-name" (optional string) ~doc:"STRING __string"
       and appId = flag "app-id" (required string) ~doc:"STRING __string"
       and backendEnvironmentName =
         flag "backend-environment-name" (required string)
           ~doc:"STRING __string"
       and serviceName =
         flag "service-name" (required json_arg) ~doc:"JSON ServiceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_backend_storage
           (Values.ImportBackendStorageRequest.make ?bucketName ~appId
              ~backendEnvironmentName
              ~serviceName:(Values.ServiceName.of_json serviceName) ())
           (Some Values.ImportBackendStorageResponse.to_json)
           (Some Values.ImportBackendStorageResponse.error_to_json)])
let list_backend_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (optional string) ~doc:"STRING __string"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT __integerMin1Max25"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and operation =
         flag "operation" (optional string) ~doc:"STRING __string"
       and status = flag "status" (optional string) ~doc:"STRING __string"
       and appId = flag "app-id" (required string) ~doc:"STRING __string"
       and backendEnvironmentName =
         flag "backend-environment-name" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_backend_jobs
           (Values.ListBackendJobsRequest.make ?jobId ?maxResults ?nextToken
              ?operation ?status ~appId ~backendEnvironmentName ())
           (Some Values.ListBackendJobsResponse.to_json)
           (Some Values.ListBackendJobsResponse.error_to_json)])
let list_s3_buckets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_s3_buckets
           (Values.ListS3BucketsRequest.make ?nextToken ())
           (Some Values.ListS3BucketsResponse.to_json)
           (Some Values.ListS3BucketsResponse.error_to_json)])
let remove_all_backends =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cleanAmplifyApp =
         flag "clean-amplify-app" (optional bool) ~doc:"BOOL __boolean"
       and appId = flag "app-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_all_backends
           (Values.RemoveAllBackendsRequest.make ?cleanAmplifyApp ~appId ())
           (Some Values.RemoveAllBackendsResponse.to_json)
           (Some Values.RemoveAllBackendsResponse.error_to_json)])
let remove_backend_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_backend_config
           (Values.RemoveBackendConfigRequest.make ~appId ())
           (Some Values.RemoveBackendConfigResponse.to_json)
           (Some Values.RemoveBackendConfigResponse.error_to_json)])
let update_backend_a_p_i =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceConfig =
         flag "resource-config" (optional json_arg)
           ~doc:"JSON BackendAPIResourceConfig"
       and appId = flag "app-id" (required string) ~doc:"STRING __string"
       and backendEnvironmentName =
         flag "backend-environment-name" (required string)
           ~doc:"STRING __string"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_backend_a_p_i
           (Values.UpdateBackendAPIRequest.make
              ?resourceConfig:(Option.map
                                 ~f:Values.BackendAPIResourceConfig.of_json
                                 resourceConfig) ~appId
              ~backendEnvironmentName ~resourceName ())
           (Some Values.UpdateBackendAPIResponse.to_json)
           (Some Values.UpdateBackendAPIResponse.error_to_json)])
let update_backend_auth =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING __string"
       and backendEnvironmentName =
         flag "backend-environment-name" (required string)
           ~doc:"STRING __string"
       and resourceConfig =
         flag "resource-config" (required json_arg)
           ~doc:"JSON UpdateBackendAuthResourceConfig"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_backend_auth
           (Values.UpdateBackendAuthRequest.make ~appId
              ~backendEnvironmentName
              ~resourceConfig:(Values.UpdateBackendAuthResourceConfig.of_json
                                 resourceConfig) ~resourceName ())
           (Some Values.UpdateBackendAuthResponse.to_json)
           (Some Values.UpdateBackendAuthResponse.error_to_json)])
let update_backend_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and loginAuthConfig =
         flag "login-auth-config" (optional json_arg)
           ~doc:"JSON LoginAuthConfigReqObj"
       and appId = flag "app-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_backend_config
           (Values.UpdateBackendConfigRequest.make
              ?loginAuthConfig:(Option.map
                                  ~f:Values.LoginAuthConfigReqObj.of_json
                                  loginAuthConfig) ~appId ())
           (Some Values.UpdateBackendConfigResponse.to_json)
           (Some Values.UpdateBackendConfigResponse.error_to_json)])
let update_backend_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and operation =
         flag "operation" (optional string) ~doc:"STRING __string"
       and status = flag "status" (optional string) ~doc:"STRING __string"
       and appId = flag "app-id" (required string) ~doc:"STRING __string"
       and backendEnvironmentName =
         flag "backend-environment-name" (required string)
           ~doc:"STRING __string"
       and jobId = flag "job-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_backend_job
           (Values.UpdateBackendJobRequest.make ?operation ?status ~appId
              ~backendEnvironmentName ~jobId ())
           (Some Values.UpdateBackendJobResponse.to_json)
           (Some Values.UpdateBackendJobResponse.error_to_json)])
let update_backend_storage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING __string"
       and backendEnvironmentName =
         flag "backend-environment-name" (required string)
           ~doc:"STRING __string"
       and resourceConfig =
         flag "resource-config" (required json_arg)
           ~doc:"JSON UpdateBackendStorageResourceConfig"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_backend_storage
           (Values.UpdateBackendStorageRequest.make ~appId
              ~backendEnvironmentName
              ~resourceConfig:(Values.UpdateBackendStorageResourceConfig.of_json
                                 resourceConfig) ~resourceName ())
           (Some Values.UpdateBackendStorageResponse.to_json)
           (Some Values.UpdateBackendStorageResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("clone-backend", clone_backend);
    ("create-backend", create_backend);
    ("create-backend-a-p-i", create_backend_a_p_i);
    ("create-backend-auth", create_backend_auth);
    ("create-backend-config", create_backend_config);
    ("create-backend-storage", create_backend_storage);
    ("create-token", create_token);
    ("delete-backend", delete_backend);
    ("delete-backend-a-p-i", delete_backend_a_p_i);
    ("delete-backend-auth", delete_backend_auth);
    ("delete-backend-storage", delete_backend_storage);
    ("delete-token", delete_token);
    ("generate-backend-a-p-i-models", generate_backend_a_p_i_models);
    ("get-backend", get_backend);
    ("get-backend-a-p-i", get_backend_a_p_i);
    ("get-backend-a-p-i-models", get_backend_a_p_i_models);
    ("get-backend-auth", get_backend_auth);
    ("get-backend-job", get_backend_job);
    ("get-backend-storage", get_backend_storage);
    ("get-token", get_token);
    ("import-backend-auth", import_backend_auth);
    ("import-backend-storage", import_backend_storage);
    ("list-backend-jobs", list_backend_jobs);
    ("list-s3-buckets", list_s3_buckets);
    ("remove-all-backends", remove_all_backends);
    ("remove-backend-config", remove_backend_config);
    ("update-backend-a-p-i", update_backend_a_p_i);
    ("update-backend-auth", update_backend_auth);
    ("update-backend-config", update_backend_config);
    ("update-backend-job", update_backend_job);
    ("update-backend-storage", update_backend_storage)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
open Cli_0[@@warning "-33"]
let describe_d_b_snapshots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBInstanceIdentifier =
         flag "d-b-instance-identifier" (optional string)
           ~doc:"STRING String"
       and dBSnapshotIdentifier =
         flag "d-b-snapshot-identifier" (optional string)
           ~doc:"STRING String"
       and snapshotType =
         flag "snapshot-type" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and includeShared =
         flag "include-shared" (optional bool) ~doc:"BOOL Boolean"
       and includePublic =
         flag "include-public" (optional bool) ~doc:"BOOL Boolean"
       and dbiResourceId =
         flag "dbi-resource-id" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_d_b_snapshots
           (Values.DescribeDBSnapshotsMessage.make ?dBInstanceIdentifier
              ?dBSnapshotIdentifier ?snapshotType
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ?includeShared ?includePublic
              ?dbiResourceId ()) (Some Values.DBSnapshotMessage.to_json)
           (Some Values.DBSnapshotMessage.error_to_json)])
let describe_d_b_subnet_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBSubnetGroupName =
         flag "d-b-subnet-group-name" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_d_b_subnet_groups
           (Values.DescribeDBSubnetGroupsMessage.make ?dBSubnetGroupName
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ())
           (Some Values.DBSubnetGroupMessage.to_json)
           (Some Values.DBSubnetGroupMessage.error_to_json)])
let describe_engine_default_cluster_parameters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and dBParameterGroupFamily =
         flag "d-b-parameter-group-family" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_engine_default_cluster_parameters
           (Values.DescribeEngineDefaultClusterParametersMessage.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ~dBParameterGroupFamily ())
           (Some Values.DescribeEngineDefaultClusterParametersResult.to_json)
           (Some
              Values.DescribeEngineDefaultClusterParametersResult.error_to_json)])
let describe_engine_default_parameters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and dBParameterGroupFamily =
         flag "d-b-parameter-group-family" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_engine_default_parameters
           (Values.DescribeEngineDefaultParametersMessage.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ~dBParameterGroupFamily ())
           (Some Values.DescribeEngineDefaultParametersResult.to_json)
           (Some Values.DescribeEngineDefaultParametersResult.error_to_json)])
let describe_event_categories =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceType =
         flag "source-type" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_event_categories
           (Values.DescribeEventCategoriesMessage.make ?sourceType
              ?filters:(Option.map ~f:Values.FilterList.of_json filters) ())
           (Some Values.EventCategoriesMessage.to_json)
           (Some Values.EventCategoriesMessage.error_to_json)])
let describe_event_subscriptions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subscriptionName =
         flag "subscription-name" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_event_subscriptions
           (Values.DescribeEventSubscriptionsMessage.make ?subscriptionName
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ())
           (Some Values.EventSubscriptionsMessage.to_json)
           (Some Values.EventSubscriptionsMessage.error_to_json)])
let describe_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceIdentifier =
         flag "source-identifier" (optional string) ~doc:"STRING String"
       and sourceType =
         flag "source-type" (optional json_arg) ~doc:"JSON SourceType"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON TStamp"
       and endTime = flag "end-time" (optional json_arg) ~doc:"JSON TStamp"
       and duration =
         flag "duration" (optional int) ~doc:"INT IntegerOptional"
       and eventCategories =
         flag "event-categories" (optional json_arg)
           ~doc:"JSON EventCategoriesList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_events
           (Values.DescribeEventsMessage.make ?sourceIdentifier
              ?sourceType:(Option.map ~f:Values.SourceType.of_json sourceType)
              ?startTime:(Option.map ~f:Values.TStamp.of_json startTime)
              ?endTime:(Option.map ~f:Values.TStamp.of_json endTime)
              ?duration
              ?eventCategories:(Option.map
                                  ~f:Values.EventCategoriesList.of_json
                                  eventCategories)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ()) (Some Values.EventsMessage.to_json)
           (Some Values.EventsMessage.error_to_json)])
let describe_export_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and exportTaskIdentifier =
         flag "export-task-identifier" (optional string) ~doc:"STRING String"
       and sourceArn =
         flag "source-arn" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT MaxRecords" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_export_tasks
           (Values.DescribeExportTasksMessage.make ?exportTaskIdentifier
              ?sourceArn
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?marker ?maxRecords ())
           (Some Values.ExportTasksMessage.to_json)
           (Some Values.ExportTasksMessage.error_to_json)])
let describe_global_clusters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalClusterIdentifier =
         flag "global-cluster-identifier" (optional string)
           ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_global_clusters
           (Values.DescribeGlobalClustersMessage.make
              ?globalClusterIdentifier
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ())
           (Some Values.GlobalClustersMessage.to_json)
           (Some Values.GlobalClustersMessage.error_to_json)])
let describe_installation_media =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and installationMediaId =
         flag "installation-media-id" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_installation_media
           (Values.DescribeInstallationMediaMessage.make ?installationMediaId
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ())
           (Some Values.InstallationMediaMessage.to_json)
           (Some Values.InstallationMediaMessage.error_to_json)])
let describe_option_group_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and majorEngineVersion =
         flag "major-engine-version" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and engineName =
         flag "engine-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_option_group_options
           (Values.DescribeOptionGroupOptionsMessage.make ?majorEngineVersion
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ~engineName ())
           (Some Values.OptionGroupOptionsMessage.to_json)
           (Some Values.OptionGroupOptionsMessage.error_to_json)])
let describe_option_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and optionGroupName =
         flag "option-group-name" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and engineName =
         flag "engine-name" (optional string) ~doc:"STRING String"
       and majorEngineVersion =
         flag "major-engine-version" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_option_groups
           (Values.DescribeOptionGroupsMessage.make ?optionGroupName
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?marker ?maxRecords ?engineName ?majorEngineVersion ())
           (Some Values.OptionGroups.to_json)
           (Some Values.OptionGroups.error_to_json)])
let describe_orderable_d_b_instance_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING String"
       and dBInstanceClass =
         flag "d-b-instance-class" (optional string) ~doc:"STRING String"
       and licenseModel =
         flag "license-model" (optional string) ~doc:"STRING String"
       and availabilityZoneGroup =
         flag "availability-zone-group" (optional string)
           ~doc:"STRING String"
       and vpc = flag "vpc" (optional bool) ~doc:"BOOL BooleanOptional"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and engine = flag "engine" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_orderable_d_b_instance_options
           (Values.DescribeOrderableDBInstanceOptionsMessage.make
              ?engineVersion ?dBInstanceClass ?licenseModel
              ?availabilityZoneGroup ?vpc
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ~engine ())
           (Some Values.OrderableDBInstanceOptionsMessage.to_json)
           (Some Values.OrderableDBInstanceOptionsMessage.error_to_json)])
let describe_pending_maintenance_actions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceIdentifier =
         flag "resource-identifier" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_pending_maintenance_actions
           (Values.DescribePendingMaintenanceActionsMessage.make
              ?resourceIdentifier
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?marker ?maxRecords ())
           (Some Values.PendingMaintenanceActionsMessage.to_json)
           (Some Values.PendingMaintenanceActionsMessage.error_to_json)])
let describe_reserved_d_b_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reservedDBInstanceId =
         flag "reserved-d-b-instance-id" (optional string)
           ~doc:"STRING String"
       and reservedDBInstancesOfferingId =
         flag "reserved-d-b-instances-offering-id" (optional string)
           ~doc:"STRING String"
       and dBInstanceClass =
         flag "d-b-instance-class" (optional string) ~doc:"STRING String"
       and duration = flag "duration" (optional string) ~doc:"STRING String"
       and productDescription =
         flag "product-description" (optional string) ~doc:"STRING String"
       and offeringType =
         flag "offering-type" (optional string) ~doc:"STRING String"
       and multiAZ =
         flag "multi-a-z" (optional bool) ~doc:"BOOL BooleanOptional"
       and leaseId = flag "lease-id" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_reserved_d_b_instances
           (Values.DescribeReservedDBInstancesMessage.make
              ?reservedDBInstanceId ?reservedDBInstancesOfferingId
              ?dBInstanceClass ?duration ?productDescription ?offeringType
              ?multiAZ ?leaseId
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ())
           (Some Values.ReservedDBInstanceMessage.to_json)
           (Some Values.ReservedDBInstanceMessage.error_to_json)])
let describe_reserved_d_b_instances_offerings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reservedDBInstancesOfferingId =
         flag "reserved-d-b-instances-offering-id" (optional string)
           ~doc:"STRING String"
       and dBInstanceClass =
         flag "d-b-instance-class" (optional string) ~doc:"STRING String"
       and duration = flag "duration" (optional string) ~doc:"STRING String"
       and productDescription =
         flag "product-description" (optional string) ~doc:"STRING String"
       and offeringType =
         flag "offering-type" (optional string) ~doc:"STRING String"
       and multiAZ =
         flag "multi-a-z" (optional bool) ~doc:"BOOL BooleanOptional"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_reserved_d_b_instances_offerings
           (Values.DescribeReservedDBInstancesOfferingsMessage.make
              ?reservedDBInstancesOfferingId ?dBInstanceClass ?duration
              ?productDescription ?offeringType ?multiAZ
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ())
           (Some Values.ReservedDBInstancesOfferingMessage.to_json)
           (Some Values.ReservedDBInstancesOfferingMessage.error_to_json)])
let describe_source_regions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and regionName =
         flag "region-name" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_source_regions
           (Values.DescribeSourceRegionsMessage.make ?regionName ?maxRecords
              ?marker
              ?filters:(Option.map ~f:Values.FilterList.of_json filters) ())
           (Some Values.SourceRegionMessage.to_json)
           (Some Values.SourceRegionMessage.error_to_json)])
let describe_valid_d_b_instance_modifications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBInstanceIdentifier =
         flag "d-b-instance-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_valid_d_b_instance_modifications
           (Values.DescribeValidDBInstanceModificationsMessage.make
              ~dBInstanceIdentifier ())
           (Some Values.DescribeValidDBInstanceModificationsResult.to_json)
           (Some
              Values.DescribeValidDBInstanceModificationsResult.error_to_json)])
let download_d_b_log_file_portion =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and numberOfLines =
         flag "number-of-lines" (optional int) ~doc:"INT Integer"
       and dBInstanceIdentifier =
         flag "d-b-instance-identifier" (required string)
           ~doc:"STRING String"
       and logFileName =
         flag "log-file-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.download_d_b_log_file_portion
           (Values.DownloadDBLogFilePortionMessage.make ?marker
              ?numberOfLines ~dBInstanceIdentifier ~logFileName ())
           (Some Values.DownloadDBLogFilePortionDetails.to_json)
           (Some Values.DownloadDBLogFilePortionDetails.error_to_json)])
let failover_d_b_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetDBInstanceIdentifier =
         flag "target-d-b-instance-identifier" (optional string)
           ~doc:"STRING String"
       and dBClusterIdentifier =
         flag "d-b-cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.failover_d_b_cluster
           (Values.FailoverDBClusterMessage.make ?targetDBInstanceIdentifier
              ~dBClusterIdentifier ())
           (Some Values.FailoverDBClusterResult.to_json)
           (Some Values.FailoverDBClusterResult.error_to_json)])
let failover_global_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalClusterIdentifier =
         flag "global-cluster-identifier" (required string)
           ~doc:"STRING GlobalClusterIdentifier"
       and targetDbClusterIdentifier =
         flag "target-db-cluster-identifier" (required string)
           ~doc:"STRING DBClusterIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.failover_global_cluster
           (Values.FailoverGlobalClusterMessage.make ~globalClusterIdentifier
              ~targetDbClusterIdentifier ())
           (Some Values.FailoverGlobalClusterResult.to_json)
           (Some Values.FailoverGlobalClusterResult.error_to_json)])
let import_installation_media =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customAvailabilityZoneId =
         flag "custom-availability-zone-id" (required string)
           ~doc:"STRING String"
       and engine = flag "engine" (required string) ~doc:"STRING String"
       and engineVersion =
         flag "engine-version" (required string) ~doc:"STRING String"
       and engineInstallationMediaPath =
         flag "engine-installation-media-path" (required string)
           ~doc:"STRING String"
       and oSInstallationMediaPath =
         flag "o-s-installation-media-path" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_installation_media
           (Values.ImportInstallationMediaMessage.make
              ~customAvailabilityZoneId ~engine ~engineVersion
              ~engineInstallationMediaPath ~oSInstallationMediaPath ())
           (Some Values.InstallationMedia.to_json)
           (Some Values.InstallationMedia.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceMessage.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ~resourceName ()) (Some Values.TagListMessage.to_json)
           (Some Values.TagListMessage.error_to_json)])
let modify_certificates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateIdentifier =
         flag "certificate-identifier" (optional string) ~doc:"STRING String"
       and removeCustomerOverride =
         flag "remove-customer-override" (optional bool)
           ~doc:"BOOL BooleanOptional" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_certificates
           (Values.ModifyCertificatesMessage.make ?certificateIdentifier
              ?removeCustomerOverride ())
           (Some Values.ModifyCertificatesResult.to_json)
           (Some Values.ModifyCertificatesResult.error_to_json)])
let modify_current_d_b_cluster_capacity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and capacity =
         flag "capacity" (optional int) ~doc:"INT IntegerOptional"
       and secondsBeforeTimeout =
         flag "seconds-before-timeout" (optional int)
           ~doc:"INT IntegerOptional"
       and timeoutAction =
         flag "timeout-action" (optional string) ~doc:"STRING String"
       and dBClusterIdentifier =
         flag "d-b-cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_current_d_b_cluster_capacity
           (Values.ModifyCurrentDBClusterCapacityMessage.make ?capacity
              ?secondsBeforeTimeout ?timeoutAction ~dBClusterIdentifier ())
           (Some Values.DBClusterCapacityInfo.to_json)
           (Some Values.DBClusterCapacityInfo.error_to_json)])
let modify_custom_d_b_engine_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and status =
         flag "status" (optional json_arg)
           ~doc:"JSON CustomEngineVersionStatus"
       and engine =
         flag "engine" (required string) ~doc:"STRING CustomEngineName"
       and engineVersion =
         flag "engine-version" (required string)
           ~doc:"STRING CustomEngineVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_custom_d_b_engine_version
           (Values.ModifyCustomDBEngineVersionMessage.make ?description
              ?status:(Option.map ~f:Values.CustomEngineVersionStatus.of_json
                         status) ~engine ~engineVersion ())
           (Some Values.DBEngineVersion.to_json)
           (Some Values.DBEngineVersion.error_to_json)])
let modify_d_b_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and newDBClusterIdentifier =
         flag "new-d-b-cluster-identifier" (optional string)
           ~doc:"STRING String"
       and applyImmediately =
         flag "apply-immediately" (optional bool) ~doc:"BOOL Boolean"
       and backupRetentionPeriod =
         flag "backup-retention-period" (optional int)
           ~doc:"INT IntegerOptional"
       and dBClusterParameterGroupName =
         flag "d-b-cluster-parameter-group-name" (optional string)
           ~doc:"STRING String"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON VpcSecurityGroupIdList"
       and port = flag "port" (optional int) ~doc:"INT IntegerOptional"
       and masterUserPassword =
         flag "master-user-password" (optional string) ~doc:"STRING String"
       and optionGroupName =
         flag "option-group-name" (optional string) ~doc:"STRING String"
       and preferredBackupWindow =
         flag "preferred-backup-window" (optional string)
           ~doc:"STRING String"
       and preferredMaintenanceWindow =
         flag "preferred-maintenance-window" (optional string)
           ~doc:"STRING String"
       and enableIAMDatabaseAuthentication =
         flag "enable-i-a-m-database-authentication" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and backtrackWindow =
         flag "backtrack-window" (optional json_arg) ~doc:"JSON LongOptional"
       and cloudwatchLogsExportConfiguration =
         flag "cloudwatch-logs-export-configuration" (optional json_arg)
           ~doc:"JSON CloudwatchLogsExportConfiguration"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING String"
       and allowMajorVersionUpgrade =
         flag "allow-major-version-upgrade" (optional bool)
           ~doc:"BOOL Boolean"
       and dBInstanceParameterGroupName =
         flag "d-b-instance-parameter-group-name" (optional string)
           ~doc:"STRING String"
       and domain = flag "domain" (optional string) ~doc:"STRING String"
       and domainIAMRoleName =
         flag "domain-i-a-m-role-name" (optional string) ~doc:"STRING String"
       and scalingConfiguration =
         flag "scaling-configuration" (optional json_arg)
           ~doc:"JSON ScalingConfiguration"
       and deletionProtection =
         flag "deletion-protection" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and enableHttpEndpoint =
         flag "enable-http-endpoint" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and copyTagsToSnapshot =
         flag "copy-tags-to-snapshot" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and enableGlobalWriteForwarding =
         flag "enable-global-write-forwarding" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and dBClusterInstanceClass =
         flag "d-b-cluster-instance-class" (optional string)
           ~doc:"STRING String"
       and allocatedStorage =
         flag "allocated-storage" (optional int) ~doc:"INT IntegerOptional"
       and storageType =
         flag "storage-type" (optional string) ~doc:"STRING String"
       and iops = flag "iops" (optional int) ~doc:"INT IntegerOptional"
       and autoMinorVersionUpgrade =
         flag "auto-minor-version-upgrade" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and monitoringInterval =
         flag "monitoring-interval" (optional int) ~doc:"INT IntegerOptional"
       and monitoringRoleArn =
         flag "monitoring-role-arn" (optional string) ~doc:"STRING String"
       and enablePerformanceInsights =
         flag "enable-performance-insights" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and performanceInsightsKMSKeyId =
         flag "performance-insights-k-m-s-key-id" (optional string)
           ~doc:"STRING String"
       and performanceInsightsRetentionPeriod =
         flag "performance-insights-retention-period" (optional int)
           ~doc:"INT IntegerOptional"
       and dBClusterIdentifier =
         flag "d-b-cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_d_b_cluster
           (Values.ModifyDBClusterMessage.make ?newDBClusterIdentifier
              ?applyImmediately ?backupRetentionPeriod
              ?dBClusterParameterGroupName
              ?vpcSecurityGroupIds:(Option.map
                                      ~f:Values.VpcSecurityGroupIdList.of_json
                                      vpcSecurityGroupIds) ?port
              ?masterUserPassword ?optionGroupName ?preferredBackupWindow
              ?preferredMaintenanceWindow ?enableIAMDatabaseAuthentication
              ?backtrackWindow:(Option.map ~f:Values.LongOptional.of_json
                                  backtrackWindow)
              ?cloudwatchLogsExportConfiguration:(Option.map
                                                    ~f:Values.CloudwatchLogsExportConfiguration.of_json
                                                    cloudwatchLogsExportConfiguration)
              ?engineVersion ?allowMajorVersionUpgrade
              ?dBInstanceParameterGroupName ?domain ?domainIAMRoleName
              ?scalingConfiguration:(Option.map
                                       ~f:Values.ScalingConfiguration.of_json
                                       scalingConfiguration)
              ?deletionProtection ?enableHttpEndpoint ?copyTagsToSnapshot
              ?enableGlobalWriteForwarding ?dBClusterInstanceClass
              ?allocatedStorage ?storageType ?iops ?autoMinorVersionUpgrade
              ?monitoringInterval ?monitoringRoleArn
              ?enablePerformanceInsights ?performanceInsightsKMSKeyId
              ?performanceInsightsRetentionPeriod ~dBClusterIdentifier ())
           (Some Values.ModifyDBClusterResult.to_json)
           (Some Values.ModifyDBClusterResult.error_to_json)])
let modify_d_b_cluster_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endpointType =
         flag "endpoint-type" (optional string) ~doc:"STRING String"
       and staticMembers =
         flag "static-members" (optional json_arg) ~doc:"JSON StringList"
       and excludedMembers =
         flag "excluded-members" (optional json_arg) ~doc:"JSON StringList"
       and dBClusterEndpointIdentifier =
         flag "d-b-cluster-endpoint-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_d_b_cluster_endpoint
           (Values.ModifyDBClusterEndpointMessage.make ?endpointType
              ?staticMembers:(Option.map ~f:Values.StringList.of_json
                                staticMembers)
              ?excludedMembers:(Option.map ~f:Values.StringList.of_json
                                  excludedMembers)
              ~dBClusterEndpointIdentifier ())
           (Some Values.DBClusterEndpoint.to_json)
           (Some Values.DBClusterEndpoint.error_to_json)])
let modify_d_b_cluster_parameter_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBClusterParameterGroupName =
         flag "d-b-cluster-parameter-group-name" (required string)
           ~doc:"STRING String"
       and parameters =
         flag "parameters" (required json_arg) ~doc:"JSON ParametersList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_d_b_cluster_parameter_group
           (Values.ModifyDBClusterParameterGroupMessage.make
              ~dBClusterParameterGroupName
              ~parameters:(Values.ParametersList.of_json parameters) ())
           (Some Values.DBClusterParameterGroupNameMessage.to_json)
           (Some Values.DBClusterParameterGroupNameMessage.error_to_json)])
let modify_d_b_cluster_snapshot_attribute =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and valuesToAdd =
         flag "values-to-add" (optional json_arg)
           ~doc:"JSON AttributeValueList"
       and valuesToRemove =
         flag "values-to-remove" (optional json_arg)
           ~doc:"JSON AttributeValueList"
       and dBClusterSnapshotIdentifier =
         flag "d-b-cluster-snapshot-identifier" (required string)
           ~doc:"STRING String"
       and attributeName =
         flag "attribute-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_d_b_cluster_snapshot_attribute
           (Values.ModifyDBClusterSnapshotAttributeMessage.make
              ?valuesToAdd:(Option.map ~f:Values.AttributeValueList.of_json
                              valuesToAdd)
              ?valuesToRemove:(Option.map
                                 ~f:Values.AttributeValueList.of_json
                                 valuesToRemove) ~dBClusterSnapshotIdentifier
              ~attributeName ())
           (Some Values.ModifyDBClusterSnapshotAttributeResult.to_json)
           (Some Values.ModifyDBClusterSnapshotAttributeResult.error_to_json)])
let modify_d_b_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and allocatedStorage =
         flag "allocated-storage" (optional int) ~doc:"INT IntegerOptional"
       and dBInstanceClass =
         flag "d-b-instance-class" (optional string) ~doc:"STRING String"
       and dBSubnetGroupName =
         flag "d-b-subnet-group-name" (optional string) ~doc:"STRING String"
       and dBSecurityGroups =
         flag "d-b-security-groups" (optional json_arg)
           ~doc:"JSON DBSecurityGroupNameList"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON VpcSecurityGroupIdList"
       and applyImmediately =
         flag "apply-immediately" (optional bool) ~doc:"BOOL Boolean"
       and masterUserPassword =
         flag "master-user-password" (optional string) ~doc:"STRING String"
       and dBParameterGroupName =
         flag "d-b-parameter-group-name" (optional string)
           ~doc:"STRING String"
       and backupRetentionPeriod =
         flag "backup-retention-period" (optional int)
           ~doc:"INT IntegerOptional"
       and preferredBackupWindow =
         flag "preferred-backup-window" (optional string)
           ~doc:"STRING String"
       and preferredMaintenanceWindow =
         flag "preferred-maintenance-window" (optional string)
           ~doc:"STRING String"
       and multiAZ =
         flag "multi-a-z" (optional bool) ~doc:"BOOL BooleanOptional"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING String"
       and allowMajorVersionUpgrade =
         flag "allow-major-version-upgrade" (optional bool)
           ~doc:"BOOL Boolean"
       and autoMinorVersionUpgrade =
         flag "auto-minor-version-upgrade" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and licenseModel =
         flag "license-model" (optional string) ~doc:"STRING String"
       and iops = flag "iops" (optional int) ~doc:"INT IntegerOptional"
       and optionGroupName =
         flag "option-group-name" (optional string) ~doc:"STRING String"
       and newDBInstanceIdentifier =
         flag "new-d-b-instance-identifier" (optional string)
           ~doc:"STRING String"
       and storageType =
         flag "storage-type" (optional string) ~doc:"STRING String"
       and tdeCredentialArn =
         flag "tde-credential-arn" (optional string) ~doc:"STRING String"
       and tdeCredentialPassword =
         flag "tde-credential-password" (optional string)
           ~doc:"STRING String"
       and cACertificateIdentifier =
         flag "c-a-certificate-identifier" (optional string)
           ~doc:"STRING String"
       and domain = flag "domain" (optional string) ~doc:"STRING String"
       and copyTagsToSnapshot =
         flag "copy-tags-to-snapshot" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and monitoringInterval =
         flag "monitoring-interval" (optional int) ~doc:"INT IntegerOptional"
       and dBPortNumber =
         flag "d-b-port-number" (optional int) ~doc:"INT IntegerOptional"
       and publiclyAccessible =
         flag "publicly-accessible" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and monitoringRoleArn =
         flag "monitoring-role-arn" (optional string) ~doc:"STRING String"
       and domainIAMRoleName =
         flag "domain-i-a-m-role-name" (optional string) ~doc:"STRING String"
       and promotionTier =
         flag "promotion-tier" (optional int) ~doc:"INT IntegerOptional"
       and enableIAMDatabaseAuthentication =
         flag "enable-i-a-m-database-authentication" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and enablePerformanceInsights =
         flag "enable-performance-insights" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and performanceInsightsKMSKeyId =
         flag "performance-insights-k-m-s-key-id" (optional string)
           ~doc:"STRING String"
       and performanceInsightsRetentionPeriod =
         flag "performance-insights-retention-period" (optional int)
           ~doc:"INT IntegerOptional"
       and cloudwatchLogsExportConfiguration =
         flag "cloudwatch-logs-export-configuration" (optional json_arg)
           ~doc:"JSON CloudwatchLogsExportConfiguration"
       and processorFeatures =
         flag "processor-features" (optional json_arg)
           ~doc:"JSON ProcessorFeatureList"
       and useDefaultProcessorFeatures =
         flag "use-default-processor-features" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and deletionProtection =
         flag "deletion-protection" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and maxAllocatedStorage =
         flag "max-allocated-storage" (optional int)
           ~doc:"INT IntegerOptional"
       and certificateRotationRestart =
         flag "certificate-rotation-restart" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and replicaMode =
         flag "replica-mode" (optional json_arg) ~doc:"JSON ReplicaMode"
       and enableCustomerOwnedIp =
         flag "enable-customer-owned-ip" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and awsBackupRecoveryPointArn =
         flag "aws-backup-recovery-point-arn" (optional string)
           ~doc:"STRING AwsBackupRecoveryPointArn"
       and automationMode =
         flag "automation-mode" (optional json_arg)
           ~doc:"JSON AutomationMode"
       and resumeFullAutomationModeMinutes =
         flag "resume-full-automation-mode-minutes" (optional int)
           ~doc:"INT IntegerOptional"
       and dBInstanceIdentifier =
         flag "d-b-instance-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_d_b_instance
           (Values.ModifyDBInstanceMessage.make ?allocatedStorage
              ?dBInstanceClass ?dBSubnetGroupName
              ?dBSecurityGroups:(Option.map
                                   ~f:Values.DBSecurityGroupNameList.of_json
                                   dBSecurityGroups)
              ?vpcSecurityGroupIds:(Option.map
                                      ~f:Values.VpcSecurityGroupIdList.of_json
                                      vpcSecurityGroupIds) ?applyImmediately
              ?masterUserPassword ?dBParameterGroupName
              ?backupRetentionPeriod ?preferredBackupWindow
              ?preferredMaintenanceWindow ?multiAZ ?engineVersion
              ?allowMajorVersionUpgrade ?autoMinorVersionUpgrade
              ?licenseModel ?iops ?optionGroupName ?newDBInstanceIdentifier
              ?storageType ?tdeCredentialArn ?tdeCredentialPassword
              ?cACertificateIdentifier ?domain ?copyTagsToSnapshot
              ?monitoringInterval ?dBPortNumber ?publiclyAccessible
              ?monitoringRoleArn ?domainIAMRoleName ?promotionTier
              ?enableIAMDatabaseAuthentication ?enablePerformanceInsights
              ?performanceInsightsKMSKeyId
              ?performanceInsightsRetentionPeriod
              ?cloudwatchLogsExportConfiguration:(Option.map
                                                    ~f:Values.CloudwatchLogsExportConfiguration.of_json
                                                    cloudwatchLogsExportConfiguration)
              ?processorFeatures:(Option.map
                                    ~f:Values.ProcessorFeatureList.of_json
                                    processorFeatures)
              ?useDefaultProcessorFeatures ?deletionProtection
              ?maxAllocatedStorage ?certificateRotationRestart
              ?replicaMode:(Option.map ~f:Values.ReplicaMode.of_json
                              replicaMode) ?enableCustomerOwnedIp
              ?awsBackupRecoveryPointArn
              ?automationMode:(Option.map ~f:Values.AutomationMode.of_json
                                 automationMode)
              ?resumeFullAutomationModeMinutes ~dBInstanceIdentifier ())
           (Some Values.ModifyDBInstanceResult.to_json)
           (Some Values.ModifyDBInstanceResult.error_to_json)])
let modify_d_b_parameter_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBParameterGroupName =
         flag "d-b-parameter-group-name" (required string)
           ~doc:"STRING String"
       and parameters =
         flag "parameters" (required json_arg) ~doc:"JSON ParametersList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_d_b_parameter_group
           (Values.ModifyDBParameterGroupMessage.make ~dBParameterGroupName
              ~parameters:(Values.ParametersList.of_json parameters) ())
           (Some Values.DBParameterGroupNameMessage.to_json)
           (Some Values.DBParameterGroupNameMessage.error_to_json)])
let modify_d_b_proxy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and newDBProxyName =
         flag "new-d-b-proxy-name" (optional string) ~doc:"STRING String"
       and auth =
         flag "auth" (optional json_arg) ~doc:"JSON UserAuthConfigList"
       and requireTLS =
         flag "require-t-l-s" (optional bool) ~doc:"BOOL BooleanOptional"
       and idleClientTimeout =
         flag "idle-client-timeout" (optional int) ~doc:"INT IntegerOptional"
       and debugLogging =
         flag "debug-logging" (optional bool) ~doc:"BOOL BooleanOptional"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING String"
       and securityGroups =
         flag "security-groups" (optional json_arg) ~doc:"JSON StringList"
       and dBProxyName =
         flag "d-b-proxy-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_d_b_proxy
           (Values.ModifyDBProxyRequest.make ?newDBProxyName
              ?auth:(Option.map ~f:Values.UserAuthConfigList.of_json auth)
              ?requireTLS ?idleClientTimeout ?debugLogging ?roleArn
              ?securityGroups:(Option.map ~f:Values.StringList.of_json
                                 securityGroups) ~dBProxyName ())
           (Some Values.ModifyDBProxyResponse.to_json)
           (Some Values.ModifyDBProxyResponse.error_to_json)])
let modify_d_b_proxy_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and newDBProxyEndpointName =
         flag "new-d-b-proxy-endpoint-name" (optional string)
           ~doc:"STRING DBProxyEndpointName"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON StringList"
       and dBProxyEndpointName =
         flag "d-b-proxy-endpoint-name" (required string)
           ~doc:"STRING DBProxyEndpointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_d_b_proxy_endpoint
           (Values.ModifyDBProxyEndpointRequest.make ?newDBProxyEndpointName
              ?vpcSecurityGroupIds:(Option.map ~f:Values.StringList.of_json
                                      vpcSecurityGroupIds)
              ~dBProxyEndpointName ())
           (Some Values.ModifyDBProxyEndpointResponse.to_json)
           (Some Values.ModifyDBProxyEndpointResponse.error_to_json)])
let modify_d_b_proxy_target_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectionPoolConfig =
         flag "connection-pool-config" (optional json_arg)
           ~doc:"JSON ConnectionPoolConfiguration"
       and newName = flag "new-name" (optional string) ~doc:"STRING String"
       and targetGroupName =
         flag "target-group-name" (required string) ~doc:"STRING String"
       and dBProxyName =
         flag "d-b-proxy-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_d_b_proxy_target_group
           (Values.ModifyDBProxyTargetGroupRequest.make
              ?connectionPoolConfig:(Option.map
                                       ~f:Values.ConnectionPoolConfiguration.of_json
                                       connectionPoolConfig) ?newName
              ~targetGroupName ~dBProxyName ())
           (Some Values.ModifyDBProxyTargetGroupResponse.to_json)
           (Some Values.ModifyDBProxyTargetGroupResponse.error_to_json)])
let modify_d_b_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING String"
       and optionGroupName =
         flag "option-group-name" (optional string) ~doc:"STRING String"
       and dBSnapshotIdentifier =
         flag "d-b-snapshot-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_d_b_snapshot
           (Values.ModifyDBSnapshotMessage.make ?engineVersion
              ?optionGroupName ~dBSnapshotIdentifier ())
           (Some Values.ModifyDBSnapshotResult.to_json)
           (Some Values.ModifyDBSnapshotResult.error_to_json)])
let modify_d_b_snapshot_attribute =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and valuesToAdd =
         flag "values-to-add" (optional json_arg)
           ~doc:"JSON AttributeValueList"
       and valuesToRemove =
         flag "values-to-remove" (optional json_arg)
           ~doc:"JSON AttributeValueList"
       and dBSnapshotIdentifier =
         flag "d-b-snapshot-identifier" (required string)
           ~doc:"STRING String"
       and attributeName =
         flag "attribute-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_d_b_snapshot_attribute
           (Values.ModifyDBSnapshotAttributeMessage.make
              ?valuesToAdd:(Option.map ~f:Values.AttributeValueList.of_json
                              valuesToAdd)
              ?valuesToRemove:(Option.map
                                 ~f:Values.AttributeValueList.of_json
                                 valuesToRemove) ~dBSnapshotIdentifier
              ~attributeName ())
           (Some Values.ModifyDBSnapshotAttributeResult.to_json)
           (Some Values.ModifyDBSnapshotAttributeResult.error_to_json)])
let modify_d_b_subnet_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBSubnetGroupDescription =
         flag "d-b-subnet-group-description" (optional string)
           ~doc:"STRING String"
       and dBSubnetGroupName =
         flag "d-b-subnet-group-name" (required string) ~doc:"STRING String"
       and subnetIds =
         flag "subnet-ids" (required json_arg)
           ~doc:"JSON SubnetIdentifierList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_d_b_subnet_group
           (Values.ModifyDBSubnetGroupMessage.make ?dBSubnetGroupDescription
              ~dBSubnetGroupName
              ~subnetIds:(Values.SubnetIdentifierList.of_json subnetIds) ())
           (Some Values.ModifyDBSubnetGroupResult.to_json)
           (Some Values.ModifyDBSubnetGroupResult.error_to_json)])
let modify_event_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and snsTopicArn =
         flag "sns-topic-arn" (optional string) ~doc:"STRING String"
       and sourceType =
         flag "source-type" (optional string) ~doc:"STRING String"
       and eventCategories =
         flag "event-categories" (optional json_arg)
           ~doc:"JSON EventCategoriesList"
       and enabled =
         flag "enabled" (optional bool) ~doc:"BOOL BooleanOptional"
       and subscriptionName =
         flag "subscription-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_event_subscription
           (Values.ModifyEventSubscriptionMessage.make ?snsTopicArn
              ?sourceType
              ?eventCategories:(Option.map
                                  ~f:Values.EventCategoriesList.of_json
                                  eventCategories) ?enabled ~subscriptionName
              ()) (Some Values.ModifyEventSubscriptionResult.to_json)
           (Some Values.ModifyEventSubscriptionResult.error_to_json)])
let modify_global_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalClusterIdentifier =
         flag "global-cluster-identifier" (optional string)
           ~doc:"STRING String"
       and newGlobalClusterIdentifier =
         flag "new-global-cluster-identifier" (optional string)
           ~doc:"STRING String"
       and deletionProtection =
         flag "deletion-protection" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING String"
       and allowMajorVersionUpgrade =
         flag "allow-major-version-upgrade" (optional bool)
           ~doc:"BOOL BooleanOptional" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_global_cluster
           (Values.ModifyGlobalClusterMessage.make ?globalClusterIdentifier
              ?newGlobalClusterIdentifier ?deletionProtection ?engineVersion
              ?allowMajorVersionUpgrade ())
           (Some Values.ModifyGlobalClusterResult.to_json)
           (Some Values.ModifyGlobalClusterResult.error_to_json)])
let modify_option_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and optionsToInclude =
         flag "options-to-include" (optional json_arg)
           ~doc:"JSON OptionConfigurationList"
       and optionsToRemove =
         flag "options-to-remove" (optional json_arg)
           ~doc:"JSON OptionNamesList"
       and applyImmediately =
         flag "apply-immediately" (optional bool) ~doc:"BOOL Boolean"
       and optionGroupName =
         flag "option-group-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_option_group
           (Values.ModifyOptionGroupMessage.make
              ?optionsToInclude:(Option.map
                                   ~f:Values.OptionConfigurationList.of_json
                                   optionsToInclude)
              ?optionsToRemove:(Option.map ~f:Values.OptionNamesList.of_json
                                  optionsToRemove) ?applyImmediately
              ~optionGroupName ())
           (Some Values.ModifyOptionGroupResult.to_json)
           (Some Values.ModifyOptionGroupResult.error_to_json)])
let promote_read_replica =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and backupRetentionPeriod =
         flag "backup-retention-period" (optional int)
           ~doc:"INT IntegerOptional"
       and preferredBackupWindow =
         flag "preferred-backup-window" (optional string)
           ~doc:"STRING String"
       and dBInstanceIdentifier =
         flag "d-b-instance-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.promote_read_replica
           (Values.PromoteReadReplicaMessage.make ?backupRetentionPeriod
              ?preferredBackupWindow ~dBInstanceIdentifier ())
           (Some Values.PromoteReadReplicaResult.to_json)
           (Some Values.PromoteReadReplicaResult.error_to_json)])
let promote_read_replica_d_b_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBClusterIdentifier =
         flag "d-b-cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.promote_read_replica_d_b_cluster
           (Values.PromoteReadReplicaDBClusterMessage.make
              ~dBClusterIdentifier ())
           (Some Values.PromoteReadReplicaDBClusterResult.to_json)
           (Some Values.PromoteReadReplicaDBClusterResult.error_to_json)])
let purchase_reserved_d_b_instances_offering =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reservedDBInstanceId =
         flag "reserved-d-b-instance-id" (optional string)
           ~doc:"STRING String"
       and dBInstanceCount =
         flag "d-b-instance-count" (optional int) ~doc:"INT IntegerOptional"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and reservedDBInstancesOfferingId =
         flag "reserved-d-b-instances-offering-id" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.purchase_reserved_d_b_instances_offering
           (Values.PurchaseReservedDBInstancesOfferingMessage.make
              ?reservedDBInstanceId ?dBInstanceCount
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~reservedDBInstancesOfferingId ())
           (Some Values.PurchaseReservedDBInstancesOfferingResult.to_json)
           (Some
              Values.PurchaseReservedDBInstancesOfferingResult.error_to_json)])
let reboot_d_b_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBClusterIdentifier =
         flag "d-b-cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reboot_d_b_cluster
           (Values.RebootDBClusterMessage.make ~dBClusterIdentifier ())
           (Some Values.RebootDBClusterResult.to_json)
           (Some Values.RebootDBClusterResult.error_to_json)])
let reboot_d_b_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and forceFailover =
         flag "force-failover" (optional bool) ~doc:"BOOL BooleanOptional"
       and dBInstanceIdentifier =
         flag "d-b-instance-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reboot_d_b_instance
           (Values.RebootDBInstanceMessage.make ?forceFailover
              ~dBInstanceIdentifier ())
           (Some Values.RebootDBInstanceResult.to_json)
           (Some Values.RebootDBInstanceResult.error_to_json)])
let register_d_b_proxy_targets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetGroupName =
         flag "target-group-name" (optional string) ~doc:"STRING String"
       and dBInstanceIdentifiers =
         flag "d-b-instance-identifiers" (optional json_arg)
           ~doc:"JSON StringList"
       and dBClusterIdentifiers =
         flag "d-b-cluster-identifiers" (optional json_arg)
           ~doc:"JSON StringList"
       and dBProxyName =
         flag "d-b-proxy-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_d_b_proxy_targets
           (Values.RegisterDBProxyTargetsRequest.make ?targetGroupName
              ?dBInstanceIdentifiers:(Option.map ~f:Values.StringList.of_json
                                        dBInstanceIdentifiers)
              ?dBClusterIdentifiers:(Option.map ~f:Values.StringList.of_json
                                       dBClusterIdentifiers) ~dBProxyName ())
           (Some Values.RegisterDBProxyTargetsResponse.to_json)
           (Some Values.RegisterDBProxyTargetsResponse.error_to_json)])
let remove_from_global_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalClusterIdentifier =
         flag "global-cluster-identifier" (optional string)
           ~doc:"STRING String"
       and dbClusterIdentifier =
         flag "db-cluster-identifier" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_from_global_cluster
           (Values.RemoveFromGlobalClusterMessage.make
              ?globalClusterIdentifier ?dbClusterIdentifier ())
           (Some Values.RemoveFromGlobalClusterResult.to_json)
           (Some Values.RemoveFromGlobalClusterResult.error_to_json)])
let remove_role_from_d_b_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and featureName =
         flag "feature-name" (optional string) ~doc:"STRING String"
       and dBClusterIdentifier =
         flag "d-b-cluster-identifier" (required string) ~doc:"STRING String"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_role_from_d_b_cluster
           (Values.RemoveRoleFromDBClusterMessage.make ?featureName
              ~dBClusterIdentifier ~roleArn ()) None None])
let remove_role_from_d_b_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBInstanceIdentifier =
         flag "d-b-instance-identifier" (required string)
           ~doc:"STRING String"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING String"
       and featureName =
         flag "feature-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_role_from_d_b_instance
           (Values.RemoveRoleFromDBInstanceMessage.make ~dBInstanceIdentifier
              ~roleArn ~featureName ()) None None])
let remove_source_identifier_from_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subscriptionName =
         flag "subscription-name" (required string) ~doc:"STRING String"
       and sourceIdentifier =
         flag "source-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_source_identifier_from_subscription
           (Values.RemoveSourceIdentifierFromSubscriptionMessage.make
              ~subscriptionName ~sourceIdentifier ())
           (Some Values.RemoveSourceIdentifierFromSubscriptionResult.to_json)
           (Some
              Values.RemoveSourceIdentifierFromSubscriptionResult.error_to_json)])
let remove_tags_from_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING String"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON KeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_tags_from_resource
           (Values.RemoveTagsFromResourceMessage.make ~resourceName
              ~tagKeys:(Values.KeyList.of_json tagKeys) ()) None None])
let reset_d_b_cluster_parameter_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resetAllParameters =
         flag "reset-all-parameters" (optional bool) ~doc:"BOOL Boolean"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON ParametersList"
       and dBClusterParameterGroupName =
         flag "d-b-cluster-parameter-group-name" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reset_d_b_cluster_parameter_group
           (Values.ResetDBClusterParameterGroupMessage.make
              ?resetAllParameters
              ?parameters:(Option.map ~f:Values.ParametersList.of_json
                             parameters) ~dBClusterParameterGroupName ())
           (Some Values.DBClusterParameterGroupNameMessage.to_json)
           (Some Values.DBClusterParameterGroupNameMessage.error_to_json)])
let reset_d_b_parameter_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resetAllParameters =
         flag "reset-all-parameters" (optional bool) ~doc:"BOOL Boolean"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON ParametersList"
       and dBParameterGroupName =
         flag "d-b-parameter-group-name" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reset_d_b_parameter_group
           (Values.ResetDBParameterGroupMessage.make ?resetAllParameters
              ?parameters:(Option.map ~f:Values.ParametersList.of_json
                             parameters) ~dBParameterGroupName ())
           (Some Values.DBParameterGroupNameMessage.to_json)
           (Some Values.DBParameterGroupNameMessage.error_to_json)])
let restore_d_b_cluster_from_s3 =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and availabilityZones =
         flag "availability-zones" (optional json_arg)
           ~doc:"JSON AvailabilityZones"
       and backupRetentionPeriod =
         flag "backup-retention-period" (optional int)
           ~doc:"INT IntegerOptional"
       and characterSetName =
         flag "character-set-name" (optional string) ~doc:"STRING String"
       and databaseName =
         flag "database-name" (optional string) ~doc:"STRING String"
       and dBClusterParameterGroupName =
         flag "d-b-cluster-parameter-group-name" (optional string)
           ~doc:"STRING String"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON VpcSecurityGroupIdList"
       and dBSubnetGroupName =
         flag "d-b-subnet-group-name" (optional string) ~doc:"STRING String"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING String"
       and port = flag "port" (optional int) ~doc:"INT IntegerOptional"
       and optionGroupName =
         flag "option-group-name" (optional string) ~doc:"STRING String"
       and preferredBackupWindow =
         flag "preferred-backup-window" (optional string)
           ~doc:"STRING String"
       and preferredMaintenanceWindow =
         flag "preferred-maintenance-window" (optional string)
           ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and storageEncrypted =
         flag "storage-encrypted" (optional bool) ~doc:"BOOL BooleanOptional"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and enableIAMDatabaseAuthentication =
         flag "enable-i-a-m-database-authentication" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and s3Prefix = flag "s3-prefix" (optional string) ~doc:"STRING String"
       and backtrackWindow =
         flag "backtrack-window" (optional json_arg) ~doc:"JSON LongOptional"
       and enableCloudwatchLogsExports =
         flag "enable-cloudwatch-logs-exports" (optional json_arg)
           ~doc:"JSON LogTypeList"
       and deletionProtection =
         flag "deletion-protection" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and copyTagsToSnapshot =
         flag "copy-tags-to-snapshot" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and domain = flag "domain" (optional string) ~doc:"STRING String"
       and domainIAMRoleName =
         flag "domain-i-a-m-role-name" (optional string) ~doc:"STRING String"
       and dBClusterIdentifier =
         flag "d-b-cluster-identifier" (required string) ~doc:"STRING String"
       and engine = flag "engine" (required string) ~doc:"STRING String"
       and masterUsername =
         flag "master-username" (required string) ~doc:"STRING String"
       and masterUserPassword =
         flag "master-user-password" (required string) ~doc:"STRING String"
       and sourceEngine =
         flag "source-engine" (required string) ~doc:"STRING String"
       and sourceEngineVersion =
         flag "source-engine-version" (required string) ~doc:"STRING String"
       and s3BucketName =
         flag "s3-bucket-name" (required string) ~doc:"STRING String"
       and s3IngestionRoleArn =
         flag "s3-ingestion-role-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_d_b_cluster_from_s3
           (Values.RestoreDBClusterFromS3Message.make
              ?availabilityZones:(Option.map
                                    ~f:Values.AvailabilityZones.of_json
                                    availabilityZones) ?backupRetentionPeriod
              ?characterSetName ?databaseName ?dBClusterParameterGroupName
              ?vpcSecurityGroupIds:(Option.map
                                      ~f:Values.VpcSecurityGroupIdList.of_json
                                      vpcSecurityGroupIds) ?dBSubnetGroupName
              ?engineVersion ?port ?optionGroupName ?preferredBackupWindow
              ?preferredMaintenanceWindow
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?storageEncrypted ?kmsKeyId ?enableIAMDatabaseAuthentication
              ?s3Prefix
              ?backtrackWindow:(Option.map ~f:Values.LongOptional.of_json
                                  backtrackWindow)
              ?enableCloudwatchLogsExports:(Option.map
                                              ~f:Values.LogTypeList.of_json
                                              enableCloudwatchLogsExports)
              ?deletionProtection ?copyTagsToSnapshot ?domain
              ?domainIAMRoleName ~dBClusterIdentifier ~engine ~masterUsername
              ~masterUserPassword ~sourceEngine ~sourceEngineVersion
              ~s3BucketName ~s3IngestionRoleArn ())
           (Some Values.RestoreDBClusterFromS3Result.to_json)
           (Some Values.RestoreDBClusterFromS3Result.error_to_json)])
let restore_d_b_cluster_from_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and availabilityZones =
         flag "availability-zones" (optional json_arg)
           ~doc:"JSON AvailabilityZones"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING String"
       and port = flag "port" (optional int) ~doc:"INT IntegerOptional"
       and dBSubnetGroupName =
         flag "d-b-subnet-group-name" (optional string) ~doc:"STRING String"
       and databaseName =
         flag "database-name" (optional string) ~doc:"STRING String"
       and optionGroupName =
         flag "option-group-name" (optional string) ~doc:"STRING String"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON VpcSecurityGroupIdList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and enableIAMDatabaseAuthentication =
         flag "enable-i-a-m-database-authentication" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and backtrackWindow =
         flag "backtrack-window" (optional json_arg) ~doc:"JSON LongOptional"
       and enableCloudwatchLogsExports =
         flag "enable-cloudwatch-logs-exports" (optional json_arg)
           ~doc:"JSON LogTypeList"
       and engineMode =
         flag "engine-mode" (optional string) ~doc:"STRING String"
       and scalingConfiguration =
         flag "scaling-configuration" (optional json_arg)
           ~doc:"JSON ScalingConfiguration"
       and dBClusterParameterGroupName =
         flag "d-b-cluster-parameter-group-name" (optional string)
           ~doc:"STRING String"
       and deletionProtection =
         flag "deletion-protection" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and copyTagsToSnapshot =
         flag "copy-tags-to-snapshot" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and domain = flag "domain" (optional string) ~doc:"STRING String"
       and domainIAMRoleName =
         flag "domain-i-a-m-role-name" (optional string) ~doc:"STRING String"
       and dBClusterInstanceClass =
         flag "d-b-cluster-instance-class" (optional string)
           ~doc:"STRING String"
       and storageType =
         flag "storage-type" (optional string) ~doc:"STRING String"
       and iops = flag "iops" (optional int) ~doc:"INT IntegerOptional"
       and publiclyAccessible =
         flag "publicly-accessible" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and dBClusterIdentifier =
         flag "d-b-cluster-identifier" (required string) ~doc:"STRING String"
       and snapshotIdentifier =
         flag "snapshot-identifier" (required string) ~doc:"STRING String"
       and engine = flag "engine" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_d_b_cluster_from_snapshot
           (Values.RestoreDBClusterFromSnapshotMessage.make
              ?availabilityZones:(Option.map
                                    ~f:Values.AvailabilityZones.of_json
                                    availabilityZones) ?engineVersion ?port
              ?dBSubnetGroupName ?databaseName ?optionGroupName
              ?vpcSecurityGroupIds:(Option.map
                                      ~f:Values.VpcSecurityGroupIdList.of_json
                                      vpcSecurityGroupIds)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?kmsKeyId
              ?enableIAMDatabaseAuthentication
              ?backtrackWindow:(Option.map ~f:Values.LongOptional.of_json
                                  backtrackWindow)
              ?enableCloudwatchLogsExports:(Option.map
                                              ~f:Values.LogTypeList.of_json
                                              enableCloudwatchLogsExports)
              ?engineMode
              ?scalingConfiguration:(Option.map
                                       ~f:Values.ScalingConfiguration.of_json
                                       scalingConfiguration)
              ?dBClusterParameterGroupName ?deletionProtection
              ?copyTagsToSnapshot ?domain ?domainIAMRoleName
              ?dBClusterInstanceClass ?storageType ?iops ?publiclyAccessible
              ~dBClusterIdentifier ~snapshotIdentifier ~engine ())
           (Some Values.RestoreDBClusterFromSnapshotResult.to_json)
           (Some Values.RestoreDBClusterFromSnapshotResult.error_to_json)])
let restore_d_b_cluster_to_point_in_time =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and restoreType =
         flag "restore-type" (optional string) ~doc:"STRING String"
       and restoreToTime =
         flag "restore-to-time" (optional json_arg) ~doc:"JSON TStamp"
       and useLatestRestorableTime =
         flag "use-latest-restorable-time" (optional bool)
           ~doc:"BOOL Boolean"
       and port = flag "port" (optional int) ~doc:"INT IntegerOptional"
       and dBSubnetGroupName =
         flag "d-b-subnet-group-name" (optional string) ~doc:"STRING String"
       and optionGroupName =
         flag "option-group-name" (optional string) ~doc:"STRING String"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON VpcSecurityGroupIdList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and enableIAMDatabaseAuthentication =
         flag "enable-i-a-m-database-authentication" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and backtrackWindow =
         flag "backtrack-window" (optional json_arg) ~doc:"JSON LongOptional"
       and enableCloudwatchLogsExports =
         flag "enable-cloudwatch-logs-exports" (optional json_arg)
           ~doc:"JSON LogTypeList"
       and dBClusterParameterGroupName =
         flag "d-b-cluster-parameter-group-name" (optional string)
           ~doc:"STRING String"
       and deletionProtection =
         flag "deletion-protection" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and copyTagsToSnapshot =
         flag "copy-tags-to-snapshot" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and domain = flag "domain" (optional string) ~doc:"STRING String"
       and domainIAMRoleName =
         flag "domain-i-a-m-role-name" (optional string) ~doc:"STRING String"
       and scalingConfiguration =
         flag "scaling-configuration" (optional json_arg)
           ~doc:"JSON ScalingConfiguration"
       and engineMode =
         flag "engine-mode" (optional string) ~doc:"STRING String"
       and dBClusterInstanceClass =
         flag "d-b-cluster-instance-class" (optional string)
           ~doc:"STRING String"
       and storageType =
         flag "storage-type" (optional string) ~doc:"STRING String"
       and publiclyAccessible =
         flag "publicly-accessible" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and iops = flag "iops" (optional int) ~doc:"INT IntegerOptional"
       and dBClusterIdentifier =
         flag "d-b-cluster-identifier" (required string) ~doc:"STRING String"
       and sourceDBClusterIdentifier =
         flag "source-d-b-cluster-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_d_b_cluster_to_point_in_time
           (Values.RestoreDBClusterToPointInTimeMessage.make ?restoreType
              ?restoreToTime:(Option.map ~f:Values.TStamp.of_json
                                restoreToTime) ?useLatestRestorableTime ?port
              ?dBSubnetGroupName ?optionGroupName
              ?vpcSecurityGroupIds:(Option.map
                                      ~f:Values.VpcSecurityGroupIdList.of_json
                                      vpcSecurityGroupIds)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?kmsKeyId
              ?enableIAMDatabaseAuthentication
              ?backtrackWindow:(Option.map ~f:Values.LongOptional.of_json
                                  backtrackWindow)
              ?enableCloudwatchLogsExports:(Option.map
                                              ~f:Values.LogTypeList.of_json
                                              enableCloudwatchLogsExports)
              ?dBClusterParameterGroupName ?deletionProtection
              ?copyTagsToSnapshot ?domain ?domainIAMRoleName
              ?scalingConfiguration:(Option.map
                                       ~f:Values.ScalingConfiguration.of_json
                                       scalingConfiguration) ?engineMode
              ?dBClusterInstanceClass ?storageType ?publiclyAccessible ?iops
              ~dBClusterIdentifier ~sourceDBClusterIdentifier ())
           (Some Values.RestoreDBClusterToPointInTimeResult.to_json)
           (Some Values.RestoreDBClusterToPointInTimeResult.error_to_json)])
let restore_d_b_instance_from_d_b_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBInstanceClass =
         flag "d-b-instance-class" (optional string) ~doc:"STRING String"
       and port = flag "port" (optional int) ~doc:"INT IntegerOptional"
       and availabilityZone =
         flag "availability-zone" (optional string) ~doc:"STRING String"
       and dBSubnetGroupName =
         flag "d-b-subnet-group-name" (optional string) ~doc:"STRING String"
       and multiAZ =
         flag "multi-a-z" (optional bool) ~doc:"BOOL BooleanOptional"
       and publiclyAccessible =
         flag "publicly-accessible" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and autoMinorVersionUpgrade =
         flag "auto-minor-version-upgrade" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and licenseModel =
         flag "license-model" (optional string) ~doc:"STRING String"
       and dBName = flag "d-b-name" (optional string) ~doc:"STRING String"
       and engine = flag "engine" (optional string) ~doc:"STRING String"
       and iops = flag "iops" (optional int) ~doc:"INT IntegerOptional"
       and optionGroupName =
         flag "option-group-name" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and storageType =
         flag "storage-type" (optional string) ~doc:"STRING String"
       and tdeCredentialArn =
         flag "tde-credential-arn" (optional string) ~doc:"STRING String"
       and tdeCredentialPassword =
         flag "tde-credential-password" (optional string)
           ~doc:"STRING String"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON VpcSecurityGroupIdList"
       and domain = flag "domain" (optional string) ~doc:"STRING String"
       and copyTagsToSnapshot =
         flag "copy-tags-to-snapshot" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and domainIAMRoleName =
         flag "domain-i-a-m-role-name" (optional string) ~doc:"STRING String"
       and enableIAMDatabaseAuthentication =
         flag "enable-i-a-m-database-authentication" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and enableCloudwatchLogsExports =
         flag "enable-cloudwatch-logs-exports" (optional json_arg)
           ~doc:"JSON LogTypeList"
       and processorFeatures =
         flag "processor-features" (optional json_arg)
           ~doc:"JSON ProcessorFeatureList"
       and useDefaultProcessorFeatures =
         flag "use-default-processor-features" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and dBParameterGroupName =
         flag "d-b-parameter-group-name" (optional string)
           ~doc:"STRING String"
       and deletionProtection =
         flag "deletion-protection" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and enableCustomerOwnedIp =
         flag "enable-customer-owned-ip" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and customIamInstanceProfile =
         flag "custom-iam-instance-profile" (optional string)
           ~doc:"STRING String"
       and backupTarget =
         flag "backup-target" (optional string) ~doc:"STRING String"
       and dBInstanceIdentifier =
         flag "d-b-instance-identifier" (required string)
           ~doc:"STRING String"
       and dBSnapshotIdentifier =
         flag "d-b-snapshot-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_d_b_instance_from_d_b_snapshot
           (Values.RestoreDBInstanceFromDBSnapshotMessage.make
              ?dBInstanceClass ?port ?availabilityZone ?dBSubnetGroupName
              ?multiAZ ?publiclyAccessible ?autoMinorVersionUpgrade
              ?licenseModel ?dBName ?engine ?iops ?optionGroupName
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?storageType
              ?tdeCredentialArn ?tdeCredentialPassword
              ?vpcSecurityGroupIds:(Option.map
                                      ~f:Values.VpcSecurityGroupIdList.of_json
                                      vpcSecurityGroupIds) ?domain
              ?copyTagsToSnapshot ?domainIAMRoleName
              ?enableIAMDatabaseAuthentication
              ?enableCloudwatchLogsExports:(Option.map
                                              ~f:Values.LogTypeList.of_json
                                              enableCloudwatchLogsExports)
              ?processorFeatures:(Option.map
                                    ~f:Values.ProcessorFeatureList.of_json
                                    processorFeatures)
              ?useDefaultProcessorFeatures ?dBParameterGroupName
              ?deletionProtection ?enableCustomerOwnedIp
              ?customIamInstanceProfile ?backupTarget ~dBInstanceIdentifier
              ~dBSnapshotIdentifier ())
           (Some Values.RestoreDBInstanceFromDBSnapshotResult.to_json)
           (Some Values.RestoreDBInstanceFromDBSnapshotResult.error_to_json)])
let restore_d_b_instance_from_s3 =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBName = flag "d-b-name" (optional string) ~doc:"STRING String"
       and allocatedStorage =
         flag "allocated-storage" (optional int) ~doc:"INT IntegerOptional"
       and masterUsername =
         flag "master-username" (optional string) ~doc:"STRING String"
       and masterUserPassword =
         flag "master-user-password" (optional string) ~doc:"STRING String"
       and dBSecurityGroups =
         flag "d-b-security-groups" (optional json_arg)
           ~doc:"JSON DBSecurityGroupNameList"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON VpcSecurityGroupIdList"
       and availabilityZone =
         flag "availability-zone" (optional string) ~doc:"STRING String"
       and dBSubnetGroupName =
         flag "d-b-subnet-group-name" (optional string) ~doc:"STRING String"
       and preferredMaintenanceWindow =
         flag "preferred-maintenance-window" (optional string)
           ~doc:"STRING String"
       and dBParameterGroupName =
         flag "d-b-parameter-group-name" (optional string)
           ~doc:"STRING String"
       and backupRetentionPeriod =
         flag "backup-retention-period" (optional int)
           ~doc:"INT IntegerOptional"
       and preferredBackupWindow =
         flag "preferred-backup-window" (optional string)
           ~doc:"STRING String"
       and port = flag "port" (optional int) ~doc:"INT IntegerOptional"
       and multiAZ =
         flag "multi-a-z" (optional bool) ~doc:"BOOL BooleanOptional"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING String"
       and autoMinorVersionUpgrade =
         flag "auto-minor-version-upgrade" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and licenseModel =
         flag "license-model" (optional string) ~doc:"STRING String"
       and iops = flag "iops" (optional int) ~doc:"INT IntegerOptional"
       and optionGroupName =
         flag "option-group-name" (optional string) ~doc:"STRING String"
       and publiclyAccessible =
         flag "publicly-accessible" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and storageType =
         flag "storage-type" (optional string) ~doc:"STRING String"
       and storageEncrypted =
         flag "storage-encrypted" (optional bool) ~doc:"BOOL BooleanOptional"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and copyTagsToSnapshot =
         flag "copy-tags-to-snapshot" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and monitoringInterval =
         flag "monitoring-interval" (optional int) ~doc:"INT IntegerOptional"
       and monitoringRoleArn =
         flag "monitoring-role-arn" (optional string) ~doc:"STRING String"
       and enableIAMDatabaseAuthentication =
         flag "enable-i-a-m-database-authentication" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and s3Prefix = flag "s3-prefix" (optional string) ~doc:"STRING String"
       and enablePerformanceInsights =
         flag "enable-performance-insights" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and performanceInsightsKMSKeyId =
         flag "performance-insights-k-m-s-key-id" (optional string)
           ~doc:"STRING String"
       and performanceInsightsRetentionPeriod =
         flag "performance-insights-retention-period" (optional int)
           ~doc:"INT IntegerOptional"
       and enableCloudwatchLogsExports =
         flag "enable-cloudwatch-logs-exports" (optional json_arg)
           ~doc:"JSON LogTypeList"
       and processorFeatures =
         flag "processor-features" (optional json_arg)
           ~doc:"JSON ProcessorFeatureList"
       and useDefaultProcessorFeatures =
         flag "use-default-processor-features" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and deletionProtection =
         flag "deletion-protection" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and maxAllocatedStorage =
         flag "max-allocated-storage" (optional int)
           ~doc:"INT IntegerOptional"
       and dBInstanceIdentifier =
         flag "d-b-instance-identifier" (required string)
           ~doc:"STRING String"
       and dBInstanceClass =
         flag "d-b-instance-class" (required string) ~doc:"STRING String"
       and engine = flag "engine" (required string) ~doc:"STRING String"
       and sourceEngine =
         flag "source-engine" (required string) ~doc:"STRING String"
       and sourceEngineVersion =
         flag "source-engine-version" (required string) ~doc:"STRING String"
       and s3BucketName =
         flag "s3-bucket-name" (required string) ~doc:"STRING String"
       and s3IngestionRoleArn =
         flag "s3-ingestion-role-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_d_b_instance_from_s3
           (Values.RestoreDBInstanceFromS3Message.make ?dBName
              ?allocatedStorage ?masterUsername ?masterUserPassword
              ?dBSecurityGroups:(Option.map
                                   ~f:Values.DBSecurityGroupNameList.of_json
                                   dBSecurityGroups)
              ?vpcSecurityGroupIds:(Option.map
                                      ~f:Values.VpcSecurityGroupIdList.of_json
                                      vpcSecurityGroupIds) ?availabilityZone
              ?dBSubnetGroupName ?preferredMaintenanceWindow
              ?dBParameterGroupName ?backupRetentionPeriod
              ?preferredBackupWindow ?port ?multiAZ ?engineVersion
              ?autoMinorVersionUpgrade ?licenseModel ?iops ?optionGroupName
              ?publiclyAccessible
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?storageType
              ?storageEncrypted ?kmsKeyId ?copyTagsToSnapshot
              ?monitoringInterval ?monitoringRoleArn
              ?enableIAMDatabaseAuthentication ?s3Prefix
              ?enablePerformanceInsights ?performanceInsightsKMSKeyId
              ?performanceInsightsRetentionPeriod
              ?enableCloudwatchLogsExports:(Option.map
                                              ~f:Values.LogTypeList.of_json
                                              enableCloudwatchLogsExports)
              ?processorFeatures:(Option.map
                                    ~f:Values.ProcessorFeatureList.of_json
                                    processorFeatures)
              ?useDefaultProcessorFeatures ?deletionProtection
              ?maxAllocatedStorage ~dBInstanceIdentifier ~dBInstanceClass
              ~engine ~sourceEngine ~sourceEngineVersion ~s3BucketName
              ~s3IngestionRoleArn ())
           (Some Values.RestoreDBInstanceFromS3Result.to_json)
           (Some Values.RestoreDBInstanceFromS3Result.error_to_json)])
let restore_d_b_instance_to_point_in_time =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceDBInstanceIdentifier =
         flag "source-d-b-instance-identifier" (optional string)
           ~doc:"STRING String"
       and restoreTime =
         flag "restore-time" (optional json_arg) ~doc:"JSON TStamp"
       and useLatestRestorableTime =
         flag "use-latest-restorable-time" (optional bool)
           ~doc:"BOOL Boolean"
       and dBInstanceClass =
         flag "d-b-instance-class" (optional string) ~doc:"STRING String"
       and port = flag "port" (optional int) ~doc:"INT IntegerOptional"
       and availabilityZone =
         flag "availability-zone" (optional string) ~doc:"STRING String"
       and dBSubnetGroupName =
         flag "d-b-subnet-group-name" (optional string) ~doc:"STRING String"
       and multiAZ =
         flag "multi-a-z" (optional bool) ~doc:"BOOL BooleanOptional"
       and publiclyAccessible =
         flag "publicly-accessible" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and autoMinorVersionUpgrade =
         flag "auto-minor-version-upgrade" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and licenseModel =
         flag "license-model" (optional string) ~doc:"STRING String"
       and dBName = flag "d-b-name" (optional string) ~doc:"STRING String"
       and engine = flag "engine" (optional string) ~doc:"STRING String"
       and iops = flag "iops" (optional int) ~doc:"INT IntegerOptional"
       and optionGroupName =
         flag "option-group-name" (optional string) ~doc:"STRING String"
       and copyTagsToSnapshot =
         flag "copy-tags-to-snapshot" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and storageType =
         flag "storage-type" (optional string) ~doc:"STRING String"
       and tdeCredentialArn =
         flag "tde-credential-arn" (optional string) ~doc:"STRING String"
       and tdeCredentialPassword =
         flag "tde-credential-password" (optional string)
           ~doc:"STRING String"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON VpcSecurityGroupIdList"
       and domain = flag "domain" (optional string) ~doc:"STRING String"
       and domainIAMRoleName =
         flag "domain-i-a-m-role-name" (optional string) ~doc:"STRING String"
       and enableIAMDatabaseAuthentication =
         flag "enable-i-a-m-database-authentication" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and enableCloudwatchLogsExports =
         flag "enable-cloudwatch-logs-exports" (optional json_arg)
           ~doc:"JSON LogTypeList"
       and processorFeatures =
         flag "processor-features" (optional json_arg)
           ~doc:"JSON ProcessorFeatureList"
       and useDefaultProcessorFeatures =
         flag "use-default-processor-features" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and dBParameterGroupName =
         flag "d-b-parameter-group-name" (optional string)
           ~doc:"STRING String"
       and deletionProtection =
         flag "deletion-protection" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and sourceDbiResourceId =
         flag "source-dbi-resource-id" (optional string) ~doc:"STRING String"
       and maxAllocatedStorage =
         flag "max-allocated-storage" (optional int)
           ~doc:"INT IntegerOptional"
       and sourceDBInstanceAutomatedBackupsArn =
         flag "source-d-b-instance-automated-backups-arn" (optional string)
           ~doc:"STRING String"
       and enableCustomerOwnedIp =
         flag "enable-customer-owned-ip" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and customIamInstanceProfile =
         flag "custom-iam-instance-profile" (optional string)
           ~doc:"STRING String"
       and backupTarget =
         flag "backup-target" (optional string) ~doc:"STRING String"
       and targetDBInstanceIdentifier =
         flag "target-d-b-instance-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_d_b_instance_to_point_in_time
           (Values.RestoreDBInstanceToPointInTimeMessage.make
              ?sourceDBInstanceIdentifier
              ?restoreTime:(Option.map ~f:Values.TStamp.of_json restoreTime)
              ?useLatestRestorableTime ?dBInstanceClass ?port
              ?availabilityZone ?dBSubnetGroupName ?multiAZ
              ?publiclyAccessible ?autoMinorVersionUpgrade ?licenseModel
              ?dBName ?engine ?iops ?optionGroupName ?copyTagsToSnapshot
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?storageType
              ?tdeCredentialArn ?tdeCredentialPassword
              ?vpcSecurityGroupIds:(Option.map
                                      ~f:Values.VpcSecurityGroupIdList.of_json
                                      vpcSecurityGroupIds) ?domain
              ?domainIAMRoleName ?enableIAMDatabaseAuthentication
              ?enableCloudwatchLogsExports:(Option.map
                                              ~f:Values.LogTypeList.of_json
                                              enableCloudwatchLogsExports)
              ?processorFeatures:(Option.map
                                    ~f:Values.ProcessorFeatureList.of_json
                                    processorFeatures)
              ?useDefaultProcessorFeatures ?dBParameterGroupName
              ?deletionProtection ?sourceDbiResourceId ?maxAllocatedStorage
              ?sourceDBInstanceAutomatedBackupsArn ?enableCustomerOwnedIp
              ?customIamInstanceProfile ?backupTarget
              ~targetDBInstanceIdentifier ())
           (Some Values.RestoreDBInstanceToPointInTimeResult.to_json)
           (Some Values.RestoreDBInstanceToPointInTimeResult.error_to_json)])
let revoke_d_b_security_group_ingress =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cIDRIP = flag "c-i-d-r-i-p" (optional string) ~doc:"STRING String"
       and eC2SecurityGroupName =
         flag "e-c2-security-group-name" (optional string)
           ~doc:"STRING String"
       and eC2SecurityGroupId =
         flag "e-c2-security-group-id" (optional string) ~doc:"STRING String"
       and eC2SecurityGroupOwnerId =
         flag "e-c2-security-group-owner-id" (optional string)
           ~doc:"STRING String"
       and dBSecurityGroupName =
         flag "d-b-security-group-name" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.revoke_d_b_security_group_ingress
           (Values.RevokeDBSecurityGroupIngressMessage.make ?cIDRIP
              ?eC2SecurityGroupName ?eC2SecurityGroupId
              ?eC2SecurityGroupOwnerId ~dBSecurityGroupName ())
           (Some Values.RevokeDBSecurityGroupIngressResult.to_json)
           (Some Values.RevokeDBSecurityGroupIngressResult.error_to_json)])
let start_activity_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applyImmediately =
         flag "apply-immediately" (optional bool) ~doc:"BOOL BooleanOptional"
       and engineNativeAuditFieldsIncluded =
         flag "engine-native-audit-fields-included" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and mode =
         flag "mode" (required json_arg) ~doc:"JSON ActivityStreamMode"
       and kmsKeyId =
         flag "kms-key-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_activity_stream
           (Values.StartActivityStreamRequest.make ?applyImmediately
              ?engineNativeAuditFieldsIncluded ~resourceArn
              ~mode:(Values.ActivityStreamMode.of_json mode) ~kmsKeyId ())
           (Some Values.StartActivityStreamResponse.to_json)
           (Some Values.StartActivityStreamResponse.error_to_json)])
let start_d_b_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBClusterIdentifier =
         flag "d-b-cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_d_b_cluster
           (Values.StartDBClusterMessage.make ~dBClusterIdentifier ())
           (Some Values.StartDBClusterResult.to_json)
           (Some Values.StartDBClusterResult.error_to_json)])
let start_d_b_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBInstanceIdentifier =
         flag "d-b-instance-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_d_b_instance
           (Values.StartDBInstanceMessage.make ~dBInstanceIdentifier ())
           (Some Values.StartDBInstanceResult.to_json)
           (Some Values.StartDBInstanceResult.error_to_json)])
let start_d_b_instance_automated_backups_replication =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and backupRetentionPeriod =
         flag "backup-retention-period" (optional int)
           ~doc:"INT IntegerOptional"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and preSignedUrl =
         flag "pre-signed-url" (optional string) ~doc:"STRING String"
       and sourceDBInstanceArn =
         flag "source-d-b-instance-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_d_b_instance_automated_backups_replication
           (Values.StartDBInstanceAutomatedBackupsReplicationMessage.make
              ?backupRetentionPeriod ?kmsKeyId ?preSignedUrl
              ~sourceDBInstanceArn ())
           (Some
              Values.StartDBInstanceAutomatedBackupsReplicationResult.to_json)
           (Some
              Values.StartDBInstanceAutomatedBackupsReplicationResult.error_to_json)])
let start_export_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and s3Prefix = flag "s3-prefix" (optional string) ~doc:"STRING String"
       and exportOnly =
         flag "export-only" (optional json_arg) ~doc:"JSON StringList"
       and exportTaskIdentifier =
         flag "export-task-identifier" (required string) ~doc:"STRING String"
       and sourceArn =
         flag "source-arn" (required string) ~doc:"STRING String"
       and s3BucketName =
         flag "s3-bucket-name" (required string) ~doc:"STRING String"
       and iamRoleArn =
         flag "iam-role-arn" (required string) ~doc:"STRING String"
       and kmsKeyId =
         flag "kms-key-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_export_task
           (Values.StartExportTaskMessage.make ?s3Prefix
              ?exportOnly:(Option.map ~f:Values.StringList.of_json exportOnly)
              ~exportTaskIdentifier ~sourceArn ~s3BucketName ~iamRoleArn
              ~kmsKeyId ()) (Some Values.ExportTask.to_json)
           (Some Values.ExportTask.error_to_json)])
let stop_activity_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applyImmediately =
         flag "apply-immediately" (optional bool) ~doc:"BOOL BooleanOptional"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_activity_stream
           (Values.StopActivityStreamRequest.make ?applyImmediately
              ~resourceArn ())
           (Some Values.StopActivityStreamResponse.to_json)
           (Some Values.StopActivityStreamResponse.error_to_json)])
let stop_d_b_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBClusterIdentifier =
         flag "d-b-cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_d_b_cluster
           (Values.StopDBClusterMessage.make ~dBClusterIdentifier ())
           (Some Values.StopDBClusterResult.to_json)
           (Some Values.StopDBClusterResult.error_to_json)])
let stop_d_b_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBSnapshotIdentifier =
         flag "d-b-snapshot-identifier" (optional string)
           ~doc:"STRING String"
       and dBInstanceIdentifier =
         flag "d-b-instance-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_d_b_instance
           (Values.StopDBInstanceMessage.make ?dBSnapshotIdentifier
              ~dBInstanceIdentifier ())
           (Some Values.StopDBInstanceResult.to_json)
           (Some Values.StopDBInstanceResult.error_to_json)])
let stop_d_b_instance_automated_backups_replication =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceDBInstanceArn =
         flag "source-d-b-instance-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_d_b_instance_automated_backups_replication
           (Values.StopDBInstanceAutomatedBackupsReplicationMessage.make
              ~sourceDBInstanceArn ())
           (Some
              Values.StopDBInstanceAutomatedBackupsReplicationResult.to_json)
           (Some
              Values.StopDBInstanceAutomatedBackupsReplicationResult.error_to_json)])
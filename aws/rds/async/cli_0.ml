(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_role_to_d_b_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and featureName =
         flag "feature-name" (optional string) ~doc:"STRING String"
       and dBClusterIdentifier =
         flag "d-b-cluster-identifier" (required string) ~doc:"STRING String"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_role_to_d_b_cluster
           (Values.AddRoleToDBClusterMessage.make ?featureName
              ~dBClusterIdentifier ~roleArn ()) None None])
let add_role_to_d_b_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBInstanceIdentifier =
         flag "d-b-instance-identifier" (required string)
           ~doc:"STRING String"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING String"
       and featureName =
         flag "feature-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_role_to_d_b_instance
           (Values.AddRoleToDBInstanceMessage.make ~dBInstanceIdentifier
              ~roleArn ~featureName ()) None None])
let add_source_identifier_to_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subscriptionName =
         flag "subscription-name" (required string) ~doc:"STRING String"
       and sourceIdentifier =
         flag "source-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_source_identifier_to_subscription
           (Values.AddSourceIdentifierToSubscriptionMessage.make
              ~subscriptionName ~sourceIdentifier ())
           (Some Values.AddSourceIdentifierToSubscriptionResult.to_json)
           (Some Values.AddSourceIdentifierToSubscriptionResult.error_to_json)])
let add_tags_to_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceName =
         flag "resource-name" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_tags_to_resource
           (Values.AddTagsToResourceMessage.make ~resourceName
              ~tags:(Values.TagList.of_json tags) ()) None None])
let apply_pending_maintenance_action =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceIdentifier =
         flag "resource-identifier" (required string) ~doc:"STRING String"
       and applyAction =
         flag "apply-action" (required string) ~doc:"STRING String"
       and optInType =
         flag "opt-in-type" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.apply_pending_maintenance_action
           (Values.ApplyPendingMaintenanceActionMessage.make
              ~resourceIdentifier ~applyAction ~optInType ())
           (Some Values.ApplyPendingMaintenanceActionResult.to_json)
           (Some Values.ApplyPendingMaintenanceActionResult.error_to_json)])
let authorize_d_b_security_group_ingress =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cIDRIP = flag "c-i-d-r-i-p" (optional string) ~doc:"STRING String"
       and eC2SecurityGroupName =
         flag "e-c2-security-group-name" (optional string)
           ~doc:"STRING String"
       and eC2SecurityGroupId =
         flag "e-c2-security-group-id" (optional string) ~doc:"STRING String"
       and eC2SecurityGroupOwnerId =
         flag "e-c2-security-group-owner-id" (optional string)
           ~doc:"STRING String"
       and dBSecurityGroupName =
         flag "d-b-security-group-name" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.authorize_d_b_security_group_ingress
           (Values.AuthorizeDBSecurityGroupIngressMessage.make ?cIDRIP
              ?eC2SecurityGroupName ?eC2SecurityGroupId
              ?eC2SecurityGroupOwnerId ~dBSecurityGroupName ())
           (Some Values.AuthorizeDBSecurityGroupIngressResult.to_json)
           (Some Values.AuthorizeDBSecurityGroupIngressResult.error_to_json)])
let backtrack_d_b_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and force = flag "force" (optional bool) ~doc:"BOOL BooleanOptional"
       and useEarliestTimeOnPointInTimeUnavailable =
         flag "use-earliest-time-on-point-in-time-unavailable"
           (optional bool) ~doc:"BOOL BooleanOptional"
       and dBClusterIdentifier =
         flag "d-b-cluster-identifier" (required string) ~doc:"STRING String"
       and backtrackTo =
         flag "backtrack-to" (required json_arg) ~doc:"JSON TStamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.backtrack_d_b_cluster
           (Values.BacktrackDBClusterMessage.make ?force
              ?useEarliestTimeOnPointInTimeUnavailable ~dBClusterIdentifier
              ~backtrackTo:(Values.TStamp.of_json backtrackTo) ())
           (Some Values.DBClusterBacktrack.to_json)
           (Some Values.DBClusterBacktrack.error_to_json)])
let cancel_export_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and exportTaskIdentifier =
         flag "export-task-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_export_task
           (Values.CancelExportTaskMessage.make ~exportTaskIdentifier ())
           (Some Values.ExportTask.to_json)
           (Some Values.ExportTask.error_to_json)])
let copy_d_b_cluster_parameter_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and sourceDBClusterParameterGroupIdentifier =
         flag "source-d-b-cluster-parameter-group-identifier"
           (required string) ~doc:"STRING String"
       and targetDBClusterParameterGroupIdentifier =
         flag "target-d-b-cluster-parameter-group-identifier"
           (required string) ~doc:"STRING String"
       and targetDBClusterParameterGroupDescription =
         flag "target-d-b-cluster-parameter-group-description"
           (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.copy_d_b_cluster_parameter_group
           (Values.CopyDBClusterParameterGroupMessage.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~sourceDBClusterParameterGroupIdentifier
              ~targetDBClusterParameterGroupIdentifier
              ~targetDBClusterParameterGroupDescription ())
           (Some Values.CopyDBClusterParameterGroupResult.to_json)
           (Some Values.CopyDBClusterParameterGroupResult.error_to_json)])
let copy_d_b_cluster_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and preSignedUrl =
         flag "pre-signed-url" (optional string) ~doc:"STRING String"
       and copyTags =
         flag "copy-tags" (optional bool) ~doc:"BOOL BooleanOptional"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and sourceDBClusterSnapshotIdentifier =
         flag "source-d-b-cluster-snapshot-identifier" (required string)
           ~doc:"STRING String"
       and targetDBClusterSnapshotIdentifier =
         flag "target-d-b-cluster-snapshot-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.copy_d_b_cluster_snapshot
           (Values.CopyDBClusterSnapshotMessage.make ?kmsKeyId ?preSignedUrl
              ?copyTags ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~sourceDBClusterSnapshotIdentifier
              ~targetDBClusterSnapshotIdentifier ())
           (Some Values.CopyDBClusterSnapshotResult.to_json)
           (Some Values.CopyDBClusterSnapshotResult.error_to_json)])
let copy_d_b_parameter_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and sourceDBParameterGroupIdentifier =
         flag "source-d-b-parameter-group-identifier" (required string)
           ~doc:"STRING String"
       and targetDBParameterGroupIdentifier =
         flag "target-d-b-parameter-group-identifier" (required string)
           ~doc:"STRING String"
       and targetDBParameterGroupDescription =
         flag "target-d-b-parameter-group-description" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.copy_d_b_parameter_group
           (Values.CopyDBParameterGroupMessage.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~sourceDBParameterGroupIdentifier
              ~targetDBParameterGroupIdentifier
              ~targetDBParameterGroupDescription ())
           (Some Values.CopyDBParameterGroupResult.to_json)
           (Some Values.CopyDBParameterGroupResult.error_to_json)])
let copy_d_b_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and copyTags =
         flag "copy-tags" (optional bool) ~doc:"BOOL BooleanOptional"
       and preSignedUrl =
         flag "pre-signed-url" (optional string) ~doc:"STRING String"
       and optionGroupName =
         flag "option-group-name" (optional string) ~doc:"STRING String"
       and targetCustomAvailabilityZone =
         flag "target-custom-availability-zone" (optional string)
           ~doc:"STRING String"
       and sourceDBSnapshotIdentifier =
         flag "source-d-b-snapshot-identifier" (required string)
           ~doc:"STRING String"
       and targetDBSnapshotIdentifier =
         flag "target-d-b-snapshot-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.copy_d_b_snapshot
           (Values.CopyDBSnapshotMessage.make ?kmsKeyId
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?copyTags
              ?preSignedUrl ?optionGroupName ?targetCustomAvailabilityZone
              ~sourceDBSnapshotIdentifier ~targetDBSnapshotIdentifier ())
           (Some Values.CopyDBSnapshotResult.to_json)
           (Some Values.CopyDBSnapshotResult.error_to_json)])
let copy_option_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and sourceOptionGroupIdentifier =
         flag "source-option-group-identifier" (required string)
           ~doc:"STRING String"
       and targetOptionGroupIdentifier =
         flag "target-option-group-identifier" (required string)
           ~doc:"STRING String"
       and targetOptionGroupDescription =
         flag "target-option-group-description" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.copy_option_group
           (Values.CopyOptionGroupMessage.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~sourceOptionGroupIdentifier ~targetOptionGroupIdentifier
              ~targetOptionGroupDescription ())
           (Some Values.CopyOptionGroupResult.to_json)
           (Some Values.CopyOptionGroupResult.error_to_json)])
let create_custom_availability_zone =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and existingVpnId =
         flag "existing-vpn-id" (optional string) ~doc:"STRING String"
       and newVpnTunnelName =
         flag "new-vpn-tunnel-name" (optional string) ~doc:"STRING String"
       and vpnTunnelOriginatorIP =
         flag "vpn-tunnel-originator-i-p" (optional string)
           ~doc:"STRING String"
       and customAvailabilityZoneName =
         flag "custom-availability-zone-name" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_custom_availability_zone
           (Values.CreateCustomAvailabilityZoneMessage.make ?existingVpnId
              ?newVpnTunnelName ?vpnTunnelOriginatorIP
              ~customAvailabilityZoneName ())
           (Some Values.CreateCustomAvailabilityZoneResult.to_json)
           (Some Values.CreateCustomAvailabilityZoneResult.error_to_json)])
let create_custom_d_b_engine_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and databaseInstallationFilesS3Prefix =
         flag "database-installation-files-s3-prefix" (optional string)
           ~doc:"STRING String255"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and engine =
         flag "engine" (required string) ~doc:"STRING CustomEngineName"
       and engineVersion =
         flag "engine-version" (required string)
           ~doc:"STRING CustomEngineVersion"
       and databaseInstallationFilesS3BucketName =
         flag "database-installation-files-s3-bucket-name" (required string)
           ~doc:"STRING BucketName"
       and kMSKeyId =
         flag "k-m-s-key-id" (required string) ~doc:"STRING KmsKeyIdOrArn"
       and manifest =
         flag "manifest" (required string)
           ~doc:"STRING CustomDBEngineVersionManifest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_custom_d_b_engine_version
           (Values.CreateCustomDBEngineVersionMessage.make
              ?databaseInstallationFilesS3Prefix ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~engine
              ~engineVersion ~databaseInstallationFilesS3BucketName ~kMSKeyId
              ~manifest ()) (Some Values.DBEngineVersion.to_json)
           (Some Values.DBEngineVersion.error_to_json)])
let create_d_b_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and availabilityZones =
         flag "availability-zones" (optional json_arg)
           ~doc:"JSON AvailabilityZones"
       and backupRetentionPeriod =
         flag "backup-retention-period" (optional int)
           ~doc:"INT IntegerOptional"
       and characterSetName =
         flag "character-set-name" (optional string) ~doc:"STRING String"
       and databaseName =
         flag "database-name" (optional string) ~doc:"STRING String"
       and dBClusterParameterGroupName =
         flag "d-b-cluster-parameter-group-name" (optional string)
           ~doc:"STRING String"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON VpcSecurityGroupIdList"
       and dBSubnetGroupName =
         flag "d-b-subnet-group-name" (optional string) ~doc:"STRING String"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING String"
       and port = flag "port" (optional int) ~doc:"INT IntegerOptional"
       and masterUsername =
         flag "master-username" (optional string) ~doc:"STRING String"
       and masterUserPassword =
         flag "master-user-password" (optional string) ~doc:"STRING String"
       and optionGroupName =
         flag "option-group-name" (optional string) ~doc:"STRING String"
       and preferredBackupWindow =
         flag "preferred-backup-window" (optional string)
           ~doc:"STRING String"
       and preferredMaintenanceWindow =
         flag "preferred-maintenance-window" (optional string)
           ~doc:"STRING String"
       and replicationSourceIdentifier =
         flag "replication-source-identifier" (optional string)
           ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and storageEncrypted =
         flag "storage-encrypted" (optional bool) ~doc:"BOOL BooleanOptional"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and preSignedUrl =
         flag "pre-signed-url" (optional string) ~doc:"STRING String"
       and enableIAMDatabaseAuthentication =
         flag "enable-i-a-m-database-authentication" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and backtrackWindow =
         flag "backtrack-window" (optional json_arg) ~doc:"JSON LongOptional"
       and enableCloudwatchLogsExports =
         flag "enable-cloudwatch-logs-exports" (optional json_arg)
           ~doc:"JSON LogTypeList"
       and engineMode =
         flag "engine-mode" (optional string) ~doc:"STRING String"
       and scalingConfiguration =
         flag "scaling-configuration" (optional json_arg)
           ~doc:"JSON ScalingConfiguration"
       and deletionProtection =
         flag "deletion-protection" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and globalClusterIdentifier =
         flag "global-cluster-identifier" (optional string)
           ~doc:"STRING String"
       and enableHttpEndpoint =
         flag "enable-http-endpoint" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and copyTagsToSnapshot =
         flag "copy-tags-to-snapshot" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and domain = flag "domain" (optional string) ~doc:"STRING String"
       and domainIAMRoleName =
         flag "domain-i-a-m-role-name" (optional string) ~doc:"STRING String"
       and enableGlobalWriteForwarding =
         flag "enable-global-write-forwarding" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and dBClusterInstanceClass =
         flag "d-b-cluster-instance-class" (optional string)
           ~doc:"STRING String"
       and allocatedStorage =
         flag "allocated-storage" (optional int) ~doc:"INT IntegerOptional"
       and storageType =
         flag "storage-type" (optional string) ~doc:"STRING String"
       and iops = flag "iops" (optional int) ~doc:"INT IntegerOptional"
       and publiclyAccessible =
         flag "publicly-accessible" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and autoMinorVersionUpgrade =
         flag "auto-minor-version-upgrade" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and monitoringInterval =
         flag "monitoring-interval" (optional int) ~doc:"INT IntegerOptional"
       and monitoringRoleArn =
         flag "monitoring-role-arn" (optional string) ~doc:"STRING String"
       and enablePerformanceInsights =
         flag "enable-performance-insights" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and performanceInsightsKMSKeyId =
         flag "performance-insights-k-m-s-key-id" (optional string)
           ~doc:"STRING String"
       and performanceInsightsRetentionPeriod =
         flag "performance-insights-retention-period" (optional int)
           ~doc:"INT IntegerOptional"
       and dBClusterIdentifier =
         flag "d-b-cluster-identifier" (required string) ~doc:"STRING String"
       and engine = flag "engine" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_d_b_cluster
           (Values.CreateDBClusterMessage.make
              ?availabilityZones:(Option.map
                                    ~f:Values.AvailabilityZones.of_json
                                    availabilityZones) ?backupRetentionPeriod
              ?characterSetName ?databaseName ?dBClusterParameterGroupName
              ?vpcSecurityGroupIds:(Option.map
                                      ~f:Values.VpcSecurityGroupIdList.of_json
                                      vpcSecurityGroupIds) ?dBSubnetGroupName
              ?engineVersion ?port ?masterUsername ?masterUserPassword
              ?optionGroupName ?preferredBackupWindow
              ?preferredMaintenanceWindow ?replicationSourceIdentifier
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?storageEncrypted ?kmsKeyId ?preSignedUrl
              ?enableIAMDatabaseAuthentication
              ?backtrackWindow:(Option.map ~f:Values.LongOptional.of_json
                                  backtrackWindow)
              ?enableCloudwatchLogsExports:(Option.map
                                              ~f:Values.LogTypeList.of_json
                                              enableCloudwatchLogsExports)
              ?engineMode
              ?scalingConfiguration:(Option.map
                                       ~f:Values.ScalingConfiguration.of_json
                                       scalingConfiguration)
              ?deletionProtection ?globalClusterIdentifier
              ?enableHttpEndpoint ?copyTagsToSnapshot ?domain
              ?domainIAMRoleName ?enableGlobalWriteForwarding
              ?dBClusterInstanceClass ?allocatedStorage ?storageType ?iops
              ?publiclyAccessible ?autoMinorVersionUpgrade
              ?monitoringInterval ?monitoringRoleArn
              ?enablePerformanceInsights ?performanceInsightsKMSKeyId
              ?performanceInsightsRetentionPeriod ~dBClusterIdentifier
              ~engine ()) (Some Values.CreateDBClusterResult.to_json)
           (Some Values.CreateDBClusterResult.error_to_json)])
let create_d_b_cluster_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and staticMembers =
         flag "static-members" (optional json_arg) ~doc:"JSON StringList"
       and excludedMembers =
         flag "excluded-members" (optional json_arg) ~doc:"JSON StringList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and dBClusterIdentifier =
         flag "d-b-cluster-identifier" (required string) ~doc:"STRING String"
       and dBClusterEndpointIdentifier =
         flag "d-b-cluster-endpoint-identifier" (required string)
           ~doc:"STRING String"
       and endpointType =
         flag "endpoint-type" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_d_b_cluster_endpoint
           (Values.CreateDBClusterEndpointMessage.make
              ?staticMembers:(Option.map ~f:Values.StringList.of_json
                                staticMembers)
              ?excludedMembers:(Option.map ~f:Values.StringList.of_json
                                  excludedMembers)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~dBClusterIdentifier ~dBClusterEndpointIdentifier ~endpointType
              ()) (Some Values.DBClusterEndpoint.to_json)
           (Some Values.DBClusterEndpoint.error_to_json)])
let create_d_b_cluster_parameter_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and dBClusterParameterGroupName =
         flag "d-b-cluster-parameter-group-name" (required string)
           ~doc:"STRING String"
       and dBParameterGroupFamily =
         flag "d-b-parameter-group-family" (required string)
           ~doc:"STRING String"
       and description =
         flag "description" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_d_b_cluster_parameter_group
           (Values.CreateDBClusterParameterGroupMessage.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~dBClusterParameterGroupName ~dBParameterGroupFamily
              ~description ())
           (Some Values.CreateDBClusterParameterGroupResult.to_json)
           (Some Values.CreateDBClusterParameterGroupResult.error_to_json)])
let create_d_b_cluster_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and dBClusterSnapshotIdentifier =
         flag "d-b-cluster-snapshot-identifier" (required string)
           ~doc:"STRING String"
       and dBClusterIdentifier =
         flag "d-b-cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_d_b_cluster_snapshot
           (Values.CreateDBClusterSnapshotMessage.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~dBClusterSnapshotIdentifier ~dBClusterIdentifier ())
           (Some Values.CreateDBClusterSnapshotResult.to_json)
           (Some Values.CreateDBClusterSnapshotResult.error_to_json)])
let create_d_b_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBName = flag "d-b-name" (optional string) ~doc:"STRING String"
       and allocatedStorage =
         flag "allocated-storage" (optional int) ~doc:"INT IntegerOptional"
       and masterUsername =
         flag "master-username" (optional string) ~doc:"STRING String"
       and masterUserPassword =
         flag "master-user-password" (optional string) ~doc:"STRING String"
       and dBSecurityGroups =
         flag "d-b-security-groups" (optional json_arg)
           ~doc:"JSON DBSecurityGroupNameList"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON VpcSecurityGroupIdList"
       and availabilityZone =
         flag "availability-zone" (optional string) ~doc:"STRING String"
       and dBSubnetGroupName =
         flag "d-b-subnet-group-name" (optional string) ~doc:"STRING String"
       and preferredMaintenanceWindow =
         flag "preferred-maintenance-window" (optional string)
           ~doc:"STRING String"
       and dBParameterGroupName =
         flag "d-b-parameter-group-name" (optional string)
           ~doc:"STRING String"
       and backupRetentionPeriod =
         flag "backup-retention-period" (optional int)
           ~doc:"INT IntegerOptional"
       and preferredBackupWindow =
         flag "preferred-backup-window" (optional string)
           ~doc:"STRING String"
       and port = flag "port" (optional int) ~doc:"INT IntegerOptional"
       and multiAZ =
         flag "multi-a-z" (optional bool) ~doc:"BOOL BooleanOptional"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING String"
       and autoMinorVersionUpgrade =
         flag "auto-minor-version-upgrade" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and licenseModel =
         flag "license-model" (optional string) ~doc:"STRING String"
       and iops = flag "iops" (optional int) ~doc:"INT IntegerOptional"
       and optionGroupName =
         flag "option-group-name" (optional string) ~doc:"STRING String"
       and characterSetName =
         flag "character-set-name" (optional string) ~doc:"STRING String"
       and ncharCharacterSetName =
         flag "nchar-character-set-name" (optional string)
           ~doc:"STRING String"
       and publiclyAccessible =
         flag "publicly-accessible" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and dBClusterIdentifier =
         flag "d-b-cluster-identifier" (optional string) ~doc:"STRING String"
       and storageType =
         flag "storage-type" (optional string) ~doc:"STRING String"
       and tdeCredentialArn =
         flag "tde-credential-arn" (optional string) ~doc:"STRING String"
       and tdeCredentialPassword =
         flag "tde-credential-password" (optional string)
           ~doc:"STRING String"
       and storageEncrypted =
         flag "storage-encrypted" (optional bool) ~doc:"BOOL BooleanOptional"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and domain = flag "domain" (optional string) ~doc:"STRING String"
       and copyTagsToSnapshot =
         flag "copy-tags-to-snapshot" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and monitoringInterval =
         flag "monitoring-interval" (optional int) ~doc:"INT IntegerOptional"
       and monitoringRoleArn =
         flag "monitoring-role-arn" (optional string) ~doc:"STRING String"
       and domainIAMRoleName =
         flag "domain-i-a-m-role-name" (optional string) ~doc:"STRING String"
       and promotionTier =
         flag "promotion-tier" (optional int) ~doc:"INT IntegerOptional"
       and timezone = flag "timezone" (optional string) ~doc:"STRING String"
       and enableIAMDatabaseAuthentication =
         flag "enable-i-a-m-database-authentication" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and enablePerformanceInsights =
         flag "enable-performance-insights" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and performanceInsightsKMSKeyId =
         flag "performance-insights-k-m-s-key-id" (optional string)
           ~doc:"STRING String"
       and performanceInsightsRetentionPeriod =
         flag "performance-insights-retention-period" (optional int)
           ~doc:"INT IntegerOptional"
       and enableCloudwatchLogsExports =
         flag "enable-cloudwatch-logs-exports" (optional json_arg)
           ~doc:"JSON LogTypeList"
       and processorFeatures =
         flag "processor-features" (optional json_arg)
           ~doc:"JSON ProcessorFeatureList"
       and deletionProtection =
         flag "deletion-protection" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and maxAllocatedStorage =
         flag "max-allocated-storage" (optional int)
           ~doc:"INT IntegerOptional"
       and enableCustomerOwnedIp =
         flag "enable-customer-owned-ip" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and customIamInstanceProfile =
         flag "custom-iam-instance-profile" (optional string)
           ~doc:"STRING String"
       and backupTarget =
         flag "backup-target" (optional string) ~doc:"STRING String"
       and dBInstanceIdentifier =
         flag "d-b-instance-identifier" (required string)
           ~doc:"STRING String"
       and dBInstanceClass =
         flag "d-b-instance-class" (required string) ~doc:"STRING String"
       and engine = flag "engine" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_d_b_instance
           (Values.CreateDBInstanceMessage.make ?dBName ?allocatedStorage
              ?masterUsername ?masterUserPassword
              ?dBSecurityGroups:(Option.map
                                   ~f:Values.DBSecurityGroupNameList.of_json
                                   dBSecurityGroups)
              ?vpcSecurityGroupIds:(Option.map
                                      ~f:Values.VpcSecurityGroupIdList.of_json
                                      vpcSecurityGroupIds) ?availabilityZone
              ?dBSubnetGroupName ?preferredMaintenanceWindow
              ?dBParameterGroupName ?backupRetentionPeriod
              ?preferredBackupWindow ?port ?multiAZ ?engineVersion
              ?autoMinorVersionUpgrade ?licenseModel ?iops ?optionGroupName
              ?characterSetName ?ncharCharacterSetName ?publiclyAccessible
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?dBClusterIdentifier ?storageType ?tdeCredentialArn
              ?tdeCredentialPassword ?storageEncrypted ?kmsKeyId ?domain
              ?copyTagsToSnapshot ?monitoringInterval ?monitoringRoleArn
              ?domainIAMRoleName ?promotionTier ?timezone
              ?enableIAMDatabaseAuthentication ?enablePerformanceInsights
              ?performanceInsightsKMSKeyId
              ?performanceInsightsRetentionPeriod
              ?enableCloudwatchLogsExports:(Option.map
                                              ~f:Values.LogTypeList.of_json
                                              enableCloudwatchLogsExports)
              ?processorFeatures:(Option.map
                                    ~f:Values.ProcessorFeatureList.of_json
                                    processorFeatures) ?deletionProtection
              ?maxAllocatedStorage ?enableCustomerOwnedIp
              ?customIamInstanceProfile ?backupTarget ~dBInstanceIdentifier
              ~dBInstanceClass ~engine ())
           (Some Values.CreateDBInstanceResult.to_json)
           (Some Values.CreateDBInstanceResult.error_to_json)])
let create_d_b_instance_read_replica =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBInstanceClass =
         flag "d-b-instance-class" (optional string) ~doc:"STRING String"
       and availabilityZone =
         flag "availability-zone" (optional string) ~doc:"STRING String"
       and port = flag "port" (optional int) ~doc:"INT IntegerOptional"
       and multiAZ =
         flag "multi-a-z" (optional bool) ~doc:"BOOL BooleanOptional"
       and autoMinorVersionUpgrade =
         flag "auto-minor-version-upgrade" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and iops = flag "iops" (optional int) ~doc:"INT IntegerOptional"
       and optionGroupName =
         flag "option-group-name" (optional string) ~doc:"STRING String"
       and dBParameterGroupName =
         flag "d-b-parameter-group-name" (optional string)
           ~doc:"STRING String"
       and publiclyAccessible =
         flag "publicly-accessible" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and dBSubnetGroupName =
         flag "d-b-subnet-group-name" (optional string) ~doc:"STRING String"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON VpcSecurityGroupIdList"
       and storageType =
         flag "storage-type" (optional string) ~doc:"STRING String"
       and copyTagsToSnapshot =
         flag "copy-tags-to-snapshot" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and monitoringInterval =
         flag "monitoring-interval" (optional int) ~doc:"INT IntegerOptional"
       and monitoringRoleArn =
         flag "monitoring-role-arn" (optional string) ~doc:"STRING String"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING String"
       and preSignedUrl =
         flag "pre-signed-url" (optional string) ~doc:"STRING String"
       and enableIAMDatabaseAuthentication =
         flag "enable-i-a-m-database-authentication" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and enablePerformanceInsights =
         flag "enable-performance-insights" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and performanceInsightsKMSKeyId =
         flag "performance-insights-k-m-s-key-id" (optional string)
           ~doc:"STRING String"
       and performanceInsightsRetentionPeriod =
         flag "performance-insights-retention-period" (optional int)
           ~doc:"INT IntegerOptional"
       and enableCloudwatchLogsExports =
         flag "enable-cloudwatch-logs-exports" (optional json_arg)
           ~doc:"JSON LogTypeList"
       and processorFeatures =
         flag "processor-features" (optional json_arg)
           ~doc:"JSON ProcessorFeatureList"
       and useDefaultProcessorFeatures =
         flag "use-default-processor-features" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and deletionProtection =
         flag "deletion-protection" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and domain = flag "domain" (optional string) ~doc:"STRING String"
       and domainIAMRoleName =
         flag "domain-i-a-m-role-name" (optional string) ~doc:"STRING String"
       and replicaMode =
         flag "replica-mode" (optional json_arg) ~doc:"JSON ReplicaMode"
       and maxAllocatedStorage =
         flag "max-allocated-storage" (optional int)
           ~doc:"INT IntegerOptional"
       and customIamInstanceProfile =
         flag "custom-iam-instance-profile" (optional string)
           ~doc:"STRING String"
       and dBInstanceIdentifier =
         flag "d-b-instance-identifier" (required string)
           ~doc:"STRING String"
       and sourceDBInstanceIdentifier =
         flag "source-d-b-instance-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_d_b_instance_read_replica
           (Values.CreateDBInstanceReadReplicaMessage.make ?dBInstanceClass
              ?availabilityZone ?port ?multiAZ ?autoMinorVersionUpgrade ?iops
              ?optionGroupName ?dBParameterGroupName ?publiclyAccessible
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?dBSubnetGroupName
              ?vpcSecurityGroupIds:(Option.map
                                      ~f:Values.VpcSecurityGroupIdList.of_json
                                      vpcSecurityGroupIds) ?storageType
              ?copyTagsToSnapshot ?monitoringInterval ?monitoringRoleArn
              ?kmsKeyId ?preSignedUrl ?enableIAMDatabaseAuthentication
              ?enablePerformanceInsights ?performanceInsightsKMSKeyId
              ?performanceInsightsRetentionPeriod
              ?enableCloudwatchLogsExports:(Option.map
                                              ~f:Values.LogTypeList.of_json
                                              enableCloudwatchLogsExports)
              ?processorFeatures:(Option.map
                                    ~f:Values.ProcessorFeatureList.of_json
                                    processorFeatures)
              ?useDefaultProcessorFeatures ?deletionProtection ?domain
              ?domainIAMRoleName
              ?replicaMode:(Option.map ~f:Values.ReplicaMode.of_json
                              replicaMode) ?maxAllocatedStorage
              ?customIamInstanceProfile ~dBInstanceIdentifier
              ~sourceDBInstanceIdentifier ())
           (Some Values.CreateDBInstanceReadReplicaResult.to_json)
           (Some Values.CreateDBInstanceReadReplicaResult.error_to_json)])
let create_d_b_parameter_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and dBParameterGroupName =
         flag "d-b-parameter-group-name" (required string)
           ~doc:"STRING String"
       and dBParameterGroupFamily =
         flag "d-b-parameter-group-family" (required string)
           ~doc:"STRING String"
       and description =
         flag "description" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_d_b_parameter_group
           (Values.CreateDBParameterGroupMessage.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~dBParameterGroupName ~dBParameterGroupFamily ~description ())
           (Some Values.CreateDBParameterGroupResult.to_json)
           (Some Values.CreateDBParameterGroupResult.error_to_json)])
let create_d_b_proxy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON StringList"
       and requireTLS =
         flag "require-t-l-s" (optional bool) ~doc:"BOOL Boolean"
       and idleClientTimeout =
         flag "idle-client-timeout" (optional int) ~doc:"INT IntegerOptional"
       and debugLogging =
         flag "debug-logging" (optional bool) ~doc:"BOOL Boolean"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and dBProxyName =
         flag "d-b-proxy-name" (required string) ~doc:"STRING String"
       and engineFamily =
         flag "engine-family" (required json_arg) ~doc:"JSON EngineFamily"
       and auth =
         flag "auth" (required json_arg) ~doc:"JSON UserAuthConfigList"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING String"
       and vpcSubnetIds =
         flag "vpc-subnet-ids" (required json_arg) ~doc:"JSON StringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_d_b_proxy
           (Values.CreateDBProxyRequest.make
              ?vpcSecurityGroupIds:(Option.map ~f:Values.StringList.of_json
                                      vpcSecurityGroupIds) ?requireTLS
              ?idleClientTimeout ?debugLogging
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~dBProxyName
              ~engineFamily:(Values.EngineFamily.of_json engineFamily)
              ~auth:(Values.UserAuthConfigList.of_json auth) ~roleArn
              ~vpcSubnetIds:(Values.StringList.of_json vpcSubnetIds) ())
           (Some Values.CreateDBProxyResponse.to_json)
           (Some Values.CreateDBProxyResponse.error_to_json)])
let create_d_b_proxy_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (optional json_arg)
           ~doc:"JSON StringList"
       and targetRole =
         flag "target-role" (optional json_arg)
           ~doc:"JSON DBProxyEndpointTargetRole"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and dBProxyName =
         flag "d-b-proxy-name" (required string) ~doc:"STRING DBProxyName"
       and dBProxyEndpointName =
         flag "d-b-proxy-endpoint-name" (required string)
           ~doc:"STRING DBProxyEndpointName"
       and vpcSubnetIds =
         flag "vpc-subnet-ids" (required json_arg) ~doc:"JSON StringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_d_b_proxy_endpoint
           (Values.CreateDBProxyEndpointRequest.make
              ?vpcSecurityGroupIds:(Option.map ~f:Values.StringList.of_json
                                      vpcSecurityGroupIds)
              ?targetRole:(Option.map
                             ~f:Values.DBProxyEndpointTargetRole.of_json
                             targetRole)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~dBProxyName
              ~dBProxyEndpointName
              ~vpcSubnetIds:(Values.StringList.of_json vpcSubnetIds) ())
           (Some Values.CreateDBProxyEndpointResponse.to_json)
           (Some Values.CreateDBProxyEndpointResponse.error_to_json)])
let create_d_b_security_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and dBSecurityGroupName =
         flag "d-b-security-group-name" (required string)
           ~doc:"STRING String"
       and dBSecurityGroupDescription =
         flag "d-b-security-group-description" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_d_b_security_group
           (Values.CreateDBSecurityGroupMessage.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~dBSecurityGroupName ~dBSecurityGroupDescription ())
           (Some Values.CreateDBSecurityGroupResult.to_json)
           (Some Values.CreateDBSecurityGroupResult.error_to_json)])
let create_d_b_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and dBSnapshotIdentifier =
         flag "d-b-snapshot-identifier" (required string)
           ~doc:"STRING String"
       and dBInstanceIdentifier =
         flag "d-b-instance-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_d_b_snapshot
           (Values.CreateDBSnapshotMessage.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~dBSnapshotIdentifier ~dBInstanceIdentifier ())
           (Some Values.CreateDBSnapshotResult.to_json)
           (Some Values.CreateDBSnapshotResult.error_to_json)])
let create_d_b_subnet_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and dBSubnetGroupName =
         flag "d-b-subnet-group-name" (required string) ~doc:"STRING String"
       and dBSubnetGroupDescription =
         flag "d-b-subnet-group-description" (required string)
           ~doc:"STRING String"
       and subnetIds =
         flag "subnet-ids" (required json_arg)
           ~doc:"JSON SubnetIdentifierList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_d_b_subnet_group
           (Values.CreateDBSubnetGroupMessage.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~dBSubnetGroupName ~dBSubnetGroupDescription
              ~subnetIds:(Values.SubnetIdentifierList.of_json subnetIds) ())
           (Some Values.CreateDBSubnetGroupResult.to_json)
           (Some Values.CreateDBSubnetGroupResult.error_to_json)])
let create_event_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceType =
         flag "source-type" (optional string) ~doc:"STRING String"
       and eventCategories =
         flag "event-categories" (optional json_arg)
           ~doc:"JSON EventCategoriesList"
       and sourceIds =
         flag "source-ids" (optional json_arg) ~doc:"JSON SourceIdsList"
       and enabled =
         flag "enabled" (optional bool) ~doc:"BOOL BooleanOptional"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and subscriptionName =
         flag "subscription-name" (required string) ~doc:"STRING String"
       and snsTopicArn =
         flag "sns-topic-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_event_subscription
           (Values.CreateEventSubscriptionMessage.make ?sourceType
              ?eventCategories:(Option.map
                                  ~f:Values.EventCategoriesList.of_json
                                  eventCategories)
              ?sourceIds:(Option.map ~f:Values.SourceIdsList.of_json
                            sourceIds) ?enabled
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~subscriptionName ~snsTopicArn ())
           (Some Values.CreateEventSubscriptionResult.to_json)
           (Some Values.CreateEventSubscriptionResult.error_to_json)])
let create_global_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalClusterIdentifier =
         flag "global-cluster-identifier" (optional string)
           ~doc:"STRING String"
       and sourceDBClusterIdentifier =
         flag "source-d-b-cluster-identifier" (optional string)
           ~doc:"STRING String"
       and engine = flag "engine" (optional string) ~doc:"STRING String"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING String"
       and deletionProtection =
         flag "deletion-protection" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and databaseName =
         flag "database-name" (optional string) ~doc:"STRING String"
       and storageEncrypted =
         flag "storage-encrypted" (optional bool) ~doc:"BOOL BooleanOptional" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_global_cluster
           (Values.CreateGlobalClusterMessage.make ?globalClusterIdentifier
              ?sourceDBClusterIdentifier ?engine ?engineVersion
              ?deletionProtection ?databaseName ?storageEncrypted ())
           (Some Values.CreateGlobalClusterResult.to_json)
           (Some Values.CreateGlobalClusterResult.error_to_json)])
let create_option_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and optionGroupName =
         flag "option-group-name" (required string) ~doc:"STRING String"
       and engineName =
         flag "engine-name" (required string) ~doc:"STRING String"
       and majorEngineVersion =
         flag "major-engine-version" (required string) ~doc:"STRING String"
       and optionGroupDescription =
         flag "option-group-description" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_option_group
           (Values.CreateOptionGroupMessage.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~optionGroupName ~engineName ~majorEngineVersion
              ~optionGroupDescription ())
           (Some Values.CreateOptionGroupResult.to_json)
           (Some Values.CreateOptionGroupResult.error_to_json)])
let delete_custom_availability_zone =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customAvailabilityZoneId =
         flag "custom-availability-zone-id" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_custom_availability_zone
           (Values.DeleteCustomAvailabilityZoneMessage.make
              ~customAvailabilityZoneId ())
           (Some Values.DeleteCustomAvailabilityZoneResult.to_json)
           (Some Values.DeleteCustomAvailabilityZoneResult.error_to_json)])
let delete_custom_d_b_engine_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and engine =
         flag "engine" (required string) ~doc:"STRING CustomEngineName"
       and engineVersion =
         flag "engine-version" (required string)
           ~doc:"STRING CustomEngineVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_custom_d_b_engine_version
           (Values.DeleteCustomDBEngineVersionMessage.make ~engine
              ~engineVersion ()) (Some Values.DBEngineVersion.to_json)
           (Some Values.DBEngineVersion.error_to_json)])
let delete_d_b_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and skipFinalSnapshot =
         flag "skip-final-snapshot" (optional bool) ~doc:"BOOL Boolean"
       and finalDBSnapshotIdentifier =
         flag "final-d-b-snapshot-identifier" (optional string)
           ~doc:"STRING String"
       and dBClusterIdentifier =
         flag "d-b-cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_d_b_cluster
           (Values.DeleteDBClusterMessage.make ?skipFinalSnapshot
              ?finalDBSnapshotIdentifier ~dBClusterIdentifier ())
           (Some Values.DeleteDBClusterResult.to_json)
           (Some Values.DeleteDBClusterResult.error_to_json)])
let delete_d_b_cluster_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBClusterEndpointIdentifier =
         flag "d-b-cluster-endpoint-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_d_b_cluster_endpoint
           (Values.DeleteDBClusterEndpointMessage.make
              ~dBClusterEndpointIdentifier ())
           (Some Values.DBClusterEndpoint.to_json)
           (Some Values.DBClusterEndpoint.error_to_json)])
let delete_d_b_cluster_parameter_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBClusterParameterGroupName =
         flag "d-b-cluster-parameter-group-name" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_d_b_cluster_parameter_group
           (Values.DeleteDBClusterParameterGroupMessage.make
              ~dBClusterParameterGroupName ()) None None])
let delete_d_b_cluster_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBClusterSnapshotIdentifier =
         flag "d-b-cluster-snapshot-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_d_b_cluster_snapshot
           (Values.DeleteDBClusterSnapshotMessage.make
              ~dBClusterSnapshotIdentifier ())
           (Some Values.DeleteDBClusterSnapshotResult.to_json)
           (Some Values.DeleteDBClusterSnapshotResult.error_to_json)])
let delete_d_b_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and skipFinalSnapshot =
         flag "skip-final-snapshot" (optional bool) ~doc:"BOOL Boolean"
       and finalDBSnapshotIdentifier =
         flag "final-d-b-snapshot-identifier" (optional string)
           ~doc:"STRING String"
       and deleteAutomatedBackups =
         flag "delete-automated-backups" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and dBInstanceIdentifier =
         flag "d-b-instance-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_d_b_instance
           (Values.DeleteDBInstanceMessage.make ?skipFinalSnapshot
              ?finalDBSnapshotIdentifier ?deleteAutomatedBackups
              ~dBInstanceIdentifier ())
           (Some Values.DeleteDBInstanceResult.to_json)
           (Some Values.DeleteDBInstanceResult.error_to_json)])
let delete_d_b_instance_automated_backup =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dbiResourceId =
         flag "dbi-resource-id" (optional string) ~doc:"STRING String"
       and dBInstanceAutomatedBackupsArn =
         flag "d-b-instance-automated-backups-arn" (optional string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_d_b_instance_automated_backup
           (Values.DeleteDBInstanceAutomatedBackupMessage.make ?dbiResourceId
              ?dBInstanceAutomatedBackupsArn ())
           (Some Values.DeleteDBInstanceAutomatedBackupResult.to_json)
           (Some Values.DeleteDBInstanceAutomatedBackupResult.error_to_json)])
let delete_d_b_parameter_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBParameterGroupName =
         flag "d-b-parameter-group-name" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_d_b_parameter_group
           (Values.DeleteDBParameterGroupMessage.make ~dBParameterGroupName
              ()) None None])
let delete_d_b_proxy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBProxyName =
         flag "d-b-proxy-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_d_b_proxy
           (Values.DeleteDBProxyRequest.make ~dBProxyName ())
           (Some Values.DeleteDBProxyResponse.to_json)
           (Some Values.DeleteDBProxyResponse.error_to_json)])
let delete_d_b_proxy_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBProxyEndpointName =
         flag "d-b-proxy-endpoint-name" (required string)
           ~doc:"STRING DBProxyEndpointName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_d_b_proxy_endpoint
           (Values.DeleteDBProxyEndpointRequest.make ~dBProxyEndpointName ())
           (Some Values.DeleteDBProxyEndpointResponse.to_json)
           (Some Values.DeleteDBProxyEndpointResponse.error_to_json)])
let delete_d_b_security_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBSecurityGroupName =
         flag "d-b-security-group-name" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_d_b_security_group
           (Values.DeleteDBSecurityGroupMessage.make ~dBSecurityGroupName ())
           None None])
let delete_d_b_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBSnapshotIdentifier =
         flag "d-b-snapshot-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_d_b_snapshot
           (Values.DeleteDBSnapshotMessage.make ~dBSnapshotIdentifier ())
           (Some Values.DeleteDBSnapshotResult.to_json)
           (Some Values.DeleteDBSnapshotResult.error_to_json)])
let delete_d_b_subnet_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBSubnetGroupName =
         flag "d-b-subnet-group-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_d_b_subnet_group
           (Values.DeleteDBSubnetGroupMessage.make ~dBSubnetGroupName ())
           None None])
let delete_event_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subscriptionName =
         flag "subscription-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_event_subscription
           (Values.DeleteEventSubscriptionMessage.make ~subscriptionName ())
           (Some Values.DeleteEventSubscriptionResult.to_json)
           (Some Values.DeleteEventSubscriptionResult.error_to_json)])
let delete_global_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalClusterIdentifier =
         flag "global-cluster-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_global_cluster
           (Values.DeleteGlobalClusterMessage.make ~globalClusterIdentifier
              ()) (Some Values.DeleteGlobalClusterResult.to_json)
           (Some Values.DeleteGlobalClusterResult.error_to_json)])
let delete_installation_media =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and installationMediaId =
         flag "installation-media-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_installation_media
           (Values.DeleteInstallationMediaMessage.make ~installationMediaId
              ()) (Some Values.InstallationMedia.to_json)
           (Some Values.InstallationMedia.error_to_json)])
let delete_option_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and optionGroupName =
         flag "option-group-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_option_group
           (Values.DeleteOptionGroupMessage.make ~optionGroupName ()) None
           None])
let deregister_d_b_proxy_targets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetGroupName =
         flag "target-group-name" (optional string) ~doc:"STRING String"
       and dBInstanceIdentifiers =
         flag "d-b-instance-identifiers" (optional json_arg)
           ~doc:"JSON StringList"
       and dBClusterIdentifiers =
         flag "d-b-cluster-identifiers" (optional json_arg)
           ~doc:"JSON StringList"
       and dBProxyName =
         flag "d-b-proxy-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_d_b_proxy_targets
           (Values.DeregisterDBProxyTargetsRequest.make ?targetGroupName
              ?dBInstanceIdentifiers:(Option.map ~f:Values.StringList.of_json
                                        dBInstanceIdentifiers)
              ?dBClusterIdentifiers:(Option.map ~f:Values.StringList.of_json
                                       dBClusterIdentifiers) ~dBProxyName ())
           (Some Values.DeregisterDBProxyTargetsResponse.to_json)
           (Some Values.DeregisterDBProxyTargetsResponse.error_to_json)])
let describe_account_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_account_attributes
           (Values.DescribeAccountAttributesMessage.make ())
           (Some Values.AccountAttributesMessage.to_json)
           (Some Values.AccountAttributesMessage.error_to_json)])
let describe_certificates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateIdentifier =
         flag "certificate-identifier" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_certificates
           (Values.DescribeCertificatesMessage.make ?certificateIdentifier
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ())
           (Some Values.CertificateMessage.to_json)
           (Some Values.CertificateMessage.error_to_json)])
let describe_custom_availability_zones =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customAvailabilityZoneId =
         flag "custom-availability-zone-id" (optional string)
           ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_custom_availability_zones
           (Values.DescribeCustomAvailabilityZonesMessage.make
              ?customAvailabilityZoneId
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ())
           (Some Values.CustomAvailabilityZoneMessage.to_json)
           (Some Values.CustomAvailabilityZoneMessage.error_to_json)])
let describe_d_b_cluster_backtracks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and backtrackIdentifier =
         flag "backtrack-identifier" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and dBClusterIdentifier =
         flag "d-b-cluster-identifier" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_d_b_cluster_backtracks
           (Values.DescribeDBClusterBacktracksMessage.make
              ?backtrackIdentifier
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ~dBClusterIdentifier ())
           (Some Values.DBClusterBacktrackMessage.to_json)
           (Some Values.DBClusterBacktrackMessage.error_to_json)])
let describe_d_b_cluster_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBClusterIdentifier =
         flag "d-b-cluster-identifier" (optional string) ~doc:"STRING String"
       and dBClusterEndpointIdentifier =
         flag "d-b-cluster-endpoint-identifier" (optional string)
           ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_d_b_cluster_endpoints
           (Values.DescribeDBClusterEndpointsMessage.make
              ?dBClusterIdentifier ?dBClusterEndpointIdentifier
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ())
           (Some Values.DBClusterEndpointMessage.to_json)
           (Some Values.DBClusterEndpointMessage.error_to_json)])
let describe_d_b_cluster_parameter_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBClusterParameterGroupName =
         flag "d-b-cluster-parameter-group-name" (optional string)
           ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_d_b_cluster_parameter_groups
           (Values.DescribeDBClusterParameterGroupsMessage.make
              ?dBClusterParameterGroupName
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ())
           (Some Values.DBClusterParameterGroupsMessage.to_json)
           (Some Values.DBClusterParameterGroupsMessage.error_to_json)])
let describe_d_b_cluster_parameters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and source = flag "source" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and dBClusterParameterGroupName =
         flag "d-b-cluster-parameter-group-name" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_d_b_cluster_parameters
           (Values.DescribeDBClusterParametersMessage.make ?source
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ~dBClusterParameterGroupName ())
           (Some Values.DBClusterParameterGroupDetails.to_json)
           (Some Values.DBClusterParameterGroupDetails.error_to_json)])
let describe_d_b_cluster_snapshot_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBClusterSnapshotIdentifier =
         flag "d-b-cluster-snapshot-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_d_b_cluster_snapshot_attributes
           (Values.DescribeDBClusterSnapshotAttributesMessage.make
              ~dBClusterSnapshotIdentifier ())
           (Some Values.DescribeDBClusterSnapshotAttributesResult.to_json)
           (Some
              Values.DescribeDBClusterSnapshotAttributesResult.error_to_json)])
let describe_d_b_cluster_snapshots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBClusterIdentifier =
         flag "d-b-cluster-identifier" (optional string) ~doc:"STRING String"
       and dBClusterSnapshotIdentifier =
         flag "d-b-cluster-snapshot-identifier" (optional string)
           ~doc:"STRING String"
       and snapshotType =
         flag "snapshot-type" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and includeShared =
         flag "include-shared" (optional bool) ~doc:"BOOL Boolean"
       and includePublic =
         flag "include-public" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_d_b_cluster_snapshots
           (Values.DescribeDBClusterSnapshotsMessage.make
              ?dBClusterIdentifier ?dBClusterSnapshotIdentifier ?snapshotType
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ?includeShared ?includePublic ())
           (Some Values.DBClusterSnapshotMessage.to_json)
           (Some Values.DBClusterSnapshotMessage.error_to_json)])
let describe_d_b_clusters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBClusterIdentifier =
         flag "d-b-cluster-identifier" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and includeShared =
         flag "include-shared" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_d_b_clusters
           (Values.DescribeDBClustersMessage.make ?dBClusterIdentifier
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ?includeShared ())
           (Some Values.DBClusterMessage.to_json)
           (Some Values.DBClusterMessage.error_to_json)])
let describe_d_b_engine_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and engine = flag "engine" (optional string) ~doc:"STRING String"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING String"
       and dBParameterGroupFamily =
         flag "d-b-parameter-group-family" (optional string)
           ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and defaultOnly =
         flag "default-only" (optional bool) ~doc:"BOOL Boolean"
       and listSupportedCharacterSets =
         flag "list-supported-character-sets" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and listSupportedTimezones =
         flag "list-supported-timezones" (optional bool)
           ~doc:"BOOL BooleanOptional"
       and includeAll =
         flag "include-all" (optional bool) ~doc:"BOOL BooleanOptional" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_d_b_engine_versions
           (Values.DescribeDBEngineVersionsMessage.make ?engine
              ?engineVersion ?dBParameterGroupFamily
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ?defaultOnly ?listSupportedCharacterSets
              ?listSupportedTimezones ?includeAll ())
           (Some Values.DBEngineVersionMessage.to_json)
           (Some Values.DBEngineVersionMessage.error_to_json)])
let describe_d_b_instance_automated_backups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dbiResourceId =
         flag "dbi-resource-id" (optional string) ~doc:"STRING String"
       and dBInstanceIdentifier =
         flag "d-b-instance-identifier" (optional string)
           ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and dBInstanceAutomatedBackupsArn =
         flag "d-b-instance-automated-backups-arn" (optional string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_d_b_instance_automated_backups
           (Values.DescribeDBInstanceAutomatedBackupsMessage.make
              ?dbiResourceId ?dBInstanceIdentifier
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ?dBInstanceAutomatedBackupsArn ())
           (Some Values.DBInstanceAutomatedBackupMessage.to_json)
           (Some Values.DBInstanceAutomatedBackupMessage.error_to_json)])
let describe_d_b_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBInstanceIdentifier =
         flag "d-b-instance-identifier" (optional string)
           ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_d_b_instances
           (Values.DescribeDBInstancesMessage.make ?dBInstanceIdentifier
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ()) (Some Values.DBInstanceMessage.to_json)
           (Some Values.DBInstanceMessage.error_to_json)])
let describe_d_b_log_files =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filenameContains =
         flag "filename-contains" (optional string) ~doc:"STRING String"
       and fileLastWritten =
         flag "file-last-written" (optional json_arg) ~doc:"JSON Long"
       and fileSize = flag "file-size" (optional json_arg) ~doc:"JSON Long"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and dBInstanceIdentifier =
         flag "d-b-instance-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_d_b_log_files
           (Values.DescribeDBLogFilesMessage.make ?filenameContains
              ?fileLastWritten:(Option.map ~f:Values.Long.of_json
                                  fileLastWritten)
              ?fileSize:(Option.map ~f:Values.Long.of_json fileSize)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ~dBInstanceIdentifier ())
           (Some Values.DescribeDBLogFilesResponse.to_json)
           (Some Values.DescribeDBLogFilesResponse.error_to_json)])
let describe_d_b_parameter_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBParameterGroupName =
         flag "d-b-parameter-group-name" (optional string)
           ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_d_b_parameter_groups
           (Values.DescribeDBParameterGroupsMessage.make
              ?dBParameterGroupName
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ())
           (Some Values.DBParameterGroupsMessage.to_json)
           (Some Values.DBParameterGroupsMessage.error_to_json)])
let describe_d_b_parameters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and source = flag "source" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and dBParameterGroupName =
         flag "d-b-parameter-group-name" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_d_b_parameters
           (Values.DescribeDBParametersMessage.make ?source
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ~dBParameterGroupName ())
           (Some Values.DBParameterGroupDetails.to_json)
           (Some Values.DBParameterGroupDetails.error_to_json)])
let describe_d_b_proxies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBProxyName =
         flag "d-b-proxy-name" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT MaxRecords" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_d_b_proxies
           (Values.DescribeDBProxiesRequest.make ?dBProxyName
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?marker ?maxRecords ())
           (Some Values.DescribeDBProxiesResponse.to_json)
           (Some Values.DescribeDBProxiesResponse.error_to_json)])
let describe_d_b_proxy_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBProxyName =
         flag "d-b-proxy-name" (optional string) ~doc:"STRING DBProxyName"
       and dBProxyEndpointName =
         flag "d-b-proxy-endpoint-name" (optional string)
           ~doc:"STRING DBProxyEndpointName"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT MaxRecords" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_d_b_proxy_endpoints
           (Values.DescribeDBProxyEndpointsRequest.make ?dBProxyName
              ?dBProxyEndpointName
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?marker ?maxRecords ())
           (Some Values.DescribeDBProxyEndpointsResponse.to_json)
           (Some Values.DescribeDBProxyEndpointsResponse.error_to_json)])
let describe_d_b_proxy_target_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetGroupName =
         flag "target-group-name" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT MaxRecords"
       and dBProxyName =
         flag "d-b-proxy-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_d_b_proxy_target_groups
           (Values.DescribeDBProxyTargetGroupsRequest.make ?targetGroupName
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?marker ?maxRecords ~dBProxyName ())
           (Some Values.DescribeDBProxyTargetGroupsResponse.to_json)
           (Some Values.DescribeDBProxyTargetGroupsResponse.error_to_json)])
let describe_d_b_proxy_targets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetGroupName =
         flag "target-group-name" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT MaxRecords"
       and dBProxyName =
         flag "d-b-proxy-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_d_b_proxy_targets
           (Values.DescribeDBProxyTargetsRequest.make ?targetGroupName
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?marker ?maxRecords ~dBProxyName ())
           (Some Values.DescribeDBProxyTargetsResponse.to_json)
           (Some Values.DescribeDBProxyTargetsResponse.error_to_json)])
let describe_d_b_security_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBSecurityGroupName =
         flag "d-b-security-group-name" (optional string)
           ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and maxRecords =
         flag "max-records" (optional int) ~doc:"INT IntegerOptional"
       and marker = flag "marker" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_d_b_security_groups
           (Values.DescribeDBSecurityGroupsMessage.make ?dBSecurityGroupName
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?maxRecords ?marker ())
           (Some Values.DBSecurityGroupMessage.to_json)
           (Some Values.DBSecurityGroupMessage.error_to_json)])
let describe_d_b_snapshot_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dBSnapshotIdentifier =
         flag "d-b-snapshot-identifier" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_d_b_snapshot_attributes
           (Values.DescribeDBSnapshotAttributesMessage.make
              ~dBSnapshotIdentifier ())
           (Some Values.DescribeDBSnapshotAttributesResult.to_json)
           (Some Values.DescribeDBSnapshotAttributesResult.error_to_json)])
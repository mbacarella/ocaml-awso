(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING ChannelArn"
       and notificationConfigurationArn =
         flag "notification-configuration-arn" (required string)
           ~doc:"STRING NotificationConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_channel
           (Values.AssociateChannelRequest.make ~arn
              ~notificationConfigurationArn ())
           (Some Values.AssociateChannelResponse.to_json)
           (Some Values.AssociateChannelResponse.error_to_json)])
let associate_managed_notification_account_contact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contactIdentifier =
         flag "contact-identifier" (required json_arg)
           ~doc:"JSON AccountContactType"
       and managedNotificationConfigurationArn =
         flag "managed-notification-configuration-arn" (required string)
           ~doc:"STRING ManagedNotificationConfigurationOsArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_managed_notification_account_contact
           (Values.AssociateManagedNotificationAccountContactRequest.make
              ~contactIdentifier:(Values.AccountContactType.of_json
                                    contactIdentifier)
              ~managedNotificationConfigurationArn ())
           (Some
              Values.AssociateManagedNotificationAccountContactResponse.to_json)
           (Some
              Values.AssociateManagedNotificationAccountContactResponse.error_to_json)])
let associate_managed_notification_additional_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChannelArn"
       and managedNotificationConfigurationArn =
         flag "managed-notification-configuration-arn" (required string)
           ~doc:"STRING ManagedNotificationConfigurationOsArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_managed_notification_additional_channel
           (Values.AssociateManagedNotificationAdditionalChannelRequest.make
              ~channelArn ~managedNotificationConfigurationArn ())
           (Some
              Values.AssociateManagedNotificationAdditionalChannelResponse.to_json)
           (Some
              Values.AssociateManagedNotificationAdditionalChannelResponse.error_to_json)])
let associate_organizational_unit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationalUnitId =
         flag "organizational-unit-id" (required string)
           ~doc:"STRING OrganizationalUnitId"
       and notificationConfigurationArn =
         flag "notification-configuration-arn" (required string)
           ~doc:"STRING NotificationConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_organizational_unit
           (Values.AssociateOrganizationalUnitRequest.make
              ~organizationalUnitId ~notificationConfigurationArn ())
           (Some Values.AssociateOrganizationalUnitResponse.to_json)
           (Some Values.AssociateOrganizationalUnitResponse.error_to_json)])
let create_event_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventPattern =
         flag "event-pattern" (optional string)
           ~doc:"STRING EventRuleEventPattern"
       and notificationConfigurationArn =
         flag "notification-configuration-arn" (required string)
           ~doc:"STRING NotificationConfigurationArn"
       and source = flag "source" (required string) ~doc:"STRING Source"
       and eventType =
         flag "event-type" (required string) ~doc:"STRING EventType"
       and regions = flag "regions" (required json_arg) ~doc:"JSON Regions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_event_rule
           (Values.CreateEventRuleRequest.make ?eventPattern
              ~notificationConfigurationArn ~source ~eventType
              ~regions:(Values.Regions.of_json regions) ())
           (Some Values.CreateEventRuleResponse.to_json)
           (Some Values.CreateEventRuleResponse.error_to_json)])
let create_notification_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aggregationDuration =
         flag "aggregation-duration" (optional json_arg)
           ~doc:"JSON AggregationDuration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name =
         flag "name" (required string)
           ~doc:"STRING NotificationConfigurationName"
       and description =
         flag "description" (required string)
           ~doc:"STRING NotificationConfigurationDescription" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_notification_configuration
           (Values.CreateNotificationConfigurationRequest.make
              ?aggregationDuration:(Option.map
                                      ~f:Values.AggregationDuration.of_json
                                      aggregationDuration)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name
              ~description ())
           (Some Values.CreateNotificationConfigurationResponse.to_json)
           (Some Values.CreateNotificationConfigurationResponse.error_to_json)])
let delete_event_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING EventRuleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_event_rule (Values.DeleteEventRuleRequest.make ~arn ())
           (Some Values.DeleteEventRuleResponse.to_json)
           (Some Values.DeleteEventRuleResponse.error_to_json)])
let delete_notification_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string)
           ~doc:"STRING NotificationConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_notification_configuration
           (Values.DeleteNotificationConfigurationRequest.make ~arn ())
           (Some Values.DeleteNotificationConfigurationResponse.to_json)
           (Some Values.DeleteNotificationConfigurationResponse.error_to_json)])
let deregister_notification_hub =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and notificationHubRegion =
         flag "notification-hub-region" (required string)
           ~doc:"STRING Region" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_notification_hub
           (Values.DeregisterNotificationHubRequest.make
              ~notificationHubRegion ())
           (Some Values.DeregisterNotificationHubResponse.to_json)
           (Some Values.DeregisterNotificationHubResponse.error_to_json)])
let disable_notifications_access_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_notifications_access_for_organization
           (Values.DisableNotificationsAccessForOrganizationRequest.make ())
           (Some
              Values.DisableNotificationsAccessForOrganizationResponse.to_json)
           (Some
              Values.DisableNotificationsAccessForOrganizationResponse.error_to_json)])
let disassociate_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING ChannelArn"
       and notificationConfigurationArn =
         flag "notification-configuration-arn" (required string)
           ~doc:"STRING NotificationConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_channel
           (Values.DisassociateChannelRequest.make ~arn
              ~notificationConfigurationArn ())
           (Some Values.DisassociateChannelResponse.to_json)
           (Some Values.DisassociateChannelResponse.error_to_json)])
let disassociate_managed_notification_account_contact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contactIdentifier =
         flag "contact-identifier" (required json_arg)
           ~doc:"JSON AccountContactType"
       and managedNotificationConfigurationArn =
         flag "managed-notification-configuration-arn" (required string)
           ~doc:"STRING ManagedNotificationConfigurationOsArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_managed_notification_account_contact
           (Values.DisassociateManagedNotificationAccountContactRequest.make
              ~contactIdentifier:(Values.AccountContactType.of_json
                                    contactIdentifier)
              ~managedNotificationConfigurationArn ())
           (Some
              Values.DisassociateManagedNotificationAccountContactResponse.to_json)
           (Some
              Values.DisassociateManagedNotificationAccountContactResponse.error_to_json)])
let disassociate_managed_notification_additional_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelArn =
         flag "channel-arn" (required string) ~doc:"STRING ChannelArn"
       and managedNotificationConfigurationArn =
         flag "managed-notification-configuration-arn" (required string)
           ~doc:"STRING ManagedNotificationConfigurationOsArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_managed_notification_additional_channel
           (Values.DisassociateManagedNotificationAdditionalChannelRequest.make
              ~channelArn ~managedNotificationConfigurationArn ())
           (Some
              Values.DisassociateManagedNotificationAdditionalChannelResponse.to_json)
           (Some
              Values.DisassociateManagedNotificationAdditionalChannelResponse.error_to_json)])
let disassociate_organizational_unit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationalUnitId =
         flag "organizational-unit-id" (required string)
           ~doc:"STRING OrganizationalUnitId"
       and notificationConfigurationArn =
         flag "notification-configuration-arn" (required string)
           ~doc:"STRING NotificationConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_organizational_unit
           (Values.DisassociateOrganizationalUnitRequest.make
              ~organizationalUnitId ~notificationConfigurationArn ())
           (Some Values.DisassociateOrganizationalUnitResponse.to_json)
           (Some Values.DisassociateOrganizationalUnitResponse.error_to_json)])
let enable_notifications_access_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_notifications_access_for_organization
           (Values.EnableNotificationsAccessForOrganizationRequest.make ())
           (Some
              Values.EnableNotificationsAccessForOrganizationResponse.to_json)
           (Some
              Values.EnableNotificationsAccessForOrganizationResponse.error_to_json)])
let get_event_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING EventRuleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_event_rule (Values.GetEventRuleRequest.make ~arn ())
           (Some Values.GetEventRuleResponse.to_json)
           (Some Values.GetEventRuleResponse.error_to_json)])
let get_managed_notification_child_event =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and locale = flag "locale" (optional json_arg) ~doc:"JSON LocaleCode"
       and arn =
         flag "arn" (required string)
           ~doc:"STRING ManagedNotificationChildEventArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_managed_notification_child_event
           (Values.GetManagedNotificationChildEventRequest.make
              ?locale:(Option.map ~f:Values.LocaleCode.of_json locale) ~arn
              ())
           (Some Values.GetManagedNotificationChildEventResponse.to_json)
           (Some
              Values.GetManagedNotificationChildEventResponse.error_to_json)])
let get_managed_notification_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string)
           ~doc:"STRING ManagedNotificationConfigurationOsArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_managed_notification_configuration
           (Values.GetManagedNotificationConfigurationRequest.make ~arn ())
           (Some Values.GetManagedNotificationConfigurationResponse.to_json)
           (Some
              Values.GetManagedNotificationConfigurationResponse.error_to_json)])
let get_managed_notification_event =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and locale = flag "locale" (optional json_arg) ~doc:"JSON LocaleCode"
       and arn =
         flag "arn" (required string)
           ~doc:"STRING ManagedNotificationEventArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_managed_notification_event
           (Values.GetManagedNotificationEventRequest.make
              ?locale:(Option.map ~f:Values.LocaleCode.of_json locale) ~arn
              ()) (Some Values.GetManagedNotificationEventResponse.to_json)
           (Some Values.GetManagedNotificationEventResponse.error_to_json)])
let get_notification_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string)
           ~doc:"STRING NotificationConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_notification_configuration
           (Values.GetNotificationConfigurationRequest.make ~arn ())
           (Some Values.GetNotificationConfigurationResponse.to_json)
           (Some Values.GetNotificationConfigurationResponse.error_to_json)])
let get_notification_event =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and locale = flag "locale" (optional json_arg) ~doc:"JSON LocaleCode"
       and arn =
         flag "arn" (required string) ~doc:"STRING NotificationEventArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_notification_event
           (Values.GetNotificationEventRequest.make
              ?locale:(Option.map ~f:Values.LocaleCode.of_json locale) ~arn
              ()) (Some Values.GetNotificationEventResponse.to_json)
           (Some Values.GetNotificationEventResponse.error_to_json)])
let get_notifications_access_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_notifications_access_for_organization
           (Values.GetNotificationsAccessForOrganizationRequest.make ())
           (Some Values.GetNotificationsAccessForOrganizationResponse.to_json)
           (Some
              Values.GetNotificationsAccessForOrganizationResponse.error_to_json)])
let list_channels =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListChannelsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and notificationConfigurationArn =
         flag "notification-configuration-arn" (required string)
           ~doc:"STRING NotificationConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_channels
           (Values.ListChannelsRequest.make ?maxResults ?nextToken
              ~notificationConfigurationArn ())
           (Some Values.ListChannelsResponse.to_json)
           (Some Values.ListChannelsResponse.error_to_json)])
let list_event_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListEventRulesRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and notificationConfigurationArn =
         flag "notification-configuration-arn" (required string)
           ~doc:"STRING NotificationConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_event_rules
           (Values.ListEventRulesRequest.make ?maxResults ?nextToken
              ~notificationConfigurationArn ())
           (Some Values.ListEventRulesResponse.to_json)
           (Some Values.ListEventRulesResponse.error_to_json)])
let list_managed_notification_channel_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListManagedNotificationChannelAssociationsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and managedNotificationConfigurationArn =
         flag "managed-notification-configuration-arn" (required string)
           ~doc:"STRING ManagedNotificationConfigurationOsArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_managed_notification_channel_associations
           (Values.ListManagedNotificationChannelAssociationsRequest.make
              ?maxResults ?nextToken ~managedNotificationConfigurationArn ())
           (Some
              Values.ListManagedNotificationChannelAssociationsResponse.to_json)
           (Some
              Values.ListManagedNotificationChannelAssociationsResponse.error_to_json)])
let list_managed_notification_child_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and startTime =
         flag "start-time" (optional json_arg)
           ~doc:"JSON SyntheticTimestamp_date_time"
       and endTime =
         flag "end-time" (optional json_arg)
           ~doc:"JSON SyntheticTimestamp_date_time"
       and locale = flag "locale" (optional json_arg) ~doc:"JSON LocaleCode"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListManagedNotificationChildEventsRequestMaxResultsInteger"
       and relatedAccount =
         flag "related-account" (optional string) ~doc:"STRING AccountId"
       and organizationalUnitId =
         flag "organizational-unit-id" (optional string)
           ~doc:"STRING OrganizationalUnitId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and aggregateManagedNotificationEventArn =
         flag "aggregate-managed-notification-event-arn" (required string)
           ~doc:"STRING ManagedNotificationEventArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_managed_notification_child_events
           (Values.ListManagedNotificationChildEventsRequest.make
              ?startTime:(Option.map
                            ~f:Values.SyntheticTimestamp_date_time.of_json
                            startTime)
              ?endTime:(Option.map
                          ~f:Values.SyntheticTimestamp_date_time.of_json
                          endTime)
              ?locale:(Option.map ~f:Values.LocaleCode.of_json locale)
              ?maxResults ?relatedAccount ?organizationalUnitId ?nextToken
              ~aggregateManagedNotificationEventArn ())
           (Some Values.ListManagedNotificationChildEventsResponse.to_json)
           (Some
              Values.ListManagedNotificationChildEventsResponse.error_to_json)])
let list_managed_notification_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelIdentifier =
         flag "channel-identifier" (optional string)
           ~doc:"STRING ChannelIdentifier"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListManagedNotificationConfigurationsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_managed_notification_configurations
           (Values.ListManagedNotificationConfigurationsRequest.make
              ?channelIdentifier ?maxResults ?nextToken ())
           (Some Values.ListManagedNotificationConfigurationsResponse.to_json)
           (Some
              Values.ListManagedNotificationConfigurationsResponse.error_to_json)])
let list_managed_notification_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and startTime =
         flag "start-time" (optional json_arg)
           ~doc:"JSON SyntheticTimestamp_date_time"
       and endTime =
         flag "end-time" (optional json_arg)
           ~doc:"JSON SyntheticTimestamp_date_time"
       and locale = flag "locale" (optional json_arg) ~doc:"JSON LocaleCode"
       and source = flag "source" (optional string) ~doc:"STRING Source"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListManagedNotificationEventsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and organizationalUnitId =
         flag "organizational-unit-id" (optional string)
           ~doc:"STRING OrganizationalUnitId"
       and relatedAccount =
         flag "related-account" (optional string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_managed_notification_events
           (Values.ListManagedNotificationEventsRequest.make
              ?startTime:(Option.map
                            ~f:Values.SyntheticTimestamp_date_time.of_json
                            startTime)
              ?endTime:(Option.map
                          ~f:Values.SyntheticTimestamp_date_time.of_json
                          endTime)
              ?locale:(Option.map ~f:Values.LocaleCode.of_json locale)
              ?source ?maxResults ?nextToken ?organizationalUnitId
              ?relatedAccount ())
           (Some Values.ListManagedNotificationEventsResponse.to_json)
           (Some Values.ListManagedNotificationEventsResponse.error_to_json)])
let list_member_accounts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListMemberAccountsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and memberAccount =
         flag "member-account" (optional string) ~doc:"STRING AccountId"
       and status =
         flag "status" (optional json_arg)
           ~doc:"JSON MemberAccountNotificationConfigurationStatus"
       and organizationalUnitId =
         flag "organizational-unit-id" (optional string)
           ~doc:"STRING OrganizationalUnitId"
       and notificationConfigurationArn =
         flag "notification-configuration-arn" (required string)
           ~doc:"STRING NotificationConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_member_accounts
           (Values.ListMemberAccountsRequest.make ?maxResults ?nextToken
              ?memberAccount
              ?status:(Option.map
                         ~f:Values.MemberAccountNotificationConfigurationStatus.of_json
                         status) ?organizationalUnitId
              ~notificationConfigurationArn ())
           (Some Values.ListMemberAccountsResponse.to_json)
           (Some Values.ListMemberAccountsResponse.error_to_json)])
let list_notification_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventRuleSource =
         flag "event-rule-source" (optional string) ~doc:"STRING Source"
       and channelArn =
         flag "channel-arn" (optional string) ~doc:"STRING ChannelArn"
       and status =
         flag "status" (optional json_arg)
           ~doc:"JSON NotificationConfigurationStatus"
       and subtype =
         flag "subtype" (optional json_arg)
           ~doc:"JSON NotificationConfigurationSubtype"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListNotificationConfigurationsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_notification_configurations
           (Values.ListNotificationConfigurationsRequest.make
              ?eventRuleSource ?channelArn
              ?status:(Option.map
                         ~f:Values.NotificationConfigurationStatus.of_json
                         status)
              ?subtype:(Option.map
                          ~f:Values.NotificationConfigurationSubtype.of_json
                          subtype) ?maxResults ?nextToken ())
           (Some Values.ListNotificationConfigurationsResponse.to_json)
           (Some Values.ListNotificationConfigurationsResponse.error_to_json)])
let list_notification_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and startTime =
         flag "start-time" (optional json_arg)
           ~doc:"JSON SyntheticTimestamp_date_time"
       and endTime =
         flag "end-time" (optional json_arg)
           ~doc:"JSON SyntheticTimestamp_date_time"
       and locale = flag "locale" (optional json_arg) ~doc:"JSON LocaleCode"
       and source = flag "source" (optional string) ~doc:"STRING Source"
       and includeChildEvents =
         flag "include-child-events" (optional bool) ~doc:"BOOL Boolean"
       and aggregateNotificationEventArn =
         flag "aggregate-notification-event-arn" (optional string)
           ~doc:"STRING NotificationEventArn"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListNotificationEventsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and organizationalUnitId =
         flag "organizational-unit-id" (optional string)
           ~doc:"STRING OrganizationalUnitId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_notification_events
           (Values.ListNotificationEventsRequest.make
              ?startTime:(Option.map
                            ~f:Values.SyntheticTimestamp_date_time.of_json
                            startTime)
              ?endTime:(Option.map
                          ~f:Values.SyntheticTimestamp_date_time.of_json
                          endTime)
              ?locale:(Option.map ~f:Values.LocaleCode.of_json locale)
              ?source ?includeChildEvents ?aggregateNotificationEventArn
              ?maxResults ?nextToken ?organizationalUnitId ())
           (Some Values.ListNotificationEventsResponse.to_json)
           (Some Values.ListNotificationEventsResponse.error_to_json)])
let list_notification_hubs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListNotificationHubsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_notification_hubs
           (Values.ListNotificationHubsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListNotificationHubsResponse.to_json)
           (Some Values.ListNotificationHubsResponse.error_to_json)])
let list_organizational_units =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListOrganizationalUnitsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and notificationConfigurationArn =
         flag "notification-configuration-arn" (required string)
           ~doc:"STRING NotificationConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_organizational_units
           (Values.ListOrganizationalUnitsRequest.make ?maxResults ?nextToken
              ~notificationConfigurationArn ())
           (Some Values.ListOrganizationalUnitsResponse.to_json)
           (Some Values.ListOrganizationalUnitsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string)
           ~doc:"STRING NotificationConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~arn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let register_notification_hub =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and notificationHubRegion =
         flag "notification-hub-region" (required string)
           ~doc:"STRING Region" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_notification_hub
           (Values.RegisterNotificationHubRequest.make ~notificationHubRegion
              ()) (Some Values.RegisterNotificationHubResponse.to_json)
           (Some Values.RegisterNotificationHubResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string)
           ~doc:"STRING NotificationConfigurationArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~arn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn =
         flag "arn" (required string)
           ~doc:"STRING NotificationConfigurationArn"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~arn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_event_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventPattern =
         flag "event-pattern" (optional string)
           ~doc:"STRING EventRuleEventPattern"
       and regions = flag "regions" (optional json_arg) ~doc:"JSON Regions"
       and arn = flag "arn" (required string) ~doc:"STRING EventRuleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_event_rule
           (Values.UpdateEventRuleRequest.make ?eventPattern
              ?regions:(Option.map ~f:Values.Regions.of_json regions) ~arn ())
           (Some Values.UpdateEventRuleResponse.to_json)
           (Some Values.UpdateEventRuleResponse.error_to_json)])
let update_notification_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string)
           ~doc:"STRING NotificationConfigurationName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING NotificationConfigurationDescription"
       and aggregationDuration =
         flag "aggregation-duration" (optional json_arg)
           ~doc:"JSON AggregationDuration"
       and arn =
         flag "arn" (required string)
           ~doc:"STRING NotificationConfigurationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_notification_configuration
           (Values.UpdateNotificationConfigurationRequest.make ?name
              ?description
              ?aggregationDuration:(Option.map
                                      ~f:Values.AggregationDuration.of_json
                                      aggregationDuration) ~arn ())
           (Some Values.UpdateNotificationConfigurationResponse.to_json)
           (Some Values.UpdateNotificationConfigurationResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-channel", associate_channel);
    ("associate-managed-notification-account-contact",
      associate_managed_notification_account_contact);
    ("associate-managed-notification-additional-channel",
      associate_managed_notification_additional_channel);
    ("associate-organizational-unit", associate_organizational_unit);
    ("create-event-rule", create_event_rule);
    ("create-notification-configuration", create_notification_configuration);
    ("delete-event-rule", delete_event_rule);
    ("delete-notification-configuration", delete_notification_configuration);
    ("deregister-notification-hub", deregister_notification_hub);
    ("disable-notifications-access-for-organization",
      disable_notifications_access_for_organization);
    ("disassociate-channel", disassociate_channel);
    ("disassociate-managed-notification-account-contact",
      disassociate_managed_notification_account_contact);
    ("disassociate-managed-notification-additional-channel",
      disassociate_managed_notification_additional_channel);
    ("disassociate-organizational-unit", disassociate_organizational_unit);
    ("enable-notifications-access-for-organization",
      enable_notifications_access_for_organization);
    ("get-event-rule", get_event_rule);
    ("get-managed-notification-child-event",
      get_managed_notification_child_event);
    ("get-managed-notification-configuration",
      get_managed_notification_configuration);
    ("get-managed-notification-event", get_managed_notification_event);
    ("get-notification-configuration", get_notification_configuration);
    ("get-notification-event", get_notification_event);
    ("get-notifications-access-for-organization",
      get_notifications_access_for_organization);
    ("list-channels", list_channels);
    ("list-event-rules", list_event_rules);
    ("list-managed-notification-channel-associations",
      list_managed_notification_channel_associations);
    ("list-managed-notification-child-events",
      list_managed_notification_child_events);
    ("list-managed-notification-configurations",
      list_managed_notification_configurations);
    ("list-managed-notification-events", list_managed_notification_events);
    ("list-member-accounts", list_member_accounts);
    ("list-notification-configurations", list_notification_configurations);
    ("list-notification-events", list_notification_events);
    ("list-notification-hubs", list_notification_hubs);
    ("list-organizational-units", list_organizational_units);
    ("list-tags-for-resource", list_tags_for_resource);
    ("register-notification-hub", register_notification_hub);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-event-rule", update_event_rule);
    ("update-notification-configuration", update_notification_configuration)]
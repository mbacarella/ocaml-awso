(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let cancel_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sessionArn =
         flag "session-arn" (required string) ~doc:"STRING SessionArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_session
           (Values.CancelSessionRequest.make ~sessionArn ())
           (Some Values.CancelSessionResponse.to_json)
           (Some Values.CancelSessionResponse.error_to_json)])
let create_approval_team =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING Token"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and approvalStrategy =
         flag "approval-strategy" (required json_arg)
           ~doc:"JSON ApprovalStrategy"
       and approvers =
         flag "approvers" (required json_arg)
           ~doc:"JSON ApprovalTeamRequestApprovers"
       and description =
         flag "description" (required string) ~doc:"STRING Description"
       and policies =
         flag "policies" (required json_arg) ~doc:"JSON PoliciesReferences"
       and name =
         flag "name" (required string) ~doc:"STRING ApprovalTeamName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_approval_team
           (Values.CreateApprovalTeamRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~approvalStrategy:(Values.ApprovalStrategy.of_json
                                   approvalStrategy)
              ~approvers:(Values.ApprovalTeamRequestApprovers.of_json
                            approvers) ~description
              ~policies:(Values.PoliciesReferences.of_json policies) ~name ())
           (Some Values.CreateApprovalTeamResponse.to_json)
           (Some Values.CreateApprovalTeamResponse.error_to_json)])
let create_identity_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING Token"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and identitySourceParameters =
         flag "identity-source-parameters" (required json_arg)
           ~doc:"JSON IdentitySourceParameters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_identity_source
           (Values.CreateIdentitySourceRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~identitySourceParameters:(Values.IdentitySourceParameters.of_json
                                           identitySourceParameters) ())
           (Some Values.CreateIdentitySourceResponse.to_json)
           (Some Values.CreateIdentitySourceResponse.error_to_json)])
let delete_identity_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identitySourceArn =
         flag "identity-source-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_identity_source
           (Values.DeleteIdentitySourceRequest.make ~identitySourceArn ())
           None None])
let delete_inactive_approval_team_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING ApprovalTeamArn"
       and versionId =
         flag "version-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_inactive_approval_team_version
           (Values.DeleteInactiveApprovalTeamVersionRequest.make ~arn
              ~versionId ())
           (Some Values.DeleteInactiveApprovalTeamVersionResponse.to_json)
           (Some
              Values.DeleteInactiveApprovalTeamVersionResponse.error_to_json)])
let get_approval_team =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING ApprovalTeamArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_approval_team (Values.GetApprovalTeamRequest.make ~arn ())
           (Some Values.GetApprovalTeamResponse.to_json)
           (Some Values.GetApprovalTeamResponse.error_to_json)])
let get_identity_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identitySourceArn =
         flag "identity-source-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_identity_source
           (Values.GetIdentitySourceRequest.make ~identitySourceArn ())
           (Some Values.GetIdentitySourceResponse.to_json)
           (Some Values.GetIdentitySourceResponse.error_to_json)])
let get_policy_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyVersionArn =
         flag "policy-version-arn" (required string)
           ~doc:"STRING QualifiedPolicyArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_policy_version
           (Values.GetPolicyVersionRequest.make ~policyVersionArn ())
           (Some Values.GetPolicyVersionResponse.to_json)
           (Some Values.GetPolicyVersionResponse.error_to_json)])
let get_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING String"
       and policyType =
         flag "policy-type" (required json_arg) ~doc:"JSON PolicyType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_policy
           (Values.GetResourcePolicyRequest.make ~resourceArn ~policyName
              ~policyType:(Values.PolicyType.of_json policyType) ())
           (Some Values.GetResourcePolicyResponse.to_json)
           (Some Values.GetResourcePolicyResponse.error_to_json)])
let get_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sessionArn =
         flag "session-arn" (required string) ~doc:"STRING SessionArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_session (Values.GetSessionRequest.make ~sessionArn ())
           (Some Values.GetSessionResponse.to_json)
           (Some Values.GetSessionResponse.error_to_json)])
let list_approval_teams =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_approval_teams
           (Values.ListApprovalTeamsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListApprovalTeamsResponse.to_json)
           (Some Values.ListApprovalTeamsResponse.error_to_json)])
let list_identity_sources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_identity_sources
           (Values.ListIdentitySourcesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListIdentitySourcesResponse.to_json)
           (Some Values.ListIdentitySourcesResponse.error_to_json)])
let list_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_policies
           (Values.ListPoliciesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListPoliciesResponse.to_json)
           (Some Values.ListPoliciesResponse.error_to_json)])
let list_policy_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and policyArn =
         flag "policy-arn" (required string)
           ~doc:"STRING UnqualifiedPolicyArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_policy_versions
           (Values.ListPolicyVersionsRequest.make ?maxResults ?nextToken
              ~policyArn ()) (Some Values.ListPolicyVersionsResponse.to_json)
           (Some Values.ListPolicyVersionsResponse.error_to_json)])
let list_resource_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resource_policies
           (Values.ListResourcePoliciesRequest.make ?maxResults ?nextToken
              ~resourceArn ())
           (Some Values.ListResourcePoliciesResponse.to_json)
           (Some Values.ListResourcePoliciesResponse.error_to_json)])
let list_sessions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters"
       and approvalTeamArn =
         flag "approval-team-arn" (required string)
           ~doc:"STRING ApprovalTeamArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_sessions
           (Values.ListSessionsRequest.make ?maxResults ?nextToken
              ?filters:(Option.map ~f:Values.Filters.of_json filters)
              ~approvalTeamArn ()) (Some Values.ListSessionsResponse.to_json)
           (Some Values.ListSessionsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let start_active_approval_team_deletion =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pendingWindowDays =
         flag "pending-window-days" (optional int) ~doc:"INT Integer"
       and arn = flag "arn" (required string) ~doc:"STRING ApprovalTeamArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_active_approval_team_deletion
           (Values.StartActiveApprovalTeamDeletionRequest.make
              ?pendingWindowDays ~arn ())
           (Some Values.StartActiveApprovalTeamDeletionResponse.to_json)
           (Some Values.StartActiveApprovalTeamDeletionResponse.error_to_json)])
let start_approval_team_baseline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and approverIds =
         flag "approver-ids" (optional json_arg)
           ~doc:"JSON StartApprovalTeamBaselineApproverIds"
       and arn = flag "arn" (required string) ~doc:"STRING ApprovalTeamArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_approval_team_baseline
           (Values.StartApprovalTeamBaselineRequest.make
              ?approverIds:(Option.map
                              ~f:Values.StartApprovalTeamBaselineApproverIds.of_json
                              approverIds) ~arn ())
           (Some Values.StartApprovalTeamBaselineResponse.to_json)
           (Some Values.StartApprovalTeamBaselineResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_approval_team =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and approvalStrategy =
         flag "approval-strategy" (optional json_arg)
           ~doc:"JSON ApprovalStrategy"
       and approvers =
         flag "approvers" (optional json_arg)
           ~doc:"JSON ApprovalTeamRequestApprovers"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and updateActions =
         flag "update-actions" (optional json_arg) ~doc:"JSON UpdateActions"
       and arn = flag "arn" (required string) ~doc:"STRING ApprovalTeamArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_approval_team
           (Values.UpdateApprovalTeamRequest.make
              ?approvalStrategy:(Option.map
                                   ~f:Values.ApprovalStrategy.of_json
                                   approvalStrategy)
              ?approvers:(Option.map
                            ~f:Values.ApprovalTeamRequestApprovers.of_json
                            approvers) ?description
              ?updateActions:(Option.map ~f:Values.UpdateActions.of_json
                                updateActions) ~arn ())
           (Some Values.UpdateApprovalTeamResponse.to_json)
           (Some Values.UpdateApprovalTeamResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("cancel-session", cancel_session);
    ("create-approval-team", create_approval_team);
    ("create-identity-source", create_identity_source);
    ("delete-identity-source", delete_identity_source);
    ("delete-inactive-approval-team-version",
      delete_inactive_approval_team_version);
    ("get-approval-team", get_approval_team);
    ("get-identity-source", get_identity_source);
    ("get-policy-version", get_policy_version);
    ("get-resource-policy", get_resource_policy);
    ("get-session", get_session);
    ("list-approval-teams", list_approval_teams);
    ("list-identity-sources", list_identity_sources);
    ("list-policies", list_policies);
    ("list-policy-versions", list_policy_versions);
    ("list-resource-policies", list_resource_policies);
    ("list-sessions", list_sessions);
    ("list-tags-for-resource", list_tags_for_resource);
    ("start-active-approval-team-deletion",
      start_active_approval_team_deletion);
    ("start-approval-team-baseline", start_approval_team_baseline);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-approval-team", update_approval_team)]
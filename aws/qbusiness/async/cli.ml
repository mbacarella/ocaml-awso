(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and conditions =
         flag "conditions" (optional json_arg)
           ~doc:"JSON PermissionConditions"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and statementId =
         flag "statement-id" (required string) ~doc:"STRING StatementId"
       and actions =
         flag "actions" (required json_arg) ~doc:"JSON QIamActions"
       and principal =
         flag "principal" (required string) ~doc:"STRING PrincipalRoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_permission
           (Values.AssociatePermissionRequest.make
              ?conditions:(Option.map ~f:Values.PermissionConditions.of_json
                             conditions) ~applicationId ~statementId
              ~actions:(Values.QIamActions.of_json actions) ~principal ())
           (Some Values.AssociatePermissionResponse.to_json)
           (Some Values.AssociatePermissionResponse.error_to_json)])
let batch_delete_document =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataSourceSyncId =
         flag "data-source-sync-id" (optional string)
           ~doc:"STRING ExecutionId"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and documents =
         flag "documents" (required json_arg) ~doc:"JSON DeleteDocuments" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_document
           (Values.BatchDeleteDocumentRequest.make ?dataSourceSyncId
              ~applicationId ~indexId
              ~documents:(Values.DeleteDocuments.of_json documents) ())
           (Some Values.BatchDeleteDocumentResponse.to_json)
           (Some Values.BatchDeleteDocumentResponse.error_to_json)])
let batch_put_document =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and dataSourceSyncId =
         flag "data-source-sync-id" (optional string)
           ~doc:"STRING ExecutionId"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and documents =
         flag "documents" (required json_arg) ~doc:"JSON Documents" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_put_document
           (Values.BatchPutDocumentRequest.make ?roleArn ?dataSourceSyncId
              ~applicationId ~indexId
              ~documents:(Values.Documents.of_json documents) ())
           (Some Values.BatchPutDocumentResponse.to_json)
           (Some Values.BatchPutDocumentResponse.error_to_json)])
let cancel_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and subscriptionId =
         flag "subscription-id" (required string)
           ~doc:"STRING SubscriptionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_subscription
           (Values.CancelSubscriptionRequest.make ~applicationId
              ~subscriptionId ())
           (Some Values.CancelSubscriptionResponse.to_json)
           (Some Values.CancelSubscriptionResponse.error_to_json)])
let chat =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userId = flag "user-id" (optional string) ~doc:"STRING UserId"
       and userGroups =
         flag "user-groups" (optional json_arg) ~doc:"JSON UserGroups"
       and conversationId =
         flag "conversation-id" (optional string)
           ~doc:"STRING ConversationId"
       and parentMessageId =
         flag "parent-message-id" (optional string) ~doc:"STRING MessageId"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and inputStream =
         flag "input-stream" (optional json_arg) ~doc:"JSON ChatInputStream"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region Io.chat
           (Values.ChatInput.make ?userId
              ?userGroups:(Option.map ~f:Values.UserGroups.of_json userGroups)
              ?conversationId ?parentMessageId ?clientToken
              ?inputStream:(Option.map ~f:Values.ChatInputStream.of_json
                              inputStream) ~applicationId ())
           (Some Values.ChatOutput.to_json)
           (Some Values.ChatOutput.error_to_json)])
let chat_sync =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userId = flag "user-id" (optional string) ~doc:"STRING UserId"
       and userGroups =
         flag "user-groups" (optional json_arg) ~doc:"JSON UserGroups"
       and userMessage =
         flag "user-message" (optional string) ~doc:"STRING UserMessage"
       and attachments =
         flag "attachments" (optional json_arg) ~doc:"JSON AttachmentsInput"
       and actionExecution =
         flag "action-execution" (optional json_arg)
           ~doc:"JSON ActionExecution"
       and authChallengeResponse =
         flag "auth-challenge-response" (optional json_arg)
           ~doc:"JSON AuthChallengeResponse"
       and conversationId =
         flag "conversation-id" (optional string)
           ~doc:"STRING ConversationId"
       and parentMessageId =
         flag "parent-message-id" (optional string) ~doc:"STRING MessageId"
       and attributeFilter =
         flag "attribute-filter" (optional json_arg)
           ~doc:"JSON AttributeFilter"
       and chatMode =
         flag "chat-mode" (optional json_arg) ~doc:"JSON ChatMode"
       and chatModeConfiguration =
         flag "chat-mode-configuration" (optional json_arg)
           ~doc:"JSON ChatModeConfiguration"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.chat_sync
           (Values.ChatSyncInput.make ?userId
              ?userGroups:(Option.map ~f:Values.UserGroups.of_json userGroups)
              ?userMessage
              ?attachments:(Option.map ~f:Values.AttachmentsInput.of_json
                              attachments)
              ?actionExecution:(Option.map ~f:Values.ActionExecution.of_json
                                  actionExecution)
              ?authChallengeResponse:(Option.map
                                        ~f:Values.AuthChallengeResponse.of_json
                                        authChallengeResponse)
              ?conversationId ?parentMessageId
              ?attributeFilter:(Option.map ~f:Values.AttributeFilter.of_json
                                  attributeFilter)
              ?chatMode:(Option.map ~f:Values.ChatMode.of_json chatMode)
              ?chatModeConfiguration:(Option.map
                                        ~f:Values.ChatModeConfiguration.of_json
                                        chatModeConfiguration) ?clientToken
              ~applicationId ()) (Some Values.ChatSyncOutput.to_json)
           (Some Values.ChatSyncOutput.error_to_json)])
let check_document_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataSourceId =
         flag "data-source-id" (optional string) ~doc:"STRING DataSourceId"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and userId = flag "user-id" (required string) ~doc:"STRING String"
       and documentId =
         flag "document-id" (required string) ~doc:"STRING DocumentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.check_document_access
           (Values.CheckDocumentAccessRequest.make ?dataSourceId
              ~applicationId ~indexId ~userId ~documentId ())
           (Some Values.CheckDocumentAccessResponse.to_json)
           (Some Values.CheckDocumentAccessResponse.error_to_json)])
let create_anonymous_web_experience_url =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sessionDurationInMinutes =
         flag "session-duration-in-minutes" (optional int)
           ~doc:"INT SessionDurationInMinutes"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and webExperienceId =
         flag "web-experience-id" (required string)
           ~doc:"STRING WebExperienceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_anonymous_web_experience_url
           (Values.CreateAnonymousWebExperienceUrlRequest.make
              ?sessionDurationInMinutes ~applicationId ~webExperienceId ())
           (Some Values.CreateAnonymousWebExperienceUrlResponse.to_json)
           (Some Values.CreateAnonymousWebExperienceUrlResponse.error_to_json)])
let create_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and identityType =
         flag "identity-type" (optional json_arg) ~doc:"JSON IdentityType"
       and iamIdentityProviderArn =
         flag "iam-identity-provider-arn" (optional string)
           ~doc:"STRING IAMIdentityProviderArn"
       and identityCenterInstanceArn =
         flag "identity-center-instance-arn" (optional string)
           ~doc:"STRING InstanceArn"
       and clientIdsForOIDC =
         flag "client-ids-for-o-i-d-c" (optional json_arg)
           ~doc:"JSON ClientIdsForOIDC"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and encryptionConfiguration =
         flag "encryption-configuration" (optional json_arg)
           ~doc:"JSON EncryptionConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and attachmentsConfiguration =
         flag "attachments-configuration" (optional json_arg)
           ~doc:"JSON AttachmentsConfiguration"
       and qAppsConfiguration =
         flag "q-apps-configuration" (optional json_arg)
           ~doc:"JSON QAppsConfiguration"
       and personalizationConfiguration =
         flag "personalization-configuration" (optional json_arg)
           ~doc:"JSON PersonalizationConfiguration"
       and quickSightConfiguration =
         flag "quick-sight-configuration" (optional json_arg)
           ~doc:"JSON QuickSightConfiguration"
       and displayName =
         flag "display-name" (required string) ~doc:"STRING ApplicationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_application
           (Values.CreateApplicationRequest.make ?roleArn
              ?identityType:(Option.map ~f:Values.IdentityType.of_json
                               identityType) ?iamIdentityProviderArn
              ?identityCenterInstanceArn
              ?clientIdsForOIDC:(Option.map
                                   ~f:Values.ClientIdsForOIDC.of_json
                                   clientIdsForOIDC) ?description
              ?encryptionConfiguration:(Option.map
                                          ~f:Values.EncryptionConfiguration.of_json
                                          encryptionConfiguration)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ?clientToken
              ?attachmentsConfiguration:(Option.map
                                           ~f:Values.AttachmentsConfiguration.of_json
                                           attachmentsConfiguration)
              ?qAppsConfiguration:(Option.map
                                     ~f:Values.QAppsConfiguration.of_json
                                     qAppsConfiguration)
              ?personalizationConfiguration:(Option.map
                                               ~f:Values.PersonalizationConfiguration.of_json
                                               personalizationConfiguration)
              ?quickSightConfiguration:(Option.map
                                          ~f:Values.QuickSightConfiguration.of_json
                                          quickSightConfiguration)
              ~displayName ())
           (Some Values.CreateApplicationResponse.to_json)
           (Some Values.CreateApplicationResponse.error_to_json)])
let create_chat_response_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and displayName =
         flag "display-name" (required string) ~doc:"STRING DisplayName"
       and responseConfigurations =
         flag "response-configurations" (required json_arg)
           ~doc:"JSON ResponseConfigurations" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_chat_response_configuration
           (Values.CreateChatResponseConfigurationRequest.make ?clientToken
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~applicationId
              ~displayName
              ~responseConfigurations:(Values.ResponseConfigurations.of_json
                                         responseConfigurations) ())
           (Some Values.CreateChatResponseConfigurationResponse.to_json)
           (Some Values.CreateChatResponseConfigurationResponse.error_to_json)])
let create_data_accessor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and authenticationDetail =
         flag "authentication-detail" (optional json_arg)
           ~doc:"JSON DataAccessorAuthenticationDetail"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and principal =
         flag "principal" (required string) ~doc:"STRING PrincipalRoleArn"
       and actionConfigurations =
         flag "action-configurations" (required json_arg)
           ~doc:"JSON ActionConfigurationList"
       and displayName =
         flag "display-name" (required string) ~doc:"STRING DataAccessorName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_accessor
           (Values.CreateDataAccessorRequest.make ?clientToken
              ?authenticationDetail:(Option.map
                                       ~f:Values.DataAccessorAuthenticationDetail.of_json
                                       authenticationDetail)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~applicationId
              ~principal
              ~actionConfigurations:(Values.ActionConfigurationList.of_json
                                       actionConfigurations) ~displayName ())
           (Some Values.CreateDataAccessorResponse.to_json)
           (Some Values.CreateDataAccessorResponse.error_to_json)])
let create_data_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vpcConfiguration =
         flag "vpc-configuration" (optional json_arg)
           ~doc:"JSON DataSourceVpcConfiguration"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and syncSchedule =
         flag "sync-schedule" (optional string) ~doc:"STRING SyncSchedule"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and documentEnrichmentConfiguration =
         flag "document-enrichment-configuration" (optional json_arg)
           ~doc:"JSON DocumentEnrichmentConfiguration"
       and mediaExtractionConfiguration =
         flag "media-extraction-configuration" (optional json_arg)
           ~doc:"JSON MediaExtractionConfiguration"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and displayName =
         flag "display-name" (required string) ~doc:"STRING DataSourceName"
       and configuration =
         flag "configuration" (required json_arg)
           ~doc:"JSON DataSourceConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_source
           (Values.CreateDataSourceRequest.make
              ?vpcConfiguration:(Option.map
                                   ~f:Values.DataSourceVpcConfiguration.of_json
                                   vpcConfiguration) ?description
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ?syncSchedule
              ?roleArn ?clientToken
              ?documentEnrichmentConfiguration:(Option.map
                                                  ~f:Values.DocumentEnrichmentConfiguration.of_json
                                                  documentEnrichmentConfiguration)
              ?mediaExtractionConfiguration:(Option.map
                                               ~f:Values.MediaExtractionConfiguration.of_json
                                               mediaExtractionConfiguration)
              ~applicationId ~indexId ~displayName
              ~configuration:(Values.DataSourceConfiguration.of_json
                                configuration) ())
           (Some Values.CreateDataSourceResponse.to_json)
           (Some Values.CreateDataSourceResponse.error_to_json)])
let create_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON IndexType"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and capacityConfiguration =
         flag "capacity-configuration" (optional json_arg)
           ~doc:"JSON IndexCapacityConfiguration"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and displayName =
         flag "display-name" (required string) ~doc:"STRING IndexName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_index
           (Values.CreateIndexRequest.make ?description
              ?type_:(Option.map ~f:Values.IndexType.of_json type_)
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?capacityConfiguration:(Option.map
                                        ~f:Values.IndexCapacityConfiguration.of_json
                                        capacityConfiguration) ?clientToken
              ~applicationId ~displayName ())
           (Some Values.CreateIndexResponse.to_json)
           (Some Values.CreateIndexResponse.error_to_json)])
let create_plugin =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serverUrl = flag "server-url" (optional string) ~doc:"STRING Url"
       and customPluginConfiguration =
         flag "custom-plugin-configuration" (optional json_arg)
           ~doc:"JSON CustomPluginConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and displayName =
         flag "display-name" (required string) ~doc:"STRING PluginName"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON PluginType"
       and authConfiguration =
         flag "auth-configuration" (required json_arg)
           ~doc:"JSON PluginAuthConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_plugin
           (Values.CreatePluginRequest.make ?serverUrl
              ?customPluginConfiguration:(Option.map
                                            ~f:Values.CustomPluginConfiguration.of_json
                                            customPluginConfiguration)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ?clientToken
              ~applicationId ~displayName
              ~type_:(Values.PluginType.of_json type_)
              ~authConfiguration:(Values.PluginAuthConfiguration.of_json
                                    authConfiguration) ())
           (Some Values.CreatePluginResponse.to_json)
           (Some Values.CreatePluginResponse.error_to_json)])
let create_retriever =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON RetrieverType"
       and displayName =
         flag "display-name" (required string) ~doc:"STRING RetrieverName"
       and configuration =
         flag "configuration" (required json_arg)
           ~doc:"JSON RetrieverConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_retriever
           (Values.CreateRetrieverRequest.make ?roleArn ?clientToken
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~applicationId
              ~type_:(Values.RetrieverType.of_json type_) ~displayName
              ~configuration:(Values.RetrieverConfiguration.of_json
                                configuration) ())
           (Some Values.CreateRetrieverResponse.to_json)
           (Some Values.CreateRetrieverResponse.error_to_json)])
let create_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and principal =
         flag "principal" (required json_arg)
           ~doc:"JSON SubscriptionPrincipal"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON SubscriptionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_subscription
           (Values.CreateSubscriptionRequest.make ?clientToken ~applicationId
              ~principal:(Values.SubscriptionPrincipal.of_json principal)
              ~type_:(Values.SubscriptionType.of_json type_) ())
           (Some Values.CreateSubscriptionResponse.to_json)
           (Some Values.CreateSubscriptionResponse.error_to_json)])
let create_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userAliases =
         flag "user-aliases" (optional json_arg)
           ~doc:"JSON CreateUserRequestUserAliasesList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and userId = flag "user-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_user
           (Values.CreateUserRequest.make
              ?userAliases:(Option.map
                              ~f:Values.CreateUserRequestUserAliasesList.of_json
                              userAliases) ?clientToken ~applicationId
              ~userId ()) (Some Values.CreateUserResponse.to_json)
           (Some Values.CreateUserResponse.error_to_json)])
let create_web_experience =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and title =
         flag "title" (optional string) ~doc:"STRING WebExperienceTitle"
       and subtitle =
         flag "subtitle" (optional string)
           ~doc:"STRING WebExperienceSubtitle"
       and welcomeMessage =
         flag "welcome-message" (optional string)
           ~doc:"STRING WebExperienceWelcomeMessage"
       and samplePromptsControlMode =
         flag "sample-prompts-control-mode" (optional json_arg)
           ~doc:"JSON WebExperienceSamplePromptsControlMode"
       and origins =
         flag "origins" (optional json_arg) ~doc:"JSON WebExperienceOrigins"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and identityProviderConfiguration =
         flag "identity-provider-configuration" (optional json_arg)
           ~doc:"JSON IdentityProviderConfiguration"
       and browserExtensionConfiguration =
         flag "browser-extension-configuration" (optional json_arg)
           ~doc:"JSON BrowserExtensionConfiguration"
       and customizationConfiguration =
         flag "customization-configuration" (optional json_arg)
           ~doc:"JSON CustomizationConfiguration"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_web_experience
           (Values.CreateWebExperienceRequest.make ?title ?subtitle
              ?welcomeMessage
              ?samplePromptsControlMode:(Option.map
                                           ~f:Values.WebExperienceSamplePromptsControlMode.of_json
                                           samplePromptsControlMode)
              ?origins:(Option.map ~f:Values.WebExperienceOrigins.of_json
                          origins) ?roleArn
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ?clientToken
              ?identityProviderConfiguration:(Option.map
                                                ~f:Values.IdentityProviderConfiguration.of_json
                                                identityProviderConfiguration)
              ?browserExtensionConfiguration:(Option.map
                                                ~f:Values.BrowserExtensionConfiguration.of_json
                                                browserExtensionConfiguration)
              ?customizationConfiguration:(Option.map
                                             ~f:Values.CustomizationConfiguration.of_json
                                             customizationConfiguration)
              ~applicationId ())
           (Some Values.CreateWebExperienceResponse.to_json)
           (Some Values.CreateWebExperienceResponse.error_to_json)])
let delete_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_application
           (Values.DeleteApplicationRequest.make ~applicationId ())
           (Some Values.DeleteApplicationResponse.to_json)
           (Some Values.DeleteApplicationResponse.error_to_json)])
let delete_attachment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userId = flag "user-id" (optional string) ~doc:"STRING UserId"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and conversationId =
         flag "conversation-id" (required string)
           ~doc:"STRING ConversationId"
       and attachmentId =
         flag "attachment-id" (required string) ~doc:"STRING AttachmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_attachment
           (Values.DeleteAttachmentRequest.make ?userId ~applicationId
              ~conversationId ~attachmentId ())
           (Some Values.DeleteAttachmentResponse.to_json)
           (Some Values.DeleteAttachmentResponse.error_to_json)])
let delete_chat_controls_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_chat_controls_configuration
           (Values.DeleteChatControlsConfigurationRequest.make ~applicationId
              ())
           (Some Values.DeleteChatControlsConfigurationResponse.to_json)
           (Some Values.DeleteChatControlsConfigurationResponse.error_to_json)])
let delete_chat_response_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and chatResponseConfigurationId =
         flag "chat-response-configuration-id" (required string)
           ~doc:"STRING ChatResponseConfigurationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_chat_response_configuration
           (Values.DeleteChatResponseConfigurationRequest.make ~applicationId
              ~chatResponseConfigurationId ())
           (Some Values.DeleteChatResponseConfigurationResponse.to_json)
           (Some Values.DeleteChatResponseConfigurationResponse.error_to_json)])
let delete_conversation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userId = flag "user-id" (optional string) ~doc:"STRING UserId"
       and conversationId =
         flag "conversation-id" (required string)
           ~doc:"STRING ConversationId"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_conversation
           (Values.DeleteConversationRequest.make ?userId ~conversationId
              ~applicationId ())
           (Some Values.DeleteConversationResponse.to_json)
           (Some Values.DeleteConversationResponse.error_to_json)])
let delete_data_accessor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and dataAccessorId =
         flag "data-accessor-id" (required string)
           ~doc:"STRING DataAccessorId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_data_accessor
           (Values.DeleteDataAccessorRequest.make ~applicationId
              ~dataAccessorId ())
           (Some Values.DeleteDataAccessorResponse.to_json)
           (Some Values.DeleteDataAccessorResponse.error_to_json)])
let delete_data_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING DataSourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_data_source
           (Values.DeleteDataSourceRequest.make ~applicationId ~indexId
              ~dataSourceId ())
           (Some Values.DeleteDataSourceResponse.to_json)
           (Some Values.DeleteDataSourceResponse.error_to_json)])
let delete_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataSourceId =
         flag "data-source-id" (optional string) ~doc:"STRING DataSourceId"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING GroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_group
           (Values.DeleteGroupRequest.make ?dataSourceId ~applicationId
              ~indexId ~groupName ())
           (Some Values.DeleteGroupResponse.to_json)
           (Some Values.DeleteGroupResponse.error_to_json)])
let delete_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_index
           (Values.DeleteIndexRequest.make ~applicationId ~indexId ())
           (Some Values.DeleteIndexResponse.to_json)
           (Some Values.DeleteIndexResponse.error_to_json)])
let delete_plugin =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and pluginId =
         flag "plugin-id" (required string) ~doc:"STRING PluginId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_plugin
           (Values.DeletePluginRequest.make ~applicationId ~pluginId ())
           (Some Values.DeletePluginResponse.to_json)
           (Some Values.DeletePluginResponse.error_to_json)])
let delete_retriever =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and retrieverId =
         flag "retriever-id" (required string) ~doc:"STRING RetrieverId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_retriever
           (Values.DeleteRetrieverRequest.make ~applicationId ~retrieverId ())
           (Some Values.DeleteRetrieverResponse.to_json)
           (Some Values.DeleteRetrieverResponse.error_to_json)])
let delete_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and userId = flag "user-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_user
           (Values.DeleteUserRequest.make ~applicationId ~userId ())
           (Some Values.DeleteUserResponse.to_json)
           (Some Values.DeleteUserResponse.error_to_json)])
let delete_web_experience =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and webExperienceId =
         flag "web-experience-id" (required string)
           ~doc:"STRING WebExperienceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_web_experience
           (Values.DeleteWebExperienceRequest.make ~applicationId
              ~webExperienceId ())
           (Some Values.DeleteWebExperienceResponse.to_json)
           (Some Values.DeleteWebExperienceResponse.error_to_json)])
let disassociate_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and statementId =
         flag "statement-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_permission
           (Values.DisassociatePermissionRequest.make ~applicationId
              ~statementId ())
           (Some Values.DisassociatePermissionResponse.to_json)
           (Some Values.DisassociatePermissionResponse.error_to_json)])
let get_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_application
           (Values.GetApplicationRequest.make ~applicationId ())
           (Some Values.GetApplicationResponse.to_json)
           (Some Values.GetApplicationResponse.error_to_json)])
let get_chat_controls_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForGetTopicConfigurations"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_chat_controls_configuration
           (Values.GetChatControlsConfigurationRequest.make ?maxResults
              ?nextToken ~applicationId ())
           (Some Values.GetChatControlsConfigurationResponse.to_json)
           (Some Values.GetChatControlsConfigurationResponse.error_to_json)])
let get_chat_response_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and chatResponseConfigurationId =
         flag "chat-response-configuration-id" (required string)
           ~doc:"STRING ChatResponseConfigurationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_chat_response_configuration
           (Values.GetChatResponseConfigurationRequest.make ~applicationId
              ~chatResponseConfigurationId ())
           (Some Values.GetChatResponseConfigurationResponse.to_json)
           (Some Values.GetChatResponseConfigurationResponse.error_to_json)])
let get_data_accessor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and dataAccessorId =
         flag "data-accessor-id" (required string)
           ~doc:"STRING DataAccessorId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_accessor
           (Values.GetDataAccessorRequest.make ~applicationId ~dataAccessorId
              ()) (Some Values.GetDataAccessorResponse.to_json)
           (Some Values.GetDataAccessorResponse.error_to_json)])
let get_data_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING DataSourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_source
           (Values.GetDataSourceRequest.make ~applicationId ~indexId
              ~dataSourceId ()) (Some Values.GetDataSourceResponse.to_json)
           (Some Values.GetDataSourceResponse.error_to_json)])
let get_document_content =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataSourceId =
         flag "data-source-id" (optional string) ~doc:"STRING DataSourceId"
       and outputFormat =
         flag "output-format" (optional json_arg) ~doc:"JSON OutputFormat"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and documentId =
         flag "document-id" (required string) ~doc:"STRING DocumentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_document_content
           (Values.GetDocumentContentRequest.make ?dataSourceId
              ?outputFormat:(Option.map ~f:Values.OutputFormat.of_json
                               outputFormat) ~applicationId ~indexId
              ~documentId ())
           (Some Values.GetDocumentContentResponse.to_json)
           (Some Values.GetDocumentContentResponse.error_to_json)])
let get_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataSourceId =
         flag "data-source-id" (optional string) ~doc:"STRING DataSourceId"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING GroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_group
           (Values.GetGroupRequest.make ?dataSourceId ~applicationId ~indexId
              ~groupName ()) (Some Values.GetGroupResponse.to_json)
           (Some Values.GetGroupResponse.error_to_json)])
let get_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_index
           (Values.GetIndexRequest.make ~applicationId ~indexId ())
           (Some Values.GetIndexResponse.to_json)
           (Some Values.GetIndexResponse.error_to_json)])
let get_media =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and conversationId =
         flag "conversation-id" (required string)
           ~doc:"STRING ConversationId"
       and messageId =
         flag "message-id" (required string) ~doc:"STRING MessageId"
       and mediaId = flag "media-id" (required string) ~doc:"STRING MediaId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_media
           (Values.GetMediaRequest.make ~applicationId ~conversationId
              ~messageId ~mediaId ()) (Some Values.GetMediaResponse.to_json)
           (Some Values.GetMediaResponse.error_to_json)])
let get_plugin =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and pluginId =
         flag "plugin-id" (required string) ~doc:"STRING PluginId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_plugin
           (Values.GetPluginRequest.make ~applicationId ~pluginId ())
           (Some Values.GetPluginResponse.to_json)
           (Some Values.GetPluginResponse.error_to_json)])
let get_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_policy (Values.GetPolicyRequest.make ~applicationId ())
           (Some Values.GetPolicyResponse.to_json)
           (Some Values.GetPolicyResponse.error_to_json)])
let get_retriever =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and retrieverId =
         flag "retriever-id" (required string) ~doc:"STRING RetrieverId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_retriever
           (Values.GetRetrieverRequest.make ~applicationId ~retrieverId ())
           (Some Values.GetRetrieverResponse.to_json)
           (Some Values.GetRetrieverResponse.error_to_json)])
let get_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and userId = flag "user-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_user (Values.GetUserRequest.make ~applicationId ~userId ())
           (Some Values.GetUserResponse.to_json)
           (Some Values.GetUserResponse.error_to_json)])
let get_web_experience =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and webExperienceId =
         flag "web-experience-id" (required string)
           ~doc:"STRING WebExperienceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_web_experience
           (Values.GetWebExperienceRequest.make ~applicationId
              ~webExperienceId ())
           (Some Values.GetWebExperienceResponse.to_json)
           (Some Values.GetWebExperienceResponse.error_to_json)])
let list_applications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListApplications" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_applications
           (Values.ListApplicationsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListApplicationsResponse.to_json)
           (Some Values.ListApplicationsResponse.error_to_json)])
let list_attachments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and conversationId =
         flag "conversation-id" (optional string)
           ~doc:"STRING ConversationId"
       and userId = flag "user-id" (optional string) ~doc:"STRING UserId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListAttachments"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_attachments
           (Values.ListAttachmentsRequest.make ?conversationId ?userId
              ?nextToken ?maxResults ~applicationId ())
           (Some Values.ListAttachmentsResponse.to_json)
           (Some Values.ListAttachmentsResponse.error_to_json)])
let list_chat_response_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_chat_response_configurations
           (Values.ListChatResponseConfigurationsRequest.make ?maxResults
              ?nextToken ~applicationId ())
           (Some Values.ListChatResponseConfigurationsResponse.to_json)
           (Some Values.ListChatResponseConfigurationsResponse.error_to_json)])
let list_conversations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userId = flag "user-id" (optional string) ~doc:"STRING UserId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListConversations"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_conversations
           (Values.ListConversationsRequest.make ?userId ?nextToken
              ?maxResults ~applicationId ())
           (Some Values.ListConversationsResponse.to_json)
           (Some Values.ListConversationsResponse.error_to_json)])
let list_data_accessors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken1500"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListDataAccessors"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_accessors
           (Values.ListDataAccessorsRequest.make ?nextToken ?maxResults
              ~applicationId ())
           (Some Values.ListDataAccessorsResponse.to_json)
           (Some Values.ListDataAccessorsResponse.error_to_json)])
let list_data_source_sync_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListDataSourcesSyncJobs"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (optional json_arg) ~doc:"JSON Timestamp"
       and statusFilter =
         flag "status-filter" (optional json_arg)
           ~doc:"JSON DataSourceSyncJobStatus"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING DataSourceId"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_source_sync_jobs
           (Values.ListDataSourceSyncJobsRequest.make ?nextToken ?maxResults
              ?startTime:(Option.map ~f:Values.Timestamp.of_json startTime)
              ?endTime:(Option.map ~f:Values.Timestamp.of_json endTime)
              ?statusFilter:(Option.map
                               ~f:Values.DataSourceSyncJobStatus.of_json
                               statusFilter) ~dataSourceId ~applicationId
              ~indexId ())
           (Some Values.ListDataSourceSyncJobsResponse.to_json)
           (Some Values.ListDataSourceSyncJobsResponse.error_to_json)])
let list_data_sources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListDataSources"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_sources
           (Values.ListDataSourcesRequest.make ?nextToken ?maxResults
              ~applicationId ~indexId ())
           (Some Values.ListDataSourcesResponse.to_json)
           (Some Values.ListDataSourcesResponse.error_to_json)])
let list_documents =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataSourceIds =
         flag "data-source-ids" (optional json_arg) ~doc:"JSON DataSourceIds"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListDocuments"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_documents
           (Values.ListDocumentsRequest.make
              ?dataSourceIds:(Option.map ~f:Values.DataSourceIds.of_json
                                dataSourceIds) ?nextToken ?maxResults
              ~applicationId ~indexId ())
           (Some Values.ListDocumentsResponse.to_json)
           (Some Values.ListDocumentsResponse.error_to_json)])
let list_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataSourceId =
         flag "data-source-id" (optional string) ~doc:"STRING DataSourceId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListGroupsRequest"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and updatedEarlierThan =
         flag "updated-earlier-than" (required json_arg)
           ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_groups
           (Values.ListGroupsRequest.make ?dataSourceId ?nextToken
              ?maxResults ~applicationId ~indexId
              ~updatedEarlierThan:(Values.Timestamp.of_json
                                     updatedEarlierThan) ())
           (Some Values.ListGroupsResponse.to_json)
           (Some Values.ListGroupsResponse.error_to_json)])
let list_indices =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListIndices"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_indices
           (Values.ListIndicesRequest.make ?nextToken ?maxResults
              ~applicationId ()) (Some Values.ListIndicesResponse.to_json)
           (Some Values.ListIndicesResponse.error_to_json)])
let list_messages =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userId = flag "user-id" (optional string) ~doc:"STRING UserId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListMessages"
       and conversationId =
         flag "conversation-id" (required string)
           ~doc:"STRING ConversationId"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_messages
           (Values.ListMessagesRequest.make ?userId ?nextToken ?maxResults
              ~conversationId ~applicationId ())
           (Some Values.ListMessagesResponse.to_json)
           (Some Values.ListMessagesResponse.error_to_json)])
let list_plugin_actions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListPluginActions"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and pluginId =
         flag "plugin-id" (required string) ~doc:"STRING PluginId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_plugin_actions
           (Values.ListPluginActionsRequest.make ?nextToken ?maxResults
              ~applicationId ~pluginId ())
           (Some Values.ListPluginActionsResponse.to_json)
           (Some Values.ListPluginActionsResponse.error_to_json)])
let list_plugin_type_actions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListPluginTypeActions"
       and pluginType =
         flag "plugin-type" (required json_arg) ~doc:"JSON PluginType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_plugin_type_actions
           (Values.ListPluginTypeActionsRequest.make ?nextToken ?maxResults
              ~pluginType:(Values.PluginType.of_json pluginType) ())
           (Some Values.ListPluginTypeActionsResponse.to_json)
           (Some Values.ListPluginTypeActionsResponse.error_to_json)])
let list_plugin_type_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListPluginTypeMetadata" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_plugin_type_metadata
           (Values.ListPluginTypeMetadataRequest.make ?nextToken ?maxResults
              ()) (Some Values.ListPluginTypeMetadataResponse.to_json)
           (Some Values.ListPluginTypeMetadataResponse.error_to_json)])
let list_plugins =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListPlugins"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_plugins
           (Values.ListPluginsRequest.make ?nextToken ?maxResults
              ~applicationId ()) (Some Values.ListPluginsResponse.to_json)
           (Some Values.ListPluginsResponse.error_to_json)])
let list_retrievers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListRetrieversRequest"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_retrievers
           (Values.ListRetrieversRequest.make ?nextToken ?maxResults
              ~applicationId ()) (Some Values.ListRetrieversResponse.to_json)
           (Some Values.ListRetrieversResponse.error_to_json)])
let list_subscriptions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListSubscriptions"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_subscriptions
           (Values.ListSubscriptionsRequest.make ?nextToken ?maxResults
              ~applicationId ())
           (Some Values.ListSubscriptionsResponse.to_json)
           (Some Values.ListSubscriptionsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_web_experiences =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxResultsIntegerForListWebExperiencesRequest"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_web_experiences
           (Values.ListWebExperiencesRequest.make ?nextToken ?maxResults
              ~applicationId ())
           (Some Values.ListWebExperiencesResponse.to_json)
           (Some Values.ListWebExperiencesResponse.error_to_json)])
let put_feedback =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userId = flag "user-id" (optional string) ~doc:"STRING UserId"
       and messageCopiedAt =
         flag "message-copied-at" (optional json_arg) ~doc:"JSON Timestamp"
       and messageUsefulness =
         flag "message-usefulness" (optional json_arg)
           ~doc:"JSON MessageUsefulnessFeedback"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and conversationId =
         flag "conversation-id" (required string)
           ~doc:"STRING ConversationId"
       and messageId =
         flag "message-id" (required string) ~doc:"STRING SystemMessageId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_feedback
           (Values.PutFeedbackRequest.make ?userId
              ?messageCopiedAt:(Option.map ~f:Values.Timestamp.of_json
                                  messageCopiedAt)
              ?messageUsefulness:(Option.map
                                    ~f:Values.MessageUsefulnessFeedback.of_json
                                    messageUsefulness) ~applicationId
              ~conversationId ~messageId ()) None None])
let put_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataSourceId =
         flag "data-source-id" (optional string) ~doc:"STRING DataSourceId"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and groupName =
         flag "group-name" (required string) ~doc:"STRING GroupName"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON MembershipType"
       and groupMembers =
         flag "group-members" (required json_arg) ~doc:"JSON GroupMembers" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_group
           (Values.PutGroupRequest.make ?dataSourceId ?roleArn ~applicationId
              ~indexId ~groupName
              ~type_:(Values.MembershipType.of_json type_)
              ~groupMembers:(Values.GroupMembers.of_json groupMembers) ())
           (Some Values.PutGroupResponse.to_json)
           (Some Values.PutGroupResponse.error_to_json)])
let search_relevant_content =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributeFilter =
         flag "attribute-filter" (optional json_arg)
           ~doc:"JSON AttributeFilter"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and queryText =
         flag "query-text" (required string) ~doc:"STRING QueryText"
       and contentSource =
         flag "content-source" (required json_arg) ~doc:"JSON ContentSource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_relevant_content
           (Values.SearchRelevantContentRequest.make
              ?attributeFilter:(Option.map ~f:Values.AttributeFilter.of_json
                                  attributeFilter) ?maxResults ?nextToken
              ~applicationId ~queryText
              ~contentSource:(Values.ContentSource.of_json contentSource) ())
           (Some Values.SearchRelevantContentResponse.to_json)
           (Some Values.SearchRelevantContentResponse.error_to_json)])
let start_data_source_sync_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING DataSourceId"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_data_source_sync_job
           (Values.StartDataSourceSyncJobRequest.make ~dataSourceId
              ~applicationId ~indexId ())
           (Some Values.StartDataSourceSyncJobResponse.to_json)
           (Some Values.StartDataSourceSyncJobResponse.error_to_json)])
let stop_data_source_sync_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING DataSourceId"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_data_source_sync_job
           (Values.StopDataSourceSyncJobRequest.make ~dataSourceId
              ~applicationId ~indexId ())
           (Some Values.StopDataSourceSyncJobResponse.to_json)
           (Some Values.StopDataSourceSyncJobResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identityCenterInstanceArn =
         flag "identity-center-instance-arn" (optional string)
           ~doc:"STRING InstanceArn"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING ApplicationName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and attachmentsConfiguration =
         flag "attachments-configuration" (optional json_arg)
           ~doc:"JSON AttachmentsConfiguration"
       and qAppsConfiguration =
         flag "q-apps-configuration" (optional json_arg)
           ~doc:"JSON QAppsConfiguration"
       and personalizationConfiguration =
         flag "personalization-configuration" (optional json_arg)
           ~doc:"JSON PersonalizationConfiguration"
       and autoSubscriptionConfiguration =
         flag "auto-subscription-configuration" (optional json_arg)
           ~doc:"JSON AutoSubscriptionConfiguration"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_application
           (Values.UpdateApplicationRequest.make ?identityCenterInstanceArn
              ?displayName ?description ?roleArn
              ?attachmentsConfiguration:(Option.map
                                           ~f:Values.AttachmentsConfiguration.of_json
                                           attachmentsConfiguration)
              ?qAppsConfiguration:(Option.map
                                     ~f:Values.QAppsConfiguration.of_json
                                     qAppsConfiguration)
              ?personalizationConfiguration:(Option.map
                                               ~f:Values.PersonalizationConfiguration.of_json
                                               personalizationConfiguration)
              ?autoSubscriptionConfiguration:(Option.map
                                                ~f:Values.AutoSubscriptionConfiguration.of_json
                                                autoSubscriptionConfiguration)
              ~applicationId ())
           (Some Values.UpdateApplicationResponse.to_json)
           (Some Values.UpdateApplicationResponse.error_to_json)])
let update_chat_controls_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and responseScope =
         flag "response-scope" (optional json_arg) ~doc:"JSON ResponseScope"
       and orchestrationConfiguration =
         flag "orchestration-configuration" (optional json_arg)
           ~doc:"JSON OrchestrationConfiguration"
       and blockedPhrasesConfigurationUpdate =
         flag "blocked-phrases-configuration-update" (optional json_arg)
           ~doc:"JSON BlockedPhrasesConfigurationUpdate"
       and topicConfigurationsToCreateOrUpdate =
         flag "topic-configurations-to-create-or-update" (optional json_arg)
           ~doc:"JSON TopicConfigurations"
       and topicConfigurationsToDelete =
         flag "topic-configurations-to-delete" (optional json_arg)
           ~doc:"JSON TopicConfigurations"
       and creatorModeConfiguration =
         flag "creator-mode-configuration" (optional json_arg)
           ~doc:"JSON CreatorModeConfiguration"
       and hallucinationReductionConfiguration =
         flag "hallucination-reduction-configuration" (optional json_arg)
           ~doc:"JSON HallucinationReductionConfiguration"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_chat_controls_configuration
           (Values.UpdateChatControlsConfigurationRequest.make ?clientToken
              ?responseScope:(Option.map ~f:Values.ResponseScope.of_json
                                responseScope)
              ?orchestrationConfiguration:(Option.map
                                             ~f:Values.OrchestrationConfiguration.of_json
                                             orchestrationConfiguration)
              ?blockedPhrasesConfigurationUpdate:(Option.map
                                                    ~f:Values.BlockedPhrasesConfigurationUpdate.of_json
                                                    blockedPhrasesConfigurationUpdate)
              ?topicConfigurationsToCreateOrUpdate:(Option.map
                                                      ~f:Values.TopicConfigurations.of_json
                                                      topicConfigurationsToCreateOrUpdate)
              ?topicConfigurationsToDelete:(Option.map
                                              ~f:Values.TopicConfigurations.of_json
                                              topicConfigurationsToDelete)
              ?creatorModeConfiguration:(Option.map
                                           ~f:Values.CreatorModeConfiguration.of_json
                                           creatorModeConfiguration)
              ?hallucinationReductionConfiguration:(Option.map
                                                      ~f:Values.HallucinationReductionConfiguration.of_json
                                                      hallucinationReductionConfiguration)
              ~applicationId ())
           (Some Values.UpdateChatControlsConfigurationResponse.to_json)
           (Some Values.UpdateChatControlsConfigurationResponse.error_to_json)])
let update_chat_response_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING DisplayName"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and chatResponseConfigurationId =
         flag "chat-response-configuration-id" (required string)
           ~doc:"STRING ChatResponseConfigurationId"
       and responseConfigurations =
         flag "response-configurations" (required json_arg)
           ~doc:"JSON ResponseConfigurations" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_chat_response_configuration
           (Values.UpdateChatResponseConfigurationRequest.make ?displayName
              ?clientToken ~applicationId ~chatResponseConfigurationId
              ~responseConfigurations:(Values.ResponseConfigurations.of_json
                                         responseConfigurations) ())
           (Some Values.UpdateChatResponseConfigurationResponse.to_json)
           (Some Values.UpdateChatResponseConfigurationResponse.error_to_json)])
let update_data_accessor =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationDetail =
         flag "authentication-detail" (optional json_arg)
           ~doc:"JSON DataAccessorAuthenticationDetail"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING DataAccessorName"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and dataAccessorId =
         flag "data-accessor-id" (required string)
           ~doc:"STRING DataAccessorId"
       and actionConfigurations =
         flag "action-configurations" (required json_arg)
           ~doc:"JSON ActionConfigurationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_data_accessor
           (Values.UpdateDataAccessorRequest.make
              ?authenticationDetail:(Option.map
                                       ~f:Values.DataAccessorAuthenticationDetail.of_json
                                       authenticationDetail) ?displayName
              ~applicationId ~dataAccessorId
              ~actionConfigurations:(Values.ActionConfigurationList.of_json
                                       actionConfigurations) ())
           (Some Values.UpdateDataAccessorResponse.to_json)
           (Some Values.UpdateDataAccessorResponse.error_to_json)])
let update_data_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING DataSourceName"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON DataSourceConfiguration"
       and vpcConfiguration =
         flag "vpc-configuration" (optional json_arg)
           ~doc:"JSON DataSourceVpcConfiguration"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and syncSchedule =
         flag "sync-schedule" (optional string) ~doc:"STRING SyncSchedule"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and documentEnrichmentConfiguration =
         flag "document-enrichment-configuration" (optional json_arg)
           ~doc:"JSON DocumentEnrichmentConfiguration"
       and mediaExtractionConfiguration =
         flag "media-extraction-configuration" (optional json_arg)
           ~doc:"JSON MediaExtractionConfiguration"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING DataSourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_data_source
           (Values.UpdateDataSourceRequest.make ?displayName
              ?configuration:(Option.map
                                ~f:Values.DataSourceConfiguration.of_json
                                configuration)
              ?vpcConfiguration:(Option.map
                                   ~f:Values.DataSourceVpcConfiguration.of_json
                                   vpcConfiguration) ?description
              ?syncSchedule ?roleArn
              ?documentEnrichmentConfiguration:(Option.map
                                                  ~f:Values.DocumentEnrichmentConfiguration.of_json
                                                  documentEnrichmentConfiguration)
              ?mediaExtractionConfiguration:(Option.map
                                               ~f:Values.MediaExtractionConfiguration.of_json
                                               mediaExtractionConfiguration)
              ~applicationId ~indexId ~dataSourceId ())
           (Some Values.UpdateDataSourceResponse.to_json)
           (Some Values.UpdateDataSourceResponse.error_to_json)])
let update_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING ApplicationName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and capacityConfiguration =
         flag "capacity-configuration" (optional json_arg)
           ~doc:"JSON IndexCapacityConfiguration"
       and documentAttributeConfigurations =
         flag "document-attribute-configurations" (optional json_arg)
           ~doc:"JSON DocumentAttributeConfigurations"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and indexId = flag "index-id" (required string) ~doc:"STRING IndexId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_index
           (Values.UpdateIndexRequest.make ?displayName ?description
              ?capacityConfiguration:(Option.map
                                        ~f:Values.IndexCapacityConfiguration.of_json
                                        capacityConfiguration)
              ?documentAttributeConfigurations:(Option.map
                                                  ~f:Values.DocumentAttributeConfigurations.of_json
                                                  documentAttributeConfigurations)
              ~applicationId ~indexId ())
           (Some Values.UpdateIndexResponse.to_json)
           (Some Values.UpdateIndexResponse.error_to_json)])
let update_plugin =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING PluginName"
       and state = flag "state" (optional json_arg) ~doc:"JSON PluginState"
       and serverUrl = flag "server-url" (optional string) ~doc:"STRING Url"
       and customPluginConfiguration =
         flag "custom-plugin-configuration" (optional json_arg)
           ~doc:"JSON CustomPluginConfiguration"
       and authConfiguration =
         flag "auth-configuration" (optional json_arg)
           ~doc:"JSON PluginAuthConfiguration"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and pluginId =
         flag "plugin-id" (required string) ~doc:"STRING PluginId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_plugin
           (Values.UpdatePluginRequest.make ?displayName
              ?state:(Option.map ~f:Values.PluginState.of_json state)
              ?serverUrl
              ?customPluginConfiguration:(Option.map
                                            ~f:Values.CustomPluginConfiguration.of_json
                                            customPluginConfiguration)
              ?authConfiguration:(Option.map
                                    ~f:Values.PluginAuthConfiguration.of_json
                                    authConfiguration) ~applicationId
              ~pluginId ()) (Some Values.UpdatePluginResponse.to_json)
           (Some Values.UpdatePluginResponse.error_to_json)])
let update_retriever =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configuration =
         flag "configuration" (optional json_arg)
           ~doc:"JSON RetrieverConfiguration"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING RetrieverName"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and retrieverId =
         flag "retriever-id" (required string) ~doc:"STRING RetrieverId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_retriever
           (Values.UpdateRetrieverRequest.make
              ?configuration:(Option.map
                                ~f:Values.RetrieverConfiguration.of_json
                                configuration) ?displayName ?roleArn
              ~applicationId ~retrieverId ())
           (Some Values.UpdateRetrieverResponse.to_json)
           (Some Values.UpdateRetrieverResponse.error_to_json)])
let update_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and subscriptionId =
         flag "subscription-id" (required string)
           ~doc:"STRING SubscriptionId"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON SubscriptionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_subscription
           (Values.UpdateSubscriptionRequest.make ~applicationId
              ~subscriptionId ~type_:(Values.SubscriptionType.of_json type_)
              ()) (Some Values.UpdateSubscriptionResponse.to_json)
           (Some Values.UpdateSubscriptionResponse.error_to_json)])
let update_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userAliasesToUpdate =
         flag "user-aliases-to-update" (optional json_arg)
           ~doc:"JSON UserAliases"
       and userAliasesToDelete =
         flag "user-aliases-to-delete" (optional json_arg)
           ~doc:"JSON UserAliases"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and userId = flag "user-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user
           (Values.UpdateUserRequest.make
              ?userAliasesToUpdate:(Option.map ~f:Values.UserAliases.of_json
                                      userAliasesToUpdate)
              ?userAliasesToDelete:(Option.map ~f:Values.UserAliases.of_json
                                      userAliasesToDelete) ~applicationId
              ~userId ()) (Some Values.UpdateUserResponse.to_json)
           (Some Values.UpdateUserResponse.error_to_json)])
let update_web_experience =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and authenticationConfiguration =
         flag "authentication-configuration" (optional json_arg)
           ~doc:"JSON WebExperienceAuthConfiguration"
       and title =
         flag "title" (optional string) ~doc:"STRING WebExperienceTitle"
       and subtitle =
         flag "subtitle" (optional string)
           ~doc:"STRING WebExperienceSubtitle"
       and welcomeMessage =
         flag "welcome-message" (optional string)
           ~doc:"STRING WebExperienceWelcomeMessage"
       and samplePromptsControlMode =
         flag "sample-prompts-control-mode" (optional json_arg)
           ~doc:"JSON WebExperienceSamplePromptsControlMode"
       and identityProviderConfiguration =
         flag "identity-provider-configuration" (optional json_arg)
           ~doc:"JSON IdentityProviderConfiguration"
       and origins =
         flag "origins" (optional json_arg) ~doc:"JSON WebExperienceOrigins"
       and browserExtensionConfiguration =
         flag "browser-extension-configuration" (optional json_arg)
           ~doc:"JSON BrowserExtensionConfiguration"
       and customizationConfiguration =
         flag "customization-configuration" (optional json_arg)
           ~doc:"JSON CustomizationConfiguration"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and webExperienceId =
         flag "web-experience-id" (required string)
           ~doc:"STRING WebExperienceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_web_experience
           (Values.UpdateWebExperienceRequest.make ?roleArn
              ?authenticationConfiguration:(Option.map
                                              ~f:Values.WebExperienceAuthConfiguration.of_json
                                              authenticationConfiguration)
              ?title ?subtitle ?welcomeMessage
              ?samplePromptsControlMode:(Option.map
                                           ~f:Values.WebExperienceSamplePromptsControlMode.of_json
                                           samplePromptsControlMode)
              ?identityProviderConfiguration:(Option.map
                                                ~f:Values.IdentityProviderConfiguration.of_json
                                                identityProviderConfiguration)
              ?origins:(Option.map ~f:Values.WebExperienceOrigins.of_json
                          origins)
              ?browserExtensionConfiguration:(Option.map
                                                ~f:Values.BrowserExtensionConfiguration.of_json
                                                browserExtensionConfiguration)
              ?customizationConfiguration:(Option.map
                                             ~f:Values.CustomizationConfiguration.of_json
                                             customizationConfiguration)
              ~applicationId ~webExperienceId ())
           (Some Values.UpdateWebExperienceResponse.to_json)
           (Some Values.UpdateWebExperienceResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-permission", associate_permission);
    ("batch-delete-document", batch_delete_document);
    ("batch-put-document", batch_put_document);
    ("cancel-subscription", cancel_subscription);
    ("chat", chat);
    ("chat-sync", chat_sync);
    ("check-document-access", check_document_access);
    ("create-anonymous-web-experience-url",
      create_anonymous_web_experience_url);
    ("create-application", create_application);
    ("create-chat-response-configuration",
      create_chat_response_configuration);
    ("create-data-accessor", create_data_accessor);
    ("create-data-source", create_data_source);
    ("create-index", create_index);
    ("create-plugin", create_plugin);
    ("create-retriever", create_retriever);
    ("create-subscription", create_subscription);
    ("create-user", create_user);
    ("create-web-experience", create_web_experience);
    ("delete-application", delete_application);
    ("delete-attachment", delete_attachment);
    ("delete-chat-controls-configuration",
      delete_chat_controls_configuration);
    ("delete-chat-response-configuration",
      delete_chat_response_configuration);
    ("delete-conversation", delete_conversation);
    ("delete-data-accessor", delete_data_accessor);
    ("delete-data-source", delete_data_source);
    ("delete-group", delete_group);
    ("delete-index", delete_index);
    ("delete-plugin", delete_plugin);
    ("delete-retriever", delete_retriever);
    ("delete-user", delete_user);
    ("delete-web-experience", delete_web_experience);
    ("disassociate-permission", disassociate_permission);
    ("get-application", get_application);
    ("get-chat-controls-configuration", get_chat_controls_configuration);
    ("get-chat-response-configuration", get_chat_response_configuration);
    ("get-data-accessor", get_data_accessor);
    ("get-data-source", get_data_source);
    ("get-document-content", get_document_content);
    ("get-group", get_group);
    ("get-index", get_index);
    ("get-media", get_media);
    ("get-plugin", get_plugin);
    ("get-policy", get_policy);
    ("get-retriever", get_retriever);
    ("get-user", get_user);
    ("get-web-experience", get_web_experience);
    ("list-applications", list_applications);
    ("list-attachments", list_attachments);
    ("list-chat-response-configurations", list_chat_response_configurations);
    ("list-conversations", list_conversations);
    ("list-data-accessors", list_data_accessors);
    ("list-data-source-sync-jobs", list_data_source_sync_jobs);
    ("list-data-sources", list_data_sources);
    ("list-documents", list_documents);
    ("list-groups", list_groups);
    ("list-indices", list_indices);
    ("list-messages", list_messages);
    ("list-plugin-actions", list_plugin_actions);
    ("list-plugin-type-actions", list_plugin_type_actions);
    ("list-plugin-type-metadata", list_plugin_type_metadata);
    ("list-plugins", list_plugins);
    ("list-retrievers", list_retrievers);
    ("list-subscriptions", list_subscriptions);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-web-experiences", list_web_experiences);
    ("put-feedback", put_feedback);
    ("put-group", put_group);
    ("search-relevant-content", search_relevant_content);
    ("start-data-source-sync-job", start_data_source_sync_job);
    ("stop-data-source-sync-job", stop_data_source_sync_job);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-application", update_application);
    ("update-chat-controls-configuration",
      update_chat_controls_configuration);
    ("update-chat-response-configuration",
      update_chat_response_configuration);
    ("update-data-accessor", update_data_accessor);
    ("update-data-source", update_data_source);
    ("update-index", update_index);
    ("update-plugin", update_plugin);
    ("update-retriever", update_retriever);
    ("update-subscription", update_subscription);
    ("update-user", update_user);
    ("update-web-experience", update_web_experience)]
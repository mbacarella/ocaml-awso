(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let apply_archive_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and analyzerArn =
         flag "analyzer-arn" (required string) ~doc:"STRING AnalyzerArn"
       and ruleName = flag "rule-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.apply_archive_rule
           (Values.ApplyArchiveRuleRequest.make ?clientToken ~analyzerArn
              ~ruleName ()) None None])
let cancel_policy_generation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_policy_generation
           (Values.CancelPolicyGenerationRequest.make ~jobId ())
           (Some Values.CancelPolicyGenerationResponse.to_json)
           (Some Values.CancelPolicyGenerationResponse.error_to_json)])
let create_access_preview =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and analyzerArn =
         flag "analyzer-arn" (required string) ~doc:"STRING AnalyzerArn"
       and configurations =
         flag "configurations" (required json_arg)
           ~doc:"JSON ConfigurationsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_access_preview
           (Values.CreateAccessPreviewRequest.make ?clientToken ~analyzerArn
              ~configurations:(Values.ConfigurationsMap.of_json
                                 configurations) ())
           (Some Values.CreateAccessPreviewResponse.to_json)
           (Some Values.CreateAccessPreviewResponse.error_to_json)])
let create_analyzer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and archiveRules =
         flag "archive-rules" (optional json_arg)
           ~doc:"JSON InlineArchiveRulesList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and analyzerName =
         flag "analyzer-name" (required string) ~doc:"STRING Name"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON Type" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_analyzer
           (Values.CreateAnalyzerRequest.make
              ?archiveRules:(Option.map
                               ~f:Values.InlineArchiveRulesList.of_json
                               archiveRules) ?clientToken
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~analyzerName
              ~type_:(Values.Type.of_json type_) ())
           (Some Values.CreateAnalyzerResponse.to_json)
           (Some Values.CreateAnalyzerResponse.error_to_json)])
let create_archive_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and analyzerName =
         flag "analyzer-name" (required string) ~doc:"STRING Name"
       and filter =
         flag "filter" (required json_arg) ~doc:"JSON FilterCriteriaMap"
       and ruleName = flag "rule-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_archive_rule
           (Values.CreateArchiveRuleRequest.make ?clientToken ~analyzerName
              ~filter:(Values.FilterCriteriaMap.of_json filter) ~ruleName ())
           None None])
let delete_analyzer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and analyzerName =
         flag "analyzer-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_analyzer
           (Values.DeleteAnalyzerRequest.make ?clientToken ~analyzerName ())
           None None])
let delete_archive_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and analyzerName =
         flag "analyzer-name" (required string) ~doc:"STRING Name"
       and ruleName = flag "rule-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_archive_rule
           (Values.DeleteArchiveRuleRequest.make ?clientToken ~analyzerName
              ~ruleName ()) None None])
let get_access_preview =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accessPreviewId =
         flag "access-preview-id" (required string)
           ~doc:"STRING AccessPreviewId"
       and analyzerArn =
         flag "analyzer-arn" (required string) ~doc:"STRING AnalyzerArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_access_preview
           (Values.GetAccessPreviewRequest.make ~accessPreviewId ~analyzerArn
              ()) (Some Values.GetAccessPreviewResponse.to_json)
           (Some Values.GetAccessPreviewResponse.error_to_json)])
let get_analyzed_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and analyzerArn =
         flag "analyzer-arn" (required string) ~doc:"STRING AnalyzerArn"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_analyzed_resource
           (Values.GetAnalyzedResourceRequest.make ~analyzerArn ~resourceArn
              ()) (Some Values.GetAnalyzedResourceResponse.to_json)
           (Some Values.GetAnalyzedResourceResponse.error_to_json)])
let get_analyzer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and analyzerName =
         flag "analyzer-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_analyzer (Values.GetAnalyzerRequest.make ~analyzerName ())
           (Some Values.GetAnalyzerResponse.to_json)
           (Some Values.GetAnalyzerResponse.error_to_json)])
let get_archive_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and analyzerName =
         flag "analyzer-name" (required string) ~doc:"STRING Name"
       and ruleName = flag "rule-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_archive_rule
           (Values.GetArchiveRuleRequest.make ~analyzerName ~ruleName ())
           (Some Values.GetArchiveRuleResponse.to_json)
           (Some Values.GetArchiveRuleResponse.error_to_json)])
let get_finding =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and analyzerArn =
         flag "analyzer-arn" (required string) ~doc:"STRING AnalyzerArn"
       and id = flag "id" (required string) ~doc:"STRING FindingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_finding (Values.GetFindingRequest.make ~analyzerArn ~id ())
           (Some Values.GetFindingResponse.to_json)
           (Some Values.GetFindingResponse.error_to_json)])
let get_generated_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeResourcePlaceholders =
         flag "include-resource-placeholders" (optional bool)
           ~doc:"BOOL Boolean"
       and includeServiceLevelTemplate =
         flag "include-service-level-template" (optional bool)
           ~doc:"BOOL Boolean"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_generated_policy
           (Values.GetGeneratedPolicyRequest.make
              ?includeResourcePlaceholders ?includeServiceLevelTemplate
              ~jobId ()) (Some Values.GetGeneratedPolicyResponse.to_json)
           (Some Values.GetGeneratedPolicyResponse.error_to_json)])
let list_access_preview_findings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON FilterCriteriaMap"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and accessPreviewId =
         flag "access-preview-id" (required string)
           ~doc:"STRING AccessPreviewId"
       and analyzerArn =
         flag "analyzer-arn" (required string) ~doc:"STRING AnalyzerArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_access_preview_findings
           (Values.ListAccessPreviewFindingsRequest.make
              ?filter:(Option.map ~f:Values.FilterCriteriaMap.of_json filter)
              ?maxResults ?nextToken ~accessPreviewId ~analyzerArn ())
           (Some Values.ListAccessPreviewFindingsResponse.to_json)
           (Some Values.ListAccessPreviewFindingsResponse.error_to_json)])
let list_access_previews =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and analyzerArn =
         flag "analyzer-arn" (required string) ~doc:"STRING AnalyzerArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_access_previews
           (Values.ListAccessPreviewsRequest.make ?maxResults ?nextToken
              ~analyzerArn ())
           (Some Values.ListAccessPreviewsResponse.to_json)
           (Some Values.ListAccessPreviewsResponse.error_to_json)])
let list_analyzed_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and resourceType =
         flag "resource-type" (optional json_arg) ~doc:"JSON ResourceType"
       and analyzerArn =
         flag "analyzer-arn" (required string) ~doc:"STRING AnalyzerArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_analyzed_resources
           (Values.ListAnalyzedResourcesRequest.make ?maxResults ?nextToken
              ?resourceType:(Option.map ~f:Values.ResourceType.of_json
                               resourceType) ~analyzerArn ())
           (Some Values.ListAnalyzedResourcesResponse.to_json)
           (Some Values.ListAnalyzedResourcesResponse.error_to_json)])
let list_analyzers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON Type" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_analyzers
           (Values.ListAnalyzersRequest.make ?maxResults ?nextToken
              ?type_:(Option.map ~f:Values.Type.of_json type_) ())
           (Some Values.ListAnalyzersResponse.to_json)
           (Some Values.ListAnalyzersResponse.error_to_json)])
let list_archive_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and analyzerName =
         flag "analyzer-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_archive_rules
           (Values.ListArchiveRulesRequest.make ?maxResults ?nextToken
              ~analyzerName ())
           (Some Values.ListArchiveRulesResponse.to_json)
           (Some Values.ListArchiveRulesResponse.error_to_json)])
let list_findings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON FilterCriteriaMap"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and sort = flag "sort" (optional json_arg) ~doc:"JSON SortCriteria"
       and analyzerArn =
         flag "analyzer-arn" (required string) ~doc:"STRING AnalyzerArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_findings
           (Values.ListFindingsRequest.make
              ?filter:(Option.map ~f:Values.FilterCriteriaMap.of_json filter)
              ?maxResults ?nextToken
              ?sort:(Option.map ~f:Values.SortCriteria.of_json sort)
              ~analyzerArn ()) (Some Values.ListFindingsResponse.to_json)
           (Some Values.ListFindingsResponse.error_to_json)])
let list_policy_generations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListPolicyGenerationsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and principalArn =
         flag "principal-arn" (optional string) ~doc:"STRING PrincipalArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_policy_generations
           (Values.ListPolicyGenerationsRequest.make ?maxResults ?nextToken
              ?principalArn ())
           (Some Values.ListPolicyGenerationsResponse.to_json)
           (Some Values.ListPolicyGenerationsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let start_policy_generation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and cloudTrailDetails =
         flag "cloud-trail-details" (optional json_arg)
           ~doc:"JSON CloudTrailDetails"
       and policyGenerationDetails =
         flag "policy-generation-details" (required json_arg)
           ~doc:"JSON PolicyGenerationDetails" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_policy_generation
           (Values.StartPolicyGenerationRequest.make ?clientToken
              ?cloudTrailDetails:(Option.map
                                    ~f:Values.CloudTrailDetails.of_json
                                    cloudTrailDetails)
              ~policyGenerationDetails:(Values.PolicyGenerationDetails.of_json
                                          policyGenerationDetails) ())
           (Some Values.StartPolicyGenerationResponse.to_json)
           (Some Values.StartPolicyGenerationResponse.error_to_json)])
let start_resource_scan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and analyzerArn =
         flag "analyzer-arn" (required string) ~doc:"STRING AnalyzerArn"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_resource_scan
           (Values.StartResourceScanRequest.make ~analyzerArn ~resourceArn ())
           None None])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagsMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_archive_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and analyzerName =
         flag "analyzer-name" (required string) ~doc:"STRING Name"
       and filter =
         flag "filter" (required json_arg) ~doc:"JSON FilterCriteriaMap"
       and ruleName = flag "rule-name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_archive_rule
           (Values.UpdateArchiveRuleRequest.make ?clientToken ~analyzerName
              ~filter:(Values.FilterCriteriaMap.of_json filter) ~ruleName ())
           None None])
let update_findings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and ids = flag "ids" (optional json_arg) ~doc:"JSON FindingIdList"
       and resourceArn =
         flag "resource-arn" (optional string) ~doc:"STRING ResourceArn"
       and analyzerArn =
         flag "analyzer-arn" (required string) ~doc:"STRING AnalyzerArn"
       and status =
         flag "status" (required json_arg) ~doc:"JSON FindingStatusUpdate" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_findings
           (Values.UpdateFindingsRequest.make ?clientToken
              ?ids:(Option.map ~f:Values.FindingIdList.of_json ids)
              ?resourceArn ~analyzerArn
              ~status:(Values.FindingStatusUpdate.of_json status) ()) None
           None])
let validate_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and locale = flag "locale" (optional json_arg) ~doc:"JSON Locale"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and validatePolicyResourceType =
         flag "validate-policy-resource-type" (optional json_arg)
           ~doc:"JSON ValidatePolicyResourceType"
       and policyDocument =
         flag "policy-document" (required string)
           ~doc:"STRING PolicyDocument"
       and policyType =
         flag "policy-type" (required json_arg) ~doc:"JSON PolicyType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.validate_policy
           (Values.ValidatePolicyRequest.make
              ?locale:(Option.map ~f:Values.Locale.of_json locale)
              ?maxResults ?nextToken
              ?validatePolicyResourceType:(Option.map
                                             ~f:Values.ValidatePolicyResourceType.of_json
                                             validatePolicyResourceType)
              ~policyDocument
              ~policyType:(Values.PolicyType.of_json policyType) ())
           (Some Values.ValidatePolicyResponse.to_json)
           (Some Values.ValidatePolicyResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("apply-archive-rule", apply_archive_rule);
    ("cancel-policy-generation", cancel_policy_generation);
    ("create-access-preview", create_access_preview);
    ("create-analyzer", create_analyzer);
    ("create-archive-rule", create_archive_rule);
    ("delete-analyzer", delete_analyzer);
    ("delete-archive-rule", delete_archive_rule);
    ("get-access-preview", get_access_preview);
    ("get-analyzed-resource", get_analyzed_resource);
    ("get-analyzer", get_analyzer);
    ("get-archive-rule", get_archive_rule);
    ("get-finding", get_finding);
    ("get-generated-policy", get_generated_policy);
    ("list-access-preview-findings", list_access_preview_findings);
    ("list-access-previews", list_access_previews);
    ("list-analyzed-resources", list_analyzed_resources);
    ("list-analyzers", list_analyzers);
    ("list-archive-rules", list_archive_rules);
    ("list-findings", list_findings);
    ("list-policy-generations", list_policy_generations);
    ("list-tags-for-resource", list_tags_for_resource);
    ("start-policy-generation", start_policy_generation);
    ("start-resource-scan", start_resource_scan);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-archive-rule", update_archive_rule);
    ("update-findings", update_findings);
    ("validate-policy", validate_policy)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | BatchGetTokenBalance: (BatchGetTokenBalanceInput.t,
  BatchGetTokenBalanceOutput.t, BatchGetTokenBalanceOutput.error) t 
  | GetAssetContract: (GetAssetContractInput.t, GetAssetContractOutput.t,
  GetAssetContractOutput.error) t 
  | GetTokenBalance: (GetTokenBalanceInput.t, GetTokenBalanceOutput.t,
  GetTokenBalanceOutput.error) t 
  | GetTransaction: (GetTransactionInput.t, GetTransactionOutput.t,
  GetTransactionOutput.error) t 
  | ListAssetContracts: (ListAssetContractsInput.t,
  ListAssetContractsOutput.t, ListAssetContractsOutput.error) t 
  | ListFilteredTransactionEvents: (ListFilteredTransactionEventsInput.t,
  ListFilteredTransactionEventsOutput.t,
  ListFilteredTransactionEventsOutput.error) t 
  | ListTokenBalances: (ListTokenBalancesInput.t, ListTokenBalancesOutput.t,
  ListTokenBalancesOutput.error) t 
  | ListTransactionEvents: (ListTransactionEventsInput.t,
  ListTransactionEventsOutput.t, ListTransactionEventsOutput.error) t 
  | ListTransactions: (ListTransactionsInput.t, ListTransactionsOutput.t,
  ListTransactionsOutput.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | BatchGetTokenBalance -> `POST
  | GetAssetContract -> `POST
  | GetTokenBalance -> `POST
  | GetTransaction -> `POST
  | ListAssetContracts -> `POST
  | ListFilteredTransactionEvents -> `POST
  | ListTokenBalances -> `POST
  | ListTransactionEvents -> `POST
  | ListTransactions -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | BatchGetTokenBalance ->
          (Format.kasprintf Uri.of_string) "/batch-get-token-balance"
      | GetAssetContract ->
          (Format.kasprintf Uri.of_string) "/get-asset-contract"
      | GetTokenBalance ->
          (Format.kasprintf Uri.of_string) "/get-token-balance"
      | GetTransaction -> (Format.kasprintf Uri.of_string) "/get-transaction"
      | ListAssetContracts ->
          (Format.kasprintf Uri.of_string) "/list-asset-contracts"
      | ListFilteredTransactionEvents ->
          (Format.kasprintf Uri.of_string)
            "/list-filtered-transaction-events"
      | ListTokenBalances ->
          (Format.kasprintf Uri.of_string) "/list-token-balances"
      | ListTransactionEvents ->
          (Format.kasprintf Uri.of_string) "/list-transaction-events"
      | ListTransactions ->
          (Format.kasprintf Uri.of_string) "/list-transactions")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | BatchGetTokenBalance ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.BatchGetTokenBalanceInput.getTokenBalanceInputs
                         ~f:(fun x ->
                               ("getTokenBalanceInputs",
                                 (GetTokenBalanceInputList.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetAssetContract ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("contractIdentifier",
                           (ContractIdentifier.to_value
                              req.GetAssetContractInput.contractIdentifier))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetTokenBalance ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("tokenIdentifier",
                           (TokenIdentifier.to_value
                              req.GetTokenBalanceInput.tokenIdentifier));
                      Some
                        ("ownerIdentifier",
                          (OwnerIdentifier.to_value
                             req.GetTokenBalanceInput.ownerIdentifier));
                      Option.map req.GetTokenBalanceInput.atBlockchainInstant
                        ~f:(fun x ->
                              ("atBlockchainInstant",
                                (BlockchainInstant.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetTransaction ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.GetTransactionInput.transactionHash
                         ~f:(fun x ->
                               ("transactionHash",
                                 (QueryTransactionHash.to_value x)));
                      Option.map req.GetTransactionInput.transactionId
                        ~f:(fun x ->
                              ("transactionId",
                                (QueryTransactionId.to_value x)));
                      Some
                        ("network",
                          (QueryNetwork.to_value
                             req.GetTransactionInput.network))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListAssetContracts ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("contractFilter",
                           (ContractFilter.to_value
                              req.ListAssetContractsInput.contractFilter));
                      Option.map req.ListAssetContractsInput.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)));
                      Option.map req.ListAssetContractsInput.maxResults
                        ~f:(fun x ->
                              ("maxResults",
                                (ListAssetContractsInputMaxResultsInteger.to_value
                                   x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListFilteredTransactionEvents ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("network",
                           (String_.to_value
                              req.ListFilteredTransactionEventsInput.network));
                      Some
                        ("addressIdentifierFilter",
                          (AddressIdentifierFilter.to_value
                             req.ListFilteredTransactionEventsInput.addressIdentifierFilter));
                      Option.map
                        req.ListFilteredTransactionEventsInput.timeFilter
                        ~f:(fun x -> ("timeFilter", (TimeFilter.to_value x)));
                      Option.map
                        req.ListFilteredTransactionEventsInput.voutFilter
                        ~f:(fun x -> ("voutFilter", (VoutFilter.to_value x)));
                      Option.map
                        req.ListFilteredTransactionEventsInput.confirmationStatusFilter
                        ~f:(fun x ->
                              ("confirmationStatusFilter",
                                (ConfirmationStatusFilter.to_value x)));
                      Option.map req.ListFilteredTransactionEventsInput.sort
                        ~f:(fun x ->
                              ("sort",
                                (ListFilteredTransactionEventsSort.to_value x)));
                      Option.map
                        req.ListFilteredTransactionEventsInput.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)));
                      Option.map
                        req.ListFilteredTransactionEventsInput.maxResults
                        ~f:(fun x ->
                              ("maxResults",
                                (ListFilteredTransactionEventsInputMaxResultsInteger.to_value
                                   x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTokenBalances ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListTokenBalancesInput.ownerFilter
                         ~f:(fun x ->
                               ("ownerFilter", (OwnerFilter.to_value x)));
                      Some
                        ("tokenFilter",
                          (TokenFilter.to_value
                             req.ListTokenBalancesInput.tokenFilter));
                      Option.map req.ListTokenBalancesInput.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)));
                      Option.map req.ListTokenBalancesInput.maxResults
                        ~f:(fun x ->
                              ("maxResults",
                                (ListTokenBalancesInputMaxResultsInteger.to_value
                                   x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTransactionEvents ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.ListTransactionEventsInput.transactionHash
                         ~f:(fun x ->
                               ("transactionHash",
                                 (QueryTransactionHash.to_value x)));
                      Option.map req.ListTransactionEventsInput.transactionId
                        ~f:(fun x ->
                              ("transactionId",
                                (QueryTransactionId.to_value x)));
                      Some
                        ("network",
                          (QueryNetwork.to_value
                             req.ListTransactionEventsInput.network));
                      Option.map req.ListTransactionEventsInput.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)));
                      Option.map req.ListTransactionEventsInput.maxResults
                        ~f:(fun x ->
                              ("maxResults",
                                (ListTransactionEventsInputMaxResultsInteger.to_value
                                   x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTransactions ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("address",
                           (ChainAddress.to_value
                              req.ListTransactionsInput.address));
                      Some
                        ("network",
                          (QueryNetwork.to_value
                             req.ListTransactionsInput.network));
                      Option.map
                        req.ListTransactionsInput.fromBlockchainInstant
                        ~f:(fun x ->
                              ("fromBlockchainInstant",
                                (BlockchainInstant.to_value x)));
                      Option.map
                        req.ListTransactionsInput.toBlockchainInstant
                        ~f:(fun x ->
                              ("toBlockchainInstant",
                                (BlockchainInstant.to_value x)));
                      Option.map req.ListTransactionsInput.sort
                        ~f:(fun x ->
                              ("sort", (ListTransactionsSort.to_value x)));
                      Option.map req.ListTransactionsInput.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)));
                      Option.map req.ListTransactionsInput.maxResults
                        ~f:(fun x ->
                              ("maxResults",
                                (ListTransactionsInputMaxResultsInteger.to_value
                                   x)));
                      Option.map
                        req.ListTransactionsInput.confirmationStatusFilter
                        ~f:(fun x ->
                              ("confirmationStatusFilter",
                                (ConfirmationStatusFilter.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | BatchGetTokenBalance ->
      if is_success
      then Ok (BatchGetTokenBalanceOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some BatchGetTokenBalanceOutput.error_of_json))
  | GetAssetContract ->
      if is_success
      then Ok (GetAssetContractOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some GetAssetContractOutput.error_of_json))
  | GetTokenBalance ->
      if is_success
      then Ok (GetTokenBalanceOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetTokenBalanceOutput.error_of_json))
  | GetTransaction ->
      if is_success
      then Ok (GetTransactionOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetTransactionOutput.error_of_json))
  | ListAssetContracts ->
      if is_success
      then Ok (ListAssetContractsOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some ListAssetContractsOutput.error_of_json))
  | ListFilteredTransactionEvents ->
      if is_success
      then
        Ok
          (ListFilteredTransactionEventsOutput.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some ListFilteredTransactionEventsOutput.error_of_json))
  | ListTokenBalances ->
      if is_success
      then Ok (ListTokenBalancesOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some ListTokenBalancesOutput.error_of_json))
  | ListTransactionEvents ->
      if is_success
      then Ok (ListTransactionEventsOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListTransactionEventsOutput.error_of_json))
  | ListTransactions ->
      if is_success
      then Ok (ListTransactionsOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some ListTransactionsOutput.error_of_json))
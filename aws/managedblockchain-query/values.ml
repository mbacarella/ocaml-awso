(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.managedblockchain_query
let apiVersion = "2023-05-04"
let endpointPrefix = "managedblockchain-query"
let serviceFullName = "Amazon Managed Blockchain Query"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module ChainAddress =
  struct
    type nonrec t = string
    let context_ = "ChainAddress"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[-A-Za-z0-9]{13,74}"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ChainAddress" j
    let to_json = simple_to_json to_value
  end
module QueryNetwork =
  struct
    type nonrec t =
      | ETHEREUM_MAINNET 
      | ETHEREUM_SEPOLIA_TESTNET 
      | BITCOIN_MAINNET 
      | BITCOIN_TESTNET 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ETHEREUM_MAINNET -> "ETHEREUM_MAINNET"
      | ETHEREUM_SEPOLIA_TESTNET -> "ETHEREUM_SEPOLIA_TESTNET"
      | BITCOIN_MAINNET -> "BITCOIN_MAINNET"
      | BITCOIN_TESTNET -> "BITCOIN_TESTNET"
      | Non_static_id s -> s
    let of_string =
      function
      | "ETHEREUM_MAINNET" -> ETHEREUM_MAINNET
      | "ETHEREUM_SEPOLIA_TESTNET" -> ETHEREUM_SEPOLIA_TESTNET
      | "BITCOIN_MAINNET" -> BITCOIN_MAINNET
      | "BITCOIN_TESTNET" -> BITCOIN_TESTNET
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration QueryNetwork" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"QueryNetwork" j)
    let to_json = simple_to_json to_value
  end
module QueryTokenId =
  struct
    type nonrec t = string
    let context_ = "QueryTokenId"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[a-zA-Z0-9]{1,66}"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"QueryTokenId" j
    let to_json = simple_to_json to_value
  end
module ConfirmationStatus =
  struct
    type nonrec t =
      | FINAL 
      | NONFINAL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | FINAL -> "FINAL"
      | NONFINAL -> "NONFINAL"
      | Non_static_id s -> s
    let of_string =
      function
      | "FINAL" -> FINAL
      | "NONFINAL" -> NONFINAL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ConfirmationStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ConfirmationStatus" j)
    let to_json = simple_to_json to_value
  end
module QueryTransactionHash =
  struct
    type nonrec t = string
    let context_ = "QueryTransactionHash"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"(0x[A-Fa-f0-9]{64}|[A-Fa-f0-9]{64})");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"QueryTransactionHash" j
    let to_json = simple_to_json to_value
  end
module QueryTransactionId =
  struct
    type nonrec t = string
    let context_ = "QueryTransactionId"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"(0x[A-Fa-f0-9]{64}|[A-Fa-f0-9]{64})");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"QueryTransactionId" j
    let to_json = simple_to_json to_value
  end
module ValidationExceptionField =
  struct
    type nonrec t =
      {
      name: String_.t option
        [@ocaml.doc
          "The name of the field that triggered the ValidationException."];
      message: String_.t option
        [@ocaml.doc "The ValidationException message."]}
    let make ?name = fun ?message -> fun () -> { name; message }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:String_.to_value));
        ("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      make ?message ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      let name = field_map json__ "name" String_.of_json in
      make ?message ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The resource passed is invalid."]
module BlockchainInstant =
  struct
    type nonrec t =
      {
      time: Timestamp.t option
        [@ocaml.doc
          "The container of the Timestamp of the blockchain instant. This timestamp will only be recorded up to the second."]}
    let make ?time = fun () -> { time }
    let to_value x =
      structure_to_value
        [("time", (Option.map x.time ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let time = (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "time") in
      make ?time ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let time = field_map json__ "time" Timestamp.of_json in make ?time ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The container for time."]
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module QueryTransactionEventType =
  struct
    type nonrec t =
      | ERC20_TRANSFER 
      | ERC20_MINT 
      | ERC20_BURN 
      | ERC20_DEPOSIT 
      | ERC20_WITHDRAWAL 
      | ERC721_TRANSFER 
      | ERC1155_TRANSFER 
      | BITCOIN_VIN 
      | BITCOIN_VOUT 
      | INTERNAL_ETH_TRANSFER 
      | ETH_TRANSFER 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ERC20_TRANSFER -> "ERC20_TRANSFER"
      | ERC20_MINT -> "ERC20_MINT"
      | ERC20_BURN -> "ERC20_BURN"
      | ERC20_DEPOSIT -> "ERC20_DEPOSIT"
      | ERC20_WITHDRAWAL -> "ERC20_WITHDRAWAL"
      | ERC721_TRANSFER -> "ERC721_TRANSFER"
      | ERC1155_TRANSFER -> "ERC1155_TRANSFER"
      | BITCOIN_VIN -> "BITCOIN_VIN"
      | BITCOIN_VOUT -> "BITCOIN_VOUT"
      | INTERNAL_ETH_TRANSFER -> "INTERNAL_ETH_TRANSFER"
      | ETH_TRANSFER -> "ETH_TRANSFER"
      | Non_static_id s -> s
    let of_string =
      function
      | "ERC20_TRANSFER" -> ERC20_TRANSFER
      | "ERC20_MINT" -> ERC20_MINT
      | "ERC20_BURN" -> ERC20_BURN
      | "ERC20_DEPOSIT" -> ERC20_DEPOSIT
      | "ERC20_WITHDRAWAL" -> ERC20_WITHDRAWAL
      | "ERC721_TRANSFER" -> ERC721_TRANSFER
      | "ERC1155_TRANSFER" -> ERC1155_TRANSFER
      | "BITCOIN_VIN" -> BITCOIN_VIN
      | "BITCOIN_VOUT" -> BITCOIN_VOUT
      | "INTERNAL_ETH_TRANSFER" -> INTERNAL_ETH_TRANSFER
      | "ETH_TRANSFER" -> ETH_TRANSFER
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration QueryTransactionEventType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"QueryTransactionEventType" j)
    let to_json = simple_to_json to_value
  end
module OwnerIdentifier =
  struct
    type nonrec t =
      {
      address: ChainAddress.t
        [@ocaml.doc "The contract or wallet address for the owner."]}
    let context_ = "OwnerIdentifier"
    let make ~address = fun () -> { address }
    let to_value x =
      structure_to_value
        [("address", (Some (ChainAddress.to_value x.address)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let address =
        ChainAddress.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "address") in
      make ~address ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let address = field_map_exn json__ "address" ChainAddress.of_json in
      make ~address ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The container for the owner identifier."]
module TokenIdentifier =
  struct
    type nonrec t =
      {
      network: QueryNetwork.t
        [@ocaml.doc "The blockchain network of the token."];
      contractAddress: ChainAddress.t option
        [@ocaml.doc "This is the token's contract address."];
      tokenId: QueryTokenId.t option
        [@ocaml.doc
          "The unique identifier of the token. For native tokens, use the 3 character abbreviation that best matches your token. For example, btc for Bitcoin, eth for Ether, etc. For all other token types you must specify the tokenId in the 64 character hexadecimal tokenid format."]}
    let context_ = "TokenIdentifier"
    let make ?contractAddress =
      fun ?tokenId ->
        fun ~network -> fun () -> { contractAddress; tokenId; network }
    let to_value x =
      structure_to_value
        [("network", (Some (QueryNetwork.to_value x.network)));
        ("contractAddress",
          (Option.map x.contractAddress ~f:ChainAddress.to_value));
        ("tokenId", (Option.map x.tokenId ~f:QueryTokenId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tokenId =
        (Option.map ~f:QueryTokenId.of_xml) (Xml.child xml_arg0 "tokenId") in
      let contractAddress =
        (Option.map ~f:ChainAddress.of_xml)
          (Xml.child xml_arg0 "contractAddress") in
      let network =
        QueryNetwork.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "network") in
      make ?tokenId ?contractAddress ~network ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tokenId = field_map json__ "tokenId" QueryTokenId.of_json in
      let contractAddress =
        field_map json__ "contractAddress" ChainAddress.of_json in
      let network = field_map_exn json__ "network" QueryNetwork.of_json in
      make ?tokenId ?contractAddress ~network ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The container for the identifier for the token including the unique token ID and its blockchain network. Only the native tokens BTC and ETH, and the ERC-20, ERC-721, and ERC 1155 token standards are supported."]
module ContractIdentifier =
  struct
    type nonrec t =
      {
      network: QueryNetwork.t
        [@ocaml.doc "The blockchain network of the contract."];
      contractAddress: ChainAddress.t
        [@ocaml.doc "Container for the blockchain address about a contract."]}
    let context_ = "ContractIdentifier"
    let make ~network =
      fun ~contractAddress -> fun () -> { network; contractAddress }
    let to_value x =
      structure_to_value
        [("network", (Some (QueryNetwork.to_value x.network)));
        ("contractAddress", (Some (ChainAddress.to_value x.contractAddress)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let contractAddress =
        ChainAddress.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "contractAddress") in
      let network =
        QueryNetwork.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "network") in
      make ~contractAddress ~network ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let contractAddress =
        field_map_exn json__ "contractAddress" ChainAddress.of_json in
      let network = field_map_exn json__ "network" QueryNetwork.of_json in
      make ~contractAddress ~network ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Container for the blockchain address and network information about a contract."]
module QueryTokenStandard =
  struct
    type nonrec t =
      | ERC20 
      | ERC721 
      | ERC1155 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ERC20 -> "ERC20"
      | ERC721 -> "ERC721"
      | ERC1155 -> "ERC1155"
      | Non_static_id s -> s
    let of_string =
      function
      | "ERC20" -> ERC20
      | "ERC721" -> ERC721
      | "ERC1155" -> ERC1155
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration QueryTokenStandard" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"QueryTokenStandard" j)
    let to_json = simple_to_json to_value
  end
module ErrorType =
  struct
    type nonrec t =
      | VALIDATION_EXCEPTION 
      | RESOURCE_NOT_FOUND_EXCEPTION 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | VALIDATION_EXCEPTION -> "VALIDATION_EXCEPTION"
      | RESOURCE_NOT_FOUND_EXCEPTION -> "RESOURCE_NOT_FOUND_EXCEPTION"
      | Non_static_id s -> s
    let of_string =
      function
      | "VALIDATION_EXCEPTION" -> VALIDATION_EXCEPTION
      | "RESOURCE_NOT_FOUND_EXCEPTION" -> RESOURCE_NOT_FOUND_EXCEPTION
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ErrorType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ErrorType" j)
    let to_json = simple_to_json to_value
  end
module ExceptionMessage =
  struct
    type nonrec t = string
    let context_ = "ExceptionMessage"
    let make i =
      let open Result in ok_or_failwith (check_string_min i ~min:1); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExceptionMessage" j
    let to_json = simple_to_json to_value
  end
module QuotaCode =
  struct
    type nonrec t = string
    let context_ = "QuotaCode"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"QuotaCode" j
    let to_json = simple_to_json to_value
  end
module ResourceId =
  struct
    type nonrec t = string
    let context_ = "ResourceId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceId" j
    let to_json = simple_to_json to_value
  end
module ResourceType =
  struct
    type nonrec t =
      | Collection 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | Collection -> "collection" | Non_static_id s -> s
    let of_string =
      function | "collection" -> Collection | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ResourceType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ResourceType" j)
    let to_json = simple_to_json to_value
  end
module ServiceCode =
  struct
    type nonrec t = string
    let context_ = "ServiceCode"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ServiceCode" j
    let to_json = simple_to_json to_value
  end
module TransactionOutputItem =
  struct
    type nonrec t =
      {
      transactionHash: QueryTransactionHash.t option
        [@ocaml.doc
          "The hash of a transaction. It is generated when a transaction is created."];
      transactionId: QueryTransactionId.t option
        [@ocaml.doc
          "The identifier of a Bitcoin transaction. It is generated when a transaction is created."];
      network: QueryNetwork.t option
        [@ocaml.doc "The blockchain network where the transaction occurred."];
      transactionTimestamp: Timestamp.t option
        [@ocaml.doc "The time when the transaction occurred."];
      confirmationStatus: ConfirmationStatus.t option
        [@ocaml.doc
          "Specifies whether to list transactions that have not reached Finality."]}
    let make ?transactionHash =
      fun ?transactionId ->
        fun ?network ->
          fun ?transactionTimestamp ->
            fun ?confirmationStatus ->
              fun () ->
                {
                  transactionHash;
                  transactionId;
                  network;
                  transactionTimestamp;
                  confirmationStatus
                }
    let to_value x =
      structure_to_value
        [("transactionHash",
           (Option.map x.transactionHash ~f:QueryTransactionHash.to_value));
        ("transactionId",
          (Option.map x.transactionId ~f:QueryTransactionId.to_value));
        ("network", (Option.map x.network ~f:QueryNetwork.to_value));
        ("transactionTimestamp",
          (Option.map x.transactionTimestamp ~f:Timestamp.to_value));
        ("confirmationStatus",
          (Option.map x.confirmationStatus ~f:ConfirmationStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let confirmationStatus =
        (Option.map ~f:ConfirmationStatus.of_xml)
          (Xml.child xml_arg0 "confirmationStatus") in
      let transactionTimestamp =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "transactionTimestamp") in
      let network =
        (Option.map ~f:QueryNetwork.of_xml) (Xml.child xml_arg0 "network") in
      let transactionId =
        (Option.map ~f:QueryTransactionId.of_xml)
          (Xml.child xml_arg0 "transactionId") in
      let transactionHash =
        (Option.map ~f:QueryTransactionHash.of_xml)
          (Xml.child xml_arg0 "transactionHash") in
      make ?confirmationStatus ?transactionTimestamp ?network ?transactionId
        ?transactionHash ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let confirmationStatus =
        field_map json__ "confirmationStatus" ConfirmationStatus.of_json in
      let transactionTimestamp =
        field_map json__ "transactionTimestamp" Timestamp.of_json in
      let network = field_map json__ "network" QueryNetwork.of_json in
      let transactionId =
        field_map json__ "transactionId" QueryTransactionId.of_json in
      let transactionHash =
        field_map json__ "transactionHash" QueryTransactionHash.of_json in
      make ?confirmationStatus ?transactionTimestamp ?network ?transactionId
        ?transactionHash ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The container of the transaction output."]
module ValidationExceptionFieldList =
  struct
    type nonrec t = ValidationExceptionField.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ValidationExceptionField.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ValidationExceptionField.of_xml)
    let of_json j =
      list_of_json ~kind:"ValidationExceptionFieldList"
        ~of_json:ValidationExceptionField.of_json j
    let to_json v = composed_to_json to_value v
  end
module ValidationExceptionReason =
  struct
    type nonrec t =
      | UnknownOperation 
      | CannotParse 
      | FieldValidationFailed 
      | Other 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | UnknownOperation -> "unknownOperation"
      | CannotParse -> "cannotParse"
      | FieldValidationFailed -> "fieldValidationFailed"
      | Other -> "other"
      | Non_static_id s -> s
    let of_string =
      function
      | "unknownOperation" -> UnknownOperation
      | "cannotParse" -> CannotParse
      | "fieldValidationFailed" -> FieldValidationFailed
      | "other" -> Other
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ValidationExceptionReason" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ValidationExceptionReason" j)
    let to_json = simple_to_json to_value
  end
module ConfirmationStatusIncludeList =
  struct
    type nonrec t = ConfirmationStatus.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ConfirmationStatus.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ConfirmationStatus.of_xml)
    let of_json j =
      list_of_json ~kind:"ConfirmationStatusIncludeList"
        ~of_json:ConfirmationStatus.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListTransactionsSortBy =
  struct
    type nonrec t =
      | TRANSACTION_TIMESTAMP 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | TRANSACTION_TIMESTAMP -> "TRANSACTION_TIMESTAMP"
      | Non_static_id s -> s
    let of_string =
      function
      | "TRANSACTION_TIMESTAMP" -> TRANSACTION_TIMESTAMP
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ListTransactionsSortBy" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ListTransactionsSortBy" j)
    let to_json = simple_to_json to_value
  end
module SortOrder =
  struct
    type nonrec t =
      | ASCENDING 
      | DESCENDING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ASCENDING -> "ASCENDING"
      | DESCENDING -> "DESCENDING"
      | Non_static_id s -> s
    let of_string =
      function
      | "ASCENDING" -> ASCENDING
      | "DESCENDING" -> DESCENDING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration SortOrder" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SortOrder" j)
    let to_json = simple_to_json to_value
  end
module TransactionEvent =
  struct
    type nonrec t =
      {
      network: QueryNetwork.t option
        [@ocaml.doc "The blockchain network where the transaction occurred."];
      transactionHash: QueryTransactionHash.t option
        [@ocaml.doc
          "The hash of a transaction. It is generated when a transaction is created."];
      eventType: QueryTransactionEventType.t option
        [@ocaml.doc "The type of transaction event."];
      from: ChainAddress.t option
        [@ocaml.doc
          "The wallet address initiating the transaction. It can either be a public key or a contract."];
      to_: ChainAddress.t option
        [@ocaml.doc
          "The wallet address receiving the transaction. It can either be a public key or a contract."];
      value: String_.t option [@ocaml.doc "The value that was transacted."];
      contractAddress: ChainAddress.t option
        [@ocaml.doc "The blockchain address for the contract"];
      tokenId: QueryTokenId.t option
        [@ocaml.doc
          "The unique identifier for the token involved in the transaction."];
      transactionId: String_.t option
        [@ocaml.doc
          "The identifier of a Bitcoin transaction. It is generated when a transaction is created."];
      voutIndex: Integer.t option
        [@ocaml.doc
          "The position of the transaction output in the transaction output list."];
      voutSpent: Boolean.t option
        [@ocaml.doc
          "Specifies if the transaction output is spent or unspent. This is only returned for BITCOIN_VOUT event types. This is only returned for BITCOIN_VOUT event types."];
      spentVoutTransactionId: String_.t option
        [@ocaml.doc
          "The transactionId that created the spent transaction output. This is only returned for BITCOIN_VIN event types."];
      spentVoutTransactionHash: String_.t option
        [@ocaml.doc
          "The transactionHash that created the spent transaction output. This is only returned for BITCOIN_VIN event types."];
      spentVoutIndex: Integer.t option
        [@ocaml.doc
          "The position of the spent transaction output in the output list of the creating transaction. This is only returned for BITCOIN_VIN event types."];
      blockchainInstant: BlockchainInstant.t option ;
      confirmationStatus: ConfirmationStatus.t option
        [@ocaml.doc
          "This container specifies whether the transaction has reached Finality."]}
    let make ?network =
      fun ?transactionHash ->
        fun ?eventType ->
          fun ?from ->
            fun ?to_ ->
              fun ?value ->
                fun ?contractAddress ->
                  fun ?tokenId ->
                    fun ?transactionId ->
                      fun ?voutIndex ->
                        fun ?voutSpent ->
                          fun ?spentVoutTransactionId ->
                            fun ?spentVoutTransactionHash ->
                              fun ?spentVoutIndex ->
                                fun ?blockchainInstant ->
                                  fun ?confirmationStatus ->
                                    fun () ->
                                      {
                                        network;
                                        transactionHash;
                                        eventType;
                                        from;
                                        to_;
                                        value;
                                        contractAddress;
                                        tokenId;
                                        transactionId;
                                        voutIndex;
                                        voutSpent;
                                        spentVoutTransactionId;
                                        spentVoutTransactionHash;
                                        spentVoutIndex;
                                        blockchainInstant;
                                        confirmationStatus
                                      }
    let to_value x =
      structure_to_value
        [("network", (Option.map x.network ~f:QueryNetwork.to_value));
        ("transactionHash",
          (Option.map x.transactionHash ~f:QueryTransactionHash.to_value));
        ("eventType",
          (Option.map x.eventType ~f:QueryTransactionEventType.to_value));
        ("from", (Option.map x.from ~f:ChainAddress.to_value));
        ("to", (Option.map x.to_ ~f:ChainAddress.to_value));
        ("value", (Option.map x.value ~f:String_.to_value));
        ("contractAddress",
          (Option.map x.contractAddress ~f:ChainAddress.to_value));
        ("tokenId", (Option.map x.tokenId ~f:QueryTokenId.to_value));
        ("transactionId", (Option.map x.transactionId ~f:String_.to_value));
        ("voutIndex", (Option.map x.voutIndex ~f:Integer.to_value));
        ("voutSpent", (Option.map x.voutSpent ~f:Boolean.to_value));
        ("spentVoutTransactionId",
          (Option.map x.spentVoutTransactionId ~f:String_.to_value));
        ("spentVoutTransactionHash",
          (Option.map x.spentVoutTransactionHash ~f:String_.to_value));
        ("spentVoutIndex", (Option.map x.spentVoutIndex ~f:Integer.to_value));
        ("blockchainInstant",
          (Option.map x.blockchainInstant ~f:BlockchainInstant.to_value));
        ("confirmationStatus",
          (Option.map x.confirmationStatus ~f:ConfirmationStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let confirmationStatus =
        (Option.map ~f:ConfirmationStatus.of_xml)
          (Xml.child xml_arg0 "confirmationStatus") in
      let blockchainInstant =
        (Option.map ~f:BlockchainInstant.of_xml)
          (Xml.child xml_arg0 "blockchainInstant") in
      let spentVoutIndex =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "spentVoutIndex") in
      let spentVoutTransactionHash =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "spentVoutTransactionHash") in
      let spentVoutTransactionId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "spentVoutTransactionId") in
      let voutSpent =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "voutSpent") in
      let voutIndex =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "voutIndex") in
      let transactionId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "transactionId") in
      let tokenId =
        (Option.map ~f:QueryTokenId.of_xml) (Xml.child xml_arg0 "tokenId") in
      let contractAddress =
        (Option.map ~f:ChainAddress.of_xml)
          (Xml.child xml_arg0 "contractAddress") in
      let value = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "value") in
      let to_ = (Option.map ~f:ChainAddress.of_xml) (Xml.child xml_arg0 "to") in
      let from =
        (Option.map ~f:ChainAddress.of_xml) (Xml.child xml_arg0 "from") in
      let eventType =
        (Option.map ~f:QueryTransactionEventType.of_xml)
          (Xml.child xml_arg0 "eventType") in
      let transactionHash =
        (Option.map ~f:QueryTransactionHash.of_xml)
          (Xml.child xml_arg0 "transactionHash") in
      let network =
        (Option.map ~f:QueryNetwork.of_xml) (Xml.child xml_arg0 "network") in
      make ?confirmationStatus ?blockchainInstant ?spentVoutIndex
        ?spentVoutTransactionHash ?spentVoutTransactionId ?voutSpent
        ?voutIndex ?transactionId ?tokenId ?contractAddress ?value ?to_ ?from
        ?eventType ?transactionHash ?network ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let confirmationStatus =
        field_map json__ "confirmationStatus" ConfirmationStatus.of_json in
      let blockchainInstant =
        field_map json__ "blockchainInstant" BlockchainInstant.of_json in
      let spentVoutIndex = field_map json__ "spentVoutIndex" Integer.of_json in
      let spentVoutTransactionHash =
        field_map json__ "spentVoutTransactionHash" String_.of_json in
      let spentVoutTransactionId =
        field_map json__ "spentVoutTransactionId" String_.of_json in
      let voutSpent = field_map json__ "voutSpent" Boolean.of_json in
      let voutIndex = field_map json__ "voutIndex" Integer.of_json in
      let transactionId = field_map json__ "transactionId" String_.of_json in
      let tokenId = field_map json__ "tokenId" QueryTokenId.of_json in
      let contractAddress =
        field_map json__ "contractAddress" ChainAddress.of_json in
      let value = field_map json__ "value" String_.of_json in
      let to_ = field_map json__ "to" ChainAddress.of_json in
      let from = field_map json__ "from" ChainAddress.of_json in
      let eventType =
        field_map json__ "eventType" QueryTransactionEventType.of_json in
      let transactionHash =
        field_map json__ "transactionHash" QueryTransactionHash.of_json in
      let network = field_map json__ "network" QueryNetwork.of_json in
      make ?confirmationStatus ?blockchainInstant ?spentVoutIndex
        ?spentVoutTransactionHash ?spentVoutTransactionId ?voutSpent
        ?voutIndex ?transactionId ?tokenId ?contractAddress ?value ?to_ ?from
        ?eventType ?transactionHash ?network ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The container for the properties of a transaction event."]
module TokenBalance =
  struct
    type nonrec t =
      {
      ownerIdentifier: OwnerIdentifier.t option
        [@ocaml.doc "The container for the identifier of the owner."];
      tokenIdentifier: TokenIdentifier.t option
        [@ocaml.doc
          "The identifier for the token, including the unique token ID and its blockchain network."];
      balance: String_.t option
        [@ocaml.doc "The container of the token balance."];
      atBlockchainInstant: BlockchainInstant.t option
        [@ocaml.doc
          "The time for when the TokenBalance is requested or the current time if a time is not provided in the request. This time will only be recorded up to the second."];
      lastUpdatedTime: BlockchainInstant.t option
        [@ocaml.doc
          "The Timestamp of the last transaction at which the balance for the token in the wallet was updated."]}
    let make ?ownerIdentifier =
      fun ?tokenIdentifier ->
        fun ?balance ->
          fun ?atBlockchainInstant ->
            fun ?lastUpdatedTime ->
              fun () ->
                {
                  ownerIdentifier;
                  tokenIdentifier;
                  balance;
                  atBlockchainInstant;
                  lastUpdatedTime
                }
    let to_value x =
      structure_to_value
        [("ownerIdentifier",
           (Option.map x.ownerIdentifier ~f:OwnerIdentifier.to_value));
        ("tokenIdentifier",
          (Option.map x.tokenIdentifier ~f:TokenIdentifier.to_value));
        ("balance", (Option.map x.balance ~f:String_.to_value));
        ("atBlockchainInstant",
          (Option.map x.atBlockchainInstant ~f:BlockchainInstant.to_value));
        ("lastUpdatedTime",
          (Option.map x.lastUpdatedTime ~f:BlockchainInstant.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdatedTime =
        (Option.map ~f:BlockchainInstant.of_xml)
          (Xml.child xml_arg0 "lastUpdatedTime") in
      let atBlockchainInstant =
        (Option.map ~f:BlockchainInstant.of_xml)
          (Xml.child xml_arg0 "atBlockchainInstant") in
      let balance =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "balance") in
      let tokenIdentifier =
        (Option.map ~f:TokenIdentifier.of_xml)
          (Xml.child xml_arg0 "tokenIdentifier") in
      let ownerIdentifier =
        (Option.map ~f:OwnerIdentifier.of_xml)
          (Xml.child xml_arg0 "ownerIdentifier") in
      make ?lastUpdatedTime ?atBlockchainInstant ?balance ?tokenIdentifier
        ?ownerIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let lastUpdatedTime =
        field_map json__ "lastUpdatedTime" BlockchainInstant.of_json in
      let atBlockchainInstant =
        field_map json__ "atBlockchainInstant" BlockchainInstant.of_json in
      let balance = field_map json__ "balance" String_.of_json in
      let tokenIdentifier =
        field_map json__ "tokenIdentifier" TokenIdentifier.of_json in
      let ownerIdentifier =
        field_map json__ "ownerIdentifier" OwnerIdentifier.of_json in
      make ?lastUpdatedTime ?atBlockchainInstant ?balance ?tokenIdentifier
        ?ownerIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The balance of the token."]
module AddressIdentifierFilterTransactionEventToAddressList =
  struct
    type nonrec t = ChainAddress.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:1) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ChainAddress.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ChainAddress.of_xml)
    let of_json j =
      list_of_json
        ~kind:"AddressIdentifierFilterTransactionEventToAddressList"
        ~of_json:ChainAddress.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListFilteredTransactionEventsSortBy =
  struct
    type nonrec t =
      | BlockchainInstant 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | BlockchainInstant -> "blockchainInstant"
      | Non_static_id s -> s
    let of_string =
      function
      | "blockchainInstant" -> BlockchainInstant
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml
           ~kind:"enumeration ListFilteredTransactionEventsSortBy" xml_arg0)
    let of_json j =
      of_string
        (string_of_json ~kind:"ListFilteredTransactionEventsSortBy" j)
    let to_json = simple_to_json to_value
  end
module AssetContract =
  struct
    type nonrec t =
      {
      contractIdentifier: ContractIdentifier.t option
        [@ocaml.doc
          "The container for the contract identifier containing its blockchain network and address."];
      tokenStandard: QueryTokenStandard.t option
        [@ocaml.doc "The token standard of the contract."];
      deployerAddress: ChainAddress.t option
        [@ocaml.doc "The address of the contract deployer."]}
    let make ?contractIdentifier =
      fun ?tokenStandard ->
        fun ?deployerAddress ->
          fun () -> { contractIdentifier; tokenStandard; deployerAddress }
    let to_value x =
      structure_to_value
        [("contractIdentifier",
           (Option.map x.contractIdentifier ~f:ContractIdentifier.to_value));
        ("tokenStandard",
          (Option.map x.tokenStandard ~f:QueryTokenStandard.to_value));
        ("deployerAddress",
          (Option.map x.deployerAddress ~f:ChainAddress.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deployerAddress =
        (Option.map ~f:ChainAddress.of_xml)
          (Xml.child xml_arg0 "deployerAddress") in
      let tokenStandard =
        (Option.map ~f:QueryTokenStandard.of_xml)
          (Xml.child xml_arg0 "tokenStandard") in
      let contractIdentifier =
        (Option.map ~f:ContractIdentifier.of_xml)
          (Xml.child xml_arg0 "contractIdentifier") in
      make ?deployerAddress ?tokenStandard ?contractIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let deployerAddress =
        field_map json__ "deployerAddress" ChainAddress.of_json in
      let tokenStandard =
        field_map json__ "tokenStandard" QueryTokenStandard.of_json in
      let contractIdentifier =
        field_map json__ "contractIdentifier" ContractIdentifier.of_json in
      make ?deployerAddress ?tokenStandard ?contractIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "This container contains information about an contract."]
module BlockHash =
  struct
    type nonrec t = string
    let context_ = "BlockHash"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"(0x[A-Fa-f0-9]{64}|[A-Fa-f0-9]{64})");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BlockHash" j
    let to_json = simple_to_json to_value
  end
module ExecutionStatus =
  struct
    type nonrec t =
      | FAILED 
      | SUCCEEDED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | FAILED -> "FAILED"
      | SUCCEEDED -> "SUCCEEDED"
      | Non_static_id s -> s
    let of_string =
      function
      | "FAILED" -> FAILED
      | "SUCCEEDED" -> SUCCEEDED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ExecutionStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ExecutionStatus" j)
    let to_json = simple_to_json to_value
  end
module Long =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module BatchGetTokenBalanceErrorItem =
  struct
    type nonrec t =
      {
      tokenIdentifier: TokenIdentifier.t option ;
      ownerIdentifier: OwnerIdentifier.t option ;
      atBlockchainInstant: BlockchainInstant.t option ;
      errorCode: String_.t option
        [@ocaml.doc "The error code associated with the error."];
      errorMessage: String_.t option
        [@ocaml.doc "The message associated with the error."];
      errorType: ErrorType.t option [@ocaml.doc "The type of error."]}
    let make ?tokenIdentifier =
      fun ?ownerIdentifier ->
        fun ?atBlockchainInstant ->
          fun ?errorCode ->
            fun ?errorMessage ->
              fun ?errorType ->
                fun () ->
                  {
                    tokenIdentifier;
                    ownerIdentifier;
                    atBlockchainInstant;
                    errorCode;
                    errorMessage;
                    errorType
                  }
    let to_value x =
      structure_to_value
        [("tokenIdentifier",
           (Option.map x.tokenIdentifier ~f:TokenIdentifier.to_value));
        ("ownerIdentifier",
          (Option.map x.ownerIdentifier ~f:OwnerIdentifier.to_value));
        ("atBlockchainInstant",
          (Option.map x.atBlockchainInstant ~f:BlockchainInstant.to_value));
        ("errorCode", (Option.map x.errorCode ~f:String_.to_value));
        ("errorMessage", (Option.map x.errorMessage ~f:String_.to_value));
        ("errorType", (Option.map x.errorType ~f:ErrorType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errorType =
        (Option.map ~f:ErrorType.of_xml) (Xml.child xml_arg0 "errorType") in
      let errorMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "errorMessage") in
      let errorCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "errorCode") in
      let atBlockchainInstant =
        (Option.map ~f:BlockchainInstant.of_xml)
          (Xml.child xml_arg0 "atBlockchainInstant") in
      let ownerIdentifier =
        (Option.map ~f:OwnerIdentifier.of_xml)
          (Xml.child xml_arg0 "ownerIdentifier") in
      let tokenIdentifier =
        (Option.map ~f:TokenIdentifier.of_xml)
          (Xml.child xml_arg0 "tokenIdentifier") in
      make ?errorType ?errorMessage ?errorCode ?atBlockchainInstant
        ?ownerIdentifier ?tokenIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let errorType = field_map json__ "errorType" ErrorType.of_json in
      let errorMessage = field_map json__ "errorMessage" String_.of_json in
      let errorCode = field_map json__ "errorCode" String_.of_json in
      let atBlockchainInstant =
        field_map json__ "atBlockchainInstant" BlockchainInstant.of_json in
      let ownerIdentifier =
        field_map json__ "ownerIdentifier" OwnerIdentifier.of_json in
      let tokenIdentifier =
        field_map json__ "tokenIdentifier" TokenIdentifier.of_json in
      make ?errorType ?errorMessage ?errorCode ?atBlockchainInstant
        ?ownerIdentifier ?tokenIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Error generated from a failed BatchGetTokenBalance request."]
module BatchGetTokenBalanceOutputItem =
  struct
    type nonrec t =
      {
      ownerIdentifier: OwnerIdentifier.t option ;
      tokenIdentifier: TokenIdentifier.t option ;
      balance: String_.t option
        [@ocaml.doc "The container for the token balance."];
      atBlockchainInstant: BlockchainInstant.t option ;
      lastUpdatedTime: BlockchainInstant.t option }
    let make ?ownerIdentifier =
      fun ?tokenIdentifier ->
        fun ?balance ->
          fun ?atBlockchainInstant ->
            fun ?lastUpdatedTime ->
              fun () ->
                {
                  ownerIdentifier;
                  tokenIdentifier;
                  balance;
                  atBlockchainInstant;
                  lastUpdatedTime
                }
    let to_value x =
      structure_to_value
        [("ownerIdentifier",
           (Option.map x.ownerIdentifier ~f:OwnerIdentifier.to_value));
        ("tokenIdentifier",
          (Option.map x.tokenIdentifier ~f:TokenIdentifier.to_value));
        ("balance", (Option.map x.balance ~f:String_.to_value));
        ("atBlockchainInstant",
          (Option.map x.atBlockchainInstant ~f:BlockchainInstant.to_value));
        ("lastUpdatedTime",
          (Option.map x.lastUpdatedTime ~f:BlockchainInstant.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdatedTime =
        (Option.map ~f:BlockchainInstant.of_xml)
          (Xml.child xml_arg0 "lastUpdatedTime") in
      let atBlockchainInstant =
        (Option.map ~f:BlockchainInstant.of_xml)
          (Xml.child xml_arg0 "atBlockchainInstant") in
      let balance =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "balance") in
      let tokenIdentifier =
        (Option.map ~f:TokenIdentifier.of_xml)
          (Xml.child xml_arg0 "tokenIdentifier") in
      let ownerIdentifier =
        (Option.map ~f:OwnerIdentifier.of_xml)
          (Xml.child xml_arg0 "ownerIdentifier") in
      make ?lastUpdatedTime ?atBlockchainInstant ?balance ?tokenIdentifier
        ?ownerIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let lastUpdatedTime =
        field_map json__ "lastUpdatedTime" BlockchainInstant.of_json in
      let atBlockchainInstant =
        field_map json__ "atBlockchainInstant" BlockchainInstant.of_json in
      let balance = field_map json__ "balance" String_.of_json in
      let tokenIdentifier =
        field_map json__ "tokenIdentifier" TokenIdentifier.of_json in
      let ownerIdentifier =
        field_map json__ "ownerIdentifier" OwnerIdentifier.of_json in
      make ?lastUpdatedTime ?atBlockchainInstant ?balance ?tokenIdentifier
        ?ownerIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The container for the properties of a token balance output."]
module BatchGetTokenBalanceInputItem =
  struct
    type nonrec t =
      {
      tokenIdentifier: TokenIdentifier.t ;
      ownerIdentifier: OwnerIdentifier.t ;
      atBlockchainInstant: BlockchainInstant.t option }
    let context_ = "BatchGetTokenBalanceInputItem"
    let make ?atBlockchainInstant =
      fun ~tokenIdentifier ->
        fun ~ownerIdentifier ->
          fun () -> { atBlockchainInstant; tokenIdentifier; ownerIdentifier }
    let to_value x =
      structure_to_value
        [("tokenIdentifier",
           (Some (TokenIdentifier.to_value x.tokenIdentifier)));
        ("ownerIdentifier",
          (Some (OwnerIdentifier.to_value x.ownerIdentifier)));
        ("atBlockchainInstant",
          (Option.map x.atBlockchainInstant ~f:BlockchainInstant.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let atBlockchainInstant =
        (Option.map ~f:BlockchainInstant.of_xml)
          (Xml.child xml_arg0 "atBlockchainInstant") in
      let ownerIdentifier =
        OwnerIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ownerIdentifier") in
      let tokenIdentifier =
        TokenIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "tokenIdentifier") in
      make ?atBlockchainInstant ~ownerIdentifier ~tokenIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let atBlockchainInstant =
        field_map json__ "atBlockchainInstant" BlockchainInstant.of_json in
      let ownerIdentifier =
        field_map_exn json__ "ownerIdentifier" OwnerIdentifier.of_json in
      let tokenIdentifier =
        field_map_exn json__ "tokenIdentifier" TokenIdentifier.of_json in
      make ?atBlockchainInstant ~ownerIdentifier ~tokenIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The container for the input for getting a token balance."]
module AccessDeniedException =
  struct
    type nonrec t =
      {
      message: ExceptionMessage.t option
        [@ocaml.doc "The container for the exception message."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ExceptionMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" ExceptionMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The Amazon Web Services account doesn\226\128\153t have access to this resource."]
module InternalServerException =
  struct
    type nonrec t =
      {
      message: ExceptionMessage.t option
        [@ocaml.doc "The container for the exception message."];
      retryAfterSeconds: Integer.t option
        [@ocaml.doc "Specifies the retryAfterSeconds value."]}
    let make ?message =
      fun ?retryAfterSeconds -> fun () -> { message; retryAfterSeconds }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ExceptionMessage.to_value));
        ("Retry-After", (Option.map x.retryAfterSeconds ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let retryAfterSeconds =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Retry-After") in
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "message") in
      make ?retryAfterSeconds ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let retryAfterSeconds =
        field_map json__ "retryAfterSeconds" Integer.of_json in
      let message = field_map json__ "message" ExceptionMessage.of_json in
      make ?retryAfterSeconds ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request processing has failed because of an internal error in the service."]
module NextToken =
  struct
    type nonrec t = string
    let context_ = "NextToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:131070) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NextToken" j
    let to_json = simple_to_json to_value
  end
module ServiceQuotaExceededException =
  struct
    type nonrec t =
      {
      message: ExceptionMessage.t option
        [@ocaml.doc "The container for the exception message."];
      resourceId: ResourceId.t option
        [@ocaml.doc
          "The resourceId of the resource that caused the exception."];
      resourceType: ResourceType.t option
        [@ocaml.doc
          "The resourceType of the resource that caused the exception."];
      serviceCode: ServiceCode.t option
        [@ocaml.doc "The container for the serviceCode."];
      quotaCode: QuotaCode.t option
        [@ocaml.doc "The container for the quotaCode."]}
    let make ?message =
      fun ?resourceId ->
        fun ?resourceType ->
          fun ?serviceCode ->
            fun ?quotaCode ->
              fun () ->
                { message; resourceId; resourceType; serviceCode; quotaCode }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ExceptionMessage.to_value));
        ("resourceId", (Option.map x.resourceId ~f:ResourceId.to_value));
        ("resourceType",
          (Option.map x.resourceType ~f:ResourceType.to_value));
        ("serviceCode", (Option.map x.serviceCode ~f:ServiceCode.to_value));
        ("quotaCode", (Option.map x.quotaCode ~f:QuotaCode.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let quotaCode =
        (Option.map ~f:QuotaCode.of_xml) (Xml.child xml_arg0 "quotaCode") in
      let serviceCode =
        (Option.map ~f:ServiceCode.of_xml) (Xml.child xml_arg0 "serviceCode") in
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "resourceType") in
      let resourceId =
        (Option.map ~f:ResourceId.of_xml) (Xml.child xml_arg0 "resourceId") in
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "message") in
      make ?quotaCode ?serviceCode ?resourceType ?resourceId ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let quotaCode = field_map json__ "quotaCode" QuotaCode.of_json in
      let serviceCode = field_map json__ "serviceCode" ServiceCode.of_json in
      let resourceType = field_map json__ "resourceType" ResourceType.of_json in
      let resourceId = field_map json__ "resourceId" ResourceId.of_json in
      let message = field_map json__ "message" ExceptionMessage.of_json in
      make ?quotaCode ?serviceCode ?resourceType ?resourceId ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The service quota has been exceeded for this resource."]
module ThrottlingException =
  struct
    type nonrec t =
      {
      message: ExceptionMessage.t option
        [@ocaml.doc "The container for the exception message."];
      serviceCode: ServiceCode.t option
        [@ocaml.doc "The container for the serviceCode."];
      quotaCode: QuotaCode.t option
        [@ocaml.doc "The container for the quotaCode."];
      retryAfterSeconds: Integer.t option
        [@ocaml.doc "The container of the retryAfterSeconds value."]}
    let make ?message =
      fun ?serviceCode ->
        fun ?quotaCode ->
          fun ?retryAfterSeconds ->
            fun () -> { message; serviceCode; quotaCode; retryAfterSeconds }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ExceptionMessage.to_value));
        ("serviceCode", (Option.map x.serviceCode ~f:ServiceCode.to_value));
        ("quotaCode", (Option.map x.quotaCode ~f:QuotaCode.to_value));
        ("Retry-After", (Option.map x.retryAfterSeconds ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let retryAfterSeconds =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Retry-After") in
      let quotaCode =
        (Option.map ~f:QuotaCode.of_xml) (Xml.child xml_arg0 "quotaCode") in
      let serviceCode =
        (Option.map ~f:ServiceCode.of_xml) (Xml.child xml_arg0 "serviceCode") in
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "message") in
      make ?retryAfterSeconds ?quotaCode ?serviceCode ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let retryAfterSeconds =
        field_map json__ "retryAfterSeconds" Integer.of_json in
      let quotaCode = field_map json__ "quotaCode" QuotaCode.of_json in
      let serviceCode = field_map json__ "serviceCode" ServiceCode.of_json in
      let message = field_map json__ "message" ExceptionMessage.of_json in
      make ?retryAfterSeconds ?quotaCode ?serviceCode ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request or operation couldn't be performed because a service is throttling requests. The most common source of throttling errors is when you create resources that exceed your service limit for this resource type. Request a limit increase or delete unused resources, if possible."]
module TransactionOutputList =
  struct
    type nonrec t = TransactionOutputItem.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:250) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TransactionOutputItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TransactionOutputItem.of_xml)
    let of_json j =
      list_of_json ~kind:"TransactionOutputList"
        ~of_json:TransactionOutputItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module ValidationException =
  struct
    type nonrec t =
      {
      message: ExceptionMessage.t option
        [@ocaml.doc "The container for the exception message."];
      reason: ValidationExceptionReason.t option
        [@ocaml.doc "The container for the reason for the exception"];
      fieldList: ValidationExceptionFieldList.t option
        [@ocaml.doc "The container for the fieldList of the exception."]}
    let make ?message =
      fun ?reason ->
        fun ?fieldList -> fun () -> { message; reason; fieldList }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ExceptionMessage.to_value));
        ("reason",
          (Option.map x.reason ~f:ValidationExceptionReason.to_value));
        ("fieldList",
          (Option.map x.fieldList ~f:ValidationExceptionFieldList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fieldList =
        (Option.map ~f:ValidationExceptionFieldList.of_xml)
          (Xml.child xml_arg0 "fieldList") in
      let reason =
        (Option.map ~f:ValidationExceptionReason.of_xml)
          (Xml.child xml_arg0 "reason") in
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "message") in
      make ?fieldList ?reason ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fieldList =
        field_map json__ "fieldList" ValidationExceptionFieldList.of_json in
      let reason =
        field_map json__ "reason" ValidationExceptionReason.of_json in
      let message = field_map json__ "message" ExceptionMessage.of_json in
      make ?fieldList ?reason ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The resource passed is invalid."]
module ConfirmationStatusFilter =
  struct
    type nonrec t =
      {
      include_: ConfirmationStatusIncludeList.t
        [@ocaml.doc
          "The container to determine whether to list results that have only reached finality . Transactions that have reached finality are always part of the response."]}
    let context_ = "ConfirmationStatusFilter"
    let make ~include_ = fun () -> { include_ }
    let to_value x =
      structure_to_value
        [("include",
           (Some (ConfirmationStatusIncludeList.to_value x.include_)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let include_ =
        ConfirmationStatusIncludeList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "include") in
      make ~include_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let include_ =
        field_map_exn json__ "include" ConfirmationStatusIncludeList.of_json in
      make ~include_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The container for the ConfirmationStatusFilter that filters for the finality of the results."]
module ListTransactionsInputMaxResultsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:250) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ListTransactionsInputMaxResultsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ListTransactionsSort =
  struct
    type nonrec t =
      {
      sortBy: ListTransactionsSortBy.t option
        [@ocaml.doc "Defaults to the value TRANSACTION_TIMESTAMP."];
      sortOrder: SortOrder.t option
        [@ocaml.doc
          "The container for the sort order for ListTransactions. The SortOrder field only accepts the values ASCENDING and DESCENDING. Not providing SortOrder will default to ASCENDING."]}
    let make ?sortBy = fun ?sortOrder -> fun () -> { sortBy; sortOrder }
    let to_value x =
      structure_to_value
        [("sortBy", (Option.map x.sortBy ~f:ListTransactionsSortBy.to_value));
        ("sortOrder", (Option.map x.sortOrder ~f:SortOrder.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sortOrder =
        (Option.map ~f:SortOrder.of_xml) (Xml.child xml_arg0 "sortOrder") in
      let sortBy =
        (Option.map ~f:ListTransactionsSortBy.of_xml)
          (Xml.child xml_arg0 "sortBy") in
      make ?sortOrder ?sortBy ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sortOrder = field_map json__ "sortOrder" SortOrder.of_json in
      let sortBy = field_map json__ "sortBy" ListTransactionsSortBy.of_json in
      make ?sortOrder ?sortBy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The container for determining how the list transaction result will be sorted."]
module TransactionEventList =
  struct
    type nonrec t = TransactionEvent.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:250) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TransactionEvent.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TransactionEvent.of_xml)
    let of_json j =
      list_of_json ~kind:"TransactionEventList"
        ~of_json:TransactionEvent.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListTransactionEventsInputMaxResultsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:250) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ListTransactionEventsInputMaxResultsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module TokenBalanceList =
  struct
    type nonrec t = TokenBalance.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:250) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TokenBalance.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TokenBalance.of_xml)
    let of_json j =
      list_of_json ~kind:"TokenBalanceList" ~of_json:TokenBalance.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListTokenBalancesInputMaxResultsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:250) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ListTokenBalancesInputMaxResultsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module OwnerFilter =
  struct
    type nonrec t =
      {
      address: ChainAddress.t [@ocaml.doc "The contract or wallet address."]}
    let context_ = "OwnerFilter"
    let make ~address = fun () -> { address }
    let to_value x =
      structure_to_value
        [("address", (Some (ChainAddress.to_value x.address)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let address =
        ChainAddress.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "address") in
      make ~address ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let address = field_map_exn json__ "address" ChainAddress.of_json in
      make ~address ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The container for the owner information to filter by."]
module TokenFilter =
  struct
    type nonrec t =
      {
      network: QueryNetwork.t
        [@ocaml.doc "The blockchain network of the token."];
      contractAddress: ChainAddress.t option
        [@ocaml.doc "This is the address of the contract."];
      tokenId: QueryTokenId.t option
        [@ocaml.doc "The unique identifier of the token."]}
    let context_ = "TokenFilter"
    let make ?contractAddress =
      fun ?tokenId ->
        fun ~network -> fun () -> { contractAddress; tokenId; network }
    let to_value x =
      structure_to_value
        [("network", (Some (QueryNetwork.to_value x.network)));
        ("contractAddress",
          (Option.map x.contractAddress ~f:ChainAddress.to_value));
        ("tokenId", (Option.map x.tokenId ~f:QueryTokenId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tokenId =
        (Option.map ~f:QueryTokenId.of_xml) (Xml.child xml_arg0 "tokenId") in
      let contractAddress =
        (Option.map ~f:ChainAddress.of_xml)
          (Xml.child xml_arg0 "contractAddress") in
      let network =
        QueryNetwork.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "network") in
      make ?tokenId ?contractAddress ~network ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tokenId = field_map json__ "tokenId" QueryTokenId.of_json in
      let contractAddress =
        field_map json__ "contractAddress" ChainAddress.of_json in
      let network = field_map_exn json__ "network" QueryNetwork.of_json in
      make ?tokenId ?contractAddress ~network ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The container of the token filter like the contract address on a given blockchain network or a unique token identifier on a given blockchain network. You must always specify the network property of this container when using this operation."]
module AddressIdentifierFilter =
  struct
    type nonrec t =
      {
      transactionEventToAddress:
        AddressIdentifierFilterTransactionEventToAddressList.t
        [@ocaml.doc
          "The container for the recipient address of the transaction."]}
    let context_ = "AddressIdentifierFilter"
    let make ~transactionEventToAddress =
      fun () -> { transactionEventToAddress }
    let to_value x =
      structure_to_value
        [("transactionEventToAddress",
           (Some
              (AddressIdentifierFilterTransactionEventToAddressList.to_value
                 x.transactionEventToAddress)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transactionEventToAddress =
        AddressIdentifierFilterTransactionEventToAddressList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "transactionEventToAddress") in
      make ~transactionEventToAddress ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transactionEventToAddress =
        field_map_exn json__ "transactionEventToAddress"
          AddressIdentifierFilterTransactionEventToAddressList.of_json in
      make ~transactionEventToAddress ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This is the container for the unique public address on the blockchain."]
module ListFilteredTransactionEventsInputMaxResultsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:250) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ListFilteredTransactionEventsInputMaxResultsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ListFilteredTransactionEventsSort =
  struct
    type nonrec t =
      {
      sortBy: ListFilteredTransactionEventsSortBy.t option
        [@ocaml.doc "Container on how the results will be sorted by?"];
      sortOrder: SortOrder.t option
        [@ocaml.doc
          "The container for the sort order for ListFilteredTransactionEvents. The SortOrder field only accepts the values ASCENDING and DESCENDING. Not providing SortOrder will default to ASCENDING."]}
    let make ?sortBy = fun ?sortOrder -> fun () -> { sortBy; sortOrder }
    let to_value x =
      structure_to_value
        [("sortBy",
           (Option.map x.sortBy
              ~f:ListFilteredTransactionEventsSortBy.to_value));
        ("sortOrder", (Option.map x.sortOrder ~f:SortOrder.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sortOrder =
        (Option.map ~f:SortOrder.of_xml) (Xml.child xml_arg0 "sortOrder") in
      let sortBy =
        (Option.map ~f:ListFilteredTransactionEventsSortBy.of_xml)
          (Xml.child xml_arg0 "sortBy") in
      make ?sortOrder ?sortBy ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sortOrder = field_map json__ "sortOrder" SortOrder.of_json in
      let sortBy =
        field_map json__ "sortBy" ListFilteredTransactionEventsSortBy.of_json in
      make ?sortOrder ?sortBy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all the transaction events for an address on the blockchain. This operation is only supported on the Bitcoin blockchain networks."]
module TimeFilter =
  struct
    type nonrec t =
      {
      from: BlockchainInstant.t option ;
      to_: BlockchainInstant.t option }
    let make ?from = fun ?to_ -> fun () -> { from; to_ }
    let to_value x =
      structure_to_value
        [("from", (Option.map x.from ~f:BlockchainInstant.to_value));
        ("to", (Option.map x.to_ ~f:BlockchainInstant.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let to_ =
        (Option.map ~f:BlockchainInstant.of_xml) (Xml.child xml_arg0 "to") in
      let from =
        (Option.map ~f:BlockchainInstant.of_xml) (Xml.child xml_arg0 "from") in
      make ?to_ ?from ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let to_ = field_map json__ "to" BlockchainInstant.of_json in
      let from = field_map json__ "from" BlockchainInstant.of_json in
      make ?to_ ?from ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "This container is used to specify a time frame."]
module VoutFilter =
  struct
    type nonrec t =
      {
      voutSpent: Boolean.t
        [@ocaml.doc
          "Specifies if the transaction output is spent or unspent."]}
    let context_ = "VoutFilter"
    let make ~voutSpent = fun () -> { voutSpent }
    let to_value x =
      structure_to_value
        [("voutSpent", (Some (Boolean.to_value x.voutSpent)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let voutSpent =
        Boolean.of_xml (Xml.child_exn ~context:context_ xml_arg0 "voutSpent") in
      make ~voutSpent ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let voutSpent = field_map_exn json__ "voutSpent" Boolean.of_json in
      make ~voutSpent ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This container specifies filtering attributes related to BITCOIN_VOUT event types"]
module AssetContractList =
  struct
    type nonrec t = AssetContract.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:250) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AssetContract.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AssetContract.of_xml)
    let of_json j =
      list_of_json ~kind:"AssetContractList" ~of_json:AssetContract.of_json j
    let to_json v = composed_to_json to_value v
  end
module ContractFilter =
  struct
    type nonrec t =
      {
      network: QueryNetwork.t
        [@ocaml.doc "The blockchain network of the contract."];
      tokenStandard: QueryTokenStandard.t
        [@ocaml.doc "The container for the token standard."];
      deployerAddress: ChainAddress.t
        [@ocaml.doc "The network address of the deployer."]}
    let context_ = "ContractFilter"
    let make ~network =
      fun ~tokenStandard ->
        fun ~deployerAddress ->
          fun () -> { network; tokenStandard; deployerAddress }
    let to_value x =
      structure_to_value
        [("network", (Some (QueryNetwork.to_value x.network)));
        ("tokenStandard",
          (Some (QueryTokenStandard.to_value x.tokenStandard)));
        ("deployerAddress", (Some (ChainAddress.to_value x.deployerAddress)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deployerAddress =
        ChainAddress.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "deployerAddress") in
      let tokenStandard =
        QueryTokenStandard.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "tokenStandard") in
      let network =
        QueryNetwork.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "network") in
      make ~deployerAddress ~tokenStandard ~network ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let deployerAddress =
        field_map_exn json__ "deployerAddress" ChainAddress.of_json in
      let tokenStandard =
        field_map_exn json__ "tokenStandard" QueryTokenStandard.of_json in
      let network = field_map_exn json__ "network" QueryNetwork.of_json in
      make ~deployerAddress ~tokenStandard ~network ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The contract or wallet address by which to filter the request."]
module ListAssetContractsInputMaxResultsInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:250) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ListAssetContractsInputMaxResultsInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ResourceNotFoundException =
  struct
    type nonrec t =
      {
      message: ExceptionMessage.t option
        [@ocaml.doc "The container for the exception message."];
      resourceId: ResourceId.t option
        [@ocaml.doc
          "The resourceId of the resource that caused the exception."];
      resourceType: ResourceType.t option
        [@ocaml.doc
          "The resourceType of the resource that caused the exception."]}
    let make ?message =
      fun ?resourceId ->
        fun ?resourceType -> fun () -> { message; resourceId; resourceType }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ExceptionMessage.to_value));
        ("resourceId", (Option.map x.resourceId ~f:ResourceId.to_value));
        ("resourceType",
          (Option.map x.resourceType ~f:ResourceType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "resourceType") in
      let resourceId =
        (Option.map ~f:ResourceId.of_xml) (Xml.child xml_arg0 "resourceId") in
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "message") in
      make ?resourceType ?resourceId ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceType = field_map json__ "resourceType" ResourceType.of_json in
      let resourceId = field_map json__ "resourceId" ResourceId.of_json in
      let message = field_map json__ "message" ExceptionMessage.of_json in
      make ?resourceType ?resourceId ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The resource was not found."]
module Transaction =
  struct
    type nonrec t =
      {
      network: QueryNetwork.t option
        [@ocaml.doc "The blockchain network where the transaction occurred."];
      blockHash: BlockHash.t option
        [@ocaml.doc
          "The block hash is a unique identifier for a block. It is a fixed-size string that is calculated by using the information in the block. The block hash is used to verify the integrity of the data in the block."];
      transactionHash: QueryTransactionHash.t option
        [@ocaml.doc
          "The hash of a transaction. It is generated when a transaction is created."];
      blockNumber: String_.t option
        [@ocaml.doc "The block number in which the transaction is recorded."];
      transactionTimestamp: Timestamp.t option
        [@ocaml.doc "The Timestamp of the transaction."];
      transactionIndex: Long.t option
        [@ocaml.doc "The index of the transaction within a blockchain."];
      numberOfTransactions: Long.t option
        [@ocaml.doc "The number of transactions in the block."];
      to_: ChainAddress.t option
        [@ocaml.doc
          "The identifier of the transaction. It is generated whenever a transaction is verified and added to the blockchain."];
      from: ChainAddress.t option
        [@ocaml.doc
          "The initiator of the transaction. It is either in the form a public key or a contract address."];
      contractAddress: ChainAddress.t option
        [@ocaml.doc "The blockchain address for the contract."];
      gasUsed: String_.t option
        [@ocaml.doc "The amount of gas used for the transaction."];
      cumulativeGasUsed: String_.t option
        [@ocaml.doc
          "The amount of gas used up to the specified point in the block."];
      effectiveGasPrice: String_.t option
        [@ocaml.doc "The effective gas price."];
      signatureV: Integer.t option
        [@ocaml.doc
          "The signature of the transaction. The Z coordinate of a point V."];
      signatureR: String_.t option
        [@ocaml.doc
          "The signature of the transaction. The X coordinate of a point R."];
      signatureS: String_.t option
        [@ocaml.doc
          "The signature of the transaction. The Y coordinate of a point S."];
      transactionFee: String_.t option [@ocaml.doc "The transaction fee."];
      transactionId: String_.t option
        [@ocaml.doc
          "The identifier of a Bitcoin transaction. It is generated when a transaction is created."];
      confirmationStatus: ConfirmationStatus.t option
        [@ocaml.doc
          "Specifies whether the transaction has reached Finality."];
      executionStatus: ExecutionStatus.t option
        [@ocaml.doc
          "Identifies whether the transaction has succeeded or failed."]}
    let make ?network =
      fun ?blockHash ->
        fun ?transactionHash ->
          fun ?blockNumber ->
            fun ?transactionTimestamp ->
              fun ?transactionIndex ->
                fun ?numberOfTransactions ->
                  fun ?to_ ->
                    fun ?from ->
                      fun ?contractAddress ->
                        fun ?gasUsed ->
                          fun ?cumulativeGasUsed ->
                            fun ?effectiveGasPrice ->
                              fun ?signatureV ->
                                fun ?signatureR ->
                                  fun ?signatureS ->
                                    fun ?transactionFee ->
                                      fun ?transactionId ->
                                        fun ?confirmationStatus ->
                                          fun ?executionStatus ->
                                            fun () ->
                                              {
                                                network;
                                                blockHash;
                                                transactionHash;
                                                blockNumber;
                                                transactionTimestamp;
                                                transactionIndex;
                                                numberOfTransactions;
                                                to_;
                                                from;
                                                contractAddress;
                                                gasUsed;
                                                cumulativeGasUsed;
                                                effectiveGasPrice;
                                                signatureV;
                                                signatureR;
                                                signatureS;
                                                transactionFee;
                                                transactionId;
                                                confirmationStatus;
                                                executionStatus
                                              }
    let to_value x =
      structure_to_value
        [("network", (Option.map x.network ~f:QueryNetwork.to_value));
        ("blockHash", (Option.map x.blockHash ~f:BlockHash.to_value));
        ("transactionHash",
          (Option.map x.transactionHash ~f:QueryTransactionHash.to_value));
        ("blockNumber", (Option.map x.blockNumber ~f:String_.to_value));
        ("transactionTimestamp",
          (Option.map x.transactionTimestamp ~f:Timestamp.to_value));
        ("transactionIndex",
          (Option.map x.transactionIndex ~f:Long.to_value));
        ("numberOfTransactions",
          (Option.map x.numberOfTransactions ~f:Long.to_value));
        ("to", (Option.map x.to_ ~f:ChainAddress.to_value));
        ("from", (Option.map x.from ~f:ChainAddress.to_value));
        ("contractAddress",
          (Option.map x.contractAddress ~f:ChainAddress.to_value));
        ("gasUsed", (Option.map x.gasUsed ~f:String_.to_value));
        ("cumulativeGasUsed",
          (Option.map x.cumulativeGasUsed ~f:String_.to_value));
        ("effectiveGasPrice",
          (Option.map x.effectiveGasPrice ~f:String_.to_value));
        ("signatureV", (Option.map x.signatureV ~f:Integer.to_value));
        ("signatureR", (Option.map x.signatureR ~f:String_.to_value));
        ("signatureS", (Option.map x.signatureS ~f:String_.to_value));
        ("transactionFee", (Option.map x.transactionFee ~f:String_.to_value));
        ("transactionId", (Option.map x.transactionId ~f:String_.to_value));
        ("confirmationStatus",
          (Option.map x.confirmationStatus ~f:ConfirmationStatus.to_value));
        ("executionStatus",
          (Option.map x.executionStatus ~f:ExecutionStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let executionStatus =
        (Option.map ~f:ExecutionStatus.of_xml)
          (Xml.child xml_arg0 "executionStatus") in
      let confirmationStatus =
        (Option.map ~f:ConfirmationStatus.of_xml)
          (Xml.child xml_arg0 "confirmationStatus") in
      let transactionId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "transactionId") in
      let transactionFee =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "transactionFee") in
      let signatureS =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "signatureS") in
      let signatureR =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "signatureR") in
      let signatureV =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "signatureV") in
      let effectiveGasPrice =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "effectiveGasPrice") in
      let cumulativeGasUsed =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "cumulativeGasUsed") in
      let gasUsed =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "gasUsed") in
      let contractAddress =
        (Option.map ~f:ChainAddress.of_xml)
          (Xml.child xml_arg0 "contractAddress") in
      let from =
        (Option.map ~f:ChainAddress.of_xml) (Xml.child xml_arg0 "from") in
      let to_ = (Option.map ~f:ChainAddress.of_xml) (Xml.child xml_arg0 "to") in
      let numberOfTransactions =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "numberOfTransactions") in
      let transactionIndex =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "transactionIndex") in
      let transactionTimestamp =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "transactionTimestamp") in
      let blockNumber =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "blockNumber") in
      let transactionHash =
        (Option.map ~f:QueryTransactionHash.of_xml)
          (Xml.child xml_arg0 "transactionHash") in
      let blockHash =
        (Option.map ~f:BlockHash.of_xml) (Xml.child xml_arg0 "blockHash") in
      let network =
        (Option.map ~f:QueryNetwork.of_xml) (Xml.child xml_arg0 "network") in
      make ?executionStatus ?confirmationStatus ?transactionId
        ?transactionFee ?signatureS ?signatureR ?signatureV
        ?effectiveGasPrice ?cumulativeGasUsed ?gasUsed ?contractAddress ?from
        ?to_ ?numberOfTransactions ?transactionIndex ?transactionTimestamp
        ?blockNumber ?transactionHash ?blockHash ?network ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let executionStatus =
        field_map json__ "executionStatus" ExecutionStatus.of_json in
      let confirmationStatus =
        field_map json__ "confirmationStatus" ConfirmationStatus.of_json in
      let transactionId = field_map json__ "transactionId" String_.of_json in
      let transactionFee = field_map json__ "transactionFee" String_.of_json in
      let signatureS = field_map json__ "signatureS" String_.of_json in
      let signatureR = field_map json__ "signatureR" String_.of_json in
      let signatureV = field_map json__ "signatureV" Integer.of_json in
      let effectiveGasPrice =
        field_map json__ "effectiveGasPrice" String_.of_json in
      let cumulativeGasUsed =
        field_map json__ "cumulativeGasUsed" String_.of_json in
      let gasUsed = field_map json__ "gasUsed" String_.of_json in
      let contractAddress =
        field_map json__ "contractAddress" ChainAddress.of_json in
      let from = field_map json__ "from" ChainAddress.of_json in
      let to_ = field_map json__ "to" ChainAddress.of_json in
      let numberOfTransactions =
        field_map json__ "numberOfTransactions" Long.of_json in
      let transactionIndex = field_map json__ "transactionIndex" Long.of_json in
      let transactionTimestamp =
        field_map json__ "transactionTimestamp" Timestamp.of_json in
      let blockNumber = field_map json__ "blockNumber" String_.of_json in
      let transactionHash =
        field_map json__ "transactionHash" QueryTransactionHash.of_json in
      let blockHash = field_map json__ "blockHash" BlockHash.of_json in
      let network = field_map json__ "network" QueryNetwork.of_json in
      make ?executionStatus ?confirmationStatus ?transactionId
        ?transactionFee ?signatureS ?signatureR ?signatureV
        ?effectiveGasPrice ?cumulativeGasUsed ?gasUsed ?contractAddress ?from
        ?to_ ?numberOfTransactions ?transactionIndex ?transactionTimestamp
        ?blockNumber ?transactionHash ?blockHash ?network ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "There are two possible types of transactions used for this data type: A Bitcoin transaction is a movement of BTC from one address to another. An Ethereum transaction refers to an action initiated by an externally owned account, which is an account managed by a human, not a contract. For example, if Bob sends Alice 1 ETH, Bob's account must be debited and Alice's must be credited. This state-changing action occurs within a transaction."]
module ContractMetadata =
  struct
    type nonrec t =
      {
      name: String_.t option [@ocaml.doc "The name of the token contract."];
      symbol: String_.t option
        [@ocaml.doc "The symbol of the token contract."];
      decimals: Integer.t option
        [@ocaml.doc "The decimals used by the token contract."]}
    let make ?name =
      fun ?symbol -> fun ?decimals -> fun () -> { name; symbol; decimals }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:String_.to_value));
        ("symbol", (Option.map x.symbol ~f:String_.to_value));
        ("decimals", (Option.map x.decimals ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let decimals =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "decimals") in
      let symbol =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "symbol") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "name") in
      make ?decimals ?symbol ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let decimals = field_map json__ "decimals" Integer.of_json in
      let symbol = field_map json__ "symbol" String_.of_json in
      let name = field_map json__ "name" String_.of_json in
      make ?decimals ?symbol ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The metadata of the contract."]
module BatchGetTokenBalanceErrors =
  struct
    type nonrec t = BatchGetTokenBalanceErrorItem.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:BatchGetTokenBalanceErrorItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BatchGetTokenBalanceErrorItem.of_xml)
    let of_json j =
      list_of_json ~kind:"BatchGetTokenBalanceErrors"
        ~of_json:BatchGetTokenBalanceErrorItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module BatchGetTokenBalanceOutputList =
  struct
    type nonrec t = BatchGetTokenBalanceOutputItem.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:BatchGetTokenBalanceOutputItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BatchGetTokenBalanceOutputItem.of_xml)
    let of_json j =
      list_of_json ~kind:"BatchGetTokenBalanceOutputList"
        ~of_json:BatchGetTokenBalanceOutputItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module GetTokenBalanceInputList =
  struct
    type nonrec t = BatchGetTokenBalanceInputItem.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:BatchGetTokenBalanceInputItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BatchGetTokenBalanceInputItem.of_xml)
    let of_json j =
      list_of_json ~kind:"GetTokenBalanceInputList"
        ~of_json:BatchGetTokenBalanceInputItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListTransactionsOutput =
  struct
    type nonrec t =
      {
      transactions: TransactionOutputList.t option
        [@ocaml.doc "The array of transactions returned by the request."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The pagination token that indicates the next set of results to retrieve."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?transactions =
      fun ?nextToken -> fun () -> { transactions; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transactions",
           (Option.map x.transactions ~f:TransactionOutputList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let transactions =
        (Option.map ~f:TransactionOutputList.of_xml)
          (Xml.child xml_arg0 "transactions") in
      make ?nextToken ?transactions ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let transactions =
        field_map json__ "transactions" TransactionOutputList.of_json in
      make ?nextToken ?transactions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists all the transaction events for a transaction."]
module ListTransactionsInput =
  struct
    type nonrec t =
      {
      address: ChainAddress.t
        [@ocaml.doc
          "The address (either a contract or wallet), whose transactions are being requested."];
      network: QueryNetwork.t
        [@ocaml.doc
          "The blockchain network where the transactions occurred."];
      fromBlockchainInstant: BlockchainInstant.t option ;
      toBlockchainInstant: BlockchainInstant.t option ;
      sort: ListTransactionsSort.t option
        [@ocaml.doc "The order by which the results will be sorted."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The pagination token that indicates the next set of results to retrieve."];
      maxResults: ListTransactionsInputMaxResultsInteger.t option
        [@ocaml.doc
          "The maximum number of transactions to list. Default: 100 Even if additional results can be retrieved, the request can return less results than maxResults or an empty array of results. To retrieve the next set of results, make another request with the returned nextToken value. The value of nextToken is null when there are no more results to return"];
      confirmationStatusFilter: ConfirmationStatusFilter.t option
        [@ocaml.doc
          "This filter is used to include transactions in the response that haven't reached finality . Transactions that have reached finality are always part of the response."]}
    let context_ = "ListTransactionsInput"
    let make ?fromBlockchainInstant =
      fun ?toBlockchainInstant ->
        fun ?sort ->
          fun ?nextToken ->
            fun ?maxResults ->
              fun ?confirmationStatusFilter ->
                fun ~address ->
                  fun ~network ->
                    fun () ->
                      {
                        fromBlockchainInstant;
                        toBlockchainInstant;
                        sort;
                        nextToken;
                        maxResults;
                        confirmationStatusFilter;
                        address;
                        network
                      }
    let to_value x =
      structure_to_value
        [("address", (Some (ChainAddress.to_value x.address)));
        ("network", (Some (QueryNetwork.to_value x.network)));
        ("fromBlockchainInstant",
          (Option.map x.fromBlockchainInstant ~f:BlockchainInstant.to_value));
        ("toBlockchainInstant",
          (Option.map x.toBlockchainInstant ~f:BlockchainInstant.to_value));
        ("sort", (Option.map x.sort ~f:ListTransactionsSort.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults",
          (Option.map x.maxResults
             ~f:ListTransactionsInputMaxResultsInteger.to_value));
        ("confirmationStatusFilter",
          (Option.map x.confirmationStatusFilter
             ~f:ConfirmationStatusFilter.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let confirmationStatusFilter =
        (Option.map ~f:ConfirmationStatusFilter.of_xml)
          (Xml.child xml_arg0 "confirmationStatusFilter") in
      let maxResults =
        (Option.map ~f:ListTransactionsInputMaxResultsInteger.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let sort =
        (Option.map ~f:ListTransactionsSort.of_xml)
          (Xml.child xml_arg0 "sort") in
      let toBlockchainInstant =
        (Option.map ~f:BlockchainInstant.of_xml)
          (Xml.child xml_arg0 "toBlockchainInstant") in
      let fromBlockchainInstant =
        (Option.map ~f:BlockchainInstant.of_xml)
          (Xml.child xml_arg0 "fromBlockchainInstant") in
      let network =
        QueryNetwork.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "network") in
      let address =
        ChainAddress.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "address") in
      make ?confirmationStatusFilter ?maxResults ?nextToken ?sort
        ?toBlockchainInstant ?fromBlockchainInstant ~network ~address ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let confirmationStatusFilter =
        field_map json__ "confirmationStatusFilter"
          ConfirmationStatusFilter.of_json in
      let maxResults =
        field_map json__ "maxResults"
          ListTransactionsInputMaxResultsInteger.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let sort = field_map json__ "sort" ListTransactionsSort.of_json in
      let toBlockchainInstant =
        field_map json__ "toBlockchainInstant" BlockchainInstant.of_json in
      let fromBlockchainInstant =
        field_map json__ "fromBlockchainInstant" BlockchainInstant.of_json in
      let network = field_map_exn json__ "network" QueryNetwork.of_json in
      let address = field_map_exn json__ "address" ChainAddress.of_json in
      make ?confirmationStatusFilter ?maxResults ?nextToken ?sort
        ?toBlockchainInstant ?fromBlockchainInstant ~network ~address ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists all the transaction events for a transaction."]
module ListTransactionEventsOutput =
  struct
    type nonrec t =
      {
      events: TransactionEventList.t option
        [@ocaml.doc
          "An array of TransactionEvent objects. Each object contains details about the transaction events."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The pagination token that indicates the next set of results to retrieve."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?events = fun ?nextToken -> fun () -> { events; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("events", (Option.map x.events ~f:TransactionEventList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let events =
        (Option.map ~f:TransactionEventList.of_xml)
          (Xml.child xml_arg0 "events") in
      make ?nextToken ?events ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let events = field_map json__ "events" TransactionEventList.of_json in
      make ?nextToken ?events ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all the transaction events for a transaction This action will return transaction details for all transactions that are confirmed on the blockchain, even if they have not reached finality."]
module ListTransactionEventsInput =
  struct
    type nonrec t =
      {
      transactionHash: QueryTransactionHash.t option
        [@ocaml.doc
          "The hash of a transaction. It is generated when a transaction is created."];
      transactionId: QueryTransactionId.t option
        [@ocaml.doc
          "The identifier of a Bitcoin transaction. It is generated when a transaction is created. transactionId is only supported on the Bitcoin networks."];
      network: QueryNetwork.t
        [@ocaml.doc
          "The blockchain network where the transaction events occurred."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The pagination token that indicates the next set of results to retrieve."];
      maxResults: ListTransactionEventsInputMaxResultsInteger.t option
        [@ocaml.doc
          "The maximum number of transaction events to list. Default: 100 Even if additional results can be retrieved, the request can return less results than maxResults or an empty array of results. To retrieve the next set of results, make another request with the returned nextToken value. The value of nextToken is null when there are no more results to return"]}
    let context_ = "ListTransactionEventsInput"
    let make ?transactionHash =
      fun ?transactionId ->
        fun ?nextToken ->
          fun ?maxResults ->
            fun ~network ->
              fun () ->
                {
                  transactionHash;
                  transactionId;
                  nextToken;
                  maxResults;
                  network
                }
    let to_value x =
      structure_to_value
        [("transactionHash",
           (Option.map x.transactionHash ~f:QueryTransactionHash.to_value));
        ("transactionId",
          (Option.map x.transactionId ~f:QueryTransactionId.to_value));
        ("network", (Some (QueryNetwork.to_value x.network)));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults",
          (Option.map x.maxResults
             ~f:ListTransactionEventsInputMaxResultsInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:ListTransactionEventsInputMaxResultsInteger.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let network =
        QueryNetwork.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "network") in
      let transactionId =
        (Option.map ~f:QueryTransactionId.of_xml)
          (Xml.child xml_arg0 "transactionId") in
      let transactionHash =
        (Option.map ~f:QueryTransactionHash.of_xml)
          (Xml.child xml_arg0 "transactionHash") in
      make ?maxResults ?nextToken ~network ?transactionId ?transactionHash ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxResults =
        field_map json__ "maxResults"
          ListTransactionEventsInputMaxResultsInteger.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let network = field_map_exn json__ "network" QueryNetwork.of_json in
      let transactionId =
        field_map json__ "transactionId" QueryTransactionId.of_json in
      let transactionHash =
        field_map json__ "transactionHash" QueryTransactionHash.of_json in
      make ?maxResults ?nextToken ~network ?transactionId ?transactionHash ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all the transaction events for a transaction This action will return transaction details for all transactions that are confirmed on the blockchain, even if they have not reached finality."]
module ListTokenBalancesOutput =
  struct
    type nonrec t =
      {
      tokenBalances: TokenBalanceList.t option
        [@ocaml.doc
          "An array of TokenBalance objects. Each object contains details about the token balance."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The pagination token that indicates the next set of results to retrieve."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tokenBalances =
      fun ?nextToken -> fun () -> { tokenBalances; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("tokenBalances",
           (Option.map x.tokenBalances ~f:TokenBalanceList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let tokenBalances =
        (Option.map ~f:TokenBalanceList.of_xml)
          (Xml.child xml_arg0 "tokenBalances") in
      make ?nextToken ?tokenBalances ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let tokenBalances =
        field_map json__ "tokenBalances" TokenBalanceList.of_json in
      make ?nextToken ?tokenBalances ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This action returns the following for a given blockchain network: Lists all token balances owned by an address (either a contract address or a wallet address). Lists all token balances for all tokens created by a contract. Lists all token balances for a given token. You must always specify the network property of the tokenFilter when using this operation."]
module ListTokenBalancesInput =
  struct
    type nonrec t =
      {
      ownerFilter: OwnerFilter.t option
        [@ocaml.doc
          "The contract or wallet address on the blockchain network by which to filter the request. You must specify the address property of the ownerFilter when listing balances of tokens owned by the address."];
      tokenFilter: TokenFilter.t
        [@ocaml.doc
          "The contract address or a token identifier on the blockchain network by which to filter the request. You must specify the contractAddress property of this container when listing tokens minted by a contract. You must always specify the network property of this container when using this operation."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The pagination token that indicates the next set of results to retrieve."];
      maxResults: ListTokenBalancesInputMaxResultsInteger.t option
        [@ocaml.doc
          "The maximum number of token balances to return. Default: 100 Even if additional results can be retrieved, the request can return less results than maxResults or an empty array of results. To retrieve the next set of results, make another request with the returned nextToken value. The value of nextToken is null when there are no more results to return"]}
    let context_ = "ListTokenBalancesInput"
    let make ?ownerFilter =
      fun ?nextToken ->
        fun ?maxResults ->
          fun ~tokenFilter ->
            fun () -> { ownerFilter; nextToken; maxResults; tokenFilter }
    let to_value x =
      structure_to_value
        [("ownerFilter", (Option.map x.ownerFilter ~f:OwnerFilter.to_value));
        ("tokenFilter", (Some (TokenFilter.to_value x.tokenFilter)));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults",
          (Option.map x.maxResults
             ~f:ListTokenBalancesInputMaxResultsInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:ListTokenBalancesInputMaxResultsInteger.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let tokenFilter =
        TokenFilter.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "tokenFilter") in
      let ownerFilter =
        (Option.map ~f:OwnerFilter.of_xml) (Xml.child xml_arg0 "ownerFilter") in
      make ?maxResults ?nextToken ~tokenFilter ?ownerFilter ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxResults =
        field_map json__ "maxResults"
          ListTokenBalancesInputMaxResultsInteger.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let tokenFilter =
        field_map_exn json__ "tokenFilter" TokenFilter.of_json in
      let ownerFilter = field_map json__ "ownerFilter" OwnerFilter.of_json in
      make ?maxResults ?nextToken ~tokenFilter ?ownerFilter ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This action returns the following for a given blockchain network: Lists all token balances owned by an address (either a contract address or a wallet address). Lists all token balances for all tokens created by a contract. Lists all token balances for a given token. You must always specify the network property of the tokenFilter when using this operation."]
module ListFilteredTransactionEventsOutput =
  struct
    type nonrec t =
      {
      events: TransactionEventList.t option
        [@ocaml.doc "The transaction events returned by the request."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The pagination token that indicates the next set of results to retrieve."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?events = fun ?nextToken -> fun () -> { events; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("events", (Option.map x.events ~f:TransactionEventList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let events =
        (Option.map ~f:TransactionEventList.of_xml)
          (Xml.child xml_arg0 "events") in
      make ?nextToken ?events ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let events = field_map json__ "events" TransactionEventList.of_json in
      make ?nextToken ?events ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all the transaction events for an address on the blockchain. This operation is only supported on the Bitcoin networks."]
module ListFilteredTransactionEventsInput =
  struct
    type nonrec t =
      {
      network: String_.t
        [@ocaml.doc
          "The blockchain network where the transaction occurred. Valid Values: BITCOIN_MAINNET | BITCOIN_TESTNET"];
      addressIdentifierFilter: AddressIdentifierFilter.t
        [@ocaml.doc
          "This is the unique public address on the blockchain for which the transaction events are being requested."];
      timeFilter: TimeFilter.t option
        [@ocaml.doc
          "This container specifies the time frame for the transaction events returned in the response."];
      voutFilter: VoutFilter.t option
        [@ocaml.doc
          "This container specifies filtering attributes related to BITCOIN_VOUT event types"];
      confirmationStatusFilter: ConfirmationStatusFilter.t option ;
      sort: ListFilteredTransactionEventsSort.t option
        [@ocaml.doc "The order by which the results will be sorted."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The pagination token that indicates the next set of results to retrieve."];
      maxResults:
        ListFilteredTransactionEventsInputMaxResultsInteger.t option
        [@ocaml.doc
          "The maximum number of transaction events to list. Default: 100 Even if additional results can be retrieved, the request can return less results than maxResults or an empty array of results. To retrieve the next set of results, make another request with the returned nextToken value. The value of nextToken is null when there are no more results to return"]}
    let context_ = "ListFilteredTransactionEventsInput"
    let make ?timeFilter =
      fun ?voutFilter ->
        fun ?confirmationStatusFilter ->
          fun ?sort ->
            fun ?nextToken ->
              fun ?maxResults ->
                fun ~network ->
                  fun ~addressIdentifierFilter ->
                    fun () ->
                      {
                        timeFilter;
                        voutFilter;
                        confirmationStatusFilter;
                        sort;
                        nextToken;
                        maxResults;
                        network;
                        addressIdentifierFilter
                      }
    let to_value x =
      structure_to_value
        [("network", (Some (String_.to_value x.network)));
        ("addressIdentifierFilter",
          (Some (AddressIdentifierFilter.to_value x.addressIdentifierFilter)));
        ("timeFilter", (Option.map x.timeFilter ~f:TimeFilter.to_value));
        ("voutFilter", (Option.map x.voutFilter ~f:VoutFilter.to_value));
        ("confirmationStatusFilter",
          (Option.map x.confirmationStatusFilter
             ~f:ConfirmationStatusFilter.to_value));
        ("sort",
          (Option.map x.sort ~f:ListFilteredTransactionEventsSort.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults",
          (Option.map x.maxResults
             ~f:ListFilteredTransactionEventsInputMaxResultsInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map
           ~f:ListFilteredTransactionEventsInputMaxResultsInteger.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let sort =
        (Option.map ~f:ListFilteredTransactionEventsSort.of_xml)
          (Xml.child xml_arg0 "sort") in
      let confirmationStatusFilter =
        (Option.map ~f:ConfirmationStatusFilter.of_xml)
          (Xml.child xml_arg0 "confirmationStatusFilter") in
      let voutFilter =
        (Option.map ~f:VoutFilter.of_xml) (Xml.child xml_arg0 "voutFilter") in
      let timeFilter =
        (Option.map ~f:TimeFilter.of_xml) (Xml.child xml_arg0 "timeFilter") in
      let addressIdentifierFilter =
        AddressIdentifierFilter.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "addressIdentifierFilter") in
      let network =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "network") in
      make ?maxResults ?nextToken ?sort ?confirmationStatusFilter ?voutFilter
        ?timeFilter ~addressIdentifierFilter ~network ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxResults =
        field_map json__ "maxResults"
          ListFilteredTransactionEventsInputMaxResultsInteger.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let sort =
        field_map json__ "sort" ListFilteredTransactionEventsSort.of_json in
      let confirmationStatusFilter =
        field_map json__ "confirmationStatusFilter"
          ConfirmationStatusFilter.of_json in
      let voutFilter = field_map json__ "voutFilter" VoutFilter.of_json in
      let timeFilter = field_map json__ "timeFilter" TimeFilter.of_json in
      let addressIdentifierFilter =
        field_map_exn json__ "addressIdentifierFilter"
          AddressIdentifierFilter.of_json in
      let network = field_map_exn json__ "network" String_.of_json in
      make ?maxResults ?nextToken ?sort ?confirmationStatusFilter ?voutFilter
        ?timeFilter ~addressIdentifierFilter ~network ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all the transaction events for an address on the blockchain. This operation is only supported on the Bitcoin networks."]
module ListAssetContractsOutput =
  struct
    type nonrec t =
      {
      contracts: AssetContractList.t option
        [@ocaml.doc
          "An array of contract objects that contain the properties for each contract."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The pagination token that indicates the next set of results to retrieve."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?contracts =
      fun ?nextToken -> fun () -> { contracts; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("contracts",
           (Option.map x.contracts ~f:AssetContractList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let contracts =
        (Option.map ~f:AssetContractList.of_xml)
          (Xml.child xml_arg0 "contracts") in
      make ?nextToken ?contracts ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let contracts = field_map json__ "contracts" AssetContractList.of_json in
      make ?nextToken ?contracts ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all the contracts for a given contract type deployed by an address (either a contract address or a wallet address). The Bitcoin blockchain networks do not support this operation."]
module ListAssetContractsInput =
  struct
    type nonrec t =
      {
      contractFilter: ContractFilter.t
        [@ocaml.doc "Contains the filter parameter for the request."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The pagination token that indicates the next set of results to retrieve."];
      maxResults: ListAssetContractsInputMaxResultsInteger.t option
        [@ocaml.doc
          "The maximum number of contracts to list. Default: 100 Even if additional results can be retrieved, the request can return less results than maxResults or an empty array of results. To retrieve the next set of results, make another request with the returned nextToken value. The value of nextToken is null when there are no more results to return"]}
    let context_ = "ListAssetContractsInput"
    let make ?nextToken =
      fun ?maxResults ->
        fun ~contractFilter ->
          fun () -> { nextToken; maxResults; contractFilter }
    let to_value x =
      structure_to_value
        [("contractFilter",
           (Some (ContractFilter.to_value x.contractFilter)));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("maxResults",
          (Option.map x.maxResults
             ~f:ListAssetContractsInputMaxResultsInteger.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:ListAssetContractsInputMaxResultsInteger.of_xml)
          (Xml.child xml_arg0 "maxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let contractFilter =
        ContractFilter.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "contractFilter") in
      make ?maxResults ?nextToken ~contractFilter ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxResults =
        field_map json__ "maxResults"
          ListAssetContractsInputMaxResultsInteger.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let contractFilter =
        field_map_exn json__ "contractFilter" ContractFilter.of_json in
      make ?maxResults ?nextToken ~contractFilter ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all the contracts for a given contract type deployed by an address (either a contract address or a wallet address). The Bitcoin blockchain networks do not support this operation."]
module GetTransactionOutput =
  struct
    type nonrec t =
      {
      transaction: Transaction.t option
        [@ocaml.doc "Contains the details of the transaction."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?transaction = fun () -> { transaction }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("transaction", (Option.map x.transaction ~f:Transaction.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let transaction =
        (Option.map ~f:Transaction.of_xml) (Xml.child xml_arg0 "transaction") in
      make ?transaction ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let transaction = field_map json__ "transaction" Transaction.of_json in
      make ?transaction ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets the details of a transaction. This action will return transaction details for all transactions that are confirmed on the blockchain, even if they have not reached finality."]
module GetTransactionInput =
  struct
    type nonrec t =
      {
      transactionHash: QueryTransactionHash.t option
        [@ocaml.doc
          "The hash of a transaction. It is generated when a transaction is created."];
      transactionId: QueryTransactionId.t option
        [@ocaml.doc
          "The identifier of a Bitcoin transaction. It is generated when a transaction is created. transactionId is only supported on the Bitcoin networks."];
      network: QueryNetwork.t
        [@ocaml.doc "The blockchain network where the transaction occurred."]}
    let context_ = "GetTransactionInput"
    let make ?transactionHash =
      fun ?transactionId ->
        fun ~network -> fun () -> { transactionHash; transactionId; network }
    let to_value x =
      structure_to_value
        [("transactionHash",
           (Option.map x.transactionHash ~f:QueryTransactionHash.to_value));
        ("transactionId",
          (Option.map x.transactionId ~f:QueryTransactionId.to_value));
        ("network", (Some (QueryNetwork.to_value x.network)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let network =
        QueryNetwork.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "network") in
      let transactionId =
        (Option.map ~f:QueryTransactionId.of_xml)
          (Xml.child xml_arg0 "transactionId") in
      let transactionHash =
        (Option.map ~f:QueryTransactionHash.of_xml)
          (Xml.child xml_arg0 "transactionHash") in
      make ~network ?transactionId ?transactionHash ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let network = field_map_exn json__ "network" QueryNetwork.of_json in
      let transactionId =
        field_map json__ "transactionId" QueryTransactionId.of_json in
      let transactionHash =
        field_map json__ "transactionHash" QueryTransactionHash.of_json in
      make ~network ?transactionId ?transactionHash ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets the details of a transaction. This action will return transaction details for all transactions that are confirmed on the blockchain, even if they have not reached finality."]
module GetTokenBalanceOutput =
  struct
    type nonrec t =
      {
      ownerIdentifier: OwnerIdentifier.t option ;
      tokenIdentifier: TokenIdentifier.t option ;
      balance: String_.t option
        [@ocaml.doc "The container for the token balance."];
      atBlockchainInstant: BlockchainInstant.t option ;
      lastUpdatedTime: BlockchainInstant.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?ownerIdentifier =
      fun ?tokenIdentifier ->
        fun ?balance ->
          fun ?atBlockchainInstant ->
            fun ?lastUpdatedTime ->
              fun () ->
                {
                  ownerIdentifier;
                  tokenIdentifier;
                  balance;
                  atBlockchainInstant;
                  lastUpdatedTime
                }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ownerIdentifier",
           (Option.map x.ownerIdentifier ~f:OwnerIdentifier.to_value));
        ("tokenIdentifier",
          (Option.map x.tokenIdentifier ~f:TokenIdentifier.to_value));
        ("balance", (Option.map x.balance ~f:String_.to_value));
        ("atBlockchainInstant",
          (Option.map x.atBlockchainInstant ~f:BlockchainInstant.to_value));
        ("lastUpdatedTime",
          (Option.map x.lastUpdatedTime ~f:BlockchainInstant.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdatedTime =
        (Option.map ~f:BlockchainInstant.of_xml)
          (Xml.child xml_arg0 "lastUpdatedTime") in
      let atBlockchainInstant =
        (Option.map ~f:BlockchainInstant.of_xml)
          (Xml.child xml_arg0 "atBlockchainInstant") in
      let balance =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "balance") in
      let tokenIdentifier =
        (Option.map ~f:TokenIdentifier.of_xml)
          (Xml.child xml_arg0 "tokenIdentifier") in
      let ownerIdentifier =
        (Option.map ~f:OwnerIdentifier.of_xml)
          (Xml.child xml_arg0 "ownerIdentifier") in
      make ?lastUpdatedTime ?atBlockchainInstant ?balance ?tokenIdentifier
        ?ownerIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let lastUpdatedTime =
        field_map json__ "lastUpdatedTime" BlockchainInstant.of_json in
      let atBlockchainInstant =
        field_map json__ "atBlockchainInstant" BlockchainInstant.of_json in
      let balance = field_map json__ "balance" String_.of_json in
      let tokenIdentifier =
        field_map json__ "tokenIdentifier" TokenIdentifier.of_json in
      let ownerIdentifier =
        field_map json__ "ownerIdentifier" OwnerIdentifier.of_json in
      make ?lastUpdatedTime ?atBlockchainInstant ?balance ?tokenIdentifier
        ?ownerIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets the balance of a specific token, including native tokens, for a given address (wallet or contract) on the blockchain. Only the native tokens BTC and ETH, and the ERC-20, ERC-721, and ERC 1155 token standards are supported."]
module GetTokenBalanceInput =
  struct
    type nonrec t =
      {
      tokenIdentifier: TokenIdentifier.t
        [@ocaml.doc
          "The container for the identifier for the token, including the unique token ID and its blockchain network."];
      ownerIdentifier: OwnerIdentifier.t
        [@ocaml.doc "The container for the identifier for the owner."];
      atBlockchainInstant: BlockchainInstant.t option
        [@ocaml.doc
          "The time for when the TokenBalance is requested or the current time if a time is not provided in the request. This time will only be recorded up to the second."]}
    let context_ = "GetTokenBalanceInput"
    let make ?atBlockchainInstant =
      fun ~tokenIdentifier ->
        fun ~ownerIdentifier ->
          fun () -> { atBlockchainInstant; tokenIdentifier; ownerIdentifier }
    let to_value x =
      structure_to_value
        [("tokenIdentifier",
           (Some (TokenIdentifier.to_value x.tokenIdentifier)));
        ("ownerIdentifier",
          (Some (OwnerIdentifier.to_value x.ownerIdentifier)));
        ("atBlockchainInstant",
          (Option.map x.atBlockchainInstant ~f:BlockchainInstant.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let atBlockchainInstant =
        (Option.map ~f:BlockchainInstant.of_xml)
          (Xml.child xml_arg0 "atBlockchainInstant") in
      let ownerIdentifier =
        OwnerIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ownerIdentifier") in
      let tokenIdentifier =
        TokenIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "tokenIdentifier") in
      make ?atBlockchainInstant ~ownerIdentifier ~tokenIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let atBlockchainInstant =
        field_map json__ "atBlockchainInstant" BlockchainInstant.of_json in
      let ownerIdentifier =
        field_map_exn json__ "ownerIdentifier" OwnerIdentifier.of_json in
      let tokenIdentifier =
        field_map_exn json__ "tokenIdentifier" TokenIdentifier.of_json in
      make ?atBlockchainInstant ~ownerIdentifier ~tokenIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets the balance of a specific token, including native tokens, for a given address (wallet or contract) on the blockchain. Only the native tokens BTC and ETH, and the ERC-20, ERC-721, and ERC 1155 token standards are supported."]
module GetAssetContractOutput =
  struct
    type nonrec t =
      {
      contractIdentifier: ContractIdentifier.t option
        [@ocaml.doc
          "Contains the blockchain address and network information about the contract."];
      tokenStandard: QueryTokenStandard.t option
        [@ocaml.doc "The token standard of the contract requested."];
      deployerAddress: ChainAddress.t option
        [@ocaml.doc "The address of the deployer of contract."];
      metadata: ContractMetadata.t option }
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?contractIdentifier =
      fun ?tokenStandard ->
        fun ?deployerAddress ->
          fun ?metadata ->
            fun () ->
              { contractIdentifier; tokenStandard; deployerAddress; metadata
              }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("contractIdentifier",
           (Option.map x.contractIdentifier ~f:ContractIdentifier.to_value));
        ("tokenStandard",
          (Option.map x.tokenStandard ~f:QueryTokenStandard.to_value));
        ("deployerAddress",
          (Option.map x.deployerAddress ~f:ChainAddress.to_value));
        ("metadata", (Option.map x.metadata ~f:ContractMetadata.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let metadata =
        (Option.map ~f:ContractMetadata.of_xml)
          (Xml.child xml_arg0 "metadata") in
      let deployerAddress =
        (Option.map ~f:ChainAddress.of_xml)
          (Xml.child xml_arg0 "deployerAddress") in
      let tokenStandard =
        (Option.map ~f:QueryTokenStandard.of_xml)
          (Xml.child xml_arg0 "tokenStandard") in
      let contractIdentifier =
        (Option.map ~f:ContractIdentifier.of_xml)
          (Xml.child xml_arg0 "contractIdentifier") in
      make ?metadata ?deployerAddress ?tokenStandard ?contractIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let metadata = field_map json__ "metadata" ContractMetadata.of_json in
      let deployerAddress =
        field_map json__ "deployerAddress" ChainAddress.of_json in
      let tokenStandard =
        field_map json__ "tokenStandard" QueryTokenStandard.of_json in
      let contractIdentifier =
        field_map json__ "contractIdentifier" ContractIdentifier.of_json in
      make ?metadata ?deployerAddress ?tokenStandard ?contractIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets the information about a specific contract deployed on the blockchain. The Bitcoin blockchain networks do not support this operation. Metadata is currently only available for some ERC-20 contracts. Metadata will be available for additional contracts in the future."]
module GetAssetContractInput =
  struct
    type nonrec t =
      {
      contractIdentifier: ContractIdentifier.t
        [@ocaml.doc
          "Contains the blockchain address and network information about the contract."]}
    let context_ = "GetAssetContractInput"
    let make ~contractIdentifier = fun () -> { contractIdentifier }
    let to_value x =
      structure_to_value
        [("contractIdentifier",
           (Some (ContractIdentifier.to_value x.contractIdentifier)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let contractIdentifier =
        ContractIdentifier.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "contractIdentifier") in
      make ~contractIdentifier ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let contractIdentifier =
        field_map_exn json__ "contractIdentifier" ContractIdentifier.of_json in
      make ~contractIdentifier ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets the information about a specific contract deployed on the blockchain. The Bitcoin blockchain networks do not support this operation. Metadata is currently only available for some ERC-20 contracts. Metadata will be available for additional contracts in the future."]
module BatchGetTokenBalanceOutput =
  struct
    type nonrec t =
      {
      tokenBalances: BatchGetTokenBalanceOutputList.t option
        [@ocaml.doc
          "An array of BatchGetTokenBalanceOutputItem objects returned by the response."];
      errors: BatchGetTokenBalanceErrors.t option
        [@ocaml.doc
          "An array of BatchGetTokenBalanceErrorItem objects returned from the request."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tokenBalances =
      fun ?errors -> fun () -> { tokenBalances; errors }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("tokenBalances",
           (Option.map x.tokenBalances
              ~f:BatchGetTokenBalanceOutputList.to_value));
        ("errors",
          (Option.map x.errors ~f:BatchGetTokenBalanceErrors.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errors =
        (Option.map ~f:BatchGetTokenBalanceErrors.of_xml)
          (Xml.child xml_arg0 "errors") in
      let tokenBalances =
        (Option.map ~f:BatchGetTokenBalanceOutputList.of_xml)
          (Xml.child xml_arg0 "tokenBalances") in
      make ?errors ?tokenBalances ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let errors =
        field_map json__ "errors" BatchGetTokenBalanceErrors.of_json in
      let tokenBalances =
        field_map json__ "tokenBalances"
          BatchGetTokenBalanceOutputList.of_json in
      make ?errors ?tokenBalances ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets the token balance for a batch of tokens by using the BatchGetTokenBalance action for every token in the request. Only the native tokens BTC and ETH, and the ERC-20, ERC-721, and ERC 1155 token standards are supported."]
module BatchGetTokenBalanceInput =
  struct
    type nonrec t =
      {
      getTokenBalanceInputs: GetTokenBalanceInputList.t option
        [@ocaml.doc
          "An array of BatchGetTokenBalanceInputItem objects whose balance is being requested."]}
    let make ?getTokenBalanceInputs = fun () -> { getTokenBalanceInputs }
    let to_value x =
      structure_to_value
        [("getTokenBalanceInputs",
           (Option.map x.getTokenBalanceInputs
              ~f:GetTokenBalanceInputList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let getTokenBalanceInputs =
        (Option.map ~f:GetTokenBalanceInputList.of_xml)
          (Xml.child xml_arg0 "getTokenBalanceInputs") in
      make ?getTokenBalanceInputs ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let getTokenBalanceInputs =
        field_map json__ "getTokenBalanceInputs"
          GetTokenBalanceInputList.of_json in
      make ?getTokenBalanceInputs ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets the token balance for a batch of tokens by using the BatchGetTokenBalance action for every token in the request. Only the native tokens BTC and ETH, and the ERC-20, ERC-721, and ERC 1155 token standards are supported."]
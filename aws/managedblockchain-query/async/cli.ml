(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_get_token_balance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and getTokenBalanceInputs =
         flag "get-token-balance-inputs" (optional json_arg)
           ~doc:"JSON GetTokenBalanceInputList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_token_balance
           (Values.BatchGetTokenBalanceInput.make
              ?getTokenBalanceInputs:(Option.map
                                        ~f:Values.GetTokenBalanceInputList.of_json
                                        getTokenBalanceInputs) ())
           (Some Values.BatchGetTokenBalanceOutput.to_json)
           (Some Values.BatchGetTokenBalanceOutput.error_to_json)])
let get_asset_contract =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contractIdentifier =
         flag "contract-identifier" (required json_arg)
           ~doc:"JSON ContractIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_asset_contract
           (Values.GetAssetContractInput.make
              ~contractIdentifier:(Values.ContractIdentifier.of_json
                                     contractIdentifier) ())
           (Some Values.GetAssetContractOutput.to_json)
           (Some Values.GetAssetContractOutput.error_to_json)])
let get_token_balance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and atBlockchainInstant =
         flag "at-blockchain-instant" (optional json_arg)
           ~doc:"JSON BlockchainInstant"
       and tokenIdentifier =
         flag "token-identifier" (required json_arg)
           ~doc:"JSON TokenIdentifier"
       and ownerIdentifier =
         flag "owner-identifier" (required json_arg)
           ~doc:"JSON OwnerIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_token_balance
           (Values.GetTokenBalanceInput.make
              ?atBlockchainInstant:(Option.map
                                      ~f:Values.BlockchainInstant.of_json
                                      atBlockchainInstant)
              ~tokenIdentifier:(Values.TokenIdentifier.of_json
                                  tokenIdentifier)
              ~ownerIdentifier:(Values.OwnerIdentifier.of_json
                                  ownerIdentifier) ())
           (Some Values.GetTokenBalanceOutput.to_json)
           (Some Values.GetTokenBalanceOutput.error_to_json)])
let get_transaction =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and transactionHash =
         flag "transaction-hash" (optional string)
           ~doc:"STRING QueryTransactionHash"
       and transactionId =
         flag "transaction-id" (optional string)
           ~doc:"STRING QueryTransactionId"
       and network =
         flag "network" (required json_arg) ~doc:"JSON QueryNetwork" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_transaction
           (Values.GetTransactionInput.make ?transactionHash ?transactionId
              ~network:(Values.QueryNetwork.of_json network) ())
           (Some Values.GetTransactionOutput.to_json)
           (Some Values.GetTransactionOutput.error_to_json)])
let list_asset_contracts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListAssetContractsInputMaxResultsInteger"
       and contractFilter =
         flag "contract-filter" (required json_arg)
           ~doc:"JSON ContractFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_asset_contracts
           (Values.ListAssetContractsInput.make ?nextToken ?maxResults
              ~contractFilter:(Values.ContractFilter.of_json contractFilter)
              ()) (Some Values.ListAssetContractsOutput.to_json)
           (Some Values.ListAssetContractsOutput.error_to_json)])
let list_filtered_transaction_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and timeFilter =
         flag "time-filter" (optional json_arg) ~doc:"JSON TimeFilter"
       and voutFilter =
         flag "vout-filter" (optional json_arg) ~doc:"JSON VoutFilter"
       and confirmationStatusFilter =
         flag "confirmation-status-filter" (optional json_arg)
           ~doc:"JSON ConfirmationStatusFilter"
       and sort =
         flag "sort" (optional json_arg)
           ~doc:"JSON ListFilteredTransactionEventsSort"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListFilteredTransactionEventsInputMaxResultsInteger"
       and network = flag "network" (required string) ~doc:"STRING String"
       and addressIdentifierFilter =
         flag "address-identifier-filter" (required json_arg)
           ~doc:"JSON AddressIdentifierFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_filtered_transaction_events
           (Values.ListFilteredTransactionEventsInput.make
              ?timeFilter:(Option.map ~f:Values.TimeFilter.of_json timeFilter)
              ?voutFilter:(Option.map ~f:Values.VoutFilter.of_json voutFilter)
              ?confirmationStatusFilter:(Option.map
                                           ~f:Values.ConfirmationStatusFilter.of_json
                                           confirmationStatusFilter)
              ?sort:(Option.map
                       ~f:Values.ListFilteredTransactionEventsSort.of_json
                       sort) ?nextToken ?maxResults ~network
              ~addressIdentifierFilter:(Values.AddressIdentifierFilter.of_json
                                          addressIdentifierFilter) ())
           (Some Values.ListFilteredTransactionEventsOutput.to_json)
           (Some Values.ListFilteredTransactionEventsOutput.error_to_json)])
let list_token_balances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ownerFilter =
         flag "owner-filter" (optional json_arg) ~doc:"JSON OwnerFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListTokenBalancesInputMaxResultsInteger"
       and tokenFilter =
         flag "token-filter" (required json_arg) ~doc:"JSON TokenFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_token_balances
           (Values.ListTokenBalancesInput.make
              ?ownerFilter:(Option.map ~f:Values.OwnerFilter.of_json
                              ownerFilter) ?nextToken ?maxResults
              ~tokenFilter:(Values.TokenFilter.of_json tokenFilter) ())
           (Some Values.ListTokenBalancesOutput.to_json)
           (Some Values.ListTokenBalancesOutput.error_to_json)])
let list_transaction_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and transactionHash =
         flag "transaction-hash" (optional string)
           ~doc:"STRING QueryTransactionHash"
       and transactionId =
         flag "transaction-id" (optional string)
           ~doc:"STRING QueryTransactionId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListTransactionEventsInputMaxResultsInteger"
       and network =
         flag "network" (required json_arg) ~doc:"JSON QueryNetwork" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_transaction_events
           (Values.ListTransactionEventsInput.make ?transactionHash
              ?transactionId ?nextToken ?maxResults
              ~network:(Values.QueryNetwork.of_json network) ())
           (Some Values.ListTransactionEventsOutput.to_json)
           (Some Values.ListTransactionEventsOutput.error_to_json)])
let list_transactions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fromBlockchainInstant =
         flag "from-blockchain-instant" (optional json_arg)
           ~doc:"JSON BlockchainInstant"
       and toBlockchainInstant =
         flag "to-blockchain-instant" (optional json_arg)
           ~doc:"JSON BlockchainInstant"
       and sort =
         flag "sort" (optional json_arg) ~doc:"JSON ListTransactionsSort"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListTransactionsInputMaxResultsInteger"
       and confirmationStatusFilter =
         flag "confirmation-status-filter" (optional json_arg)
           ~doc:"JSON ConfirmationStatusFilter"
       and address =
         flag "address" (required string) ~doc:"STRING ChainAddress"
       and network =
         flag "network" (required json_arg) ~doc:"JSON QueryNetwork" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_transactions
           (Values.ListTransactionsInput.make
              ?fromBlockchainInstant:(Option.map
                                        ~f:Values.BlockchainInstant.of_json
                                        fromBlockchainInstant)
              ?toBlockchainInstant:(Option.map
                                      ~f:Values.BlockchainInstant.of_json
                                      toBlockchainInstant)
              ?sort:(Option.map ~f:Values.ListTransactionsSort.of_json sort)
              ?nextToken ?maxResults
              ?confirmationStatusFilter:(Option.map
                                           ~f:Values.ConfirmationStatusFilter.of_json
                                           confirmationStatusFilter) ~address
              ~network:(Values.QueryNetwork.of_json network) ())
           (Some Values.ListTransactionsOutput.to_json)
           (Some Values.ListTransactionsOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("batch-get-token-balance", batch_get_token_balance);
    ("get-asset-contract", get_asset_contract);
    ("get-token-balance", get_token_balance);
    ("get-transaction", get_transaction);
    ("list-asset-contracts", list_asset_contracts);
    ("list-filtered-transaction-events", list_filtered_transaction_events);
    ("list-token-balances", list_token_balances);
    ("list-transaction-events", list_transaction_events);
    ("list-transactions", list_transactions)]
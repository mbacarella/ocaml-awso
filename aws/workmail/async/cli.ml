(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_delegate_to_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING EntityIdentifier"
       and entityId =
         flag "entity-id" (required string) ~doc:"STRING EntityIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_delegate_to_resource
           (Values.AssociateDelegateToResourceRequest.make ~organizationId
              ~resourceId ~entityId ())
           (Some Values.AssociateDelegateToResourceResponse.to_json)
           (Some Values.AssociateDelegateToResourceResponse.error_to_json)])
let associate_member_to_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and groupId =
         flag "group-id" (required string) ~doc:"STRING EntityIdentifier"
       and memberId =
         flag "member-id" (required string) ~doc:"STRING EntityIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_member_to_group
           (Values.AssociateMemberToGroupRequest.make ~organizationId
              ~groupId ~memberId ())
           (Some Values.AssociateMemberToGroupResponse.to_json)
           (Some Values.AssociateMemberToGroupResponse.error_to_json)])
let assume_impersonation_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and impersonationRoleId =
         flag "impersonation-role-id" (required string)
           ~doc:"STRING ImpersonationRoleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.assume_impersonation_role
           (Values.AssumeImpersonationRoleRequest.make ~organizationId
              ~impersonationRoleId ())
           (Some Values.AssumeImpersonationRoleResponse.to_json)
           (Some Values.AssumeImpersonationRoleResponse.error_to_json)])
let cancel_mailbox_export_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (required string)
           ~doc:"STRING IdempotencyClientToken"
       and jobId =
         flag "job-id" (required string) ~doc:"STRING MailboxExportJobId"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_mailbox_export_job
           (Values.CancelMailboxExportJobRequest.make ~clientToken ~jobId
              ~organizationId ())
           (Some Values.CancelMailboxExportJobResponse.to_json)
           (Some Values.CancelMailboxExportJobResponse.error_to_json)])
let create_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and entityId =
         flag "entity-id" (required string) ~doc:"STRING WorkMailIdentifier"
       and alias = flag "alias" (required string) ~doc:"STRING EmailAddress" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_alias
           (Values.CreateAliasRequest.make ~organizationId ~entityId ~alias
              ()) (Some Values.CreateAliasResponse.to_json)
           (Some Values.CreateAliasResponse.error_to_json)])
let create_availability_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING IdempotencyClientToken"
       and ewsProvider =
         flag "ews-provider" (optional json_arg)
           ~doc:"JSON EwsAvailabilityProvider"
       and lambdaProvider =
         flag "lambda-provider" (optional json_arg)
           ~doc:"JSON LambdaAvailabilityProvider"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_availability_configuration
           (Values.CreateAvailabilityConfigurationRequest.make ?clientToken
              ?ewsProvider:(Option.map
                              ~f:Values.EwsAvailabilityProvider.of_json
                              ewsProvider)
              ?lambdaProvider:(Option.map
                                 ~f:Values.LambdaAvailabilityProvider.of_json
                                 lambdaProvider) ~organizationId ~domainName
              ())
           (Some Values.CreateAvailabilityConfigurationResponse.to_json)
           (Some Values.CreateAvailabilityConfigurationResponse.error_to_json)])
let create_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hiddenFromGlobalAddressList =
         flag "hidden-from-global-address-list" (optional bool)
           ~doc:"BOOL Boolean"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and name = flag "name" (required string) ~doc:"STRING GroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_group
           (Values.CreateGroupRequest.make ?hiddenFromGlobalAddressList
              ~organizationId ~name ())
           (Some Values.CreateGroupResponse.to_json)
           (Some Values.CreateGroupResponse.error_to_json)])
let create_identity_center_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING IdempotencyClientToken"
       and name =
         flag "name" (required string)
           ~doc:"STRING IdentityCenterApplicationName"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_identity_center_application
           (Values.CreateIdentityCenterApplicationRequest.make ?clientToken
              ~name ~instanceArn ())
           (Some Values.CreateIdentityCenterApplicationResponse.to_json)
           (Some Values.CreateIdentityCenterApplicationResponse.error_to_json)])
let create_impersonation_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING IdempotencyClientToken"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ImpersonationRoleDescription"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and name =
         flag "name" (required string) ~doc:"STRING ImpersonationRoleName"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON ImpersonationRoleType"
       and rules =
         flag "rules" (required json_arg) ~doc:"JSON ImpersonationRuleList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_impersonation_role
           (Values.CreateImpersonationRoleRequest.make ?clientToken
              ?description ~organizationId ~name
              ~type_:(Values.ImpersonationRoleType.of_json type_)
              ~rules:(Values.ImpersonationRuleList.of_json rules) ())
           (Some Values.CreateImpersonationRoleResponse.to_json)
           (Some Values.CreateImpersonationRoleResponse.error_to_json)])
let create_mobile_device_access_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING IdempotencyClientToken"
       and description =
         flag "description" (optional string)
           ~doc:"STRING MobileDeviceAccessRuleDescription"
       and deviceTypes =
         flag "device-types" (optional json_arg) ~doc:"JSON DeviceTypeList"
       and notDeviceTypes =
         flag "not-device-types" (optional json_arg)
           ~doc:"JSON DeviceTypeList"
       and deviceModels =
         flag "device-models" (optional json_arg) ~doc:"JSON DeviceModelList"
       and notDeviceModels =
         flag "not-device-models" (optional json_arg)
           ~doc:"JSON DeviceModelList"
       and deviceOperatingSystems =
         flag "device-operating-systems" (optional json_arg)
           ~doc:"JSON DeviceOperatingSystemList"
       and notDeviceOperatingSystems =
         flag "not-device-operating-systems" (optional json_arg)
           ~doc:"JSON DeviceOperatingSystemList"
       and deviceUserAgents =
         flag "device-user-agents" (optional json_arg)
           ~doc:"JSON DeviceUserAgentList"
       and notDeviceUserAgents =
         flag "not-device-user-agents" (optional json_arg)
           ~doc:"JSON DeviceUserAgentList"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and name =
         flag "name" (required string)
           ~doc:"STRING MobileDeviceAccessRuleName"
       and effect_ =
         flag "effect-" (required json_arg)
           ~doc:"JSON MobileDeviceAccessRuleEffect" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_mobile_device_access_rule
           (Values.CreateMobileDeviceAccessRuleRequest.make ?clientToken
              ?description
              ?deviceTypes:(Option.map ~f:Values.DeviceTypeList.of_json
                              deviceTypes)
              ?notDeviceTypes:(Option.map ~f:Values.DeviceTypeList.of_json
                                 notDeviceTypes)
              ?deviceModels:(Option.map ~f:Values.DeviceModelList.of_json
                               deviceModels)
              ?notDeviceModels:(Option.map ~f:Values.DeviceModelList.of_json
                                  notDeviceModels)
              ?deviceOperatingSystems:(Option.map
                                         ~f:Values.DeviceOperatingSystemList.of_json
                                         deviceOperatingSystems)
              ?notDeviceOperatingSystems:(Option.map
                                            ~f:Values.DeviceOperatingSystemList.of_json
                                            notDeviceOperatingSystems)
              ?deviceUserAgents:(Option.map
                                   ~f:Values.DeviceUserAgentList.of_json
                                   deviceUserAgents)
              ?notDeviceUserAgents:(Option.map
                                      ~f:Values.DeviceUserAgentList.of_json
                                      notDeviceUserAgents) ~organizationId
              ~name
              ~effect_:(Values.MobileDeviceAccessRuleEffect.of_json effect_)
              ()) (Some Values.CreateMobileDeviceAccessRuleResponse.to_json)
           (Some Values.CreateMobileDeviceAccessRuleResponse.error_to_json)])
let create_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and directoryId =
         flag "directory-id" (optional string) ~doc:"STRING DirectoryId"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING IdempotencyClientToken"
       and domains = flag "domains" (optional json_arg) ~doc:"JSON Domains"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and enableInteroperability =
         flag "enable-interoperability" (optional bool) ~doc:"BOOL Boolean"
       and alias =
         flag "alias" (required string) ~doc:"STRING OrganizationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_organization
           (Values.CreateOrganizationRequest.make ?directoryId ?clientToken
              ?domains:(Option.map ~f:Values.Domains.of_json domains)
              ?kmsKeyArn ?enableInteroperability ~alias ())
           (Some Values.CreateOrganizationResponse.to_json)
           (Some Values.CreateOrganizationResponse.error_to_json)])
let create_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ResourceDescription"
       and hiddenFromGlobalAddressList =
         flag "hidden-from-global-address-list" (optional bool)
           ~doc:"BOOL Boolean"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON ResourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_resource
           (Values.CreateResourceRequest.make ?description
              ?hiddenFromGlobalAddressList ~organizationId ~name
              ~type_:(Values.ResourceType.of_json type_) ())
           (Some Values.CreateResourceResponse.to_json)
           (Some Values.CreateResourceResponse.error_to_json)])
let create_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and password =
         flag "password" (optional string) ~doc:"STRING Password"
       and role = flag "role" (optional json_arg) ~doc:"JSON UserRole"
       and firstName =
         flag "first-name" (optional string) ~doc:"STRING UserAttribute"
       and lastName =
         flag "last-name" (optional string) ~doc:"STRING UserAttribute"
       and hiddenFromGlobalAddressList =
         flag "hidden-from-global-address-list" (optional bool)
           ~doc:"BOOL Boolean"
       and identityProviderUserId =
         flag "identity-provider-user-id" (optional string)
           ~doc:"STRING IdentityProviderUserId"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and name = flag "name" (required string) ~doc:"STRING UserName"
       and displayName =
         flag "display-name" (required string) ~doc:"STRING UserAttribute" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_user
           (Values.CreateUserRequest.make ?password
              ?role:(Option.map ~f:Values.UserRole.of_json role) ?firstName
              ?lastName ?hiddenFromGlobalAddressList ?identityProviderUserId
              ~organizationId ~name ~displayName ())
           (Some Values.CreateUserResponse.to_json)
           (Some Values.CreateUserResponse.error_to_json)])
let delete_access_control_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and name =
         flag "name" (required string) ~doc:"STRING AccessControlRuleName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_access_control_rule
           (Values.DeleteAccessControlRuleRequest.make ~organizationId ~name
              ()) (Some Values.DeleteAccessControlRuleResponse.to_json)
           (Some Values.DeleteAccessControlRuleResponse.error_to_json)])
let delete_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and entityId =
         flag "entity-id" (required string) ~doc:"STRING WorkMailIdentifier"
       and alias = flag "alias" (required string) ~doc:"STRING EmailAddress" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_alias
           (Values.DeleteAliasRequest.make ~organizationId ~entityId ~alias
              ()) (Some Values.DeleteAliasResponse.to_json)
           (Some Values.DeleteAliasResponse.error_to_json)])
let delete_availability_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_availability_configuration
           (Values.DeleteAvailabilityConfigurationRequest.make
              ~organizationId ~domainName ())
           (Some Values.DeleteAvailabilityConfigurationResponse.to_json)
           (Some Values.DeleteAvailabilityConfigurationResponse.error_to_json)])
let delete_email_monitoring_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_email_monitoring_configuration
           (Values.DeleteEmailMonitoringConfigurationRequest.make
              ~organizationId ())
           (Some Values.DeleteEmailMonitoringConfigurationResponse.to_json)
           (Some
              Values.DeleteEmailMonitoringConfigurationResponse.error_to_json)])
let delete_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and groupId =
         flag "group-id" (required string) ~doc:"STRING EntityIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_group
           (Values.DeleteGroupRequest.make ~organizationId ~groupId ())
           (Some Values.DeleteGroupResponse.to_json)
           (Some Values.DeleteGroupResponse.error_to_json)])
let delete_identity_center_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationArn =
         flag "application-arn" (required string)
           ~doc:"STRING ApplicationArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_identity_center_application
           (Values.DeleteIdentityCenterApplicationRequest.make
              ~applicationArn ())
           (Some Values.DeleteIdentityCenterApplicationResponse.to_json)
           (Some Values.DeleteIdentityCenterApplicationResponse.error_to_json)])
let delete_identity_provider_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_identity_provider_configuration
           (Values.DeleteIdentityProviderConfigurationRequest.make
              ~organizationId ())
           (Some Values.DeleteIdentityProviderConfigurationResponse.to_json)
           (Some
              Values.DeleteIdentityProviderConfigurationResponse.error_to_json)])
let delete_impersonation_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and impersonationRoleId =
         flag "impersonation-role-id" (required string)
           ~doc:"STRING ImpersonationRoleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_impersonation_role
           (Values.DeleteImpersonationRoleRequest.make ~organizationId
              ~impersonationRoleId ())
           (Some Values.DeleteImpersonationRoleResponse.to_json)
           (Some Values.DeleteImpersonationRoleResponse.error_to_json)])
let delete_mailbox_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and entityId =
         flag "entity-id" (required string) ~doc:"STRING EntityIdentifier"
       and granteeId =
         flag "grantee-id" (required string) ~doc:"STRING EntityIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_mailbox_permissions
           (Values.DeleteMailboxPermissionsRequest.make ~organizationId
              ~entityId ~granteeId ())
           (Some Values.DeleteMailboxPermissionsResponse.to_json)
           (Some Values.DeleteMailboxPermissionsResponse.error_to_json)])
let delete_mobile_device_access_override =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and userId =
         flag "user-id" (required string) ~doc:"STRING EntityIdentifier"
       and deviceId =
         flag "device-id" (required string) ~doc:"STRING DeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_mobile_device_access_override
           (Values.DeleteMobileDeviceAccessOverrideRequest.make
              ~organizationId ~userId ~deviceId ())
           (Some Values.DeleteMobileDeviceAccessOverrideResponse.to_json)
           (Some
              Values.DeleteMobileDeviceAccessOverrideResponse.error_to_json)])
let delete_mobile_device_access_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and mobileDeviceAccessRuleId =
         flag "mobile-device-access-rule-id" (required string)
           ~doc:"STRING MobileDeviceAccessRuleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_mobile_device_access_rule
           (Values.DeleteMobileDeviceAccessRuleRequest.make ~organizationId
              ~mobileDeviceAccessRuleId ())
           (Some Values.DeleteMobileDeviceAccessRuleResponse.to_json)
           (Some Values.DeleteMobileDeviceAccessRuleResponse.error_to_json)])
let delete_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING IdempotencyClientToken"
       and forceDelete =
         flag "force-delete" (optional bool) ~doc:"BOOL Boolean"
       and deleteIdentityCenterApplication =
         flag "delete-identity-center-application" (optional bool)
           ~doc:"BOOL Boolean"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and deleteDirectory =
         flag "delete-directory" (required bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_organization
           (Values.DeleteOrganizationRequest.make ?clientToken ?forceDelete
              ?deleteIdentityCenterApplication ~organizationId
              ~deleteDirectory ())
           (Some Values.DeleteOrganizationResponse.to_json)
           (Some Values.DeleteOrganizationResponse.error_to_json)])
let delete_personal_access_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and personalAccessTokenId =
         flag "personal-access-token-id" (required string)
           ~doc:"STRING PersonalAccessTokenId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_personal_access_token
           (Values.DeletePersonalAccessTokenRequest.make ~organizationId
              ~personalAccessTokenId ())
           (Some Values.DeletePersonalAccessTokenResponse.to_json)
           (Some Values.DeletePersonalAccessTokenResponse.error_to_json)])
let delete_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING EntityIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource
           (Values.DeleteResourceRequest.make ~organizationId ~resourceId ())
           (Some Values.DeleteResourceResponse.to_json)
           (Some Values.DeleteResourceResponse.error_to_json)])
let delete_retention_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and id = flag "id" (required string) ~doc:"STRING ShortString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_retention_policy
           (Values.DeleteRetentionPolicyRequest.make ~organizationId ~id ())
           (Some Values.DeleteRetentionPolicyResponse.to_json)
           (Some Values.DeleteRetentionPolicyResponse.error_to_json)])
let delete_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and userId =
         flag "user-id" (required string) ~doc:"STRING EntityIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_user
           (Values.DeleteUserRequest.make ~organizationId ~userId ())
           (Some Values.DeleteUserResponse.to_json)
           (Some Values.DeleteUserResponse.error_to_json)])
let deregister_from_work_mail =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and entityId =
         flag "entity-id" (required string) ~doc:"STRING EntityIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_from_work_mail
           (Values.DeregisterFromWorkMailRequest.make ~organizationId
              ~entityId ())
           (Some Values.DeregisterFromWorkMailResponse.to_json)
           (Some Values.DeregisterFromWorkMailResponse.error_to_json)])
let deregister_mail_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and domainName =
         flag "domain-name" (required string)
           ~doc:"STRING WorkMailDomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_mail_domain
           (Values.DeregisterMailDomainRequest.make ~organizationId
              ~domainName ())
           (Some Values.DeregisterMailDomainResponse.to_json)
           (Some Values.DeregisterMailDomainResponse.error_to_json)])
let describe_email_monitoring_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_email_monitoring_configuration
           (Values.DescribeEmailMonitoringConfigurationRequest.make
              ~organizationId ())
           (Some Values.DescribeEmailMonitoringConfigurationResponse.to_json)
           (Some
              Values.DescribeEmailMonitoringConfigurationResponse.error_to_json)])
let describe_entity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and email = flag "email" (required string) ~doc:"STRING EmailAddress" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_entity
           (Values.DescribeEntityRequest.make ~organizationId ~email ())
           (Some Values.DescribeEntityResponse.to_json)
           (Some Values.DescribeEntityResponse.error_to_json)])
let describe_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and groupId =
         flag "group-id" (required string) ~doc:"STRING EntityIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_group
           (Values.DescribeGroupRequest.make ~organizationId ~groupId ())
           (Some Values.DescribeGroupResponse.to_json)
           (Some Values.DescribeGroupResponse.error_to_json)])
let describe_identity_provider_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_identity_provider_configuration
           (Values.DescribeIdentityProviderConfigurationRequest.make
              ~organizationId ())
           (Some Values.DescribeIdentityProviderConfigurationResponse.to_json)
           (Some
              Values.DescribeIdentityProviderConfigurationResponse.error_to_json)])
let describe_inbound_dmarc_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_inbound_dmarc_settings
           (Values.DescribeInboundDmarcSettingsRequest.make ~organizationId
              ()) (Some Values.DescribeInboundDmarcSettingsResponse.to_json)
           (Some Values.DescribeInboundDmarcSettingsResponse.error_to_json)])
let describe_mailbox_export_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId =
         flag "job-id" (required string) ~doc:"STRING MailboxExportJobId"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_mailbox_export_job
           (Values.DescribeMailboxExportJobRequest.make ~jobId
              ~organizationId ())
           (Some Values.DescribeMailboxExportJobResponse.to_json)
           (Some Values.DescribeMailboxExportJobResponse.error_to_json)])
let describe_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_organization
           (Values.DescribeOrganizationRequest.make ~organizationId ())
           (Some Values.DescribeOrganizationResponse.to_json)
           (Some Values.DescribeOrganizationResponse.error_to_json)])
let describe_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING EntityIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_resource
           (Values.DescribeResourceRequest.make ~organizationId ~resourceId
              ()) (Some Values.DescribeResourceResponse.to_json)
           (Some Values.DescribeResourceResponse.error_to_json)])
let describe_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and userId =
         flag "user-id" (required string) ~doc:"STRING EntityIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_user
           (Values.DescribeUserRequest.make ~organizationId ~userId ())
           (Some Values.DescribeUserResponse.to_json)
           (Some Values.DescribeUserResponse.error_to_json)])
let disassociate_delegate_from_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING EntityIdentifier"
       and entityId =
         flag "entity-id" (required string) ~doc:"STRING EntityIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_delegate_from_resource
           (Values.DisassociateDelegateFromResourceRequest.make
              ~organizationId ~resourceId ~entityId ())
           (Some Values.DisassociateDelegateFromResourceResponse.to_json)
           (Some
              Values.DisassociateDelegateFromResourceResponse.error_to_json)])
let disassociate_member_from_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and groupId =
         flag "group-id" (required string) ~doc:"STRING EntityIdentifier"
       and memberId =
         flag "member-id" (required string) ~doc:"STRING EntityIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_member_from_group
           (Values.DisassociateMemberFromGroupRequest.make ~organizationId
              ~groupId ~memberId ())
           (Some Values.DisassociateMemberFromGroupResponse.to_json)
           (Some Values.DisassociateMemberFromGroupResponse.error_to_json)])
let get_access_control_effect =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userId =
         flag "user-id" (optional string) ~doc:"STRING WorkMailIdentifier"
       and impersonationRoleId =
         flag "impersonation-role-id" (optional string)
           ~doc:"STRING ImpersonationRoleId"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and ipAddress =
         flag "ip-address" (required string) ~doc:"STRING IpAddress"
       and action =
         flag "action" (required string)
           ~doc:"STRING AccessControlRuleAction" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_access_control_effect
           (Values.GetAccessControlEffectRequest.make ?userId
              ?impersonationRoleId ~organizationId ~ipAddress ~action ())
           (Some Values.GetAccessControlEffectResponse.to_json)
           (Some Values.GetAccessControlEffectResponse.error_to_json)])
let get_default_retention_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_default_retention_policy
           (Values.GetDefaultRetentionPolicyRequest.make ~organizationId ())
           (Some Values.GetDefaultRetentionPolicyResponse.to_json)
           (Some Values.GetDefaultRetentionPolicyResponse.error_to_json)])
let get_impersonation_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and impersonationRoleId =
         flag "impersonation-role-id" (required string)
           ~doc:"STRING ImpersonationRoleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_impersonation_role
           (Values.GetImpersonationRoleRequest.make ~organizationId
              ~impersonationRoleId ())
           (Some Values.GetImpersonationRoleResponse.to_json)
           (Some Values.GetImpersonationRoleResponse.error_to_json)])
let get_impersonation_role_effect =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and impersonationRoleId =
         flag "impersonation-role-id" (required string)
           ~doc:"STRING ImpersonationRoleId"
       and targetUser =
         flag "target-user" (required string) ~doc:"STRING EntityIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_impersonation_role_effect
           (Values.GetImpersonationRoleEffectRequest.make ~organizationId
              ~impersonationRoleId ~targetUser ())
           (Some Values.GetImpersonationRoleEffectResponse.to_json)
           (Some Values.GetImpersonationRoleEffectResponse.error_to_json)])
let get_mail_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and domainName =
         flag "domain-name" (required string)
           ~doc:"STRING WorkMailDomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_mail_domain
           (Values.GetMailDomainRequest.make ~organizationId ~domainName ())
           (Some Values.GetMailDomainResponse.to_json)
           (Some Values.GetMailDomainResponse.error_to_json)])
let get_mailbox_details =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and userId =
         flag "user-id" (required string) ~doc:"STRING EntityIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_mailbox_details
           (Values.GetMailboxDetailsRequest.make ~organizationId ~userId ())
           (Some Values.GetMailboxDetailsResponse.to_json)
           (Some Values.GetMailboxDetailsResponse.error_to_json)])
let get_mobile_device_access_effect =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceType =
         flag "device-type" (optional string) ~doc:"STRING DeviceType"
       and deviceModel =
         flag "device-model" (optional string) ~doc:"STRING DeviceModel"
       and deviceOperatingSystem =
         flag "device-operating-system" (optional string)
           ~doc:"STRING DeviceOperatingSystem"
       and deviceUserAgent =
         flag "device-user-agent" (optional string)
           ~doc:"STRING DeviceUserAgent"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_mobile_device_access_effect
           (Values.GetMobileDeviceAccessEffectRequest.make ?deviceType
              ?deviceModel ?deviceOperatingSystem ?deviceUserAgent
              ~organizationId ())
           (Some Values.GetMobileDeviceAccessEffectResponse.to_json)
           (Some Values.GetMobileDeviceAccessEffectResponse.error_to_json)])
let get_mobile_device_access_override =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and userId =
         flag "user-id" (required string) ~doc:"STRING EntityIdentifier"
       and deviceId =
         flag "device-id" (required string) ~doc:"STRING DeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_mobile_device_access_override
           (Values.GetMobileDeviceAccessOverrideRequest.make ~organizationId
              ~userId ~deviceId ())
           (Some Values.GetMobileDeviceAccessOverrideResponse.to_json)
           (Some Values.GetMobileDeviceAccessOverrideResponse.error_to_json)])
let get_personal_access_token_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and personalAccessTokenId =
         flag "personal-access-token-id" (required string)
           ~doc:"STRING PersonalAccessTokenId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_personal_access_token_metadata
           (Values.GetPersonalAccessTokenMetadataRequest.make ~organizationId
              ~personalAccessTokenId ())
           (Some Values.GetPersonalAccessTokenMetadataResponse.to_json)
           (Some Values.GetPersonalAccessTokenMetadataResponse.error_to_json)])
let list_access_control_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_access_control_rules
           (Values.ListAccessControlRulesRequest.make ~organizationId ())
           (Some Values.ListAccessControlRulesResponse.to_json)
           (Some Values.ListAccessControlRulesResponse.error_to_json)])
let list_aliases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and entityId =
         flag "entity-id" (required string) ~doc:"STRING WorkMailIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_aliases
           (Values.ListAliasesRequest.make ?nextToken ?maxResults
              ~organizationId ~entityId ())
           (Some Values.ListAliasesResponse.to_json)
           (Some Values.ListAliasesResponse.error_to_json)])
let list_availability_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_availability_configurations
           (Values.ListAvailabilityConfigurationsRequest.make ?maxResults
              ?nextToken ~organizationId ())
           (Some Values.ListAvailabilityConfigurationsResponse.to_json)
           (Some Values.ListAvailabilityConfigurationsResponse.error_to_json)])
let list_group_members =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and groupId =
         flag "group-id" (required string) ~doc:"STRING EntityIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_group_members
           (Values.ListGroupMembersRequest.make ?nextToken ?maxResults
              ~organizationId ~groupId ())
           (Some Values.ListGroupMembersResponse.to_json)
           (Some Values.ListGroupMembersResponse.error_to_json)])
let list_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON ListGroupsFilters"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_groups
           (Values.ListGroupsRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.ListGroupsFilters.of_json
                          filters) ~organizationId ())
           (Some Values.ListGroupsResponse.to_json)
           (Some Values.ListGroupsResponse.error_to_json)])
let list_groups_for_entity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON ListGroupsForEntityFilters"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and entityId =
         flag "entity-id" (required string) ~doc:"STRING EntityIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_groups_for_entity
           (Values.ListGroupsForEntityRequest.make
              ?filters:(Option.map
                          ~f:Values.ListGroupsForEntityFilters.of_json
                          filters) ?nextToken ?maxResults ~organizationId
              ~entityId ()) (Some Values.ListGroupsForEntityResponse.to_json)
           (Some Values.ListGroupsForEntityResponse.error_to_json)])
let list_impersonation_roles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_impersonation_roles
           (Values.ListImpersonationRolesRequest.make ?nextToken ?maxResults
              ~organizationId ())
           (Some Values.ListImpersonationRolesResponse.to_json)
           (Some Values.ListImpersonationRolesResponse.error_to_json)])
let list_mail_domains =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_mail_domains
           (Values.ListMailDomainsRequest.make ?maxResults ?nextToken
              ~organizationId ())
           (Some Values.ListMailDomainsResponse.to_json)
           (Some Values.ListMailDomainsResponse.error_to_json)])
let list_mailbox_export_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_mailbox_export_jobs
           (Values.ListMailboxExportJobsRequest.make ?nextToken ?maxResults
              ~organizationId ())
           (Some Values.ListMailboxExportJobsResponse.to_json)
           (Some Values.ListMailboxExportJobsResponse.error_to_json)])
let list_mailbox_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and entityId =
         flag "entity-id" (required string) ~doc:"STRING EntityIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_mailbox_permissions
           (Values.ListMailboxPermissionsRequest.make ?nextToken ?maxResults
              ~organizationId ~entityId ())
           (Some Values.ListMailboxPermissionsResponse.to_json)
           (Some Values.ListMailboxPermissionsResponse.error_to_json)])
let list_mobile_device_access_overrides =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userId =
         flag "user-id" (optional string) ~doc:"STRING EntityIdentifier"
       and deviceId =
         flag "device-id" (optional string) ~doc:"STRING DeviceId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_mobile_device_access_overrides
           (Values.ListMobileDeviceAccessOverridesRequest.make ?userId
              ?deviceId ?nextToken ?maxResults ~organizationId ())
           (Some Values.ListMobileDeviceAccessOverridesResponse.to_json)
           (Some Values.ListMobileDeviceAccessOverridesResponse.error_to_json)])
let list_mobile_device_access_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_mobile_device_access_rules
           (Values.ListMobileDeviceAccessRulesRequest.make ~organizationId ())
           (Some Values.ListMobileDeviceAccessRulesResponse.to_json)
           (Some Values.ListMobileDeviceAccessRulesResponse.error_to_json)])
let list_organizations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_organizations
           (Values.ListOrganizationsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListOrganizationsResponse.to_json)
           (Some Values.ListOrganizationsResponse.error_to_json)])
let list_personal_access_tokens =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userId =
         flag "user-id" (optional string) ~doc:"STRING EntityIdentifier"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_personal_access_tokens
           (Values.ListPersonalAccessTokensRequest.make ?userId ?nextToken
              ?maxResults ~organizationId ())
           (Some Values.ListPersonalAccessTokensResponse.to_json)
           (Some Values.ListPersonalAccessTokensResponse.error_to_json)])
let list_resource_delegates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING EntityIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resource_delegates
           (Values.ListResourceDelegatesRequest.make ?nextToken ?maxResults
              ~organizationId ~resourceId ())
           (Some Values.ListResourceDelegatesResponse.to_json)
           (Some Values.ListResourceDelegatesResponse.error_to_json)])
let list_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON ListResourcesFilters"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resources
           (Values.ListResourcesRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.ListResourcesFilters.of_json
                          filters) ~organizationId ())
           (Some Values.ListResourcesResponse.to_json)
           (Some Values.ListResourcesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_users =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON ListUsersFilters"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_users
           (Values.ListUsersRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.ListUsersFilters.of_json filters)
              ~organizationId ()) (Some Values.ListUsersResponse.to_json)
           (Some Values.ListUsersResponse.error_to_json)])
let put_access_control_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ipRanges =
         flag "ip-ranges" (optional json_arg) ~doc:"JSON IpRangeList"
       and notIpRanges =
         flag "not-ip-ranges" (optional json_arg) ~doc:"JSON IpRangeList"
       and actions =
         flag "actions" (optional json_arg) ~doc:"JSON ActionsList"
       and notActions =
         flag "not-actions" (optional json_arg) ~doc:"JSON ActionsList"
       and userIds =
         flag "user-ids" (optional json_arg) ~doc:"JSON UserIdList"
       and notUserIds =
         flag "not-user-ids" (optional json_arg) ~doc:"JSON UserIdList"
       and impersonationRoleIds =
         flag "impersonation-role-ids" (optional json_arg)
           ~doc:"JSON ImpersonationRoleIdList"
       and notImpersonationRoleIds =
         flag "not-impersonation-role-ids" (optional json_arg)
           ~doc:"JSON ImpersonationRoleIdList"
       and name =
         flag "name" (required string) ~doc:"STRING AccessControlRuleName"
       and effect_ =
         flag "effect-" (required json_arg)
           ~doc:"JSON AccessControlRuleEffect"
       and description =
         flag "description" (required string)
           ~doc:"STRING AccessControlRuleDescription"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_access_control_rule
           (Values.PutAccessControlRuleRequest.make
              ?ipRanges:(Option.map ~f:Values.IpRangeList.of_json ipRanges)
              ?notIpRanges:(Option.map ~f:Values.IpRangeList.of_json
                              notIpRanges)
              ?actions:(Option.map ~f:Values.ActionsList.of_json actions)
              ?notActions:(Option.map ~f:Values.ActionsList.of_json
                             notActions)
              ?userIds:(Option.map ~f:Values.UserIdList.of_json userIds)
              ?notUserIds:(Option.map ~f:Values.UserIdList.of_json notUserIds)
              ?impersonationRoleIds:(Option.map
                                       ~f:Values.ImpersonationRoleIdList.of_json
                                       impersonationRoleIds)
              ?notImpersonationRoleIds:(Option.map
                                          ~f:Values.ImpersonationRoleIdList.of_json
                                          notImpersonationRoleIds) ~name
              ~effect_:(Values.AccessControlRuleEffect.of_json effect_)
              ~description ~organizationId ())
           (Some Values.PutAccessControlRuleResponse.to_json)
           (Some Values.PutAccessControlRuleResponse.error_to_json)])
let put_email_monitoring_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and logGroupArn =
         flag "log-group-arn" (required string) ~doc:"STRING LogGroupArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_email_monitoring_configuration
           (Values.PutEmailMonitoringConfigurationRequest.make ?roleArn
              ~organizationId ~logGroupArn ())
           (Some Values.PutEmailMonitoringConfigurationResponse.to_json)
           (Some Values.PutEmailMonitoringConfigurationResponse.error_to_json)])
let put_identity_provider_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and authenticationMode =
         flag "authentication-mode" (required json_arg)
           ~doc:"JSON IdentityProviderAuthenticationMode"
       and identityCenterConfiguration =
         flag "identity-center-configuration" (required json_arg)
           ~doc:"JSON IdentityCenterConfiguration"
       and personalAccessTokenConfiguration =
         flag "personal-access-token-configuration" (required json_arg)
           ~doc:"JSON PersonalAccessTokenConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_identity_provider_configuration
           (Values.PutIdentityProviderConfigurationRequest.make
              ~organizationId
              ~authenticationMode:(Values.IdentityProviderAuthenticationMode.of_json
                                     authenticationMode)
              ~identityCenterConfiguration:(Values.IdentityCenterConfiguration.of_json
                                              identityCenterConfiguration)
              ~personalAccessTokenConfiguration:(Values.PersonalAccessTokenConfiguration.of_json
                                                   personalAccessTokenConfiguration)
              ())
           (Some Values.PutIdentityProviderConfigurationResponse.to_json)
           (Some
              Values.PutIdentityProviderConfigurationResponse.error_to_json)])
let put_inbound_dmarc_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and enforced =
         flag "enforced" (required bool) ~doc:"BOOL BooleanObject" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_inbound_dmarc_settings
           (Values.PutInboundDmarcSettingsRequest.make ~organizationId
              ~enforced ())
           (Some Values.PutInboundDmarcSettingsResponse.to_json)
           (Some Values.PutInboundDmarcSettingsResponse.error_to_json)])
let put_mailbox_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and entityId =
         flag "entity-id" (required string) ~doc:"STRING EntityIdentifier"
       and granteeId =
         flag "grantee-id" (required string) ~doc:"STRING EntityIdentifier"
       and permissionValues =
         flag "permission-values" (required json_arg)
           ~doc:"JSON PermissionValues" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_mailbox_permissions
           (Values.PutMailboxPermissionsRequest.make ~organizationId
              ~entityId ~granteeId
              ~permissionValues:(Values.PermissionValues.of_json
                                   permissionValues) ())
           (Some Values.PutMailboxPermissionsResponse.to_json)
           (Some Values.PutMailboxPermissionsResponse.error_to_json)])
let put_mobile_device_access_override =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING MobileDeviceAccessRuleDescription"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and userId =
         flag "user-id" (required string) ~doc:"STRING EntityIdentifier"
       and deviceId =
         flag "device-id" (required string) ~doc:"STRING DeviceId"
       and effect_ =
         flag "effect-" (required json_arg)
           ~doc:"JSON MobileDeviceAccessRuleEffect" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_mobile_device_access_override
           (Values.PutMobileDeviceAccessOverrideRequest.make ?description
              ~organizationId ~userId ~deviceId
              ~effect_:(Values.MobileDeviceAccessRuleEffect.of_json effect_)
              ()) (Some Values.PutMobileDeviceAccessOverrideResponse.to_json)
           (Some Values.PutMobileDeviceAccessOverrideResponse.error_to_json)])
let put_retention_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (optional string) ~doc:"STRING ShortString"
       and description =
         flag "description" (optional string) ~doc:"STRING PolicyDescription"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and name = flag "name" (required string) ~doc:"STRING ShortString"
       and folderConfigurations =
         flag "folder-configurations" (required json_arg)
           ~doc:"JSON FolderConfigurations" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_retention_policy
           (Values.PutRetentionPolicyRequest.make ?id ?description
              ~organizationId ~name
              ~folderConfigurations:(Values.FolderConfigurations.of_json
                                       folderConfigurations) ())
           (Some Values.PutRetentionPolicyResponse.to_json)
           (Some Values.PutRetentionPolicyResponse.error_to_json)])
let register_mail_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING IdempotencyClientToken"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and domainName =
         flag "domain-name" (required string)
           ~doc:"STRING WorkMailDomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_mail_domain
           (Values.RegisterMailDomainRequest.make ?clientToken
              ~organizationId ~domainName ())
           (Some Values.RegisterMailDomainResponse.to_json)
           (Some Values.RegisterMailDomainResponse.error_to_json)])
let register_to_work_mail =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and entityId =
         flag "entity-id" (required string) ~doc:"STRING EntityIdentifier"
       and email = flag "email" (required string) ~doc:"STRING EmailAddress" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_to_work_mail
           (Values.RegisterToWorkMailRequest.make ~organizationId ~entityId
              ~email ()) (Some Values.RegisterToWorkMailResponse.to_json)
           (Some Values.RegisterToWorkMailResponse.error_to_json)])
let reset_password =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and userId =
         flag "user-id" (required string) ~doc:"STRING WorkMailIdentifier"
       and password =
         flag "password" (required string) ~doc:"STRING Password" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reset_password
           (Values.ResetPasswordRequest.make ~organizationId ~userId
              ~password ()) (Some Values.ResetPasswordResponse.to_json)
           (Some Values.ResetPasswordResponse.error_to_json)])
let start_mailbox_export_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and clientToken =
         flag "client-token" (required string)
           ~doc:"STRING IdempotencyClientToken"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and entityId =
         flag "entity-id" (required string) ~doc:"STRING EntityIdentifier"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn"
       and kmsKeyArn =
         flag "kms-key-arn" (required string) ~doc:"STRING KmsKeyArn"
       and s3BucketName =
         flag "s3-bucket-name" (required string) ~doc:"STRING S3BucketName"
       and s3Prefix =
         flag "s3-prefix" (required string) ~doc:"STRING S3ObjectKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_mailbox_export_job
           (Values.StartMailboxExportJobRequest.make ?description
              ~clientToken ~organizationId ~entityId ~roleArn ~kmsKeyArn
              ~s3BucketName ~s3Prefix ())
           (Some Values.StartMailboxExportJobResponse.to_json)
           (Some Values.StartMailboxExportJobResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let test_availability_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (optional string) ~doc:"STRING DomainName"
       and ewsProvider =
         flag "ews-provider" (optional json_arg)
           ~doc:"JSON EwsAvailabilityProvider"
       and lambdaProvider =
         flag "lambda-provider" (optional json_arg)
           ~doc:"JSON LambdaAvailabilityProvider"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.test_availability_configuration
           (Values.TestAvailabilityConfigurationRequest.make ?domainName
              ?ewsProvider:(Option.map
                              ~f:Values.EwsAvailabilityProvider.of_json
                              ewsProvider)
              ?lambdaProvider:(Option.map
                                 ~f:Values.LambdaAvailabilityProvider.of_json
                                 lambdaProvider) ~organizationId ())
           (Some Values.TestAvailabilityConfigurationResponse.to_json)
           (Some Values.TestAvailabilityConfigurationResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_availability_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ewsProvider =
         flag "ews-provider" (optional json_arg)
           ~doc:"JSON EwsAvailabilityProvider"
       and lambdaProvider =
         flag "lambda-provider" (optional json_arg)
           ~doc:"JSON LambdaAvailabilityProvider"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_availability_configuration
           (Values.UpdateAvailabilityConfigurationRequest.make
              ?ewsProvider:(Option.map
                              ~f:Values.EwsAvailabilityProvider.of_json
                              ewsProvider)
              ?lambdaProvider:(Option.map
                                 ~f:Values.LambdaAvailabilityProvider.of_json
                                 lambdaProvider) ~organizationId ~domainName
              ())
           (Some Values.UpdateAvailabilityConfigurationResponse.to_json)
           (Some Values.UpdateAvailabilityConfigurationResponse.error_to_json)])
let update_default_mail_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and domainName =
         flag "domain-name" (required string)
           ~doc:"STRING WorkMailDomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_default_mail_domain
           (Values.UpdateDefaultMailDomainRequest.make ~organizationId
              ~domainName ())
           (Some Values.UpdateDefaultMailDomainResponse.to_json)
           (Some Values.UpdateDefaultMailDomainResponse.error_to_json)])
let update_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hiddenFromGlobalAddressList =
         flag "hidden-from-global-address-list" (optional bool)
           ~doc:"BOOL BooleanObject"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and groupId =
         flag "group-id" (required string) ~doc:"STRING EntityIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_group
           (Values.UpdateGroupRequest.make ?hiddenFromGlobalAddressList
              ~organizationId ~groupId ())
           (Some Values.UpdateGroupResponse.to_json)
           (Some Values.UpdateGroupResponse.error_to_json)])
let update_impersonation_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ImpersonationRoleDescription"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and impersonationRoleId =
         flag "impersonation-role-id" (required string)
           ~doc:"STRING ImpersonationRoleId"
       and name =
         flag "name" (required string) ~doc:"STRING ImpersonationRoleName"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON ImpersonationRoleType"
       and rules =
         flag "rules" (required json_arg) ~doc:"JSON ImpersonationRuleList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_impersonation_role
           (Values.UpdateImpersonationRoleRequest.make ?description
              ~organizationId ~impersonationRoleId ~name
              ~type_:(Values.ImpersonationRoleType.of_json type_)
              ~rules:(Values.ImpersonationRuleList.of_json rules) ())
           (Some Values.UpdateImpersonationRoleResponse.to_json)
           (Some Values.UpdateImpersonationRoleResponse.error_to_json)])
let update_mailbox_quota =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and userId =
         flag "user-id" (required string) ~doc:"STRING EntityIdentifier"
       and mailboxQuota =
         flag "mailbox-quota" (required int) ~doc:"INT MailboxQuota" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_mailbox_quota
           (Values.UpdateMailboxQuotaRequest.make ~organizationId ~userId
              ~mailboxQuota ())
           (Some Values.UpdateMailboxQuotaResponse.to_json)
           (Some Values.UpdateMailboxQuotaResponse.error_to_json)])
let update_mobile_device_access_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING MobileDeviceAccessRuleDescription"
       and deviceTypes =
         flag "device-types" (optional json_arg) ~doc:"JSON DeviceTypeList"
       and notDeviceTypes =
         flag "not-device-types" (optional json_arg)
           ~doc:"JSON DeviceTypeList"
       and deviceModels =
         flag "device-models" (optional json_arg) ~doc:"JSON DeviceModelList"
       and notDeviceModels =
         flag "not-device-models" (optional json_arg)
           ~doc:"JSON DeviceModelList"
       and deviceOperatingSystems =
         flag "device-operating-systems" (optional json_arg)
           ~doc:"JSON DeviceOperatingSystemList"
       and notDeviceOperatingSystems =
         flag "not-device-operating-systems" (optional json_arg)
           ~doc:"JSON DeviceOperatingSystemList"
       and deviceUserAgents =
         flag "device-user-agents" (optional json_arg)
           ~doc:"JSON DeviceUserAgentList"
       and notDeviceUserAgents =
         flag "not-device-user-agents" (optional json_arg)
           ~doc:"JSON DeviceUserAgentList"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and mobileDeviceAccessRuleId =
         flag "mobile-device-access-rule-id" (required string)
           ~doc:"STRING MobileDeviceAccessRuleId"
       and name =
         flag "name" (required string)
           ~doc:"STRING MobileDeviceAccessRuleName"
       and effect_ =
         flag "effect-" (required json_arg)
           ~doc:"JSON MobileDeviceAccessRuleEffect" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_mobile_device_access_rule
           (Values.UpdateMobileDeviceAccessRuleRequest.make ?description
              ?deviceTypes:(Option.map ~f:Values.DeviceTypeList.of_json
                              deviceTypes)
              ?notDeviceTypes:(Option.map ~f:Values.DeviceTypeList.of_json
                                 notDeviceTypes)
              ?deviceModels:(Option.map ~f:Values.DeviceModelList.of_json
                               deviceModels)
              ?notDeviceModels:(Option.map ~f:Values.DeviceModelList.of_json
                                  notDeviceModels)
              ?deviceOperatingSystems:(Option.map
                                         ~f:Values.DeviceOperatingSystemList.of_json
                                         deviceOperatingSystems)
              ?notDeviceOperatingSystems:(Option.map
                                            ~f:Values.DeviceOperatingSystemList.of_json
                                            notDeviceOperatingSystems)
              ?deviceUserAgents:(Option.map
                                   ~f:Values.DeviceUserAgentList.of_json
                                   deviceUserAgents)
              ?notDeviceUserAgents:(Option.map
                                      ~f:Values.DeviceUserAgentList.of_json
                                      notDeviceUserAgents) ~organizationId
              ~mobileDeviceAccessRuleId ~name
              ~effect_:(Values.MobileDeviceAccessRuleEffect.of_json effect_)
              ()) (Some Values.UpdateMobileDeviceAccessRuleResponse.to_json)
           (Some Values.UpdateMobileDeviceAccessRuleResponse.error_to_json)])
let update_primary_email_address =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and entityId =
         flag "entity-id" (required string) ~doc:"STRING EntityIdentifier"
       and email = flag "email" (required string) ~doc:"STRING EmailAddress" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_primary_email_address
           (Values.UpdatePrimaryEmailAddressRequest.make ~organizationId
              ~entityId ~email ())
           (Some Values.UpdatePrimaryEmailAddressResponse.to_json)
           (Some Values.UpdatePrimaryEmailAddressResponse.error_to_json)])
let update_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING ResourceName"
       and bookingOptions =
         flag "booking-options" (optional json_arg)
           ~doc:"JSON BookingOptions"
       and description =
         flag "description" (optional string)
           ~doc:"STRING NewResourceDescription"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON ResourceType"
       and hiddenFromGlobalAddressList =
         flag "hidden-from-global-address-list" (optional bool)
           ~doc:"BOOL BooleanObject"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING EntityIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_resource
           (Values.UpdateResourceRequest.make ?name
              ?bookingOptions:(Option.map ~f:Values.BookingOptions.of_json
                                 bookingOptions) ?description
              ?type_:(Option.map ~f:Values.ResourceType.of_json type_)
              ?hiddenFromGlobalAddressList ~organizationId ~resourceId ())
           (Some Values.UpdateResourceResponse.to_json)
           (Some Values.UpdateResourceResponse.error_to_json)])
let update_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and role = flag "role" (optional json_arg) ~doc:"JSON UserRole"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING UserAttribute"
       and firstName =
         flag "first-name" (optional string) ~doc:"STRING UserAttribute"
       and lastName =
         flag "last-name" (optional string) ~doc:"STRING UserAttribute"
       and hiddenFromGlobalAddressList =
         flag "hidden-from-global-address-list" (optional bool)
           ~doc:"BOOL BooleanObject"
       and initials =
         flag "initials" (optional string) ~doc:"STRING UserAttribute"
       and telephone =
         flag "telephone" (optional string) ~doc:"STRING UserAttribute"
       and street =
         flag "street" (optional string) ~doc:"STRING UserAttribute"
       and jobTitle =
         flag "job-title" (optional string) ~doc:"STRING UserAttribute"
       and city = flag "city" (optional string) ~doc:"STRING UserAttribute"
       and company =
         flag "company" (optional string) ~doc:"STRING UserAttribute"
       and zipCode =
         flag "zip-code" (optional string) ~doc:"STRING UserAttribute"
       and department =
         flag "department" (optional string) ~doc:"STRING UserAttribute"
       and country =
         flag "country" (optional string) ~doc:"STRING UserAttribute"
       and office =
         flag "office" (optional string) ~doc:"STRING UserAttribute"
       and identityProviderUserId =
         flag "identity-provider-user-id" (optional string)
           ~doc:"STRING IdentityProviderUserIdForUpdate"
       and organizationId =
         flag "organization-id" (required string)
           ~doc:"STRING OrganizationId"
       and userId =
         flag "user-id" (required string) ~doc:"STRING EntityIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user
           (Values.UpdateUserRequest.make
              ?role:(Option.map ~f:Values.UserRole.of_json role) ?displayName
              ?firstName ?lastName ?hiddenFromGlobalAddressList ?initials
              ?telephone ?street ?jobTitle ?city ?company ?zipCode
              ?department ?country ?office ?identityProviderUserId
              ~organizationId ~userId ())
           (Some Values.UpdateUserResponse.to_json)
           (Some Values.UpdateUserResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-delegate-to-resource", associate_delegate_to_resource);
    ("associate-member-to-group", associate_member_to_group);
    ("assume-impersonation-role", assume_impersonation_role);
    ("cancel-mailbox-export-job", cancel_mailbox_export_job);
    ("create-alias", create_alias);
    ("create-availability-configuration", create_availability_configuration);
    ("create-group", create_group);
    ("create-identity-center-application",
      create_identity_center_application);
    ("create-impersonation-role", create_impersonation_role);
    ("create-mobile-device-access-rule", create_mobile_device_access_rule);
    ("create-organization", create_organization);
    ("create-resource", create_resource);
    ("create-user", create_user);
    ("delete-access-control-rule", delete_access_control_rule);
    ("delete-alias", delete_alias);
    ("delete-availability-configuration", delete_availability_configuration);
    ("delete-email-monitoring-configuration",
      delete_email_monitoring_configuration);
    ("delete-group", delete_group);
    ("delete-identity-center-application",
      delete_identity_center_application);
    ("delete-identity-provider-configuration",
      delete_identity_provider_configuration);
    ("delete-impersonation-role", delete_impersonation_role);
    ("delete-mailbox-permissions", delete_mailbox_permissions);
    ("delete-mobile-device-access-override",
      delete_mobile_device_access_override);
    ("delete-mobile-device-access-rule", delete_mobile_device_access_rule);
    ("delete-organization", delete_organization);
    ("delete-personal-access-token", delete_personal_access_token);
    ("delete-resource", delete_resource);
    ("delete-retention-policy", delete_retention_policy);
    ("delete-user", delete_user);
    ("deregister-from-work-mail", deregister_from_work_mail);
    ("deregister-mail-domain", deregister_mail_domain);
    ("describe-email-monitoring-configuration",
      describe_email_monitoring_configuration);
    ("describe-entity", describe_entity);
    ("describe-group", describe_group);
    ("describe-identity-provider-configuration",
      describe_identity_provider_configuration);
    ("describe-inbound-dmarc-settings", describe_inbound_dmarc_settings);
    ("describe-mailbox-export-job", describe_mailbox_export_job);
    ("describe-organization", describe_organization);
    ("describe-resource", describe_resource);
    ("describe-user", describe_user);
    ("disassociate-delegate-from-resource",
      disassociate_delegate_from_resource);
    ("disassociate-member-from-group", disassociate_member_from_group);
    ("get-access-control-effect", get_access_control_effect);
    ("get-default-retention-policy", get_default_retention_policy);
    ("get-impersonation-role", get_impersonation_role);
    ("get-impersonation-role-effect", get_impersonation_role_effect);
    ("get-mail-domain", get_mail_domain);
    ("get-mailbox-details", get_mailbox_details);
    ("get-mobile-device-access-effect", get_mobile_device_access_effect);
    ("get-mobile-device-access-override", get_mobile_device_access_override);
    ("get-personal-access-token-metadata",
      get_personal_access_token_metadata);
    ("list-access-control-rules", list_access_control_rules);
    ("list-aliases", list_aliases);
    ("list-availability-configurations", list_availability_configurations);
    ("list-group-members", list_group_members);
    ("list-groups", list_groups);
    ("list-groups-for-entity", list_groups_for_entity);
    ("list-impersonation-roles", list_impersonation_roles);
    ("list-mail-domains", list_mail_domains);
    ("list-mailbox-export-jobs", list_mailbox_export_jobs);
    ("list-mailbox-permissions", list_mailbox_permissions);
    ("list-mobile-device-access-overrides",
      list_mobile_device_access_overrides);
    ("list-mobile-device-access-rules", list_mobile_device_access_rules);
    ("list-organizations", list_organizations);
    ("list-personal-access-tokens", list_personal_access_tokens);
    ("list-resource-delegates", list_resource_delegates);
    ("list-resources", list_resources);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-users", list_users);
    ("put-access-control-rule", put_access_control_rule);
    ("put-email-monitoring-configuration",
      put_email_monitoring_configuration);
    ("put-identity-provider-configuration",
      put_identity_provider_configuration);
    ("put-inbound-dmarc-settings", put_inbound_dmarc_settings);
    ("put-mailbox-permissions", put_mailbox_permissions);
    ("put-mobile-device-access-override", put_mobile_device_access_override);
    ("put-retention-policy", put_retention_policy);
    ("register-mail-domain", register_mail_domain);
    ("register-to-work-mail", register_to_work_mail);
    ("reset-password", reset_password);
    ("start-mailbox-export-job", start_mailbox_export_job);
    ("tag-resource", tag_resource);
    ("test-availability-configuration", test_availability_configuration);
    ("untag-resource", untag_resource);
    ("update-availability-configuration", update_availability_configuration);
    ("update-default-mail-domain", update_default_mail_domain);
    ("update-group", update_group);
    ("update-impersonation-role", update_impersonation_role);
    ("update-mailbox-quota", update_mailbox_quota);
    ("update-mobile-device-access-rule", update_mobile_device_access_rule);
    ("update-primary-email-address", update_primary_email_address);
    ("update-resource", update_resource);
    ("update-user", update_user)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_changeset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and datasetId =
         flag "dataset-id" (required string) ~doc:"STRING DatasetId"
       and changeType =
         flag "change-type" (required json_arg) ~doc:"JSON ChangeType"
       and sourceParams =
         flag "source-params" (required json_arg) ~doc:"JSON SourceParams"
       and formatParams =
         flag "format-params" (required json_arg) ~doc:"JSON FormatParams" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_changeset
           (Values.CreateChangesetRequest.make ?clientToken ~datasetId
              ~changeType:(Values.ChangeType.of_json changeType)
              ~sourceParams:(Values.SourceParams.of_json sourceParams)
              ~formatParams:(Values.FormatParams.of_json formatParams) ())
           (Some Values.CreateChangesetResponse.to_json)
           (Some Values.CreateChangesetResponse.error_to_json)])
let create_data_view =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and autoUpdate =
         flag "auto-update" (optional bool) ~doc:"BOOL Boolean"
       and sortColumns =
         flag "sort-columns" (optional json_arg) ~doc:"JSON SortColumnList"
       and partitionColumns =
         flag "partition-columns" (optional json_arg)
           ~doc:"JSON PartitionColumnList"
       and asOfTimestamp =
         flag "as-of-timestamp" (optional json_arg)
           ~doc:"JSON TimestampEpoch"
       and datasetId =
         flag "dataset-id" (required string) ~doc:"STRING DatasetId"
       and destinationTypeParams =
         flag "destination-type-params" (required json_arg)
           ~doc:"JSON DataViewDestinationTypeParams" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_view
           (Values.CreateDataViewRequest.make ?clientToken ?autoUpdate
              ?sortColumns:(Option.map ~f:Values.SortColumnList.of_json
                              sortColumns)
              ?partitionColumns:(Option.map
                                   ~f:Values.PartitionColumnList.of_json
                                   partitionColumns)
              ?asOfTimestamp:(Option.map ~f:Values.TimestampEpoch.of_json
                                asOfTimestamp) ~datasetId
              ~destinationTypeParams:(Values.DataViewDestinationTypeParams.of_json
                                        destinationTypeParams) ())
           (Some Values.CreateDataViewResponse.to_json)
           (Some Values.CreateDataViewResponse.error_to_json)])
let create_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and datasetDescription =
         flag "dataset-description" (optional string)
           ~doc:"STRING DatasetDescription"
       and ownerInfo =
         flag "owner-info" (optional json_arg) ~doc:"JSON DatasetOwnerInfo"
       and alias = flag "alias" (optional string) ~doc:"STRING AliasString"
       and schemaDefinition =
         flag "schema-definition" (optional json_arg) ~doc:"JSON SchemaUnion"
       and datasetTitle =
         flag "dataset-title" (required string) ~doc:"STRING DatasetTitle"
       and kind = flag "kind" (required json_arg) ~doc:"JSON DatasetKind"
       and permissionGroupParams =
         flag "permission-group-params" (required json_arg)
           ~doc:"JSON PermissionGroupParams" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_dataset
           (Values.CreateDatasetRequest.make ?clientToken ?datasetDescription
              ?ownerInfo:(Option.map ~f:Values.DatasetOwnerInfo.of_json
                            ownerInfo) ?alias
              ?schemaDefinition:(Option.map ~f:Values.SchemaUnion.of_json
                                   schemaDefinition) ~datasetTitle
              ~kind:(Values.DatasetKind.of_json kind)
              ~permissionGroupParams:(Values.PermissionGroupParams.of_json
                                        permissionGroupParams) ())
           (Some Values.CreateDatasetResponse.to_json)
           (Some Values.CreateDatasetResponse.error_to_json)])
let create_permission_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING PermissionGroupDescription"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and name =
         flag "name" (required string) ~doc:"STRING PermissionGroupName"
       and applicationPermissions =
         flag "application-permissions" (required json_arg)
           ~doc:"JSON ApplicationPermissionList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_permission_group
           (Values.CreatePermissionGroupRequest.make ?description
              ?clientToken ~name
              ~applicationPermissions:(Values.ApplicationPermissionList.of_json
                                         applicationPermissions) ())
           (Some Values.CreatePermissionGroupResponse.to_json)
           (Some Values.CreatePermissionGroupResponse.error_to_json)])
let create_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and firstName =
         flag "first-name" (optional string) ~doc:"STRING FirstName"
       and lastName =
         flag "last-name" (optional string) ~doc:"STRING LastName"
       and apiAccess =
         flag "api-access" (optional json_arg) ~doc:"JSON ApiAccess"
       and apiAccessPrincipalArn =
         flag "api-access-principal-arn" (optional string)
           ~doc:"STRING RoleArn"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and emailAddress =
         flag "email-address" (required string) ~doc:"STRING Email"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON UserType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_user
           (Values.CreateUserRequest.make ?firstName ?lastName
              ?apiAccess:(Option.map ~f:Values.ApiAccess.of_json apiAccess)
              ?apiAccessPrincipalArn ?clientToken ~emailAddress
              ~type_:(Values.UserType.of_json type_) ())
           (Some Values.CreateUserResponse.to_json)
           (Some Values.CreateUserResponse.error_to_json)])
let delete_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and datasetId =
         flag "dataset-id" (required string) ~doc:"STRING DatasetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_dataset
           (Values.DeleteDatasetRequest.make ?clientToken ~datasetId ())
           (Some Values.DeleteDatasetResponse.to_json)
           (Some Values.DeleteDatasetResponse.error_to_json)])
let delete_permission_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and permissionGroupId =
         flag "permission-group-id" (required string)
           ~doc:"STRING PermissionGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_permission_group
           (Values.DeletePermissionGroupRequest.make ?clientToken
              ~permissionGroupId ())
           (Some Values.DeletePermissionGroupResponse.to_json)
           (Some Values.DeletePermissionGroupResponse.error_to_json)])
let disable_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and userId = flag "user-id" (required string) ~doc:"STRING UserId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_user
           (Values.DisableUserRequest.make ?clientToken ~userId ())
           (Some Values.DisableUserResponse.to_json)
           (Some Values.DisableUserResponse.error_to_json)])
let enable_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and userId = flag "user-id" (required string) ~doc:"STRING UserId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_user
           (Values.EnableUserRequest.make ?clientToken ~userId ())
           (Some Values.EnableUserResponse.to_json)
           (Some Values.EnableUserResponse.error_to_json)])
let get_changeset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetId =
         flag "dataset-id" (required string) ~doc:"STRING DatasetId"
       and changesetId =
         flag "changeset-id" (required string) ~doc:"STRING ChangesetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_changeset
           (Values.GetChangesetRequest.make ~datasetId ~changesetId ())
           (Some Values.GetChangesetResponse.to_json)
           (Some Values.GetChangesetResponse.error_to_json)])
let get_data_view =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataViewId =
         flag "data-view-id" (required string) ~doc:"STRING DataViewId"
       and datasetId =
         flag "dataset-id" (required string) ~doc:"STRING DatasetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_view
           (Values.GetDataViewRequest.make ~dataViewId ~datasetId ())
           (Some Values.GetDataViewResponse.to_json)
           (Some Values.GetDataViewResponse.error_to_json)])
let get_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datasetId =
         flag "dataset-id" (required string)
           ~doc:"STRING StringValueLength1to255" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_dataset (Values.GetDatasetRequest.make ~datasetId ())
           (Some Values.GetDatasetResponse.to_json)
           (Some Values.GetDatasetResponse.error_to_json)])
let get_programmatic_access_credentials =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and durationInMinutes =
         flag "duration-in-minutes" (optional json_arg)
           ~doc:"JSON SessionDuration"
       and environmentId =
         flag "environment-id" (required string) ~doc:"STRING IdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_programmatic_access_credentials
           (Values.GetProgrammaticAccessCredentialsRequest.make
              ?durationInMinutes:(Option.map
                                    ~f:Values.SessionDuration.of_json
                                    durationInMinutes) ~environmentId ())
           (Some Values.GetProgrammaticAccessCredentialsResponse.to_json)
           (Some
              Values.GetProgrammaticAccessCredentialsResponse.error_to_json)])
let get_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userId = flag "user-id" (required string) ~doc:"STRING UserId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_user (Values.GetUserRequest.make ~userId ())
           (Some Values.GetUserResponse.to_json)
           (Some Values.GetUserResponse.error_to_json)])
let get_working_location =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and locationType =
         flag "location-type" (optional json_arg) ~doc:"JSON locationType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_working_location
           (Values.GetWorkingLocationRequest.make
              ?locationType:(Option.map ~f:Values.LocationType.of_json
                               locationType) ())
           (Some Values.GetWorkingLocationResponse.to_json)
           (Some Values.GetWorkingLocationResponse.error_to_json)])
let list_changesets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultLimit"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and datasetId =
         flag "dataset-id" (required string) ~doc:"STRING DatasetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_changesets
           (Values.ListChangesetsRequest.make ?maxResults ?nextToken
              ~datasetId ()) (Some Values.ListChangesetsResponse.to_json)
           (Some Values.ListChangesetsResponse.error_to_json)])
let list_data_views =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultLimit"
       and datasetId =
         flag "dataset-id" (required string) ~doc:"STRING DatasetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_data_views
           (Values.ListDataViewsRequest.make ?nextToken ?maxResults
              ~datasetId ()) (Some Values.ListDataViewsResponse.to_json)
           (Some Values.ListDataViewsResponse.error_to_json)])
let list_datasets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_datasets
           (Values.ListDatasetsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListDatasetsResponse.to_json)
           (Some Values.ListDatasetsResponse.error_to_json)])
let list_permission_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (required int) ~doc:"INT ResultLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_permission_groups
           (Values.ListPermissionGroupsRequest.make ?nextToken ~maxResults ())
           (Some Values.ListPermissionGroupsResponse.to_json)
           (Some Values.ListPermissionGroupsResponse.error_to_json)])
let list_users =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (required int) ~doc:"INT ResultLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_users
           (Values.ListUsersRequest.make ?nextToken ~maxResults ())
           (Some Values.ListUsersResponse.to_json)
           (Some Values.ListUsersResponse.error_to_json)])
let reset_user_password =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and userId = flag "user-id" (required string) ~doc:"STRING UserId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reset_user_password
           (Values.ResetUserPasswordRequest.make ?clientToken ~userId ())
           (Some Values.ResetUserPasswordResponse.to_json)
           (Some Values.ResetUserPasswordResponse.error_to_json)])
let update_changeset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and datasetId =
         flag "dataset-id" (required string) ~doc:"STRING DatasetId"
       and changesetId =
         flag "changeset-id" (required string) ~doc:"STRING ChangesetId"
       and sourceParams =
         flag "source-params" (required json_arg) ~doc:"JSON SourceParams"
       and formatParams =
         flag "format-params" (required json_arg) ~doc:"JSON FormatParams" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_changeset
           (Values.UpdateChangesetRequest.make ?clientToken ~datasetId
              ~changesetId
              ~sourceParams:(Values.SourceParams.of_json sourceParams)
              ~formatParams:(Values.FormatParams.of_json formatParams) ())
           (Some Values.UpdateChangesetResponse.to_json)
           (Some Values.UpdateChangesetResponse.error_to_json)])
let update_dataset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and datasetDescription =
         flag "dataset-description" (optional string)
           ~doc:"STRING DatasetDescription"
       and alias = flag "alias" (optional string) ~doc:"STRING AliasString"
       and schemaDefinition =
         flag "schema-definition" (optional json_arg) ~doc:"JSON SchemaUnion"
       and datasetId =
         flag "dataset-id" (required string) ~doc:"STRING DatasetId"
       and datasetTitle =
         flag "dataset-title" (required string) ~doc:"STRING DatasetTitle"
       and kind = flag "kind" (required json_arg) ~doc:"JSON DatasetKind" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_dataset
           (Values.UpdateDatasetRequest.make ?clientToken ?datasetDescription
              ?alias
              ?schemaDefinition:(Option.map ~f:Values.SchemaUnion.of_json
                                   schemaDefinition) ~datasetId ~datasetTitle
              ~kind:(Values.DatasetKind.of_json kind) ())
           (Some Values.UpdateDatasetResponse.to_json)
           (Some Values.UpdateDatasetResponse.error_to_json)])
let update_permission_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING PermissionGroupName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING PermissionGroupDescription"
       and applicationPermissions =
         flag "application-permissions" (optional json_arg)
           ~doc:"JSON ApplicationPermissionList"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and permissionGroupId =
         flag "permission-group-id" (required string)
           ~doc:"STRING PermissionGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_permission_group
           (Values.UpdatePermissionGroupRequest.make ?name ?description
              ?applicationPermissions:(Option.map
                                         ~f:Values.ApplicationPermissionList.of_json
                                         applicationPermissions) ?clientToken
              ~permissionGroupId ())
           (Some Values.UpdatePermissionGroupResponse.to_json)
           (Some Values.UpdatePermissionGroupResponse.error_to_json)])
let update_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON UserType"
       and firstName =
         flag "first-name" (optional string) ~doc:"STRING FirstName"
       and lastName =
         flag "last-name" (optional string) ~doc:"STRING LastName"
       and apiAccess =
         flag "api-access" (optional json_arg) ~doc:"JSON ApiAccess"
       and apiAccessPrincipalArn =
         flag "api-access-principal-arn" (optional string)
           ~doc:"STRING RoleArn"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and userId = flag "user-id" (required string) ~doc:"STRING UserId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user
           (Values.UpdateUserRequest.make
              ?type_:(Option.map ~f:Values.UserType.of_json type_) ?firstName
              ?lastName
              ?apiAccess:(Option.map ~f:Values.ApiAccess.of_json apiAccess)
              ?apiAccessPrincipalArn ?clientToken ~userId ())
           (Some Values.UpdateUserResponse.to_json)
           (Some Values.UpdateUserResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-changeset", create_changeset);
    ("create-data-view", create_data_view);
    ("create-dataset", create_dataset);
    ("create-permission-group", create_permission_group);
    ("create-user", create_user);
    ("delete-dataset", delete_dataset);
    ("delete-permission-group", delete_permission_group);
    ("disable-user", disable_user);
    ("enable-user", enable_user);
    ("get-changeset", get_changeset);
    ("get-data-view", get_data_view);
    ("get-dataset", get_dataset);
    ("get-programmatic-access-credentials",
      get_programmatic_access_credentials);
    ("get-user", get_user);
    ("get-working-location", get_working_location);
    ("list-changesets", list_changesets);
    ("list-data-views", list_data_views);
    ("list-datasets", list_datasets);
    ("list-permission-groups", list_permission_groups);
    ("list-users", list_users);
    ("reset-user-password", reset_user_password);
    ("update-changeset", update_changeset);
    ("update-dataset", update_dataset);
    ("update-permission-group", update_permission_group);
    ("update-user", update_user)]
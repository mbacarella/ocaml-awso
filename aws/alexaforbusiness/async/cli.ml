(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let approve_skill =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and skillId = flag "skill-id" (required string) ~doc:"STRING SkillId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.approve_skill (Values.ApproveSkillRequest.make ~skillId ())
           (Some Values.ApproveSkillResponse.to_json)
           (Some Values.ApproveSkillResponse.error_to_json)])
let associate_contact_with_address_book =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contactArn =
         flag "contact-arn" (required string) ~doc:"STRING Arn"
       and addressBookArn =
         flag "address-book-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_contact_with_address_book
           (Values.AssociateContactWithAddressBookRequest.make ~contactArn
              ~addressBookArn ())
           (Some Values.AssociateContactWithAddressBookResponse.to_json)
           (Some Values.AssociateContactWithAddressBookResponse.error_to_json)])
let associate_device_with_network_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceArn = flag "device-arn" (required string) ~doc:"STRING Arn"
       and networkProfileArn =
         flag "network-profile-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_device_with_network_profile
           (Values.AssociateDeviceWithNetworkProfileRequest.make ~deviceArn
              ~networkProfileArn ())
           (Some Values.AssociateDeviceWithNetworkProfileResponse.to_json)
           (Some
              Values.AssociateDeviceWithNetworkProfileResponse.error_to_json)])
let associate_device_with_room =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceArn = flag "device-arn" (optional string) ~doc:"STRING Arn"
       and roomArn = flag "room-arn" (optional string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_device_with_room
           (Values.AssociateDeviceWithRoomRequest.make ?deviceArn ?roomArn ())
           (Some Values.AssociateDeviceWithRoomResponse.to_json)
           (Some Values.AssociateDeviceWithRoomResponse.error_to_json)])
let associate_skill_group_with_room =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and skillGroupArn =
         flag "skill-group-arn" (optional string) ~doc:"STRING Arn"
       and roomArn = flag "room-arn" (optional string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_skill_group_with_room
           (Values.AssociateSkillGroupWithRoomRequest.make ?skillGroupArn
              ?roomArn ())
           (Some Values.AssociateSkillGroupWithRoomResponse.to_json)
           (Some Values.AssociateSkillGroupWithRoomResponse.error_to_json)])
let associate_skill_with_skill_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and skillGroupArn =
         flag "skill-group-arn" (optional string) ~doc:"STRING Arn"
       and skillId = flag "skill-id" (required string) ~doc:"STRING SkillId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_skill_with_skill_group
           (Values.AssociateSkillWithSkillGroupRequest.make ?skillGroupArn
              ~skillId ())
           (Some Values.AssociateSkillWithSkillGroupResponse.to_json)
           (Some Values.AssociateSkillWithSkillGroupResponse.error_to_json)])
let associate_skill_with_users =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and skillId = flag "skill-id" (required string) ~doc:"STRING SkillId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_skill_with_users
           (Values.AssociateSkillWithUsersRequest.make ~skillId ())
           (Some Values.AssociateSkillWithUsersResponse.to_json)
           (Some Values.AssociateSkillWithUsersResponse.error_to_json)])
let create_address_book =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING AddressBookDescription"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name = flag "name" (required string) ~doc:"STRING AddressBookName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_address_book
           (Values.CreateAddressBookRequest.make ?description
              ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name ())
           (Some Values.CreateAddressBookResponse.to_json)
           (Some Values.CreateAddressBookResponse.error_to_json)])
let create_business_report_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scheduleName =
         flag "schedule-name" (optional string)
           ~doc:"STRING BusinessReportScheduleName"
       and s3BucketName =
         flag "s3-bucket-name" (optional string)
           ~doc:"STRING CustomerS3BucketName"
       and s3KeyPrefix =
         flag "s3-key-prefix" (optional string) ~doc:"STRING S3KeyPrefix"
       and recurrence =
         flag "recurrence" (optional json_arg)
           ~doc:"JSON BusinessReportRecurrence"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and format =
         flag "format" (required json_arg) ~doc:"JSON BusinessReportFormat"
       and contentRange =
         flag "content-range" (required json_arg)
           ~doc:"JSON BusinessReportContentRange" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_business_report_schedule
           (Values.CreateBusinessReportScheduleRequest.make ?scheduleName
              ?s3BucketName ?s3KeyPrefix
              ?recurrence:(Option.map
                             ~f:Values.BusinessReportRecurrence.of_json
                             recurrence) ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~format:(Values.BusinessReportFormat.of_json format)
              ~contentRange:(Values.BusinessReportContentRange.of_json
                               contentRange) ())
           (Some Values.CreateBusinessReportScheduleResponse.to_json)
           (Some Values.CreateBusinessReportScheduleResponse.error_to_json)])
let create_conference_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and iPDialIn =
         flag "i-p-dial-in" (optional json_arg) ~doc:"JSON IPDialIn"
       and pSTNDialIn =
         flag "p-s-t-n-dial-in" (optional json_arg) ~doc:"JSON PSTNDialIn"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and conferenceProviderName =
         flag "conference-provider-name" (required string)
           ~doc:"STRING ConferenceProviderName"
       and conferenceProviderType =
         flag "conference-provider-type" (required json_arg)
           ~doc:"JSON ConferenceProviderType"
       and meetingSetting =
         flag "meeting-setting" (required json_arg)
           ~doc:"JSON MeetingSetting" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_conference_provider
           (Values.CreateConferenceProviderRequest.make
              ?iPDialIn:(Option.map ~f:Values.IPDialIn.of_json iPDialIn)
              ?pSTNDialIn:(Option.map ~f:Values.PSTNDialIn.of_json pSTNDialIn)
              ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~conferenceProviderName
              ~conferenceProviderType:(Values.ConferenceProviderType.of_json
                                         conferenceProviderType)
              ~meetingSetting:(Values.MeetingSetting.of_json meetingSetting)
              ()) (Some Values.CreateConferenceProviderResponse.to_json)
           (Some Values.CreateConferenceProviderResponse.error_to_json)])
let create_contact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING ContactName"
       and lastName =
         flag "last-name" (optional string) ~doc:"STRING ContactName"
       and phoneNumber =
         flag "phone-number" (optional string) ~doc:"STRING RawPhoneNumber"
       and phoneNumbers =
         flag "phone-numbers" (optional json_arg) ~doc:"JSON PhoneNumberList"
       and sipAddresses =
         flag "sip-addresses" (optional json_arg) ~doc:"JSON SipAddressList"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and firstName =
         flag "first-name" (required string) ~doc:"STRING ContactName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_contact
           (Values.CreateContactRequest.make ?displayName ?lastName
              ?phoneNumber
              ?phoneNumbers:(Option.map ~f:Values.PhoneNumberList.of_json
                               phoneNumbers)
              ?sipAddresses:(Option.map ~f:Values.SipAddressList.of_json
                               sipAddresses) ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~firstName ())
           (Some Values.CreateContactResponse.to_json)
           (Some Values.CreateContactResponse.error_to_json)])
let create_gateway_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING GatewayGroupDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name =
         flag "name" (required string) ~doc:"STRING GatewayGroupName"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_gateway_group
           (Values.CreateGatewayGroupRequest.make ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name
              ~clientRequestToken ())
           (Some Values.CreateGatewayGroupResponse.to_json)
           (Some Values.CreateGatewayGroupResponse.error_to_json)])
let create_network_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING NetworkProfileDescription"
       and eapMethod =
         flag "eap-method" (optional json_arg) ~doc:"JSON NetworkEapMethod"
       and currentPassword =
         flag "current-password" (optional string)
           ~doc:"STRING CurrentWiFiPassword"
       and nextPassword =
         flag "next-password" (optional string)
           ~doc:"STRING NextWiFiPassword"
       and certificateAuthorityArn =
         flag "certificate-authority-arn" (optional string) ~doc:"STRING Arn"
       and trustAnchors =
         flag "trust-anchors" (optional json_arg) ~doc:"JSON TrustAnchorList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and networkProfileName =
         flag "network-profile-name" (required string)
           ~doc:"STRING NetworkProfileName"
       and ssid = flag "ssid" (required string) ~doc:"STRING NetworkSsid"
       and securityType =
         flag "security-type" (required json_arg)
           ~doc:"JSON NetworkSecurityType"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_network_profile
           (Values.CreateNetworkProfileRequest.make ?description
              ?eapMethod:(Option.map ~f:Values.NetworkEapMethod.of_json
                            eapMethod) ?currentPassword ?nextPassword
              ?certificateAuthorityArn
              ?trustAnchors:(Option.map ~f:Values.TrustAnchorList.of_json
                               trustAnchors)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~networkProfileName ~ssid
              ~securityType:(Values.NetworkSecurityType.of_json securityType)
              ~clientRequestToken ())
           (Some Values.CreateNetworkProfileResponse.to_json)
           (Some Values.CreateNetworkProfileResponse.error_to_json)])
let create_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and locale =
         flag "locale" (optional string) ~doc:"STRING DeviceLocale"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and setupModeDisabled =
         flag "setup-mode-disabled" (optional bool) ~doc:"BOOL Boolean"
       and maxVolumeLimit =
         flag "max-volume-limit" (optional int) ~doc:"INT MaxVolumeLimit"
       and pSTNEnabled =
         flag "p-s-t-n-enabled" (optional bool) ~doc:"BOOL Boolean"
       and dataRetentionOptIn =
         flag "data-retention-opt-in" (optional bool) ~doc:"BOOL Boolean"
       and meetingRoomConfiguration =
         flag "meeting-room-configuration" (optional json_arg)
           ~doc:"JSON CreateMeetingRoomConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and profileName =
         flag "profile-name" (required string) ~doc:"STRING ProfileName"
       and timezone =
         flag "timezone" (required string) ~doc:"STRING Timezone"
       and address = flag "address" (required string) ~doc:"STRING Address"
       and distanceUnit =
         flag "distance-unit" (required json_arg) ~doc:"JSON DistanceUnit"
       and temperatureUnit =
         flag "temperature-unit" (required json_arg)
           ~doc:"JSON TemperatureUnit"
       and wakeWord =
         flag "wake-word" (required json_arg) ~doc:"JSON WakeWord" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_profile
           (Values.CreateProfileRequest.make ?locale ?clientRequestToken
              ?setupModeDisabled ?maxVolumeLimit ?pSTNEnabled
              ?dataRetentionOptIn
              ?meetingRoomConfiguration:(Option.map
                                           ~f:Values.CreateMeetingRoomConfiguration.of_json
                                           meetingRoomConfiguration)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~profileName
              ~timezone ~address
              ~distanceUnit:(Values.DistanceUnit.of_json distanceUnit)
              ~temperatureUnit:(Values.TemperatureUnit.of_json
                                  temperatureUnit)
              ~wakeWord:(Values.WakeWord.of_json wakeWord) ())
           (Some Values.CreateProfileResponse.to_json)
           (Some Values.CreateProfileResponse.error_to_json)])
let create_room =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING RoomDescription"
       and profileArn =
         flag "profile-arn" (optional string) ~doc:"STRING Arn"
       and providerCalendarId =
         flag "provider-calendar-id" (optional string)
           ~doc:"STRING ProviderCalendarId"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and roomName =
         flag "room-name" (required string) ~doc:"STRING RoomName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_room
           (Values.CreateRoomRequest.make ?description ?profileArn
              ?providerCalendarId ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~roomName ())
           (Some Values.CreateRoomResponse.to_json)
           (Some Values.CreateRoomResponse.error_to_json)])
let create_skill_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING SkillGroupDescription"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and skillGroupName =
         flag "skill-group-name" (required string)
           ~doc:"STRING SkillGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_skill_group
           (Values.CreateSkillGroupRequest.make ?description
              ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~skillGroupName ())
           (Some Values.CreateSkillGroupResponse.to_json)
           (Some Values.CreateSkillGroupResponse.error_to_json)])
let create_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and firstName =
         flag "first-name" (optional string) ~doc:"STRING user_FirstName"
       and lastName =
         flag "last-name" (optional string) ~doc:"STRING user_LastName"
       and email = flag "email" (optional string) ~doc:"STRING Email"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and userId =
         flag "user-id" (required string) ~doc:"STRING user_UserId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_user
           (Values.CreateUserRequest.make ?firstName ?lastName ?email
              ?clientRequestToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~userId ())
           (Some Values.CreateUserResponse.to_json)
           (Some Values.CreateUserResponse.error_to_json)])
let delete_address_book =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and addressBookArn =
         flag "address-book-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_address_book
           (Values.DeleteAddressBookRequest.make ~addressBookArn ())
           (Some Values.DeleteAddressBookResponse.to_json)
           (Some Values.DeleteAddressBookResponse.error_to_json)])
let delete_business_report_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scheduleArn =
         flag "schedule-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_business_report_schedule
           (Values.DeleteBusinessReportScheduleRequest.make ~scheduleArn ())
           (Some Values.DeleteBusinessReportScheduleResponse.to_json)
           (Some Values.DeleteBusinessReportScheduleResponse.error_to_json)])
let delete_conference_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and conferenceProviderArn =
         flag "conference-provider-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_conference_provider
           (Values.DeleteConferenceProviderRequest.make
              ~conferenceProviderArn ())
           (Some Values.DeleteConferenceProviderResponse.to_json)
           (Some Values.DeleteConferenceProviderResponse.error_to_json)])
let delete_contact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contactArn =
         flag "contact-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_contact
           (Values.DeleteContactRequest.make ~contactArn ())
           (Some Values.DeleteContactResponse.to_json)
           (Some Values.DeleteContactResponse.error_to_json)])
let delete_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceArn = flag "device-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_device (Values.DeleteDeviceRequest.make ~deviceArn ())
           (Some Values.DeleteDeviceResponse.to_json)
           (Some Values.DeleteDeviceResponse.error_to_json)])
let delete_device_usage_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceArn = flag "device-arn" (required string) ~doc:"STRING Arn"
       and deviceUsageType =
         flag "device-usage-type" (required json_arg)
           ~doc:"JSON DeviceUsageType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_device_usage_data
           (Values.DeleteDeviceUsageDataRequest.make ~deviceArn
              ~deviceUsageType:(Values.DeviceUsageType.of_json
                                  deviceUsageType) ())
           (Some Values.DeleteDeviceUsageDataResponse.to_json)
           (Some Values.DeleteDeviceUsageDataResponse.error_to_json)])
let delete_gateway_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayGroupArn =
         flag "gateway-group-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_gateway_group
           (Values.DeleteGatewayGroupRequest.make ~gatewayGroupArn ())
           (Some Values.DeleteGatewayGroupResponse.to_json)
           (Some Values.DeleteGatewayGroupResponse.error_to_json)])
let delete_network_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkProfileArn =
         flag "network-profile-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_network_profile
           (Values.DeleteNetworkProfileRequest.make ~networkProfileArn ())
           (Some Values.DeleteNetworkProfileResponse.to_json)
           (Some Values.DeleteNetworkProfileResponse.error_to_json)])
let delete_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileArn =
         flag "profile-arn" (optional string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_profile
           (Values.DeleteProfileRequest.make ?profileArn ())
           (Some Values.DeleteProfileResponse.to_json)
           (Some Values.DeleteProfileResponse.error_to_json)])
let delete_room =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roomArn = flag "room-arn" (optional string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_room (Values.DeleteRoomRequest.make ?roomArn ())
           (Some Values.DeleteRoomResponse.to_json)
           (Some Values.DeleteRoomResponse.error_to_json)])
let delete_room_skill_parameter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roomArn = flag "room-arn" (optional string) ~doc:"STRING Arn"
       and skillId = flag "skill-id" (required string) ~doc:"STRING SkillId"
       and parameterKey =
         flag "parameter-key" (required string)
           ~doc:"STRING RoomSkillParameterKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_room_skill_parameter
           (Values.DeleteRoomSkillParameterRequest.make ?roomArn ~skillId
              ~parameterKey ())
           (Some Values.DeleteRoomSkillParameterResponse.to_json)
           (Some Values.DeleteRoomSkillParameterResponse.error_to_json)])
let delete_skill_authorization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roomArn = flag "room-arn" (optional string) ~doc:"STRING Arn"
       and skillId = flag "skill-id" (required string) ~doc:"STRING SkillId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_skill_authorization
           (Values.DeleteSkillAuthorizationRequest.make ?roomArn ~skillId ())
           (Some Values.DeleteSkillAuthorizationResponse.to_json)
           (Some Values.DeleteSkillAuthorizationResponse.error_to_json)])
let delete_skill_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and skillGroupArn =
         flag "skill-group-arn" (optional string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_skill_group
           (Values.DeleteSkillGroupRequest.make ?skillGroupArn ())
           (Some Values.DeleteSkillGroupResponse.to_json)
           (Some Values.DeleteSkillGroupResponse.error_to_json)])
let delete_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userArn = flag "user-arn" (optional string) ~doc:"STRING Arn"
       and enrollmentId =
         flag "enrollment-id" (required string) ~doc:"STRING EnrollmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_user
           (Values.DeleteUserRequest.make ?userArn ~enrollmentId ())
           (Some Values.DeleteUserResponse.to_json)
           (Some Values.DeleteUserResponse.error_to_json)])
let disassociate_contact_from_address_book =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contactArn =
         flag "contact-arn" (required string) ~doc:"STRING Arn"
       and addressBookArn =
         flag "address-book-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_contact_from_address_book
           (Values.DisassociateContactFromAddressBookRequest.make ~contactArn
              ~addressBookArn ())
           (Some Values.DisassociateContactFromAddressBookResponse.to_json)
           (Some
              Values.DisassociateContactFromAddressBookResponse.error_to_json)])
let disassociate_device_from_room =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceArn = flag "device-arn" (optional string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_device_from_room
           (Values.DisassociateDeviceFromRoomRequest.make ?deviceArn ())
           (Some Values.DisassociateDeviceFromRoomResponse.to_json)
           (Some Values.DisassociateDeviceFromRoomResponse.error_to_json)])
let disassociate_skill_from_skill_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and skillGroupArn =
         flag "skill-group-arn" (optional string) ~doc:"STRING Arn"
       and skillId = flag "skill-id" (required string) ~doc:"STRING SkillId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_skill_from_skill_group
           (Values.DisassociateSkillFromSkillGroupRequest.make ?skillGroupArn
              ~skillId ())
           (Some Values.DisassociateSkillFromSkillGroupResponse.to_json)
           (Some Values.DisassociateSkillFromSkillGroupResponse.error_to_json)])
let disassociate_skill_from_users =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and skillId = flag "skill-id" (required string) ~doc:"STRING SkillId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_skill_from_users
           (Values.DisassociateSkillFromUsersRequest.make ~skillId ())
           (Some Values.DisassociateSkillFromUsersResponse.to_json)
           (Some Values.DisassociateSkillFromUsersResponse.error_to_json)])
let disassociate_skill_group_from_room =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and skillGroupArn =
         flag "skill-group-arn" (optional string) ~doc:"STRING Arn"
       and roomArn = flag "room-arn" (optional string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_skill_group_from_room
           (Values.DisassociateSkillGroupFromRoomRequest.make ?skillGroupArn
              ?roomArn ())
           (Some Values.DisassociateSkillGroupFromRoomResponse.to_json)
           (Some Values.DisassociateSkillGroupFromRoomResponse.error_to_json)])
let forget_smart_home_appliances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roomArn = flag "room-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.forget_smart_home_appliances
           (Values.ForgetSmartHomeAppliancesRequest.make ~roomArn ())
           (Some Values.ForgetSmartHomeAppliancesResponse.to_json)
           (Some Values.ForgetSmartHomeAppliancesResponse.error_to_json)])
let get_address_book =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and addressBookArn =
         flag "address-book-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_address_book
           (Values.GetAddressBookRequest.make ~addressBookArn ())
           (Some Values.GetAddressBookResponse.to_json)
           (Some Values.GetAddressBookResponse.error_to_json)])
let get_conference_preference =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_conference_preference
           (Values.GetConferencePreferenceRequest.make ())
           (Some Values.GetConferencePreferenceResponse.to_json)
           (Some Values.GetConferencePreferenceResponse.error_to_json)])
let get_conference_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and conferenceProviderArn =
         flag "conference-provider-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_conference_provider
           (Values.GetConferenceProviderRequest.make ~conferenceProviderArn
              ()) (Some Values.GetConferenceProviderResponse.to_json)
           (Some Values.GetConferenceProviderResponse.error_to_json)])
let get_contact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contactArn =
         flag "contact-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_contact (Values.GetContactRequest.make ~contactArn ())
           (Some Values.GetContactResponse.to_json)
           (Some Values.GetContactResponse.error_to_json)])
let get_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceArn = flag "device-arn" (optional string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_device (Values.GetDeviceRequest.make ?deviceArn ())
           (Some Values.GetDeviceResponse.to_json)
           (Some Values.GetDeviceResponse.error_to_json)])
let get_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayArn =
         flag "gateway-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_gateway (Values.GetGatewayRequest.make ~gatewayArn ())
           (Some Values.GetGatewayResponse.to_json)
           (Some Values.GetGatewayResponse.error_to_json)])
let get_gateway_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayGroupArn =
         flag "gateway-group-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_gateway_group
           (Values.GetGatewayGroupRequest.make ~gatewayGroupArn ())
           (Some Values.GetGatewayGroupResponse.to_json)
           (Some Values.GetGatewayGroupResponse.error_to_json)])
let get_invitation_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_invitation_configuration
           (Values.GetInvitationConfigurationRequest.make ())
           (Some Values.GetInvitationConfigurationResponse.to_json)
           (Some Values.GetInvitationConfigurationResponse.error_to_json)])
let get_network_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkProfileArn =
         flag "network-profile-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_network_profile
           (Values.GetNetworkProfileRequest.make ~networkProfileArn ())
           (Some Values.GetNetworkProfileResponse.to_json)
           (Some Values.GetNetworkProfileResponse.error_to_json)])
let get_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileArn =
         flag "profile-arn" (optional string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_profile (Values.GetProfileRequest.make ?profileArn ())
           (Some Values.GetProfileResponse.to_json)
           (Some Values.GetProfileResponse.error_to_json)])
let get_room =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roomArn = flag "room-arn" (optional string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_room (Values.GetRoomRequest.make ?roomArn ())
           (Some Values.GetRoomResponse.to_json)
           (Some Values.GetRoomResponse.error_to_json)])
let get_room_skill_parameter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roomArn = flag "room-arn" (optional string) ~doc:"STRING Arn"
       and skillId = flag "skill-id" (required string) ~doc:"STRING SkillId"
       and parameterKey =
         flag "parameter-key" (required string)
           ~doc:"STRING RoomSkillParameterKey" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_room_skill_parameter
           (Values.GetRoomSkillParameterRequest.make ?roomArn ~skillId
              ~parameterKey ())
           (Some Values.GetRoomSkillParameterResponse.to_json)
           (Some Values.GetRoomSkillParameterResponse.error_to_json)])
let get_skill_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and skillGroupArn =
         flag "skill-group-arn" (optional string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_skill_group
           (Values.GetSkillGroupRequest.make ?skillGroupArn ())
           (Some Values.GetSkillGroupResponse.to_json)
           (Some Values.GetSkillGroupResponse.error_to_json)])
let list_business_report_schedules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_business_report_schedules
           (Values.ListBusinessReportSchedulesRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListBusinessReportSchedulesResponse.to_json)
           (Some Values.ListBusinessReportSchedulesResponse.error_to_json)])
let list_conference_providers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_conference_providers
           (Values.ListConferenceProvidersRequest.make ?nextToken ?maxResults
              ()) (Some Values.ListConferenceProvidersResponse.to_json)
           (Some Values.ListConferenceProvidersResponse.error_to_json)])
let list_device_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventType =
         flag "event-type" (optional json_arg) ~doc:"JSON DeviceEventType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and deviceArn = flag "device-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_device_events
           (Values.ListDeviceEventsRequest.make
              ?eventType:(Option.map ~f:Values.DeviceEventType.of_json
                            eventType) ?nextToken ?maxResults ~deviceArn ())
           (Some Values.ListDeviceEventsResponse.to_json)
           (Some Values.ListDeviceEventsResponse.error_to_json)])
let list_gateway_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_gateway_groups
           (Values.ListGatewayGroupsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListGatewayGroupsResponse.to_json)
           (Some Values.ListGatewayGroupsResponse.error_to_json)])
let list_gateways =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayGroupArn =
         flag "gateway-group-arn" (optional string) ~doc:"STRING Arn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_gateways
           (Values.ListGatewaysRequest.make ?gatewayGroupArn ?nextToken
              ?maxResults ()) (Some Values.ListGatewaysResponse.to_json)
           (Some Values.ListGatewaysResponse.error_to_json)])
let list_skills =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and skillGroupArn =
         flag "skill-group-arn" (optional string) ~doc:"STRING Arn"
       and enablementType =
         flag "enablement-type" (optional json_arg)
           ~doc:"JSON EnablementTypeFilter"
       and skillType =
         flag "skill-type" (optional json_arg) ~doc:"JSON SkillTypeFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT SkillListMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_skills
           (Values.ListSkillsRequest.make ?skillGroupArn
              ?enablementType:(Option.map
                                 ~f:Values.EnablementTypeFilter.of_json
                                 enablementType)
              ?skillType:(Option.map ~f:Values.SkillTypeFilter.of_json
                            skillType) ?nextToken ?maxResults ())
           (Some Values.ListSkillsResponse.to_json)
           (Some Values.ListSkillsResponse.error_to_json)])
let list_skills_store_categories =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_skills_store_categories
           (Values.ListSkillsStoreCategoriesRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListSkillsStoreCategoriesResponse.to_json)
           (Some Values.ListSkillsStoreCategoriesResponse.error_to_json)])
let list_skills_store_skills_by_category =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT SkillListMaxResults"
       and categoryId =
         flag "category-id" (required json_arg) ~doc:"JSON CategoryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_skills_store_skills_by_category
           (Values.ListSkillsStoreSkillsByCategoryRequest.make ?nextToken
              ?maxResults ~categoryId:(Values.CategoryId.of_json categoryId)
              ())
           (Some Values.ListSkillsStoreSkillsByCategoryResponse.to_json)
           (Some Values.ListSkillsStoreSkillsByCategoryResponse.error_to_json)])
let list_smart_home_appliances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and roomArn = flag "room-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_smart_home_appliances
           (Values.ListSmartHomeAppliancesRequest.make ?maxResults ?nextToken
              ~roomArn ())
           (Some Values.ListSmartHomeAppliancesResponse.to_json)
           (Some Values.ListSmartHomeAppliancesResponse.error_to_json)])
let list_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and arn = flag "arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags
           (Values.ListTagsRequest.make ?nextToken ?maxResults ~arn ())
           (Some Values.ListTagsResponse.to_json)
           (Some Values.ListTagsResponse.error_to_json)])
let put_conference_preference =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and conferencePreference =
         flag "conference-preference" (required json_arg)
           ~doc:"JSON ConferencePreference" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_conference_preference
           (Values.PutConferencePreferenceRequest.make
              ~conferencePreference:(Values.ConferencePreference.of_json
                                       conferencePreference) ())
           (Some Values.PutConferencePreferenceResponse.to_json)
           (Some Values.PutConferencePreferenceResponse.error_to_json)])
let put_invitation_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contactEmail =
         flag "contact-email" (optional string) ~doc:"STRING Email"
       and privateSkillIds =
         flag "private-skill-ids" (optional json_arg)
           ~doc:"JSON ShortSkillIdList"
       and organizationName =
         flag "organization-name" (required string)
           ~doc:"STRING OrganizationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_invitation_configuration
           (Values.PutInvitationConfigurationRequest.make ?contactEmail
              ?privateSkillIds:(Option.map ~f:Values.ShortSkillIdList.of_json
                                  privateSkillIds) ~organizationName ())
           (Some Values.PutInvitationConfigurationResponse.to_json)
           (Some Values.PutInvitationConfigurationResponse.error_to_json)])
let put_room_skill_parameter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roomArn = flag "room-arn" (optional string) ~doc:"STRING Arn"
       and skillId = flag "skill-id" (required string) ~doc:"STRING SkillId"
       and roomSkillParameter =
         flag "room-skill-parameter" (required json_arg)
           ~doc:"JSON RoomSkillParameter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_room_skill_parameter
           (Values.PutRoomSkillParameterRequest.make ?roomArn ~skillId
              ~roomSkillParameter:(Values.RoomSkillParameter.of_json
                                     roomSkillParameter) ())
           (Some Values.PutRoomSkillParameterResponse.to_json)
           (Some Values.PutRoomSkillParameterResponse.error_to_json)])
let put_skill_authorization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roomArn = flag "room-arn" (optional string) ~doc:"STRING Arn"
       and authorizationResult =
         flag "authorization-result" (required json_arg)
           ~doc:"JSON AuthorizationResult"
       and skillId = flag "skill-id" (required string) ~doc:"STRING SkillId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_skill_authorization
           (Values.PutSkillAuthorizationRequest.make ?roomArn
              ~authorizationResult:(Values.AuthorizationResult.of_json
                                      authorizationResult) ~skillId ())
           (Some Values.PutSkillAuthorizationResponse.to_json)
           (Some Values.PutSkillAuthorizationResponse.error_to_json)])
let register_a_v_s_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceSerialNumber =
         flag "device-serial-number" (optional string)
           ~doc:"STRING DeviceSerialNumberForAVS"
       and roomArn = flag "room-arn" (optional string) ~doc:"STRING Arn"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and clientId =
         flag "client-id" (required string) ~doc:"STRING ClientId"
       and userCode =
         flag "user-code" (required string) ~doc:"STRING UserCode"
       and productId =
         flag "product-id" (required string) ~doc:"STRING ProductId"
       and amazonId =
         flag "amazon-id" (required string) ~doc:"STRING AmazonId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_a_v_s_device
           (Values.RegisterAVSDeviceRequest.make ?deviceSerialNumber ?roomArn
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~clientId
              ~userCode ~productId ~amazonId ())
           (Some Values.RegisterAVSDeviceResponse.to_json)
           (Some Values.RegisterAVSDeviceResponse.error_to_json)])
let reject_skill =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and skillId = flag "skill-id" (required string) ~doc:"STRING SkillId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reject_skill (Values.RejectSkillRequest.make ~skillId ())
           (Some Values.RejectSkillResponse.to_json)
           (Some Values.RejectSkillResponse.error_to_json)])
let resolve_room =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userId = flag "user-id" (required string) ~doc:"STRING UserId"
       and skillId = flag "skill-id" (required string) ~doc:"STRING SkillId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.resolve_room
           (Values.ResolveRoomRequest.make ~userId ~skillId ())
           (Some Values.ResolveRoomResponse.to_json)
           (Some Values.ResolveRoomResponse.error_to_json)])
let revoke_invitation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userArn = flag "user-arn" (optional string) ~doc:"STRING Arn"
       and enrollmentId =
         flag "enrollment-id" (optional string) ~doc:"STRING EnrollmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.revoke_invitation
           (Values.RevokeInvitationRequest.make ?userArn ?enrollmentId ())
           (Some Values.RevokeInvitationResponse.to_json)
           (Some Values.RevokeInvitationResponse.error_to_json)])
let search_address_books =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and sortCriteria =
         flag "sort-criteria" (optional json_arg) ~doc:"JSON SortList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_address_books
           (Values.SearchAddressBooksRequest.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?sortCriteria:(Option.map ~f:Values.SortList.of_json
                               sortCriteria) ?nextToken ?maxResults ())
           (Some Values.SearchAddressBooksResponse.to_json)
           (Some Values.SearchAddressBooksResponse.error_to_json)])
let search_contacts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and sortCriteria =
         flag "sort-criteria" (optional json_arg) ~doc:"JSON SortList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_contacts
           (Values.SearchContactsRequest.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?sortCriteria:(Option.map ~f:Values.SortList.of_json
                               sortCriteria) ?nextToken ?maxResults ())
           (Some Values.SearchContactsResponse.to_json)
           (Some Values.SearchContactsResponse.error_to_json)])
let search_devices =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and sortCriteria =
         flag "sort-criteria" (optional json_arg) ~doc:"JSON SortList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_devices
           (Values.SearchDevicesRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?sortCriteria:(Option.map ~f:Values.SortList.of_json
                               sortCriteria) ())
           (Some Values.SearchDevicesResponse.to_json)
           (Some Values.SearchDevicesResponse.error_to_json)])
let search_network_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and sortCriteria =
         flag "sort-criteria" (optional json_arg) ~doc:"JSON SortList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_network_profiles
           (Values.SearchNetworkProfilesRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?sortCriteria:(Option.map ~f:Values.SortList.of_json
                               sortCriteria) ())
           (Some Values.SearchNetworkProfilesResponse.to_json)
           (Some Values.SearchNetworkProfilesResponse.error_to_json)])
let search_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and sortCriteria =
         flag "sort-criteria" (optional json_arg) ~doc:"JSON SortList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_profiles
           (Values.SearchProfilesRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?sortCriteria:(Option.map ~f:Values.SortList.of_json
                               sortCriteria) ())
           (Some Values.SearchProfilesResponse.to_json)
           (Some Values.SearchProfilesResponse.error_to_json)])
let search_rooms =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and sortCriteria =
         flag "sort-criteria" (optional json_arg) ~doc:"JSON SortList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_rooms
           (Values.SearchRoomsRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?sortCriteria:(Option.map ~f:Values.SortList.of_json
                               sortCriteria) ())
           (Some Values.SearchRoomsResponse.to_json)
           (Some Values.SearchRoomsResponse.error_to_json)])
let search_skill_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and sortCriteria =
         flag "sort-criteria" (optional json_arg) ~doc:"JSON SortList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_skill_groups
           (Values.SearchSkillGroupsRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?sortCriteria:(Option.map ~f:Values.SortList.of_json
                               sortCriteria) ())
           (Some Values.SearchSkillGroupsResponse.to_json)
           (Some Values.SearchSkillGroupsResponse.error_to_json)])
let search_users =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and sortCriteria =
         flag "sort-criteria" (optional json_arg) ~doc:"JSON SortList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_users
           (Values.SearchUsersRequest.make ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?sortCriteria:(Option.map ~f:Values.SortList.of_json
                               sortCriteria) ())
           (Some Values.SearchUsersResponse.to_json)
           (Some Values.SearchUsersResponse.error_to_json)])
let send_announcement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and timeToLiveInSeconds =
         flag "time-to-live-in-seconds" (optional int)
           ~doc:"INT TimeToLiveInSeconds"
       and roomFilters =
         flag "room-filters" (required json_arg) ~doc:"JSON FilterList"
       and content = flag "content" (required json_arg) ~doc:"JSON Content"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_announcement
           (Values.SendAnnouncementRequest.make ?timeToLiveInSeconds
              ~roomFilters:(Values.FilterList.of_json roomFilters)
              ~content:(Values.Content.of_json content) ~clientRequestToken
              ()) (Some Values.SendAnnouncementResponse.to_json)
           (Some Values.SendAnnouncementResponse.error_to_json)])
let send_invitation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userArn = flag "user-arn" (optional string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_invitation (Values.SendInvitationRequest.make ?userArn ())
           (Some Values.SendInvitationResponse.to_json)
           (Some Values.SendInvitationResponse.error_to_json)])
let start_device_sync =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roomArn = flag "room-arn" (optional string) ~doc:"STRING Arn"
       and deviceArn = flag "device-arn" (optional string) ~doc:"STRING Arn"
       and features =
         flag "features" (required json_arg) ~doc:"JSON Features" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_device_sync
           (Values.StartDeviceSyncRequest.make ?roomArn ?deviceArn
              ~features:(Values.Features.of_json features) ())
           (Some Values.StartDeviceSyncResponse.to_json)
           (Some Values.StartDeviceSyncResponse.error_to_json)])
let start_smart_home_appliance_discovery =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roomArn = flag "room-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_smart_home_appliance_discovery
           (Values.StartSmartHomeApplianceDiscoveryRequest.make ~roomArn ())
           (Some Values.StartSmartHomeApplianceDiscoveryResponse.to_json)
           (Some
              Values.StartSmartHomeApplianceDiscoveryResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~arn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~arn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_address_book =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING AddressBookName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING AddressBookDescription"
       and addressBookArn =
         flag "address-book-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_address_book
           (Values.UpdateAddressBookRequest.make ?name ?description
              ~addressBookArn ())
           (Some Values.UpdateAddressBookResponse.to_json)
           (Some Values.UpdateAddressBookResponse.error_to_json)])
let update_business_report_schedule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and s3BucketName =
         flag "s3-bucket-name" (optional string)
           ~doc:"STRING CustomerS3BucketName"
       and s3KeyPrefix =
         flag "s3-key-prefix" (optional string) ~doc:"STRING S3KeyPrefix"
       and format =
         flag "format" (optional json_arg) ~doc:"JSON BusinessReportFormat"
       and scheduleName =
         flag "schedule-name" (optional string)
           ~doc:"STRING BusinessReportScheduleName"
       and recurrence =
         flag "recurrence" (optional json_arg)
           ~doc:"JSON BusinessReportRecurrence"
       and scheduleArn =
         flag "schedule-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_business_report_schedule
           (Values.UpdateBusinessReportScheduleRequest.make ?s3BucketName
              ?s3KeyPrefix
              ?format:(Option.map ~f:Values.BusinessReportFormat.of_json
                         format) ?scheduleName
              ?recurrence:(Option.map
                             ~f:Values.BusinessReportRecurrence.of_json
                             recurrence) ~scheduleArn ())
           (Some Values.UpdateBusinessReportScheduleResponse.to_json)
           (Some Values.UpdateBusinessReportScheduleResponse.error_to_json)])
let update_conference_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and iPDialIn =
         flag "i-p-dial-in" (optional json_arg) ~doc:"JSON IPDialIn"
       and pSTNDialIn =
         flag "p-s-t-n-dial-in" (optional json_arg) ~doc:"JSON PSTNDialIn"
       and conferenceProviderArn =
         flag "conference-provider-arn" (required string) ~doc:"STRING Arn"
       and conferenceProviderType =
         flag "conference-provider-type" (required json_arg)
           ~doc:"JSON ConferenceProviderType"
       and meetingSetting =
         flag "meeting-setting" (required json_arg)
           ~doc:"JSON MeetingSetting" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_conference_provider
           (Values.UpdateConferenceProviderRequest.make
              ?iPDialIn:(Option.map ~f:Values.IPDialIn.of_json iPDialIn)
              ?pSTNDialIn:(Option.map ~f:Values.PSTNDialIn.of_json pSTNDialIn)
              ~conferenceProviderArn
              ~conferenceProviderType:(Values.ConferenceProviderType.of_json
                                         conferenceProviderType)
              ~meetingSetting:(Values.MeetingSetting.of_json meetingSetting)
              ()) (Some Values.UpdateConferenceProviderResponse.to_json)
           (Some Values.UpdateConferenceProviderResponse.error_to_json)])
let update_contact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING ContactName"
       and firstName =
         flag "first-name" (optional string) ~doc:"STRING ContactName"
       and lastName =
         flag "last-name" (optional string) ~doc:"STRING ContactName"
       and phoneNumber =
         flag "phone-number" (optional string) ~doc:"STRING RawPhoneNumber"
       and phoneNumbers =
         flag "phone-numbers" (optional json_arg) ~doc:"JSON PhoneNumberList"
       and sipAddresses =
         flag "sip-addresses" (optional json_arg) ~doc:"JSON SipAddressList"
       and contactArn =
         flag "contact-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_contact
           (Values.UpdateContactRequest.make ?displayName ?firstName
              ?lastName ?phoneNumber
              ?phoneNumbers:(Option.map ~f:Values.PhoneNumberList.of_json
                               phoneNumbers)
              ?sipAddresses:(Option.map ~f:Values.SipAddressList.of_json
                               sipAddresses) ~contactArn ())
           (Some Values.UpdateContactResponse.to_json)
           (Some Values.UpdateContactResponse.error_to_json)])
let update_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deviceArn = flag "device-arn" (optional string) ~doc:"STRING Arn"
       and deviceName =
         flag "device-name" (optional string) ~doc:"STRING DeviceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_device
           (Values.UpdateDeviceRequest.make ?deviceArn ?deviceName ())
           (Some Values.UpdateDeviceResponse.to_json)
           (Some Values.UpdateDeviceResponse.error_to_json)])
let update_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING GatewayName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING GatewayDescription"
       and softwareVersion =
         flag "software-version" (optional string)
           ~doc:"STRING GatewayVersion"
       and gatewayArn =
         flag "gateway-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_gateway
           (Values.UpdateGatewayRequest.make ?name ?description
              ?softwareVersion ~gatewayArn ())
           (Some Values.UpdateGatewayResponse.to_json)
           (Some Values.UpdateGatewayResponse.error_to_json)])
let update_gateway_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING GatewayGroupName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING GatewayGroupDescription"
       and gatewayGroupArn =
         flag "gateway-group-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_gateway_group
           (Values.UpdateGatewayGroupRequest.make ?name ?description
              ~gatewayGroupArn ())
           (Some Values.UpdateGatewayGroupResponse.to_json)
           (Some Values.UpdateGatewayGroupResponse.error_to_json)])
let update_network_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and networkProfileName =
         flag "network-profile-name" (optional string)
           ~doc:"STRING NetworkProfileName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING NetworkProfileDescription"
       and currentPassword =
         flag "current-password" (optional string)
           ~doc:"STRING CurrentWiFiPassword"
       and nextPassword =
         flag "next-password" (optional string)
           ~doc:"STRING NextWiFiPassword"
       and certificateAuthorityArn =
         flag "certificate-authority-arn" (optional string) ~doc:"STRING Arn"
       and trustAnchors =
         flag "trust-anchors" (optional json_arg) ~doc:"JSON TrustAnchorList"
       and networkProfileArn =
         flag "network-profile-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_network_profile
           (Values.UpdateNetworkProfileRequest.make ?networkProfileName
              ?description ?currentPassword ?nextPassword
              ?certificateAuthorityArn
              ?trustAnchors:(Option.map ~f:Values.TrustAnchorList.of_json
                               trustAnchors) ~networkProfileArn ())
           (Some Values.UpdateNetworkProfileResponse.to_json)
           (Some Values.UpdateNetworkProfileResponse.error_to_json)])
let update_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileArn =
         flag "profile-arn" (optional string) ~doc:"STRING Arn"
       and profileName =
         flag "profile-name" (optional string) ~doc:"STRING ProfileName"
       and isDefault = flag "is-default" (optional bool) ~doc:"BOOL Boolean"
       and timezone =
         flag "timezone" (optional string) ~doc:"STRING Timezone"
       and address = flag "address" (optional string) ~doc:"STRING Address"
       and distanceUnit =
         flag "distance-unit" (optional json_arg) ~doc:"JSON DistanceUnit"
       and temperatureUnit =
         flag "temperature-unit" (optional json_arg)
           ~doc:"JSON TemperatureUnit"
       and wakeWord =
         flag "wake-word" (optional json_arg) ~doc:"JSON WakeWord"
       and locale =
         flag "locale" (optional string) ~doc:"STRING DeviceLocale"
       and setupModeDisabled =
         flag "setup-mode-disabled" (optional bool) ~doc:"BOOL Boolean"
       and maxVolumeLimit =
         flag "max-volume-limit" (optional int) ~doc:"INT MaxVolumeLimit"
       and pSTNEnabled =
         flag "p-s-t-n-enabled" (optional bool) ~doc:"BOOL Boolean"
       and dataRetentionOptIn =
         flag "data-retention-opt-in" (optional bool) ~doc:"BOOL Boolean"
       and meetingRoomConfiguration =
         flag "meeting-room-configuration" (optional json_arg)
           ~doc:"JSON UpdateMeetingRoomConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_profile
           (Values.UpdateProfileRequest.make ?profileArn ?profileName
              ?isDefault ?timezone ?address
              ?distanceUnit:(Option.map ~f:Values.DistanceUnit.of_json
                               distanceUnit)
              ?temperatureUnit:(Option.map ~f:Values.TemperatureUnit.of_json
                                  temperatureUnit)
              ?wakeWord:(Option.map ~f:Values.WakeWord.of_json wakeWord)
              ?locale ?setupModeDisabled ?maxVolumeLimit ?pSTNEnabled
              ?dataRetentionOptIn
              ?meetingRoomConfiguration:(Option.map
                                           ~f:Values.UpdateMeetingRoomConfiguration.of_json
                                           meetingRoomConfiguration) ())
           (Some Values.UpdateProfileResponse.to_json)
           (Some Values.UpdateProfileResponse.error_to_json)])
let update_room =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roomArn = flag "room-arn" (optional string) ~doc:"STRING Arn"
       and roomName =
         flag "room-name" (optional string) ~doc:"STRING RoomName"
       and description =
         flag "description" (optional string) ~doc:"STRING RoomDescription"
       and providerCalendarId =
         flag "provider-calendar-id" (optional string)
           ~doc:"STRING ProviderCalendarId"
       and profileArn =
         flag "profile-arn" (optional string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_room
           (Values.UpdateRoomRequest.make ?roomArn ?roomName ?description
              ?providerCalendarId ?profileArn ())
           (Some Values.UpdateRoomResponse.to_json)
           (Some Values.UpdateRoomResponse.error_to_json)])
let update_skill_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and skillGroupArn =
         flag "skill-group-arn" (optional string) ~doc:"STRING Arn"
       and skillGroupName =
         flag "skill-group-name" (optional string)
           ~doc:"STRING SkillGroupName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING SkillGroupDescription" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_skill_group
           (Values.UpdateSkillGroupRequest.make ?skillGroupArn
              ?skillGroupName ?description ())
           (Some Values.UpdateSkillGroupResponse.to_json)
           (Some Values.UpdateSkillGroupResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("approve-skill", approve_skill);
    ("associate-contact-with-address-book",
      associate_contact_with_address_book);
    ("associate-device-with-network-profile",
      associate_device_with_network_profile);
    ("associate-device-with-room", associate_device_with_room);
    ("associate-skill-group-with-room", associate_skill_group_with_room);
    ("associate-skill-with-skill-group", associate_skill_with_skill_group);
    ("associate-skill-with-users", associate_skill_with_users);
    ("create-address-book", create_address_book);
    ("create-business-report-schedule", create_business_report_schedule);
    ("create-conference-provider", create_conference_provider);
    ("create-contact", create_contact);
    ("create-gateway-group", create_gateway_group);
    ("create-network-profile", create_network_profile);
    ("create-profile", create_profile);
    ("create-room", create_room);
    ("create-skill-group", create_skill_group);
    ("create-user", create_user);
    ("delete-address-book", delete_address_book);
    ("delete-business-report-schedule", delete_business_report_schedule);
    ("delete-conference-provider", delete_conference_provider);
    ("delete-contact", delete_contact);
    ("delete-device", delete_device);
    ("delete-device-usage-data", delete_device_usage_data);
    ("delete-gateway-group", delete_gateway_group);
    ("delete-network-profile", delete_network_profile);
    ("delete-profile", delete_profile);
    ("delete-room", delete_room);
    ("delete-room-skill-parameter", delete_room_skill_parameter);
    ("delete-skill-authorization", delete_skill_authorization);
    ("delete-skill-group", delete_skill_group);
    ("delete-user", delete_user);
    ("disassociate-contact-from-address-book",
      disassociate_contact_from_address_book);
    ("disassociate-device-from-room", disassociate_device_from_room);
    ("disassociate-skill-from-skill-group",
      disassociate_skill_from_skill_group);
    ("disassociate-skill-from-users", disassociate_skill_from_users);
    ("disassociate-skill-group-from-room",
      disassociate_skill_group_from_room);
    ("forget-smart-home-appliances", forget_smart_home_appliances);
    ("get-address-book", get_address_book);
    ("get-conference-preference", get_conference_preference);
    ("get-conference-provider", get_conference_provider);
    ("get-contact", get_contact);
    ("get-device", get_device);
    ("get-gateway", get_gateway);
    ("get-gateway-group", get_gateway_group);
    ("get-invitation-configuration", get_invitation_configuration);
    ("get-network-profile", get_network_profile);
    ("get-profile", get_profile);
    ("get-room", get_room);
    ("get-room-skill-parameter", get_room_skill_parameter);
    ("get-skill-group", get_skill_group);
    ("list-business-report-schedules", list_business_report_schedules);
    ("list-conference-providers", list_conference_providers);
    ("list-device-events", list_device_events);
    ("list-gateway-groups", list_gateway_groups);
    ("list-gateways", list_gateways);
    ("list-skills", list_skills);
    ("list-skills-store-categories", list_skills_store_categories);
    ("list-skills-store-skills-by-category",
      list_skills_store_skills_by_category);
    ("list-smart-home-appliances", list_smart_home_appliances);
    ("list-tags", list_tags);
    ("put-conference-preference", put_conference_preference);
    ("put-invitation-configuration", put_invitation_configuration);
    ("put-room-skill-parameter", put_room_skill_parameter);
    ("put-skill-authorization", put_skill_authorization);
    ("register-a-v-s-device", register_a_v_s_device);
    ("reject-skill", reject_skill);
    ("resolve-room", resolve_room);
    ("revoke-invitation", revoke_invitation);
    ("search-address-books", search_address_books);
    ("search-contacts", search_contacts);
    ("search-devices", search_devices);
    ("search-network-profiles", search_network_profiles);
    ("search-profiles", search_profiles);
    ("search-rooms", search_rooms);
    ("search-skill-groups", search_skill_groups);
    ("search-users", search_users);
    ("send-announcement", send_announcement);
    ("send-invitation", send_invitation);
    ("start-device-sync", start_device_sync);
    ("start-smart-home-appliance-discovery",
      start_smart_home_appliance_discovery);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-address-book", update_address_book);
    ("update-business-report-schedule", update_business_report_schedule);
    ("update-conference-provider", update_conference_provider);
    ("update-contact", update_contact);
    ("update-device", update_device);
    ("update-gateway", update_gateway);
    ("update-gateway-group", update_gateway_group);
    ("update-network-profile", update_network_profile);
    ("update-profile", update_profile);
    ("update-room", update_room);
    ("update-skill-group", update_skill_group)]
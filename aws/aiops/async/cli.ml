(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_investigation_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and encryptionConfiguration =
         flag "encryption-configuration" (optional json_arg)
           ~doc:"JSON EncryptionConfiguration"
       and retentionInDays =
         flag "retention-in-days" (optional json_arg) ~doc:"JSON Retention"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and tagKeyBoundaries =
         flag "tag-key-boundaries" (optional json_arg)
           ~doc:"JSON TagKeyBoundaries"
       and chatbotNotificationChannel =
         flag "chatbot-notification-channel" (optional json_arg)
           ~doc:"JSON ChatbotNotificationChannel"
       and isCloudTrailEventHistoryEnabled =
         flag "is-cloud-trail-event-history-enabled" (optional bool)
           ~doc:"BOOL Boolean"
       and crossAccountConfigurations =
         flag "cross-account-configurations" (optional json_arg)
           ~doc:"JSON CrossAccountConfigurations"
       and name =
         flag "name" (required string)
           ~doc:"STRING StringWithPatternAndLengthLimits"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_investigation_group
           (Values.CreateInvestigationGroupInput.make
              ?encryptionConfiguration:(Option.map
                                          ~f:Values.EncryptionConfiguration.of_json
                                          encryptionConfiguration)
              ?retentionInDays:(Option.map ~f:Values.Retention.of_json
                                  retentionInDays)
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?tagKeyBoundaries:(Option.map
                                   ~f:Values.TagKeyBoundaries.of_json
                                   tagKeyBoundaries)
              ?chatbotNotificationChannel:(Option.map
                                             ~f:Values.ChatbotNotificationChannel.of_json
                                             chatbotNotificationChannel)
              ?isCloudTrailEventHistoryEnabled
              ?crossAccountConfigurations:(Option.map
                                             ~f:Values.CrossAccountConfigurations.of_json
                                             crossAccountConfigurations)
              ~name ~roleArn ())
           (Some Values.CreateInvestigationGroupOutput.to_json)
           (Some Values.CreateInvestigationGroupOutput.error_to_json)])
let delete_investigation_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING InvestigationGroupIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_investigation_group
           (Values.DeleteInvestigationGroupRequest.make ~identifier ()) None
           None])
let delete_investigation_group_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING InvestigationGroupIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_investigation_group_policy
           (Values.DeleteInvestigationGroupPolicyRequest.make ~identifier ())
           (Some Values.DeleteInvestigationGroupPolicyOutput.to_json)
           (Some Values.DeleteInvestigationGroupPolicyOutput.error_to_json)])
let get_investigation_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING InvestigationGroupIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_investigation_group
           (Values.GetInvestigationGroupRequest.make ~identifier ())
           (Some Values.GetInvestigationGroupResponse.to_json)
           (Some Values.GetInvestigationGroupResponse.error_to_json)])
let get_investigation_group_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING InvestigationGroupIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_investigation_group_policy
           (Values.GetInvestigationGroupPolicyRequest.make ~identifier ())
           (Some Values.GetInvestigationGroupPolicyResponse.to_json)
           (Some Values.GetInvestigationGroupPolicyResponse.error_to_json)])
let list_investigation_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING SensitiveStringWithLengthLimits"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListInvestigationGroupsInputMaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_investigation_groups
           (Values.ListInvestigationGroupsInput.make ?nextToken ?maxResults
              ()) (Some Values.ListInvestigationGroupsOutput.to_json)
           (Some Values.ListInvestigationGroupsOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let put_investigation_group_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING InvestigationGroupIdentifier"
       and policy =
         flag "policy" (required string)
           ~doc:"STRING InvestigationGroupPolicyDocument" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_investigation_group_policy
           (Values.PutInvestigationGroupPolicyRequest.make ~identifier
              ~policy ())
           (Some Values.PutInvestigationGroupPolicyResponse.to_json)
           (Some Values.PutInvestigationGroupPolicyResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_investigation_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleArn = flag "role-arn" (optional string) ~doc:"STRING RoleArn"
       and encryptionConfiguration =
         flag "encryption-configuration" (optional json_arg)
           ~doc:"JSON EncryptionConfiguration"
       and tagKeyBoundaries =
         flag "tag-key-boundaries" (optional json_arg)
           ~doc:"JSON TagKeyBoundaries"
       and chatbotNotificationChannel =
         flag "chatbot-notification-channel" (optional json_arg)
           ~doc:"JSON ChatbotNotificationChannel"
       and isCloudTrailEventHistoryEnabled =
         flag "is-cloud-trail-event-history-enabled" (optional bool)
           ~doc:"BOOL Boolean"
       and crossAccountConfigurations =
         flag "cross-account-configurations" (optional json_arg)
           ~doc:"JSON CrossAccountConfigurations"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING InvestigationGroupIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_investigation_group
           (Values.UpdateInvestigationGroupRequest.make ?roleArn
              ?encryptionConfiguration:(Option.map
                                          ~f:Values.EncryptionConfiguration.of_json
                                          encryptionConfiguration)
              ?tagKeyBoundaries:(Option.map
                                   ~f:Values.TagKeyBoundaries.of_json
                                   tagKeyBoundaries)
              ?chatbotNotificationChannel:(Option.map
                                             ~f:Values.ChatbotNotificationChannel.of_json
                                             chatbotNotificationChannel)
              ?isCloudTrailEventHistoryEnabled
              ?crossAccountConfigurations:(Option.map
                                             ~f:Values.CrossAccountConfigurations.of_json
                                             crossAccountConfigurations)
              ~identifier ())
           (Some Values.UpdateInvestigationGroupOutput.to_json)
           (Some Values.UpdateInvestigationGroupOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-investigation-group", create_investigation_group);
    ("delete-investigation-group", delete_investigation_group);
    ("delete-investigation-group-policy", delete_investigation_group_policy);
    ("get-investigation-group", get_investigation_group);
    ("get-investigation-group-policy", get_investigation_group_policy);
    ("list-investigation-groups", list_investigation_groups);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-investigation-group-policy", put_investigation_group_policy);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-investigation-group", update_investigation_group)]
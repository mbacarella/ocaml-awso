(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | AddProfilePermission: (AddProfilePermissionRequest.t,
  AddProfilePermissionResponse.t, AddProfilePermissionResponse.error) t 
  | CancelSigningProfile: (CancelSigningProfileRequest.t, unit, unit) t 
  | DescribeSigningJob: (DescribeSigningJobRequest.t,
  DescribeSigningJobResponse.t, DescribeSigningJobResponse.error) t 
  | GetRevocationStatus: (GetRevocationStatusRequest.t,
  GetRevocationStatusResponse.t, GetRevocationStatusResponse.error) t 
  | GetSigningPlatform: (GetSigningPlatformRequest.t,
  GetSigningPlatformResponse.t, GetSigningPlatformResponse.error) t 
  | GetSigningProfile: (GetSigningProfileRequest.t,
  GetSigningProfileResponse.t, GetSigningProfileResponse.error) t 
  | ListProfilePermissions: (ListProfilePermissionsRequest.t,
  ListProfilePermissionsResponse.t, ListProfilePermissionsResponse.error) t 
  | ListSigningJobs: (ListSigningJobsRequest.t, ListSigningJobsResponse.t,
  ListSigningJobsResponse.error) t 
  | ListSigningPlatforms: (ListSigningPlatformsRequest.t,
  ListSigningPlatformsResponse.t, ListSigningPlatformsResponse.error) t 
  | ListSigningProfiles: (ListSigningProfilesRequest.t,
  ListSigningProfilesResponse.t, ListSigningProfilesResponse.error) t 
  | ListTagsForResource: (ListTagsForResourceRequest.t,
  ListTagsForResourceResponse.t, ListTagsForResourceResponse.error) t 
  | PutSigningProfile: (PutSigningProfileRequest.t,
  PutSigningProfileResponse.t, PutSigningProfileResponse.error) t 
  | RemoveProfilePermission: (RemoveProfilePermissionRequest.t,
  RemoveProfilePermissionResponse.t, RemoveProfilePermissionResponse.error) t
  
  | RevokeSignature: (RevokeSignatureRequest.t, unit, unit) t 
  | RevokeSigningProfile: (RevokeSigningProfileRequest.t, unit, unit) t 
  | SignPayload: (SignPayloadRequest.t, SignPayloadResponse.t,
  SignPayloadResponse.error) t 
  | StartSigningJob: (StartSigningJobRequest.t, StartSigningJobResponse.t,
  StartSigningJobResponse.error) t 
  | TagResource: (TagResourceRequest.t, TagResourceResponse.t,
  TagResourceResponse.error) t 
  | UntagResource: (UntagResourceRequest.t, UntagResourceResponse.t,
  UntagResourceResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | AddProfilePermission -> `POST
  | CancelSigningProfile -> `DELETE
  | DescribeSigningJob -> `GET
  | GetRevocationStatus -> `GET
  | GetSigningPlatform -> `GET
  | GetSigningProfile -> `GET
  | ListProfilePermissions -> `GET
  | ListSigningJobs -> `GET
  | ListSigningPlatforms -> `GET
  | ListSigningProfiles -> `GET
  | ListTagsForResource -> `GET
  | PutSigningProfile -> `PUT
  | RemoveProfilePermission -> `DELETE
  | RevokeSignature -> `PUT
  | RevokeSigningProfile -> `PUT
  | SignPayload -> `POST
  | StartSigningJob -> `POST
  | TagResource -> `POST
  | UntagResource -> `DELETE
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | AddProfilePermission ->
          (Format.kasprintf Uri.of_string) "/signing-profiles/%s/permissions"
            (ProfileName.to_header x.AddProfilePermissionRequest.profileName)
      | CancelSigningProfile ->
          (Format.kasprintf Uri.of_string) "/signing-profiles/%s"
            (ProfileName.to_header x.CancelSigningProfileRequest.profileName)
      | DescribeSigningJob ->
          (Format.kasprintf Uri.of_string) "/signing-jobs/%s"
            (JobId.to_header x.DescribeSigningJobRequest.jobId)
      | GetRevocationStatus ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/revocations")
            (List.filter_opt
               [Some
                  ("signatureTimestamp",
                    (Timestamp.to_header x.signatureTimestamp));
               Some ("platformId", (PlatformId.to_header x.platformId));
               Some
                 ("profileVersionArn", (Arn.to_header x.profileVersionArn));
               Some ("jobArn", (Arn.to_header x.jobArn));
               Some
                 ("certificateHashes",
                   (CertificateHashes.to_header x.certificateHashes))])
      | GetSigningPlatform ->
          (Format.kasprintf Uri.of_string) "/signing-platforms/%s"
            (PlatformId.to_header x.GetSigningPlatformRequest.platformId)
      | GetSigningProfile ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/signing-profiles/%s"
               (ProfileName.to_header x.GetSigningProfileRequest.profileName))
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("profileOwner", (AccountId.to_header v)))
                  x.profileOwner])
      | ListProfilePermissions ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string)
               "/signing-profiles/%s/permissions"
               (ProfileName.to_header
                  x.ListProfilePermissionsRequest.profileName))
            (List.filter_opt
               [Option.map ~f:(fun v -> ("nextToken", (String_.to_header v)))
                  x.nextToken])
      | ListSigningJobs ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/signing-jobs")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("status", (SigningStatus.to_header v)))
                  x.status;
               Option.map
                 ~f:(fun v -> ("platformId", (PlatformId.to_header v)))
                 x.platformId;
               Option.map
                 ~f:(fun v -> ("requestedBy", (RequestedBy.to_header v)))
                 x.requestedBy;
               Option.map
                 ~f:(fun v -> ("maxResults", (MaxResults.to_header v)))
                 x.maxResults;
               Option.map
                 ~f:(fun v -> ("nextToken", (NextToken.to_header v)))
                 x.nextToken;
               Option.map ~f:(fun v -> ("isRevoked", (Bool_.to_header v)))
                 x.isRevoked;
               Option.map
                 ~f:(fun v ->
                       ("signatureExpiresBefore", (Timestamp.to_header v)))
                 x.signatureExpiresBefore;
               Option.map
                 ~f:(fun v ->
                       ("signatureExpiresAfter", (Timestamp.to_header v)))
                 x.signatureExpiresAfter;
               Option.map
                 ~f:(fun v -> ("jobInvoker", (AccountId.to_header v)))
                 x.jobInvoker])
      | ListSigningPlatforms ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/signing-platforms")
            (List.filter_opt
               [Option.map ~f:(fun v -> ("category", (String_.to_header v)))
                  x.category;
               Option.map ~f:(fun v -> ("partner", (String_.to_header v)))
                 x.partner;
               Option.map ~f:(fun v -> ("target", (String_.to_header v)))
                 x.target;
               Option.map
                 ~f:(fun v -> ("maxResults", (MaxResults.to_header v)))
                 x.maxResults;
               Option.map ~f:(fun v -> ("nextToken", (String_.to_header v)))
                 x.nextToken])
      | ListSigningProfiles ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/signing-profiles")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("includeCanceled", (Bool_.to_header v)))
                  x.includeCanceled;
               Option.map
                 ~f:(fun v -> ("maxResults", (MaxResults.to_header v)))
                 x.maxResults;
               Option.map
                 ~f:(fun v -> ("nextToken", (NextToken.to_header v)))
                 x.nextToken;
               Option.map
                 ~f:(fun v -> ("platformId", (PlatformId.to_header v)))
                 x.platformId;
               Option.map ~f:(fun v -> ("statuses", (Statuses.to_header v)))
                 x.statuses])
      | ListTagsForResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (String_.to_header x.ListTagsForResourceRequest.resourceArn)
      | PutSigningProfile ->
          (Format.kasprintf Uri.of_string) "/signing-profiles/%s"
            (ProfileName.to_header x.PutSigningProfileRequest.profileName)
      | RemoveProfilePermission ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string)
               "/signing-profiles/%s/permissions/%s"
               (ProfileName.to_header
                  x.RemoveProfilePermissionRequest.profileName)
               (String_.to_header
                  x.RemoveProfilePermissionRequest.statementId))
            (List.filter_opt
               [Some ("revisionId", (String_.to_header x.revisionId))])
      | RevokeSignature ->
          (Format.kasprintf Uri.of_string) "/signing-jobs/%s/revoke"
            (JobId.to_header x.RevokeSignatureRequest.jobId)
      | RevokeSigningProfile ->
          (Format.kasprintf Uri.of_string) "/signing-profiles/%s/revoke"
            (ProfileName.to_header x.RevokeSigningProfileRequest.profileName)
      | SignPayload ->
          (Format.kasprintf Uri.of_string) "/signing-jobs/with-payload"
      | StartSigningJob -> (Format.kasprintf Uri.of_string) "/signing-jobs"
      | TagResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (String_.to_header x.TagResourceRequest.resourceArn)
      | UntagResource ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/tags/%s"
               (String_.to_header x.UntagResourceRequest.resourceArn))
            (List.filter_opt
               [Some ("tagKeys", (TagKeyList.to_header x.tagKeys))]))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | AddProfilePermission ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.AddProfilePermissionRequest.profileVersion
                         ~f:(fun x ->
                               ("profileVersion",
                                 (ProfileVersion.to_value x)));
                      Some
                        ("action",
                          (String_.to_value
                             req.AddProfilePermissionRequest.action));
                      Some
                        ("principal",
                          (String_.to_value
                             req.AddProfilePermissionRequest.principal));
                      Option.map req.AddProfilePermissionRequest.revisionId
                        ~f:(fun x -> ("revisionId", (String_.to_value x)));
                      Some
                        ("statementId",
                          (String_.to_value
                             req.AddProfilePermissionRequest.statementId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CancelSigningProfile -> Awso.Http.Request.make (method_of_endpoint endp)
  | DescribeSigningJob ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetRevocationStatus ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetSigningPlatform ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetSigningProfile ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListProfilePermissions ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListSigningJobs ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListSigningPlatforms ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListSigningProfiles ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTagsForResource ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | PutSigningProfile -> Awso.Http.Request.make (method_of_endpoint endp)
  | RemoveProfilePermission ->
      Awso.Http.Request.make (method_of_endpoint endp)
  | RevokeSignature -> Awso.Http.Request.make (method_of_endpoint endp)
  | RevokeSigningProfile -> Awso.Http.Request.make (method_of_endpoint endp)
  | SignPayload ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("profileName",
                           (ProfileName.to_value
                              req.SignPayloadRequest.profileName));
                      Option.map req.SignPayloadRequest.profileOwner
                        ~f:(fun x -> ("profileOwner", (AccountId.to_value x)));
                      Some
                        ("payload",
                          (Payload.to_value req.SignPayloadRequest.payload));
                      Some
                        ("payloadFormat",
                          (String_.to_value
                             req.SignPayloadRequest.payloadFormat))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StartSigningJob ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("source",
                           (Source.to_value req.StartSigningJobRequest.source));
                      Some
                        ("destination",
                          (Destination.to_value
                             req.StartSigningJobRequest.destination));
                      Some
                        ("profileName",
                          (ProfileName.to_value
                             req.StartSigningJobRequest.profileName));
                      Some
                        ("clientRequestToken",
                          (ClientRequestToken.to_value
                             req.StartSigningJobRequest.clientRequestToken));
                      Option.map req.StartSigningJobRequest.profileOwner
                        ~f:(fun x -> ("profileOwner", (AccountId.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | TagResource ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("tags",
                           (TagMap.to_value req.TagResourceRequest.tags))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UntagResource -> Awso.Http.Request.make (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | AddProfilePermission ->
      if is_success
      then Ok (AddProfilePermissionResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some AddProfilePermissionResponse.error_of_json))
  | CancelSigningProfile ->
      if is_success then Ok () else Error (parse_aws_error None)
  | DescribeSigningJob ->
      if is_success
      then Ok (DescribeSigningJobResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some DescribeSigningJobResponse.error_of_json))
  | GetRevocationStatus ->
      if is_success
      then Ok (GetRevocationStatusResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some GetRevocationStatusResponse.error_of_json))
  | GetSigningPlatform ->
      if is_success
      then Ok (GetSigningPlatformResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some GetSigningPlatformResponse.error_of_json))
  | GetSigningProfile ->
      if is_success
      then Ok (GetSigningProfileResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some GetSigningProfileResponse.error_of_json))
  | ListProfilePermissions ->
      if is_success
      then
        Ok (ListProfilePermissionsResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some ListProfilePermissionsResponse.error_of_json))
  | ListSigningJobs ->
      if is_success
      then Ok (ListSigningJobsResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some ListSigningJobsResponse.error_of_json))
  | ListSigningPlatforms ->
      if is_success
      then Ok (ListSigningPlatformsResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListSigningPlatformsResponse.error_of_json))
  | ListSigningProfiles ->
      if is_success
      then Ok (ListSigningProfilesResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListSigningProfilesResponse.error_of_json))
  | ListTagsForResource ->
      if is_success
      then Ok (ListTagsForResourceResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListTagsForResourceResponse.error_of_json))
  | PutSigningProfile ->
      if is_success
      then Ok (PutSigningProfileResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some PutSigningProfileResponse.error_of_json))
  | RemoveProfilePermission ->
      if is_success
      then
        Ok (RemoveProfilePermissionResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some RemoveProfilePermissionResponse.error_of_json))
  | RevokeSignature ->
      if is_success then Ok () else Error (parse_aws_error None)
  | RevokeSigningProfile ->
      if is_success then Ok () else Error (parse_aws_error None)
  | SignPayload ->
      if is_success
      then Ok (SignPayloadResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some SignPayloadResponse.error_of_json))
  | StartSigningJob ->
      if is_success
      then Ok (StartSigningJobResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some StartSigningJobResponse.error_of_json))
  | TagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (TagResourceResponse.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some TagResourceResponse.error_of_json))
  | UntagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (UntagResourceResponse.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some UntagResourceResponse.error_of_json))
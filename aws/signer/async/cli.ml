(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_profile_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileVersion =
         flag "profile-version" (optional string)
           ~doc:"STRING ProfileVersion"
       and revisionId =
         flag "revision-id" (optional string) ~doc:"STRING String"
       and profileName =
         flag "profile-name" (required string) ~doc:"STRING ProfileName"
       and action = flag "action" (required string) ~doc:"STRING String"
       and principal =
         flag "principal" (required string) ~doc:"STRING String"
       and statementId =
         flag "statement-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_profile_permission
           (Values.AddProfilePermissionRequest.make ?profileVersion
              ?revisionId ~profileName ~action ~principal ~statementId ())
           (Some Values.AddProfilePermissionResponse.to_json)
           (Some Values.AddProfilePermissionResponse.error_to_json)])
let cancel_signing_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileName =
         flag "profile-name" (required string) ~doc:"STRING ProfileName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_signing_profile
           (Values.CancelSigningProfileRequest.make ~profileName ()) None
           None])
let describe_signing_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_signing_job
           (Values.DescribeSigningJobRequest.make ~jobId ())
           (Some Values.DescribeSigningJobResponse.to_json)
           (Some Values.DescribeSigningJobResponse.error_to_json)])
let get_signing_platform =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and platformId =
         flag "platform-id" (required string) ~doc:"STRING PlatformId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_signing_platform
           (Values.GetSigningPlatformRequest.make ~platformId ())
           (Some Values.GetSigningPlatformResponse.to_json)
           (Some Values.GetSigningPlatformResponse.error_to_json)])
let get_signing_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileOwner =
         flag "profile-owner" (optional string) ~doc:"STRING AccountId"
       and profileName =
         flag "profile-name" (required string) ~doc:"STRING ProfileName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_signing_profile
           (Values.GetSigningProfileRequest.make ?profileOwner ~profileName
              ()) (Some Values.GetSigningProfileResponse.to_json)
           (Some Values.GetSigningProfileResponse.error_to_json)])
let list_profile_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and profileName =
         flag "profile-name" (required string) ~doc:"STRING ProfileName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_profile_permissions
           (Values.ListProfilePermissionsRequest.make ?nextToken ~profileName
              ()) (Some Values.ListProfilePermissionsResponse.to_json)
           (Some Values.ListProfilePermissionsResponse.error_to_json)])
let list_signing_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON SigningStatus"
       and platformId =
         flag "platform-id" (optional string) ~doc:"STRING PlatformId"
       and requestedBy =
         flag "requested-by" (optional string) ~doc:"STRING RequestedBy"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and isRevoked = flag "is-revoked" (optional bool) ~doc:"BOOL bool"
       and signatureExpiresBefore =
         flag "signature-expires-before" (optional json_arg)
           ~doc:"JSON Timestamp"
       and signatureExpiresAfter =
         flag "signature-expires-after" (optional json_arg)
           ~doc:"JSON Timestamp"
       and jobInvoker =
         flag "job-invoker" (optional string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_signing_jobs
           (Values.ListSigningJobsRequest.make
              ?status:(Option.map ~f:Values.SigningStatus.of_json status)
              ?platformId ?requestedBy ?maxResults ?nextToken ?isRevoked
              ?signatureExpiresBefore:(Option.map ~f:Values.Timestamp.of_json
                                         signatureExpiresBefore)
              ?signatureExpiresAfter:(Option.map ~f:Values.Timestamp.of_json
                                        signatureExpiresAfter) ?jobInvoker ())
           (Some Values.ListSigningJobsResponse.to_json)
           (Some Values.ListSigningJobsResponse.error_to_json)])
let list_signing_platforms =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and category = flag "category" (optional string) ~doc:"STRING String"
       and partner = flag "partner" (optional string) ~doc:"STRING String"
       and target = flag "target" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_signing_platforms
           (Values.ListSigningPlatformsRequest.make ?category ?partner
              ?target ?maxResults ?nextToken ())
           (Some Values.ListSigningPlatformsResponse.to_json)
           (Some Values.ListSigningPlatformsResponse.error_to_json)])
let list_signing_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeCanceled =
         flag "include-canceled" (optional bool) ~doc:"BOOL bool"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and platformId =
         flag "platform-id" (optional string) ~doc:"STRING PlatformId"
       and statuses =
         flag "statuses" (optional json_arg) ~doc:"JSON Statuses" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_signing_profiles
           (Values.ListSigningProfilesRequest.make ?includeCanceled
              ?maxResults ?nextToken ?platformId
              ?statuses:(Option.map ~f:Values.Statuses.of_json statuses) ())
           (Some Values.ListSigningProfilesResponse.to_json)
           (Some Values.ListSigningProfilesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_signing_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and signingMaterial =
         flag "signing-material" (optional json_arg)
           ~doc:"JSON SigningMaterial"
       and signatureValidityPeriod =
         flag "signature-validity-period" (optional json_arg)
           ~doc:"JSON SignatureValidityPeriod"
       and overrides =
         flag "overrides" (optional json_arg)
           ~doc:"JSON SigningPlatformOverrides"
       and signingParameters =
         flag "signing-parameters" (optional json_arg)
           ~doc:"JSON SigningParameters"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and profileName =
         flag "profile-name" (required string) ~doc:"STRING ProfileName"
       and platformId =
         flag "platform-id" (required string) ~doc:"STRING PlatformId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_signing_profile
           (Values.PutSigningProfileRequest.make
              ?signingMaterial:(Option.map ~f:Values.SigningMaterial.of_json
                                  signingMaterial)
              ?signatureValidityPeriod:(Option.map
                                          ~f:Values.SignatureValidityPeriod.of_json
                                          signatureValidityPeriod)
              ?overrides:(Option.map
                            ~f:Values.SigningPlatformOverrides.of_json
                            overrides)
              ?signingParameters:(Option.map
                                    ~f:Values.SigningParameters.of_json
                                    signingParameters)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~profileName
              ~platformId ()) (Some Values.PutSigningProfileResponse.to_json)
           (Some Values.PutSigningProfileResponse.error_to_json)])
let remove_profile_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileName =
         flag "profile-name" (required string) ~doc:"STRING ProfileName"
       and revisionId =
         flag "revision-id" (required string) ~doc:"STRING String"
       and statementId =
         flag "statement-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_profile_permission
           (Values.RemoveProfilePermissionRequest.make ~profileName
              ~revisionId ~statementId ())
           (Some Values.RemoveProfilePermissionResponse.to_json)
           (Some Values.RemoveProfilePermissionResponse.error_to_json)])
let revoke_signature =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobOwner =
         flag "job-owner" (optional string) ~doc:"STRING AccountId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId"
       and reason =
         flag "reason" (required string) ~doc:"STRING RevocationReasonString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.revoke_signature
           (Values.RevokeSignatureRequest.make ?jobOwner ~jobId ~reason ())
           None None])
let revoke_signing_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileName =
         flag "profile-name" (required string) ~doc:"STRING ProfileName"
       and profileVersion =
         flag "profile-version" (required string)
           ~doc:"STRING ProfileVersion"
       and reason =
         flag "reason" (required string) ~doc:"STRING RevocationReasonString"
       and effectiveTime =
         flag "effective-time" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.revoke_signing_profile
           (Values.RevokeSigningProfileRequest.make ~profileName
              ~profileVersion ~reason
              ~effectiveTime:(Values.Timestamp.of_json effectiveTime) ())
           None None])
let start_signing_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileOwner =
         flag "profile-owner" (optional string) ~doc:"STRING AccountId"
       and source = flag "source" (required json_arg) ~doc:"JSON Source"
       and destination =
         flag "destination" (required json_arg) ~doc:"JSON Destination"
       and profileName =
         flag "profile-name" (required string) ~doc:"STRING ProfileName"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_signing_job
           (Values.StartSigningJobRequest.make ?profileOwner
              ~source:(Values.Source.of_json source)
              ~destination:(Values.Destination.of_json destination)
              ~profileName ~clientRequestToken ())
           (Some Values.StartSigningJobResponse.to_json)
           (Some Values.StartSigningJobResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("add-profile-permission", add_profile_permission);
    ("cancel-signing-profile", cancel_signing_profile);
    ("describe-signing-job", describe_signing_job);
    ("get-signing-platform", get_signing_platform);
    ("get-signing-profile", get_signing_profile);
    ("list-profile-permissions", list_profile_permissions);
    ("list-signing-jobs", list_signing_jobs);
    ("list-signing-platforms", list_signing_platforms);
    ("list-signing-profiles", list_signing_profiles);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-signing-profile", put_signing_profile);
    ("remove-profile-permission", remove_profile_permission);
    ("revoke-signature", revoke_signature);
    ("revoke-signing-profile", revoke_signing_profile);
    ("start-signing-job", start_signing_job);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource)]
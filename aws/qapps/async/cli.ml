(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_library_item_review =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and libraryItemId =
         flag "library-item-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_library_item_review
           (Values.AssociateLibraryItemReviewInput.make ~instanceId
              ~libraryItemId ()) None None])
let associate_q_app_with_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and appId = flag "app-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_q_app_with_user
           (Values.AssociateQAppWithUserInput.make ~instanceId ~appId ())
           None None])
let batch_create_category =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and categories =
         flag "categories" (required json_arg)
           ~doc:"JSON BatchCreateCategoryInputCategoryList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_create_category
           (Values.BatchCreateCategoryInput.make ~instanceId
              ~categories:(Values.BatchCreateCategoryInputCategoryList.of_json
                             categories) ()) None None])
let batch_delete_category =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and categories =
         flag "categories" (required json_arg)
           ~doc:"JSON DeleteCategoryInputList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_category
           (Values.BatchDeleteCategoryInput.make ~instanceId
              ~categories:(Values.DeleteCategoryInputList.of_json categories)
              ()) None None])
let batch_update_category =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and categories =
         flag "categories" (required json_arg) ~doc:"JSON CategoryListInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_update_category
           (Values.BatchUpdateCategoryInput.make ~instanceId
              ~categories:(Values.CategoryListInput.of_json categories) ())
           None None])
let create_library_item =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and appId = flag "app-id" (required string) ~doc:"STRING UUID"
       and appVersion =
         flag "app-version" (required int) ~doc:"INT AppVersion"
       and categories =
         flag "categories" (required json_arg) ~doc:"JSON CategoryIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_library_item
           (Values.CreateLibraryItemInput.make ~instanceId ~appId ~appVersion
              ~categories:(Values.CategoryIdList.of_json categories) ())
           (Some Values.CreateLibraryItemOutput.to_json)
           (Some Values.CreateLibraryItemOutput.error_to_json)])
let create_presigned_url =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sessionId = flag "session-id" (optional string) ~doc:"STRING UUID"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and cardId = flag "card-id" (required string) ~doc:"STRING UUID"
       and appId = flag "app-id" (required string) ~doc:"STRING UUID"
       and fileContentsSha256 =
         flag "file-contents-sha256" (required string)
           ~doc:"STRING CreatePresignedUrlInputFileContentsSha256String"
       and fileName =
         flag "file-name" (required string) ~doc:"STRING Filename"
       and scope = flag "scope" (required json_arg) ~doc:"JSON DocumentScope" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_presigned_url
           (Values.CreatePresignedUrlInput.make ?sessionId ~instanceId
              ~cardId ~appId ~fileContentsSha256 ~fileName
              ~scope:(Values.DocumentScope.of_json scope) ())
           (Some Values.CreatePresignedUrlOutput.to_json)
           (Some Values.CreatePresignedUrlOutput.error_to_json)])
let create_q_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and title = flag "title" (required string) ~doc:"STRING Title"
       and appDefinition =
         flag "app-definition" (required json_arg)
           ~doc:"JSON AppDefinitionInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_q_app
           (Values.CreateQAppInput.make ?description
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~instanceId
              ~title
              ~appDefinition:(Values.AppDefinitionInput.of_json appDefinition)
              ()) (Some Values.CreateQAppOutput.to_json)
           (Some Values.CreateQAppOutput.error_to_json)])
let delete_library_item =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and libraryItemId =
         flag "library-item-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_library_item
           (Values.DeleteLibraryItemInput.make ~instanceId ~libraryItemId ())
           None None])
let delete_q_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and appId = flag "app-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_q_app
           (Values.DeleteQAppInput.make ~instanceId ~appId ()) None None])
let describe_q_app_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and appId = flag "app-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_q_app_permissions
           (Values.DescribeQAppPermissionsInput.make ~instanceId ~appId ())
           (Some Values.DescribeQAppPermissionsOutput.to_json)
           (Some Values.DescribeQAppPermissionsOutput.error_to_json)])
let disassociate_library_item_review =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and libraryItemId =
         flag "library-item-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_library_item_review
           (Values.DisassociateLibraryItemReviewInput.make ~instanceId
              ~libraryItemId ()) None None])
let disassociate_q_app_from_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and appId = flag "app-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_q_app_from_user
           (Values.DisassociateQAppFromUserInput.make ~instanceId ~appId ())
           None None])
let export_q_app_session_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and sessionId = flag "session-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.export_q_app_session_data
           (Values.ExportQAppSessionDataInput.make ~instanceId ~sessionId ())
           (Some Values.ExportQAppSessionDataOutput.to_json)
           (Some Values.ExportQAppSessionDataOutput.error_to_json)])
let get_library_item =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (optional string) ~doc:"STRING UUID"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and libraryItemId =
         flag "library-item-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_library_item
           (Values.GetLibraryItemInput.make ?appId ~instanceId ~libraryItemId
              ()) (Some Values.GetLibraryItemOutput.to_json)
           (Some Values.GetLibraryItemOutput.error_to_json)])
let get_q_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appVersion =
         flag "app-version" (optional int) ~doc:"INT AppVersion"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and appId = flag "app-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_q_app
           (Values.GetQAppInput.make ?appVersion ~instanceId ~appId ())
           (Some Values.GetQAppOutput.to_json)
           (Some Values.GetQAppOutput.error_to_json)])
let get_q_app_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and sessionId = flag "session-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_q_app_session
           (Values.GetQAppSessionInput.make ~instanceId ~sessionId ())
           (Some Values.GetQAppSessionOutput.to_json)
           (Some Values.GetQAppSessionOutput.error_to_json)])
let get_q_app_session_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and sessionId = flag "session-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_q_app_session_metadata
           (Values.GetQAppSessionMetadataInput.make ~instanceId ~sessionId ())
           (Some Values.GetQAppSessionMetadataOutput.to_json)
           (Some Values.GetQAppSessionMetadataOutput.error_to_json)])
let import_document =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sessionId = flag "session-id" (optional string) ~doc:"STRING UUID"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and cardId = flag "card-id" (required string) ~doc:"STRING UUID"
       and appId = flag "app-id" (required string) ~doc:"STRING UUID"
       and fileContentsBase64 =
         flag "file-contents-base64" (required string) ~doc:"STRING String"
       and fileName =
         flag "file-name" (required string) ~doc:"STRING Filename"
       and scope = flag "scope" (required json_arg) ~doc:"JSON DocumentScope" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_document
           (Values.ImportDocumentInput.make ?sessionId ~instanceId ~cardId
              ~appId ~fileContentsBase64 ~fileName
              ~scope:(Values.DocumentScope.of_json scope) ())
           (Some Values.ImportDocumentOutput.to_json)
           (Some Values.ImportDocumentOutput.error_to_json)])
let list_categories =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_categories
           (Values.ListCategoriesInput.make ~instanceId ())
           (Some Values.ListCategoriesOutput.to_json)
           (Some Values.ListCategoriesOutput.error_to_json)])
let list_library_items =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit = flag "limit" (optional int) ~doc:"INT PageLimit"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and categoryId =
         flag "category-id" (optional string) ~doc:"STRING UUID"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_library_items
           (Values.ListLibraryItemsInput.make ?limit ?nextToken ?categoryId
              ~instanceId ()) (Some Values.ListLibraryItemsOutput.to_json)
           (Some Values.ListLibraryItemsOutput.error_to_json)])
let list_q_app_session_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and sessionId = flag "session-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_q_app_session_data
           (Values.ListQAppSessionDataInput.make ~instanceId ~sessionId ())
           (Some Values.ListQAppSessionDataOutput.to_json)
           (Some Values.ListQAppSessionDataOutput.error_to_json)])
let list_q_apps =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit = flag "limit" (optional int) ~doc:"INT PageLimit"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_q_apps
           (Values.ListQAppsInput.make ?limit ?nextToken ~instanceId ())
           (Some Values.ListQAppsOutput.to_json)
           (Some Values.ListQAppsOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let predict_q_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and options =
         flag "options" (optional json_arg)
           ~doc:"JSON PredictQAppInputOptions"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.predict_q_app
           (Values.PredictQAppInput.make
              ?options:(Option.map ~f:Values.PredictQAppInputOptions.of_json
                          options) ~instanceId ())
           (Some Values.PredictQAppOutput.to_json)
           (Some Values.PredictQAppOutput.error_to_json)])
let start_q_app_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and initialValues =
         flag "initial-values" (optional json_arg) ~doc:"JSON CardValueList"
       and sessionId =
         flag "session-id" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and appId = flag "app-id" (required string) ~doc:"STRING UUID"
       and appVersion =
         flag "app-version" (required int) ~doc:"INT AppVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_q_app_session
           (Values.StartQAppSessionInput.make
              ?initialValues:(Option.map ~f:Values.CardValueList.of_json
                                initialValues) ?sessionId
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~instanceId
              ~appId ~appVersion ())
           (Some Values.StartQAppSessionOutput.to_json)
           (Some Values.StartQAppSessionOutput.error_to_json)])
let stop_q_app_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and sessionId = flag "session-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_q_app_session
           (Values.StopQAppSessionInput.make ~instanceId ~sessionId ()) None
           None])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_library_item =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON LibraryItemStatus"
       and categories =
         flag "categories" (optional json_arg) ~doc:"JSON CategoryIdList"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and libraryItemId =
         flag "library-item-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_library_item
           (Values.UpdateLibraryItemInput.make
              ?status:(Option.map ~f:Values.LibraryItemStatus.of_json status)
              ?categories:(Option.map ~f:Values.CategoryIdList.of_json
                             categories) ~instanceId ~libraryItemId ())
           (Some Values.UpdateLibraryItemOutput.to_json)
           (Some Values.UpdateLibraryItemOutput.error_to_json)])
let update_library_item_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and isVerified =
         flag "is-verified" (optional bool) ~doc:"BOOL Boolean"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and libraryItemId =
         flag "library-item-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_library_item_metadata
           (Values.UpdateLibraryItemMetadataInput.make ?isVerified
              ~instanceId ~libraryItemId ()) None None])
let update_q_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and title = flag "title" (optional string) ~doc:"STRING Title"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and appDefinition =
         flag "app-definition" (optional json_arg)
           ~doc:"JSON AppDefinitionInput"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and appId = flag "app-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_q_app
           (Values.UpdateQAppInput.make ?title ?description
              ?appDefinition:(Option.map ~f:Values.AppDefinitionInput.of_json
                                appDefinition) ~instanceId ~appId ())
           (Some Values.UpdateQAppOutput.to_json)
           (Some Values.UpdateQAppOutput.error_to_json)])
let update_q_app_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and grantPermissions =
         flag "grant-permissions" (optional json_arg)
           ~doc:"JSON UpdateQAppPermissionsInputGrantPermissionsList"
       and revokePermissions =
         flag "revoke-permissions" (optional json_arg)
           ~doc:"JSON UpdateQAppPermissionsInputRevokePermissionsList"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and appId = flag "app-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_q_app_permissions
           (Values.UpdateQAppPermissionsInput.make
              ?grantPermissions:(Option.map
                                   ~f:Values.UpdateQAppPermissionsInputGrantPermissionsList.of_json
                                   grantPermissions)
              ?revokePermissions:(Option.map
                                    ~f:Values.UpdateQAppPermissionsInputRevokePermissionsList.of_json
                                    revokePermissions) ~instanceId ~appId ())
           (Some Values.UpdateQAppPermissionsOutput.to_json)
           (Some Values.UpdateQAppPermissionsOutput.error_to_json)])
let update_q_app_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and values =
         flag "values" (optional json_arg) ~doc:"JSON CardValueList"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and sessionId = flag "session-id" (required string) ~doc:"STRING UUID" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_q_app_session
           (Values.UpdateQAppSessionInput.make
              ?values:(Option.map ~f:Values.CardValueList.of_json values)
              ~instanceId ~sessionId ())
           (Some Values.UpdateQAppSessionOutput.to_json)
           (Some Values.UpdateQAppSessionOutput.error_to_json)])
let update_q_app_session_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sessionName =
         flag "session-name" (optional string) ~doc:"STRING SessionName"
       and instanceId =
         flag "instance-id" (required string) ~doc:"STRING InstanceId"
       and sessionId = flag "session-id" (required string) ~doc:"STRING UUID"
       and sharingConfiguration =
         flag "sharing-configuration" (required json_arg)
           ~doc:"JSON SessionSharingConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_q_app_session_metadata
           (Values.UpdateQAppSessionMetadataInput.make ?sessionName
              ~instanceId ~sessionId
              ~sharingConfiguration:(Values.SessionSharingConfiguration.of_json
                                       sharingConfiguration) ())
           (Some Values.UpdateQAppSessionMetadataOutput.to_json)
           (Some Values.UpdateQAppSessionMetadataOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-library-item-review", associate_library_item_review);
    ("associate-q-app-with-user", associate_q_app_with_user);
    ("batch-create-category", batch_create_category);
    ("batch-delete-category", batch_delete_category);
    ("batch-update-category", batch_update_category);
    ("create-library-item", create_library_item);
    ("create-presigned-url", create_presigned_url);
    ("create-q-app", create_q_app);
    ("delete-library-item", delete_library_item);
    ("delete-q-app", delete_q_app);
    ("describe-q-app-permissions", describe_q_app_permissions);
    ("disassociate-library-item-review", disassociate_library_item_review);
    ("disassociate-q-app-from-user", disassociate_q_app_from_user);
    ("export-q-app-session-data", export_q_app_session_data);
    ("get-library-item", get_library_item);
    ("get-q-app", get_q_app);
    ("get-q-app-session", get_q_app_session);
    ("get-q-app-session-metadata", get_q_app_session_metadata);
    ("import-document", import_document);
    ("list-categories", list_categories);
    ("list-library-items", list_library_items);
    ("list-q-app-session-data", list_q_app_session_data);
    ("list-q-apps", list_q_apps);
    ("list-tags-for-resource", list_tags_for_resource);
    ("predict-q-app", predict_q_app);
    ("start-q-app-session", start_q_app_session);
    ("stop-q-app-session", stop_q_app_session);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-library-item", update_library_item);
    ("update-library-item-metadata", update_library_item_metadata);
    ("update-q-app", update_q_app);
    ("update-q-app-permissions", update_q_app_permissions);
    ("update-q-app-session", update_q_app_session);
    ("update-q-app-session-metadata", update_q_app_session_metadata)]
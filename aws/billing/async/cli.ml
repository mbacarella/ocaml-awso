(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_source_views =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING BillingViewArn"
       and sourceViews =
         flag "source-views" (required json_arg)
           ~doc:"JSON BillingViewSourceViewsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_source_views
           (Values.AssociateSourceViewsRequest.make ~arn
              ~sourceViews:(Values.BillingViewSourceViewsList.of_json
                              sourceViews) ())
           (Some Values.AssociateSourceViewsResponse.to_json)
           (Some Values.AssociateSourceViewsResponse.error_to_json)])
let create_billing_view =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING BillingViewDescription"
       and dataFilterExpression =
         flag "data-filter-expression" (optional json_arg)
           ~doc:"JSON Expression"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and resourceTags =
         flag "resource-tags" (optional json_arg) ~doc:"JSON ResourceTagList"
       and name = flag "name" (required string) ~doc:"STRING BillingViewName"
       and sourceViews =
         flag "source-views" (required json_arg)
           ~doc:"JSON BillingViewSourceViewsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_billing_view
           (Values.CreateBillingViewRequest.make ?description
              ?dataFilterExpression:(Option.map ~f:Values.Expression.of_json
                                       dataFilterExpression) ?clientToken
              ?resourceTags:(Option.map ~f:Values.ResourceTagList.of_json
                               resourceTags) ~name
              ~sourceViews:(Values.BillingViewSourceViewsList.of_json
                              sourceViews) ())
           (Some Values.CreateBillingViewResponse.to_json)
           (Some Values.CreateBillingViewResponse.error_to_json)])
let delete_billing_view =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and force = flag "force" (optional bool) ~doc:"BOOL Boolean"
       and arn = flag "arn" (required string) ~doc:"STRING BillingViewArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_billing_view
           (Values.DeleteBillingViewRequest.make ?force ~arn ())
           (Some Values.DeleteBillingViewResponse.to_json)
           (Some Values.DeleteBillingViewResponse.error_to_json)])
let disassociate_source_views =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING BillingViewArn"
       and sourceViews =
         flag "source-views" (required json_arg)
           ~doc:"JSON BillingViewSourceViewsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_source_views
           (Values.DisassociateSourceViewsRequest.make ~arn
              ~sourceViews:(Values.BillingViewSourceViewsList.of_json
                              sourceViews) ())
           (Some Values.DisassociateSourceViewsResponse.to_json)
           (Some Values.DisassociateSourceViewsResponse.error_to_json)])
let get_billing_view =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING BillingViewArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_billing_view (Values.GetBillingViewRequest.make ~arn ())
           (Some Values.GetBillingViewResponse.to_json)
           (Some Values.GetBillingViewResponse.error_to_json)])
let get_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_policy
           (Values.GetResourcePolicyRequest.make ~resourceArn ())
           (Some Values.GetResourcePolicyResponse.to_json)
           (Some Values.GetResourcePolicyResponse.error_to_json)])
let list_billing_views =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and activeTimeRange =
         flag "active-time-range" (optional json_arg)
           ~doc:"JSON ActiveTimeRange"
       and arns =
         flag "arns" (optional json_arg) ~doc:"JSON BillingViewArnList"
       and billingViewTypes =
         flag "billing-view-types" (optional json_arg)
           ~doc:"JSON BillingViewTypeList"
       and names =
         flag "names" (optional json_arg) ~doc:"JSON StringSearches"
       and ownerAccountId =
         flag "owner-account-id" (optional string) ~doc:"STRING AccountId"
       and sourceAccountId =
         flag "source-account-id" (optional string) ~doc:"STRING AccountId"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BillingViewsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PageToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_billing_views
           (Values.ListBillingViewsRequest.make
              ?activeTimeRange:(Option.map ~f:Values.ActiveTimeRange.of_json
                                  activeTimeRange)
              ?arns:(Option.map ~f:Values.BillingViewArnList.of_json arns)
              ?billingViewTypes:(Option.map
                                   ~f:Values.BillingViewTypeList.of_json
                                   billingViewTypes)
              ?names:(Option.map ~f:Values.StringSearches.of_json names)
              ?ownerAccountId ?sourceAccountId ?maxResults ?nextToken ())
           (Some Values.ListBillingViewsResponse.to_json)
           (Some Values.ListBillingViewsResponse.error_to_json)])
let list_source_views_for_billing_view =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BillingViewsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PageToken"
       and arn = flag "arn" (required string) ~doc:"STRING BillingViewArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_source_views_for_billing_view
           (Values.ListSourceViewsForBillingViewRequest.make ?maxResults
              ?nextToken ~arn ())
           (Some Values.ListSourceViewsForBillingViewResponse.to_json)
           (Some Values.ListSourceViewsForBillingViewResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and resourceTags =
         flag "resource-tags" (required json_arg) ~doc:"JSON ResourceTagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~resourceTags:(Values.ResourceTagList.of_json resourceTags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and resourceTagKeys =
         flag "resource-tag-keys" (required json_arg)
           ~doc:"JSON ResourceTagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~resourceTagKeys:(Values.ResourceTagKeyList.of_json
                                  resourceTagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_billing_view =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING BillingViewName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING BillingViewDescription"
       and dataFilterExpression =
         flag "data-filter-expression" (optional json_arg)
           ~doc:"JSON Expression"
       and arn = flag "arn" (required string) ~doc:"STRING BillingViewArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_billing_view
           (Values.UpdateBillingViewRequest.make ?name ?description
              ?dataFilterExpression:(Option.map ~f:Values.Expression.of_json
                                       dataFilterExpression) ~arn ())
           (Some Values.UpdateBillingViewResponse.to_json)
           (Some Values.UpdateBillingViewResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-source-views", associate_source_views);
    ("create-billing-view", create_billing_view);
    ("delete-billing-view", delete_billing_view);
    ("disassociate-source-views", disassociate_source_views);
    ("get-billing-view", get_billing_view);
    ("get-resource-policy", get_resource_policy);
    ("list-billing-views", list_billing_views);
    ("list-source-views-for-billing-view",
      list_source_views_for_billing_view);
    ("list-tags-for-resource", list_tags_for_resource);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-billing-view", update_billing_view)]
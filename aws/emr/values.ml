(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.emr
let apiVersion = "2009-03-31"
let endpointPrefix = "elasticmapreduce"
let serviceFullName = "Amazon EMR"
let signatureVersion = "v4"
let protocol = "json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let serviceAbbreviation = "Amazon EMR"
let targetPrefix = "ElasticMapReduce"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module ActionOnFailure =
  struct
    type nonrec t =
      | TERMINATE_JOB_FLOW 
      | TERMINATE_CLUSTER 
      | CANCEL_AND_WAIT 
      | CONTINUE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | TERMINATE_JOB_FLOW -> "TERMINATE_JOB_FLOW"
      | TERMINATE_CLUSTER -> "TERMINATE_CLUSTER"
      | CANCEL_AND_WAIT -> "CANCEL_AND_WAIT"
      | CONTINUE -> "CONTINUE"
      | Non_static_id s -> s
    let of_string =
      function
      | "TERMINATE_JOB_FLOW" -> TERMINATE_JOB_FLOW
      | "TERMINATE_CLUSTER" -> TERMINATE_CLUSTER
      | "CANCEL_AND_WAIT" -> CANCEL_AND_WAIT
      | "CONTINUE" -> CONTINUE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ActionOnFailure" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ActionOnFailure" j)
    let to_json = simple_to_json to_value
  end
module XmlStringMaxLen256 =
  struct
    type nonrec t = string
    let context_ = "XmlStringMaxLen256"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[\\u0020-\\uD7FF\\uE000-\\uFFFD\\uD800\\uDC00-\\uDBFF\\uDFFF\\r\\n\\t]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"XmlStringMaxLen256" j
    let to_json = simple_to_json to_value
  end
module WholeNumber =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for WholeNumber" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module NonNegativeDouble =
  struct
    type nonrec t = float
    let make i =
      let open Result in ok_or_failwith (check_float_min i ~min:0.); i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module InstanceType =
  struct
    type nonrec t = string
    let context_ = "InstanceType"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[\\u0020-\\uD7FF\\uE000-\\uFFFD\\uD800\\uDC00-\\uDBFF\\uDFFF\\r\\n\\t]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"InstanceType" j
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module VolumeSpecification =
  struct
    type nonrec t =
      {
      volumeType: String_.t ;
      iops: Integer.t option ;
      sizeInGB: Integer.t }
    let context_ = "VolumeSpecification"
    let make ?iops =
      fun ~volumeType ->
        fun ~sizeInGB -> fun () -> { iops; volumeType; sizeInGB }
    let to_value x =
      structure_to_value
        [("VolumeType", (Some (String_.to_value x.volumeType)));
        ("Iops", (Option.map x.iops ~f:Integer.to_value));
        ("SizeInGB", (Some (Integer.to_value x.sizeInGB)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sizeInGB =
        Integer.of_xml (Xml.child_exn ~context:context_ xml_arg0 "SizeInGB") in
      let iops = (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Iops") in
      let volumeType =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VolumeType") in
      make ~sizeInGB ?iops ~volumeType ()
    let of_json json =
      let sizeInGB = field_map_exn json "SizeInGB" Integer.of_json in
      let iops = field_map json "Iops" Integer.of_json in
      let volumeType = field_map_exn json "VolumeType" String_.of_json in
      make ~sizeInGB ?iops ~volumeType ()
    let to_json v = composed_to_json to_value v
  end
module EbsBlockDeviceConfig =
  struct
    type nonrec t =
      {
      volumeSpecification: VolumeSpecification.t ;
      volumesPerInstance: Integer.t option }
    let context_ = "EbsBlockDeviceConfig"
    let make ?volumesPerInstance =
      fun ~volumeSpecification ->
        fun () -> { volumesPerInstance; volumeSpecification }
    let to_value x =
      structure_to_value
        [("VolumeSpecification",
           (Some (VolumeSpecification.to_value x.volumeSpecification)));
        ("VolumesPerInstance",
          (Option.map x.volumesPerInstance ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let volumesPerInstance =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "VolumesPerInstance") in
      let volumeSpecification =
        VolumeSpecification.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VolumeSpecification") in
      make ?volumesPerInstance ~volumeSpecification ()
    let of_json json =
      let volumesPerInstance =
        field_map json "VolumesPerInstance" Integer.of_json in
      let volumeSpecification =
        field_map_exn json "VolumeSpecification" VolumeSpecification.of_json in
      make ?volumesPerInstance ~volumeSpecification ()
    let to_json v = composed_to_json to_value v
  end
module EbsBlockDeviceConfigList =
  struct
    type nonrec t = EbsBlockDeviceConfig.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:EbsBlockDeviceConfig.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EbsBlockDeviceConfig.of_xml)
    let of_json j =
      list_of_json ~kind:"EbsBlockDeviceConfigList"
        ~of_json:EbsBlockDeviceConfig.of_json j
    let to_json v = composed_to_json to_value v
  end
module BooleanObject =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module EbsConfiguration =
  struct
    type nonrec t =
      {
      ebsBlockDeviceConfigs: EbsBlockDeviceConfigList.t option ;
      ebsOptimized: BooleanObject.t option }
    let make ?ebsBlockDeviceConfigs =
      fun ?ebsOptimized -> fun () -> { ebsBlockDeviceConfigs; ebsOptimized }
    let to_value x =
      structure_to_value
        [("EbsBlockDeviceConfigs",
           (Option.map x.ebsBlockDeviceConfigs
              ~f:EbsBlockDeviceConfigList.to_value));
        ("EbsOptimized",
          (Option.map x.ebsOptimized ~f:BooleanObject.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ebsOptimized =
        (Option.map ~f:BooleanObject.of_xml)
          (Xml.child xml_arg0 "EbsOptimized") in
      let ebsBlockDeviceConfigs =
        (Option.map ~f:EbsBlockDeviceConfigList.of_xml)
          (Xml.child xml_arg0 "EbsBlockDeviceConfigs") in
      make ?ebsOptimized ?ebsBlockDeviceConfigs ()
    let of_json json =
      let ebsOptimized = field_map json "EbsOptimized" BooleanObject.of_json in
      let ebsBlockDeviceConfigs =
        field_map json "EbsBlockDeviceConfigs"
          EbsBlockDeviceConfigList.of_json in
      make ?ebsOptimized ?ebsBlockDeviceConfigs ()
    let to_json v = composed_to_json to_value v
  end
module StringMap =
  struct
    type nonrec t = (String_.t * String_.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((String_.of_string chopped),
                              (String_.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x -> (String_.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module rec
  Configuration:sig
                  type nonrec t =
                    {
                    classification: String_.t option ;
                    configurations: ConfigurationList.t option ;
                    properties: StringMap.t option }
                  val make :
                    ?classification:String_.t ->
                      ?configurations:ConfigurationList.t ->
                        ?properties:StringMap.t -> unit -> t
                  val to_value : t -> Botodata.value
                  val to_query : t -> Client.Query.t
                  val of_xml : Xml.t -> t
                  val of_json : Yojson.Safe.t -> t
                  val to_json : t -> Yojson.Safe.t
                end =
  struct
    type nonrec t =
      {
      classification: String_.t option ;
      configurations: ConfigurationList.t option ;
      properties: StringMap.t option }
    let make ?classification =
      fun ?configurations ->
        fun ?properties ->
          fun () -> { classification; configurations; properties }
    let to_value x =
      structure_to_value
        [("Classification",
           (Option.map x.classification ~f:String_.to_value));
        ("Configurations",
          (Option.map x.configurations ~f:ConfigurationList.to_value));
        ("Properties", (Option.map x.properties ~f:StringMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let properties =
        (Option.map ~f:StringMap.of_xml) (Xml.child xml_arg0 "Properties") in
      let configurations =
        (Option.map ~f:ConfigurationList.of_xml)
          (Xml.child xml_arg0 "Configurations") in
      let classification =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Classification") in
      make ?properties ?configurations ?classification ()
    let of_json json =
      let properties = field_map json "Properties" StringMap.of_json in
      let configurations =
        field_map json "Configurations" ConfigurationList.of_json in
      let classification = field_map json "Classification" String_.of_json in
      make ?properties ?configurations ?classification ()
    let to_json v = composed_to_json to_value v
  end
 and
  ConfigurationList:sig
                      type nonrec t = Configuration.t list
                      val make : Configuration.t list -> t
                      val to_value : t -> Botodata.value
                      val to_query : t -> Client.Query.t
                      val of_xml : Xml.t -> Configuration.t list
                      val of_json : Yojson.Safe.t -> t
                      val to_json : t -> Yojson.Safe.t
                      val to_header : t -> string
                    end =
  struct
    type nonrec t = Configuration.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Configuration.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Configuration.of_xml)
    let of_json j =
      list_of_json ~kind:"ConfigurationList" ~of_json:Configuration.of_json j
    let to_json v = composed_to_json to_value v
  end
module InstanceTypeConfig =
  struct
    type nonrec t =
      {
      instanceType: InstanceType.t ;
      weightedCapacity: WholeNumber.t option ;
      bidPrice: XmlStringMaxLen256.t option ;
      bidPriceAsPercentageOfOnDemandPrice: NonNegativeDouble.t option ;
      ebsConfiguration: EbsConfiguration.t option ;
      configurations: ConfigurationList.t option ;
      customAmiId: XmlStringMaxLen256.t option }
    let context_ = "InstanceTypeConfig"
    let make ?weightedCapacity =
      fun ?bidPrice ->
        fun ?bidPriceAsPercentageOfOnDemandPrice ->
          fun ?ebsConfiguration ->
            fun ?configurations ->
              fun ?customAmiId ->
                fun ~instanceType ->
                  fun () ->
                    {
                      weightedCapacity;
                      bidPrice;
                      bidPriceAsPercentageOfOnDemandPrice;
                      ebsConfiguration;
                      configurations;
                      customAmiId;
                      instanceType
                    }
    let to_value x =
      structure_to_value
        [("InstanceType", (Some (InstanceType.to_value x.instanceType)));
        ("WeightedCapacity",
          (Option.map x.weightedCapacity ~f:WholeNumber.to_value));
        ("BidPrice", (Option.map x.bidPrice ~f:XmlStringMaxLen256.to_value));
        ("BidPriceAsPercentageOfOnDemandPrice",
          (Option.map x.bidPriceAsPercentageOfOnDemandPrice
             ~f:NonNegativeDouble.to_value));
        ("EbsConfiguration",
          (Option.map x.ebsConfiguration ~f:EbsConfiguration.to_value));
        ("Configurations",
          (Option.map x.configurations ~f:ConfigurationList.to_value));
        ("CustomAmiId",
          (Option.map x.customAmiId ~f:XmlStringMaxLen256.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let customAmiId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "CustomAmiId") in
      let configurations =
        (Option.map ~f:ConfigurationList.of_xml)
          (Xml.child xml_arg0 "Configurations") in
      let ebsConfiguration =
        (Option.map ~f:EbsConfiguration.of_xml)
          (Xml.child xml_arg0 "EbsConfiguration") in
      let bidPriceAsPercentageOfOnDemandPrice =
        (Option.map ~f:NonNegativeDouble.of_xml)
          (Xml.child xml_arg0 "BidPriceAsPercentageOfOnDemandPrice") in
      let bidPrice =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "BidPrice") in
      let weightedCapacity =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "WeightedCapacity") in
      let instanceType =
        InstanceType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceType") in
      make ?customAmiId ?configurations ?ebsConfiguration
        ?bidPriceAsPercentageOfOnDemandPrice ?bidPrice ?weightedCapacity
        ~instanceType ()
    let of_json json =
      let customAmiId =
        field_map json "CustomAmiId" XmlStringMaxLen256.of_json in
      let configurations =
        field_map json "Configurations" ConfigurationList.of_json in
      let ebsConfiguration =
        field_map json "EbsConfiguration" EbsConfiguration.of_json in
      let bidPriceAsPercentageOfOnDemandPrice =
        field_map json "BidPriceAsPercentageOfOnDemandPrice"
          NonNegativeDouble.of_json in
      let bidPrice = field_map json "BidPrice" XmlStringMaxLen256.of_json in
      let weightedCapacity =
        field_map json "WeightedCapacity" WholeNumber.of_json in
      let instanceType =
        field_map_exn json "InstanceType" InstanceType.of_json in
      make ?customAmiId ?configurations ?ebsConfiguration
        ?bidPriceAsPercentageOfOnDemandPrice ?bidPrice ?weightedCapacity
        ~instanceType ()
    let to_json v = composed_to_json to_value v
  end
module InstanceTypeConfigList =
  struct
    type nonrec t = InstanceTypeConfig.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:InstanceTypeConfig.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InstanceTypeConfig.of_xml)
    let of_json j =
      list_of_json ~kind:"InstanceTypeConfigList"
        ~of_json:InstanceTypeConfig.of_json j
    let to_json v = composed_to_json to_value v
  end
module InstanceFleetType =
  struct
    type nonrec t =
      | MASTER 
      | CORE 
      | TASK 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | MASTER -> "MASTER"
      | CORE -> "CORE"
      | TASK -> "TASK"
      | Non_static_id s -> s
    let of_string =
      function
      | "MASTER" -> MASTER
      | "CORE" -> CORE
      | "TASK" -> TASK
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration InstanceFleetType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"InstanceFleetType" j)
    let to_json = simple_to_json to_value
  end
module SpotProvisioningTimeoutAction =
  struct
    type nonrec t =
      | SWITCH_TO_ON_DEMAND 
      | TERMINATE_CLUSTER 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SWITCH_TO_ON_DEMAND -> "SWITCH_TO_ON_DEMAND"
      | TERMINATE_CLUSTER -> "TERMINATE_CLUSTER"
      | Non_static_id s -> s
    let of_string =
      function
      | "SWITCH_TO_ON_DEMAND" -> SWITCH_TO_ON_DEMAND
      | "TERMINATE_CLUSTER" -> TERMINATE_CLUSTER
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration SpotProvisioningTimeoutAction"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"SpotProvisioningTimeoutAction" j)
    let to_json = simple_to_json to_value
  end
module SpotProvisioningAllocationStrategy =
  struct
    type nonrec t =
      | Capacity_optimized 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Capacity_optimized -> "capacity-optimized"
      | Non_static_id s -> s
    let of_string =
      function
      | "capacity-optimized" -> Capacity_optimized
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration SpotProvisioningAllocationStrategy"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"SpotProvisioningAllocationStrategy" j)
    let to_json = simple_to_json to_value
  end
module SpotProvisioningSpecification =
  struct
    type nonrec t =
      {
      timeoutDurationMinutes: WholeNumber.t ;
      timeoutAction: SpotProvisioningTimeoutAction.t ;
      blockDurationMinutes: WholeNumber.t option ;
      allocationStrategy: SpotProvisioningAllocationStrategy.t option }
    let context_ = "SpotProvisioningSpecification"
    let make ?blockDurationMinutes =
      fun ?allocationStrategy ->
        fun ~timeoutDurationMinutes ->
          fun ~timeoutAction ->
            fun () ->
              {
                blockDurationMinutes;
                allocationStrategy;
                timeoutDurationMinutes;
                timeoutAction
              }
    let to_value x =
      structure_to_value
        [("TimeoutDurationMinutes",
           (Some (WholeNumber.to_value x.timeoutDurationMinutes)));
        ("TimeoutAction",
          (Some (SpotProvisioningTimeoutAction.to_value x.timeoutAction)));
        ("BlockDurationMinutes",
          (Option.map x.blockDurationMinutes ~f:WholeNumber.to_value));
        ("AllocationStrategy",
          (Option.map x.allocationStrategy
             ~f:SpotProvisioningAllocationStrategy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let allocationStrategy =
        (Option.map ~f:SpotProvisioningAllocationStrategy.of_xml)
          (Xml.child xml_arg0 "AllocationStrategy") in
      let blockDurationMinutes =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "BlockDurationMinutes") in
      let timeoutAction =
        SpotProvisioningTimeoutAction.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TimeoutAction") in
      let timeoutDurationMinutes =
        WholeNumber.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TimeoutDurationMinutes") in
      make ?allocationStrategy ?blockDurationMinutes ~timeoutAction
        ~timeoutDurationMinutes ()
    let of_json json =
      let allocationStrategy =
        field_map json "AllocationStrategy"
          SpotProvisioningAllocationStrategy.of_json in
      let blockDurationMinutes =
        field_map json "BlockDurationMinutes" WholeNumber.of_json in
      let timeoutAction =
        field_map_exn json "TimeoutAction"
          SpotProvisioningTimeoutAction.of_json in
      let timeoutDurationMinutes =
        field_map_exn json "TimeoutDurationMinutes" WholeNumber.of_json in
      make ?allocationStrategy ?blockDurationMinutes ~timeoutAction
        ~timeoutDurationMinutes ()
    let to_json v = composed_to_json to_value v
  end
module OnDemandProvisioningAllocationStrategy =
  struct
    type nonrec t =
      | Lowest_price 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | Lowest_price -> "lowest-price" | Non_static_id s -> s
    let of_string =
      function | "lowest-price" -> Lowest_price | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml
           ~kind:"enumeration OnDemandProvisioningAllocationStrategy"
           xml_arg0)
    let of_json j =
      of_string
        (string_of_json ~kind:"OnDemandProvisioningAllocationStrategy" j)
    let to_json = simple_to_json to_value
  end
module OnDemandCapacityReservationUsageStrategy =
  struct
    type nonrec t =
      | Use_capacity_reservations_first 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Use_capacity_reservations_first -> "use-capacity-reservations-first"
      | Non_static_id s -> s
    let of_string =
      function
      | "use-capacity-reservations-first" -> Use_capacity_reservations_first
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml
           ~kind:"enumeration OnDemandCapacityReservationUsageStrategy"
           xml_arg0)
    let of_json j =
      of_string
        (string_of_json ~kind:"OnDemandCapacityReservationUsageStrategy" j)
    let to_json = simple_to_json to_value
  end
module OnDemandCapacityReservationPreference =
  struct
    type nonrec t =
      | Open 
      | None 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | Open -> "open" | None -> "none" | Non_static_id s -> s
    let of_string =
      function | "open" -> Open | "none" -> None | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml
           ~kind:"enumeration OnDemandCapacityReservationPreference" xml_arg0)
    let of_json j =
      of_string
        (string_of_json ~kind:"OnDemandCapacityReservationPreference" j)
    let to_json = simple_to_json to_value
  end
module OnDemandCapacityReservationOptions =
  struct
    type nonrec t =
      {
      usageStrategy: OnDemandCapacityReservationUsageStrategy.t option ;
      capacityReservationPreference:
        OnDemandCapacityReservationPreference.t option ;
      capacityReservationResourceGroupArn: XmlStringMaxLen256.t option }
    let make ?usageStrategy =
      fun ?capacityReservationPreference ->
        fun ?capacityReservationResourceGroupArn ->
          fun () ->
            {
              usageStrategy;
              capacityReservationPreference;
              capacityReservationResourceGroupArn
            }
    let to_value x =
      structure_to_value
        [("UsageStrategy",
           (Option.map x.usageStrategy
              ~f:OnDemandCapacityReservationUsageStrategy.to_value));
        ("CapacityReservationPreference",
          (Option.map x.capacityReservationPreference
             ~f:OnDemandCapacityReservationPreference.to_value));
        ("CapacityReservationResourceGroupArn",
          (Option.map x.capacityReservationResourceGroupArn
             ~f:XmlStringMaxLen256.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let capacityReservationResourceGroupArn =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "CapacityReservationResourceGroupArn") in
      let capacityReservationPreference =
        (Option.map ~f:OnDemandCapacityReservationPreference.of_xml)
          (Xml.child xml_arg0 "CapacityReservationPreference") in
      let usageStrategy =
        (Option.map ~f:OnDemandCapacityReservationUsageStrategy.of_xml)
          (Xml.child xml_arg0 "UsageStrategy") in
      make ?capacityReservationResourceGroupArn
        ?capacityReservationPreference ?usageStrategy ()
    let of_json json =
      let capacityReservationResourceGroupArn =
        field_map json "CapacityReservationResourceGroupArn"
          XmlStringMaxLen256.of_json in
      let capacityReservationPreference =
        field_map json "CapacityReservationPreference"
          OnDemandCapacityReservationPreference.of_json in
      let usageStrategy =
        field_map json "UsageStrategy"
          OnDemandCapacityReservationUsageStrategy.of_json in
      make ?capacityReservationResourceGroupArn
        ?capacityReservationPreference ?usageStrategy ()
    let to_json v = composed_to_json to_value v
  end
module OnDemandProvisioningSpecification =
  struct
    type nonrec t =
      {
      allocationStrategy: OnDemandProvisioningAllocationStrategy.t ;
      capacityReservationOptions: OnDemandCapacityReservationOptions.t option }
    let context_ = "OnDemandProvisioningSpecification"
    let make ?capacityReservationOptions =
      fun ~allocationStrategy ->
        fun () -> { capacityReservationOptions; allocationStrategy }
    let to_value x =
      structure_to_value
        [("AllocationStrategy",
           (Some
              (OnDemandProvisioningAllocationStrategy.to_value
                 x.allocationStrategy)));
        ("CapacityReservationOptions",
          (Option.map x.capacityReservationOptions
             ~f:OnDemandCapacityReservationOptions.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let capacityReservationOptions =
        (Option.map ~f:OnDemandCapacityReservationOptions.of_xml)
          (Xml.child xml_arg0 "CapacityReservationOptions") in
      let allocationStrategy =
        OnDemandProvisioningAllocationStrategy.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AllocationStrategy") in
      make ?capacityReservationOptions ~allocationStrategy ()
    let of_json json =
      let capacityReservationOptions =
        field_map json "CapacityReservationOptions"
          OnDemandCapacityReservationOptions.of_json in
      let allocationStrategy =
        field_map_exn json "AllocationStrategy"
          OnDemandProvisioningAllocationStrategy.of_json in
      make ?capacityReservationOptions ~allocationStrategy ()
    let to_json v = composed_to_json to_value v
  end
module InstanceFleetProvisioningSpecifications =
  struct
    type nonrec t =
      {
      spotSpecification: SpotProvisioningSpecification.t option ;
      onDemandSpecification: OnDemandProvisioningSpecification.t option }
    let make ?spotSpecification =
      fun ?onDemandSpecification ->
        fun () -> { spotSpecification; onDemandSpecification }
    let to_value x =
      structure_to_value
        [("SpotSpecification",
           (Option.map x.spotSpecification
              ~f:SpotProvisioningSpecification.to_value));
        ("OnDemandSpecification",
          (Option.map x.onDemandSpecification
             ~f:OnDemandProvisioningSpecification.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let onDemandSpecification =
        (Option.map ~f:OnDemandProvisioningSpecification.of_xml)
          (Xml.child xml_arg0 "OnDemandSpecification") in
      let spotSpecification =
        (Option.map ~f:SpotProvisioningSpecification.of_xml)
          (Xml.child xml_arg0 "SpotSpecification") in
      make ?onDemandSpecification ?spotSpecification ()
    let of_json json =
      let onDemandSpecification =
        field_map json "OnDemandSpecification"
          OnDemandProvisioningSpecification.of_json in
      let spotSpecification =
        field_map json "SpotSpecification"
          SpotProvisioningSpecification.of_json in
      make ?onDemandSpecification ?spotSpecification ()
    let to_json v = composed_to_json to_value v
  end
module InstanceFleetConfig =
  struct
    type nonrec t =
      {
      name: XmlStringMaxLen256.t option ;
      instanceFleetType: InstanceFleetType.t ;
      targetOnDemandCapacity: WholeNumber.t option ;
      targetSpotCapacity: WholeNumber.t option ;
      instanceTypeConfigs: InstanceTypeConfigList.t option ;
      launchSpecifications: InstanceFleetProvisioningSpecifications.t option }
    let context_ = "InstanceFleetConfig"
    let make ?name =
      fun ?targetOnDemandCapacity ->
        fun ?targetSpotCapacity ->
          fun ?instanceTypeConfigs ->
            fun ?launchSpecifications ->
              fun ~instanceFleetType ->
                fun () ->
                  {
                    name;
                    targetOnDemandCapacity;
                    targetSpotCapacity;
                    instanceTypeConfigs;
                    launchSpecifications;
                    instanceFleetType
                  }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:XmlStringMaxLen256.to_value));
        ("InstanceFleetType",
          (Some (InstanceFleetType.to_value x.instanceFleetType)));
        ("TargetOnDemandCapacity",
          (Option.map x.targetOnDemandCapacity ~f:WholeNumber.to_value));
        ("TargetSpotCapacity",
          (Option.map x.targetSpotCapacity ~f:WholeNumber.to_value));
        ("InstanceTypeConfigs",
          (Option.map x.instanceTypeConfigs
             ~f:InstanceTypeConfigList.to_value));
        ("LaunchSpecifications",
          (Option.map x.launchSpecifications
             ~f:InstanceFleetProvisioningSpecifications.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let launchSpecifications =
        (Option.map ~f:InstanceFleetProvisioningSpecifications.of_xml)
          (Xml.child xml_arg0 "LaunchSpecifications") in
      let instanceTypeConfigs =
        (Option.map ~f:InstanceTypeConfigList.of_xml)
          (Xml.child xml_arg0 "InstanceTypeConfigs") in
      let targetSpotCapacity =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "TargetSpotCapacity") in
      let targetOnDemandCapacity =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "TargetOnDemandCapacity") in
      let instanceFleetType =
        InstanceFleetType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceFleetType") in
      let name =
        (Option.map ~f:XmlStringMaxLen256.of_xml) (Xml.child xml_arg0 "Name") in
      make ?launchSpecifications ?instanceTypeConfigs ?targetSpotCapacity
        ?targetOnDemandCapacity ~instanceFleetType ?name ()
    let of_json json =
      let launchSpecifications =
        field_map json "LaunchSpecifications"
          InstanceFleetProvisioningSpecifications.of_json in
      let instanceTypeConfigs =
        field_map json "InstanceTypeConfigs" InstanceTypeConfigList.of_json in
      let targetSpotCapacity =
        field_map json "TargetSpotCapacity" WholeNumber.of_json in
      let targetOnDemandCapacity =
        field_map json "TargetOnDemandCapacity" WholeNumber.of_json in
      let instanceFleetType =
        field_map_exn json "InstanceFleetType" InstanceFleetType.of_json in
      let name = field_map json "Name" XmlStringMaxLen256.of_json in
      make ?launchSpecifications ?instanceTypeConfigs ?targetSpotCapacity
        ?targetOnDemandCapacity ~instanceFleetType ?name ()
    let to_json v = composed_to_json to_value v
  end
module AddInstanceFleetInput =
  struct
    type nonrec t =
      {
      clusterId: XmlStringMaxLen256.t ;
      instanceFleet: InstanceFleetConfig.t }
    let context_ = "AddInstanceFleetInput"
    let make ~clusterId =
      fun ~instanceFleet -> fun () -> { clusterId; instanceFleet }
    let to_value x =
      structure_to_value
        [("ClusterId", (Some (XmlStringMaxLen256.to_value x.clusterId)));
        ("InstanceFleet",
          (Some (InstanceFleetConfig.to_value x.instanceFleet)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceFleet =
        InstanceFleetConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceFleet") in
      let clusterId =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClusterId") in
      make ~instanceFleet ~clusterId ()
    let of_json json =
      let instanceFleet =
        field_map_exn json "InstanceFleet" InstanceFleetConfig.of_json in
      let clusterId =
        field_map_exn json "ClusterId" XmlStringMaxLen256.of_json in
      make ~instanceFleet ~clusterId ()
    let to_json v = composed_to_json to_value v
  end
module ErrorMessage =
  struct
    type nonrec t = string
    let context_ = "ErrorMessage"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorMessage" j
    let to_json = simple_to_json to_value
  end
module ErrorCode =
  struct
    type nonrec t = string
    let context_ = "ErrorCode"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorCode" j
    let to_json = simple_to_json to_value
  end
module InvalidRequestException =
  struct
    type nonrec t =
      {
      errorCode: ErrorCode.t option ;
      message: ErrorMessage.t option }
    let make ?errorCode = fun ?message -> fun () -> { errorCode; message }
    let to_value x =
      structure_to_value
        [("ErrorCode", (Option.map x.errorCode ~f:ErrorCode.to_value));
        ("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      let errorCode =
        (Option.map ~f:ErrorCode.of_xml) (Xml.child xml_arg0 "ErrorCode") in
      make ?message ?errorCode ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      let errorCode = field_map json "ErrorCode" ErrorCode.of_json in
      make ?message ?errorCode ()
    let to_json v = composed_to_json to_value v
  end
module InternalServerException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_json json =
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end
module InstanceFleetId =
  struct
    type nonrec t = string
    let context_ = "InstanceFleetId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"InstanceFleetId" j
    let to_json = simple_to_json to_value
  end
module ArnType =
  struct
    type nonrec t = string
    let context_ = "ArnType"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_string_min i ~min:20));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ArnType" j
    let to_json = simple_to_json to_value
  end
module AddInstanceFleetOutput =
  struct
    type nonrec t =
      {
      clusterId: XmlStringMaxLen256.t option ;
      instanceFleetId: InstanceFleetId.t option ;
      clusterArn: ArnType.t option }
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?clusterId =
      fun ?instanceFleetId ->
        fun ?clusterArn ->
          fun () -> { clusterId; instanceFleetId; clusterArn }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ClusterId",
           (Option.map x.clusterId ~f:XmlStringMaxLen256.to_value));
        ("InstanceFleetId",
          (Option.map x.instanceFleetId ~f:InstanceFleetId.to_value));
        ("ClusterArn", (Option.map x.clusterArn ~f:ArnType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clusterArn =
        (Option.map ~f:ArnType.of_xml) (Xml.child xml_arg0 "ClusterArn") in
      let instanceFleetId =
        (Option.map ~f:InstanceFleetId.of_xml)
          (Xml.child xml_arg0 "InstanceFleetId") in
      let clusterId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "ClusterId") in
      make ?clusterArn ?instanceFleetId ?clusterId ()
    let of_json json =
      let clusterArn = field_map json "ClusterArn" ArnType.of_json in
      let instanceFleetId =
        field_map json "InstanceFleetId" InstanceFleetId.of_json in
      let clusterId = field_map json "ClusterId" XmlStringMaxLen256.of_json in
      make ?clusterArn ?instanceFleetId ?clusterId ()
    let to_json v = composed_to_json to_value v
  end
module MarketType =
  struct
    type nonrec t =
      | ON_DEMAND 
      | SPOT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ON_DEMAND -> "ON_DEMAND"
      | SPOT -> "SPOT"
      | Non_static_id s -> s
    let of_string =
      function
      | "ON_DEMAND" -> ON_DEMAND
      | "SPOT" -> SPOT
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration MarketType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"MarketType" j)
    let to_json = simple_to_json to_value
  end
module InstanceRoleType =
  struct
    type nonrec t =
      | MASTER 
      | CORE 
      | TASK 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | MASTER -> "MASTER"
      | CORE -> "CORE"
      | TASK -> "TASK"
      | Non_static_id s -> s
    let of_string =
      function
      | "MASTER" -> MASTER
      | "CORE" -> CORE
      | "TASK" -> TASK
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration InstanceRoleType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"InstanceRoleType" j)
    let to_json = simple_to_json to_value
  end
module Unit =
  struct
    type nonrec t =
      | NONE 
      | SECONDS 
      | MICRO_SECONDS 
      | MILLI_SECONDS 
      | BYTES 
      | KILO_BYTES 
      | MEGA_BYTES 
      | GIGA_BYTES 
      | TERA_BYTES 
      | BITS 
      | KILO_BITS 
      | MEGA_BITS 
      | GIGA_BITS 
      | TERA_BITS 
      | PERCENT 
      | COUNT 
      | BYTES_PER_SECOND 
      | KILO_BYTES_PER_SECOND 
      | MEGA_BYTES_PER_SECOND 
      | GIGA_BYTES_PER_SECOND 
      | TERA_BYTES_PER_SECOND 
      | BITS_PER_SECOND 
      | KILO_BITS_PER_SECOND 
      | MEGA_BITS_PER_SECOND 
      | GIGA_BITS_PER_SECOND 
      | TERA_BITS_PER_SECOND 
      | COUNT_PER_SECOND 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NONE -> "NONE"
      | SECONDS -> "SECONDS"
      | MICRO_SECONDS -> "MICRO_SECONDS"
      | MILLI_SECONDS -> "MILLI_SECONDS"
      | BYTES -> "BYTES"
      | KILO_BYTES -> "KILO_BYTES"
      | MEGA_BYTES -> "MEGA_BYTES"
      | GIGA_BYTES -> "GIGA_BYTES"
      | TERA_BYTES -> "TERA_BYTES"
      | BITS -> "BITS"
      | KILO_BITS -> "KILO_BITS"
      | MEGA_BITS -> "MEGA_BITS"
      | GIGA_BITS -> "GIGA_BITS"
      | TERA_BITS -> "TERA_BITS"
      | PERCENT -> "PERCENT"
      | COUNT -> "COUNT"
      | BYTES_PER_SECOND -> "BYTES_PER_SECOND"
      | KILO_BYTES_PER_SECOND -> "KILO_BYTES_PER_SECOND"
      | MEGA_BYTES_PER_SECOND -> "MEGA_BYTES_PER_SECOND"
      | GIGA_BYTES_PER_SECOND -> "GIGA_BYTES_PER_SECOND"
      | TERA_BYTES_PER_SECOND -> "TERA_BYTES_PER_SECOND"
      | BITS_PER_SECOND -> "BITS_PER_SECOND"
      | KILO_BITS_PER_SECOND -> "KILO_BITS_PER_SECOND"
      | MEGA_BITS_PER_SECOND -> "MEGA_BITS_PER_SECOND"
      | GIGA_BITS_PER_SECOND -> "GIGA_BITS_PER_SECOND"
      | TERA_BITS_PER_SECOND -> "TERA_BITS_PER_SECOND"
      | COUNT_PER_SECOND -> "COUNT_PER_SECOND"
      | Non_static_id s -> s
    let of_string =
      function
      | "NONE" -> NONE
      | "SECONDS" -> SECONDS
      | "MICRO_SECONDS" -> MICRO_SECONDS
      | "MILLI_SECONDS" -> MILLI_SECONDS
      | "BYTES" -> BYTES
      | "KILO_BYTES" -> KILO_BYTES
      | "MEGA_BYTES" -> MEGA_BYTES
      | "GIGA_BYTES" -> GIGA_BYTES
      | "TERA_BYTES" -> TERA_BYTES
      | "BITS" -> BITS
      | "KILO_BITS" -> KILO_BITS
      | "MEGA_BITS" -> MEGA_BITS
      | "GIGA_BITS" -> GIGA_BITS
      | "TERA_BITS" -> TERA_BITS
      | "PERCENT" -> PERCENT
      | "COUNT" -> COUNT
      | "BYTES_PER_SECOND" -> BYTES_PER_SECOND
      | "KILO_BYTES_PER_SECOND" -> KILO_BYTES_PER_SECOND
      | "MEGA_BYTES_PER_SECOND" -> MEGA_BYTES_PER_SECOND
      | "GIGA_BYTES_PER_SECOND" -> GIGA_BYTES_PER_SECOND
      | "TERA_BYTES_PER_SECOND" -> TERA_BYTES_PER_SECOND
      | "BITS_PER_SECOND" -> BITS_PER_SECOND
      | "KILO_BITS_PER_SECOND" -> KILO_BITS_PER_SECOND
      | "MEGA_BITS_PER_SECOND" -> MEGA_BITS_PER_SECOND
      | "GIGA_BITS_PER_SECOND" -> GIGA_BITS_PER_SECOND
      | "TERA_BITS_PER_SECOND" -> TERA_BITS_PER_SECOND
      | "COUNT_PER_SECOND" -> COUNT_PER_SECOND
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Unit" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Unit" j)
    let to_json = simple_to_json to_value
  end
module Statistic =
  struct
    type nonrec t =
      | SAMPLE_COUNT 
      | AVERAGE 
      | SUM 
      | MINIMUM 
      | MAXIMUM 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SAMPLE_COUNT -> "SAMPLE_COUNT"
      | AVERAGE -> "AVERAGE"
      | SUM -> "SUM"
      | MINIMUM -> "MINIMUM"
      | MAXIMUM -> "MAXIMUM"
      | Non_static_id s -> s
    let of_string =
      function
      | "SAMPLE_COUNT" -> SAMPLE_COUNT
      | "AVERAGE" -> AVERAGE
      | "SUM" -> SUM
      | "MINIMUM" -> MINIMUM
      | "MAXIMUM" -> MAXIMUM
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Statistic" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Statistic" j)
    let to_json = simple_to_json to_value
  end
module MetricDimension =
  struct
    type nonrec t = {
      key: String_.t option ;
      value: String_.t option }
    let make ?key = fun ?value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("Key", (Option.map x.key ~f:String_.to_value));
        ("Value", (Option.map x.value ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Value") in
      let key = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Key") in
      make ?value ?key ()
    let of_json json =
      let value = field_map json "Value" String_.of_json in
      let key = field_map json "Key" String_.of_json in make ?value ?key ()
    let to_json v = composed_to_json to_value v
  end
module MetricDimensionList =
  struct
    type nonrec t = MetricDimension.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:MetricDimension.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:MetricDimension.of_xml)
    let of_json j =
      list_of_json ~kind:"MetricDimensionList"
        ~of_json:MetricDimension.of_json j
    let to_json v = composed_to_json to_value v
  end
module ComparisonOperator =
  struct
    type nonrec t =
      | GREATER_THAN_OR_EQUAL 
      | GREATER_THAN 
      | LESS_THAN 
      | LESS_THAN_OR_EQUAL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | GREATER_THAN_OR_EQUAL -> "GREATER_THAN_OR_EQUAL"
      | GREATER_THAN -> "GREATER_THAN"
      | LESS_THAN -> "LESS_THAN"
      | LESS_THAN_OR_EQUAL -> "LESS_THAN_OR_EQUAL"
      | Non_static_id s -> s
    let of_string =
      function
      | "GREATER_THAN_OR_EQUAL" -> GREATER_THAN_OR_EQUAL
      | "GREATER_THAN" -> GREATER_THAN
      | "LESS_THAN" -> LESS_THAN
      | "LESS_THAN_OR_EQUAL" -> LESS_THAN_OR_EQUAL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ComparisonOperator" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ComparisonOperator" j)
    let to_json = simple_to_json to_value
  end
module CloudWatchAlarmDefinition =
  struct
    type nonrec t =
      {
      comparisonOperator: ComparisonOperator.t ;
      evaluationPeriods: Integer.t option ;
      metricName: String_.t ;
      namespace: String_.t option ;
      period: Integer.t ;
      statistic: Statistic.t option ;
      threshold: NonNegativeDouble.t ;
      unit: Unit.t option ;
      dimensions: MetricDimensionList.t option }
    let context_ = "CloudWatchAlarmDefinition"
    let make ?evaluationPeriods =
      fun ?namespace ->
        fun ?statistic ->
          fun ?unit ->
            fun ?dimensions ->
              fun ~comparisonOperator ->
                fun ~metricName ->
                  fun ~period ->
                    fun ~threshold ->
                      fun () ->
                        {
                          evaluationPeriods;
                          namespace;
                          statistic;
                          unit;
                          dimensions;
                          comparisonOperator;
                          metricName;
                          period;
                          threshold
                        }
    let to_value x =
      structure_to_value
        [("ComparisonOperator",
           (Some (ComparisonOperator.to_value x.comparisonOperator)));
        ("EvaluationPeriods",
          (Option.map x.evaluationPeriods ~f:Integer.to_value));
        ("MetricName", (Some (String_.to_value x.metricName)));
        ("Namespace", (Option.map x.namespace ~f:String_.to_value));
        ("Period", (Some (Integer.to_value x.period)));
        ("Statistic", (Option.map x.statistic ~f:Statistic.to_value));
        ("Threshold", (Some (NonNegativeDouble.to_value x.threshold)));
        ("Unit", (Option.map x.unit ~f:Unit.to_value));
        ("Dimensions",
          (Option.map x.dimensions ~f:MetricDimensionList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dimensions =
        (Option.map ~f:MetricDimensionList.of_xml)
          (Xml.child xml_arg0 "Dimensions") in
      let unit = (Option.map ~f:Unit.of_xml) (Xml.child xml_arg0 "Unit") in
      let threshold =
        NonNegativeDouble.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Threshold") in
      let statistic =
        (Option.map ~f:Statistic.of_xml) (Xml.child xml_arg0 "Statistic") in
      let period =
        Integer.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Period") in
      let namespace =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Namespace") in
      let metricName =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MetricName") in
      let evaluationPeriods =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "EvaluationPeriods") in
      let comparisonOperator =
        ComparisonOperator.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ComparisonOperator") in
      make ?dimensions ?unit ~threshold ?statistic ~period ?namespace
        ~metricName ?evaluationPeriods ~comparisonOperator ()
    let of_json json =
      let dimensions =
        field_map json "Dimensions" MetricDimensionList.of_json in
      let unit = field_map json "Unit" Unit.of_json in
      let threshold =
        field_map_exn json "Threshold" NonNegativeDouble.of_json in
      let statistic = field_map json "Statistic" Statistic.of_json in
      let period = field_map_exn json "Period" Integer.of_json in
      let namespace = field_map json "Namespace" String_.of_json in
      let metricName = field_map_exn json "MetricName" String_.of_json in
      let evaluationPeriods =
        field_map json "EvaluationPeriods" Integer.of_json in
      let comparisonOperator =
        field_map_exn json "ComparisonOperator" ComparisonOperator.of_json in
      make ?dimensions ?unit ~threshold ?statistic ~period ?namespace
        ~metricName ?evaluationPeriods ~comparisonOperator ()
    let to_json v = composed_to_json to_value v
  end
module ScalingTrigger =
  struct
    type nonrec t = {
      cloudWatchAlarmDefinition: CloudWatchAlarmDefinition.t }
    let context_ = "ScalingTrigger"
    let make ~cloudWatchAlarmDefinition =
      fun () -> { cloudWatchAlarmDefinition }
    let to_value x =
      structure_to_value
        [("CloudWatchAlarmDefinition",
           (Some
              (CloudWatchAlarmDefinition.to_value x.cloudWatchAlarmDefinition)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cloudWatchAlarmDefinition =
        CloudWatchAlarmDefinition.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "CloudWatchAlarmDefinition") in
      make ~cloudWatchAlarmDefinition ()
    let of_json json =
      let cloudWatchAlarmDefinition =
        field_map_exn json "CloudWatchAlarmDefinition"
          CloudWatchAlarmDefinition.of_json in
      make ~cloudWatchAlarmDefinition ()
    let to_json v = composed_to_json to_value v
  end
module AdjustmentType =
  struct
    type nonrec t =
      | CHANGE_IN_CAPACITY 
      | PERCENT_CHANGE_IN_CAPACITY 
      | EXACT_CAPACITY 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CHANGE_IN_CAPACITY -> "CHANGE_IN_CAPACITY"
      | PERCENT_CHANGE_IN_CAPACITY -> "PERCENT_CHANGE_IN_CAPACITY"
      | EXACT_CAPACITY -> "EXACT_CAPACITY"
      | Non_static_id s -> s
    let of_string =
      function
      | "CHANGE_IN_CAPACITY" -> CHANGE_IN_CAPACITY
      | "PERCENT_CHANGE_IN_CAPACITY" -> PERCENT_CHANGE_IN_CAPACITY
      | "EXACT_CAPACITY" -> EXACT_CAPACITY
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration AdjustmentType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AdjustmentType" j)
    let to_json = simple_to_json to_value
  end
module SimpleScalingPolicyConfiguration =
  struct
    type nonrec t =
      {
      adjustmentType: AdjustmentType.t option ;
      scalingAdjustment: Integer.t ;
      coolDown: Integer.t option }
    let context_ = "SimpleScalingPolicyConfiguration"
    let make ?adjustmentType =
      fun ?coolDown ->
        fun ~scalingAdjustment ->
          fun () -> { adjustmentType; coolDown; scalingAdjustment }
    let to_value x =
      structure_to_value
        [("AdjustmentType",
           (Option.map x.adjustmentType ~f:AdjustmentType.to_value));
        ("ScalingAdjustment", (Some (Integer.to_value x.scalingAdjustment)));
        ("CoolDown", (Option.map x.coolDown ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let coolDown =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "CoolDown") in
      let scalingAdjustment =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ScalingAdjustment") in
      let adjustmentType =
        (Option.map ~f:AdjustmentType.of_xml)
          (Xml.child xml_arg0 "AdjustmentType") in
      make ?coolDown ~scalingAdjustment ?adjustmentType ()
    let of_json json =
      let coolDown = field_map json "CoolDown" Integer.of_json in
      let scalingAdjustment =
        field_map_exn json "ScalingAdjustment" Integer.of_json in
      let adjustmentType =
        field_map json "AdjustmentType" AdjustmentType.of_json in
      make ?coolDown ~scalingAdjustment ?adjustmentType ()
    let to_json v = composed_to_json to_value v
  end
module ScalingAction =
  struct
    type nonrec t =
      {
      market: MarketType.t option ;
      simpleScalingPolicyConfiguration: SimpleScalingPolicyConfiguration.t }
    let context_ = "ScalingAction"
    let make ?market =
      fun ~simpleScalingPolicyConfiguration ->
        fun () -> { market; simpleScalingPolicyConfiguration }
    let to_value x =
      structure_to_value
        [("Market", (Option.map x.market ~f:MarketType.to_value));
        ("SimpleScalingPolicyConfiguration",
          (Some
             (SimpleScalingPolicyConfiguration.to_value
                x.simpleScalingPolicyConfiguration)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let simpleScalingPolicyConfiguration =
        SimpleScalingPolicyConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "SimpleScalingPolicyConfiguration") in
      let market =
        (Option.map ~f:MarketType.of_xml) (Xml.child xml_arg0 "Market") in
      make ~simpleScalingPolicyConfiguration ?market ()
    let of_json json =
      let simpleScalingPolicyConfiguration =
        field_map_exn json "SimpleScalingPolicyConfiguration"
          SimpleScalingPolicyConfiguration.of_json in
      let market = field_map json "Market" MarketType.of_json in
      make ~simpleScalingPolicyConfiguration ?market ()
    let to_json v = composed_to_json to_value v
  end
module ScalingRule =
  struct
    type nonrec t =
      {
      name: String_.t ;
      description: String_.t option ;
      action: ScalingAction.t ;
      trigger: ScalingTrigger.t }
    let context_ = "ScalingRule"
    let make ?description =
      fun ~name ->
        fun ~action ->
          fun ~trigger -> fun () -> { description; name; action; trigger }
    let to_value x =
      structure_to_value
        [("Name", (Some (String_.to_value x.name)));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("Action", (Some (ScalingAction.to_value x.action)));
        ("Trigger", (Some (ScalingTrigger.to_value x.trigger)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trigger =
        ScalingTrigger.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Trigger") in
      let action =
        ScalingAction.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Action") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let name =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ~trigger ~action ?description ~name ()
    let of_json json =
      let trigger = field_map_exn json "Trigger" ScalingTrigger.of_json in
      let action = field_map_exn json "Action" ScalingAction.of_json in
      let description = field_map json "Description" String_.of_json in
      let name = field_map_exn json "Name" String_.of_json in
      make ~trigger ~action ?description ~name ()
    let to_json v = composed_to_json to_value v
  end
module ScalingRuleList =
  struct
    type nonrec t = ScalingRule.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ScalingRule.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ScalingRule.of_xml)
    let of_json j =
      list_of_json ~kind:"ScalingRuleList" ~of_json:ScalingRule.of_json j
    let to_json v = composed_to_json to_value v
  end
module ScalingConstraints =
  struct
    type nonrec t = {
      minCapacity: Integer.t ;
      maxCapacity: Integer.t }
    let context_ = "ScalingConstraints"
    let make ~minCapacity =
      fun ~maxCapacity -> fun () -> { minCapacity; maxCapacity }
    let to_value x =
      structure_to_value
        [("MinCapacity", (Some (Integer.to_value x.minCapacity)));
        ("MaxCapacity", (Some (Integer.to_value x.maxCapacity)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxCapacity =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MaxCapacity") in
      let minCapacity =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MinCapacity") in
      make ~maxCapacity ~minCapacity ()
    let of_json json =
      let maxCapacity = field_map_exn json "MaxCapacity" Integer.of_json in
      let minCapacity = field_map_exn json "MinCapacity" Integer.of_json in
      make ~maxCapacity ~minCapacity ()
    let to_json v = composed_to_json to_value v
  end
module AutoScalingPolicy =
  struct
    type nonrec t =
      {
      constraints: ScalingConstraints.t ;
      rules: ScalingRuleList.t }
    let context_ = "AutoScalingPolicy"
    let make ~constraints = fun ~rules -> fun () -> { constraints; rules }
    let to_value x =
      structure_to_value
        [("Constraints", (Some (ScalingConstraints.to_value x.constraints)));
        ("Rules", (Some (ScalingRuleList.to_value x.rules)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let rules =
        ScalingRuleList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Rules") in
      let constraints =
        ScalingConstraints.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Constraints") in
      make ~rules ~constraints ()
    let of_json json =
      let rules = field_map_exn json "Rules" ScalingRuleList.of_json in
      let constraints =
        field_map_exn json "Constraints" ScalingConstraints.of_json in
      make ~rules ~constraints ()
    let to_json v = composed_to_json to_value v
  end
module InstanceGroupConfig =
  struct
    type nonrec t =
      {
      name: XmlStringMaxLen256.t option ;
      market: MarketType.t option ;
      instanceRole: InstanceRoleType.t ;
      bidPrice: XmlStringMaxLen256.t option ;
      instanceType: InstanceType.t ;
      instanceCount: Integer.t ;
      configurations: ConfigurationList.t option ;
      ebsConfiguration: EbsConfiguration.t option ;
      autoScalingPolicy: AutoScalingPolicy.t option ;
      customAmiId: XmlStringMaxLen256.t option }
    let context_ = "InstanceGroupConfig"
    let make ?name =
      fun ?market ->
        fun ?bidPrice ->
          fun ?configurations ->
            fun ?ebsConfiguration ->
              fun ?autoScalingPolicy ->
                fun ?customAmiId ->
                  fun ~instanceRole ->
                    fun ~instanceType ->
                      fun ~instanceCount ->
                        fun () ->
                          {
                            name;
                            market;
                            bidPrice;
                            configurations;
                            ebsConfiguration;
                            autoScalingPolicy;
                            customAmiId;
                            instanceRole;
                            instanceType;
                            instanceCount
                          }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:XmlStringMaxLen256.to_value));
        ("Market", (Option.map x.market ~f:MarketType.to_value));
        ("InstanceRole", (Some (InstanceRoleType.to_value x.instanceRole)));
        ("BidPrice", (Option.map x.bidPrice ~f:XmlStringMaxLen256.to_value));
        ("InstanceType", (Some (InstanceType.to_value x.instanceType)));
        ("InstanceCount", (Some (Integer.to_value x.instanceCount)));
        ("Configurations",
          (Option.map x.configurations ~f:ConfigurationList.to_value));
        ("EbsConfiguration",
          (Option.map x.ebsConfiguration ~f:EbsConfiguration.to_value));
        ("AutoScalingPolicy",
          (Option.map x.autoScalingPolicy ~f:AutoScalingPolicy.to_value));
        ("CustomAmiId",
          (Option.map x.customAmiId ~f:XmlStringMaxLen256.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let customAmiId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "CustomAmiId") in
      let autoScalingPolicy =
        (Option.map ~f:AutoScalingPolicy.of_xml)
          (Xml.child xml_arg0 "AutoScalingPolicy") in
      let ebsConfiguration =
        (Option.map ~f:EbsConfiguration.of_xml)
          (Xml.child xml_arg0 "EbsConfiguration") in
      let configurations =
        (Option.map ~f:ConfigurationList.of_xml)
          (Xml.child xml_arg0 "Configurations") in
      let instanceCount =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceCount") in
      let instanceType =
        InstanceType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceType") in
      let bidPrice =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "BidPrice") in
      let instanceRole =
        InstanceRoleType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceRole") in
      let market =
        (Option.map ~f:MarketType.of_xml) (Xml.child xml_arg0 "Market") in
      let name =
        (Option.map ~f:XmlStringMaxLen256.of_xml) (Xml.child xml_arg0 "Name") in
      make ?customAmiId ?autoScalingPolicy ?ebsConfiguration ?configurations
        ~instanceCount ~instanceType ?bidPrice ~instanceRole ?market ?name ()
    let of_json json =
      let customAmiId =
        field_map json "CustomAmiId" XmlStringMaxLen256.of_json in
      let autoScalingPolicy =
        field_map json "AutoScalingPolicy" AutoScalingPolicy.of_json in
      let ebsConfiguration =
        field_map json "EbsConfiguration" EbsConfiguration.of_json in
      let configurations =
        field_map json "Configurations" ConfigurationList.of_json in
      let instanceCount = field_map_exn json "InstanceCount" Integer.of_json in
      let instanceType =
        field_map_exn json "InstanceType" InstanceType.of_json in
      let bidPrice = field_map json "BidPrice" XmlStringMaxLen256.of_json in
      let instanceRole =
        field_map_exn json "InstanceRole" InstanceRoleType.of_json in
      let market = field_map json "Market" MarketType.of_json in
      let name = field_map json "Name" XmlStringMaxLen256.of_json in
      make ?customAmiId ?autoScalingPolicy ?ebsConfiguration ?configurations
        ~instanceCount ~instanceType ?bidPrice ~instanceRole ?market ?name ()
    let to_json v = composed_to_json to_value v
  end
module InstanceGroupConfigList =
  struct
    type nonrec t = InstanceGroupConfig.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:InstanceGroupConfig.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InstanceGroupConfig.of_xml)
    let of_json j =
      list_of_json ~kind:"InstanceGroupConfigList"
        ~of_json:InstanceGroupConfig.of_json j
    let to_json v = composed_to_json to_value v
  end
module AddInstanceGroupsInput =
  struct
    type nonrec t =
      {
      instanceGroups: InstanceGroupConfigList.t ;
      jobFlowId: XmlStringMaxLen256.t }
    let context_ = "AddInstanceGroupsInput"
    let make ~instanceGroups =
      fun ~jobFlowId -> fun () -> { instanceGroups; jobFlowId }
    let to_value x =
      structure_to_value
        [("InstanceGroups",
           (Some (InstanceGroupConfigList.to_value x.instanceGroups)));
        ("JobFlowId", (Some (XmlStringMaxLen256.to_value x.jobFlowId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let jobFlowId =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "JobFlowId") in
      let instanceGroups =
        InstanceGroupConfigList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceGroups") in
      make ~jobFlowId ~instanceGroups ()
    let of_json json =
      let jobFlowId =
        field_map_exn json "JobFlowId" XmlStringMaxLen256.of_json in
      let instanceGroups =
        field_map_exn json "InstanceGroups" InstanceGroupConfigList.of_json in
      make ~jobFlowId ~instanceGroups ()
    let to_json v = composed_to_json to_value v
  end
module InternalServerError =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module InstanceGroupIdsList =
  struct
    type nonrec t = XmlStringMaxLen256.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:XmlStringMaxLen256.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:XmlStringMaxLen256.of_xml)
    let of_json j =
      list_of_json ~kind:"InstanceGroupIdsList"
        ~of_json:XmlStringMaxLen256.of_json j
    let to_json v = composed_to_json to_value v
  end
module AddInstanceGroupsOutput =
  struct
    type nonrec t =
      {
      jobFlowId: XmlStringMaxLen256.t option ;
      instanceGroupIds: InstanceGroupIdsList.t option ;
      clusterArn: ArnType.t option }
    type nonrec error =
      [ `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?jobFlowId =
      fun ?instanceGroupIds ->
        fun ?clusterArn ->
          fun () -> { jobFlowId; instanceGroupIds; clusterArn }
    let error_of_json name json =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("JobFlowId",
           (Option.map x.jobFlowId ~f:XmlStringMaxLen256.to_value));
        ("InstanceGroupIds",
          (Option.map x.instanceGroupIds ~f:InstanceGroupIdsList.to_value));
        ("ClusterArn", (Option.map x.clusterArn ~f:ArnType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clusterArn =
        (Option.map ~f:ArnType.of_xml) (Xml.child xml_arg0 "ClusterArn") in
      let instanceGroupIds =
        (Option.map ~f:InstanceGroupIdsList.of_xml)
          (Xml.child xml_arg0 "InstanceGroupIds") in
      let jobFlowId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "JobFlowId") in
      make ?clusterArn ?instanceGroupIds ?jobFlowId ()
    let of_json json =
      let clusterArn = field_map json "ClusterArn" ArnType.of_json in
      let instanceGroupIds =
        field_map json "InstanceGroupIds" InstanceGroupIdsList.of_json in
      let jobFlowId = field_map json "JobFlowId" XmlStringMaxLen256.of_json in
      make ?clusterArn ?instanceGroupIds ?jobFlowId ()
    let to_json v = composed_to_json to_value v
  end
module XmlString =
  struct
    type nonrec t = string
    let context_ = "XmlString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:10280) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[\\u0020-\\uD7FF\\uE000-\\uFFFD\\uD800\\uDC00-\\uDBFF\\uDFFF\\r\\n\\t]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"XmlString" j
    let to_json = simple_to_json to_value
  end
module XmlStringList =
  struct
    type nonrec t = XmlString.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:XmlString.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:XmlString.of_xml)
    let of_json j =
      list_of_json ~kind:"XmlStringList" ~of_json:XmlString.of_json j
    let to_json v = composed_to_json to_value v
  end
module KeyValue =
  struct
    type nonrec t = {
      key: XmlString.t option ;
      value: XmlString.t option }
    let make ?key = fun ?value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("Key", (Option.map x.key ~f:XmlString.to_value));
        ("Value", (Option.map x.value ~f:XmlString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "Value") in
      let key = (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "Key") in
      make ?value ?key ()
    let of_json json =
      let value = field_map json "Value" XmlString.of_json in
      let key = field_map json "Key" XmlString.of_json in make ?value ?key ()
    let to_json v = composed_to_json to_value v
  end
module KeyValueList =
  struct
    type nonrec t = KeyValue.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:KeyValue.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:KeyValue.of_xml)
    let of_json j =
      list_of_json ~kind:"KeyValueList" ~of_json:KeyValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module HadoopJarStepConfig =
  struct
    type nonrec t =
      {
      properties: KeyValueList.t option ;
      jar: XmlString.t ;
      mainClass: XmlString.t option ;
      args: XmlStringList.t option }
    let context_ = "HadoopJarStepConfig"
    let make ?properties =
      fun ?mainClass ->
        fun ?args ->
          fun ~jar -> fun () -> { properties; mainClass; args; jar }
    let to_value x =
      structure_to_value
        [("Properties", (Option.map x.properties ~f:KeyValueList.to_value));
        ("Jar", (Some (XmlString.to_value x.jar)));
        ("MainClass", (Option.map x.mainClass ~f:XmlString.to_value));
        ("Args", (Option.map x.args ~f:XmlStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let args =
        (Option.map ~f:XmlStringList.of_xml) (Xml.child xml_arg0 "Args") in
      let mainClass =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "MainClass") in
      let jar =
        XmlString.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Jar") in
      let properties =
        (Option.map ~f:KeyValueList.of_xml) (Xml.child xml_arg0 "Properties") in
      make ?args ?mainClass ~jar ?properties ()
    let of_json json =
      let args = field_map json "Args" XmlStringList.of_json in
      let mainClass = field_map json "MainClass" XmlString.of_json in
      let jar = field_map_exn json "Jar" XmlString.of_json in
      let properties = field_map json "Properties" KeyValueList.of_json in
      make ?args ?mainClass ~jar ?properties ()
    let to_json v = composed_to_json to_value v
  end
module StepConfig =
  struct
    type nonrec t =
      {
      name: XmlStringMaxLen256.t ;
      actionOnFailure: ActionOnFailure.t option ;
      hadoopJarStep: HadoopJarStepConfig.t }
    let context_ = "StepConfig"
    let make ?actionOnFailure =
      fun ~name ->
        fun ~hadoopJarStep ->
          fun () -> { actionOnFailure; name; hadoopJarStep }
    let to_value x =
      structure_to_value
        [("Name", (Some (XmlStringMaxLen256.to_value x.name)));
        ("ActionOnFailure",
          (Option.map x.actionOnFailure ~f:ActionOnFailure.to_value));
        ("HadoopJarStep",
          (Some (HadoopJarStepConfig.to_value x.hadoopJarStep)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let hadoopJarStep =
        HadoopJarStepConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HadoopJarStep") in
      let actionOnFailure =
        (Option.map ~f:ActionOnFailure.of_xml)
          (Xml.child xml_arg0 "ActionOnFailure") in
      let name =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ~hadoopJarStep ?actionOnFailure ~name ()
    let of_json json =
      let hadoopJarStep =
        field_map_exn json "HadoopJarStep" HadoopJarStepConfig.of_json in
      let actionOnFailure =
        field_map json "ActionOnFailure" ActionOnFailure.of_json in
      let name = field_map_exn json "Name" XmlStringMaxLen256.of_json in
      make ~hadoopJarStep ?actionOnFailure ~name ()
    let to_json v = composed_to_json to_value v
  end
module StepConfigList =
  struct
    type nonrec t = StepConfig.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:StepConfig.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:StepConfig.of_xml)
    let of_json j =
      list_of_json ~kind:"StepConfigList" ~of_json:StepConfig.of_json j
    let to_json v = composed_to_json to_value v
  end
module AddJobFlowStepsInput =
  struct
    type nonrec t =
      {
      jobFlowId: XmlStringMaxLen256.t ;
      steps: StepConfigList.t }
    let context_ = "AddJobFlowStepsInput"
    let make ~jobFlowId = fun ~steps -> fun () -> { jobFlowId; steps }
    let to_value x =
      structure_to_value
        [("JobFlowId", (Some (XmlStringMaxLen256.to_value x.jobFlowId)));
        ("Steps", (Some (StepConfigList.to_value x.steps)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let steps =
        StepConfigList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Steps") in
      let jobFlowId =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "JobFlowId") in
      make ~steps ~jobFlowId ()
    let of_json json =
      let steps = field_map_exn json "Steps" StepConfigList.of_json in
      let jobFlowId =
        field_map_exn json "JobFlowId" XmlStringMaxLen256.of_json in
      make ~steps ~jobFlowId ()
    let to_json v = composed_to_json to_value v
  end
module StepIdsList =
  struct
    type nonrec t = XmlStringMaxLen256.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:XmlStringMaxLen256.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:XmlStringMaxLen256.of_xml)
    let of_json j =
      list_of_json ~kind:"StepIdsList" ~of_json:XmlStringMaxLen256.of_json j
    let to_json v = composed_to_json to_value v
  end
module AddJobFlowStepsOutput =
  struct
    type nonrec t = {
      stepIds: StepIdsList.t option }
    type nonrec error =
      [ `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?stepIds = fun () -> { stepIds }
    let error_of_json name json =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("StepIds", (Option.map x.stepIds ~f:StepIdsList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stepIds =
        (Option.map ~f:StepIdsList.of_xml) (Xml.child xml_arg0 "StepIds") in
      make ?stepIds ()
    let of_json json =
      let stepIds = field_map json "StepIds" StepIdsList.of_json in
      make ?stepIds ()
    let to_json v = composed_to_json to_value v
  end
module Tag =
  struct
    type nonrec t = {
      key: String_.t option ;
      value: String_.t option }
    let make ?key = fun ?value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("Key", (Option.map x.key ~f:String_.to_value));
        ("Value", (Option.map x.value ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Value") in
      let key = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Key") in
      make ?value ?key ()
    let of_json json =
      let value = field_map json "Value" String_.of_json in
      let key = field_map json "Key" String_.of_json in make ?value ?key ()
    let to_json v = composed_to_json to_value v
  end
module TagList =
  struct
    type nonrec t = Tag.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Tag.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tag.of_xml)
    let of_json j = list_of_json ~kind:"TagList" ~of_json:Tag.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceId =
  struct
    type nonrec t = string
    let context_ = "ResourceId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceId" j
    let to_json = simple_to_json to_value
  end
module AddTagsInput =
  struct
    type nonrec t = {
      resourceId: ResourceId.t ;
      tags: TagList.t }
    let context_ = "AddTagsInput"
    let make ~resourceId = fun ~tags -> fun () -> { resourceId; tags }
    let to_value x =
      structure_to_value
        [("ResourceId", (Some (ResourceId.to_value x.resourceId)));
        ("Tags", (Some (TagList.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagList.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Tags") in
      let resourceId =
        ResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceId") in
      make ~tags ~resourceId ()
    let of_json json =
      let tags = field_map_exn json "Tags" TagList.of_json in
      let resourceId = field_map_exn json "ResourceId" ResourceId.of_json in
      make ~tags ~resourceId ()
    let to_json v = composed_to_json to_value v
  end
module AddTagsOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module StringList =
  struct
    type nonrec t = String_.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"StringList" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module Application =
  struct
    type nonrec t =
      {
      name: String_.t option ;
      version: String_.t option ;
      args: StringList.t option ;
      additionalInfo: StringMap.t option }
    let make ?name =
      fun ?version ->
        fun ?args ->
          fun ?additionalInfo ->
            fun () -> { name; version; args; additionalInfo }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:String_.to_value));
        ("Version", (Option.map x.version ~f:String_.to_value));
        ("Args", (Option.map x.args ~f:StringList.to_value));
        ("AdditionalInfo",
          (Option.map x.additionalInfo ~f:StringMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let additionalInfo =
        (Option.map ~f:StringMap.of_xml)
          (Xml.child xml_arg0 "AdditionalInfo") in
      let args =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "Args") in
      let version =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Version") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      make ?additionalInfo ?args ?version ?name ()
    let of_json json =
      let additionalInfo = field_map json "AdditionalInfo" StringMap.of_json in
      let args = field_map json "Args" StringList.of_json in
      let version = field_map json "Version" String_.of_json in
      let name = field_map json "Name" String_.of_json in
      make ?additionalInfo ?args ?version ?name ()
    let to_json v = composed_to_json to_value v
  end
module ApplicationList =
  struct
    type nonrec t = Application.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Application.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Application.of_xml)
    let of_json j =
      list_of_json ~kind:"ApplicationList" ~of_json:Application.of_json j
    let to_json v = composed_to_json to_value v
  end
module AuthMode =
  struct
    type nonrec t =
      | SSO 
      | IAM 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | SSO -> "SSO" | IAM -> "IAM" | Non_static_id s -> s
    let of_string =
      function | "SSO" -> SSO | "IAM" -> IAM | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration AuthMode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AuthMode" j)
    let to_json = simple_to_json to_value
  end
module AutoScalingPolicyStateChangeReasonCode =
  struct
    type nonrec t =
      | USER_REQUEST 
      | PROVISION_FAILURE 
      | CLEANUP_FAILURE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | USER_REQUEST -> "USER_REQUEST"
      | PROVISION_FAILURE -> "PROVISION_FAILURE"
      | CLEANUP_FAILURE -> "CLEANUP_FAILURE"
      | Non_static_id s -> s
    let of_string =
      function
      | "USER_REQUEST" -> USER_REQUEST
      | "PROVISION_FAILURE" -> PROVISION_FAILURE
      | "CLEANUP_FAILURE" -> CLEANUP_FAILURE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml
           ~kind:"enumeration AutoScalingPolicyStateChangeReasonCode"
           xml_arg0)
    let of_json j =
      of_string
        (string_of_json ~kind:"AutoScalingPolicyStateChangeReasonCode" j)
    let to_json = simple_to_json to_value
  end
module AutoScalingPolicyStateChangeReason =
  struct
    type nonrec t =
      {
      code: AutoScalingPolicyStateChangeReasonCode.t option ;
      message: String_.t option }
    let make ?code = fun ?message -> fun () -> { code; message }
    let to_value x =
      structure_to_value
        [("Code",
           (Option.map x.code
              ~f:AutoScalingPolicyStateChangeReasonCode.to_value));
        ("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      let code =
        (Option.map ~f:AutoScalingPolicyStateChangeReasonCode.of_xml)
          (Xml.child xml_arg0 "Code") in
      make ?message ?code ()
    let of_json json =
      let message = field_map json "Message" String_.of_json in
      let code =
        field_map json "Code" AutoScalingPolicyStateChangeReasonCode.of_json in
      make ?message ?code ()
    let to_json v = composed_to_json to_value v
  end
module AutoScalingPolicyState =
  struct
    type nonrec t =
      | PENDING 
      | ATTACHING 
      | ATTACHED 
      | DETACHING 
      | DETACHED 
      | FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PENDING -> "PENDING"
      | ATTACHING -> "ATTACHING"
      | ATTACHED -> "ATTACHED"
      | DETACHING -> "DETACHING"
      | DETACHED -> "DETACHED"
      | FAILED -> "FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "PENDING" -> PENDING
      | "ATTACHING" -> ATTACHING
      | "ATTACHED" -> ATTACHED
      | "DETACHING" -> DETACHING
      | "DETACHED" -> DETACHED
      | "FAILED" -> FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration AutoScalingPolicyState" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"AutoScalingPolicyState" j)
    let to_json = simple_to_json to_value
  end
module AutoScalingPolicyStatus =
  struct
    type nonrec t =
      {
      state: AutoScalingPolicyState.t option ;
      stateChangeReason: AutoScalingPolicyStateChangeReason.t option }
    let make ?state =
      fun ?stateChangeReason -> fun () -> { state; stateChangeReason }
    let to_value x =
      structure_to_value
        [("State", (Option.map x.state ~f:AutoScalingPolicyState.to_value));
        ("StateChangeReason",
          (Option.map x.stateChangeReason
             ~f:AutoScalingPolicyStateChangeReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stateChangeReason =
        (Option.map ~f:AutoScalingPolicyStateChangeReason.of_xml)
          (Xml.child xml_arg0 "StateChangeReason") in
      let state =
        (Option.map ~f:AutoScalingPolicyState.of_xml)
          (Xml.child xml_arg0 "State") in
      make ?stateChangeReason ?state ()
    let of_json json =
      let stateChangeReason =
        field_map json "StateChangeReason"
          AutoScalingPolicyStateChangeReason.of_json in
      let state = field_map json "State" AutoScalingPolicyState.of_json in
      make ?stateChangeReason ?state ()
    let to_json v = composed_to_json to_value v
  end
module AutoScalingPolicyDescription =
  struct
    type nonrec t =
      {
      status: AutoScalingPolicyStatus.t option ;
      constraints: ScalingConstraints.t option ;
      rules: ScalingRuleList.t option }
    let make ?status =
      fun ?constraints ->
        fun ?rules -> fun () -> { status; constraints; rules }
    let to_value x =
      structure_to_value
        [("Status",
           (Option.map x.status ~f:AutoScalingPolicyStatus.to_value));
        ("Constraints",
          (Option.map x.constraints ~f:ScalingConstraints.to_value));
        ("Rules", (Option.map x.rules ~f:ScalingRuleList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let rules =
        (Option.map ~f:ScalingRuleList.of_xml) (Xml.child xml_arg0 "Rules") in
      let constraints =
        (Option.map ~f:ScalingConstraints.of_xml)
          (Xml.child xml_arg0 "Constraints") in
      let status =
        (Option.map ~f:AutoScalingPolicyStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      make ?rules ?constraints ?status ()
    let of_json json =
      let rules = field_map json "Rules" ScalingRuleList.of_json in
      let constraints =
        field_map json "Constraints" ScalingConstraints.of_json in
      let status = field_map json "Status" AutoScalingPolicyStatus.of_json in
      make ?rules ?constraints ?status ()
    let to_json v = composed_to_json to_value v
  end
module Long =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module AutoTerminationPolicy =
  struct
    type nonrec t = {
      idleTimeout: Long.t option }
    let make ?idleTimeout = fun () -> { idleTimeout }
    let to_value x =
      structure_to_value
        [("IdleTimeout", (Option.map x.idleTimeout ~f:Long.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let idleTimeout =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "IdleTimeout") in
      make ?idleTimeout ()
    let of_json json =
      let idleTimeout = field_map json "IdleTimeout" Long.of_json in
      make ?idleTimeout ()
    let to_json v = composed_to_json to_value v
  end
module Port =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:65535) >>=
             (fun () -> check_int_min i ~min:(-1)));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Port" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module PortRange =
  struct
    type nonrec t = {
      minRange: Port.t ;
      maxRange: Port.t option }
    let context_ = "PortRange"
    let make ?maxRange = fun ~minRange -> fun () -> { maxRange; minRange }
    let to_value x =
      structure_to_value
        [("MinRange", (Some (Port.to_value x.minRange)));
        ("MaxRange", (Option.map x.maxRange ~f:Port.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxRange =
        (Option.map ~f:Port.of_xml) (Xml.child xml_arg0 "MaxRange") in
      let minRange =
        Port.of_xml (Xml.child_exn ~context:context_ xml_arg0 "MinRange") in
      make ?maxRange ~minRange ()
    let of_json json =
      let maxRange = field_map json "MaxRange" Port.of_json in
      let minRange = field_map_exn json "MinRange" Port.of_json in
      make ?maxRange ~minRange ()
    let to_json v = composed_to_json to_value v
  end
module PortRanges =
  struct
    type nonrec t = PortRange.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:PortRange.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PortRange.of_xml)
    let of_json j =
      list_of_json ~kind:"PortRanges" ~of_json:PortRange.of_json j
    let to_json v = composed_to_json to_value v
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module BlockPublicAccessConfiguration =
  struct
    type nonrec t =
      {
      blockPublicSecurityGroupRules: Boolean.t ;
      permittedPublicSecurityGroupRuleRanges: PortRanges.t option }
    let context_ = "BlockPublicAccessConfiguration"
    let make ?permittedPublicSecurityGroupRuleRanges =
      fun ~blockPublicSecurityGroupRules ->
        fun () ->
          {
            permittedPublicSecurityGroupRuleRanges;
            blockPublicSecurityGroupRules
          }
    let to_value x =
      structure_to_value
        [("BlockPublicSecurityGroupRules",
           (Some (Boolean.to_value x.blockPublicSecurityGroupRules)));
        ("PermittedPublicSecurityGroupRuleRanges",
          (Option.map x.permittedPublicSecurityGroupRuleRanges
             ~f:PortRanges.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let permittedPublicSecurityGroupRuleRanges =
        (Option.map ~f:PortRanges.of_xml)
          (Xml.child xml_arg0 "PermittedPublicSecurityGroupRuleRanges") in
      let blockPublicSecurityGroupRules =
        Boolean.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "BlockPublicSecurityGroupRules") in
      make ?permittedPublicSecurityGroupRuleRanges
        ~blockPublicSecurityGroupRules ()
    let of_json json =
      let permittedPublicSecurityGroupRuleRanges =
        field_map json "PermittedPublicSecurityGroupRuleRanges"
          PortRanges.of_json in
      let blockPublicSecurityGroupRules =
        field_map_exn json "BlockPublicSecurityGroupRules" Boolean.of_json in
      make ?permittedPublicSecurityGroupRuleRanges
        ~blockPublicSecurityGroupRules ()
    let to_json v = composed_to_json to_value v
  end
module Date =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module BlockPublicAccessConfigurationMetadata =
  struct
    type nonrec t = {
      creationDateTime: Date.t ;
      createdByArn: ArnType.t }
    let context_ = "BlockPublicAccessConfigurationMetadata"
    let make ~creationDateTime =
      fun ~createdByArn -> fun () -> { creationDateTime; createdByArn }
    let to_value x =
      structure_to_value
        [("CreationDateTime", (Some (Date.to_value x.creationDateTime)));
        ("CreatedByArn", (Some (ArnType.to_value x.createdByArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let createdByArn =
        ArnType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CreatedByArn") in
      let creationDateTime =
        Date.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CreationDateTime") in
      make ~createdByArn ~creationDateTime ()
    let of_json json =
      let createdByArn = field_map_exn json "CreatedByArn" ArnType.of_json in
      let creationDateTime =
        field_map_exn json "CreationDateTime" Date.of_json in
      make ~createdByArn ~creationDateTime ()
    let to_json v = composed_to_json to_value v
  end
module ScriptBootstrapActionConfig =
  struct
    type nonrec t = {
      path: XmlString.t ;
      args: XmlStringList.t option }
    let context_ = "ScriptBootstrapActionConfig"
    let make ?args = fun ~path -> fun () -> { args; path }
    let to_value x =
      structure_to_value
        [("Path", (Some (XmlString.to_value x.path)));
        ("Args", (Option.map x.args ~f:XmlStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let args =
        (Option.map ~f:XmlStringList.of_xml) (Xml.child xml_arg0 "Args") in
      let path =
        XmlString.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Path") in
      make ?args ~path ()
    let of_json json =
      let args = field_map json "Args" XmlStringList.of_json in
      let path = field_map_exn json "Path" XmlString.of_json in
      make ?args ~path ()
    let to_json v = composed_to_json to_value v
  end
module BootstrapActionConfig =
  struct
    type nonrec t =
      {
      name: XmlStringMaxLen256.t ;
      scriptBootstrapAction: ScriptBootstrapActionConfig.t }
    let context_ = "BootstrapActionConfig"
    let make ~name =
      fun ~scriptBootstrapAction -> fun () -> { name; scriptBootstrapAction }
    let to_value x =
      structure_to_value
        [("Name", (Some (XmlStringMaxLen256.to_value x.name)));
        ("ScriptBootstrapAction",
          (Some
             (ScriptBootstrapActionConfig.to_value x.scriptBootstrapAction)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scriptBootstrapAction =
        ScriptBootstrapActionConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ScriptBootstrapAction") in
      let name =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ~scriptBootstrapAction ~name ()
    let of_json json =
      let scriptBootstrapAction =
        field_map_exn json "ScriptBootstrapAction"
          ScriptBootstrapActionConfig.of_json in
      let name = field_map_exn json "Name" XmlStringMaxLen256.of_json in
      make ~scriptBootstrapAction ~name ()
    let to_json v = composed_to_json to_value v
  end
module BootstrapActionConfigList =
  struct
    type nonrec t = BootstrapActionConfig.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:BootstrapActionConfig.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BootstrapActionConfig.of_xml)
    let of_json j =
      list_of_json ~kind:"BootstrapActionConfigList"
        ~of_json:BootstrapActionConfig.of_json j
    let to_json v = composed_to_json to_value v
  end
module BootstrapActionDetail =
  struct
    type nonrec t = {
      bootstrapActionConfig: BootstrapActionConfig.t option }
    let make ?bootstrapActionConfig = fun () -> { bootstrapActionConfig }
    let to_value x =
      structure_to_value
        [("BootstrapActionConfig",
           (Option.map x.bootstrapActionConfig
              ~f:BootstrapActionConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bootstrapActionConfig =
        (Option.map ~f:BootstrapActionConfig.of_xml)
          (Xml.child xml_arg0 "BootstrapActionConfig") in
      make ?bootstrapActionConfig ()
    let of_json json =
      let bootstrapActionConfig =
        field_map json "BootstrapActionConfig" BootstrapActionConfig.of_json in
      make ?bootstrapActionConfig ()
    let to_json v = composed_to_json to_value v
  end
module BootstrapActionDetailList =
  struct
    type nonrec t = BootstrapActionDetail.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:BootstrapActionDetail.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BootstrapActionDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"BootstrapActionDetailList"
        ~of_json:BootstrapActionDetail.of_json j
    let to_json v = composed_to_json to_value v
  end
module StepId =
  struct
    type nonrec t = string
    let context_ = "StepId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StepId" j
    let to_json = simple_to_json to_value
  end
module CancelStepsRequestStatus =
  struct
    type nonrec t =
      | SUBMITTED 
      | FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SUBMITTED -> "SUBMITTED"
      | FAILED -> "FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "SUBMITTED" -> SUBMITTED
      | "FAILED" -> FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration CancelStepsRequestStatus" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"CancelStepsRequestStatus" j)
    let to_json = simple_to_json to_value
  end
module CancelStepsInfo =
  struct
    type nonrec t =
      {
      stepId: StepId.t option ;
      status: CancelStepsRequestStatus.t option ;
      reason: String_.t option }
    let make ?stepId =
      fun ?status -> fun ?reason -> fun () -> { stepId; status; reason }
    let to_value x =
      structure_to_value
        [("StepId", (Option.map x.stepId ~f:StepId.to_value));
        ("Status",
          (Option.map x.status ~f:CancelStepsRequestStatus.to_value));
        ("Reason", (Option.map x.reason ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reason =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Reason") in
      let status =
        (Option.map ~f:CancelStepsRequestStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let stepId =
        (Option.map ~f:StepId.of_xml) (Xml.child xml_arg0 "StepId") in
      make ?reason ?status ?stepId ()
    let of_json json =
      let reason = field_map json "Reason" String_.of_json in
      let status = field_map json "Status" CancelStepsRequestStatus.of_json in
      let stepId = field_map json "StepId" StepId.of_json in
      make ?reason ?status ?stepId ()
    let to_json v = composed_to_json to_value v
  end
module CancelStepsInfoList =
  struct
    type nonrec t = CancelStepsInfo.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:CancelStepsInfo.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CancelStepsInfo.of_xml)
    let of_json j =
      list_of_json ~kind:"CancelStepsInfoList"
        ~of_json:CancelStepsInfo.of_json j
    let to_json v = composed_to_json to_value v
  end
module StepCancellationOption =
  struct
    type nonrec t =
      | SEND_INTERRUPT 
      | TERMINATE_PROCESS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SEND_INTERRUPT -> "SEND_INTERRUPT"
      | TERMINATE_PROCESS -> "TERMINATE_PROCESS"
      | Non_static_id s -> s
    let of_string =
      function
      | "SEND_INTERRUPT" -> SEND_INTERRUPT
      | "TERMINATE_PROCESS" -> TERMINATE_PROCESS
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration StepCancellationOption" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"StepCancellationOption" j)
    let to_json = simple_to_json to_value
  end
module CancelStepsInput =
  struct
    type nonrec t =
      {
      clusterId: XmlStringMaxLen256.t ;
      stepIds: StepIdsList.t ;
      stepCancellationOption: StepCancellationOption.t option }
    let context_ = "CancelStepsInput"
    let make ?stepCancellationOption =
      fun ~clusterId ->
        fun ~stepIds ->
          fun () -> { stepCancellationOption; clusterId; stepIds }
    let to_value x =
      structure_to_value
        [("ClusterId", (Some (XmlStringMaxLen256.to_value x.clusterId)));
        ("StepIds", (Some (StepIdsList.to_value x.stepIds)));
        ("StepCancellationOption",
          (Option.map x.stepCancellationOption
             ~f:StepCancellationOption.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stepCancellationOption =
        (Option.map ~f:StepCancellationOption.of_xml)
          (Xml.child xml_arg0 "StepCancellationOption") in
      let stepIds =
        StepIdsList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "StepIds") in
      let clusterId =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClusterId") in
      make ?stepCancellationOption ~stepIds ~clusterId ()
    let of_json json =
      let stepCancellationOption =
        field_map json "StepCancellationOption"
          StepCancellationOption.of_json in
      let stepIds = field_map_exn json "StepIds" StepIdsList.of_json in
      let clusterId =
        field_map_exn json "ClusterId" XmlStringMaxLen256.of_json in
      make ?stepCancellationOption ~stepIds ~clusterId ()
    let to_json v = composed_to_json to_value v
  end
module CancelStepsOutput =
  struct
    type nonrec t = {
      cancelStepsInfoList: CancelStepsInfoList.t option }
    type nonrec error =
      [ `InternalServerError of InternalServerError.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?cancelStepsInfoList = fun () -> { cancelStepsInfoList }
    let error_of_json name json =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("CancelStepsInfoList",
           (Option.map x.cancelStepsInfoList ~f:CancelStepsInfoList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cancelStepsInfoList =
        (Option.map ~f:CancelStepsInfoList.of_xml)
          (Xml.child xml_arg0 "CancelStepsInfoList") in
      make ?cancelStepsInfoList ()
    let of_json json =
      let cancelStepsInfoList =
        field_map json "CancelStepsInfoList" CancelStepsInfoList.of_json in
      make ?cancelStepsInfoList ()
    let to_json v = composed_to_json to_value v
  end
module ScaleDownBehavior =
  struct
    type nonrec t =
      | TERMINATE_AT_INSTANCE_HOUR 
      | TERMINATE_AT_TASK_COMPLETION 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | TERMINATE_AT_INSTANCE_HOUR -> "TERMINATE_AT_INSTANCE_HOUR"
      | TERMINATE_AT_TASK_COMPLETION -> "TERMINATE_AT_TASK_COMPLETION"
      | Non_static_id s -> s
    let of_string =
      function
      | "TERMINATE_AT_INSTANCE_HOUR" -> TERMINATE_AT_INSTANCE_HOUR
      | "TERMINATE_AT_TASK_COMPLETION" -> TERMINATE_AT_TASK_COMPLETION
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ScaleDownBehavior" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ScaleDownBehavior" j)
    let to_json = simple_to_json to_value
  end
module RepoUpgradeOnBoot =
  struct
    type nonrec t =
      | SECURITY 
      | NONE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SECURITY -> "SECURITY"
      | NONE -> "NONE"
      | Non_static_id s -> s
    let of_string =
      function
      | "SECURITY" -> SECURITY
      | "NONE" -> NONE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration RepoUpgradeOnBoot" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"RepoUpgradeOnBoot" j)
    let to_json = simple_to_json to_value
  end
module PlacementGroupStrategy =
  struct
    type nonrec t =
      | SPREAD 
      | PARTITION 
      | CLUSTER 
      | NONE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SPREAD -> "SPREAD"
      | PARTITION -> "PARTITION"
      | CLUSTER -> "CLUSTER"
      | NONE -> "NONE"
      | Non_static_id s -> s
    let of_string =
      function
      | "SPREAD" -> SPREAD
      | "PARTITION" -> PARTITION
      | "CLUSTER" -> CLUSTER
      | "NONE" -> NONE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration PlacementGroupStrategy" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"PlacementGroupStrategy" j)
    let to_json = simple_to_json to_value
  end
module PlacementGroupConfig =
  struct
    type nonrec t =
      {
      instanceRole: InstanceRoleType.t ;
      placementStrategy: PlacementGroupStrategy.t option }
    let context_ = "PlacementGroupConfig"
    let make ?placementStrategy =
      fun ~instanceRole -> fun () -> { placementStrategy; instanceRole }
    let to_value x =
      structure_to_value
        [("InstanceRole", (Some (InstanceRoleType.to_value x.instanceRole)));
        ("PlacementStrategy",
          (Option.map x.placementStrategy ~f:PlacementGroupStrategy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let placementStrategy =
        (Option.map ~f:PlacementGroupStrategy.of_xml)
          (Xml.child xml_arg0 "PlacementStrategy") in
      let instanceRole =
        InstanceRoleType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceRole") in
      make ?placementStrategy ~instanceRole ()
    let of_json json =
      let placementStrategy =
        field_map json "PlacementStrategy" PlacementGroupStrategy.of_json in
      let instanceRole =
        field_map_exn json "InstanceRole" InstanceRoleType.of_json in
      make ?placementStrategy ~instanceRole ()
    let to_json v = composed_to_json to_value v
  end
module PlacementGroupConfigList =
  struct
    type nonrec t = PlacementGroupConfig.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:PlacementGroupConfig.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PlacementGroupConfig.of_xml)
    let of_json j =
      list_of_json ~kind:"PlacementGroupConfigList"
        ~of_json:PlacementGroupConfig.of_json j
    let to_json v = composed_to_json to_value v
  end
module OptionalArnType =
  struct
    type nonrec t = string
    let context_ = "OptionalArnType"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"OptionalArnType" j
    let to_json = simple_to_json to_value
  end
module KerberosAttributes =
  struct
    type nonrec t =
      {
      realm: XmlStringMaxLen256.t ;
      kdcAdminPassword: XmlStringMaxLen256.t ;
      crossRealmTrustPrincipalPassword: XmlStringMaxLen256.t option ;
      aDDomainJoinUser: XmlStringMaxLen256.t option ;
      aDDomainJoinPassword: XmlStringMaxLen256.t option }
    let context_ = "KerberosAttributes"
    let make ?crossRealmTrustPrincipalPassword =
      fun ?aDDomainJoinUser ->
        fun ?aDDomainJoinPassword ->
          fun ~realm ->
            fun ~kdcAdminPassword ->
              fun () ->
                {
                  crossRealmTrustPrincipalPassword;
                  aDDomainJoinUser;
                  aDDomainJoinPassword;
                  realm;
                  kdcAdminPassword
                }
    let to_value x =
      structure_to_value
        [("Realm", (Some (XmlStringMaxLen256.to_value x.realm)));
        ("KdcAdminPassword",
          (Some (XmlStringMaxLen256.to_value x.kdcAdminPassword)));
        ("CrossRealmTrustPrincipalPassword",
          (Option.map x.crossRealmTrustPrincipalPassword
             ~f:XmlStringMaxLen256.to_value));
        ("ADDomainJoinUser",
          (Option.map x.aDDomainJoinUser ~f:XmlStringMaxLen256.to_value));
        ("ADDomainJoinPassword",
          (Option.map x.aDDomainJoinPassword ~f:XmlStringMaxLen256.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let aDDomainJoinPassword =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "ADDomainJoinPassword") in
      let aDDomainJoinUser =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "ADDomainJoinUser") in
      let crossRealmTrustPrincipalPassword =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "CrossRealmTrustPrincipalPassword") in
      let kdcAdminPassword =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "KdcAdminPassword") in
      let realm =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Realm") in
      make ?aDDomainJoinPassword ?aDDomainJoinUser
        ?crossRealmTrustPrincipalPassword ~kdcAdminPassword ~realm ()
    let of_json json =
      let aDDomainJoinPassword =
        field_map json "ADDomainJoinPassword" XmlStringMaxLen256.of_json in
      let aDDomainJoinUser =
        field_map json "ADDomainJoinUser" XmlStringMaxLen256.of_json in
      let crossRealmTrustPrincipalPassword =
        field_map json "CrossRealmTrustPrincipalPassword"
          XmlStringMaxLen256.of_json in
      let kdcAdminPassword =
        field_map_exn json "KdcAdminPassword" XmlStringMaxLen256.of_json in
      let realm = field_map_exn json "Realm" XmlStringMaxLen256.of_json in
      make ?aDDomainJoinPassword ?aDDomainJoinUser
        ?crossRealmTrustPrincipalPassword ~kdcAdminPassword ~realm ()
    let to_json v = composed_to_json to_value v
  end
module InstanceCollectionType =
  struct
    type nonrec t =
      | INSTANCE_FLEET 
      | INSTANCE_GROUP 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | INSTANCE_FLEET -> "INSTANCE_FLEET"
      | INSTANCE_GROUP -> "INSTANCE_GROUP"
      | Non_static_id s -> s
    let of_string =
      function
      | "INSTANCE_FLEET" -> INSTANCE_FLEET
      | "INSTANCE_GROUP" -> INSTANCE_GROUP
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration InstanceCollectionType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"InstanceCollectionType" j)
    let to_json = simple_to_json to_value
  end
module XmlStringMaxLen256List =
  struct
    type nonrec t = XmlStringMaxLen256.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:XmlStringMaxLen256.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:XmlStringMaxLen256.of_xml)
    let of_json j =
      list_of_json ~kind:"XmlStringMaxLen256List"
        ~of_json:XmlStringMaxLen256.of_json j
    let to_json v = composed_to_json to_value v
  end
module Ec2InstanceAttributes =
  struct
    type nonrec t =
      {
      ec2KeyName: String_.t option ;
      ec2SubnetId: String_.t option ;
      requestedEc2SubnetIds: XmlStringMaxLen256List.t option ;
      ec2AvailabilityZone: String_.t option ;
      requestedEc2AvailabilityZones: XmlStringMaxLen256List.t option ;
      iamInstanceProfile: String_.t option ;
      emrManagedMasterSecurityGroup: String_.t option ;
      emrManagedSlaveSecurityGroup: String_.t option ;
      serviceAccessSecurityGroup: String_.t option ;
      additionalMasterSecurityGroups: StringList.t option ;
      additionalSlaveSecurityGroups: StringList.t option }
    let make ?ec2KeyName =
      fun ?ec2SubnetId ->
        fun ?requestedEc2SubnetIds ->
          fun ?ec2AvailabilityZone ->
            fun ?requestedEc2AvailabilityZones ->
              fun ?iamInstanceProfile ->
                fun ?emrManagedMasterSecurityGroup ->
                  fun ?emrManagedSlaveSecurityGroup ->
                    fun ?serviceAccessSecurityGroup ->
                      fun ?additionalMasterSecurityGroups ->
                        fun ?additionalSlaveSecurityGroups ->
                          fun () ->
                            {
                              ec2KeyName;
                              ec2SubnetId;
                              requestedEc2SubnetIds;
                              ec2AvailabilityZone;
                              requestedEc2AvailabilityZones;
                              iamInstanceProfile;
                              emrManagedMasterSecurityGroup;
                              emrManagedSlaveSecurityGroup;
                              serviceAccessSecurityGroup;
                              additionalMasterSecurityGroups;
                              additionalSlaveSecurityGroups
                            }
    let to_value x =
      structure_to_value
        [("Ec2KeyName", (Option.map x.ec2KeyName ~f:String_.to_value));
        ("Ec2SubnetId", (Option.map x.ec2SubnetId ~f:String_.to_value));
        ("RequestedEc2SubnetIds",
          (Option.map x.requestedEc2SubnetIds
             ~f:XmlStringMaxLen256List.to_value));
        ("Ec2AvailabilityZone",
          (Option.map x.ec2AvailabilityZone ~f:String_.to_value));
        ("RequestedEc2AvailabilityZones",
          (Option.map x.requestedEc2AvailabilityZones
             ~f:XmlStringMaxLen256List.to_value));
        ("IamInstanceProfile",
          (Option.map x.iamInstanceProfile ~f:String_.to_value));
        ("EmrManagedMasterSecurityGroup",
          (Option.map x.emrManagedMasterSecurityGroup ~f:String_.to_value));
        ("EmrManagedSlaveSecurityGroup",
          (Option.map x.emrManagedSlaveSecurityGroup ~f:String_.to_value));
        ("ServiceAccessSecurityGroup",
          (Option.map x.serviceAccessSecurityGroup ~f:String_.to_value));
        ("AdditionalMasterSecurityGroups",
          (Option.map x.additionalMasterSecurityGroups ~f:StringList.to_value));
        ("AdditionalSlaveSecurityGroups",
          (Option.map x.additionalSlaveSecurityGroups ~f:StringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let additionalSlaveSecurityGroups =
        (Option.map ~f:StringList.of_xml)
          (Xml.child xml_arg0 "AdditionalSlaveSecurityGroups") in
      let additionalMasterSecurityGroups =
        (Option.map ~f:StringList.of_xml)
          (Xml.child xml_arg0 "AdditionalMasterSecurityGroups") in
      let serviceAccessSecurityGroup =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "ServiceAccessSecurityGroup") in
      let emrManagedSlaveSecurityGroup =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "EmrManagedSlaveSecurityGroup") in
      let emrManagedMasterSecurityGroup =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "EmrManagedMasterSecurityGroup") in
      let iamInstanceProfile =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "IamInstanceProfile") in
      let requestedEc2AvailabilityZones =
        (Option.map ~f:XmlStringMaxLen256List.of_xml)
          (Xml.child xml_arg0 "RequestedEc2AvailabilityZones") in
      let ec2AvailabilityZone =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "Ec2AvailabilityZone") in
      let requestedEc2SubnetIds =
        (Option.map ~f:XmlStringMaxLen256List.of_xml)
          (Xml.child xml_arg0 "RequestedEc2SubnetIds") in
      let ec2SubnetId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Ec2SubnetId") in
      let ec2KeyName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Ec2KeyName") in
      make ?additionalSlaveSecurityGroups ?additionalMasterSecurityGroups
        ?serviceAccessSecurityGroup ?emrManagedSlaveSecurityGroup
        ?emrManagedMasterSecurityGroup ?iamInstanceProfile
        ?requestedEc2AvailabilityZones ?ec2AvailabilityZone
        ?requestedEc2SubnetIds ?ec2SubnetId ?ec2KeyName ()
    let of_json json =
      let additionalSlaveSecurityGroups =
        field_map json "AdditionalSlaveSecurityGroups" StringList.of_json in
      let additionalMasterSecurityGroups =
        field_map json "AdditionalMasterSecurityGroups" StringList.of_json in
      let serviceAccessSecurityGroup =
        field_map json "ServiceAccessSecurityGroup" String_.of_json in
      let emrManagedSlaveSecurityGroup =
        field_map json "EmrManagedSlaveSecurityGroup" String_.of_json in
      let emrManagedMasterSecurityGroup =
        field_map json "EmrManagedMasterSecurityGroup" String_.of_json in
      let iamInstanceProfile =
        field_map json "IamInstanceProfile" String_.of_json in
      let requestedEc2AvailabilityZones =
        field_map json "RequestedEc2AvailabilityZones"
          XmlStringMaxLen256List.of_json in
      let ec2AvailabilityZone =
        field_map json "Ec2AvailabilityZone" String_.of_json in
      let requestedEc2SubnetIds =
        field_map json "RequestedEc2SubnetIds" XmlStringMaxLen256List.of_json in
      let ec2SubnetId = field_map json "Ec2SubnetId" String_.of_json in
      let ec2KeyName = field_map json "Ec2KeyName" String_.of_json in
      make ?additionalSlaveSecurityGroups ?additionalMasterSecurityGroups
        ?serviceAccessSecurityGroup ?emrManagedSlaveSecurityGroup
        ?emrManagedMasterSecurityGroup ?iamInstanceProfile
        ?requestedEc2AvailabilityZones ?ec2AvailabilityZone
        ?requestedEc2SubnetIds ?ec2SubnetId ?ec2KeyName ()
    let to_json v = composed_to_json to_value v
  end
module ClusterTimeline =
  struct
    type nonrec t =
      {
      creationDateTime: Date.t option ;
      readyDateTime: Date.t option ;
      endDateTime: Date.t option }
    let make ?creationDateTime =
      fun ?readyDateTime ->
        fun ?endDateTime ->
          fun () -> { creationDateTime; readyDateTime; endDateTime }
    let to_value x =
      structure_to_value
        [("CreationDateTime",
           (Option.map x.creationDateTime ~f:Date.to_value));
        ("ReadyDateTime", (Option.map x.readyDateTime ~f:Date.to_value));
        ("EndDateTime", (Option.map x.endDateTime ~f:Date.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let endDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "EndDateTime") in
      let readyDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "ReadyDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "CreationDateTime") in
      make ?endDateTime ?readyDateTime ?creationDateTime ()
    let of_json json =
      let endDateTime = field_map json "EndDateTime" Date.of_json in
      let readyDateTime = field_map json "ReadyDateTime" Date.of_json in
      let creationDateTime = field_map json "CreationDateTime" Date.of_json in
      make ?endDateTime ?readyDateTime ?creationDateTime ()
    let to_json v = composed_to_json to_value v
  end
module ClusterStateChangeReasonCode =
  struct
    type nonrec t =
      | INTERNAL_ERROR 
      | VALIDATION_ERROR 
      | INSTANCE_FAILURE 
      | INSTANCE_FLEET_TIMEOUT 
      | BOOTSTRAP_FAILURE 
      | USER_REQUEST 
      | STEP_FAILURE 
      | ALL_STEPS_COMPLETED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | INTERNAL_ERROR -> "INTERNAL_ERROR"
      | VALIDATION_ERROR -> "VALIDATION_ERROR"
      | INSTANCE_FAILURE -> "INSTANCE_FAILURE"
      | INSTANCE_FLEET_TIMEOUT -> "INSTANCE_FLEET_TIMEOUT"
      | BOOTSTRAP_FAILURE -> "BOOTSTRAP_FAILURE"
      | USER_REQUEST -> "USER_REQUEST"
      | STEP_FAILURE -> "STEP_FAILURE"
      | ALL_STEPS_COMPLETED -> "ALL_STEPS_COMPLETED"
      | Non_static_id s -> s
    let of_string =
      function
      | "INTERNAL_ERROR" -> INTERNAL_ERROR
      | "VALIDATION_ERROR" -> VALIDATION_ERROR
      | "INSTANCE_FAILURE" -> INSTANCE_FAILURE
      | "INSTANCE_FLEET_TIMEOUT" -> INSTANCE_FLEET_TIMEOUT
      | "BOOTSTRAP_FAILURE" -> BOOTSTRAP_FAILURE
      | "USER_REQUEST" -> USER_REQUEST
      | "STEP_FAILURE" -> STEP_FAILURE
      | "ALL_STEPS_COMPLETED" -> ALL_STEPS_COMPLETED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ClusterStateChangeReasonCode"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ClusterStateChangeReasonCode" j)
    let to_json = simple_to_json to_value
  end
module ClusterStateChangeReason =
  struct
    type nonrec t =
      {
      code: ClusterStateChangeReasonCode.t option ;
      message: String_.t option }
    let make ?code = fun ?message -> fun () -> { code; message }
    let to_value x =
      structure_to_value
        [("Code",
           (Option.map x.code ~f:ClusterStateChangeReasonCode.to_value));
        ("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      let code =
        (Option.map ~f:ClusterStateChangeReasonCode.of_xml)
          (Xml.child xml_arg0 "Code") in
      make ?message ?code ()
    let of_json json =
      let message = field_map json "Message" String_.of_json in
      let code = field_map json "Code" ClusterStateChangeReasonCode.of_json in
      make ?message ?code ()
    let to_json v = composed_to_json to_value v
  end
module ClusterState =
  struct
    type nonrec t =
      | STARTING 
      | BOOTSTRAPPING 
      | RUNNING 
      | WAITING 
      | TERMINATING 
      | TERMINATED 
      | TERMINATED_WITH_ERRORS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | STARTING -> "STARTING"
      | BOOTSTRAPPING -> "BOOTSTRAPPING"
      | RUNNING -> "RUNNING"
      | WAITING -> "WAITING"
      | TERMINATING -> "TERMINATING"
      | TERMINATED -> "TERMINATED"
      | TERMINATED_WITH_ERRORS -> "TERMINATED_WITH_ERRORS"
      | Non_static_id s -> s
    let of_string =
      function
      | "STARTING" -> STARTING
      | "BOOTSTRAPPING" -> BOOTSTRAPPING
      | "RUNNING" -> RUNNING
      | "WAITING" -> WAITING
      | "TERMINATING" -> TERMINATING
      | "TERMINATED" -> TERMINATED
      | "TERMINATED_WITH_ERRORS" -> TERMINATED_WITH_ERRORS
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ClusterState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ClusterState" j)
    let to_json = simple_to_json to_value
  end
module ClusterStatus =
  struct
    type nonrec t =
      {
      state: ClusterState.t option ;
      stateChangeReason: ClusterStateChangeReason.t option ;
      timeline: ClusterTimeline.t option }
    let make ?state =
      fun ?stateChangeReason ->
        fun ?timeline -> fun () -> { state; stateChangeReason; timeline }
    let to_value x =
      structure_to_value
        [("State", (Option.map x.state ~f:ClusterState.to_value));
        ("StateChangeReason",
          (Option.map x.stateChangeReason
             ~f:ClusterStateChangeReason.to_value));
        ("Timeline", (Option.map x.timeline ~f:ClusterTimeline.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timeline =
        (Option.map ~f:ClusterTimeline.of_xml)
          (Xml.child xml_arg0 "Timeline") in
      let stateChangeReason =
        (Option.map ~f:ClusterStateChangeReason.of_xml)
          (Xml.child xml_arg0 "StateChangeReason") in
      let state =
        (Option.map ~f:ClusterState.of_xml) (Xml.child xml_arg0 "State") in
      make ?timeline ?stateChangeReason ?state ()
    let of_json json =
      let timeline = field_map json "Timeline" ClusterTimeline.of_json in
      let stateChangeReason =
        field_map json "StateChangeReason" ClusterStateChangeReason.of_json in
      let state = field_map json "State" ClusterState.of_json in
      make ?timeline ?stateChangeReason ?state ()
    let to_json v = composed_to_json to_value v
  end
module ClusterId =
  struct
    type nonrec t = string
    let context_ = "ClusterId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ClusterId" j
    let to_json = simple_to_json to_value
  end
module Cluster =
  struct
    type nonrec t =
      {
      id: ClusterId.t option ;
      name: String_.t option ;
      status: ClusterStatus.t option ;
      ec2InstanceAttributes: Ec2InstanceAttributes.t option ;
      instanceCollectionType: InstanceCollectionType.t option ;
      logUri: String_.t option ;
      logEncryptionKmsKeyId: String_.t option ;
      requestedAmiVersion: String_.t option ;
      runningAmiVersion: String_.t option ;
      releaseLabel: String_.t option ;
      autoTerminate: Boolean.t option ;
      terminationProtected: Boolean.t option ;
      visibleToAllUsers: Boolean.t option ;
      applications: ApplicationList.t option ;
      tags: TagList.t option ;
      serviceRole: String_.t option ;
      normalizedInstanceHours: Integer.t option ;
      masterPublicDnsName: String_.t option ;
      configurations: ConfigurationList.t option ;
      securityConfiguration: XmlString.t option ;
      autoScalingRole: XmlString.t option ;
      scaleDownBehavior: ScaleDownBehavior.t option ;
      customAmiId: XmlStringMaxLen256.t option ;
      ebsRootVolumeSize: Integer.t option ;
      repoUpgradeOnBoot: RepoUpgradeOnBoot.t option ;
      kerberosAttributes: KerberosAttributes.t option ;
      clusterArn: ArnType.t option ;
      outpostArn: OptionalArnType.t option ;
      stepConcurrencyLevel: Integer.t option ;
      placementGroups: PlacementGroupConfigList.t option }
    let make ?id =
      fun ?name ->
        fun ?status ->
          fun ?ec2InstanceAttributes ->
            fun ?instanceCollectionType ->
              fun ?logUri ->
                fun ?logEncryptionKmsKeyId ->
                  fun ?requestedAmiVersion ->
                    fun ?runningAmiVersion ->
                      fun ?releaseLabel ->
                        fun ?autoTerminate ->
                          fun ?terminationProtected ->
                            fun ?visibleToAllUsers ->
                              fun ?applications ->
                                fun ?tags ->
                                  fun ?serviceRole ->
                                    fun ?normalizedInstanceHours ->
                                      fun ?masterPublicDnsName ->
                                        fun ?configurations ->
                                          fun ?securityConfiguration ->
                                            fun ?autoScalingRole ->
                                              fun ?scaleDownBehavior ->
                                                fun ?customAmiId ->
                                                  fun ?ebsRootVolumeSize ->
                                                    fun ?repoUpgradeOnBoot ->
                                                      fun ?kerberosAttributes
                                                        ->
                                                        fun ?clusterArn ->
                                                          fun ?outpostArn ->
                                                            fun
                                                              ?stepConcurrencyLevel
                                                              ->
                                                              fun
                                                                ?placementGroups
                                                                ->
                                                                fun () ->
                                                                  {
                                                                    id;
                                                                    name;
                                                                    status;
                                                                    ec2InstanceAttributes;
                                                                    instanceCollectionType;
                                                                    logUri;
                                                                    logEncryptionKmsKeyId;
                                                                    requestedAmiVersion;
                                                                    runningAmiVersion;
                                                                    releaseLabel;
                                                                    autoTerminate;
                                                                    terminationProtected;
                                                                    visibleToAllUsers;
                                                                    applications;
                                                                    tags;
                                                                    serviceRole;
                                                                    normalizedInstanceHours;
                                                                    masterPublicDnsName;
                                                                    configurations;
                                                                    securityConfiguration;
                                                                    autoScalingRole;
                                                                    scaleDownBehavior;
                                                                    customAmiId;
                                                                    ebsRootVolumeSize;
                                                                    repoUpgradeOnBoot;
                                                                    kerberosAttributes;
                                                                    clusterArn;
                                                                    outpostArn;
                                                                    stepConcurrencyLevel;
                                                                    placementGroups
                                                                  }
    let to_value x =
      structure_to_value
        [("Id", (Option.map x.id ~f:ClusterId.to_value));
        ("Name", (Option.map x.name ~f:String_.to_value));
        ("Status", (Option.map x.status ~f:ClusterStatus.to_value));
        ("Ec2InstanceAttributes",
          (Option.map x.ec2InstanceAttributes
             ~f:Ec2InstanceAttributes.to_value));
        ("InstanceCollectionType",
          (Option.map x.instanceCollectionType
             ~f:InstanceCollectionType.to_value));
        ("LogUri", (Option.map x.logUri ~f:String_.to_value));
        ("LogEncryptionKmsKeyId",
          (Option.map x.logEncryptionKmsKeyId ~f:String_.to_value));
        ("RequestedAmiVersion",
          (Option.map x.requestedAmiVersion ~f:String_.to_value));
        ("RunningAmiVersion",
          (Option.map x.runningAmiVersion ~f:String_.to_value));
        ("ReleaseLabel", (Option.map x.releaseLabel ~f:String_.to_value));
        ("AutoTerminate", (Option.map x.autoTerminate ~f:Boolean.to_value));
        ("TerminationProtected",
          (Option.map x.terminationProtected ~f:Boolean.to_value));
        ("VisibleToAllUsers",
          (Option.map x.visibleToAllUsers ~f:Boolean.to_value));
        ("Applications",
          (Option.map x.applications ~f:ApplicationList.to_value));
        ("Tags", (Option.map x.tags ~f:TagList.to_value));
        ("ServiceRole", (Option.map x.serviceRole ~f:String_.to_value));
        ("NormalizedInstanceHours",
          (Option.map x.normalizedInstanceHours ~f:Integer.to_value));
        ("MasterPublicDnsName",
          (Option.map x.masterPublicDnsName ~f:String_.to_value));
        ("Configurations",
          (Option.map x.configurations ~f:ConfigurationList.to_value));
        ("SecurityConfiguration",
          (Option.map x.securityConfiguration ~f:XmlString.to_value));
        ("AutoScalingRole",
          (Option.map x.autoScalingRole ~f:XmlString.to_value));
        ("ScaleDownBehavior",
          (Option.map x.scaleDownBehavior ~f:ScaleDownBehavior.to_value));
        ("CustomAmiId",
          (Option.map x.customAmiId ~f:XmlStringMaxLen256.to_value));
        ("EbsRootVolumeSize",
          (Option.map x.ebsRootVolumeSize ~f:Integer.to_value));
        ("RepoUpgradeOnBoot",
          (Option.map x.repoUpgradeOnBoot ~f:RepoUpgradeOnBoot.to_value));
        ("KerberosAttributes",
          (Option.map x.kerberosAttributes ~f:KerberosAttributes.to_value));
        ("ClusterArn", (Option.map x.clusterArn ~f:ArnType.to_value));
        ("OutpostArn", (Option.map x.outpostArn ~f:OptionalArnType.to_value));
        ("StepConcurrencyLevel",
          (Option.map x.stepConcurrencyLevel ~f:Integer.to_value));
        ("PlacementGroups",
          (Option.map x.placementGroups ~f:PlacementGroupConfigList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let placementGroups =
        (Option.map ~f:PlacementGroupConfigList.of_xml)
          (Xml.child xml_arg0 "PlacementGroups") in
      let stepConcurrencyLevel =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "StepConcurrencyLevel") in
      let outpostArn =
        (Option.map ~f:OptionalArnType.of_xml)
          (Xml.child xml_arg0 "OutpostArn") in
      let clusterArn =
        (Option.map ~f:ArnType.of_xml) (Xml.child xml_arg0 "ClusterArn") in
      let kerberosAttributes =
        (Option.map ~f:KerberosAttributes.of_xml)
          (Xml.child xml_arg0 "KerberosAttributes") in
      let repoUpgradeOnBoot =
        (Option.map ~f:RepoUpgradeOnBoot.of_xml)
          (Xml.child xml_arg0 "RepoUpgradeOnBoot") in
      let ebsRootVolumeSize =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "EbsRootVolumeSize") in
      let customAmiId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "CustomAmiId") in
      let scaleDownBehavior =
        (Option.map ~f:ScaleDownBehavior.of_xml)
          (Xml.child xml_arg0 "ScaleDownBehavior") in
      let autoScalingRole =
        (Option.map ~f:XmlString.of_xml)
          (Xml.child xml_arg0 "AutoScalingRole") in
      let securityConfiguration =
        (Option.map ~f:XmlString.of_xml)
          (Xml.child xml_arg0 "SecurityConfiguration") in
      let configurations =
        (Option.map ~f:ConfigurationList.of_xml)
          (Xml.child xml_arg0 "Configurations") in
      let masterPublicDnsName =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "MasterPublicDnsName") in
      let normalizedInstanceHours =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "NormalizedInstanceHours") in
      let serviceRole =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ServiceRole") in
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let applications =
        (Option.map ~f:ApplicationList.of_xml)
          (Xml.child xml_arg0 "Applications") in
      let visibleToAllUsers =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "VisibleToAllUsers") in
      let terminationProtected =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "TerminationProtected") in
      let autoTerminate =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "AutoTerminate") in
      let releaseLabel =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ReleaseLabel") in
      let runningAmiVersion =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "RunningAmiVersion") in
      let requestedAmiVersion =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "RequestedAmiVersion") in
      let logEncryptionKmsKeyId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "LogEncryptionKmsKeyId") in
      let logUri =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "LogUri") in
      let instanceCollectionType =
        (Option.map ~f:InstanceCollectionType.of_xml)
          (Xml.child xml_arg0 "InstanceCollectionType") in
      let ec2InstanceAttributes =
        (Option.map ~f:Ec2InstanceAttributes.of_xml)
          (Xml.child xml_arg0 "Ec2InstanceAttributes") in
      let status =
        (Option.map ~f:ClusterStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      let id = (Option.map ~f:ClusterId.of_xml) (Xml.child xml_arg0 "Id") in
      make ?placementGroups ?stepConcurrencyLevel ?outpostArn ?clusterArn
        ?kerberosAttributes ?repoUpgradeOnBoot ?ebsRootVolumeSize
        ?customAmiId ?scaleDownBehavior ?autoScalingRole
        ?securityConfiguration ?configurations ?masterPublicDnsName
        ?normalizedInstanceHours ?serviceRole ?tags ?applications
        ?visibleToAllUsers ?terminationProtected ?autoTerminate ?releaseLabel
        ?runningAmiVersion ?requestedAmiVersion ?logEncryptionKmsKeyId
        ?logUri ?instanceCollectionType ?ec2InstanceAttributes ?status ?name
        ?id ()
    let of_json json =
      let placementGroups =
        field_map json "PlacementGroups" PlacementGroupConfigList.of_json in
      let stepConcurrencyLevel =
        field_map json "StepConcurrencyLevel" Integer.of_json in
      let outpostArn = field_map json "OutpostArn" OptionalArnType.of_json in
      let clusterArn = field_map json "ClusterArn" ArnType.of_json in
      let kerberosAttributes =
        field_map json "KerberosAttributes" KerberosAttributes.of_json in
      let repoUpgradeOnBoot =
        field_map json "RepoUpgradeOnBoot" RepoUpgradeOnBoot.of_json in
      let ebsRootVolumeSize =
        field_map json "EbsRootVolumeSize" Integer.of_json in
      let customAmiId =
        field_map json "CustomAmiId" XmlStringMaxLen256.of_json in
      let scaleDownBehavior =
        field_map json "ScaleDownBehavior" ScaleDownBehavior.of_json in
      let autoScalingRole =
        field_map json "AutoScalingRole" XmlString.of_json in
      let securityConfiguration =
        field_map json "SecurityConfiguration" XmlString.of_json in
      let configurations =
        field_map json "Configurations" ConfigurationList.of_json in
      let masterPublicDnsName =
        field_map json "MasterPublicDnsName" String_.of_json in
      let normalizedInstanceHours =
        field_map json "NormalizedInstanceHours" Integer.of_json in
      let serviceRole = field_map json "ServiceRole" String_.of_json in
      let tags = field_map json "Tags" TagList.of_json in
      let applications =
        field_map json "Applications" ApplicationList.of_json in
      let visibleToAllUsers =
        field_map json "VisibleToAllUsers" Boolean.of_json in
      let terminationProtected =
        field_map json "TerminationProtected" Boolean.of_json in
      let autoTerminate = field_map json "AutoTerminate" Boolean.of_json in
      let releaseLabel = field_map json "ReleaseLabel" String_.of_json in
      let runningAmiVersion =
        field_map json "RunningAmiVersion" String_.of_json in
      let requestedAmiVersion =
        field_map json "RequestedAmiVersion" String_.of_json in
      let logEncryptionKmsKeyId =
        field_map json "LogEncryptionKmsKeyId" String_.of_json in
      let logUri = field_map json "LogUri" String_.of_json in
      let instanceCollectionType =
        field_map json "InstanceCollectionType"
          InstanceCollectionType.of_json in
      let ec2InstanceAttributes =
        field_map json "Ec2InstanceAttributes" Ec2InstanceAttributes.of_json in
      let status = field_map json "Status" ClusterStatus.of_json in
      let name = field_map json "Name" String_.of_json in
      let id = field_map json "Id" ClusterId.of_json in
      make ?placementGroups ?stepConcurrencyLevel ?outpostArn ?clusterArn
        ?kerberosAttributes ?repoUpgradeOnBoot ?ebsRootVolumeSize
        ?customAmiId ?scaleDownBehavior ?autoScalingRole
        ?securityConfiguration ?configurations ?masterPublicDnsName
        ?normalizedInstanceHours ?serviceRole ?tags ?applications
        ?visibleToAllUsers ?terminationProtected ?autoTerminate ?releaseLabel
        ?runningAmiVersion ?requestedAmiVersion ?logEncryptionKmsKeyId
        ?logUri ?instanceCollectionType ?ec2InstanceAttributes ?status ?name
        ?id ()
    let to_json v = composed_to_json to_value v
  end
module ClusterStateList =
  struct
    type nonrec t = ClusterState.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ClusterState.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ClusterState.of_xml)
    let of_json j =
      list_of_json ~kind:"ClusterStateList" ~of_json:ClusterState.of_json j
    let to_json v = composed_to_json to_value v
  end
module ClusterSummary =
  struct
    type nonrec t =
      {
      id: ClusterId.t option ;
      name: String_.t option ;
      status: ClusterStatus.t option ;
      normalizedInstanceHours: Integer.t option ;
      clusterArn: ArnType.t option ;
      outpostArn: OptionalArnType.t option }
    let make ?id =
      fun ?name ->
        fun ?status ->
          fun ?normalizedInstanceHours ->
            fun ?clusterArn ->
              fun ?outpostArn ->
                fun () ->
                  {
                    id;
                    name;
                    status;
                    normalizedInstanceHours;
                    clusterArn;
                    outpostArn
                  }
    let to_value x =
      structure_to_value
        [("Id", (Option.map x.id ~f:ClusterId.to_value));
        ("Name", (Option.map x.name ~f:String_.to_value));
        ("Status", (Option.map x.status ~f:ClusterStatus.to_value));
        ("NormalizedInstanceHours",
          (Option.map x.normalizedInstanceHours ~f:Integer.to_value));
        ("ClusterArn", (Option.map x.clusterArn ~f:ArnType.to_value));
        ("OutpostArn", (Option.map x.outpostArn ~f:OptionalArnType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let outpostArn =
        (Option.map ~f:OptionalArnType.of_xml)
          (Xml.child xml_arg0 "OutpostArn") in
      let clusterArn =
        (Option.map ~f:ArnType.of_xml) (Xml.child xml_arg0 "ClusterArn") in
      let normalizedInstanceHours =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "NormalizedInstanceHours") in
      let status =
        (Option.map ~f:ClusterStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      let id = (Option.map ~f:ClusterId.of_xml) (Xml.child xml_arg0 "Id") in
      make ?outpostArn ?clusterArn ?normalizedInstanceHours ?status ?name ?id
        ()
    let of_json json =
      let outpostArn = field_map json "OutpostArn" OptionalArnType.of_json in
      let clusterArn = field_map json "ClusterArn" ArnType.of_json in
      let normalizedInstanceHours =
        field_map json "NormalizedInstanceHours" Integer.of_json in
      let status = field_map json "Status" ClusterStatus.of_json in
      let name = field_map json "Name" String_.of_json in
      let id = field_map json "Id" ClusterId.of_json in
      make ?outpostArn ?clusterArn ?normalizedInstanceHours ?status ?name ?id
        ()
    let to_json v = composed_to_json to_value v
  end
module ClusterSummaryList =
  struct
    type nonrec t = ClusterSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ClusterSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ClusterSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"ClusterSummaryList" ~of_json:ClusterSummary.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module Command =
  struct
    type nonrec t =
      {
      name: String_.t option ;
      scriptPath: String_.t option ;
      args: StringList.t option }
    let make ?name =
      fun ?scriptPath -> fun ?args -> fun () -> { name; scriptPath; args }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:String_.to_value));
        ("ScriptPath", (Option.map x.scriptPath ~f:String_.to_value));
        ("Args", (Option.map x.args ~f:StringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let args =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "Args") in
      let scriptPath =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ScriptPath") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      make ?args ?scriptPath ?name ()
    let of_json json =
      let args = field_map json "Args" StringList.of_json in
      let scriptPath = field_map json "ScriptPath" String_.of_json in
      let name = field_map json "Name" String_.of_json in
      make ?args ?scriptPath ?name ()
    let to_json v = composed_to_json to_value v
  end
module CommandList =
  struct
    type nonrec t = Command.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Command.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Command.of_xml)
    let of_json j =
      list_of_json ~kind:"CommandList" ~of_json:Command.of_json j
    let to_json v = composed_to_json to_value v
  end
module ComputeLimitsUnitType =
  struct
    type nonrec t =
      | InstanceFleetUnits 
      | Instances 
      | VCPU 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | InstanceFleetUnits -> "InstanceFleetUnits"
      | Instances -> "Instances"
      | VCPU -> "VCPU"
      | Non_static_id s -> s
    let of_string =
      function
      | "InstanceFleetUnits" -> InstanceFleetUnits
      | "Instances" -> Instances
      | "VCPU" -> VCPU
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ComputeLimitsUnitType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ComputeLimitsUnitType" j)
    let to_json = simple_to_json to_value
  end
module ComputeLimits =
  struct
    type nonrec t =
      {
      unitType: ComputeLimitsUnitType.t ;
      minimumCapacityUnits: Integer.t ;
      maximumCapacityUnits: Integer.t ;
      maximumOnDemandCapacityUnits: Integer.t option ;
      maximumCoreCapacityUnits: Integer.t option }
    let context_ = "ComputeLimits"
    let make ?maximumOnDemandCapacityUnits =
      fun ?maximumCoreCapacityUnits ->
        fun ~unitType ->
          fun ~minimumCapacityUnits ->
            fun ~maximumCapacityUnits ->
              fun () ->
                {
                  maximumOnDemandCapacityUnits;
                  maximumCoreCapacityUnits;
                  unitType;
                  minimumCapacityUnits;
                  maximumCapacityUnits
                }
    let to_value x =
      structure_to_value
        [("UnitType", (Some (ComputeLimitsUnitType.to_value x.unitType)));
        ("MinimumCapacityUnits",
          (Some (Integer.to_value x.minimumCapacityUnits)));
        ("MaximumCapacityUnits",
          (Some (Integer.to_value x.maximumCapacityUnits)));
        ("MaximumOnDemandCapacityUnits",
          (Option.map x.maximumOnDemandCapacityUnits ~f:Integer.to_value));
        ("MaximumCoreCapacityUnits",
          (Option.map x.maximumCoreCapacityUnits ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maximumCoreCapacityUnits =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "MaximumCoreCapacityUnits") in
      let maximumOnDemandCapacityUnits =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "MaximumOnDemandCapacityUnits") in
      let maximumCapacityUnits =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MaximumCapacityUnits") in
      let minimumCapacityUnits =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MinimumCapacityUnits") in
      let unitType =
        ComputeLimitsUnitType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "UnitType") in
      make ?maximumCoreCapacityUnits ?maximumOnDemandCapacityUnits
        ~maximumCapacityUnits ~minimumCapacityUnits ~unitType ()
    let of_json json =
      let maximumCoreCapacityUnits =
        field_map json "MaximumCoreCapacityUnits" Integer.of_json in
      let maximumOnDemandCapacityUnits =
        field_map json "MaximumOnDemandCapacityUnits" Integer.of_json in
      let maximumCapacityUnits =
        field_map_exn json "MaximumCapacityUnits" Integer.of_json in
      let minimumCapacityUnits =
        field_map_exn json "MinimumCapacityUnits" Integer.of_json in
      let unitType =
        field_map_exn json "UnitType" ComputeLimitsUnitType.of_json in
      make ?maximumCoreCapacityUnits ?maximumOnDemandCapacityUnits
        ~maximumCapacityUnits ~minimumCapacityUnits ~unitType ()
    let to_json v = composed_to_json to_value v
  end
module CreateSecurityConfigurationInput =
  struct
    type nonrec t = {
      name: XmlString.t ;
      securityConfiguration: String_.t }
    let context_ = "CreateSecurityConfigurationInput"
    let make ~name =
      fun ~securityConfiguration -> fun () -> { name; securityConfiguration }
    let to_value x =
      structure_to_value
        [("Name", (Some (XmlString.to_value x.name)));
        ("SecurityConfiguration",
          (Some (String_.to_value x.securityConfiguration)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let securityConfiguration =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SecurityConfiguration") in
      let name =
        XmlString.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ~securityConfiguration ~name ()
    let of_json json =
      let securityConfiguration =
        field_map_exn json "SecurityConfiguration" String_.of_json in
      let name = field_map_exn json "Name" XmlString.of_json in
      make ~securityConfiguration ~name ()
    let to_json v = composed_to_json to_value v
  end
module CreateSecurityConfigurationOutput =
  struct
    type nonrec t = {
      name: XmlString.t ;
      creationDateTime: Date.t }
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "CreateSecurityConfigurationOutput"
    let make ~name =
      fun ~creationDateTime -> fun () -> { name; creationDateTime }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Name", (Some (XmlString.to_value x.name)));
        ("CreationDateTime", (Some (Date.to_value x.creationDateTime)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creationDateTime =
        Date.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CreationDateTime") in
      let name =
        XmlString.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ~creationDateTime ~name ()
    let of_json json =
      let creationDateTime =
        field_map_exn json "CreationDateTime" Date.of_json in
      let name = field_map_exn json "Name" XmlString.of_json in
      make ~creationDateTime ~name ()
    let to_json v = composed_to_json to_value v
  end
module SubnetIdList =
  struct
    type nonrec t = String_.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"SubnetIdList" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module CreateStudioInput =
  struct
    type nonrec t =
      {
      name: XmlStringMaxLen256.t ;
      description: XmlStringMaxLen256.t option ;
      authMode: AuthMode.t ;
      vpcId: XmlStringMaxLen256.t ;
      subnetIds: SubnetIdList.t ;
      serviceRole: XmlString.t ;
      userRole: XmlString.t option ;
      workspaceSecurityGroupId: XmlStringMaxLen256.t ;
      engineSecurityGroupId: XmlStringMaxLen256.t ;
      defaultS3Location: XmlString.t ;
      idpAuthUrl: XmlString.t option ;
      idpRelayStateParameterName: XmlStringMaxLen256.t option ;
      tags: TagList.t option }
    let context_ = "CreateStudioInput"
    let make ?description =
      fun ?userRole ->
        fun ?idpAuthUrl ->
          fun ?idpRelayStateParameterName ->
            fun ?tags ->
              fun ~name ->
                fun ~authMode ->
                  fun ~vpcId ->
                    fun ~subnetIds ->
                      fun ~serviceRole ->
                        fun ~workspaceSecurityGroupId ->
                          fun ~engineSecurityGroupId ->
                            fun ~defaultS3Location ->
                              fun () ->
                                {
                                  description;
                                  userRole;
                                  idpAuthUrl;
                                  idpRelayStateParameterName;
                                  tags;
                                  name;
                                  authMode;
                                  vpcId;
                                  subnetIds;
                                  serviceRole;
                                  workspaceSecurityGroupId;
                                  engineSecurityGroupId;
                                  defaultS3Location
                                }
    let to_value x =
      structure_to_value
        [("Name", (Some (XmlStringMaxLen256.to_value x.name)));
        ("Description",
          (Option.map x.description ~f:XmlStringMaxLen256.to_value));
        ("AuthMode", (Some (AuthMode.to_value x.authMode)));
        ("VpcId", (Some (XmlStringMaxLen256.to_value x.vpcId)));
        ("SubnetIds", (Some (SubnetIdList.to_value x.subnetIds)));
        ("ServiceRole", (Some (XmlString.to_value x.serviceRole)));
        ("UserRole", (Option.map x.userRole ~f:XmlString.to_value));
        ("WorkspaceSecurityGroupId",
          (Some (XmlStringMaxLen256.to_value x.workspaceSecurityGroupId)));
        ("EngineSecurityGroupId",
          (Some (XmlStringMaxLen256.to_value x.engineSecurityGroupId)));
        ("DefaultS3Location",
          (Some (XmlString.to_value x.defaultS3Location)));
        ("IdpAuthUrl", (Option.map x.idpAuthUrl ~f:XmlString.to_value));
        ("IdpRelayStateParameterName",
          (Option.map x.idpRelayStateParameterName
             ~f:XmlStringMaxLen256.to_value));
        ("Tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let idpRelayStateParameterName =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "IdpRelayStateParameterName") in
      let idpAuthUrl =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "IdpAuthUrl") in
      let defaultS3Location =
        XmlString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DefaultS3Location") in
      let engineSecurityGroupId =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "EngineSecurityGroupId") in
      let workspaceSecurityGroupId =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "WorkspaceSecurityGroupId") in
      let userRole =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "UserRole") in
      let serviceRole =
        XmlString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServiceRole") in
      let subnetIds =
        SubnetIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SubnetIds") in
      let vpcId =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VpcId") in
      let authMode =
        AuthMode.of_xml (Xml.child_exn ~context:context_ xml_arg0 "AuthMode") in
      let description =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "Description") in
      let name =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ?tags ?idpRelayStateParameterName ?idpAuthUrl ~defaultS3Location
        ~engineSecurityGroupId ~workspaceSecurityGroupId ?userRole
        ~serviceRole ~subnetIds ~vpcId ~authMode ?description ~name ()
    let of_json json =
      let tags = field_map json "Tags" TagList.of_json in
      let idpRelayStateParameterName =
        field_map json "IdpRelayStateParameterName"
          XmlStringMaxLen256.of_json in
      let idpAuthUrl = field_map json "IdpAuthUrl" XmlString.of_json in
      let defaultS3Location =
        field_map_exn json "DefaultS3Location" XmlString.of_json in
      let engineSecurityGroupId =
        field_map_exn json "EngineSecurityGroupId" XmlStringMaxLen256.of_json in
      let workspaceSecurityGroupId =
        field_map_exn json "WorkspaceSecurityGroupId"
          XmlStringMaxLen256.of_json in
      let userRole = field_map json "UserRole" XmlString.of_json in
      let serviceRole = field_map_exn json "ServiceRole" XmlString.of_json in
      let subnetIds = field_map_exn json "SubnetIds" SubnetIdList.of_json in
      let vpcId = field_map_exn json "VpcId" XmlStringMaxLen256.of_json in
      let authMode = field_map_exn json "AuthMode" AuthMode.of_json in
      let description =
        field_map json "Description" XmlStringMaxLen256.of_json in
      let name = field_map_exn json "Name" XmlStringMaxLen256.of_json in
      make ?tags ?idpRelayStateParameterName ?idpAuthUrl ~defaultS3Location
        ~engineSecurityGroupId ~workspaceSecurityGroupId ?userRole
        ~serviceRole ~subnetIds ~vpcId ~authMode ?description ~name ()
    let to_json v = composed_to_json to_value v
  end
module CreateStudioOutput =
  struct
    type nonrec t =
      {
      studioId: XmlStringMaxLen256.t option ;
      url: XmlString.t option }
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?studioId = fun ?url -> fun () -> { studioId; url }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("StudioId", (Option.map x.studioId ~f:XmlStringMaxLen256.to_value));
        ("Url", (Option.map x.url ~f:XmlString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let url = (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "Url") in
      let studioId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "StudioId") in
      make ?url ?studioId ()
    let of_json json =
      let url = field_map json "Url" XmlString.of_json in
      let studioId = field_map json "StudioId" XmlStringMaxLen256.of_json in
      make ?url ?studioId ()
    let to_json v = composed_to_json to_value v
  end
module IdentityType =
  struct
    type nonrec t =
      | USER 
      | GROUP 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | USER -> "USER" | GROUP -> "GROUP" | Non_static_id s -> s
    let of_string =
      function | "USER" -> USER | "GROUP" -> GROUP | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration IdentityType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"IdentityType" j)
    let to_json = simple_to_json to_value
  end
module CreateStudioSessionMappingInput =
  struct
    type nonrec t =
      {
      studioId: XmlStringMaxLen256.t ;
      identityId: XmlStringMaxLen256.t option ;
      identityName: XmlStringMaxLen256.t option ;
      identityType: IdentityType.t ;
      sessionPolicyArn: XmlStringMaxLen256.t }
    let context_ = "CreateStudioSessionMappingInput"
    let make ?identityId =
      fun ?identityName ->
        fun ~studioId ->
          fun ~identityType ->
            fun ~sessionPolicyArn ->
              fun () ->
                {
                  identityId;
                  identityName;
                  studioId;
                  identityType;
                  sessionPolicyArn
                }
    let to_value x =
      structure_to_value
        [("StudioId", (Some (XmlStringMaxLen256.to_value x.studioId)));
        ("IdentityId",
          (Option.map x.identityId ~f:XmlStringMaxLen256.to_value));
        ("IdentityName",
          (Option.map x.identityName ~f:XmlStringMaxLen256.to_value));
        ("IdentityType", (Some (IdentityType.to_value x.identityType)));
        ("SessionPolicyArn",
          (Some (XmlStringMaxLen256.to_value x.sessionPolicyArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionPolicyArn =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SessionPolicyArn") in
      let identityType =
        IdentityType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IdentityType") in
      let identityName =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "IdentityName") in
      let identityId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "IdentityId") in
      let studioId =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "StudioId") in
      make ~sessionPolicyArn ~identityType ?identityName ?identityId
        ~studioId ()
    let of_json json =
      let sessionPolicyArn =
        field_map_exn json "SessionPolicyArn" XmlStringMaxLen256.of_json in
      let identityType =
        field_map_exn json "IdentityType" IdentityType.of_json in
      let identityName =
        field_map json "IdentityName" XmlStringMaxLen256.of_json in
      let identityId = field_map json "IdentityId" XmlStringMaxLen256.of_json in
      let studioId = field_map_exn json "StudioId" XmlStringMaxLen256.of_json in
      make ~sessionPolicyArn ~identityType ?identityName ?identityId
        ~studioId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteSecurityConfigurationInput =
  struct
    type nonrec t = {
      name: XmlString.t }
    let context_ = "DeleteSecurityConfigurationInput"
    let make ~name = fun () -> { name }
    let to_value x =
      structure_to_value [("Name", (Some (XmlString.to_value x.name)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        XmlString.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ~name ()
    let of_json json =
      let name = field_map_exn json "Name" XmlString.of_json in make ~name ()
    let to_json v = composed_to_json to_value v
  end
module DeleteSecurityConfigurationOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module DeleteStudioInput =
  struct
    type nonrec t = {
      studioId: XmlStringMaxLen256.t }
    let context_ = "DeleteStudioInput"
    let make ~studioId = fun () -> { studioId }
    let to_value x =
      structure_to_value
        [("StudioId", (Some (XmlStringMaxLen256.to_value x.studioId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let studioId =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "StudioId") in
      make ~studioId ()
    let of_json json =
      let studioId = field_map_exn json "StudioId" XmlStringMaxLen256.of_json in
      make ~studioId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteStudioSessionMappingInput =
  struct
    type nonrec t =
      {
      studioId: XmlStringMaxLen256.t ;
      identityId: XmlStringMaxLen256.t option ;
      identityName: XmlStringMaxLen256.t option ;
      identityType: IdentityType.t }
    let context_ = "DeleteStudioSessionMappingInput"
    let make ?identityId =
      fun ?identityName ->
        fun ~studioId ->
          fun ~identityType ->
            fun () -> { identityId; identityName; studioId; identityType }
    let to_value x =
      structure_to_value
        [("StudioId", (Some (XmlStringMaxLen256.to_value x.studioId)));
        ("IdentityId",
          (Option.map x.identityId ~f:XmlStringMaxLen256.to_value));
        ("IdentityName",
          (Option.map x.identityName ~f:XmlStringMaxLen256.to_value));
        ("IdentityType", (Some (IdentityType.to_value x.identityType)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let identityType =
        IdentityType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IdentityType") in
      let identityName =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "IdentityName") in
      let identityId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "IdentityId") in
      let studioId =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "StudioId") in
      make ~identityType ?identityName ?identityId ~studioId ()
    let of_json json =
      let identityType =
        field_map_exn json "IdentityType" IdentityType.of_json in
      let identityName =
        field_map json "IdentityName" XmlStringMaxLen256.of_json in
      let identityId = field_map json "IdentityId" XmlStringMaxLen256.of_json in
      let studioId = field_map_exn json "StudioId" XmlStringMaxLen256.of_json in
      make ~identityType ?identityName ?identityId ~studioId ()
    let to_json v = composed_to_json to_value v
  end
module DescribeClusterInput =
  struct
    type nonrec t = {
      clusterId: ClusterId.t }
    let context_ = "DescribeClusterInput"
    let make ~clusterId = fun () -> { clusterId }
    let to_value x =
      structure_to_value
        [("ClusterId", (Some (ClusterId.to_value x.clusterId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clusterId =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClusterId") in
      make ~clusterId ()
    let of_json json =
      let clusterId = field_map_exn json "ClusterId" ClusterId.of_json in
      make ~clusterId ()
    let to_json v = composed_to_json to_value v
  end
module DescribeClusterOutput =
  struct
    type nonrec t = {
      cluster: Cluster.t option }
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?cluster = fun () -> { cluster }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Cluster", (Option.map x.cluster ~f:Cluster.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cluster =
        (Option.map ~f:Cluster.of_xml) (Xml.child xml_arg0 "Cluster") in
      make ?cluster ()
    let of_json json =
      let cluster = field_map json "Cluster" Cluster.of_json in
      make ?cluster ()
    let to_json v = composed_to_json to_value v
  end
module JobFlowExecutionState =
  struct
    type nonrec t =
      | STARTING 
      | BOOTSTRAPPING 
      | RUNNING 
      | WAITING 
      | SHUTTING_DOWN 
      | TERMINATED 
      | COMPLETED 
      | FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | STARTING -> "STARTING"
      | BOOTSTRAPPING -> "BOOTSTRAPPING"
      | RUNNING -> "RUNNING"
      | WAITING -> "WAITING"
      | SHUTTING_DOWN -> "SHUTTING_DOWN"
      | TERMINATED -> "TERMINATED"
      | COMPLETED -> "COMPLETED"
      | FAILED -> "FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "STARTING" -> STARTING
      | "BOOTSTRAPPING" -> BOOTSTRAPPING
      | "RUNNING" -> RUNNING
      | "WAITING" -> WAITING
      | "SHUTTING_DOWN" -> SHUTTING_DOWN
      | "TERMINATED" -> TERMINATED
      | "COMPLETED" -> COMPLETED
      | "FAILED" -> FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration JobFlowExecutionState" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"JobFlowExecutionState" j)
    let to_json = simple_to_json to_value
  end
module JobFlowExecutionStateList =
  struct
    type nonrec t = JobFlowExecutionState.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:JobFlowExecutionState.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:JobFlowExecutionState.of_xml)
    let of_json j =
      list_of_json ~kind:"JobFlowExecutionStateList"
        ~of_json:JobFlowExecutionState.of_json j
    let to_json v = composed_to_json to_value v
  end
module DescribeJobFlowsInput =
  struct
    type nonrec t =
      {
      createdAfter: Date.t option ;
      createdBefore: Date.t option ;
      jobFlowIds: XmlStringList.t option ;
      jobFlowStates: JobFlowExecutionStateList.t option }
    let make ?createdAfter =
      fun ?createdBefore ->
        fun ?jobFlowIds ->
          fun ?jobFlowStates ->
            fun () ->
              { createdAfter; createdBefore; jobFlowIds; jobFlowStates }
    let to_value x =
      structure_to_value
        [("CreatedAfter", (Option.map x.createdAfter ~f:Date.to_value));
        ("CreatedBefore", (Option.map x.createdBefore ~f:Date.to_value));
        ("JobFlowIds", (Option.map x.jobFlowIds ~f:XmlStringList.to_value));
        ("JobFlowStates",
          (Option.map x.jobFlowStates ~f:JobFlowExecutionStateList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let jobFlowStates =
        (Option.map ~f:JobFlowExecutionStateList.of_xml)
          (Xml.child xml_arg0 "JobFlowStates") in
      let jobFlowIds =
        (Option.map ~f:XmlStringList.of_xml)
          (Xml.child xml_arg0 "JobFlowIds") in
      let createdBefore =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "CreatedBefore") in
      let createdAfter =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "CreatedAfter") in
      make ?jobFlowStates ?jobFlowIds ?createdBefore ?createdAfter ()
    let of_json json =
      let jobFlowStates =
        field_map json "JobFlowStates" JobFlowExecutionStateList.of_json in
      let jobFlowIds = field_map json "JobFlowIds" XmlStringList.of_json in
      let createdBefore = field_map json "CreatedBefore" Date.of_json in
      let createdAfter = field_map json "CreatedAfter" Date.of_json in
      make ?jobFlowStates ?jobFlowIds ?createdBefore ?createdAfter ()
    let to_json v = composed_to_json to_value v
  end
module SupportedProductsList =
  struct
    type nonrec t = XmlStringMaxLen256.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:XmlStringMaxLen256.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:XmlStringMaxLen256.of_xml)
    let of_json j =
      list_of_json ~kind:"SupportedProductsList"
        ~of_json:XmlStringMaxLen256.of_json j
    let to_json v = composed_to_json to_value v
  end
module StepExecutionState =
  struct
    type nonrec t =
      | PENDING 
      | RUNNING 
      | CONTINUE 
      | COMPLETED 
      | CANCELLED 
      | FAILED 
      | INTERRUPTED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PENDING -> "PENDING"
      | RUNNING -> "RUNNING"
      | CONTINUE -> "CONTINUE"
      | COMPLETED -> "COMPLETED"
      | CANCELLED -> "CANCELLED"
      | FAILED -> "FAILED"
      | INTERRUPTED -> "INTERRUPTED"
      | Non_static_id s -> s
    let of_string =
      function
      | "PENDING" -> PENDING
      | "RUNNING" -> RUNNING
      | "CONTINUE" -> CONTINUE
      | "COMPLETED" -> COMPLETED
      | "CANCELLED" -> CANCELLED
      | "FAILED" -> FAILED
      | "INTERRUPTED" -> INTERRUPTED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration StepExecutionState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"StepExecutionState" j)
    let to_json = simple_to_json to_value
  end
module StepExecutionStatusDetail =
  struct
    type nonrec t =
      {
      state: StepExecutionState.t ;
      creationDateTime: Date.t ;
      startDateTime: Date.t option ;
      endDateTime: Date.t option ;
      lastStateChangeReason: XmlString.t option }
    let context_ = "StepExecutionStatusDetail"
    let make ?startDateTime =
      fun ?endDateTime ->
        fun ?lastStateChangeReason ->
          fun ~state ->
            fun ~creationDateTime ->
              fun () ->
                {
                  startDateTime;
                  endDateTime;
                  lastStateChangeReason;
                  state;
                  creationDateTime
                }
    let to_value x =
      structure_to_value
        [("State", (Some (StepExecutionState.to_value x.state)));
        ("CreationDateTime", (Some (Date.to_value x.creationDateTime)));
        ("StartDateTime", (Option.map x.startDateTime ~f:Date.to_value));
        ("EndDateTime", (Option.map x.endDateTime ~f:Date.to_value));
        ("LastStateChangeReason",
          (Option.map x.lastStateChangeReason ~f:XmlString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastStateChangeReason =
        (Option.map ~f:XmlString.of_xml)
          (Xml.child xml_arg0 "LastStateChangeReason") in
      let endDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "EndDateTime") in
      let startDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "StartDateTime") in
      let creationDateTime =
        Date.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CreationDateTime") in
      let state =
        StepExecutionState.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "State") in
      make ?lastStateChangeReason ?endDateTime ?startDateTime
        ~creationDateTime ~state ()
    let of_json json =
      let lastStateChangeReason =
        field_map json "LastStateChangeReason" XmlString.of_json in
      let endDateTime = field_map json "EndDateTime" Date.of_json in
      let startDateTime = field_map json "StartDateTime" Date.of_json in
      let creationDateTime =
        field_map_exn json "CreationDateTime" Date.of_json in
      let state = field_map_exn json "State" StepExecutionState.of_json in
      make ?lastStateChangeReason ?endDateTime ?startDateTime
        ~creationDateTime ~state ()
    let to_json v = composed_to_json to_value v
  end
module StepDetail =
  struct
    type nonrec t =
      {
      stepConfig: StepConfig.t ;
      executionStatusDetail: StepExecutionStatusDetail.t }
    let context_ = "StepDetail"
    let make ~stepConfig =
      fun ~executionStatusDetail ->
        fun () -> { stepConfig; executionStatusDetail }
    let to_value x =
      structure_to_value
        [("StepConfig", (Some (StepConfig.to_value x.stepConfig)));
        ("ExecutionStatusDetail",
          (Some (StepExecutionStatusDetail.to_value x.executionStatusDetail)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let executionStatusDetail =
        StepExecutionStatusDetail.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ExecutionStatusDetail") in
      let stepConfig =
        StepConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "StepConfig") in
      make ~executionStatusDetail ~stepConfig ()
    let of_json json =
      let executionStatusDetail =
        field_map_exn json "ExecutionStatusDetail"
          StepExecutionStatusDetail.of_json in
      let stepConfig = field_map_exn json "StepConfig" StepConfig.of_json in
      make ~executionStatusDetail ~stepConfig ()
    let to_json v = composed_to_json to_value v
  end
module StepDetailList =
  struct
    type nonrec t = StepDetail.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:StepDetail.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:StepDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"StepDetailList" ~of_json:StepDetail.of_json j
    let to_json v = composed_to_json to_value v
  end
module PlacementType =
  struct
    type nonrec t =
      {
      availabilityZone: XmlString.t option ;
      availabilityZones: XmlStringMaxLen256List.t option }
    let make ?availabilityZone =
      fun ?availabilityZones ->
        fun () -> { availabilityZone; availabilityZones }
    let to_value x =
      structure_to_value
        [("AvailabilityZone",
           (Option.map x.availabilityZone ~f:XmlString.to_value));
        ("AvailabilityZones",
          (Option.map x.availabilityZones ~f:XmlStringMaxLen256List.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let availabilityZones =
        (Option.map ~f:XmlStringMaxLen256List.of_xml)
          (Xml.child xml_arg0 "AvailabilityZones") in
      let availabilityZone =
        (Option.map ~f:XmlString.of_xml)
          (Xml.child xml_arg0 "AvailabilityZone") in
      make ?availabilityZones ?availabilityZone ()
    let of_json json =
      let availabilityZones =
        field_map json "AvailabilityZones" XmlStringMaxLen256List.of_json in
      let availabilityZone =
        field_map json "AvailabilityZone" XmlString.of_json in
      make ?availabilityZones ?availabilityZone ()
    let to_json v = composed_to_json to_value v
  end
module InstanceGroupState =
  struct
    type nonrec t =
      | PROVISIONING 
      | BOOTSTRAPPING 
      | RUNNING 
      | RECONFIGURING 
      | RESIZING 
      | SUSPENDED 
      | TERMINATING 
      | TERMINATED 
      | ARRESTED 
      | SHUTTING_DOWN 
      | ENDED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PROVISIONING -> "PROVISIONING"
      | BOOTSTRAPPING -> "BOOTSTRAPPING"
      | RUNNING -> "RUNNING"
      | RECONFIGURING -> "RECONFIGURING"
      | RESIZING -> "RESIZING"
      | SUSPENDED -> "SUSPENDED"
      | TERMINATING -> "TERMINATING"
      | TERMINATED -> "TERMINATED"
      | ARRESTED -> "ARRESTED"
      | SHUTTING_DOWN -> "SHUTTING_DOWN"
      | ENDED -> "ENDED"
      | Non_static_id s -> s
    let of_string =
      function
      | "PROVISIONING" -> PROVISIONING
      | "BOOTSTRAPPING" -> BOOTSTRAPPING
      | "RUNNING" -> RUNNING
      | "RECONFIGURING" -> RECONFIGURING
      | "RESIZING" -> RESIZING
      | "SUSPENDED" -> SUSPENDED
      | "TERMINATING" -> TERMINATING
      | "TERMINATED" -> TERMINATED
      | "ARRESTED" -> ARRESTED
      | "SHUTTING_DOWN" -> SHUTTING_DOWN
      | "ENDED" -> ENDED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration InstanceGroupState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"InstanceGroupState" j)
    let to_json = simple_to_json to_value
  end
module InstanceGroupDetail =
  struct
    type nonrec t =
      {
      instanceGroupId: XmlStringMaxLen256.t option ;
      name: XmlStringMaxLen256.t option ;
      market: MarketType.t ;
      instanceRole: InstanceRoleType.t ;
      bidPrice: XmlStringMaxLen256.t option ;
      instanceType: InstanceType.t ;
      instanceRequestCount: Integer.t ;
      instanceRunningCount: Integer.t ;
      state: InstanceGroupState.t ;
      lastStateChangeReason: XmlString.t option ;
      creationDateTime: Date.t ;
      startDateTime: Date.t option ;
      readyDateTime: Date.t option ;
      endDateTime: Date.t option ;
      customAmiId: XmlStringMaxLen256.t option }
    let context_ = "InstanceGroupDetail"
    let make ?instanceGroupId =
      fun ?name ->
        fun ?bidPrice ->
          fun ?lastStateChangeReason ->
            fun ?startDateTime ->
              fun ?readyDateTime ->
                fun ?endDateTime ->
                  fun ?customAmiId ->
                    fun ~market ->
                      fun ~instanceRole ->
                        fun ~instanceType ->
                          fun ~instanceRequestCount ->
                            fun ~instanceRunningCount ->
                              fun ~state ->
                                fun ~creationDateTime ->
                                  fun () ->
                                    {
                                      instanceGroupId;
                                      name;
                                      bidPrice;
                                      lastStateChangeReason;
                                      startDateTime;
                                      readyDateTime;
                                      endDateTime;
                                      customAmiId;
                                      market;
                                      instanceRole;
                                      instanceType;
                                      instanceRequestCount;
                                      instanceRunningCount;
                                      state;
                                      creationDateTime
                                    }
    let to_value x =
      structure_to_value
        [("InstanceGroupId",
           (Option.map x.instanceGroupId ~f:XmlStringMaxLen256.to_value));
        ("Name", (Option.map x.name ~f:XmlStringMaxLen256.to_value));
        ("Market", (Some (MarketType.to_value x.market)));
        ("InstanceRole", (Some (InstanceRoleType.to_value x.instanceRole)));
        ("BidPrice", (Option.map x.bidPrice ~f:XmlStringMaxLen256.to_value));
        ("InstanceType", (Some (InstanceType.to_value x.instanceType)));
        ("InstanceRequestCount",
          (Some (Integer.to_value x.instanceRequestCount)));
        ("InstanceRunningCount",
          (Some (Integer.to_value x.instanceRunningCount)));
        ("State", (Some (InstanceGroupState.to_value x.state)));
        ("LastStateChangeReason",
          (Option.map x.lastStateChangeReason ~f:XmlString.to_value));
        ("CreationDateTime", (Some (Date.to_value x.creationDateTime)));
        ("StartDateTime", (Option.map x.startDateTime ~f:Date.to_value));
        ("ReadyDateTime", (Option.map x.readyDateTime ~f:Date.to_value));
        ("EndDateTime", (Option.map x.endDateTime ~f:Date.to_value));
        ("CustomAmiId",
          (Option.map x.customAmiId ~f:XmlStringMaxLen256.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let customAmiId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "CustomAmiId") in
      let endDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "EndDateTime") in
      let readyDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "ReadyDateTime") in
      let startDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "StartDateTime") in
      let creationDateTime =
        Date.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CreationDateTime") in
      let lastStateChangeReason =
        (Option.map ~f:XmlString.of_xml)
          (Xml.child xml_arg0 "LastStateChangeReason") in
      let state =
        InstanceGroupState.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "State") in
      let instanceRunningCount =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceRunningCount") in
      let instanceRequestCount =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceRequestCount") in
      let instanceType =
        InstanceType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceType") in
      let bidPrice =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "BidPrice") in
      let instanceRole =
        InstanceRoleType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceRole") in
      let market =
        MarketType.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Market") in
      let name =
        (Option.map ~f:XmlStringMaxLen256.of_xml) (Xml.child xml_arg0 "Name") in
      let instanceGroupId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "InstanceGroupId") in
      make ?customAmiId ?endDateTime ?readyDateTime ?startDateTime
        ~creationDateTime ?lastStateChangeReason ~state ~instanceRunningCount
        ~instanceRequestCount ~instanceType ?bidPrice ~instanceRole ~market
        ?name ?instanceGroupId ()
    let of_json json =
      let customAmiId =
        field_map json "CustomAmiId" XmlStringMaxLen256.of_json in
      let endDateTime = field_map json "EndDateTime" Date.of_json in
      let readyDateTime = field_map json "ReadyDateTime" Date.of_json in
      let startDateTime = field_map json "StartDateTime" Date.of_json in
      let creationDateTime =
        field_map_exn json "CreationDateTime" Date.of_json in
      let lastStateChangeReason =
        field_map json "LastStateChangeReason" XmlString.of_json in
      let state = field_map_exn json "State" InstanceGroupState.of_json in
      let instanceRunningCount =
        field_map_exn json "InstanceRunningCount" Integer.of_json in
      let instanceRequestCount =
        field_map_exn json "InstanceRequestCount" Integer.of_json in
      let instanceType =
        field_map_exn json "InstanceType" InstanceType.of_json in
      let bidPrice = field_map json "BidPrice" XmlStringMaxLen256.of_json in
      let instanceRole =
        field_map_exn json "InstanceRole" InstanceRoleType.of_json in
      let market = field_map_exn json "Market" MarketType.of_json in
      let name = field_map json "Name" XmlStringMaxLen256.of_json in
      let instanceGroupId =
        field_map json "InstanceGroupId" XmlStringMaxLen256.of_json in
      make ?customAmiId ?endDateTime ?readyDateTime ?startDateTime
        ~creationDateTime ?lastStateChangeReason ~state ~instanceRunningCount
        ~instanceRequestCount ~instanceType ?bidPrice ~instanceRole ~market
        ?name ?instanceGroupId ()
    let to_json v = composed_to_json to_value v
  end
module InstanceGroupDetailList =
  struct
    type nonrec t = InstanceGroupDetail.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:InstanceGroupDetail.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InstanceGroupDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"InstanceGroupDetailList"
        ~of_json:InstanceGroupDetail.of_json j
    let to_json v = composed_to_json to_value v
  end
module JobFlowInstancesDetail =
  struct
    type nonrec t =
      {
      masterInstanceType: InstanceType.t ;
      masterPublicDnsName: XmlString.t option ;
      masterInstanceId: XmlString.t option ;
      slaveInstanceType: InstanceType.t ;
      instanceCount: Integer.t ;
      instanceGroups: InstanceGroupDetailList.t option ;
      normalizedInstanceHours: Integer.t option ;
      ec2KeyName: XmlStringMaxLen256.t option ;
      ec2SubnetId: XmlStringMaxLen256.t option ;
      placement: PlacementType.t option ;
      keepJobFlowAliveWhenNoSteps: Boolean.t option ;
      terminationProtected: Boolean.t option ;
      hadoopVersion: XmlStringMaxLen256.t option }
    let context_ = "JobFlowInstancesDetail"
    let make ?masterPublicDnsName =
      fun ?masterInstanceId ->
        fun ?instanceGroups ->
          fun ?normalizedInstanceHours ->
            fun ?ec2KeyName ->
              fun ?ec2SubnetId ->
                fun ?placement ->
                  fun ?keepJobFlowAliveWhenNoSteps ->
                    fun ?terminationProtected ->
                      fun ?hadoopVersion ->
                        fun ~masterInstanceType ->
                          fun ~slaveInstanceType ->
                            fun ~instanceCount ->
                              fun () ->
                                {
                                  masterPublicDnsName;
                                  masterInstanceId;
                                  instanceGroups;
                                  normalizedInstanceHours;
                                  ec2KeyName;
                                  ec2SubnetId;
                                  placement;
                                  keepJobFlowAliveWhenNoSteps;
                                  terminationProtected;
                                  hadoopVersion;
                                  masterInstanceType;
                                  slaveInstanceType;
                                  instanceCount
                                }
    let to_value x =
      structure_to_value
        [("MasterInstanceType",
           (Some (InstanceType.to_value x.masterInstanceType)));
        ("MasterPublicDnsName",
          (Option.map x.masterPublicDnsName ~f:XmlString.to_value));
        ("MasterInstanceId",
          (Option.map x.masterInstanceId ~f:XmlString.to_value));
        ("SlaveInstanceType",
          (Some (InstanceType.to_value x.slaveInstanceType)));
        ("InstanceCount", (Some (Integer.to_value x.instanceCount)));
        ("InstanceGroups",
          (Option.map x.instanceGroups ~f:InstanceGroupDetailList.to_value));
        ("NormalizedInstanceHours",
          (Option.map x.normalizedInstanceHours ~f:Integer.to_value));
        ("Ec2KeyName",
          (Option.map x.ec2KeyName ~f:XmlStringMaxLen256.to_value));
        ("Ec2SubnetId",
          (Option.map x.ec2SubnetId ~f:XmlStringMaxLen256.to_value));
        ("Placement", (Option.map x.placement ~f:PlacementType.to_value));
        ("KeepJobFlowAliveWhenNoSteps",
          (Option.map x.keepJobFlowAliveWhenNoSteps ~f:Boolean.to_value));
        ("TerminationProtected",
          (Option.map x.terminationProtected ~f:Boolean.to_value));
        ("HadoopVersion",
          (Option.map x.hadoopVersion ~f:XmlStringMaxLen256.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let hadoopVersion =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "HadoopVersion") in
      let terminationProtected =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "TerminationProtected") in
      let keepJobFlowAliveWhenNoSteps =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "KeepJobFlowAliveWhenNoSteps") in
      let placement =
        (Option.map ~f:PlacementType.of_xml) (Xml.child xml_arg0 "Placement") in
      let ec2SubnetId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "Ec2SubnetId") in
      let ec2KeyName =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "Ec2KeyName") in
      let normalizedInstanceHours =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "NormalizedInstanceHours") in
      let instanceGroups =
        (Option.map ~f:InstanceGroupDetailList.of_xml)
          (Xml.child xml_arg0 "InstanceGroups") in
      let instanceCount =
        Integer.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceCount") in
      let slaveInstanceType =
        InstanceType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SlaveInstanceType") in
      let masterInstanceId =
        (Option.map ~f:XmlString.of_xml)
          (Xml.child xml_arg0 "MasterInstanceId") in
      let masterPublicDnsName =
        (Option.map ~f:XmlString.of_xml)
          (Xml.child xml_arg0 "MasterPublicDnsName") in
      let masterInstanceType =
        InstanceType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MasterInstanceType") in
      make ?hadoopVersion ?terminationProtected ?keepJobFlowAliveWhenNoSteps
        ?placement ?ec2SubnetId ?ec2KeyName ?normalizedInstanceHours
        ?instanceGroups ~instanceCount ~slaveInstanceType ?masterInstanceId
        ?masterPublicDnsName ~masterInstanceType ()
    let of_json json =
      let hadoopVersion =
        field_map json "HadoopVersion" XmlStringMaxLen256.of_json in
      let terminationProtected =
        field_map json "TerminationProtected" Boolean.of_json in
      let keepJobFlowAliveWhenNoSteps =
        field_map json "KeepJobFlowAliveWhenNoSteps" Boolean.of_json in
      let placement = field_map json "Placement" PlacementType.of_json in
      let ec2SubnetId =
        field_map json "Ec2SubnetId" XmlStringMaxLen256.of_json in
      let ec2KeyName = field_map json "Ec2KeyName" XmlStringMaxLen256.of_json in
      let normalizedInstanceHours =
        field_map json "NormalizedInstanceHours" Integer.of_json in
      let instanceGroups =
        field_map json "InstanceGroups" InstanceGroupDetailList.of_json in
      let instanceCount = field_map_exn json "InstanceCount" Integer.of_json in
      let slaveInstanceType =
        field_map_exn json "SlaveInstanceType" InstanceType.of_json in
      let masterInstanceId =
        field_map json "MasterInstanceId" XmlString.of_json in
      let masterPublicDnsName =
        field_map json "MasterPublicDnsName" XmlString.of_json in
      let masterInstanceType =
        field_map_exn json "MasterInstanceType" InstanceType.of_json in
      make ?hadoopVersion ?terminationProtected ?keepJobFlowAliveWhenNoSteps
        ?placement ?ec2SubnetId ?ec2KeyName ?normalizedInstanceHours
        ?instanceGroups ~instanceCount ~slaveInstanceType ?masterInstanceId
        ?masterPublicDnsName ~masterInstanceType ()
    let to_json v = composed_to_json to_value v
  end
module JobFlowExecutionStatusDetail =
  struct
    type nonrec t =
      {
      state: JobFlowExecutionState.t ;
      creationDateTime: Date.t ;
      startDateTime: Date.t option ;
      readyDateTime: Date.t option ;
      endDateTime: Date.t option ;
      lastStateChangeReason: XmlString.t option }
    let context_ = "JobFlowExecutionStatusDetail"
    let make ?startDateTime =
      fun ?readyDateTime ->
        fun ?endDateTime ->
          fun ?lastStateChangeReason ->
            fun ~state ->
              fun ~creationDateTime ->
                fun () ->
                  {
                    startDateTime;
                    readyDateTime;
                    endDateTime;
                    lastStateChangeReason;
                    state;
                    creationDateTime
                  }
    let to_value x =
      structure_to_value
        [("State", (Some (JobFlowExecutionState.to_value x.state)));
        ("CreationDateTime", (Some (Date.to_value x.creationDateTime)));
        ("StartDateTime", (Option.map x.startDateTime ~f:Date.to_value));
        ("ReadyDateTime", (Option.map x.readyDateTime ~f:Date.to_value));
        ("EndDateTime", (Option.map x.endDateTime ~f:Date.to_value));
        ("LastStateChangeReason",
          (Option.map x.lastStateChangeReason ~f:XmlString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastStateChangeReason =
        (Option.map ~f:XmlString.of_xml)
          (Xml.child xml_arg0 "LastStateChangeReason") in
      let endDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "EndDateTime") in
      let readyDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "ReadyDateTime") in
      let startDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "StartDateTime") in
      let creationDateTime =
        Date.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CreationDateTime") in
      let state =
        JobFlowExecutionState.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "State") in
      make ?lastStateChangeReason ?endDateTime ?readyDateTime ?startDateTime
        ~creationDateTime ~state ()
    let of_json json =
      let lastStateChangeReason =
        field_map json "LastStateChangeReason" XmlString.of_json in
      let endDateTime = field_map json "EndDateTime" Date.of_json in
      let readyDateTime = field_map json "ReadyDateTime" Date.of_json in
      let startDateTime = field_map json "StartDateTime" Date.of_json in
      let creationDateTime =
        field_map_exn json "CreationDateTime" Date.of_json in
      let state = field_map_exn json "State" JobFlowExecutionState.of_json in
      make ?lastStateChangeReason ?endDateTime ?readyDateTime ?startDateTime
        ~creationDateTime ~state ()
    let to_json v = composed_to_json to_value v
  end
module JobFlowDetail =
  struct
    type nonrec t =
      {
      jobFlowId: XmlStringMaxLen256.t ;
      name: XmlStringMaxLen256.t ;
      logUri: XmlString.t option ;
      logEncryptionKmsKeyId: XmlString.t option ;
      amiVersion: XmlStringMaxLen256.t option ;
      executionStatusDetail: JobFlowExecutionStatusDetail.t ;
      instances: JobFlowInstancesDetail.t ;
      steps: StepDetailList.t option ;
      bootstrapActions: BootstrapActionDetailList.t option ;
      supportedProducts: SupportedProductsList.t option ;
      visibleToAllUsers: Boolean.t option ;
      jobFlowRole: XmlString.t option ;
      serviceRole: XmlString.t option ;
      autoScalingRole: XmlString.t option ;
      scaleDownBehavior: ScaleDownBehavior.t option }
    let context_ = "JobFlowDetail"
    let make ?logUri =
      fun ?logEncryptionKmsKeyId ->
        fun ?amiVersion ->
          fun ?steps ->
            fun ?bootstrapActions ->
              fun ?supportedProducts ->
                fun ?visibleToAllUsers ->
                  fun ?jobFlowRole ->
                    fun ?serviceRole ->
                      fun ?autoScalingRole ->
                        fun ?scaleDownBehavior ->
                          fun ~jobFlowId ->
                            fun ~name ->
                              fun ~executionStatusDetail ->
                                fun ~instances ->
                                  fun () ->
                                    {
                                      logUri;
                                      logEncryptionKmsKeyId;
                                      amiVersion;
                                      steps;
                                      bootstrapActions;
                                      supportedProducts;
                                      visibleToAllUsers;
                                      jobFlowRole;
                                      serviceRole;
                                      autoScalingRole;
                                      scaleDownBehavior;
                                      jobFlowId;
                                      name;
                                      executionStatusDetail;
                                      instances
                                    }
    let to_value x =
      structure_to_value
        [("JobFlowId", (Some (XmlStringMaxLen256.to_value x.jobFlowId)));
        ("Name", (Some (XmlStringMaxLen256.to_value x.name)));
        ("LogUri", (Option.map x.logUri ~f:XmlString.to_value));
        ("LogEncryptionKmsKeyId",
          (Option.map x.logEncryptionKmsKeyId ~f:XmlString.to_value));
        ("AmiVersion",
          (Option.map x.amiVersion ~f:XmlStringMaxLen256.to_value));
        ("ExecutionStatusDetail",
          (Some
             (JobFlowExecutionStatusDetail.to_value x.executionStatusDetail)));
        ("Instances", (Some (JobFlowInstancesDetail.to_value x.instances)));
        ("Steps", (Option.map x.steps ~f:StepDetailList.to_value));
        ("BootstrapActions",
          (Option.map x.bootstrapActions
             ~f:BootstrapActionDetailList.to_value));
        ("SupportedProducts",
          (Option.map x.supportedProducts ~f:SupportedProductsList.to_value));
        ("VisibleToAllUsers",
          (Option.map x.visibleToAllUsers ~f:Boolean.to_value));
        ("JobFlowRole", (Option.map x.jobFlowRole ~f:XmlString.to_value));
        ("ServiceRole", (Option.map x.serviceRole ~f:XmlString.to_value));
        ("AutoScalingRole",
          (Option.map x.autoScalingRole ~f:XmlString.to_value));
        ("ScaleDownBehavior",
          (Option.map x.scaleDownBehavior ~f:ScaleDownBehavior.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let scaleDownBehavior =
        (Option.map ~f:ScaleDownBehavior.of_xml)
          (Xml.child xml_arg0 "ScaleDownBehavior") in
      let autoScalingRole =
        (Option.map ~f:XmlString.of_xml)
          (Xml.child xml_arg0 "AutoScalingRole") in
      let serviceRole =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "ServiceRole") in
      let jobFlowRole =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "JobFlowRole") in
      let visibleToAllUsers =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "VisibleToAllUsers") in
      let supportedProducts =
        (Option.map ~f:SupportedProductsList.of_xml)
          (Xml.child xml_arg0 "SupportedProducts") in
      let bootstrapActions =
        (Option.map ~f:BootstrapActionDetailList.of_xml)
          (Xml.child xml_arg0 "BootstrapActions") in
      let steps =
        (Option.map ~f:StepDetailList.of_xml) (Xml.child xml_arg0 "Steps") in
      let instances =
        JobFlowInstancesDetail.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Instances") in
      let executionStatusDetail =
        JobFlowExecutionStatusDetail.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ExecutionStatusDetail") in
      let amiVersion =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "AmiVersion") in
      let logEncryptionKmsKeyId =
        (Option.map ~f:XmlString.of_xml)
          (Xml.child xml_arg0 "LogEncryptionKmsKeyId") in
      let logUri =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "LogUri") in
      let name =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      let jobFlowId =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "JobFlowId") in
      make ?scaleDownBehavior ?autoScalingRole ?serviceRole ?jobFlowRole
        ?visibleToAllUsers ?supportedProducts ?bootstrapActions ?steps
        ~instances ~executionStatusDetail ?amiVersion ?logEncryptionKmsKeyId
        ?logUri ~name ~jobFlowId ()
    let of_json json =
      let scaleDownBehavior =
        field_map json "ScaleDownBehavior" ScaleDownBehavior.of_json in
      let autoScalingRole =
        field_map json "AutoScalingRole" XmlString.of_json in
      let serviceRole = field_map json "ServiceRole" XmlString.of_json in
      let jobFlowRole = field_map json "JobFlowRole" XmlString.of_json in
      let visibleToAllUsers =
        field_map json "VisibleToAllUsers" Boolean.of_json in
      let supportedProducts =
        field_map json "SupportedProducts" SupportedProductsList.of_json in
      let bootstrapActions =
        field_map json "BootstrapActions" BootstrapActionDetailList.of_json in
      let steps = field_map json "Steps" StepDetailList.of_json in
      let instances =
        field_map_exn json "Instances" JobFlowInstancesDetail.of_json in
      let executionStatusDetail =
        field_map_exn json "ExecutionStatusDetail"
          JobFlowExecutionStatusDetail.of_json in
      let amiVersion = field_map json "AmiVersion" XmlStringMaxLen256.of_json in
      let logEncryptionKmsKeyId =
        field_map json "LogEncryptionKmsKeyId" XmlString.of_json in
      let logUri = field_map json "LogUri" XmlString.of_json in
      let name = field_map_exn json "Name" XmlStringMaxLen256.of_json in
      let jobFlowId =
        field_map_exn json "JobFlowId" XmlStringMaxLen256.of_json in
      make ?scaleDownBehavior ?autoScalingRole ?serviceRole ?jobFlowRole
        ?visibleToAllUsers ?supportedProducts ?bootstrapActions ?steps
        ~instances ~executionStatusDetail ?amiVersion ?logEncryptionKmsKeyId
        ?logUri ~name ~jobFlowId ()
    let to_json v = composed_to_json to_value v
  end
module JobFlowDetailList =
  struct
    type nonrec t = JobFlowDetail.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:JobFlowDetail.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:JobFlowDetail.of_xml)
    let of_json j =
      list_of_json ~kind:"JobFlowDetailList" ~of_json:JobFlowDetail.of_json j
    let to_json v = composed_to_json to_value v
  end
module DescribeJobFlowsOutput =
  struct
    type nonrec t = {
      jobFlows: JobFlowDetailList.t option }
    type nonrec error =
      [ `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?jobFlows = fun () -> { jobFlows }
    let error_of_json name json =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("JobFlows", (Option.map x.jobFlows ~f:JobFlowDetailList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let jobFlows =
        (Option.map ~f:JobFlowDetailList.of_xml)
          (Xml.child xml_arg0 "JobFlows") in
      make ?jobFlows ()
    let of_json json =
      let jobFlows = field_map json "JobFlows" JobFlowDetailList.of_json in
      make ?jobFlows ()
    let to_json v = composed_to_json to_value v
  end
module DescribeNotebookExecutionInput =
  struct
    type nonrec t = {
      notebookExecutionId: XmlStringMaxLen256.t }
    let context_ = "DescribeNotebookExecutionInput"
    let make ~notebookExecutionId = fun () -> { notebookExecutionId }
    let to_value x =
      structure_to_value
        [("NotebookExecutionId",
           (Some (XmlStringMaxLen256.to_value x.notebookExecutionId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let notebookExecutionId =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "NotebookExecutionId") in
      make ~notebookExecutionId ()
    let of_json json =
      let notebookExecutionId =
        field_map_exn json "NotebookExecutionId" XmlStringMaxLen256.of_json in
      make ~notebookExecutionId ()
    let to_json v = composed_to_json to_value v
  end
module NotebookExecutionStatus =
  struct
    type nonrec t =
      | START_PENDING 
      | STARTING 
      | RUNNING 
      | FINISHING 
      | FINISHED 
      | FAILING 
      | FAILED 
      | STOP_PENDING 
      | STOPPING 
      | STOPPED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | START_PENDING -> "START_PENDING"
      | STARTING -> "STARTING"
      | RUNNING -> "RUNNING"
      | FINISHING -> "FINISHING"
      | FINISHED -> "FINISHED"
      | FAILING -> "FAILING"
      | FAILED -> "FAILED"
      | STOP_PENDING -> "STOP_PENDING"
      | STOPPING -> "STOPPING"
      | STOPPED -> "STOPPED"
      | Non_static_id s -> s
    let of_string =
      function
      | "START_PENDING" -> START_PENDING
      | "STARTING" -> STARTING
      | "RUNNING" -> RUNNING
      | "FINISHING" -> FINISHING
      | "FINISHED" -> FINISHED
      | "FAILING" -> FAILING
      | "FAILED" -> FAILED
      | "STOP_PENDING" -> STOP_PENDING
      | "STOPPING" -> STOPPING
      | "STOPPED" -> STOPPED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration NotebookExecutionStatus" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"NotebookExecutionStatus" j)
    let to_json = simple_to_json to_value
  end
module ExecutionEngineType =
  struct
    type nonrec t =
      | EMR 
      | Non_static_id of string 
    let make i = i
    let to_string = function | EMR -> "EMR" | Non_static_id s -> s
    let of_string = function | "EMR" -> EMR | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ExecutionEngineType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ExecutionEngineType" j)
    let to_json = simple_to_json to_value
  end
module ExecutionEngineConfig =
  struct
    type nonrec t =
      {
      id: XmlStringMaxLen256.t ;
      type_: ExecutionEngineType.t option ;
      masterInstanceSecurityGroupId: XmlStringMaxLen256.t option }
    let context_ = "ExecutionEngineConfig"
    let make ?type_ =
      fun ?masterInstanceSecurityGroupId ->
        fun ~id -> fun () -> { type_; masterInstanceSecurityGroupId; id }
    let to_value x =
      structure_to_value
        [("Id", (Some (XmlStringMaxLen256.to_value x.id)));
        ("Type", (Option.map x.type_ ~f:ExecutionEngineType.to_value));
        ("MasterInstanceSecurityGroupId",
          (Option.map x.masterInstanceSecurityGroupId
             ~f:XmlStringMaxLen256.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let masterInstanceSecurityGroupId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "MasterInstanceSecurityGroupId") in
      let type_ =
        (Option.map ~f:ExecutionEngineType.of_xml)
          (Xml.child xml_arg0 "Type") in
      let id =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ?masterInstanceSecurityGroupId ?type_ ~id ()
    let of_json json =
      let masterInstanceSecurityGroupId =
        field_map json "MasterInstanceSecurityGroupId"
          XmlStringMaxLen256.of_json in
      let type_ = field_map json "Type" ExecutionEngineType.of_json in
      let id = field_map_exn json "Id" XmlStringMaxLen256.of_json in
      make ?masterInstanceSecurityGroupId ?type_ ~id ()
    let to_json v = composed_to_json to_value v
  end
module NotebookExecution =
  struct
    type nonrec t =
      {
      notebookExecutionId: XmlStringMaxLen256.t option ;
      editorId: XmlStringMaxLen256.t option ;
      executionEngine: ExecutionEngineConfig.t option ;
      notebookExecutionName: XmlStringMaxLen256.t option ;
      notebookParams: XmlString.t option ;
      status: NotebookExecutionStatus.t option ;
      startTime: Date.t option ;
      endTime: Date.t option ;
      arn: XmlStringMaxLen256.t option ;
      outputNotebookURI: XmlString.t option ;
      lastStateChangeReason: XmlString.t option ;
      notebookInstanceSecurityGroupId: XmlStringMaxLen256.t option ;
      tags: TagList.t option }
    let make ?notebookExecutionId =
      fun ?editorId ->
        fun ?executionEngine ->
          fun ?notebookExecutionName ->
            fun ?notebookParams ->
              fun ?status ->
                fun ?startTime ->
                  fun ?endTime ->
                    fun ?arn ->
                      fun ?outputNotebookURI ->
                        fun ?lastStateChangeReason ->
                          fun ?notebookInstanceSecurityGroupId ->
                            fun ?tags ->
                              fun () ->
                                {
                                  notebookExecutionId;
                                  editorId;
                                  executionEngine;
                                  notebookExecutionName;
                                  notebookParams;
                                  status;
                                  startTime;
                                  endTime;
                                  arn;
                                  outputNotebookURI;
                                  lastStateChangeReason;
                                  notebookInstanceSecurityGroupId;
                                  tags
                                }
    let to_value x =
      structure_to_value
        [("NotebookExecutionId",
           (Option.map x.notebookExecutionId ~f:XmlStringMaxLen256.to_value));
        ("EditorId", (Option.map x.editorId ~f:XmlStringMaxLen256.to_value));
        ("ExecutionEngine",
          (Option.map x.executionEngine ~f:ExecutionEngineConfig.to_value));
        ("NotebookExecutionName",
          (Option.map x.notebookExecutionName ~f:XmlStringMaxLen256.to_value));
        ("NotebookParams",
          (Option.map x.notebookParams ~f:XmlString.to_value));
        ("Status", (Option.map x.status ~f:NotebookExecutionStatus.to_value));
        ("StartTime", (Option.map x.startTime ~f:Date.to_value));
        ("EndTime", (Option.map x.endTime ~f:Date.to_value));
        ("Arn", (Option.map x.arn ~f:XmlStringMaxLen256.to_value));
        ("OutputNotebookURI",
          (Option.map x.outputNotebookURI ~f:XmlString.to_value));
        ("LastStateChangeReason",
          (Option.map x.lastStateChangeReason ~f:XmlString.to_value));
        ("NotebookInstanceSecurityGroupId",
          (Option.map x.notebookInstanceSecurityGroupId
             ~f:XmlStringMaxLen256.to_value));
        ("Tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let notebookInstanceSecurityGroupId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "NotebookInstanceSecurityGroupId") in
      let lastStateChangeReason =
        (Option.map ~f:XmlString.of_xml)
          (Xml.child xml_arg0 "LastStateChangeReason") in
      let outputNotebookURI =
        (Option.map ~f:XmlString.of_xml)
          (Xml.child xml_arg0 "OutputNotebookURI") in
      let arn =
        (Option.map ~f:XmlStringMaxLen256.of_xml) (Xml.child xml_arg0 "Arn") in
      let endTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "EndTime") in
      let startTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "StartTime") in
      let status =
        (Option.map ~f:NotebookExecutionStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let notebookParams =
        (Option.map ~f:XmlString.of_xml)
          (Xml.child xml_arg0 "NotebookParams") in
      let notebookExecutionName =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "NotebookExecutionName") in
      let executionEngine =
        (Option.map ~f:ExecutionEngineConfig.of_xml)
          (Xml.child xml_arg0 "ExecutionEngine") in
      let editorId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "EditorId") in
      let notebookExecutionId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "NotebookExecutionId") in
      make ?tags ?notebookInstanceSecurityGroupId ?lastStateChangeReason
        ?outputNotebookURI ?arn ?endTime ?startTime ?status ?notebookParams
        ?notebookExecutionName ?executionEngine ?editorId
        ?notebookExecutionId ()
    let of_json json =
      let tags = field_map json "Tags" TagList.of_json in
      let notebookInstanceSecurityGroupId =
        field_map json "NotebookInstanceSecurityGroupId"
          XmlStringMaxLen256.of_json in
      let lastStateChangeReason =
        field_map json "LastStateChangeReason" XmlString.of_json in
      let outputNotebookURI =
        field_map json "OutputNotebookURI" XmlString.of_json in
      let arn = field_map json "Arn" XmlStringMaxLen256.of_json in
      let endTime = field_map json "EndTime" Date.of_json in
      let startTime = field_map json "StartTime" Date.of_json in
      let status = field_map json "Status" NotebookExecutionStatus.of_json in
      let notebookParams = field_map json "NotebookParams" XmlString.of_json in
      let notebookExecutionName =
        field_map json "NotebookExecutionName" XmlStringMaxLen256.of_json in
      let executionEngine =
        field_map json "ExecutionEngine" ExecutionEngineConfig.of_json in
      let editorId = field_map json "EditorId" XmlStringMaxLen256.of_json in
      let notebookExecutionId =
        field_map json "NotebookExecutionId" XmlStringMaxLen256.of_json in
      make ?tags ?notebookInstanceSecurityGroupId ?lastStateChangeReason
        ?outputNotebookURI ?arn ?endTime ?startTime ?status ?notebookParams
        ?notebookExecutionName ?executionEngine ?editorId
        ?notebookExecutionId ()
    let to_json v = composed_to_json to_value v
  end
module DescribeNotebookExecutionOutput =
  struct
    type nonrec t = {
      notebookExecution: NotebookExecution.t option }
    type nonrec error =
      [ `InternalServerError of InternalServerError.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?notebookExecution = fun () -> { notebookExecution }
    let error_of_json name json =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("NotebookExecution",
           (Option.map x.notebookExecution ~f:NotebookExecution.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let notebookExecution =
        (Option.map ~f:NotebookExecution.of_xml)
          (Xml.child xml_arg0 "NotebookExecution") in
      make ?notebookExecution ()
    let of_json json =
      let notebookExecution =
        field_map json "NotebookExecution" NotebookExecution.of_json in
      make ?notebookExecution ()
    let to_json v = composed_to_json to_value v
  end
module MaxResultsNumber =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResultsNumber" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module DescribeReleaseLabelInput =
  struct
    type nonrec t =
      {
      releaseLabel: String_.t option ;
      nextToken: String_.t option ;
      maxResults: MaxResultsNumber.t option }
    let make ?releaseLabel =
      fun ?nextToken ->
        fun ?maxResults -> fun () -> { releaseLabel; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("ReleaseLabel", (Option.map x.releaseLabel ~f:String_.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:MaxResultsNumber.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResultsNumber.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let releaseLabel =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ReleaseLabel") in
      make ?maxResults ?nextToken ?releaseLabel ()
    let of_json json =
      let maxResults = field_map json "MaxResults" MaxResultsNumber.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let releaseLabel = field_map json "ReleaseLabel" String_.of_json in
      make ?maxResults ?nextToken ?releaseLabel ()
    let to_json v = composed_to_json to_value v
  end
module SimplifiedApplication =
  struct
    type nonrec t = {
      name: String_.t option ;
      version: String_.t option }
    let make ?name = fun ?version -> fun () -> { name; version }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:String_.to_value));
        ("Version", (Option.map x.version ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let version =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Version") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      make ?version ?name ()
    let of_json json =
      let version = field_map json "Version" String_.of_json in
      let name = field_map json "Name" String_.of_json in
      make ?version ?name ()
    let to_json v = composed_to_json to_value v
  end
module SimplifiedApplicationList =
  struct
    type nonrec t = SimplifiedApplication.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SimplifiedApplication.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SimplifiedApplication.of_xml)
    let of_json j =
      list_of_json ~kind:"SimplifiedApplicationList"
        ~of_json:SimplifiedApplication.of_json j
    let to_json v = composed_to_json to_value v
  end
module DescribeReleaseLabelOutput =
  struct
    type nonrec t =
      {
      releaseLabel: String_.t option ;
      applications: SimplifiedApplicationList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?releaseLabel =
      fun ?applications ->
        fun ?nextToken -> fun () -> { releaseLabel; applications; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ReleaseLabel", (Option.map x.releaseLabel ~f:String_.to_value));
        ("Applications",
          (Option.map x.applications ~f:SimplifiedApplicationList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let applications =
        (Option.map ~f:SimplifiedApplicationList.of_xml)
          (Xml.child xml_arg0 "Applications") in
      let releaseLabel =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ReleaseLabel") in
      make ?nextToken ?applications ?releaseLabel ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let applications =
        field_map json "Applications" SimplifiedApplicationList.of_json in
      let releaseLabel = field_map json "ReleaseLabel" String_.of_json in
      make ?nextToken ?applications ?releaseLabel ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSecurityConfigurationInput =
  struct
    type nonrec t = {
      name: XmlString.t }
    let context_ = "DescribeSecurityConfigurationInput"
    let make ~name = fun () -> { name }
    let to_value x =
      structure_to_value [("Name", (Some (XmlString.to_value x.name)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        XmlString.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ~name ()
    let of_json json =
      let name = field_map_exn json "Name" XmlString.of_json in make ~name ()
    let to_json v = composed_to_json to_value v
  end
module DescribeSecurityConfigurationOutput =
  struct
    type nonrec t =
      {
      name: XmlString.t option ;
      securityConfiguration: String_.t option ;
      creationDateTime: Date.t option }
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?name =
      fun ?securityConfiguration ->
        fun ?creationDateTime ->
          fun () -> { name; securityConfiguration; creationDateTime }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:XmlString.to_value));
        ("SecurityConfiguration",
          (Option.map x.securityConfiguration ~f:String_.to_value));
        ("CreationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "CreationDateTime") in
      let securityConfiguration =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "SecurityConfiguration") in
      let name = (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "Name") in
      make ?creationDateTime ?securityConfiguration ?name ()
    let of_json json =
      let creationDateTime = field_map json "CreationDateTime" Date.of_json in
      let securityConfiguration =
        field_map json "SecurityConfiguration" String_.of_json in
      let name = field_map json "Name" XmlString.of_json in
      make ?creationDateTime ?securityConfiguration ?name ()
    let to_json v = composed_to_json to_value v
  end
module DescribeStepInput =
  struct
    type nonrec t = {
      clusterId: ClusterId.t ;
      stepId: StepId.t }
    let context_ = "DescribeStepInput"
    let make ~clusterId = fun ~stepId -> fun () -> { clusterId; stepId }
    let to_value x =
      structure_to_value
        [("ClusterId", (Some (ClusterId.to_value x.clusterId)));
        ("StepId", (Some (StepId.to_value x.stepId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stepId =
        StepId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "StepId") in
      let clusterId =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClusterId") in
      make ~stepId ~clusterId ()
    let of_json json =
      let stepId = field_map_exn json "StepId" StepId.of_json in
      let clusterId = field_map_exn json "ClusterId" ClusterId.of_json in
      make ~stepId ~clusterId ()
    let to_json v = composed_to_json to_value v
  end
module StepTimeline =
  struct
    type nonrec t =
      {
      creationDateTime: Date.t option ;
      startDateTime: Date.t option ;
      endDateTime: Date.t option }
    let make ?creationDateTime =
      fun ?startDateTime ->
        fun ?endDateTime ->
          fun () -> { creationDateTime; startDateTime; endDateTime }
    let to_value x =
      structure_to_value
        [("CreationDateTime",
           (Option.map x.creationDateTime ~f:Date.to_value));
        ("StartDateTime", (Option.map x.startDateTime ~f:Date.to_value));
        ("EndDateTime", (Option.map x.endDateTime ~f:Date.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let endDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "EndDateTime") in
      let startDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "StartDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "CreationDateTime") in
      make ?endDateTime ?startDateTime ?creationDateTime ()
    let of_json json =
      let endDateTime = field_map json "EndDateTime" Date.of_json in
      let startDateTime = field_map json "StartDateTime" Date.of_json in
      let creationDateTime = field_map json "CreationDateTime" Date.of_json in
      make ?endDateTime ?startDateTime ?creationDateTime ()
    let to_json v = composed_to_json to_value v
  end
module StepStateChangeReasonCode =
  struct
    type nonrec t =
      | NONE 
      | Non_static_id of string 
    let make i = i
    let to_string = function | NONE -> "NONE" | Non_static_id s -> s
    let of_string = function | "NONE" -> NONE | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration StepStateChangeReasonCode" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"StepStateChangeReasonCode" j)
    let to_json = simple_to_json to_value
  end
module StepStateChangeReason =
  struct
    type nonrec t =
      {
      code: StepStateChangeReasonCode.t option ;
      message: String_.t option }
    let make ?code = fun ?message -> fun () -> { code; message }
    let to_value x =
      structure_to_value
        [("Code", (Option.map x.code ~f:StepStateChangeReasonCode.to_value));
        ("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      let code =
        (Option.map ~f:StepStateChangeReasonCode.of_xml)
          (Xml.child xml_arg0 "Code") in
      make ?message ?code ()
    let of_json json =
      let message = field_map json "Message" String_.of_json in
      let code = field_map json "Code" StepStateChangeReasonCode.of_json in
      make ?message ?code ()
    let to_json v = composed_to_json to_value v
  end
module StepState =
  struct
    type nonrec t =
      | PENDING 
      | CANCEL_PENDING 
      | RUNNING 
      | COMPLETED 
      | CANCELLED 
      | FAILED 
      | INTERRUPTED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PENDING -> "PENDING"
      | CANCEL_PENDING -> "CANCEL_PENDING"
      | RUNNING -> "RUNNING"
      | COMPLETED -> "COMPLETED"
      | CANCELLED -> "CANCELLED"
      | FAILED -> "FAILED"
      | INTERRUPTED -> "INTERRUPTED"
      | Non_static_id s -> s
    let of_string =
      function
      | "PENDING" -> PENDING
      | "CANCEL_PENDING" -> CANCEL_PENDING
      | "RUNNING" -> RUNNING
      | "COMPLETED" -> COMPLETED
      | "CANCELLED" -> CANCELLED
      | "FAILED" -> FAILED
      | "INTERRUPTED" -> INTERRUPTED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration StepState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"StepState" j)
    let to_json = simple_to_json to_value
  end
module FailureDetails =
  struct
    type nonrec t =
      {
      reason: String_.t option ;
      message: String_.t option ;
      logFile: String_.t option }
    let make ?reason =
      fun ?message -> fun ?logFile -> fun () -> { reason; message; logFile }
    let to_value x =
      structure_to_value
        [("Reason", (Option.map x.reason ~f:String_.to_value));
        ("Message", (Option.map x.message ~f:String_.to_value));
        ("LogFile", (Option.map x.logFile ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let logFile =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "LogFile") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      let reason =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Reason") in
      make ?logFile ?message ?reason ()
    let of_json json =
      let logFile = field_map json "LogFile" String_.of_json in
      let message = field_map json "Message" String_.of_json in
      let reason = field_map json "Reason" String_.of_json in
      make ?logFile ?message ?reason ()
    let to_json v = composed_to_json to_value v
  end
module StepStatus =
  struct
    type nonrec t =
      {
      state: StepState.t option ;
      stateChangeReason: StepStateChangeReason.t option ;
      failureDetails: FailureDetails.t option ;
      timeline: StepTimeline.t option }
    let make ?state =
      fun ?stateChangeReason ->
        fun ?failureDetails ->
          fun ?timeline ->
            fun () -> { state; stateChangeReason; failureDetails; timeline }
    let to_value x =
      structure_to_value
        [("State", (Option.map x.state ~f:StepState.to_value));
        ("StateChangeReason",
          (Option.map x.stateChangeReason ~f:StepStateChangeReason.to_value));
        ("FailureDetails",
          (Option.map x.failureDetails ~f:FailureDetails.to_value));
        ("Timeline", (Option.map x.timeline ~f:StepTimeline.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timeline =
        (Option.map ~f:StepTimeline.of_xml) (Xml.child xml_arg0 "Timeline") in
      let failureDetails =
        (Option.map ~f:FailureDetails.of_xml)
          (Xml.child xml_arg0 "FailureDetails") in
      let stateChangeReason =
        (Option.map ~f:StepStateChangeReason.of_xml)
          (Xml.child xml_arg0 "StateChangeReason") in
      let state =
        (Option.map ~f:StepState.of_xml) (Xml.child xml_arg0 "State") in
      make ?timeline ?failureDetails ?stateChangeReason ?state ()
    let of_json json =
      let timeline = field_map json "Timeline" StepTimeline.of_json in
      let failureDetails =
        field_map json "FailureDetails" FailureDetails.of_json in
      let stateChangeReason =
        field_map json "StateChangeReason" StepStateChangeReason.of_json in
      let state = field_map json "State" StepState.of_json in
      make ?timeline ?failureDetails ?stateChangeReason ?state ()
    let to_json v = composed_to_json to_value v
  end
module HadoopStepConfig =
  struct
    type nonrec t =
      {
      jar: String_.t option ;
      properties: StringMap.t option ;
      mainClass: String_.t option ;
      args: StringList.t option }
    let make ?jar =
      fun ?properties ->
        fun ?mainClass ->
          fun ?args -> fun () -> { jar; properties; mainClass; args }
    let to_value x =
      structure_to_value
        [("Jar", (Option.map x.jar ~f:String_.to_value));
        ("Properties", (Option.map x.properties ~f:StringMap.to_value));
        ("MainClass", (Option.map x.mainClass ~f:String_.to_value));
        ("Args", (Option.map x.args ~f:StringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let args =
        (Option.map ~f:StringList.of_xml) (Xml.child xml_arg0 "Args") in
      let mainClass =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "MainClass") in
      let properties =
        (Option.map ~f:StringMap.of_xml) (Xml.child xml_arg0 "Properties") in
      let jar = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Jar") in
      make ?args ?mainClass ?properties ?jar ()
    let of_json json =
      let args = field_map json "Args" StringList.of_json in
      let mainClass = field_map json "MainClass" String_.of_json in
      let properties = field_map json "Properties" StringMap.of_json in
      let jar = field_map json "Jar" String_.of_json in
      make ?args ?mainClass ?properties ?jar ()
    let to_json v = composed_to_json to_value v
  end
module Step =
  struct
    type nonrec t =
      {
      id: StepId.t option ;
      name: String_.t option ;
      config: HadoopStepConfig.t option ;
      actionOnFailure: ActionOnFailure.t option ;
      status: StepStatus.t option }
    let make ?id =
      fun ?name ->
        fun ?config ->
          fun ?actionOnFailure ->
            fun ?status ->
              fun () -> { id; name; config; actionOnFailure; status }
    let to_value x =
      structure_to_value
        [("Id", (Option.map x.id ~f:StepId.to_value));
        ("Name", (Option.map x.name ~f:String_.to_value));
        ("Config", (Option.map x.config ~f:HadoopStepConfig.to_value));
        ("ActionOnFailure",
          (Option.map x.actionOnFailure ~f:ActionOnFailure.to_value));
        ("Status", (Option.map x.status ~f:StepStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:StepStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let actionOnFailure =
        (Option.map ~f:ActionOnFailure.of_xml)
          (Xml.child xml_arg0 "ActionOnFailure") in
      let config =
        (Option.map ~f:HadoopStepConfig.of_xml) (Xml.child xml_arg0 "Config") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      let id = (Option.map ~f:StepId.of_xml) (Xml.child xml_arg0 "Id") in
      make ?status ?actionOnFailure ?config ?name ?id ()
    let of_json json =
      let status = field_map json "Status" StepStatus.of_json in
      let actionOnFailure =
        field_map json "ActionOnFailure" ActionOnFailure.of_json in
      let config = field_map json "Config" HadoopStepConfig.of_json in
      let name = field_map json "Name" String_.of_json in
      let id = field_map json "Id" StepId.of_json in
      make ?status ?actionOnFailure ?config ?name ?id ()
    let to_json v = composed_to_json to_value v
  end
module DescribeStepOutput =
  struct
    type nonrec t = {
      step: Step.t option }
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?step = fun () -> { step }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("Step", (Option.map x.step ~f:Step.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let step = (Option.map ~f:Step.of_xml) (Xml.child xml_arg0 "Step") in
      make ?step ()
    let of_json json =
      let step = field_map json "Step" Step.of_json in make ?step ()
    let to_json v = composed_to_json to_value v
  end
module DescribeStudioInput =
  struct
    type nonrec t = {
      studioId: XmlStringMaxLen256.t }
    let context_ = "DescribeStudioInput"
    let make ~studioId = fun () -> { studioId }
    let to_value x =
      structure_to_value
        [("StudioId", (Some (XmlStringMaxLen256.to_value x.studioId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let studioId =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "StudioId") in
      make ~studioId ()
    let of_json json =
      let studioId = field_map_exn json "StudioId" XmlStringMaxLen256.of_json in
      make ~studioId ()
    let to_json v = composed_to_json to_value v
  end
module Studio =
  struct
    type nonrec t =
      {
      studioId: XmlStringMaxLen256.t option ;
      studioArn: XmlStringMaxLen256.t option ;
      name: XmlStringMaxLen256.t option ;
      description: XmlStringMaxLen256.t option ;
      authMode: AuthMode.t option ;
      vpcId: XmlStringMaxLen256.t option ;
      subnetIds: SubnetIdList.t option ;
      serviceRole: XmlString.t option ;
      userRole: XmlString.t option ;
      workspaceSecurityGroupId: XmlStringMaxLen256.t option ;
      engineSecurityGroupId: XmlStringMaxLen256.t option ;
      url: XmlString.t option ;
      creationTime: Date.t option ;
      defaultS3Location: XmlString.t option ;
      idpAuthUrl: XmlString.t option ;
      idpRelayStateParameterName: XmlStringMaxLen256.t option ;
      tags: TagList.t option }
    let make ?studioId =
      fun ?studioArn ->
        fun ?name ->
          fun ?description ->
            fun ?authMode ->
              fun ?vpcId ->
                fun ?subnetIds ->
                  fun ?serviceRole ->
                    fun ?userRole ->
                      fun ?workspaceSecurityGroupId ->
                        fun ?engineSecurityGroupId ->
                          fun ?url ->
                            fun ?creationTime ->
                              fun ?defaultS3Location ->
                                fun ?idpAuthUrl ->
                                  fun ?idpRelayStateParameterName ->
                                    fun ?tags ->
                                      fun () ->
                                        {
                                          studioId;
                                          studioArn;
                                          name;
                                          description;
                                          authMode;
                                          vpcId;
                                          subnetIds;
                                          serviceRole;
                                          userRole;
                                          workspaceSecurityGroupId;
                                          engineSecurityGroupId;
                                          url;
                                          creationTime;
                                          defaultS3Location;
                                          idpAuthUrl;
                                          idpRelayStateParameterName;
                                          tags
                                        }
    let to_value x =
      structure_to_value
        [("StudioId", (Option.map x.studioId ~f:XmlStringMaxLen256.to_value));
        ("StudioArn",
          (Option.map x.studioArn ~f:XmlStringMaxLen256.to_value));
        ("Name", (Option.map x.name ~f:XmlStringMaxLen256.to_value));
        ("Description",
          (Option.map x.description ~f:XmlStringMaxLen256.to_value));
        ("AuthMode", (Option.map x.authMode ~f:AuthMode.to_value));
        ("VpcId", (Option.map x.vpcId ~f:XmlStringMaxLen256.to_value));
        ("SubnetIds", (Option.map x.subnetIds ~f:SubnetIdList.to_value));
        ("ServiceRole", (Option.map x.serviceRole ~f:XmlString.to_value));
        ("UserRole", (Option.map x.userRole ~f:XmlString.to_value));
        ("WorkspaceSecurityGroupId",
          (Option.map x.workspaceSecurityGroupId
             ~f:XmlStringMaxLen256.to_value));
        ("EngineSecurityGroupId",
          (Option.map x.engineSecurityGroupId ~f:XmlStringMaxLen256.to_value));
        ("Url", (Option.map x.url ~f:XmlString.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:Date.to_value));
        ("DefaultS3Location",
          (Option.map x.defaultS3Location ~f:XmlString.to_value));
        ("IdpAuthUrl", (Option.map x.idpAuthUrl ~f:XmlString.to_value));
        ("IdpRelayStateParameterName",
          (Option.map x.idpRelayStateParameterName
             ~f:XmlStringMaxLen256.to_value));
        ("Tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let idpRelayStateParameterName =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "IdpRelayStateParameterName") in
      let idpAuthUrl =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "IdpAuthUrl") in
      let defaultS3Location =
        (Option.map ~f:XmlString.of_xml)
          (Xml.child xml_arg0 "DefaultS3Location") in
      let creationTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let url = (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "Url") in
      let engineSecurityGroupId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "EngineSecurityGroupId") in
      let workspaceSecurityGroupId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "WorkspaceSecurityGroupId") in
      let userRole =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "UserRole") in
      let serviceRole =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "ServiceRole") in
      let subnetIds =
        (Option.map ~f:SubnetIdList.of_xml) (Xml.child xml_arg0 "SubnetIds") in
      let vpcId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "VpcId") in
      let authMode =
        (Option.map ~f:AuthMode.of_xml) (Xml.child xml_arg0 "AuthMode") in
      let description =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "Description") in
      let name =
        (Option.map ~f:XmlStringMaxLen256.of_xml) (Xml.child xml_arg0 "Name") in
      let studioArn =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "StudioArn") in
      let studioId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "StudioId") in
      make ?tags ?idpRelayStateParameterName ?idpAuthUrl ?defaultS3Location
        ?creationTime ?url ?engineSecurityGroupId ?workspaceSecurityGroupId
        ?userRole ?serviceRole ?subnetIds ?vpcId ?authMode ?description ?name
        ?studioArn ?studioId ()
    let of_json json =
      let tags = field_map json "Tags" TagList.of_json in
      let idpRelayStateParameterName =
        field_map json "IdpRelayStateParameterName"
          XmlStringMaxLen256.of_json in
      let idpAuthUrl = field_map json "IdpAuthUrl" XmlString.of_json in
      let defaultS3Location =
        field_map json "DefaultS3Location" XmlString.of_json in
      let creationTime = field_map json "CreationTime" Date.of_json in
      let url = field_map json "Url" XmlString.of_json in
      let engineSecurityGroupId =
        field_map json "EngineSecurityGroupId" XmlStringMaxLen256.of_json in
      let workspaceSecurityGroupId =
        field_map json "WorkspaceSecurityGroupId" XmlStringMaxLen256.of_json in
      let userRole = field_map json "UserRole" XmlString.of_json in
      let serviceRole = field_map json "ServiceRole" XmlString.of_json in
      let subnetIds = field_map json "SubnetIds" SubnetIdList.of_json in
      let vpcId = field_map json "VpcId" XmlStringMaxLen256.of_json in
      let authMode = field_map json "AuthMode" AuthMode.of_json in
      let description =
        field_map json "Description" XmlStringMaxLen256.of_json in
      let name = field_map json "Name" XmlStringMaxLen256.of_json in
      let studioArn = field_map json "StudioArn" XmlStringMaxLen256.of_json in
      let studioId = field_map json "StudioId" XmlStringMaxLen256.of_json in
      make ?tags ?idpRelayStateParameterName ?idpAuthUrl ?defaultS3Location
        ?creationTime ?url ?engineSecurityGroupId ?workspaceSecurityGroupId
        ?userRole ?serviceRole ?subnetIds ?vpcId ?authMode ?description ?name
        ?studioArn ?studioId ()
    let to_json v = composed_to_json to_value v
  end
module DescribeStudioOutput =
  struct
    type nonrec t = {
      studio: Studio.t option }
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?studio = fun () -> { studio }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Studio", (Option.map x.studio ~f:Studio.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let studio =
        (Option.map ~f:Studio.of_xml) (Xml.child xml_arg0 "Studio") in
      make ?studio ()
    let of_json json =
      let studio = field_map json "Studio" Studio.of_json in make ?studio ()
    let to_json v = composed_to_json to_value v
  end
module InstanceId =
  struct
    type nonrec t = string
    let context_ = "InstanceId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"InstanceId" j
    let to_json = simple_to_json to_value
  end
module EC2InstanceIdsList =
  struct
    type nonrec t = InstanceId.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:InstanceId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InstanceId.of_xml)
    let of_json j =
      list_of_json ~kind:"EC2InstanceIdsList" ~of_json:InstanceId.of_json j
    let to_json v = composed_to_json to_value v
  end
module EC2InstanceIdsToTerminateList =
  struct
    type nonrec t = InstanceId.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:InstanceId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InstanceId.of_xml)
    let of_json j =
      list_of_json ~kind:"EC2InstanceIdsToTerminateList"
        ~of_json:InstanceId.of_json j
    let to_json v = composed_to_json to_value v
  end
module EbsBlockDevice =
  struct
    type nonrec t =
      {
      volumeSpecification: VolumeSpecification.t option ;
      device: String_.t option }
    let make ?volumeSpecification =
      fun ?device -> fun () -> { volumeSpecification; device }
    let to_value x =
      structure_to_value
        [("VolumeSpecification",
           (Option.map x.volumeSpecification ~f:VolumeSpecification.to_value));
        ("Device", (Option.map x.device ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let device =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Device") in
      let volumeSpecification =
        (Option.map ~f:VolumeSpecification.of_xml)
          (Xml.child xml_arg0 "VolumeSpecification") in
      make ?device ?volumeSpecification ()
    let of_json json =
      let device = field_map json "Device" String_.of_json in
      let volumeSpecification =
        field_map json "VolumeSpecification" VolumeSpecification.of_json in
      make ?device ?volumeSpecification ()
    let to_json v = composed_to_json to_value v
  end
module EbsBlockDeviceList =
  struct
    type nonrec t = EbsBlockDevice.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:EbsBlockDevice.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EbsBlockDevice.of_xml)
    let of_json j =
      list_of_json ~kind:"EbsBlockDeviceList" ~of_json:EbsBlockDevice.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module EbsVolume =
  struct
    type nonrec t = {
      device: String_.t option ;
      volumeId: String_.t option }
    let make ?device = fun ?volumeId -> fun () -> { device; volumeId }
    let to_value x =
      structure_to_value
        [("Device", (Option.map x.device ~f:String_.to_value));
        ("VolumeId", (Option.map x.volumeId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let volumeId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "VolumeId") in
      let device =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Device") in
      make ?volumeId ?device ()
    let of_json json =
      let volumeId = field_map json "VolumeId" String_.of_json in
      let device = field_map json "Device" String_.of_json in
      make ?volumeId ?device ()
    let to_json v = composed_to_json to_value v
  end
module EbsVolumeList =
  struct
    type nonrec t = EbsVolume.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:EbsVolume.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EbsVolume.of_xml)
    let of_json j =
      list_of_json ~kind:"EbsVolumeList" ~of_json:EbsVolume.of_json j
    let to_json v = composed_to_json to_value v
  end
module GetAutoTerminationPolicyInput =
  struct
    type nonrec t = {
      clusterId: ClusterId.t }
    let context_ = "GetAutoTerminationPolicyInput"
    let make ~clusterId = fun () -> { clusterId }
    let to_value x =
      structure_to_value
        [("ClusterId", (Some (ClusterId.to_value x.clusterId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clusterId =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClusterId") in
      make ~clusterId ()
    let of_json json =
      let clusterId = field_map_exn json "ClusterId" ClusterId.of_json in
      make ~clusterId ()
    let to_json v = composed_to_json to_value v
  end
module GetAutoTerminationPolicyOutput =
  struct
    type nonrec t = {
      autoTerminationPolicy: AutoTerminationPolicy.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?autoTerminationPolicy = fun () -> { autoTerminationPolicy }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("AutoTerminationPolicy",
           (Option.map x.autoTerminationPolicy
              ~f:AutoTerminationPolicy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let autoTerminationPolicy =
        (Option.map ~f:AutoTerminationPolicy.of_xml)
          (Xml.child xml_arg0 "AutoTerminationPolicy") in
      make ?autoTerminationPolicy ()
    let of_json json =
      let autoTerminationPolicy =
        field_map json "AutoTerminationPolicy" AutoTerminationPolicy.of_json in
      make ?autoTerminationPolicy ()
    let to_json v = composed_to_json to_value v
  end
module GetBlockPublicAccessConfigurationInput =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module GetBlockPublicAccessConfigurationOutput =
  struct
    type nonrec t =
      {
      blockPublicAccessConfiguration: BlockPublicAccessConfiguration.t ;
      blockPublicAccessConfigurationMetadata:
        BlockPublicAccessConfigurationMetadata.t }
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetBlockPublicAccessConfigurationOutput"
    let make ~blockPublicAccessConfiguration =
      fun ~blockPublicAccessConfigurationMetadata ->
        fun () ->
          {
            blockPublicAccessConfiguration;
            blockPublicAccessConfigurationMetadata
          }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("BlockPublicAccessConfiguration",
           (Some
              (BlockPublicAccessConfiguration.to_value
                 x.blockPublicAccessConfiguration)));
        ("BlockPublicAccessConfigurationMetadata",
          (Some
             (BlockPublicAccessConfigurationMetadata.to_value
                x.blockPublicAccessConfigurationMetadata)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let blockPublicAccessConfigurationMetadata =
        BlockPublicAccessConfigurationMetadata.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "BlockPublicAccessConfigurationMetadata") in
      let blockPublicAccessConfiguration =
        BlockPublicAccessConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "BlockPublicAccessConfiguration") in
      make ~blockPublicAccessConfigurationMetadata
        ~blockPublicAccessConfiguration ()
    let of_json json =
      let blockPublicAccessConfigurationMetadata =
        field_map_exn json "BlockPublicAccessConfigurationMetadata"
          BlockPublicAccessConfigurationMetadata.of_json in
      let blockPublicAccessConfiguration =
        field_map_exn json "BlockPublicAccessConfiguration"
          BlockPublicAccessConfiguration.of_json in
      make ~blockPublicAccessConfigurationMetadata
        ~blockPublicAccessConfiguration ()
    let to_json v = composed_to_json to_value v
  end
module GetManagedScalingPolicyInput =
  struct
    type nonrec t = {
      clusterId: ClusterId.t }
    let context_ = "GetManagedScalingPolicyInput"
    let make ~clusterId = fun () -> { clusterId }
    let to_value x =
      structure_to_value
        [("ClusterId", (Some (ClusterId.to_value x.clusterId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clusterId =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClusterId") in
      make ~clusterId ()
    let of_json json =
      let clusterId = field_map_exn json "ClusterId" ClusterId.of_json in
      make ~clusterId ()
    let to_json v = composed_to_json to_value v
  end
module ManagedScalingPolicy =
  struct
    type nonrec t = {
      computeLimits: ComputeLimits.t option }
    let make ?computeLimits = fun () -> { computeLimits }
    let to_value x =
      structure_to_value
        [("ComputeLimits",
           (Option.map x.computeLimits ~f:ComputeLimits.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let computeLimits =
        (Option.map ~f:ComputeLimits.of_xml)
          (Xml.child xml_arg0 "ComputeLimits") in
      make ?computeLimits ()
    let of_json json =
      let computeLimits =
        field_map json "ComputeLimits" ComputeLimits.of_json in
      make ?computeLimits ()
    let to_json v = composed_to_json to_value v
  end
module GetManagedScalingPolicyOutput =
  struct
    type nonrec t = {
      managedScalingPolicy: ManagedScalingPolicy.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?managedScalingPolicy = fun () -> { managedScalingPolicy }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ManagedScalingPolicy",
           (Option.map x.managedScalingPolicy
              ~f:ManagedScalingPolicy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let managedScalingPolicy =
        (Option.map ~f:ManagedScalingPolicy.of_xml)
          (Xml.child xml_arg0 "ManagedScalingPolicy") in
      make ?managedScalingPolicy ()
    let of_json json =
      let managedScalingPolicy =
        field_map json "ManagedScalingPolicy" ManagedScalingPolicy.of_json in
      make ?managedScalingPolicy ()
    let to_json v = composed_to_json to_value v
  end
module GetStudioSessionMappingInput =
  struct
    type nonrec t =
      {
      studioId: XmlStringMaxLen256.t ;
      identityId: XmlStringMaxLen256.t option ;
      identityName: XmlStringMaxLen256.t option ;
      identityType: IdentityType.t }
    let context_ = "GetStudioSessionMappingInput"
    let make ?identityId =
      fun ?identityName ->
        fun ~studioId ->
          fun ~identityType ->
            fun () -> { identityId; identityName; studioId; identityType }
    let to_value x =
      structure_to_value
        [("StudioId", (Some (XmlStringMaxLen256.to_value x.studioId)));
        ("IdentityId",
          (Option.map x.identityId ~f:XmlStringMaxLen256.to_value));
        ("IdentityName",
          (Option.map x.identityName ~f:XmlStringMaxLen256.to_value));
        ("IdentityType", (Some (IdentityType.to_value x.identityType)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let identityType =
        IdentityType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IdentityType") in
      let identityName =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "IdentityName") in
      let identityId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "IdentityId") in
      let studioId =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "StudioId") in
      make ~identityType ?identityName ?identityId ~studioId ()
    let of_json json =
      let identityType =
        field_map_exn json "IdentityType" IdentityType.of_json in
      let identityName =
        field_map json "IdentityName" XmlStringMaxLen256.of_json in
      let identityId = field_map json "IdentityId" XmlStringMaxLen256.of_json in
      let studioId = field_map_exn json "StudioId" XmlStringMaxLen256.of_json in
      make ~identityType ?identityName ?identityId ~studioId ()
    let to_json v = composed_to_json to_value v
  end
module SessionMappingDetail =
  struct
    type nonrec t =
      {
      studioId: XmlStringMaxLen256.t option ;
      identityId: XmlStringMaxLen256.t option ;
      identityName: XmlStringMaxLen256.t option ;
      identityType: IdentityType.t option ;
      sessionPolicyArn: XmlStringMaxLen256.t option ;
      creationTime: Date.t option ;
      lastModifiedTime: Date.t option }
    let make ?studioId =
      fun ?identityId ->
        fun ?identityName ->
          fun ?identityType ->
            fun ?sessionPolicyArn ->
              fun ?creationTime ->
                fun ?lastModifiedTime ->
                  fun () ->
                    {
                      studioId;
                      identityId;
                      identityName;
                      identityType;
                      sessionPolicyArn;
                      creationTime;
                      lastModifiedTime
                    }
    let to_value x =
      structure_to_value
        [("StudioId", (Option.map x.studioId ~f:XmlStringMaxLen256.to_value));
        ("IdentityId",
          (Option.map x.identityId ~f:XmlStringMaxLen256.to_value));
        ("IdentityName",
          (Option.map x.identityName ~f:XmlStringMaxLen256.to_value));
        ("IdentityType",
          (Option.map x.identityType ~f:IdentityType.to_value));
        ("SessionPolicyArn",
          (Option.map x.sessionPolicyArn ~f:XmlStringMaxLen256.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:Date.to_value));
        ("LastModifiedTime",
          (Option.map x.lastModifiedTime ~f:Date.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastModifiedTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "LastModifiedTime") in
      let creationTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let sessionPolicyArn =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "SessionPolicyArn") in
      let identityType =
        (Option.map ~f:IdentityType.of_xml)
          (Xml.child xml_arg0 "IdentityType") in
      let identityName =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "IdentityName") in
      let identityId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "IdentityId") in
      let studioId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "StudioId") in
      make ?lastModifiedTime ?creationTime ?sessionPolicyArn ?identityType
        ?identityName ?identityId ?studioId ()
    let of_json json =
      let lastModifiedTime = field_map json "LastModifiedTime" Date.of_json in
      let creationTime = field_map json "CreationTime" Date.of_json in
      let sessionPolicyArn =
        field_map json "SessionPolicyArn" XmlStringMaxLen256.of_json in
      let identityType = field_map json "IdentityType" IdentityType.of_json in
      let identityName =
        field_map json "IdentityName" XmlStringMaxLen256.of_json in
      let identityId = field_map json "IdentityId" XmlStringMaxLen256.of_json in
      let studioId = field_map json "StudioId" XmlStringMaxLen256.of_json in
      make ?lastModifiedTime ?creationTime ?sessionPolicyArn ?identityType
        ?identityName ?identityId ?studioId ()
    let to_json v = composed_to_json to_value v
  end
module GetStudioSessionMappingOutput =
  struct
    type nonrec t = {
      sessionMapping: SessionMappingDetail.t option }
    type nonrec error =
      [ `InternalServerError of InternalServerError.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?sessionMapping = fun () -> { sessionMapping }
    let error_of_json name json =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("SessionMapping",
           (Option.map x.sessionMapping ~f:SessionMappingDetail.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionMapping =
        (Option.map ~f:SessionMappingDetail.of_xml)
          (Xml.child xml_arg0 "SessionMapping") in
      make ?sessionMapping ()
    let of_json json =
      let sessionMapping =
        field_map json "SessionMapping" SessionMappingDetail.of_json in
      make ?sessionMapping ()
    let to_json v = composed_to_json to_value v
  end
module InstanceTimeline =
  struct
    type nonrec t =
      {
      creationDateTime: Date.t option ;
      readyDateTime: Date.t option ;
      endDateTime: Date.t option }
    let make ?creationDateTime =
      fun ?readyDateTime ->
        fun ?endDateTime ->
          fun () -> { creationDateTime; readyDateTime; endDateTime }
    let to_value x =
      structure_to_value
        [("CreationDateTime",
           (Option.map x.creationDateTime ~f:Date.to_value));
        ("ReadyDateTime", (Option.map x.readyDateTime ~f:Date.to_value));
        ("EndDateTime", (Option.map x.endDateTime ~f:Date.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let endDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "EndDateTime") in
      let readyDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "ReadyDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "CreationDateTime") in
      make ?endDateTime ?readyDateTime ?creationDateTime ()
    let of_json json =
      let endDateTime = field_map json "EndDateTime" Date.of_json in
      let readyDateTime = field_map json "ReadyDateTime" Date.of_json in
      let creationDateTime = field_map json "CreationDateTime" Date.of_json in
      make ?endDateTime ?readyDateTime ?creationDateTime ()
    let to_json v = composed_to_json to_value v
  end
module InstanceStateChangeReasonCode =
  struct
    type nonrec t =
      | INTERNAL_ERROR 
      | VALIDATION_ERROR 
      | INSTANCE_FAILURE 
      | BOOTSTRAP_FAILURE 
      | CLUSTER_TERMINATED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | INTERNAL_ERROR -> "INTERNAL_ERROR"
      | VALIDATION_ERROR -> "VALIDATION_ERROR"
      | INSTANCE_FAILURE -> "INSTANCE_FAILURE"
      | BOOTSTRAP_FAILURE -> "BOOTSTRAP_FAILURE"
      | CLUSTER_TERMINATED -> "CLUSTER_TERMINATED"
      | Non_static_id s -> s
    let of_string =
      function
      | "INTERNAL_ERROR" -> INTERNAL_ERROR
      | "VALIDATION_ERROR" -> VALIDATION_ERROR
      | "INSTANCE_FAILURE" -> INSTANCE_FAILURE
      | "BOOTSTRAP_FAILURE" -> BOOTSTRAP_FAILURE
      | "CLUSTER_TERMINATED" -> CLUSTER_TERMINATED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration InstanceStateChangeReasonCode"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"InstanceStateChangeReasonCode" j)
    let to_json = simple_to_json to_value
  end
module InstanceStateChangeReason =
  struct
    type nonrec t =
      {
      code: InstanceStateChangeReasonCode.t option ;
      message: String_.t option }
    let make ?code = fun ?message -> fun () -> { code; message }
    let to_value x =
      structure_to_value
        [("Code",
           (Option.map x.code ~f:InstanceStateChangeReasonCode.to_value));
        ("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      let code =
        (Option.map ~f:InstanceStateChangeReasonCode.of_xml)
          (Xml.child xml_arg0 "Code") in
      make ?message ?code ()
    let of_json json =
      let message = field_map json "Message" String_.of_json in
      let code = field_map json "Code" InstanceStateChangeReasonCode.of_json in
      make ?message ?code ()
    let to_json v = composed_to_json to_value v
  end
module InstanceState =
  struct
    type nonrec t =
      | AWAITING_FULFILLMENT 
      | PROVISIONING 
      | BOOTSTRAPPING 
      | RUNNING 
      | TERMINATED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | AWAITING_FULFILLMENT -> "AWAITING_FULFILLMENT"
      | PROVISIONING -> "PROVISIONING"
      | BOOTSTRAPPING -> "BOOTSTRAPPING"
      | RUNNING -> "RUNNING"
      | TERMINATED -> "TERMINATED"
      | Non_static_id s -> s
    let of_string =
      function
      | "AWAITING_FULFILLMENT" -> AWAITING_FULFILLMENT
      | "PROVISIONING" -> PROVISIONING
      | "BOOTSTRAPPING" -> BOOTSTRAPPING
      | "RUNNING" -> RUNNING
      | "TERMINATED" -> TERMINATED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration InstanceState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"InstanceState" j)
    let to_json = simple_to_json to_value
  end
module InstanceStatus =
  struct
    type nonrec t =
      {
      state: InstanceState.t option ;
      stateChangeReason: InstanceStateChangeReason.t option ;
      timeline: InstanceTimeline.t option }
    let make ?state =
      fun ?stateChangeReason ->
        fun ?timeline -> fun () -> { state; stateChangeReason; timeline }
    let to_value x =
      structure_to_value
        [("State", (Option.map x.state ~f:InstanceState.to_value));
        ("StateChangeReason",
          (Option.map x.stateChangeReason
             ~f:InstanceStateChangeReason.to_value));
        ("Timeline", (Option.map x.timeline ~f:InstanceTimeline.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timeline =
        (Option.map ~f:InstanceTimeline.of_xml)
          (Xml.child xml_arg0 "Timeline") in
      let stateChangeReason =
        (Option.map ~f:InstanceStateChangeReason.of_xml)
          (Xml.child xml_arg0 "StateChangeReason") in
      let state =
        (Option.map ~f:InstanceState.of_xml) (Xml.child xml_arg0 "State") in
      make ?timeline ?stateChangeReason ?state ()
    let of_json json =
      let timeline = field_map json "Timeline" InstanceTimeline.of_json in
      let stateChangeReason =
        field_map json "StateChangeReason" InstanceStateChangeReason.of_json in
      let state = field_map json "State" InstanceState.of_json in
      make ?timeline ?stateChangeReason ?state ()
    let to_json v = composed_to_json to_value v
  end
module Instance =
  struct
    type nonrec t =
      {
      id: InstanceId.t option ;
      ec2InstanceId: InstanceId.t option ;
      publicDnsName: String_.t option ;
      publicIpAddress: String_.t option ;
      privateDnsName: String_.t option ;
      privateIpAddress: String_.t option ;
      status: InstanceStatus.t option ;
      instanceGroupId: String_.t option ;
      instanceFleetId: InstanceFleetId.t option ;
      market: MarketType.t option ;
      instanceType: InstanceType.t option ;
      ebsVolumes: EbsVolumeList.t option }
    let make ?id =
      fun ?ec2InstanceId ->
        fun ?publicDnsName ->
          fun ?publicIpAddress ->
            fun ?privateDnsName ->
              fun ?privateIpAddress ->
                fun ?status ->
                  fun ?instanceGroupId ->
                    fun ?instanceFleetId ->
                      fun ?market ->
                        fun ?instanceType ->
                          fun ?ebsVolumes ->
                            fun () ->
                              {
                                id;
                                ec2InstanceId;
                                publicDnsName;
                                publicIpAddress;
                                privateDnsName;
                                privateIpAddress;
                                status;
                                instanceGroupId;
                                instanceFleetId;
                                market;
                                instanceType;
                                ebsVolumes
                              }
    let to_value x =
      structure_to_value
        [("Id", (Option.map x.id ~f:InstanceId.to_value));
        ("Ec2InstanceId",
          (Option.map x.ec2InstanceId ~f:InstanceId.to_value));
        ("PublicDnsName", (Option.map x.publicDnsName ~f:String_.to_value));
        ("PublicIpAddress",
          (Option.map x.publicIpAddress ~f:String_.to_value));
        ("PrivateDnsName", (Option.map x.privateDnsName ~f:String_.to_value));
        ("PrivateIpAddress",
          (Option.map x.privateIpAddress ~f:String_.to_value));
        ("Status", (Option.map x.status ~f:InstanceStatus.to_value));
        ("InstanceGroupId",
          (Option.map x.instanceGroupId ~f:String_.to_value));
        ("InstanceFleetId",
          (Option.map x.instanceFleetId ~f:InstanceFleetId.to_value));
        ("Market", (Option.map x.market ~f:MarketType.to_value));
        ("InstanceType",
          (Option.map x.instanceType ~f:InstanceType.to_value));
        ("EbsVolumes", (Option.map x.ebsVolumes ~f:EbsVolumeList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ebsVolumes =
        (Option.map ~f:EbsVolumeList.of_xml)
          (Xml.child xml_arg0 "EbsVolumes") in
      let instanceType =
        (Option.map ~f:InstanceType.of_xml)
          (Xml.child xml_arg0 "InstanceType") in
      let market =
        (Option.map ~f:MarketType.of_xml) (Xml.child xml_arg0 "Market") in
      let instanceFleetId =
        (Option.map ~f:InstanceFleetId.of_xml)
          (Xml.child xml_arg0 "InstanceFleetId") in
      let instanceGroupId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "InstanceGroupId") in
      let status =
        (Option.map ~f:InstanceStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let privateIpAddress =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "PrivateIpAddress") in
      let privateDnsName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "PrivateDnsName") in
      let publicIpAddress =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "PublicIpAddress") in
      let publicDnsName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "PublicDnsName") in
      let ec2InstanceId =
        (Option.map ~f:InstanceId.of_xml)
          (Xml.child xml_arg0 "Ec2InstanceId") in
      let id = (Option.map ~f:InstanceId.of_xml) (Xml.child xml_arg0 "Id") in
      make ?ebsVolumes ?instanceType ?market ?instanceFleetId
        ?instanceGroupId ?status ?privateIpAddress ?privateDnsName
        ?publicIpAddress ?publicDnsName ?ec2InstanceId ?id ()
    let of_json json =
      let ebsVolumes = field_map json "EbsVolumes" EbsVolumeList.of_json in
      let instanceType = field_map json "InstanceType" InstanceType.of_json in
      let market = field_map json "Market" MarketType.of_json in
      let instanceFleetId =
        field_map json "InstanceFleetId" InstanceFleetId.of_json in
      let instanceGroupId = field_map json "InstanceGroupId" String_.of_json in
      let status = field_map json "Status" InstanceStatus.of_json in
      let privateIpAddress =
        field_map json "PrivateIpAddress" String_.of_json in
      let privateDnsName = field_map json "PrivateDnsName" String_.of_json in
      let publicIpAddress = field_map json "PublicIpAddress" String_.of_json in
      let publicDnsName = field_map json "PublicDnsName" String_.of_json in
      let ec2InstanceId = field_map json "Ec2InstanceId" InstanceId.of_json in
      let id = field_map json "Id" InstanceId.of_json in
      make ?ebsVolumes ?instanceType ?market ?instanceFleetId
        ?instanceGroupId ?status ?privateIpAddress ?privateDnsName
        ?publicIpAddress ?publicDnsName ?ec2InstanceId ?id ()
    let to_json v = composed_to_json to_value v
  end
module InstanceTypeSpecification =
  struct
    type nonrec t =
      {
      instanceType: InstanceType.t option ;
      weightedCapacity: WholeNumber.t option ;
      bidPrice: XmlStringMaxLen256.t option ;
      bidPriceAsPercentageOfOnDemandPrice: NonNegativeDouble.t option ;
      configurations: ConfigurationList.t option ;
      ebsBlockDevices: EbsBlockDeviceList.t option ;
      ebsOptimized: BooleanObject.t option ;
      customAmiId: XmlStringMaxLen256.t option }
    let make ?instanceType =
      fun ?weightedCapacity ->
        fun ?bidPrice ->
          fun ?bidPriceAsPercentageOfOnDemandPrice ->
            fun ?configurations ->
              fun ?ebsBlockDevices ->
                fun ?ebsOptimized ->
                  fun ?customAmiId ->
                    fun () ->
                      {
                        instanceType;
                        weightedCapacity;
                        bidPrice;
                        bidPriceAsPercentageOfOnDemandPrice;
                        configurations;
                        ebsBlockDevices;
                        ebsOptimized;
                        customAmiId
                      }
    let to_value x =
      structure_to_value
        [("InstanceType",
           (Option.map x.instanceType ~f:InstanceType.to_value));
        ("WeightedCapacity",
          (Option.map x.weightedCapacity ~f:WholeNumber.to_value));
        ("BidPrice", (Option.map x.bidPrice ~f:XmlStringMaxLen256.to_value));
        ("BidPriceAsPercentageOfOnDemandPrice",
          (Option.map x.bidPriceAsPercentageOfOnDemandPrice
             ~f:NonNegativeDouble.to_value));
        ("Configurations",
          (Option.map x.configurations ~f:ConfigurationList.to_value));
        ("EbsBlockDevices",
          (Option.map x.ebsBlockDevices ~f:EbsBlockDeviceList.to_value));
        ("EbsOptimized",
          (Option.map x.ebsOptimized ~f:BooleanObject.to_value));
        ("CustomAmiId",
          (Option.map x.customAmiId ~f:XmlStringMaxLen256.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let customAmiId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "CustomAmiId") in
      let ebsOptimized =
        (Option.map ~f:BooleanObject.of_xml)
          (Xml.child xml_arg0 "EbsOptimized") in
      let ebsBlockDevices =
        (Option.map ~f:EbsBlockDeviceList.of_xml)
          (Xml.child xml_arg0 "EbsBlockDevices") in
      let configurations =
        (Option.map ~f:ConfigurationList.of_xml)
          (Xml.child xml_arg0 "Configurations") in
      let bidPriceAsPercentageOfOnDemandPrice =
        (Option.map ~f:NonNegativeDouble.of_xml)
          (Xml.child xml_arg0 "BidPriceAsPercentageOfOnDemandPrice") in
      let bidPrice =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "BidPrice") in
      let weightedCapacity =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "WeightedCapacity") in
      let instanceType =
        (Option.map ~f:InstanceType.of_xml)
          (Xml.child xml_arg0 "InstanceType") in
      make ?customAmiId ?ebsOptimized ?ebsBlockDevices ?configurations
        ?bidPriceAsPercentageOfOnDemandPrice ?bidPrice ?weightedCapacity
        ?instanceType ()
    let of_json json =
      let customAmiId =
        field_map json "CustomAmiId" XmlStringMaxLen256.of_json in
      let ebsOptimized = field_map json "EbsOptimized" BooleanObject.of_json in
      let ebsBlockDevices =
        field_map json "EbsBlockDevices" EbsBlockDeviceList.of_json in
      let configurations =
        field_map json "Configurations" ConfigurationList.of_json in
      let bidPriceAsPercentageOfOnDemandPrice =
        field_map json "BidPriceAsPercentageOfOnDemandPrice"
          NonNegativeDouble.of_json in
      let bidPrice = field_map json "BidPrice" XmlStringMaxLen256.of_json in
      let weightedCapacity =
        field_map json "WeightedCapacity" WholeNumber.of_json in
      let instanceType = field_map json "InstanceType" InstanceType.of_json in
      make ?customAmiId ?ebsOptimized ?ebsBlockDevices ?configurations
        ?bidPriceAsPercentageOfOnDemandPrice ?bidPrice ?weightedCapacity
        ?instanceType ()
    let to_json v = composed_to_json to_value v
  end
module InstanceTypeSpecificationList =
  struct
    type nonrec t = InstanceTypeSpecification.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:InstanceTypeSpecification.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InstanceTypeSpecification.of_xml)
    let of_json j =
      list_of_json ~kind:"InstanceTypeSpecificationList"
        ~of_json:InstanceTypeSpecification.of_json j
    let to_json v = composed_to_json to_value v
  end
module InstanceFleetTimeline =
  struct
    type nonrec t =
      {
      creationDateTime: Date.t option ;
      readyDateTime: Date.t option ;
      endDateTime: Date.t option }
    let make ?creationDateTime =
      fun ?readyDateTime ->
        fun ?endDateTime ->
          fun () -> { creationDateTime; readyDateTime; endDateTime }
    let to_value x =
      structure_to_value
        [("CreationDateTime",
           (Option.map x.creationDateTime ~f:Date.to_value));
        ("ReadyDateTime", (Option.map x.readyDateTime ~f:Date.to_value));
        ("EndDateTime", (Option.map x.endDateTime ~f:Date.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let endDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "EndDateTime") in
      let readyDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "ReadyDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "CreationDateTime") in
      make ?endDateTime ?readyDateTime ?creationDateTime ()
    let of_json json =
      let endDateTime = field_map json "EndDateTime" Date.of_json in
      let readyDateTime = field_map json "ReadyDateTime" Date.of_json in
      let creationDateTime = field_map json "CreationDateTime" Date.of_json in
      make ?endDateTime ?readyDateTime ?creationDateTime ()
    let to_json v = composed_to_json to_value v
  end
module InstanceFleetStateChangeReasonCode =
  struct
    type nonrec t =
      | INTERNAL_ERROR 
      | VALIDATION_ERROR 
      | INSTANCE_FAILURE 
      | CLUSTER_TERMINATED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | INTERNAL_ERROR -> "INTERNAL_ERROR"
      | VALIDATION_ERROR -> "VALIDATION_ERROR"
      | INSTANCE_FAILURE -> "INSTANCE_FAILURE"
      | CLUSTER_TERMINATED -> "CLUSTER_TERMINATED"
      | Non_static_id s -> s
    let of_string =
      function
      | "INTERNAL_ERROR" -> INTERNAL_ERROR
      | "VALIDATION_ERROR" -> VALIDATION_ERROR
      | "INSTANCE_FAILURE" -> INSTANCE_FAILURE
      | "CLUSTER_TERMINATED" -> CLUSTER_TERMINATED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration InstanceFleetStateChangeReasonCode"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"InstanceFleetStateChangeReasonCode" j)
    let to_json = simple_to_json to_value
  end
module InstanceFleetStateChangeReason =
  struct
    type nonrec t =
      {
      code: InstanceFleetStateChangeReasonCode.t option ;
      message: String_.t option }
    let make ?code = fun ?message -> fun () -> { code; message }
    let to_value x =
      structure_to_value
        [("Code",
           (Option.map x.code ~f:InstanceFleetStateChangeReasonCode.to_value));
        ("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      let code =
        (Option.map ~f:InstanceFleetStateChangeReasonCode.of_xml)
          (Xml.child xml_arg0 "Code") in
      make ?message ?code ()
    let of_json json =
      let message = field_map json "Message" String_.of_json in
      let code =
        field_map json "Code" InstanceFleetStateChangeReasonCode.of_json in
      make ?message ?code ()
    let to_json v = composed_to_json to_value v
  end
module InstanceFleetState =
  struct
    type nonrec t =
      | PROVISIONING 
      | BOOTSTRAPPING 
      | RUNNING 
      | RESIZING 
      | SUSPENDED 
      | TERMINATING 
      | TERMINATED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PROVISIONING -> "PROVISIONING"
      | BOOTSTRAPPING -> "BOOTSTRAPPING"
      | RUNNING -> "RUNNING"
      | RESIZING -> "RESIZING"
      | SUSPENDED -> "SUSPENDED"
      | TERMINATING -> "TERMINATING"
      | TERMINATED -> "TERMINATED"
      | Non_static_id s -> s
    let of_string =
      function
      | "PROVISIONING" -> PROVISIONING
      | "BOOTSTRAPPING" -> BOOTSTRAPPING
      | "RUNNING" -> RUNNING
      | "RESIZING" -> RESIZING
      | "SUSPENDED" -> SUSPENDED
      | "TERMINATING" -> TERMINATING
      | "TERMINATED" -> TERMINATED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration InstanceFleetState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"InstanceFleetState" j)
    let to_json = simple_to_json to_value
  end
module InstanceFleetStatus =
  struct
    type nonrec t =
      {
      state: InstanceFleetState.t option ;
      stateChangeReason: InstanceFleetStateChangeReason.t option ;
      timeline: InstanceFleetTimeline.t option }
    let make ?state =
      fun ?stateChangeReason ->
        fun ?timeline -> fun () -> { state; stateChangeReason; timeline }
    let to_value x =
      structure_to_value
        [("State", (Option.map x.state ~f:InstanceFleetState.to_value));
        ("StateChangeReason",
          (Option.map x.stateChangeReason
             ~f:InstanceFleetStateChangeReason.to_value));
        ("Timeline",
          (Option.map x.timeline ~f:InstanceFleetTimeline.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timeline =
        (Option.map ~f:InstanceFleetTimeline.of_xml)
          (Xml.child xml_arg0 "Timeline") in
      let stateChangeReason =
        (Option.map ~f:InstanceFleetStateChangeReason.of_xml)
          (Xml.child xml_arg0 "StateChangeReason") in
      let state =
        (Option.map ~f:InstanceFleetState.of_xml)
          (Xml.child xml_arg0 "State") in
      make ?timeline ?stateChangeReason ?state ()
    let of_json json =
      let timeline = field_map json "Timeline" InstanceFleetTimeline.of_json in
      let stateChangeReason =
        field_map json "StateChangeReason"
          InstanceFleetStateChangeReason.of_json in
      let state = field_map json "State" InstanceFleetState.of_json in
      make ?timeline ?stateChangeReason ?state ()
    let to_json v = composed_to_json to_value v
  end
module InstanceFleet =
  struct
    type nonrec t =
      {
      id: InstanceFleetId.t option ;
      name: XmlStringMaxLen256.t option ;
      status: InstanceFleetStatus.t option ;
      instanceFleetType: InstanceFleetType.t option ;
      targetOnDemandCapacity: WholeNumber.t option ;
      targetSpotCapacity: WholeNumber.t option ;
      provisionedOnDemandCapacity: WholeNumber.t option ;
      provisionedSpotCapacity: WholeNumber.t option ;
      instanceTypeSpecifications: InstanceTypeSpecificationList.t option ;
      launchSpecifications: InstanceFleetProvisioningSpecifications.t option }
    let make ?id =
      fun ?name ->
        fun ?status ->
          fun ?instanceFleetType ->
            fun ?targetOnDemandCapacity ->
              fun ?targetSpotCapacity ->
                fun ?provisionedOnDemandCapacity ->
                  fun ?provisionedSpotCapacity ->
                    fun ?instanceTypeSpecifications ->
                      fun ?launchSpecifications ->
                        fun () ->
                          {
                            id;
                            name;
                            status;
                            instanceFleetType;
                            targetOnDemandCapacity;
                            targetSpotCapacity;
                            provisionedOnDemandCapacity;
                            provisionedSpotCapacity;
                            instanceTypeSpecifications;
                            launchSpecifications
                          }
    let to_value x =
      structure_to_value
        [("Id", (Option.map x.id ~f:InstanceFleetId.to_value));
        ("Name", (Option.map x.name ~f:XmlStringMaxLen256.to_value));
        ("Status", (Option.map x.status ~f:InstanceFleetStatus.to_value));
        ("InstanceFleetType",
          (Option.map x.instanceFleetType ~f:InstanceFleetType.to_value));
        ("TargetOnDemandCapacity",
          (Option.map x.targetOnDemandCapacity ~f:WholeNumber.to_value));
        ("TargetSpotCapacity",
          (Option.map x.targetSpotCapacity ~f:WholeNumber.to_value));
        ("ProvisionedOnDemandCapacity",
          (Option.map x.provisionedOnDemandCapacity ~f:WholeNumber.to_value));
        ("ProvisionedSpotCapacity",
          (Option.map x.provisionedSpotCapacity ~f:WholeNumber.to_value));
        ("InstanceTypeSpecifications",
          (Option.map x.instanceTypeSpecifications
             ~f:InstanceTypeSpecificationList.to_value));
        ("LaunchSpecifications",
          (Option.map x.launchSpecifications
             ~f:InstanceFleetProvisioningSpecifications.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let launchSpecifications =
        (Option.map ~f:InstanceFleetProvisioningSpecifications.of_xml)
          (Xml.child xml_arg0 "LaunchSpecifications") in
      let instanceTypeSpecifications =
        (Option.map ~f:InstanceTypeSpecificationList.of_xml)
          (Xml.child xml_arg0 "InstanceTypeSpecifications") in
      let provisionedSpotCapacity =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "ProvisionedSpotCapacity") in
      let provisionedOnDemandCapacity =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "ProvisionedOnDemandCapacity") in
      let targetSpotCapacity =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "TargetSpotCapacity") in
      let targetOnDemandCapacity =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "TargetOnDemandCapacity") in
      let instanceFleetType =
        (Option.map ~f:InstanceFleetType.of_xml)
          (Xml.child xml_arg0 "InstanceFleetType") in
      let status =
        (Option.map ~f:InstanceFleetStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let name =
        (Option.map ~f:XmlStringMaxLen256.of_xml) (Xml.child xml_arg0 "Name") in
      let id =
        (Option.map ~f:InstanceFleetId.of_xml) (Xml.child xml_arg0 "Id") in
      make ?launchSpecifications ?instanceTypeSpecifications
        ?provisionedSpotCapacity ?provisionedOnDemandCapacity
        ?targetSpotCapacity ?targetOnDemandCapacity ?instanceFleetType
        ?status ?name ?id ()
    let of_json json =
      let launchSpecifications =
        field_map json "LaunchSpecifications"
          InstanceFleetProvisioningSpecifications.of_json in
      let instanceTypeSpecifications =
        field_map json "InstanceTypeSpecifications"
          InstanceTypeSpecificationList.of_json in
      let provisionedSpotCapacity =
        field_map json "ProvisionedSpotCapacity" WholeNumber.of_json in
      let provisionedOnDemandCapacity =
        field_map json "ProvisionedOnDemandCapacity" WholeNumber.of_json in
      let targetSpotCapacity =
        field_map json "TargetSpotCapacity" WholeNumber.of_json in
      let targetOnDemandCapacity =
        field_map json "TargetOnDemandCapacity" WholeNumber.of_json in
      let instanceFleetType =
        field_map json "InstanceFleetType" InstanceFleetType.of_json in
      let status = field_map json "Status" InstanceFleetStatus.of_json in
      let name = field_map json "Name" XmlStringMaxLen256.of_json in
      let id = field_map json "Id" InstanceFleetId.of_json in
      make ?launchSpecifications ?instanceTypeSpecifications
        ?provisionedSpotCapacity ?provisionedOnDemandCapacity
        ?targetSpotCapacity ?targetOnDemandCapacity ?instanceFleetType
        ?status ?name ?id ()
    let to_json v = composed_to_json to_value v
  end
module InstanceFleetConfigList =
  struct
    type nonrec t = InstanceFleetConfig.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:InstanceFleetConfig.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InstanceFleetConfig.of_xml)
    let of_json j =
      list_of_json ~kind:"InstanceFleetConfigList"
        ~of_json:InstanceFleetConfig.of_json j
    let to_json v = composed_to_json to_value v
  end
module InstanceFleetList =
  struct
    type nonrec t = InstanceFleet.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:InstanceFleet.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InstanceFleet.of_xml)
    let of_json j =
      list_of_json ~kind:"InstanceFleetList" ~of_json:InstanceFleet.of_json j
    let to_json v = composed_to_json to_value v
  end
module InstanceFleetModifyConfig =
  struct
    type nonrec t =
      {
      instanceFleetId: InstanceFleetId.t ;
      targetOnDemandCapacity: WholeNumber.t option ;
      targetSpotCapacity: WholeNumber.t option }
    let context_ = "InstanceFleetModifyConfig"
    let make ?targetOnDemandCapacity =
      fun ?targetSpotCapacity ->
        fun ~instanceFleetId ->
          fun () ->
            { targetOnDemandCapacity; targetSpotCapacity; instanceFleetId }
    let to_value x =
      structure_to_value
        [("InstanceFleetId",
           (Some (InstanceFleetId.to_value x.instanceFleetId)));
        ("TargetOnDemandCapacity",
          (Option.map x.targetOnDemandCapacity ~f:WholeNumber.to_value));
        ("TargetSpotCapacity",
          (Option.map x.targetSpotCapacity ~f:WholeNumber.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targetSpotCapacity =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "TargetSpotCapacity") in
      let targetOnDemandCapacity =
        (Option.map ~f:WholeNumber.of_xml)
          (Xml.child xml_arg0 "TargetOnDemandCapacity") in
      let instanceFleetId =
        InstanceFleetId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceFleetId") in
      make ?targetSpotCapacity ?targetOnDemandCapacity ~instanceFleetId ()
    let of_json json =
      let targetSpotCapacity =
        field_map json "TargetSpotCapacity" WholeNumber.of_json in
      let targetOnDemandCapacity =
        field_map json "TargetOnDemandCapacity" WholeNumber.of_json in
      let instanceFleetId =
        field_map_exn json "InstanceFleetId" InstanceFleetId.of_json in
      make ?targetSpotCapacity ?targetOnDemandCapacity ~instanceFleetId ()
    let to_json v = composed_to_json to_value v
  end
module InstanceResizePolicy =
  struct
    type nonrec t =
      {
      instancesToTerminate: EC2InstanceIdsList.t option ;
      instancesToProtect: EC2InstanceIdsList.t option ;
      instanceTerminationTimeout: Integer.t option }
    let make ?instancesToTerminate =
      fun ?instancesToProtect ->
        fun ?instanceTerminationTimeout ->
          fun () ->
            {
              instancesToTerminate;
              instancesToProtect;
              instanceTerminationTimeout
            }
    let to_value x =
      structure_to_value
        [("InstancesToTerminate",
           (Option.map x.instancesToTerminate ~f:EC2InstanceIdsList.to_value));
        ("InstancesToProtect",
          (Option.map x.instancesToProtect ~f:EC2InstanceIdsList.to_value));
        ("InstanceTerminationTimeout",
          (Option.map x.instanceTerminationTimeout ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceTerminationTimeout =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "InstanceTerminationTimeout") in
      let instancesToProtect =
        (Option.map ~f:EC2InstanceIdsList.of_xml)
          (Xml.child xml_arg0 "InstancesToProtect") in
      let instancesToTerminate =
        (Option.map ~f:EC2InstanceIdsList.of_xml)
          (Xml.child xml_arg0 "InstancesToTerminate") in
      make ?instanceTerminationTimeout ?instancesToProtect
        ?instancesToTerminate ()
    let of_json json =
      let instanceTerminationTimeout =
        field_map json "InstanceTerminationTimeout" Integer.of_json in
      let instancesToProtect =
        field_map json "InstancesToProtect" EC2InstanceIdsList.of_json in
      let instancesToTerminate =
        field_map json "InstancesToTerminate" EC2InstanceIdsList.of_json in
      make ?instanceTerminationTimeout ?instancesToProtect
        ?instancesToTerminate ()
    let to_json v = composed_to_json to_value v
  end
module ShrinkPolicy =
  struct
    type nonrec t =
      {
      decommissionTimeout: Integer.t option ;
      instanceResizePolicy: InstanceResizePolicy.t option }
    let make ?decommissionTimeout =
      fun ?instanceResizePolicy ->
        fun () -> { decommissionTimeout; instanceResizePolicy }
    let to_value x =
      structure_to_value
        [("DecommissionTimeout",
           (Option.map x.decommissionTimeout ~f:Integer.to_value));
        ("InstanceResizePolicy",
          (Option.map x.instanceResizePolicy ~f:InstanceResizePolicy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceResizePolicy =
        (Option.map ~f:InstanceResizePolicy.of_xml)
          (Xml.child xml_arg0 "InstanceResizePolicy") in
      let decommissionTimeout =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "DecommissionTimeout") in
      make ?instanceResizePolicy ?decommissionTimeout ()
    let of_json json =
      let instanceResizePolicy =
        field_map json "InstanceResizePolicy" InstanceResizePolicy.of_json in
      let decommissionTimeout =
        field_map json "DecommissionTimeout" Integer.of_json in
      make ?instanceResizePolicy ?decommissionTimeout ()
    let to_json v = composed_to_json to_value v
  end
module InstanceGroupType =
  struct
    type nonrec t =
      | MASTER 
      | CORE 
      | TASK 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | MASTER -> "MASTER"
      | CORE -> "CORE"
      | TASK -> "TASK"
      | Non_static_id s -> s
    let of_string =
      function
      | "MASTER" -> MASTER
      | "CORE" -> CORE
      | "TASK" -> TASK
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration InstanceGroupType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"InstanceGroupType" j)
    let to_json = simple_to_json to_value
  end
module InstanceGroupTimeline =
  struct
    type nonrec t =
      {
      creationDateTime: Date.t option ;
      readyDateTime: Date.t option ;
      endDateTime: Date.t option }
    let make ?creationDateTime =
      fun ?readyDateTime ->
        fun ?endDateTime ->
          fun () -> { creationDateTime; readyDateTime; endDateTime }
    let to_value x =
      structure_to_value
        [("CreationDateTime",
           (Option.map x.creationDateTime ~f:Date.to_value));
        ("ReadyDateTime", (Option.map x.readyDateTime ~f:Date.to_value));
        ("EndDateTime", (Option.map x.endDateTime ~f:Date.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let endDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "EndDateTime") in
      let readyDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "ReadyDateTime") in
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "CreationDateTime") in
      make ?endDateTime ?readyDateTime ?creationDateTime ()
    let of_json json =
      let endDateTime = field_map json "EndDateTime" Date.of_json in
      let readyDateTime = field_map json "ReadyDateTime" Date.of_json in
      let creationDateTime = field_map json "CreationDateTime" Date.of_json in
      make ?endDateTime ?readyDateTime ?creationDateTime ()
    let to_json v = composed_to_json to_value v
  end
module InstanceGroupStateChangeReasonCode =
  struct
    type nonrec t =
      | INTERNAL_ERROR 
      | VALIDATION_ERROR 
      | INSTANCE_FAILURE 
      | CLUSTER_TERMINATED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | INTERNAL_ERROR -> "INTERNAL_ERROR"
      | VALIDATION_ERROR -> "VALIDATION_ERROR"
      | INSTANCE_FAILURE -> "INSTANCE_FAILURE"
      | CLUSTER_TERMINATED -> "CLUSTER_TERMINATED"
      | Non_static_id s -> s
    let of_string =
      function
      | "INTERNAL_ERROR" -> INTERNAL_ERROR
      | "VALIDATION_ERROR" -> VALIDATION_ERROR
      | "INSTANCE_FAILURE" -> INSTANCE_FAILURE
      | "CLUSTER_TERMINATED" -> CLUSTER_TERMINATED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration InstanceGroupStateChangeReasonCode"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"InstanceGroupStateChangeReasonCode" j)
    let to_json = simple_to_json to_value
  end
module InstanceGroupStateChangeReason =
  struct
    type nonrec t =
      {
      code: InstanceGroupStateChangeReasonCode.t option ;
      message: String_.t option }
    let make ?code = fun ?message -> fun () -> { code; message }
    let to_value x =
      structure_to_value
        [("Code",
           (Option.map x.code ~f:InstanceGroupStateChangeReasonCode.to_value));
        ("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      let code =
        (Option.map ~f:InstanceGroupStateChangeReasonCode.of_xml)
          (Xml.child xml_arg0 "Code") in
      make ?message ?code ()
    let of_json json =
      let message = field_map json "Message" String_.of_json in
      let code =
        field_map json "Code" InstanceGroupStateChangeReasonCode.of_json in
      make ?message ?code ()
    let to_json v = composed_to_json to_value v
  end
module InstanceGroupStatus =
  struct
    type nonrec t =
      {
      state: InstanceGroupState.t option ;
      stateChangeReason: InstanceGroupStateChangeReason.t option ;
      timeline: InstanceGroupTimeline.t option }
    let make ?state =
      fun ?stateChangeReason ->
        fun ?timeline -> fun () -> { state; stateChangeReason; timeline }
    let to_value x =
      structure_to_value
        [("State", (Option.map x.state ~f:InstanceGroupState.to_value));
        ("StateChangeReason",
          (Option.map x.stateChangeReason
             ~f:InstanceGroupStateChangeReason.to_value));
        ("Timeline",
          (Option.map x.timeline ~f:InstanceGroupTimeline.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let timeline =
        (Option.map ~f:InstanceGroupTimeline.of_xml)
          (Xml.child xml_arg0 "Timeline") in
      let stateChangeReason =
        (Option.map ~f:InstanceGroupStateChangeReason.of_xml)
          (Xml.child xml_arg0 "StateChangeReason") in
      let state =
        (Option.map ~f:InstanceGroupState.of_xml)
          (Xml.child xml_arg0 "State") in
      make ?timeline ?stateChangeReason ?state ()
    let of_json json =
      let timeline = field_map json "Timeline" InstanceGroupTimeline.of_json in
      let stateChangeReason =
        field_map json "StateChangeReason"
          InstanceGroupStateChangeReason.of_json in
      let state = field_map json "State" InstanceGroupState.of_json in
      make ?timeline ?stateChangeReason ?state ()
    let to_json v = composed_to_json to_value v
  end
module InstanceGroupId =
  struct
    type nonrec t = string
    let context_ = "InstanceGroupId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"InstanceGroupId" j
    let to_json = simple_to_json to_value
  end
module InstanceGroup =
  struct
    type nonrec t =
      {
      id: InstanceGroupId.t option ;
      name: String_.t option ;
      market: MarketType.t option ;
      instanceGroupType: InstanceGroupType.t option ;
      bidPrice: String_.t option ;
      instanceType: InstanceType.t option ;
      requestedInstanceCount: Integer.t option ;
      runningInstanceCount: Integer.t option ;
      status: InstanceGroupStatus.t option ;
      configurations: ConfigurationList.t option ;
      configurationsVersion: Long.t option ;
      lastSuccessfullyAppliedConfigurations: ConfigurationList.t option ;
      lastSuccessfullyAppliedConfigurationsVersion: Long.t option ;
      ebsBlockDevices: EbsBlockDeviceList.t option ;
      ebsOptimized: BooleanObject.t option ;
      shrinkPolicy: ShrinkPolicy.t option ;
      autoScalingPolicy: AutoScalingPolicyDescription.t option ;
      customAmiId: XmlStringMaxLen256.t option }
    let make ?id =
      fun ?name ->
        fun ?market ->
          fun ?instanceGroupType ->
            fun ?bidPrice ->
              fun ?instanceType ->
                fun ?requestedInstanceCount ->
                  fun ?runningInstanceCount ->
                    fun ?status ->
                      fun ?configurations ->
                        fun ?configurationsVersion ->
                          fun ?lastSuccessfullyAppliedConfigurations ->
                            fun ?lastSuccessfullyAppliedConfigurationsVersion
                              ->
                              fun ?ebsBlockDevices ->
                                fun ?ebsOptimized ->
                                  fun ?shrinkPolicy ->
                                    fun ?autoScalingPolicy ->
                                      fun ?customAmiId ->
                                        fun () ->
                                          {
                                            id;
                                            name;
                                            market;
                                            instanceGroupType;
                                            bidPrice;
                                            instanceType;
                                            requestedInstanceCount;
                                            runningInstanceCount;
                                            status;
                                            configurations;
                                            configurationsVersion;
                                            lastSuccessfullyAppliedConfigurations;
                                            lastSuccessfullyAppliedConfigurationsVersion;
                                            ebsBlockDevices;
                                            ebsOptimized;
                                            shrinkPolicy;
                                            autoScalingPolicy;
                                            customAmiId
                                          }
    let to_value x =
      structure_to_value
        [("Id", (Option.map x.id ~f:InstanceGroupId.to_value));
        ("Name", (Option.map x.name ~f:String_.to_value));
        ("Market", (Option.map x.market ~f:MarketType.to_value));
        ("InstanceGroupType",
          (Option.map x.instanceGroupType ~f:InstanceGroupType.to_value));
        ("BidPrice", (Option.map x.bidPrice ~f:String_.to_value));
        ("InstanceType",
          (Option.map x.instanceType ~f:InstanceType.to_value));
        ("RequestedInstanceCount",
          (Option.map x.requestedInstanceCount ~f:Integer.to_value));
        ("RunningInstanceCount",
          (Option.map x.runningInstanceCount ~f:Integer.to_value));
        ("Status", (Option.map x.status ~f:InstanceGroupStatus.to_value));
        ("Configurations",
          (Option.map x.configurations ~f:ConfigurationList.to_value));
        ("ConfigurationsVersion",
          (Option.map x.configurationsVersion ~f:Long.to_value));
        ("LastSuccessfullyAppliedConfigurations",
          (Option.map x.lastSuccessfullyAppliedConfigurations
             ~f:ConfigurationList.to_value));
        ("LastSuccessfullyAppliedConfigurationsVersion",
          (Option.map x.lastSuccessfullyAppliedConfigurationsVersion
             ~f:Long.to_value));
        ("EbsBlockDevices",
          (Option.map x.ebsBlockDevices ~f:EbsBlockDeviceList.to_value));
        ("EbsOptimized",
          (Option.map x.ebsOptimized ~f:BooleanObject.to_value));
        ("ShrinkPolicy",
          (Option.map x.shrinkPolicy ~f:ShrinkPolicy.to_value));
        ("AutoScalingPolicy",
          (Option.map x.autoScalingPolicy
             ~f:AutoScalingPolicyDescription.to_value));
        ("CustomAmiId",
          (Option.map x.customAmiId ~f:XmlStringMaxLen256.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let customAmiId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "CustomAmiId") in
      let autoScalingPolicy =
        (Option.map ~f:AutoScalingPolicyDescription.of_xml)
          (Xml.child xml_arg0 "AutoScalingPolicy") in
      let shrinkPolicy =
        (Option.map ~f:ShrinkPolicy.of_xml)
          (Xml.child xml_arg0 "ShrinkPolicy") in
      let ebsOptimized =
        (Option.map ~f:BooleanObject.of_xml)
          (Xml.child xml_arg0 "EbsOptimized") in
      let ebsBlockDevices =
        (Option.map ~f:EbsBlockDeviceList.of_xml)
          (Xml.child xml_arg0 "EbsBlockDevices") in
      let lastSuccessfullyAppliedConfigurationsVersion =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "LastSuccessfullyAppliedConfigurationsVersion") in
      let lastSuccessfullyAppliedConfigurations =
        (Option.map ~f:ConfigurationList.of_xml)
          (Xml.child xml_arg0 "LastSuccessfullyAppliedConfigurations") in
      let configurationsVersion =
        (Option.map ~f:Long.of_xml)
          (Xml.child xml_arg0 "ConfigurationsVersion") in
      let configurations =
        (Option.map ~f:ConfigurationList.of_xml)
          (Xml.child xml_arg0 "Configurations") in
      let status =
        (Option.map ~f:InstanceGroupStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let runningInstanceCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "RunningInstanceCount") in
      let requestedInstanceCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "RequestedInstanceCount") in
      let instanceType =
        (Option.map ~f:InstanceType.of_xml)
          (Xml.child xml_arg0 "InstanceType") in
      let bidPrice =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "BidPrice") in
      let instanceGroupType =
        (Option.map ~f:InstanceGroupType.of_xml)
          (Xml.child xml_arg0 "InstanceGroupType") in
      let market =
        (Option.map ~f:MarketType.of_xml) (Xml.child xml_arg0 "Market") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      let id =
        (Option.map ~f:InstanceGroupId.of_xml) (Xml.child xml_arg0 "Id") in
      make ?customAmiId ?autoScalingPolicy ?shrinkPolicy ?ebsOptimized
        ?ebsBlockDevices ?lastSuccessfullyAppliedConfigurationsVersion
        ?lastSuccessfullyAppliedConfigurations ?configurationsVersion
        ?configurations ?status ?runningInstanceCount ?requestedInstanceCount
        ?instanceType ?bidPrice ?instanceGroupType ?market ?name ?id ()
    let of_json json =
      let customAmiId =
        field_map json "CustomAmiId" XmlStringMaxLen256.of_json in
      let autoScalingPolicy =
        field_map json "AutoScalingPolicy"
          AutoScalingPolicyDescription.of_json in
      let shrinkPolicy = field_map json "ShrinkPolicy" ShrinkPolicy.of_json in
      let ebsOptimized = field_map json "EbsOptimized" BooleanObject.of_json in
      let ebsBlockDevices =
        field_map json "EbsBlockDevices" EbsBlockDeviceList.of_json in
      let lastSuccessfullyAppliedConfigurationsVersion =
        field_map json "LastSuccessfullyAppliedConfigurationsVersion"
          Long.of_json in
      let lastSuccessfullyAppliedConfigurations =
        field_map json "LastSuccessfullyAppliedConfigurations"
          ConfigurationList.of_json in
      let configurationsVersion =
        field_map json "ConfigurationsVersion" Long.of_json in
      let configurations =
        field_map json "Configurations" ConfigurationList.of_json in
      let status = field_map json "Status" InstanceGroupStatus.of_json in
      let runningInstanceCount =
        field_map json "RunningInstanceCount" Integer.of_json in
      let requestedInstanceCount =
        field_map json "RequestedInstanceCount" Integer.of_json in
      let instanceType = field_map json "InstanceType" InstanceType.of_json in
      let bidPrice = field_map json "BidPrice" String_.of_json in
      let instanceGroupType =
        field_map json "InstanceGroupType" InstanceGroupType.of_json in
      let market = field_map json "Market" MarketType.of_json in
      let name = field_map json "Name" String_.of_json in
      let id = field_map json "Id" InstanceGroupId.of_json in
      make ?customAmiId ?autoScalingPolicy ?shrinkPolicy ?ebsOptimized
        ?ebsBlockDevices ?lastSuccessfullyAppliedConfigurationsVersion
        ?lastSuccessfullyAppliedConfigurations ?configurationsVersion
        ?configurations ?status ?runningInstanceCount ?requestedInstanceCount
        ?instanceType ?bidPrice ?instanceGroupType ?market ?name ?id ()
    let to_json v = composed_to_json to_value v
  end
module InstanceGroupList =
  struct
    type nonrec t = InstanceGroup.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:InstanceGroup.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InstanceGroup.of_xml)
    let of_json j =
      list_of_json ~kind:"InstanceGroupList" ~of_json:InstanceGroup.of_json j
    let to_json v = composed_to_json to_value v
  end
module InstanceGroupModifyConfig =
  struct
    type nonrec t =
      {
      instanceGroupId: XmlStringMaxLen256.t ;
      instanceCount: Integer.t option ;
      eC2InstanceIdsToTerminate: EC2InstanceIdsToTerminateList.t option ;
      shrinkPolicy: ShrinkPolicy.t option ;
      configurations: ConfigurationList.t option }
    let context_ = "InstanceGroupModifyConfig"
    let make ?instanceCount =
      fun ?eC2InstanceIdsToTerminate ->
        fun ?shrinkPolicy ->
          fun ?configurations ->
            fun ~instanceGroupId ->
              fun () ->
                {
                  instanceCount;
                  eC2InstanceIdsToTerminate;
                  shrinkPolicy;
                  configurations;
                  instanceGroupId
                }
    let to_value x =
      structure_to_value
        [("InstanceGroupId",
           (Some (XmlStringMaxLen256.to_value x.instanceGroupId)));
        ("InstanceCount", (Option.map x.instanceCount ~f:Integer.to_value));
        ("EC2InstanceIdsToTerminate",
          (Option.map x.eC2InstanceIdsToTerminate
             ~f:EC2InstanceIdsToTerminateList.to_value));
        ("ShrinkPolicy",
          (Option.map x.shrinkPolicy ~f:ShrinkPolicy.to_value));
        ("Configurations",
          (Option.map x.configurations ~f:ConfigurationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configurations =
        (Option.map ~f:ConfigurationList.of_xml)
          (Xml.child xml_arg0 "Configurations") in
      let shrinkPolicy =
        (Option.map ~f:ShrinkPolicy.of_xml)
          (Xml.child xml_arg0 "ShrinkPolicy") in
      let eC2InstanceIdsToTerminate =
        (Option.map ~f:EC2InstanceIdsToTerminateList.of_xml)
          (Xml.child xml_arg0 "EC2InstanceIdsToTerminate") in
      let instanceCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "InstanceCount") in
      let instanceGroupId =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceGroupId") in
      make ?configurations ?shrinkPolicy ?eC2InstanceIdsToTerminate
        ?instanceCount ~instanceGroupId ()
    let of_json json =
      let configurations =
        field_map json "Configurations" ConfigurationList.of_json in
      let shrinkPolicy = field_map json "ShrinkPolicy" ShrinkPolicy.of_json in
      let eC2InstanceIdsToTerminate =
        field_map json "EC2InstanceIdsToTerminate"
          EC2InstanceIdsToTerminateList.of_json in
      let instanceCount = field_map json "InstanceCount" Integer.of_json in
      let instanceGroupId =
        field_map_exn json "InstanceGroupId" XmlStringMaxLen256.of_json in
      make ?configurations ?shrinkPolicy ?eC2InstanceIdsToTerminate
        ?instanceCount ~instanceGroupId ()
    let to_json v = composed_to_json to_value v
  end
module InstanceGroupModifyConfigList =
  struct
    type nonrec t = InstanceGroupModifyConfig.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:InstanceGroupModifyConfig.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InstanceGroupModifyConfig.of_xml)
    let of_json j =
      list_of_json ~kind:"InstanceGroupModifyConfigList"
        ~of_json:InstanceGroupModifyConfig.of_json j
    let to_json v = composed_to_json to_value v
  end
module InstanceGroupTypeList =
  struct
    type nonrec t = InstanceGroupType.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:InstanceGroupType.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InstanceGroupType.of_xml)
    let of_json j =
      list_of_json ~kind:"InstanceGroupTypeList"
        ~of_json:InstanceGroupType.of_json j
    let to_json v = composed_to_json to_value v
  end
module InstanceList =
  struct
    type nonrec t = Instance.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Instance.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Instance.of_xml)
    let of_json j =
      list_of_json ~kind:"InstanceList" ~of_json:Instance.of_json j
    let to_json v = composed_to_json to_value v
  end
module InstanceStateList =
  struct
    type nonrec t = InstanceState.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:InstanceState.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InstanceState.of_xml)
    let of_json j =
      list_of_json ~kind:"InstanceStateList" ~of_json:InstanceState.of_json j
    let to_json v = composed_to_json to_value v
  end
module SecurityGroupsList =
  struct
    type nonrec t = XmlStringMaxLen256.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:XmlStringMaxLen256.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:XmlStringMaxLen256.of_xml)
    let of_json j =
      list_of_json ~kind:"SecurityGroupsList"
        ~of_json:XmlStringMaxLen256.of_json j
    let to_json v = composed_to_json to_value v
  end
module JobFlowInstancesConfig =
  struct
    type nonrec t =
      {
      masterInstanceType: InstanceType.t option ;
      slaveInstanceType: InstanceType.t option ;
      instanceCount: Integer.t option ;
      instanceGroups: InstanceGroupConfigList.t option ;
      instanceFleets: InstanceFleetConfigList.t option ;
      ec2KeyName: XmlStringMaxLen256.t option ;
      placement: PlacementType.t option ;
      keepJobFlowAliveWhenNoSteps: Boolean.t option ;
      terminationProtected: Boolean.t option ;
      hadoopVersion: XmlStringMaxLen256.t option ;
      ec2SubnetId: XmlStringMaxLen256.t option ;
      ec2SubnetIds: XmlStringMaxLen256List.t option ;
      emrManagedMasterSecurityGroup: XmlStringMaxLen256.t option ;
      emrManagedSlaveSecurityGroup: XmlStringMaxLen256.t option ;
      serviceAccessSecurityGroup: XmlStringMaxLen256.t option ;
      additionalMasterSecurityGroups: SecurityGroupsList.t option ;
      additionalSlaveSecurityGroups: SecurityGroupsList.t option }
    let make ?masterInstanceType =
      fun ?slaveInstanceType ->
        fun ?instanceCount ->
          fun ?instanceGroups ->
            fun ?instanceFleets ->
              fun ?ec2KeyName ->
                fun ?placement ->
                  fun ?keepJobFlowAliveWhenNoSteps ->
                    fun ?terminationProtected ->
                      fun ?hadoopVersion ->
                        fun ?ec2SubnetId ->
                          fun ?ec2SubnetIds ->
                            fun ?emrManagedMasterSecurityGroup ->
                              fun ?emrManagedSlaveSecurityGroup ->
                                fun ?serviceAccessSecurityGroup ->
                                  fun ?additionalMasterSecurityGroups ->
                                    fun ?additionalSlaveSecurityGroups ->
                                      fun () ->
                                        {
                                          masterInstanceType;
                                          slaveInstanceType;
                                          instanceCount;
                                          instanceGroups;
                                          instanceFleets;
                                          ec2KeyName;
                                          placement;
                                          keepJobFlowAliveWhenNoSteps;
                                          terminationProtected;
                                          hadoopVersion;
                                          ec2SubnetId;
                                          ec2SubnetIds;
                                          emrManagedMasterSecurityGroup;
                                          emrManagedSlaveSecurityGroup;
                                          serviceAccessSecurityGroup;
                                          additionalMasterSecurityGroups;
                                          additionalSlaveSecurityGroups
                                        }
    let to_value x =
      structure_to_value
        [("MasterInstanceType",
           (Option.map x.masterInstanceType ~f:InstanceType.to_value));
        ("SlaveInstanceType",
          (Option.map x.slaveInstanceType ~f:InstanceType.to_value));
        ("InstanceCount", (Option.map x.instanceCount ~f:Integer.to_value));
        ("InstanceGroups",
          (Option.map x.instanceGroups ~f:InstanceGroupConfigList.to_value));
        ("InstanceFleets",
          (Option.map x.instanceFleets ~f:InstanceFleetConfigList.to_value));
        ("Ec2KeyName",
          (Option.map x.ec2KeyName ~f:XmlStringMaxLen256.to_value));
        ("Placement", (Option.map x.placement ~f:PlacementType.to_value));
        ("KeepJobFlowAliveWhenNoSteps",
          (Option.map x.keepJobFlowAliveWhenNoSteps ~f:Boolean.to_value));
        ("TerminationProtected",
          (Option.map x.terminationProtected ~f:Boolean.to_value));
        ("HadoopVersion",
          (Option.map x.hadoopVersion ~f:XmlStringMaxLen256.to_value));
        ("Ec2SubnetId",
          (Option.map x.ec2SubnetId ~f:XmlStringMaxLen256.to_value));
        ("Ec2SubnetIds",
          (Option.map x.ec2SubnetIds ~f:XmlStringMaxLen256List.to_value));
        ("EmrManagedMasterSecurityGroup",
          (Option.map x.emrManagedMasterSecurityGroup
             ~f:XmlStringMaxLen256.to_value));
        ("EmrManagedSlaveSecurityGroup",
          (Option.map x.emrManagedSlaveSecurityGroup
             ~f:XmlStringMaxLen256.to_value));
        ("ServiceAccessSecurityGroup",
          (Option.map x.serviceAccessSecurityGroup
             ~f:XmlStringMaxLen256.to_value));
        ("AdditionalMasterSecurityGroups",
          (Option.map x.additionalMasterSecurityGroups
             ~f:SecurityGroupsList.to_value));
        ("AdditionalSlaveSecurityGroups",
          (Option.map x.additionalSlaveSecurityGroups
             ~f:SecurityGroupsList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let additionalSlaveSecurityGroups =
        (Option.map ~f:SecurityGroupsList.of_xml)
          (Xml.child xml_arg0 "AdditionalSlaveSecurityGroups") in
      let additionalMasterSecurityGroups =
        (Option.map ~f:SecurityGroupsList.of_xml)
          (Xml.child xml_arg0 "AdditionalMasterSecurityGroups") in
      let serviceAccessSecurityGroup =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "ServiceAccessSecurityGroup") in
      let emrManagedSlaveSecurityGroup =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "EmrManagedSlaveSecurityGroup") in
      let emrManagedMasterSecurityGroup =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "EmrManagedMasterSecurityGroup") in
      let ec2SubnetIds =
        (Option.map ~f:XmlStringMaxLen256List.of_xml)
          (Xml.child xml_arg0 "Ec2SubnetIds") in
      let ec2SubnetId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "Ec2SubnetId") in
      let hadoopVersion =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "HadoopVersion") in
      let terminationProtected =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "TerminationProtected") in
      let keepJobFlowAliveWhenNoSteps =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "KeepJobFlowAliveWhenNoSteps") in
      let placement =
        (Option.map ~f:PlacementType.of_xml) (Xml.child xml_arg0 "Placement") in
      let ec2KeyName =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "Ec2KeyName") in
      let instanceFleets =
        (Option.map ~f:InstanceFleetConfigList.of_xml)
          (Xml.child xml_arg0 "InstanceFleets") in
      let instanceGroups =
        (Option.map ~f:InstanceGroupConfigList.of_xml)
          (Xml.child xml_arg0 "InstanceGroups") in
      let instanceCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "InstanceCount") in
      let slaveInstanceType =
        (Option.map ~f:InstanceType.of_xml)
          (Xml.child xml_arg0 "SlaveInstanceType") in
      let masterInstanceType =
        (Option.map ~f:InstanceType.of_xml)
          (Xml.child xml_arg0 "MasterInstanceType") in
      make ?additionalSlaveSecurityGroups ?additionalMasterSecurityGroups
        ?serviceAccessSecurityGroup ?emrManagedSlaveSecurityGroup
        ?emrManagedMasterSecurityGroup ?ec2SubnetIds ?ec2SubnetId
        ?hadoopVersion ?terminationProtected ?keepJobFlowAliveWhenNoSteps
        ?placement ?ec2KeyName ?instanceFleets ?instanceGroups ?instanceCount
        ?slaveInstanceType ?masterInstanceType ()
    let of_json json =
      let additionalSlaveSecurityGroups =
        field_map json "AdditionalSlaveSecurityGroups"
          SecurityGroupsList.of_json in
      let additionalMasterSecurityGroups =
        field_map json "AdditionalMasterSecurityGroups"
          SecurityGroupsList.of_json in
      let serviceAccessSecurityGroup =
        field_map json "ServiceAccessSecurityGroup"
          XmlStringMaxLen256.of_json in
      let emrManagedSlaveSecurityGroup =
        field_map json "EmrManagedSlaveSecurityGroup"
          XmlStringMaxLen256.of_json in
      let emrManagedMasterSecurityGroup =
        field_map json "EmrManagedMasterSecurityGroup"
          XmlStringMaxLen256.of_json in
      let ec2SubnetIds =
        field_map json "Ec2SubnetIds" XmlStringMaxLen256List.of_json in
      let ec2SubnetId =
        field_map json "Ec2SubnetId" XmlStringMaxLen256.of_json in
      let hadoopVersion =
        field_map json "HadoopVersion" XmlStringMaxLen256.of_json in
      let terminationProtected =
        field_map json "TerminationProtected" Boolean.of_json in
      let keepJobFlowAliveWhenNoSteps =
        field_map json "KeepJobFlowAliveWhenNoSteps" Boolean.of_json in
      let placement = field_map json "Placement" PlacementType.of_json in
      let ec2KeyName = field_map json "Ec2KeyName" XmlStringMaxLen256.of_json in
      let instanceFleets =
        field_map json "InstanceFleets" InstanceFleetConfigList.of_json in
      let instanceGroups =
        field_map json "InstanceGroups" InstanceGroupConfigList.of_json in
      let instanceCount = field_map json "InstanceCount" Integer.of_json in
      let slaveInstanceType =
        field_map json "SlaveInstanceType" InstanceType.of_json in
      let masterInstanceType =
        field_map json "MasterInstanceType" InstanceType.of_json in
      make ?additionalSlaveSecurityGroups ?additionalMasterSecurityGroups
        ?serviceAccessSecurityGroup ?emrManagedSlaveSecurityGroup
        ?emrManagedMasterSecurityGroup ?ec2SubnetIds ?ec2SubnetId
        ?hadoopVersion ?terminationProtected ?keepJobFlowAliveWhenNoSteps
        ?placement ?ec2KeyName ?instanceFleets ?instanceGroups ?instanceCount
        ?slaveInstanceType ?masterInstanceType ()
    let to_json v = composed_to_json to_value v
  end
module Marker =
  struct
    type nonrec t = string
    let context_ = "Marker"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Marker" j
    let to_json = simple_to_json to_value
  end
module ListBootstrapActionsInput =
  struct
    type nonrec t = {
      clusterId: ClusterId.t ;
      marker: Marker.t option }
    let context_ = "ListBootstrapActionsInput"
    let make ?marker = fun ~clusterId -> fun () -> { marker; clusterId }
    let to_value x =
      structure_to_value
        [("ClusterId", (Some (ClusterId.to_value x.clusterId)));
        ("Marker", (Option.map x.marker ~f:Marker.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let marker =
        (Option.map ~f:Marker.of_xml) (Xml.child xml_arg0 "Marker") in
      let clusterId =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClusterId") in
      make ?marker ~clusterId ()
    let of_json json =
      let marker = field_map json "Marker" Marker.of_json in
      let clusterId = field_map_exn json "ClusterId" ClusterId.of_json in
      make ?marker ~clusterId ()
    let to_json v = composed_to_json to_value v
  end
module ListBootstrapActionsOutput =
  struct
    type nonrec t =
      {
      bootstrapActions: CommandList.t option ;
      marker: Marker.t option }
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?bootstrapActions =
      fun ?marker -> fun () -> { bootstrapActions; marker }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("BootstrapActions",
           (Option.map x.bootstrapActions ~f:CommandList.to_value));
        ("Marker", (Option.map x.marker ~f:Marker.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let marker =
        (Option.map ~f:Marker.of_xml) (Xml.child xml_arg0 "Marker") in
      let bootstrapActions =
        (Option.map ~f:CommandList.of_xml)
          (Xml.child xml_arg0 "BootstrapActions") in
      make ?marker ?bootstrapActions ()
    let of_json json =
      let marker = field_map json "Marker" Marker.of_json in
      let bootstrapActions =
        field_map json "BootstrapActions" CommandList.of_json in
      make ?marker ?bootstrapActions ()
    let to_json v = composed_to_json to_value v
  end
module ListClustersInput =
  struct
    type nonrec t =
      {
      createdAfter: Date.t option ;
      createdBefore: Date.t option ;
      clusterStates: ClusterStateList.t option ;
      marker: Marker.t option }
    let make ?createdAfter =
      fun ?createdBefore ->
        fun ?clusterStates ->
          fun ?marker ->
            fun () -> { createdAfter; createdBefore; clusterStates; marker }
    let to_value x =
      structure_to_value
        [("CreatedAfter", (Option.map x.createdAfter ~f:Date.to_value));
        ("CreatedBefore", (Option.map x.createdBefore ~f:Date.to_value));
        ("ClusterStates",
          (Option.map x.clusterStates ~f:ClusterStateList.to_value));
        ("Marker", (Option.map x.marker ~f:Marker.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let marker =
        (Option.map ~f:Marker.of_xml) (Xml.child xml_arg0 "Marker") in
      let clusterStates =
        (Option.map ~f:ClusterStateList.of_xml)
          (Xml.child xml_arg0 "ClusterStates") in
      let createdBefore =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "CreatedBefore") in
      let createdAfter =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "CreatedAfter") in
      make ?marker ?clusterStates ?createdBefore ?createdAfter ()
    let of_json json =
      let marker = field_map json "Marker" Marker.of_json in
      let clusterStates =
        field_map json "ClusterStates" ClusterStateList.of_json in
      let createdBefore = field_map json "CreatedBefore" Date.of_json in
      let createdAfter = field_map json "CreatedAfter" Date.of_json in
      make ?marker ?clusterStates ?createdBefore ?createdAfter ()
    let to_json v = composed_to_json to_value v
  end
module ListClustersOutput =
  struct
    type nonrec t =
      {
      clusters: ClusterSummaryList.t option ;
      marker: Marker.t option }
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?clusters = fun ?marker -> fun () -> { clusters; marker }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Clusters", (Option.map x.clusters ~f:ClusterSummaryList.to_value));
        ("Marker", (Option.map x.marker ~f:Marker.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let marker =
        (Option.map ~f:Marker.of_xml) (Xml.child xml_arg0 "Marker") in
      let clusters =
        (Option.map ~f:ClusterSummaryList.of_xml)
          (Xml.child xml_arg0 "Clusters") in
      make ?marker ?clusters ()
    let of_json json =
      let marker = field_map json "Marker" Marker.of_json in
      let clusters = field_map json "Clusters" ClusterSummaryList.of_json in
      make ?marker ?clusters ()
    let to_json v = composed_to_json to_value v
  end
module ListInstanceFleetsInput =
  struct
    type nonrec t = {
      clusterId: ClusterId.t ;
      marker: Marker.t option }
    let context_ = "ListInstanceFleetsInput"
    let make ?marker = fun ~clusterId -> fun () -> { marker; clusterId }
    let to_value x =
      structure_to_value
        [("ClusterId", (Some (ClusterId.to_value x.clusterId)));
        ("Marker", (Option.map x.marker ~f:Marker.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let marker =
        (Option.map ~f:Marker.of_xml) (Xml.child xml_arg0 "Marker") in
      let clusterId =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClusterId") in
      make ?marker ~clusterId ()
    let of_json json =
      let marker = field_map json "Marker" Marker.of_json in
      let clusterId = field_map_exn json "ClusterId" ClusterId.of_json in
      make ?marker ~clusterId ()
    let to_json v = composed_to_json to_value v
  end
module ListInstanceFleetsOutput =
  struct
    type nonrec t =
      {
      instanceFleets: InstanceFleetList.t option ;
      marker: Marker.t option }
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?instanceFleets =
      fun ?marker -> fun () -> { instanceFleets; marker }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("InstanceFleets",
           (Option.map x.instanceFleets ~f:InstanceFleetList.to_value));
        ("Marker", (Option.map x.marker ~f:Marker.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let marker =
        (Option.map ~f:Marker.of_xml) (Xml.child xml_arg0 "Marker") in
      let instanceFleets =
        (Option.map ~f:InstanceFleetList.of_xml)
          (Xml.child xml_arg0 "InstanceFleets") in
      make ?marker ?instanceFleets ()
    let of_json json =
      let marker = field_map json "Marker" Marker.of_json in
      let instanceFleets =
        field_map json "InstanceFleets" InstanceFleetList.of_json in
      make ?marker ?instanceFleets ()
    let to_json v = composed_to_json to_value v
  end
module ListInstanceGroupsInput =
  struct
    type nonrec t = {
      clusterId: ClusterId.t ;
      marker: Marker.t option }
    let context_ = "ListInstanceGroupsInput"
    let make ?marker = fun ~clusterId -> fun () -> { marker; clusterId }
    let to_value x =
      structure_to_value
        [("ClusterId", (Some (ClusterId.to_value x.clusterId)));
        ("Marker", (Option.map x.marker ~f:Marker.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let marker =
        (Option.map ~f:Marker.of_xml) (Xml.child xml_arg0 "Marker") in
      let clusterId =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClusterId") in
      make ?marker ~clusterId ()
    let of_json json =
      let marker = field_map json "Marker" Marker.of_json in
      let clusterId = field_map_exn json "ClusterId" ClusterId.of_json in
      make ?marker ~clusterId ()
    let to_json v = composed_to_json to_value v
  end
module ListInstanceGroupsOutput =
  struct
    type nonrec t =
      {
      instanceGroups: InstanceGroupList.t option ;
      marker: Marker.t option }
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?instanceGroups =
      fun ?marker -> fun () -> { instanceGroups; marker }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("InstanceGroups",
           (Option.map x.instanceGroups ~f:InstanceGroupList.to_value));
        ("Marker", (Option.map x.marker ~f:Marker.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let marker =
        (Option.map ~f:Marker.of_xml) (Xml.child xml_arg0 "Marker") in
      let instanceGroups =
        (Option.map ~f:InstanceGroupList.of_xml)
          (Xml.child xml_arg0 "InstanceGroups") in
      make ?marker ?instanceGroups ()
    let of_json json =
      let marker = field_map json "Marker" Marker.of_json in
      let instanceGroups =
        field_map json "InstanceGroups" InstanceGroupList.of_json in
      make ?marker ?instanceGroups ()
    let to_json v = composed_to_json to_value v
  end
module ListInstancesInput =
  struct
    type nonrec t =
      {
      clusterId: ClusterId.t ;
      instanceGroupId: InstanceGroupId.t option ;
      instanceGroupTypes: InstanceGroupTypeList.t option ;
      instanceFleetId: InstanceFleetId.t option ;
      instanceFleetType: InstanceFleetType.t option ;
      instanceStates: InstanceStateList.t option ;
      marker: Marker.t option }
    let context_ = "ListInstancesInput"
    let make ?instanceGroupId =
      fun ?instanceGroupTypes ->
        fun ?instanceFleetId ->
          fun ?instanceFleetType ->
            fun ?instanceStates ->
              fun ?marker ->
                fun ~clusterId ->
                  fun () ->
                    {
                      instanceGroupId;
                      instanceGroupTypes;
                      instanceFleetId;
                      instanceFleetType;
                      instanceStates;
                      marker;
                      clusterId
                    }
    let to_value x =
      structure_to_value
        [("ClusterId", (Some (ClusterId.to_value x.clusterId)));
        ("InstanceGroupId",
          (Option.map x.instanceGroupId ~f:InstanceGroupId.to_value));
        ("InstanceGroupTypes",
          (Option.map x.instanceGroupTypes ~f:InstanceGroupTypeList.to_value));
        ("InstanceFleetId",
          (Option.map x.instanceFleetId ~f:InstanceFleetId.to_value));
        ("InstanceFleetType",
          (Option.map x.instanceFleetType ~f:InstanceFleetType.to_value));
        ("InstanceStates",
          (Option.map x.instanceStates ~f:InstanceStateList.to_value));
        ("Marker", (Option.map x.marker ~f:Marker.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let marker =
        (Option.map ~f:Marker.of_xml) (Xml.child xml_arg0 "Marker") in
      let instanceStates =
        (Option.map ~f:InstanceStateList.of_xml)
          (Xml.child xml_arg0 "InstanceStates") in
      let instanceFleetType =
        (Option.map ~f:InstanceFleetType.of_xml)
          (Xml.child xml_arg0 "InstanceFleetType") in
      let instanceFleetId =
        (Option.map ~f:InstanceFleetId.of_xml)
          (Xml.child xml_arg0 "InstanceFleetId") in
      let instanceGroupTypes =
        (Option.map ~f:InstanceGroupTypeList.of_xml)
          (Xml.child xml_arg0 "InstanceGroupTypes") in
      let instanceGroupId =
        (Option.map ~f:InstanceGroupId.of_xml)
          (Xml.child xml_arg0 "InstanceGroupId") in
      let clusterId =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClusterId") in
      make ?marker ?instanceStates ?instanceFleetType ?instanceFleetId
        ?instanceGroupTypes ?instanceGroupId ~clusterId ()
    let of_json json =
      let marker = field_map json "Marker" Marker.of_json in
      let instanceStates =
        field_map json "InstanceStates" InstanceStateList.of_json in
      let instanceFleetType =
        field_map json "InstanceFleetType" InstanceFleetType.of_json in
      let instanceFleetId =
        field_map json "InstanceFleetId" InstanceFleetId.of_json in
      let instanceGroupTypes =
        field_map json "InstanceGroupTypes" InstanceGroupTypeList.of_json in
      let instanceGroupId =
        field_map json "InstanceGroupId" InstanceGroupId.of_json in
      let clusterId = field_map_exn json "ClusterId" ClusterId.of_json in
      make ?marker ?instanceStates ?instanceFleetType ?instanceFleetId
        ?instanceGroupTypes ?instanceGroupId ~clusterId ()
    let to_json v = composed_to_json to_value v
  end
module ListInstancesOutput =
  struct
    type nonrec t =
      {
      instances: InstanceList.t option ;
      marker: Marker.t option }
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?instances = fun ?marker -> fun () -> { instances; marker }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Instances", (Option.map x.instances ~f:InstanceList.to_value));
        ("Marker", (Option.map x.marker ~f:Marker.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let marker =
        (Option.map ~f:Marker.of_xml) (Xml.child xml_arg0 "Marker") in
      let instances =
        (Option.map ~f:InstanceList.of_xml) (Xml.child xml_arg0 "Instances") in
      make ?marker ?instances ()
    let of_json json =
      let marker = field_map json "Marker" Marker.of_json in
      let instances = field_map json "Instances" InstanceList.of_json in
      make ?marker ?instances ()
    let to_json v = composed_to_json to_value v
  end
module ListNotebookExecutionsInput =
  struct
    type nonrec t =
      {
      editorId: XmlStringMaxLen256.t option ;
      status: NotebookExecutionStatus.t option ;
      from: Date.t option ;
      to_: Date.t option ;
      marker: Marker.t option }
    let make ?editorId =
      fun ?status ->
        fun ?from ->
          fun ?to_ ->
            fun ?marker -> fun () -> { editorId; status; from; to_; marker }
    let to_value x =
      structure_to_value
        [("EditorId", (Option.map x.editorId ~f:XmlStringMaxLen256.to_value));
        ("Status", (Option.map x.status ~f:NotebookExecutionStatus.to_value));
        ("From", (Option.map x.from ~f:Date.to_value));
        ("To", (Option.map x.to_ ~f:Date.to_value));
        ("Marker", (Option.map x.marker ~f:Marker.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let marker =
        (Option.map ~f:Marker.of_xml) (Xml.child xml_arg0 "Marker") in
      let to_ = (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "To") in
      let from = (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "From") in
      let status =
        (Option.map ~f:NotebookExecutionStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let editorId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "EditorId") in
      make ?marker ?to_ ?from ?status ?editorId ()
    let of_json json =
      let marker = field_map json "Marker" Marker.of_json in
      let to_ = field_map json "To" Date.of_json in
      let from = field_map json "From" Date.of_json in
      let status = field_map json "Status" NotebookExecutionStatus.of_json in
      let editorId = field_map json "EditorId" XmlStringMaxLen256.of_json in
      make ?marker ?to_ ?from ?status ?editorId ()
    let to_json v = composed_to_json to_value v
  end
module NotebookExecutionSummary =
  struct
    type nonrec t =
      {
      notebookExecutionId: XmlStringMaxLen256.t option ;
      editorId: XmlStringMaxLen256.t option ;
      notebookExecutionName: XmlStringMaxLen256.t option ;
      status: NotebookExecutionStatus.t option ;
      startTime: Date.t option ;
      endTime: Date.t option }
    let make ?notebookExecutionId =
      fun ?editorId ->
        fun ?notebookExecutionName ->
          fun ?status ->
            fun ?startTime ->
              fun ?endTime ->
                fun () ->
                  {
                    notebookExecutionId;
                    editorId;
                    notebookExecutionName;
                    status;
                    startTime;
                    endTime
                  }
    let to_value x =
      structure_to_value
        [("NotebookExecutionId",
           (Option.map x.notebookExecutionId ~f:XmlStringMaxLen256.to_value));
        ("EditorId", (Option.map x.editorId ~f:XmlStringMaxLen256.to_value));
        ("NotebookExecutionName",
          (Option.map x.notebookExecutionName ~f:XmlStringMaxLen256.to_value));
        ("Status", (Option.map x.status ~f:NotebookExecutionStatus.to_value));
        ("StartTime", (Option.map x.startTime ~f:Date.to_value));
        ("EndTime", (Option.map x.endTime ~f:Date.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let endTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "EndTime") in
      let startTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "StartTime") in
      let status =
        (Option.map ~f:NotebookExecutionStatus.of_xml)
          (Xml.child xml_arg0 "Status") in
      let notebookExecutionName =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "NotebookExecutionName") in
      let editorId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "EditorId") in
      let notebookExecutionId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "NotebookExecutionId") in
      make ?endTime ?startTime ?status ?notebookExecutionName ?editorId
        ?notebookExecutionId ()
    let of_json json =
      let endTime = field_map json "EndTime" Date.of_json in
      let startTime = field_map json "StartTime" Date.of_json in
      let status = field_map json "Status" NotebookExecutionStatus.of_json in
      let notebookExecutionName =
        field_map json "NotebookExecutionName" XmlStringMaxLen256.of_json in
      let editorId = field_map json "EditorId" XmlStringMaxLen256.of_json in
      let notebookExecutionId =
        field_map json "NotebookExecutionId" XmlStringMaxLen256.of_json in
      make ?endTime ?startTime ?status ?notebookExecutionName ?editorId
        ?notebookExecutionId ()
    let to_json v = composed_to_json to_value v
  end
module NotebookExecutionSummaryList =
  struct
    type nonrec t = NotebookExecutionSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:NotebookExecutionSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:NotebookExecutionSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"NotebookExecutionSummaryList"
        ~of_json:NotebookExecutionSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListNotebookExecutionsOutput =
  struct
    type nonrec t =
      {
      notebookExecutions: NotebookExecutionSummaryList.t option ;
      marker: Marker.t option }
    type nonrec error =
      [ `InternalServerError of InternalServerError.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?notebookExecutions =
      fun ?marker -> fun () -> { notebookExecutions; marker }
    let error_of_json name json =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("NotebookExecutions",
           (Option.map x.notebookExecutions
              ~f:NotebookExecutionSummaryList.to_value));
        ("Marker", (Option.map x.marker ~f:Marker.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let marker =
        (Option.map ~f:Marker.of_xml) (Xml.child xml_arg0 "Marker") in
      let notebookExecutions =
        (Option.map ~f:NotebookExecutionSummaryList.of_xml)
          (Xml.child xml_arg0 "NotebookExecutions") in
      make ?marker ?notebookExecutions ()
    let of_json json =
      let marker = field_map json "Marker" Marker.of_json in
      let notebookExecutions =
        field_map json "NotebookExecutions"
          NotebookExecutionSummaryList.of_json in
      make ?marker ?notebookExecutions ()
    let to_json v = composed_to_json to_value v
  end
module ReleaseLabelFilter =
  struct
    type nonrec t =
      {
      prefix: String_.t option ;
      application: String_.t option }
    let make ?prefix = fun ?application -> fun () -> { prefix; application }
    let to_value x =
      structure_to_value
        [("Prefix", (Option.map x.prefix ~f:String_.to_value));
        ("Application", (Option.map x.application ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let application =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Application") in
      let prefix =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Prefix") in
      make ?application ?prefix ()
    let of_json json =
      let application = field_map json "Application" String_.of_json in
      let prefix = field_map json "Prefix" String_.of_json in
      make ?application ?prefix ()
    let to_json v = composed_to_json to_value v
  end
module ListReleaseLabelsInput =
  struct
    type nonrec t =
      {
      filters: ReleaseLabelFilter.t option ;
      nextToken: String_.t option ;
      maxResults: MaxResultsNumber.t option }
    let make ?filters =
      fun ?nextToken ->
        fun ?maxResults -> fun () -> { filters; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("Filters", (Option.map x.filters ~f:ReleaseLabelFilter.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value));
        ("MaxResults",
          (Option.map x.maxResults ~f:MaxResultsNumber.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResultsNumber.of_xml)
          (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let filters =
        (Option.map ~f:ReleaseLabelFilter.of_xml)
          (Xml.child xml_arg0 "Filters") in
      make ?maxResults ?nextToken ?filters ()
    let of_json json =
      let maxResults = field_map json "MaxResults" MaxResultsNumber.of_json in
      let nextToken = field_map json "NextToken" String_.of_json in
      let filters = field_map json "Filters" ReleaseLabelFilter.of_json in
      make ?maxResults ?nextToken ?filters ()
    let to_json v = composed_to_json to_value v
  end
module ListReleaseLabelsOutput =
  struct
    type nonrec t =
      {
      releaseLabels: StringList.t option ;
      nextToken: String_.t option }
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?releaseLabels =
      fun ?nextToken -> fun () -> { releaseLabels; nextToken }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ReleaseLabels",
           (Option.map x.releaseLabels ~f:StringList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let releaseLabels =
        (Option.map ~f:StringList.of_xml)
          (Xml.child xml_arg0 "ReleaseLabels") in
      make ?nextToken ?releaseLabels ()
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let releaseLabels = field_map json "ReleaseLabels" StringList.of_json in
      make ?nextToken ?releaseLabels ()
    let to_json v = composed_to_json to_value v
  end
module ListSecurityConfigurationsInput =
  struct
    type nonrec t = {
      marker: Marker.t option }
    let make ?marker = fun () -> { marker }
    let to_value x =
      structure_to_value
        [("Marker", (Option.map x.marker ~f:Marker.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let marker =
        (Option.map ~f:Marker.of_xml) (Xml.child xml_arg0 "Marker") in
      make ?marker ()
    let of_json json =
      let marker = field_map json "Marker" Marker.of_json in make ?marker ()
    let to_json v = composed_to_json to_value v
  end
module SecurityConfigurationSummary =
  struct
    type nonrec t =
      {
      name: XmlString.t option ;
      creationDateTime: Date.t option }
    let make ?name =
      fun ?creationDateTime -> fun () -> { name; creationDateTime }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:XmlString.to_value));
        ("CreationDateTime",
          (Option.map x.creationDateTime ~f:Date.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creationDateTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "CreationDateTime") in
      let name = (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "Name") in
      make ?creationDateTime ?name ()
    let of_json json =
      let creationDateTime = field_map json "CreationDateTime" Date.of_json in
      let name = field_map json "Name" XmlString.of_json in
      make ?creationDateTime ?name ()
    let to_json v = composed_to_json to_value v
  end
module SecurityConfigurationList =
  struct
    type nonrec t = SecurityConfigurationSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SecurityConfigurationSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SecurityConfigurationSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"SecurityConfigurationList"
        ~of_json:SecurityConfigurationSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListSecurityConfigurationsOutput =
  struct
    type nonrec t =
      {
      securityConfigurations: SecurityConfigurationList.t option ;
      marker: Marker.t option }
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?securityConfigurations =
      fun ?marker -> fun () -> { securityConfigurations; marker }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("SecurityConfigurations",
           (Option.map x.securityConfigurations
              ~f:SecurityConfigurationList.to_value));
        ("Marker", (Option.map x.marker ~f:Marker.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let marker =
        (Option.map ~f:Marker.of_xml) (Xml.child xml_arg0 "Marker") in
      let securityConfigurations =
        (Option.map ~f:SecurityConfigurationList.of_xml)
          (Xml.child xml_arg0 "SecurityConfigurations") in
      make ?marker ?securityConfigurations ()
    let of_json json =
      let marker = field_map json "Marker" Marker.of_json in
      let securityConfigurations =
        field_map json "SecurityConfigurations"
          SecurityConfigurationList.of_json in
      make ?marker ?securityConfigurations ()
    let to_json v = composed_to_json to_value v
  end
module StepStateList =
  struct
    type nonrec t = StepState.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:StepState.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:StepState.of_xml)
    let of_json j =
      list_of_json ~kind:"StepStateList" ~of_json:StepState.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListStepsInput =
  struct
    type nonrec t =
      {
      clusterId: ClusterId.t ;
      stepStates: StepStateList.t option ;
      stepIds: XmlStringList.t option ;
      marker: Marker.t option }
    let context_ = "ListStepsInput"
    let make ?stepStates =
      fun ?stepIds ->
        fun ?marker ->
          fun ~clusterId ->
            fun () -> { stepStates; stepIds; marker; clusterId }
    let to_value x =
      structure_to_value
        [("ClusterId", (Some (ClusterId.to_value x.clusterId)));
        ("StepStates", (Option.map x.stepStates ~f:StepStateList.to_value));
        ("StepIds", (Option.map x.stepIds ~f:XmlStringList.to_value));
        ("Marker", (Option.map x.marker ~f:Marker.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let marker =
        (Option.map ~f:Marker.of_xml) (Xml.child xml_arg0 "Marker") in
      let stepIds =
        (Option.map ~f:XmlStringList.of_xml) (Xml.child xml_arg0 "StepIds") in
      let stepStates =
        (Option.map ~f:StepStateList.of_xml)
          (Xml.child xml_arg0 "StepStates") in
      let clusterId =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClusterId") in
      make ?marker ?stepIds ?stepStates ~clusterId ()
    let of_json json =
      let marker = field_map json "Marker" Marker.of_json in
      let stepIds = field_map json "StepIds" XmlStringList.of_json in
      let stepStates = field_map json "StepStates" StepStateList.of_json in
      let clusterId = field_map_exn json "ClusterId" ClusterId.of_json in
      make ?marker ?stepIds ?stepStates ~clusterId ()
    let to_json v = composed_to_json to_value v
  end
module StepSummary =
  struct
    type nonrec t =
      {
      id: StepId.t option ;
      name: String_.t option ;
      config: HadoopStepConfig.t option ;
      actionOnFailure: ActionOnFailure.t option ;
      status: StepStatus.t option }
    let make ?id =
      fun ?name ->
        fun ?config ->
          fun ?actionOnFailure ->
            fun ?status ->
              fun () -> { id; name; config; actionOnFailure; status }
    let to_value x =
      structure_to_value
        [("Id", (Option.map x.id ~f:StepId.to_value));
        ("Name", (Option.map x.name ~f:String_.to_value));
        ("Config", (Option.map x.config ~f:HadoopStepConfig.to_value));
        ("ActionOnFailure",
          (Option.map x.actionOnFailure ~f:ActionOnFailure.to_value));
        ("Status", (Option.map x.status ~f:StepStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:StepStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let actionOnFailure =
        (Option.map ~f:ActionOnFailure.of_xml)
          (Xml.child xml_arg0 "ActionOnFailure") in
      let config =
        (Option.map ~f:HadoopStepConfig.of_xml) (Xml.child xml_arg0 "Config") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      let id = (Option.map ~f:StepId.of_xml) (Xml.child xml_arg0 "Id") in
      make ?status ?actionOnFailure ?config ?name ?id ()
    let of_json json =
      let status = field_map json "Status" StepStatus.of_json in
      let actionOnFailure =
        field_map json "ActionOnFailure" ActionOnFailure.of_json in
      let config = field_map json "Config" HadoopStepConfig.of_json in
      let name = field_map json "Name" String_.of_json in
      let id = field_map json "Id" StepId.of_json in
      make ?status ?actionOnFailure ?config ?name ?id ()
    let to_json v = composed_to_json to_value v
  end
module StepSummaryList =
  struct
    type nonrec t = StepSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:StepSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:StepSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"StepSummaryList" ~of_json:StepSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListStepsOutput =
  struct
    type nonrec t =
      {
      steps: StepSummaryList.t option ;
      marker: Marker.t option }
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?steps = fun ?marker -> fun () -> { steps; marker }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Steps", (Option.map x.steps ~f:StepSummaryList.to_value));
        ("Marker", (Option.map x.marker ~f:Marker.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let marker =
        (Option.map ~f:Marker.of_xml) (Xml.child xml_arg0 "Marker") in
      let steps =
        (Option.map ~f:StepSummaryList.of_xml) (Xml.child xml_arg0 "Steps") in
      make ?marker ?steps ()
    let of_json json =
      let marker = field_map json "Marker" Marker.of_json in
      let steps = field_map json "Steps" StepSummaryList.of_json in
      make ?marker ?steps ()
    let to_json v = composed_to_json to_value v
  end
module ListStudioSessionMappingsInput =
  struct
    type nonrec t =
      {
      studioId: XmlStringMaxLen256.t option ;
      identityType: IdentityType.t option ;
      marker: Marker.t option }
    let make ?studioId =
      fun ?identityType ->
        fun ?marker -> fun () -> { studioId; identityType; marker }
    let to_value x =
      structure_to_value
        [("StudioId", (Option.map x.studioId ~f:XmlStringMaxLen256.to_value));
        ("IdentityType",
          (Option.map x.identityType ~f:IdentityType.to_value));
        ("Marker", (Option.map x.marker ~f:Marker.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let marker =
        (Option.map ~f:Marker.of_xml) (Xml.child xml_arg0 "Marker") in
      let identityType =
        (Option.map ~f:IdentityType.of_xml)
          (Xml.child xml_arg0 "IdentityType") in
      let studioId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "StudioId") in
      make ?marker ?identityType ?studioId ()
    let of_json json =
      let marker = field_map json "Marker" Marker.of_json in
      let identityType = field_map json "IdentityType" IdentityType.of_json in
      let studioId = field_map json "StudioId" XmlStringMaxLen256.of_json in
      make ?marker ?identityType ?studioId ()
    let to_json v = composed_to_json to_value v
  end
module SessionMappingSummary =
  struct
    type nonrec t =
      {
      studioId: XmlStringMaxLen256.t option ;
      identityId: XmlStringMaxLen256.t option ;
      identityName: XmlStringMaxLen256.t option ;
      identityType: IdentityType.t option ;
      sessionPolicyArn: XmlStringMaxLen256.t option ;
      creationTime: Date.t option }
    let make ?studioId =
      fun ?identityId ->
        fun ?identityName ->
          fun ?identityType ->
            fun ?sessionPolicyArn ->
              fun ?creationTime ->
                fun () ->
                  {
                    studioId;
                    identityId;
                    identityName;
                    identityType;
                    sessionPolicyArn;
                    creationTime
                  }
    let to_value x =
      structure_to_value
        [("StudioId", (Option.map x.studioId ~f:XmlStringMaxLen256.to_value));
        ("IdentityId",
          (Option.map x.identityId ~f:XmlStringMaxLen256.to_value));
        ("IdentityName",
          (Option.map x.identityName ~f:XmlStringMaxLen256.to_value));
        ("IdentityType",
          (Option.map x.identityType ~f:IdentityType.to_value));
        ("SessionPolicyArn",
          (Option.map x.sessionPolicyArn ~f:XmlStringMaxLen256.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:Date.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creationTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let sessionPolicyArn =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "SessionPolicyArn") in
      let identityType =
        (Option.map ~f:IdentityType.of_xml)
          (Xml.child xml_arg0 "IdentityType") in
      let identityName =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "IdentityName") in
      let identityId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "IdentityId") in
      let studioId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "StudioId") in
      make ?creationTime ?sessionPolicyArn ?identityType ?identityName
        ?identityId ?studioId ()
    let of_json json =
      let creationTime = field_map json "CreationTime" Date.of_json in
      let sessionPolicyArn =
        field_map json "SessionPolicyArn" XmlStringMaxLen256.of_json in
      let identityType = field_map json "IdentityType" IdentityType.of_json in
      let identityName =
        field_map json "IdentityName" XmlStringMaxLen256.of_json in
      let identityId = field_map json "IdentityId" XmlStringMaxLen256.of_json in
      let studioId = field_map json "StudioId" XmlStringMaxLen256.of_json in
      make ?creationTime ?sessionPolicyArn ?identityType ?identityName
        ?identityId ?studioId ()
    let to_json v = composed_to_json to_value v
  end
module SessionMappingSummaryList =
  struct
    type nonrec t = SessionMappingSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SessionMappingSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SessionMappingSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"SessionMappingSummaryList"
        ~of_json:SessionMappingSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListStudioSessionMappingsOutput =
  struct
    type nonrec t =
      {
      sessionMappings: SessionMappingSummaryList.t option ;
      marker: Marker.t option }
    type nonrec error =
      [ `InternalServerError of InternalServerError.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?sessionMappings =
      fun ?marker -> fun () -> { sessionMappings; marker }
    let error_of_json name json =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("SessionMappings",
           (Option.map x.sessionMappings
              ~f:SessionMappingSummaryList.to_value));
        ("Marker", (Option.map x.marker ~f:Marker.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let marker =
        (Option.map ~f:Marker.of_xml) (Xml.child xml_arg0 "Marker") in
      let sessionMappings =
        (Option.map ~f:SessionMappingSummaryList.of_xml)
          (Xml.child xml_arg0 "SessionMappings") in
      make ?marker ?sessionMappings ()
    let of_json json =
      let marker = field_map json "Marker" Marker.of_json in
      let sessionMappings =
        field_map json "SessionMappings" SessionMappingSummaryList.of_json in
      make ?marker ?sessionMappings ()
    let to_json v = composed_to_json to_value v
  end
module ListStudiosInput =
  struct
    type nonrec t = {
      marker: Marker.t option }
    let make ?marker = fun () -> { marker }
    let to_value x =
      structure_to_value
        [("Marker", (Option.map x.marker ~f:Marker.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let marker =
        (Option.map ~f:Marker.of_xml) (Xml.child xml_arg0 "Marker") in
      make ?marker ()
    let of_json json =
      let marker = field_map json "Marker" Marker.of_json in make ?marker ()
    let to_json v = composed_to_json to_value v
  end
module StudioSummary =
  struct
    type nonrec t =
      {
      studioId: XmlStringMaxLen256.t option ;
      name: XmlStringMaxLen256.t option ;
      vpcId: XmlStringMaxLen256.t option ;
      description: XmlStringMaxLen256.t option ;
      url: XmlStringMaxLen256.t option ;
      authMode: AuthMode.t option ;
      creationTime: Date.t option }
    let make ?studioId =
      fun ?name ->
        fun ?vpcId ->
          fun ?description ->
            fun ?url ->
              fun ?authMode ->
                fun ?creationTime ->
                  fun () ->
                    {
                      studioId;
                      name;
                      vpcId;
                      description;
                      url;
                      authMode;
                      creationTime
                    }
    let to_value x =
      structure_to_value
        [("StudioId", (Option.map x.studioId ~f:XmlStringMaxLen256.to_value));
        ("Name", (Option.map x.name ~f:XmlStringMaxLen256.to_value));
        ("VpcId", (Option.map x.vpcId ~f:XmlStringMaxLen256.to_value));
        ("Description",
          (Option.map x.description ~f:XmlStringMaxLen256.to_value));
        ("Url", (Option.map x.url ~f:XmlStringMaxLen256.to_value));
        ("AuthMode", (Option.map x.authMode ~f:AuthMode.to_value));
        ("CreationTime", (Option.map x.creationTime ~f:Date.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creationTime =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "CreationTime") in
      let authMode =
        (Option.map ~f:AuthMode.of_xml) (Xml.child xml_arg0 "AuthMode") in
      let url =
        (Option.map ~f:XmlStringMaxLen256.of_xml) (Xml.child xml_arg0 "Url") in
      let description =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "Description") in
      let vpcId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "VpcId") in
      let name =
        (Option.map ~f:XmlStringMaxLen256.of_xml) (Xml.child xml_arg0 "Name") in
      let studioId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "StudioId") in
      make ?creationTime ?authMode ?url ?description ?vpcId ?name ?studioId
        ()
    let of_json json =
      let creationTime = field_map json "CreationTime" Date.of_json in
      let authMode = field_map json "AuthMode" AuthMode.of_json in
      let url = field_map json "Url" XmlStringMaxLen256.of_json in
      let description =
        field_map json "Description" XmlStringMaxLen256.of_json in
      let vpcId = field_map json "VpcId" XmlStringMaxLen256.of_json in
      let name = field_map json "Name" XmlStringMaxLen256.of_json in
      let studioId = field_map json "StudioId" XmlStringMaxLen256.of_json in
      make ?creationTime ?authMode ?url ?description ?vpcId ?name ?studioId
        ()
    let to_json v = composed_to_json to_value v
  end
module StudioSummaryList =
  struct
    type nonrec t = StudioSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:StudioSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:StudioSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"StudioSummaryList" ~of_json:StudioSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListStudiosOutput =
  struct
    type nonrec t =
      {
      studios: StudioSummaryList.t option ;
      marker: Marker.t option }
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?studios = fun ?marker -> fun () -> { studios; marker }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Studios", (Option.map x.studios ~f:StudioSummaryList.to_value));
        ("Marker", (Option.map x.marker ~f:Marker.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let marker =
        (Option.map ~f:Marker.of_xml) (Xml.child xml_arg0 "Marker") in
      let studios =
        (Option.map ~f:StudioSummaryList.of_xml)
          (Xml.child xml_arg0 "Studios") in
      make ?marker ?studios ()
    let of_json json =
      let marker = field_map json "Marker" Marker.of_json in
      let studios = field_map json "Studios" StudioSummaryList.of_json in
      make ?marker ?studios ()
    let to_json v = composed_to_json to_value v
  end
module ModifyClusterInput =
  struct
    type nonrec t =
      {
      clusterId: String_.t ;
      stepConcurrencyLevel: Integer.t option }
    let context_ = "ModifyClusterInput"
    let make ?stepConcurrencyLevel =
      fun ~clusterId -> fun () -> { stepConcurrencyLevel; clusterId }
    let to_value x =
      structure_to_value
        [("ClusterId", (Some (String_.to_value x.clusterId)));
        ("StepConcurrencyLevel",
          (Option.map x.stepConcurrencyLevel ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stepConcurrencyLevel =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "StepConcurrencyLevel") in
      let clusterId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "ClusterId") in
      make ?stepConcurrencyLevel ~clusterId ()
    let of_json json =
      let stepConcurrencyLevel =
        field_map json "StepConcurrencyLevel" Integer.of_json in
      let clusterId = field_map_exn json "ClusterId" String_.of_json in
      make ?stepConcurrencyLevel ~clusterId ()
    let to_json v = composed_to_json to_value v
  end
module ModifyClusterOutput =
  struct
    type nonrec t = {
      stepConcurrencyLevel: Integer.t option }
    type nonrec error =
      [ `InternalServerError of InternalServerError.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?stepConcurrencyLevel = fun () -> { stepConcurrencyLevel }
    let error_of_json name json =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("StepConcurrencyLevel",
           (Option.map x.stepConcurrencyLevel ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let stepConcurrencyLevel =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "StepConcurrencyLevel") in
      make ?stepConcurrencyLevel ()
    let of_json json =
      let stepConcurrencyLevel =
        field_map json "StepConcurrencyLevel" Integer.of_json in
      make ?stepConcurrencyLevel ()
    let to_json v = composed_to_json to_value v
  end
module ModifyInstanceFleetInput =
  struct
    type nonrec t =
      {
      clusterId: ClusterId.t ;
      instanceFleet: InstanceFleetModifyConfig.t }
    let context_ = "ModifyInstanceFleetInput"
    let make ~clusterId =
      fun ~instanceFleet -> fun () -> { clusterId; instanceFleet }
    let to_value x =
      structure_to_value
        [("ClusterId", (Some (ClusterId.to_value x.clusterId)));
        ("InstanceFleet",
          (Some (InstanceFleetModifyConfig.to_value x.instanceFleet)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceFleet =
        InstanceFleetModifyConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceFleet") in
      let clusterId =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClusterId") in
      make ~instanceFleet ~clusterId ()
    let of_json json =
      let instanceFleet =
        field_map_exn json "InstanceFleet" InstanceFleetModifyConfig.of_json in
      let clusterId = field_map_exn json "ClusterId" ClusterId.of_json in
      make ~instanceFleet ~clusterId ()
    let to_json v = composed_to_json to_value v
  end
module ModifyInstanceGroupsInput =
  struct
    type nonrec t =
      {
      clusterId: ClusterId.t option ;
      instanceGroups: InstanceGroupModifyConfigList.t option }
    let make ?clusterId =
      fun ?instanceGroups -> fun () -> { clusterId; instanceGroups }
    let to_value x =
      structure_to_value
        [("ClusterId", (Option.map x.clusterId ~f:ClusterId.to_value));
        ("InstanceGroups",
          (Option.map x.instanceGroups
             ~f:InstanceGroupModifyConfigList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceGroups =
        (Option.map ~f:InstanceGroupModifyConfigList.of_xml)
          (Xml.child xml_arg0 "InstanceGroups") in
      let clusterId =
        (Option.map ~f:ClusterId.of_xml) (Xml.child xml_arg0 "ClusterId") in
      make ?instanceGroups ?clusterId ()
    let of_json json =
      let instanceGroups =
        field_map json "InstanceGroups" InstanceGroupModifyConfigList.of_json in
      let clusterId = field_map json "ClusterId" ClusterId.of_json in
      make ?instanceGroups ?clusterId ()
    let to_json v = composed_to_json to_value v
  end
module SupportedProductConfig =
  struct
    type nonrec t =
      {
      name: XmlStringMaxLen256.t option ;
      args: XmlStringList.t option }
    let make ?name = fun ?args -> fun () -> { name; args }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:XmlStringMaxLen256.to_value));
        ("Args", (Option.map x.args ~f:XmlStringList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let args =
        (Option.map ~f:XmlStringList.of_xml) (Xml.child xml_arg0 "Args") in
      let name =
        (Option.map ~f:XmlStringMaxLen256.of_xml) (Xml.child xml_arg0 "Name") in
      make ?args ?name ()
    let of_json json =
      let args = field_map json "Args" XmlStringList.of_json in
      let name = field_map json "Name" XmlStringMaxLen256.of_json in
      make ?args ?name ()
    let to_json v = composed_to_json to_value v
  end
module NewSupportedProductsList =
  struct
    type nonrec t = SupportedProductConfig.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SupportedProductConfig.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SupportedProductConfig.of_xml)
    let of_json j =
      list_of_json ~kind:"NewSupportedProductsList"
        ~of_json:SupportedProductConfig.of_json j
    let to_json v = composed_to_json to_value v
  end
module PutAutoScalingPolicyInput =
  struct
    type nonrec t =
      {
      clusterId: ClusterId.t ;
      instanceGroupId: InstanceGroupId.t ;
      autoScalingPolicy: AutoScalingPolicy.t }
    let context_ = "PutAutoScalingPolicyInput"
    let make ~clusterId =
      fun ~instanceGroupId ->
        fun ~autoScalingPolicy ->
          fun () -> { clusterId; instanceGroupId; autoScalingPolicy }
    let to_value x =
      structure_to_value
        [("ClusterId", (Some (ClusterId.to_value x.clusterId)));
        ("InstanceGroupId",
          (Some (InstanceGroupId.to_value x.instanceGroupId)));
        ("AutoScalingPolicy",
          (Some (AutoScalingPolicy.to_value x.autoScalingPolicy)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let autoScalingPolicy =
        AutoScalingPolicy.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AutoScalingPolicy") in
      let instanceGroupId =
        InstanceGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceGroupId") in
      let clusterId =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClusterId") in
      make ~autoScalingPolicy ~instanceGroupId ~clusterId ()
    let of_json json =
      let autoScalingPolicy =
        field_map_exn json "AutoScalingPolicy" AutoScalingPolicy.of_json in
      let instanceGroupId =
        field_map_exn json "InstanceGroupId" InstanceGroupId.of_json in
      let clusterId = field_map_exn json "ClusterId" ClusterId.of_json in
      make ~autoScalingPolicy ~instanceGroupId ~clusterId ()
    let to_json v = composed_to_json to_value v
  end
module PutAutoScalingPolicyOutput =
  struct
    type nonrec t =
      {
      clusterId: ClusterId.t option ;
      instanceGroupId: InstanceGroupId.t option ;
      autoScalingPolicy: AutoScalingPolicyDescription.t option ;
      clusterArn: ArnType.t option }
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?clusterId =
      fun ?instanceGroupId ->
        fun ?autoScalingPolicy ->
          fun ?clusterArn ->
            fun () ->
              { clusterId; instanceGroupId; autoScalingPolicy; clusterArn }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ClusterId", (Option.map x.clusterId ~f:ClusterId.to_value));
        ("InstanceGroupId",
          (Option.map x.instanceGroupId ~f:InstanceGroupId.to_value));
        ("AutoScalingPolicy",
          (Option.map x.autoScalingPolicy
             ~f:AutoScalingPolicyDescription.to_value));
        ("ClusterArn", (Option.map x.clusterArn ~f:ArnType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clusterArn =
        (Option.map ~f:ArnType.of_xml) (Xml.child xml_arg0 "ClusterArn") in
      let autoScalingPolicy =
        (Option.map ~f:AutoScalingPolicyDescription.of_xml)
          (Xml.child xml_arg0 "AutoScalingPolicy") in
      let instanceGroupId =
        (Option.map ~f:InstanceGroupId.of_xml)
          (Xml.child xml_arg0 "InstanceGroupId") in
      let clusterId =
        (Option.map ~f:ClusterId.of_xml) (Xml.child xml_arg0 "ClusterId") in
      make ?clusterArn ?autoScalingPolicy ?instanceGroupId ?clusterId ()
    let of_json json =
      let clusterArn = field_map json "ClusterArn" ArnType.of_json in
      let autoScalingPolicy =
        field_map json "AutoScalingPolicy"
          AutoScalingPolicyDescription.of_json in
      let instanceGroupId =
        field_map json "InstanceGroupId" InstanceGroupId.of_json in
      let clusterId = field_map json "ClusterId" ClusterId.of_json in
      make ?clusterArn ?autoScalingPolicy ?instanceGroupId ?clusterId ()
    let to_json v = composed_to_json to_value v
  end
module PutAutoTerminationPolicyInput =
  struct
    type nonrec t =
      {
      clusterId: ClusterId.t ;
      autoTerminationPolicy: AutoTerminationPolicy.t option }
    let context_ = "PutAutoTerminationPolicyInput"
    let make ?autoTerminationPolicy =
      fun ~clusterId -> fun () -> { autoTerminationPolicy; clusterId }
    let to_value x =
      structure_to_value
        [("ClusterId", (Some (ClusterId.to_value x.clusterId)));
        ("AutoTerminationPolicy",
          (Option.map x.autoTerminationPolicy
             ~f:AutoTerminationPolicy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let autoTerminationPolicy =
        (Option.map ~f:AutoTerminationPolicy.of_xml)
          (Xml.child xml_arg0 "AutoTerminationPolicy") in
      let clusterId =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClusterId") in
      make ?autoTerminationPolicy ~clusterId ()
    let of_json json =
      let autoTerminationPolicy =
        field_map json "AutoTerminationPolicy" AutoTerminationPolicy.of_json in
      let clusterId = field_map_exn json "ClusterId" ClusterId.of_json in
      make ?autoTerminationPolicy ~clusterId ()
    let to_json v = composed_to_json to_value v
  end
module PutAutoTerminationPolicyOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module PutBlockPublicAccessConfigurationInput =
  struct
    type nonrec t =
      {
      blockPublicAccessConfiguration: BlockPublicAccessConfiguration.t }
    let context_ = "PutBlockPublicAccessConfigurationInput"
    let make ~blockPublicAccessConfiguration =
      fun () -> { blockPublicAccessConfiguration }
    let to_value x =
      structure_to_value
        [("BlockPublicAccessConfiguration",
           (Some
              (BlockPublicAccessConfiguration.to_value
                 x.blockPublicAccessConfiguration)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let blockPublicAccessConfiguration =
        BlockPublicAccessConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "BlockPublicAccessConfiguration") in
      make ~blockPublicAccessConfiguration ()
    let of_json json =
      let blockPublicAccessConfiguration =
        field_map_exn json "BlockPublicAccessConfiguration"
          BlockPublicAccessConfiguration.of_json in
      make ~blockPublicAccessConfiguration ()
    let to_json v = composed_to_json to_value v
  end
module PutBlockPublicAccessConfigurationOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module PutManagedScalingPolicyInput =
  struct
    type nonrec t =
      {
      clusterId: ClusterId.t ;
      managedScalingPolicy: ManagedScalingPolicy.t }
    let context_ = "PutManagedScalingPolicyInput"
    let make ~clusterId =
      fun ~managedScalingPolicy ->
        fun () -> { clusterId; managedScalingPolicy }
    let to_value x =
      structure_to_value
        [("ClusterId", (Some (ClusterId.to_value x.clusterId)));
        ("ManagedScalingPolicy",
          (Some (ManagedScalingPolicy.to_value x.managedScalingPolicy)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let managedScalingPolicy =
        ManagedScalingPolicy.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ManagedScalingPolicy") in
      let clusterId =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClusterId") in
      make ~managedScalingPolicy ~clusterId ()
    let of_json json =
      let managedScalingPolicy =
        field_map_exn json "ManagedScalingPolicy"
          ManagedScalingPolicy.of_json in
      let clusterId = field_map_exn json "ClusterId" ClusterId.of_json in
      make ~managedScalingPolicy ~clusterId ()
    let to_json v = composed_to_json to_value v
  end
module PutManagedScalingPolicyOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module RemoveAutoScalingPolicyInput =
  struct
    type nonrec t =
      {
      clusterId: ClusterId.t ;
      instanceGroupId: InstanceGroupId.t }
    let context_ = "RemoveAutoScalingPolicyInput"
    let make ~clusterId =
      fun ~instanceGroupId -> fun () -> { clusterId; instanceGroupId }
    let to_value x =
      structure_to_value
        [("ClusterId", (Some (ClusterId.to_value x.clusterId)));
        ("InstanceGroupId",
          (Some (InstanceGroupId.to_value x.instanceGroupId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instanceGroupId =
        InstanceGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceGroupId") in
      let clusterId =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClusterId") in
      make ~instanceGroupId ~clusterId ()
    let of_json json =
      let instanceGroupId =
        field_map_exn json "InstanceGroupId" InstanceGroupId.of_json in
      let clusterId = field_map_exn json "ClusterId" ClusterId.of_json in
      make ~instanceGroupId ~clusterId ()
    let to_json v = composed_to_json to_value v
  end
module RemoveAutoScalingPolicyOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module RemoveAutoTerminationPolicyInput =
  struct
    type nonrec t = {
      clusterId: ClusterId.t }
    let context_ = "RemoveAutoTerminationPolicyInput"
    let make ~clusterId = fun () -> { clusterId }
    let to_value x =
      structure_to_value
        [("ClusterId", (Some (ClusterId.to_value x.clusterId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clusterId =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClusterId") in
      make ~clusterId ()
    let of_json json =
      let clusterId = field_map_exn json "ClusterId" ClusterId.of_json in
      make ~clusterId ()
    let to_json v = composed_to_json to_value v
  end
module RemoveAutoTerminationPolicyOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module RemoveManagedScalingPolicyInput =
  struct
    type nonrec t = {
      clusterId: ClusterId.t }
    let context_ = "RemoveManagedScalingPolicyInput"
    let make ~clusterId = fun () -> { clusterId }
    let to_value x =
      structure_to_value
        [("ClusterId", (Some (ClusterId.to_value x.clusterId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clusterId =
        ClusterId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ClusterId") in
      make ~clusterId ()
    let of_json json =
      let clusterId = field_map_exn json "ClusterId" ClusterId.of_json in
      make ~clusterId ()
    let to_json v = composed_to_json to_value v
  end
module RemoveManagedScalingPolicyOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module RemoveTagsInput =
  struct
    type nonrec t = {
      resourceId: ResourceId.t ;
      tagKeys: StringList.t }
    let context_ = "RemoveTagsInput"
    let make ~resourceId = fun ~tagKeys -> fun () -> { resourceId; tagKeys }
    let to_value x =
      structure_to_value
        [("ResourceId", (Some (ResourceId.to_value x.resourceId)));
        ("TagKeys", (Some (StringList.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        StringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TagKeys") in
      let resourceId =
        ResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceId") in
      make ~tagKeys ~resourceId ()
    let of_json json =
      let tagKeys = field_map_exn json "TagKeys" StringList.of_json in
      let resourceId = field_map_exn json "ResourceId" ResourceId.of_json in
      make ~tagKeys ~resourceId ()
    let to_json v = composed_to_json to_value v
  end
module RemoveTagsOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end
module RunJobFlowInput =
  struct
    type nonrec t =
      {
      name: XmlStringMaxLen256.t ;
      logUri: XmlString.t option ;
      logEncryptionKmsKeyId: XmlString.t option ;
      additionalInfo: XmlString.t option ;
      amiVersion: XmlStringMaxLen256.t option ;
      releaseLabel: XmlStringMaxLen256.t option ;
      instances: JobFlowInstancesConfig.t ;
      steps: StepConfigList.t option ;
      bootstrapActions: BootstrapActionConfigList.t option ;
      supportedProducts: SupportedProductsList.t option ;
      newSupportedProducts: NewSupportedProductsList.t option ;
      applications: ApplicationList.t option ;
      configurations: ConfigurationList.t option ;
      visibleToAllUsers: Boolean.t option ;
      jobFlowRole: XmlString.t option ;
      serviceRole: XmlString.t option ;
      tags: TagList.t option ;
      securityConfiguration: XmlString.t option ;
      autoScalingRole: XmlString.t option ;
      scaleDownBehavior: ScaleDownBehavior.t option ;
      customAmiId: XmlStringMaxLen256.t option ;
      ebsRootVolumeSize: Integer.t option ;
      repoUpgradeOnBoot: RepoUpgradeOnBoot.t option ;
      kerberosAttributes: KerberosAttributes.t option ;
      stepConcurrencyLevel: Integer.t option ;
      managedScalingPolicy: ManagedScalingPolicy.t option ;
      placementGroupConfigs: PlacementGroupConfigList.t option ;
      autoTerminationPolicy: AutoTerminationPolicy.t option }
    let context_ = "RunJobFlowInput"
    let make ?logUri =
      fun ?logEncryptionKmsKeyId ->
        fun ?additionalInfo ->
          fun ?amiVersion ->
            fun ?releaseLabel ->
              fun ?steps ->
                fun ?bootstrapActions ->
                  fun ?supportedProducts ->
                    fun ?newSupportedProducts ->
                      fun ?applications ->
                        fun ?configurations ->
                          fun ?visibleToAllUsers ->
                            fun ?jobFlowRole ->
                              fun ?serviceRole ->
                                fun ?tags ->
                                  fun ?securityConfiguration ->
                                    fun ?autoScalingRole ->
                                      fun ?scaleDownBehavior ->
                                        fun ?customAmiId ->
                                          fun ?ebsRootVolumeSize ->
                                            fun ?repoUpgradeOnBoot ->
                                              fun ?kerberosAttributes ->
                                                fun ?stepConcurrencyLevel ->
                                                  fun ?managedScalingPolicy
                                                    ->
                                                    fun
                                                      ?placementGroupConfigs
                                                      ->
                                                      fun
                                                        ?autoTerminationPolicy
                                                        ->
                                                        fun ~name ->
                                                          fun ~instances ->
                                                            fun () ->
                                                              {
                                                                logUri;
                                                                logEncryptionKmsKeyId;
                                                                additionalInfo;
                                                                amiVersion;
                                                                releaseLabel;
                                                                steps;
                                                                bootstrapActions;
                                                                supportedProducts;
                                                                newSupportedProducts;
                                                                applications;
                                                                configurations;
                                                                visibleToAllUsers;
                                                                jobFlowRole;
                                                                serviceRole;
                                                                tags;
                                                                securityConfiguration;
                                                                autoScalingRole;
                                                                scaleDownBehavior;
                                                                customAmiId;
                                                                ebsRootVolumeSize;
                                                                repoUpgradeOnBoot;
                                                                kerberosAttributes;
                                                                stepConcurrencyLevel;
                                                                managedScalingPolicy;
                                                                placementGroupConfigs;
                                                                autoTerminationPolicy;
                                                                name;
                                                                instances
                                                              }
    let to_value x =
      structure_to_value
        [("Name", (Some (XmlStringMaxLen256.to_value x.name)));
        ("LogUri", (Option.map x.logUri ~f:XmlString.to_value));
        ("LogEncryptionKmsKeyId",
          (Option.map x.logEncryptionKmsKeyId ~f:XmlString.to_value));
        ("AdditionalInfo",
          (Option.map x.additionalInfo ~f:XmlString.to_value));
        ("AmiVersion",
          (Option.map x.amiVersion ~f:XmlStringMaxLen256.to_value));
        ("ReleaseLabel",
          (Option.map x.releaseLabel ~f:XmlStringMaxLen256.to_value));
        ("Instances", (Some (JobFlowInstancesConfig.to_value x.instances)));
        ("Steps", (Option.map x.steps ~f:StepConfigList.to_value));
        ("BootstrapActions",
          (Option.map x.bootstrapActions
             ~f:BootstrapActionConfigList.to_value));
        ("SupportedProducts",
          (Option.map x.supportedProducts ~f:SupportedProductsList.to_value));
        ("NewSupportedProducts",
          (Option.map x.newSupportedProducts
             ~f:NewSupportedProductsList.to_value));
        ("Applications",
          (Option.map x.applications ~f:ApplicationList.to_value));
        ("Configurations",
          (Option.map x.configurations ~f:ConfigurationList.to_value));
        ("VisibleToAllUsers",
          (Option.map x.visibleToAllUsers ~f:Boolean.to_value));
        ("JobFlowRole", (Option.map x.jobFlowRole ~f:XmlString.to_value));
        ("ServiceRole", (Option.map x.serviceRole ~f:XmlString.to_value));
        ("Tags", (Option.map x.tags ~f:TagList.to_value));
        ("SecurityConfiguration",
          (Option.map x.securityConfiguration ~f:XmlString.to_value));
        ("AutoScalingRole",
          (Option.map x.autoScalingRole ~f:XmlString.to_value));
        ("ScaleDownBehavior",
          (Option.map x.scaleDownBehavior ~f:ScaleDownBehavior.to_value));
        ("CustomAmiId",
          (Option.map x.customAmiId ~f:XmlStringMaxLen256.to_value));
        ("EbsRootVolumeSize",
          (Option.map x.ebsRootVolumeSize ~f:Integer.to_value));
        ("RepoUpgradeOnBoot",
          (Option.map x.repoUpgradeOnBoot ~f:RepoUpgradeOnBoot.to_value));
        ("KerberosAttributes",
          (Option.map x.kerberosAttributes ~f:KerberosAttributes.to_value));
        ("StepConcurrencyLevel",
          (Option.map x.stepConcurrencyLevel ~f:Integer.to_value));
        ("ManagedScalingPolicy",
          (Option.map x.managedScalingPolicy ~f:ManagedScalingPolicy.to_value));
        ("PlacementGroupConfigs",
          (Option.map x.placementGroupConfigs
             ~f:PlacementGroupConfigList.to_value));
        ("AutoTerminationPolicy",
          (Option.map x.autoTerminationPolicy
             ~f:AutoTerminationPolicy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let autoTerminationPolicy =
        (Option.map ~f:AutoTerminationPolicy.of_xml)
          (Xml.child xml_arg0 "AutoTerminationPolicy") in
      let placementGroupConfigs =
        (Option.map ~f:PlacementGroupConfigList.of_xml)
          (Xml.child xml_arg0 "PlacementGroupConfigs") in
      let managedScalingPolicy =
        (Option.map ~f:ManagedScalingPolicy.of_xml)
          (Xml.child xml_arg0 "ManagedScalingPolicy") in
      let stepConcurrencyLevel =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "StepConcurrencyLevel") in
      let kerberosAttributes =
        (Option.map ~f:KerberosAttributes.of_xml)
          (Xml.child xml_arg0 "KerberosAttributes") in
      let repoUpgradeOnBoot =
        (Option.map ~f:RepoUpgradeOnBoot.of_xml)
          (Xml.child xml_arg0 "RepoUpgradeOnBoot") in
      let ebsRootVolumeSize =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "EbsRootVolumeSize") in
      let customAmiId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "CustomAmiId") in
      let scaleDownBehavior =
        (Option.map ~f:ScaleDownBehavior.of_xml)
          (Xml.child xml_arg0 "ScaleDownBehavior") in
      let autoScalingRole =
        (Option.map ~f:XmlString.of_xml)
          (Xml.child xml_arg0 "AutoScalingRole") in
      let securityConfiguration =
        (Option.map ~f:XmlString.of_xml)
          (Xml.child xml_arg0 "SecurityConfiguration") in
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let serviceRole =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "ServiceRole") in
      let jobFlowRole =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "JobFlowRole") in
      let visibleToAllUsers =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "VisibleToAllUsers") in
      let configurations =
        (Option.map ~f:ConfigurationList.of_xml)
          (Xml.child xml_arg0 "Configurations") in
      let applications =
        (Option.map ~f:ApplicationList.of_xml)
          (Xml.child xml_arg0 "Applications") in
      let newSupportedProducts =
        (Option.map ~f:NewSupportedProductsList.of_xml)
          (Xml.child xml_arg0 "NewSupportedProducts") in
      let supportedProducts =
        (Option.map ~f:SupportedProductsList.of_xml)
          (Xml.child xml_arg0 "SupportedProducts") in
      let bootstrapActions =
        (Option.map ~f:BootstrapActionConfigList.of_xml)
          (Xml.child xml_arg0 "BootstrapActions") in
      let steps =
        (Option.map ~f:StepConfigList.of_xml) (Xml.child xml_arg0 "Steps") in
      let instances =
        JobFlowInstancesConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Instances") in
      let releaseLabel =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "ReleaseLabel") in
      let amiVersion =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "AmiVersion") in
      let additionalInfo =
        (Option.map ~f:XmlString.of_xml)
          (Xml.child xml_arg0 "AdditionalInfo") in
      let logEncryptionKmsKeyId =
        (Option.map ~f:XmlString.of_xml)
          (Xml.child xml_arg0 "LogEncryptionKmsKeyId") in
      let logUri =
        (Option.map ~f:XmlString.of_xml) (Xml.child xml_arg0 "LogUri") in
      let name =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ?autoTerminationPolicy ?placementGroupConfigs
        ?managedScalingPolicy ?stepConcurrencyLevel ?kerberosAttributes
        ?repoUpgradeOnBoot ?ebsRootVolumeSize ?customAmiId ?scaleDownBehavior
        ?autoScalingRole ?securityConfiguration ?tags ?serviceRole
        ?jobFlowRole ?visibleToAllUsers ?configurations ?applications
        ?newSupportedProducts ?supportedProducts ?bootstrapActions ?steps
        ~instances ?releaseLabel ?amiVersion ?additionalInfo
        ?logEncryptionKmsKeyId ?logUri ~name ()
    let of_json json =
      let autoTerminationPolicy =
        field_map json "AutoTerminationPolicy" AutoTerminationPolicy.of_json in
      let placementGroupConfigs =
        field_map json "PlacementGroupConfigs"
          PlacementGroupConfigList.of_json in
      let managedScalingPolicy =
        field_map json "ManagedScalingPolicy" ManagedScalingPolicy.of_json in
      let stepConcurrencyLevel =
        field_map json "StepConcurrencyLevel" Integer.of_json in
      let kerberosAttributes =
        field_map json "KerberosAttributes" KerberosAttributes.of_json in
      let repoUpgradeOnBoot =
        field_map json "RepoUpgradeOnBoot" RepoUpgradeOnBoot.of_json in
      let ebsRootVolumeSize =
        field_map json "EbsRootVolumeSize" Integer.of_json in
      let customAmiId =
        field_map json "CustomAmiId" XmlStringMaxLen256.of_json in
      let scaleDownBehavior =
        field_map json "ScaleDownBehavior" ScaleDownBehavior.of_json in
      let autoScalingRole =
        field_map json "AutoScalingRole" XmlString.of_json in
      let securityConfiguration =
        field_map json "SecurityConfiguration" XmlString.of_json in
      let tags = field_map json "Tags" TagList.of_json in
      let serviceRole = field_map json "ServiceRole" XmlString.of_json in
      let jobFlowRole = field_map json "JobFlowRole" XmlString.of_json in
      let visibleToAllUsers =
        field_map json "VisibleToAllUsers" Boolean.of_json in
      let configurations =
        field_map json "Configurations" ConfigurationList.of_json in
      let applications =
        field_map json "Applications" ApplicationList.of_json in
      let newSupportedProducts =
        field_map json "NewSupportedProducts"
          NewSupportedProductsList.of_json in
      let supportedProducts =
        field_map json "SupportedProducts" SupportedProductsList.of_json in
      let bootstrapActions =
        field_map json "BootstrapActions" BootstrapActionConfigList.of_json in
      let steps = field_map json "Steps" StepConfigList.of_json in
      let instances =
        field_map_exn json "Instances" JobFlowInstancesConfig.of_json in
      let releaseLabel =
        field_map json "ReleaseLabel" XmlStringMaxLen256.of_json in
      let amiVersion = field_map json "AmiVersion" XmlStringMaxLen256.of_json in
      let additionalInfo = field_map json "AdditionalInfo" XmlString.of_json in
      let logEncryptionKmsKeyId =
        field_map json "LogEncryptionKmsKeyId" XmlString.of_json in
      let logUri = field_map json "LogUri" XmlString.of_json in
      let name = field_map_exn json "Name" XmlStringMaxLen256.of_json in
      make ?autoTerminationPolicy ?placementGroupConfigs
        ?managedScalingPolicy ?stepConcurrencyLevel ?kerberosAttributes
        ?repoUpgradeOnBoot ?ebsRootVolumeSize ?customAmiId ?scaleDownBehavior
        ?autoScalingRole ?securityConfiguration ?tags ?serviceRole
        ?jobFlowRole ?visibleToAllUsers ?configurations ?applications
        ?newSupportedProducts ?supportedProducts ?bootstrapActions ?steps
        ~instances ?releaseLabel ?amiVersion ?additionalInfo
        ?logEncryptionKmsKeyId ?logUri ~name ()
    let to_json v = composed_to_json to_value v
  end
module RunJobFlowOutput =
  struct
    type nonrec t =
      {
      jobFlowId: XmlStringMaxLen256.t option ;
      clusterArn: ArnType.t option }
    type nonrec error =
      [ `InternalServerError of InternalServerError.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?jobFlowId =
      fun ?clusterArn -> fun () -> { jobFlowId; clusterArn }
    let error_of_json name json =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerError" ->
          `InternalServerError (InternalServerError.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerError e ->
          `Assoc
            [("error", (`String "InternalServerError"));
            ("details", (InternalServerError.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("JobFlowId",
           (Option.map x.jobFlowId ~f:XmlStringMaxLen256.to_value));
        ("ClusterArn", (Option.map x.clusterArn ~f:ArnType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clusterArn =
        (Option.map ~f:ArnType.of_xml) (Xml.child xml_arg0 "ClusterArn") in
      let jobFlowId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "JobFlowId") in
      make ?clusterArn ?jobFlowId ()
    let of_json json =
      let clusterArn = field_map json "ClusterArn" ArnType.of_json in
      let jobFlowId = field_map json "JobFlowId" XmlStringMaxLen256.of_json in
      make ?clusterArn ?jobFlowId ()
    let to_json v = composed_to_json to_value v
  end
module SetTerminationProtectionInput =
  struct
    type nonrec t =
      {
      jobFlowIds: XmlStringList.t ;
      terminationProtected: Boolean.t }
    let context_ = "SetTerminationProtectionInput"
    let make ~jobFlowIds =
      fun ~terminationProtected ->
        fun () -> { jobFlowIds; terminationProtected }
    let to_value x =
      structure_to_value
        [("JobFlowIds", (Some (XmlStringList.to_value x.jobFlowIds)));
        ("TerminationProtected",
          (Some (Boolean.to_value x.terminationProtected)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let terminationProtected =
        Boolean.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TerminationProtected") in
      let jobFlowIds =
        XmlStringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "JobFlowIds") in
      make ~terminationProtected ~jobFlowIds ()
    let of_json json =
      let terminationProtected =
        field_map_exn json "TerminationProtected" Boolean.of_json in
      let jobFlowIds = field_map_exn json "JobFlowIds" XmlStringList.of_json in
      make ~terminationProtected ~jobFlowIds ()
    let to_json v = composed_to_json to_value v
  end
module SetVisibleToAllUsersInput =
  struct
    type nonrec t =
      {
      jobFlowIds: XmlStringList.t ;
      visibleToAllUsers: Boolean.t }
    let context_ = "SetVisibleToAllUsersInput"
    let make ~jobFlowIds =
      fun ~visibleToAllUsers -> fun () -> { jobFlowIds; visibleToAllUsers }
    let to_value x =
      structure_to_value
        [("JobFlowIds", (Some (XmlStringList.to_value x.jobFlowIds)));
        ("VisibleToAllUsers", (Some (Boolean.to_value x.visibleToAllUsers)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let visibleToAllUsers =
        Boolean.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VisibleToAllUsers") in
      let jobFlowIds =
        XmlStringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "JobFlowIds") in
      make ~visibleToAllUsers ~jobFlowIds ()
    let of_json json =
      let visibleToAllUsers =
        field_map_exn json "VisibleToAllUsers" Boolean.of_json in
      let jobFlowIds = field_map_exn json "JobFlowIds" XmlStringList.of_json in
      make ~visibleToAllUsers ~jobFlowIds ()
    let to_json v = composed_to_json to_value v
  end
module StartNotebookExecutionInput =
  struct
    type nonrec t =
      {
      editorId: XmlStringMaxLen256.t ;
      relativePath: XmlString.t ;
      notebookExecutionName: XmlStringMaxLen256.t option ;
      notebookParams: XmlString.t option ;
      executionEngine: ExecutionEngineConfig.t ;
      serviceRole: XmlString.t ;
      notebookInstanceSecurityGroupId: XmlStringMaxLen256.t option ;
      tags: TagList.t option }
    let context_ = "StartNotebookExecutionInput"
    let make ?notebookExecutionName =
      fun ?notebookParams ->
        fun ?notebookInstanceSecurityGroupId ->
          fun ?tags ->
            fun ~editorId ->
              fun ~relativePath ->
                fun ~executionEngine ->
                  fun ~serviceRole ->
                    fun () ->
                      {
                        notebookExecutionName;
                        notebookParams;
                        notebookInstanceSecurityGroupId;
                        tags;
                        editorId;
                        relativePath;
                        executionEngine;
                        serviceRole
                      }
    let to_value x =
      structure_to_value
        [("EditorId", (Some (XmlStringMaxLen256.to_value x.editorId)));
        ("RelativePath", (Some (XmlString.to_value x.relativePath)));
        ("NotebookExecutionName",
          (Option.map x.notebookExecutionName ~f:XmlStringMaxLen256.to_value));
        ("NotebookParams",
          (Option.map x.notebookParams ~f:XmlString.to_value));
        ("ExecutionEngine",
          (Some (ExecutionEngineConfig.to_value x.executionEngine)));
        ("ServiceRole", (Some (XmlString.to_value x.serviceRole)));
        ("NotebookInstanceSecurityGroupId",
          (Option.map x.notebookInstanceSecurityGroupId
             ~f:XmlStringMaxLen256.to_value));
        ("Tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let notebookInstanceSecurityGroupId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "NotebookInstanceSecurityGroupId") in
      let serviceRole =
        XmlString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServiceRole") in
      let executionEngine =
        ExecutionEngineConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ExecutionEngine") in
      let notebookParams =
        (Option.map ~f:XmlString.of_xml)
          (Xml.child xml_arg0 "NotebookParams") in
      let notebookExecutionName =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "NotebookExecutionName") in
      let relativePath =
        XmlString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RelativePath") in
      let editorId =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "EditorId") in
      make ?tags ?notebookInstanceSecurityGroupId ~serviceRole
        ~executionEngine ?notebookParams ?notebookExecutionName ~relativePath
        ~editorId ()
    let of_json json =
      let tags = field_map json "Tags" TagList.of_json in
      let notebookInstanceSecurityGroupId =
        field_map json "NotebookInstanceSecurityGroupId"
          XmlStringMaxLen256.of_json in
      let serviceRole = field_map_exn json "ServiceRole" XmlString.of_json in
      let executionEngine =
        field_map_exn json "ExecutionEngine" ExecutionEngineConfig.of_json in
      let notebookParams = field_map json "NotebookParams" XmlString.of_json in
      let notebookExecutionName =
        field_map json "NotebookExecutionName" XmlStringMaxLen256.of_json in
      let relativePath = field_map_exn json "RelativePath" XmlString.of_json in
      let editorId = field_map_exn json "EditorId" XmlStringMaxLen256.of_json in
      make ?tags ?notebookInstanceSecurityGroupId ~serviceRole
        ~executionEngine ?notebookParams ?notebookExecutionName ~relativePath
        ~editorId ()
    let to_json v = composed_to_json to_value v
  end
module StartNotebookExecutionOutput =
  struct
    type nonrec t = {
      notebookExecutionId: XmlStringMaxLen256.t option }
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?notebookExecutionId = fun () -> { notebookExecutionId }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("NotebookExecutionId",
           (Option.map x.notebookExecutionId ~f:XmlStringMaxLen256.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let notebookExecutionId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "NotebookExecutionId") in
      make ?notebookExecutionId ()
    let of_json json =
      let notebookExecutionId =
        field_map json "NotebookExecutionId" XmlStringMaxLen256.of_json in
      make ?notebookExecutionId ()
    let to_json v = composed_to_json to_value v
  end
module StopNotebookExecutionInput =
  struct
    type nonrec t = {
      notebookExecutionId: XmlStringMaxLen256.t }
    let context_ = "StopNotebookExecutionInput"
    let make ~notebookExecutionId = fun () -> { notebookExecutionId }
    let to_value x =
      structure_to_value
        [("NotebookExecutionId",
           (Some (XmlStringMaxLen256.to_value x.notebookExecutionId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let notebookExecutionId =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "NotebookExecutionId") in
      make ~notebookExecutionId ()
    let of_json json =
      let notebookExecutionId =
        field_map_exn json "NotebookExecutionId" XmlStringMaxLen256.of_json in
      make ~notebookExecutionId ()
    let to_json v = composed_to_json to_value v
  end
module TerminateJobFlowsInput =
  struct
    type nonrec t = {
      jobFlowIds: XmlStringList.t }
    let context_ = "TerminateJobFlowsInput"
    let make ~jobFlowIds = fun () -> { jobFlowIds }
    let to_value x =
      structure_to_value
        [("JobFlowIds", (Some (XmlStringList.to_value x.jobFlowIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let jobFlowIds =
        XmlStringList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "JobFlowIds") in
      make ~jobFlowIds ()
    let of_json json =
      let jobFlowIds = field_map_exn json "JobFlowIds" XmlStringList.of_json in
      make ~jobFlowIds ()
    let to_json v = composed_to_json to_value v
  end
module UpdateStudioInput =
  struct
    type nonrec t =
      {
      studioId: XmlStringMaxLen256.t ;
      name: XmlStringMaxLen256.t option ;
      description: XmlStringMaxLen256.t option ;
      subnetIds: SubnetIdList.t option ;
      defaultS3Location: XmlString.t option }
    let context_ = "UpdateStudioInput"
    let make ?name =
      fun ?description ->
        fun ?subnetIds ->
          fun ?defaultS3Location ->
            fun ~studioId ->
              fun () ->
                { name; description; subnetIds; defaultS3Location; studioId }
    let to_value x =
      structure_to_value
        [("StudioId", (Some (XmlStringMaxLen256.to_value x.studioId)));
        ("Name", (Option.map x.name ~f:XmlStringMaxLen256.to_value));
        ("Description",
          (Option.map x.description ~f:XmlStringMaxLen256.to_value));
        ("SubnetIds", (Option.map x.subnetIds ~f:SubnetIdList.to_value));
        ("DefaultS3Location",
          (Option.map x.defaultS3Location ~f:XmlString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let defaultS3Location =
        (Option.map ~f:XmlString.of_xml)
          (Xml.child xml_arg0 "DefaultS3Location") in
      let subnetIds =
        (Option.map ~f:SubnetIdList.of_xml) (Xml.child xml_arg0 "SubnetIds") in
      let description =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "Description") in
      let name =
        (Option.map ~f:XmlStringMaxLen256.of_xml) (Xml.child xml_arg0 "Name") in
      let studioId =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "StudioId") in
      make ?defaultS3Location ?subnetIds ?description ?name ~studioId ()
    let of_json json =
      let defaultS3Location =
        field_map json "DefaultS3Location" XmlString.of_json in
      let subnetIds = field_map json "SubnetIds" SubnetIdList.of_json in
      let description =
        field_map json "Description" XmlStringMaxLen256.of_json in
      let name = field_map json "Name" XmlStringMaxLen256.of_json in
      let studioId = field_map_exn json "StudioId" XmlStringMaxLen256.of_json in
      make ?defaultS3Location ?subnetIds ?description ?name ~studioId ()
    let to_json v = composed_to_json to_value v
  end
module UpdateStudioSessionMappingInput =
  struct
    type nonrec t =
      {
      studioId: XmlStringMaxLen256.t ;
      identityId: XmlStringMaxLen256.t option ;
      identityName: XmlStringMaxLen256.t option ;
      identityType: IdentityType.t ;
      sessionPolicyArn: XmlStringMaxLen256.t }
    let context_ = "UpdateStudioSessionMappingInput"
    let make ?identityId =
      fun ?identityName ->
        fun ~studioId ->
          fun ~identityType ->
            fun ~sessionPolicyArn ->
              fun () ->
                {
                  identityId;
                  identityName;
                  studioId;
                  identityType;
                  sessionPolicyArn
                }
    let to_value x =
      structure_to_value
        [("StudioId", (Some (XmlStringMaxLen256.to_value x.studioId)));
        ("IdentityId",
          (Option.map x.identityId ~f:XmlStringMaxLen256.to_value));
        ("IdentityName",
          (Option.map x.identityName ~f:XmlStringMaxLen256.to_value));
        ("IdentityType", (Some (IdentityType.to_value x.identityType)));
        ("SessionPolicyArn",
          (Some (XmlStringMaxLen256.to_value x.sessionPolicyArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionPolicyArn =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SessionPolicyArn") in
      let identityType =
        IdentityType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IdentityType") in
      let identityName =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "IdentityName") in
      let identityId =
        (Option.map ~f:XmlStringMaxLen256.of_xml)
          (Xml.child xml_arg0 "IdentityId") in
      let studioId =
        XmlStringMaxLen256.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "StudioId") in
      make ~sessionPolicyArn ~identityType ?identityName ?identityId
        ~studioId ()
    let of_json json =
      let sessionPolicyArn =
        field_map_exn json "SessionPolicyArn" XmlStringMaxLen256.of_json in
      let identityType =
        field_map_exn json "IdentityType" IdentityType.of_json in
      let identityName =
        field_map json "IdentityName" XmlStringMaxLen256.of_json in
      let identityId = field_map json "IdentityId" XmlStringMaxLen256.of_json in
      let studioId = field_map_exn json "StudioId" XmlStringMaxLen256.of_json in
      make ~sessionPolicyArn ~identityType ?identityName ?identityId
        ~studioId ()
    let to_json v = composed_to_json to_value v
  end
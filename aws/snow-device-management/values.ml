(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.snow_device_management
let apiVersion = "2021-08-04"
let endpointPrefix = "snow-device-management"
let serviceFullName = "AWS Snow Device Management"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module AttachmentStatus =
  struct
    type nonrec t =
      | ATTACHING 
      | ATTACHED 
      | DETACHING 
      | DETACHED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ATTACHING -> "ATTACHING"
      | ATTACHED -> "ATTACHED"
      | DETACHING -> "DETACHING"
      | DETACHED -> "DETACHED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ATTACHING" -> ATTACHING
      | "ATTACHED" -> ATTACHED
      | "DETACHING" -> DETACHING
      | "DETACHED" -> DETACHED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration AttachmentStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AttachmentStatus" j)
    let to_json = simple_to_json to_value
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module EbsInstanceBlockDevice =
  struct
    type nonrec t =
      {
      attachTime: Timestamp.t option
        [@ocaml.doc "When the attachment was initiated."];
      deleteOnTermination: Boolean.t option
        [@ocaml.doc
          "A value that indicates whether the volume is deleted on instance termination."];
      status: AttachmentStatus.t option [@ocaml.doc "The attachment state."];
      volumeId: String_.t option
        [@ocaml.doc "The ID of the Amazon EBS volume."]}
    let make ?attachTime =
      fun ?deleteOnTermination ->
        fun ?status ->
          fun ?volumeId ->
            fun () -> { attachTime; deleteOnTermination; status; volumeId }
    let to_value x =
      structure_to_value
        [("attachTime", (Option.map x.attachTime ~f:Timestamp.to_value));
        ("deleteOnTermination",
          (Option.map x.deleteOnTermination ~f:Boolean.to_value));
        ("status", (Option.map x.status ~f:AttachmentStatus.to_value));
        ("volumeId", (Option.map x.volumeId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let volumeId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "volumeId") in
      let status =
        (Option.map ~f:AttachmentStatus.of_xml) (Xml.child xml_arg0 "status") in
      let deleteOnTermination =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "deleteOnTermination") in
      let attachTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "attachTime") in
      make ?volumeId ?status ?deleteOnTermination ?attachTime ()
    let of_json json =
      let volumeId = field_map json "volumeId" String_.of_json in
      let status = field_map json "status" AttachmentStatus.of_json in
      let deleteOnTermination =
        field_map json "deleteOnTermination" Boolean.of_json in
      let attachTime = field_map json "attachTime" Timestamp.of_json in
      make ?volumeId ?status ?deleteOnTermination ?attachTime ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes a parameter used to set up an Amazon Elastic Block Store (Amazon EBS) volume in a block device mapping."]
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module InstanceBlockDeviceMapping =
  struct
    type nonrec t =
      {
      deviceName: String_.t option [@ocaml.doc "The block device name."];
      ebs: EbsInstanceBlockDevice.t option
        [@ocaml.doc
          "The parameters used to automatically set up Amazon Elastic Block Store (Amazon EBS) volumes when the instance is launched."]}
    let make ?deviceName = fun ?ebs -> fun () -> { deviceName; ebs }
    let to_value x =
      structure_to_value
        [("deviceName", (Option.map x.deviceName ~f:String_.to_value));
        ("ebs", (Option.map x.ebs ~f:EbsInstanceBlockDevice.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let ebs =
        (Option.map ~f:EbsInstanceBlockDevice.of_xml)
          (Xml.child xml_arg0 "ebs") in
      let deviceName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "deviceName") in
      make ?ebs ?deviceName ()
    let of_json json =
      let ebs = field_map json "ebs" EbsInstanceBlockDevice.of_json in
      let deviceName = field_map json "deviceName" String_.of_json in
      make ?ebs ?deviceName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The description of a block device mapping."]
module InstanceStateName =
  struct
    type nonrec t =
      | PENDING 
      | RUNNING 
      | SHUTTING_DOWN 
      | TERMINATED 
      | STOPPING 
      | STOPPED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PENDING -> "PENDING"
      | RUNNING -> "RUNNING"
      | SHUTTING_DOWN -> "SHUTTING_DOWN"
      | TERMINATED -> "TERMINATED"
      | STOPPING -> "STOPPING"
      | STOPPED -> "STOPPED"
      | Non_static_id s -> s
    let of_string =
      function
      | "PENDING" -> PENDING
      | "RUNNING" -> RUNNING
      | "SHUTTING_DOWN" -> SHUTTING_DOWN
      | "TERMINATED" -> TERMINATED
      | "STOPPING" -> STOPPING
      | "STOPPED" -> STOPPED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration InstanceStateName" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"InstanceStateName" j)
    let to_json = simple_to_json to_value
  end
module SecurityGroupIdentifier =
  struct
    type nonrec t =
      {
      groupId: String_.t option [@ocaml.doc "The security group ID."];
      groupName: String_.t option [@ocaml.doc "The security group name."]}
    let make ?groupId = fun ?groupName -> fun () -> { groupId; groupName }
    let to_value x =
      structure_to_value
        [("groupId", (Option.map x.groupId ~f:String_.to_value));
        ("groupName", (Option.map x.groupName ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let groupName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "groupName") in
      let groupId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "groupId") in
      make ?groupName ?groupId ()
    let of_json json =
      let groupName = field_map json "groupName" String_.of_json in
      let groupId = field_map json "groupId" String_.of_json in
      make ?groupName ?groupId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about the device's security group."]
module CpuOptions =
  struct
    type nonrec t =
      {
      coreCount: Integer.t option
        [@ocaml.doc "The number of cores that the CPU can use."];
      threadsPerCore: Integer.t option
        [@ocaml.doc "The number of threads per core in the CPU."]}
    let make ?coreCount =
      fun ?threadsPerCore -> fun () -> { coreCount; threadsPerCore }
    let to_value x =
      structure_to_value
        [("coreCount", (Option.map x.coreCount ~f:Integer.to_value));
        ("threadsPerCore", (Option.map x.threadsPerCore ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let threadsPerCore =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "threadsPerCore") in
      let coreCount =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "coreCount") in
      make ?threadsPerCore ?coreCount ()
    let of_json json =
      let threadsPerCore = field_map json "threadsPerCore" Integer.of_json in
      let coreCount = field_map json "coreCount" Integer.of_json in
      make ?threadsPerCore ?coreCount ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The options for how a device's CPU is configured."]
module InstanceBlockDeviceMappingList =
  struct
    type nonrec t = InstanceBlockDeviceMapping.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:InstanceBlockDeviceMapping.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InstanceBlockDeviceMapping.of_xml)
    let of_json j =
      list_of_json ~kind:"InstanceBlockDeviceMappingList"
        ~of_json:InstanceBlockDeviceMapping.of_json j
    let to_json v = composed_to_json to_value v
  end
module InstanceState =
  struct
    type nonrec t =
      {
      code: Integer.t option
        [@ocaml.doc
          "The state of the instance as a 16-bit unsigned integer. The high byte is all of the bits between 2^8 and (2^16)-1, which equals decimal values between 256 and 65,535. These numerical values are used for internal purposes and should be ignored. The low byte is all of the bits between 2^0 and (2^8)-1, which equals decimal values between 0 and 255. The valid values for the instance state code are all in the range of the low byte. These values are: 0 : pending 16 : running 32 : shutting-down 48 : terminated 64 : stopping 80 : stopped You can ignore the high byte value by zeroing out all of the bits above 2^8 or 256 in decimal."];
      name: InstanceStateName.t option
        [@ocaml.doc "The current state of the instance."]}
    let make ?code = fun ?name -> fun () -> { code; name }
    let to_value x =
      structure_to_value
        [("code", (Option.map x.code ~f:Integer.to_value));
        ("name", (Option.map x.name ~f:InstanceStateName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        (Option.map ~f:InstanceStateName.of_xml) (Xml.child xml_arg0 "name") in
      let code = (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "code") in
      make ?name ?code ()
    let of_json json =
      let name = field_map json "name" InstanceStateName.of_json in
      let code = field_map json "code" Integer.of_json in make ?name ?code ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The description of the current state of an instance."]
module SecurityGroupIdentifierList =
  struct
    type nonrec t = SecurityGroupIdentifier.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SecurityGroupIdentifier.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SecurityGroupIdentifier.of_xml)
    let of_json j =
      list_of_json ~kind:"SecurityGroupIdentifierList"
        ~of_json:SecurityGroupIdentifier.of_json j
    let to_json v = composed_to_json to_value v
  end
module TagMap =
  struct
    type nonrec t = (String_.t * String_.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((String_.of_string chopped),
                              (String_.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x -> (String_.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module TaskId =
  struct
    type nonrec t = string
    let context_ = "TaskId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TaskId" j
    let to_json = simple_to_json to_value
  end
module TaskState =
  struct
    type nonrec t =
      | IN_PROGRESS 
      | CANCELED 
      | COMPLETED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | IN_PROGRESS -> "IN_PROGRESS"
      | CANCELED -> "CANCELED"
      | COMPLETED -> "COMPLETED"
      | Non_static_id s -> s
    let of_string =
      function
      | "IN_PROGRESS" -> IN_PROGRESS
      | "CANCELED" -> CANCELED
      | "COMPLETED" -> COMPLETED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration TaskState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"TaskState" j)
    let to_json = simple_to_json to_value
  end
module ExecutionId =
  struct
    type nonrec t = string
    let context_ = "ExecutionId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExecutionId" j
    let to_json = simple_to_json to_value
  end
module ExecutionState =
  struct
    type nonrec t =
      | QUEUED 
      | IN_PROGRESS 
      | CANCELED 
      | FAILED 
      | SUCCEEDED 
      | REJECTED 
      | TIMED_OUT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | QUEUED -> "QUEUED"
      | IN_PROGRESS -> "IN_PROGRESS"
      | CANCELED -> "CANCELED"
      | FAILED -> "FAILED"
      | SUCCEEDED -> "SUCCEEDED"
      | REJECTED -> "REJECTED"
      | TIMED_OUT -> "TIMED_OUT"
      | Non_static_id s -> s
    let of_string =
      function
      | "QUEUED" -> QUEUED
      | "IN_PROGRESS" -> IN_PROGRESS
      | "CANCELED" -> CANCELED
      | "FAILED" -> FAILED
      | "SUCCEEDED" -> SUCCEEDED
      | "REJECTED" -> REJECTED
      | "TIMED_OUT" -> TIMED_OUT
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ExecutionState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ExecutionState" j)
    let to_json = simple_to_json to_value
  end
module ManagedDeviceId =
  struct
    type nonrec t = string
    let context_ = "ManagedDeviceId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ManagedDeviceId" j
    let to_json = simple_to_json to_value
  end
module CapacityNameString =
  struct
    type nonrec t = string
    let context_ = "CapacityNameString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CapacityNameString" j
    let to_json = simple_to_json to_value
  end
module CapacityUnitString =
  struct
    type nonrec t = string
    let context_ = "CapacityUnitString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:20) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CapacityUnitString" j
    let to_json = simple_to_json to_value
  end
module Long =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module IpAddressAssignment =
  struct
    type nonrec t =
      | DHCP 
      | STATIC 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | DHCP -> "DHCP" | STATIC -> "STATIC" | Non_static_id s -> s
    let of_string =
      function | "DHCP" -> DHCP | "STATIC" -> STATIC | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration IpAddressAssignment" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"IpAddressAssignment" j)
    let to_json = simple_to_json to_value
  end
module PhysicalConnectorType =
  struct
    type nonrec t =
      | RJ45 
      | SFP_PLUS 
      | QSFP 
      | RJ45_2 
      | WIFI 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | RJ45 -> "RJ45"
      | SFP_PLUS -> "SFP_PLUS"
      | QSFP -> "QSFP"
      | RJ45_2 -> "RJ45_2"
      | WIFI -> "WIFI"
      | Non_static_id s -> s
    let of_string =
      function
      | "RJ45" -> RJ45
      | "SFP_PLUS" -> SFP_PLUS
      | "QSFP" -> QSFP
      | "RJ45_2" -> RJ45_2
      | "WIFI" -> WIFI
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration PhysicalConnectorType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"PhysicalConnectorType" j)
    let to_json = simple_to_json to_value
  end
module Instance =
  struct
    type nonrec t =
      {
      amiLaunchIndex: Integer.t option
        [@ocaml.doc
          "The Amazon Machine Image (AMI) launch index, which you can use to find this instance in the launch group."];
      blockDeviceMappings: InstanceBlockDeviceMappingList.t option
        [@ocaml.doc "Any block device mapping entries for the instance."];
      cpuOptions: CpuOptions.t option
        [@ocaml.doc "The CPU options for the instance."];
      createdAt: Timestamp.t option
        [@ocaml.doc "When the instance was created."];
      imageId: String_.t option
        [@ocaml.doc "The ID of the AMI used to launch the instance."];
      instanceId: String_.t option [@ocaml.doc "The ID of the instance."];
      instanceType: String_.t option [@ocaml.doc "The instance type."];
      privateIpAddress: String_.t option
        [@ocaml.doc "The private IPv4 address assigned to the instance."];
      publicIpAddress: String_.t option
        [@ocaml.doc "The public IPv4 address assigned to the instance."];
      rootDeviceName: String_.t option
        [@ocaml.doc
          "The device name of the root device volume (for example, /dev/sda1)."];
      securityGroups: SecurityGroupIdentifierList.t option
        [@ocaml.doc "The security groups for the instance."];
      state: InstanceState.t option ;
      updatedAt: Timestamp.t option
        [@ocaml.doc "When the instance was last updated."]}
    let make ?amiLaunchIndex =
      fun ?blockDeviceMappings ->
        fun ?cpuOptions ->
          fun ?createdAt ->
            fun ?imageId ->
              fun ?instanceId ->
                fun ?instanceType ->
                  fun ?privateIpAddress ->
                    fun ?publicIpAddress ->
                      fun ?rootDeviceName ->
                        fun ?securityGroups ->
                          fun ?state ->
                            fun ?updatedAt ->
                              fun () ->
                                {
                                  amiLaunchIndex;
                                  blockDeviceMappings;
                                  cpuOptions;
                                  createdAt;
                                  imageId;
                                  instanceId;
                                  instanceType;
                                  privateIpAddress;
                                  publicIpAddress;
                                  rootDeviceName;
                                  securityGroups;
                                  state;
                                  updatedAt
                                }
    let to_value x =
      structure_to_value
        [("amiLaunchIndex",
           (Option.map x.amiLaunchIndex ~f:Integer.to_value));
        ("blockDeviceMappings",
          (Option.map x.blockDeviceMappings
             ~f:InstanceBlockDeviceMappingList.to_value));
        ("cpuOptions", (Option.map x.cpuOptions ~f:CpuOptions.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("imageId", (Option.map x.imageId ~f:String_.to_value));
        ("instanceId", (Option.map x.instanceId ~f:String_.to_value));
        ("instanceType", (Option.map x.instanceType ~f:String_.to_value));
        ("privateIpAddress",
          (Option.map x.privateIpAddress ~f:String_.to_value));
        ("publicIpAddress",
          (Option.map x.publicIpAddress ~f:String_.to_value));
        ("rootDeviceName", (Option.map x.rootDeviceName ~f:String_.to_value));
        ("securityGroups",
          (Option.map x.securityGroups
             ~f:SecurityGroupIdentifierList.to_value));
        ("state", (Option.map x.state ~f:InstanceState.to_value));
        ("updatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "updatedAt") in
      let state =
        (Option.map ~f:InstanceState.of_xml) (Xml.child xml_arg0 "state") in
      let securityGroups =
        (Option.map ~f:SecurityGroupIdentifierList.of_xml)
          (Xml.child xml_arg0 "securityGroups") in
      let rootDeviceName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "rootDeviceName") in
      let publicIpAddress =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "publicIpAddress") in
      let privateIpAddress =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "privateIpAddress") in
      let instanceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "instanceType") in
      let instanceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "instanceId") in
      let imageId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "imageId") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let cpuOptions =
        (Option.map ~f:CpuOptions.of_xml) (Xml.child xml_arg0 "cpuOptions") in
      let blockDeviceMappings =
        (Option.map ~f:InstanceBlockDeviceMappingList.of_xml)
          (Xml.child xml_arg0 "blockDeviceMappings") in
      let amiLaunchIndex =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "amiLaunchIndex") in
      make ?updatedAt ?state ?securityGroups ?rootDeviceName ?publicIpAddress
        ?privateIpAddress ?instanceType ?instanceId ?imageId ?createdAt
        ?cpuOptions ?blockDeviceMappings ?amiLaunchIndex ()
    let of_json json =
      let updatedAt = field_map json "updatedAt" Timestamp.of_json in
      let state = field_map json "state" InstanceState.of_json in
      let securityGroups =
        field_map json "securityGroups" SecurityGroupIdentifierList.of_json in
      let rootDeviceName = field_map json "rootDeviceName" String_.of_json in
      let publicIpAddress = field_map json "publicIpAddress" String_.of_json in
      let privateIpAddress =
        field_map json "privateIpAddress" String_.of_json in
      let instanceType = field_map json "instanceType" String_.of_json in
      let instanceId = field_map json "instanceId" String_.of_json in
      let imageId = field_map json "imageId" String_.of_json in
      let createdAt = field_map json "createdAt" Timestamp.of_json in
      let cpuOptions = field_map json "cpuOptions" CpuOptions.of_json in
      let blockDeviceMappings =
        field_map json "blockDeviceMappings"
          InstanceBlockDeviceMappingList.of_json in
      let amiLaunchIndex = field_map json "amiLaunchIndex" Integer.of_json in
      make ?updatedAt ?state ?securityGroups ?rootDeviceName ?publicIpAddress
        ?privateIpAddress ?instanceType ?instanceId ?imageId ?createdAt
        ?cpuOptions ?blockDeviceMappings ?amiLaunchIndex ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The description of an instance. Currently, Amazon EC2 instances are the only supported instance type."]
module TaskSummary =
  struct
    type nonrec t =
      {
      state: TaskState.t option
        [@ocaml.doc "The state of the task assigned to one or many devices."];
      tags: TagMap.t option
        [@ocaml.doc
          "Optional metadata that you assign to a resource. You can use tags to categorize a resource in different ways, such as by purpose, owner, or environment."];
      taskArn: String_.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the task."];
      taskId: TaskId.t [@ocaml.doc "The task ID."]}
    let context_ = "TaskSummary"
    let make ?state =
      fun ?tags ->
        fun ?taskArn ->
          fun ~taskId -> fun () -> { state; tags; taskArn; taskId }
    let to_value x =
      structure_to_value
        [("state", (Option.map x.state ~f:TaskState.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value));
        ("taskArn", (Option.map x.taskArn ~f:String_.to_value));
        ("taskId", (Some (TaskId.to_value x.taskId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let taskId =
        TaskId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "taskId") in
      let taskArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "taskArn") in
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let state =
        (Option.map ~f:TaskState.of_xml) (Xml.child xml_arg0 "state") in
      make ~taskId ?taskArn ?tags ?state ()
    let of_json json =
      let taskId = field_map_exn json "taskId" TaskId.of_json in
      let taskArn = field_map json "taskArn" String_.of_json in
      let tags = field_map json "tags" TagMap.of_json in
      let state = field_map json "state" TaskState.of_json in
      make ~taskId ?taskArn ?tags ?state ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the task assigned to one or many devices."]
module ExecutionSummary =
  struct
    type nonrec t =
      {
      executionId: ExecutionId.t option
        [@ocaml.doc "The ID of the execution."];
      managedDeviceId: ManagedDeviceId.t option
        [@ocaml.doc
          "The ID of the managed device that the task is being executed on."];
      state: ExecutionState.t option
        [@ocaml.doc "The state of the execution."];
      taskId: TaskId.t option [@ocaml.doc "The ID of the task."]}
    let make ?executionId =
      fun ?managedDeviceId ->
        fun ?state ->
          fun ?taskId ->
            fun () -> { executionId; managedDeviceId; state; taskId }
    let to_value x =
      structure_to_value
        [("executionId", (Option.map x.executionId ~f:ExecutionId.to_value));
        ("managedDeviceId",
          (Option.map x.managedDeviceId ~f:ManagedDeviceId.to_value));
        ("state", (Option.map x.state ~f:ExecutionState.to_value));
        ("taskId", (Option.map x.taskId ~f:TaskId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let taskId =
        (Option.map ~f:TaskId.of_xml) (Xml.child xml_arg0 "taskId") in
      let state =
        (Option.map ~f:ExecutionState.of_xml) (Xml.child xml_arg0 "state") in
      let managedDeviceId =
        (Option.map ~f:ManagedDeviceId.of_xml)
          (Xml.child xml_arg0 "managedDeviceId") in
      let executionId =
        (Option.map ~f:ExecutionId.of_xml) (Xml.child xml_arg0 "executionId") in
      make ?taskId ?state ?managedDeviceId ?executionId ()
    let of_json json =
      let taskId = field_map json "taskId" TaskId.of_json in
      let state = field_map json "state" ExecutionState.of_json in
      let managedDeviceId =
        field_map json "managedDeviceId" ManagedDeviceId.of_json in
      let executionId = field_map json "executionId" ExecutionId.of_json in
      make ?taskId ?state ?managedDeviceId ?executionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The summary of a task execution on a specified device."]
module DeviceSummary =
  struct
    type nonrec t =
      {
      associatedWithJob: String_.t option
        [@ocaml.doc "The ID of the job used to order the device."];
      managedDeviceArn: String_.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the device."];
      managedDeviceId: ManagedDeviceId.t option
        [@ocaml.doc "The ID of the device."];
      tags: TagMap.t option
        [@ocaml.doc
          "Optional metadata that you assign to a resource. You can use tags to categorize a resource in different ways, such as by purpose, owner, or environment."]}
    let make ?associatedWithJob =
      fun ?managedDeviceArn ->
        fun ?managedDeviceId ->
          fun ?tags ->
            fun () ->
              { associatedWithJob; managedDeviceArn; managedDeviceId; tags }
    let to_value x =
      structure_to_value
        [("associatedWithJob",
           (Option.map x.associatedWithJob ~f:String_.to_value));
        ("managedDeviceArn",
          (Option.map x.managedDeviceArn ~f:String_.to_value));
        ("managedDeviceId",
          (Option.map x.managedDeviceId ~f:ManagedDeviceId.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let managedDeviceId =
        (Option.map ~f:ManagedDeviceId.of_xml)
          (Xml.child xml_arg0 "managedDeviceId") in
      let managedDeviceArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "managedDeviceArn") in
      let associatedWithJob =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "associatedWithJob") in
      make ?tags ?managedDeviceId ?managedDeviceArn ?associatedWithJob ()
    let of_json json =
      let tags = field_map json "tags" TagMap.of_json in
      let managedDeviceId =
        field_map json "managedDeviceId" ManagedDeviceId.of_json in
      let managedDeviceArn =
        field_map json "managedDeviceArn" String_.of_json in
      let associatedWithJob =
        field_map json "associatedWithJob" String_.of_json in
      make ?tags ?managedDeviceId ?managedDeviceArn ?associatedWithJob ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Identifying information about the device."]
module ResourceSummary =
  struct
    type nonrec t =
      {
      arn: String_.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the resource."];
      id: String_.t option [@ocaml.doc "The ID of the resource."];
      resourceType: String_.t [@ocaml.doc "The resource type."]}
    let context_ = "ResourceSummary"
    let make ?arn =
      fun ?id -> fun ~resourceType -> fun () -> { arn; id; resourceType }
    let to_value x =
      structure_to_value
        [("arn", (Option.map x.arn ~f:String_.to_value));
        ("id", (Option.map x.id ~f:String_.to_value));
        ("resourceType", (Some (String_.to_value x.resourceType)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceType") in
      let id = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "id") in
      let arn = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "arn") in
      make ~resourceType ?id ?arn ()
    let of_json json =
      let resourceType = field_map_exn json "resourceType" String_.of_json in
      let id = field_map json "id" String_.of_json in
      let arn = field_map json "arn" String_.of_json in
      make ~resourceType ?id ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A summary of a resource available on the device."]
module Capacity =
  struct
    type nonrec t =
      {
      available: Long.t option
        [@ocaml.doc
          "The amount of capacity available for use on the device."];
      name: CapacityNameString.t option
        [@ocaml.doc "The name of the type of capacity, such as memory."];
      total: Long.t option [@ocaml.doc "The total capacity on the device."];
      unit: CapacityUnitString.t option
        [@ocaml.doc "The unit of measure for the type of capacity."];
      used: Long.t option
        [@ocaml.doc "The amount of capacity used on the device."]}
    let make ?available =
      fun ?name ->
        fun ?total ->
          fun ?unit ->
            fun ?used -> fun () -> { available; name; total; unit; used }
    let to_value x =
      structure_to_value
        [("available", (Option.map x.available ~f:Long.to_value));
        ("name", (Option.map x.name ~f:CapacityNameString.to_value));
        ("total", (Option.map x.total ~f:Long.to_value));
        ("unit", (Option.map x.unit ~f:CapacityUnitString.to_value));
        ("used", (Option.map x.used ~f:Long.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let used = (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "used") in
      let unit =
        (Option.map ~f:CapacityUnitString.of_xml) (Xml.child xml_arg0 "unit") in
      let total = (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "total") in
      let name =
        (Option.map ~f:CapacityNameString.of_xml) (Xml.child xml_arg0 "name") in
      let available =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "available") in
      make ?used ?unit ?total ?name ?available ()
    let of_json json =
      let used = field_map json "used" Long.of_json in
      let unit = field_map json "unit" CapacityUnitString.of_json in
      let total = field_map json "total" Long.of_json in
      let name = field_map json "name" CapacityNameString.of_json in
      let available = field_map json "available" Long.of_json in
      make ?used ?unit ?total ?name ?available ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The physical capacity of the Amazon Web Services Snow Family device."]
module PhysicalNetworkInterface =
  struct
    type nonrec t =
      {
      defaultGateway: String_.t option
        [@ocaml.doc "The default gateway of the device."];
      ipAddress: String_.t option
        [@ocaml.doc "The IP address of the device."];
      ipAddressAssignment: IpAddressAssignment.t option
        [@ocaml.doc
          "A value that describes whether the IP address is dynamic or persistent."];
      macAddress: String_.t option
        [@ocaml.doc "The MAC address of the device."];
      netmask: String_.t option
        [@ocaml.doc
          "The netmask used to divide the IP address into subnets."];
      physicalConnectorType: PhysicalConnectorType.t option
        [@ocaml.doc "The physical connector type."];
      physicalNetworkInterfaceId: String_.t option
        [@ocaml.doc "The physical network interface ID."]}
    let make ?defaultGateway =
      fun ?ipAddress ->
        fun ?ipAddressAssignment ->
          fun ?macAddress ->
            fun ?netmask ->
              fun ?physicalConnectorType ->
                fun ?physicalNetworkInterfaceId ->
                  fun () ->
                    {
                      defaultGateway;
                      ipAddress;
                      ipAddressAssignment;
                      macAddress;
                      netmask;
                      physicalConnectorType;
                      physicalNetworkInterfaceId
                    }
    let to_value x =
      structure_to_value
        [("defaultGateway",
           (Option.map x.defaultGateway ~f:String_.to_value));
        ("ipAddress", (Option.map x.ipAddress ~f:String_.to_value));
        ("ipAddressAssignment",
          (Option.map x.ipAddressAssignment ~f:IpAddressAssignment.to_value));
        ("macAddress", (Option.map x.macAddress ~f:String_.to_value));
        ("netmask", (Option.map x.netmask ~f:String_.to_value));
        ("physicalConnectorType",
          (Option.map x.physicalConnectorType
             ~f:PhysicalConnectorType.to_value));
        ("physicalNetworkInterfaceId",
          (Option.map x.physicalNetworkInterfaceId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let physicalNetworkInterfaceId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "physicalNetworkInterfaceId") in
      let physicalConnectorType =
        (Option.map ~f:PhysicalConnectorType.of_xml)
          (Xml.child xml_arg0 "physicalConnectorType") in
      let netmask =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "netmask") in
      let macAddress =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "macAddress") in
      let ipAddressAssignment =
        (Option.map ~f:IpAddressAssignment.of_xml)
          (Xml.child xml_arg0 "ipAddressAssignment") in
      let ipAddress =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ipAddress") in
      let defaultGateway =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "defaultGateway") in
      make ?physicalNetworkInterfaceId ?physicalConnectorType ?netmask
        ?macAddress ?ipAddressAssignment ?ipAddress ?defaultGateway ()
    let of_json json =
      let physicalNetworkInterfaceId =
        field_map json "physicalNetworkInterfaceId" String_.of_json in
      let physicalConnectorType =
        field_map json "physicalConnectorType" PhysicalConnectorType.of_json in
      let netmask = field_map json "netmask" String_.of_json in
      let macAddress = field_map json "macAddress" String_.of_json in
      let ipAddressAssignment =
        field_map json "ipAddressAssignment" IpAddressAssignment.of_json in
      let ipAddress = field_map json "ipAddress" String_.of_json in
      let defaultGateway = field_map json "defaultGateway" String_.of_json in
      make ?physicalNetworkInterfaceId ?physicalConnectorType ?netmask
        ?macAddress ?ipAddressAssignment ?ipAddress ?defaultGateway ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The details about the physical network interface for the device."]
module InstanceSummary =
  struct
    type nonrec t =
      {
      instance: Instance.t option
        [@ocaml.doc "A structure containing details about the instance."];
      lastUpdatedAt: Timestamp.t option
        [@ocaml.doc "When the instance summary was last updated."]}
    let make ?instance =
      fun ?lastUpdatedAt -> fun () -> { instance; lastUpdatedAt }
    let to_value x =
      structure_to_value
        [("instance", (Option.map x.instance ~f:Instance.to_value));
        ("lastUpdatedAt", (Option.map x.lastUpdatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastUpdatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastUpdatedAt") in
      let instance =
        (Option.map ~f:Instance.of_xml) (Xml.child xml_arg0 "instance") in
      make ?lastUpdatedAt ?instance ()
    let of_json json =
      let lastUpdatedAt = field_map json "lastUpdatedAt" Timestamp.of_json in
      let instance = field_map json "instance" Instance.of_json in
      make ?lastUpdatedAt ?instance ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The details about the instance."]
module Reboot =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A structure used to reboot the device."]
module Unlock =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A structure used to unlock a device."]
module TagKeys =
  struct
    type nonrec t = String_.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j = list_of_json ~kind:"TagKeys" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module AccessDeniedException =
  struct
    type nonrec t = {
      message: String_.t }
    let context_ = "AccessDeniedException"
    let make ~message = fun () -> { message }
    let to_value x =
      structure_to_value [("message", (Some (String_.to_value x.message)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "message") in
      make ~message ()
    let of_json json =
      let message = field_map_exn json "message" String_.of_json in
      make ~message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "You don't have sufficient access to perform this action."]
module InternalServerException =
  struct
    type nonrec t = {
      message: String_.t }
    let context_ = "InternalServerException"
    let make ~message = fun () -> { message }
    let to_value x =
      structure_to_value [("message", (Some (String_.to_value x.message)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "message") in
      make ~message ()
    let of_json json =
      let message = field_map_exn json "message" String_.of_json in
      make ~message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An unexpected error occurred while processing the request."]
module NextToken =
  struct
    type nonrec t = string
    let context_ = "NextToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9+/=]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NextToken" j
    let to_json = simple_to_json to_value
  end
module TaskSummaryList =
  struct
    type nonrec t = TaskSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TaskSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TaskSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"TaskSummaryList" ~of_json:TaskSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ThrottlingException =
  struct
    type nonrec t = {
      message: String_.t }
    let context_ = "ThrottlingException"
    let make ~message = fun () -> { message }
    let to_value x =
      structure_to_value [("message", (Some (String_.to_value x.message)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "message") in
      make ~message ()
    let of_json json =
      let message = field_map_exn json "message" String_.of_json in
      make ~message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The request was denied due to request throttling."]
module ValidationException =
  struct
    type nonrec t = {
      message: String_.t }
    let context_ = "ValidationException"
    let make ~message = fun () -> { message }
    let to_value x =
      structure_to_value [("message", (Some (String_.to_value x.message)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "message") in
      make ~message ()
    let of_json json =
      let message = field_map_exn json "message" String_.of_json in
      make ~message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The input fails to satisfy the constraints specified by an Amazon Web Services service."]
module MaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:100) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ResourceNotFoundException =
  struct
    type nonrec t = {
      message: String_.t }
    let context_ = "ResourceNotFoundException"
    let make ~message = fun () -> { message }
    let to_value x =
      structure_to_value [("message", (Some (String_.to_value x.message)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "message") in
      make ~message ()
    let of_json json =
      let message = field_map_exn json "message" String_.of_json in
      make ~message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The request references a resource that doesn't exist."]
module ExecutionSummaryList =
  struct
    type nonrec t = ExecutionSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ExecutionSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ExecutionSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"ExecutionSummaryList"
        ~of_json:ExecutionSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module DeviceSummaryList =
  struct
    type nonrec t = DeviceSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:DeviceSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DeviceSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"DeviceSummaryList" ~of_json:DeviceSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module JobId =
  struct
    type nonrec t = string
    let context_ = "JobId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"JobId" j
    let to_json = simple_to_json to_value
  end
module ResourceSummaryList =
  struct
    type nonrec t = ResourceSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ResourceSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResourceSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"ResourceSummaryList"
        ~of_json:ResourceSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ListDeviceResourcesInputTypeString =
  struct
    type nonrec t = string
    let context_ = "ListDeviceResourcesInputTypeString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:50) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"ListDeviceResourcesInputTypeString" j
    let to_json = simple_to_json to_value
  end
module TargetList =
  struct
    type nonrec t = String_.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"TargetList" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module TaskDescriptionString =
  struct
    type nonrec t = string
    let context_ = "TaskDescriptionString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () -> check_pattern i ~pattern:"[A-Za-z0-9 _.,!#]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TaskDescriptionString" j
    let to_json = simple_to_json to_value
  end
module CapacityList =
  struct
    type nonrec t = Capacity.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:100) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:Capacity.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Capacity.of_xml)
    let of_json j =
      list_of_json ~kind:"CapacityList" ~of_json:Capacity.of_json j
    let to_json v = composed_to_json to_value v
  end
module PhysicalNetworkInterfaceList =
  struct
    type nonrec t = PhysicalNetworkInterface.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:PhysicalNetworkInterface.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PhysicalNetworkInterface.of_xml)
    let of_json j =
      list_of_json ~kind:"PhysicalNetworkInterfaceList"
        ~of_json:PhysicalNetworkInterface.of_json j
    let to_json v = composed_to_json to_value v
  end
module SoftwareInformation =
  struct
    type nonrec t =
      {
      installState: String_.t option
        [@ocaml.doc
          "The state of the software that is installed or that is being installed on the device."];
      installedVersion: String_.t option
        [@ocaml.doc
          "The version of the software currently installed on the device."];
      installingVersion: String_.t option
        [@ocaml.doc
          "The version of the software being installed on the device."]}
    let make ?installState =
      fun ?installedVersion ->
        fun ?installingVersion ->
          fun () -> { installState; installedVersion; installingVersion }
    let to_value x =
      structure_to_value
        [("installState", (Option.map x.installState ~f:String_.to_value));
        ("installedVersion",
          (Option.map x.installedVersion ~f:String_.to_value));
        ("installingVersion",
          (Option.map x.installingVersion ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let installingVersion =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "installingVersion") in
      let installedVersion =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "installedVersion") in
      let installState =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "installState") in
      make ?installingVersion ?installedVersion ?installState ()
    let of_json json =
      let installingVersion =
        field_map json "installingVersion" String_.of_json in
      let installedVersion =
        field_map json "installedVersion" String_.of_json in
      let installState = field_map json "installState" String_.of_json in
      make ?installingVersion ?installedVersion ?installState ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Information about the software on the device."]
module UnlockState =
  struct
    type nonrec t =
      | UNLOCKED 
      | LOCKED 
      | UNLOCKING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | UNLOCKED -> "UNLOCKED"
      | LOCKED -> "LOCKED"
      | UNLOCKING -> "UNLOCKING"
      | Non_static_id s -> s
    let of_string =
      function
      | "UNLOCKED" -> UNLOCKED
      | "LOCKED" -> LOCKED
      | "UNLOCKING" -> UNLOCKING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration UnlockState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"UnlockState" j)
    let to_json = simple_to_json to_value
  end
module InstanceSummaryList =
  struct
    type nonrec t = InstanceSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:InstanceSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:InstanceSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"InstanceSummaryList"
        ~of_json:InstanceSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module InstanceIdsList =
  struct
    type nonrec t = String_.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"InstanceIdsList" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module ServiceQuotaExceededException =
  struct
    type nonrec t = {
      message: String_.t }
    let context_ = "ServiceQuotaExceededException"
    let make ~message = fun () -> { message }
    let to_value x =
      structure_to_value [("message", (Some (String_.to_value x.message)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "message") in
      make ~message ()
    let of_json json =
      let message = field_map_exn json "message" String_.of_json in
      make ~message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The request would cause a service quota to be exceeded."]
module Command =
  struct
    type nonrec t =
      {
      reboot: Reboot.t option [@ocaml.doc "Reboots the device."];
      unlock: Unlock.t option [@ocaml.doc "Unlocks the device."]}
    let make ?reboot = fun ?unlock -> fun () -> { reboot; unlock }
    let to_value x =
      structure_to_value
        [("reboot", (Option.map x.reboot ~f:Reboot.to_value));
        ("unlock", (Option.map x.unlock ~f:Unlock.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unlock =
        (Option.map ~f:Unlock.of_xml) (Xml.child xml_arg0 "unlock") in
      let reboot =
        (Option.map ~f:Reboot.of_xml) (Xml.child xml_arg0 "reboot") in
      make ?unlock ?reboot ()
    let of_json json =
      let unlock = field_map json "unlock" Unlock.of_json in
      let reboot = field_map json "reboot" Reboot.of_json in
      make ?unlock ?reboot ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The command given to the device to execute."]
module IdempotencyToken =
  struct
    type nonrec t = string
    let context_ = "IdempotencyToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"[!-~]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"IdempotencyToken" j
    let to_json = simple_to_json to_value
  end
module UntagResourceInput =
  struct
    type nonrec t =
      {
      resourceArn: String_.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the device or task."];
      tagKeys: TagKeys.t
        [@ocaml.doc
          "Optional metadata that you assign to a resource. You can use tags to categorize a resource in different ways, such as by purpose, owner, or environment."]}
    let context_ = "UntagResourceInput"
    let make ~resourceArn =
      fun ~tagKeys -> fun () -> { resourceArn; tagKeys }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (String_.to_value x.resourceArn)));
        ("tagKeys", (Some (TagKeys.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeys.of_xml (Xml.child_exn ~context:context_ xml_arg0 "tagKeys") in
      let resourceArn =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tagKeys ~resourceArn ()
    let of_json json =
      let tagKeys = field_map_exn json "tagKeys" TagKeys.of_json in
      let resourceArn = field_map_exn json "resourceArn" String_.of_json in
      make ~tagKeys ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Removes a tag from a device or task."]
module TagResourceInput =
  struct
    type nonrec t =
      {
      resourceArn: String_.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the device or task."];
      tags: TagMap.t
        [@ocaml.doc
          "Optional metadata that you assign to a resource. You can use tags to categorize a resource in different ways, such as by purpose, owner, or environment."]}
    let context_ = "TagResourceInput"
    let make ~resourceArn = fun ~tags -> fun () -> { resourceArn; tags }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (String_.to_value x.resourceArn)));
        ("tags", (Some (TagMap.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagMap.of_xml (Xml.child_exn ~context:context_ xml_arg0 "tags") in
      let resourceArn =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tags ~resourceArn ()
    let of_json json =
      let tags = field_map_exn json "tags" TagMap.of_json in
      let resourceArn = field_map_exn json "resourceArn" String_.of_json in
      make ~tags ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Adds or replaces tags on a device or task."]
module ListTasksOutput =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "A pagination token to continue to the next page of tasks."];
      tasks: TaskSummaryList.t option
        [@ocaml.doc
          "A list of task structures containing details about each task."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken = fun ?tasks -> fun () -> { nextToken; tasks }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("tasks", (Option.map x.tasks ~f:TaskSummaryList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tasks =
        (Option.map ~f:TaskSummaryList.of_xml) (Xml.child xml_arg0 "tasks") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?tasks ?nextToken ()
    let of_json json =
      let tasks = field_map json "tasks" TaskSummaryList.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      make ?tasks ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns a list of tasks that can be filtered by state."]
module ListTasksInput =
  struct
    type nonrec t =
      {
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of tasks per page."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A pagination token to continue to the next page of tasks."];
      state: TaskState.t option
        [@ocaml.doc "A structure used to filter the list of tasks."]}
    let make ?maxResults =
      fun ?nextToken ->
        fun ?state -> fun () -> { maxResults; nextToken; state }
    let to_value x =
      structure_to_value
        [("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("state", (Option.map x.state ~f:TaskState.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let state =
        (Option.map ~f:TaskState.of_xml) (Xml.child xml_arg0 "state") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      make ?state ?nextToken ?maxResults ()
    let of_json json =
      let state = field_map json "state" TaskState.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      make ?state ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns a list of tasks that can be filtered by state."]
module ListTagsForResourceOutput =
  struct
    type nonrec t =
      {
      tags: TagMap.t option
        [@ocaml.doc "The list of tags for the device or task."]}
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      make ?tags ()
    let of_json json =
      let tags = field_map json "tags" TagMap.of_json in make ?tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns a list of tags for a managed device or task."]
module ListTagsForResourceInput =
  struct
    type nonrec t =
      {
      resourceArn: String_.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the device or task."]}
    let context_ = "ListTagsForResourceInput"
    let make ~resourceArn = fun () -> { resourceArn }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (String_.to_value x.resourceArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~resourceArn ()
    let of_json json =
      let resourceArn = field_map_exn json "resourceArn" String_.of_json in
      make ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Returns a list of tags for a managed device or task."]
module ListExecutionsOutput =
  struct
    type nonrec t =
      {
      executions: ExecutionSummaryList.t option
        [@ocaml.doc
          "A list of executions. Each execution contains the task ID, the device that the task is executing on, the execution ID, and the status of the execution."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A pagination token to continue to the next page of executions."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?executions =
      fun ?nextToken -> fun () -> { executions; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("executions",
           (Option.map x.executions ~f:ExecutionSummaryList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let executions =
        (Option.map ~f:ExecutionSummaryList.of_xml)
          (Xml.child xml_arg0 "executions") in
      make ?nextToken ?executions ()
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let executions =
        field_map json "executions" ExecutionSummaryList.of_json in
      make ?nextToken ?executions ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the status of tasks for one or more target devices."]
module ListExecutionsInput =
  struct
    type nonrec t =
      {
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of tasks to list per page."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A pagination token to continue to the next page of tasks."];
      state: ExecutionState.t option
        [@ocaml.doc
          "A structure used to filter the tasks by their current state."];
      taskId: TaskId.t [@ocaml.doc "The ID of the task."]}
    let context_ = "ListExecutionsInput"
    let make ?maxResults =
      fun ?nextToken ->
        fun ?state ->
          fun ~taskId -> fun () -> { maxResults; nextToken; state; taskId }
    let to_value x =
      structure_to_value
        [("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("state", (Option.map x.state ~f:ExecutionState.to_value));
        ("taskId", (Some (TaskId.to_value x.taskId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let taskId =
        TaskId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "taskId") in
      let state =
        (Option.map ~f:ExecutionState.of_xml) (Xml.child xml_arg0 "state") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      make ~taskId ?state ?nextToken ?maxResults ()
    let of_json json =
      let taskId = field_map_exn json "taskId" TaskId.of_json in
      let state = field_map json "state" ExecutionState.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      make ~taskId ?state ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the status of tasks for one or more target devices."]
module ListDevicesOutput =
  struct
    type nonrec t =
      {
      devices: DeviceSummaryList.t option
        [@ocaml.doc
          "A list of device structures that contain information about the device."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A pagination token to continue to the next page of devices."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?devices = fun ?nextToken -> fun () -> { devices; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("devices", (Option.map x.devices ~f:DeviceSummaryList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let devices =
        (Option.map ~f:DeviceSummaryList.of_xml)
          (Xml.child xml_arg0 "devices") in
      make ?nextToken ?devices ()
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let devices = field_map json "devices" DeviceSummaryList.of_json in
      make ?nextToken ?devices ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of all devices on your Amazon Web Services account that have Amazon Web Services Snow Device Management enabled in the Amazon Web Services Region where the command is run."]
module ListDevicesInput =
  struct
    type nonrec t =
      {
      jobId: JobId.t option
        [@ocaml.doc "The ID of the job used to order the device."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of devices to list per page."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A pagination token to continue to the next page of results."]}
    let make ?jobId =
      fun ?maxResults ->
        fun ?nextToken -> fun () -> { jobId; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("jobId", (Option.map x.jobId ~f:JobId.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let jobId = (Option.map ~f:JobId.of_xml) (Xml.child xml_arg0 "jobId") in
      make ?nextToken ?maxResults ?jobId ()
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let jobId = field_map json "jobId" JobId.of_json in
      make ?nextToken ?maxResults ?jobId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of all devices on your Amazon Web Services account that have Amazon Web Services Snow Device Management enabled in the Amazon Web Services Region where the command is run."]
module ListDeviceResourcesOutput =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "A pagination token to continue to the next page of results."];
      resources: ResourceSummaryList.t option
        [@ocaml.doc
          "A structure defining the resource's type, Amazon Resource Name (ARN), and ID."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken =
      fun ?resources -> fun () -> { nextToken; resources }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("resources",
          (Option.map x.resources ~f:ResourceSummaryList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resources =
        (Option.map ~f:ResourceSummaryList.of_xml)
          (Xml.child xml_arg0 "resources") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?resources ?nextToken ()
    let of_json json =
      let resources = field_map json "resources" ResourceSummaryList.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      make ?resources ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of the Amazon Web Services resources available for a device. Currently, Amazon EC2 instances are the only supported resource type."]
module ListDeviceResourcesInput =
  struct
    type nonrec t =
      {
      managedDeviceId: ManagedDeviceId.t
        [@ocaml.doc
          "The ID of the managed device that you are listing the resources of."];
      maxResults: MaxResults.t option
        [@ocaml.doc "The maximum number of resources per page."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A pagination token to continue to the next page of results."];
      type_: ListDeviceResourcesInputTypeString.t option
        [@ocaml.doc
          "A structure used to filter the results by type of resource."]}
    let context_ = "ListDeviceResourcesInput"
    let make ?maxResults =
      fun ?nextToken ->
        fun ?type_ ->
          fun ~managedDeviceId ->
            fun () -> { maxResults; nextToken; type_; managedDeviceId }
    let to_value x =
      structure_to_value
        [("managedDeviceId",
           (Some (ManagedDeviceId.to_value x.managedDeviceId)));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("type",
          (Option.map x.type_ ~f:ListDeviceResourcesInputTypeString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:ListDeviceResourcesInputTypeString.of_xml)
          (Xml.child xml_arg0 "type") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let managedDeviceId =
        ManagedDeviceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "managedDeviceId") in
      make ?type_ ?nextToken ?maxResults ~managedDeviceId ()
    let of_json json =
      let type_ =
        field_map json "type" ListDeviceResourcesInputTypeString.of_json in
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      let managedDeviceId =
        field_map_exn json "managedDeviceId" ManagedDeviceId.of_json in
      make ?type_ ?nextToken ?maxResults ~managedDeviceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of the Amazon Web Services resources available for a device. Currently, Amazon EC2 instances are the only supported resource type."]
module DescribeTaskOutput =
  struct
    type nonrec t =
      {
      completedAt: Timestamp.t option
        [@ocaml.doc "When the task was completed."];
      createdAt: Timestamp.t option
        [@ocaml.doc "When the CreateTask operation was called."];
      description: TaskDescriptionString.t option
        [@ocaml.doc
          "The description provided of the task and managed devices."];
      lastUpdatedAt: Timestamp.t option
        [@ocaml.doc "When the state of the task was last updated."];
      state: TaskState.t option [@ocaml.doc "The current state of the task."];
      tags: TagMap.t option
        [@ocaml.doc
          "Optional metadata that you assign to a resource. You can use tags to categorize a resource in different ways, such as by purpose, owner, or environment."];
      targets: TargetList.t option
        [@ocaml.doc "The managed devices that the task was sent to."];
      taskArn: String_.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the task."];
      taskId: String_.t option [@ocaml.doc "The ID of the task."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?completedAt =
      fun ?createdAt ->
        fun ?description ->
          fun ?lastUpdatedAt ->
            fun ?state ->
              fun ?tags ->
                fun ?targets ->
                  fun ?taskArn ->
                    fun ?taskId ->
                      fun () ->
                        {
                          completedAt;
                          createdAt;
                          description;
                          lastUpdatedAt;
                          state;
                          tags;
                          targets;
                          taskArn;
                          taskId
                        }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("completedAt", (Option.map x.completedAt ~f:Timestamp.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("description",
          (Option.map x.description ~f:TaskDescriptionString.to_value));
        ("lastUpdatedAt", (Option.map x.lastUpdatedAt ~f:Timestamp.to_value));
        ("state", (Option.map x.state ~f:TaskState.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value));
        ("targets", (Option.map x.targets ~f:TargetList.to_value));
        ("taskArn", (Option.map x.taskArn ~f:String_.to_value));
        ("taskId", (Option.map x.taskId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let taskId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "taskId") in
      let taskArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "taskArn") in
      let targets =
        (Option.map ~f:TargetList.of_xml) (Xml.child xml_arg0 "targets") in
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let state =
        (Option.map ~f:TaskState.of_xml) (Xml.child xml_arg0 "state") in
      let lastUpdatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastUpdatedAt") in
      let description =
        (Option.map ~f:TaskDescriptionString.of_xml)
          (Xml.child xml_arg0 "description") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let completedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "completedAt") in
      make ?taskId ?taskArn ?targets ?tags ?state ?lastUpdatedAt ?description
        ?createdAt ?completedAt ()
    let of_json json =
      let taskId = field_map json "taskId" String_.of_json in
      let taskArn = field_map json "taskArn" String_.of_json in
      let targets = field_map json "targets" TargetList.of_json in
      let tags = field_map json "tags" TagMap.of_json in
      let state = field_map json "state" TaskState.of_json in
      let lastUpdatedAt = field_map json "lastUpdatedAt" Timestamp.of_json in
      let description =
        field_map json "description" TaskDescriptionString.of_json in
      let createdAt = field_map json "createdAt" Timestamp.of_json in
      let completedAt = field_map json "completedAt" Timestamp.of_json in
      make ?taskId ?taskArn ?targets ?tags ?state ?lastUpdatedAt ?description
        ?createdAt ?completedAt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Checks the metadata for a given task on a device."]
module DescribeTaskInput =
  struct
    type nonrec t =
      {
      taskId: TaskId.t [@ocaml.doc "The ID of the task to be described."]}
    let context_ = "DescribeTaskInput"
    let make ~taskId = fun () -> { taskId }
    let to_value x =
      structure_to_value [("taskId", (Some (TaskId.to_value x.taskId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let taskId =
        TaskId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "taskId") in
      make ~taskId ()
    let of_json json =
      let taskId = field_map_exn json "taskId" TaskId.of_json in
      make ~taskId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Checks the metadata for a given task on a device."]
module DescribeExecutionOutput =
  struct
    type nonrec t =
      {
      executionId: ExecutionId.t option
        [@ocaml.doc "The ID of the execution."];
      lastUpdatedAt: Timestamp.t option
        [@ocaml.doc "When the status of the execution was last updated."];
      managedDeviceId: ManagedDeviceId.t option
        [@ocaml.doc
          "The ID of the managed device that the task is being executed on."];
      startedAt: Timestamp.t option [@ocaml.doc "When the execution began."];
      state: ExecutionState.t option
        [@ocaml.doc "The current state of the execution."];
      taskId: TaskId.t option
        [@ocaml.doc "The ID of the task being executed on the device."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?executionId =
      fun ?lastUpdatedAt ->
        fun ?managedDeviceId ->
          fun ?startedAt ->
            fun ?state ->
              fun ?taskId ->
                fun () ->
                  {
                    executionId;
                    lastUpdatedAt;
                    managedDeviceId;
                    startedAt;
                    state;
                    taskId
                  }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("executionId", (Option.map x.executionId ~f:ExecutionId.to_value));
        ("lastUpdatedAt", (Option.map x.lastUpdatedAt ~f:Timestamp.to_value));
        ("managedDeviceId",
          (Option.map x.managedDeviceId ~f:ManagedDeviceId.to_value));
        ("startedAt", (Option.map x.startedAt ~f:Timestamp.to_value));
        ("state", (Option.map x.state ~f:ExecutionState.to_value));
        ("taskId", (Option.map x.taskId ~f:TaskId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let taskId =
        (Option.map ~f:TaskId.of_xml) (Xml.child xml_arg0 "taskId") in
      let state =
        (Option.map ~f:ExecutionState.of_xml) (Xml.child xml_arg0 "state") in
      let startedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "startedAt") in
      let managedDeviceId =
        (Option.map ~f:ManagedDeviceId.of_xml)
          (Xml.child xml_arg0 "managedDeviceId") in
      let lastUpdatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastUpdatedAt") in
      let executionId =
        (Option.map ~f:ExecutionId.of_xml) (Xml.child xml_arg0 "executionId") in
      make ?taskId ?state ?startedAt ?managedDeviceId ?lastUpdatedAt
        ?executionId ()
    let of_json json =
      let taskId = field_map json "taskId" TaskId.of_json in
      let state = field_map json "state" ExecutionState.of_json in
      let startedAt = field_map json "startedAt" Timestamp.of_json in
      let managedDeviceId =
        field_map json "managedDeviceId" ManagedDeviceId.of_json in
      let lastUpdatedAt = field_map json "lastUpdatedAt" Timestamp.of_json in
      let executionId = field_map json "executionId" ExecutionId.of_json in
      make ?taskId ?state ?startedAt ?managedDeviceId ?lastUpdatedAt
        ?executionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Checks the status of a remote task running on one or more target devices."]
module DescribeExecutionInput =
  struct
    type nonrec t =
      {
      managedDeviceId: ManagedDeviceId.t
        [@ocaml.doc "The ID of the managed device."];
      taskId: TaskId.t
        [@ocaml.doc "The ID of the task that the action is describing."]}
    let context_ = "DescribeExecutionInput"
    let make ~managedDeviceId =
      fun ~taskId -> fun () -> { managedDeviceId; taskId }
    let to_value x =
      structure_to_value
        [("managedDeviceId",
           (Some (ManagedDeviceId.to_value x.managedDeviceId)));
        ("taskId", (Some (TaskId.to_value x.taskId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let taskId =
        TaskId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "taskId") in
      let managedDeviceId =
        ManagedDeviceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "managedDeviceId") in
      make ~taskId ~managedDeviceId ()
    let of_json json =
      let taskId = field_map_exn json "taskId" TaskId.of_json in
      let managedDeviceId =
        field_map_exn json "managedDeviceId" ManagedDeviceId.of_json in
      make ~taskId ~managedDeviceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Checks the status of a remote task running on one or more target devices."]
module DescribeDeviceOutput =
  struct
    type nonrec t =
      {
      associatedWithJob: String_.t option
        [@ocaml.doc "The ID of the job used when ordering the device."];
      deviceCapacities: CapacityList.t option
        [@ocaml.doc "The hardware specifications of the device."];
      deviceState: UnlockState.t option
        [@ocaml.doc "The current state of the device."];
      deviceType: String_.t option
        [@ocaml.doc "The type of Amazon Web Services Snow Family device."];
      lastReachedOutAt: Timestamp.t option
        [@ocaml.doc
          "When the device last contacted the Amazon Web Services Cloud. Indicates that the device is online."];
      lastUpdatedAt: Timestamp.t option
        [@ocaml.doc
          "When the device last pushed an update to the Amazon Web Services Cloud. Indicates when the device cache was refreshed."];
      managedDeviceArn: String_.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the device."];
      managedDeviceId: ManagedDeviceId.t option
        [@ocaml.doc
          "The ID of the device that you checked the information for."];
      physicalNetworkInterfaces: PhysicalNetworkInterfaceList.t option
        [@ocaml.doc "The network interfaces available on the device."];
      software: SoftwareInformation.t option
        [@ocaml.doc "The software installed on the device."];
      tags: TagMap.t option
        [@ocaml.doc
          "Optional metadata that you assign to a resource. You can use tags to categorize a resource in different ways, such as by purpose, owner, or environment."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?associatedWithJob =
      fun ?deviceCapacities ->
        fun ?deviceState ->
          fun ?deviceType ->
            fun ?lastReachedOutAt ->
              fun ?lastUpdatedAt ->
                fun ?managedDeviceArn ->
                  fun ?managedDeviceId ->
                    fun ?physicalNetworkInterfaces ->
                      fun ?software ->
                        fun ?tags ->
                          fun () ->
                            {
                              associatedWithJob;
                              deviceCapacities;
                              deviceState;
                              deviceType;
                              lastReachedOutAt;
                              lastUpdatedAt;
                              managedDeviceArn;
                              managedDeviceId;
                              physicalNetworkInterfaces;
                              software;
                              tags
                            }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("associatedWithJob",
           (Option.map x.associatedWithJob ~f:String_.to_value));
        ("deviceCapacities",
          (Option.map x.deviceCapacities ~f:CapacityList.to_value));
        ("deviceState", (Option.map x.deviceState ~f:UnlockState.to_value));
        ("deviceType", (Option.map x.deviceType ~f:String_.to_value));
        ("lastReachedOutAt",
          (Option.map x.lastReachedOutAt ~f:Timestamp.to_value));
        ("lastUpdatedAt", (Option.map x.lastUpdatedAt ~f:Timestamp.to_value));
        ("managedDeviceArn",
          (Option.map x.managedDeviceArn ~f:String_.to_value));
        ("managedDeviceId",
          (Option.map x.managedDeviceId ~f:ManagedDeviceId.to_value));
        ("physicalNetworkInterfaces",
          (Option.map x.physicalNetworkInterfaces
             ~f:PhysicalNetworkInterfaceList.to_value));
        ("software", (Option.map x.software ~f:SoftwareInformation.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let software =
        (Option.map ~f:SoftwareInformation.of_xml)
          (Xml.child xml_arg0 "software") in
      let physicalNetworkInterfaces =
        (Option.map ~f:PhysicalNetworkInterfaceList.of_xml)
          (Xml.child xml_arg0 "physicalNetworkInterfaces") in
      let managedDeviceId =
        (Option.map ~f:ManagedDeviceId.of_xml)
          (Xml.child xml_arg0 "managedDeviceId") in
      let managedDeviceArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "managedDeviceArn") in
      let lastUpdatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "lastUpdatedAt") in
      let lastReachedOutAt =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "lastReachedOutAt") in
      let deviceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "deviceType") in
      let deviceState =
        (Option.map ~f:UnlockState.of_xml) (Xml.child xml_arg0 "deviceState") in
      let deviceCapacities =
        (Option.map ~f:CapacityList.of_xml)
          (Xml.child xml_arg0 "deviceCapacities") in
      let associatedWithJob =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "associatedWithJob") in
      make ?tags ?software ?physicalNetworkInterfaces ?managedDeviceId
        ?managedDeviceArn ?lastUpdatedAt ?lastReachedOutAt ?deviceType
        ?deviceState ?deviceCapacities ?associatedWithJob ()
    let of_json json =
      let tags = field_map json "tags" TagMap.of_json in
      let software = field_map json "software" SoftwareInformation.of_json in
      let physicalNetworkInterfaces =
        field_map json "physicalNetworkInterfaces"
          PhysicalNetworkInterfaceList.of_json in
      let managedDeviceId =
        field_map json "managedDeviceId" ManagedDeviceId.of_json in
      let managedDeviceArn =
        field_map json "managedDeviceArn" String_.of_json in
      let lastUpdatedAt = field_map json "lastUpdatedAt" Timestamp.of_json in
      let lastReachedOutAt =
        field_map json "lastReachedOutAt" Timestamp.of_json in
      let deviceType = field_map json "deviceType" String_.of_json in
      let deviceState = field_map json "deviceState" UnlockState.of_json in
      let deviceCapacities =
        field_map json "deviceCapacities" CapacityList.of_json in
      let associatedWithJob =
        field_map json "associatedWithJob" String_.of_json in
      make ?tags ?software ?physicalNetworkInterfaces ?managedDeviceId
        ?managedDeviceArn ?lastUpdatedAt ?lastReachedOutAt ?deviceType
        ?deviceState ?deviceCapacities ?associatedWithJob ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Checks device-specific information, such as the device type, software version, IP addresses, and lock status."]
module DescribeDeviceInput =
  struct
    type nonrec t =
      {
      managedDeviceId: ManagedDeviceId.t
        [@ocaml.doc
          "The ID of the device that you are checking the information of."]}
    let context_ = "DescribeDeviceInput"
    let make ~managedDeviceId = fun () -> { managedDeviceId }
    let to_value x =
      structure_to_value
        [("managedDeviceId",
           (Some (ManagedDeviceId.to_value x.managedDeviceId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let managedDeviceId =
        ManagedDeviceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "managedDeviceId") in
      make ~managedDeviceId ()
    let of_json json =
      let managedDeviceId =
        field_map_exn json "managedDeviceId" ManagedDeviceId.of_json in
      make ~managedDeviceId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Checks device-specific information, such as the device type, software version, IP addresses, and lock status."]
module DescribeDeviceEc2Output =
  struct
    type nonrec t =
      {
      instances: InstanceSummaryList.t option
        [@ocaml.doc
          "A list of structures containing information about each instance."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?instances = fun () -> { instances }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("instances",
           (Option.map x.instances ~f:InstanceSummaryList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let instances =
        (Option.map ~f:InstanceSummaryList.of_xml)
          (Xml.child xml_arg0 "instances") in
      make ?instances ()
    let of_json json =
      let instances = field_map json "instances" InstanceSummaryList.of_json in
      make ?instances ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Checks the current state of the Amazon EC2 instances. The output is similar to describeDevice, but the results are sourced from the device cache in the Amazon Web Services Cloud and include a subset of the available fields."]
module DescribeDeviceEc2Input =
  struct
    type nonrec t =
      {
      instanceIds: InstanceIdsList.t
        [@ocaml.doc
          "A list of instance IDs associated with the managed device."];
      managedDeviceId: ManagedDeviceId.t
        [@ocaml.doc "The ID of the managed device."]}
    let context_ = "DescribeDeviceEc2Input"
    let make ~instanceIds =
      fun ~managedDeviceId -> fun () -> { instanceIds; managedDeviceId }
    let to_value x =
      structure_to_value
        [("instanceIds", (Some (InstanceIdsList.to_value x.instanceIds)));
        ("managedDeviceId",
          (Some (ManagedDeviceId.to_value x.managedDeviceId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let managedDeviceId =
        ManagedDeviceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "managedDeviceId") in
      let instanceIds =
        InstanceIdsList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "instanceIds") in
      make ~managedDeviceId ~instanceIds ()
    let of_json json =
      let managedDeviceId =
        field_map_exn json "managedDeviceId" ManagedDeviceId.of_json in
      let instanceIds =
        field_map_exn json "instanceIds" InstanceIdsList.of_json in
      make ~managedDeviceId ~instanceIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Checks the current state of the Amazon EC2 instances. The output is similar to describeDevice, but the results are sourced from the device cache in the Amazon Web Services Cloud and include a subset of the available fields."]
module CreateTaskOutput =
  struct
    type nonrec t =
      {
      taskArn: String_.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the task that you created."];
      taskId: String_.t option
        [@ocaml.doc "The ID of the task that you created."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?taskArn = fun ?taskId -> fun () -> { taskArn; taskId }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("taskArn", (Option.map x.taskArn ~f:String_.to_value));
        ("taskId", (Option.map x.taskId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let taskId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "taskId") in
      let taskArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "taskArn") in
      make ?taskId ?taskArn ()
    let of_json json =
      let taskId = field_map json "taskId" String_.of_json in
      let taskArn = field_map json "taskArn" String_.of_json in
      make ?taskId ?taskArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Instructs one or more devices to start a task, such as unlocking or rebooting."]
module CreateTaskInput =
  struct
    type nonrec t =
      {
      clientToken: IdempotencyToken.t option
        [@ocaml.doc
          "A token ensuring that the action is called only once with the specified details."];
      command: Command.t
        [@ocaml.doc
          "The task to be performed. Only one task is executed on a device at a time."];
      description: TaskDescriptionString.t option
        [@ocaml.doc "A description of the task and its targets."];
      tags: TagMap.t option
        [@ocaml.doc
          "Optional metadata that you assign to a resource. You can use tags to categorize a resource in different ways, such as by purpose, owner, or environment."];
      targets: TargetList.t [@ocaml.doc "A list of managed device IDs."]}
    let context_ = "CreateTaskInput"
    let make ?clientToken =
      fun ?description ->
        fun ?tags ->
          fun ~command ->
            fun ~targets ->
              fun () -> { clientToken; description; tags; command; targets }
    let to_value x =
      structure_to_value
        [("clientToken",
           (Option.map x.clientToken ~f:IdempotencyToken.to_value));
        ("command", (Some (Command.to_value x.command)));
        ("description",
          (Option.map x.description ~f:TaskDescriptionString.to_value));
        ("tags", (Option.map x.tags ~f:TagMap.to_value));
        ("targets", (Some (TargetList.to_value x.targets)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let targets =
        TargetList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "targets") in
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "tags") in
      let description =
        (Option.map ~f:TaskDescriptionString.of_xml)
          (Xml.child xml_arg0 "description") in
      let command =
        Command.of_xml (Xml.child_exn ~context:context_ xml_arg0 "command") in
      let clientToken =
        (Option.map ~f:IdempotencyToken.of_xml)
          (Xml.child xml_arg0 "clientToken") in
      make ~targets ?tags ?description ~command ?clientToken ()
    let of_json json =
      let targets = field_map_exn json "targets" TargetList.of_json in
      let tags = field_map json "tags" TagMap.of_json in
      let description =
        field_map json "description" TaskDescriptionString.of_json in
      let command = field_map_exn json "command" Command.of_json in
      let clientToken = field_map json "clientToken" IdempotencyToken.of_json in
      make ~targets ?tags ?description ~command ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Instructs one or more devices to start a task, such as unlocking or rebooting."]
module CancelTaskOutput =
  struct
    type nonrec t =
      {
      taskId: String_.t option
        [@ocaml.doc "The ID of the task that you are attempting to cancel."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?taskId = fun () -> { taskId }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("taskId", (Option.map x.taskId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let taskId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "taskId") in
      make ?taskId ()
    let of_json json =
      let taskId = field_map json "taskId" String_.of_json in make ?taskId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Sends a cancel request for a specified task. You can cancel a task only if it's still in a QUEUED state. Tasks that are already running can't be cancelled. A task might still run if it's processed from the queue before the CancelTask operation changes the task's state."]
module CancelTaskInput =
  struct
    type nonrec t =
      {
      taskId: TaskId.t
        [@ocaml.doc
          "The ID of the task that you are attempting to cancel. You can retrieve a task ID by using the ListTasks operation."]}
    let context_ = "CancelTaskInput"
    let make ~taskId = fun () -> { taskId }
    let to_value x =
      structure_to_value [("taskId", (Some (TaskId.to_value x.taskId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let taskId =
        TaskId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "taskId") in
      make ~taskId ()
    let of_json json =
      let taskId = field_map_exn json "taskId" TaskId.of_json in
      make ~taskId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Sends a cancel request for a specified task. You can cancel a task only if it's still in a QUEUED state. Tasks that are already running can't be cancelled. A task might still run if it's processed from the queue before the CancelTask operation changes the task's state."]
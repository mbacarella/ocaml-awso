(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let cancel_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskId = flag "task-id" (required string) ~doc:"STRING TaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_task (Values.CancelTaskInput.make ~taskId ())
           (Some Values.CancelTaskOutput.to_json)
           (Some Values.CancelTaskOutput.error_to_json)])
let create_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING IdempotencyToken"
       and description =
         flag "description" (optional string)
           ~doc:"STRING TaskDescriptionString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and command = flag "command" (required json_arg) ~doc:"JSON Command"
       and targets =
         flag "targets" (required json_arg) ~doc:"JSON TargetList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_task
           (Values.CreateTaskInput.make ?clientToken ?description
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~command:(Values.Command.of_json command)
              ~targets:(Values.TargetList.of_json targets) ())
           (Some Values.CreateTaskOutput.to_json)
           (Some Values.CreateTaskOutput.error_to_json)])
let describe_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and managedDeviceId =
         flag "managed-device-id" (required string)
           ~doc:"STRING ManagedDeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_device
           (Values.DescribeDeviceInput.make ~managedDeviceId ())
           (Some Values.DescribeDeviceOutput.to_json)
           (Some Values.DescribeDeviceOutput.error_to_json)])
let describe_device_ec2_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceIds =
         flag "instance-ids" (required json_arg) ~doc:"JSON InstanceIdsList"
       and managedDeviceId =
         flag "managed-device-id" (required string)
           ~doc:"STRING ManagedDeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_device_ec2_instances
           (Values.DescribeDeviceEc2Input.make
              ~instanceIds:(Values.InstanceIdsList.of_json instanceIds)
              ~managedDeviceId ())
           (Some Values.DescribeDeviceEc2Output.to_json)
           (Some Values.DescribeDeviceEc2Output.error_to_json)])
let describe_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and managedDeviceId =
         flag "managed-device-id" (required string)
           ~doc:"STRING ManagedDeviceId"
       and taskId = flag "task-id" (required string) ~doc:"STRING TaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_execution
           (Values.DescribeExecutionInput.make ~managedDeviceId ~taskId ())
           (Some Values.DescribeExecutionOutput.to_json)
           (Some Values.DescribeExecutionOutput.error_to_json)])
let describe_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and taskId = flag "task-id" (required string) ~doc:"STRING TaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_task (Values.DescribeTaskInput.make ~taskId ())
           (Some Values.DescribeTaskOutput.to_json)
           (Some Values.DescribeTaskOutput.error_to_json)])
let list_device_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and type_ =
         flag "type-" (optional string)
           ~doc:"STRING ListDeviceResourcesInputTypeString"
       and managedDeviceId =
         flag "managed-device-id" (required string)
           ~doc:"STRING ManagedDeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_device_resources
           (Values.ListDeviceResourcesInput.make ?maxResults ?nextToken
              ?type_ ~managedDeviceId ())
           (Some Values.ListDeviceResourcesOutput.to_json)
           (Some Values.ListDeviceResourcesOutput.error_to_json)])
let list_devices =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (optional string) ~doc:"STRING JobId"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_devices
           (Values.ListDevicesInput.make ?jobId ?maxResults ?nextToken ())
           (Some Values.ListDevicesOutput.to_json)
           (Some Values.ListDevicesOutput.error_to_json)])
let list_executions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and state =
         flag "state" (optional json_arg) ~doc:"JSON ExecutionState"
       and taskId = flag "task-id" (required string) ~doc:"STRING TaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_executions
           (Values.ListExecutionsInput.make ?maxResults ?nextToken
              ?state:(Option.map ~f:Values.ExecutionState.of_json state)
              ~taskId ()) (Some Values.ListExecutionsOutput.to_json)
           (Some Values.ListExecutionsOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let list_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and state = flag "state" (optional json_arg) ~doc:"JSON TaskState" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tasks
           (Values.ListTasksInput.make ?maxResults ?nextToken
              ?state:(Option.map ~f:Values.TaskState.of_json state) ())
           (Some Values.ListTasksOutput.to_json)
           (Some Values.ListTasksOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ()) None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ()) None None])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("cancel-task", cancel_task);
    ("create-task", create_task);
    ("describe-device", describe_device);
    ("describe-device-ec2-instances", describe_device_ec2_instances);
    ("describe-execution", describe_execution);
    ("describe-task", describe_task);
    ("list-device-resources", list_device_resources);
    ("list-devices", list_devices);
    ("list-executions", list_executions);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-tasks", list_tasks);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_member =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_member
           (Values.AssociateMemberRequest.make ~accountId ())
           (Some Values.AssociateMemberResponse.to_json)
           (Some Values.AssociateMemberResponse.error_to_json)])
let batch_get_account_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIds =
         flag "account-ids" (optional json_arg) ~doc:"JSON AccountIdSet" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_account_status
           (Values.BatchGetAccountStatusRequest.make
              ?accountIds:(Option.map ~f:Values.AccountIdSet.of_json
                             accountIds) ())
           (Some Values.BatchGetAccountStatusResponse.to_json)
           (Some Values.BatchGetAccountStatusResponse.error_to_json)])
let batch_get_free_trial_info =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIds =
         flag "account-ids" (required json_arg)
           ~doc:"JSON BatchGetFreeTrialInfoRequestAccountIdsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_free_trial_info
           (Values.BatchGetFreeTrialInfoRequest.make
              ~accountIds:(Values.BatchGetFreeTrialInfoRequestAccountIdsList.of_json
                             accountIds) ())
           (Some Values.BatchGetFreeTrialInfoResponse.to_json)
           (Some Values.BatchGetFreeTrialInfoResponse.error_to_json)])
let cancel_findings_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reportId =
         flag "report-id" (required string) ~doc:"STRING ReportId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_findings_report
           (Values.CancelFindingsReportRequest.make ~reportId ())
           (Some Values.CancelFindingsReportResponse.to_json)
           (Some Values.CancelFindingsReportResponse.error_to_json)])
let create_filter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING FilterDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and action =
         flag "action" (required json_arg) ~doc:"JSON FilterAction"
       and filterCriteria =
         flag "filter-criteria" (required json_arg)
           ~doc:"JSON FilterCriteria"
       and name = flag "name" (required string) ~doc:"STRING FilterName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_filter
           (Values.CreateFilterRequest.make ?description
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~action:(Values.FilterAction.of_json action)
              ~filterCriteria:(Values.FilterCriteria.of_json filterCriteria)
              ~name ()) (Some Values.CreateFilterResponse.to_json)
           (Some Values.CreateFilterResponse.error_to_json)])
let create_findings_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterCriteria =
         flag "filter-criteria" (optional json_arg)
           ~doc:"JSON FilterCriteria"
       and reportFormat =
         flag "report-format" (required json_arg) ~doc:"JSON ReportFormat"
       and s3Destination =
         flag "s3-destination" (required json_arg) ~doc:"JSON Destination" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_findings_report
           (Values.CreateFindingsReportRequest.make
              ?filterCriteria:(Option.map ~f:Values.FilterCriteria.of_json
                                 filterCriteria)
              ~reportFormat:(Values.ReportFormat.of_json reportFormat)
              ~s3Destination:(Values.Destination.of_json s3Destination) ())
           (Some Values.CreateFindingsReportResponse.to_json)
           (Some Values.CreateFindingsReportResponse.error_to_json)])
let delete_filter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING FilterArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_filter (Values.DeleteFilterRequest.make ~arn ())
           (Some Values.DeleteFilterResponse.to_json)
           (Some Values.DeleteFilterResponse.error_to_json)])
let describe_organization_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_organization_configuration
           (Values.DescribeOrganizationConfigurationRequest.make ())
           (Some Values.DescribeOrganizationConfigurationResponse.to_json)
           (Some
              Values.DescribeOrganizationConfigurationResponse.error_to_json)])
let disable =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIds =
         flag "account-ids" (optional json_arg) ~doc:"JSON AccountIdSet"
       and resourceTypes =
         flag "resource-types" (optional json_arg)
           ~doc:"JSON DisableResourceTypeList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable
           (Values.DisableRequest.make
              ?accountIds:(Option.map ~f:Values.AccountIdSet.of_json
                             accountIds)
              ?resourceTypes:(Option.map
                                ~f:Values.DisableResourceTypeList.of_json
                                resourceTypes) ())
           (Some Values.DisableResponse.to_json)
           (Some Values.DisableResponse.error_to_json)])
let disable_delegated_admin_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and delegatedAdminAccountId =
         flag "delegated-admin-account-id" (required string)
           ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_delegated_admin_account
           (Values.DisableDelegatedAdminAccountRequest.make
              ~delegatedAdminAccountId ())
           (Some Values.DisableDelegatedAdminAccountResponse.to_json)
           (Some Values.DisableDelegatedAdminAccountResponse.error_to_json)])
let disassociate_member =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_member
           (Values.DisassociateMemberRequest.make ~accountId ())
           (Some Values.DisassociateMemberResponse.to_json)
           (Some Values.DisassociateMemberResponse.error_to_json)])
let enable =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIds =
         flag "account-ids" (optional json_arg) ~doc:"JSON AccountIdSet"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and resourceTypes =
         flag "resource-types" (required json_arg)
           ~doc:"JSON EnableResourceTypeList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region Io.enable
           (Values.EnableRequest.make
              ?accountIds:(Option.map ~f:Values.AccountIdSet.of_json
                             accountIds) ?clientToken
              ~resourceTypes:(Values.EnableResourceTypeList.of_json
                                resourceTypes) ())
           (Some Values.EnableResponse.to_json)
           (Some Values.EnableResponse.error_to_json)])
let enable_delegated_admin_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and delegatedAdminAccountId =
         flag "delegated-admin-account-id" (required string)
           ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_delegated_admin_account
           (Values.EnableDelegatedAdminAccountRequest.make ?clientToken
              ~delegatedAdminAccountId ())
           (Some Values.EnableDelegatedAdminAccountResponse.to_json)
           (Some Values.EnableDelegatedAdminAccountResponse.error_to_json)])
let get_delegated_admin_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_delegated_admin_account
           (Values.GetDelegatedAdminAccountRequest.make ())
           (Some Values.GetDelegatedAdminAccountResponse.to_json)
           (Some Values.GetDelegatedAdminAccountResponse.error_to_json)])
let get_findings_report_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reportId =
         flag "report-id" (optional string) ~doc:"STRING ReportId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_findings_report_status
           (Values.GetFindingsReportStatusRequest.make ?reportId ())
           (Some Values.GetFindingsReportStatusResponse.to_json)
           (Some Values.GetFindingsReportStatusResponse.error_to_json)])
let get_member =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING AccountId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_member (Values.GetMemberRequest.make ~accountId ())
           (Some Values.GetMemberResponse.to_json)
           (Some Values.GetMemberResponse.error_to_json)])
let list_account_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListAccountPermissionsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and service = flag "service" (optional json_arg) ~doc:"JSON Service" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_account_permissions
           (Values.ListAccountPermissionsRequest.make ?maxResults ?nextToken
              ?service:(Option.map ~f:Values.Service.of_json service) ())
           (Some Values.ListAccountPermissionsResponse.to_json)
           (Some Values.ListAccountPermissionsResponse.error_to_json)])
let list_coverage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterCriteria =
         flag "filter-criteria" (optional json_arg)
           ~doc:"JSON CoverageFilterCriteria"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListCoverageMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_coverage
           (Values.ListCoverageRequest.make
              ?filterCriteria:(Option.map
                                 ~f:Values.CoverageFilterCriteria.of_json
                                 filterCriteria) ?maxResults ?nextToken ())
           (Some Values.ListCoverageResponse.to_json)
           (Some Values.ListCoverageResponse.error_to_json)])
let list_coverage_statistics =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterCriteria =
         flag "filter-criteria" (optional json_arg)
           ~doc:"JSON CoverageFilterCriteria"
       and groupBy = flag "group-by" (optional json_arg) ~doc:"JSON GroupKey"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_coverage_statistics
           (Values.ListCoverageStatisticsRequest.make
              ?filterCriteria:(Option.map
                                 ~f:Values.CoverageFilterCriteria.of_json
                                 filterCriteria)
              ?groupBy:(Option.map ~f:Values.GroupKey.of_json groupBy)
              ?nextToken ())
           (Some Values.ListCoverageStatisticsResponse.to_json)
           (Some Values.ListCoverageStatisticsResponse.error_to_json)])
let list_delegated_admin_accounts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListDelegatedAdminMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_delegated_admin_accounts
           (Values.ListDelegatedAdminAccountsRequest.make ?maxResults
              ?nextToken ())
           (Some Values.ListDelegatedAdminAccountsResponse.to_json)
           (Some Values.ListDelegatedAdminAccountsResponse.error_to_json)])
let list_filters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and action =
         flag "action" (optional json_arg) ~doc:"JSON FilterAction"
       and arns = flag "arns" (optional json_arg) ~doc:"JSON FilterArnList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListFilterMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_filters
           (Values.ListFiltersRequest.make
              ?action:(Option.map ~f:Values.FilterAction.of_json action)
              ?arns:(Option.map ~f:Values.FilterArnList.of_json arns)
              ?maxResults ?nextToken ())
           (Some Values.ListFiltersResponse.to_json)
           (Some Values.ListFiltersResponse.error_to_json)])
let list_finding_aggregations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIds =
         flag "account-ids" (optional json_arg) ~doc:"JSON StringFilterList"
       and aggregationRequest =
         flag "aggregation-request" (optional json_arg)
           ~doc:"JSON AggregationRequest"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListFindingAggregationsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and aggregationType =
         flag "aggregation-type" (required json_arg)
           ~doc:"JSON AggregationType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_finding_aggregations
           (Values.ListFindingAggregationsRequest.make
              ?accountIds:(Option.map ~f:Values.StringFilterList.of_json
                             accountIds)
              ?aggregationRequest:(Option.map
                                     ~f:Values.AggregationRequest.of_json
                                     aggregationRequest) ?maxResults
              ?nextToken
              ~aggregationType:(Values.AggregationType.of_json
                                  aggregationType) ())
           (Some Values.ListFindingAggregationsResponse.to_json)
           (Some Values.ListFindingAggregationsResponse.error_to_json)])
let list_findings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterCriteria =
         flag "filter-criteria" (optional json_arg)
           ~doc:"JSON FilterCriteria"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListFindingsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and sortCriteria =
         flag "sort-criteria" (optional json_arg) ~doc:"JSON SortCriteria" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_findings
           (Values.ListFindingsRequest.make
              ?filterCriteria:(Option.map ~f:Values.FilterCriteria.of_json
                                 filterCriteria) ?maxResults ?nextToken
              ?sortCriteria:(Option.map ~f:Values.SortCriteria.of_json
                               sortCriteria) ())
           (Some Values.ListFindingsResponse.to_json)
           (Some Values.ListFindingsResponse.error_to_json)])
let list_members =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListMembersMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and onlyAssociated =
         flag "only-associated" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_members
           (Values.ListMembersRequest.make ?maxResults ?nextToken
              ?onlyAssociated ()) (Some Values.ListMembersResponse.to_json)
           (Some Values.ListMembersResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_usage_totals =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIds =
         flag "account-ids" (optional json_arg)
           ~doc:"JSON UsageAccountIdList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListUsageTotalsMaxResults"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListUsageTotalsNextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_usage_totals
           (Values.ListUsageTotalsRequest.make
              ?accountIds:(Option.map ~f:Values.UsageAccountIdList.of_json
                             accountIds) ?maxResults ?nextToken ())
           (Some Values.ListUsageTotalsResponse.to_json)
           (Some Values.ListUsageTotalsResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_filter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and action =
         flag "action" (optional json_arg) ~doc:"JSON FilterAction"
       and description =
         flag "description" (optional string) ~doc:"STRING FilterDescription"
       and filterCriteria =
         flag "filter-criteria" (optional json_arg)
           ~doc:"JSON FilterCriteria"
       and name = flag "name" (optional string) ~doc:"STRING FilterName"
       and filterArn =
         flag "filter-arn" (required string) ~doc:"STRING FilterArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_filter
           (Values.UpdateFilterRequest.make
              ?action:(Option.map ~f:Values.FilterAction.of_json action)
              ?description
              ?filterCriteria:(Option.map ~f:Values.FilterCriteria.of_json
                                 filterCriteria) ?name ~filterArn ())
           (Some Values.UpdateFilterResponse.to_json)
           (Some Values.UpdateFilterResponse.error_to_json)])
let update_organization_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoEnable =
         flag "auto-enable" (required json_arg) ~doc:"JSON AutoEnable" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_organization_configuration
           (Values.UpdateOrganizationConfigurationRequest.make
              ~autoEnable:(Values.AutoEnable.of_json autoEnable) ())
           (Some Values.UpdateOrganizationConfigurationResponse.to_json)
           (Some Values.UpdateOrganizationConfigurationResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-member", associate_member);
    ("batch-get-account-status", batch_get_account_status);
    ("batch-get-free-trial-info", batch_get_free_trial_info);
    ("cancel-findings-report", cancel_findings_report);
    ("create-filter", create_filter);
    ("create-findings-report", create_findings_report);
    ("delete-filter", delete_filter);
    ("describe-organization-configuration",
      describe_organization_configuration);
    ("disable", disable);
    ("disable-delegated-admin-account", disable_delegated_admin_account);
    ("disassociate-member", disassociate_member);
    ("enable", enable);
    ("enable-delegated-admin-account", enable_delegated_admin_account);
    ("get-delegated-admin-account", get_delegated_admin_account);
    ("get-findings-report-status", get_findings_report_status);
    ("get-member", get_member);
    ("list-account-permissions", list_account_permissions);
    ("list-coverage", list_coverage);
    ("list-coverage-statistics", list_coverage_statistics);
    ("list-delegated-admin-accounts", list_delegated_admin_accounts);
    ("list-filters", list_filters);
    ("list-finding-aggregations", list_finding_aggregations);
    ("list-findings", list_findings);
    ("list-members", list_members);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-usage-totals", list_usage_totals);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-filter", update_filter);
    ("update-organization-configuration", update_organization_configuration)]
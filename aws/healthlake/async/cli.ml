(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_f_h_i_r_datastore =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datastoreName =
         flag "datastore-name" (optional string) ~doc:"STRING DatastoreName"
       and sseConfiguration =
         flag "sse-configuration" (optional json_arg)
           ~doc:"JSON SseConfiguration"
       and preloadDataConfig =
         flag "preload-data-config" (optional json_arg)
           ~doc:"JSON PreloadDataConfig"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING ClientTokenString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and identityProviderConfiguration =
         flag "identity-provider-configuration" (optional json_arg)
           ~doc:"JSON IdentityProviderConfiguration"
       and datastoreTypeVersion =
         flag "datastore-type-version" (required json_arg)
           ~doc:"JSON FHIRVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_f_h_i_r_datastore
           (Values.CreateFHIRDatastoreRequest.make ?datastoreName
              ?sseConfiguration:(Option.map
                                   ~f:Values.SseConfiguration.of_json
                                   sseConfiguration)
              ?preloadDataConfig:(Option.map
                                    ~f:Values.PreloadDataConfig.of_json
                                    preloadDataConfig) ?clientToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?identityProviderConfiguration:(Option.map
                                                ~f:Values.IdentityProviderConfiguration.of_json
                                                identityProviderConfiguration)
              ~datastoreTypeVersion:(Values.FHIRVersion.of_json
                                       datastoreTypeVersion) ())
           (Some Values.CreateFHIRDatastoreResponse.to_json)
           (Some Values.CreateFHIRDatastoreResponse.error_to_json)])
let delete_f_h_i_r_datastore =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datastoreId =
         flag "datastore-id" (required string) ~doc:"STRING DatastoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_f_h_i_r_datastore
           (Values.DeleteFHIRDatastoreRequest.make ~datastoreId ())
           (Some Values.DeleteFHIRDatastoreResponse.to_json)
           (Some Values.DeleteFHIRDatastoreResponse.error_to_json)])
let describe_f_h_i_r_datastore =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datastoreId =
         flag "datastore-id" (required string) ~doc:"STRING DatastoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_f_h_i_r_datastore
           (Values.DescribeFHIRDatastoreRequest.make ~datastoreId ())
           (Some Values.DescribeFHIRDatastoreResponse.to_json)
           (Some Values.DescribeFHIRDatastoreResponse.error_to_json)])
let describe_f_h_i_r_export_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datastoreId =
         flag "datastore-id" (required string) ~doc:"STRING DatastoreId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_f_h_i_r_export_job
           (Values.DescribeFHIRExportJobRequest.make ~datastoreId ~jobId ())
           (Some Values.DescribeFHIRExportJobResponse.to_json)
           (Some Values.DescribeFHIRExportJobResponse.error_to_json)])
let describe_f_h_i_r_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datastoreId =
         flag "datastore-id" (required string) ~doc:"STRING DatastoreId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_f_h_i_r_import_job
           (Values.DescribeFHIRImportJobRequest.make ~datastoreId ~jobId ())
           (Some Values.DescribeFHIRImportJobResponse.to_json)
           (Some Values.DescribeFHIRImportJobResponse.error_to_json)])
let list_f_h_i_r_datastores =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON DatastoreFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_f_h_i_r_datastores
           (Values.ListFHIRDatastoresRequest.make
              ?filter:(Option.map ~f:Values.DatastoreFilter.of_json filter)
              ?nextToken ?maxResults ())
           (Some Values.ListFHIRDatastoresResponse.to_json)
           (Some Values.ListFHIRDatastoresResponse.error_to_json)])
let list_f_h_i_r_export_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger"
       and jobName = flag "job-name" (optional string) ~doc:"STRING JobName"
       and jobStatus =
         flag "job-status" (optional json_arg) ~doc:"JSON JobStatus"
       and submittedBefore =
         flag "submitted-before" (optional json_arg) ~doc:"JSON Timestamp"
       and submittedAfter =
         flag "submitted-after" (optional json_arg) ~doc:"JSON Timestamp"
       and datastoreId =
         flag "datastore-id" (required string) ~doc:"STRING DatastoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_f_h_i_r_export_jobs
           (Values.ListFHIRExportJobsRequest.make ?nextToken ?maxResults
              ?jobName
              ?jobStatus:(Option.map ~f:Values.JobStatus.of_json jobStatus)
              ?submittedBefore:(Option.map ~f:Values.Timestamp.of_json
                                  submittedBefore)
              ?submittedAfter:(Option.map ~f:Values.Timestamp.of_json
                                 submittedAfter) ~datastoreId ())
           (Some Values.ListFHIRExportJobsResponse.to_json)
           (Some Values.ListFHIRExportJobsResponse.error_to_json)])
let list_f_h_i_r_import_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger"
       and jobName = flag "job-name" (optional string) ~doc:"STRING JobName"
       and jobStatus =
         flag "job-status" (optional json_arg) ~doc:"JSON JobStatus"
       and submittedBefore =
         flag "submitted-before" (optional json_arg) ~doc:"JSON Timestamp"
       and submittedAfter =
         flag "submitted-after" (optional json_arg) ~doc:"JSON Timestamp"
       and datastoreId =
         flag "datastore-id" (required string) ~doc:"STRING DatastoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_f_h_i_r_import_jobs
           (Values.ListFHIRImportJobsRequest.make ?nextToken ?maxResults
              ?jobName
              ?jobStatus:(Option.map ~f:Values.JobStatus.of_json jobStatus)
              ?submittedBefore:(Option.map ~f:Values.Timestamp.of_json
                                  submittedBefore)
              ?submittedAfter:(Option.map ~f:Values.Timestamp.of_json
                                 submittedAfter) ~datastoreId ())
           (Some Values.ListFHIRImportJobsResponse.to_json)
           (Some Values.ListFHIRImportJobsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let start_f_h_i_r_export_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobName = flag "job-name" (optional string) ~doc:"STRING JobName"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING ClientTokenString"
       and outputDataConfig =
         flag "output-data-config" (required json_arg)
           ~doc:"JSON OutputDataConfig"
       and datastoreId =
         flag "datastore-id" (required string) ~doc:"STRING DatastoreId"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (required string)
           ~doc:"STRING IamRoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_f_h_i_r_export_job
           (Values.StartFHIRExportJobRequest.make ?jobName ?clientToken
              ~outputDataConfig:(Values.OutputDataConfig.of_json
                                   outputDataConfig) ~datastoreId
              ~dataAccessRoleArn ())
           (Some Values.StartFHIRExportJobResponse.to_json)
           (Some Values.StartFHIRExportJobResponse.error_to_json)])
let start_f_h_i_r_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobName = flag "job-name" (optional string) ~doc:"STRING JobName"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING ClientTokenString"
       and validationLevel =
         flag "validation-level" (optional json_arg)
           ~doc:"JSON ValidationLevel"
       and inputDataConfig =
         flag "input-data-config" (required json_arg)
           ~doc:"JSON InputDataConfig"
       and jobOutputDataConfig =
         flag "job-output-data-config" (required json_arg)
           ~doc:"JSON OutputDataConfig"
       and datastoreId =
         flag "datastore-id" (required string) ~doc:"STRING DatastoreId"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (required string)
           ~doc:"STRING IamRoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_f_h_i_r_import_job
           (Values.StartFHIRImportJobRequest.make ?jobName ?clientToken
              ?validationLevel:(Option.map ~f:Values.ValidationLevel.of_json
                                  validationLevel)
              ~inputDataConfig:(Values.InputDataConfig.of_json
                                  inputDataConfig)
              ~jobOutputDataConfig:(Values.OutputDataConfig.of_json
                                      jobOutputDataConfig) ~datastoreId
              ~dataAccessRoleArn ())
           (Some Values.StartFHIRImportJobResponse.to_json)
           (Some Values.StartFHIRImportJobResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-f-h-i-r-datastore", create_f_h_i_r_datastore);
    ("delete-f-h-i-r-datastore", delete_f_h_i_r_datastore);
    ("describe-f-h-i-r-datastore", describe_f_h_i_r_datastore);
    ("describe-f-h-i-r-export-job", describe_f_h_i_r_export_job);
    ("describe-f-h-i-r-import-job", describe_f_h_i_r_import_job);
    ("list-f-h-i-r-datastores", list_f_h_i_r_datastores);
    ("list-f-h-i-r-export-jobs", list_f_h_i_r_export_jobs);
    ("list-f-h-i-r-import-jobs", list_f_h_i_r_import_jobs);
    ("list-tags-for-resource", list_tags_for_resource);
    ("start-f-h-i-r-export-job", start_f_h_i_r_export_job);
    ("start-f-h-i-r-import-job", start_f_h_i_r_import_job);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource)]
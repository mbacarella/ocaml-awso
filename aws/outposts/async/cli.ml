(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let cancel_order =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and orderId = flag "order-id" (required string) ~doc:"STRING OrderId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_order (Values.CancelOrderInput.make ~orderId ())
           (Some Values.CancelOrderOutput.to_json)
           (Some Values.CancelOrderOutput.error_to_json)])
let create_order =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and paymentTerm =
         flag "payment-term" (optional json_arg) ~doc:"JSON PaymentTerm"
       and outpostIdentifier =
         flag "outpost-identifier" (required string)
           ~doc:"STRING OutpostIdentifier"
       and lineItems =
         flag "line-items" (required json_arg)
           ~doc:"JSON LineItemRequestListDefinition"
       and paymentOption =
         flag "payment-option" (required json_arg) ~doc:"JSON PaymentOption" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_order
           (Values.CreateOrderInput.make
              ?paymentTerm:(Option.map ~f:Values.PaymentTerm.of_json
                              paymentTerm) ~outpostIdentifier
              ~lineItems:(Values.LineItemRequestListDefinition.of_json
                            lineItems)
              ~paymentOption:(Values.PaymentOption.of_json paymentOption) ())
           (Some Values.CreateOrderOutput.to_json)
           (Some Values.CreateOrderOutput.error_to_json)])
let create_outpost =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING OutpostDescription"
       and availabilityZone =
         flag "availability-zone" (optional string)
           ~doc:"STRING AvailabilityZone"
       and availabilityZoneId =
         flag "availability-zone-id" (optional string)
           ~doc:"STRING AvailabilityZoneId"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and supportedHardwareType =
         flag "supported-hardware-type" (optional json_arg)
           ~doc:"JSON SupportedHardwareType"
       and name = flag "name" (required string) ~doc:"STRING OutpostName"
       and siteId = flag "site-id" (required string) ~doc:"STRING SiteId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_outpost
           (Values.CreateOutpostInput.make ?description ?availabilityZone
              ?availabilityZoneId
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?supportedHardwareType:(Option.map
                                        ~f:Values.SupportedHardwareType.of_json
                                        supportedHardwareType) ~name ~siteId
              ()) (Some Values.CreateOutpostOutput.to_json)
           (Some Values.CreateOutpostOutput.error_to_json)])
let create_site =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING SiteDescription"
       and notes = flag "notes" (optional string) ~doc:"STRING SiteNotes"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and operatingAddress =
         flag "operating-address" (optional json_arg) ~doc:"JSON Address"
       and shippingAddress =
         flag "shipping-address" (optional json_arg) ~doc:"JSON Address"
       and rackPhysicalProperties =
         flag "rack-physical-properties" (optional json_arg)
           ~doc:"JSON RackPhysicalProperties"
       and name = flag "name" (required string) ~doc:"STRING SiteName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_site
           (Values.CreateSiteInput.make ?description ?notes
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?operatingAddress:(Option.map ~f:Values.Address.of_json
                                   operatingAddress)
              ?shippingAddress:(Option.map ~f:Values.Address.of_json
                                  shippingAddress)
              ?rackPhysicalProperties:(Option.map
                                         ~f:Values.RackPhysicalProperties.of_json
                                         rackPhysicalProperties) ~name ())
           (Some Values.CreateSiteOutput.to_json)
           (Some Values.CreateSiteOutput.error_to_json)])
let delete_outpost =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and outpostId =
         flag "outpost-id" (required string) ~doc:"STRING OutpostId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_outpost (Values.DeleteOutpostInput.make ~outpostId ())
           (Some Values.DeleteOutpostOutput.to_json)
           (Some Values.DeleteOutpostOutput.error_to_json)])
let delete_site =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and siteId = flag "site-id" (required string) ~doc:"STRING SiteId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_site (Values.DeleteSiteInput.make ~siteId ())
           (Some Values.DeleteSiteOutput.to_json)
           (Some Values.DeleteSiteOutput.error_to_json)])
let get_catalog_item =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalogItemId =
         flag "catalog-item-id" (required string) ~doc:"STRING SkuCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_catalog_item
           (Values.GetCatalogItemInput.make ~catalogItemId ())
           (Some Values.GetCatalogItemOutput.to_json)
           (Some Values.GetCatalogItemOutput.error_to_json)])
let get_order =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and orderId = flag "order-id" (required string) ~doc:"STRING OrderId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_order (Values.GetOrderInput.make ~orderId ())
           (Some Values.GetOrderOutput.to_json)
           (Some Values.GetOrderOutput.error_to_json)])
let get_outpost =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and outpostId =
         flag "outpost-id" (required string) ~doc:"STRING OutpostId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_outpost (Values.GetOutpostInput.make ~outpostId ())
           (Some Values.GetOutpostOutput.to_json)
           (Some Values.GetOutpostOutput.error_to_json)])
let get_outpost_instance_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults1000"
       and outpostId =
         flag "outpost-id" (required string) ~doc:"STRING OutpostId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_outpost_instance_types
           (Values.GetOutpostInstanceTypesInput.make ?nextToken ?maxResults
              ~outpostId ())
           (Some Values.GetOutpostInstanceTypesOutput.to_json)
           (Some Values.GetOutpostInstanceTypesOutput.error_to_json)])
let get_site =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and siteId = flag "site-id" (required string) ~doc:"STRING SiteId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_site (Values.GetSiteInput.make ~siteId ())
           (Some Values.GetSiteOutput.to_json)
           (Some Values.GetSiteOutput.error_to_json)])
let get_site_address =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and siteId = flag "site-id" (required string) ~doc:"STRING SiteId"
       and addressType =
         flag "address-type" (required json_arg) ~doc:"JSON AddressType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_site_address
           (Values.GetSiteAddressInput.make ~siteId
              ~addressType:(Values.AddressType.of_json addressType) ())
           (Some Values.GetSiteAddressOutput.to_json)
           (Some Values.GetSiteAddressOutput.error_to_json)])
let list_catalog_items =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults1000"
       and itemClassFilter =
         flag "item-class-filter" (optional json_arg)
           ~doc:"JSON CatalogItemClassList"
       and supportedStorageFilter =
         flag "supported-storage-filter" (optional json_arg)
           ~doc:"JSON SupportedStorageList"
       and eC2FamilyFilter =
         flag "e-c2-family-filter" (optional json_arg)
           ~doc:"JSON EC2FamilyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_catalog_items
           (Values.ListCatalogItemsInput.make ?nextToken ?maxResults
              ?itemClassFilter:(Option.map
                                  ~f:Values.CatalogItemClassList.of_json
                                  itemClassFilter)
              ?supportedStorageFilter:(Option.map
                                         ~f:Values.SupportedStorageList.of_json
                                         supportedStorageFilter)
              ?eC2FamilyFilter:(Option.map ~f:Values.EC2FamilyList.of_json
                                  eC2FamilyFilter) ())
           (Some Values.ListCatalogItemsOutput.to_json)
           (Some Values.ListCatalogItemsOutput.error_to_json)])
let list_orders =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and outpostIdentifierFilter =
         flag "outpost-identifier-filter" (optional string)
           ~doc:"STRING OutpostIdentifier"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults1000" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_orders
           (Values.ListOrdersInput.make ?outpostIdentifierFilter ?nextToken
              ?maxResults ()) (Some Values.ListOrdersOutput.to_json)
           (Some Values.ListOrdersOutput.error_to_json)])
let list_outposts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults1000"
       and lifeCycleStatusFilter =
         flag "life-cycle-status-filter" (optional json_arg)
           ~doc:"JSON LifeCycleStatusList"
       and availabilityZoneFilter =
         flag "availability-zone-filter" (optional json_arg)
           ~doc:"JSON AvailabilityZoneList"
       and availabilityZoneIdFilter =
         flag "availability-zone-id-filter" (optional json_arg)
           ~doc:"JSON AvailabilityZoneIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_outposts
           (Values.ListOutpostsInput.make ?nextToken ?maxResults
              ?lifeCycleStatusFilter:(Option.map
                                        ~f:Values.LifeCycleStatusList.of_json
                                        lifeCycleStatusFilter)
              ?availabilityZoneFilter:(Option.map
                                         ~f:Values.AvailabilityZoneList.of_json
                                         availabilityZoneFilter)
              ?availabilityZoneIdFilter:(Option.map
                                           ~f:Values.AvailabilityZoneIdList.of_json
                                           availabilityZoneIdFilter) ())
           (Some Values.ListOutpostsOutput.to_json)
           (Some Values.ListOutpostsOutput.error_to_json)])
let list_sites =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults1000"
       and operatingAddressCountryCodeFilter =
         flag "operating-address-country-code-filter" (optional json_arg)
           ~doc:"JSON CountryCodeList"
       and operatingAddressStateOrRegionFilter =
         flag "operating-address-state-or-region-filter" (optional json_arg)
           ~doc:"JSON StateOrRegionList"
       and operatingAddressCityFilter =
         flag "operating-address-city-filter" (optional json_arg)
           ~doc:"JSON CityList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_sites
           (Values.ListSitesInput.make ?nextToken ?maxResults
              ?operatingAddressCountryCodeFilter:(Option.map
                                                    ~f:Values.CountryCodeList.of_json
                                                    operatingAddressCountryCodeFilter)
              ?operatingAddressStateOrRegionFilter:(Option.map
                                                      ~f:Values.StateOrRegionList.of_json
                                                      operatingAddressStateOrRegionFilter)
              ?operatingAddressCityFilter:(Option.map
                                             ~f:Values.CityList.of_json
                                             operatingAddressCityFilter) ())
           (Some Values.ListSitesOutput.to_json)
           (Some Values.ListSitesOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_outpost =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING OutpostName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING OutpostDescription"
       and supportedHardwareType =
         flag "supported-hardware-type" (optional json_arg)
           ~doc:"JSON SupportedHardwareType"
       and outpostId =
         flag "outpost-id" (required string) ~doc:"STRING OutpostId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_outpost
           (Values.UpdateOutpostInput.make ?name ?description
              ?supportedHardwareType:(Option.map
                                        ~f:Values.SupportedHardwareType.of_json
                                        supportedHardwareType) ~outpostId ())
           (Some Values.UpdateOutpostOutput.to_json)
           (Some Values.UpdateOutpostOutput.error_to_json)])
let update_site =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING SiteName"
       and description =
         flag "description" (optional string) ~doc:"STRING SiteDescription"
       and notes = flag "notes" (optional string) ~doc:"STRING SiteNotes"
       and siteId = flag "site-id" (required string) ~doc:"STRING SiteId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_site
           (Values.UpdateSiteInput.make ?name ?description ?notes ~siteId ())
           (Some Values.UpdateSiteOutput.to_json)
           (Some Values.UpdateSiteOutput.error_to_json)])
let update_site_address =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and siteId = flag "site-id" (required string) ~doc:"STRING SiteId"
       and addressType =
         flag "address-type" (required json_arg) ~doc:"JSON AddressType"
       and address = flag "address" (required json_arg) ~doc:"JSON Address" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_site_address
           (Values.UpdateSiteAddressInput.make ~siteId
              ~addressType:(Values.AddressType.of_json addressType)
              ~address:(Values.Address.of_json address) ())
           (Some Values.UpdateSiteAddressOutput.to_json)
           (Some Values.UpdateSiteAddressOutput.error_to_json)])
let update_site_rack_physical_properties =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and powerDrawKva =
         flag "power-draw-kva" (optional json_arg) ~doc:"JSON PowerDrawKva"
       and powerPhase =
         flag "power-phase" (optional json_arg) ~doc:"JSON PowerPhase"
       and powerConnector =
         flag "power-connector" (optional json_arg)
           ~doc:"JSON PowerConnector"
       and powerFeedDrop =
         flag "power-feed-drop" (optional json_arg) ~doc:"JSON PowerFeedDrop"
       and uplinkGbps =
         flag "uplink-gbps" (optional json_arg) ~doc:"JSON UplinkGbps"
       and uplinkCount =
         flag "uplink-count" (optional json_arg) ~doc:"JSON UplinkCount"
       and fiberOpticCableType =
         flag "fiber-optic-cable-type" (optional json_arg)
           ~doc:"JSON FiberOpticCableType"
       and opticalStandard =
         flag "optical-standard" (optional json_arg)
           ~doc:"JSON OpticalStandard"
       and maximumSupportedWeightLbs =
         flag "maximum-supported-weight-lbs" (optional json_arg)
           ~doc:"JSON MaximumSupportedWeightLbs"
       and siteId = flag "site-id" (required string) ~doc:"STRING SiteId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_site_rack_physical_properties
           (Values.UpdateSiteRackPhysicalPropertiesInput.make
              ?powerDrawKva:(Option.map ~f:Values.PowerDrawKva.of_json
                               powerDrawKva)
              ?powerPhase:(Option.map ~f:Values.PowerPhase.of_json powerPhase)
              ?powerConnector:(Option.map ~f:Values.PowerConnector.of_json
                                 powerConnector)
              ?powerFeedDrop:(Option.map ~f:Values.PowerFeedDrop.of_json
                                powerFeedDrop)
              ?uplinkGbps:(Option.map ~f:Values.UplinkGbps.of_json uplinkGbps)
              ?uplinkCount:(Option.map ~f:Values.UplinkCount.of_json
                              uplinkCount)
              ?fiberOpticCableType:(Option.map
                                      ~f:Values.FiberOpticCableType.of_json
                                      fiberOpticCableType)
              ?opticalStandard:(Option.map ~f:Values.OpticalStandard.of_json
                                  opticalStandard)
              ?maximumSupportedWeightLbs:(Option.map
                                            ~f:Values.MaximumSupportedWeightLbs.of_json
                                            maximumSupportedWeightLbs)
              ~siteId ())
           (Some Values.UpdateSiteRackPhysicalPropertiesOutput.to_json)
           (Some Values.UpdateSiteRackPhysicalPropertiesOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("cancel-order", cancel_order);
    ("create-order", create_order);
    ("create-outpost", create_outpost);
    ("create-site", create_site);
    ("delete-outpost", delete_outpost);
    ("delete-site", delete_site);
    ("get-catalog-item", get_catalog_item);
    ("get-order", get_order);
    ("get-outpost", get_outpost);
    ("get-outpost-instance-types", get_outpost_instance_types);
    ("get-site", get_site);
    ("get-site-address", get_site_address);
    ("list-catalog-items", list_catalog_items);
    ("list-orders", list_orders);
    ("list-outposts", list_outposts);
    ("list-sites", list_sites);
    ("list-tags-for-resource", list_tags_for_resource);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-outpost", update_outpost);
    ("update-site", update_site);
    ("update-site-address", update_site_address);
    ("update-site-rack-physical-properties",
      update_site_rack_physical_properties)]
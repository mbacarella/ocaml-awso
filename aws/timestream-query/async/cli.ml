(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let cancel_query =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and queryId = flag "query-id" (required string) ~doc:"STRING QueryId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_query (Values.CancelQueryRequest.make ~queryId ())
           (Some Values.CancelQueryResponse.to_json)
           (Some Values.CancelQueryResponse.error_to_json)])
let create_scheduled_query =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetConfiguration =
         flag "target-configuration" (optional json_arg)
           ~doc:"JSON TargetConfiguration"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and kmsKeyId =
         flag "kms-key-id" (optional string) ~doc:"STRING StringValue2048"
       and name =
         flag "name" (required string) ~doc:"STRING ScheduledQueryName"
       and queryString =
         flag "query-string" (required string) ~doc:"STRING QueryString"
       and scheduleConfiguration =
         flag "schedule-configuration" (required json_arg)
           ~doc:"JSON ScheduleConfiguration"
       and notificationConfiguration =
         flag "notification-configuration" (required json_arg)
           ~doc:"JSON NotificationConfiguration"
       and scheduledQueryExecutionRoleArn =
         flag "scheduled-query-execution-role-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and errorReportConfiguration =
         flag "error-report-configuration" (required json_arg)
           ~doc:"JSON ErrorReportConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_scheduled_query
           (Values.CreateScheduledQueryRequest.make
              ?targetConfiguration:(Option.map
                                      ~f:Values.TargetConfiguration.of_json
                                      targetConfiguration) ?clientToken
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?kmsKeyId
              ~name ~queryString
              ~scheduleConfiguration:(Values.ScheduleConfiguration.of_json
                                        scheduleConfiguration)
              ~notificationConfiguration:(Values.NotificationConfiguration.of_json
                                            notificationConfiguration)
              ~scheduledQueryExecutionRoleArn
              ~errorReportConfiguration:(Values.ErrorReportConfiguration.of_json
                                           errorReportConfiguration) ())
           (Some Values.CreateScheduledQueryResponse.to_json)
           (Some Values.CreateScheduledQueryResponse.error_to_json)])
let delete_scheduled_query =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scheduledQueryArn =
         flag "scheduled-query-arn" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_scheduled_query
           (Values.DeleteScheduledQueryRequest.make ~scheduledQueryArn ())
           None None])
let describe_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_endpoints (Values.DescribeEndpointsRequest.make ())
           (Some Values.DescribeEndpointsResponse.to_json)
           (Some Values.DescribeEndpointsResponse.error_to_json)])
let describe_scheduled_query =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scheduledQueryArn =
         flag "scheduled-query-arn" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_scheduled_query
           (Values.DescribeScheduledQueryRequest.make ~scheduledQueryArn ())
           (Some Values.DescribeScheduledQueryResponse.to_json)
           (Some Values.DescribeScheduledQueryResponse.error_to_json)])
let execute_scheduled_query =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and scheduledQueryArn =
         flag "scheduled-query-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and invocationTime =
         flag "invocation-time" (required json_arg) ~doc:"JSON Time" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.execute_scheduled_query
           (Values.ExecuteScheduledQueryRequest.make ?clientToken
              ~scheduledQueryArn
              ~invocationTime:(Values.Time.of_json invocationTime) ()) None
           None])
let list_scheduled_queries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxScheduledQueriesResults"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING NextScheduledQueriesResultsToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_scheduled_queries
           (Values.ListScheduledQueriesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListScheduledQueriesResponse.to_json)
           (Some Values.ListScheduledQueriesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxTagsForResourceResult"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING NextTagsForResourceResultsToken"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ?maxResults ?nextToken
              ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let prepare_query =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and validateOnly =
         flag "validate-only" (optional bool) ~doc:"BOOL NullableBoolean"
       and queryString =
         flag "query-string" (required string) ~doc:"STRING QueryString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.prepare_query
           (Values.PrepareQueryRequest.make ?validateOnly ~queryString ())
           (Some Values.PrepareQueryResponse.to_json)
           (Some Values.PrepareQueryResponse.error_to_json)])
let query =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxRows =
         flag "max-rows" (optional int) ~doc:"INT MaxQueryResults"
       and queryString =
         flag "query-string" (required string) ~doc:"STRING QueryString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region Io.query
           (Values.QueryRequest.make ?clientToken ?nextToken ?maxRows
              ~queryString ()) (Some Values.QueryResponse.to_json)
           (Some Values.QueryResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_scheduled_query =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scheduledQueryArn =
         flag "scheduled-query-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and state =
         flag "state" (required json_arg) ~doc:"JSON ScheduledQueryState" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_scheduled_query
           (Values.UpdateScheduledQueryRequest.make ~scheduledQueryArn
              ~state:(Values.ScheduledQueryState.of_json state) ()) None None])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("cancel-query", cancel_query);
    ("create-scheduled-query", create_scheduled_query);
    ("delete-scheduled-query", delete_scheduled_query);
    ("describe-endpoints", describe_endpoints);
    ("describe-scheduled-query", describe_scheduled_query);
    ("execute-scheduled-query", execute_scheduled_query);
    ("list-scheduled-queries", list_scheduled_queries);
    ("list-tags-for-resource", list_tags_for_resource);
    ("prepare-query", prepare_query);
    ("query", query);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-scheduled-query", update_scheduled_query)]
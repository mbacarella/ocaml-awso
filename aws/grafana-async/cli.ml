(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_license =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and licenseType =
         flag "license-type" (required json_arg) ~doc:"JSON LicenseType"
       and workspaceId =
         flag "workspace-id" (required string) ~doc:"STRING WorkspaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_license
           (Values.AssociateLicenseRequest.make
              ~licenseType:(Values.LicenseType.of_json licenseType)
              ~workspaceId ()) (Some Values.AssociateLicenseResponse.to_json)
           (Some Values.AssociateLicenseResponse.error_to_json)])
let create_workspace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and organizationRoleName =
         flag "organization-role-name" (optional string)
           ~doc:"STRING OrganizationRoleName"
       and stackSetName =
         flag "stack-set-name" (optional string) ~doc:"STRING StackSetName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and workspaceDataSources =
         flag "workspace-data-sources" (optional json_arg)
           ~doc:"JSON DataSourceTypesList"
       and workspaceDescription =
         flag "workspace-description" (optional string)
           ~doc:"STRING Description"
       and workspaceName =
         flag "workspace-name" (optional string) ~doc:"STRING WorkspaceName"
       and workspaceNotificationDestinations =
         flag "workspace-notification-destinations" (optional json_arg)
           ~doc:"JSON NotificationDestinationsList"
       and workspaceOrganizationalUnits =
         flag "workspace-organizational-units" (optional json_arg)
           ~doc:"JSON OrganizationalUnitList"
       and workspaceRoleArn =
         flag "workspace-role-arn" (optional string) ~doc:"STRING IamRoleArn"
       and accountAccessType =
         flag "account-access-type" (required json_arg)
           ~doc:"JSON AccountAccessType"
       and authenticationProviders =
         flag "authentication-providers" (required json_arg)
           ~doc:"JSON AuthenticationProviders"
       and permissionType =
         flag "permission-type" (required json_arg)
           ~doc:"JSON PermissionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_workspace
           (Values.CreateWorkspaceRequest.make ?clientToken
              ?organizationRoleName ?stackSetName
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?workspaceDataSources:(Option.map
                                       ~f:Values.DataSourceTypesList.of_json
                                       workspaceDataSources)
              ?workspaceDescription ?workspaceName
              ?workspaceNotificationDestinations:(Option.map
                                                    ~f:Values.NotificationDestinationsList.of_json
                                                    workspaceNotificationDestinations)
              ?workspaceOrganizationalUnits:(Option.map
                                               ~f:Values.OrganizationalUnitList.of_json
                                               workspaceOrganizationalUnits)
              ?workspaceRoleArn
              ~accountAccessType:(Values.AccountAccessType.of_json
                                    accountAccessType)
              ~authenticationProviders:(Values.AuthenticationProviders.of_json
                                          authenticationProviders)
              ~permissionType:(Values.PermissionType.of_json permissionType)
              ()) (Some Values.CreateWorkspaceResponse.to_json)
           (Some Values.CreateWorkspaceResponse.error_to_json)])
let delete_workspace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workspaceId =
         flag "workspace-id" (required string) ~doc:"STRING WorkspaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_workspace
           (Values.DeleteWorkspaceRequest.make ~workspaceId ())
           (Some Values.DeleteWorkspaceResponse.to_json)
           (Some Values.DeleteWorkspaceResponse.error_to_json)])
let describe_workspace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workspaceId =
         flag "workspace-id" (required string) ~doc:"STRING WorkspaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_workspace
           (Values.DescribeWorkspaceRequest.make ~workspaceId ())
           (Some Values.DescribeWorkspaceResponse.to_json)
           (Some Values.DescribeWorkspaceResponse.error_to_json)])
let describe_workspace_authentication =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workspaceId =
         flag "workspace-id" (required string) ~doc:"STRING WorkspaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_workspace_authentication
           (Values.DescribeWorkspaceAuthenticationRequest.make ~workspaceId
              ())
           (Some Values.DescribeWorkspaceAuthenticationResponse.to_json)
           (Some Values.DescribeWorkspaceAuthenticationResponse.error_to_json)])
let disassociate_license =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and licenseType =
         flag "license-type" (required json_arg) ~doc:"JSON LicenseType"
       and workspaceId =
         flag "workspace-id" (required string) ~doc:"STRING WorkspaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_license
           (Values.DisassociateLicenseRequest.make
              ~licenseType:(Values.LicenseType.of_json licenseType)
              ~workspaceId ())
           (Some Values.DisassociateLicenseResponse.to_json)
           (Some Values.DisassociateLicenseResponse.error_to_json)])
let list_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupId = flag "group-id" (optional string) ~doc:"STRING SsoId"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListPermissionsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and userId = flag "user-id" (optional string) ~doc:"STRING SsoId"
       and userType =
         flag "user-type" (optional json_arg) ~doc:"JSON UserType"
       and workspaceId =
         flag "workspace-id" (required string) ~doc:"STRING WorkspaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_permissions
           (Values.ListPermissionsRequest.make ?groupId ?maxResults
              ?nextToken ?userId
              ?userType:(Option.map ~f:Values.UserType.of_json userType)
              ~workspaceId ()) (Some Values.ListPermissionsResponse.to_json)
           (Some Values.ListPermissionsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_workspaces =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListWorkspacesRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_workspaces
           (Values.ListWorkspacesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListWorkspacesResponse.to_json)
           (Some Values.ListWorkspacesResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and updateInstructionBatch =
         flag "update-instruction-batch" (required json_arg)
           ~doc:"JSON UpdateInstructionBatch"
       and workspaceId =
         flag "workspace-id" (required string) ~doc:"STRING WorkspaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_permissions
           (Values.UpdatePermissionsRequest.make
              ~updateInstructionBatch:(Values.UpdateInstructionBatch.of_json
                                         updateInstructionBatch) ~workspaceId
              ()) (Some Values.UpdatePermissionsResponse.to_json)
           (Some Values.UpdatePermissionsResponse.error_to_json)])
let update_workspace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountAccessType =
         flag "account-access-type" (optional json_arg)
           ~doc:"JSON AccountAccessType"
       and organizationRoleName =
         flag "organization-role-name" (optional string)
           ~doc:"STRING OrganizationRoleName"
       and permissionType =
         flag "permission-type" (optional json_arg)
           ~doc:"JSON PermissionType"
       and stackSetName =
         flag "stack-set-name" (optional string) ~doc:"STRING StackSetName"
       and workspaceDataSources =
         flag "workspace-data-sources" (optional json_arg)
           ~doc:"JSON DataSourceTypesList"
       and workspaceDescription =
         flag "workspace-description" (optional string)
           ~doc:"STRING Description"
       and workspaceName =
         flag "workspace-name" (optional string) ~doc:"STRING WorkspaceName"
       and workspaceNotificationDestinations =
         flag "workspace-notification-destinations" (optional json_arg)
           ~doc:"JSON NotificationDestinationsList"
       and workspaceOrganizationalUnits =
         flag "workspace-organizational-units" (optional json_arg)
           ~doc:"JSON OrganizationalUnitList"
       and workspaceRoleArn =
         flag "workspace-role-arn" (optional string) ~doc:"STRING IamRoleArn"
       and workspaceId =
         flag "workspace-id" (required string) ~doc:"STRING WorkspaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_workspace
           (Values.UpdateWorkspaceRequest.make
              ?accountAccessType:(Option.map
                                    ~f:Values.AccountAccessType.of_json
                                    accountAccessType) ?organizationRoleName
              ?permissionType:(Option.map ~f:Values.PermissionType.of_json
                                 permissionType) ?stackSetName
              ?workspaceDataSources:(Option.map
                                       ~f:Values.DataSourceTypesList.of_json
                                       workspaceDataSources)
              ?workspaceDescription ?workspaceName
              ?workspaceNotificationDestinations:(Option.map
                                                    ~f:Values.NotificationDestinationsList.of_json
                                                    workspaceNotificationDestinations)
              ?workspaceOrganizationalUnits:(Option.map
                                               ~f:Values.OrganizationalUnitList.of_json
                                               workspaceOrganizationalUnits)
              ?workspaceRoleArn ~workspaceId ())
           (Some Values.UpdateWorkspaceResponse.to_json)
           (Some Values.UpdateWorkspaceResponse.error_to_json)])
let update_workspace_authentication =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and samlConfiguration =
         flag "saml-configuration" (optional json_arg)
           ~doc:"JSON SamlConfiguration"
       and authenticationProviders =
         flag "authentication-providers" (required json_arg)
           ~doc:"JSON AuthenticationProviders"
       and workspaceId =
         flag "workspace-id" (required string) ~doc:"STRING WorkspaceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_workspace_authentication
           (Values.UpdateWorkspaceAuthenticationRequest.make
              ?samlConfiguration:(Option.map
                                    ~f:Values.SamlConfiguration.of_json
                                    samlConfiguration)
              ~authenticationProviders:(Values.AuthenticationProviders.of_json
                                          authenticationProviders)
              ~workspaceId ())
           (Some Values.UpdateWorkspaceAuthenticationResponse.to_json)
           (Some Values.UpdateWorkspaceAuthenticationResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-license", associate_license);
    ("create-workspace", create_workspace);
    ("delete-workspace", delete_workspace);
    ("describe-workspace", describe_workspace);
    ("describe-workspace-authentication", describe_workspace_authentication);
    ("disassociate-license", disassociate_license);
    ("list-permissions", list_permissions);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-workspaces", list_workspaces);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-permissions", update_permissions);
    ("update-workspace", update_workspace);
    ("update-workspace-authentication", update_workspace_authentication)]
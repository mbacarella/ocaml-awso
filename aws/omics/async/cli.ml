(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let abort_multipart_read_set_upload =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sequenceStoreId =
         flag "sequence-store-id" (required string)
           ~doc:"STRING SequenceStoreId"
       and uploadId =
         flag "upload-id" (required string) ~doc:"STRING UploadId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.abort_multipart_read_set_upload
           (Values.AbortMultipartReadSetUploadRequest.make ~sequenceStoreId
              ~uploadId ())
           (Some Values.AbortMultipartReadSetUploadResponse.to_json)
           (Some Values.AbortMultipartReadSetUploadResponse.error_to_json)])
let accept_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and shareId = flag "share-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_share (Values.AcceptShareRequest.make ~shareId ())
           (Some Values.AcceptShareResponse.to_json)
           (Some Values.AcceptShareResponse.error_to_json)])
let batch_delete_read_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ids = flag "ids" (required json_arg) ~doc:"JSON ReadSetIdList"
       and sequenceStoreId =
         flag "sequence-store-id" (required string)
           ~doc:"STRING SequenceStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_delete_read_set
           (Values.BatchDeleteReadSetRequest.make
              ~ids:(Values.ReadSetIdList.of_json ids) ~sequenceStoreId ())
           (Some Values.BatchDeleteReadSetResponse.to_json)
           (Some Values.BatchDeleteReadSetResponse.error_to_json)])
let cancel_annotation_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_annotation_import_job
           (Values.CancelAnnotationImportRequest.make ~jobId ())
           (Some Values.CancelAnnotationImportResponse.to_json)
           (Some Values.CancelAnnotationImportResponse.error_to_json)])
let cancel_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING RunId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_run (Values.CancelRunRequest.make ~id ()) None None])
let cancel_run_batch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and batchId = flag "batch-id" (required string) ~doc:"STRING BatchId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_run_batch
           (Values.CancelRunBatchRequest.make ~batchId ())
           (Some Values.CancelRunBatchResponse.to_json)
           (Some Values.CancelRunBatchResponse.error_to_json)])
let cancel_variant_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_variant_import_job
           (Values.CancelVariantImportRequest.make ~jobId ())
           (Some Values.CancelVariantImportResponse.to_json)
           (Some Values.CancelVariantImportResponse.error_to_json)])
let complete_multipart_read_set_upload =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sequenceStoreId =
         flag "sequence-store-id" (required string)
           ~doc:"STRING SequenceStoreId"
       and uploadId =
         flag "upload-id" (required string) ~doc:"STRING UploadId"
       and parts =
         flag "parts" (required json_arg)
           ~doc:"JSON CompleteReadSetUploadPartList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.complete_multipart_read_set_upload
           (Values.CompleteMultipartReadSetUploadRequest.make
              ~sequenceStoreId ~uploadId
              ~parts:(Values.CompleteReadSetUploadPartList.of_json parts) ())
           (Some Values.CompleteMultipartReadSetUploadResponse.to_json)
           (Some Values.CompleteMultipartReadSetUploadResponse.error_to_json)])
let create_annotation_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reference =
         flag "reference" (optional json_arg) ~doc:"JSON ReferenceItem"
       and name = flag "name" (optional string) ~doc:"STRING StoreName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and versionName =
         flag "version-name" (optional string) ~doc:"STRING VersionName"
       and sseConfig =
         flag "sse-config" (optional json_arg) ~doc:"JSON SseConfig"
       and storeOptions =
         flag "store-options" (optional json_arg) ~doc:"JSON StoreOptions"
       and storeFormat =
         flag "store-format" (required json_arg) ~doc:"JSON StoreFormat" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_annotation_store
           (Values.CreateAnnotationStoreRequest.make
              ?reference:(Option.map ~f:Values.ReferenceItem.of_json
                            reference) ?name ?description
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ?versionName
              ?sseConfig:(Option.map ~f:Values.SseConfig.of_json sseConfig)
              ?storeOptions:(Option.map ~f:Values.StoreOptions.of_json
                               storeOptions)
              ~storeFormat:(Values.StoreFormat.of_json storeFormat) ())
           (Some Values.CreateAnnotationStoreResponse.to_json)
           (Some Values.CreateAnnotationStoreResponse.error_to_json)])
let create_annotation_store_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and versionOptions =
         flag "version-options" (optional json_arg)
           ~doc:"JSON VersionOptions"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name = flag "name" (required string) ~doc:"STRING StoreName"
       and versionName =
         flag "version-name" (required string) ~doc:"STRING VersionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_annotation_store_version
           (Values.CreateAnnotationStoreVersionRequest.make ?description
              ?versionOptions:(Option.map ~f:Values.VersionOptions.of_json
                                 versionOptions)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name
              ~versionName ())
           (Some Values.CreateAnnotationStoreVersionResponse.to_json)
           (Some Values.CreateAnnotationStoreVersionResponse.error_to_json)])
let create_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ConfigurationDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name =
         flag "name" (required string) ~doc:"STRING ConfigurationName"
       and runConfigurations =
         flag "run-configurations" (required json_arg)
           ~doc:"JSON RunConfigurations"
       and requestId =
         flag "request-id" (required string)
           ~doc:"STRING ConfigurationRequestId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_configuration
           (Values.CreateConfigurationRequest.make ?description
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name
              ~runConfigurations:(Values.RunConfigurations.of_json
                                    runConfigurations) ~requestId ())
           (Some Values.CreateConfigurationResponse.to_json)
           (Some Values.CreateConfigurationResponse.error_to_json)])
let create_multipart_read_set_upload =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and generatedFrom =
         flag "generated-from" (optional string) ~doc:"STRING GeneratedFrom"
       and referenceArn =
         flag "reference-arn" (optional string) ~doc:"STRING ReferenceArn"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ReadSetDescription"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and sequenceStoreId =
         flag "sequence-store-id" (required string)
           ~doc:"STRING SequenceStoreId"
       and sourceFileType =
         flag "source-file-type" (required json_arg) ~doc:"JSON FileType"
       and subjectId =
         flag "subject-id" (required string) ~doc:"STRING SubjectId"
       and sampleId =
         flag "sample-id" (required string) ~doc:"STRING SampleId"
       and name = flag "name" (required string) ~doc:"STRING ReadSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_multipart_read_set_upload
           (Values.CreateMultipartReadSetUploadRequest.make ?clientToken
              ?generatedFrom ?referenceArn ?description
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~sequenceStoreId
              ~sourceFileType:(Values.FileType.of_json sourceFileType)
              ~subjectId ~sampleId ~name ())
           (Some Values.CreateMultipartReadSetUploadResponse.to_json)
           (Some Values.CreateMultipartReadSetUploadResponse.error_to_json)])
let create_reference_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ReferenceStoreDescription"
       and sseConfig =
         flag "sse-config" (optional json_arg) ~doc:"JSON SseConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and name =
         flag "name" (required string) ~doc:"STRING ReferenceStoreName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_reference_store
           (Values.CreateReferenceStoreRequest.make ?description
              ?sseConfig:(Option.map ~f:Values.SseConfig.of_json sseConfig)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ?clientToken
              ~name ()) (Some Values.CreateReferenceStoreResponse.to_json)
           (Some Values.CreateReferenceStoreResponse.error_to_json)])
let create_run_cache =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cacheBehavior =
         flag "cache-behavior" (optional json_arg) ~doc:"JSON CacheBehavior"
       and description =
         flag "description" (optional string)
           ~doc:"STRING UserCustomDescription"
       and name = flag "name" (optional string) ~doc:"STRING UserCustomName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and cacheBucketOwnerId =
         flag "cache-bucket-owner-id" (optional string)
           ~doc:"STRING AwsAccountId"
       and cacheS3Location =
         flag "cache-s3-location" (required string)
           ~doc:"STRING S3UriForBucketOrObject"
       and requestId =
         flag "request-id" (required string) ~doc:"STRING RunCacheRequestId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_run_cache
           (Values.CreateRunCacheRequest.make
              ?cacheBehavior:(Option.map ~f:Values.CacheBehavior.of_json
                                cacheBehavior) ?description ?name
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?cacheBucketOwnerId ~cacheS3Location ~requestId ())
           (Some Values.CreateRunCacheResponse.to_json)
           (Some Values.CreateRunCacheResponse.error_to_json)])
let create_run_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING RunGroupName"
       and maxCpus =
         flag "max-cpus" (optional int)
           ~doc:"INT CreateRunGroupRequestMaxCpusInteger"
       and maxRuns =
         flag "max-runs" (optional int)
           ~doc:"INT CreateRunGroupRequestMaxRunsInteger"
       and maxDuration =
         flag "max-duration" (optional int)
           ~doc:"INT CreateRunGroupRequestMaxDurationInteger"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and maxGpus =
         flag "max-gpus" (optional int)
           ~doc:"INT CreateRunGroupRequestMaxGpusInteger"
       and requestId =
         flag "request-id" (required string) ~doc:"STRING RunGroupRequestId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_run_group
           (Values.CreateRunGroupRequest.make ?name ?maxCpus ?maxRuns
              ?maxDuration ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?maxGpus ~requestId ())
           (Some Values.CreateRunGroupResponse.to_json)
           (Some Values.CreateRunGroupResponse.error_to_json)])
let create_sequence_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING SequenceStoreDescription"
       and sseConfig =
         flag "sse-config" (optional json_arg) ~doc:"JSON SseConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and fallbackLocation =
         flag "fallback-location" (optional string)
           ~doc:"STRING FallbackLocation"
       and eTagAlgorithmFamily =
         flag "e-tag-algorithm-family" (optional json_arg)
           ~doc:"JSON ETagAlgorithmFamily"
       and propagatedSetLevelTags =
         flag "propagated-set-level-tags" (optional json_arg)
           ~doc:"JSON PropagatedSetLevelTags"
       and s3AccessConfig =
         flag "s3-access-config" (optional json_arg)
           ~doc:"JSON S3AccessConfig"
       and name =
         flag "name" (required string) ~doc:"STRING SequenceStoreName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_sequence_store
           (Values.CreateSequenceStoreRequest.make ?description
              ?sseConfig:(Option.map ~f:Values.SseConfig.of_json sseConfig)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ?clientToken
              ?fallbackLocation
              ?eTagAlgorithmFamily:(Option.map
                                      ~f:Values.ETagAlgorithmFamily.of_json
                                      eTagAlgorithmFamily)
              ?propagatedSetLevelTags:(Option.map
                                         ~f:Values.PropagatedSetLevelTags.of_json
                                         propagatedSetLevelTags)
              ?s3AccessConfig:(Option.map ~f:Values.S3AccessConfig.of_json
                                 s3AccessConfig) ~name ())
           (Some Values.CreateSequenceStoreResponse.to_json)
           (Some Values.CreateSequenceStoreResponse.error_to_json)])
let create_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and shareName =
         flag "share-name" (optional string) ~doc:"STRING ShareName"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and principalSubscriber =
         flag "principal-subscriber" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_share
           (Values.CreateShareRequest.make ?shareName ~resourceArn
              ~principalSubscriber ())
           (Some Values.CreateShareResponse.to_json)
           (Some Values.CreateShareResponse.error_to_json)])
let create_variant_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING StoreName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and sseConfig =
         flag "sse-config" (optional json_arg) ~doc:"JSON SseConfig"
       and reference =
         flag "reference" (required json_arg) ~doc:"JSON ReferenceItem" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_variant_store
           (Values.CreateVariantStoreRequest.make ?name ?description
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?sseConfig:(Option.map ~f:Values.SseConfig.of_json sseConfig)
              ~reference:(Values.ReferenceItem.of_json reference) ())
           (Some Values.CreateVariantStoreResponse.to_json)
           (Some Values.CreateVariantStoreResponse.error_to_json)])
let create_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING WorkflowName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING WorkflowDescription"
       and engine =
         flag "engine" (optional json_arg) ~doc:"JSON WorkflowEngine"
       and definitionZip =
         flag "definition-zip" (optional json_arg) ~doc:"JSON Blob"
       and definitionUri =
         flag "definition-uri" (optional string)
           ~doc:"STRING WorkflowDefinition"
       and main = flag "main" (optional string) ~doc:"STRING WorkflowMain"
       and parameterTemplate =
         flag "parameter-template" (optional json_arg)
           ~doc:"JSON WorkflowParameterTemplate"
       and storageCapacity =
         flag "storage-capacity" (optional int)
           ~doc:"INT CreateWorkflowRequestStorageCapacityInteger"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and accelerators =
         flag "accelerators" (optional json_arg) ~doc:"JSON Accelerators"
       and storageType =
         flag "storage-type" (optional json_arg) ~doc:"JSON StorageType"
       and containerRegistryMap =
         flag "container-registry-map" (optional json_arg)
           ~doc:"JSON ContainerRegistryMap"
       and containerRegistryMapUri =
         flag "container-registry-map-uri" (optional string)
           ~doc:"STRING Uri"
       and readmeMarkdown =
         flag "readme-markdown" (optional string)
           ~doc:"STRING ReadmeMarkdown"
       and parameterTemplatePath =
         flag "parameter-template-path" (optional string)
           ~doc:"STRING ParameterTemplatePath"
       and readmePath =
         flag "readme-path" (optional string) ~doc:"STRING ReadmePath"
       and definitionRepository =
         flag "definition-repository" (optional json_arg)
           ~doc:"JSON DefinitionRepository"
       and workflowBucketOwnerId =
         flag "workflow-bucket-owner-id" (optional string)
           ~doc:"STRING WorkflowBucketOwnerId"
       and readmeUri =
         flag "readme-uri" (optional string) ~doc:"STRING S3UriForObject"
       and requestId =
         flag "request-id" (required string) ~doc:"STRING WorkflowRequestId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_workflow
           (Values.CreateWorkflowRequest.make ?name ?description
              ?engine:(Option.map ~f:Values.WorkflowEngine.of_json engine)
              ?definitionZip:(Option.map ~f:Values.Blob.of_json definitionZip)
              ?definitionUri ?main
              ?parameterTemplate:(Option.map
                                    ~f:Values.WorkflowParameterTemplate.of_json
                                    parameterTemplate) ?storageCapacity
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?accelerators:(Option.map ~f:Values.Accelerators.of_json
                               accelerators)
              ?storageType:(Option.map ~f:Values.StorageType.of_json
                              storageType)
              ?containerRegistryMap:(Option.map
                                       ~f:Values.ContainerRegistryMap.of_json
                                       containerRegistryMap)
              ?containerRegistryMapUri ?readmeMarkdown ?parameterTemplatePath
              ?readmePath
              ?definitionRepository:(Option.map
                                       ~f:Values.DefinitionRepository.of_json
                                       definitionRepository)
              ?workflowBucketOwnerId ?readmeUri ~requestId ())
           (Some Values.CreateWorkflowResponse.to_json)
           (Some Values.CreateWorkflowResponse.error_to_json)])
let create_workflow_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and definitionZip =
         flag "definition-zip" (optional json_arg) ~doc:"JSON Blob"
       and definitionUri =
         flag "definition-uri" (optional string)
           ~doc:"STRING WorkflowDefinition"
       and accelerators =
         flag "accelerators" (optional json_arg) ~doc:"JSON Accelerators"
       and description =
         flag "description" (optional string)
           ~doc:"STRING WorkflowVersionDescription"
       and engine =
         flag "engine" (optional json_arg) ~doc:"JSON WorkflowEngine"
       and main = flag "main" (optional string) ~doc:"STRING WorkflowMain"
       and parameterTemplate =
         flag "parameter-template" (optional json_arg)
           ~doc:"JSON WorkflowParameterTemplate"
       and storageType =
         flag "storage-type" (optional json_arg) ~doc:"JSON StorageType"
       and storageCapacity =
         flag "storage-capacity" (optional int)
           ~doc:"INT CreateWorkflowVersionRequestStorageCapacityInteger"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and workflowBucketOwnerId =
         flag "workflow-bucket-owner-id" (optional string)
           ~doc:"STRING WorkflowBucketOwnerId"
       and containerRegistryMap =
         flag "container-registry-map" (optional json_arg)
           ~doc:"JSON ContainerRegistryMap"
       and containerRegistryMapUri =
         flag "container-registry-map-uri" (optional string)
           ~doc:"STRING Uri"
       and readmeMarkdown =
         flag "readme-markdown" (optional string)
           ~doc:"STRING ReadmeMarkdown"
       and parameterTemplatePath =
         flag "parameter-template-path" (optional string)
           ~doc:"STRING ParameterTemplatePath"
       and readmePath =
         flag "readme-path" (optional string) ~doc:"STRING ReadmePath"
       and definitionRepository =
         flag "definition-repository" (optional json_arg)
           ~doc:"JSON DefinitionRepository"
       and readmeUri =
         flag "readme-uri" (optional string) ~doc:"STRING S3UriForObject"
       and workflowId =
         flag "workflow-id" (required string) ~doc:"STRING WorkflowId"
       and versionName =
         flag "version-name" (required string)
           ~doc:"STRING WorkflowVersionName"
       and requestId =
         flag "request-id" (required string) ~doc:"STRING WorkflowRequestId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_workflow_version
           (Values.CreateWorkflowVersionRequest.make
              ?definitionZip:(Option.map ~f:Values.Blob.of_json definitionZip)
              ?definitionUri
              ?accelerators:(Option.map ~f:Values.Accelerators.of_json
                               accelerators) ?description
              ?engine:(Option.map ~f:Values.WorkflowEngine.of_json engine)
              ?main
              ?parameterTemplate:(Option.map
                                    ~f:Values.WorkflowParameterTemplate.of_json
                                    parameterTemplate)
              ?storageType:(Option.map ~f:Values.StorageType.of_json
                              storageType) ?storageCapacity
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?workflowBucketOwnerId
              ?containerRegistryMap:(Option.map
                                       ~f:Values.ContainerRegistryMap.of_json
                                       containerRegistryMap)
              ?containerRegistryMapUri ?readmeMarkdown ?parameterTemplatePath
              ?readmePath
              ?definitionRepository:(Option.map
                                       ~f:Values.DefinitionRepository.of_json
                                       definitionRepository) ?readmeUri
              ~workflowId ~versionName ~requestId ())
           (Some Values.CreateWorkflowVersionResponse.to_json)
           (Some Values.CreateWorkflowVersionResponse.error_to_json)])
let delete_annotation_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and force = flag "force" (optional bool) ~doc:"BOOL PrimitiveBoolean"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_annotation_store
           (Values.DeleteAnnotationStoreRequest.make ?force ~name ())
           (Some Values.DeleteAnnotationStoreResponse.to_json)
           (Some Values.DeleteAnnotationStoreResponse.error_to_json)])
let delete_annotation_store_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and force = flag "force" (optional bool) ~doc:"BOOL PrimitiveBoolean"
       and name = flag "name" (required string) ~doc:"STRING String"
       and versions =
         flag "versions" (required json_arg) ~doc:"JSON VersionList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_annotation_store_versions
           (Values.DeleteAnnotationStoreVersionsRequest.make ?force ~name
              ~versions:(Values.VersionList.of_json versions) ())
           (Some Values.DeleteAnnotationStoreVersionsResponse.to_json)
           (Some Values.DeleteAnnotationStoreVersionsResponse.error_to_json)])
let delete_batch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and batchId = flag "batch-id" (required string) ~doc:"STRING BatchId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_batch (Values.DeleteBatchRequest.make ~batchId ()) None
           None])
let delete_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING ConfigurationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_configuration
           (Values.DeleteConfigurationRequest.make ~name ()) None None])
let delete_reference =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ReferenceId"
       and referenceStoreId =
         flag "reference-store-id" (required string)
           ~doc:"STRING ReferenceStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_reference
           (Values.DeleteReferenceRequest.make ~id ~referenceStoreId ())
           (Some Values.DeleteReferenceResponse.to_json)
           (Some Values.DeleteReferenceResponse.error_to_json)])
let delete_reference_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ReferenceStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_reference_store
           (Values.DeleteReferenceStoreRequest.make ~id ())
           (Some Values.DeleteReferenceStoreResponse.to_json)
           (Some Values.DeleteReferenceStoreResponse.error_to_json)])
let delete_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING RunId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_run (Values.DeleteRunRequest.make ~id ()) None None])
let delete_run_batch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and batchId = flag "batch-id" (required string) ~doc:"STRING BatchId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_run_batch
           (Values.DeleteRunBatchRequest.make ~batchId ())
           (Some Values.DeleteRunBatchResponse.to_json)
           (Some Values.DeleteRunBatchResponse.error_to_json)])
let delete_run_cache =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING RunCacheId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_run_cache (Values.DeleteRunCacheRequest.make ~id ())
           None None])
let delete_run_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING RunGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_run_group (Values.DeleteRunGroupRequest.make ~id ())
           None None])
let delete_s3_access_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and s3AccessPointArn =
         flag "s3-access-point-arn" (required string)
           ~doc:"STRING S3AccessPointArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_s3_access_policy
           (Values.DeleteS3AccessPolicyRequest.make ~s3AccessPointArn ())
           (Some Values.DeleteS3AccessPolicyResponse.to_json)
           (Some Values.DeleteS3AccessPolicyResponse.error_to_json)])
let delete_sequence_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING SequenceStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_sequence_store
           (Values.DeleteSequenceStoreRequest.make ~id ())
           (Some Values.DeleteSequenceStoreResponse.to_json)
           (Some Values.DeleteSequenceStoreResponse.error_to_json)])
let delete_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and shareId = flag "share-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_share (Values.DeleteShareRequest.make ~shareId ())
           (Some Values.DeleteShareResponse.to_json)
           (Some Values.DeleteShareResponse.error_to_json)])
let delete_variant_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and force = flag "force" (optional bool) ~doc:"BOOL PrimitiveBoolean"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_variant_store
           (Values.DeleteVariantStoreRequest.make ?force ~name ())
           (Some Values.DeleteVariantStoreResponse.to_json)
           (Some Values.DeleteVariantStoreResponse.error_to_json)])
let delete_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING WorkflowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_workflow (Values.DeleteWorkflowRequest.make ~id ()) None
           None])
let delete_workflow_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workflowId =
         flag "workflow-id" (required string) ~doc:"STRING WorkflowId"
       and versionName =
         flag "version-name" (required string)
           ~doc:"STRING WorkflowVersionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_workflow_version
           (Values.DeleteWorkflowVersionRequest.make ~workflowId ~versionName
              ()) None None])
let get_annotation_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_annotation_import_job
           (Values.GetAnnotationImportRequest.make ~jobId ())
           (Some Values.GetAnnotationImportResponse.to_json)
           (Some Values.GetAnnotationImportResponse.error_to_json)])
let get_annotation_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_annotation_store
           (Values.GetAnnotationStoreRequest.make ~name ())
           (Some Values.GetAnnotationStoreResponse.to_json)
           (Some Values.GetAnnotationStoreResponse.error_to_json)])
let get_annotation_store_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING String"
       and versionName =
         flag "version-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_annotation_store_version
           (Values.GetAnnotationStoreVersionRequest.make ~name ~versionName
              ()) (Some Values.GetAnnotationStoreVersionResponse.to_json)
           (Some Values.GetAnnotationStoreVersionResponse.error_to_json)])
let get_batch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and batchId = flag "batch-id" (required string) ~doc:"STRING BatchId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_batch (Values.GetBatchRequest.make ~batchId ())
           (Some Values.GetBatchResponse.to_json)
           (Some Values.GetBatchResponse.error_to_json)])
let get_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (required string) ~doc:"STRING ConfigurationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_configuration
           (Values.GetConfigurationRequest.make ~name ())
           (Some Values.GetConfigurationResponse.to_json)
           (Some Values.GetConfigurationResponse.error_to_json)])
let get_read_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and file = flag "file" (optional json_arg) ~doc:"JSON ReadSetFile"
       and id = flag "id" (required string) ~doc:"STRING ReadSetId"
       and sequenceStoreId =
         flag "sequence-store-id" (required string)
           ~doc:"STRING SequenceStoreId"
       and partNumber =
         flag "part-number" (required int)
           ~doc:"INT GetReadSetRequestPartNumberInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_read_set
           (Values.GetReadSetRequest.make
              ?file:(Option.map ~f:Values.ReadSetFile.of_json file) ~id
              ~sequenceStoreId ~partNumber ())
           (Some Values.GetReadSetResponse.to_json)
           (Some Values.GetReadSetResponse.error_to_json)])
let get_read_set_activation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ActivationJobId"
       and sequenceStoreId =
         flag "sequence-store-id" (required string)
           ~doc:"STRING SequenceStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_read_set_activation_job
           (Values.GetReadSetActivationJobRequest.make ~id ~sequenceStoreId
              ()) (Some Values.GetReadSetActivationJobResponse.to_json)
           (Some Values.GetReadSetActivationJobResponse.error_to_json)])
let get_read_set_export_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sequenceStoreId =
         flag "sequence-store-id" (required string)
           ~doc:"STRING SequenceStoreId"
       and id = flag "id" (required string) ~doc:"STRING ExportJobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_read_set_export_job
           (Values.GetReadSetExportJobRequest.make ~sequenceStoreId ~id ())
           (Some Values.GetReadSetExportJobResponse.to_json)
           (Some Values.GetReadSetExportJobResponse.error_to_json)])
let get_read_set_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ImportJobId"
       and sequenceStoreId =
         flag "sequence-store-id" (required string)
           ~doc:"STRING SequenceStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_read_set_import_job
           (Values.GetReadSetImportJobRequest.make ~id ~sequenceStoreId ())
           (Some Values.GetReadSetImportJobResponse.to_json)
           (Some Values.GetReadSetImportJobResponse.error_to_json)])
let get_read_set_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ReadSetId"
       and sequenceStoreId =
         flag "sequence-store-id" (required string)
           ~doc:"STRING SequenceStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_read_set_metadata
           (Values.GetReadSetMetadataRequest.make ~id ~sequenceStoreId ())
           (Some Values.GetReadSetMetadataResponse.to_json)
           (Some Values.GetReadSetMetadataResponse.error_to_json)])
let get_reference =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and range = flag "range" (optional string) ~doc:"STRING Range"
       and file = flag "file" (optional json_arg) ~doc:"JSON ReferenceFile"
       and id = flag "id" (required string) ~doc:"STRING ReferenceId"
       and referenceStoreId =
         flag "reference-store-id" (required string)
           ~doc:"STRING ReferenceStoreId"
       and partNumber =
         flag "part-number" (required int)
           ~doc:"INT GetReferenceRequestPartNumberInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_reference
           (Values.GetReferenceRequest.make ?range
              ?file:(Option.map ~f:Values.ReferenceFile.of_json file) ~id
              ~referenceStoreId ~partNumber ())
           (Some Values.GetReferenceResponse.to_json)
           (Some Values.GetReferenceResponse.error_to_json)])
let get_reference_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ImportJobId"
       and referenceStoreId =
         flag "reference-store-id" (required string)
           ~doc:"STRING ReferenceStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_reference_import_job
           (Values.GetReferenceImportJobRequest.make ~id ~referenceStoreId ())
           (Some Values.GetReferenceImportJobResponse.to_json)
           (Some Values.GetReferenceImportJobResponse.error_to_json)])
let get_reference_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ReferenceId"
       and referenceStoreId =
         flag "reference-store-id" (required string)
           ~doc:"STRING ReferenceStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_reference_metadata
           (Values.GetReferenceMetadataRequest.make ~id ~referenceStoreId ())
           (Some Values.GetReferenceMetadataResponse.to_json)
           (Some Values.GetReferenceMetadataResponse.error_to_json)])
let get_reference_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ReferenceStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_reference_store
           (Values.GetReferenceStoreRequest.make ~id ())
           (Some Values.GetReferenceStoreResponse.to_json)
           (Some Values.GetReferenceStoreResponse.error_to_json)])
let get_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and export =
         flag "export" (optional json_arg) ~doc:"JSON RunExportList"
       and id = flag "id" (required string) ~doc:"STRING RunId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_run
           (Values.GetRunRequest.make
              ?export:(Option.map ~f:Values.RunExportList.of_json export) ~id
              ()) (Some Values.GetRunResponse.to_json)
           (Some Values.GetRunResponse.error_to_json)])
let get_run_cache =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING RunCacheId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_run_cache (Values.GetRunCacheRequest.make ~id ())
           (Some Values.GetRunCacheResponse.to_json)
           (Some Values.GetRunCacheResponse.error_to_json)])
let get_run_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING RunGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_run_group (Values.GetRunGroupRequest.make ~id ())
           (Some Values.GetRunGroupResponse.to_json)
           (Some Values.GetRunGroupResponse.error_to_json)])
let get_run_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING RunId"
       and taskId = flag "task-id" (required string) ~doc:"STRING TaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_run_task (Values.GetRunTaskRequest.make ~id ~taskId ())
           (Some Values.GetRunTaskResponse.to_json)
           (Some Values.GetRunTaskResponse.error_to_json)])
let get_s3_access_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and s3AccessPointArn =
         flag "s3-access-point-arn" (required string)
           ~doc:"STRING S3AccessPointArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_s3_access_policy
           (Values.GetS3AccessPolicyRequest.make ~s3AccessPointArn ())
           (Some Values.GetS3AccessPolicyResponse.to_json)
           (Some Values.GetS3AccessPolicyResponse.error_to_json)])
let get_sequence_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING SequenceStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sequence_store (Values.GetSequenceStoreRequest.make ~id ())
           (Some Values.GetSequenceStoreResponse.to_json)
           (Some Values.GetSequenceStoreResponse.error_to_json)])
let get_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and shareId = flag "share-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_share (Values.GetShareRequest.make ~shareId ())
           (Some Values.GetShareResponse.to_json)
           (Some Values.GetShareResponse.error_to_json)])
let get_variant_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_variant_import_job
           (Values.GetVariantImportRequest.make ~jobId ())
           (Some Values.GetVariantImportResponse.to_json)
           (Some Values.GetVariantImportResponse.error_to_json)])
let get_variant_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_variant_store (Values.GetVariantStoreRequest.make ~name ())
           (Some Values.GetVariantStoreResponse.to_json)
           (Some Values.GetVariantStoreResponse.error_to_json)])
let get_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON WorkflowType"
       and export =
         flag "export" (optional json_arg) ~doc:"JSON WorkflowExportList"
       and workflowOwnerId =
         flag "workflow-owner-id" (optional string)
           ~doc:"STRING WorkflowOwnerId"
       and id = flag "id" (required string) ~doc:"STRING WorkflowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_workflow
           (Values.GetWorkflowRequest.make
              ?type_:(Option.map ~f:Values.WorkflowType.of_json type_)
              ?export:(Option.map ~f:Values.WorkflowExportList.of_json export)
              ?workflowOwnerId ~id ())
           (Some Values.GetWorkflowResponse.to_json)
           (Some Values.GetWorkflowResponse.error_to_json)])
let get_workflow_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON WorkflowType"
       and export =
         flag "export" (optional json_arg) ~doc:"JSON WorkflowExportList"
       and workflowOwnerId =
         flag "workflow-owner-id" (optional string)
           ~doc:"STRING WorkflowOwnerId"
       and workflowId =
         flag "workflow-id" (required string) ~doc:"STRING WorkflowId"
       and versionName =
         flag "version-name" (required string)
           ~doc:"STRING WorkflowVersionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_workflow_version
           (Values.GetWorkflowVersionRequest.make
              ?type_:(Option.map ~f:Values.WorkflowType.of_json type_)
              ?export:(Option.map ~f:Values.WorkflowExportList.of_json export)
              ?workflowOwnerId ~workflowId ~versionName ())
           (Some Values.GetWorkflowVersionResponse.to_json)
           (Some Values.GetWorkflowVersionResponse.error_to_json)])
let list_annotation_import_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListAnnotationImportJobsRequestMaxResultsInteger"
       and ids =
         flag "ids" (optional json_arg)
           ~doc:"JSON ListAnnotationImportJobsRequestIdsList"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListAnnotationImportJobsRequestNextTokenString"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON ListAnnotationImportJobsFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_annotation_import_jobs
           (Values.ListAnnotationImportJobsRequest.make ?maxResults
              ?ids:(Option.map
                      ~f:Values.ListAnnotationImportJobsRequestIdsList.of_json
                      ids) ?nextToken
              ?filter:(Option.map
                         ~f:Values.ListAnnotationImportJobsFilter.of_json
                         filter) ())
           (Some Values.ListAnnotationImportJobsResponse.to_json)
           (Some Values.ListAnnotationImportJobsResponse.error_to_json)])
let list_annotation_store_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListAnnotationStoreVersionsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListAnnotationStoreVersionsRequestNextTokenString"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON ListAnnotationStoreVersionsFilter"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_annotation_store_versions
           (Values.ListAnnotationStoreVersionsRequest.make ?maxResults
              ?nextToken
              ?filter:(Option.map
                         ~f:Values.ListAnnotationStoreVersionsFilter.of_json
                         filter) ~name ())
           (Some Values.ListAnnotationStoreVersionsResponse.to_json)
           (Some Values.ListAnnotationStoreVersionsResponse.error_to_json)])
let list_annotation_stores =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ids =
         flag "ids" (optional json_arg)
           ~doc:"JSON ListAnnotationStoresRequestIdsList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListAnnotationStoresRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListAnnotationStoresRequestNextTokenString"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON ListAnnotationStoresFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_annotation_stores
           (Values.ListAnnotationStoresRequest.make
              ?ids:(Option.map
                      ~f:Values.ListAnnotationStoresRequestIdsList.of_json
                      ids) ?maxResults ?nextToken
              ?filter:(Option.map
                         ~f:Values.ListAnnotationStoresFilter.of_json filter)
              ()) (Some Values.ListAnnotationStoresResponse.to_json)
           (Some Values.ListAnnotationStoresResponse.error_to_json)])
let list_batch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxItems =
         flag "max-items" (optional int)
           ~doc:"INT ListBatchRequestMaxItemsInteger"
       and startingToken =
         flag "starting-token" (optional string) ~doc:"STRING ListToken"
       and status = flag "status" (optional json_arg) ~doc:"JSON BatchStatus"
       and name = flag "name" (optional string) ~doc:"STRING BatchName"
       and runGroupId =
         flag "run-group-id" (optional string) ~doc:"STRING RunGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_batch
           (Values.ListBatchRequest.make ?maxItems ?startingToken
              ?status:(Option.map ~f:Values.BatchStatus.of_json status) ?name
              ?runGroupId ()) (Some Values.ListBatchResponse.to_json)
           (Some Values.ListBatchResponse.error_to_json)])
let list_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListConfigurationsRequestMaxResultsInteger"
       and startingToken =
         flag "starting-token" (optional string)
           ~doc:"STRING ConfigurationListToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_configurations
           (Values.ListConfigurationsRequest.make ?maxResults ?startingToken
              ()) (Some Values.ListConfigurationsResponse.to_json)
           (Some Values.ListConfigurationsResponse.error_to_json)])
let list_multipart_read_set_uploads =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListMultipartReadSetUploadsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and sequenceStoreId =
         flag "sequence-store-id" (required string)
           ~doc:"STRING SequenceStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_multipart_read_set_uploads
           (Values.ListMultipartReadSetUploadsRequest.make ?maxResults
              ?nextToken ~sequenceStoreId ())
           (Some Values.ListMultipartReadSetUploadsResponse.to_json)
           (Some Values.ListMultipartReadSetUploadsResponse.error_to_json)])
let list_read_set_activation_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListReadSetActivationJobsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON ActivateReadSetFilter"
       and sequenceStoreId =
         flag "sequence-store-id" (required string)
           ~doc:"STRING SequenceStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_read_set_activation_jobs
           (Values.ListReadSetActivationJobsRequest.make ?maxResults
              ?nextToken
              ?filter:(Option.map ~f:Values.ActivateReadSetFilter.of_json
                         filter) ~sequenceStoreId ())
           (Some Values.ListReadSetActivationJobsResponse.to_json)
           (Some Values.ListReadSetActivationJobsResponse.error_to_json)])
let list_read_set_export_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListReadSetExportJobsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON ExportReadSetFilter"
       and sequenceStoreId =
         flag "sequence-store-id" (required string)
           ~doc:"STRING SequenceStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_read_set_export_jobs
           (Values.ListReadSetExportJobsRequest.make ?maxResults ?nextToken
              ?filter:(Option.map ~f:Values.ExportReadSetFilter.of_json
                         filter) ~sequenceStoreId ())
           (Some Values.ListReadSetExportJobsResponse.to_json)
           (Some Values.ListReadSetExportJobsResponse.error_to_json)])
let list_read_set_import_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListReadSetImportJobsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON ImportReadSetFilter"
       and sequenceStoreId =
         flag "sequence-store-id" (required string)
           ~doc:"STRING SequenceStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_read_set_import_jobs
           (Values.ListReadSetImportJobsRequest.make ?maxResults ?nextToken
              ?filter:(Option.map ~f:Values.ImportReadSetFilter.of_json
                         filter) ~sequenceStoreId ())
           (Some Values.ListReadSetImportJobsResponse.to_json)
           (Some Values.ListReadSetImportJobsResponse.error_to_json)])
let list_read_set_upload_parts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListReadSetUploadPartsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON ReadSetUploadPartListFilter"
       and sequenceStoreId =
         flag "sequence-store-id" (required string)
           ~doc:"STRING SequenceStoreId"
       and uploadId =
         flag "upload-id" (required string) ~doc:"STRING UploadId"
       and partSource =
         flag "part-source" (required json_arg) ~doc:"JSON ReadSetPartSource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_read_set_upload_parts
           (Values.ListReadSetUploadPartsRequest.make ?maxResults ?nextToken
              ?filter:(Option.map
                         ~f:Values.ReadSetUploadPartListFilter.of_json filter)
              ~sequenceStoreId ~uploadId
              ~partSource:(Values.ReadSetPartSource.of_json partSource) ())
           (Some Values.ListReadSetUploadPartsResponse.to_json)
           (Some Values.ListReadSetUploadPartsResponse.error_to_json)])
let list_read_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListReadSetsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON ReadSetFilter"
       and sequenceStoreId =
         flag "sequence-store-id" (required string)
           ~doc:"STRING SequenceStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_read_sets
           (Values.ListReadSetsRequest.make ?maxResults ?nextToken
              ?filter:(Option.map ~f:Values.ReadSetFilter.of_json filter)
              ~sequenceStoreId ()) (Some Values.ListReadSetsResponse.to_json)
           (Some Values.ListReadSetsResponse.error_to_json)])
let list_reference_import_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListReferenceImportJobsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON ImportReferenceFilter"
       and referenceStoreId =
         flag "reference-store-id" (required string)
           ~doc:"STRING ReferenceStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_reference_import_jobs
           (Values.ListReferenceImportJobsRequest.make ?maxResults ?nextToken
              ?filter:(Option.map ~f:Values.ImportReferenceFilter.of_json
                         filter) ~referenceStoreId ())
           (Some Values.ListReferenceImportJobsResponse.to_json)
           (Some Values.ListReferenceImportJobsResponse.error_to_json)])
let list_reference_stores =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListReferenceStoresRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON ReferenceStoreFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_reference_stores
           (Values.ListReferenceStoresRequest.make ?maxResults ?nextToken
              ?filter:(Option.map ~f:Values.ReferenceStoreFilter.of_json
                         filter) ())
           (Some Values.ListReferenceStoresResponse.to_json)
           (Some Values.ListReferenceStoresResponse.error_to_json)])
let list_references =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListReferencesRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON ReferenceFilter"
       and referenceStoreId =
         flag "reference-store-id" (required string)
           ~doc:"STRING ReferenceStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_references
           (Values.ListReferencesRequest.make ?maxResults ?nextToken
              ?filter:(Option.map ~f:Values.ReferenceFilter.of_json filter)
              ~referenceStoreId ())
           (Some Values.ListReferencesResponse.to_json)
           (Some Values.ListReferencesResponse.error_to_json)])
let list_run_caches =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListRunCachesRequestMaxResultsInteger"
       and startingToken =
         flag "starting-token" (optional string) ~doc:"STRING ListToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_run_caches
           (Values.ListRunCachesRequest.make ?maxResults ?startingToken ())
           (Some Values.ListRunCachesResponse.to_json)
           (Some Values.ListRunCachesResponse.error_to_json)])
let list_run_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING RunGroupName"
       and startingToken =
         flag "starting-token" (optional string)
           ~doc:"STRING RunGroupListToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListRunGroupsRequestMaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_run_groups
           (Values.ListRunGroupsRequest.make ?name ?startingToken ?maxResults
              ()) (Some Values.ListRunGroupsResponse.to_json)
           (Some Values.ListRunGroupsResponse.error_to_json)])
let list_run_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status = flag "status" (optional json_arg) ~doc:"JSON TaskStatus"
       and startingToken =
         flag "starting-token" (optional string) ~doc:"STRING TaskListToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListRunTasksRequestMaxResultsInteger"
       and id = flag "id" (required string) ~doc:"STRING RunId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_run_tasks
           (Values.ListRunTasksRequest.make
              ?status:(Option.map ~f:Values.TaskStatus.of_json status)
              ?startingToken ?maxResults ~id ())
           (Some Values.ListRunTasksResponse.to_json)
           (Some Values.ListRunTasksResponse.error_to_json)])
let list_runs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING RunName"
       and runGroupId =
         flag "run-group-id" (optional string) ~doc:"STRING RunGroupId"
       and batchId = flag "batch-id" (optional string) ~doc:"STRING BatchId"
       and startingToken =
         flag "starting-token" (optional string) ~doc:"STRING RunListToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListRunsRequestMaxResultsInteger"
       and status = flag "status" (optional json_arg) ~doc:"JSON RunStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_runs
           (Values.ListRunsRequest.make ?name ?runGroupId ?batchId
              ?startingToken ?maxResults
              ?status:(Option.map ~f:Values.RunStatus.of_json status) ())
           (Some Values.ListRunsResponse.to_json)
           (Some Values.ListRunsResponse.error_to_json)])
let list_runs_in_batch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxItems =
         flag "max-items" (optional int)
           ~doc:"INT ListRunsInBatchRequestMaxItemsInteger"
       and startingToken =
         flag "starting-token" (optional string) ~doc:"STRING ListToken"
       and submissionStatus =
         flag "submission-status" (optional json_arg)
           ~doc:"JSON SubmissionStatus"
       and runSettingId =
         flag "run-setting-id" (optional string) ~doc:"STRING String"
       and runId = flag "run-id" (optional string) ~doc:"STRING String"
       and batchId = flag "batch-id" (required string) ~doc:"STRING BatchId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_runs_in_batch
           (Values.ListRunsInBatchRequest.make ?maxItems ?startingToken
              ?submissionStatus:(Option.map
                                   ~f:Values.SubmissionStatus.of_json
                                   submissionStatus) ?runSettingId ?runId
              ~batchId ()) (Some Values.ListRunsInBatchResponse.to_json)
           (Some Values.ListRunsInBatchResponse.error_to_json)])
let list_sequence_stores =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListSequenceStoresRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON SequenceStoreFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_sequence_stores
           (Values.ListSequenceStoresRequest.make ?maxResults ?nextToken
              ?filter:(Option.map ~f:Values.SequenceStoreFilter.of_json
                         filter) ())
           (Some Values.ListSequenceStoresResponse.to_json)
           (Some Values.ListSequenceStoresResponse.error_to_json)])
let list_shares =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter = flag "filter" (optional json_arg) ~doc:"JSON Filter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and resourceOwner =
         flag "resource-owner" (required json_arg) ~doc:"JSON ResourceOwner" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_shares
           (Values.ListSharesRequest.make
              ?filter:(Option.map ~f:Values.Filter.of_json filter) ?nextToken
              ?maxResults
              ~resourceOwner:(Values.ResourceOwner.of_json resourceOwner) ())
           (Some Values.ListSharesResponse.to_json)
           (Some Values.ListSharesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TagArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_variant_import_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListVariantImportJobsRequestMaxResultsInteger"
       and ids =
         flag "ids" (optional json_arg)
           ~doc:"JSON ListVariantImportJobsRequestIdsList"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListVariantImportJobsRequestNextTokenString"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON ListVariantImportJobsFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_variant_import_jobs
           (Values.ListVariantImportJobsRequest.make ?maxResults
              ?ids:(Option.map
                      ~f:Values.ListVariantImportJobsRequestIdsList.of_json
                      ids) ?nextToken
              ?filter:(Option.map
                         ~f:Values.ListVariantImportJobsFilter.of_json filter)
              ()) (Some Values.ListVariantImportJobsResponse.to_json)
           (Some Values.ListVariantImportJobsResponse.error_to_json)])
let list_variant_stores =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListVariantStoresRequestMaxResultsInteger"
       and ids =
         flag "ids" (optional json_arg)
           ~doc:"JSON ListVariantStoresRequestIdsList"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListVariantStoresRequestNextTokenString"
       and filter =
         flag "filter" (optional json_arg)
           ~doc:"JSON ListVariantStoresFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_variant_stores
           (Values.ListVariantStoresRequest.make ?maxResults
              ?ids:(Option.map
                      ~f:Values.ListVariantStoresRequestIdsList.of_json ids)
              ?nextToken
              ?filter:(Option.map ~f:Values.ListVariantStoresFilter.of_json
                         filter) ())
           (Some Values.ListVariantStoresResponse.to_json)
           (Some Values.ListVariantStoresResponse.error_to_json)])
let list_workflow_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON WorkflowType"
       and workflowOwnerId =
         flag "workflow-owner-id" (optional string)
           ~doc:"STRING WorkflowOwnerId"
       and startingToken =
         flag "starting-token" (optional string)
           ~doc:"STRING WorkflowVersionListToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListWorkflowVersionsRequestMaxResultsInteger"
       and workflowId =
         flag "workflow-id" (required string) ~doc:"STRING WorkflowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_workflow_versions
           (Values.ListWorkflowVersionsRequest.make
              ?type_:(Option.map ~f:Values.WorkflowType.of_json type_)
              ?workflowOwnerId ?startingToken ?maxResults ~workflowId ())
           (Some Values.ListWorkflowVersionsResponse.to_json)
           (Some Values.ListWorkflowVersionsResponse.error_to_json)])
let list_workflows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON WorkflowType"
       and name = flag "name" (optional string) ~doc:"STRING WorkflowName"
       and startingToken =
         flag "starting-token" (optional string)
           ~doc:"STRING WorkflowListToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListWorkflowsRequestMaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_workflows
           (Values.ListWorkflowsRequest.make
              ?type_:(Option.map ~f:Values.WorkflowType.of_json type_) ?name
              ?startingToken ?maxResults ())
           (Some Values.ListWorkflowsResponse.to_json)
           (Some Values.ListWorkflowsResponse.error_to_json)])
let put_s3_access_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and s3AccessPointArn =
         flag "s3-access-point-arn" (required string)
           ~doc:"STRING S3AccessPointArn"
       and s3AccessPolicy =
         flag "s3-access-policy" (required string)
           ~doc:"STRING S3AccessPolicy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_s3_access_policy
           (Values.PutS3AccessPolicyRequest.make ~s3AccessPointArn
              ~s3AccessPolicy ())
           (Some Values.PutS3AccessPolicyResponse.to_json)
           (Some Values.PutS3AccessPolicyResponse.error_to_json)])
let start_annotation_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionName =
         flag "version-name" (optional string) ~doc:"STRING VersionName"
       and formatOptions =
         flag "format-options" (optional json_arg) ~doc:"JSON FormatOptions"
       and runLeftNormalization =
         flag "run-left-normalization" (optional bool)
           ~doc:"BOOL RunLeftNormalization"
       and annotationFields =
         flag "annotation-fields" (optional json_arg)
           ~doc:"JSON AnnotationFieldMap"
       and destinationName =
         flag "destination-name" (required string) ~doc:"STRING StoreName"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING Arn"
       and items =
         flag "items" (required json_arg)
           ~doc:"JSON AnnotationImportItemSources" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_annotation_import_job
           (Values.StartAnnotationImportRequest.make ?versionName
              ?formatOptions:(Option.map ~f:Values.FormatOptions.of_json
                                formatOptions) ?runLeftNormalization
              ?annotationFields:(Option.map
                                   ~f:Values.AnnotationFieldMap.of_json
                                   annotationFields) ~destinationName
              ~roleArn
              ~items:(Values.AnnotationImportItemSources.of_json items) ())
           (Some Values.StartAnnotationImportResponse.to_json)
           (Some Values.StartAnnotationImportResponse.error_to_json)])
let start_read_set_activation_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and sequenceStoreId =
         flag "sequence-store-id" (required string)
           ~doc:"STRING SequenceStoreId"
       and sources =
         flag "sources" (required json_arg)
           ~doc:"JSON StartReadSetActivationJobRequestSourcesList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_read_set_activation_job
           (Values.StartReadSetActivationJobRequest.make ?clientToken
              ~sequenceStoreId
              ~sources:(Values.StartReadSetActivationJobRequestSourcesList.of_json
                          sources) ())
           (Some Values.StartReadSetActivationJobResponse.to_json)
           (Some Values.StartReadSetActivationJobResponse.error_to_json)])
let start_read_set_export_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and sequenceStoreId =
         flag "sequence-store-id" (required string)
           ~doc:"STRING SequenceStoreId"
       and destination =
         flag "destination" (required string) ~doc:"STRING S3Destination"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn"
       and sources =
         flag "sources" (required json_arg)
           ~doc:"JSON StartReadSetExportJobRequestSourcesList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_read_set_export_job
           (Values.StartReadSetExportJobRequest.make ?clientToken
              ~sequenceStoreId ~destination ~roleArn
              ~sources:(Values.StartReadSetExportJobRequestSourcesList.of_json
                          sources) ())
           (Some Values.StartReadSetExportJobResponse.to_json)
           (Some Values.StartReadSetExportJobResponse.error_to_json)])
let start_read_set_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and sequenceStoreId =
         flag "sequence-store-id" (required string)
           ~doc:"STRING SequenceStoreId"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn"
       and sources =
         flag "sources" (required json_arg)
           ~doc:"JSON StartReadSetImportJobRequestSourcesList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_read_set_import_job
           (Values.StartReadSetImportJobRequest.make ?clientToken
              ~sequenceStoreId ~roleArn
              ~sources:(Values.StartReadSetImportJobRequestSourcesList.of_json
                          sources) ())
           (Some Values.StartReadSetImportJobResponse.to_json)
           (Some Values.StartReadSetImportJobResponse.error_to_json)])
let start_reference_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and referenceStoreId =
         flag "reference-store-id" (required string)
           ~doc:"STRING ReferenceStoreId"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn"
       and sources =
         flag "sources" (required json_arg)
           ~doc:"JSON StartReferenceImportJobRequestSourcesList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_reference_import_job
           (Values.StartReferenceImportJobRequest.make ?clientToken
              ~referenceStoreId ~roleArn
              ~sources:(Values.StartReferenceImportJobRequestSourcesList.of_json
                          sources) ())
           (Some Values.StartReferenceImportJobResponse.to_json)
           (Some Values.StartReferenceImportJobResponse.error_to_json)])
let start_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workflowId =
         flag "workflow-id" (optional string) ~doc:"STRING WorkflowId"
       and workflowType =
         flag "workflow-type" (optional json_arg) ~doc:"JSON WorkflowType"
       and runId = flag "run-id" (optional string) ~doc:"STRING RunId"
       and name = flag "name" (optional string) ~doc:"STRING RunName"
       and cacheId =
         flag "cache-id" (optional string) ~doc:"STRING NumericIdInArn"
       and cacheBehavior =
         flag "cache-behavior" (optional json_arg) ~doc:"JSON CacheBehavior"
       and runGroupId =
         flag "run-group-id" (optional string) ~doc:"STRING RunGroupId"
       and priority =
         flag "priority" (optional int)
           ~doc:"INT StartRunRequestPriorityInteger"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON RunParameters"
       and storageCapacity =
         flag "storage-capacity" (optional int)
           ~doc:"INT StartRunRequestStorageCapacityInteger"
       and logLevel =
         flag "log-level" (optional json_arg) ~doc:"JSON RunLogLevel"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and retentionMode =
         flag "retention-mode" (optional json_arg)
           ~doc:"JSON RunRetentionMode"
       and storageType =
         flag "storage-type" (optional json_arg) ~doc:"JSON StorageType"
       and workflowOwnerId =
         flag "workflow-owner-id" (optional string)
           ~doc:"STRING WorkflowOwnerId"
       and workflowVersionName =
         flag "workflow-version-name" (optional string)
           ~doc:"STRING WorkflowVersionName"
       and networkingMode =
         flag "networking-mode" (optional json_arg)
           ~doc:"JSON NetworkingMode"
       and configurationName =
         flag "configuration-name" (optional string)
           ~doc:"STRING ConfigurationName"
       and roleArn =
         flag "role-arn" (required string) ~doc:"STRING RunRoleArn"
       and outputUri =
         flag "output-uri" (required string) ~doc:"STRING RunOutputUri"
       and requestId =
         flag "request-id" (required string) ~doc:"STRING RunRequestId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_run
           (Values.StartRunRequest.make ?workflowId
              ?workflowType:(Option.map ~f:Values.WorkflowType.of_json
                               workflowType) ?runId ?name ?cacheId
              ?cacheBehavior:(Option.map ~f:Values.CacheBehavior.of_json
                                cacheBehavior) ?runGroupId ?priority
              ?parameters:(Option.map ~f:Values.RunParameters.of_json
                             parameters) ?storageCapacity
              ?logLevel:(Option.map ~f:Values.RunLogLevel.of_json logLevel)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?retentionMode:(Option.map ~f:Values.RunRetentionMode.of_json
                                retentionMode)
              ?storageType:(Option.map ~f:Values.StorageType.of_json
                              storageType) ?workflowOwnerId
              ?workflowVersionName
              ?networkingMode:(Option.map ~f:Values.NetworkingMode.of_json
                                 networkingMode) ?configurationName ~roleArn
              ~outputUri ~requestId ())
           (Some Values.StartRunResponse.to_json)
           (Some Values.StartRunResponse.error_to_json)])
let start_run_batch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and batchName =
         flag "batch-name" (optional string) ~doc:"STRING BatchName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and requestId =
         flag "request-id" (required string) ~doc:"STRING BatchRequestId"
       and defaultRunSetting =
         flag "default-run-setting" (required json_arg)
           ~doc:"JSON DefaultRunSetting"
       and batchRunSettings =
         flag "batch-run-settings" (required json_arg)
           ~doc:"JSON BatchRunSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_run_batch
           (Values.StartRunBatchRequest.make ?batchName
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~requestId
              ~defaultRunSetting:(Values.DefaultRunSetting.of_json
                                    defaultRunSetting)
              ~batchRunSettings:(Values.BatchRunSettings.of_json
                                   batchRunSettings) ())
           (Some Values.StartRunBatchResponse.to_json)
           (Some Values.StartRunBatchResponse.error_to_json)])
let start_variant_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and runLeftNormalization =
         flag "run-left-normalization" (optional bool)
           ~doc:"BOOL RunLeftNormalization"
       and annotationFields =
         flag "annotation-fields" (optional json_arg)
           ~doc:"JSON AnnotationFieldMap"
       and destinationName =
         flag "destination-name" (required string) ~doc:"STRING StoreName"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING Arn"
       and items =
         flag "items" (required json_arg)
           ~doc:"JSON VariantImportItemSources" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_variant_import_job
           (Values.StartVariantImportRequest.make ?runLeftNormalization
              ?annotationFields:(Option.map
                                   ~f:Values.AnnotationFieldMap.of_json
                                   annotationFields) ~destinationName
              ~roleArn ~items:(Values.VariantImportItemSources.of_json items)
              ()) (Some Values.StartVariantImportResponse.to_json)
           (Some Values.StartVariantImportResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TagArn"
       and tags =
         flag "tags" (required json_arg)
           ~doc:"JSON TagResourceRequestTagsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagResourceRequestTagsMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TagArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_annotation_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_annotation_store
           (Values.UpdateAnnotationStoreRequest.make ?description ~name ())
           (Some Values.UpdateAnnotationStoreResponse.to_json)
           (Some Values.UpdateAnnotationStoreResponse.error_to_json)])
let update_annotation_store_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and name = flag "name" (required string) ~doc:"STRING String"
       and versionName =
         flag "version-name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_annotation_store_version
           (Values.UpdateAnnotationStoreVersionRequest.make ?description
              ~name ~versionName ())
           (Some Values.UpdateAnnotationStoreVersionResponse.to_json)
           (Some Values.UpdateAnnotationStoreVersionResponse.error_to_json)])
let update_run_cache =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cacheBehavior =
         flag "cache-behavior" (optional json_arg) ~doc:"JSON CacheBehavior"
       and description =
         flag "description" (optional string)
           ~doc:"STRING UserCustomDescription"
       and name = flag "name" (optional string) ~doc:"STRING UserCustomName"
       and id = flag "id" (required string) ~doc:"STRING RunCacheId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_run_cache
           (Values.UpdateRunCacheRequest.make
              ?cacheBehavior:(Option.map ~f:Values.CacheBehavior.of_json
                                cacheBehavior) ?description ?name ~id ())
           None None])
let update_run_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING RunGroupName"
       and maxCpus =
         flag "max-cpus" (optional int)
           ~doc:"INT UpdateRunGroupRequestMaxCpusInteger"
       and maxRuns =
         flag "max-runs" (optional int)
           ~doc:"INT UpdateRunGroupRequestMaxRunsInteger"
       and maxDuration =
         flag "max-duration" (optional int)
           ~doc:"INT UpdateRunGroupRequestMaxDurationInteger"
       and maxGpus =
         flag "max-gpus" (optional int)
           ~doc:"INT UpdateRunGroupRequestMaxGpusInteger"
       and id = flag "id" (required string) ~doc:"STRING RunGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_run_group
           (Values.UpdateRunGroupRequest.make ?name ?maxCpus ?maxRuns
              ?maxDuration ?maxGpus ~id ()) None None])
let update_sequence_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING SequenceStoreName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING SequenceStoreDescription"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and fallbackLocation =
         flag "fallback-location" (optional string)
           ~doc:"STRING FallbackLocation"
       and propagatedSetLevelTags =
         flag "propagated-set-level-tags" (optional json_arg)
           ~doc:"JSON PropagatedSetLevelTags"
       and s3AccessConfig =
         flag "s3-access-config" (optional json_arg)
           ~doc:"JSON S3AccessConfig"
       and id = flag "id" (required string) ~doc:"STRING SequenceStoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_sequence_store
           (Values.UpdateSequenceStoreRequest.make ?name ?description
              ?clientToken ?fallbackLocation
              ?propagatedSetLevelTags:(Option.map
                                         ~f:Values.PropagatedSetLevelTags.of_json
                                         propagatedSetLevelTags)
              ?s3AccessConfig:(Option.map ~f:Values.S3AccessConfig.of_json
                                 s3AccessConfig) ~id ())
           (Some Values.UpdateSequenceStoreResponse.to_json)
           (Some Values.UpdateSequenceStoreResponse.error_to_json)])
let update_variant_store =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and name = flag "name" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_variant_store
           (Values.UpdateVariantStoreRequest.make ?description ~name ())
           (Some Values.UpdateVariantStoreResponse.to_json)
           (Some Values.UpdateVariantStoreResponse.error_to_json)])
let update_workflow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING WorkflowName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING WorkflowDescription"
       and storageType =
         flag "storage-type" (optional json_arg) ~doc:"JSON StorageType"
       and storageCapacity =
         flag "storage-capacity" (optional int)
           ~doc:"INT UpdateWorkflowRequestStorageCapacityInteger"
       and readmeMarkdown =
         flag "readme-markdown" (optional string)
           ~doc:"STRING ReadmeMarkdown"
       and id = flag "id" (required string) ~doc:"STRING WorkflowId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_workflow
           (Values.UpdateWorkflowRequest.make ?name ?description
              ?storageType:(Option.map ~f:Values.StorageType.of_json
                              storageType) ?storageCapacity ?readmeMarkdown
              ~id ()) None None])
let update_workflow_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING WorkflowVersionDescription"
       and storageType =
         flag "storage-type" (optional json_arg) ~doc:"JSON StorageType"
       and storageCapacity =
         flag "storage-capacity" (optional int)
           ~doc:"INT UpdateWorkflowVersionRequestStorageCapacityInteger"
       and readmeMarkdown =
         flag "readme-markdown" (optional string)
           ~doc:"STRING ReadmeMarkdown"
       and workflowId =
         flag "workflow-id" (required string) ~doc:"STRING WorkflowId"
       and versionName =
         flag "version-name" (required string)
           ~doc:"STRING WorkflowVersionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_workflow_version
           (Values.UpdateWorkflowVersionRequest.make ?description
              ?storageType:(Option.map ~f:Values.StorageType.of_json
                              storageType) ?storageCapacity ?readmeMarkdown
              ~workflowId ~versionName ()) None None])
let upload_read_set_part =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sequenceStoreId =
         flag "sequence-store-id" (required string)
           ~doc:"STRING SequenceStoreId"
       and uploadId =
         flag "upload-id" (required string) ~doc:"STRING UploadId"
       and partSource =
         flag "part-source" (required json_arg) ~doc:"JSON ReadSetPartSource"
       and partNumber =
         flag "part-number" (required int)
           ~doc:"INT UploadReadSetPartRequestPartNumberInteger"
       and payload =
         flag "payload" (required json_arg)
           ~doc:"JSON ReadSetPartStreamingBlob" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.upload_read_set_part
           (Values.UploadReadSetPartRequest.make ~sequenceStoreId ~uploadId
              ~partSource:(Values.ReadSetPartSource.of_json partSource)
              ~partNumber
              ~payload:(Values.ReadSetPartStreamingBlob.of_json payload) ())
           (Some Values.UploadReadSetPartResponse.to_json)
           (Some Values.UploadReadSetPartResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("abort-multipart-read-set-upload", abort_multipart_read_set_upload);
    ("accept-share", accept_share);
    ("batch-delete-read-set", batch_delete_read_set);
    ("cancel-annotation-import-job", cancel_annotation_import_job);
    ("cancel-run", cancel_run);
    ("cancel-run-batch", cancel_run_batch);
    ("cancel-variant-import-job", cancel_variant_import_job);
    ("complete-multipart-read-set-upload",
      complete_multipart_read_set_upload);
    ("create-annotation-store", create_annotation_store);
    ("create-annotation-store-version", create_annotation_store_version);
    ("create-configuration", create_configuration);
    ("create-multipart-read-set-upload", create_multipart_read_set_upload);
    ("create-reference-store", create_reference_store);
    ("create-run-cache", create_run_cache);
    ("create-run-group", create_run_group);
    ("create-sequence-store", create_sequence_store);
    ("create-share", create_share);
    ("create-variant-store", create_variant_store);
    ("create-workflow", create_workflow);
    ("create-workflow-version", create_workflow_version);
    ("delete-annotation-store", delete_annotation_store);
    ("delete-annotation-store-versions", delete_annotation_store_versions);
    ("delete-batch", delete_batch);
    ("delete-configuration", delete_configuration);
    ("delete-reference", delete_reference);
    ("delete-reference-store", delete_reference_store);
    ("delete-run", delete_run);
    ("delete-run-batch", delete_run_batch);
    ("delete-run-cache", delete_run_cache);
    ("delete-run-group", delete_run_group);
    ("delete-s3-access-policy", delete_s3_access_policy);
    ("delete-sequence-store", delete_sequence_store);
    ("delete-share", delete_share);
    ("delete-variant-store", delete_variant_store);
    ("delete-workflow", delete_workflow);
    ("delete-workflow-version", delete_workflow_version);
    ("get-annotation-import-job", get_annotation_import_job);
    ("get-annotation-store", get_annotation_store);
    ("get-annotation-store-version", get_annotation_store_version);
    ("get-batch", get_batch);
    ("get-configuration", get_configuration);
    ("get-read-set", get_read_set);
    ("get-read-set-activation-job", get_read_set_activation_job);
    ("get-read-set-export-job", get_read_set_export_job);
    ("get-read-set-import-job", get_read_set_import_job);
    ("get-read-set-metadata", get_read_set_metadata);
    ("get-reference", get_reference);
    ("get-reference-import-job", get_reference_import_job);
    ("get-reference-metadata", get_reference_metadata);
    ("get-reference-store", get_reference_store);
    ("get-run", get_run);
    ("get-run-cache", get_run_cache);
    ("get-run-group", get_run_group);
    ("get-run-task", get_run_task);
    ("get-s3-access-policy", get_s3_access_policy);
    ("get-sequence-store", get_sequence_store);
    ("get-share", get_share);
    ("get-variant-import-job", get_variant_import_job);
    ("get-variant-store", get_variant_store);
    ("get-workflow", get_workflow);
    ("get-workflow-version", get_workflow_version);
    ("list-annotation-import-jobs", list_annotation_import_jobs);
    ("list-annotation-store-versions", list_annotation_store_versions);
    ("list-annotation-stores", list_annotation_stores);
    ("list-batch", list_batch);
    ("list-configurations", list_configurations);
    ("list-multipart-read-set-uploads", list_multipart_read_set_uploads);
    ("list-read-set-activation-jobs", list_read_set_activation_jobs);
    ("list-read-set-export-jobs", list_read_set_export_jobs);
    ("list-read-set-import-jobs", list_read_set_import_jobs);
    ("list-read-set-upload-parts", list_read_set_upload_parts);
    ("list-read-sets", list_read_sets);
    ("list-reference-import-jobs", list_reference_import_jobs);
    ("list-reference-stores", list_reference_stores);
    ("list-references", list_references);
    ("list-run-caches", list_run_caches);
    ("list-run-groups", list_run_groups);
    ("list-run-tasks", list_run_tasks);
    ("list-runs", list_runs);
    ("list-runs-in-batch", list_runs_in_batch);
    ("list-sequence-stores", list_sequence_stores);
    ("list-shares", list_shares);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-variant-import-jobs", list_variant_import_jobs);
    ("list-variant-stores", list_variant_stores);
    ("list-workflow-versions", list_workflow_versions);
    ("list-workflows", list_workflows);
    ("put-s3-access-policy", put_s3_access_policy);
    ("start-annotation-import-job", start_annotation_import_job);
    ("start-read-set-activation-job", start_read_set_activation_job);
    ("start-read-set-export-job", start_read_set_export_job);
    ("start-read-set-import-job", start_read_set_import_job);
    ("start-reference-import-job", start_reference_import_job);
    ("start-run", start_run);
    ("start-run-batch", start_run_batch);
    ("start-variant-import-job", start_variant_import_job);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-annotation-store", update_annotation_store);
    ("update-annotation-store-version", update_annotation_store_version);
    ("update-run-cache", update_run_cache);
    ("update-run-group", update_run_group);
    ("update-sequence-store", update_sequence_store);
    ("update-variant-store", update_variant_store);
    ("update-workflow", update_workflow);
    ("update-workflow-version", update_workflow_version);
    ("upload-read-set-part", upload_read_set_part)]
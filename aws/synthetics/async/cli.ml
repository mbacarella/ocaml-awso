(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_canary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and runConfig =
         flag "run-config" (optional json_arg)
           ~doc:"JSON CanaryRunConfigInput"
       and successRetentionPeriodInDays =
         flag "success-retention-period-in-days" (optional int)
           ~doc:"INT MaxSize1024"
       and failureRetentionPeriodInDays =
         flag "failure-retention-period-in-days" (optional int)
           ~doc:"INT MaxSize1024"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON VpcConfigInput"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and artifactConfig =
         flag "artifact-config" (optional json_arg)
           ~doc:"JSON ArtifactConfigInput"
       and name = flag "name" (required string) ~doc:"STRING CanaryName"
       and code = flag "code" (required json_arg) ~doc:"JSON CanaryCodeInput"
       and artifactS3Location =
         flag "artifact-s3-location" (required string) ~doc:"STRING String"
       and executionRoleArn =
         flag "execution-role-arn" (required string) ~doc:"STRING RoleArn"
       and schedule =
         flag "schedule" (required json_arg) ~doc:"JSON CanaryScheduleInput"
       and runtimeVersion =
         flag "runtime-version" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_canary
           (Values.CreateCanaryRequest.make
              ?runConfig:(Option.map ~f:Values.CanaryRunConfigInput.of_json
                            runConfig) ?successRetentionPeriodInDays
              ?failureRetentionPeriodInDays
              ?vpcConfig:(Option.map ~f:Values.VpcConfigInput.of_json
                            vpcConfig)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?artifactConfig:(Option.map
                                 ~f:Values.ArtifactConfigInput.of_json
                                 artifactConfig) ~name
              ~code:(Values.CanaryCodeInput.of_json code) ~artifactS3Location
              ~executionRoleArn
              ~schedule:(Values.CanaryScheduleInput.of_json schedule)
              ~runtimeVersion ()) (Some Values.CreateCanaryResponse.to_json)
           (Some Values.CreateCanaryResponse.error_to_json)])
let delete_canary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING CanaryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_canary (Values.DeleteCanaryRequest.make ~name ())
           (Some Values.DeleteCanaryResponse.to_json)
           (Some Values.DeleteCanaryResponse.error_to_json)])
let describe_canaries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxCanaryResults"
       and names =
         flag "names" (optional json_arg)
           ~doc:"JSON DescribeCanariesNameFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_canaries
           (Values.DescribeCanariesRequest.make ?nextToken ?maxResults
              ?names:(Option.map ~f:Values.DescribeCanariesNameFilter.of_json
                        names) ())
           (Some Values.DescribeCanariesResponse.to_json)
           (Some Values.DescribeCanariesResponse.error_to_json)])
let describe_canaries_last_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxSize100"
       and names =
         flag "names" (optional json_arg)
           ~doc:"JSON DescribeCanariesLastRunNameFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_canaries_last_run
           (Values.DescribeCanariesLastRunRequest.make ?nextToken ?maxResults
              ?names:(Option.map
                        ~f:Values.DescribeCanariesLastRunNameFilter.of_json
                        names) ())
           (Some Values.DescribeCanariesLastRunResponse.to_json)
           (Some Values.DescribeCanariesLastRunResponse.error_to_json)])
let describe_runtime_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxSize100" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_runtime_versions
           (Values.DescribeRuntimeVersionsRequest.make ?nextToken ?maxResults
              ()) (Some Values.DescribeRuntimeVersionsResponse.to_json)
           (Some Values.DescribeRuntimeVersionsResponse.error_to_json)])
let get_canary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING CanaryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_canary (Values.GetCanaryRequest.make ~name ())
           (Some Values.GetCanaryResponse.to_json)
           (Some Values.GetCanaryResponse.error_to_json)])
let get_canary_runs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxSize100"
       and name = flag "name" (required string) ~doc:"STRING CanaryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_canary_runs
           (Values.GetCanaryRunsRequest.make ?nextToken ?maxResults ~name ())
           (Some Values.GetCanaryRunsResponse.to_json)
           (Some Values.GetCanaryRunsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING CanaryArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let start_canary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING CanaryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_canary (Values.StartCanaryRequest.make ~name ())
           (Some Values.StartCanaryResponse.to_json)
           (Some Values.StartCanaryResponse.error_to_json)])
let stop_canary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING CanaryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_canary (Values.StopCanaryRequest.make ~name ())
           (Some Values.StopCanaryResponse.to_json)
           (Some Values.StopCanaryResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING CanaryArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING CanaryArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_canary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and code = flag "code" (optional json_arg) ~doc:"JSON CanaryCodeInput"
       and executionRoleArn =
         flag "execution-role-arn" (optional string) ~doc:"STRING RoleArn"
       and runtimeVersion =
         flag "runtime-version" (optional string) ~doc:"STRING String"
       and schedule =
         flag "schedule" (optional json_arg) ~doc:"JSON CanaryScheduleInput"
       and runConfig =
         flag "run-config" (optional json_arg)
           ~doc:"JSON CanaryRunConfigInput"
       and successRetentionPeriodInDays =
         flag "success-retention-period-in-days" (optional int)
           ~doc:"INT MaxSize1024"
       and failureRetentionPeriodInDays =
         flag "failure-retention-period-in-days" (optional int)
           ~doc:"INT MaxSize1024"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON VpcConfigInput"
       and visualReference =
         flag "visual-reference" (optional json_arg)
           ~doc:"JSON VisualReferenceInput"
       and artifactS3Location =
         flag "artifact-s3-location" (optional string) ~doc:"STRING String"
       and artifactConfig =
         flag "artifact-config" (optional json_arg)
           ~doc:"JSON ArtifactConfigInput"
       and name = flag "name" (required string) ~doc:"STRING CanaryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_canary
           (Values.UpdateCanaryRequest.make
              ?code:(Option.map ~f:Values.CanaryCodeInput.of_json code)
              ?executionRoleArn ?runtimeVersion
              ?schedule:(Option.map ~f:Values.CanaryScheduleInput.of_json
                           schedule)
              ?runConfig:(Option.map ~f:Values.CanaryRunConfigInput.of_json
                            runConfig) ?successRetentionPeriodInDays
              ?failureRetentionPeriodInDays
              ?vpcConfig:(Option.map ~f:Values.VpcConfigInput.of_json
                            vpcConfig)
              ?visualReference:(Option.map
                                  ~f:Values.VisualReferenceInput.of_json
                                  visualReference) ?artifactS3Location
              ?artifactConfig:(Option.map
                                 ~f:Values.ArtifactConfigInput.of_json
                                 artifactConfig) ~name ())
           (Some Values.UpdateCanaryResponse.to_json)
           (Some Values.UpdateCanaryResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-canary", create_canary);
    ("delete-canary", delete_canary);
    ("describe-canaries", describe_canaries);
    ("describe-canaries-last-run", describe_canaries_last_run);
    ("describe-runtime-versions", describe_runtime_versions);
    ("get-canary", get_canary);
    ("get-canary-runs", get_canary_runs);
    ("list-tags-for-resource", list_tags_for_resource);
    ("start-canary", start_canary);
    ("stop-canary", stop_canary);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-canary", update_canary)]
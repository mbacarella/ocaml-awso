(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | CreateCanary: (CreateCanaryRequest.t, CreateCanaryResponse.t,
  CreateCanaryResponse.error) t 
  | DeleteCanary: (DeleteCanaryRequest.t, DeleteCanaryResponse.t,
  DeleteCanaryResponse.error) t 
  | DescribeCanaries: (DescribeCanariesRequest.t, DescribeCanariesResponse.t,
  DescribeCanariesResponse.error) t 
  | DescribeCanariesLastRun: (DescribeCanariesLastRunRequest.t,
  DescribeCanariesLastRunResponse.t, DescribeCanariesLastRunResponse.error) t
  
  | DescribeRuntimeVersions: (DescribeRuntimeVersionsRequest.t,
  DescribeRuntimeVersionsResponse.t, DescribeRuntimeVersionsResponse.error) t
  
  | GetCanary: (GetCanaryRequest.t, GetCanaryResponse.t,
  GetCanaryResponse.error) t 
  | GetCanaryRuns: (GetCanaryRunsRequest.t, GetCanaryRunsResponse.t,
  GetCanaryRunsResponse.error) t 
  | ListTagsForResource: (ListTagsForResourceRequest.t,
  ListTagsForResourceResponse.t, ListTagsForResourceResponse.error) t 
  | StartCanary: (StartCanaryRequest.t, StartCanaryResponse.t,
  StartCanaryResponse.error) t 
  | StopCanary: (StopCanaryRequest.t, StopCanaryResponse.t,
  StopCanaryResponse.error) t 
  | TagResource: (TagResourceRequest.t, TagResourceResponse.t,
  TagResourceResponse.error) t 
  | UntagResource: (UntagResourceRequest.t, UntagResourceResponse.t,
  UntagResourceResponse.error) t 
  | UpdateCanary: (UpdateCanaryRequest.t, UpdateCanaryResponse.t,
  UpdateCanaryResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | CreateCanary -> `POST
  | DeleteCanary -> `DELETE
  | DescribeCanaries -> `POST
  | DescribeCanariesLastRun -> `POST
  | DescribeRuntimeVersions -> `POST
  | GetCanary -> `GET
  | GetCanaryRuns -> `POST
  | ListTagsForResource -> `GET
  | StartCanary -> `POST
  | StopCanary -> `POST
  | TagResource -> `POST
  | UntagResource -> `DELETE
  | UpdateCanary -> `PATCH
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | CreateCanary -> (Format.kasprintf Uri.of_string) "/canary"
      | DeleteCanary ->
          (Format.kasprintf Uri.of_string) "/canary/%s"
            (CanaryName.to_header x.DeleteCanaryRequest.name)
      | DescribeCanaries -> (Format.kasprintf Uri.of_string) "/canaries"
      | DescribeCanariesLastRun ->
          (Format.kasprintf Uri.of_string) "/canaries/last-run"
      | DescribeRuntimeVersions ->
          (Format.kasprintf Uri.of_string) "/runtime-versions"
      | GetCanary ->
          (Format.kasprintf Uri.of_string) "/canary/%s"
            (CanaryName.to_header x.GetCanaryRequest.name)
      | GetCanaryRuns ->
          (Format.kasprintf Uri.of_string) "/canary/%s/runs"
            (CanaryName.to_header x.GetCanaryRunsRequest.name)
      | ListTagsForResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (CanaryArn.to_header x.ListTagsForResourceRequest.resourceArn)
      | StartCanary ->
          (Format.kasprintf Uri.of_string) "/canary/%s/start"
            (CanaryName.to_header x.StartCanaryRequest.name)
      | StopCanary ->
          (Format.kasprintf Uri.of_string) "/canary/%s/stop"
            (CanaryName.to_header x.StopCanaryRequest.name)
      | TagResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (CanaryArn.to_header x.TagResourceRequest.resourceArn)
      | UntagResource ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/tags/%s"
               (CanaryArn.to_header x.UntagResourceRequest.resourceArn))
            (List.filter_opt
               [Some ("tagKeys", (TagKeyList.to_header x.tagKeys))])
      | UpdateCanary ->
          (Format.kasprintf Uri.of_string) "/canary/%s"
            (CanaryName.to_header x.UpdateCanaryRequest.name))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | CreateCanary ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("Name",
                           (CanaryName.to_value req.CreateCanaryRequest.name));
                      Some
                        ("Code",
                          (CanaryCodeInput.to_value
                             req.CreateCanaryRequest.code));
                      Some
                        ("ArtifactS3Location",
                          (String_.to_value
                             req.CreateCanaryRequest.artifactS3Location));
                      Some
                        ("ExecutionRoleArn",
                          (RoleArn.to_value
                             req.CreateCanaryRequest.executionRoleArn));
                      Some
                        ("Schedule",
                          (CanaryScheduleInput.to_value
                             req.CreateCanaryRequest.schedule));
                      Option.map req.CreateCanaryRequest.runConfig
                        ~f:(fun x ->
                              ("RunConfig",
                                (CanaryRunConfigInput.to_value x)));
                      Option.map
                        req.CreateCanaryRequest.successRetentionPeriodInDays
                        ~f:(fun x ->
                              ("SuccessRetentionPeriodInDays",
                                (MaxSize1024.to_value x)));
                      Option.map
                        req.CreateCanaryRequest.failureRetentionPeriodInDays
                        ~f:(fun x ->
                              ("FailureRetentionPeriodInDays",
                                (MaxSize1024.to_value x)));
                      Some
                        ("RuntimeVersion",
                          (String_.to_value
                             req.CreateCanaryRequest.runtimeVersion));
                      Option.map req.CreateCanaryRequest.vpcConfig
                        ~f:(fun x ->
                              ("VpcConfig", (VpcConfigInput.to_value x)));
                      Option.map req.CreateCanaryRequest.tags
                        ~f:(fun x -> ("Tags", (TagMap.to_value x)));
                      Option.map req.CreateCanaryRequest.artifactConfig
                        ~f:(fun x ->
                              ("ArtifactConfig",
                                (ArtifactConfigInput.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteCanary -> Awso.Http.Request.make (method_of_endpoint endp)
  | DescribeCanaries ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.DescribeCanariesRequest.nextToken
                         ~f:(fun x -> ("NextToken", (Token.to_value x)));
                      Option.map req.DescribeCanariesRequest.maxResults
                        ~f:(fun x ->
                              ("MaxResults", (MaxCanaryResults.to_value x)));
                      Option.map req.DescribeCanariesRequest.names
                        ~f:(fun x ->
                              ("Names",
                                (DescribeCanariesNameFilter.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DescribeCanariesLastRun ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.DescribeCanariesLastRunRequest.nextToken
                         ~f:(fun x -> ("NextToken", (Token.to_value x)));
                      Option.map
                        req.DescribeCanariesLastRunRequest.maxResults
                        ~f:(fun x -> ("MaxResults", (MaxSize100.to_value x)));
                      Option.map req.DescribeCanariesLastRunRequest.names
                        ~f:(fun x ->
                              ("Names",
                                (DescribeCanariesLastRunNameFilter.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DescribeRuntimeVersions ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.DescribeRuntimeVersionsRequest.nextToken
                         ~f:(fun x -> ("NextToken", (Token.to_value x)));
                      Option.map
                        req.DescribeRuntimeVersionsRequest.maxResults
                        ~f:(fun x -> ("MaxResults", (MaxSize100.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetCanary ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetCanaryRuns ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.GetCanaryRunsRequest.nextToken
                         ~f:(fun x -> ("NextToken", (Token.to_value x)));
                      Option.map req.GetCanaryRunsRequest.maxResults
                        ~f:(fun x -> ("MaxResults", (MaxSize100.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTagsForResource ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StartCanary ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StopCanary ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | TagResource ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("Tags",
                           (TagMap.to_value req.TagResourceRequest.tags))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UntagResource -> Awso.Http.Request.make (method_of_endpoint endp)
  | UpdateCanary -> Awso.Http.Request.make (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | CreateCanary ->
      if is_success
      then Ok (CreateCanaryResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some CreateCanaryResponse.error_of_json))
  | DeleteCanary ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DeleteCanaryResponse.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some DeleteCanaryResponse.error_of_json))
  | DescribeCanaries ->
      if is_success
      then Ok (DescribeCanariesResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some DescribeCanariesResponse.error_of_json))
  | DescribeCanariesLastRun ->
      if is_success
      then
        Ok (DescribeCanariesLastRunResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some DescribeCanariesLastRunResponse.error_of_json))
  | DescribeRuntimeVersions ->
      if is_success
      then
        Ok (DescribeRuntimeVersionsResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some DescribeRuntimeVersionsResponse.error_of_json))
  | GetCanary ->
      if is_success
      then Ok (GetCanaryResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetCanaryResponse.error_of_json))
  | GetCanaryRuns ->
      if is_success
      then Ok (GetCanaryRunsResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetCanaryRunsResponse.error_of_json))
  | ListTagsForResource ->
      if is_success
      then Ok (ListTagsForResourceResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListTagsForResourceResponse.error_of_json))
  | StartCanary ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (StartCanaryResponse.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some StartCanaryResponse.error_of_json))
  | StopCanary ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (StopCanaryResponse.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some StopCanaryResponse.error_of_json))
  | TagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (TagResourceResponse.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some TagResourceResponse.error_of_json))
  | UntagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (UntagResourceResponse.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some UntagResourceResponse.error_of_json))
  | UpdateCanary ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (UpdateCanaryResponse.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some UpdateCanaryResponse.error_of_json))
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let abort_document_version_upload =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and documentId =
         flag "document-id" (required string) ~doc:"STRING ResourceIdType"
       and versionId =
         flag "version-id" (required string)
           ~doc:"STRING DocumentVersionIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.abort_document_version_upload
           (Values.AbortDocumentVersionUploadRequest.make
              ?authenticationToken ~documentId ~versionId ()) None None])
let activate_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and userId = flag "user-id" (required string) ~doc:"STRING IdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.activate_user
           (Values.ActivateUserRequest.make ?authenticationToken ~userId ())
           (Some Values.ActivateUserResponse.to_json)
           (Some Values.ActivateUserResponse.error_to_json)])
let add_resource_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and notificationOptions =
         flag "notification-options" (optional json_arg)
           ~doc:"JSON NotificationOptions"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceIdType"
       and principals =
         flag "principals" (required json_arg) ~doc:"JSON SharePrincipalList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_resource_permissions
           (Values.AddResourcePermissionsRequest.make ?authenticationToken
              ?notificationOptions:(Option.map
                                      ~f:Values.NotificationOptions.of_json
                                      notificationOptions) ~resourceId
              ~principals:(Values.SharePrincipalList.of_json principals) ())
           (Some Values.AddResourcePermissionsResponse.to_json)
           (Some Values.AddResourcePermissionsResponse.error_to_json)])
let create_comment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and parentId =
         flag "parent-id" (optional string) ~doc:"STRING CommentIdType"
       and threadId =
         flag "thread-id" (optional string) ~doc:"STRING CommentIdType"
       and visibility =
         flag "visibility" (optional json_arg)
           ~doc:"JSON CommentVisibilityType"
       and notifyCollaborators =
         flag "notify-collaborators" (optional bool) ~doc:"BOOL BooleanType"
       and documentId =
         flag "document-id" (required string) ~doc:"STRING ResourceIdType"
       and versionId =
         flag "version-id" (required string)
           ~doc:"STRING DocumentVersionIdType"
       and text = flag "text" (required string) ~doc:"STRING CommentTextType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_comment
           (Values.CreateCommentRequest.make ?authenticationToken ?parentId
              ?threadId
              ?visibility:(Option.map ~f:Values.CommentVisibilityType.of_json
                             visibility) ?notifyCollaborators ~documentId
              ~versionId ~text ())
           (Some Values.CreateCommentResponse.to_json)
           (Some Values.CreateCommentResponse.error_to_json)])
let create_custom_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and versionId =
         flag "version-id" (optional string)
           ~doc:"STRING DocumentVersionIdType"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceIdType"
       and customMetadata =
         flag "custom-metadata" (required json_arg)
           ~doc:"JSON CustomMetadataMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_custom_metadata
           (Values.CreateCustomMetadataRequest.make ?authenticationToken
              ?versionId ~resourceId
              ~customMetadata:(Values.CustomMetadataMap.of_json
                                 customMetadata) ())
           (Some Values.CreateCustomMetadataResponse.to_json)
           (Some Values.CreateCustomMetadataResponse.error_to_json)])
let create_folder =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and name =
         flag "name" (optional string) ~doc:"STRING ResourceNameType"
       and parentFolderId =
         flag "parent-folder-id" (required string)
           ~doc:"STRING ResourceIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_folder
           (Values.CreateFolderRequest.make ?authenticationToken ?name
              ~parentFolderId ()) (Some Values.CreateFolderResponse.to_json)
           (Some Values.CreateFolderResponse.error_to_json)])
let create_labels =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceIdType"
       and labels =
         flag "labels" (required json_arg) ~doc:"JSON SharedLabels" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_labels
           (Values.CreateLabelsRequest.make ?authenticationToken ~resourceId
              ~labels:(Values.SharedLabels.of_json labels) ())
           (Some Values.CreateLabelsResponse.to_json)
           (Some Values.CreateLabelsResponse.error_to_json)])
let create_notification_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (required string) ~doc:"STRING IdType"
       and endpoint =
         flag "endpoint" (required string)
           ~doc:"STRING SubscriptionEndPointType"
       and protocol =
         flag "protocol" (required json_arg)
           ~doc:"JSON SubscriptionProtocolType"
       and subscriptionType =
         flag "subscription-type" (required json_arg)
           ~doc:"JSON SubscriptionType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_notification_subscription
           (Values.CreateNotificationSubscriptionRequest.make ~organizationId
              ~endpoint
              ~protocol:(Values.SubscriptionProtocolType.of_json protocol)
              ~subscriptionType:(Values.SubscriptionType.of_json
                                   subscriptionType) ())
           (Some Values.CreateNotificationSubscriptionResponse.to_json)
           (Some Values.CreateNotificationSubscriptionResponse.error_to_json)])
let create_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (optional string) ~doc:"STRING IdType"
       and emailAddress =
         flag "email-address" (optional string)
           ~doc:"STRING EmailAddressType"
       and timeZoneId =
         flag "time-zone-id" (optional string) ~doc:"STRING TimeZoneIdType"
       and storageRule =
         flag "storage-rule" (optional json_arg) ~doc:"JSON StorageRuleType"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and username =
         flag "username" (required string) ~doc:"STRING UsernameType"
       and givenName =
         flag "given-name" (required string)
           ~doc:"STRING UserAttributeValueType"
       and surname =
         flag "surname" (required string)
           ~doc:"STRING UserAttributeValueType"
       and password =
         flag "password" (required string) ~doc:"STRING PasswordType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_user
           (Values.CreateUserRequest.make ?organizationId ?emailAddress
              ?timeZoneId
              ?storageRule:(Option.map ~f:Values.StorageRuleType.of_json
                              storageRule) ?authenticationToken ~username
              ~givenName ~surname ~password ())
           (Some Values.CreateUserResponse.to_json)
           (Some Values.CreateUserResponse.error_to_json)])
let deactivate_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and userId = flag "user-id" (required string) ~doc:"STRING IdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deactivate_user
           (Values.DeactivateUserRequest.make ?authenticationToken ~userId ())
           None None])
let delete_comment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and documentId =
         flag "document-id" (required string) ~doc:"STRING ResourceIdType"
       and versionId =
         flag "version-id" (required string)
           ~doc:"STRING DocumentVersionIdType"
       and commentId =
         flag "comment-id" (required string) ~doc:"STRING CommentIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_comment
           (Values.DeleteCommentRequest.make ?authenticationToken ~documentId
              ~versionId ~commentId ()) None None])
let delete_custom_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and versionId =
         flag "version-id" (optional string)
           ~doc:"STRING DocumentVersionIdType"
       and keys =
         flag "keys" (optional json_arg) ~doc:"JSON CustomMetadataKeyList"
       and deleteAll =
         flag "delete-all" (optional bool) ~doc:"BOOL BooleanType"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_custom_metadata
           (Values.DeleteCustomMetadataRequest.make ?authenticationToken
              ?versionId
              ?keys:(Option.map ~f:Values.CustomMetadataKeyList.of_json keys)
              ?deleteAll ~resourceId ())
           (Some Values.DeleteCustomMetadataResponse.to_json)
           (Some Values.DeleteCustomMetadataResponse.error_to_json)])
let delete_document =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and documentId =
         flag "document-id" (required string) ~doc:"STRING ResourceIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_document
           (Values.DeleteDocumentRequest.make ?authenticationToken
              ~documentId ()) None None])
let delete_document_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and documentId =
         flag "document-id" (required string) ~doc:"STRING ResourceIdType"
       and versionId =
         flag "version-id" (required string)
           ~doc:"STRING DocumentVersionIdType"
       and deletePriorVersions =
         flag "delete-prior-versions" (required bool) ~doc:"BOOL BooleanType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_document_version
           (Values.DeleteDocumentVersionRequest.make ?authenticationToken
              ~documentId ~versionId ~deletePriorVersions ()) None None])
let delete_folder =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and folderId =
         flag "folder-id" (required string) ~doc:"STRING ResourceIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_folder
           (Values.DeleteFolderRequest.make ?authenticationToken ~folderId ())
           None None])
let delete_folder_contents =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and folderId =
         flag "folder-id" (required string) ~doc:"STRING ResourceIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_folder_contents
           (Values.DeleteFolderContentsRequest.make ?authenticationToken
              ~folderId ()) None None])
let delete_labels =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and labels =
         flag "labels" (optional json_arg) ~doc:"JSON SharedLabels"
       and deleteAll =
         flag "delete-all" (optional bool) ~doc:"BOOL BooleanType"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_labels
           (Values.DeleteLabelsRequest.make ?authenticationToken
              ?labels:(Option.map ~f:Values.SharedLabels.of_json labels)
              ?deleteAll ~resourceId ())
           (Some Values.DeleteLabelsResponse.to_json)
           (Some Values.DeleteLabelsResponse.error_to_json)])
let delete_notification_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subscriptionId =
         flag "subscription-id" (required string) ~doc:"STRING IdType"
       and organizationId =
         flag "organization-id" (required string) ~doc:"STRING IdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_notification_subscription
           (Values.DeleteNotificationSubscriptionRequest.make ~subscriptionId
              ~organizationId ()) None None])
let delete_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and userId = flag "user-id" (required string) ~doc:"STRING IdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_user
           (Values.DeleteUserRequest.make ?authenticationToken ~userId ())
           None None])
let describe_activities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON TimestampType"
       and endTime =
         flag "end-time" (optional json_arg) ~doc:"JSON TimestampType"
       and organizationId =
         flag "organization-id" (optional string) ~doc:"STRING IdType"
       and activityTypes =
         flag "activity-types" (optional string)
           ~doc:"STRING ActivityNamesFilterType"
       and resourceId =
         flag "resource-id" (optional string) ~doc:"STRING IdType"
       and userId = flag "user-id" (optional string) ~doc:"STRING IdType"
       and includeIndirectActivities =
         flag "include-indirect-activities" (optional bool)
           ~doc:"BOOL BooleanType"
       and limit = flag "limit" (optional int) ~doc:"INT LimitType"
       and marker =
         flag "marker" (optional string) ~doc:"STRING SearchMarkerType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_activities
           (Values.DescribeActivitiesRequest.make ?authenticationToken
              ?startTime:(Option.map ~f:Values.TimestampType.of_json
                            startTime)
              ?endTime:(Option.map ~f:Values.TimestampType.of_json endTime)
              ?organizationId ?activityTypes ?resourceId ?userId
              ?includeIndirectActivities ?limit ?marker ())
           (Some Values.DescribeActivitiesResponse.to_json)
           (Some Values.DescribeActivitiesResponse.error_to_json)])
let describe_comments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and limit = flag "limit" (optional int) ~doc:"INT LimitType"
       and marker = flag "marker" (optional string) ~doc:"STRING MarkerType"
       and documentId =
         flag "document-id" (required string) ~doc:"STRING ResourceIdType"
       and versionId =
         flag "version-id" (required string)
           ~doc:"STRING DocumentVersionIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_comments
           (Values.DescribeCommentsRequest.make ?authenticationToken ?limit
              ?marker ~documentId ~versionId ())
           (Some Values.DescribeCommentsResponse.to_json)
           (Some Values.DescribeCommentsResponse.error_to_json)])
let describe_document_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and marker =
         flag "marker" (optional string) ~doc:"STRING PageMarkerType"
       and limit = flag "limit" (optional int) ~doc:"INT LimitType"
       and include_ =
         flag "include-" (optional string) ~doc:"STRING FieldNamesType"
       and fields =
         flag "fields" (optional string) ~doc:"STRING FieldNamesType"
       and documentId =
         flag "document-id" (required string) ~doc:"STRING ResourceIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_document_versions
           (Values.DescribeDocumentVersionsRequest.make ?authenticationToken
              ?marker ?limit ?include_ ?fields ~documentId ())
           (Some Values.DescribeDocumentVersionsResponse.to_json)
           (Some Values.DescribeDocumentVersionsResponse.error_to_json)])
let describe_folder_contents =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and sort =
         flag "sort" (optional json_arg) ~doc:"JSON ResourceSortType"
       and order = flag "order" (optional json_arg) ~doc:"JSON OrderType"
       and limit = flag "limit" (optional int) ~doc:"INT LimitType"
       and marker =
         flag "marker" (optional string) ~doc:"STRING PageMarkerType"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON FolderContentType"
       and include_ =
         flag "include-" (optional string) ~doc:"STRING FieldNamesType"
       and folderId =
         flag "folder-id" (required string) ~doc:"STRING ResourceIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_folder_contents
           (Values.DescribeFolderContentsRequest.make ?authenticationToken
              ?sort:(Option.map ~f:Values.ResourceSortType.of_json sort)
              ?order:(Option.map ~f:Values.OrderType.of_json order) ?limit
              ?marker
              ?type_:(Option.map ~f:Values.FolderContentType.of_json type_)
              ?include_ ~folderId ())
           (Some Values.DescribeFolderContentsResponse.to_json)
           (Some Values.DescribeFolderContentsResponse.error_to_json)])
let describe_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and organizationId =
         flag "organization-id" (optional string) ~doc:"STRING IdType"
       and marker = flag "marker" (optional string) ~doc:"STRING MarkerType"
       and limit = flag "limit" (optional int) ~doc:"INT PositiveIntegerType"
       and searchQuery =
         flag "search-query" (required string) ~doc:"STRING SearchQueryType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_groups
           (Values.DescribeGroupsRequest.make ?authenticationToken
              ?organizationId ?marker ?limit ~searchQuery ())
           (Some Values.DescribeGroupsResponse.to_json)
           (Some Values.DescribeGroupsResponse.error_to_json)])
let describe_notification_subscriptions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker =
         flag "marker" (optional string) ~doc:"STRING PageMarkerType"
       and limit = flag "limit" (optional int) ~doc:"INT LimitType"
       and organizationId =
         flag "organization-id" (required string) ~doc:"STRING IdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_notification_subscriptions
           (Values.DescribeNotificationSubscriptionsRequest.make ?marker
              ?limit ~organizationId ())
           (Some Values.DescribeNotificationSubscriptionsResponse.to_json)
           (Some
              Values.DescribeNotificationSubscriptionsResponse.error_to_json)])
let describe_resource_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and principalId =
         flag "principal-id" (optional string) ~doc:"STRING IdType"
       and limit = flag "limit" (optional int) ~doc:"INT LimitType"
       and marker =
         flag "marker" (optional string) ~doc:"STRING PageMarkerType"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_resource_permissions
           (Values.DescribeResourcePermissionsRequest.make
              ?authenticationToken ?principalId ?limit ?marker ~resourceId ())
           (Some Values.DescribeResourcePermissionsResponse.to_json)
           (Some Values.DescribeResourcePermissionsResponse.error_to_json)])
let describe_root_folders =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit = flag "limit" (optional int) ~doc:"INT LimitType"
       and marker =
         flag "marker" (optional string) ~doc:"STRING PageMarkerType"
       and authenticationToken =
         flag "authentication-token" (required string)
           ~doc:"STRING AuthenticationHeaderType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_root_folders
           (Values.DescribeRootFoldersRequest.make ?limit ?marker
              ~authenticationToken ())
           (Some Values.DescribeRootFoldersResponse.to_json)
           (Some Values.DescribeRootFoldersResponse.error_to_json)])
let describe_users =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and organizationId =
         flag "organization-id" (optional string) ~doc:"STRING IdType"
       and userIds =
         flag "user-ids" (optional string) ~doc:"STRING UserIdsType"
       and query =
         flag "query" (optional string) ~doc:"STRING SearchQueryType"
       and include_ =
         flag "include-" (optional json_arg) ~doc:"JSON UserFilterType"
       and order = flag "order" (optional json_arg) ~doc:"JSON OrderType"
       and sort = flag "sort" (optional json_arg) ~doc:"JSON UserSortType"
       and marker =
         flag "marker" (optional string) ~doc:"STRING PageMarkerType"
       and limit = flag "limit" (optional int) ~doc:"INT LimitType"
       and fields =
         flag "fields" (optional string) ~doc:"STRING FieldNamesType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_users
           (Values.DescribeUsersRequest.make ?authenticationToken
              ?organizationId ?userIds ?query
              ?include_:(Option.map ~f:Values.UserFilterType.of_json include_)
              ?order:(Option.map ~f:Values.OrderType.of_json order)
              ?sort:(Option.map ~f:Values.UserSortType.of_json sort) ?marker
              ?limit ?fields ()) (Some Values.DescribeUsersResponse.to_json)
           (Some Values.DescribeUsersResponse.error_to_json)])
let get_current_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (required string)
           ~doc:"STRING AuthenticationHeaderType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_current_user
           (Values.GetCurrentUserRequest.make ~authenticationToken ())
           (Some Values.GetCurrentUserResponse.to_json)
           (Some Values.GetCurrentUserResponse.error_to_json)])
let get_document =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and includeCustomMetadata =
         flag "include-custom-metadata" (optional bool)
           ~doc:"BOOL BooleanType"
       and documentId =
         flag "document-id" (required string) ~doc:"STRING ResourceIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_document
           (Values.GetDocumentRequest.make ?authenticationToken
              ?includeCustomMetadata ~documentId ())
           (Some Values.GetDocumentResponse.to_json)
           (Some Values.GetDocumentResponse.error_to_json)])
let get_document_path =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and limit = flag "limit" (optional int) ~doc:"INT LimitType"
       and fields =
         flag "fields" (optional string) ~doc:"STRING FieldNamesType"
       and marker =
         flag "marker" (optional string) ~doc:"STRING PageMarkerType"
       and documentId =
         flag "document-id" (required string) ~doc:"STRING IdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_document_path
           (Values.GetDocumentPathRequest.make ?authenticationToken ?limit
              ?fields ?marker ~documentId ())
           (Some Values.GetDocumentPathResponse.to_json)
           (Some Values.GetDocumentPathResponse.error_to_json)])
let get_document_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and fields =
         flag "fields" (optional string) ~doc:"STRING FieldNamesType"
       and includeCustomMetadata =
         flag "include-custom-metadata" (optional bool)
           ~doc:"BOOL BooleanType"
       and documentId =
         flag "document-id" (required string) ~doc:"STRING ResourceIdType"
       and versionId =
         flag "version-id" (required string)
           ~doc:"STRING DocumentVersionIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_document_version
           (Values.GetDocumentVersionRequest.make ?authenticationToken
              ?fields ?includeCustomMetadata ~documentId ~versionId ())
           (Some Values.GetDocumentVersionResponse.to_json)
           (Some Values.GetDocumentVersionResponse.error_to_json)])
let get_folder =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and includeCustomMetadata =
         flag "include-custom-metadata" (optional bool)
           ~doc:"BOOL BooleanType"
       and folderId =
         flag "folder-id" (required string) ~doc:"STRING ResourceIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_folder
           (Values.GetFolderRequest.make ?authenticationToken
              ?includeCustomMetadata ~folderId ())
           (Some Values.GetFolderResponse.to_json)
           (Some Values.GetFolderResponse.error_to_json)])
let get_folder_path =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and limit = flag "limit" (optional int) ~doc:"INT LimitType"
       and fields =
         flag "fields" (optional string) ~doc:"STRING FieldNamesType"
       and marker =
         flag "marker" (optional string) ~doc:"STRING PageMarkerType"
       and folderId = flag "folder-id" (required string) ~doc:"STRING IdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_folder_path
           (Values.GetFolderPathRequest.make ?authenticationToken ?limit
              ?fields ?marker ~folderId ())
           (Some Values.GetFolderPathResponse.to_json)
           (Some Values.GetFolderPathResponse.error_to_json)])
let get_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and userId = flag "user-id" (optional string) ~doc:"STRING IdType"
       and collectionType =
         flag "collection-type" (optional json_arg)
           ~doc:"JSON ResourceCollectionType"
       and limit = flag "limit" (optional int) ~doc:"INT LimitType"
       and marker =
         flag "marker" (optional string) ~doc:"STRING PageMarkerType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resources
           (Values.GetResourcesRequest.make ?authenticationToken ?userId
              ?collectionType:(Option.map
                                 ~f:Values.ResourceCollectionType.of_json
                                 collectionType) ?limit ?marker ())
           (Some Values.GetResourcesResponse.to_json)
           (Some Values.GetResourcesResponse.error_to_json)])
let initiate_document_version_upload =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and id = flag "id" (optional string) ~doc:"STRING ResourceIdType"
       and name =
         flag "name" (optional string) ~doc:"STRING ResourceNameType"
       and contentCreatedTimestamp =
         flag "content-created-timestamp" (optional json_arg)
           ~doc:"JSON TimestampType"
       and contentModifiedTimestamp =
         flag "content-modified-timestamp" (optional json_arg)
           ~doc:"JSON TimestampType"
       and contentType =
         flag "content-type" (optional string)
           ~doc:"STRING DocumentContentType"
       and documentSizeInBytes =
         flag "document-size-in-bytes" (optional json_arg)
           ~doc:"JSON SizeType"
       and parentFolderId =
         flag "parent-folder-id" (optional string)
           ~doc:"STRING ResourceIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.initiate_document_version_upload
           (Values.InitiateDocumentVersionUploadRequest.make
              ?authenticationToken ?id ?name
              ?contentCreatedTimestamp:(Option.map
                                          ~f:Values.TimestampType.of_json
                                          contentCreatedTimestamp)
              ?contentModifiedTimestamp:(Option.map
                                           ~f:Values.TimestampType.of_json
                                           contentModifiedTimestamp)
              ?contentType
              ?documentSizeInBytes:(Option.map ~f:Values.SizeType.of_json
                                      documentSizeInBytes) ?parentFolderId ())
           (Some Values.InitiateDocumentVersionUploadResponse.to_json)
           (Some Values.InitiateDocumentVersionUploadResponse.error_to_json)])
let remove_all_resource_permissions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_all_resource_permissions
           (Values.RemoveAllResourcePermissionsRequest.make
              ?authenticationToken ~resourceId ()) None None])
let remove_resource_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and principalType =
         flag "principal-type" (optional json_arg) ~doc:"JSON PrincipalType"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceIdType"
       and principalId =
         flag "principal-id" (required string) ~doc:"STRING IdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_resource_permission
           (Values.RemoveResourcePermissionRequest.make ?authenticationToken
              ?principalType:(Option.map ~f:Values.PrincipalType.of_json
                                principalType) ~resourceId ~principalId ())
           None None])
let restore_document_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and documentId =
         flag "document-id" (required string) ~doc:"STRING ResourceIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_document_versions
           (Values.RestoreDocumentVersionsRequest.make ?authenticationToken
              ~documentId ()) None None])
let search_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and queryText =
         flag "query-text" (optional string) ~doc:"STRING SearchQueryType"
       and queryScopes =
         flag "query-scopes" (optional json_arg)
           ~doc:"JSON SearchQueryScopeTypeList"
       and organizationId =
         flag "organization-id" (optional string) ~doc:"STRING IdType"
       and additionalResponseFields =
         flag "additional-response-fields" (optional json_arg)
           ~doc:"JSON AdditionalResponseFieldsList"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters"
       and orderBy =
         flag "order-by" (optional json_arg) ~doc:"JSON SearchResultSortList"
       and limit =
         flag "limit" (optional int) ~doc:"INT SearchResultsLimitType"
       and marker =
         flag "marker" (optional string) ~doc:"STRING NextMarkerType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_resources
           (Values.SearchResourcesRequest.make ?authenticationToken
              ?queryText
              ?queryScopes:(Option.map
                              ~f:Values.SearchQueryScopeTypeList.of_json
                              queryScopes) ?organizationId
              ?additionalResponseFields:(Option.map
                                           ~f:Values.AdditionalResponseFieldsList.of_json
                                           additionalResponseFields)
              ?filters:(Option.map ~f:Values.Filters.of_json filters)
              ?orderBy:(Option.map ~f:Values.SearchResultSortList.of_json
                          orderBy) ?limit ?marker ())
           (Some Values.SearchResourcesResponse.to_json)
           (Some Values.SearchResourcesResponse.error_to_json)])
let update_document =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and name =
         flag "name" (optional string) ~doc:"STRING ResourceNameType"
       and parentFolderId =
         flag "parent-folder-id" (optional string)
           ~doc:"STRING ResourceIdType"
       and resourceState =
         flag "resource-state" (optional json_arg)
           ~doc:"JSON ResourceStateType"
       and documentId =
         flag "document-id" (required string) ~doc:"STRING ResourceIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_document
           (Values.UpdateDocumentRequest.make ?authenticationToken ?name
              ?parentFolderId
              ?resourceState:(Option.map ~f:Values.ResourceStateType.of_json
                                resourceState) ~documentId ()) None None])
let update_document_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and versionStatus =
         flag "version-status" (optional json_arg)
           ~doc:"JSON DocumentVersionStatus"
       and documentId =
         flag "document-id" (required string) ~doc:"STRING ResourceIdType"
       and versionId =
         flag "version-id" (required string)
           ~doc:"STRING DocumentVersionIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_document_version
           (Values.UpdateDocumentVersionRequest.make ?authenticationToken
              ?versionStatus:(Option.map
                                ~f:Values.DocumentVersionStatus.of_json
                                versionStatus) ~documentId ~versionId ())
           None None])
let update_folder =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and name =
         flag "name" (optional string) ~doc:"STRING ResourceNameType"
       and parentFolderId =
         flag "parent-folder-id" (optional string)
           ~doc:"STRING ResourceIdType"
       and resourceState =
         flag "resource-state" (optional json_arg)
           ~doc:"JSON ResourceStateType"
       and folderId =
         flag "folder-id" (required string) ~doc:"STRING ResourceIdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_folder
           (Values.UpdateFolderRequest.make ?authenticationToken ?name
              ?parentFolderId
              ?resourceState:(Option.map ~f:Values.ResourceStateType.of_json
                                resourceState) ~folderId ()) None None])
let update_user =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authenticationToken =
         flag "authentication-token" (optional string)
           ~doc:"STRING AuthenticationHeaderType"
       and givenName =
         flag "given-name" (optional string)
           ~doc:"STRING UserAttributeValueType"
       and surname =
         flag "surname" (optional string)
           ~doc:"STRING UserAttributeValueType"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON UserType"
       and storageRule =
         flag "storage-rule" (optional json_arg) ~doc:"JSON StorageRuleType"
       and timeZoneId =
         flag "time-zone-id" (optional string) ~doc:"STRING TimeZoneIdType"
       and locale = flag "locale" (optional json_arg) ~doc:"JSON LocaleType"
       and grantPoweruserPrivileges =
         flag "grant-poweruser-privileges" (optional json_arg)
           ~doc:"JSON BooleanEnumType"
       and userId = flag "user-id" (required string) ~doc:"STRING IdType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user
           (Values.UpdateUserRequest.make ?authenticationToken ?givenName
              ?surname ?type_:(Option.map ~f:Values.UserType.of_json type_)
              ?storageRule:(Option.map ~f:Values.StorageRuleType.of_json
                              storageRule) ?timeZoneId
              ?locale:(Option.map ~f:Values.LocaleType.of_json locale)
              ?grantPoweruserPrivileges:(Option.map
                                           ~f:Values.BooleanEnumType.of_json
                                           grantPoweruserPrivileges) ~userId
              ()) (Some Values.UpdateUserResponse.to_json)
           (Some Values.UpdateUserResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("abort-document-version-upload", abort_document_version_upload);
    ("activate-user", activate_user);
    ("add-resource-permissions", add_resource_permissions);
    ("create-comment", create_comment);
    ("create-custom-metadata", create_custom_metadata);
    ("create-folder", create_folder);
    ("create-labels", create_labels);
    ("create-notification-subscription", create_notification_subscription);
    ("create-user", create_user);
    ("deactivate-user", deactivate_user);
    ("delete-comment", delete_comment);
    ("delete-custom-metadata", delete_custom_metadata);
    ("delete-document", delete_document);
    ("delete-document-version", delete_document_version);
    ("delete-folder", delete_folder);
    ("delete-folder-contents", delete_folder_contents);
    ("delete-labels", delete_labels);
    ("delete-notification-subscription", delete_notification_subscription);
    ("delete-user", delete_user);
    ("describe-activities", describe_activities);
    ("describe-comments", describe_comments);
    ("describe-document-versions", describe_document_versions);
    ("describe-folder-contents", describe_folder_contents);
    ("describe-groups", describe_groups);
    ("describe-notification-subscriptions",
      describe_notification_subscriptions);
    ("describe-resource-permissions", describe_resource_permissions);
    ("describe-root-folders", describe_root_folders);
    ("describe-users", describe_users);
    ("get-current-user", get_current_user);
    ("get-document", get_document);
    ("get-document-path", get_document_path);
    ("get-document-version", get_document_version);
    ("get-folder", get_folder);
    ("get-folder-path", get_folder_path);
    ("get-resources", get_resources);
    ("initiate-document-version-upload", initiate_document_version_upload);
    ("remove-all-resource-permissions", remove_all_resource_permissions);
    ("remove-resource-permission", remove_resource_permission);
    ("restore-document-versions", restore_document_versions);
    ("search-resources", search_resources);
    ("update-document", update_document);
    ("update-document-version", update_document_version);
    ("update-folder", update_folder);
    ("update-user", update_user)]
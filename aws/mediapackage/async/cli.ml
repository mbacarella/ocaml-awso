(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let configure_logs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and egressAccessLogs =
         flag "egress-access-logs" (optional json_arg)
           ~doc:"JSON EgressAccessLogs"
       and ingressAccessLogs =
         flag "ingress-access-logs" (optional json_arg)
           ~doc:"JSON IngressAccessLogs"
       and id = flag "id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.configure_logs
           (Values.ConfigureLogsRequest.make
              ?egressAccessLogs:(Option.map
                                   ~f:Values.EgressAccessLogs.of_json
                                   egressAccessLogs)
              ?ingressAccessLogs:(Option.map
                                    ~f:Values.IngressAccessLogs.of_json
                                    ingressAccessLogs) ~id ())
           (Some Values.ConfigureLogsResponse.to_json)
           (Some Values.ConfigureLogsResponse.error_to_json)])
let create_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING __string"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and id = flag "id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_channel
           (Values.CreateChannelRequest.make ?description
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~id ())
           (Some Values.CreateChannelResponse.to_json)
           (Some Values.CreateChannelResponse.error_to_json)])
let create_harvest_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endTime = flag "end-time" (required string) ~doc:"STRING __string"
       and id = flag "id" (required string) ~doc:"STRING __string"
       and originEndpointId =
         flag "origin-endpoint-id" (required string) ~doc:"STRING __string"
       and s3Destination =
         flag "s3-destination" (required json_arg) ~doc:"JSON S3Destination"
       and startTime =
         flag "start-time" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_harvest_job
           (Values.CreateHarvestJobRequest.make ~endTime ~id
              ~originEndpointId
              ~s3Destination:(Values.S3Destination.of_json s3Destination)
              ~startTime ()) (Some Values.CreateHarvestJobResponse.to_json)
           (Some Values.CreateHarvestJobResponse.error_to_json)])
let create_origin_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authorization =
         flag "authorization" (optional json_arg) ~doc:"JSON Authorization"
       and cmafPackage =
         flag "cmaf-package" (optional json_arg)
           ~doc:"JSON CmafPackageCreateOrUpdateParameters"
       and dashPackage =
         flag "dash-package" (optional json_arg) ~doc:"JSON DashPackage"
       and description =
         flag "description" (optional string) ~doc:"STRING __string"
       and hlsPackage =
         flag "hls-package" (optional json_arg) ~doc:"JSON HlsPackage"
       and manifestName =
         flag "manifest-name" (optional string) ~doc:"STRING __string"
       and mssPackage =
         flag "mss-package" (optional json_arg) ~doc:"JSON MssPackage"
       and origination =
         flag "origination" (optional json_arg) ~doc:"JSON Origination"
       and startoverWindowSeconds =
         flag "startover-window-seconds" (optional int) ~doc:"INT __integer"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and timeDelaySeconds =
         flag "time-delay-seconds" (optional int) ~doc:"INT __integer"
       and whitelist =
         flag "whitelist" (optional json_arg) ~doc:"JSON __listOf__string"
       and channelId =
         flag "channel-id" (required string) ~doc:"STRING __string"
       and id = flag "id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_origin_endpoint
           (Values.CreateOriginEndpointRequest.make
              ?authorization:(Option.map ~f:Values.Authorization.of_json
                                authorization)
              ?cmafPackage:(Option.map
                              ~f:Values.CmafPackageCreateOrUpdateParameters.of_json
                              cmafPackage)
              ?dashPackage:(Option.map ~f:Values.DashPackage.of_json
                              dashPackage) ?description
              ?hlsPackage:(Option.map ~f:Values.HlsPackage.of_json hlsPackage)
              ?manifestName
              ?mssPackage:(Option.map ~f:Values.MssPackage.of_json mssPackage)
              ?origination:(Option.map ~f:Values.Origination.of_json
                              origination) ?startoverWindowSeconds
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?timeDelaySeconds
              ?whitelist:(Option.map ~f:Values.Zz__listOf__string.of_json
                            whitelist) ~channelId ~id ())
           (Some Values.CreateOriginEndpointResponse.to_json)
           (Some Values.CreateOriginEndpointResponse.error_to_json)])
let delete_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_channel (Values.DeleteChannelRequest.make ~id ())
           (Some Values.DeleteChannelResponse.to_json)
           (Some Values.DeleteChannelResponse.error_to_json)])
let delete_origin_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_origin_endpoint
           (Values.DeleteOriginEndpointRequest.make ~id ())
           (Some Values.DeleteOriginEndpointResponse.to_json)
           (Some Values.DeleteOriginEndpointResponse.error_to_json)])
let describe_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_channel (Values.DescribeChannelRequest.make ~id ())
           (Some Values.DescribeChannelResponse.to_json)
           (Some Values.DescribeChannelResponse.error_to_json)])
let describe_harvest_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_harvest_job
           (Values.DescribeHarvestJobRequest.make ~id ())
           (Some Values.DescribeHarvestJobResponse.to_json)
           (Some Values.DescribeHarvestJobResponse.error_to_json)])
let describe_origin_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_origin_endpoint
           (Values.DescribeOriginEndpointRequest.make ~id ())
           (Some Values.DescribeOriginEndpointResponse.to_json)
           (Some Values.DescribeOriginEndpointResponse.error_to_json)])
let list_channels =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_channels
           (Values.ListChannelsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListChannelsResponse.to_json)
           (Some Values.ListChannelsResponse.error_to_json)])
let list_harvest_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeChannelId =
         flag "include-channel-id" (optional string) ~doc:"STRING __string"
       and includeStatus =
         flag "include-status" (optional string) ~doc:"STRING __string"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_harvest_jobs
           (Values.ListHarvestJobsRequest.make ?includeChannelId
              ?includeStatus ?maxResults ?nextToken ())
           (Some Values.ListHarvestJobsResponse.to_json)
           (Some Values.ListHarvestJobsResponse.error_to_json)])
let list_origin_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelId =
         flag "channel-id" (optional string) ~doc:"STRING __string"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_origin_endpoints
           (Values.ListOriginEndpointsRequest.make ?channelId ?maxResults
              ?nextToken ())
           (Some Values.ListOriginEndpointsResponse.to_json)
           (Some Values.ListOriginEndpointsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let rotate_channel_credentials =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.rotate_channel_credentials
           (Values.RotateChannelCredentialsRequest.make ~id ())
           (Some Values.RotateChannelCredentialsResponse.to_json)
           (Some Values.RotateChannelCredentialsResponse.error_to_json)])
let rotate_ingest_endpoint_credentials =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING __string"
       and ingestEndpointId =
         flag "ingest-endpoint-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.rotate_ingest_endpoint_credentials
           (Values.RotateIngestEndpointCredentialsRequest.make ~id
              ~ingestEndpointId ())
           (Some Values.RotateIngestEndpointCredentialsResponse.to_json)
           (Some Values.RotateIngestEndpointCredentialsResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string"
       and tags = flag "tags" (required json_arg) ~doc:"JSON __mapOf__string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Zz__mapOf__string.of_json tags) ()) None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON __listOf__string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.Zz__listOf__string.of_json tagKeys) ()) None
           None])
let update_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING __string"
       and id = flag "id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_channel
           (Values.UpdateChannelRequest.make ?description ~id ())
           (Some Values.UpdateChannelResponse.to_json)
           (Some Values.UpdateChannelResponse.error_to_json)])
let update_origin_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and authorization =
         flag "authorization" (optional json_arg) ~doc:"JSON Authorization"
       and cmafPackage =
         flag "cmaf-package" (optional json_arg)
           ~doc:"JSON CmafPackageCreateOrUpdateParameters"
       and dashPackage =
         flag "dash-package" (optional json_arg) ~doc:"JSON DashPackage"
       and description =
         flag "description" (optional string) ~doc:"STRING __string"
       and hlsPackage =
         flag "hls-package" (optional json_arg) ~doc:"JSON HlsPackage"
       and manifestName =
         flag "manifest-name" (optional string) ~doc:"STRING __string"
       and mssPackage =
         flag "mss-package" (optional json_arg) ~doc:"JSON MssPackage"
       and origination =
         flag "origination" (optional json_arg) ~doc:"JSON Origination"
       and startoverWindowSeconds =
         flag "startover-window-seconds" (optional int) ~doc:"INT __integer"
       and timeDelaySeconds =
         flag "time-delay-seconds" (optional int) ~doc:"INT __integer"
       and whitelist =
         flag "whitelist" (optional json_arg) ~doc:"JSON __listOf__string"
       and id = flag "id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_origin_endpoint
           (Values.UpdateOriginEndpointRequest.make
              ?authorization:(Option.map ~f:Values.Authorization.of_json
                                authorization)
              ?cmafPackage:(Option.map
                              ~f:Values.CmafPackageCreateOrUpdateParameters.of_json
                              cmafPackage)
              ?dashPackage:(Option.map ~f:Values.DashPackage.of_json
                              dashPackage) ?description
              ?hlsPackage:(Option.map ~f:Values.HlsPackage.of_json hlsPackage)
              ?manifestName
              ?mssPackage:(Option.map ~f:Values.MssPackage.of_json mssPackage)
              ?origination:(Option.map ~f:Values.Origination.of_json
                              origination) ?startoverWindowSeconds
              ?timeDelaySeconds
              ?whitelist:(Option.map ~f:Values.Zz__listOf__string.of_json
                            whitelist) ~id ())
           (Some Values.UpdateOriginEndpointResponse.to_json)
           (Some Values.UpdateOriginEndpointResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("configure-logs", configure_logs);
    ("create-channel", create_channel);
    ("create-harvest-job", create_harvest_job);
    ("create-origin-endpoint", create_origin_endpoint);
    ("delete-channel", delete_channel);
    ("delete-origin-endpoint", delete_origin_endpoint);
    ("describe-channel", describe_channel);
    ("describe-harvest-job", describe_harvest_job);
    ("describe-origin-endpoint", describe_origin_endpoint);
    ("list-channels", list_channels);
    ("list-harvest-jobs", list_harvest_jobs);
    ("list-origin-endpoints", list_origin_endpoints);
    ("list-tags-for-resource", list_tags_for_resource);
    ("rotate-channel-credentials", rotate_channel_credentials);
    ("rotate-ingest-endpoint-credentials",
      rotate_ingest_endpoint_credentials);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-channel", update_channel);
    ("update-origin-endpoint", update_origin_endpoint)]
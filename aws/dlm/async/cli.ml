(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_lifecycle_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyDetails =
         flag "policy-details" (optional json_arg) ~doc:"JSON PolicyDetails"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and defaultPolicy =
         flag "default-policy" (optional json_arg)
           ~doc:"JSON DefaultPolicyTypeValues"
       and createInterval =
         flag "create-interval" (optional int) ~doc:"INT CreateInterval"
       and retainInterval =
         flag "retain-interval" (optional int) ~doc:"INT RetainInterval"
       and copyTags =
         flag "copy-tags" (optional bool) ~doc:"BOOL CopyTagsNullable"
       and extendDeletion =
         flag "extend-deletion" (optional bool) ~doc:"BOOL ExtendDeletion"
       and crossRegionCopyTargets =
         flag "cross-region-copy-targets" (optional json_arg)
           ~doc:"JSON CrossRegionCopyTargetList"
       and exclusions =
         flag "exclusions" (optional json_arg) ~doc:"JSON Exclusions"
       and executionRoleArn =
         flag "execution-role-arn" (required string)
           ~doc:"STRING ExecutionRoleArn"
       and description =
         flag "description" (required string) ~doc:"STRING PolicyDescription"
       and state =
         flag "state" (required json_arg)
           ~doc:"JSON SettablePolicyStateValues" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_lifecycle_policy
           (Values.CreateLifecyclePolicyRequest.make
              ?policyDetails:(Option.map ~f:Values.PolicyDetails.of_json
                                policyDetails)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?defaultPolicy:(Option.map
                                ~f:Values.DefaultPolicyTypeValues.of_json
                                defaultPolicy) ?createInterval
              ?retainInterval ?copyTags ?extendDeletion
              ?crossRegionCopyTargets:(Option.map
                                         ~f:Values.CrossRegionCopyTargetList.of_json
                                         crossRegionCopyTargets)
              ?exclusions:(Option.map ~f:Values.Exclusions.of_json exclusions)
              ~executionRoleArn ~description
              ~state:(Values.SettablePolicyStateValues.of_json state) ())
           (Some Values.CreateLifecyclePolicyResponse.to_json)
           (Some Values.CreateLifecyclePolicyResponse.error_to_json)])
let delete_lifecycle_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyId =
         flag "policy-id" (required string) ~doc:"STRING PolicyId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_lifecycle_policy
           (Values.DeleteLifecyclePolicyRequest.make ~policyId ())
           (Some Values.DeleteLifecyclePolicyResponse.to_json)
           (Some Values.DeleteLifecyclePolicyResponse.error_to_json)])
let get_lifecycle_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyIds =
         flag "policy-ids" (optional json_arg) ~doc:"JSON PolicyIdList"
       and state =
         flag "state" (optional json_arg)
           ~doc:"JSON GettablePolicyStateValues"
       and resourceTypes =
         flag "resource-types" (optional json_arg)
           ~doc:"JSON ResourceTypeValuesList"
       and targetTags =
         flag "target-tags" (optional json_arg)
           ~doc:"JSON TargetTagsFilterList"
       and tagsToAdd =
         flag "tags-to-add" (optional json_arg)
           ~doc:"JSON TagsToAddFilterList"
       and defaultPolicyType =
         flag "default-policy-type" (optional json_arg)
           ~doc:"JSON DefaultPoliciesTypeValues" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_lifecycle_policies
           (Values.GetLifecyclePoliciesRequest.make
              ?policyIds:(Option.map ~f:Values.PolicyIdList.of_json policyIds)
              ?state:(Option.map ~f:Values.GettablePolicyStateValues.of_json
                        state)
              ?resourceTypes:(Option.map
                                ~f:Values.ResourceTypeValuesList.of_json
                                resourceTypes)
              ?targetTags:(Option.map ~f:Values.TargetTagsFilterList.of_json
                             targetTags)
              ?tagsToAdd:(Option.map ~f:Values.TagsToAddFilterList.of_json
                            tagsToAdd)
              ?defaultPolicyType:(Option.map
                                    ~f:Values.DefaultPoliciesTypeValues.of_json
                                    defaultPolicyType) ())
           (Some Values.GetLifecyclePoliciesResponse.to_json)
           (Some Values.GetLifecyclePoliciesResponse.error_to_json)])
let get_lifecycle_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policyId =
         flag "policy-id" (required string) ~doc:"STRING PolicyId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_lifecycle_policy
           (Values.GetLifecyclePolicyRequest.make ~policyId ())
           (Some Values.GetLifecyclePolicyResponse.to_json)
           (Some Values.GetLifecyclePolicyResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING PolicyArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING PolicyArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING PolicyArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_lifecycle_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and executionRoleArn =
         flag "execution-role-arn" (optional string)
           ~doc:"STRING ExecutionRoleArn"
       and state =
         flag "state" (optional json_arg)
           ~doc:"JSON SettablePolicyStateValues"
       and description =
         flag "description" (optional string) ~doc:"STRING PolicyDescription"
       and policyDetails =
         flag "policy-details" (optional json_arg) ~doc:"JSON PolicyDetails"
       and createInterval =
         flag "create-interval" (optional int) ~doc:"INT CreateInterval"
       and retainInterval =
         flag "retain-interval" (optional int) ~doc:"INT RetainInterval"
       and copyTags =
         flag "copy-tags" (optional bool) ~doc:"BOOL CopyTagsNullable"
       and extendDeletion =
         flag "extend-deletion" (optional bool) ~doc:"BOOL ExtendDeletion"
       and crossRegionCopyTargets =
         flag "cross-region-copy-targets" (optional json_arg)
           ~doc:"JSON CrossRegionCopyTargetList"
       and exclusions =
         flag "exclusions" (optional json_arg) ~doc:"JSON Exclusions"
       and policyId =
         flag "policy-id" (required string) ~doc:"STRING PolicyId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_lifecycle_policy
           (Values.UpdateLifecyclePolicyRequest.make ?executionRoleArn
              ?state:(Option.map ~f:Values.SettablePolicyStateValues.of_json
                        state) ?description
              ?policyDetails:(Option.map ~f:Values.PolicyDetails.of_json
                                policyDetails) ?createInterval
              ?retainInterval ?copyTags ?extendDeletion
              ?crossRegionCopyTargets:(Option.map
                                         ~f:Values.CrossRegionCopyTargetList.of_json
                                         crossRegionCopyTargets)
              ?exclusions:(Option.map ~f:Values.Exclusions.of_json exclusions)
              ~policyId ())
           (Some Values.UpdateLifecyclePolicyResponse.to_json)
           (Some Values.UpdateLifecyclePolicyResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-lifecycle-policy", create_lifecycle_policy);
    ("delete-lifecycle-policy", delete_lifecycle_policy);
    ("get-lifecycle-policies", get_lifecycle_policies);
    ("get-lifecycle-policy", get_lifecycle_policy);
    ("list-tags-for-resource", list_tags_for_resource);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-lifecycle-policy", update_lifecycle_policy)]
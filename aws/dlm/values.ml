(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.dlm
let apiVersion = "2018-01-12"
let endpointPrefix = "dlm"
let serviceFullName = "Amazon Data Lifecycle Manager"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let serviceAbbreviation = "Amazon DLM"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module Interval =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:1); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Interval" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module RetentionIntervalUnitValues =
  struct
    type nonrec t =
      | DAYS 
      | WEEKS 
      | MONTHS 
      | YEARS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DAYS -> "DAYS"
      | WEEKS -> "WEEKS"
      | MONTHS -> "MONTHS"
      | YEARS -> "YEARS"
      | Non_static_id s -> s
    let of_string =
      function
      | "DAYS" -> DAYS
      | "WEEKS" -> WEEKS
      | "MONTHS" -> MONTHS
      | "YEARS" -> YEARS
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration RetentionIntervalUnitValues"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"RetentionIntervalUnitValues" j)
    let to_json = simple_to_json to_value
  end
module CmkArn =
  struct
    type nonrec t = string
    let context_ = "CmkArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:aws(-[a-z]{1,3}){0,2}:kms:([a-z]+-){2,3}\\d:\\d+:key/.*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CmkArn" j
    let to_json = simple_to_json to_value
  end
module Encrypted =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module AwsAccountId =
  struct
    type nonrec t = string
    let context_ = "AwsAccountId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:12) >>=
             (fun () ->
                (check_string_max i ~max:12) >>=
                  (fun () -> check_pattern i ~pattern:"^[0-9]{12}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AwsAccountId" j
    let to_json = simple_to_json to_value
  end
module CrossRegionCopyRetainRule =
  struct
    type nonrec t =
      {
      interval: Interval.t option
        [@ocaml.doc
          "The amount of time to retain each snapshot. The maximum is 100 years. This is equivalent to 1200 months, 5200 weeks, or 36500 days."];
      intervalUnit: RetentionIntervalUnitValues.t option
        [@ocaml.doc "The unit of time for time-based retention."]}
    let make ?interval =
      fun ?intervalUnit -> fun () -> { interval; intervalUnit }
    let to_value x =
      structure_to_value
        [("Interval", (Option.map x.interval ~f:Interval.to_value));
        ("IntervalUnit",
          (Option.map x.intervalUnit ~f:RetentionIntervalUnitValues.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let intervalUnit =
        (Option.map ~f:RetentionIntervalUnitValues.of_xml)
          (Xml.child xml_arg0 "IntervalUnit") in
      let interval =
        (Option.map ~f:Interval.of_xml) (Xml.child xml_arg0 "Interval") in
      make ?intervalUnit ?interval ()
    let of_json json =
      let intervalUnit =
        field_map json "IntervalUnit" RetentionIntervalUnitValues.of_json in
      let interval = field_map json "Interval" Interval.of_json in
      make ?intervalUnit ?interval ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies the retention rule for cross-Region snapshot copies."]
module EncryptionConfiguration =
  struct
    type nonrec t =
      {
      encrypted: Encrypted.t
        [@ocaml.doc
          "To encrypt a copy of an unencrypted snapshot when encryption by default is not enabled, enable encryption using this parameter. Copies of encrypted snapshots are encrypted, even if this parameter is false or when encryption by default is not enabled."];
      cmkArn: CmkArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the KMS key to use for EBS encryption. If this parameter is not specified, the default KMS key for the account is used."]}
    let context_ = "EncryptionConfiguration"
    let make ?cmkArn = fun ~encrypted -> fun () -> { cmkArn; encrypted }
    let to_value x =
      structure_to_value
        [("Encrypted", (Some (Encrypted.to_value x.encrypted)));
        ("CmkArn", (Option.map x.cmkArn ~f:CmkArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cmkArn =
        (Option.map ~f:CmkArn.of_xml) (Xml.child xml_arg0 "CmkArn") in
      let encrypted =
        Encrypted.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Encrypted") in
      make ?cmkArn ~encrypted ()
    let of_json json =
      let cmkArn = field_map json "CmkArn" CmkArn.of_json in
      let encrypted = field_map_exn json "Encrypted" Encrypted.of_json in
      make ?cmkArn ~encrypted ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies the encryption settings for shared snapshots that are copied across Regions."]
module Target =
  struct
    type nonrec t = string
    let context_ = "Target"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () -> check_pattern i ~pattern:"^[\\w:\\-\\/\\*]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Target" j
    let to_json = simple_to_json to_value
  end
module Time =
  struct
    type nonrec t = string
    let context_ = "Time"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:5) >>=
             (fun () ->
                (check_string_max i ~max:5) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^(0[0-9]|1[0-9]|2[0-3]):[0-5][0-9]$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Time" j
    let to_json = simple_to_json to_value
  end
module CopyTagsNullable =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module CrossRegionCopyDeprecateRule =
  struct
    type nonrec t =
      {
      interval: Interval.t option
        [@ocaml.doc
          "The period after which to deprecate the cross-Region AMI copies. The period must be less than or equal to the cross-Region AMI copy retention period, and it can't be greater than 10 years. This is equivalent to 120 months, 520 weeks, or 3650 days."];
      intervalUnit: RetentionIntervalUnitValues.t option
        [@ocaml.doc "The unit of time in which to measure the Interval."]}
    let make ?interval =
      fun ?intervalUnit -> fun () -> { interval; intervalUnit }
    let to_value x =
      structure_to_value
        [("Interval", (Option.map x.interval ~f:Interval.to_value));
        ("IntervalUnit",
          (Option.map x.intervalUnit ~f:RetentionIntervalUnitValues.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let intervalUnit =
        (Option.map ~f:RetentionIntervalUnitValues.of_xml)
          (Xml.child xml_arg0 "IntervalUnit") in
      let interval =
        (Option.map ~f:Interval.of_xml) (Xml.child xml_arg0 "Interval") in
      make ?intervalUnit ?interval ()
    let of_json json =
      let intervalUnit =
        field_map json "IntervalUnit" RetentionIntervalUnitValues.of_json in
      let interval = field_map json "Interval" Interval.of_json in
      make ?intervalUnit ?interval ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies an AMI deprecation rule for cross-Region AMI copies created by a cross-Region copy rule."]
module TargetRegion =
  struct
    type nonrec t = string
    let context_ = "TargetRegion"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:16) >>=
                  (fun () -> check_pattern i ~pattern:"([a-z]+-){2,3}\\d")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TargetRegion" j
    let to_json = simple_to_json to_value
  end
module AvailabilityZone =
  struct
    type nonrec t = string
    let context_ = "AvailabilityZone"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:16) >>=
                  (fun () ->
                     check_pattern i ~pattern:"([a-z]+-){2,3}\\d[a-z]")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AvailabilityZone" j
    let to_json = simple_to_json to_value
  end
module ShareTargetAccountList =
  struct
    type nonrec t = AwsAccountId.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let to_value xs =
      (xs |> (List.map ~f:AwsAccountId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AwsAccountId.of_xml)
    let of_json j =
      list_of_json ~kind:"ShareTargetAccountList"
        ~of_json:AwsAccountId.of_json j
    let to_json v = composed_to_json to_value v
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:500) >>=
                  (fun () -> check_pattern i ~pattern:"[\\p{all}]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module CrossRegionCopyAction =
  struct
    type nonrec t =
      {
      target: Target.t [@ocaml.doc "The target Region."];
      encryptionConfiguration: EncryptionConfiguration.t
        [@ocaml.doc "The encryption settings for the copied snapshot."];
      retainRule: CrossRegionCopyRetainRule.t option }
    let context_ = "CrossRegionCopyAction"
    let make ?retainRule =
      fun ~target ->
        fun ~encryptionConfiguration ->
          fun () -> { retainRule; target; encryptionConfiguration }
    let to_value x =
      structure_to_value
        [("Target", (Some (Target.to_value x.target)));
        ("EncryptionConfiguration",
          (Some (EncryptionConfiguration.to_value x.encryptionConfiguration)));
        ("RetainRule",
          (Option.map x.retainRule ~f:CrossRegionCopyRetainRule.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let retainRule =
        (Option.map ~f:CrossRegionCopyRetainRule.of_xml)
          (Xml.child xml_arg0 "RetainRule") in
      let encryptionConfiguration =
        EncryptionConfiguration.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "EncryptionConfiguration") in
      let target =
        Target.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Target") in
      make ?retainRule ~encryptionConfiguration ~target ()
    let of_json json =
      let retainRule =
        field_map json "RetainRule" CrossRegionCopyRetainRule.of_json in
      let encryptionConfiguration =
        field_map_exn json "EncryptionConfiguration"
          EncryptionConfiguration.of_json in
      let target = field_map_exn json "Target" Target.of_json in
      make ?retainRule ~encryptionConfiguration ~target ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies a rule for copying shared snapshots across Regions."]
module CronExpression =
  struct
    type nonrec t = string
    let context_ = "CronExpression"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:17) >>=
             (fun () ->
                (check_string_max i ~max:106) >>=
                  (fun () ->
                     check_pattern i ~pattern:"cron\\([^\\n]{11,100}\\)")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CronExpression" j
    let to_json = simple_to_json to_value
  end
module IntervalUnitValues =
  struct
    type nonrec t =
      | HOURS 
      | Non_static_id of string 
    let make i = i
    let to_string = function | HOURS -> "HOURS" | Non_static_id s -> s
    let of_string = function | "HOURS" -> HOURS | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration IntervalUnitValues" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"IntervalUnitValues" j)
    let to_json = simple_to_json to_value
  end
module LocationValues =
  struct
    type nonrec t =
      | CLOUD 
      | OUTPOST_LOCAL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CLOUD -> "CLOUD"
      | OUTPOST_LOCAL -> "OUTPOST_LOCAL"
      | Non_static_id s -> s
    let of_string =
      function
      | "CLOUD" -> CLOUD
      | "OUTPOST_LOCAL" -> OUTPOST_LOCAL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration LocationValues" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"LocationValues" j)
    let to_json = simple_to_json to_value
  end
module TimesList =
  struct
    type nonrec t = Time.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:1); i
    let to_value xs =
      (xs |> (List.map ~f:Time.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Time.of_xml)
    let of_json j = list_of_json ~kind:"TimesList" ~of_json:Time.of_json j
    let to_json v = composed_to_json to_value v
  end
module CrossRegionCopyRule =
  struct
    type nonrec t =
      {
      targetRegion: TargetRegion.t option
        [@ocaml.doc
          "Avoid using this parameter when creating new policies. Instead, use Target to specify a target Region or a target Outpost for snapshot copies. For policies created before the Target parameter was introduced, this parameter indicates the target Region for snapshot copies."];
      target: Target.t option
        [@ocaml.doc
          "The target Region or the Amazon Resource Name (ARN) of the target Outpost for the snapshot copies. Use this parameter instead of TargetRegion. Do not specify both."];
      encrypted: Encrypted.t
        [@ocaml.doc
          "To encrypt a copy of an unencrypted snapshot if encryption by default is not enabled, enable encryption using this parameter. Copies of encrypted snapshots are encrypted, even if this parameter is false or if encryption by default is not enabled."];
      cmkArn: CmkArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the KMS key to use for EBS encryption. If this parameter is not specified, the default KMS key for the account is used."];
      copyTags: CopyTagsNullable.t option
        [@ocaml.doc
          "Indicates whether to copy all user-defined tags from the source snapshot to the cross-Region snapshot copy."];
      retainRule: CrossRegionCopyRetainRule.t option
        [@ocaml.doc
          "The retention rule that indicates how long snapshot copies are to be retained in the destination Region."];
      deprecateRule: CrossRegionCopyDeprecateRule.t option
        [@ocaml.doc
          "The AMI deprecation rule for cross-Region AMI copies created by the rule."]}
    let context_ = "CrossRegionCopyRule"
    let make ?targetRegion =
      fun ?target ->
        fun ?cmkArn ->
          fun ?copyTags ->
            fun ?retainRule ->
              fun ?deprecateRule ->
                fun ~encrypted ->
                  fun () ->
                    {
                      targetRegion;
                      target;
                      cmkArn;
                      copyTags;
                      retainRule;
                      deprecateRule;
                      encrypted
                    }
    let to_value x =
      structure_to_value
        [("TargetRegion",
           (Option.map x.targetRegion ~f:TargetRegion.to_value));
        ("Target", (Option.map x.target ~f:Target.to_value));
        ("Encrypted", (Some (Encrypted.to_value x.encrypted)));
        ("CmkArn", (Option.map x.cmkArn ~f:CmkArn.to_value));
        ("CopyTags", (Option.map x.copyTags ~f:CopyTagsNullable.to_value));
        ("RetainRule",
          (Option.map x.retainRule ~f:CrossRegionCopyRetainRule.to_value));
        ("DeprecateRule",
          (Option.map x.deprecateRule
             ~f:CrossRegionCopyDeprecateRule.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deprecateRule =
        (Option.map ~f:CrossRegionCopyDeprecateRule.of_xml)
          (Xml.child xml_arg0 "DeprecateRule") in
      let retainRule =
        (Option.map ~f:CrossRegionCopyRetainRule.of_xml)
          (Xml.child xml_arg0 "RetainRule") in
      let copyTags =
        (Option.map ~f:CopyTagsNullable.of_xml)
          (Xml.child xml_arg0 "CopyTags") in
      let cmkArn =
        (Option.map ~f:CmkArn.of_xml) (Xml.child xml_arg0 "CmkArn") in
      let encrypted =
        Encrypted.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Encrypted") in
      let target =
        (Option.map ~f:Target.of_xml) (Xml.child xml_arg0 "Target") in
      let targetRegion =
        (Option.map ~f:TargetRegion.of_xml)
          (Xml.child xml_arg0 "TargetRegion") in
      make ?deprecateRule ?retainRule ?copyTags ?cmkArn ~encrypted ?target
        ?targetRegion ()
    let of_json json =
      let deprecateRule =
        field_map json "DeprecateRule" CrossRegionCopyDeprecateRule.of_json in
      let retainRule =
        field_map json "RetainRule" CrossRegionCopyRetainRule.of_json in
      let copyTags = field_map json "CopyTags" CopyTagsNullable.of_json in
      let cmkArn = field_map json "CmkArn" CmkArn.of_json in
      let encrypted = field_map_exn json "Encrypted" Encrypted.of_json in
      let target = field_map json "Target" Target.of_json in
      let targetRegion = field_map json "TargetRegion" TargetRegion.of_json in
      make ?deprecateRule ?retainRule ?copyTags ?cmkArn ~encrypted ?target
        ?targetRegion ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies a rule for cross-Region snapshot copies."]
module Count =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1000) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Count" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module AvailabilityZoneList =
  struct
    type nonrec t = AvailabilityZone.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:AvailabilityZone.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AvailabilityZone.of_xml)
    let of_json j =
      list_of_json ~kind:"AvailabilityZoneList"
        ~of_json:AvailabilityZone.of_json j
    let to_json v = composed_to_json to_value v
  end
module ShareRule =
  struct
    type nonrec t =
      {
      targetAccounts: ShareTargetAccountList.t
        [@ocaml.doc
          "The IDs of the Amazon Web Services accounts with which to share the snapshots."];
      unshareInterval: Interval.t option
        [@ocaml.doc
          "The period after which snapshots that are shared with other Amazon Web Services accounts are automatically unshared."];
      unshareIntervalUnit: RetentionIntervalUnitValues.t option
        [@ocaml.doc "The unit of time for the automatic unsharing interval."]}
    let context_ = "ShareRule"
    let make ?unshareInterval =
      fun ?unshareIntervalUnit ->
        fun ~targetAccounts ->
          fun () -> { unshareInterval; unshareIntervalUnit; targetAccounts }
    let to_value x =
      structure_to_value
        [("TargetAccounts",
           (Some (ShareTargetAccountList.to_value x.targetAccounts)));
        ("UnshareInterval",
          (Option.map x.unshareInterval ~f:Interval.to_value));
        ("UnshareIntervalUnit",
          (Option.map x.unshareIntervalUnit
             ~f:RetentionIntervalUnitValues.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unshareIntervalUnit =
        (Option.map ~f:RetentionIntervalUnitValues.of_xml)
          (Xml.child xml_arg0 "UnshareIntervalUnit") in
      let unshareInterval =
        (Option.map ~f:Interval.of_xml)
          (Xml.child xml_arg0 "UnshareInterval") in
      let targetAccounts =
        ShareTargetAccountList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TargetAccounts") in
      make ?unshareIntervalUnit ?unshareInterval ~targetAccounts ()
    let of_json json =
      let unshareIntervalUnit =
        field_map json "UnshareIntervalUnit"
          RetentionIntervalUnitValues.of_json in
      let unshareInterval = field_map json "UnshareInterval" Interval.of_json in
      let targetAccounts =
        field_map_exn json "TargetAccounts" ShareTargetAccountList.of_json in
      make ?unshareIntervalUnit ?unshareInterval ~targetAccounts ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies a rule for sharing snapshots across Amazon Web Services accounts."]
module Tag =
  struct
    type nonrec t =
      {
      key: String_.t [@ocaml.doc "The tag key."];
      value: String_.t [@ocaml.doc "The tag value."]}
    let context_ = "Tag"
    let make ~key = fun ~value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("Key", (Some (String_.to_value x.key)));
        ("Value", (Some (String_.to_value x.value)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Value") in
      let key =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Key") in
      make ~value ~key ()
    let of_json json =
      let value = field_map_exn json "Value" String_.of_json in
      let key = field_map_exn json "Key" String_.of_json in
      make ~value ~key ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies a tag for a resource."]
module ActionName =
  struct
    type nonrec t = string
    let context_ = "ActionName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:120) >>=
                  (fun () -> check_pattern i ~pattern:"[0-9A-Za-z _-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ActionName" j
    let to_json = simple_to_json to_value
  end
module CrossRegionCopyActionList =
  struct
    type nonrec t = CrossRegionCopyAction.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:3) >>= (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:CrossRegionCopyAction.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CrossRegionCopyAction.of_xml)
    let of_json j =
      list_of_json ~kind:"CrossRegionCopyActionList"
        ~of_json:CrossRegionCopyAction.of_json j
    let to_json v = composed_to_json to_value v
  end
module DescriptionRegex =
  struct
    type nonrec t = string
    let context_ = "DescriptionRegex"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:1000) >>=
                  (fun () -> check_pattern i ~pattern:"[\\p{all}]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DescriptionRegex" j
    let to_json = simple_to_json to_value
  end
module EventTypeValues =
  struct
    type nonrec t =
      | ShareSnapshot 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | ShareSnapshot -> "shareSnapshot" | Non_static_id s -> s
    let of_string =
      function | "shareSnapshot" -> ShareSnapshot | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration EventTypeValues" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"EventTypeValues" j)
    let to_json = simple_to_json to_value
  end
module SnapshotOwnerList =
  struct
    type nonrec t = AwsAccountId.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:AwsAccountId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AwsAccountId.of_xml)
    let of_json j =
      list_of_json ~kind:"SnapshotOwnerList" ~of_json:AwsAccountId.of_json j
    let to_json v = composed_to_json to_value v
  end
module CopyTags =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module CreateRule =
  struct
    type nonrec t =
      {
      location: LocationValues.t option
        [@ocaml.doc
          "Specifies the destination for snapshots created by the policy. To create snapshots in the same Region as the source resource, specify CLOUD. To create snapshots on the same Outpost as the source resource, specify OUTPOST_LOCAL. If you omit this parameter, CLOUD is used by default. If the policy targets resources in an Amazon Web Services Region, then you must create snapshots in the same Region as the source resource. If the policy targets resources on an Outpost, then you can create snapshots on the same Outpost as the source resource, or in the Region of that Outpost."];
      interval: Interval.t option
        [@ocaml.doc
          "The interval between snapshots. The supported values are 1, 2, 3, 4, 6, 8, 12, and 24."];
      intervalUnit: IntervalUnitValues.t option
        [@ocaml.doc "The interval unit."];
      times: TimesList.t option
        [@ocaml.doc
          "The time, in UTC, to start the operation. The supported format is hh:mm. The operation occurs within a one-hour window following the specified time. If you do not specify a time, Amazon DLM selects a time within the next 24 hours."];
      cronExpression: CronExpression.t option
        [@ocaml.doc
          "The schedule, as a Cron expression. The schedule interval must be between 1 hour and 1 year. For more information, see Cron expressions in the Amazon CloudWatch User Guide."]}
    let make ?location =
      fun ?interval ->
        fun ?intervalUnit ->
          fun ?times ->
            fun ?cronExpression ->
              fun () ->
                { location; interval; intervalUnit; times; cronExpression }
    let to_value x =
      structure_to_value
        [("Location", (Option.map x.location ~f:LocationValues.to_value));
        ("Interval", (Option.map x.interval ~f:Interval.to_value));
        ("IntervalUnit",
          (Option.map x.intervalUnit ~f:IntervalUnitValues.to_value));
        ("Times", (Option.map x.times ~f:TimesList.to_value));
        ("CronExpression",
          (Option.map x.cronExpression ~f:CronExpression.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cronExpression =
        (Option.map ~f:CronExpression.of_xml)
          (Xml.child xml_arg0 "CronExpression") in
      let times =
        (Option.map ~f:TimesList.of_xml) (Xml.child xml_arg0 "Times") in
      let intervalUnit =
        (Option.map ~f:IntervalUnitValues.of_xml)
          (Xml.child xml_arg0 "IntervalUnit") in
      let interval =
        (Option.map ~f:Interval.of_xml) (Xml.child xml_arg0 "Interval") in
      let location =
        (Option.map ~f:LocationValues.of_xml) (Xml.child xml_arg0 "Location") in
      make ?cronExpression ?times ?intervalUnit ?interval ?location ()
    let of_json json =
      let cronExpression =
        field_map json "CronExpression" CronExpression.of_json in
      let times = field_map json "Times" TimesList.of_json in
      let intervalUnit =
        field_map json "IntervalUnit" IntervalUnitValues.of_json in
      let interval = field_map json "Interval" Interval.of_json in
      let location = field_map json "Location" LocationValues.of_json in
      make ?cronExpression ?times ?intervalUnit ?interval ?location ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies when to create snapshots of EBS volumes. You must specify either a Cron expression or an interval, interval unit, and start time. You cannot specify both."]
module CrossRegionCopyRules =
  struct
    type nonrec t = CrossRegionCopyRule.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:3) >>= (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:CrossRegionCopyRule.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:CrossRegionCopyRule.of_xml)
    let of_json j =
      list_of_json ~kind:"CrossRegionCopyRules"
        ~of_json:CrossRegionCopyRule.of_json j
    let to_json v = composed_to_json to_value v
  end
module DeprecateRule =
  struct
    type nonrec t =
      {
      count: Count.t option
        [@ocaml.doc
          "If the schedule has a count-based retention rule, this parameter specifies the number of oldest AMIs to deprecate. The count must be less than or equal to the schedule's retention count, and it can't be greater than 1000."];
      interval: Interval.t option
        [@ocaml.doc
          "If the schedule has an age-based retention rule, this parameter specifies the period after which to deprecate AMIs created by the schedule. The period must be less than or equal to the schedule's retention period, and it can't be greater than 10 years. This is equivalent to 120 months, 520 weeks, or 3650 days."];
      intervalUnit: RetentionIntervalUnitValues.t option
        [@ocaml.doc "The unit of time in which to measure the Interval."]}
    let make ?count =
      fun ?interval ->
        fun ?intervalUnit -> fun () -> { count; interval; intervalUnit }
    let to_value x =
      structure_to_value
        [("Count", (Option.map x.count ~f:Count.to_value));
        ("Interval", (Option.map x.interval ~f:Interval.to_value));
        ("IntervalUnit",
          (Option.map x.intervalUnit ~f:RetentionIntervalUnitValues.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let intervalUnit =
        (Option.map ~f:RetentionIntervalUnitValues.of_xml)
          (Xml.child xml_arg0 "IntervalUnit") in
      let interval =
        (Option.map ~f:Interval.of_xml) (Xml.child xml_arg0 "Interval") in
      let count = (Option.map ~f:Count.of_xml) (Xml.child xml_arg0 "Count") in
      make ?intervalUnit ?interval ?count ()
    let of_json json =
      let intervalUnit =
        field_map json "IntervalUnit" RetentionIntervalUnitValues.of_json in
      let interval = field_map json "Interval" Interval.of_json in
      let count = field_map json "Count" Count.of_json in
      make ?intervalUnit ?interval ?count ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies an AMI deprecation rule for a schedule."]
module FastRestoreRule =
  struct
    type nonrec t =
      {
      count: Count.t option
        [@ocaml.doc
          "The number of snapshots to be enabled with fast snapshot restore."];
      interval: Interval.t option
        [@ocaml.doc
          "The amount of time to enable fast snapshot restore. The maximum is 100 years. This is equivalent to 1200 months, 5200 weeks, or 36500 days."];
      intervalUnit: RetentionIntervalUnitValues.t option
        [@ocaml.doc "The unit of time for enabling fast snapshot restore."];
      availabilityZones: AvailabilityZoneList.t
        [@ocaml.doc
          "The Availability Zones in which to enable fast snapshot restore."]}
    let context_ = "FastRestoreRule"
    let make ?count =
      fun ?interval ->
        fun ?intervalUnit ->
          fun ~availabilityZones ->
            fun () -> { count; interval; intervalUnit; availabilityZones }
    let to_value x =
      structure_to_value
        [("Count", (Option.map x.count ~f:Count.to_value));
        ("Interval", (Option.map x.interval ~f:Interval.to_value));
        ("IntervalUnit",
          (Option.map x.intervalUnit ~f:RetentionIntervalUnitValues.to_value));
        ("AvailabilityZones",
          (Some (AvailabilityZoneList.to_value x.availabilityZones)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let availabilityZones =
        AvailabilityZoneList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AvailabilityZones") in
      let intervalUnit =
        (Option.map ~f:RetentionIntervalUnitValues.of_xml)
          (Xml.child xml_arg0 "IntervalUnit") in
      let interval =
        (Option.map ~f:Interval.of_xml) (Xml.child xml_arg0 "Interval") in
      let count = (Option.map ~f:Count.of_xml) (Xml.child xml_arg0 "Count") in
      make ~availabilityZones ?intervalUnit ?interval ?count ()
    let of_json json =
      let availabilityZones =
        field_map_exn json "AvailabilityZones" AvailabilityZoneList.of_json in
      let intervalUnit =
        field_map json "IntervalUnit" RetentionIntervalUnitValues.of_json in
      let interval = field_map json "Interval" Interval.of_json in
      let count = field_map json "Count" Count.of_json in
      make ~availabilityZones ?intervalUnit ?interval ?count ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies a rule for enabling fast snapshot restore. You can enable fast snapshot restore based on either a count or a time interval."]
module RetainRule =
  struct
    type nonrec t =
      {
      count: Count.t option
        [@ocaml.doc
          "The number of snapshots to retain for each volume, up to a maximum of 1000."];
      interval: Interval.t option
        [@ocaml.doc
          "The amount of time to retain each snapshot. The maximum is 100 years. This is equivalent to 1200 months, 5200 weeks, or 36500 days."];
      intervalUnit: RetentionIntervalUnitValues.t option
        [@ocaml.doc "The unit of time for time-based retention."]}
    let make ?count =
      fun ?interval ->
        fun ?intervalUnit -> fun () -> { count; interval; intervalUnit }
    let to_value x =
      structure_to_value
        [("Count", (Option.map x.count ~f:Count.to_value));
        ("Interval", (Option.map x.interval ~f:Interval.to_value));
        ("IntervalUnit",
          (Option.map x.intervalUnit ~f:RetentionIntervalUnitValues.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let intervalUnit =
        (Option.map ~f:RetentionIntervalUnitValues.of_xml)
          (Xml.child xml_arg0 "IntervalUnit") in
      let interval =
        (Option.map ~f:Interval.of_xml) (Xml.child xml_arg0 "Interval") in
      let count = (Option.map ~f:Count.of_xml) (Xml.child xml_arg0 "Count") in
      make ?intervalUnit ?interval ?count ()
    let of_json json =
      let intervalUnit =
        field_map json "IntervalUnit" RetentionIntervalUnitValues.of_json in
      let interval = field_map json "Interval" Interval.of_json in
      let count = field_map json "Count" Count.of_json in
      make ?intervalUnit ?interval ?count ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies the retention rule for a lifecycle policy. You can retain snapshots based on either a count or a time interval."]
module ScheduleName =
  struct
    type nonrec t = string
    let context_ = "ScheduleName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:120) >>=
                  (fun () -> check_pattern i ~pattern:"[0-9A-Za-z _-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ScheduleName" j
    let to_json = simple_to_json to_value
  end
module ShareRules =
  struct
    type nonrec t = ShareRule.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:1) >>= (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:ShareRule.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ShareRule.of_xml)
    let of_json j =
      list_of_json ~kind:"ShareRules" ~of_json:ShareRule.of_json j
    let to_json v = composed_to_json to_value v
  end
module TagsToAddList =
  struct
    type nonrec t = Tag.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:45) >>= (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:Tag.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tag.of_xml)
    let of_json j = list_of_json ~kind:"TagsToAddList" ~of_json:Tag.of_json j
    let to_json v = composed_to_json to_value v
  end
module VariableTagsList =
  struct
    type nonrec t = Tag.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:45) >>= (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:Tag.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tag.of_xml)
    let of_json j =
      list_of_json ~kind:"VariableTagsList" ~of_json:Tag.of_json j
    let to_json v = composed_to_json to_value v
  end
module Action =
  struct
    type nonrec t =
      {
      name: ActionName.t [@ocaml.doc "A descriptive name for the action."];
      crossRegionCopy: CrossRegionCopyActionList.t
        [@ocaml.doc "The rule for copying shared snapshots across Regions."]}
    let context_ = "Action"
    let make ~name =
      fun ~crossRegionCopy -> fun () -> { name; crossRegionCopy }
    let to_value x =
      structure_to_value
        [("Name", (Some (ActionName.to_value x.name)));
        ("CrossRegionCopy",
          (Some (CrossRegionCopyActionList.to_value x.crossRegionCopy)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let crossRegionCopy =
        CrossRegionCopyActionList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CrossRegionCopy") in
      let name =
        ActionName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ~crossRegionCopy ~name ()
    let of_json json =
      let crossRegionCopy =
        field_map_exn json "CrossRegionCopy"
          CrossRegionCopyActionList.of_json in
      let name = field_map_exn json "Name" ActionName.of_json in
      make ~crossRegionCopy ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies an action for an event-based policy."]
module EventParameters =
  struct
    type nonrec t =
      {
      eventType: EventTypeValues.t
        [@ocaml.doc
          "The type of event. Currently, only snapshot sharing events are supported."];
      snapshotOwner: SnapshotOwnerList.t
        [@ocaml.doc
          "The IDs of the Amazon Web Services accounts that can trigger policy by sharing snapshots with your account. The policy only runs if one of the specified Amazon Web Services accounts shares a snapshot with your account."];
      descriptionRegex: DescriptionRegex.t
        [@ocaml.doc
          "The snapshot description that can trigger the policy. The description pattern is specified using a regular expression. The policy runs only if a snapshot with a description that matches the specified pattern is shared with your account. For example, specifying ^.*Created for policy: policy-1234567890abcdef0.*$ configures the policy to run only if snapshots created by policy policy-1234567890abcdef0 are shared with your account."]}
    let context_ = "EventParameters"
    let make ~eventType =
      fun ~snapshotOwner ->
        fun ~descriptionRegex ->
          fun () -> { eventType; snapshotOwner; descriptionRegex }
    let to_value x =
      structure_to_value
        [("EventType", (Some (EventTypeValues.to_value x.eventType)));
        ("SnapshotOwner",
          (Some (SnapshotOwnerList.to_value x.snapshotOwner)));
        ("DescriptionRegex",
          (Some (DescriptionRegex.to_value x.descriptionRegex)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let descriptionRegex =
        DescriptionRegex.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DescriptionRegex") in
      let snapshotOwner =
        SnapshotOwnerList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SnapshotOwner") in
      let eventType =
        EventTypeValues.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "EventType") in
      make ~descriptionRegex ~snapshotOwner ~eventType ()
    let of_json json =
      let descriptionRegex =
        field_map_exn json "DescriptionRegex" DescriptionRegex.of_json in
      let snapshotOwner =
        field_map_exn json "SnapshotOwner" SnapshotOwnerList.of_json in
      let eventType = field_map_exn json "EventType" EventTypeValues.of_json in
      make ~descriptionRegex ~snapshotOwner ~eventType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies an event that triggers an event-based policy."]
module EventSourceValues =
  struct
    type nonrec t =
      | MANAGED_CWE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | MANAGED_CWE -> "MANAGED_CWE" | Non_static_id s -> s
    let of_string =
      function | "MANAGED_CWE" -> MANAGED_CWE | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration EventSourceValues" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"EventSourceValues" j)
    let to_json = simple_to_json to_value
  end
module ExcludeBootVolume =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module NoReboot =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module ResourceLocationValues =
  struct
    type nonrec t =
      | CLOUD 
      | OUTPOST 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CLOUD -> "CLOUD"
      | OUTPOST -> "OUTPOST"
      | Non_static_id s -> s
    let of_string =
      function
      | "CLOUD" -> CLOUD
      | "OUTPOST" -> OUTPOST
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ResourceLocationValues" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ResourceLocationValues" j)
    let to_json = simple_to_json to_value
  end
module ResourceTypeValues =
  struct
    type nonrec t =
      | VOLUME 
      | INSTANCE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | VOLUME -> "VOLUME"
      | INSTANCE -> "INSTANCE"
      | Non_static_id s -> s
    let of_string =
      function
      | "VOLUME" -> VOLUME
      | "INSTANCE" -> INSTANCE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ResourceTypeValues" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ResourceTypeValues" j)
    let to_json = simple_to_json to_value
  end
module Schedule =
  struct
    type nonrec t =
      {
      name: ScheduleName.t option [@ocaml.doc "The name of the schedule."];
      copyTags: CopyTags.t option
        [@ocaml.doc
          "Copy all user-defined tags on a source volume to snapshots of the volume created by this policy."];
      tagsToAdd: TagsToAddList.t option
        [@ocaml.doc
          "The tags to apply to policy-created resources. These user-defined tags are in addition to the Amazon Web Services-added lifecycle tags."];
      variableTags: VariableTagsList.t option
        [@ocaml.doc
          "A collection of key/value pairs with values determined dynamically when the policy is executed. Keys may be any valid Amazon EC2 tag key. Values must be in one of the two following formats: $(instance-id) or $(timestamp). Variable tags are only valid for EBS Snapshot Management \226\128\147 Instance policies."];
      createRule: CreateRule.t option [@ocaml.doc "The creation rule."];
      retainRule: RetainRule.t option [@ocaml.doc "The retention rule."];
      fastRestoreRule: FastRestoreRule.t option
        [@ocaml.doc "The rule for enabling fast snapshot restore."];
      crossRegionCopyRules: CrossRegionCopyRules.t option
        [@ocaml.doc
          "The rule for cross-Region snapshot copies. You can only specify cross-Region copy rules for policies that create snapshots in a Region. If the policy creates snapshots on an Outpost, then you cannot copy the snapshots to a Region or to an Outpost. If the policy creates snapshots in a Region, then snapshots can be copied to up to three Regions or Outposts."];
      shareRules: ShareRules.t option
        [@ocaml.doc
          "The rule for sharing snapshots with other Amazon Web Services accounts."];
      deprecateRule: DeprecateRule.t option
        [@ocaml.doc "The AMI deprecation rule for the schedule."]}
    let make ?name =
      fun ?copyTags ->
        fun ?tagsToAdd ->
          fun ?variableTags ->
            fun ?createRule ->
              fun ?retainRule ->
                fun ?fastRestoreRule ->
                  fun ?crossRegionCopyRules ->
                    fun ?shareRules ->
                      fun ?deprecateRule ->
                        fun () ->
                          {
                            name;
                            copyTags;
                            tagsToAdd;
                            variableTags;
                            createRule;
                            retainRule;
                            fastRestoreRule;
                            crossRegionCopyRules;
                            shareRules;
                            deprecateRule
                          }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:ScheduleName.to_value));
        ("CopyTags", (Option.map x.copyTags ~f:CopyTags.to_value));
        ("TagsToAdd", (Option.map x.tagsToAdd ~f:TagsToAddList.to_value));
        ("VariableTags",
          (Option.map x.variableTags ~f:VariableTagsList.to_value));
        ("CreateRule", (Option.map x.createRule ~f:CreateRule.to_value));
        ("RetainRule", (Option.map x.retainRule ~f:RetainRule.to_value));
        ("FastRestoreRule",
          (Option.map x.fastRestoreRule ~f:FastRestoreRule.to_value));
        ("CrossRegionCopyRules",
          (Option.map x.crossRegionCopyRules ~f:CrossRegionCopyRules.to_value));
        ("ShareRules", (Option.map x.shareRules ~f:ShareRules.to_value));
        ("DeprecateRule",
          (Option.map x.deprecateRule ~f:DeprecateRule.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let deprecateRule =
        (Option.map ~f:DeprecateRule.of_xml)
          (Xml.child xml_arg0 "DeprecateRule") in
      let shareRules =
        (Option.map ~f:ShareRules.of_xml) (Xml.child xml_arg0 "ShareRules") in
      let crossRegionCopyRules =
        (Option.map ~f:CrossRegionCopyRules.of_xml)
          (Xml.child xml_arg0 "CrossRegionCopyRules") in
      let fastRestoreRule =
        (Option.map ~f:FastRestoreRule.of_xml)
          (Xml.child xml_arg0 "FastRestoreRule") in
      let retainRule =
        (Option.map ~f:RetainRule.of_xml) (Xml.child xml_arg0 "RetainRule") in
      let createRule =
        (Option.map ~f:CreateRule.of_xml) (Xml.child xml_arg0 "CreateRule") in
      let variableTags =
        (Option.map ~f:VariableTagsList.of_xml)
          (Xml.child xml_arg0 "VariableTags") in
      let tagsToAdd =
        (Option.map ~f:TagsToAddList.of_xml) (Xml.child xml_arg0 "TagsToAdd") in
      let copyTags =
        (Option.map ~f:CopyTags.of_xml) (Xml.child xml_arg0 "CopyTags") in
      let name =
        (Option.map ~f:ScheduleName.of_xml) (Xml.child xml_arg0 "Name") in
      make ?deprecateRule ?shareRules ?crossRegionCopyRules ?fastRestoreRule
        ?retainRule ?createRule ?variableTags ?tagsToAdd ?copyTags ?name ()
    let of_json json =
      let deprecateRule =
        field_map json "DeprecateRule" DeprecateRule.of_json in
      let shareRules = field_map json "ShareRules" ShareRules.of_json in
      let crossRegionCopyRules =
        field_map json "CrossRegionCopyRules" CrossRegionCopyRules.of_json in
      let fastRestoreRule =
        field_map json "FastRestoreRule" FastRestoreRule.of_json in
      let retainRule = field_map json "RetainRule" RetainRule.of_json in
      let createRule = field_map json "CreateRule" CreateRule.of_json in
      let variableTags =
        field_map json "VariableTags" VariableTagsList.of_json in
      let tagsToAdd = field_map json "TagsToAdd" TagsToAddList.of_json in
      let copyTags = field_map json "CopyTags" CopyTags.of_json in
      let name = field_map json "Name" ScheduleName.of_json in
      make ?deprecateRule ?shareRules ?crossRegionCopyRules ?fastRestoreRule
        ?retainRule ?createRule ?variableTags ?tagsToAdd ?copyTags ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies a backup schedule for a snapshot or AMI lifecycle policy."]
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^(?!aws:)[a-zA-Z+-=._:/]+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_pattern i ~pattern:"[\\p{all}]*"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module Parameter =
  struct
    type nonrec t = string
    let context_ = "Parameter"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Parameter" j
    let to_json = simple_to_json to_value
  end
module PolicyId =
  struct
    type nonrec t = string
    let context_ = "PolicyId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"policy-[A-Za-z0-9]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PolicyId" j
    let to_json = simple_to_json to_value
  end
module ActionList =
  struct
    type nonrec t = Action.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:1) >>= (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:Action.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Action.of_xml)
    let of_json j = list_of_json ~kind:"ActionList" ~of_json:Action.of_json j
    let to_json v = composed_to_json to_value v
  end
module EventSource =
  struct
    type nonrec t =
      {
      type_: EventSourceValues.t
        [@ocaml.doc
          "The source of the event. Currently only managed CloudWatch Events rules are supported."];
      parameters: EventParameters.t option
        [@ocaml.doc "Information about the event."]}
    let context_ = "EventSource"
    let make ?parameters = fun ~type_ -> fun () -> { parameters; type_ }
    let to_value x =
      structure_to_value
        [("Type", (Some (EventSourceValues.to_value x.type_)));
        ("Parameters", (Option.map x.parameters ~f:EventParameters.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let parameters =
        (Option.map ~f:EventParameters.of_xml)
          (Xml.child xml_arg0 "Parameters") in
      let type_ =
        EventSourceValues.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Type") in
      make ?parameters ~type_ ()
    let of_json json =
      let parameters = field_map json "Parameters" EventParameters.of_json in
      let type_ = field_map_exn json "Type" EventSourceValues.of_json in
      make ?parameters ~type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies an event that triggers an event-based policy."]
module Parameters =
  struct
    type nonrec t =
      {
      excludeBootVolume: ExcludeBootVolume.t option
        [@ocaml.doc
          "[EBS Snapshot Management \226\128\147 Instance policies only] Indicates whether to exclude the root volume from snapshots created using CreateSnapshots. The default is false."];
      noReboot: NoReboot.t option
        [@ocaml.doc
          "Applies to AMI lifecycle policies only. Indicates whether targeted instances are rebooted when the lifecycle policy runs. true indicates that targeted instances are not rebooted when the policy runs. false indicates that target instances are rebooted when the policy runs. The default is true (instances are not rebooted)."]}
    let make ?excludeBootVolume =
      fun ?noReboot -> fun () -> { excludeBootVolume; noReboot }
    let to_value x =
      structure_to_value
        [("ExcludeBootVolume",
           (Option.map x.excludeBootVolume ~f:ExcludeBootVolume.to_value));
        ("NoReboot", (Option.map x.noReboot ~f:NoReboot.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let noReboot =
        (Option.map ~f:NoReboot.of_xml) (Xml.child xml_arg0 "NoReboot") in
      let excludeBootVolume =
        (Option.map ~f:ExcludeBootVolume.of_xml)
          (Xml.child xml_arg0 "ExcludeBootVolume") in
      make ?noReboot ?excludeBootVolume ()
    let of_json json =
      let noReboot = field_map json "NoReboot" NoReboot.of_json in
      let excludeBootVolume =
        field_map json "ExcludeBootVolume" ExcludeBootVolume.of_json in
      make ?noReboot ?excludeBootVolume ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies optional parameters to add to a policy. The set of valid parameters depends on the combination of policy type and resource type."]
module PolicyTypeValues =
  struct
    type nonrec t =
      | EBS_SNAPSHOT_MANAGEMENT 
      | IMAGE_MANAGEMENT 
      | EVENT_BASED_POLICY 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | EBS_SNAPSHOT_MANAGEMENT -> "EBS_SNAPSHOT_MANAGEMENT"
      | IMAGE_MANAGEMENT -> "IMAGE_MANAGEMENT"
      | EVENT_BASED_POLICY -> "EVENT_BASED_POLICY"
      | Non_static_id s -> s
    let of_string =
      function
      | "EBS_SNAPSHOT_MANAGEMENT" -> EBS_SNAPSHOT_MANAGEMENT
      | "IMAGE_MANAGEMENT" -> IMAGE_MANAGEMENT
      | "EVENT_BASED_POLICY" -> EVENT_BASED_POLICY
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration PolicyTypeValues" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"PolicyTypeValues" j)
    let to_json = simple_to_json to_value
  end
module ResourceLocationList =
  struct
    type nonrec t = ResourceLocationValues.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:1) >>= (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:ResourceLocationValues.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResourceLocationValues.of_xml)
    let of_json j =
      list_of_json ~kind:"ResourceLocationList"
        ~of_json:ResourceLocationValues.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceTypeValuesList =
  struct
    type nonrec t = ResourceTypeValues.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:1) >>= (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:ResourceTypeValues.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResourceTypeValues.of_xml)
    let of_json j =
      list_of_json ~kind:"ResourceTypeValuesList"
        ~of_json:ResourceTypeValues.of_json j
    let to_json v = composed_to_json to_value v
  end
module ScheduleList =
  struct
    type nonrec t = Schedule.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:4) >>= (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:Schedule.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Schedule.of_xml)
    let of_json j =
      list_of_json ~kind:"ScheduleList" ~of_json:Schedule.of_json j
    let to_json v = composed_to_json to_value v
  end
module TargetTagList =
  struct
    type nonrec t = Tag.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:Tag.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tag.of_xml)
    let of_json j = list_of_json ~kind:"TargetTagList" ~of_json:Tag.of_json j
    let to_json v = composed_to_json to_value v
  end
module GettablePolicyStateValues =
  struct
    type nonrec t =
      | ENABLED 
      | DISABLED 
      | ERROR 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ENABLED -> "ENABLED"
      | DISABLED -> "DISABLED"
      | ERROR -> "ERROR"
      | Non_static_id s -> s
    let of_string =
      function
      | "ENABLED" -> ENABLED
      | "DISABLED" -> DISABLED
      | "ERROR" -> ERROR
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration GettablePolicyStateValues" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"GettablePolicyStateValues" j)
    let to_json = simple_to_json to_value
  end
module PolicyDescription =
  struct
    type nonrec t = string
    let context_ = "PolicyDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:500) >>=
                  (fun () -> check_pattern i ~pattern:"[0-9A-Za-z _-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PolicyDescription" j
    let to_json = simple_to_json to_value
  end
module TagMap =
  struct
    type nonrec t = (TagKey.t * TagValue.t) list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((TagKey.of_string chopped),
                              (TagValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (TagKey.to_value x) |>
                    (fun x -> (TagValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:TagKey.of_string
        ~of_json:TagValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module ErrorCode =
  struct
    type nonrec t = string
    let context_ = "ErrorCode"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorCode" j
    let to_json = simple_to_json to_value
  end
module ErrorMessage =
  struct
    type nonrec t = string
    let context_ = "ErrorMessage"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorMessage" j
    let to_json = simple_to_json to_value
  end
module ParameterList =
  struct
    type nonrec t = Parameter.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Parameter.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Parameter.of_xml)
    let of_json j =
      list_of_json ~kind:"ParameterList" ~of_json:Parameter.of_json j
    let to_json v = composed_to_json to_value v
  end
module PolicyIdList =
  struct
    type nonrec t = PolicyId.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:PolicyId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PolicyId.of_xml)
    let of_json j =
      list_of_json ~kind:"PolicyIdList" ~of_json:PolicyId.of_json j
    let to_json v = composed_to_json to_value v
  end
module ExecutionRoleArn =
  struct
    type nonrec t = string
    let context_ = "ExecutionRoleArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:aws(-[a-z]{1,3}){0,2}:iam::\\d+:role/.*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExecutionRoleArn" j
    let to_json = simple_to_json to_value
  end
module PolicyArn =
  struct
    type nonrec t = string
    let context_ = "PolicyArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^arn:aws(-[a-z]{1,3}){0,2}:dlm:[A-Za-z0-9_/.-]{0,63}:\\d+:policy/[0-9A-Za-z_-]{1,128}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PolicyArn" j
    let to_json = simple_to_json to_value
  end
module PolicyDetails =
  struct
    type nonrec t =
      {
      policyType: PolicyTypeValues.t option
        [@ocaml.doc
          "The valid target resource types and actions a policy can manage. Specify EBS_SNAPSHOT_MANAGEMENT to create a lifecycle policy that manages the lifecycle of Amazon EBS snapshots. Specify IMAGE_MANAGEMENT to create a lifecycle policy that manages the lifecycle of EBS-backed AMIs. Specify EVENT_BASED_POLICY to create an event-based policy that performs specific actions when a defined event occurs in your Amazon Web Services account. The default is EBS_SNAPSHOT_MANAGEMENT."];
      resourceTypes: ResourceTypeValuesList.t option
        [@ocaml.doc
          "The target resource type for snapshot and AMI lifecycle policies. Use VOLUME to create snapshots of individual volumes or use INSTANCE to create multi-volume snapshots from the volumes for an instance. This parameter is required for snapshot and AMI policies only. If you are creating an event-based policy, omit this parameter."];
      resourceLocations: ResourceLocationList.t option
        [@ocaml.doc
          "The location of the resources to backup. If the source resources are located in an Amazon Web Services Region, specify CLOUD. If the source resources are located on an Outpost in your account, specify OUTPOST. If you specify OUTPOST, Amazon Data Lifecycle Manager backs up all resources of the specified type with matching target tags across all of the Outposts in your account."];
      targetTags: TargetTagList.t option
        [@ocaml.doc
          "The single tag that identifies targeted resources for this policy. This parameter is required for snapshot and AMI policies only. If you are creating an event-based policy, omit this parameter."];
      schedules: ScheduleList.t option
        [@ocaml.doc
          "The schedules of policy-defined actions for snapshot and AMI lifecycle policies. A policy can have up to four schedules\226\128\148one mandatory schedule and up to three optional schedules. This parameter is required for snapshot and AMI policies only. If you are creating an event-based policy, omit this parameter."];
      parameters: Parameters.t option
        [@ocaml.doc
          "A set of optional parameters for snapshot and AMI lifecycle policies. This parameter is required for snapshot and AMI policies only. If you are creating an event-based policy, omit this parameter."];
      eventSource: EventSource.t option
        [@ocaml.doc
          "The event that triggers the event-based policy. This parameter is required for event-based policies only. If you are creating a snapshot or AMI policy, omit this parameter."];
      actions: ActionList.t option
        [@ocaml.doc
          "The actions to be performed when the event-based policy is triggered. You can specify only one action per policy. This parameter is required for event-based policies only. If you are creating a snapshot or AMI policy, omit this parameter."]}
    let make ?policyType =
      fun ?resourceTypes ->
        fun ?resourceLocations ->
          fun ?targetTags ->
            fun ?schedules ->
              fun ?parameters ->
                fun ?eventSource ->
                  fun ?actions ->
                    fun () ->
                      {
                        policyType;
                        resourceTypes;
                        resourceLocations;
                        targetTags;
                        schedules;
                        parameters;
                        eventSource;
                        actions
                      }
    let to_value x =
      structure_to_value
        [("PolicyType",
           (Option.map x.policyType ~f:PolicyTypeValues.to_value));
        ("ResourceTypes",
          (Option.map x.resourceTypes ~f:ResourceTypeValuesList.to_value));
        ("ResourceLocations",
          (Option.map x.resourceLocations ~f:ResourceLocationList.to_value));
        ("TargetTags", (Option.map x.targetTags ~f:TargetTagList.to_value));
        ("Schedules", (Option.map x.schedules ~f:ScheduleList.to_value));
        ("Parameters", (Option.map x.parameters ~f:Parameters.to_value));
        ("EventSource", (Option.map x.eventSource ~f:EventSource.to_value));
        ("Actions", (Option.map x.actions ~f:ActionList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let actions =
        (Option.map ~f:ActionList.of_xml) (Xml.child xml_arg0 "Actions") in
      let eventSource =
        (Option.map ~f:EventSource.of_xml) (Xml.child xml_arg0 "EventSource") in
      let parameters =
        (Option.map ~f:Parameters.of_xml) (Xml.child xml_arg0 "Parameters") in
      let schedules =
        (Option.map ~f:ScheduleList.of_xml) (Xml.child xml_arg0 "Schedules") in
      let targetTags =
        (Option.map ~f:TargetTagList.of_xml)
          (Xml.child xml_arg0 "TargetTags") in
      let resourceLocations =
        (Option.map ~f:ResourceLocationList.of_xml)
          (Xml.child xml_arg0 "ResourceLocations") in
      let resourceTypes =
        (Option.map ~f:ResourceTypeValuesList.of_xml)
          (Xml.child xml_arg0 "ResourceTypes") in
      let policyType =
        (Option.map ~f:PolicyTypeValues.of_xml)
          (Xml.child xml_arg0 "PolicyType") in
      make ?actions ?eventSource ?parameters ?schedules ?targetTags
        ?resourceLocations ?resourceTypes ?policyType ()
    let of_json json =
      let actions = field_map json "Actions" ActionList.of_json in
      let eventSource = field_map json "EventSource" EventSource.of_json in
      let parameters = field_map json "Parameters" Parameters.of_json in
      let schedules = field_map json "Schedules" ScheduleList.of_json in
      let targetTags = field_map json "TargetTags" TargetTagList.of_json in
      let resourceLocations =
        field_map json "ResourceLocations" ResourceLocationList.of_json in
      let resourceTypes =
        field_map json "ResourceTypes" ResourceTypeValuesList.of_json in
      let policyType = field_map json "PolicyType" PolicyTypeValues.of_json in
      make ?actions ?eventSource ?parameters ?schedules ?targetTags
        ?resourceLocations ?resourceTypes ?policyType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Specifies the configuration of a lifecycle policy."]
module StatusMessage =
  struct
    type nonrec t = string
    let context_ = "StatusMessage"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:500) >>=
                  (fun () -> check_pattern i ~pattern:"[\\p{all}]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StatusMessage" j
    let to_json = simple_to_json to_value
  end
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module LifecyclePolicySummary =
  struct
    type nonrec t =
      {
      policyId: PolicyId.t option
        [@ocaml.doc "The identifier of the lifecycle policy."];
      description: PolicyDescription.t option
        [@ocaml.doc "The description of the lifecycle policy."];
      state: GettablePolicyStateValues.t option
        [@ocaml.doc "The activation state of the lifecycle policy."];
      tags: TagMap.t option [@ocaml.doc "The tags."];
      policyType: PolicyTypeValues.t option
        [@ocaml.doc
          "The type of policy. EBS_SNAPSHOT_MANAGEMENT indicates that the policy manages the lifecycle of Amazon EBS snapshots. IMAGE_MANAGEMENT indicates that the policy manages the lifecycle of EBS-backed AMIs."]}
    let make ?policyId =
      fun ?description ->
        fun ?state ->
          fun ?tags ->
            fun ?policyType ->
              fun () -> { policyId; description; state; tags; policyType }
    let to_value x =
      structure_to_value
        [("PolicyId", (Option.map x.policyId ~f:PolicyId.to_value));
        ("Description",
          (Option.map x.description ~f:PolicyDescription.to_value));
        ("State", (Option.map x.state ~f:GettablePolicyStateValues.to_value));
        ("Tags", (Option.map x.tags ~f:TagMap.to_value));
        ("PolicyType",
          (Option.map x.policyType ~f:PolicyTypeValues.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyType =
        (Option.map ~f:PolicyTypeValues.of_xml)
          (Xml.child xml_arg0 "PolicyType") in
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "Tags") in
      let state =
        (Option.map ~f:GettablePolicyStateValues.of_xml)
          (Xml.child xml_arg0 "State") in
      let description =
        (Option.map ~f:PolicyDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let policyId =
        (Option.map ~f:PolicyId.of_xml) (Xml.child xml_arg0 "PolicyId") in
      make ?policyType ?tags ?state ?description ?policyId ()
    let of_json json =
      let policyType = field_map json "PolicyType" PolicyTypeValues.of_json in
      let tags = field_map json "Tags" TagMap.of_json in
      let state = field_map json "State" GettablePolicyStateValues.of_json in
      let description =
        field_map json "Description" PolicyDescription.of_json in
      let policyId = field_map json "PolicyId" PolicyId.of_json in
      make ?policyType ?tags ?state ?description ?policyId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Summary information about a lifecycle policy."]
module TagFilter =
  struct
    type nonrec t = string
    let context_ = "TagFilter"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:"[\\p{all}]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagFilter" j
    let to_json = simple_to_json to_value
  end
module InternalServerException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option ;
      code: ErrorCode.t option }
    let make ?message = fun ?code -> fun () -> { message; code }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value));
        ("Code", (Option.map x.code ~f:ErrorCode.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let code = (Option.map ~f:ErrorCode.of_xml) (Xml.child xml_arg0 "Code") in
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?code ?message ()
    let of_json json =
      let code = field_map json "Code" ErrorCode.of_json in
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?code ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The service failed in an unexpected way."]
module InvalidRequestException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option ;
      code: ErrorCode.t option ;
      requiredParameters: ParameterList.t option
        [@ocaml.doc "The request omitted one or more required parameters."];
      mutuallyExclusiveParameters: ParameterList.t option
        [@ocaml.doc
          "The request included parameters that cannot be provided together."]}
    let make ?message =
      fun ?code ->
        fun ?requiredParameters ->
          fun ?mutuallyExclusiveParameters ->
            fun () ->
              {
                message;
                code;
                requiredParameters;
                mutuallyExclusiveParameters
              }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value));
        ("Code", (Option.map x.code ~f:ErrorCode.to_value));
        ("RequiredParameters",
          (Option.map x.requiredParameters ~f:ParameterList.to_value));
        ("MutuallyExclusiveParameters",
          (Option.map x.mutuallyExclusiveParameters ~f:ParameterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let mutuallyExclusiveParameters =
        (Option.map ~f:ParameterList.of_xml)
          (Xml.child xml_arg0 "MutuallyExclusiveParameters") in
      let requiredParameters =
        (Option.map ~f:ParameterList.of_xml)
          (Xml.child xml_arg0 "RequiredParameters") in
      let code = (Option.map ~f:ErrorCode.of_xml) (Xml.child xml_arg0 "Code") in
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?mutuallyExclusiveParameters ?requiredParameters ?code ?message ()
    let of_json json =
      let mutuallyExclusiveParameters =
        field_map json "MutuallyExclusiveParameters" ParameterList.of_json in
      let requiredParameters =
        field_map json "RequiredParameters" ParameterList.of_json in
      let code = field_map json "Code" ErrorCode.of_json in
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?mutuallyExclusiveParameters ?requiredParameters ?code ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Bad request. The request is missing required parameters or has invalid parameters."]
module LimitExceededException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option ;
      code: ErrorCode.t option ;
      resourceType: String_.t option
        [@ocaml.doc
          "Value is the type of resource for which a limit was exceeded."]}
    let make ?message =
      fun ?code ->
        fun ?resourceType -> fun () -> { message; code; resourceType }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value));
        ("Code", (Option.map x.code ~f:ErrorCode.to_value));
        ("ResourceType", (Option.map x.resourceType ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ResourceType") in
      let code = (Option.map ~f:ErrorCode.of_xml) (Xml.child xml_arg0 "Code") in
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?resourceType ?code ?message ()
    let of_json json =
      let resourceType = field_map json "ResourceType" String_.of_json in
      let code = field_map json "Code" ErrorCode.of_json in
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?resourceType ?code ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The request failed because a limit was exceeded."]
module ResourceNotFoundException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option ;
      code: ErrorCode.t option ;
      resourceType: String_.t option
        [@ocaml.doc "Value is the type of resource that was not found."];
      resourceIds: PolicyIdList.t option
        [@ocaml.doc "Value is a list of resource IDs that were not found."]}
    let make ?message =
      fun ?code ->
        fun ?resourceType ->
          fun ?resourceIds ->
            fun () -> { message; code; resourceType; resourceIds }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value));
        ("Code", (Option.map x.code ~f:ErrorCode.to_value));
        ("ResourceType", (Option.map x.resourceType ~f:String_.to_value));
        ("ResourceIds", (Option.map x.resourceIds ~f:PolicyIdList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceIds =
        (Option.map ~f:PolicyIdList.of_xml)
          (Xml.child xml_arg0 "ResourceIds") in
      let resourceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ResourceType") in
      let code = (Option.map ~f:ErrorCode.of_xml) (Xml.child xml_arg0 "Code") in
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?resourceIds ?resourceType ?code ?message ()
    let of_json json =
      let resourceIds = field_map json "ResourceIds" PolicyIdList.of_json in
      let resourceType = field_map json "ResourceType" String_.of_json in
      let code = field_map json "Code" ErrorCode.of_json in
      let message = field_map json "Message" ErrorMessage.of_json in
      make ?resourceIds ?resourceType ?code ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A requested resource was not found."]
module SettablePolicyStateValues =
  struct
    type nonrec t =
      | ENABLED 
      | DISABLED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ENABLED -> "ENABLED"
      | DISABLED -> "DISABLED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ENABLED" -> ENABLED
      | "DISABLED" -> DISABLED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration SettablePolicyStateValues" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"SettablePolicyStateValues" j)
    let to_json = simple_to_json to_value
  end
module TagKeyList =
  struct
    type nonrec t = TagKey.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeyList" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module LifecyclePolicy =
  struct
    type nonrec t =
      {
      policyId: PolicyId.t option
        [@ocaml.doc "The identifier of the lifecycle policy."];
      description: PolicyDescription.t option
        [@ocaml.doc "The description of the lifecycle policy."];
      state: GettablePolicyStateValues.t option
        [@ocaml.doc "The activation state of the lifecycle policy."];
      statusMessage: StatusMessage.t option
        [@ocaml.doc "The description of the status."];
      executionRoleArn: ExecutionRoleArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the IAM role used to run the operations specified by the lifecycle policy."];
      dateCreated: Timestamp.t option
        [@ocaml.doc
          "The local date and time when the lifecycle policy was created."];
      dateModified: Timestamp.t option
        [@ocaml.doc
          "The local date and time when the lifecycle policy was last modified."];
      policyDetails: PolicyDetails.t option
        [@ocaml.doc "The configuration of the lifecycle policy"];
      tags: TagMap.t option [@ocaml.doc "The tags."];
      policyArn: PolicyArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the policy."]}
    let make ?policyId =
      fun ?description ->
        fun ?state ->
          fun ?statusMessage ->
            fun ?executionRoleArn ->
              fun ?dateCreated ->
                fun ?dateModified ->
                  fun ?policyDetails ->
                    fun ?tags ->
                      fun ?policyArn ->
                        fun () ->
                          {
                            policyId;
                            description;
                            state;
                            statusMessage;
                            executionRoleArn;
                            dateCreated;
                            dateModified;
                            policyDetails;
                            tags;
                            policyArn
                          }
    let to_value x =
      structure_to_value
        [("PolicyId", (Option.map x.policyId ~f:PolicyId.to_value));
        ("Description",
          (Option.map x.description ~f:PolicyDescription.to_value));
        ("State", (Option.map x.state ~f:GettablePolicyStateValues.to_value));
        ("StatusMessage",
          (Option.map x.statusMessage ~f:StatusMessage.to_value));
        ("ExecutionRoleArn",
          (Option.map x.executionRoleArn ~f:ExecutionRoleArn.to_value));
        ("DateCreated", (Option.map x.dateCreated ~f:Timestamp.to_value));
        ("DateModified", (Option.map x.dateModified ~f:Timestamp.to_value));
        ("PolicyDetails",
          (Option.map x.policyDetails ~f:PolicyDetails.to_value));
        ("Tags", (Option.map x.tags ~f:TagMap.to_value));
        ("PolicyArn", (Option.map x.policyArn ~f:PolicyArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyArn =
        (Option.map ~f:PolicyArn.of_xml) (Xml.child xml_arg0 "PolicyArn") in
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "Tags") in
      let policyDetails =
        (Option.map ~f:PolicyDetails.of_xml)
          (Xml.child xml_arg0 "PolicyDetails") in
      let dateModified =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "DateModified") in
      let dateCreated =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "DateCreated") in
      let executionRoleArn =
        (Option.map ~f:ExecutionRoleArn.of_xml)
          (Xml.child xml_arg0 "ExecutionRoleArn") in
      let statusMessage =
        (Option.map ~f:StatusMessage.of_xml)
          (Xml.child xml_arg0 "StatusMessage") in
      let state =
        (Option.map ~f:GettablePolicyStateValues.of_xml)
          (Xml.child xml_arg0 "State") in
      let description =
        (Option.map ~f:PolicyDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let policyId =
        (Option.map ~f:PolicyId.of_xml) (Xml.child xml_arg0 "PolicyId") in
      make ?policyArn ?tags ?policyDetails ?dateModified ?dateCreated
        ?executionRoleArn ?statusMessage ?state ?description ?policyId ()
    let of_json json =
      let policyArn = field_map json "PolicyArn" PolicyArn.of_json in
      let tags = field_map json "Tags" TagMap.of_json in
      let policyDetails =
        field_map json "PolicyDetails" PolicyDetails.of_json in
      let dateModified = field_map json "DateModified" Timestamp.of_json in
      let dateCreated = field_map json "DateCreated" Timestamp.of_json in
      let executionRoleArn =
        field_map json "ExecutionRoleArn" ExecutionRoleArn.of_json in
      let statusMessage =
        field_map json "StatusMessage" StatusMessage.of_json in
      let state = field_map json "State" GettablePolicyStateValues.of_json in
      let description =
        field_map json "Description" PolicyDescription.of_json in
      let policyId = field_map json "PolicyId" PolicyId.of_json in
      make ?policyArn ?tags ?policyDetails ?dateModified ?dateCreated
        ?executionRoleArn ?statusMessage ?state ?description ?policyId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Detailed information about a lifecycle policy."]
module LifecyclePolicySummaryList =
  struct
    type nonrec t = LifecyclePolicySummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:LifecyclePolicySummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LifecyclePolicySummary.of_xml)
    let of_json j =
      list_of_json ~kind:"LifecyclePolicySummaryList"
        ~of_json:LifecyclePolicySummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module TagsToAddFilterList =
  struct
    type nonrec t = TagFilter.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:TagFilter.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"TagsToAddFilterList" ~of_json:TagFilter.of_json j
    let to_json v = composed_to_json to_value v
  end
module TargetTagsFilterList =
  struct
    type nonrec t = TagFilter.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:50) >>= (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:TagFilter.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagFilter.of_xml)
    let of_json j =
      list_of_json ~kind:"TargetTagsFilterList" ~of_json:TagFilter.of_json j
    let to_json v = composed_to_json to_value v
  end
module UpdateLifecyclePolicyResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates the specified lifecycle policy."]
module UpdateLifecyclePolicyRequest =
  struct
    type nonrec t =
      {
      policyId: PolicyId.t
        [@ocaml.doc "The identifier of the lifecycle policy."];
      executionRoleArn: ExecutionRoleArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the IAM role used to run the operations specified by the lifecycle policy."];
      state: SettablePolicyStateValues.t option
        [@ocaml.doc
          "The desired activation state of the lifecycle policy after creation."];
      description: PolicyDescription.t option
        [@ocaml.doc "A description of the lifecycle policy."];
      policyDetails: PolicyDetails.t option
        [@ocaml.doc
          "The configuration of the lifecycle policy. You cannot update the policy type or the resource type."]}
    let context_ = "UpdateLifecyclePolicyRequest"
    let make ?executionRoleArn =
      fun ?state ->
        fun ?description ->
          fun ?policyDetails ->
            fun ~policyId ->
              fun () ->
                {
                  executionRoleArn;
                  state;
                  description;
                  policyDetails;
                  policyId
                }
    let to_value x =
      structure_to_value
        [("policyId", (Some (PolicyId.to_value x.policyId)));
        ("ExecutionRoleArn",
          (Option.map x.executionRoleArn ~f:ExecutionRoleArn.to_value));
        ("State", (Option.map x.state ~f:SettablePolicyStateValues.to_value));
        ("Description",
          (Option.map x.description ~f:PolicyDescription.to_value));
        ("PolicyDetails",
          (Option.map x.policyDetails ~f:PolicyDetails.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyDetails =
        (Option.map ~f:PolicyDetails.of_xml)
          (Xml.child xml_arg0 "PolicyDetails") in
      let description =
        (Option.map ~f:PolicyDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let state =
        (Option.map ~f:SettablePolicyStateValues.of_xml)
          (Xml.child xml_arg0 "State") in
      let executionRoleArn =
        (Option.map ~f:ExecutionRoleArn.of_xml)
          (Xml.child xml_arg0 "ExecutionRoleArn") in
      let policyId =
        PolicyId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "policyId") in
      make ?policyDetails ?description ?state ?executionRoleArn ~policyId ()
    let of_json json =
      let policyDetails =
        field_map json "PolicyDetails" PolicyDetails.of_json in
      let description =
        field_map json "Description" PolicyDescription.of_json in
      let state = field_map json "State" SettablePolicyStateValues.of_json in
      let executionRoleArn =
        field_map json "ExecutionRoleArn" ExecutionRoleArn.of_json in
      let policyId = field_map_exn json "PolicyId" PolicyId.of_json in
      make ?policyDetails ?description ?state ?executionRoleArn ~policyId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Updates the specified lifecycle policy."]
module UntagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Removes the specified tags from the specified resource."]
module UntagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: PolicyArn.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the resource."];
      tagKeys: TagKeyList.t [@ocaml.doc "The tag keys."]}
    let context_ = "UntagResourceRequest"
    let make ~resourceArn =
      fun ~tagKeys -> fun () -> { resourceArn; tagKeys }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (PolicyArn.to_value x.resourceArn)));
        ("tagKeys", (Some (TagKeyList.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "tagKeys") in
      let resourceArn =
        PolicyArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tagKeys ~resourceArn ()
    let of_json json =
      let tagKeys = field_map_exn json "TagKeys" TagKeyList.of_json in
      let resourceArn = field_map_exn json "ResourceArn" PolicyArn.of_json in
      make ~tagKeys ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Removes the specified tags from the specified resource."]
module TagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Adds the specified tags to the specified resource."]
module TagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: PolicyArn.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the resource."];
      tags: TagMap.t [@ocaml.doc "One or more tags."]}
    let context_ = "TagResourceRequest"
    let make ~resourceArn = fun ~tags -> fun () -> { resourceArn; tags }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (PolicyArn.to_value x.resourceArn)));
        ("Tags", (Some (TagMap.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagMap.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Tags") in
      let resourceArn =
        PolicyArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~tags ~resourceArn ()
    let of_json json =
      let tags = field_map_exn json "Tags" TagMap.of_json in
      let resourceArn = field_map_exn json "ResourceArn" PolicyArn.of_json in
      make ~tags ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Adds the specified tags to the specified resource."]
module ListTagsForResourceResponse =
  struct
    type nonrec t =
      {
      tags: TagMap.t option [@ocaml.doc "Information about the tags."]}
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("Tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "Tags") in
      make ?tags ()
    let of_json json =
      let tags = field_map json "Tags" TagMap.of_json in make ?tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists the tags for the specified resource."]
module ListTagsForResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: PolicyArn.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the resource."]}
    let context_ = "ListTagsForResourceRequest"
    let make ~resourceArn = fun () -> { resourceArn }
    let to_value x =
      structure_to_value
        [("resourceArn", (Some (PolicyArn.to_value x.resourceArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        PolicyArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "resourceArn") in
      make ~resourceArn ()
    let of_json json =
      let resourceArn = field_map_exn json "ResourceArn" PolicyArn.of_json in
      make ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists the tags for the specified resource."]
module GetLifecyclePolicyResponse =
  struct
    type nonrec t =
      {
      policy: LifecyclePolicy.t option
        [@ocaml.doc "Detailed information about the lifecycle policy."]}
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?policy = fun () -> { policy }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Policy", (Option.map x.policy ~f:LifecyclePolicy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policy =
        (Option.map ~f:LifecyclePolicy.of_xml) (Xml.child xml_arg0 "Policy") in
      make ?policy ()
    let of_json json =
      let policy = field_map json "Policy" LifecyclePolicy.of_json in
      make ?policy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets detailed information about the specified lifecycle policy."]
module GetLifecyclePolicyRequest =
  struct
    type nonrec t =
      {
      policyId: PolicyId.t
        [@ocaml.doc "The identifier of the lifecycle policy."]}
    let context_ = "GetLifecyclePolicyRequest"
    let make ~policyId = fun () -> { policyId }
    let to_value x =
      structure_to_value
        [("policyId", (Some (PolicyId.to_value x.policyId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyId =
        PolicyId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "policyId") in
      make ~policyId ()
    let of_json json =
      let policyId = field_map_exn json "PolicyId" PolicyId.of_json in
      make ~policyId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets detailed information about the specified lifecycle policy."]
module GetLifecyclePoliciesResponse =
  struct
    type nonrec t =
      {
      policies: LifecyclePolicySummaryList.t option
        [@ocaml.doc "Summary information about the lifecycle policies."]}
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?policies = fun () -> { policies }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Policies",
           (Option.map x.policies ~f:LifecyclePolicySummaryList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policies =
        (Option.map ~f:LifecyclePolicySummaryList.of_xml)
          (Xml.child xml_arg0 "Policies") in
      make ?policies ()
    let of_json json =
      let policies =
        field_map json "Policies" LifecyclePolicySummaryList.of_json in
      make ?policies ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets summary information about all or the specified data lifecycle policies. To get complete information about a policy, use GetLifecyclePolicy."]
module GetLifecyclePoliciesRequest =
  struct
    type nonrec t =
      {
      policyIds: PolicyIdList.t option
        [@ocaml.doc "The identifiers of the data lifecycle policies."];
      state: GettablePolicyStateValues.t option
        [@ocaml.doc "The activation state."];
      resourceTypes: ResourceTypeValuesList.t option
        [@ocaml.doc "The resource type."];
      targetTags: TargetTagsFilterList.t option
        [@ocaml.doc
          "The target tag for a policy. Tags are strings in the format key=value."];
      tagsToAdd: TagsToAddFilterList.t option
        [@ocaml.doc
          "The tags to add to objects created by the policy. Tags are strings in the format key=value. These user-defined tags are added in addition to the Amazon Web Services-added lifecycle tags."]}
    let make ?policyIds =
      fun ?state ->
        fun ?resourceTypes ->
          fun ?targetTags ->
            fun ?tagsToAdd ->
              fun () ->
                { policyIds; state; resourceTypes; targetTags; tagsToAdd }
    let to_value x =
      structure_to_value
        [("policyIds", (Option.map x.policyIds ~f:PolicyIdList.to_value));
        ("state", (Option.map x.state ~f:GettablePolicyStateValues.to_value));
        ("resourceTypes",
          (Option.map x.resourceTypes ~f:ResourceTypeValuesList.to_value));
        ("targetTags",
          (Option.map x.targetTags ~f:TargetTagsFilterList.to_value));
        ("tagsToAdd",
          (Option.map x.tagsToAdd ~f:TagsToAddFilterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagsToAdd =
        (Option.map ~f:TagsToAddFilterList.of_xml)
          (Xml.child xml_arg0 "tagsToAdd") in
      let targetTags =
        (Option.map ~f:TargetTagsFilterList.of_xml)
          (Xml.child xml_arg0 "targetTags") in
      let resourceTypes =
        (Option.map ~f:ResourceTypeValuesList.of_xml)
          (Xml.child xml_arg0 "resourceTypes") in
      let state =
        (Option.map ~f:GettablePolicyStateValues.of_xml)
          (Xml.child xml_arg0 "state") in
      let policyIds =
        (Option.map ~f:PolicyIdList.of_xml) (Xml.child xml_arg0 "policyIds") in
      make ?tagsToAdd ?targetTags ?resourceTypes ?state ?policyIds ()
    let of_json json =
      let tagsToAdd = field_map json "TagsToAdd" TagsToAddFilterList.of_json in
      let targetTags =
        field_map json "TargetTags" TargetTagsFilterList.of_json in
      let resourceTypes =
        field_map json "ResourceTypes" ResourceTypeValuesList.of_json in
      let state = field_map json "State" GettablePolicyStateValues.of_json in
      let policyIds = field_map json "PolicyIds" PolicyIdList.of_json in
      make ?tagsToAdd ?targetTags ?resourceTypes ?state ?policyIds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets summary information about all or the specified data lifecycle policies. To get complete information about a policy, use GetLifecyclePolicy."]
module DeleteLifecyclePolicyResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified lifecycle policy and halts the automated operations that the policy specified."]
module DeleteLifecyclePolicyRequest =
  struct
    type nonrec t =
      {
      policyId: PolicyId.t
        [@ocaml.doc "The identifier of the lifecycle policy."]}
    let context_ = "DeleteLifecyclePolicyRequest"
    let make ~policyId = fun () -> { policyId }
    let to_value x =
      structure_to_value
        [("policyId", (Some (PolicyId.to_value x.policyId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyId =
        PolicyId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "policyId") in
      make ~policyId ()
    let of_json json =
      let policyId = field_map_exn json "PolicyId" PolicyId.of_json in
      make ~policyId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified lifecycle policy and halts the automated operations that the policy specified."]
module CreateLifecyclePolicyResponse =
  struct
    type nonrec t =
      {
      policyId: PolicyId.t option
        [@ocaml.doc "The identifier of the lifecycle policy."]}
    type nonrec error =
      [ `InternalServerException of InternalServerException.t 
      | `InvalidRequestException of InvalidRequestException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?policyId = fun () -> { policyId }
    let error_of_json name json =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "InvalidRequestException" ->
          `InvalidRequestException (InvalidRequestException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `InvalidRequestException e ->
          `Assoc
            [("error", (`String "InvalidRequestException"));
            ("details", (InvalidRequestException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("PolicyId", (Option.map x.policyId ~f:PolicyId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policyId =
        (Option.map ~f:PolicyId.of_xml) (Xml.child xml_arg0 "PolicyId") in
      make ?policyId ()
    let of_json json =
      let policyId = field_map json "PolicyId" PolicyId.of_json in
      make ?policyId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a policy to manage the lifecycle of the specified Amazon Web Services resources. You can create up to 100 lifecycle policies."]
module CreateLifecyclePolicyRequest =
  struct
    type nonrec t =
      {
      executionRoleArn: ExecutionRoleArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the IAM role used to run the operations specified by the lifecycle policy."];
      description: PolicyDescription.t
        [@ocaml.doc
          "A description of the lifecycle policy. The characters ^[0-9A-Za-z _-]+$ are supported."];
      state: SettablePolicyStateValues.t
        [@ocaml.doc
          "The desired activation state of the lifecycle policy after creation."];
      policyDetails: PolicyDetails.t
        [@ocaml.doc "The configuration details of the lifecycle policy."];
      tags: TagMap.t option
        [@ocaml.doc
          "The tags to apply to the lifecycle policy during creation."]}
    let context_ = "CreateLifecyclePolicyRequest"
    let make ?tags =
      fun ~executionRoleArn ->
        fun ~description ->
          fun ~state ->
            fun ~policyDetails ->
              fun () ->
                { tags; executionRoleArn; description; state; policyDetails }
    let to_value x =
      structure_to_value
        [("ExecutionRoleArn",
           (Some (ExecutionRoleArn.to_value x.executionRoleArn)));
        ("Description", (Some (PolicyDescription.to_value x.description)));
        ("State", (Some (SettablePolicyStateValues.to_value x.state)));
        ("PolicyDetails", (Some (PolicyDetails.to_value x.policyDetails)));
        ("Tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "Tags") in
      let policyDetails =
        PolicyDetails.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PolicyDetails") in
      let state =
        SettablePolicyStateValues.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "State") in
      let description =
        PolicyDescription.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Description") in
      let executionRoleArn =
        ExecutionRoleArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ExecutionRoleArn") in
      make ?tags ~policyDetails ~state ~description ~executionRoleArn ()
    let of_json json =
      let tags = field_map json "Tags" TagMap.of_json in
      let policyDetails =
        field_map_exn json "PolicyDetails" PolicyDetails.of_json in
      let state =
        field_map_exn json "State" SettablePolicyStateValues.of_json in
      let description =
        field_map_exn json "Description" PolicyDescription.of_json in
      let executionRoleArn =
        field_map_exn json "ExecutionRoleArn" ExecutionRoleArn.of_json in
      make ?tags ~policyDetails ~state ~description ~executionRoleArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a policy to manage the lifecycle of the specified Amazon Web Services resources. You can create up to 100 lifecycle policies."]
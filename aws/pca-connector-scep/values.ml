(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.pca_connector_scep
let apiVersion = "2018-05-10"
let endpointPrefix = "pca-connector-scep"
let serviceFullName = "Private CA Connector for SCEP"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module AzureApplicationId =
  struct
    type nonrec t = string
    let context_ = "AzureApplicationId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:15) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"[a-zA-Z0-9]{2,15}-[a-zA-Z0-9]{2,15}-[a-zA-Z0-9]{2,15}-[a-zA-Z0-9]{2,15}-[a-zA-Z0-9]{2,15}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AzureApplicationId" j
    let to_json = simple_to_json to_value
  end
module AzureDomain =
  struct
    type nonrec t = string
    let context_ = "AzureDomain"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9._-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AzureDomain" j
    let to_json = simple_to_json to_value
  end
module IntuneConfiguration =
  struct
    type nonrec t =
      {
      azureApplicationId: AzureApplicationId.t
        [@ocaml.doc
          "The directory (tenant) ID from your Microsoft Entra ID app registration."];
      domain: AzureDomain.t
        [@ocaml.doc
          "The primary domain from your Microsoft Entra ID app registration."]}
    let context_ = "IntuneConfiguration"
    let make ~azureApplicationId =
      fun ~domain -> fun () -> { azureApplicationId; domain }
    let to_value x =
      structure_to_value
        [("AzureApplicationId",
           (Some (AzureApplicationId.to_value x.azureApplicationId)));
        ("Domain", (Some (AzureDomain.to_value x.domain)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let domain =
        AzureDomain.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Domain") in
      let azureApplicationId =
        AzureApplicationId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AzureApplicationId") in
      make ~domain ~azureApplicationId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let domain = field_map_exn json__ "Domain" AzureDomain.of_json in
      let azureApplicationId =
        field_map_exn json__ "AzureApplicationId" AzureApplicationId.of_json in
      make ~domain ~azureApplicationId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains configuration details for use with Microsoft Intune. For information about using Connector for SCEP for Microsoft Intune, see Using Connector for SCEP for Microsoft Intune. When you use Connector for SCEP for Microsoft Intune, certain functionalities are enabled by accessing Microsoft Intune through the Microsoft API. Your use of the Connector for SCEP and accompanying Amazon Web Services services doesn't remove your need to have a valid license for your use of the Microsoft Intune service. You should also review the Microsoft Intune\194\174 App Protection Policies."]
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module CertificateAuthorityArn =
  struct
    type nonrec t = string
    let context_ = "CertificateAuthorityArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:5) >>=
             (fun () ->
                (check_string_max i ~max:200) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:aws(-[a-z]+)*:acm-pca:[a-z]+(-[a-z]+)+-[1-9]\\d*:\\d{12}:certificate-authority\\/[0-9a-f]{8}(-[0-9a-f]{4}){3}-[0-9a-f]{12}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CertificateAuthorityArn" j
    let to_json = simple_to_json to_value
  end
module ConnectorArn =
  struct
    type nonrec t = string
    let context_ = "ConnectorArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:5) >>=
             (fun () ->
                (check_string_max i ~max:200) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:aws(-[a-z]+)*:pca-connector-scep:[a-z]+(-[a-z]+)+-[1-9]\\d*:\\d{12}:connector\\/[0-9a-f]{8}(-[0-9a-f]{4}){3}-[0-9a-f]{12}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ConnectorArn" j
    let to_json = simple_to_json to_value
  end
module ConnectorStatus =
  struct
    type nonrec t =
      | CREATING 
      | ACTIVE 
      | DELETING 
      | FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CREATING -> "CREATING"
      | ACTIVE -> "ACTIVE"
      | DELETING -> "DELETING"
      | FAILED -> "FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "CREATING" -> CREATING
      | "ACTIVE" -> ACTIVE
      | "DELETING" -> DELETING
      | "FAILED" -> FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ConnectorStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ConnectorStatus" j)
    let to_json = simple_to_json to_value
  end
module ConnectorStatusReason =
  struct
    type nonrec t =
      | INTERNAL_FAILURE 
      | PRIVATECA_ACCESS_DENIED 
      | PRIVATECA_INVALID_STATE 
      | PRIVATECA_RESOURCE_NOT_FOUND 
      | VPC_ENDPOINT_RESOURCE_NOT_FOUND 
      | VPC_ENDPOINT_DNS_ENTRIES_NOT_FOUND 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | INTERNAL_FAILURE -> "INTERNAL_FAILURE"
      | PRIVATECA_ACCESS_DENIED -> "PRIVATECA_ACCESS_DENIED"
      | PRIVATECA_INVALID_STATE -> "PRIVATECA_INVALID_STATE"
      | PRIVATECA_RESOURCE_NOT_FOUND -> "PRIVATECA_RESOURCE_NOT_FOUND"
      | VPC_ENDPOINT_RESOURCE_NOT_FOUND -> "VPC_ENDPOINT_RESOURCE_NOT_FOUND"
      | VPC_ENDPOINT_DNS_ENTRIES_NOT_FOUND ->
          "VPC_ENDPOINT_DNS_ENTRIES_NOT_FOUND"
      | Non_static_id s -> s
    let of_string =
      function
      | "INTERNAL_FAILURE" -> INTERNAL_FAILURE
      | "PRIVATECA_ACCESS_DENIED" -> PRIVATECA_ACCESS_DENIED
      | "PRIVATECA_INVALID_STATE" -> PRIVATECA_INVALID_STATE
      | "PRIVATECA_RESOURCE_NOT_FOUND" -> PRIVATECA_RESOURCE_NOT_FOUND
      | "VPC_ENDPOINT_RESOURCE_NOT_FOUND" -> VPC_ENDPOINT_RESOURCE_NOT_FOUND
      | "VPC_ENDPOINT_DNS_ENTRIES_NOT_FOUND" ->
          VPC_ENDPOINT_DNS_ENTRIES_NOT_FOUND
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ConnectorStatusReason" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ConnectorStatusReason" j)
    let to_json = simple_to_json to_value
  end
module ConnectorType =
  struct
    type nonrec t =
      | GENERAL_PURPOSE 
      | INTUNE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | GENERAL_PURPOSE -> "GENERAL_PURPOSE"
      | INTUNE -> "INTUNE"
      | Non_static_id s -> s
    let of_string =
      function
      | "GENERAL_PURPOSE" -> GENERAL_PURPOSE
      | "INTUNE" -> INTUNE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ConnectorType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ConnectorType" j)
    let to_json = simple_to_json to_value
  end
module MobileDeviceManagement =
  struct
    type nonrec t =
      {
      intune: IntuneConfiguration.t option
        [@ocaml.doc
          "Configuration settings for use with Microsoft Intune. For information about using Connector for SCEP for Microsoft Intune, see Using Connector for SCEP for Microsoft Intune."]}
    let make ?intune = fun () -> { intune }
    let to_value x =
      structure_to_value
        [("Intune", (Option.map x.intune ~f:IntuneConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let intune =
        (Option.map ~f:IntuneConfiguration.of_xml)
          (Xml.child xml_arg0 "Intune") in
      make ?intune ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let intune = field_map json__ "Intune" IntuneConfiguration.of_json in
      make ?intune ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "If you don't supply a value, by default Connector for SCEP creates a connector for general-purpose use. A general-purpose connector is designed to work with clients or endpoints that support the SCEP protocol, except Connector for SCEP for Microsoft Intune. For information about considerations and limitations with using Connector for SCEP, see Considerations and Limitations. If you provide an IntuneConfiguration, Connector for SCEP creates a connector for use with Microsoft Intune, and you manage the challenge passwords using Microsoft Intune. For more information, see Using Connector for SCEP for Microsoft Intune."]
module OpenIdConfiguration =
  struct
    type nonrec t =
      {
      issuer: String_.t option
        [@ocaml.doc
          "The issuer value to copy into your Microsoft Entra app registration's OIDC."];
      subject: String_.t option
        [@ocaml.doc
          "The subject value to copy into your Microsoft Entra app registration's OIDC."];
      audience: String_.t option
        [@ocaml.doc
          "The audience value to copy into your Microsoft Entra app registration's OIDC."]}
    let make ?issuer =
      fun ?subject ->
        fun ?audience -> fun () -> { issuer; subject; audience }
    let to_value x =
      structure_to_value
        [("Issuer", (Option.map x.issuer ~f:String_.to_value));
        ("Subject", (Option.map x.subject ~f:String_.to_value));
        ("Audience", (Option.map x.audience ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let audience =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Audience") in
      let subject =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Subject") in
      let issuer =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Issuer") in
      make ?audience ?subject ?issuer ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let audience = field_map json__ "Audience" String_.of_json in
      let subject = field_map json__ "Subject" String_.of_json in
      let issuer = field_map json__ "Issuer" String_.of_json in
      make ?audience ?subject ?issuer ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains OpenID Connect (OIDC) parameters for use with Microsoft Intune. For more information about using Connector for SCEP for Microsoft Intune, see Using Connector for SCEP for Microsoft Intune."]
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module ChallengeArn =
  struct
    type nonrec t = string
    let context_ = "ChallengeArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:5) >>=
             (fun () ->
                (check_string_max i ~max:200) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:aws(-[a-z]+)*:pca-connector-scep:[a-z]+(-[a-z]+)+-[1-9]\\d*:\\d{12}:connector\\/[0-9a-f]{8}(-[0-9a-f]{4}){3}-[0-9a-f]{12}\\/challenge\\/[0-9a-f]{8}(-[0-9a-f]{4}){3}-[0-9a-f]{12}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ChallengeArn" j
    let to_json = simple_to_json to_value
  end
module ValidationExceptionReason =
  struct
    type nonrec t =
      | CA_CERT_VALIDITY_TOO_SHORT 
      | INVALID_CA_USAGE_MODE 
      | INVALID_CONNECTOR_TYPE 
      | INVALID_STATE 
      | NO_CLIENT_TOKEN 
      | UNKNOWN_OPERATION 
      | OTHER 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CA_CERT_VALIDITY_TOO_SHORT -> "CA_CERT_VALIDITY_TOO_SHORT"
      | INVALID_CA_USAGE_MODE -> "INVALID_CA_USAGE_MODE"
      | INVALID_CONNECTOR_TYPE -> "INVALID_CONNECTOR_TYPE"
      | INVALID_STATE -> "INVALID_STATE"
      | NO_CLIENT_TOKEN -> "NO_CLIENT_TOKEN"
      | UNKNOWN_OPERATION -> "UNKNOWN_OPERATION"
      | OTHER -> "OTHER"
      | Non_static_id s -> s
    let of_string =
      function
      | "CA_CERT_VALIDITY_TOO_SHORT" -> CA_CERT_VALIDITY_TOO_SHORT
      | "INVALID_CA_USAGE_MODE" -> INVALID_CA_USAGE_MODE
      | "INVALID_CONNECTOR_TYPE" -> INVALID_CONNECTOR_TYPE
      | "INVALID_STATE" -> INVALID_STATE
      | "NO_CLIENT_TOKEN" -> NO_CLIENT_TOKEN
      | "UNKNOWN_OPERATION" -> UNKNOWN_OPERATION
      | "OTHER" -> OTHER
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ValidationExceptionReason" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ValidationExceptionReason" j)
    let to_json = simple_to_json to_value
  end
module ConnectorSummary =
  struct
    type nonrec t =
      {
      arn: ConnectorArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the connector."];
      certificateAuthorityArn: CertificateAuthorityArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the connector's associated certificate authority."];
      type_: ConnectorType.t option [@ocaml.doc "The connector type."];
      mobileDeviceManagement: MobileDeviceManagement.t option
        [@ocaml.doc
          "Contains settings relevant to the mobile device management system that you chose for the connector. If you didn't configure MobileDeviceManagement, then the connector is for general-purpose use and this object is empty."];
      openIdConfiguration: OpenIdConfiguration.t option
        [@ocaml.doc
          "Contains OpenID Connect (OIDC) parameters for use with Microsoft Intune."];
      status: ConnectorStatus.t option
        [@ocaml.doc
          "The connector's status. Status can be creating, active, deleting, or failed."];
      statusReason: ConnectorStatusReason.t option
        [@ocaml.doc
          "Information about why connector creation failed, if status is FAILED."];
      endpoint: String_.t option
        [@ocaml.doc "The connector's HTTPS public SCEP URL."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The date and time that the challenge was created."];
      updatedAt: Timestamp.t option
        [@ocaml.doc "The date and time that the challenge was updated."]}
    let make ?arn =
      fun ?certificateAuthorityArn ->
        fun ?type_ ->
          fun ?mobileDeviceManagement ->
            fun ?openIdConfiguration ->
              fun ?status ->
                fun ?statusReason ->
                  fun ?endpoint ->
                    fun ?createdAt ->
                      fun ?updatedAt ->
                        fun () ->
                          {
                            arn;
                            certificateAuthorityArn;
                            type_;
                            mobileDeviceManagement;
                            openIdConfiguration;
                            status;
                            statusReason;
                            endpoint;
                            createdAt;
                            updatedAt
                          }
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:ConnectorArn.to_value));
        ("CertificateAuthorityArn",
          (Option.map x.certificateAuthorityArn
             ~f:CertificateAuthorityArn.to_value));
        ("Type", (Option.map x.type_ ~f:ConnectorType.to_value));
        ("MobileDeviceManagement",
          (Option.map x.mobileDeviceManagement
             ~f:MobileDeviceManagement.to_value));
        ("OpenIdConfiguration",
          (Option.map x.openIdConfiguration ~f:OpenIdConfiguration.to_value));
        ("Status", (Option.map x.status ~f:ConnectorStatus.to_value));
        ("StatusReason",
          (Option.map x.statusReason ~f:ConnectorStatusReason.to_value));
        ("Endpoint", (Option.map x.endpoint ~f:String_.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("UpdatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "UpdatedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let endpoint =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Endpoint") in
      let statusReason =
        (Option.map ~f:ConnectorStatusReason.of_xml)
          (Xml.child xml_arg0 "StatusReason") in
      let status =
        (Option.map ~f:ConnectorStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let openIdConfiguration =
        (Option.map ~f:OpenIdConfiguration.of_xml)
          (Xml.child xml_arg0 "OpenIdConfiguration") in
      let mobileDeviceManagement =
        (Option.map ~f:MobileDeviceManagement.of_xml)
          (Xml.child xml_arg0 "MobileDeviceManagement") in
      let type_ =
        (Option.map ~f:ConnectorType.of_xml) (Xml.child xml_arg0 "Type") in
      let certificateAuthorityArn =
        (Option.map ~f:CertificateAuthorityArn.of_xml)
          (Xml.child xml_arg0 "CertificateAuthorityArn") in
      let arn =
        (Option.map ~f:ConnectorArn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?updatedAt ?createdAt ?endpoint ?statusReason ?status
        ?openIdConfiguration ?mobileDeviceManagement ?type_
        ?certificateAuthorityArn ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt = field_map json__ "UpdatedAt" Timestamp.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let endpoint = field_map json__ "Endpoint" String_.of_json in
      let statusReason =
        field_map json__ "StatusReason" ConnectorStatusReason.of_json in
      let status = field_map json__ "Status" ConnectorStatus.of_json in
      let openIdConfiguration =
        field_map json__ "OpenIdConfiguration" OpenIdConfiguration.of_json in
      let mobileDeviceManagement =
        field_map json__ "MobileDeviceManagement"
          MobileDeviceManagement.of_json in
      let type_ = field_map json__ "Type" ConnectorType.of_json in
      let certificateAuthorityArn =
        field_map json__ "CertificateAuthorityArn"
          CertificateAuthorityArn.of_json in
      let arn = field_map json__ "Arn" ConnectorArn.of_json in
      make ?updatedAt ?createdAt ?endpoint ?statusReason ?status
        ?openIdConfiguration ?mobileDeviceManagement ?type_
        ?certificateAuthorityArn ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the Amazon Web Services Private CA SCEP connectors belonging to your Amazon Web Services account."]
module ChallengeMetadataSummary =
  struct
    type nonrec t =
      {
      arn: ChallengeArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the challenge."];
      connectorArn: ConnectorArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the connector."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The date and time that the challenge was created."];
      updatedAt: Timestamp.t option
        [@ocaml.doc "The date and time that the challenge was updated."]}
    let make ?arn =
      fun ?connectorArn ->
        fun ?createdAt ->
          fun ?updatedAt ->
            fun () -> { arn; connectorArn; createdAt; updatedAt }
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:ChallengeArn.to_value));
        ("ConnectorArn",
          (Option.map x.connectorArn ~f:ConnectorArn.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("UpdatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "UpdatedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let connectorArn =
        (Option.map ~f:ConnectorArn.of_xml)
          (Xml.child xml_arg0 "ConnectorArn") in
      let arn =
        (Option.map ~f:ChallengeArn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?updatedAt ?createdAt ?connectorArn ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt = field_map json__ "UpdatedAt" Timestamp.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let connectorArn = field_map json__ "ConnectorArn" ConnectorArn.of_json in
      let arn = field_map json__ "Arn" ChallengeArn.of_json in
      make ?updatedAt ?createdAt ?connectorArn ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Details about the specified challenge, returned by the GetChallengeMetadata action."]
module SensitiveString =
  struct
    type nonrec t = string
    let context_ = "SensitiveString"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SensitiveString" j
    let to_json = simple_to_json to_value
  end
module TagKeyList =
  struct
    type nonrec t = String_.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"TagKeyList" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module Tags =
  struct
    type nonrec t = (String_.t * String_.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((String_.of_string chopped),
                              (String_.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x -> (String_.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module AccessDeniedException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You can receive this error if you attempt to perform an operation and you don't have the required permissions. This can be caused by insufficient permissions in policies attached to your Amazon Web Services Identity and Access Management (IAM) principal. It can also happen because of restrictions in place from an Amazon Web Services Organizations service control policy (SCP) that affects your Amazon Web Services account."]
module InternalServerException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request processing has failed because of an unknown error, exception or failure with an internal server."]
module ResourceNotFoundException =
  struct
    type nonrec t =
      {
      message: String_.t option ;
      resourceId: String_.t option
        [@ocaml.doc "The identifier of the Amazon Web Services resource."];
      resourceType: String_.t option
        [@ocaml.doc
          "The resource type, which can be either Connector or Challenge."]}
    let make ?message =
      fun ?resourceId ->
        fun ?resourceType -> fun () -> { message; resourceId; resourceType }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value));
        ("ResourceId", (Option.map x.resourceId ~f:String_.to_value));
        ("ResourceType", (Option.map x.resourceType ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ResourceType") in
      let resourceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ResourceId") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?resourceType ?resourceId ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceType = field_map json__ "ResourceType" String_.of_json in
      let resourceId = field_map json__ "ResourceId" String_.of_json in
      let message = field_map json__ "Message" String_.of_json in
      make ?resourceType ?resourceId ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The operation tried to access a nonexistent resource. The resource might be incorrectly specified, or it might have a status other than ACTIVE."]
module ThrottlingException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The limit on the number of requests per second was exceeded."]
module ValidationException =
  struct
    type nonrec t =
      {
      message: String_.t option ;
      reason: ValidationExceptionReason.t option
        [@ocaml.doc
          "The reason for the validation error, if available. The service doesn't return a reason for every validation exception."]}
    let make ?message = fun ?reason -> fun () -> { message; reason }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value));
        ("Reason",
          (Option.map x.reason ~f:ValidationExceptionReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reason =
        (Option.map ~f:ValidationExceptionReason.of_xml)
          (Xml.child xml_arg0 "Reason") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?reason ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reason =
        field_map json__ "Reason" ValidationExceptionReason.of_json in
      let message = field_map json__ "Message" String_.of_json in
      make ?reason ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An input validation error occurred. For example, invalid characters in a name tag, or an invalid pagination token."]
module ConnectorList =
  struct
    type nonrec t = ConnectorSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ConnectorSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ConnectorSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"ConnectorList" ~of_json:ConnectorSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module NextToken =
  struct
    type nonrec t = string
    let context_ = "NextToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1000) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"(?:[A-Za-z0-9_-]{4})*(?:[A-Za-z0-9_-]{2}==|[A-Za-z0-9_-]{3}=)?")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NextToken" j
    let to_json = simple_to_json to_value
  end
module MaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1000) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ChallengeMetadataList =
  struct
    type nonrec t = ChallengeMetadataSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ChallengeMetadataSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ChallengeMetadataSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"ChallengeMetadataList"
        ~of_json:ChallengeMetadataSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module Connector =
  struct
    type nonrec t =
      {
      arn: ConnectorArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the connector."];
      certificateAuthorityArn: CertificateAuthorityArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the certificate authority associated with the connector."];
      type_: ConnectorType.t option [@ocaml.doc "The connector type."];
      mobileDeviceManagement: MobileDeviceManagement.t option
        [@ocaml.doc
          "Contains settings relevant to the mobile device management system that you chose for the connector. If you didn't configure MobileDeviceManagement, then the connector is for general-purpose use and this object is empty."];
      openIdConfiguration: OpenIdConfiguration.t option
        [@ocaml.doc
          "Contains OpenID Connect (OIDC) parameters for use with Connector for SCEP for Microsoft Intune. For more information about using Connector for SCEP for Microsoft Intune, see Using Connector for SCEP for Microsoft Intune."];
      status: ConnectorStatus.t option [@ocaml.doc "The connector's status."];
      statusReason: ConnectorStatusReason.t option
        [@ocaml.doc
          "Information about why connector creation failed, if status is FAILED."];
      endpoint: String_.t option
        [@ocaml.doc "The connector's HTTPS public SCEP URL."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The date and time that the connector was created."];
      updatedAt: Timestamp.t option
        [@ocaml.doc "The date and time that the connector was updated."]}
    let make ?arn =
      fun ?certificateAuthorityArn ->
        fun ?type_ ->
          fun ?mobileDeviceManagement ->
            fun ?openIdConfiguration ->
              fun ?status ->
                fun ?statusReason ->
                  fun ?endpoint ->
                    fun ?createdAt ->
                      fun ?updatedAt ->
                        fun () ->
                          {
                            arn;
                            certificateAuthorityArn;
                            type_;
                            mobileDeviceManagement;
                            openIdConfiguration;
                            status;
                            statusReason;
                            endpoint;
                            createdAt;
                            updatedAt
                          }
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:ConnectorArn.to_value));
        ("CertificateAuthorityArn",
          (Option.map x.certificateAuthorityArn
             ~f:CertificateAuthorityArn.to_value));
        ("Type", (Option.map x.type_ ~f:ConnectorType.to_value));
        ("MobileDeviceManagement",
          (Option.map x.mobileDeviceManagement
             ~f:MobileDeviceManagement.to_value));
        ("OpenIdConfiguration",
          (Option.map x.openIdConfiguration ~f:OpenIdConfiguration.to_value));
        ("Status", (Option.map x.status ~f:ConnectorStatus.to_value));
        ("StatusReason",
          (Option.map x.statusReason ~f:ConnectorStatusReason.to_value));
        ("Endpoint", (Option.map x.endpoint ~f:String_.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("UpdatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "UpdatedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let endpoint =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Endpoint") in
      let statusReason =
        (Option.map ~f:ConnectorStatusReason.of_xml)
          (Xml.child xml_arg0 "StatusReason") in
      let status =
        (Option.map ~f:ConnectorStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let openIdConfiguration =
        (Option.map ~f:OpenIdConfiguration.of_xml)
          (Xml.child xml_arg0 "OpenIdConfiguration") in
      let mobileDeviceManagement =
        (Option.map ~f:MobileDeviceManagement.of_xml)
          (Xml.child xml_arg0 "MobileDeviceManagement") in
      let type_ =
        (Option.map ~f:ConnectorType.of_xml) (Xml.child xml_arg0 "Type") in
      let certificateAuthorityArn =
        (Option.map ~f:CertificateAuthorityArn.of_xml)
          (Xml.child xml_arg0 "CertificateAuthorityArn") in
      let arn =
        (Option.map ~f:ConnectorArn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?updatedAt ?createdAt ?endpoint ?statusReason ?status
        ?openIdConfiguration ?mobileDeviceManagement ?type_
        ?certificateAuthorityArn ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt = field_map json__ "UpdatedAt" Timestamp.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let endpoint = field_map json__ "Endpoint" String_.of_json in
      let statusReason =
        field_map json__ "StatusReason" ConnectorStatusReason.of_json in
      let status = field_map json__ "Status" ConnectorStatus.of_json in
      let openIdConfiguration =
        field_map json__ "OpenIdConfiguration" OpenIdConfiguration.of_json in
      let mobileDeviceManagement =
        field_map json__ "MobileDeviceManagement"
          MobileDeviceManagement.of_json in
      let type_ = field_map json__ "Type" ConnectorType.of_json in
      let certificateAuthorityArn =
        field_map json__ "CertificateAuthorityArn"
          CertificateAuthorityArn.of_json in
      let arn = field_map json__ "Arn" ConnectorArn.of_json in
      make ?updatedAt ?createdAt ?endpoint ?statusReason ?status
        ?openIdConfiguration ?mobileDeviceManagement ?type_
        ?certificateAuthorityArn ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Connector for SCEP is a service that links Amazon Web Services Private Certificate Authority to your SCEP-enabled devices. The connector brokers the exchange of certificates from Amazon Web Services Private CA to your SCEP-enabled devices and mobile device management systems. The connector is a complex type that contains the connector's configuration settings."]
module ChallengeMetadata =
  struct
    type nonrec t =
      {
      arn: ChallengeArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the challenge."];
      connectorArn: ConnectorArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the connector."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The date and time that the connector was created."];
      updatedAt: Timestamp.t option
        [@ocaml.doc "The date and time that the connector was updated."]}
    let make ?arn =
      fun ?connectorArn ->
        fun ?createdAt ->
          fun ?updatedAt ->
            fun () -> { arn; connectorArn; createdAt; updatedAt }
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:ChallengeArn.to_value));
        ("ConnectorArn",
          (Option.map x.connectorArn ~f:ConnectorArn.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("UpdatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "UpdatedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let connectorArn =
        (Option.map ~f:ConnectorArn.of_xml)
          (Xml.child xml_arg0 "ConnectorArn") in
      let arn =
        (Option.map ~f:ChallengeArn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?updatedAt ?createdAt ?connectorArn ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt = field_map json__ "UpdatedAt" Timestamp.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let connectorArn = field_map json__ "ConnectorArn" ConnectorArn.of_json in
      let arn = field_map json__ "Arn" ChallengeArn.of_json in
      make ?updatedAt ?createdAt ?connectorArn ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Contains details about the connector's challenge."]
module ConflictException =
  struct
    type nonrec t =
      {
      message: String_.t option ;
      resourceId: String_.t option
        [@ocaml.doc "The identifier of the Amazon Web Services resource."];
      resourceType: String_.t option
        [@ocaml.doc
          "The resource type, which can be either Connector or Challenge."]}
    let make ?message =
      fun ?resourceId ->
        fun ?resourceType -> fun () -> { message; resourceId; resourceType }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value));
        ("ResourceId", (Option.map x.resourceId ~f:String_.to_value));
        ("ResourceType", (Option.map x.resourceType ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ResourceType") in
      let resourceId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ResourceId") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?resourceType ?resourceId ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceType = field_map json__ "ResourceType" String_.of_json in
      let resourceId = field_map json__ "ResourceId" String_.of_json in
      let message = field_map json__ "Message" String_.of_json in
      make ?resourceType ?resourceId ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This request can't be completed for one of the following reasons because the requested resource was being concurrently modified by another request."]
module ServiceQuotaExceededException =
  struct
    type nonrec t =
      {
      message: String_.t option ;
      resourceType: String_.t option
        [@ocaml.doc
          "The resource type, which can be either Connector or Challenge."];
      serviceCode: String_.t option
        [@ocaml.doc "Identifies the originating service."];
      quotaCode: String_.t option [@ocaml.doc "The quota identifier."]}
    let make ?message =
      fun ?resourceType ->
        fun ?serviceCode ->
          fun ?quotaCode ->
            fun () -> { message; resourceType; serviceCode; quotaCode }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value));
        ("ResourceType", (Option.map x.resourceType ~f:String_.to_value));
        ("ServiceCode", (Option.map x.serviceCode ~f:String_.to_value));
        ("QuotaCode", (Option.map x.quotaCode ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let quotaCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "QuotaCode") in
      let serviceCode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ServiceCode") in
      let resourceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ResourceType") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?quotaCode ?serviceCode ?resourceType ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let quotaCode = field_map json__ "QuotaCode" String_.of_json in
      let serviceCode = field_map json__ "ServiceCode" String_.of_json in
      let resourceType = field_map json__ "ResourceType" String_.of_json in
      let message = field_map json__ "Message" String_.of_json in
      make ?quotaCode ?serviceCode ?resourceType ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The request would cause a service quota to be exceeded."]
module ClientToken =
  struct
    type nonrec t = string
    let context_ = "ClientToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"[!-~]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ClientToken" j
    let to_json = simple_to_json to_value
  end
module VpcEndpointId =
  struct
    type nonrec t = string
    let context_ = "VpcEndpointId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:13) >>=
             (fun () ->
                (check_string_max i ~max:22) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"vpce-[0-9a-f]{8}([0-9a-f]{9})?")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"VpcEndpointId" j
    let to_json = simple_to_json to_value
  end
module BadRequestException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request is malformed or contains an error such as an invalid parameter value or a missing required parameter."]
module Challenge =
  struct
    type nonrec t =
      {
      arn: ChallengeArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the challenge."];
      connectorArn: ConnectorArn.t option
        [@ocaml.doc "The Amazon Resource Name (ARN) of the connector."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The date and time that the challenge was created."];
      updatedAt: Timestamp.t option
        [@ocaml.doc "The date and time that the challenge was updated."];
      password: SensitiveString.t option
        [@ocaml.doc "The SCEP challenge password, in UUID format."]}
    let make ?arn =
      fun ?connectorArn ->
        fun ?createdAt ->
          fun ?updatedAt ->
            fun ?password ->
              fun () -> { arn; connectorArn; createdAt; updatedAt; password }
    let to_value x =
      structure_to_value
        [("Arn", (Option.map x.arn ~f:ChallengeArn.to_value));
        ("ConnectorArn",
          (Option.map x.connectorArn ~f:ConnectorArn.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("UpdatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value));
        ("Password", (Option.map x.password ~f:SensitiveString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let password =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "Password") in
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "UpdatedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let connectorArn =
        (Option.map ~f:ConnectorArn.of_xml)
          (Xml.child xml_arg0 "ConnectorArn") in
      let arn =
        (Option.map ~f:ChallengeArn.of_xml) (Xml.child xml_arg0 "Arn") in
      make ?password ?updatedAt ?createdAt ?connectorArn ?arn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let password = field_map json__ "Password" SensitiveString.of_json in
      let updatedAt = field_map json__ "UpdatedAt" Timestamp.of_json in
      let createdAt = field_map json__ "CreatedAt" Timestamp.of_json in
      let connectorArn = field_map json__ "ConnectorArn" ConnectorArn.of_json in
      let arn = field_map json__ "Arn" ChallengeArn.of_json in
      make ?password ?updatedAt ?createdAt ?connectorArn ?arn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "For Connector for SCEP for general-purpose. An object containing information about the specified connector's SCEP challenge passwords."]
module UntagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: String_.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the resource."];
      tagKeys: TagKeyList.t
        [@ocaml.doc
          "Specifies a list of tag keys that you want to remove from the specified resources."]}
    let context_ = "UntagResourceRequest"
    let make ~resourceArn =
      fun ~tagKeys -> fun () -> { resourceArn; tagKeys }
    let to_value x =
      structure_to_value
        [("ResourceArn", (Some (String_.to_value x.resourceArn)));
        ("tagKeys", (Some (TagKeyList.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "tagKeys") in
      let resourceArn =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceArn") in
      make ~tagKeys ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tagKeys = field_map_exn json__ "TagKeys" TagKeyList.of_json in
      let resourceArn = field_map_exn json__ "ResourceArn" String_.of_json in
      make ~tagKeys ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Removes one or more tags from your resource."]
module TagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: String_.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the resource."];
      tags: Tags.t
        [@ocaml.doc "The key-value pairs to associate with the resource."]}
    let context_ = "TagResourceRequest"
    let make ~resourceArn = fun ~tags -> fun () -> { resourceArn; tags }
    let to_value x =
      structure_to_value
        [("ResourceArn", (Some (String_.to_value x.resourceArn)));
        ("Tags", (Some (Tags.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        Tags.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Tags") in
      let resourceArn =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceArn") in
      make ~tags ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map_exn json__ "Tags" Tags.of_json in
      let resourceArn = field_map_exn json__ "ResourceArn" String_.of_json in
      make ~tags ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Adds one or more tags to your resource."]
module ListTagsForResourceResponse =
  struct
    type nonrec t =
      {
      tags: Tags.t option
        [@ocaml.doc "The key-value pairs to associate with the resource."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("Tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      make ?tags ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" Tags.of_json in make ?tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the tags associated with the specified resource. Tags are key-value pairs that you can use to categorize and manage your resources, for purposes like billing. For example, you might set the tag key to \"customer\" and the value to the customer name or ID. You can specify one or more tags to add to each Amazon Web Services resource, up to 50 tags for a resource."]
module ListTagsForResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: String_.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the resource."]}
    let context_ = "ListTagsForResourceRequest"
    let make ~resourceArn = fun () -> { resourceArn }
    let to_value x =
      structure_to_value
        [("ResourceArn", (Some (String_.to_value x.resourceArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceArn") in
      make ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceArn = field_map_exn json__ "ResourceArn" String_.of_json in
      make ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the tags associated with the specified resource. Tags are key-value pairs that you can use to categorize and manage your resources, for purposes like billing. For example, you might set the tag key to \"customer\" and the value to the customer name or ID. You can specify one or more tags to add to each Amazon Web Services resource, up to 50 tags for a resource."]
module ListConnectorsResponse =
  struct
    type nonrec t =
      {
      connectors: ConnectorList.t option
        [@ocaml.doc
          "The connectors belonging to your Amazon Web Services account."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "When you request a list of objects with a MaxResults setting, if the number of objects that are still available for retrieval exceeds the maximum you requested, Connector for SCEP returns a NextToken value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?connectors =
      fun ?nextToken -> fun () -> { connectors; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Connectors", (Option.map x.connectors ~f:ConnectorList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let connectors =
        (Option.map ~f:ConnectorList.of_xml)
          (Xml.child xml_arg0 "Connectors") in
      make ?nextToken ?connectors ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let connectors = field_map json__ "Connectors" ConnectorList.of_json in
      make ?nextToken ?connectors ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the connectors belonging to your Amazon Web Services account."]
module ListConnectorsRequest =
  struct
    type nonrec t =
      {
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of objects that you want Connector for SCEP to return for this request. If more objects are available, in the response, Connector for SCEP provides a NextToken value that you can use in a subsequent call to get the next batch of objects."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "When you request a list of objects with a MaxResults setting, if the number of objects that are still available for retrieval exceeds the maximum you requested, Connector for SCEP returns a NextToken value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request."]}
    let make ?maxResults =
      fun ?nextToken -> fun () -> { maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      make ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults = field_map json__ "MaxResults" MaxResults.of_json in
      make ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the connectors belonging to your Amazon Web Services account."]
module ListChallengeMetadataResponse =
  struct
    type nonrec t =
      {
      challenges: ChallengeMetadataList.t option
        [@ocaml.doc
          "The challenge metadata for the challenges belonging to your Amazon Web Services account."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "When you request a list of objects with a MaxResults setting, if the number of objects that are still available for retrieval exceeds the maximum you requested, Connector for SCEP returns a NextToken value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?challenges =
      fun ?nextToken -> fun () -> { challenges; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Challenges",
           (Option.map x.challenges ~f:ChallengeMetadataList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let challenges =
        (Option.map ~f:ChallengeMetadataList.of_xml)
          (Xml.child xml_arg0 "Challenges") in
      make ?nextToken ?challenges ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let challenges =
        field_map json__ "Challenges" ChallengeMetadataList.of_json in
      make ?nextToken ?challenges ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves the challenge metadata for the specified ARN."]
module ListChallengeMetadataRequest =
  struct
    type nonrec t =
      {
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of objects that you want Connector for SCEP to return for this request. If more objects are available, in the response, Connector for SCEP provides a NextToken value that you can use in a subsequent call to get the next batch of objects."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "When you request a list of objects with a MaxResults setting, if the number of objects that are still available for retrieval exceeds the maximum you requested, Connector for SCEP returns a NextToken value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request."];
      connectorArn: ConnectorArn.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the connector."]}
    let context_ = "ListChallengeMetadataRequest"
    let make ?maxResults =
      fun ?nextToken ->
        fun ~connectorArn ->
          fun () -> { maxResults; nextToken; connectorArn }
    let to_value x =
      structure_to_value
        [("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("ConnectorArn", (Some (ConnectorArn.to_value x.connectorArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let connectorArn =
        ConnectorArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConnectorArn") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      make ~connectorArn ?nextToken ?maxResults ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let connectorArn =
        field_map_exn json__ "ConnectorArn" ConnectorArn.of_json in
      let nextToken = field_map json__ "NextToken" NextToken.of_json in
      let maxResults = field_map json__ "MaxResults" MaxResults.of_json in
      make ~connectorArn ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves the challenge metadata for the specified ARN."]
module GetConnectorResponse =
  struct
    type nonrec t =
      {
      connector: Connector.t option
        [@ocaml.doc "The properties of the connector."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?connector = fun () -> { connector }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Connector", (Option.map x.connector ~f:Connector.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let connector =
        (Option.map ~f:Connector.of_xml) (Xml.child xml_arg0 "Connector") in
      make ?connector ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let connector = field_map json__ "Connector" Connector.of_json in
      make ?connector ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves details about the specified Connector. Calling this action returns important details about the connector, such as the public SCEP URL where your clients can request certificates."]
module GetConnectorRequest =
  struct
    type nonrec t =
      {
      connectorArn: ConnectorArn.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the connector."]}
    let context_ = "GetConnectorRequest"
    let make ~connectorArn = fun () -> { connectorArn }
    let to_value x =
      structure_to_value
        [("ConnectorArn", (Some (ConnectorArn.to_value x.connectorArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let connectorArn =
        ConnectorArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConnectorArn") in
      make ~connectorArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let connectorArn =
        field_map_exn json__ "ConnectorArn" ConnectorArn.of_json in
      make ~connectorArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves details about the specified Connector. Calling this action returns important details about the connector, such as the public SCEP URL where your clients can request certificates."]
module GetChallengePasswordResponse =
  struct
    type nonrec t =
      {
      password: SensitiveString.t option
        [@ocaml.doc "The SCEP challenge password."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?password = fun () -> { password }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Password", (Option.map x.password ~f:SensitiveString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let password =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "Password") in
      make ?password ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let password = field_map json__ "Password" SensitiveString.of_json in
      make ?password ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the challenge password for the specified Challenge."]
module GetChallengePasswordRequest =
  struct
    type nonrec t =
      {
      challengeArn: ChallengeArn.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the challenge."]}
    let context_ = "GetChallengePasswordRequest"
    let make ~challengeArn = fun () -> { challengeArn }
    let to_value x =
      structure_to_value
        [("ChallengeArn", (Some (ChallengeArn.to_value x.challengeArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let challengeArn =
        ChallengeArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChallengeArn") in
      make ~challengeArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let challengeArn =
        field_map_exn json__ "ChallengeArn" ChallengeArn.of_json in
      make ~challengeArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the challenge password for the specified Challenge."]
module GetChallengeMetadataResponse =
  struct
    type nonrec t =
      {
      challengeMetadata: ChallengeMetadata.t option
        [@ocaml.doc "The metadata for the challenge."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?challengeMetadata = fun () -> { challengeMetadata }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ChallengeMetadata",
           (Option.map x.challengeMetadata ~f:ChallengeMetadata.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let challengeMetadata =
        (Option.map ~f:ChallengeMetadata.of_xml)
          (Xml.child xml_arg0 "ChallengeMetadata") in
      make ?challengeMetadata ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let challengeMetadata =
        field_map json__ "ChallengeMetadata" ChallengeMetadata.of_json in
      make ?challengeMetadata ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves the metadata for the specified Challenge."]
module GetChallengeMetadataRequest =
  struct
    type nonrec t =
      {
      challengeArn: ChallengeArn.t
        [@ocaml.doc "The Amazon Resource Name (ARN) of the challenge."]}
    let context_ = "GetChallengeMetadataRequest"
    let make ~challengeArn = fun () -> { challengeArn }
    let to_value x =
      structure_to_value
        [("ChallengeArn", (Some (ChallengeArn.to_value x.challengeArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let challengeArn =
        ChallengeArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChallengeArn") in
      make ~challengeArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let challengeArn =
        field_map_exn json__ "ChallengeArn" ChallengeArn.of_json in
      make ~challengeArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves the metadata for the specified Challenge."]
module DeleteConnectorRequest =
  struct
    type nonrec t =
      {
      connectorArn: ConnectorArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the connector to delete."]}
    let context_ = "DeleteConnectorRequest"
    let make ~connectorArn = fun () -> { connectorArn }
    let to_value x =
      structure_to_value
        [("ConnectorArn", (Some (ConnectorArn.to_value x.connectorArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let connectorArn =
        ConnectorArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConnectorArn") in
      make ~connectorArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let connectorArn =
        field_map_exn json__ "ConnectorArn" ConnectorArn.of_json in
      make ~connectorArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the specified Connector. This operation also deletes any challenges associated with the connector."]
module DeleteChallengeRequest =
  struct
    type nonrec t =
      {
      challengeArn: ChallengeArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the challenge password to delete."]}
    let context_ = "DeleteChallengeRequest"
    let make ~challengeArn = fun () -> { challengeArn }
    let to_value x =
      structure_to_value
        [("ChallengeArn", (Some (ChallengeArn.to_value x.challengeArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let challengeArn =
        ChallengeArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChallengeArn") in
      make ~challengeArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let challengeArn =
        field_map_exn json__ "ChallengeArn" ChallengeArn.of_json in
      make ~challengeArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes the specified Challenge."]
module CreateConnectorResponse =
  struct
    type nonrec t =
      {
      connectorArn: ConnectorArn.t option
        [@ocaml.doc
          "Returns the Amazon Resource Name (ARN) of the connector."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?connectorArn = fun () -> { connectorArn }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ConnectorArn",
           (Option.map x.connectorArn ~f:ConnectorArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let connectorArn =
        (Option.map ~f:ConnectorArn.of_xml)
          (Xml.child xml_arg0 "ConnectorArn") in
      make ?connectorArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let connectorArn = field_map json__ "ConnectorArn" ConnectorArn.of_json in
      make ?connectorArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a SCEP connector. A SCEP connector links Amazon Web Services Private Certificate Authority to your SCEP-compatible devices and mobile device management (MDM) systems. Before you create a connector, you must complete a set of prerequisites, including creation of a private certificate authority (CA) to use with this connector. For more information, see Connector for SCEP prerequisites."]
module CreateConnectorRequest =
  struct
    type nonrec t =
      {
      certificateAuthorityArn: CertificateAuthorityArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the Amazon Web Services Private Certificate Authority certificate authority to use with this connector. Due to security vulnerabilities present in the SCEP protocol, we recommend using a private CA that's dedicated for use with the connector. To retrieve the private CAs associated with your account, you can call ListCertificateAuthorities using the Amazon Web Services Private CA API."];
      mobileDeviceManagement: MobileDeviceManagement.t option
        [@ocaml.doc
          "If you don't supply a value, by default Connector for SCEP creates a connector for general-purpose use. A general-purpose connector is designed to work with clients or endpoints that support the SCEP protocol, except Connector for SCEP for Microsoft Intune. With connectors for general-purpose use, you manage SCEP challenge passwords using Connector for SCEP. For information about considerations and limitations with using Connector for SCEP, see Considerations and Limitations. If you provide an IntuneConfiguration, Connector for SCEP creates a connector for use with Microsoft Intune, and you manage the challenge passwords using Microsoft Intune. For more information, see Using Connector for SCEP for Microsoft Intune."];
      vpcEndpointId: VpcEndpointId.t option
        [@ocaml.doc
          "If you don't supply a value, by default Connector for SCEP creates a connector accessible over the public internet. If you provide a VPC endpoint ID, creates a connector accessible only through that specific VPC endpoint."];
      clientToken: ClientToken.t option
        [@ocaml.doc
          "Custom string that can be used to distinguish between calls to the CreateChallenge action. Client tokens for CreateChallenge time out after five minutes. Therefore, if you call CreateChallenge multiple times with the same client token within five minutes, Connector for SCEP recognizes that you are requesting only one challenge and will only respond with one. If you change the client token for each call, Connector for SCEP recognizes that you are requesting multiple challenge passwords."];
      tags: Tags.t option
        [@ocaml.doc "The key-value pairs to associate with the resource."]}
    let context_ = "CreateConnectorRequest"
    let make ?mobileDeviceManagement =
      fun ?vpcEndpointId ->
        fun ?clientToken ->
          fun ?tags ->
            fun ~certificateAuthorityArn ->
              fun () ->
                {
                  mobileDeviceManagement;
                  vpcEndpointId;
                  clientToken;
                  tags;
                  certificateAuthorityArn
                }
    let to_value x =
      structure_to_value
        [("CertificateAuthorityArn",
           (Some (CertificateAuthorityArn.to_value x.certificateAuthorityArn)));
        ("MobileDeviceManagement",
          (Option.map x.mobileDeviceManagement
             ~f:MobileDeviceManagement.to_value));
        ("VpcEndpointId",
          (Option.map x.vpcEndpointId ~f:VpcEndpointId.to_value));
        ("ClientToken", (Option.map x.clientToken ~f:ClientToken.to_value));
        ("Tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let vpcEndpointId =
        (Option.map ~f:VpcEndpointId.of_xml)
          (Xml.child xml_arg0 "VpcEndpointId") in
      let mobileDeviceManagement =
        (Option.map ~f:MobileDeviceManagement.of_xml)
          (Xml.child xml_arg0 "MobileDeviceManagement") in
      let certificateAuthorityArn =
        CertificateAuthorityArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CertificateAuthorityArn") in
      make ?tags ?clientToken ?vpcEndpointId ?mobileDeviceManagement
        ~certificateAuthorityArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" Tags.of_json in
      let clientToken = field_map json__ "ClientToken" ClientToken.of_json in
      let vpcEndpointId =
        field_map json__ "VpcEndpointId" VpcEndpointId.of_json in
      let mobileDeviceManagement =
        field_map json__ "MobileDeviceManagement"
          MobileDeviceManagement.of_json in
      let certificateAuthorityArn =
        field_map_exn json__ "CertificateAuthorityArn"
          CertificateAuthorityArn.of_json in
      make ?tags ?clientToken ?vpcEndpointId ?mobileDeviceManagement
        ~certificateAuthorityArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a SCEP connector. A SCEP connector links Amazon Web Services Private Certificate Authority to your SCEP-compatible devices and mobile device management (MDM) systems. Before you create a connector, you must complete a set of prerequisites, including creation of a private certificate authority (CA) to use with this connector. For more information, see Connector for SCEP prerequisites."]
module CreateChallengeResponse =
  struct
    type nonrec t =
      {
      challenge: Challenge.t option
        [@ocaml.doc
          "Returns the challenge details for the specified connector."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `BadRequestException of BadRequestException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?challenge = fun () -> { challenge }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Challenge", (Option.map x.challenge ~f:Challenge.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let challenge =
        (Option.map ~f:Challenge.of_xml) (Xml.child xml_arg0 "Challenge") in
      make ?challenge ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let challenge = field_map json__ "Challenge" Challenge.of_json in
      make ?challenge ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "For general-purpose connectors. Creates a challenge password for the specified connector. The SCEP protocol uses a challenge password to authenticate a request before issuing a certificate from a certificate authority (CA). Your SCEP clients include the challenge password as part of their certificate request to Connector for SCEP. To retrieve the connector Amazon Resource Names (ARNs) for the connectors in your account, call ListConnectors. To create additional challenge passwords for the connector, call CreateChallenge again. We recommend frequently rotating your challenge passwords."]
module CreateChallengeRequest =
  struct
    type nonrec t =
      {
      connectorArn: ConnectorArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the connector that you want to create a challenge for."];
      clientToken: ClientToken.t option
        [@ocaml.doc
          "Custom string that can be used to distinguish between calls to the CreateChallenge action. Client tokens for CreateChallenge time out after five minutes. Therefore, if you call CreateChallenge multiple times with the same client token within five minutes, Connector for SCEP recognizes that you are requesting only one challenge and will only respond with one. If you change the client token for each call, Connector for SCEP recognizes that you are requesting multiple challenge passwords."];
      tags: Tags.t option
        [@ocaml.doc "The key-value pairs to associate with the resource."]}
    let context_ = "CreateChallengeRequest"
    let make ?clientToken =
      fun ?tags ->
        fun ~connectorArn -> fun () -> { clientToken; tags; connectorArn }
    let to_value x =
      structure_to_value
        [("ConnectorArn", (Some (ConnectorArn.to_value x.connectorArn)));
        ("ClientToken", (Option.map x.clientToken ~f:ClientToken.to_value));
        ("Tags", (Option.map x.tags ~f:Tags.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:Tags.of_xml) (Xml.child xml_arg0 "Tags") in
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "ClientToken") in
      let connectorArn =
        ConnectorArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ConnectorArn") in
      make ?tags ?clientToken ~connectorArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let tags = field_map json__ "Tags" Tags.of_json in
      let clientToken = field_map json__ "ClientToken" ClientToken.of_json in
      let connectorArn =
        field_map_exn json__ "ConnectorArn" ConnectorArn.of_json in
      make ?tags ?clientToken ~connectorArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "For general-purpose connectors. Creates a challenge password for the specified connector. The SCEP protocol uses a challenge password to authenticate a request before issuing a certificate from a certificate authority (CA). Your SCEP clients include the challenge password as part of their certificate request to Connector for SCEP. To retrieve the connector Amazon Resource Names (ARNs) for the connectors in your account, call ListConnectors. To create additional challenge passwords for the connector, call CreateChallenge again. We recommend frequently rotating your challenge passwords."]
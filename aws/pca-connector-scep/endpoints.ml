(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | CreateChallenge: (CreateChallengeRequest.t, CreateChallengeResponse.t,
  CreateChallengeResponse.error) t 
  | CreateConnector: (CreateConnectorRequest.t, CreateConnectorResponse.t,
  CreateConnectorResponse.error) t 
  | DeleteChallenge: (DeleteChallengeRequest.t, unit, unit) t 
  | DeleteConnector: (DeleteConnectorRequest.t, unit, unit) t 
  | GetChallengeMetadata: (GetChallengeMetadataRequest.t,
  GetChallengeMetadataResponse.t, GetChallengeMetadataResponse.error) t 
  | GetChallengePassword: (GetChallengePasswordRequest.t,
  GetChallengePasswordResponse.t, GetChallengePasswordResponse.error) t 
  | GetConnector: (GetConnectorRequest.t, GetConnectorResponse.t,
  GetConnectorResponse.error) t 
  | ListChallengeMetadata: (ListChallengeMetadataRequest.t,
  ListChallengeMetadataResponse.t, ListChallengeMetadataResponse.error) t 
  | ListConnectors: (ListConnectorsRequest.t, ListConnectorsResponse.t,
  ListConnectorsResponse.error) t 
  | ListTagsForResource: (ListTagsForResourceRequest.t,
  ListTagsForResourceResponse.t, ListTagsForResourceResponse.error) t 
  | TagResource: (TagResourceRequest.t, unit, unit) t 
  | UntagResource: (UntagResourceRequest.t, unit, unit) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | CreateChallenge -> `POST
  | CreateConnector -> `POST
  | DeleteChallenge -> `DELETE
  | DeleteConnector -> `DELETE
  | GetChallengeMetadata -> `GET
  | GetChallengePassword -> `GET
  | GetConnector -> `GET
  | ListChallengeMetadata -> `GET
  | ListConnectors -> `GET
  | ListTagsForResource -> `GET
  | TagResource -> `POST
  | UntagResource -> `DELETE
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | CreateChallenge -> (Format.kasprintf Uri.of_string) "/challenges"
      | CreateConnector -> (Format.kasprintf Uri.of_string) "/connectors"
      | DeleteChallenge ->
          (Format.kasprintf Uri.of_string) "/challenges/%s"
            (ChallengeArn.to_header x.DeleteChallengeRequest.challengeArn)
      | DeleteConnector ->
          (Format.kasprintf Uri.of_string) "/connectors/%s"
            (ConnectorArn.to_header x.DeleteConnectorRequest.connectorArn)
      | GetChallengeMetadata ->
          (Format.kasprintf Uri.of_string) "/challengeMetadata/%s"
            (ChallengeArn.to_header
               x.GetChallengeMetadataRequest.challengeArn)
      | GetChallengePassword ->
          (Format.kasprintf Uri.of_string) "/challengePasswords/%s"
            (ChallengeArn.to_header
               x.GetChallengePasswordRequest.challengeArn)
      | GetConnector ->
          (Format.kasprintf Uri.of_string) "/connectors/%s"
            (ConnectorArn.to_header x.GetConnectorRequest.connectorArn)
      | ListChallengeMetadata ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/challengeMetadata")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("MaxResults", (MaxResults.to_header v)))
                  x.maxResults;
               Option.map
                 ~f:(fun v -> ("NextToken", (NextToken.to_header v)))
                 x.nextToken;
               Some ("ConnectorArn", (ConnectorArn.to_header x.connectorArn))])
      | ListConnectors ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/connectors")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("MaxResults", (MaxResults.to_header v)))
                  x.maxResults;
               Option.map
                 ~f:(fun v -> ("NextToken", (NextToken.to_header v)))
                 x.nextToken])
      | ListTagsForResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (String_.to_header x.ListTagsForResourceRequest.resourceArn)
      | TagResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (String_.to_header x.TagResourceRequest.resourceArn)
      | UntagResource ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/tags/%s"
               (String_.to_header x.UntagResourceRequest.resourceArn))
            (List.filter_opt
               [Some ("tagKeys", (TagKeyList.to_header x.tagKeys))]))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | CreateChallenge ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("ConnectorArn",
                           (ConnectorArn.to_value
                              req.CreateChallengeRequest.connectorArn));
                      Option.map req.CreateChallengeRequest.clientToken
                        ~f:(fun x ->
                              ("ClientToken", (ClientToken.to_value x)));
                      Option.map req.CreateChallengeRequest.tags
                        ~f:(fun x -> ("Tags", (Tags.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CreateConnector ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("CertificateAuthorityArn",
                           (CertificateAuthorityArn.to_value
                              req.CreateConnectorRequest.certificateAuthorityArn));
                      Option.map
                        req.CreateConnectorRequest.mobileDeviceManagement
                        ~f:(fun x ->
                              ("MobileDeviceManagement",
                                (MobileDeviceManagement.to_value x)));
                      Option.map req.CreateConnectorRequest.vpcEndpointId
                        ~f:(fun x ->
                              ("VpcEndpointId", (VpcEndpointId.to_value x)));
                      Option.map req.CreateConnectorRequest.clientToken
                        ~f:(fun x ->
                              ("ClientToken", (ClientToken.to_value x)));
                      Option.map req.CreateConnectorRequest.tags
                        ~f:(fun x -> ("Tags", (Tags.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteChallenge -> Awso.Http.Request.make (method_of_endpoint endp)
  | DeleteConnector -> Awso.Http.Request.make (method_of_endpoint endp)
  | GetChallengeMetadata ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetChallengePassword ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetConnector ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListChallengeMetadata ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListConnectors ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTagsForResource ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | TagResource ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("Tags",
                           (Tags.to_value req.TagResourceRequest.tags))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UntagResource -> Awso.Http.Request.make (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | CreateChallenge ->
      if is_success
      then Ok (CreateChallengeResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some CreateChallengeResponse.error_of_json))
  | CreateConnector ->
      if is_success
      then Ok (CreateConnectorResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some CreateConnectorResponse.error_of_json))
  | DeleteChallenge ->
      if is_success then Ok () else Error (parse_aws_error None)
  | DeleteConnector ->
      if is_success then Ok () else Error (parse_aws_error None)
  | GetChallengeMetadata ->
      if is_success
      then Ok (GetChallengeMetadataResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some GetChallengeMetadataResponse.error_of_json))
  | GetChallengePassword ->
      if is_success
      then Ok (GetChallengePasswordResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some GetChallengePasswordResponse.error_of_json))
  | GetConnector ->
      if is_success
      then Ok (GetConnectorResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetConnectorResponse.error_of_json))
  | ListChallengeMetadata ->
      if is_success
      then Ok (ListChallengeMetadataResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListChallengeMetadataResponse.error_of_json))
  | ListConnectors ->
      if is_success
      then Ok (ListConnectorsResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some ListConnectorsResponse.error_of_json))
  | ListTagsForResource ->
      if is_success
      then Ok (ListTagsForResourceResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListTagsForResourceResponse.error_of_json))
  | TagResource -> if is_success then Ok () else Error (parse_aws_error None)
  | UntagResource ->
      if is_success then Ok () else Error (parse_aws_error None)
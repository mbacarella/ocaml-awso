(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_cell =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cells =
         flag "cells" (optional json_arg) ~doc:"JSON __listOf__string"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and cellName =
         flag "cell-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cell
           (Values.CreateCellRequest.make
              ?cells:(Option.map ~f:Values.Zz__listOf__string.of_json cells)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~cellName ())
           (Some Values.CreateCellResponse.to_json)
           (Some Values.CreateCellResponse.error_to_json)])
let create_cross_account_authorization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and crossAccountAuthorization =
         flag "cross-account-authorization" (required string)
           ~doc:"STRING CrossAccountAuthorization" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_cross_account_authorization
           (Values.CreateCrossAccountAuthorizationRequest.make
              ~crossAccountAuthorization ())
           (Some Values.CreateCrossAccountAuthorizationResponse.to_json)
           (Some Values.CreateCrossAccountAuthorizationResponse.error_to_json)])
let create_readiness_check =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and readinessCheckName =
         flag "readiness-check-name" (required string) ~doc:"STRING __string"
       and resourceSetName =
         flag "resource-set-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_readiness_check
           (Values.CreateReadinessCheckRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~readinessCheckName ~resourceSetName ())
           (Some Values.CreateReadinessCheckResponse.to_json)
           (Some Values.CreateReadinessCheckResponse.error_to_json)])
let create_recovery_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cells =
         flag "cells" (optional json_arg) ~doc:"JSON __listOf__string"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and recoveryGroupName =
         flag "recovery-group-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_recovery_group
           (Values.CreateRecoveryGroupRequest.make
              ?cells:(Option.map ~f:Values.Zz__listOf__string.of_json cells)
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~recoveryGroupName ())
           (Some Values.CreateRecoveryGroupResponse.to_json)
           (Some Values.CreateRecoveryGroupResponse.error_to_json)])
let create_resource_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and resourceSetName =
         flag "resource-set-name" (required string) ~doc:"STRING __string"
       and resourceSetType =
         flag "resource-set-type" (required string)
           ~doc:"STRING __stringPatternAWSAZaZ09AZaZ09"
       and resources =
         flag "resources" (required json_arg) ~doc:"JSON __listOfResource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_resource_set
           (Values.CreateResourceSetRequest.make
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~resourceSetName
              ~resourceSetType
              ~resources:(Values.Zz__listOfResource.of_json resources) ())
           (Some Values.CreateResourceSetResponse.to_json)
           (Some Values.CreateResourceSetResponse.error_to_json)])
let delete_cell =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cellName =
         flag "cell-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cell (Values.DeleteCellRequest.make ~cellName ()) None
           None])
let delete_cross_account_authorization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and crossAccountAuthorization =
         flag "cross-account-authorization" (required string)
           ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_cross_account_authorization
           (Values.DeleteCrossAccountAuthorizationRequest.make
              ~crossAccountAuthorization ())
           (Some Values.DeleteCrossAccountAuthorizationResponse.to_json)
           (Some Values.DeleteCrossAccountAuthorizationResponse.error_to_json)])
let delete_readiness_check =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and readinessCheckName =
         flag "readiness-check-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_readiness_check
           (Values.DeleteReadinessCheckRequest.make ~readinessCheckName ())
           None None])
let delete_recovery_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and recoveryGroupName =
         flag "recovery-group-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_recovery_group
           (Values.DeleteRecoveryGroupRequest.make ~recoveryGroupName ())
           None None])
let delete_resource_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceSetName =
         flag "resource-set-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_set
           (Values.DeleteResourceSetRequest.make ~resourceSetName ()) None
           None])
let get_architecture_recommendations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and recoveryGroupName =
         flag "recovery-group-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_architecture_recommendations
           (Values.GetArchitectureRecommendationsRequest.make ?maxResults
              ?nextToken ~recoveryGroupName ())
           (Some Values.GetArchitectureRecommendationsResponse.to_json)
           (Some Values.GetArchitectureRecommendationsResponse.error_to_json)])
let get_cell =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cellName =
         flag "cell-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_cell (Values.GetCellRequest.make ~cellName ())
           (Some Values.GetCellResponse.to_json)
           (Some Values.GetCellResponse.error_to_json)])
let get_cell_readiness_summary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and cellName =
         flag "cell-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_cell_readiness_summary
           (Values.GetCellReadinessSummaryRequest.make ?maxResults ?nextToken
              ~cellName ())
           (Some Values.GetCellReadinessSummaryResponse.to_json)
           (Some Values.GetCellReadinessSummaryResponse.error_to_json)])
let get_readiness_check =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and readinessCheckName =
         flag "readiness-check-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_readiness_check
           (Values.GetReadinessCheckRequest.make ~readinessCheckName ())
           (Some Values.GetReadinessCheckResponse.to_json)
           (Some Values.GetReadinessCheckResponse.error_to_json)])
let get_readiness_check_resource_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and readinessCheckName =
         flag "readiness-check-name" (required string) ~doc:"STRING __string"
       and resourceIdentifier =
         flag "resource-identifier" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_readiness_check_resource_status
           (Values.GetReadinessCheckResourceStatusRequest.make ?maxResults
              ?nextToken ~readinessCheckName ~resourceIdentifier ())
           (Some Values.GetReadinessCheckResourceStatusResponse.to_json)
           (Some Values.GetReadinessCheckResourceStatusResponse.error_to_json)])
let get_readiness_check_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and readinessCheckName =
         flag "readiness-check-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_readiness_check_status
           (Values.GetReadinessCheckStatusRequest.make ?maxResults ?nextToken
              ~readinessCheckName ())
           (Some Values.GetReadinessCheckStatusResponse.to_json)
           (Some Values.GetReadinessCheckStatusResponse.error_to_json)])
let get_recovery_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and recoveryGroupName =
         flag "recovery-group-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_recovery_group
           (Values.GetRecoveryGroupRequest.make ~recoveryGroupName ())
           (Some Values.GetRecoveryGroupResponse.to_json)
           (Some Values.GetRecoveryGroupResponse.error_to_json)])
let get_recovery_group_readiness_summary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and recoveryGroupName =
         flag "recovery-group-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_recovery_group_readiness_summary
           (Values.GetRecoveryGroupReadinessSummaryRequest.make ?maxResults
              ?nextToken ~recoveryGroupName ())
           (Some Values.GetRecoveryGroupReadinessSummaryResponse.to_json)
           (Some
              Values.GetRecoveryGroupReadinessSummaryResponse.error_to_json)])
let get_resource_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceSetName =
         flag "resource-set-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_set
           (Values.GetResourceSetRequest.make ~resourceSetName ())
           (Some Values.GetResourceSetResponse.to_json)
           (Some Values.GetResourceSetResponse.error_to_json)])
let list_cells =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_cells
           (Values.ListCellsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListCellsResponse.to_json)
           (Some Values.ListCellsResponse.error_to_json)])
let list_cross_account_authorizations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_cross_account_authorizations
           (Values.ListCrossAccountAuthorizationsRequest.make ?maxResults
              ?nextToken ())
           (Some Values.ListCrossAccountAuthorizationsResponse.to_json)
           (Some Values.ListCrossAccountAuthorizationsResponse.error_to_json)])
let list_readiness_checks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_readiness_checks
           (Values.ListReadinessChecksRequest.make ?maxResults ?nextToken ())
           (Some Values.ListReadinessChecksResponse.to_json)
           (Some Values.ListReadinessChecksResponse.error_to_json)])
let list_recovery_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_recovery_groups
           (Values.ListRecoveryGroupsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListRecoveryGroupsResponse.to_json)
           (Some Values.ListRecoveryGroupsResponse.error_to_json)])
let list_resource_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resource_sets
           (Values.ListResourceSetsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListResourceSetsResponse.to_json)
           (Some Values.ListResourceSetsResponse.error_to_json)])
let list_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and resourceType =
         flag "resource-type" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_rules
           (Values.ListRulesRequest.make ?maxResults ?nextToken ?resourceType
              ()) (Some Values.ListRulesResponse.to_json)
           (Some Values.ListRulesResponse.error_to_json)])
let list_tags_for_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resources
           (Values.ListTagsForResourcesRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourcesResponse.to_json)
           (Some Values.ListTagsForResourcesResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING __string"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON __listOf__string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.Zz__listOf__string.of_json tagKeys) ()) None
           None])
let update_cell =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cellName =
         flag "cell-name" (required string) ~doc:"STRING __string"
       and cells =
         flag "cells" (required json_arg) ~doc:"JSON __listOf__string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_cell
           (Values.UpdateCellRequest.make ~cellName
              ~cells:(Values.Zz__listOf__string.of_json cells) ())
           (Some Values.UpdateCellResponse.to_json)
           (Some Values.UpdateCellResponse.error_to_json)])
let update_readiness_check =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and readinessCheckName =
         flag "readiness-check-name" (required string) ~doc:"STRING __string"
       and resourceSetName =
         flag "resource-set-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_readiness_check
           (Values.UpdateReadinessCheckRequest.make ~readinessCheckName
              ~resourceSetName ())
           (Some Values.UpdateReadinessCheckResponse.to_json)
           (Some Values.UpdateReadinessCheckResponse.error_to_json)])
let update_recovery_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and cells =
         flag "cells" (required json_arg) ~doc:"JSON __listOf__string"
       and recoveryGroupName =
         flag "recovery-group-name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_recovery_group
           (Values.UpdateRecoveryGroupRequest.make
              ~cells:(Values.Zz__listOf__string.of_json cells)
              ~recoveryGroupName ())
           (Some Values.UpdateRecoveryGroupResponse.to_json)
           (Some Values.UpdateRecoveryGroupResponse.error_to_json)])
let update_resource_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceSetName =
         flag "resource-set-name" (required string) ~doc:"STRING __string"
       and resourceSetType =
         flag "resource-set-type" (required string)
           ~doc:"STRING __stringPatternAWSAZaZ09AZaZ09"
       and resources =
         flag "resources" (required json_arg) ~doc:"JSON __listOfResource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_resource_set
           (Values.UpdateResourceSetRequest.make ~resourceSetName
              ~resourceSetType
              ~resources:(Values.Zz__listOfResource.of_json resources) ())
           (Some Values.UpdateResourceSetResponse.to_json)
           (Some Values.UpdateResourceSetResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-cell", create_cell);
    ("create-cross-account-authorization",
      create_cross_account_authorization);
    ("create-readiness-check", create_readiness_check);
    ("create-recovery-group", create_recovery_group);
    ("create-resource-set", create_resource_set);
    ("delete-cell", delete_cell);
    ("delete-cross-account-authorization",
      delete_cross_account_authorization);
    ("delete-readiness-check", delete_readiness_check);
    ("delete-recovery-group", delete_recovery_group);
    ("delete-resource-set", delete_resource_set);
    ("get-architecture-recommendations", get_architecture_recommendations);
    ("get-cell", get_cell);
    ("get-cell-readiness-summary", get_cell_readiness_summary);
    ("get-readiness-check", get_readiness_check);
    ("get-readiness-check-resource-status",
      get_readiness_check_resource_status);
    ("get-readiness-check-status", get_readiness_check_status);
    ("get-recovery-group", get_recovery_group);
    ("get-recovery-group-readiness-summary",
      get_recovery_group_readiness_summary);
    ("get-resource-set", get_resource_set);
    ("list-cells", list_cells);
    ("list-cross-account-authorizations", list_cross_account_authorizations);
    ("list-readiness-checks", list_readiness_checks);
    ("list-recovery-groups", list_recovery_groups);
    ("list-resource-sets", list_resource_sets);
    ("list-rules", list_rules);
    ("list-tags-for-resources", list_tags_for_resources);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-cell", update_cell);
    ("update-readiness-check", update_readiness_check);
    ("update-recovery-group", update_recovery_group);
    ("update-resource-set", update_resource_set)]
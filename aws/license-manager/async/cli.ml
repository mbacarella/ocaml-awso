(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let accept_grant =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and grantArn = flag "grant-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_grant (Values.AcceptGrantRequest.make ~grantArn ())
           (Some Values.AcceptGrantResponse.to_json)
           (Some Values.AcceptGrantResponse.error_to_json)])
let check_in_license =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and beneficiary =
         flag "beneficiary" (optional string) ~doc:"STRING String"
       and licenseConsumptionToken =
         flag "license-consumption-token" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.check_in_license
           (Values.CheckInLicenseRequest.make ?beneficiary
              ~licenseConsumptionToken ())
           (Some Values.CheckInLicenseResponse.to_json)
           (Some Values.CheckInLicenseResponse.error_to_json)])
let checkout_borrow_license =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nodeId = flag "node-id" (optional string) ~doc:"STRING String"
       and checkoutMetadata =
         flag "checkout-metadata" (optional json_arg)
           ~doc:"JSON MetadataList"
       and licenseArn =
         flag "license-arn" (required string) ~doc:"STRING Arn"
       and entitlements =
         flag "entitlements" (required json_arg)
           ~doc:"JSON EntitlementDataList"
       and digitalSignatureMethod =
         flag "digital-signature-method" (required json_arg)
           ~doc:"JSON DigitalSignatureMethod"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.checkout_borrow_license
           (Values.CheckoutBorrowLicenseRequest.make ?nodeId
              ?checkoutMetadata:(Option.map ~f:Values.MetadataList.of_json
                                   checkoutMetadata) ~licenseArn
              ~entitlements:(Values.EntitlementDataList.of_json entitlements)
              ~digitalSignatureMethod:(Values.DigitalSignatureMethod.of_json
                                         digitalSignatureMethod) ~clientToken
              ()) (Some Values.CheckoutBorrowLicenseResponse.to_json)
           (Some Values.CheckoutBorrowLicenseResponse.error_to_json)])
let checkout_license =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and beneficiary =
         flag "beneficiary" (optional string) ~doc:"STRING String"
       and nodeId = flag "node-id" (optional string) ~doc:"STRING String"
       and productSKU =
         flag "product-s-k-u" (required string) ~doc:"STRING String"
       and checkoutType =
         flag "checkout-type" (required json_arg) ~doc:"JSON CheckoutType"
       and keyFingerprint =
         flag "key-fingerprint" (required string) ~doc:"STRING String"
       and entitlements =
         flag "entitlements" (required json_arg)
           ~doc:"JSON EntitlementDataList"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.checkout_license
           (Values.CheckoutLicenseRequest.make ?beneficiary ?nodeId
              ~productSKU
              ~checkoutType:(Values.CheckoutType.of_json checkoutType)
              ~keyFingerprint
              ~entitlements:(Values.EntitlementDataList.of_json entitlements)
              ~clientToken ()) (Some Values.CheckoutLicenseResponse.to_json)
           (Some Values.CheckoutLicenseResponse.error_to_json)])
let create_grant =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken"
       and grantName =
         flag "grant-name" (required string) ~doc:"STRING String"
       and licenseArn =
         flag "license-arn" (required string) ~doc:"STRING Arn"
       and principals =
         flag "principals" (required json_arg) ~doc:"JSON PrincipalArnList"
       and homeRegion =
         flag "home-region" (required string) ~doc:"STRING String"
       and allowedOperations =
         flag "allowed-operations" (required json_arg)
           ~doc:"JSON AllowedOperationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_grant
           (Values.CreateGrantRequest.make ~clientToken ~grantName
              ~licenseArn
              ~principals:(Values.PrincipalArnList.of_json principals)
              ~homeRegion
              ~allowedOperations:(Values.AllowedOperationList.of_json
                                    allowedOperations) ())
           (Some Values.CreateGrantResponse.to_json)
           (Some Values.CreateGrantResponse.error_to_json)])
let create_grant_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and grantName =
         flag "grant-name" (optional string) ~doc:"STRING String"
       and allowedOperations =
         flag "allowed-operations" (optional json_arg)
           ~doc:"JSON AllowedOperationList"
       and status = flag "status" (optional json_arg) ~doc:"JSON GrantStatus"
       and statusReason =
         flag "status-reason" (optional string)
           ~doc:"STRING StatusReasonMessage"
       and sourceVersion =
         flag "source-version" (optional string) ~doc:"STRING String"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken"
       and grantArn = flag "grant-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_grant_version
           (Values.CreateGrantVersionRequest.make ?grantName
              ?allowedOperations:(Option.map
                                    ~f:Values.AllowedOperationList.of_json
                                    allowedOperations)
              ?status:(Option.map ~f:Values.GrantStatus.of_json status)
              ?statusReason ?sourceVersion ~clientToken ~grantArn ())
           (Some Values.CreateGrantVersionResponse.to_json)
           (Some Values.CreateGrantVersionResponse.error_to_json)])
let create_license =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and licenseMetadata =
         flag "license-metadata" (optional json_arg) ~doc:"JSON MetadataList"
       and licenseName =
         flag "license-name" (required string) ~doc:"STRING String"
       and productName =
         flag "product-name" (required string) ~doc:"STRING String"
       and productSKU =
         flag "product-s-k-u" (required string) ~doc:"STRING String"
       and issuer = flag "issuer" (required json_arg) ~doc:"JSON Issuer"
       and homeRegion =
         flag "home-region" (required string) ~doc:"STRING String"
       and validity =
         flag "validity" (required json_arg) ~doc:"JSON DatetimeRange"
       and entitlements =
         flag "entitlements" (required json_arg) ~doc:"JSON EntitlementList"
       and beneficiary =
         flag "beneficiary" (required string) ~doc:"STRING String"
       and consumptionConfiguration =
         flag "consumption-configuration" (required json_arg)
           ~doc:"JSON ConsumptionConfiguration"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_license
           (Values.CreateLicenseRequest.make
              ?licenseMetadata:(Option.map ~f:Values.MetadataList.of_json
                                  licenseMetadata) ~licenseName ~productName
              ~productSKU ~issuer:(Values.Issuer.of_json issuer) ~homeRegion
              ~validity:(Values.DatetimeRange.of_json validity)
              ~entitlements:(Values.EntitlementList.of_json entitlements)
              ~beneficiary
              ~consumptionConfiguration:(Values.ConsumptionConfiguration.of_json
                                           consumptionConfiguration)
              ~clientToken ()) (Some Values.CreateLicenseResponse.to_json)
           (Some Values.CreateLicenseResponse.error_to_json)])
let create_license_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and licenseCount =
         flag "license-count" (optional json_arg) ~doc:"JSON BoxLong"
       and licenseCountHardLimit =
         flag "license-count-hard-limit" (optional bool)
           ~doc:"BOOL BoxBoolean"
       and licenseRules =
         flag "license-rules" (optional json_arg) ~doc:"JSON StringList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and disassociateWhenNotFound =
         flag "disassociate-when-not-found" (optional bool)
           ~doc:"BOOL BoxBoolean"
       and productInformationList =
         flag "product-information-list" (optional json_arg)
           ~doc:"JSON ProductInformationList"
       and name = flag "name" (required string) ~doc:"STRING String"
       and licenseCountingType =
         flag "license-counting-type" (required json_arg)
           ~doc:"JSON LicenseCountingType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_license_configuration
           (Values.CreateLicenseConfigurationRequest.make ?description
              ?licenseCount:(Option.map ~f:Values.BoxLong.of_json
                               licenseCount) ?licenseCountHardLimit
              ?licenseRules:(Option.map ~f:Values.StringList.of_json
                               licenseRules)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?disassociateWhenNotFound
              ?productInformationList:(Option.map
                                         ~f:Values.ProductInformationList.of_json
                                         productInformationList) ~name
              ~licenseCountingType:(Values.LicenseCountingType.of_json
                                      licenseCountingType) ())
           (Some Values.CreateLicenseConfigurationResponse.to_json)
           (Some Values.CreateLicenseConfigurationResponse.error_to_json)])
let create_license_conversion_task_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and sourceLicenseContext =
         flag "source-license-context" (required json_arg)
           ~doc:"JSON LicenseConversionContext"
       and destinationLicenseContext =
         flag "destination-license-context" (required json_arg)
           ~doc:"JSON LicenseConversionContext" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_license_conversion_task_for_resource
           (Values.CreateLicenseConversionTaskForResourceRequest.make
              ~resourceArn
              ~sourceLicenseContext:(Values.LicenseConversionContext.of_json
                                       sourceLicenseContext)
              ~destinationLicenseContext:(Values.LicenseConversionContext.of_json
                                            destinationLicenseContext) ())
           (Some
              Values.CreateLicenseConversionTaskForResourceResponse.to_json)
           (Some
              Values.CreateLicenseConversionTaskForResourceResponse.error_to_json)])
let create_license_manager_report_generator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and reportGeneratorName =
         flag "report-generator-name" (required string)
           ~doc:"STRING ReportGeneratorName"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON ReportTypeList"
       and reportContext =
         flag "report-context" (required json_arg) ~doc:"JSON ReportContext"
       and reportFrequency =
         flag "report-frequency" (required json_arg)
           ~doc:"JSON ReportFrequency"
       and clientToken =
         flag "client-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_license_manager_report_generator
           (Values.CreateLicenseManagerReportGeneratorRequest.make
              ?description ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~reportGeneratorName
              ~type_:(Values.ReportTypeList.of_json type_)
              ~reportContext:(Values.ReportContext.of_json reportContext)
              ~reportFrequency:(Values.ReportFrequency.of_json
                                  reportFrequency) ~clientToken ())
           (Some Values.CreateLicenseManagerReportGeneratorResponse.to_json)
           (Some
              Values.CreateLicenseManagerReportGeneratorResponse.error_to_json)])
let create_license_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and licenseMetadata =
         flag "license-metadata" (optional json_arg) ~doc:"JSON MetadataList"
       and sourceVersion =
         flag "source-version" (optional string) ~doc:"STRING String"
       and licenseArn =
         flag "license-arn" (required string) ~doc:"STRING Arn"
       and licenseName =
         flag "license-name" (required string) ~doc:"STRING String"
       and productName =
         flag "product-name" (required string) ~doc:"STRING String"
       and issuer = flag "issuer" (required json_arg) ~doc:"JSON Issuer"
       and homeRegion =
         flag "home-region" (required string) ~doc:"STRING String"
       and validity =
         flag "validity" (required json_arg) ~doc:"JSON DatetimeRange"
       and entitlements =
         flag "entitlements" (required json_arg) ~doc:"JSON EntitlementList"
       and consumptionConfiguration =
         flag "consumption-configuration" (required json_arg)
           ~doc:"JSON ConsumptionConfiguration"
       and status =
         flag "status" (required json_arg) ~doc:"JSON LicenseStatus"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_license_version
           (Values.CreateLicenseVersionRequest.make
              ?licenseMetadata:(Option.map ~f:Values.MetadataList.of_json
                                  licenseMetadata) ?sourceVersion ~licenseArn
              ~licenseName ~productName
              ~issuer:(Values.Issuer.of_json issuer) ~homeRegion
              ~validity:(Values.DatetimeRange.of_json validity)
              ~entitlements:(Values.EntitlementList.of_json entitlements)
              ~consumptionConfiguration:(Values.ConsumptionConfiguration.of_json
                                           consumptionConfiguration)
              ~status:(Values.LicenseStatus.of_json status) ~clientToken ())
           (Some Values.CreateLicenseVersionResponse.to_json)
           (Some Values.CreateLicenseVersionResponse.error_to_json)])
let create_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleArns =
         flag "role-arns" (optional json_arg) ~doc:"JSON ArnList"
       and expirationInDays =
         flag "expiration-in-days" (optional int) ~doc:"INT Integer"
       and tokenProperties =
         flag "token-properties" (optional json_arg)
           ~doc:"JSON MaxSize3StringList"
       and licenseArn =
         flag "license-arn" (required string) ~doc:"STRING Arn"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_token
           (Values.CreateTokenRequest.make
              ?roleArns:(Option.map ~f:Values.ArnList.of_json roleArns)
              ?expirationInDays
              ?tokenProperties:(Option.map
                                  ~f:Values.MaxSize3StringList.of_json
                                  tokenProperties) ~licenseArn ~clientToken
              ()) (Some Values.CreateTokenResponse.to_json)
           (Some Values.CreateTokenResponse.error_to_json)])
let delete_grant =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and statusReason =
         flag "status-reason" (optional string)
           ~doc:"STRING StatusReasonMessage"
       and grantArn = flag "grant-arn" (required string) ~doc:"STRING Arn"
       and version = flag "version" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_grant
           (Values.DeleteGrantRequest.make ?statusReason ~grantArn ~version
              ()) (Some Values.DeleteGrantResponse.to_json)
           (Some Values.DeleteGrantResponse.error_to_json)])
let delete_license =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and licenseArn =
         flag "license-arn" (required string) ~doc:"STRING Arn"
       and sourceVersion =
         flag "source-version" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_license
           (Values.DeleteLicenseRequest.make ~licenseArn ~sourceVersion ())
           (Some Values.DeleteLicenseResponse.to_json)
           (Some Values.DeleteLicenseResponse.error_to_json)])
let delete_license_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and licenseConfigurationArn =
         flag "license-configuration-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_license_configuration
           (Values.DeleteLicenseConfigurationRequest.make
              ~licenseConfigurationArn ())
           (Some Values.DeleteLicenseConfigurationResponse.to_json)
           (Some Values.DeleteLicenseConfigurationResponse.error_to_json)])
let delete_license_manager_report_generator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and licenseManagerReportGeneratorArn =
         flag "license-manager-report-generator-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_license_manager_report_generator
           (Values.DeleteLicenseManagerReportGeneratorRequest.make
              ~licenseManagerReportGeneratorArn ())
           (Some Values.DeleteLicenseManagerReportGeneratorResponse.to_json)
           (Some
              Values.DeleteLicenseManagerReportGeneratorResponse.error_to_json)])
let delete_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tokenId = flag "token-id" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_token (Values.DeleteTokenRequest.make ~tokenId ())
           (Some Values.DeleteTokenResponse.to_json)
           (Some Values.DeleteTokenResponse.error_to_json)])
let extend_license_consumption =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and licenseConsumptionToken =
         flag "license-consumption-token" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.extend_license_consumption
           (Values.ExtendLicenseConsumptionRequest.make ?dryRun
              ~licenseConsumptionToken ())
           (Some Values.ExtendLicenseConsumptionResponse.to_json)
           (Some Values.ExtendLicenseConsumptionResponse.error_to_json)])
let get_access_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tokenProperties =
         flag "token-properties" (optional json_arg)
           ~doc:"JSON MaxSize3StringList"
       and token = flag "token" (required string) ~doc:"STRING TokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_access_token
           (Values.GetAccessTokenRequest.make
              ?tokenProperties:(Option.map
                                  ~f:Values.MaxSize3StringList.of_json
                                  tokenProperties) ~token ())
           (Some Values.GetAccessTokenResponse.to_json)
           (Some Values.GetAccessTokenResponse.error_to_json)])
let get_grant =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and version = flag "version" (optional string) ~doc:"STRING String"
       and grantArn = flag "grant-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_grant (Values.GetGrantRequest.make ?version ~grantArn ())
           (Some Values.GetGrantResponse.to_json)
           (Some Values.GetGrantResponse.error_to_json)])
let get_license =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and version = flag "version" (optional string) ~doc:"STRING String"
       and licenseArn =
         flag "license-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_license
           (Values.GetLicenseRequest.make ?version ~licenseArn ())
           (Some Values.GetLicenseResponse.to_json)
           (Some Values.GetLicenseResponse.error_to_json)])
let get_license_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and licenseConfigurationArn =
         flag "license-configuration-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_license_configuration
           (Values.GetLicenseConfigurationRequest.make
              ~licenseConfigurationArn ())
           (Some Values.GetLicenseConfigurationResponse.to_json)
           (Some Values.GetLicenseConfigurationResponse.error_to_json)])
let get_license_conversion_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and licenseConversionTaskId =
         flag "license-conversion-task-id" (required string)
           ~doc:"STRING LicenseConversionTaskId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_license_conversion_task
           (Values.GetLicenseConversionTaskRequest.make
              ~licenseConversionTaskId ())
           (Some Values.GetLicenseConversionTaskResponse.to_json)
           (Some Values.GetLicenseConversionTaskResponse.error_to_json)])
let get_license_manager_report_generator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and licenseManagerReportGeneratorArn =
         flag "license-manager-report-generator-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_license_manager_report_generator
           (Values.GetLicenseManagerReportGeneratorRequest.make
              ~licenseManagerReportGeneratorArn ())
           (Some Values.GetLicenseManagerReportGeneratorResponse.to_json)
           (Some
              Values.GetLicenseManagerReportGeneratorResponse.error_to_json)])
let get_license_usage =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and licenseArn =
         flag "license-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_license_usage
           (Values.GetLicenseUsageRequest.make ~licenseArn ())
           (Some Values.GetLicenseUsageResponse.to_json)
           (Some Values.GetLicenseUsageResponse.error_to_json)])
let get_service_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service_settings (Values.GetServiceSettingsRequest.make ())
           (Some Values.GetServiceSettingsResponse.to_json)
           (Some Values.GetServiceSettingsResponse.error_to_json)])
let list_associations_for_license_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BoxInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and licenseConfigurationArn =
         flag "license-configuration-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_associations_for_license_configuration
           (Values.ListAssociationsForLicenseConfigurationRequest.make
              ?maxResults ?nextToken ~licenseConfigurationArn ())
           (Some
              Values.ListAssociationsForLicenseConfigurationResponse.to_json)
           (Some
              Values.ListAssociationsForLicenseConfigurationResponse.error_to_json)])
let list_distributed_grants =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and grantArns =
         flag "grant-arns" (optional json_arg) ~doc:"JSON ArnList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxSize100" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_distributed_grants
           (Values.ListDistributedGrantsRequest.make
              ?grantArns:(Option.map ~f:Values.ArnList.of_json grantArns)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?nextToken ?maxResults ())
           (Some Values.ListDistributedGrantsResponse.to_json)
           (Some Values.ListDistributedGrantsResponse.error_to_json)])
let list_failures_for_license_configuration_operations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BoxInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and licenseConfigurationArn =
         flag "license-configuration-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_failures_for_license_configuration_operations
           (Values.ListFailuresForLicenseConfigurationOperationsRequest.make
              ?maxResults ?nextToken ~licenseConfigurationArn ())
           (Some
              Values.ListFailuresForLicenseConfigurationOperationsResponse.to_json)
           (Some
              Values.ListFailuresForLicenseConfigurationOperationsResponse.error_to_json)])
let list_license_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and licenseConfigurationArns =
         flag "license-configuration-arns" (optional json_arg)
           ~doc:"JSON StringList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BoxInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_license_configurations
           (Values.ListLicenseConfigurationsRequest.make
              ?licenseConfigurationArns:(Option.map
                                           ~f:Values.StringList.of_json
                                           licenseConfigurationArns)
              ?maxResults ?nextToken
              ?filters:(Option.map ~f:Values.Filters.of_json filters) ())
           (Some Values.ListLicenseConfigurationsResponse.to_json)
           (Some Values.ListLicenseConfigurationsResponse.error_to_json)])
let list_license_conversion_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BoxInteger"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_license_conversion_tasks
           (Values.ListLicenseConversionTasksRequest.make ?nextToken
              ?maxResults
              ?filters:(Option.map ~f:Values.Filters.of_json filters) ())
           (Some Values.ListLicenseConversionTasksResponse.to_json)
           (Some Values.ListLicenseConversionTasksResponse.error_to_json)])
let list_license_manager_report_generators =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxSize100" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_license_manager_report_generators
           (Values.ListLicenseManagerReportGeneratorsRequest.make
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?nextToken ?maxResults ())
           (Some Values.ListLicenseManagerReportGeneratorsResponse.to_json)
           (Some
              Values.ListLicenseManagerReportGeneratorsResponse.error_to_json)])
let list_license_specifications_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BoxInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_license_specifications_for_resource
           (Values.ListLicenseSpecificationsForResourceRequest.make
              ?maxResults ?nextToken ~resourceArn ())
           (Some Values.ListLicenseSpecificationsForResourceResponse.to_json)
           (Some
              Values.ListLicenseSpecificationsForResourceResponse.error_to_json)])
let list_license_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxSize100"
       and licenseArn =
         flag "license-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_license_versions
           (Values.ListLicenseVersionsRequest.make ?nextToken ?maxResults
              ~licenseArn ())
           (Some Values.ListLicenseVersionsResponse.to_json)
           (Some Values.ListLicenseVersionsResponse.error_to_json)])
let list_licenses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and licenseArns =
         flag "license-arns" (optional json_arg) ~doc:"JSON ArnList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxSize100" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_licenses
           (Values.ListLicensesRequest.make
              ?licenseArns:(Option.map ~f:Values.ArnList.of_json licenseArns)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?nextToken ?maxResults ())
           (Some Values.ListLicensesResponse.to_json)
           (Some Values.ListLicensesResponse.error_to_json)])
let list_received_grants =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and grantArns =
         flag "grant-arns" (optional json_arg) ~doc:"JSON ArnList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxSize100" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_received_grants
           (Values.ListReceivedGrantsRequest.make
              ?grantArns:(Option.map ~f:Values.ArnList.of_json grantArns)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?nextToken ?maxResults ())
           (Some Values.ListReceivedGrantsResponse.to_json)
           (Some Values.ListReceivedGrantsResponse.error_to_json)])
let list_received_licenses =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and licenseArns =
         flag "license-arns" (optional json_arg) ~doc:"JSON ArnList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxSize100" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_received_licenses
           (Values.ListReceivedLicensesRequest.make
              ?licenseArns:(Option.map ~f:Values.ArnList.of_json licenseArns)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?nextToken ?maxResults ())
           (Some Values.ListReceivedLicensesResponse.to_json)
           (Some Values.ListReceivedLicensesResponse.error_to_json)])
let list_resource_inventory =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BoxInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON InventoryFilterList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resource_inventory
           (Values.ListResourceInventoryRequest.make ?maxResults ?nextToken
              ?filters:(Option.map ~f:Values.InventoryFilterList.of_json
                          filters) ())
           (Some Values.ListResourceInventoryResponse.to_json)
           (Some Values.ListResourceInventoryResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_tokens =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tokenIds =
         flag "token-ids" (optional json_arg) ~doc:"JSON StringList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxSize100" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tokens
           (Values.ListTokensRequest.make
              ?tokenIds:(Option.map ~f:Values.StringList.of_json tokenIds)
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ?nextToken ?maxResults ())
           (Some Values.ListTokensResponse.to_json)
           (Some Values.ListTokensResponse.error_to_json)])
let list_usage_for_license_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT BoxInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters"
       and licenseConfigurationArn =
         flag "license-configuration-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_usage_for_license_configuration
           (Values.ListUsageForLicenseConfigurationRequest.make ?maxResults
              ?nextToken
              ?filters:(Option.map ~f:Values.Filters.of_json filters)
              ~licenseConfigurationArn ())
           (Some Values.ListUsageForLicenseConfigurationResponse.to_json)
           (Some
              Values.ListUsageForLicenseConfigurationResponse.error_to_json)])
let reject_grant =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and grantArn = flag "grant-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reject_grant (Values.RejectGrantRequest.make ~grantArn ())
           (Some Values.RejectGrantResponse.to_json)
           (Some Values.RejectGrantResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_license_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and licenseConfigurationStatus =
         flag "license-configuration-status" (optional json_arg)
           ~doc:"JSON LicenseConfigurationStatus"
       and licenseRules =
         flag "license-rules" (optional json_arg) ~doc:"JSON StringList"
       and licenseCount =
         flag "license-count" (optional json_arg) ~doc:"JSON BoxLong"
       and licenseCountHardLimit =
         flag "license-count-hard-limit" (optional bool)
           ~doc:"BOOL BoxBoolean"
       and name = flag "name" (optional string) ~doc:"STRING String"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and productInformationList =
         flag "product-information-list" (optional json_arg)
           ~doc:"JSON ProductInformationList"
       and disassociateWhenNotFound =
         flag "disassociate-when-not-found" (optional bool)
           ~doc:"BOOL BoxBoolean"
       and licenseConfigurationArn =
         flag "license-configuration-arn" (required string)
           ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_license_configuration
           (Values.UpdateLicenseConfigurationRequest.make
              ?licenseConfigurationStatus:(Option.map
                                             ~f:Values.LicenseConfigurationStatus.of_json
                                             licenseConfigurationStatus)
              ?licenseRules:(Option.map ~f:Values.StringList.of_json
                               licenseRules)
              ?licenseCount:(Option.map ~f:Values.BoxLong.of_json
                               licenseCount) ?licenseCountHardLimit ?name
              ?description
              ?productInformationList:(Option.map
                                         ~f:Values.ProductInformationList.of_json
                                         productInformationList)
              ?disassociateWhenNotFound ~licenseConfigurationArn ())
           (Some Values.UpdateLicenseConfigurationResponse.to_json)
           (Some Values.UpdateLicenseConfigurationResponse.error_to_json)])
let update_license_manager_report_generator =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and licenseManagerReportGeneratorArn =
         flag "license-manager-report-generator-arn" (required string)
           ~doc:"STRING String"
       and reportGeneratorName =
         flag "report-generator-name" (required string)
           ~doc:"STRING ReportGeneratorName"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON ReportTypeList"
       and reportContext =
         flag "report-context" (required json_arg) ~doc:"JSON ReportContext"
       and reportFrequency =
         flag "report-frequency" (required json_arg)
           ~doc:"JSON ReportFrequency"
       and clientToken =
         flag "client-token" (required string)
           ~doc:"STRING ClientRequestToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_license_manager_report_generator
           (Values.UpdateLicenseManagerReportGeneratorRequest.make
              ?description ~licenseManagerReportGeneratorArn
              ~reportGeneratorName
              ~type_:(Values.ReportTypeList.of_json type_)
              ~reportContext:(Values.ReportContext.of_json reportContext)
              ~reportFrequency:(Values.ReportFrequency.of_json
                                  reportFrequency) ~clientToken ())
           (Some Values.UpdateLicenseManagerReportGeneratorResponse.to_json)
           (Some
              Values.UpdateLicenseManagerReportGeneratorResponse.error_to_json)])
let update_license_specifications_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and addLicenseSpecifications =
         flag "add-license-specifications" (optional json_arg)
           ~doc:"JSON LicenseSpecifications"
       and removeLicenseSpecifications =
         flag "remove-license-specifications" (optional json_arg)
           ~doc:"JSON LicenseSpecifications"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_license_specifications_for_resource
           (Values.UpdateLicenseSpecificationsForResourceRequest.make
              ?addLicenseSpecifications:(Option.map
                                           ~f:Values.LicenseSpecifications.of_json
                                           addLicenseSpecifications)
              ?removeLicenseSpecifications:(Option.map
                                              ~f:Values.LicenseSpecifications.of_json
                                              removeLicenseSpecifications)
              ~resourceArn ())
           (Some
              Values.UpdateLicenseSpecificationsForResourceResponse.to_json)
           (Some
              Values.UpdateLicenseSpecificationsForResourceResponse.error_to_json)])
let update_service_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and s3BucketArn =
         flag "s3-bucket-arn" (optional string) ~doc:"STRING String"
       and snsTopicArn =
         flag "sns-topic-arn" (optional string) ~doc:"STRING String"
       and organizationConfiguration =
         flag "organization-configuration" (optional json_arg)
           ~doc:"JSON OrganizationConfiguration"
       and enableCrossAccountsDiscovery =
         flag "enable-cross-accounts-discovery" (optional bool)
           ~doc:"BOOL BoxBoolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_service_settings
           (Values.UpdateServiceSettingsRequest.make ?s3BucketArn
              ?snsTopicArn
              ?organizationConfiguration:(Option.map
                                            ~f:Values.OrganizationConfiguration.of_json
                                            organizationConfiguration)
              ?enableCrossAccountsDiscovery ())
           (Some Values.UpdateServiceSettingsResponse.to_json)
           (Some Values.UpdateServiceSettingsResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("accept-grant", accept_grant);
    ("check-in-license", check_in_license);
    ("checkout-borrow-license", checkout_borrow_license);
    ("checkout-license", checkout_license);
    ("create-grant", create_grant);
    ("create-grant-version", create_grant_version);
    ("create-license", create_license);
    ("create-license-configuration", create_license_configuration);
    ("create-license-conversion-task-for-resource",
      create_license_conversion_task_for_resource);
    ("create-license-manager-report-generator",
      create_license_manager_report_generator);
    ("create-license-version", create_license_version);
    ("create-token", create_token);
    ("delete-grant", delete_grant);
    ("delete-license", delete_license);
    ("delete-license-configuration", delete_license_configuration);
    ("delete-license-manager-report-generator",
      delete_license_manager_report_generator);
    ("delete-token", delete_token);
    ("extend-license-consumption", extend_license_consumption);
    ("get-access-token", get_access_token);
    ("get-grant", get_grant);
    ("get-license", get_license);
    ("get-license-configuration", get_license_configuration);
    ("get-license-conversion-task", get_license_conversion_task);
    ("get-license-manager-report-generator",
      get_license_manager_report_generator);
    ("get-license-usage", get_license_usage);
    ("get-service-settings", get_service_settings);
    ("list-associations-for-license-configuration",
      list_associations_for_license_configuration);
    ("list-distributed-grants", list_distributed_grants);
    ("list-failures-for-license-configuration-operations",
      list_failures_for_license_configuration_operations);
    ("list-license-configurations", list_license_configurations);
    ("list-license-conversion-tasks", list_license_conversion_tasks);
    ("list-license-manager-report-generators",
      list_license_manager_report_generators);
    ("list-license-specifications-for-resource",
      list_license_specifications_for_resource);
    ("list-license-versions", list_license_versions);
    ("list-licenses", list_licenses);
    ("list-received-grants", list_received_grants);
    ("list-received-licenses", list_received_licenses);
    ("list-resource-inventory", list_resource_inventory);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-tokens", list_tokens);
    ("list-usage-for-license-configuration",
      list_usage_for_license_configuration);
    ("reject-grant", reject_grant);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-license-configuration", update_license_configuration);
    ("update-license-manager-report-generator",
      update_license_manager_report_generator);
    ("update-license-specifications-for-resource",
      update_license_specifications_for_resource);
    ("update-service-settings", update_service_settings)]
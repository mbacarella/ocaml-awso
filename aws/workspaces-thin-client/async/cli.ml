(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING EnvironmentName"
       and desktopEndpoint =
         flag "desktop-endpoint" (optional string)
           ~doc:"STRING DesktopEndpoint"
       and softwareSetUpdateSchedule =
         flag "software-set-update-schedule" (optional json_arg)
           ~doc:"JSON SoftwareSetUpdateSchedule"
       and maintenanceWindow =
         flag "maintenance-window" (optional json_arg)
           ~doc:"JSON MaintenanceWindow"
       and softwareSetUpdateMode =
         flag "software-set-update-mode" (optional json_arg)
           ~doc:"JSON SoftwareSetUpdateMode"
       and desiredSoftwareSetId =
         flag "desired-software-set-id" (optional string)
           ~doc:"STRING SoftwareSetId"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and deviceCreationTags =
         flag "device-creation-tags" (optional json_arg)
           ~doc:"JSON DeviceCreationTagsMap"
       and desktopArn =
         flag "desktop-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_environment
           (Values.CreateEnvironmentRequest.make ?name ?desktopEndpoint
              ?softwareSetUpdateSchedule:(Option.map
                                            ~f:Values.SoftwareSetUpdateSchedule.of_json
                                            softwareSetUpdateSchedule)
              ?maintenanceWindow:(Option.map
                                    ~f:Values.MaintenanceWindow.of_json
                                    maintenanceWindow)
              ?softwareSetUpdateMode:(Option.map
                                        ~f:Values.SoftwareSetUpdateMode.of_json
                                        softwareSetUpdateMode)
              ?desiredSoftwareSetId ?kmsKeyArn ?clientToken
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ?deviceCreationTags:(Option.map
                                     ~f:Values.DeviceCreationTagsMap.of_json
                                     deviceCreationTags) ~desktopArn ())
           (Some Values.CreateEnvironmentResponse.to_json)
           (Some Values.CreateEnvironmentResponse.error_to_json)])
let delete_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and id = flag "id" (required string) ~doc:"STRING DeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_device
           (Values.DeleteDeviceRequest.make ?clientToken ~id ())
           (Some Values.DeleteDeviceResponse.to_json)
           (Some Values.DeleteDeviceResponse.error_to_json)])
let delete_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and id = flag "id" (required string) ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_environment
           (Values.DeleteEnvironmentRequest.make ?clientToken ~id ())
           (Some Values.DeleteEnvironmentResponse.to_json)
           (Some Values.DeleteEnvironmentResponse.error_to_json)])
let deregister_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetDeviceStatus =
         flag "target-device-status" (optional json_arg)
           ~doc:"JSON TargetDeviceStatus"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and id = flag "id" (required string) ~doc:"STRING DeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_device
           (Values.DeregisterDeviceRequest.make
              ?targetDeviceStatus:(Option.map
                                     ~f:Values.TargetDeviceStatus.of_json
                                     targetDeviceStatus) ?clientToken ~id ())
           (Some Values.DeregisterDeviceResponse.to_json)
           (Some Values.DeregisterDeviceResponse.error_to_json)])
let get_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING DeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_device (Values.GetDeviceRequest.make ~id ())
           (Some Values.GetDeviceResponse.to_json)
           (Some Values.GetDeviceResponse.error_to_json)])
let get_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_environment (Values.GetEnvironmentRequest.make ~id ())
           (Some Values.GetEnvironmentResponse.to_json)
           (Some Values.GetEnvironmentResponse.error_to_json)])
let get_software_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING SoftwareSetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_software_set (Values.GetSoftwareSetRequest.make ~id ())
           (Some Values.GetSoftwareSetResponse.to_json)
           (Some Values.GetSoftwareSetResponse.error_to_json)])
let list_devices =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_devices
           (Values.ListDevicesRequest.make ?nextToken ?maxResults ())
           (Some Values.ListDevicesResponse.to_json)
           (Some Values.ListDevicesResponse.error_to_json)])
let list_environments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_environments
           (Values.ListEnvironmentsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListEnvironmentsResponse.to_json)
           (Some Values.ListEnvironmentsResponse.error_to_json)])
let list_software_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_software_sets
           (Values.ListSoftwareSetsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListSoftwareSetsResponse.to_json)
           (Some Values.ListSoftwareSetsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagsMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_device =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING DeviceName"
       and desiredSoftwareSetId =
         flag "desired-software-set-id" (optional string)
           ~doc:"STRING SoftwareSetId"
       and softwareSetUpdateSchedule =
         flag "software-set-update-schedule" (optional json_arg)
           ~doc:"JSON SoftwareSetUpdateSchedule"
       and id = flag "id" (required string) ~doc:"STRING DeviceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_device
           (Values.UpdateDeviceRequest.make ?name ?desiredSoftwareSetId
              ?softwareSetUpdateSchedule:(Option.map
                                            ~f:Values.SoftwareSetUpdateSchedule.of_json
                                            softwareSetUpdateSchedule) ~id ())
           (Some Values.UpdateDeviceResponse.to_json)
           (Some Values.UpdateDeviceResponse.error_to_json)])
let update_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING EnvironmentName"
       and desktopArn =
         flag "desktop-arn" (optional string) ~doc:"STRING Arn"
       and desktopEndpoint =
         flag "desktop-endpoint" (optional string)
           ~doc:"STRING DesktopEndpoint"
       and softwareSetUpdateSchedule =
         flag "software-set-update-schedule" (optional json_arg)
           ~doc:"JSON SoftwareSetUpdateSchedule"
       and maintenanceWindow =
         flag "maintenance-window" (optional json_arg)
           ~doc:"JSON MaintenanceWindow"
       and softwareSetUpdateMode =
         flag "software-set-update-mode" (optional json_arg)
           ~doc:"JSON SoftwareSetUpdateMode"
       and desiredSoftwareSetId =
         flag "desired-software-set-id" (optional string)
           ~doc:"STRING SoftwareSetIdOrEmptyString"
       and deviceCreationTags =
         flag "device-creation-tags" (optional json_arg)
           ~doc:"JSON DeviceCreationTagsMap"
       and id = flag "id" (required string) ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_environment
           (Values.UpdateEnvironmentRequest.make ?name ?desktopArn
              ?desktopEndpoint
              ?softwareSetUpdateSchedule:(Option.map
                                            ~f:Values.SoftwareSetUpdateSchedule.of_json
                                            softwareSetUpdateSchedule)
              ?maintenanceWindow:(Option.map
                                    ~f:Values.MaintenanceWindow.of_json
                                    maintenanceWindow)
              ?softwareSetUpdateMode:(Option.map
                                        ~f:Values.SoftwareSetUpdateMode.of_json
                                        softwareSetUpdateMode)
              ?desiredSoftwareSetId
              ?deviceCreationTags:(Option.map
                                     ~f:Values.DeviceCreationTagsMap.of_json
                                     deviceCreationTags) ~id ())
           (Some Values.UpdateEnvironmentResponse.to_json)
           (Some Values.UpdateEnvironmentResponse.error_to_json)])
let update_software_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING SoftwareSetId"
       and validationStatus =
         flag "validation-status" (required json_arg)
           ~doc:"JSON SoftwareSetValidationStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_software_set
           (Values.UpdateSoftwareSetRequest.make ~id
              ~validationStatus:(Values.SoftwareSetValidationStatus.of_json
                                   validationStatus) ())
           (Some Values.UpdateSoftwareSetResponse.to_json)
           (Some Values.UpdateSoftwareSetResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-environment", create_environment);
    ("delete-device", delete_device);
    ("delete-environment", delete_environment);
    ("deregister-device", deregister_device);
    ("get-device", get_device);
    ("get-environment", get_environment);
    ("get-software-set", get_software_set);
    ("list-devices", list_devices);
    ("list-environments", list_environments);
    ("list-software-sets", list_software_sets);
    ("list-tags-for-resource", list_tags_for_resource);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-device", update_device);
    ("update-environment", update_environment);
    ("update-software-set", update_software_set)]
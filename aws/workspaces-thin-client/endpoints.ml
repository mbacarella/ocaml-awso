(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | CreateEnvironment: (CreateEnvironmentRequest.t,
  CreateEnvironmentResponse.t, CreateEnvironmentResponse.error) t 
  | DeleteDevice: (DeleteDeviceRequest.t, DeleteDeviceResponse.t,
  DeleteDeviceResponse.error) t 
  | DeleteEnvironment: (DeleteEnvironmentRequest.t,
  DeleteEnvironmentResponse.t, DeleteEnvironmentResponse.error) t 
  | DeregisterDevice: (DeregisterDeviceRequest.t, DeregisterDeviceResponse.t,
  DeregisterDeviceResponse.error) t 
  | GetDevice: (GetDeviceRequest.t, GetDeviceResponse.t,
  GetDeviceResponse.error) t 
  | GetEnvironment: (GetEnvironmentRequest.t, GetEnvironmentResponse.t,
  GetEnvironmentResponse.error) t 
  | GetSoftwareSet: (GetSoftwareSetRequest.t, GetSoftwareSetResponse.t,
  GetSoftwareSetResponse.error) t 
  | ListDevices: (ListDevicesRequest.t, ListDevicesResponse.t,
  ListDevicesResponse.error) t 
  | ListEnvironments: (ListEnvironmentsRequest.t, ListEnvironmentsResponse.t,
  ListEnvironmentsResponse.error) t 
  | ListSoftwareSets: (ListSoftwareSetsRequest.t, ListSoftwareSetsResponse.t,
  ListSoftwareSetsResponse.error) t 
  | ListTagsForResource: (ListTagsForResourceRequest.t,
  ListTagsForResourceResponse.t, ListTagsForResourceResponse.error) t 
  | TagResource: (TagResourceRequest.t, TagResourceResponse.t,
  TagResourceResponse.error) t 
  | UntagResource: (UntagResourceRequest.t, UntagResourceResponse.t,
  UntagResourceResponse.error) t 
  | UpdateDevice: (UpdateDeviceRequest.t, UpdateDeviceResponse.t,
  UpdateDeviceResponse.error) t 
  | UpdateEnvironment: (UpdateEnvironmentRequest.t,
  UpdateEnvironmentResponse.t, UpdateEnvironmentResponse.error) t 
  | UpdateSoftwareSet: (UpdateSoftwareSetRequest.t,
  UpdateSoftwareSetResponse.t, UpdateSoftwareSetResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | CreateEnvironment -> `POST
  | DeleteDevice -> `DELETE
  | DeleteEnvironment -> `DELETE
  | DeregisterDevice -> `POST
  | GetDevice -> `GET
  | GetEnvironment -> `GET
  | GetSoftwareSet -> `GET
  | ListDevices -> `GET
  | ListEnvironments -> `GET
  | ListSoftwareSets -> `GET
  | ListTagsForResource -> `GET
  | TagResource -> `POST
  | UntagResource -> `DELETE
  | UpdateDevice -> `PATCH
  | UpdateEnvironment -> `PATCH
  | UpdateSoftwareSet -> `PATCH
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | CreateEnvironment -> (Format.kasprintf Uri.of_string) "/environments"
      | DeleteDevice ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/devices/%s"
               (DeviceId.to_header x.DeleteDeviceRequest.id))
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("clientToken", (ClientToken.to_header v)))
                  x.clientToken])
      | DeleteEnvironment ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/environments/%s"
               (EnvironmentId.to_header x.DeleteEnvironmentRequest.id))
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("clientToken", (ClientToken.to_header v)))
                  x.clientToken])
      | DeregisterDevice ->
          (Format.kasprintf Uri.of_string) "/deregister-device/%s"
            (DeviceId.to_header x.DeregisterDeviceRequest.id)
      | GetDevice ->
          (Format.kasprintf Uri.of_string) "/devices/%s"
            (DeviceId.to_header x.GetDeviceRequest.id)
      | GetEnvironment ->
          (Format.kasprintf Uri.of_string) "/environments/%s"
            (EnvironmentId.to_header x.GetEnvironmentRequest.id)
      | GetSoftwareSet ->
          (Format.kasprintf Uri.of_string) "/softwaresets/%s"
            (SoftwareSetId.to_header x.GetSoftwareSetRequest.id)
      | ListDevices ->
          Uri.add_query_params' ((Format.kasprintf Uri.of_string) "/devices")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("nextToken", (PaginationToken.to_header v)))
                  x.nextToken;
               Option.map
                 ~f:(fun v -> ("maxResults", (MaxResults.to_header v)))
                 x.maxResults])
      | ListEnvironments ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/environments")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("nextToken", (PaginationToken.to_header v)))
                  x.nextToken;
               Option.map
                 ~f:(fun v -> ("maxResults", (MaxResults.to_header v)))
                 x.maxResults])
      | ListSoftwareSets ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/softwaresets")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("nextToken", (PaginationToken.to_header v)))
                  x.nextToken;
               Option.map
                 ~f:(fun v -> ("maxResults", (MaxResults.to_header v)))
                 x.maxResults])
      | ListTagsForResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (String_.to_header x.ListTagsForResourceRequest.resourceArn)
      | TagResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (String_.to_header x.TagResourceRequest.resourceArn)
      | UntagResource ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/tags/%s"
               (String_.to_header x.UntagResourceRequest.resourceArn))
            (List.filter_opt
               [Some ("tagKeys", (TagKeys.to_header x.tagKeys))])
      | UpdateDevice ->
          (Format.kasprintf Uri.of_string) "/devices/%s"
            (DeviceId.to_header x.UpdateDeviceRequest.id)
      | UpdateEnvironment ->
          (Format.kasprintf Uri.of_string) "/environments/%s"
            (EnvironmentId.to_header x.UpdateEnvironmentRequest.id)
      | UpdateSoftwareSet ->
          (Format.kasprintf Uri.of_string) "/softwaresets/%s"
            (SoftwareSetId.to_header x.UpdateSoftwareSetRequest.id))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | CreateEnvironment ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.CreateEnvironmentRequest.name
                         ~f:(fun x -> ("name", (EnvironmentName.to_value x)));
                      Some
                        ("desktopArn",
                          (Arn.to_value
                             req.CreateEnvironmentRequest.desktopArn));
                      Option.map req.CreateEnvironmentRequest.desktopEndpoint
                        ~f:(fun x ->
                              ("desktopEndpoint",
                                (DesktopEndpoint.to_value x)));
                      Option.map
                        req.CreateEnvironmentRequest.softwareSetUpdateSchedule
                        ~f:(fun x ->
                              ("softwareSetUpdateSchedule",
                                (SoftwareSetUpdateSchedule.to_value x)));
                      Option.map
                        req.CreateEnvironmentRequest.maintenanceWindow
                        ~f:(fun x ->
                              ("maintenanceWindow",
                                (MaintenanceWindow.to_value x)));
                      Option.map
                        req.CreateEnvironmentRequest.softwareSetUpdateMode
                        ~f:(fun x ->
                              ("softwareSetUpdateMode",
                                (SoftwareSetUpdateMode.to_value x)));
                      Option.map
                        req.CreateEnvironmentRequest.desiredSoftwareSetId
                        ~f:(fun x ->
                              ("desiredSoftwareSetId",
                                (SoftwareSetId.to_value x)));
                      Option.map req.CreateEnvironmentRequest.kmsKeyArn
                        ~f:(fun x -> ("kmsKeyArn", (KmsKeyArn.to_value x)));
                      Option.map req.CreateEnvironmentRequest.clientToken
                        ~f:(fun x ->
                              ("clientToken", (ClientToken.to_value x)));
                      Option.map req.CreateEnvironmentRequest.tags
                        ~f:(fun x -> ("tags", (TagsMap.to_value x)));
                      Option.map
                        req.CreateEnvironmentRequest.deviceCreationTags
                        ~f:(fun x ->
                              ("deviceCreationTags",
                                (DeviceCreationTagsMap.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteDevice -> Awso.Http.Request.make (method_of_endpoint endp)
  | DeleteEnvironment -> Awso.Http.Request.make (method_of_endpoint endp)
  | DeregisterDevice ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.DeregisterDeviceRequest.targetDeviceStatus
                         ~f:(fun x ->
                               ("targetDeviceStatus",
                                 (TargetDeviceStatus.to_value x)));
                      Option.map req.DeregisterDeviceRequest.clientToken
                        ~f:(fun x ->
                              ("clientToken", (ClientToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetDevice ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetEnvironment ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetSoftwareSet ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListDevices ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListEnvironments ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListSoftwareSets ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTagsForResource ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | TagResource ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("tags",
                           (TagsMap.to_value req.TagResourceRequest.tags))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UntagResource -> Awso.Http.Request.make (method_of_endpoint endp)
  | UpdateDevice -> Awso.Http.Request.make (method_of_endpoint endp)
  | UpdateEnvironment -> Awso.Http.Request.make (method_of_endpoint endp)
  | UpdateSoftwareSet -> Awso.Http.Request.make (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | CreateEnvironment ->
      if is_success
      then Ok (CreateEnvironmentResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some CreateEnvironmentResponse.error_of_json))
  | DeleteDevice ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DeleteDeviceResponse.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some DeleteDeviceResponse.error_of_json))
  | DeleteEnvironment ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DeleteEnvironmentResponse.of_header_and_body (headers, ()))
      else
        Error
          (parse_aws_error (Some DeleteEnvironmentResponse.error_of_json))
  | DeregisterDevice ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DeregisterDeviceResponse.of_header_and_body (headers, ()))
      else
        Error (parse_aws_error (Some DeregisterDeviceResponse.error_of_json))
  | GetDevice ->
      if is_success
      then Ok (GetDeviceResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetDeviceResponse.error_of_json))
  | GetEnvironment ->
      if is_success
      then Ok (GetEnvironmentResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some GetEnvironmentResponse.error_of_json))
  | GetSoftwareSet ->
      if is_success
      then Ok (GetSoftwareSetResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some GetSoftwareSetResponse.error_of_json))
  | ListDevices ->
      if is_success
      then Ok (ListDevicesResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListDevicesResponse.error_of_json))
  | ListEnvironments ->
      if is_success
      then Ok (ListEnvironmentsResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some ListEnvironmentsResponse.error_of_json))
  | ListSoftwareSets ->
      if is_success
      then Ok (ListSoftwareSetsResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some ListSoftwareSetsResponse.error_of_json))
  | ListTagsForResource ->
      if is_success
      then Ok (ListTagsForResourceResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListTagsForResourceResponse.error_of_json))
  | TagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (TagResourceResponse.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some TagResourceResponse.error_of_json))
  | UntagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (UntagResourceResponse.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some UntagResourceResponse.error_of_json))
  | UpdateDevice ->
      if is_success
      then Ok (UpdateDeviceResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some UpdateDeviceResponse.error_of_json))
  | UpdateEnvironment ->
      if is_success
      then Ok (UpdateEnvironmentResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some UpdateEnvironmentResponse.error_of_json))
  | UpdateSoftwareSet ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (UpdateSoftwareSetResponse.of_header_and_body (headers, ()))
      else
        Error
          (parse_aws_error (Some UpdateSoftwareSetResponse.error_of_json))
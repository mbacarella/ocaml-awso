(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.lex_runtime
let apiVersion = "2016-11-28"
let endpointPrefix = "runtime.lex"
let serviceFullName = "Amazon Lex Runtime Service"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module ButtonTextStringWithLength =
  struct
    type nonrec t = string
    let context_ = "ButtonTextStringWithLength"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:15) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ButtonTextStringWithLength" j
    let to_json = simple_to_json to_value
  end
module ButtonValueStringWithLength =
  struct
    type nonrec t = string
    let context_ = "ButtonValueStringWithLength"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ButtonValueStringWithLength" j
    let to_json = simple_to_json to_value
  end
module Button =
  struct
    type nonrec t =
      {
      text: ButtonTextStringWithLength.t option
        [@ocaml.doc "Text that is visible to the user on the button."];
      value: ButtonValueStringWithLength.t option
        [@ocaml.doc
          "The value sent to Amazon Lex when a user chooses the button. For example, consider button text \"NYC.\" When the user chooses the button, the value sent can be \"New York City.\""]}
    let make ?text = fun ?value -> fun () -> { text; value }
    let to_value x =
      structure_to_value
        [("text", (Option.map x.text ~f:ButtonTextStringWithLength.to_value));
        ("value",
          (Option.map x.value ~f:ButtonValueStringWithLength.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:ButtonValueStringWithLength.of_xml)
          (Xml.child xml_arg0 "value") in
      let text =
        (Option.map ~f:ButtonTextStringWithLength.of_xml)
          (Xml.child xml_arg0 "text") in
      make ?value ?text ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let value =
        field_map json__ "value" ButtonValueStringWithLength.of_json in
      let text = field_map json__ "text" ButtonTextStringWithLength.of_json in
      make ?value ?text ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents an option to be shown on the client platform (Facebook, Slack, etc.)"]
module ParameterName =
  struct
    type nonrec t = string
    let context_ = "ParameterName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:100) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ParameterName" j
    let to_json = simple_to_json to_value
  end
module Text =
  struct
    type nonrec t = string
    let context_ = "Text"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Text" j
    let to_json = simple_to_json to_value
  end
module ActiveContextTimeToLiveInSeconds =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:86400) >>=
             (fun () -> check_int_min i ~min:5));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for ActiveContextTimeToLiveInSeconds" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ActiveContextTurnsToLive =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:20) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ActiveContextTurnsToLive"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module Double =
  struct
    type nonrec t = float
    let make i = i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module StringUrlWithLength =
  struct
    type nonrec t = string
    let context_ = "StringUrlWithLength"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2048) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StringUrlWithLength" j
    let to_json = simple_to_json to_value
  end
module StringWithLength =
  struct
    type nonrec t = string
    let context_ = "StringWithLength"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:80) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"StringWithLength" j
    let to_json = simple_to_json to_value
  end
module ListOfButtons =
  struct
    type nonrec t = Button.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:5) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Button.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Button.of_xml)
    let of_json j =
      list_of_json ~kind:"listOfButtons" ~of_json:Button.of_json j
    let to_json v = composed_to_json to_value v
  end
module ActiveContextName =
  struct
    type nonrec t = string
    let context_ = "ActiveContextName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () -> check_pattern i ~pattern:"^([A-Za-z]_?)+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ActiveContextName" j
    let to_json = simple_to_json to_value
  end
module ActiveContextParametersMap =
  struct
    type nonrec t = (ParameterName.t * Text.t) list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((ParameterName.of_string chopped),
                              (Text.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (ParameterName.to_value x) |>
                    (fun x -> (Text.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:ParameterName.of_string
        ~of_json:Text.of_json j
    let to_json v = composed_to_json to_value v
  end
module ActiveContextTimeToLive =
  struct
    type nonrec t =
      {
      timeToLiveInSeconds: ActiveContextTimeToLiveInSeconds.t option
        [@ocaml.doc
          "The number of seconds that the context should be active after it is first sent in a PostContent or PostText response. You can set the value between 5 and 86,400 seconds (24 hours)."];
      turnsToLive: ActiveContextTurnsToLive.t option
        [@ocaml.doc
          "The number of conversation turns that the context should be active. A conversation turn is one PostContent or PostText request and the corresponding response from Amazon Lex."]}
    let make ?timeToLiveInSeconds =
      fun ?turnsToLive -> fun () -> { timeToLiveInSeconds; turnsToLive }
    let to_value x =
      structure_to_value
        [("timeToLiveInSeconds",
           (Option.map x.timeToLiveInSeconds
              ~f:ActiveContextTimeToLiveInSeconds.to_value));
        ("turnsToLive",
          (Option.map x.turnsToLive ~f:ActiveContextTurnsToLive.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let turnsToLive =
        (Option.map ~f:ActiveContextTurnsToLive.of_xml)
          (Xml.child xml_arg0 "turnsToLive") in
      let timeToLiveInSeconds =
        (Option.map ~f:ActiveContextTimeToLiveInSeconds.of_xml)
          (Xml.child xml_arg0 "timeToLiveInSeconds") in
      make ?turnsToLive ?timeToLiveInSeconds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let turnsToLive =
        field_map json__ "turnsToLive" ActiveContextTurnsToLive.of_json in
      let timeToLiveInSeconds =
        field_map json__ "timeToLiveInSeconds"
          ActiveContextTimeToLiveInSeconds.of_json in
      make ?turnsToLive ?timeToLiveInSeconds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The length of time or number of turns that a context remains active."]
module ConfirmationStatus =
  struct
    type nonrec t =
      | None 
      | Confirmed 
      | Denied 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | None -> "None"
      | Confirmed -> "Confirmed"
      | Denied -> "Denied"
      | Non_static_id s -> s
    let of_string =
      function
      | "None" -> None
      | "Confirmed" -> Confirmed
      | "Denied" -> Denied
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ConfirmationStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ConfirmationStatus" j)
    let to_json = simple_to_json to_value
  end
module DialogActionType =
  struct
    type nonrec t =
      | ElicitIntent 
      | ConfirmIntent 
      | ElicitSlot 
      | Close 
      | Delegate 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ElicitIntent -> "ElicitIntent"
      | ConfirmIntent -> "ConfirmIntent"
      | ElicitSlot -> "ElicitSlot"
      | Close -> "Close"
      | Delegate -> "Delegate"
      | Non_static_id s -> s
    let of_string =
      function
      | "ElicitIntent" -> ElicitIntent
      | "ConfirmIntent" -> ConfirmIntent
      | "ElicitSlot" -> ElicitSlot
      | "Close" -> Close
      | "Delegate" -> Delegate
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration DialogActionType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"DialogActionType" j)
    let to_json = simple_to_json to_value
  end
module FulfillmentState =
  struct
    type nonrec t =
      | Fulfilled 
      | Failed 
      | ReadyForFulfillment 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Fulfilled -> "Fulfilled"
      | Failed -> "Failed"
      | ReadyForFulfillment -> "ReadyForFulfillment"
      | Non_static_id s -> s
    let of_string =
      function
      | "Fulfilled" -> Fulfilled
      | "Failed" -> Failed
      | "ReadyForFulfillment" -> ReadyForFulfillment
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration FulfillmentState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"FulfillmentState" j)
    let to_json = simple_to_json to_value
  end
module IntentName =
  struct
    type nonrec t = string
    let context_ = "IntentName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"IntentName" j
    let to_json = simple_to_json to_value
  end
module IntentSummaryCheckpointLabel =
  struct
    type nonrec t = string
    let context_ = "IntentSummaryCheckpointLabel"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:255) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"IntentSummaryCheckpointLabel" j
    let to_json = simple_to_json to_value
  end
module StringMap =
  struct
    type nonrec t = (String_.t * String_.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((String_.of_string chopped),
                              (String_.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x -> (String_.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for Map_shape objects" ()
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module IntentConfidence =
  struct
    type nonrec t =
      {
      score: Double.t option
        [@ocaml.doc
          "A score that indicates how confident Amazon Lex is that an intent satisfies the user's intent. Ranges between 0.00 and 1.00. Higher scores indicate higher confidence."]}
    let make ?score = fun () -> { score }
    let to_value x =
      structure_to_value [("score", (Option.map x.score ~f:Double.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let score = (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "score") in
      make ?score ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let score = field_map json__ "score" Double.of_json in make ?score ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides a score that indicates the confidence that Amazon Lex has that an intent is the one that satisfies the user's intent."]
module GenericAttachment =
  struct
    type nonrec t =
      {
      title: StringWithLength.t option
        [@ocaml.doc "The title of the option."];
      subTitle: StringWithLength.t option
        [@ocaml.doc "The subtitle shown below the title."];
      attachmentLinkUrl: StringUrlWithLength.t option
        [@ocaml.doc "The URL of an attachment to the response card."];
      imageUrl: StringUrlWithLength.t option
        [@ocaml.doc "The URL of an image that is displayed to the user."];
      buttons: ListOfButtons.t option
        [@ocaml.doc "The list of options to show to the user."]}
    let make ?title =
      fun ?subTitle ->
        fun ?attachmentLinkUrl ->
          fun ?imageUrl ->
            fun ?buttons ->
              fun () ->
                { title; subTitle; attachmentLinkUrl; imageUrl; buttons }
    let to_value x =
      structure_to_value
        [("title", (Option.map x.title ~f:StringWithLength.to_value));
        ("subTitle", (Option.map x.subTitle ~f:StringWithLength.to_value));
        ("attachmentLinkUrl",
          (Option.map x.attachmentLinkUrl ~f:StringUrlWithLength.to_value));
        ("imageUrl", (Option.map x.imageUrl ~f:StringUrlWithLength.to_value));
        ("buttons", (Option.map x.buttons ~f:ListOfButtons.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let buttons =
        (Option.map ~f:ListOfButtons.of_xml) (Xml.child xml_arg0 "buttons") in
      let imageUrl =
        (Option.map ~f:StringUrlWithLength.of_xml)
          (Xml.child xml_arg0 "imageUrl") in
      let attachmentLinkUrl =
        (Option.map ~f:StringUrlWithLength.of_xml)
          (Xml.child xml_arg0 "attachmentLinkUrl") in
      let subTitle =
        (Option.map ~f:StringWithLength.of_xml)
          (Xml.child xml_arg0 "subTitle") in
      let title =
        (Option.map ~f:StringWithLength.of_xml) (Xml.child xml_arg0 "title") in
      make ?buttons ?imageUrl ?attachmentLinkUrl ?subTitle ?title ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let buttons = field_map json__ "buttons" ListOfButtons.of_json in
      let imageUrl = field_map json__ "imageUrl" StringUrlWithLength.of_json in
      let attachmentLinkUrl =
        field_map json__ "attachmentLinkUrl" StringUrlWithLength.of_json in
      let subTitle = field_map json__ "subTitle" StringWithLength.of_json in
      let title = field_map json__ "title" StringWithLength.of_json in
      make ?buttons ?imageUrl ?attachmentLinkUrl ?subTitle ?title ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents an option rendered to the user when a prompt is shown. It could be an image, a button, a link, or text."]
module ErrorMessage =
  struct
    type nonrec t = string
    let context_ = "ErrorMessage"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorMessage" j
    let to_json = simple_to_json to_value
  end
module ActiveContext =
  struct
    type nonrec t =
      {
      name: ActiveContextName.t [@ocaml.doc "The name of the context."];
      timeToLive: ActiveContextTimeToLive.t
        [@ocaml.doc
          "The length of time or number of turns that a context remains active."];
      parameters: ActiveContextParametersMap.t
        [@ocaml.doc
          "State variables for the current context. You can use these values as default values for slots in subsequent events."]}
    let context_ = "ActiveContext"
    let make ~name =
      fun ~timeToLive ->
        fun ~parameters -> fun () -> { name; timeToLive; parameters }
    let to_value x =
      structure_to_value
        [("name", (Some (ActiveContextName.to_value x.name)));
        ("timeToLive",
          (Some (ActiveContextTimeToLive.to_value x.timeToLive)));
        ("parameters",
          (Some (ActiveContextParametersMap.to_value x.parameters)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let parameters =
        ActiveContextParametersMap.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "parameters") in
      let timeToLive =
        ActiveContextTimeToLive.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "timeToLive") in
      let name =
        ActiveContextName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~parameters ~timeToLive ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let parameters =
        field_map_exn json__ "parameters" ActiveContextParametersMap.of_json in
      let timeToLive =
        field_map_exn json__ "timeToLive" ActiveContextTimeToLive.of_json in
      let name = field_map_exn json__ "name" ActiveContextName.of_json in
      make ~parameters ~timeToLive ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A context is a variable that contains information about the current state of the conversation between a user and Amazon Lex. Context can be set automatically by Amazon Lex when an intent is fulfilled, or it can be set at runtime using the PutContent, PutText, or PutSession operation."]
module MessageFormatType =
  struct
    type nonrec t =
      | PlainText 
      | CustomPayload 
      | SSML 
      | Composite 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PlainText -> "PlainText"
      | CustomPayload -> "CustomPayload"
      | SSML -> "SSML"
      | Composite -> "Composite"
      | Non_static_id s -> s
    let of_string =
      function
      | "PlainText" -> PlainText
      | "CustomPayload" -> CustomPayload
      | "SSML" -> SSML
      | "Composite" -> Composite
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration MessageFormatType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"MessageFormatType" j)
    let to_json = simple_to_json to_value
  end
module IntentSummary =
  struct
    type nonrec t =
      {
      intentName: IntentName.t option [@ocaml.doc "The name of the intent."];
      checkpointLabel: IntentSummaryCheckpointLabel.t option
        [@ocaml.doc
          "A user-defined label that identifies a particular intent. You can use this label to return to a previous intent. Use the checkpointLabelFilter parameter of the GetSessionRequest operation to filter the intents returned by the operation to those with only the specified label."];
      slots: StringMap.t option
        [@ocaml.doc
          "Map of the slots that have been gathered and their values."];
      confirmationStatus: ConfirmationStatus.t option
        [@ocaml.doc
          "The status of the intent after the user responds to the confirmation prompt. If the user confirms the intent, Amazon Lex sets this field to Confirmed. If the user denies the intent, Amazon Lex sets this value to Denied. The possible values are: Confirmed - The user has responded \"Yes\" to the confirmation prompt, confirming that the intent is complete and that it is ready to be fulfilled. Denied - The user has responded \"No\" to the confirmation prompt. None - The user has never been prompted for confirmation; or, the user was prompted but did not confirm or deny the prompt."];
      dialogActionType: DialogActionType.t
        [@ocaml.doc
          "The next action that the bot should take in its interaction with the user. The possible values are: ConfirmIntent - The next action is asking the user if the intent is complete and ready to be fulfilled. This is a yes/no question such as \"Place the order?\" Close - Indicates that the there will not be a response from the user. For example, the statement \"Your order has been placed\" does not require a response. ElicitIntent - The next action is to determine the intent that the user wants to fulfill. ElicitSlot - The next action is to elicit a slot value from the user."];
      fulfillmentState: FulfillmentState.t option
        [@ocaml.doc
          "The fulfillment state of the intent. The possible values are: Failed - The Lambda function associated with the intent failed to fulfill the intent. Fulfilled - The intent has fulfilled by the Lambda function associated with the intent. ReadyForFulfillment - All of the information necessary for the intent is present and the intent ready to be fulfilled by the client application."];
      slotToElicit: String_.t option
        [@ocaml.doc
          "The next slot to elicit from the user. If there is not slot to elicit, the field is blank."]}
    let context_ = "IntentSummary"
    let make ?intentName =
      fun ?checkpointLabel ->
        fun ?slots ->
          fun ?confirmationStatus ->
            fun ?fulfillmentState ->
              fun ?slotToElicit ->
                fun ~dialogActionType ->
                  fun () ->
                    {
                      intentName;
                      checkpointLabel;
                      slots;
                      confirmationStatus;
                      fulfillmentState;
                      slotToElicit;
                      dialogActionType
                    }
    let to_value x =
      structure_to_value
        [("intentName", (Option.map x.intentName ~f:IntentName.to_value));
        ("checkpointLabel",
          (Option.map x.checkpointLabel
             ~f:IntentSummaryCheckpointLabel.to_value));
        ("slots", (Option.map x.slots ~f:StringMap.to_value));
        ("confirmationStatus",
          (Option.map x.confirmationStatus ~f:ConfirmationStatus.to_value));
        ("dialogActionType",
          (Some (DialogActionType.to_value x.dialogActionType)));
        ("fulfillmentState",
          (Option.map x.fulfillmentState ~f:FulfillmentState.to_value));
        ("slotToElicit", (Option.map x.slotToElicit ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let slotToElicit =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "slotToElicit") in
      let fulfillmentState =
        (Option.map ~f:FulfillmentState.of_xml)
          (Xml.child xml_arg0 "fulfillmentState") in
      let dialogActionType =
        DialogActionType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "dialogActionType") in
      let confirmationStatus =
        (Option.map ~f:ConfirmationStatus.of_xml)
          (Xml.child xml_arg0 "confirmationStatus") in
      let slots =
        (Option.map ~f:StringMap.of_xml) (Xml.child xml_arg0 "slots") in
      let checkpointLabel =
        (Option.map ~f:IntentSummaryCheckpointLabel.of_xml)
          (Xml.child xml_arg0 "checkpointLabel") in
      let intentName =
        (Option.map ~f:IntentName.of_xml) (Xml.child xml_arg0 "intentName") in
      make ?slotToElicit ?fulfillmentState ~dialogActionType
        ?confirmationStatus ?slots ?checkpointLabel ?intentName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let slotToElicit = field_map json__ "slotToElicit" String_.of_json in
      let fulfillmentState =
        field_map json__ "fulfillmentState" FulfillmentState.of_json in
      let dialogActionType =
        field_map_exn json__ "dialogActionType" DialogActionType.of_json in
      let confirmationStatus =
        field_map json__ "confirmationStatus" ConfirmationStatus.of_json in
      let slots = field_map json__ "slots" StringMap.of_json in
      let checkpointLabel =
        field_map json__ "checkpointLabel"
          IntentSummaryCheckpointLabel.of_json in
      let intentName = field_map json__ "intentName" IntentName.of_json in
      make ?slotToElicit ?fulfillmentState ~dialogActionType
        ?confirmationStatus ?slots ?checkpointLabel ?intentName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides information about the state of an intent. You can use this information to get the current state of an intent so that you can process the intent, or so that you can return the intent to its previous state."]
module PredictedIntent =
  struct
    type nonrec t =
      {
      intentName: IntentName.t option
        [@ocaml.doc
          "The name of the intent that Amazon Lex suggests satisfies the user's intent."];
      nluIntentConfidence: IntentConfidence.t option
        [@ocaml.doc
          "Indicates how confident Amazon Lex is that an intent satisfies the user's intent."];
      slots: StringMap.t option
        [@ocaml.doc
          "The slot and slot values associated with the predicted intent."]}
    let make ?intentName =
      fun ?nluIntentConfidence ->
        fun ?slots -> fun () -> { intentName; nluIntentConfidence; slots }
    let to_value x =
      structure_to_value
        [("intentName", (Option.map x.intentName ~f:IntentName.to_value));
        ("nluIntentConfidence",
          (Option.map x.nluIntentConfidence ~f:IntentConfidence.to_value));
        ("slots", (Option.map x.slots ~f:StringMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let slots =
        (Option.map ~f:StringMap.of_xml) (Xml.child xml_arg0 "slots") in
      let nluIntentConfidence =
        (Option.map ~f:IntentConfidence.of_xml)
          (Xml.child xml_arg0 "nluIntentConfidence") in
      let intentName =
        (Option.map ~f:IntentName.of_xml) (Xml.child xml_arg0 "intentName") in
      make ?slots ?nluIntentConfidence ?intentName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let slots = field_map json__ "slots" StringMap.of_json in
      let nluIntentConfidence =
        field_map json__ "nluIntentConfidence" IntentConfidence.of_json in
      let intentName = field_map json__ "intentName" IntentName.of_json in
      make ?slots ?nluIntentConfidence ?intentName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An intent that Amazon Lex suggests satisfies the user's intent. Includes the name of the intent, the confidence that Amazon Lex has that the user's intent is satisfied, and the slots defined for the intent."]
module ContentType =
  struct
    type nonrec t =
      | Application_vnd_amazonaws_card_generic 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Application_vnd_amazonaws_card_generic ->
          "application/vnd.amazonaws.card.generic"
      | Non_static_id s -> s
    let of_string =
      function
      | "application/vnd.amazonaws.card.generic" ->
          Application_vnd_amazonaws_card_generic
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ContentType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ContentType" j)
    let to_json = simple_to_json to_value
  end
module GenericAttachmentList =
  struct
    type nonrec t = GenericAttachment.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GenericAttachment.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GenericAttachment.of_xml)
    let of_json j =
      list_of_json ~kind:"genericAttachmentList"
        ~of_json:GenericAttachment.of_json j
    let to_json v = composed_to_json to_value v
  end
module SentimentLabel =
  struct
    type nonrec t = string
    let context_ = "SentimentLabel"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SentimentLabel" j
    let to_json = simple_to_json to_value
  end
module SentimentScore =
  struct
    type nonrec t = string
    let context_ = "SentimentScore"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SentimentScore" j
    let to_json = simple_to_json to_value
  end
module ActiveContextsString =
  struct
    type nonrec t = string
    let context_ = "ActiveContextsString"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ActiveContextsString" j
    let to_json = simple_to_json to_value
  end
module BadGatewayException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Either the Amazon Lex bot is still building, or one of the dependent services (Amazon Polly, AWS Lambda) failed with an internal service error."]
module BadRequestException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Request validation failed, there is no usable message in the context, or the bot build failed, is still in progress, or contains unbuilt changes."]
module BlobStream =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module ConflictException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Two clients are using the same AWS account, Amazon Lex bot, and user ID."]
module DependencyFailedException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "One of the dependencies, such as AWS Lambda or Amazon Polly, threw an exception. For example, If Amazon Lex does not have sufficient permissions to call a Lambda function. If a Lambda function takes longer than 30 seconds to execute. If a fulfillment Lambda function returns a Delegate dialog action without removing any slot values."]
module DialogState =
  struct
    type nonrec t =
      | ElicitIntent 
      | ConfirmIntent 
      | ElicitSlot 
      | Fulfilled 
      | ReadyForFulfillment 
      | Failed 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ElicitIntent -> "ElicitIntent"
      | ConfirmIntent -> "ConfirmIntent"
      | ElicitSlot -> "ElicitSlot"
      | Fulfilled -> "Fulfilled"
      | ReadyForFulfillment -> "ReadyForFulfillment"
      | Failed -> "Failed"
      | Non_static_id s -> s
    let of_string =
      function
      | "ElicitIntent" -> ElicitIntent
      | "ConfirmIntent" -> ConfirmIntent
      | "ElicitSlot" -> ElicitSlot
      | "Fulfilled" -> Fulfilled
      | "ReadyForFulfillment" -> ReadyForFulfillment
      | "Failed" -> Failed
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration DialogState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"DialogState" j)
    let to_json = simple_to_json to_value
  end
module HttpContentType =
  struct
    type nonrec t = string
    let context_ = "HttpContentType"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"HttpContentType" j
    let to_json = simple_to_json to_value
  end
module InternalFailureException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Internal service error. Retry the call."]
module LimitExceededException =
  struct
    type nonrec t =
      {
      retryAfterSeconds: String_.t option ;
      message: String_.t option }
    let make ?retryAfterSeconds =
      fun ?message -> fun () -> { retryAfterSeconds; message }
    let to_value x =
      structure_to_value
        [("Retry-After",
           (Option.map x.retryAfterSeconds ~f:String_.to_value));
        ("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let retryAfterSeconds =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Retry-After") in
      make ?message ?retryAfterSeconds ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      let retryAfterSeconds =
        field_map json__ "retryAfterSeconds" String_.of_json in
      make ?message ?retryAfterSeconds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Exceeded a limit."]
module NotAcceptableException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The accept header in the request does not have a valid value."]
module NotFoundException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The resource (such as the Amazon Lex bot or an alias) that is referred to is not found."]
module SensitiveString =
  struct
    type nonrec t = string
    let context_ = "SensitiveString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1366) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SensitiveString" j
    let to_json = simple_to_json to_value
  end
module Accept =
  struct
    type nonrec t = string
    let context_ = "Accept"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Accept" j
    let to_json = simple_to_json to_value
  end
module ActiveContextsList =
  struct
    type nonrec t = ActiveContext.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:20) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ActiveContext.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ActiveContext.of_xml)
    let of_json j =
      list_of_json ~kind:"ActiveContextsList" ~of_json:ActiveContext.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module BotAlias =
  struct
    type nonrec t = string
    let context_ = "BotAlias"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BotAlias" j
    let to_json = simple_to_json to_value
  end
module BotName =
  struct
    type nonrec t = string
    let context_ = "BotName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BotName" j
    let to_json = simple_to_json to_value
  end
module DialogAction =
  struct
    type nonrec t =
      {
      type_: DialogActionType.t
        [@ocaml.doc
          "The next action that the bot should take in its interaction with the user. The possible values are: ConfirmIntent - The next action is asking the user if the intent is complete and ready to be fulfilled. This is a yes/no question such as \"Place the order?\" Close - Indicates that the there will not be a response from the user. For example, the statement \"Your order has been placed\" does not require a response. Delegate - The next action is determined by Amazon Lex. ElicitIntent - The next action is to determine the intent that the user wants to fulfill. ElicitSlot - The next action is to elicit a slot value from the user."];
      intentName: IntentName.t option [@ocaml.doc "The name of the intent."];
      slots: StringMap.t option
        [@ocaml.doc
          "Map of the slots that have been gathered and their values."];
      slotToElicit: String_.t option
        [@ocaml.doc
          "The name of the slot that should be elicited from the user."];
      fulfillmentState: FulfillmentState.t option
        [@ocaml.doc
          "The fulfillment state of the intent. The possible values are: Failed - The Lambda function associated with the intent failed to fulfill the intent. Fulfilled - The intent has fulfilled by the Lambda function associated with the intent. ReadyForFulfillment - All of the information necessary for the intent is present and the intent ready to be fulfilled by the client application."];
      message: Text.t option
        [@ocaml.doc
          "The message that should be shown to the user. If you don't specify a message, Amazon Lex will use the message configured for the intent."];
      messageFormat: MessageFormatType.t option
        [@ocaml.doc
          "PlainText - The message contains plain UTF-8 text. CustomPayload - The message is a custom format for the client. SSML - The message contains text formatted for voice output. Composite - The message contains an escaped JSON object containing one or more messages. For more information, see Message Groups."]}
    let context_ = "DialogAction"
    let make ?intentName =
      fun ?slots ->
        fun ?slotToElicit ->
          fun ?fulfillmentState ->
            fun ?message ->
              fun ?messageFormat ->
                fun ~type_ ->
                  fun () ->
                    {
                      intentName;
                      slots;
                      slotToElicit;
                      fulfillmentState;
                      message;
                      messageFormat;
                      type_
                    }
    let to_value x =
      structure_to_value
        [("type", (Some (DialogActionType.to_value x.type_)));
        ("intentName", (Option.map x.intentName ~f:IntentName.to_value));
        ("slots", (Option.map x.slots ~f:StringMap.to_value));
        ("slotToElicit", (Option.map x.slotToElicit ~f:String_.to_value));
        ("fulfillmentState",
          (Option.map x.fulfillmentState ~f:FulfillmentState.to_value));
        ("message", (Option.map x.message ~f:Text.to_value));
        ("messageFormat",
          (Option.map x.messageFormat ~f:MessageFormatType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let messageFormat =
        (Option.map ~f:MessageFormatType.of_xml)
          (Xml.child xml_arg0 "messageFormat") in
      let message =
        (Option.map ~f:Text.of_xml) (Xml.child xml_arg0 "message") in
      let fulfillmentState =
        (Option.map ~f:FulfillmentState.of_xml)
          (Xml.child xml_arg0 "fulfillmentState") in
      let slotToElicit =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "slotToElicit") in
      let slots =
        (Option.map ~f:StringMap.of_xml) (Xml.child xml_arg0 "slots") in
      let intentName =
        (Option.map ~f:IntentName.of_xml) (Xml.child xml_arg0 "intentName") in
      let type_ =
        DialogActionType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "type") in
      make ?messageFormat ?message ?fulfillmentState ?slotToElicit ?slots
        ?intentName ~type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let messageFormat =
        field_map json__ "messageFormat" MessageFormatType.of_json in
      let message = field_map json__ "message" Text.of_json in
      let fulfillmentState =
        field_map json__ "fulfillmentState" FulfillmentState.of_json in
      let slotToElicit = field_map json__ "slotToElicit" String_.of_json in
      let slots = field_map json__ "slots" StringMap.of_json in
      let intentName = field_map json__ "intentName" IntentName.of_json in
      let type_ = field_map_exn json__ "type" DialogActionType.of_json in
      make ?messageFormat ?message ?fulfillmentState ?slotToElicit ?slots
        ?intentName ~type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes the next action that the bot should take in its interaction with the user and provides information about the context in which the action takes place. Use the DialogAction data type to set the interaction to a specific state, or to return the interaction to a previous state."]
module IntentSummaryList =
  struct
    type nonrec t = IntentSummary.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:3) >>= (fun () -> check_list_min i ~min:0));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:IntentSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:IntentSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"IntentSummaryList" ~of_json:IntentSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module UserId =
  struct
    type nonrec t = string
    let context_ = "UserId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:2) >>=
             (fun () ->
                (check_string_max i ~max:100) >>=
                  (fun () -> check_pattern i ~pattern:"[0-9a-zA-Z._:-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"UserId" j
    let to_json = simple_to_json to_value
  end
module BotVersion =
  struct
    type nonrec t = string
    let context_ = "BotVersion"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"[0-9]+|\\$LATEST")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BotVersion" j
    let to_json = simple_to_json to_value
  end
module IntentList =
  struct
    type nonrec t = PredictedIntent.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:4); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:PredictedIntent.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PredictedIntent.of_xml)
    let of_json j =
      list_of_json ~kind:"IntentList" ~of_json:PredictedIntent.of_json j
    let to_json v = composed_to_json to_value v
  end
module LoopDetectedException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "Message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "This exception is not used."]
module ResponseCard =
  struct
    type nonrec t =
      {
      version: String_.t option
        [@ocaml.doc "The version of the response card format."];
      contentType: ContentType.t option
        [@ocaml.doc "The content type of the response."];
      genericAttachments: GenericAttachmentList.t option
        [@ocaml.doc "An array of attachment objects representing options."]}
    let make ?version =
      fun ?contentType ->
        fun ?genericAttachments ->
          fun () -> { version; contentType; genericAttachments }
    let to_value x =
      structure_to_value
        [("version", (Option.map x.version ~f:String_.to_value));
        ("contentType", (Option.map x.contentType ~f:ContentType.to_value));
        ("genericAttachments",
          (Option.map x.genericAttachments ~f:GenericAttachmentList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let genericAttachments =
        (Option.map ~f:GenericAttachmentList.of_xml)
          (Xml.child xml_arg0 "genericAttachments") in
      let contentType =
        (Option.map ~f:ContentType.of_xml) (Xml.child xml_arg0 "contentType") in
      let version =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "version") in
      make ?genericAttachments ?contentType ?version ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let genericAttachments =
        field_map json__ "genericAttachments" GenericAttachmentList.of_json in
      let contentType = field_map json__ "contentType" ContentType.of_json in
      let version = field_map json__ "version" String_.of_json in
      make ?genericAttachments ?contentType ?version ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "If you configure a response card when creating your bots, Amazon Lex substitutes the session attributes and slot values that are available, and then returns it. The response card can also come from a Lambda function ( dialogCodeHook and fulfillmentActivity on an intent)."]
module SentimentResponse =
  struct
    type nonrec t =
      {
      sentimentLabel: SentimentLabel.t option
        [@ocaml.doc
          "The inferred sentiment that Amazon Comprehend has the highest confidence in."];
      sentimentScore: SentimentScore.t option
        [@ocaml.doc
          "The likelihood that the sentiment was correctly inferred."]}
    let make ?sentimentLabel =
      fun ?sentimentScore -> fun () -> { sentimentLabel; sentimentScore }
    let to_value x =
      structure_to_value
        [("sentimentLabel",
           (Option.map x.sentimentLabel ~f:SentimentLabel.to_value));
        ("sentimentScore",
          (Option.map x.sentimentScore ~f:SentimentScore.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sentimentScore =
        (Option.map ~f:SentimentScore.of_xml)
          (Xml.child xml_arg0 "sentimentScore") in
      let sentimentLabel =
        (Option.map ~f:SentimentLabel.of_xml)
          (Xml.child xml_arg0 "sentimentLabel") in
      make ?sentimentScore ?sentimentLabel ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sentimentScore =
        field_map json__ "sentimentScore" SentimentScore.of_json in
      let sentimentLabel =
        field_map json__ "sentimentLabel" SentimentLabel.of_json in
      make ?sentimentScore ?sentimentLabel ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The sentiment expressed in an utterance. When the bot is configured to send utterances to Amazon Comprehend for sentiment analysis, this field structure contains the result of the analysis."]
module RequestTimeoutException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The input speech is too long."]
module SensitiveStringUnbounded =
  struct
    type nonrec t = string
    let context_ = "SensitiveStringUnbounded"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SensitiveStringUnbounded" j
    let to_json = simple_to_json to_value
  end
module UnsupportedMediaTypeException =
  struct
    type nonrec t = {
      message: String_.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The Content-Type header (PostContent API) has an invalid value."]
module AttributesString =
  struct
    type nonrec t = string
    let context_ = "AttributesString"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AttributesString" j
    let to_json = simple_to_json to_value
  end
module PutSessionResponse =
  struct
    type nonrec t =
      {
      contentType: HttpContentType.t option
        [@ocaml.doc
          "Content type as specified in the Accept HTTP header in the request."];
      intentName: IntentName.t option
        [@ocaml.doc "The name of the current intent."];
      slots: String_.t option
        [@ocaml.doc
          "Map of zero or more intent slots Amazon Lex detected from the user input during the conversation. Amazon Lex creates a resolution list containing likely values for a slot. The value that it returns is determined by the valueSelectionStrategy selected when the slot type was created or updated. If valueSelectionStrategy is set to ORIGINAL_VALUE, the value provided by the user is returned, if the user value is similar to the slot values. If valueSelectionStrategy is set to TOP_RESOLUTION Amazon Lex returns the first value in the resolution list or, if there is no resolution list, null. If you don't specify a valueSelectionStrategy the default is ORIGINAL_VALUE."];
      sessionAttributes: String_.t option
        [@ocaml.doc
          "Map of key/value pairs representing session-specific context information."];
      message: Text.t option
        [@ocaml.doc
          "The next message that should be presented to the user. You can only use this field in the de-DE, en-AU, en-GB, en-US, es-419, es-ES, es-US, fr-CA, fr-FR, and it-IT locales. In all other locales, the message field is null. You should use the encodedMessage field instead."];
      encodedMessage: SensitiveString.t option
        [@ocaml.doc
          "The next message that should be presented to the user. The encodedMessage field is base-64 encoded. You must decode the field before you can use the value."];
      messageFormat: MessageFormatType.t option
        [@ocaml.doc
          "The format of the response message. One of the following values: PlainText - The message contains plain UTF-8 text. CustomPayload - The message is a custom format for the client. SSML - The message contains text formatted for voice output. Composite - The message contains an escaped JSON object containing one or more messages from the groups that messages were assigned to when the intent was created."];
      dialogState: DialogState.t option
        [@ocaml.doc
          "ConfirmIntent - Amazon Lex is expecting a \"yes\" or \"no\" response to confirm the intent before fulfilling an intent. ElicitIntent - Amazon Lex wants to elicit the user's intent. ElicitSlot - Amazon Lex is expecting the value of a slot for the current intent. Failed - Conveys that the conversation with the user has failed. This can happen for various reasons, including the user does not provide an appropriate response to prompts from the service, or if the Lambda function fails to fulfill the intent. Fulfilled - Conveys that the Lambda function has sucessfully fulfilled the intent. ReadyForFulfillment - Conveys that the client has to fulfill the intent."];
      slotToElicit: String_.t option
        [@ocaml.doc
          "If the dialogState is ElicitSlot, returns the name of the slot for which Amazon Lex is eliciting a value."];
      audioStream: BlobStream.t option
        [@ocaml.doc
          "The audio version of the message to convey to the user."];
      sessionId: String_.t option
        [@ocaml.doc "A unique identifier for the session."];
      activeContexts: ActiveContextsString.t option
        [@ocaml.doc "A list of active contexts for the session."]}
    type nonrec error =
      [ `BadGatewayException of BadGatewayException.t 
      | `BadRequestException of BadRequestException.t 
      | `ConflictException of ConflictException.t 
      | `DependencyFailedException of DependencyFailedException.t 
      | `InternalFailureException of InternalFailureException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotAcceptableException of NotAcceptableException.t 
      | `NotFoundException of NotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?contentType =
      fun ?intentName ->
        fun ?slots ->
          fun ?sessionAttributes ->
            fun ?message ->
              fun ?encodedMessage ->
                fun ?messageFormat ->
                  fun ?dialogState ->
                    fun ?slotToElicit ->
                      fun ?audioStream ->
                        fun ?sessionId ->
                          fun ?activeContexts ->
                            fun () ->
                              {
                                contentType;
                                intentName;
                                slots;
                                sessionAttributes;
                                message;
                                encodedMessage;
                                messageFormat;
                                dialogState;
                                slotToElicit;
                                audioStream;
                                sessionId;
                                activeContexts
                              }
    let error_of_json name json =
      match name with
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_json json)
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_json json)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotAcceptableException" ->
          `NotAcceptableException (NotAcceptableException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_xml xml)
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_xml xml)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotAcceptableException" ->
          `NotAcceptableException (NotAcceptableException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadGatewayException e ->
          `Assoc
            [("error", (`String "BadGatewayException"));
            ("details", (BadGatewayException.to_json e))]
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `DependencyFailedException e ->
          `Assoc
            [("error", (`String "DependencyFailedException"));
            ("details", (DependencyFailedException.to_json e))]
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotAcceptableException e ->
          `Assoc
            [("error", (`String "NotAcceptableException"));
            ("details", (NotAcceptableException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body =
      ((fun (xs, pipe) ->
          make
            ?contentType:(Option.map
                            ((List.Assoc.find ~equal:String.Caseless.equal)
                               xs "Content-Type")
                            ~f:HttpContentType.of_string)
            ?intentName:(Option.map
                           ((List.Assoc.find ~equal:String.Caseless.equal) xs
                              "x-amz-lex-intent-name")
                           ~f:IntentName.of_string)
            ?slots:(Option.map
                      ((List.Assoc.find ~equal:String.Caseless.equal) xs
                         "x-amz-lex-slots") ~f:String_.of_string)
            ?sessionAttributes:(Option.map
                                  ((List.Assoc.find
                                      ~equal:String.Caseless.equal) xs
                                     "x-amz-lex-session-attributes")
                                  ~f:String_.of_string)
            ?message:(Option.map
                        ((List.Assoc.find ~equal:String.Caseless.equal) xs
                           "x-amz-lex-message") ~f:Text.of_string)
            ?encodedMessage:(Option.map
                               ((List.Assoc.find ~equal:String.Caseless.equal)
                                  xs "x-amz-lex-encoded-message")
                               ~f:SensitiveString.of_string)
            ?messageFormat:(Option.map
                              ((List.Assoc.find ~equal:String.Caseless.equal)
                                 xs "x-amz-lex-message-format")
                              ~f:MessageFormatType.of_string)
            ?dialogState:(Option.map
                            ((List.Assoc.find ~equal:String.Caseless.equal)
                               xs "x-amz-lex-dialog-state")
                            ~f:DialogState.of_string)
            ?slotToElicit:(Option.map
                             ((List.Assoc.find ~equal:String.Caseless.equal)
                                xs "x-amz-lex-slot-to-elicit")
                             ~f:String_.of_string) ?audioStream:(Some pipe)
            ?sessionId:(Option.map
                          ((List.Assoc.find ~equal:String.Caseless.equal) xs
                             "x-amz-lex-session-id") ~f:String_.of_string)
            ?activeContexts:(Option.map
                               ((List.Assoc.find ~equal:String.Caseless.equal)
                                  xs "x-amz-lex-active-contexts")
                               ~f:ActiveContextsString.of_string) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("Content-Type",
           (Option.map x.contentType ~f:HttpContentType.to_value));
        ("x-amz-lex-intent-name",
          (Option.map x.intentName ~f:IntentName.to_value));
        ("x-amz-lex-slots", (Option.map x.slots ~f:String_.to_value));
        ("x-amz-lex-session-attributes",
          (Option.map x.sessionAttributes ~f:String_.to_value));
        ("x-amz-lex-message", (Option.map x.message ~f:Text.to_value));
        ("x-amz-lex-encoded-message",
          (Option.map x.encodedMessage ~f:SensitiveString.to_value));
        ("x-amz-lex-message-format",
          (Option.map x.messageFormat ~f:MessageFormatType.to_value));
        ("x-amz-lex-dialog-state",
          (Option.map x.dialogState ~f:DialogState.to_value));
        ("x-amz-lex-slot-to-elicit",
          (Option.map x.slotToElicit ~f:String_.to_value));
        ("audioStream", (Option.map x.audioStream ~f:BlobStream.to_value));
        ("x-amz-lex-session-id",
          (Option.map x.sessionId ~f:String_.to_value));
        ("x-amz-lex-active-contexts",
          (Option.map x.activeContexts ~f:ActiveContextsString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let activeContexts =
        (Option.map ~f:ActiveContextsString.of_xml)
          (Xml.child xml_arg0 "x-amz-lex-active-contexts") in
      let sessionId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amz-lex-session-id") in
      let audioStream =
        (Option.map ~f:BlobStream.of_xml) (Xml.child xml_arg0 "audioStream") in
      let slotToElicit =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amz-lex-slot-to-elicit") in
      let dialogState =
        (Option.map ~f:DialogState.of_xml)
          (Xml.child xml_arg0 "x-amz-lex-dialog-state") in
      let messageFormat =
        (Option.map ~f:MessageFormatType.of_xml)
          (Xml.child xml_arg0 "x-amz-lex-message-format") in
      let encodedMessage =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "x-amz-lex-encoded-message") in
      let message =
        (Option.map ~f:Text.of_xml) (Xml.child xml_arg0 "x-amz-lex-message") in
      let sessionAttributes =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amz-lex-session-attributes") in
      let slots =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "x-amz-lex-slots") in
      let intentName =
        (Option.map ~f:IntentName.of_xml)
          (Xml.child xml_arg0 "x-amz-lex-intent-name") in
      let contentType =
        (Option.map ~f:HttpContentType.of_xml)
          (Xml.child xml_arg0 "Content-Type") in
      make ?activeContexts ?sessionId ?audioStream ?slotToElicit ?dialogState
        ?messageFormat ?encodedMessage ?message ?sessionAttributes ?slots
        ?intentName ?contentType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let activeContexts =
        field_map json__ "activeContexts" ActiveContextsString.of_json in
      let sessionId = field_map json__ "sessionId" String_.of_json in
      let audioStream = field_map json__ "audioStream" BlobStream.of_json in
      let slotToElicit = field_map json__ "slotToElicit" String_.of_json in
      let dialogState = field_map json__ "dialogState" DialogState.of_json in
      let messageFormat =
        field_map json__ "messageFormat" MessageFormatType.of_json in
      let encodedMessage =
        field_map json__ "encodedMessage" SensitiveString.of_json in
      let message = field_map json__ "message" Text.of_json in
      let sessionAttributes =
        field_map json__ "sessionAttributes" String_.of_json in
      let slots = field_map json__ "slots" String_.of_json in
      let intentName = field_map json__ "intentName" IntentName.of_json in
      let contentType =
        field_map json__ "contentType" HttpContentType.of_json in
      make ?activeContexts ?sessionId ?audioStream ?slotToElicit ?dialogState
        ?messageFormat ?encodedMessage ?message ?sessionAttributes ?slots
        ?intentName ?contentType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new session or modifies an existing session with an Amazon Lex bot. Use this operation to enable your application to set the state of the bot. For more information, see Managing Sessions."]
module PutSessionRequest =
  struct
    type nonrec t =
      {
      botName: BotName.t
        [@ocaml.doc "The name of the bot that contains the session data."];
      botAlias: BotAlias.t
        [@ocaml.doc
          "The alias in use for the bot that contains the session data."];
      userId: UserId.t
        [@ocaml.doc
          "The ID of the client application user. Amazon Lex uses this to identify a user's conversation with your bot."];
      sessionAttributes: StringMap.t option
        [@ocaml.doc
          "Map of key/value pairs representing the session-specific context information. It contains application information passed between Amazon Lex and a client application."];
      dialogAction: DialogAction.t option
        [@ocaml.doc
          "Sets the next action that the bot should take to fulfill the conversation."];
      recentIntentSummaryView: IntentSummaryList.t option
        [@ocaml.doc
          "A summary of the recent intents for the bot. You can use the intent summary view to set a checkpoint label on an intent and modify attributes of intents. You can also use it to remove or add intent summary objects to the list. An intent that you modify or add to the list must make sense for the bot. For example, the intent name must be valid for the bot. You must provide valid values for: intentName slot names slotToElict If you send the recentIntentSummaryView parameter in a PutSession request, the contents of the new summary view replaces the old summary view. For example, if a GetSession request returns three intents in the summary view and you call PutSession with one intent in the summary view, the next call to GetSession will only return one intent."];
      accept: Accept.t option
        [@ocaml.doc
          "The message that Amazon Lex returns in the response can be either text or speech based depending on the value of this field. If the value is text/plain; charset=utf-8, Amazon Lex returns text in the response. If the value begins with audio/, Amazon Lex returns speech in the response. Amazon Lex uses Amazon Polly to generate the speech in the configuration that you specify. For example, if you specify audio/mpeg as the value, Amazon Lex returns speech in the MPEG format. If the value is audio/pcm, the speech is returned as audio/pcm in 16-bit, little endian format. The following are the accepted values: audio/mpeg audio/ogg audio/pcm audio/* (defaults to mpeg) text/plain; charset=utf-8"];
      activeContexts: ActiveContextsList.t option
        [@ocaml.doc
          "A list of contexts active for the request. A context can be activated when a previous intent is fulfilled, or by including the context in the request, If you don't specify a list of contexts, Amazon Lex will use the current list of contexts for the session. If you specify an empty list, all contexts for the session are cleared."]}
    let context_ = "PutSessionRequest"
    let make ?sessionAttributes =
      fun ?dialogAction ->
        fun ?recentIntentSummaryView ->
          fun ?accept ->
            fun ?activeContexts ->
              fun ~botName ->
                fun ~botAlias ->
                  fun ~userId ->
                    fun () ->
                      {
                        sessionAttributes;
                        dialogAction;
                        recentIntentSummaryView;
                        accept;
                        activeContexts;
                        botName;
                        botAlias;
                        userId
                      }
    let to_value x =
      structure_to_value
        [("botName", (Some (BotName.to_value x.botName)));
        ("botAlias", (Some (BotAlias.to_value x.botAlias)));
        ("userId", (Some (UserId.to_value x.userId)));
        ("sessionAttributes",
          (Option.map x.sessionAttributes ~f:StringMap.to_value));
        ("dialogAction",
          (Option.map x.dialogAction ~f:DialogAction.to_value));
        ("recentIntentSummaryView",
          (Option.map x.recentIntentSummaryView ~f:IntentSummaryList.to_value));
        ("Accept", (Option.map x.accept ~f:Accept.to_value));
        ("activeContexts",
          (Option.map x.activeContexts ~f:ActiveContextsList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let activeContexts =
        (Option.map ~f:ActiveContextsList.of_xml)
          (Xml.child xml_arg0 "activeContexts") in
      let accept =
        (Option.map ~f:Accept.of_xml) (Xml.child xml_arg0 "Accept") in
      let recentIntentSummaryView =
        (Option.map ~f:IntentSummaryList.of_xml)
          (Xml.child xml_arg0 "recentIntentSummaryView") in
      let dialogAction =
        (Option.map ~f:DialogAction.of_xml)
          (Xml.child xml_arg0 "dialogAction") in
      let sessionAttributes =
        (Option.map ~f:StringMap.of_xml)
          (Xml.child xml_arg0 "sessionAttributes") in
      let userId =
        UserId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "userId") in
      let botAlias =
        BotAlias.of_xml (Xml.child_exn ~context:context_ xml_arg0 "botAlias") in
      let botName =
        BotName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "botName") in
      make ?activeContexts ?accept ?recentIntentSummaryView ?dialogAction
        ?sessionAttributes ~userId ~botAlias ~botName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let activeContexts =
        field_map json__ "activeContexts" ActiveContextsList.of_json in
      let accept = field_map json__ "accept" Accept.of_json in
      let recentIntentSummaryView =
        field_map json__ "recentIntentSummaryView" IntentSummaryList.of_json in
      let dialogAction = field_map json__ "dialogAction" DialogAction.of_json in
      let sessionAttributes =
        field_map json__ "sessionAttributes" StringMap.of_json in
      let userId = field_map_exn json__ "userId" UserId.of_json in
      let botAlias = field_map_exn json__ "botAlias" BotAlias.of_json in
      let botName = field_map_exn json__ "botName" BotName.of_json in
      make ?activeContexts ?accept ?recentIntentSummaryView ?dialogAction
        ?sessionAttributes ~userId ~botAlias ~botName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new session or modifies an existing session with an Amazon Lex bot. Use this operation to enable your application to set the state of the bot. For more information, see Managing Sessions."]
module PostTextResponse =
  struct
    type nonrec t =
      {
      intentName: IntentName.t option
        [@ocaml.doc "The current user intent that Amazon Lex is aware of."];
      nluIntentConfidence: IntentConfidence.t option
        [@ocaml.doc
          "Provides a score that indicates how confident Amazon Lex is that the returned intent is the one that matches the user's intent. The score is between 0.0 and 1.0. For more information, see Confidence Scores. The score is a relative score, not an absolute score. The score may change based on improvements to Amazon Lex."];
      alternativeIntents: IntentList.t option
        [@ocaml.doc
          "One to four alternative intents that may be applicable to the user's intent. Each alternative includes a score that indicates how confident Amazon Lex is that the intent matches the user's intent. The intents are sorted by the confidence score."];
      slots: StringMap.t option
        [@ocaml.doc
          "The intent slots that Amazon Lex detected from the user input in the conversation. Amazon Lex creates a resolution list containing likely values for a slot. The value that it returns is determined by the valueSelectionStrategy selected when the slot type was created or updated. If valueSelectionStrategy is set to ORIGINAL_VALUE, the value provided by the user is returned, if the user value is similar to the slot values. If valueSelectionStrategy is set to TOP_RESOLUTION Amazon Lex returns the first value in the resolution list or, if there is no resolution list, null. If you don't specify a valueSelectionStrategy, the default is ORIGINAL_VALUE."];
      sessionAttributes: StringMap.t option
        [@ocaml.doc
          "A map of key-value pairs representing the session-specific context information."];
      message: Text.t option
        [@ocaml.doc
          "The message to convey to the user. The message can come from the bot's configuration or from a Lambda function. If the intent is not configured with a Lambda function, or if the Lambda function returned Delegate as the dialogAction.type its response, Amazon Lex decides on the next course of action and selects an appropriate message from the bot's configuration based on the current interaction context. For example, if Amazon Lex isn't able to understand user input, it uses a clarification prompt message. When you create an intent you can assign messages to groups. When messages are assigned to groups Amazon Lex returns one message from each group in the response. The message field is an escaped JSON string containing the messages. For more information about the structure of the JSON string returned, see msg-prompts-formats. If the Lambda function returns a message, Amazon Lex passes it to the client in its response."];
      sentimentResponse: SentimentResponse.t option
        [@ocaml.doc
          "The sentiment expressed in and utterance. When the bot is configured to send utterances to Amazon Comprehend for sentiment analysis, this field contains the result of the analysis."];
      messageFormat: MessageFormatType.t option
        [@ocaml.doc
          "The format of the response message. One of the following values: PlainText - The message contains plain UTF-8 text. CustomPayload - The message is a custom format defined by the Lambda function. SSML - The message contains text formatted for voice output. Composite - The message contains an escaped JSON object containing one or more messages from the groups that messages were assigned to when the intent was created."];
      dialogState: DialogState.t option
        [@ocaml.doc
          "Identifies the current state of the user interaction. Amazon Lex returns one of the following values as dialogState. The client can optionally use this information to customize the user interface. ElicitIntent - Amazon Lex wants to elicit user intent. For example, a user might utter an intent (\"I want to order a pizza\"). If Amazon Lex cannot infer the user intent from this utterance, it will return this dialogState. ConfirmIntent - Amazon Lex is expecting a \"yes\" or \"no\" response. For example, Amazon Lex wants user confirmation before fulfilling an intent. Instead of a simple \"yes\" or \"no,\" a user might respond with additional information. For example, \"yes, but make it thick crust pizza\" or \"no, I want to order a drink\". Amazon Lex can process such additional information (in these examples, update the crust type slot value, or change intent from OrderPizza to OrderDrink). ElicitSlot - Amazon Lex is expecting a slot value for the current intent. For example, suppose that in the response Amazon Lex sends this message: \"What size pizza would you like?\". A user might reply with the slot value (e.g., \"medium\"). The user might also provide additional information in the response (e.g., \"medium thick crust pizza\"). Amazon Lex can process such additional information appropriately. Fulfilled - Conveys that the Lambda function configured for the intent has successfully fulfilled the intent. ReadyForFulfillment - Conveys that the client has to fulfill the intent. Failed - Conveys that the conversation with the user failed. This can happen for various reasons including that the user did not provide an appropriate response to prompts from the service (you can configure how many times Amazon Lex can prompt a user for specific information), or the Lambda function failed to fulfill the intent."];
      slotToElicit: String_.t option
        [@ocaml.doc
          "If the dialogState value is ElicitSlot, returns the name of the slot for which Amazon Lex is eliciting a value."];
      responseCard: ResponseCard.t option
        [@ocaml.doc
          "Represents the options that the user has to respond to the current prompt. Response Card can come from the bot configuration (in the Amazon Lex console, choose the settings button next to a slot) or from a code hook (Lambda function)."];
      sessionId: String_.t option
        [@ocaml.doc "A unique identifier for the session."];
      botVersion: BotVersion.t option
        [@ocaml.doc
          "The version of the bot that responded to the conversation. You can use this information to help determine if one version of a bot is performing better than another version."];
      activeContexts: ActiveContextsList.t option
        [@ocaml.doc
          "A list of active contexts for the session. A context can be set when an intent is fulfilled or by calling the PostContent, PostText, or PutSession operation. You can use a context to control the intents that can follow up an intent, or to modify the operation of your application."]}
    type nonrec error =
      [ `BadGatewayException of BadGatewayException.t 
      | `BadRequestException of BadRequestException.t 
      | `ConflictException of ConflictException.t 
      | `DependencyFailedException of DependencyFailedException.t 
      | `InternalFailureException of InternalFailureException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `LoopDetectedException of LoopDetectedException.t 
      | `NotFoundException of NotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?intentName =
      fun ?nluIntentConfidence ->
        fun ?alternativeIntents ->
          fun ?slots ->
            fun ?sessionAttributes ->
              fun ?message ->
                fun ?sentimentResponse ->
                  fun ?messageFormat ->
                    fun ?dialogState ->
                      fun ?slotToElicit ->
                        fun ?responseCard ->
                          fun ?sessionId ->
                            fun ?botVersion ->
                              fun ?activeContexts ->
                                fun () ->
                                  {
                                    intentName;
                                    nluIntentConfidence;
                                    alternativeIntents;
                                    slots;
                                    sessionAttributes;
                                    message;
                                    sentimentResponse;
                                    messageFormat;
                                    dialogState;
                                    slotToElicit;
                                    responseCard;
                                    sessionId;
                                    botVersion;
                                    activeContexts
                                  }
    let error_of_json name json =
      match name with
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_json json)
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_json json)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "LoopDetectedException" ->
          `LoopDetectedException (LoopDetectedException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_xml xml)
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_xml xml)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "LoopDetectedException" ->
          `LoopDetectedException (LoopDetectedException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadGatewayException e ->
          `Assoc
            [("error", (`String "BadGatewayException"));
            ("details", (BadGatewayException.to_json e))]
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `DependencyFailedException e ->
          `Assoc
            [("error", (`String "DependencyFailedException"));
            ("details", (DependencyFailedException.to_json e))]
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `LoopDetectedException e ->
          `Assoc
            [("error", (`String "LoopDetectedException"));
            ("details", (LoopDetectedException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("intentName", (Option.map x.intentName ~f:IntentName.to_value));
        ("nluIntentConfidence",
          (Option.map x.nluIntentConfidence ~f:IntentConfidence.to_value));
        ("alternativeIntents",
          (Option.map x.alternativeIntents ~f:IntentList.to_value));
        ("slots", (Option.map x.slots ~f:StringMap.to_value));
        ("sessionAttributes",
          (Option.map x.sessionAttributes ~f:StringMap.to_value));
        ("message", (Option.map x.message ~f:Text.to_value));
        ("sentimentResponse",
          (Option.map x.sentimentResponse ~f:SentimentResponse.to_value));
        ("messageFormat",
          (Option.map x.messageFormat ~f:MessageFormatType.to_value));
        ("dialogState", (Option.map x.dialogState ~f:DialogState.to_value));
        ("slotToElicit", (Option.map x.slotToElicit ~f:String_.to_value));
        ("responseCard",
          (Option.map x.responseCard ~f:ResponseCard.to_value));
        ("sessionId", (Option.map x.sessionId ~f:String_.to_value));
        ("botVersion", (Option.map x.botVersion ~f:BotVersion.to_value));
        ("activeContexts",
          (Option.map x.activeContexts ~f:ActiveContextsList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let activeContexts =
        (Option.map ~f:ActiveContextsList.of_xml)
          (Xml.child xml_arg0 "activeContexts") in
      let botVersion =
        (Option.map ~f:BotVersion.of_xml) (Xml.child xml_arg0 "botVersion") in
      let sessionId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "sessionId") in
      let responseCard =
        (Option.map ~f:ResponseCard.of_xml)
          (Xml.child xml_arg0 "responseCard") in
      let slotToElicit =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "slotToElicit") in
      let dialogState =
        (Option.map ~f:DialogState.of_xml) (Xml.child xml_arg0 "dialogState") in
      let messageFormat =
        (Option.map ~f:MessageFormatType.of_xml)
          (Xml.child xml_arg0 "messageFormat") in
      let sentimentResponse =
        (Option.map ~f:SentimentResponse.of_xml)
          (Xml.child xml_arg0 "sentimentResponse") in
      let message =
        (Option.map ~f:Text.of_xml) (Xml.child xml_arg0 "message") in
      let sessionAttributes =
        (Option.map ~f:StringMap.of_xml)
          (Xml.child xml_arg0 "sessionAttributes") in
      let slots =
        (Option.map ~f:StringMap.of_xml) (Xml.child xml_arg0 "slots") in
      let alternativeIntents =
        (Option.map ~f:IntentList.of_xml)
          (Xml.child xml_arg0 "alternativeIntents") in
      let nluIntentConfidence =
        (Option.map ~f:IntentConfidence.of_xml)
          (Xml.child xml_arg0 "nluIntentConfidence") in
      let intentName =
        (Option.map ~f:IntentName.of_xml) (Xml.child xml_arg0 "intentName") in
      make ?activeContexts ?botVersion ?sessionId ?responseCard ?slotToElicit
        ?dialogState ?messageFormat ?sentimentResponse ?message
        ?sessionAttributes ?slots ?alternativeIntents ?nluIntentConfidence
        ?intentName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let activeContexts =
        field_map json__ "activeContexts" ActiveContextsList.of_json in
      let botVersion = field_map json__ "botVersion" BotVersion.of_json in
      let sessionId = field_map json__ "sessionId" String_.of_json in
      let responseCard = field_map json__ "responseCard" ResponseCard.of_json in
      let slotToElicit = field_map json__ "slotToElicit" String_.of_json in
      let dialogState = field_map json__ "dialogState" DialogState.of_json in
      let messageFormat =
        field_map json__ "messageFormat" MessageFormatType.of_json in
      let sentimentResponse =
        field_map json__ "sentimentResponse" SentimentResponse.of_json in
      let message = field_map json__ "message" Text.of_json in
      let sessionAttributes =
        field_map json__ "sessionAttributes" StringMap.of_json in
      let slots = field_map json__ "slots" StringMap.of_json in
      let alternativeIntents =
        field_map json__ "alternativeIntents" IntentList.of_json in
      let nluIntentConfidence =
        field_map json__ "nluIntentConfidence" IntentConfidence.of_json in
      let intentName = field_map json__ "intentName" IntentName.of_json in
      make ?activeContexts ?botVersion ?sessionId ?responseCard ?slotToElicit
        ?dialogState ?messageFormat ?sentimentResponse ?message
        ?sessionAttributes ?slots ?alternativeIntents ?nluIntentConfidence
        ?intentName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Sends user input to Amazon Lex. Client applications can use this API to send requests to Amazon Lex at runtime. Amazon Lex then interprets the user input using the machine learning model it built for the bot. In response, Amazon Lex returns the next message to convey to the user an optional responseCard to display. Consider the following example messages: For a user input \"I would like a pizza\", Amazon Lex might return a response with a message eliciting slot data (for example, PizzaSize): \"What size pizza would you like?\" After the user provides all of the pizza order information, Amazon Lex might return a response with a message to obtain user confirmation \"Proceed with the pizza order?\". After the user replies to a confirmation prompt with a \"yes\", Amazon Lex might return a conclusion statement: \"Thank you, your cheese pizza has been ordered.\". Not all Amazon Lex messages require a user response. For example, a conclusion statement does not require a response. Some messages require only a \"yes\" or \"no\" user response. In addition to the message, Amazon Lex provides additional context about the message in the response that you might use to enhance client behavior, for example, to display the appropriate client user interface. These are the slotToElicit, dialogState, intentName, and slots fields in the response. Consider the following examples: If the message is to elicit slot data, Amazon Lex returns the following context information: dialogState set to ElicitSlot intentName set to the intent name in the current context slotToElicit set to the slot name for which the message is eliciting information slots set to a map of slots, configured for the intent, with currently known values If the message is a confirmation prompt, the dialogState is set to ConfirmIntent and SlotToElicit is set to null. If the message is a clarification prompt (configured for the intent) that indicates that user intent is not understood, the dialogState is set to ElicitIntent and slotToElicit is set to null. In addition, Amazon Lex also returns your application-specific sessionAttributes. For more information, see Managing Conversation Context."]
module PostTextRequest =
  struct
    type nonrec t =
      {
      botName: BotName.t [@ocaml.doc "The name of the Amazon Lex bot."];
      botAlias: BotAlias.t [@ocaml.doc "The alias of the Amazon Lex bot."];
      userId: UserId.t
        [@ocaml.doc
          "The ID of the client application user. Amazon Lex uses this to identify a user's conversation with your bot. At runtime, each request must contain the userID field. To decide the user ID to use for your application, consider the following factors. The userID field must not contain any personally identifiable information of the user, for example, name, personal identification numbers, or other end user personal information. If you want a user to start a conversation on one device and continue on another device, use a user-specific identifier. If you want the same user to be able to have two independent conversations on two different devices, choose a device-specific identifier. A user can't have two independent conversations with two different versions of the same bot. For example, a user can't have a conversation with the PROD and BETA versions of the same bot. If you anticipate that a user will need to have conversation with two different versions, for example, while testing, include the bot alias in the user ID to separate the two conversations."];
      sessionAttributes: StringMap.t option
        [@ocaml.doc
          "Application-specific information passed between Amazon Lex and a client application. For more information, see Setting Session Attributes."];
      requestAttributes: StringMap.t option
        [@ocaml.doc
          "Request-specific information passed between Amazon Lex and a client application. The namespace x-amz-lex: is reserved for special attributes. Don't create any request attributes with the prefix x-amz-lex:. For more information, see Setting Request Attributes."];
      inputText: Text.t
        [@ocaml.doc
          "The text that the user entered (Amazon Lex interprets this text)."];
      activeContexts: ActiveContextsList.t option
        [@ocaml.doc
          "A list of contexts active for the request. A context can be activated when a previous intent is fulfilled, or by including the context in the request, If you don't specify a list of contexts, Amazon Lex will use the current list of contexts for the session. If you specify an empty list, all contexts for the session are cleared."]}
    let context_ = "PostTextRequest"
    let make ?sessionAttributes =
      fun ?requestAttributes ->
        fun ?activeContexts ->
          fun ~botName ->
            fun ~botAlias ->
              fun ~userId ->
                fun ~inputText ->
                  fun () ->
                    {
                      sessionAttributes;
                      requestAttributes;
                      activeContexts;
                      botName;
                      botAlias;
                      userId;
                      inputText
                    }
    let to_value x =
      structure_to_value
        [("botName", (Some (BotName.to_value x.botName)));
        ("botAlias", (Some (BotAlias.to_value x.botAlias)));
        ("userId", (Some (UserId.to_value x.userId)));
        ("sessionAttributes",
          (Option.map x.sessionAttributes ~f:StringMap.to_value));
        ("requestAttributes",
          (Option.map x.requestAttributes ~f:StringMap.to_value));
        ("inputText", (Some (Text.to_value x.inputText)));
        ("activeContexts",
          (Option.map x.activeContexts ~f:ActiveContextsList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let activeContexts =
        (Option.map ~f:ActiveContextsList.of_xml)
          (Xml.child xml_arg0 "activeContexts") in
      let inputText =
        Text.of_xml (Xml.child_exn ~context:context_ xml_arg0 "inputText") in
      let requestAttributes =
        (Option.map ~f:StringMap.of_xml)
          (Xml.child xml_arg0 "requestAttributes") in
      let sessionAttributes =
        (Option.map ~f:StringMap.of_xml)
          (Xml.child xml_arg0 "sessionAttributes") in
      let userId =
        UserId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "userId") in
      let botAlias =
        BotAlias.of_xml (Xml.child_exn ~context:context_ xml_arg0 "botAlias") in
      let botName =
        BotName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "botName") in
      make ?activeContexts ~inputText ?requestAttributes ?sessionAttributes
        ~userId ~botAlias ~botName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let activeContexts =
        field_map json__ "activeContexts" ActiveContextsList.of_json in
      let inputText = field_map_exn json__ "inputText" Text.of_json in
      let requestAttributes =
        field_map json__ "requestAttributes" StringMap.of_json in
      let sessionAttributes =
        field_map json__ "sessionAttributes" StringMap.of_json in
      let userId = field_map_exn json__ "userId" UserId.of_json in
      let botAlias = field_map_exn json__ "botAlias" BotAlias.of_json in
      let botName = field_map_exn json__ "botName" BotName.of_json in
      make ?activeContexts ~inputText ?requestAttributes ?sessionAttributes
        ~userId ~botAlias ~botName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Sends user input to Amazon Lex. Client applications can use this API to send requests to Amazon Lex at runtime. Amazon Lex then interprets the user input using the machine learning model it built for the bot. In response, Amazon Lex returns the next message to convey to the user an optional responseCard to display. Consider the following example messages: For a user input \"I would like a pizza\", Amazon Lex might return a response with a message eliciting slot data (for example, PizzaSize): \"What size pizza would you like?\" After the user provides all of the pizza order information, Amazon Lex might return a response with a message to obtain user confirmation \"Proceed with the pizza order?\". After the user replies to a confirmation prompt with a \"yes\", Amazon Lex might return a conclusion statement: \"Thank you, your cheese pizza has been ordered.\". Not all Amazon Lex messages require a user response. For example, a conclusion statement does not require a response. Some messages require only a \"yes\" or \"no\" user response. In addition to the message, Amazon Lex provides additional context about the message in the response that you might use to enhance client behavior, for example, to display the appropriate client user interface. These are the slotToElicit, dialogState, intentName, and slots fields in the response. Consider the following examples: If the message is to elicit slot data, Amazon Lex returns the following context information: dialogState set to ElicitSlot intentName set to the intent name in the current context slotToElicit set to the slot name for which the message is eliciting information slots set to a map of slots, configured for the intent, with currently known values If the message is a confirmation prompt, the dialogState is set to ConfirmIntent and SlotToElicit is set to null. If the message is a clarification prompt (configured for the intent) that indicates that user intent is not understood, the dialogState is set to ElicitIntent and slotToElicit is set to null. In addition, Amazon Lex also returns your application-specific sessionAttributes. For more information, see Managing Conversation Context."]
module PostContentResponse =
  struct
    type nonrec t =
      {
      contentType: HttpContentType.t option
        [@ocaml.doc
          "Content type as specified in the Accept HTTP header in the request."];
      intentName: IntentName.t option
        [@ocaml.doc "Current user intent that Amazon Lex is aware of."];
      nluIntentConfidence: String_.t option
        [@ocaml.doc
          "Provides a score that indicates how confident Amazon Lex is that the returned intent is the one that matches the user's intent. The score is between 0.0 and 1.0. The score is a relative score, not an absolute score. The score may change based on improvements to Amazon Lex."];
      alternativeIntents: String_.t option
        [@ocaml.doc
          "One to four alternative intents that may be applicable to the user's intent. Each alternative includes a score that indicates how confident Amazon Lex is that the intent matches the user's intent. The intents are sorted by the confidence score."];
      slots: String_.t option
        [@ocaml.doc
          "Map of zero or more intent slots (name/value pairs) Amazon Lex detected from the user input during the conversation. The field is base-64 encoded. Amazon Lex creates a resolution list containing likely values for a slot. The value that it returns is determined by the valueSelectionStrategy selected when the slot type was created or updated. If valueSelectionStrategy is set to ORIGINAL_VALUE, the value provided by the user is returned, if the user value is similar to the slot values. If valueSelectionStrategy is set to TOP_RESOLUTION Amazon Lex returns the first value in the resolution list or, if there is no resolution list, null. If you don't specify a valueSelectionStrategy, the default is ORIGINAL_VALUE."];
      sessionAttributes: String_.t option
        [@ocaml.doc
          "Map of key/value pairs representing the session-specific context information."];
      sentimentResponse: String_.t option
        [@ocaml.doc
          "The sentiment expressed in an utterance. When the bot is configured to send utterances to Amazon Comprehend for sentiment analysis, this field contains the result of the analysis."];
      message: Text.t option
        [@ocaml.doc
          "You can only use this field in the de-DE, en-AU, en-GB, en-US, es-419, es-ES, es-US, fr-CA, fr-FR, and it-IT locales. In all other locales, the message field is null. You should use the encodedMessage field instead. The message to convey to the user. The message can come from the bot's configuration or from a Lambda function. If the intent is not configured with a Lambda function, or if the Lambda function returned Delegate as the dialogAction.type in its response, Amazon Lex decides on the next course of action and selects an appropriate message from the bot's configuration based on the current interaction context. For example, if Amazon Lex isn't able to understand user input, it uses a clarification prompt message. When you create an intent you can assign messages to groups. When messages are assigned to groups Amazon Lex returns one message from each group in the response. The message field is an escaped JSON string containing the messages. For more information about the structure of the JSON string returned, see msg-prompts-formats. If the Lambda function returns a message, Amazon Lex passes it to the client in its response."];
      encodedMessage: SensitiveString.t option
        [@ocaml.doc
          "The message to convey to the user. The message can come from the bot's configuration or from a Lambda function. If the intent is not configured with a Lambda function, or if the Lambda function returned Delegate as the dialogAction.type in its response, Amazon Lex decides on the next course of action and selects an appropriate message from the bot's configuration based on the current interaction context. For example, if Amazon Lex isn't able to understand user input, it uses a clarification prompt message. When you create an intent you can assign messages to groups. When messages are assigned to groups Amazon Lex returns one message from each group in the response. The message field is an escaped JSON string containing the messages. For more information about the structure of the JSON string returned, see msg-prompts-formats. If the Lambda function returns a message, Amazon Lex passes it to the client in its response. The encodedMessage field is base-64 encoded. You must decode the field before you can use the value."];
      messageFormat: MessageFormatType.t option
        [@ocaml.doc
          "The format of the response message. One of the following values: PlainText - The message contains plain UTF-8 text. CustomPayload - The message is a custom format for the client. SSML - The message contains text formatted for voice output. Composite - The message contains an escaped JSON object containing one or more messages from the groups that messages were assigned to when the intent was created."];
      dialogState: DialogState.t option
        [@ocaml.doc
          "Identifies the current state of the user interaction. Amazon Lex returns one of the following values as dialogState. The client can optionally use this information to customize the user interface. ElicitIntent - Amazon Lex wants to elicit the user's intent. Consider the following examples: For example, a user might utter an intent (\"I want to order a pizza\"). If Amazon Lex cannot infer the user intent from this utterance, it will return this dialog state. ConfirmIntent - Amazon Lex is expecting a \"yes\" or \"no\" response. For example, Amazon Lex wants user confirmation before fulfilling an intent. Instead of a simple \"yes\" or \"no\" response, a user might respond with additional information. For example, \"yes, but make it a thick crust pizza\" or \"no, I want to order a drink.\" Amazon Lex can process such additional information (in these examples, update the crust type slot or change the intent from OrderPizza to OrderDrink). ElicitSlot - Amazon Lex is expecting the value of a slot for the current intent. For example, suppose that in the response Amazon Lex sends this message: \"What size pizza would you like?\". A user might reply with the slot value (e.g., \"medium\"). The user might also provide additional information in the response (e.g., \"medium thick crust pizza\"). Amazon Lex can process such additional information appropriately. Fulfilled - Conveys that the Lambda function has successfully fulfilled the intent. ReadyForFulfillment - Conveys that the client has to fulfill the request. Failed - Conveys that the conversation with the user failed. This can happen for various reasons, including that the user does not provide an appropriate response to prompts from the service (you can configure how many times Amazon Lex can prompt a user for specific information), or if the Lambda function fails to fulfill the intent."];
      slotToElicit: String_.t option
        [@ocaml.doc
          "If the dialogState value is ElicitSlot, returns the name of the slot for which Amazon Lex is eliciting a value."];
      inputTranscript: String_.t option
        [@ocaml.doc
          "The text used to process the request. You can use this field only in the de-DE, en-AU, en-GB, en-US, es-419, es-ES, es-US, fr-CA, fr-FR, and it-IT locales. In all other locales, the inputTranscript field is null. You should use the encodedInputTranscript field instead. If the input was an audio stream, the inputTranscript field contains the text extracted from the audio stream. This is the text that is actually processed to recognize intents and slot values. You can use this information to determine if Amazon Lex is correctly processing the audio that you send."];
      encodedInputTranscript: SensitiveStringUnbounded.t option
        [@ocaml.doc
          "The text used to process the request. If the input was an audio stream, the encodedInputTranscript field contains the text extracted from the audio stream. This is the text that is actually processed to recognize intents and slot values. You can use this information to determine if Amazon Lex is correctly processing the audio that you send. The encodedInputTranscript field is base-64 encoded. You must decode the field before you can use the value."];
      audioStream: BlobStream.t option
        [@ocaml.doc
          "The prompt (or statement) to convey to the user. This is based on the bot configuration and context. For example, if Amazon Lex did not understand the user intent, it sends the clarificationPrompt configured for the bot. If the intent requires confirmation before taking the fulfillment action, it sends the confirmationPrompt. Another example: Suppose that the Lambda function successfully fulfilled the intent, and sent a message to convey to the user. Then Amazon Lex sends that message in the response."];
      botVersion: BotVersion.t option
        [@ocaml.doc
          "The version of the bot that responded to the conversation. You can use this information to help determine if one version of a bot is performing better than another version."];
      sessionId: String_.t option
        [@ocaml.doc "The unique identifier for the session."];
      activeContexts: ActiveContextsString.t option
        [@ocaml.doc
          "A list of active contexts for the session. A context can be set when an intent is fulfilled or by calling the PostContent, PostText, or PutSession operation. You can use a context to control the intents that can follow up an intent, or to modify the operation of your application."]}
    type nonrec error =
      [ `BadGatewayException of BadGatewayException.t 
      | `BadRequestException of BadRequestException.t 
      | `ConflictException of ConflictException.t 
      | `DependencyFailedException of DependencyFailedException.t 
      | `InternalFailureException of InternalFailureException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `LoopDetectedException of LoopDetectedException.t 
      | `NotAcceptableException of NotAcceptableException.t 
      | `NotFoundException of NotFoundException.t 
      | `RequestTimeoutException of RequestTimeoutException.t 
      | `UnsupportedMediaTypeException of UnsupportedMediaTypeException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?contentType =
      fun ?intentName ->
        fun ?nluIntentConfidence ->
          fun ?alternativeIntents ->
            fun ?slots ->
              fun ?sessionAttributes ->
                fun ?sentimentResponse ->
                  fun ?message ->
                    fun ?encodedMessage ->
                      fun ?messageFormat ->
                        fun ?dialogState ->
                          fun ?slotToElicit ->
                            fun ?inputTranscript ->
                              fun ?encodedInputTranscript ->
                                fun ?audioStream ->
                                  fun ?botVersion ->
                                    fun ?sessionId ->
                                      fun ?activeContexts ->
                                        fun () ->
                                          {
                                            contentType;
                                            intentName;
                                            nluIntentConfidence;
                                            alternativeIntents;
                                            slots;
                                            sessionAttributes;
                                            sentimentResponse;
                                            message;
                                            encodedMessage;
                                            messageFormat;
                                            dialogState;
                                            slotToElicit;
                                            inputTranscript;
                                            encodedInputTranscript;
                                            audioStream;
                                            botVersion;
                                            sessionId;
                                            activeContexts
                                          }
    let error_of_json name json =
      match name with
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_json json)
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_json json)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "LoopDetectedException" ->
          `LoopDetectedException (LoopDetectedException.of_json json)
      | "NotAcceptableException" ->
          `NotAcceptableException (NotAcceptableException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "RequestTimeoutException" ->
          `RequestTimeoutException (RequestTimeoutException.of_json json)
      | "UnsupportedMediaTypeException" ->
          `UnsupportedMediaTypeException
            (UnsupportedMediaTypeException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadGatewayException" ->
          `BadGatewayException (BadGatewayException.of_xml xml)
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "DependencyFailedException" ->
          `DependencyFailedException (DependencyFailedException.of_xml xml)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "LoopDetectedException" ->
          `LoopDetectedException (LoopDetectedException.of_xml xml)
      | "NotAcceptableException" ->
          `NotAcceptableException (NotAcceptableException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "RequestTimeoutException" ->
          `RequestTimeoutException (RequestTimeoutException.of_xml xml)
      | "UnsupportedMediaTypeException" ->
          `UnsupportedMediaTypeException
            (UnsupportedMediaTypeException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadGatewayException e ->
          `Assoc
            [("error", (`String "BadGatewayException"));
            ("details", (BadGatewayException.to_json e))]
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `DependencyFailedException e ->
          `Assoc
            [("error", (`String "DependencyFailedException"));
            ("details", (DependencyFailedException.to_json e))]
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `LoopDetectedException e ->
          `Assoc
            [("error", (`String "LoopDetectedException"));
            ("details", (LoopDetectedException.to_json e))]
      | `NotAcceptableException e ->
          `Assoc
            [("error", (`String "NotAcceptableException"));
            ("details", (NotAcceptableException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `RequestTimeoutException e ->
          `Assoc
            [("error", (`String "RequestTimeoutException"));
            ("details", (RequestTimeoutException.to_json e))]
      | `UnsupportedMediaTypeException e ->
          `Assoc
            [("error", (`String "UnsupportedMediaTypeException"));
            ("details", (UnsupportedMediaTypeException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body =
      ((fun (xs, pipe) ->
          make
            ?contentType:(Option.map
                            ((List.Assoc.find ~equal:String.Caseless.equal)
                               xs "Content-Type")
                            ~f:HttpContentType.of_string)
            ?intentName:(Option.map
                           ((List.Assoc.find ~equal:String.Caseless.equal) xs
                              "x-amz-lex-intent-name")
                           ~f:IntentName.of_string)
            ?nluIntentConfidence:(Option.map
                                    ((List.Assoc.find
                                        ~equal:String.Caseless.equal) xs
                                       "x-amz-lex-nlu-intent-confidence")
                                    ~f:String_.of_string)
            ?alternativeIntents:(Option.map
                                   ((List.Assoc.find
                                       ~equal:String.Caseless.equal) xs
                                      "x-amz-lex-alternative-intents")
                                   ~f:String_.of_string)
            ?slots:(Option.map
                      ((List.Assoc.find ~equal:String.Caseless.equal) xs
                         "x-amz-lex-slots") ~f:String_.of_string)
            ?sessionAttributes:(Option.map
                                  ((List.Assoc.find
                                      ~equal:String.Caseless.equal) xs
                                     "x-amz-lex-session-attributes")
                                  ~f:String_.of_string)
            ?sentimentResponse:(Option.map
                                  ((List.Assoc.find
                                      ~equal:String.Caseless.equal) xs
                                     "x-amz-lex-sentiment")
                                  ~f:String_.of_string)
            ?message:(Option.map
                        ((List.Assoc.find ~equal:String.Caseless.equal) xs
                           "x-amz-lex-message") ~f:Text.of_string)
            ?encodedMessage:(Option.map
                               ((List.Assoc.find ~equal:String.Caseless.equal)
                                  xs "x-amz-lex-encoded-message")
                               ~f:SensitiveString.of_string)
            ?messageFormat:(Option.map
                              ((List.Assoc.find ~equal:String.Caseless.equal)
                                 xs "x-amz-lex-message-format")
                              ~f:MessageFormatType.of_string)
            ?dialogState:(Option.map
                            ((List.Assoc.find ~equal:String.Caseless.equal)
                               xs "x-amz-lex-dialog-state")
                            ~f:DialogState.of_string)
            ?slotToElicit:(Option.map
                             ((List.Assoc.find ~equal:String.Caseless.equal)
                                xs "x-amz-lex-slot-to-elicit")
                             ~f:String_.of_string)
            ?inputTranscript:(Option.map
                                ((List.Assoc.find
                                    ~equal:String.Caseless.equal) xs
                                   "x-amz-lex-input-transcript")
                                ~f:String_.of_string)
            ?encodedInputTranscript:(Option.map
                                       ((List.Assoc.find
                                           ~equal:String.Caseless.equal) xs
                                          "x-amz-lex-encoded-input-transcript")
                                       ~f:SensitiveStringUnbounded.of_string)
            ?audioStream:(Some pipe)
            ?botVersion:(Option.map
                           ((List.Assoc.find ~equal:String.Caseless.equal) xs
                              "x-amz-lex-bot-version")
                           ~f:BotVersion.of_string)
            ?sessionId:(Option.map
                          ((List.Assoc.find ~equal:String.Caseless.equal) xs
                             "x-amz-lex-session-id") ~f:String_.of_string)
            ?activeContexts:(Option.map
                               ((List.Assoc.find ~equal:String.Caseless.equal)
                                  xs "x-amz-lex-active-contexts")
                               ~f:ActiveContextsString.of_string) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("Content-Type",
           (Option.map x.contentType ~f:HttpContentType.to_value));
        ("x-amz-lex-intent-name",
          (Option.map x.intentName ~f:IntentName.to_value));
        ("x-amz-lex-nlu-intent-confidence",
          (Option.map x.nluIntentConfidence ~f:String_.to_value));
        ("x-amz-lex-alternative-intents",
          (Option.map x.alternativeIntents ~f:String_.to_value));
        ("x-amz-lex-slots", (Option.map x.slots ~f:String_.to_value));
        ("x-amz-lex-session-attributes",
          (Option.map x.sessionAttributes ~f:String_.to_value));
        ("x-amz-lex-sentiment",
          (Option.map x.sentimentResponse ~f:String_.to_value));
        ("x-amz-lex-message", (Option.map x.message ~f:Text.to_value));
        ("x-amz-lex-encoded-message",
          (Option.map x.encodedMessage ~f:SensitiveString.to_value));
        ("x-amz-lex-message-format",
          (Option.map x.messageFormat ~f:MessageFormatType.to_value));
        ("x-amz-lex-dialog-state",
          (Option.map x.dialogState ~f:DialogState.to_value));
        ("x-amz-lex-slot-to-elicit",
          (Option.map x.slotToElicit ~f:String_.to_value));
        ("x-amz-lex-input-transcript",
          (Option.map x.inputTranscript ~f:String_.to_value));
        ("x-amz-lex-encoded-input-transcript",
          (Option.map x.encodedInputTranscript
             ~f:SensitiveStringUnbounded.to_value));
        ("audioStream", (Option.map x.audioStream ~f:BlobStream.to_value));
        ("x-amz-lex-bot-version",
          (Option.map x.botVersion ~f:BotVersion.to_value));
        ("x-amz-lex-session-id",
          (Option.map x.sessionId ~f:String_.to_value));
        ("x-amz-lex-active-contexts",
          (Option.map x.activeContexts ~f:ActiveContextsString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let activeContexts =
        (Option.map ~f:ActiveContextsString.of_xml)
          (Xml.child xml_arg0 "x-amz-lex-active-contexts") in
      let sessionId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amz-lex-session-id") in
      let botVersion =
        (Option.map ~f:BotVersion.of_xml)
          (Xml.child xml_arg0 "x-amz-lex-bot-version") in
      let audioStream =
        (Option.map ~f:BlobStream.of_xml) (Xml.child xml_arg0 "audioStream") in
      let encodedInputTranscript =
        (Option.map ~f:SensitiveStringUnbounded.of_xml)
          (Xml.child xml_arg0 "x-amz-lex-encoded-input-transcript") in
      let inputTranscript =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amz-lex-input-transcript") in
      let slotToElicit =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amz-lex-slot-to-elicit") in
      let dialogState =
        (Option.map ~f:DialogState.of_xml)
          (Xml.child xml_arg0 "x-amz-lex-dialog-state") in
      let messageFormat =
        (Option.map ~f:MessageFormatType.of_xml)
          (Xml.child xml_arg0 "x-amz-lex-message-format") in
      let encodedMessage =
        (Option.map ~f:SensitiveString.of_xml)
          (Xml.child xml_arg0 "x-amz-lex-encoded-message") in
      let message =
        (Option.map ~f:Text.of_xml) (Xml.child xml_arg0 "x-amz-lex-message") in
      let sentimentResponse =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amz-lex-sentiment") in
      let sessionAttributes =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amz-lex-session-attributes") in
      let slots =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "x-amz-lex-slots") in
      let alternativeIntents =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amz-lex-alternative-intents") in
      let nluIntentConfidence =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amz-lex-nlu-intent-confidence") in
      let intentName =
        (Option.map ~f:IntentName.of_xml)
          (Xml.child xml_arg0 "x-amz-lex-intent-name") in
      let contentType =
        (Option.map ~f:HttpContentType.of_xml)
          (Xml.child xml_arg0 "Content-Type") in
      make ?activeContexts ?sessionId ?botVersion ?audioStream
        ?encodedInputTranscript ?inputTranscript ?slotToElicit ?dialogState
        ?messageFormat ?encodedMessage ?message ?sentimentResponse
        ?sessionAttributes ?slots ?alternativeIntents ?nluIntentConfidence
        ?intentName ?contentType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let activeContexts =
        field_map json__ "activeContexts" ActiveContextsString.of_json in
      let sessionId = field_map json__ "sessionId" String_.of_json in
      let botVersion = field_map json__ "botVersion" BotVersion.of_json in
      let audioStream = field_map json__ "audioStream" BlobStream.of_json in
      let encodedInputTranscript =
        field_map json__ "encodedInputTranscript"
          SensitiveStringUnbounded.of_json in
      let inputTranscript =
        field_map json__ "inputTranscript" String_.of_json in
      let slotToElicit = field_map json__ "slotToElicit" String_.of_json in
      let dialogState = field_map json__ "dialogState" DialogState.of_json in
      let messageFormat =
        field_map json__ "messageFormat" MessageFormatType.of_json in
      let encodedMessage =
        field_map json__ "encodedMessage" SensitiveString.of_json in
      let message = field_map json__ "message" Text.of_json in
      let sentimentResponse =
        field_map json__ "sentimentResponse" String_.of_json in
      let sessionAttributes =
        field_map json__ "sessionAttributes" String_.of_json in
      let slots = field_map json__ "slots" String_.of_json in
      let alternativeIntents =
        field_map json__ "alternativeIntents" String_.of_json in
      let nluIntentConfidence =
        field_map json__ "nluIntentConfidence" String_.of_json in
      let intentName = field_map json__ "intentName" IntentName.of_json in
      let contentType =
        field_map json__ "contentType" HttpContentType.of_json in
      make ?activeContexts ?sessionId ?botVersion ?audioStream
        ?encodedInputTranscript ?inputTranscript ?slotToElicit ?dialogState
        ?messageFormat ?encodedMessage ?message ?sentimentResponse
        ?sessionAttributes ?slots ?alternativeIntents ?nluIntentConfidence
        ?intentName ?contentType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Sends user input (text or speech) to Amazon Lex. Clients use this API to send text and audio requests to Amazon Lex at runtime. Amazon Lex interprets the user input using the machine learning model that it built for the bot. The PostContent operation supports audio input at 8kHz and 16kHz. You can use 8kHz audio to achieve higher speech recognition accuracy in telephone audio applications. In response, Amazon Lex returns the next message to convey to the user. Consider the following example messages: For a user input \"I would like a pizza,\" Amazon Lex might return a response with a message eliciting slot data (for example, PizzaSize): \"What size pizza would you like?\". After the user provides all of the pizza order information, Amazon Lex might return a response with a message to get user confirmation: \"Order the pizza?\". After the user replies \"Yes\" to the confirmation prompt, Amazon Lex might return a conclusion statement: \"Thank you, your cheese pizza has been ordered.\". Not all Amazon Lex messages require a response from the user. For example, conclusion statements do not require a response. Some messages require only a yes or no response. In addition to the message, Amazon Lex provides additional context about the message in the response that you can use to enhance client behavior, such as displaying the appropriate client user interface. Consider the following examples: If the message is to elicit slot data, Amazon Lex returns the following context information: x-amz-lex-dialog-state header set to ElicitSlot x-amz-lex-intent-name header set to the intent name in the current context x-amz-lex-slot-to-elicit header set to the slot name for which the message is eliciting information x-amz-lex-slots header set to a map of slots configured for the intent with their current values If the message is a confirmation prompt, the x-amz-lex-dialog-state header is set to Confirmation and the x-amz-lex-slot-to-elicit header is omitted. If the message is a clarification prompt configured for the intent, indicating that the user intent is not understood, the x-amz-dialog-state header is set to ElicitIntent and the x-amz-slot-to-elicit header is omitted. In addition, Amazon Lex also returns your application-specific sessionAttributes. For more information, see Managing Conversation Context."]
module PostContentRequest =
  struct
    type nonrec t =
      {
      botName: BotName.t [@ocaml.doc "Name of the Amazon Lex bot."];
      botAlias: BotAlias.t [@ocaml.doc "Alias of the Amazon Lex bot."];
      userId: UserId.t
        [@ocaml.doc
          "The ID of the client application user. Amazon Lex uses this to identify a user's conversation with your bot. At runtime, each request must contain the userID field. To decide the user ID to use for your application, consider the following factors. The userID field must not contain any personally identifiable information of the user, for example, name, personal identification numbers, or other end user personal information. If you want a user to start a conversation on one device and continue on another device, use a user-specific identifier. If you want the same user to be able to have two independent conversations on two different devices, choose a device-specific identifier. A user can't have two independent conversations with two different versions of the same bot. For example, a user can't have a conversation with the PROD and BETA versions of the same bot. If you anticipate that a user will need to have conversation with two different versions, for example, while testing, include the bot alias in the user ID to separate the two conversations."];
      sessionAttributes: AttributesString.t option
        [@ocaml.doc
          "You pass this value as the x-amz-lex-session-attributes HTTP header. Application-specific information passed between Amazon Lex and a client application. The value must be a JSON serialized and base64 encoded map with string keys and values. The total size of the sessionAttributes and requestAttributes headers is limited to 12 KB. For more information, see Setting Session Attributes."];
      requestAttributes: AttributesString.t option
        [@ocaml.doc
          "You pass this value as the x-amz-lex-request-attributes HTTP header. Request-specific information passed between Amazon Lex and a client application. The value must be a JSON serialized and base64 encoded map with string keys and values. The total size of the requestAttributes and sessionAttributes headers is limited to 12 KB. The namespace x-amz-lex: is reserved for special attributes. Don't create any request attributes with the prefix x-amz-lex:. For more information, see Setting Request Attributes."];
      contentType: HttpContentType.t
        [@ocaml.doc
          "You pass this value as the Content-Type HTTP header. Indicates the audio format or text. The header value must start with one of the following prefixes: PCM format, audio data must be in little-endian byte order. audio/l16; rate=16000; channels=1 audio/x-l16; sample-rate=16000; channel-count=1 audio/lpcm; sample-rate=8000; sample-size-bits=16; channel-count=1; is-big-endian=false Opus format audio/x-cbr-opus-with-preamble; preamble-size=0; bit-rate=256000; frame-size-milliseconds=4 Text format text/plain; charset=utf-8"];
      accept: Accept.t option
        [@ocaml.doc
          "You pass this value as the Accept HTTP header. The message Amazon Lex returns in the response can be either text or speech based on the Accept HTTP header value in the request. If the value is text/plain; charset=utf-8, Amazon Lex returns text in the response. If the value begins with audio/, Amazon Lex returns speech in the response. Amazon Lex uses Amazon Polly to generate the speech (using the configuration you specified in the Accept header). For example, if you specify audio/mpeg as the value, Amazon Lex returns speech in the MPEG format. If the value is audio/pcm, the speech returned is audio/pcm in 16-bit, little endian format. The following are the accepted values: audio/mpeg audio/ogg audio/pcm text/plain; charset=utf-8 audio/* (defaults to mpeg)"];
      inputStream: BlobStream.t
        [@ocaml.doc
          "User input in PCM or Opus audio format or text format as described in the Content-Type HTTP header. You can stream audio data to Amazon Lex or you can create a local buffer that captures all of the audio data before sending. In general, you get better performance if you stream audio data rather than buffering the data locally."];
      activeContexts: ActiveContextsString.t option
        [@ocaml.doc
          "A list of contexts active for the request. A context can be activated when a previous intent is fulfilled, or by including the context in the request, If you don't specify a list of contexts, Amazon Lex will use the current list of contexts for the session. If you specify an empty list, all contexts for the session are cleared."]}
    let context_ = "PostContentRequest"
    let make ?sessionAttributes =
      fun ?requestAttributes ->
        fun ?accept ->
          fun ?activeContexts ->
            fun ~botName ->
              fun ~botAlias ->
                fun ~userId ->
                  fun ~contentType ->
                    fun ~inputStream ->
                      fun () ->
                        {
                          sessionAttributes;
                          requestAttributes;
                          accept;
                          activeContexts;
                          botName;
                          botAlias;
                          userId;
                          contentType;
                          inputStream
                        }
    let of_header_and_body =
      ((fun (xs, pipe) ->
          make
            ~botName:(BotName.of_string
                        ((List.Assoc.find_exn ~equal:String.Caseless.equal)
                           xs "botName"))
            ~botAlias:(BotAlias.of_string
                         ((List.Assoc.find_exn ~equal:String.Caseless.equal)
                            xs "botAlias"))
            ~userId:(UserId.of_string
                       ((List.Assoc.find_exn ~equal:String.Caseless.equal) xs
                          "userId"))
            ?sessionAttributes:(Option.map
                                  ((List.Assoc.find
                                      ~equal:String.Caseless.equal) xs
                                     "x-amz-lex-session-attributes")
                                  ~f:AttributesString.of_string)
            ?requestAttributes:(Option.map
                                  ((List.Assoc.find
                                      ~equal:String.Caseless.equal) xs
                                     "x-amz-lex-request-attributes")
                                  ~f:AttributesString.of_string)
            ~contentType:(HttpContentType.of_string
                            ((List.Assoc.find_exn
                                ~equal:String.Caseless.equal) xs
                               "Content-Type"))
            ?accept:(Option.map
                       ((List.Assoc.find ~equal:String.Caseless.equal) xs
                          "Accept") ~f:Accept.of_string) ~inputStream:pipe
            ?activeContexts:(Option.map
                               ((List.Assoc.find ~equal:String.Caseless.equal)
                                  xs "x-amz-lex-active-contexts")
                               ~f:ActiveContextsString.of_string) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("botName", (Some (BotName.to_value x.botName)));
        ("botAlias", (Some (BotAlias.to_value x.botAlias)));
        ("userId", (Some (UserId.to_value x.userId)));
        ("x-amz-lex-session-attributes",
          (Option.map x.sessionAttributes ~f:AttributesString.to_value));
        ("x-amz-lex-request-attributes",
          (Option.map x.requestAttributes ~f:AttributesString.to_value));
        ("Content-Type", (Some (HttpContentType.to_value x.contentType)));
        ("Accept", (Option.map x.accept ~f:Accept.to_value));
        ("inputStream", (Some (BlobStream.to_value x.inputStream)));
        ("x-amz-lex-active-contexts",
          (Option.map x.activeContexts ~f:ActiveContextsString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let activeContexts =
        (Option.map ~f:ActiveContextsString.of_xml)
          (Xml.child xml_arg0 "x-amz-lex-active-contexts") in
      let inputStream =
        BlobStream.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "inputStream") in
      let accept =
        (Option.map ~f:Accept.of_xml) (Xml.child xml_arg0 "Accept") in
      let contentType =
        HttpContentType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Content-Type") in
      let requestAttributes =
        (Option.map ~f:AttributesString.of_xml)
          (Xml.child xml_arg0 "x-amz-lex-request-attributes") in
      let sessionAttributes =
        (Option.map ~f:AttributesString.of_xml)
          (Xml.child xml_arg0 "x-amz-lex-session-attributes") in
      let userId =
        UserId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "userId") in
      let botAlias =
        BotAlias.of_xml (Xml.child_exn ~context:context_ xml_arg0 "botAlias") in
      let botName =
        BotName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "botName") in
      make ?activeContexts ~inputStream ?accept ~contentType
        ?requestAttributes ?sessionAttributes ~userId ~botAlias ~botName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let activeContexts =
        field_map json__ "activeContexts" ActiveContextsString.of_json in
      let inputStream = field_map_exn json__ "inputStream" BlobStream.of_json in
      let accept = field_map json__ "accept" Accept.of_json in
      let contentType =
        field_map_exn json__ "contentType" HttpContentType.of_json in
      let requestAttributes =
        field_map json__ "requestAttributes" AttributesString.of_json in
      let sessionAttributes =
        field_map json__ "sessionAttributes" AttributesString.of_json in
      let userId = field_map_exn json__ "userId" UserId.of_json in
      let botAlias = field_map_exn json__ "botAlias" BotAlias.of_json in
      let botName = field_map_exn json__ "botName" BotName.of_json in
      make ?activeContexts ~inputStream ?accept ~contentType
        ?requestAttributes ?sessionAttributes ~userId ~botAlias ~botName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Sends user input (text or speech) to Amazon Lex. Clients use this API to send text and audio requests to Amazon Lex at runtime. Amazon Lex interprets the user input using the machine learning model that it built for the bot. The PostContent operation supports audio input at 8kHz and 16kHz. You can use 8kHz audio to achieve higher speech recognition accuracy in telephone audio applications. In response, Amazon Lex returns the next message to convey to the user. Consider the following example messages: For a user input \"I would like a pizza,\" Amazon Lex might return a response with a message eliciting slot data (for example, PizzaSize): \"What size pizza would you like?\". After the user provides all of the pizza order information, Amazon Lex might return a response with a message to get user confirmation: \"Order the pizza?\". After the user replies \"Yes\" to the confirmation prompt, Amazon Lex might return a conclusion statement: \"Thank you, your cheese pizza has been ordered.\". Not all Amazon Lex messages require a response from the user. For example, conclusion statements do not require a response. Some messages require only a yes or no response. In addition to the message, Amazon Lex provides additional context about the message in the response that you can use to enhance client behavior, such as displaying the appropriate client user interface. Consider the following examples: If the message is to elicit slot data, Amazon Lex returns the following context information: x-amz-lex-dialog-state header set to ElicitSlot x-amz-lex-intent-name header set to the intent name in the current context x-amz-lex-slot-to-elicit header set to the slot name for which the message is eliciting information x-amz-lex-slots header set to a map of slots configured for the intent with their current values If the message is a confirmation prompt, the x-amz-lex-dialog-state header is set to Confirmation and the x-amz-lex-slot-to-elicit header is omitted. If the message is a clarification prompt configured for the intent, indicating that the user intent is not understood, the x-amz-dialog-state header is set to ElicitIntent and the x-amz-slot-to-elicit header is omitted. In addition, Amazon Lex also returns your application-specific sessionAttributes. For more information, see Managing Conversation Context."]
module GetSessionResponse =
  struct
    type nonrec t =
      {
      recentIntentSummaryView: IntentSummaryList.t option
        [@ocaml.doc
          "An array of information about the intents used in the session. The array can contain a maximum of three summaries. If more than three intents are used in the session, the recentIntentSummaryView operation contains information about the last three intents used. If you set the checkpointLabelFilter parameter in the request, the array contains only the intents with the specified label."];
      sessionAttributes: StringMap.t option
        [@ocaml.doc
          "Map of key/value pairs representing the session-specific context information. It contains application information passed between Amazon Lex and a client application."];
      sessionId: String_.t option
        [@ocaml.doc "A unique identifier for the session."];
      dialogAction: DialogAction.t option
        [@ocaml.doc "Describes the current state of the bot."];
      activeContexts: ActiveContextsList.t option
        [@ocaml.doc
          "A list of active contexts for the session. A context can be set when an intent is fulfilled or by calling the PostContent, PostText, or PutSession operation. You can use a context to control the intents that can follow up an intent, or to modify the operation of your application."]}
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `InternalFailureException of InternalFailureException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?recentIntentSummaryView =
      fun ?sessionAttributes ->
        fun ?sessionId ->
          fun ?dialogAction ->
            fun ?activeContexts ->
              fun () ->
                {
                  recentIntentSummaryView;
                  sessionAttributes;
                  sessionId;
                  dialogAction;
                  activeContexts
                }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("recentIntentSummaryView",
           (Option.map x.recentIntentSummaryView
              ~f:IntentSummaryList.to_value));
        ("sessionAttributes",
          (Option.map x.sessionAttributes ~f:StringMap.to_value));
        ("sessionId", (Option.map x.sessionId ~f:String_.to_value));
        ("dialogAction",
          (Option.map x.dialogAction ~f:DialogAction.to_value));
        ("activeContexts",
          (Option.map x.activeContexts ~f:ActiveContextsList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let activeContexts =
        (Option.map ~f:ActiveContextsList.of_xml)
          (Xml.child xml_arg0 "activeContexts") in
      let dialogAction =
        (Option.map ~f:DialogAction.of_xml)
          (Xml.child xml_arg0 "dialogAction") in
      let sessionId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "sessionId") in
      let sessionAttributes =
        (Option.map ~f:StringMap.of_xml)
          (Xml.child xml_arg0 "sessionAttributes") in
      let recentIntentSummaryView =
        (Option.map ~f:IntentSummaryList.of_xml)
          (Xml.child xml_arg0 "recentIntentSummaryView") in
      make ?activeContexts ?dialogAction ?sessionId ?sessionAttributes
        ?recentIntentSummaryView ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let activeContexts =
        field_map json__ "activeContexts" ActiveContextsList.of_json in
      let dialogAction = field_map json__ "dialogAction" DialogAction.of_json in
      let sessionId = field_map json__ "sessionId" String_.of_json in
      let sessionAttributes =
        field_map json__ "sessionAttributes" StringMap.of_json in
      let recentIntentSummaryView =
        field_map json__ "recentIntentSummaryView" IntentSummaryList.of_json in
      make ?activeContexts ?dialogAction ?sessionId ?sessionAttributes
        ?recentIntentSummaryView ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns session information for a specified bot, alias, and user ID."]
module GetSessionRequest =
  struct
    type nonrec t =
      {
      botName: BotName.t
        [@ocaml.doc "The name of the bot that contains the session data."];
      botAlias: BotAlias.t
        [@ocaml.doc
          "The alias in use for the bot that contains the session data."];
      userId: UserId.t
        [@ocaml.doc
          "The ID of the client application user. Amazon Lex uses this to identify a user's conversation with your bot."];
      checkpointLabelFilter: IntentSummaryCheckpointLabel.t option
        [@ocaml.doc
          "A string used to filter the intents returned in the recentIntentSummaryView structure. When you specify a filter, only intents with their checkpointLabel field set to that string are returned."]}
    let context_ = "GetSessionRequest"
    let make ?checkpointLabelFilter =
      fun ~botName ->
        fun ~botAlias ->
          fun ~userId ->
            fun () -> { checkpointLabelFilter; botName; botAlias; userId }
    let to_value x =
      structure_to_value
        [("botName", (Some (BotName.to_value x.botName)));
        ("botAlias", (Some (BotAlias.to_value x.botAlias)));
        ("userId", (Some (UserId.to_value x.userId)));
        ("checkpointLabelFilter",
          (Option.map x.checkpointLabelFilter
             ~f:IntentSummaryCheckpointLabel.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let checkpointLabelFilter =
        (Option.map ~f:IntentSummaryCheckpointLabel.of_xml)
          (Xml.child xml_arg0 "checkpointLabelFilter") in
      let userId =
        UserId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "userId") in
      let botAlias =
        BotAlias.of_xml (Xml.child_exn ~context:context_ xml_arg0 "botAlias") in
      let botName =
        BotName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "botName") in
      make ?checkpointLabelFilter ~userId ~botAlias ~botName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let checkpointLabelFilter =
        field_map json__ "checkpointLabelFilter"
          IntentSummaryCheckpointLabel.of_json in
      let userId = field_map_exn json__ "userId" UserId.of_json in
      let botAlias = field_map_exn json__ "botAlias" BotAlias.of_json in
      let botName = field_map_exn json__ "botName" BotName.of_json in
      make ?checkpointLabelFilter ~userId ~botAlias ~botName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns session information for a specified bot, alias, and user ID."]
module DeleteSessionResponse =
  struct
    type nonrec t =
      {
      botName: BotName.t option
        [@ocaml.doc "The name of the bot associated with the session data."];
      botAlias: BotAlias.t option
        [@ocaml.doc
          "The alias in use for the bot associated with the session data."];
      userId: UserId.t option
        [@ocaml.doc "The ID of the client application user."];
      sessionId: String_.t option
        [@ocaml.doc "The unique identifier for the session."]}
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `ConflictException of ConflictException.t 
      | `InternalFailureException of InternalFailureException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?botName =
      fun ?botAlias ->
        fun ?userId ->
          fun ?sessionId ->
            fun () -> { botName; botAlias; userId; sessionId }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("botName", (Option.map x.botName ~f:BotName.to_value));
        ("botAlias", (Option.map x.botAlias ~f:BotAlias.to_value));
        ("userId", (Option.map x.userId ~f:UserId.to_value));
        ("sessionId", (Option.map x.sessionId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sessionId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "sessionId") in
      let userId =
        (Option.map ~f:UserId.of_xml) (Xml.child xml_arg0 "userId") in
      let botAlias =
        (Option.map ~f:BotAlias.of_xml) (Xml.child xml_arg0 "botAlias") in
      let botName =
        (Option.map ~f:BotName.of_xml) (Xml.child xml_arg0 "botName") in
      make ?sessionId ?userId ?botAlias ?botName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sessionId = field_map json__ "sessionId" String_.of_json in
      let userId = field_map json__ "userId" UserId.of_json in
      let botAlias = field_map json__ "botAlias" BotAlias.of_json in
      let botName = field_map json__ "botName" BotName.of_json in
      make ?sessionId ?userId ?botAlias ?botName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes session information for a specified bot, alias, and user ID."]
module DeleteSessionRequest =
  struct
    type nonrec t =
      {
      botName: BotName.t
        [@ocaml.doc "The name of the bot that contains the session data."];
      botAlias: BotAlias.t
        [@ocaml.doc
          "The alias in use for the bot that contains the session data."];
      userId: UserId.t
        [@ocaml.doc
          "The identifier of the user associated with the session data."]}
    let context_ = "DeleteSessionRequest"
    let make ~botName =
      fun ~botAlias -> fun ~userId -> fun () -> { botName; botAlias; userId }
    let to_value x =
      structure_to_value
        [("botName", (Some (BotName.to_value x.botName)));
        ("botAlias", (Some (BotAlias.to_value x.botAlias)));
        ("userId", (Some (UserId.to_value x.userId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let userId =
        UserId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "userId") in
      let botAlias =
        BotAlias.of_xml (Xml.child_exn ~context:context_ xml_arg0 "botAlias") in
      let botName =
        BotName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "botName") in
      make ~userId ~botAlias ~botName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let userId = field_map_exn json__ "userId" UserId.of_json in
      let botAlias = field_map_exn json__ "botAlias" BotAlias.of_json in
      let botName = field_map_exn json__ "botName" BotName.of_json in
      make ~userId ~botAlias ~botName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes session information for a specified bot, alias, and user ID."]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let delete_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and botName = flag "bot-name" (required string) ~doc:"STRING BotName"
       and botAlias =
         flag "bot-alias" (required string) ~doc:"STRING BotAlias"
       and userId = flag "user-id" (required string) ~doc:"STRING UserId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_session
           (Values.DeleteSessionRequest.make ~botName ~botAlias ~userId ())
           (Some Values.DeleteSessionResponse.to_json)
           (Some Values.DeleteSessionResponse.error_to_json)])
let get_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and checkpointLabelFilter =
         flag "checkpoint-label-filter" (optional string)
           ~doc:"STRING IntentSummaryCheckpointLabel"
       and botName = flag "bot-name" (required string) ~doc:"STRING BotName"
       and botAlias =
         flag "bot-alias" (required string) ~doc:"STRING BotAlias"
       and userId = flag "user-id" (required string) ~doc:"STRING UserId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_session
           (Values.GetSessionRequest.make ?checkpointLabelFilter ~botName
              ~botAlias ~userId ()) (Some Values.GetSessionResponse.to_json)
           (Some Values.GetSessionResponse.error_to_json)])
let post_content =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sessionAttributes =
         flag "session-attributes" (optional string)
           ~doc:"STRING AttributesString"
       and requestAttributes =
         flag "request-attributes" (optional string)
           ~doc:"STRING AttributesString"
       and accept = flag "accept" (optional string) ~doc:"STRING Accept"
       and activeContexts =
         flag "active-contexts" (optional string)
           ~doc:"STRING ActiveContextsString"
       and botName = flag "bot-name" (required string) ~doc:"STRING BotName"
       and botAlias =
         flag "bot-alias" (required string) ~doc:"STRING BotAlias"
       and userId = flag "user-id" (required string) ~doc:"STRING UserId"
       and contentType =
         flag "content-type" (required string) ~doc:"STRING HttpContentType"
       and inputStream =
         flag "input-stream" (required json_arg) ~doc:"JSON BlobStream" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.post_content
           (Values.PostContentRequest.make ?sessionAttributes
              ?requestAttributes ?accept ?activeContexts ~botName ~botAlias
              ~userId ~contentType
              ~inputStream:(Values.BlobStream.of_json inputStream) ())
           (Some Values.PostContentResponse.to_json)
           (Some Values.PostContentResponse.error_to_json)])
let post_text =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sessionAttributes =
         flag "session-attributes" (optional json_arg) ~doc:"JSON StringMap"
       and requestAttributes =
         flag "request-attributes" (optional json_arg) ~doc:"JSON StringMap"
       and activeContexts =
         flag "active-contexts" (optional json_arg)
           ~doc:"JSON ActiveContextsList"
       and botName = flag "bot-name" (required string) ~doc:"STRING BotName"
       and botAlias =
         flag "bot-alias" (required string) ~doc:"STRING BotAlias"
       and userId = flag "user-id" (required string) ~doc:"STRING UserId"
       and inputText = flag "input-text" (required string) ~doc:"STRING Text" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.post_text
           (Values.PostTextRequest.make
              ?sessionAttributes:(Option.map ~f:Values.StringMap.of_json
                                    sessionAttributes)
              ?requestAttributes:(Option.map ~f:Values.StringMap.of_json
                                    requestAttributes)
              ?activeContexts:(Option.map
                                 ~f:Values.ActiveContextsList.of_json
                                 activeContexts) ~botName ~botAlias ~userId
              ~inputText ()) (Some Values.PostTextResponse.to_json)
           (Some Values.PostTextResponse.error_to_json)])
let put_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sessionAttributes =
         flag "session-attributes" (optional json_arg) ~doc:"JSON StringMap"
       and dialogAction =
         flag "dialog-action" (optional json_arg) ~doc:"JSON DialogAction"
       and recentIntentSummaryView =
         flag "recent-intent-summary-view" (optional json_arg)
           ~doc:"JSON IntentSummaryList"
       and accept = flag "accept" (optional string) ~doc:"STRING Accept"
       and activeContexts =
         flag "active-contexts" (optional json_arg)
           ~doc:"JSON ActiveContextsList"
       and botName = flag "bot-name" (required string) ~doc:"STRING BotName"
       and botAlias =
         flag "bot-alias" (required string) ~doc:"STRING BotAlias"
       and userId = flag "user-id" (required string) ~doc:"STRING UserId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_session
           (Values.PutSessionRequest.make
              ?sessionAttributes:(Option.map ~f:Values.StringMap.of_json
                                    sessionAttributes)
              ?dialogAction:(Option.map ~f:Values.DialogAction.of_json
                               dialogAction)
              ?recentIntentSummaryView:(Option.map
                                          ~f:Values.IntentSummaryList.of_json
                                          recentIntentSummaryView) ?accept
              ?activeContexts:(Option.map
                                 ~f:Values.ActiveContextsList.of_json
                                 activeContexts) ~botName ~botAlias ~userId
              ()) (Some Values.PutSessionResponse.to_json)
           (Some Values.PutSessionResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("delete-session", delete_session);
    ("get-session", get_session);
    ("post-content", post_content);
    ("post-text", post_text);
    ("put-session", put_session)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | DeleteSession: (DeleteSessionRequest.t, DeleteSessionResponse.t,
  DeleteSessionResponse.error) t 
  | GetSession: (GetSessionRequest.t, GetSessionResponse.t,
  GetSessionResponse.error) t 
  | PostContent: (PostContentRequest.t, PostContentResponse.t,
  PostContentResponse.error) t 
  | PostText: (PostTextRequest.t, PostTextResponse.t, PostTextResponse.error)
  t 
  | PutSession: (PutSessionRequest.t, PutSessionResponse.t,
  PutSessionResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | DeleteSession -> `DELETE
  | GetSession -> `GET
  | PostContent -> `POST
  | PostText -> `POST
  | PutSession -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | DeleteSession ->
          (Format.kasprintf Uri.of_string) "/bot/%s/alias/%s/user/%s/session"
            (BotName.to_header x.DeleteSessionRequest.botName)
            (BotAlias.to_header x.DeleteSessionRequest.botAlias)
            (UserId.to_header x.DeleteSessionRequest.userId)
      | GetSession ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string)
               "/bot/%s/alias/%s/user/%s/session/"
               (BotName.to_header x.GetSessionRequest.botName)
               (BotAlias.to_header x.GetSessionRequest.botAlias)
               (UserId.to_header x.GetSessionRequest.userId))
            (List.filter_opt
               [Option.map
                  ~f:(fun v ->
                        ("checkpointLabelFilter",
                          (IntentSummaryCheckpointLabel.to_header v)))
                  x.checkpointLabelFilter])
      | PostContent ->
          (Format.kasprintf Uri.of_string) "/bot/%s/alias/%s/user/%s/content"
            (BotName.to_header x.PostContentRequest.botName)
            (BotAlias.to_header x.PostContentRequest.botAlias)
            (UserId.to_header x.PostContentRequest.userId)
      | PostText ->
          (Format.kasprintf Uri.of_string) "/bot/%s/alias/%s/user/%s/text"
            (BotName.to_header x.PostTextRequest.botName)
            (BotAlias.to_header x.PostTextRequest.botAlias)
            (UserId.to_header x.PostTextRequest.userId)
      | PutSession ->
          (Format.kasprintf Uri.of_string) "/bot/%s/alias/%s/user/%s/session"
            (BotName.to_header x.PutSessionRequest.botName)
            (BotAlias.to_header x.PutSessionRequest.botAlias)
            (UserId.to_header x.PutSessionRequest.userId))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | DeleteSession -> Awso.Http.Request.make (method_of_endpoint endp)
  | GetSession ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | PostContent ->
      let headers =
        Some
          ((List.filter_opt
              [Option.map req.PostContentRequest.sessionAttributes
                 ~f:(fun x ->
                       ("x-amz-lex-session-attributes",
                         (AttributesString.to_header x)));
              Option.map req.PostContentRequest.requestAttributes
                ~f:(fun x ->
                      ("x-amz-lex-request-attributes",
                        (AttributesString.to_header x)));
              Some
                ("Content-Type",
                  (HttpContentType.to_header
                     req.PostContentRequest.contentType));
              Option.map req.PostContentRequest.accept
                ~f:(fun x -> ("Accept", (Accept.to_header x)));
              Option.map req.PostContentRequest.activeContexts
                ~f:(fun x ->
                      ("x-amz-lex-active-contexts",
                        (ActiveContextsString.to_header x)))])
             |> Awso.Http.Headers.of_list) in
      let body = BlobStream.to_header req.inputStream in
      Awso.Http.Request.make ?headers ~body (method_of_endpoint endp)
  | PostText ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.PostTextRequest.sessionAttributes
                         ~f:(fun x ->
                               ("sessionAttributes", (StringMap.to_value x)));
                      Option.map req.PostTextRequest.requestAttributes
                        ~f:(fun x ->
                              ("requestAttributes", (StringMap.to_value x)));
                      Some
                        ("inputText",
                          (Text.to_value req.PostTextRequest.inputText));
                      Option.map req.PostTextRequest.activeContexts
                        ~f:(fun x ->
                              ("activeContexts",
                                (ActiveContextsList.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | PutSession ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Option.map req.PutSessionRequest.accept
                   ~f:(fun x -> ("Accept", (Accept.to_header x)))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.PutSessionRequest.sessionAttributes
                         ~f:(fun x ->
                               ("sessionAttributes", (StringMap.to_value x)));
                      Option.map req.PutSessionRequest.dialogAction
                        ~f:(fun x ->
                              ("dialogAction", (DialogAction.to_value x)));
                      Option.map
                        req.PutSessionRequest.recentIntentSummaryView
                        ~f:(fun x ->
                              ("recentIntentSummaryView",
                                (IntentSummaryList.to_value x)));
                      Option.map req.PutSessionRequest.activeContexts
                        ~f:(fun x ->
                              ("activeContexts",
                                (ActiveContextsList.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | DeleteSession ->
      if is_success
      then Ok (DeleteSessionResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some DeleteSessionResponse.error_of_json))
  | GetSession ->
      if is_success
      then Ok (GetSessionResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetSessionResponse.error_of_json))
  | PostContent ->
      if is_success
      then
        let body = BlobStream.of_string (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (PostContentResponse.of_header_and_body (headers, body))
      else Error (parse_aws_error (Some PostContentResponse.error_of_json))
  | PostText ->
      if is_success
      then Ok (PostTextResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some PostTextResponse.error_of_json))
  | PutSession ->
      if is_success
      then
        let body = BlobStream.of_string (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (PutSessionResponse.of_header_and_body (headers, body))
      else Error (parse_aws_error (Some PutSessionResponse.error_of_json))
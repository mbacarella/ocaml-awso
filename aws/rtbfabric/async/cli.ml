(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let accept_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON LinkAttributes"
       and timeoutInMillis =
         flag "timeout-in-millis" (optional json_arg)
           ~doc:"JSON LinkTimeoutInMillis"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId"
       and linkId = flag "link-id" (required string) ~doc:"STRING LinkId"
       and logSettings =
         flag "log-settings" (required json_arg) ~doc:"JSON LinkLogSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_link
           (Values.AcceptLinkRequest.make
              ?attributes:(Option.map ~f:Values.LinkAttributes.of_json
                             attributes)
              ?timeoutInMillis:(Option.map
                                  ~f:Values.LinkTimeoutInMillis.of_json
                                  timeoutInMillis) ~gatewayId ~linkId
              ~logSettings:(Values.LinkLogSettings.of_json logSettings) ())
           (Some Values.AcceptLinkResponse.to_json)
           (Some Values.AcceptLinkResponse.error_to_json)])
let associate_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId"
       and acmCertificateArn =
         flag "acm-certificate-arn" (required string)
           ~doc:"STRING AcmCertificateArn"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_certificate
           (Values.AssociateCertificateRequest.make ~gatewayId
              ~acmCertificateArn ~clientToken ())
           (Some Values.AssociateCertificateResponse.to_json)
           (Some Values.AssociateCertificateResponse.error_to_json)])
let create_inbound_external_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON LinkAttributes"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING String"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId"
       and logSettings =
         flag "log-settings" (required json_arg) ~doc:"JSON LinkLogSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_inbound_external_link
           (Values.CreateInboundExternalLinkRequest.make
              ?attributes:(Option.map ~f:Values.LinkAttributes.of_json
                             attributes)
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~clientToken
              ~gatewayId
              ~logSettings:(Values.LinkLogSettings.of_json logSettings) ())
           (Some Values.CreateInboundExternalLinkResponse.to_json)
           (Some Values.CreateInboundExternalLinkResponse.error_to_json)])
let create_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON LinkAttributes"
       and httpResponderAllowed =
         flag "http-responder-allowed" (optional bool) ~doc:"BOOL Boolean"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and timeoutInMillis =
         flag "timeout-in-millis" (optional json_arg)
           ~doc:"JSON LinkTimeoutInMillis"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId"
       and peerGatewayId =
         flag "peer-gateway-id" (required string) ~doc:"STRING GatewayId"
       and logSettings =
         flag "log-settings" (required json_arg) ~doc:"JSON LinkLogSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_link
           (Values.CreateLinkRequest.make
              ?attributes:(Option.map ~f:Values.LinkAttributes.of_json
                             attributes) ?httpResponderAllowed
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ?timeoutInMillis:(Option.map
                                  ~f:Values.LinkTimeoutInMillis.of_json
                                  timeoutInMillis) ~gatewayId ~peerGatewayId
              ~logSettings:(Values.LinkLogSettings.of_json logSettings) ())
           (Some Values.CreateLinkResponse.to_json)
           (Some Values.CreateLinkResponse.error_to_json)])
let create_link_routing_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING String"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId"
       and linkId = flag "link-id" (required string) ~doc:"STRING LinkId"
       and priority = flag "priority" (required int) ~doc:"INT RulePriority"
       and conditions =
         flag "conditions" (required json_arg) ~doc:"JSON RuleCondition" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_link_routing_rule
           (Values.CreateLinkRoutingRuleRequest.make
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~clientToken
              ~gatewayId ~linkId ~priority
              ~conditions:(Values.RuleCondition.of_json conditions) ())
           (Some Values.CreateLinkRoutingRuleResponse.to_json)
           (Some Values.CreateLinkRoutingRuleResponse.error_to_json)])
let create_outbound_external_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributes =
         flag "attributes" (optional json_arg) ~doc:"JSON LinkAttributes"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING String"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId"
       and publicEndpoint =
         flag "public-endpoint" (required string) ~doc:"STRING URL"
       and logSettings =
         flag "log-settings" (required json_arg) ~doc:"JSON LinkLogSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_outbound_external_link
           (Values.CreateOutboundExternalLinkRequest.make
              ?attributes:(Option.map ~f:Values.LinkAttributes.of_json
                             attributes)
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~clientToken
              ~gatewayId ~publicEndpoint
              ~logSettings:(Values.LinkLogSettings.of_json logSettings) ())
           (Some Values.CreateOutboundExternalLinkResponse.to_json)
           (Some Values.CreateOutboundExternalLinkResponse.error_to_json)])
let create_requester_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING CreateRequesterGatewayRequestDescriptionString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId"
       and subnetIds =
         flag "subnet-ids" (required json_arg)
           ~doc:"JSON CreateRequesterGatewayRequestSubnetIdsList"
       and securityGroupIds =
         flag "security-group-ids" (required json_arg)
           ~doc:"JSON CreateRequesterGatewayRequestSecurityGroupIdsList"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_requester_gateway
           (Values.CreateRequesterGatewayRequest.make ?description
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~vpcId
              ~subnetIds:(Values.CreateRequesterGatewayRequestSubnetIdsList.of_json
                            subnetIds)
              ~securityGroupIds:(Values.CreateRequesterGatewayRequestSecurityGroupIdsList.of_json
                                   securityGroupIds) ~clientToken ())
           (Some Values.CreateRequesterGatewayResponse.to_json)
           (Some Values.CreateRequesterGatewayResponse.error_to_json)])
let create_responder_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (optional string) ~doc:"STRING DomainName"
       and listenerConfig =
         flag "listener-config" (optional json_arg)
           ~doc:"JSON ListenerConfig"
       and trustStoreConfiguration =
         flag "trust-store-configuration" (optional json_arg)
           ~doc:"JSON TrustStoreConfiguration"
       and managedEndpointConfiguration =
         flag "managed-endpoint-configuration" (optional json_arg)
           ~doc:"JSON ManagedEndpointConfiguration"
       and description =
         flag "description" (optional string)
           ~doc:"STRING CreateResponderGatewayRequestDescriptionString"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and gatewayType =
         flag "gateway-type" (optional json_arg) ~doc:"JSON GatewayType"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId"
       and subnetIds =
         flag "subnet-ids" (required json_arg)
           ~doc:"JSON CreateResponderGatewayRequestSubnetIdsList"
       and securityGroupIds =
         flag "security-group-ids" (required json_arg)
           ~doc:"JSON CreateResponderGatewayRequestSecurityGroupIdsList"
       and port =
         flag "port" (required int)
           ~doc:"INT CreateResponderGatewayRequestPortInteger"
       and protocol =
         flag "protocol" (required json_arg) ~doc:"JSON Protocol"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_responder_gateway
           (Values.CreateResponderGatewayRequest.make ?domainName
              ?listenerConfig:(Option.map ~f:Values.ListenerConfig.of_json
                                 listenerConfig)
              ?trustStoreConfiguration:(Option.map
                                          ~f:Values.TrustStoreConfiguration.of_json
                                          trustStoreConfiguration)
              ?managedEndpointConfiguration:(Option.map
                                               ~f:Values.ManagedEndpointConfiguration.of_json
                                               managedEndpointConfiguration)
              ?description ?tags:(Option.map ~f:Values.TagsMap.of_json tags)
              ?gatewayType:(Option.map ~f:Values.GatewayType.of_json
                              gatewayType) ~vpcId
              ~subnetIds:(Values.CreateResponderGatewayRequestSubnetIdsList.of_json
                            subnetIds)
              ~securityGroupIds:(Values.CreateResponderGatewayRequestSecurityGroupIdsList.of_json
                                   securityGroupIds) ~port
              ~protocol:(Values.Protocol.of_json protocol) ~clientToken ())
           (Some Values.CreateResponderGatewayResponse.to_json)
           (Some Values.CreateResponderGatewayResponse.error_to_json)])
let delete_inbound_external_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId"
       and linkId = flag "link-id" (required string) ~doc:"STRING LinkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_inbound_external_link
           (Values.DeleteInboundExternalLinkRequest.make ~gatewayId ~linkId
              ()) (Some Values.DeleteInboundExternalLinkResponse.to_json)
           (Some Values.DeleteInboundExternalLinkResponse.error_to_json)])
let delete_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId"
       and linkId = flag "link-id" (required string) ~doc:"STRING LinkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_link
           (Values.DeleteLinkRequest.make ~gatewayId ~linkId ())
           (Some Values.DeleteLinkResponse.to_json)
           (Some Values.DeleteLinkResponse.error_to_json)])
let delete_link_routing_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId"
       and linkId = flag "link-id" (required string) ~doc:"STRING LinkId"
       and ruleId = flag "rule-id" (required string) ~doc:"STRING RuleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_link_routing_rule
           (Values.DeleteLinkRoutingRuleRequest.make ~gatewayId ~linkId
              ~ruleId ()) (Some Values.DeleteLinkRoutingRuleResponse.to_json)
           (Some Values.DeleteLinkRoutingRuleResponse.error_to_json)])
let delete_outbound_external_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId"
       and linkId = flag "link-id" (required string) ~doc:"STRING LinkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_outbound_external_link
           (Values.DeleteOutboundExternalLinkRequest.make ~gatewayId ~linkId
              ()) (Some Values.DeleteOutboundExternalLinkResponse.to_json)
           (Some Values.DeleteOutboundExternalLinkResponse.error_to_json)])
let delete_requester_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_requester_gateway
           (Values.DeleteRequesterGatewayRequest.make ~gatewayId ())
           (Some Values.DeleteRequesterGatewayResponse.to_json)
           (Some Values.DeleteRequesterGatewayResponse.error_to_json)])
let delete_responder_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_responder_gateway
           (Values.DeleteResponderGatewayRequest.make ~gatewayId ())
           (Some Values.DeleteResponderGatewayResponse.to_json)
           (Some Values.DeleteResponderGatewayResponse.error_to_json)])
let disassociate_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId"
       and acmCertificateArn =
         flag "acm-certificate-arn" (required string)
           ~doc:"STRING AcmCertificateArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_certificate
           (Values.DisassociateCertificateRequest.make ~gatewayId
              ~acmCertificateArn ())
           (Some Values.DisassociateCertificateResponse.to_json)
           (Some Values.DisassociateCertificateResponse.error_to_json)])
let get_certificate_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId"
       and acmCertificateArn =
         flag "acm-certificate-arn" (required string)
           ~doc:"STRING AcmCertificateArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_certificate_association
           (Values.GetCertificateAssociationRequest.make ~gatewayId
              ~acmCertificateArn ())
           (Some Values.GetCertificateAssociationResponse.to_json)
           (Some Values.GetCertificateAssociationResponse.error_to_json)])
let get_inbound_external_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId"
       and linkId = flag "link-id" (required string) ~doc:"STRING LinkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_inbound_external_link
           (Values.GetInboundExternalLinkRequest.make ~gatewayId ~linkId ())
           (Some Values.GetInboundExternalLinkResponse.to_json)
           (Some Values.GetInboundExternalLinkResponse.error_to_json)])
let get_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId"
       and linkId = flag "link-id" (required string) ~doc:"STRING LinkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_link (Values.GetLinkRequest.make ~gatewayId ~linkId ())
           (Some Values.GetLinkResponse.to_json)
           (Some Values.GetLinkResponse.error_to_json)])
let get_link_routing_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId"
       and linkId = flag "link-id" (required string) ~doc:"STRING LinkId"
       and ruleId = flag "rule-id" (required string) ~doc:"STRING RuleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_link_routing_rule
           (Values.GetLinkRoutingRuleRequest.make ~gatewayId ~linkId ~ruleId
              ()) (Some Values.GetLinkRoutingRuleResponse.to_json)
           (Some Values.GetLinkRoutingRuleResponse.error_to_json)])
let get_outbound_external_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId"
       and linkId = flag "link-id" (required string) ~doc:"STRING LinkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_outbound_external_link
           (Values.GetOutboundExternalLinkRequest.make ~gatewayId ~linkId ())
           (Some Values.GetOutboundExternalLinkResponse.to_json)
           (Some Values.GetOutboundExternalLinkResponse.error_to_json)])
let get_requester_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_requester_gateway
           (Values.GetRequesterGatewayRequest.make ~gatewayId ())
           (Some Values.GetRequesterGatewayResponse.to_json)
           (Some Values.GetRequesterGatewayResponse.error_to_json)])
let get_responder_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_responder_gateway
           (Values.GetResponderGatewayRequest.make ~gatewayId ())
           (Some Values.GetResponderGatewayResponse.to_json)
           (Some Values.GetResponderGatewayResponse.error_to_json)])
let list_certificate_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListCertificateAssociationsRequestMaxResultsInteger"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_certificate_associations
           (Values.ListCertificateAssociationsRequest.make ?nextToken
              ?maxResults ~gatewayId ())
           (Some Values.ListCertificateAssociationsResponse.to_json)
           (Some Values.ListCertificateAssociationsResponse.error_to_json)])
let list_link_routing_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListLinkRoutingRulesRequestMaxResultsInteger"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId"
       and linkId = flag "link-id" (required string) ~doc:"STRING LinkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_link_routing_rules
           (Values.ListLinkRoutingRulesRequest.make ?nextToken ?maxResults
              ~gatewayId ~linkId ())
           (Some Values.ListLinkRoutingRulesResponse.to_json)
           (Some Values.ListLinkRoutingRulesResponse.error_to_json)])
let list_links =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults = flag "max-results" (optional int) ~doc:"INT Integer"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_links
           (Values.ListLinksRequest.make ?nextToken ?maxResults ~gatewayId ())
           (Some Values.ListLinksResponse.to_json)
           (Some Values.ListLinksResponse.error_to_json)])
let list_requester_gateways =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListRequesterGatewaysRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_requester_gateways
           (Values.ListRequesterGatewaysRequest.make ?maxResults ?nextToken
              ()) (Some Values.ListRequesterGatewaysResponse.to_json)
           (Some Values.ListRequesterGatewaysResponse.error_to_json)])
let list_responder_gateways =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListResponderGatewaysRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_responder_gateways
           (Values.ListResponderGatewaysRequest.make ?maxResults ?nextToken
              ()) (Some Values.ListResponderGatewaysResponse.to_json)
           (Some Values.ListResponderGatewaysResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING RtbTaggableResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let reject_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId"
       and linkId = flag "link-id" (required string) ~doc:"STRING LinkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reject_link
           (Values.RejectLinkRequest.make ~gatewayId ~linkId ())
           (Some Values.RejectLinkResponse.to_json)
           (Some Values.RejectLinkResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING RtbTaggableResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagsMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING RtbTaggableResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_link =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and logSettings =
         flag "log-settings" (optional json_arg) ~doc:"JSON LinkLogSettings"
       and timeoutInMillis =
         flag "timeout-in-millis" (optional json_arg)
           ~doc:"JSON LinkTimeoutInMillis"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId"
       and linkId = flag "link-id" (required string) ~doc:"STRING LinkId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_link
           (Values.UpdateLinkRequest.make
              ?logSettings:(Option.map ~f:Values.LinkLogSettings.of_json
                              logSettings)
              ?timeoutInMillis:(Option.map
                                  ~f:Values.LinkTimeoutInMillis.of_json
                                  timeoutInMillis) ~gatewayId ~linkId ())
           (Some Values.UpdateLinkResponse.to_json)
           (Some Values.UpdateLinkResponse.error_to_json)])
let update_link_module_flow =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING String"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId"
       and linkId = flag "link-id" (required string) ~doc:"STRING LinkId"
       and modules =
         flag "modules" (required json_arg)
           ~doc:"JSON ModuleConfigurationList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_link_module_flow
           (Values.UpdateLinkModuleFlowRequest.make ~clientToken ~gatewayId
              ~linkId
              ~modules:(Values.ModuleConfigurationList.of_json modules) ())
           (Some Values.UpdateLinkModuleFlowResponse.to_json)
           (Some Values.UpdateLinkModuleFlowResponse.error_to_json)])
let update_link_routing_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId"
       and linkId = flag "link-id" (required string) ~doc:"STRING LinkId"
       and ruleId = flag "rule-id" (required string) ~doc:"STRING RuleId"
       and priority = flag "priority" (required int) ~doc:"INT RulePriority"
       and conditions =
         flag "conditions" (required json_arg) ~doc:"JSON RuleCondition" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_link_routing_rule
           (Values.UpdateLinkRoutingRuleRequest.make ~gatewayId ~linkId
              ~ruleId ~priority
              ~conditions:(Values.RuleCondition.of_json conditions) ())
           (Some Values.UpdateLinkRoutingRuleResponse.to_json)
           (Some Values.UpdateLinkRoutingRuleResponse.error_to_json)])
let update_requester_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING UpdateRequesterGatewayRequestDescriptionString"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING String"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_requester_gateway
           (Values.UpdateRequesterGatewayRequest.make ?description
              ~clientToken ~gatewayId ())
           (Some Values.UpdateRequesterGatewayResponse.to_json)
           (Some Values.UpdateRequesterGatewayResponse.error_to_json)])
let update_responder_gateway =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domainName =
         flag "domain-name" (optional string) ~doc:"STRING DomainName"
       and listenerConfig =
         flag "listener-config" (optional json_arg)
           ~doc:"JSON ListenerConfig"
       and trustStoreConfiguration =
         flag "trust-store-configuration" (optional json_arg)
           ~doc:"JSON TrustStoreConfiguration"
       and managedEndpointConfiguration =
         flag "managed-endpoint-configuration" (optional json_arg)
           ~doc:"JSON ManagedEndpointConfiguration"
       and description =
         flag "description" (optional string)
           ~doc:"STRING UpdateResponderGatewayRequestDescriptionString"
       and port =
         flag "port" (required int)
           ~doc:"INT UpdateResponderGatewayRequestPortInteger"
       and protocol =
         flag "protocol" (required json_arg) ~doc:"JSON Protocol"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING String"
       and gatewayId =
         flag "gateway-id" (required string) ~doc:"STRING GatewayId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_responder_gateway
           (Values.UpdateResponderGatewayRequest.make ?domainName
              ?listenerConfig:(Option.map ~f:Values.ListenerConfig.of_json
                                 listenerConfig)
              ?trustStoreConfiguration:(Option.map
                                          ~f:Values.TrustStoreConfiguration.of_json
                                          trustStoreConfiguration)
              ?managedEndpointConfiguration:(Option.map
                                               ~f:Values.ManagedEndpointConfiguration.of_json
                                               managedEndpointConfiguration)
              ?description ~port ~protocol:(Values.Protocol.of_json protocol)
              ~clientToken ~gatewayId ())
           (Some Values.UpdateResponderGatewayResponse.to_json)
           (Some Values.UpdateResponderGatewayResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("accept-link", accept_link);
    ("associate-certificate", associate_certificate);
    ("create-inbound-external-link", create_inbound_external_link);
    ("create-link", create_link);
    ("create-link-routing-rule", create_link_routing_rule);
    ("create-outbound-external-link", create_outbound_external_link);
    ("create-requester-gateway", create_requester_gateway);
    ("create-responder-gateway", create_responder_gateway);
    ("delete-inbound-external-link", delete_inbound_external_link);
    ("delete-link", delete_link);
    ("delete-link-routing-rule", delete_link_routing_rule);
    ("delete-outbound-external-link", delete_outbound_external_link);
    ("delete-requester-gateway", delete_requester_gateway);
    ("delete-responder-gateway", delete_responder_gateway);
    ("disassociate-certificate", disassociate_certificate);
    ("get-certificate-association", get_certificate_association);
    ("get-inbound-external-link", get_inbound_external_link);
    ("get-link", get_link);
    ("get-link-routing-rule", get_link_routing_rule);
    ("get-outbound-external-link", get_outbound_external_link);
    ("get-requester-gateway", get_requester_gateway);
    ("get-responder-gateway", get_responder_gateway);
    ("list-certificate-associations", list_certificate_associations);
    ("list-link-routing-rules", list_link_routing_rules);
    ("list-links", list_links);
    ("list-requester-gateways", list_requester_gateways);
    ("list-responder-gateways", list_responder_gateways);
    ("list-tags-for-resource", list_tags_for_resource);
    ("reject-link", reject_link);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-link", update_link);
    ("update-link-module-flow", update_link_module_flow);
    ("update-link-routing-rule", update_link_routing_rule);
    ("update-requester-gateway", update_requester_gateway);
    ("update-responder-gateway", update_responder_gateway)]
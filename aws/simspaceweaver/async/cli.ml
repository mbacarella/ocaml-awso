(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and destination =
         flag "destination" (required json_arg) ~doc:"JSON S3Destination"
       and simulation =
         flag "simulation" (required string)
           ~doc:"STRING SimSpaceWeaverResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_snapshot
           (Values.CreateSnapshotInput.make
              ~destination:(Values.S3Destination.of_json destination)
              ~simulation ()) (Some Values.CreateSnapshotOutput.to_json)
           (Some Values.CreateSnapshotOutput.error_to_json)])
let delete_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and app =
         flag "app" (required string)
           ~doc:"STRING SimSpaceWeaverResourceName"
       and domain =
         flag "domain" (required string)
           ~doc:"STRING SimSpaceWeaverResourceName"
       and simulation =
         flag "simulation" (required string)
           ~doc:"STRING SimSpaceWeaverResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_app
           (Values.DeleteAppInput.make ~app ~domain ~simulation ())
           (Some Values.DeleteAppOutput.to_json)
           (Some Values.DeleteAppOutput.error_to_json)])
let delete_simulation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and simulation =
         flag "simulation" (required string)
           ~doc:"STRING SimSpaceWeaverResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_simulation
           (Values.DeleteSimulationInput.make ~simulation ())
           (Some Values.DeleteSimulationOutput.to_json)
           (Some Values.DeleteSimulationOutput.error_to_json)])
let describe_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and app =
         flag "app" (required string)
           ~doc:"STRING SimSpaceWeaverLongResourceName"
       and domain =
         flag "domain" (required string)
           ~doc:"STRING SimSpaceWeaverResourceName"
       and simulation =
         flag "simulation" (required string)
           ~doc:"STRING SimSpaceWeaverResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_app
           (Values.DescribeAppInput.make ~app ~domain ~simulation ())
           (Some Values.DescribeAppOutput.to_json)
           (Some Values.DescribeAppOutput.error_to_json)])
let describe_simulation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and simulation =
         flag "simulation" (required string)
           ~doc:"STRING SimSpaceWeaverResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_simulation
           (Values.DescribeSimulationInput.make ~simulation ())
           (Some Values.DescribeSimulationOutput.to_json)
           (Some Values.DescribeSimulationOutput.error_to_json)])
let list_apps =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domain =
         flag "domain" (optional string)
           ~doc:"STRING SimSpaceWeaverResourceName"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT PositiveInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING OptionalString"
       and simulation =
         flag "simulation" (required string)
           ~doc:"STRING SimSpaceWeaverResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_apps
           (Values.ListAppsInput.make ?domain ?maxResults ?nextToken
              ~simulation ()) (Some Values.ListAppsOutput.to_json)
           (Some Values.ListAppsOutput.error_to_json)])
let list_simulations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT PositiveInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING OptionalString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_simulations
           (Values.ListSimulationsInput.make ?maxResults ?nextToken ())
           (Some Values.ListSimulationsOutput.to_json)
           (Some Values.ListSimulationsOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING SimSpaceWeaverArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let start_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and launchOverrides =
         flag "launch-overrides" (optional json_arg)
           ~doc:"JSON LaunchOverrides"
       and domain =
         flag "domain" (required string)
           ~doc:"STRING SimSpaceWeaverResourceName"
       and name =
         flag "name" (required string)
           ~doc:"STRING SimSpaceWeaverResourceName"
       and simulation =
         flag "simulation" (required string)
           ~doc:"STRING SimSpaceWeaverResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_app
           (Values.StartAppInput.make ?clientToken ?description
              ?launchOverrides:(Option.map ~f:Values.LaunchOverrides.of_json
                                  launchOverrides) ~domain ~name ~simulation
              ()) (Some Values.StartAppOutput.to_json)
           (Some Values.StartAppOutput.error_to_json)])
let start_clock =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and simulation =
         flag "simulation" (required string)
           ~doc:"STRING SimSpaceWeaverResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_clock (Values.StartClockInput.make ~simulation ())
           (Some Values.StartClockOutput.to_json)
           (Some Values.StartClockOutput.error_to_json)])
let start_simulation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and maximumDuration =
         flag "maximum-duration" (optional string)
           ~doc:"STRING TimeToLiveString"
       and schemaS3Location =
         flag "schema-s3-location" (optional json_arg) ~doc:"JSON S3Location"
       and snapshotS3Location =
         flag "snapshot-s3-location" (optional json_arg)
           ~doc:"JSON S3Location"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name =
         flag "name" (required string)
           ~doc:"STRING SimSpaceWeaverResourceName"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_simulation
           (Values.StartSimulationInput.make ?clientToken ?description
              ?maximumDuration
              ?schemaS3Location:(Option.map ~f:Values.S3Location.of_json
                                   schemaS3Location)
              ?snapshotS3Location:(Option.map ~f:Values.S3Location.of_json
                                     snapshotS3Location)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name ~roleArn
              ()) (Some Values.StartSimulationOutput.to_json)
           (Some Values.StartSimulationOutput.error_to_json)])
let stop_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and app =
         flag "app" (required string)
           ~doc:"STRING SimSpaceWeaverResourceName"
       and domain =
         flag "domain" (required string)
           ~doc:"STRING SimSpaceWeaverResourceName"
       and simulation =
         flag "simulation" (required string)
           ~doc:"STRING SimSpaceWeaverResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_app (Values.StopAppInput.make ~app ~domain ~simulation ())
           (Some Values.StopAppOutput.to_json)
           (Some Values.StopAppOutput.error_to_json)])
let stop_clock =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and simulation =
         flag "simulation" (required string)
           ~doc:"STRING SimSpaceWeaverResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_clock (Values.StopClockInput.make ~simulation ())
           (Some Values.StopClockOutput.to_json)
           (Some Values.StopClockOutput.error_to_json)])
let stop_simulation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and simulation =
         flag "simulation" (required string)
           ~doc:"STRING SimSpaceWeaverResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_simulation
           (Values.StopSimulationInput.make ~simulation ())
           (Some Values.StopSimulationOutput.to_json)
           (Some Values.StopSimulationOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING SimSpaceWeaverArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceOutput.to_json)
           (Some Values.TagResourceOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING SimSpaceWeaverArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceOutput.to_json)
           (Some Values.UntagResourceOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-snapshot", create_snapshot);
    ("delete-app", delete_app);
    ("delete-simulation", delete_simulation);
    ("describe-app", describe_app);
    ("describe-simulation", describe_simulation);
    ("list-apps", list_apps);
    ("list-simulations", list_simulations);
    ("list-tags-for-resource", list_tags_for_resource);
    ("start-app", start_app);
    ("start-clock", start_clock);
    ("start-simulation", start_simulation);
    ("stop-app", stop_app);
    ("stop-clock", stop_clock);
    ("stop-simulation", stop_simulation);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | CreateSnapshot: (CreateSnapshotInput.t, CreateSnapshotOutput.t,
  CreateSnapshotOutput.error) t 
  | DeleteApp: (DeleteAppInput.t, DeleteAppOutput.t, DeleteAppOutput.error) t
  
  | DeleteSimulation: (DeleteSimulationInput.t, DeleteSimulationOutput.t,
  DeleteSimulationOutput.error) t 
  | DescribeApp: (DescribeAppInput.t, DescribeAppOutput.t,
  DescribeAppOutput.error) t 
  | DescribeSimulation: (DescribeSimulationInput.t,
  DescribeSimulationOutput.t, DescribeSimulationOutput.error) t 
  | ListApps: (ListAppsInput.t, ListAppsOutput.t, ListAppsOutput.error) t 
  | ListSimulations: (ListSimulationsInput.t, ListSimulationsOutput.t,
  ListSimulationsOutput.error) t 
  | ListTagsForResource: (ListTagsForResourceInput.t,
  ListTagsForResourceOutput.t, ListTagsForResourceOutput.error) t 
  | StartApp: (StartAppInput.t, StartAppOutput.t, StartAppOutput.error) t 
  | StartClock: (StartClockInput.t, StartClockOutput.t,
  StartClockOutput.error) t 
  | StartSimulation: (StartSimulationInput.t, StartSimulationOutput.t,
  StartSimulationOutput.error) t 
  | StopApp: (StopAppInput.t, StopAppOutput.t, StopAppOutput.error) t 
  | StopClock: (StopClockInput.t, StopClockOutput.t, StopClockOutput.error) t
  
  | StopSimulation: (StopSimulationInput.t, StopSimulationOutput.t,
  StopSimulationOutput.error) t 
  | TagResource: (TagResourceInput.t, TagResourceOutput.t,
  TagResourceOutput.error) t 
  | UntagResource: (UntagResourceInput.t, UntagResourceOutput.t,
  UntagResourceOutput.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | CreateSnapshot -> `POST
  | DeleteApp -> `DELETE
  | DeleteSimulation -> `DELETE
  | DescribeApp -> `GET
  | DescribeSimulation -> `GET
  | ListApps -> `GET
  | ListSimulations -> `GET
  | ListTagsForResource -> `GET
  | StartApp -> `POST
  | StartClock -> `POST
  | StartSimulation -> `POST
  | StopApp -> `POST
  | StopClock -> `POST
  | StopSimulation -> `POST
  | TagResource -> `POST
  | UntagResource -> `DELETE
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | CreateSnapshot -> (Format.kasprintf Uri.of_string) "/createsnapshot"
      | DeleteApp ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/deleteapp")
            (List.filter_opt
               [Some ("app", (SimSpaceWeaverResourceName.to_header x.app));
               Some
                 ("domain", (SimSpaceWeaverResourceName.to_header x.domain));
               Some
                 ("simulation",
                   (SimSpaceWeaverResourceName.to_header x.simulation))])
      | DeleteSimulation ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/deletesimulation")
            (List.filter_opt
               [Some
                  ("simulation",
                    (SimSpaceWeaverResourceName.to_header x.simulation))])
      | DescribeApp ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/describeapp")
            (List.filter_opt
               [Some
                  ("app", (SimSpaceWeaverLongResourceName.to_header x.app));
               Some
                 ("domain", (SimSpaceWeaverResourceName.to_header x.domain));
               Some
                 ("simulation",
                   (SimSpaceWeaverResourceName.to_header x.simulation))])
      | DescribeSimulation ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/describesimulation")
            (List.filter_opt
               [Some
                  ("simulation",
                    (SimSpaceWeaverResourceName.to_header x.simulation))])
      | ListApps ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/listapps")
            (List.filter_opt
               [Option.map
                  ~f:(fun v ->
                        ("domain", (SimSpaceWeaverResourceName.to_header v)))
                  x.domain;
               Option.map
                 ~f:(fun v -> ("maxResults", (PositiveInteger.to_header v)))
                 x.maxResults;
               Option.map
                 ~f:(fun v -> ("nextToken", (OptionalString.to_header v)))
                 x.nextToken;
               Some
                 ("simulation",
                   (SimSpaceWeaverResourceName.to_header x.simulation))])
      | ListSimulations ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/listsimulations")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("maxResults", (PositiveInteger.to_header v)))
                  x.maxResults;
               Option.map
                 ~f:(fun v -> ("nextToken", (OptionalString.to_header v)))
                 x.nextToken])
      | ListTagsForResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (SimSpaceWeaverArn.to_header
               x.ListTagsForResourceInput.resourceArn)
      | StartApp -> (Format.kasprintf Uri.of_string) "/startapp"
      | StartClock -> (Format.kasprintf Uri.of_string) "/startclock"
      | StartSimulation ->
          (Format.kasprintf Uri.of_string) "/startsimulation"
      | StopApp -> (Format.kasprintf Uri.of_string) "/stopapp"
      | StopClock -> (Format.kasprintf Uri.of_string) "/stopclock"
      | StopSimulation -> (Format.kasprintf Uri.of_string) "/stopsimulation"
      | TagResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (SimSpaceWeaverArn.to_header x.TagResourceInput.resourceArn)
      | UntagResource ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/tags/%s"
               (SimSpaceWeaverArn.to_header x.UntagResourceInput.resourceArn))
            (List.filter_opt
               [Some ("tagKeys", (TagKeyList.to_header x.tagKeys))]))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | CreateSnapshot ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("Destination",
                           (S3Destination.to_value
                              req.CreateSnapshotInput.destination));
                      Some
                        ("Simulation",
                          (SimSpaceWeaverResourceName.to_value
                             req.CreateSnapshotInput.simulation))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteApp -> Awso.Http.Request.make (method_of_endpoint endp)
  | DeleteSimulation -> Awso.Http.Request.make (method_of_endpoint endp)
  | DescribeApp ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DescribeSimulation ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListApps ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListSimulations ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTagsForResource ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StartApp ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.StartAppInput.clientToken
                         ~f:(fun x ->
                               ("ClientToken", (ClientToken.to_value x)));
                      Option.map req.StartAppInput.description
                        ~f:(fun x ->
                              ("Description", (Description.to_value x)));
                      Some
                        ("Domain",
                          (SimSpaceWeaverResourceName.to_value
                             req.StartAppInput.domain));
                      Option.map req.StartAppInput.launchOverrides
                        ~f:(fun x ->
                              ("LaunchOverrides",
                                (LaunchOverrides.to_value x)));
                      Some
                        ("Name",
                          (SimSpaceWeaverResourceName.to_value
                             req.StartAppInput.name));
                      Some
                        ("Simulation",
                          (SimSpaceWeaverResourceName.to_value
                             req.StartAppInput.simulation))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StartClock ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("Simulation",
                           (SimSpaceWeaverResourceName.to_value
                              req.StartClockInput.simulation))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StartSimulation ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.StartSimulationInput.clientToken
                         ~f:(fun x ->
                               ("ClientToken", (ClientToken.to_value x)));
                      Option.map req.StartSimulationInput.description
                        ~f:(fun x ->
                              ("Description", (Description.to_value x)));
                      Option.map req.StartSimulationInput.maximumDuration
                        ~f:(fun x ->
                              ("MaximumDuration",
                                (TimeToLiveString.to_value x)));
                      Some
                        ("Name",
                          (SimSpaceWeaverResourceName.to_value
                             req.StartSimulationInput.name));
                      Some
                        ("RoleArn",
                          (RoleArn.to_value req.StartSimulationInput.roleArn));
                      Option.map req.StartSimulationInput.schemaS3Location
                        ~f:(fun x ->
                              ("SchemaS3Location", (S3Location.to_value x)));
                      Option.map req.StartSimulationInput.snapshotS3Location
                        ~f:(fun x ->
                              ("SnapshotS3Location", (S3Location.to_value x)));
                      Option.map req.StartSimulationInput.tags
                        ~f:(fun x -> ("Tags", (TagMap.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StopApp ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("App",
                           (SimSpaceWeaverResourceName.to_value
                              req.StopAppInput.app));
                      Some
                        ("Domain",
                          (SimSpaceWeaverResourceName.to_value
                             req.StopAppInput.domain));
                      Some
                        ("Simulation",
                          (SimSpaceWeaverResourceName.to_value
                             req.StopAppInput.simulation))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StopClock ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("Simulation",
                           (SimSpaceWeaverResourceName.to_value
                              req.StopClockInput.simulation))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StopSimulation ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("Simulation",
                           (SimSpaceWeaverResourceName.to_value
                              req.StopSimulationInput.simulation))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | TagResource ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("Tags",
                           (TagMap.to_value req.TagResourceInput.tags))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UntagResource -> Awso.Http.Request.make (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | CreateSnapshot ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (CreateSnapshotOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some CreateSnapshotOutput.error_of_json))
  | DeleteApp ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DeleteAppOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some DeleteAppOutput.error_of_json))
  | DeleteSimulation ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (DeleteSimulationOutput.of_header_and_body (headers, ()))
      else
        Error (parse_aws_error (Some DeleteSimulationOutput.error_of_json))
  | DescribeApp ->
      if is_success
      then Ok (DescribeAppOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some DescribeAppOutput.error_of_json))
  | DescribeSimulation ->
      if is_success
      then Ok (DescribeSimulationOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some DescribeSimulationOutput.error_of_json))
  | ListApps ->
      if is_success
      then Ok (ListAppsOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListAppsOutput.error_of_json))
  | ListSimulations ->
      if is_success
      then Ok (ListSimulationsOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListSimulationsOutput.error_of_json))
  | ListTagsForResource ->
      if is_success
      then Ok (ListTagsForResourceOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListTagsForResourceOutput.error_of_json))
  | StartApp ->
      if is_success
      then Ok (StartAppOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some StartAppOutput.error_of_json))
  | StartClock ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (StartClockOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some StartClockOutput.error_of_json))
  | StartSimulation ->
      if is_success
      then Ok (StartSimulationOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some StartSimulationOutput.error_of_json))
  | StopApp ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (StopAppOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some StopAppOutput.error_of_json))
  | StopClock ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (StopClockOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some StopClockOutput.error_of_json))
  | StopSimulation ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (StopSimulationOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some StopSimulationOutput.error_of_json))
  | TagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (TagResourceOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some TagResourceOutput.error_of_json))
  | UntagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (UntagResourceOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some UntagResourceOutput.error_of_json))
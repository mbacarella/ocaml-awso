(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.marketplace_agreement
let apiVersion = "2020-03-01"
let endpointPrefix = "agreement-marketplace"
let serviceFullName = "AWS Marketplace Agreement Service"
let signatureVersion = "v4"
let protocol = "json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let serviceAbbreviation = "Agreement Service"
let targetPrefix = "AWSMPCommerceService_v20200301"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module BoundedString =
  struct
    type nonrec t = string
    let context_ = "BoundedString"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:4096) >>=
                  (fun () -> check_pattern i ~pattern:"(.)+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BoundedString" j
    let to_json = simple_to_json to_value
  end
module ZeroValueInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:0); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for ZeroValueInteger" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module RateCardItem =
  struct
    type nonrec t =
      {
      dimensionKey: BoundedString.t option
        [@ocaml.doc
          "Dimension for which the given entitlement applies. Dimensions represent categories of capacity in a product and are specified when the product is listed in AWS Marketplace."];
      price: BoundedString.t option
        [@ocaml.doc
          "Per unit price for the product dimension that\226\128\153s used for calculating the amount to be charged."]}
    let make ?dimensionKey = fun ?price -> fun () -> { dimensionKey; price }
    let to_value x =
      structure_to_value
        [("dimensionKey",
           (Option.map x.dimensionKey ~f:BoundedString.to_value));
        ("price", (Option.map x.price ~f:BoundedString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let price =
        (Option.map ~f:BoundedString.of_xml) (Xml.child xml_arg0 "price") in
      let dimensionKey =
        (Option.map ~f:BoundedString.of_xml)
          (Xml.child xml_arg0 "dimensionKey") in
      make ?price ?dimensionKey ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let price = field_map json__ "price" BoundedString.of_json in
      let dimensionKey =
        field_map json__ "dimensionKey" BoundedString.of_json in
      make ?price ?dimensionKey ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines the per unit rates for each individual product dimension."]
module AgreementResourceType =
  struct
    type nonrec t = string
    let context_ = "AgreementResourceType"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AgreementResourceType" j
    let to_json = simple_to_json to_value
  end
module ResourceId =
  struct
    type nonrec t = string
    let context_ = "ResourceId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"[A-Za-z0-9_/-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceId" j
    let to_json = simple_to_json to_value
  end
module Dimension =
  struct
    type nonrec t =
      {
      dimensionKey: BoundedString.t
        [@ocaml.doc "The name of key value of the dimension."];
      dimensionValue: ZeroValueInteger.t
        [@ocaml.doc
          "The number of units of the dimension the acceptor has purchased. For Agreements with ConfigurableUpfrontPricingTerm, the RateCard section will define the prices and dimensions defined by the seller (proposer), whereas the Configuration section will define the actual dimensions, prices, and units the buyer has chosen to accept."]}
    let context_ = "Dimension"
    let make ~dimensionKey =
      fun ~dimensionValue -> fun () -> { dimensionKey; dimensionValue }
    let to_value x =
      structure_to_value
        [("dimensionKey", (Some (BoundedString.to_value x.dimensionKey)));
        ("dimensionValue",
          (Some (ZeroValueInteger.to_value x.dimensionValue)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dimensionValue =
        ZeroValueInteger.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "dimensionValue") in
      let dimensionKey =
        BoundedString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "dimensionKey") in
      make ~dimensionValue ~dimensionKey ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dimensionValue =
        field_map_exn json__ "dimensionValue" ZeroValueInteger.of_json in
      let dimensionKey =
        field_map_exn json__ "dimensionKey" BoundedString.of_json in
      make ~dimensionValue ~dimensionKey ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines the dimensions that the acceptor has purchased from the overall set of dimensions presented in the rate card."]
module Constraints =
  struct
    type nonrec t =
      {
      multipleDimensionSelection: BoundedString.t option
        [@ocaml.doc
          "Determines if buyers are allowed to select multiple dimensions in the rate card. The possible values are Allowed and Disallowed. The default value is Allowed."];
      quantityConfiguration: BoundedString.t option
        [@ocaml.doc
          "Determines if acceptors are allowed to configure quantity for each dimension in rate card. The possible values are Allowed and Disallowed. The default value is Allowed."]}
    let make ?multipleDimensionSelection =
      fun ?quantityConfiguration ->
        fun () -> { multipleDimensionSelection; quantityConfiguration }
    let to_value x =
      structure_to_value
        [("multipleDimensionSelection",
           (Option.map x.multipleDimensionSelection ~f:BoundedString.to_value));
        ("quantityConfiguration",
          (Option.map x.quantityConfiguration ~f:BoundedString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let quantityConfiguration =
        (Option.map ~f:BoundedString.of_xml)
          (Xml.child xml_arg0 "quantityConfiguration") in
      let multipleDimensionSelection =
        (Option.map ~f:BoundedString.of_xml)
          (Xml.child xml_arg0 "multipleDimensionSelection") in
      make ?quantityConfiguration ?multipleDimensionSelection ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let quantityConfiguration =
        field_map json__ "quantityConfiguration" BoundedString.of_json in
      let multipleDimensionSelection =
        field_map json__ "multipleDimensionSelection" BoundedString.of_json in
      make ?quantityConfiguration ?multipleDimensionSelection ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines limits on how the term can be configured by acceptors."]
module RateCardList =
  struct
    type nonrec t = RateCardItem.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:RateCardItem.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RateCardItem.of_xml)
    let of_json j =
      list_of_json ~kind:"RateCardList" ~of_json:RateCardItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module Selector =
  struct
    type nonrec t =
      {
      type_: BoundedString.t option [@ocaml.doc "Category of selector."];
      value: BoundedString.t option
        [@ocaml.doc
          "Contract duration. This field supports the ISO 8601 format."]}
    let make ?type_ = fun ?value -> fun () -> { type_; value }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:BoundedString.to_value));
        ("value", (Option.map x.value ~f:BoundedString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:BoundedString.of_xml) (Xml.child xml_arg0 "value") in
      let type_ =
        (Option.map ~f:BoundedString.of_xml) (Xml.child xml_arg0 "type") in
      make ?value ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let value = field_map json__ "value" BoundedString.of_json in
      let type_ = field_map json__ "type" BoundedString.of_json in
      make ?value ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Differentiates between the mutually exclusive rate cards in the same pricing term to be selected by the buyer."]
module PositiveIntegerWithDefaultValueOne =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:1); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for PositiveIntegerWithDefaultValueOne" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module TaxBreakdownItem =
  struct
    type nonrec t =
      {
      amount: BoundedString.t option [@ocaml.doc "The estimated tax amount."];
      rate: BoundedString.t option [@ocaml.doc "The tax rate, in decimals."];
      type_: BoundedString.t option
        [@ocaml.doc "The type of tax (for example, VAT, ST, or GST)."]}
    let make ?amount =
      fun ?rate -> fun ?type_ -> fun () -> { amount; rate; type_ }
    let to_value x =
      structure_to_value
        [("amount", (Option.map x.amount ~f:BoundedString.to_value));
        ("rate", (Option.map x.rate ~f:BoundedString.to_value));
        ("type", (Option.map x.type_ ~f:BoundedString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:BoundedString.of_xml) (Xml.child xml_arg0 "type") in
      let rate =
        (Option.map ~f:BoundedString.of_xml) (Xml.child xml_arg0 "rate") in
      let amount =
        (Option.map ~f:BoundedString.of_xml) (Xml.child xml_arg0 "amount") in
      make ?type_ ?rate ?amount ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" BoundedString.of_json in
      let rate = field_map json__ "rate" BoundedString.of_json in
      let amount = field_map json__ "amount" BoundedString.of_json in
      make ?type_ ?rate ?amount ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a single tax breakdown entry with amount, rate, and type."]
module Resource =
  struct
    type nonrec t =
      {
      id: ResourceId.t option
        [@ocaml.doc
          "The unique identifier of the resource. We mention the term resource, which is most commonly a product, so a resourceId is also a productId."];
      type_: AgreementResourceType.t option
        [@ocaml.doc
          "Type of the resource, which is the product (for example, SaaSProduct, AmiProduct, ContainerProduct)."]}
    let make ?id = fun ?type_ -> fun () -> { id; type_ }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:ResourceId.to_value));
        ("type", (Option.map x.type_ ~f:AgreementResourceType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        (Option.map ~f:AgreementResourceType.of_xml)
          (Xml.child xml_arg0 "type") in
      let id = (Option.map ~f:ResourceId.of_xml) (Xml.child xml_arg0 "id") in
      make ?type_ ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let type_ = field_map json__ "type" AgreementResourceType.of_json in
      let id = field_map json__ "id" ResourceId.of_json in make ?type_ ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The list of resources involved in the agreement."]
module DimensionList =
  struct
    type nonrec t = Dimension.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Dimension.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Dimension.of_xml)
    let of_json j =
      list_of_json ~kind:"DimensionList" ~of_json:Dimension.of_json j
    let to_json v = composed_to_json to_value v
  end
module ConfigurableUpfrontRateCardItem =
  struct
    type nonrec t =
      {
      selector: Selector.t option
        [@ocaml.doc
          "Differentiates between the mutually exclusive rate cards in the same pricing term to be selected by the buyer."];
      constraints: Constraints.t option
        [@ocaml.doc
          "Defines limits on how the term can be configured by acceptors."];
      rateCard: RateCardList.t option
        [@ocaml.doc "Defines the per unit rates for product dimensions."]}
    let make ?selector =
      fun ?constraints ->
        fun ?rateCard -> fun () -> { selector; constraints; rateCard }
    let to_value x =
      structure_to_value
        [("selector", (Option.map x.selector ~f:Selector.to_value));
        ("constraints", (Option.map x.constraints ~f:Constraints.to_value));
        ("rateCard", (Option.map x.rateCard ~f:RateCardList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let rateCard =
        (Option.map ~f:RateCardList.of_xml) (Xml.child xml_arg0 "rateCard") in
      let constraints =
        (Option.map ~f:Constraints.of_xml) (Xml.child xml_arg0 "constraints") in
      let selector =
        (Option.map ~f:Selector.of_xml) (Xml.child xml_arg0 "selector") in
      make ?rateCard ?constraints ?selector ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let rateCard = field_map json__ "rateCard" RateCardList.of_json in
      let constraints = field_map json__ "constraints" Constraints.of_json in
      let selector = field_map json__ "selector" Selector.of_json in
      make ?rateCard ?constraints ?selector ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Within the prepaid payment model defined under ConfigurableUpfrontPricingTerm, the RateCardItem defines all the various rate cards (including pricing and dimensions) that have been proposed."]
module GrantItem =
  struct
    type nonrec t =
      {
      dimensionKey: BoundedString.t option
        [@ocaml.doc
          "Unique dimension key defined in the product document. Dimensions represent categories of capacity in a product and are specified when the product is listed in AWS Marketplace."];
      maxQuantity: PositiveIntegerWithDefaultValueOne.t option
        [@ocaml.doc
          "Maximum amount of capacity that the buyer can be entitled to the given dimension of the product. If MaxQuantity is not provided, the buyer will be able to use an unlimited amount of the given dimension."]}
    let make ?dimensionKey =
      fun ?maxQuantity -> fun () -> { dimensionKey; maxQuantity }
    let to_value x =
      structure_to_value
        [("dimensionKey",
           (Option.map x.dimensionKey ~f:BoundedString.to_value));
        ("maxQuantity",
          (Option.map x.maxQuantity
             ~f:PositiveIntegerWithDefaultValueOne.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxQuantity =
        (Option.map ~f:PositiveIntegerWithDefaultValueOne.of_xml)
          (Xml.child xml_arg0 "maxQuantity") in
      let dimensionKey =
        (Option.map ~f:BoundedString.of_xml)
          (Xml.child xml_arg0 "dimensionKey") in
      make ?maxQuantity ?dimensionKey ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let maxQuantity =
        field_map json__ "maxQuantity"
          PositiveIntegerWithDefaultValueOne.of_json in
      let dimensionKey =
        field_map json__ "dimensionKey" BoundedString.of_json in
      make ?maxQuantity ?dimensionKey ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Entitlements granted to the acceptor of fixed upfront as part of agreement execution."]
module DocumentItem =
  struct
    type nonrec t =
      {
      type_: BoundedString.t option
        [@ocaml.doc
          "Category of the document. Document types include: CustomEula \226\128\147 A custom EULA provided by you as seller. A URL for a EULA stored in an accessible Amazon S3 bucket is required for this document type. CustomDsa \226\128\147 A custom Data Subscription Agreement (DSA) provided by you as seller. A URL for a DSA stored in an accessible Amazon S3 bucket is required for this document type. StandardEula \226\128\147 The Standard Contract for AWS Marketplace (SCMP). For more information about SCMP, see the AWS Marketplace Seller Guide. You don\226\128\153t provide a URL for this type because it\226\128\153s managed by AWS Marketplace. StandardDsa \226\128\147 DSA for AWS Marketplace. For more information about the DSA, see the AWS Data Exchange User Guide. You don\226\128\153t provide a URL for this type because it\226\128\153s managed by AWS Marketplace."];
      url: BoundedString.t option
        [@ocaml.doc
          "A URL to the legal document for buyers to read. Required when Type is CustomEula."];
      version: BoundedString.t option
        [@ocaml.doc
          "Version of standard contracts provided by AWS Marketplace. Required when Type is StandardEula or StandardDsa."]}
    let make ?type_ =
      fun ?url -> fun ?version -> fun () -> { type_; url; version }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:BoundedString.to_value));
        ("url", (Option.map x.url ~f:BoundedString.to_value));
        ("version", (Option.map x.version ~f:BoundedString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let version =
        (Option.map ~f:BoundedString.of_xml) (Xml.child xml_arg0 "version") in
      let url =
        (Option.map ~f:BoundedString.of_xml) (Xml.child xml_arg0 "url") in
      let type_ =
        (Option.map ~f:BoundedString.of_xml) (Xml.child xml_arg0 "type") in
      make ?version ?url ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let version = field_map json__ "version" BoundedString.of_json in
      let url = field_map json__ "url" BoundedString.of_json in
      let type_ = field_map json__ "type" BoundedString.of_json in
      make ?version ?url ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Includes the list of references to legal resources proposed by the proposer to the acceptor. Each DocumentItem refers to an individual reference."]
module ScheduleItem =
  struct
    type nonrec t =
      {
      chargeDate: Timestamp.t option
        [@ocaml.doc
          "The date that the customer would pay the price defined in this payment schedule term. Invoices are generated on the date provided."];
      chargeAmount: BoundedString.t option
        [@ocaml.doc
          "The price that the customer would pay on the scheduled date (chargeDate)."]}
    let make ?chargeDate =
      fun ?chargeAmount -> fun () -> { chargeDate; chargeAmount }
    let to_value x =
      structure_to_value
        [("chargeDate", (Option.map x.chargeDate ~f:Timestamp.to_value));
        ("chargeAmount",
          (Option.map x.chargeAmount ~f:BoundedString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let chargeAmount =
        (Option.map ~f:BoundedString.of_xml)
          (Xml.child xml_arg0 "chargeAmount") in
      let chargeDate =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "chargeDate") in
      make ?chargeAmount ?chargeDate ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let chargeAmount =
        field_map json__ "chargeAmount" BoundedString.of_json in
      let chargeDate = field_map json__ "chargeDate" Timestamp.of_json in
      make ?chargeAmount ?chargeDate ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An individual installment of the payment that includes the date and amount of the charge."]
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module UsageBasedRateCardItem =
  struct
    type nonrec t =
      {
      rateCard: RateCardList.t option
        [@ocaml.doc "Defines the per unit rates for product dimensions."]}
    let make ?rateCard = fun () -> { rateCard }
    let to_value x =
      structure_to_value
        [("rateCard", (Option.map x.rateCard ~f:RateCardList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let rateCard =
        (Option.map ~f:RateCardList.of_xml) (Xml.child xml_arg0 "rateCard") in
      make ?rateCard ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let rateCard = field_map json__ "rateCard" RateCardList.of_json in
      make ?rateCard ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Within the pay-as-you-go model defined under UsageBasedPricingTerm, the UsageBasedRateCardItem defines an individual rate for a product dimension."]
module ISO8601Duration =
  struct
    type nonrec t = string
    let context_ = "ISO8601Duration"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i
             ~pattern:"([-+]?)P(?:([-+]?[0-9]+)D)?(T(?:([-+]?[0-9]+)H)?(?:([-+]?[0-9]+)M)?(?:([-+]?[0-9]+)(?:[.,]([0-9]{0,9}))?S)?)?");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ISO8601Duration" j
    let to_json = simple_to_json to_value
  end
module PaymentRequestApprovalStrategy =
  struct
    type nonrec t =
      | AUTO_APPROVE_ON_EXPIRATION 
      | WAIT_FOR_APPROVAL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | AUTO_APPROVE_ON_EXPIRATION -> "AUTO_APPROVE_ON_EXPIRATION"
      | WAIT_FOR_APPROVAL -> "WAIT_FOR_APPROVAL"
      | Non_static_id s -> s
    let of_string =
      function
      | "AUTO_APPROVE_ON_EXPIRATION" -> AUTO_APPROVE_ON_EXPIRATION
      | "WAIT_FOR_APPROVAL" -> WAIT_FOR_APPROVAL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration PaymentRequestApprovalStrategy"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"PaymentRequestApprovalStrategy" j)
    let to_json = simple_to_json to_value
  end
module TaxBreakdown =
  struct
    type nonrec t = TaxBreakdownItem.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:TaxBreakdownItem.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TaxBreakdownItem.of_xml)
    let of_json j =
      list_of_json ~kind:"TaxBreakdown" ~of_json:TaxBreakdownItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module AWSAccountId =
  struct
    type nonrec t = string
    let context_ = "AWSAccountId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:12) >>=
             (fun () ->
                (check_string_max i ~max:12) >>=
                  (fun () -> check_pattern i ~pattern:"[0-9]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AWSAccountId" j
    let to_json = simple_to_json to_value
  end
module OfferId =
  struct
    type nonrec t = string
    let context_ = "OfferId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"\\S{1,64}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"OfferId" j
    let to_json = simple_to_json to_value
  end
module OfferSetId =
  struct
    type nonrec t = string
    let context_ = "OfferSetId"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"\\S{1,50}"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"OfferSetId" j
    let to_json = simple_to_json to_value
  end
module Resources =
  struct
    type nonrec t = Resource.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Resource.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Resource.of_xml)
    let of_json j =
      list_of_json ~kind:"Resources" ~of_json:Resource.of_json j
    let to_json v = composed_to_json to_value v
  end
module FilterValue =
  struct
    type nonrec t = string
    let context_ = "FilterValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"[A-Za-z0-9+:_-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FilterValue" j
    let to_json = simple_to_json to_value
  end
module InvoiceBillingPeriodMonthInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:12) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml
           ~kind:"an integer for InvoiceBillingPeriodMonthInteger" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module InvoiceBillingPeriodYearInteger =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:2050) >>=
             (fun () -> check_int_min i ~min:2023));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for InvoiceBillingPeriodYearInteger"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module CurrencyCode =
  struct
    type nonrec t = string
    let context_ = "CurrencyCode"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:3) >>=
             (fun () ->
                (check_string_max i ~max:3) >>=
                  (fun () -> check_pattern i ~pattern:"[A-Z]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CurrencyCode" j
    let to_json = simple_to_json to_value
  end
module TermId =
  struct
    type nonrec t = string
    let context_ = "TermId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i ~pattern:"[A-Za-z0-9+=;,.@\\-_]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TermId" j
    let to_json = simple_to_json to_value
  end
module UnversionedTermType =
  struct
    type nonrec t = string
    let context_ = "UnversionedTermType"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:4096) >>=
                  (fun () -> check_pattern i ~pattern:"[A-Za-z]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"UnversionedTermType" j
    let to_json = simple_to_json to_value
  end
module ConfigurableUpfrontPricingTermConfiguration =
  struct
    type nonrec t =
      {
      selectorValue: BoundedString.t
        [@ocaml.doc
          "Defines the length of time for which the particular pricing/dimension is being purchased by the acceptor."];
      dimensions: DimensionList.t
        [@ocaml.doc
          "Defines the dimensions that the acceptor has purchased from the overall set of dimensions presented in the rate card."]}
    let context_ = "ConfigurableUpfrontPricingTermConfiguration"
    let make ~selectorValue =
      fun ~dimensions -> fun () -> { selectorValue; dimensions }
    let to_value x =
      structure_to_value
        [("selectorValue", (Some (BoundedString.to_value x.selectorValue)));
        ("dimensions", (Some (DimensionList.to_value x.dimensions)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dimensions =
        DimensionList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "dimensions") in
      let selectorValue =
        BoundedString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "selectorValue") in
      make ~dimensions ~selectorValue ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let dimensions =
        field_map_exn json__ "dimensions" DimensionList.of_json in
      let selectorValue =
        field_map_exn json__ "selectorValue" BoundedString.of_json in
      make ~dimensions ~selectorValue ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines a prepaid payment model that allows buyers to configure the entitlements they want to purchase and the duration."]
module ConfigurableUpfrontRateCardList =
  struct
    type nonrec t = ConfigurableUpfrontRateCardItem.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ConfigurableUpfrontRateCardItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ConfigurableUpfrontRateCardItem.of_xml)
    let of_json j =
      list_of_json ~kind:"ConfigurableUpfrontRateCardList"
        ~of_json:ConfigurableUpfrontRateCardItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module GrantList =
  struct
    type nonrec t = GrantItem.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:GrantItem.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GrantItem.of_xml)
    let of_json j =
      list_of_json ~kind:"GrantList" ~of_json:GrantItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module DocumentList =
  struct
    type nonrec t = DocumentItem.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:DocumentItem.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DocumentItem.of_xml)
    let of_json j =
      list_of_json ~kind:"DocumentList" ~of_json:DocumentItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module ScheduleList =
  struct
    type nonrec t = ScheduleItem.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ScheduleItem.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ScheduleItem.of_xml)
    let of_json j =
      list_of_json ~kind:"ScheduleList" ~of_json:ScheduleItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module RenewalTermConfiguration =
  struct
    type nonrec t =
      {
      enableAutoRenew: Boolean.t
        [@ocaml.doc
          "Defines whether the acceptor has chosen to auto-renew the agreement at the end of its lifecycle. Can be set to True or False."]}
    let context_ = "RenewalTermConfiguration"
    let make ~enableAutoRenew = fun () -> { enableAutoRenew }
    let to_value x =
      structure_to_value
        [("enableAutoRenew", (Some (Boolean.to_value x.enableAutoRenew)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let enableAutoRenew =
        Boolean.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "enableAutoRenew") in
      make ~enableAutoRenew ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let enableAutoRenew =
        field_map_exn json__ "enableAutoRenew" Boolean.of_json in
      make ~enableAutoRenew ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Additional parameters specified by the acceptor while accepting the term."]
module UsageBasedRateCardList =
  struct
    type nonrec t = UsageBasedRateCardItem.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:UsageBasedRateCardItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:UsageBasedRateCardItem.of_xml)
    let of_json j =
      list_of_json ~kind:"UsageBasedRateCardList"
        ~of_json:UsageBasedRateCardItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module VariablePaymentTermConfiguration =
  struct
    type nonrec t =
      {
      paymentRequestApprovalStrategy: PaymentRequestApprovalStrategy.t
        [@ocaml.doc
          "Defines the strategy for approving payment requests. Values include AUTO_APPROVE_ON_EXPIRATION and WAIT_FOR_APPROVAL"];
      expirationDuration: ISO8601Duration.t option
        [@ocaml.doc
          "Defines the duration after which a payment request is automatically approved if no further action is taken. This only applies when the payment request approval strategy is set to AUTO_APPROVE_ON_EXPIRATION. The duration is represented in the ISO_8601 format (e.g., P10D for 10 days)."]}
    let context_ = "VariablePaymentTermConfiguration"
    let make ?expirationDuration =
      fun ~paymentRequestApprovalStrategy ->
        fun () -> { expirationDuration; paymentRequestApprovalStrategy }
    let to_value x =
      structure_to_value
        [("paymentRequestApprovalStrategy",
           (Some
              (PaymentRequestApprovalStrategy.to_value
                 x.paymentRequestApprovalStrategy)));
        ("expirationDuration",
          (Option.map x.expirationDuration ~f:ISO8601Duration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let expirationDuration =
        (Option.map ~f:ISO8601Duration.of_xml)
          (Xml.child xml_arg0 "expirationDuration") in
      let paymentRequestApprovalStrategy =
        PaymentRequestApprovalStrategy.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "paymentRequestApprovalStrategy") in
      make ?expirationDuration ~paymentRequestApprovalStrategy ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let expirationDuration =
        field_map json__ "expirationDuration" ISO8601Duration.of_json in
      let paymentRequestApprovalStrategy =
        field_map_exn json__ "paymentRequestApprovalStrategy"
          PaymentRequestApprovalStrategy.of_json in
      make ?expirationDuration ~paymentRequestApprovalStrategy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Additional parameters specified by the acceptor while accepting the variable payment term."]
module EstimatedTaxes =
  struct
    type nonrec t =
      {
      breakdown: TaxBreakdown.t option
        [@ocaml.doc "A list of tax breakdown information."];
      totalAmount: BoundedString.t option
        [@ocaml.doc
          "The total amount of tax aggregated from the tax breakdown."]}
    let make ?breakdown =
      fun ?totalAmount -> fun () -> { breakdown; totalAmount }
    let to_value x =
      structure_to_value
        [("breakdown", (Option.map x.breakdown ~f:TaxBreakdown.to_value));
        ("totalAmount", (Option.map x.totalAmount ~f:BoundedString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let totalAmount =
        (Option.map ~f:BoundedString.of_xml)
          (Xml.child xml_arg0 "totalAmount") in
      let breakdown =
        (Option.map ~f:TaxBreakdown.of_xml) (Xml.child xml_arg0 "breakdown") in
      make ?totalAmount ?breakdown ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let totalAmount = field_map json__ "totalAmount" BoundedString.of_json in
      let breakdown = field_map json__ "breakdown" TaxBreakdown.of_json in
      make ?totalAmount ?breakdown ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides an aggregated view of estimated tax information."]
module Timing =
  struct
    type nonrec t =
      | ON_ACCEPTANCE 
      | SCHEDULED 
      | BILLING_PERIOD 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ON_ACCEPTANCE -> "ON_ACCEPTANCE"
      | SCHEDULED -> "SCHEDULED"
      | BILLING_PERIOD -> "BILLING_PERIOD"
      | Non_static_id s -> s
    let of_string =
      function
      | "ON_ACCEPTANCE" -> ON_ACCEPTANCE
      | "SCHEDULED" -> SCHEDULED
      | "BILLING_PERIOD" -> BILLING_PERIOD
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Timing" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Timing" j)
    let to_json = simple_to_json to_value
  end
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ValidationExceptionField =
  struct
    type nonrec t =
      {
      name: BoundedString.t option
        [@ocaml.doc "The name of the field associated with the error."];
      message: BoundedString.t option [@ocaml.doc "See applicable actions."]}
    let make ?name = fun ?message -> fun () -> { name; message }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:BoundedString.to_value));
        ("message", (Option.map x.message ~f:BoundedString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:BoundedString.of_xml) (Xml.child xml_arg0 "message") in
      let name =
        (Option.map ~f:BoundedString.of_xml) (Xml.child xml_arg0 "name") in
      make ?message ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" BoundedString.of_json in
      let name = field_map json__ "name" BoundedString.of_json in
      make ?message ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The input fails to satisfy the constraints specified by the service."]
module ChargeRevision =
  struct
    type nonrec t = Int64.t
    let make i =
      let open Result in ok_or_failwith (check_int64_min i ~min:1L); i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module PurchaseOrderReference =
  struct
    type nonrec t = string
    let context_ = "PurchaseOrderReference"
    let make i =
      let open Result in ok_or_failwith (check_string_min i ~min:1); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PurchaseOrderReference" j
    let to_json = simple_to_json to_value
  end
module Acceptor =
  struct
    type nonrec t =
      {
      accountId: AWSAccountId.t option
        [@ocaml.doc "The AWS account ID of the acceptor."]}
    let make ?accountId = fun () -> { accountId }
    let to_value x =
      structure_to_value
        [("accountId", (Option.map x.accountId ~f:AWSAccountId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let accountId =
        (Option.map ~f:AWSAccountId.of_xml) (Xml.child xml_arg0 "accountId") in
      make ?accountId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let accountId = field_map json__ "accountId" AWSAccountId.of_json in
      make ?accountId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The details of the party accepting the agreement terms. This is commonly the buyer for PurchaseAgreement."]
module AgreementStatus =
  struct
    type nonrec t =
      | ACTIVE 
      | ARCHIVED 
      | CANCELLED 
      | EXPIRED 
      | RENEWED 
      | REPLACED 
      | ROLLED_BACK 
      | SUPERSEDED 
      | TERMINATED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTIVE -> "ACTIVE"
      | ARCHIVED -> "ARCHIVED"
      | CANCELLED -> "CANCELLED"
      | EXPIRED -> "EXPIRED"
      | RENEWED -> "RENEWED"
      | REPLACED -> "REPLACED"
      | ROLLED_BACK -> "ROLLED_BACK"
      | SUPERSEDED -> "SUPERSEDED"
      | TERMINATED -> "TERMINATED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTIVE" -> ACTIVE
      | "ARCHIVED" -> ARCHIVED
      | "CANCELLED" -> CANCELLED
      | "EXPIRED" -> EXPIRED
      | "RENEWED" -> RENEWED
      | "REPLACED" -> REPLACED
      | "ROLLED_BACK" -> ROLLED_BACK
      | "SUPERSEDED" -> SUPERSEDED
      | "TERMINATED" -> TERMINATED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration AgreementStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AgreementStatus" j)
    let to_json = simple_to_json to_value
  end
module AgreementType =
  struct
    type nonrec t = string
    let context_ = "AgreementType"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"[A-Za-z]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AgreementType" j
    let to_json = simple_to_json to_value
  end
module ProposalSummary =
  struct
    type nonrec t =
      {
      resources: Resources.t option
        [@ocaml.doc "The list of resources involved in the agreement."];
      offerId: OfferId.t option
        [@ocaml.doc "The unique identifier of the offer in AWS Marketplace."];
      offerSetId: OfferSetId.t option
        [@ocaml.doc
          "A unique identifier for the offer set containing this offer. All agreements created from offers in this set include this identifier as context."]}
    let make ?resources =
      fun ?offerId ->
        fun ?offerSetId -> fun () -> { resources; offerId; offerSetId }
    let to_value x =
      structure_to_value
        [("resources", (Option.map x.resources ~f:Resources.to_value));
        ("offerId", (Option.map x.offerId ~f:OfferId.to_value));
        ("offerSetId", (Option.map x.offerSetId ~f:OfferSetId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let offerSetId =
        (Option.map ~f:OfferSetId.of_xml) (Xml.child xml_arg0 "offerSetId") in
      let offerId =
        (Option.map ~f:OfferId.of_xml) (Xml.child xml_arg0 "offerId") in
      let resources =
        (Option.map ~f:Resources.of_xml) (Xml.child xml_arg0 "resources") in
      make ?offerSetId ?offerId ?resources ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let offerSetId = field_map json__ "offerSetId" OfferSetId.of_json in
      let offerId = field_map json__ "offerId" OfferId.of_json in
      let resources = field_map json__ "resources" Resources.of_json in
      make ?offerSetId ?offerId ?resources ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A summary of the proposal received from the proposer."]
module Proposer =
  struct
    type nonrec t =
      {
      accountId: AWSAccountId.t option
        [@ocaml.doc "The AWS account ID of the proposer."]}
    let make ?accountId = fun () -> { accountId }
    let to_value x =
      structure_to_value
        [("accountId", (Option.map x.accountId ~f:AWSAccountId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let accountId =
        (Option.map ~f:AWSAccountId.of_xml) (Xml.child xml_arg0 "accountId") in
      make ?accountId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let accountId = field_map json__ "accountId" AWSAccountId.of_json in
      make ?accountId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Details of the party proposing the agreement terms,. This is commonly the seller for PurchaseAgreement."]
module FilterName =
  struct
    type nonrec t = string
    let context_ = "FilterName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:32) >>=
                  (fun () -> check_pattern i ~pattern:"[A-Za-z_]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FilterName" j
    let to_json = simple_to_json to_value
  end
module FilterValueList =
  struct
    type nonrec t = FilterValue.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:1) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:FilterValue.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:FilterValue.of_xml)
    let of_json j =
      list_of_json ~kind:"FilterValueList" ~of_json:FilterValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module AgreementId =
  struct
    type nonrec t = string
    let context_ = "AgreementId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"[A-Za-z0-9_/-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AgreementId" j
    let to_json = simple_to_json to_value
  end
module BillingAdjustmentRequestId =
  struct
    type nonrec t = string
    let context_ = "BillingAdjustmentRequestId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"ba-[a-zA-Z0-9]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BillingAdjustmentRequestId" j
    let to_json = simple_to_json to_value
  end
module BillingAdjustmentStatus =
  struct
    type nonrec t =
      | PENDING 
      | VALIDATION_FAILED 
      | COMPLETED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PENDING -> "PENDING"
      | VALIDATION_FAILED -> "VALIDATION_FAILED"
      | COMPLETED -> "COMPLETED"
      | Non_static_id s -> s
    let of_string =
      function
      | "PENDING" -> PENDING
      | "VALIDATION_FAILED" -> VALIDATION_FAILED
      | "COMPLETED" -> COMPLETED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration BillingAdjustmentStatus" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"BillingAdjustmentStatus" j)
    let to_json = simple_to_json to_value
  end
module Catalog =
  struct
    type nonrec t = string
    let context_ = "Catalog"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9.-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Catalog" j
    let to_json = simple_to_json to_value
  end
module InvoiceId =
  struct
    type nonrec t = string
    let context_ = "InvoiceId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:255) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"InvoiceId" j
    let to_json = simple_to_json to_value
  end
module PositiveAmountUpto8Decimals =
  struct
    type nonrec t = string
    let context_ = "PositiveAmountUpto8Decimals"
    let make i =
      let open Result in
        ok_or_failwith (check_pattern i ~pattern:"[0-9]*(\\.[0-9]{0,8})?"); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PositiveAmountUpto8Decimals" j
    let to_json = simple_to_json to_value
  end
module ChargeId =
  struct
    type nonrec t = string
    let context_ = "ChargeId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"ch-[a-zA-Z0-9]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ChargeId" j
    let to_json = simple_to_json to_value
  end
module PaymentRequestId =
  struct
    type nonrec t = string
    let context_ = "PaymentRequestId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"pr-[a-zA-Z0-9]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PaymentRequestId" j
    let to_json = simple_to_json to_value
  end
module PaymentRequestName =
  struct
    type nonrec t = string
    let context_ = "PaymentRequestName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:5) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:".+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PaymentRequestName" j
    let to_json = simple_to_json to_value
  end
module PaymentRequestStatus =
  struct
    type nonrec t =
      | VALIDATING 
      | VALIDATION_FAILED 
      | PENDING_APPROVAL 
      | APPROVED 
      | REJECTED 
      | CANCELLED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | VALIDATING -> "VALIDATING"
      | VALIDATION_FAILED -> "VALIDATION_FAILED"
      | PENDING_APPROVAL -> "PENDING_APPROVAL"
      | APPROVED -> "APPROVED"
      | REJECTED -> "REJECTED"
      | CANCELLED -> "CANCELLED"
      | Non_static_id s -> s
    let of_string =
      function
      | "VALIDATING" -> VALIDATING
      | "VALIDATION_FAILED" -> VALIDATION_FAILED
      | "PENDING_APPROVAL" -> PENDING_APPROVAL
      | "APPROVED" -> APPROVED
      | "REJECTED" -> REJECTED
      | "CANCELLED" -> CANCELLED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration PaymentRequestStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"PaymentRequestStatus" j)
    let to_json = simple_to_json to_value
  end
module InvoiceBillingPeriod =
  struct
    type nonrec t =
      {
      month: InvoiceBillingPeriodMonthInteger.t
        [@ocaml.doc "The billing period month. Valid range: 1-12."];
      year: InvoiceBillingPeriodYearInteger.t
        [@ocaml.doc "The billing period year."]}
    let context_ = "InvoiceBillingPeriod"
    let make ~month = fun ~year -> fun () -> { month; year }
    let to_value x =
      structure_to_value
        [("month",
           (Some (InvoiceBillingPeriodMonthInteger.to_value x.month)));
        ("year", (Some (InvoiceBillingPeriodYearInteger.to_value x.year)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let year =
        InvoiceBillingPeriodYearInteger.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "year") in
      let month =
        InvoiceBillingPeriodMonthInteger.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "month") in
      make ~year ~month ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let year =
        field_map_exn json__ "year" InvoiceBillingPeriodYearInteger.of_json in
      let month =
        field_map_exn json__ "month" InvoiceBillingPeriodMonthInteger.of_json in
      make ~year ~month ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The billing period for an invoice, specified by month and year."]
module InvoiceType =
  struct
    type nonrec t =
      | INVOICE 
      | CREDIT_MEMO 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | INVOICE -> "INVOICE"
      | CREDIT_MEMO -> "CREDIT_MEMO"
      | Non_static_id s -> s
    let of_string =
      function
      | "INVOICE" -> INVOICE
      | "CREDIT_MEMO" -> CREDIT_MEMO
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration InvoiceType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"InvoiceType" j)
    let to_json = simple_to_json to_value
  end
module InvoicingEntity =
  struct
    type nonrec t =
      {
      legalName: BoundedString.t option
        [@ocaml.doc "Legal name of the entity issuing the invoice."];
      branchName: BoundedString.t option
        [@ocaml.doc "The branch where the issuing entity is operating from."]}
    let make ?legalName =
      fun ?branchName -> fun () -> { legalName; branchName }
    let to_value x =
      structure_to_value
        [("legalName", (Option.map x.legalName ~f:BoundedString.to_value));
        ("branchName", (Option.map x.branchName ~f:BoundedString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let branchName =
        (Option.map ~f:BoundedString.of_xml)
          (Xml.child xml_arg0 "branchName") in
      let legalName =
        (Option.map ~f:BoundedString.of_xml) (Xml.child xml_arg0 "legalName") in
      make ?branchName ?legalName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let branchName = field_map json__ "branchName" BoundedString.of_json in
      let legalName = field_map json__ "legalName" BoundedString.of_json in
      make ?branchName ?legalName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The entity responsible for issuing the invoice."]
module PricingCurrencyAmount =
  struct
    type nonrec t =
      {
      amount: BoundedString.t option
        [@ocaml.doc "The monetary amount before tax."];
      maxAdjustmentAmount: BoundedString.t option
        [@ocaml.doc
          "The maximum refundable amount as a string representation of a decimal number."];
      currencyCode: CurrencyCode.t option
        [@ocaml.doc
          "The 3-letter ISO 4217 currency code (e.g., USD, EUR, JPY)."]}
    let make ?amount =
      fun ?maxAdjustmentAmount ->
        fun ?currencyCode ->
          fun () -> { amount; maxAdjustmentAmount; currencyCode }
    let to_value x =
      structure_to_value
        [("amount", (Option.map x.amount ~f:BoundedString.to_value));
        ("maxAdjustmentAmount",
          (Option.map x.maxAdjustmentAmount ~f:BoundedString.to_value));
        ("currencyCode",
          (Option.map x.currencyCode ~f:CurrencyCode.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let currencyCode =
        (Option.map ~f:CurrencyCode.of_xml)
          (Xml.child xml_arg0 "currencyCode") in
      let maxAdjustmentAmount =
        (Option.map ~f:BoundedString.of_xml)
          (Xml.child xml_arg0 "maxAdjustmentAmount") in
      let amount =
        (Option.map ~f:BoundedString.of_xml) (Xml.child xml_arg0 "amount") in
      make ?currencyCode ?maxAdjustmentAmount ?amount ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let currencyCode = field_map json__ "currencyCode" CurrencyCode.of_json in
      let maxAdjustmentAmount =
        field_map json__ "maxAdjustmentAmount" BoundedString.of_json in
      let amount = field_map json__ "amount" BoundedString.of_json in
      make ?currencyCode ?maxAdjustmentAmount ?amount ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Monetary amounts associated with an invoice line item group."]
module AgreementCancellationRequestId =
  struct
    type nonrec t = string
    let context_ = "AgreementCancellationRequestId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"acr-[a-zA-Z0-9]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AgreementCancellationRequestId" j
    let to_json = simple_to_json to_value
  end
module AgreementCancellationRequestReasonCode =
  struct
    type nonrec t =
      | INCORRECT_TERMS_ACCEPTED 
      | REPLACING_AGREEMENT 
      | TEST_AGREEMENT 
      | ALTERNATIVE_PROCUREMENT_CHANNEL 
      | PRODUCT_DISCONTINUED 
      | UNINTENDED_RENEWAL 
      | BUYER_DISSATISFACTION 
      | OTHER 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | INCORRECT_TERMS_ACCEPTED -> "INCORRECT_TERMS_ACCEPTED"
      | REPLACING_AGREEMENT -> "REPLACING_AGREEMENT"
      | TEST_AGREEMENT -> "TEST_AGREEMENT"
      | ALTERNATIVE_PROCUREMENT_CHANNEL -> "ALTERNATIVE_PROCUREMENT_CHANNEL"
      | PRODUCT_DISCONTINUED -> "PRODUCT_DISCONTINUED"
      | UNINTENDED_RENEWAL -> "UNINTENDED_RENEWAL"
      | BUYER_DISSATISFACTION -> "BUYER_DISSATISFACTION"
      | OTHER -> "OTHER"
      | Non_static_id s -> s
    let of_string =
      function
      | "INCORRECT_TERMS_ACCEPTED" -> INCORRECT_TERMS_ACCEPTED
      | "REPLACING_AGREEMENT" -> REPLACING_AGREEMENT
      | "TEST_AGREEMENT" -> TEST_AGREEMENT
      | "ALTERNATIVE_PROCUREMENT_CHANNEL" -> ALTERNATIVE_PROCUREMENT_CHANNEL
      | "PRODUCT_DISCONTINUED" -> PRODUCT_DISCONTINUED
      | "UNINTENDED_RENEWAL" -> UNINTENDED_RENEWAL
      | "BUYER_DISSATISFACTION" -> BUYER_DISSATISFACTION
      | "OTHER" -> OTHER
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml
           ~kind:"enumeration AgreementCancellationRequestReasonCode"
           xml_arg0)
    let of_json j =
      of_string
        (string_of_json ~kind:"AgreementCancellationRequestReasonCode" j)
    let to_json = simple_to_json to_value
  end
module AgreementCancellationRequestStatus =
  struct
    type nonrec t =
      | PENDING_APPROVAL 
      | APPROVED 
      | REJECTED 
      | CANCELLED 
      | VALIDATION_FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PENDING_APPROVAL -> "PENDING_APPROVAL"
      | APPROVED -> "APPROVED"
      | REJECTED -> "REJECTED"
      | CANCELLED -> "CANCELLED"
      | VALIDATION_FAILED -> "VALIDATION_FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "PENDING_APPROVAL" -> PENDING_APPROVAL
      | "APPROVED" -> APPROVED
      | "REJECTED" -> REJECTED
      | "CANCELLED" -> CANCELLED
      | "VALIDATION_FAILED" -> VALIDATION_FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration AgreementCancellationRequestStatus"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"AgreementCancellationRequestStatus" j)
    let to_json = simple_to_json to_value
  end
module ByolPricingTerm =
  struct
    type nonrec t =
      {
      type_: UnversionedTermType.t option
        [@ocaml.doc "Type of the term being updated."];
      id: TermId.t option [@ocaml.doc "The unique identifier for the term."]}
    let make ?type_ = fun ?id -> fun () -> { type_; id }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:UnversionedTermType.to_value));
        ("id", (Option.map x.id ~f:TermId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id = (Option.map ~f:TermId.of_xml) (Xml.child xml_arg0 "id") in
      let type_ =
        (Option.map ~f:UnversionedTermType.of_xml)
          (Xml.child xml_arg0 "type") in
      make ?id ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let id = field_map json__ "id" TermId.of_json in
      let type_ = field_map json__ "type" UnversionedTermType.of_json in
      make ?id ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Enables you and your customers to move your existing agreements to AWS Marketplace. The customer won't be charged for product usage in AWS Marketplace because they already paid for the product outside of AWS Marketplace."]
module ConfigurableUpfrontPricingTerm =
  struct
    type nonrec t =
      {
      type_: UnversionedTermType.t option
        [@ocaml.doc "Category of selector."];
      id: TermId.t option [@ocaml.doc "The unique identifier of the term."];
      currencyCode: CurrencyCode.t option
        [@ocaml.doc
          "Defines the currency for the prices mentioned in the term."];
      rateCards: ConfigurableUpfrontRateCardList.t option
        [@ocaml.doc
          "A rate card defines the per unit rates for product dimensions."];
      configuration: ConfigurableUpfrontPricingTermConfiguration.t option
        [@ocaml.doc
          "Additional parameters specified by the acceptor while accepting the term."]}
    let make ?type_ =
      fun ?id ->
        fun ?currencyCode ->
          fun ?rateCards ->
            fun ?configuration ->
              fun () -> { type_; id; currencyCode; rateCards; configuration }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:UnversionedTermType.to_value));
        ("id", (Option.map x.id ~f:TermId.to_value));
        ("currencyCode",
          (Option.map x.currencyCode ~f:CurrencyCode.to_value));
        ("rateCards",
          (Option.map x.rateCards ~f:ConfigurableUpfrontRateCardList.to_value));
        ("configuration",
          (Option.map x.configuration
             ~f:ConfigurableUpfrontPricingTermConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configuration =
        (Option.map ~f:ConfigurableUpfrontPricingTermConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      let rateCards =
        (Option.map ~f:ConfigurableUpfrontRateCardList.of_xml)
          (Xml.child xml_arg0 "rateCards") in
      let currencyCode =
        (Option.map ~f:CurrencyCode.of_xml)
          (Xml.child xml_arg0 "currencyCode") in
      let id = (Option.map ~f:TermId.of_xml) (Xml.child xml_arg0 "id") in
      let type_ =
        (Option.map ~f:UnversionedTermType.of_xml)
          (Xml.child xml_arg0 "type") in
      make ?configuration ?rateCards ?currencyCode ?id ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let configuration =
        field_map json__ "configuration"
          ConfigurableUpfrontPricingTermConfiguration.of_json in
      let rateCards =
        field_map json__ "rateCards" ConfigurableUpfrontRateCardList.of_json in
      let currencyCode = field_map json__ "currencyCode" CurrencyCode.of_json in
      let id = field_map json__ "id" TermId.of_json in
      let type_ = field_map json__ "type" UnversionedTermType.of_json in
      make ?configuration ?rateCards ?currencyCode ?id ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines a prepaid payment model that allows buyers to configure the entitlements they want to purchase and the duration."]
module FixedUpfrontPricingTerm =
  struct
    type nonrec t =
      {
      type_: UnversionedTermType.t option
        [@ocaml.doc "Category of the term being updated."];
      id: TermId.t option [@ocaml.doc "The unique identifier for the term."];
      currencyCode: CurrencyCode.t option
        [@ocaml.doc
          "Defines the currency for the prices mentioned in this term."];
      duration: BoundedString.t option
        [@ocaml.doc "Contract duration for the terms."];
      price: BoundedString.t option
        [@ocaml.doc
          "Fixed amount to be charged to the customer when this term is accepted."];
      grants: GrantList.t option
        [@ocaml.doc
          "Entitlements granted to the acceptor of fixed upfront as part of agreement execution."]}
    let make ?type_ =
      fun ?id ->
        fun ?currencyCode ->
          fun ?duration ->
            fun ?price ->
              fun ?grants ->
                fun () ->
                  { type_; id; currencyCode; duration; price; grants }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:UnversionedTermType.to_value));
        ("id", (Option.map x.id ~f:TermId.to_value));
        ("currencyCode",
          (Option.map x.currencyCode ~f:CurrencyCode.to_value));
        ("duration", (Option.map x.duration ~f:BoundedString.to_value));
        ("price", (Option.map x.price ~f:BoundedString.to_value));
        ("grants", (Option.map x.grants ~f:GrantList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let grants =
        (Option.map ~f:GrantList.of_xml) (Xml.child xml_arg0 "grants") in
      let price =
        (Option.map ~f:BoundedString.of_xml) (Xml.child xml_arg0 "price") in
      let duration =
        (Option.map ~f:BoundedString.of_xml) (Xml.child xml_arg0 "duration") in
      let currencyCode =
        (Option.map ~f:CurrencyCode.of_xml)
          (Xml.child xml_arg0 "currencyCode") in
      let id = (Option.map ~f:TermId.of_xml) (Xml.child xml_arg0 "id") in
      let type_ =
        (Option.map ~f:UnversionedTermType.of_xml)
          (Xml.child xml_arg0 "type") in
      make ?grants ?price ?duration ?currencyCode ?id ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let grants = field_map json__ "grants" GrantList.of_json in
      let price = field_map json__ "price" BoundedString.of_json in
      let duration = field_map json__ "duration" BoundedString.of_json in
      let currencyCode = field_map json__ "currencyCode" CurrencyCode.of_json in
      let id = field_map json__ "id" TermId.of_json in
      let type_ = field_map json__ "type" UnversionedTermType.of_json in
      make ?grants ?price ?duration ?currencyCode ?id ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines a prepaid pricing model where the customers are charged a fixed upfront amount."]
module FreeTrialPricingTerm =
  struct
    type nonrec t =
      {
      type_: UnversionedTermType.t option
        [@ocaml.doc "Category of the term."];
      id: TermId.t option [@ocaml.doc "The unique identifier for the terms."];
      duration: BoundedString.t option
        [@ocaml.doc
          "Duration of the free trial period (5\226\128\14731 days)."];
      grants: GrantList.t option
        [@ocaml.doc
          "Entitlements granted to the acceptor of a free trial as part of an agreement execution."]}
    let make ?type_ =
      fun ?id ->
        fun ?duration ->
          fun ?grants -> fun () -> { type_; id; duration; grants }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:UnversionedTermType.to_value));
        ("id", (Option.map x.id ~f:TermId.to_value));
        ("duration", (Option.map x.duration ~f:BoundedString.to_value));
        ("grants", (Option.map x.grants ~f:GrantList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let grants =
        (Option.map ~f:GrantList.of_xml) (Xml.child xml_arg0 "grants") in
      let duration =
        (Option.map ~f:BoundedString.of_xml) (Xml.child xml_arg0 "duration") in
      let id = (Option.map ~f:TermId.of_xml) (Xml.child xml_arg0 "id") in
      let type_ =
        (Option.map ~f:UnversionedTermType.of_xml)
          (Xml.child xml_arg0 "type") in
      make ?grants ?duration ?id ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let grants = field_map json__ "grants" GrantList.of_json in
      let duration = field_map json__ "duration" BoundedString.of_json in
      let id = field_map json__ "id" TermId.of_json in
      let type_ = field_map json__ "type" UnversionedTermType.of_json in
      make ?grants ?duration ?id ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines a short-term free pricing model where the buyers aren\226\128\153t charged anything within a specified limit."]
module LegalTerm =
  struct
    type nonrec t =
      {
      type_: UnversionedTermType.t option
        [@ocaml.doc "Category of the term being updated."];
      id: TermId.t option [@ocaml.doc "The unique identifer for the term."];
      documents: DocumentList.t option
        [@ocaml.doc
          "List of references to legal resources proposed to the buyers. An example is the EULA."]}
    let make ?type_ =
      fun ?id -> fun ?documents -> fun () -> { type_; id; documents }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:UnversionedTermType.to_value));
        ("id", (Option.map x.id ~f:TermId.to_value));
        ("documents", (Option.map x.documents ~f:DocumentList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let documents =
        (Option.map ~f:DocumentList.of_xml) (Xml.child xml_arg0 "documents") in
      let id = (Option.map ~f:TermId.of_xml) (Xml.child xml_arg0 "id") in
      let type_ =
        (Option.map ~f:UnversionedTermType.of_xml)
          (Xml.child xml_arg0 "type") in
      make ?documents ?id ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let documents = field_map json__ "documents" DocumentList.of_json in
      let id = field_map json__ "id" TermId.of_json in
      let type_ = field_map json__ "type" UnversionedTermType.of_json in
      make ?documents ?id ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines the list of text agreements proposed to the acceptors. An example is the end user license agreement (EULA)."]
module PaymentScheduleTerm =
  struct
    type nonrec t =
      {
      type_: UnversionedTermType.t option [@ocaml.doc "Type of the term."];
      id: TermId.t option [@ocaml.doc "The unique identifier for the term."];
      currencyCode: CurrencyCode.t option
        [@ocaml.doc
          "Defines the currency for the prices mentioned in the term."];
      schedule: ScheduleList.t option
        [@ocaml.doc
          "List of the payment schedule where each element defines one installment of payment. It contains the information necessary for calculating the price."]}
    let make ?type_ =
      fun ?id ->
        fun ?currencyCode ->
          fun ?schedule -> fun () -> { type_; id; currencyCode; schedule }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:UnversionedTermType.to_value));
        ("id", (Option.map x.id ~f:TermId.to_value));
        ("currencyCode",
          (Option.map x.currencyCode ~f:CurrencyCode.to_value));
        ("schedule", (Option.map x.schedule ~f:ScheduleList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let schedule =
        (Option.map ~f:ScheduleList.of_xml) (Xml.child xml_arg0 "schedule") in
      let currencyCode =
        (Option.map ~f:CurrencyCode.of_xml)
          (Xml.child xml_arg0 "currencyCode") in
      let id = (Option.map ~f:TermId.of_xml) (Xml.child xml_arg0 "id") in
      let type_ =
        (Option.map ~f:UnversionedTermType.of_xml)
          (Xml.child xml_arg0 "type") in
      make ?schedule ?currencyCode ?id ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let schedule = field_map json__ "schedule" ScheduleList.of_json in
      let currencyCode = field_map json__ "currencyCode" CurrencyCode.of_json in
      let id = field_map json__ "id" TermId.of_json in
      let type_ = field_map json__ "type" UnversionedTermType.of_json in
      make ?schedule ?currencyCode ?id ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines an installment-based pricing model where customers are charged a fixed price on different dates during the agreement validity period. This is used most commonly for flexible payment schedule pricing."]
module RecurringPaymentTerm =
  struct
    type nonrec t =
      {
      type_: UnversionedTermType.t option
        [@ocaml.doc "Type of the term being updated."];
      id: TermId.t option [@ocaml.doc "The unique identifier for the term."];
      currencyCode: CurrencyCode.t option
        [@ocaml.doc
          "Defines the currency for the prices mentioned in this term."];
      billingPeriod: BoundedString.t option
        [@ocaml.doc "Defines the recurrence at which buyers are charged."];
      price: BoundedString.t option
        [@ocaml.doc "Amount charged to the buyer every billing period."]}
    let make ?type_ =
      fun ?id ->
        fun ?currencyCode ->
          fun ?billingPeriod ->
            fun ?price ->
              fun () -> { type_; id; currencyCode; billingPeriod; price }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:UnversionedTermType.to_value));
        ("id", (Option.map x.id ~f:TermId.to_value));
        ("currencyCode",
          (Option.map x.currencyCode ~f:CurrencyCode.to_value));
        ("billingPeriod",
          (Option.map x.billingPeriod ~f:BoundedString.to_value));
        ("price", (Option.map x.price ~f:BoundedString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let price =
        (Option.map ~f:BoundedString.of_xml) (Xml.child xml_arg0 "price") in
      let billingPeriod =
        (Option.map ~f:BoundedString.of_xml)
          (Xml.child xml_arg0 "billingPeriod") in
      let currencyCode =
        (Option.map ~f:CurrencyCode.of_xml)
          (Xml.child xml_arg0 "currencyCode") in
      let id = (Option.map ~f:TermId.of_xml) (Xml.child xml_arg0 "id") in
      let type_ =
        (Option.map ~f:UnversionedTermType.of_xml)
          (Xml.child xml_arg0 "type") in
      make ?price ?billingPeriod ?currencyCode ?id ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let price = field_map json__ "price" BoundedString.of_json in
      let billingPeriod =
        field_map json__ "billingPeriod" BoundedString.of_json in
      let currencyCode = field_map json__ "currencyCode" CurrencyCode.of_json in
      let id = field_map json__ "id" TermId.of_json in
      let type_ = field_map json__ "type" UnversionedTermType.of_json in
      make ?price ?billingPeriod ?currencyCode ?id ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines a pricing model where customers are charged a fixed recurring price at the end of each billing period."]
module RenewalTerm =
  struct
    type nonrec t =
      {
      type_: UnversionedTermType.t option
        [@ocaml.doc "Category of the term being updated."];
      id: TermId.t option [@ocaml.doc "The unique identifier for the term."];
      configuration: RenewalTermConfiguration.t option
        [@ocaml.doc
          "Additional parameters specified by the acceptor while accepting the term."]}
    let make ?type_ =
      fun ?id -> fun ?configuration -> fun () -> { type_; id; configuration }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:UnversionedTermType.to_value));
        ("id", (Option.map x.id ~f:TermId.to_value));
        ("configuration",
          (Option.map x.configuration ~f:RenewalTermConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configuration =
        (Option.map ~f:RenewalTermConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      let id = (Option.map ~f:TermId.of_xml) (Xml.child xml_arg0 "id") in
      let type_ =
        (Option.map ~f:UnversionedTermType.of_xml)
          (Xml.child xml_arg0 "type") in
      make ?configuration ?id ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let configuration =
        field_map json__ "configuration" RenewalTermConfiguration.of_json in
      let id = field_map json__ "id" TermId.of_json in
      let type_ = field_map json__ "type" UnversionedTermType.of_json in
      make ?configuration ?id ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines that on graceful expiration of the agreement (when the agreement ends on its pre-defined end date), a new agreement will be created using the accepted terms on the existing agreement. In other words, the agreement will be renewed. The presence of RenewalTerm in the offer document means that auto-renewal is allowed. Buyers will have the option to accept or decline auto-renewal at the offer acceptance/agreement creation. Buyers can also change this flag from True to False or False to True at anytime during the agreement's lifecycle."]
module SupportTerm =
  struct
    type nonrec t =
      {
      type_: UnversionedTermType.t option
        [@ocaml.doc "Category of the term being updated."];
      id: TermId.t option [@ocaml.doc "The unique identifier for the term."];
      refundPolicy: BoundedString.t option
        [@ocaml.doc
          "Free-text field about the refund policy description that will be shown to customers as is on the website and console."]}
    let make ?type_ =
      fun ?id -> fun ?refundPolicy -> fun () -> { type_; id; refundPolicy }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:UnversionedTermType.to_value));
        ("id", (Option.map x.id ~f:TermId.to_value));
        ("refundPolicy",
          (Option.map x.refundPolicy ~f:BoundedString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let refundPolicy =
        (Option.map ~f:BoundedString.of_xml)
          (Xml.child xml_arg0 "refundPolicy") in
      let id = (Option.map ~f:TermId.of_xml) (Xml.child xml_arg0 "id") in
      let type_ =
        (Option.map ~f:UnversionedTermType.of_xml)
          (Xml.child xml_arg0 "type") in
      make ?refundPolicy ?id ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let refundPolicy =
        field_map json__ "refundPolicy" BoundedString.of_json in
      let id = field_map json__ "id" TermId.of_json in
      let type_ = field_map json__ "type" UnversionedTermType.of_json in
      make ?refundPolicy ?id ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines the customer support available for the acceptors when they purchase the software."]
module UsageBasedPricingTerm =
  struct
    type nonrec t =
      {
      type_: UnversionedTermType.t option
        [@ocaml.doc "Category of the term."];
      id: TermId.t option [@ocaml.doc "The unique identifier for the term."];
      currencyCode: CurrencyCode.t option
        [@ocaml.doc
          "Defines the currency for the prices mentioned in the term."];
      rateCards: UsageBasedRateCardList.t option
        [@ocaml.doc "List of rate cards."]}
    let make ?type_ =
      fun ?id ->
        fun ?currencyCode ->
          fun ?rateCards -> fun () -> { type_; id; currencyCode; rateCards }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:UnversionedTermType.to_value));
        ("id", (Option.map x.id ~f:TermId.to_value));
        ("currencyCode",
          (Option.map x.currencyCode ~f:CurrencyCode.to_value));
        ("rateCards",
          (Option.map x.rateCards ~f:UsageBasedRateCardList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let rateCards =
        (Option.map ~f:UsageBasedRateCardList.of_xml)
          (Xml.child xml_arg0 "rateCards") in
      let currencyCode =
        (Option.map ~f:CurrencyCode.of_xml)
          (Xml.child xml_arg0 "currencyCode") in
      let id = (Option.map ~f:TermId.of_xml) (Xml.child xml_arg0 "id") in
      let type_ =
        (Option.map ~f:UnversionedTermType.of_xml)
          (Xml.child xml_arg0 "type") in
      make ?rateCards ?currencyCode ?id ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let rateCards =
        field_map json__ "rateCards" UsageBasedRateCardList.of_json in
      let currencyCode = field_map json__ "currencyCode" CurrencyCode.of_json in
      let id = field_map json__ "id" TermId.of_json in
      let type_ = field_map json__ "type" UnversionedTermType.of_json in
      make ?rateCards ?currencyCode ?id ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines a usage-based pricing model (typically, pay-as-you-go pricing), where the customers are charged based on product usage."]
module ValidityTerm =
  struct
    type nonrec t =
      {
      type_: UnversionedTermType.t option
        [@ocaml.doc "Category of the term being updated."];
      id: TermId.t option [@ocaml.doc "The unique identifier for the term."];
      agreementDuration: BoundedString.t option
        [@ocaml.doc
          "Defines the duration that the agreement remains active. If AgreementStartDate isn\226\128\153t provided, the agreement duration is relative to the agreement signature time. The duration is represented in the ISO_8601 format."];
      agreementStartDate: Timestamp.t option
        [@ocaml.doc
          "Defines the date when agreement starts. The agreement starts at 00:00:00.000 UTC on the date provided. If AgreementStartDate isn\226\128\153t provided, the agreement start date is determined based on agreement signature time."];
      agreementEndDate: Timestamp.t option
        [@ocaml.doc
          "Defines the date when the agreement ends. The agreement ends at 23:59:59.999 UTC on the date provided. If AgreementEndDate isn\226\128\153t provided, the agreement end date is determined by the validity of individual terms."]}
    let make ?type_ =
      fun ?id ->
        fun ?agreementDuration ->
          fun ?agreementStartDate ->
            fun ?agreementEndDate ->
              fun () ->
                {
                  type_;
                  id;
                  agreementDuration;
                  agreementStartDate;
                  agreementEndDate
                }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:UnversionedTermType.to_value));
        ("id", (Option.map x.id ~f:TermId.to_value));
        ("agreementDuration",
          (Option.map x.agreementDuration ~f:BoundedString.to_value));
        ("agreementStartDate",
          (Option.map x.agreementStartDate ~f:Timestamp.to_value));
        ("agreementEndDate",
          (Option.map x.agreementEndDate ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let agreementEndDate =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "agreementEndDate") in
      let agreementStartDate =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "agreementStartDate") in
      let agreementDuration =
        (Option.map ~f:BoundedString.of_xml)
          (Xml.child xml_arg0 "agreementDuration") in
      let id = (Option.map ~f:TermId.of_xml) (Xml.child xml_arg0 "id") in
      let type_ =
        (Option.map ~f:UnversionedTermType.of_xml)
          (Xml.child xml_arg0 "type") in
      make ?agreementEndDate ?agreementStartDate ?agreementDuration ?id
        ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let agreementEndDate =
        field_map json__ "agreementEndDate" Timestamp.of_json in
      let agreementStartDate =
        field_map json__ "agreementStartDate" Timestamp.of_json in
      let agreementDuration =
        field_map json__ "agreementDuration" BoundedString.of_json in
      let id = field_map json__ "id" TermId.of_json in
      let type_ = field_map json__ "type" UnversionedTermType.of_json in
      make ?agreementEndDate ?agreementStartDate ?agreementDuration ?id
        ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines the conditions that will keep an agreement created from this offer valid."]
module VariablePaymentTerm =
  struct
    type nonrec t =
      {
      type_: UnversionedTermType.t option [@ocaml.doc "Type of the term."];
      id: TermId.t option [@ocaml.doc "The unique identifier for the term."];
      currencyCode: CurrencyCode.t option
        [@ocaml.doc
          "Defines the currency for the prices mentioned in the term."];
      maxTotalChargeAmount: BoundedString.t option
        [@ocaml.doc
          "The maximum total amount that can be charged to the customer through variable payment requests under this term."];
      configuration: VariablePaymentTermConfiguration.t option
        [@ocaml.doc
          "Additional parameters specified by the acceptor while accepting the term."]}
    let make ?type_ =
      fun ?id ->
        fun ?currencyCode ->
          fun ?maxTotalChargeAmount ->
            fun ?configuration ->
              fun () ->
                {
                  type_;
                  id;
                  currencyCode;
                  maxTotalChargeAmount;
                  configuration
                }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:UnversionedTermType.to_value));
        ("id", (Option.map x.id ~f:TermId.to_value));
        ("currencyCode",
          (Option.map x.currencyCode ~f:CurrencyCode.to_value));
        ("maxTotalChargeAmount",
          (Option.map x.maxTotalChargeAmount ~f:BoundedString.to_value));
        ("configuration",
          (Option.map x.configuration
             ~f:VariablePaymentTermConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configuration =
        (Option.map ~f:VariablePaymentTermConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      let maxTotalChargeAmount =
        (Option.map ~f:BoundedString.of_xml)
          (Xml.child xml_arg0 "maxTotalChargeAmount") in
      let currencyCode =
        (Option.map ~f:CurrencyCode.of_xml)
          (Xml.child xml_arg0 "currencyCode") in
      let id = (Option.map ~f:TermId.of_xml) (Xml.child xml_arg0 "id") in
      let type_ =
        (Option.map ~f:UnversionedTermType.of_xml)
          (Xml.child xml_arg0 "type") in
      make ?configuration ?maxTotalChargeAmount ?currencyCode ?id ?type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let configuration =
        field_map json__ "configuration"
          VariablePaymentTermConfiguration.of_json in
      let maxTotalChargeAmount =
        field_map json__ "maxTotalChargeAmount" BoundedString.of_json in
      let currencyCode = field_map json__ "currencyCode" CurrencyCode.of_json in
      let id = field_map json__ "id" TermId.of_json in
      let type_ = field_map json__ "type" UnversionedTermType.of_json in
      make ?configuration ?maxTotalChargeAmount ?currencyCode ?id ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines a payment model where sellers can submit variable payment requests up to a maximum charge amount, with configurable approval strategies and expiration timelines."]
module AgreementEntitlementStatus =
  struct
    type nonrec t =
      | PROVISIONED 
      | SCHEDULED 
      | PENDING 
      | FAILED 
      | DEPROVISIONED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PROVISIONED -> "PROVISIONED"
      | SCHEDULED -> "SCHEDULED"
      | PENDING -> "PENDING"
      | FAILED -> "FAILED"
      | DEPROVISIONED -> "DEPROVISIONED"
      | Non_static_id s -> s
    let of_string =
      function
      | "PROVISIONED" -> PROVISIONED
      | "SCHEDULED" -> SCHEDULED
      | "PENDING" -> PENDING
      | "FAILED" -> FAILED
      | "DEPROVISIONED" -> DEPROVISIONED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration AgreementEntitlementStatus"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"AgreementEntitlementStatus" j)
    let to_json = simple_to_json to_value
  end
module AgreementEntitlementStatusReasonCode =
  struct
    type nonrec t =
      | PROVISIONING_IN_PROGRESS 
      | FUTURE_START_DATE 
      | INVALID_PAYMENT_INSTRUMENT 
      | INCOMPATIBLE_CURRENCY 
      | ACCOUNT_SUSPENDED 
      | UNSUPPORTED_OPERATION 
      | AGREEMENT_INACTIVE 
      | AGREEMENT_ACTIVE 
      | PRODUCT_RESTRICTED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PROVISIONING_IN_PROGRESS -> "PROVISIONING_IN_PROGRESS"
      | FUTURE_START_DATE -> "FUTURE_START_DATE"
      | INVALID_PAYMENT_INSTRUMENT -> "INVALID_PAYMENT_INSTRUMENT"
      | INCOMPATIBLE_CURRENCY -> "INCOMPATIBLE_CURRENCY"
      | ACCOUNT_SUSPENDED -> "ACCOUNT_SUSPENDED"
      | UNSUPPORTED_OPERATION -> "UNSUPPORTED_OPERATION"
      | AGREEMENT_INACTIVE -> "AGREEMENT_INACTIVE"
      | AGREEMENT_ACTIVE -> "AGREEMENT_ACTIVE"
      | PRODUCT_RESTRICTED -> "PRODUCT_RESTRICTED"
      | Non_static_id s -> s
    let of_string =
      function
      | "PROVISIONING_IN_PROGRESS" -> PROVISIONING_IN_PROGRESS
      | "FUTURE_START_DATE" -> FUTURE_START_DATE
      | "INVALID_PAYMENT_INSTRUMENT" -> INVALID_PAYMENT_INSTRUMENT
      | "INCOMPATIBLE_CURRENCY" -> INCOMPATIBLE_CURRENCY
      | "ACCOUNT_SUSPENDED" -> ACCOUNT_SUSPENDED
      | "UNSUPPORTED_OPERATION" -> UNSUPPORTED_OPERATION
      | "AGREEMENT_INACTIVE" -> AGREEMENT_INACTIVE
      | "AGREEMENT_ACTIVE" -> AGREEMENT_ACTIVE
      | "PRODUCT_RESTRICTED" -> PRODUCT_RESTRICTED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml
           ~kind:"enumeration AgreementEntitlementStatusReasonCode" xml_arg0)
    let of_json j =
      of_string
        (string_of_json ~kind:"AgreementEntitlementStatusReasonCode" j)
    let to_json = simple_to_json to_value
  end
module AwsArn =
  struct
    type nonrec t = string
    let context_ = "AwsArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"arn:aws[a-zA-Z-]*:[A-Za-z0-9][A-Za-z0-9_/.-]{0,62}:[A-Za-z0-9_/.-]{0,63}:[A-Za-z0-9_/.-]{0,63}:[A-Za-z0-9][A-Za-z0-9:_/+=,@.-]{0,1023}")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AwsArn" j
    let to_json = simple_to_json to_value
  end
module EntitlementType =
  struct
    type nonrec t = string
    let context_ = "EntitlementType"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"[A-Za-z:]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"EntitlementType" j
    let to_json = simple_to_json to_value
  end
module RegistrationToken =
  struct
    type nonrec t = string
    let context_ = "RegistrationToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:512) >>=
                  (fun () -> check_pattern i ~pattern:"[A-Za-z0-9+/=.:_-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RegistrationToken" j
    let to_json = simple_to_json to_value
  end
module ExpectedCharge =
  struct
    type nonrec t =
      {
      id: ResourceId.t option
        [@ocaml.doc "Unique identifier of the charge for a given agreement."];
      time: Timestamp.t option
        [@ocaml.doc
          "The date and time when the charge is due to be invoiced. This is available only when the charge date is known."];
      amount: BoundedString.t option
        [@ocaml.doc
          "The tax-exclusive amount of the charge. Only available when the charge amount is known."];
      amountAfterTax: BoundedString.t option
        [@ocaml.doc
          "The tax-inclusive amount the acceptor has to pay. The amount is only present for fixed charges."];
      timing: Timing.t option
        [@ocaml.doc
          "Indicates when the charge amount will be incurred. Values include ON_ACCEPTANCE (charged immediately when the agreement request is accepted), BILLING_PERIOD (charged on each billing period), and SCHEDULED (charged at a predetermined future date)."];
      estimatedTaxes: EstimatedTaxes.t option
        [@ocaml.doc
          "Provides an aggregated view of estimated tax information for this specific charge."]}
    let make ?id =
      fun ?time ->
        fun ?amount ->
          fun ?amountAfterTax ->
            fun ?timing ->
              fun ?estimatedTaxes ->
                fun () ->
                  { id; time; amount; amountAfterTax; timing; estimatedTaxes
                  }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:ResourceId.to_value));
        ("time", (Option.map x.time ~f:Timestamp.to_value));
        ("amount", (Option.map x.amount ~f:BoundedString.to_value));
        ("amountAfterTax",
          (Option.map x.amountAfterTax ~f:BoundedString.to_value));
        ("timing", (Option.map x.timing ~f:Timing.to_value));
        ("estimatedTaxes",
          (Option.map x.estimatedTaxes ~f:EstimatedTaxes.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let estimatedTaxes =
        (Option.map ~f:EstimatedTaxes.of_xml)
          (Xml.child xml_arg0 "estimatedTaxes") in
      let timing =
        (Option.map ~f:Timing.of_xml) (Xml.child xml_arg0 "timing") in
      let amountAfterTax =
        (Option.map ~f:BoundedString.of_xml)
          (Xml.child xml_arg0 "amountAfterTax") in
      let amount =
        (Option.map ~f:BoundedString.of_xml) (Xml.child xml_arg0 "amount") in
      let time = (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "time") in
      let id = (Option.map ~f:ResourceId.of_xml) (Xml.child xml_arg0 "id") in
      make ?estimatedTaxes ?timing ?amountAfterTax ?amount ?time ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let estimatedTaxes =
        field_map json__ "estimatedTaxes" EstimatedTaxes.of_json in
      let timing = field_map json__ "timing" Timing.of_json in
      let amountAfterTax =
        field_map json__ "amountAfterTax" BoundedString.of_json in
      let amount = field_map json__ "amount" BoundedString.of_json in
      let time = field_map json__ "time" Timestamp.of_json in
      let id = field_map json__ "id" ResourceId.of_json in
      make ?estimatedTaxes ?timing ?amountAfterTax ?amount ?time ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Estimated charge for the request."]
module ItemizedCharge =
  struct
    type nonrec t =
      {
      dimensionKey: BoundedString.t option
        [@ocaml.doc "The dimension key as specified in the accepted term."];
      newQuantity: Integer.t option
        [@ocaml.doc "The requested quantity for this dimension."];
      oldQuantity: Integer.t option
        [@ocaml.doc
          "The existing quantity for this dimension from the source agreement. This value is 0 for NEW intent."];
      chargeReference: ResourceId.t option
        [@ocaml.doc
          "The identifier of the expected charge that this itemized charge contributes to."];
      incrementalChargeAmount: BoundedString.t option
        [@ocaml.doc
          "The total incremental charge amount for this dimension."]}
    let make ?dimensionKey =
      fun ?newQuantity ->
        fun ?oldQuantity ->
          fun ?chargeReference ->
            fun ?incrementalChargeAmount ->
              fun () ->
                {
                  dimensionKey;
                  newQuantity;
                  oldQuantity;
                  chargeReference;
                  incrementalChargeAmount
                }
    let to_value x =
      structure_to_value
        [("dimensionKey",
           (Option.map x.dimensionKey ~f:BoundedString.to_value));
        ("newQuantity", (Option.map x.newQuantity ~f:Integer.to_value));
        ("oldQuantity", (Option.map x.oldQuantity ~f:Integer.to_value));
        ("chargeReference",
          (Option.map x.chargeReference ~f:ResourceId.to_value));
        ("incrementalChargeAmount",
          (Option.map x.incrementalChargeAmount ~f:BoundedString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let incrementalChargeAmount =
        (Option.map ~f:BoundedString.of_xml)
          (Xml.child xml_arg0 "incrementalChargeAmount") in
      let chargeReference =
        (Option.map ~f:ResourceId.of_xml)
          (Xml.child xml_arg0 "chargeReference") in
      let oldQuantity =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "oldQuantity") in
      let newQuantity =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "newQuantity") in
      let dimensionKey =
        (Option.map ~f:BoundedString.of_xml)
          (Xml.child xml_arg0 "dimensionKey") in
      make ?incrementalChargeAmount ?chargeReference ?oldQuantity
        ?newQuantity ?dimensionKey ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let incrementalChargeAmount =
        field_map json__ "incrementalChargeAmount" BoundedString.of_json in
      let chargeReference =
        field_map json__ "chargeReference" ResourceId.of_json in
      let oldQuantity = field_map json__ "oldQuantity" Integer.of_json in
      let newQuantity = field_map json__ "newQuantity" Integer.of_json in
      let dimensionKey =
        field_map json__ "dimensionKey" BoundedString.of_json in
      make ?incrementalChargeAmount ?chargeReference ?oldQuantity
        ?newQuantity ?dimensionKey ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A breakdown of individual charges or line items within a billing or pricing context."]
module RequestedTermConfiguration =
  struct
    type nonrec t =
      {
      configurableUpfrontPricingTermConfiguration:
        ConfigurableUpfrontPricingTermConfiguration.t option ;
      renewalTermConfiguration: RenewalTermConfiguration.t option ;
      variablePaymentTermConfiguration:
        VariablePaymentTermConfiguration.t option }
    let make ?configurableUpfrontPricingTermConfiguration =
      fun ?renewalTermConfiguration ->
        fun ?variablePaymentTermConfiguration ->
          fun () ->
            {
              configurableUpfrontPricingTermConfiguration;
              renewalTermConfiguration;
              variablePaymentTermConfiguration
            }
    let to_value x =
      structure_to_value
        [("configurableUpfrontPricingTermConfiguration",
           (Option.map x.configurableUpfrontPricingTermConfiguration
              ~f:ConfigurableUpfrontPricingTermConfiguration.to_value));
        ("renewalTermConfiguration",
          (Option.map x.renewalTermConfiguration
             ~f:RenewalTermConfiguration.to_value));
        ("variablePaymentTermConfiguration",
          (Option.map x.variablePaymentTermConfiguration
             ~f:VariablePaymentTermConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let variablePaymentTermConfiguration =
        (Option.map ~f:VariablePaymentTermConfiguration.of_xml)
          (Xml.child xml_arg0 "variablePaymentTermConfiguration") in
      let renewalTermConfiguration =
        (Option.map ~f:RenewalTermConfiguration.of_xml)
          (Xml.child xml_arg0 "renewalTermConfiguration") in
      let configurableUpfrontPricingTermConfiguration =
        (Option.map ~f:ConfigurableUpfrontPricingTermConfiguration.of_xml)
          (Xml.child xml_arg0 "configurableUpfrontPricingTermConfiguration") in
      make ?variablePaymentTermConfiguration ?renewalTermConfiguration
        ?configurableUpfrontPricingTermConfiguration ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let variablePaymentTermConfiguration =
        field_map json__ "variablePaymentTermConfiguration"
          VariablePaymentTermConfiguration.of_json in
      let renewalTermConfiguration =
        field_map json__ "renewalTermConfiguration"
          RenewalTermConfiguration.of_json in
      let configurableUpfrontPricingTermConfiguration =
        field_map json__ "configurableUpfrontPricingTermConfiguration"
          ConfigurableUpfrontPricingTermConfiguration.of_json in
      make ?variablePaymentTermConfiguration ?renewalTermConfiguration
        ?configurableUpfrontPricingTermConfiguration ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A tagged union that represents the term configuration provided by the acceptor. Only one configuration is accepted per term."]
module BillingAdjustmentErrorCode =
  struct
    type nonrec t =
      | CONFLICT_EXCEPTION 
      | VALIDATION_EXCEPTION 
      | RESOURCE_NOT_FOUND_EXCEPTION 
      | INTERNAL_FAILURE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CONFLICT_EXCEPTION -> "CONFLICT_EXCEPTION"
      | VALIDATION_EXCEPTION -> "VALIDATION_EXCEPTION"
      | RESOURCE_NOT_FOUND_EXCEPTION -> "RESOURCE_NOT_FOUND_EXCEPTION"
      | INTERNAL_FAILURE -> "INTERNAL_FAILURE"
      | Non_static_id s -> s
    let of_string =
      function
      | "CONFLICT_EXCEPTION" -> CONFLICT_EXCEPTION
      | "VALIDATION_EXCEPTION" -> VALIDATION_EXCEPTION
      | "RESOURCE_NOT_FOUND_EXCEPTION" -> RESOURCE_NOT_FOUND_EXCEPTION
      | "INTERNAL_FAILURE" -> INTERNAL_FAILURE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration BillingAdjustmentErrorCode"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"BillingAdjustmentErrorCode" j)
    let to_json = simple_to_json to_value
  end
module ClientToken =
  struct
    type nonrec t = string
    let context_ = "ClientToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ClientToken" j
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module BillingAdjustmentDescription =
  struct
    type nonrec t = string
    let context_ = "BillingAdjustmentDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:500) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BillingAdjustmentDescription" j
    let to_json = simple_to_json to_value
  end
module BillingAdjustmentReasonCode =
  struct
    type nonrec t =
      | INCORRECT_TERMS_ACCEPTED 
      | INCORRECT_METERING 
      | TEST_ENVIRONMENT_CHARGES 
      | ALTERNATIVE_PROCUREMENT_CHANNEL 
      | UNINTENDED_RENEWAL 
      | BUYER_DISSATISFACTION 
      | OTHER 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | INCORRECT_TERMS_ACCEPTED -> "INCORRECT_TERMS_ACCEPTED"
      | INCORRECT_METERING -> "INCORRECT_METERING"
      | TEST_ENVIRONMENT_CHARGES -> "TEST_ENVIRONMENT_CHARGES"
      | ALTERNATIVE_PROCUREMENT_CHANNEL -> "ALTERNATIVE_PROCUREMENT_CHANNEL"
      | UNINTENDED_RENEWAL -> "UNINTENDED_RENEWAL"
      | BUYER_DISSATISFACTION -> "BUYER_DISSATISFACTION"
      | OTHER -> "OTHER"
      | Non_static_id s -> s
    let of_string =
      function
      | "INCORRECT_TERMS_ACCEPTED" -> INCORRECT_TERMS_ACCEPTED
      | "INCORRECT_METERING" -> INCORRECT_METERING
      | "TEST_ENVIRONMENT_CHARGES" -> TEST_ENVIRONMENT_CHARGES
      | "ALTERNATIVE_PROCUREMENT_CHANNEL" -> ALTERNATIVE_PROCUREMENT_CHANNEL
      | "UNINTENDED_RENEWAL" -> UNINTENDED_RENEWAL
      | "BUYER_DISSATISFACTION" -> BUYER_DISSATISFACTION
      | "OTHER" -> OTHER
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration BillingAdjustmentReasonCode"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"BillingAdjustmentReasonCode" j)
    let to_json = simple_to_json to_value
  end
module AccessDeniedExceptionReason =
  struct
    type nonrec t =
      | INVALID_ACCOUNT_STATE 
      | DENIED_BY_PRIVATE_MARKETPLACE_POLICY 
      | FAILED_KYC_COMPLIANCE 
      | MISSING_MFA 
      | INVALID_ACCESS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | INVALID_ACCOUNT_STATE -> "INVALID_ACCOUNT_STATE"
      | DENIED_BY_PRIVATE_MARKETPLACE_POLICY ->
          "DENIED_BY_PRIVATE_MARKETPLACE_POLICY"
      | FAILED_KYC_COMPLIANCE -> "FAILED_KYC_COMPLIANCE"
      | MISSING_MFA -> "MISSING_MFA"
      | INVALID_ACCESS -> "INVALID_ACCESS"
      | Non_static_id s -> s
    let of_string =
      function
      | "INVALID_ACCOUNT_STATE" -> INVALID_ACCOUNT_STATE
      | "DENIED_BY_PRIVATE_MARKETPLACE_POLICY" ->
          DENIED_BY_PRIVATE_MARKETPLACE_POLICY
      | "FAILED_KYC_COMPLIANCE" -> FAILED_KYC_COMPLIANCE
      | "MISSING_MFA" -> MISSING_MFA
      | "INVALID_ACCESS" -> INVALID_ACCESS
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration AccessDeniedExceptionReason"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"AccessDeniedExceptionReason" j)
    let to_json = simple_to_json to_value
  end
module ExceptionMessage =
  struct
    type nonrec t = string
    let context_ = "ExceptionMessage"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ExceptionMessage" j
    let to_json = simple_to_json to_value
  end
module RequestId =
  struct
    type nonrec t = string
    let context_ = "RequestId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () -> check_pattern i ~pattern:"[A-Za-z0-9-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RequestId" j
    let to_json = simple_to_json to_value
  end
module ResourceType =
  struct
    type nonrec t =
      | Agreement 
      | AgreementRequest 
      | AgreementProposal 
      | Charge 
      | PaymentRequest 
      | Invoice 
      | AgreementCancellationRequest 
      | BillingAdjustmentRequest 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Agreement -> "Agreement"
      | AgreementRequest -> "AgreementRequest"
      | AgreementProposal -> "AgreementProposal"
      | Charge -> "Charge"
      | PaymentRequest -> "PaymentRequest"
      | Invoice -> "Invoice"
      | AgreementCancellationRequest -> "AgreementCancellationRequest"
      | BillingAdjustmentRequest -> "BillingAdjustmentRequest"
      | Non_static_id s -> s
    let of_string =
      function
      | "Agreement" -> Agreement
      | "AgreementRequest" -> AgreementRequest
      | "AgreementProposal" -> AgreementProposal
      | "Charge" -> Charge
      | "PaymentRequest" -> PaymentRequest
      | "Invoice" -> Invoice
      | "AgreementCancellationRequest" -> AgreementCancellationRequest
      | "BillingAdjustmentRequest" -> BillingAdjustmentRequest
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ResourceType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ResourceType" j)
    let to_json = simple_to_json to_value
  end
module ValidationExceptionFieldList =
  struct
    type nonrec t = ValidationExceptionField.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ValidationExceptionField.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ValidationExceptionField.of_xml)
    let of_json j =
      list_of_json ~kind:"ValidationExceptionFieldList"
        ~of_json:ValidationExceptionField.of_json j
    let to_json v = composed_to_json to_value v
  end
module ValidationExceptionReason =
  struct
    type nonrec t =
      | MISSING_BILLING_ADJUSTMENTS 
      | BILLING_ADJUSTMENTS_LIMIT_EXCEEDED 
      | MISSING_INVOICE_ID 
      | INVALID_ADJUSTMENT_AMOUNT 
      | MISSING_ADJUSTMENT_AMOUNT 
      | INVALID_REASON_CODE 
      | MISSING_REASON_CODE 
      | MISSING_DESCRIPTION 
      | INVALID_INVOICE_ADJUSTMENT_PERIOD 
      | INVALID_CURRENCY_CODE 
      | MISSING_CURRENCY_CODE 
      | EXCEEDED_MAXIMUM_ADJUSTMENT_AMOUNT 
      | MISSING_BILLING_ADJUSTMENT_REQUEST_ENTRY 
      | MULTIPLE_AGREEMENT_IDS 
      | INVALID_AGREEMENT_CANCELLATION_REQUEST_ID 
      | MISSING_AGREEMENT_CANCELLATION_REQUEST_ID 
      | MISSING_REASON 
      | INVALID_REASON 
      | INVALID_STATUS 
      | INVALID_AGREEMENT_ID 
      | MISSING_AGREEMENT_ID 
      | INVALID_CATALOG 
      | INVALID_FILTERS 
      | INVALID_FILTER_NAME 
      | MISSING_FILTER_NAME 
      | INVALID_FILTER_VALUES 
      | MISSING_FILTER_VALUES 
      | INVALID_SORT_BY 
      | INVALID_SORT_ORDER 
      | INVALID_NEXT_TOKEN 
      | INVALID_MAX_RESULTS 
      | INVALID_TERM_ID 
      | MISSING_TERM_ID 
      | MISSING_NAME 
      | INVALID_NAME 
      | INVALID_DESCRIPTION 
      | MISSING_CHARGE_AMOUNT 
      | INVALID_CHARGE_AMOUNT 
      | MISSING_PAYMENT_REQUEST_ID 
      | INVALID_PAYMENT_REQUEST_ID 
      | MISSING_PARTY_TYPE 
      | INVALID_PARTY_TYPE 
      | UNSUPPORTED_FILTERS 
      | INVALID_CLIENT_TOKEN 
      | INVALID_INTENT 
      | MISSING_INTENT 
      | INVALID_SOURCE_AGREEMENT_IDENTIFIER 
      | MISSING_SOURCE_AGREEMENT_IDENTIFIER 
      | INVALID_AGREEMENT_PROPOSAL_IDENTIFIER 
      | MISSING_AGREEMENT_PROPOSAL_IDENTIFIER 
      | INVALID_REQUESTED_TERMS 
      | MISSING_REQUESTED_TERMS 
      | INVALID_REQUESTED_TERM_ID 
      | MISSING_REQUESTED_TERM_ID 
      | INVALID_REQUESTED_TERM_CONFIGURATION 
      | MISSING_REQUESTED_TERM_CONFIGURATION 
      | INVALID_AGREEMENT_REQUEST_ID 
      | MISSING_AGREEMENT_REQUEST_ID 
      | INVALID_PURCHASE_ORDERS 
      | MISSING_PURCHASE_ORDERS 
      | INVALID_CHARGE_ID 
      | MISSING_CHARGE_ID 
      | INVALID_CHARGE_REVISION 
      | MISSING_CHARGE_REVISION 
      | INVALID_AGREEMENT_TYPE 
      | INVALID_PURCHASE_ORDER_REFERENCE 
      | INACTIVE_AGREEMENT 
      | SUPERSEDED_AGREEMENT_PROPOSAL 
      | EXPIRED_AGREEMENT_PROPOSAL 
      | MISSING_MANDATORY_TERMS 
      | INCOMPATIBLE_TERMS 
      | MISSING_USAGE_AGREEMENT 
      | INVALID_INCREMENTAL_CHARGE 
      | MISSING_ACCOUNT_ADDRESS 
      | UNSUPPORTED_ACTION 
      | INVALID_REJECTION_REASON 
      | INVALID_PAYMENT_REQUEST_STATUS 
      | OTHER 
      | DUPLICATE_CHARGES 
      | UNSUPPORTED_ACCOUNT_PLAN 
      | DUPLICATE_AGREEMENT_IN_ORGANIZATION 
      | MISSING_PURCHASE_ORDER_REFERENCE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | MISSING_BILLING_ADJUSTMENTS -> "MISSING_BILLING_ADJUSTMENTS"
      | BILLING_ADJUSTMENTS_LIMIT_EXCEEDED ->
          "BILLING_ADJUSTMENTS_LIMIT_EXCEEDED"
      | MISSING_INVOICE_ID -> "MISSING_INVOICE_ID"
      | INVALID_ADJUSTMENT_AMOUNT -> "INVALID_ADJUSTMENT_AMOUNT"
      | MISSING_ADJUSTMENT_AMOUNT -> "MISSING_ADJUSTMENT_AMOUNT"
      | INVALID_REASON_CODE -> "INVALID_REASON_CODE"
      | MISSING_REASON_CODE -> "MISSING_REASON_CODE"
      | MISSING_DESCRIPTION -> "MISSING_DESCRIPTION"
      | INVALID_INVOICE_ADJUSTMENT_PERIOD ->
          "INVALID_INVOICE_ADJUSTMENT_PERIOD"
      | INVALID_CURRENCY_CODE -> "INVALID_CURRENCY_CODE"
      | MISSING_CURRENCY_CODE -> "MISSING_CURRENCY_CODE"
      | EXCEEDED_MAXIMUM_ADJUSTMENT_AMOUNT ->
          "EXCEEDED_MAXIMUM_ADJUSTMENT_AMOUNT"
      | MISSING_BILLING_ADJUSTMENT_REQUEST_ENTRY ->
          "MISSING_BILLING_ADJUSTMENT_REQUEST_ENTRY"
      | MULTIPLE_AGREEMENT_IDS -> "MULTIPLE_AGREEMENT_IDS"
      | INVALID_AGREEMENT_CANCELLATION_REQUEST_ID ->
          "INVALID_AGREEMENT_CANCELLATION_REQUEST_ID"
      | MISSING_AGREEMENT_CANCELLATION_REQUEST_ID ->
          "MISSING_AGREEMENT_CANCELLATION_REQUEST_ID"
      | MISSING_REASON -> "MISSING_REASON"
      | INVALID_REASON -> "INVALID_REASON"
      | INVALID_STATUS -> "INVALID_STATUS"
      | INVALID_AGREEMENT_ID -> "INVALID_AGREEMENT_ID"
      | MISSING_AGREEMENT_ID -> "MISSING_AGREEMENT_ID"
      | INVALID_CATALOG -> "INVALID_CATALOG"
      | INVALID_FILTERS -> "INVALID_FILTERS"
      | INVALID_FILTER_NAME -> "INVALID_FILTER_NAME"
      | MISSING_FILTER_NAME -> "MISSING_FILTER_NAME"
      | INVALID_FILTER_VALUES -> "INVALID_FILTER_VALUES"
      | MISSING_FILTER_VALUES -> "MISSING_FILTER_VALUES"
      | INVALID_SORT_BY -> "INVALID_SORT_BY"
      | INVALID_SORT_ORDER -> "INVALID_SORT_ORDER"
      | INVALID_NEXT_TOKEN -> "INVALID_NEXT_TOKEN"
      | INVALID_MAX_RESULTS -> "INVALID_MAX_RESULTS"
      | INVALID_TERM_ID -> "INVALID_TERM_ID"
      | MISSING_TERM_ID -> "MISSING_TERM_ID"
      | MISSING_NAME -> "MISSING_NAME"
      | INVALID_NAME -> "INVALID_NAME"
      | INVALID_DESCRIPTION -> "INVALID_DESCRIPTION"
      | MISSING_CHARGE_AMOUNT -> "MISSING_CHARGE_AMOUNT"
      | INVALID_CHARGE_AMOUNT -> "INVALID_CHARGE_AMOUNT"
      | MISSING_PAYMENT_REQUEST_ID -> "MISSING_PAYMENT_REQUEST_ID"
      | INVALID_PAYMENT_REQUEST_ID -> "INVALID_PAYMENT_REQUEST_ID"
      | MISSING_PARTY_TYPE -> "MISSING_PARTY_TYPE"
      | INVALID_PARTY_TYPE -> "INVALID_PARTY_TYPE"
      | UNSUPPORTED_FILTERS -> "UNSUPPORTED_FILTERS"
      | INVALID_CLIENT_TOKEN -> "INVALID_CLIENT_TOKEN"
      | INVALID_INTENT -> "INVALID_INTENT"
      | MISSING_INTENT -> "MISSING_INTENT"
      | INVALID_SOURCE_AGREEMENT_IDENTIFIER ->
          "INVALID_SOURCE_AGREEMENT_IDENTIFIER"
      | MISSING_SOURCE_AGREEMENT_IDENTIFIER ->
          "MISSING_SOURCE_AGREEMENT_IDENTIFIER"
      | INVALID_AGREEMENT_PROPOSAL_IDENTIFIER ->
          "INVALID_AGREEMENT_PROPOSAL_IDENTIFIER"
      | MISSING_AGREEMENT_PROPOSAL_IDENTIFIER ->
          "MISSING_AGREEMENT_PROPOSAL_IDENTIFIER"
      | INVALID_REQUESTED_TERMS -> "INVALID_REQUESTED_TERMS"
      | MISSING_REQUESTED_TERMS -> "MISSING_REQUESTED_TERMS"
      | INVALID_REQUESTED_TERM_ID -> "INVALID_REQUESTED_TERM_ID"
      | MISSING_REQUESTED_TERM_ID -> "MISSING_REQUESTED_TERM_ID"
      | INVALID_REQUESTED_TERM_CONFIGURATION ->
          "INVALID_REQUESTED_TERM_CONFIGURATION"
      | MISSING_REQUESTED_TERM_CONFIGURATION ->
          "MISSING_REQUESTED_TERM_CONFIGURATION"
      | INVALID_AGREEMENT_REQUEST_ID -> "INVALID_AGREEMENT_REQUEST_ID"
      | MISSING_AGREEMENT_REQUEST_ID -> "MISSING_AGREEMENT_REQUEST_ID"
      | INVALID_PURCHASE_ORDERS -> "INVALID_PURCHASE_ORDERS"
      | MISSING_PURCHASE_ORDERS -> "MISSING_PURCHASE_ORDERS"
      | INVALID_CHARGE_ID -> "INVALID_CHARGE_ID"
      | MISSING_CHARGE_ID -> "MISSING_CHARGE_ID"
      | INVALID_CHARGE_REVISION -> "INVALID_CHARGE_REVISION"
      | MISSING_CHARGE_REVISION -> "MISSING_CHARGE_REVISION"
      | INVALID_AGREEMENT_TYPE -> "INVALID_AGREEMENT_TYPE"
      | INVALID_PURCHASE_ORDER_REFERENCE ->
          "INVALID_PURCHASE_ORDER_REFERENCE"
      | INACTIVE_AGREEMENT -> "INACTIVE_AGREEMENT"
      | SUPERSEDED_AGREEMENT_PROPOSAL -> "SUPERSEDED_AGREEMENT_PROPOSAL"
      | EXPIRED_AGREEMENT_PROPOSAL -> "EXPIRED_AGREEMENT_PROPOSAL"
      | MISSING_MANDATORY_TERMS -> "MISSING_MANDATORY_TERMS"
      | INCOMPATIBLE_TERMS -> "INCOMPATIBLE_TERMS"
      | MISSING_USAGE_AGREEMENT -> "MISSING_USAGE_AGREEMENT"
      | INVALID_INCREMENTAL_CHARGE -> "INVALID_INCREMENTAL_CHARGE"
      | MISSING_ACCOUNT_ADDRESS -> "MISSING_ACCOUNT_ADDRESS"
      | UNSUPPORTED_ACTION -> "UNSUPPORTED_ACTION"
      | INVALID_REJECTION_REASON -> "INVALID_REJECTION_REASON"
      | INVALID_PAYMENT_REQUEST_STATUS -> "INVALID_PAYMENT_REQUEST_STATUS"
      | OTHER -> "OTHER"
      | DUPLICATE_CHARGES -> "DUPLICATE_CHARGES"
      | UNSUPPORTED_ACCOUNT_PLAN -> "UNSUPPORTED_ACCOUNT_PLAN"
      | DUPLICATE_AGREEMENT_IN_ORGANIZATION ->
          "DUPLICATE_AGREEMENT_IN_ORGANIZATION"
      | MISSING_PURCHASE_ORDER_REFERENCE ->
          "MISSING_PURCHASE_ORDER_REFERENCE"
      | Non_static_id s -> s
    let of_string =
      function
      | "MISSING_BILLING_ADJUSTMENTS" -> MISSING_BILLING_ADJUSTMENTS
      | "BILLING_ADJUSTMENTS_LIMIT_EXCEEDED" ->
          BILLING_ADJUSTMENTS_LIMIT_EXCEEDED
      | "MISSING_INVOICE_ID" -> MISSING_INVOICE_ID
      | "INVALID_ADJUSTMENT_AMOUNT" -> INVALID_ADJUSTMENT_AMOUNT
      | "MISSING_ADJUSTMENT_AMOUNT" -> MISSING_ADJUSTMENT_AMOUNT
      | "INVALID_REASON_CODE" -> INVALID_REASON_CODE
      | "MISSING_REASON_CODE" -> MISSING_REASON_CODE
      | "MISSING_DESCRIPTION" -> MISSING_DESCRIPTION
      | "INVALID_INVOICE_ADJUSTMENT_PERIOD" ->
          INVALID_INVOICE_ADJUSTMENT_PERIOD
      | "INVALID_CURRENCY_CODE" -> INVALID_CURRENCY_CODE
      | "MISSING_CURRENCY_CODE" -> MISSING_CURRENCY_CODE
      | "EXCEEDED_MAXIMUM_ADJUSTMENT_AMOUNT" ->
          EXCEEDED_MAXIMUM_ADJUSTMENT_AMOUNT
      | "MISSING_BILLING_ADJUSTMENT_REQUEST_ENTRY" ->
          MISSING_BILLING_ADJUSTMENT_REQUEST_ENTRY
      | "MULTIPLE_AGREEMENT_IDS" -> MULTIPLE_AGREEMENT_IDS
      | "INVALID_AGREEMENT_CANCELLATION_REQUEST_ID" ->
          INVALID_AGREEMENT_CANCELLATION_REQUEST_ID
      | "MISSING_AGREEMENT_CANCELLATION_REQUEST_ID" ->
          MISSING_AGREEMENT_CANCELLATION_REQUEST_ID
      | "MISSING_REASON" -> MISSING_REASON
      | "INVALID_REASON" -> INVALID_REASON
      | "INVALID_STATUS" -> INVALID_STATUS
      | "INVALID_AGREEMENT_ID" -> INVALID_AGREEMENT_ID
      | "MISSING_AGREEMENT_ID" -> MISSING_AGREEMENT_ID
      | "INVALID_CATALOG" -> INVALID_CATALOG
      | "INVALID_FILTERS" -> INVALID_FILTERS
      | "INVALID_FILTER_NAME" -> INVALID_FILTER_NAME
      | "MISSING_FILTER_NAME" -> MISSING_FILTER_NAME
      | "INVALID_FILTER_VALUES" -> INVALID_FILTER_VALUES
      | "MISSING_FILTER_VALUES" -> MISSING_FILTER_VALUES
      | "INVALID_SORT_BY" -> INVALID_SORT_BY
      | "INVALID_SORT_ORDER" -> INVALID_SORT_ORDER
      | "INVALID_NEXT_TOKEN" -> INVALID_NEXT_TOKEN
      | "INVALID_MAX_RESULTS" -> INVALID_MAX_RESULTS
      | "INVALID_TERM_ID" -> INVALID_TERM_ID
      | "MISSING_TERM_ID" -> MISSING_TERM_ID
      | "MISSING_NAME" -> MISSING_NAME
      | "INVALID_NAME" -> INVALID_NAME
      | "INVALID_DESCRIPTION" -> INVALID_DESCRIPTION
      | "MISSING_CHARGE_AMOUNT" -> MISSING_CHARGE_AMOUNT
      | "INVALID_CHARGE_AMOUNT" -> INVALID_CHARGE_AMOUNT
      | "MISSING_PAYMENT_REQUEST_ID" -> MISSING_PAYMENT_REQUEST_ID
      | "INVALID_PAYMENT_REQUEST_ID" -> INVALID_PAYMENT_REQUEST_ID
      | "MISSING_PARTY_TYPE" -> MISSING_PARTY_TYPE
      | "INVALID_PARTY_TYPE" -> INVALID_PARTY_TYPE
      | "UNSUPPORTED_FILTERS" -> UNSUPPORTED_FILTERS
      | "INVALID_CLIENT_TOKEN" -> INVALID_CLIENT_TOKEN
      | "INVALID_INTENT" -> INVALID_INTENT
      | "MISSING_INTENT" -> MISSING_INTENT
      | "INVALID_SOURCE_AGREEMENT_IDENTIFIER" ->
          INVALID_SOURCE_AGREEMENT_IDENTIFIER
      | "MISSING_SOURCE_AGREEMENT_IDENTIFIER" ->
          MISSING_SOURCE_AGREEMENT_IDENTIFIER
      | "INVALID_AGREEMENT_PROPOSAL_IDENTIFIER" ->
          INVALID_AGREEMENT_PROPOSAL_IDENTIFIER
      | "MISSING_AGREEMENT_PROPOSAL_IDENTIFIER" ->
          MISSING_AGREEMENT_PROPOSAL_IDENTIFIER
      | "INVALID_REQUESTED_TERMS" -> INVALID_REQUESTED_TERMS
      | "MISSING_REQUESTED_TERMS" -> MISSING_REQUESTED_TERMS
      | "INVALID_REQUESTED_TERM_ID" -> INVALID_REQUESTED_TERM_ID
      | "MISSING_REQUESTED_TERM_ID" -> MISSING_REQUESTED_TERM_ID
      | "INVALID_REQUESTED_TERM_CONFIGURATION" ->
          INVALID_REQUESTED_TERM_CONFIGURATION
      | "MISSING_REQUESTED_TERM_CONFIGURATION" ->
          MISSING_REQUESTED_TERM_CONFIGURATION
      | "INVALID_AGREEMENT_REQUEST_ID" -> INVALID_AGREEMENT_REQUEST_ID
      | "MISSING_AGREEMENT_REQUEST_ID" -> MISSING_AGREEMENT_REQUEST_ID
      | "INVALID_PURCHASE_ORDERS" -> INVALID_PURCHASE_ORDERS
      | "MISSING_PURCHASE_ORDERS" -> MISSING_PURCHASE_ORDERS
      | "INVALID_CHARGE_ID" -> INVALID_CHARGE_ID
      | "MISSING_CHARGE_ID" -> MISSING_CHARGE_ID
      | "INVALID_CHARGE_REVISION" -> INVALID_CHARGE_REVISION
      | "MISSING_CHARGE_REVISION" -> MISSING_CHARGE_REVISION
      | "INVALID_AGREEMENT_TYPE" -> INVALID_AGREEMENT_TYPE
      | "INVALID_PURCHASE_ORDER_REFERENCE" ->
          INVALID_PURCHASE_ORDER_REFERENCE
      | "INACTIVE_AGREEMENT" -> INACTIVE_AGREEMENT
      | "SUPERSEDED_AGREEMENT_PROPOSAL" -> SUPERSEDED_AGREEMENT_PROPOSAL
      | "EXPIRED_AGREEMENT_PROPOSAL" -> EXPIRED_AGREEMENT_PROPOSAL
      | "MISSING_MANDATORY_TERMS" -> MISSING_MANDATORY_TERMS
      | "INCOMPATIBLE_TERMS" -> INCOMPATIBLE_TERMS
      | "MISSING_USAGE_AGREEMENT" -> MISSING_USAGE_AGREEMENT
      | "INVALID_INCREMENTAL_CHARGE" -> INVALID_INCREMENTAL_CHARGE
      | "MISSING_ACCOUNT_ADDRESS" -> MISSING_ACCOUNT_ADDRESS
      | "UNSUPPORTED_ACTION" -> UNSUPPORTED_ACTION
      | "INVALID_REJECTION_REASON" -> INVALID_REJECTION_REASON
      | "INVALID_PAYMENT_REQUEST_STATUS" -> INVALID_PAYMENT_REQUEST_STATUS
      | "OTHER" -> OTHER
      | "DUPLICATE_CHARGES" -> DUPLICATE_CHARGES
      | "UNSUPPORTED_ACCOUNT_PLAN" -> UNSUPPORTED_ACCOUNT_PLAN
      | "DUPLICATE_AGREEMENT_IN_ORGANIZATION" ->
          DUPLICATE_AGREEMENT_IN_ORGANIZATION
      | "MISSING_PURCHASE_ORDER_REFERENCE" ->
          MISSING_PURCHASE_ORDER_REFERENCE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ValidationExceptionReason" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ValidationExceptionReason" j)
    let to_json = simple_to_json to_value
  end
module PurchaseOrder =
  struct
    type nonrec t =
      {
      chargeId: ResourceId.t
        [@ocaml.doc
          "The unique identifier of the charge to associate the purchase order with."];
      chargeRevision: ChargeRevision.t option
        [@ocaml.doc "The revision of the charge."];
      agreementId: ResourceId.t option
        [@ocaml.doc
          "The unique identifier of the agreement associated with this charge."];
      purchaseOrderReference: PurchaseOrderReference.t option
        [@ocaml.doc
          "The purchase order reference to associate with the charge."]}
    let context_ = "PurchaseOrder"
    let make ?chargeRevision =
      fun ?agreementId ->
        fun ?purchaseOrderReference ->
          fun ~chargeId ->
            fun () ->
              { chargeRevision; agreementId; purchaseOrderReference; chargeId
              }
    let to_value x =
      structure_to_value
        [("chargeId", (Some (ResourceId.to_value x.chargeId)));
        ("chargeRevision",
          (Option.map x.chargeRevision ~f:ChargeRevision.to_value));
        ("agreementId", (Option.map x.agreementId ~f:ResourceId.to_value));
        ("purchaseOrderReference",
          (Option.map x.purchaseOrderReference
             ~f:PurchaseOrderReference.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let purchaseOrderReference =
        (Option.map ~f:PurchaseOrderReference.of_xml)
          (Xml.child xml_arg0 "purchaseOrderReference") in
      let agreementId =
        (Option.map ~f:ResourceId.of_xml) (Xml.child xml_arg0 "agreementId") in
      let chargeRevision =
        (Option.map ~f:ChargeRevision.of_xml)
          (Xml.child xml_arg0 "chargeRevision") in
      let chargeId =
        ResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "chargeId") in
      make ?purchaseOrderReference ?agreementId ?chargeRevision ~chargeId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let purchaseOrderReference =
        field_map json__ "purchaseOrderReference"
          PurchaseOrderReference.of_json in
      let agreementId = field_map json__ "agreementId" ResourceId.of_json in
      let chargeRevision =
        field_map json__ "chargeRevision" ChargeRevision.of_json in
      let chargeId = field_map_exn json__ "chargeId" ResourceId.of_json in
      make ?purchaseOrderReference ?agreementId ?chargeRevision ~chargeId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contains information about a purchase order association to a charge within an agreement."]
module AgreementViewSummary =
  struct
    type nonrec t =
      {
      agreementId: ResourceId.t option
        [@ocaml.doc "The unique identifier of the agreement."];
      acceptanceTime: Timestamp.t option
        [@ocaml.doc "The date and time that the agreement was accepted."];
      startTime: Timestamp.t option
        [@ocaml.doc "The date and time when the agreement starts."];
      endTime: Timestamp.t option
        [@ocaml.doc
          "The date and time when the agreement ends. The field is null for pay-as-you-go agreements, which don\226\128\153t have end dates."];
      agreementType: AgreementType.t option
        [@ocaml.doc "The type of agreement."];
      acceptor: Acceptor.t option
        [@ocaml.doc
          "Details of the party accepting the agreement terms. This is commonly the buyer for PurchaseAgreement."];
      proposer: Proposer.t option
        [@ocaml.doc
          "Details of the party proposing the agreement terms, most commonly the seller for PurchaseAgreement."];
      proposalSummary: ProposalSummary.t option
        [@ocaml.doc "A summary of the proposal"];
      status: AgreementStatus.t option
        [@ocaml.doc "The current status of the agreement."]}
    let make ?agreementId =
      fun ?acceptanceTime ->
        fun ?startTime ->
          fun ?endTime ->
            fun ?agreementType ->
              fun ?acceptor ->
                fun ?proposer ->
                  fun ?proposalSummary ->
                    fun ?status ->
                      fun () ->
                        {
                          agreementId;
                          acceptanceTime;
                          startTime;
                          endTime;
                          agreementType;
                          acceptor;
                          proposer;
                          proposalSummary;
                          status
                        }
    let to_value x =
      structure_to_value
        [("agreementId", (Option.map x.agreementId ~f:ResourceId.to_value));
        ("acceptanceTime",
          (Option.map x.acceptanceTime ~f:Timestamp.to_value));
        ("startTime", (Option.map x.startTime ~f:Timestamp.to_value));
        ("endTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("agreementType",
          (Option.map x.agreementType ~f:AgreementType.to_value));
        ("acceptor", (Option.map x.acceptor ~f:Acceptor.to_value));
        ("proposer", (Option.map x.proposer ~f:Proposer.to_value));
        ("proposalSummary",
          (Option.map x.proposalSummary ~f:ProposalSummary.to_value));
        ("status", (Option.map x.status ~f:AgreementStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:AgreementStatus.of_xml) (Xml.child xml_arg0 "status") in
      let proposalSummary =
        (Option.map ~f:ProposalSummary.of_xml)
          (Xml.child xml_arg0 "proposalSummary") in
      let proposer =
        (Option.map ~f:Proposer.of_xml) (Xml.child xml_arg0 "proposer") in
      let acceptor =
        (Option.map ~f:Acceptor.of_xml) (Xml.child xml_arg0 "acceptor") in
      let agreementType =
        (Option.map ~f:AgreementType.of_xml)
          (Xml.child xml_arg0 "agreementType") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "endTime") in
      let startTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "startTime") in
      let acceptanceTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "acceptanceTime") in
      let agreementId =
        (Option.map ~f:ResourceId.of_xml) (Xml.child xml_arg0 "agreementId") in
      make ?status ?proposalSummary ?proposer ?acceptor ?agreementType
        ?endTime ?startTime ?acceptanceTime ?agreementId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "status" AgreementStatus.of_json in
      let proposalSummary =
        field_map json__ "proposalSummary" ProposalSummary.of_json in
      let proposer = field_map json__ "proposer" Proposer.of_json in
      let acceptor = field_map json__ "acceptor" Acceptor.of_json in
      let agreementType =
        field_map json__ "agreementType" AgreementType.of_json in
      let endTime = field_map json__ "endTime" Timestamp.of_json in
      let startTime = field_map json__ "startTime" Timestamp.of_json in
      let acceptanceTime =
        field_map json__ "acceptanceTime" Timestamp.of_json in
      let agreementId = field_map json__ "agreementId" ResourceId.of_json in
      make ?status ?proposalSummary ?proposer ?acceptor ?agreementType
        ?endTime ?startTime ?acceptanceTime ?agreementId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A summary of the agreement, including top-level attributes (for example, the agreement ID, proposer, and acceptor)."]
module Filter =
  struct
    type nonrec t =
      {
      name: FilterName.t option [@ocaml.doc "The name of the filter."];
      values: FilterValueList.t option [@ocaml.doc "The filter value."]}
    let make ?name = fun ?values -> fun () -> { name; values }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:FilterName.to_value));
        ("values", (Option.map x.values ~f:FilterValueList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let values =
        (Option.map ~f:FilterValueList.of_xml) (Xml.child xml_arg0 "values") in
      let name =
        (Option.map ~f:FilterName.of_xml) (Xml.child xml_arg0 "name") in
      make ?values ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let values = field_map json__ "values" FilterValueList.of_json in
      let name = field_map json__ "name" FilterName.of_json in
      make ?values ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The filter name and value pair that is used to return a more specific list of results. Filters can be used to match a set of resources by various criteria, such as offerId or productId."]
module SortBy =
  struct
    type nonrec t = string
    let context_ = "SortBy"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:255) >>=
                  (fun () -> check_pattern i ~pattern:"[A-Za-z_]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SortBy" j
    let to_json = simple_to_json to_value
  end
module SortOrder =
  struct
    type nonrec t =
      | ASCENDING 
      | DESCENDING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ASCENDING -> "ASCENDING"
      | DESCENDING -> "DESCENDING"
      | Non_static_id s -> s
    let of_string =
      function
      | "ASCENDING" -> ASCENDING
      | "DESCENDING" -> DESCENDING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration SortOrder" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SortOrder" j)
    let to_json = simple_to_json to_value
  end
module BillingAdjustmentSummary =
  struct
    type nonrec t =
      {
      billingAdjustmentRequestId: BillingAdjustmentRequestId.t option
        [@ocaml.doc
          "The unique identifier of the billing adjustment request."];
      originalInvoiceId: InvoiceId.t option
        [@ocaml.doc "The identifier of the original invoice being adjusted."];
      adjustmentAmount: PositiveAmountUpto8Decimals.t option
        [@ocaml.doc
          "The adjustment amount as a string representation of a decimal number."];
      currencyCode: CurrencyCode.t option
        [@ocaml.doc "The currency code for the adjustment amount."];
      status: BillingAdjustmentStatus.t option
        [@ocaml.doc "The current status of the billing adjustment request."];
      agreementId: AgreementId.t option
        [@ocaml.doc
          "The unique identifier of the agreement associated with this billing adjustment request."];
      createdAt: Timestamp.t option
        [@ocaml.doc
          "The date and time when the billing adjustment request was created."];
      updatedAt: Timestamp.t option
        [@ocaml.doc
          "The date and time when the billing adjustment request was last updated."];
      agreementType: AgreementType.t option
        [@ocaml.doc "The type of agreement."];
      catalog: Catalog.t option
        [@ocaml.doc "The catalog in which the agreement was created."]}
    let make ?billingAdjustmentRequestId =
      fun ?originalInvoiceId ->
        fun ?adjustmentAmount ->
          fun ?currencyCode ->
            fun ?status ->
              fun ?agreementId ->
                fun ?createdAt ->
                  fun ?updatedAt ->
                    fun ?agreementType ->
                      fun ?catalog ->
                        fun () ->
                          {
                            billingAdjustmentRequestId;
                            originalInvoiceId;
                            adjustmentAmount;
                            currencyCode;
                            status;
                            agreementId;
                            createdAt;
                            updatedAt;
                            agreementType;
                            catalog
                          }
    let to_value x =
      structure_to_value
        [("billingAdjustmentRequestId",
           (Option.map x.billingAdjustmentRequestId
              ~f:BillingAdjustmentRequestId.to_value));
        ("originalInvoiceId",
          (Option.map x.originalInvoiceId ~f:InvoiceId.to_value));
        ("adjustmentAmount",
          (Option.map x.adjustmentAmount
             ~f:PositiveAmountUpto8Decimals.to_value));
        ("currencyCode",
          (Option.map x.currencyCode ~f:CurrencyCode.to_value));
        ("status", (Option.map x.status ~f:BillingAdjustmentStatus.to_value));
        ("agreementId", (Option.map x.agreementId ~f:AgreementId.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("updatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value));
        ("agreementType",
          (Option.map x.agreementType ~f:AgreementType.to_value));
        ("catalog", (Option.map x.catalog ~f:Catalog.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let catalog =
        (Option.map ~f:Catalog.of_xml) (Xml.child xml_arg0 "catalog") in
      let agreementType =
        (Option.map ~f:AgreementType.of_xml)
          (Xml.child xml_arg0 "agreementType") in
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "updatedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let agreementId =
        (Option.map ~f:AgreementId.of_xml) (Xml.child xml_arg0 "agreementId") in
      let status =
        (Option.map ~f:BillingAdjustmentStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let currencyCode =
        (Option.map ~f:CurrencyCode.of_xml)
          (Xml.child xml_arg0 "currencyCode") in
      let adjustmentAmount =
        (Option.map ~f:PositiveAmountUpto8Decimals.of_xml)
          (Xml.child xml_arg0 "adjustmentAmount") in
      let originalInvoiceId =
        (Option.map ~f:InvoiceId.of_xml)
          (Xml.child xml_arg0 "originalInvoiceId") in
      let billingAdjustmentRequestId =
        (Option.map ~f:BillingAdjustmentRequestId.of_xml)
          (Xml.child xml_arg0 "billingAdjustmentRequestId") in
      make ?catalog ?agreementType ?updatedAt ?createdAt ?agreementId ?status
        ?currencyCode ?adjustmentAmount ?originalInvoiceId
        ?billingAdjustmentRequestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let catalog = field_map json__ "catalog" Catalog.of_json in
      let agreementType =
        field_map json__ "agreementType" AgreementType.of_json in
      let updatedAt = field_map json__ "updatedAt" Timestamp.of_json in
      let createdAt = field_map json__ "createdAt" Timestamp.of_json in
      let agreementId = field_map json__ "agreementId" AgreementId.of_json in
      let status = field_map json__ "status" BillingAdjustmentStatus.of_json in
      let currencyCode = field_map json__ "currencyCode" CurrencyCode.of_json in
      let adjustmentAmount =
        field_map json__ "adjustmentAmount"
          PositiveAmountUpto8Decimals.of_json in
      let originalInvoiceId =
        field_map json__ "originalInvoiceId" InvoiceId.of_json in
      let billingAdjustmentRequestId =
        field_map json__ "billingAdjustmentRequestId"
          BillingAdjustmentRequestId.of_json in
      make ?catalog ?agreementType ?updatedAt ?createdAt ?agreementId ?status
        ?currencyCode ?adjustmentAmount ?originalInvoiceId
        ?billingAdjustmentRequestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Summary view of a billing adjustment request."]
module PaymentRequestSummary =
  struct
    type nonrec t =
      {
      paymentRequestId: PaymentRequestId.t option
        [@ocaml.doc "The unique identifier of the payment request."];
      agreementId: AgreementId.t option
        [@ocaml.doc
          "The unique identifier of the agreement associated with this payment request."];
      status: PaymentRequestStatus.t option
        [@ocaml.doc
          "The current status of the payment request. Possible values include VALIDATING, VALIDATION_FAILED, PENDING_APPROVAL, APPROVED, REJECTED, and CANCELLED."];
      name: PaymentRequestName.t option
        [@ocaml.doc "The descriptive name of the payment request."];
      chargeId: ChargeId.t option
        [@ocaml.doc
          "The unique identifier of the charge created after the payment request is approved. This field is only present for approved payment requests."];
      chargeAmount: PositiveAmountUpto8Decimals.t option
        [@ocaml.doc "The amount charged or to be charged to the buyer."];
      currencyCode: CurrencyCode.t option
        [@ocaml.doc "The currency code for the charge amount."];
      createdAt: Timestamp.t option
        [@ocaml.doc
          "The date and time when the payment request was created."];
      updatedAt: Timestamp.t option
        [@ocaml.doc
          "The date and time when the payment request was last updated."]}
    let make ?paymentRequestId =
      fun ?agreementId ->
        fun ?status ->
          fun ?name ->
            fun ?chargeId ->
              fun ?chargeAmount ->
                fun ?currencyCode ->
                  fun ?createdAt ->
                    fun ?updatedAt ->
                      fun () ->
                        {
                          paymentRequestId;
                          agreementId;
                          status;
                          name;
                          chargeId;
                          chargeAmount;
                          currencyCode;
                          createdAt;
                          updatedAt
                        }
    let to_value x =
      structure_to_value
        [("paymentRequestId",
           (Option.map x.paymentRequestId ~f:PaymentRequestId.to_value));
        ("agreementId", (Option.map x.agreementId ~f:AgreementId.to_value));
        ("status", (Option.map x.status ~f:PaymentRequestStatus.to_value));
        ("name", (Option.map x.name ~f:PaymentRequestName.to_value));
        ("chargeId", (Option.map x.chargeId ~f:ChargeId.to_value));
        ("chargeAmount",
          (Option.map x.chargeAmount ~f:PositiveAmountUpto8Decimals.to_value));
        ("currencyCode",
          (Option.map x.currencyCode ~f:CurrencyCode.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("updatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "updatedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let currencyCode =
        (Option.map ~f:CurrencyCode.of_xml)
          (Xml.child xml_arg0 "currencyCode") in
      let chargeAmount =
        (Option.map ~f:PositiveAmountUpto8Decimals.of_xml)
          (Xml.child xml_arg0 "chargeAmount") in
      let chargeId =
        (Option.map ~f:ChargeId.of_xml) (Xml.child xml_arg0 "chargeId") in
      let name =
        (Option.map ~f:PaymentRequestName.of_xml) (Xml.child xml_arg0 "name") in
      let status =
        (Option.map ~f:PaymentRequestStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let agreementId =
        (Option.map ~f:AgreementId.of_xml) (Xml.child xml_arg0 "agreementId") in
      let paymentRequestId =
        (Option.map ~f:PaymentRequestId.of_xml)
          (Xml.child xml_arg0 "paymentRequestId") in
      make ?updatedAt ?createdAt ?currencyCode ?chargeAmount ?chargeId ?name
        ?status ?agreementId ?paymentRequestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt = field_map json__ "updatedAt" Timestamp.of_json in
      let createdAt = field_map json__ "createdAt" Timestamp.of_json in
      let currencyCode = field_map json__ "currencyCode" CurrencyCode.of_json in
      let chargeAmount =
        field_map json__ "chargeAmount" PositiveAmountUpto8Decimals.of_json in
      let chargeId = field_map json__ "chargeId" ChargeId.of_json in
      let name = field_map json__ "name" PaymentRequestName.of_json in
      let status = field_map json__ "status" PaymentRequestStatus.of_json in
      let agreementId = field_map json__ "agreementId" AgreementId.of_json in
      let paymentRequestId =
        field_map json__ "paymentRequestId" PaymentRequestId.of_json in
      make ?updatedAt ?createdAt ?currencyCode ?chargeAmount ?chargeId ?name
        ?status ?agreementId ?paymentRequestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Summary view of a payment request."]
module AgreementInvoiceLineItemGroupSummary =
  struct
    type nonrec t =
      {
      agreementId: ResourceId.t option
        [@ocaml.doc "The unique identifier of the agreement."];
      invoiceId: ResourceId.t option
        [@ocaml.doc "The identifier of the invoice for this group."];
      pricingCurrencyAmount: PricingCurrencyAmount.t option
        [@ocaml.doc "Monetary amounts for this invoice group."];
      invoiceBillingPeriod: InvoiceBillingPeriod.t option
        [@ocaml.doc "The billing period associated with this group."];
      issuedTime: Timestamp.t option
        [@ocaml.doc
          "The timestamp when the invoice containing this group was created."];
      invoiceType: InvoiceType.t option
        [@ocaml.doc
          "The type of invoice. Valid values are INVOICE and CREDIT_MEMO."];
      invoicingEntity: InvoicingEntity.t option
        [@ocaml.doc "The entity that issues the invoice."]}
    let make ?agreementId =
      fun ?invoiceId ->
        fun ?pricingCurrencyAmount ->
          fun ?invoiceBillingPeriod ->
            fun ?issuedTime ->
              fun ?invoiceType ->
                fun ?invoicingEntity ->
                  fun () ->
                    {
                      agreementId;
                      invoiceId;
                      pricingCurrencyAmount;
                      invoiceBillingPeriod;
                      issuedTime;
                      invoiceType;
                      invoicingEntity
                    }
    let to_value x =
      structure_to_value
        [("agreementId", (Option.map x.agreementId ~f:ResourceId.to_value));
        ("invoiceId", (Option.map x.invoiceId ~f:ResourceId.to_value));
        ("pricingCurrencyAmount",
          (Option.map x.pricingCurrencyAmount
             ~f:PricingCurrencyAmount.to_value));
        ("invoiceBillingPeriod",
          (Option.map x.invoiceBillingPeriod ~f:InvoiceBillingPeriod.to_value));
        ("issuedTime", (Option.map x.issuedTime ~f:Timestamp.to_value));
        ("invoiceType", (Option.map x.invoiceType ~f:InvoiceType.to_value));
        ("invoicingEntity",
          (Option.map x.invoicingEntity ~f:InvoicingEntity.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let invoicingEntity =
        (Option.map ~f:InvoicingEntity.of_xml)
          (Xml.child xml_arg0 "invoicingEntity") in
      let invoiceType =
        (Option.map ~f:InvoiceType.of_xml) (Xml.child xml_arg0 "invoiceType") in
      let issuedTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "issuedTime") in
      let invoiceBillingPeriod =
        (Option.map ~f:InvoiceBillingPeriod.of_xml)
          (Xml.child xml_arg0 "invoiceBillingPeriod") in
      let pricingCurrencyAmount =
        (Option.map ~f:PricingCurrencyAmount.of_xml)
          (Xml.child xml_arg0 "pricingCurrencyAmount") in
      let invoiceId =
        (Option.map ~f:ResourceId.of_xml) (Xml.child xml_arg0 "invoiceId") in
      let agreementId =
        (Option.map ~f:ResourceId.of_xml) (Xml.child xml_arg0 "agreementId") in
      make ?invoicingEntity ?invoiceType ?issuedTime ?invoiceBillingPeriod
        ?pricingCurrencyAmount ?invoiceId ?agreementId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let invoicingEntity =
        field_map json__ "invoicingEntity" InvoicingEntity.of_json in
      let invoiceType = field_map json__ "invoiceType" InvoiceType.of_json in
      let issuedTime = field_map json__ "issuedTime" Timestamp.of_json in
      let invoiceBillingPeriod =
        field_map json__ "invoiceBillingPeriod" InvoiceBillingPeriod.of_json in
      let pricingCurrencyAmount =
        field_map json__ "pricingCurrencyAmount"
          PricingCurrencyAmount.of_json in
      let invoiceId = field_map json__ "invoiceId" ResourceId.of_json in
      let agreementId = field_map json__ "agreementId" ResourceId.of_json in
      make ?invoicingEntity ?invoiceType ?issuedTime ?invoiceBillingPeriod
        ?pricingCurrencyAmount ?invoiceId ?agreementId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A summary of grouped billing data for an agreement invoice line item."]
module Charge =
  struct
    type nonrec t =
      {
      id: ResourceId.t option
        [@ocaml.doc "The unique identifier of the charge."];
      revision: ChargeRevision.t option
        [@ocaml.doc "The revision number of the charge."];
      agreementId: ResourceId.t option
        [@ocaml.doc
          "The unique identifier of the agreement that resulted in this charge."];
      agreementType: AgreementType.t option
        [@ocaml.doc
          "The type of agreement that resulted in this charge (for example, PurchaseAgreement)."];
      purchaseOrderReference: PurchaseOrderReference.t option
        [@ocaml.doc
          "The purchase order reference associated with the charge, if any."];
      currencyCode: CurrencyCode.t option
        [@ocaml.doc "The currency code for the charge amount."];
      amount: BoundedString.t option [@ocaml.doc "The amount of the charge."];
      time: Timestamp.t option
        [@ocaml.doc
          "The date and time when the charge will be incurred. This is available only when the charge date is known."]}
    let make ?id =
      fun ?revision ->
        fun ?agreementId ->
          fun ?agreementType ->
            fun ?purchaseOrderReference ->
              fun ?currencyCode ->
                fun ?amount ->
                  fun ?time ->
                    fun () ->
                      {
                        id;
                        revision;
                        agreementId;
                        agreementType;
                        purchaseOrderReference;
                        currencyCode;
                        amount;
                        time
                      }
    let to_value x =
      structure_to_value
        [("id", (Option.map x.id ~f:ResourceId.to_value));
        ("revision", (Option.map x.revision ~f:ChargeRevision.to_value));
        ("agreementId", (Option.map x.agreementId ~f:ResourceId.to_value));
        ("agreementType",
          (Option.map x.agreementType ~f:AgreementType.to_value));
        ("purchaseOrderReference",
          (Option.map x.purchaseOrderReference
             ~f:PurchaseOrderReference.to_value));
        ("currencyCode",
          (Option.map x.currencyCode ~f:CurrencyCode.to_value));
        ("amount", (Option.map x.amount ~f:BoundedString.to_value));
        ("time", (Option.map x.time ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let time = (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "time") in
      let amount =
        (Option.map ~f:BoundedString.of_xml) (Xml.child xml_arg0 "amount") in
      let currencyCode =
        (Option.map ~f:CurrencyCode.of_xml)
          (Xml.child xml_arg0 "currencyCode") in
      let purchaseOrderReference =
        (Option.map ~f:PurchaseOrderReference.of_xml)
          (Xml.child xml_arg0 "purchaseOrderReference") in
      let agreementType =
        (Option.map ~f:AgreementType.of_xml)
          (Xml.child xml_arg0 "agreementType") in
      let agreementId =
        (Option.map ~f:ResourceId.of_xml) (Xml.child xml_arg0 "agreementId") in
      let revision =
        (Option.map ~f:ChargeRevision.of_xml) (Xml.child xml_arg0 "revision") in
      let id = (Option.map ~f:ResourceId.of_xml) (Xml.child xml_arg0 "id") in
      make ?time ?amount ?currencyCode ?purchaseOrderReference ?agreementType
        ?agreementId ?revision ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let time = field_map json__ "time" Timestamp.of_json in
      let amount = field_map json__ "amount" BoundedString.of_json in
      let currencyCode = field_map json__ "currencyCode" CurrencyCode.of_json in
      let purchaseOrderReference =
        field_map json__ "purchaseOrderReference"
          PurchaseOrderReference.of_json in
      let agreementType =
        field_map json__ "agreementType" AgreementType.of_json in
      let agreementId = field_map json__ "agreementId" ResourceId.of_json in
      let revision = field_map json__ "revision" ChargeRevision.of_json in
      let id = field_map json__ "id" ResourceId.of_json in
      make ?time ?amount ?currencyCode ?purchaseOrderReference ?agreementType
        ?agreementId ?revision ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents a charge associated with an agreement, including amount, timing, and purchase order details."]
module AgreementCancellationRequestSummary =
  struct
    type nonrec t =
      {
      agreementCancellationRequestId: AgreementCancellationRequestId.t option
        [@ocaml.doc "The unique identifier of the cancellation request."];
      agreementId: AgreementId.t option
        [@ocaml.doc
          "The unique identifier of the agreement associated with this cancellation request."];
      status: AgreementCancellationRequestStatus.t option
        [@ocaml.doc
          "The current status of the cancellation request. Possible values include PENDING_APPROVAL, APPROVED, REJECTED, CANCELLED, and VALIDATION_FAILED."];
      reasonCode: AgreementCancellationRequestReasonCode.t option
        [@ocaml.doc "The reason code provided for the cancellation."];
      agreementType: AgreementType.t option
        [@ocaml.doc "The type of agreement."];
      catalog: Catalog.t option
        [@ocaml.doc "The catalog in which the agreement was created."];
      createdAt: Timestamp.t option
        [@ocaml.doc
          "The date and time when the cancellation request was created."];
      updatedAt: Timestamp.t option
        [@ocaml.doc
          "The date and time when the cancellation request was last updated."]}
    let make ?agreementCancellationRequestId =
      fun ?agreementId ->
        fun ?status ->
          fun ?reasonCode ->
            fun ?agreementType ->
              fun ?catalog ->
                fun ?createdAt ->
                  fun ?updatedAt ->
                    fun () ->
                      {
                        agreementCancellationRequestId;
                        agreementId;
                        status;
                        reasonCode;
                        agreementType;
                        catalog;
                        createdAt;
                        updatedAt
                      }
    let to_value x =
      structure_to_value
        [("agreementCancellationRequestId",
           (Option.map x.agreementCancellationRequestId
              ~f:AgreementCancellationRequestId.to_value));
        ("agreementId", (Option.map x.agreementId ~f:AgreementId.to_value));
        ("status",
          (Option.map x.status ~f:AgreementCancellationRequestStatus.to_value));
        ("reasonCode",
          (Option.map x.reasonCode
             ~f:AgreementCancellationRequestReasonCode.to_value));
        ("agreementType",
          (Option.map x.agreementType ~f:AgreementType.to_value));
        ("catalog", (Option.map x.catalog ~f:Catalog.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("updatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "updatedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let catalog =
        (Option.map ~f:Catalog.of_xml) (Xml.child xml_arg0 "catalog") in
      let agreementType =
        (Option.map ~f:AgreementType.of_xml)
          (Xml.child xml_arg0 "agreementType") in
      let reasonCode =
        (Option.map ~f:AgreementCancellationRequestReasonCode.of_xml)
          (Xml.child xml_arg0 "reasonCode") in
      let status =
        (Option.map ~f:AgreementCancellationRequestStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let agreementId =
        (Option.map ~f:AgreementId.of_xml) (Xml.child xml_arg0 "agreementId") in
      let agreementCancellationRequestId =
        (Option.map ~f:AgreementCancellationRequestId.of_xml)
          (Xml.child xml_arg0 "agreementCancellationRequestId") in
      make ?updatedAt ?createdAt ?catalog ?agreementType ?reasonCode ?status
        ?agreementId ?agreementCancellationRequestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt = field_map json__ "updatedAt" Timestamp.of_json in
      let createdAt = field_map json__ "createdAt" Timestamp.of_json in
      let catalog = field_map json__ "catalog" Catalog.of_json in
      let agreementType =
        field_map json__ "agreementType" AgreementType.of_json in
      let reasonCode =
        field_map json__ "reasonCode"
          AgreementCancellationRequestReasonCode.of_json in
      let status =
        field_map json__ "status" AgreementCancellationRequestStatus.of_json in
      let agreementId = field_map json__ "agreementId" AgreementId.of_json in
      let agreementCancellationRequestId =
        field_map json__ "agreementCancellationRequestId"
          AgreementCancellationRequestId.of_json in
      make ?updatedAt ?createdAt ?catalog ?agreementType ?reasonCode ?status
        ?agreementId ?agreementCancellationRequestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Summary view of an agreement cancellation request."]
module AcceptedTerm =
  struct
    type nonrec t =
      {
      legalTerm: LegalTerm.t option
        [@ocaml.doc
          "Defines the list of text agreements proposed to the acceptors. An example is the end user license agreement (EULA)."];
      supportTerm: SupportTerm.t option
        [@ocaml.doc
          "Defines the customer support available for the acceptors when they purchase the software."];
      renewalTerm: RenewalTerm.t option
        [@ocaml.doc
          "Defines that on graceful expiration of the agreement (when the agreement ends on its pre-defined end date), a new agreement will be created using the accepted terms on the existing agreement. In other words, the agreement will be renewed. Presence of RenewalTerm in the offer document means that auto-renewal is allowed. Buyers will have the option to accept or decline auto-renewal at the offer acceptance/agreement creation. Buyers can also change this flag from True to False or False to True at anytime during the agreement's lifecycle."];
      usageBasedPricingTerm: UsageBasedPricingTerm.t option
        [@ocaml.doc
          "Defines a usage-based pricing model (typically, pay-as-you-go pricing), where the customers are charged based on product usage."];
      configurableUpfrontPricingTerm: ConfigurableUpfrontPricingTerm.t option
        [@ocaml.doc
          "Defines a prepaid payment model that allows buyers to configure the entitlements they want to purchase and the duration."];
      byolPricingTerm: ByolPricingTerm.t option
        [@ocaml.doc
          "Enables you and your customers to move your existing agreements to AWS Marketplace. The customer won't be charged for product usage in AWS Marketplace because they already paid for the product outside of AWS Marketplace."];
      recurringPaymentTerm: RecurringPaymentTerm.t option
        [@ocaml.doc
          "Defines a pricing model where customers are charged a fixed recurring price at the end of each billing period."];
      validityTerm: ValidityTerm.t option
        [@ocaml.doc
          "Defines the conditions that will keep an agreement created from this offer valid."];
      paymentScheduleTerm: PaymentScheduleTerm.t option
        [@ocaml.doc
          "Defines an installment-based pricing model where customers are charged a fixed price on different dates during the agreement validity period. This is used most commonly for flexible payment schedule pricing."];
      freeTrialPricingTerm: FreeTrialPricingTerm.t option
        [@ocaml.doc
          "Defines a short-term free pricing model where the buyers aren\226\128\153t charged anything within a specified limit."];
      fixedUpfrontPricingTerm: FixedUpfrontPricingTerm.t option
        [@ocaml.doc
          "Defines a pre-paid pricing model where the customers are charged a fixed upfront amount."];
      variablePaymentTerm: VariablePaymentTerm.t option
        [@ocaml.doc
          "Defines a payment model where sellers can submit variable payment requests up to a maximum charge amount, with configurable approval strategies and expiration timelines."]}
    let make ?legalTerm =
      fun ?supportTerm ->
        fun ?renewalTerm ->
          fun ?usageBasedPricingTerm ->
            fun ?configurableUpfrontPricingTerm ->
              fun ?byolPricingTerm ->
                fun ?recurringPaymentTerm ->
                  fun ?validityTerm ->
                    fun ?paymentScheduleTerm ->
                      fun ?freeTrialPricingTerm ->
                        fun ?fixedUpfrontPricingTerm ->
                          fun ?variablePaymentTerm ->
                            fun () ->
                              {
                                legalTerm;
                                supportTerm;
                                renewalTerm;
                                usageBasedPricingTerm;
                                configurableUpfrontPricingTerm;
                                byolPricingTerm;
                                recurringPaymentTerm;
                                validityTerm;
                                paymentScheduleTerm;
                                freeTrialPricingTerm;
                                fixedUpfrontPricingTerm;
                                variablePaymentTerm
                              }
    let to_value x =
      structure_to_value
        [("legalTerm", (Option.map x.legalTerm ~f:LegalTerm.to_value));
        ("supportTerm", (Option.map x.supportTerm ~f:SupportTerm.to_value));
        ("renewalTerm", (Option.map x.renewalTerm ~f:RenewalTerm.to_value));
        ("usageBasedPricingTerm",
          (Option.map x.usageBasedPricingTerm
             ~f:UsageBasedPricingTerm.to_value));
        ("configurableUpfrontPricingTerm",
          (Option.map x.configurableUpfrontPricingTerm
             ~f:ConfigurableUpfrontPricingTerm.to_value));
        ("byolPricingTerm",
          (Option.map x.byolPricingTerm ~f:ByolPricingTerm.to_value));
        ("recurringPaymentTerm",
          (Option.map x.recurringPaymentTerm ~f:RecurringPaymentTerm.to_value));
        ("validityTerm",
          (Option.map x.validityTerm ~f:ValidityTerm.to_value));
        ("paymentScheduleTerm",
          (Option.map x.paymentScheduleTerm ~f:PaymentScheduleTerm.to_value));
        ("freeTrialPricingTerm",
          (Option.map x.freeTrialPricingTerm ~f:FreeTrialPricingTerm.to_value));
        ("fixedUpfrontPricingTerm",
          (Option.map x.fixedUpfrontPricingTerm
             ~f:FixedUpfrontPricingTerm.to_value));
        ("variablePaymentTerm",
          (Option.map x.variablePaymentTerm ~f:VariablePaymentTerm.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let variablePaymentTerm =
        (Option.map ~f:VariablePaymentTerm.of_xml)
          (Xml.child xml_arg0 "variablePaymentTerm") in
      let fixedUpfrontPricingTerm =
        (Option.map ~f:FixedUpfrontPricingTerm.of_xml)
          (Xml.child xml_arg0 "fixedUpfrontPricingTerm") in
      let freeTrialPricingTerm =
        (Option.map ~f:FreeTrialPricingTerm.of_xml)
          (Xml.child xml_arg0 "freeTrialPricingTerm") in
      let paymentScheduleTerm =
        (Option.map ~f:PaymentScheduleTerm.of_xml)
          (Xml.child xml_arg0 "paymentScheduleTerm") in
      let validityTerm =
        (Option.map ~f:ValidityTerm.of_xml)
          (Xml.child xml_arg0 "validityTerm") in
      let recurringPaymentTerm =
        (Option.map ~f:RecurringPaymentTerm.of_xml)
          (Xml.child xml_arg0 "recurringPaymentTerm") in
      let byolPricingTerm =
        (Option.map ~f:ByolPricingTerm.of_xml)
          (Xml.child xml_arg0 "byolPricingTerm") in
      let configurableUpfrontPricingTerm =
        (Option.map ~f:ConfigurableUpfrontPricingTerm.of_xml)
          (Xml.child xml_arg0 "configurableUpfrontPricingTerm") in
      let usageBasedPricingTerm =
        (Option.map ~f:UsageBasedPricingTerm.of_xml)
          (Xml.child xml_arg0 "usageBasedPricingTerm") in
      let renewalTerm =
        (Option.map ~f:RenewalTerm.of_xml) (Xml.child xml_arg0 "renewalTerm") in
      let supportTerm =
        (Option.map ~f:SupportTerm.of_xml) (Xml.child xml_arg0 "supportTerm") in
      let legalTerm =
        (Option.map ~f:LegalTerm.of_xml) (Xml.child xml_arg0 "legalTerm") in
      make ?variablePaymentTerm ?fixedUpfrontPricingTerm
        ?freeTrialPricingTerm ?paymentScheduleTerm ?validityTerm
        ?recurringPaymentTerm ?byolPricingTerm
        ?configurableUpfrontPricingTerm ?usageBasedPricingTerm ?renewalTerm
        ?supportTerm ?legalTerm ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let variablePaymentTerm =
        field_map json__ "variablePaymentTerm" VariablePaymentTerm.of_json in
      let fixedUpfrontPricingTerm =
        field_map json__ "fixedUpfrontPricingTerm"
          FixedUpfrontPricingTerm.of_json in
      let freeTrialPricingTerm =
        field_map json__ "freeTrialPricingTerm" FreeTrialPricingTerm.of_json in
      let paymentScheduleTerm =
        field_map json__ "paymentScheduleTerm" PaymentScheduleTerm.of_json in
      let validityTerm = field_map json__ "validityTerm" ValidityTerm.of_json in
      let recurringPaymentTerm =
        field_map json__ "recurringPaymentTerm" RecurringPaymentTerm.of_json in
      let byolPricingTerm =
        field_map json__ "byolPricingTerm" ByolPricingTerm.of_json in
      let configurableUpfrontPricingTerm =
        field_map json__ "configurableUpfrontPricingTerm"
          ConfigurableUpfrontPricingTerm.of_json in
      let usageBasedPricingTerm =
        field_map json__ "usageBasedPricingTerm"
          UsageBasedPricingTerm.of_json in
      let renewalTerm = field_map json__ "renewalTerm" RenewalTerm.of_json in
      let supportTerm = field_map json__ "supportTerm" SupportTerm.of_json in
      let legalTerm = field_map json__ "legalTerm" LegalTerm.of_json in
      make ?variablePaymentTerm ?fixedUpfrontPricingTerm
        ?freeTrialPricingTerm ?paymentScheduleTerm ?validityTerm
        ?recurringPaymentTerm ?byolPricingTerm
        ?configurableUpfrontPricingTerm ?usageBasedPricingTerm ?renewalTerm
        ?supportTerm ?legalTerm ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A subset of terms proposed by the proposer, which have been accepted by the acceptor as part of agreement creation."]
module AgreementEntitlement =
  struct
    type nonrec t =
      {
      resource: Resource.t option
        [@ocaml.doc
          "The resource that the entitlement is provisioned to, such as a product."];
      type_: EntitlementType.t option [@ocaml.doc "The type of entitlement."];
      registrationToken: RegistrationToken.t option
        [@ocaml.doc
          "A short-lived token required by acceptors to register their account with the product provider. The token is only valid for 30 minutes after creation and is only applicable for purchase agreements."];
      status: AgreementEntitlementStatus.t option
        [@ocaml.doc "The current state of an entitlement."];
      statusReasonCode: AgreementEntitlementStatusReasonCode.t option
        [@ocaml.doc
          "Provides more information about the status of an entitlement."];
      licenseArn: AwsArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the AWS License Manager license associated with the entitlement."]}
    let make ?resource =
      fun ?type_ ->
        fun ?registrationToken ->
          fun ?status ->
            fun ?statusReasonCode ->
              fun ?licenseArn ->
                fun () ->
                  {
                    resource;
                    type_;
                    registrationToken;
                    status;
                    statusReasonCode;
                    licenseArn
                  }
    let to_value x =
      structure_to_value
        [("resource", (Option.map x.resource ~f:Resource.to_value));
        ("type", (Option.map x.type_ ~f:EntitlementType.to_value));
        ("registrationToken",
          (Option.map x.registrationToken ~f:RegistrationToken.to_value));
        ("status",
          (Option.map x.status ~f:AgreementEntitlementStatus.to_value));
        ("statusReasonCode",
          (Option.map x.statusReasonCode
             ~f:AgreementEntitlementStatusReasonCode.to_value));
        ("licenseArn", (Option.map x.licenseArn ~f:AwsArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let licenseArn =
        (Option.map ~f:AwsArn.of_xml) (Xml.child xml_arg0 "licenseArn") in
      let statusReasonCode =
        (Option.map ~f:AgreementEntitlementStatusReasonCode.of_xml)
          (Xml.child xml_arg0 "statusReasonCode") in
      let status =
        (Option.map ~f:AgreementEntitlementStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let registrationToken =
        (Option.map ~f:RegistrationToken.of_xml)
          (Xml.child xml_arg0 "registrationToken") in
      let type_ =
        (Option.map ~f:EntitlementType.of_xml) (Xml.child xml_arg0 "type") in
      let resource =
        (Option.map ~f:Resource.of_xml) (Xml.child xml_arg0 "resource") in
      make ?licenseArn ?statusReasonCode ?status ?registrationToken ?type_
        ?resource ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let licenseArn = field_map json__ "licenseArn" AwsArn.of_json in
      let statusReasonCode =
        field_map json__ "statusReasonCode"
          AgreementEntitlementStatusReasonCode.of_json in
      let status =
        field_map json__ "status" AgreementEntitlementStatus.of_json in
      let registrationToken =
        field_map json__ "registrationToken" RegistrationToken.of_json in
      let type_ = field_map json__ "type" EntitlementType.of_json in
      let resource = field_map json__ "resource" Resource.of_json in
      make ?licenseArn ?statusReasonCode ?status ?registrationToken ?type_
        ?resource ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Represents an entitlement associated with an agreement, including the provisioning status, resource, and type."]
module ExpectedChargeList =
  struct
    type nonrec t = ExpectedCharge.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ExpectedCharge.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ExpectedCharge.of_xml)
    let of_json j =
      list_of_json ~kind:"ExpectedChargeList" ~of_json:ExpectedCharge.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module ItemizedChargeList =
  struct
    type nonrec t = ItemizedCharge.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:ItemizedCharge.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ItemizedCharge.of_xml)
    let of_json j =
      list_of_json ~kind:"ItemizedChargeList" ~of_json:ItemizedCharge.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module RequestedTerm =
  struct
    type nonrec t =
      {
      id: TermId.t
        [@ocaml.doc
          "The unique identifier of the term in the agreement proposal."];
      configuration: RequestedTermConfiguration.t option
        [@ocaml.doc
          "Additional configuration for the requested terms. This configuration is applicable only to the terms that accept a customer-provided configuration, such as ConfigurableUpfrontPricingTerm."]}
    let context_ = "RequestedTerm"
    let make ?configuration = fun ~id -> fun () -> { configuration; id }
    let to_value x =
      structure_to_value
        [("id", (Some (TermId.to_value x.id)));
        ("configuration",
          (Option.map x.configuration ~f:RequestedTermConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let configuration =
        (Option.map ~f:RequestedTermConfiguration.of_xml)
          (Xml.child xml_arg0 "configuration") in
      let id = TermId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ?configuration ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let configuration =
        field_map json__ "configuration" RequestedTermConfiguration.of_json in
      let id = field_map_exn json__ "id" TermId.of_json in
      make ?configuration ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Defines what is being accepted as part of the agreement creation or update request, and it includes their configurations."]
module TaxEstimation =
  struct
    type nonrec t =
      | DISABLED 
      | ENABLED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DISABLED -> "DISABLED"
      | ENABLED -> "ENABLED"
      | Non_static_id s -> s
    let of_string =
      function
      | "DISABLED" -> DISABLED
      | "ENABLED" -> ENABLED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration TaxEstimation" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"TaxEstimation" j)
    let to_json = simple_to_json to_value
  end
module BatchCreateBillingAdjustmentError =
  struct
    type nonrec t =
      {
      code: BillingAdjustmentErrorCode.t option
        [@ocaml.doc "The error code indicating the reason for failure."];
      message: String_.t option
        [@ocaml.doc "A human-readable message describing the error."];
      clientToken: ClientToken.t option
        [@ocaml.doc "The client token of the request entry that failed."]}
    let make ?code =
      fun ?message ->
        fun ?clientToken -> fun () -> { code; message; clientToken }
    let to_value x =
      structure_to_value
        [("code", (Option.map x.code ~f:BillingAdjustmentErrorCode.to_value));
        ("message", (Option.map x.message ~f:String_.to_value));
        ("clientToken", (Option.map x.clientToken ~f:ClientToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "clientToken") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let code =
        (Option.map ~f:BillingAdjustmentErrorCode.of_xml)
          (Xml.child xml_arg0 "code") in
      make ?clientToken ?message ?code ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "clientToken" ClientToken.of_json in
      let message = field_map json__ "message" String_.of_json in
      let code = field_map json__ "code" BillingAdjustmentErrorCode.of_json in
      make ?clientToken ?message ?code ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An error for a billing adjustment request entry that failed validation."]
module BatchCreateBillingAdjustmentItem =
  struct
    type nonrec t =
      {
      billingAdjustmentRequestId: BillingAdjustmentRequestId.t option
        [@ocaml.doc
          "The unique identifier of the created billing adjustment request."];
      clientToken: ClientToken.t option
        [@ocaml.doc
          "The client token provided in the corresponding request entry."]}
    let make ?billingAdjustmentRequestId =
      fun ?clientToken ->
        fun () -> { billingAdjustmentRequestId; clientToken }
    let to_value x =
      structure_to_value
        [("billingAdjustmentRequestId",
           (Option.map x.billingAdjustmentRequestId
              ~f:BillingAdjustmentRequestId.to_value));
        ("clientToken", (Option.map x.clientToken ~f:ClientToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "clientToken") in
      let billingAdjustmentRequestId =
        (Option.map ~f:BillingAdjustmentRequestId.of_xml)
          (Xml.child xml_arg0 "billingAdjustmentRequestId") in
      make ?clientToken ?billingAdjustmentRequestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken = field_map json__ "clientToken" ClientToken.of_json in
      let billingAdjustmentRequestId =
        field_map json__ "billingAdjustmentRequestId"
          BillingAdjustmentRequestId.of_json in
      make ?clientToken ?billingAdjustmentRequestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A successfully created billing adjustment request item."]
module BatchCreateBillingAdjustmentRequestEntry =
  struct
    type nonrec t =
      {
      agreementId: AgreementId.t
        [@ocaml.doc
          "The unique identifier of the agreement associated with the invoice."];
      originalInvoiceId: InvoiceId.t
        [@ocaml.doc "The identifier of the original invoice to adjust."];
      adjustmentAmount: PositiveAmountUpto8Decimals.t
        [@ocaml.doc
          "The adjustment amount as a string representation of a decimal number in the currency of the invoice."];
      currencyCode: CurrencyCode.t
        [@ocaml.doc
          "The 3-letter ISO 4217 currency code for the adjustment amount. Must match the currency code of the offer associated with the agreement (e.g., USD)."];
      adjustmentReasonCode: BillingAdjustmentReasonCode.t
        [@ocaml.doc "The reason code for the billing adjustment."];
      description: BillingAdjustmentDescription.t option
        [@ocaml.doc
          "An optional detailed description of the adjustment reason."];
      clientToken: ClientToken.t
        [@ocaml.doc
          "A unique, case-sensitive identifier that you provide to ensure the idempotency of the request."]}
    let context_ = "BatchCreateBillingAdjustmentRequestEntry"
    let make ?description =
      fun ~agreementId ->
        fun ~originalInvoiceId ->
          fun ~adjustmentAmount ->
            fun ~currencyCode ->
              fun ~adjustmentReasonCode ->
                fun ~clientToken ->
                  fun () ->
                    {
                      description;
                      agreementId;
                      originalInvoiceId;
                      adjustmentAmount;
                      currencyCode;
                      adjustmentReasonCode;
                      clientToken
                    }
    let to_value x =
      structure_to_value
        [("agreementId", (Some (AgreementId.to_value x.agreementId)));
        ("originalInvoiceId",
          (Some (InvoiceId.to_value x.originalInvoiceId)));
        ("adjustmentAmount",
          (Some (PositiveAmountUpto8Decimals.to_value x.adjustmentAmount)));
        ("currencyCode", (Some (CurrencyCode.to_value x.currencyCode)));
        ("adjustmentReasonCode",
          (Some (BillingAdjustmentReasonCode.to_value x.adjustmentReasonCode)));
        ("description",
          (Option.map x.description ~f:BillingAdjustmentDescription.to_value));
        ("clientToken", (Some (ClientToken.to_value x.clientToken)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let clientToken =
        ClientToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "clientToken") in
      let description =
        (Option.map ~f:BillingAdjustmentDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let adjustmentReasonCode =
        BillingAdjustmentReasonCode.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "adjustmentReasonCode") in
      let currencyCode =
        CurrencyCode.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "currencyCode") in
      let adjustmentAmount =
        PositiveAmountUpto8Decimals.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "adjustmentAmount") in
      let originalInvoiceId =
        InvoiceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "originalInvoiceId") in
      let agreementId =
        AgreementId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "agreementId") in
      make ~clientToken ?description ~adjustmentReasonCode ~currencyCode
        ~adjustmentAmount ~originalInvoiceId ~agreementId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let clientToken =
        field_map_exn json__ "clientToken" ClientToken.of_json in
      let description =
        field_map json__ "description" BillingAdjustmentDescription.of_json in
      let adjustmentReasonCode =
        field_map_exn json__ "adjustmentReasonCode"
          BillingAdjustmentReasonCode.of_json in
      let currencyCode =
        field_map_exn json__ "currencyCode" CurrencyCode.of_json in
      let adjustmentAmount =
        field_map_exn json__ "adjustmentAmount"
          PositiveAmountUpto8Decimals.of_json in
      let originalInvoiceId =
        field_map_exn json__ "originalInvoiceId" InvoiceId.of_json in
      let agreementId =
        field_map_exn json__ "agreementId" AgreementId.of_json in
      make ~clientToken ?description ~adjustmentReasonCode ~currencyCode
        ~adjustmentAmount ~originalInvoiceId ~agreementId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An individual entry in a batch billing adjustment request, specifying the invoice and adjustment details."]
module AccessDeniedException =
  struct
    type nonrec t =
      {
      requestId: RequestId.t option
        [@ocaml.doc "The unique identifier for the error."];
      message: ExceptionMessage.t option
        [@ocaml.doc "Description of the error."];
      reason: AccessDeniedExceptionReason.t option
        [@ocaml.doc "The reason for the access denied exception."]}
    let make ?requestId =
      fun ?message -> fun ?reason -> fun () -> { requestId; message; reason }
    let to_value x =
      structure_to_value
        [("requestId", (Option.map x.requestId ~f:RequestId.to_value));
        ("message", (Option.map x.message ~f:ExceptionMessage.to_value));
        ("reason",
          (Option.map x.reason ~f:AccessDeniedExceptionReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let reason =
        (Option.map ~f:AccessDeniedExceptionReason.of_xml)
          (Xml.child xml_arg0 "reason") in
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "message") in
      let requestId =
        (Option.map ~f:RequestId.of_xml) (Xml.child xml_arg0 "requestId") in
      make ?reason ?message ?requestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let reason =
        field_map json__ "reason" AccessDeniedExceptionReason.of_json in
      let message = field_map json__ "message" ExceptionMessage.of_json in
      let requestId = field_map json__ "requestId" RequestId.of_json in
      make ?reason ?message ?requestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "User does not have sufficient access to perform this action."]
module ConflictException =
  struct
    type nonrec t =
      {
      requestId: RequestId.t option
        [@ocaml.doc "The unique identifier for the error."];
      message: ExceptionMessage.t option
        [@ocaml.doc "Description of the error."];
      resourceId: ResourceId.t option
        [@ocaml.doc
          "The unique identifier of the resource involved in the conflict."];
      resourceType: ResourceType.t option
        [@ocaml.doc "The type of the resource involved in the conflict."]}
    let make ?requestId =
      fun ?message ->
        fun ?resourceId ->
          fun ?resourceType ->
            fun () -> { requestId; message; resourceId; resourceType }
    let to_value x =
      structure_to_value
        [("requestId", (Option.map x.requestId ~f:RequestId.to_value));
        ("message", (Option.map x.message ~f:ExceptionMessage.to_value));
        ("resourceId", (Option.map x.resourceId ~f:ResourceId.to_value));
        ("resourceType",
          (Option.map x.resourceType ~f:ResourceType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "resourceType") in
      let resourceId =
        (Option.map ~f:ResourceId.of_xml) (Xml.child xml_arg0 "resourceId") in
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "message") in
      let requestId =
        (Option.map ~f:RequestId.of_xml) (Xml.child xml_arg0 "requestId") in
      make ?resourceType ?resourceId ?message ?requestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceType = field_map json__ "resourceType" ResourceType.of_json in
      let resourceId = field_map json__ "resourceId" ResourceId.of_json in
      let message = field_map json__ "message" ExceptionMessage.of_json in
      let requestId = field_map json__ "requestId" RequestId.of_json in
      make ?resourceType ?resourceId ?message ?requestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Request was denied due to a resource conflict."]
module InternalServerException =
  struct
    type nonrec t =
      {
      requestId: RequestId.t option
        [@ocaml.doc "The unique identifier for the error."];
      message: ExceptionMessage.t option
        [@ocaml.doc "Description of the error."]}
    let make ?requestId = fun ?message -> fun () -> { requestId; message }
    let to_value x =
      structure_to_value
        [("requestId", (Option.map x.requestId ~f:RequestId.to_value));
        ("message", (Option.map x.message ~f:ExceptionMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "message") in
      let requestId =
        (Option.map ~f:RequestId.of_xml) (Xml.child xml_arg0 "requestId") in
      make ?message ?requestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" ExceptionMessage.of_json in
      let requestId = field_map json__ "requestId" RequestId.of_json in
      make ?message ?requestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Unexpected error during processing of request."]
module ResourceNotFoundException =
  struct
    type nonrec t =
      {
      requestId: RequestId.t option
        [@ocaml.doc "The unique identifier for the error."];
      message: ExceptionMessage.t option
        [@ocaml.doc "Description of the error."];
      resourceId: ResourceId.t option
        [@ocaml.doc "The unique identifier for the resource."];
      resourceType: ResourceType.t option
        [@ocaml.doc "The type of resource."]}
    let make ?requestId =
      fun ?message ->
        fun ?resourceId ->
          fun ?resourceType ->
            fun () -> { requestId; message; resourceId; resourceType }
    let to_value x =
      structure_to_value
        [("requestId", (Option.map x.requestId ~f:RequestId.to_value));
        ("message", (Option.map x.message ~f:ExceptionMessage.to_value));
        ("resourceId", (Option.map x.resourceId ~f:ResourceId.to_value));
        ("resourceType",
          (Option.map x.resourceType ~f:ResourceType.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:ResourceType.of_xml)
          (Xml.child xml_arg0 "resourceType") in
      let resourceId =
        (Option.map ~f:ResourceId.of_xml) (Xml.child xml_arg0 "resourceId") in
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "message") in
      let requestId =
        (Option.map ~f:RequestId.of_xml) (Xml.child xml_arg0 "requestId") in
      make ?resourceType ?resourceId ?message ?requestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceType = field_map json__ "resourceType" ResourceType.of_json in
      let resourceId = field_map json__ "resourceId" ResourceId.of_json in
      let message = field_map json__ "message" ExceptionMessage.of_json in
      let requestId = field_map json__ "requestId" RequestId.of_json in
      make ?resourceType ?resourceId ?message ?requestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Request references a resource which does not exist."]
module ThrottlingException =
  struct
    type nonrec t =
      {
      requestId: RequestId.t option
        [@ocaml.doc "The unique identifier for the error."];
      message: ExceptionMessage.t option
        [@ocaml.doc "Description of the error."]}
    let make ?requestId = fun ?message -> fun () -> { requestId; message }
    let to_value x =
      structure_to_value
        [("requestId", (Option.map x.requestId ~f:RequestId.to_value));
        ("message", (Option.map x.message ~f:ExceptionMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "message") in
      let requestId =
        (Option.map ~f:RequestId.of_xml) (Xml.child xml_arg0 "requestId") in
      make ?message ?requestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let message = field_map json__ "message" ExceptionMessage.of_json in
      let requestId = field_map json__ "requestId" RequestId.of_json in
      make ?message ?requestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Request was denied due to request throttling."]
module ValidationException =
  struct
    type nonrec t =
      {
      requestId: RequestId.t option
        [@ocaml.doc "The unique identifier associated with the error."];
      message: ExceptionMessage.t option
        [@ocaml.doc "Description of the error."];
      reason: ValidationExceptionReason.t option
        [@ocaml.doc "The reason associated with the error."];
      fields: ValidationExceptionFieldList.t option
        [@ocaml.doc "The fields associated with the error."]}
    let make ?requestId =
      fun ?message ->
        fun ?reason ->
          fun ?fields -> fun () -> { requestId; message; reason; fields }
    let to_value x =
      structure_to_value
        [("requestId", (Option.map x.requestId ~f:RequestId.to_value));
        ("message", (Option.map x.message ~f:ExceptionMessage.to_value));
        ("reason",
          (Option.map x.reason ~f:ValidationExceptionReason.to_value));
        ("fields",
          (Option.map x.fields ~f:ValidationExceptionFieldList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fields =
        (Option.map ~f:ValidationExceptionFieldList.of_xml)
          (Xml.child xml_arg0 "fields") in
      let reason =
        (Option.map ~f:ValidationExceptionReason.of_xml)
          (Xml.child xml_arg0 "reason") in
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "message") in
      let requestId =
        (Option.map ~f:RequestId.of_xml) (Xml.child xml_arg0 "requestId") in
      make ?fields ?reason ?message ?requestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let fields =
        field_map json__ "fields" ValidationExceptionFieldList.of_json in
      let reason =
        field_map json__ "reason" ValidationExceptionReason.of_json in
      let message = field_map json__ "message" ExceptionMessage.of_json in
      let requestId = field_map json__ "requestId" RequestId.of_json in
      make ?fields ?reason ?message ?requestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The input fails to satisfy the constraints specified by the service."]
module PurchaseOrders =
  struct
    type nonrec t = PurchaseOrder.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:86) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:PurchaseOrder.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PurchaseOrder.of_xml)
    let of_json j =
      list_of_json ~kind:"PurchaseOrders" ~of_json:PurchaseOrder.of_json j
    let to_json v = composed_to_json to_value v
  end
module PaymentRequestDescription =
  struct
    type nonrec t = string
    let context_ = "PaymentRequestDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PaymentRequestDescription" j
    let to_json = simple_to_json to_value
  end
module AgreementCancellationRequestDescription =
  struct
    type nonrec t = string
    let context_ = "AgreementCancellationRequestDescription"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"AgreementCancellationRequestDescription" j
    let to_json = simple_to_json to_value
  end
module AgreementViewSummaryList =
  struct
    type nonrec t = AgreementViewSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AgreementViewSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AgreementViewSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"AgreementViewSummaryList"
        ~of_json:AgreementViewSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module NextToken =
  struct
    type nonrec t = string
    let context_ = "NextToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:8192) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9+/=_-]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NextToken" j
    let to_json = simple_to_json to_value
  end
module FilterList =
  struct
    type nonrec t = Filter.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Filter.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Filter.of_xml)
    let of_json j = list_of_json ~kind:"FilterList" ~of_json:Filter.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:50) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Sort =
  struct
    type nonrec t =
      {
      sortBy: SortBy.t option
        [@ocaml.doc
          "The attribute on which the data is grouped, which can be by StartTime and EndTime. The default value is EndTime."];
      sortOrder: SortOrder.t option
        [@ocaml.doc
          "The sorting order, which can be ASCENDING or DESCENDING. The default value is DESCENDING."]}
    let make ?sortBy = fun ?sortOrder -> fun () -> { sortBy; sortOrder }
    let to_value x =
      structure_to_value
        [("sortBy", (Option.map x.sortBy ~f:SortBy.to_value));
        ("sortOrder", (Option.map x.sortOrder ~f:SortOrder.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sortOrder =
        (Option.map ~f:SortOrder.of_xml) (Xml.child xml_arg0 "sortOrder") in
      let sortBy =
        (Option.map ~f:SortBy.of_xml) (Xml.child xml_arg0 "sortBy") in
      make ?sortOrder ?sortBy ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let sortOrder = field_map json__ "sortOrder" SortOrder.of_json in
      let sortBy = field_map json__ "sortBy" SortBy.of_json in
      make ?sortOrder ?sortBy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An object that contains the SortBy and SortOrder attributes."]
module PaymentRequestStatusMessage =
  struct
    type nonrec t = string
    let context_ = "PaymentRequestStatusMessage"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PaymentRequestStatusMessage" j
    let to_json = simple_to_json to_value
  end
module PaymentRequestRejectionReason =
  struct
    type nonrec t = string
    let context_ = "PaymentRequestRejectionReason"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:250) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PaymentRequestRejectionReason" j
    let to_json = simple_to_json to_value
  end
module AgreementCancellationRequestStatusMessage =
  struct
    type nonrec t = string
    let context_ = "AgreementCancellationRequestStatusMessage"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"AgreementCancellationRequestStatusMessage" j
    let to_json = simple_to_json to_value
  end
module AgreementCancellationRequestRejectionReason =
  struct
    type nonrec t = string
    let context_ = "AgreementCancellationRequestRejectionReason"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"AgreementCancellationRequestRejectionReason" j
    let to_json = simple_to_json to_value
  end
module BillingAdjustmentSummaryList =
  struct
    type nonrec t = BillingAdjustmentSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:BillingAdjustmentSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BillingAdjustmentSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"BillingAdjustmentSummaryList"
        ~of_json:BillingAdjustmentSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module PaymentRequestSummaryList =
  struct
    type nonrec t = PaymentRequestSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:PaymentRequestSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PaymentRequestSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"PaymentRequestSummaryList"
        ~of_json:PaymentRequestSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module PartyType =
  struct
    type nonrec t = string
    let context_ = "PartyType"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:32) >>=
                  (fun () -> check_pattern i ~pattern:"[A-Za-z]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PartyType" j
    let to_json = simple_to_json to_value
  end
module AgreementInvoiceLineItemGroupSummaries =
  struct
    type nonrec t = AgreementInvoiceLineItemGroupSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AgreementInvoiceLineItemGroupSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:AgreementInvoiceLineItemGroupSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"AgreementInvoiceLineItemGroupSummaries"
        ~of_json:AgreementInvoiceLineItemGroupSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module LineItemGroupBy =
  struct
    type nonrec t =
      | INVOICE_ID 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | INVOICE_ID -> "INVOICE_ID" | Non_static_id s -> s
    let of_string =
      function | "INVOICE_ID" -> INVOICE_ID | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration LineItemGroupBy" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"LineItemGroupBy" j)
    let to_json = simple_to_json to_value
  end
module Charges =
  struct
    type nonrec t = Charge.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:Charge.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Charge.of_xml)
    let of_json j = list_of_json ~kind:"Charges" ~of_json:Charge.of_json j
    let to_json v = composed_to_json to_value v
  end
module AgreementCancellationRequestSummaryList =
  struct
    type nonrec t = AgreementCancellationRequestSummary.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AgreementCancellationRequestSummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:AgreementCancellationRequestSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"AgreementCancellationRequestSummaryList"
        ~of_json:AgreementCancellationRequestSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module BillingAdjustmentStatusMessage =
  struct
    type nonrec t = string
    let context_ = "BillingAdjustmentStatusMessage"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:500) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BillingAdjustmentStatusMessage" j
    let to_json = simple_to_json to_value
  end
module AcceptedTermList =
  struct
    type nonrec t = AcceptedTerm.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AcceptedTerm.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AcceptedTerm.of_xml)
    let of_json j =
      list_of_json ~kind:"AcceptedTermList" ~of_json:AcceptedTerm.of_json j
    let to_json v = composed_to_json to_value v
  end
module AgreementEntitlementList =
  struct
    type nonrec t = AgreementEntitlement.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:AgreementEntitlement.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AgreementEntitlement.of_xml)
    let of_json j =
      list_of_json ~kind:"AgreementEntitlementList"
        ~of_json:AgreementEntitlement.of_json j
    let to_json v = composed_to_json to_value v
  end
module EstimatedCharges =
  struct
    type nonrec t =
      {
      currencyCode: CurrencyCode.t option
        [@ocaml.doc "Defines the currency code for the charge."];
      agreementValue: BoundedString.t option
        [@ocaml.doc
          "The total known amount customer has to pay across the lifecycle of the agreement. This is the total contract value if accepted terms contain ConfigurableUpfrontPricingTerm or FixedUpfrontPricingTerm. In the case of pure contract pricing, this will be the total value of the contract. In the case of contracts with consumption pricing, this will only include the committed value and not include any overages that occur. If the accepted terms contain PaymentScheduleTerm, it will be the total payment schedule amount. This occurs when flexible payment schedule is used, and is the sum of all invoice charges in the payment schedule. In case a customer has amended an agreement, by purchasing more units of any dimension, this will include both the original cost as well as the added cost incurred due to addition of new units. This is 0 if the accepted terms contain UsageBasedPricingTerm without ConfigurableUpfrontPricingTerm or RecurringPaymentTerm. This occurs for usage-based pricing (such as SaaS metered or AMI/container hourly or monthly), because the exact usage is not known upfront."]}
    let make ?currencyCode =
      fun ?agreementValue -> fun () -> { currencyCode; agreementValue }
    let to_value x =
      structure_to_value
        [("currencyCode",
           (Option.map x.currencyCode ~f:CurrencyCode.to_value));
        ("agreementValue",
          (Option.map x.agreementValue ~f:BoundedString.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let agreementValue =
        (Option.map ~f:BoundedString.of_xml)
          (Xml.child xml_arg0 "agreementValue") in
      let currencyCode =
        (Option.map ~f:CurrencyCode.of_xml)
          (Xml.child xml_arg0 "currencyCode") in
      make ?agreementValue ?currencyCode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let agreementValue =
        field_map json__ "agreementValue" BoundedString.of_json in
      let currencyCode = field_map json__ "currencyCode" CurrencyCode.of_json in
      make ?agreementValue ?currencyCode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Estimated cost of the agreement."]
module AgreementRequestId =
  struct
    type nonrec t = string
    let context_ = "AgreementRequestId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"ar-[A-Za-z0-9]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AgreementRequestId" j
    let to_json = simple_to_json to_value
  end
module ChargeSummary =
  struct
    type nonrec t =
      {
      currencyCode: CurrencyCode.t option
        [@ocaml.doc
          "The three-letter currency code for all charges (e.g., USD)."];
      newAgreementValue: BoundedString.t option
        [@ocaml.doc
          "The total value of the agreement, which includes any amendments."];
      newAgreementValueAfterTax: BoundedString.t option
        [@ocaml.doc
          "Expected new agreement value after estimated taxes are applied."];
      expectedCharges: ExpectedChargeList.t option
        [@ocaml.doc "A list of expected charges for the agreement request."];
      estimatedTaxes: EstimatedTaxes.t option
        [@ocaml.doc
          "Provides an aggregated view of estimated tax information for the agreement."];
      itemizedCharges: ItemizedChargeList.t option
        [@ocaml.doc "An itemized list of charges for the agreement request."];
      invoicingEntity: InvoicingEntity.t option
        [@ocaml.doc "The entity responsible for issuing the invoice."]}
    let make ?currencyCode =
      fun ?newAgreementValue ->
        fun ?newAgreementValueAfterTax ->
          fun ?expectedCharges ->
            fun ?estimatedTaxes ->
              fun ?itemizedCharges ->
                fun ?invoicingEntity ->
                  fun () ->
                    {
                      currencyCode;
                      newAgreementValue;
                      newAgreementValueAfterTax;
                      expectedCharges;
                      estimatedTaxes;
                      itemizedCharges;
                      invoicingEntity
                    }
    let to_value x =
      structure_to_value
        [("currencyCode",
           (Option.map x.currencyCode ~f:CurrencyCode.to_value));
        ("newAgreementValue",
          (Option.map x.newAgreementValue ~f:BoundedString.to_value));
        ("newAgreementValueAfterTax",
          (Option.map x.newAgreementValueAfterTax ~f:BoundedString.to_value));
        ("expectedCharges",
          (Option.map x.expectedCharges ~f:ExpectedChargeList.to_value));
        ("estimatedTaxes",
          (Option.map x.estimatedTaxes ~f:EstimatedTaxes.to_value));
        ("itemizedCharges",
          (Option.map x.itemizedCharges ~f:ItemizedChargeList.to_value));
        ("invoicingEntity",
          (Option.map x.invoicingEntity ~f:InvoicingEntity.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let invoicingEntity =
        (Option.map ~f:InvoicingEntity.of_xml)
          (Xml.child xml_arg0 "invoicingEntity") in
      let itemizedCharges =
        (Option.map ~f:ItemizedChargeList.of_xml)
          (Xml.child xml_arg0 "itemizedCharges") in
      let estimatedTaxes =
        (Option.map ~f:EstimatedTaxes.of_xml)
          (Xml.child xml_arg0 "estimatedTaxes") in
      let expectedCharges =
        (Option.map ~f:ExpectedChargeList.of_xml)
          (Xml.child xml_arg0 "expectedCharges") in
      let newAgreementValueAfterTax =
        (Option.map ~f:BoundedString.of_xml)
          (Xml.child xml_arg0 "newAgreementValueAfterTax") in
      let newAgreementValue =
        (Option.map ~f:BoundedString.of_xml)
          (Xml.child xml_arg0 "newAgreementValue") in
      let currencyCode =
        (Option.map ~f:CurrencyCode.of_xml)
          (Xml.child xml_arg0 "currencyCode") in
      make ?invoicingEntity ?itemizedCharges ?estimatedTaxes ?expectedCharges
        ?newAgreementValueAfterTax ?newAgreementValue ?currencyCode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let invoicingEntity =
        field_map json__ "invoicingEntity" InvoicingEntity.of_json in
      let itemizedCharges =
        field_map json__ "itemizedCharges" ItemizedChargeList.of_json in
      let estimatedTaxes =
        field_map json__ "estimatedTaxes" EstimatedTaxes.of_json in
      let expectedCharges =
        field_map json__ "expectedCharges" ExpectedChargeList.of_json in
      let newAgreementValueAfterTax =
        field_map json__ "newAgreementValueAfterTax" BoundedString.of_json in
      let newAgreementValue =
        field_map json__ "newAgreementValue" BoundedString.of_json in
      let currencyCode = field_map json__ "currencyCode" CurrencyCode.of_json in
      make ?invoicingEntity ?itemizedCharges ?estimatedTaxes ?expectedCharges
        ?newAgreementValueAfterTax ?newAgreementValue ?currencyCode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The ChargeSummary provides a detailed breakdown of charges that are associated with an agreement request. This is applicable only when a request is created for a PurchaseAgreement. Tax and invoicing fields (such as estimatedTaxes, amountAfterTax, newAgreementValueAfterTax, and invoicingEntity) are returned on a best-effort basis and do not cause the request to fail if unavailable. A null tax amount can have two meanings: Tax estimation was unavailable at the time of the request. The charge timing is BILLING_PERIOD, so the charge amount is not determined at request time. In this case, the tax breakdown may still include the tax rate and type."]
module ServiceQuotaExceededException =
  struct
    type nonrec t =
      {
      requestId: RequestId.t option
        [@ocaml.doc "The unique identifier for the error."];
      message: ExceptionMessage.t option
        [@ocaml.doc "Description of the error."];
      quotaCode: BoundedString.t option
        [@ocaml.doc "The code of the quota that was exceeded."];
      serviceCode: BoundedString.t option
        [@ocaml.doc "The code of the service whose quota was exceeded."];
      resourceType: BoundedString.t option
        [@ocaml.doc "The type of the resource that exceeded the quota."];
      resourceId: ResourceId.t option
        [@ocaml.doc
          "The unique identifier of the resource that exceeded the quota."]}
    let make ?requestId =
      fun ?message ->
        fun ?quotaCode ->
          fun ?serviceCode ->
            fun ?resourceType ->
              fun ?resourceId ->
                fun () ->
                  {
                    requestId;
                    message;
                    quotaCode;
                    serviceCode;
                    resourceType;
                    resourceId
                  }
    let to_value x =
      structure_to_value
        [("requestId", (Option.map x.requestId ~f:RequestId.to_value));
        ("message", (Option.map x.message ~f:ExceptionMessage.to_value));
        ("quotaCode", (Option.map x.quotaCode ~f:BoundedString.to_value));
        ("serviceCode", (Option.map x.serviceCode ~f:BoundedString.to_value));
        ("resourceType",
          (Option.map x.resourceType ~f:BoundedString.to_value));
        ("resourceId", (Option.map x.resourceId ~f:ResourceId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceId =
        (Option.map ~f:ResourceId.of_xml) (Xml.child xml_arg0 "resourceId") in
      let resourceType =
        (Option.map ~f:BoundedString.of_xml)
          (Xml.child xml_arg0 "resourceType") in
      let serviceCode =
        (Option.map ~f:BoundedString.of_xml)
          (Xml.child xml_arg0 "serviceCode") in
      let quotaCode =
        (Option.map ~f:BoundedString.of_xml) (Xml.child xml_arg0 "quotaCode") in
      let message =
        (Option.map ~f:ExceptionMessage.of_xml)
          (Xml.child xml_arg0 "message") in
      let requestId =
        (Option.map ~f:RequestId.of_xml) (Xml.child xml_arg0 "requestId") in
      make ?resourceId ?resourceType ?serviceCode ?quotaCode ?message
        ?requestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let resourceId = field_map json__ "resourceId" ResourceId.of_json in
      let resourceType =
        field_map json__ "resourceType" BoundedString.of_json in
      let serviceCode = field_map json__ "serviceCode" BoundedString.of_json in
      let quotaCode = field_map json__ "quotaCode" BoundedString.of_json in
      let message = field_map json__ "message" ExceptionMessage.of_json in
      let requestId = field_map json__ "requestId" RequestId.of_json in
      make ?resourceId ?resourceType ?serviceCode ?quotaCode ?message
        ?requestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Request exceeded the maximum allowed limit (quota) for a specific resource or API operation."]
module AgreementProposalId =
  struct
    type nonrec t = string
    let context_ = "AgreementProposalId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"(at-|ap-)[A-Za-z0-9]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AgreementProposalId" j
    let to_json = simple_to_json to_value
  end
module Intent =
  struct
    type nonrec t =
      | NEW 
      | AMEND 
      | REPLACE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NEW -> "NEW"
      | AMEND -> "AMEND"
      | REPLACE -> "REPLACE"
      | Non_static_id s -> s
    let of_string =
      function
      | "NEW" -> NEW
      | "AMEND" -> AMEND
      | "REPLACE" -> REPLACE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Intent" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Intent" j)
    let to_json = simple_to_json to_value
  end
module RequestedTermList =
  struct
    type nonrec t = RequestedTerm.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:30) >>= (fun () -> check_list_min i ~min:1));
        i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:RequestedTerm.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RequestedTerm.of_xml)
    let of_json j =
      list_of_json ~kind:"RequestedTermList" ~of_json:RequestedTerm.of_json j
    let to_json v = composed_to_json to_value v
  end
module TaxConfiguration =
  struct
    type nonrec t =
      {
      taxEstimation: TaxEstimation.t option
        [@ocaml.doc
          "Toggle to estimate tax as part of the response. Values include ENABLED and DISABLED. Default is DISABLED."]}
    let make ?taxEstimation = fun () -> { taxEstimation }
    let to_value x =
      structure_to_value
        [("taxEstimation",
           (Option.map x.taxEstimation ~f:TaxEstimation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let taxEstimation =
        (Option.map ~f:TaxEstimation.of_xml)
          (Xml.child xml_arg0 "taxEstimation") in
      make ?taxEstimation ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let taxEstimation =
        field_map json__ "taxEstimation" TaxEstimation.of_json in
      make ?taxEstimation ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Configuration controls for tax estimation in the agreement request."]
module AgreementCancellationRequestCancellationReason =
  struct
    type nonrec t = string
    let context_ = "AgreementCancellationRequestCancellationReason"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2000) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j =
      string_of_json ~kind:"AgreementCancellationRequestCancellationReason" j
    let to_json = simple_to_json to_value
  end
module BatchCreateBillingAdjustmentErrorList =
  struct
    type nonrec t = BatchCreateBillingAdjustmentError.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:BatchCreateBillingAdjustmentError.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:BatchCreateBillingAdjustmentError.of_xml)
    let of_json j =
      list_of_json ~kind:"BatchCreateBillingAdjustmentErrorList"
        ~of_json:BatchCreateBillingAdjustmentError.of_json j
    let to_json v = composed_to_json to_value v
  end
module BatchCreateBillingAdjustmentItemList =
  struct
    type nonrec t = BatchCreateBillingAdjustmentItem.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:BatchCreateBillingAdjustmentItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:BatchCreateBillingAdjustmentItem.of_xml)
    let of_json j =
      list_of_json ~kind:"BatchCreateBillingAdjustmentItemList"
        ~of_json:BatchCreateBillingAdjustmentItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module BatchCreateBillingAdjustmentRequestEntryList =
  struct
    type nonrec t = BatchCreateBillingAdjustmentRequestEntry.t list
    let make i = i
    let of_string _ =
      failwithf "of_string is not implemented for List_shape objects" ()
      [@@warning "-32"]
    let to_value xs =
      (xs |> (List.map ~f:BatchCreateBillingAdjustmentRequestEntry.to_value))
        |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true)))
           ~f:BatchCreateBillingAdjustmentRequestEntry.of_xml)
    let of_json j =
      list_of_json ~kind:"BatchCreateBillingAdjustmentRequestEntryList"
        ~of_json:BatchCreateBillingAdjustmentRequestEntry.of_json j
    let to_json v = composed_to_json to_value v
  end
module UpdatePurchaseOrdersOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows acceptors to associate purchase orders with agreement charges after an agreement is created."]
module UpdatePurchaseOrdersInput =
  struct
    type nonrec t =
      {
      purchaseOrders: PurchaseOrders.t
        [@ocaml.doc
          "Contains information about purchase order associations."]}
    let context_ = "UpdatePurchaseOrdersInput"
    let make ~purchaseOrders = fun () -> { purchaseOrders }
    let to_value x =
      structure_to_value
        [("purchaseOrders",
           (Some (PurchaseOrders.to_value x.purchaseOrders)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let purchaseOrders =
        PurchaseOrders.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "purchaseOrders") in
      make ~purchaseOrders ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let purchaseOrders =
        field_map_exn json__ "purchaseOrders" PurchaseOrders.of_json in
      make ~purchaseOrders ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows acceptors to associate purchase orders with agreement charges after an agreement is created."]
module SendAgreementPaymentRequestOutput =
  struct
    type nonrec t =
      {
      paymentRequestId: PaymentRequestId.t option
        [@ocaml.doc "The unique identifier for the sent payment request."];
      agreementId: AgreementId.t option
        [@ocaml.doc "The agreement identifier for this payment request."];
      status: PaymentRequestStatus.t option
        [@ocaml.doc
          "The current status of the payment request. The initial status is PENDING_APPROVAL."];
      name: PaymentRequestName.t option
        [@ocaml.doc "The descriptive name of the payment request."];
      description: PaymentRequestDescription.t option
        [@ocaml.doc
          "The detailed description of the payment request, if provided."];
      chargeAmount: PositiveAmountUpto8Decimals.t option
        [@ocaml.doc "The amount being charged to the buyer."];
      currencyCode: CurrencyCode.t option
        [@ocaml.doc "The currency code for the charge amount (e.g., USD)."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The time when the payment request was created."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?paymentRequestId =
      fun ?agreementId ->
        fun ?status ->
          fun ?name ->
            fun ?description ->
              fun ?chargeAmount ->
                fun ?currencyCode ->
                  fun ?createdAt ->
                    fun () ->
                      {
                        paymentRequestId;
                        agreementId;
                        status;
                        name;
                        description;
                        chargeAmount;
                        currencyCode;
                        createdAt
                      }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("paymentRequestId",
           (Option.map x.paymentRequestId ~f:PaymentRequestId.to_value));
        ("agreementId", (Option.map x.agreementId ~f:AgreementId.to_value));
        ("status", (Option.map x.status ~f:PaymentRequestStatus.to_value));
        ("name", (Option.map x.name ~f:PaymentRequestName.to_value));
        ("description",
          (Option.map x.description ~f:PaymentRequestDescription.to_value));
        ("chargeAmount",
          (Option.map x.chargeAmount ~f:PositiveAmountUpto8Decimals.to_value));
        ("currencyCode",
          (Option.map x.currencyCode ~f:CurrencyCode.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let currencyCode =
        (Option.map ~f:CurrencyCode.of_xml)
          (Xml.child xml_arg0 "currencyCode") in
      let chargeAmount =
        (Option.map ~f:PositiveAmountUpto8Decimals.of_xml)
          (Xml.child xml_arg0 "chargeAmount") in
      let description =
        (Option.map ~f:PaymentRequestDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        (Option.map ~f:PaymentRequestName.of_xml) (Xml.child xml_arg0 "name") in
      let status =
        (Option.map ~f:PaymentRequestStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let agreementId =
        (Option.map ~f:AgreementId.of_xml) (Xml.child xml_arg0 "agreementId") in
      let paymentRequestId =
        (Option.map ~f:PaymentRequestId.of_xml)
          (Xml.child xml_arg0 "paymentRequestId") in
      make ?createdAt ?currencyCode ?chargeAmount ?description ?name ?status
        ?agreementId ?paymentRequestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let createdAt = field_map json__ "createdAt" Timestamp.of_json in
      let currencyCode = field_map json__ "currencyCode" CurrencyCode.of_json in
      let chargeAmount =
        field_map json__ "chargeAmount" PositiveAmountUpto8Decimals.of_json in
      let description =
        field_map json__ "description" PaymentRequestDescription.of_json in
      let name = field_map json__ "name" PaymentRequestName.of_json in
      let status = field_map json__ "status" PaymentRequestStatus.of_json in
      let agreementId = field_map json__ "agreementId" AgreementId.of_json in
      let paymentRequestId =
        field_map json__ "paymentRequestId" PaymentRequestId.of_json in
      make ?createdAt ?currencyCode ?chargeAmount ?description ?name ?status
        ?agreementId ?paymentRequestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows sellers (proposers) to submit a payment request to buyers (acceptors) for a specific charge amount for an agreement that includes a VariablePaymentTerm. The payment request is created in PENDING_APPROVAL status, at which point the buyer can accept or reject it. The agreement must be active and have a VariablePaymentTerm to support payment requests. The chargeAmount must not exceed the remaining available balance under the VariablePaymentTerm maxTotalChargeAmount."]
module SendAgreementPaymentRequestInput =
  struct
    type nonrec t =
      {
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique, case-sensitive identifier that you provide to ensure the idempotency of the request."];
      agreementId: AgreementId.t
        [@ocaml.doc
          "The unique identifier of the agreement for which the payment request is being submitted. Use GetAgreementTerms to retrieve agreement term details."];
      termId: TermId.t
        [@ocaml.doc
          "The unique identifier of the VariablePaymentTerm for the agreement that the payment request is being sent for."];
      name: PaymentRequestName.t
        [@ocaml.doc
          "A descriptive name for the payment request (5-64 characters)."];
      chargeAmount: PositiveAmountUpto8Decimals.t
        [@ocaml.doc
          "The amount requested to be charged to the buyer, positive decimal value in the currency of the accepted term. A ValidationException is returned if the chargeAmount exceeds the available balance, if the agreement doesn't have an active VariablePaymentTerm, or if the termId is invalid."];
      description: PaymentRequestDescription.t option
        [@ocaml.doc
          "An optional detailed description of the payment request (1-2000 characters)."]}
    let context_ = "SendAgreementPaymentRequestInput"
    let make ?clientToken =
      fun ?description ->
        fun ~agreementId ->
          fun ~termId ->
            fun ~name ->
              fun ~chargeAmount ->
                fun () ->
                  {
                    clientToken;
                    description;
                    agreementId;
                    termId;
                    name;
                    chargeAmount
                  }
    let to_value x =
      structure_to_value
        [("clientToken", (Option.map x.clientToken ~f:ClientToken.to_value));
        ("agreementId", (Some (AgreementId.to_value x.agreementId)));
        ("termId", (Some (TermId.to_value x.termId)));
        ("name", (Some (PaymentRequestName.to_value x.name)));
        ("chargeAmount",
          (Some (PositiveAmountUpto8Decimals.to_value x.chargeAmount)));
        ("description",
          (Option.map x.description ~f:PaymentRequestDescription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let description =
        (Option.map ~f:PaymentRequestDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let chargeAmount =
        PositiveAmountUpto8Decimals.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "chargeAmount") in
      let name =
        PaymentRequestName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "name") in
      let termId =
        TermId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "termId") in
      let agreementId =
        AgreementId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "agreementId") in
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "clientToken") in
      make ?description ~chargeAmount ~name ~termId ~agreementId ?clientToken
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let description =
        field_map json__ "description" PaymentRequestDescription.of_json in
      let chargeAmount =
        field_map_exn json__ "chargeAmount"
          PositiveAmountUpto8Decimals.of_json in
      let name = field_map_exn json__ "name" PaymentRequestName.of_json in
      let termId = field_map_exn json__ "termId" TermId.of_json in
      let agreementId =
        field_map_exn json__ "agreementId" AgreementId.of_json in
      let clientToken = field_map json__ "clientToken" ClientToken.of_json in
      make ?description ~chargeAmount ~name ~termId ~agreementId ?clientToken
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows sellers (proposers) to submit a payment request to buyers (acceptors) for a specific charge amount for an agreement that includes a VariablePaymentTerm. The payment request is created in PENDING_APPROVAL status, at which point the buyer can accept or reject it. The agreement must be active and have a VariablePaymentTerm to support payment requests. The chargeAmount must not exceed the remaining available balance under the VariablePaymentTerm maxTotalChargeAmount."]
module SendAgreementCancellationRequestOutput =
  struct
    type nonrec t =
      {
      agreementId: AgreementId.t option
        [@ocaml.doc "The unique identifier of the agreement."];
      agreementCancellationRequestId: AgreementCancellationRequestId.t option
        [@ocaml.doc
          "The unique identifier for the created cancellation request."];
      status: AgreementCancellationRequestStatus.t option
        [@ocaml.doc
          "The current status of the cancellation request. The initial status is PENDING_APPROVAL."];
      reasonCode: AgreementCancellationRequestReasonCode.t option
        [@ocaml.doc "The reason code provided for the cancellation."];
      description: AgreementCancellationRequestDescription.t option
        [@ocaml.doc
          "The detailed description of the cancellation reason, if provided."];
      createdAt: Timestamp.t option
        [@ocaml.doc "The time when the cancellation request was created."];
      updatedAt: Timestamp.t option
        [@ocaml.doc
          "The time when the cancellation request was last updated."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?agreementId =
      fun ?agreementCancellationRequestId ->
        fun ?status ->
          fun ?reasonCode ->
            fun ?description ->
              fun ?createdAt ->
                fun ?updatedAt ->
                  fun () ->
                    {
                      agreementId;
                      agreementCancellationRequestId;
                      status;
                      reasonCode;
                      description;
                      createdAt;
                      updatedAt
                    }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("agreementId", (Option.map x.agreementId ~f:AgreementId.to_value));
        ("agreementCancellationRequestId",
          (Option.map x.agreementCancellationRequestId
             ~f:AgreementCancellationRequestId.to_value));
        ("status",
          (Option.map x.status ~f:AgreementCancellationRequestStatus.to_value));
        ("reasonCode",
          (Option.map x.reasonCode
             ~f:AgreementCancellationRequestReasonCode.to_value));
        ("description",
          (Option.map x.description
             ~f:AgreementCancellationRequestDescription.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("updatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "updatedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let description =
        (Option.map ~f:AgreementCancellationRequestDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let reasonCode =
        (Option.map ~f:AgreementCancellationRequestReasonCode.of_xml)
          (Xml.child xml_arg0 "reasonCode") in
      let status =
        (Option.map ~f:AgreementCancellationRequestStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let agreementCancellationRequestId =
        (Option.map ~f:AgreementCancellationRequestId.of_xml)
          (Xml.child xml_arg0 "agreementCancellationRequestId") in
      let agreementId =
        (Option.map ~f:AgreementId.of_xml) (Xml.child xml_arg0 "agreementId") in
      make ?updatedAt ?createdAt ?description ?reasonCode ?status
        ?agreementCancellationRequestId ?agreementId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt = field_map json__ "updatedAt" Timestamp.of_json in
      let createdAt = field_map json__ "createdAt" Timestamp.of_json in
      let description =
        field_map json__ "description"
          AgreementCancellationRequestDescription.of_json in
      let reasonCode =
        field_map json__ "reasonCode"
          AgreementCancellationRequestReasonCode.of_json in
      let status =
        field_map json__ "status" AgreementCancellationRequestStatus.of_json in
      let agreementCancellationRequestId =
        field_map json__ "agreementCancellationRequestId"
          AgreementCancellationRequestId.of_json in
      let agreementId = field_map json__ "agreementId" AgreementId.of_json in
      make ?updatedAt ?createdAt ?description ?reasonCode ?status
        ?agreementCancellationRequestId ?agreementId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows sellers (proposers) to submit a cancellation request for an active agreement. The cancellation request is created in PENDING_APPROVAL status, at which point the buyer can review it."]
module SendAgreementCancellationRequestInput =
  struct
    type nonrec t =
      {
      agreementId: AgreementId.t
        [@ocaml.doc
          "The unique identifier of the agreement for which the cancellation request is being submitted."];
      reasonCode: AgreementCancellationRequestReasonCode.t
        [@ocaml.doc "The reason code for the cancellation request."];
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique, case-sensitive identifier that you provide to ensure the idempotency of the request."];
      description: AgreementCancellationRequestDescription.t option
        [@ocaml.doc
          "An optional detailed description of the cancellation reason (1-2000 characters)."]}
    let context_ = "SendAgreementCancellationRequestInput"
    let make ?clientToken =
      fun ?description ->
        fun ~agreementId ->
          fun ~reasonCode ->
            fun () -> { clientToken; description; agreementId; reasonCode }
    let to_value x =
      structure_to_value
        [("agreementId", (Some (AgreementId.to_value x.agreementId)));
        ("reasonCode",
          (Some
             (AgreementCancellationRequestReasonCode.to_value x.reasonCode)));
        ("clientToken", (Option.map x.clientToken ~f:ClientToken.to_value));
        ("description",
          (Option.map x.description
             ~f:AgreementCancellationRequestDescription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let description =
        (Option.map ~f:AgreementCancellationRequestDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "clientToken") in
      let reasonCode =
        AgreementCancellationRequestReasonCode.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "reasonCode") in
      let agreementId =
        AgreementId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "agreementId") in
      make ?description ?clientToken ~reasonCode ~agreementId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let description =
        field_map json__ "description"
          AgreementCancellationRequestDescription.of_json in
      let clientToken = field_map json__ "clientToken" ClientToken.of_json in
      let reasonCode =
        field_map_exn json__ "reasonCode"
          AgreementCancellationRequestReasonCode.of_json in
      let agreementId =
        field_map_exn json__ "agreementId" AgreementId.of_json in
      make ?description ?clientToken ~reasonCode ~agreementId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows sellers (proposers) to submit a cancellation request for an active agreement. The cancellation request is created in PENDING_APPROVAL status, at which point the buyer can review it."]
module SearchAgreementsOutput =
  struct
    type nonrec t =
      {
      agreementViewSummaries: AgreementViewSummaryList.t option
        [@ocaml.doc
          "A summary of the agreement, including top-level attributes (for example, the agreement ID, proposer, and acceptor)."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token used for pagination. The field is null if there are no more results."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?agreementViewSummaries =
      fun ?nextToken -> fun () -> { agreementViewSummaries; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("agreementViewSummaries",
           (Option.map x.agreementViewSummaries
              ~f:AgreementViewSummaryList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let agreementViewSummaries =
        (Option.map ~f:AgreementViewSummaryList.of_xml)
          (Xml.child xml_arg0 "agreementViewSummaries") in
      make ?nextToken ?agreementViewSummaries ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let agreementViewSummaries =
        field_map json__ "agreementViewSummaries"
          AgreementViewSummaryList.of_json in
      make ?nextToken ?agreementViewSummaries ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Searches across all agreements that a proposer or an acceptor has in AWS Marketplace. The search returns a list of agreements with basic agreement information. The following filter combinations are supported when the PartyType is Proposer: AgreementType AgreementType + EndTime AgreementType + ResourceType AgreementType + ResourceType + EndTime AgreementType + ResourceType + Status AgreementType + ResourceType + Status + EndTime AgreementType + ResourceIdentifier AgreementType + ResourceIdentifier + EndTime AgreementType + ResourceIdentifier + Status AgreementType + ResourceIdentifier + Status + EndTime AgreementType + AcceptorAccountId AgreementType + AcceptorAccountId + EndTime AgreementType + AcceptorAccountId + Status AgreementType + AcceptorAccountId + Status + EndTime AgreementType + AcceptorAccountId + OfferId AgreementType + AcceptorAccountId + OfferId + Status AgreementType + AcceptorAccountId + OfferId + EndTime AgreementType + AcceptorAccountId + OfferId + Status + EndTime AgreementType + AcceptorAccountId + ResourceIdentifier AgreementType + AcceptorAccountId + ResourceIdentifier + Status AgreementType + AcceptorAccountId + ResourceIdentifier + EndTime AgreementType + AcceptorAccountId + ResourceIdentifier + Status + EndTime AgreementType + AcceptorAccountId + ResourceType AgreementType + AcceptorAccountId + ResourceType + EndTime AgreementType + AcceptorAccountId + ResourceType + Status AgreementType + AcceptorAccountId + ResourceType + Status + EndTime AgreementType + Status AgreementType + Status + EndTime AgreementType + OfferId AgreementType + OfferId + EndTime AgreementType + OfferId + Status AgreementType + OfferId + Status + EndTime AgreementType + OfferSetId AgreementType + OfferSetId + EndTime AgreementType + OfferSetId + Status AgreementType + OfferSetId + Status + EndTime To filter by EndTime, you can use BeforeEndTime and/or AfterEndTime. Only EndTime is supported for sorting. The following filter combinations are supported when the PartyType is Acceptor: AgreementType AgreementType + Status AgreementType + EndTime AgreementType + Status + EndTime AgreementType + ResourceIdentifier AgreementType + ResourceIdentifier + EndTime AgreementType + ResourceIdentifier + Status AgreementType + ResourceIdentifier + Status + EndTime AgreementType + ResourceType AgreementType + ResourceType + EndTime AgreementType + OfferId AgreementType + OfferId + EndTime AgreementType + OfferId + Status AgreementType + OfferId + Status + EndTime AgreementType + OfferSetId AgreementType + OfferSetId + EndTime AgreementType + OfferSetId + Status AgreementType + OfferSetId + Status + EndTime"]
module SearchAgreementsInput =
  struct
    type nonrec t =
      {
      catalog: Catalog.t option
        [@ocaml.doc "The catalog in which the agreement was created."];
      filters: FilterList.t option
        [@ocaml.doc
          "The filter name and value pair used to return a specific list of results. The following filters are supported: ResourceIdentifier \226\128\147 The unique identifier of the resource. ResourceType \226\128\147 Type of the resource, which is the product (AmiProduct, ContainerProduct, SaaSProduct, ProfessionalServicesProduct, or MachineLearningProduct). PartyType \226\128\147 The party type of the caller. Use Proposer or Acceptor. AcceptorAccountId \226\128\147 The AWS account ID of the party accepting the agreement terms. OfferId \226\128\147 The unique identifier of the offer in which the terms are registered in the agreement token. Status \226\128\147 The current status of the agreement. Values include ACTIVE, ARCHIVED, CANCELLED, EXPIRED, RENEWED, REPLACED, and TERMINATED. BeforeEndTime \226\128\147 A date used to filter agreements with a date before the endTime of an agreement. AfterEndTime \226\128\147 A date used to filter agreements with a date after the endTime of an agreement. AgreementType \226\128\147 The type of agreement. Supported value includes PurchaseAgreement. OfferSetId \226\128\147 A unique identifier for the offer set containing this offer. All agreements created from offers in this set include this identifier as context."];
      sort: Sort.t option
        [@ocaml.doc
          "An object that contains the SortBy and SortOrder attributes. Only EndTime is supported for SearchAgreements. The default sort is EndTime descending."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of agreements to return in the response."];
      nextToken: NextToken.t option
        [@ocaml.doc "A token to specify where to start pagination."]}
    let make ?catalog =
      fun ?filters ->
        fun ?sort ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () -> { catalog; filters; sort; maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("catalog", (Option.map x.catalog ~f:Catalog.to_value));
        ("filters", (Option.map x.filters ~f:FilterList.to_value));
        ("sort", (Option.map x.sort ~f:Sort.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let sort = (Option.map ~f:Sort.of_xml) (Xml.child xml_arg0 "sort") in
      let filters =
        (Option.map ~f:FilterList.of_xml) (Xml.child xml_arg0 "filters") in
      let catalog =
        (Option.map ~f:Catalog.of_xml) (Xml.child xml_arg0 "catalog") in
      make ?nextToken ?maxResults ?sort ?filters ?catalog ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      let sort = field_map json__ "sort" Sort.of_json in
      let filters = field_map json__ "filters" FilterList.of_json in
      let catalog = field_map json__ "catalog" Catalog.of_json in
      make ?nextToken ?maxResults ?sort ?filters ?catalog ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Searches across all agreements that a proposer or an acceptor has in AWS Marketplace. The search returns a list of agreements with basic agreement information. The following filter combinations are supported when the PartyType is Proposer: AgreementType AgreementType + EndTime AgreementType + ResourceType AgreementType + ResourceType + EndTime AgreementType + ResourceType + Status AgreementType + ResourceType + Status + EndTime AgreementType + ResourceIdentifier AgreementType + ResourceIdentifier + EndTime AgreementType + ResourceIdentifier + Status AgreementType + ResourceIdentifier + Status + EndTime AgreementType + AcceptorAccountId AgreementType + AcceptorAccountId + EndTime AgreementType + AcceptorAccountId + Status AgreementType + AcceptorAccountId + Status + EndTime AgreementType + AcceptorAccountId + OfferId AgreementType + AcceptorAccountId + OfferId + Status AgreementType + AcceptorAccountId + OfferId + EndTime AgreementType + AcceptorAccountId + OfferId + Status + EndTime AgreementType + AcceptorAccountId + ResourceIdentifier AgreementType + AcceptorAccountId + ResourceIdentifier + Status AgreementType + AcceptorAccountId + ResourceIdentifier + EndTime AgreementType + AcceptorAccountId + ResourceIdentifier + Status + EndTime AgreementType + AcceptorAccountId + ResourceType AgreementType + AcceptorAccountId + ResourceType + EndTime AgreementType + AcceptorAccountId + ResourceType + Status AgreementType + AcceptorAccountId + ResourceType + Status + EndTime AgreementType + Status AgreementType + Status + EndTime AgreementType + OfferId AgreementType + OfferId + EndTime AgreementType + OfferId + Status AgreementType + OfferId + Status + EndTime AgreementType + OfferSetId AgreementType + OfferSetId + EndTime AgreementType + OfferSetId + Status AgreementType + OfferSetId + Status + EndTime To filter by EndTime, you can use BeforeEndTime and/or AfterEndTime. Only EndTime is supported for sorting. The following filter combinations are supported when the PartyType is Acceptor: AgreementType AgreementType + Status AgreementType + EndTime AgreementType + Status + EndTime AgreementType + ResourceIdentifier AgreementType + ResourceIdentifier + EndTime AgreementType + ResourceIdentifier + Status AgreementType + ResourceIdentifier + Status + EndTime AgreementType + ResourceType AgreementType + ResourceType + EndTime AgreementType + OfferId AgreementType + OfferId + EndTime AgreementType + OfferId + Status AgreementType + OfferId + Status + EndTime AgreementType + OfferSetId AgreementType + OfferSetId + EndTime AgreementType + OfferSetId + Status AgreementType + OfferSetId + Status + EndTime"]
module RejectAgreementPaymentRequestOutput =
  struct
    type nonrec t =
      {
      paymentRequestId: PaymentRequestId.t option
        [@ocaml.doc "The unique identifier of the rejected payment request."];
      agreementId: AgreementId.t option
        [@ocaml.doc
          "The unique identifier of the agreement associated with this payment request."];
      status: PaymentRequestStatus.t option
        [@ocaml.doc
          "The updated status of the payment request, which is REJECTED."];
      statusMessage: PaymentRequestStatusMessage.t option
        [@ocaml.doc "The rejection reason provided by the buyer, if any."];
      name: PaymentRequestName.t option
        [@ocaml.doc "The descriptive name of the payment request."];
      description: PaymentRequestDescription.t option
        [@ocaml.doc
          "The detailed description of the payment request, if provided."];
      chargeAmount: PositiveAmountUpto8Decimals.t option
        [@ocaml.doc "The amount that was requested to be charged."];
      currencyCode: CurrencyCode.t option
        [@ocaml.doc "The currency code for the charge amount."];
      createdAt: Timestamp.t option
        [@ocaml.doc
          "The date and time when the payment request was originally created."];
      updatedAt: Timestamp.t option
        [@ocaml.doc
          "The date and time when the payment request was rejected."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?paymentRequestId =
      fun ?agreementId ->
        fun ?status ->
          fun ?statusMessage ->
            fun ?name ->
              fun ?description ->
                fun ?chargeAmount ->
                  fun ?currencyCode ->
                    fun ?createdAt ->
                      fun ?updatedAt ->
                        fun () ->
                          {
                            paymentRequestId;
                            agreementId;
                            status;
                            statusMessage;
                            name;
                            description;
                            chargeAmount;
                            currencyCode;
                            createdAt;
                            updatedAt
                          }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("paymentRequestId",
           (Option.map x.paymentRequestId ~f:PaymentRequestId.to_value));
        ("agreementId", (Option.map x.agreementId ~f:AgreementId.to_value));
        ("status", (Option.map x.status ~f:PaymentRequestStatus.to_value));
        ("statusMessage",
          (Option.map x.statusMessage ~f:PaymentRequestStatusMessage.to_value));
        ("name", (Option.map x.name ~f:PaymentRequestName.to_value));
        ("description",
          (Option.map x.description ~f:PaymentRequestDescription.to_value));
        ("chargeAmount",
          (Option.map x.chargeAmount ~f:PositiveAmountUpto8Decimals.to_value));
        ("currencyCode",
          (Option.map x.currencyCode ~f:CurrencyCode.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("updatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "updatedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let currencyCode =
        (Option.map ~f:CurrencyCode.of_xml)
          (Xml.child xml_arg0 "currencyCode") in
      let chargeAmount =
        (Option.map ~f:PositiveAmountUpto8Decimals.of_xml)
          (Xml.child xml_arg0 "chargeAmount") in
      let description =
        (Option.map ~f:PaymentRequestDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        (Option.map ~f:PaymentRequestName.of_xml) (Xml.child xml_arg0 "name") in
      let statusMessage =
        (Option.map ~f:PaymentRequestStatusMessage.of_xml)
          (Xml.child xml_arg0 "statusMessage") in
      let status =
        (Option.map ~f:PaymentRequestStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let agreementId =
        (Option.map ~f:AgreementId.of_xml) (Xml.child xml_arg0 "agreementId") in
      let paymentRequestId =
        (Option.map ~f:PaymentRequestId.of_xml)
          (Xml.child xml_arg0 "paymentRequestId") in
      make ?updatedAt ?createdAt ?currencyCode ?chargeAmount ?description
        ?name ?statusMessage ?status ?agreementId ?paymentRequestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt = field_map json__ "updatedAt" Timestamp.of_json in
      let createdAt = field_map json__ "createdAt" Timestamp.of_json in
      let currencyCode = field_map json__ "currencyCode" CurrencyCode.of_json in
      let chargeAmount =
        field_map json__ "chargeAmount" PositiveAmountUpto8Decimals.of_json in
      let description =
        field_map json__ "description" PaymentRequestDescription.of_json in
      let name = field_map json__ "name" PaymentRequestName.of_json in
      let statusMessage =
        field_map json__ "statusMessage" PaymentRequestStatusMessage.of_json in
      let status = field_map json__ "status" PaymentRequestStatus.of_json in
      let agreementId = field_map json__ "agreementId" AgreementId.of_json in
      let paymentRequestId =
        field_map json__ "paymentRequestId" PaymentRequestId.of_json in
      make ?updatedAt ?createdAt ?currencyCode ?chargeAmount ?description
        ?name ?statusMessage ?status ?agreementId ?paymentRequestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows buyers (acceptors) to reject a payment request that is in PENDING_APPROVAL status. Once rejected, the payment request transitions to REJECTED status and cannot be accepted. Buyers can optionally provide a reason for the rejection. Only payment requests in PENDING_APPROVAL status can be rejected. A ConflictException is thrown if the payment request is in any other status."]
module RejectAgreementPaymentRequestInput =
  struct
    type nonrec t =
      {
      paymentRequestId: PaymentRequestId.t
        [@ocaml.doc
          "The unique identifier of the payment request to reject."];
      agreementId: AgreementId.t
        [@ocaml.doc
          "The unique identifier of the agreement associated with the payment request."];
      rejectionReason: PaymentRequestRejectionReason.t option
        [@ocaml.doc
          "An optional reason for rejecting the payment request (1-250 characters). This message is visible to the seller."]}
    let context_ = "RejectAgreementPaymentRequestInput"
    let make ?rejectionReason =
      fun ~paymentRequestId ->
        fun ~agreementId ->
          fun () -> { rejectionReason; paymentRequestId; agreementId }
    let to_value x =
      structure_to_value
        [("paymentRequestId",
           (Some (PaymentRequestId.to_value x.paymentRequestId)));
        ("agreementId", (Some (AgreementId.to_value x.agreementId)));
        ("rejectionReason",
          (Option.map x.rejectionReason
             ~f:PaymentRequestRejectionReason.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let rejectionReason =
        (Option.map ~f:PaymentRequestRejectionReason.of_xml)
          (Xml.child xml_arg0 "rejectionReason") in
      let agreementId =
        AgreementId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "agreementId") in
      let paymentRequestId =
        PaymentRequestId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "paymentRequestId") in
      make ?rejectionReason ~agreementId ~paymentRequestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let rejectionReason =
        field_map json__ "rejectionReason"
          PaymentRequestRejectionReason.of_json in
      let agreementId =
        field_map_exn json__ "agreementId" AgreementId.of_json in
      let paymentRequestId =
        field_map_exn json__ "paymentRequestId" PaymentRequestId.of_json in
      make ?rejectionReason ~agreementId ~paymentRequestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows buyers (acceptors) to reject a payment request that is in PENDING_APPROVAL status. Once rejected, the payment request transitions to REJECTED status and cannot be accepted. Buyers can optionally provide a reason for the rejection. Only payment requests in PENDING_APPROVAL status can be rejected. A ConflictException is thrown if the payment request is in any other status."]
module RejectAgreementCancellationRequestOutput =
  struct
    type nonrec t =
      {
      agreementId: AgreementId.t option
        [@ocaml.doc
          "The unique identifier of the agreement associated with this cancellation request."];
      agreementCancellationRequestId: AgreementCancellationRequestId.t option
        [@ocaml.doc
          "The unique identifier of the rejected cancellation request."];
      status: AgreementCancellationRequestStatus.t option
        [@ocaml.doc
          "The updated status of the cancellation request, which is REJECTED."];
      statusMessage: AgreementCancellationRequestStatusMessage.t option
        [@ocaml.doc "The rejection reason provided by the buyer."];
      reasonCode: AgreementCancellationRequestReasonCode.t option
        [@ocaml.doc
          "The original reason code provided when the cancellation request was created."];
      description: AgreementCancellationRequestDescription.t option
        [@ocaml.doc
          "The detailed description of the cancellation reason, if provided."];
      createdAt: Timestamp.t option
        [@ocaml.doc
          "The date and time when the cancellation request was originally created."];
      updatedAt: Timestamp.t option
        [@ocaml.doc
          "The date and time when the cancellation request was rejected."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?agreementId =
      fun ?agreementCancellationRequestId ->
        fun ?status ->
          fun ?statusMessage ->
            fun ?reasonCode ->
              fun ?description ->
                fun ?createdAt ->
                  fun ?updatedAt ->
                    fun () ->
                      {
                        agreementId;
                        agreementCancellationRequestId;
                        status;
                        statusMessage;
                        reasonCode;
                        description;
                        createdAt;
                        updatedAt
                      }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("agreementId", (Option.map x.agreementId ~f:AgreementId.to_value));
        ("agreementCancellationRequestId",
          (Option.map x.agreementCancellationRequestId
             ~f:AgreementCancellationRequestId.to_value));
        ("status",
          (Option.map x.status ~f:AgreementCancellationRequestStatus.to_value));
        ("statusMessage",
          (Option.map x.statusMessage
             ~f:AgreementCancellationRequestStatusMessage.to_value));
        ("reasonCode",
          (Option.map x.reasonCode
             ~f:AgreementCancellationRequestReasonCode.to_value));
        ("description",
          (Option.map x.description
             ~f:AgreementCancellationRequestDescription.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("updatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "updatedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let description =
        (Option.map ~f:AgreementCancellationRequestDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let reasonCode =
        (Option.map ~f:AgreementCancellationRequestReasonCode.of_xml)
          (Xml.child xml_arg0 "reasonCode") in
      let statusMessage =
        (Option.map ~f:AgreementCancellationRequestStatusMessage.of_xml)
          (Xml.child xml_arg0 "statusMessage") in
      let status =
        (Option.map ~f:AgreementCancellationRequestStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let agreementCancellationRequestId =
        (Option.map ~f:AgreementCancellationRequestId.of_xml)
          (Xml.child xml_arg0 "agreementCancellationRequestId") in
      let agreementId =
        (Option.map ~f:AgreementId.of_xml) (Xml.child xml_arg0 "agreementId") in
      make ?updatedAt ?createdAt ?description ?reasonCode ?statusMessage
        ?status ?agreementCancellationRequestId ?agreementId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt = field_map json__ "updatedAt" Timestamp.of_json in
      let createdAt = field_map json__ "createdAt" Timestamp.of_json in
      let description =
        field_map json__ "description"
          AgreementCancellationRequestDescription.of_json in
      let reasonCode =
        field_map json__ "reasonCode"
          AgreementCancellationRequestReasonCode.of_json in
      let statusMessage =
        field_map json__ "statusMessage"
          AgreementCancellationRequestStatusMessage.of_json in
      let status =
        field_map json__ "status" AgreementCancellationRequestStatus.of_json in
      let agreementCancellationRequestId =
        field_map json__ "agreementCancellationRequestId"
          AgreementCancellationRequestId.of_json in
      let agreementId = field_map json__ "agreementId" AgreementId.of_json in
      make ?updatedAt ?createdAt ?description ?reasonCode ?statusMessage
        ?status ?agreementCancellationRequestId ?agreementId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows buyers (acceptors) to reject a cancellation request that is in PENDING_APPROVAL status. Once rejected, the cancellation request transitions to REJECTED status and the agreement remains active. Buyers must provide a reason for the rejection. Only cancellation requests in PENDING_APPROVAL status can be rejected. A ConflictException is thrown if the cancellation request is in any other status."]
module RejectAgreementCancellationRequestInput =
  struct
    type nonrec t =
      {
      agreementId: AgreementId.t
        [@ocaml.doc
          "The unique identifier of the agreement associated with the cancellation request."];
      agreementCancellationRequestId: AgreementCancellationRequestId.t
        [@ocaml.doc
          "The unique identifier of the cancellation request to reject."];
      rejectionReason: AgreementCancellationRequestRejectionReason.t
        [@ocaml.doc
          "The reason for rejecting the cancellation request (1-2000 characters). This message is visible to the seller."]}
    let context_ = "RejectAgreementCancellationRequestInput"
    let make ~agreementId =
      fun ~agreementCancellationRequestId ->
        fun ~rejectionReason ->
          fun () ->
            { agreementId; agreementCancellationRequestId; rejectionReason }
    let to_value x =
      structure_to_value
        [("agreementId", (Some (AgreementId.to_value x.agreementId)));
        ("agreementCancellationRequestId",
          (Some
             (AgreementCancellationRequestId.to_value
                x.agreementCancellationRequestId)));
        ("rejectionReason",
          (Some
             (AgreementCancellationRequestRejectionReason.to_value
                x.rejectionReason)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let rejectionReason =
        AgreementCancellationRequestRejectionReason.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "rejectionReason") in
      let agreementCancellationRequestId =
        AgreementCancellationRequestId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "agreementCancellationRequestId") in
      let agreementId =
        AgreementId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "agreementId") in
      make ~rejectionReason ~agreementCancellationRequestId ~agreementId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let rejectionReason =
        field_map_exn json__ "rejectionReason"
          AgreementCancellationRequestRejectionReason.of_json in
      let agreementCancellationRequestId =
        field_map_exn json__ "agreementCancellationRequestId"
          AgreementCancellationRequestId.of_json in
      let agreementId =
        field_map_exn json__ "agreementId" AgreementId.of_json in
      make ~rejectionReason ~agreementCancellationRequestId ~agreementId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows buyers (acceptors) to reject a cancellation request that is in PENDING_APPROVAL status. Once rejected, the cancellation request transitions to REJECTED status and the agreement remains active. Buyers must provide a reason for the rejection. Only cancellation requests in PENDING_APPROVAL status can be rejected. A ConflictException is thrown if the cancellation request is in any other status."]
module ListBillingAdjustmentRequestsOutput =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token used for pagination. The field is null if there are no more results."];
      items: BillingAdjustmentSummaryList.t option
        [@ocaml.doc
          "An array of BillingAdjustmentSummary objects containing summary information about each billing adjustment request."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken = fun ?items -> fun () -> { nextToken; items }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("items",
          (Option.map x.items ~f:BillingAdjustmentSummaryList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let items =
        (Option.map ~f:BillingAdjustmentSummaryList.of_xml)
          (Xml.child xml_arg0 "items") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?items ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let items =
        field_map json__ "items" BillingAdjustmentSummaryList.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      make ?items ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists billing adjustment requests for a specific agreement. Sellers (proposers) can use this operation to view all billing adjustment requests associated with an agreement."]
module ListBillingAdjustmentRequestsInput =
  struct
    type nonrec t =
      {
      agreementId: AgreementId.t option
        [@ocaml.doc
          "The unique identifier of the agreement to list billing adjustment requests for."];
      status: BillingAdjustmentStatus.t option
        [@ocaml.doc
          "An optional filter to return billing adjustment requests with the specified status."];
      createdAfter: Timestamp.t option
        [@ocaml.doc
          "An optional filter to return billing adjustment requests created after the specified timestamp."];
      createdBefore: Timestamp.t option
        [@ocaml.doc
          "An optional filter to return billing adjustment requests created before the specified timestamp."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of billing adjustment requests to return in the response."];
      catalog: Catalog.t option
        [@ocaml.doc
          "An optional filter to return billing adjustment requests by catalog (e.g., AWSMarketplace)."];
      agreementType: AgreementType.t option
        [@ocaml.doc
          "An optional filter to return billing adjustment requests by agreement type (e.g., PurchaseAgreement)."];
      nextToken: NextToken.t option
        [@ocaml.doc "A token to specify where to start pagination."]}
    let make ?agreementId =
      fun ?status ->
        fun ?createdAfter ->
          fun ?createdBefore ->
            fun ?maxResults ->
              fun ?catalog ->
                fun ?agreementType ->
                  fun ?nextToken ->
                    fun () ->
                      {
                        agreementId;
                        status;
                        createdAfter;
                        createdBefore;
                        maxResults;
                        catalog;
                        agreementType;
                        nextToken
                      }
    let to_value x =
      structure_to_value
        [("agreementId", (Option.map x.agreementId ~f:AgreementId.to_value));
        ("status", (Option.map x.status ~f:BillingAdjustmentStatus.to_value));
        ("createdAfter", (Option.map x.createdAfter ~f:Timestamp.to_value));
        ("createdBefore", (Option.map x.createdBefore ~f:Timestamp.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("catalog", (Option.map x.catalog ~f:Catalog.to_value));
        ("agreementType",
          (Option.map x.agreementType ~f:AgreementType.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let agreementType =
        (Option.map ~f:AgreementType.of_xml)
          (Xml.child xml_arg0 "agreementType") in
      let catalog =
        (Option.map ~f:Catalog.of_xml) (Xml.child xml_arg0 "catalog") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let createdBefore =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdBefore") in
      let createdAfter =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAfter") in
      let status =
        (Option.map ~f:BillingAdjustmentStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let agreementId =
        (Option.map ~f:AgreementId.of_xml) (Xml.child xml_arg0 "agreementId") in
      make ?nextToken ?agreementType ?catalog ?maxResults ?createdBefore
        ?createdAfter ?status ?agreementId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let agreementType =
        field_map json__ "agreementType" AgreementType.of_json in
      let catalog = field_map json__ "catalog" Catalog.of_json in
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      let createdBefore = field_map json__ "createdBefore" Timestamp.of_json in
      let createdAfter = field_map json__ "createdAfter" Timestamp.of_json in
      let status = field_map json__ "status" BillingAdjustmentStatus.of_json in
      let agreementId = field_map json__ "agreementId" AgreementId.of_json in
      make ?nextToken ?agreementType ?catalog ?maxResults ?createdBefore
        ?createdAfter ?status ?agreementId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists billing adjustment requests for a specific agreement. Sellers (proposers) can use this operation to view all billing adjustment requests associated with an agreement."]
module ListAgreementPaymentRequestsOutput =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token used for pagination. The field is null if there are no more results."];
      items: PaymentRequestSummaryList.t option
        [@ocaml.doc
          "An array of PaymentRequestSummary objects containing summary information about each payment request."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken = fun ?items -> fun () -> { nextToken; items }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("items", (Option.map x.items ~f:PaymentRequestSummaryList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let items =
        (Option.map ~f:PaymentRequestSummaryList.of_xml)
          (Xml.child xml_arg0 "items") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?items ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let items = field_map json__ "items" PaymentRequestSummaryList.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      make ?items ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists payment requests available to you as a seller or buyer. Both sellers (proposers) and buyers (acceptors) can use this operation to find payment requests by specifying their party type and applying optional parameters. PartyType is a required parameter. A ValidationException is returned if PartyType is not provided. Pagination is supported through maxResults (1-50, default 50) and nextToken parameters."]
module ListAgreementPaymentRequestsInput =
  struct
    type nonrec t =
      {
      partyType: PartyType.t
        [@ocaml.doc
          "The party type for the payment requests. Required parameter. Use Proposer to list payment requests where you are the seller, or Acceptor to list payment requests where you are the buyer."];
      agreementType: AgreementType.t option
        [@ocaml.doc
          "An optional parameter to list payment requests by agreement type (e.g., PurchaseAgreement)."];
      catalog: Catalog.t option
        [@ocaml.doc
          "An optional parameter to list payment requests by catalog (e.g., AWSMarketplace)."];
      agreementId: AgreementId.t option
        [@ocaml.doc
          "An optional parameter to list payment requests for a specific agreement."];
      status: PaymentRequestStatus.t option
        [@ocaml.doc
          "An optional parameter to list payment requests by status. Valid values include VALIDATING, VALIDATION_FAILED, PENDING_APPROVAL, APPROVED, REJECTED, and CANCELLED."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of payment requests to return in a single response (1-50). Default is 50."];
      nextToken: NextToken.t option
        [@ocaml.doc "A token to specify where to start pagination."]}
    let context_ = "ListAgreementPaymentRequestsInput"
    let make ?agreementType =
      fun ?catalog ->
        fun ?agreementId ->
          fun ?status ->
            fun ?maxResults ->
              fun ?nextToken ->
                fun ~partyType ->
                  fun () ->
                    {
                      agreementType;
                      catalog;
                      agreementId;
                      status;
                      maxResults;
                      nextToken;
                      partyType
                    }
    let to_value x =
      structure_to_value
        [("partyType", (Some (PartyType.to_value x.partyType)));
        ("agreementType",
          (Option.map x.agreementType ~f:AgreementType.to_value));
        ("catalog", (Option.map x.catalog ~f:Catalog.to_value));
        ("agreementId", (Option.map x.agreementId ~f:AgreementId.to_value));
        ("status", (Option.map x.status ~f:PaymentRequestStatus.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let status =
        (Option.map ~f:PaymentRequestStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let agreementId =
        (Option.map ~f:AgreementId.of_xml) (Xml.child xml_arg0 "agreementId") in
      let catalog =
        (Option.map ~f:Catalog.of_xml) (Xml.child xml_arg0 "catalog") in
      let agreementType =
        (Option.map ~f:AgreementType.of_xml)
          (Xml.child xml_arg0 "agreementType") in
      let partyType =
        PartyType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "partyType") in
      make ?nextToken ?maxResults ?status ?agreementId ?catalog
        ?agreementType ~partyType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      let status = field_map json__ "status" PaymentRequestStatus.of_json in
      let agreementId = field_map json__ "agreementId" AgreementId.of_json in
      let catalog = field_map json__ "catalog" Catalog.of_json in
      let agreementType =
        field_map json__ "agreementType" AgreementType.of_json in
      let partyType = field_map_exn json__ "partyType" PartyType.of_json in
      make ?nextToken ?maxResults ?status ?agreementId ?catalog
        ?agreementType ~partyType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists payment requests available to you as a seller or buyer. Both sellers (proposers) and buyers (acceptors) can use this operation to find payment requests by specifying their party type and applying optional parameters. PartyType is a required parameter. A ValidationException is returned if PartyType is not provided. Pagination is supported through maxResults (1-50, default 50) and nextToken parameters."]
module ListAgreementInvoiceLineItemsOutput =
  struct
    type nonrec t =
      {
      agreementInvoiceLineItemGroupSummaries:
        AgreementInvoiceLineItemGroupSummaries.t option
        [@ocaml.doc "A list of grouped billing data objects."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token used for pagination. The field is null if there are no more results."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?agreementInvoiceLineItemGroupSummaries =
      fun ?nextToken ->
        fun () -> { agreementInvoiceLineItemGroupSummaries; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("agreementInvoiceLineItemGroupSummaries",
           (Option.map x.agreementInvoiceLineItemGroupSummaries
              ~f:AgreementInvoiceLineItemGroupSummaries.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let agreementInvoiceLineItemGroupSummaries =
        (Option.map ~f:AgreementInvoiceLineItemGroupSummaries.of_xml)
          (Xml.child xml_arg0 "agreementInvoiceLineItemGroupSummaries") in
      make ?nextToken ?agreementInvoiceLineItemGroupSummaries ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let agreementInvoiceLineItemGroupSummaries =
        field_map json__ "agreementInvoiceLineItemGroupSummaries"
          AgreementInvoiceLineItemGroupSummaries.of_json in
      make ?nextToken ?agreementInvoiceLineItemGroupSummaries ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows sellers (proposers) to retrieve aggregated billing data from AWS Marketplace agreements using flexible grouping. Supports invoice-level aggregation with filtering by billing period, invoice type, and issued date. The groupBy parameter is required and supports only INVOICE_ID as a value. The agreementId parameter is required."]
module ListAgreementInvoiceLineItemsInput =
  struct
    type nonrec t =
      {
      agreementId: ResourceId.t
        [@ocaml.doc "The unique identifier of the agreement."];
      groupBy: LineItemGroupBy.t
        [@ocaml.doc
          "Specifies a grouping strategy for line items. Currently supports INVOICE_ID."];
      invoiceId: ResourceId.t option
        [@ocaml.doc
          "An optional filter to retrieve invoice information for a specific invoice."];
      invoiceType: InvoiceType.t option
        [@ocaml.doc
          "An optional filter for the type of invoice. Valid values are INVOICE and CREDIT_MEMO."];
      invoiceBillingPeriod: InvoiceBillingPeriod.t option
        [@ocaml.doc
          "An optional filter for the billing period associated with the invoice."];
      beforeIssuedTime: Timestamp.t option
        [@ocaml.doc
          "An optional filter for invoices issued before the specified timestamp."];
      afterIssuedTime: Timestamp.t option
        [@ocaml.doc
          "An optional filter for invoices issued after the specified timestamp."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of results to return in the response."];
      nextToken: NextToken.t option
        [@ocaml.doc "A token to specify where to start pagination."]}
    let context_ = "ListAgreementInvoiceLineItemsInput"
    let make ?invoiceId =
      fun ?invoiceType ->
        fun ?invoiceBillingPeriod ->
          fun ?beforeIssuedTime ->
            fun ?afterIssuedTime ->
              fun ?maxResults ->
                fun ?nextToken ->
                  fun ~agreementId ->
                    fun ~groupBy ->
                      fun () ->
                        {
                          invoiceId;
                          invoiceType;
                          invoiceBillingPeriod;
                          beforeIssuedTime;
                          afterIssuedTime;
                          maxResults;
                          nextToken;
                          agreementId;
                          groupBy
                        }
    let to_value x =
      structure_to_value
        [("agreementId", (Some (ResourceId.to_value x.agreementId)));
        ("groupBy", (Some (LineItemGroupBy.to_value x.groupBy)));
        ("invoiceId", (Option.map x.invoiceId ~f:ResourceId.to_value));
        ("invoiceType", (Option.map x.invoiceType ~f:InvoiceType.to_value));
        ("invoiceBillingPeriod",
          (Option.map x.invoiceBillingPeriod ~f:InvoiceBillingPeriod.to_value));
        ("beforeIssuedTime",
          (Option.map x.beforeIssuedTime ~f:Timestamp.to_value));
        ("afterIssuedTime",
          (Option.map x.afterIssuedTime ~f:Timestamp.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let afterIssuedTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "afterIssuedTime") in
      let beforeIssuedTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "beforeIssuedTime") in
      let invoiceBillingPeriod =
        (Option.map ~f:InvoiceBillingPeriod.of_xml)
          (Xml.child xml_arg0 "invoiceBillingPeriod") in
      let invoiceType =
        (Option.map ~f:InvoiceType.of_xml) (Xml.child xml_arg0 "invoiceType") in
      let invoiceId =
        (Option.map ~f:ResourceId.of_xml) (Xml.child xml_arg0 "invoiceId") in
      let groupBy =
        LineItemGroupBy.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "groupBy") in
      let agreementId =
        ResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "agreementId") in
      make ?nextToken ?maxResults ?afterIssuedTime ?beforeIssuedTime
        ?invoiceBillingPeriod ?invoiceType ?invoiceId ~groupBy ~agreementId
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      let afterIssuedTime =
        field_map json__ "afterIssuedTime" Timestamp.of_json in
      let beforeIssuedTime =
        field_map json__ "beforeIssuedTime" Timestamp.of_json in
      let invoiceBillingPeriod =
        field_map json__ "invoiceBillingPeriod" InvoiceBillingPeriod.of_json in
      let invoiceType = field_map json__ "invoiceType" InvoiceType.of_json in
      let invoiceId = field_map json__ "invoiceId" ResourceId.of_json in
      let groupBy = field_map_exn json__ "groupBy" LineItemGroupBy.of_json in
      let agreementId = field_map_exn json__ "agreementId" ResourceId.of_json in
      make ?nextToken ?maxResults ?afterIssuedTime ?beforeIssuedTime
        ?invoiceBillingPeriod ?invoiceType ?invoiceId ~groupBy ~agreementId
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows sellers (proposers) to retrieve aggregated billing data from AWS Marketplace agreements using flexible grouping. Supports invoice-level aggregation with filtering by billing period, invoice type, and issued date. The groupBy parameter is required and supports only INVOICE_ID as a value. The agreementId parameter is required."]
module ListAgreementChargesOutput =
  struct
    type nonrec t =
      {
      items: Charges.t option [@ocaml.doc "A list of agreement charges."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token used for pagination. The field is null if there are no more results."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?items = fun ?nextToken -> fun () -> { items; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("items", (Option.map x.items ~f:Charges.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let items = (Option.map ~f:Charges.of_xml) (Xml.child xml_arg0 "items") in
      make ?nextToken ?items ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let items = field_map json__ "items" Charges.of_json in
      make ?nextToken ?items ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows acceptors to view charges and purchase orders that are associated with an agreement. The response includes details about all charges regardless of whether a purchase order is linked to each charge."]
module ListAgreementChargesInput =
  struct
    type nonrec t =
      {
      catalog: Catalog.t option
        [@ocaml.doc "The catalog in which the charges were created."];
      agreementId: ResourceId.t option
        [@ocaml.doc "The unique identifier of the agreement."];
      agreementType: AgreementType.t option
        [@ocaml.doc
          "Filter to retrieve charges of a specific agreement type (for example, PurchaseAgreement)."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of charges to return in the response."];
      nextToken: NextToken.t option
        [@ocaml.doc "A token to specify where to start pagination."]}
    let make ?catalog =
      fun ?agreementId ->
        fun ?agreementType ->
          fun ?maxResults ->
            fun ?nextToken ->
              fun () ->
                { catalog; agreementId; agreementType; maxResults; nextToken
                }
    let to_value x =
      structure_to_value
        [("catalog", (Option.map x.catalog ~f:Catalog.to_value));
        ("agreementId", (Option.map x.agreementId ~f:ResourceId.to_value));
        ("agreementType",
          (Option.map x.agreementType ~f:AgreementType.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let agreementType =
        (Option.map ~f:AgreementType.of_xml)
          (Xml.child xml_arg0 "agreementType") in
      let agreementId =
        (Option.map ~f:ResourceId.of_xml) (Xml.child xml_arg0 "agreementId") in
      let catalog =
        (Option.map ~f:Catalog.of_xml) (Xml.child xml_arg0 "catalog") in
      make ?nextToken ?maxResults ?agreementType ?agreementId ?catalog ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      let agreementType =
        field_map json__ "agreementType" AgreementType.of_json in
      let agreementId = field_map json__ "agreementId" ResourceId.of_json in
      let catalog = field_map json__ "catalog" Catalog.of_json in
      make ?nextToken ?maxResults ?agreementType ?agreementId ?catalog ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows acceptors to view charges and purchase orders that are associated with an agreement. The response includes details about all charges regardless of whether a purchase order is linked to each charge."]
module ListAgreementCancellationRequestsOutput =
  struct
    type nonrec t =
      {
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token used for pagination. The field is null if there are no more results."];
      items: AgreementCancellationRequestSummaryList.t option
        [@ocaml.doc
          "An array of AgreementCancellationRequestSummary objects containing summary information about each cancellation request."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nextToken = fun ?items -> fun () -> { nextToken; items }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("nextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("items",
          (Option.map x.items
             ~f:AgreementCancellationRequestSummaryList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let items =
        (Option.map ~f:AgreementCancellationRequestSummaryList.of_xml)
          (Xml.child xml_arg0 "items") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      make ?items ?nextToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let items =
        field_map json__ "items"
          AgreementCancellationRequestSummaryList.of_json in
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      make ?items ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists agreement cancellation requests available to you as a seller or buyer. Both sellers (proposers) and buyers (acceptors) can use this operation to find cancellation requests by specifying their party type and applying optional filters. PartyType is a required parameter. A ValidationException is returned if PartyType is not provided."]
module ListAgreementCancellationRequestsInput =
  struct
    type nonrec t =
      {
      partyType: PartyType.t
        [@ocaml.doc
          "The party type for the cancellation requests. Required parameter. Use Proposer to list cancellation requests where you are the seller, or Acceptor to list cancellation requests where you are the buyer."];
      agreementId: AgreementId.t option
        [@ocaml.doc
          "An optional parameter to filter cancellation requests for a specific agreement."];
      status: AgreementCancellationRequestStatus.t option
        [@ocaml.doc
          "An optional parameter to filter cancellation requests by status."];
      agreementType: AgreementType.t option
        [@ocaml.doc
          "An optional parameter to filter cancellation requests by agreement type (e.g., PurchaseAgreement)."];
      catalog: Catalog.t option
        [@ocaml.doc
          "An optional parameter to filter cancellation requests by catalog (e.g., AWSMarketplace)."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of cancellation requests to return in the response."];
      nextToken: NextToken.t option
        [@ocaml.doc "A token to specify where to start pagination."]}
    let context_ = "ListAgreementCancellationRequestsInput"
    let make ?agreementId =
      fun ?status ->
        fun ?agreementType ->
          fun ?catalog ->
            fun ?maxResults ->
              fun ?nextToken ->
                fun ~partyType ->
                  fun () ->
                    {
                      agreementId;
                      status;
                      agreementType;
                      catalog;
                      maxResults;
                      nextToken;
                      partyType
                    }
    let to_value x =
      structure_to_value
        [("partyType", (Some (PartyType.to_value x.partyType)));
        ("agreementId", (Option.map x.agreementId ~f:AgreementId.to_value));
        ("status",
          (Option.map x.status ~f:AgreementCancellationRequestStatus.to_value));
        ("agreementType",
          (Option.map x.agreementType ~f:AgreementType.to_value));
        ("catalog", (Option.map x.catalog ~f:Catalog.to_value));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let catalog =
        (Option.map ~f:Catalog.of_xml) (Xml.child xml_arg0 "catalog") in
      let agreementType =
        (Option.map ~f:AgreementType.of_xml)
          (Xml.child xml_arg0 "agreementType") in
      let status =
        (Option.map ~f:AgreementCancellationRequestStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let agreementId =
        (Option.map ~f:AgreementId.of_xml) (Xml.child xml_arg0 "agreementId") in
      let partyType =
        PartyType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "partyType") in
      make ?nextToken ?maxResults ?catalog ?agreementType ?status
        ?agreementId ~partyType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      let catalog = field_map json__ "catalog" Catalog.of_json in
      let agreementType =
        field_map json__ "agreementType" AgreementType.of_json in
      let status =
        field_map json__ "status" AgreementCancellationRequestStatus.of_json in
      let agreementId = field_map json__ "agreementId" AgreementId.of_json in
      let partyType = field_map_exn json__ "partyType" PartyType.of_json in
      make ?nextToken ?maxResults ?catalog ?agreementType ?status
        ?agreementId ~partyType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists agreement cancellation requests available to you as a seller or buyer. Both sellers (proposers) and buyers (acceptors) can use this operation to find cancellation requests by specifying their party type and applying optional filters. PartyType is a required parameter. A ValidationException is returned if PartyType is not provided."]
module GetBillingAdjustmentRequestOutput =
  struct
    type nonrec t =
      {
      billingAdjustmentRequestId: BillingAdjustmentRequestId.t option
        [@ocaml.doc
          "The unique identifier of the billing adjustment request."];
      agreementId: AgreementId.t option
        [@ocaml.doc
          "The unique identifier of the agreement associated with this billing adjustment request."];
      adjustmentReasonCode: BillingAdjustmentReasonCode.t option
        [@ocaml.doc "The reason code for the billing adjustment."];
      description: String_.t option
        [@ocaml.doc
          "The detailed description of the billing adjustment reason, if provided."];
      originalInvoiceId: InvoiceId.t option
        [@ocaml.doc "The identifier of the original invoice being adjusted."];
      adjustmentAmount: PositiveAmountUpto8Decimals.t option
        [@ocaml.doc
          "The adjustment amount as a string representation of a decimal number."];
      currencyCode: CurrencyCode.t option
        [@ocaml.doc
          "The currency code for the adjustment amount (e.g., USD)."];
      status: BillingAdjustmentStatus.t option
        [@ocaml.doc "The current status of the billing adjustment request."];
      statusMessage: BillingAdjustmentStatusMessage.t option
        [@ocaml.doc
          "A message providing additional context about the billing adjustment request status. This field is populated only when the status is VALIDATION_FAILED."];
      createdAt: Timestamp.t option
        [@ocaml.doc
          "The date and time when the billing adjustment request was created."];
      updatedAt: Timestamp.t option
        [@ocaml.doc
          "The date and time when the billing adjustment request was last updated."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?billingAdjustmentRequestId =
      fun ?agreementId ->
        fun ?adjustmentReasonCode ->
          fun ?description ->
            fun ?originalInvoiceId ->
              fun ?adjustmentAmount ->
                fun ?currencyCode ->
                  fun ?status ->
                    fun ?statusMessage ->
                      fun ?createdAt ->
                        fun ?updatedAt ->
                          fun () ->
                            {
                              billingAdjustmentRequestId;
                              agreementId;
                              adjustmentReasonCode;
                              description;
                              originalInvoiceId;
                              adjustmentAmount;
                              currencyCode;
                              status;
                              statusMessage;
                              createdAt;
                              updatedAt
                            }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("billingAdjustmentRequestId",
           (Option.map x.billingAdjustmentRequestId
              ~f:BillingAdjustmentRequestId.to_value));
        ("agreementId", (Option.map x.agreementId ~f:AgreementId.to_value));
        ("adjustmentReasonCode",
          (Option.map x.adjustmentReasonCode
             ~f:BillingAdjustmentReasonCode.to_value));
        ("description", (Option.map x.description ~f:String_.to_value));
        ("originalInvoiceId",
          (Option.map x.originalInvoiceId ~f:InvoiceId.to_value));
        ("adjustmentAmount",
          (Option.map x.adjustmentAmount
             ~f:PositiveAmountUpto8Decimals.to_value));
        ("currencyCode",
          (Option.map x.currencyCode ~f:CurrencyCode.to_value));
        ("status", (Option.map x.status ~f:BillingAdjustmentStatus.to_value));
        ("statusMessage",
          (Option.map x.statusMessage
             ~f:BillingAdjustmentStatusMessage.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("updatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "updatedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let statusMessage =
        (Option.map ~f:BillingAdjustmentStatusMessage.of_xml)
          (Xml.child xml_arg0 "statusMessage") in
      let status =
        (Option.map ~f:BillingAdjustmentStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let currencyCode =
        (Option.map ~f:CurrencyCode.of_xml)
          (Xml.child xml_arg0 "currencyCode") in
      let adjustmentAmount =
        (Option.map ~f:PositiveAmountUpto8Decimals.of_xml)
          (Xml.child xml_arg0 "adjustmentAmount") in
      let originalInvoiceId =
        (Option.map ~f:InvoiceId.of_xml)
          (Xml.child xml_arg0 "originalInvoiceId") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "description") in
      let adjustmentReasonCode =
        (Option.map ~f:BillingAdjustmentReasonCode.of_xml)
          (Xml.child xml_arg0 "adjustmentReasonCode") in
      let agreementId =
        (Option.map ~f:AgreementId.of_xml) (Xml.child xml_arg0 "agreementId") in
      let billingAdjustmentRequestId =
        (Option.map ~f:BillingAdjustmentRequestId.of_xml)
          (Xml.child xml_arg0 "billingAdjustmentRequestId") in
      make ?updatedAt ?createdAt ?statusMessage ?status ?currencyCode
        ?adjustmentAmount ?originalInvoiceId ?description
        ?adjustmentReasonCode ?agreementId ?billingAdjustmentRequestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt = field_map json__ "updatedAt" Timestamp.of_json in
      let createdAt = field_map json__ "createdAt" Timestamp.of_json in
      let statusMessage =
        field_map json__ "statusMessage"
          BillingAdjustmentStatusMessage.of_json in
      let status = field_map json__ "status" BillingAdjustmentStatus.of_json in
      let currencyCode = field_map json__ "currencyCode" CurrencyCode.of_json in
      let adjustmentAmount =
        field_map json__ "adjustmentAmount"
          PositiveAmountUpto8Decimals.of_json in
      let originalInvoiceId =
        field_map json__ "originalInvoiceId" InvoiceId.of_json in
      let description = field_map json__ "description" String_.of_json in
      let adjustmentReasonCode =
        field_map json__ "adjustmentReasonCode"
          BillingAdjustmentReasonCode.of_json in
      let agreementId = field_map json__ "agreementId" AgreementId.of_json in
      let billingAdjustmentRequestId =
        field_map json__ "billingAdjustmentRequestId"
          BillingAdjustmentRequestId.of_json in
      make ?updatedAt ?createdAt ?statusMessage ?status ?currencyCode
        ?adjustmentAmount ?originalInvoiceId ?description
        ?adjustmentReasonCode ?agreementId ?billingAdjustmentRequestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves detailed information about a specific billing adjustment request. Sellers (proposers) can use this operation to view the status and details of a billing adjustment request they submitted."]
module GetBillingAdjustmentRequestInput =
  struct
    type nonrec t =
      {
      agreementId: AgreementId.t
        [@ocaml.doc
          "The unique identifier of the agreement associated with the billing adjustment request."];
      billingAdjustmentRequestId: BillingAdjustmentRequestId.t
        [@ocaml.doc
          "The unique identifier of the billing adjustment request."]}
    let context_ = "GetBillingAdjustmentRequestInput"
    let make ~agreementId =
      fun ~billingAdjustmentRequestId ->
        fun () -> { agreementId; billingAdjustmentRequestId }
    let to_value x =
      structure_to_value
        [("agreementId", (Some (AgreementId.to_value x.agreementId)));
        ("billingAdjustmentRequestId",
          (Some
             (BillingAdjustmentRequestId.to_value
                x.billingAdjustmentRequestId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let billingAdjustmentRequestId =
        BillingAdjustmentRequestId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "billingAdjustmentRequestId") in
      let agreementId =
        AgreementId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "agreementId") in
      make ~billingAdjustmentRequestId ~agreementId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let billingAdjustmentRequestId =
        field_map_exn json__ "billingAdjustmentRequestId"
          BillingAdjustmentRequestId.of_json in
      let agreementId =
        field_map_exn json__ "agreementId" AgreementId.of_json in
      make ~billingAdjustmentRequestId ~agreementId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves detailed information about a specific billing adjustment request. Sellers (proposers) can use this operation to view the status and details of a billing adjustment request they submitted."]
module GetAgreementTermsOutput =
  struct
    type nonrec t =
      {
      acceptedTerms: AcceptedTermList.t option
        [@ocaml.doc
          "A subset of terms proposed by the proposer that have been accepted by the acceptor as part of the agreement creation."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token used for pagination. The field is null if there are no more results."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?acceptedTerms =
      fun ?nextToken -> fun () -> { acceptedTerms; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("acceptedTerms",
           (Option.map x.acceptedTerms ~f:AcceptedTermList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let acceptedTerms =
        (Option.map ~f:AcceptedTermList.of_xml)
          (Xml.child xml_arg0 "acceptedTerms") in
      make ?nextToken ?acceptedTerms ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let acceptedTerms =
        field_map json__ "acceptedTerms" AcceptedTermList.of_json in
      make ?nextToken ?acceptedTerms ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Obtains details about the terms in an agreement that you participated in as proposer or acceptor. The details include: TermType \226\128\147 The type of term, such as LegalTerm, RenewalTerm, or ConfigurableUpfrontPricingTerm. TermID \226\128\147 The ID of the particular term, which is common between offer and agreement. TermPayload \226\128\147 The key information contained in the term, such as the EULA for LegalTerm or pricing and dimensions for various pricing terms, such as ConfigurableUpfrontPricingTerm or UsageBasedPricingTerm. Configuration \226\128\147 The buyer/acceptor's selection at the time of agreement creation, such as the number of units purchased for a dimension or setting the EnableAutoRenew flag."]
module GetAgreementTermsInput =
  struct
    type nonrec t =
      {
      agreementId: ResourceId.t
        [@ocaml.doc "The unique identifier of the agreement."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of agreements to return in the response."];
      nextToken: NextToken.t option
        [@ocaml.doc "A token to specify where to start pagination."]}
    let context_ = "GetAgreementTermsInput"
    let make ?maxResults =
      fun ?nextToken ->
        fun ~agreementId -> fun () -> { maxResults; nextToken; agreementId }
    let to_value x =
      structure_to_value
        [("agreementId", (Some (ResourceId.to_value x.agreementId)));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let agreementId =
        ResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "agreementId") in
      make ?nextToken ?maxResults ~agreementId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      let agreementId = field_map_exn json__ "agreementId" ResourceId.of_json in
      make ?nextToken ?maxResults ~agreementId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Obtains details about the terms in an agreement that you participated in as proposer or acceptor. The details include: TermType \226\128\147 The type of term, such as LegalTerm, RenewalTerm, or ConfigurableUpfrontPricingTerm. TermID \226\128\147 The ID of the particular term, which is common between offer and agreement. TermPayload \226\128\147 The key information contained in the term, such as the EULA for LegalTerm or pricing and dimensions for various pricing terms, such as ConfigurableUpfrontPricingTerm or UsageBasedPricingTerm. Configuration \226\128\147 The buyer/acceptor's selection at the time of agreement creation, such as the number of units purchased for a dimension or setting the EnableAutoRenew flag."]
module GetAgreementPaymentRequestOutput =
  struct
    type nonrec t =
      {
      paymentRequestId: PaymentRequestId.t option
        [@ocaml.doc "The unique identifier of the payment request."];
      agreementId: AgreementId.t option
        [@ocaml.doc
          "The unique identifier of the agreement associated with this payment request. Use DescribeAgreement to retrieve full agreement details."];
      status: PaymentRequestStatus.t option
        [@ocaml.doc
          "The current status of the payment request. Possible values include: VALIDATING \226\128\147 The payment request is being validated against agreement terms. VALIDATION_FAILED \226\128\147 The payment request failed validation. PENDING_APPROVAL \226\128\147 The payment request is awaiting buyer action. APPROVED \226\128\147 The buyer has approved the payment request. REJECTED \226\128\147 The buyer has rejected the payment request. CANCELLED \226\128\147 The seller has cancelled the payment request."];
      statusMessage: PaymentRequestStatusMessage.t option
        [@ocaml.doc
          "An optional message providing additional context about the payment request status, such as a rejection reason or validation failure details."];
      name: PaymentRequestName.t option
        [@ocaml.doc "The descriptive name of the payment request."];
      description: PaymentRequestDescription.t option
        [@ocaml.doc
          "The detailed description of the payment request, if provided."];
      chargeId: ChargeId.t option
        [@ocaml.doc
          "The unique identifier of the charge created after the payment request is approved. This field is only present for approved payment requests and follows the pattern ch-\\[a-zA-Z0-9\\]+."];
      chargeAmount: PositiveAmountUpto8Decimals.t option
        [@ocaml.doc "The amount charged or to be charged to the buyer."];
      currencyCode: CurrencyCode.t option
        [@ocaml.doc "The currency code for the charge amount."];
      createdAt: Timestamp.t option
        [@ocaml.doc
          "The date and time when the payment request was created."];
      updatedAt: Timestamp.t option
        [@ocaml.doc
          "The date and time when the payment request was last updated."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?paymentRequestId =
      fun ?agreementId ->
        fun ?status ->
          fun ?statusMessage ->
            fun ?name ->
              fun ?description ->
                fun ?chargeId ->
                  fun ?chargeAmount ->
                    fun ?currencyCode ->
                      fun ?createdAt ->
                        fun ?updatedAt ->
                          fun () ->
                            {
                              paymentRequestId;
                              agreementId;
                              status;
                              statusMessage;
                              name;
                              description;
                              chargeId;
                              chargeAmount;
                              currencyCode;
                              createdAt;
                              updatedAt
                            }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("paymentRequestId",
           (Option.map x.paymentRequestId ~f:PaymentRequestId.to_value));
        ("agreementId", (Option.map x.agreementId ~f:AgreementId.to_value));
        ("status", (Option.map x.status ~f:PaymentRequestStatus.to_value));
        ("statusMessage",
          (Option.map x.statusMessage ~f:PaymentRequestStatusMessage.to_value));
        ("name", (Option.map x.name ~f:PaymentRequestName.to_value));
        ("description",
          (Option.map x.description ~f:PaymentRequestDescription.to_value));
        ("chargeId", (Option.map x.chargeId ~f:ChargeId.to_value));
        ("chargeAmount",
          (Option.map x.chargeAmount ~f:PositiveAmountUpto8Decimals.to_value));
        ("currencyCode",
          (Option.map x.currencyCode ~f:CurrencyCode.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("updatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "updatedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let currencyCode =
        (Option.map ~f:CurrencyCode.of_xml)
          (Xml.child xml_arg0 "currencyCode") in
      let chargeAmount =
        (Option.map ~f:PositiveAmountUpto8Decimals.of_xml)
          (Xml.child xml_arg0 "chargeAmount") in
      let chargeId =
        (Option.map ~f:ChargeId.of_xml) (Xml.child xml_arg0 "chargeId") in
      let description =
        (Option.map ~f:PaymentRequestDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        (Option.map ~f:PaymentRequestName.of_xml) (Xml.child xml_arg0 "name") in
      let statusMessage =
        (Option.map ~f:PaymentRequestStatusMessage.of_xml)
          (Xml.child xml_arg0 "statusMessage") in
      let status =
        (Option.map ~f:PaymentRequestStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let agreementId =
        (Option.map ~f:AgreementId.of_xml) (Xml.child xml_arg0 "agreementId") in
      let paymentRequestId =
        (Option.map ~f:PaymentRequestId.of_xml)
          (Xml.child xml_arg0 "paymentRequestId") in
      make ?updatedAt ?createdAt ?currencyCode ?chargeAmount ?chargeId
        ?description ?name ?statusMessage ?status ?agreementId
        ?paymentRequestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt = field_map json__ "updatedAt" Timestamp.of_json in
      let createdAt = field_map json__ "createdAt" Timestamp.of_json in
      let currencyCode = field_map json__ "currencyCode" CurrencyCode.of_json in
      let chargeAmount =
        field_map json__ "chargeAmount" PositiveAmountUpto8Decimals.of_json in
      let chargeId = field_map json__ "chargeId" ChargeId.of_json in
      let description =
        field_map json__ "description" PaymentRequestDescription.of_json in
      let name = field_map json__ "name" PaymentRequestName.of_json in
      let statusMessage =
        field_map json__ "statusMessage" PaymentRequestStatusMessage.of_json in
      let status = field_map json__ "status" PaymentRequestStatus.of_json in
      let agreementId = field_map json__ "agreementId" AgreementId.of_json in
      let paymentRequestId =
        field_map json__ "paymentRequestId" PaymentRequestId.of_json in
      make ?updatedAt ?createdAt ?currencyCode ?chargeAmount ?chargeId
        ?description ?name ?statusMessage ?status ?agreementId
        ?paymentRequestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves detailed information about a specific payment request. Both sellers (proposers) and buyers (acceptors) can use this operation to view payment requests associated with their agreements. The response includes the current status, charge details, timestamps, and the charge ID if the request has been approved. The calling identity must be either the acceptor or proposer of the payment request. A ResourceNotFoundException is returned if the payment request does not exist."]
module GetAgreementPaymentRequestInput =
  struct
    type nonrec t =
      {
      paymentRequestId: PaymentRequestId.t
        [@ocaml.doc "The identifier of the payment request."];
      agreementId: AgreementId.t
        [@ocaml.doc
          "The unique identifier of the agreement associated with the payment request."]}
    let context_ = "GetAgreementPaymentRequestInput"
    let make ~paymentRequestId =
      fun ~agreementId -> fun () -> { paymentRequestId; agreementId }
    let to_value x =
      structure_to_value
        [("paymentRequestId",
           (Some (PaymentRequestId.to_value x.paymentRequestId)));
        ("agreementId", (Some (AgreementId.to_value x.agreementId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let agreementId =
        AgreementId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "agreementId") in
      let paymentRequestId =
        PaymentRequestId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "paymentRequestId") in
      make ~agreementId ~paymentRequestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let agreementId =
        field_map_exn json__ "agreementId" AgreementId.of_json in
      let paymentRequestId =
        field_map_exn json__ "paymentRequestId" PaymentRequestId.of_json in
      make ~agreementId ~paymentRequestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves detailed information about a specific payment request. Both sellers (proposers) and buyers (acceptors) can use this operation to view payment requests associated with their agreements. The response includes the current status, charge details, timestamps, and the charge ID if the request has been approved. The calling identity must be either the acceptor or proposer of the payment request. A ResourceNotFoundException is returned if the payment request does not exist."]
module GetAgreementEntitlementsOutput =
  struct
    type nonrec t =
      {
      agreementEntitlements: AgreementEntitlementList.t option
        [@ocaml.doc
          "A list of agreement entitlements which are part of the latest agreement."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "The token used for pagination. The field is null if there are no more results."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?agreementEntitlements =
      fun ?nextToken -> fun () -> { agreementEntitlements; nextToken }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("agreementEntitlements",
           (Option.map x.agreementEntitlements
              ~f:AgreementEntitlementList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let agreementEntitlements =
        (Option.map ~f:AgreementEntitlementList.of_xml)
          (Xml.child xml_arg0 "agreementEntitlements") in
      make ?nextToken ?agreementEntitlements ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let agreementEntitlements =
        field_map json__ "agreementEntitlements"
          AgreementEntitlementList.of_json in
      make ?nextToken ?agreementEntitlements ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Obtains details about the entitlements of an agreement."]
module GetAgreementEntitlementsInput =
  struct
    type nonrec t =
      {
      agreementId: ResourceId.t
        [@ocaml.doc "The unique identifier of the agreement."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The maximum number of agreement entitlements to return in the response."];
      nextToken: NextToken.t option
        [@ocaml.doc "A token to specify where to start pagination."]}
    let context_ = "GetAgreementEntitlementsInput"
    let make ?maxResults =
      fun ?nextToken ->
        fun ~agreementId -> fun () -> { maxResults; nextToken; agreementId }
    let to_value x =
      structure_to_value
        [("agreementId", (Some (ResourceId.to_value x.agreementId)));
        ("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      let agreementId =
        ResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "agreementId") in
      make ?nextToken ?maxResults ~agreementId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let nextToken = field_map json__ "nextToken" NextToken.of_json in
      let maxResults = field_map json__ "maxResults" MaxResults.of_json in
      let agreementId = field_map_exn json__ "agreementId" ResourceId.of_json in
      make ?nextToken ?maxResults ~agreementId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Obtains details about the entitlements of an agreement."]
module GetAgreementCancellationRequestOutput =
  struct
    type nonrec t =
      {
      agreementCancellationRequestId: AgreementCancellationRequestId.t option
        [@ocaml.doc "The unique identifier of the cancellation request."];
      agreementId: AgreementId.t option
        [@ocaml.doc
          "The unique identifier of the agreement associated with this cancellation request. Use DescribeAgreement to retrieve full agreement details."];
      reasonCode: AgreementCancellationRequestReasonCode.t option
        [@ocaml.doc "The reason code provided for the cancellation."];
      description: AgreementCancellationRequestDescription.t option
        [@ocaml.doc
          "The detailed description of the cancellation reason, if provided."];
      status: AgreementCancellationRequestStatus.t option
        [@ocaml.doc "The current status of the cancellation request."];
      statusMessage: AgreementCancellationRequestStatusMessage.t option
        [@ocaml.doc
          "A message providing additional context about the cancellation request status."];
      createdAt: Timestamp.t option
        [@ocaml.doc
          "The date and time when the cancellation request was created."];
      updatedAt: Timestamp.t option
        [@ocaml.doc
          "The date and time when the cancellation request was last updated."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?agreementCancellationRequestId =
      fun ?agreementId ->
        fun ?reasonCode ->
          fun ?description ->
            fun ?status ->
              fun ?statusMessage ->
                fun ?createdAt ->
                  fun ?updatedAt ->
                    fun () ->
                      {
                        agreementCancellationRequestId;
                        agreementId;
                        reasonCode;
                        description;
                        status;
                        statusMessage;
                        createdAt;
                        updatedAt
                      }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("agreementCancellationRequestId",
           (Option.map x.agreementCancellationRequestId
              ~f:AgreementCancellationRequestId.to_value));
        ("agreementId", (Option.map x.agreementId ~f:AgreementId.to_value));
        ("reasonCode",
          (Option.map x.reasonCode
             ~f:AgreementCancellationRequestReasonCode.to_value));
        ("description",
          (Option.map x.description
             ~f:AgreementCancellationRequestDescription.to_value));
        ("status",
          (Option.map x.status ~f:AgreementCancellationRequestStatus.to_value));
        ("statusMessage",
          (Option.map x.statusMessage
             ~f:AgreementCancellationRequestStatusMessage.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("updatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "updatedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let statusMessage =
        (Option.map ~f:AgreementCancellationRequestStatusMessage.of_xml)
          (Xml.child xml_arg0 "statusMessage") in
      let status =
        (Option.map ~f:AgreementCancellationRequestStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let description =
        (Option.map ~f:AgreementCancellationRequestDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let reasonCode =
        (Option.map ~f:AgreementCancellationRequestReasonCode.of_xml)
          (Xml.child xml_arg0 "reasonCode") in
      let agreementId =
        (Option.map ~f:AgreementId.of_xml) (Xml.child xml_arg0 "agreementId") in
      let agreementCancellationRequestId =
        (Option.map ~f:AgreementCancellationRequestId.of_xml)
          (Xml.child xml_arg0 "agreementCancellationRequestId") in
      make ?updatedAt ?createdAt ?statusMessage ?status ?description
        ?reasonCode ?agreementId ?agreementCancellationRequestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt = field_map json__ "updatedAt" Timestamp.of_json in
      let createdAt = field_map json__ "createdAt" Timestamp.of_json in
      let statusMessage =
        field_map json__ "statusMessage"
          AgreementCancellationRequestStatusMessage.of_json in
      let status =
        field_map json__ "status" AgreementCancellationRequestStatus.of_json in
      let description =
        field_map json__ "description"
          AgreementCancellationRequestDescription.of_json in
      let reasonCode =
        field_map json__ "reasonCode"
          AgreementCancellationRequestReasonCode.of_json in
      let agreementId = field_map json__ "agreementId" AgreementId.of_json in
      let agreementCancellationRequestId =
        field_map json__ "agreementCancellationRequestId"
          AgreementCancellationRequestId.of_json in
      make ?updatedAt ?createdAt ?statusMessage ?status ?description
        ?reasonCode ?agreementId ?agreementCancellationRequestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves detailed information about a specific agreement cancellation request. Both sellers (proposers) and buyers (acceptors) can use this operation to view cancellation requests associated with their agreements."]
module GetAgreementCancellationRequestInput =
  struct
    type nonrec t =
      {
      agreementCancellationRequestId: AgreementCancellationRequestId.t
        [@ocaml.doc "The unique identifier of the cancellation request."];
      agreementId: AgreementId.t
        [@ocaml.doc
          "The unique identifier of the agreement associated with the cancellation request."]}
    let context_ = "GetAgreementCancellationRequestInput"
    let make ~agreementCancellationRequestId =
      fun ~agreementId ->
        fun () -> { agreementCancellationRequestId; agreementId }
    let to_value x =
      structure_to_value
        [("agreementCancellationRequestId",
           (Some
              (AgreementCancellationRequestId.to_value
                 x.agreementCancellationRequestId)));
        ("agreementId", (Some (AgreementId.to_value x.agreementId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let agreementId =
        AgreementId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "agreementId") in
      let agreementCancellationRequestId =
        AgreementCancellationRequestId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "agreementCancellationRequestId") in
      make ~agreementId ~agreementCancellationRequestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let agreementId =
        field_map_exn json__ "agreementId" AgreementId.of_json in
      let agreementCancellationRequestId =
        field_map_exn json__ "agreementCancellationRequestId"
          AgreementCancellationRequestId.of_json in
      make ~agreementId ~agreementCancellationRequestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves detailed information about a specific agreement cancellation request. Both sellers (proposers) and buyers (acceptors) can use this operation to view cancellation requests associated with their agreements."]
module DescribeAgreementOutput =
  struct
    type nonrec t =
      {
      agreementId: ResourceId.t option
        [@ocaml.doc "The unique identifier of the agreement."];
      acceptor: Acceptor.t option
        [@ocaml.doc
          "The details of the party accepting the agreement terms. This is commonly the buyer for PurchaseAgreement."];
      proposer: Proposer.t option
        [@ocaml.doc
          "The details of the party proposing the agreement terms. This is commonly the seller for PurchaseAgreement."];
      startTime: Timestamp.t option
        [@ocaml.doc "The date and time when the agreement starts."];
      endTime: Timestamp.t option
        [@ocaml.doc
          "The date and time when the agreement ends. The field is null for pay-as-you-go agreements, which don\226\128\153t have end dates."];
      acceptanceTime: Timestamp.t option
        [@ocaml.doc
          "The date and time the offer was accepted or the agreement was created. AcceptanceTime and StartTime can differ for future dated agreements (FDAs)."];
      agreementType: AgreementType.t option
        [@ocaml.doc
          "The type of agreement. Values are PurchaseAgreement or VendorInsightsAgreement."];
      estimatedCharges: EstimatedCharges.t option
        [@ocaml.doc "The estimated cost of the agreement."];
      proposalSummary: ProposalSummary.t option
        [@ocaml.doc "A summary of the proposal received from the proposer."];
      status: AgreementStatus.t option
        [@ocaml.doc
          "The current status of the agreement. Statuses include: ACTIVE \226\128\147 The terms of the agreement are active. ARCHIVED \226\128\147 The agreement ended without a specified reason. CANCELLED \226\128\147 The acceptor ended the agreement before the defined end date. EXPIRED \226\128\147 The agreement ended on the defined end date. RENEWED \226\128\147 The agreement was renewed into a new agreement (for example, an auto-renewal). REPLACED \226\128\147 The agreement was replaced using an agreement replacement offer. TERMINATED \226\128\147 The agreement ended before the defined end date because of an AWS termination (for example, a payment failure)."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?agreementId =
      fun ?acceptor ->
        fun ?proposer ->
          fun ?startTime ->
            fun ?endTime ->
              fun ?acceptanceTime ->
                fun ?agreementType ->
                  fun ?estimatedCharges ->
                    fun ?proposalSummary ->
                      fun ?status ->
                        fun () ->
                          {
                            agreementId;
                            acceptor;
                            proposer;
                            startTime;
                            endTime;
                            acceptanceTime;
                            agreementType;
                            estimatedCharges;
                            proposalSummary;
                            status
                          }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("agreementId", (Option.map x.agreementId ~f:ResourceId.to_value));
        ("acceptor", (Option.map x.acceptor ~f:Acceptor.to_value));
        ("proposer", (Option.map x.proposer ~f:Proposer.to_value));
        ("startTime", (Option.map x.startTime ~f:Timestamp.to_value));
        ("endTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("acceptanceTime",
          (Option.map x.acceptanceTime ~f:Timestamp.to_value));
        ("agreementType",
          (Option.map x.agreementType ~f:AgreementType.to_value));
        ("estimatedCharges",
          (Option.map x.estimatedCharges ~f:EstimatedCharges.to_value));
        ("proposalSummary",
          (Option.map x.proposalSummary ~f:ProposalSummary.to_value));
        ("status", (Option.map x.status ~f:AgreementStatus.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        (Option.map ~f:AgreementStatus.of_xml) (Xml.child xml_arg0 "status") in
      let proposalSummary =
        (Option.map ~f:ProposalSummary.of_xml)
          (Xml.child xml_arg0 "proposalSummary") in
      let estimatedCharges =
        (Option.map ~f:EstimatedCharges.of_xml)
          (Xml.child xml_arg0 "estimatedCharges") in
      let agreementType =
        (Option.map ~f:AgreementType.of_xml)
          (Xml.child xml_arg0 "agreementType") in
      let acceptanceTime =
        (Option.map ~f:Timestamp.of_xml)
          (Xml.child xml_arg0 "acceptanceTime") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "endTime") in
      let startTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "startTime") in
      let proposer =
        (Option.map ~f:Proposer.of_xml) (Xml.child xml_arg0 "proposer") in
      let acceptor =
        (Option.map ~f:Acceptor.of_xml) (Xml.child xml_arg0 "acceptor") in
      let agreementId =
        (Option.map ~f:ResourceId.of_xml) (Xml.child xml_arg0 "agreementId") in
      make ?status ?proposalSummary ?estimatedCharges ?agreementType
        ?acceptanceTime ?endTime ?startTime ?proposer ?acceptor ?agreementId
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let status = field_map json__ "status" AgreementStatus.of_json in
      let proposalSummary =
        field_map json__ "proposalSummary" ProposalSummary.of_json in
      let estimatedCharges =
        field_map json__ "estimatedCharges" EstimatedCharges.of_json in
      let agreementType =
        field_map json__ "agreementType" AgreementType.of_json in
      let acceptanceTime =
        field_map json__ "acceptanceTime" Timestamp.of_json in
      let endTime = field_map json__ "endTime" Timestamp.of_json in
      let startTime = field_map json__ "startTime" Timestamp.of_json in
      let proposer = field_map json__ "proposer" Proposer.of_json in
      let acceptor = field_map json__ "acceptor" Acceptor.of_json in
      let agreementId = field_map json__ "agreementId" ResourceId.of_json in
      make ?status ?proposalSummary ?estimatedCharges ?agreementType
        ?acceptanceTime ?endTime ?startTime ?proposer ?acceptor ?agreementId
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides details about an agreement, such as the proposer, acceptor, start date, and end date."]
module DescribeAgreementInput =
  struct
    type nonrec t =
      {
      agreementId: ResourceId.t
        [@ocaml.doc "The unique identifier of the agreement."]}
    let context_ = "DescribeAgreementInput"
    let make ~agreementId = fun () -> { agreementId }
    let to_value x =
      structure_to_value
        [("agreementId", (Some (ResourceId.to_value x.agreementId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let agreementId =
        ResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "agreementId") in
      make ~agreementId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let agreementId = field_map_exn json__ "agreementId" ResourceId.of_json in
      make ~agreementId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides details about an agreement, such as the proposer, acceptor, start date, and end date."]
module CreateAgreementRequestOutput =
  struct
    type nonrec t =
      {
      agreementRequestId: AgreementRequestId.t option
        [@ocaml.doc
          "The unique identifier of the agreement request created. Use this identifier with AcceptAgreementRequest to accept the agreement."];
      chargeSummary: ChargeSummary.t option
        [@ocaml.doc
          "Provides details of the charges associated with the agreement request. This is only applicable when a request is created for PurchaseAgreement."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceQuotaExceededException of ServiceQuotaExceededException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?agreementRequestId =
      fun ?chargeSummary -> fun () -> { agreementRequestId; chargeSummary }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceQuotaExceededException" ->
          `ServiceQuotaExceededException
            (ServiceQuotaExceededException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceQuotaExceededException e ->
          `Assoc
            [("error", (`String "ServiceQuotaExceededException"));
            ("details", (ServiceQuotaExceededException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("agreementRequestId",
           (Option.map x.agreementRequestId ~f:AgreementRequestId.to_value));
        ("chargeSummary",
          (Option.map x.chargeSummary ~f:ChargeSummary.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let chargeSummary =
        (Option.map ~f:ChargeSummary.of_xml)
          (Xml.child xml_arg0 "chargeSummary") in
      let agreementRequestId =
        (Option.map ~f:AgreementRequestId.of_xml)
          (Xml.child xml_arg0 "agreementRequestId") in
      make ?chargeSummary ?agreementRequestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let chargeSummary =
        field_map json__ "chargeSummary" ChargeSummary.of_json in
      let agreementRequestId =
        field_map json__ "agreementRequestId" AgreementRequestId.of_json in
      make ?chargeSummary ?agreementRequestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an agreement request that acts as a quote for the terms you want to accept. The agreement request captures the requested terms, calculates charges, and returns a summary. Use AcceptAgreementRequest with the returned agreementRequestId to finalize the agreement."]
module CreateAgreementRequestInput =
  struct
    type nonrec t =
      {
      clientToken: ClientToken.t option
        [@ocaml.doc
          "A unique, case-sensitive identifier that you provide to ensure the idempotency of the request."];
      intent: Intent.t
        [@ocaml.doc
          "The purpose and desired outcome of the agreement request. This is a required parameter that determines how the agreement request is processed. NEW \226\128\147 Creates a new agreement for terms in the request. AMEND \226\128\147 Modifies an existing agreement with terms that are accepted in the request. REPLACE \226\128\147 Creates a new agreement with accepted terms and replaces the existing agreement."];
      requestedTerms: RequestedTermList.t
        [@ocaml.doc
          "A list of terms that define what is being accepted as part of the agreement. Some terms require configuration."];
      sourceAgreementIdentifier: ResourceId.t option
        [@ocaml.doc
          "The agreement's identifier that the request acts upon. This parameter is required for all non-NEW intents (i.e., AMEND or REPLACE). Don't provide this parameter if the intent is NEW."];
      agreementProposalIdentifier: AgreementProposalId.t option
        [@ocaml.doc
          "The agreement proposal signed by the proposer. The proposal includes the requested resources and the terms that outline an agreement outcome. This parameter is required if the intent is not AMEND."];
      taxConfiguration: TaxConfiguration.t option
        [@ocaml.doc
          "Configuration for tax estimation in the agreement request response."]}
    let context_ = "CreateAgreementRequestInput"
    let make ?clientToken =
      fun ?sourceAgreementIdentifier ->
        fun ?agreementProposalIdentifier ->
          fun ?taxConfiguration ->
            fun ~intent ->
              fun ~requestedTerms ->
                fun () ->
                  {
                    clientToken;
                    sourceAgreementIdentifier;
                    agreementProposalIdentifier;
                    taxConfiguration;
                    intent;
                    requestedTerms
                  }
    let to_value x =
      structure_to_value
        [("clientToken", (Option.map x.clientToken ~f:ClientToken.to_value));
        ("intent", (Some (Intent.to_value x.intent)));
        ("requestedTerms",
          (Some (RequestedTermList.to_value x.requestedTerms)));
        ("sourceAgreementIdentifier",
          (Option.map x.sourceAgreementIdentifier ~f:ResourceId.to_value));
        ("agreementProposalIdentifier",
          (Option.map x.agreementProposalIdentifier
             ~f:AgreementProposalId.to_value));
        ("taxConfiguration",
          (Option.map x.taxConfiguration ~f:TaxConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let taxConfiguration =
        (Option.map ~f:TaxConfiguration.of_xml)
          (Xml.child xml_arg0 "taxConfiguration") in
      let agreementProposalIdentifier =
        (Option.map ~f:AgreementProposalId.of_xml)
          (Xml.child xml_arg0 "agreementProposalIdentifier") in
      let sourceAgreementIdentifier =
        (Option.map ~f:ResourceId.of_xml)
          (Xml.child xml_arg0 "sourceAgreementIdentifier") in
      let requestedTerms =
        RequestedTermList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "requestedTerms") in
      let intent =
        Intent.of_xml (Xml.child_exn ~context:context_ xml_arg0 "intent") in
      let clientToken =
        (Option.map ~f:ClientToken.of_xml) (Xml.child xml_arg0 "clientToken") in
      make ?taxConfiguration ?agreementProposalIdentifier
        ?sourceAgreementIdentifier ~requestedTerms ~intent ?clientToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let taxConfiguration =
        field_map json__ "taxConfiguration" TaxConfiguration.of_json in
      let agreementProposalIdentifier =
        field_map json__ "agreementProposalIdentifier"
          AgreementProposalId.of_json in
      let sourceAgreementIdentifier =
        field_map json__ "sourceAgreementIdentifier" ResourceId.of_json in
      let requestedTerms =
        field_map_exn json__ "requestedTerms" RequestedTermList.of_json in
      let intent = field_map_exn json__ "intent" Intent.of_json in
      let clientToken = field_map json__ "clientToken" ClientToken.of_json in
      make ?taxConfiguration ?agreementProposalIdentifier
        ?sourceAgreementIdentifier ~requestedTerms ~intent ?clientToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an agreement request that acts as a quote for the terms you want to accept. The agreement request captures the requested terms, calculates charges, and returns a summary. Use AcceptAgreementRequest with the returned agreementRequestId to finalize the agreement."]
module CancelAgreementPaymentRequestOutput =
  struct
    type nonrec t =
      {
      paymentRequestId: PaymentRequestId.t option
        [@ocaml.doc
          "The unique identifier of the cancelled payment request."];
      agreementId: AgreementId.t option
        [@ocaml.doc
          "The unique identifier of the agreement associated with this payment request."];
      status: PaymentRequestStatus.t option
        [@ocaml.doc
          "The updated status of the payment request, which is CANCELLED."];
      name: PaymentRequestName.t option
        [@ocaml.doc "The descriptive name of the payment request."];
      description: PaymentRequestDescription.t option
        [@ocaml.doc
          "The detailed description of the payment request, if provided."];
      chargeAmount: PositiveAmountUpto8Decimals.t option
        [@ocaml.doc "The amount that was requested to be charged."];
      currencyCode: CurrencyCode.t option
        [@ocaml.doc "The currency code for the charge amount."];
      createdAt: Timestamp.t option
        [@ocaml.doc
          "The date and time when the payment request was originally created."];
      updatedAt: Timestamp.t option
        [@ocaml.doc
          "The date and time when the payment request was cancelled."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?paymentRequestId =
      fun ?agreementId ->
        fun ?status ->
          fun ?name ->
            fun ?description ->
              fun ?chargeAmount ->
                fun ?currencyCode ->
                  fun ?createdAt ->
                    fun ?updatedAt ->
                      fun () ->
                        {
                          paymentRequestId;
                          agreementId;
                          status;
                          name;
                          description;
                          chargeAmount;
                          currencyCode;
                          createdAt;
                          updatedAt
                        }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("paymentRequestId",
           (Option.map x.paymentRequestId ~f:PaymentRequestId.to_value));
        ("agreementId", (Option.map x.agreementId ~f:AgreementId.to_value));
        ("status", (Option.map x.status ~f:PaymentRequestStatus.to_value));
        ("name", (Option.map x.name ~f:PaymentRequestName.to_value));
        ("description",
          (Option.map x.description ~f:PaymentRequestDescription.to_value));
        ("chargeAmount",
          (Option.map x.chargeAmount ~f:PositiveAmountUpto8Decimals.to_value));
        ("currencyCode",
          (Option.map x.currencyCode ~f:CurrencyCode.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("updatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "updatedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let currencyCode =
        (Option.map ~f:CurrencyCode.of_xml)
          (Xml.child xml_arg0 "currencyCode") in
      let chargeAmount =
        (Option.map ~f:PositiveAmountUpto8Decimals.of_xml)
          (Xml.child xml_arg0 "chargeAmount") in
      let description =
        (Option.map ~f:PaymentRequestDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        (Option.map ~f:PaymentRequestName.of_xml) (Xml.child xml_arg0 "name") in
      let status =
        (Option.map ~f:PaymentRequestStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let agreementId =
        (Option.map ~f:AgreementId.of_xml) (Xml.child xml_arg0 "agreementId") in
      let paymentRequestId =
        (Option.map ~f:PaymentRequestId.of_xml)
          (Xml.child xml_arg0 "paymentRequestId") in
      make ?updatedAt ?createdAt ?currencyCode ?chargeAmount ?description
        ?name ?status ?agreementId ?paymentRequestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt = field_map json__ "updatedAt" Timestamp.of_json in
      let createdAt = field_map json__ "createdAt" Timestamp.of_json in
      let currencyCode = field_map json__ "currencyCode" CurrencyCode.of_json in
      let chargeAmount =
        field_map json__ "chargeAmount" PositiveAmountUpto8Decimals.of_json in
      let description =
        field_map json__ "description" PaymentRequestDescription.of_json in
      let name = field_map json__ "name" PaymentRequestName.of_json in
      let status = field_map json__ "status" PaymentRequestStatus.of_json in
      let agreementId = field_map json__ "agreementId" AgreementId.of_json in
      let paymentRequestId =
        field_map json__ "paymentRequestId" PaymentRequestId.of_json in
      make ?updatedAt ?createdAt ?currencyCode ?chargeAmount ?description
        ?name ?status ?agreementId ?paymentRequestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows sellers (proposers) to cancel a payment request that is in PENDING_APPROVAL status. Once cancelled, the payment request transitions to CANCELLED status and can no longer be accepted or rejected by the buyer. Only payment requests in PENDING_APPROVAL status can be cancelled. A ConflictException is thrown if the payment request is in any other status."]
module CancelAgreementPaymentRequestInput =
  struct
    type nonrec t =
      {
      paymentRequestId: PaymentRequestId.t
        [@ocaml.doc
          "The unique identifier of the payment request to cancel."];
      agreementId: AgreementId.t
        [@ocaml.doc
          "The unique identifier of the agreement associated with the payment request."]}
    let context_ = "CancelAgreementPaymentRequestInput"
    let make ~paymentRequestId =
      fun ~agreementId -> fun () -> { paymentRequestId; agreementId }
    let to_value x =
      structure_to_value
        [("paymentRequestId",
           (Some (PaymentRequestId.to_value x.paymentRequestId)));
        ("agreementId", (Some (AgreementId.to_value x.agreementId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let agreementId =
        AgreementId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "agreementId") in
      let paymentRequestId =
        PaymentRequestId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "paymentRequestId") in
      make ~agreementId ~paymentRequestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let agreementId =
        field_map_exn json__ "agreementId" AgreementId.of_json in
      let paymentRequestId =
        field_map_exn json__ "paymentRequestId" PaymentRequestId.of_json in
      make ~agreementId ~paymentRequestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows sellers (proposers) to cancel a payment request that is in PENDING_APPROVAL status. Once cancelled, the payment request transitions to CANCELLED status and can no longer be accepted or rejected by the buyer. Only payment requests in PENDING_APPROVAL status can be cancelled. A ConflictException is thrown if the payment request is in any other status."]
module CancelAgreementOutput =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows an acceptor to cancel an active agreement. Not all agreements are eligible for cancellation. Use the error response to determine why a cancellation request was rejected."]
module CancelAgreementInput =
  struct
    type nonrec t =
      {
      agreementId: ResourceId.t
        [@ocaml.doc "The unique identifier of the agreement."]}
    let context_ = "CancelAgreementInput"
    let make ~agreementId = fun () -> { agreementId }
    let to_value x =
      structure_to_value
        [("agreementId", (Some (ResourceId.to_value x.agreementId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let agreementId =
        ResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "agreementId") in
      make ~agreementId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let agreementId = field_map_exn json__ "agreementId" ResourceId.of_json in
      make ~agreementId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows an acceptor to cancel an active agreement. Not all agreements are eligible for cancellation. Use the error response to determine why a cancellation request was rejected."]
module CancelAgreementCancellationRequestOutput =
  struct
    type nonrec t =
      {
      agreementCancellationRequestId: AgreementCancellationRequestId.t option
        [@ocaml.doc
          "The unique identifier of the cancelled cancellation request."];
      agreementId: AgreementId.t option
        [@ocaml.doc
          "The unique identifier of the agreement associated with this cancellation request."];
      reasonCode: AgreementCancellationRequestReasonCode.t option
        [@ocaml.doc
          "The original reason code provided when the cancellation request was created."];
      description: AgreementCancellationRequestDescription.t option
        [@ocaml.doc
          "The detailed description of the original cancellation reason, if provided."];
      status: AgreementCancellationRequestStatus.t option
        [@ocaml.doc
          "The updated status of the cancellation request, which is CANCELLED."];
      statusMessage: AgreementCancellationRequestStatusMessage.t option
        [@ocaml.doc
          "A message providing additional context about the cancellation request status."];
      createdAt: Timestamp.t option
        [@ocaml.doc
          "The date and time when the cancellation request was originally created."];
      updatedAt: Timestamp.t option
        [@ocaml.doc
          "The date and time when the cancellation request was cancelled."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?agreementCancellationRequestId =
      fun ?agreementId ->
        fun ?reasonCode ->
          fun ?description ->
            fun ?status ->
              fun ?statusMessage ->
                fun ?createdAt ->
                  fun ?updatedAt ->
                    fun () ->
                      {
                        agreementCancellationRequestId;
                        agreementId;
                        reasonCode;
                        description;
                        status;
                        statusMessage;
                        createdAt;
                        updatedAt
                      }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("agreementCancellationRequestId",
           (Option.map x.agreementCancellationRequestId
              ~f:AgreementCancellationRequestId.to_value));
        ("agreementId", (Option.map x.agreementId ~f:AgreementId.to_value));
        ("reasonCode",
          (Option.map x.reasonCode
             ~f:AgreementCancellationRequestReasonCode.to_value));
        ("description",
          (Option.map x.description
             ~f:AgreementCancellationRequestDescription.to_value));
        ("status",
          (Option.map x.status ~f:AgreementCancellationRequestStatus.to_value));
        ("statusMessage",
          (Option.map x.statusMessage
             ~f:AgreementCancellationRequestStatusMessage.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("updatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "updatedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let statusMessage =
        (Option.map ~f:AgreementCancellationRequestStatusMessage.of_xml)
          (Xml.child xml_arg0 "statusMessage") in
      let status =
        (Option.map ~f:AgreementCancellationRequestStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let description =
        (Option.map ~f:AgreementCancellationRequestDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let reasonCode =
        (Option.map ~f:AgreementCancellationRequestReasonCode.of_xml)
          (Xml.child xml_arg0 "reasonCode") in
      let agreementId =
        (Option.map ~f:AgreementId.of_xml) (Xml.child xml_arg0 "agreementId") in
      let agreementCancellationRequestId =
        (Option.map ~f:AgreementCancellationRequestId.of_xml)
          (Xml.child xml_arg0 "agreementCancellationRequestId") in
      make ?updatedAt ?createdAt ?statusMessage ?status ?description
        ?reasonCode ?agreementId ?agreementCancellationRequestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt = field_map json__ "updatedAt" Timestamp.of_json in
      let createdAt = field_map json__ "createdAt" Timestamp.of_json in
      let statusMessage =
        field_map json__ "statusMessage"
          AgreementCancellationRequestStatusMessage.of_json in
      let status =
        field_map json__ "status" AgreementCancellationRequestStatus.of_json in
      let description =
        field_map json__ "description"
          AgreementCancellationRequestDescription.of_json in
      let reasonCode =
        field_map json__ "reasonCode"
          AgreementCancellationRequestReasonCode.of_json in
      let agreementId = field_map json__ "agreementId" AgreementId.of_json in
      let agreementCancellationRequestId =
        field_map json__ "agreementCancellationRequestId"
          AgreementCancellationRequestId.of_json in
      make ?updatedAt ?createdAt ?statusMessage ?status ?description
        ?reasonCode ?agreementId ?agreementCancellationRequestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows sellers (proposers) to withdraw an existing agreement cancellation request that is in a pending state. Once cancelled, the cancellation request transitions to CANCELLED status and can no longer be approved or rejected by the buyer. Only cancellation requests in PENDING_APPROVAL status can be cancelled. A ConflictException is thrown if the cancellation request is in any other status."]
module CancelAgreementCancellationRequestInput =
  struct
    type nonrec t =
      {
      agreementId: AgreementId.t
        [@ocaml.doc
          "The unique identifier of the agreement associated with the cancellation request."];
      agreementCancellationRequestId: AgreementCancellationRequestId.t
        [@ocaml.doc
          "The unique identifier of the cancellation request to cancel."];
      cancellationReason: AgreementCancellationRequestCancellationReason.t
        [@ocaml.doc
          "A required message explaining why the cancellation request is being withdrawn (1-2000 characters)."]}
    let context_ = "CancelAgreementCancellationRequestInput"
    let make ~agreementId =
      fun ~agreementCancellationRequestId ->
        fun ~cancellationReason ->
          fun () ->
            { agreementId; agreementCancellationRequestId; cancellationReason
            }
    let to_value x =
      structure_to_value
        [("agreementId", (Some (AgreementId.to_value x.agreementId)));
        ("agreementCancellationRequestId",
          (Some
             (AgreementCancellationRequestId.to_value
                x.agreementCancellationRequestId)));
        ("cancellationReason",
          (Some
             (AgreementCancellationRequestCancellationReason.to_value
                x.cancellationReason)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cancellationReason =
        AgreementCancellationRequestCancellationReason.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "cancellationReason") in
      let agreementCancellationRequestId =
        AgreementCancellationRequestId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "agreementCancellationRequestId") in
      let agreementId =
        AgreementId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "agreementId") in
      make ~cancellationReason ~agreementCancellationRequestId ~agreementId
        ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let cancellationReason =
        field_map_exn json__ "cancellationReason"
          AgreementCancellationRequestCancellationReason.of_json in
      let agreementCancellationRequestId =
        field_map_exn json__ "agreementCancellationRequestId"
          AgreementCancellationRequestId.of_json in
      let agreementId =
        field_map_exn json__ "agreementId" AgreementId.of_json in
      make ~cancellationReason ~agreementCancellationRequestId ~agreementId
        ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows sellers (proposers) to withdraw an existing agreement cancellation request that is in a pending state. Once cancelled, the cancellation request transitions to CANCELLED status and can no longer be approved or rejected by the buyer. Only cancellation requests in PENDING_APPROVAL status can be cancelled. A ConflictException is thrown if the cancellation request is in any other status."]
module BatchCreateBillingAdjustmentRequestOutput =
  struct
    type nonrec t =
      {
      items: BatchCreateBillingAdjustmentItemList.t option
        [@ocaml.doc
          "A list of successfully created billing adjustment items, each containing the billingAdjustmentRequestId and clientToken."];
      errors: BatchCreateBillingAdjustmentErrorList.t option
        [@ocaml.doc
          "A list of errors for entries that failed validation, each containing the clientToken, error code, and message."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?items = fun ?errors -> fun () -> { items; errors }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("items",
           (Option.map x.items
              ~f:BatchCreateBillingAdjustmentItemList.to_value));
        ("errors",
          (Option.map x.errors
             ~f:BatchCreateBillingAdjustmentErrorList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let errors =
        (Option.map ~f:BatchCreateBillingAdjustmentErrorList.of_xml)
          (Xml.child xml_arg0 "errors") in
      let items =
        (Option.map ~f:BatchCreateBillingAdjustmentItemList.of_xml)
          (Xml.child xml_arg0 "items") in
      make ?errors ?items ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let errors =
        field_map json__ "errors"
          BatchCreateBillingAdjustmentErrorList.of_json in
      let items =
        field_map json__ "items" BatchCreateBillingAdjustmentItemList.of_json in
      make ?errors ?items ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows sellers (proposers) to submit billing adjustment requests for one or more invoices within an agreement. Each entry in the batch specifies an invoice and the adjustment amount. The operation returns successfully created adjustment request IDs and any errors for entries that failed to process. Each entry requires a unique clientToken for idempotency."]
module BatchCreateBillingAdjustmentRequestInput =
  struct
    type nonrec t =
      {
      billingAdjustmentRequestEntries:
        BatchCreateBillingAdjustmentRequestEntryList.t
        [@ocaml.doc
          "A list of billing adjustment request entries. Each entry specifies the invoice and adjustment details."]}
    let context_ = "BatchCreateBillingAdjustmentRequestInput"
    let make ~billingAdjustmentRequestEntries =
      fun () -> { billingAdjustmentRequestEntries }
    let to_value x =
      structure_to_value
        [("billingAdjustmentRequestEntries",
           (Some
              (BatchCreateBillingAdjustmentRequestEntryList.to_value
                 x.billingAdjustmentRequestEntries)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let billingAdjustmentRequestEntries =
        BatchCreateBillingAdjustmentRequestEntryList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "billingAdjustmentRequestEntries") in
      make ~billingAdjustmentRequestEntries ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let billingAdjustmentRequestEntries =
        field_map_exn json__ "billingAdjustmentRequestEntries"
          BatchCreateBillingAdjustmentRequestEntryList.of_json in
      make ~billingAdjustmentRequestEntries ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows sellers (proposers) to submit billing adjustment requests for one or more invoices within an agreement. Each entry in the batch specifies an invoice and the adjustment amount. The operation returns successfully created adjustment request IDs and any errors for entries that failed to process. Each entry requires a unique clientToken for idempotency."]
module AcceptAgreementRequestOutput =
  struct
    type nonrec t =
      {
      agreementId: ResourceId.t option
        [@ocaml.doc
          "The unique identifier of the agreement created or modified by accepting the agreement request."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?agreementId = fun () -> { agreementId }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("agreementId", (Option.map x.agreementId ~f:ResourceId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let agreementId =
        (Option.map ~f:ResourceId.of_xml) (Xml.child xml_arg0 "agreementId") in
      make ?agreementId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let agreementId = field_map json__ "agreementId" ResourceId.of_json in
      make ?agreementId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Accepts an agreement request to finalize the agreement. The acceptor can optionally provide purchase orders to associate with the agreement charges."]
module AcceptAgreementRequestInput =
  struct
    type nonrec t =
      {
      agreementRequestId: AgreementRequestId.t
        [@ocaml.doc "The unique identifier of the agreement request."];
      purchaseOrders: PurchaseOrders.t option
        [@ocaml.doc
          "A list of purchase orders associated with accepting a marketplace agreement request."]}
    let context_ = "AcceptAgreementRequestInput"
    let make ?purchaseOrders =
      fun ~agreementRequestId ->
        fun () -> { purchaseOrders; agreementRequestId }
    let to_value x =
      structure_to_value
        [("agreementRequestId",
           (Some (AgreementRequestId.to_value x.agreementRequestId)));
        ("purchaseOrders",
          (Option.map x.purchaseOrders ~f:PurchaseOrders.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let purchaseOrders =
        (Option.map ~f:PurchaseOrders.of_xml)
          (Xml.child xml_arg0 "purchaseOrders") in
      let agreementRequestId =
        AgreementRequestId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "agreementRequestId") in
      make ?purchaseOrders ~agreementRequestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let purchaseOrders =
        field_map json__ "purchaseOrders" PurchaseOrders.of_json in
      let agreementRequestId =
        field_map_exn json__ "agreementRequestId" AgreementRequestId.of_json in
      make ?purchaseOrders ~agreementRequestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Accepts an agreement request to finalize the agreement. The acceptor can optionally provide purchase orders to associate with the agreement charges."]
module AcceptAgreementPaymentRequestOutput =
  struct
    type nonrec t =
      {
      paymentRequestId: PaymentRequestId.t option
        [@ocaml.doc "The unique identifier of the accepted payment request."];
      agreementId: AgreementId.t option
        [@ocaml.doc
          "The unique identifier of the agreement associated with this payment request."];
      status: PaymentRequestStatus.t option
        [@ocaml.doc
          "The updated status of the payment request, which is APPROVED."];
      name: PaymentRequestName.t option
        [@ocaml.doc "The descriptive name of the payment request."];
      description: PaymentRequestDescription.t option
        [@ocaml.doc
          "The detailed description of the payment request, if provided."];
      chargeAmount: PositiveAmountUpto8Decimals.t option
        [@ocaml.doc "The amount that was approved to be charged."];
      currencyCode: CurrencyCode.t option
        [@ocaml.doc "The currency code for the charge amount."];
      createdAt: Timestamp.t option
        [@ocaml.doc
          "The date and time when the payment request was originally created."];
      updatedAt: Timestamp.t option
        [@ocaml.doc
          "The date and time when the payment request was accepted."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?paymentRequestId =
      fun ?agreementId ->
        fun ?status ->
          fun ?name ->
            fun ?description ->
              fun ?chargeAmount ->
                fun ?currencyCode ->
                  fun ?createdAt ->
                    fun ?updatedAt ->
                      fun () ->
                        {
                          paymentRequestId;
                          agreementId;
                          status;
                          name;
                          description;
                          chargeAmount;
                          currencyCode;
                          createdAt;
                          updatedAt
                        }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("paymentRequestId",
           (Option.map x.paymentRequestId ~f:PaymentRequestId.to_value));
        ("agreementId", (Option.map x.agreementId ~f:AgreementId.to_value));
        ("status", (Option.map x.status ~f:PaymentRequestStatus.to_value));
        ("name", (Option.map x.name ~f:PaymentRequestName.to_value));
        ("description",
          (Option.map x.description ~f:PaymentRequestDescription.to_value));
        ("chargeAmount",
          (Option.map x.chargeAmount ~f:PositiveAmountUpto8Decimals.to_value));
        ("currencyCode",
          (Option.map x.currencyCode ~f:CurrencyCode.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("updatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "updatedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let currencyCode =
        (Option.map ~f:CurrencyCode.of_xml)
          (Xml.child xml_arg0 "currencyCode") in
      let chargeAmount =
        (Option.map ~f:PositiveAmountUpto8Decimals.of_xml)
          (Xml.child xml_arg0 "chargeAmount") in
      let description =
        (Option.map ~f:PaymentRequestDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let name =
        (Option.map ~f:PaymentRequestName.of_xml) (Xml.child xml_arg0 "name") in
      let status =
        (Option.map ~f:PaymentRequestStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let agreementId =
        (Option.map ~f:AgreementId.of_xml) (Xml.child xml_arg0 "agreementId") in
      let paymentRequestId =
        (Option.map ~f:PaymentRequestId.of_xml)
          (Xml.child xml_arg0 "paymentRequestId") in
      make ?updatedAt ?createdAt ?currencyCode ?chargeAmount ?description
        ?name ?status ?agreementId ?paymentRequestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt = field_map json__ "updatedAt" Timestamp.of_json in
      let createdAt = field_map json__ "createdAt" Timestamp.of_json in
      let currencyCode = field_map json__ "currencyCode" CurrencyCode.of_json in
      let chargeAmount =
        field_map json__ "chargeAmount" PositiveAmountUpto8Decimals.of_json in
      let description =
        field_map json__ "description" PaymentRequestDescription.of_json in
      let name = field_map json__ "name" PaymentRequestName.of_json in
      let status = field_map json__ "status" PaymentRequestStatus.of_json in
      let agreementId = field_map json__ "agreementId" AgreementId.of_json in
      let paymentRequestId =
        field_map json__ "paymentRequestId" PaymentRequestId.of_json in
      make ?updatedAt ?createdAt ?currencyCode ?chargeAmount ?description
        ?name ?status ?agreementId ?paymentRequestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows buyers (acceptors) to accept a payment request that is in PENDING_APPROVAL status. Once accepted, the payment request transitions to APPROVED status and the charge will be processed. Buyers can optionally provide a purchase order reference for their internal tracking. Only payment requests in PENDING_APPROVAL status can be accepted. A ConflictException is thrown if the payment request is in any other status."]
module AcceptAgreementPaymentRequestInput =
  struct
    type nonrec t =
      {
      paymentRequestId: PaymentRequestId.t
        [@ocaml.doc
          "The unique identifier of the payment request to accept."];
      agreementId: AgreementId.t
        [@ocaml.doc
          "The unique identifier of the agreement associated with the payment request."];
      purchaseOrderReference: PurchaseOrderReference.t option
        [@ocaml.doc
          "An optional purchase order reference that buyers can provide to associate the payment request with their internal purchase order system."]}
    let context_ = "AcceptAgreementPaymentRequestInput"
    let make ?purchaseOrderReference =
      fun ~paymentRequestId ->
        fun ~agreementId ->
          fun () -> { purchaseOrderReference; paymentRequestId; agreementId }
    let to_value x =
      structure_to_value
        [("paymentRequestId",
           (Some (PaymentRequestId.to_value x.paymentRequestId)));
        ("agreementId", (Some (AgreementId.to_value x.agreementId)));
        ("purchaseOrderReference",
          (Option.map x.purchaseOrderReference
             ~f:PurchaseOrderReference.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let purchaseOrderReference =
        (Option.map ~f:PurchaseOrderReference.of_xml)
          (Xml.child xml_arg0 "purchaseOrderReference") in
      let agreementId =
        AgreementId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "agreementId") in
      let paymentRequestId =
        PaymentRequestId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "paymentRequestId") in
      make ?purchaseOrderReference ~agreementId ~paymentRequestId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let purchaseOrderReference =
        field_map json__ "purchaseOrderReference"
          PurchaseOrderReference.of_json in
      let agreementId =
        field_map_exn json__ "agreementId" AgreementId.of_json in
      let paymentRequestId =
        field_map_exn json__ "paymentRequestId" PaymentRequestId.of_json in
      make ?purchaseOrderReference ~agreementId ~paymentRequestId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows buyers (acceptors) to accept a payment request that is in PENDING_APPROVAL status. Once accepted, the payment request transitions to APPROVED status and the charge will be processed. Buyers can optionally provide a purchase order reference for their internal tracking. Only payment requests in PENDING_APPROVAL status can be accepted. A ConflictException is thrown if the payment request is in any other status."]
module AcceptAgreementCancellationRequestOutput =
  struct
    type nonrec t =
      {
      agreementId: AgreementId.t option
        [@ocaml.doc
          "The unique identifier of the agreement associated with this cancellation request."];
      agreementCancellationRequestId: AgreementCancellationRequestId.t option
        [@ocaml.doc
          "The unique identifier of the accepted cancellation request."];
      status: AgreementCancellationRequestStatus.t option
        [@ocaml.doc
          "The updated status of the cancellation request, which is APPROVED."];
      reasonCode: AgreementCancellationRequestReasonCode.t option
        [@ocaml.doc
          "The original reason code provided when the cancellation request was created."];
      description: AgreementCancellationRequestDescription.t option
        [@ocaml.doc
          "The detailed description of the cancellation reason, if provided."];
      createdAt: Timestamp.t option
        [@ocaml.doc
          "The date and time when the cancellation request was originally created."];
      updatedAt: Timestamp.t option
        [@ocaml.doc
          "The date and time when the cancellation request was accepted."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `ConflictException of ConflictException.t 
      | `InternalServerException of InternalServerException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ThrottlingException of ThrottlingException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?agreementId =
      fun ?agreementCancellationRequestId ->
        fun ?status ->
          fun ?reasonCode ->
            fun ?description ->
              fun ?createdAt ->
                fun ?updatedAt ->
                  fun () ->
                    {
                      agreementId;
                      agreementCancellationRequestId;
                      status;
                      reasonCode;
                      description;
                      createdAt;
                      updatedAt
                    }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_json json)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "ConflictException" ->
          `ConflictException (ConflictException.of_xml xml)
      | "InternalServerException" ->
          `InternalServerException (InternalServerException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `ConflictException e ->
          `Assoc
            [("error", (`String "ConflictException"));
            ("details", (ConflictException.to_json e))]
      | `InternalServerException e ->
          `Assoc
            [("error", (`String "InternalServerException"));
            ("details", (InternalServerException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("agreementId", (Option.map x.agreementId ~f:AgreementId.to_value));
        ("agreementCancellationRequestId",
          (Option.map x.agreementCancellationRequestId
             ~f:AgreementCancellationRequestId.to_value));
        ("status",
          (Option.map x.status ~f:AgreementCancellationRequestStatus.to_value));
        ("reasonCode",
          (Option.map x.reasonCode
             ~f:AgreementCancellationRequestReasonCode.to_value));
        ("description",
          (Option.map x.description
             ~f:AgreementCancellationRequestDescription.to_value));
        ("createdAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("updatedAt", (Option.map x.updatedAt ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let updatedAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "updatedAt") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "createdAt") in
      let description =
        (Option.map ~f:AgreementCancellationRequestDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let reasonCode =
        (Option.map ~f:AgreementCancellationRequestReasonCode.of_xml)
          (Xml.child xml_arg0 "reasonCode") in
      let status =
        (Option.map ~f:AgreementCancellationRequestStatus.of_xml)
          (Xml.child xml_arg0 "status") in
      let agreementCancellationRequestId =
        (Option.map ~f:AgreementCancellationRequestId.of_xml)
          (Xml.child xml_arg0 "agreementCancellationRequestId") in
      let agreementId =
        (Option.map ~f:AgreementId.of_xml) (Xml.child xml_arg0 "agreementId") in
      make ?updatedAt ?createdAt ?description ?reasonCode ?status
        ?agreementCancellationRequestId ?agreementId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let updatedAt = field_map json__ "updatedAt" Timestamp.of_json in
      let createdAt = field_map json__ "createdAt" Timestamp.of_json in
      let description =
        field_map json__ "description"
          AgreementCancellationRequestDescription.of_json in
      let reasonCode =
        field_map json__ "reasonCode"
          AgreementCancellationRequestReasonCode.of_json in
      let status =
        field_map json__ "status" AgreementCancellationRequestStatus.of_json in
      let agreementCancellationRequestId =
        field_map json__ "agreementCancellationRequestId"
          AgreementCancellationRequestId.of_json in
      let agreementId = field_map json__ "agreementId" AgreementId.of_json in
      make ?updatedAt ?createdAt ?description ?reasonCode ?status
        ?agreementCancellationRequestId ?agreementId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows buyers (acceptors) to accept a cancellation request that is in PENDING_APPROVAL status. Once accepted, the cancellation request transitions to APPROVED status and the agreement cancellation will be processed. Only cancellation requests in PENDING_APPROVAL status can be accepted. A ConflictException is thrown if the cancellation request is in any other status."]
module AcceptAgreementCancellationRequestInput =
  struct
    type nonrec t =
      {
      agreementId: AgreementId.t
        [@ocaml.doc
          "The unique identifier of the agreement associated with the cancellation request."];
      agreementCancellationRequestId: AgreementCancellationRequestId.t
        [@ocaml.doc
          "The unique identifier of the cancellation request to accept."]}
    let context_ = "AcceptAgreementCancellationRequestInput"
    let make ~agreementId =
      fun ~agreementCancellationRequestId ->
        fun () -> { agreementId; agreementCancellationRequestId }
    let to_value x =
      structure_to_value
        [("agreementId", (Some (AgreementId.to_value x.agreementId)));
        ("agreementCancellationRequestId",
          (Some
             (AgreementCancellationRequestId.to_value
                x.agreementCancellationRequestId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let agreementCancellationRequestId =
        AgreementCancellationRequestId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "agreementCancellationRequestId") in
      let agreementId =
        AgreementId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "agreementId") in
      make ~agreementCancellationRequestId ~agreementId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json__ =
      let agreementCancellationRequestId =
        field_map_exn json__ "agreementCancellationRequestId"
          AgreementCancellationRequestId.of_json in
      let agreementId =
        field_map_exn json__ "agreementId" AgreementId.of_json in
      make ~agreementCancellationRequestId ~agreementId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Allows buyers (acceptors) to accept a cancellation request that is in PENDING_APPROVAL status. Once accepted, the cancellation request transitions to APPROVED status and the agreement cancellation will be processed. Only cancellation requests in PENDING_APPROVAL status can be accepted. A ConflictException is thrown if the cancellation request is in any other status."]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_entity_to_thing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namespaceVersion =
         flag "namespace-version" (optional json_arg) ~doc:"JSON Version"
       and thingName =
         flag "thing-name" (required string) ~doc:"STRING ThingName"
       and entityId = flag "entity-id" (required string) ~doc:"STRING Urn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_entity_to_thing
           (Values.AssociateEntityToThingRequest.make
              ?namespaceVersion:(Option.map ~f:Values.Version.of_json
                                   namespaceVersion) ~thingName ~entityId ())
           (Some Values.AssociateEntityToThingResponse.to_json)
           (Some Values.AssociateEntityToThingResponse.error_to_json)])
let create_flow_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and compatibleNamespaceVersion =
         flag "compatible-namespace-version" (optional json_arg)
           ~doc:"JSON Version"
       and definition =
         flag "definition" (required json_arg) ~doc:"JSON DefinitionDocument" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_flow_template
           (Values.CreateFlowTemplateRequest.make
              ?compatibleNamespaceVersion:(Option.map
                                             ~f:Values.Version.of_json
                                             compatibleNamespaceVersion)
              ~definition:(Values.DefinitionDocument.of_json definition) ())
           (Some Values.CreateFlowTemplateResponse.to_json)
           (Some Values.CreateFlowTemplateResponse.error_to_json)])
let create_system_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and greengrassGroupName =
         flag "greengrass-group-name" (optional string)
           ~doc:"STRING GroupName"
       and s3BucketName =
         flag "s3-bucket-name" (optional string) ~doc:"STRING S3BucketName"
       and metricsConfiguration =
         flag "metrics-configuration" (optional json_arg)
           ~doc:"JSON MetricsConfiguration"
       and flowActionsRoleArn =
         flag "flow-actions-role-arn" (optional string) ~doc:"STRING RoleArn"
       and definition =
         flag "definition" (required json_arg) ~doc:"JSON DefinitionDocument"
       and target =
         flag "target" (required json_arg) ~doc:"JSON DeploymentTarget" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_system_instance
           (Values.CreateSystemInstanceRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?greengrassGroupName ?s3BucketName
              ?metricsConfiguration:(Option.map
                                       ~f:Values.MetricsConfiguration.of_json
                                       metricsConfiguration)
              ?flowActionsRoleArn
              ~definition:(Values.DefinitionDocument.of_json definition)
              ~target:(Values.DeploymentTarget.of_json target) ())
           (Some Values.CreateSystemInstanceResponse.to_json)
           (Some Values.CreateSystemInstanceResponse.error_to_json)])
let create_system_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and compatibleNamespaceVersion =
         flag "compatible-namespace-version" (optional json_arg)
           ~doc:"JSON Version"
       and definition =
         flag "definition" (required json_arg) ~doc:"JSON DefinitionDocument" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_system_template
           (Values.CreateSystemTemplateRequest.make
              ?compatibleNamespaceVersion:(Option.map
                                             ~f:Values.Version.of_json
                                             compatibleNamespaceVersion)
              ~definition:(Values.DefinitionDocument.of_json definition) ())
           (Some Values.CreateSystemTemplateResponse.to_json)
           (Some Values.CreateSystemTemplateResponse.error_to_json)])
let delete_flow_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING Urn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_flow_template
           (Values.DeleteFlowTemplateRequest.make ~id ())
           (Some Values.DeleteFlowTemplateResponse.to_json)
           (Some Values.DeleteFlowTemplateResponse.error_to_json)])
let delete_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_namespace (Values.DeleteNamespaceRequest.make ())
           (Some Values.DeleteNamespaceResponse.to_json)
           (Some Values.DeleteNamespaceResponse.error_to_json)])
let delete_system_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (optional string) ~doc:"STRING Urn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_system_instance
           (Values.DeleteSystemInstanceRequest.make ?id ())
           (Some Values.DeleteSystemInstanceResponse.to_json)
           (Some Values.DeleteSystemInstanceResponse.error_to_json)])
let delete_system_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING Urn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_system_template
           (Values.DeleteSystemTemplateRequest.make ~id ())
           (Some Values.DeleteSystemTemplateResponse.to_json)
           (Some Values.DeleteSystemTemplateResponse.error_to_json)])
let deploy_system_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (optional string) ~doc:"STRING Urn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deploy_system_instance
           (Values.DeploySystemInstanceRequest.make ?id ())
           (Some Values.DeploySystemInstanceResponse.to_json)
           (Some Values.DeploySystemInstanceResponse.error_to_json)])
let deprecate_flow_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING Urn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deprecate_flow_template
           (Values.DeprecateFlowTemplateRequest.make ~id ())
           (Some Values.DeprecateFlowTemplateResponse.to_json)
           (Some Values.DeprecateFlowTemplateResponse.error_to_json)])
let deprecate_system_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING Urn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deprecate_system_template
           (Values.DeprecateSystemTemplateRequest.make ~id ())
           (Some Values.DeprecateSystemTemplateResponse.to_json)
           (Some Values.DeprecateSystemTemplateResponse.error_to_json)])
let describe_namespace =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namespaceName =
         flag "namespace-name" (optional string) ~doc:"STRING NamespaceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_namespace
           (Values.DescribeNamespaceRequest.make ?namespaceName ())
           (Some Values.DescribeNamespaceResponse.to_json)
           (Some Values.DescribeNamespaceResponse.error_to_json)])
let dissociate_entity_from_thing =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and thingName =
         flag "thing-name" (required string) ~doc:"STRING ThingName"
       and entityType =
         flag "entity-type" (required json_arg) ~doc:"JSON EntityType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.dissociate_entity_from_thing
           (Values.DissociateEntityFromThingRequest.make ~thingName
              ~entityType:(Values.EntityType.of_json entityType) ())
           (Some Values.DissociateEntityFromThingResponse.to_json)
           (Some Values.DissociateEntityFromThingResponse.error_to_json)])
let get_entities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and namespaceVersion =
         flag "namespace-version" (optional json_arg) ~doc:"JSON Version"
       and ids = flag "ids" (required json_arg) ~doc:"JSON Urns" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_entities
           (Values.GetEntitiesRequest.make
              ?namespaceVersion:(Option.map ~f:Values.Version.of_json
                                   namespaceVersion)
              ~ids:(Values.Urns.of_json ids) ())
           (Some Values.GetEntitiesResponse.to_json)
           (Some Values.GetEntitiesResponse.error_to_json)])
let get_flow_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and revisionNumber =
         flag "revision-number" (optional json_arg) ~doc:"JSON Version"
       and id = flag "id" (required string) ~doc:"STRING Urn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_flow_template
           (Values.GetFlowTemplateRequest.make
              ?revisionNumber:(Option.map ~f:Values.Version.of_json
                                 revisionNumber) ~id ())
           (Some Values.GetFlowTemplateResponse.to_json)
           (Some Values.GetFlowTemplateResponse.error_to_json)])
let get_flow_template_revisions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and id = flag "id" (required string) ~doc:"STRING Urn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_flow_template_revisions
           (Values.GetFlowTemplateRevisionsRequest.make ?nextToken
              ?maxResults ~id ())
           (Some Values.GetFlowTemplateRevisionsResponse.to_json)
           (Some Values.GetFlowTemplateRevisionsResponse.error_to_json)])
let get_namespace_deletion_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_namespace_deletion_status
           (Values.GetNamespaceDeletionStatusRequest.make ())
           (Some Values.GetNamespaceDeletionStatusResponse.to_json)
           (Some Values.GetNamespaceDeletionStatusResponse.error_to_json)])
let get_system_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING Urn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_system_instance
           (Values.GetSystemInstanceRequest.make ~id ())
           (Some Values.GetSystemInstanceResponse.to_json)
           (Some Values.GetSystemInstanceResponse.error_to_json)])
let get_system_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and revisionNumber =
         flag "revision-number" (optional json_arg) ~doc:"JSON Version"
       and id = flag "id" (required string) ~doc:"STRING Urn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_system_template
           (Values.GetSystemTemplateRequest.make
              ?revisionNumber:(Option.map ~f:Values.Version.of_json
                                 revisionNumber) ~id ())
           (Some Values.GetSystemTemplateResponse.to_json)
           (Some Values.GetSystemTemplateResponse.error_to_json)])
let get_system_template_revisions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and id = flag "id" (required string) ~doc:"STRING Urn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_system_template_revisions
           (Values.GetSystemTemplateRevisionsRequest.make ?nextToken
              ?maxResults ~id ())
           (Some Values.GetSystemTemplateRevisionsResponse.to_json)
           (Some Values.GetSystemTemplateRevisionsResponse.error_to_json)])
let get_upload_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and uploadId =
         flag "upload-id" (required string) ~doc:"STRING UploadId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_upload_status
           (Values.GetUploadStatusRequest.make ~uploadId ())
           (Some Values.GetUploadStatusResponse.to_json)
           (Some Values.GetUploadStatusResponse.error_to_json)])
let list_flow_execution_messages =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and flowExecutionId =
         flag "flow-execution-id" (required string)
           ~doc:"STRING FlowExecutionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_flow_execution_messages
           (Values.ListFlowExecutionMessagesRequest.make ?nextToken
              ?maxResults ~flowExecutionId ())
           (Some Values.ListFlowExecutionMessagesResponse.to_json)
           (Some Values.ListFlowExecutionMessagesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ?maxResults ?nextToken
              ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let search_entities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON EntityFilters"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and namespaceVersion =
         flag "namespace-version" (optional json_arg) ~doc:"JSON Version"
       and entityTypes =
         flag "entity-types" (required json_arg) ~doc:"JSON EntityTypes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_entities
           (Values.SearchEntitiesRequest.make
              ?filters:(Option.map ~f:Values.EntityFilters.of_json filters)
              ?nextToken ?maxResults
              ?namespaceVersion:(Option.map ~f:Values.Version.of_json
                                   namespaceVersion)
              ~entityTypes:(Values.EntityTypes.of_json entityTypes) ())
           (Some Values.SearchEntitiesResponse.to_json)
           (Some Values.SearchEntitiesResponse.error_to_json)])
let search_flow_executions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and flowExecutionId =
         flag "flow-execution-id" (optional string)
           ~doc:"STRING FlowExecutionId"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (optional json_arg) ~doc:"JSON Timestamp"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and systemInstanceId =
         flag "system-instance-id" (required string) ~doc:"STRING Urn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_flow_executions
           (Values.SearchFlowExecutionsRequest.make ?flowExecutionId
              ?startTime:(Option.map ~f:Values.Timestamp.of_json startTime)
              ?endTime:(Option.map ~f:Values.Timestamp.of_json endTime)
              ?nextToken ?maxResults ~systemInstanceId ())
           (Some Values.SearchFlowExecutionsResponse.to_json)
           (Some Values.SearchFlowExecutionsResponse.error_to_json)])
let search_flow_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FlowTemplateFilters"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_flow_templates
           (Values.SearchFlowTemplatesRequest.make
              ?filters:(Option.map ~f:Values.FlowTemplateFilters.of_json
                          filters) ?nextToken ?maxResults ())
           (Some Values.SearchFlowTemplatesResponse.to_json)
           (Some Values.SearchFlowTemplatesResponse.error_to_json)])
let search_system_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON SystemInstanceFilters"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_system_instances
           (Values.SearchSystemInstancesRequest.make
              ?filters:(Option.map ~f:Values.SystemInstanceFilters.of_json
                          filters) ?nextToken ?maxResults ())
           (Some Values.SearchSystemInstancesResponse.to_json)
           (Some Values.SearchSystemInstancesResponse.error_to_json)])
let search_system_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON SystemTemplateFilters"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_system_templates
           (Values.SearchSystemTemplatesRequest.make
              ?filters:(Option.map ~f:Values.SystemTemplateFilters.of_json
                          filters) ?nextToken ?maxResults ())
           (Some Values.SearchSystemTemplatesResponse.to_json)
           (Some Values.SearchSystemTemplatesResponse.error_to_json)])
let search_things =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and namespaceVersion =
         flag "namespace-version" (optional json_arg) ~doc:"JSON Version"
       and entityId = flag "entity-id" (required string) ~doc:"STRING Urn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_things
           (Values.SearchThingsRequest.make ?nextToken ?maxResults
              ?namespaceVersion:(Option.map ~f:Values.Version.of_json
                                   namespaceVersion) ~entityId ())
           (Some Values.SearchThingsResponse.to_json)
           (Some Values.SearchThingsResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let undeploy_system_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (optional string) ~doc:"STRING Urn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.undeploy_system_instance
           (Values.UndeploySystemInstanceRequest.make ?id ())
           (Some Values.UndeploySystemInstanceResponse.to_json)
           (Some Values.UndeploySystemInstanceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_flow_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and compatibleNamespaceVersion =
         flag "compatible-namespace-version" (optional json_arg)
           ~doc:"JSON Version"
       and id = flag "id" (required string) ~doc:"STRING Urn"
       and definition =
         flag "definition" (required json_arg) ~doc:"JSON DefinitionDocument" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_flow_template
           (Values.UpdateFlowTemplateRequest.make
              ?compatibleNamespaceVersion:(Option.map
                                             ~f:Values.Version.of_json
                                             compatibleNamespaceVersion) ~id
              ~definition:(Values.DefinitionDocument.of_json definition) ())
           (Some Values.UpdateFlowTemplateResponse.to_json)
           (Some Values.UpdateFlowTemplateResponse.error_to_json)])
let update_system_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and compatibleNamespaceVersion =
         flag "compatible-namespace-version" (optional json_arg)
           ~doc:"JSON Version"
       and id = flag "id" (required string) ~doc:"STRING Urn"
       and definition =
         flag "definition" (required json_arg) ~doc:"JSON DefinitionDocument" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_system_template
           (Values.UpdateSystemTemplateRequest.make
              ?compatibleNamespaceVersion:(Option.map
                                             ~f:Values.Version.of_json
                                             compatibleNamespaceVersion) ~id
              ~definition:(Values.DefinitionDocument.of_json definition) ())
           (Some Values.UpdateSystemTemplateResponse.to_json)
           (Some Values.UpdateSystemTemplateResponse.error_to_json)])
let upload_entity_definitions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and document =
         flag "document" (optional json_arg) ~doc:"JSON DefinitionDocument"
       and syncWithPublicNamespace =
         flag "sync-with-public-namespace" (optional bool)
           ~doc:"BOOL SyncWithPublicNamespace"
       and deprecateExistingEntities =
         flag "deprecate-existing-entities" (optional bool)
           ~doc:"BOOL DeprecateExistingEntities" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.upload_entity_definitions
           (Values.UploadEntityDefinitionsRequest.make
              ?document:(Option.map ~f:Values.DefinitionDocument.of_json
                           document) ?syncWithPublicNamespace
              ?deprecateExistingEntities ())
           (Some Values.UploadEntityDefinitionsResponse.to_json)
           (Some Values.UploadEntityDefinitionsResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-entity-to-thing", associate_entity_to_thing);
    ("create-flow-template", create_flow_template);
    ("create-system-instance", create_system_instance);
    ("create-system-template", create_system_template);
    ("delete-flow-template", delete_flow_template);
    ("delete-namespace", delete_namespace);
    ("delete-system-instance", delete_system_instance);
    ("delete-system-template", delete_system_template);
    ("deploy-system-instance", deploy_system_instance);
    ("deprecate-flow-template", deprecate_flow_template);
    ("deprecate-system-template", deprecate_system_template);
    ("describe-namespace", describe_namespace);
    ("dissociate-entity-from-thing", dissociate_entity_from_thing);
    ("get-entities", get_entities);
    ("get-flow-template", get_flow_template);
    ("get-flow-template-revisions", get_flow_template_revisions);
    ("get-namespace-deletion-status", get_namespace_deletion_status);
    ("get-system-instance", get_system_instance);
    ("get-system-template", get_system_template);
    ("get-system-template-revisions", get_system_template_revisions);
    ("get-upload-status", get_upload_status);
    ("list-flow-execution-messages", list_flow_execution_messages);
    ("list-tags-for-resource", list_tags_for_resource);
    ("search-entities", search_entities);
    ("search-flow-executions", search_flow_executions);
    ("search-flow-templates", search_flow_templates);
    ("search-system-instances", search_system_instances);
    ("search-system-templates", search_system_templates);
    ("search-things", search_things);
    ("tag-resource", tag_resource);
    ("undeploy-system-instance", undeploy_system_instance);
    ("untag-resource", untag_resource);
    ("update-flow-template", update_flow_template);
    ("update-system-template", update_system_template);
    ("upload-entity-definitions", upload_entity_definitions)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_team_member =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and remoteAccessAllowed =
         flag "remote-access-allowed" (optional bool)
           ~doc:"BOOL RemoteAccessAllowed"
       and projectId =
         flag "project-id" (required string) ~doc:"STRING ProjectId"
       and userArn = flag "user-arn" (required string) ~doc:"STRING UserArn"
       and projectRole =
         flag "project-role" (required string) ~doc:"STRING Role" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_team_member
           (Values.AssociateTeamMemberRequest.make ?clientRequestToken
              ?remoteAccessAllowed ~projectId ~userArn ~projectRole ())
           (Some Values.AssociateTeamMemberResult.to_json)
           (Some Values.AssociateTeamMemberResult.error_to_json)])
let create_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ProjectDescription"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and sourceCode =
         flag "source-code" (optional json_arg) ~doc:"JSON SourceCode"
       and toolchain =
         flag "toolchain" (optional json_arg) ~doc:"JSON Toolchain"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and name = flag "name" (required string) ~doc:"STRING ProjectName"
       and id = flag "id" (required string) ~doc:"STRING ProjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_project
           (Values.CreateProjectRequest.make ?description ?clientRequestToken
              ?sourceCode:(Option.map ~f:Values.SourceCode.of_json sourceCode)
              ?toolchain:(Option.map ~f:Values.Toolchain.of_json toolchain)
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~name ~id ())
           (Some Values.CreateProjectResult.to_json)
           (Some Values.CreateProjectResult.error_to_json)])
let create_user_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sshPublicKey =
         flag "ssh-public-key" (optional string) ~doc:"STRING SshPublicKey"
       and userArn = flag "user-arn" (required string) ~doc:"STRING UserArn"
       and displayName =
         flag "display-name" (required string)
           ~doc:"STRING UserProfileDisplayName"
       and emailAddress =
         flag "email-address" (required string) ~doc:"STRING Email" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_user_profile
           (Values.CreateUserProfileRequest.make ?sshPublicKey ~userArn
              ~displayName ~emailAddress ())
           (Some Values.CreateUserProfileResult.to_json)
           (Some Values.CreateUserProfileResult.error_to_json)])
let delete_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and deleteStack =
         flag "delete-stack" (optional bool) ~doc:"BOOL DeleteStack"
       and id = flag "id" (required string) ~doc:"STRING ProjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_project
           (Values.DeleteProjectRequest.make ?clientRequestToken ?deleteStack
              ~id ()) (Some Values.DeleteProjectResult.to_json)
           (Some Values.DeleteProjectResult.error_to_json)])
let delete_user_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userArn = flag "user-arn" (required string) ~doc:"STRING UserArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_user_profile
           (Values.DeleteUserProfileRequest.make ~userArn ())
           (Some Values.DeleteUserProfileResult.to_json)
           (Some Values.DeleteUserProfileResult.error_to_json)])
let describe_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ProjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_project (Values.DescribeProjectRequest.make ~id ())
           (Some Values.DescribeProjectResult.to_json)
           (Some Values.DescribeProjectResult.error_to_json)])
let describe_user_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and userArn = flag "user-arn" (required string) ~doc:"STRING UserArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_user_profile
           (Values.DescribeUserProfileRequest.make ~userArn ())
           (Some Values.DescribeUserProfileResult.to_json)
           (Some Values.DescribeUserProfileResult.error_to_json)])
let disassociate_team_member =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectId =
         flag "project-id" (required string) ~doc:"STRING ProjectId"
       and userArn = flag "user-arn" (required string) ~doc:"STRING UserArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_team_member
           (Values.DisassociateTeamMemberRequest.make ~projectId ~userArn ())
           (Some Values.DisassociateTeamMemberResult.to_json)
           (Some Values.DisassociateTeamMemberResult.error_to_json)])
let list_projects =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_projects
           (Values.ListProjectsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListProjectsResult.to_json)
           (Some Values.ListProjectsResult.error_to_json)])
let list_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and projectId =
         flag "project-id" (required string) ~doc:"STRING ProjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resources
           (Values.ListResourcesRequest.make ?nextToken ?maxResults
              ~projectId ()) (Some Values.ListResourcesResult.to_json)
           (Some Values.ListResourcesResult.error_to_json)])
let list_tags_for_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and id = flag "id" (required string) ~doc:"STRING ProjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_project
           (Values.ListTagsForProjectRequest.make ?nextToken ?maxResults ~id
              ()) (Some Values.ListTagsForProjectResult.to_json)
           (Some Values.ListTagsForProjectResult.error_to_json)])
let list_team_members =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and projectId =
         flag "project-id" (required string) ~doc:"STRING ProjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_team_members
           (Values.ListTeamMembersRequest.make ?nextToken ?maxResults
              ~projectId ()) (Some Values.ListTeamMembersResult.to_json)
           (Some Values.ListTeamMembersResult.error_to_json)])
let list_user_profiles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_user_profiles
           (Values.ListUserProfilesRequest.make ?nextToken ?maxResults ())
           (Some Values.ListUserProfilesResult.to_json)
           (Some Values.ListUserProfilesResult.error_to_json)])
let tag_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ProjectId"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_project
           (Values.TagProjectRequest.make ~id
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagProjectResult.to_json)
           (Some Values.TagProjectResult.error_to_json)])
let untag_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ProjectId"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_project
           (Values.UntagProjectRequest.make ~id
              ~tags:(Values.TagKeys.of_json tags) ())
           (Some Values.UntagProjectResult.to_json)
           (Some Values.UntagProjectResult.error_to_json)])
let update_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING ProjectName"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ProjectDescription"
       and id = flag "id" (required string) ~doc:"STRING ProjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_project
           (Values.UpdateProjectRequest.make ?name ?description ~id ())
           (Some Values.UpdateProjectResult.to_json)
           (Some Values.UpdateProjectResult.error_to_json)])
let update_team_member =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectRole =
         flag "project-role" (optional string) ~doc:"STRING Role"
       and remoteAccessAllowed =
         flag "remote-access-allowed" (optional bool)
           ~doc:"BOOL RemoteAccessAllowed"
       and projectId =
         flag "project-id" (required string) ~doc:"STRING ProjectId"
       and userArn = flag "user-arn" (required string) ~doc:"STRING UserArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_team_member
           (Values.UpdateTeamMemberRequest.make ?projectRole
              ?remoteAccessAllowed ~projectId ~userArn ())
           (Some Values.UpdateTeamMemberResult.to_json)
           (Some Values.UpdateTeamMemberResult.error_to_json)])
let update_user_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and displayName =
         flag "display-name" (optional string)
           ~doc:"STRING UserProfileDisplayName"
       and emailAddress =
         flag "email-address" (optional string) ~doc:"STRING Email"
       and sshPublicKey =
         flag "ssh-public-key" (optional string) ~doc:"STRING SshPublicKey"
       and userArn = flag "user-arn" (required string) ~doc:"STRING UserArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_user_profile
           (Values.UpdateUserProfileRequest.make ?displayName ?emailAddress
              ?sshPublicKey ~userArn ())
           (Some Values.UpdateUserProfileResult.to_json)
           (Some Values.UpdateUserProfileResult.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-team-member", associate_team_member);
    ("create-project", create_project);
    ("create-user-profile", create_user_profile);
    ("delete-project", delete_project);
    ("delete-user-profile", delete_user_profile);
    ("describe-project", describe_project);
    ("describe-user-profile", describe_user_profile);
    ("disassociate-team-member", disassociate_team_member);
    ("list-projects", list_projects);
    ("list-resources", list_resources);
    ("list-tags-for-project", list_tags_for_project);
    ("list-team-members", list_team_members);
    ("list-user-profiles", list_user_profiles);
    ("tag-project", tag_project);
    ("untag-project", untag_project);
    ("update-project", update_project);
    ("update-team-member", update_team_member);
    ("update-user-profile", update_user_profile)]
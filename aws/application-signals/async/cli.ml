(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_get_service_level_objective_budget_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and timestamp =
         flag "timestamp" (required json_arg) ~doc:"JSON Timestamp"
       and sloIds =
         flag "slo-ids" (required json_arg)
           ~doc:"JSON ServiceLevelObjectiveIds" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_service_level_objective_budget_report
           (Values.BatchGetServiceLevelObjectiveBudgetReportInput.make
              ~timestamp:(Values.Timestamp.of_json timestamp)
              ~sloIds:(Values.ServiceLevelObjectiveIds.of_json sloIds) ())
           (Some
              Values.BatchGetServiceLevelObjectiveBudgetReportOutput.to_json)
           (Some
              Values.BatchGetServiceLevelObjectiveBudgetReportOutput.error_to_json)])
let batch_update_exclusion_windows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and addExclusionWindows =
         flag "add-exclusion-windows" (optional json_arg)
           ~doc:"JSON ExclusionWindows"
       and removeExclusionWindows =
         flag "remove-exclusion-windows" (optional json_arg)
           ~doc:"JSON ExclusionWindows"
       and sloIds =
         flag "slo-ids" (required json_arg)
           ~doc:"JSON ServiceLevelObjectiveIds" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_update_exclusion_windows
           (Values.BatchUpdateExclusionWindowsInput.make
              ?addExclusionWindows:(Option.map
                                      ~f:Values.ExclusionWindows.of_json
                                      addExclusionWindows)
              ?removeExclusionWindows:(Option.map
                                         ~f:Values.ExclusionWindows.of_json
                                         removeExclusionWindows)
              ~sloIds:(Values.ServiceLevelObjectiveIds.of_json sloIds) ())
           (Some Values.BatchUpdateExclusionWindowsOutput.to_json)
           (Some Values.BatchUpdateExclusionWindowsOutput.error_to_json)])
let create_service_level_objective =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ServiceLevelObjectiveDescription"
       and sliConfig =
         flag "sli-config" (optional json_arg)
           ~doc:"JSON ServiceLevelIndicatorConfig"
       and requestBasedSliConfig =
         flag "request-based-sli-config" (optional json_arg)
           ~doc:"JSON RequestBasedServiceLevelIndicatorConfig"
       and goal = flag "goal" (optional json_arg) ~doc:"JSON Goal"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and burnRateConfigurations =
         flag "burn-rate-configurations" (optional json_arg)
           ~doc:"JSON BurnRateConfigurations"
       and createRecommendedSlo =
         flag "create-recommended-slo" (optional bool) ~doc:"BOOL Boolean"
       and autoInvestigationEnabled =
         flag "auto-investigation-enabled" (optional bool)
           ~doc:"BOOL Boolean"
       and name =
         flag "name" (required string)
           ~doc:"STRING ServiceLevelObjectiveName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_service_level_objective
           (Values.CreateServiceLevelObjectiveInput.make ?description
              ?sliConfig:(Option.map
                            ~f:Values.ServiceLevelIndicatorConfig.of_json
                            sliConfig)
              ?requestBasedSliConfig:(Option.map
                                        ~f:Values.RequestBasedServiceLevelIndicatorConfig.of_json
                                        requestBasedSliConfig)
              ?goal:(Option.map ~f:Values.Goal.of_json goal)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?burnRateConfigurations:(Option.map
                                         ~f:Values.BurnRateConfigurations.of_json
                                         burnRateConfigurations)
              ?createRecommendedSlo ?autoInvestigationEnabled ~name ())
           (Some Values.CreateServiceLevelObjectiveOutput.to_json)
           (Some Values.CreateServiceLevelObjectiveOutput.error_to_json)])
let delete_grouping_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_grouping_configuration (Fn.id ())
           (Some Values.DeleteGroupingConfigurationOutput.to_json)
           (Some Values.DeleteGroupingConfigurationOutput.error_to_json)])
let delete_service_level_objective =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id =
         flag "id" (required string) ~doc:"STRING ServiceLevelObjectiveId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_service_level_objective
           (Values.DeleteServiceLevelObjectiveInput.make ~id ())
           (Some Values.DeleteServiceLevelObjectiveOutput.to_json)
           (Some Values.DeleteServiceLevelObjectiveOutput.error_to_json)])
let get_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON Timestamp"
       and keyAttributes =
         flag "key-attributes" (required json_arg) ~doc:"JSON Attributes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service
           (Values.GetServiceInput.make
              ~startTime:(Values.Timestamp.of_json startTime)
              ~endTime:(Values.Timestamp.of_json endTime)
              ~keyAttributes:(Values.Attributes.of_json keyAttributes) ())
           (Some Values.GetServiceOutput.to_json)
           (Some Values.GetServiceOutput.error_to_json)])
let get_service_level_objective =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id =
         flag "id" (required string) ~doc:"STRING ServiceLevelObjectiveId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service_level_objective
           (Values.GetServiceLevelObjectiveInput.make ~id ())
           (Some Values.GetServiceLevelObjectiveOutput.to_json)
           (Some Values.GetServiceLevelObjectiveOutput.error_to_json)])
let list_audit_findings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and auditors =
         flag "auditors" (optional json_arg) ~doc:"JSON Auditors"
       and detailLevel =
         flag "detail-level" (optional json_arg) ~doc:"JSON DetailLevel"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListAuditFindingMaxResults"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON Timestamp"
       and auditTargets =
         flag "audit-targets" (required json_arg) ~doc:"JSON AuditTargets" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_audit_findings
           (Values.ListAuditFindingsInput.make
              ?auditors:(Option.map ~f:Values.Auditors.of_json auditors)
              ?detailLevel:(Option.map ~f:Values.DetailLevel.of_json
                              detailLevel) ?nextToken ?maxResults
              ~startTime:(Values.Timestamp.of_json startTime)
              ~endTime:(Values.Timestamp.of_json endTime)
              ~auditTargets:(Values.AuditTargets.of_json auditTargets) ())
           (Some Values.ListAuditFindingsOutput.to_json)
           (Some Values.ListAuditFindingsOutput.error_to_json)])
let list_entity_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListEntityEventsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and entity = flag "entity" (required json_arg) ~doc:"JSON Attributes"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_entity_events
           (Values.ListEntityEventsInput.make ?maxResults ?nextToken
              ~entity:(Values.Attributes.of_json entity)
              ~startTime:(Values.Timestamp.of_json startTime)
              ~endTime:(Values.Timestamp.of_json endTime) ())
           (Some Values.ListEntityEventsOutput.to_json)
           (Some Values.ListEntityEventsOutput.error_to_json)])
let list_grouping_attribute_definitions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and awsAccountId =
         flag "aws-account-id" (optional string) ~doc:"STRING AwsAccountId"
       and includeLinkedAccounts =
         flag "include-linked-accounts" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_grouping_attribute_definitions
           (Values.ListGroupingAttributeDefinitionsInput.make ?nextToken
              ?awsAccountId ?includeLinkedAccounts ())
           (Some Values.ListGroupingAttributeDefinitionsOutput.to_json)
           (Some Values.ListGroupingAttributeDefinitionsOutput.error_to_json)])
let list_service_dependencies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListServiceDependenciesMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON Timestamp"
       and keyAttributes =
         flag "key-attributes" (required json_arg) ~doc:"JSON Attributes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_dependencies
           (Values.ListServiceDependenciesInput.make ?maxResults ?nextToken
              ~startTime:(Values.Timestamp.of_json startTime)
              ~endTime:(Values.Timestamp.of_json endTime)
              ~keyAttributes:(Values.Attributes.of_json keyAttributes) ())
           (Some Values.ListServiceDependenciesOutput.to_json)
           (Some Values.ListServiceDependenciesOutput.error_to_json)])
let list_service_dependents =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListServiceDependentsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON Timestamp"
       and keyAttributes =
         flag "key-attributes" (required json_arg) ~doc:"JSON Attributes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_dependents
           (Values.ListServiceDependentsInput.make ?maxResults ?nextToken
              ~startTime:(Values.Timestamp.of_json startTime)
              ~endTime:(Values.Timestamp.of_json endTime)
              ~keyAttributes:(Values.Attributes.of_json keyAttributes) ())
           (Some Values.ListServiceDependentsOutput.to_json)
           (Some Values.ListServiceDependentsOutput.error_to_json)])
let list_service_level_objective_exclusion_windows =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListServiceLevelObjectiveExclusionWindowsMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and id =
         flag "id" (required string) ~doc:"STRING ServiceLevelObjectiveId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_level_objective_exclusion_windows
           (Values.ListServiceLevelObjectiveExclusionWindowsInput.make
              ?maxResults ?nextToken ~id ())
           (Some
              Values.ListServiceLevelObjectiveExclusionWindowsOutput.to_json)
           (Some
              Values.ListServiceLevelObjectiveExclusionWindowsOutput.error_to_json)])
let list_service_level_objectives =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and keyAttributes =
         flag "key-attributes" (optional json_arg) ~doc:"JSON Attributes"
       and operationName =
         flag "operation-name" (optional string) ~doc:"STRING OperationName"
       and dependencyConfig =
         flag "dependency-config" (optional json_arg)
           ~doc:"JSON DependencyConfig"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListServiceLevelObjectivesMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and metricSourceTypes =
         flag "metric-source-types" (optional json_arg)
           ~doc:"JSON MetricSourceTypes"
       and includeLinkedAccounts =
         flag "include-linked-accounts" (optional bool) ~doc:"BOOL Boolean"
       and sloOwnerAwsAccountId =
         flag "slo-owner-aws-account-id" (optional string)
           ~doc:"STRING AwsAccountId"
       and metricSource =
         flag "metric-source" (optional json_arg) ~doc:"JSON MetricSource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_level_objectives
           (Values.ListServiceLevelObjectivesInput.make
              ?keyAttributes:(Option.map ~f:Values.Attributes.of_json
                                keyAttributes) ?operationName
              ?dependencyConfig:(Option.map
                                   ~f:Values.DependencyConfig.of_json
                                   dependencyConfig) ?maxResults ?nextToken
              ?metricSourceTypes:(Option.map
                                    ~f:Values.MetricSourceTypes.of_json
                                    metricSourceTypes) ?includeLinkedAccounts
              ?sloOwnerAwsAccountId
              ?metricSource:(Option.map ~f:Values.MetricSource.of_json
                               metricSource) ())
           (Some Values.ListServiceLevelObjectivesOutput.to_json)
           (Some Values.ListServiceLevelObjectivesOutput.error_to_json)])
let list_service_operations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListServiceOperationMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON Timestamp"
       and keyAttributes =
         flag "key-attributes" (required json_arg) ~doc:"JSON Attributes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_operations
           (Values.ListServiceOperationsInput.make ?maxResults ?nextToken
              ~startTime:(Values.Timestamp.of_json startTime)
              ~endTime:(Values.Timestamp.of_json endTime)
              ~keyAttributes:(Values.Attributes.of_json keyAttributes) ())
           (Some Values.ListServiceOperationsOutput.to_json)
           (Some Values.ListServiceOperationsOutput.error_to_json)])
let list_service_states =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListServiceStatesMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and includeLinkedAccounts =
         flag "include-linked-accounts" (optional bool) ~doc:"BOOL Boolean"
       and awsAccountId =
         flag "aws-account-id" (optional string) ~doc:"STRING AwsAccountId"
       and attributeFilters =
         flag "attribute-filters" (optional json_arg)
           ~doc:"JSON AttributeFilters"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_states
           (Values.ListServiceStatesInput.make ?maxResults ?nextToken
              ?includeLinkedAccounts ?awsAccountId
              ?attributeFilters:(Option.map
                                   ~f:Values.AttributeFilters.of_json
                                   attributeFilters)
              ~startTime:(Values.Timestamp.of_json startTime)
              ~endTime:(Values.Timestamp.of_json endTime) ())
           (Some Values.ListServiceStatesOutput.to_json)
           (Some Values.ListServiceStatesOutput.error_to_json)])
let list_services =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListServicesMaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and includeLinkedAccounts =
         flag "include-linked-accounts" (optional bool) ~doc:"BOOL Boolean"
       and awsAccountId =
         flag "aws-account-id" (optional string) ~doc:"STRING AwsAccountId"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_services
           (Values.ListServicesInput.make ?maxResults ?nextToken
              ?includeLinkedAccounts ?awsAccountId
              ~startTime:(Values.Timestamp.of_json startTime)
              ~endTime:(Values.Timestamp.of_json endTime) ())
           (Some Values.ListServicesOutput.to_json)
           (Some Values.ListServicesOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_grouping_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and groupingAttributeDefinitions =
         flag "grouping-attribute-definitions" (required json_arg)
           ~doc:"JSON GroupingAttributeDefinitions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_grouping_configuration
           (Values.PutGroupingConfigurationInput.make
              ~groupingAttributeDefinitions:(Values.GroupingAttributeDefinitions.of_json
                                               groupingAttributeDefinitions)
              ()) (Some Values.PutGroupingConfigurationOutput.to_json)
           (Some Values.PutGroupingConfigurationOutput.error_to_json)])
let start_discovery =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_discovery (Values.StartDiscoveryInput.make ())
           (Some Values.StartDiscoveryOutput.to_json)
           (Some Values.StartDiscoveryOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_service_level_objective =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING ServiceLevelObjectiveDescription"
       and sliConfig =
         flag "sli-config" (optional json_arg)
           ~doc:"JSON ServiceLevelIndicatorConfig"
       and requestBasedSliConfig =
         flag "request-based-sli-config" (optional json_arg)
           ~doc:"JSON RequestBasedServiceLevelIndicatorConfig"
       and goal = flag "goal" (optional json_arg) ~doc:"JSON Goal"
       and burnRateConfigurations =
         flag "burn-rate-configurations" (optional json_arg)
           ~doc:"JSON BurnRateConfigurations"
       and autoInvestigationEnabled =
         flag "auto-investigation-enabled" (optional bool)
           ~doc:"BOOL Boolean"
       and id =
         flag "id" (required string) ~doc:"STRING ServiceLevelObjectiveId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_service_level_objective
           (Values.UpdateServiceLevelObjectiveInput.make ?description
              ?sliConfig:(Option.map
                            ~f:Values.ServiceLevelIndicatorConfig.of_json
                            sliConfig)
              ?requestBasedSliConfig:(Option.map
                                        ~f:Values.RequestBasedServiceLevelIndicatorConfig.of_json
                                        requestBasedSliConfig)
              ?goal:(Option.map ~f:Values.Goal.of_json goal)
              ?burnRateConfigurations:(Option.map
                                         ~f:Values.BurnRateConfigurations.of_json
                                         burnRateConfigurations)
              ?autoInvestigationEnabled ~id ())
           (Some Values.UpdateServiceLevelObjectiveOutput.to_json)
           (Some Values.UpdateServiceLevelObjectiveOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("batch-get-service-level-objective-budget-report",
       batch_get_service_level_objective_budget_report);
    ("batch-update-exclusion-windows", batch_update_exclusion_windows);
    ("create-service-level-objective", create_service_level_objective);
    ("delete-grouping-configuration", delete_grouping_configuration);
    ("delete-service-level-objective", delete_service_level_objective);
    ("get-service", get_service);
    ("get-service-level-objective", get_service_level_objective);
    ("list-audit-findings", list_audit_findings);
    ("list-entity-events", list_entity_events);
    ("list-grouping-attribute-definitions",
      list_grouping_attribute_definitions);
    ("list-service-dependencies", list_service_dependencies);
    ("list-service-dependents", list_service_dependents);
    ("list-service-level-objective-exclusion-windows",
      list_service_level_objective_exclusion_windows);
    ("list-service-level-objectives", list_service_level_objectives);
    ("list-service-operations", list_service_operations);
    ("list-service-states", list_service_states);
    ("list-services", list_services);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-grouping-configuration", put_grouping_configuration);
    ("start-discovery", start_discovery);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-service-level-objective", update_service_level_objective)]
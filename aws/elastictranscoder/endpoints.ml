(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | CancelJob: (CancelJobRequest.t, CancelJobResponse.t,
  CancelJobResponse.error) t 
  | CreateJob: (CreateJobRequest.t, CreateJobResponse.t,
  CreateJobResponse.error) t 
  | CreatePipeline: (CreatePipelineRequest.t, CreatePipelineResponse.t,
  CreatePipelineResponse.error) t 
  | CreatePreset: (CreatePresetRequest.t, CreatePresetResponse.t,
  CreatePresetResponse.error) t 
  | DeletePipeline: (DeletePipelineRequest.t, DeletePipelineResponse.t,
  DeletePipelineResponse.error) t 
  | DeletePreset: (DeletePresetRequest.t, DeletePresetResponse.t,
  DeletePresetResponse.error) t 
  | ListJobsByPipeline: (ListJobsByPipelineRequest.t,
  ListJobsByPipelineResponse.t, ListJobsByPipelineResponse.error) t 
  | ListJobsByStatus: (ListJobsByStatusRequest.t, ListJobsByStatusResponse.t,
  ListJobsByStatusResponse.error) t 
  | ListPipelines: (ListPipelinesRequest.t, ListPipelinesResponse.t,
  ListPipelinesResponse.error) t 
  | ListPresets: (ListPresetsRequest.t, ListPresetsResponse.t,
  ListPresetsResponse.error) t 
  | ReadJob: (ReadJobRequest.t, ReadJobResponse.t, ReadJobResponse.error) t 
  | ReadPipeline: (ReadPipelineRequest.t, ReadPipelineResponse.t,
  ReadPipelineResponse.error) t 
  | ReadPreset: (ReadPresetRequest.t, ReadPresetResponse.t,
  ReadPresetResponse.error) t 
  | TestRole: (TestRoleRequest.t, TestRoleResponse.t, TestRoleResponse.error)
  t 
  | UpdatePipeline: (UpdatePipelineRequest.t, UpdatePipelineResponse.t,
  UpdatePipelineResponse.error) t 
  | UpdatePipelineNotifications: (UpdatePipelineNotificationsRequest.t,
  UpdatePipelineNotificationsResponse.t,
  UpdatePipelineNotificationsResponse.error) t 
  | UpdatePipelineStatus: (UpdatePipelineStatusRequest.t,
  UpdatePipelineStatusResponse.t, UpdatePipelineStatusResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | CancelJob -> `DELETE
  | CreateJob -> `POST
  | CreatePipeline -> `POST
  | CreatePreset -> `POST
  | DeletePipeline -> `DELETE
  | DeletePreset -> `DELETE
  | ListJobsByPipeline -> `GET
  | ListJobsByStatus -> `GET
  | ListPipelines -> `GET
  | ListPresets -> `GET
  | ReadJob -> `GET
  | ReadPipeline -> `GET
  | ReadPreset -> `GET
  | TestRole -> `POST
  | UpdatePipeline -> `PUT
  | UpdatePipelineNotifications -> `POST
  | UpdatePipelineStatus -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | CancelJob ->
          (Format.kasprintf Uri.of_string) "/2012-09-25/jobs/%s"
            (Id.to_header x.CancelJobRequest.id)
      | CreateJob -> (Format.kasprintf Uri.of_string) "/2012-09-25/jobs"
      | CreatePipeline ->
          (Format.kasprintf Uri.of_string) "/2012-09-25/pipelines"
      | CreatePreset ->
          (Format.kasprintf Uri.of_string) "/2012-09-25/presets"
      | DeletePipeline ->
          (Format.kasprintf Uri.of_string) "/2012-09-25/pipelines/%s"
            (Id.to_header x.DeletePipelineRequest.id)
      | DeletePreset ->
          (Format.kasprintf Uri.of_string) "/2012-09-25/presets/%s"
            (Id.to_header x.DeletePresetRequest.id)
      | ListJobsByPipeline ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/2012-09-25/jobsByPipeline/%s"
               (Id.to_header x.ListJobsByPipelineRequest.pipelineId))
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("Ascending", (Ascending.to_header v)))
                  x.ascending;
               Option.map ~f:(fun v -> ("PageToken", (Id.to_header v)))
                 x.pageToken])
      | ListJobsByStatus ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/2012-09-25/jobsByStatus/%s"
               (JobStatus.to_header x.ListJobsByStatusRequest.status))
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("Ascending", (Ascending.to_header v)))
                  x.ascending;
               Option.map ~f:(fun v -> ("PageToken", (Id.to_header v)))
                 x.pageToken])
      | ListPipelines ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/2012-09-25/pipelines")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("Ascending", (Ascending.to_header v)))
                  x.ascending;
               Option.map ~f:(fun v -> ("PageToken", (Id.to_header v)))
                 x.pageToken])
      | ListPresets ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/2012-09-25/presets")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("Ascending", (Ascending.to_header v)))
                  x.ascending;
               Option.map ~f:(fun v -> ("PageToken", (Id.to_header v)))
                 x.pageToken])
      | ReadJob ->
          (Format.kasprintf Uri.of_string) "/2012-09-25/jobs/%s"
            (Id.to_header x.ReadJobRequest.id)
      | ReadPipeline ->
          (Format.kasprintf Uri.of_string) "/2012-09-25/pipelines/%s"
            (Id.to_header x.ReadPipelineRequest.id)
      | ReadPreset ->
          (Format.kasprintf Uri.of_string) "/2012-09-25/presets/%s"
            (Id.to_header x.ReadPresetRequest.id)
      | TestRole -> (Format.kasprintf Uri.of_string) "/2012-09-25/roleTests"
      | UpdatePipeline ->
          (Format.kasprintf Uri.of_string) "/2012-09-25/pipelines/%s"
            (Id.to_header x.UpdatePipelineRequest.id)
      | UpdatePipelineNotifications ->
          (Format.kasprintf Uri.of_string)
            "/2012-09-25/pipelines/%s/notifications"
            (Id.to_header x.UpdatePipelineNotificationsRequest.id)
      | UpdatePipelineStatus ->
          (Format.kasprintf Uri.of_string) "/2012-09-25/pipelines/%s/status"
            (Id.to_header x.UpdatePipelineStatusRequest.id))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | CancelJob -> Awso.Http.Request.make (method_of_endpoint endp)
  | CreateJob ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("PipelineId",
                           (Id.to_value req.CreateJobRequest.pipelineId));
                      Option.map req.CreateJobRequest.input
                        ~f:(fun x -> ("Input", (JobInput.to_value x)));
                      Option.map req.CreateJobRequest.inputs
                        ~f:(fun x -> ("Inputs", (JobInputs.to_value x)));
                      Option.map req.CreateJobRequest.output
                        ~f:(fun x -> ("Output", (CreateJobOutput.to_value x)));
                      Option.map req.CreateJobRequest.outputs
                        ~f:(fun x ->
                              ("Outputs", (CreateJobOutputs.to_value x)));
                      Option.map req.CreateJobRequest.outputKeyPrefix
                        ~f:(fun x -> ("OutputKeyPrefix", (Key.to_value x)));
                      Option.map req.CreateJobRequest.playlists
                        ~f:(fun x ->
                              ("Playlists", (CreateJobPlaylists.to_value x)));
                      Option.map req.CreateJobRequest.userMetadata
                        ~f:(fun x ->
                              ("UserMetadata", (UserMetadata.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CreatePipeline ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("Name",
                           (Name.to_value req.CreatePipelineRequest.name));
                      Some
                        ("InputBucket",
                          (BucketName.to_value
                             req.CreatePipelineRequest.inputBucket));
                      Option.map req.CreatePipelineRequest.outputBucket
                        ~f:(fun x ->
                              ("OutputBucket", (BucketName.to_value x)));
                      Some
                        ("Role",
                          (Role.to_value req.CreatePipelineRequest.role));
                      Option.map req.CreatePipelineRequest.awsKmsKeyArn
                        ~f:(fun x -> ("AwsKmsKeyArn", (KeyArn.to_value x)));
                      Option.map req.CreatePipelineRequest.notifications
                        ~f:(fun x ->
                              ("Notifications", (Notifications.to_value x)));
                      Option.map req.CreatePipelineRequest.contentConfig
                        ~f:(fun x ->
                              ("ContentConfig",
                                (PipelineOutputConfig.to_value x)));
                      Option.map req.CreatePipelineRequest.thumbnailConfig
                        ~f:(fun x ->
                              ("ThumbnailConfig",
                                (PipelineOutputConfig.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CreatePreset ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("Name",
                           (Name.to_value req.CreatePresetRequest.name));
                      Option.map req.CreatePresetRequest.description
                        ~f:(fun x ->
                              ("Description", (Description.to_value x)));
                      Some
                        ("Container",
                          (PresetContainer.to_value
                             req.CreatePresetRequest.container));
                      Option.map req.CreatePresetRequest.video
                        ~f:(fun x -> ("Video", (VideoParameters.to_value x)));
                      Option.map req.CreatePresetRequest.audio
                        ~f:(fun x -> ("Audio", (AudioParameters.to_value x)));
                      Option.map req.CreatePresetRequest.thumbnails
                        ~f:(fun x -> ("Thumbnails", (Thumbnails.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeletePipeline -> Awso.Http.Request.make (method_of_endpoint endp)
  | DeletePreset -> Awso.Http.Request.make (method_of_endpoint endp)
  | ListJobsByPipeline ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListJobsByStatus ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListPipelines ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListPresets ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ReadJob ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ReadPipeline ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ReadPreset ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | TestRole ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("Role", (Role.to_value req.TestRoleRequest.role));
                      Some
                        ("InputBucket",
                          (BucketName.to_value
                             req.TestRoleRequest.inputBucket));
                      Some
                        ("OutputBucket",
                          (BucketName.to_value
                             req.TestRoleRequest.outputBucket));
                      Some
                        ("Topics",
                          (SnsTopics.to_value req.TestRoleRequest.topics))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UpdatePipeline -> Awso.Http.Request.make (method_of_endpoint endp)
  | UpdatePipelineNotifications ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("Notifications",
                           (Notifications.to_value
                              req.UpdatePipelineNotificationsRequest.notifications))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UpdatePipelineStatus ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("Status",
                           (PipelineStatus.to_value
                              req.UpdatePipelineStatusRequest.status))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : (Awso.Http.Response.t, Awso.Http.Io.Error.call) result) :
  (o, [ `AWS of e  | `Transport of Awso.Http.Io.Error.call ]) result=
  let handle_error err error_of_json =
    match err with
    | `Too_many_redirects -> Error (`Transport `Too_many_redirects)
    | `Bad_response
        { Awso.Http.Io.Error.code = code; body; x_amzn_error_type } ->
        let generic_error () =
          Error
            (`Transport
               (`Bad_response
                  { Awso.Http.Io.Error.code = code; body; x_amzn_error_type })) in
        (match (x_amzn_error_type, error_of_json,
                 ((code >= 400) && (code <= 599)))
         with
         | (Some error_type, Some error_of_json, true) ->
             let json = Yojson.Safe.from_string body in
             Error (`AWS (error_of_json error_type json))
         | (None, Some error_of_json, true) ->
             (try
                let json = Yojson.Safe.from_string body in
                match json |> (Yojson.Safe.Util.member "__type") with
                | `String error_type ->
                    let error_type =
                      match String.lsplit2 error_type ~on:'#' with
                      | Some (_, s) -> s
                      | None -> error_type in
                    Error (`AWS (error_of_json error_type json))
                | `Null -> generic_error ()
                | _ ->
                    failwithf "Error '__type' did not have string type: %s"
                      body ()
              with | _ -> generic_error ())
         | (None, _, _) | (_, None, _) | (_, _, false) -> generic_error ()) in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = resp in
  let _ = handle_error in
  let _ = response_to_json in
  match endpoint with
  | CancelJob ->
      (match resp with
       | Error err -> handle_error err (Some CancelJobResponse.error_of_json)
       | Ok resp ->
           let headers =
             Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
           Ok (CancelJobResponse.of_header_and_body (headers, ())))
  | CreateJob ->
      (match resp with
       | Error err -> handle_error err (Some CreateJobResponse.error_of_json)
       | Ok resp -> Ok (CreateJobResponse.of_json (response_to_json resp)))
  | CreatePipeline ->
      (match resp with
       | Error err ->
           handle_error err (Some CreatePipelineResponse.error_of_json)
       | Ok resp ->
           Ok (CreatePipelineResponse.of_json (response_to_json resp)))
  | CreatePreset ->
      (match resp with
       | Error err ->
           handle_error err (Some CreatePresetResponse.error_of_json)
       | Ok resp -> Ok (CreatePresetResponse.of_json (response_to_json resp)))
  | DeletePipeline ->
      (match resp with
       | Error err ->
           handle_error err (Some DeletePipelineResponse.error_of_json)
       | Ok resp ->
           let headers =
             Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
           Ok (DeletePipelineResponse.of_header_and_body (headers, ())))
  | DeletePreset ->
      (match resp with
       | Error err ->
           handle_error err (Some DeletePresetResponse.error_of_json)
       | Ok resp ->
           let headers =
             Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
           Ok (DeletePresetResponse.of_header_and_body (headers, ())))
  | ListJobsByPipeline ->
      (match resp with
       | Error err ->
           handle_error err (Some ListJobsByPipelineResponse.error_of_json)
       | Ok resp ->
           Ok (ListJobsByPipelineResponse.of_json (response_to_json resp)))
  | ListJobsByStatus ->
      (match resp with
       | Error err ->
           handle_error err (Some ListJobsByStatusResponse.error_of_json)
       | Ok resp ->
           Ok (ListJobsByStatusResponse.of_json (response_to_json resp)))
  | ListPipelines ->
      (match resp with
       | Error err ->
           handle_error err (Some ListPipelinesResponse.error_of_json)
       | Ok resp ->
           Ok (ListPipelinesResponse.of_json (response_to_json resp)))
  | ListPresets ->
      (match resp with
       | Error err ->
           handle_error err (Some ListPresetsResponse.error_of_json)
       | Ok resp -> Ok (ListPresetsResponse.of_json (response_to_json resp)))
  | ReadJob ->
      (match resp with
       | Error err -> handle_error err (Some ReadJobResponse.error_of_json)
       | Ok resp -> Ok (ReadJobResponse.of_json (response_to_json resp)))
  | ReadPipeline ->
      (match resp with
       | Error err ->
           handle_error err (Some ReadPipelineResponse.error_of_json)
       | Ok resp -> Ok (ReadPipelineResponse.of_json (response_to_json resp)))
  | ReadPreset ->
      (match resp with
       | Error err ->
           handle_error err (Some ReadPresetResponse.error_of_json)
       | Ok resp -> Ok (ReadPresetResponse.of_json (response_to_json resp)))
  | TestRole ->
      (match resp with
       | Error err -> handle_error err (Some TestRoleResponse.error_of_json)
       | Ok resp -> Ok (TestRoleResponse.of_json (response_to_json resp)))
  | UpdatePipeline ->
      (match resp with
       | Error err ->
           handle_error err (Some UpdatePipelineResponse.error_of_json)
       | Ok resp ->
           Ok (UpdatePipelineResponse.of_json (response_to_json resp)))
  | UpdatePipelineNotifications ->
      (match resp with
       | Error err ->
           handle_error err
             (Some UpdatePipelineNotificationsResponse.error_of_json)
       | Ok resp ->
           Ok
             (UpdatePipelineNotificationsResponse.of_json
                (response_to_json resp)))
  | UpdatePipelineStatus ->
      (match resp with
       | Error err ->
           handle_error err (Some UpdatePipelineStatusResponse.error_of_json)
       | Ok resp ->
           Ok (UpdatePipelineStatusResponse.of_json (response_to_json resp)))
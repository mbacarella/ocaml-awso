(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let cancel_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_job (Values.CancelJobRequest.make ~id ())
           (Some Values.CancelJobResponse.to_json)
           (Some Values.CancelJobResponse.error_to_json)])
let create_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and input = flag "input" (optional json_arg) ~doc:"JSON JobInput"
       and inputs = flag "inputs" (optional json_arg) ~doc:"JSON JobInputs"
       and output =
         flag "output" (optional json_arg) ~doc:"JSON CreateJobOutput"
       and outputs =
         flag "outputs" (optional json_arg) ~doc:"JSON CreateJobOutputs"
       and outputKeyPrefix =
         flag "output-key-prefix" (optional string) ~doc:"STRING Key"
       and playlists =
         flag "playlists" (optional json_arg) ~doc:"JSON CreateJobPlaylists"
       and userMetadata =
         flag "user-metadata" (optional json_arg) ~doc:"JSON UserMetadata"
       and pipelineId = flag "pipeline-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_job
           (Values.CreateJobRequest.make
              ?input:(Option.map ~f:Values.JobInput.of_json input)
              ?inputs:(Option.map ~f:Values.JobInputs.of_json inputs)
              ?output:(Option.map ~f:Values.CreateJobOutput.of_json output)
              ?outputs:(Option.map ~f:Values.CreateJobOutputs.of_json outputs)
              ?outputKeyPrefix
              ?playlists:(Option.map ~f:Values.CreateJobPlaylists.of_json
                            playlists)
              ?userMetadata:(Option.map ~f:Values.UserMetadata.of_json
                               userMetadata) ~pipelineId ())
           (Some Values.CreateJobResponse.to_json)
           (Some Values.CreateJobResponse.error_to_json)])
let create_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and outputBucket =
         flag "output-bucket" (optional string) ~doc:"STRING BucketName"
       and awsKmsKeyArn =
         flag "aws-kms-key-arn" (optional string) ~doc:"STRING KeyArn"
       and notifications =
         flag "notifications" (optional json_arg) ~doc:"JSON Notifications"
       and contentConfig =
         flag "content-config" (optional json_arg)
           ~doc:"JSON PipelineOutputConfig"
       and thumbnailConfig =
         flag "thumbnail-config" (optional json_arg)
           ~doc:"JSON PipelineOutputConfig"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and inputBucket =
         flag "input-bucket" (required string) ~doc:"STRING BucketName"
       and role = flag "role" (required string) ~doc:"STRING Role" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_pipeline
           (Values.CreatePipelineRequest.make ?outputBucket ?awsKmsKeyArn
              ?notifications:(Option.map ~f:Values.Notifications.of_json
                                notifications)
              ?contentConfig:(Option.map
                                ~f:Values.PipelineOutputConfig.of_json
                                contentConfig)
              ?thumbnailConfig:(Option.map
                                  ~f:Values.PipelineOutputConfig.of_json
                                  thumbnailConfig) ~name ~inputBucket ~role
              ()) (Some Values.CreatePipelineResponse.to_json)
           (Some Values.CreatePipelineResponse.error_to_json)])
let create_preset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and video =
         flag "video" (optional json_arg) ~doc:"JSON VideoParameters"
       and audio =
         flag "audio" (optional json_arg) ~doc:"JSON AudioParameters"
       and thumbnails =
         flag "thumbnails" (optional json_arg) ~doc:"JSON Thumbnails"
       and name = flag "name" (required string) ~doc:"STRING Name"
       and container =
         flag "container" (required string) ~doc:"STRING PresetContainer" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_preset
           (Values.CreatePresetRequest.make ?description
              ?video:(Option.map ~f:Values.VideoParameters.of_json video)
              ?audio:(Option.map ~f:Values.AudioParameters.of_json audio)
              ?thumbnails:(Option.map ~f:Values.Thumbnails.of_json thumbnails)
              ~name ~container ()) (Some Values.CreatePresetResponse.to_json)
           (Some Values.CreatePresetResponse.error_to_json)])
let delete_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_pipeline (Values.DeletePipelineRequest.make ~id ())
           (Some Values.DeletePipelineResponse.to_json)
           (Some Values.DeletePipelineResponse.error_to_json)])
let delete_preset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_preset (Values.DeletePresetRequest.make ~id ())
           (Some Values.DeletePresetResponse.to_json)
           (Some Values.DeletePresetResponse.error_to_json)])
let list_jobs_by_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ascending =
         flag "ascending" (optional string) ~doc:"STRING Ascending"
       and pageToken = flag "page-token" (optional string) ~doc:"STRING Id"
       and pipelineId = flag "pipeline-id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_jobs_by_pipeline
           (Values.ListJobsByPipelineRequest.make ?ascending ?pageToken
              ~pipelineId ())
           (Some Values.ListJobsByPipelineResponse.to_json)
           (Some Values.ListJobsByPipelineResponse.error_to_json)])
let list_jobs_by_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ascending =
         flag "ascending" (optional string) ~doc:"STRING Ascending"
       and pageToken = flag "page-token" (optional string) ~doc:"STRING Id"
       and status = flag "status" (required string) ~doc:"STRING JobStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_jobs_by_status
           (Values.ListJobsByStatusRequest.make ?ascending ?pageToken ~status
              ()) (Some Values.ListJobsByStatusResponse.to_json)
           (Some Values.ListJobsByStatusResponse.error_to_json)])
let list_pipelines =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ascending =
         flag "ascending" (optional string) ~doc:"STRING Ascending"
       and pageToken = flag "page-token" (optional string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_pipelines
           (Values.ListPipelinesRequest.make ?ascending ?pageToken ())
           (Some Values.ListPipelinesResponse.to_json)
           (Some Values.ListPipelinesResponse.error_to_json)])
let list_presets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ascending =
         flag "ascending" (optional string) ~doc:"STRING Ascending"
       and pageToken = flag "page-token" (optional string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_presets
           (Values.ListPresetsRequest.make ?ascending ?pageToken ())
           (Some Values.ListPresetsResponse.to_json)
           (Some Values.ListPresetsResponse.error_to_json)])
let read_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.read_job (Values.ReadJobRequest.make ~id ())
           (Some Values.ReadJobResponse.to_json)
           (Some Values.ReadJobResponse.error_to_json)])
let read_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.read_pipeline (Values.ReadPipelineRequest.make ~id ())
           (Some Values.ReadPipelineResponse.to_json)
           (Some Values.ReadPipelineResponse.error_to_json)])
let read_preset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.read_preset (Values.ReadPresetRequest.make ~id ())
           (Some Values.ReadPresetResponse.to_json)
           (Some Values.ReadPresetResponse.error_to_json)])
let test_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and role = flag "role" (required string) ~doc:"STRING Role"
       and inputBucket =
         flag "input-bucket" (required string) ~doc:"STRING BucketName"
       and outputBucket =
         flag "output-bucket" (required string) ~doc:"STRING BucketName"
       and topics = flag "topics" (required json_arg) ~doc:"JSON SnsTopics" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.test_role
           (Values.TestRoleRequest.make ~role ~inputBucket ~outputBucket
              ~topics:(Values.SnsTopics.of_json topics) ())
           (Some Values.TestRoleResponse.to_json)
           (Some Values.TestRoleResponse.error_to_json)])
let update_pipeline =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING Name"
       and inputBucket =
         flag "input-bucket" (optional string) ~doc:"STRING BucketName"
       and role = flag "role" (optional string) ~doc:"STRING Role"
       and awsKmsKeyArn =
         flag "aws-kms-key-arn" (optional string) ~doc:"STRING KeyArn"
       and notifications =
         flag "notifications" (optional json_arg) ~doc:"JSON Notifications"
       and contentConfig =
         flag "content-config" (optional json_arg)
           ~doc:"JSON PipelineOutputConfig"
       and thumbnailConfig =
         flag "thumbnail-config" (optional json_arg)
           ~doc:"JSON PipelineOutputConfig"
       and id = flag "id" (required string) ~doc:"STRING Id" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_pipeline
           (Values.UpdatePipelineRequest.make ?name ?inputBucket ?role
              ?awsKmsKeyArn
              ?notifications:(Option.map ~f:Values.Notifications.of_json
                                notifications)
              ?contentConfig:(Option.map
                                ~f:Values.PipelineOutputConfig.of_json
                                contentConfig)
              ?thumbnailConfig:(Option.map
                                  ~f:Values.PipelineOutputConfig.of_json
                                  thumbnailConfig) ~id ())
           (Some Values.UpdatePipelineResponse.to_json)
           (Some Values.UpdatePipelineResponse.error_to_json)])
let update_pipeline_notifications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING Id"
       and notifications =
         flag "notifications" (required json_arg) ~doc:"JSON Notifications" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_pipeline_notifications
           (Values.UpdatePipelineNotificationsRequest.make ~id
              ~notifications:(Values.Notifications.of_json notifications) ())
           (Some Values.UpdatePipelineNotificationsResponse.to_json)
           (Some Values.UpdatePipelineNotificationsResponse.error_to_json)])
let update_pipeline_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING Id"
       and status =
         flag "status" (required string) ~doc:"STRING PipelineStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_pipeline_status
           (Values.UpdatePipelineStatusRequest.make ~id ~status ())
           (Some Values.UpdatePipelineStatusResponse.to_json)
           (Some Values.UpdatePipelineStatusResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("cancel-job", cancel_job);
    ("create-job", create_job);
    ("create-pipeline", create_pipeline);
    ("create-preset", create_preset);
    ("delete-pipeline", delete_pipeline);
    ("delete-preset", delete_preset);
    ("list-jobs-by-pipeline", list_jobs_by_pipeline);
    ("list-jobs-by-status", list_jobs_by_status);
    ("list-pipelines", list_pipelines);
    ("list-presets", list_presets);
    ("read-job", read_job);
    ("read-pipeline", read_pipeline);
    ("read-preset", read_preset);
    ("test-role", test_role);
    ("update-pipeline", update_pipeline);
    ("update-pipeline-notifications", update_pipeline_notifications);
    ("update-pipeline-status", update_pipeline_status)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_db_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and username =
         flag "username" (optional string) ~doc:"STRING Username"
       and password =
         flag "password" (optional string) ~doc:"STRING Password"
       and organization =
         flag "organization" (optional string) ~doc:"STRING Organization"
       and bucket = flag "bucket" (optional string) ~doc:"STRING Bucket"
       and port = flag "port" (optional int) ~doc:"INT Port"
       and dbParameterGroupIdentifier =
         flag "db-parameter-group-identifier" (optional string)
           ~doc:"STRING DbParameterGroupIdentifier"
       and dbStorageType =
         flag "db-storage-type" (optional json_arg) ~doc:"JSON DbStorageType"
       and allocatedStorage =
         flag "allocated-storage" (optional int) ~doc:"INT AllocatedStorage"
       and networkType =
         flag "network-type" (optional json_arg) ~doc:"JSON NetworkType"
       and publiclyAccessible =
         flag "publicly-accessible" (optional bool) ~doc:"BOOL Boolean"
       and deploymentType =
         flag "deployment-type" (optional json_arg)
           ~doc:"JSON ClusterDeploymentType"
       and failoverMode =
         flag "failover-mode" (optional json_arg) ~doc:"JSON FailoverMode"
       and logDeliveryConfiguration =
         flag "log-delivery-configuration" (optional json_arg)
           ~doc:"JSON LogDeliveryConfiguration"
       and maintenanceSchedule =
         flag "maintenance-schedule" (optional json_arg)
           ~doc:"JSON MaintenanceSchedule"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON RequestTagMap"
       and name = flag "name" (required string) ~doc:"STRING DbClusterName"
       and dbInstanceType =
         flag "db-instance-type" (required json_arg)
           ~doc:"JSON DbInstanceType"
       and vpcSubnetIds =
         flag "vpc-subnet-ids" (required json_arg)
           ~doc:"JSON VpcSubnetIdList"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (required json_arg)
           ~doc:"JSON VpcSecurityGroupIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_db_cluster
           (Values.CreateDbClusterInput.make ?username ?password
              ?organization ?bucket ?port ?dbParameterGroupIdentifier
              ?dbStorageType:(Option.map ~f:Values.DbStorageType.of_json
                                dbStorageType) ?allocatedStorage
              ?networkType:(Option.map ~f:Values.NetworkType.of_json
                              networkType) ?publiclyAccessible
              ?deploymentType:(Option.map
                                 ~f:Values.ClusterDeploymentType.of_json
                                 deploymentType)
              ?failoverMode:(Option.map ~f:Values.FailoverMode.of_json
                               failoverMode)
              ?logDeliveryConfiguration:(Option.map
                                           ~f:Values.LogDeliveryConfiguration.of_json
                                           logDeliveryConfiguration)
              ?maintenanceSchedule:(Option.map
                                      ~f:Values.MaintenanceSchedule.of_json
                                      maintenanceSchedule)
              ?tags:(Option.map ~f:Values.RequestTagMap.of_json tags) ~name
              ~dbInstanceType:(Values.DbInstanceType.of_json dbInstanceType)
              ~vpcSubnetIds:(Values.VpcSubnetIdList.of_json vpcSubnetIds)
              ~vpcSecurityGroupIds:(Values.VpcSecurityGroupIdList.of_json
                                      vpcSecurityGroupIds) ())
           (Some Values.CreateDbClusterOutput.to_json)
           (Some Values.CreateDbClusterOutput.error_to_json)])
let create_db_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and username =
         flag "username" (optional string) ~doc:"STRING Username"
       and organization =
         flag "organization" (optional string) ~doc:"STRING Organization"
       and bucket = flag "bucket" (optional string) ~doc:"STRING Bucket"
       and publiclyAccessible =
         flag "publicly-accessible" (optional bool) ~doc:"BOOL Boolean"
       and dbStorageType =
         flag "db-storage-type" (optional json_arg) ~doc:"JSON DbStorageType"
       and dbParameterGroupIdentifier =
         flag "db-parameter-group-identifier" (optional string)
           ~doc:"STRING DbParameterGroupIdentifier"
       and deploymentType =
         flag "deployment-type" (optional json_arg)
           ~doc:"JSON DeploymentType"
       and logDeliveryConfiguration =
         flag "log-delivery-configuration" (optional json_arg)
           ~doc:"JSON LogDeliveryConfiguration"
       and maintenanceSchedule =
         flag "maintenance-schedule" (optional json_arg)
           ~doc:"JSON MaintenanceSchedule"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON RequestTagMap"
       and port = flag "port" (optional int) ~doc:"INT Port"
       and networkType =
         flag "network-type" (optional json_arg) ~doc:"JSON NetworkType"
       and name = flag "name" (required string) ~doc:"STRING DbInstanceName"
       and password =
         flag "password" (required string) ~doc:"STRING Password"
       and dbInstanceType =
         flag "db-instance-type" (required json_arg)
           ~doc:"JSON DbInstanceType"
       and vpcSubnetIds =
         flag "vpc-subnet-ids" (required json_arg)
           ~doc:"JSON VpcSubnetIdList"
       and vpcSecurityGroupIds =
         flag "vpc-security-group-ids" (required json_arg)
           ~doc:"JSON VpcSecurityGroupIdList"
       and allocatedStorage =
         flag "allocated-storage" (required int) ~doc:"INT AllocatedStorage" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_db_instance
           (Values.CreateDbInstanceInput.make ?username ?organization ?bucket
              ?publiclyAccessible
              ?dbStorageType:(Option.map ~f:Values.DbStorageType.of_json
                                dbStorageType) ?dbParameterGroupIdentifier
              ?deploymentType:(Option.map ~f:Values.DeploymentType.of_json
                                 deploymentType)
              ?logDeliveryConfiguration:(Option.map
                                           ~f:Values.LogDeliveryConfiguration.of_json
                                           logDeliveryConfiguration)
              ?maintenanceSchedule:(Option.map
                                      ~f:Values.MaintenanceSchedule.of_json
                                      maintenanceSchedule)
              ?tags:(Option.map ~f:Values.RequestTagMap.of_json tags) ?port
              ?networkType:(Option.map ~f:Values.NetworkType.of_json
                              networkType) ~name ~password
              ~dbInstanceType:(Values.DbInstanceType.of_json dbInstanceType)
              ~vpcSubnetIds:(Values.VpcSubnetIdList.of_json vpcSubnetIds)
              ~vpcSecurityGroupIds:(Values.VpcSecurityGroupIdList.of_json
                                      vpcSecurityGroupIds) ~allocatedStorage
              ()) (Some Values.CreateDbInstanceOutput.to_json)
           (Some Values.CreateDbInstanceOutput.error_to_json)])
let create_db_parameter_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string)
           ~doc:"STRING CreateDbParameterGroupInputDescriptionString"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON Parameters"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON RequestTagMap"
       and name =
         flag "name" (required string) ~doc:"STRING DbParameterGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_db_parameter_group
           (Values.CreateDbParameterGroupInput.make ?description
              ?parameters:(Option.map ~f:Values.Parameters.of_json parameters)
              ?tags:(Option.map ~f:Values.RequestTagMap.of_json tags) ~name
              ()) (Some Values.CreateDbParameterGroupOutput.to_json)
           (Some Values.CreateDbParameterGroupOutput.error_to_json)])
let delete_db_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dbClusterId =
         flag "db-cluster-id" (required string) ~doc:"STRING DbClusterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_db_cluster
           (Values.DeleteDbClusterInput.make ~dbClusterId ())
           (Some Values.DeleteDbClusterOutput.to_json)
           (Some Values.DeleteDbClusterOutput.error_to_json)])
let delete_db_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING DbInstanceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_db_instance
           (Values.DeleteDbInstanceInput.make ~identifier ())
           (Some Values.DeleteDbInstanceOutput.to_json)
           (Some Values.DeleteDbInstanceOutput.error_to_json)])
let get_db_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dbClusterId =
         flag "db-cluster-id" (required string) ~doc:"STRING DbClusterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_db_cluster (Values.GetDbClusterInput.make ~dbClusterId ())
           (Some Values.GetDbClusterOutput.to_json)
           (Some Values.GetDbClusterOutput.error_to_json)])
let get_db_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING DbInstanceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_db_instance (Values.GetDbInstanceInput.make ~identifier ())
           (Some Values.GetDbInstanceOutput.to_json)
           (Some Values.GetDbInstanceOutput.error_to_json)])
let get_db_parameter_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING DbParameterGroupIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_db_parameter_group
           (Values.GetDbParameterGroupInput.make ~identifier ())
           (Some Values.GetDbParameterGroupOutput.to_json)
           (Some Values.GetDbParameterGroupOutput.error_to_json)])
let list_db_clusters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_db_clusters
           (Values.ListDbClustersInput.make ?nextToken ?maxResults ())
           (Some Values.ListDbClustersOutput.to_json)
           (Some Values.ListDbClustersOutput.error_to_json)])
let list_db_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_db_instances
           (Values.ListDbInstancesInput.make ?nextToken ?maxResults ())
           (Some Values.ListDbInstancesOutput.to_json)
           (Some Values.ListDbInstancesOutput.error_to_json)])
let list_db_instances_for_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and dbClusterId =
         flag "db-cluster-id" (required string) ~doc:"STRING DbClusterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_db_instances_for_cluster
           (Values.ListDbInstancesForClusterInput.make ?nextToken ?maxResults
              ~dbClusterId ())
           (Some Values.ListDbInstancesForClusterOutput.to_json)
           (Some Values.ListDbInstancesForClusterOutput.error_to_json)])
let list_db_parameter_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_db_parameter_groups
           (Values.ListDbParameterGroupsInput.make ?nextToken ?maxResults ())
           (Some Values.ListDbParameterGroupsOutput.to_json)
           (Some Values.ListDbParameterGroupsOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let reboot_db_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceIds =
         flag "instance-ids" (optional json_arg)
           ~doc:"JSON RebootDbClusterInputInstanceIdsList"
       and dbClusterId =
         flag "db-cluster-id" (required string) ~doc:"STRING DbClusterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reboot_db_cluster
           (Values.RebootDbClusterInput.make
              ?instanceIds:(Option.map
                              ~f:Values.RebootDbClusterInputInstanceIdsList.of_json
                              instanceIds) ~dbClusterId ())
           (Some Values.RebootDbClusterOutput.to_json)
           (Some Values.RebootDbClusterOutput.error_to_json)])
let reboot_db_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING DbInstanceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reboot_db_instance
           (Values.RebootDbInstanceInput.make ~identifier ())
           (Some Values.RebootDbInstanceOutput.to_json)
           (Some Values.RebootDbInstanceOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON RequestTagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.RequestTagMap.of_json tags) ()) None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ()) None None])
let update_db_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and logDeliveryConfiguration =
         flag "log-delivery-configuration" (optional json_arg)
           ~doc:"JSON LogDeliveryConfiguration"
       and dbParameterGroupIdentifier =
         flag "db-parameter-group-identifier" (optional string)
           ~doc:"STRING DbParameterGroupIdentifier"
       and port = flag "port" (optional int) ~doc:"INT Port"
       and dbInstanceType =
         flag "db-instance-type" (optional json_arg)
           ~doc:"JSON DbInstanceType"
       and failoverMode =
         flag "failover-mode" (optional json_arg) ~doc:"JSON FailoverMode"
       and maintenanceSchedule =
         flag "maintenance-schedule" (optional json_arg)
           ~doc:"JSON MaintenanceSchedule"
       and dbClusterId =
         flag "db-cluster-id" (required string) ~doc:"STRING DbClusterId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_db_cluster
           (Values.UpdateDbClusterInput.make
              ?logDeliveryConfiguration:(Option.map
                                           ~f:Values.LogDeliveryConfiguration.of_json
                                           logDeliveryConfiguration)
              ?dbParameterGroupIdentifier ?port
              ?dbInstanceType:(Option.map ~f:Values.DbInstanceType.of_json
                                 dbInstanceType)
              ?failoverMode:(Option.map ~f:Values.FailoverMode.of_json
                               failoverMode)
              ?maintenanceSchedule:(Option.map
                                      ~f:Values.MaintenanceSchedule.of_json
                                      maintenanceSchedule) ~dbClusterId ())
           (Some Values.UpdateDbClusterOutput.to_json)
           (Some Values.UpdateDbClusterOutput.error_to_json)])
let update_db_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and logDeliveryConfiguration =
         flag "log-delivery-configuration" (optional json_arg)
           ~doc:"JSON LogDeliveryConfiguration"
       and dbParameterGroupIdentifier =
         flag "db-parameter-group-identifier" (optional string)
           ~doc:"STRING DbParameterGroupIdentifier"
       and port = flag "port" (optional int) ~doc:"INT Port"
       and dbInstanceType =
         flag "db-instance-type" (optional json_arg)
           ~doc:"JSON DbInstanceType"
       and deploymentType =
         flag "deployment-type" (optional json_arg)
           ~doc:"JSON DeploymentType"
       and dbStorageType =
         flag "db-storage-type" (optional json_arg) ~doc:"JSON DbStorageType"
       and allocatedStorage =
         flag "allocated-storage" (optional int) ~doc:"INT AllocatedStorage"
       and maintenanceSchedule =
         flag "maintenance-schedule" (optional json_arg)
           ~doc:"JSON MaintenanceSchedule"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING DbInstanceIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_db_instance
           (Values.UpdateDbInstanceInput.make
              ?logDeliveryConfiguration:(Option.map
                                           ~f:Values.LogDeliveryConfiguration.of_json
                                           logDeliveryConfiguration)
              ?dbParameterGroupIdentifier ?port
              ?dbInstanceType:(Option.map ~f:Values.DbInstanceType.of_json
                                 dbInstanceType)
              ?deploymentType:(Option.map ~f:Values.DeploymentType.of_json
                                 deploymentType)
              ?dbStorageType:(Option.map ~f:Values.DbStorageType.of_json
                                dbStorageType) ?allocatedStorage
              ?maintenanceSchedule:(Option.map
                                      ~f:Values.MaintenanceSchedule.of_json
                                      maintenanceSchedule) ~identifier ())
           (Some Values.UpdateDbInstanceOutput.to_json)
           (Some Values.UpdateDbInstanceOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-db-cluster", create_db_cluster);
    ("create-db-instance", create_db_instance);
    ("create-db-parameter-group", create_db_parameter_group);
    ("delete-db-cluster", delete_db_cluster);
    ("delete-db-instance", delete_db_instance);
    ("get-db-cluster", get_db_cluster);
    ("get-db-instance", get_db_instance);
    ("get-db-parameter-group", get_db_parameter_group);
    ("list-db-clusters", list_db_clusters);
    ("list-db-instances", list_db_instances);
    ("list-db-instances-for-cluster", list_db_instances_for_cluster);
    ("list-db-parameter-groups", list_db_parameter_groups);
    ("list-tags-for-resource", list_tags_for_resource);
    ("reboot-db-cluster", reboot_db_cluster);
    ("reboot-db-instance", reboot_db_instance);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-db-cluster", update_db_cluster);
    ("update-db-instance", update_db_instance)]
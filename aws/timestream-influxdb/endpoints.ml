(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | CreateDbCluster: (CreateDbClusterInput.t, CreateDbClusterOutput.t,
  CreateDbClusterOutput.error) t 
  | CreateDbInstance: (CreateDbInstanceInput.t, CreateDbInstanceOutput.t,
  CreateDbInstanceOutput.error) t 
  | CreateDbParameterGroup: (CreateDbParameterGroupInput.t,
  CreateDbParameterGroupOutput.t, CreateDbParameterGroupOutput.error) t 
  | DeleteDbCluster: (DeleteDbClusterInput.t, DeleteDbClusterOutput.t,
  DeleteDbClusterOutput.error) t 
  | DeleteDbInstance: (DeleteDbInstanceInput.t, DeleteDbInstanceOutput.t,
  DeleteDbInstanceOutput.error) t 
  | GetDbCluster: (GetDbClusterInput.t, GetDbClusterOutput.t,
  GetDbClusterOutput.error) t 
  | GetDbInstance: (GetDbInstanceInput.t, GetDbInstanceOutput.t,
  GetDbInstanceOutput.error) t 
  | GetDbParameterGroup: (GetDbParameterGroupInput.t,
  GetDbParameterGroupOutput.t, GetDbParameterGroupOutput.error) t 
  | ListDbClusters: (ListDbClustersInput.t, ListDbClustersOutput.t,
  ListDbClustersOutput.error) t 
  | ListDbInstances: (ListDbInstancesInput.t, ListDbInstancesOutput.t,
  ListDbInstancesOutput.error) t 
  | ListDbInstancesForCluster: (ListDbInstancesForClusterInput.t,
  ListDbInstancesForClusterOutput.t, ListDbInstancesForClusterOutput.error) t
  
  | ListDbParameterGroups: (ListDbParameterGroupsInput.t,
  ListDbParameterGroupsOutput.t, ListDbParameterGroupsOutput.error) t 
  | ListTagsForResource: (ListTagsForResourceRequest.t,
  ListTagsForResourceResponse.t, ListTagsForResourceResponse.error) t 
  | RebootDbCluster: (RebootDbClusterInput.t, RebootDbClusterOutput.t,
  RebootDbClusterOutput.error) t 
  | RebootDbInstance: (RebootDbInstanceInput.t, RebootDbInstanceOutput.t,
  RebootDbInstanceOutput.error) t 
  | TagResource: (TagResourceRequest.t, unit, unit) t 
  | UntagResource: (UntagResourceRequest.t, unit, unit) t 
  | UpdateDbCluster: (UpdateDbClusterInput.t, UpdateDbClusterOutput.t,
  UpdateDbClusterOutput.error) t 
  | UpdateDbInstance: (UpdateDbInstanceInput.t, UpdateDbInstanceOutput.t,
  UpdateDbInstanceOutput.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | CreateDbCluster -> `POST
  | CreateDbInstance -> `POST
  | CreateDbParameterGroup -> `POST
  | DeleteDbCluster -> `POST
  | DeleteDbInstance -> `POST
  | GetDbCluster -> `POST
  | GetDbInstance -> `POST
  | GetDbParameterGroup -> `POST
  | ListDbClusters -> `POST
  | ListDbInstances -> `POST
  | ListDbInstancesForCluster -> `POST
  | ListDbParameterGroups -> `POST
  | ListTagsForResource -> `POST
  | RebootDbCluster -> `POST
  | RebootDbInstance -> `POST
  | TagResource -> `POST
  | UntagResource -> `POST
  | UpdateDbCluster -> `POST
  | UpdateDbInstance -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | CreateDbCluster -> (Format.kasprintf Uri.of_string) "/"
      | CreateDbInstance -> (Format.kasprintf Uri.of_string) "/"
      | CreateDbParameterGroup -> (Format.kasprintf Uri.of_string) "/"
      | DeleteDbCluster -> (Format.kasprintf Uri.of_string) "/"
      | DeleteDbInstance -> (Format.kasprintf Uri.of_string) "/"
      | GetDbCluster -> (Format.kasprintf Uri.of_string) "/"
      | GetDbInstance -> (Format.kasprintf Uri.of_string) "/"
      | GetDbParameterGroup -> (Format.kasprintf Uri.of_string) "/"
      | ListDbClusters -> (Format.kasprintf Uri.of_string) "/"
      | ListDbInstances -> (Format.kasprintf Uri.of_string) "/"
      | ListDbInstancesForCluster -> (Format.kasprintf Uri.of_string) "/"
      | ListDbParameterGroups -> (Format.kasprintf Uri.of_string) "/"
      | ListTagsForResource -> (Format.kasprintf Uri.of_string) "/"
      | RebootDbCluster -> (Format.kasprintf Uri.of_string) "/"
      | RebootDbInstance -> (Format.kasprintf Uri.of_string) "/"
      | TagResource -> (Format.kasprintf Uri.of_string) "/"
      | UntagResource -> (Format.kasprintf Uri.of_string) "/"
      | UpdateDbCluster -> (Format.kasprintf Uri.of_string) "/"
      | UpdateDbInstance -> (Format.kasprintf Uri.of_string) "/")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  match endp with
  | CreateDbCluster ->
      let json = CreateDbClusterInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "AmazonTimestreamInfluxDB.CreateDbCluster")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | CreateDbInstance ->
      let json = CreateDbInstanceInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "AmazonTimestreamInfluxDB.CreateDbInstance")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | CreateDbParameterGroup ->
      let json = CreateDbParameterGroupInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "AmazonTimestreamInfluxDB.CreateDbParameterGroup")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | DeleteDbCluster ->
      let json = DeleteDbClusterInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "AmazonTimestreamInfluxDB.DeleteDbCluster")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | DeleteDbInstance ->
      let json = DeleteDbInstanceInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "AmazonTimestreamInfluxDB.DeleteDbInstance")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | GetDbCluster ->
      let json = GetDbClusterInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "AmazonTimestreamInfluxDB.GetDbCluster")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | GetDbInstance ->
      let json = GetDbInstanceInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "AmazonTimestreamInfluxDB.GetDbInstance")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | GetDbParameterGroup ->
      let json = GetDbParameterGroupInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "AmazonTimestreamInfluxDB.GetDbParameterGroup")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListDbClusters ->
      let json = ListDbClustersInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "AmazonTimestreamInfluxDB.ListDbClusters")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListDbInstances ->
      let json = ListDbInstancesInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "AmazonTimestreamInfluxDB.ListDbInstances")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListDbInstancesForCluster ->
      let json = ListDbInstancesForClusterInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target",
            "AmazonTimestreamInfluxDB.ListDbInstancesForCluster")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListDbParameterGroups ->
      let json = ListDbParameterGroupsInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "AmazonTimestreamInfluxDB.ListDbParameterGroups")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | ListTagsForResource ->
      let json = ListTagsForResourceRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "AmazonTimestreamInfluxDB.ListTagsForResource")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | RebootDbCluster ->
      let json = RebootDbClusterInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "AmazonTimestreamInfluxDB.RebootDbCluster")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | RebootDbInstance ->
      let json = RebootDbInstanceInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "AmazonTimestreamInfluxDB.RebootDbInstance")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | TagResource ->
      let json = TagResourceRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "AmazonTimestreamInfluxDB.TagResource")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | UntagResource ->
      let json = UntagResourceRequest.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "AmazonTimestreamInfluxDB.UntagResource")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | UpdateDbCluster ->
      let json = UpdateDbClusterInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "AmazonTimestreamInfluxDB.UpdateDbCluster")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
  | UpdateDbInstance ->
      let json = UpdateDbInstanceInput.to_json req in
      let body = Yojson.Safe.to_string json in
      let headers =
        Awso.Http.Headers.of_list
          [("Content-Type", "application/x-amz-json-1.0");
          ("X-Amz-Target", "AmazonTimestreamInfluxDB.UpdateDbInstance")] in
      Awso.Http.Request.make ~body ~headers (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type = None }) in
    match (error_of_json, ((code >= 400) && (code <= 599))) with
    | (Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        (match json |> (Yojson.Safe.Util.member "__type") with
         | `String error_type -> error_of_json error_type json
         | `Null -> bail ()
         | _ ->
             failwith
               (sprintf "Error '__type' did not have string type: %s" body))
    | (None, _) | (_, false) -> bail () in
  let _ = parse_aws_error in
  let _ = resp in
  match endpoint with
  | CreateDbCluster ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (CreateDbClusterOutput.of_json json)
      else Error (parse_aws_error (Some CreateDbClusterOutput.error_of_json))
  | CreateDbInstance ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (CreateDbInstanceOutput.of_json json)
      else
        Error (parse_aws_error (Some CreateDbInstanceOutput.error_of_json))
  | CreateDbParameterGroup ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (CreateDbParameterGroupOutput.of_json json)
      else
        Error
          (parse_aws_error (Some CreateDbParameterGroupOutput.error_of_json))
  | DeleteDbCluster ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (DeleteDbClusterOutput.of_json json)
      else Error (parse_aws_error (Some DeleteDbClusterOutput.error_of_json))
  | DeleteDbInstance ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (DeleteDbInstanceOutput.of_json json)
      else
        Error (parse_aws_error (Some DeleteDbInstanceOutput.error_of_json))
  | GetDbCluster ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (GetDbClusterOutput.of_json json)
      else Error (parse_aws_error (Some GetDbClusterOutput.error_of_json))
  | GetDbInstance ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (GetDbInstanceOutput.of_json json)
      else Error (parse_aws_error (Some GetDbInstanceOutput.error_of_json))
  | GetDbParameterGroup ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (GetDbParameterGroupOutput.of_json json)
      else
        Error
          (parse_aws_error (Some GetDbParameterGroupOutput.error_of_json))
  | ListDbClusters ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ListDbClustersOutput.of_json json)
      else Error (parse_aws_error (Some ListDbClustersOutput.error_of_json))
  | ListDbInstances ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ListDbInstancesOutput.of_json json)
      else Error (parse_aws_error (Some ListDbInstancesOutput.error_of_json))
  | ListDbInstancesForCluster ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ListDbInstancesForClusterOutput.of_json json)
      else
        Error
          (parse_aws_error
             (Some ListDbInstancesForClusterOutput.error_of_json))
  | ListDbParameterGroups ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ListDbParameterGroupsOutput.of_json json)
      else
        Error
          (parse_aws_error (Some ListDbParameterGroupsOutput.error_of_json))
  | ListTagsForResource ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (ListTagsForResourceResponse.of_json json)
      else
        Error
          (parse_aws_error (Some ListTagsForResourceResponse.error_of_json))
  | RebootDbCluster ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (RebootDbClusterOutput.of_json json)
      else Error (parse_aws_error (Some RebootDbClusterOutput.error_of_json))
  | RebootDbInstance ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (RebootDbInstanceOutput.of_json json)
      else
        Error (parse_aws_error (Some RebootDbInstanceOutput.error_of_json))
  | TagResource -> if is_success then Ok () else Error (parse_aws_error None)
  | UntagResource ->
      if is_success then Ok () else Error (parse_aws_error None)
  | UpdateDbCluster ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (UpdateDbClusterOutput.of_json json)
      else Error (parse_aws_error (Some UpdateDbClusterOutput.error_of_json))
  | UpdateDbInstance ->
      if is_success
      then
        let json = Yojson.Safe.from_string (Awso.Http.Response.body resp) in
        Ok (UpdateDbInstanceOutput.of_json json)
      else
        Error (parse_aws_error (Some UpdateDbInstanceOutput.error_of_json))
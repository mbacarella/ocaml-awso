(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.shield
let apiVersion = "2016-06-02"
let endpointPrefix = "shield"
let serviceFullName = "AWS Shield"
let signatureVersion = "v4"
let protocol = "json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let serviceAbbreviation = "AWS Shield"
let targetPrefix = "AWSShield_20160616"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module Double =
  struct
    type nonrec t = float
    let make i = i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module SummarizedCounter =
  struct
    type nonrec t =
      {
      name: String_.t option [@ocaml.doc "The counter name."];
      max: Double.t option
        [@ocaml.doc
          "The maximum value of the counter for a specified time period."];
      average: Double.t option
        [@ocaml.doc
          "The average value of the counter for a specified time period."];
      sum: Double.t option
        [@ocaml.doc
          "The total of counter values for a specified time period."];
      n: Integer.t option
        [@ocaml.doc "The number of counters for a specified time period."];
      unit: String_.t option [@ocaml.doc "The unit of the counters."]}
    let make ?name =
      fun ?max ->
        fun ?average ->
          fun ?sum ->
            fun ?n ->
              fun ?unit -> fun () -> { name; max; average; sum; n; unit }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:String_.to_value));
        ("Max", (Option.map x.max ~f:Double.to_value));
        ("Average", (Option.map x.average ~f:Double.to_value));
        ("Sum", (Option.map x.sum ~f:Double.to_value));
        ("N", (Option.map x.n ~f:Integer.to_value));
        ("Unit", (Option.map x.unit ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let unit = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Unit") in
      let n = (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "N") in
      let sum = (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "Sum") in
      let average =
        (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "Average") in
      let max = (Option.map ~f:Double.of_xml) (Xml.child xml_arg0 "Max") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      make ?unit ?n ?sum ?average ?max ?name ()
    let of_json json =
      let unit = field_map json "Unit" String_.of_json in
      let n = field_map json "N" Integer.of_json in
      let sum = field_map json "Sum" Double.of_json in
      let average = field_map json "Average" Double.of_json in
      let max = field_map json "Max" Double.of_json in
      let name = field_map json "Name" String_.of_json in
      make ?unit ?n ?sum ?average ?max ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The counter that describes a DDoS attack."]
module Long =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module SummarizedCounterList =
  struct
    type nonrec t = SummarizedCounter.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SummarizedCounter.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SummarizedCounter.of_xml)
    let of_json j =
      list_of_json ~kind:"SummarizedCounterList"
        ~of_json:SummarizedCounter.of_json j
    let to_json v = composed_to_json to_value v
  end
module BlockAction =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies that Shield Advanced should configure its WAF rules with the WAF Block action. This is only used in the context of the ResponseAction setting. JSON specification: \"Block\": \\{\\}"]
module CountAction =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies that Shield Advanced should configure its WAF rules with the WAF Count action. This is only used in the context of the ResponseAction setting. JSON specification: \"Count\": \\{\\}"]
module ProtectionGroupArbitraryPatternLimits =
  struct
    type nonrec t =
      {
      maxMembers: Long.t
        [@ocaml.doc
          "The maximum number of resources you can specify for a single arbitrary pattern in a protection group."]}
    let context_ = "ProtectionGroupArbitraryPatternLimits"
    let make ~maxMembers = fun () -> { maxMembers }
    let to_value x =
      structure_to_value
        [("MaxMembers", (Some (Long.to_value x.maxMembers)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxMembers =
        Long.of_xml (Xml.child_exn ~context:context_ xml_arg0 "MaxMembers") in
      make ~maxMembers ()
    let of_json json =
      let maxMembers = field_map_exn json "MaxMembers" Long.of_json in
      make ~maxMembers ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Limits settings on protection groups with arbitrary pattern type."]
module Limit =
  struct
    type nonrec t =
      {
      type_: String_.t option [@ocaml.doc "The type of protection."];
      max: Long.t option
        [@ocaml.doc
          "The maximum number of protections that can be created for the specified Type."]}
    let make ?type_ = fun ?max -> fun () -> { type_; max }
    let to_value x =
      structure_to_value
        [("Type", (Option.map x.type_ ~f:String_.to_value));
        ("Max", (Option.map x.max ~f:Long.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let max = (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "Max") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Type") in
      make ?max ?type_ ()
    let of_json json =
      let max = field_map json "Max" Long.of_json in
      let type_ = field_map json "Type" String_.of_json in
      make ?max ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies how many protections of a given type you can create."]
module Contributor =
  struct
    type nonrec t =
      {
      name: String_.t option
        [@ocaml.doc
          "The name of the contributor. The type of name that you'll find here depends on the AttackPropertyIdentifier setting in the AttackProperty where this contributor is defined. For example, if the AttackPropertyIdentifier is SOURCE_COUNTRY, the Name could be United States."];
      value: Long.t option
        [@ocaml.doc
          "The contribution of this contributor expressed in Protection units. For example 10,000."]}
    let make ?name = fun ?value -> fun () -> { name; value }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:String_.to_value));
        ("Value", (Option.map x.value ~f:Long.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value = (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "Value") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      make ?value ?name ()
    let of_json json =
      let value = field_map json "Value" Long.of_json in
      let name = field_map json "Name" String_.of_json in
      make ?value ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A contributor to the attack and their contribution."]
module SummarizedAttackVector =
  struct
    type nonrec t =
      {
      vectorType: String_.t
        [@ocaml.doc
          "The attack type, for example, SNMP reflection or SYN flood."];
      vectorCounters: SummarizedCounterList.t option
        [@ocaml.doc
          "The list of counters that describe the details of the attack."]}
    let context_ = "SummarizedAttackVector"
    let make ?vectorCounters =
      fun ~vectorType -> fun () -> { vectorCounters; vectorType }
    let to_value x =
      structure_to_value
        [("VectorType", (Some (String_.to_value x.vectorType)));
        ("VectorCounters",
          (Option.map x.vectorCounters ~f:SummarizedCounterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vectorCounters =
        (Option.map ~f:SummarizedCounterList.of_xml)
          (Xml.child xml_arg0 "VectorCounters") in
      let vectorType =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VectorType") in
      make ?vectorCounters ~vectorType ()
    let of_json json =
      let vectorCounters =
        field_map json "VectorCounters" SummarizedCounterList.of_json in
      let vectorType = field_map_exn json "VectorType" String_.of_json in
      make ?vectorCounters ~vectorType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A summary of information about the attack."]
module ApplicationLayerAutomaticResponseStatus =
  struct
    type nonrec t =
      | ENABLED 
      | DISABLED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ENABLED -> "ENABLED"
      | DISABLED -> "DISABLED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ENABLED" -> ENABLED
      | "DISABLED" -> DISABLED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml
           ~kind:"enumeration ApplicationLayerAutomaticResponseStatus"
           xml_arg0)
    let of_json j =
      of_string
        (string_of_json ~kind:"ApplicationLayerAutomaticResponseStatus" j)
    let to_json = simple_to_json to_value
  end
module ResponseAction =
  struct
    type nonrec t =
      {
      block: BlockAction.t option
        [@ocaml.doc
          "Specifies that Shield Advanced should configure its WAF rules with the WAF Block action. You must specify exactly one action, either Block or Count."];
      count: CountAction.t option
        [@ocaml.doc
          "Specifies that Shield Advanced should configure its WAF rules with the WAF Count action. You must specify exactly one action, either Block or Count."]}
    let make ?block = fun ?count -> fun () -> { block; count }
    let to_value x =
      structure_to_value
        [("Block", (Option.map x.block ~f:BlockAction.to_value));
        ("Count", (Option.map x.count ~f:CountAction.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let count =
        (Option.map ~f:CountAction.of_xml) (Xml.child xml_arg0 "Count") in
      let block =
        (Option.map ~f:BlockAction.of_xml) (Xml.child xml_arg0 "Block") in
      make ?count ?block ()
    let of_json json =
      let count = field_map json "Count" CountAction.of_json in
      let block = field_map json "Block" BlockAction.of_json in
      make ?count ?block ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Specifies the action setting that Shield Advanced should use in the WAF rules that it creates on behalf of the protected resource in response to DDoS attacks. You specify this as part of the configuration for the automatic application layer DDoS mitigation feature, when you enable or update automatic mitigation. Shield Advanced creates the WAF rules in a Shield Advanced-managed rule group, inside the web ACL that you have associated with the resource."]
module HealthCheckId =
  struct
    type nonrec t = string
    let context_ = "HealthCheckId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"HealthCheckId" j
    let to_json = simple_to_json to_value
  end
module ResourceArn =
  struct
    type nonrec t = string
    let context_ = "ResourceArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () -> check_pattern i ~pattern:"^arn:aws.*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceArn" j
    let to_json = simple_to_json to_value
  end
module AttackVectorDescription =
  struct
    type nonrec t =
      {
      vectorType: String_.t
        [@ocaml.doc
          "The attack type. Valid values: UDP_TRAFFIC UDP_FRAGMENT GENERIC_UDP_REFLECTION DNS_REFLECTION NTP_REFLECTION CHARGEN_REFLECTION SSDP_REFLECTION PORT_MAPPER RIP_REFLECTION SNMP_REFLECTION MSSQL_REFLECTION NET_BIOS_REFLECTION SYN_FLOOD ACK_FLOOD REQUEST_FLOOD HTTP_REFLECTION UDS_REFLECTION MEMCACHED_REFLECTION"]}
    let context_ = "AttackVectorDescription"
    let make ~vectorType = fun () -> { vectorType }
    let to_value x =
      structure_to_value
        [("VectorType", (Some (String_.to_value x.vectorType)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vectorType =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "VectorType") in
      make ~vectorType ()
    let of_json json =
      let vectorType = field_map_exn json "VectorType" String_.of_json in
      make ~vectorType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the attack."]
module ProtectionGroupPatternTypeLimits =
  struct
    type nonrec t =
      {
      arbitraryPatternLimits: ProtectionGroupArbitraryPatternLimits.t
        [@ocaml.doc
          "Limits settings on protection groups with arbitrary pattern type."]}
    let context_ = "ProtectionGroupPatternTypeLimits"
    let make ~arbitraryPatternLimits = fun () -> { arbitraryPatternLimits }
    let to_value x =
      structure_to_value
        [("ArbitraryPatternLimits",
           (Some
              (ProtectionGroupArbitraryPatternLimits.to_value
                 x.arbitraryPatternLimits)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let arbitraryPatternLimits =
        ProtectionGroupArbitraryPatternLimits.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ArbitraryPatternLimits") in
      make ~arbitraryPatternLimits ()
    let of_json json =
      let arbitraryPatternLimits =
        field_map_exn json "ArbitraryPatternLimits"
          ProtectionGroupArbitraryPatternLimits.of_json in
      make ~arbitraryPatternLimits ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Limits settings by pattern type in the protection groups for your subscription."]
module Limits =
  struct
    type nonrec t = Limit.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Limit.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Limit.of_xml)
    let of_json j = list_of_json ~kind:"Limits" ~of_json:Limit.of_json j
    let to_json v = composed_to_json to_value v
  end
module AttackVolumeStatistics =
  struct
    type nonrec t =
      {
      max: Double.t
        [@ocaml.doc "The maximum attack volume observed for the given unit."]}
    let context_ = "AttackVolumeStatistics"
    let make ~max = fun () -> { max }
    let to_value x =
      structure_to_value [("Max", (Some (Double.to_value x.max)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let max =
        Double.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Max") in
      make ~max ()
    let of_json json =
      let max = field_map_exn json "Max" Double.of_json in make ~max ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Statistics objects for the various data types in AttackVolume."]
module AttackLayer =
  struct
    type nonrec t =
      | NETWORK 
      | APPLICATION 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NETWORK -> "NETWORK"
      | APPLICATION -> "APPLICATION"
      | Non_static_id s -> s
    let of_string =
      function
      | "NETWORK" -> NETWORK
      | "APPLICATION" -> APPLICATION
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration AttackLayer" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AttackLayer" j)
    let to_json = simple_to_json to_value
  end
module AttackPropertyIdentifier =
  struct
    type nonrec t =
      | DESTINATION_URL 
      | REFERRER 
      | SOURCE_ASN 
      | SOURCE_COUNTRY 
      | SOURCE_IP_ADDRESS 
      | SOURCE_USER_AGENT 
      | WORDPRESS_PINGBACK_REFLECTOR 
      | WORDPRESS_PINGBACK_SOURCE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | DESTINATION_URL -> "DESTINATION_URL"
      | REFERRER -> "REFERRER"
      | SOURCE_ASN -> "SOURCE_ASN"
      | SOURCE_COUNTRY -> "SOURCE_COUNTRY"
      | SOURCE_IP_ADDRESS -> "SOURCE_IP_ADDRESS"
      | SOURCE_USER_AGENT -> "SOURCE_USER_AGENT"
      | WORDPRESS_PINGBACK_REFLECTOR -> "WORDPRESS_PINGBACK_REFLECTOR"
      | WORDPRESS_PINGBACK_SOURCE -> "WORDPRESS_PINGBACK_SOURCE"
      | Non_static_id s -> s
    let of_string =
      function
      | "DESTINATION_URL" -> DESTINATION_URL
      | "REFERRER" -> REFERRER
      | "SOURCE_ASN" -> SOURCE_ASN
      | "SOURCE_COUNTRY" -> SOURCE_COUNTRY
      | "SOURCE_IP_ADDRESS" -> SOURCE_IP_ADDRESS
      | "SOURCE_USER_AGENT" -> SOURCE_USER_AGENT
      | "WORDPRESS_PINGBACK_REFLECTOR" -> WORDPRESS_PINGBACK_REFLECTOR
      | "WORDPRESS_PINGBACK_SOURCE" -> WORDPRESS_PINGBACK_SOURCE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration AttackPropertyIdentifier" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"AttackPropertyIdentifier" j)
    let to_json = simple_to_json to_value
  end
module TopContributors =
  struct
    type nonrec t = Contributor.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Contributor.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Contributor.of_xml)
    let of_json j =
      list_of_json ~kind:"TopContributors" ~of_json:Contributor.of_json j
    let to_json v = composed_to_json to_value v
  end
module Unit =
  struct
    type nonrec t =
      | BITS 
      | BYTES 
      | PACKETS 
      | REQUESTS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | BITS -> "BITS"
      | BYTES -> "BYTES"
      | PACKETS -> "PACKETS"
      | REQUESTS -> "REQUESTS"
      | Non_static_id s -> s
    let of_string =
      function
      | "BITS" -> BITS
      | "BYTES" -> BYTES
      | "PACKETS" -> PACKETS
      | "REQUESTS" -> REQUESTS
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Unit" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Unit" j)
    let to_json = simple_to_json to_value
  end
module SubResourceType =
  struct
    type nonrec t =
      | IP 
      | URL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function | IP -> "IP" | URL -> "URL" | Non_static_id s -> s
    let of_string =
      function | "IP" -> IP | "URL" -> URL | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration SubResourceType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SubResourceType" j)
    let to_json = simple_to_json to_value
  end
module SummarizedAttackVectorList =
  struct
    type nonrec t = SummarizedAttackVector.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SummarizedAttackVector.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SummarizedAttackVector.of_xml)
    let of_json j =
      list_of_json ~kind:"SummarizedAttackVectorList"
        ~of_json:SummarizedAttackVector.of_json j
    let to_json v = composed_to_json to_value v
  end
module ValidationExceptionField =
  struct
    type nonrec t =
      {
      name: String_.t
        [@ocaml.doc "The name of the parameter that failed validation."];
      message: String_.t
        [@ocaml.doc
          "The message describing why the parameter failed validation."]}
    let context_ = "ValidationExceptionField"
    let make ~name = fun ~message -> fun () -> { name; message }
    let to_value x =
      structure_to_value
        [("name", (Some (String_.to_value x.name)));
        ("message", (Some (String_.to_value x.message)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "message") in
      let name =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "name") in
      make ~message ~name ()
    let of_json json =
      let message = field_map_exn json "message" String_.of_json in
      let name = field_map_exn json "name" String_.of_json in
      make ~message ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides information about a particular parameter passed inside a request that resulted in an exception."]
module ContactNotes =
  struct
    type nonrec t = string
    let context_ = "ContactNotes"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:1024) >>=
                  (fun () ->
                     check_pattern i ~pattern:"^[\\w\\s\\.\\-,:/()+@]*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ContactNotes" j
    let to_json = simple_to_json to_value
  end
module EmailAddress =
  struct
    type nonrec t = string
    let context_ = "EmailAddress"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:150) >>=
                  (fun () -> check_pattern i ~pattern:"^\\S+@\\S+\\.\\S+$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"EmailAddress" j
    let to_json = simple_to_json to_value
  end
module PhoneNumber =
  struct
    type nonrec t = string
    let context_ = "PhoneNumber"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:16) >>=
                  (fun () -> check_pattern i ~pattern:"^\\+[1-9]\\d{1,14}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PhoneNumber" j
    let to_json = simple_to_json to_value
  end
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module ApplicationLayerAutomaticResponseConfiguration =
  struct
    type nonrec t =
      {
      status: ApplicationLayerAutomaticResponseStatus.t
        [@ocaml.doc
          "Indicates whether automatic application layer DDoS mitigation is enabled for the protection."];
      action: ResponseAction.t }
    let context_ = "ApplicationLayerAutomaticResponseConfiguration"
    let make ~status = fun ~action -> fun () -> { status; action }
    let to_value x =
      structure_to_value
        [("Status",
           (Some (ApplicationLayerAutomaticResponseStatus.to_value x.status)));
        ("Action", (Some (ResponseAction.to_value x.action)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let action =
        ResponseAction.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Action") in
      let status =
        ApplicationLayerAutomaticResponseStatus.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Status") in
      make ~action ~status ()
    let of_json json =
      let action = field_map_exn json "Action" ResponseAction.of_json in
      let status =
        field_map_exn json "Status"
          ApplicationLayerAutomaticResponseStatus.of_json in
      make ~action ~status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The automatic application layer DDoS mitigation settings for a Protection. This configuration determines whether Shield Advanced automatically manages rules in the web ACL in order to respond to application layer events that Shield Advanced determines to be DDoS attacks."]
module HealthCheckIds =
  struct
    type nonrec t = HealthCheckId.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:HealthCheckId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:HealthCheckId.of_xml)
    let of_json j =
      list_of_json ~kind:"HealthCheckIds" ~of_json:HealthCheckId.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProtectionId =
  struct
    type nonrec t = string
    let context_ = "ProtectionId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:36) >>=
             (fun () ->
                (check_string_max i ~max:36) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9\\\\-]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProtectionId" j
    let to_json = simple_to_json to_value
  end
module ProtectionName =
  struct
    type nonrec t = string
    let context_ = "ProtectionName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i ~pattern:"[ a-zA-Z0-9_\\\\.\\\\-]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProtectionName" j
    let to_json = simple_to_json to_value
  end
module ProtectedResourceType =
  struct
    type nonrec t =
      | CLOUDFRONT_DISTRIBUTION 
      | ROUTE_53_HOSTED_ZONE 
      | ELASTIC_IP_ALLOCATION 
      | CLASSIC_LOAD_BALANCER 
      | APPLICATION_LOAD_BALANCER 
      | GLOBAL_ACCELERATOR 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CLOUDFRONT_DISTRIBUTION -> "CLOUDFRONT_DISTRIBUTION"
      | ROUTE_53_HOSTED_ZONE -> "ROUTE_53_HOSTED_ZONE"
      | ELASTIC_IP_ALLOCATION -> "ELASTIC_IP_ALLOCATION"
      | CLASSIC_LOAD_BALANCER -> "CLASSIC_LOAD_BALANCER"
      | APPLICATION_LOAD_BALANCER -> "APPLICATION_LOAD_BALANCER"
      | GLOBAL_ACCELERATOR -> "GLOBAL_ACCELERATOR"
      | Non_static_id s -> s
    let of_string =
      function
      | "CLOUDFRONT_DISTRIBUTION" -> CLOUDFRONT_DISTRIBUTION
      | "ROUTE_53_HOSTED_ZONE" -> ROUTE_53_HOSTED_ZONE
      | "ELASTIC_IP_ALLOCATION" -> ELASTIC_IP_ALLOCATION
      | "CLASSIC_LOAD_BALANCER" -> CLASSIC_LOAD_BALANCER
      | "APPLICATION_LOAD_BALANCER" -> APPLICATION_LOAD_BALANCER
      | "GLOBAL_ACCELERATOR" -> GLOBAL_ACCELERATOR
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ProtectedResourceType" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ProtectedResourceType" j)
    let to_json = simple_to_json to_value
  end
module ProtectionGroupAggregation =
  struct
    type nonrec t =
      | SUM 
      | MEAN 
      | MAX 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SUM -> "SUM"
      | MEAN -> "MEAN"
      | MAX -> "MAX"
      | Non_static_id s -> s
    let of_string =
      function
      | "SUM" -> SUM
      | "MEAN" -> MEAN
      | "MAX" -> MAX
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ProtectionGroupAggregation"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ProtectionGroupAggregation" j)
    let to_json = simple_to_json to_value
  end
module ProtectionGroupId =
  struct
    type nonrec t = string
    let context_ = "ProtectionGroupId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:36) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9\\\\-]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProtectionGroupId" j
    let to_json = simple_to_json to_value
  end
module ProtectionGroupMembers =
  struct
    type nonrec t = ResourceArn.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10000) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:ResourceArn.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResourceArn.of_xml)
    let of_json j =
      list_of_json ~kind:"ProtectionGroupMembers"
        ~of_json:ResourceArn.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProtectionGroupPattern =
  struct
    type nonrec t =
      | ALL 
      | ARBITRARY 
      | BY_RESOURCE_TYPE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ALL -> "ALL"
      | ARBITRARY -> "ARBITRARY"
      | BY_RESOURCE_TYPE -> "BY_RESOURCE_TYPE"
      | Non_static_id s -> s
    let of_string =
      function
      | "ALL" -> ALL
      | "ARBITRARY" -> ARBITRARY
      | "BY_RESOURCE_TYPE" -> BY_RESOURCE_TYPE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ProtectionGroupPattern" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ProtectionGroupPattern" j)
    let to_json = simple_to_json to_value
  end
module AttackTimestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module AttackVectorDescriptionList =
  struct
    type nonrec t = AttackVectorDescription.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AttackVectorDescription.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AttackVectorDescription.of_xml)
    let of_json j =
      list_of_json ~kind:"AttackVectorDescriptionList"
        ~of_json:AttackVectorDescription.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProtectionGroupLimits =
  struct
    type nonrec t =
      {
      maxProtectionGroups: Long.t
        [@ocaml.doc
          "The maximum number of protection groups that you can have at one time."];
      patternTypeLimits: ProtectionGroupPatternTypeLimits.t
        [@ocaml.doc
          "Limits settings by pattern type in the protection groups for your subscription."]}
    let context_ = "ProtectionGroupLimits"
    let make ~maxProtectionGroups =
      fun ~patternTypeLimits ->
        fun () -> { maxProtectionGroups; patternTypeLimits }
    let to_value x =
      structure_to_value
        [("MaxProtectionGroups",
           (Some (Long.to_value x.maxProtectionGroups)));
        ("PatternTypeLimits",
          (Some
             (ProtectionGroupPatternTypeLimits.to_value x.patternTypeLimits)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let patternTypeLimits =
        ProtectionGroupPatternTypeLimits.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "PatternTypeLimits") in
      let maxProtectionGroups =
        Long.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MaxProtectionGroups") in
      make ~patternTypeLimits ~maxProtectionGroups ()
    let of_json json =
      let patternTypeLimits =
        field_map_exn json "PatternTypeLimits"
          ProtectionGroupPatternTypeLimits.of_json in
      let maxProtectionGroups =
        field_map_exn json "MaxProtectionGroups" Long.of_json in
      make ~patternTypeLimits ~maxProtectionGroups ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Limits settings on protection groups for your subscription."]
module ProtectionLimits =
  struct
    type nonrec t =
      {
      protectedResourceTypeLimits: Limits.t
        [@ocaml.doc
          "The maximum number of resource types that you can specify in a protection."]}
    let context_ = "ProtectionLimits"
    let make ~protectedResourceTypeLimits =
      fun () -> { protectedResourceTypeLimits }
    let to_value x =
      structure_to_value
        [("ProtectedResourceTypeLimits",
           (Some (Limits.to_value x.protectedResourceTypeLimits)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let protectedResourceTypeLimits =
        Limits.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "ProtectedResourceTypeLimits") in
      make ~protectedResourceTypeLimits ()
    let of_json json =
      let protectedResourceTypeLimits =
        field_map_exn json "ProtectedResourceTypeLimits" Limits.of_json in
      make ~protectedResourceTypeLimits ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Limits settings on protections for your subscription."]
module AttackVolume =
  struct
    type nonrec t =
      {
      bitsPerSecond: AttackVolumeStatistics.t option
        [@ocaml.doc
          "A statistics object that uses bits per second as the unit. This is included for network level attacks."];
      packetsPerSecond: AttackVolumeStatistics.t option
        [@ocaml.doc
          "A statistics object that uses packets per second as the unit. This is included for network level attacks."];
      requestsPerSecond: AttackVolumeStatistics.t option
        [@ocaml.doc
          "A statistics object that uses requests per second as the unit. This is included for application level attacks, and is only available for accounts that are subscribed to Shield Advanced."]}
    let make ?bitsPerSecond =
      fun ?packetsPerSecond ->
        fun ?requestsPerSecond ->
          fun () -> { bitsPerSecond; packetsPerSecond; requestsPerSecond }
    let to_value x =
      structure_to_value
        [("BitsPerSecond",
           (Option.map x.bitsPerSecond ~f:AttackVolumeStatistics.to_value));
        ("PacketsPerSecond",
          (Option.map x.packetsPerSecond ~f:AttackVolumeStatistics.to_value));
        ("RequestsPerSecond",
          (Option.map x.requestsPerSecond ~f:AttackVolumeStatistics.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let requestsPerSecond =
        (Option.map ~f:AttackVolumeStatistics.of_xml)
          (Xml.child xml_arg0 "RequestsPerSecond") in
      let packetsPerSecond =
        (Option.map ~f:AttackVolumeStatistics.of_xml)
          (Xml.child xml_arg0 "PacketsPerSecond") in
      let bitsPerSecond =
        (Option.map ~f:AttackVolumeStatistics.of_xml)
          (Xml.child xml_arg0 "BitsPerSecond") in
      make ?requestsPerSecond ?packetsPerSecond ?bitsPerSecond ()
    let of_json json =
      let requestsPerSecond =
        field_map json "RequestsPerSecond" AttackVolumeStatistics.of_json in
      let packetsPerSecond =
        field_map json "PacketsPerSecond" AttackVolumeStatistics.of_json in
      let bitsPerSecond =
        field_map json "BitsPerSecond" AttackVolumeStatistics.of_json in
      make ?requestsPerSecond ?packetsPerSecond ?bitsPerSecond ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the volume of attacks during the time period, included in an AttackStatisticsDataItem. If the accompanying AttackCount in the statistics object is zero, this setting might be empty."]
module AttackProperty =
  struct
    type nonrec t =
      {
      attackLayer: AttackLayer.t option
        [@ocaml.doc
          "The type of Shield event that was observed. NETWORK indicates layer 3 and layer 4 events and APPLICATION indicates layer 7 events. For infrastructure layer events (L3 and L4 events), you can view metrics for top contributors in Amazon CloudWatch metrics. For more information, see Shield metrics and alarms in the WAF Developer Guide."];
      attackPropertyIdentifier: AttackPropertyIdentifier.t option
        [@ocaml.doc
          "Defines the Shield event property information that is provided. The WORDPRESS_PINGBACK_REFLECTOR and WORDPRESS_PINGBACK_SOURCE values are valid only for WordPress reflective pingback events."];
      topContributors: TopContributors.t option
        [@ocaml.doc
          "Contributor objects for the top five contributors to a Shield event. A contributor is a source of traffic that Shield Advanced identifies as responsible for some or all of an event."];
      unit: Unit.t option
        [@ocaml.doc "The unit used for the Contributor Value property."];
      total: Long.t option
        [@ocaml.doc
          "The total contributions made to this Shield event by all contributors."]}
    let make ?attackLayer =
      fun ?attackPropertyIdentifier ->
        fun ?topContributors ->
          fun ?unit ->
            fun ?total ->
              fun () ->
                {
                  attackLayer;
                  attackPropertyIdentifier;
                  topContributors;
                  unit;
                  total
                }
    let to_value x =
      structure_to_value
        [("AttackLayer", (Option.map x.attackLayer ~f:AttackLayer.to_value));
        ("AttackPropertyIdentifier",
          (Option.map x.attackPropertyIdentifier
             ~f:AttackPropertyIdentifier.to_value));
        ("TopContributors",
          (Option.map x.topContributors ~f:TopContributors.to_value));
        ("Unit", (Option.map x.unit ~f:Unit.to_value));
        ("Total", (Option.map x.total ~f:Long.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let total = (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "Total") in
      let unit = (Option.map ~f:Unit.of_xml) (Xml.child xml_arg0 "Unit") in
      let topContributors =
        (Option.map ~f:TopContributors.of_xml)
          (Xml.child xml_arg0 "TopContributors") in
      let attackPropertyIdentifier =
        (Option.map ~f:AttackPropertyIdentifier.of_xml)
          (Xml.child xml_arg0 "AttackPropertyIdentifier") in
      let attackLayer =
        (Option.map ~f:AttackLayer.of_xml) (Xml.child xml_arg0 "AttackLayer") in
      make ?total ?unit ?topContributors ?attackPropertyIdentifier
        ?attackLayer ()
    let of_json json =
      let total = field_map json "Total" Long.of_json in
      let unit = field_map json "Unit" Unit.of_json in
      let topContributors =
        field_map json "TopContributors" TopContributors.of_json in
      let attackPropertyIdentifier =
        field_map json "AttackPropertyIdentifier"
          AttackPropertyIdentifier.of_json in
      let attackLayer = field_map json "AttackLayer" AttackLayer.of_json in
      make ?total ?unit ?topContributors ?attackPropertyIdentifier
        ?attackLayer ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Details of a Shield event. This is provided as part of an AttackDetail."]
module Mitigation =
  struct
    type nonrec t =
      {
      mitigationName: String_.t option
        [@ocaml.doc "The name of the mitigation taken for this attack."]}
    let make ?mitigationName = fun () -> { mitigationName }
    let to_value x =
      structure_to_value
        [("MitigationName",
           (Option.map x.mitigationName ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let mitigationName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "MitigationName") in
      make ?mitigationName ()
    let of_json json =
      let mitigationName = field_map json "MitigationName" String_.of_json in
      make ?mitigationName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The mitigation applied to a DDoS attack."]
module SubResourceSummary =
  struct
    type nonrec t =
      {
      type_: SubResourceType.t option [@ocaml.doc "The SubResource type."];
      id: String_.t option
        [@ocaml.doc "The unique identifier (ID) of the SubResource."];
      attackVectors: SummarizedAttackVectorList.t option
        [@ocaml.doc "The list of attack types and associated counters."];
      counters: SummarizedCounterList.t option
        [@ocaml.doc "The counters that describe the details of the attack."]}
    let make ?type_ =
      fun ?id ->
        fun ?attackVectors ->
          fun ?counters -> fun () -> { type_; id; attackVectors; counters }
    let to_value x =
      structure_to_value
        [("Type", (Option.map x.type_ ~f:SubResourceType.to_value));
        ("Id", (Option.map x.id ~f:String_.to_value));
        ("AttackVectors",
          (Option.map x.attackVectors ~f:SummarizedAttackVectorList.to_value));
        ("Counters",
          (Option.map x.counters ~f:SummarizedCounterList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let counters =
        (Option.map ~f:SummarizedCounterList.of_xml)
          (Xml.child xml_arg0 "Counters") in
      let attackVectors =
        (Option.map ~f:SummarizedAttackVectorList.of_xml)
          (Xml.child xml_arg0 "AttackVectors") in
      let id = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Id") in
      let type_ =
        (Option.map ~f:SubResourceType.of_xml) (Xml.child xml_arg0 "Type") in
      make ?counters ?attackVectors ?id ?type_ ()
    let of_json json =
      let counters = field_map json "Counters" SummarizedCounterList.of_json in
      let attackVectors =
        field_map json "AttackVectors" SummarizedAttackVectorList.of_json in
      let id = field_map json "Id" String_.of_json in
      let type_ = field_map json "Type" SubResourceType.of_json in
      make ?counters ?attackVectors ?id ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The attack information for the specified SubResource."]
module ErrorMessage =
  struct
    type nonrec t = string
    let context_ = "errorMessage"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"errorMessage" j
    let to_json = simple_to_json to_value
  end
module ValidationExceptionFieldList =
  struct
    type nonrec t = ValidationExceptionField.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ValidationExceptionField.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ValidationExceptionField.of_xml)
    let of_json j =
      list_of_json ~kind:"ValidationExceptionFieldList"
        ~of_json:ValidationExceptionField.of_json j
    let to_json v = composed_to_json to_value v
  end
module ValidationExceptionReason =
  struct
    type nonrec t =
      | FIELD_VALIDATION_FAILED 
      | OTHER 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | FIELD_VALIDATION_FAILED -> "FIELD_VALIDATION_FAILED"
      | OTHER -> "OTHER"
      | Non_static_id s -> s
    let of_string =
      function
      | "FIELD_VALIDATION_FAILED" -> FIELD_VALIDATION_FAILED
      | "OTHER" -> OTHER
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ValidationExceptionReason" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ValidationExceptionReason" j)
    let to_json = simple_to_json to_value
  end
module EmergencyContact =
  struct
    type nonrec t =
      {
      emailAddress: EmailAddress.t
        [@ocaml.doc "The email address for the contact."];
      phoneNumber: PhoneNumber.t option
        [@ocaml.doc "The phone number for the contact."];
      contactNotes: ContactNotes.t option
        [@ocaml.doc "Additional notes regarding the contact."]}
    let context_ = "EmergencyContact"
    let make ?phoneNumber =
      fun ?contactNotes ->
        fun ~emailAddress ->
          fun () -> { phoneNumber; contactNotes; emailAddress }
    let to_value x =
      structure_to_value
        [("EmailAddress", (Some (EmailAddress.to_value x.emailAddress)));
        ("PhoneNumber", (Option.map x.phoneNumber ~f:PhoneNumber.to_value));
        ("ContactNotes",
          (Option.map x.contactNotes ~f:ContactNotes.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let contactNotes =
        (Option.map ~f:ContactNotes.of_xml)
          (Xml.child xml_arg0 "ContactNotes") in
      let phoneNumber =
        (Option.map ~f:PhoneNumber.of_xml) (Xml.child xml_arg0 "PhoneNumber") in
      let emailAddress =
        EmailAddress.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "EmailAddress") in
      make ?contactNotes ?phoneNumber ~emailAddress ()
    let of_json json =
      let contactNotes = field_map json "ContactNotes" ContactNotes.of_json in
      let phoneNumber = field_map json "PhoneNumber" PhoneNumber.of_json in
      let emailAddress =
        field_map_exn json "EmailAddress" EmailAddress.of_json in
      make ?contactNotes ?phoneNumber ~emailAddress ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Contact information that the SRT can use to contact you if you have proactive engagement enabled, for escalations to the SRT and to initiate proactive customer support."]
module Tag =
  struct
    type nonrec t =
      {
      key: TagKey.t option
        [@ocaml.doc
          "Part of the key:value pair that defines a tag. You can use a tag key to describe a category of information, such as \"customer.\" Tag keys are case-sensitive."];
      value: TagValue.t option
        [@ocaml.doc
          "Part of the key:value pair that defines a tag. You can use a tag value to describe a specific value within a category, such as \"companyA\" or \"companyB.\" Tag values are case-sensitive."]}
    let make ?key = fun ?value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("Key", (Option.map x.key ~f:TagKey.to_value));
        ("Value", (Option.map x.value ~f:TagValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:TagValue.of_xml) (Xml.child xml_arg0 "Value") in
      let key = (Option.map ~f:TagKey.of_xml) (Xml.child xml_arg0 "Key") in
      make ?value ?key ()
    let of_json json =
      let value = field_map json "Value" TagValue.of_json in
      let key = field_map json "Key" TagKey.of_json in make ?value ?key ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A tag associated with an Amazon Web Services resource. Tags are key:value pairs that you can use to categorize and manage your resources, for purposes like billing or other management. Typically, the tag key represents a category, such as \"environment\", and the tag value represents a specific value within that category, such as \"test,\" \"development,\" or \"production\". Or you might set the tag key to \"customer\" and the value to the customer name or ID. You can specify one or more tags to add to each Amazon Web Services resource, up to 50 tags for a resource."]
module Protection =
  struct
    type nonrec t =
      {
      id: ProtectionId.t option
        [@ocaml.doc "The unique identifier (ID) of the protection."];
      name: ProtectionName.t option
        [@ocaml.doc
          "The name of the protection. For example, My CloudFront distributions."];
      resourceArn: ResourceArn.t option
        [@ocaml.doc
          "The ARN (Amazon Resource Name) of the Amazon Web Services resource that is protected."];
      healthCheckIds: HealthCheckIds.t option
        [@ocaml.doc
          "The unique identifier (ID) for the Route\194\16053 health check that's associated with the protection."];
      protectionArn: ResourceArn.t option
        [@ocaml.doc "The ARN (Amazon Resource Name) of the protection."];
      applicationLayerAutomaticResponseConfiguration:
        ApplicationLayerAutomaticResponseConfiguration.t option
        [@ocaml.doc
          "The automatic application layer DDoS mitigation settings for the protection. This configuration determines whether Shield Advanced automatically manages rules in the web ACL in order to respond to application layer events that Shield Advanced determines to be DDoS attacks."]}
    let make ?id =
      fun ?name ->
        fun ?resourceArn ->
          fun ?healthCheckIds ->
            fun ?protectionArn ->
              fun ?applicationLayerAutomaticResponseConfiguration ->
                fun () ->
                  {
                    id;
                    name;
                    resourceArn;
                    healthCheckIds;
                    protectionArn;
                    applicationLayerAutomaticResponseConfiguration
                  }
    let to_value x =
      structure_to_value
        [("Id", (Option.map x.id ~f:ProtectionId.to_value));
        ("Name", (Option.map x.name ~f:ProtectionName.to_value));
        ("ResourceArn", (Option.map x.resourceArn ~f:ResourceArn.to_value));
        ("HealthCheckIds",
          (Option.map x.healthCheckIds ~f:HealthCheckIds.to_value));
        ("ProtectionArn",
          (Option.map x.protectionArn ~f:ResourceArn.to_value));
        ("ApplicationLayerAutomaticResponseConfiguration",
          (Option.map x.applicationLayerAutomaticResponseConfiguration
             ~f:ApplicationLayerAutomaticResponseConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let applicationLayerAutomaticResponseConfiguration =
        (Option.map ~f:ApplicationLayerAutomaticResponseConfiguration.of_xml)
          (Xml.child xml_arg0
             "ApplicationLayerAutomaticResponseConfiguration") in
      let protectionArn =
        (Option.map ~f:ResourceArn.of_xml)
          (Xml.child xml_arg0 "ProtectionArn") in
      let healthCheckIds =
        (Option.map ~f:HealthCheckIds.of_xml)
          (Xml.child xml_arg0 "HealthCheckIds") in
      let resourceArn =
        (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "ResourceArn") in
      let name =
        (Option.map ~f:ProtectionName.of_xml) (Xml.child xml_arg0 "Name") in
      let id = (Option.map ~f:ProtectionId.of_xml) (Xml.child xml_arg0 "Id") in
      make ?applicationLayerAutomaticResponseConfiguration ?protectionArn
        ?healthCheckIds ?resourceArn ?name ?id ()
    let of_json json =
      let applicationLayerAutomaticResponseConfiguration =
        field_map json "ApplicationLayerAutomaticResponseConfiguration"
          ApplicationLayerAutomaticResponseConfiguration.of_json in
      let protectionArn = field_map json "ProtectionArn" ResourceArn.of_json in
      let healthCheckIds =
        field_map json "HealthCheckIds" HealthCheckIds.of_json in
      let resourceArn = field_map json "ResourceArn" ResourceArn.of_json in
      let name = field_map json "Name" ProtectionName.of_json in
      let id = field_map json "Id" ProtectionId.of_json in
      make ?applicationLayerAutomaticResponseConfiguration ?protectionArn
        ?healthCheckIds ?resourceArn ?name ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An object that represents a resource that is under DDoS protection."]
module ProtectionGroup =
  struct
    type nonrec t =
      {
      protectionGroupId: ProtectionGroupId.t
        [@ocaml.doc
          "The name of the protection group. You use this to identify the protection group in lists and to manage the protection group, for example to update, delete, or describe it."];
      aggregation: ProtectionGroupAggregation.t
        [@ocaml.doc
          "Defines how Shield combines resource data for the group in order to detect, mitigate, and report events. Sum - Use the total traffic across the group. This is a good choice for most cases. Examples include Elastic IP addresses for EC2 instances that scale manually or automatically. Mean - Use the average of the traffic across the group. This is a good choice for resources that share traffic uniformly. Examples include accelerators and load balancers. Max - Use the highest traffic from each resource. This is useful for resources that don't share traffic and for resources that share that traffic in a non-uniform way. Examples include Amazon CloudFront distributions and origin resources for CloudFront distributions."];
      pattern: ProtectionGroupPattern.t
        [@ocaml.doc
          "The criteria to use to choose the protected resources for inclusion in the group. You can include all resources that have protections, provide a list of resource Amazon Resource Names (ARNs), or include all resources of a specified resource type."];
      resourceType: ProtectedResourceType.t option
        [@ocaml.doc
          "The resource type to include in the protection group. All protected resources of this type are included in the protection group. You must set this when you set Pattern to BY_RESOURCE_TYPE and you must not set it for any other Pattern setting."];
      members: ProtectionGroupMembers.t
        [@ocaml.doc
          "The Amazon Resource Names (ARNs) of the resources to include in the protection group. You must set this when you set Pattern to ARBITRARY and you must not set it for any other Pattern setting."];
      protectionGroupArn: ResourceArn.t option
        [@ocaml.doc
          "The ARN (Amazon Resource Name) of the protection group."]}
    let context_ = "ProtectionGroup"
    let make ?resourceType =
      fun ?protectionGroupArn ->
        fun ~protectionGroupId ->
          fun ~aggregation ->
            fun ~pattern ->
              fun ~members ->
                fun () ->
                  {
                    resourceType;
                    protectionGroupArn;
                    protectionGroupId;
                    aggregation;
                    pattern;
                    members
                  }
    let to_value x =
      structure_to_value
        [("ProtectionGroupId",
           (Some (ProtectionGroupId.to_value x.protectionGroupId)));
        ("Aggregation",
          (Some (ProtectionGroupAggregation.to_value x.aggregation)));
        ("Pattern", (Some (ProtectionGroupPattern.to_value x.pattern)));
        ("ResourceType",
          (Option.map x.resourceType ~f:ProtectedResourceType.to_value));
        ("Members", (Some (ProtectionGroupMembers.to_value x.members)));
        ("ProtectionGroupArn",
          (Option.map x.protectionGroupArn ~f:ResourceArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let protectionGroupArn =
        (Option.map ~f:ResourceArn.of_xml)
          (Xml.child xml_arg0 "ProtectionGroupArn") in
      let members =
        ProtectionGroupMembers.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Members") in
      let resourceType =
        (Option.map ~f:ProtectedResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let pattern =
        ProtectionGroupPattern.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Pattern") in
      let aggregation =
        ProtectionGroupAggregation.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Aggregation") in
      let protectionGroupId =
        ProtectionGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProtectionGroupId") in
      make ?protectionGroupArn ~members ?resourceType ~pattern ~aggregation
        ~protectionGroupId ()
    let of_json json =
      let protectionGroupArn =
        field_map json "ProtectionGroupArn" ResourceArn.of_json in
      let members =
        field_map_exn json "Members" ProtectionGroupMembers.of_json in
      let resourceType =
        field_map json "ResourceType" ProtectedResourceType.of_json in
      let pattern =
        field_map_exn json "Pattern" ProtectionGroupPattern.of_json in
      let aggregation =
        field_map_exn json "Aggregation" ProtectionGroupAggregation.of_json in
      let protectionGroupId =
        field_map_exn json "ProtectionGroupId" ProtectionGroupId.of_json in
      make ?protectionGroupArn ~members ?resourceType ~pattern ~aggregation
        ~protectionGroupId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A grouping of protected resources that you and Shield Advanced can monitor as a collective. This resource grouping improves the accuracy of detection and reduces false positives."]
module AttackSummary =
  struct
    type nonrec t =
      {
      attackId: String_.t option
        [@ocaml.doc "The unique identifier (ID) of the attack."];
      resourceArn: String_.t option
        [@ocaml.doc
          "The ARN (Amazon Resource Name) of the resource that was attacked."];
      startTime: AttackTimestamp.t option
        [@ocaml.doc "The start time of the attack, in Unix time in seconds."];
      endTime: AttackTimestamp.t option
        [@ocaml.doc "The end time of the attack, in Unix time in seconds."];
      attackVectors: AttackVectorDescriptionList.t option
        [@ocaml.doc "The list of attacks for a specified time period."]}
    let make ?attackId =
      fun ?resourceArn ->
        fun ?startTime ->
          fun ?endTime ->
            fun ?attackVectors ->
              fun () ->
                { attackId; resourceArn; startTime; endTime; attackVectors }
    let to_value x =
      structure_to_value
        [("AttackId", (Option.map x.attackId ~f:String_.to_value));
        ("ResourceArn", (Option.map x.resourceArn ~f:String_.to_value));
        ("StartTime", (Option.map x.startTime ~f:AttackTimestamp.to_value));
        ("EndTime", (Option.map x.endTime ~f:AttackTimestamp.to_value));
        ("AttackVectors",
          (Option.map x.attackVectors ~f:AttackVectorDescriptionList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attackVectors =
        (Option.map ~f:AttackVectorDescriptionList.of_xml)
          (Xml.child xml_arg0 "AttackVectors") in
      let endTime =
        (Option.map ~f:AttackTimestamp.of_xml) (Xml.child xml_arg0 "EndTime") in
      let startTime =
        (Option.map ~f:AttackTimestamp.of_xml)
          (Xml.child xml_arg0 "StartTime") in
      let resourceArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ResourceArn") in
      let attackId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "AttackId") in
      make ?attackVectors ?endTime ?startTime ?resourceArn ?attackId ()
    let of_json json =
      let attackVectors =
        field_map json "AttackVectors" AttackVectorDescriptionList.of_json in
      let endTime = field_map json "EndTime" AttackTimestamp.of_json in
      let startTime = field_map json "StartTime" AttackTimestamp.of_json in
      let resourceArn = field_map json "ResourceArn" String_.of_json in
      let attackId = field_map json "AttackId" String_.of_json in
      make ?attackVectors ?endTime ?startTime ?resourceArn ?attackId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Summarizes all DDoS attacks for a specified time period."]
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module LimitNumber =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module LimitType =
  struct
    type nonrec t = string
    let context_ = "LimitType"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LimitType" j
    let to_json = simple_to_json to_value
  end
module AutoRenew =
  struct
    type nonrec t =
      | ENABLED 
      | DISABLED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ENABLED -> "ENABLED"
      | DISABLED -> "DISABLED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ENABLED" -> ENABLED
      | "DISABLED" -> DISABLED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration AutoRenew" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AutoRenew" j)
    let to_json = simple_to_json to_value
  end
module DurationInSeconds =
  struct
    type nonrec t = Int64.t
    let make i =
      let open Result in ok_or_failwith (check_int64_min i ~min:0L); i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module ProactiveEngagementStatus =
  struct
    type nonrec t =
      | ENABLED 
      | DISABLED 
      | PENDING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ENABLED -> "ENABLED"
      | DISABLED -> "DISABLED"
      | PENDING -> "PENDING"
      | Non_static_id s -> s
    let of_string =
      function
      | "ENABLED" -> ENABLED
      | "DISABLED" -> DISABLED
      | "PENDING" -> PENDING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ProactiveEngagementStatus" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ProactiveEngagementStatus" j)
    let to_json = simple_to_json to_value
  end
module SubscriptionLimits =
  struct
    type nonrec t =
      {
      protectionLimits: ProtectionLimits.t
        [@ocaml.doc "Limits settings on protections for your subscription."];
      protectionGroupLimits: ProtectionGroupLimits.t
        [@ocaml.doc
          "Limits settings on protection groups for your subscription."]}
    let context_ = "SubscriptionLimits"
    let make ~protectionLimits =
      fun ~protectionGroupLimits ->
        fun () -> { protectionLimits; protectionGroupLimits }
    let to_value x =
      structure_to_value
        [("ProtectionLimits",
           (Some (ProtectionLimits.to_value x.protectionLimits)));
        ("ProtectionGroupLimits",
          (Some (ProtectionGroupLimits.to_value x.protectionGroupLimits)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let protectionGroupLimits =
        ProtectionGroupLimits.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProtectionGroupLimits") in
      let protectionLimits =
        ProtectionLimits.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProtectionLimits") in
      make ~protectionGroupLimits ~protectionLimits ()
    let of_json json =
      let protectionGroupLimits =
        field_map_exn json "ProtectionGroupLimits"
          ProtectionGroupLimits.of_json in
      let protectionLimits =
        field_map_exn json "ProtectionLimits" ProtectionLimits.of_json in
      make ~protectionGroupLimits ~protectionLimits ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Limits settings for your subscription."]
module LogBucket =
  struct
    type nonrec t = string
    let context_ = "LogBucket"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:3) >>=
             (fun () ->
                (check_string_max i ~max:63) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^([a-z]|(\\d(?!\\d{0,2}\\.\\d{1,3}\\.\\d{1,3}\\.\\d{1,3})))([a-z\\d]|(\\.(?!(\\.|-)))|(-(?!\\.))){1,61}[a-z\\d]$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"LogBucket" j
    let to_json = simple_to_json to_value
  end
module AttackStatisticsDataItem =
  struct
    type nonrec t =
      {
      attackVolume: AttackVolume.t option
        [@ocaml.doc
          "Information about the volume of attacks during the time period. If the accompanying AttackCount is zero, this setting might be empty."];
      attackCount: Long.t
        [@ocaml.doc
          "The number of attacks detected during the time period. This is always present, but might be zero."]}
    let context_ = "AttackStatisticsDataItem"
    let make ?attackVolume =
      fun ~attackCount -> fun () -> { attackVolume; attackCount }
    let to_value x =
      structure_to_value
        [("AttackVolume",
           (Option.map x.attackVolume ~f:AttackVolume.to_value));
        ("AttackCount", (Some (Long.to_value x.attackCount)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attackCount =
        Long.of_xml (Xml.child_exn ~context:context_ xml_arg0 "AttackCount") in
      let attackVolume =
        (Option.map ~f:AttackVolume.of_xml)
          (Xml.child xml_arg0 "AttackVolume") in
      make ~attackCount ?attackVolume ()
    let of_json json =
      let attackCount = field_map_exn json "AttackCount" Long.of_json in
      let attackVolume = field_map json "AttackVolume" AttackVolume.of_json in
      make ~attackCount ?attackVolume ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A single attack statistics data record. This is returned by DescribeAttackStatistics along with a time range indicating the time period that the attack statistics apply to."]
module AttackId =
  struct
    type nonrec t = string
    let context_ = "AttackId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () -> check_pattern i ~pattern:"[a-zA-Z0-9\\\\-]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AttackId" j
    let to_json = simple_to_json to_value
  end
module AttackProperties =
  struct
    type nonrec t = AttackProperty.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AttackProperty.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AttackProperty.of_xml)
    let of_json j =
      list_of_json ~kind:"AttackProperties" ~of_json:AttackProperty.of_json j
    let to_json v = composed_to_json to_value v
  end
module MitigationList =
  struct
    type nonrec t = Mitigation.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Mitigation.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Mitigation.of_xml)
    let of_json j =
      list_of_json ~kind:"MitigationList" ~of_json:Mitigation.of_json j
    let to_json v = composed_to_json to_value v
  end
module SubResourceSummaryList =
  struct
    type nonrec t = SubResourceSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:SubResourceSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:SubResourceSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"SubResourceSummaryList"
        ~of_json:SubResourceSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module InternalErrorException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Exception that indicates that a problem occurred with the service infrastructure. You can retry the request."]
module InvalidParameterException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option ;
      reason: ValidationExceptionReason.t option
        [@ocaml.doc "Additional information about the exception."];
      fields: ValidationExceptionFieldList.t option
        [@ocaml.doc "Fields that caused the exception."]}
    let make ?message =
      fun ?reason -> fun ?fields -> fun () -> { message; reason; fields }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value));
        ("reason",
          (Option.map x.reason ~f:ValidationExceptionReason.to_value));
        ("fields",
          (Option.map x.fields ~f:ValidationExceptionFieldList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let fields =
        (Option.map ~f:ValidationExceptionFieldList.of_xml)
          (Xml.child xml_arg0 "fields") in
      let reason =
        (Option.map ~f:ValidationExceptionReason.of_xml)
          (Xml.child xml_arg0 "reason") in
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?fields ?reason ?message ()
    let of_json json =
      let fields =
        field_map json "fields" ValidationExceptionFieldList.of_json in
      let reason = field_map json "reason" ValidationExceptionReason.of_json in
      let message = field_map json "message" ErrorMessage.of_json in
      make ?fields ?reason ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Exception that indicates that the parameters passed to the API are invalid. If available, this exception includes details in additional properties."]
module LockedSubscriptionException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You are trying to update a subscription that has not yet completed the 1-year commitment. You can change the AutoRenew parameter during the last 30 days of your subscription. This exception indicates that you are attempting to change AutoRenew prior to that period."]
module OptimisticLockException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Exception that indicates that the resource state has been modified by another client. Retrieve the resource and then retry your request."]
module ResourceNotFoundException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option ;
      resourceType: String_.t option [@ocaml.doc "Type of resource."]}
    let make ?message =
      fun ?resourceType -> fun () -> { message; resourceType }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value));
        ("resourceType", (Option.map x.resourceType ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "resourceType") in
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?resourceType ?message ()
    let of_json json =
      let resourceType = field_map json "resourceType" String_.of_json in
      let message = field_map json "message" ErrorMessage.of_json in
      make ?resourceType ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Exception indicating the specified resource does not exist. If available, this exception includes details in additional properties."]
module EmergencyContactList =
  struct
    type nonrec t = EmergencyContact.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:EmergencyContact.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EmergencyContact.of_xml)
    let of_json j =
      list_of_json ~kind:"EmergencyContactList"
        ~of_json:EmergencyContact.of_json j
    let to_json v = composed_to_json to_value v
  end
module InvalidOperationException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Exception that indicates that the operation would not cause any change to occur."]
module InvalidResourceException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Exception that indicates that the resource is invalid. You might not have access to the resource, or the resource might not exist."]
module TagKeyList =
  struct
    type nonrec t = TagKey.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeyList" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module TagList =
  struct
    type nonrec t = Tag.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:Tag.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tag.of_xml)
    let of_json j = list_of_json ~kind:"TagList" ~of_json:Tag.of_json j
    let to_json v = composed_to_json to_value v
  end
module InvalidPaginationTokenException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Exception that indicates that the NextToken specified in the request is invalid. Submit the request using the NextToken value that was returned in the prior response."]
module ResourceArnList =
  struct
    type nonrec t = ResourceArn.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ResourceArn.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResourceArn.of_xml)
    let of_json j =
      list_of_json ~kind:"ResourceArnList" ~of_json:ResourceArn.of_json j
    let to_json v = composed_to_json to_value v
  end
module Token =
  struct
    type nonrec t = string
    let context_ = "Token"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:4096) >>=
                  (fun () -> check_pattern i ~pattern:"^.*$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Token" j
    let to_json = simple_to_json to_value
  end
module MaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:10000) >>=
             (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Protections =
  struct
    type nonrec t = Protection.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Protection.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Protection.of_xml)
    let of_json j =
      list_of_json ~kind:"Protections" ~of_json:Protection.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProtectionGroups =
  struct
    type nonrec t = ProtectionGroup.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ProtectionGroup.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ProtectionGroup.of_xml)
    let of_json j =
      list_of_json ~kind:"ProtectionGroups" ~of_json:ProtectionGroup.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module AttackSummaries =
  struct
    type nonrec t = AttackSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AttackSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AttackSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"AttackSummaries" ~of_json:AttackSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceArnFilterList =
  struct
    type nonrec t = ResourceArn.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ResourceArn.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResourceArn.of_xml)
    let of_json j =
      list_of_json ~kind:"ResourceArnFilterList" ~of_json:ResourceArn.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module TimeRange =
  struct
    type nonrec t =
      {
      fromInclusive: Timestamp.t option
        [@ocaml.doc "The start time, in Unix time in seconds."];
      toExclusive: Timestamp.t option
        [@ocaml.doc "The end time, in Unix time in seconds."]}
    let make ?fromInclusive =
      fun ?toExclusive -> fun () -> { fromInclusive; toExclusive }
    let to_value x =
      structure_to_value
        [("FromInclusive",
           (Option.map x.fromInclusive ~f:Timestamp.to_value));
        ("ToExclusive", (Option.map x.toExclusive ~f:Timestamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let toExclusive =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "ToExclusive") in
      let fromInclusive =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "FromInclusive") in
      make ?toExclusive ?fromInclusive ()
    let of_json json =
      let toExclusive = field_map json "ToExclusive" Timestamp.of_json in
      let fromInclusive = field_map json "FromInclusive" Timestamp.of_json in
      make ?toExclusive ?fromInclusive ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The time range."]
module SubscriptionState =
  struct
    type nonrec t =
      | ACTIVE 
      | INACTIVE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ACTIVE -> "ACTIVE"
      | INACTIVE -> "INACTIVE"
      | Non_static_id s -> s
    let of_string =
      function
      | "ACTIVE" -> ACTIVE
      | "INACTIVE" -> INACTIVE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration SubscriptionState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"SubscriptionState" j)
    let to_json = simple_to_json to_value
  end
module LimitsExceededException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option ;
      type_: LimitType.t option
        [@ocaml.doc "The type of limit that would be exceeded."];
      limit: LimitNumber.t option
        [@ocaml.doc "The threshold that would be exceeded."]}
    let make ?message =
      fun ?type_ -> fun ?limit -> fun () -> { message; type_; limit }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value));
        ("Type", (Option.map x.type_ ~f:LimitType.to_value));
        ("Limit", (Option.map x.limit ~f:LimitNumber.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let limit =
        (Option.map ~f:LimitNumber.of_xml) (Xml.child xml_arg0 "Limit") in
      let type_ =
        (Option.map ~f:LimitType.of_xml) (Xml.child xml_arg0 "Type") in
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?limit ?type_ ?message ()
    let of_json json =
      let limit = field_map json "Limit" LimitNumber.of_json in
      let type_ = field_map json "Type" LimitType.of_json in
      let message = field_map json "message" ErrorMessage.of_json in
      make ?limit ?type_ ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Exception that indicates that the operation would exceed a limit."]
module HealthCheckArn =
  struct
    type nonrec t = string
    let context_ = "HealthCheckArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^arn:aws:route53:::healthcheck/\\S{36}$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"HealthCheckArn" j
    let to_json = simple_to_json to_value
  end
module AccessDeniedForDependencyException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "In order to grant the necessary access to the Shield Response Team (SRT) the user submitting the request must have the iam:PassRole permission. This error indicates the user did not have the appropriate permissions. For more information, see Granting a User Permissions to Pass a Role to an Amazon Web Services Service."]
module NoAssociatedRoleException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The ARN of the role that you specifed does not exist."]
module Subscription =
  struct
    type nonrec t =
      {
      startTime: Timestamp.t option
        [@ocaml.doc
          "The start time of the subscription, in Unix time in seconds."];
      endTime: Timestamp.t option
        [@ocaml.doc "The date and time your subscription will end."];
      timeCommitmentInSeconds: DurationInSeconds.t option
        [@ocaml.doc
          "The length, in seconds, of the Shield Advanced subscription for the account."];
      autoRenew: AutoRenew.t option
        [@ocaml.doc
          "If ENABLED, the subscription will be automatically renewed at the end of the existing subscription period. When you initally create a subscription, AutoRenew is set to ENABLED. You can change this by submitting an UpdateSubscription request. If the UpdateSubscription request does not included a value for AutoRenew, the existing value for AutoRenew remains unchanged."];
      limits: Limits.t option
        [@ocaml.doc
          "Specifies how many protections of a given type you can create."];
      proactiveEngagementStatus: ProactiveEngagementStatus.t option
        [@ocaml.doc
          "If ENABLED, the Shield Response Team (SRT) will use email and phone to notify contacts about escalations to the SRT and to initiate proactive customer support. If PENDING, you have requested proactive engagement and the request is pending. The status changes to ENABLED when your request is fully processed. If DISABLED, the SRT will not proactively notify contacts about escalations or to initiate proactive customer support."];
      subscriptionLimits: SubscriptionLimits.t
        [@ocaml.doc "Limits settings for your subscription."];
      subscriptionArn: ResourceArn.t option
        [@ocaml.doc "The ARN (Amazon Resource Name) of the subscription."]}
    let context_ = "Subscription"
    let make ?startTime =
      fun ?endTime ->
        fun ?timeCommitmentInSeconds ->
          fun ?autoRenew ->
            fun ?limits ->
              fun ?proactiveEngagementStatus ->
                fun ?subscriptionArn ->
                  fun ~subscriptionLimits ->
                    fun () ->
                      {
                        startTime;
                        endTime;
                        timeCommitmentInSeconds;
                        autoRenew;
                        limits;
                        proactiveEngagementStatus;
                        subscriptionArn;
                        subscriptionLimits
                      }
    let to_value x =
      structure_to_value
        [("StartTime", (Option.map x.startTime ~f:Timestamp.to_value));
        ("EndTime", (Option.map x.endTime ~f:Timestamp.to_value));
        ("TimeCommitmentInSeconds",
          (Option.map x.timeCommitmentInSeconds ~f:DurationInSeconds.to_value));
        ("AutoRenew", (Option.map x.autoRenew ~f:AutoRenew.to_value));
        ("Limits", (Option.map x.limits ~f:Limits.to_value));
        ("ProactiveEngagementStatus",
          (Option.map x.proactiveEngagementStatus
             ~f:ProactiveEngagementStatus.to_value));
        ("SubscriptionLimits",
          (Some (SubscriptionLimits.to_value x.subscriptionLimits)));
        ("SubscriptionArn",
          (Option.map x.subscriptionArn ~f:ResourceArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let subscriptionArn =
        (Option.map ~f:ResourceArn.of_xml)
          (Xml.child xml_arg0 "SubscriptionArn") in
      let subscriptionLimits =
        SubscriptionLimits.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SubscriptionLimits") in
      let proactiveEngagementStatus =
        (Option.map ~f:ProactiveEngagementStatus.of_xml)
          (Xml.child xml_arg0 "ProactiveEngagementStatus") in
      let limits =
        (Option.map ~f:Limits.of_xml) (Xml.child xml_arg0 "Limits") in
      let autoRenew =
        (Option.map ~f:AutoRenew.of_xml) (Xml.child xml_arg0 "AutoRenew") in
      let timeCommitmentInSeconds =
        (Option.map ~f:DurationInSeconds.of_xml)
          (Xml.child xml_arg0 "TimeCommitmentInSeconds") in
      let endTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "EndTime") in
      let startTime =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "StartTime") in
      make ?subscriptionArn ~subscriptionLimits ?proactiveEngagementStatus
        ?limits ?autoRenew ?timeCommitmentInSeconds ?endTime ?startTime ()
    let of_json json =
      let subscriptionArn =
        field_map json "SubscriptionArn" ResourceArn.of_json in
      let subscriptionLimits =
        field_map_exn json "SubscriptionLimits" SubscriptionLimits.of_json in
      let proactiveEngagementStatus =
        field_map json "ProactiveEngagementStatus"
          ProactiveEngagementStatus.of_json in
      let limits = field_map json "Limits" Limits.of_json in
      let autoRenew = field_map json "AutoRenew" AutoRenew.of_json in
      let timeCommitmentInSeconds =
        field_map json "TimeCommitmentInSeconds" DurationInSeconds.of_json in
      let endTime = field_map json "EndTime" Timestamp.of_json in
      let startTime = field_map json "StartTime" Timestamp.of_json in
      make ?subscriptionArn ~subscriptionLimits ?proactiveEngagementStatus
        ?limits ?autoRenew ?timeCommitmentInSeconds ?endTime ?startTime ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the Shield Advanced subscription for an account."]
module LogBucketList =
  struct
    type nonrec t = LogBucket.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:LogBucket.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:LogBucket.of_xml)
    let of_json j =
      list_of_json ~kind:"LogBucketList" ~of_json:LogBucket.of_json j
    let to_json v = composed_to_json to_value v
  end
module RoleArn =
  struct
    type nonrec t = string
    let context_ = "RoleArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:2048) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^arn:aws:iam::\\d{12}:role/?[a-zA-Z_0-9+=,.@\\-_/]+")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RoleArn" j
    let to_json = simple_to_json to_value
  end
module AttackStatisticsDataList =
  struct
    type nonrec t = AttackStatisticsDataItem.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AttackStatisticsDataItem.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AttackStatisticsDataItem.of_xml)
    let of_json j =
      list_of_json ~kind:"AttackStatisticsDataList"
        ~of_json:AttackStatisticsDataItem.of_json j
    let to_json v = composed_to_json to_value v
  end
module AccessDeniedException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Exception that indicates the specified AttackId does not exist, or the requester does not have the appropriate permissions to access the AttackId."]
module AttackDetail =
  struct
    type nonrec t =
      {
      attackId: AttackId.t option
        [@ocaml.doc "The unique identifier (ID) of the attack."];
      resourceArn: ResourceArn.t option
        [@ocaml.doc
          "The ARN (Amazon Resource Name) of the resource that was attacked."];
      subResources: SubResourceSummaryList.t option
        [@ocaml.doc
          "If applicable, additional detail about the resource being attacked, for example, IP address or URL."];
      startTime: AttackTimestamp.t option
        [@ocaml.doc "The time the attack started, in Unix time in seconds."];
      endTime: AttackTimestamp.t option
        [@ocaml.doc "The time the attack ended, in Unix time in seconds."];
      attackCounters: SummarizedCounterList.t option
        [@ocaml.doc
          "List of counters that describe the attack for the specified time period."];
      attackProperties: AttackProperties.t option
        [@ocaml.doc
          "The array of objects that provide details of the Shield event. For infrastructure layer events (L3 and L4 events), you can view metrics for top contributors in Amazon CloudWatch metrics. For more information, see Shield metrics and alarms in the WAF Developer Guide."];
      mitigations: MitigationList.t option
        [@ocaml.doc "List of mitigation actions taken for the attack."]}
    let make ?attackId =
      fun ?resourceArn ->
        fun ?subResources ->
          fun ?startTime ->
            fun ?endTime ->
              fun ?attackCounters ->
                fun ?attackProperties ->
                  fun ?mitigations ->
                    fun () ->
                      {
                        attackId;
                        resourceArn;
                        subResources;
                        startTime;
                        endTime;
                        attackCounters;
                        attackProperties;
                        mitigations
                      }
    let to_value x =
      structure_to_value
        [("AttackId", (Option.map x.attackId ~f:AttackId.to_value));
        ("ResourceArn", (Option.map x.resourceArn ~f:ResourceArn.to_value));
        ("SubResources",
          (Option.map x.subResources ~f:SubResourceSummaryList.to_value));
        ("StartTime", (Option.map x.startTime ~f:AttackTimestamp.to_value));
        ("EndTime", (Option.map x.endTime ~f:AttackTimestamp.to_value));
        ("AttackCounters",
          (Option.map x.attackCounters ~f:SummarizedCounterList.to_value));
        ("AttackProperties",
          (Option.map x.attackProperties ~f:AttackProperties.to_value));
        ("Mitigations",
          (Option.map x.mitigations ~f:MitigationList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let mitigations =
        (Option.map ~f:MitigationList.of_xml)
          (Xml.child xml_arg0 "Mitigations") in
      let attackProperties =
        (Option.map ~f:AttackProperties.of_xml)
          (Xml.child xml_arg0 "AttackProperties") in
      let attackCounters =
        (Option.map ~f:SummarizedCounterList.of_xml)
          (Xml.child xml_arg0 "AttackCounters") in
      let endTime =
        (Option.map ~f:AttackTimestamp.of_xml) (Xml.child xml_arg0 "EndTime") in
      let startTime =
        (Option.map ~f:AttackTimestamp.of_xml)
          (Xml.child xml_arg0 "StartTime") in
      let subResources =
        (Option.map ~f:SubResourceSummaryList.of_xml)
          (Xml.child xml_arg0 "SubResources") in
      let resourceArn =
        (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "ResourceArn") in
      let attackId =
        (Option.map ~f:AttackId.of_xml) (Xml.child xml_arg0 "AttackId") in
      make ?mitigations ?attackProperties ?attackCounters ?endTime ?startTime
        ?subResources ?resourceArn ?attackId ()
    let of_json json =
      let mitigations = field_map json "Mitigations" MitigationList.of_json in
      let attackProperties =
        field_map json "AttackProperties" AttackProperties.of_json in
      let attackCounters =
        field_map json "AttackCounters" SummarizedCounterList.of_json in
      let endTime = field_map json "EndTime" AttackTimestamp.of_json in
      let startTime = field_map json "StartTime" AttackTimestamp.of_json in
      let subResources =
        field_map json "SubResources" SubResourceSummaryList.of_json in
      let resourceArn = field_map json "ResourceArn" ResourceArn.of_json in
      let attackId = field_map json "AttackId" AttackId.of_json in
      make ?mitigations ?attackProperties ?attackCounters ?endTime ?startTime
        ?subResources ?resourceArn ?attackId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The details of a DDoS attack."]
module ResourceAlreadyExistsException =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option ;
      resourceType: String_.t option
        [@ocaml.doc "The type of resource that already exists."]}
    let make ?message =
      fun ?resourceType -> fun () -> { message; resourceType }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value));
        ("resourceType", (Option.map x.resourceType ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "resourceType") in
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?resourceType ?message ()
    let of_json json =
      let resourceType = field_map json "resourceType" String_.of_json in
      let message = field_map json "message" ErrorMessage.of_json in
      make ?resourceType ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Exception indicating the specified resource already exists. If available, this exception includes details in additional properties."]
module UpdateSubscriptionResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `LockedSubscriptionException of LockedSubscriptionException.t 
      | `OptimisticLockException of OptimisticLockException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "LockedSubscriptionException" ->
          `LockedSubscriptionException
            (LockedSubscriptionException.of_json json)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "LockedSubscriptionException" ->
          `LockedSubscriptionException
            (LockedSubscriptionException.of_xml xml)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `LockedSubscriptionException e ->
          `Assoc
            [("error", (`String "LockedSubscriptionException"));
            ("details", (LockedSubscriptionException.to_json e))]
      | `OptimisticLockException e ->
          `Assoc
            [("error", (`String "OptimisticLockException"));
            ("details", (OptimisticLockException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the details of an existing subscription. Only enter values for parameters you want to change. Empty parameters are not updated."]
module UpdateSubscriptionRequest =
  struct
    type nonrec t =
      {
      autoRenew: AutoRenew.t option
        [@ocaml.doc
          "When you initally create a subscription, AutoRenew is set to ENABLED. If ENABLED, the subscription will be automatically renewed at the end of the existing subscription period. You can change this by submitting an UpdateSubscription request. If the UpdateSubscription request does not included a value for AutoRenew, the existing value for AutoRenew remains unchanged."]}
    let make ?autoRenew = fun () -> { autoRenew }
    let to_value x =
      structure_to_value
        [("AutoRenew", (Option.map x.autoRenew ~f:AutoRenew.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let autoRenew =
        (Option.map ~f:AutoRenew.of_xml) (Xml.child xml_arg0 "AutoRenew") in
      make ?autoRenew ()
    let of_json json =
      let autoRenew = field_map json "AutoRenew" AutoRenew.of_json in
      make ?autoRenew ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the details of an existing subscription. Only enter values for parameters you want to change. Empty parameters are not updated."]
module UpdateProtectionGroupResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `OptimisticLockException of OptimisticLockException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `OptimisticLockException e ->
          `Assoc
            [("error", (`String "OptimisticLockException"));
            ("details", (OptimisticLockException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates an existing protection group. A protection group is a grouping of protected resources so they can be handled as a collective. This resource grouping improves the accuracy of detection and reduces false positives."]
module UpdateProtectionGroupRequest =
  struct
    type nonrec t =
      {
      protectionGroupId: ProtectionGroupId.t
        [@ocaml.doc
          "The name of the protection group. You use this to identify the protection group in lists and to manage the protection group, for example to update, delete, or describe it."];
      aggregation: ProtectionGroupAggregation.t
        [@ocaml.doc
          "Defines how Shield combines resource data for the group in order to detect, mitigate, and report events. Sum - Use the total traffic across the group. This is a good choice for most cases. Examples include Elastic IP addresses for EC2 instances that scale manually or automatically. Mean - Use the average of the traffic across the group. This is a good choice for resources that share traffic uniformly. Examples include accelerators and load balancers. Max - Use the highest traffic from each resource. This is useful for resources that don't share traffic and for resources that share that traffic in a non-uniform way. Examples include Amazon CloudFront distributions and origin resources for CloudFront distributions."];
      pattern: ProtectionGroupPattern.t
        [@ocaml.doc
          "The criteria to use to choose the protected resources for inclusion in the group. You can include all resources that have protections, provide a list of resource Amazon Resource Names (ARNs), or include all resources of a specified resource type."];
      resourceType: ProtectedResourceType.t option
        [@ocaml.doc
          "The resource type to include in the protection group. All protected resources of this type are included in the protection group. You must set this when you set Pattern to BY_RESOURCE_TYPE and you must not set it for any other Pattern setting."];
      members: ProtectionGroupMembers.t option
        [@ocaml.doc
          "The Amazon Resource Names (ARNs) of the resources to include in the protection group. You must set this when you set Pattern to ARBITRARY and you must not set it for any other Pattern setting."]}
    let context_ = "UpdateProtectionGroupRequest"
    let make ?resourceType =
      fun ?members ->
        fun ~protectionGroupId ->
          fun ~aggregation ->
            fun ~pattern ->
              fun () ->
                {
                  resourceType;
                  members;
                  protectionGroupId;
                  aggregation;
                  pattern
                }
    let to_value x =
      structure_to_value
        [("ProtectionGroupId",
           (Some (ProtectionGroupId.to_value x.protectionGroupId)));
        ("Aggregation",
          (Some (ProtectionGroupAggregation.to_value x.aggregation)));
        ("Pattern", (Some (ProtectionGroupPattern.to_value x.pattern)));
        ("ResourceType",
          (Option.map x.resourceType ~f:ProtectedResourceType.to_value));
        ("Members",
          (Option.map x.members ~f:ProtectionGroupMembers.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let members =
        (Option.map ~f:ProtectionGroupMembers.of_xml)
          (Xml.child xml_arg0 "Members") in
      let resourceType =
        (Option.map ~f:ProtectedResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let pattern =
        ProtectionGroupPattern.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Pattern") in
      let aggregation =
        ProtectionGroupAggregation.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Aggregation") in
      let protectionGroupId =
        ProtectionGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProtectionGroupId") in
      make ?members ?resourceType ~pattern ~aggregation ~protectionGroupId ()
    let of_json json =
      let members = field_map json "Members" ProtectionGroupMembers.of_json in
      let resourceType =
        field_map json "ResourceType" ProtectedResourceType.of_json in
      let pattern =
        field_map_exn json "Pattern" ProtectionGroupPattern.of_json in
      let aggregation =
        field_map_exn json "Aggregation" ProtectionGroupAggregation.of_json in
      let protectionGroupId =
        field_map_exn json "ProtectionGroupId" ProtectionGroupId.of_json in
      make ?members ?resourceType ~pattern ~aggregation ~protectionGroupId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates an existing protection group. A protection group is a grouping of protected resources so they can be handled as a collective. This resource grouping improves the accuracy of detection and reduces false positives."]
module UpdateEmergencyContactSettingsResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `OptimisticLockException of OptimisticLockException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `OptimisticLockException e ->
          `Assoc
            [("error", (`String "OptimisticLockException"));
            ("details", (OptimisticLockException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the details of the list of email addresses and phone numbers that the Shield Response Team (SRT) can use to contact you if you have proactive engagement enabled, for escalations to the SRT and to initiate proactive customer support."]
module UpdateEmergencyContactSettingsRequest =
  struct
    type nonrec t =
      {
      emergencyContactList: EmergencyContactList.t option
        [@ocaml.doc
          "A list of email addresses and phone numbers that the Shield Response Team (SRT) can use to contact you if you have proactive engagement enabled, for escalations to the SRT and to initiate proactive customer support. If you have proactive engagement enabled, the contact list must include at least one phone number."]}
    let make ?emergencyContactList = fun () -> { emergencyContactList }
    let to_value x =
      structure_to_value
        [("EmergencyContactList",
           (Option.map x.emergencyContactList
              ~f:EmergencyContactList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let emergencyContactList =
        (Option.map ~f:EmergencyContactList.of_xml)
          (Xml.child xml_arg0 "EmergencyContactList") in
      make ?emergencyContactList ()
    let of_json json =
      let emergencyContactList =
        field_map json "EmergencyContactList" EmergencyContactList.of_json in
      make ?emergencyContactList ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates the details of the list of email addresses and phone numbers that the Shield Response Team (SRT) can use to contact you if you have proactive engagement enabled, for escalations to the SRT and to initiate proactive customer support."]
module UpdateApplicationLayerAutomaticResponseResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `InvalidOperationException of InvalidOperationException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `OptimisticLockException of OptimisticLockException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `InvalidOperationException e ->
          `Assoc
            [("error", (`String "InvalidOperationException"));
            ("details", (InvalidOperationException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `OptimisticLockException e ->
          `Assoc
            [("error", (`String "OptimisticLockException"));
            ("details", (OptimisticLockException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates an existing Shield Advanced automatic application layer DDoS mitigation configuration for the specified resource."]
module UpdateApplicationLayerAutomaticResponseRequest =
  struct
    type nonrec t =
      {
      resourceArn: ResourceArn.t
        [@ocaml.doc "The ARN (Amazon Resource Name) of the resource."];
      action: ResponseAction.t
        [@ocaml.doc
          "Specifies the action setting that Shield Advanced should use in the WAF rules that it creates on behalf of the protected resource in response to DDoS attacks. You specify this as part of the configuration for the automatic application layer DDoS mitigation feature, when you enable or update automatic mitigation. Shield Advanced creates the WAF rules in a Shield Advanced-managed rule group, inside the web ACL that you have associated with the resource."]}
    let context_ = "UpdateApplicationLayerAutomaticResponseRequest"
    let make ~resourceArn = fun ~action -> fun () -> { resourceArn; action }
    let to_value x =
      structure_to_value
        [("ResourceArn", (Some (ResourceArn.to_value x.resourceArn)));
        ("Action", (Some (ResponseAction.to_value x.action)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let action =
        ResponseAction.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Action") in
      let resourceArn =
        ResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceArn") in
      make ~action ~resourceArn ()
    let of_json json =
      let action = field_map_exn json "Action" ResponseAction.of_json in
      let resourceArn = field_map_exn json "ResourceArn" ResourceArn.of_json in
      make ~action ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates an existing Shield Advanced automatic application layer DDoS mitigation configuration for the specified resource."]
module UntagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `InvalidResourceException of InvalidResourceException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "InvalidResourceException" ->
          `InvalidResourceException (InvalidResourceException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "InvalidResourceException" ->
          `InvalidResourceException (InvalidResourceException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `InvalidResourceException e ->
          `Assoc
            [("error", (`String "InvalidResourceException"));
            ("details", (InvalidResourceException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Removes tags from a resource in Shield."]
module UntagResourceRequest =
  struct
    type nonrec t =
      {
      resourceARN: ResourceArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the resource that you want to remove tags from."];
      tagKeys: TagKeyList.t
        [@ocaml.doc
          "The tag key for each tag that you want to remove from the resource."]}
    let context_ = "UntagResourceRequest"
    let make ~resourceARN =
      fun ~tagKeys -> fun () -> { resourceARN; tagKeys }
    let to_value x =
      structure_to_value
        [("ResourceARN", (Some (ResourceArn.to_value x.resourceARN)));
        ("TagKeys", (Some (TagKeyList.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TagKeys") in
      let resourceARN =
        ResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceARN") in
      make ~tagKeys ~resourceARN ()
    let of_json json =
      let tagKeys = field_map_exn json "TagKeys" TagKeyList.of_json in
      let resourceARN = field_map_exn json "ResourceARN" ResourceArn.of_json in
      make ~tagKeys ~resourceARN ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Removes tags from a resource in Shield."]
module TagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `InvalidResourceException of InvalidResourceException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "InvalidResourceException" ->
          `InvalidResourceException (InvalidResourceException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "InvalidResourceException" ->
          `InvalidResourceException (InvalidResourceException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `InvalidResourceException e ->
          `Assoc
            [("error", (`String "InvalidResourceException"));
            ("details", (InvalidResourceException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Adds or updates tags for a resource in Shield."]
module TagResourceRequest =
  struct
    type nonrec t =
      {
      resourceARN: ResourceArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the resource that you want to add or update tags for."];
      tags: TagList.t
        [@ocaml.doc
          "The tags that you want to modify or add to the resource."]}
    let context_ = "TagResourceRequest"
    let make ~resourceARN = fun ~tags -> fun () -> { resourceARN; tags }
    let to_value x =
      structure_to_value
        [("ResourceARN", (Some (ResourceArn.to_value x.resourceARN)));
        ("Tags", (Some (TagList.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagList.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Tags") in
      let resourceARN =
        ResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceARN") in
      make ~tags ~resourceARN ()
    let of_json json =
      let tags = field_map_exn json "Tags" TagList.of_json in
      let resourceARN = field_map_exn json "ResourceARN" ResourceArn.of_json in
      make ~tags ~resourceARN ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Adds or updates tags for a resource in Shield."]
module ListTagsForResourceResponse =
  struct
    type nonrec t =
      {
      tags: TagList.t option
        [@ocaml.doc
          "A list of tag key and value pairs associated with the specified resource."]}
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `InvalidResourceException of InvalidResourceException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "InvalidResourceException" ->
          `InvalidResourceException (InvalidResourceException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "InvalidResourceException" ->
          `InvalidResourceException (InvalidResourceException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `InvalidResourceException e ->
          `Assoc
            [("error", (`String "InvalidResourceException"));
            ("details", (InvalidResourceException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("Tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      make ?tags ()
    let of_json json =
      let tags = field_map json "Tags" TagList.of_json in make ?tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about Amazon Web Services tags for a specified Amazon Resource Name (ARN) in Shield."]
module ListTagsForResourceRequest =
  struct
    type nonrec t =
      {
      resourceARN: ResourceArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the resource to get tags for."]}
    let context_ = "ListTagsForResourceRequest"
    let make ~resourceARN = fun () -> { resourceARN }
    let to_value x =
      structure_to_value
        [("ResourceARN", (Some (ResourceArn.to_value x.resourceARN)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceARN =
        ResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceARN") in
      make ~resourceARN ()
    let of_json json =
      let resourceARN = field_map_exn json "ResourceARN" ResourceArn.of_json in
      make ~resourceARN ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about Amazon Web Services tags for a specified Amazon Resource Name (ARN) in Shield."]
module ListResourcesInProtectionGroupResponse =
  struct
    type nonrec t =
      {
      resourceArns: ResourceArnList.t
        [@ocaml.doc
          "The Amazon Resource Names (ARNs) of the resources that are included in the protection group."];
      nextToken: Token.t option
        [@ocaml.doc
          "When you request a list of objects from Shield Advanced, if the response does not include all of the remaining available objects, Shield Advanced includes a NextToken value in the response. You can retrieve the next batch of objects by requesting the list again and providing the token that was returned by the prior call in your request. You can indicate the maximum number of objects that you want Shield Advanced to return for a single call with the MaxResults setting. Shield Advanced will not return more than MaxResults objects, but may return fewer, even if more objects are still available. Whenever more objects remain that Shield Advanced has not yet returned to you, the response will include a NextToken value."]}
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `InvalidPaginationTokenException of InvalidPaginationTokenException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListResourcesInProtectionGroupResponse"
    let make ?nextToken =
      fun ~resourceArns -> fun () -> { nextToken; resourceArns }
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "InvalidPaginationTokenException" ->
          `InvalidPaginationTokenException
            (InvalidPaginationTokenException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "InvalidPaginationTokenException" ->
          `InvalidPaginationTokenException
            (InvalidPaginationTokenException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `InvalidPaginationTokenException e ->
          `Assoc
            [("error", (`String "InvalidPaginationTokenException"));
            ("details", (InvalidPaginationTokenException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ResourceArns", (Some (ResourceArnList.to_value x.resourceArns)));
        ("NextToken", (Option.map x.nextToken ~f:Token.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "NextToken") in
      let resourceArns =
        ResourceArnList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceArns") in
      make ?nextToken ~resourceArns ()
    let of_json json =
      let nextToken = field_map json "NextToken" Token.of_json in
      let resourceArns =
        field_map_exn json "ResourceArns" ResourceArnList.of_json in
      make ?nextToken ~resourceArns ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the resources that are included in the protection group."]
module ListResourcesInProtectionGroupRequest =
  struct
    type nonrec t =
      {
      protectionGroupId: ProtectionGroupId.t
        [@ocaml.doc
          "The name of the protection group. You use this to identify the protection group in lists and to manage the protection group, for example to update, delete, or describe it."];
      nextToken: Token.t option
        [@ocaml.doc
          "When you request a list of objects from Shield Advanced, if the response does not include all of the remaining available objects, Shield Advanced includes a NextToken value in the response. You can retrieve the next batch of objects by requesting the list again and providing the token that was returned by the prior call in your request. You can indicate the maximum number of objects that you want Shield Advanced to return for a single call with the MaxResults setting. Shield Advanced will not return more than MaxResults objects, but may return fewer, even if more objects are still available. Whenever more objects remain that Shield Advanced has not yet returned to you, the response will include a NextToken value. On your first call to a list operation, leave this setting empty."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The greatest number of objects that you want Shield Advanced to return to the list request. Shield Advanced might return fewer objects than you indicate in this setting, even if more objects are available. If there are more objects remaining, Shield Advanced will always also return a NextToken value in the response. The default setting is 20."]}
    let context_ = "ListResourcesInProtectionGroupRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ~protectionGroupId ->
          fun () -> { nextToken; maxResults; protectionGroupId }
    let to_value x =
      structure_to_value
        [("ProtectionGroupId",
           (Some (ProtectionGroupId.to_value x.protectionGroupId)));
        ("NextToken", (Option.map x.nextToken ~f:Token.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "NextToken") in
      let protectionGroupId =
        ProtectionGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProtectionGroupId") in
      make ?maxResults ?nextToken ~protectionGroupId ()
    let of_json json =
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" Token.of_json in
      let protectionGroupId =
        field_map_exn json "ProtectionGroupId" ProtectionGroupId.of_json in
      make ?maxResults ?nextToken ~protectionGroupId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves the resources that are included in the protection group."]
module ListProtectionsResponse =
  struct
    type nonrec t =
      {
      protections: Protections.t option
        [@ocaml.doc "The array of enabled Protection objects."];
      nextToken: Token.t option
        [@ocaml.doc
          "When you request a list of objects from Shield Advanced, if the response does not include all of the remaining available objects, Shield Advanced includes a NextToken value in the response. You can retrieve the next batch of objects by requesting the list again and providing the token that was returned by the prior call in your request. You can indicate the maximum number of objects that you want Shield Advanced to return for a single call with the MaxResults setting. Shield Advanced will not return more than MaxResults objects, but may return fewer, even if more objects are still available. Whenever more objects remain that Shield Advanced has not yet returned to you, the response will include a NextToken value."]}
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `InvalidPaginationTokenException of InvalidPaginationTokenException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?protections =
      fun ?nextToken -> fun () -> { protections; nextToken }
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "InvalidPaginationTokenException" ->
          `InvalidPaginationTokenException
            (InvalidPaginationTokenException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "InvalidPaginationTokenException" ->
          `InvalidPaginationTokenException
            (InvalidPaginationTokenException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `InvalidPaginationTokenException e ->
          `Assoc
            [("error", (`String "InvalidPaginationTokenException"));
            ("details", (InvalidPaginationTokenException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Protections", (Option.map x.protections ~f:Protections.to_value));
        ("NextToken", (Option.map x.nextToken ~f:Token.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "NextToken") in
      let protections =
        (Option.map ~f:Protections.of_xml) (Xml.child xml_arg0 "Protections") in
      make ?nextToken ?protections ()
    let of_json json =
      let nextToken = field_map json "NextToken" Token.of_json in
      let protections = field_map json "Protections" Protections.of_json in
      make ?nextToken ?protections ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists all Protection objects for the account."]
module ListProtectionsRequest =
  struct
    type nonrec t =
      {
      nextToken: Token.t option
        [@ocaml.doc
          "When you request a list of objects from Shield Advanced, if the response does not include all of the remaining available objects, Shield Advanced includes a NextToken value in the response. You can retrieve the next batch of objects by requesting the list again and providing the token that was returned by the prior call in your request. You can indicate the maximum number of objects that you want Shield Advanced to return for a single call with the MaxResults setting. Shield Advanced will not return more than MaxResults objects, but may return fewer, even if more objects are still available. Whenever more objects remain that Shield Advanced has not yet returned to you, the response will include a NextToken value. On your first call to a list operation, leave this setting empty."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The greatest number of objects that you want Shield Advanced to return to the list request. Shield Advanced might return fewer objects than you indicate in this setting, even if more objects are available. If there are more objects remaining, Shield Advanced will always also return a NextToken value in the response. The default setting is 20."]}
    let make ?nextToken =
      fun ?maxResults -> fun () -> { nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("NextToken", (Option.map x.nextToken ~f:Token.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "NextToken") in
      make ?maxResults ?nextToken ()
    let of_json json =
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" Token.of_json in
      make ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists all Protection objects for the account."]
module ListProtectionGroupsResponse =
  struct
    type nonrec t =
      {
      protectionGroups: ProtectionGroups.t ;
      nextToken: Token.t option
        [@ocaml.doc
          "When you request a list of objects from Shield Advanced, if the response does not include all of the remaining available objects, Shield Advanced includes a NextToken value in the response. You can retrieve the next batch of objects by requesting the list again and providing the token that was returned by the prior call in your request. You can indicate the maximum number of objects that you want Shield Advanced to return for a single call with the MaxResults setting. Shield Advanced will not return more than MaxResults objects, but may return fewer, even if more objects are still available. Whenever more objects remain that Shield Advanced has not yet returned to you, the response will include a NextToken value."]}
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `InvalidPaginationTokenException of InvalidPaginationTokenException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListProtectionGroupsResponse"
    let make ?nextToken =
      fun ~protectionGroups -> fun () -> { nextToken; protectionGroups }
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "InvalidPaginationTokenException" ->
          `InvalidPaginationTokenException
            (InvalidPaginationTokenException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "InvalidPaginationTokenException" ->
          `InvalidPaginationTokenException
            (InvalidPaginationTokenException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `InvalidPaginationTokenException e ->
          `Assoc
            [("error", (`String "InvalidPaginationTokenException"));
            ("details", (InvalidPaginationTokenException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ProtectionGroups",
           (Some (ProtectionGroups.to_value x.protectionGroups)));
        ("NextToken", (Option.map x.nextToken ~f:Token.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "NextToken") in
      let protectionGroups =
        ProtectionGroups.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProtectionGroups") in
      make ?nextToken ~protectionGroups ()
    let of_json json =
      let nextToken = field_map json "NextToken" Token.of_json in
      let protectionGroups =
        field_map_exn json "ProtectionGroups" ProtectionGroups.of_json in
      make ?nextToken ~protectionGroups ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves the ProtectionGroup objects for the account."]
module ListProtectionGroupsRequest =
  struct
    type nonrec t =
      {
      nextToken: Token.t option
        [@ocaml.doc
          "When you request a list of objects from Shield Advanced, if the response does not include all of the remaining available objects, Shield Advanced includes a NextToken value in the response. You can retrieve the next batch of objects by requesting the list again and providing the token that was returned by the prior call in your request. You can indicate the maximum number of objects that you want Shield Advanced to return for a single call with the MaxResults setting. Shield Advanced will not return more than MaxResults objects, but may return fewer, even if more objects are still available. Whenever more objects remain that Shield Advanced has not yet returned to you, the response will include a NextToken value. On your first call to a list operation, leave this setting empty."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The greatest number of objects that you want Shield Advanced to return to the list request. Shield Advanced might return fewer objects than you indicate in this setting, even if more objects are available. If there are more objects remaining, Shield Advanced will always also return a NextToken value in the response. The default setting is 20."]}
    let make ?nextToken =
      fun ?maxResults -> fun () -> { nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("NextToken", (Option.map x.nextToken ~f:Token.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "NextToken") in
      make ?maxResults ?nextToken ()
    let of_json json =
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" Token.of_json in
      make ?maxResults ?nextToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Retrieves the ProtectionGroup objects for the account."]
module ListAttacksResponse =
  struct
    type nonrec t =
      {
      attackSummaries: AttackSummaries.t option
        [@ocaml.doc "The attack information for the specified time range."];
      nextToken: Token.t option
        [@ocaml.doc
          "When you request a list of objects from Shield Advanced, if the response does not include all of the remaining available objects, Shield Advanced includes a NextToken value in the response. You can retrieve the next batch of objects by requesting the list again and providing the token that was returned by the prior call in your request. You can indicate the maximum number of objects that you want Shield Advanced to return for a single call with the MaxResults setting. Shield Advanced will not return more than MaxResults objects, but may return fewer, even if more objects are still available. Whenever more objects remain that Shield Advanced has not yet returned to you, the response will include a NextToken value."]}
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `InvalidOperationException of InvalidOperationException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?attackSummaries =
      fun ?nextToken -> fun () -> { attackSummaries; nextToken }
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `InvalidOperationException e ->
          `Assoc
            [("error", (`String "InvalidOperationException"));
            ("details", (InvalidOperationException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("AttackSummaries",
           (Option.map x.attackSummaries ~f:AttackSummaries.to_value));
        ("NextToken", (Option.map x.nextToken ~f:Token.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "NextToken") in
      let attackSummaries =
        (Option.map ~f:AttackSummaries.of_xml)
          (Xml.child xml_arg0 "AttackSummaries") in
      make ?nextToken ?attackSummaries ()
    let of_json json =
      let nextToken = field_map json "NextToken" Token.of_json in
      let attackSummaries =
        field_map json "AttackSummaries" AttackSummaries.of_json in
      make ?nextToken ?attackSummaries ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns all ongoing DDoS attacks or all DDoS attacks during a specified time period."]
module ListAttacksRequest =
  struct
    type nonrec t =
      {
      resourceArns: ResourceArnFilterList.t option
        [@ocaml.doc
          "The ARNs (Amazon Resource Names) of the resources that were attacked. If you leave this blank, all applicable resources for this account will be included."];
      startTime: TimeRange.t option
        [@ocaml.doc
          "The start of the time period for the attacks. This is a timestamp type. The request syntax listing for this call indicates a number type, but you can provide the time in any valid timestamp format setting."];
      endTime: TimeRange.t option
        [@ocaml.doc
          "The end of the time period for the attacks. This is a timestamp type. The request syntax listing for this call indicates a number type, but you can provide the time in any valid timestamp format setting."];
      nextToken: Token.t option
        [@ocaml.doc
          "When you request a list of objects from Shield Advanced, if the response does not include all of the remaining available objects, Shield Advanced includes a NextToken value in the response. You can retrieve the next batch of objects by requesting the list again and providing the token that was returned by the prior call in your request. You can indicate the maximum number of objects that you want Shield Advanced to return for a single call with the MaxResults setting. Shield Advanced will not return more than MaxResults objects, but may return fewer, even if more objects are still available. Whenever more objects remain that Shield Advanced has not yet returned to you, the response will include a NextToken value. On your first call to a list operation, leave this setting empty."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "The greatest number of objects that you want Shield Advanced to return to the list request. Shield Advanced might return fewer objects than you indicate in this setting, even if more objects are available. If there are more objects remaining, Shield Advanced will always also return a NextToken value in the response. The default setting is 20."]}
    let make ?resourceArns =
      fun ?startTime ->
        fun ?endTime ->
          fun ?nextToken ->
            fun ?maxResults ->
              fun () ->
                { resourceArns; startTime; endTime; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("ResourceArns",
           (Option.map x.resourceArns ~f:ResourceArnFilterList.to_value));
        ("StartTime", (Option.map x.startTime ~f:TimeRange.to_value));
        ("EndTime", (Option.map x.endTime ~f:TimeRange.to_value));
        ("NextToken", (Option.map x.nextToken ~f:Token.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:Token.of_xml) (Xml.child xml_arg0 "NextToken") in
      let endTime =
        (Option.map ~f:TimeRange.of_xml) (Xml.child xml_arg0 "EndTime") in
      let startTime =
        (Option.map ~f:TimeRange.of_xml) (Xml.child xml_arg0 "StartTime") in
      let resourceArns =
        (Option.map ~f:ResourceArnFilterList.of_xml)
          (Xml.child xml_arg0 "ResourceArns") in
      make ?maxResults ?nextToken ?endTime ?startTime ?resourceArns ()
    let of_json json =
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" Token.of_json in
      let endTime = field_map json "EndTime" TimeRange.of_json in
      let startTime = field_map json "StartTime" TimeRange.of_json in
      let resourceArns =
        field_map json "ResourceArns" ResourceArnFilterList.of_json in
      make ?maxResults ?nextToken ?endTime ?startTime ?resourceArns ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns all ongoing DDoS attacks or all DDoS attacks during a specified time period."]
module GetSubscriptionStateResponse =
  struct
    type nonrec t =
      {
      subscriptionState: SubscriptionState.t
        [@ocaml.doc "The status of the subscription."]}
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetSubscriptionStateResponse"
    let make ~subscriptionState = fun () -> { subscriptionState }
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("SubscriptionState",
           (Some (SubscriptionState.to_value x.subscriptionState)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let subscriptionState =
        SubscriptionState.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SubscriptionState") in
      make ~subscriptionState ()
    let of_json json =
      let subscriptionState =
        field_map_exn json "SubscriptionState" SubscriptionState.of_json in
      make ~subscriptionState ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the SubscriptionState, either Active or Inactive."]
module GetSubscriptionStateRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the SubscriptionState, either Active or Inactive."]
module EnableProactiveEngagementResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `InvalidOperationException of InvalidOperationException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `OptimisticLockException of OptimisticLockException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `InvalidOperationException e ->
          `Assoc
            [("error", (`String "InvalidOperationException"));
            ("details", (InvalidOperationException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `OptimisticLockException e ->
          `Assoc
            [("error", (`String "OptimisticLockException"));
            ("details", (OptimisticLockException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Authorizes the Shield Response Team (SRT) to use email and phone to notify contacts about escalations to the SRT and to initiate proactive customer support."]
module EnableProactiveEngagementRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Authorizes the Shield Response Team (SRT) to use email and phone to notify contacts about escalations to the SRT and to initiate proactive customer support."]
module EnableApplicationLayerAutomaticResponseResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `InvalidOperationException of InvalidOperationException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `LimitsExceededException of LimitsExceededException.t 
      | `OptimisticLockException of OptimisticLockException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "LimitsExceededException" ->
          `LimitsExceededException (LimitsExceededException.of_json json)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "LimitsExceededException" ->
          `LimitsExceededException (LimitsExceededException.of_xml xml)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `InvalidOperationException e ->
          `Assoc
            [("error", (`String "InvalidOperationException"));
            ("details", (InvalidOperationException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `LimitsExceededException e ->
          `Assoc
            [("error", (`String "LimitsExceededException"));
            ("details", (LimitsExceededException.to_json e))]
      | `OptimisticLockException e ->
          `Assoc
            [("error", (`String "OptimisticLockException"));
            ("details", (OptimisticLockException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Enable the Shield Advanced automatic application layer DDoS mitigation for the resource. This feature is available for Amazon CloudFront distributions only. This causes Shield Advanced to create, verify, and apply WAF rules for DDoS attacks that it detects for the resource. Shield Advanced applies the rules in a Shield rule group inside the web ACL that you've associated with the resource. For information about how automatic mitigation works and the requirements for using it, see Shield Advanced automatic application layer DDoS mitigation. Don't use this action to make changes to automatic mitigation settings when it's already enabled for a resource. Instead, use UpdateApplicationLayerAutomaticResponse. To use this feature, you must associate a web ACL with the protected resource. The web ACL must be created using the latest version of WAF (v2). You can associate the web ACL through the Shield Advanced console at https://console.aws.amazon.com/wafv2/shieldv2#/. For more information, see Getting Started with Shield Advanced. You can also do this through the WAF console or the WAF API, but you must manage Shield Advanced automatic mitigation through Shield Advanced. For information about WAF, see WAF Developer Guide."]
module EnableApplicationLayerAutomaticResponseRequest =
  struct
    type nonrec t =
      {
      resourceArn: ResourceArn.t
        [@ocaml.doc "The ARN (Amazon Resource Name) of the resource."];
      action: ResponseAction.t
        [@ocaml.doc
          "Specifies the action setting that Shield Advanced should use in the WAF rules that it creates on behalf of the protected resource in response to DDoS attacks. You specify this as part of the configuration for the automatic application layer DDoS mitigation feature, when you enable or update automatic mitigation. Shield Advanced creates the WAF rules in a Shield Advanced-managed rule group, inside the web ACL that you have associated with the resource."]}
    let context_ = "EnableApplicationLayerAutomaticResponseRequest"
    let make ~resourceArn = fun ~action -> fun () -> { resourceArn; action }
    let to_value x =
      structure_to_value
        [("ResourceArn", (Some (ResourceArn.to_value x.resourceArn)));
        ("Action", (Some (ResponseAction.to_value x.action)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let action =
        ResponseAction.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Action") in
      let resourceArn =
        ResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceArn") in
      make ~action ~resourceArn ()
    let of_json json =
      let action = field_map_exn json "Action" ResponseAction.of_json in
      let resourceArn = field_map_exn json "ResourceArn" ResourceArn.of_json in
      make ~action ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Enable the Shield Advanced automatic application layer DDoS mitigation for the resource. This feature is available for Amazon CloudFront distributions only. This causes Shield Advanced to create, verify, and apply WAF rules for DDoS attacks that it detects for the resource. Shield Advanced applies the rules in a Shield rule group inside the web ACL that you've associated with the resource. For information about how automatic mitigation works and the requirements for using it, see Shield Advanced automatic application layer DDoS mitigation. Don't use this action to make changes to automatic mitigation settings when it's already enabled for a resource. Instead, use UpdateApplicationLayerAutomaticResponse. To use this feature, you must associate a web ACL with the protected resource. The web ACL must be created using the latest version of WAF (v2). You can associate the web ACL through the Shield Advanced console at https://console.aws.amazon.com/wafv2/shieldv2#/. For more information, see Getting Started with Shield Advanced. You can also do this through the WAF console or the WAF API, but you must manage Shield Advanced automatic mitigation through Shield Advanced. For information about WAF, see WAF Developer Guide."]
module DisassociateHealthCheckResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `InvalidResourceException of InvalidResourceException.t 
      | `OptimisticLockException of OptimisticLockException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "InvalidResourceException" ->
          `InvalidResourceException (InvalidResourceException.of_json json)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "InvalidResourceException" ->
          `InvalidResourceException (InvalidResourceException.of_xml xml)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `InvalidResourceException e ->
          `Assoc
            [("error", (`String "InvalidResourceException"));
            ("details", (InvalidResourceException.to_json e))]
      | `OptimisticLockException e ->
          `Assoc
            [("error", (`String "OptimisticLockException"));
            ("details", (OptimisticLockException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes health-based detection from the Shield Advanced protection for a resource. Shield Advanced health-based detection uses the health of your Amazon Web Services resource to improve responsiveness and accuracy in attack detection and response. You define the health check in Route\194\16053 and then associate or disassociate it with your Shield Advanced protection. For more information, see Shield Advanced Health-Based Detection in the WAF Developer Guide."]
module DisassociateHealthCheckRequest =
  struct
    type nonrec t =
      {
      protectionId: ProtectionId.t
        [@ocaml.doc
          "The unique identifier (ID) for the Protection object to remove the health check association from."];
      healthCheckArn: HealthCheckArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the health check that is associated with the protection."]}
    let context_ = "DisassociateHealthCheckRequest"
    let make ~protectionId =
      fun ~healthCheckArn -> fun () -> { protectionId; healthCheckArn }
    let to_value x =
      structure_to_value
        [("ProtectionId", (Some (ProtectionId.to_value x.protectionId)));
        ("HealthCheckArn", (Some (HealthCheckArn.to_value x.healthCheckArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let healthCheckArn =
        HealthCheckArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HealthCheckArn") in
      let protectionId =
        ProtectionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProtectionId") in
      make ~healthCheckArn ~protectionId ()
    let of_json json =
      let healthCheckArn =
        field_map_exn json "HealthCheckArn" HealthCheckArn.of_json in
      let protectionId =
        field_map_exn json "ProtectionId" ProtectionId.of_json in
      make ~healthCheckArn ~protectionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes health-based detection from the Shield Advanced protection for a resource. Shield Advanced health-based detection uses the health of your Amazon Web Services resource to improve responsiveness and accuracy in attack detection and response. You define the health check in Route\194\16053 and then associate or disassociate it with your Shield Advanced protection. For more information, see Shield Advanced Health-Based Detection in the WAF Developer Guide."]
module DisassociateDRTRoleResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `InvalidOperationException of InvalidOperationException.t 
      | `OptimisticLockException of OptimisticLockException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_json json)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_xml xml)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `InvalidOperationException e ->
          `Assoc
            [("error", (`String "InvalidOperationException"));
            ("details", (InvalidOperationException.to_json e))]
      | `OptimisticLockException e ->
          `Assoc
            [("error", (`String "OptimisticLockException"));
            ("details", (OptimisticLockException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes the Shield Response Team's (SRT) access to your Amazon Web Services account."]
module DisassociateDRTRoleRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes the Shield Response Team's (SRT) access to your Amazon Web Services account."]
module DisassociateDRTLogBucketResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [
        `AccessDeniedForDependencyException of
          AccessDeniedForDependencyException.t 
      | `InternalErrorException of InternalErrorException.t 
      | `InvalidOperationException of InvalidOperationException.t 
      | `NoAssociatedRoleException of NoAssociatedRoleException.t 
      | `OptimisticLockException of OptimisticLockException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedForDependencyException" ->
          `AccessDeniedForDependencyException
            (AccessDeniedForDependencyException.of_json json)
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_json json)
      | "NoAssociatedRoleException" ->
          `NoAssociatedRoleException (NoAssociatedRoleException.of_json json)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedForDependencyException" ->
          `AccessDeniedForDependencyException
            (AccessDeniedForDependencyException.of_xml xml)
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_xml xml)
      | "NoAssociatedRoleException" ->
          `NoAssociatedRoleException (NoAssociatedRoleException.of_xml xml)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedForDependencyException e ->
          `Assoc
            [("error", (`String "AccessDeniedForDependencyException"));
            ("details", (AccessDeniedForDependencyException.to_json e))]
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `InvalidOperationException e ->
          `Assoc
            [("error", (`String "InvalidOperationException"));
            ("details", (InvalidOperationException.to_json e))]
      | `NoAssociatedRoleException e ->
          `Assoc
            [("error", (`String "NoAssociatedRoleException"));
            ("details", (NoAssociatedRoleException.to_json e))]
      | `OptimisticLockException e ->
          `Assoc
            [("error", (`String "OptimisticLockException"));
            ("details", (OptimisticLockException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes the Shield Response Team's (SRT) access to the specified Amazon S3 bucket containing the logs that you shared previously."]
module DisassociateDRTLogBucketRequest =
  struct
    type nonrec t =
      {
      logBucket: LogBucket.t
        [@ocaml.doc
          "The Amazon S3 bucket that contains the logs that you want to share."]}
    let context_ = "DisassociateDRTLogBucketRequest"
    let make ~logBucket = fun () -> { logBucket }
    let to_value x =
      structure_to_value
        [("LogBucket", (Some (LogBucket.to_value x.logBucket)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let logBucket =
        LogBucket.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LogBucket") in
      make ~logBucket ()
    let of_json json =
      let logBucket = field_map_exn json "LogBucket" LogBucket.of_json in
      make ~logBucket ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes the Shield Response Team's (SRT) access to the specified Amazon S3 bucket containing the logs that you shared previously."]
module DisableProactiveEngagementResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `InvalidOperationException of InvalidOperationException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `OptimisticLockException of OptimisticLockException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `InvalidOperationException e ->
          `Assoc
            [("error", (`String "InvalidOperationException"));
            ("details", (InvalidOperationException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `OptimisticLockException e ->
          `Assoc
            [("error", (`String "OptimisticLockException"));
            ("details", (OptimisticLockException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes authorization from the Shield Response Team (SRT) to notify contacts about escalations to the SRT and to initiate proactive customer support."]
module DisableProactiveEngagementRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes authorization from the Shield Response Team (SRT) to notify contacts about escalations to the SRT and to initiate proactive customer support."]
module DisableApplicationLayerAutomaticResponseResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `InvalidOperationException of InvalidOperationException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `OptimisticLockException of OptimisticLockException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `InvalidOperationException e ->
          `Assoc
            [("error", (`String "InvalidOperationException"));
            ("details", (InvalidOperationException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `OptimisticLockException e ->
          `Assoc
            [("error", (`String "OptimisticLockException"));
            ("details", (OptimisticLockException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Disable the Shield Advanced automatic application layer DDoS mitigation feature for the resource. This stops Shield Advanced from creating, verifying, and applying WAF rules for attacks that it detects for the resource."]
module DisableApplicationLayerAutomaticResponseRequest =
  struct
    type nonrec t =
      {
      resourceArn: ResourceArn.t
        [@ocaml.doc "The ARN (Amazon Resource Name) of the resource."]}
    let context_ = "DisableApplicationLayerAutomaticResponseRequest"
    let make ~resourceArn = fun () -> { resourceArn }
    let to_value x =
      structure_to_value
        [("ResourceArn", (Some (ResourceArn.to_value x.resourceArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        ResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceArn") in
      make ~resourceArn ()
    let of_json json =
      let resourceArn = field_map_exn json "ResourceArn" ResourceArn.of_json in
      make ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Disable the Shield Advanced automatic application layer DDoS mitigation feature for the resource. This stops Shield Advanced from creating, verifying, and applying WAF rules for attacks that it detects for the resource."]
module DescribeSubscriptionResponse =
  struct
    type nonrec t =
      {
      subscription: Subscription.t option
        [@ocaml.doc
          "The Shield Advanced subscription details for an account."]}
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?subscription = fun () -> { subscription }
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Subscription",
           (Option.map x.subscription ~f:Subscription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let subscription =
        (Option.map ~f:Subscription.of_xml)
          (Xml.child xml_arg0 "Subscription") in
      make ?subscription ()
    let of_json json =
      let subscription = field_map json "Subscription" Subscription.of_json in
      make ?subscription ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides details about the Shield Advanced subscription for an account."]
module DescribeSubscriptionRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides details about the Shield Advanced subscription for an account."]
module DescribeProtectionResponse =
  struct
    type nonrec t =
      {
      protection: Protection.t option
        [@ocaml.doc "The Protection object that is described."]}
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?protection = fun () -> { protection }
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Protection", (Option.map x.protection ~f:Protection.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let protection =
        (Option.map ~f:Protection.of_xml) (Xml.child xml_arg0 "Protection") in
      make ?protection ()
    let of_json json =
      let protection = field_map json "Protection" Protection.of_json in
      make ?protection ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists the details of a Protection object."]
module DescribeProtectionRequest =
  struct
    type nonrec t =
      {
      protectionId: ProtectionId.t option
        [@ocaml.doc
          "The unique identifier (ID) for the Protection object that is described. When submitting the DescribeProtection request you must provide either the ResourceArn or the ProtectionID, but not both."];
      resourceArn: ResourceArn.t option
        [@ocaml.doc
          "The ARN (Amazon Resource Name) of the Amazon Web Services resource for the Protection object that is described. When submitting the DescribeProtection request you must provide either the ResourceArn or the ProtectionID, but not both."]}
    let make ?protectionId =
      fun ?resourceArn -> fun () -> { protectionId; resourceArn }
    let to_value x =
      structure_to_value
        [("ProtectionId",
           (Option.map x.protectionId ~f:ProtectionId.to_value));
        ("ResourceArn", (Option.map x.resourceArn ~f:ResourceArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceArn =
        (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "ResourceArn") in
      let protectionId =
        (Option.map ~f:ProtectionId.of_xml)
          (Xml.child xml_arg0 "ProtectionId") in
      make ?resourceArn ?protectionId ()
    let of_json json =
      let resourceArn = field_map json "ResourceArn" ResourceArn.of_json in
      let protectionId = field_map json "ProtectionId" ProtectionId.of_json in
      make ?resourceArn ?protectionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Lists the details of a Protection object."]
module DescribeProtectionGroupResponse =
  struct
    type nonrec t =
      {
      protectionGroup: ProtectionGroup.t
        [@ocaml.doc
          "A grouping of protected resources that you and Shield Advanced can monitor as a collective. This resource grouping improves the accuracy of detection and reduces false positives."]}
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeProtectionGroupResponse"
    let make ~protectionGroup = fun () -> { protectionGroup }
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ProtectionGroup",
           (Some (ProtectionGroup.to_value x.protectionGroup)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let protectionGroup =
        ProtectionGroup.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProtectionGroup") in
      make ~protectionGroup ()
    let of_json json =
      let protectionGroup =
        field_map_exn json "ProtectionGroup" ProtectionGroup.of_json in
      make ~protectionGroup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the specification for the specified protection group."]
module DescribeProtectionGroupRequest =
  struct
    type nonrec t =
      {
      protectionGroupId: ProtectionGroupId.t
        [@ocaml.doc
          "The name of the protection group. You use this to identify the protection group in lists and to manage the protection group, for example to update, delete, or describe it."]}
    let context_ = "DescribeProtectionGroupRequest"
    let make ~protectionGroupId = fun () -> { protectionGroupId }
    let to_value x =
      structure_to_value
        [("ProtectionGroupId",
           (Some (ProtectionGroupId.to_value x.protectionGroupId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let protectionGroupId =
        ProtectionGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProtectionGroupId") in
      make ~protectionGroupId ()
    let of_json json =
      let protectionGroupId =
        field_map_exn json "ProtectionGroupId" ProtectionGroupId.of_json in
      make ~protectionGroupId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the specification for the specified protection group."]
module DescribeEmergencyContactSettingsResponse =
  struct
    type nonrec t =
      {
      emergencyContactList: EmergencyContactList.t option
        [@ocaml.doc
          "A list of email addresses and phone numbers that the Shield Response Team (SRT) can use to contact you if you have proactive engagement enabled, for escalations to the SRT and to initiate proactive customer support."]}
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?emergencyContactList = fun () -> { emergencyContactList }
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("EmergencyContactList",
           (Option.map x.emergencyContactList
              ~f:EmergencyContactList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let emergencyContactList =
        (Option.map ~f:EmergencyContactList.of_xml)
          (Xml.child xml_arg0 "EmergencyContactList") in
      make ?emergencyContactList ()
    let of_json json =
      let emergencyContactList =
        field_map json "EmergencyContactList" EmergencyContactList.of_json in
      make ?emergencyContactList ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A list of email addresses and phone numbers that the Shield Response Team (SRT) can use to contact you if you have proactive engagement enabled, for escalations to the SRT and to initiate proactive customer support."]
module DescribeEmergencyContactSettingsRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A list of email addresses and phone numbers that the Shield Response Team (SRT) can use to contact you if you have proactive engagement enabled, for escalations to the SRT and to initiate proactive customer support."]
module DescribeDRTAccessResponse =
  struct
    type nonrec t =
      {
      roleArn: RoleArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the role the SRT used to access your Amazon Web Services account."];
      logBucketList: LogBucketList.t option
        [@ocaml.doc "The list of Amazon S3 buckets accessed by the SRT."]}
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?roleArn =
      fun ?logBucketList -> fun () -> { roleArn; logBucketList }
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("RoleArn", (Option.map x.roleArn ~f:RoleArn.to_value));
        ("LogBucketList",
          (Option.map x.logBucketList ~f:LogBucketList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let logBucketList =
        (Option.map ~f:LogBucketList.of_xml)
          (Xml.child xml_arg0 "LogBucketList") in
      let roleArn =
        (Option.map ~f:RoleArn.of_xml) (Xml.child xml_arg0 "RoleArn") in
      make ?logBucketList ?roleArn ()
    let of_json json =
      let logBucketList =
        field_map json "LogBucketList" LogBucketList.of_json in
      let roleArn = field_map json "RoleArn" RoleArn.of_json in
      make ?logBucketList ?roleArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the current role and list of Amazon S3 log buckets used by the Shield Response Team (SRT) to access your Amazon Web Services account while assisting with attack mitigation."]
module DescribeDRTAccessRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the current role and list of Amazon S3 log buckets used by the Shield Response Team (SRT) to access your Amazon Web Services account while assisting with attack mitigation."]
module DescribeAttackStatisticsResponse =
  struct
    type nonrec t =
      {
      timeRange: TimeRange.t ;
      dataItems: AttackStatisticsDataList.t
        [@ocaml.doc
          "The data that describes the attacks detected during the time period."]}
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DescribeAttackStatisticsResponse"
    let make ~timeRange =
      fun ~dataItems -> fun () -> { timeRange; dataItems }
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("TimeRange", (Some (TimeRange.to_value x.timeRange)));
        ("DataItems", (Some (AttackStatisticsDataList.to_value x.dataItems)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dataItems =
        AttackStatisticsDataList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DataItems") in
      let timeRange =
        TimeRange.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TimeRange") in
      make ~dataItems ~timeRange ()
    let of_json json =
      let dataItems =
        field_map_exn json "DataItems" AttackStatisticsDataList.of_json in
      let timeRange = field_map_exn json "TimeRange" TimeRange.of_json in
      make ~dataItems ~timeRange ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides information about the number and type of attacks Shield has detected in the last year for all resources that belong to your account, regardless of whether you've defined Shield protections for them. This operation is available to Shield customers as well as to Shield Advanced customers. The operation returns data for the time range of midnight UTC, one year ago, to midnight UTC, today. For example, if the current time is 2020-10-26 15:39:32 PDT, equal to 2020-10-26 22:39:32 UTC, then the time range for the attack data returned is from 2019-10-26 00:00:00 UTC to 2020-10-26 00:00:00 UTC. The time range indicates the period covered by the attack statistics data items."]
module DescribeAttackStatisticsRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Provides information about the number and type of attacks Shield has detected in the last year for all resources that belong to your account, regardless of whether you've defined Shield protections for them. This operation is available to Shield customers as well as to Shield Advanced customers. The operation returns data for the time range of midnight UTC, one year ago, to midnight UTC, today. For example, if the current time is 2020-10-26 15:39:32 PDT, equal to 2020-10-26 22:39:32 UTC, then the time range for the attack data returned is from 2019-10-26 00:00:00 UTC to 2020-10-26 00:00:00 UTC. The time range indicates the period covered by the attack statistics data items."]
module DescribeAttackResponse =
  struct
    type nonrec t =
      {
      attack: AttackDetail.t option
        [@ocaml.doc "The attack that is described."]}
    type nonrec error =
      [ `AccessDeniedException of AccessDeniedException.t 
      | `InternalErrorException of InternalErrorException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?attack = fun () -> { attack }
    let error_of_json name json =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_json json)
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedException" ->
          `AccessDeniedException (AccessDeniedException.of_xml xml)
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedException e ->
          `Assoc
            [("error", (`String "AccessDeniedException"));
            ("details", (AccessDeniedException.to_json e))]
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Attack", (Option.map x.attack ~f:AttackDetail.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attack =
        (Option.map ~f:AttackDetail.of_xml) (Xml.child xml_arg0 "Attack") in
      make ?attack ()
    let of_json json =
      let attack = field_map json "Attack" AttackDetail.of_json in
      make ?attack ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the details of a DDoS attack."]
module DescribeAttackRequest =
  struct
    type nonrec t =
      {
      attackId: AttackId.t
        [@ocaml.doc "The unique identifier (ID) for the attack."]}
    let context_ = "DescribeAttackRequest"
    let make ~attackId = fun () -> { attackId }
    let to_value x =
      structure_to_value
        [("AttackId", (Some (AttackId.to_value x.attackId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attackId =
        AttackId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "AttackId") in
      make ~attackId ()
    let of_json json =
      let attackId = field_map_exn json "AttackId" AttackId.of_json in
      make ~attackId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes the details of a DDoS attack."]
module DeleteSubscriptionResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `LockedSubscriptionException of LockedSubscriptionException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "LockedSubscriptionException" ->
          `LockedSubscriptionException
            (LockedSubscriptionException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "LockedSubscriptionException" ->
          `LockedSubscriptionException
            (LockedSubscriptionException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `LockedSubscriptionException e ->
          `Assoc
            [("error", (`String "LockedSubscriptionException"));
            ("details", (LockedSubscriptionException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes Shield Advanced from an account. Shield Advanced requires a 1-year subscription commitment. You cannot delete a subscription prior to the completion of that commitment."]
module DeleteSubscriptionRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes Shield Advanced from an account. Shield Advanced requires a 1-year subscription commitment. You cannot delete a subscription prior to the completion of that commitment."]
module DeleteProtectionResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `OptimisticLockException of OptimisticLockException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `OptimisticLockException e ->
          `Assoc
            [("error", (`String "OptimisticLockException"));
            ("details", (OptimisticLockException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes an Shield Advanced Protection."]
module DeleteProtectionRequest =
  struct
    type nonrec t =
      {
      protectionId: ProtectionId.t
        [@ocaml.doc
          "The unique identifier (ID) for the Protection object to be deleted."]}
    let context_ = "DeleteProtectionRequest"
    let make ~protectionId = fun () -> { protectionId }
    let to_value x =
      structure_to_value
        [("ProtectionId", (Some (ProtectionId.to_value x.protectionId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let protectionId =
        ProtectionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProtectionId") in
      make ~protectionId ()
    let of_json json =
      let protectionId =
        field_map_exn json "ProtectionId" ProtectionId.of_json in
      make ~protectionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Deletes an Shield Advanced Protection."]
module DeleteProtectionGroupResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `OptimisticLockException of OptimisticLockException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `OptimisticLockException e ->
          `Assoc
            [("error", (`String "OptimisticLockException"));
            ("details", (OptimisticLockException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Removes the specified protection group."]
module DeleteProtectionGroupRequest =
  struct
    type nonrec t =
      {
      protectionGroupId: ProtectionGroupId.t
        [@ocaml.doc
          "The name of the protection group. You use this to identify the protection group in lists and to manage the protection group, for example to update, delete, or describe it."]}
    let context_ = "DeleteProtectionGroupRequest"
    let make ~protectionGroupId = fun () -> { protectionGroupId }
    let to_value x =
      structure_to_value
        [("ProtectionGroupId",
           (Some (ProtectionGroupId.to_value x.protectionGroupId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let protectionGroupId =
        ProtectionGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProtectionGroupId") in
      make ~protectionGroupId ()
    let of_json json =
      let protectionGroupId =
        field_map_exn json "ProtectionGroupId" ProtectionGroupId.of_json in
      make ~protectionGroupId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Removes the specified protection group."]
module CreateSubscriptionResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `ResourceAlreadyExistsException of ResourceAlreadyExistsException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `ResourceAlreadyExistsException e ->
          `Assoc
            [("error", (`String "ResourceAlreadyExistsException"));
            ("details", (ResourceAlreadyExistsException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Activates Shield Advanced for an account. When you initally create a subscription, your subscription is set to be automatically renewed at the end of the existing subscription period. You can change this by submitting an UpdateSubscription request."]
module CreateSubscriptionRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Activates Shield Advanced for an account. When you initally create a subscription, your subscription is set to be automatically renewed at the end of the existing subscription period. You can change this by submitting an UpdateSubscription request."]
module CreateProtectionResponse =
  struct
    type nonrec t =
      {
      protectionId: ProtectionId.t option
        [@ocaml.doc
          "The unique identifier (ID) for the Protection object that is created."]}
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `InvalidOperationException of InvalidOperationException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `InvalidResourceException of InvalidResourceException.t 
      | `LimitsExceededException of LimitsExceededException.t 
      | `OptimisticLockException of OptimisticLockException.t 
      | `ResourceAlreadyExistsException of ResourceAlreadyExistsException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?protectionId = fun () -> { protectionId }
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "InvalidResourceException" ->
          `InvalidResourceException (InvalidResourceException.of_json json)
      | "LimitsExceededException" ->
          `LimitsExceededException (LimitsExceededException.of_json json)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_json json)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "InvalidResourceException" ->
          `InvalidResourceException (InvalidResourceException.of_xml xml)
      | "LimitsExceededException" ->
          `LimitsExceededException (LimitsExceededException.of_xml xml)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_xml xml)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `InvalidOperationException e ->
          `Assoc
            [("error", (`String "InvalidOperationException"));
            ("details", (InvalidOperationException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `InvalidResourceException e ->
          `Assoc
            [("error", (`String "InvalidResourceException"));
            ("details", (InvalidResourceException.to_json e))]
      | `LimitsExceededException e ->
          `Assoc
            [("error", (`String "LimitsExceededException"));
            ("details", (LimitsExceededException.to_json e))]
      | `OptimisticLockException e ->
          `Assoc
            [("error", (`String "OptimisticLockException"));
            ("details", (OptimisticLockException.to_json e))]
      | `ResourceAlreadyExistsException e ->
          `Assoc
            [("error", (`String "ResourceAlreadyExistsException"));
            ("details", (ResourceAlreadyExistsException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ProtectionId",
           (Option.map x.protectionId ~f:ProtectionId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let protectionId =
        (Option.map ~f:ProtectionId.of_xml)
          (Xml.child xml_arg0 "ProtectionId") in
      make ?protectionId ()
    let of_json json =
      let protectionId = field_map json "ProtectionId" ProtectionId.of_json in
      make ?protectionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Enables Shield Advanced for a specific Amazon Web Services resource. The resource can be an Amazon CloudFront distribution, Elastic Load Balancing load balancer, Global Accelerator accelerator, Elastic IP Address, or an Amazon Route\194\16053 hosted zone. You can add protection to only a single resource with each CreateProtection request. You can add protection to multiple resources at once through the Shield Advanced console at https://console.aws.amazon.com/wafv2/shieldv2#/. For more information see Getting Started with Shield Advanced and Adding Shield Advanced protection to Amazon Web Services resources."]
module CreateProtectionRequest =
  struct
    type nonrec t =
      {
      name: ProtectionName.t
        [@ocaml.doc "Friendly name for the Protection you are creating."];
      resourceArn: ResourceArn.t
        [@ocaml.doc
          "The ARN (Amazon Resource Name) of the resource to be protected. The ARN should be in one of the following formats: For an Application Load Balancer: arn:aws:elasticloadbalancing:region:account-id:loadbalancer/app/load-balancer-name/load-balancer-id For an Elastic Load Balancer (Classic Load Balancer): arn:aws:elasticloadbalancing:region:account-id:loadbalancer/load-balancer-name For an Amazon CloudFront distribution: arn:aws:cloudfront::account-id:distribution/distribution-id For an Global Accelerator accelerator: arn:aws:globalaccelerator::account-id:accelerator/accelerator-id For Amazon Route\194\16053: arn:aws:route53:::hostedzone/hosted-zone-id For an Elastic IP address: arn:aws:ec2:region:account-id:eip-allocation/allocation-id"];
      tags: TagList.t option
        [@ocaml.doc
          "One or more tag key-value pairs for the Protection object that is created."]}
    let context_ = "CreateProtectionRequest"
    let make ?tags =
      fun ~name -> fun ~resourceArn -> fun () -> { tags; name; resourceArn }
    let to_value x =
      structure_to_value
        [("Name", (Some (ProtectionName.to_value x.name)));
        ("ResourceArn", (Some (ResourceArn.to_value x.resourceArn)));
        ("Tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let resourceArn =
        ResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceArn") in
      let name =
        ProtectionName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ?tags ~resourceArn ~name ()
    let of_json json =
      let tags = field_map json "Tags" TagList.of_json in
      let resourceArn = field_map_exn json "ResourceArn" ResourceArn.of_json in
      let name = field_map_exn json "Name" ProtectionName.of_json in
      make ?tags ~resourceArn ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Enables Shield Advanced for a specific Amazon Web Services resource. The resource can be an Amazon CloudFront distribution, Elastic Load Balancing load balancer, Global Accelerator accelerator, Elastic IP Address, or an Amazon Route\194\16053 hosted zone. You can add protection to only a single resource with each CreateProtection request. You can add protection to multiple resources at once through the Shield Advanced console at https://console.aws.amazon.com/wafv2/shieldv2#/. For more information see Getting Started with Shield Advanced and Adding Shield Advanced protection to Amazon Web Services resources."]
module CreateProtectionGroupResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `LimitsExceededException of LimitsExceededException.t 
      | `OptimisticLockException of OptimisticLockException.t 
      | `ResourceAlreadyExistsException of ResourceAlreadyExistsException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "LimitsExceededException" ->
          `LimitsExceededException (LimitsExceededException.of_json json)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_json json)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "LimitsExceededException" ->
          `LimitsExceededException (LimitsExceededException.of_xml xml)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_xml xml)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `LimitsExceededException e ->
          `Assoc
            [("error", (`String "LimitsExceededException"));
            ("details", (LimitsExceededException.to_json e))]
      | `OptimisticLockException e ->
          `Assoc
            [("error", (`String "OptimisticLockException"));
            ("details", (OptimisticLockException.to_json e))]
      | `ResourceAlreadyExistsException e ->
          `Assoc
            [("error", (`String "ResourceAlreadyExistsException"));
            ("details", (ResourceAlreadyExistsException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a grouping of protected resources so they can be handled as a collective. This resource grouping improves the accuracy of detection and reduces false positives."]
module CreateProtectionGroupRequest =
  struct
    type nonrec t =
      {
      protectionGroupId: ProtectionGroupId.t
        [@ocaml.doc
          "The name of the protection group. You use this to identify the protection group in lists and to manage the protection group, for example to update, delete, or describe it."];
      aggregation: ProtectionGroupAggregation.t
        [@ocaml.doc
          "Defines how Shield combines resource data for the group in order to detect, mitigate, and report events. Sum - Use the total traffic across the group. This is a good choice for most cases. Examples include Elastic IP addresses for EC2 instances that scale manually or automatically. Mean - Use the average of the traffic across the group. This is a good choice for resources that share traffic uniformly. Examples include accelerators and load balancers. Max - Use the highest traffic from each resource. This is useful for resources that don't share traffic and for resources that share that traffic in a non-uniform way. Examples include Amazon CloudFront and origin resources for CloudFront distributions."];
      pattern: ProtectionGroupPattern.t
        [@ocaml.doc
          "The criteria to use to choose the protected resources for inclusion in the group. You can include all resources that have protections, provide a list of resource Amazon Resource Names (ARNs), or include all resources of a specified resource type."];
      resourceType: ProtectedResourceType.t option
        [@ocaml.doc
          "The resource type to include in the protection group. All protected resources of this type are included in the protection group. Newly protected resources of this type are automatically added to the group. You must set this when you set Pattern to BY_RESOURCE_TYPE and you must not set it for any other Pattern setting."];
      members: ProtectionGroupMembers.t option
        [@ocaml.doc
          "The Amazon Resource Names (ARNs) of the resources to include in the protection group. You must set this when you set Pattern to ARBITRARY and you must not set it for any other Pattern setting."];
      tags: TagList.t option
        [@ocaml.doc
          "One or more tag key-value pairs for the protection group."]}
    let context_ = "CreateProtectionGroupRequest"
    let make ?resourceType =
      fun ?members ->
        fun ?tags ->
          fun ~protectionGroupId ->
            fun ~aggregation ->
              fun ~pattern ->
                fun () ->
                  {
                    resourceType;
                    members;
                    tags;
                    protectionGroupId;
                    aggregation;
                    pattern
                  }
    let to_value x =
      structure_to_value
        [("ProtectionGroupId",
           (Some (ProtectionGroupId.to_value x.protectionGroupId)));
        ("Aggregation",
          (Some (ProtectionGroupAggregation.to_value x.aggregation)));
        ("Pattern", (Some (ProtectionGroupPattern.to_value x.pattern)));
        ("ResourceType",
          (Option.map x.resourceType ~f:ProtectedResourceType.to_value));
        ("Members",
          (Option.map x.members ~f:ProtectionGroupMembers.to_value));
        ("Tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let members =
        (Option.map ~f:ProtectionGroupMembers.of_xml)
          (Xml.child xml_arg0 "Members") in
      let resourceType =
        (Option.map ~f:ProtectedResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      let pattern =
        ProtectionGroupPattern.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Pattern") in
      let aggregation =
        ProtectionGroupAggregation.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Aggregation") in
      let protectionGroupId =
        ProtectionGroupId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProtectionGroupId") in
      make ?tags ?members ?resourceType ~pattern ~aggregation
        ~protectionGroupId ()
    let of_json json =
      let tags = field_map json "Tags" TagList.of_json in
      let members = field_map json "Members" ProtectionGroupMembers.of_json in
      let resourceType =
        field_map json "ResourceType" ProtectedResourceType.of_json in
      let pattern =
        field_map_exn json "Pattern" ProtectionGroupPattern.of_json in
      let aggregation =
        field_map_exn json "Aggregation" ProtectionGroupAggregation.of_json in
      let protectionGroupId =
        field_map_exn json "ProtectionGroupId" ProtectionGroupId.of_json in
      make ?tags ?members ?resourceType ~pattern ~aggregation
        ~protectionGroupId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a grouping of protected resources so they can be handled as a collective. This resource grouping improves the accuracy of detection and reduces false positives."]
module AssociateProactiveEngagementDetailsResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `InvalidOperationException of InvalidOperationException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `OptimisticLockException of OptimisticLockException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `InvalidOperationException e ->
          `Assoc
            [("error", (`String "InvalidOperationException"));
            ("details", (InvalidOperationException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `OptimisticLockException e ->
          `Assoc
            [("error", (`String "OptimisticLockException"));
            ("details", (OptimisticLockException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Initializes proactive engagement and sets the list of contacts for the Shield Response Team (SRT) to use. You must provide at least one phone number in the emergency contact list. After you have initialized proactive engagement using this call, to disable or enable proactive engagement, use the calls DisableProactiveEngagement and EnableProactiveEngagement. This call defines the list of email addresses and phone numbers that the SRT can use to contact you for escalations to the SRT and to initiate proactive customer support. The contacts that you provide in the request replace any contacts that were already defined. If you already have contacts defined and want to use them, retrieve the list using DescribeEmergencyContactSettings and then provide it to this call."]
module AssociateProactiveEngagementDetailsRequest =
  struct
    type nonrec t =
      {
      emergencyContactList: EmergencyContactList.t
        [@ocaml.doc
          "A list of email addresses and phone numbers that the Shield Response Team (SRT) can use to contact you for escalations to the SRT and to initiate proactive customer support. To enable proactive engagement, the contact list must include at least one phone number. The contacts that you provide here replace any contacts that were already defined. If you already have contacts defined and want to use them, retrieve the list using DescribeEmergencyContactSettings and then provide it here."]}
    let context_ = "AssociateProactiveEngagementDetailsRequest"
    let make ~emergencyContactList = fun () -> { emergencyContactList }
    let to_value x =
      structure_to_value
        [("EmergencyContactList",
           (Some (EmergencyContactList.to_value x.emergencyContactList)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let emergencyContactList =
        EmergencyContactList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "EmergencyContactList") in
      make ~emergencyContactList ()
    let of_json json =
      let emergencyContactList =
        field_map_exn json "EmergencyContactList"
          EmergencyContactList.of_json in
      make ~emergencyContactList ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Initializes proactive engagement and sets the list of contacts for the Shield Response Team (SRT) to use. You must provide at least one phone number in the emergency contact list. After you have initialized proactive engagement using this call, to disable or enable proactive engagement, use the calls DisableProactiveEngagement and EnableProactiveEngagement. This call defines the list of email addresses and phone numbers that the SRT can use to contact you for escalations to the SRT and to initiate proactive customer support. The contacts that you provide in the request replace any contacts that were already defined. If you already have contacts defined and want to use them, retrieve the list using DescribeEmergencyContactSettings and then provide it to this call."]
module AssociateHealthCheckResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InternalErrorException of InternalErrorException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `InvalidResourceException of InvalidResourceException.t 
      | `LimitsExceededException of LimitsExceededException.t 
      | `OptimisticLockException of OptimisticLockException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "InvalidResourceException" ->
          `InvalidResourceException (InvalidResourceException.of_json json)
      | "LimitsExceededException" ->
          `LimitsExceededException (LimitsExceededException.of_json json)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "InvalidResourceException" ->
          `InvalidResourceException (InvalidResourceException.of_xml xml)
      | "LimitsExceededException" ->
          `LimitsExceededException (LimitsExceededException.of_xml xml)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `InvalidResourceException e ->
          `Assoc
            [("error", (`String "InvalidResourceException"));
            ("details", (InvalidResourceException.to_json e))]
      | `LimitsExceededException e ->
          `Assoc
            [("error", (`String "LimitsExceededException"));
            ("details", (LimitsExceededException.to_json e))]
      | `OptimisticLockException e ->
          `Assoc
            [("error", (`String "OptimisticLockException"));
            ("details", (OptimisticLockException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Adds health-based detection to the Shield Advanced protection for a resource. Shield Advanced health-based detection uses the health of your Amazon Web Services resource to improve responsiveness and accuracy in attack detection and response. You define the health check in Route\194\16053 and then associate it with your Shield Advanced protection. For more information, see Shield Advanced Health-Based Detection in the WAF Developer Guide."]
module AssociateHealthCheckRequest =
  struct
    type nonrec t =
      {
      protectionId: ProtectionId.t
        [@ocaml.doc
          "The unique identifier (ID) for the Protection object to add the health check association to."];
      healthCheckArn: HealthCheckArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the health check to associate with the protection."]}
    let context_ = "AssociateHealthCheckRequest"
    let make ~protectionId =
      fun ~healthCheckArn -> fun () -> { protectionId; healthCheckArn }
    let to_value x =
      structure_to_value
        [("ProtectionId", (Some (ProtectionId.to_value x.protectionId)));
        ("HealthCheckArn", (Some (HealthCheckArn.to_value x.healthCheckArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let healthCheckArn =
        HealthCheckArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HealthCheckArn") in
      let protectionId =
        ProtectionId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ProtectionId") in
      make ~healthCheckArn ~protectionId ()
    let of_json json =
      let healthCheckArn =
        field_map_exn json "HealthCheckArn" HealthCheckArn.of_json in
      let protectionId =
        field_map_exn json "ProtectionId" ProtectionId.of_json in
      make ~healthCheckArn ~protectionId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Adds health-based detection to the Shield Advanced protection for a resource. Shield Advanced health-based detection uses the health of your Amazon Web Services resource to improve responsiveness and accuracy in attack detection and response. You define the health check in Route\194\16053 and then associate it with your Shield Advanced protection. For more information, see Shield Advanced Health-Based Detection in the WAF Developer Guide."]
module AssociateDRTRoleResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [
        `AccessDeniedForDependencyException of
          AccessDeniedForDependencyException.t 
      | `InternalErrorException of InternalErrorException.t 
      | `InvalidOperationException of InvalidOperationException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `OptimisticLockException of OptimisticLockException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedForDependencyException" ->
          `AccessDeniedForDependencyException
            (AccessDeniedForDependencyException.of_json json)
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedForDependencyException" ->
          `AccessDeniedForDependencyException
            (AccessDeniedForDependencyException.of_xml xml)
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedForDependencyException e ->
          `Assoc
            [("error", (`String "AccessDeniedForDependencyException"));
            ("details", (AccessDeniedForDependencyException.to_json e))]
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `InvalidOperationException e ->
          `Assoc
            [("error", (`String "InvalidOperationException"));
            ("details", (InvalidOperationException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `OptimisticLockException e ->
          `Assoc
            [("error", (`String "OptimisticLockException"));
            ("details", (OptimisticLockException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Authorizes the Shield Response Team (SRT) using the specified role, to access your Amazon Web Services account to assist with DDoS attack mitigation during potential attacks. This enables the SRT to inspect your WAF configuration and create or update WAF rules and web ACLs. You can associate only one RoleArn with your subscription. If you submit an AssociateDRTRole request for an account that already has an associated role, the new RoleArn will replace the existing RoleArn. Prior to making the AssociateDRTRole request, you must attach the AWSShieldDRTAccessPolicy managed policy to the role that you'll specify in the request. You can access this policy in the IAM console at AWSShieldDRTAccessPolicy. For more information see Adding and removing IAM identity permissions. The role must also trust the service principal drt.shield.amazonaws.com. For more information, see IAM JSON policy elements: Principal. The SRT will have access only to your WAF and Shield resources. By submitting this request, you authorize the SRT to inspect your WAF and Shield configuration and create and update WAF rules and web ACLs on your behalf. The SRT takes these actions only if explicitly authorized by you. You must have the iam:PassRole permission to make an AssociateDRTRole request. For more information, see Granting a user permissions to pass a role to an Amazon Web Services service. To use the services of the SRT and make an AssociateDRTRole request, you must be subscribed to the Business Support plan or the Enterprise Support plan."]
module AssociateDRTRoleRequest =
  struct
    type nonrec t =
      {
      roleArn: RoleArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the role the SRT will use to access your Amazon Web Services account. Prior to making the AssociateDRTRole request, you must attach the AWSShieldDRTAccessPolicy managed policy to this role. For more information see Attaching and Detaching IAM Policies."]}
    let context_ = "AssociateDRTRoleRequest"
    let make ~roleArn = fun () -> { roleArn }
    let to_value x =
      structure_to_value [("RoleArn", (Some (RoleArn.to_value x.roleArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let roleArn =
        RoleArn.of_xml (Xml.child_exn ~context:context_ xml_arg0 "RoleArn") in
      make ~roleArn ()
    let of_json json =
      let roleArn = field_map_exn json "RoleArn" RoleArn.of_json in
      make ~roleArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Authorizes the Shield Response Team (SRT) using the specified role, to access your Amazon Web Services account to assist with DDoS attack mitigation during potential attacks. This enables the SRT to inspect your WAF configuration and create or update WAF rules and web ACLs. You can associate only one RoleArn with your subscription. If you submit an AssociateDRTRole request for an account that already has an associated role, the new RoleArn will replace the existing RoleArn. Prior to making the AssociateDRTRole request, you must attach the AWSShieldDRTAccessPolicy managed policy to the role that you'll specify in the request. You can access this policy in the IAM console at AWSShieldDRTAccessPolicy. For more information see Adding and removing IAM identity permissions. The role must also trust the service principal drt.shield.amazonaws.com. For more information, see IAM JSON policy elements: Principal. The SRT will have access only to your WAF and Shield resources. By submitting this request, you authorize the SRT to inspect your WAF and Shield configuration and create and update WAF rules and web ACLs on your behalf. The SRT takes these actions only if explicitly authorized by you. You must have the iam:PassRole permission to make an AssociateDRTRole request. For more information, see Granting a user permissions to pass a role to an Amazon Web Services service. To use the services of the SRT and make an AssociateDRTRole request, you must be subscribed to the Business Support plan or the Enterprise Support plan."]
module AssociateDRTLogBucketResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [
        `AccessDeniedForDependencyException of
          AccessDeniedForDependencyException.t 
      | `InternalErrorException of InternalErrorException.t 
      | `InvalidOperationException of InvalidOperationException.t 
      | `InvalidParameterException of InvalidParameterException.t 
      | `LimitsExceededException of LimitsExceededException.t 
      | `NoAssociatedRoleException of NoAssociatedRoleException.t 
      | `OptimisticLockException of OptimisticLockException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "AccessDeniedForDependencyException" ->
          `AccessDeniedForDependencyException
            (AccessDeniedForDependencyException.of_json json)
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_json json)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_json json)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_json json)
      | "LimitsExceededException" ->
          `LimitsExceededException (LimitsExceededException.of_json json)
      | "NoAssociatedRoleException" ->
          `NoAssociatedRoleException (NoAssociatedRoleException.of_json json)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccessDeniedForDependencyException" ->
          `AccessDeniedForDependencyException
            (AccessDeniedForDependencyException.of_xml xml)
      | "InternalErrorException" ->
          `InternalErrorException (InternalErrorException.of_xml xml)
      | "InvalidOperationException" ->
          `InvalidOperationException (InvalidOperationException.of_xml xml)
      | "InvalidParameterException" ->
          `InvalidParameterException (InvalidParameterException.of_xml xml)
      | "LimitsExceededException" ->
          `LimitsExceededException (LimitsExceededException.of_xml xml)
      | "NoAssociatedRoleException" ->
          `NoAssociatedRoleException (NoAssociatedRoleException.of_xml xml)
      | "OptimisticLockException" ->
          `OptimisticLockException (OptimisticLockException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccessDeniedForDependencyException e ->
          `Assoc
            [("error", (`String "AccessDeniedForDependencyException"));
            ("details", (AccessDeniedForDependencyException.to_json e))]
      | `InternalErrorException e ->
          `Assoc
            [("error", (`String "InternalErrorException"));
            ("details", (InternalErrorException.to_json e))]
      | `InvalidOperationException e ->
          `Assoc
            [("error", (`String "InvalidOperationException"));
            ("details", (InvalidOperationException.to_json e))]
      | `InvalidParameterException e ->
          `Assoc
            [("error", (`String "InvalidParameterException"));
            ("details", (InvalidParameterException.to_json e))]
      | `LimitsExceededException e ->
          `Assoc
            [("error", (`String "LimitsExceededException"));
            ("details", (LimitsExceededException.to_json e))]
      | `NoAssociatedRoleException e ->
          `Assoc
            [("error", (`String "NoAssociatedRoleException"));
            ("details", (NoAssociatedRoleException.to_json e))]
      | `OptimisticLockException e ->
          `Assoc
            [("error", (`String "OptimisticLockException"));
            ("details", (OptimisticLockException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Authorizes the Shield Response Team (SRT) to access the specified Amazon S3 bucket containing log data such as Application Load Balancer access logs, CloudFront logs, or logs from third party sources. You can associate up to 10 Amazon S3 buckets with your subscription. To use the services of the SRT and make an AssociateDRTLogBucket request, you must be subscribed to the Business Support plan or the Enterprise Support plan."]
module AssociateDRTLogBucketRequest =
  struct
    type nonrec t =
      {
      logBucket: LogBucket.t
        [@ocaml.doc
          "The Amazon S3 bucket that contains the logs that you want to share."]}
    let context_ = "AssociateDRTLogBucketRequest"
    let make ~logBucket = fun () -> { logBucket }
    let to_value x =
      structure_to_value
        [("LogBucket", (Some (LogBucket.to_value x.logBucket)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let logBucket =
        LogBucket.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LogBucket") in
      make ~logBucket ()
    let of_json json =
      let logBucket = field_map_exn json "LogBucket" LogBucket.of_json in
      make ~logBucket ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Authorizes the Shield Response Team (SRT) to access the specified Amazon S3 bucket containing log data such as Application Load Balancer access logs, CloudFront logs, or logs from third party sources. You can associate up to 10 Amazon S3 buckets with your subscription. To use the services of the SRT and make an AssociateDRTLogBucket request, you must be subscribed to the Business Support plan or the Enterprise Support plan."]
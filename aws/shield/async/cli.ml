(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_d_r_t_log_bucket =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and logBucket =
         flag "log-bucket" (required string) ~doc:"STRING LogBucket" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_d_r_t_log_bucket
           (Values.AssociateDRTLogBucketRequest.make ~logBucket ())
           (Some Values.AssociateDRTLogBucketResponse.to_json)
           (Some Values.AssociateDRTLogBucketResponse.error_to_json)])
let associate_d_r_t_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and roleArn = flag "role-arn" (required string) ~doc:"STRING RoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_d_r_t_role
           (Values.AssociateDRTRoleRequest.make ~roleArn ())
           (Some Values.AssociateDRTRoleResponse.to_json)
           (Some Values.AssociateDRTRoleResponse.error_to_json)])
let associate_health_check =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and protectionId =
         flag "protection-id" (required string) ~doc:"STRING ProtectionId"
       and healthCheckArn =
         flag "health-check-arn" (required string)
           ~doc:"STRING HealthCheckArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_health_check
           (Values.AssociateHealthCheckRequest.make ~protectionId
              ~healthCheckArn ())
           (Some Values.AssociateHealthCheckResponse.to_json)
           (Some Values.AssociateHealthCheckResponse.error_to_json)])
let associate_proactive_engagement_details =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and emergencyContactList =
         flag "emergency-contact-list" (required json_arg)
           ~doc:"JSON EmergencyContactList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_proactive_engagement_details
           (Values.AssociateProactiveEngagementDetailsRequest.make
              ~emergencyContactList:(Values.EmergencyContactList.of_json
                                       emergencyContactList) ())
           (Some Values.AssociateProactiveEngagementDetailsResponse.to_json)
           (Some
              Values.AssociateProactiveEngagementDetailsResponse.error_to_json)])
let create_protection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name = flag "name" (required string) ~doc:"STRING ProtectionName"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_protection
           (Values.CreateProtectionRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name
              ~resourceArn ()) (Some Values.CreateProtectionResponse.to_json)
           (Some Values.CreateProtectionResponse.error_to_json)])
let create_protection_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceType =
         flag "resource-type" (optional json_arg)
           ~doc:"JSON ProtectedResourceType"
       and members =
         flag "members" (optional json_arg)
           ~doc:"JSON ProtectionGroupMembers"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and protectionGroupId =
         flag "protection-group-id" (required string)
           ~doc:"STRING ProtectionGroupId"
       and aggregation =
         flag "aggregation" (required json_arg)
           ~doc:"JSON ProtectionGroupAggregation"
       and pattern =
         flag "pattern" (required json_arg)
           ~doc:"JSON ProtectionGroupPattern" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_protection_group
           (Values.CreateProtectionGroupRequest.make
              ?resourceType:(Option.map
                               ~f:Values.ProtectedResourceType.of_json
                               resourceType)
              ?members:(Option.map ~f:Values.ProtectionGroupMembers.of_json
                          members)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~protectionGroupId
              ~aggregation:(Values.ProtectionGroupAggregation.of_json
                              aggregation)
              ~pattern:(Values.ProtectionGroupPattern.of_json pattern) ())
           (Some Values.CreateProtectionGroupResponse.to_json)
           (Some Values.CreateProtectionGroupResponse.error_to_json)])
let create_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_subscription (Values.CreateSubscriptionRequest.make ())
           (Some Values.CreateSubscriptionResponse.to_json)
           (Some Values.CreateSubscriptionResponse.error_to_json)])
let delete_protection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and protectionId =
         flag "protection-id" (required string) ~doc:"STRING ProtectionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_protection
           (Values.DeleteProtectionRequest.make ~protectionId ())
           (Some Values.DeleteProtectionResponse.to_json)
           (Some Values.DeleteProtectionResponse.error_to_json)])
let delete_protection_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and protectionGroupId =
         flag "protection-group-id" (required string)
           ~doc:"STRING ProtectionGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_protection_group
           (Values.DeleteProtectionGroupRequest.make ~protectionGroupId ())
           (Some Values.DeleteProtectionGroupResponse.to_json)
           (Some Values.DeleteProtectionGroupResponse.error_to_json)])
let delete_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_subscription (Values.DeleteSubscriptionRequest.make ())
           (Some Values.DeleteSubscriptionResponse.to_json)
           (Some Values.DeleteSubscriptionResponse.error_to_json)])
let describe_attack =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attackId =
         flag "attack-id" (required string) ~doc:"STRING AttackId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_attack
           (Values.DescribeAttackRequest.make ~attackId ())
           (Some Values.DescribeAttackResponse.to_json)
           (Some Values.DescribeAttackResponse.error_to_json)])
let describe_attack_statistics =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_attack_statistics
           (Values.DescribeAttackStatisticsRequest.make ())
           (Some Values.DescribeAttackStatisticsResponse.to_json)
           (Some Values.DescribeAttackStatisticsResponse.error_to_json)])
let describe_d_r_t_access =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_d_r_t_access (Values.DescribeDRTAccessRequest.make ())
           (Some Values.DescribeDRTAccessResponse.to_json)
           (Some Values.DescribeDRTAccessResponse.error_to_json)])
let describe_emergency_contact_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_emergency_contact_settings
           (Values.DescribeEmergencyContactSettingsRequest.make ())
           (Some Values.DescribeEmergencyContactSettingsResponse.to_json)
           (Some
              Values.DescribeEmergencyContactSettingsResponse.error_to_json)])
let describe_protection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and protectionId =
         flag "protection-id" (optional string) ~doc:"STRING ProtectionId"
       and resourceArn =
         flag "resource-arn" (optional string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_protection
           (Values.DescribeProtectionRequest.make ?protectionId ?resourceArn
              ()) (Some Values.DescribeProtectionResponse.to_json)
           (Some Values.DescribeProtectionResponse.error_to_json)])
let describe_protection_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and protectionGroupId =
         flag "protection-group-id" (required string)
           ~doc:"STRING ProtectionGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_protection_group
           (Values.DescribeProtectionGroupRequest.make ~protectionGroupId ())
           (Some Values.DescribeProtectionGroupResponse.to_json)
           (Some Values.DescribeProtectionGroupResponse.error_to_json)])
let describe_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_subscription
           (Values.DescribeSubscriptionRequest.make ())
           (Some Values.DescribeSubscriptionResponse.to_json)
           (Some Values.DescribeSubscriptionResponse.error_to_json)])
let disable_application_layer_automatic_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_application_layer_automatic_response
           (Values.DisableApplicationLayerAutomaticResponseRequest.make
              ~resourceArn ())
           (Some
              Values.DisableApplicationLayerAutomaticResponseResponse.to_json)
           (Some
              Values.DisableApplicationLayerAutomaticResponseResponse.error_to_json)])
let disable_proactive_engagement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_proactive_engagement
           (Values.DisableProactiveEngagementRequest.make ())
           (Some Values.DisableProactiveEngagementResponse.to_json)
           (Some Values.DisableProactiveEngagementResponse.error_to_json)])
let disassociate_d_r_t_log_bucket =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and logBucket =
         flag "log-bucket" (required string) ~doc:"STRING LogBucket" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_d_r_t_log_bucket
           (Values.DisassociateDRTLogBucketRequest.make ~logBucket ())
           (Some Values.DisassociateDRTLogBucketResponse.to_json)
           (Some Values.DisassociateDRTLogBucketResponse.error_to_json)])
let disassociate_d_r_t_role =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_d_r_t_role
           (Values.DisassociateDRTRoleRequest.make ())
           (Some Values.DisassociateDRTRoleResponse.to_json)
           (Some Values.DisassociateDRTRoleResponse.error_to_json)])
let disassociate_health_check =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and protectionId =
         flag "protection-id" (required string) ~doc:"STRING ProtectionId"
       and healthCheckArn =
         flag "health-check-arn" (required string)
           ~doc:"STRING HealthCheckArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_health_check
           (Values.DisassociateHealthCheckRequest.make ~protectionId
              ~healthCheckArn ())
           (Some Values.DisassociateHealthCheckResponse.to_json)
           (Some Values.DisassociateHealthCheckResponse.error_to_json)])
let enable_application_layer_automatic_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and action =
         flag "action" (required json_arg) ~doc:"JSON ResponseAction" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_application_layer_automatic_response
           (Values.EnableApplicationLayerAutomaticResponseRequest.make
              ~resourceArn ~action:(Values.ResponseAction.of_json action) ())
           (Some
              Values.EnableApplicationLayerAutomaticResponseResponse.to_json)
           (Some
              Values.EnableApplicationLayerAutomaticResponseResponse.error_to_json)])
let enable_proactive_engagement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_proactive_engagement
           (Values.EnableProactiveEngagementRequest.make ())
           (Some Values.EnableProactiveEngagementResponse.to_json)
           (Some Values.EnableProactiveEngagementResponse.error_to_json)])
let get_subscription_state =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_subscription_state
           (Values.GetSubscriptionStateRequest.make ())
           (Some Values.GetSubscriptionStateResponse.to_json)
           (Some Values.GetSubscriptionStateResponse.error_to_json)])
let list_attacks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArns =
         flag "resource-arns" (optional json_arg)
           ~doc:"JSON ResourceArnFilterList"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON TimeRange"
       and endTime =
         flag "end-time" (optional json_arg) ~doc:"JSON TimeRange"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_attacks
           (Values.ListAttacksRequest.make
              ?resourceArns:(Option.map
                               ~f:Values.ResourceArnFilterList.of_json
                               resourceArns)
              ?startTime:(Option.map ~f:Values.TimeRange.of_json startTime)
              ?endTime:(Option.map ~f:Values.TimeRange.of_json endTime)
              ?nextToken ?maxResults ())
           (Some Values.ListAttacksResponse.to_json)
           (Some Values.ListAttacksResponse.error_to_json)])
let list_protection_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_protection_groups
           (Values.ListProtectionGroupsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListProtectionGroupsResponse.to_json)
           (Some Values.ListProtectionGroupsResponse.error_to_json)])
let list_protections =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_protections
           (Values.ListProtectionsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListProtectionsResponse.to_json)
           (Some Values.ListProtectionsResponse.error_to_json)])
let list_resources_in_protection_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING Token"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and protectionGroupId =
         flag "protection-group-id" (required string)
           ~doc:"STRING ProtectionGroupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resources_in_protection_group
           (Values.ListResourcesInProtectionGroupRequest.make ?nextToken
              ?maxResults ~protectionGroupId ())
           (Some Values.ListResourcesInProtectionGroupResponse.to_json)
           (Some Values.ListResourcesInProtectionGroupResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING ResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_application_layer_automatic_response =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and action =
         flag "action" (required json_arg) ~doc:"JSON ResponseAction" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_application_layer_automatic_response
           (Values.UpdateApplicationLayerAutomaticResponseRequest.make
              ~resourceArn ~action:(Values.ResponseAction.of_json action) ())
           (Some
              Values.UpdateApplicationLayerAutomaticResponseResponse.to_json)
           (Some
              Values.UpdateApplicationLayerAutomaticResponseResponse.error_to_json)])
let update_emergency_contact_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and emergencyContactList =
         flag "emergency-contact-list" (optional json_arg)
           ~doc:"JSON EmergencyContactList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_emergency_contact_settings
           (Values.UpdateEmergencyContactSettingsRequest.make
              ?emergencyContactList:(Option.map
                                       ~f:Values.EmergencyContactList.of_json
                                       emergencyContactList) ())
           (Some Values.UpdateEmergencyContactSettingsResponse.to_json)
           (Some Values.UpdateEmergencyContactSettingsResponse.error_to_json)])
let update_protection_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceType =
         flag "resource-type" (optional json_arg)
           ~doc:"JSON ProtectedResourceType"
       and members =
         flag "members" (optional json_arg)
           ~doc:"JSON ProtectionGroupMembers"
       and protectionGroupId =
         flag "protection-group-id" (required string)
           ~doc:"STRING ProtectionGroupId"
       and aggregation =
         flag "aggregation" (required json_arg)
           ~doc:"JSON ProtectionGroupAggregation"
       and pattern =
         flag "pattern" (required json_arg)
           ~doc:"JSON ProtectionGroupPattern" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_protection_group
           (Values.UpdateProtectionGroupRequest.make
              ?resourceType:(Option.map
                               ~f:Values.ProtectedResourceType.of_json
                               resourceType)
              ?members:(Option.map ~f:Values.ProtectionGroupMembers.of_json
                          members) ~protectionGroupId
              ~aggregation:(Values.ProtectionGroupAggregation.of_json
                              aggregation)
              ~pattern:(Values.ProtectionGroupPattern.of_json pattern) ())
           (Some Values.UpdateProtectionGroupResponse.to_json)
           (Some Values.UpdateProtectionGroupResponse.error_to_json)])
let update_subscription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoRenew =
         flag "auto-renew" (optional json_arg) ~doc:"JSON AutoRenew" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_subscription
           (Values.UpdateSubscriptionRequest.make
              ?autoRenew:(Option.map ~f:Values.AutoRenew.of_json autoRenew)
              ()) (Some Values.UpdateSubscriptionResponse.to_json)
           (Some Values.UpdateSubscriptionResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-d-r-t-log-bucket", associate_d_r_t_log_bucket);
    ("associate-d-r-t-role", associate_d_r_t_role);
    ("associate-health-check", associate_health_check);
    ("associate-proactive-engagement-details",
      associate_proactive_engagement_details);
    ("create-protection", create_protection);
    ("create-protection-group", create_protection_group);
    ("create-subscription", create_subscription);
    ("delete-protection", delete_protection);
    ("delete-protection-group", delete_protection_group);
    ("delete-subscription", delete_subscription);
    ("describe-attack", describe_attack);
    ("describe-attack-statistics", describe_attack_statistics);
    ("describe-d-r-t-access", describe_d_r_t_access);
    ("describe-emergency-contact-settings",
      describe_emergency_contact_settings);
    ("describe-protection", describe_protection);
    ("describe-protection-group", describe_protection_group);
    ("describe-subscription", describe_subscription);
    ("disable-application-layer-automatic-response",
      disable_application_layer_automatic_response);
    ("disable-proactive-engagement", disable_proactive_engagement);
    ("disassociate-d-r-t-log-bucket", disassociate_d_r_t_log_bucket);
    ("disassociate-d-r-t-role", disassociate_d_r_t_role);
    ("disassociate-health-check", disassociate_health_check);
    ("enable-application-layer-automatic-response",
      enable_application_layer_automatic_response);
    ("enable-proactive-engagement", enable_proactive_engagement);
    ("get-subscription-state", get_subscription_state);
    ("list-attacks", list_attacks);
    ("list-protection-groups", list_protection_groups);
    ("list-protections", list_protections);
    ("list-resources-in-protection-group",
      list_resources_in_protection_group);
    ("list-tags-for-resource", list_tags_for_resource);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-application-layer-automatic-response",
      update_application_layer_automatic_response);
    ("update-emergency-contact-settings", update_emergency_contact_settings);
    ("update-protection-group", update_protection_group);
    ("update-subscription", update_subscription)]
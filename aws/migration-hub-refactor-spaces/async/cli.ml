(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and apiGatewayProxy =
         flag "api-gateway-proxy" (optional json_arg)
           ~doc:"JSON ApiGatewayProxyInput"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId"
       and name = flag "name" (required string) ~doc:"STRING ApplicationName"
       and proxyType =
         flag "proxy-type" (required json_arg) ~doc:"JSON ProxyType"
       and vpcId = flag "vpc-id" (required string) ~doc:"STRING VpcId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_application
           (Values.CreateApplicationRequest.make
              ?apiGatewayProxy:(Option.map
                                  ~f:Values.ApiGatewayProxyInput.of_json
                                  apiGatewayProxy) ?clientToken
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~environmentIdentifier ~name
              ~proxyType:(Values.ProxyType.of_json proxyType) ~vpcId ())
           (Some Values.CreateApplicationResponse.to_json)
           (Some Values.CreateApplicationResponse.error_to_json)])
let create_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name = flag "name" (required string) ~doc:"STRING EnvironmentName"
       and networkFabricType =
         flag "network-fabric-type" (required json_arg)
           ~doc:"JSON NetworkFabricType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_environment
           (Values.CreateEnvironmentRequest.make ?clientToken ?description
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name
              ~networkFabricType:(Values.NetworkFabricType.of_json
                                    networkFabricType) ())
           (Some Values.CreateEnvironmentResponse.to_json)
           (Some Values.CreateEnvironmentResponse.error_to_json)])
let create_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and defaultRoute =
         flag "default-route" (optional json_arg)
           ~doc:"JSON DefaultRouteInput"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and uriPathRoute =
         flag "uri-path-route" (optional json_arg)
           ~doc:"JSON UriPathRouteInput"
       and applicationIdentifier =
         flag "application-identifier" (required string)
           ~doc:"STRING ApplicationId"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId"
       and routeType =
         flag "route-type" (required json_arg) ~doc:"JSON RouteType"
       and serviceIdentifier =
         flag "service-identifier" (required string) ~doc:"STRING ServiceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_route
           (Values.CreateRouteRequest.make ?clientToken
              ?defaultRoute:(Option.map ~f:Values.DefaultRouteInput.of_json
                               defaultRoute)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?uriPathRoute:(Option.map ~f:Values.UriPathRouteInput.of_json
                               uriPathRoute) ~applicationIdentifier
              ~environmentIdentifier
              ~routeType:(Values.RouteType.of_json routeType)
              ~serviceIdentifier ())
           (Some Values.CreateRouteResponse.to_json)
           (Some Values.CreateRouteResponse.error_to_json)])
let create_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and lambdaEndpoint =
         flag "lambda-endpoint" (optional json_arg)
           ~doc:"JSON LambdaEndpointInput"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and urlEndpoint =
         flag "url-endpoint" (optional json_arg) ~doc:"JSON UrlEndpointInput"
       and vpcId = flag "vpc-id" (optional string) ~doc:"STRING VpcId"
       and applicationIdentifier =
         flag "application-identifier" (required string)
           ~doc:"STRING ApplicationId"
       and endpointType =
         flag "endpoint-type" (required json_arg)
           ~doc:"JSON ServiceEndpointType"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId"
       and name = flag "name" (required string) ~doc:"STRING ServiceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_service
           (Values.CreateServiceRequest.make ?clientToken ?description
              ?lambdaEndpoint:(Option.map
                                 ~f:Values.LambdaEndpointInput.of_json
                                 lambdaEndpoint)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?urlEndpoint:(Option.map ~f:Values.UrlEndpointInput.of_json
                              urlEndpoint) ?vpcId ~applicationIdentifier
              ~endpointType:(Values.ServiceEndpointType.of_json endpointType)
              ~environmentIdentifier ~name ())
           (Some Values.CreateServiceResponse.to_json)
           (Some Values.CreateServiceResponse.error_to_json)])
let delete_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationIdentifier =
         flag "application-identifier" (required string)
           ~doc:"STRING ApplicationId"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_application
           (Values.DeleteApplicationRequest.make ~applicationIdentifier
              ~environmentIdentifier ())
           (Some Values.DeleteApplicationResponse.to_json)
           (Some Values.DeleteApplicationResponse.error_to_json)])
let delete_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_environment
           (Values.DeleteEnvironmentRequest.make ~environmentIdentifier ())
           (Some Values.DeleteEnvironmentResponse.to_json)
           (Some Values.DeleteEnvironmentResponse.error_to_json)])
let delete_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING ResourcePolicyIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_policy
           (Values.DeleteResourcePolicyRequest.make ~identifier ())
           (Some Values.DeleteResourcePolicyResponse.to_json)
           (Some Values.DeleteResourcePolicyResponse.error_to_json)])
let delete_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationIdentifier =
         flag "application-identifier" (required string)
           ~doc:"STRING ApplicationId"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId"
       and routeIdentifier =
         flag "route-identifier" (required string) ~doc:"STRING RouteId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_route
           (Values.DeleteRouteRequest.make ~applicationIdentifier
              ~environmentIdentifier ~routeIdentifier ())
           (Some Values.DeleteRouteResponse.to_json)
           (Some Values.DeleteRouteResponse.error_to_json)])
let delete_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationIdentifier =
         flag "application-identifier" (required string)
           ~doc:"STRING ApplicationId"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId"
       and serviceIdentifier =
         flag "service-identifier" (required string) ~doc:"STRING ServiceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_service
           (Values.DeleteServiceRequest.make ~applicationIdentifier
              ~environmentIdentifier ~serviceIdentifier ())
           (Some Values.DeleteServiceResponse.to_json)
           (Some Values.DeleteServiceResponse.error_to_json)])
let get_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationIdentifier =
         flag "application-identifier" (required string)
           ~doc:"STRING ApplicationId"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_application
           (Values.GetApplicationRequest.make ~applicationIdentifier
              ~environmentIdentifier ())
           (Some Values.GetApplicationResponse.to_json)
           (Some Values.GetApplicationResponse.error_to_json)])
let get_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_environment
           (Values.GetEnvironmentRequest.make ~environmentIdentifier ())
           (Some Values.GetEnvironmentResponse.to_json)
           (Some Values.GetEnvironmentResponse.error_to_json)])
let get_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING ResourcePolicyIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_policy
           (Values.GetResourcePolicyRequest.make ~identifier ())
           (Some Values.GetResourcePolicyResponse.to_json)
           (Some Values.GetResourcePolicyResponse.error_to_json)])
let get_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationIdentifier =
         flag "application-identifier" (required string)
           ~doc:"STRING ApplicationId"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId"
       and routeIdentifier =
         flag "route-identifier" (required string) ~doc:"STRING RouteId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_route
           (Values.GetRouteRequest.make ~applicationIdentifier
              ~environmentIdentifier ~routeIdentifier ())
           (Some Values.GetRouteResponse.to_json)
           (Some Values.GetRouteResponse.error_to_json)])
let get_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationIdentifier =
         flag "application-identifier" (required string)
           ~doc:"STRING ApplicationId"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId"
       and serviceIdentifier =
         flag "service-identifier" (required string) ~doc:"STRING ServiceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service
           (Values.GetServiceRequest.make ~applicationIdentifier
              ~environmentIdentifier ~serviceIdentifier ())
           (Some Values.GetServiceResponse.to_json)
           (Some Values.GetServiceResponse.error_to_json)])
let list_applications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_applications
           (Values.ListApplicationsRequest.make ?maxResults ?nextToken
              ~environmentIdentifier ())
           (Some Values.ListApplicationsResponse.to_json)
           (Some Values.ListApplicationsResponse.error_to_json)])
let list_environment_vpcs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_environment_vpcs
           (Values.ListEnvironmentVpcsRequest.make ?maxResults ?nextToken
              ~environmentIdentifier ())
           (Some Values.ListEnvironmentVpcsResponse.to_json)
           (Some Values.ListEnvironmentVpcsResponse.error_to_json)])
let list_environments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_environments
           (Values.ListEnvironmentsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListEnvironmentsResponse.to_json)
           (Some Values.ListEnvironmentsResponse.error_to_json)])
let list_routes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and applicationIdentifier =
         flag "application-identifier" (required string)
           ~doc:"STRING ApplicationId"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_routes
           (Values.ListRoutesRequest.make ?maxResults ?nextToken
              ~applicationIdentifier ~environmentIdentifier ())
           (Some Values.ListRoutesResponse.to_json)
           (Some Values.ListRoutesResponse.error_to_json)])
let list_services =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and applicationIdentifier =
         flag "application-identifier" (required string)
           ~doc:"STRING ApplicationId"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_services
           (Values.ListServicesRequest.make ?maxResults ?nextToken
              ~applicationIdentifier ~environmentIdentifier ())
           (Some Values.ListServicesResponse.to_json)
           (Some Values.ListServicesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policy =
         flag "policy" (required string) ~doc:"STRING PolicyString"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_resource_policy
           (Values.PutResourcePolicyRequest.make ~policy ~resourceArn ())
           (Some Values.PutResourcePolicyResponse.to_json)
           (Some Values.PutResourcePolicyResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_route =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and activationState =
         flag "activation-state" (required json_arg)
           ~doc:"JSON RouteActivationState"
       and applicationIdentifier =
         flag "application-identifier" (required string)
           ~doc:"STRING ApplicationId"
       and environmentIdentifier =
         flag "environment-identifier" (required string)
           ~doc:"STRING EnvironmentId"
       and routeIdentifier =
         flag "route-identifier" (required string) ~doc:"STRING RouteId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_route
           (Values.UpdateRouteRequest.make
              ~activationState:(Values.RouteActivationState.of_json
                                  activationState) ~applicationIdentifier
              ~environmentIdentifier ~routeIdentifier ())
           (Some Values.UpdateRouteResponse.to_json)
           (Some Values.UpdateRouteResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-application", create_application);
    ("create-environment", create_environment);
    ("create-route", create_route);
    ("create-service", create_service);
    ("delete-application", delete_application);
    ("delete-environment", delete_environment);
    ("delete-resource-policy", delete_resource_policy);
    ("delete-route", delete_route);
    ("delete-service", delete_service);
    ("get-application", get_application);
    ("get-environment", get_environment);
    ("get-resource-policy", get_resource_policy);
    ("get-route", get_route);
    ("get-service", get_service);
    ("list-applications", list_applications);
    ("list-environment-vpcs", list_environment_vpcs);
    ("list-environments", list_environments);
    ("list-routes", list_routes);
    ("list-services", list_services);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-resource-policy", put_resource_policy);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-route", update_route)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_layer_version_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and organizationId =
         flag "organization-id" (optional string)
           ~doc:"STRING OrganizationId"
       and revisionId =
         flag "revision-id" (optional string) ~doc:"STRING String"
       and layerName =
         flag "layer-name" (required string) ~doc:"STRING LayerName"
       and versionNumber =
         flag "version-number" (required json_arg)
           ~doc:"JSON LayerVersionNumber"
       and statementId =
         flag "statement-id" (required string) ~doc:"STRING StatementId"
       and action =
         flag "action" (required string)
           ~doc:"STRING LayerPermissionAllowedAction"
       and principal =
         flag "principal" (required string)
           ~doc:"STRING LayerPermissionAllowedPrincipal" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_layer_version_permission
           (Values.AddLayerVersionPermissionRequest.make ?organizationId
              ?revisionId ~layerName
              ~versionNumber:(Values.LayerVersionNumber.of_json versionNumber)
              ~statementId ~action ~principal ())
           (Some Values.AddLayerVersionPermissionResponse.to_json)
           (Some Values.AddLayerVersionPermissionResponse.error_to_json)])
let add_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceArn = flag "source-arn" (optional string) ~doc:"STRING Arn"
       and sourceAccount =
         flag "source-account" (optional string) ~doc:"STRING SourceOwner"
       and eventSourceToken =
         flag "event-source-token" (optional string)
           ~doc:"STRING EventSourceToken"
       and qualifier =
         flag "qualifier" (optional string)
           ~doc:"STRING NumericLatestPublishedOrAliasQualifier"
       and revisionId =
         flag "revision-id" (optional string) ~doc:"STRING String"
       and principalOrgID =
         flag "principal-org-i-d" (optional string)
           ~doc:"STRING PrincipalOrgID"
       and functionUrlAuthType =
         flag "function-url-auth-type" (optional json_arg)
           ~doc:"JSON FunctionUrlAuthType"
       and invokedViaFunctionUrl =
         flag "invoked-via-function-url" (optional bool)
           ~doc:"BOOL InvokedViaFunctionUrl"
       and functionName =
         flag "function-name" (required string)
           ~doc:"STRING NamespacedFunctionName"
       and statementId =
         flag "statement-id" (required string) ~doc:"STRING StatementId"
       and action = flag "action" (required string) ~doc:"STRING Action"
       and principal =
         flag "principal" (required string) ~doc:"STRING Principal" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_permission
           (Values.AddPermissionRequest.make ?sourceArn ?sourceAccount
              ?eventSourceToken ?qualifier ?revisionId ?principalOrgID
              ?functionUrlAuthType:(Option.map
                                      ~f:Values.FunctionUrlAuthType.of_json
                                      functionUrlAuthType)
              ?invokedViaFunctionUrl ~functionName ~statementId ~action
              ~principal ()) (Some Values.AddPermissionResponse.to_json)
           (Some Values.AddPermissionResponse.error_to_json)])
let checkpoint_durable_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and updates =
         flag "updates" (optional json_arg) ~doc:"JSON OperationUpdates"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and durableExecutionArn =
         flag "durable-execution-arn" (required string)
           ~doc:"STRING DurableExecutionArn"
       and checkpointToken =
         flag "checkpoint-token" (required string)
           ~doc:"STRING CheckpointToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.checkpoint_durable_execution
           (Values.CheckpointDurableExecutionRequest.make
              ?updates:(Option.map ~f:Values.OperationUpdates.of_json updates)
              ?clientToken ~durableExecutionArn ~checkpointToken ())
           (Some Values.CheckpointDurableExecutionResponse.to_json)
           (Some Values.CheckpointDurableExecutionResponse.error_to_json)])
let create_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and routingConfig =
         flag "routing-config" (optional json_arg)
           ~doc:"JSON AliasRoutingConfiguration"
       and functionName =
         flag "function-name" (required string) ~doc:"STRING FunctionName"
       and name = flag "name" (required string) ~doc:"STRING Alias"
       and functionVersion =
         flag "function-version" (required string)
           ~doc:"STRING VersionWithLatestPublished" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_alias
           (Values.CreateAliasRequest.make ?description
              ?routingConfig:(Option.map
                                ~f:Values.AliasRoutingConfiguration.of_json
                                routingConfig) ~functionName ~name
              ~functionVersion ()) (Some Values.AliasConfiguration.to_json)
           (Some Values.AliasConfiguration.error_to_json)])
let create_capacity_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceRequirements =
         flag "instance-requirements" (optional json_arg)
           ~doc:"JSON InstanceRequirements"
       and capacityProviderScalingConfig =
         flag "capacity-provider-scaling-config" (optional json_arg)
           ~doc:"JSON CapacityProviderScalingConfig"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KMSKeyArnNonEmpty"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and capacityProviderName =
         flag "capacity-provider-name" (required string)
           ~doc:"STRING CapacityProviderName"
       and vpcConfig =
         flag "vpc-config" (required json_arg)
           ~doc:"JSON CapacityProviderVpcConfig"
       and permissionsConfig =
         flag "permissions-config" (required json_arg)
           ~doc:"JSON CapacityProviderPermissionsConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_capacity_provider
           (Values.CreateCapacityProviderRequest.make
              ?instanceRequirements:(Option.map
                                       ~f:Values.InstanceRequirements.of_json
                                       instanceRequirements)
              ?capacityProviderScalingConfig:(Option.map
                                                ~f:Values.CapacityProviderScalingConfig.of_json
                                                capacityProviderScalingConfig)
              ?kmsKeyArn ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~capacityProviderName
              ~vpcConfig:(Values.CapacityProviderVpcConfig.of_json vpcConfig)
              ~permissionsConfig:(Values.CapacityProviderPermissionsConfig.of_json
                                    permissionsConfig) ())
           (Some Values.CreateCapacityProviderResponse.to_json)
           (Some Values.CreateCapacityProviderResponse.error_to_json)])
let create_code_signing_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and codeSigningPolicies =
         flag "code-signing-policies" (optional json_arg)
           ~doc:"JSON CodeSigningPolicies"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and allowedPublishers =
         flag "allowed-publishers" (required json_arg)
           ~doc:"JSON AllowedPublishers" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_code_signing_config
           (Values.CreateCodeSigningConfigRequest.make ?description
              ?codeSigningPolicies:(Option.map
                                      ~f:Values.CodeSigningPolicies.of_json
                                      codeSigningPolicies)
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ~allowedPublishers:(Values.AllowedPublishers.of_json
                                    allowedPublishers) ())
           (Some Values.CreateCodeSigningConfigResponse.to_json)
           (Some Values.CreateCodeSigningConfigResponse.error_to_json)])
let create_event_source_mapping =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventSourceArn =
         flag "event-source-arn" (optional string) ~doc:"STRING Arn"
       and enabled = flag "enabled" (optional bool) ~doc:"BOOL Enabled"
       and batchSize = flag "batch-size" (optional int) ~doc:"INT BatchSize"
       and filterCriteria =
         flag "filter-criteria" (optional json_arg)
           ~doc:"JSON FilterCriteria"
       and maximumBatchingWindowInSeconds =
         flag "maximum-batching-window-in-seconds" (optional int)
           ~doc:"INT MaximumBatchingWindowInSeconds"
       and parallelizationFactor =
         flag "parallelization-factor" (optional int)
           ~doc:"INT ParallelizationFactor"
       and startingPosition =
         flag "starting-position" (optional json_arg)
           ~doc:"JSON EventSourcePosition"
       and startingPositionTimestamp =
         flag "starting-position-timestamp" (optional json_arg)
           ~doc:"JSON Date"
       and destinationConfig =
         flag "destination-config" (optional json_arg)
           ~doc:"JSON DestinationConfig"
       and maximumRecordAgeInSeconds =
         flag "maximum-record-age-in-seconds" (optional int)
           ~doc:"INT MaximumRecordAgeInSeconds"
       and bisectBatchOnFunctionError =
         flag "bisect-batch-on-function-error" (optional bool)
           ~doc:"BOOL BisectBatchOnFunctionError"
       and maximumRetryAttempts =
         flag "maximum-retry-attempts" (optional int)
           ~doc:"INT MaximumRetryAttemptsEventSourceMapping"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and tumblingWindowInSeconds =
         flag "tumbling-window-in-seconds" (optional int)
           ~doc:"INT TumblingWindowInSeconds"
       and topics = flag "topics" (optional json_arg) ~doc:"JSON Topics"
       and queues = flag "queues" (optional json_arg) ~doc:"JSON Queues"
       and sourceAccessConfigurations =
         flag "source-access-configurations" (optional json_arg)
           ~doc:"JSON SourceAccessConfigurations"
       and selfManagedEventSource =
         flag "self-managed-event-source" (optional json_arg)
           ~doc:"JSON SelfManagedEventSource"
       and functionResponseTypes =
         flag "function-response-types" (optional json_arg)
           ~doc:"JSON FunctionResponseTypeList"
       and amazonManagedKafkaEventSourceConfig =
         flag "amazon-managed-kafka-event-source-config" (optional json_arg)
           ~doc:"JSON AmazonManagedKafkaEventSourceConfig"
       and selfManagedKafkaEventSourceConfig =
         flag "self-managed-kafka-event-source-config" (optional json_arg)
           ~doc:"JSON SelfManagedKafkaEventSourceConfig"
       and scalingConfig =
         flag "scaling-config" (optional json_arg) ~doc:"JSON ScalingConfig"
       and documentDBEventSourceConfig =
         flag "document-d-b-event-source-config" (optional json_arg)
           ~doc:"JSON DocumentDBEventSourceConfig"
       and kMSKeyArn =
         flag "k-m-s-key-arn" (optional string) ~doc:"STRING KMSKeyArn"
       and metricsConfig =
         flag "metrics-config" (optional json_arg)
           ~doc:"JSON EventSourceMappingMetricsConfig"
       and loggingConfig =
         flag "logging-config" (optional json_arg)
           ~doc:"JSON EventSourceMappingLoggingConfig"
       and provisionedPollerConfig =
         flag "provisioned-poller-config" (optional json_arg)
           ~doc:"JSON ProvisionedPollerConfig"
       and functionName =
         flag "function-name" (required string)
           ~doc:"STRING NamespacedFunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_event_source_mapping
           (Values.CreateEventSourceMappingRequest.make ?eventSourceArn
              ?enabled ?batchSize
              ?filterCriteria:(Option.map ~f:Values.FilterCriteria.of_json
                                 filterCriteria)
              ?maximumBatchingWindowInSeconds ?parallelizationFactor
              ?startingPosition:(Option.map
                                   ~f:Values.EventSourcePosition.of_json
                                   startingPosition)
              ?startingPositionTimestamp:(Option.map ~f:Values.Date.of_json
                                            startingPositionTimestamp)
              ?destinationConfig:(Option.map
                                    ~f:Values.DestinationConfig.of_json
                                    destinationConfig)
              ?maximumRecordAgeInSeconds ?bisectBatchOnFunctionError
              ?maximumRetryAttempts
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?tumblingWindowInSeconds
              ?topics:(Option.map ~f:Values.Topics.of_json topics)
              ?queues:(Option.map ~f:Values.Queues.of_json queues)
              ?sourceAccessConfigurations:(Option.map
                                             ~f:Values.SourceAccessConfigurations.of_json
                                             sourceAccessConfigurations)
              ?selfManagedEventSource:(Option.map
                                         ~f:Values.SelfManagedEventSource.of_json
                                         selfManagedEventSource)
              ?functionResponseTypes:(Option.map
                                        ~f:Values.FunctionResponseTypeList.of_json
                                        functionResponseTypes)
              ?amazonManagedKafkaEventSourceConfig:(Option.map
                                                      ~f:Values.AmazonManagedKafkaEventSourceConfig.of_json
                                                      amazonManagedKafkaEventSourceConfig)
              ?selfManagedKafkaEventSourceConfig:(Option.map
                                                    ~f:Values.SelfManagedKafkaEventSourceConfig.of_json
                                                    selfManagedKafkaEventSourceConfig)
              ?scalingConfig:(Option.map ~f:Values.ScalingConfig.of_json
                                scalingConfig)
              ?documentDBEventSourceConfig:(Option.map
                                              ~f:Values.DocumentDBEventSourceConfig.of_json
                                              documentDBEventSourceConfig)
              ?kMSKeyArn
              ?metricsConfig:(Option.map
                                ~f:Values.EventSourceMappingMetricsConfig.of_json
                                metricsConfig)
              ?loggingConfig:(Option.map
                                ~f:Values.EventSourceMappingLoggingConfig.of_json
                                loggingConfig)
              ?provisionedPollerConfig:(Option.map
                                          ~f:Values.ProvisionedPollerConfig.of_json
                                          provisionedPollerConfig)
              ~functionName ())
           (Some Values.EventSourceMappingConfiguration.to_json)
           (Some Values.EventSourceMappingConfiguration.error_to_json)])
let create_function =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and runtime = flag "runtime" (optional json_arg) ~doc:"JSON Runtime"
       and handler = flag "handler" (optional string) ~doc:"STRING Handler"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and timeout = flag "timeout" (optional int) ~doc:"INT Timeout"
       and memorySize =
         flag "memory-size" (optional int) ~doc:"INT MemorySize"
       and publish = flag "publish" (optional bool) ~doc:"BOOL Boolean"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON VpcConfig"
       and packageType =
         flag "package-type" (optional json_arg) ~doc:"JSON PackageType"
       and deadLetterConfig =
         flag "dead-letter-config" (optional json_arg)
           ~doc:"JSON DeadLetterConfig"
       and environment =
         flag "environment" (optional json_arg) ~doc:"JSON Environment"
       and kMSKeyArn =
         flag "k-m-s-key-arn" (optional string) ~doc:"STRING KMSKeyArn"
       and tracingConfig =
         flag "tracing-config" (optional json_arg) ~doc:"JSON TracingConfig"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and layers = flag "layers" (optional json_arg) ~doc:"JSON LayerList"
       and fileSystemConfigs =
         flag "file-system-configs" (optional json_arg)
           ~doc:"JSON FileSystemConfigList"
       and imageConfig =
         flag "image-config" (optional json_arg) ~doc:"JSON ImageConfig"
       and codeSigningConfigArn =
         flag "code-signing-config-arn" (optional string)
           ~doc:"STRING CodeSigningConfigArn"
       and architectures =
         flag "architectures" (optional json_arg)
           ~doc:"JSON ArchitecturesList"
       and ephemeralStorage =
         flag "ephemeral-storage" (optional json_arg)
           ~doc:"JSON EphemeralStorage"
       and snapStart =
         flag "snap-start" (optional json_arg) ~doc:"JSON SnapStart"
       and loggingConfig =
         flag "logging-config" (optional json_arg) ~doc:"JSON LoggingConfig"
       and capacityProviderConfig =
         flag "capacity-provider-config" (optional json_arg)
           ~doc:"JSON CapacityProviderConfig"
       and publishTo =
         flag "publish-to" (optional json_arg)
           ~doc:"JSON FunctionVersionLatestPublished"
       and durableConfig =
         flag "durable-config" (optional json_arg) ~doc:"JSON DurableConfig"
       and tenancyConfig =
         flag "tenancy-config" (optional json_arg) ~doc:"JSON TenancyConfig"
       and functionName =
         flag "function-name" (required string) ~doc:"STRING FunctionName"
       and role = flag "role" (required string) ~doc:"STRING RoleArn"
       and code = flag "code" (required json_arg) ~doc:"JSON FunctionCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_function
           (Values.CreateFunctionRequest.make
              ?runtime:(Option.map ~f:Values.Runtime.of_json runtime)
              ?handler ?description ?timeout ?memorySize ?publish
              ?vpcConfig:(Option.map ~f:Values.VpcConfig.of_json vpcConfig)
              ?packageType:(Option.map ~f:Values.PackageType.of_json
                              packageType)
              ?deadLetterConfig:(Option.map
                                   ~f:Values.DeadLetterConfig.of_json
                                   deadLetterConfig)
              ?environment:(Option.map ~f:Values.Environment.of_json
                              environment) ?kMSKeyArn
              ?tracingConfig:(Option.map ~f:Values.TracingConfig.of_json
                                tracingConfig)
              ?tags:(Option.map ~f:Values.Tags.of_json tags)
              ?layers:(Option.map ~f:Values.LayerList.of_json layers)
              ?fileSystemConfigs:(Option.map
                                    ~f:Values.FileSystemConfigList.of_json
                                    fileSystemConfigs)
              ?imageConfig:(Option.map ~f:Values.ImageConfig.of_json
                              imageConfig) ?codeSigningConfigArn
              ?architectures:(Option.map ~f:Values.ArchitecturesList.of_json
                                architectures)
              ?ephemeralStorage:(Option.map
                                   ~f:Values.EphemeralStorage.of_json
                                   ephemeralStorage)
              ?snapStart:(Option.map ~f:Values.SnapStart.of_json snapStart)
              ?loggingConfig:(Option.map ~f:Values.LoggingConfig.of_json
                                loggingConfig)
              ?capacityProviderConfig:(Option.map
                                         ~f:Values.CapacityProviderConfig.of_json
                                         capacityProviderConfig)
              ?publishTo:(Option.map
                            ~f:Values.FunctionVersionLatestPublished.of_json
                            publishTo)
              ?durableConfig:(Option.map ~f:Values.DurableConfig.of_json
                                durableConfig)
              ?tenancyConfig:(Option.map ~f:Values.TenancyConfig.of_json
                                tenancyConfig) ~functionName ~role
              ~code:(Values.FunctionCode.of_json code) ())
           (Some Values.FunctionConfiguration.to_json)
           (Some Values.FunctionConfiguration.error_to_json)])
let create_function_url_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and qualifier =
         flag "qualifier" (optional string)
           ~doc:"STRING FunctionUrlQualifier"
       and cors = flag "cors" (optional json_arg) ~doc:"JSON Cors"
       and invokeMode =
         flag "invoke-mode" (optional json_arg) ~doc:"JSON InvokeMode"
       and functionName =
         flag "function-name" (required string) ~doc:"STRING FunctionName"
       and authType =
         flag "auth-type" (required json_arg) ~doc:"JSON FunctionUrlAuthType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_function_url_config
           (Values.CreateFunctionUrlConfigRequest.make ?qualifier
              ?cors:(Option.map ~f:Values.Cors.of_json cors)
              ?invokeMode:(Option.map ~f:Values.InvokeMode.of_json invokeMode)
              ~functionName
              ~authType:(Values.FunctionUrlAuthType.of_json authType) ())
           (Some Values.CreateFunctionUrlConfigResponse.to_json)
           (Some Values.CreateFunctionUrlConfigResponse.error_to_json)])
let delete_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and functionName =
         flag "function-name" (required string) ~doc:"STRING FunctionName"
       and name = flag "name" (required string) ~doc:"STRING Alias" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_alias
           (Values.DeleteAliasRequest.make ~functionName ~name ()) None None])
let delete_capacity_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and capacityProviderName =
         flag "capacity-provider-name" (required string)
           ~doc:"STRING CapacityProviderName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_capacity_provider
           (Values.DeleteCapacityProviderRequest.make ~capacityProviderName
              ()) (Some Values.DeleteCapacityProviderResponse.to_json)
           (Some Values.DeleteCapacityProviderResponse.error_to_json)])
let delete_code_signing_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and codeSigningConfigArn =
         flag "code-signing-config-arn" (required string)
           ~doc:"STRING CodeSigningConfigArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_code_signing_config
           (Values.DeleteCodeSigningConfigRequest.make ~codeSigningConfigArn
              ()) (Some Values.DeleteCodeSigningConfigResponse.to_json)
           (Some Values.DeleteCodeSigningConfigResponse.error_to_json)])
let delete_event_source_mapping =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and uUID = flag "u-u-i-d" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_event_source_mapping
           (Values.DeleteEventSourceMappingRequest.make ~uUID ())
           (Some Values.EventSourceMappingConfiguration.to_json)
           (Some Values.EventSourceMappingConfiguration.error_to_json)])
let delete_function =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and qualifier =
         flag "qualifier" (optional string)
           ~doc:"STRING NumericLatestPublishedOrAliasQualifier"
       and functionName =
         flag "function-name" (required string)
           ~doc:"STRING NamespacedFunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_function
           (Values.DeleteFunctionRequest.make ?qualifier ~functionName ())
           (Some Values.DeleteFunctionResponse.to_json)
           (Some Values.DeleteFunctionResponse.error_to_json)])
let delete_function_code_signing_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and functionName =
         flag "function-name" (required string)
           ~doc:"STRING NamespacedFunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_function_code_signing_config
           (Values.DeleteFunctionCodeSigningConfigRequest.make ~functionName
              ()) None None])
let delete_function_concurrency =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and functionName =
         flag "function-name" (required string) ~doc:"STRING FunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_function_concurrency
           (Values.DeleteFunctionConcurrencyRequest.make ~functionName ())
           None None])
let delete_function_event_invoke_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and qualifier =
         flag "qualifier" (optional string)
           ~doc:"STRING NumericLatestPublishedOrAliasQualifier"
       and functionName =
         flag "function-name" (required string)
           ~doc:"STRING NamespacedFunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_function_event_invoke_config
           (Values.DeleteFunctionEventInvokeConfigRequest.make ?qualifier
              ~functionName ()) None None])
let delete_function_url_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and qualifier =
         flag "qualifier" (optional string)
           ~doc:"STRING FunctionUrlQualifier"
       and functionName =
         flag "function-name" (required string) ~doc:"STRING FunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_function_url_config
           (Values.DeleteFunctionUrlConfigRequest.make ?qualifier
              ~functionName ()) None None])
let delete_layer_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and layerName =
         flag "layer-name" (required string) ~doc:"STRING LayerName"
       and versionNumber =
         flag "version-number" (required json_arg)
           ~doc:"JSON LayerVersionNumber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_layer_version
           (Values.DeleteLayerVersionRequest.make ~layerName
              ~versionNumber:(Values.LayerVersionNumber.of_json versionNumber)
              ()) None None])
let delete_provisioned_concurrency_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and functionName =
         flag "function-name" (required string) ~doc:"STRING FunctionName"
       and qualifier =
         flag "qualifier" (required string) ~doc:"STRING Qualifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_provisioned_concurrency_config
           (Values.DeleteProvisionedConcurrencyConfigRequest.make
              ~functionName ~qualifier ()) None None])
let get_account_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_account_settings (Values.GetAccountSettingsRequest.make ())
           (Some Values.GetAccountSettingsResponse.to_json)
           (Some Values.GetAccountSettingsResponse.error_to_json)])
let get_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and functionName =
         flag "function-name" (required string) ~doc:"STRING FunctionName"
       and name = flag "name" (required string) ~doc:"STRING Alias" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_alias (Values.GetAliasRequest.make ~functionName ~name ())
           (Some Values.AliasConfiguration.to_json)
           (Some Values.AliasConfiguration.error_to_json)])
let get_capacity_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and capacityProviderName =
         flag "capacity-provider-name" (required string)
           ~doc:"STRING CapacityProviderName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_capacity_provider
           (Values.GetCapacityProviderRequest.make ~capacityProviderName ())
           (Some Values.GetCapacityProviderResponse.to_json)
           (Some Values.GetCapacityProviderResponse.error_to_json)])
let get_code_signing_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and codeSigningConfigArn =
         flag "code-signing-config-arn" (required string)
           ~doc:"STRING CodeSigningConfigArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_code_signing_config
           (Values.GetCodeSigningConfigRequest.make ~codeSigningConfigArn ())
           (Some Values.GetCodeSigningConfigResponse.to_json)
           (Some Values.GetCodeSigningConfigResponse.error_to_json)])
let get_durable_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and durableExecutionArn =
         flag "durable-execution-arn" (required string)
           ~doc:"STRING DurableExecutionArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_durable_execution
           (Values.GetDurableExecutionRequest.make ~durableExecutionArn ())
           (Some Values.GetDurableExecutionResponse.to_json)
           (Some Values.GetDurableExecutionResponse.error_to_json)])
let get_durable_execution_history =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeExecutionData =
         flag "include-execution-data" (optional bool)
           ~doc:"BOOL IncludeExecutionData"
       and maxItems = flag "max-items" (optional int) ~doc:"INT ItemCount"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and reverseOrder =
         flag "reverse-order" (optional bool) ~doc:"BOOL ReverseOrder"
       and durableExecutionArn =
         flag "durable-execution-arn" (required string)
           ~doc:"STRING DurableExecutionArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_durable_execution_history
           (Values.GetDurableExecutionHistoryRequest.make
              ?includeExecutionData ?maxItems ?marker ?reverseOrder
              ~durableExecutionArn ())
           (Some Values.GetDurableExecutionHistoryResponse.to_json)
           (Some Values.GetDurableExecutionHistoryResponse.error_to_json)])
let get_durable_execution_state =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxItems = flag "max-items" (optional int) ~doc:"INT ItemCount"
       and durableExecutionArn =
         flag "durable-execution-arn" (required string)
           ~doc:"STRING DurableExecutionArn"
       and checkpointToken =
         flag "checkpoint-token" (required string)
           ~doc:"STRING CheckpointToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_durable_execution_state
           (Values.GetDurableExecutionStateRequest.make ?marker ?maxItems
              ~durableExecutionArn ~checkpointToken ())
           (Some Values.GetDurableExecutionStateResponse.to_json)
           (Some Values.GetDurableExecutionStateResponse.error_to_json)])
let get_event_source_mapping =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and uUID = flag "u-u-i-d" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_event_source_mapping
           (Values.GetEventSourceMappingRequest.make ~uUID ())
           (Some Values.EventSourceMappingConfiguration.to_json)
           (Some Values.EventSourceMappingConfiguration.error_to_json)])
let get_function =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and qualifier =
         flag "qualifier" (optional string)
           ~doc:"STRING NumericLatestPublishedOrAliasQualifier"
       and functionName =
         flag "function-name" (required string)
           ~doc:"STRING NamespacedFunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_function
           (Values.GetFunctionRequest.make ?qualifier ~functionName ())
           (Some Values.GetFunctionResponse.to_json)
           (Some Values.GetFunctionResponse.error_to_json)])
let get_function_code_signing_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and functionName =
         flag "function-name" (required string)
           ~doc:"STRING NamespacedFunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_function_code_signing_config
           (Values.GetFunctionCodeSigningConfigRequest.make ~functionName ())
           (Some Values.GetFunctionCodeSigningConfigResponse.to_json)
           (Some Values.GetFunctionCodeSigningConfigResponse.error_to_json)])
let get_function_concurrency =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and functionName =
         flag "function-name" (required string) ~doc:"STRING FunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_function_concurrency
           (Values.GetFunctionConcurrencyRequest.make ~functionName ())
           (Some Values.GetFunctionConcurrencyResponse.to_json)
           (Some Values.GetFunctionConcurrencyResponse.error_to_json)])
let get_function_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and qualifier =
         flag "qualifier" (optional string)
           ~doc:"STRING NumericLatestPublishedOrAliasQualifier"
       and functionName =
         flag "function-name" (required string)
           ~doc:"STRING NamespacedFunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_function_configuration
           (Values.GetFunctionConfigurationRequest.make ?qualifier
              ~functionName ()) (Some Values.FunctionConfiguration.to_json)
           (Some Values.FunctionConfiguration.error_to_json)])
let get_function_event_invoke_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and qualifier =
         flag "qualifier" (optional string)
           ~doc:"STRING NumericLatestPublishedOrAliasQualifier"
       and functionName =
         flag "function-name" (required string)
           ~doc:"STRING NamespacedFunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_function_event_invoke_config
           (Values.GetFunctionEventInvokeConfigRequest.make ?qualifier
              ~functionName ())
           (Some Values.FunctionEventInvokeConfig.to_json)
           (Some Values.FunctionEventInvokeConfig.error_to_json)])
let get_function_recursion_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and functionName =
         flag "function-name" (required string)
           ~doc:"STRING UnqualifiedFunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_function_recursion_config
           (Values.GetFunctionRecursionConfigRequest.make ~functionName ())
           (Some Values.GetFunctionRecursionConfigResponse.to_json)
           (Some Values.GetFunctionRecursionConfigResponse.error_to_json)])
let get_function_scaling_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and functionName =
         flag "function-name" (required string)
           ~doc:"STRING UnqualifiedFunctionName"
       and qualifier =
         flag "qualifier" (required string)
           ~doc:"STRING PublishedFunctionQualifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_function_scaling_config
           (Values.GetFunctionScalingConfigRequest.make ~functionName
              ~qualifier ())
           (Some Values.GetFunctionScalingConfigResponse.to_json)
           (Some Values.GetFunctionScalingConfigResponse.error_to_json)])
let get_function_url_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and qualifier =
         flag "qualifier" (optional string)
           ~doc:"STRING FunctionUrlQualifier"
       and functionName =
         flag "function-name" (required string) ~doc:"STRING FunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_function_url_config
           (Values.GetFunctionUrlConfigRequest.make ?qualifier ~functionName
              ()) (Some Values.GetFunctionUrlConfigResponse.to_json)
           (Some Values.GetFunctionUrlConfigResponse.error_to_json)])
let get_layer_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and layerName =
         flag "layer-name" (required string) ~doc:"STRING LayerName"
       and versionNumber =
         flag "version-number" (required json_arg)
           ~doc:"JSON LayerVersionNumber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_layer_version
           (Values.GetLayerVersionRequest.make ~layerName
              ~versionNumber:(Values.LayerVersionNumber.of_json versionNumber)
              ()) (Some Values.GetLayerVersionResponse.to_json)
           (Some Values.GetLayerVersionResponse.error_to_json)])
let get_layer_version_by_arn =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING LayerVersionArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_layer_version_by_arn
           (Values.GetLayerVersionByArnRequest.make ~arn ())
           (Some Values.GetLayerVersionResponse.to_json)
           (Some Values.GetLayerVersionResponse.error_to_json)])
let get_layer_version_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and layerName =
         flag "layer-name" (required string) ~doc:"STRING LayerName"
       and versionNumber =
         flag "version-number" (required json_arg)
           ~doc:"JSON LayerVersionNumber" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_layer_version_policy
           (Values.GetLayerVersionPolicyRequest.make ~layerName
              ~versionNumber:(Values.LayerVersionNumber.of_json versionNumber)
              ()) (Some Values.GetLayerVersionPolicyResponse.to_json)
           (Some Values.GetLayerVersionPolicyResponse.error_to_json)])
let get_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and qualifier =
         flag "qualifier" (optional string)
           ~doc:"STRING NumericLatestPublishedOrAliasQualifier"
       and functionName =
         flag "function-name" (required string)
           ~doc:"STRING NamespacedFunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_policy
           (Values.GetPolicyRequest.make ?qualifier ~functionName ())
           (Some Values.GetPolicyResponse.to_json)
           (Some Values.GetPolicyResponse.error_to_json)])
let get_provisioned_concurrency_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and functionName =
         flag "function-name" (required string) ~doc:"STRING FunctionName"
       and qualifier =
         flag "qualifier" (required string) ~doc:"STRING Qualifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_provisioned_concurrency_config
           (Values.GetProvisionedConcurrencyConfigRequest.make ~functionName
              ~qualifier ())
           (Some Values.GetProvisionedConcurrencyConfigResponse.to_json)
           (Some Values.GetProvisionedConcurrencyConfigResponse.error_to_json)])
let get_runtime_management_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and qualifier =
         flag "qualifier" (optional string)
           ~doc:"STRING NumericLatestPublishedOrAliasQualifier"
       and functionName =
         flag "function-name" (required string)
           ~doc:"STRING NamespacedFunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_runtime_management_config
           (Values.GetRuntimeManagementConfigRequest.make ?qualifier
              ~functionName ())
           (Some Values.GetRuntimeManagementConfigResponse.to_json)
           (Some Values.GetRuntimeManagementConfigResponse.error_to_json)])
let invoke =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and invocationType =
         flag "invocation-type" (optional json_arg)
           ~doc:"JSON InvocationType"
       and logType = flag "log-type" (optional json_arg) ~doc:"JSON LogType"
       and clientContext =
         flag "client-context" (optional string) ~doc:"STRING String"
       and durableExecutionName =
         flag "durable-execution-name" (optional string)
           ~doc:"STRING DurableExecutionName"
       and payload = flag "payload" (optional json_arg) ~doc:"JSON Blob"
       and qualifier =
         flag "qualifier" (optional string)
           ~doc:"STRING NumericLatestPublishedOrAliasQualifier"
       and tenantId =
         flag "tenant-id" (optional string) ~doc:"STRING TenantId"
       and functionName =
         flag "function-name" (required string)
           ~doc:"STRING NamespacedFunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region Io.invoke
           (Values.InvocationRequest.make
              ?invocationType:(Option.map ~f:Values.InvocationType.of_json
                                 invocationType)
              ?logType:(Option.map ~f:Values.LogType.of_json logType)
              ?clientContext ?durableExecutionName
              ?payload:(Option.map ~f:Values.Blob.of_json payload) ?qualifier
              ?tenantId ~functionName ())
           (Some Values.InvocationResponse.to_json)
           (Some Values.InvocationResponse.error_to_json)])
let invoke_async =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and functionName =
         flag "function-name" (required string)
           ~doc:"STRING NamespacedFunctionName"
       and invokeArgs =
         flag "invoke-args" (required json_arg) ~doc:"JSON BlobStream" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.invoke_async
           (Values.InvokeAsyncRequest.make ~functionName
              ~invokeArgs:(Values.BlobStream.of_json invokeArgs) ())
           (Some Values.InvokeAsyncResponse.to_json)
           (Some Values.InvokeAsyncResponse.error_to_json)])
let invoke_with_response_stream =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and invocationType =
         flag "invocation-type" (optional json_arg)
           ~doc:"JSON ResponseStreamingInvocationType"
       and logType = flag "log-type" (optional json_arg) ~doc:"JSON LogType"
       and clientContext =
         flag "client-context" (optional string) ~doc:"STRING String"
       and qualifier =
         flag "qualifier" (optional string)
           ~doc:"STRING NumericLatestPublishedOrAliasQualifier"
       and payload = flag "payload" (optional json_arg) ~doc:"JSON Blob"
       and tenantId =
         flag "tenant-id" (optional string) ~doc:"STRING TenantId"
       and functionName =
         flag "function-name" (required string)
           ~doc:"STRING NamespacedFunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.invoke_with_response_stream
           (Values.InvokeWithResponseStreamRequest.make
              ?invocationType:(Option.map
                                 ~f:Values.ResponseStreamingInvocationType.of_json
                                 invocationType)
              ?logType:(Option.map ~f:Values.LogType.of_json logType)
              ?clientContext ?qualifier
              ?payload:(Option.map ~f:Values.Blob.of_json payload) ?tenantId
              ~functionName ())
           (Some Values.InvokeWithResponseStreamResponse.to_json)
           (Some Values.InvokeWithResponseStreamResponse.error_to_json)])
let list_aliases =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and functionVersion =
         flag "function-version" (optional string)
           ~doc:"STRING VersionWithLatestPublished"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxItems = flag "max-items" (optional int) ~doc:"INT MaxListItems"
       and functionName =
         flag "function-name" (required string) ~doc:"STRING FunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_aliases
           (Values.ListAliasesRequest.make ?functionVersion ?marker ?maxItems
              ~functionName ()) (Some Values.ListAliasesResponse.to_json)
           (Some Values.ListAliasesResponse.error_to_json)])
let list_capacity_providers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and state =
         flag "state" (optional json_arg) ~doc:"JSON CapacityProviderState"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxItems =
         flag "max-items" (optional int) ~doc:"INT MaxFiftyListItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_capacity_providers
           (Values.ListCapacityProvidersRequest.make
              ?state:(Option.map ~f:Values.CapacityProviderState.of_json
                        state) ?marker ?maxItems ())
           (Some Values.ListCapacityProvidersResponse.to_json)
           (Some Values.ListCapacityProvidersResponse.error_to_json)])
let list_code_signing_configs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxItems = flag "max-items" (optional int) ~doc:"INT MaxListItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_code_signing_configs
           (Values.ListCodeSigningConfigsRequest.make ?marker ?maxItems ())
           (Some Values.ListCodeSigningConfigsResponse.to_json)
           (Some Values.ListCodeSigningConfigsResponse.error_to_json)])
let list_durable_executions_by_function =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and qualifier =
         flag "qualifier" (optional string)
           ~doc:"STRING NumericLatestPublishedOrAliasQualifier"
       and durableExecutionName =
         flag "durable-execution-name" (optional string)
           ~doc:"STRING DurableExecutionName"
       and statuses =
         flag "statuses" (optional json_arg) ~doc:"JSON ExecutionStatusList"
       and startedAfter =
         flag "started-after" (optional json_arg)
           ~doc:"JSON ExecutionTimestamp"
       and startedBefore =
         flag "started-before" (optional json_arg)
           ~doc:"JSON ExecutionTimestamp"
       and reverseOrder =
         flag "reverse-order" (optional bool) ~doc:"BOOL ReverseOrder"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxItems = flag "max-items" (optional int) ~doc:"INT ItemCount"
       and functionName =
         flag "function-name" (required string)
           ~doc:"STRING NamespacedFunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_durable_executions_by_function
           (Values.ListDurableExecutionsByFunctionRequest.make ?qualifier
              ?durableExecutionName
              ?statuses:(Option.map ~f:Values.ExecutionStatusList.of_json
                           statuses)
              ?startedAfter:(Option.map ~f:Values.ExecutionTimestamp.of_json
                               startedAfter)
              ?startedBefore:(Option.map ~f:Values.ExecutionTimestamp.of_json
                                startedBefore) ?reverseOrder ?marker
              ?maxItems ~functionName ())
           (Some Values.ListDurableExecutionsByFunctionResponse.to_json)
           (Some Values.ListDurableExecutionsByFunctionResponse.error_to_json)])
let list_event_source_mappings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and eventSourceArn =
         flag "event-source-arn" (optional string) ~doc:"STRING Arn"
       and functionName =
         flag "function-name" (optional string)
           ~doc:"STRING NamespacedFunctionName"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxItems = flag "max-items" (optional int) ~doc:"INT MaxListItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_event_source_mappings
           (Values.ListEventSourceMappingsRequest.make ?eventSourceArn
              ?functionName ?marker ?maxItems ())
           (Some Values.ListEventSourceMappingsResponse.to_json)
           (Some Values.ListEventSourceMappingsResponse.error_to_json)])
let list_function_event_invoke_configs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxItems =
         flag "max-items" (optional int)
           ~doc:"INT MaxFunctionEventInvokeConfigListItems"
       and functionName =
         flag "function-name" (required string)
           ~doc:"STRING NamespacedFunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_function_event_invoke_configs
           (Values.ListFunctionEventInvokeConfigsRequest.make ?marker
              ?maxItems ~functionName ())
           (Some Values.ListFunctionEventInvokeConfigsResponse.to_json)
           (Some Values.ListFunctionEventInvokeConfigsResponse.error_to_json)])
let list_function_url_configs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxItems = flag "max-items" (optional int) ~doc:"INT MaxItems"
       and functionName =
         flag "function-name" (required string) ~doc:"STRING FunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_function_url_configs
           (Values.ListFunctionUrlConfigsRequest.make ?marker ?maxItems
              ~functionName ())
           (Some Values.ListFunctionUrlConfigsResponse.to_json)
           (Some Values.ListFunctionUrlConfigsResponse.error_to_json)])
let list_function_versions_by_capacity_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxItems =
         flag "max-items" (optional int) ~doc:"INT MaxFiftyListItems"
       and capacityProviderName =
         flag "capacity-provider-name" (required string)
           ~doc:"STRING CapacityProviderName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_function_versions_by_capacity_provider
           (Values.ListFunctionVersionsByCapacityProviderRequest.make ?marker
              ?maxItems ~capacityProviderName ())
           (Some
              Values.ListFunctionVersionsByCapacityProviderResponse.to_json)
           (Some
              Values.ListFunctionVersionsByCapacityProviderResponse.error_to_json)])
let list_functions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and masterRegion =
         flag "master-region" (optional string) ~doc:"STRING MasterRegion"
       and functionVersion =
         flag "function-version" (optional json_arg)
           ~doc:"JSON FunctionVersion"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxItems = flag "max-items" (optional int) ~doc:"INT MaxListItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_functions
           (Values.ListFunctionsRequest.make ?masterRegion
              ?functionVersion:(Option.map ~f:Values.FunctionVersion.of_json
                                  functionVersion) ?marker ?maxItems ())
           (Some Values.ListFunctionsResponse.to_json)
           (Some Values.ListFunctionsResponse.error_to_json)])
let list_functions_by_code_signing_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxItems = flag "max-items" (optional int) ~doc:"INT MaxListItems"
       and codeSigningConfigArn =
         flag "code-signing-config-arn" (required string)
           ~doc:"STRING CodeSigningConfigArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_functions_by_code_signing_config
           (Values.ListFunctionsByCodeSigningConfigRequest.make ?marker
              ?maxItems ~codeSigningConfigArn ())
           (Some Values.ListFunctionsByCodeSigningConfigResponse.to_json)
           (Some
              Values.ListFunctionsByCodeSigningConfigResponse.error_to_json)])
let list_layer_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and compatibleRuntime =
         flag "compatible-runtime" (optional json_arg) ~doc:"JSON Runtime"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxItems =
         flag "max-items" (optional int) ~doc:"INT MaxLayerListItems"
       and compatibleArchitecture =
         flag "compatible-architecture" (optional json_arg)
           ~doc:"JSON Architecture"
       and layerName =
         flag "layer-name" (required string) ~doc:"STRING LayerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_layer_versions
           (Values.ListLayerVersionsRequest.make
              ?compatibleRuntime:(Option.map ~f:Values.Runtime.of_json
                                    compatibleRuntime) ?marker ?maxItems
              ?compatibleArchitecture:(Option.map
                                         ~f:Values.Architecture.of_json
                                         compatibleArchitecture) ~layerName
              ()) (Some Values.ListLayerVersionsResponse.to_json)
           (Some Values.ListLayerVersionsResponse.error_to_json)])
let list_layers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and compatibleRuntime =
         flag "compatible-runtime" (optional json_arg) ~doc:"JSON Runtime"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxItems =
         flag "max-items" (optional int) ~doc:"INT MaxLayerListItems"
       and compatibleArchitecture =
         flag "compatible-architecture" (optional json_arg)
           ~doc:"JSON Architecture" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_layers
           (Values.ListLayersRequest.make
              ?compatibleRuntime:(Option.map ~f:Values.Runtime.of_json
                                    compatibleRuntime) ?marker ?maxItems
              ?compatibleArchitecture:(Option.map
                                         ~f:Values.Architecture.of_json
                                         compatibleArchitecture) ())
           (Some Values.ListLayersResponse.to_json)
           (Some Values.ListLayersResponse.error_to_json)])
let list_provisioned_concurrency_configs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxItems =
         flag "max-items" (optional int)
           ~doc:"INT MaxProvisionedConcurrencyConfigListItems"
       and functionName =
         flag "function-name" (required string) ~doc:"STRING FunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_provisioned_concurrency_configs
           (Values.ListProvisionedConcurrencyConfigsRequest.make ?marker
              ?maxItems ~functionName ())
           (Some Values.ListProvisionedConcurrencyConfigsResponse.to_json)
           (Some
              Values.ListProvisionedConcurrencyConfigsResponse.error_to_json)])
let list_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resource =
         flag "resource" (required string) ~doc:"STRING TaggableResource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags (Values.ListTagsRequest.make ~resource ())
           (Some Values.ListTagsResponse.to_json)
           (Some Values.ListTagsResponse.error_to_json)])
let list_versions_by_function =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING String"
       and maxItems = flag "max-items" (optional int) ~doc:"INT MaxListItems"
       and functionName =
         flag "function-name" (required string)
           ~doc:"STRING NamespacedFunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_versions_by_function
           (Values.ListVersionsByFunctionRequest.make ?marker ?maxItems
              ~functionName ())
           (Some Values.ListVersionsByFunctionResponse.to_json)
           (Some Values.ListVersionsByFunctionResponse.error_to_json)])
let publish_layer_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and compatibleRuntimes =
         flag "compatible-runtimes" (optional json_arg)
           ~doc:"JSON CompatibleRuntimes"
       and licenseInfo =
         flag "license-info" (optional string) ~doc:"STRING LicenseInfo"
       and compatibleArchitectures =
         flag "compatible-architectures" (optional json_arg)
           ~doc:"JSON CompatibleArchitectures"
       and layerName =
         flag "layer-name" (required string) ~doc:"STRING LayerName"
       and content =
         flag "content" (required json_arg)
           ~doc:"JSON LayerVersionContentInput" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.publish_layer_version
           (Values.PublishLayerVersionRequest.make ?description
              ?compatibleRuntimes:(Option.map
                                     ~f:Values.CompatibleRuntimes.of_json
                                     compatibleRuntimes) ?licenseInfo
              ?compatibleArchitectures:(Option.map
                                          ~f:Values.CompatibleArchitectures.of_json
                                          compatibleArchitectures) ~layerName
              ~content:(Values.LayerVersionContentInput.of_json content) ())
           (Some Values.PublishLayerVersionResponse.to_json)
           (Some Values.PublishLayerVersionResponse.error_to_json)])
let publish_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and codeSha256 =
         flag "code-sha256" (optional string) ~doc:"STRING String"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and revisionId =
         flag "revision-id" (optional string) ~doc:"STRING String"
       and publishTo =
         flag "publish-to" (optional json_arg)
           ~doc:"JSON FunctionVersionLatestPublished"
       and functionName =
         flag "function-name" (required string) ~doc:"STRING FunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.publish_version
           (Values.PublishVersionRequest.make ?codeSha256 ?description
              ?revisionId
              ?publishTo:(Option.map
                            ~f:Values.FunctionVersionLatestPublished.of_json
                            publishTo) ~functionName ())
           (Some Values.FunctionConfiguration.to_json)
           (Some Values.FunctionConfiguration.error_to_json)])
let put_function_code_signing_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and codeSigningConfigArn =
         flag "code-signing-config-arn" (required string)
           ~doc:"STRING CodeSigningConfigArn"
       and functionName =
         flag "function-name" (required string)
           ~doc:"STRING NamespacedFunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_function_code_signing_config
           (Values.PutFunctionCodeSigningConfigRequest.make
              ~codeSigningConfigArn ~functionName ())
           (Some Values.PutFunctionCodeSigningConfigResponse.to_json)
           (Some Values.PutFunctionCodeSigningConfigResponse.error_to_json)])
let put_function_concurrency =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and functionName =
         flag "function-name" (required string) ~doc:"STRING FunctionName"
       and reservedConcurrentExecutions =
         flag "reserved-concurrent-executions" (required int)
           ~doc:"INT ReservedConcurrentExecutions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_function_concurrency
           (Values.PutFunctionConcurrencyRequest.make ~functionName
              ~reservedConcurrentExecutions ())
           (Some Values.Concurrency.to_json)
           (Some Values.Concurrency.error_to_json)])
let put_function_event_invoke_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and qualifier =
         flag "qualifier" (optional string)
           ~doc:"STRING NumericLatestPublishedOrAliasQualifier"
       and maximumRetryAttempts =
         flag "maximum-retry-attempts" (optional int)
           ~doc:"INT MaximumRetryAttempts"
       and maximumEventAgeInSeconds =
         flag "maximum-event-age-in-seconds" (optional int)
           ~doc:"INT MaximumEventAgeInSeconds"
       and destinationConfig =
         flag "destination-config" (optional json_arg)
           ~doc:"JSON DestinationConfig"
       and functionName =
         flag "function-name" (required string)
           ~doc:"STRING NamespacedFunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_function_event_invoke_config
           (Values.PutFunctionEventInvokeConfigRequest.make ?qualifier
              ?maximumRetryAttempts ?maximumEventAgeInSeconds
              ?destinationConfig:(Option.map
                                    ~f:Values.DestinationConfig.of_json
                                    destinationConfig) ~functionName ())
           (Some Values.FunctionEventInvokeConfig.to_json)
           (Some Values.FunctionEventInvokeConfig.error_to_json)])
let put_function_recursion_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and functionName =
         flag "function-name" (required string)
           ~doc:"STRING UnqualifiedFunctionName"
       and recursiveLoop =
         flag "recursive-loop" (required json_arg) ~doc:"JSON RecursiveLoop" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_function_recursion_config
           (Values.PutFunctionRecursionConfigRequest.make ~functionName
              ~recursiveLoop:(Values.RecursiveLoop.of_json recursiveLoop) ())
           (Some Values.PutFunctionRecursionConfigResponse.to_json)
           (Some Values.PutFunctionRecursionConfigResponse.error_to_json)])
let put_function_scaling_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and functionScalingConfig =
         flag "function-scaling-config" (optional json_arg)
           ~doc:"JSON FunctionScalingConfig"
       and functionName =
         flag "function-name" (required string)
           ~doc:"STRING UnqualifiedFunctionName"
       and qualifier =
         flag "qualifier" (required string)
           ~doc:"STRING PublishedFunctionQualifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_function_scaling_config
           (Values.PutFunctionScalingConfigRequest.make
              ?functionScalingConfig:(Option.map
                                        ~f:Values.FunctionScalingConfig.of_json
                                        functionScalingConfig) ~functionName
              ~qualifier ())
           (Some Values.PutFunctionScalingConfigResponse.to_json)
           (Some Values.PutFunctionScalingConfigResponse.error_to_json)])
let put_provisioned_concurrency_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and functionName =
         flag "function-name" (required string) ~doc:"STRING FunctionName"
       and qualifier =
         flag "qualifier" (required string) ~doc:"STRING Qualifier"
       and provisionedConcurrentExecutions =
         flag "provisioned-concurrent-executions" (required int)
           ~doc:"INT PositiveInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_provisioned_concurrency_config
           (Values.PutProvisionedConcurrencyConfigRequest.make ~functionName
              ~qualifier ~provisionedConcurrentExecutions ())
           (Some Values.PutProvisionedConcurrencyConfigResponse.to_json)
           (Some Values.PutProvisionedConcurrencyConfigResponse.error_to_json)])
let put_runtime_management_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and qualifier =
         flag "qualifier" (optional string)
           ~doc:"STRING NumericLatestPublishedOrAliasQualifier"
       and runtimeVersionArn =
         flag "runtime-version-arn" (optional string)
           ~doc:"STRING RuntimeVersionArn"
       and functionName =
         flag "function-name" (required string)
           ~doc:"STRING NamespacedFunctionName"
       and updateRuntimeOn =
         flag "update-runtime-on" (required json_arg)
           ~doc:"JSON UpdateRuntimeOn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_runtime_management_config
           (Values.PutRuntimeManagementConfigRequest.make ?qualifier
              ?runtimeVersionArn ~functionName
              ~updateRuntimeOn:(Values.UpdateRuntimeOn.of_json
                                  updateRuntimeOn) ())
           (Some Values.PutRuntimeManagementConfigResponse.to_json)
           (Some Values.PutRuntimeManagementConfigResponse.error_to_json)])
let remove_layer_version_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and revisionId =
         flag "revision-id" (optional string) ~doc:"STRING String"
       and layerName =
         flag "layer-name" (required string) ~doc:"STRING LayerName"
       and versionNumber =
         flag "version-number" (required json_arg)
           ~doc:"JSON LayerVersionNumber"
       and statementId =
         flag "statement-id" (required string) ~doc:"STRING StatementId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_layer_version_permission
           (Values.RemoveLayerVersionPermissionRequest.make ?revisionId
              ~layerName
              ~versionNumber:(Values.LayerVersionNumber.of_json versionNumber)
              ~statementId ()) None None])
let remove_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and qualifier =
         flag "qualifier" (optional string)
           ~doc:"STRING NumericLatestPublishedOrAliasQualifier"
       and revisionId =
         flag "revision-id" (optional string) ~doc:"STRING String"
       and functionName =
         flag "function-name" (required string)
           ~doc:"STRING NamespacedFunctionName"
       and statementId =
         flag "statement-id" (required string)
           ~doc:"STRING NamespacedStatementId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_permission
           (Values.RemovePermissionRequest.make ?qualifier ?revisionId
              ~functionName ~statementId ()) None None])
let send_durable_execution_callback_failure =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and error = flag "error" (optional json_arg) ~doc:"JSON ErrorObject"
       and callbackId =
         flag "callback-id" (required string) ~doc:"STRING CallbackId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_durable_execution_callback_failure
           (Values.SendDurableExecutionCallbackFailureRequest.make
              ?error:(Option.map ~f:Values.ErrorObject.of_json error)
              ~callbackId ())
           (Some Values.SendDurableExecutionCallbackFailureResponse.to_json)
           (Some
              Values.SendDurableExecutionCallbackFailureResponse.error_to_json)])
let send_durable_execution_callback_heartbeat =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and callbackId =
         flag "callback-id" (required string) ~doc:"STRING CallbackId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_durable_execution_callback_heartbeat
           (Values.SendDurableExecutionCallbackHeartbeatRequest.make
              ~callbackId ())
           (Some Values.SendDurableExecutionCallbackHeartbeatResponse.to_json)
           (Some
              Values.SendDurableExecutionCallbackHeartbeatResponse.error_to_json)])
let send_durable_execution_callback_success =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and result =
         flag "result" (optional json_arg) ~doc:"JSON BinaryOperationPayload"
       and callbackId =
         flag "callback-id" (required string) ~doc:"STRING CallbackId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_durable_execution_callback_success
           (Values.SendDurableExecutionCallbackSuccessRequest.make
              ?result:(Option.map ~f:Values.BinaryOperationPayload.of_json
                         result) ~callbackId ())
           (Some Values.SendDurableExecutionCallbackSuccessResponse.to_json)
           (Some
              Values.SendDurableExecutionCallbackSuccessResponse.error_to_json)])
let stop_durable_execution =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and error = flag "error" (optional json_arg) ~doc:"JSON ErrorObject"
       and durableExecutionArn =
         flag "durable-execution-arn" (required string)
           ~doc:"STRING DurableExecutionArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_durable_execution
           (Values.StopDurableExecutionRequest.make
              ?error:(Option.map ~f:Values.ErrorObject.of_json error)
              ~durableExecutionArn ())
           (Some Values.StopDurableExecutionResponse.to_json)
           (Some Values.StopDurableExecutionResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resource =
         flag "resource" (required string) ~doc:"STRING TaggableResource"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resource
              ~tags:(Values.Tags.of_json tags) ()) None None])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resource =
         flag "resource" (required string) ~doc:"STRING TaggableResource"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resource
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ()) None None])
let update_alias =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and functionVersion =
         flag "function-version" (optional string)
           ~doc:"STRING VersionWithLatestPublished"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and routingConfig =
         flag "routing-config" (optional json_arg)
           ~doc:"JSON AliasRoutingConfiguration"
       and revisionId =
         flag "revision-id" (optional string) ~doc:"STRING String"
       and functionName =
         flag "function-name" (required string) ~doc:"STRING FunctionName"
       and name = flag "name" (required string) ~doc:"STRING Alias" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_alias
           (Values.UpdateAliasRequest.make ?functionVersion ?description
              ?routingConfig:(Option.map
                                ~f:Values.AliasRoutingConfiguration.of_json
                                routingConfig) ?revisionId ~functionName
              ~name ()) (Some Values.AliasConfiguration.to_json)
           (Some Values.AliasConfiguration.error_to_json)])
let update_capacity_provider =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and capacityProviderScalingConfig =
         flag "capacity-provider-scaling-config" (optional json_arg)
           ~doc:"JSON CapacityProviderScalingConfig"
       and capacityProviderName =
         flag "capacity-provider-name" (required string)
           ~doc:"STRING CapacityProviderName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_capacity_provider
           (Values.UpdateCapacityProviderRequest.make
              ?capacityProviderScalingConfig:(Option.map
                                                ~f:Values.CapacityProviderScalingConfig.of_json
                                                capacityProviderScalingConfig)
              ~capacityProviderName ())
           (Some Values.UpdateCapacityProviderResponse.to_json)
           (Some Values.UpdateCapacityProviderResponse.error_to_json)])
let update_code_signing_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and allowedPublishers =
         flag "allowed-publishers" (optional json_arg)
           ~doc:"JSON AllowedPublishers"
       and codeSigningPolicies =
         flag "code-signing-policies" (optional json_arg)
           ~doc:"JSON CodeSigningPolicies"
       and codeSigningConfigArn =
         flag "code-signing-config-arn" (required string)
           ~doc:"STRING CodeSigningConfigArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_code_signing_config
           (Values.UpdateCodeSigningConfigRequest.make ?description
              ?allowedPublishers:(Option.map
                                    ~f:Values.AllowedPublishers.of_json
                                    allowedPublishers)
              ?codeSigningPolicies:(Option.map
                                      ~f:Values.CodeSigningPolicies.of_json
                                      codeSigningPolicies)
              ~codeSigningConfigArn ())
           (Some Values.UpdateCodeSigningConfigResponse.to_json)
           (Some Values.UpdateCodeSigningConfigResponse.error_to_json)])
let update_event_source_mapping =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and functionName =
         flag "function-name" (optional string)
           ~doc:"STRING NamespacedFunctionName"
       and enabled = flag "enabled" (optional bool) ~doc:"BOOL Enabled"
       and batchSize = flag "batch-size" (optional int) ~doc:"INT BatchSize"
       and filterCriteria =
         flag "filter-criteria" (optional json_arg)
           ~doc:"JSON FilterCriteria"
       and maximumBatchingWindowInSeconds =
         flag "maximum-batching-window-in-seconds" (optional int)
           ~doc:"INT MaximumBatchingWindowInSeconds"
       and destinationConfig =
         flag "destination-config" (optional json_arg)
           ~doc:"JSON DestinationConfig"
       and maximumRecordAgeInSeconds =
         flag "maximum-record-age-in-seconds" (optional int)
           ~doc:"INT MaximumRecordAgeInSeconds"
       and bisectBatchOnFunctionError =
         flag "bisect-batch-on-function-error" (optional bool)
           ~doc:"BOOL BisectBatchOnFunctionError"
       and maximumRetryAttempts =
         flag "maximum-retry-attempts" (optional int)
           ~doc:"INT MaximumRetryAttemptsEventSourceMapping"
       and parallelizationFactor =
         flag "parallelization-factor" (optional int)
           ~doc:"INT ParallelizationFactor"
       and sourceAccessConfigurations =
         flag "source-access-configurations" (optional json_arg)
           ~doc:"JSON SourceAccessConfigurations"
       and tumblingWindowInSeconds =
         flag "tumbling-window-in-seconds" (optional int)
           ~doc:"INT TumblingWindowInSeconds"
       and functionResponseTypes =
         flag "function-response-types" (optional json_arg)
           ~doc:"JSON FunctionResponseTypeList"
       and scalingConfig =
         flag "scaling-config" (optional json_arg) ~doc:"JSON ScalingConfig"
       and amazonManagedKafkaEventSourceConfig =
         flag "amazon-managed-kafka-event-source-config" (optional json_arg)
           ~doc:"JSON AmazonManagedKafkaEventSourceConfig"
       and selfManagedKafkaEventSourceConfig =
         flag "self-managed-kafka-event-source-config" (optional json_arg)
           ~doc:"JSON SelfManagedKafkaEventSourceConfig"
       and documentDBEventSourceConfig =
         flag "document-d-b-event-source-config" (optional json_arg)
           ~doc:"JSON DocumentDBEventSourceConfig"
       and kMSKeyArn =
         flag "k-m-s-key-arn" (optional string) ~doc:"STRING KMSKeyArn"
       and metricsConfig =
         flag "metrics-config" (optional json_arg)
           ~doc:"JSON EventSourceMappingMetricsConfig"
       and loggingConfig =
         flag "logging-config" (optional json_arg)
           ~doc:"JSON EventSourceMappingLoggingConfig"
       and provisionedPollerConfig =
         flag "provisioned-poller-config" (optional json_arg)
           ~doc:"JSON ProvisionedPollerConfig"
       and uUID = flag "u-u-i-d" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_event_source_mapping
           (Values.UpdateEventSourceMappingRequest.make ?functionName
              ?enabled ?batchSize
              ?filterCriteria:(Option.map ~f:Values.FilterCriteria.of_json
                                 filterCriteria)
              ?maximumBatchingWindowInSeconds
              ?destinationConfig:(Option.map
                                    ~f:Values.DestinationConfig.of_json
                                    destinationConfig)
              ?maximumRecordAgeInSeconds ?bisectBatchOnFunctionError
              ?maximumRetryAttempts ?parallelizationFactor
              ?sourceAccessConfigurations:(Option.map
                                             ~f:Values.SourceAccessConfigurations.of_json
                                             sourceAccessConfigurations)
              ?tumblingWindowInSeconds
              ?functionResponseTypes:(Option.map
                                        ~f:Values.FunctionResponseTypeList.of_json
                                        functionResponseTypes)
              ?scalingConfig:(Option.map ~f:Values.ScalingConfig.of_json
                                scalingConfig)
              ?amazonManagedKafkaEventSourceConfig:(Option.map
                                                      ~f:Values.AmazonManagedKafkaEventSourceConfig.of_json
                                                      amazonManagedKafkaEventSourceConfig)
              ?selfManagedKafkaEventSourceConfig:(Option.map
                                                    ~f:Values.SelfManagedKafkaEventSourceConfig.of_json
                                                    selfManagedKafkaEventSourceConfig)
              ?documentDBEventSourceConfig:(Option.map
                                              ~f:Values.DocumentDBEventSourceConfig.of_json
                                              documentDBEventSourceConfig)
              ?kMSKeyArn
              ?metricsConfig:(Option.map
                                ~f:Values.EventSourceMappingMetricsConfig.of_json
                                metricsConfig)
              ?loggingConfig:(Option.map
                                ~f:Values.EventSourceMappingLoggingConfig.of_json
                                loggingConfig)
              ?provisionedPollerConfig:(Option.map
                                          ~f:Values.ProvisionedPollerConfig.of_json
                                          provisionedPollerConfig) ~uUID ())
           (Some Values.EventSourceMappingConfiguration.to_json)
           (Some Values.EventSourceMappingConfiguration.error_to_json)])
let update_function_code =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and zipFile = flag "zip-file" (optional json_arg) ~doc:"JSON Blob"
       and s3Bucket =
         flag "s3-bucket" (optional string) ~doc:"STRING S3Bucket"
       and s3Key = flag "s3-key" (optional string) ~doc:"STRING S3Key"
       and s3ObjectVersion =
         flag "s3-object-version" (optional string)
           ~doc:"STRING S3ObjectVersion"
       and imageUri = flag "image-uri" (optional string) ~doc:"STRING String"
       and publish = flag "publish" (optional bool) ~doc:"BOOL Boolean"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and revisionId =
         flag "revision-id" (optional string) ~doc:"STRING String"
       and architectures =
         flag "architectures" (optional json_arg)
           ~doc:"JSON ArchitecturesList"
       and sourceKMSKeyArn =
         flag "source-k-m-s-key-arn" (optional string)
           ~doc:"STRING KMSKeyArn"
       and publishTo =
         flag "publish-to" (optional json_arg)
           ~doc:"JSON FunctionVersionLatestPublished"
       and functionName =
         flag "function-name" (required string) ~doc:"STRING FunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_function_code
           (Values.UpdateFunctionCodeRequest.make
              ?zipFile:(Option.map ~f:Values.Blob.of_json zipFile) ?s3Bucket
              ?s3Key ?s3ObjectVersion ?imageUri ?publish ?dryRun ?revisionId
              ?architectures:(Option.map ~f:Values.ArchitecturesList.of_json
                                architectures) ?sourceKMSKeyArn
              ?publishTo:(Option.map
                            ~f:Values.FunctionVersionLatestPublished.of_json
                            publishTo) ~functionName ())
           (Some Values.FunctionConfiguration.to_json)
           (Some Values.FunctionConfiguration.error_to_json)])
let update_function_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and role = flag "role" (optional string) ~doc:"STRING RoleArn"
       and handler = flag "handler" (optional string) ~doc:"STRING Handler"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and timeout = flag "timeout" (optional int) ~doc:"INT Timeout"
       and memorySize =
         flag "memory-size" (optional int) ~doc:"INT MemorySize"
       and vpcConfig =
         flag "vpc-config" (optional json_arg) ~doc:"JSON VpcConfig"
       and environment =
         flag "environment" (optional json_arg) ~doc:"JSON Environment"
       and runtime = flag "runtime" (optional json_arg) ~doc:"JSON Runtime"
       and deadLetterConfig =
         flag "dead-letter-config" (optional json_arg)
           ~doc:"JSON DeadLetterConfig"
       and kMSKeyArn =
         flag "k-m-s-key-arn" (optional string) ~doc:"STRING KMSKeyArn"
       and tracingConfig =
         flag "tracing-config" (optional json_arg) ~doc:"JSON TracingConfig"
       and revisionId =
         flag "revision-id" (optional string) ~doc:"STRING String"
       and layers = flag "layers" (optional json_arg) ~doc:"JSON LayerList"
       and fileSystemConfigs =
         flag "file-system-configs" (optional json_arg)
           ~doc:"JSON FileSystemConfigList"
       and imageConfig =
         flag "image-config" (optional json_arg) ~doc:"JSON ImageConfig"
       and ephemeralStorage =
         flag "ephemeral-storage" (optional json_arg)
           ~doc:"JSON EphemeralStorage"
       and snapStart =
         flag "snap-start" (optional json_arg) ~doc:"JSON SnapStart"
       and loggingConfig =
         flag "logging-config" (optional json_arg) ~doc:"JSON LoggingConfig"
       and capacityProviderConfig =
         flag "capacity-provider-config" (optional json_arg)
           ~doc:"JSON CapacityProviderConfig"
       and durableConfig =
         flag "durable-config" (optional json_arg) ~doc:"JSON DurableConfig"
       and functionName =
         flag "function-name" (required string) ~doc:"STRING FunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_function_configuration
           (Values.UpdateFunctionConfigurationRequest.make ?role ?handler
              ?description ?timeout ?memorySize
              ?vpcConfig:(Option.map ~f:Values.VpcConfig.of_json vpcConfig)
              ?environment:(Option.map ~f:Values.Environment.of_json
                              environment)
              ?runtime:(Option.map ~f:Values.Runtime.of_json runtime)
              ?deadLetterConfig:(Option.map
                                   ~f:Values.DeadLetterConfig.of_json
                                   deadLetterConfig) ?kMSKeyArn
              ?tracingConfig:(Option.map ~f:Values.TracingConfig.of_json
                                tracingConfig) ?revisionId
              ?layers:(Option.map ~f:Values.LayerList.of_json layers)
              ?fileSystemConfigs:(Option.map
                                    ~f:Values.FileSystemConfigList.of_json
                                    fileSystemConfigs)
              ?imageConfig:(Option.map ~f:Values.ImageConfig.of_json
                              imageConfig)
              ?ephemeralStorage:(Option.map
                                   ~f:Values.EphemeralStorage.of_json
                                   ephemeralStorage)
              ?snapStart:(Option.map ~f:Values.SnapStart.of_json snapStart)
              ?loggingConfig:(Option.map ~f:Values.LoggingConfig.of_json
                                loggingConfig)
              ?capacityProviderConfig:(Option.map
                                         ~f:Values.CapacityProviderConfig.of_json
                                         capacityProviderConfig)
              ?durableConfig:(Option.map ~f:Values.DurableConfig.of_json
                                durableConfig) ~functionName ())
           (Some Values.FunctionConfiguration.to_json)
           (Some Values.FunctionConfiguration.error_to_json)])
let update_function_event_invoke_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and qualifier =
         flag "qualifier" (optional string)
           ~doc:"STRING NumericLatestPublishedOrAliasQualifier"
       and maximumRetryAttempts =
         flag "maximum-retry-attempts" (optional int)
           ~doc:"INT MaximumRetryAttempts"
       and maximumEventAgeInSeconds =
         flag "maximum-event-age-in-seconds" (optional int)
           ~doc:"INT MaximumEventAgeInSeconds"
       and destinationConfig =
         flag "destination-config" (optional json_arg)
           ~doc:"JSON DestinationConfig"
       and functionName =
         flag "function-name" (required string)
           ~doc:"STRING NamespacedFunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_function_event_invoke_config
           (Values.UpdateFunctionEventInvokeConfigRequest.make ?qualifier
              ?maximumRetryAttempts ?maximumEventAgeInSeconds
              ?destinationConfig:(Option.map
                                    ~f:Values.DestinationConfig.of_json
                                    destinationConfig) ~functionName ())
           (Some Values.FunctionEventInvokeConfig.to_json)
           (Some Values.FunctionEventInvokeConfig.error_to_json)])
let update_function_url_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and qualifier =
         flag "qualifier" (optional string)
           ~doc:"STRING FunctionUrlQualifier"
       and authType =
         flag "auth-type" (optional json_arg) ~doc:"JSON FunctionUrlAuthType"
       and cors = flag "cors" (optional json_arg) ~doc:"JSON Cors"
       and invokeMode =
         flag "invoke-mode" (optional json_arg) ~doc:"JSON InvokeMode"
       and functionName =
         flag "function-name" (required string) ~doc:"STRING FunctionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_function_url_config
           (Values.UpdateFunctionUrlConfigRequest.make ?qualifier
              ?authType:(Option.map ~f:Values.FunctionUrlAuthType.of_json
                           authType)
              ?cors:(Option.map ~f:Values.Cors.of_json cors)
              ?invokeMode:(Option.map ~f:Values.InvokeMode.of_json invokeMode)
              ~functionName ())
           (Some Values.UpdateFunctionUrlConfigResponse.to_json)
           (Some Values.UpdateFunctionUrlConfigResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("add-layer-version-permission", add_layer_version_permission);
    ("add-permission", add_permission);
    ("checkpoint-durable-execution", checkpoint_durable_execution);
    ("create-alias", create_alias);
    ("create-capacity-provider", create_capacity_provider);
    ("create-code-signing-config", create_code_signing_config);
    ("create-event-source-mapping", create_event_source_mapping);
    ("create-function", create_function);
    ("create-function-url-config", create_function_url_config);
    ("delete-alias", delete_alias);
    ("delete-capacity-provider", delete_capacity_provider);
    ("delete-code-signing-config", delete_code_signing_config);
    ("delete-event-source-mapping", delete_event_source_mapping);
    ("delete-function", delete_function);
    ("delete-function-code-signing-config",
      delete_function_code_signing_config);
    ("delete-function-concurrency", delete_function_concurrency);
    ("delete-function-event-invoke-config",
      delete_function_event_invoke_config);
    ("delete-function-url-config", delete_function_url_config);
    ("delete-layer-version", delete_layer_version);
    ("delete-provisioned-concurrency-config",
      delete_provisioned_concurrency_config);
    ("get-account-settings", get_account_settings);
    ("get-alias", get_alias);
    ("get-capacity-provider", get_capacity_provider);
    ("get-code-signing-config", get_code_signing_config);
    ("get-durable-execution", get_durable_execution);
    ("get-durable-execution-history", get_durable_execution_history);
    ("get-durable-execution-state", get_durable_execution_state);
    ("get-event-source-mapping", get_event_source_mapping);
    ("get-function", get_function);
    ("get-function-code-signing-config", get_function_code_signing_config);
    ("get-function-concurrency", get_function_concurrency);
    ("get-function-configuration", get_function_configuration);
    ("get-function-event-invoke-config", get_function_event_invoke_config);
    ("get-function-recursion-config", get_function_recursion_config);
    ("get-function-scaling-config", get_function_scaling_config);
    ("get-function-url-config", get_function_url_config);
    ("get-layer-version", get_layer_version);
    ("get-layer-version-by-arn", get_layer_version_by_arn);
    ("get-layer-version-policy", get_layer_version_policy);
    ("get-policy", get_policy);
    ("get-provisioned-concurrency-config",
      get_provisioned_concurrency_config);
    ("get-runtime-management-config", get_runtime_management_config);
    ("invoke", invoke);
    ("invoke-async", invoke_async);
    ("invoke-with-response-stream", invoke_with_response_stream);
    ("list-aliases", list_aliases);
    ("list-capacity-providers", list_capacity_providers);
    ("list-code-signing-configs", list_code_signing_configs);
    ("list-durable-executions-by-function",
      list_durable_executions_by_function);
    ("list-event-source-mappings", list_event_source_mappings);
    ("list-function-event-invoke-configs",
      list_function_event_invoke_configs);
    ("list-function-url-configs", list_function_url_configs);
    ("list-function-versions-by-capacity-provider",
      list_function_versions_by_capacity_provider);
    ("list-functions", list_functions);
    ("list-functions-by-code-signing-config",
      list_functions_by_code_signing_config);
    ("list-layer-versions", list_layer_versions);
    ("list-layers", list_layers);
    ("list-provisioned-concurrency-configs",
      list_provisioned_concurrency_configs);
    ("list-tags", list_tags);
    ("list-versions-by-function", list_versions_by_function);
    ("publish-layer-version", publish_layer_version);
    ("publish-version", publish_version);
    ("put-function-code-signing-config", put_function_code_signing_config);
    ("put-function-concurrency", put_function_concurrency);
    ("put-function-event-invoke-config", put_function_event_invoke_config);
    ("put-function-recursion-config", put_function_recursion_config);
    ("put-function-scaling-config", put_function_scaling_config);
    ("put-provisioned-concurrency-config",
      put_provisioned_concurrency_config);
    ("put-runtime-management-config", put_runtime_management_config);
    ("remove-layer-version-permission", remove_layer_version_permission);
    ("remove-permission", remove_permission);
    ("send-durable-execution-callback-failure",
      send_durable_execution_callback_failure);
    ("send-durable-execution-callback-heartbeat",
      send_durable_execution_callback_heartbeat);
    ("send-durable-execution-callback-success",
      send_durable_execution_callback_success);
    ("stop-durable-execution", stop_durable_execution);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-alias", update_alias);
    ("update-capacity-provider", update_capacity_provider);
    ("update-code-signing-config", update_code_signing_config);
    ("update-event-source-mapping", update_event_source_mapping);
    ("update-function-code", update_function_code);
    ("update-function-configuration", update_function_configuration);
    ("update-function-event-invoke-config",
      update_function_event_invoke_config);
    ("update-function-url-config", update_function_url_config)]
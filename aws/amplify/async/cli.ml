(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and repository =
         flag "repository" (optional string) ~doc:"STRING Repository"
       and platform =
         flag "platform" (optional json_arg) ~doc:"JSON Platform"
       and computeRoleArn =
         flag "compute-role-arn" (optional string)
           ~doc:"STRING ComputeRoleArn"
       and iamServiceRoleArn =
         flag "iam-service-role-arn" (optional string)
           ~doc:"STRING ServiceRoleArn"
       and oauthToken =
         flag "oauth-token" (optional string) ~doc:"STRING OauthToken"
       and accessToken =
         flag "access-token" (optional string) ~doc:"STRING AccessToken"
       and environmentVariables =
         flag "environment-variables" (optional json_arg)
           ~doc:"JSON EnvironmentVariables"
       and enableBranchAutoBuild =
         flag "enable-branch-auto-build" (optional bool)
           ~doc:"BOOL EnableBranchAutoBuild"
       and enableBranchAutoDeletion =
         flag "enable-branch-auto-deletion" (optional bool)
           ~doc:"BOOL EnableBranchAutoDeletion"
       and enableBasicAuth =
         flag "enable-basic-auth" (optional bool) ~doc:"BOOL EnableBasicAuth"
       and basicAuthCredentials =
         flag "basic-auth-credentials" (optional string)
           ~doc:"STRING BasicAuthCredentials"
       and customRules =
         flag "custom-rules" (optional json_arg) ~doc:"JSON CustomRules"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and buildSpec =
         flag "build-spec" (optional string) ~doc:"STRING BuildSpec"
       and customHeaders =
         flag "custom-headers" (optional string) ~doc:"STRING CustomHeaders"
       and enableAutoBranchCreation =
         flag "enable-auto-branch-creation" (optional bool)
           ~doc:"BOOL EnableAutoBranchCreation"
       and autoBranchCreationPatterns =
         flag "auto-branch-creation-patterns" (optional json_arg)
           ~doc:"JSON AutoBranchCreationPatterns"
       and autoBranchCreationConfig =
         flag "auto-branch-creation-config" (optional json_arg)
           ~doc:"JSON AutoBranchCreationConfig"
       and jobConfig =
         flag "job-config" (optional json_arg) ~doc:"JSON JobConfig"
       and cacheConfig =
         flag "cache-config" (optional json_arg) ~doc:"JSON CacheConfig"
       and name = flag "name" (required string) ~doc:"STRING Name" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_app
           (Values.CreateAppRequest.make ?description ?repository
              ?platform:(Option.map ~f:Values.Platform.of_json platform)
              ?computeRoleArn ?iamServiceRoleArn ?oauthToken ?accessToken
              ?environmentVariables:(Option.map
                                       ~f:Values.EnvironmentVariables.of_json
                                       environmentVariables)
              ?enableBranchAutoBuild ?enableBranchAutoDeletion
              ?enableBasicAuth ?basicAuthCredentials
              ?customRules:(Option.map ~f:Values.CustomRules.of_json
                              customRules)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ?buildSpec
              ?customHeaders ?enableAutoBranchCreation
              ?autoBranchCreationPatterns:(Option.map
                                             ~f:Values.AutoBranchCreationPatterns.of_json
                                             autoBranchCreationPatterns)
              ?autoBranchCreationConfig:(Option.map
                                           ~f:Values.AutoBranchCreationConfig.of_json
                                           autoBranchCreationConfig)
              ?jobConfig:(Option.map ~f:Values.JobConfig.of_json jobConfig)
              ?cacheConfig:(Option.map ~f:Values.CacheConfig.of_json
                              cacheConfig) ~name ())
           (Some Values.CreateAppResult.to_json)
           (Some Values.CreateAppResult.error_to_json)])
let create_backend_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and stackName =
         flag "stack-name" (optional string) ~doc:"STRING StackName"
       and deploymentArtifacts =
         flag "deployment-artifacts" (optional string)
           ~doc:"STRING DeploymentArtifacts"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId"
       and environmentName =
         flag "environment-name" (required string)
           ~doc:"STRING EnvironmentName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_backend_environment
           (Values.CreateBackendEnvironmentRequest.make ?stackName
              ?deploymentArtifacts ~appId ~environmentName ())
           (Some Values.CreateBackendEnvironmentResult.to_json)
           (Some Values.CreateBackendEnvironmentResult.error_to_json)])
let create_branch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and stage = flag "stage" (optional json_arg) ~doc:"JSON Stage"
       and framework =
         flag "framework" (optional string) ~doc:"STRING Framework"
       and enableNotification =
         flag "enable-notification" (optional bool)
           ~doc:"BOOL EnableNotification"
       and enableAutoBuild =
         flag "enable-auto-build" (optional bool) ~doc:"BOOL EnableAutoBuild"
       and enableSkewProtection =
         flag "enable-skew-protection" (optional bool)
           ~doc:"BOOL EnableSkewProtection"
       and environmentVariables =
         flag "environment-variables" (optional json_arg)
           ~doc:"JSON EnvironmentVariables"
       and basicAuthCredentials =
         flag "basic-auth-credentials" (optional string)
           ~doc:"STRING BasicAuthCredentials"
       and enableBasicAuth =
         flag "enable-basic-auth" (optional bool) ~doc:"BOOL EnableBasicAuth"
       and enablePerformanceMode =
         flag "enable-performance-mode" (optional bool)
           ~doc:"BOOL EnablePerformanceMode"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and buildSpec =
         flag "build-spec" (optional string) ~doc:"STRING BuildSpec"
       and ttl = flag "ttl" (optional string) ~doc:"STRING TTL"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING DisplayName"
       and enablePullRequestPreview =
         flag "enable-pull-request-preview" (optional bool)
           ~doc:"BOOL EnablePullRequestPreview"
       and pullRequestEnvironmentName =
         flag "pull-request-environment-name" (optional string)
           ~doc:"STRING PullRequestEnvironmentName"
       and backendEnvironmentArn =
         flag "backend-environment-arn" (optional string)
           ~doc:"STRING BackendEnvironmentArn"
       and backend = flag "backend" (optional json_arg) ~doc:"JSON Backend"
       and computeRoleArn =
         flag "compute-role-arn" (optional string)
           ~doc:"STRING ComputeRoleArn"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId"
       and branchName =
         flag "branch-name" (required string) ~doc:"STRING BranchName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_branch
           (Values.CreateBranchRequest.make ?description
              ?stage:(Option.map ~f:Values.Stage.of_json stage) ?framework
              ?enableNotification ?enableAutoBuild ?enableSkewProtection
              ?environmentVariables:(Option.map
                                       ~f:Values.EnvironmentVariables.of_json
                                       environmentVariables)
              ?basicAuthCredentials ?enableBasicAuth ?enablePerformanceMode
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ?buildSpec
              ?ttl ?displayName ?enablePullRequestPreview
              ?pullRequestEnvironmentName ?backendEnvironmentArn
              ?backend:(Option.map ~f:Values.Backend.of_json backend)
              ?computeRoleArn ~appId ~branchName ())
           (Some Values.CreateBranchResult.to_json)
           (Some Values.CreateBranchResult.error_to_json)])
let create_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fileMap = flag "file-map" (optional json_arg) ~doc:"JSON FileMap"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId"
       and branchName =
         flag "branch-name" (required string) ~doc:"STRING BranchName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_deployment
           (Values.CreateDeploymentRequest.make
              ?fileMap:(Option.map ~f:Values.FileMap.of_json fileMap) ~appId
              ~branchName ()) (Some Values.CreateDeploymentResult.to_json)
           (Some Values.CreateDeploymentResult.error_to_json)])
let create_domain_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and enableAutoSubDomain =
         flag "enable-auto-sub-domain" (optional bool)
           ~doc:"BOOL EnableAutoSubDomain"
       and autoSubDomainCreationPatterns =
         flag "auto-sub-domain-creation-patterns" (optional json_arg)
           ~doc:"JSON AutoSubDomainCreationPatterns"
       and autoSubDomainIAMRole =
         flag "auto-sub-domain-i-a-m-role" (optional string)
           ~doc:"STRING AutoSubDomainIAMRole"
       and certificateSettings =
         flag "certificate-settings" (optional json_arg)
           ~doc:"JSON CertificateSettings"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and subDomainSettings =
         flag "sub-domain-settings" (required json_arg)
           ~doc:"JSON SubDomainSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_domain_association
           (Values.CreateDomainAssociationRequest.make ?enableAutoSubDomain
              ?autoSubDomainCreationPatterns:(Option.map
                                                ~f:Values.AutoSubDomainCreationPatterns.of_json
                                                autoSubDomainCreationPatterns)
              ?autoSubDomainIAMRole
              ?certificateSettings:(Option.map
                                      ~f:Values.CertificateSettings.of_json
                                      certificateSettings) ~appId ~domainName
              ~subDomainSettings:(Values.SubDomainSettings.of_json
                                    subDomainSettings) ())
           (Some Values.CreateDomainAssociationResult.to_json)
           (Some Values.CreateDomainAssociationResult.error_to_json)])
let create_webhook =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId"
       and branchName =
         flag "branch-name" (required string) ~doc:"STRING BranchName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_webhook
           (Values.CreateWebhookRequest.make ?description ~appId ~branchName
              ()) (Some Values.CreateWebhookResult.to_json)
           (Some Values.CreateWebhookResult.error_to_json)])
let delete_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_app (Values.DeleteAppRequest.make ~appId ())
           (Some Values.DeleteAppResult.to_json)
           (Some Values.DeleteAppResult.error_to_json)])
let delete_backend_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId"
       and environmentName =
         flag "environment-name" (required string)
           ~doc:"STRING EnvironmentName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_backend_environment
           (Values.DeleteBackendEnvironmentRequest.make ~appId
              ~environmentName ())
           (Some Values.DeleteBackendEnvironmentResult.to_json)
           (Some Values.DeleteBackendEnvironmentResult.error_to_json)])
let delete_branch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId"
       and branchName =
         flag "branch-name" (required string) ~doc:"STRING BranchName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_branch
           (Values.DeleteBranchRequest.make ~appId ~branchName ())
           (Some Values.DeleteBranchResult.to_json)
           (Some Values.DeleteBranchResult.error_to_json)])
let delete_domain_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_domain_association
           (Values.DeleteDomainAssociationRequest.make ~appId ~domainName ())
           (Some Values.DeleteDomainAssociationResult.to_json)
           (Some Values.DeleteDomainAssociationResult.error_to_json)])
let delete_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId"
       and branchName =
         flag "branch-name" (required string) ~doc:"STRING BranchName"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_job
           (Values.DeleteJobRequest.make ~appId ~branchName ~jobId ())
           (Some Values.DeleteJobResult.to_json)
           (Some Values.DeleteJobResult.error_to_json)])
let delete_webhook =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and webhookId =
         flag "webhook-id" (required string) ~doc:"STRING WebhookId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_webhook (Values.DeleteWebhookRequest.make ~webhookId ())
           (Some Values.DeleteWebhookResult.to_json)
           (Some Values.DeleteWebhookResult.error_to_json)])
let generate_access_logs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON StartTime"
       and endTime = flag "end-time" (optional json_arg) ~doc:"JSON EndTime"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.generate_access_logs
           (Values.GenerateAccessLogsRequest.make
              ?startTime:(Option.map ~f:Values.StartTime.of_json startTime)
              ?endTime:(Option.map ~f:Values.EndTime.of_json endTime)
              ~domainName ~appId ())
           (Some Values.GenerateAccessLogsResult.to_json)
           (Some Values.GenerateAccessLogsResult.error_to_json)])
let get_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_app (Values.GetAppRequest.make ~appId ())
           (Some Values.GetAppResult.to_json)
           (Some Values.GetAppResult.error_to_json)])
let get_artifact_url =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and artifactId =
         flag "artifact-id" (required string) ~doc:"STRING ArtifactId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_artifact_url
           (Values.GetArtifactUrlRequest.make ~artifactId ())
           (Some Values.GetArtifactUrlResult.to_json)
           (Some Values.GetArtifactUrlResult.error_to_json)])
let get_backend_environment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId"
       and environmentName =
         flag "environment-name" (required string)
           ~doc:"STRING EnvironmentName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_backend_environment
           (Values.GetBackendEnvironmentRequest.make ~appId ~environmentName
              ()) (Some Values.GetBackendEnvironmentResult.to_json)
           (Some Values.GetBackendEnvironmentResult.error_to_json)])
let get_branch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId"
       and branchName =
         flag "branch-name" (required string) ~doc:"STRING BranchName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_branch (Values.GetBranchRequest.make ~appId ~branchName ())
           (Some Values.GetBranchResult.to_json)
           (Some Values.GetBranchResult.error_to_json)])
let get_domain_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_domain_association
           (Values.GetDomainAssociationRequest.make ~appId ~domainName ())
           (Some Values.GetDomainAssociationResult.to_json)
           (Some Values.GetDomainAssociationResult.error_to_json)])
let get_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId"
       and branchName =
         flag "branch-name" (required string) ~doc:"STRING BranchName"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_job
           (Values.GetJobRequest.make ~appId ~branchName ~jobId ())
           (Some Values.GetJobResult.to_json)
           (Some Values.GetJobResult.error_to_json)])
let get_webhook =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and webhookId =
         flag "webhook-id" (required string) ~doc:"STRING WebhookId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_webhook (Values.GetWebhookRequest.make ~webhookId ())
           (Some Values.GetWebhookResult.to_json)
           (Some Values.GetWebhookResult.error_to_json)])
let list_apps =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsForListApps" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_apps
           (Values.ListAppsRequest.make ?nextToken ?maxResults ())
           (Some Values.ListAppsResult.to_json)
           (Some Values.ListAppsResult.error_to_json)])
let list_artifacts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId"
       and branchName =
         flag "branch-name" (required string) ~doc:"STRING BranchName"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_artifacts
           (Values.ListArtifactsRequest.make ?nextToken ?maxResults ~appId
              ~branchName ~jobId ())
           (Some Values.ListArtifactsResult.to_json)
           (Some Values.ListArtifactsResult.error_to_json)])
let list_backend_environments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and environmentName =
         flag "environment-name" (optional string)
           ~doc:"STRING EnvironmentName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_backend_environments
           (Values.ListBackendEnvironmentsRequest.make ?environmentName
              ?nextToken ?maxResults ~appId ())
           (Some Values.ListBackendEnvironmentsResult.to_json)
           (Some Values.ListBackendEnvironmentsResult.error_to_json)])
let list_branches =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_branches
           (Values.ListBranchesRequest.make ?nextToken ?maxResults ~appId ())
           (Some Values.ListBranchesResult.to_json)
           (Some Values.ListBranchesResult.error_to_json)])
let list_domain_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_domain_associations
           (Values.ListDomainAssociationsRequest.make ?nextToken ?maxResults
              ~appId ()) (Some Values.ListDomainAssociationsResult.to_json)
           (Some Values.ListDomainAssociationsResult.error_to_json)])
let list_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId"
       and branchName =
         flag "branch-name" (required string) ~doc:"STRING BranchName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_jobs
           (Values.ListJobsRequest.make ?nextToken ?maxResults ~appId
              ~branchName ()) (Some Values.ListJobsResult.to_json)
           (Some Values.ListJobsResult.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_webhooks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_webhooks
           (Values.ListWebhooksRequest.make ?nextToken ?maxResults ~appId ())
           (Some Values.ListWebhooksResult.to_json)
           (Some Values.ListWebhooksResult.error_to_json)])
let start_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (optional string) ~doc:"STRING JobId"
       and sourceUrl =
         flag "source-url" (optional string) ~doc:"STRING SourceUrl"
       and sourceUrlType =
         flag "source-url-type" (optional json_arg) ~doc:"JSON SourceUrlType"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId"
       and branchName =
         flag "branch-name" (required string) ~doc:"STRING BranchName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_deployment
           (Values.StartDeploymentRequest.make ?jobId ?sourceUrl
              ?sourceUrlType:(Option.map ~f:Values.SourceUrlType.of_json
                                sourceUrlType) ~appId ~branchName ())
           (Some Values.StartDeploymentResult.to_json)
           (Some Values.StartDeploymentResult.error_to_json)])
let start_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (optional string) ~doc:"STRING JobId"
       and jobReason =
         flag "job-reason" (optional string) ~doc:"STRING JobReason"
       and commitId =
         flag "commit-id" (optional string) ~doc:"STRING CommitId"
       and commitMessage =
         flag "commit-message" (optional string) ~doc:"STRING CommitMessage"
       and commitTime =
         flag "commit-time" (optional json_arg) ~doc:"JSON CommitTime"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId"
       and branchName =
         flag "branch-name" (required string) ~doc:"STRING BranchName"
       and jobType = flag "job-type" (required json_arg) ~doc:"JSON JobType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_job
           (Values.StartJobRequest.make ?jobId ?jobReason ?commitId
              ?commitMessage
              ?commitTime:(Option.map ~f:Values.CommitTime.of_json commitTime)
              ~appId ~branchName ~jobType:(Values.JobType.of_json jobType) ())
           (Some Values.StartJobResult.to_json)
           (Some Values.StartJobResult.error_to_json)])
let stop_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId"
       and branchName =
         flag "branch-name" (required string) ~doc:"STRING BranchName"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_job
           (Values.StopJobRequest.make ~appId ~branchName ~jobId ())
           (Some Values.StopJobResult.to_json)
           (Some Values.StopJobResult.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_app =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING Name"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and platform =
         flag "platform" (optional json_arg) ~doc:"JSON Platform"
       and computeRoleArn =
         flag "compute-role-arn" (optional string)
           ~doc:"STRING ComputeRoleArn"
       and iamServiceRoleArn =
         flag "iam-service-role-arn" (optional string)
           ~doc:"STRING ServiceRoleArn"
       and environmentVariables =
         flag "environment-variables" (optional json_arg)
           ~doc:"JSON EnvironmentVariables"
       and enableBranchAutoBuild =
         flag "enable-branch-auto-build" (optional bool)
           ~doc:"BOOL EnableAutoBuild"
       and enableBranchAutoDeletion =
         flag "enable-branch-auto-deletion" (optional bool)
           ~doc:"BOOL EnableBranchAutoDeletion"
       and enableBasicAuth =
         flag "enable-basic-auth" (optional bool) ~doc:"BOOL EnableBasicAuth"
       and basicAuthCredentials =
         flag "basic-auth-credentials" (optional string)
           ~doc:"STRING BasicAuthCredentials"
       and customRules =
         flag "custom-rules" (optional json_arg) ~doc:"JSON CustomRules"
       and buildSpec =
         flag "build-spec" (optional string) ~doc:"STRING BuildSpec"
       and customHeaders =
         flag "custom-headers" (optional string) ~doc:"STRING CustomHeaders"
       and enableAutoBranchCreation =
         flag "enable-auto-branch-creation" (optional bool)
           ~doc:"BOOL EnableAutoBranchCreation"
       and autoBranchCreationPatterns =
         flag "auto-branch-creation-patterns" (optional json_arg)
           ~doc:"JSON AutoBranchCreationPatterns"
       and autoBranchCreationConfig =
         flag "auto-branch-creation-config" (optional json_arg)
           ~doc:"JSON AutoBranchCreationConfig"
       and repository =
         flag "repository" (optional string) ~doc:"STRING Repository"
       and oauthToken =
         flag "oauth-token" (optional string) ~doc:"STRING OauthToken"
       and accessToken =
         flag "access-token" (optional string) ~doc:"STRING AccessToken"
       and jobConfig =
         flag "job-config" (optional json_arg) ~doc:"JSON JobConfig"
       and cacheConfig =
         flag "cache-config" (optional json_arg) ~doc:"JSON CacheConfig"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_app
           (Values.UpdateAppRequest.make ?name ?description
              ?platform:(Option.map ~f:Values.Platform.of_json platform)
              ?computeRoleArn ?iamServiceRoleArn
              ?environmentVariables:(Option.map
                                       ~f:Values.EnvironmentVariables.of_json
                                       environmentVariables)
              ?enableBranchAutoBuild ?enableBranchAutoDeletion
              ?enableBasicAuth ?basicAuthCredentials
              ?customRules:(Option.map ~f:Values.CustomRules.of_json
                              customRules) ?buildSpec ?customHeaders
              ?enableAutoBranchCreation
              ?autoBranchCreationPatterns:(Option.map
                                             ~f:Values.AutoBranchCreationPatterns.of_json
                                             autoBranchCreationPatterns)
              ?autoBranchCreationConfig:(Option.map
                                           ~f:Values.AutoBranchCreationConfig.of_json
                                           autoBranchCreationConfig)
              ?repository ?oauthToken ?accessToken
              ?jobConfig:(Option.map ~f:Values.JobConfig.of_json jobConfig)
              ?cacheConfig:(Option.map ~f:Values.CacheConfig.of_json
                              cacheConfig) ~appId ())
           (Some Values.UpdateAppResult.to_json)
           (Some Values.UpdateAppResult.error_to_json)])
let update_branch =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and framework =
         flag "framework" (optional string) ~doc:"STRING Framework"
       and stage = flag "stage" (optional json_arg) ~doc:"JSON Stage"
       and enableNotification =
         flag "enable-notification" (optional bool)
           ~doc:"BOOL EnableNotification"
       and enableAutoBuild =
         flag "enable-auto-build" (optional bool) ~doc:"BOOL EnableAutoBuild"
       and enableSkewProtection =
         flag "enable-skew-protection" (optional bool)
           ~doc:"BOOL EnableSkewProtection"
       and environmentVariables =
         flag "environment-variables" (optional json_arg)
           ~doc:"JSON EnvironmentVariables"
       and basicAuthCredentials =
         flag "basic-auth-credentials" (optional string)
           ~doc:"STRING BasicAuthCredentials"
       and enableBasicAuth =
         flag "enable-basic-auth" (optional bool) ~doc:"BOOL EnableBasicAuth"
       and enablePerformanceMode =
         flag "enable-performance-mode" (optional bool)
           ~doc:"BOOL EnablePerformanceMode"
       and buildSpec =
         flag "build-spec" (optional string) ~doc:"STRING BuildSpec"
       and ttl = flag "ttl" (optional string) ~doc:"STRING TTL"
       and displayName =
         flag "display-name" (optional string) ~doc:"STRING DisplayName"
       and enablePullRequestPreview =
         flag "enable-pull-request-preview" (optional bool)
           ~doc:"BOOL EnablePullRequestPreview"
       and pullRequestEnvironmentName =
         flag "pull-request-environment-name" (optional string)
           ~doc:"STRING PullRequestEnvironmentName"
       and backendEnvironmentArn =
         flag "backend-environment-arn" (optional string)
           ~doc:"STRING BackendEnvironmentArn"
       and backend = flag "backend" (optional json_arg) ~doc:"JSON Backend"
       and computeRoleArn =
         flag "compute-role-arn" (optional string)
           ~doc:"STRING ComputeRoleArn"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId"
       and branchName =
         flag "branch-name" (required string) ~doc:"STRING BranchName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_branch
           (Values.UpdateBranchRequest.make ?description ?framework
              ?stage:(Option.map ~f:Values.Stage.of_json stage)
              ?enableNotification ?enableAutoBuild ?enableSkewProtection
              ?environmentVariables:(Option.map
                                       ~f:Values.EnvironmentVariables.of_json
                                       environmentVariables)
              ?basicAuthCredentials ?enableBasicAuth ?enablePerformanceMode
              ?buildSpec ?ttl ?displayName ?enablePullRequestPreview
              ?pullRequestEnvironmentName ?backendEnvironmentArn
              ?backend:(Option.map ~f:Values.Backend.of_json backend)
              ?computeRoleArn ~appId ~branchName ())
           (Some Values.UpdateBranchResult.to_json)
           (Some Values.UpdateBranchResult.error_to_json)])
let update_domain_association =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and enableAutoSubDomain =
         flag "enable-auto-sub-domain" (optional bool)
           ~doc:"BOOL EnableAutoSubDomain"
       and subDomainSettings =
         flag "sub-domain-settings" (optional json_arg)
           ~doc:"JSON SubDomainSettings"
       and autoSubDomainCreationPatterns =
         flag "auto-sub-domain-creation-patterns" (optional json_arg)
           ~doc:"JSON AutoSubDomainCreationPatterns"
       and autoSubDomainIAMRole =
         flag "auto-sub-domain-i-a-m-role" (optional string)
           ~doc:"STRING AutoSubDomainIAMRole"
       and certificateSettings =
         flag "certificate-settings" (optional json_arg)
           ~doc:"JSON CertificateSettings"
       and appId = flag "app-id" (required string) ~doc:"STRING AppId"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_domain_association
           (Values.UpdateDomainAssociationRequest.make ?enableAutoSubDomain
              ?subDomainSettings:(Option.map
                                    ~f:Values.SubDomainSettings.of_json
                                    subDomainSettings)
              ?autoSubDomainCreationPatterns:(Option.map
                                                ~f:Values.AutoSubDomainCreationPatterns.of_json
                                                autoSubDomainCreationPatterns)
              ?autoSubDomainIAMRole
              ?certificateSettings:(Option.map
                                      ~f:Values.CertificateSettings.of_json
                                      certificateSettings) ~appId ~domainName
              ()) (Some Values.UpdateDomainAssociationResult.to_json)
           (Some Values.UpdateDomainAssociationResult.error_to_json)])
let update_webhook =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and branchName =
         flag "branch-name" (optional string) ~doc:"STRING BranchName"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and webhookId =
         flag "webhook-id" (required string) ~doc:"STRING WebhookId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_webhook
           (Values.UpdateWebhookRequest.make ?branchName ?description
              ~webhookId ()) (Some Values.UpdateWebhookResult.to_json)
           (Some Values.UpdateWebhookResult.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-app", create_app);
    ("create-backend-environment", create_backend_environment);
    ("create-branch", create_branch);
    ("create-deployment", create_deployment);
    ("create-domain-association", create_domain_association);
    ("create-webhook", create_webhook);
    ("delete-app", delete_app);
    ("delete-backend-environment", delete_backend_environment);
    ("delete-branch", delete_branch);
    ("delete-domain-association", delete_domain_association);
    ("delete-job", delete_job);
    ("delete-webhook", delete_webhook);
    ("generate-access-logs", generate_access_logs);
    ("get-app", get_app);
    ("get-artifact-url", get_artifact_url);
    ("get-backend-environment", get_backend_environment);
    ("get-branch", get_branch);
    ("get-domain-association", get_domain_association);
    ("get-job", get_job);
    ("get-webhook", get_webhook);
    ("list-apps", list_apps);
    ("list-artifacts", list_artifacts);
    ("list-backend-environments", list_backend_environments);
    ("list-branches", list_branches);
    ("list-domain-associations", list_domain_associations);
    ("list-jobs", list_jobs);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-webhooks", list_webhooks);
    ("start-deployment", start_deployment);
    ("start-job", start_job);
    ("stop-job", stop_job);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-app", update_app);
    ("update-branch", update_branch);
    ("update-domain-association", update_domain_association);
    ("update-webhook", update_webhook)]
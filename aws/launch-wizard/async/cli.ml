(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON Tags"
       and workloadName =
         flag "workload-name" (required string) ~doc:"STRING WorkloadName"
       and deploymentPatternName =
         flag "deployment-pattern-name" (required string)
           ~doc:"STRING DeploymentPatternName"
       and name = flag "name" (required string) ~doc:"STRING DeploymentName"
       and specifications =
         flag "specifications" (required json_arg)
           ~doc:"JSON DeploymentSpecifications" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_deployment
           (Values.CreateDeploymentInput.make ?dryRun
              ?tags:(Option.map ~f:Values.Tags.of_json tags) ~workloadName
              ~deploymentPatternName ~name
              ~specifications:(Values.DeploymentSpecifications.of_json
                                 specifications) ())
           (Some Values.CreateDeploymentOutput.to_json)
           (Some Values.CreateDeploymentOutput.error_to_json)])
let delete_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deploymentId =
         flag "deployment-id" (required string) ~doc:"STRING DeploymentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_deployment
           (Values.DeleteDeploymentInput.make ~deploymentId ())
           (Some Values.DeleteDeploymentOutput.to_json)
           (Some Values.DeleteDeploymentOutput.error_to_json)])
let get_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and deploymentId =
         flag "deployment-id" (required string) ~doc:"STRING DeploymentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_deployment
           (Values.GetDeploymentInput.make ~deploymentId ())
           (Some Values.GetDeploymentOutput.to_json)
           (Some Values.GetDeploymentOutput.error_to_json)])
let get_deployment_pattern_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workloadName =
         flag "workload-name" (required string) ~doc:"STRING WorkloadName"
       and deploymentPatternName =
         flag "deployment-pattern-name" (required string)
           ~doc:"STRING DeploymentPatternName"
       and deploymentPatternVersionName =
         flag "deployment-pattern-version-name" (required string)
           ~doc:"STRING DeploymentPatternVersionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_deployment_pattern_version
           (Values.GetDeploymentPatternVersionInput.make ~workloadName
              ~deploymentPatternName ~deploymentPatternVersionName ())
           (Some Values.GetDeploymentPatternVersionOutput.to_json)
           (Some Values.GetDeploymentPatternVersionOutput.error_to_json)])
let get_workload =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workloadName =
         flag "workload-name" (required string) ~doc:"STRING WorkloadName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_workload (Values.GetWorkloadInput.make ~workloadName ())
           (Some Values.GetWorkloadOutput.to_json)
           (Some Values.GetWorkloadOutput.error_to_json)])
let get_workload_deployment_pattern =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workloadName =
         flag "workload-name" (required string) ~doc:"STRING WorkloadName"
       and deploymentPatternName =
         flag "deployment-pattern-name" (required string)
           ~doc:"STRING DeploymentPatternName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_workload_deployment_pattern
           (Values.GetWorkloadDeploymentPatternInput.make ~workloadName
              ~deploymentPatternName ())
           (Some Values.GetWorkloadDeploymentPatternOutput.to_json)
           (Some Values.GetWorkloadDeploymentPatternOutput.error_to_json)])
let list_deployment_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxDeploymentEventResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and deploymentId =
         flag "deployment-id" (required string) ~doc:"STRING DeploymentId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_deployment_events
           (Values.ListDeploymentEventsInput.make ?maxResults ?nextToken
              ~deploymentId ())
           (Some Values.ListDeploymentEventsOutput.to_json)
           (Some Values.ListDeploymentEventsOutput.error_to_json)])
let list_deployment_pattern_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxWorkloadResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON FilterList"
       and workloadName =
         flag "workload-name" (required string) ~doc:"STRING WorkloadName"
       and deploymentPatternName =
         flag "deployment-pattern-name" (required string)
           ~doc:"STRING DeploymentPatternName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_deployment_pattern_versions
           (Values.ListDeploymentPatternVersionsInput.make ?maxResults
              ?nextToken
              ?filters:(Option.map ~f:Values.FilterList.of_json filters)
              ~workloadName ~deploymentPatternName ())
           (Some Values.ListDeploymentPatternVersionsOutput.to_json)
           (Some Values.ListDeploymentPatternVersionsOutput.error_to_json)])
let list_deployments =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON DeploymentFilterList"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxDeploymentResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_deployments
           (Values.ListDeploymentsInput.make
              ?filters:(Option.map ~f:Values.DeploymentFilterList.of_json
                          filters) ?maxResults ?nextToken ())
           (Some Values.ListDeploymentsOutput.to_json)
           (Some Values.ListDeploymentsOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let list_workload_deployment_patterns =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT MaxWorkloadDeploymentPatternResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and workloadName =
         flag "workload-name" (required string) ~doc:"STRING WorkloadName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_workload_deployment_patterns
           (Values.ListWorkloadDeploymentPatternsInput.make ?maxResults
              ?nextToken ~workloadName ())
           (Some Values.ListWorkloadDeploymentPatternsOutput.to_json)
           (Some Values.ListWorkloadDeploymentPatternsOutput.error_to_json)])
let list_workloads =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxWorkloadResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_workloads
           (Values.ListWorkloadsInput.make ?maxResults ?nextToken ())
           (Some Values.ListWorkloadsOutput.to_json)
           (Some Values.ListWorkloadsOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tags = flag "tags" (required json_arg) ~doc:"JSON Tags" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.Tags.of_json tags) ())
           (Some Values.TagResourceOutput.to_json)
           (Some Values.TagResourceOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceOutput.to_json)
           (Some Values.UntagResourceOutput.error_to_json)])
let update_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and workloadVersionName =
         flag "workload-version-name" (optional string)
           ~doc:"STRING WorkloadVersionName"
       and deploymentPatternVersionName =
         flag "deployment-pattern-version-name" (optional string)
           ~doc:"STRING DeploymentPatternVersionName"
       and dryRun = flag "dry-run" (optional bool) ~doc:"BOOL Boolean"
       and force = flag "force" (optional bool) ~doc:"BOOL Boolean"
       and deploymentId =
         flag "deployment-id" (required string) ~doc:"STRING DeploymentId"
       and specifications =
         flag "specifications" (required json_arg)
           ~doc:"JSON DeploymentSpecifications" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_deployment
           (Values.UpdateDeploymentInput.make ?workloadVersionName
              ?deploymentPatternVersionName ?dryRun ?force ~deploymentId
              ~specifications:(Values.DeploymentSpecifications.of_json
                                 specifications) ())
           (Some Values.UpdateDeploymentOutput.to_json)
           (Some Values.UpdateDeploymentOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-deployment", create_deployment);
    ("delete-deployment", delete_deployment);
    ("get-deployment", get_deployment);
    ("get-deployment-pattern-version", get_deployment_pattern_version);
    ("get-workload", get_workload);
    ("get-workload-deployment-pattern", get_workload_deployment_pattern);
    ("list-deployment-events", list_deployment_events);
    ("list-deployment-pattern-versions", list_deployment_pattern_versions);
    ("list-deployments", list_deployments);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-workload-deployment-patterns", list_workload_deployment_patterns);
    ("list-workloads", list_workloads);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-deployment", update_deployment)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | CreateDeployment: (CreateDeploymentInput.t, CreateDeploymentOutput.t,
  CreateDeploymentOutput.error) t 
  | DeleteDeployment: (DeleteDeploymentInput.t, DeleteDeploymentOutput.t,
  DeleteDeploymentOutput.error) t 
  | GetDeployment: (GetDeploymentInput.t, GetDeploymentOutput.t,
  GetDeploymentOutput.error) t 
  | GetDeploymentPatternVersion: (GetDeploymentPatternVersionInput.t,
  GetDeploymentPatternVersionOutput.t,
  GetDeploymentPatternVersionOutput.error) t 
  | GetWorkload: (GetWorkloadInput.t, GetWorkloadOutput.t,
  GetWorkloadOutput.error) t 
  | GetWorkloadDeploymentPattern: (GetWorkloadDeploymentPatternInput.t,
  GetWorkloadDeploymentPatternOutput.t,
  GetWorkloadDeploymentPatternOutput.error) t 
  | ListDeploymentEvents: (ListDeploymentEventsInput.t,
  ListDeploymentEventsOutput.t, ListDeploymentEventsOutput.error) t 
  | ListDeploymentPatternVersions: (ListDeploymentPatternVersionsInput.t,
  ListDeploymentPatternVersionsOutput.t,
  ListDeploymentPatternVersionsOutput.error) t 
  | ListDeployments: (ListDeploymentsInput.t, ListDeploymentsOutput.t,
  ListDeploymentsOutput.error) t 
  | ListTagsForResource: (ListTagsForResourceInput.t,
  ListTagsForResourceOutput.t, ListTagsForResourceOutput.error) t 
  | ListWorkloadDeploymentPatterns: (ListWorkloadDeploymentPatternsInput.t,
  ListWorkloadDeploymentPatternsOutput.t,
  ListWorkloadDeploymentPatternsOutput.error) t 
  | ListWorkloads: (ListWorkloadsInput.t, ListWorkloadsOutput.t,
  ListWorkloadsOutput.error) t 
  | TagResource: (TagResourceInput.t, TagResourceOutput.t,
  TagResourceOutput.error) t 
  | UntagResource: (UntagResourceInput.t, UntagResourceOutput.t,
  UntagResourceOutput.error) t 
  | UpdateDeployment: (UpdateDeploymentInput.t, UpdateDeploymentOutput.t,
  UpdateDeploymentOutput.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | CreateDeployment -> `POST
  | DeleteDeployment -> `POST
  | GetDeployment -> `POST
  | GetDeploymentPatternVersion -> `POST
  | GetWorkload -> `POST
  | GetWorkloadDeploymentPattern -> `POST
  | ListDeploymentEvents -> `POST
  | ListDeploymentPatternVersions -> `POST
  | ListDeployments -> `POST
  | ListTagsForResource -> `GET
  | ListWorkloadDeploymentPatterns -> `POST
  | ListWorkloads -> `POST
  | TagResource -> `POST
  | UntagResource -> `DELETE
  | UpdateDeployment -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | CreateDeployment ->
          (Format.kasprintf Uri.of_string) "/createDeployment"
      | DeleteDeployment ->
          (Format.kasprintf Uri.of_string) "/deleteDeployment"
      | GetDeployment -> (Format.kasprintf Uri.of_string) "/getDeployment"
      | GetDeploymentPatternVersion ->
          (Format.kasprintf Uri.of_string) "/getDeploymentPatternVersion"
      | GetWorkload -> (Format.kasprintf Uri.of_string) "/getWorkload"
      | GetWorkloadDeploymentPattern ->
          (Format.kasprintf Uri.of_string) "/getWorkloadDeploymentPattern"
      | ListDeploymentEvents ->
          (Format.kasprintf Uri.of_string) "/listDeploymentEvents"
      | ListDeploymentPatternVersions ->
          (Format.kasprintf Uri.of_string) "/listDeploymentPatternVersions"
      | ListDeployments ->
          (Format.kasprintf Uri.of_string) "/listDeployments"
      | ListTagsForResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (String_.to_header x.ListTagsForResourceInput.resourceArn)
      | ListWorkloadDeploymentPatterns ->
          (Format.kasprintf Uri.of_string) "/listWorkloadDeploymentPatterns"
      | ListWorkloads -> (Format.kasprintf Uri.of_string) "/listWorkloads"
      | TagResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (String_.to_header x.TagResourceInput.resourceArn)
      | UntagResource ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/tags/%s"
               (String_.to_header x.UntagResourceInput.resourceArn))
            (List.filter_opt
               [Some ("tagKeys", (TagKeyList.to_header x.tagKeys))])
      | UpdateDeployment ->
          (Format.kasprintf Uri.of_string) "/updateDeployment")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | CreateDeployment ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("workloadName",
                           (WorkloadName.to_value
                              req.CreateDeploymentInput.workloadName));
                      Some
                        ("deploymentPatternName",
                          (DeploymentPatternName.to_value
                             req.CreateDeploymentInput.deploymentPatternName));
                      Some
                        ("name",
                          (DeploymentName.to_value
                             req.CreateDeploymentInput.name));
                      Some
                        ("specifications",
                          (DeploymentSpecifications.to_value
                             req.CreateDeploymentInput.specifications));
                      Option.map req.CreateDeploymentInput.dryRun
                        ~f:(fun x -> ("dryRun", (Boolean.to_value x)));
                      Option.map req.CreateDeploymentInput.tags
                        ~f:(fun x -> ("tags", (Tags.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteDeployment ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("deploymentId",
                           (DeploymentId.to_value
                              req.DeleteDeploymentInput.deploymentId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetDeployment ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("deploymentId",
                           (DeploymentId.to_value
                              req.GetDeploymentInput.deploymentId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetDeploymentPatternVersion ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("workloadName",
                           (WorkloadName.to_value
                              req.GetDeploymentPatternVersionInput.workloadName));
                      Some
                        ("deploymentPatternName",
                          (DeploymentPatternName.to_value
                             req.GetDeploymentPatternVersionInput.deploymentPatternName));
                      Some
                        ("deploymentPatternVersionName",
                          (DeploymentPatternVersionName.to_value
                             req.GetDeploymentPatternVersionInput.deploymentPatternVersionName))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetWorkload ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("workloadName",
                           (WorkloadName.to_value
                              req.GetWorkloadInput.workloadName))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetWorkloadDeploymentPattern ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("workloadName",
                           (WorkloadName.to_value
                              req.GetWorkloadDeploymentPatternInput.workloadName));
                      Some
                        ("deploymentPatternName",
                          (DeploymentPatternName.to_value
                             req.GetWorkloadDeploymentPatternInput.deploymentPatternName))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListDeploymentEvents ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("deploymentId",
                           (DeploymentId.to_value
                              req.ListDeploymentEventsInput.deploymentId));
                      Option.map req.ListDeploymentEventsInput.maxResults
                        ~f:(fun x ->
                              ("maxResults",
                                (MaxDeploymentEventResults.to_value x)));
                      Option.map req.ListDeploymentEventsInput.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListDeploymentPatternVersions ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("workloadName",
                           (WorkloadName.to_value
                              req.ListDeploymentPatternVersionsInput.workloadName));
                      Some
                        ("deploymentPatternName",
                          (DeploymentPatternName.to_value
                             req.ListDeploymentPatternVersionsInput.deploymentPatternName));
                      Option.map
                        req.ListDeploymentPatternVersionsInput.maxResults
                        ~f:(fun x ->
                              ("maxResults", (MaxWorkloadResults.to_value x)));
                      Option.map
                        req.ListDeploymentPatternVersionsInput.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)));
                      Option.map
                        req.ListDeploymentPatternVersionsInput.filters
                        ~f:(fun x -> ("filters", (FilterList.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListDeployments ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListDeploymentsInput.filters
                         ~f:(fun x ->
                               ("filters", (DeploymentFilterList.to_value x)));
                      Option.map req.ListDeploymentsInput.maxResults
                        ~f:(fun x ->
                              ("maxResults",
                                (MaxDeploymentResults.to_value x)));
                      Option.map req.ListDeploymentsInput.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTagsForResource ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListWorkloadDeploymentPatterns ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("workloadName",
                           (WorkloadName.to_value
                              req.ListWorkloadDeploymentPatternsInput.workloadName));
                      Option.map
                        req.ListWorkloadDeploymentPatternsInput.maxResults
                        ~f:(fun x ->
                              ("maxResults",
                                (MaxWorkloadDeploymentPatternResults.to_value
                                   x)));
                      Option.map
                        req.ListWorkloadDeploymentPatternsInput.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListWorkloads ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListWorkloadsInput.maxResults
                         ~f:(fun x ->
                               ("maxResults",
                                 (MaxWorkloadResults.to_value x)));
                      Option.map req.ListWorkloadsInput.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | TagResource ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("tags", (Tags.to_value req.TagResourceInput.tags))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UntagResource -> Awso.Http.Request.make (method_of_endpoint endp)
  | UpdateDeployment ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("deploymentId",
                           (DeploymentId.to_value
                              req.UpdateDeploymentInput.deploymentId));
                      Some
                        ("specifications",
                          (DeploymentSpecifications.to_value
                             req.UpdateDeploymentInput.specifications));
                      Option.map
                        req.UpdateDeploymentInput.workloadVersionName
                        ~f:(fun x ->
                              ("workloadVersionName",
                                (WorkloadVersionName.to_value x)));
                      Option.map
                        req.UpdateDeploymentInput.deploymentPatternVersionName
                        ~f:(fun x ->
                              ("deploymentPatternVersionName",
                                (DeploymentPatternVersionName.to_value x)));
                      Option.map req.UpdateDeploymentInput.dryRun
                        ~f:(fun x -> ("dryRun", (Boolean.to_value x)));
                      Option.map req.UpdateDeploymentInput.force
                        ~f:(fun x -> ("force", (Boolean.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | CreateDeployment ->
      if is_success
      then Ok (CreateDeploymentOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some CreateDeploymentOutput.error_of_json))
  | DeleteDeployment ->
      if is_success
      then Ok (DeleteDeploymentOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some DeleteDeploymentOutput.error_of_json))
  | GetDeployment ->
      if is_success
      then Ok (GetDeploymentOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetDeploymentOutput.error_of_json))
  | GetDeploymentPatternVersion ->
      if is_success
      then
        Ok
          (GetDeploymentPatternVersionOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some GetDeploymentPatternVersionOutput.error_of_json))
  | GetWorkload ->
      if is_success
      then Ok (GetWorkloadOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetWorkloadOutput.error_of_json))
  | GetWorkloadDeploymentPattern ->
      if is_success
      then
        Ok
          (GetWorkloadDeploymentPatternOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some GetWorkloadDeploymentPatternOutput.error_of_json))
  | ListDeploymentEvents ->
      if is_success
      then Ok (ListDeploymentEventsOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListDeploymentEventsOutput.error_of_json))
  | ListDeploymentPatternVersions ->
      if is_success
      then
        Ok
          (ListDeploymentPatternVersionsOutput.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some ListDeploymentPatternVersionsOutput.error_of_json))
  | ListDeployments ->
      if is_success
      then Ok (ListDeploymentsOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListDeploymentsOutput.error_of_json))
  | ListTagsForResource ->
      if is_success
      then Ok (ListTagsForResourceOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListTagsForResourceOutput.error_of_json))
  | ListWorkloadDeploymentPatterns ->
      if is_success
      then
        Ok
          (ListWorkloadDeploymentPatternsOutput.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some ListWorkloadDeploymentPatternsOutput.error_of_json))
  | ListWorkloads ->
      if is_success
      then Ok (ListWorkloadsOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListWorkloadsOutput.error_of_json))
  | TagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (TagResourceOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some TagResourceOutput.error_of_json))
  | UntagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (UntagResourceOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some UntagResourceOutput.error_of_json))
  | UpdateDeployment ->
      if is_success
      then Ok (UpdateDeploymentOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some UpdateDeploymentOutput.error_of_json))
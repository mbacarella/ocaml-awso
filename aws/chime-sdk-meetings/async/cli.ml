(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_create_attendee =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString"
       and attendees =
         flag "attendees" (required json_arg)
           ~doc:"JSON CreateAttendeeRequestItemList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_create_attendee
           (Values.BatchCreateAttendeeRequest.make ~meetingId
              ~attendees:(Values.CreateAttendeeRequestItemList.of_json
                            attendees) ())
           (Some Values.BatchCreateAttendeeResponse.to_json)
           (Some Values.BatchCreateAttendeeResponse.error_to_json)])
let batch_update_attendee_capabilities_except =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString"
       and excludedAttendeeIds =
         flag "excluded-attendee-ids" (required json_arg)
           ~doc:"JSON AttendeeIdsList"
       and capabilities =
         flag "capabilities" (required json_arg)
           ~doc:"JSON AttendeeCapabilities" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_update_attendee_capabilities_except
           (Values.BatchUpdateAttendeeCapabilitiesExceptRequest.make
              ~meetingId
              ~excludedAttendeeIds:(Values.AttendeeIdsList.of_json
                                      excludedAttendeeIds)
              ~capabilities:(Values.AttendeeCapabilities.of_json capabilities)
              ()) None None])
let create_attendee =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and capabilities =
         flag "capabilities" (optional json_arg)
           ~doc:"JSON AttendeeCapabilities"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString"
       and externalUserId =
         flag "external-user-id" (required string)
           ~doc:"STRING ExternalUserId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_attendee
           (Values.CreateAttendeeRequest.make
              ?capabilities:(Option.map
                               ~f:Values.AttendeeCapabilities.of_json
                               capabilities) ~meetingId ~externalUserId ())
           (Some Values.CreateAttendeeResponse.to_json)
           (Some Values.CreateAttendeeResponse.error_to_json)])
let create_meeting =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meetingHostId =
         flag "meeting-host-id" (optional string)
           ~doc:"STRING ExternalUserId"
       and notificationsConfiguration =
         flag "notifications-configuration" (optional json_arg)
           ~doc:"JSON NotificationsConfiguration"
       and meetingFeatures =
         flag "meeting-features" (optional json_arg)
           ~doc:"JSON MeetingFeaturesConfiguration"
       and primaryMeetingId =
         flag "primary-meeting-id" (optional string)
           ~doc:"STRING PrimaryMeetingId"
       and tenantIds =
         flag "tenant-ids" (optional json_arg) ~doc:"JSON TenantIdList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and mediaPlacementNetworkType =
         flag "media-placement-network-type" (optional json_arg)
           ~doc:"JSON MediaPlacementNetworkType"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken"
       and mediaRegion =
         flag "media-region" (required string) ~doc:"STRING MediaRegion"
       and externalMeetingId =
         flag "external-meeting-id" (required string)
           ~doc:"STRING ExternalMeetingId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_meeting
           (Values.CreateMeetingRequest.make ?meetingHostId
              ?notificationsConfiguration:(Option.map
                                             ~f:Values.NotificationsConfiguration.of_json
                                             notificationsConfiguration)
              ?meetingFeatures:(Option.map
                                  ~f:Values.MeetingFeaturesConfiguration.of_json
                                  meetingFeatures) ?primaryMeetingId
              ?tenantIds:(Option.map ~f:Values.TenantIdList.of_json tenantIds)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?mediaPlacementNetworkType:(Option.map
                                            ~f:Values.MediaPlacementNetworkType.of_json
                                            mediaPlacementNetworkType)
              ~clientRequestToken ~mediaRegion ~externalMeetingId ())
           (Some Values.CreateMeetingResponse.to_json)
           (Some Values.CreateMeetingResponse.error_to_json)])
let create_meeting_with_attendees =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meetingHostId =
         flag "meeting-host-id" (optional string)
           ~doc:"STRING ExternalUserId"
       and meetingFeatures =
         flag "meeting-features" (optional json_arg)
           ~doc:"JSON MeetingFeaturesConfiguration"
       and notificationsConfiguration =
         flag "notifications-configuration" (optional json_arg)
           ~doc:"JSON NotificationsConfiguration"
       and primaryMeetingId =
         flag "primary-meeting-id" (optional string)
           ~doc:"STRING PrimaryMeetingId"
       and tenantIds =
         flag "tenant-ids" (optional json_arg) ~doc:"JSON TenantIdList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and mediaPlacementNetworkType =
         flag "media-placement-network-type" (optional json_arg)
           ~doc:"JSON MediaPlacementNetworkType"
       and clientRequestToken =
         flag "client-request-token" (required string)
           ~doc:"STRING ClientRequestToken"
       and mediaRegion =
         flag "media-region" (required string) ~doc:"STRING MediaRegion"
       and externalMeetingId =
         flag "external-meeting-id" (required string)
           ~doc:"STRING ExternalMeetingId"
       and attendees =
         flag "attendees" (required json_arg)
           ~doc:"JSON CreateMeetingWithAttendeesRequestItemList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_meeting_with_attendees
           (Values.CreateMeetingWithAttendeesRequest.make ?meetingHostId
              ?meetingFeatures:(Option.map
                                  ~f:Values.MeetingFeaturesConfiguration.of_json
                                  meetingFeatures)
              ?notificationsConfiguration:(Option.map
                                             ~f:Values.NotificationsConfiguration.of_json
                                             notificationsConfiguration)
              ?primaryMeetingId
              ?tenantIds:(Option.map ~f:Values.TenantIdList.of_json tenantIds)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?mediaPlacementNetworkType:(Option.map
                                            ~f:Values.MediaPlacementNetworkType.of_json
                                            mediaPlacementNetworkType)
              ~clientRequestToken ~mediaRegion ~externalMeetingId
              ~attendees:(Values.CreateMeetingWithAttendeesRequestItemList.of_json
                            attendees) ())
           (Some Values.CreateMeetingWithAttendeesResponse.to_json)
           (Some Values.CreateMeetingWithAttendeesResponse.error_to_json)])
let delete_attendee =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString"
       and attendeeId =
         flag "attendee-id" (required string) ~doc:"STRING GuidString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_attendee
           (Values.DeleteAttendeeRequest.make ~meetingId ~attendeeId ()) None
           None])
let delete_meeting =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_meeting (Values.DeleteMeetingRequest.make ~meetingId ())
           None None])
let get_attendee =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString"
       and attendeeId =
         flag "attendee-id" (required string) ~doc:"STRING GuidString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_attendee
           (Values.GetAttendeeRequest.make ~meetingId ~attendeeId ())
           (Some Values.GetAttendeeResponse.to_json)
           (Some Values.GetAttendeeResponse.error_to_json)])
let get_meeting =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_meeting (Values.GetMeetingRequest.make ~meetingId ())
           (Some Values.GetMeetingResponse.to_json)
           (Some Values.GetMeetingResponse.error_to_json)])
let list_attendees =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ResultMax"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_attendees
           (Values.ListAttendeesRequest.make ?nextToken ?maxResults
              ~meetingId ()) (Some Values.ListAttendeesResponse.to_json)
           (Some Values.ListAttendeesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let start_meeting_transcription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString"
       and transcriptionConfiguration =
         flag "transcription-configuration" (required json_arg)
           ~doc:"JSON TranscriptionConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_meeting_transcription
           (Values.StartMeetingTranscriptionRequest.make ~meetingId
              ~transcriptionConfiguration:(Values.TranscriptionConfiguration.of_json
                                             transcriptionConfiguration) ())
           None None])
let stop_meeting_transcription =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_meeting_transcription
           (Values.StopMeetingTranscriptionRequest.make ~meetingId ()) None
           None])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_attendee_capabilities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and meetingId =
         flag "meeting-id" (required string) ~doc:"STRING GuidString"
       and attendeeId =
         flag "attendee-id" (required string) ~doc:"STRING GuidString"
       and capabilities =
         flag "capabilities" (required json_arg)
           ~doc:"JSON AttendeeCapabilities" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_attendee_capabilities
           (Values.UpdateAttendeeCapabilitiesRequest.make ~meetingId
              ~attendeeId
              ~capabilities:(Values.AttendeeCapabilities.of_json capabilities)
              ()) (Some Values.UpdateAttendeeCapabilitiesResponse.to_json)
           (Some Values.UpdateAttendeeCapabilitiesResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("batch-create-attendee", batch_create_attendee);
    ("batch-update-attendee-capabilities-except",
      batch_update_attendee_capabilities_except);
    ("create-attendee", create_attendee);
    ("create-meeting", create_meeting);
    ("create-meeting-with-attendees", create_meeting_with_attendees);
    ("delete-attendee", delete_attendee);
    ("delete-meeting", delete_meeting);
    ("get-attendee", get_attendee);
    ("get-meeting", get_meeting);
    ("list-attendees", list_attendees);
    ("list-tags-for-resource", list_tags_for_resource);
    ("start-meeting-transcription", start_meeting_transcription);
    ("stop-meeting-transcription", stop_meeting_transcription);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-attendee-capabilities", update_attendee_capabilities)]
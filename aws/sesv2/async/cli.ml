(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_configuration_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trackingOptions =
         flag "tracking-options" (optional json_arg)
           ~doc:"JSON TrackingOptions"
       and deliveryOptions =
         flag "delivery-options" (optional json_arg)
           ~doc:"JSON DeliveryOptions"
       and reputationOptions =
         flag "reputation-options" (optional json_arg)
           ~doc:"JSON ReputationOptions"
       and sendingOptions =
         flag "sending-options" (optional json_arg)
           ~doc:"JSON SendingOptions"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and suppressionOptions =
         flag "suppression-options" (optional json_arg)
           ~doc:"JSON SuppressionOptions"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_configuration_set
           (Values.CreateConfigurationSetRequest.make
              ?trackingOptions:(Option.map ~f:Values.TrackingOptions.of_json
                                  trackingOptions)
              ?deliveryOptions:(Option.map ~f:Values.DeliveryOptions.of_json
                                  deliveryOptions)
              ?reputationOptions:(Option.map
                                    ~f:Values.ReputationOptions.of_json
                                    reputationOptions)
              ?sendingOptions:(Option.map ~f:Values.SendingOptions.of_json
                                 sendingOptions)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?suppressionOptions:(Option.map
                                     ~f:Values.SuppressionOptions.of_json
                                     suppressionOptions)
              ~configurationSetName ())
           (Some Values.CreateConfigurationSetResponse.to_json)
           (Some Values.CreateConfigurationSetResponse.error_to_json)])
let create_configuration_set_event_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName"
       and eventDestinationName =
         flag "event-destination-name" (required string)
           ~doc:"STRING EventDestinationName"
       and eventDestination =
         flag "event-destination" (required json_arg)
           ~doc:"JSON EventDestinationDefinition" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_configuration_set_event_destination
           (Values.CreateConfigurationSetEventDestinationRequest.make
              ~configurationSetName ~eventDestinationName
              ~eventDestination:(Values.EventDestinationDefinition.of_json
                                   eventDestination) ())
           (Some
              Values.CreateConfigurationSetEventDestinationResponse.to_json)
           (Some
              Values.CreateConfigurationSetEventDestinationResponse.error_to_json)])
let create_contact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and topicPreferences =
         flag "topic-preferences" (optional json_arg)
           ~doc:"JSON TopicPreferenceList"
       and unsubscribeAll =
         flag "unsubscribe-all" (optional bool) ~doc:"BOOL UnsubscribeAll"
       and attributesData =
         flag "attributes-data" (optional string)
           ~doc:"STRING AttributesData"
       and contactListName =
         flag "contact-list-name" (required string)
           ~doc:"STRING ContactListName"
       and emailAddress =
         flag "email-address" (required string) ~doc:"STRING EmailAddress" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_contact
           (Values.CreateContactRequest.make
              ?topicPreferences:(Option.map
                                   ~f:Values.TopicPreferenceList.of_json
                                   topicPreferences) ?unsubscribeAll
              ?attributesData ~contactListName ~emailAddress ())
           (Some Values.CreateContactResponse.to_json)
           (Some Values.CreateContactResponse.error_to_json)])
let create_contact_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and topics = flag "topics" (optional json_arg) ~doc:"JSON Topics"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and contactListName =
         flag "contact-list-name" (required string)
           ~doc:"STRING ContactListName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_contact_list
           (Values.CreateContactListRequest.make
              ?topics:(Option.map ~f:Values.Topics.of_json topics)
              ?description ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~contactListName ())
           (Some Values.CreateContactListResponse.to_json)
           (Some Values.CreateContactListResponse.error_to_json)])
let create_custom_verification_email_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (required string)
           ~doc:"STRING EmailTemplateName"
       and fromEmailAddress =
         flag "from-email-address" (required string)
           ~doc:"STRING EmailAddress"
       and templateSubject =
         flag "template-subject" (required string)
           ~doc:"STRING EmailTemplateSubject"
       and templateContent =
         flag "template-content" (required string)
           ~doc:"STRING TemplateContent"
       and successRedirectionURL =
         flag "success-redirection-u-r-l" (required string)
           ~doc:"STRING SuccessRedirectionURL"
       and failureRedirectionURL =
         flag "failure-redirection-u-r-l" (required string)
           ~doc:"STRING FailureRedirectionURL" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_custom_verification_email_template
           (Values.CreateCustomVerificationEmailTemplateRequest.make
              ~templateName ~fromEmailAddress ~templateSubject
              ~templateContent ~successRedirectionURL ~failureRedirectionURL
              ())
           (Some Values.CreateCustomVerificationEmailTemplateResponse.to_json)
           (Some
              Values.CreateCustomVerificationEmailTemplateResponse.error_to_json)])
let create_dedicated_ip_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and poolName =
         flag "pool-name" (required string) ~doc:"STRING PoolName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_dedicated_ip_pool
           (Values.CreateDedicatedIpPoolRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~poolName ())
           (Some Values.CreateDedicatedIpPoolResponse.to_json)
           (Some Values.CreateDedicatedIpPoolResponse.error_to_json)])
let create_deliverability_test_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reportName =
         flag "report-name" (optional string) ~doc:"STRING ReportName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and fromEmailAddress =
         flag "from-email-address" (required string)
           ~doc:"STRING EmailAddress"
       and content =
         flag "content" (required json_arg) ~doc:"JSON EmailContent" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_deliverability_test_report
           (Values.CreateDeliverabilityTestReportRequest.make ?reportName
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~fromEmailAddress
              ~content:(Values.EmailContent.of_json content) ())
           (Some Values.CreateDeliverabilityTestReportResponse.to_json)
           (Some Values.CreateDeliverabilityTestReportResponse.error_to_json)])
let create_email_identity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and dkimSigningAttributes =
         flag "dkim-signing-attributes" (optional json_arg)
           ~doc:"JSON DkimSigningAttributes"
       and configurationSetName =
         flag "configuration-set-name" (optional string)
           ~doc:"STRING ConfigurationSetName"
       and emailIdentity =
         flag "email-identity" (required string) ~doc:"STRING Identity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_email_identity
           (Values.CreateEmailIdentityRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?dkimSigningAttributes:(Option.map
                                        ~f:Values.DkimSigningAttributes.of_json
                                        dkimSigningAttributes)
              ?configurationSetName ~emailIdentity ())
           (Some Values.CreateEmailIdentityResponse.to_json)
           (Some Values.CreateEmailIdentityResponse.error_to_json)])
let create_email_identity_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and emailIdentity =
         flag "email-identity" (required string) ~doc:"STRING Identity"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING PolicyName"
       and policy = flag "policy" (required string) ~doc:"STRING Policy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_email_identity_policy
           (Values.CreateEmailIdentityPolicyRequest.make ~emailIdentity
              ~policyName ~policy ())
           (Some Values.CreateEmailIdentityPolicyResponse.to_json)
           (Some Values.CreateEmailIdentityPolicyResponse.error_to_json)])
let create_email_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (required string)
           ~doc:"STRING EmailTemplateName"
       and templateContent =
         flag "template-content" (required json_arg)
           ~doc:"JSON EmailTemplateContent" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_email_template
           (Values.CreateEmailTemplateRequest.make ~templateName
              ~templateContent:(Values.EmailTemplateContent.of_json
                                  templateContent) ())
           (Some Values.CreateEmailTemplateResponse.to_json)
           (Some Values.CreateEmailTemplateResponse.error_to_json)])
let create_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and importDestination =
         flag "import-destination" (required json_arg)
           ~doc:"JSON ImportDestination"
       and importDataSource =
         flag "import-data-source" (required json_arg)
           ~doc:"JSON ImportDataSource" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_import_job
           (Values.CreateImportJobRequest.make
              ~importDestination:(Values.ImportDestination.of_json
                                    importDestination)
              ~importDataSource:(Values.ImportDataSource.of_json
                                   importDataSource) ())
           (Some Values.CreateImportJobResponse.to_json)
           (Some Values.CreateImportJobResponse.error_to_json)])
let delete_configuration_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_configuration_set
           (Values.DeleteConfigurationSetRequest.make ~configurationSetName
              ()) (Some Values.DeleteConfigurationSetResponse.to_json)
           (Some Values.DeleteConfigurationSetResponse.error_to_json)])
let delete_configuration_set_event_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName"
       and eventDestinationName =
         flag "event-destination-name" (required string)
           ~doc:"STRING EventDestinationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_configuration_set_event_destination
           (Values.DeleteConfigurationSetEventDestinationRequest.make
              ~configurationSetName ~eventDestinationName ())
           (Some
              Values.DeleteConfigurationSetEventDestinationResponse.to_json)
           (Some
              Values.DeleteConfigurationSetEventDestinationResponse.error_to_json)])
let delete_contact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contactListName =
         flag "contact-list-name" (required string)
           ~doc:"STRING ContactListName"
       and emailAddress =
         flag "email-address" (required string) ~doc:"STRING EmailAddress" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_contact
           (Values.DeleteContactRequest.make ~contactListName ~emailAddress
              ()) (Some Values.DeleteContactResponse.to_json)
           (Some Values.DeleteContactResponse.error_to_json)])
let delete_contact_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contactListName =
         flag "contact-list-name" (required string)
           ~doc:"STRING ContactListName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_contact_list
           (Values.DeleteContactListRequest.make ~contactListName ())
           (Some Values.DeleteContactListResponse.to_json)
           (Some Values.DeleteContactListResponse.error_to_json)])
let delete_custom_verification_email_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (required string)
           ~doc:"STRING EmailTemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_custom_verification_email_template
           (Values.DeleteCustomVerificationEmailTemplateRequest.make
              ~templateName ())
           (Some Values.DeleteCustomVerificationEmailTemplateResponse.to_json)
           (Some
              Values.DeleteCustomVerificationEmailTemplateResponse.error_to_json)])
let delete_dedicated_ip_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and poolName =
         flag "pool-name" (required string) ~doc:"STRING PoolName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_dedicated_ip_pool
           (Values.DeleteDedicatedIpPoolRequest.make ~poolName ())
           (Some Values.DeleteDedicatedIpPoolResponse.to_json)
           (Some Values.DeleteDedicatedIpPoolResponse.error_to_json)])
let delete_email_identity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and emailIdentity =
         flag "email-identity" (required string) ~doc:"STRING Identity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_email_identity
           (Values.DeleteEmailIdentityRequest.make ~emailIdentity ())
           (Some Values.DeleteEmailIdentityResponse.to_json)
           (Some Values.DeleteEmailIdentityResponse.error_to_json)])
let delete_email_identity_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and emailIdentity =
         flag "email-identity" (required string) ~doc:"STRING Identity"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING PolicyName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_email_identity_policy
           (Values.DeleteEmailIdentityPolicyRequest.make ~emailIdentity
              ~policyName ())
           (Some Values.DeleteEmailIdentityPolicyResponse.to_json)
           (Some Values.DeleteEmailIdentityPolicyResponse.error_to_json)])
let delete_email_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (required string)
           ~doc:"STRING EmailTemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_email_template
           (Values.DeleteEmailTemplateRequest.make ~templateName ())
           (Some Values.DeleteEmailTemplateResponse.to_json)
           (Some Values.DeleteEmailTemplateResponse.error_to_json)])
let delete_suppressed_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and emailAddress =
         flag "email-address" (required string) ~doc:"STRING EmailAddress" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_suppressed_destination
           (Values.DeleteSuppressedDestinationRequest.make ~emailAddress ())
           (Some Values.DeleteSuppressedDestinationResponse.to_json)
           (Some Values.DeleteSuppressedDestinationResponse.error_to_json)])
let get_account =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_account (Values.GetAccountRequest.make ())
           (Some Values.GetAccountResponse.to_json)
           (Some Values.GetAccountResponse.error_to_json)])
let get_blacklist_reports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and blacklistItemNames =
         flag "blacklist-item-names" (required json_arg)
           ~doc:"JSON BlacklistItemNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_blacklist_reports
           (Values.GetBlacklistReportsRequest.make
              ~blacklistItemNames:(Values.BlacklistItemNames.of_json
                                     blacklistItemNames) ())
           (Some Values.GetBlacklistReportsResponse.to_json)
           (Some Values.GetBlacklistReportsResponse.error_to_json)])
let get_configuration_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_configuration_set
           (Values.GetConfigurationSetRequest.make ~configurationSetName ())
           (Some Values.GetConfigurationSetResponse.to_json)
           (Some Values.GetConfigurationSetResponse.error_to_json)])
let get_configuration_set_event_destinations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_configuration_set_event_destinations
           (Values.GetConfigurationSetEventDestinationsRequest.make
              ~configurationSetName ())
           (Some Values.GetConfigurationSetEventDestinationsResponse.to_json)
           (Some
              Values.GetConfigurationSetEventDestinationsResponse.error_to_json)])
let get_contact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contactListName =
         flag "contact-list-name" (required string)
           ~doc:"STRING ContactListName"
       and emailAddress =
         flag "email-address" (required string) ~doc:"STRING EmailAddress" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_contact
           (Values.GetContactRequest.make ~contactListName ~emailAddress ())
           (Some Values.GetContactResponse.to_json)
           (Some Values.GetContactResponse.error_to_json)])
let get_contact_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contactListName =
         flag "contact-list-name" (required string)
           ~doc:"STRING ContactListName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_contact_list
           (Values.GetContactListRequest.make ~contactListName ())
           (Some Values.GetContactListResponse.to_json)
           (Some Values.GetContactListResponse.error_to_json)])
let get_custom_verification_email_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (required string)
           ~doc:"STRING EmailTemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_custom_verification_email_template
           (Values.GetCustomVerificationEmailTemplateRequest.make
              ~templateName ())
           (Some Values.GetCustomVerificationEmailTemplateResponse.to_json)
           (Some
              Values.GetCustomVerificationEmailTemplateResponse.error_to_json)])
let get_dedicated_ip =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ip = flag "ip" (required string) ~doc:"STRING Ip" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_dedicated_ip (Values.GetDedicatedIpRequest.make ~ip ())
           (Some Values.GetDedicatedIpResponse.to_json)
           (Some Values.GetDedicatedIpResponse.error_to_json)])
let get_dedicated_ips =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and poolName =
         flag "pool-name" (optional string) ~doc:"STRING PoolName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT MaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_dedicated_ips
           (Values.GetDedicatedIpsRequest.make ?poolName ?nextToken ?pageSize
              ()) (Some Values.GetDedicatedIpsResponse.to_json)
           (Some Values.GetDedicatedIpsResponse.error_to_json)])
let get_deliverability_dashboard_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_deliverability_dashboard_options
           (Values.GetDeliverabilityDashboardOptionsRequest.make ())
           (Some Values.GetDeliverabilityDashboardOptionsResponse.to_json)
           (Some
              Values.GetDeliverabilityDashboardOptionsResponse.error_to_json)])
let get_deliverability_test_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reportId =
         flag "report-id" (required string) ~doc:"STRING ReportId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_deliverability_test_report
           (Values.GetDeliverabilityTestReportRequest.make ~reportId ())
           (Some Values.GetDeliverabilityTestReportResponse.to_json)
           (Some Values.GetDeliverabilityTestReportResponse.error_to_json)])
let get_domain_deliverability_campaign =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and campaignId =
         flag "campaign-id" (required string) ~doc:"STRING CampaignId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_domain_deliverability_campaign
           (Values.GetDomainDeliverabilityCampaignRequest.make ~campaignId ())
           (Some Values.GetDomainDeliverabilityCampaignResponse.to_json)
           (Some Values.GetDomainDeliverabilityCampaignResponse.error_to_json)])
let get_domain_statistics_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and domain = flag "domain" (required string) ~doc:"STRING Identity"
       and startDate =
         flag "start-date" (required json_arg) ~doc:"JSON Timestamp"
       and endDate =
         flag "end-date" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_domain_statistics_report
           (Values.GetDomainStatisticsReportRequest.make ~domain
              ~startDate:(Values.Timestamp.of_json startDate)
              ~endDate:(Values.Timestamp.of_json endDate) ())
           (Some Values.GetDomainStatisticsReportResponse.to_json)
           (Some Values.GetDomainStatisticsReportResponse.error_to_json)])
let get_email_identity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and emailIdentity =
         flag "email-identity" (required string) ~doc:"STRING Identity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_email_identity
           (Values.GetEmailIdentityRequest.make ~emailIdentity ())
           (Some Values.GetEmailIdentityResponse.to_json)
           (Some Values.GetEmailIdentityResponse.error_to_json)])
let get_email_identity_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and emailIdentity =
         flag "email-identity" (required string) ~doc:"STRING Identity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_email_identity_policies
           (Values.GetEmailIdentityPoliciesRequest.make ~emailIdentity ())
           (Some Values.GetEmailIdentityPoliciesResponse.to_json)
           (Some Values.GetEmailIdentityPoliciesResponse.error_to_json)])
let get_email_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (required string)
           ~doc:"STRING EmailTemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_email_template
           (Values.GetEmailTemplateRequest.make ~templateName ())
           (Some Values.GetEmailTemplateResponse.to_json)
           (Some Values.GetEmailTemplateResponse.error_to_json)])
let get_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_import_job (Values.GetImportJobRequest.make ~jobId ())
           (Some Values.GetImportJobResponse.to_json)
           (Some Values.GetImportJobResponse.error_to_json)])
let get_suppressed_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and emailAddress =
         flag "email-address" (required string) ~doc:"STRING EmailAddress" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_suppressed_destination
           (Values.GetSuppressedDestinationRequest.make ~emailAddress ())
           (Some Values.GetSuppressedDestinationResponse.to_json)
           (Some Values.GetSuppressedDestinationResponse.error_to_json)])
let list_configuration_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT MaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_configuration_sets
           (Values.ListConfigurationSetsRequest.make ?nextToken ?pageSize ())
           (Some Values.ListConfigurationSetsResponse.to_json)
           (Some Values.ListConfigurationSetsResponse.error_to_json)])
let list_contact_lists =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and pageSize = flag "page-size" (optional int) ~doc:"INT MaxItems"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_contact_lists
           (Values.ListContactListsRequest.make ?pageSize ?nextToken ())
           (Some Values.ListContactListsResponse.to_json)
           (Some Values.ListContactListsResponse.error_to_json)])
let list_contacts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON ListContactsFilter"
       and pageSize = flag "page-size" (optional int) ~doc:"INT MaxItems"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and contactListName =
         flag "contact-list-name" (required string)
           ~doc:"STRING ContactListName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_contacts
           (Values.ListContactsRequest.make
              ?filter:(Option.map ~f:Values.ListContactsFilter.of_json filter)
              ?pageSize ?nextToken ~contactListName ())
           (Some Values.ListContactsResponse.to_json)
           (Some Values.ListContactsResponse.error_to_json)])
let list_custom_verification_email_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT MaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_custom_verification_email_templates
           (Values.ListCustomVerificationEmailTemplatesRequest.make
              ?nextToken ?pageSize ())
           (Some Values.ListCustomVerificationEmailTemplatesResponse.to_json)
           (Some
              Values.ListCustomVerificationEmailTemplatesResponse.error_to_json)])
let list_dedicated_ip_pools =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT MaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_dedicated_ip_pools
           (Values.ListDedicatedIpPoolsRequest.make ?nextToken ?pageSize ())
           (Some Values.ListDedicatedIpPoolsResponse.to_json)
           (Some Values.ListDedicatedIpPoolsResponse.error_to_json)])
let list_deliverability_test_reports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT MaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_deliverability_test_reports
           (Values.ListDeliverabilityTestReportsRequest.make ?nextToken
              ?pageSize ())
           (Some Values.ListDeliverabilityTestReportsResponse.to_json)
           (Some Values.ListDeliverabilityTestReportsResponse.error_to_json)])
let list_domain_deliverability_campaigns =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT MaxItems"
       and startDate =
         flag "start-date" (required json_arg) ~doc:"JSON Timestamp"
       and endDate =
         flag "end-date" (required json_arg) ~doc:"JSON Timestamp"
       and subscribedDomain =
         flag "subscribed-domain" (required string) ~doc:"STRING Domain" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_domain_deliverability_campaigns
           (Values.ListDomainDeliverabilityCampaignsRequest.make ?nextToken
              ?pageSize ~startDate:(Values.Timestamp.of_json startDate)
              ~endDate:(Values.Timestamp.of_json endDate) ~subscribedDomain
              ())
           (Some Values.ListDomainDeliverabilityCampaignsResponse.to_json)
           (Some
              Values.ListDomainDeliverabilityCampaignsResponse.error_to_json)])
let list_email_identities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT MaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_email_identities
           (Values.ListEmailIdentitiesRequest.make ?nextToken ?pageSize ())
           (Some Values.ListEmailIdentitiesResponse.to_json)
           (Some Values.ListEmailIdentitiesResponse.error_to_json)])
let list_email_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT MaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_email_templates
           (Values.ListEmailTemplatesRequest.make ?nextToken ?pageSize ())
           (Some Values.ListEmailTemplatesResponse.to_json)
           (Some Values.ListEmailTemplatesResponse.error_to_json)])
let list_import_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and importDestinationType =
         flag "import-destination-type" (optional json_arg)
           ~doc:"JSON ImportDestinationType"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT MaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_import_jobs
           (Values.ListImportJobsRequest.make
              ?importDestinationType:(Option.map
                                        ~f:Values.ImportDestinationType.of_json
                                        importDestinationType) ?nextToken
              ?pageSize ()) (Some Values.ListImportJobsResponse.to_json)
           (Some Values.ListImportJobsResponse.error_to_json)])
let list_suppressed_destinations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reasons =
         flag "reasons" (optional json_arg)
           ~doc:"JSON SuppressionListReasons"
       and startDate =
         flag "start-date" (optional json_arg) ~doc:"JSON Timestamp"
       and endDate =
         flag "end-date" (optional json_arg) ~doc:"JSON Timestamp"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and pageSize = flag "page-size" (optional int) ~doc:"INT MaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_suppressed_destinations
           (Values.ListSuppressedDestinationsRequest.make
              ?reasons:(Option.map ~f:Values.SuppressionListReasons.of_json
                          reasons)
              ?startDate:(Option.map ~f:Values.Timestamp.of_json startDate)
              ?endDate:(Option.map ~f:Values.Timestamp.of_json endDate)
              ?nextToken ?pageSize ())
           (Some Values.ListSuppressedDestinationsResponse.to_json)
           (Some Values.ListSuppressedDestinationsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_account_dedicated_ip_warmup_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoWarmupEnabled =
         flag "auto-warmup-enabled" (optional bool) ~doc:"BOOL Enabled" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_account_dedicated_ip_warmup_attributes
           (Values.PutAccountDedicatedIpWarmupAttributesRequest.make
              ?autoWarmupEnabled ())
           (Some Values.PutAccountDedicatedIpWarmupAttributesResponse.to_json)
           (Some
              Values.PutAccountDedicatedIpWarmupAttributesResponse.error_to_json)])
let put_account_details =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contactLanguage =
         flag "contact-language" (optional json_arg)
           ~doc:"JSON ContactLanguage"
       and additionalContactEmailAddresses =
         flag "additional-contact-email-addresses" (optional json_arg)
           ~doc:"JSON AdditionalContactEmailAddresses"
       and productionAccessEnabled =
         flag "production-access-enabled" (optional bool)
           ~doc:"BOOL EnabledWrapper"
       and mailType =
         flag "mail-type" (required json_arg) ~doc:"JSON MailType"
       and websiteURL =
         flag "website-u-r-l" (required string) ~doc:"STRING WebsiteURL"
       and useCaseDescription =
         flag "use-case-description" (required string)
           ~doc:"STRING UseCaseDescription" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_account_details
           (Values.PutAccountDetailsRequest.make
              ?contactLanguage:(Option.map ~f:Values.ContactLanguage.of_json
                                  contactLanguage)
              ?additionalContactEmailAddresses:(Option.map
                                                  ~f:Values.AdditionalContactEmailAddresses.of_json
                                                  additionalContactEmailAddresses)
              ?productionAccessEnabled
              ~mailType:(Values.MailType.of_json mailType) ~websiteURL
              ~useCaseDescription ())
           (Some Values.PutAccountDetailsResponse.to_json)
           (Some Values.PutAccountDetailsResponse.error_to_json)])
let put_account_sending_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sendingEnabled =
         flag "sending-enabled" (optional bool) ~doc:"BOOL Enabled" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_account_sending_attributes
           (Values.PutAccountSendingAttributesRequest.make ?sendingEnabled ())
           (Some Values.PutAccountSendingAttributesResponse.to_json)
           (Some Values.PutAccountSendingAttributesResponse.error_to_json)])
let put_account_suppression_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and suppressedReasons =
         flag "suppressed-reasons" (optional json_arg)
           ~doc:"JSON SuppressionListReasons" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_account_suppression_attributes
           (Values.PutAccountSuppressionAttributesRequest.make
              ?suppressedReasons:(Option.map
                                    ~f:Values.SuppressionListReasons.of_json
                                    suppressedReasons) ())
           (Some Values.PutAccountSuppressionAttributesResponse.to_json)
           (Some Values.PutAccountSuppressionAttributesResponse.error_to_json)])
let put_configuration_set_delivery_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tlsPolicy =
         flag "tls-policy" (optional json_arg) ~doc:"JSON TlsPolicy"
       and sendingPoolName =
         flag "sending-pool-name" (optional string)
           ~doc:"STRING SendingPoolName"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_configuration_set_delivery_options
           (Values.PutConfigurationSetDeliveryOptionsRequest.make
              ?tlsPolicy:(Option.map ~f:Values.TlsPolicy.of_json tlsPolicy)
              ?sendingPoolName ~configurationSetName ())
           (Some Values.PutConfigurationSetDeliveryOptionsResponse.to_json)
           (Some
              Values.PutConfigurationSetDeliveryOptionsResponse.error_to_json)])
let put_configuration_set_reputation_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reputationMetricsEnabled =
         flag "reputation-metrics-enabled" (optional bool)
           ~doc:"BOOL Enabled"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_configuration_set_reputation_options
           (Values.PutConfigurationSetReputationOptionsRequest.make
              ?reputationMetricsEnabled ~configurationSetName ())
           (Some Values.PutConfigurationSetReputationOptionsResponse.to_json)
           (Some
              Values.PutConfigurationSetReputationOptionsResponse.error_to_json)])
let put_configuration_set_sending_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sendingEnabled =
         flag "sending-enabled" (optional bool) ~doc:"BOOL Enabled"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_configuration_set_sending_options
           (Values.PutConfigurationSetSendingOptionsRequest.make
              ?sendingEnabled ~configurationSetName ())
           (Some Values.PutConfigurationSetSendingOptionsResponse.to_json)
           (Some
              Values.PutConfigurationSetSendingOptionsResponse.error_to_json)])
let put_configuration_set_suppression_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and suppressedReasons =
         flag "suppressed-reasons" (optional json_arg)
           ~doc:"JSON SuppressionListReasons"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_configuration_set_suppression_options
           (Values.PutConfigurationSetSuppressionOptionsRequest.make
              ?suppressedReasons:(Option.map
                                    ~f:Values.SuppressionListReasons.of_json
                                    suppressedReasons) ~configurationSetName
              ())
           (Some Values.PutConfigurationSetSuppressionOptionsResponse.to_json)
           (Some
              Values.PutConfigurationSetSuppressionOptionsResponse.error_to_json)])
let put_configuration_set_tracking_options =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and customRedirectDomain =
         flag "custom-redirect-domain" (optional string)
           ~doc:"STRING CustomRedirectDomain"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_configuration_set_tracking_options
           (Values.PutConfigurationSetTrackingOptionsRequest.make
              ?customRedirectDomain ~configurationSetName ())
           (Some Values.PutConfigurationSetTrackingOptionsResponse.to_json)
           (Some
              Values.PutConfigurationSetTrackingOptionsResponse.error_to_json)])
let put_dedicated_ip_in_pool =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ip = flag "ip" (required string) ~doc:"STRING Ip"
       and destinationPoolName =
         flag "destination-pool-name" (required string)
           ~doc:"STRING PoolName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_dedicated_ip_in_pool
           (Values.PutDedicatedIpInPoolRequest.make ~ip ~destinationPoolName
              ()) (Some Values.PutDedicatedIpInPoolResponse.to_json)
           (Some Values.PutDedicatedIpInPoolResponse.error_to_json)])
let put_dedicated_ip_warmup_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ip = flag "ip" (required string) ~doc:"STRING Ip"
       and warmupPercentage =
         flag "warmup-percentage" (required int)
           ~doc:"INT Percentage100Wrapper" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_dedicated_ip_warmup_attributes
           (Values.PutDedicatedIpWarmupAttributesRequest.make ~ip
              ~warmupPercentage ())
           (Some Values.PutDedicatedIpWarmupAttributesResponse.to_json)
           (Some Values.PutDedicatedIpWarmupAttributesResponse.error_to_json)])
let put_deliverability_dashboard_option =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subscribedDomains =
         flag "subscribed-domains" (optional json_arg)
           ~doc:"JSON DomainDeliverabilityTrackingOptions"
       and dashboardEnabled =
         flag "dashboard-enabled" (required bool) ~doc:"BOOL Enabled" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_deliverability_dashboard_option
           (Values.PutDeliverabilityDashboardOptionRequest.make
              ?subscribedDomains:(Option.map
                                    ~f:Values.DomainDeliverabilityTrackingOptions.of_json
                                    subscribedDomains) ~dashboardEnabled ())
           (Some Values.PutDeliverabilityDashboardOptionResponse.to_json)
           (Some
              Values.PutDeliverabilityDashboardOptionResponse.error_to_json)])
let put_email_identity_configuration_set_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (optional string)
           ~doc:"STRING ConfigurationSetName"
       and emailIdentity =
         flag "email-identity" (required string) ~doc:"STRING Identity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_email_identity_configuration_set_attributes
           (Values.PutEmailIdentityConfigurationSetAttributesRequest.make
              ?configurationSetName ~emailIdentity ())
           (Some
              Values.PutEmailIdentityConfigurationSetAttributesResponse.to_json)
           (Some
              Values.PutEmailIdentityConfigurationSetAttributesResponse.error_to_json)])
let put_email_identity_dkim_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and signingEnabled =
         flag "signing-enabled" (optional bool) ~doc:"BOOL Enabled"
       and emailIdentity =
         flag "email-identity" (required string) ~doc:"STRING Identity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_email_identity_dkim_attributes
           (Values.PutEmailIdentityDkimAttributesRequest.make ?signingEnabled
              ~emailIdentity ())
           (Some Values.PutEmailIdentityDkimAttributesResponse.to_json)
           (Some Values.PutEmailIdentityDkimAttributesResponse.error_to_json)])
let put_email_identity_dkim_signing_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and signingAttributes =
         flag "signing-attributes" (optional json_arg)
           ~doc:"JSON DkimSigningAttributes"
       and emailIdentity =
         flag "email-identity" (required string) ~doc:"STRING Identity"
       and signingAttributesOrigin =
         flag "signing-attributes-origin" (required json_arg)
           ~doc:"JSON DkimSigningAttributesOrigin" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_email_identity_dkim_signing_attributes
           (Values.PutEmailIdentityDkimSigningAttributesRequest.make
              ?signingAttributes:(Option.map
                                    ~f:Values.DkimSigningAttributes.of_json
                                    signingAttributes) ~emailIdentity
              ~signingAttributesOrigin:(Values.DkimSigningAttributesOrigin.of_json
                                          signingAttributesOrigin) ())
           (Some Values.PutEmailIdentityDkimSigningAttributesResponse.to_json)
           (Some
              Values.PutEmailIdentityDkimSigningAttributesResponse.error_to_json)])
let put_email_identity_feedback_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and emailForwardingEnabled =
         flag "email-forwarding-enabled" (optional bool) ~doc:"BOOL Enabled"
       and emailIdentity =
         flag "email-identity" (required string) ~doc:"STRING Identity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_email_identity_feedback_attributes
           (Values.PutEmailIdentityFeedbackAttributesRequest.make
              ?emailForwardingEnabled ~emailIdentity ())
           (Some Values.PutEmailIdentityFeedbackAttributesResponse.to_json)
           (Some
              Values.PutEmailIdentityFeedbackAttributesResponse.error_to_json)])
let put_email_identity_mail_from_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mailFromDomain =
         flag "mail-from-domain" (optional string)
           ~doc:"STRING MailFromDomainName"
       and behaviorOnMxFailure =
         flag "behavior-on-mx-failure" (optional json_arg)
           ~doc:"JSON BehaviorOnMxFailure"
       and emailIdentity =
         flag "email-identity" (required string) ~doc:"STRING Identity" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_email_identity_mail_from_attributes
           (Values.PutEmailIdentityMailFromAttributesRequest.make
              ?mailFromDomain
              ?behaviorOnMxFailure:(Option.map
                                      ~f:Values.BehaviorOnMxFailure.of_json
                                      behaviorOnMxFailure) ~emailIdentity ())
           (Some Values.PutEmailIdentityMailFromAttributesResponse.to_json)
           (Some
              Values.PutEmailIdentityMailFromAttributesResponse.error_to_json)])
let put_suppressed_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and emailAddress =
         flag "email-address" (required string) ~doc:"STRING EmailAddress"
       and reason =
         flag "reason" (required json_arg) ~doc:"JSON SuppressionListReason" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_suppressed_destination
           (Values.PutSuppressedDestinationRequest.make ~emailAddress
              ~reason:(Values.SuppressionListReason.of_json reason) ())
           (Some Values.PutSuppressedDestinationResponse.to_json)
           (Some Values.PutSuppressedDestinationResponse.error_to_json)])
let send_bulk_email =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fromEmailAddress =
         flag "from-email-address" (optional string)
           ~doc:"STRING EmailAddress"
       and fromEmailAddressIdentityArn =
         flag "from-email-address-identity-arn" (optional string)
           ~doc:"STRING AmazonResourceName"
       and replyToAddresses =
         flag "reply-to-addresses" (optional json_arg)
           ~doc:"JSON EmailAddressList"
       and feedbackForwardingEmailAddress =
         flag "feedback-forwarding-email-address" (optional string)
           ~doc:"STRING EmailAddress"
       and feedbackForwardingEmailAddressIdentityArn =
         flag "feedback-forwarding-email-address-identity-arn"
           (optional string) ~doc:"STRING AmazonResourceName"
       and defaultEmailTags =
         flag "default-email-tags" (optional json_arg)
           ~doc:"JSON MessageTagList"
       and configurationSetName =
         flag "configuration-set-name" (optional string)
           ~doc:"STRING ConfigurationSetName"
       and defaultContent =
         flag "default-content" (required json_arg)
           ~doc:"JSON BulkEmailContent"
       and bulkEmailEntries =
         flag "bulk-email-entries" (required json_arg)
           ~doc:"JSON BulkEmailEntryList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_bulk_email
           (Values.SendBulkEmailRequest.make ?fromEmailAddress
              ?fromEmailAddressIdentityArn
              ?replyToAddresses:(Option.map
                                   ~f:Values.EmailAddressList.of_json
                                   replyToAddresses)
              ?feedbackForwardingEmailAddress
              ?feedbackForwardingEmailAddressIdentityArn
              ?defaultEmailTags:(Option.map ~f:Values.MessageTagList.of_json
                                   defaultEmailTags) ?configurationSetName
              ~defaultContent:(Values.BulkEmailContent.of_json defaultContent)
              ~bulkEmailEntries:(Values.BulkEmailEntryList.of_json
                                   bulkEmailEntries) ())
           (Some Values.SendBulkEmailResponse.to_json)
           (Some Values.SendBulkEmailResponse.error_to_json)])
let send_custom_verification_email =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (optional string)
           ~doc:"STRING ConfigurationSetName"
       and emailAddress =
         flag "email-address" (required string) ~doc:"STRING EmailAddress"
       and templateName =
         flag "template-name" (required string)
           ~doc:"STRING EmailTemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_custom_verification_email
           (Values.SendCustomVerificationEmailRequest.make
              ?configurationSetName ~emailAddress ~templateName ())
           (Some Values.SendCustomVerificationEmailResponse.to_json)
           (Some Values.SendCustomVerificationEmailResponse.error_to_json)])
let send_email =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fromEmailAddress =
         flag "from-email-address" (optional string)
           ~doc:"STRING EmailAddress"
       and fromEmailAddressIdentityArn =
         flag "from-email-address-identity-arn" (optional string)
           ~doc:"STRING AmazonResourceName"
       and destination =
         flag "destination" (optional json_arg) ~doc:"JSON Destination"
       and replyToAddresses =
         flag "reply-to-addresses" (optional json_arg)
           ~doc:"JSON EmailAddressList"
       and feedbackForwardingEmailAddress =
         flag "feedback-forwarding-email-address" (optional string)
           ~doc:"STRING EmailAddress"
       and feedbackForwardingEmailAddressIdentityArn =
         flag "feedback-forwarding-email-address-identity-arn"
           (optional string) ~doc:"STRING AmazonResourceName"
       and emailTags =
         flag "email-tags" (optional json_arg) ~doc:"JSON MessageTagList"
       and configurationSetName =
         flag "configuration-set-name" (optional string)
           ~doc:"STRING ConfigurationSetName"
       and listManagementOptions =
         flag "list-management-options" (optional json_arg)
           ~doc:"JSON ListManagementOptions"
       and content =
         flag "content" (required json_arg) ~doc:"JSON EmailContent" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.send_email
           (Values.SendEmailRequest.make ?fromEmailAddress
              ?fromEmailAddressIdentityArn
              ?destination:(Option.map ~f:Values.Destination.of_json
                              destination)
              ?replyToAddresses:(Option.map
                                   ~f:Values.EmailAddressList.of_json
                                   replyToAddresses)
              ?feedbackForwardingEmailAddress
              ?feedbackForwardingEmailAddressIdentityArn
              ?emailTags:(Option.map ~f:Values.MessageTagList.of_json
                            emailTags) ?configurationSetName
              ?listManagementOptions:(Option.map
                                        ~f:Values.ListManagementOptions.of_json
                                        listManagementOptions)
              ~content:(Values.EmailContent.of_json content) ())
           (Some Values.SendEmailResponse.to_json)
           (Some Values.SendEmailResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let test_render_email_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (required string)
           ~doc:"STRING EmailTemplateName"
       and templateData =
         flag "template-data" (required string)
           ~doc:"STRING EmailTemplateData" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.test_render_email_template
           (Values.TestRenderEmailTemplateRequest.make ~templateName
              ~templateData ())
           (Some Values.TestRenderEmailTemplateResponse.to_json)
           (Some Values.TestRenderEmailTemplateResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AmazonResourceName"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_configuration_set_event_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationSetName =
         flag "configuration-set-name" (required string)
           ~doc:"STRING ConfigurationSetName"
       and eventDestinationName =
         flag "event-destination-name" (required string)
           ~doc:"STRING EventDestinationName"
       and eventDestination =
         flag "event-destination" (required json_arg)
           ~doc:"JSON EventDestinationDefinition" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_configuration_set_event_destination
           (Values.UpdateConfigurationSetEventDestinationRequest.make
              ~configurationSetName ~eventDestinationName
              ~eventDestination:(Values.EventDestinationDefinition.of_json
                                   eventDestination) ())
           (Some
              Values.UpdateConfigurationSetEventDestinationResponse.to_json)
           (Some
              Values.UpdateConfigurationSetEventDestinationResponse.error_to_json)])
let update_contact =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and topicPreferences =
         flag "topic-preferences" (optional json_arg)
           ~doc:"JSON TopicPreferenceList"
       and unsubscribeAll =
         flag "unsubscribe-all" (optional bool) ~doc:"BOOL UnsubscribeAll"
       and attributesData =
         flag "attributes-data" (optional string)
           ~doc:"STRING AttributesData"
       and contactListName =
         flag "contact-list-name" (required string)
           ~doc:"STRING ContactListName"
       and emailAddress =
         flag "email-address" (required string) ~doc:"STRING EmailAddress" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_contact
           (Values.UpdateContactRequest.make
              ?topicPreferences:(Option.map
                                   ~f:Values.TopicPreferenceList.of_json
                                   topicPreferences) ?unsubscribeAll
              ?attributesData ~contactListName ~emailAddress ())
           (Some Values.UpdateContactResponse.to_json)
           (Some Values.UpdateContactResponse.error_to_json)])
let update_contact_list =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and topics = flag "topics" (optional json_arg) ~doc:"JSON Topics"
       and description =
         flag "description" (optional string) ~doc:"STRING Description"
       and contactListName =
         flag "contact-list-name" (required string)
           ~doc:"STRING ContactListName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_contact_list
           (Values.UpdateContactListRequest.make
              ?topics:(Option.map ~f:Values.Topics.of_json topics)
              ?description ~contactListName ())
           (Some Values.UpdateContactListResponse.to_json)
           (Some Values.UpdateContactListResponse.error_to_json)])
let update_custom_verification_email_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (required string)
           ~doc:"STRING EmailTemplateName"
       and fromEmailAddress =
         flag "from-email-address" (required string)
           ~doc:"STRING EmailAddress"
       and templateSubject =
         flag "template-subject" (required string)
           ~doc:"STRING EmailTemplateSubject"
       and templateContent =
         flag "template-content" (required string)
           ~doc:"STRING TemplateContent"
       and successRedirectionURL =
         flag "success-redirection-u-r-l" (required string)
           ~doc:"STRING SuccessRedirectionURL"
       and failureRedirectionURL =
         flag "failure-redirection-u-r-l" (required string)
           ~doc:"STRING FailureRedirectionURL" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_custom_verification_email_template
           (Values.UpdateCustomVerificationEmailTemplateRequest.make
              ~templateName ~fromEmailAddress ~templateSubject
              ~templateContent ~successRedirectionURL ~failureRedirectionURL
              ())
           (Some Values.UpdateCustomVerificationEmailTemplateResponse.to_json)
           (Some
              Values.UpdateCustomVerificationEmailTemplateResponse.error_to_json)])
let update_email_identity_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and emailIdentity =
         flag "email-identity" (required string) ~doc:"STRING Identity"
       and policyName =
         flag "policy-name" (required string) ~doc:"STRING PolicyName"
       and policy = flag "policy" (required string) ~doc:"STRING Policy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_email_identity_policy
           (Values.UpdateEmailIdentityPolicyRequest.make ~emailIdentity
              ~policyName ~policy ())
           (Some Values.UpdateEmailIdentityPolicyResponse.to_json)
           (Some Values.UpdateEmailIdentityPolicyResponse.error_to_json)])
let update_email_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and templateName =
         flag "template-name" (required string)
           ~doc:"STRING EmailTemplateName"
       and templateContent =
         flag "template-content" (required json_arg)
           ~doc:"JSON EmailTemplateContent" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_email_template
           (Values.UpdateEmailTemplateRequest.make ~templateName
              ~templateContent:(Values.EmailTemplateContent.of_json
                                  templateContent) ())
           (Some Values.UpdateEmailTemplateResponse.to_json)
           (Some Values.UpdateEmailTemplateResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-configuration-set", create_configuration_set);
    ("create-configuration-set-event-destination",
      create_configuration_set_event_destination);
    ("create-contact", create_contact);
    ("create-contact-list", create_contact_list);
    ("create-custom-verification-email-template",
      create_custom_verification_email_template);
    ("create-dedicated-ip-pool", create_dedicated_ip_pool);
    ("create-deliverability-test-report", create_deliverability_test_report);
    ("create-email-identity", create_email_identity);
    ("create-email-identity-policy", create_email_identity_policy);
    ("create-email-template", create_email_template);
    ("create-import-job", create_import_job);
    ("delete-configuration-set", delete_configuration_set);
    ("delete-configuration-set-event-destination",
      delete_configuration_set_event_destination);
    ("delete-contact", delete_contact);
    ("delete-contact-list", delete_contact_list);
    ("delete-custom-verification-email-template",
      delete_custom_verification_email_template);
    ("delete-dedicated-ip-pool", delete_dedicated_ip_pool);
    ("delete-email-identity", delete_email_identity);
    ("delete-email-identity-policy", delete_email_identity_policy);
    ("delete-email-template", delete_email_template);
    ("delete-suppressed-destination", delete_suppressed_destination);
    ("get-account", get_account);
    ("get-blacklist-reports", get_blacklist_reports);
    ("get-configuration-set", get_configuration_set);
    ("get-configuration-set-event-destinations",
      get_configuration_set_event_destinations);
    ("get-contact", get_contact);
    ("get-contact-list", get_contact_list);
    ("get-custom-verification-email-template",
      get_custom_verification_email_template);
    ("get-dedicated-ip", get_dedicated_ip);
    ("get-dedicated-ips", get_dedicated_ips);
    ("get-deliverability-dashboard-options",
      get_deliverability_dashboard_options);
    ("get-deliverability-test-report", get_deliverability_test_report);
    ("get-domain-deliverability-campaign",
      get_domain_deliverability_campaign);
    ("get-domain-statistics-report", get_domain_statistics_report);
    ("get-email-identity", get_email_identity);
    ("get-email-identity-policies", get_email_identity_policies);
    ("get-email-template", get_email_template);
    ("get-import-job", get_import_job);
    ("get-suppressed-destination", get_suppressed_destination);
    ("list-configuration-sets", list_configuration_sets);
    ("list-contact-lists", list_contact_lists);
    ("list-contacts", list_contacts);
    ("list-custom-verification-email-templates",
      list_custom_verification_email_templates);
    ("list-dedicated-ip-pools", list_dedicated_ip_pools);
    ("list-deliverability-test-reports", list_deliverability_test_reports);
    ("list-domain-deliverability-campaigns",
      list_domain_deliverability_campaigns);
    ("list-email-identities", list_email_identities);
    ("list-email-templates", list_email_templates);
    ("list-import-jobs", list_import_jobs);
    ("list-suppressed-destinations", list_suppressed_destinations);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-account-dedicated-ip-warmup-attributes",
      put_account_dedicated_ip_warmup_attributes);
    ("put-account-details", put_account_details);
    ("put-account-sending-attributes", put_account_sending_attributes);
    ("put-account-suppression-attributes",
      put_account_suppression_attributes);
    ("put-configuration-set-delivery-options",
      put_configuration_set_delivery_options);
    ("put-configuration-set-reputation-options",
      put_configuration_set_reputation_options);
    ("put-configuration-set-sending-options",
      put_configuration_set_sending_options);
    ("put-configuration-set-suppression-options",
      put_configuration_set_suppression_options);
    ("put-configuration-set-tracking-options",
      put_configuration_set_tracking_options);
    ("put-dedicated-ip-in-pool", put_dedicated_ip_in_pool);
    ("put-dedicated-ip-warmup-attributes",
      put_dedicated_ip_warmup_attributes);
    ("put-deliverability-dashboard-option",
      put_deliverability_dashboard_option);
    ("put-email-identity-configuration-set-attributes",
      put_email_identity_configuration_set_attributes);
    ("put-email-identity-dkim-attributes",
      put_email_identity_dkim_attributes);
    ("put-email-identity-dkim-signing-attributes",
      put_email_identity_dkim_signing_attributes);
    ("put-email-identity-feedback-attributes",
      put_email_identity_feedback_attributes);
    ("put-email-identity-mail-from-attributes",
      put_email_identity_mail_from_attributes);
    ("put-suppressed-destination", put_suppressed_destination);
    ("send-bulk-email", send_bulk_email);
    ("send-custom-verification-email", send_custom_verification_email);
    ("send-email", send_email);
    ("tag-resource", tag_resource);
    ("test-render-email-template", test_render_email_template);
    ("untag-resource", untag_resource);
    ("update-configuration-set-event-destination",
      update_configuration_set_event_destination);
    ("update-contact", update_contact);
    ("update-contact-list", update_contact_list);
    ("update-custom-verification-email-template",
      update_custom_verification_email_template);
    ("update-email-identity-policy", update_email_identity_policy);
    ("update-email-template", update_email_template)]
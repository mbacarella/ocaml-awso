(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let delete_recommendation_preferences =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scope = flag "scope" (optional json_arg) ~doc:"JSON Scope"
       and resourceType =
         flag "resource-type" (required json_arg) ~doc:"JSON ResourceType"
       and recommendationPreferenceNames =
         flag "recommendation-preference-names" (required json_arg)
           ~doc:"JSON RecommendationPreferenceNames" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_recommendation_preferences
           (Values.DeleteRecommendationPreferencesRequest.make
              ?scope:(Option.map ~f:Values.Scope.of_json scope)
              ~resourceType:(Values.ResourceType.of_json resourceType)
              ~recommendationPreferenceNames:(Values.RecommendationPreferenceNames.of_json
                                                recommendationPreferenceNames)
              ())
           (Some Values.DeleteRecommendationPreferencesResponse.to_json)
           (Some Values.DeleteRecommendationPreferencesResponse.error_to_json)])
let describe_recommendation_export_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobIds = flag "job-ids" (optional json_arg) ~doc:"JSON JobIds"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON JobFilters"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_recommendation_export_jobs
           (Values.DescribeRecommendationExportJobsRequest.make
              ?jobIds:(Option.map ~f:Values.JobIds.of_json jobIds)
              ?filters:(Option.map ~f:Values.JobFilters.of_json filters)
              ?nextToken ?maxResults ())
           (Some Values.DescribeRecommendationExportJobsResponse.to_json)
           (Some
              Values.DescribeRecommendationExportJobsResponse.error_to_json)])
let export_auto_scaling_group_recommendations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIds =
         flag "account-ids" (optional json_arg) ~doc:"JSON AccountIds"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters"
       and fieldsToExport =
         flag "fields-to-export" (optional json_arg)
           ~doc:"JSON ExportableAutoScalingGroupFields"
       and fileFormat =
         flag "file-format" (optional json_arg) ~doc:"JSON FileFormat"
       and includeMemberAccounts =
         flag "include-member-accounts" (optional bool)
           ~doc:"BOOL IncludeMemberAccounts"
       and recommendationPreferences =
         flag "recommendation-preferences" (optional json_arg)
           ~doc:"JSON RecommendationPreferences"
       and s3DestinationConfig =
         flag "s3-destination-config" (required json_arg)
           ~doc:"JSON S3DestinationConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.export_auto_scaling_group_recommendations
           (Values.ExportAutoScalingGroupRecommendationsRequest.make
              ?accountIds:(Option.map ~f:Values.AccountIds.of_json accountIds)
              ?filters:(Option.map ~f:Values.Filters.of_json filters)
              ?fieldsToExport:(Option.map
                                 ~f:Values.ExportableAutoScalingGroupFields.of_json
                                 fieldsToExport)
              ?fileFormat:(Option.map ~f:Values.FileFormat.of_json fileFormat)
              ?includeMemberAccounts
              ?recommendationPreferences:(Option.map
                                            ~f:Values.RecommendationPreferences.of_json
                                            recommendationPreferences)
              ~s3DestinationConfig:(Values.S3DestinationConfig.of_json
                                      s3DestinationConfig) ())
           (Some Values.ExportAutoScalingGroupRecommendationsResponse.to_json)
           (Some
              Values.ExportAutoScalingGroupRecommendationsResponse.error_to_json)])
let export_e_b_s_volume_recommendations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIds =
         flag "account-ids" (optional json_arg) ~doc:"JSON AccountIds"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON EBSFilters"
       and fieldsToExport =
         flag "fields-to-export" (optional json_arg)
           ~doc:"JSON ExportableVolumeFields"
       and fileFormat =
         flag "file-format" (optional json_arg) ~doc:"JSON FileFormat"
       and includeMemberAccounts =
         flag "include-member-accounts" (optional bool)
           ~doc:"BOOL IncludeMemberAccounts"
       and s3DestinationConfig =
         flag "s3-destination-config" (required json_arg)
           ~doc:"JSON S3DestinationConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.export_e_b_s_volume_recommendations
           (Values.ExportEBSVolumeRecommendationsRequest.make
              ?accountIds:(Option.map ~f:Values.AccountIds.of_json accountIds)
              ?filters:(Option.map ~f:Values.EBSFilters.of_json filters)
              ?fieldsToExport:(Option.map
                                 ~f:Values.ExportableVolumeFields.of_json
                                 fieldsToExport)
              ?fileFormat:(Option.map ~f:Values.FileFormat.of_json fileFormat)
              ?includeMemberAccounts
              ~s3DestinationConfig:(Values.S3DestinationConfig.of_json
                                      s3DestinationConfig) ())
           (Some Values.ExportEBSVolumeRecommendationsResponse.to_json)
           (Some Values.ExportEBSVolumeRecommendationsResponse.error_to_json)])
let export_e_c2_instance_recommendations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIds =
         flag "account-ids" (optional json_arg) ~doc:"JSON AccountIds"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters"
       and fieldsToExport =
         flag "fields-to-export" (optional json_arg)
           ~doc:"JSON ExportableInstanceFields"
       and fileFormat =
         flag "file-format" (optional json_arg) ~doc:"JSON FileFormat"
       and includeMemberAccounts =
         flag "include-member-accounts" (optional bool)
           ~doc:"BOOL IncludeMemberAccounts"
       and recommendationPreferences =
         flag "recommendation-preferences" (optional json_arg)
           ~doc:"JSON RecommendationPreferences"
       and s3DestinationConfig =
         flag "s3-destination-config" (required json_arg)
           ~doc:"JSON S3DestinationConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.export_e_c2_instance_recommendations
           (Values.ExportEC2InstanceRecommendationsRequest.make
              ?accountIds:(Option.map ~f:Values.AccountIds.of_json accountIds)
              ?filters:(Option.map ~f:Values.Filters.of_json filters)
              ?fieldsToExport:(Option.map
                                 ~f:Values.ExportableInstanceFields.of_json
                                 fieldsToExport)
              ?fileFormat:(Option.map ~f:Values.FileFormat.of_json fileFormat)
              ?includeMemberAccounts
              ?recommendationPreferences:(Option.map
                                            ~f:Values.RecommendationPreferences.of_json
                                            recommendationPreferences)
              ~s3DestinationConfig:(Values.S3DestinationConfig.of_json
                                      s3DestinationConfig) ())
           (Some Values.ExportEC2InstanceRecommendationsResponse.to_json)
           (Some
              Values.ExportEC2InstanceRecommendationsResponse.error_to_json)])
let export_lambda_function_recommendations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIds =
         flag "account-ids" (optional json_arg) ~doc:"JSON AccountIds"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON LambdaFunctionRecommendationFilters"
       and fieldsToExport =
         flag "fields-to-export" (optional json_arg)
           ~doc:"JSON ExportableLambdaFunctionFields"
       and fileFormat =
         flag "file-format" (optional json_arg) ~doc:"JSON FileFormat"
       and includeMemberAccounts =
         flag "include-member-accounts" (optional bool)
           ~doc:"BOOL IncludeMemberAccounts"
       and s3DestinationConfig =
         flag "s3-destination-config" (required json_arg)
           ~doc:"JSON S3DestinationConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.export_lambda_function_recommendations
           (Values.ExportLambdaFunctionRecommendationsRequest.make
              ?accountIds:(Option.map ~f:Values.AccountIds.of_json accountIds)
              ?filters:(Option.map
                          ~f:Values.LambdaFunctionRecommendationFilters.of_json
                          filters)
              ?fieldsToExport:(Option.map
                                 ~f:Values.ExportableLambdaFunctionFields.of_json
                                 fieldsToExport)
              ?fileFormat:(Option.map ~f:Values.FileFormat.of_json fileFormat)
              ?includeMemberAccounts
              ~s3DestinationConfig:(Values.S3DestinationConfig.of_json
                                      s3DestinationConfig) ())
           (Some Values.ExportLambdaFunctionRecommendationsResponse.to_json)
           (Some
              Values.ExportLambdaFunctionRecommendationsResponse.error_to_json)])
let get_auto_scaling_group_recommendations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIds =
         flag "account-ids" (optional json_arg) ~doc:"JSON AccountIds"
       and autoScalingGroupArns =
         flag "auto-scaling-group-arns" (optional json_arg)
           ~doc:"JSON AutoScalingGroupArns"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters"
       and recommendationPreferences =
         flag "recommendation-preferences" (optional json_arg)
           ~doc:"JSON RecommendationPreferences" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_auto_scaling_group_recommendations
           (Values.GetAutoScalingGroupRecommendationsRequest.make
              ?accountIds:(Option.map ~f:Values.AccountIds.of_json accountIds)
              ?autoScalingGroupArns:(Option.map
                                       ~f:Values.AutoScalingGroupArns.of_json
                                       autoScalingGroupArns) ?nextToken
              ?maxResults
              ?filters:(Option.map ~f:Values.Filters.of_json filters)
              ?recommendationPreferences:(Option.map
                                            ~f:Values.RecommendationPreferences.of_json
                                            recommendationPreferences) ())
           (Some Values.GetAutoScalingGroupRecommendationsResponse.to_json)
           (Some
              Values.GetAutoScalingGroupRecommendationsResponse.error_to_json)])
let get_e_b_s_volume_recommendations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and volumeArns =
         flag "volume-arns" (optional json_arg) ~doc:"JSON VolumeArns"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON EBSFilters"
       and accountIds =
         flag "account-ids" (optional json_arg) ~doc:"JSON AccountIds" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_e_b_s_volume_recommendations
           (Values.GetEBSVolumeRecommendationsRequest.make
              ?volumeArns:(Option.map ~f:Values.VolumeArns.of_json volumeArns)
              ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.EBSFilters.of_json filters)
              ?accountIds:(Option.map ~f:Values.AccountIds.of_json accountIds)
              ()) (Some Values.GetEBSVolumeRecommendationsResponse.to_json)
           (Some Values.GetEBSVolumeRecommendationsResponse.error_to_json)])
let get_e_c2_instance_recommendations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceArns =
         flag "instance-arns" (optional json_arg) ~doc:"JSON InstanceArns"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters"
       and accountIds =
         flag "account-ids" (optional json_arg) ~doc:"JSON AccountIds"
       and recommendationPreferences =
         flag "recommendation-preferences" (optional json_arg)
           ~doc:"JSON RecommendationPreferences" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_e_c2_instance_recommendations
           (Values.GetEC2InstanceRecommendationsRequest.make
              ?instanceArns:(Option.map ~f:Values.InstanceArns.of_json
                               instanceArns) ?nextToken ?maxResults
              ?filters:(Option.map ~f:Values.Filters.of_json filters)
              ?accountIds:(Option.map ~f:Values.AccountIds.of_json accountIds)
              ?recommendationPreferences:(Option.map
                                            ~f:Values.RecommendationPreferences.of_json
                                            recommendationPreferences) ())
           (Some Values.GetEC2InstanceRecommendationsResponse.to_json)
           (Some Values.GetEC2InstanceRecommendationsResponse.error_to_json)])
let get_e_c2_recommendation_projected_metrics =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and recommendationPreferences =
         flag "recommendation-preferences" (optional json_arg)
           ~doc:"JSON RecommendationPreferences"
       and instanceArn =
         flag "instance-arn" (required string) ~doc:"STRING InstanceArn"
       and stat = flag "stat" (required json_arg) ~doc:"JSON MetricStatistic"
       and period = flag "period" (required int) ~doc:"INT Period"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON Timestamp"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_e_c2_recommendation_projected_metrics
           (Values.GetEC2RecommendationProjectedMetricsRequest.make
              ?recommendationPreferences:(Option.map
                                            ~f:Values.RecommendationPreferences.of_json
                                            recommendationPreferences)
              ~instanceArn ~stat:(Values.MetricStatistic.of_json stat)
              ~period ~startTime:(Values.Timestamp.of_json startTime)
              ~endTime:(Values.Timestamp.of_json endTime) ())
           (Some Values.GetEC2RecommendationProjectedMetricsResponse.to_json)
           (Some
              Values.GetEC2RecommendationProjectedMetricsResponse.error_to_json)])
let get_effective_recommendation_preferences =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_effective_recommendation_preferences
           (Values.GetEffectiveRecommendationPreferencesRequest.make
              ~resourceArn ())
           (Some Values.GetEffectiveRecommendationPreferencesResponse.to_json)
           (Some
              Values.GetEffectiveRecommendationPreferencesResponse.error_to_json)])
let get_enrollment_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_enrollment_status
           (Values.GetEnrollmentStatusRequest.make ())
           (Some Values.GetEnrollmentStatusResponse.to_json)
           (Some Values.GetEnrollmentStatusResponse.error_to_json)])
let get_enrollment_statuses_for_organization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON EnrollmentFilters"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_enrollment_statuses_for_organization
           (Values.GetEnrollmentStatusesForOrganizationRequest.make
              ?filters:(Option.map ~f:Values.EnrollmentFilters.of_json
                          filters) ?nextToken ?maxResults ())
           (Some Values.GetEnrollmentStatusesForOrganizationResponse.to_json)
           (Some
              Values.GetEnrollmentStatusesForOrganizationResponse.error_to_json)])
let get_lambda_function_recommendations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and functionArns =
         flag "function-arns" (optional json_arg) ~doc:"JSON FunctionArns"
       and accountIds =
         flag "account-ids" (optional json_arg) ~doc:"JSON AccountIds"
       and filters =
         flag "filters" (optional json_arg)
           ~doc:"JSON LambdaFunctionRecommendationFilters"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_lambda_function_recommendations
           (Values.GetLambdaFunctionRecommendationsRequest.make
              ?functionArns:(Option.map ~f:Values.FunctionArns.of_json
                               functionArns)
              ?accountIds:(Option.map ~f:Values.AccountIds.of_json accountIds)
              ?filters:(Option.map
                          ~f:Values.LambdaFunctionRecommendationFilters.of_json
                          filters) ?nextToken ?maxResults ())
           (Some Values.GetLambdaFunctionRecommendationsResponse.to_json)
           (Some
              Values.GetLambdaFunctionRecommendationsResponse.error_to_json)])
let get_recommendation_preferences =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scope = flag "scope" (optional json_arg) ~doc:"JSON Scope"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and resourceType =
         flag "resource-type" (required json_arg) ~doc:"JSON ResourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_recommendation_preferences
           (Values.GetRecommendationPreferencesRequest.make
              ?scope:(Option.map ~f:Values.Scope.of_json scope) ?nextToken
              ?maxResults
              ~resourceType:(Values.ResourceType.of_json resourceType) ())
           (Some Values.GetRecommendationPreferencesResponse.to_json)
           (Some Values.GetRecommendationPreferencesResponse.error_to_json)])
let get_recommendation_summaries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIds =
         flag "account-ids" (optional json_arg) ~doc:"JSON AccountIds"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_recommendation_summaries
           (Values.GetRecommendationSummariesRequest.make
              ?accountIds:(Option.map ~f:Values.AccountIds.of_json accountIds)
              ?nextToken ?maxResults ())
           (Some Values.GetRecommendationSummariesResponse.to_json)
           (Some Values.GetRecommendationSummariesResponse.error_to_json)])
let put_recommendation_preferences =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and scope = flag "scope" (optional json_arg) ~doc:"JSON Scope"
       and enhancedInfrastructureMetrics =
         flag "enhanced-infrastructure-metrics" (optional json_arg)
           ~doc:"JSON EnhancedInfrastructureMetrics"
       and inferredWorkloadTypes =
         flag "inferred-workload-types" (optional json_arg)
           ~doc:"JSON InferredWorkloadTypesPreference"
       and resourceType =
         flag "resource-type" (required json_arg) ~doc:"JSON ResourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_recommendation_preferences
           (Values.PutRecommendationPreferencesRequest.make
              ?scope:(Option.map ~f:Values.Scope.of_json scope)
              ?enhancedInfrastructureMetrics:(Option.map
                                                ~f:Values.EnhancedInfrastructureMetrics.of_json
                                                enhancedInfrastructureMetrics)
              ?inferredWorkloadTypes:(Option.map
                                        ~f:Values.InferredWorkloadTypesPreference.of_json
                                        inferredWorkloadTypes)
              ~resourceType:(Values.ResourceType.of_json resourceType) ())
           (Some Values.PutRecommendationPreferencesResponse.to_json)
           (Some Values.PutRecommendationPreferencesResponse.error_to_json)])
let update_enrollment_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeMemberAccounts =
         flag "include-member-accounts" (optional bool)
           ~doc:"BOOL IncludeMemberAccounts"
       and status = flag "status" (required json_arg) ~doc:"JSON Status" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_enrollment_status
           (Values.UpdateEnrollmentStatusRequest.make ?includeMemberAccounts
              ~status:(Values.Status.of_json status) ())
           (Some Values.UpdateEnrollmentStatusResponse.to_json)
           (Some Values.UpdateEnrollmentStatusResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("delete-recommendation-preferences", delete_recommendation_preferences);
    ("describe-recommendation-export-jobs",
      describe_recommendation_export_jobs);
    ("export-auto-scaling-group-recommendations",
      export_auto_scaling_group_recommendations);
    ("export-e-b-s-volume-recommendations",
      export_e_b_s_volume_recommendations);
    ("export-e-c2-instance-recommendations",
      export_e_c2_instance_recommendations);
    ("export-lambda-function-recommendations",
      export_lambda_function_recommendations);
    ("get-auto-scaling-group-recommendations",
      get_auto_scaling_group_recommendations);
    ("get-e-b-s-volume-recommendations", get_e_b_s_volume_recommendations);
    ("get-e-c2-instance-recommendations", get_e_c2_instance_recommendations);
    ("get-e-c2-recommendation-projected-metrics",
      get_e_c2_recommendation_projected_metrics);
    ("get-effective-recommendation-preferences",
      get_effective_recommendation_preferences);
    ("get-enrollment-status", get_enrollment_status);
    ("get-enrollment-statuses-for-organization",
      get_enrollment_statuses_for_organization);
    ("get-lambda-function-recommendations",
      get_lambda_function_recommendations);
    ("get-recommendation-preferences", get_recommendation_preferences);
    ("get-recommendation-summaries", get_recommendation_summaries);
    ("put-recommendation-preferences", put_recommendation_preferences);
    ("update-enrollment-status", update_enrollment_status)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | AssociateDefaultView: (AssociateDefaultViewInput.t,
  AssociateDefaultViewOutput.t, AssociateDefaultViewOutput.error) t 
  | BatchGetView: (BatchGetViewInput.t, BatchGetViewOutput.t,
  BatchGetViewOutput.error) t 
  | CreateIndex: (CreateIndexInput.t, CreateIndexOutput.t,
  CreateIndexOutput.error) t 
  | CreateResourceExplorerSetup: (CreateResourceExplorerSetupInput.t,
  CreateResourceExplorerSetupOutput.t,
  CreateResourceExplorerSetupOutput.error) t 
  | CreateView: (CreateViewInput.t, CreateViewOutput.t,
  CreateViewOutput.error) t 
  | DeleteIndex: (DeleteIndexInput.t, DeleteIndexOutput.t,
  DeleteIndexOutput.error) t 
  | DeleteResourceExplorerSetup: (DeleteResourceExplorerSetupInput.t,
  DeleteResourceExplorerSetupOutput.t,
  DeleteResourceExplorerSetupOutput.error) t 
  | DeleteView: (DeleteViewInput.t, DeleteViewOutput.t,
  DeleteViewOutput.error) t 
  | DisassociateDefaultView: (unit, unit, unit) t 
  | GetAccountLevelServiceConfiguration: (unit,
  GetAccountLevelServiceConfigurationOutput.t,
  GetAccountLevelServiceConfigurationOutput.error) t 
  | GetDefaultView: (unit, GetDefaultViewOutput.t,
  GetDefaultViewOutput.error) t 
  | GetIndex: (unit, GetIndexOutput.t, GetIndexOutput.error) t 
  | GetManagedView: (GetManagedViewInput.t, GetManagedViewOutput.t,
  GetManagedViewOutput.error) t 
  | GetResourceExplorerSetup: (GetResourceExplorerSetupInput.t,
  GetResourceExplorerSetupOutput.t, GetResourceExplorerSetupOutput.error) t 
  | GetServiceIndex: (unit, GetServiceIndexOutput.t,
  GetServiceIndexOutput.error) t 
  | GetServiceView: (GetServiceViewInput.t, GetServiceViewOutput.t,
  GetServiceViewOutput.error) t 
  | GetView: (GetViewInput.t, GetViewOutput.t, GetViewOutput.error) t 
  | ListIndexes: (ListIndexesInput.t, ListIndexesOutput.t,
  ListIndexesOutput.error) t 
  | ListIndexesForMembers: (ListIndexesForMembersInput.t,
  ListIndexesForMembersOutput.t, ListIndexesForMembersOutput.error) t 
  | ListManagedViews: (ListManagedViewsInput.t, ListManagedViewsOutput.t,
  ListManagedViewsOutput.error) t 
  | ListResources: (ListResourcesInput.t, ListResourcesOutput.t,
  ListResourcesOutput.error) t 
  | ListServiceIndexes: (ListServiceIndexesInput.t,
  ListServiceIndexesOutput.t, ListServiceIndexesOutput.error) t 
  | ListServiceViews: (ListServiceViewsInput.t, ListServiceViewsOutput.t,
  ListServiceViewsOutput.error) t 
  | ListStreamingAccessForServices: (ListStreamingAccessForServicesInput.t,
  ListStreamingAccessForServicesOutput.t,
  ListStreamingAccessForServicesOutput.error) t 
  | ListSupportedResourceTypes: (ListSupportedResourceTypesInput.t,
  ListSupportedResourceTypesOutput.t, ListSupportedResourceTypesOutput.error)
  t 
  | ListTagsForResource: (ListTagsForResourceInput.t,
  ListTagsForResourceOutput.t, ListTagsForResourceOutput.error) t 
  | ListViews: (ListViewsInput.t, ListViewsOutput.t, ListViewsOutput.error) t
  
  | Search: (SearchInput.t, SearchOutput.t, SearchOutput.error) t 
  | TagResource: (TagResourceInput.t, TagResourceOutput.t,
  TagResourceOutput.error) t 
  | UntagResource: (UntagResourceInput.t, UntagResourceOutput.t,
  UntagResourceOutput.error) t 
  | UpdateIndexType: (UpdateIndexTypeInput.t, UpdateIndexTypeOutput.t,
  UpdateIndexTypeOutput.error) t 
  | UpdateView: (UpdateViewInput.t, UpdateViewOutput.t,
  UpdateViewOutput.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | AssociateDefaultView -> `POST
  | BatchGetView -> `POST
  | CreateIndex -> `POST
  | CreateResourceExplorerSetup -> `POST
  | CreateView -> `POST
  | DeleteIndex -> `POST
  | DeleteResourceExplorerSetup -> `POST
  | DeleteView -> `POST
  | DisassociateDefaultView -> `POST
  | GetAccountLevelServiceConfiguration -> `POST
  | GetDefaultView -> `POST
  | GetIndex -> `POST
  | GetManagedView -> `POST
  | GetResourceExplorerSetup -> `POST
  | GetServiceIndex -> `POST
  | GetServiceView -> `POST
  | GetView -> `POST
  | ListIndexes -> `POST
  | ListIndexesForMembers -> `POST
  | ListManagedViews -> `POST
  | ListResources -> `POST
  | ListServiceIndexes -> `POST
  | ListServiceViews -> `POST
  | ListStreamingAccessForServices -> `POST
  | ListSupportedResourceTypes -> `POST
  | ListTagsForResource -> `GET
  | ListViews -> `POST
  | Search -> `POST
  | TagResource -> `POST
  | UntagResource -> `DELETE
  | UpdateIndexType -> `POST
  | UpdateView -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | AssociateDefaultView ->
          (Format.kasprintf Uri.of_string) "/AssociateDefaultView"
      | BatchGetView -> (Format.kasprintf Uri.of_string) "/BatchGetView"
      | CreateIndex -> (Format.kasprintf Uri.of_string) "/CreateIndex"
      | CreateResourceExplorerSetup ->
          (Format.kasprintf Uri.of_string) "/CreateResourceExplorerSetup"
      | CreateView -> (Format.kasprintf Uri.of_string) "/CreateView"
      | DeleteIndex -> (Format.kasprintf Uri.of_string) "/DeleteIndex"
      | DeleteResourceExplorerSetup ->
          (Format.kasprintf Uri.of_string) "/DeleteResourceExplorerSetup"
      | DeleteView -> (Format.kasprintf Uri.of_string) "/DeleteView"
      | DisassociateDefaultView ->
          (Format.kasprintf Uri.of_string) "/DisassociateDefaultView"
      | GetAccountLevelServiceConfiguration ->
          (Format.kasprintf Uri.of_string)
            "/GetAccountLevelServiceConfiguration"
      | GetDefaultView -> (Format.kasprintf Uri.of_string) "/GetDefaultView"
      | GetIndex -> (Format.kasprintf Uri.of_string) "/GetIndex"
      | GetManagedView -> (Format.kasprintf Uri.of_string) "/GetManagedView"
      | GetResourceExplorerSetup ->
          (Format.kasprintf Uri.of_string) "/GetResourceExplorerSetup"
      | GetServiceIndex ->
          (Format.kasprintf Uri.of_string) "/GetServiceIndex"
      | GetServiceView -> (Format.kasprintf Uri.of_string) "/GetServiceView"
      | GetView -> (Format.kasprintf Uri.of_string) "/GetView"
      | ListIndexes -> (Format.kasprintf Uri.of_string) "/ListIndexes"
      | ListIndexesForMembers ->
          (Format.kasprintf Uri.of_string) "/ListIndexesForMembers"
      | ListManagedViews ->
          (Format.kasprintf Uri.of_string) "/ListManagedViews"
      | ListResources -> (Format.kasprintf Uri.of_string) "/ListResources"
      | ListServiceIndexes ->
          (Format.kasprintf Uri.of_string) "/ListServiceIndexes"
      | ListServiceViews ->
          (Format.kasprintf Uri.of_string) "/ListServiceViews"
      | ListStreamingAccessForServices ->
          (Format.kasprintf Uri.of_string) "/ListStreamingAccessForServices"
      | ListSupportedResourceTypes ->
          (Format.kasprintf Uri.of_string) "/ListSupportedResourceTypes"
      | ListTagsForResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (String_.to_header x.ListTagsForResourceInput.resourceArn)
      | ListViews -> (Format.kasprintf Uri.of_string) "/ListViews"
      | Search -> (Format.kasprintf Uri.of_string) "/Search"
      | TagResource ->
          (Format.kasprintf Uri.of_string) "/tags/%s"
            (String_.to_header x.TagResourceInput.resourceArn)
      | UntagResource ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/tags/%s"
               (String_.to_header x.UntagResourceInput.resourceArn))
            (List.filter_opt
               [Some ("tagKeys", (StringList.to_header x.tagKeys))])
      | UpdateIndexType ->
          (Format.kasprintf Uri.of_string) "/UpdateIndexType"
      | UpdateView -> (Format.kasprintf Uri.of_string) "/UpdateView")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | AssociateDefaultView ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("ViewArn",
                           (AssociateDefaultViewInputViewArnString.to_value
                              req.AssociateDefaultViewInput.viewArn))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | BatchGetView ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.BatchGetViewInput.viewArns
                         ~f:(fun x ->
                               ("ViewArns",
                                 (BatchGetViewInputViewArnsList.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CreateIndex ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.CreateIndexInput.clientToken
                         ~f:(fun x -> ("ClientToken", (String_.to_value x)));
                      Option.map req.CreateIndexInput.tags
                        ~f:(fun x -> ("Tags", (TagMap.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CreateResourceExplorerSetup ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("RegionList",
                           (CreateResourceExplorerSetupInputRegionListList.to_value
                              req.CreateResourceExplorerSetupInput.regionList));
                      Option.map
                        req.CreateResourceExplorerSetupInput.aggregatorRegions
                        ~f:(fun x ->
                              ("AggregatorRegions",
                                (CreateResourceExplorerSetupInputAggregatorRegionsList.to_value
                                   x)));
                      Some
                        ("ViewName",
                          (CreateResourceExplorerSetupInputViewNameString.to_value
                             req.CreateResourceExplorerSetupInput.viewName))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CreateView ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.CreateViewInput.clientToken
                         ~f:(fun x ->
                               ("ClientToken",
                                 (CreateViewInputClientTokenString.to_value x)));
                      Some
                        ("ViewName",
                          (ViewName.to_value req.CreateViewInput.viewName));
                      Option.map req.CreateViewInput.includedProperties
                        ~f:(fun x ->
                              ("IncludedProperties",
                                (IncludedPropertyList.to_value x)));
                      Option.map req.CreateViewInput.scope
                        ~f:(fun x ->
                              ("Scope",
                                (CreateViewInputScopeString.to_value x)));
                      Option.map req.CreateViewInput.filters
                        ~f:(fun x -> ("Filters", (SearchFilter.to_value x)));
                      Option.map req.CreateViewInput.tags
                        ~f:(fun x -> ("Tags", (TagMap.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteIndex ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("Arn", (String_.to_value req.DeleteIndexInput.arn))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteResourceExplorerSetup ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.DeleteResourceExplorerSetupInput.regionList
                         ~f:(fun x -> ("RegionList", (RegionList.to_value x)));
                      Option.map
                        req.DeleteResourceExplorerSetupInput.deleteInAllRegions
                        ~f:(fun x ->
                              ("DeleteInAllRegions", (Boolean.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteView ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("ViewArn",
                           (DeleteViewInputViewArnString.to_value
                              req.DeleteViewInput.viewArn))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DisassociateDefaultView ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetAccountLevelServiceConfiguration ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetDefaultView ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetIndex ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetManagedView ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("ManagedViewArn",
                           (GetManagedViewInputManagedViewArnString.to_value
                              req.GetManagedViewInput.managedViewArn))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetResourceExplorerSetup ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("TaskId",
                           (GetResourceExplorerSetupInputTaskIdString.to_value
                              req.GetResourceExplorerSetupInput.taskId));
                      Option.map req.GetResourceExplorerSetupInput.maxResults
                        ~f:(fun x ->
                              ("MaxResults",
                                (GetResourceExplorerSetupInputMaxResultsInteger.to_value
                                   x)));
                      Option.map req.GetResourceExplorerSetupInput.nextToken
                        ~f:(fun x ->
                              ("NextToken",
                                (GetResourceExplorerSetupInputNextTokenString.to_value
                                   x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetServiceIndex ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetServiceView ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("ServiceViewArn",
                           (GetServiceViewInputServiceViewArnString.to_value
                              req.GetServiceViewInput.serviceViewArn))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetView ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("ViewArn",
                           (GetViewInputViewArnString.to_value
                              req.GetViewInput.viewArn))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListIndexes ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListIndexesInput.type_
                         ~f:(fun x -> ("Type", (IndexType.to_value x)));
                      Option.map req.ListIndexesInput.regions
                        ~f:(fun x ->
                              ("Regions",
                                (ListIndexesInputRegionsList.to_value x)));
                      Option.map req.ListIndexesInput.maxResults
                        ~f:(fun x ->
                              ("MaxResults",
                                (ListIndexesInputMaxResultsInteger.to_value x)));
                      Option.map req.ListIndexesInput.nextToken
                        ~f:(fun x ->
                              ("NextToken",
                                (ListIndexesInputNextTokenString.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListIndexesForMembers ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("AccountIdList",
                           (ListIndexesForMembersInputAccountIdListList.to_value
                              req.ListIndexesForMembersInput.accountIdList));
                      Option.map req.ListIndexesForMembersInput.maxResults
                        ~f:(fun x ->
                              ("MaxResults",
                                (ListIndexesForMembersInputMaxResultsInteger.to_value
                                   x)));
                      Option.map req.ListIndexesForMembersInput.nextToken
                        ~f:(fun x ->
                              ("NextToken",
                                (ListIndexesForMembersInputNextTokenString.to_value
                                   x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListManagedViews ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListManagedViewsInput.maxResults
                         ~f:(fun x ->
                               ("MaxResults",
                                 (ListManagedViewsInputMaxResultsInteger.to_value
                                    x)));
                      Option.map req.ListManagedViewsInput.nextToken
                        ~f:(fun x ->
                              ("NextToken",
                                (ListManagedViewsInputNextTokenString.to_value
                                   x)));
                      Option.map req.ListManagedViewsInput.servicePrincipal
                        ~f:(fun x ->
                              ("ServicePrincipal",
                                (ListManagedViewsInputServicePrincipalString.to_value
                                   x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListResources ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListResourcesInput.filters
                         ~f:(fun x -> ("Filters", (SearchFilter.to_value x)));
                      Option.map req.ListResourcesInput.maxResults
                        ~f:(fun x ->
                              ("MaxResults",
                                (ListResourcesInputMaxResultsInteger.to_value
                                   x)));
                      Option.map req.ListResourcesInput.viewArn
                        ~f:(fun x ->
                              ("ViewArn",
                                (ListResourcesInputViewArnString.to_value x)));
                      Option.map req.ListResourcesInput.nextToken
                        ~f:(fun x ->
                              ("NextToken",
                                (ListResourcesInputNextTokenString.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListServiceIndexes ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListServiceIndexesInput.regions
                         ~f:(fun x ->
                               ("Regions",
                                 (ListServiceIndexesInputRegionsList.to_value
                                    x)));
                      Option.map req.ListServiceIndexesInput.maxResults
                        ~f:(fun x ->
                              ("MaxResults",
                                (ListServiceIndexesInputMaxResultsInteger.to_value
                                   x)));
                      Option.map req.ListServiceIndexesInput.nextToken
                        ~f:(fun x ->
                              ("NextToken",
                                (ListServiceIndexesInputNextTokenString.to_value
                                   x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListServiceViews ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListServiceViewsInput.maxResults
                         ~f:(fun x ->
                               ("MaxResults",
                                 (ListServiceViewsInputMaxResultsInteger.to_value
                                    x)));
                      Option.map req.ListServiceViewsInput.nextToken
                        ~f:(fun x -> ("NextToken", (String_.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListStreamingAccessForServices ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.ListStreamingAccessForServicesInput.maxResults
                         ~f:(fun x ->
                               ("MaxResults",
                                 (ListStreamingAccessForServicesInputMaxResultsInteger.to_value
                                    x)));
                      Option.map
                        req.ListStreamingAccessForServicesInput.nextToken
                        ~f:(fun x -> ("NextToken", (String_.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListSupportedResourceTypes ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.ListSupportedResourceTypesInput.nextToken
                         ~f:(fun x -> ("NextToken", (String_.to_value x)));
                      Option.map
                        req.ListSupportedResourceTypesInput.maxResults
                        ~f:(fun x ->
                              ("MaxResults",
                                (ListSupportedResourceTypesInputMaxResultsInteger.to_value
                                   x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListTagsForResource ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListViews ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListViewsInput.nextToken
                         ~f:(fun x -> ("NextToken", (String_.to_value x)));
                      Option.map req.ListViewsInput.maxResults
                        ~f:(fun x ->
                              ("MaxResults",
                                (ListViewsInputMaxResultsInteger.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | Search ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("QueryString",
                           (QueryString.to_value req.SearchInput.queryString));
                      Option.map req.SearchInput.maxResults
                        ~f:(fun x ->
                              ("MaxResults",
                                (SearchInputMaxResultsInteger.to_value x)));
                      Option.map req.SearchInput.viewArn
                        ~f:(fun x ->
                              ("ViewArn",
                                (SearchInputViewArnString.to_value x)));
                      Option.map req.SearchInput.nextToken
                        ~f:(fun x ->
                              ("NextToken",
                                (SearchInputNextTokenString.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | TagResource ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.TagResourceInput.tags
                         ~f:(fun x -> ("Tags", (TagMap.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UntagResource -> Awso.Http.Request.make (method_of_endpoint endp)
  | UpdateIndexType ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("Arn",
                           (String_.to_value req.UpdateIndexTypeInput.arn));
                      Some
                        ("Type",
                          (IndexType.to_value req.UpdateIndexTypeInput.type_))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UpdateView ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("ViewArn",
                           (UpdateViewInputViewArnString.to_value
                              req.UpdateViewInput.viewArn));
                      Option.map req.UpdateViewInput.includedProperties
                        ~f:(fun x ->
                              ("IncludedProperties",
                                (IncludedPropertyList.to_value x)));
                      Option.map req.UpdateViewInput.filters
                        ~f:(fun x -> ("Filters", (SearchFilter.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | AssociateDefaultView ->
      if is_success
      then Ok (AssociateDefaultViewOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some AssociateDefaultViewOutput.error_of_json))
  | BatchGetView ->
      if is_success
      then Ok (BatchGetViewOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some BatchGetViewOutput.error_of_json))
  | CreateIndex ->
      if is_success
      then Ok (CreateIndexOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some CreateIndexOutput.error_of_json))
  | CreateResourceExplorerSetup ->
      if is_success
      then
        Ok
          (CreateResourceExplorerSetupOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some CreateResourceExplorerSetupOutput.error_of_json))
  | CreateView ->
      if is_success
      then Ok (CreateViewOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some CreateViewOutput.error_of_json))
  | DeleteIndex ->
      if is_success
      then Ok (DeleteIndexOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some DeleteIndexOutput.error_of_json))
  | DeleteResourceExplorerSetup ->
      if is_success
      then
        Ok
          (DeleteResourceExplorerSetupOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some DeleteResourceExplorerSetupOutput.error_of_json))
  | DeleteView ->
      if is_success
      then Ok (DeleteViewOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some DeleteViewOutput.error_of_json))
  | DisassociateDefaultView ->
      if is_success then Ok () else Error (parse_aws_error None)
  | GetAccountLevelServiceConfiguration ->
      if is_success
      then
        Ok
          (GetAccountLevelServiceConfigurationOutput.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some GetAccountLevelServiceConfigurationOutput.error_of_json))
  | GetDefaultView ->
      if is_success
      then Ok (GetDefaultViewOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetDefaultViewOutput.error_of_json))
  | GetIndex ->
      if is_success
      then Ok (GetIndexOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetIndexOutput.error_of_json))
  | GetManagedView ->
      if is_success
      then Ok (GetManagedViewOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetManagedViewOutput.error_of_json))
  | GetResourceExplorerSetup ->
      if is_success
      then
        Ok (GetResourceExplorerSetupOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some GetResourceExplorerSetupOutput.error_of_json))
  | GetServiceIndex ->
      if is_success
      then Ok (GetServiceIndexOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetServiceIndexOutput.error_of_json))
  | GetServiceView ->
      if is_success
      then Ok (GetServiceViewOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetServiceViewOutput.error_of_json))
  | GetView ->
      if is_success
      then Ok (GetViewOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetViewOutput.error_of_json))
  | ListIndexes ->
      if is_success
      then Ok (ListIndexesOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListIndexesOutput.error_of_json))
  | ListIndexesForMembers ->
      if is_success
      then Ok (ListIndexesForMembersOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListIndexesForMembersOutput.error_of_json))
  | ListManagedViews ->
      if is_success
      then Ok (ListManagedViewsOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some ListManagedViewsOutput.error_of_json))
  | ListResources ->
      if is_success
      then Ok (ListResourcesOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListResourcesOutput.error_of_json))
  | ListServiceIndexes ->
      if is_success
      then Ok (ListServiceIndexesOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some ListServiceIndexesOutput.error_of_json))
  | ListServiceViews ->
      if is_success
      then Ok (ListServiceViewsOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some ListServiceViewsOutput.error_of_json))
  | ListStreamingAccessForServices ->
      if is_success
      then
        Ok
          (ListStreamingAccessForServicesOutput.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some ListStreamingAccessForServicesOutput.error_of_json))
  | ListSupportedResourceTypes ->
      if is_success
      then
        Ok (ListSupportedResourceTypesOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some ListSupportedResourceTypesOutput.error_of_json))
  | ListTagsForResource ->
      if is_success
      then Ok (ListTagsForResourceOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListTagsForResourceOutput.error_of_json))
  | ListViews ->
      if is_success
      then Ok (ListViewsOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListViewsOutput.error_of_json))
  | Search ->
      if is_success
      then Ok (SearchOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some SearchOutput.error_of_json))
  | TagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (TagResourceOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some TagResourceOutput.error_of_json))
  | UntagResource ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (UntagResourceOutput.of_header_and_body (headers, ()))
      else Error (parse_aws_error (Some UntagResourceOutput.error_of_json))
  | UpdateIndexType ->
      if is_success
      then Ok (UpdateIndexTypeOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some UpdateIndexTypeOutput.error_of_json))
  | UpdateView ->
      if is_success
      then Ok (UpdateViewOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some UpdateViewOutput.error_of_json))
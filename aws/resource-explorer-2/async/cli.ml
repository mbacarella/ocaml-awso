(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_default_view =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and viewArn =
         flag "view-arn" (required string)
           ~doc:"STRING AssociateDefaultViewInputViewArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_default_view
           (Values.AssociateDefaultViewInput.make ~viewArn ())
           (Some Values.AssociateDefaultViewOutput.to_json)
           (Some Values.AssociateDefaultViewOutput.error_to_json)])
let batch_get_view =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and viewArns =
         flag "view-arns" (optional json_arg)
           ~doc:"JSON BatchGetViewInputViewArnsList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_view
           (Values.BatchGetViewInput.make
              ?viewArns:(Option.map
                           ~f:Values.BatchGetViewInputViewArnsList.of_json
                           viewArns) ())
           (Some Values.BatchGetViewOutput.to_json)
           (Some Values.BatchGetViewOutput.error_to_json)])
let create_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_index
           (Values.CreateIndexInput.make ?clientToken
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ())
           (Some Values.CreateIndexOutput.to_json)
           (Some Values.CreateIndexOutput.error_to_json)])
let create_resource_explorer_setup =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and aggregatorRegions =
         flag "aggregator-regions" (optional json_arg)
           ~doc:"JSON CreateResourceExplorerSetupInputAggregatorRegionsList"
       and regionList =
         flag "region-list" (required json_arg)
           ~doc:"JSON CreateResourceExplorerSetupInputRegionListList"
       and viewName =
         flag "view-name" (required string)
           ~doc:"STRING CreateResourceExplorerSetupInputViewNameString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_resource_explorer_setup
           (Values.CreateResourceExplorerSetupInput.make
              ?aggregatorRegions:(Option.map
                                    ~f:Values.CreateResourceExplorerSetupInputAggregatorRegionsList.of_json
                                    aggregatorRegions)
              ~regionList:(Values.CreateResourceExplorerSetupInputRegionListList.of_json
                             regionList) ~viewName ())
           (Some Values.CreateResourceExplorerSetupOutput.to_json)
           (Some Values.CreateResourceExplorerSetupOutput.error_to_json)])
let create_view =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING CreateViewInputClientTokenString"
       and includedProperties =
         flag "included-properties" (optional json_arg)
           ~doc:"JSON IncludedPropertyList"
       and scope =
         flag "scope" (optional string)
           ~doc:"STRING CreateViewInputScopeString"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON SearchFilter"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and viewName =
         flag "view-name" (required string) ~doc:"STRING ViewName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_view
           (Values.CreateViewInput.make ?clientToken
              ?includedProperties:(Option.map
                                     ~f:Values.IncludedPropertyList.of_json
                                     includedProperties) ?scope
              ?filters:(Option.map ~f:Values.SearchFilter.of_json filters)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~viewName ())
           (Some Values.CreateViewOutput.to_json)
           (Some Values.CreateViewOutput.error_to_json)])
let delete_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_index (Values.DeleteIndexInput.make ~arn ())
           (Some Values.DeleteIndexOutput.to_json)
           (Some Values.DeleteIndexOutput.error_to_json)])
let delete_resource_explorer_setup =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and regionList =
         flag "region-list" (optional json_arg) ~doc:"JSON RegionList"
       and deleteInAllRegions =
         flag "delete-in-all-regions" (optional bool) ~doc:"BOOL Boolean" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_explorer_setup
           (Values.DeleteResourceExplorerSetupInput.make
              ?regionList:(Option.map ~f:Values.RegionList.of_json regionList)
              ?deleteInAllRegions ())
           (Some Values.DeleteResourceExplorerSetupOutput.to_json)
           (Some Values.DeleteResourceExplorerSetupOutput.error_to_json)])
let delete_view =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and viewArn =
         flag "view-arn" (required string)
           ~doc:"STRING DeleteViewInputViewArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_view (Values.DeleteViewInput.make ~viewArn ())
           (Some Values.DeleteViewOutput.to_json)
           (Some Values.DeleteViewOutput.error_to_json)])
let disassociate_default_view =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_default_view (Fn.id ()) None None])
let get_account_level_service_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_account_level_service_configuration (Fn.id ())
           (Some Values.GetAccountLevelServiceConfigurationOutput.to_json)
           (Some
              Values.GetAccountLevelServiceConfigurationOutput.error_to_json)])
let get_default_view =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_default_view (Fn.id ())
           (Some Values.GetDefaultViewOutput.to_json)
           (Some Values.GetDefaultViewOutput.error_to_json)])
let get_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_index (Fn.id ()) (Some Values.GetIndexOutput.to_json)
           (Some Values.GetIndexOutput.error_to_json)])
let get_managed_view =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and managedViewArn =
         flag "managed-view-arn" (required string)
           ~doc:"STRING GetManagedViewInputManagedViewArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_managed_view
           (Values.GetManagedViewInput.make ~managedViewArn ())
           (Some Values.GetManagedViewOutput.to_json)
           (Some Values.GetManagedViewOutput.error_to_json)])
let get_resource_explorer_setup =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT GetResourceExplorerSetupInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING GetResourceExplorerSetupInputNextTokenString"
       and taskId =
         flag "task-id" (required string)
           ~doc:"STRING GetResourceExplorerSetupInputTaskIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_explorer_setup
           (Values.GetResourceExplorerSetupInput.make ?maxResults ?nextToken
              ~taskId ())
           (Some Values.GetResourceExplorerSetupOutput.to_json)
           (Some Values.GetResourceExplorerSetupOutput.error_to_json)])
let get_service_index =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service_index (Fn.id ())
           (Some Values.GetServiceIndexOutput.to_json)
           (Some Values.GetServiceIndexOutput.error_to_json)])
let get_service_view =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceViewArn =
         flag "service-view-arn" (required string)
           ~doc:"STRING GetServiceViewInputServiceViewArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_service_view
           (Values.GetServiceViewInput.make ~serviceViewArn ())
           (Some Values.GetServiceViewOutput.to_json)
           (Some Values.GetServiceViewOutput.error_to_json)])
let get_view =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and viewArn =
         flag "view-arn" (required string)
           ~doc:"STRING GetViewInputViewArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_view (Values.GetViewInput.make ~viewArn ())
           (Some Values.GetViewOutput.to_json)
           (Some Values.GetViewOutput.error_to_json)])
let list_indexes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ = flag "type-" (optional json_arg) ~doc:"JSON IndexType"
       and regions =
         flag "regions" (optional json_arg)
           ~doc:"JSON ListIndexesInputRegionsList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListIndexesInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListIndexesInputNextTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_indexes
           (Values.ListIndexesInput.make
              ?type_:(Option.map ~f:Values.IndexType.of_json type_)
              ?regions:(Option.map
                          ~f:Values.ListIndexesInputRegionsList.of_json
                          regions) ?maxResults ?nextToken ())
           (Some Values.ListIndexesOutput.to_json)
           (Some Values.ListIndexesOutput.error_to_json)])
let list_indexes_for_members =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListIndexesForMembersInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListIndexesForMembersInputNextTokenString"
       and accountIdList =
         flag "account-id-list" (required json_arg)
           ~doc:"JSON ListIndexesForMembersInputAccountIdListList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_indexes_for_members
           (Values.ListIndexesForMembersInput.make ?maxResults ?nextToken
              ~accountIdList:(Values.ListIndexesForMembersInputAccountIdListList.of_json
                                accountIdList) ())
           (Some Values.ListIndexesForMembersOutput.to_json)
           (Some Values.ListIndexesForMembersOutput.error_to_json)])
let list_managed_views =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListManagedViewsInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListManagedViewsInputNextTokenString"
       and servicePrincipal =
         flag "service-principal" (optional string)
           ~doc:"STRING ListManagedViewsInputServicePrincipalString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_managed_views
           (Values.ListManagedViewsInput.make ?maxResults ?nextToken
              ?servicePrincipal ())
           (Some Values.ListManagedViewsOutput.to_json)
           (Some Values.ListManagedViewsOutput.error_to_json)])
let list_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON SearchFilter"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListResourcesInputMaxResultsInteger"
       and viewArn =
         flag "view-arn" (optional string)
           ~doc:"STRING ListResourcesInputViewArnString"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListResourcesInputNextTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resources
           (Values.ListResourcesInput.make
              ?filters:(Option.map ~f:Values.SearchFilter.of_json filters)
              ?maxResults ?viewArn ?nextToken ())
           (Some Values.ListResourcesOutput.to_json)
           (Some Values.ListResourcesOutput.error_to_json)])
let list_service_indexes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and regions =
         flag "regions" (optional json_arg)
           ~doc:"JSON ListServiceIndexesInputRegionsList"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListServiceIndexesInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListServiceIndexesInputNextTokenString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_indexes
           (Values.ListServiceIndexesInput.make
              ?regions:(Option.map
                          ~f:Values.ListServiceIndexesInputRegionsList.of_json
                          regions) ?maxResults ?nextToken ())
           (Some Values.ListServiceIndexesOutput.to_json)
           (Some Values.ListServiceIndexesOutput.error_to_json)])
let list_service_views =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListServiceViewsInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_service_views
           (Values.ListServiceViewsInput.make ?maxResults ?nextToken ())
           (Some Values.ListServiceViewsOutput.to_json)
           (Some Values.ListServiceViewsOutput.error_to_json)])
let list_streaming_access_for_services =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListStreamingAccessForServicesInputMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_streaming_access_for_services
           (Values.ListStreamingAccessForServicesInput.make ?maxResults
              ?nextToken ())
           (Some Values.ListStreamingAccessForServicesOutput.to_json)
           (Some Values.ListStreamingAccessForServicesOutput.error_to_json)])
let list_supported_resource_types =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListSupportedResourceTypesInputMaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_supported_resource_types
           (Values.ListSupportedResourceTypesInput.make ?nextToken
              ?maxResults ())
           (Some Values.ListSupportedResourceTypesOutput.to_json)
           (Some Values.ListSupportedResourceTypesOutput.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceInput.make ~resourceArn ())
           (Some Values.ListTagsForResourceOutput.to_json)
           (Some Values.ListTagsForResourceOutput.error_to_json)])
let list_views =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListViewsInputMaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_views
           (Values.ListViewsInput.make ?nextToken ?maxResults ())
           (Some Values.ListViewsOutput.to_json)
           (Some Values.ListViewsOutput.error_to_json)])
let search =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT SearchInputMaxResultsInteger"
       and viewArn =
         flag "view-arn" (optional string)
           ~doc:"STRING SearchInputViewArnString"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING SearchInputNextTokenString"
       and queryString =
         flag "query-string" (required string) ~doc:"STRING QueryString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region Io.search
           (Values.SearchInput.make ?maxResults ?viewArn ?nextToken
              ~queryString ()) (Some Values.SearchOutput.to_json)
           (Some Values.SearchOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~resourceArn
              ()) (Some Values.TagResourceOutput.to_json)
           (Some Values.TagResourceOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING String"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON StringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.StringList.of_json tagKeys) ())
           (Some Values.UntagResourceOutput.to_json)
           (Some Values.UntagResourceOutput.error_to_json)])
let update_index_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING String"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON IndexType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_index_type
           (Values.UpdateIndexTypeInput.make ~arn
              ~type_:(Values.IndexType.of_json type_) ())
           (Some Values.UpdateIndexTypeOutput.to_json)
           (Some Values.UpdateIndexTypeOutput.error_to_json)])
let update_view =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includedProperties =
         flag "included-properties" (optional json_arg)
           ~doc:"JSON IncludedPropertyList"
       and filters =
         flag "filters" (optional json_arg) ~doc:"JSON SearchFilter"
       and viewArn =
         flag "view-arn" (required string)
           ~doc:"STRING UpdateViewInputViewArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_view
           (Values.UpdateViewInput.make
              ?includedProperties:(Option.map
                                     ~f:Values.IncludedPropertyList.of_json
                                     includedProperties)
              ?filters:(Option.map ~f:Values.SearchFilter.of_json filters)
              ~viewArn ()) (Some Values.UpdateViewOutput.to_json)
           (Some Values.UpdateViewOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-default-view", associate_default_view);
    ("batch-get-view", batch_get_view);
    ("create-index", create_index);
    ("create-resource-explorer-setup", create_resource_explorer_setup);
    ("create-view", create_view);
    ("delete-index", delete_index);
    ("delete-resource-explorer-setup", delete_resource_explorer_setup);
    ("delete-view", delete_view);
    ("disassociate-default-view", disassociate_default_view);
    ("get-account-level-service-configuration",
      get_account_level_service_configuration);
    ("get-default-view", get_default_view);
    ("get-index", get_index);
    ("get-managed-view", get_managed_view);
    ("get-resource-explorer-setup", get_resource_explorer_setup);
    ("get-service-index", get_service_index);
    ("get-service-view", get_service_view);
    ("get-view", get_view);
    ("list-indexes", list_indexes);
    ("list-indexes-for-members", list_indexes_for_members);
    ("list-managed-views", list_managed_views);
    ("list-resources", list_resources);
    ("list-service-indexes", list_service_indexes);
    ("list-service-views", list_service_views);
    ("list-streaming-access-for-services",
      list_streaming_access_for_services);
    ("list-supported-resource-types", list_supported_resource_types);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-views", list_views);
    ("search", search);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-index-type", update_index_type);
    ("update-view", update_view)]
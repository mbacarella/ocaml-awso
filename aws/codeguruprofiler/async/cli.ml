(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_notification_channels =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channels =
         flag "channels" (required json_arg) ~doc:"JSON Channels"
       and profilingGroupName =
         flag "profiling-group-name" (required string)
           ~doc:"STRING ProfilingGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_notification_channels
           (Values.AddNotificationChannelsRequest.make
              ~channels:(Values.Channels.of_json channels)
              ~profilingGroupName ())
           (Some Values.AddNotificationChannelsResponse.to_json)
           (Some Values.AddNotificationChannelsResponse.error_to_json)])
let batch_get_frame_metric_data =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and endTime =
         flag "end-time" (optional json_arg) ~doc:"JSON Timestamp"
       and frameMetrics =
         flag "frame-metrics" (optional json_arg) ~doc:"JSON FrameMetrics"
       and period = flag "period" (optional string) ~doc:"STRING Period"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON Timestamp"
       and targetResolution =
         flag "target-resolution" (optional json_arg)
           ~doc:"JSON AggregationPeriod"
       and profilingGroupName =
         flag "profiling-group-name" (required string)
           ~doc:"STRING ProfilingGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_frame_metric_data
           (Values.BatchGetFrameMetricDataRequest.make
              ?endTime:(Option.map ~f:Values.Timestamp.of_json endTime)
              ?frameMetrics:(Option.map ~f:Values.FrameMetrics.of_json
                               frameMetrics) ?period
              ?startTime:(Option.map ~f:Values.Timestamp.of_json startTime)
              ?targetResolution:(Option.map
                                   ~f:Values.AggregationPeriod.of_json
                                   targetResolution) ~profilingGroupName ())
           (Some Values.BatchGetFrameMetricDataResponse.to_json)
           (Some Values.BatchGetFrameMetricDataResponse.error_to_json)])
let configure_agent =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and fleetInstanceId =
         flag "fleet-instance-id" (optional string)
           ~doc:"STRING FleetInstanceId"
       and metadata =
         flag "metadata" (optional json_arg) ~doc:"JSON Metadata"
       and profilingGroupName =
         flag "profiling-group-name" (required string)
           ~doc:"STRING ProfilingGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.configure_agent
           (Values.ConfigureAgentRequest.make ?fleetInstanceId
              ?metadata:(Option.map ~f:Values.Metadata.of_json metadata)
              ~profilingGroupName ())
           (Some Values.ConfigureAgentResponse.to_json)
           (Some Values.ConfigureAgentResponse.error_to_json)])
let create_profiling_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentOrchestrationConfig =
         flag "agent-orchestration-config" (optional json_arg)
           ~doc:"JSON AgentOrchestrationConfig"
       and computePlatform =
         flag "compute-platform" (optional json_arg)
           ~doc:"JSON ComputePlatform"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagsMap"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken"
       and profilingGroupName =
         flag "profiling-group-name" (required string)
           ~doc:"STRING ProfilingGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_profiling_group
           (Values.CreateProfilingGroupRequest.make
              ?agentOrchestrationConfig:(Option.map
                                           ~f:Values.AgentOrchestrationConfig.of_json
                                           agentOrchestrationConfig)
              ?computePlatform:(Option.map ~f:Values.ComputePlatform.of_json
                                  computePlatform)
              ?tags:(Option.map ~f:Values.TagsMap.of_json tags) ~clientToken
              ~profilingGroupName ())
           (Some Values.CreateProfilingGroupResponse.to_json)
           (Some Values.CreateProfilingGroupResponse.error_to_json)])
let delete_profiling_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profilingGroupName =
         flag "profiling-group-name" (required string)
           ~doc:"STRING ProfilingGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_profiling_group
           (Values.DeleteProfilingGroupRequest.make ~profilingGroupName ())
           (Some Values.DeleteProfilingGroupResponse.to_json)
           (Some Values.DeleteProfilingGroupResponse.error_to_json)])
let describe_profiling_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profilingGroupName =
         flag "profiling-group-name" (required string)
           ~doc:"STRING ProfilingGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_profiling_group
           (Values.DescribeProfilingGroupRequest.make ~profilingGroupName ())
           (Some Values.DescribeProfilingGroupResponse.to_json)
           (Some Values.DescribeProfilingGroupResponse.error_to_json)])
let get_findings_report_account_summary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dailyReportsOnly =
         flag "daily-reports-only" (optional bool) ~doc:"BOOL Boolean"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_findings_report_account_summary
           (Values.GetFindingsReportAccountSummaryRequest.make
              ?dailyReportsOnly ?maxResults ?nextToken ())
           (Some Values.GetFindingsReportAccountSummaryResponse.to_json)
           (Some Values.GetFindingsReportAccountSummaryResponse.error_to_json)])
let get_notification_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profilingGroupName =
         flag "profiling-group-name" (required string)
           ~doc:"STRING ProfilingGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_notification_configuration
           (Values.GetNotificationConfigurationRequest.make
              ~profilingGroupName ())
           (Some Values.GetNotificationConfigurationResponse.to_json)
           (Some Values.GetNotificationConfigurationResponse.error_to_json)])
let get_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profilingGroupName =
         flag "profiling-group-name" (required string)
           ~doc:"STRING ProfilingGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_policy
           (Values.GetPolicyRequest.make ~profilingGroupName ())
           (Some Values.GetPolicyResponse.to_json)
           (Some Values.GetPolicyResponse.error_to_json)])
let get_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accept = flag "accept" (optional string) ~doc:"STRING String"
       and endTime =
         flag "end-time" (optional json_arg) ~doc:"JSON Timestamp"
       and maxDepth = flag "max-depth" (optional int) ~doc:"INT MaxDepth"
       and period = flag "period" (optional string) ~doc:"STRING Period"
       and startTime =
         flag "start-time" (optional json_arg) ~doc:"JSON Timestamp"
       and profilingGroupName =
         flag "profiling-group-name" (required string)
           ~doc:"STRING ProfilingGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_profile
           (Values.GetProfileRequest.make ?accept
              ?endTime:(Option.map ~f:Values.Timestamp.of_json endTime)
              ?maxDepth ?period
              ?startTime:(Option.map ~f:Values.Timestamp.of_json startTime)
              ~profilingGroupName ())
           (Some Values.GetProfileResponse.to_json)
           (Some Values.GetProfileResponse.error_to_json)])
let get_recommendations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and locale = flag "locale" (optional string) ~doc:"STRING Locale"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON Timestamp"
       and profilingGroupName =
         flag "profiling-group-name" (required string)
           ~doc:"STRING ProfilingGroupName"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_recommendations
           (Values.GetRecommendationsRequest.make ?locale
              ~endTime:(Values.Timestamp.of_json endTime) ~profilingGroupName
              ~startTime:(Values.Timestamp.of_json startTime) ())
           (Some Values.GetRecommendationsResponse.to_json)
           (Some Values.GetRecommendationsResponse.error_to_json)])
let list_findings_reports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dailyReportsOnly =
         flag "daily-reports-only" (optional bool) ~doc:"BOOL Boolean"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON Timestamp"
       and profilingGroupName =
         flag "profiling-group-name" (required string)
           ~doc:"STRING ProfilingGroupName"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_findings_reports
           (Values.ListFindingsReportsRequest.make ?dailyReportsOnly
              ?maxResults ?nextToken
              ~endTime:(Values.Timestamp.of_json endTime) ~profilingGroupName
              ~startTime:(Values.Timestamp.of_json startTime) ())
           (Some Values.ListFindingsReportsResponse.to_json)
           (Some Values.ListFindingsReportsResponse.error_to_json)])
let list_profile_times =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and orderBy = flag "order-by" (optional json_arg) ~doc:"JSON OrderBy"
       and endTime =
         flag "end-time" (required json_arg) ~doc:"JSON Timestamp"
       and period =
         flag "period" (required json_arg) ~doc:"JSON AggregationPeriod"
       and profilingGroupName =
         flag "profiling-group-name" (required string)
           ~doc:"STRING ProfilingGroupName"
       and startTime =
         flag "start-time" (required json_arg) ~doc:"JSON Timestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_profile_times
           (Values.ListProfileTimesRequest.make ?maxResults ?nextToken
              ?orderBy:(Option.map ~f:Values.OrderBy.of_json orderBy)
              ~endTime:(Values.Timestamp.of_json endTime)
              ~period:(Values.AggregationPeriod.of_json period)
              ~profilingGroupName
              ~startTime:(Values.Timestamp.of_json startTime) ())
           (Some Values.ListProfileTimesResponse.to_json)
           (Some Values.ListProfileTimesResponse.error_to_json)])
let list_profiling_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and includeDescription =
         flag "include-description" (optional bool) ~doc:"BOOL Boolean"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_profiling_groups
           (Values.ListProfilingGroupsRequest.make ?includeDescription
              ?maxResults ?nextToken ())
           (Some Values.ListProfilingGroupsResponse.to_json)
           (Some Values.ListProfilingGroupsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING ProfilingGroupArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let post_agent_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and profileToken =
         flag "profile-token" (optional string) ~doc:"STRING ClientToken"
       and agentProfile =
         flag "agent-profile" (required json_arg) ~doc:"JSON AgentProfile"
       and contentType =
         flag "content-type" (required string) ~doc:"STRING String"
       and profilingGroupName =
         flag "profiling-group-name" (required string)
           ~doc:"STRING ProfilingGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.post_agent_profile
           (Values.PostAgentProfileRequest.make ?profileToken
              ~agentProfile:(Values.AgentProfile.of_json agentProfile)
              ~contentType ~profilingGroupName ())
           (Some Values.PostAgentProfileResponse.to_json)
           (Some Values.PostAgentProfileResponse.error_to_json)])
let put_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and revisionId =
         flag "revision-id" (optional string) ~doc:"STRING RevisionId"
       and actionGroup =
         flag "action-group" (required json_arg) ~doc:"JSON ActionGroup"
       and principals =
         flag "principals" (required json_arg) ~doc:"JSON Principals"
       and profilingGroupName =
         flag "profiling-group-name" (required string)
           ~doc:"STRING ProfilingGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_permission
           (Values.PutPermissionRequest.make ?revisionId
              ~actionGroup:(Values.ActionGroup.of_json actionGroup)
              ~principals:(Values.Principals.of_json principals)
              ~profilingGroupName ())
           (Some Values.PutPermissionResponse.to_json)
           (Some Values.PutPermissionResponse.error_to_json)])
let remove_notification_channel =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and channelId =
         flag "channel-id" (required string) ~doc:"STRING ChannelId"
       and profilingGroupName =
         flag "profiling-group-name" (required string)
           ~doc:"STRING ProfilingGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_notification_channel
           (Values.RemoveNotificationChannelRequest.make ~channelId
              ~profilingGroupName ())
           (Some Values.RemoveNotificationChannelResponse.to_json)
           (Some Values.RemoveNotificationChannelResponse.error_to_json)])
let remove_permission =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and actionGroup =
         flag "action-group" (required json_arg) ~doc:"JSON ActionGroup"
       and profilingGroupName =
         flag "profiling-group-name" (required string)
           ~doc:"STRING ProfilingGroupName"
       and revisionId =
         flag "revision-id" (required string) ~doc:"STRING RevisionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_permission
           (Values.RemovePermissionRequest.make
              ~actionGroup:(Values.ActionGroup.of_json actionGroup)
              ~profilingGroupName ~revisionId ())
           (Some Values.RemovePermissionResponse.to_json)
           (Some Values.RemovePermissionResponse.error_to_json)])
let submit_feedback =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and comment = flag "comment" (optional string) ~doc:"STRING String"
       and anomalyInstanceId =
         flag "anomaly-instance-id" (required string)
           ~doc:"STRING AnomalyInstanceId"
       and profilingGroupName =
         flag "profiling-group-name" (required string)
           ~doc:"STRING ProfilingGroupName"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON FeedbackType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.submit_feedback
           (Values.SubmitFeedbackRequest.make ?comment ~anomalyInstanceId
              ~profilingGroupName ~type_:(Values.FeedbackType.of_json type_)
              ()) (Some Values.SubmitFeedbackResponse.to_json)
           (Some Values.SubmitFeedbackResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING ProfilingGroupArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagsMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagsMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING ProfilingGroupArn"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_profiling_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and agentOrchestrationConfig =
         flag "agent-orchestration-config" (required json_arg)
           ~doc:"JSON AgentOrchestrationConfig"
       and profilingGroupName =
         flag "profiling-group-name" (required string)
           ~doc:"STRING ProfilingGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_profiling_group
           (Values.UpdateProfilingGroupRequest.make
              ~agentOrchestrationConfig:(Values.AgentOrchestrationConfig.of_json
                                           agentOrchestrationConfig)
              ~profilingGroupName ())
           (Some Values.UpdateProfilingGroupResponse.to_json)
           (Some Values.UpdateProfilingGroupResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("add-notification-channels", add_notification_channels);
    ("batch-get-frame-metric-data", batch_get_frame_metric_data);
    ("configure-agent", configure_agent);
    ("create-profiling-group", create_profiling_group);
    ("delete-profiling-group", delete_profiling_group);
    ("describe-profiling-group", describe_profiling_group);
    ("get-findings-report-account-summary",
      get_findings_report_account_summary);
    ("get-notification-configuration", get_notification_configuration);
    ("get-policy", get_policy);
    ("get-profile", get_profile);
    ("get-recommendations", get_recommendations);
    ("list-findings-reports", list_findings_reports);
    ("list-profile-times", list_profile_times);
    ("list-profiling-groups", list_profiling_groups);
    ("list-tags-for-resource", list_tags_for_resource);
    ("post-agent-profile", post_agent_profile);
    ("put-permission", put_permission);
    ("remove-notification-channel", remove_notification_channel);
    ("remove-permission", remove_permission);
    ("submit-feedback", submit_feedback);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-profiling-group", update_profiling_group)]
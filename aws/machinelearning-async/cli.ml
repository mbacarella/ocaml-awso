(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING EntityId"
       and resourceType =
         flag "resource-type" (required json_arg)
           ~doc:"JSON TaggableResourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_tags
           (Values.AddTagsInput.make ~tags:(Values.TagList.of_json tags)
              ~resourceId
              ~resourceType:(Values.TaggableResourceType.of_json resourceType)
              ()) (Some Values.AddTagsOutput.to_json)
           (Some Values.AddTagsOutput.error_to_json)])
let create_batch_prediction =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and batchPredictionName =
         flag "batch-prediction-name" (optional string)
           ~doc:"STRING EntityName"
       and batchPredictionId =
         flag "batch-prediction-id" (required string) ~doc:"STRING EntityId"
       and mLModelId =
         flag "m-l-model-id" (required string) ~doc:"STRING EntityId"
       and batchPredictionDataSourceId =
         flag "batch-prediction-data-source-id" (required string)
           ~doc:"STRING EntityId"
       and outputUri =
         flag "output-uri" (required string) ~doc:"STRING S3Url" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_batch_prediction
           (Values.CreateBatchPredictionInput.make ?batchPredictionName
              ~batchPredictionId ~mLModelId ~batchPredictionDataSourceId
              ~outputUri ())
           (Some Values.CreateBatchPredictionOutput.to_json)
           (Some Values.CreateBatchPredictionOutput.error_to_json)])
let create_data_source_from_r_d_s =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataSourceName =
         flag "data-source-name" (optional string) ~doc:"STRING EntityName"
       and computeStatistics =
         flag "compute-statistics" (optional bool)
           ~doc:"BOOL ComputeStatistics"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING EntityId"
       and rDSData =
         flag "r-d-s-data" (required json_arg) ~doc:"JSON RDSDataSpec"
       and roleARN =
         flag "role-a-r-n" (required string) ~doc:"STRING RoleARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_source_from_r_d_s
           (Values.CreateDataSourceFromRDSInput.make ?dataSourceName
              ?computeStatistics ~dataSourceId
              ~rDSData:(Values.RDSDataSpec.of_json rDSData) ~roleARN ())
           (Some Values.CreateDataSourceFromRDSOutput.to_json)
           (Some Values.CreateDataSourceFromRDSOutput.error_to_json)])
let create_data_source_from_redshift =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataSourceName =
         flag "data-source-name" (optional string) ~doc:"STRING EntityName"
       and computeStatistics =
         flag "compute-statistics" (optional bool)
           ~doc:"BOOL ComputeStatistics"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING EntityId"
       and dataSpec =
         flag "data-spec" (required json_arg) ~doc:"JSON RedshiftDataSpec"
       and roleARN =
         flag "role-a-r-n" (required string) ~doc:"STRING RoleARN" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_source_from_redshift
           (Values.CreateDataSourceFromRedshiftInput.make ?dataSourceName
              ?computeStatistics ~dataSourceId
              ~dataSpec:(Values.RedshiftDataSpec.of_json dataSpec) ~roleARN
              ()) (Some Values.CreateDataSourceFromRedshiftOutput.to_json)
           (Some Values.CreateDataSourceFromRedshiftOutput.error_to_json)])
let create_data_source_from_s3 =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataSourceName =
         flag "data-source-name" (optional string) ~doc:"STRING EntityName"
       and computeStatistics =
         flag "compute-statistics" (optional bool)
           ~doc:"BOOL ComputeStatistics"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING EntityId"
       and dataSpec =
         flag "data-spec" (required json_arg) ~doc:"JSON S3DataSpec" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_data_source_from_s3
           (Values.CreateDataSourceFromS3Input.make ?dataSourceName
              ?computeStatistics ~dataSourceId
              ~dataSpec:(Values.S3DataSpec.of_json dataSpec) ())
           (Some Values.CreateDataSourceFromS3Output.to_json)
           (Some Values.CreateDataSourceFromS3Output.error_to_json)])
let create_evaluation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and evaluationName =
         flag "evaluation-name" (optional string) ~doc:"STRING EntityName"
       and evaluationId =
         flag "evaluation-id" (required string) ~doc:"STRING EntityId"
       and mLModelId =
         flag "m-l-model-id" (required string) ~doc:"STRING EntityId"
       and evaluationDataSourceId =
         flag "evaluation-data-source-id" (required string)
           ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_evaluation
           (Values.CreateEvaluationInput.make ?evaluationName ~evaluationId
              ~mLModelId ~evaluationDataSourceId ())
           (Some Values.CreateEvaluationOutput.to_json)
           (Some Values.CreateEvaluationOutput.error_to_json)])
let create_m_l_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mLModelName =
         flag "m-l-model-name" (optional string) ~doc:"STRING EntityName"
       and parameters =
         flag "parameters" (optional json_arg) ~doc:"JSON TrainingParameters"
       and recipe = flag "recipe" (optional string) ~doc:"STRING Recipe"
       and recipeUri =
         flag "recipe-uri" (optional string) ~doc:"STRING S3Url"
       and mLModelId =
         flag "m-l-model-id" (required string) ~doc:"STRING EntityId"
       and mLModelType =
         flag "m-l-model-type" (required json_arg) ~doc:"JSON MLModelType"
       and trainingDataSourceId =
         flag "training-data-source-id" (required string)
           ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_m_l_model
           (Values.CreateMLModelInput.make ?mLModelName
              ?parameters:(Option.map ~f:Values.TrainingParameters.of_json
                             parameters) ?recipe ?recipeUri ~mLModelId
              ~mLModelType:(Values.MLModelType.of_json mLModelType)
              ~trainingDataSourceId ())
           (Some Values.CreateMLModelOutput.to_json)
           (Some Values.CreateMLModelOutput.error_to_json)])
let create_realtime_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mLModelId =
         flag "m-l-model-id" (required string) ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_realtime_endpoint
           (Values.CreateRealtimeEndpointInput.make ~mLModelId ())
           (Some Values.CreateRealtimeEndpointOutput.to_json)
           (Some Values.CreateRealtimeEndpointOutput.error_to_json)])
let delete_batch_prediction =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and batchPredictionId =
         flag "batch-prediction-id" (required string) ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_batch_prediction
           (Values.DeleteBatchPredictionInput.make ~batchPredictionId ())
           (Some Values.DeleteBatchPredictionOutput.to_json)
           (Some Values.DeleteBatchPredictionOutput.error_to_json)])
let delete_data_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_data_source
           (Values.DeleteDataSourceInput.make ~dataSourceId ())
           (Some Values.DeleteDataSourceOutput.to_json)
           (Some Values.DeleteDataSourceOutput.error_to_json)])
let delete_evaluation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and evaluationId =
         flag "evaluation-id" (required string) ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_evaluation
           (Values.DeleteEvaluationInput.make ~evaluationId ())
           (Some Values.DeleteEvaluationOutput.to_json)
           (Some Values.DeleteEvaluationOutput.error_to_json)])
let delete_m_l_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mLModelId =
         flag "m-l-model-id" (required string) ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_m_l_model (Values.DeleteMLModelInput.make ~mLModelId ())
           (Some Values.DeleteMLModelOutput.to_json)
           (Some Values.DeleteMLModelOutput.error_to_json)])
let delete_realtime_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mLModelId =
         flag "m-l-model-id" (required string) ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_realtime_endpoint
           (Values.DeleteRealtimeEndpointInput.make ~mLModelId ())
           (Some Values.DeleteRealtimeEndpointOutput.to_json)
           (Some Values.DeleteRealtimeEndpointOutput.error_to_json)])
let delete_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING EntityId"
       and resourceType =
         flag "resource-type" (required json_arg)
           ~doc:"JSON TaggableResourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_tags
           (Values.DeleteTagsInput.make
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ~resourceId
              ~resourceType:(Values.TaggableResourceType.of_json resourceType)
              ()) (Some Values.DeleteTagsOutput.to_json)
           (Some Values.DeleteTagsOutput.error_to_json)])
let describe_batch_predictions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterVariable =
         flag "filter-variable" (optional json_arg)
           ~doc:"JSON BatchPredictionFilterVariable"
       and eQ = flag "e-q" (optional string) ~doc:"STRING ComparatorValue"
       and gT = flag "g-t" (optional string) ~doc:"STRING ComparatorValue"
       and lT = flag "l-t" (optional string) ~doc:"STRING ComparatorValue"
       and gE = flag "g-e" (optional string) ~doc:"STRING ComparatorValue"
       and lE = flag "l-e" (optional string) ~doc:"STRING ComparatorValue"
       and nE = flag "n-e" (optional string) ~doc:"STRING ComparatorValue"
       and prefix =
         flag "prefix" (optional string) ~doc:"STRING ComparatorValue"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING StringType"
       and limit = flag "limit" (optional int) ~doc:"INT PageLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_batch_predictions
           (Values.DescribeBatchPredictionsInput.make
              ?filterVariable:(Option.map
                                 ~f:Values.BatchPredictionFilterVariable.of_json
                                 filterVariable) ?eQ ?gT ?lT ?gE ?lE ?nE
              ?prefix
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?nextToken ?limit ())
           (Some Values.DescribeBatchPredictionsOutput.to_json)
           (Some Values.DescribeBatchPredictionsOutput.error_to_json)])
let describe_data_sources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterVariable =
         flag "filter-variable" (optional json_arg)
           ~doc:"JSON DataSourceFilterVariable"
       and eQ = flag "e-q" (optional string) ~doc:"STRING ComparatorValue"
       and gT = flag "g-t" (optional string) ~doc:"STRING ComparatorValue"
       and lT = flag "l-t" (optional string) ~doc:"STRING ComparatorValue"
       and gE = flag "g-e" (optional string) ~doc:"STRING ComparatorValue"
       and lE = flag "l-e" (optional string) ~doc:"STRING ComparatorValue"
       and nE = flag "n-e" (optional string) ~doc:"STRING ComparatorValue"
       and prefix =
         flag "prefix" (optional string) ~doc:"STRING ComparatorValue"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING StringType"
       and limit = flag "limit" (optional int) ~doc:"INT PageLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_data_sources
           (Values.DescribeDataSourcesInput.make
              ?filterVariable:(Option.map
                                 ~f:Values.DataSourceFilterVariable.of_json
                                 filterVariable) ?eQ ?gT ?lT ?gE ?lE ?nE
              ?prefix
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?nextToken ?limit ())
           (Some Values.DescribeDataSourcesOutput.to_json)
           (Some Values.DescribeDataSourcesOutput.error_to_json)])
let describe_evaluations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterVariable =
         flag "filter-variable" (optional json_arg)
           ~doc:"JSON EvaluationFilterVariable"
       and eQ = flag "e-q" (optional string) ~doc:"STRING ComparatorValue"
       and gT = flag "g-t" (optional string) ~doc:"STRING ComparatorValue"
       and lT = flag "l-t" (optional string) ~doc:"STRING ComparatorValue"
       and gE = flag "g-e" (optional string) ~doc:"STRING ComparatorValue"
       and lE = flag "l-e" (optional string) ~doc:"STRING ComparatorValue"
       and nE = flag "n-e" (optional string) ~doc:"STRING ComparatorValue"
       and prefix =
         flag "prefix" (optional string) ~doc:"STRING ComparatorValue"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING StringType"
       and limit = flag "limit" (optional int) ~doc:"INT PageLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_evaluations
           (Values.DescribeEvaluationsInput.make
              ?filterVariable:(Option.map
                                 ~f:Values.EvaluationFilterVariable.of_json
                                 filterVariable) ?eQ ?gT ?lT ?gE ?lE ?nE
              ?prefix
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?nextToken ?limit ())
           (Some Values.DescribeEvaluationsOutput.to_json)
           (Some Values.DescribeEvaluationsOutput.error_to_json)])
let describe_m_l_models =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterVariable =
         flag "filter-variable" (optional json_arg)
           ~doc:"JSON MLModelFilterVariable"
       and eQ = flag "e-q" (optional string) ~doc:"STRING ComparatorValue"
       and gT = flag "g-t" (optional string) ~doc:"STRING ComparatorValue"
       and lT = flag "l-t" (optional string) ~doc:"STRING ComparatorValue"
       and gE = flag "g-e" (optional string) ~doc:"STRING ComparatorValue"
       and lE = flag "l-e" (optional string) ~doc:"STRING ComparatorValue"
       and nE = flag "n-e" (optional string) ~doc:"STRING ComparatorValue"
       and prefix =
         flag "prefix" (optional string) ~doc:"STRING ComparatorValue"
       and sortOrder =
         flag "sort-order" (optional json_arg) ~doc:"JSON SortOrder"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING StringType"
       and limit = flag "limit" (optional int) ~doc:"INT PageLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_m_l_models
           (Values.DescribeMLModelsInput.make
              ?filterVariable:(Option.map
                                 ~f:Values.MLModelFilterVariable.of_json
                                 filterVariable) ?eQ ?gT ?lT ?gE ?lE ?nE
              ?prefix
              ?sortOrder:(Option.map ~f:Values.SortOrder.of_json sortOrder)
              ?nextToken ?limit ())
           (Some Values.DescribeMLModelsOutput.to_json)
           (Some Values.DescribeMLModelsOutput.error_to_json)])
let describe_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING EntityId"
       and resourceType =
         flag "resource-type" (required json_arg)
           ~doc:"JSON TaggableResourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_tags
           (Values.DescribeTagsInput.make ~resourceId
              ~resourceType:(Values.TaggableResourceType.of_json resourceType)
              ()) (Some Values.DescribeTagsOutput.to_json)
           (Some Values.DescribeTagsOutput.error_to_json)])
let get_batch_prediction =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and batchPredictionId =
         flag "batch-prediction-id" (required string) ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_batch_prediction
           (Values.GetBatchPredictionInput.make ~batchPredictionId ())
           (Some Values.GetBatchPredictionOutput.to_json)
           (Some Values.GetBatchPredictionOutput.error_to_json)])
let get_data_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and verbose = flag "verbose" (optional bool) ~doc:"BOOL Verbose"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_source
           (Values.GetDataSourceInput.make ?verbose ~dataSourceId ())
           (Some Values.GetDataSourceOutput.to_json)
           (Some Values.GetDataSourceOutput.error_to_json)])
let get_evaluation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and evaluationId =
         flag "evaluation-id" (required string) ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_evaluation
           (Values.GetEvaluationInput.make ~evaluationId ())
           (Some Values.GetEvaluationOutput.to_json)
           (Some Values.GetEvaluationOutput.error_to_json)])
let get_m_l_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and verbose = flag "verbose" (optional bool) ~doc:"BOOL Verbose"
       and mLModelId =
         flag "m-l-model-id" (required string) ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_m_l_model
           (Values.GetMLModelInput.make ?verbose ~mLModelId ())
           (Some Values.GetMLModelOutput.to_json)
           (Some Values.GetMLModelOutput.error_to_json)])
let predict =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mLModelId =
         flag "m-l-model-id" (required string) ~doc:"STRING EntityId"
       and record = flag "record" (required json_arg) ~doc:"JSON Record"
       and predictEndpoint =
         flag "predict-endpoint" (required string) ~doc:"STRING VipURL" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.predict
           (Values.PredictInput.make ~mLModelId
              ~record:(Values.Record.of_json record) ~predictEndpoint ())
           (Some Values.PredictOutput.to_json)
           (Some Values.PredictOutput.error_to_json)])
let update_batch_prediction =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and batchPredictionId =
         flag "batch-prediction-id" (required string) ~doc:"STRING EntityId"
       and batchPredictionName =
         flag "batch-prediction-name" (required string)
           ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_batch_prediction
           (Values.UpdateBatchPredictionInput.make ~batchPredictionId
              ~batchPredictionName ())
           (Some Values.UpdateBatchPredictionOutput.to_json)
           (Some Values.UpdateBatchPredictionOutput.error_to_json)])
let update_data_source =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dataSourceId =
         flag "data-source-id" (required string) ~doc:"STRING EntityId"
       and dataSourceName =
         flag "data-source-name" (required string) ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_data_source
           (Values.UpdateDataSourceInput.make ~dataSourceId ~dataSourceName
              ()) (Some Values.UpdateDataSourceOutput.to_json)
           (Some Values.UpdateDataSourceOutput.error_to_json)])
let update_evaluation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and evaluationId =
         flag "evaluation-id" (required string) ~doc:"STRING EntityId"
       and evaluationName =
         flag "evaluation-name" (required string) ~doc:"STRING EntityName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_evaluation
           (Values.UpdateEvaluationInput.make ~evaluationId ~evaluationName
              ()) (Some Values.UpdateEvaluationOutput.to_json)
           (Some Values.UpdateEvaluationOutput.error_to_json)])
let update_m_l_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mLModelName =
         flag "m-l-model-name" (optional string) ~doc:"STRING EntityName"
       and scoreThreshold =
         flag "score-threshold" (optional float) ~doc:"FLOAT ScoreThreshold"
       and mLModelId =
         flag "m-l-model-id" (required string) ~doc:"STRING EntityId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_m_l_model
           (Values.UpdateMLModelInput.make ?mLModelName ?scoreThreshold
              ~mLModelId ()) (Some Values.UpdateMLModelOutput.to_json)
           (Some Values.UpdateMLModelOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("add-tags", add_tags);
    ("create-batch-prediction", create_batch_prediction);
    ("create-data-source-from-r-d-s", create_data_source_from_r_d_s);
    ("create-data-source-from-redshift", create_data_source_from_redshift);
    ("create-data-source-from-s3", create_data_source_from_s3);
    ("create-evaluation", create_evaluation);
    ("create-m-l-model", create_m_l_model);
    ("create-realtime-endpoint", create_realtime_endpoint);
    ("delete-batch-prediction", delete_batch_prediction);
    ("delete-data-source", delete_data_source);
    ("delete-evaluation", delete_evaluation);
    ("delete-m-l-model", delete_m_l_model);
    ("delete-realtime-endpoint", delete_realtime_endpoint);
    ("delete-tags", delete_tags);
    ("describe-batch-predictions", describe_batch_predictions);
    ("describe-data-sources", describe_data_sources);
    ("describe-evaluations", describe_evaluations);
    ("describe-m-l-models", describe_m_l_models);
    ("describe-tags", describe_tags);
    ("get-batch-prediction", get_batch_prediction);
    ("get-data-source", get_data_source);
    ("get-evaluation", get_evaluation);
    ("get-m-l-model", get_m_l_model);
    ("predict", predict);
    ("update-batch-prediction", update_batch_prediction);
    ("update-data-source", update_data_source);
    ("update-evaluation", update_evaluation);
    ("update-m-l-model", update_m_l_model)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_execute_statement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and returnConsumedCapacity =
         flag "return-consumed-capacity" (optional json_arg)
           ~doc:"JSON ReturnConsumedCapacity"
       and statements =
         flag "statements" (required json_arg)
           ~doc:"JSON PartiQLBatchRequest" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_execute_statement
           (Values.BatchExecuteStatementInput.make
              ?returnConsumedCapacity:(Option.map
                                         ~f:Values.ReturnConsumedCapacity.of_json
                                         returnConsumedCapacity)
              ~statements:(Values.PartiQLBatchRequest.of_json statements) ())
           (Some Values.BatchExecuteStatementOutput.to_json)
           (Some Values.BatchExecuteStatementOutput.error_to_json)])
let batch_get_item =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and returnConsumedCapacity =
         flag "return-consumed-capacity" (optional json_arg)
           ~doc:"JSON ReturnConsumedCapacity"
       and requestItems =
         flag "request-items" (required json_arg)
           ~doc:"JSON BatchGetRequestMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_item
           (Values.BatchGetItemInput.make
              ?returnConsumedCapacity:(Option.map
                                         ~f:Values.ReturnConsumedCapacity.of_json
                                         returnConsumedCapacity)
              ~requestItems:(Values.BatchGetRequestMap.of_json requestItems)
              ()) (Some Values.BatchGetItemOutput.to_json)
           (Some Values.BatchGetItemOutput.error_to_json)])
let batch_write_item =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and returnConsumedCapacity =
         flag "return-consumed-capacity" (optional json_arg)
           ~doc:"JSON ReturnConsumedCapacity"
       and returnItemCollectionMetrics =
         flag "return-item-collection-metrics" (optional json_arg)
           ~doc:"JSON ReturnItemCollectionMetrics"
       and requestItems =
         flag "request-items" (required json_arg)
           ~doc:"JSON BatchWriteItemRequestMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_write_item
           (Values.BatchWriteItemInput.make
              ?returnConsumedCapacity:(Option.map
                                         ~f:Values.ReturnConsumedCapacity.of_json
                                         returnConsumedCapacity)
              ?returnItemCollectionMetrics:(Option.map
                                              ~f:Values.ReturnItemCollectionMetrics.of_json
                                              returnItemCollectionMetrics)
              ~requestItems:(Values.BatchWriteItemRequestMap.of_json
                               requestItems) ())
           (Some Values.BatchWriteItemOutput.to_json)
           (Some Values.BatchWriteItemOutput.error_to_json)])
let create_backup =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableArn"
       and backupName =
         flag "backup-name" (required string) ~doc:"STRING BackupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_backup
           (Values.CreateBackupInput.make ~tableName ~backupName ())
           (Some Values.CreateBackupOutput.to_json)
           (Some Values.CreateBackupOutput.error_to_json)])
let create_global_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalTableName =
         flag "global-table-name" (required string) ~doc:"STRING TableName"
       and replicationGroup =
         flag "replication-group" (required json_arg) ~doc:"JSON ReplicaList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_global_table
           (Values.CreateGlobalTableInput.make ~globalTableName
              ~replicationGroup:(Values.ReplicaList.of_json replicationGroup)
              ()) (Some Values.CreateGlobalTableOutput.to_json)
           (Some Values.CreateGlobalTableOutput.error_to_json)])
let create_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributeDefinitions =
         flag "attribute-definitions" (optional json_arg)
           ~doc:"JSON AttributeDefinitions"
       and keySchema =
         flag "key-schema" (optional json_arg) ~doc:"JSON KeySchema"
       and localSecondaryIndexes =
         flag "local-secondary-indexes" (optional json_arg)
           ~doc:"JSON LocalSecondaryIndexList"
       and globalSecondaryIndexes =
         flag "global-secondary-indexes" (optional json_arg)
           ~doc:"JSON GlobalSecondaryIndexList"
       and billingMode =
         flag "billing-mode" (optional json_arg) ~doc:"JSON BillingMode"
       and provisionedThroughput =
         flag "provisioned-throughput" (optional json_arg)
           ~doc:"JSON ProvisionedThroughput"
       and streamSpecification =
         flag "stream-specification" (optional json_arg)
           ~doc:"JSON StreamSpecification"
       and sSESpecification =
         flag "s-s-e-specification" (optional json_arg)
           ~doc:"JSON SSESpecification"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and tableClass =
         flag "table-class" (optional json_arg) ~doc:"JSON TableClass"
       and deletionProtectionEnabled =
         flag "deletion-protection-enabled" (optional bool)
           ~doc:"BOOL DeletionProtectionEnabled"
       and warmThroughput =
         flag "warm-throughput" (optional json_arg)
           ~doc:"JSON WarmThroughput"
       and resourcePolicy =
         flag "resource-policy" (optional string)
           ~doc:"STRING ResourcePolicy"
       and onDemandThroughput =
         flag "on-demand-throughput" (optional json_arg)
           ~doc:"JSON OnDemandThroughput"
       and globalTableSourceArn =
         flag "global-table-source-arn" (optional string)
           ~doc:"STRING TableArn"
       and globalTableSettingsReplicationMode =
         flag "global-table-settings-replication-mode" (optional json_arg)
           ~doc:"JSON GlobalTableSettingsReplicationMode"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_table
           (Values.CreateTableInput.make
              ?attributeDefinitions:(Option.map
                                       ~f:Values.AttributeDefinitions.of_json
                                       attributeDefinitions)
              ?keySchema:(Option.map ~f:Values.KeySchema.of_json keySchema)
              ?localSecondaryIndexes:(Option.map
                                        ~f:Values.LocalSecondaryIndexList.of_json
                                        localSecondaryIndexes)
              ?globalSecondaryIndexes:(Option.map
                                         ~f:Values.GlobalSecondaryIndexList.of_json
                                         globalSecondaryIndexes)
              ?billingMode:(Option.map ~f:Values.BillingMode.of_json
                              billingMode)
              ?provisionedThroughput:(Option.map
                                        ~f:Values.ProvisionedThroughput.of_json
                                        provisionedThroughput)
              ?streamSpecification:(Option.map
                                      ~f:Values.StreamSpecification.of_json
                                      streamSpecification)
              ?sSESpecification:(Option.map
                                   ~f:Values.SSESpecification.of_json
                                   sSESpecification)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?tableClass:(Option.map ~f:Values.TableClass.of_json tableClass)
              ?deletionProtectionEnabled
              ?warmThroughput:(Option.map ~f:Values.WarmThroughput.of_json
                                 warmThroughput) ?resourcePolicy
              ?onDemandThroughput:(Option.map
                                     ~f:Values.OnDemandThroughput.of_json
                                     onDemandThroughput)
              ?globalTableSourceArn
              ?globalTableSettingsReplicationMode:(Option.map
                                                     ~f:Values.GlobalTableSettingsReplicationMode.of_json
                                                     globalTableSettingsReplicationMode)
              ~tableName ()) (Some Values.CreateTableOutput.to_json)
           (Some Values.CreateTableOutput.error_to_json)])
let delete_backup =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and backupArn =
         flag "backup-arn" (required string) ~doc:"STRING BackupArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_backup (Values.DeleteBackupInput.make ~backupArn ())
           (Some Values.DeleteBackupOutput.to_json)
           (Some Values.DeleteBackupOutput.error_to_json)])
let delete_item =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expected =
         flag "expected" (optional json_arg) ~doc:"JSON ExpectedAttributeMap"
       and conditionalOperator =
         flag "conditional-operator" (optional json_arg)
           ~doc:"JSON ConditionalOperator"
       and returnValues =
         flag "return-values" (optional json_arg) ~doc:"JSON ReturnValue"
       and returnConsumedCapacity =
         flag "return-consumed-capacity" (optional json_arg)
           ~doc:"JSON ReturnConsumedCapacity"
       and returnItemCollectionMetrics =
         flag "return-item-collection-metrics" (optional json_arg)
           ~doc:"JSON ReturnItemCollectionMetrics"
       and conditionExpression =
         flag "condition-expression" (optional string)
           ~doc:"STRING ConditionExpression"
       and expressionAttributeNames =
         flag "expression-attribute-names" (optional json_arg)
           ~doc:"JSON ExpressionAttributeNameMap"
       and expressionAttributeValues =
         flag "expression-attribute-values" (optional json_arg)
           ~doc:"JSON ExpressionAttributeValueMap"
       and returnValuesOnConditionCheckFailure =
         flag "return-values-on-condition-check-failure" (optional json_arg)
           ~doc:"JSON ReturnValuesOnConditionCheckFailure"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableArn"
       and key = flag "key" (required json_arg) ~doc:"JSON Key" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_item
           (Values.DeleteItemInput.make
              ?expected:(Option.map ~f:Values.ExpectedAttributeMap.of_json
                           expected)
              ?conditionalOperator:(Option.map
                                      ~f:Values.ConditionalOperator.of_json
                                      conditionalOperator)
              ?returnValues:(Option.map ~f:Values.ReturnValue.of_json
                               returnValues)
              ?returnConsumedCapacity:(Option.map
                                         ~f:Values.ReturnConsumedCapacity.of_json
                                         returnConsumedCapacity)
              ?returnItemCollectionMetrics:(Option.map
                                              ~f:Values.ReturnItemCollectionMetrics.of_json
                                              returnItemCollectionMetrics)
              ?conditionExpression
              ?expressionAttributeNames:(Option.map
                                           ~f:Values.ExpressionAttributeNameMap.of_json
                                           expressionAttributeNames)
              ?expressionAttributeValues:(Option.map
                                            ~f:Values.ExpressionAttributeValueMap.of_json
                                            expressionAttributeValues)
              ?returnValuesOnConditionCheckFailure:(Option.map
                                                      ~f:Values.ReturnValuesOnConditionCheckFailure.of_json
                                                      returnValuesOnConditionCheckFailure)
              ~tableName ~key:(Values.Key.of_json key) ())
           (Some Values.DeleteItemOutput.to_json)
           (Some Values.DeleteItemOutput.error_to_json)])
let delete_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedRevisionId =
         flag "expected-revision-id" (optional string)
           ~doc:"STRING PolicyRevisionId"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING ResourceArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_policy
           (Values.DeleteResourcePolicyInput.make ?expectedRevisionId
              ~resourceArn ())
           (Some Values.DeleteResourcePolicyOutput.to_json)
           (Some Values.DeleteResourcePolicyOutput.error_to_json)])
let delete_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_table (Values.DeleteTableInput.make ~tableName ())
           (Some Values.DeleteTableOutput.to_json)
           (Some Values.DeleteTableOutput.error_to_json)])
let describe_backup =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and backupArn =
         flag "backup-arn" (required string) ~doc:"STRING BackupArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_backup (Values.DescribeBackupInput.make ~backupArn ())
           (Some Values.DescribeBackupOutput.to_json)
           (Some Values.DescribeBackupOutput.error_to_json)])
let describe_continuous_backups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_continuous_backups
           (Values.DescribeContinuousBackupsInput.make ~tableName ())
           (Some Values.DescribeContinuousBackupsOutput.to_json)
           (Some Values.DescribeContinuousBackupsOutput.error_to_json)])
let describe_contributor_insights =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and indexName =
         flag "index-name" (optional string) ~doc:"STRING IndexName"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_contributor_insights
           (Values.DescribeContributorInsightsInput.make ?indexName
              ~tableName ())
           (Some Values.DescribeContributorInsightsOutput.to_json)
           (Some Values.DescribeContributorInsightsOutput.error_to_json)])
let describe_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_endpoints (Values.DescribeEndpointsRequest.make ())
           (Some Values.DescribeEndpointsResponse.to_json)
           (Some Values.DescribeEndpointsResponse.error_to_json)])
let describe_export =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and exportArn =
         flag "export-arn" (required string) ~doc:"STRING ExportArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_export (Values.DescribeExportInput.make ~exportArn ())
           (Some Values.DescribeExportOutput.to_json)
           (Some Values.DescribeExportOutput.error_to_json)])
let describe_global_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalTableName =
         flag "global-table-name" (required string) ~doc:"STRING TableName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_global_table
           (Values.DescribeGlobalTableInput.make ~globalTableName ())
           (Some Values.DescribeGlobalTableOutput.to_json)
           (Some Values.DescribeGlobalTableOutput.error_to_json)])
let describe_global_table_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalTableName =
         flag "global-table-name" (required string) ~doc:"STRING TableName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_global_table_settings
           (Values.DescribeGlobalTableSettingsInput.make ~globalTableName ())
           (Some Values.DescribeGlobalTableSettingsOutput.to_json)
           (Some Values.DescribeGlobalTableSettingsOutput.error_to_json)])
let describe_import =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and importArn =
         flag "import-arn" (required string) ~doc:"STRING ImportArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_import (Values.DescribeImportInput.make ~importArn ())
           (Some Values.DescribeImportOutput.to_json)
           (Some Values.DescribeImportOutput.error_to_json)])
let describe_kinesis_streaming_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_kinesis_streaming_destination
           (Values.DescribeKinesisStreamingDestinationInput.make ~tableName
              ())
           (Some Values.DescribeKinesisStreamingDestinationOutput.to_json)
           (Some
              Values.DescribeKinesisStreamingDestinationOutput.error_to_json)])
let describe_limits =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_limits (Values.DescribeLimitsInput.make ())
           (Some Values.DescribeLimitsOutput.to_json)
           (Some Values.DescribeLimitsOutput.error_to_json)])
let describe_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_table (Values.DescribeTableInput.make ~tableName ())
           (Some Values.DescribeTableOutput.to_json)
           (Some Values.DescribeTableOutput.error_to_json)])
let describe_table_replica_auto_scaling =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_table_replica_auto_scaling
           (Values.DescribeTableReplicaAutoScalingInput.make ~tableName ())
           (Some Values.DescribeTableReplicaAutoScalingOutput.to_json)
           (Some Values.DescribeTableReplicaAutoScalingOutput.error_to_json)])
let describe_time_to_live =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_time_to_live
           (Values.DescribeTimeToLiveInput.make ~tableName ())
           (Some Values.DescribeTimeToLiveOutput.to_json)
           (Some Values.DescribeTimeToLiveOutput.error_to_json)])
let disable_kinesis_streaming_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and enableKinesisStreamingConfiguration =
         flag "enable-kinesis-streaming-configuration" (optional json_arg)
           ~doc:"JSON EnableKinesisStreamingConfiguration"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableArn"
       and streamArn =
         flag "stream-arn" (required string) ~doc:"STRING StreamArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_kinesis_streaming_destination
           (Values.KinesisStreamingDestinationInput.make
              ?enableKinesisStreamingConfiguration:(Option.map
                                                      ~f:Values.EnableKinesisStreamingConfiguration.of_json
                                                      enableKinesisStreamingConfiguration)
              ~tableName ~streamArn ())
           (Some Values.KinesisStreamingDestinationOutput.to_json)
           (Some Values.KinesisStreamingDestinationOutput.error_to_json)])
let enable_kinesis_streaming_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and enableKinesisStreamingConfiguration =
         flag "enable-kinesis-streaming-configuration" (optional json_arg)
           ~doc:"JSON EnableKinesisStreamingConfiguration"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableArn"
       and streamArn =
         flag "stream-arn" (required string) ~doc:"STRING StreamArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_kinesis_streaming_destination
           (Values.KinesisStreamingDestinationInput.make
              ?enableKinesisStreamingConfiguration:(Option.map
                                                      ~f:Values.EnableKinesisStreamingConfiguration.of_json
                                                      enableKinesisStreamingConfiguration)
              ~tableName ~streamArn ())
           (Some Values.KinesisStreamingDestinationOutput.to_json)
           (Some Values.KinesisStreamingDestinationOutput.error_to_json)])
let execute_statement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and parameters =
         flag "parameters" (optional json_arg)
           ~doc:"JSON PreparedStatementParameters"
       and consistentRead =
         flag "consistent-read" (optional bool) ~doc:"BOOL ConsistentRead"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PartiQLNextToken"
       and returnConsumedCapacity =
         flag "return-consumed-capacity" (optional json_arg)
           ~doc:"JSON ReturnConsumedCapacity"
       and limit =
         flag "limit" (optional int) ~doc:"INT PositiveIntegerObject"
       and returnValuesOnConditionCheckFailure =
         flag "return-values-on-condition-check-failure" (optional json_arg)
           ~doc:"JSON ReturnValuesOnConditionCheckFailure"
       and statement =
         flag "statement" (required string) ~doc:"STRING PartiQLStatement" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.execute_statement
           (Values.ExecuteStatementInput.make
              ?parameters:(Option.map
                             ~f:Values.PreparedStatementParameters.of_json
                             parameters) ?consistentRead ?nextToken
              ?returnConsumedCapacity:(Option.map
                                         ~f:Values.ReturnConsumedCapacity.of_json
                                         returnConsumedCapacity) ?limit
              ?returnValuesOnConditionCheckFailure:(Option.map
                                                      ~f:Values.ReturnValuesOnConditionCheckFailure.of_json
                                                      returnValuesOnConditionCheckFailure)
              ~statement ()) (Some Values.ExecuteStatementOutput.to_json)
           (Some Values.ExecuteStatementOutput.error_to_json)])
let execute_transaction =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and returnConsumedCapacity =
         flag "return-consumed-capacity" (optional json_arg)
           ~doc:"JSON ReturnConsumedCapacity"
       and transactStatements =
         flag "transact-statements" (required json_arg)
           ~doc:"JSON ParameterizedStatements" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.execute_transaction
           (Values.ExecuteTransactionInput.make ?clientRequestToken
              ?returnConsumedCapacity:(Option.map
                                         ~f:Values.ReturnConsumedCapacity.of_json
                                         returnConsumedCapacity)
              ~transactStatements:(Values.ParameterizedStatements.of_json
                                     transactStatements) ())
           (Some Values.ExecuteTransactionOutput.to_json)
           (Some Values.ExecuteTransactionOutput.error_to_json)])
let export_table_to_point_in_time =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and exportTime =
         flag "export-time" (optional json_arg) ~doc:"JSON ExportTime"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and s3BucketOwner =
         flag "s3-bucket-owner" (optional string) ~doc:"STRING S3BucketOwner"
       and s3Prefix =
         flag "s3-prefix" (optional string) ~doc:"STRING S3Prefix"
       and s3SseAlgorithm =
         flag "s3-sse-algorithm" (optional json_arg)
           ~doc:"JSON S3SseAlgorithm"
       and s3SseKmsKeyId =
         flag "s3-sse-kms-key-id" (optional string)
           ~doc:"STRING S3SseKmsKeyId"
       and exportFormat =
         flag "export-format" (optional json_arg) ~doc:"JSON ExportFormat"
       and exportType =
         flag "export-type" (optional json_arg) ~doc:"JSON ExportType"
       and incrementalExportSpecification =
         flag "incremental-export-specification" (optional json_arg)
           ~doc:"JSON IncrementalExportSpecification"
       and tableArn =
         flag "table-arn" (required string) ~doc:"STRING TableArn"
       and s3Bucket =
         flag "s3-bucket" (required string) ~doc:"STRING S3Bucket" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.export_table_to_point_in_time
           (Values.ExportTableToPointInTimeInput.make
              ?exportTime:(Option.map ~f:Values.ExportTime.of_json exportTime)
              ?clientToken ?s3BucketOwner ?s3Prefix
              ?s3SseAlgorithm:(Option.map ~f:Values.S3SseAlgorithm.of_json
                                 s3SseAlgorithm) ?s3SseKmsKeyId
              ?exportFormat:(Option.map ~f:Values.ExportFormat.of_json
                               exportFormat)
              ?exportType:(Option.map ~f:Values.ExportType.of_json exportType)
              ?incrementalExportSpecification:(Option.map
                                                 ~f:Values.IncrementalExportSpecification.of_json
                                                 incrementalExportSpecification)
              ~tableArn ~s3Bucket ())
           (Some Values.ExportTableToPointInTimeOutput.to_json)
           (Some Values.ExportTableToPointInTimeOutput.error_to_json)])
let get_item =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributesToGet =
         flag "attributes-to-get" (optional json_arg)
           ~doc:"JSON AttributeNameList"
       and consistentRead =
         flag "consistent-read" (optional bool) ~doc:"BOOL ConsistentRead"
       and returnConsumedCapacity =
         flag "return-consumed-capacity" (optional json_arg)
           ~doc:"JSON ReturnConsumedCapacity"
       and projectionExpression =
         flag "projection-expression" (optional string)
           ~doc:"STRING ProjectionExpression"
       and expressionAttributeNames =
         flag "expression-attribute-names" (optional json_arg)
           ~doc:"JSON ExpressionAttributeNameMap"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableArn"
       and key = flag "key" (required json_arg) ~doc:"JSON Key" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_item
           (Values.GetItemInput.make
              ?attributesToGet:(Option.map
                                  ~f:Values.AttributeNameList.of_json
                                  attributesToGet) ?consistentRead
              ?returnConsumedCapacity:(Option.map
                                         ~f:Values.ReturnConsumedCapacity.of_json
                                         returnConsumedCapacity)
              ?projectionExpression
              ?expressionAttributeNames:(Option.map
                                           ~f:Values.ExpressionAttributeNameMap.of_json
                                           expressionAttributeNames)
              ~tableName ~key:(Values.Key.of_json key) ())
           (Some Values.GetItemOutput.to_json)
           (Some Values.GetItemOutput.error_to_json)])
let get_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING ResourceArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_policy
           (Values.GetResourcePolicyInput.make ~resourceArn ())
           (Some Values.GetResourcePolicyOutput.to_json)
           (Some Values.GetResourcePolicyOutput.error_to_json)])
let import_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and clientToken =
         flag "client-token" (optional string) ~doc:"STRING ClientToken"
       and inputFormatOptions =
         flag "input-format-options" (optional json_arg)
           ~doc:"JSON InputFormatOptions"
       and inputCompressionType =
         flag "input-compression-type" (optional json_arg)
           ~doc:"JSON InputCompressionType"
       and s3BucketSource =
         flag "s3-bucket-source" (required json_arg)
           ~doc:"JSON S3BucketSource"
       and inputFormat =
         flag "input-format" (required json_arg) ~doc:"JSON InputFormat"
       and tableCreationParameters =
         flag "table-creation-parameters" (required json_arg)
           ~doc:"JSON TableCreationParameters" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.import_table
           (Values.ImportTableInput.make ?clientToken
              ?inputFormatOptions:(Option.map
                                     ~f:Values.InputFormatOptions.of_json
                                     inputFormatOptions)
              ?inputCompressionType:(Option.map
                                       ~f:Values.InputCompressionType.of_json
                                       inputCompressionType)
              ~s3BucketSource:(Values.S3BucketSource.of_json s3BucketSource)
              ~inputFormat:(Values.InputFormat.of_json inputFormat)
              ~tableCreationParameters:(Values.TableCreationParameters.of_json
                                          tableCreationParameters) ())
           (Some Values.ImportTableOutput.to_json)
           (Some Values.ImportTableOutput.error_to_json)])
let list_backups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableName =
         flag "table-name" (optional string) ~doc:"STRING TableArn"
       and limit = flag "limit" (optional int) ~doc:"INT BackupsInputLimit"
       and timeRangeLowerBound =
         flag "time-range-lower-bound" (optional json_arg)
           ~doc:"JSON TimeRangeLowerBound"
       and timeRangeUpperBound =
         flag "time-range-upper-bound" (optional json_arg)
           ~doc:"JSON TimeRangeUpperBound"
       and exclusiveStartBackupArn =
         flag "exclusive-start-backup-arn" (optional string)
           ~doc:"STRING BackupArn"
       and backupType =
         flag "backup-type" (optional json_arg) ~doc:"JSON BackupTypeFilter" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_backups
           (Values.ListBackupsInput.make ?tableName ?limit
              ?timeRangeLowerBound:(Option.map
                                      ~f:Values.TimeRangeLowerBound.of_json
                                      timeRangeLowerBound)
              ?timeRangeUpperBound:(Option.map
                                      ~f:Values.TimeRangeUpperBound.of_json
                                      timeRangeUpperBound)
              ?exclusiveStartBackupArn
              ?backupType:(Option.map ~f:Values.BackupTypeFilter.of_json
                             backupType) ())
           (Some Values.ListBackupsOutput.to_json)
           (Some Values.ListBackupsOutput.error_to_json)])
let list_contributor_insights =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableName =
         flag "table-name" (optional string) ~doc:"STRING TableArn"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextTokenString"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListContributorInsightsLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_contributor_insights
           (Values.ListContributorInsightsInput.make ?tableName ?nextToken
              ?maxResults ())
           (Some Values.ListContributorInsightsOutput.to_json)
           (Some Values.ListContributorInsightsOutput.error_to_json)])
let list_exports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableArn =
         flag "table-arn" (optional string) ~doc:"STRING TableArn"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListExportsMaxLimit"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING ExportNextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_exports
           (Values.ListExportsInput.make ?tableArn ?maxResults ?nextToken ())
           (Some Values.ListExportsOutput.to_json)
           (Some Values.ListExportsOutput.error_to_json)])
let list_global_tables =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and exclusiveStartGlobalTableName =
         flag "exclusive-start-global-table-name" (optional string)
           ~doc:"STRING TableName"
       and limit =
         flag "limit" (optional int) ~doc:"INT PositiveIntegerObject"
       and regionName =
         flag "region-name" (optional string) ~doc:"STRING RegionName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_global_tables
           (Values.ListGlobalTablesInput.make ?exclusiveStartGlobalTableName
              ?limit ?regionName ())
           (Some Values.ListGlobalTablesOutput.to_json)
           (Some Values.ListGlobalTablesOutput.error_to_json)])
let list_imports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableArn =
         flag "table-arn" (optional string) ~doc:"STRING TableArn"
       and pageSize =
         flag "page-size" (optional int) ~doc:"INT ListImportsMaxLimit"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING ImportNextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_imports
           (Values.ListImportsInput.make ?tableArn ?pageSize ?nextToken ())
           (Some Values.ListImportsOutput.to_json)
           (Some Values.ListImportsOutput.error_to_json)])
let list_tables =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and exclusiveStartTableName =
         flag "exclusive-start-table-name" (optional string)
           ~doc:"STRING TableName"
       and limit =
         flag "limit" (optional int) ~doc:"INT ListTablesInputLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tables
           (Values.ListTablesInput.make ?exclusiveStartTableName ?limit ())
           (Some Values.ListTablesOutput.to_json)
           (Some Values.ListTablesOutput.error_to_json)])
let list_tags_of_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextTokenString"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING ResourceArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_of_resource
           (Values.ListTagsOfResourceInput.make ?nextToken ~resourceArn ())
           (Some Values.ListTagsOfResourceOutput.to_json)
           (Some Values.ListTagsOfResourceOutput.error_to_json)])
let put_item =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expected =
         flag "expected" (optional json_arg) ~doc:"JSON ExpectedAttributeMap"
       and returnValues =
         flag "return-values" (optional json_arg) ~doc:"JSON ReturnValue"
       and returnConsumedCapacity =
         flag "return-consumed-capacity" (optional json_arg)
           ~doc:"JSON ReturnConsumedCapacity"
       and returnItemCollectionMetrics =
         flag "return-item-collection-metrics" (optional json_arg)
           ~doc:"JSON ReturnItemCollectionMetrics"
       and conditionalOperator =
         flag "conditional-operator" (optional json_arg)
           ~doc:"JSON ConditionalOperator"
       and conditionExpression =
         flag "condition-expression" (optional string)
           ~doc:"STRING ConditionExpression"
       and expressionAttributeNames =
         flag "expression-attribute-names" (optional json_arg)
           ~doc:"JSON ExpressionAttributeNameMap"
       and expressionAttributeValues =
         flag "expression-attribute-values" (optional json_arg)
           ~doc:"JSON ExpressionAttributeValueMap"
       and returnValuesOnConditionCheckFailure =
         flag "return-values-on-condition-check-failure" (optional json_arg)
           ~doc:"JSON ReturnValuesOnConditionCheckFailure"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableArn"
       and item =
         flag "item" (required json_arg) ~doc:"JSON PutItemInputAttributeMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_item
           (Values.PutItemInput.make
              ?expected:(Option.map ~f:Values.ExpectedAttributeMap.of_json
                           expected)
              ?returnValues:(Option.map ~f:Values.ReturnValue.of_json
                               returnValues)
              ?returnConsumedCapacity:(Option.map
                                         ~f:Values.ReturnConsumedCapacity.of_json
                                         returnConsumedCapacity)
              ?returnItemCollectionMetrics:(Option.map
                                              ~f:Values.ReturnItemCollectionMetrics.of_json
                                              returnItemCollectionMetrics)
              ?conditionalOperator:(Option.map
                                      ~f:Values.ConditionalOperator.of_json
                                      conditionalOperator)
              ?conditionExpression
              ?expressionAttributeNames:(Option.map
                                           ~f:Values.ExpressionAttributeNameMap.of_json
                                           expressionAttributeNames)
              ?expressionAttributeValues:(Option.map
                                            ~f:Values.ExpressionAttributeValueMap.of_json
                                            expressionAttributeValues)
              ?returnValuesOnConditionCheckFailure:(Option.map
                                                      ~f:Values.ReturnValuesOnConditionCheckFailure.of_json
                                                      returnValuesOnConditionCheckFailure)
              ~tableName ~item:(Values.PutItemInputAttributeMap.of_json item)
              ()) (Some Values.PutItemOutput.to_json)
           (Some Values.PutItemOutput.error_to_json)])
let put_resource_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and expectedRevisionId =
         flag "expected-revision-id" (optional string)
           ~doc:"STRING PolicyRevisionId"
       and confirmRemoveSelfResourceAccess =
         flag "confirm-remove-self-resource-access" (optional bool)
           ~doc:"BOOL ConfirmRemoveSelfResourceAccess"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING ResourceArnString"
       and policy =
         flag "policy" (required string) ~doc:"STRING ResourcePolicy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_resource_policy
           (Values.PutResourcePolicyInput.make ?expectedRevisionId
              ?confirmRemoveSelfResourceAccess ~resourceArn ~policy ())
           (Some Values.PutResourcePolicyOutput.to_json)
           (Some Values.PutResourcePolicyOutput.error_to_json)])
let query =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and indexName =
         flag "index-name" (optional string) ~doc:"STRING IndexName"
       and select = flag "select" (optional json_arg) ~doc:"JSON Select"
       and attributesToGet =
         flag "attributes-to-get" (optional json_arg)
           ~doc:"JSON AttributeNameList"
       and limit =
         flag "limit" (optional int) ~doc:"INT PositiveIntegerObject"
       and consistentRead =
         flag "consistent-read" (optional bool) ~doc:"BOOL ConsistentRead"
       and keyConditions =
         flag "key-conditions" (optional json_arg) ~doc:"JSON KeyConditions"
       and queryFilter =
         flag "query-filter" (optional json_arg)
           ~doc:"JSON FilterConditionMap"
       and conditionalOperator =
         flag "conditional-operator" (optional json_arg)
           ~doc:"JSON ConditionalOperator"
       and scanIndexForward =
         flag "scan-index-forward" (optional bool) ~doc:"BOOL BooleanObject"
       and exclusiveStartKey =
         flag "exclusive-start-key" (optional json_arg) ~doc:"JSON Key"
       and returnConsumedCapacity =
         flag "return-consumed-capacity" (optional json_arg)
           ~doc:"JSON ReturnConsumedCapacity"
       and projectionExpression =
         flag "projection-expression" (optional string)
           ~doc:"STRING ProjectionExpression"
       and filterExpression =
         flag "filter-expression" (optional string)
           ~doc:"STRING ConditionExpression"
       and keyConditionExpression =
         flag "key-condition-expression" (optional string)
           ~doc:"STRING KeyExpression"
       and expressionAttributeNames =
         flag "expression-attribute-names" (optional json_arg)
           ~doc:"JSON ExpressionAttributeNameMap"
       and expressionAttributeValues =
         flag "expression-attribute-values" (optional json_arg)
           ~doc:"JSON ExpressionAttributeValueMap"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region Io.query
           (Values.QueryInput.make ?indexName
              ?select:(Option.map ~f:Values.Select.of_json select)
              ?attributesToGet:(Option.map
                                  ~f:Values.AttributeNameList.of_json
                                  attributesToGet) ?limit ?consistentRead
              ?keyConditions:(Option.map ~f:Values.KeyConditions.of_json
                                keyConditions)
              ?queryFilter:(Option.map ~f:Values.FilterConditionMap.of_json
                              queryFilter)
              ?conditionalOperator:(Option.map
                                      ~f:Values.ConditionalOperator.of_json
                                      conditionalOperator) ?scanIndexForward
              ?exclusiveStartKey:(Option.map ~f:Values.Key.of_json
                                    exclusiveStartKey)
              ?returnConsumedCapacity:(Option.map
                                         ~f:Values.ReturnConsumedCapacity.of_json
                                         returnConsumedCapacity)
              ?projectionExpression ?filterExpression ?keyConditionExpression
              ?expressionAttributeNames:(Option.map
                                           ~f:Values.ExpressionAttributeNameMap.of_json
                                           expressionAttributeNames)
              ?expressionAttributeValues:(Option.map
                                            ~f:Values.ExpressionAttributeValueMap.of_json
                                            expressionAttributeValues)
              ~tableName ()) (Some Values.QueryOutput.to_json)
           (Some Values.QueryOutput.error_to_json)])
let restore_table_from_backup =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and billingModeOverride =
         flag "billing-mode-override" (optional json_arg)
           ~doc:"JSON BillingMode"
       and globalSecondaryIndexOverride =
         flag "global-secondary-index-override" (optional json_arg)
           ~doc:"JSON GlobalSecondaryIndexList"
       and localSecondaryIndexOverride =
         flag "local-secondary-index-override" (optional json_arg)
           ~doc:"JSON LocalSecondaryIndexList"
       and provisionedThroughputOverride =
         flag "provisioned-throughput-override" (optional json_arg)
           ~doc:"JSON ProvisionedThroughput"
       and onDemandThroughputOverride =
         flag "on-demand-throughput-override" (optional json_arg)
           ~doc:"JSON OnDemandThroughput"
       and sSESpecificationOverride =
         flag "s-s-e-specification-override" (optional json_arg)
           ~doc:"JSON SSESpecification"
       and targetTableName =
         flag "target-table-name" (required string) ~doc:"STRING TableName"
       and backupArn =
         flag "backup-arn" (required string) ~doc:"STRING BackupArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_table_from_backup
           (Values.RestoreTableFromBackupInput.make
              ?billingModeOverride:(Option.map ~f:Values.BillingMode.of_json
                                      billingModeOverride)
              ?globalSecondaryIndexOverride:(Option.map
                                               ~f:Values.GlobalSecondaryIndexList.of_json
                                               globalSecondaryIndexOverride)
              ?localSecondaryIndexOverride:(Option.map
                                              ~f:Values.LocalSecondaryIndexList.of_json
                                              localSecondaryIndexOverride)
              ?provisionedThroughputOverride:(Option.map
                                                ~f:Values.ProvisionedThroughput.of_json
                                                provisionedThroughputOverride)
              ?onDemandThroughputOverride:(Option.map
                                             ~f:Values.OnDemandThroughput.of_json
                                             onDemandThroughputOverride)
              ?sSESpecificationOverride:(Option.map
                                           ~f:Values.SSESpecification.of_json
                                           sSESpecificationOverride)
              ~targetTableName ~backupArn ())
           (Some Values.RestoreTableFromBackupOutput.to_json)
           (Some Values.RestoreTableFromBackupOutput.error_to_json)])
let restore_table_to_point_in_time =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceTableArn =
         flag "source-table-arn" (optional string) ~doc:"STRING TableArn"
       and sourceTableName =
         flag "source-table-name" (optional string) ~doc:"STRING TableName"
       and useLatestRestorableTime =
         flag "use-latest-restorable-time" (optional bool)
           ~doc:"BOOL BooleanObject"
       and restoreDateTime =
         flag "restore-date-time" (optional json_arg) ~doc:"JSON Date"
       and billingModeOverride =
         flag "billing-mode-override" (optional json_arg)
           ~doc:"JSON BillingMode"
       and globalSecondaryIndexOverride =
         flag "global-secondary-index-override" (optional json_arg)
           ~doc:"JSON GlobalSecondaryIndexList"
       and localSecondaryIndexOverride =
         flag "local-secondary-index-override" (optional json_arg)
           ~doc:"JSON LocalSecondaryIndexList"
       and provisionedThroughputOverride =
         flag "provisioned-throughput-override" (optional json_arg)
           ~doc:"JSON ProvisionedThroughput"
       and onDemandThroughputOverride =
         flag "on-demand-throughput-override" (optional json_arg)
           ~doc:"JSON OnDemandThroughput"
       and sSESpecificationOverride =
         flag "s-s-e-specification-override" (optional json_arg)
           ~doc:"JSON SSESpecification"
       and targetTableName =
         flag "target-table-name" (required string) ~doc:"STRING TableName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_table_to_point_in_time
           (Values.RestoreTableToPointInTimeInput.make ?sourceTableArn
              ?sourceTableName ?useLatestRestorableTime
              ?restoreDateTime:(Option.map ~f:Values.Date.of_json
                                  restoreDateTime)
              ?billingModeOverride:(Option.map ~f:Values.BillingMode.of_json
                                      billingModeOverride)
              ?globalSecondaryIndexOverride:(Option.map
                                               ~f:Values.GlobalSecondaryIndexList.of_json
                                               globalSecondaryIndexOverride)
              ?localSecondaryIndexOverride:(Option.map
                                              ~f:Values.LocalSecondaryIndexList.of_json
                                              localSecondaryIndexOverride)
              ?provisionedThroughputOverride:(Option.map
                                                ~f:Values.ProvisionedThroughput.of_json
                                                provisionedThroughputOverride)
              ?onDemandThroughputOverride:(Option.map
                                             ~f:Values.OnDemandThroughput.of_json
                                             onDemandThroughputOverride)
              ?sSESpecificationOverride:(Option.map
                                           ~f:Values.SSESpecification.of_json
                                           sSESpecificationOverride)
              ~targetTableName ())
           (Some Values.RestoreTableToPointInTimeOutput.to_json)
           (Some Values.RestoreTableToPointInTimeOutput.error_to_json)])
let scan =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and indexName =
         flag "index-name" (optional string) ~doc:"STRING IndexName"
       and attributesToGet =
         flag "attributes-to-get" (optional json_arg)
           ~doc:"JSON AttributeNameList"
       and limit =
         flag "limit" (optional int) ~doc:"INT PositiveIntegerObject"
       and select = flag "select" (optional json_arg) ~doc:"JSON Select"
       and scanFilter =
         flag "scan-filter" (optional json_arg)
           ~doc:"JSON FilterConditionMap"
       and conditionalOperator =
         flag "conditional-operator" (optional json_arg)
           ~doc:"JSON ConditionalOperator"
       and exclusiveStartKey =
         flag "exclusive-start-key" (optional json_arg) ~doc:"JSON Key"
       and returnConsumedCapacity =
         flag "return-consumed-capacity" (optional json_arg)
           ~doc:"JSON ReturnConsumedCapacity"
       and totalSegments =
         flag "total-segments" (optional int) ~doc:"INT ScanTotalSegments"
       and segment = flag "segment" (optional int) ~doc:"INT ScanSegment"
       and projectionExpression =
         flag "projection-expression" (optional string)
           ~doc:"STRING ProjectionExpression"
       and filterExpression =
         flag "filter-expression" (optional string)
           ~doc:"STRING ConditionExpression"
       and expressionAttributeNames =
         flag "expression-attribute-names" (optional json_arg)
           ~doc:"JSON ExpressionAttributeNameMap"
       and expressionAttributeValues =
         flag "expression-attribute-values" (optional json_arg)
           ~doc:"JSON ExpressionAttributeValueMap"
       and consistentRead =
         flag "consistent-read" (optional bool) ~doc:"BOOL ConsistentRead"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region Io.scan
           (Values.ScanInput.make ?indexName
              ?attributesToGet:(Option.map
                                  ~f:Values.AttributeNameList.of_json
                                  attributesToGet) ?limit
              ?select:(Option.map ~f:Values.Select.of_json select)
              ?scanFilter:(Option.map ~f:Values.FilterConditionMap.of_json
                             scanFilter)
              ?conditionalOperator:(Option.map
                                      ~f:Values.ConditionalOperator.of_json
                                      conditionalOperator)
              ?exclusiveStartKey:(Option.map ~f:Values.Key.of_json
                                    exclusiveStartKey)
              ?returnConsumedCapacity:(Option.map
                                         ~f:Values.ReturnConsumedCapacity.of_json
                                         returnConsumedCapacity)
              ?totalSegments ?segment ?projectionExpression ?filterExpression
              ?expressionAttributeNames:(Option.map
                                           ~f:Values.ExpressionAttributeNameMap.of_json
                                           expressionAttributeNames)
              ?expressionAttributeValues:(Option.map
                                            ~f:Values.ExpressionAttributeValueMap.of_json
                                            expressionAttributeValues)
              ?consistentRead ~tableName ()) (Some Values.ScanOutput.to_json)
           (Some Values.ScanOutput.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING ResourceArnString"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceInput.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ()) None None])
let transact_get_items =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and returnConsumedCapacity =
         flag "return-consumed-capacity" (optional json_arg)
           ~doc:"JSON ReturnConsumedCapacity"
       and transactItems =
         flag "transact-items" (required json_arg)
           ~doc:"JSON TransactGetItemList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.transact_get_items
           (Values.TransactGetItemsInput.make
              ?returnConsumedCapacity:(Option.map
                                         ~f:Values.ReturnConsumedCapacity.of_json
                                         returnConsumedCapacity)
              ~transactItems:(Values.TransactGetItemList.of_json
                                transactItems) ())
           (Some Values.TransactGetItemsOutput.to_json)
           (Some Values.TransactGetItemsOutput.error_to_json)])
let transact_write_items =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and returnConsumedCapacity =
         flag "return-consumed-capacity" (optional json_arg)
           ~doc:"JSON ReturnConsumedCapacity"
       and returnItemCollectionMetrics =
         flag "return-item-collection-metrics" (optional json_arg)
           ~doc:"JSON ReturnItemCollectionMetrics"
       and clientRequestToken =
         flag "client-request-token" (optional string)
           ~doc:"STRING ClientRequestToken"
       and transactItems =
         flag "transact-items" (required json_arg)
           ~doc:"JSON TransactWriteItemList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.transact_write_items
           (Values.TransactWriteItemsInput.make
              ?returnConsumedCapacity:(Option.map
                                         ~f:Values.ReturnConsumedCapacity.of_json
                                         returnConsumedCapacity)
              ?returnItemCollectionMetrics:(Option.map
                                              ~f:Values.ReturnItemCollectionMetrics.of_json
                                              returnItemCollectionMetrics)
              ?clientRequestToken
              ~transactItems:(Values.TransactWriteItemList.of_json
                                transactItems) ())
           (Some Values.TransactWriteItemsOutput.to_json)
           (Some Values.TransactWriteItemsOutput.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING ResourceArnString"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceInput.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ()) None None])
let update_continuous_backups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableArn"
       and pointInTimeRecoverySpecification =
         flag "point-in-time-recovery-specification" (required json_arg)
           ~doc:"JSON PointInTimeRecoverySpecification" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_continuous_backups
           (Values.UpdateContinuousBackupsInput.make ~tableName
              ~pointInTimeRecoverySpecification:(Values.PointInTimeRecoverySpecification.of_json
                                                   pointInTimeRecoverySpecification)
              ()) (Some Values.UpdateContinuousBackupsOutput.to_json)
           (Some Values.UpdateContinuousBackupsOutput.error_to_json)])
let update_contributor_insights =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and indexName =
         flag "index-name" (optional string) ~doc:"STRING IndexName"
       and contributorInsightsMode =
         flag "contributor-insights-mode" (optional json_arg)
           ~doc:"JSON ContributorInsightsMode"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableArn"
       and contributorInsightsAction =
         flag "contributor-insights-action" (required json_arg)
           ~doc:"JSON ContributorInsightsAction" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_contributor_insights
           (Values.UpdateContributorInsightsInput.make ?indexName
              ?contributorInsightsMode:(Option.map
                                          ~f:Values.ContributorInsightsMode.of_json
                                          contributorInsightsMode) ~tableName
              ~contributorInsightsAction:(Values.ContributorInsightsAction.of_json
                                            contributorInsightsAction) ())
           (Some Values.UpdateContributorInsightsOutput.to_json)
           (Some Values.UpdateContributorInsightsOutput.error_to_json)])
let update_global_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalTableName =
         flag "global-table-name" (required string) ~doc:"STRING TableName"
       and replicaUpdates =
         flag "replica-updates" (required json_arg)
           ~doc:"JSON ReplicaUpdateList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_global_table
           (Values.UpdateGlobalTableInput.make ~globalTableName
              ~replicaUpdates:(Values.ReplicaUpdateList.of_json
                                 replicaUpdates) ())
           (Some Values.UpdateGlobalTableOutput.to_json)
           (Some Values.UpdateGlobalTableOutput.error_to_json)])
let update_global_table_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalTableBillingMode =
         flag "global-table-billing-mode" (optional json_arg)
           ~doc:"JSON BillingMode"
       and globalTableProvisionedWriteCapacityUnits =
         flag "global-table-provisioned-write-capacity-units"
           (optional json_arg) ~doc:"JSON PositiveLongObject"
       and globalTableProvisionedWriteCapacityAutoScalingSettingsUpdate =
         flag
           "global-table-provisioned-write-capacity-auto-scaling-settings-update"
           (optional json_arg) ~doc:"JSON AutoScalingSettingsUpdate"
       and globalTableGlobalSecondaryIndexSettingsUpdate =
         flag "global-table-global-secondary-index-settings-update"
           (optional json_arg)
           ~doc:"JSON GlobalTableGlobalSecondaryIndexSettingsUpdateList"
       and replicaSettingsUpdate =
         flag "replica-settings-update" (optional json_arg)
           ~doc:"JSON ReplicaSettingsUpdateList"
       and globalTableName =
         flag "global-table-name" (required string) ~doc:"STRING TableName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_global_table_settings
           (Values.UpdateGlobalTableSettingsInput.make
              ?globalTableBillingMode:(Option.map
                                         ~f:Values.BillingMode.of_json
                                         globalTableBillingMode)
              ?globalTableProvisionedWriteCapacityUnits:(Option.map
                                                           ~f:Values.PositiveLongObject.of_json
                                                           globalTableProvisionedWriteCapacityUnits)
              ?globalTableProvisionedWriteCapacityAutoScalingSettingsUpdate:(
              Option.map ~f:Values.AutoScalingSettingsUpdate.of_json
                globalTableProvisionedWriteCapacityAutoScalingSettingsUpdate)
              ?globalTableGlobalSecondaryIndexSettingsUpdate:(Option.map
                                                                ~f:Values.GlobalTableGlobalSecondaryIndexSettingsUpdateList.of_json
                                                                globalTableGlobalSecondaryIndexSettingsUpdate)
              ?replicaSettingsUpdate:(Option.map
                                        ~f:Values.ReplicaSettingsUpdateList.of_json
                                        replicaSettingsUpdate)
              ~globalTableName ())
           (Some Values.UpdateGlobalTableSettingsOutput.to_json)
           (Some Values.UpdateGlobalTableSettingsOutput.error_to_json)])
let update_item =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributeUpdates =
         flag "attribute-updates" (optional json_arg)
           ~doc:"JSON AttributeUpdates"
       and expected =
         flag "expected" (optional json_arg) ~doc:"JSON ExpectedAttributeMap"
       and conditionalOperator =
         flag "conditional-operator" (optional json_arg)
           ~doc:"JSON ConditionalOperator"
       and returnValues =
         flag "return-values" (optional json_arg) ~doc:"JSON ReturnValue"
       and returnConsumedCapacity =
         flag "return-consumed-capacity" (optional json_arg)
           ~doc:"JSON ReturnConsumedCapacity"
       and returnItemCollectionMetrics =
         flag "return-item-collection-metrics" (optional json_arg)
           ~doc:"JSON ReturnItemCollectionMetrics"
       and updateExpression =
         flag "update-expression" (optional string)
           ~doc:"STRING UpdateExpression"
       and conditionExpression =
         flag "condition-expression" (optional string)
           ~doc:"STRING ConditionExpression"
       and expressionAttributeNames =
         flag "expression-attribute-names" (optional json_arg)
           ~doc:"JSON ExpressionAttributeNameMap"
       and expressionAttributeValues =
         flag "expression-attribute-values" (optional json_arg)
           ~doc:"JSON ExpressionAttributeValueMap"
       and returnValuesOnConditionCheckFailure =
         flag "return-values-on-condition-check-failure" (optional json_arg)
           ~doc:"JSON ReturnValuesOnConditionCheckFailure"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableArn"
       and key = flag "key" (required json_arg) ~doc:"JSON Key" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_item
           (Values.UpdateItemInput.make
              ?attributeUpdates:(Option.map
                                   ~f:Values.AttributeUpdates.of_json
                                   attributeUpdates)
              ?expected:(Option.map ~f:Values.ExpectedAttributeMap.of_json
                           expected)
              ?conditionalOperator:(Option.map
                                      ~f:Values.ConditionalOperator.of_json
                                      conditionalOperator)
              ?returnValues:(Option.map ~f:Values.ReturnValue.of_json
                               returnValues)
              ?returnConsumedCapacity:(Option.map
                                         ~f:Values.ReturnConsumedCapacity.of_json
                                         returnConsumedCapacity)
              ?returnItemCollectionMetrics:(Option.map
                                              ~f:Values.ReturnItemCollectionMetrics.of_json
                                              returnItemCollectionMetrics)
              ?updateExpression ?conditionExpression
              ?expressionAttributeNames:(Option.map
                                           ~f:Values.ExpressionAttributeNameMap.of_json
                                           expressionAttributeNames)
              ?expressionAttributeValues:(Option.map
                                            ~f:Values.ExpressionAttributeValueMap.of_json
                                            expressionAttributeValues)
              ?returnValuesOnConditionCheckFailure:(Option.map
                                                      ~f:Values.ReturnValuesOnConditionCheckFailure.of_json
                                                      returnValuesOnConditionCheckFailure)
              ~tableName ~key:(Values.Key.of_json key) ())
           (Some Values.UpdateItemOutput.to_json)
           (Some Values.UpdateItemOutput.error_to_json)])
let update_kinesis_streaming_destination =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and updateKinesisStreamingConfiguration =
         flag "update-kinesis-streaming-configuration" (optional json_arg)
           ~doc:"JSON UpdateKinesisStreamingConfiguration"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableArn"
       and streamArn =
         flag "stream-arn" (required string) ~doc:"STRING StreamArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_kinesis_streaming_destination
           (Values.UpdateKinesisStreamingDestinationInput.make
              ?updateKinesisStreamingConfiguration:(Option.map
                                                      ~f:Values.UpdateKinesisStreamingConfiguration.of_json
                                                      updateKinesisStreamingConfiguration)
              ~tableName ~streamArn ())
           (Some Values.UpdateKinesisStreamingDestinationOutput.to_json)
           (Some Values.UpdateKinesisStreamingDestinationOutput.error_to_json)])
let update_table =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributeDefinitions =
         flag "attribute-definitions" (optional json_arg)
           ~doc:"JSON AttributeDefinitions"
       and billingMode =
         flag "billing-mode" (optional json_arg) ~doc:"JSON BillingMode"
       and provisionedThroughput =
         flag "provisioned-throughput" (optional json_arg)
           ~doc:"JSON ProvisionedThroughput"
       and globalSecondaryIndexUpdates =
         flag "global-secondary-index-updates" (optional json_arg)
           ~doc:"JSON GlobalSecondaryIndexUpdateList"
       and streamSpecification =
         flag "stream-specification" (optional json_arg)
           ~doc:"JSON StreamSpecification"
       and sSESpecification =
         flag "s-s-e-specification" (optional json_arg)
           ~doc:"JSON SSESpecification"
       and replicaUpdates =
         flag "replica-updates" (optional json_arg)
           ~doc:"JSON ReplicationGroupUpdateList"
       and tableClass =
         flag "table-class" (optional json_arg) ~doc:"JSON TableClass"
       and deletionProtectionEnabled =
         flag "deletion-protection-enabled" (optional bool)
           ~doc:"BOOL DeletionProtectionEnabled"
       and multiRegionConsistency =
         flag "multi-region-consistency" (optional json_arg)
           ~doc:"JSON MultiRegionConsistency"
       and globalTableWitnessUpdates =
         flag "global-table-witness-updates" (optional json_arg)
           ~doc:"JSON GlobalTableWitnessGroupUpdateList"
       and onDemandThroughput =
         flag "on-demand-throughput" (optional json_arg)
           ~doc:"JSON OnDemandThroughput"
       and warmThroughput =
         flag "warm-throughput" (optional json_arg)
           ~doc:"JSON WarmThroughput"
       and globalTableSettingsReplicationMode =
         flag "global-table-settings-replication-mode" (optional json_arg)
           ~doc:"JSON GlobalTableSettingsReplicationMode"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_table
           (Values.UpdateTableInput.make
              ?attributeDefinitions:(Option.map
                                       ~f:Values.AttributeDefinitions.of_json
                                       attributeDefinitions)
              ?billingMode:(Option.map ~f:Values.BillingMode.of_json
                              billingMode)
              ?provisionedThroughput:(Option.map
                                        ~f:Values.ProvisionedThroughput.of_json
                                        provisionedThroughput)
              ?globalSecondaryIndexUpdates:(Option.map
                                              ~f:Values.GlobalSecondaryIndexUpdateList.of_json
                                              globalSecondaryIndexUpdates)
              ?streamSpecification:(Option.map
                                      ~f:Values.StreamSpecification.of_json
                                      streamSpecification)
              ?sSESpecification:(Option.map
                                   ~f:Values.SSESpecification.of_json
                                   sSESpecification)
              ?replicaUpdates:(Option.map
                                 ~f:Values.ReplicationGroupUpdateList.of_json
                                 replicaUpdates)
              ?tableClass:(Option.map ~f:Values.TableClass.of_json tableClass)
              ?deletionProtectionEnabled
              ?multiRegionConsistency:(Option.map
                                         ~f:Values.MultiRegionConsistency.of_json
                                         multiRegionConsistency)
              ?globalTableWitnessUpdates:(Option.map
                                            ~f:Values.GlobalTableWitnessGroupUpdateList.of_json
                                            globalTableWitnessUpdates)
              ?onDemandThroughput:(Option.map
                                     ~f:Values.OnDemandThroughput.of_json
                                     onDemandThroughput)
              ?warmThroughput:(Option.map ~f:Values.WarmThroughput.of_json
                                 warmThroughput)
              ?globalTableSettingsReplicationMode:(Option.map
                                                     ~f:Values.GlobalTableSettingsReplicationMode.of_json
                                                     globalTableSettingsReplicationMode)
              ~tableName ()) (Some Values.UpdateTableOutput.to_json)
           (Some Values.UpdateTableOutput.error_to_json)])
let update_table_replica_auto_scaling =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and globalSecondaryIndexUpdates =
         flag "global-secondary-index-updates" (optional json_arg)
           ~doc:"JSON GlobalSecondaryIndexAutoScalingUpdateList"
       and provisionedWriteCapacityAutoScalingUpdate =
         flag "provisioned-write-capacity-auto-scaling-update"
           (optional json_arg) ~doc:"JSON AutoScalingSettingsUpdate"
       and replicaUpdates =
         flag "replica-updates" (optional json_arg)
           ~doc:"JSON ReplicaAutoScalingUpdateList"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_table_replica_auto_scaling
           (Values.UpdateTableReplicaAutoScalingInput.make
              ?globalSecondaryIndexUpdates:(Option.map
                                              ~f:Values.GlobalSecondaryIndexAutoScalingUpdateList.of_json
                                              globalSecondaryIndexUpdates)
              ?provisionedWriteCapacityAutoScalingUpdate:(Option.map
                                                            ~f:Values.AutoScalingSettingsUpdate.of_json
                                                            provisionedWriteCapacityAutoScalingUpdate)
              ?replicaUpdates:(Option.map
                                 ~f:Values.ReplicaAutoScalingUpdateList.of_json
                                 replicaUpdates) ~tableName ())
           (Some Values.UpdateTableReplicaAutoScalingOutput.to_json)
           (Some Values.UpdateTableReplicaAutoScalingOutput.error_to_json)])
let update_time_to_live =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tableName =
         flag "table-name" (required string) ~doc:"STRING TableArn"
       and timeToLiveSpecification =
         flag "time-to-live-specification" (required json_arg)
           ~doc:"JSON TimeToLiveSpecification" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_time_to_live
           (Values.UpdateTimeToLiveInput.make ~tableName
              ~timeToLiveSpecification:(Values.TimeToLiveSpecification.of_json
                                          timeToLiveSpecification) ())
           (Some Values.UpdateTimeToLiveOutput.to_json)
           (Some Values.UpdateTimeToLiveOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("batch-execute-statement", batch_execute_statement);
    ("batch-get-item", batch_get_item);
    ("batch-write-item", batch_write_item);
    ("create-backup", create_backup);
    ("create-global-table", create_global_table);
    ("create-table", create_table);
    ("delete-backup", delete_backup);
    ("delete-item", delete_item);
    ("delete-resource-policy", delete_resource_policy);
    ("delete-table", delete_table);
    ("describe-backup", describe_backup);
    ("describe-continuous-backups", describe_continuous_backups);
    ("describe-contributor-insights", describe_contributor_insights);
    ("describe-endpoints", describe_endpoints);
    ("describe-export", describe_export);
    ("describe-global-table", describe_global_table);
    ("describe-global-table-settings", describe_global_table_settings);
    ("describe-import", describe_import);
    ("describe-kinesis-streaming-destination",
      describe_kinesis_streaming_destination);
    ("describe-limits", describe_limits);
    ("describe-table", describe_table);
    ("describe-table-replica-auto-scaling",
      describe_table_replica_auto_scaling);
    ("describe-time-to-live", describe_time_to_live);
    ("disable-kinesis-streaming-destination",
      disable_kinesis_streaming_destination);
    ("enable-kinesis-streaming-destination",
      enable_kinesis_streaming_destination);
    ("execute-statement", execute_statement);
    ("execute-transaction", execute_transaction);
    ("export-table-to-point-in-time", export_table_to_point_in_time);
    ("get-item", get_item);
    ("get-resource-policy", get_resource_policy);
    ("import-table", import_table);
    ("list-backups", list_backups);
    ("list-contributor-insights", list_contributor_insights);
    ("list-exports", list_exports);
    ("list-global-tables", list_global_tables);
    ("list-imports", list_imports);
    ("list-tables", list_tables);
    ("list-tags-of-resource", list_tags_of_resource);
    ("put-item", put_item);
    ("put-resource-policy", put_resource_policy);
    ("query", query);
    ("restore-table-from-backup", restore_table_from_backup);
    ("restore-table-to-point-in-time", restore_table_to_point_in_time);
    ("scan", scan);
    ("tag-resource", tag_resource);
    ("transact-get-items", transact_get_items);
    ("transact-write-items", transact_write_items);
    ("untag-resource", untag_resource);
    ("update-continuous-backups", update_continuous_backups);
    ("update-contributor-insights", update_contributor_insights);
    ("update-global-table", update_global_table);
    ("update-global-table-settings", update_global_table_settings);
    ("update-item", update_item);
    ("update-kinesis-streaming-destination",
      update_kinesis_streaming_destination);
    ("update-table", update_table);
    ("update-table-replica-auto-scaling", update_table_replica_auto_scaling);
    ("update-time-to-live", update_time_to_live)]
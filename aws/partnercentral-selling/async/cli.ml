(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let accept_engagement_invitation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING EngagementInvitationArnOrIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.accept_engagement_invitation
           (Values.AcceptEngagementInvitationRequest.make ~catalog
              ~identifier ()) None None])
let assign_opportunity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING OpportunityIdentifier"
       and assignee =
         flag "assignee" (required json_arg) ~doc:"JSON AssigneeContact" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.assign_opportunity
           (Values.AssignOpportunityRequest.make ~catalog ~identifier
              ~assignee:(Values.AssigneeContact.of_json assignee) ()) None
           None])
let associate_opportunity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and opportunityIdentifier =
         flag "opportunity-identifier" (required string)
           ~doc:"STRING OpportunityIdentifier"
       and relatedEntityType =
         flag "related-entity-type" (required json_arg)
           ~doc:"JSON RelatedEntityType"
       and relatedEntityIdentifier =
         flag "related-entity-identifier" (required string)
           ~doc:"STRING AssociateOpportunityRequestRelatedEntityIdentifierString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_opportunity
           (Values.AssociateOpportunityRequest.make ~catalog
              ~opportunityIdentifier
              ~relatedEntityType:(Values.RelatedEntityType.of_json
                                    relatedEntityType)
              ~relatedEntityIdentifier ()) None None])
let create_engagement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contexts =
         flag "contexts" (optional json_arg) ~doc:"JSON EngagementContexts"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken"
       and title =
         flag "title" (required string) ~doc:"STRING EngagementTitle"
       and description =
         flag "description" (required string)
           ~doc:"STRING EngagementDescription" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_engagement
           (Values.CreateEngagementRequest.make
              ?contexts:(Option.map ~f:Values.EngagementContexts.of_json
                           contexts) ~catalog ~clientToken ~title
              ~description ()) (Some Values.CreateEngagementResponse.to_json)
           (Some Values.CreateEngagementResponse.error_to_json)])
let create_engagement_context =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and engagementIdentifier =
         flag "engagement-identifier" (required string)
           ~doc:"STRING EngagementArnOrIdentifier"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON EngagementContextType"
       and payload =
         flag "payload" (required json_arg)
           ~doc:"JSON EngagementContextPayload" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_engagement_context
           (Values.CreateEngagementContextRequest.make ~catalog
              ~engagementIdentifier ~clientToken
              ~type_:(Values.EngagementContextType.of_json type_)
              ~payload:(Values.EngagementContextPayload.of_json payload) ())
           (Some Values.CreateEngagementContextResponse.to_json)
           (Some Values.CreateEngagementContextResponse.error_to_json)])
let create_engagement_invitation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken"
       and engagementIdentifier =
         flag "engagement-identifier" (required string)
           ~doc:"STRING EngagementIdentifier"
       and invitation =
         flag "invitation" (required json_arg) ~doc:"JSON Invitation" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_engagement_invitation
           (Values.CreateEngagementInvitationRequest.make ~catalog
              ~clientToken ~engagementIdentifier
              ~invitation:(Values.Invitation.of_json invitation) ())
           (Some Values.CreateEngagementInvitationResponse.to_json)
           (Some Values.CreateEngagementInvitationResponse.error_to_json)])
let create_opportunity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and primaryNeedsFromAws =
         flag "primary-needs-from-aws" (optional json_arg)
           ~doc:"JSON PrimaryNeedsFromAws"
       and nationalSecurity =
         flag "national-security" (optional json_arg)
           ~doc:"JSON NationalSecurity"
       and partnerOpportunityIdentifier =
         flag "partner-opportunity-identifier" (optional string)
           ~doc:"STRING CreateOpportunityRequestPartnerOpportunityIdentifierString"
       and customer =
         flag "customer" (optional json_arg) ~doc:"JSON Customer"
       and project = flag "project" (optional json_arg) ~doc:"JSON Project"
       and opportunityType =
         flag "opportunity-type" (optional json_arg)
           ~doc:"JSON OpportunityType"
       and marketing =
         flag "marketing" (optional json_arg) ~doc:"JSON Marketing"
       and softwareRevenue =
         flag "software-revenue" (optional json_arg)
           ~doc:"JSON SoftwareRevenue"
       and lifeCycle =
         flag "life-cycle" (optional json_arg) ~doc:"JSON LifeCycle"
       and origin =
         flag "origin" (optional json_arg) ~doc:"JSON OpportunityOrigin"
       and opportunityTeam =
         flag "opportunity-team" (optional json_arg)
           ~doc:"JSON PartnerOpportunityTeamMembersList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_opportunity
           (Values.CreateOpportunityRequest.make
              ?primaryNeedsFromAws:(Option.map
                                      ~f:Values.PrimaryNeedsFromAws.of_json
                                      primaryNeedsFromAws)
              ?nationalSecurity:(Option.map
                                   ~f:Values.NationalSecurity.of_json
                                   nationalSecurity)
              ?partnerOpportunityIdentifier
              ?customer:(Option.map ~f:Values.Customer.of_json customer)
              ?project:(Option.map ~f:Values.Project.of_json project)
              ?opportunityType:(Option.map ~f:Values.OpportunityType.of_json
                                  opportunityType)
              ?marketing:(Option.map ~f:Values.Marketing.of_json marketing)
              ?softwareRevenue:(Option.map ~f:Values.SoftwareRevenue.of_json
                                  softwareRevenue)
              ?lifeCycle:(Option.map ~f:Values.LifeCycle.of_json lifeCycle)
              ?origin:(Option.map ~f:Values.OpportunityOrigin.of_json origin)
              ?opportunityTeam:(Option.map
                                  ~f:Values.PartnerOpportunityTeamMembersList.of_json
                                  opportunityTeam)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~catalog
              ~clientToken ())
           (Some Values.CreateOpportunityResponse.to_json)
           (Some Values.CreateOpportunityResponse.error_to_json)])
let create_resource_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and engagementIdentifier =
         flag "engagement-identifier" (required string)
           ~doc:"STRING EngagementIdentifier"
       and resourceType =
         flag "resource-type" (required json_arg) ~doc:"JSON ResourceType"
       and resourceIdentifier =
         flag "resource-identifier" (required string)
           ~doc:"STRING ResourceIdentifier"
       and resourceSnapshotTemplateIdentifier =
         flag "resource-snapshot-template-identifier" (required string)
           ~doc:"STRING ResourceTemplateName"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_resource_snapshot
           (Values.CreateResourceSnapshotRequest.make ~catalog
              ~engagementIdentifier
              ~resourceType:(Values.ResourceType.of_json resourceType)
              ~resourceIdentifier ~resourceSnapshotTemplateIdentifier
              ~clientToken ())
           (Some Values.CreateResourceSnapshotResponse.to_json)
           (Some Values.CreateResourceSnapshotResponse.error_to_json)])
let create_resource_snapshot_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken"
       and engagementIdentifier =
         flag "engagement-identifier" (required string)
           ~doc:"STRING EngagementIdentifier"
       and resourceType =
         flag "resource-type" (required json_arg) ~doc:"JSON ResourceType"
       and resourceIdentifier =
         flag "resource-identifier" (required string)
           ~doc:"STRING ResourceIdentifier"
       and resourceSnapshotTemplateIdentifier =
         flag "resource-snapshot-template-identifier" (required string)
           ~doc:"STRING ResourceTemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_resource_snapshot_job
           (Values.CreateResourceSnapshotJobRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~catalog
              ~clientToken ~engagementIdentifier
              ~resourceType:(Values.ResourceType.of_json resourceType)
              ~resourceIdentifier ~resourceSnapshotTemplateIdentifier ())
           (Some Values.CreateResourceSnapshotJobResponse.to_json)
           (Some Values.CreateResourceSnapshotJobResponse.error_to_json)])
let delete_resource_snapshot_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and resourceSnapshotJobIdentifier =
         flag "resource-snapshot-job-identifier" (required string)
           ~doc:"STRING ResourceSnapshotJobIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_resource_snapshot_job
           (Values.DeleteResourceSnapshotJobRequest.make ~catalog
              ~resourceSnapshotJobIdentifier ()) None None])
let disassociate_opportunity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and opportunityIdentifier =
         flag "opportunity-identifier" (required string)
           ~doc:"STRING OpportunityIdentifier"
       and relatedEntityType =
         flag "related-entity-type" (required json_arg)
           ~doc:"JSON RelatedEntityType"
       and relatedEntityIdentifier =
         flag "related-entity-identifier" (required string)
           ~doc:"STRING DisassociateOpportunityRequestRelatedEntityIdentifierString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_opportunity
           (Values.DisassociateOpportunityRequest.make ~catalog
              ~opportunityIdentifier
              ~relatedEntityType:(Values.RelatedEntityType.of_json
                                    relatedEntityType)
              ~relatedEntityIdentifier ()) None None])
let get_aws_opportunity_summary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and relatedOpportunityIdentifier =
         flag "related-opportunity-identifier" (required string)
           ~doc:"STRING OpportunityIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_aws_opportunity_summary
           (Values.GetAwsOpportunitySummaryRequest.make ~catalog
              ~relatedOpportunityIdentifier ())
           (Some Values.GetAwsOpportunitySummaryResponse.to_json)
           (Some Values.GetAwsOpportunitySummaryResponse.error_to_json)])
let get_engagement =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING EngagementArnOrIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_engagement
           (Values.GetEngagementRequest.make ~catalog ~identifier ())
           (Some Values.GetEngagementResponse.to_json)
           (Some Values.GetEngagementResponse.error_to_json)])
let get_engagement_invitation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING EngagementInvitationArnOrIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_engagement_invitation
           (Values.GetEngagementInvitationRequest.make ~catalog ~identifier
              ()) (Some Values.GetEngagementInvitationResponse.to_json)
           (Some Values.GetEngagementInvitationResponse.error_to_json)])
let get_opportunity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING OpportunityIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_opportunity
           (Values.GetOpportunityRequest.make ~catalog ~identifier ())
           (Some Values.GetOpportunityResponse.to_json)
           (Some Values.GetOpportunityResponse.error_to_json)])
let get_resource_snapshot =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and revision =
         flag "revision" (optional int) ~doc:"INT ResourceSnapshotRevision"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and engagementIdentifier =
         flag "engagement-identifier" (required string)
           ~doc:"STRING EngagementIdentifier"
       and resourceType =
         flag "resource-type" (required json_arg) ~doc:"JSON ResourceType"
       and resourceIdentifier =
         flag "resource-identifier" (required string)
           ~doc:"STRING ResourceIdentifier"
       and resourceSnapshotTemplateIdentifier =
         flag "resource-snapshot-template-identifier" (required string)
           ~doc:"STRING ResourceTemplateName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_snapshot
           (Values.GetResourceSnapshotRequest.make ?revision ~catalog
              ~engagementIdentifier
              ~resourceType:(Values.ResourceType.of_json resourceType)
              ~resourceIdentifier ~resourceSnapshotTemplateIdentifier ())
           (Some Values.GetResourceSnapshotResponse.to_json)
           (Some Values.GetResourceSnapshotResponse.error_to_json)])
let get_resource_snapshot_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and resourceSnapshotJobIdentifier =
         flag "resource-snapshot-job-identifier" (required string)
           ~doc:"STRING ResourceSnapshotJobIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_snapshot_job
           (Values.GetResourceSnapshotJobRequest.make ~catalog
              ~resourceSnapshotJobIdentifier ())
           (Some Values.GetResourceSnapshotJobResponse.to_json)
           (Some Values.GetResourceSnapshotJobResponse.error_to_json)])
let get_selling_system_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_selling_system_settings
           (Values.GetSellingSystemSettingsRequest.make ~catalog ())
           (Some Values.GetSellingSystemSettingsResponse.to_json)
           (Some Values.GetSellingSystemSettingsResponse.error_to_json)])
let list_engagement_by_accepting_invitation_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListEngagementByAcceptingInvitationTasksRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListEngagementByAcceptingInvitationTasksRequestNextTokenString"
       and sort =
         flag "sort" (optional json_arg) ~doc:"JSON ListTasksSortBase"
       and taskStatus =
         flag "task-status" (optional json_arg) ~doc:"JSON TaskStatuses"
       and opportunityIdentifier =
         flag "opportunity-identifier" (optional json_arg)
           ~doc:"JSON OpportunityIdentifiers"
       and engagementInvitationIdentifier =
         flag "engagement-invitation-identifier" (optional json_arg)
           ~doc:"JSON EngagementInvitationIdentifiers"
       and taskIdentifier =
         flag "task-identifier" (optional json_arg)
           ~doc:"JSON TaskIdentifiers"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_engagement_by_accepting_invitation_tasks
           (Values.ListEngagementByAcceptingInvitationTasksRequest.make
              ?maxResults ?nextToken
              ?sort:(Option.map ~f:Values.ListTasksSortBase.of_json sort)
              ?taskStatus:(Option.map ~f:Values.TaskStatuses.of_json
                             taskStatus)
              ?opportunityIdentifier:(Option.map
                                        ~f:Values.OpportunityIdentifiers.of_json
                                        opportunityIdentifier)
              ?engagementInvitationIdentifier:(Option.map
                                                 ~f:Values.EngagementInvitationIdentifiers.of_json
                                                 engagementInvitationIdentifier)
              ?taskIdentifier:(Option.map ~f:Values.TaskIdentifiers.of_json
                                 taskIdentifier) ~catalog ())
           (Some
              Values.ListEngagementByAcceptingInvitationTasksResponse.to_json)
           (Some
              Values.ListEngagementByAcceptingInvitationTasksResponse.error_to_json)])
let list_engagement_from_opportunity_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListEngagementFromOpportunityTasksRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListEngagementFromOpportunityTasksRequestNextTokenString"
       and sort =
         flag "sort" (optional json_arg) ~doc:"JSON ListTasksSortBase"
       and taskStatus =
         flag "task-status" (optional json_arg) ~doc:"JSON TaskStatuses"
       and taskIdentifier =
         flag "task-identifier" (optional json_arg)
           ~doc:"JSON TaskIdentifiers"
       and opportunityIdentifier =
         flag "opportunity-identifier" (optional json_arg)
           ~doc:"JSON OpportunityIdentifiers"
       and engagementIdentifier =
         flag "engagement-identifier" (optional json_arg)
           ~doc:"JSON EngagementIdentifiers"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_engagement_from_opportunity_tasks
           (Values.ListEngagementFromOpportunityTasksRequest.make ?maxResults
              ?nextToken
              ?sort:(Option.map ~f:Values.ListTasksSortBase.of_json sort)
              ?taskStatus:(Option.map ~f:Values.TaskStatuses.of_json
                             taskStatus)
              ?taskIdentifier:(Option.map ~f:Values.TaskIdentifiers.of_json
                                 taskIdentifier)
              ?opportunityIdentifier:(Option.map
                                        ~f:Values.OpportunityIdentifiers.of_json
                                        opportunityIdentifier)
              ?engagementIdentifier:(Option.map
                                       ~f:Values.EngagementIdentifiers.of_json
                                       engagementIdentifier) ~catalog ())
           (Some Values.ListEngagementFromOpportunityTasksResponse.to_json)
           (Some
              Values.ListEngagementFromOpportunityTasksResponse.error_to_json)])
let list_engagement_invitations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and sort =
         flag "sort" (optional json_arg)
           ~doc:"JSON OpportunityEngagementInvitationSort"
       and payloadType =
         flag "payload-type" (optional json_arg)
           ~doc:"JSON EngagementInvitationsPayloadType"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON InvitationStatusList"
       and engagementIdentifier =
         flag "engagement-identifier" (optional json_arg)
           ~doc:"JSON EngagementIdentifiers"
       and senderAwsAccountId =
         flag "sender-aws-account-id" (optional json_arg)
           ~doc:"JSON AwsAccountIdOrAliasList"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and participantType =
         flag "participant-type" (required json_arg)
           ~doc:"JSON ParticipantType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_engagement_invitations
           (Values.ListEngagementInvitationsRequest.make ?maxResults
              ?nextToken
              ?sort:(Option.map
                       ~f:Values.OpportunityEngagementInvitationSort.of_json
                       sort)
              ?payloadType:(Option.map
                              ~f:Values.EngagementInvitationsPayloadType.of_json
                              payloadType)
              ?status:(Option.map ~f:Values.InvitationStatusList.of_json
                         status)
              ?engagementIdentifier:(Option.map
                                       ~f:Values.EngagementIdentifiers.of_json
                                       engagementIdentifier)
              ?senderAwsAccountId:(Option.map
                                     ~f:Values.AwsAccountIdOrAliasList.of_json
                                     senderAwsAccountId) ~catalog
              ~participantType:(Values.ParticipantType.of_json
                                  participantType) ())
           (Some Values.ListEngagementInvitationsResponse.to_json)
           (Some Values.ListEngagementInvitationsResponse.error_to_json)])
let list_engagement_members =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MemberPageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING EngagementArnOrIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_engagement_members
           (Values.ListEngagementMembersRequest.make ?maxResults ?nextToken
              ~catalog ~identifier ())
           (Some Values.ListEngagementMembersResponse.to_json)
           (Some Values.ListEngagementMembersResponse.error_to_json)])
let list_engagement_resource_associations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListEngagementResourceAssociationsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and engagementIdentifier =
         flag "engagement-identifier" (optional string)
           ~doc:"STRING EngagementIdentifier"
       and resourceType =
         flag "resource-type" (optional json_arg) ~doc:"JSON ResourceType"
       and resourceIdentifier =
         flag "resource-identifier" (optional string)
           ~doc:"STRING ResourceIdentifier"
       and createdBy =
         flag "created-by" (optional string) ~doc:"STRING AwsAccount"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_engagement_resource_associations
           (Values.ListEngagementResourceAssociationsRequest.make ?maxResults
              ?nextToken ?engagementIdentifier
              ?resourceType:(Option.map ~f:Values.ResourceType.of_json
                               resourceType) ?resourceIdentifier ?createdBy
              ~catalog ())
           (Some Values.ListEngagementResourceAssociationsResponse.to_json)
           (Some
              Values.ListEngagementResourceAssociationsResponse.error_to_json)])
let list_engagements =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and createdBy =
         flag "created-by" (optional json_arg) ~doc:"JSON AwsAccountList"
       and excludeCreatedBy =
         flag "exclude-created-by" (optional json_arg)
           ~doc:"JSON AwsAccountList"
       and contextTypes =
         flag "context-types" (optional json_arg)
           ~doc:"JSON EngagementContextTypeList"
       and excludeContextTypes =
         flag "exclude-context-types" (optional json_arg)
           ~doc:"JSON EngagementContextTypeList"
       and sort = flag "sort" (optional json_arg) ~doc:"JSON EngagementSort"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT EngagementPageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and engagementIdentifier =
         flag "engagement-identifier" (optional json_arg)
           ~doc:"JSON EngagementIdentifiers"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_engagements
           (Values.ListEngagementsRequest.make
              ?createdBy:(Option.map ~f:Values.AwsAccountList.of_json
                            createdBy)
              ?excludeCreatedBy:(Option.map ~f:Values.AwsAccountList.of_json
                                   excludeCreatedBy)
              ?contextTypes:(Option.map
                               ~f:Values.EngagementContextTypeList.of_json
                               contextTypes)
              ?excludeContextTypes:(Option.map
                                      ~f:Values.EngagementContextTypeList.of_json
                                      excludeContextTypes)
              ?sort:(Option.map ~f:Values.EngagementSort.of_json sort)
              ?maxResults ?nextToken
              ?engagementIdentifier:(Option.map
                                       ~f:Values.EngagementIdentifiers.of_json
                                       engagementIdentifier) ~catalog ())
           (Some Values.ListEngagementsResponse.to_json)
           (Some Values.ListEngagementsResponse.error_to_json)])
let list_opportunities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and sort = flag "sort" (optional json_arg) ~doc:"JSON OpportunitySort"
       and lastModifiedDate =
         flag "last-modified-date" (optional json_arg)
           ~doc:"JSON LastModifiedDate"
       and identifier =
         flag "identifier" (optional json_arg)
           ~doc:"JSON ListOpportunitiesRequestIdentifierList"
       and lifeCycleStage =
         flag "life-cycle-stage" (optional json_arg)
           ~doc:"JSON ListOpportunitiesRequestLifeCycleStageList"
       and lifeCycleReviewStatus =
         flag "life-cycle-review-status" (optional json_arg)
           ~doc:"JSON ListOpportunitiesRequestLifeCycleReviewStatusList"
       and customerCompanyName =
         flag "customer-company-name" (optional json_arg)
           ~doc:"JSON ListOpportunitiesRequestCustomerCompanyNameList"
       and createdDate =
         flag "created-date" (optional json_arg)
           ~doc:"JSON CreatedDateFilter"
       and targetCloseDate =
         flag "target-close-date" (optional json_arg)
           ~doc:"JSON TargetCloseDateFilter"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_opportunities
           (Values.ListOpportunitiesRequest.make ?maxResults ?nextToken
              ?sort:(Option.map ~f:Values.OpportunitySort.of_json sort)
              ?lastModifiedDate:(Option.map
                                   ~f:Values.LastModifiedDate.of_json
                                   lastModifiedDate)
              ?identifier:(Option.map
                             ~f:Values.ListOpportunitiesRequestIdentifierList.of_json
                             identifier)
              ?lifeCycleStage:(Option.map
                                 ~f:Values.ListOpportunitiesRequestLifeCycleStageList.of_json
                                 lifeCycleStage)
              ?lifeCycleReviewStatus:(Option.map
                                        ~f:Values.ListOpportunitiesRequestLifeCycleReviewStatusList.of_json
                                        lifeCycleReviewStatus)
              ?customerCompanyName:(Option.map
                                      ~f:Values.ListOpportunitiesRequestCustomerCompanyNameList.of_json
                                      customerCompanyName)
              ?createdDate:(Option.map ~f:Values.CreatedDateFilter.of_json
                              createdDate)
              ?targetCloseDate:(Option.map
                                  ~f:Values.TargetCloseDateFilter.of_json
                                  targetCloseDate) ~catalog ())
           (Some Values.ListOpportunitiesResponse.to_json)
           (Some Values.ListOpportunitiesResponse.error_to_json)])
let list_opportunity_from_engagement_tasks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListOpportunityFromEngagementTasksRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING ListOpportunityFromEngagementTasksRequestNextTokenString"
       and sort =
         flag "sort" (optional json_arg) ~doc:"JSON ListTasksSortBase"
       and taskStatus =
         flag "task-status" (optional json_arg) ~doc:"JSON TaskStatuses"
       and taskIdentifier =
         flag "task-identifier" (optional json_arg)
           ~doc:"JSON TaskIdentifiers"
       and opportunityIdentifier =
         flag "opportunity-identifier" (optional json_arg)
           ~doc:"JSON OpportunityIdentifiers"
       and engagementIdentifier =
         flag "engagement-identifier" (optional json_arg)
           ~doc:"JSON EngagementIdentifiers"
       and contextIdentifier =
         flag "context-identifier" (optional json_arg)
           ~doc:"JSON ContextIdentifiers"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_opportunity_from_engagement_tasks
           (Values.ListOpportunityFromEngagementTasksRequest.make ?maxResults
              ?nextToken
              ?sort:(Option.map ~f:Values.ListTasksSortBase.of_json sort)
              ?taskStatus:(Option.map ~f:Values.TaskStatuses.of_json
                             taskStatus)
              ?taskIdentifier:(Option.map ~f:Values.TaskIdentifiers.of_json
                                 taskIdentifier)
              ?opportunityIdentifier:(Option.map
                                        ~f:Values.OpportunityIdentifiers.of_json
                                        opportunityIdentifier)
              ?engagementIdentifier:(Option.map
                                       ~f:Values.EngagementIdentifiers.of_json
                                       engagementIdentifier)
              ?contextIdentifier:(Option.map
                                    ~f:Values.ContextIdentifiers.of_json
                                    contextIdentifier) ~catalog ())
           (Some Values.ListOpportunityFromEngagementTasksResponse.to_json)
           (Some
              Values.ListOpportunityFromEngagementTasksResponse.error_to_json)])
let list_resource_snapshot_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListResourceSnapshotJobsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and engagementIdentifier =
         flag "engagement-identifier" (optional string)
           ~doc:"STRING EngagementIdentifier"
       and status =
         flag "status" (optional json_arg)
           ~doc:"JSON ResourceSnapshotJobStatus"
       and sort = flag "sort" (optional json_arg) ~doc:"JSON SortObject"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resource_snapshot_jobs
           (Values.ListResourceSnapshotJobsRequest.make ?maxResults
              ?nextToken ?engagementIdentifier
              ?status:(Option.map ~f:Values.ResourceSnapshotJobStatus.of_json
                         status)
              ?sort:(Option.map ~f:Values.SortObject.of_json sort) ~catalog
              ()) (Some Values.ListResourceSnapshotJobsResponse.to_json)
           (Some Values.ListResourceSnapshotJobsResponse.error_to_json)])
let list_resource_snapshots =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListResourceSnapshotsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and resourceType =
         flag "resource-type" (optional json_arg) ~doc:"JSON ResourceType"
       and resourceIdentifier =
         flag "resource-identifier" (optional string)
           ~doc:"STRING ResourceIdentifier"
       and resourceSnapshotTemplateIdentifier =
         flag "resource-snapshot-template-identifier" (optional string)
           ~doc:"STRING ResourceTemplateName"
       and createdBy =
         flag "created-by" (optional string) ~doc:"STRING AwsAccount"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and engagementIdentifier =
         flag "engagement-identifier" (required string)
           ~doc:"STRING EngagementIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resource_snapshots
           (Values.ListResourceSnapshotsRequest.make ?maxResults ?nextToken
              ?resourceType:(Option.map ~f:Values.ResourceType.of_json
                               resourceType) ?resourceIdentifier
              ?resourceSnapshotTemplateIdentifier ?createdBy ~catalog
              ~engagementIdentifier ())
           (Some Values.ListResourceSnapshotsResponse.to_json)
           (Some Values.ListResourceSnapshotsResponse.error_to_json)])
let list_solutions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults = flag "max-results" (optional int) ~doc:"INT PageSize"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and sort = flag "sort" (optional json_arg) ~doc:"JSON SolutionSort"
       and status =
         flag "status" (optional json_arg)
           ~doc:"JSON ListSolutionsRequestStatusList"
       and identifier =
         flag "identifier" (optional json_arg)
           ~doc:"JSON ListSolutionsRequestIdentifierList"
       and category =
         flag "category" (optional json_arg)
           ~doc:"JSON ListSolutionsRequestCategoryList"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_solutions
           (Values.ListSolutionsRequest.make ?maxResults ?nextToken
              ?sort:(Option.map ~f:Values.SolutionSort.of_json sort)
              ?status:(Option.map
                         ~f:Values.ListSolutionsRequestStatusList.of_json
                         status)
              ?identifier:(Option.map
                             ~f:Values.ListSolutionsRequestIdentifierList.of_json
                             identifier)
              ?category:(Option.map
                           ~f:Values.ListSolutionsRequestCategoryList.of_json
                           category) ~catalog ())
           (Some Values.ListSolutionsResponse.to_json)
           (Some Values.ListSolutionsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING TaggableResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_selling_system_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceSnapshotJobRoleIdentifier =
         flag "resource-snapshot-job-role-identifier" (optional string)
           ~doc:"STRING ResourceSnapshotJobRoleIdentifier"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_selling_system_settings
           (Values.PutSellingSystemSettingsRequest.make
              ?resourceSnapshotJobRoleIdentifier ~catalog ())
           (Some Values.PutSellingSystemSettingsResponse.to_json)
           (Some Values.PutSellingSystemSettingsResponse.error_to_json)])
let reject_engagement_invitation =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and rejectionReason =
         flag "rejection-reason" (optional string)
           ~doc:"STRING RejectionReasonString"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING EngagementInvitationArnOrIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.reject_engagement_invitation
           (Values.RejectEngagementInvitationRequest.make ?rejectionReason
              ~catalog ~identifier ()) None None])
let start_engagement_by_accepting_invitation_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING EngagementInvitationArnOrIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_engagement_by_accepting_invitation_task
           (Values.StartEngagementByAcceptingInvitationTaskRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~catalog
              ~clientToken ~identifier ())
           (Some
              Values.StartEngagementByAcceptingInvitationTaskResponse.to_json)
           (Some
              Values.StartEngagementByAcceptingInvitationTaskResponse.error_to_json)])
let start_engagement_from_opportunity_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING OpportunityIdentifier"
       and awsSubmission =
         flag "aws-submission" (required json_arg) ~doc:"JSON AwsSubmission" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_engagement_from_opportunity_task
           (Values.StartEngagementFromOpportunityTaskRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~catalog
              ~clientToken ~identifier
              ~awsSubmission:(Values.AwsSubmission.of_json awsSubmission) ())
           (Some Values.StartEngagementFromOpportunityTaskResponse.to_json)
           (Some
              Values.StartEngagementFromOpportunityTaskResponse.error_to_json)])
let start_opportunity_from_engagement_task =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING EngagementArnOrIdentifier"
       and contextIdentifier =
         flag "context-identifier" (required string)
           ~doc:"STRING ContextIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_opportunity_from_engagement_task
           (Values.StartOpportunityFromEngagementTaskRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~catalog
              ~clientToken ~identifier ~contextIdentifier ())
           (Some Values.StartOpportunityFromEngagementTaskResponse.to_json)
           (Some
              Values.StartOpportunityFromEngagementTaskResponse.error_to_json)])
let start_resource_snapshot_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and resourceSnapshotJobIdentifier =
         flag "resource-snapshot-job-identifier" (required string)
           ~doc:"STRING ResourceSnapshotJobIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_resource_snapshot_job
           (Values.StartResourceSnapshotJobRequest.make ~catalog
              ~resourceSnapshotJobIdentifier ()) None None])
let stop_resource_snapshot_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and resourceSnapshotJobIdentifier =
         flag "resource-snapshot-job-identifier" (required string)
           ~doc:"STRING ResourceSnapshotJobIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_resource_snapshot_job
           (Values.StopResourceSnapshotJobRequest.make ~catalog
              ~resourceSnapshotJobIdentifier ()) None None])
let submit_opportunity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and visibility =
         flag "visibility" (optional json_arg) ~doc:"JSON Visibility"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING OpportunityIdentifier"
       and involvementType =
         flag "involvement-type" (required json_arg)
           ~doc:"JSON SalesInvolvementType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.submit_opportunity
           (Values.SubmitOpportunityRequest.make
              ?visibility:(Option.map ~f:Values.Visibility.of_json visibility)
              ~catalog ~identifier
              ~involvementType:(Values.SalesInvolvementType.of_json
                                  involvementType) ()) None None])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING TaggableResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING TaggableResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_engagement_context =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and engagementIdentifier =
         flag "engagement-identifier" (required string)
           ~doc:"STRING EngagementArnOrIdentifier"
       and contextIdentifier =
         flag "context-identifier" (required string)
           ~doc:"STRING EngagementContextIdentifier"
       and engagementLastModifiedAt =
         flag "engagement-last-modified-at" (required json_arg)
           ~doc:"JSON DateTime"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON EngagementContextType"
       and payload =
         flag "payload" (required json_arg)
           ~doc:"JSON UpdateEngagementContextPayload" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_engagement_context
           (Values.UpdateEngagementContextRequest.make ~catalog
              ~engagementIdentifier ~contextIdentifier
              ~engagementLastModifiedAt:(Values.DateTime.of_json
                                           engagementLastModifiedAt)
              ~type_:(Values.EngagementContextType.of_json type_)
              ~payload:(Values.UpdateEngagementContextPayload.of_json payload)
              ()) (Some Values.UpdateEngagementContextResponse.to_json)
           (Some Values.UpdateEngagementContextResponse.error_to_json)])
let update_opportunity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and primaryNeedsFromAws =
         flag "primary-needs-from-aws" (optional json_arg)
           ~doc:"JSON PrimaryNeedsFromAws"
       and nationalSecurity =
         flag "national-security" (optional json_arg)
           ~doc:"JSON NationalSecurity"
       and partnerOpportunityIdentifier =
         flag "partner-opportunity-identifier" (optional string)
           ~doc:"STRING UpdateOpportunityRequestPartnerOpportunityIdentifierString"
       and customer =
         flag "customer" (optional json_arg) ~doc:"JSON Customer"
       and project = flag "project" (optional json_arg) ~doc:"JSON Project"
       and opportunityType =
         flag "opportunity-type" (optional json_arg)
           ~doc:"JSON OpportunityType"
       and marketing =
         flag "marketing" (optional json_arg) ~doc:"JSON Marketing"
       and softwareRevenue =
         flag "software-revenue" (optional json_arg)
           ~doc:"JSON SoftwareRevenue"
       and lifeCycle =
         flag "life-cycle" (optional json_arg) ~doc:"JSON LifeCycle"
       and catalog =
         flag "catalog" (required string) ~doc:"STRING CatalogIdentifier"
       and lastModifiedDate =
         flag "last-modified-date" (required json_arg) ~doc:"JSON DateTime"
       and identifier =
         flag "identifier" (required string)
           ~doc:"STRING OpportunityIdentifier" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_opportunity
           (Values.UpdateOpportunityRequest.make
              ?primaryNeedsFromAws:(Option.map
                                      ~f:Values.PrimaryNeedsFromAws.of_json
                                      primaryNeedsFromAws)
              ?nationalSecurity:(Option.map
                                   ~f:Values.NationalSecurity.of_json
                                   nationalSecurity)
              ?partnerOpportunityIdentifier
              ?customer:(Option.map ~f:Values.Customer.of_json customer)
              ?project:(Option.map ~f:Values.Project.of_json project)
              ?opportunityType:(Option.map ~f:Values.OpportunityType.of_json
                                  opportunityType)
              ?marketing:(Option.map ~f:Values.Marketing.of_json marketing)
              ?softwareRevenue:(Option.map ~f:Values.SoftwareRevenue.of_json
                                  softwareRevenue)
              ?lifeCycle:(Option.map ~f:Values.LifeCycle.of_json lifeCycle)
              ~catalog
              ~lastModifiedDate:(Values.DateTime.of_json lastModifiedDate)
              ~identifier ()) (Some Values.UpdateOpportunityResponse.to_json)
           (Some Values.UpdateOpportunityResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("accept-engagement-invitation", accept_engagement_invitation);
    ("assign-opportunity", assign_opportunity);
    ("associate-opportunity", associate_opportunity);
    ("create-engagement", create_engagement);
    ("create-engagement-context", create_engagement_context);
    ("create-engagement-invitation", create_engagement_invitation);
    ("create-opportunity", create_opportunity);
    ("create-resource-snapshot", create_resource_snapshot);
    ("create-resource-snapshot-job", create_resource_snapshot_job);
    ("delete-resource-snapshot-job", delete_resource_snapshot_job);
    ("disassociate-opportunity", disassociate_opportunity);
    ("get-aws-opportunity-summary", get_aws_opportunity_summary);
    ("get-engagement", get_engagement);
    ("get-engagement-invitation", get_engagement_invitation);
    ("get-opportunity", get_opportunity);
    ("get-resource-snapshot", get_resource_snapshot);
    ("get-resource-snapshot-job", get_resource_snapshot_job);
    ("get-selling-system-settings", get_selling_system_settings);
    ("list-engagement-by-accepting-invitation-tasks",
      list_engagement_by_accepting_invitation_tasks);
    ("list-engagement-from-opportunity-tasks",
      list_engagement_from_opportunity_tasks);
    ("list-engagement-invitations", list_engagement_invitations);
    ("list-engagement-members", list_engagement_members);
    ("list-engagement-resource-associations",
      list_engagement_resource_associations);
    ("list-engagements", list_engagements);
    ("list-opportunities", list_opportunities);
    ("list-opportunity-from-engagement-tasks",
      list_opportunity_from_engagement_tasks);
    ("list-resource-snapshot-jobs", list_resource_snapshot_jobs);
    ("list-resource-snapshots", list_resource_snapshots);
    ("list-solutions", list_solutions);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-selling-system-settings", put_selling_system_settings);
    ("reject-engagement-invitation", reject_engagement_invitation);
    ("start-engagement-by-accepting-invitation-task",
      start_engagement_by_accepting_invitation_task);
    ("start-engagement-from-opportunity-task",
      start_engagement_from_opportunity_task);
    ("start-opportunity-from-engagement-task",
      start_opportunity_from_engagement_task);
    ("start-resource-snapshot-job", start_resource_snapshot_job);
    ("stop-resource-snapshot-job", stop_resource_snapshot_job);
    ("submit-opportunity", submit_opportunity);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-engagement-context", update_engagement_context);
    ("update-opportunity", update_opportunity)]
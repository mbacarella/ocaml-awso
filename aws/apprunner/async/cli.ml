(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_custom_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and enableWWWSubdomain =
         flag "enable-w-w-w-subdomain" (optional bool)
           ~doc:"BOOL NullableBoolean"
       and serviceArn =
         flag "service-arn" (required string)
           ~doc:"STRING AppRunnerResourceArn"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_custom_domain
           (Values.AssociateCustomDomainRequest.make ?enableWWWSubdomain
              ~serviceArn ~domainName ())
           (Some Values.AssociateCustomDomainResponse.to_json)
           (Some Values.AssociateCustomDomainResponse.error_to_json)])
let create_auto_scaling_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxConcurrency =
         flag "max-concurrency" (optional int)
           ~doc:"INT ASConfigMaxConcurrency"
       and minSize =
         flag "min-size" (optional int) ~doc:"INT ASConfigMinSize"
       and maxSize =
         flag "max-size" (optional int) ~doc:"INT ASConfigMaxSize"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and autoScalingConfigurationName =
         flag "auto-scaling-configuration-name" (required string)
           ~doc:"STRING AutoScalingConfigurationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_auto_scaling_configuration
           (Values.CreateAutoScalingConfigurationRequest.make ?maxConcurrency
              ?minSize ?maxSize
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~autoScalingConfigurationName ())
           (Some Values.CreateAutoScalingConfigurationResponse.to_json)
           (Some Values.CreateAutoScalingConfigurationResponse.error_to_json)])
let create_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and connectionName =
         flag "connection-name" (required string)
           ~doc:"STRING ConnectionName"
       and providerType =
         flag "provider-type" (required json_arg) ~doc:"JSON ProviderType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_connection
           (Values.CreateConnectionRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~connectionName
              ~providerType:(Values.ProviderType.of_json providerType) ())
           (Some Values.CreateConnectionResponse.to_json)
           (Some Values.CreateConnectionResponse.error_to_json)])
let create_observability_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and traceConfiguration =
         flag "trace-configuration" (optional json_arg)
           ~doc:"JSON TraceConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and observabilityConfigurationName =
         flag "observability-configuration-name" (required string)
           ~doc:"STRING ObservabilityConfigurationName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_observability_configuration
           (Values.CreateObservabilityConfigurationRequest.make
              ?traceConfiguration:(Option.map
                                     ~f:Values.TraceConfiguration.of_json
                                     traceConfiguration)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~observabilityConfigurationName ())
           (Some Values.CreateObservabilityConfigurationResponse.to_json)
           (Some
              Values.CreateObservabilityConfigurationResponse.error_to_json)])
let create_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceConfiguration =
         flag "instance-configuration" (optional json_arg)
           ~doc:"JSON InstanceConfiguration"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and encryptionConfiguration =
         flag "encryption-configuration" (optional json_arg)
           ~doc:"JSON EncryptionConfiguration"
       and healthCheckConfiguration =
         flag "health-check-configuration" (optional json_arg)
           ~doc:"JSON HealthCheckConfiguration"
       and autoScalingConfigurationArn =
         flag "auto-scaling-configuration-arn" (optional string)
           ~doc:"STRING AppRunnerResourceArn"
       and networkConfiguration =
         flag "network-configuration" (optional json_arg)
           ~doc:"JSON NetworkConfiguration"
       and observabilityConfiguration =
         flag "observability-configuration" (optional json_arg)
           ~doc:"JSON ServiceObservabilityConfiguration"
       and serviceName =
         flag "service-name" (required string) ~doc:"STRING ServiceName"
       and sourceConfiguration =
         flag "source-configuration" (required json_arg)
           ~doc:"JSON SourceConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_service
           (Values.CreateServiceRequest.make
              ?instanceConfiguration:(Option.map
                                        ~f:Values.InstanceConfiguration.of_json
                                        instanceConfiguration)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?encryptionConfiguration:(Option.map
                                          ~f:Values.EncryptionConfiguration.of_json
                                          encryptionConfiguration)
              ?healthCheckConfiguration:(Option.map
                                           ~f:Values.HealthCheckConfiguration.of_json
                                           healthCheckConfiguration)
              ?autoScalingConfigurationArn
              ?networkConfiguration:(Option.map
                                       ~f:Values.NetworkConfiguration.of_json
                                       networkConfiguration)
              ?observabilityConfiguration:(Option.map
                                             ~f:Values.ServiceObservabilityConfiguration.of_json
                                             observabilityConfiguration)
              ~serviceName
              ~sourceConfiguration:(Values.SourceConfiguration.of_json
                                      sourceConfiguration) ())
           (Some Values.CreateServiceResponse.to_json)
           (Some Values.CreateServiceResponse.error_to_json)])
let create_vpc_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and securityGroups =
         flag "security-groups" (optional json_arg) ~doc:"JSON StringList"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and vpcConnectorName =
         flag "vpc-connector-name" (required string)
           ~doc:"STRING VpcConnectorName"
       and subnets =
         flag "subnets" (required json_arg) ~doc:"JSON StringList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_vpc_connector
           (Values.CreateVpcConnectorRequest.make
              ?securityGroups:(Option.map ~f:Values.StringList.of_json
                                 securityGroups)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~vpcConnectorName ~subnets:(Values.StringList.of_json subnets)
              ()) (Some Values.CreateVpcConnectorResponse.to_json)
           (Some Values.CreateVpcConnectorResponse.error_to_json)])
let delete_auto_scaling_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoScalingConfigurationArn =
         flag "auto-scaling-configuration-arn" (required string)
           ~doc:"STRING AppRunnerResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_auto_scaling_configuration
           (Values.DeleteAutoScalingConfigurationRequest.make
              ~autoScalingConfigurationArn ())
           (Some Values.DeleteAutoScalingConfigurationResponse.to_json)
           (Some Values.DeleteAutoScalingConfigurationResponse.error_to_json)])
let delete_connection =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectionArn =
         flag "connection-arn" (required string)
           ~doc:"STRING AppRunnerResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_connection
           (Values.DeleteConnectionRequest.make ~connectionArn ())
           (Some Values.DeleteConnectionResponse.to_json)
           (Some Values.DeleteConnectionResponse.error_to_json)])
let delete_observability_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and observabilityConfigurationArn =
         flag "observability-configuration-arn" (required string)
           ~doc:"STRING AppRunnerResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_observability_configuration
           (Values.DeleteObservabilityConfigurationRequest.make
              ~observabilityConfigurationArn ())
           (Some Values.DeleteObservabilityConfigurationResponse.to_json)
           (Some
              Values.DeleteObservabilityConfigurationResponse.error_to_json)])
let delete_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceArn =
         flag "service-arn" (required string)
           ~doc:"STRING AppRunnerResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_service
           (Values.DeleteServiceRequest.make ~serviceArn ())
           (Some Values.DeleteServiceResponse.to_json)
           (Some Values.DeleteServiceResponse.error_to_json)])
let delete_vpc_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vpcConnectorArn =
         flag "vpc-connector-arn" (required string)
           ~doc:"STRING AppRunnerResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vpc_connector
           (Values.DeleteVpcConnectorRequest.make ~vpcConnectorArn ())
           (Some Values.DeleteVpcConnectorResponse.to_json)
           (Some Values.DeleteVpcConnectorResponse.error_to_json)])
let describe_auto_scaling_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoScalingConfigurationArn =
         flag "auto-scaling-configuration-arn" (required string)
           ~doc:"STRING AppRunnerResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_auto_scaling_configuration
           (Values.DescribeAutoScalingConfigurationRequest.make
              ~autoScalingConfigurationArn ())
           (Some Values.DescribeAutoScalingConfigurationResponse.to_json)
           (Some
              Values.DescribeAutoScalingConfigurationResponse.error_to_json)])
let describe_custom_domains =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT DescribeCustomDomainsMaxResults"
       and serviceArn =
         flag "service-arn" (required string)
           ~doc:"STRING AppRunnerResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_custom_domains
           (Values.DescribeCustomDomainsRequest.make ?nextToken ?maxResults
              ~serviceArn ())
           (Some Values.DescribeCustomDomainsResponse.to_json)
           (Some Values.DescribeCustomDomainsResponse.error_to_json)])
let describe_observability_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and observabilityConfigurationArn =
         flag "observability-configuration-arn" (required string)
           ~doc:"STRING AppRunnerResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_observability_configuration
           (Values.DescribeObservabilityConfigurationRequest.make
              ~observabilityConfigurationArn ())
           (Some Values.DescribeObservabilityConfigurationResponse.to_json)
           (Some
              Values.DescribeObservabilityConfigurationResponse.error_to_json)])
let describe_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceArn =
         flag "service-arn" (required string)
           ~doc:"STRING AppRunnerResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_service
           (Values.DescribeServiceRequest.make ~serviceArn ())
           (Some Values.DescribeServiceResponse.to_json)
           (Some Values.DescribeServiceResponse.error_to_json)])
let describe_vpc_connector =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vpcConnectorArn =
         flag "vpc-connector-arn" (required string)
           ~doc:"STRING AppRunnerResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_vpc_connector
           (Values.DescribeVpcConnectorRequest.make ~vpcConnectorArn ())
           (Some Values.DescribeVpcConnectorResponse.to_json)
           (Some Values.DescribeVpcConnectorResponse.error_to_json)])
let disassociate_custom_domain =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceArn =
         flag "service-arn" (required string)
           ~doc:"STRING AppRunnerResourceArn"
       and domainName =
         flag "domain-name" (required string) ~doc:"STRING DomainName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_custom_domain
           (Values.DisassociateCustomDomainRequest.make ~serviceArn
              ~domainName ())
           (Some Values.DisassociateCustomDomainResponse.to_json)
           (Some Values.DisassociateCustomDomainResponse.error_to_json)])
let list_auto_scaling_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and autoScalingConfigurationName =
         flag "auto-scaling-configuration-name" (optional string)
           ~doc:"STRING AutoScalingConfigurationName"
       and latestOnly =
         flag "latest-only" (optional bool) ~doc:"BOOL Boolean"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_auto_scaling_configurations
           (Values.ListAutoScalingConfigurationsRequest.make
              ?autoScalingConfigurationName ?latestOnly ?maxResults
              ?nextToken ())
           (Some Values.ListAutoScalingConfigurationsResponse.to_json)
           (Some Values.ListAutoScalingConfigurationsResponse.error_to_json)])
let list_connections =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and connectionName =
         flag "connection-name" (optional string)
           ~doc:"STRING ConnectionName"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_connections
           (Values.ListConnectionsRequest.make ?connectionName ?maxResults
              ?nextToken ()) (Some Values.ListConnectionsResponse.to_json)
           (Some Values.ListConnectionsResponse.error_to_json)])
let list_observability_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and observabilityConfigurationName =
         flag "observability-configuration-name" (optional string)
           ~doc:"STRING ObservabilityConfigurationName"
       and latestOnly =
         flag "latest-only" (optional bool) ~doc:"BOOL Boolean"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_observability_configurations
           (Values.ListObservabilityConfigurationsRequest.make
              ?observabilityConfigurationName ?latestOnly ?maxResults
              ?nextToken ())
           (Some Values.ListObservabilityConfigurationsResponse.to_json)
           (Some Values.ListObservabilityConfigurationsResponse.error_to_json)])
let list_operations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListOperationsMaxResults"
       and serviceArn =
         flag "service-arn" (required string)
           ~doc:"STRING AppRunnerResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_operations
           (Values.ListOperationsRequest.make ?nextToken ?maxResults
              ~serviceArn ()) (Some Values.ListOperationsResponse.to_json)
           (Some Values.ListOperationsResponse.error_to_json)])
let list_services =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING String"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ServiceMaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_services
           (Values.ListServicesRequest.make ?nextToken ?maxResults ())
           (Some Values.ListServicesResponse.to_json)
           (Some Values.ListServicesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AppRunnerResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_vpc_connectors =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_vpc_connectors
           (Values.ListVpcConnectorsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListVpcConnectorsResponse.to_json)
           (Some Values.ListVpcConnectorsResponse.error_to_json)])
let pause_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceArn =
         flag "service-arn" (required string)
           ~doc:"STRING AppRunnerResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.pause_service (Values.PauseServiceRequest.make ~serviceArn ())
           (Some Values.PauseServiceResponse.to_json)
           (Some Values.PauseServiceResponse.error_to_json)])
let resume_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceArn =
         flag "service-arn" (required string)
           ~doc:"STRING AppRunnerResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.resume_service
           (Values.ResumeServiceRequest.make ~serviceArn ())
           (Some Values.ResumeServiceResponse.to_json)
           (Some Values.ResumeServiceResponse.error_to_json)])
let start_deployment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serviceArn =
         flag "service-arn" (required string)
           ~doc:"STRING AppRunnerResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_deployment
           (Values.StartDeploymentRequest.make ~serviceArn ())
           (Some Values.StartDeploymentResponse.to_json)
           (Some Values.StartDeploymentResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AppRunnerResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AppRunnerResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_service =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sourceConfiguration =
         flag "source-configuration" (optional json_arg)
           ~doc:"JSON SourceConfiguration"
       and instanceConfiguration =
         flag "instance-configuration" (optional json_arg)
           ~doc:"JSON InstanceConfiguration"
       and autoScalingConfigurationArn =
         flag "auto-scaling-configuration-arn" (optional string)
           ~doc:"STRING AppRunnerResourceArn"
       and healthCheckConfiguration =
         flag "health-check-configuration" (optional json_arg)
           ~doc:"JSON HealthCheckConfiguration"
       and networkConfiguration =
         flag "network-configuration" (optional json_arg)
           ~doc:"JSON NetworkConfiguration"
       and observabilityConfiguration =
         flag "observability-configuration" (optional json_arg)
           ~doc:"JSON ServiceObservabilityConfiguration"
       and serviceArn =
         flag "service-arn" (required string)
           ~doc:"STRING AppRunnerResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_service
           (Values.UpdateServiceRequest.make
              ?sourceConfiguration:(Option.map
                                      ~f:Values.SourceConfiguration.of_json
                                      sourceConfiguration)
              ?instanceConfiguration:(Option.map
                                        ~f:Values.InstanceConfiguration.of_json
                                        instanceConfiguration)
              ?autoScalingConfigurationArn
              ?healthCheckConfiguration:(Option.map
                                           ~f:Values.HealthCheckConfiguration.of_json
                                           healthCheckConfiguration)
              ?networkConfiguration:(Option.map
                                       ~f:Values.NetworkConfiguration.of_json
                                       networkConfiguration)
              ?observabilityConfiguration:(Option.map
                                             ~f:Values.ServiceObservabilityConfiguration.of_json
                                             observabilityConfiguration)
              ~serviceArn ()) (Some Values.UpdateServiceResponse.to_json)
           (Some Values.UpdateServiceResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-custom-domain", associate_custom_domain);
    ("create-auto-scaling-configuration", create_auto_scaling_configuration);
    ("create-connection", create_connection);
    ("create-observability-configuration",
      create_observability_configuration);
    ("create-service", create_service);
    ("create-vpc-connector", create_vpc_connector);
    ("delete-auto-scaling-configuration", delete_auto_scaling_configuration);
    ("delete-connection", delete_connection);
    ("delete-observability-configuration",
      delete_observability_configuration);
    ("delete-service", delete_service);
    ("delete-vpc-connector", delete_vpc_connector);
    ("describe-auto-scaling-configuration",
      describe_auto_scaling_configuration);
    ("describe-custom-domains", describe_custom_domains);
    ("describe-observability-configuration",
      describe_observability_configuration);
    ("describe-service", describe_service);
    ("describe-vpc-connector", describe_vpc_connector);
    ("disassociate-custom-domain", disassociate_custom_domain);
    ("list-auto-scaling-configurations", list_auto_scaling_configurations);
    ("list-connections", list_connections);
    ("list-observability-configurations", list_observability_configurations);
    ("list-operations", list_operations);
    ("list-services", list_services);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-vpc-connectors", list_vpc_connectors);
    ("pause-service", pause_service);
    ("resume-service", resume_service);
    ("start-deployment", start_deployment);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-service", update_service)]
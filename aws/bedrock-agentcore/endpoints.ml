(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | BatchCreateMemoryRecords: (BatchCreateMemoryRecordsInput.t,
  BatchCreateMemoryRecordsOutput.t, BatchCreateMemoryRecordsOutput.error) t 
  | BatchDeleteMemoryRecords: (BatchDeleteMemoryRecordsInput.t,
  BatchDeleteMemoryRecordsOutput.t, BatchDeleteMemoryRecordsOutput.error) t 
  | BatchUpdateMemoryRecords: (BatchUpdateMemoryRecordsInput.t,
  BatchUpdateMemoryRecordsOutput.t, BatchUpdateMemoryRecordsOutput.error) t 
  | CompleteResourceTokenAuth: (CompleteResourceTokenAuthRequest.t,
  CompleteResourceTokenAuthResponse.t,
  CompleteResourceTokenAuthResponse.error) t 
  | CreateABTest: (CreateABTestRequest.t, CreateABTestResponse.t,
  CreateABTestResponse.error) t 
  | CreateEvent: (CreateEventInput.t, CreateEventOutput.t,
  CreateEventOutput.error) t 
  | CreatePaymentInstrument: (CreatePaymentInstrumentRequest.t,
  CreatePaymentInstrumentResponse.t, CreatePaymentInstrumentResponse.error) t
  
  | CreatePaymentSession: (CreatePaymentSessionRequest.t,
  CreatePaymentSessionResponse.t, CreatePaymentSessionResponse.error) t 
  | DeleteABTest: (DeleteABTestRequest.t, DeleteABTestResponse.t,
  DeleteABTestResponse.error) t 
  | DeleteBatchEvaluation: (DeleteBatchEvaluationRequest.t,
  DeleteBatchEvaluationResponse.t, DeleteBatchEvaluationResponse.error) t 
  | DeleteEvent: (DeleteEventInput.t, DeleteEventOutput.t,
  DeleteEventOutput.error) t 
  | DeleteMemoryRecord: (DeleteMemoryRecordInput.t,
  DeleteMemoryRecordOutput.t, DeleteMemoryRecordOutput.error) t 
  | DeletePaymentInstrument: (DeletePaymentInstrumentRequest.t,
  DeletePaymentInstrumentResponse.t, DeletePaymentInstrumentResponse.error) t
  
  | DeletePaymentSession: (DeletePaymentSessionRequest.t,
  DeletePaymentSessionResponse.t, DeletePaymentSessionResponse.error) t 
  | DeleteRecommendation: (DeleteRecommendationRequest.t,
  DeleteRecommendationResponse.t, DeleteRecommendationResponse.error) t 
  | Evaluate: (EvaluateRequest.t, EvaluateResponse.t, EvaluateResponse.error)
  t 
  | GetABTest: (GetABTestRequest.t, GetABTestResponse.t,
  GetABTestResponse.error) t 
  | GetAgentCard: (GetAgentCardRequest.t, GetAgentCardResponse.t,
  GetAgentCardResponse.error) t 
  | GetBatchEvaluation: (GetBatchEvaluationRequest.t,
  GetBatchEvaluationResponse.t, GetBatchEvaluationResponse.error) t 
  | GetBrowserSession: (GetBrowserSessionRequest.t,
  GetBrowserSessionResponse.t, GetBrowserSessionResponse.error) t 
  | GetCodeInterpreterSession: (GetCodeInterpreterSessionRequest.t,
  GetCodeInterpreterSessionResponse.t,
  GetCodeInterpreterSessionResponse.error) t 
  | GetEvent: (GetEventInput.t, GetEventOutput.t, GetEventOutput.error) t 
  | GetMemoryRecord: (GetMemoryRecordInput.t, GetMemoryRecordOutput.t,
  GetMemoryRecordOutput.error) t 
  | GetPaymentInstrument: (GetPaymentInstrumentRequest.t,
  GetPaymentInstrumentResponse.t, GetPaymentInstrumentResponse.error) t 
  | GetPaymentInstrumentBalance: (GetPaymentInstrumentBalanceRequest.t,
  GetPaymentInstrumentBalanceResponse.t,
  GetPaymentInstrumentBalanceResponse.error) t 
  | GetPaymentSession: (GetPaymentSessionRequest.t,
  GetPaymentSessionResponse.t, GetPaymentSessionResponse.error) t 
  | GetRecommendation: (GetRecommendationRequest.t,
  GetRecommendationResponse.t, GetRecommendationResponse.error) t 
  | GetResourceApiKey: (GetResourceApiKeyRequest.t,
  GetResourceApiKeyResponse.t, GetResourceApiKeyResponse.error) t 
  | GetResourceOauth2Token: (GetResourceOauth2TokenRequest.t,
  GetResourceOauth2TokenResponse.t, GetResourceOauth2TokenResponse.error) t 
  | GetResourcePaymentToken: (GetResourcePaymentTokenRequest.t,
  GetResourcePaymentTokenResponse.t, GetResourcePaymentTokenResponse.error) t
  
  | GetWorkloadAccessToken: (GetWorkloadAccessTokenRequest.t,
  GetWorkloadAccessTokenResponse.t, GetWorkloadAccessTokenResponse.error) t 
  | GetWorkloadAccessTokenForJWT: (GetWorkloadAccessTokenForJWTRequest.t,
  GetWorkloadAccessTokenForJWTResponse.t,
  GetWorkloadAccessTokenForJWTResponse.error) t 
  | GetWorkloadAccessTokenForUserId:
  (GetWorkloadAccessTokenForUserIdRequest.t,
  GetWorkloadAccessTokenForUserIdResponse.t,
  GetWorkloadAccessTokenForUserIdResponse.error) t 
  | InvokeAgentRuntime: (InvokeAgentRuntimeRequest.t,
  InvokeAgentRuntimeResponse.t, InvokeAgentRuntimeResponse.error) t 
  | InvokeAgentRuntimeCommand: (InvokeAgentRuntimeCommandRequest.t,
  InvokeAgentRuntimeCommandResponse.t,
  InvokeAgentRuntimeCommandResponse.error) t 
  | InvokeBrowser: (InvokeBrowserRequest.t, InvokeBrowserResponse.t,
  InvokeBrowserResponse.error) t 
  | InvokeCodeInterpreter: (InvokeCodeInterpreterRequest.t,
  InvokeCodeInterpreterResponse.t, InvokeCodeInterpreterResponse.error) t 
  | InvokeHarness: (InvokeHarnessRequest.t, InvokeHarnessResponse.t,
  InvokeHarnessResponse.error) t 
  | ListABTests: (ListABTestsRequest.t, ListABTestsResponse.t,
  ListABTestsResponse.error) t 
  | ListActors: (ListActorsInput.t, ListActorsOutput.t,
  ListActorsOutput.error) t 
  | ListBatchEvaluations: (ListBatchEvaluationsRequest.t,
  ListBatchEvaluationsResponse.t, ListBatchEvaluationsResponse.error) t 
  | ListBrowserSessions: (ListBrowserSessionsRequest.t,
  ListBrowserSessionsResponse.t, ListBrowserSessionsResponse.error) t 
  | ListCodeInterpreterSessions: (ListCodeInterpreterSessionsRequest.t,
  ListCodeInterpreterSessionsResponse.t,
  ListCodeInterpreterSessionsResponse.error) t 
  | ListEvents: (ListEventsInput.t, ListEventsOutput.t,
  ListEventsOutput.error) t 
  | ListMemoryExtractionJobs: (ListMemoryExtractionJobsInput.t,
  ListMemoryExtractionJobsOutput.t, ListMemoryExtractionJobsOutput.error) t 
  | ListMemoryRecords: (ListMemoryRecordsInput.t, ListMemoryRecordsOutput.t,
  ListMemoryRecordsOutput.error) t 
  | ListPaymentInstruments: (ListPaymentInstrumentsRequest.t,
  ListPaymentInstrumentsResponse.t, ListPaymentInstrumentsResponse.error) t 
  | ListPaymentSessions: (ListPaymentSessionsRequest.t,
  ListPaymentSessionsResponse.t, ListPaymentSessionsResponse.error) t 
  | ListRecommendations: (ListRecommendationsRequest.t,
  ListRecommendationsResponse.t, ListRecommendationsResponse.error) t 
  | ListSessions: (ListSessionsInput.t, ListSessionsOutput.t,
  ListSessionsOutput.error) t 
  | ProcessPayment: (ProcessPaymentRequest.t, ProcessPaymentResponse.t,
  ProcessPaymentResponse.error) t 
  | RetrieveMemoryRecords: (RetrieveMemoryRecordsInput.t,
  RetrieveMemoryRecordsOutput.t, RetrieveMemoryRecordsOutput.error) t 
  | SaveBrowserSessionProfile: (SaveBrowserSessionProfileRequest.t,
  SaveBrowserSessionProfileResponse.t,
  SaveBrowserSessionProfileResponse.error) t 
  | SearchRegistryRecords: (SearchRegistryRecordsRequest.t,
  SearchRegistryRecordsResponse.t, SearchRegistryRecordsResponse.error) t 
  | StartBatchEvaluation: (StartBatchEvaluationRequest.t,
  StartBatchEvaluationResponse.t, StartBatchEvaluationResponse.error) t 
  | StartBrowserSession: (StartBrowserSessionRequest.t,
  StartBrowserSessionResponse.t, StartBrowserSessionResponse.error) t 
  | StartCodeInterpreterSession: (StartCodeInterpreterSessionRequest.t,
  StartCodeInterpreterSessionResponse.t,
  StartCodeInterpreterSessionResponse.error) t 
  | StartMemoryExtractionJob: (StartMemoryExtractionJobInput.t,
  StartMemoryExtractionJobOutput.t, StartMemoryExtractionJobOutput.error) t 
  | StartRecommendation: (StartRecommendationRequest.t,
  StartRecommendationResponse.t, StartRecommendationResponse.error) t 
  | StopBatchEvaluation: (StopBatchEvaluationRequest.t,
  StopBatchEvaluationResponse.t, StopBatchEvaluationResponse.error) t 
  | StopBrowserSession: (StopBrowserSessionRequest.t,
  StopBrowserSessionResponse.t, StopBrowserSessionResponse.error) t 
  | StopCodeInterpreterSession: (StopCodeInterpreterSessionRequest.t,
  StopCodeInterpreterSessionResponse.t,
  StopCodeInterpreterSessionResponse.error) t 
  | StopRuntimeSession: (StopRuntimeSessionRequest.t,
  StopRuntimeSessionResponse.t, StopRuntimeSessionResponse.error) t 
  | UpdateABTest: (UpdateABTestRequest.t, UpdateABTestResponse.t,
  UpdateABTestResponse.error) t 
  | UpdateBrowserStream: (UpdateBrowserStreamRequest.t,
  UpdateBrowserStreamResponse.t, UpdateBrowserStreamResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | BatchCreateMemoryRecords -> `POST
  | BatchDeleteMemoryRecords -> `POST
  | BatchUpdateMemoryRecords -> `POST
  | CompleteResourceTokenAuth -> `POST
  | CreateABTest -> `POST
  | CreateEvent -> `POST
  | CreatePaymentInstrument -> `POST
  | CreatePaymentSession -> `POST
  | DeleteABTest -> `DELETE
  | DeleteBatchEvaluation -> `DELETE
  | DeleteEvent -> `DELETE
  | DeleteMemoryRecord -> `DELETE
  | DeletePaymentInstrument -> `POST
  | DeletePaymentSession -> `POST
  | DeleteRecommendation -> `DELETE
  | Evaluate -> `POST
  | GetABTest -> `GET
  | GetAgentCard -> `GET
  | GetBatchEvaluation -> `GET
  | GetBrowserSession -> `GET
  | GetCodeInterpreterSession -> `GET
  | GetEvent -> `GET
  | GetMemoryRecord -> `GET
  | GetPaymentInstrument -> `POST
  | GetPaymentInstrumentBalance -> `POST
  | GetPaymentSession -> `POST
  | GetRecommendation -> `GET
  | GetResourceApiKey -> `POST
  | GetResourceOauth2Token -> `POST
  | GetResourcePaymentToken -> `POST
  | GetWorkloadAccessToken -> `POST
  | GetWorkloadAccessTokenForJWT -> `POST
  | GetWorkloadAccessTokenForUserId -> `POST
  | InvokeAgentRuntime -> `POST
  | InvokeAgentRuntimeCommand -> `POST
  | InvokeBrowser -> `POST
  | InvokeCodeInterpreter -> `POST
  | InvokeHarness -> `POST
  | ListABTests -> `GET
  | ListActors -> `POST
  | ListBatchEvaluations -> `GET
  | ListBrowserSessions -> `POST
  | ListCodeInterpreterSessions -> `POST
  | ListEvents -> `POST
  | ListMemoryExtractionJobs -> `POST
  | ListMemoryRecords -> `POST
  | ListPaymentInstruments -> `POST
  | ListPaymentSessions -> `POST
  | ListRecommendations -> `GET
  | ListSessions -> `POST
  | ProcessPayment -> `POST
  | RetrieveMemoryRecords -> `POST
  | SaveBrowserSessionProfile -> `PUT
  | SearchRegistryRecords -> `POST
  | StartBatchEvaluation -> `POST
  | StartBrowserSession -> `PUT
  | StartCodeInterpreterSession -> `PUT
  | StartMemoryExtractionJob -> `POST
  | StartRecommendation -> `POST
  | StopBatchEvaluation -> `POST
  | StopBrowserSession -> `PUT
  | StopCodeInterpreterSession -> `PUT
  | StopRuntimeSession -> `POST
  | UpdateABTest -> `PUT
  | UpdateBrowserStream -> `PUT
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | BatchCreateMemoryRecords ->
          (Format.kasprintf Uri.of_string)
            "/memories/%s/memoryRecords/batchCreate"
            (MemoryId.to_header x.BatchCreateMemoryRecordsInput.memoryId)
      | BatchDeleteMemoryRecords ->
          (Format.kasprintf Uri.of_string)
            "/memories/%s/memoryRecords/batchDelete"
            (MemoryId.to_header x.BatchDeleteMemoryRecordsInput.memoryId)
      | BatchUpdateMemoryRecords ->
          (Format.kasprintf Uri.of_string)
            "/memories/%s/memoryRecords/batchUpdate"
            (MemoryId.to_header x.BatchUpdateMemoryRecordsInput.memoryId)
      | CompleteResourceTokenAuth ->
          (Format.kasprintf Uri.of_string)
            "/identities/CompleteResourceTokenAuth"
      | CreateABTest -> (Format.kasprintf Uri.of_string) "/ab-tests"
      | CreateEvent ->
          (Format.kasprintf Uri.of_string) "/memories/%s/events"
            (MemoryId.to_header x.CreateEventInput.memoryId)
      | CreatePaymentInstrument ->
          (Format.kasprintf Uri.of_string)
            "/payments/createPaymentInstrument"
      | CreatePaymentSession ->
          (Format.kasprintf Uri.of_string) "/payments/createPaymentSession"
      | DeleteABTest ->
          (Format.kasprintf Uri.of_string) "/ab-tests/%s"
            (ABTestId.to_header x.DeleteABTestRequest.abTestId)
      | DeleteBatchEvaluation ->
          (Format.kasprintf Uri.of_string) "/evaluations/batch-evaluate/%s"
            (BatchEvaluationId.to_header
               x.DeleteBatchEvaluationRequest.batchEvaluationId)
      | DeleteEvent ->
          (Format.kasprintf Uri.of_string)
            "/memories/%s/actor/%s/sessions/%s/events/%s"
            (MemoryId.to_header x.DeleteEventInput.memoryId)
            (ActorId.to_header x.DeleteEventInput.actorId)
            (SessionId.to_header x.DeleteEventInput.sessionId)
            (EventId.to_header x.DeleteEventInput.eventId)
      | DeleteMemoryRecord ->
          (Format.kasprintf Uri.of_string) "/memories/%s/memoryRecords/%s"
            (MemoryId.to_header x.DeleteMemoryRecordInput.memoryId)
            (MemoryRecordId.to_header
               x.DeleteMemoryRecordInput.memoryRecordId)
      | DeletePaymentInstrument ->
          (Format.kasprintf Uri.of_string)
            "/payments/deletePaymentInstrument"
      | DeletePaymentSession ->
          (Format.kasprintf Uri.of_string) "/payments/deletePaymentSession"
      | DeleteRecommendation ->
          (Format.kasprintf Uri.of_string) "/recommendations/%s"
            (RecommendationId.to_header
               x.DeleteRecommendationRequest.recommendationId)
      | Evaluate ->
          (Format.kasprintf Uri.of_string) "/evaluations/evaluate/%s"
            (EvaluatorId.to_header x.EvaluateRequest.evaluatorId)
      | GetABTest ->
          (Format.kasprintf Uri.of_string) "/ab-tests/%s"
            (ABTestId.to_header x.GetABTestRequest.abTestId)
      | GetAgentCard ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string)
               "/runtimes/%s/invocations/.well-known/agent-card.json"
               (String_.to_header x.GetAgentCardRequest.agentRuntimeArn))
            (List.filter_opt
               [Option.map ~f:(fun v -> ("qualifier", (String_.to_header v)))
                  x.qualifier])
      | GetBatchEvaluation ->
          (Format.kasprintf Uri.of_string) "/evaluations/batch-evaluate/%s"
            (BatchEvaluationId.to_header
               x.GetBatchEvaluationRequest.batchEvaluationId)
      | GetBrowserSession ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/browsers/%s/sessions/get"
               (String_.to_header
                  x.GetBrowserSessionRequest.browserIdentifier))
            (List.filter_opt
               [Some ("sessionId", (BrowserSessionId.to_header x.sessionId))])
      | GetCodeInterpreterSession ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string)
               "/code-interpreters/%s/sessions/get"
               (String_.to_header
                  x.GetCodeInterpreterSessionRequest.codeInterpreterIdentifier))
            (List.filter_opt
               [Some
                  ("sessionId",
                    (CodeInterpreterSessionId.to_header x.sessionId))])
      | GetEvent ->
          (Format.kasprintf Uri.of_string)
            "/memories/%s/actor/%s/sessions/%s/events/%s"
            (MemoryId.to_header x.GetEventInput.memoryId)
            (ActorId.to_header x.GetEventInput.actorId)
            (SessionId.to_header x.GetEventInput.sessionId)
            (EventId.to_header x.GetEventInput.eventId)
      | GetMemoryRecord ->
          (Format.kasprintf Uri.of_string) "/memories/%s/memoryRecord/%s"
            (MemoryId.to_header x.GetMemoryRecordInput.memoryId)
            (MemoryRecordId.to_header x.GetMemoryRecordInput.memoryRecordId)
      | GetPaymentInstrument ->
          (Format.kasprintf Uri.of_string) "/payments/getPaymentInstrument"
      | GetPaymentInstrumentBalance ->
          (Format.kasprintf Uri.of_string)
            "/payments/getPaymentInstrumentBalance"
      | GetPaymentSession ->
          (Format.kasprintf Uri.of_string) "/payments/getPaymentSession"
      | GetRecommendation ->
          (Format.kasprintf Uri.of_string) "/recommendations/%s"
            (RecommendationId.to_header
               x.GetRecommendationRequest.recommendationId)
      | GetResourceApiKey ->
          (Format.kasprintf Uri.of_string) "/identities/api-key"
      | GetResourceOauth2Token ->
          (Format.kasprintf Uri.of_string) "/identities/oauth2/token"
      | GetResourcePaymentToken ->
          (Format.kasprintf Uri.of_string) "/identities/payment/token"
      | GetWorkloadAccessToken ->
          (Format.kasprintf Uri.of_string)
            "/identities/GetWorkloadAccessToken"
      | GetWorkloadAccessTokenForJWT ->
          (Format.kasprintf Uri.of_string)
            "/identities/GetWorkloadAccessTokenForJWT"
      | GetWorkloadAccessTokenForUserId ->
          (Format.kasprintf Uri.of_string)
            "/identities/GetWorkloadAccessTokenForUserId"
      | InvokeAgentRuntime ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/runtimes/%s/invocations"
               (String_.to_header x.InvokeAgentRuntimeRequest.agentRuntimeArn))
            (List.filter_opt
               [Option.map ~f:(fun v -> ("qualifier", (String_.to_header v)))
                  x.qualifier;
               Option.map
                 ~f:(fun v ->
                       ("accountId",
                         (InvokeAgentRuntimeRequestAccountIdString.to_header
                            v))) x.accountId])
      | InvokeAgentRuntimeCommand ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/runtimes/%s/commands"
               (String_.to_header
                  x.InvokeAgentRuntimeCommandRequest.agentRuntimeArn))
            (List.filter_opt
               [Option.map ~f:(fun v -> ("qualifier", (String_.to_header v)))
                  x.qualifier;
               Option.map
                 ~f:(fun v ->
                       ("accountId",
                         (InvokeAgentRuntimeCommandRequestAccountIdString.to_header
                            v))) x.accountId])
      | InvokeBrowser ->
          (Format.kasprintf Uri.of_string) "/browsers/%s/sessions/invoke"
            (String_.to_header x.InvokeBrowserRequest.browserIdentifier)
      | InvokeCodeInterpreter ->
          (Format.kasprintf Uri.of_string)
            "/code-interpreters/%s/tools/invoke"
            (String_.to_header
               x.InvokeCodeInterpreterRequest.codeInterpreterIdentifier)
      | InvokeHarness ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/harnesses/invoke")
            (List.filter_opt
               [Some ("harnessArn", (HarnessArn.to_header x.harnessArn))])
      | ListABTests ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/ab-tests")
            (List.filter_opt
               [Option.map
                  ~f:(fun v ->
                        ("maxResults",
                          (ListABTestsRequestMaxResultsInteger.to_header v)))
                  x.maxResults;
               Option.map ~f:(fun v -> ("nextToken", (String_.to_header v)))
                 x.nextToken])
      | ListActors ->
          (Format.kasprintf Uri.of_string) "/memories/%s/actors"
            (MemoryId.to_header x.ListActorsInput.memoryId)
      | ListBatchEvaluations ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/evaluations/batch-evaluate")
            (List.filter_opt
               [Option.map
                  ~f:(fun v ->
                        ("maxResults",
                          (ListBatchEvaluationsRequestMaxResultsInteger.to_header
                             v))) x.maxResults;
               Option.map ~f:(fun v -> ("nextToken", (String_.to_header v)))
                 x.nextToken])
      | ListBrowserSessions ->
          (Format.kasprintf Uri.of_string) "/browsers/%s/sessions/list"
            (String_.to_header x.ListBrowserSessionsRequest.browserIdentifier)
      | ListCodeInterpreterSessions ->
          (Format.kasprintf Uri.of_string)
            "/code-interpreters/%s/sessions/list"
            (String_.to_header
               x.ListCodeInterpreterSessionsRequest.codeInterpreterIdentifier)
      | ListEvents ->
          (Format.kasprintf Uri.of_string)
            "/memories/%s/actor/%s/sessions/%s"
            (MemoryId.to_header x.ListEventsInput.memoryId)
            (ActorId.to_header x.ListEventsInput.actorId)
            (SessionId.to_header x.ListEventsInput.sessionId)
      | ListMemoryExtractionJobs ->
          (Format.kasprintf Uri.of_string) "/memories/%s/extractionJobs"
            (MemoryId.to_header x.ListMemoryExtractionJobsInput.memoryId)
      | ListMemoryRecords ->
          (Format.kasprintf Uri.of_string) "/memories/%s/memoryRecords"
            (MemoryId.to_header x.ListMemoryRecordsInput.memoryId)
      | ListPaymentInstruments ->
          (Format.kasprintf Uri.of_string) "/payments/listPaymentInstruments"
      | ListPaymentSessions ->
          (Format.kasprintf Uri.of_string) "/payments/listPaymentSessions"
      | ListRecommendations ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/recommendations")
            (List.filter_opt
               [Option.map
                  ~f:(fun v ->
                        ("maxResults",
                          (ListRecommendationsRequestMaxResultsInteger.to_header
                             v))) x.maxResults;
               Option.map
                 ~f:(fun v -> ("nextToken", (NextToken.to_header v)))
                 x.nextToken;
               Option.map
                 ~f:(fun v -> ("status", (RecommendationStatus.to_header v)))
                 x.statusFilter])
      | ListSessions ->
          (Format.kasprintf Uri.of_string) "/memories/%s/actor/%s/sessions"
            (MemoryId.to_header x.ListSessionsInput.memoryId)
            (ActorId.to_header x.ListSessionsInput.actorId)
      | ProcessPayment ->
          (Format.kasprintf Uri.of_string) "/payments/processPayment"
      | RetrieveMemoryRecords ->
          (Format.kasprintf Uri.of_string) "/memories/%s/retrieve"
            (MemoryId.to_header x.RetrieveMemoryRecordsInput.memoryId)
      | SaveBrowserSessionProfile ->
          (Format.kasprintf Uri.of_string) "/browser-profiles/%s/save"
            (BrowserProfileId.to_header
               x.SaveBrowserSessionProfileRequest.profileIdentifier)
      | SearchRegistryRecords ->
          (Format.kasprintf Uri.of_string) "/registry-records/search"
      | StartBatchEvaluation ->
          (Format.kasprintf Uri.of_string) "/evaluations/batch-evaluate"
      | StartBrowserSession ->
          (Format.kasprintf Uri.of_string) "/browsers/%s/sessions/start"
            (String_.to_header x.StartBrowserSessionRequest.browserIdentifier)
      | StartCodeInterpreterSession ->
          (Format.kasprintf Uri.of_string)
            "/code-interpreters/%s/sessions/start"
            (String_.to_header
               x.StartCodeInterpreterSessionRequest.codeInterpreterIdentifier)
      | StartMemoryExtractionJob ->
          (Format.kasprintf Uri.of_string)
            "/memories/%s/extractionJobs/start"
            (MemoryId.to_header x.StartMemoryExtractionJobInput.memoryId)
      | StartRecommendation ->
          (Format.kasprintf Uri.of_string) "/recommendations"
      | StopBatchEvaluation ->
          (Format.kasprintf Uri.of_string)
            "/evaluations/batch-evaluate/%s/stop"
            (BatchEvaluationId.to_header
               x.StopBatchEvaluationRequest.batchEvaluationId)
      | StopBrowserSession ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/browsers/%s/sessions/stop"
               (String_.to_header
                  x.StopBrowserSessionRequest.browserIdentifier))
            (List.filter_opt
               [Some ("sessionId", (BrowserSessionId.to_header x.sessionId))])
      | StopCodeInterpreterSession ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string)
               "/code-interpreters/%s/sessions/stop"
               (String_.to_header
                  x.StopCodeInterpreterSessionRequest.codeInterpreterIdentifier))
            (List.filter_opt
               [Some
                  ("sessionId",
                    (CodeInterpreterSessionId.to_header x.sessionId))])
      | StopRuntimeSession ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string)
               "/runtimes/%s/stopruntimesession"
               (String_.to_header x.StopRuntimeSessionRequest.agentRuntimeArn))
            (List.filter_opt
               [Option.map ~f:(fun v -> ("qualifier", (String_.to_header v)))
                  x.qualifier])
      | UpdateABTest ->
          (Format.kasprintf Uri.of_string) "/ab-tests/%s"
            (ABTestId.to_header x.UpdateABTestRequest.abTestId)
      | UpdateBrowserStream ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string)
               "/browsers/%s/sessions/streams/update"
               (String_.to_header
                  x.UpdateBrowserStreamRequest.browserIdentifier))
            (List.filter_opt
               [Some ("sessionId", (BrowserSessionId.to_header x.sessionId))]))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | BatchCreateMemoryRecords ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("records",
                           (MemoryRecordsCreateInputList.to_value
                              req.BatchCreateMemoryRecordsInput.records));
                      Option.map
                        req.BatchCreateMemoryRecordsInput.clientToken
                        ~f:(fun x -> ("clientToken", (String_.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | BatchDeleteMemoryRecords ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("records",
                           (MemoryRecordsDeleteInputList.to_value
                              req.BatchDeleteMemoryRecordsInput.records))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | BatchUpdateMemoryRecords ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("records",
                           (MemoryRecordsUpdateInputList.to_value
                              req.BatchUpdateMemoryRecordsInput.records))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CompleteResourceTokenAuth ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("userIdentifier",
                           (UserIdentifier.to_value
                              req.CompleteResourceTokenAuthRequest.userIdentifier));
                      Some
                        ("sessionUri",
                          (RequestUri.to_value
                             req.CompleteResourceTokenAuthRequest.sessionUri))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CreateABTest ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("name",
                           (ABTestName.to_value req.CreateABTestRequest.name));
                      Option.map req.CreateABTestRequest.description
                        ~f:(fun x ->
                              ("description", (ABTestDescription.to_value x)));
                      Some
                        ("gatewayArn",
                          (GatewayArn.to_value
                             req.CreateABTestRequest.gatewayArn));
                      Some
                        ("variants",
                          (VariantList.to_value
                             req.CreateABTestRequest.variants));
                      Option.map req.CreateABTestRequest.gatewayFilter
                        ~f:(fun x ->
                              ("gatewayFilter", (GatewayFilter.to_value x)));
                      Some
                        ("evaluationConfig",
                          (ABTestEvaluationConfig.to_value
                             req.CreateABTestRequest.evaluationConfig));
                      Some
                        ("roleArn",
                          (RoleArn.to_value req.CreateABTestRequest.roleArn));
                      Option.map req.CreateABTestRequest.enableOnCreate
                        ~f:(fun x -> ("enableOnCreate", (Boolean.to_value x)));
                      Option.map req.CreateABTestRequest.clientToken
                        ~f:(fun x ->
                              ("clientToken", (ClientToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CreateEvent ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("actorId",
                           (ActorId.to_value req.CreateEventInput.actorId));
                      Option.map req.CreateEventInput.sessionId
                        ~f:(fun x -> ("sessionId", (SessionId.to_value x)));
                      Some
                        ("eventTimestamp",
                          (Timestamp.to_value
                             req.CreateEventInput.eventTimestamp));
                      Some
                        ("payload",
                          (PayloadTypeList.to_value
                             req.CreateEventInput.payload));
                      Option.map req.CreateEventInput.branch
                        ~f:(fun x -> ("branch", (Branch.to_value x)));
                      Option.map req.CreateEventInput.clientToken
                        ~f:(fun x -> ("clientToken", (String_.to_value x)));
                      Option.map req.CreateEventInput.metadata
                        ~f:(fun x -> ("metadata", (MetadataMap.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CreatePaymentInstrument ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Option.map req.CreatePaymentInstrumentRequest.userId
                   ~f:(fun x ->
                         ("X-Amzn-Bedrock-AgentCore-Payments-User-Id",
                           (UserId.to_header x)));
                Option.map req.CreatePaymentInstrumentRequest.agentName
                  ~f:(fun x ->
                        ("X-Amzn-Bedrock-AgentCore-Payments-Agent-Name",
                          (PaymentAgentName.to_header x)))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("paymentManagerArn",
                           (PaymentManagerArn.to_value
                              req.CreatePaymentInstrumentRequest.paymentManagerArn));
                      Some
                        ("paymentConnectorId",
                          (PaymentConnectorId.to_value
                             req.CreatePaymentInstrumentRequest.paymentConnectorId));
                      Some
                        ("paymentInstrumentType",
                          (PaymentInstrumentType.to_value
                             req.CreatePaymentInstrumentRequest.paymentInstrumentType));
                      Some
                        ("paymentInstrumentDetails",
                          (PaymentInstrumentDetails.to_value
                             req.CreatePaymentInstrumentRequest.paymentInstrumentDetails));
                      Option.map
                        req.CreatePaymentInstrumentRequest.clientToken
                        ~f:(fun x ->
                              ("clientToken", (ClientToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | CreatePaymentSession ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Option.map req.CreatePaymentSessionRequest.userId
                   ~f:(fun x ->
                         ("X-Amzn-Bedrock-AgentCore-Payments-User-Id",
                           (UserId.to_header x)));
                Option.map req.CreatePaymentSessionRequest.agentName
                  ~f:(fun x ->
                        ("X-Amzn-Bedrock-AgentCore-Payments-Agent-Name",
                          (PaymentAgentName.to_header x)))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("paymentManagerArn",
                           (PaymentManagerArn.to_value
                              req.CreatePaymentSessionRequest.paymentManagerArn));
                      Option.map req.CreatePaymentSessionRequest.limits
                        ~f:(fun x -> ("limits", (SessionLimits.to_value x)));
                      Some
                        ("expiryTimeInMinutes",
                          (CreatePaymentSessionRequestExpiryTimeInMinutesInteger.to_value
                             req.CreatePaymentSessionRequest.expiryTimeInMinutes));
                      Option.map req.CreatePaymentSessionRequest.clientToken
                        ~f:(fun x ->
                              ("clientToken", (ClientToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteABTest -> Awso.Http.Request.make (method_of_endpoint endp)
  | DeleteBatchEvaluation -> Awso.Http.Request.make (method_of_endpoint endp)
  | DeleteEvent -> Awso.Http.Request.make (method_of_endpoint endp)
  | DeleteMemoryRecord -> Awso.Http.Request.make (method_of_endpoint endp)
  | DeletePaymentInstrument ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Option.map req.DeletePaymentInstrumentRequest.userId
                   ~f:(fun x ->
                         ("X-Amzn-Bedrock-AgentCore-Payments-User-Id",
                           (UserId.to_header x)))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("paymentManagerArn",
                           (PaymentManagerArn.to_value
                              req.DeletePaymentInstrumentRequest.paymentManagerArn));
                      Some
                        ("paymentConnectorId",
                          (PaymentConnectorId.to_value
                             req.DeletePaymentInstrumentRequest.paymentConnectorId));
                      Some
                        ("paymentInstrumentId",
                          (PaymentInstrumentId.to_value
                             req.DeletePaymentInstrumentRequest.paymentInstrumentId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeletePaymentSession ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Option.map req.DeletePaymentSessionRequest.userId
                   ~f:(fun x ->
                         ("X-Amzn-Bedrock-AgentCore-Payments-User-Id",
                           (UserId.to_header x)))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("paymentManagerArn",
                           (PaymentManagerArn.to_value
                              req.DeletePaymentSessionRequest.paymentManagerArn));
                      Some
                        ("paymentSessionId",
                          (PaymentSessionId.to_value
                             req.DeletePaymentSessionRequest.paymentSessionId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteRecommendation -> Awso.Http.Request.make (method_of_endpoint endp)
  | Evaluate ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("evaluationInput",
                           (EvaluationInput.to_value
                              req.EvaluateRequest.evaluationInput));
                      Option.map req.EvaluateRequest.evaluationTarget
                        ~f:(fun x ->
                              ("evaluationTarget",
                                (EvaluationTarget.to_value x)));
                      Option.map
                        req.EvaluateRequest.evaluationReferenceInputs
                        ~f:(fun x ->
                              ("evaluationReferenceInputs",
                                (EvaluationReferenceInputs.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetABTest ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetAgentCard ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Option.map req.GetAgentCardRequest.runtimeSessionId
                   ~f:(fun x ->
                         ("X-Amzn-Bedrock-AgentCore-Runtime-Session-Id",
                           (SessionType.to_header x)))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map (List.filter_opt [])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetBatchEvaluation ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetBrowserSession ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetCodeInterpreterSession ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetEvent ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetMemoryRecord ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetPaymentInstrument ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Option.map req.GetPaymentInstrumentRequest.userId
                   ~f:(fun x ->
                         ("X-Amzn-Bedrock-AgentCore-Payments-User-Id",
                           (UserId.to_header x)));
                Option.map req.GetPaymentInstrumentRequest.agentName
                  ~f:(fun x ->
                        ("X-Amzn-Bedrock-AgentCore-Payments-Agent-Name",
                          (PaymentAgentName.to_header x)))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("paymentManagerArn",
                           (PaymentManagerArn.to_value
                              req.GetPaymentInstrumentRequest.paymentManagerArn));
                      Option.map
                        req.GetPaymentInstrumentRequest.paymentConnectorId
                        ~f:(fun x ->
                              ("paymentConnectorId",
                                (PaymentConnectorId.to_value x)));
                      Some
                        ("paymentInstrumentId",
                          (PaymentInstrumentId.to_value
                             req.GetPaymentInstrumentRequest.paymentInstrumentId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetPaymentInstrumentBalance ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Option.map req.GetPaymentInstrumentBalanceRequest.userId
                   ~f:(fun x ->
                         ("X-Amzn-Bedrock-AgentCore-Payments-User-Id",
                           (UserId.to_header x)));
                Option.map req.GetPaymentInstrumentBalanceRequest.agentName
                  ~f:(fun x ->
                        ("X-Amzn-Bedrock-AgentCore-Payments-Agent-Name",
                          (PaymentAgentName.to_header x)))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("paymentManagerArn",
                           (PaymentManagerArn.to_value
                              req.GetPaymentInstrumentBalanceRequest.paymentManagerArn));
                      Some
                        ("paymentConnectorId",
                          (PaymentConnectorId.to_value
                             req.GetPaymentInstrumentBalanceRequest.paymentConnectorId));
                      Some
                        ("paymentInstrumentId",
                          (PaymentInstrumentId.to_value
                             req.GetPaymentInstrumentBalanceRequest.paymentInstrumentId));
                      Some
                        ("chain",
                          (BlockchainChainId.to_value
                             req.GetPaymentInstrumentBalanceRequest.chain));
                      Some
                        ("token",
                          (InstrumentBalanceToken.to_value
                             req.GetPaymentInstrumentBalanceRequest.token))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetPaymentSession ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Option.map req.GetPaymentSessionRequest.userId
                   ~f:(fun x ->
                         ("X-Amzn-Bedrock-AgentCore-Payments-User-Id",
                           (UserId.to_header x)));
                Option.map req.GetPaymentSessionRequest.agentName
                  ~f:(fun x ->
                        ("X-Amzn-Bedrock-AgentCore-Payments-Agent-Name",
                          (PaymentAgentName.to_header x)))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("paymentManagerArn",
                           (PaymentManagerArn.to_value
                              req.GetPaymentSessionRequest.paymentManagerArn));
                      Some
                        ("paymentSessionId",
                          (PaymentSessionId.to_value
                             req.GetPaymentSessionRequest.paymentSessionId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetRecommendation ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetResourceApiKey ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("workloadIdentityToken",
                           (WorkloadIdentityTokenType.to_value
                              req.GetResourceApiKeyRequest.workloadIdentityToken));
                      Some
                        ("resourceCredentialProviderName",
                          (CredentialProviderName.to_value
                             req.GetResourceApiKeyRequest.resourceCredentialProviderName))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetResourceOauth2Token ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("workloadIdentityToken",
                           (WorkloadIdentityTokenType.to_value
                              req.GetResourceOauth2TokenRequest.workloadIdentityToken));
                      Some
                        ("resourceCredentialProviderName",
                          (CredentialProviderName.to_value
                             req.GetResourceOauth2TokenRequest.resourceCredentialProviderName));
                      Some
                        ("scopes",
                          (ScopesListType.to_value
                             req.GetResourceOauth2TokenRequest.scopes));
                      Some
                        ("oauth2Flow",
                          (Oauth2FlowType.to_value
                             req.GetResourceOauth2TokenRequest.oauth2Flow));
                      Option.map req.GetResourceOauth2TokenRequest.sessionUri
                        ~f:(fun x -> ("sessionUri", (RequestUri.to_value x)));
                      Option.map
                        req.GetResourceOauth2TokenRequest.resourceOauth2ReturnUrl
                        ~f:(fun x ->
                              ("resourceOauth2ReturnUrl",
                                (ResourceOauth2ReturnUrlType.to_value x)));
                      Option.map
                        req.GetResourceOauth2TokenRequest.forceAuthentication
                        ~f:(fun x ->
                              ("forceAuthentication", (Boolean.to_value x)));
                      Option.map
                        req.GetResourceOauth2TokenRequest.customParameters
                        ~f:(fun x ->
                              ("customParameters",
                                (CustomRequestParametersType.to_value x)));
                      Option.map
                        req.GetResourceOauth2TokenRequest.customState
                        ~f:(fun x -> ("customState", (State.to_value x)));
                      Option.map req.GetResourceOauth2TokenRequest.resources
                        ~f:(fun x ->
                              ("resources", (ResourcesListType.to_value x)));
                      Option.map req.GetResourceOauth2TokenRequest.audiences
                        ~f:(fun x ->
                              ("audiences", (AudiencesListType.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetResourcePaymentToken ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("workloadIdentityToken",
                           (WorkloadIdentityTokenType.to_value
                              req.GetResourcePaymentTokenRequest.workloadIdentityToken));
                      Some
                        ("resourceCredentialProviderName",
                          (CredentialProviderName.to_value
                             req.GetResourcePaymentTokenRequest.resourceCredentialProviderName));
                      Some
                        ("paymentTokenRequest",
                          (PaymentTokenRequestInput.to_value
                             req.GetResourcePaymentTokenRequest.paymentTokenRequest))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetWorkloadAccessToken ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("workloadName",
                           (WorkloadIdentityNameType.to_value
                              req.GetWorkloadAccessTokenRequest.workloadName))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetWorkloadAccessTokenForJWT ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("workloadName",
                           (WorkloadIdentityNameType.to_value
                              req.GetWorkloadAccessTokenForJWTRequest.workloadName));
                      Some
                        ("userToken",
                          (UserTokenType.to_value
                             req.GetWorkloadAccessTokenForJWTRequest.userToken))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | GetWorkloadAccessTokenForUserId ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("workloadName",
                           (WorkloadIdentityNameType.to_value
                              req.GetWorkloadAccessTokenForUserIdRequest.workloadName));
                      Some
                        ("userId",
                          (UserIdType.to_value
                             req.GetWorkloadAccessTokenForUserIdRequest.userId))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | InvokeAgentRuntime ->
      let headers =
        Some
          ((List.filter_opt
              [Option.map req.InvokeAgentRuntimeRequest.contentType
                 ~f:(fun x -> ("Content-Type", (MimeType.to_header x)));
              Option.map req.InvokeAgentRuntimeRequest.accept
                ~f:(fun x -> ("Accept", (MimeType.to_header x)));
              Option.map req.InvokeAgentRuntimeRequest.mcpSessionId
                ~f:(fun x -> ("Mcp-Session-Id", (StringType.to_header x)));
              Option.map req.InvokeAgentRuntimeRequest.runtimeSessionId
                ~f:(fun x ->
                      ("X-Amzn-Bedrock-AgentCore-Runtime-Session-Id",
                        (SessionType.to_header x)));
              Option.map req.InvokeAgentRuntimeRequest.mcpProtocolVersion
                ~f:(fun x ->
                      ("Mcp-Protocol-Version", (StringType.to_header x)));
              Option.map req.InvokeAgentRuntimeRequest.runtimeUserId
                ~f:(fun x ->
                      ("X-Amzn-Bedrock-AgentCore-Runtime-User-Id",
                        (StringType.to_header x)));
              Option.map req.InvokeAgentRuntimeRequest.traceId
                ~f:(fun x ->
                      ("X-Amzn-Trace-Id",
                        (InvokeAgentRuntimeRequestTraceIdString.to_header x)));
              Option.map req.InvokeAgentRuntimeRequest.traceParent
                ~f:(fun x ->
                      ("traceparent",
                        (InvokeAgentRuntimeRequestTraceParentString.to_header
                           x)));
              Option.map req.InvokeAgentRuntimeRequest.traceState
                ~f:(fun x ->
                      ("tracestate",
                        (InvokeAgentRuntimeRequestTraceStateString.to_header
                           x)));
              Option.map req.InvokeAgentRuntimeRequest.baggage
                ~f:(fun x ->
                      ("baggage",
                        (InvokeAgentRuntimeRequestBaggageString.to_header x)))])
             |> Awso.Http.Headers.of_list) in
      let body = Body.to_header req.payload in
      Awso.Http.Request.make ?headers ~body (method_of_endpoint endp)
  | InvokeAgentRuntimeCommand ->
      let headers =
        Some
          ((List.filter_opt
              [Option.map req.InvokeAgentRuntimeCommandRequest.contentType
                 ~f:(fun x -> ("Content-Type", (MimeType.to_header x)));
              Option.map req.InvokeAgentRuntimeCommandRequest.accept
                ~f:(fun x -> ("Accept", (MimeType.to_header x)));
              Option.map
                req.InvokeAgentRuntimeCommandRequest.runtimeSessionId
                ~f:(fun x ->
                      ("X-Amzn-Bedrock-AgentCore-Runtime-Session-Id",
                        (SessionType.to_header x)));
              Option.map req.InvokeAgentRuntimeCommandRequest.traceId
                ~f:(fun x ->
                      ("X-Amzn-Trace-Id",
                        (InvokeAgentRuntimeCommandRequestTraceIdString.to_header
                           x)));
              Option.map req.InvokeAgentRuntimeCommandRequest.traceParent
                ~f:(fun x ->
                      ("traceparent",
                        (InvokeAgentRuntimeCommandRequestTraceParentString.to_header
                           x)));
              Option.map req.InvokeAgentRuntimeCommandRequest.traceState
                ~f:(fun x ->
                      ("tracestate",
                        (InvokeAgentRuntimeCommandRequestTraceStateString.to_header
                           x)));
              Option.map req.InvokeAgentRuntimeCommandRequest.baggage
                ~f:(fun x ->
                      ("baggage",
                        (InvokeAgentRuntimeCommandRequestBaggageString.to_header
                           x)))])
             |> Awso.Http.Headers.of_list) in
      let body =
        (fun param ->
           ((param |> InvokeAgentRuntimeCommandRequestBody.to_value) |>
              Awso.Botodata.Json.value_to_json)
             |> Yojson.Safe.to_string) req.body in
      Awso.Http.Request.make ?headers ~body (method_of_endpoint endp)
  | InvokeBrowser ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Some
                   ("x-amzn-browser-session-id",
                     (BrowserSessionId.to_header
                        req.InvokeBrowserRequest.sessionId))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("action",
                           (BrowserAction.to_value
                              req.InvokeBrowserRequest.action))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | InvokeCodeInterpreter ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Option.map req.InvokeCodeInterpreterRequest.sessionId
                   ~f:(fun x ->
                         ("x-amzn-code-interpreter-session-id",
                           (CodeInterpreterSessionId.to_header x)));
                Option.map req.InvokeCodeInterpreterRequest.traceId
                  ~f:(fun x ->
                        ("X-Amzn-Trace-Id",
                          (InvokeCodeInterpreterRequestTraceIdString.to_header
                             x)));
                Option.map req.InvokeCodeInterpreterRequest.traceParent
                  ~f:(fun x ->
                        ("traceparent",
                          (InvokeCodeInterpreterRequestTraceParentString.to_header
                             x)))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("name",
                           (ToolName.to_value
                              req.InvokeCodeInterpreterRequest.name));
                      Option.map req.InvokeCodeInterpreterRequest.arguments
                        ~f:(fun x ->
                              ("arguments", (ToolArguments.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | InvokeHarness ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Some
                   ("X-Amzn-Bedrock-AgentCore-Runtime-Session-Id",
                     (SessionId.to_header
                        req.InvokeHarnessRequest.runtimeSessionId))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("messages",
                           (HarnessMessages.to_value
                              req.InvokeHarnessRequest.messages));
                      Option.map req.InvokeHarnessRequest.model
                        ~f:(fun x ->
                              ("model",
                                (HarnessModelConfiguration.to_value x)));
                      Option.map req.InvokeHarnessRequest.systemPrompt
                        ~f:(fun x ->
                              ("systemPrompt",
                                (HarnessSystemPrompt.to_value x)));
                      Option.map req.InvokeHarnessRequest.tools
                        ~f:(fun x -> ("tools", (HarnessTools.to_value x)));
                      Option.map req.InvokeHarnessRequest.skills
                        ~f:(fun x -> ("skills", (HarnessSkills.to_value x)));
                      Option.map req.InvokeHarnessRequest.allowedTools
                        ~f:(fun x ->
                              ("allowedTools",
                                (HarnessAllowedTools.to_value x)));
                      Option.map req.InvokeHarnessRequest.maxIterations
                        ~f:(fun x -> ("maxIterations", (Integer.to_value x)));
                      Option.map req.InvokeHarnessRequest.maxTokens
                        ~f:(fun x -> ("maxTokens", (Integer.to_value x)));
                      Option.map req.InvokeHarnessRequest.timeoutSeconds
                        ~f:(fun x -> ("timeoutSeconds", (Integer.to_value x)));
                      Option.map req.InvokeHarnessRequest.actorId
                        ~f:(fun x -> ("actorId", (String_.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListABTests ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListActors ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListActorsInput.maxResults
                         ~f:(fun x -> ("maxResults", (MaxResults.to_value x)));
                      Option.map req.ListActorsInput.nextToken
                        ~f:(fun x ->
                              ("nextToken", (PaginationToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListBatchEvaluations ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListBrowserSessions ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListBrowserSessionsRequest.maxResults
                         ~f:(fun x -> ("maxResults", (MaxResults.to_value x)));
                      Option.map req.ListBrowserSessionsRequest.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)));
                      Option.map req.ListBrowserSessionsRequest.status
                        ~f:(fun x ->
                              ("status", (BrowserSessionStatus.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListCodeInterpreterSessions ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.ListCodeInterpreterSessionsRequest.maxResults
                         ~f:(fun x -> ("maxResults", (MaxResults.to_value x)));
                      Option.map
                        req.ListCodeInterpreterSessionsRequest.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)));
                      Option.map
                        req.ListCodeInterpreterSessionsRequest.status
                        ~f:(fun x ->
                              ("status",
                                (CodeInterpreterSessionStatus.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListEvents ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListEventsInput.includePayloads
                         ~f:(fun x ->
                               ("includePayloads", (Boolean.to_value x)));
                      Option.map req.ListEventsInput.filter
                        ~f:(fun x -> ("filter", (FilterInput.to_value x)));
                      Option.map req.ListEventsInput.maxResults
                        ~f:(fun x -> ("maxResults", (MaxResults.to_value x)));
                      Option.map req.ListEventsInput.nextToken
                        ~f:(fun x ->
                              ("nextToken", (PaginationToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListMemoryExtractionJobs ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map
                         req.ListMemoryExtractionJobsInput.maxResults
                         ~f:(fun x ->
                               ("maxResults",
                                 (ListMemoryExtractionJobsInputMaxResultsInteger.to_value
                                    x)));
                      Option.map req.ListMemoryExtractionJobsInput.filter
                        ~f:(fun x ->
                              ("filter",
                                (ExtractionJobFilterInput.to_value x)));
                      Option.map req.ListMemoryExtractionJobsInput.nextToken
                        ~f:(fun x ->
                              ("nextToken", (PaginationToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListMemoryRecords ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListMemoryRecordsInput.namespace
                         ~f:(fun x -> ("namespace", (Namespace.to_value x)));
                      Option.map req.ListMemoryRecordsInput.namespacePath
                        ~f:(fun x ->
                              ("namespacePath", (Namespace.to_value x)));
                      Option.map req.ListMemoryRecordsInput.memoryStrategyId
                        ~f:(fun x ->
                              ("memoryStrategyId",
                                (MemoryStrategyId.to_value x)));
                      Option.map req.ListMemoryRecordsInput.maxResults
                        ~f:(fun x -> ("maxResults", (MaxResults.to_value x)));
                      Option.map req.ListMemoryRecordsInput.nextToken
                        ~f:(fun x ->
                              ("nextToken", (PaginationToken.to_value x)));
                      Option.map req.ListMemoryRecordsInput.metadataFilters
                        ~f:(fun x ->
                              ("metadataFilters",
                                (MemoryMetadataFilterList.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListPaymentInstruments ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Option.map req.ListPaymentInstrumentsRequest.userId
                   ~f:(fun x ->
                         ("X-Amzn-Bedrock-AgentCore-Payments-User-Id",
                           (UserId.to_header x)));
                Option.map req.ListPaymentInstrumentsRequest.agentName
                  ~f:(fun x ->
                        ("X-Amzn-Bedrock-AgentCore-Payments-Agent-Name",
                          (PaymentAgentName.to_header x)))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("paymentManagerArn",
                           (PaymentManagerArn.to_value
                              req.ListPaymentInstrumentsRequest.paymentManagerArn));
                      Option.map
                        req.ListPaymentInstrumentsRequest.paymentConnectorId
                        ~f:(fun x ->
                              ("paymentConnectorId",
                                (PaymentConnectorId.to_value x)));
                      Option.map req.ListPaymentInstrumentsRequest.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)));
                      Option.map req.ListPaymentInstrumentsRequest.maxResults
                        ~f:(fun x -> ("maxResults", (Integer.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListPaymentSessions ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Option.map req.ListPaymentSessionsRequest.userId
                   ~f:(fun x ->
                         ("X-Amzn-Bedrock-AgentCore-Payments-User-Id",
                           (UserId.to_header x)));
                Option.map req.ListPaymentSessionsRequest.agentName
                  ~f:(fun x ->
                        ("X-Amzn-Bedrock-AgentCore-Payments-Agent-Name",
                          (PaymentAgentName.to_header x)))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("paymentManagerArn",
                           (PaymentManagerArn.to_value
                              req.ListPaymentSessionsRequest.paymentManagerArn));
                      Option.map req.ListPaymentSessionsRequest.nextToken
                        ~f:(fun x -> ("nextToken", (NextToken.to_value x)));
                      Option.map req.ListPaymentSessionsRequest.maxResults
                        ~f:(fun x -> ("maxResults", (Integer.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListRecommendations ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListSessions ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.ListSessionsInput.maxResults
                         ~f:(fun x -> ("maxResults", (MaxResults.to_value x)));
                      Option.map req.ListSessionsInput.nextToken
                        ~f:(fun x ->
                              ("nextToken", (PaginationToken.to_value x)));
                      Option.map req.ListSessionsInput.filter
                        ~f:(fun x -> ("filter", (SessionFilter.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ProcessPayment ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Option.map req.ProcessPaymentRequest.userId
                   ~f:(fun x ->
                         ("X-Amzn-Bedrock-AgentCore-Payments-User-Id",
                           (UserId.to_header x)));
                Option.map req.ProcessPaymentRequest.agentName
                  ~f:(fun x ->
                        ("X-Amzn-Bedrock-AgentCore-Payments-Agent-Name",
                          (PaymentAgentName.to_header x)))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("paymentManagerArn",
                           (PaymentManagerArn.to_value
                              req.ProcessPaymentRequest.paymentManagerArn));
                      Some
                        ("paymentSessionId",
                          (PaymentSessionId.to_value
                             req.ProcessPaymentRequest.paymentSessionId));
                      Some
                        ("paymentInstrumentId",
                          (PaymentInstrumentId.to_value
                             req.ProcessPaymentRequest.paymentInstrumentId));
                      Some
                        ("paymentType",
                          (PaymentType.to_value
                             req.ProcessPaymentRequest.paymentType));
                      Some
                        ("paymentInput",
                          (PaymentInput.to_value
                             req.ProcessPaymentRequest.paymentInput));
                      Option.map req.ProcessPaymentRequest.clientToken
                        ~f:(fun x ->
                              ("clientToken", (ClientToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | RetrieveMemoryRecords ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.RetrieveMemoryRecordsInput.namespace
                         ~f:(fun x -> ("namespace", (Namespace.to_value x)));
                      Option.map req.RetrieveMemoryRecordsInput.namespacePath
                        ~f:(fun x ->
                              ("namespacePath", (Namespace.to_value x)));
                      Some
                        ("searchCriteria",
                          (SearchCriteria.to_value
                             req.RetrieveMemoryRecordsInput.searchCriteria));
                      Option.map req.RetrieveMemoryRecordsInput.nextToken
                        ~f:(fun x ->
                              ("nextToken", (PaginationToken.to_value x)));
                      Option.map req.RetrieveMemoryRecordsInput.maxResults
                        ~f:(fun x -> ("maxResults", (MaxResults.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | SaveBrowserSessionProfile ->
      Awso.Http.Request.make (method_of_endpoint endp)
  | SearchRegistryRecords ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("searchQuery",
                           (SearchRegistryRecordsRequestSearchQueryString.to_value
                              req.SearchRegistryRecordsRequest.searchQuery));
                      Some
                        ("registryIds",
                          (SearchRegistryRecordsRequestRegistryIdsList.to_value
                             req.SearchRegistryRecordsRequest.registryIds));
                      Option.map req.SearchRegistryRecordsRequest.maxResults
                        ~f:(fun x ->
                              ("maxResults",
                                (SearchRegistryRecordsRequestMaxResultsInteger.to_value
                                   x)));
                      Option.map req.SearchRegistryRecordsRequest.filters
                        ~f:(fun x ->
                              ("filters",
                                (MetadataFilterExpression.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StartBatchEvaluation ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("batchEvaluationName",
                           (BatchEvaluationName.to_value
                              req.StartBatchEvaluationRequest.batchEvaluationName));
                      Option.map req.StartBatchEvaluationRequest.evaluators
                        ~f:(fun x ->
                              ("evaluators",
                                (StartBatchEvaluationRequestEvaluatorsList.to_value
                                   x)));
                      Some
                        ("dataSourceConfig",
                          (DataSourceConfig.to_value
                             req.StartBatchEvaluationRequest.dataSourceConfig));
                      Option.map req.StartBatchEvaluationRequest.clientToken
                        ~f:(fun x ->
                              ("clientToken", (ClientToken.to_value x)));
                      Option.map
                        req.StartBatchEvaluationRequest.evaluationMetadata
                        ~f:(fun x ->
                              ("evaluationMetadata",
                                (EvaluationMetadata.to_value x)));
                      Option.map req.StartBatchEvaluationRequest.description
                        ~f:(fun x ->
                              ("description",
                                (BatchEvaluationDescription.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StartBrowserSession -> Awso.Http.Request.make (method_of_endpoint endp)
  | StartCodeInterpreterSession ->
      Awso.Http.Request.make (method_of_endpoint endp)
  | StartMemoryExtractionJob ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("extractionJob",
                           (ExtractionJob.to_value
                              req.StartMemoryExtractionJobInput.extractionJob));
                      Option.map
                        req.StartMemoryExtractionJobInput.clientToken
                        ~f:(fun x -> ("clientToken", (String_.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StartRecommendation ->
      let (headers, body) =
        let headers =
          Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Some
                         ("name",
                           (RecommendationName.to_value
                              req.StartRecommendationRequest.name));
                      Option.map req.StartRecommendationRequest.description
                        ~f:(fun x ->
                              ("description",
                                (RecommendationDescription.to_value x)));
                      Some
                        ("type",
                          (RecommendationType.to_value
                             req.StartRecommendationRequest.type_));
                      Some
                        ("recommendationConfig",
                          (RecommendationConfig.to_value
                             req.StartRecommendationRequest.recommendationConfig));
                      Option.map req.StartRecommendationRequest.clientToken
                        ~f:(fun x ->
                              ("clientToken", (ClientToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StopBatchEvaluation ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | StopBrowserSession -> Awso.Http.Request.make (method_of_endpoint endp)
  | StopCodeInterpreterSession ->
      Awso.Http.Request.make (method_of_endpoint endp)
  | StopRuntimeSession ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Some
                   ("X-Amzn-Bedrock-AgentCore-Runtime-Session-Id",
                     (SessionType.to_header
                        req.StopRuntimeSessionRequest.runtimeSessionId))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map
                   (List.filter_opt
                      [Option.map req.StopRuntimeSessionRequest.clientToken
                         ~f:(fun x ->
                               ("clientToken", (ClientToken.to_value x)))])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UpdateABTest -> Awso.Http.Request.make (method_of_endpoint endp)
  | UpdateBrowserStream -> Awso.Http.Request.make (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | BatchCreateMemoryRecords ->
      if is_success
      then
        Ok (BatchCreateMemoryRecordsOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some BatchCreateMemoryRecordsOutput.error_of_json))
  | BatchDeleteMemoryRecords ->
      if is_success
      then
        Ok (BatchDeleteMemoryRecordsOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some BatchDeleteMemoryRecordsOutput.error_of_json))
  | BatchUpdateMemoryRecords ->
      if is_success
      then
        Ok (BatchUpdateMemoryRecordsOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some BatchUpdateMemoryRecordsOutput.error_of_json))
  | CompleteResourceTokenAuth ->
      if is_success
      then
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok
          (CompleteResourceTokenAuthResponse.of_header_and_body (headers, ()))
      else
        Error
          (parse_aws_error
             (Some CompleteResourceTokenAuthResponse.error_of_json))
  | CreateABTest ->
      if is_success
      then Ok (CreateABTestResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some CreateABTestResponse.error_of_json))
  | CreateEvent ->
      if is_success
      then Ok (CreateEventOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some CreateEventOutput.error_of_json))
  | CreatePaymentInstrument ->
      if is_success
      then
        Ok (CreatePaymentInstrumentResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some CreatePaymentInstrumentResponse.error_of_json))
  | CreatePaymentSession ->
      if is_success
      then Ok (CreatePaymentSessionResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some CreatePaymentSessionResponse.error_of_json))
  | DeleteABTest ->
      if is_success
      then Ok (DeleteABTestResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some DeleteABTestResponse.error_of_json))
  | DeleteBatchEvaluation ->
      if is_success
      then Ok (DeleteBatchEvaluationResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some DeleteBatchEvaluationResponse.error_of_json))
  | DeleteEvent ->
      if is_success
      then Ok (DeleteEventOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some DeleteEventOutput.error_of_json))
  | DeleteMemoryRecord ->
      if is_success
      then Ok (DeleteMemoryRecordOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some DeleteMemoryRecordOutput.error_of_json))
  | DeletePaymentInstrument ->
      if is_success
      then
        Ok (DeletePaymentInstrumentResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some DeletePaymentInstrumentResponse.error_of_json))
  | DeletePaymentSession ->
      if is_success
      then Ok (DeletePaymentSessionResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some DeletePaymentSessionResponse.error_of_json))
  | DeleteRecommendation ->
      if is_success
      then Ok (DeleteRecommendationResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some DeleteRecommendationResponse.error_of_json))
  | Evaluate ->
      if is_success
      then Ok (EvaluateResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some EvaluateResponse.error_of_json))
  | GetABTest ->
      if is_success
      then Ok (GetABTestResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetABTestResponse.error_of_json))
  | GetAgentCard ->
      if is_success
      then
        let body = AgentCard.of_string (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (GetAgentCardResponse.of_header_and_body (headers, body))
      else Error (parse_aws_error (Some GetAgentCardResponse.error_of_json))
  | GetBatchEvaluation ->
      if is_success
      then Ok (GetBatchEvaluationResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some GetBatchEvaluationResponse.error_of_json))
  | GetBrowserSession ->
      if is_success
      then Ok (GetBrowserSessionResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some GetBrowserSessionResponse.error_of_json))
  | GetCodeInterpreterSession ->
      if is_success
      then
        Ok
          (GetCodeInterpreterSessionResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some GetCodeInterpreterSessionResponse.error_of_json))
  | GetEvent ->
      if is_success
      then Ok (GetEventOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetEventOutput.error_of_json))
  | GetMemoryRecord ->
      if is_success
      then Ok (GetMemoryRecordOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some GetMemoryRecordOutput.error_of_json))
  | GetPaymentInstrument ->
      if is_success
      then Ok (GetPaymentInstrumentResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some GetPaymentInstrumentResponse.error_of_json))
  | GetPaymentInstrumentBalance ->
      if is_success
      then
        Ok
          (GetPaymentInstrumentBalanceResponse.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some GetPaymentInstrumentBalanceResponse.error_of_json))
  | GetPaymentSession ->
      if is_success
      then Ok (GetPaymentSessionResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some GetPaymentSessionResponse.error_of_json))
  | GetRecommendation ->
      if is_success
      then Ok (GetRecommendationResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some GetRecommendationResponse.error_of_json))
  | GetResourceApiKey ->
      if is_success
      then Ok (GetResourceApiKeyResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some GetResourceApiKeyResponse.error_of_json))
  | GetResourceOauth2Token ->
      if is_success
      then
        Ok (GetResourceOauth2TokenResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some GetResourceOauth2TokenResponse.error_of_json))
  | GetResourcePaymentToken ->
      if is_success
      then
        Ok (GetResourcePaymentTokenResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some GetResourcePaymentTokenResponse.error_of_json))
  | GetWorkloadAccessToken ->
      if is_success
      then
        Ok (GetWorkloadAccessTokenResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some GetWorkloadAccessTokenResponse.error_of_json))
  | GetWorkloadAccessTokenForJWT ->
      if is_success
      then
        Ok
          (GetWorkloadAccessTokenForJWTResponse.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some GetWorkloadAccessTokenForJWTResponse.error_of_json))
  | GetWorkloadAccessTokenForUserId ->
      if is_success
      then
        Ok
          (GetWorkloadAccessTokenForUserIdResponse.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some GetWorkloadAccessTokenForUserIdResponse.error_of_json))
  | InvokeAgentRuntime ->
      if is_success
      then
        let body = ResponseStream.of_string (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (InvokeAgentRuntimeResponse.of_header_and_body (headers, body))
      else
        Error
          (parse_aws_error (Some InvokeAgentRuntimeResponse.error_of_json))
  | InvokeAgentRuntimeCommand ->
      if is_success
      then
        let body =
          InvokeAgentRuntimeCommandStreamOutput.of_string
            (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok
          (InvokeAgentRuntimeCommandResponse.of_header_and_body
             (headers, body))
      else
        Error
          (parse_aws_error
             (Some InvokeAgentRuntimeCommandResponse.error_of_json))
  | InvokeBrowser ->
      if is_success
      then Ok (InvokeBrowserResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some InvokeBrowserResponse.error_of_json))
  | InvokeCodeInterpreter ->
      if is_success
      then
        let body =
          CodeInterpreterStreamOutput.of_string
            (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (InvokeCodeInterpreterResponse.of_header_and_body (headers, body))
      else
        Error
          (parse_aws_error (Some InvokeCodeInterpreterResponse.error_of_json))
  | InvokeHarness ->
      if is_success
      then
        let body =
          InvokeHarnessStreamOutput.of_string (Awso.Http.Response.body resp) in
        let headers =
          Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
        Ok (InvokeHarnessResponse.of_header_and_body (headers, body))
      else Error (parse_aws_error (Some InvokeHarnessResponse.error_of_json))
  | ListABTests ->
      if is_success
      then Ok (ListABTestsResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListABTestsResponse.error_of_json))
  | ListActors ->
      if is_success
      then Ok (ListActorsOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListActorsOutput.error_of_json))
  | ListBatchEvaluations ->
      if is_success
      then Ok (ListBatchEvaluationsResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListBatchEvaluationsResponse.error_of_json))
  | ListBrowserSessions ->
      if is_success
      then Ok (ListBrowserSessionsResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListBrowserSessionsResponse.error_of_json))
  | ListCodeInterpreterSessions ->
      if is_success
      then
        Ok
          (ListCodeInterpreterSessionsResponse.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some ListCodeInterpreterSessionsResponse.error_of_json))
  | ListEvents ->
      if is_success
      then Ok (ListEventsOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListEventsOutput.error_of_json))
  | ListMemoryExtractionJobs ->
      if is_success
      then
        Ok (ListMemoryExtractionJobsOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some ListMemoryExtractionJobsOutput.error_of_json))
  | ListMemoryRecords ->
      if is_success
      then Ok (ListMemoryRecordsOutput.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some ListMemoryRecordsOutput.error_of_json))
  | ListPaymentInstruments ->
      if is_success
      then
        Ok (ListPaymentInstrumentsResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some ListPaymentInstrumentsResponse.error_of_json))
  | ListPaymentSessions ->
      if is_success
      then Ok (ListPaymentSessionsResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListPaymentSessionsResponse.error_of_json))
  | ListRecommendations ->
      if is_success
      then Ok (ListRecommendationsResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some ListRecommendationsResponse.error_of_json))
  | ListSessions ->
      if is_success
      then Ok (ListSessionsOutput.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListSessionsOutput.error_of_json))
  | ProcessPayment ->
      if is_success
      then Ok (ProcessPaymentResponse.of_json (response_to_json resp))
      else
        Error (parse_aws_error (Some ProcessPaymentResponse.error_of_json))
  | RetrieveMemoryRecords ->
      if is_success
      then Ok (RetrieveMemoryRecordsOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some RetrieveMemoryRecordsOutput.error_of_json))
  | SaveBrowserSessionProfile ->
      if is_success
      then
        Ok
          (SaveBrowserSessionProfileResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some SaveBrowserSessionProfileResponse.error_of_json))
  | SearchRegistryRecords ->
      if is_success
      then Ok (SearchRegistryRecordsResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some SearchRegistryRecordsResponse.error_of_json))
  | StartBatchEvaluation ->
      if is_success
      then Ok (StartBatchEvaluationResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some StartBatchEvaluationResponse.error_of_json))
  | StartBrowserSession ->
      if is_success
      then Ok (StartBrowserSessionResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some StartBrowserSessionResponse.error_of_json))
  | StartCodeInterpreterSession ->
      if is_success
      then
        Ok
          (StartCodeInterpreterSessionResponse.of_json
             (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some StartCodeInterpreterSessionResponse.error_of_json))
  | StartMemoryExtractionJob ->
      if is_success
      then
        Ok (StartMemoryExtractionJobOutput.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some StartMemoryExtractionJobOutput.error_of_json))
  | StartRecommendation ->
      if is_success
      then Ok (StartRecommendationResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some StartRecommendationResponse.error_of_json))
  | StopBatchEvaluation ->
      if is_success
      then Ok (StopBatchEvaluationResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some StopBatchEvaluationResponse.error_of_json))
  | StopBrowserSession ->
      if is_success
      then Ok (StopBrowserSessionResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some StopBrowserSessionResponse.error_of_json))
  | StopCodeInterpreterSession ->
      if is_success
      then
        Ok
          (StopCodeInterpreterSessionResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error
             (Some StopCodeInterpreterSessionResponse.error_of_json))
  | StopRuntimeSession ->
      if is_success
      then Ok (StopRuntimeSessionResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some StopRuntimeSessionResponse.error_of_json))
  | UpdateABTest ->
      if is_success
      then Ok (UpdateABTestResponse.of_json (response_to_json resp))
      else Error (parse_aws_error (Some UpdateABTestResponse.error_of_json))
  | UpdateBrowserStream ->
      if is_success
      then Ok (UpdateBrowserStreamResponse.of_json (response_to_json resp))
      else
        Error
          (parse_aws_error (Some UpdateBrowserStreamResponse.error_of_json))
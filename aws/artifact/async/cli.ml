(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let get_account_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_account_settings (Values.GetAccountSettingsRequest.make ())
           (Some Values.GetAccountSettingsResponse.to_json)
           (Some Values.GetAccountSettingsResponse.error_to_json)])
let get_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reportVersion =
         flag "report-version" (optional json_arg)
           ~doc:"JSON VersionAttribute"
       and reportId =
         flag "report-id" (required string) ~doc:"STRING ReportId"
       and termToken =
         flag "term-token" (required string)
           ~doc:"STRING ShortStringAttribute" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_report
           (Values.GetReportRequest.make
              ?reportVersion:(Option.map ~f:Values.VersionAttribute.of_json
                                reportVersion) ~reportId ~termToken ())
           (Some Values.GetReportResponse.to_json)
           (Some Values.GetReportResponse.error_to_json)])
let get_report_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reportVersion =
         flag "report-version" (optional json_arg)
           ~doc:"JSON VersionAttribute"
       and reportId =
         flag "report-id" (required string) ~doc:"STRING ReportId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_report_metadata
           (Values.GetReportMetadataRequest.make
              ?reportVersion:(Option.map ~f:Values.VersionAttribute.of_json
                                reportVersion) ~reportId ())
           (Some Values.GetReportMetadataResponse.to_json)
           (Some Values.GetReportMetadataResponse.error_to_json)])
let get_term_for_report =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and reportVersion =
         flag "report-version" (optional json_arg)
           ~doc:"JSON VersionAttribute"
       and reportId =
         flag "report-id" (required string) ~doc:"STRING ReportId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_term_for_report
           (Values.GetTermForReportRequest.make
              ?reportVersion:(Option.map ~f:Values.VersionAttribute.of_json
                                reportVersion) ~reportId ())
           (Some Values.GetTermForReportResponse.to_json)
           (Some Values.GetTermForReportResponse.error_to_json)])
let list_customer_agreements =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsAttribute"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextTokenAttribute" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_customer_agreements
           (Values.ListCustomerAgreementsRequest.make ?maxResults ?nextToken
              ()) (Some Values.ListCustomerAgreementsResponse.to_json)
           (Some Values.ListCustomerAgreementsResponse.error_to_json)])
let list_report_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsAttribute"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextTokenAttribute"
       and reportId =
         flag "report-id" (required string) ~doc:"STRING ReportId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_report_versions
           (Values.ListReportVersionsRequest.make ?maxResults ?nextToken
              ~reportId ()) (Some Values.ListReportVersionsResponse.to_json)
           (Some Values.ListReportVersionsResponse.error_to_json)])
let list_reports =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsAttribute"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextTokenAttribute" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_reports
           (Values.ListReportsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListReportsResponse.to_json)
           (Some Values.ListReportsResponse.error_to_json)])
let put_account_settings =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and notificationSubscriptionStatus =
         flag "notification-subscription-status" (optional json_arg)
           ~doc:"JSON NotificationSubscriptionStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_account_settings
           (Values.PutAccountSettingsRequest.make
              ?notificationSubscriptionStatus:(Option.map
                                                 ~f:Values.NotificationSubscriptionStatus.of_json
                                                 notificationSubscriptionStatus)
              ()) (Some Values.PutAccountSettingsResponse.to_json)
           (Some Values.PutAccountSettingsResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("get-account-settings", get_account_settings);
    ("get-report", get_report);
    ("get-report-metadata", get_report_metadata);
    ("get-term-for-report", get_term_for_report);
    ("list-customer-agreements", list_customer_agreements);
    ("list-report-versions", list_report_versions);
    ("list-reports", list_reports);
    ("put-account-settings", put_account_settings)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_call_analytics_category =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and categoryName =
         flag "category-name" (required string) ~doc:"STRING CategoryName"
       and rules = flag "rules" (required json_arg) ~doc:"JSON RuleList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_call_analytics_category
           (Values.CreateCallAnalyticsCategoryRequest.make ~categoryName
              ~rules:(Values.RuleList.of_json rules) ())
           (Some Values.CreateCallAnalyticsCategoryResponse.to_json)
           (Some Values.CreateCallAnalyticsCategoryResponse.error_to_json)])
let create_language_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON CLMLanguageCode"
       and baseModelName =
         flag "base-model-name" (required json_arg) ~doc:"JSON BaseModelName"
       and modelName =
         flag "model-name" (required string) ~doc:"STRING ModelName"
       and inputDataConfig =
         flag "input-data-config" (required json_arg)
           ~doc:"JSON InputDataConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_language_model
           (Values.CreateLanguageModelRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~languageCode:(Values.CLMLanguageCode.of_json languageCode)
              ~baseModelName:(Values.BaseModelName.of_json baseModelName)
              ~modelName
              ~inputDataConfig:(Values.InputDataConfig.of_json
                                  inputDataConfig) ())
           (Some Values.CreateLanguageModelResponse.to_json)
           (Some Values.CreateLanguageModelResponse.error_to_json)])
let create_medical_vocabulary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and vocabularyName =
         flag "vocabulary-name" (required string)
           ~doc:"STRING VocabularyName"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode"
       and vocabularyFileUri =
         flag "vocabulary-file-uri" (required string) ~doc:"STRING Uri" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_medical_vocabulary
           (Values.CreateMedicalVocabularyRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~vocabularyName
              ~languageCode:(Values.LanguageCode.of_json languageCode)
              ~vocabularyFileUri ())
           (Some Values.CreateMedicalVocabularyResponse.to_json)
           (Some Values.CreateMedicalVocabularyResponse.error_to_json)])
let create_vocabulary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and phrases = flag "phrases" (optional json_arg) ~doc:"JSON Phrases"
       and vocabularyFileUri =
         flag "vocabulary-file-uri" (optional string) ~doc:"STRING Uri"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and vocabularyName =
         flag "vocabulary-name" (required string)
           ~doc:"STRING VocabularyName"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_vocabulary
           (Values.CreateVocabularyRequest.make
              ?phrases:(Option.map ~f:Values.Phrases.of_json phrases)
              ?vocabularyFileUri
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~vocabularyName
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.CreateVocabularyResponse.to_json)
           (Some Values.CreateVocabularyResponse.error_to_json)])
let create_vocabulary_filter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and words = flag "words" (optional json_arg) ~doc:"JSON Words"
       and vocabularyFilterFileUri =
         flag "vocabulary-filter-file-uri" (optional string)
           ~doc:"STRING Uri"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and vocabularyFilterName =
         flag "vocabulary-filter-name" (required string)
           ~doc:"STRING VocabularyFilterName"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_vocabulary_filter
           (Values.CreateVocabularyFilterRequest.make
              ?words:(Option.map ~f:Values.Words.of_json words)
              ?vocabularyFilterFileUri
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~vocabularyFilterName
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.CreateVocabularyFilterResponse.to_json)
           (Some Values.CreateVocabularyFilterResponse.error_to_json)])
let delete_call_analytics_category =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and categoryName =
         flag "category-name" (required string) ~doc:"STRING CategoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_call_analytics_category
           (Values.DeleteCallAnalyticsCategoryRequest.make ~categoryName ())
           (Some Values.DeleteCallAnalyticsCategoryResponse.to_json)
           (Some Values.DeleteCallAnalyticsCategoryResponse.error_to_json)])
let delete_call_analytics_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and callAnalyticsJobName =
         flag "call-analytics-job-name" (required string)
           ~doc:"STRING CallAnalyticsJobName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_call_analytics_job
           (Values.DeleteCallAnalyticsJobRequest.make ~callAnalyticsJobName
              ()) (Some Values.DeleteCallAnalyticsJobResponse.to_json)
           (Some Values.DeleteCallAnalyticsJobResponse.error_to_json)])
let delete_language_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelName =
         flag "model-name" (required string) ~doc:"STRING ModelName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_language_model
           (Values.DeleteLanguageModelRequest.make ~modelName ()) None None])
let delete_medical_transcription_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and medicalTranscriptionJobName =
         flag "medical-transcription-job-name" (required string)
           ~doc:"STRING TranscriptionJobName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_medical_transcription_job
           (Values.DeleteMedicalTranscriptionJobRequest.make
              ~medicalTranscriptionJobName ()) None None])
let delete_medical_vocabulary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vocabularyName =
         flag "vocabulary-name" (required string)
           ~doc:"STRING VocabularyName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_medical_vocabulary
           (Values.DeleteMedicalVocabularyRequest.make ~vocabularyName ())
           None None])
let delete_transcription_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and transcriptionJobName =
         flag "transcription-job-name" (required string)
           ~doc:"STRING TranscriptionJobName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_transcription_job
           (Values.DeleteTranscriptionJobRequest.make ~transcriptionJobName
              ()) None None])
let delete_vocabulary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vocabularyName =
         flag "vocabulary-name" (required string)
           ~doc:"STRING VocabularyName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vocabulary
           (Values.DeleteVocabularyRequest.make ~vocabularyName ()) None None])
let delete_vocabulary_filter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vocabularyFilterName =
         flag "vocabulary-filter-name" (required string)
           ~doc:"STRING VocabularyFilterName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vocabulary_filter
           (Values.DeleteVocabularyFilterRequest.make ~vocabularyFilterName
              ()) None None])
let describe_language_model =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and modelName =
         flag "model-name" (required string) ~doc:"STRING ModelName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_language_model
           (Values.DescribeLanguageModelRequest.make ~modelName ())
           (Some Values.DescribeLanguageModelResponse.to_json)
           (Some Values.DescribeLanguageModelResponse.error_to_json)])
let get_call_analytics_category =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and categoryName =
         flag "category-name" (required string) ~doc:"STRING CategoryName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_call_analytics_category
           (Values.GetCallAnalyticsCategoryRequest.make ~categoryName ())
           (Some Values.GetCallAnalyticsCategoryResponse.to_json)
           (Some Values.GetCallAnalyticsCategoryResponse.error_to_json)])
let get_call_analytics_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and callAnalyticsJobName =
         flag "call-analytics-job-name" (required string)
           ~doc:"STRING CallAnalyticsJobName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_call_analytics_job
           (Values.GetCallAnalyticsJobRequest.make ~callAnalyticsJobName ())
           (Some Values.GetCallAnalyticsJobResponse.to_json)
           (Some Values.GetCallAnalyticsJobResponse.error_to_json)])
let get_medical_transcription_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and medicalTranscriptionJobName =
         flag "medical-transcription-job-name" (required string)
           ~doc:"STRING TranscriptionJobName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_medical_transcription_job
           (Values.GetMedicalTranscriptionJobRequest.make
              ~medicalTranscriptionJobName ())
           (Some Values.GetMedicalTranscriptionJobResponse.to_json)
           (Some Values.GetMedicalTranscriptionJobResponse.error_to_json)])
let get_medical_vocabulary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vocabularyName =
         flag "vocabulary-name" (required string)
           ~doc:"STRING VocabularyName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_medical_vocabulary
           (Values.GetMedicalVocabularyRequest.make ~vocabularyName ())
           (Some Values.GetMedicalVocabularyResponse.to_json)
           (Some Values.GetMedicalVocabularyResponse.error_to_json)])
let get_transcription_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and transcriptionJobName =
         flag "transcription-job-name" (required string)
           ~doc:"STRING TranscriptionJobName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_transcription_job
           (Values.GetTranscriptionJobRequest.make ~transcriptionJobName ())
           (Some Values.GetTranscriptionJobResponse.to_json)
           (Some Values.GetTranscriptionJobResponse.error_to_json)])
let get_vocabulary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vocabularyName =
         flag "vocabulary-name" (required string)
           ~doc:"STRING VocabularyName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_vocabulary
           (Values.GetVocabularyRequest.make ~vocabularyName ())
           (Some Values.GetVocabularyResponse.to_json)
           (Some Values.GetVocabularyResponse.error_to_json)])
let get_vocabulary_filter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vocabularyFilterName =
         flag "vocabulary-filter-name" (required string)
           ~doc:"STRING VocabularyFilterName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_vocabulary_filter
           (Values.GetVocabularyFilterRequest.make ~vocabularyFilterName ())
           (Some Values.GetVocabularyFilterResponse.to_json)
           (Some Values.GetVocabularyFilterResponse.error_to_json)])
let list_call_analytics_categories =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_call_analytics_categories
           (Values.ListCallAnalyticsCategoriesRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListCallAnalyticsCategoriesResponse.to_json)
           (Some Values.ListCallAnalyticsCategoriesResponse.error_to_json)])
let list_call_analytics_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON CallAnalyticsJobStatus"
       and jobNameContains =
         flag "job-name-contains" (optional string)
           ~doc:"STRING CallAnalyticsJobName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_call_analytics_jobs
           (Values.ListCallAnalyticsJobsRequest.make
              ?status:(Option.map ~f:Values.CallAnalyticsJobStatus.of_json
                         status) ?jobNameContains ?nextToken ?maxResults ())
           (Some Values.ListCallAnalyticsJobsResponse.to_json)
           (Some Values.ListCallAnalyticsJobsResponse.error_to_json)])
let list_language_models =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and statusEquals =
         flag "status-equals" (optional json_arg) ~doc:"JSON ModelStatus"
       and nameContains =
         flag "name-contains" (optional string) ~doc:"STRING ModelName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_language_models
           (Values.ListLanguageModelsRequest.make
              ?statusEquals:(Option.map ~f:Values.ModelStatus.of_json
                               statusEquals) ?nameContains ?nextToken
              ?maxResults ())
           (Some Values.ListLanguageModelsResponse.to_json)
           (Some Values.ListLanguageModelsResponse.error_to_json)])
let list_medical_transcription_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON TranscriptionJobStatus"
       and jobNameContains =
         flag "job-name-contains" (optional string)
           ~doc:"STRING TranscriptionJobName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_medical_transcription_jobs
           (Values.ListMedicalTranscriptionJobsRequest.make
              ?status:(Option.map ~f:Values.TranscriptionJobStatus.of_json
                         status) ?jobNameContains ?nextToken ?maxResults ())
           (Some Values.ListMedicalTranscriptionJobsResponse.to_json)
           (Some Values.ListMedicalTranscriptionJobsResponse.error_to_json)])
let list_medical_vocabularies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and stateEquals =
         flag "state-equals" (optional json_arg) ~doc:"JSON VocabularyState"
       and nameContains =
         flag "name-contains" (optional string) ~doc:"STRING VocabularyName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_medical_vocabularies
           (Values.ListMedicalVocabulariesRequest.make ?nextToken ?maxResults
              ?stateEquals:(Option.map ~f:Values.VocabularyState.of_json
                              stateEquals) ?nameContains ())
           (Some Values.ListMedicalVocabulariesResponse.to_json)
           (Some Values.ListMedicalVocabulariesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TranscribeArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_transcription_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and status =
         flag "status" (optional json_arg) ~doc:"JSON TranscriptionJobStatus"
       and jobNameContains =
         flag "job-name-contains" (optional string)
           ~doc:"STRING TranscriptionJobName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_transcription_jobs
           (Values.ListTranscriptionJobsRequest.make
              ?status:(Option.map ~f:Values.TranscriptionJobStatus.of_json
                         status) ?jobNameContains ?nextToken ?maxResults ())
           (Some Values.ListTranscriptionJobsResponse.to_json)
           (Some Values.ListTranscriptionJobsResponse.error_to_json)])
let list_vocabularies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and stateEquals =
         flag "state-equals" (optional json_arg) ~doc:"JSON VocabularyState"
       and nameContains =
         flag "name-contains" (optional string) ~doc:"STRING VocabularyName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_vocabularies
           (Values.ListVocabulariesRequest.make ?nextToken ?maxResults
              ?stateEquals:(Option.map ~f:Values.VocabularyState.of_json
                              stateEquals) ?nameContains ())
           (Some Values.ListVocabulariesResponse.to_json)
           (Some Values.ListVocabulariesResponse.error_to_json)])
let list_vocabulary_filters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nameContains =
         flag "name-contains" (optional string)
           ~doc:"STRING VocabularyFilterName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_vocabulary_filters
           (Values.ListVocabularyFiltersRequest.make ?nextToken ?maxResults
              ?nameContains ())
           (Some Values.ListVocabularyFiltersResponse.to_json)
           (Some Values.ListVocabularyFiltersResponse.error_to_json)])
let start_call_analytics_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and outputLocation =
         flag "output-location" (optional string) ~doc:"STRING Uri"
       and outputEncryptionKMSKeyId =
         flag "output-encryption-k-m-s-key-id" (optional string)
           ~doc:"STRING KMSKeyId"
       and settings =
         flag "settings" (optional json_arg)
           ~doc:"JSON CallAnalyticsJobSettings"
       and channelDefinitions =
         flag "channel-definitions" (optional json_arg)
           ~doc:"JSON ChannelDefinitions"
       and callAnalyticsJobName =
         flag "call-analytics-job-name" (required string)
           ~doc:"STRING CallAnalyticsJobName"
       and media = flag "media" (required json_arg) ~doc:"JSON Media"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (required string)
           ~doc:"STRING DataAccessRoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_call_analytics_job
           (Values.StartCallAnalyticsJobRequest.make ?outputLocation
              ?outputEncryptionKMSKeyId
              ?settings:(Option.map
                           ~f:Values.CallAnalyticsJobSettings.of_json
                           settings)
              ?channelDefinitions:(Option.map
                                     ~f:Values.ChannelDefinitions.of_json
                                     channelDefinitions)
              ~callAnalyticsJobName ~media:(Values.Media.of_json media)
              ~dataAccessRoleArn ())
           (Some Values.StartCallAnalyticsJobResponse.to_json)
           (Some Values.StartCallAnalyticsJobResponse.error_to_json)])
let start_medical_transcription_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and mediaSampleRateHertz =
         flag "media-sample-rate-hertz" (optional int)
           ~doc:"INT MedicalMediaSampleRateHertz"
       and mediaFormat =
         flag "media-format" (optional json_arg) ~doc:"JSON MediaFormat"
       and outputKey =
         flag "output-key" (optional string) ~doc:"STRING OutputKey"
       and outputEncryptionKMSKeyId =
         flag "output-encryption-k-m-s-key-id" (optional string)
           ~doc:"STRING KMSKeyId"
       and kMSEncryptionContext =
         flag "k-m-s-encryption-context" (optional json_arg)
           ~doc:"JSON KMSEncryptionContextMap"
       and settings =
         flag "settings" (optional json_arg)
           ~doc:"JSON MedicalTranscriptionSetting"
       and contentIdentificationType =
         flag "content-identification-type" (optional json_arg)
           ~doc:"JSON MedicalContentIdentificationType"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and medicalTranscriptionJobName =
         flag "medical-transcription-job-name" (required string)
           ~doc:"STRING TranscriptionJobName"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode"
       and media = flag "media" (required json_arg) ~doc:"JSON Media"
       and outputBucketName =
         flag "output-bucket-name" (required string)
           ~doc:"STRING OutputBucketName"
       and specialty =
         flag "specialty" (required json_arg) ~doc:"JSON Specialty"
       and type_ = flag "type-" (required json_arg) ~doc:"JSON Type" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_medical_transcription_job
           (Values.StartMedicalTranscriptionJobRequest.make
              ?mediaSampleRateHertz
              ?mediaFormat:(Option.map ~f:Values.MediaFormat.of_json
                              mediaFormat) ?outputKey
              ?outputEncryptionKMSKeyId
              ?kMSEncryptionContext:(Option.map
                                       ~f:Values.KMSEncryptionContextMap.of_json
                                       kMSEncryptionContext)
              ?settings:(Option.map
                           ~f:Values.MedicalTranscriptionSetting.of_json
                           settings)
              ?contentIdentificationType:(Option.map
                                            ~f:Values.MedicalContentIdentificationType.of_json
                                            contentIdentificationType)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~medicalTranscriptionJobName
              ~languageCode:(Values.LanguageCode.of_json languageCode)
              ~media:(Values.Media.of_json media) ~outputBucketName
              ~specialty:(Values.Specialty.of_json specialty)
              ~type_:(Values.Type.of_json type_) ())
           (Some Values.StartMedicalTranscriptionJobResponse.to_json)
           (Some Values.StartMedicalTranscriptionJobResponse.error_to_json)])
let start_transcription_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and languageCode =
         flag "language-code" (optional json_arg) ~doc:"JSON LanguageCode"
       and mediaSampleRateHertz =
         flag "media-sample-rate-hertz" (optional int)
           ~doc:"INT MediaSampleRateHertz"
       and mediaFormat =
         flag "media-format" (optional json_arg) ~doc:"JSON MediaFormat"
       and outputBucketName =
         flag "output-bucket-name" (optional string)
           ~doc:"STRING OutputBucketName"
       and outputKey =
         flag "output-key" (optional string) ~doc:"STRING OutputKey"
       and outputEncryptionKMSKeyId =
         flag "output-encryption-k-m-s-key-id" (optional string)
           ~doc:"STRING KMSKeyId"
       and kMSEncryptionContext =
         flag "k-m-s-encryption-context" (optional json_arg)
           ~doc:"JSON KMSEncryptionContextMap"
       and settings =
         flag "settings" (optional json_arg) ~doc:"JSON Settings"
       and modelSettings =
         flag "model-settings" (optional json_arg) ~doc:"JSON ModelSettings"
       and jobExecutionSettings =
         flag "job-execution-settings" (optional json_arg)
           ~doc:"JSON JobExecutionSettings"
       and contentRedaction =
         flag "content-redaction" (optional json_arg)
           ~doc:"JSON ContentRedaction"
       and identifyLanguage =
         flag "identify-language" (optional bool) ~doc:"BOOL Boolean"
       and languageOptions =
         flag "language-options" (optional json_arg)
           ~doc:"JSON LanguageOptions"
       and subtitles =
         flag "subtitles" (optional json_arg) ~doc:"JSON Subtitles"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and languageIdSettings =
         flag "language-id-settings" (optional json_arg)
           ~doc:"JSON LanguageIdSettingsMap"
       and transcriptionJobName =
         flag "transcription-job-name" (required string)
           ~doc:"STRING TranscriptionJobName"
       and media = flag "media" (required json_arg) ~doc:"JSON Media" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_transcription_job
           (Values.StartTranscriptionJobRequest.make
              ?languageCode:(Option.map ~f:Values.LanguageCode.of_json
                               languageCode) ?mediaSampleRateHertz
              ?mediaFormat:(Option.map ~f:Values.MediaFormat.of_json
                              mediaFormat) ?outputBucketName ?outputKey
              ?outputEncryptionKMSKeyId
              ?kMSEncryptionContext:(Option.map
                                       ~f:Values.KMSEncryptionContextMap.of_json
                                       kMSEncryptionContext)
              ?settings:(Option.map ~f:Values.Settings.of_json settings)
              ?modelSettings:(Option.map ~f:Values.ModelSettings.of_json
                                modelSettings)
              ?jobExecutionSettings:(Option.map
                                       ~f:Values.JobExecutionSettings.of_json
                                       jobExecutionSettings)
              ?contentRedaction:(Option.map
                                   ~f:Values.ContentRedaction.of_json
                                   contentRedaction) ?identifyLanguage
              ?languageOptions:(Option.map ~f:Values.LanguageOptions.of_json
                                  languageOptions)
              ?subtitles:(Option.map ~f:Values.Subtitles.of_json subtitles)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?languageIdSettings:(Option.map
                                     ~f:Values.LanguageIdSettingsMap.of_json
                                     languageIdSettings)
              ~transcriptionJobName ~media:(Values.Media.of_json media) ())
           (Some Values.StartTranscriptionJobResponse.to_json)
           (Some Values.StartTranscriptionJobResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TranscribeArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TranscribeArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_call_analytics_category =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and categoryName =
         flag "category-name" (required string) ~doc:"STRING CategoryName"
       and rules = flag "rules" (required json_arg) ~doc:"JSON RuleList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_call_analytics_category
           (Values.UpdateCallAnalyticsCategoryRequest.make ~categoryName
              ~rules:(Values.RuleList.of_json rules) ())
           (Some Values.UpdateCallAnalyticsCategoryResponse.to_json)
           (Some Values.UpdateCallAnalyticsCategoryResponse.error_to_json)])
let update_medical_vocabulary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vocabularyFileUri =
         flag "vocabulary-file-uri" (optional string) ~doc:"STRING Uri"
       and vocabularyName =
         flag "vocabulary-name" (required string)
           ~doc:"STRING VocabularyName"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_medical_vocabulary
           (Values.UpdateMedicalVocabularyRequest.make ?vocabularyFileUri
              ~vocabularyName
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.UpdateMedicalVocabularyResponse.to_json)
           (Some Values.UpdateMedicalVocabularyResponse.error_to_json)])
let update_vocabulary =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and phrases = flag "phrases" (optional json_arg) ~doc:"JSON Phrases"
       and vocabularyFileUri =
         flag "vocabulary-file-uri" (optional string) ~doc:"STRING Uri"
       and vocabularyName =
         flag "vocabulary-name" (required string)
           ~doc:"STRING VocabularyName"
       and languageCode =
         flag "language-code" (required json_arg) ~doc:"JSON LanguageCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_vocabulary
           (Values.UpdateVocabularyRequest.make
              ?phrases:(Option.map ~f:Values.Phrases.of_json phrases)
              ?vocabularyFileUri ~vocabularyName
              ~languageCode:(Values.LanguageCode.of_json languageCode) ())
           (Some Values.UpdateVocabularyResponse.to_json)
           (Some Values.UpdateVocabularyResponse.error_to_json)])
let update_vocabulary_filter =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and words = flag "words" (optional json_arg) ~doc:"JSON Words"
       and vocabularyFilterFileUri =
         flag "vocabulary-filter-file-uri" (optional string)
           ~doc:"STRING Uri"
       and vocabularyFilterName =
         flag "vocabulary-filter-name" (required string)
           ~doc:"STRING VocabularyFilterName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_vocabulary_filter
           (Values.UpdateVocabularyFilterRequest.make
              ?words:(Option.map ~f:Values.Words.of_json words)
              ?vocabularyFilterFileUri ~vocabularyFilterName ())
           (Some Values.UpdateVocabularyFilterResponse.to_json)
           (Some Values.UpdateVocabularyFilterResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("create-call-analytics-category", create_call_analytics_category);
    ("create-language-model", create_language_model);
    ("create-medical-vocabulary", create_medical_vocabulary);
    ("create-vocabulary", create_vocabulary);
    ("create-vocabulary-filter", create_vocabulary_filter);
    ("delete-call-analytics-category", delete_call_analytics_category);
    ("delete-call-analytics-job", delete_call_analytics_job);
    ("delete-language-model", delete_language_model);
    ("delete-medical-transcription-job", delete_medical_transcription_job);
    ("delete-medical-vocabulary", delete_medical_vocabulary);
    ("delete-transcription-job", delete_transcription_job);
    ("delete-vocabulary", delete_vocabulary);
    ("delete-vocabulary-filter", delete_vocabulary_filter);
    ("describe-language-model", describe_language_model);
    ("get-call-analytics-category", get_call_analytics_category);
    ("get-call-analytics-job", get_call_analytics_job);
    ("get-medical-transcription-job", get_medical_transcription_job);
    ("get-medical-vocabulary", get_medical_vocabulary);
    ("get-transcription-job", get_transcription_job);
    ("get-vocabulary", get_vocabulary);
    ("get-vocabulary-filter", get_vocabulary_filter);
    ("list-call-analytics-categories", list_call_analytics_categories);
    ("list-call-analytics-jobs", list_call_analytics_jobs);
    ("list-language-models", list_language_models);
    ("list-medical-transcription-jobs", list_medical_transcription_jobs);
    ("list-medical-vocabularies", list_medical_vocabularies);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-transcription-jobs", list_transcription_jobs);
    ("list-vocabularies", list_vocabularies);
    ("list-vocabulary-filters", list_vocabulary_filters);
    ("start-call-analytics-job", start_call_analytics_job);
    ("start-medical-transcription-job", start_medical_transcription_job);
    ("start-transcription-job", start_transcription_job);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-call-analytics-category", update_call_analytics_category);
    ("update-medical-vocabulary", update_medical_vocabulary);
    ("update-vocabulary", update_vocabulary);
    ("update-vocabulary-filter", update_vocabulary_filter)]
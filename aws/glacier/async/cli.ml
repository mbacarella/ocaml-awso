(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let abort_multipart_upload =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string"
       and uploadId = flag "upload-id" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.abort_multipart_upload
           (Values.AbortMultipartUploadInput.make ~accountId ~vaultName
              ~uploadId ()) None None])
let abort_vault_lock =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.abort_vault_lock
           (Values.AbortVaultLockInput.make ~accountId ~vaultName ()) None
           None])
let add_tags_to_vault =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_tags_to_vault
           (Values.AddTagsToVaultInput.make
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~accountId
              ~vaultName ()) None None])
let complete_multipart_upload =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and archiveSize =
         flag "archive-size" (optional string) ~doc:"STRING string"
       and checksum = flag "checksum" (optional string) ~doc:"STRING string"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string"
       and uploadId = flag "upload-id" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.complete_multipart_upload
           (Values.CompleteMultipartUploadInput.make ?archiveSize ?checksum
              ~accountId ~vaultName ~uploadId ())
           (Some Values.ArchiveCreationOutput.to_json)
           (Some Values.ArchiveCreationOutput.error_to_json)])
let complete_vault_lock =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string"
       and lockId = flag "lock-id" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.complete_vault_lock
           (Values.CompleteVaultLockInput.make ~accountId ~vaultName ~lockId
              ()) None None])
let create_vault =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_vault
           (Values.CreateVaultInput.make ~accountId ~vaultName ())
           (Some Values.CreateVaultOutput.to_json)
           (Some Values.CreateVaultOutput.error_to_json)])
let delete_archive =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string"
       and archiveId =
         flag "archive-id" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_archive
           (Values.DeleteArchiveInput.make ~accountId ~vaultName ~archiveId
              ()) None None])
let delete_vault =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vault
           (Values.DeleteVaultInput.make ~accountId ~vaultName ()) None None])
let delete_vault_access_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vault_access_policy
           (Values.DeleteVaultAccessPolicyInput.make ~accountId ~vaultName ())
           None None])
let delete_vault_notifications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_vault_notifications
           (Values.DeleteVaultNotificationsInput.make ~accountId ~vaultName
              ()) None None])
let describe_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string"
       and jobId = flag "job-id" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_job
           (Values.DescribeJobInput.make ~accountId ~vaultName ~jobId ())
           (Some Values.GlacierJobDescription.to_json)
           (Some Values.GlacierJobDescription.error_to_json)])
let describe_vault =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_vault
           (Values.DescribeVaultInput.make ~accountId ~vaultName ())
           (Some Values.DescribeVaultOutput.to_json)
           (Some Values.DescribeVaultOutput.error_to_json)])
let get_data_retrieval_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_data_retrieval_policy
           (Values.GetDataRetrievalPolicyInput.make ~accountId ())
           (Some Values.GetDataRetrievalPolicyOutput.to_json)
           (Some Values.GetDataRetrievalPolicyOutput.error_to_json)])
let get_job_output =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and range = flag "range" (optional string) ~doc:"STRING string"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string"
       and jobId = flag "job-id" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_job_output
           (Values.GetJobOutputInput.make ?range ~accountId ~vaultName ~jobId
              ()) (Some Values.GetJobOutputOutput.to_json)
           (Some Values.GetJobOutputOutput.error_to_json)])
let get_vault_access_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_vault_access_policy
           (Values.GetVaultAccessPolicyInput.make ~accountId ~vaultName ())
           (Some Values.GetVaultAccessPolicyOutput.to_json)
           (Some Values.GetVaultAccessPolicyOutput.error_to_json)])
let get_vault_lock =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_vault_lock
           (Values.GetVaultLockInput.make ~accountId ~vaultName ())
           (Some Values.GetVaultLockOutput.to_json)
           (Some Values.GetVaultLockOutput.error_to_json)])
let get_vault_notifications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_vault_notifications
           (Values.GetVaultNotificationsInput.make ~accountId ~vaultName ())
           (Some Values.GetVaultNotificationsOutput.to_json)
           (Some Values.GetVaultNotificationsOutput.error_to_json)])
let initiate_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobParameters =
         flag "job-parameters" (optional json_arg) ~doc:"JSON JobParameters"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.initiate_job
           (Values.InitiateJobInput.make
              ?jobParameters:(Option.map ~f:Values.JobParameters.of_json
                                jobParameters) ~accountId ~vaultName ())
           (Some Values.InitiateJobOutput.to_json)
           (Some Values.InitiateJobOutput.error_to_json)])
let initiate_multipart_upload =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and archiveDescription =
         flag "archive-description" (optional string) ~doc:"STRING string"
       and partSize = flag "part-size" (optional string) ~doc:"STRING string"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.initiate_multipart_upload
           (Values.InitiateMultipartUploadInput.make ?archiveDescription
              ?partSize ~accountId ~vaultName ())
           (Some Values.InitiateMultipartUploadOutput.to_json)
           (Some Values.InitiateMultipartUploadOutput.error_to_json)])
let initiate_vault_lock =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policy =
         flag "policy" (optional json_arg) ~doc:"JSON VaultLockPolicy"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.initiate_vault_lock
           (Values.InitiateVaultLockInput.make
              ?policy:(Option.map ~f:Values.VaultLockPolicy.of_json policy)
              ~accountId ~vaultName ())
           (Some Values.InitiateVaultLockOutput.to_json)
           (Some Values.InitiateVaultLockOutput.error_to_json)])
let list_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and limit = flag "limit" (optional string) ~doc:"STRING string"
       and marker = flag "marker" (optional string) ~doc:"STRING string"
       and statuscode =
         flag "statuscode" (optional string) ~doc:"STRING string"
       and completed =
         flag "completed" (optional string) ~doc:"STRING string"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_jobs
           (Values.ListJobsInput.make ?limit ?marker ?statuscode ?completed
              ~accountId ~vaultName ()) (Some Values.ListJobsOutput.to_json)
           (Some Values.ListJobsOutput.error_to_json)])
let list_multipart_uploads =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING string"
       and limit = flag "limit" (optional string) ~doc:"STRING string"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_multipart_uploads
           (Values.ListMultipartUploadsInput.make ?marker ?limit ~accountId
              ~vaultName ()) (Some Values.ListMultipartUploadsOutput.to_json)
           (Some Values.ListMultipartUploadsOutput.error_to_json)])
let list_parts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING string"
       and limit = flag "limit" (optional string) ~doc:"STRING string"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string"
       and uploadId = flag "upload-id" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_parts
           (Values.ListPartsInput.make ?marker ?limit ~accountId ~vaultName
              ~uploadId ()) (Some Values.ListPartsOutput.to_json)
           (Some Values.ListPartsOutput.error_to_json)])
let list_provisioned_capacity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_provisioned_capacity
           (Values.ListProvisionedCapacityInput.make ~accountId ())
           (Some Values.ListProvisionedCapacityOutput.to_json)
           (Some Values.ListProvisionedCapacityOutput.error_to_json)])
let list_tags_for_vault =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_vault
           (Values.ListTagsForVaultInput.make ~accountId ~vaultName ())
           (Some Values.ListTagsForVaultOutput.to_json)
           (Some Values.ListTagsForVaultOutput.error_to_json)])
let list_vaults =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING string"
       and limit = flag "limit" (optional string) ~doc:"STRING string"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_vaults
           (Values.ListVaultsInput.make ?marker ?limit ~accountId ())
           (Some Values.ListVaultsOutput.to_json)
           (Some Values.ListVaultsOutput.error_to_json)])
let purchase_provisioned_capacity =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.purchase_provisioned_capacity
           (Values.PurchaseProvisionedCapacityInput.make ~accountId ())
           (Some Values.PurchaseProvisionedCapacityOutput.to_json)
           (Some Values.PurchaseProvisionedCapacityOutput.error_to_json)])
let remove_tags_from_vault =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tagKeys =
         flag "tag-keys" (optional json_arg) ~doc:"JSON TagKeyList"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_tags_from_vault
           (Values.RemoveTagsFromVaultInput.make
              ?tagKeys:(Option.map ~f:Values.TagKeyList.of_json tagKeys)
              ~accountId ~vaultName ()) None None])
let set_data_retrieval_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policy =
         flag "policy" (optional json_arg) ~doc:"JSON DataRetrievalPolicy"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_data_retrieval_policy
           (Values.SetDataRetrievalPolicyInput.make
              ?policy:(Option.map ~f:Values.DataRetrievalPolicy.of_json
                         policy) ~accountId ()) None None])
let set_vault_access_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policy =
         flag "policy" (optional json_arg) ~doc:"JSON VaultAccessPolicy"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_vault_access_policy
           (Values.SetVaultAccessPolicyInput.make
              ?policy:(Option.map ~f:Values.VaultAccessPolicy.of_json policy)
              ~accountId ~vaultName ()) None None])
let set_vault_notifications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vaultNotificationConfig =
         flag "vault-notification-config" (optional json_arg)
           ~doc:"JSON VaultNotificationConfig"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_vault_notifications
           (Values.SetVaultNotificationsInput.make
              ?vaultNotificationConfig:(Option.map
                                          ~f:Values.VaultNotificationConfig.of_json
                                          vaultNotificationConfig) ~accountId
              ~vaultName ()) None None])
let upload_archive =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and archiveDescription =
         flag "archive-description" (optional string) ~doc:"STRING string"
       and checksum = flag "checksum" (optional string) ~doc:"STRING string"
       and body = flag "body" (optional json_arg) ~doc:"JSON Stream"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.upload_archive
           (Values.UploadArchiveInput.make ?archiveDescription ?checksum
              ?body:(Option.map ~f:Values.Stream.of_json body) ~vaultName
              ~accountId ()) (Some Values.ArchiveCreationOutput.to_json)
           (Some Values.ArchiveCreationOutput.error_to_json)])
let upload_multipart_part =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and checksum = flag "checksum" (optional string) ~doc:"STRING string"
       and range = flag "range" (optional string) ~doc:"STRING string"
       and body = flag "body" (optional json_arg) ~doc:"JSON Stream"
       and accountId =
         flag "account-id" (required string) ~doc:"STRING string"
       and vaultName =
         flag "vault-name" (required string) ~doc:"STRING string"
       and uploadId = flag "upload-id" (required string) ~doc:"STRING string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.upload_multipart_part
           (Values.UploadMultipartPartInput.make ?checksum ?range
              ?body:(Option.map ~f:Values.Stream.of_json body) ~accountId
              ~vaultName ~uploadId ())
           (Some Values.UploadMultipartPartOutput.to_json)
           (Some Values.UploadMultipartPartOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("abort-multipart-upload", abort_multipart_upload);
    ("abort-vault-lock", abort_vault_lock);
    ("add-tags-to-vault", add_tags_to_vault);
    ("complete-multipart-upload", complete_multipart_upload);
    ("complete-vault-lock", complete_vault_lock);
    ("create-vault", create_vault);
    ("delete-archive", delete_archive);
    ("delete-vault", delete_vault);
    ("delete-vault-access-policy", delete_vault_access_policy);
    ("delete-vault-notifications", delete_vault_notifications);
    ("describe-job", describe_job);
    ("describe-vault", describe_vault);
    ("get-data-retrieval-policy", get_data_retrieval_policy);
    ("get-job-output", get_job_output);
    ("get-vault-access-policy", get_vault_access_policy);
    ("get-vault-lock", get_vault_lock);
    ("get-vault-notifications", get_vault_notifications);
    ("initiate-job", initiate_job);
    ("initiate-multipart-upload", initiate_multipart_upload);
    ("initiate-vault-lock", initiate_vault_lock);
    ("list-jobs", list_jobs);
    ("list-multipart-uploads", list_multipart_uploads);
    ("list-parts", list_parts);
    ("list-provisioned-capacity", list_provisioned_capacity);
    ("list-tags-for-vault", list_tags_for_vault);
    ("list-vaults", list_vaults);
    ("purchase-provisioned-capacity", purchase_provisioned_capacity);
    ("remove-tags-from-vault", remove_tags_from_vault);
    ("set-data-retrieval-policy", set_data_retrieval_policy);
    ("set-vault-access-policy", set_vault_access_policy);
    ("set-vault-notifications", set_vault_notifications);
    ("upload-archive", upload_archive);
    ("upload-multipart-part", upload_multipart_part)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.glacier
let apiVersion = "2012-06-01"
let endpointPrefix = "glacier"
let serviceFullName = "Amazon Glacier"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module Type =
  struct
    type nonrec t =
      | AmazonCustomerByEmail 
      | CanonicalUser 
      | Group 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | AmazonCustomerByEmail -> "AmazonCustomerByEmail"
      | CanonicalUser -> "CanonicalUser"
      | Group -> "Group"
      | Non_static_id s -> s
    let of_string =
      function
      | "AmazonCustomerByEmail" -> AmazonCustomerByEmail
      | "CanonicalUser" -> CanonicalUser
      | "Group" -> Group
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Type" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Type" j)
    let to_json = simple_to_json to_value
  end
module String_ =
  struct
    type nonrec t = string
    let context_ = "string"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"string" j
    let to_json = simple_to_json to_value
  end
module Grantee =
  struct
    type nonrec t =
      {
      type_: Type.t ;
      displayName: String_.t option ;
      uRI: String_.t option ;
      iD: String_.t option ;
      emailAddress: String_.t option }
    let context_ = "Grantee"
    let make ?displayName =
      fun ?uRI ->
        fun ?iD ->
          fun ?emailAddress ->
            fun ~type_ ->
              fun () -> { displayName; uRI; iD; emailAddress; type_ }
    let to_value x =
      structure_to_value
        [("Type", (Some (Type.to_value x.type_)));
        ("DisplayName", (Option.map x.displayName ~f:String_.to_value));
        ("URI", (Option.map x.uRI ~f:String_.to_value));
        ("ID", (Option.map x.iD ~f:String_.to_value));
        ("EmailAddress", (Option.map x.emailAddress ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let emailAddress =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "EmailAddress") in
      let iD = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ID") in
      let uRI = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "URI") in
      let displayName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "DisplayName") in
      let type_ =
        Type.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Type") in
      make ?emailAddress ?iD ?uRI ?displayName ~type_ ()
    let of_json json =
      let emailAddress = field_map json "EmailAddress" String_.of_json in
      let iD = field_map json "ID" String_.of_json in
      let uRI = field_map json "URI" String_.of_json in
      let displayName = field_map json "DisplayName" String_.of_json in
      let type_ = field_map_exn json "Type" Type.of_json in
      make ?emailAddress ?iD ?uRI ?displayName ~type_ ()
    let to_json v = composed_to_json to_value v
  end
module Permission =
  struct
    type nonrec t =
      | FULL_CONTROL 
      | WRITE 
      | WRITE_ACP 
      | READ 
      | READ_ACP 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | FULL_CONTROL -> "FULL_CONTROL"
      | WRITE -> "WRITE"
      | WRITE_ACP -> "WRITE_ACP"
      | READ -> "READ"
      | READ_ACP -> "READ_ACP"
      | Non_static_id s -> s
    let of_string =
      function
      | "FULL_CONTROL" -> FULL_CONTROL
      | "WRITE" -> WRITE
      | "WRITE_ACP" -> WRITE_ACP
      | "READ" -> READ
      | "READ_ACP" -> READ_ACP
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Permission" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Permission" j)
    let to_json = simple_to_json to_value
  end
module Grant =
  struct
    type nonrec t =
      {
      grantee: Grantee.t option ;
      permission: Permission.t option }
    let make ?grantee = fun ?permission -> fun () -> { grantee; permission }
    let to_value x =
      structure_to_value
        [("Grantee", (Option.map x.grantee ~f:Grantee.to_value));
        ("Permission", (Option.map x.permission ~f:Permission.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let permission =
        (Option.map ~f:Permission.of_xml) (Xml.child xml_arg0 "Permission") in
      let grantee =
        (Option.map ~f:Grantee.of_xml) (Xml.child xml_arg0 "Grantee") in
      make ?permission ?grantee ()
    let of_json json =
      let permission = field_map json "Permission" Permission.of_json in
      let grantee = field_map json "Grantee" Grantee.of_json in
      make ?permission ?grantee ()
    let to_json v = composed_to_json to_value v
  end
module EncryptionType =
  struct
    type nonrec t =
      | Aws_kms 
      | AES256 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Aws_kms -> "aws:kms"
      | AES256 -> "AES256"
      | Non_static_id s -> s
    let of_string =
      function
      | "aws:kms" -> Aws_kms
      | "AES256" -> AES256
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration EncryptionType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"EncryptionType" j)
    let to_json = simple_to_json to_value
  end
module FileHeaderInfo =
  struct
    type nonrec t =
      | USE 
      | IGNORE 
      | NONE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | USE -> "USE"
      | IGNORE -> "IGNORE"
      | NONE -> "NONE"
      | Non_static_id s -> s
    let of_string =
      function
      | "USE" -> USE
      | "IGNORE" -> IGNORE
      | "NONE" -> NONE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration FileHeaderInfo" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"FileHeaderInfo" j)
    let to_json = simple_to_json to_value
  end
module QuoteFields =
  struct
    type nonrec t =
      | ALWAYS 
      | ASNEEDED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ALWAYS -> "ALWAYS"
      | ASNEEDED -> "ASNEEDED"
      | Non_static_id s -> s
    let of_string =
      function
      | "ALWAYS" -> ALWAYS
      | "ASNEEDED" -> ASNEEDED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration QuoteFields" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"QuoteFields" j)
    let to_json = simple_to_json to_value
  end
module AccessControlPolicyList =
  struct
    type nonrec t = Grant.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Grant.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Grant.of_xml)
    let of_json j =
      list_of_json ~kind:"AccessControlPolicyList" ~of_json:Grant.of_json j
    let to_json v = composed_to_json to_value v
  end
module CannedACL =
  struct
    type nonrec t =
      | Private 
      | Public_read 
      | Public_read_write 
      | Aws_exec_read 
      | Authenticated_read 
      | Bucket_owner_read 
      | Bucket_owner_full_control 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Private -> "private"
      | Public_read -> "public-read"
      | Public_read_write -> "public-read-write"
      | Aws_exec_read -> "aws-exec-read"
      | Authenticated_read -> "authenticated-read"
      | Bucket_owner_read -> "bucket-owner-read"
      | Bucket_owner_full_control -> "bucket-owner-full-control"
      | Non_static_id s -> s
    let of_string =
      function
      | "private" -> Private
      | "public-read" -> Public_read
      | "public-read-write" -> Public_read_write
      | "aws-exec-read" -> Aws_exec_read
      | "authenticated-read" -> Authenticated_read
      | "bucket-owner-read" -> Bucket_owner_read
      | "bucket-owner-full-control" -> Bucket_owner_full_control
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration CannedACL" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"CannedACL" j)
    let to_json = simple_to_json to_value
  end
module Encryption =
  struct
    type nonrec t =
      {
      encryptionType: EncryptionType.t option ;
      kMSKeyId: String_.t option ;
      kMSContext: String_.t option }
    let make ?encryptionType =
      fun ?kMSKeyId ->
        fun ?kMSContext -> fun () -> { encryptionType; kMSKeyId; kMSContext }
    let to_value x =
      structure_to_value
        [("EncryptionType",
           (Option.map x.encryptionType ~f:EncryptionType.to_value));
        ("KMSKeyId", (Option.map x.kMSKeyId ~f:String_.to_value));
        ("KMSContext", (Option.map x.kMSContext ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let kMSContext =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "KMSContext") in
      let kMSKeyId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "KMSKeyId") in
      let encryptionType =
        (Option.map ~f:EncryptionType.of_xml)
          (Xml.child xml_arg0 "EncryptionType") in
      make ?kMSContext ?kMSKeyId ?encryptionType ()
    let of_json json =
      let kMSContext = field_map json "KMSContext" String_.of_json in
      let kMSKeyId = field_map json "KMSKeyId" String_.of_json in
      let encryptionType =
        field_map json "EncryptionType" EncryptionType.of_json in
      make ?kMSContext ?kMSKeyId ?encryptionType ()
    let to_json v = composed_to_json to_value v
  end
module StorageClass =
  struct
    type nonrec t =
      | STANDARD 
      | REDUCED_REDUNDANCY 
      | STANDARD_IA 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | STANDARD -> "STANDARD"
      | REDUCED_REDUNDANCY -> "REDUCED_REDUNDANCY"
      | STANDARD_IA -> "STANDARD_IA"
      | Non_static_id s -> s
    let of_string =
      function
      | "STANDARD" -> STANDARD
      | "REDUCED_REDUNDANCY" -> REDUCED_REDUNDANCY
      | "STANDARD_IA" -> STANDARD_IA
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration StorageClass" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"StorageClass" j)
    let to_json = simple_to_json to_value
  end
module Hashmap =
  struct
    type nonrec t = (String_.t * String_.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((String_.of_string chopped),
                              (String_.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (String_.to_value x) |>
                    (fun x -> (String_.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:String_.of_string
        ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module CSVInput =
  struct
    type nonrec t =
      {
      fileHeaderInfo: FileHeaderInfo.t option ;
      comments: String_.t option ;
      quoteEscapeCharacter: String_.t option ;
      recordDelimiter: String_.t option ;
      fieldDelimiter: String_.t option ;
      quoteCharacter: String_.t option }
    let make ?fileHeaderInfo =
      fun ?comments ->
        fun ?quoteEscapeCharacter ->
          fun ?recordDelimiter ->
            fun ?fieldDelimiter ->
              fun ?quoteCharacter ->
                fun () ->
                  {
                    fileHeaderInfo;
                    comments;
                    quoteEscapeCharacter;
                    recordDelimiter;
                    fieldDelimiter;
                    quoteCharacter
                  }
    let to_value x =
      structure_to_value
        [("FileHeaderInfo",
           (Option.map x.fileHeaderInfo ~f:FileHeaderInfo.to_value));
        ("Comments", (Option.map x.comments ~f:String_.to_value));
        ("QuoteEscapeCharacter",
          (Option.map x.quoteEscapeCharacter ~f:String_.to_value));
        ("RecordDelimiter",
          (Option.map x.recordDelimiter ~f:String_.to_value));
        ("FieldDelimiter", (Option.map x.fieldDelimiter ~f:String_.to_value));
        ("QuoteCharacter", (Option.map x.quoteCharacter ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let quoteCharacter =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "QuoteCharacter") in
      let fieldDelimiter =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "FieldDelimiter") in
      let recordDelimiter =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "RecordDelimiter") in
      let quoteEscapeCharacter =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "QuoteEscapeCharacter") in
      let comments =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Comments") in
      let fileHeaderInfo =
        (Option.map ~f:FileHeaderInfo.of_xml)
          (Xml.child xml_arg0 "FileHeaderInfo") in
      make ?quoteCharacter ?fieldDelimiter ?recordDelimiter
        ?quoteEscapeCharacter ?comments ?fileHeaderInfo ()
    let of_json json =
      let quoteCharacter = field_map json "QuoteCharacter" String_.of_json in
      let fieldDelimiter = field_map json "FieldDelimiter" String_.of_json in
      let recordDelimiter = field_map json "RecordDelimiter" String_.of_json in
      let quoteEscapeCharacter =
        field_map json "QuoteEscapeCharacter" String_.of_json in
      let comments = field_map json "Comments" String_.of_json in
      let fileHeaderInfo =
        field_map json "FileHeaderInfo" FileHeaderInfo.of_json in
      make ?quoteCharacter ?fieldDelimiter ?recordDelimiter
        ?quoteEscapeCharacter ?comments ?fileHeaderInfo ()
    let to_json v = composed_to_json to_value v
  end
module CSVOutput =
  struct
    type nonrec t =
      {
      quoteFields: QuoteFields.t option ;
      quoteEscapeCharacter: String_.t option ;
      recordDelimiter: String_.t option ;
      fieldDelimiter: String_.t option ;
      quoteCharacter: String_.t option }
    let make ?quoteFields =
      fun ?quoteEscapeCharacter ->
        fun ?recordDelimiter ->
          fun ?fieldDelimiter ->
            fun ?quoteCharacter ->
              fun () ->
                {
                  quoteFields;
                  quoteEscapeCharacter;
                  recordDelimiter;
                  fieldDelimiter;
                  quoteCharacter
                }
    let to_value x =
      structure_to_value
        [("QuoteFields", (Option.map x.quoteFields ~f:QuoteFields.to_value));
        ("QuoteEscapeCharacter",
          (Option.map x.quoteEscapeCharacter ~f:String_.to_value));
        ("RecordDelimiter",
          (Option.map x.recordDelimiter ~f:String_.to_value));
        ("FieldDelimiter", (Option.map x.fieldDelimiter ~f:String_.to_value));
        ("QuoteCharacter", (Option.map x.quoteCharacter ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let quoteCharacter =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "QuoteCharacter") in
      let fieldDelimiter =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "FieldDelimiter") in
      let recordDelimiter =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "RecordDelimiter") in
      let quoteEscapeCharacter =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "QuoteEscapeCharacter") in
      let quoteFields =
        (Option.map ~f:QuoteFields.of_xml) (Xml.child xml_arg0 "QuoteFields") in
      make ?quoteCharacter ?fieldDelimiter ?recordDelimiter
        ?quoteEscapeCharacter ?quoteFields ()
    let of_json json =
      let quoteCharacter = field_map json "QuoteCharacter" String_.of_json in
      let fieldDelimiter = field_map json "FieldDelimiter" String_.of_json in
      let recordDelimiter = field_map json "RecordDelimiter" String_.of_json in
      let quoteEscapeCharacter =
        field_map json "QuoteEscapeCharacter" String_.of_json in
      let quoteFields = field_map json "QuoteFields" QuoteFields.of_json in
      make ?quoteCharacter ?fieldDelimiter ?recordDelimiter
        ?quoteEscapeCharacter ?quoteFields ()
    let to_json v = composed_to_json to_value v
  end
module NullableLong =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module DateTime =
  struct
    type nonrec t = string
    let context_ = "DateTime"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DateTime" j
    let to_json = simple_to_json to_value
  end
module S3Location =
  struct
    type nonrec t =
      {
      bucketName: String_.t option ;
      prefix: String_.t option ;
      encryption: Encryption.t option ;
      cannedACL: CannedACL.t option ;
      accessControlList: AccessControlPolicyList.t option ;
      tagging: Hashmap.t option ;
      userMetadata: Hashmap.t option ;
      storageClass: StorageClass.t option }
    let make ?bucketName =
      fun ?prefix ->
        fun ?encryption ->
          fun ?cannedACL ->
            fun ?accessControlList ->
              fun ?tagging ->
                fun ?userMetadata ->
                  fun ?storageClass ->
                    fun () ->
                      {
                        bucketName;
                        prefix;
                        encryption;
                        cannedACL;
                        accessControlList;
                        tagging;
                        userMetadata;
                        storageClass
                      }
    let to_value x =
      structure_to_value
        [("BucketName", (Option.map x.bucketName ~f:String_.to_value));
        ("Prefix", (Option.map x.prefix ~f:String_.to_value));
        ("Encryption", (Option.map x.encryption ~f:Encryption.to_value));
        ("CannedACL", (Option.map x.cannedACL ~f:CannedACL.to_value));
        ("AccessControlList",
          (Option.map x.accessControlList ~f:AccessControlPolicyList.to_value));
        ("Tagging", (Option.map x.tagging ~f:Hashmap.to_value));
        ("UserMetadata", (Option.map x.userMetadata ~f:Hashmap.to_value));
        ("StorageClass",
          (Option.map x.storageClass ~f:StorageClass.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let storageClass =
        (Option.map ~f:StorageClass.of_xml)
          (Xml.child xml_arg0 "StorageClass") in
      let userMetadata =
        (Option.map ~f:Hashmap.of_xml) (Xml.child xml_arg0 "UserMetadata") in
      let tagging =
        (Option.map ~f:Hashmap.of_xml) (Xml.child xml_arg0 "Tagging") in
      let accessControlList =
        (Option.map ~f:AccessControlPolicyList.of_xml)
          (Xml.child xml_arg0 "AccessControlList") in
      let cannedACL =
        (Option.map ~f:CannedACL.of_xml) (Xml.child xml_arg0 "CannedACL") in
      let encryption =
        (Option.map ~f:Encryption.of_xml) (Xml.child xml_arg0 "Encryption") in
      let prefix =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Prefix") in
      let bucketName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "BucketName") in
      make ?storageClass ?userMetadata ?tagging ?accessControlList ?cannedACL
        ?encryption ?prefix ?bucketName ()
    let of_json json =
      let storageClass = field_map json "StorageClass" StorageClass.of_json in
      let userMetadata = field_map json "UserMetadata" Hashmap.of_json in
      let tagging = field_map json "Tagging" Hashmap.of_json in
      let accessControlList =
        field_map json "AccessControlList" AccessControlPolicyList.of_json in
      let cannedACL = field_map json "CannedACL" CannedACL.of_json in
      let encryption = field_map json "Encryption" Encryption.of_json in
      let prefix = field_map json "Prefix" String_.of_json in
      let bucketName = field_map json "BucketName" String_.of_json in
      make ?storageClass ?userMetadata ?tagging ?accessControlList ?cannedACL
        ?encryption ?prefix ?bucketName ()
    let to_json v = composed_to_json to_value v
  end
module ExpressionType =
  struct
    type nonrec t =
      | SQL 
      | Non_static_id of string 
    let make i = i
    let to_string = function | SQL -> "SQL" | Non_static_id s -> s
    let of_string = function | "SQL" -> SQL | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ExpressionType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ExpressionType" j)
    let to_json = simple_to_json to_value
  end
module InputSerialization =
  struct
    type nonrec t = {
      csv: CSVInput.t option }
    let make ?csv = fun () -> { csv }
    let to_value x =
      structure_to_value [("csv", (Option.map x.csv ~f:CSVInput.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let csv = (Option.map ~f:CSVInput.of_xml) (Xml.child xml_arg0 "csv") in
      make ?csv ()
    let of_json json =
      let csv = field_map json "csv" CSVInput.of_json in make ?csv ()
    let to_json v = composed_to_json to_value v
  end
module OutputSerialization =
  struct
    type nonrec t = {
      csv: CSVOutput.t option }
    let make ?csv = fun () -> { csv }
    let to_value x =
      structure_to_value [("csv", (Option.map x.csv ~f:CSVOutput.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let csv = (Option.map ~f:CSVOutput.of_xml) (Xml.child xml_arg0 "csv") in
      make ?csv ()
    let of_json json =
      let csv = field_map json "csv" CSVOutput.of_json in make ?csv ()
    let to_json v = composed_to_json to_value v
  end
module DataRetrievalRule =
  struct
    type nonrec t =
      {
      strategy: String_.t option ;
      bytesPerHour: NullableLong.t option }
    let make ?strategy =
      fun ?bytesPerHour -> fun () -> { strategy; bytesPerHour }
    let to_value x =
      structure_to_value
        [("Strategy", (Option.map x.strategy ~f:String_.to_value));
        ("BytesPerHour",
          (Option.map x.bytesPerHour ~f:NullableLong.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bytesPerHour =
        (Option.map ~f:NullableLong.of_xml)
          (Xml.child xml_arg0 "BytesPerHour") in
      let strategy =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Strategy") in
      make ?bytesPerHour ?strategy ()
    let of_json json =
      let bytesPerHour = field_map json "BytesPerHour" NullableLong.of_json in
      let strategy = field_map json "Strategy" String_.of_json in
      make ?bytesPerHour ?strategy ()
    let to_json v = composed_to_json to_value v
  end
module InvalidParameterValueException =
  struct
    type nonrec t =
      {
      type_: String_.t option ;
      code: String_.t option ;
      message: String_.t option }
    let make ?type_ =
      fun ?code -> fun ?message -> fun () -> { type_; code; message }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:String_.to_value));
        ("code", (Option.map x.code ~f:String_.to_value));
        ("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let code = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "code") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "type") in
      make ?message ?code ?type_ ()
    let of_json json =
      let message = field_map json "message" String_.of_json in
      let code = field_map json "code" String_.of_json in
      let type_ = field_map json "type" String_.of_json in
      make ?message ?code ?type_ ()
    let to_json v = composed_to_json to_value v
  end
module MissingParameterValueException =
  struct
    type nonrec t =
      {
      type_: String_.t option ;
      code: String_.t option ;
      message: String_.t option }
    let make ?type_ =
      fun ?code -> fun ?message -> fun () -> { type_; code; message }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:String_.to_value));
        ("code", (Option.map x.code ~f:String_.to_value));
        ("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let code = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "code") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "type") in
      make ?message ?code ?type_ ()
    let of_json json =
      let message = field_map json "message" String_.of_json in
      let code = field_map json "code" String_.of_json in
      let type_ = field_map json "type" String_.of_json in
      make ?message ?code ?type_ ()
    let to_json v = composed_to_json to_value v
  end
module ResourceNotFoundException =
  struct
    type nonrec t =
      {
      type_: String_.t option ;
      code: String_.t option ;
      message: String_.t option }
    let make ?type_ =
      fun ?code -> fun ?message -> fun () -> { type_; code; message }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:String_.to_value));
        ("code", (Option.map x.code ~f:String_.to_value));
        ("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let code = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "code") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "type") in
      make ?message ?code ?type_ ()
    let of_json json =
      let message = field_map json "message" String_.of_json in
      let code = field_map json "code" String_.of_json in
      let type_ = field_map json "type" String_.of_json in
      make ?message ?code ?type_ ()
    let to_json v = composed_to_json to_value v
  end
module ServiceUnavailableException =
  struct
    type nonrec t =
      {
      type_: String_.t option ;
      code: String_.t option ;
      message: String_.t option }
    let make ?type_ =
      fun ?code -> fun ?message -> fun () -> { type_; code; message }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:String_.to_value));
        ("code", (Option.map x.code ~f:String_.to_value));
        ("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let code = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "code") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "type") in
      make ?message ?code ?type_ ()
    let of_json json =
      let message = field_map json "message" String_.of_json in
      let code = field_map json "code" String_.of_json in
      let type_ = field_map json "type" String_.of_json in
      make ?message ?code ?type_ ()
    let to_json v = composed_to_json to_value v
  end
module Long =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module ActionCode =
  struct
    type nonrec t =
      | ArchiveRetrieval 
      | InventoryRetrieval 
      | Select 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | ArchiveRetrieval -> "ArchiveRetrieval"
      | InventoryRetrieval -> "InventoryRetrieval"
      | Select -> "Select"
      | Non_static_id s -> s
    let of_string =
      function
      | "ArchiveRetrieval" -> ArchiveRetrieval
      | "InventoryRetrieval" -> InventoryRetrieval
      | "Select" -> Select
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ActionCode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ActionCode" j)
    let to_json = simple_to_json to_value
  end
module InventoryRetrievalJobDescription =
  struct
    type nonrec t =
      {
      format: String_.t option ;
      startDate: DateTime.t option ;
      endDate: DateTime.t option ;
      limit: String_.t option ;
      marker: String_.t option }
    let make ?format =
      fun ?startDate ->
        fun ?endDate ->
          fun ?limit ->
            fun ?marker ->
              fun () -> { format; startDate; endDate; limit; marker }
    let to_value x =
      structure_to_value
        [("Format", (Option.map x.format ~f:String_.to_value));
        ("StartDate", (Option.map x.startDate ~f:DateTime.to_value));
        ("EndDate", (Option.map x.endDate ~f:DateTime.to_value));
        ("Limit", (Option.map x.limit ~f:String_.to_value));
        ("Marker", (Option.map x.marker ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let marker =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Marker") in
      let limit = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Limit") in
      let endDate =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "EndDate") in
      let startDate =
        (Option.map ~f:DateTime.of_xml) (Xml.child xml_arg0 "StartDate") in
      let format =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Format") in
      make ?marker ?limit ?endDate ?startDate ?format ()
    let of_json json =
      let marker = field_map json "Marker" String_.of_json in
      let limit = field_map json "Limit" String_.of_json in
      let endDate = field_map json "EndDate" DateTime.of_json in
      let startDate = field_map json "StartDate" DateTime.of_json in
      let format = field_map json "Format" String_.of_json in
      make ?marker ?limit ?endDate ?startDate ?format ()
    let to_json v = composed_to_json to_value v
  end
module OutputLocation =
  struct
    type nonrec t = {
      s3: S3Location.t option }
    let make ?s3 = fun () -> { s3 }
    let to_value x =
      structure_to_value [("S3", (Option.map x.s3 ~f:S3Location.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let s3 = (Option.map ~f:S3Location.of_xml) (Xml.child xml_arg0 "S3") in
      make ?s3 ()
    let of_json json =
      let s3 = field_map json "S3" S3Location.of_json in make ?s3 ()
    let to_json v = composed_to_json to_value v
  end
module SelectParameters =
  struct
    type nonrec t =
      {
      inputSerialization: InputSerialization.t option ;
      expressionType: ExpressionType.t option ;
      expression: String_.t option ;
      outputSerialization: OutputSerialization.t option }
    let make ?inputSerialization =
      fun ?expressionType ->
        fun ?expression ->
          fun ?outputSerialization ->
            fun () ->
              {
                inputSerialization;
                expressionType;
                expression;
                outputSerialization
              }
    let to_value x =
      structure_to_value
        [("InputSerialization",
           (Option.map x.inputSerialization ~f:InputSerialization.to_value));
        ("ExpressionType",
          (Option.map x.expressionType ~f:ExpressionType.to_value));
        ("Expression", (Option.map x.expression ~f:String_.to_value));
        ("OutputSerialization",
          (Option.map x.outputSerialization ~f:OutputSerialization.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let outputSerialization =
        (Option.map ~f:OutputSerialization.of_xml)
          (Xml.child xml_arg0 "OutputSerialization") in
      let expression =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Expression") in
      let expressionType =
        (Option.map ~f:ExpressionType.of_xml)
          (Xml.child xml_arg0 "ExpressionType") in
      let inputSerialization =
        (Option.map ~f:InputSerialization.of_xml)
          (Xml.child xml_arg0 "InputSerialization") in
      make ?outputSerialization ?expression ?expressionType
        ?inputSerialization ()
    let of_json json =
      let outputSerialization =
        field_map json "OutputSerialization" OutputSerialization.of_json in
      let expression = field_map json "Expression" String_.of_json in
      let expressionType =
        field_map json "ExpressionType" ExpressionType.of_json in
      let inputSerialization =
        field_map json "InputSerialization" InputSerialization.of_json in
      make ?outputSerialization ?expression ?expressionType
        ?inputSerialization ()
    let to_json v = composed_to_json to_value v
  end
module Size =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module StatusCode =
  struct
    type nonrec t =
      | InProgress 
      | Succeeded 
      | Failed 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | InProgress -> "InProgress"
      | Succeeded -> "Succeeded"
      | Failed -> "Failed"
      | Non_static_id s -> s
    let of_string =
      function
      | "InProgress" -> InProgress
      | "Succeeded" -> Succeeded
      | "Failed" -> Failed
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration StatusCode" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"StatusCode" j)
    let to_json = simple_to_json to_value
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module NotificationEventList =
  struct
    type nonrec t = String_.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"NotificationEventList" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module DataRetrievalRulesList =
  struct
    type nonrec t = DataRetrievalRule.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:DataRetrievalRule.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DataRetrievalRule.of_xml)
    let of_json j =
      list_of_json ~kind:"DataRetrievalRulesList"
        ~of_json:DataRetrievalRule.of_json j
    let to_json v = composed_to_json to_value v
  end
module DescribeVaultOutput =
  struct
    type nonrec t =
      {
      vaultARN: String_.t option ;
      vaultName: String_.t option ;
      creationDate: String_.t option ;
      lastInventoryDate: String_.t option ;
      numberOfArchives: Long.t option ;
      sizeInBytes: Long.t option }
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?vaultARN =
      fun ?vaultName ->
        fun ?creationDate ->
          fun ?lastInventoryDate ->
            fun ?numberOfArchives ->
              fun ?sizeInBytes ->
                fun () ->
                  {
                    vaultARN;
                    vaultName;
                    creationDate;
                    lastInventoryDate;
                    numberOfArchives;
                    sizeInBytes
                  }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("VaultARN", (Option.map x.vaultARN ~f:String_.to_value));
        ("VaultName", (Option.map x.vaultName ~f:String_.to_value));
        ("CreationDate", (Option.map x.creationDate ~f:String_.to_value));
        ("LastInventoryDate",
          (Option.map x.lastInventoryDate ~f:String_.to_value));
        ("NumberOfArchives",
          (Option.map x.numberOfArchives ~f:Long.to_value));
        ("SizeInBytes", (Option.map x.sizeInBytes ~f:Long.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sizeInBytes =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "SizeInBytes") in
      let numberOfArchives =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "NumberOfArchives") in
      let lastInventoryDate =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "LastInventoryDate") in
      let creationDate =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "CreationDate") in
      let vaultName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "VaultName") in
      let vaultARN =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "VaultARN") in
      make ?sizeInBytes ?numberOfArchives ?lastInventoryDate ?creationDate
        ?vaultName ?vaultARN ()
    let of_json json =
      let sizeInBytes = field_map json "SizeInBytes" Long.of_json in
      let numberOfArchives = field_map json "NumberOfArchives" Long.of_json in
      let lastInventoryDate =
        field_map json "LastInventoryDate" String_.of_json in
      let creationDate = field_map json "CreationDate" String_.of_json in
      let vaultName = field_map json "VaultName" String_.of_json in
      let vaultARN = field_map json "VaultARN" String_.of_json in
      make ?sizeInBytes ?numberOfArchives ?lastInventoryDate ?creationDate
        ?vaultName ?vaultARN ()
    let to_json v = composed_to_json to_value v
  end
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module ProvisionedCapacityDescription =
  struct
    type nonrec t =
      {
      capacityId: String_.t option ;
      startDate: String_.t option ;
      expirationDate: String_.t option }
    let make ?capacityId =
      fun ?startDate ->
        fun ?expirationDate ->
          fun () -> { capacityId; startDate; expirationDate }
    let to_value x =
      structure_to_value
        [("CapacityId", (Option.map x.capacityId ~f:String_.to_value));
        ("StartDate", (Option.map x.startDate ~f:String_.to_value));
        ("ExpirationDate", (Option.map x.expirationDate ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let expirationDate =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ExpirationDate") in
      let startDate =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "StartDate") in
      let capacityId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "CapacityId") in
      make ?expirationDate ?startDate ?capacityId ()
    let of_json json =
      let expirationDate = field_map json "ExpirationDate" String_.of_json in
      let startDate = field_map json "StartDate" String_.of_json in
      let capacityId = field_map json "CapacityId" String_.of_json in
      make ?expirationDate ?startDate ?capacityId ()
    let to_json v = composed_to_json to_value v
  end
module PartListElement =
  struct
    type nonrec t =
      {
      rangeInBytes: String_.t option ;
      sHA256TreeHash: String_.t option }
    let make ?rangeInBytes =
      fun ?sHA256TreeHash -> fun () -> { rangeInBytes; sHA256TreeHash }
    let to_value x =
      structure_to_value
        [("RangeInBytes", (Option.map x.rangeInBytes ~f:String_.to_value));
        ("SHA256TreeHash", (Option.map x.sHA256TreeHash ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let sHA256TreeHash =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "SHA256TreeHash") in
      let rangeInBytes =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "RangeInBytes") in
      make ?sHA256TreeHash ?rangeInBytes ()
    let of_json json =
      let sHA256TreeHash = field_map json "SHA256TreeHash" String_.of_json in
      let rangeInBytes = field_map json "RangeInBytes" String_.of_json in
      make ?sHA256TreeHash ?rangeInBytes ()
    let to_json v = composed_to_json to_value v
  end
module UploadListElement =
  struct
    type nonrec t =
      {
      multipartUploadId: String_.t option ;
      vaultARN: String_.t option ;
      archiveDescription: String_.t option ;
      partSizeInBytes: Long.t option ;
      creationDate: String_.t option }
    let make ?multipartUploadId =
      fun ?vaultARN ->
        fun ?archiveDescription ->
          fun ?partSizeInBytes ->
            fun ?creationDate ->
              fun () ->
                {
                  multipartUploadId;
                  vaultARN;
                  archiveDescription;
                  partSizeInBytes;
                  creationDate
                }
    let to_value x =
      structure_to_value
        [("MultipartUploadId",
           (Option.map x.multipartUploadId ~f:String_.to_value));
        ("VaultARN", (Option.map x.vaultARN ~f:String_.to_value));
        ("ArchiveDescription",
          (Option.map x.archiveDescription ~f:String_.to_value));
        ("PartSizeInBytes", (Option.map x.partSizeInBytes ~f:Long.to_value));
        ("CreationDate", (Option.map x.creationDate ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creationDate =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "CreationDate") in
      let partSizeInBytes =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "PartSizeInBytes") in
      let archiveDescription =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "ArchiveDescription") in
      let vaultARN =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "VaultARN") in
      let multipartUploadId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "MultipartUploadId") in
      make ?creationDate ?partSizeInBytes ?archiveDescription ?vaultARN
        ?multipartUploadId ()
    let of_json json =
      let creationDate = field_map json "CreationDate" String_.of_json in
      let partSizeInBytes = field_map json "PartSizeInBytes" Long.of_json in
      let archiveDescription =
        field_map json "ArchiveDescription" String_.of_json in
      let vaultARN = field_map json "VaultARN" String_.of_json in
      let multipartUploadId =
        field_map json "MultipartUploadId" String_.of_json in
      make ?creationDate ?partSizeInBytes ?archiveDescription ?vaultARN
        ?multipartUploadId ()
    let to_json v = composed_to_json to_value v
  end
module GlacierJobDescription =
  struct
    type nonrec t =
      {
      jobId: String_.t option ;
      jobDescription: String_.t option ;
      action: ActionCode.t option ;
      archiveId: String_.t option ;
      vaultARN: String_.t option ;
      creationDate: String_.t option ;
      completed: Boolean.t option ;
      statusCode: StatusCode.t option ;
      statusMessage: String_.t option ;
      archiveSizeInBytes: Size.t option ;
      inventorySizeInBytes: Size.t option ;
      sNSTopic: String_.t option ;
      completionDate: String_.t option ;
      sHA256TreeHash: String_.t option ;
      archiveSHA256TreeHash: String_.t option ;
      retrievalByteRange: String_.t option ;
      tier: String_.t option ;
      inventoryRetrievalParameters: InventoryRetrievalJobDescription.t option ;
      jobOutputPath: String_.t option ;
      selectParameters: SelectParameters.t option ;
      outputLocation: OutputLocation.t option }
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?jobId =
      fun ?jobDescription ->
        fun ?action ->
          fun ?archiveId ->
            fun ?vaultARN ->
              fun ?creationDate ->
                fun ?completed ->
                  fun ?statusCode ->
                    fun ?statusMessage ->
                      fun ?archiveSizeInBytes ->
                        fun ?inventorySizeInBytes ->
                          fun ?sNSTopic ->
                            fun ?completionDate ->
                              fun ?sHA256TreeHash ->
                                fun ?archiveSHA256TreeHash ->
                                  fun ?retrievalByteRange ->
                                    fun ?tier ->
                                      fun ?inventoryRetrievalParameters ->
                                        fun ?jobOutputPath ->
                                          fun ?selectParameters ->
                                            fun ?outputLocation ->
                                              fun () ->
                                                {
                                                  jobId;
                                                  jobDescription;
                                                  action;
                                                  archiveId;
                                                  vaultARN;
                                                  creationDate;
                                                  completed;
                                                  statusCode;
                                                  statusMessage;
                                                  archiveSizeInBytes;
                                                  inventorySizeInBytes;
                                                  sNSTopic;
                                                  completionDate;
                                                  sHA256TreeHash;
                                                  archiveSHA256TreeHash;
                                                  retrievalByteRange;
                                                  tier;
                                                  inventoryRetrievalParameters;
                                                  jobOutputPath;
                                                  selectParameters;
                                                  outputLocation
                                                }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("JobId", (Option.map x.jobId ~f:String_.to_value));
        ("JobDescription", (Option.map x.jobDescription ~f:String_.to_value));
        ("Action", (Option.map x.action ~f:ActionCode.to_value));
        ("ArchiveId", (Option.map x.archiveId ~f:String_.to_value));
        ("VaultARN", (Option.map x.vaultARN ~f:String_.to_value));
        ("CreationDate", (Option.map x.creationDate ~f:String_.to_value));
        ("Completed", (Option.map x.completed ~f:Boolean.to_value));
        ("StatusCode", (Option.map x.statusCode ~f:StatusCode.to_value));
        ("StatusMessage", (Option.map x.statusMessage ~f:String_.to_value));
        ("ArchiveSizeInBytes",
          (Option.map x.archiveSizeInBytes ~f:Size.to_value));
        ("InventorySizeInBytes",
          (Option.map x.inventorySizeInBytes ~f:Size.to_value));
        ("SNSTopic", (Option.map x.sNSTopic ~f:String_.to_value));
        ("CompletionDate", (Option.map x.completionDate ~f:String_.to_value));
        ("SHA256TreeHash", (Option.map x.sHA256TreeHash ~f:String_.to_value));
        ("ArchiveSHA256TreeHash",
          (Option.map x.archiveSHA256TreeHash ~f:String_.to_value));
        ("RetrievalByteRange",
          (Option.map x.retrievalByteRange ~f:String_.to_value));
        ("Tier", (Option.map x.tier ~f:String_.to_value));
        ("InventoryRetrievalParameters",
          (Option.map x.inventoryRetrievalParameters
             ~f:InventoryRetrievalJobDescription.to_value));
        ("JobOutputPath", (Option.map x.jobOutputPath ~f:String_.to_value));
        ("SelectParameters",
          (Option.map x.selectParameters ~f:SelectParameters.to_value));
        ("OutputLocation",
          (Option.map x.outputLocation ~f:OutputLocation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let outputLocation =
        (Option.map ~f:OutputLocation.of_xml)
          (Xml.child xml_arg0 "OutputLocation") in
      let selectParameters =
        (Option.map ~f:SelectParameters.of_xml)
          (Xml.child xml_arg0 "SelectParameters") in
      let jobOutputPath =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "JobOutputPath") in
      let inventoryRetrievalParameters =
        (Option.map ~f:InventoryRetrievalJobDescription.of_xml)
          (Xml.child xml_arg0 "InventoryRetrievalParameters") in
      let tier = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Tier") in
      let retrievalByteRange =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "RetrievalByteRange") in
      let archiveSHA256TreeHash =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "ArchiveSHA256TreeHash") in
      let sHA256TreeHash =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "SHA256TreeHash") in
      let completionDate =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "CompletionDate") in
      let sNSTopic =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "SNSTopic") in
      let inventorySizeInBytes =
        (Option.map ~f:Size.of_xml)
          (Xml.child xml_arg0 "InventorySizeInBytes") in
      let archiveSizeInBytes =
        (Option.map ~f:Size.of_xml) (Xml.child xml_arg0 "ArchiveSizeInBytes") in
      let statusMessage =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "StatusMessage") in
      let statusCode =
        (Option.map ~f:StatusCode.of_xml) (Xml.child xml_arg0 "StatusCode") in
      let completed =
        (Option.map ~f:Boolean.of_xml) (Xml.child xml_arg0 "Completed") in
      let creationDate =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "CreationDate") in
      let vaultARN =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "VaultARN") in
      let archiveId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ArchiveId") in
      let action =
        (Option.map ~f:ActionCode.of_xml) (Xml.child xml_arg0 "Action") in
      let jobDescription =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "JobDescription") in
      let jobId = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "JobId") in
      make ?outputLocation ?selectParameters ?jobOutputPath
        ?inventoryRetrievalParameters ?tier ?retrievalByteRange
        ?archiveSHA256TreeHash ?sHA256TreeHash ?completionDate ?sNSTopic
        ?inventorySizeInBytes ?archiveSizeInBytes ?statusMessage ?statusCode
        ?completed ?creationDate ?vaultARN ?archiveId ?action ?jobDescription
        ?jobId ()
    let of_json json =
      let outputLocation =
        field_map json "OutputLocation" OutputLocation.of_json in
      let selectParameters =
        field_map json "SelectParameters" SelectParameters.of_json in
      let jobOutputPath = field_map json "JobOutputPath" String_.of_json in
      let inventoryRetrievalParameters =
        field_map json "InventoryRetrievalParameters"
          InventoryRetrievalJobDescription.of_json in
      let tier = field_map json "Tier" String_.of_json in
      let retrievalByteRange =
        field_map json "RetrievalByteRange" String_.of_json in
      let archiveSHA256TreeHash =
        field_map json "ArchiveSHA256TreeHash" String_.of_json in
      let sHA256TreeHash = field_map json "SHA256TreeHash" String_.of_json in
      let completionDate = field_map json "CompletionDate" String_.of_json in
      let sNSTopic = field_map json "SNSTopic" String_.of_json in
      let inventorySizeInBytes =
        field_map json "InventorySizeInBytes" Size.of_json in
      let archiveSizeInBytes =
        field_map json "ArchiveSizeInBytes" Size.of_json in
      let statusMessage = field_map json "StatusMessage" String_.of_json in
      let statusCode = field_map json "StatusCode" StatusCode.of_json in
      let completed = field_map json "Completed" Boolean.of_json in
      let creationDate = field_map json "CreationDate" String_.of_json in
      let vaultARN = field_map json "VaultARN" String_.of_json in
      let archiveId = field_map json "ArchiveId" String_.of_json in
      let action = field_map json "Action" ActionCode.of_json in
      let jobDescription = field_map json "JobDescription" String_.of_json in
      let jobId = field_map json "JobId" String_.of_json in
      make ?outputLocation ?selectParameters ?jobOutputPath
        ?inventoryRetrievalParameters ?tier ?retrievalByteRange
        ?archiveSHA256TreeHash ?sHA256TreeHash ?completionDate ?sNSTopic
        ?inventorySizeInBytes ?archiveSizeInBytes ?statusMessage ?statusCode
        ?completed ?creationDate ?vaultARN ?archiveId ?action ?jobDescription
        ?jobId ()
    let to_json v = composed_to_json to_value v
  end
module InventoryRetrievalJobInput =
  struct
    type nonrec t =
      {
      startDate: String_.t option ;
      endDate: String_.t option ;
      limit: String_.t option ;
      marker: String_.t option }
    let make ?startDate =
      fun ?endDate ->
        fun ?limit ->
          fun ?marker -> fun () -> { startDate; endDate; limit; marker }
    let to_value x =
      structure_to_value
        [("StartDate", (Option.map x.startDate ~f:String_.to_value));
        ("EndDate", (Option.map x.endDate ~f:String_.to_value));
        ("Limit", (Option.map x.limit ~f:String_.to_value));
        ("Marker", (Option.map x.marker ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let marker =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Marker") in
      let limit = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Limit") in
      let endDate =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "EndDate") in
      let startDate =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "StartDate") in
      make ?marker ?limit ?endDate ?startDate ()
    let of_json json =
      let marker = field_map json "Marker" String_.of_json in
      let limit = field_map json "Limit" String_.of_json in
      let endDate = field_map json "EndDate" String_.of_json in
      let startDate = field_map json "StartDate" String_.of_json in
      make ?marker ?limit ?endDate ?startDate ()
    let to_json v = composed_to_json to_value v
  end
module RequestTimeoutException =
  struct
    type nonrec t =
      {
      type_: String_.t option ;
      code: String_.t option ;
      message: String_.t option }
    let make ?type_ =
      fun ?code -> fun ?message -> fun () -> { type_; code; message }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:String_.to_value));
        ("code", (Option.map x.code ~f:String_.to_value));
        ("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let code = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "code") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "type") in
      make ?message ?code ?type_ ()
    let of_json json =
      let message = field_map json "message" String_.of_json in
      let code = field_map json "code" String_.of_json in
      let type_ = field_map json "type" String_.of_json in
      make ?message ?code ?type_ ()
    let to_json v = composed_to_json to_value v
  end
module Stream =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end
module VaultNotificationConfig =
  struct
    type nonrec t =
      {
      sNSTopic: String_.t option ;
      events: NotificationEventList.t option }
    let make ?sNSTopic = fun ?events -> fun () -> { sNSTopic; events }
    let to_value x =
      structure_to_value
        [("SNSTopic", (Option.map x.sNSTopic ~f:String_.to_value));
        ("Events", (Option.map x.events ~f:NotificationEventList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let events =
        (Option.map ~f:NotificationEventList.of_xml)
          (Xml.child xml_arg0 "Events") in
      let sNSTopic =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "SNSTopic") in
      make ?events ?sNSTopic ()
    let of_json json =
      let events = field_map json "Events" NotificationEventList.of_json in
      let sNSTopic = field_map json "SNSTopic" String_.of_json in
      make ?events ?sNSTopic ()
    let to_json v = composed_to_json to_value v
  end
module VaultAccessPolicy =
  struct
    type nonrec t = {
      policy: String_.t option }
    let make ?policy = fun () -> { policy }
    let to_value x =
      structure_to_value
        [("Policy", (Option.map x.policy ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policy =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Policy") in
      make ?policy ()
    let of_json json =
      let policy = field_map json "Policy" String_.of_json in make ?policy ()
    let to_json v = composed_to_json to_value v
  end
module DataRetrievalPolicy =
  struct
    type nonrec t = {
      rules: DataRetrievalRulesList.t option }
    let make ?rules = fun () -> { rules }
    let to_value x =
      structure_to_value
        [("Rules", (Option.map x.rules ~f:DataRetrievalRulesList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let rules =
        (Option.map ~f:DataRetrievalRulesList.of_xml)
          (Xml.child xml_arg0 "Rules") in
      make ?rules ()
    let of_json json =
      let rules = field_map json "Rules" DataRetrievalRulesList.of_json in
      make ?rules ()
    let to_json v = composed_to_json to_value v
  end
module TagKeyList =
  struct
    type nonrec t = String_.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j =
      list_of_json ~kind:"TagKeyList" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module LimitExceededException =
  struct
    type nonrec t =
      {
      type_: String_.t option ;
      code: String_.t option ;
      message: String_.t option }
    let make ?type_ =
      fun ?code -> fun ?message -> fun () -> { type_; code; message }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:String_.to_value));
        ("code", (Option.map x.code ~f:String_.to_value));
        ("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let code = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "code") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "type") in
      make ?message ?code ?type_ ()
    let of_json json =
      let message = field_map json "message" String_.of_json in
      let code = field_map json "code" String_.of_json in
      let type_ = field_map json "type" String_.of_json in
      make ?message ?code ?type_ ()
    let to_json v = composed_to_json to_value v
  end
module VaultList =
  struct
    type nonrec t = DescribeVaultOutput.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:DescribeVaultOutput.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DescribeVaultOutput.of_xml)
    let of_json j =
      list_of_json ~kind:"VaultList" ~of_json:DescribeVaultOutput.of_json j
    let to_json v = composed_to_json to_value v
  end
module TagMap =
  struct
    type nonrec t = (TagKey.t * TagValue.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((TagKey.of_string chopped),
                              (TagValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (TagKey.to_value x) |>
                    (fun x -> (TagValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:TagKey.of_string
        ~of_json:TagValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProvisionedCapacityList =
  struct
    type nonrec t = ProvisionedCapacityDescription.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ProvisionedCapacityDescription.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ProvisionedCapacityDescription.of_xml)
    let of_json j =
      list_of_json ~kind:"ProvisionedCapacityList"
        ~of_json:ProvisionedCapacityDescription.of_json j
    let to_json v = composed_to_json to_value v
  end
module PartList =
  struct
    type nonrec t = PartListElement.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:PartListElement.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:PartListElement.of_xml)
    let of_json j =
      list_of_json ~kind:"PartList" ~of_json:PartListElement.of_json j
    let to_json v = composed_to_json to_value v
  end
module UploadsList =
  struct
    type nonrec t = UploadListElement.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:UploadListElement.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:UploadListElement.of_xml)
    let of_json j =
      list_of_json ~kind:"UploadsList" ~of_json:UploadListElement.of_json j
    let to_json v = composed_to_json to_value v
  end
module JobList =
  struct
    type nonrec t = GlacierJobDescription.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:GlacierJobDescription.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GlacierJobDescription.of_xml)
    let of_json j =
      list_of_json ~kind:"JobList" ~of_json:GlacierJobDescription.of_json j
    let to_json v = composed_to_json to_value v
  end
module VaultLockPolicy =
  struct
    type nonrec t = {
      policy: String_.t option }
    let make ?policy = fun () -> { policy }
    let to_value x =
      structure_to_value
        [("Policy", (Option.map x.policy ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policy =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Policy") in
      make ?policy ()
    let of_json json =
      let policy = field_map json "Policy" String_.of_json in make ?policy ()
    let to_json v = composed_to_json to_value v
  end
module InsufficientCapacityException =
  struct
    type nonrec t =
      {
      type_: String_.t option ;
      code: String_.t option ;
      message: String_.t option }
    let make ?type_ =
      fun ?code -> fun ?message -> fun () -> { type_; code; message }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:String_.to_value));
        ("code", (Option.map x.code ~f:String_.to_value));
        ("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let code = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "code") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "type") in
      make ?message ?code ?type_ ()
    let of_json json =
      let message = field_map json "message" String_.of_json in
      let code = field_map json "code" String_.of_json in
      let type_ = field_map json "type" String_.of_json in
      make ?message ?code ?type_ ()
    let to_json v = composed_to_json to_value v
  end
module PolicyEnforcedException =
  struct
    type nonrec t =
      {
      type_: String_.t option ;
      code: String_.t option ;
      message: String_.t option }
    let make ?type_ =
      fun ?code -> fun ?message -> fun () -> { type_; code; message }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:String_.to_value));
        ("code", (Option.map x.code ~f:String_.to_value));
        ("message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "message") in
      let code = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "code") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "type") in
      make ?message ?code ?type_ ()
    let of_json json =
      let message = field_map json "message" String_.of_json in
      let code = field_map json "code" String_.of_json in
      let type_ = field_map json "type" String_.of_json in
      make ?message ?code ?type_ ()
    let to_json v = composed_to_json to_value v
  end
module JobParameters =
  struct
    type nonrec t =
      {
      format: String_.t option ;
      type_: String_.t option ;
      archiveId: String_.t option ;
      description: String_.t option ;
      sNSTopic: String_.t option ;
      retrievalByteRange: String_.t option ;
      tier: String_.t option ;
      inventoryRetrievalParameters: InventoryRetrievalJobInput.t option ;
      selectParameters: SelectParameters.t option ;
      outputLocation: OutputLocation.t option }
    let make ?format =
      fun ?type_ ->
        fun ?archiveId ->
          fun ?description ->
            fun ?sNSTopic ->
              fun ?retrievalByteRange ->
                fun ?tier ->
                  fun ?inventoryRetrievalParameters ->
                    fun ?selectParameters ->
                      fun ?outputLocation ->
                        fun () ->
                          {
                            format;
                            type_;
                            archiveId;
                            description;
                            sNSTopic;
                            retrievalByteRange;
                            tier;
                            inventoryRetrievalParameters;
                            selectParameters;
                            outputLocation
                          }
    let to_value x =
      structure_to_value
        [("Format", (Option.map x.format ~f:String_.to_value));
        ("Type", (Option.map x.type_ ~f:String_.to_value));
        ("ArchiveId", (Option.map x.archiveId ~f:String_.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("SNSTopic", (Option.map x.sNSTopic ~f:String_.to_value));
        ("RetrievalByteRange",
          (Option.map x.retrievalByteRange ~f:String_.to_value));
        ("Tier", (Option.map x.tier ~f:String_.to_value));
        ("InventoryRetrievalParameters",
          (Option.map x.inventoryRetrievalParameters
             ~f:InventoryRetrievalJobInput.to_value));
        ("SelectParameters",
          (Option.map x.selectParameters ~f:SelectParameters.to_value));
        ("OutputLocation",
          (Option.map x.outputLocation ~f:OutputLocation.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let outputLocation =
        (Option.map ~f:OutputLocation.of_xml)
          (Xml.child xml_arg0 "OutputLocation") in
      let selectParameters =
        (Option.map ~f:SelectParameters.of_xml)
          (Xml.child xml_arg0 "SelectParameters") in
      let inventoryRetrievalParameters =
        (Option.map ~f:InventoryRetrievalJobInput.of_xml)
          (Xml.child xml_arg0 "InventoryRetrievalParameters") in
      let tier = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Tier") in
      let retrievalByteRange =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "RetrievalByteRange") in
      let sNSTopic =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "SNSTopic") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let archiveId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ArchiveId") in
      let type_ = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Type") in
      let format =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Format") in
      make ?outputLocation ?selectParameters ?inventoryRetrievalParameters
        ?tier ?retrievalByteRange ?sNSTopic ?description ?archiveId ?type_
        ?format ()
    let of_json json =
      let outputLocation =
        field_map json "OutputLocation" OutputLocation.of_json in
      let selectParameters =
        field_map json "SelectParameters" SelectParameters.of_json in
      let inventoryRetrievalParameters =
        field_map json "InventoryRetrievalParameters"
          InventoryRetrievalJobInput.of_json in
      let tier = field_map json "Tier" String_.of_json in
      let retrievalByteRange =
        field_map json "RetrievalByteRange" String_.of_json in
      let sNSTopic = field_map json "SNSTopic" String_.of_json in
      let description = field_map json "Description" String_.of_json in
      let archiveId = field_map json "ArchiveId" String_.of_json in
      let type_ = field_map json "Type" String_.of_json in
      let format = field_map json "Format" String_.of_json in
      make ?outputLocation ?selectParameters ?inventoryRetrievalParameters
        ?tier ?retrievalByteRange ?sNSTopic ?description ?archiveId ?type_
        ?format ()
    let to_json v = composed_to_json to_value v
  end
module Httpstatus =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for httpstatus" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module UploadMultipartPartOutput =
  struct
    type nonrec t = {
      checksum: String_.t option }
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `RequestTimeoutException of RequestTimeoutException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?checksum = fun () -> { checksum }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "RequestTimeoutException" ->
          `RequestTimeoutException (RequestTimeoutException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "RequestTimeoutException" ->
          `RequestTimeoutException (RequestTimeoutException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `RequestTimeoutException e ->
          `Assoc
            [("error", (`String "RequestTimeoutException"));
            ("details", (RequestTimeoutException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body =
      ((fun (xs, pipe) ->
          make
            ?checksum:(Option.map
                         ((List.Assoc.find ~equal:String.Caseless.equal) xs
                            "x-amz-sha256-tree-hash") ~f:String_.of_string)
            ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("x-amz-sha256-tree-hash",
           (Option.map x.checksum ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let checksum =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amz-sha256-tree-hash") in
      make ?checksum ()
    let of_json json =
      let checksum = field_map json "checksum" String_.of_json in
      make ?checksum ()
    let to_json v = composed_to_json to_value v
  end
module UploadMultipartPartInput =
  struct
    type nonrec t =
      {
      accountId: String_.t ;
      vaultName: String_.t ;
      uploadId: String_.t ;
      checksum: String_.t option ;
      range: String_.t option ;
      body: Stream.t option }
    let context_ = "UploadMultipartPartInput"
    let make ?checksum =
      fun ?range ->
        fun ?body ->
          fun ~accountId ->
            fun ~vaultName ->
              fun ~uploadId ->
                fun () ->
                  { checksum; range; body; accountId; vaultName; uploadId }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)));
        ("uploadId", (Some (String_.to_value x.uploadId)));
        ("x-amz-sha256-tree-hash",
          (Option.map x.checksum ~f:String_.to_value));
        ("Content-Range", (Option.map x.range ~f:String_.to_value));
        ("body", (Option.map x.body ~f:Stream.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let body = (Option.map ~f:Stream.of_xml) (Xml.child xml_arg0 "body") in
      let range =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Content-Range") in
      let checksum =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amz-sha256-tree-hash") in
      let uploadId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "uploadId") in
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ?body ?range ?checksum ~uploadId ~vaultName ~accountId ()
    let of_json json =
      let body = field_map json "body" Stream.of_json in
      let range = field_map json "range" String_.of_json in
      let checksum = field_map json "checksum" String_.of_json in
      let uploadId = field_map_exn json "uploadId" String_.of_json in
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ?body ?range ?checksum ~uploadId ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module UploadArchiveInput =
  struct
    type nonrec t =
      {
      vaultName: String_.t ;
      accountId: String_.t ;
      archiveDescription: String_.t option ;
      checksum: String_.t option ;
      body: Stream.t option }
    let context_ = "UploadArchiveInput"
    let make ?archiveDescription =
      fun ?checksum ->
        fun ?body ->
          fun ~vaultName ->
            fun ~accountId ->
              fun () ->
                { archiveDescription; checksum; body; vaultName; accountId }
    let to_value x =
      structure_to_value
        [("vaultName", (Some (String_.to_value x.vaultName)));
        ("accountId", (Some (String_.to_value x.accountId)));
        ("x-amz-archive-description",
          (Option.map x.archiveDescription ~f:String_.to_value));
        ("x-amz-sha256-tree-hash",
          (Option.map x.checksum ~f:String_.to_value));
        ("body", (Option.map x.body ~f:Stream.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let body = (Option.map ~f:Stream.of_xml) (Xml.child xml_arg0 "body") in
      let checksum =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amz-sha256-tree-hash") in
      let archiveDescription =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amz-archive-description") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      make ?body ?checksum ?archiveDescription ~accountId ~vaultName ()
    let of_json json =
      let body = field_map json "body" Stream.of_json in
      let checksum = field_map json "checksum" String_.of_json in
      let archiveDescription =
        field_map json "archiveDescription" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      make ?body ?checksum ?archiveDescription ~accountId ~vaultName ()
    let to_json v = composed_to_json to_value v
  end
module SetVaultNotificationsInput =
  struct
    type nonrec t =
      {
      accountId: String_.t ;
      vaultName: String_.t ;
      vaultNotificationConfig: VaultNotificationConfig.t option }
    let context_ = "SetVaultNotificationsInput"
    let make ?vaultNotificationConfig =
      fun ~accountId ->
        fun ~vaultName ->
          fun () -> { vaultNotificationConfig; accountId; vaultName }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)));
        ("vaultNotificationConfig",
          (Option.map x.vaultNotificationConfig
             ~f:VaultNotificationConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vaultNotificationConfig =
        (Option.map ~f:VaultNotificationConfig.of_xml)
          (Xml.child xml_arg0 "vaultNotificationConfig") in
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ?vaultNotificationConfig ~vaultName ~accountId ()
    let of_json json =
      let vaultNotificationConfig =
        field_map json "vaultNotificationConfig"
          VaultNotificationConfig.of_json in
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ?vaultNotificationConfig ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module SetVaultAccessPolicyInput =
  struct
    type nonrec t =
      {
      accountId: String_.t ;
      vaultName: String_.t ;
      policy: VaultAccessPolicy.t option }
    let context_ = "SetVaultAccessPolicyInput"
    let make ?policy =
      fun ~accountId ->
        fun ~vaultName -> fun () -> { policy; accountId; vaultName }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)));
        ("policy", (Option.map x.policy ~f:VaultAccessPolicy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policy =
        (Option.map ~f:VaultAccessPolicy.of_xml)
          (Xml.child xml_arg0 "policy") in
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ?policy ~vaultName ~accountId ()
    let of_json json =
      let policy = field_map json "policy" VaultAccessPolicy.of_json in
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ?policy ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module SetDataRetrievalPolicyInput =
  struct
    type nonrec t =
      {
      accountId: String_.t ;
      policy: DataRetrievalPolicy.t option }
    let context_ = "SetDataRetrievalPolicyInput"
    let make ?policy = fun ~accountId -> fun () -> { policy; accountId }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("Policy", (Option.map x.policy ~f:DataRetrievalPolicy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policy =
        (Option.map ~f:DataRetrievalPolicy.of_xml)
          (Xml.child xml_arg0 "Policy") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ?policy ~accountId ()
    let of_json json =
      let policy = field_map json "Policy" DataRetrievalPolicy.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ?policy ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module RemoveTagsFromVaultInput =
  struct
    type nonrec t =
      {
      accountId: String_.t ;
      vaultName: String_.t ;
      tagKeys: TagKeyList.t option }
    let context_ = "RemoveTagsFromVaultInput"
    let make ?tagKeys =
      fun ~accountId ->
        fun ~vaultName -> fun () -> { tagKeys; accountId; vaultName }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)));
        ("TagKeys", (Option.map x.tagKeys ~f:TagKeyList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        (Option.map ~f:TagKeyList.of_xml) (Xml.child xml_arg0 "TagKeys") in
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ?tagKeys ~vaultName ~accountId ()
    let of_json json =
      let tagKeys = field_map json "TagKeys" TagKeyList.of_json in
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ?tagKeys ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module PurchaseProvisionedCapacityOutput =
  struct
    type nonrec t = {
      capacityId: String_.t option }
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?capacityId = fun () -> { capacityId }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body =
      ((fun (xs, pipe) ->
          make
            ?capacityId:(Option.map
                           ((List.Assoc.find ~equal:String.Caseless.equal) xs
                              "x-amz-capacity-id") ~f:String_.of_string) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("x-amz-capacity-id", (Option.map x.capacityId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let capacityId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amz-capacity-id") in
      make ?capacityId ()
    let of_json json =
      let capacityId = field_map json "capacityId" String_.of_json in
      make ?capacityId ()
    let to_json v = composed_to_json to_value v
  end
module PurchaseProvisionedCapacityInput =
  struct
    type nonrec t = {
      accountId: String_.t }
    let context_ = "PurchaseProvisionedCapacityInput"
    let make ~accountId = fun () -> { accountId }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ~accountId ()
    let of_json json =
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module ListVaultsOutput =
  struct
    type nonrec t =
      {
      vaultList: VaultList.t option ;
      marker: String_.t option }
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?vaultList = fun ?marker -> fun () -> { vaultList; marker }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("VaultList", (Option.map x.vaultList ~f:VaultList.to_value));
        ("Marker", (Option.map x.marker ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let marker =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Marker") in
      let vaultList =
        (Option.map ~f:VaultList.of_xml) (Xml.child xml_arg0 "VaultList") in
      make ?marker ?vaultList ()
    let of_json json =
      let marker = field_map json "Marker" String_.of_json in
      let vaultList = field_map json "VaultList" VaultList.of_json in
      make ?marker ?vaultList ()
    let to_json v = composed_to_json to_value v
  end
module ListVaultsInput =
  struct
    type nonrec t =
      {
      accountId: String_.t ;
      marker: String_.t option ;
      limit: String_.t option }
    let context_ = "ListVaultsInput"
    let make ?marker =
      fun ?limit -> fun ~accountId -> fun () -> { marker; limit; accountId }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("marker", (Option.map x.marker ~f:String_.to_value));
        ("limit", (Option.map x.limit ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let limit = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "limit") in
      let marker =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "marker") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ?limit ?marker ~accountId ()
    let of_json json =
      let limit = field_map json "limit" String_.of_json in
      let marker = field_map json "marker" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ?limit ?marker ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module ListTagsForVaultOutput =
  struct
    type nonrec t = {
      tags: TagMap.t option }
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun () -> { tags }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value [("Tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "Tags") in
      make ?tags ()
    let of_json json =
      let tags = field_map json "Tags" TagMap.of_json in make ?tags ()
    let to_json v = composed_to_json to_value v
  end
module ListTagsForVaultInput =
  struct
    type nonrec t = {
      accountId: String_.t ;
      vaultName: String_.t }
    let context_ = "ListTagsForVaultInput"
    let make ~accountId =
      fun ~vaultName -> fun () -> { accountId; vaultName }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ~vaultName ~accountId ()
    let of_json json =
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module ListProvisionedCapacityOutput =
  struct
    type nonrec t =
      {
      provisionedCapacityList: ProvisionedCapacityList.t option }
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?provisionedCapacityList = fun () -> { provisionedCapacityList }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ProvisionedCapacityList",
           (Option.map x.provisionedCapacityList
              ~f:ProvisionedCapacityList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let provisionedCapacityList =
        (Option.map ~f:ProvisionedCapacityList.of_xml)
          (Xml.child xml_arg0 "ProvisionedCapacityList") in
      make ?provisionedCapacityList ()
    let of_json json =
      let provisionedCapacityList =
        field_map json "ProvisionedCapacityList"
          ProvisionedCapacityList.of_json in
      make ?provisionedCapacityList ()
    let to_json v = composed_to_json to_value v
  end
module ListProvisionedCapacityInput =
  struct
    type nonrec t = {
      accountId: String_.t }
    let context_ = "ListProvisionedCapacityInput"
    let make ~accountId = fun () -> { accountId }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ~accountId ()
    let of_json json =
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module ListPartsOutput =
  struct
    type nonrec t =
      {
      multipartUploadId: String_.t option ;
      vaultARN: String_.t option ;
      archiveDescription: String_.t option ;
      partSizeInBytes: Long.t option ;
      creationDate: String_.t option ;
      parts: PartList.t option ;
      marker: String_.t option }
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?multipartUploadId =
      fun ?vaultARN ->
        fun ?archiveDescription ->
          fun ?partSizeInBytes ->
            fun ?creationDate ->
              fun ?parts ->
                fun ?marker ->
                  fun () ->
                    {
                      multipartUploadId;
                      vaultARN;
                      archiveDescription;
                      partSizeInBytes;
                      creationDate;
                      parts;
                      marker
                    }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("MultipartUploadId",
           (Option.map x.multipartUploadId ~f:String_.to_value));
        ("VaultARN", (Option.map x.vaultARN ~f:String_.to_value));
        ("ArchiveDescription",
          (Option.map x.archiveDescription ~f:String_.to_value));
        ("PartSizeInBytes", (Option.map x.partSizeInBytes ~f:Long.to_value));
        ("CreationDate", (Option.map x.creationDate ~f:String_.to_value));
        ("Parts", (Option.map x.parts ~f:PartList.to_value));
        ("Marker", (Option.map x.marker ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let marker =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Marker") in
      let parts =
        (Option.map ~f:PartList.of_xml) (Xml.child xml_arg0 "Parts") in
      let creationDate =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "CreationDate") in
      let partSizeInBytes =
        (Option.map ~f:Long.of_xml) (Xml.child xml_arg0 "PartSizeInBytes") in
      let archiveDescription =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "ArchiveDescription") in
      let vaultARN =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "VaultARN") in
      let multipartUploadId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "MultipartUploadId") in
      make ?marker ?parts ?creationDate ?partSizeInBytes ?archiveDescription
        ?vaultARN ?multipartUploadId ()
    let of_json json =
      let marker = field_map json "Marker" String_.of_json in
      let parts = field_map json "Parts" PartList.of_json in
      let creationDate = field_map json "CreationDate" String_.of_json in
      let partSizeInBytes = field_map json "PartSizeInBytes" Long.of_json in
      let archiveDescription =
        field_map json "ArchiveDescription" String_.of_json in
      let vaultARN = field_map json "VaultARN" String_.of_json in
      let multipartUploadId =
        field_map json "MultipartUploadId" String_.of_json in
      make ?marker ?parts ?creationDate ?partSizeInBytes ?archiveDescription
        ?vaultARN ?multipartUploadId ()
    let to_json v = composed_to_json to_value v
  end
module ListPartsInput =
  struct
    type nonrec t =
      {
      accountId: String_.t ;
      vaultName: String_.t ;
      uploadId: String_.t ;
      marker: String_.t option ;
      limit: String_.t option }
    let context_ = "ListPartsInput"
    let make ?marker =
      fun ?limit ->
        fun ~accountId ->
          fun ~vaultName ->
            fun ~uploadId ->
              fun () -> { marker; limit; accountId; vaultName; uploadId }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)));
        ("uploadId", (Some (String_.to_value x.uploadId)));
        ("marker", (Option.map x.marker ~f:String_.to_value));
        ("limit", (Option.map x.limit ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let limit = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "limit") in
      let marker =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "marker") in
      let uploadId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "uploadId") in
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ?limit ?marker ~uploadId ~vaultName ~accountId ()
    let of_json json =
      let limit = field_map json "limit" String_.of_json in
      let marker = field_map json "marker" String_.of_json in
      let uploadId = field_map_exn json "uploadId" String_.of_json in
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ?limit ?marker ~uploadId ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module ListMultipartUploadsOutput =
  struct
    type nonrec t =
      {
      uploadsList: UploadsList.t option ;
      marker: String_.t option }
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?uploadsList = fun ?marker -> fun () -> { uploadsList; marker }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("UploadsList", (Option.map x.uploadsList ~f:UploadsList.to_value));
        ("Marker", (Option.map x.marker ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let marker =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Marker") in
      let uploadsList =
        (Option.map ~f:UploadsList.of_xml) (Xml.child xml_arg0 "UploadsList") in
      make ?marker ?uploadsList ()
    let of_json json =
      let marker = field_map json "Marker" String_.of_json in
      let uploadsList = field_map json "UploadsList" UploadsList.of_json in
      make ?marker ?uploadsList ()
    let to_json v = composed_to_json to_value v
  end
module ListMultipartUploadsInput =
  struct
    type nonrec t =
      {
      accountId: String_.t ;
      vaultName: String_.t ;
      marker: String_.t option ;
      limit: String_.t option }
    let context_ = "ListMultipartUploadsInput"
    let make ?marker =
      fun ?limit ->
        fun ~accountId ->
          fun ~vaultName -> fun () -> { marker; limit; accountId; vaultName }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)));
        ("marker", (Option.map x.marker ~f:String_.to_value));
        ("limit", (Option.map x.limit ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let limit = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "limit") in
      let marker =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "marker") in
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ?limit ?marker ~vaultName ~accountId ()
    let of_json json =
      let limit = field_map json "limit" String_.of_json in
      let marker = field_map json "marker" String_.of_json in
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ?limit ?marker ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module ListJobsOutput =
  struct
    type nonrec t = {
      jobList: JobList.t option ;
      marker: String_.t option }
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?jobList = fun ?marker -> fun () -> { jobList; marker }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("JobList", (Option.map x.jobList ~f:JobList.to_value));
        ("Marker", (Option.map x.marker ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let marker =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Marker") in
      let jobList =
        (Option.map ~f:JobList.of_xml) (Xml.child xml_arg0 "JobList") in
      make ?marker ?jobList ()
    let of_json json =
      let marker = field_map json "Marker" String_.of_json in
      let jobList = field_map json "JobList" JobList.of_json in
      make ?marker ?jobList ()
    let to_json v = composed_to_json to_value v
  end
module ListJobsInput =
  struct
    type nonrec t =
      {
      accountId: String_.t ;
      vaultName: String_.t ;
      limit: String_.t option ;
      marker: String_.t option ;
      statuscode: String_.t option ;
      completed: String_.t option }
    let context_ = "ListJobsInput"
    let make ?limit =
      fun ?marker ->
        fun ?statuscode ->
          fun ?completed ->
            fun ~accountId ->
              fun ~vaultName ->
                fun () ->
                  {
                    limit;
                    marker;
                    statuscode;
                    completed;
                    accountId;
                    vaultName
                  }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)));
        ("limit", (Option.map x.limit ~f:String_.to_value));
        ("marker", (Option.map x.marker ~f:String_.to_value));
        ("statuscode", (Option.map x.statuscode ~f:String_.to_value));
        ("completed", (Option.map x.completed ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let completed =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "completed") in
      let statuscode =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "statuscode") in
      let marker =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "marker") in
      let limit = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "limit") in
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ?completed ?statuscode ?marker ?limit ~vaultName ~accountId ()
    let of_json json =
      let completed = field_map json "completed" String_.of_json in
      let statuscode = field_map json "statuscode" String_.of_json in
      let marker = field_map json "marker" String_.of_json in
      let limit = field_map json "limit" String_.of_json in
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ?completed ?statuscode ?marker ?limit ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module InitiateVaultLockOutput =
  struct
    type nonrec t = {
      lockId: String_.t option }
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?lockId = fun () -> { lockId }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body =
      ((fun (xs, pipe) ->
          make
            ?lockId:(Option.map
                       ((List.Assoc.find ~equal:String.Caseless.equal) xs
                          "x-amz-lock-id") ~f:String_.of_string) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("x-amz-lock-id", (Option.map x.lockId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lockId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "x-amz-lock-id") in
      make ?lockId ()
    let of_json json =
      let lockId = field_map json "lockId" String_.of_json in make ?lockId ()
    let to_json v = composed_to_json to_value v
  end
module InitiateVaultLockInput =
  struct
    type nonrec t =
      {
      accountId: String_.t ;
      vaultName: String_.t ;
      policy: VaultLockPolicy.t option }
    let context_ = "InitiateVaultLockInput"
    let make ?policy =
      fun ~accountId ->
        fun ~vaultName -> fun () -> { policy; accountId; vaultName }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)));
        ("policy", (Option.map x.policy ~f:VaultLockPolicy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policy =
        (Option.map ~f:VaultLockPolicy.of_xml) (Xml.child xml_arg0 "policy") in
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ?policy ~vaultName ~accountId ()
    let of_json json =
      let policy = field_map json "policy" VaultLockPolicy.of_json in
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ?policy ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module InitiateMultipartUploadOutput =
  struct
    type nonrec t = {
      location: String_.t option ;
      uploadId: String_.t option }
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?location = fun ?uploadId -> fun () -> { location; uploadId }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body =
      ((fun (xs, pipe) ->
          make
            ?location:(Option.map
                         ((List.Assoc.find ~equal:String.Caseless.equal) xs
                            "Location") ~f:String_.of_string)
            ?uploadId:(Option.map
                         ((List.Assoc.find ~equal:String.Caseless.equal) xs
                            "x-amz-multipart-upload-id") ~f:String_.of_string)
            ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("Location", (Option.map x.location ~f:String_.to_value));
        ("x-amz-multipart-upload-id",
          (Option.map x.uploadId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let uploadId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amz-multipart-upload-id") in
      let location =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Location") in
      make ?uploadId ?location ()
    let of_json json =
      let uploadId = field_map json "uploadId" String_.of_json in
      let location = field_map json "location" String_.of_json in
      make ?uploadId ?location ()
    let to_json v = composed_to_json to_value v
  end
module InitiateMultipartUploadInput =
  struct
    type nonrec t =
      {
      accountId: String_.t ;
      vaultName: String_.t ;
      archiveDescription: String_.t option ;
      partSize: String_.t option }
    let context_ = "InitiateMultipartUploadInput"
    let make ?archiveDescription =
      fun ?partSize ->
        fun ~accountId ->
          fun ~vaultName ->
            fun () -> { archiveDescription; partSize; accountId; vaultName }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)));
        ("x-amz-archive-description",
          (Option.map x.archiveDescription ~f:String_.to_value));
        ("x-amz-part-size", (Option.map x.partSize ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let partSize =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "x-amz-part-size") in
      let archiveDescription =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amz-archive-description") in
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ?partSize ?archiveDescription ~vaultName ~accountId ()
    let of_json json =
      let partSize = field_map json "partSize" String_.of_json in
      let archiveDescription =
        field_map json "archiveDescription" String_.of_json in
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ?partSize ?archiveDescription ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module InitiateJobOutput =
  struct
    type nonrec t =
      {
      location: String_.t option ;
      jobId: String_.t option ;
      jobOutputPath: String_.t option }
    type nonrec error =
      [ `InsufficientCapacityException of InsufficientCapacityException.t 
      | `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `PolicyEnforcedException of PolicyEnforcedException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?location =
      fun ?jobId ->
        fun ?jobOutputPath -> fun () -> { location; jobId; jobOutputPath }
    let error_of_json name json =
      match name with
      | "InsufficientCapacityException" ->
          `InsufficientCapacityException
            (InsufficientCapacityException.of_json json)
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "PolicyEnforcedException" ->
          `PolicyEnforcedException (PolicyEnforcedException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InsufficientCapacityException" ->
          `InsufficientCapacityException
            (InsufficientCapacityException.of_xml xml)
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "PolicyEnforcedException" ->
          `PolicyEnforcedException (PolicyEnforcedException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InsufficientCapacityException e ->
          `Assoc
            [("error", (`String "InsufficientCapacityException"));
            ("details", (InsufficientCapacityException.to_json e))]
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `PolicyEnforcedException e ->
          `Assoc
            [("error", (`String "PolicyEnforcedException"));
            ("details", (PolicyEnforcedException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body =
      ((fun (xs, pipe) ->
          make
            ?location:(Option.map
                         ((List.Assoc.find ~equal:String.Caseless.equal) xs
                            "Location") ~f:String_.of_string)
            ?jobId:(Option.map
                      ((List.Assoc.find ~equal:String.Caseless.equal) xs
                         "x-amz-job-id") ~f:String_.of_string)
            ?jobOutputPath:(Option.map
                              ((List.Assoc.find ~equal:String.Caseless.equal)
                                 xs "x-amz-job-output-path")
                              ~f:String_.of_string) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("Location", (Option.map x.location ~f:String_.to_value));
        ("x-amz-job-id", (Option.map x.jobId ~f:String_.to_value));
        ("x-amz-job-output-path",
          (Option.map x.jobOutputPath ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let jobOutputPath =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amz-job-output-path") in
      let jobId =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "x-amz-job-id") in
      let location =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Location") in
      make ?jobOutputPath ?jobId ?location ()
    let of_json json =
      let jobOutputPath = field_map json "jobOutputPath" String_.of_json in
      let jobId = field_map json "jobId" String_.of_json in
      let location = field_map json "location" String_.of_json in
      make ?jobOutputPath ?jobId ?location ()
    let to_json v = composed_to_json to_value v
  end
module InitiateJobInput =
  struct
    type nonrec t =
      {
      accountId: String_.t ;
      vaultName: String_.t ;
      jobParameters: JobParameters.t option }
    let context_ = "InitiateJobInput"
    let make ?jobParameters =
      fun ~accountId ->
        fun ~vaultName -> fun () -> { jobParameters; accountId; vaultName }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)));
        ("jobParameters",
          (Option.map x.jobParameters ~f:JobParameters.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let jobParameters =
        (Option.map ~f:JobParameters.of_xml)
          (Xml.child xml_arg0 "jobParameters") in
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ?jobParameters ~vaultName ~accountId ()
    let of_json json =
      let jobParameters =
        field_map json "jobParameters" JobParameters.of_json in
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ?jobParameters ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module GetVaultNotificationsOutput =
  struct
    type nonrec t =
      {
      vaultNotificationConfig: VaultNotificationConfig.t option }
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?vaultNotificationConfig = fun () -> { vaultNotificationConfig }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("vaultNotificationConfig",
           (Option.map x.vaultNotificationConfig
              ~f:VaultNotificationConfig.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vaultNotificationConfig =
        (Option.map ~f:VaultNotificationConfig.of_xml)
          (Xml.child xml_arg0 "vaultNotificationConfig") in
      make ?vaultNotificationConfig ()
    let of_json json =
      let vaultNotificationConfig =
        field_map json "vaultNotificationConfig"
          VaultNotificationConfig.of_json in
      make ?vaultNotificationConfig ()
    let to_json v = composed_to_json to_value v
  end
module GetVaultNotificationsInput =
  struct
    type nonrec t = {
      accountId: String_.t ;
      vaultName: String_.t }
    let context_ = "GetVaultNotificationsInput"
    let make ~accountId =
      fun ~vaultName -> fun () -> { accountId; vaultName }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ~vaultName ~accountId ()
    let of_json json =
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module GetVaultLockOutput =
  struct
    type nonrec t =
      {
      policy: String_.t option ;
      state: String_.t option ;
      expirationDate: String_.t option ;
      creationDate: String_.t option }
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?policy =
      fun ?state ->
        fun ?expirationDate ->
          fun ?creationDate ->
            fun () -> { policy; state; expirationDate; creationDate }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Policy", (Option.map x.policy ~f:String_.to_value));
        ("State", (Option.map x.state ~f:String_.to_value));
        ("ExpirationDate", (Option.map x.expirationDate ~f:String_.to_value));
        ("CreationDate", (Option.map x.creationDate ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let creationDate =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "CreationDate") in
      let expirationDate =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ExpirationDate") in
      let state = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "State") in
      let policy =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Policy") in
      make ?creationDate ?expirationDate ?state ?policy ()
    let of_json json =
      let creationDate = field_map json "CreationDate" String_.of_json in
      let expirationDate = field_map json "ExpirationDate" String_.of_json in
      let state = field_map json "State" String_.of_json in
      let policy = field_map json "Policy" String_.of_json in
      make ?creationDate ?expirationDate ?state ?policy ()
    let to_json v = composed_to_json to_value v
  end
module GetVaultLockInput =
  struct
    type nonrec t = {
      accountId: String_.t ;
      vaultName: String_.t }
    let context_ = "GetVaultLockInput"
    let make ~accountId =
      fun ~vaultName -> fun () -> { accountId; vaultName }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ~vaultName ~accountId ()
    let of_json json =
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module GetVaultAccessPolicyOutput =
  struct
    type nonrec t = {
      policy: VaultAccessPolicy.t option }
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?policy = fun () -> { policy }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("policy", (Option.map x.policy ~f:VaultAccessPolicy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policy =
        (Option.map ~f:VaultAccessPolicy.of_xml)
          (Xml.child xml_arg0 "policy") in
      make ?policy ()
    let of_json json =
      let policy = field_map json "policy" VaultAccessPolicy.of_json in
      make ?policy ()
    let to_json v = composed_to_json to_value v
  end
module GetVaultAccessPolicyInput =
  struct
    type nonrec t = {
      accountId: String_.t ;
      vaultName: String_.t }
    let context_ = "GetVaultAccessPolicyInput"
    let make ~accountId =
      fun ~vaultName -> fun () -> { accountId; vaultName }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ~vaultName ~accountId ()
    let of_json json =
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module GetJobOutputOutput =
  struct
    type nonrec t =
      {
      body: Stream.t option ;
      checksum: String_.t option ;
      status: Httpstatus.t option ;
      contentRange: String_.t option ;
      acceptRanges: String_.t option ;
      contentType: String_.t option ;
      archiveDescription: String_.t option }
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?body =
      fun ?checksum ->
        fun ?status ->
          fun ?contentRange ->
            fun ?acceptRanges ->
              fun ?contentType ->
                fun ?archiveDescription ->
                  fun () ->
                    {
                      body;
                      checksum;
                      status;
                      contentRange;
                      acceptRanges;
                      contentType;
                      archiveDescription
                    }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("body", (Option.map x.body ~f:Stream.to_value));
        ("x-amz-sha256-tree-hash",
          (Option.map x.checksum ~f:String_.to_value));
        ("status", (Option.map x.status ~f:Httpstatus.to_value));
        ("Content-Range", (Option.map x.contentRange ~f:String_.to_value));
        ("Accept-Ranges", (Option.map x.acceptRanges ~f:String_.to_value));
        ("Content-Type", (Option.map x.contentType ~f:String_.to_value));
        ("x-amz-archive-description",
          (Option.map x.archiveDescription ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let archiveDescription =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amz-archive-description") in
      let contentType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Content-Type") in
      let acceptRanges =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Accept-Ranges") in
      let contentRange =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Content-Range") in
      let status =
        (Option.map ~f:Httpstatus.of_xml) (Xml.child xml_arg0 "status") in
      let checksum =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amz-sha256-tree-hash") in
      let body = (Option.map ~f:Stream.of_xml) (Xml.child xml_arg0 "body") in
      make ?archiveDescription ?contentType ?acceptRanges ?contentRange
        ?status ?checksum ?body ()
    let of_json json =
      let archiveDescription =
        field_map json "archiveDescription" String_.of_json in
      let contentType = field_map json "contentType" String_.of_json in
      let acceptRanges = field_map json "acceptRanges" String_.of_json in
      let contentRange = field_map json "contentRange" String_.of_json in
      let status = field_map json "status" Httpstatus.of_json in
      let checksum = field_map json "checksum" String_.of_json in
      let body = field_map json "body" Stream.of_json in
      make ?archiveDescription ?contentType ?acceptRanges ?contentRange
        ?status ?checksum ?body ()
    let to_json v = composed_to_json to_value v
  end
module GetJobOutputInput =
  struct
    type nonrec t =
      {
      accountId: String_.t ;
      vaultName: String_.t ;
      jobId: String_.t ;
      range: String_.t option }
    let context_ = "GetJobOutputInput"
    let make ?range =
      fun ~accountId ->
        fun ~vaultName ->
          fun ~jobId -> fun () -> { range; accountId; vaultName; jobId }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)));
        ("jobId", (Some (String_.to_value x.jobId)));
        ("Range", (Option.map x.range ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let range = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Range") in
      let jobId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "jobId") in
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ?range ~jobId ~vaultName ~accountId ()
    let of_json json =
      let range = field_map json "range" String_.of_json in
      let jobId = field_map_exn json "jobId" String_.of_json in
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ?range ~jobId ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module GetDataRetrievalPolicyOutput =
  struct
    type nonrec t = {
      policy: DataRetrievalPolicy.t option }
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?policy = fun () -> { policy }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Policy", (Option.map x.policy ~f:DataRetrievalPolicy.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let policy =
        (Option.map ~f:DataRetrievalPolicy.of_xml)
          (Xml.child xml_arg0 "Policy") in
      make ?policy ()
    let of_json json =
      let policy = field_map json "Policy" DataRetrievalPolicy.of_json in
      make ?policy ()
    let to_json v = composed_to_json to_value v
  end
module GetDataRetrievalPolicyInput =
  struct
    type nonrec t = {
      accountId: String_.t }
    let context_ = "GetDataRetrievalPolicyInput"
    let make ~accountId = fun () -> { accountId }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ~accountId ()
    let of_json json =
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module DescribeVaultInput =
  struct
    type nonrec t = {
      accountId: String_.t ;
      vaultName: String_.t }
    let context_ = "DescribeVaultInput"
    let make ~accountId =
      fun ~vaultName -> fun () -> { accountId; vaultName }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ~vaultName ~accountId ()
    let of_json json =
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module DescribeJobInput =
  struct
    type nonrec t =
      {
      accountId: String_.t ;
      vaultName: String_.t ;
      jobId: String_.t }
    let context_ = "DescribeJobInput"
    let make ~accountId =
      fun ~vaultName ->
        fun ~jobId -> fun () -> { accountId; vaultName; jobId }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)));
        ("jobId", (Some (String_.to_value x.jobId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let jobId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "jobId") in
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ~jobId ~vaultName ~accountId ()
    let of_json json =
      let jobId = field_map_exn json "jobId" String_.of_json in
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ~jobId ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteVaultNotificationsInput =
  struct
    type nonrec t = {
      accountId: String_.t ;
      vaultName: String_.t }
    let context_ = "DeleteVaultNotificationsInput"
    let make ~accountId =
      fun ~vaultName -> fun () -> { accountId; vaultName }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ~vaultName ~accountId ()
    let of_json json =
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteVaultInput =
  struct
    type nonrec t = {
      accountId: String_.t ;
      vaultName: String_.t }
    let context_ = "DeleteVaultInput"
    let make ~accountId =
      fun ~vaultName -> fun () -> { accountId; vaultName }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ~vaultName ~accountId ()
    let of_json json =
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteVaultAccessPolicyInput =
  struct
    type nonrec t = {
      accountId: String_.t ;
      vaultName: String_.t }
    let context_ = "DeleteVaultAccessPolicyInput"
    let make ~accountId =
      fun ~vaultName -> fun () -> { accountId; vaultName }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ~vaultName ~accountId ()
    let of_json json =
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module DeleteArchiveInput =
  struct
    type nonrec t =
      {
      accountId: String_.t ;
      vaultName: String_.t ;
      archiveId: String_.t }
    let context_ = "DeleteArchiveInput"
    let make ~accountId =
      fun ~vaultName ->
        fun ~archiveId -> fun () -> { accountId; vaultName; archiveId }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)));
        ("archiveId", (Some (String_.to_value x.archiveId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let archiveId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "archiveId") in
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ~archiveId ~vaultName ~accountId ()
    let of_json json =
      let archiveId = field_map_exn json "archiveId" String_.of_json in
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ~archiveId ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module CreateVaultOutput =
  struct
    type nonrec t = {
      location: String_.t option }
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?location = fun () -> { location }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body =
      ((fun (xs, pipe) ->
          make
            ?location:(Option.map
                         ((List.Assoc.find ~equal:String.Caseless.equal) xs
                            "Location") ~f:String_.of_string) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("Location", (Option.map x.location ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Location") in
      make ?location ()
    let of_json json =
      let location = field_map json "location" String_.of_json in
      make ?location ()
    let to_json v = composed_to_json to_value v
  end
module CreateVaultInput =
  struct
    type nonrec t = {
      accountId: String_.t ;
      vaultName: String_.t }
    let context_ = "CreateVaultInput"
    let make ~accountId =
      fun ~vaultName -> fun () -> { accountId; vaultName }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ~vaultName ~accountId ()
    let of_json json =
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module CompleteVaultLockInput =
  struct
    type nonrec t =
      {
      accountId: String_.t ;
      vaultName: String_.t ;
      lockId: String_.t }
    let context_ = "CompleteVaultLockInput"
    let make ~accountId =
      fun ~vaultName ->
        fun ~lockId -> fun () -> { accountId; vaultName; lockId }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)));
        ("lockId", (Some (String_.to_value x.lockId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lockId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "lockId") in
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ~lockId ~vaultName ~accountId ()
    let of_json json =
      let lockId = field_map_exn json "lockId" String_.of_json in
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ~lockId ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module CompleteMultipartUploadInput =
  struct
    type nonrec t =
      {
      accountId: String_.t ;
      vaultName: String_.t ;
      uploadId: String_.t ;
      archiveSize: String_.t option ;
      checksum: String_.t option }
    let context_ = "CompleteMultipartUploadInput"
    let make ?archiveSize =
      fun ?checksum ->
        fun ~accountId ->
          fun ~vaultName ->
            fun ~uploadId ->
              fun () ->
                { archiveSize; checksum; accountId; vaultName; uploadId }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)));
        ("uploadId", (Some (String_.to_value x.uploadId)));
        ("x-amz-archive-size",
          (Option.map x.archiveSize ~f:String_.to_value));
        ("x-amz-sha256-tree-hash",
          (Option.map x.checksum ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let checksum =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amz-sha256-tree-hash") in
      let archiveSize =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amz-archive-size") in
      let uploadId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "uploadId") in
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ?checksum ?archiveSize ~uploadId ~vaultName ~accountId ()
    let of_json json =
      let checksum = field_map json "checksum" String_.of_json in
      let archiveSize = field_map json "archiveSize" String_.of_json in
      let uploadId = field_map_exn json "uploadId" String_.of_json in
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ?checksum ?archiveSize ~uploadId ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module ArchiveCreationOutput =
  struct
    type nonrec t =
      {
      location: String_.t option ;
      checksum: String_.t option ;
      archiveId: String_.t option }
    type nonrec error =
      [ `InvalidParameterValueException of InvalidParameterValueException.t 
      | `MissingParameterValueException of MissingParameterValueException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?location =
      fun ?checksum ->
        fun ?archiveId -> fun () -> { location; checksum; archiveId }
    let error_of_json name json =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_json json)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidParameterValueException" ->
          `InvalidParameterValueException
            (InvalidParameterValueException.of_xml xml)
      | "MissingParameterValueException" ->
          `MissingParameterValueException
            (MissingParameterValueException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidParameterValueException e ->
          `Assoc
            [("error", (`String "InvalidParameterValueException"));
            ("details", (InvalidParameterValueException.to_json e))]
      | `MissingParameterValueException e ->
          `Assoc
            [("error", (`String "MissingParameterValueException"));
            ("details", (MissingParameterValueException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body =
      ((fun (xs, pipe) ->
          make
            ?location:(Option.map
                         ((List.Assoc.find ~equal:String.Caseless.equal) xs
                            "Location") ~f:String_.of_string)
            ?checksum:(Option.map
                         ((List.Assoc.find ~equal:String.Caseless.equal) xs
                            "x-amz-sha256-tree-hash") ~f:String_.of_string)
            ?archiveId:(Option.map
                          ((List.Assoc.find ~equal:String.Caseless.equal) xs
                             "x-amz-archive-id") ~f:String_.of_string) ())
      [@warning "-27"])
    let to_value x =
      structure_to_value
        [("Location", (Option.map x.location ~f:String_.to_value));
        ("x-amz-sha256-tree-hash",
          (Option.map x.checksum ~f:String_.to_value));
        ("x-amz-archive-id", (Option.map x.archiveId ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let archiveId =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amz-archive-id") in
      let checksum =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "x-amz-sha256-tree-hash") in
      let location =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Location") in
      make ?archiveId ?checksum ?location ()
    let of_json json =
      let archiveId = field_map json "archiveId" String_.of_json in
      let checksum = field_map json "checksum" String_.of_json in
      let location = field_map json "location" String_.of_json in
      make ?archiveId ?checksum ?location ()
    let to_json v = composed_to_json to_value v
  end
module AddTagsToVaultInput =
  struct
    type nonrec t =
      {
      accountId: String_.t ;
      vaultName: String_.t ;
      tags: TagMap.t option }
    let context_ = "AddTagsToVaultInput"
    let make ?tags =
      fun ~accountId ->
        fun ~vaultName -> fun () -> { tags; accountId; vaultName }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)));
        ("Tags", (Option.map x.tags ~f:TagMap.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagMap.of_xml) (Xml.child xml_arg0 "Tags") in
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ?tags ~vaultName ~accountId ()
    let of_json json =
      let tags = field_map json "Tags" TagMap.of_json in
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ?tags ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module AbortVaultLockInput =
  struct
    type nonrec t = {
      accountId: String_.t ;
      vaultName: String_.t }
    let context_ = "AbortVaultLockInput"
    let make ~accountId =
      fun ~vaultName -> fun () -> { accountId; vaultName }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ~vaultName ~accountId ()
    let of_json json =
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
module AbortMultipartUploadInput =
  struct
    type nonrec t =
      {
      accountId: String_.t ;
      vaultName: String_.t ;
      uploadId: String_.t }
    let context_ = "AbortMultipartUploadInput"
    let make ~accountId =
      fun ~vaultName ->
        fun ~uploadId -> fun () -> { accountId; vaultName; uploadId }
    let to_value x =
      structure_to_value
        [("accountId", (Some (String_.to_value x.accountId)));
        ("vaultName", (Some (String_.to_value x.vaultName)));
        ("uploadId", (Some (String_.to_value x.uploadId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let uploadId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "uploadId") in
      let vaultName =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vaultName") in
      let accountId =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "accountId") in
      make ~uploadId ~vaultName ~accountId ()
    let of_json json =
      let uploadId = field_map_exn json "uploadId" String_.of_json in
      let vaultName = field_map_exn json "vaultName" String_.of_json in
      let accountId = field_map_exn json "accountId" String_.of_json in
      make ~uploadId ~vaultName ~accountId ()
    let to_json v = composed_to_json to_value v
  end
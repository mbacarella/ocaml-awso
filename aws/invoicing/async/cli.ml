(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let batch_get_invoice_profile =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accountIds =
         flag "account-ids" (required json_arg) ~doc:"JSON AccountIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.batch_get_invoice_profile
           (Values.BatchGetInvoiceProfileRequest.make
              ~accountIds:(Values.AccountIdList.of_json accountIds) ())
           (Some Values.BatchGetInvoiceProfileResponse.to_json)
           (Some Values.BatchGetInvoiceProfileResponse.error_to_json)])
let create_invoice_unit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionString"
       and taxInheritanceDisabled =
         flag "tax-inheritance-disabled" (optional bool)
           ~doc:"BOOL TaxInheritanceDisabledFlag"
       and resourceTags =
         flag "resource-tags" (optional json_arg) ~doc:"JSON ResourceTagList"
       and name = flag "name" (required string) ~doc:"STRING InvoiceUnitName"
       and invoiceReceiver =
         flag "invoice-receiver" (required string)
           ~doc:"STRING AccountIdString"
       and rule = flag "rule" (required json_arg) ~doc:"JSON InvoiceUnitRule" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_invoice_unit
           (Values.CreateInvoiceUnitRequest.make ?description
              ?taxInheritanceDisabled
              ?resourceTags:(Option.map ~f:Values.ResourceTagList.of_json
                               resourceTags) ~name ~invoiceReceiver
              ~rule:(Values.InvoiceUnitRule.of_json rule) ())
           (Some Values.CreateInvoiceUnitResponse.to_json)
           (Some Values.CreateInvoiceUnitResponse.error_to_json)])
let create_procurement_portal_preference =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and selector =
         flag "selector" (optional json_arg)
           ~doc:"JSON ProcurementPortalPreferenceSelector"
       and procurementPortalSharedSecret =
         flag "procurement-portal-shared-secret" (optional string)
           ~doc:"STRING SensitiveBasicStringWithoutSpace"
       and procurementPortalInstanceEndpoint =
         flag "procurement-portal-instance-endpoint" (optional string)
           ~doc:"STRING BasicStringWithoutSpace"
       and testEnvPreference =
         flag "test-env-preference" (optional json_arg)
           ~doc:"JSON TestEnvPreferenceInput"
       and einvoiceDeliveryPreference =
         flag "einvoice-delivery-preference" (optional json_arg)
           ~doc:"JSON EinvoiceDeliveryPreference"
       and resourceTags =
         flag "resource-tags" (optional json_arg) ~doc:"JSON ResourceTagList"
       and clientToken =
         flag "client-token" (optional string)
           ~doc:"STRING BasicStringWithoutSpace"
       and procurementPortalName =
         flag "procurement-portal-name" (required json_arg)
           ~doc:"JSON ProcurementPortalName"
       and buyerDomain =
         flag "buyer-domain" (required json_arg) ~doc:"JSON BuyerDomain"
       and buyerIdentifier =
         flag "buyer-identifier" (required string)
           ~doc:"STRING BasicStringWithoutSpace"
       and supplierDomain =
         flag "supplier-domain" (required json_arg)
           ~doc:"JSON SupplierDomain"
       and supplierIdentifier =
         flag "supplier-identifier" (required string)
           ~doc:"STRING BasicStringWithoutSpace"
       and einvoiceDeliveryEnabled =
         flag "einvoice-delivery-enabled" (required bool) ~doc:"BOOL Boolean"
       and purchaseOrderRetrievalEnabled =
         flag "purchase-order-retrieval-enabled" (required bool)
           ~doc:"BOOL Boolean"
       and contacts =
         flag "contacts" (required json_arg) ~doc:"JSON Contacts" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_procurement_portal_preference
           (Values.CreateProcurementPortalPreferenceRequest.make
              ?selector:(Option.map
                           ~f:Values.ProcurementPortalPreferenceSelector.of_json
                           selector) ?procurementPortalSharedSecret
              ?procurementPortalInstanceEndpoint
              ?testEnvPreference:(Option.map
                                    ~f:Values.TestEnvPreferenceInput.of_json
                                    testEnvPreference)
              ?einvoiceDeliveryPreference:(Option.map
                                             ~f:Values.EinvoiceDeliveryPreference.of_json
                                             einvoiceDeliveryPreference)
              ?resourceTags:(Option.map ~f:Values.ResourceTagList.of_json
                               resourceTags) ?clientToken
              ~procurementPortalName:(Values.ProcurementPortalName.of_json
                                        procurementPortalName)
              ~buyerDomain:(Values.BuyerDomain.of_json buyerDomain)
              ~buyerIdentifier
              ~supplierDomain:(Values.SupplierDomain.of_json supplierDomain)
              ~supplierIdentifier ~einvoiceDeliveryEnabled
              ~purchaseOrderRetrievalEnabled
              ~contacts:(Values.Contacts.of_json contacts) ())
           (Some Values.CreateProcurementPortalPreferenceResponse.to_json)
           (Some
              Values.CreateProcurementPortalPreferenceResponse.error_to_json)])
let delete_invoice_unit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and invoiceUnitArn =
         flag "invoice-unit-arn" (required string)
           ~doc:"STRING InvoiceUnitArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_invoice_unit
           (Values.DeleteInvoiceUnitRequest.make ~invoiceUnitArn ())
           (Some Values.DeleteInvoiceUnitResponse.to_json)
           (Some Values.DeleteInvoiceUnitResponse.error_to_json)])
let delete_procurement_portal_preference =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and procurementPortalPreferenceArn =
         flag "procurement-portal-preference-arn" (required string)
           ~doc:"STRING ProcurementPortalPreferenceArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_procurement_portal_preference
           (Values.DeleteProcurementPortalPreferenceRequest.make
              ~procurementPortalPreferenceArn ())
           (Some Values.DeleteProcurementPortalPreferenceResponse.to_json)
           (Some
              Values.DeleteProcurementPortalPreferenceResponse.error_to_json)])
let get_invoice_p_d_f =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and invoiceId =
         flag "invoice-id" (required string)
           ~doc:"STRING StringWithoutNewLine" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_invoice_p_d_f
           (Values.GetInvoicePDFRequest.make ~invoiceId ())
           (Some Values.GetInvoicePDFResponse.to_json)
           (Some Values.GetInvoicePDFResponse.error_to_json)])
let get_invoice_unit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and asOf = flag "as-of" (optional json_arg) ~doc:"JSON AsOfTimestamp"
       and invoiceUnitArn =
         flag "invoice-unit-arn" (required string)
           ~doc:"STRING InvoiceUnitArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_invoice_unit
           (Values.GetInvoiceUnitRequest.make
              ?asOf:(Option.map ~f:Values.AsOfTimestamp.of_json asOf)
              ~invoiceUnitArn ())
           (Some Values.GetInvoiceUnitResponse.to_json)
           (Some Values.GetInvoiceUnitResponse.error_to_json)])
let get_procurement_portal_preference =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and procurementPortalPreferenceArn =
         flag "procurement-portal-preference-arn" (required string)
           ~doc:"STRING ProcurementPortalPreferenceArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_procurement_portal_preference
           (Values.GetProcurementPortalPreferenceRequest.make
              ~procurementPortalPreferenceArn ())
           (Some Values.GetProcurementPortalPreferenceResponse.to_json)
           (Some Values.GetProcurementPortalPreferenceResponse.error_to_json)])
let list_invoice_summaries =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filter =
         flag "filter" (optional json_arg) ~doc:"JSON InvoiceSummariesFilter"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextTokenString"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT InvoiceSummariesMaxResults"
       and selector =
         flag "selector" (required json_arg)
           ~doc:"JSON InvoiceSummariesSelector" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_invoice_summaries
           (Values.ListInvoiceSummariesRequest.make
              ?filter:(Option.map ~f:Values.InvoiceSummariesFilter.of_json
                         filter) ?nextToken ?maxResults
              ~selector:(Values.InvoiceSummariesSelector.of_json selector) ())
           (Some Values.ListInvoiceSummariesResponse.to_json)
           (Some Values.ListInvoiceSummariesResponse.error_to_json)])
let list_invoice_units =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filters = flag "filters" (optional json_arg) ~doc:"JSON Filters"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextTokenString"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResultsInteger"
       and asOf = flag "as-of" (optional json_arg) ~doc:"JSON AsOfTimestamp" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_invoice_units
           (Values.ListInvoiceUnitsRequest.make
              ?filters:(Option.map ~f:Values.Filters.of_json filters)
              ?nextToken ?maxResults
              ?asOf:(Option.map ~f:Values.AsOfTimestamp.of_json asOf) ())
           (Some Values.ListInvoiceUnitsResponse.to_json)
           (Some Values.ListInvoiceUnitsResponse.error_to_json)])
let list_procurement_portal_preferences =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string)
           ~doc:"STRING BasicStringWithoutSpace"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_procurement_portal_preferences
           (Values.ListProcurementPortalPreferencesRequest.make ?nextToken
              ?maxResults ())
           (Some Values.ListProcurementPortalPreferencesResponse.to_json)
           (Some
              Values.ListProcurementPortalPreferencesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TagrisArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let put_procurement_portal_preference =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and selector =
         flag "selector" (optional json_arg)
           ~doc:"JSON ProcurementPortalPreferenceSelector"
       and procurementPortalSharedSecret =
         flag "procurement-portal-shared-secret" (optional string)
           ~doc:"STRING SensitiveBasicStringWithoutSpace"
       and procurementPortalInstanceEndpoint =
         flag "procurement-portal-instance-endpoint" (optional string)
           ~doc:"STRING BasicStringWithoutSpace"
       and testEnvPreference =
         flag "test-env-preference" (optional json_arg)
           ~doc:"JSON TestEnvPreferenceInput"
       and einvoiceDeliveryPreference =
         flag "einvoice-delivery-preference" (optional json_arg)
           ~doc:"JSON EinvoiceDeliveryPreference"
       and procurementPortalPreferenceArn =
         flag "procurement-portal-preference-arn" (required string)
           ~doc:"STRING ProcurementPortalPreferenceArnString"
       and einvoiceDeliveryEnabled =
         flag "einvoice-delivery-enabled" (required bool) ~doc:"BOOL Boolean"
       and purchaseOrderRetrievalEnabled =
         flag "purchase-order-retrieval-enabled" (required bool)
           ~doc:"BOOL Boolean"
       and contacts =
         flag "contacts" (required json_arg) ~doc:"JSON Contacts" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_procurement_portal_preference
           (Values.PutProcurementPortalPreferenceRequest.make
              ?selector:(Option.map
                           ~f:Values.ProcurementPortalPreferenceSelector.of_json
                           selector) ?procurementPortalSharedSecret
              ?procurementPortalInstanceEndpoint
              ?testEnvPreference:(Option.map
                                    ~f:Values.TestEnvPreferenceInput.of_json
                                    testEnvPreference)
              ?einvoiceDeliveryPreference:(Option.map
                                             ~f:Values.EinvoiceDeliveryPreference.of_json
                                             einvoiceDeliveryPreference)
              ~procurementPortalPreferenceArn ~einvoiceDeliveryEnabled
              ~purchaseOrderRetrievalEnabled
              ~contacts:(Values.Contacts.of_json contacts) ())
           (Some Values.PutProcurementPortalPreferenceResponse.to_json)
           (Some Values.PutProcurementPortalPreferenceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TagrisArn"
       and resourceTags =
         flag "resource-tags" (required json_arg) ~doc:"JSON ResourceTagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~resourceTags:(Values.ResourceTagList.of_json resourceTags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING TagrisArn"
       and resourceTagKeys =
         flag "resource-tag-keys" (required json_arg)
           ~doc:"JSON ResourceTagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~resourceTagKeys:(Values.ResourceTagKeyList.of_json
                                  resourceTagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_invoice_unit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING DescriptionString"
       and taxInheritanceDisabled =
         flag "tax-inheritance-disabled" (optional bool)
           ~doc:"BOOL TaxInheritanceDisabledFlag"
       and rule = flag "rule" (optional json_arg) ~doc:"JSON InvoiceUnitRule"
       and invoiceUnitArn =
         flag "invoice-unit-arn" (required string)
           ~doc:"STRING InvoiceUnitArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_invoice_unit
           (Values.UpdateInvoiceUnitRequest.make ?description
              ?taxInheritanceDisabled
              ?rule:(Option.map ~f:Values.InvoiceUnitRule.of_json rule)
              ~invoiceUnitArn ())
           (Some Values.UpdateInvoiceUnitResponse.to_json)
           (Some Values.UpdateInvoiceUnitResponse.error_to_json)])
let update_procurement_portal_preference_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and einvoiceDeliveryPreferenceStatus =
         flag "einvoice-delivery-preference-status" (optional json_arg)
           ~doc:"JSON ProcurementPortalPreferenceStatus"
       and einvoiceDeliveryPreferenceStatusReason =
         flag "einvoice-delivery-preference-status-reason" (optional string)
           ~doc:"STRING BasicString"
       and purchaseOrderRetrievalPreferenceStatus =
         flag "purchase-order-retrieval-preference-status"
           (optional json_arg) ~doc:"JSON ProcurementPortalPreferenceStatus"
       and purchaseOrderRetrievalPreferenceStatusReason =
         flag "purchase-order-retrieval-preference-status-reason"
           (optional string) ~doc:"STRING BasicString"
       and procurementPortalPreferenceArn =
         flag "procurement-portal-preference-arn" (required string)
           ~doc:"STRING ProcurementPortalPreferenceArnString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_procurement_portal_preference_status
           (Values.UpdateProcurementPortalPreferenceStatusRequest.make
              ?einvoiceDeliveryPreferenceStatus:(Option.map
                                                   ~f:Values.ProcurementPortalPreferenceStatus.of_json
                                                   einvoiceDeliveryPreferenceStatus)
              ?einvoiceDeliveryPreferenceStatusReason
              ?purchaseOrderRetrievalPreferenceStatus:(Option.map
                                                         ~f:Values.ProcurementPortalPreferenceStatus.of_json
                                                         purchaseOrderRetrievalPreferenceStatus)
              ?purchaseOrderRetrievalPreferenceStatusReason
              ~procurementPortalPreferenceArn ())
           (Some
              Values.UpdateProcurementPortalPreferenceStatusResponse.to_json)
           (Some
              Values.UpdateProcurementPortalPreferenceStatusResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("batch-get-invoice-profile", batch_get_invoice_profile);
    ("create-invoice-unit", create_invoice_unit);
    ("create-procurement-portal-preference",
      create_procurement_portal_preference);
    ("delete-invoice-unit", delete_invoice_unit);
    ("delete-procurement-portal-preference",
      delete_procurement_portal_preference);
    ("get-invoice-p-d-f", get_invoice_p_d_f);
    ("get-invoice-unit", get_invoice_unit);
    ("get-procurement-portal-preference", get_procurement_portal_preference);
    ("list-invoice-summaries", list_invoice_summaries);
    ("list-invoice-units", list_invoice_units);
    ("list-procurement-portal-preferences",
      list_procurement_portal_preferences);
    ("list-tags-for-resource", list_tags_for_resource);
    ("put-procurement-portal-preference", put_procurement_portal_preference);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-invoice-unit", update_invoice_unit);
    ("update-procurement-portal-preference-status",
      update_procurement_portal_preference_status)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.opsworkscm
let apiVersion = "2016-11-01"
let endpointPrefix = "opsworks-cm"
let serviceFullName = "AWS OpsWorks CM"
let signatureVersion = "v4"
let protocol = "json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let serviceAbbreviation = "OpsWorksCM"
let targetPrefix = "OpsWorksCM_V2016_11_01"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module EngineAttributeName =
  struct
    type nonrec t = string
    let context_ = "EngineAttributeName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:10000) >>=
             (fun () -> check_pattern i ~pattern:"(?s).*"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"EngineAttributeName" j
    let to_json = simple_to_json to_value
  end
module EngineAttributeValue =
  struct
    type nonrec t = string
    let context_ = "EngineAttributeValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:10000) >>=
             (fun () -> check_pattern i ~pattern:"(?s).*"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"EngineAttributeValue" j
    let to_json = simple_to_json to_value
  end
module EngineAttribute =
  struct
    type nonrec t =
      {
      name: EngineAttributeName.t option
        [@ocaml.doc "The name of the engine attribute."];
      value: EngineAttributeValue.t option
        [@ocaml.doc "The value of the engine attribute."]}
    let make ?name = fun ?value -> fun () -> { name; value }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:EngineAttributeName.to_value));
        ("Value", (Option.map x.value ~f:EngineAttributeValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:EngineAttributeValue.of_xml)
          (Xml.child xml_arg0 "Value") in
      let name =
        (Option.map ~f:EngineAttributeName.of_xml)
          (Xml.child xml_arg0 "Name") in
      make ?value ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let value = field_map json "Value" EngineAttributeValue.of_json in
      let name = field_map json "Name" EngineAttributeName.of_json in
      make ?value ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A name and value pair that is specific to the engine of the server."]
module String_ =
  struct
    type nonrec t = string
    let context_ = "String"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:10000) >>=
             (fun () -> check_pattern i ~pattern:"(?s).*"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"String" j
    let to_json = simple_to_json to_value
  end
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:128) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^([\\p{L}\\p{Z}\\p{N}_.:/=+\\-@]*)$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:0) >>=
             (fun () ->
                (check_string_max i ~max:256) >>=
                  (fun () ->
                     check_pattern i
                       ~pattern:"^([\\p{L}\\p{Z}\\p{N}_.:/=+\\-@]*)$")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module CustomDomain =
  struct
    type nonrec t = string
    let context_ = "CustomDomain"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:253) >>=
             (fun () ->
                check_pattern i
                  ~pattern:"^(((?!-)[A-Za-z0-9-]{0,62}[A-Za-z0-9])\\.)+((?!-)[A-Za-z0-9-]{1,62}[A-Za-z0-9])$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CustomDomain" j
    let to_json = simple_to_json to_value
  end
module EngineAttributes =
  struct
    type nonrec t = EngineAttribute.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:EngineAttribute.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:EngineAttribute.of_xml)
    let of_json j =
      list_of_json ~kind:"EngineAttributes" ~of_json:EngineAttribute.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module Integer =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Integer" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module MaintenanceStatus =
  struct
    type nonrec t =
      | SUCCESS 
      | FAILED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SUCCESS -> "SUCCESS"
      | FAILED -> "FAILED"
      | Non_static_id s -> s
    let of_string =
      function
      | "SUCCESS" -> SUCCESS
      | "FAILED" -> FAILED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration MaintenanceStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"MaintenanceStatus" j)
    let to_json = simple_to_json to_value
  end
module ServerStatus =
  struct
    type nonrec t =
      | BACKING_UP 
      | CONNECTION_LOST 
      | CREATING 
      | DELETING 
      | MODIFYING 
      | FAILED 
      | HEALTHY 
      | RUNNING 
      | RESTORING 
      | SETUP 
      | UNDER_MAINTENANCE 
      | UNHEALTHY 
      | TERMINATED 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | BACKING_UP -> "BACKING_UP"
      | CONNECTION_LOST -> "CONNECTION_LOST"
      | CREATING -> "CREATING"
      | DELETING -> "DELETING"
      | MODIFYING -> "MODIFYING"
      | FAILED -> "FAILED"
      | HEALTHY -> "HEALTHY"
      | RUNNING -> "RUNNING"
      | RESTORING -> "RESTORING"
      | SETUP -> "SETUP"
      | UNDER_MAINTENANCE -> "UNDER_MAINTENANCE"
      | UNHEALTHY -> "UNHEALTHY"
      | TERMINATED -> "TERMINATED"
      | Non_static_id s -> s
    let of_string =
      function
      | "BACKING_UP" -> BACKING_UP
      | "CONNECTION_LOST" -> CONNECTION_LOST
      | "CREATING" -> CREATING
      | "DELETING" -> DELETING
      | "MODIFYING" -> MODIFYING
      | "FAILED" -> FAILED
      | "HEALTHY" -> HEALTHY
      | "RUNNING" -> RUNNING
      | "RESTORING" -> RESTORING
      | "SETUP" -> SETUP
      | "UNDER_MAINTENANCE" -> UNDER_MAINTENANCE
      | "UNHEALTHY" -> UNHEALTHY
      | "TERMINATED" -> TERMINATED
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ServerStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ServerStatus" j)
    let to_json = simple_to_json to_value
  end
module Strings =
  struct
    type nonrec t = String_.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:String_.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:String_.of_xml)
    let of_json j = list_of_json ~kind:"Strings" ~of_json:String_.of_json j
    let to_json v = composed_to_json to_value v
  end
module TimeWindowDefinition =
  struct
    type nonrec t = string[@@ocaml.doc
                            "DDD:HH:MM (weekly start time) or HH:MM (daily start time). Time windows always use coordinated universal time (UTC). Valid strings for day of week (DDD) are: Mon, Tue, Wed, Thr, Fri, Sat, or Sun."]
    let context_ = "TimeWindowDefinition"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:10000) >>=
             (fun () ->
                check_pattern i
                  ~pattern:"^((Mon|Tue|Wed|Thu|Fri|Sat|Sun):)?([0-1][0-9]|2[0-3]):[0-5][0-9]$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TimeWindowDefinition" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "DDD:HH:MM (weekly start time) or HH:MM (daily start time). Time windows always use coordinated universal time (UTC). Valid strings for day of week (DDD) are: Mon, Tue, Wed, Thr, Fri, Sat, or Sun."]
module Timestamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module BackupId =
  struct
    type nonrec t = string
    let context_ = "BackupId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:79) >>=
             (fun () ->
                check_pattern i ~pattern:"[a-zA-Z][a-zA-Z0-9\\-\\.\\:]*"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BackupId" j
    let to_json = simple_to_json to_value
  end
module BackupStatus =
  struct
    type nonrec t =
      | IN_PROGRESS 
      | OK 
      | FAILED 
      | DELETING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | IN_PROGRESS -> "IN_PROGRESS"
      | OK -> "OK"
      | FAILED -> "FAILED"
      | DELETING -> "DELETING"
      | Non_static_id s -> s
    let of_string =
      function
      | "IN_PROGRESS" -> IN_PROGRESS
      | "OK" -> OK
      | "FAILED" -> FAILED
      | "DELETING" -> DELETING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration BackupStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"BackupStatus" j)
    let to_json = simple_to_json to_value
  end
module BackupType =
  struct
    type nonrec t =
      | AUTOMATED 
      | MANUAL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | AUTOMATED -> "AUTOMATED"
      | MANUAL -> "MANUAL"
      | Non_static_id s -> s
    let of_string =
      function
      | "AUTOMATED" -> AUTOMATED
      | "MANUAL" -> MANUAL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration BackupType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"BackupType" j)
    let to_json = simple_to_json to_value
  end
module ServerName =
  struct
    type nonrec t = string
    let context_ = "ServerName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:40) >>=
                  (fun () ->
                     check_pattern i ~pattern:"[a-zA-Z][a-zA-Z0-9\\-]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ServerName" j
    let to_json = simple_to_json to_value
  end
module Tag =
  struct
    type nonrec t =
      {
      key: TagKey.t
        [@ocaml.doc
          "A tag key, such as Stage or Name. A tag key cannot be empty. The key can be a maximum of 127 characters, and can contain only Unicode letters, numbers, or separators, or the following special characters: + - = . _ : /"];
      value: TagValue.t
        [@ocaml.doc
          "An optional tag value, such as Production or test-owcm-server. The value can be a maximum of 255 characters, and contain only Unicode letters, numbers, or separators, or the following special characters: + - = . _ : /"]}
    let context_ = "Tag"
    let make ~key = fun ~value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("Key", (Some (TagKey.to_value x.key)));
        ("Value", (Some (TagValue.to_value x.value)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        TagValue.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Value") in
      let key =
        TagKey.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Key") in
      make ~value ~key ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let value = field_map_exn json "Value" TagValue.of_json in
      let key = field_map_exn json "Key" TagKey.of_json in
      make ~value ~key ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A map that contains tag keys and tag values to attach to an AWS OpsWorks for Chef Automate or AWS OpsWorks for Puppet Enterprise server. Leading and trailing white spaces are trimmed from both the key and value. A maximum of 50 user-applied tags is allowed for tag-supported AWS OpsWorks-CM resources."]
module Server =
  struct
    type nonrec t =
      {
      associatePublicIpAddress: Boolean.t option
        [@ocaml.doc
          "Associate a public IP address with a server that you are launching."];
      backupRetentionCount: Integer.t option
        [@ocaml.doc "The number of automated backups to keep."];
      serverName: String_.t option [@ocaml.doc "The name of the server."];
      createdAt: Timestamp.t option
        [@ocaml.doc
          "Time stamp of server creation. Example 2016-07-29T13:38:47.520Z"];
      cloudFormationStackArn: String_.t option
        [@ocaml.doc
          "The ARN of the CloudFormation stack that was used to create the server."];
      customDomain: CustomDomain.t option
        [@ocaml.doc
          "An optional public endpoint of a server, such as https://aws.my-company.com. You cannot access the server by using the Endpoint value if the server has a CustomDomain specified."];
      disableAutomatedBackup: Boolean.t option
        [@ocaml.doc
          "Disables automated backups. The number of stored backups is dependent on the value of PreferredBackupCount."];
      endpoint: String_.t option
        [@ocaml.doc
          "A DNS name that can be used to access the engine. Example: myserver-asdfghjkl.us-east-1.opsworks.io. You cannot access the server by using the Endpoint value if the server has a CustomDomain specified."];
      engine: String_.t option
        [@ocaml.doc
          "The engine type of the server. Valid values in this release include ChefAutomate and Puppet."];
      engineModel: String_.t option
        [@ocaml.doc
          "The engine model of the server. Valid values in this release include Monolithic for Puppet and Single for Chef."];
      engineAttributes: EngineAttributes.t option
        [@ocaml.doc
          "The response of a createServer() request returns the master credential to access the server in EngineAttributes. These credentials are not stored by AWS OpsWorks CM; they are returned only as part of the result of createServer(). Attributes returned in a createServer response for Chef CHEF_AUTOMATE_PIVOTAL_KEY: A base64-encoded RSA private key that is generated by AWS OpsWorks for Chef Automate. This private key is required to access the Chef API. CHEF_STARTER_KIT: A base64-encoded ZIP file. The ZIP file contains a Chef starter kit, which includes a README, a configuration file, and the required RSA private key. Save this file, unzip it, and then change to the directory where you've unzipped the file contents. From this directory, you can run Knife commands. Attributes returned in a createServer response for Puppet PUPPET_STARTER_KIT: A base64-encoded ZIP file. The ZIP file contains a Puppet starter kit, including a README and a required private key. Save this file, unzip it, and then change to the directory where you've unzipped the file contents. PUPPET_ADMIN_PASSWORD: An administrator password that you can use to sign in to the Puppet Enterprise console after the server is online."];
      engineVersion: String_.t option
        [@ocaml.doc
          "The engine version of the server. For a Chef server, the valid value for EngineVersion is currently 2. For a Puppet server, specify either 2019 or 2017."];
      instanceProfileArn: String_.t option
        [@ocaml.doc "The instance profile ARN of the server."];
      instanceType: String_.t option
        [@ocaml.doc
          "The instance type for the server, as specified in the CloudFormation stack. This might not be the same instance type that is shown in the EC2 console."];
      keyPair: String_.t option
        [@ocaml.doc "The key pair associated with the server."];
      maintenanceStatus: MaintenanceStatus.t option
        [@ocaml.doc
          "The status of the most recent server maintenance run. Shows SUCCESS or FAILED."];
      preferredMaintenanceWindow: TimeWindowDefinition.t option
        [@ocaml.doc
          "The preferred maintenance period specified for the server."];
      preferredBackupWindow: TimeWindowDefinition.t option
        [@ocaml.doc "The preferred backup period specified for the server."];
      securityGroupIds: Strings.t option
        [@ocaml.doc
          "The security group IDs for the server, as specified in the CloudFormation stack. These might not be the same security groups that are shown in the EC2 console."];
      serviceRoleArn: String_.t option
        [@ocaml.doc "The service role ARN used to create the server."];
      status: ServerStatus.t option
        [@ocaml.doc
          "The server's status. This field displays the states of actions in progress, such as creating, running, or backing up the server, as well as the server's health state."];
      statusReason: String_.t option
        [@ocaml.doc
          "Depending on the server status, this field has either a human-readable message (such as a create or backup error), or an escaped block of JSON (used for health check results)."];
      subnetIds: Strings.t option
        [@ocaml.doc "The subnet IDs specified in a CreateServer request."];
      serverArn: String_.t option [@ocaml.doc "The ARN of the server."]}
    let make ?associatePublicIpAddress =
      fun ?backupRetentionCount ->
        fun ?serverName ->
          fun ?createdAt ->
            fun ?cloudFormationStackArn ->
              fun ?customDomain ->
                fun ?disableAutomatedBackup ->
                  fun ?endpoint ->
                    fun ?engine ->
                      fun ?engineModel ->
                        fun ?engineAttributes ->
                          fun ?engineVersion ->
                            fun ?instanceProfileArn ->
                              fun ?instanceType ->
                                fun ?keyPair ->
                                  fun ?maintenanceStatus ->
                                    fun ?preferredMaintenanceWindow ->
                                      fun ?preferredBackupWindow ->
                                        fun ?securityGroupIds ->
                                          fun ?serviceRoleArn ->
                                            fun ?status ->
                                              fun ?statusReason ->
                                                fun ?subnetIds ->
                                                  fun ?serverArn ->
                                                    fun () ->
                                                      {
                                                        associatePublicIpAddress;
                                                        backupRetentionCount;
                                                        serverName;
                                                        createdAt;
                                                        cloudFormationStackArn;
                                                        customDomain;
                                                        disableAutomatedBackup;
                                                        endpoint;
                                                        engine;
                                                        engineModel;
                                                        engineAttributes;
                                                        engineVersion;
                                                        instanceProfileArn;
                                                        instanceType;
                                                        keyPair;
                                                        maintenanceStatus;
                                                        preferredMaintenanceWindow;
                                                        preferredBackupWindow;
                                                        securityGroupIds;
                                                        serviceRoleArn;
                                                        status;
                                                        statusReason;
                                                        subnetIds;
                                                        serverArn
                                                      }
    let to_value x =
      structure_to_value
        [("AssociatePublicIpAddress",
           (Option.map x.associatePublicIpAddress ~f:Boolean.to_value));
        ("BackupRetentionCount",
          (Option.map x.backupRetentionCount ~f:Integer.to_value));
        ("ServerName", (Option.map x.serverName ~f:String_.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("CloudFormationStackArn",
          (Option.map x.cloudFormationStackArn ~f:String_.to_value));
        ("CustomDomain",
          (Option.map x.customDomain ~f:CustomDomain.to_value));
        ("DisableAutomatedBackup",
          (Option.map x.disableAutomatedBackup ~f:Boolean.to_value));
        ("Endpoint", (Option.map x.endpoint ~f:String_.to_value));
        ("Engine", (Option.map x.engine ~f:String_.to_value));
        ("EngineModel", (Option.map x.engineModel ~f:String_.to_value));
        ("EngineAttributes",
          (Option.map x.engineAttributes ~f:EngineAttributes.to_value));
        ("EngineVersion", (Option.map x.engineVersion ~f:String_.to_value));
        ("InstanceProfileArn",
          (Option.map x.instanceProfileArn ~f:String_.to_value));
        ("InstanceType", (Option.map x.instanceType ~f:String_.to_value));
        ("KeyPair", (Option.map x.keyPair ~f:String_.to_value));
        ("MaintenanceStatus",
          (Option.map x.maintenanceStatus ~f:MaintenanceStatus.to_value));
        ("PreferredMaintenanceWindow",
          (Option.map x.preferredMaintenanceWindow
             ~f:TimeWindowDefinition.to_value));
        ("PreferredBackupWindow",
          (Option.map x.preferredBackupWindow
             ~f:TimeWindowDefinition.to_value));
        ("SecurityGroupIds",
          (Option.map x.securityGroupIds ~f:Strings.to_value));
        ("ServiceRoleArn", (Option.map x.serviceRoleArn ~f:String_.to_value));
        ("Status", (Option.map x.status ~f:ServerStatus.to_value));
        ("StatusReason", (Option.map x.statusReason ~f:String_.to_value));
        ("SubnetIds", (Option.map x.subnetIds ~f:Strings.to_value));
        ("ServerArn", (Option.map x.serverArn ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serverArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ServerArn") in
      let subnetIds =
        (Option.map ~f:Strings.of_xml) (Xml.child xml_arg0 "SubnetIds") in
      let statusReason =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "StatusReason") in
      let status =
        (Option.map ~f:ServerStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let serviceRoleArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ServiceRoleArn") in
      let securityGroupIds =
        (Option.map ~f:Strings.of_xml)
          (Xml.child xml_arg0 "SecurityGroupIds") in
      let preferredBackupWindow =
        (Option.map ~f:TimeWindowDefinition.of_xml)
          (Xml.child xml_arg0 "PreferredBackupWindow") in
      let preferredMaintenanceWindow =
        (Option.map ~f:TimeWindowDefinition.of_xml)
          (Xml.child xml_arg0 "PreferredMaintenanceWindow") in
      let maintenanceStatus =
        (Option.map ~f:MaintenanceStatus.of_xml)
          (Xml.child xml_arg0 "MaintenanceStatus") in
      let keyPair =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "KeyPair") in
      let instanceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "InstanceType") in
      let instanceProfileArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "InstanceProfileArn") in
      let engineVersion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "EngineVersion") in
      let engineAttributes =
        (Option.map ~f:EngineAttributes.of_xml)
          (Xml.child xml_arg0 "EngineAttributes") in
      let engineModel =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "EngineModel") in
      let engine =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Engine") in
      let endpoint =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Endpoint") in
      let disableAutomatedBackup =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "DisableAutomatedBackup") in
      let customDomain =
        (Option.map ~f:CustomDomain.of_xml)
          (Xml.child xml_arg0 "CustomDomain") in
      let cloudFormationStackArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "CloudFormationStackArn") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let serverName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ServerName") in
      let backupRetentionCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "BackupRetentionCount") in
      let associatePublicIpAddress =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "AssociatePublicIpAddress") in
      make ?serverArn ?subnetIds ?statusReason ?status ?serviceRoleArn
        ?securityGroupIds ?preferredBackupWindow ?preferredMaintenanceWindow
        ?maintenanceStatus ?keyPair ?instanceType ?instanceProfileArn
        ?engineVersion ?engineAttributes ?engineModel ?engine ?endpoint
        ?disableAutomatedBackup ?customDomain ?cloudFormationStackArn
        ?createdAt ?serverName ?backupRetentionCount
        ?associatePublicIpAddress ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let serverArn = field_map json "ServerArn" String_.of_json in
      let subnetIds = field_map json "SubnetIds" Strings.of_json in
      let statusReason = field_map json "StatusReason" String_.of_json in
      let status = field_map json "Status" ServerStatus.of_json in
      let serviceRoleArn = field_map json "ServiceRoleArn" String_.of_json in
      let securityGroupIds =
        field_map json "SecurityGroupIds" Strings.of_json in
      let preferredBackupWindow =
        field_map json "PreferredBackupWindow" TimeWindowDefinition.of_json in
      let preferredMaintenanceWindow =
        field_map json "PreferredMaintenanceWindow"
          TimeWindowDefinition.of_json in
      let maintenanceStatus =
        field_map json "MaintenanceStatus" MaintenanceStatus.of_json in
      let keyPair = field_map json "KeyPair" String_.of_json in
      let instanceType = field_map json "InstanceType" String_.of_json in
      let instanceProfileArn =
        field_map json "InstanceProfileArn" String_.of_json in
      let engineVersion = field_map json "EngineVersion" String_.of_json in
      let engineAttributes =
        field_map json "EngineAttributes" EngineAttributes.of_json in
      let engineModel = field_map json "EngineModel" String_.of_json in
      let engine = field_map json "Engine" String_.of_json in
      let endpoint = field_map json "Endpoint" String_.of_json in
      let disableAutomatedBackup =
        field_map json "DisableAutomatedBackup" Boolean.of_json in
      let customDomain = field_map json "CustomDomain" CustomDomain.of_json in
      let cloudFormationStackArn =
        field_map json "CloudFormationStackArn" String_.of_json in
      let createdAt = field_map json "CreatedAt" Timestamp.of_json in
      let serverName = field_map json "ServerName" String_.of_json in
      let backupRetentionCount =
        field_map json "BackupRetentionCount" Integer.of_json in
      let associatePublicIpAddress =
        field_map json "AssociatePublicIpAddress" Boolean.of_json in
      make ?serverArn ?subnetIds ?statusReason ?status ?serviceRoleArn
        ?securityGroupIds ?preferredBackupWindow ?preferredMaintenanceWindow
        ?maintenanceStatus ?keyPair ?instanceType ?instanceProfileArn
        ?engineVersion ?engineAttributes ?engineModel ?engine ?endpoint
        ?disableAutomatedBackup ?customDomain ?cloudFormationStackArn
        ?createdAt ?serverName ?backupRetentionCount
        ?associatePublicIpAddress ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes a configuration management server."]
module ServerEvent =
  struct
    type nonrec t =
      {
      createdAt: Timestamp.t option
        [@ocaml.doc "The time when the event occurred."];
      serverName: String_.t option
        [@ocaml.doc
          "The name of the server on or for which the event occurred."];
      message: String_.t option
        [@ocaml.doc "A human-readable informational or status message."];
      logUrl: String_.t option
        [@ocaml.doc "The Amazon S3 URL of the event's log file."]}
    let make ?createdAt =
      fun ?serverName ->
        fun ?message ->
          fun ?logUrl -> fun () -> { createdAt; serverName; message; logUrl }
    let to_value x =
      structure_to_value
        [("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("ServerName", (Option.map x.serverName ~f:String_.to_value));
        ("Message", (Option.map x.message ~f:String_.to_value));
        ("LogUrl", (Option.map x.logUrl ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let logUrl =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "LogUrl") in
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      let serverName =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ServerName") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      make ?logUrl ?message ?serverName ?createdAt ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let logUrl = field_map json "LogUrl" String_.of_json in
      let message = field_map json "Message" String_.of_json in
      let serverName = field_map json "ServerName" String_.of_json in
      let createdAt = field_map json "CreatedAt" Timestamp.of_json in
      make ?logUrl ?message ?serverName ?createdAt ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "An event that is related to the server, such as the start of maintenance or backup."]
module Backup =
  struct
    type nonrec t =
      {
      backupArn: String_.t option [@ocaml.doc "The ARN of the backup."];
      backupId: BackupId.t option
        [@ocaml.doc
          "The generated ID of the backup. Example: myServerName-yyyyMMddHHmmssSSS"];
      backupType: BackupType.t option
        [@ocaml.doc "The backup type. Valid values are automated or manual."];
      createdAt: Timestamp.t option
        [@ocaml.doc
          "The time stamp when the backup was created in the database. Example: 2016-07-29T13:38:47.520Z"];
      description: String_.t option
        [@ocaml.doc
          "A user-provided description for a manual backup. This field is empty for automated backups."];
      engine: String_.t option
        [@ocaml.doc
          "The engine type that is obtained from the server when the backup is created."];
      engineModel: String_.t option
        [@ocaml.doc
          "The engine model that is obtained from the server when the backup is created."];
      engineVersion: String_.t option
        [@ocaml.doc
          "The engine version that is obtained from the server when the backup is created."];
      instanceProfileArn: String_.t option
        [@ocaml.doc
          "The EC2 instance profile ARN that is obtained from the server when the backup is created. Because this value is stored, you are not required to provide the InstanceProfileArn again if you restore a backup."];
      instanceType: String_.t option
        [@ocaml.doc
          "The instance type that is obtained from the server when the backup is created."];
      keyPair: String_.t option
        [@ocaml.doc
          "The key pair that is obtained from the server when the backup is created."];
      preferredBackupWindow: TimeWindowDefinition.t option
        [@ocaml.doc
          "The preferred backup period that is obtained from the server when the backup is created."];
      preferredMaintenanceWindow: TimeWindowDefinition.t option
        [@ocaml.doc
          "The preferred maintenance period that is obtained from the server when the backup is created."];
      s3DataSize: Integer.t option
        [@ocaml.doc "This field is deprecated and is no longer used."];
      s3DataUrl: String_.t option
        [@ocaml.doc "This field is deprecated and is no longer used."];
      s3LogUrl: String_.t option
        [@ocaml.doc "The Amazon S3 URL of the backup's log file."];
      securityGroupIds: Strings.t option
        [@ocaml.doc
          "The security group IDs that are obtained from the server when the backup is created."];
      serverName: ServerName.t option
        [@ocaml.doc "The name of the server from which the backup was made."];
      serviceRoleArn: String_.t option
        [@ocaml.doc
          "The service role ARN that is obtained from the server when the backup is created."];
      status: BackupStatus.t option
        [@ocaml.doc "The status of a backup while in progress."];
      statusDescription: String_.t option
        [@ocaml.doc "An informational message about backup status."];
      subnetIds: Strings.t option
        [@ocaml.doc
          "The subnet IDs that are obtained from the server when the backup is created."];
      toolsVersion: String_.t option
        [@ocaml.doc
          "The version of AWS OpsWorks CM-specific tools that is obtained from the server when the backup is created."];
      userArn: String_.t option
        [@ocaml.doc
          "The IAM user ARN of the requester for manual backups. This field is empty for automated backups."]}
    let make ?backupArn =
      fun ?backupId ->
        fun ?backupType ->
          fun ?createdAt ->
            fun ?description ->
              fun ?engine ->
                fun ?engineModel ->
                  fun ?engineVersion ->
                    fun ?instanceProfileArn ->
                      fun ?instanceType ->
                        fun ?keyPair ->
                          fun ?preferredBackupWindow ->
                            fun ?preferredMaintenanceWindow ->
                              fun ?s3DataSize ->
                                fun ?s3DataUrl ->
                                  fun ?s3LogUrl ->
                                    fun ?securityGroupIds ->
                                      fun ?serverName ->
                                        fun ?serviceRoleArn ->
                                          fun ?status ->
                                            fun ?statusDescription ->
                                              fun ?subnetIds ->
                                                fun ?toolsVersion ->
                                                  fun ?userArn ->
                                                    fun () ->
                                                      {
                                                        backupArn;
                                                        backupId;
                                                        backupType;
                                                        createdAt;
                                                        description;
                                                        engine;
                                                        engineModel;
                                                        engineVersion;
                                                        instanceProfileArn;
                                                        instanceType;
                                                        keyPair;
                                                        preferredBackupWindow;
                                                        preferredMaintenanceWindow;
                                                        s3DataSize;
                                                        s3DataUrl;
                                                        s3LogUrl;
                                                        securityGroupIds;
                                                        serverName;
                                                        serviceRoleArn;
                                                        status;
                                                        statusDescription;
                                                        subnetIds;
                                                        toolsVersion;
                                                        userArn
                                                      }
    let to_value x =
      structure_to_value
        [("BackupArn", (Option.map x.backupArn ~f:String_.to_value));
        ("BackupId", (Option.map x.backupId ~f:BackupId.to_value));
        ("BackupType", (Option.map x.backupType ~f:BackupType.to_value));
        ("CreatedAt", (Option.map x.createdAt ~f:Timestamp.to_value));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("Engine", (Option.map x.engine ~f:String_.to_value));
        ("EngineModel", (Option.map x.engineModel ~f:String_.to_value));
        ("EngineVersion", (Option.map x.engineVersion ~f:String_.to_value));
        ("InstanceProfileArn",
          (Option.map x.instanceProfileArn ~f:String_.to_value));
        ("InstanceType", (Option.map x.instanceType ~f:String_.to_value));
        ("KeyPair", (Option.map x.keyPair ~f:String_.to_value));
        ("PreferredBackupWindow",
          (Option.map x.preferredBackupWindow
             ~f:TimeWindowDefinition.to_value));
        ("PreferredMaintenanceWindow",
          (Option.map x.preferredMaintenanceWindow
             ~f:TimeWindowDefinition.to_value));
        ("S3DataSize", (Option.map x.s3DataSize ~f:Integer.to_value));
        ("S3DataUrl", (Option.map x.s3DataUrl ~f:String_.to_value));
        ("S3LogUrl", (Option.map x.s3LogUrl ~f:String_.to_value));
        ("SecurityGroupIds",
          (Option.map x.securityGroupIds ~f:Strings.to_value));
        ("ServerName", (Option.map x.serverName ~f:ServerName.to_value));
        ("ServiceRoleArn", (Option.map x.serviceRoleArn ~f:String_.to_value));
        ("Status", (Option.map x.status ~f:BackupStatus.to_value));
        ("StatusDescription",
          (Option.map x.statusDescription ~f:String_.to_value));
        ("SubnetIds", (Option.map x.subnetIds ~f:Strings.to_value));
        ("ToolsVersion", (Option.map x.toolsVersion ~f:String_.to_value));
        ("UserArn", (Option.map x.userArn ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let userArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "UserArn") in
      let toolsVersion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ToolsVersion") in
      let subnetIds =
        (Option.map ~f:Strings.of_xml) (Xml.child xml_arg0 "SubnetIds") in
      let statusDescription =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "StatusDescription") in
      let status =
        (Option.map ~f:BackupStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let serviceRoleArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "ServiceRoleArn") in
      let serverName =
        (Option.map ~f:ServerName.of_xml) (Xml.child xml_arg0 "ServerName") in
      let securityGroupIds =
        (Option.map ~f:Strings.of_xml)
          (Xml.child xml_arg0 "SecurityGroupIds") in
      let s3LogUrl =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "S3LogUrl") in
      let s3DataUrl =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "S3DataUrl") in
      let s3DataSize =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "S3DataSize") in
      let preferredMaintenanceWindow =
        (Option.map ~f:TimeWindowDefinition.of_xml)
          (Xml.child xml_arg0 "PreferredMaintenanceWindow") in
      let preferredBackupWindow =
        (Option.map ~f:TimeWindowDefinition.of_xml)
          (Xml.child xml_arg0 "PreferredBackupWindow") in
      let keyPair =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "KeyPair") in
      let instanceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "InstanceType") in
      let instanceProfileArn =
        (Option.map ~f:String_.of_xml)
          (Xml.child xml_arg0 "InstanceProfileArn") in
      let engineVersion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "EngineVersion") in
      let engineModel =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "EngineModel") in
      let engine =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Engine") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let createdAt =
        (Option.map ~f:Timestamp.of_xml) (Xml.child xml_arg0 "CreatedAt") in
      let backupType =
        (Option.map ~f:BackupType.of_xml) (Xml.child xml_arg0 "BackupType") in
      let backupId =
        (Option.map ~f:BackupId.of_xml) (Xml.child xml_arg0 "BackupId") in
      let backupArn =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "BackupArn") in
      make ?userArn ?toolsVersion ?subnetIds ?statusDescription ?status
        ?serviceRoleArn ?serverName ?securityGroupIds ?s3LogUrl ?s3DataUrl
        ?s3DataSize ?preferredMaintenanceWindow ?preferredBackupWindow
        ?keyPair ?instanceType ?instanceProfileArn ?engineVersion
        ?engineModel ?engine ?description ?createdAt ?backupType ?backupId
        ?backupArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let userArn = field_map json "UserArn" String_.of_json in
      let toolsVersion = field_map json "ToolsVersion" String_.of_json in
      let subnetIds = field_map json "SubnetIds" Strings.of_json in
      let statusDescription =
        field_map json "StatusDescription" String_.of_json in
      let status = field_map json "Status" BackupStatus.of_json in
      let serviceRoleArn = field_map json "ServiceRoleArn" String_.of_json in
      let serverName = field_map json "ServerName" ServerName.of_json in
      let securityGroupIds =
        field_map json "SecurityGroupIds" Strings.of_json in
      let s3LogUrl = field_map json "S3LogUrl" String_.of_json in
      let s3DataUrl = field_map json "S3DataUrl" String_.of_json in
      let s3DataSize = field_map json "S3DataSize" Integer.of_json in
      let preferredMaintenanceWindow =
        field_map json "PreferredMaintenanceWindow"
          TimeWindowDefinition.of_json in
      let preferredBackupWindow =
        field_map json "PreferredBackupWindow" TimeWindowDefinition.of_json in
      let keyPair = field_map json "KeyPair" String_.of_json in
      let instanceType = field_map json "InstanceType" String_.of_json in
      let instanceProfileArn =
        field_map json "InstanceProfileArn" String_.of_json in
      let engineVersion = field_map json "EngineVersion" String_.of_json in
      let engineModel = field_map json "EngineModel" String_.of_json in
      let engine = field_map json "Engine" String_.of_json in
      let description = field_map json "Description" String_.of_json in
      let createdAt = field_map json "CreatedAt" Timestamp.of_json in
      let backupType = field_map json "BackupType" BackupType.of_json in
      let backupId = field_map json "BackupId" BackupId.of_json in
      let backupArn = field_map json "BackupArn" String_.of_json in
      make ?userArn ?toolsVersion ?subnetIds ?statusDescription ?status
        ?serviceRoleArn ?serverName ?securityGroupIds ?s3LogUrl ?s3DataUrl
        ?s3DataSize ?preferredMaintenanceWindow ?preferredBackupWindow
        ?keyPair ?instanceType ?instanceProfileArn ?engineVersion
        ?engineModel ?engine ?description ?createdAt ?backupType ?backupId
        ?backupArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Describes a single backup."]
module AccountAttribute =
  struct
    type nonrec t =
      {
      name: String_.t option
        [@ocaml.doc
          "The attribute name. The following are supported attribute names. ServerLimit: The number of current servers/maximum number of servers allowed. By default, you can have a maximum of 10 servers. ManualBackupLimit: The number of current manual backups/maximum number of backups allowed. By default, you can have a maximum of 50 manual backups saved."];
      maximum: Integer.t option [@ocaml.doc "The maximum allowed value."];
      used: Integer.t option
        [@ocaml.doc
          "The current usage, such as the current number of servers that are associated with the account."]}
    let make ?name =
      fun ?maximum -> fun ?used -> fun () -> { name; maximum; used }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:String_.to_value));
        ("Maximum", (Option.map x.maximum ~f:Integer.to_value));
        ("Used", (Option.map x.used ~f:Integer.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let used = (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Used") in
      let maximum =
        (Option.map ~f:Integer.of_xml) (Xml.child xml_arg0 "Maximum") in
      let name = (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Name") in
      make ?used ?maximum ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let used = field_map json "Used" Integer.of_json in
      let maximum = field_map json "Maximum" Integer.of_json in
      let name = field_map json "Name" String_.of_json in
      make ?used ?maximum ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Stores account attributes."]
module InvalidStateException =
  struct
    type nonrec t =
      {
      message: String_.t option
        [@ocaml.doc
          "Error or informational message that provides more detail if a resource is in a state that is not valid for performing a specified action."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The resource is in a state that does not allow you to perform a specified action."]
module ResourceNotFoundException =
  struct
    type nonrec t =
      {
      message: String_.t option
        [@ocaml.doc
          "Error or informational message that can contain more detail about problems locating or accessing a resource."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The requested resource does not exist, or access was denied."]
module ValidationException =
  struct
    type nonrec t =
      {
      message: String_.t option
        [@ocaml.doc
          "Error or informational message that can contain more detail about a validation failure."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "One or more of the provided request parameters are not valid."]
module AttributeName =
  struct
    type nonrec t = string
    let context_ = "AttributeName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_min i ~min:1) >>=
             (fun () ->
                (check_string_max i ~max:64) >>=
                  (fun () -> check_pattern i ~pattern:"[A-Z][A-Z0-9_]*")));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AttributeName" j
    let to_json = simple_to_json to_value
  end
module AttributeValue =
  struct
    type nonrec t = string
    let context_ = "AttributeValue"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:10000) >>=
             (fun () -> check_pattern i ~pattern:"(?s).*"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AttributeValue" j
    let to_json = simple_to_json to_value
  end
module AWSOpsWorksCMResourceArn =
  struct
    type nonrec t = string
    let context_ = "AWSOpsWorksCMResourceArn"
    let make i =
      let open Result in
        ok_or_failwith
          (check_pattern i ~pattern:"arn:aws.*:opsworks-cm:.*:[0-9]{12}:.*");
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AWSOpsWorksCMResourceArn" j
    let to_json = simple_to_json to_value
  end
module TagKeyList =
  struct
    type nonrec t = TagKey.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeyList" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module TagList =
  struct
    type nonrec t = Tag.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:200) >>=
             (fun () -> check_list_min i ~min:0));
        i
    let to_value xs =
      (xs |> (List.map ~f:Tag.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tag.of_xml)
    let of_json j = list_of_json ~kind:"TagList" ~of_json:Tag.of_json j
    let to_json v = composed_to_json to_value v
  end
module KeyPair =
  struct
    type nonrec t = string
    let context_ = "KeyPair"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:10000) >>=
             (fun () -> check_pattern i ~pattern:".*"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"KeyPair" j
    let to_json = simple_to_json to_value
  end
module NextToken =
  struct
    type nonrec t = string
    let context_ = "NextToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:10000) >>=
             (fun () -> check_pattern i ~pattern:"(?s).*"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NextToken" j
    let to_json = simple_to_json to_value
  end
module MaxResults =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:1); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module NodeAssociationStatusToken =
  struct
    type nonrec t = string
    let context_ = "NodeAssociationStatusToken"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:10000) >>=
             (fun () -> check_pattern i ~pattern:"(?s).*"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NodeAssociationStatusToken" j
    let to_json = simple_to_json to_value
  end
module NodeName =
  struct
    type nonrec t = string[@@ocaml.doc
                            "The node name that is used by chef-client or puppet-agentfor a new node. We recommend to use a unique FQDN as hostname. For more information, see the Chef or Puppet documentation."]
    let context_ = "NodeName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:10000) >>=
             (fun () -> check_pattern i ~pattern:"^[\\-\\p{Alnum}_:.]+$"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NodeName" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "The node name that is used by chef-client or puppet-agentfor a new node. We recommend to use a unique FQDN as hostname. For more information, see the Chef or Puppet documentation."]
module InvalidNextTokenException =
  struct
    type nonrec t =
      {
      message: String_.t option
        [@ocaml.doc
          "Error or informational message that can contain more detail about a nextToken failure."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "This occurs when the provided nextToken is not valid."]
module Servers =
  struct
    type nonrec t = Server.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Server.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Server.of_xml)
    let of_json j = list_of_json ~kind:"Servers" ~of_json:Server.of_json j
    let to_json v = composed_to_json to_value v
  end
module NodeAssociationStatus =
  struct
    type nonrec t =
      | SUCCESS 
      | FAILED 
      | IN_PROGRESS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SUCCESS -> "SUCCESS"
      | FAILED -> "FAILED"
      | IN_PROGRESS -> "IN_PROGRESS"
      | Non_static_id s -> s
    let of_string =
      function
      | "SUCCESS" -> SUCCESS
      | "FAILED" -> FAILED
      | "IN_PROGRESS" -> IN_PROGRESS
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration NodeAssociationStatus" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"NodeAssociationStatus" j)
    let to_json = simple_to_json to_value
  end
module ServerEvents =
  struct
    type nonrec t = ServerEvent.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ServerEvent.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ServerEvent.of_xml)
    let of_json j =
      list_of_json ~kind:"ServerEvents" ~of_json:ServerEvent.of_json j
    let to_json v = composed_to_json to_value v
  end
module Backups =
  struct
    type nonrec t = Backup.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Backup.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Backup.of_xml)
    let of_json j = list_of_json ~kind:"Backups" ~of_json:Backup.of_json j
    let to_json v = composed_to_json to_value v
  end
module AccountAttributes =
  struct
    type nonrec t = AccountAttribute.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:AccountAttribute.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:AccountAttribute.of_xml)
    let of_json j =
      list_of_json ~kind:"AccountAttributes"
        ~of_json:AccountAttribute.of_json j
    let to_json v = composed_to_json to_value v
  end
module LimitExceededException =
  struct
    type nonrec t =
      {
      message: String_.t option
        [@ocaml.doc
          "Error or informational message that the maximum allowed number of servers or backups has been exceeded."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The limit of servers or backups has been reached."]
module ResourceAlreadyExistsException =
  struct
    type nonrec t =
      {
      message: String_.t option
        [@ocaml.doc
          "Error or informational message in response to a CreateServer request that a resource cannot be created because it already exists."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("Message", (Option.map x.message ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "Message" String_.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The requested resource cannot be created because it already exists."]
module BackupRetentionCountDefinition =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:1); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for BackupRetentionCountDefinition"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module CustomCertificate =
  struct
    type nonrec t = string
    let context_ = "CustomCertificate"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2097152) >>=
             (fun () ->
                check_pattern i
                  ~pattern:"(?s)\\s*-----BEGIN CERTIFICATE-----.+-----END CERTIFICATE-----\\s*"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CustomCertificate" j
    let to_json = simple_to_json to_value
  end
module CustomPrivateKey =
  struct
    type nonrec t = string
    let context_ = "CustomPrivateKey"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:4096) >>=
             (fun () ->
                check_pattern i
                  ~pattern:"(?ms)\\s*^-----BEGIN (?-s:.*)PRIVATE KEY-----$.*?^-----END (?-s:.*)PRIVATE KEY-----$\\s*"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CustomPrivateKey" j
    let to_json = simple_to_json to_value
  end
module InstanceProfileArn =
  struct
    type nonrec t = string
    let context_ = "InstanceProfileArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:10000) >>=
             (fun () ->
                check_pattern i
                  ~pattern:"arn:aws:iam::[0-9]{12}:instance-profile/.*"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"InstanceProfileArn" j
    let to_json = simple_to_json to_value
  end
module ServiceRoleArn =
  struct
    type nonrec t = string
    let context_ = "ServiceRoleArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:10000) >>=
             (fun () ->
                check_pattern i ~pattern:"arn:aws:iam::[0-9]{12}:role/.*"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ServiceRoleArn" j
    let to_json = simple_to_json to_value
  end
module UpdateServerResponse =
  struct
    type nonrec t =
      {
      server: Server.t option
        [@ocaml.doc "Contains the response to a UpdateServer request."]}
    type nonrec error =
      [ `InvalidStateException of InvalidStateException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?server = fun () -> { server }
    let error_of_json name json =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidStateException e ->
          `Assoc
            [("error", (`String "InvalidStateException"));
            ("details", (InvalidStateException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Server", (Option.map x.server ~f:Server.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let server =
        (Option.map ~f:Server.of_xml) (Xml.child xml_arg0 "Server") in
      make ?server ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let server = field_map json "Server" Server.of_json in make ?server ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates settings for a server. This operation is synchronous."]
module UpdateServerRequest =
  struct
    type nonrec t =
      {
      disableAutomatedBackup: Boolean.t option
        [@ocaml.doc
          "Setting DisableAutomatedBackup to true disables automated or scheduled backups. Automated backups are enabled by default."];
      backupRetentionCount: Integer.t option
        [@ocaml.doc
          "Sets the number of automated backups that you want to keep."];
      serverName: ServerName.t
        [@ocaml.doc "The name of the server to update."];
      preferredMaintenanceWindow: TimeWindowDefinition.t option ;
      preferredBackupWindow: TimeWindowDefinition.t option }
    let context_ = "UpdateServerRequest"
    let make ?disableAutomatedBackup =
      fun ?backupRetentionCount ->
        fun ?preferredMaintenanceWindow ->
          fun ?preferredBackupWindow ->
            fun ~serverName ->
              fun () ->
                {
                  disableAutomatedBackup;
                  backupRetentionCount;
                  preferredMaintenanceWindow;
                  preferredBackupWindow;
                  serverName
                }
    let to_value x =
      structure_to_value
        [("DisableAutomatedBackup",
           (Option.map x.disableAutomatedBackup ~f:Boolean.to_value));
        ("BackupRetentionCount",
          (Option.map x.backupRetentionCount ~f:Integer.to_value));
        ("ServerName", (Some (ServerName.to_value x.serverName)));
        ("PreferredMaintenanceWindow",
          (Option.map x.preferredMaintenanceWindow
             ~f:TimeWindowDefinition.to_value));
        ("PreferredBackupWindow",
          (Option.map x.preferredBackupWindow
             ~f:TimeWindowDefinition.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let preferredBackupWindow =
        (Option.map ~f:TimeWindowDefinition.of_xml)
          (Xml.child xml_arg0 "PreferredBackupWindow") in
      let preferredMaintenanceWindow =
        (Option.map ~f:TimeWindowDefinition.of_xml)
          (Xml.child xml_arg0 "PreferredMaintenanceWindow") in
      let serverName =
        ServerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServerName") in
      let backupRetentionCount =
        (Option.map ~f:Integer.of_xml)
          (Xml.child xml_arg0 "BackupRetentionCount") in
      let disableAutomatedBackup =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "DisableAutomatedBackup") in
      make ?preferredBackupWindow ?preferredMaintenanceWindow ~serverName
        ?backupRetentionCount ?disableAutomatedBackup ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let preferredBackupWindow =
        field_map json "PreferredBackupWindow" TimeWindowDefinition.of_json in
      let preferredMaintenanceWindow =
        field_map json "PreferredMaintenanceWindow"
          TimeWindowDefinition.of_json in
      let serverName = field_map_exn json "ServerName" ServerName.of_json in
      let backupRetentionCount =
        field_map json "BackupRetentionCount" Integer.of_json in
      let disableAutomatedBackup =
        field_map json "DisableAutomatedBackup" Boolean.of_json in
      make ?preferredBackupWindow ?preferredMaintenanceWindow ~serverName
        ?backupRetentionCount ?disableAutomatedBackup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates settings for a server. This operation is synchronous."]
module UpdateServerEngineAttributesResponse =
  struct
    type nonrec t =
      {
      server: Server.t option
        [@ocaml.doc
          "Contains the response to an UpdateServerEngineAttributes request."]}
    type nonrec error =
      [ `InvalidStateException of InvalidStateException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?server = fun () -> { server }
    let error_of_json name json =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidStateException e ->
          `Assoc
            [("error", (`String "InvalidStateException"));
            ("details", (InvalidStateException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Server", (Option.map x.server ~f:Server.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let server =
        (Option.map ~f:Server.of_xml) (Xml.child xml_arg0 "Server") in
      make ?server ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let server = field_map json "Server" Server.of_json in make ?server ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates engine-specific attributes on a specified server. The server enters the MODIFYING state when this operation is in progress. Only one update can occur at a time. You can use this command to reset a Chef server's public key (CHEF_PIVOTAL_KEY) or a Puppet server's admin password (PUPPET_ADMIN_PASSWORD). This operation is asynchronous. This operation can only be called for servers in HEALTHY or UNHEALTHY states. Otherwise, an InvalidStateException is raised. A ResourceNotFoundException is thrown when the server does not exist. A ValidationException is raised when parameters of the request are not valid."]
module UpdateServerEngineAttributesRequest =
  struct
    type nonrec t =
      {
      serverName: ServerName.t
        [@ocaml.doc "The name of the server to update."];
      attributeName: AttributeName.t
        [@ocaml.doc "The name of the engine attribute to update."];
      attributeValue: AttributeValue.t option
        [@ocaml.doc "The value to set for the attribute."]}
    let context_ = "UpdateServerEngineAttributesRequest"
    let make ?attributeValue =
      fun ~serverName ->
        fun ~attributeName ->
          fun () -> { attributeValue; serverName; attributeName }
    let to_value x =
      structure_to_value
        [("ServerName", (Some (ServerName.to_value x.serverName)));
        ("AttributeName", (Some (AttributeName.to_value x.attributeName)));
        ("AttributeValue",
          (Option.map x.attributeValue ~f:AttributeValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attributeValue =
        (Option.map ~f:AttributeValue.of_xml)
          (Xml.child xml_arg0 "AttributeValue") in
      let attributeName =
        AttributeName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "AttributeName") in
      let serverName =
        ServerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServerName") in
      make ?attributeValue ~attributeName ~serverName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let attributeValue =
        field_map json "AttributeValue" AttributeValue.of_json in
      let attributeName =
        field_map_exn json "AttributeName" AttributeName.of_json in
      let serverName = field_map_exn json "ServerName" ServerName.of_json in
      make ?attributeValue ~attributeName ~serverName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Updates engine-specific attributes on a specified server. The server enters the MODIFYING state when this operation is in progress. Only one update can occur at a time. You can use this command to reset a Chef server's public key (CHEF_PIVOTAL_KEY) or a Puppet server's admin password (PUPPET_ADMIN_PASSWORD). This operation is asynchronous. This operation can only be called for servers in HEALTHY or UNHEALTHY states. Otherwise, an InvalidStateException is raised. A ResourceNotFoundException is thrown when the server does not exist. A ValidationException is raised when parameters of the request are not valid."]
module UntagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidStateException of InvalidStateException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidStateException e ->
          `Assoc
            [("error", (`String "InvalidStateException"));
            ("details", (InvalidStateException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes specified tags from an AWS OpsWorks-CM server or backup."]
module UntagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: AWSOpsWorksCMResourceArn.t
        [@ocaml.doc
          "The Amazon Resource Number (ARN) of a resource from which you want to remove tags. For example, arn:aws:opsworks-cm:us-west-2:123456789012:server/test-owcm-server/EXAMPLE-66b0-4196-8274-d1a2bEXAMPLE."];
      tagKeys: TagKeyList.t
        [@ocaml.doc "The keys of tags that you want to remove."]}
    let context_ = "UntagResourceRequest"
    let make ~resourceArn =
      fun ~tagKeys -> fun () -> { resourceArn; tagKeys }
    let to_value x =
      structure_to_value
        [("ResourceArn",
           (Some (AWSOpsWorksCMResourceArn.to_value x.resourceArn)));
        ("TagKeys", (Some (TagKeyList.to_value x.tagKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tagKeys =
        TagKeyList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TagKeys") in
      let resourceArn =
        AWSOpsWorksCMResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceArn") in
      make ~tagKeys ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tagKeys = field_map_exn json "TagKeys" TagKeyList.of_json in
      let resourceArn =
        field_map_exn json "ResourceArn" AWSOpsWorksCMResourceArn.of_json in
      make ~tagKeys ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Removes specified tags from an AWS OpsWorks-CM server or backup."]
module TagResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidStateException of InvalidStateException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidStateException e ->
          `Assoc
            [("error", (`String "InvalidStateException"));
            ("details", (InvalidStateException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Applies tags to an AWS OpsWorks for Chef Automate or AWS OpsWorks for Puppet Enterprise server, or to server backups."]
module TagResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: AWSOpsWorksCMResourceArn.t
        [@ocaml.doc
          "The Amazon Resource Number (ARN) of a resource to which you want to apply tags. For example, arn:aws:opsworks-cm:us-west-2:123456789012:server/test-owcm-server/EXAMPLE-66b0-4196-8274-d1a2bEXAMPLE."];
      tags: TagList.t
        [@ocaml.doc
          "A map that contains tag keys and tag values to attach to AWS OpsWorks-CM servers or backups. The key cannot be empty. The key can be a maximum of 127 characters, and can contain only Unicode letters, numbers, or separators, or the following special characters: + - = . _ : / The value can be a maximum 255 characters, and contain only Unicode letters, numbers, or separators, or the following special characters: + - = . _ : / Leading and trailing white spaces are trimmed from both the key and value. A maximum of 50 user-applied tags is allowed for any AWS OpsWorks-CM server or backup."]}
    let context_ = "TagResourceRequest"
    let make ~resourceArn = fun ~tags -> fun () -> { resourceArn; tags }
    let to_value x =
      structure_to_value
        [("ResourceArn",
           (Some (AWSOpsWorksCMResourceArn.to_value x.resourceArn)));
        ("Tags", (Some (TagList.to_value x.tags)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags =
        TagList.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Tags") in
      let resourceArn =
        AWSOpsWorksCMResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceArn") in
      make ~tags ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map_exn json "Tags" TagList.of_json in
      let resourceArn =
        field_map_exn json "ResourceArn" AWSOpsWorksCMResourceArn.of_json in
      make ~tags ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Applies tags to an AWS OpsWorks for Chef Automate or AWS OpsWorks for Puppet Enterprise server, or to server backups."]
module StartMaintenanceResponse =
  struct
    type nonrec t =
      {
      server: Server.t option
        [@ocaml.doc "Contains the response to a StartMaintenance request."]}
    type nonrec error =
      [ `InvalidStateException of InvalidStateException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?server = fun () -> { server }
    let error_of_json name json =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidStateException e ->
          `Assoc
            [("error", (`String "InvalidStateException"));
            ("details", (InvalidStateException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Server", (Option.map x.server ~f:Server.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let server =
        (Option.map ~f:Server.of_xml) (Xml.child xml_arg0 "Server") in
      make ?server ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let server = field_map json "Server" Server.of_json in make ?server ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Manually starts server maintenance. This command can be useful if an earlier maintenance attempt failed, and the underlying cause of maintenance failure has been resolved. The server is in an UNDER_MAINTENANCE state while maintenance is in progress. Maintenance can only be started on servers in HEALTHY and UNHEALTHY states. Otherwise, an InvalidStateException is thrown. A ResourceNotFoundException is thrown when the server does not exist. A ValidationException is raised when parameters of the request are not valid."]
module StartMaintenanceRequest =
  struct
    type nonrec t =
      {
      serverName: ServerName.t
        [@ocaml.doc "The name of the server on which to run maintenance."];
      engineAttributes: EngineAttributes.t option
        [@ocaml.doc
          "Engine attributes that are specific to the server on which you want to run maintenance. Attributes accepted in a StartMaintenance request for Chef CHEF_MAJOR_UPGRADE: If a Chef Automate server is eligible for upgrade to Chef Automate 2, add this engine attribute to a StartMaintenance request and set the value to true to upgrade the server to Chef Automate 2. For more information, see Upgrade an AWS OpsWorks for Chef Automate Server to Chef Automate 2."]}
    let context_ = "StartMaintenanceRequest"
    let make ?engineAttributes =
      fun ~serverName -> fun () -> { engineAttributes; serverName }
    let to_value x =
      structure_to_value
        [("ServerName", (Some (ServerName.to_value x.serverName)));
        ("EngineAttributes",
          (Option.map x.engineAttributes ~f:EngineAttributes.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let engineAttributes =
        (Option.map ~f:EngineAttributes.of_xml)
          (Xml.child xml_arg0 "EngineAttributes") in
      let serverName =
        ServerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServerName") in
      make ?engineAttributes ~serverName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let engineAttributes =
        field_map json "EngineAttributes" EngineAttributes.of_json in
      let serverName = field_map_exn json "ServerName" ServerName.of_json in
      make ?engineAttributes ~serverName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Manually starts server maintenance. This command can be useful if an earlier maintenance attempt failed, and the underlying cause of maintenance failure has been resolved. The server is in an UNDER_MAINTENANCE state while maintenance is in progress. Maintenance can only be started on servers in HEALTHY and UNHEALTHY states. Otherwise, an InvalidStateException is thrown. A ResourceNotFoundException is thrown when the server does not exist. A ValidationException is raised when parameters of the request are not valid."]
module RestoreServerResponse =
  struct
    type nonrec t = {
      server: Server.t option }
    type nonrec error =
      [ `InvalidStateException of InvalidStateException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?server = fun () -> { server }
    let error_of_json name json =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidStateException e ->
          `Assoc
            [("error", (`String "InvalidStateException"));
            ("details", (InvalidStateException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Server", (Option.map x.server ~f:Server.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let server =
        (Option.map ~f:Server.of_xml) (Xml.child xml_arg0 "Server") in
      make ?server ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let server = field_map json "Server" Server.of_json in make ?server ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Restores a backup to a server that is in a CONNECTION_LOST, HEALTHY, RUNNING, UNHEALTHY, or TERMINATED state. When you run RestoreServer, the server's EC2 instance is deleted, and a new EC2 instance is configured. RestoreServer maintains the existing server endpoint, so configuration management of the server's client devices (nodes) should continue to work. Restoring from a backup is performed by creating a new EC2 instance. If restoration is successful, and the server is in a HEALTHY state, AWS OpsWorks CM switches traffic over to the new instance. After restoration is finished, the old EC2 instance is maintained in a Running or Stopped state, but is eventually terminated. This operation is asynchronous. An InvalidStateException is thrown when the server is not in a valid state. A ResourceNotFoundException is thrown when the server does not exist. A ValidationException is raised when parameters of the request are not valid."]
module RestoreServerRequest =
  struct
    type nonrec t =
      {
      backupId: BackupId.t
        [@ocaml.doc
          "The ID of the backup that you want to use to restore a server."];
      serverName: ServerName.t
        [@ocaml.doc "The name of the server that you want to restore."];
      instanceType: String_.t option
        [@ocaml.doc
          "The type of instance to restore. Valid values must be specified in the following format: ^(\\[cm\\]\\[34\\]|t2).* For example, m5.large. Valid values are m5.large, r5.xlarge, and r5.2xlarge. If you do not specify this parameter, RestoreServer uses the instance type from the specified backup."];
      keyPair: KeyPair.t option
        [@ocaml.doc
          "The name of the key pair to set on the new EC2 instance. This can be helpful if the administrator no longer has the SSH key."]}
    let context_ = "RestoreServerRequest"
    let make ?instanceType =
      fun ?keyPair ->
        fun ~backupId ->
          fun ~serverName ->
            fun () -> { instanceType; keyPair; backupId; serverName }
    let to_value x =
      structure_to_value
        [("BackupId", (Some (BackupId.to_value x.backupId)));
        ("ServerName", (Some (ServerName.to_value x.serverName)));
        ("InstanceType", (Option.map x.instanceType ~f:String_.to_value));
        ("KeyPair", (Option.map x.keyPair ~f:KeyPair.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let keyPair =
        (Option.map ~f:KeyPair.of_xml) (Xml.child xml_arg0 "KeyPair") in
      let instanceType =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "InstanceType") in
      let serverName =
        ServerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServerName") in
      let backupId =
        BackupId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "BackupId") in
      make ?keyPair ?instanceType ~serverName ~backupId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let keyPair = field_map json "KeyPair" KeyPair.of_json in
      let instanceType = field_map json "InstanceType" String_.of_json in
      let serverName = field_map_exn json "ServerName" ServerName.of_json in
      let backupId = field_map_exn json "BackupId" BackupId.of_json in
      make ?keyPair ?instanceType ~serverName ~backupId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Restores a backup to a server that is in a CONNECTION_LOST, HEALTHY, RUNNING, UNHEALTHY, or TERMINATED state. When you run RestoreServer, the server's EC2 instance is deleted, and a new EC2 instance is configured. RestoreServer maintains the existing server endpoint, so configuration management of the server's client devices (nodes) should continue to work. Restoring from a backup is performed by creating a new EC2 instance. If restoration is successful, and the server is in a HEALTHY state, AWS OpsWorks CM switches traffic over to the new instance. After restoration is finished, the old EC2 instance is maintained in a Running or Stopped state, but is eventually terminated. This operation is asynchronous. An InvalidStateException is thrown when the server is not in a valid state. A ResourceNotFoundException is thrown when the server does not exist. A ValidationException is raised when parameters of the request are not valid."]
module ListTagsForResourceResponse =
  struct
    type nonrec t =
      {
      tags: TagList.t option
        [@ocaml.doc "Tags that have been applied to the resource."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "A token that you can use as the value of NextToken in subsequent calls to the API to show more results."]}
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?tags = fun ?nextToken -> fun () -> { tags; nextToken }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Tags", (Option.map x.tags ~f:TagList.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      make ?nextToken ?tags ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let tags = field_map json "Tags" TagList.of_json in
      make ?nextToken ?tags ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of tags that are applied to the specified AWS OpsWorks for Chef Automate or AWS OpsWorks for Puppet Enterprise servers or backups."]
module ListTagsForResourceRequest =
  struct
    type nonrec t =
      {
      resourceArn: AWSOpsWorksCMResourceArn.t
        [@ocaml.doc
          "The Amazon Resource Number (ARN) of an AWS OpsWorks for Chef Automate or AWS OpsWorks for Puppet Enterprise server for which you want to show applied tags. For example, arn:aws:opsworks-cm:us-west-2:123456789012:server/test-owcm-server/EXAMPLE-66b0-4196-8274-d1a2bEXAMPLE."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "NextToken is a string that is returned in some command responses. It indicates that not all entries have been returned, and that you must run at least one more request to get remaining items. To get remaining results, call ListTagsForResource again, and assign the token from the previous results as the value of the nextToken parameter. If there are no more results, the response object's nextToken parameter value is null. Setting a nextToken value that was not returned in your previous results causes an InvalidNextTokenException to occur."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "To receive a paginated response, use this parameter to specify the maximum number of results to be returned with a single call. If the number of available results exceeds this maximum, the response includes a NextToken value that you can assign to the NextToken request parameter to get the next set of results."]}
    let context_ = "ListTagsForResourceRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ~resourceArn -> fun () -> { nextToken; maxResults; resourceArn }
    let to_value x =
      structure_to_value
        [("ResourceArn",
           (Some (AWSOpsWorksCMResourceArn.to_value x.resourceArn)));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let resourceArn =
        AWSOpsWorksCMResourceArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceArn") in
      make ?maxResults ?nextToken ~resourceArn ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let resourceArn =
        field_map_exn json "ResourceArn" AWSOpsWorksCMResourceArn.of_json in
      make ?maxResults ?nextToken ~resourceArn ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns a list of tags that are applied to the specified AWS OpsWorks for Chef Automate or AWS OpsWorks for Puppet Enterprise servers or backups."]
module ExportServerEngineAttributeResponse =
  struct
    type nonrec t =
      {
      engineAttribute: EngineAttribute.t option
        [@ocaml.doc
          "The requested engine attribute pair with attribute name and value."];
      serverName: ServerName.t option
        [@ocaml.doc "The server name used in the request."]}
    type nonrec error =
      [ `InvalidStateException of InvalidStateException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?engineAttribute =
      fun ?serverName -> fun () -> { engineAttribute; serverName }
    let error_of_json name json =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidStateException e ->
          `Assoc
            [("error", (`String "InvalidStateException"));
            ("details", (InvalidStateException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("EngineAttribute",
           (Option.map x.engineAttribute ~f:EngineAttribute.to_value));
        ("ServerName", (Option.map x.serverName ~f:ServerName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serverName =
        (Option.map ~f:ServerName.of_xml) (Xml.child xml_arg0 "ServerName") in
      let engineAttribute =
        (Option.map ~f:EngineAttribute.of_xml)
          (Xml.child xml_arg0 "EngineAttribute") in
      make ?serverName ?engineAttribute ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let serverName = field_map json "ServerName" ServerName.of_json in
      let engineAttribute =
        field_map json "EngineAttribute" EngineAttribute.of_json in
      make ?serverName ?engineAttribute ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Exports a specified server engine attribute as a base64-encoded string. For example, you can export user data that you can use in EC2 to associate nodes with a server. This operation is synchronous. A ValidationException is raised when parameters of the request are not valid. A ResourceNotFoundException is thrown when the server does not exist. An InvalidStateException is thrown when the server is in any of the following states: CREATING, TERMINATED, FAILED or DELETING."]
module ExportServerEngineAttributeRequest =
  struct
    type nonrec t =
      {
      exportAttributeName: String_.t
        [@ocaml.doc
          "The name of the export attribute. Currently, the supported export attribute is Userdata. This exports a user data script that includes parameters and values provided in the InputAttributes list."];
      serverName: ServerName.t
        [@ocaml.doc
          "The name of the server from which you are exporting the attribute."];
      inputAttributes: EngineAttributes.t option
        [@ocaml.doc
          "The list of engine attributes. The list type is EngineAttribute. An EngineAttribute list item is a pair that includes an attribute name and its value. For the Userdata ExportAttributeName, the following are supported engine attribute names. RunList In Chef, a list of roles or recipes that are run in the specified order. In Puppet, this parameter is ignored. OrganizationName In Chef, an organization name. AWS OpsWorks for Chef Automate always creates the organization default. In Puppet, this parameter is ignored. NodeEnvironment In Chef, a node environment (for example, development, staging, or one-box). In Puppet, this parameter is ignored. NodeClientVersion In Chef, the version of the Chef engine (three numbers separated by dots, such as 13.8.5). If this attribute is empty, OpsWorks for Chef Automate uses the most current version. In Puppet, this parameter is ignored."]}
    let context_ = "ExportServerEngineAttributeRequest"
    let make ?inputAttributes =
      fun ~exportAttributeName ->
        fun ~serverName ->
          fun () -> { inputAttributes; exportAttributeName; serverName }
    let to_value x =
      structure_to_value
        [("ExportAttributeName",
           (Some (String_.to_value x.exportAttributeName)));
        ("ServerName", (Some (ServerName.to_value x.serverName)));
        ("InputAttributes",
          (Option.map x.inputAttributes ~f:EngineAttributes.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let inputAttributes =
        (Option.map ~f:EngineAttributes.of_xml)
          (Xml.child xml_arg0 "InputAttributes") in
      let serverName =
        ServerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServerName") in
      let exportAttributeName =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ExportAttributeName") in
      make ?inputAttributes ~serverName ~exportAttributeName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let inputAttributes =
        field_map json "InputAttributes" EngineAttributes.of_json in
      let serverName = field_map_exn json "ServerName" ServerName.of_json in
      let exportAttributeName =
        field_map_exn json "ExportAttributeName" String_.of_json in
      make ?inputAttributes ~serverName ~exportAttributeName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Exports a specified server engine attribute as a base64-encoded string. For example, you can export user data that you can use in EC2 to associate nodes with a server. This operation is synchronous. A ValidationException is raised when parameters of the request are not valid. A ResourceNotFoundException is thrown when the server does not exist. An InvalidStateException is thrown when the server is in any of the following states: CREATING, TERMINATED, FAILED or DELETING."]
module DisassociateNodeResponse =
  struct
    type nonrec t =
      {
      nodeAssociationStatusToken: NodeAssociationStatusToken.t option
        [@ocaml.doc
          "Contains a token which can be passed to the DescribeNodeAssociationStatus API call to get the status of the disassociation request."]}
    type nonrec error =
      [ `InvalidStateException of InvalidStateException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nodeAssociationStatusToken =
      fun () -> { nodeAssociationStatusToken }
    let error_of_json name json =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidStateException e ->
          `Assoc
            [("error", (`String "InvalidStateException"));
            ("details", (InvalidStateException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("NodeAssociationStatusToken",
           (Option.map x.nodeAssociationStatusToken
              ~f:NodeAssociationStatusToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nodeAssociationStatusToken =
        (Option.map ~f:NodeAssociationStatusToken.of_xml)
          (Xml.child xml_arg0 "NodeAssociationStatusToken") in
      make ?nodeAssociationStatusToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nodeAssociationStatusToken =
        field_map json "NodeAssociationStatusToken"
          NodeAssociationStatusToken.of_json in
      make ?nodeAssociationStatusToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Disassociates a node from an AWS OpsWorks CM server, and removes the node from the server's managed nodes. After a node is disassociated, the node key pair is no longer valid for accessing the configuration manager's API. For more information about how to associate a node, see AssociateNode. A node can can only be disassociated from a server that is in a HEALTHY state. Otherwise, an InvalidStateException is thrown. A ResourceNotFoundException is thrown when the server does not exist. A ValidationException is raised when parameters of the request are not valid."]
module DisassociateNodeRequest =
  struct
    type nonrec t =
      {
      serverName: ServerName.t
        [@ocaml.doc
          "The name of the server from which to disassociate the node."];
      nodeName: NodeName.t [@ocaml.doc "The name of the client node."];
      engineAttributes: EngineAttributes.t option
        [@ocaml.doc
          "Engine attributes that are used for disassociating the node. No attributes are required for Puppet. Attributes required in a DisassociateNode request for Chef CHEF_ORGANIZATION: The Chef organization with which the node was associated. By default only one organization named default can exist."]}
    let context_ = "DisassociateNodeRequest"
    let make ?engineAttributes =
      fun ~serverName ->
        fun ~nodeName -> fun () -> { engineAttributes; serverName; nodeName }
    let to_value x =
      structure_to_value
        [("ServerName", (Some (ServerName.to_value x.serverName)));
        ("NodeName", (Some (NodeName.to_value x.nodeName)));
        ("EngineAttributes",
          (Option.map x.engineAttributes ~f:EngineAttributes.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let engineAttributes =
        (Option.map ~f:EngineAttributes.of_xml)
          (Xml.child xml_arg0 "EngineAttributes") in
      let nodeName =
        NodeName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "NodeName") in
      let serverName =
        ServerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServerName") in
      make ?engineAttributes ~nodeName ~serverName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let engineAttributes =
        field_map json "EngineAttributes" EngineAttributes.of_json in
      let nodeName = field_map_exn json "NodeName" NodeName.of_json in
      let serverName = field_map_exn json "ServerName" ServerName.of_json in
      make ?engineAttributes ~nodeName ~serverName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Disassociates a node from an AWS OpsWorks CM server, and removes the node from the server's managed nodes. After a node is disassociated, the node key pair is no longer valid for accessing the configuration manager's API. For more information about how to associate a node, see AssociateNode. A node can can only be disassociated from a server that is in a HEALTHY state. Otherwise, an InvalidStateException is thrown. A ResourceNotFoundException is thrown when the server does not exist. A ValidationException is raised when parameters of the request are not valid."]
module DescribeServersResponse =
  struct
    type nonrec t =
      {
      servers: Servers.t option
        [@ocaml.doc
          "Contains the response to a DescribeServers request. For Chef Automate servers: If DescribeServersResponse$Servers$EngineAttributes includes CHEF_MAJOR_UPGRADE_AVAILABLE, you can upgrade the Chef Automate server to Chef Automate 2. To be eligible for upgrade, a server running Chef Automate 1 must have had at least one successful maintenance run after November 1, 2019. For Puppet servers: DescribeServersResponse$Servers$EngineAttributes contains the following two responses: PUPPET_API_CA_CERT, the PEM-encoded CA certificate that is used by the Puppet API over TCP port number 8140. The CA certificate is also used to sign node certificates. PUPPET_API_CRL, a certificate revocation list. The certificate revocation list is for internal maintenance purposes only. For more information about the Puppet certificate revocation list, see Man Page: puppet certificate_revocation_list in the Puppet documentation."];
      nextToken: String_.t option
        [@ocaml.doc
          "This is not currently implemented for DescribeServers requests."]}
    type nonrec error =
      [ `InvalidNextTokenException of InvalidNextTokenException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?servers = fun ?nextToken -> fun () -> { servers; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidNextTokenException e ->
          `Assoc
            [("error", (`String "InvalidNextTokenException"));
            ("details", (InvalidNextTokenException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Servers", (Option.map x.servers ~f:Servers.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let servers =
        (Option.map ~f:Servers.of_xml) (Xml.child xml_arg0 "Servers") in
      make ?nextToken ?servers ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let servers = field_map json "Servers" Servers.of_json in
      make ?nextToken ?servers ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all configuration management servers that are identified with your account. Only the stored results from Amazon DynamoDB are returned. AWS OpsWorks CM does not query other services. This operation is synchronous. A ResourceNotFoundException is thrown when the server does not exist. A ValidationException is raised when parameters of the request are not valid."]
module DescribeServersRequest =
  struct
    type nonrec t =
      {
      serverName: ServerName.t option
        [@ocaml.doc "Describes the server with the specified ServerName."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "This is not currently implemented for DescribeServers requests."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "This is not currently implemented for DescribeServers requests."]}
    let make ?serverName =
      fun ?nextToken ->
        fun ?maxResults -> fun () -> { serverName; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("ServerName", (Option.map x.serverName ~f:ServerName.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let serverName =
        (Option.map ~f:ServerName.of_xml) (Xml.child xml_arg0 "ServerName") in
      make ?maxResults ?nextToken ?serverName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let serverName = field_map json "ServerName" ServerName.of_json in
      make ?maxResults ?nextToken ?serverName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all configuration management servers that are identified with your account. Only the stored results from Amazon DynamoDB are returned. AWS OpsWorks CM does not query other services. This operation is synchronous. A ResourceNotFoundException is thrown when the server does not exist. A ValidationException is raised when parameters of the request are not valid."]
module DescribeNodeAssociationStatusResponse =
  struct
    type nonrec t =
      {
      nodeAssociationStatus: NodeAssociationStatus.t option
        [@ocaml.doc
          "The status of the association or disassociation request. Possible values: SUCCESS: The association or disassociation succeeded. FAILED: The association or disassociation failed. IN_PROGRESS: The association or disassociation is still in progress."];
      engineAttributes: EngineAttributes.t option
        [@ocaml.doc
          "Attributes specific to the node association. In Puppet, the attibute PUPPET_NODE_CERT contains the signed certificate (the result of the CSR)."]}
    type nonrec error =
      [ `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nodeAssociationStatus =
      fun ?engineAttributes ->
        fun () -> { nodeAssociationStatus; engineAttributes }
    let error_of_json name json =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("NodeAssociationStatus",
           (Option.map x.nodeAssociationStatus
              ~f:NodeAssociationStatus.to_value));
        ("EngineAttributes",
          (Option.map x.engineAttributes ~f:EngineAttributes.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let engineAttributes =
        (Option.map ~f:EngineAttributes.of_xml)
          (Xml.child xml_arg0 "EngineAttributes") in
      let nodeAssociationStatus =
        (Option.map ~f:NodeAssociationStatus.of_xml)
          (Xml.child xml_arg0 "NodeAssociationStatus") in
      make ?engineAttributes ?nodeAssociationStatus ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let engineAttributes =
        field_map json "EngineAttributes" EngineAttributes.of_json in
      let nodeAssociationStatus =
        field_map json "NodeAssociationStatus" NodeAssociationStatus.of_json in
      make ?engineAttributes ?nodeAssociationStatus ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the current status of an existing association or disassociation request. A ResourceNotFoundException is thrown when no recent association or disassociation request with the specified token is found, or when the server does not exist. A ValidationException is raised when parameters of the request are not valid."]
module DescribeNodeAssociationStatusRequest =
  struct
    type nonrec t =
      {
      nodeAssociationStatusToken: NodeAssociationStatusToken.t
        [@ocaml.doc
          "The token returned in either the AssociateNodeResponse or the DisassociateNodeResponse."];
      serverName: ServerName.t
        [@ocaml.doc
          "The name of the server from which to disassociate the node."]}
    let context_ = "DescribeNodeAssociationStatusRequest"
    let make ~nodeAssociationStatusToken =
      fun ~serverName -> fun () -> { nodeAssociationStatusToken; serverName }
    let to_value x =
      structure_to_value
        [("NodeAssociationStatusToken",
           (Some
              (NodeAssociationStatusToken.to_value
                 x.nodeAssociationStatusToken)));
        ("ServerName", (Some (ServerName.to_value x.serverName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serverName =
        ServerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServerName") in
      let nodeAssociationStatusToken =
        NodeAssociationStatusToken.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "NodeAssociationStatusToken") in
      make ~serverName ~nodeAssociationStatusToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let serverName = field_map_exn json "ServerName" ServerName.of_json in
      let nodeAssociationStatusToken =
        field_map_exn json "NodeAssociationStatusToken"
          NodeAssociationStatusToken.of_json in
      make ~serverName ~nodeAssociationStatusToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns the current status of an existing association or disassociation request. A ResourceNotFoundException is thrown when no recent association or disassociation request with the specified token is found, or when the server does not exist. A ValidationException is raised when parameters of the request are not valid."]
module DescribeEventsResponse =
  struct
    type nonrec t =
      {
      serverEvents: ServerEvents.t option
        [@ocaml.doc "Contains the response to a DescribeEvents request."];
      nextToken: String_.t option
        [@ocaml.doc
          "NextToken is a string that is returned in some command responses. It indicates that not all entries have been returned, and that you must run at least one more request to get remaining items. To get remaining results, call DescribeEvents again, and assign the token from the previous results as the value of the nextToken parameter. If there are no more results, the response object's nextToken parameter value is null. Setting a nextToken value that was not returned in your previous results causes an InvalidNextTokenException to occur."]}
    type nonrec error =
      [ `InvalidNextTokenException of InvalidNextTokenException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?serverEvents =
      fun ?nextToken -> fun () -> { serverEvents; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidNextTokenException e ->
          `Assoc
            [("error", (`String "InvalidNextTokenException"));
            ("details", (InvalidNextTokenException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ServerEvents",
           (Option.map x.serverEvents ~f:ServerEvents.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let serverEvents =
        (Option.map ~f:ServerEvents.of_xml)
          (Xml.child xml_arg0 "ServerEvents") in
      make ?nextToken ?serverEvents ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let serverEvents = field_map json "ServerEvents" ServerEvents.of_json in
      make ?nextToken ?serverEvents ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes events for a specified server. Results are ordered by time, with newest events first. This operation is synchronous. A ResourceNotFoundException is thrown when the server does not exist. A ValidationException is raised when parameters of the request are not valid."]
module DescribeEventsRequest =
  struct
    type nonrec t =
      {
      serverName: ServerName.t
        [@ocaml.doc
          "The name of the server for which you want to view events."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "NextToken is a string that is returned in some command responses. It indicates that not all entries have been returned, and that you must run at least one more request to get remaining items. To get remaining results, call DescribeEvents again, and assign the token from the previous results as the value of the nextToken parameter. If there are no more results, the response object's nextToken parameter value is null. Setting a nextToken value that was not returned in your previous results causes an InvalidNextTokenException to occur."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "To receive a paginated response, use this parameter to specify the maximum number of results to be returned with a single call. If the number of available results exceeds this maximum, the response includes a NextToken value that you can assign to the NextToken request parameter to get the next set of results."]}
    let context_ = "DescribeEventsRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ~serverName -> fun () -> { nextToken; maxResults; serverName }
    let to_value x =
      structure_to_value
        [("ServerName", (Some (ServerName.to_value x.serverName)));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let serverName =
        ServerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServerName") in
      make ?maxResults ?nextToken ~serverName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let serverName = field_map_exn json "ServerName" ServerName.of_json in
      make ?maxResults ?nextToken ~serverName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes events for a specified server. Results are ordered by time, with newest events first. This operation is synchronous. A ResourceNotFoundException is thrown when the server does not exist. A ValidationException is raised when parameters of the request are not valid."]
module DescribeBackupsResponse =
  struct
    type nonrec t =
      {
      backups: Backups.t option
        [@ocaml.doc "Contains the response to a DescribeBackups request."];
      nextToken: String_.t option
        [@ocaml.doc
          "This is not currently implemented for DescribeBackups requests."]}
    type nonrec error =
      [ `InvalidNextTokenException of InvalidNextTokenException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?backups = fun ?nextToken -> fun () -> { backups; nextToken }
    let error_of_json name json =
      match name with
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidNextTokenException" ->
          `InvalidNextTokenException (InvalidNextTokenException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidNextTokenException e ->
          `Assoc
            [("error", (`String "InvalidNextTokenException"));
            ("details", (InvalidNextTokenException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Backups", (Option.map x.backups ~f:Backups.to_value));
        ("NextToken", (Option.map x.nextToken ~f:String_.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "NextToken") in
      let backups =
        (Option.map ~f:Backups.of_xml) (Xml.child xml_arg0 "Backups") in
      make ?nextToken ?backups ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" String_.of_json in
      let backups = field_map json "Backups" Backups.of_json in
      make ?nextToken ?backups ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes backups. The results are ordered by time, with newest backups first. If you do not specify a BackupId or ServerName, the command returns all backups. This operation is synchronous. A ResourceNotFoundException is thrown when the backup does not exist. A ValidationException is raised when parameters of the request are not valid."]
module DescribeBackupsRequest =
  struct
    type nonrec t =
      {
      backupId: BackupId.t option [@ocaml.doc "Describes a single backup."];
      serverName: ServerName.t option
        [@ocaml.doc
          "Returns backups for the server with the specified ServerName."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "This is not currently implemented for DescribeBackups requests."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "This is not currently implemented for DescribeBackups requests."]}
    let make ?backupId =
      fun ?serverName ->
        fun ?nextToken ->
          fun ?maxResults ->
            fun () -> { backupId; serverName; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("BackupId", (Option.map x.backupId ~f:BackupId.to_value));
        ("ServerName", (Option.map x.serverName ~f:ServerName.to_value));
        ("NextToken", (Option.map x.nextToken ~f:NextToken.to_value));
        ("MaxResults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "MaxResults") in
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "NextToken") in
      let serverName =
        (Option.map ~f:ServerName.of_xml) (Xml.child xml_arg0 "ServerName") in
      let backupId =
        (Option.map ~f:BackupId.of_xml) (Xml.child xml_arg0 "BackupId") in
      make ?maxResults ?nextToken ?serverName ?backupId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" NextToken.of_json in
      let serverName = field_map json "ServerName" ServerName.of_json in
      let backupId = field_map json "BackupId" BackupId.of_json in
      make ?maxResults ?nextToken ?serverName ?backupId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes backups. The results are ordered by time, with newest backups first. If you do not specify a BackupId or ServerName, the command returns all backups. This operation is synchronous. A ResourceNotFoundException is thrown when the backup does not exist. A ValidationException is raised when parameters of the request are not valid."]
module DescribeAccountAttributesResponse =
  struct
    type nonrec t =
      {
      attributes: AccountAttributes.t option
        [@ocaml.doc "The attributes that are currently set for the account."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let make ?attributes = fun () -> { attributes }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Attributes",
           (Option.map x.attributes ~f:AccountAttributes.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attributes =
        (Option.map ~f:AccountAttributes.of_xml)
          (Xml.child xml_arg0 "Attributes") in
      make ?attributes ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let attributes = field_map json "Attributes" AccountAttributes.of_json in
      make ?attributes ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes your OpsWorks-CM account attributes. This operation is synchronous."]
module DescribeAccountAttributesRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Describes your OpsWorks-CM account attributes. This operation is synchronous."]
module DeleteServerResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidStateException of InvalidStateException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidStateException e ->
          `Assoc
            [("error", (`String "InvalidStateException"));
            ("details", (InvalidStateException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the server and the underlying AWS CloudFormation stacks (including the server's EC2 instance). When you run this command, the server state is updated to DELETING. After the server is deleted, it is no longer returned by DescribeServer requests. If the AWS CloudFormation stack cannot be deleted, the server cannot be deleted. This operation is asynchronous. An InvalidStateException is thrown when a server deletion is already in progress. A ResourceNotFoundException is thrown when the server does not exist. A ValidationException is raised when parameters of the request are not valid."]
module DeleteServerRequest =
  struct
    type nonrec t =
      {
      serverName: ServerName.t [@ocaml.doc "The ID of the server to delete."]}
    let context_ = "DeleteServerRequest"
    let make ~serverName = fun () -> { serverName }
    let to_value x =
      structure_to_value
        [("ServerName", (Some (ServerName.to_value x.serverName)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let serverName =
        ServerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServerName") in
      make ~serverName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let serverName = field_map_exn json "ServerName" ServerName.of_json in
      make ~serverName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes the server and the underlying AWS CloudFormation stacks (including the server's EC2 instance). When you run this command, the server state is updated to DELETING. After the server is deleted, it is no longer returned by DescribeServer requests. If the AWS CloudFormation stack cannot be deleted, the server cannot be deleted. This operation is asynchronous. An InvalidStateException is thrown when a server deletion is already in progress. A ResourceNotFoundException is thrown when the server does not exist. A ValidationException is raised when parameters of the request are not valid."]
module DeleteBackupResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidStateException of InvalidStateException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidStateException e ->
          `Assoc
            [("error", (`String "InvalidStateException"));
            ("details", (InvalidStateException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a backup. You can delete both manual and automated backups. This operation is asynchronous. An InvalidStateException is thrown when a backup deletion is already in progress. A ResourceNotFoundException is thrown when the backup does not exist. A ValidationException is thrown when parameters of the request are not valid."]
module DeleteBackupRequest =
  struct
    type nonrec t =
      {
      backupId: BackupId.t
        [@ocaml.doc
          "The ID of the backup to delete. Run the DescribeBackups command to get a list of backup IDs. Backup IDs are in the format ServerName-yyyyMMddHHmmssSSS."]}
    let context_ = "DeleteBackupRequest"
    let make ~backupId = fun () -> { backupId }
    let to_value x =
      structure_to_value
        [("BackupId", (Some (BackupId.to_value x.backupId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupId =
        BackupId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "BackupId") in
      make ~backupId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupId = field_map_exn json "BackupId" BackupId.of_json in
      make ~backupId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a backup. You can delete both manual and automated backups. This operation is asynchronous. An InvalidStateException is thrown when a backup deletion is already in progress. A ResourceNotFoundException is thrown when the backup does not exist. A ValidationException is thrown when parameters of the request are not valid."]
module CreateServerResponse =
  struct
    type nonrec t =
      {
      server: Server.t option
        [@ocaml.doc "The server that is created by the request."]}
    type nonrec error =
      [ `LimitExceededException of LimitExceededException.t 
      | `ResourceAlreadyExistsException of ResourceAlreadyExistsException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?server = fun () -> { server }
    let error_of_json name json =
      match name with
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceAlreadyExistsException" ->
          `ResourceAlreadyExistsException
            (ResourceAlreadyExistsException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceAlreadyExistsException e ->
          `Assoc
            [("error", (`String "ResourceAlreadyExistsException"));
            ("details", (ResourceAlreadyExistsException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Server", (Option.map x.server ~f:Server.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let server =
        (Option.map ~f:Server.of_xml) (Xml.child xml_arg0 "Server") in
      make ?server ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let server = field_map json "Server" Server.of_json in make ?server ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates and immedately starts a new server. The server is ready to use when it is in the HEALTHY state. By default, you can create a maximum of 10 servers. This operation is asynchronous. A LimitExceededException is thrown when you have created the maximum number of servers (10). A ResourceAlreadyExistsException is thrown when a server with the same name already exists in the account. A ResourceNotFoundException is thrown when you specify a backup ID that is not valid or is for a backup that does not exist. A ValidationException is thrown when parameters of the request are not valid. If you do not specify a security group by adding the SecurityGroupIds parameter, AWS OpsWorks creates a new security group. Chef Automate: The default security group opens the Chef server to the world on TCP port 443. If a KeyName is present, AWS OpsWorks enables SSH access. SSH is also open to the world on TCP port 22. Puppet Enterprise: The default security group opens TCP ports 22, 443, 4433, 8140, 8142, 8143, and 8170. If a KeyName is present, AWS OpsWorks enables SSH access. SSH is also open to the world on TCP port 22. By default, your server is accessible from any IP address. We recommend that you update your security group rules to allow access from known IP addresses and address ranges only. To edit security group rules, open Security Groups in the navigation pane of the EC2 management console. To specify your own domain for a server, and provide your own self-signed or CA-signed certificate and private key, specify values for CustomDomain, CustomCertificate, and CustomPrivateKey."]
module CreateServerRequest =
  struct
    type nonrec t =
      {
      associatePublicIpAddress: Boolean.t option
        [@ocaml.doc
          "Associate a public IP address with a server that you are launching. Valid values are true or false. The default value is true."];
      customDomain: CustomDomain.t option
        [@ocaml.doc
          "An optional public endpoint of a server, such as https://aws.my-company.com. To access the server, create a CNAME DNS record in your preferred DNS service that points the custom domain to the endpoint that is generated when the server is created (the value of the CreateServer Endpoint attribute). You cannot access the server by using the generated Endpoint value if the server is using a custom domain. If you specify a custom domain, you must also specify values for CustomCertificate and CustomPrivateKey."];
      customCertificate: CustomCertificate.t option
        [@ocaml.doc
          "A PEM-formatted HTTPS certificate. The value can be be a single, self-signed certificate, or a certificate chain. If you specify a custom certificate, you must also specify values for CustomDomain and CustomPrivateKey. The following are requirements for the CustomCertificate value: You can provide either a self-signed, custom certificate, or the full certificate chain. The certificate must be a valid X509 certificate, or a certificate chain in PEM format. The certificate must be valid at the time of upload. A certificate can't be used before its validity period begins (the certificate's NotBefore date), or after it expires (the certificate's NotAfter date). The certificate\226\128\153s common name or subject alternative names (SANs), if present, must match the value of CustomDomain. The certificate must match the value of CustomPrivateKey."];
      customPrivateKey: CustomPrivateKey.t option
        [@ocaml.doc
          "A private key in PEM format for connecting to the server by using HTTPS. The private key must not be encrypted; it cannot be protected by a password or passphrase. If you specify a custom private key, you must also specify values for CustomDomain and CustomCertificate."];
      disableAutomatedBackup: Boolean.t option
        [@ocaml.doc
          "Enable or disable scheduled backups. Valid values are true or false. The default value is true."];
      engine: String_.t
        [@ocaml.doc
          "The configuration management engine to use. Valid values include ChefAutomate and Puppet."];
      engineModel: String_.t option
        [@ocaml.doc
          "The engine model of the server. Valid values in this release include Monolithic for Puppet and Single for Chef."];
      engineVersion: String_.t option
        [@ocaml.doc
          "The major release version of the engine that you want to use. For a Chef server, the valid value for EngineVersion is currently 2. For a Puppet server, valid values are 2019 or 2017."];
      engineAttributes: EngineAttributes.t option
        [@ocaml.doc
          "Optional engine attributes on a specified server. Attributes accepted in a Chef createServer request: CHEF_AUTOMATE_PIVOTAL_KEY: A base64-encoded RSA public key. The corresponding private key is required to access the Chef API. When no CHEF_AUTOMATE_PIVOTAL_KEY is set, a private key is generated and returned in the response. CHEF_AUTOMATE_ADMIN_PASSWORD: The password for the administrative user in the Chef Automate web-based dashboard. The password length is a minimum of eight characters, and a maximum of 32. The password can contain letters, numbers, and special characters (!/\\@#$%^&+=_). The password must contain at least one lower case letter, one upper case letter, one number, and one special character. When no CHEF_AUTOMATE_ADMIN_PASSWORD is set, one is generated and returned in the response. Attributes accepted in a Puppet createServer request: PUPPET_ADMIN_PASSWORD: To work with the Puppet Enterprise console, a password must use ASCII characters. PUPPET_R10K_REMOTE: The r10k remote is the URL of your control repository (for example, ssh://git\\@your.git-repo.com:user/control-repo.git). Specifying an r10k remote opens TCP port 8170. PUPPET_R10K_PRIVATE_KEY: If you are using a private Git repository, add PUPPET_R10K_PRIVATE_KEY to specify a PEM-encoded private SSH key."];
      backupRetentionCount: BackupRetentionCountDefinition.t option
        [@ocaml.doc
          "The number of automated backups that you want to keep. Whenever a new backup is created, AWS OpsWorks CM deletes the oldest backups if this number is exceeded. The default value is 1."];
      serverName: ServerName.t
        [@ocaml.doc
          "The name of the server. The server name must be unique within your AWS account, within each region. Server names must start with a letter; then letters, numbers, or hyphens (-) are allowed, up to a maximum of 40 characters."];
      instanceProfileArn: InstanceProfileArn.t
        [@ocaml.doc
          "The ARN of the instance profile that your Amazon EC2 instances use. Although the AWS OpsWorks console typically creates the instance profile for you, if you are using API commands instead, run the service-role-creation.yaml AWS CloudFormation template, located at https://s3.amazonaws.com/opsworks-cm-us-east-1-prod-default-assets/misc/opsworks-cm-roles.yaml. This template creates a CloudFormation stack that includes the instance profile you need."];
      instanceType: String_.t
        [@ocaml.doc
          "The Amazon EC2 instance type to use. For example, m5.large."];
      keyPair: KeyPair.t option
        [@ocaml.doc
          "The Amazon EC2 key pair to set for the instance. This parameter is optional; if desired, you may specify this parameter to connect to your instances by using SSH."];
      preferredMaintenanceWindow: TimeWindowDefinition.t option
        [@ocaml.doc
          "The start time for a one-hour period each week during which AWS OpsWorks CM performs maintenance on the instance. Valid values must be specified in the following format: DDD:HH:MM. MM must be specified as 00. The specified time is in coordinated universal time (UTC). The default value is a random one-hour period on Tuesday, Wednesday, or Friday. See TimeWindowDefinition for more information. Example: Mon:08:00, which represents a start time of every Monday at 08:00 UTC. (8:00 a.m.)"];
      preferredBackupWindow: TimeWindowDefinition.t option
        [@ocaml.doc
          "The start time for a one-hour period during which AWS OpsWorks CM backs up application-level data on your server if automated backups are enabled. Valid values must be specified in one of the following formats: HH:MM for daily backups DDD:HH:MM for weekly backups MM must be specified as 00. The specified time is in coordinated universal time (UTC). The default value is a random, daily start time. Example: 08:00, which represents a daily start time of 08:00 UTC. Example: Mon:08:00, which represents a start time of every Monday at 08:00 UTC. (8:00 a.m.)"];
      securityGroupIds: Strings.t option
        [@ocaml.doc
          "A list of security group IDs to attach to the Amazon EC2 instance. If you add this parameter, the specified security groups must be within the VPC that is specified by SubnetIds. If you do not specify this parameter, AWS OpsWorks CM creates one new security group that uses TCP ports 22 and 443, open to 0.0.0.0/0 (everyone)."];
      serviceRoleArn: ServiceRoleArn.t
        [@ocaml.doc
          "The service role that the AWS OpsWorks CM service backend uses to work with your account. Although the AWS OpsWorks management console typically creates the service role for you, if you are using the AWS CLI or API commands, run the service-role-creation.yaml AWS CloudFormation template, located at https://s3.amazonaws.com/opsworks-cm-us-east-1-prod-default-assets/misc/opsworks-cm-roles.yaml. This template creates a CloudFormation stack that includes the service role and instance profile that you need."];
      subnetIds: Strings.t option
        [@ocaml.doc
          "The IDs of subnets in which to launch the server EC2 instance. Amazon EC2-Classic customers: This field is required. All servers must run within a VPC. The VPC must have \"Auto Assign Public IP\" enabled. EC2-VPC customers: This field is optional. If you do not specify subnet IDs, your EC2 instances are created in a default subnet that is selected by Amazon EC2. If you specify subnet IDs, the VPC must have \"Auto Assign Public IP\" enabled. For more information about supported Amazon EC2 platforms, see Supported Platforms."];
      tags: TagList.t option
        [@ocaml.doc
          "A map that contains tag keys and tag values to attach to an AWS OpsWorks for Chef Automate or AWS OpsWorks for Puppet Enterprise server. The key cannot be empty. The key can be a maximum of 127 characters, and can contain only Unicode letters, numbers, or separators, or the following special characters: + - = . _ : / \\@ The value can be a maximum 255 characters, and contain only Unicode letters, numbers, or separators, or the following special characters: + - = . _ : / \\@ Leading and trailing white spaces are trimmed from both the key and value. A maximum of 50 user-applied tags is allowed for any AWS OpsWorks-CM server."];
      backupId: BackupId.t option
        [@ocaml.doc
          "If you specify this field, AWS OpsWorks CM creates the server by using the backup represented by BackupId."]}
    let context_ = "CreateServerRequest"
    let make ?associatePublicIpAddress =
      fun ?customDomain ->
        fun ?customCertificate ->
          fun ?customPrivateKey ->
            fun ?disableAutomatedBackup ->
              fun ?engineModel ->
                fun ?engineVersion ->
                  fun ?engineAttributes ->
                    fun ?backupRetentionCount ->
                      fun ?keyPair ->
                        fun ?preferredMaintenanceWindow ->
                          fun ?preferredBackupWindow ->
                            fun ?securityGroupIds ->
                              fun ?subnetIds ->
                                fun ?tags ->
                                  fun ?backupId ->
                                    fun ~engine ->
                                      fun ~serverName ->
                                        fun ~instanceProfileArn ->
                                          fun ~instanceType ->
                                            fun ~serviceRoleArn ->
                                              fun () ->
                                                {
                                                  associatePublicIpAddress;
                                                  customDomain;
                                                  customCertificate;
                                                  customPrivateKey;
                                                  disableAutomatedBackup;
                                                  engineModel;
                                                  engineVersion;
                                                  engineAttributes;
                                                  backupRetentionCount;
                                                  keyPair;
                                                  preferredMaintenanceWindow;
                                                  preferredBackupWindow;
                                                  securityGroupIds;
                                                  subnetIds;
                                                  tags;
                                                  backupId;
                                                  engine;
                                                  serverName;
                                                  instanceProfileArn;
                                                  instanceType;
                                                  serviceRoleArn
                                                }
    let to_value x =
      structure_to_value
        [("AssociatePublicIpAddress",
           (Option.map x.associatePublicIpAddress ~f:Boolean.to_value));
        ("CustomDomain",
          (Option.map x.customDomain ~f:CustomDomain.to_value));
        ("CustomCertificate",
          (Option.map x.customCertificate ~f:CustomCertificate.to_value));
        ("CustomPrivateKey",
          (Option.map x.customPrivateKey ~f:CustomPrivateKey.to_value));
        ("DisableAutomatedBackup",
          (Option.map x.disableAutomatedBackup ~f:Boolean.to_value));
        ("Engine", (Some (String_.to_value x.engine)));
        ("EngineModel", (Option.map x.engineModel ~f:String_.to_value));
        ("EngineVersion", (Option.map x.engineVersion ~f:String_.to_value));
        ("EngineAttributes",
          (Option.map x.engineAttributes ~f:EngineAttributes.to_value));
        ("BackupRetentionCount",
          (Option.map x.backupRetentionCount
             ~f:BackupRetentionCountDefinition.to_value));
        ("ServerName", (Some (ServerName.to_value x.serverName)));
        ("InstanceProfileArn",
          (Some (InstanceProfileArn.to_value x.instanceProfileArn)));
        ("InstanceType", (Some (String_.to_value x.instanceType)));
        ("KeyPair", (Option.map x.keyPair ~f:KeyPair.to_value));
        ("PreferredMaintenanceWindow",
          (Option.map x.preferredMaintenanceWindow
             ~f:TimeWindowDefinition.to_value));
        ("PreferredBackupWindow",
          (Option.map x.preferredBackupWindow
             ~f:TimeWindowDefinition.to_value));
        ("SecurityGroupIds",
          (Option.map x.securityGroupIds ~f:Strings.to_value));
        ("ServiceRoleArn", (Some (ServiceRoleArn.to_value x.serviceRoleArn)));
        ("SubnetIds", (Option.map x.subnetIds ~f:Strings.to_value));
        ("Tags", (Option.map x.tags ~f:TagList.to_value));
        ("BackupId", (Option.map x.backupId ~f:BackupId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backupId =
        (Option.map ~f:BackupId.of_xml) (Xml.child xml_arg0 "BackupId") in
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let subnetIds =
        (Option.map ~f:Strings.of_xml) (Xml.child xml_arg0 "SubnetIds") in
      let serviceRoleArn =
        ServiceRoleArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServiceRoleArn") in
      let securityGroupIds =
        (Option.map ~f:Strings.of_xml)
          (Xml.child xml_arg0 "SecurityGroupIds") in
      let preferredBackupWindow =
        (Option.map ~f:TimeWindowDefinition.of_xml)
          (Xml.child xml_arg0 "PreferredBackupWindow") in
      let preferredMaintenanceWindow =
        (Option.map ~f:TimeWindowDefinition.of_xml)
          (Xml.child xml_arg0 "PreferredMaintenanceWindow") in
      let keyPair =
        (Option.map ~f:KeyPair.of_xml) (Xml.child xml_arg0 "KeyPair") in
      let instanceType =
        String_.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceType") in
      let instanceProfileArn =
        InstanceProfileArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "InstanceProfileArn") in
      let serverName =
        ServerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServerName") in
      let backupRetentionCount =
        (Option.map ~f:BackupRetentionCountDefinition.of_xml)
          (Xml.child xml_arg0 "BackupRetentionCount") in
      let engineAttributes =
        (Option.map ~f:EngineAttributes.of_xml)
          (Xml.child xml_arg0 "EngineAttributes") in
      let engineVersion =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "EngineVersion") in
      let engineModel =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "EngineModel") in
      let engine =
        String_.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Engine") in
      let disableAutomatedBackup =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "DisableAutomatedBackup") in
      let customPrivateKey =
        (Option.map ~f:CustomPrivateKey.of_xml)
          (Xml.child xml_arg0 "CustomPrivateKey") in
      let customCertificate =
        (Option.map ~f:CustomCertificate.of_xml)
          (Xml.child xml_arg0 "CustomCertificate") in
      let customDomain =
        (Option.map ~f:CustomDomain.of_xml)
          (Xml.child xml_arg0 "CustomDomain") in
      let associatePublicIpAddress =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "AssociatePublicIpAddress") in
      make ?backupId ?tags ?subnetIds ~serviceRoleArn ?securityGroupIds
        ?preferredBackupWindow ?preferredMaintenanceWindow ?keyPair
        ~instanceType ~instanceProfileArn ~serverName ?backupRetentionCount
        ?engineAttributes ?engineVersion ?engineModel ~engine
        ?disableAutomatedBackup ?customPrivateKey ?customCertificate
        ?customDomain ?associatePublicIpAddress ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backupId = field_map json "BackupId" BackupId.of_json in
      let tags = field_map json "Tags" TagList.of_json in
      let subnetIds = field_map json "SubnetIds" Strings.of_json in
      let serviceRoleArn =
        field_map_exn json "ServiceRoleArn" ServiceRoleArn.of_json in
      let securityGroupIds =
        field_map json "SecurityGroupIds" Strings.of_json in
      let preferredBackupWindow =
        field_map json "PreferredBackupWindow" TimeWindowDefinition.of_json in
      let preferredMaintenanceWindow =
        field_map json "PreferredMaintenanceWindow"
          TimeWindowDefinition.of_json in
      let keyPair = field_map json "KeyPair" KeyPair.of_json in
      let instanceType = field_map_exn json "InstanceType" String_.of_json in
      let instanceProfileArn =
        field_map_exn json "InstanceProfileArn" InstanceProfileArn.of_json in
      let serverName = field_map_exn json "ServerName" ServerName.of_json in
      let backupRetentionCount =
        field_map json "BackupRetentionCount"
          BackupRetentionCountDefinition.of_json in
      let engineAttributes =
        field_map json "EngineAttributes" EngineAttributes.of_json in
      let engineVersion = field_map json "EngineVersion" String_.of_json in
      let engineModel = field_map json "EngineModel" String_.of_json in
      let engine = field_map_exn json "Engine" String_.of_json in
      let disableAutomatedBackup =
        field_map json "DisableAutomatedBackup" Boolean.of_json in
      let customPrivateKey =
        field_map json "CustomPrivateKey" CustomPrivateKey.of_json in
      let customCertificate =
        field_map json "CustomCertificate" CustomCertificate.of_json in
      let customDomain = field_map json "CustomDomain" CustomDomain.of_json in
      let associatePublicIpAddress =
        field_map json "AssociatePublicIpAddress" Boolean.of_json in
      make ?backupId ?tags ?subnetIds ~serviceRoleArn ?securityGroupIds
        ?preferredBackupWindow ?preferredMaintenanceWindow ?keyPair
        ~instanceType ~instanceProfileArn ~serverName ?backupRetentionCount
        ?engineAttributes ?engineVersion ?engineModel ~engine
        ?disableAutomatedBackup ?customPrivateKey ?customCertificate
        ?customDomain ?associatePublicIpAddress ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates and immedately starts a new server. The server is ready to use when it is in the HEALTHY state. By default, you can create a maximum of 10 servers. This operation is asynchronous. A LimitExceededException is thrown when you have created the maximum number of servers (10). A ResourceAlreadyExistsException is thrown when a server with the same name already exists in the account. A ResourceNotFoundException is thrown when you specify a backup ID that is not valid or is for a backup that does not exist. A ValidationException is thrown when parameters of the request are not valid. If you do not specify a security group by adding the SecurityGroupIds parameter, AWS OpsWorks creates a new security group. Chef Automate: The default security group opens the Chef server to the world on TCP port 443. If a KeyName is present, AWS OpsWorks enables SSH access. SSH is also open to the world on TCP port 22. Puppet Enterprise: The default security group opens TCP ports 22, 443, 4433, 8140, 8142, 8143, and 8170. If a KeyName is present, AWS OpsWorks enables SSH access. SSH is also open to the world on TCP port 22. By default, your server is accessible from any IP address. We recommend that you update your security group rules to allow access from known IP addresses and address ranges only. To edit security group rules, open Security Groups in the navigation pane of the EC2 management console. To specify your own domain for a server, and provide your own self-signed or CA-signed certificate and private key, specify values for CustomDomain, CustomCertificate, and CustomPrivateKey."]
module CreateBackupResponse =
  struct
    type nonrec t =
      {
      backup: Backup.t option [@ocaml.doc "Backup created by request."]}
    type nonrec error =
      [ `InvalidStateException of InvalidStateException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?backup = fun () -> { backup }
    let error_of_json name json =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidStateException e ->
          `Assoc
            [("error", (`String "InvalidStateException"));
            ("details", (InvalidStateException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Backup", (Option.map x.backup ~f:Backup.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let backup =
        (Option.map ~f:Backup.of_xml) (Xml.child xml_arg0 "Backup") in
      make ?backup ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let backup = field_map json "Backup" Backup.of_json in make ?backup ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an application-level backup of a server. While the server is in the BACKING_UP state, the server cannot be changed, and no additional backup can be created. Backups can be created for servers in RUNNING, HEALTHY, and UNHEALTHY states. By default, you can create a maximum of 50 manual backups. This operation is asynchronous. A LimitExceededException is thrown when the maximum number of manual backups is reached. An InvalidStateException is thrown when the server is not in any of the following states: RUNNING, HEALTHY, or UNHEALTHY. A ResourceNotFoundException is thrown when the server is not found. A ValidationException is thrown when parameters of the request are not valid."]
module CreateBackupRequest =
  struct
    type nonrec t =
      {
      serverName: ServerName.t
        [@ocaml.doc "The name of the server that you want to back up."];
      description: String_.t option
        [@ocaml.doc "A user-defined description of the backup."];
      tags: TagList.t option
        [@ocaml.doc
          "A map that contains tag keys and tag values to attach to an AWS OpsWorks-CM server backup. The key cannot be empty. The key can be a maximum of 127 characters, and can contain only Unicode letters, numbers, or separators, or the following special characters: + - = . _ : / The value can be a maximum 255 characters, and contain only Unicode letters, numbers, or separators, or the following special characters: + - = . _ : / Leading and trailing white spaces are trimmed from both the key and value. A maximum of 50 user-applied tags is allowed for tag-supported AWS OpsWorks-CM resources."]}
    let context_ = "CreateBackupRequest"
    let make ?description =
      fun ?tags ->
        fun ~serverName -> fun () -> { description; tags; serverName }
    let to_value x =
      structure_to_value
        [("ServerName", (Some (ServerName.to_value x.serverName)));
        ("Description", (Option.map x.description ~f:String_.to_value));
        ("Tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let description =
        (Option.map ~f:String_.of_xml) (Xml.child xml_arg0 "Description") in
      let serverName =
        ServerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServerName") in
      make ?tags ?description ~serverName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map json "Tags" TagList.of_json in
      let description = field_map json "Description" String_.of_json in
      let serverName = field_map_exn json "ServerName" ServerName.of_json in
      make ?tags ?description ~serverName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates an application-level backup of a server. While the server is in the BACKING_UP state, the server cannot be changed, and no additional backup can be created. Backups can be created for servers in RUNNING, HEALTHY, and UNHEALTHY states. By default, you can create a maximum of 50 manual backups. This operation is asynchronous. A LimitExceededException is thrown when the maximum number of manual backups is reached. An InvalidStateException is thrown when the server is not in any of the following states: RUNNING, HEALTHY, or UNHEALTHY. A ResourceNotFoundException is thrown when the server is not found. A ValidationException is thrown when parameters of the request are not valid."]
module AssociateNodeResponse =
  struct
    type nonrec t =
      {
      nodeAssociationStatusToken: NodeAssociationStatusToken.t option
        [@ocaml.doc
          "Contains a token which can be passed to the DescribeNodeAssociationStatus API call to get the status of the association request."]}
    type nonrec error =
      [ `InvalidStateException of InvalidStateException.t 
      | `ResourceNotFoundException of ResourceNotFoundException.t 
      | `ValidationException of ValidationException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?nodeAssociationStatusToken =
      fun () -> { nodeAssociationStatusToken }
    let error_of_json name json =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_json json)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_json json)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidStateException" ->
          `InvalidStateException (InvalidStateException.of_xml xml)
      | "ResourceNotFoundException" ->
          `ResourceNotFoundException (ResourceNotFoundException.of_xml xml)
      | "ValidationException" ->
          `ValidationException (ValidationException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidStateException e ->
          `Assoc
            [("error", (`String "InvalidStateException"));
            ("details", (InvalidStateException.to_json e))]
      | `ResourceNotFoundException e ->
          `Assoc
            [("error", (`String "ResourceNotFoundException"));
            ("details", (ResourceNotFoundException.to_json e))]
      | `ValidationException e ->
          `Assoc
            [("error", (`String "ValidationException"));
            ("details", (ValidationException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("NodeAssociationStatusToken",
           (Option.map x.nodeAssociationStatusToken
              ~f:NodeAssociationStatusToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nodeAssociationStatusToken =
        (Option.map ~f:NodeAssociationStatusToken.of_xml)
          (Xml.child xml_arg0 "NodeAssociationStatusToken") in
      make ?nodeAssociationStatusToken ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nodeAssociationStatusToken =
        field_map json "NodeAssociationStatusToken"
          NodeAssociationStatusToken.of_json in
      make ?nodeAssociationStatusToken ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates a new node with the server. For more information about how to disassociate a node, see DisassociateNode. On a Chef server: This command is an alternative to knife bootstrap. Example (Chef): aws opsworks-cm associate-node --server-name MyServer --node-name MyManagedNode --engine-attributes \"Name=CHEF_ORGANIZATION,Value=default\" \"Name=CHEF_NODE_PUBLIC_KEY,Value=public-key-pem\" On a Puppet server, this command is an alternative to the puppet cert sign command that signs a Puppet node CSR. Example (Puppet): aws opsworks-cm associate-node --server-name MyServer --node-name MyManagedNode --engine-attributes \"Name=PUPPET_NODE_CSR,Value=csr-pem\" A node can can only be associated with servers that are in a HEALTHY state. Otherwise, an InvalidStateException is thrown. A ResourceNotFoundException is thrown when the server does not exist. A ValidationException is raised when parameters of the request are not valid. The AssociateNode API call can be integrated into Auto Scaling configurations, AWS Cloudformation templates, or the user data of a server's instance."]
module AssociateNodeRequest =
  struct
    type nonrec t =
      {
      serverName: ServerName.t
        [@ocaml.doc
          "The name of the server with which to associate the node."];
      nodeName: NodeName.t [@ocaml.doc "The name of the node."];
      engineAttributes: EngineAttributes.t
        [@ocaml.doc
          "Engine attributes used for associating the node. Attributes accepted in a AssociateNode request for Chef CHEF_ORGANIZATION: The Chef organization with which the node is associated. By default only one organization named default can exist. CHEF_NODE_PUBLIC_KEY: A PEM-formatted public key. This key is required for the chef-client agent to access the Chef API. Attributes accepted in a AssociateNode request for Puppet PUPPET_NODE_CSR: A PEM-formatted certificate-signing request (CSR) that is created by the node."]}
    let context_ = "AssociateNodeRequest"
    let make ~serverName =
      fun ~nodeName ->
        fun ~engineAttributes ->
          fun () -> { serverName; nodeName; engineAttributes }
    let to_value x =
      structure_to_value
        [("ServerName", (Some (ServerName.to_value x.serverName)));
        ("NodeName", (Some (NodeName.to_value x.nodeName)));
        ("EngineAttributes",
          (Some (EngineAttributes.to_value x.engineAttributes)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let engineAttributes =
        EngineAttributes.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "EngineAttributes") in
      let nodeName =
        NodeName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "NodeName") in
      let serverName =
        ServerName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ServerName") in
      make ~engineAttributes ~nodeName ~serverName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let engineAttributes =
        field_map_exn json "EngineAttributes" EngineAttributes.of_json in
      let nodeName = field_map_exn json "NodeName" NodeName.of_json in
      let serverName = field_map_exn json "ServerName" ServerName.of_json in
      make ~engineAttributes ~nodeName ~serverName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associates a new node with the server. For more information about how to disassociate a node, see DisassociateNode. On a Chef server: This command is an alternative to knife bootstrap. Example (Chef): aws opsworks-cm associate-node --server-name MyServer --node-name MyManagedNode --engine-attributes \"Name=CHEF_ORGANIZATION,Value=default\" \"Name=CHEF_NODE_PUBLIC_KEY,Value=public-key-pem\" On a Puppet server, this command is an alternative to the puppet cert sign command that signs a Puppet node CSR. Example (Puppet): aws opsworks-cm associate-node --server-name MyServer --node-name MyManagedNode --engine-attributes \"Name=PUPPET_NODE_CSR,Value=csr-pem\" A node can can only be associated with servers that are in a HEALTHY state. Otherwise, an InvalidStateException is thrown. A ResourceNotFoundException is thrown when the server does not exist. A ValidationException is raised when parameters of the request are not valid. The AssociateNode API call can be integrated into Auto Scaling configurations, AWS Cloudformation templates, or the user data of a server's instance."]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_node =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serverName =
         flag "server-name" (required string) ~doc:"STRING ServerName"
       and nodeName =
         flag "node-name" (required string) ~doc:"STRING NodeName"
       and engineAttributes =
         flag "engine-attributes" (required json_arg)
           ~doc:"JSON EngineAttributes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_node
           (Values.AssociateNodeRequest.make ~serverName ~nodeName
              ~engineAttributes:(Values.EngineAttributes.of_json
                                   engineAttributes) ())
           (Some Values.AssociateNodeResponse.to_json)
           (Some Values.AssociateNodeResponse.error_to_json)])
let create_backup =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and description =
         flag "description" (optional string) ~doc:"STRING String"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and serverName =
         flag "server-name" (required string) ~doc:"STRING ServerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_backup
           (Values.CreateBackupRequest.make ?description
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~serverName
              ()) (Some Values.CreateBackupResponse.to_json)
           (Some Values.CreateBackupResponse.error_to_json)])
let create_server =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and associatePublicIpAddress =
         flag "associate-public-ip-address" (optional bool)
           ~doc:"BOOL Boolean"
       and customDomain =
         flag "custom-domain" (optional string) ~doc:"STRING CustomDomain"
       and customCertificate =
         flag "custom-certificate" (optional string)
           ~doc:"STRING CustomCertificate"
       and customPrivateKey =
         flag "custom-private-key" (optional string)
           ~doc:"STRING CustomPrivateKey"
       and disableAutomatedBackup =
         flag "disable-automated-backup" (optional bool) ~doc:"BOOL Boolean"
       and engineModel =
         flag "engine-model" (optional string) ~doc:"STRING String"
       and engineVersion =
         flag "engine-version" (optional string) ~doc:"STRING String"
       and engineAttributes =
         flag "engine-attributes" (optional json_arg)
           ~doc:"JSON EngineAttributes"
       and backupRetentionCount =
         flag "backup-retention-count" (optional int)
           ~doc:"INT BackupRetentionCountDefinition"
       and keyPair = flag "key-pair" (optional string) ~doc:"STRING KeyPair"
       and preferredMaintenanceWindow =
         flag "preferred-maintenance-window" (optional string)
           ~doc:"STRING TimeWindowDefinition"
       and preferredBackupWindow =
         flag "preferred-backup-window" (optional string)
           ~doc:"STRING TimeWindowDefinition"
       and securityGroupIds =
         flag "security-group-ids" (optional json_arg) ~doc:"JSON Strings"
       and subnetIds =
         flag "subnet-ids" (optional json_arg) ~doc:"JSON Strings"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and backupId =
         flag "backup-id" (optional string) ~doc:"STRING BackupId"
       and engine = flag "engine" (required string) ~doc:"STRING String"
       and serverName =
         flag "server-name" (required string) ~doc:"STRING ServerName"
       and instanceProfileArn =
         flag "instance-profile-arn" (required string)
           ~doc:"STRING InstanceProfileArn"
       and instanceType =
         flag "instance-type" (required string) ~doc:"STRING String"
       and serviceRoleArn =
         flag "service-role-arn" (required string)
           ~doc:"STRING ServiceRoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_server
           (Values.CreateServerRequest.make ?associatePublicIpAddress
              ?customDomain ?customCertificate ?customPrivateKey
              ?disableAutomatedBackup ?engineModel ?engineVersion
              ?engineAttributes:(Option.map
                                   ~f:Values.EngineAttributes.of_json
                                   engineAttributes) ?backupRetentionCount
              ?keyPair ?preferredMaintenanceWindow ?preferredBackupWindow
              ?securityGroupIds:(Option.map ~f:Values.Strings.of_json
                                   securityGroupIds)
              ?subnetIds:(Option.map ~f:Values.Strings.of_json subnetIds)
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ?backupId
              ~engine ~serverName ~instanceProfileArn ~instanceType
              ~serviceRoleArn ()) (Some Values.CreateServerResponse.to_json)
           (Some Values.CreateServerResponse.error_to_json)])
let delete_backup =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and backupId =
         flag "backup-id" (required string) ~doc:"STRING BackupId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_backup (Values.DeleteBackupRequest.make ~backupId ())
           (Some Values.DeleteBackupResponse.to_json)
           (Some Values.DeleteBackupResponse.error_to_json)])
let delete_server =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serverName =
         flag "server-name" (required string) ~doc:"STRING ServerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_server (Values.DeleteServerRequest.make ~serverName ())
           (Some Values.DeleteServerResponse.to_json)
           (Some Values.DeleteServerResponse.error_to_json)])
let describe_account_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_account_attributes
           (Values.DescribeAccountAttributesRequest.make ())
           (Some Values.DescribeAccountAttributesResponse.to_json)
           (Some Values.DescribeAccountAttributesResponse.error_to_json)])
let describe_backups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and backupId =
         flag "backup-id" (optional string) ~doc:"STRING BackupId"
       and serverName =
         flag "server-name" (optional string) ~doc:"STRING ServerName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_backups
           (Values.DescribeBackupsRequest.make ?backupId ?serverName
              ?nextToken ?maxResults ())
           (Some Values.DescribeBackupsResponse.to_json)
           (Some Values.DescribeBackupsResponse.error_to_json)])
let describe_events =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and serverName =
         flag "server-name" (required string) ~doc:"STRING ServerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_events
           (Values.DescribeEventsRequest.make ?nextToken ?maxResults
              ~serverName ()) (Some Values.DescribeEventsResponse.to_json)
           (Some Values.DescribeEventsResponse.error_to_json)])
let describe_node_association_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nodeAssociationStatusToken =
         flag "node-association-status-token" (required string)
           ~doc:"STRING NodeAssociationStatusToken"
       and serverName =
         flag "server-name" (required string) ~doc:"STRING ServerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_node_association_status
           (Values.DescribeNodeAssociationStatusRequest.make
              ~nodeAssociationStatusToken ~serverName ())
           (Some Values.DescribeNodeAssociationStatusResponse.to_json)
           (Some Values.DescribeNodeAssociationStatusResponse.error_to_json)])
let describe_servers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and serverName =
         flag "server-name" (optional string) ~doc:"STRING ServerName"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_servers
           (Values.DescribeServersRequest.make ?serverName ?nextToken
              ?maxResults ()) (Some Values.DescribeServersResponse.to_json)
           (Some Values.DescribeServersResponse.error_to_json)])
let disassociate_node =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and engineAttributes =
         flag "engine-attributes" (optional json_arg)
           ~doc:"JSON EngineAttributes"
       and serverName =
         flag "server-name" (required string) ~doc:"STRING ServerName"
       and nodeName =
         flag "node-name" (required string) ~doc:"STRING NodeName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_node
           (Values.DisassociateNodeRequest.make
              ?engineAttributes:(Option.map
                                   ~f:Values.EngineAttributes.of_json
                                   engineAttributes) ~serverName ~nodeName ())
           (Some Values.DisassociateNodeResponse.to_json)
           (Some Values.DisassociateNodeResponse.error_to_json)])
let export_server_engine_attribute =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and inputAttributes =
         flag "input-attributes" (optional json_arg)
           ~doc:"JSON EngineAttributes"
       and exportAttributeName =
         flag "export-attribute-name" (required string) ~doc:"STRING String"
       and serverName =
         flag "server-name" (required string) ~doc:"STRING ServerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.export_server_engine_attribute
           (Values.ExportServerEngineAttributeRequest.make
              ?inputAttributes:(Option.map ~f:Values.EngineAttributes.of_json
                                  inputAttributes) ~exportAttributeName
              ~serverName ())
           (Some Values.ExportServerEngineAttributeResponse.to_json)
           (Some Values.ExportServerEngineAttributeResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AWSOpsWorksCMResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ?nextToken ?maxResults
              ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let restore_server =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and instanceType =
         flag "instance-type" (optional string) ~doc:"STRING String"
       and keyPair = flag "key-pair" (optional string) ~doc:"STRING KeyPair"
       and backupId =
         flag "backup-id" (required string) ~doc:"STRING BackupId"
       and serverName =
         flag "server-name" (required string) ~doc:"STRING ServerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.restore_server
           (Values.RestoreServerRequest.make ?instanceType ?keyPair ~backupId
              ~serverName ()) (Some Values.RestoreServerResponse.to_json)
           (Some Values.RestoreServerResponse.error_to_json)])
let start_maintenance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and engineAttributes =
         flag "engine-attributes" (optional json_arg)
           ~doc:"JSON EngineAttributes"
       and serverName =
         flag "server-name" (required string) ~doc:"STRING ServerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_maintenance
           (Values.StartMaintenanceRequest.make
              ?engineAttributes:(Option.map
                                   ~f:Values.EngineAttributes.of_json
                                   engineAttributes) ~serverName ())
           (Some Values.StartMaintenanceResponse.to_json)
           (Some Values.StartMaintenanceResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AWSOpsWorksCMResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string)
           ~doc:"STRING AWSOpsWorksCMResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_server =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and disableAutomatedBackup =
         flag "disable-automated-backup" (optional bool) ~doc:"BOOL Boolean"
       and backupRetentionCount =
         flag "backup-retention-count" (optional int) ~doc:"INT Integer"
       and preferredMaintenanceWindow =
         flag "preferred-maintenance-window" (optional string)
           ~doc:"STRING TimeWindowDefinition"
       and preferredBackupWindow =
         flag "preferred-backup-window" (optional string)
           ~doc:"STRING TimeWindowDefinition"
       and serverName =
         flag "server-name" (required string) ~doc:"STRING ServerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_server
           (Values.UpdateServerRequest.make ?disableAutomatedBackup
              ?backupRetentionCount ?preferredMaintenanceWindow
              ?preferredBackupWindow ~serverName ())
           (Some Values.UpdateServerResponse.to_json)
           (Some Values.UpdateServerResponse.error_to_json)])
let update_server_engine_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attributeValue =
         flag "attribute-value" (optional string)
           ~doc:"STRING AttributeValue"
       and serverName =
         flag "server-name" (required string) ~doc:"STRING ServerName"
       and attributeName =
         flag "attribute-name" (required string) ~doc:"STRING AttributeName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_server_engine_attributes
           (Values.UpdateServerEngineAttributesRequest.make ?attributeValue
              ~serverName ~attributeName ())
           (Some Values.UpdateServerEngineAttributesResponse.to_json)
           (Some Values.UpdateServerEngineAttributesResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("associate-node", associate_node);
    ("create-backup", create_backup);
    ("create-server", create_server);
    ("delete-backup", delete_backup);
    ("delete-server", delete_server);
    ("describe-account-attributes", describe_account_attributes);
    ("describe-backups", describe_backups);
    ("describe-events", describe_events);
    ("describe-node-association-status", describe_node_association_status);
    ("describe-servers", describe_servers);
    ("disassociate-node", disassociate_node);
    ("export-server-engine-attribute", export_server_engine_attribute);
    ("list-tags-for-resource", list_tags_for_resource);
    ("restore-server", restore_server);
    ("start-maintenance", start_maintenance);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-server", update_server);
    ("update-server-engine-attributes", update_server_engine_attributes)]
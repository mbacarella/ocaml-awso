(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | CreateProject: (CreateProjectRequest.t, CreateProjectResult.t,
  CreateProjectResult.error) t 
  | DeleteProject: (DeleteProjectRequest.t, DeleteProjectResult.t,
  DeleteProjectResult.error) t 
  | DescribeBundle: (DescribeBundleRequest.t, DescribeBundleResult.t,
  DescribeBundleResult.error) t 
  | DescribeProject: (DescribeProjectRequest.t, DescribeProjectResult.t,
  DescribeProjectResult.error) t 
  | ExportBundle: (ExportBundleRequest.t, ExportBundleResult.t,
  ExportBundleResult.error) t 
  | ExportProject: (ExportProjectRequest.t, ExportProjectResult.t,
  ExportProjectResult.error) t 
  | ListBundles: (ListBundlesRequest.t, ListBundlesResult.t,
  ListBundlesResult.error) t 
  | ListProjects: (ListProjectsRequest.t, ListProjectsResult.t,
  ListProjectsResult.error) t 
  | UpdateProject: (UpdateProjectRequest.t, UpdateProjectResult.t,
  UpdateProjectResult.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | CreateProject -> `POST
  | DeleteProject -> `DELETE
  | DescribeBundle -> `GET
  | DescribeProject -> `GET
  | ExportBundle -> `POST
  | ExportProject -> `POST
  | ListBundles -> `GET
  | ListProjects -> `GET
  | UpdateProject -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | CreateProject ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/projects")
            (List.filter_opt
               [Option.map ~f:(fun v -> ("name", (ProjectName.to_header v)))
                  x.name;
               Option.map
                 ~f:(fun v -> ("region", (ProjectRegion.to_header v)))
                 x.region;
               Option.map
                 ~f:(fun v -> ("snapshotId", (SnapshotId.to_header v)))
                 x.snapshotId])
      | DeleteProject ->
          (Format.kasprintf Uri.of_string) "/projects/%s"
            (ProjectId.to_header x.DeleteProjectRequest.projectId)
      | DescribeBundle ->
          (Format.kasprintf Uri.of_string) "/bundles/%s"
            (BundleId.to_header x.DescribeBundleRequest.bundleId)
      | DescribeProject ->
          Uri.add_query_params' ((Format.kasprintf Uri.of_string) "/project")
            (List.filter_opt
               [Some ("projectId", (ProjectId.to_header x.projectId));
               Option.map
                 ~f:(fun v -> ("syncFromResources", (Boolean.to_header v)))
                 x.syncFromResources])
      | ExportBundle ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/bundles/%s"
               (BundleId.to_header x.ExportBundleRequest.bundleId))
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("projectId", (ProjectId.to_header v)))
                  x.projectId;
               Option.map ~f:(fun v -> ("platform", (Platform.to_header v)))
                 x.platform])
      | ExportProject ->
          (Format.kasprintf Uri.of_string) "/exports/%s"
            (ProjectId.to_header x.ExportProjectRequest.projectId)
      | ListBundles ->
          Uri.add_query_params' ((Format.kasprintf Uri.of_string) "/bundles")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("maxResults", (MaxResults.to_header v)))
                  x.maxResults;
               Option.map
                 ~f:(fun v -> ("nextToken", (NextToken.to_header v)))
                 x.nextToken])
      | ListProjects ->
          Uri.add_query_params'
            ((Format.kasprintf Uri.of_string) "/projects")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("maxResults", (MaxResults.to_header v)))
                  x.maxResults;
               Option.map
                 ~f:(fun v -> ("nextToken", (NextToken.to_header v)))
                 x.nextToken])
      | UpdateProject ->
          Uri.add_query_params' ((Format.kasprintf Uri.of_string) "/update")
            (List.filter_opt
               [Some ("projectId", (ProjectId.to_header x.projectId))]))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | CreateProject ->
      let headers = Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
      let body = Option.map req.contents ~f:Contents.to_header in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DeleteProject -> Awso.Http.Request.make (method_of_endpoint endp)
  | DescribeBundle ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | DescribeProject ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ExportBundle ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ExportProject ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListBundles ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListProjects ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | UpdateProject ->
      let headers = Some ((List.filter_opt []) |> Awso.Http.Headers.of_list) in
      let body = Option.map req.contents ~f:Contents.to_header in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let x_amzn_error_type =
    let headers = Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
    match List.Assoc.find ~equal:String.Caseless.equal headers
            "x-amzn-ErrorType"
    with
    | None -> None
    | Some value ->
        (match String.lsplit2 value ~on:':' with
         | None -> Some value
         | Some (v, _) -> Some v) in
  let parse_aws_error error_of_json =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type }) in
    match (x_amzn_error_type, error_of_json,
            ((code >= 400) && (code <= 599)))
    with
    | (Some error_type, Some error_of_json, true) ->
        let json = Yojson.Safe.from_string body in
        error_of_json error_type json
    | (None, Some error_of_json, true) ->
        (try
           let json = Yojson.Safe.from_string body in
           match json |> (Yojson.Safe.Util.member "__type") with
           | `String error_type ->
               let error_type =
                 match String.lsplit2 error_type ~on:'#' with
                 | Some (_, s) -> s
                 | None -> error_type in
               error_of_json error_type json
           | `Null -> bail ()
           | _ ->
               failwithf "Error '__type' did not have string type: %s" body
                 ()
         with | _ -> bail ())
    | (None, _, _) | (_, None, _) | (_, _, false) -> bail () in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = parse_aws_error in
  let _ = response_to_json in
  let _ = resp in
  match endpoint with
  | CreateProject ->
      if is_success
      then Ok (CreateProjectResult.of_json (response_to_json resp))
      else Error (parse_aws_error (Some CreateProjectResult.error_of_json))
  | DeleteProject ->
      if is_success
      then Ok (DeleteProjectResult.of_json (response_to_json resp))
      else Error (parse_aws_error (Some DeleteProjectResult.error_of_json))
  | DescribeBundle ->
      if is_success
      then Ok (DescribeBundleResult.of_json (response_to_json resp))
      else Error (parse_aws_error (Some DescribeBundleResult.error_of_json))
  | DescribeProject ->
      if is_success
      then Ok (DescribeProjectResult.of_json (response_to_json resp))
      else Error (parse_aws_error (Some DescribeProjectResult.error_of_json))
  | ExportBundle ->
      if is_success
      then Ok (ExportBundleResult.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ExportBundleResult.error_of_json))
  | ExportProject ->
      if is_success
      then Ok (ExportProjectResult.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ExportProjectResult.error_of_json))
  | ListBundles ->
      if is_success
      then Ok (ListBundlesResult.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListBundlesResult.error_of_json))
  | ListProjects ->
      if is_success
      then Ok (ListProjectsResult.of_json (response_to_json resp))
      else Error (parse_aws_error (Some ListProjectsResult.error_of_json))
  | UpdateProject ->
      if is_success
      then Ok (UpdateProjectResult.of_json (response_to_json resp))
      else Error (parse_aws_error (Some UpdateProjectResult.error_of_json))
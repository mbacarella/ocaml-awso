(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let create_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING ProjectName"
       and region =
         flag "region" (optional string) ~doc:"STRING ProjectRegion"
       and contents =
         flag "contents" (optional json_arg) ~doc:"JSON Contents"
       and snapshotId =
         flag "snapshot-id" (optional string) ~doc:"STRING SnapshotId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_project
           (Values.CreateProjectRequest.make ?name ?region
              ?contents:(Option.map ~f:Values.Contents.of_json contents)
              ?snapshotId ()) (Some Values.CreateProjectResult.to_json)
           (Some Values.CreateProjectResult.error_to_json)])
let delete_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectId =
         flag "project-id" (required string) ~doc:"STRING ProjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_project (Values.DeleteProjectRequest.make ~projectId ())
           (Some Values.DeleteProjectResult.to_json)
           (Some Values.DeleteProjectResult.error_to_json)])
let describe_bundle =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and bundleId =
         flag "bundle-id" (required string) ~doc:"STRING BundleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_bundle
           (Values.DescribeBundleRequest.make ~bundleId ())
           (Some Values.DescribeBundleResult.to_json)
           (Some Values.DescribeBundleResult.error_to_json)])
let describe_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and syncFromResources =
         flag "sync-from-resources" (optional bool) ~doc:"BOOL Boolean"
       and projectId =
         flag "project-id" (required string) ~doc:"STRING ProjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_project
           (Values.DescribeProjectRequest.make ?syncFromResources ~projectId
              ()) (Some Values.DescribeProjectResult.to_json)
           (Some Values.DescribeProjectResult.error_to_json)])
let export_bundle =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectId =
         flag "project-id" (optional string) ~doc:"STRING ProjectId"
       and platform =
         flag "platform" (optional json_arg) ~doc:"JSON Platform"
       and bundleId =
         flag "bundle-id" (required string) ~doc:"STRING BundleId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.export_bundle
           (Values.ExportBundleRequest.make ?projectId
              ?platform:(Option.map ~f:Values.Platform.of_json platform)
              ~bundleId ()) (Some Values.ExportBundleResult.to_json)
           (Some Values.ExportBundleResult.error_to_json)])
let export_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and projectId =
         flag "project-id" (required string) ~doc:"STRING ProjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.export_project (Values.ExportProjectRequest.make ~projectId ())
           (Some Values.ExportProjectResult.to_json)
           (Some Values.ExportProjectResult.error_to_json)])
let list_bundles =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_bundles
           (Values.ListBundlesRequest.make ?maxResults ?nextToken ())
           (Some Values.ListBundlesResult.to_json)
           (Some Values.ListBundlesResult.error_to_json)])
let list_projects =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT MaxResults"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_projects
           (Values.ListProjectsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListProjectsResult.to_json)
           (Some Values.ListProjectsResult.error_to_json)])
let update_project =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contents =
         flag "contents" (optional json_arg) ~doc:"JSON Contents"
       and projectId =
         flag "project-id" (required string) ~doc:"STRING ProjectId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_project
           (Values.UpdateProjectRequest.make
              ?contents:(Option.map ~f:Values.Contents.of_json contents)
              ~projectId ()) (Some Values.UpdateProjectResult.to_json)
           (Some Values.UpdateProjectResult.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("create-project", create_project);
    ("delete-project", delete_project);
    ("describe-bundle", describe_bundle);
    ("describe-project", describe_project);
    ("export-bundle", export_bundle);
    ("export-project", export_project);
    ("list-bundles", list_bundles);
    ("list-projects", list_projects);
    ("update-project", update_project)]
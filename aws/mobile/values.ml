(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.mobile
let apiVersion = "2017-07-01"
let endpointPrefix = "mobile"
let serviceFullName = "AWS Mobile"
let signatureVersion = "v4"
let protocol = "rest_json"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module AttributeKey =
  struct
    type nonrec t = string[@@ocaml.doc
                            "Key part of key-value attribute pairs."]
    let context_ = "AttributeKey"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AttributeKey" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Key part of key-value attribute pairs."]
module AttributeValue =
  struct
    type nonrec t = string[@@ocaml.doc
                            "Value part of key-value attribute pairs."]
    let context_ = "AttributeValue"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AttributeValue" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Value part of key-value attribute pairs."]
module Attributes =
  struct
    type nonrec t = (AttributeKey.t * AttributeValue.t) list
    let make i = i
    let of_header xs =
      make
        (List.filter_map xs
           ~f:(fun (k, v) ->
                 (Base.String.chop_prefix k ~prefix:"x-amz-meta-") |>
                   (Option.map
                      ~f:(fun chopped ->
                            ((AttributeKey.of_string chopped),
                              (AttributeValue.of_string v))))))
    let to_value xs =
      (xs |>
         (List.map
            ~f:(fun (x, y) ->
                  (AttributeKey.to_value x) |>
                    (fun x ->
                       (AttributeValue.to_value y) |> (fun y -> (x, y))))))
        |> (fun x -> `Map x)
    let to_query v = to_query to_value v
    let of_xml _ =
      failwith "of_xml_converter_of_shape: Map_shape case not implemented"
    let of_json j =
      object_of_json ~key_of_string:AttributeKey.of_string
        ~of_json:AttributeValue.of_json j
    let to_json v = composed_to_json to_value v
  end
module Feature =
  struct
    type nonrec t = string[@@ocaml.doc
                            "Identifies which feature in AWS Mobile Hub is associated with this AWS resource."]
    let context_ = "Feature"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Feature" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "Identifies which feature in AWS Mobile Hub is associated with this AWS resource."]
module ResourceArn =
  struct
    type nonrec t = string[@@ocaml.doc
                            "AWS resource name which uniquely identifies the resource in AWS systems."]
    let context_ = "ResourceArn"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceArn" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "AWS resource name which uniquely identifies the resource in AWS systems."]
module ResourceName =
  struct
    type nonrec t = string[@@ocaml.doc
                            "Name of the AWS resource (e.g., for an Amazon S3 bucket this is the name of the bucket)."]
    let context_ = "ResourceName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceName" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "Name of the AWS resource (e.g., for an Amazon S3 bucket this is the name of the bucket)."]
module ResourceType =
  struct
    type nonrec t = string[@@ocaml.doc
                            "Simplified name for type of AWS resource (e.g., bucket is an Amazon S3 bucket)."]
    let context_ = "ResourceType"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceType" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "Simplified name for type of AWS resource (e.g., bucket is an Amazon S3 bucket)."]
module Platform =
  struct
    type nonrec t =
      | OSX 
      | WINDOWS 
      | LINUX 
      | OBJC 
      | SWIFT 
      | ANDROID 
      | JAVASCRIPT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | OSX -> "OSX"
      | WINDOWS -> "WINDOWS"
      | LINUX -> "LINUX"
      | OBJC -> "OBJC"
      | SWIFT -> "SWIFT"
      | ANDROID -> "ANDROID"
      | JAVASCRIPT -> "JAVASCRIPT"
      | Non_static_id s -> s
    let of_string =
      function
      | "OSX" -> OSX
      | "WINDOWS" -> WINDOWS
      | "LINUX" -> LINUX
      | "OBJC" -> OBJC
      | "SWIFT" -> SWIFT
      | "ANDROID" -> ANDROID
      | "JAVASCRIPT" -> JAVASCRIPT
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Platform" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Platform" j)
    let to_json = simple_to_json to_value
  end
module Resource =
  struct
    type nonrec t =
      {
      type_: ResourceType.t option ;
      name: ResourceName.t option ;
      arn: ResourceArn.t option ;
      feature: Feature.t option ;
      attributes: Attributes.t option }
    let make ?type_ =
      fun ?name ->
        fun ?arn ->
          fun ?feature ->
            fun ?attributes ->
              fun () -> { type_; name; arn; feature; attributes }
    let to_value x =
      structure_to_value
        [("type", (Option.map x.type_ ~f:ResourceType.to_value));
        ("name", (Option.map x.name ~f:ResourceName.to_value));
        ("arn", (Option.map x.arn ~f:ResourceArn.to_value));
        ("feature", (Option.map x.feature ~f:Feature.to_value));
        ("attributes", (Option.map x.attributes ~f:Attributes.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let attributes =
        (Option.map ~f:Attributes.of_xml) (Xml.child xml_arg0 "attributes") in
      let feature =
        (Option.map ~f:Feature.of_xml) (Xml.child xml_arg0 "feature") in
      let arn = (Option.map ~f:ResourceArn.of_xml) (Xml.child xml_arg0 "arn") in
      let name =
        (Option.map ~f:ResourceName.of_xml) (Xml.child xml_arg0 "name") in
      let type_ =
        (Option.map ~f:ResourceType.of_xml) (Xml.child xml_arg0 "type") in
      make ?attributes ?feature ?arn ?name ?type_ ()
    let of_json json =
      let attributes = field_map json "attributes" Attributes.of_json in
      let feature = field_map json "feature" Feature.of_json in
      let arn = field_map json "arn" ResourceArn.of_json in
      let name = field_map json "name" ResourceName.of_json in
      let type_ = field_map json "type" ResourceType.of_json in
      make ?attributes ?feature ?arn ?name ?type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about an instance of an AWS resource associated with a project."]
module ProjectId =
  struct
    type nonrec t = string[@@ocaml.doc "Unique project identifier."]
    let context_ = "ProjectId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProjectId" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Unique project identifier."]
module ProjectName =
  struct
    type nonrec t = string[@@ocaml.doc "Name of the project."]
    let context_ = "ProjectName"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProjectName" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Name of the project."]
module BundleDescription =
  struct
    type nonrec t = string[@@ocaml.doc "Description of the download bundle."]
    let context_ = "BundleDescription"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BundleDescription" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Description of the download bundle."]
module BundleId =
  struct
    type nonrec t = string[@@ocaml.doc "Unique bundle identifier."]
    let context_ = "BundleId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BundleId" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Unique bundle identifier."]
module BundleTitle =
  struct
    type nonrec t = string[@@ocaml.doc "Title of the download bundle."]
    let context_ = "BundleTitle"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BundleTitle" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Title of the download bundle."]
module BundleVersion =
  struct
    type nonrec t = string[@@ocaml.doc "Version of the download bundle."]
    let context_ = "BundleVersion"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"BundleVersion" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Version of the download bundle."]
module IconUrl =
  struct
    type nonrec t = string[@@ocaml.doc "Icon for the download bundle."]
    let context_ = "IconUrl"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"IconUrl" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Icon for the download bundle."]
module Platforms =
  struct
    type nonrec t = Platform.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Platform.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Platform.of_xml)
    let of_json j =
      list_of_json ~kind:"Platforms" ~of_json:Platform.of_json j
    let to_json v = composed_to_json to_value v
  end
module ErrorMessage =
  struct
    type nonrec t = string[@@ocaml.doc "The Exception Error Message."]
    let context_ = "ErrorMessage"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorMessage" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "The Exception Error Message."]
module ConsoleUrl =
  struct
    type nonrec t = string
    let context_ = "ConsoleUrl"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ConsoleUrl" j
    let to_json = simple_to_json to_value
  end
module Date =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module ProjectRegion =
  struct
    type nonrec t = string[@@ocaml.doc
                            "Default region to use for AWS resource creation in the AWS Mobile Hub project."]
    let context_ = "ProjectRegion"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ProjectRegion" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "Default region to use for AWS resource creation in the AWS Mobile Hub project."]
module ProjectState =
  struct
    type nonrec t =
      | NORMAL 
      | SYNCING 
      | IMPORTING 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | NORMAL -> "NORMAL"
      | SYNCING -> "SYNCING"
      | IMPORTING -> "IMPORTING"
      | Non_static_id s -> s
    let of_string =
      function
      | "NORMAL" -> NORMAL
      | "SYNCING" -> SYNCING
      | "IMPORTING" -> IMPORTING
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ProjectState" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ProjectState" j)
    let to_json = simple_to_json to_value
  end
module Resources =
  struct
    type nonrec t = Resource.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:Resource.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Resource.of_xml)
    let of_json j =
      list_of_json ~kind:"Resources" ~of_json:Resource.of_json j
    let to_json v = composed_to_json to_value v
  end
module ProjectSummary =
  struct
    type nonrec t =
      {
      name: ProjectName.t option [@ocaml.doc "Name of the project."];
      projectId: ProjectId.t option [@ocaml.doc "Unique project identifier."]}
    let make ?name = fun ?projectId -> fun () -> { name; projectId }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:ProjectName.to_value));
        ("projectId", (Option.map x.projectId ~f:ProjectId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let projectId =
        (Option.map ~f:ProjectId.of_xml) (Xml.child xml_arg0 "projectId") in
      let name =
        (Option.map ~f:ProjectName.of_xml) (Xml.child xml_arg0 "name") in
      make ?projectId ?name ()
    let of_json json =
      let projectId = field_map json "projectId" ProjectId.of_json in
      let name = field_map json "name" ProjectName.of_json in
      make ?projectId ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Summary information about an AWS Mobile Hub project."]
module BundleDetails =
  struct
    type nonrec t =
      {
      bundleId: BundleId.t option ;
      title: BundleTitle.t option ;
      version: BundleVersion.t option ;
      description: BundleDescription.t option ;
      iconUrl: IconUrl.t option ;
      availablePlatforms: Platforms.t option }
    let make ?bundleId =
      fun ?title ->
        fun ?version ->
          fun ?description ->
            fun ?iconUrl ->
              fun ?availablePlatforms ->
                fun () ->
                  {
                    bundleId;
                    title;
                    version;
                    description;
                    iconUrl;
                    availablePlatforms
                  }
    let to_value x =
      structure_to_value
        [("bundleId", (Option.map x.bundleId ~f:BundleId.to_value));
        ("title", (Option.map x.title ~f:BundleTitle.to_value));
        ("version", (Option.map x.version ~f:BundleVersion.to_value));
        ("description",
          (Option.map x.description ~f:BundleDescription.to_value));
        ("iconUrl", (Option.map x.iconUrl ~f:IconUrl.to_value));
        ("availablePlatforms",
          (Option.map x.availablePlatforms ~f:Platforms.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let availablePlatforms =
        (Option.map ~f:Platforms.of_xml)
          (Xml.child xml_arg0 "availablePlatforms") in
      let iconUrl =
        (Option.map ~f:IconUrl.of_xml) (Xml.child xml_arg0 "iconUrl") in
      let description =
        (Option.map ~f:BundleDescription.of_xml)
          (Xml.child xml_arg0 "description") in
      let version =
        (Option.map ~f:BundleVersion.of_xml) (Xml.child xml_arg0 "version") in
      let title =
        (Option.map ~f:BundleTitle.of_xml) (Xml.child xml_arg0 "title") in
      let bundleId =
        (Option.map ~f:BundleId.of_xml) (Xml.child xml_arg0 "bundleId") in
      make ?availablePlatforms ?iconUrl ?description ?version ?title
        ?bundleId ()
    let of_json json =
      let availablePlatforms =
        field_map json "availablePlatforms" Platforms.of_json in
      let iconUrl = field_map json "iconUrl" IconUrl.of_json in
      let description =
        field_map json "description" BundleDescription.of_json in
      let version = field_map json "version" BundleVersion.of_json in
      let title = field_map json "title" BundleTitle.of_json in
      let bundleId = field_map json "bundleId" BundleId.of_json in
      make ?availablePlatforms ?iconUrl ?description ?version ?title
        ?bundleId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The details of the bundle."]
module AccountActionRequiredException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Account Action is required in order to continue the request."]
module BadRequestException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The request cannot be processed because some parameter is not valid or the project state prevents the operation from being performed."]
module InternalFailureException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The service has encountered an unexpected error condition which prevents it from servicing the request."]
module LimitExceededException =
  struct
    type nonrec t =
      {
      retryAfterSeconds: ErrorMessage.t option ;
      message: ErrorMessage.t option }
    let make ?retryAfterSeconds =
      fun ?message -> fun () -> { retryAfterSeconds; message }
    let to_value x =
      structure_to_value
        [("Retry-After",
           (Option.map x.retryAfterSeconds ~f:ErrorMessage.to_value));
        ("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      let retryAfterSeconds =
        (Option.map ~f:ErrorMessage.of_xml)
          (Xml.child xml_arg0 "Retry-After") in
      make ?message ?retryAfterSeconds ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      let retryAfterSeconds =
        field_map json "retryAfterSeconds" ErrorMessage.of_json in
      make ?message ?retryAfterSeconds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "There are too many AWS Mobile Hub projects in the account or the account has exceeded the maximum number of resources in some AWS service. You should create another sub-account using AWS Organizations or remove some resources and retry your request."]
module NotFoundException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "No entity can be found with the specified identifier."]
module ProjectDetails =
  struct
    type nonrec t =
      {
      name: ProjectName.t option ;
      projectId: ProjectId.t option ;
      region: ProjectRegion.t option ;
      state: ProjectState.t option ;
      createdDate: Date.t option [@ocaml.doc "Date the project was created."];
      lastUpdatedDate: Date.t option
        [@ocaml.doc "Date of the last modification of the project."];
      consoleUrl: ConsoleUrl.t option
        [@ocaml.doc
          "Website URL for this project in the AWS Mobile Hub console."];
      resources: Resources.t option }
    let make ?name =
      fun ?projectId ->
        fun ?region ->
          fun ?state ->
            fun ?createdDate ->
              fun ?lastUpdatedDate ->
                fun ?consoleUrl ->
                  fun ?resources ->
                    fun () ->
                      {
                        name;
                        projectId;
                        region;
                        state;
                        createdDate;
                        lastUpdatedDate;
                        consoleUrl;
                        resources
                      }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:ProjectName.to_value));
        ("projectId", (Option.map x.projectId ~f:ProjectId.to_value));
        ("region", (Option.map x.region ~f:ProjectRegion.to_value));
        ("state", (Option.map x.state ~f:ProjectState.to_value));
        ("createdDate", (Option.map x.createdDate ~f:Date.to_value));
        ("lastUpdatedDate", (Option.map x.lastUpdatedDate ~f:Date.to_value));
        ("consoleUrl", (Option.map x.consoleUrl ~f:ConsoleUrl.to_value));
        ("resources", (Option.map x.resources ~f:Resources.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resources =
        (Option.map ~f:Resources.of_xml) (Xml.child xml_arg0 "resources") in
      let consoleUrl =
        (Option.map ~f:ConsoleUrl.of_xml) (Xml.child xml_arg0 "consoleUrl") in
      let lastUpdatedDate =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "lastUpdatedDate") in
      let createdDate =
        (Option.map ~f:Date.of_xml) (Xml.child xml_arg0 "createdDate") in
      let state =
        (Option.map ~f:ProjectState.of_xml) (Xml.child xml_arg0 "state") in
      let region =
        (Option.map ~f:ProjectRegion.of_xml) (Xml.child xml_arg0 "region") in
      let projectId =
        (Option.map ~f:ProjectId.of_xml) (Xml.child xml_arg0 "projectId") in
      let name =
        (Option.map ~f:ProjectName.of_xml) (Xml.child xml_arg0 "name") in
      make ?resources ?consoleUrl ?lastUpdatedDate ?createdDate ?state
        ?region ?projectId ?name ()
    let of_json json =
      let resources = field_map json "resources" Resources.of_json in
      let consoleUrl = field_map json "consoleUrl" ConsoleUrl.of_json in
      let lastUpdatedDate = field_map json "lastUpdatedDate" Date.of_json in
      let createdDate = field_map json "createdDate" Date.of_json in
      let state = field_map json "state" ProjectState.of_json in
      let region = field_map json "region" ProjectRegion.of_json in
      let projectId = field_map json "projectId" ProjectId.of_json in
      let name = field_map json "name" ProjectName.of_json in
      make ?resources ?consoleUrl ?lastUpdatedDate ?createdDate ?state
        ?region ?projectId ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Detailed information about an AWS Mobile Hub project."]
module ServiceUnavailableException =
  struct
    type nonrec t =
      {
      retryAfterSeconds: ErrorMessage.t option ;
      message: ErrorMessage.t option }
    let make ?retryAfterSeconds =
      fun ?message -> fun () -> { retryAfterSeconds; message }
    let to_value x =
      structure_to_value
        [("Retry-After",
           (Option.map x.retryAfterSeconds ~f:ErrorMessage.to_value));
        ("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      let retryAfterSeconds =
        (Option.map ~f:ErrorMessage.of_xml)
          (Xml.child xml_arg0 "Retry-After") in
      make ?message ?retryAfterSeconds ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      let retryAfterSeconds =
        field_map json "retryAfterSeconds" ErrorMessage.of_json in
      make ?message ?retryAfterSeconds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The service is temporarily unavailable. The request should be retried after some time delay."]
module TooManyRequestsException =
  struct
    type nonrec t =
      {
      retryAfterSeconds: ErrorMessage.t option ;
      message: ErrorMessage.t option }
    let make ?retryAfterSeconds =
      fun ?message -> fun () -> { retryAfterSeconds; message }
    let to_value x =
      structure_to_value
        [("Retry-After",
           (Option.map x.retryAfterSeconds ~f:ErrorMessage.to_value));
        ("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      let retryAfterSeconds =
        (Option.map ~f:ErrorMessage.of_xml)
          (Xml.child xml_arg0 "Retry-After") in
      make ?message ?retryAfterSeconds ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      let retryAfterSeconds =
        field_map json "retryAfterSeconds" ErrorMessage.of_json in
      make ?message ?retryAfterSeconds ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Too many requests have been received for this AWS account in too short a time. The request should be retried after some time delay."]
module UnauthorizedException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Credentials of the caller are insufficient to authorize the request."]
module Contents =
  struct
    type nonrec t = string[@@ocaml.doc "Binary file data."]
    let make i = i
    let of_string x = x
    let to_value x = `Blob x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml xml_arg0 = string_of_xml ~kind:"a blob" xml_arg0
    let of_json j = string_of_json ~kind:"a blob" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Binary file data."]
module NextToken =
  struct
    type nonrec t = string[@@ocaml.doc
                            "Pagination token. Set to null to start listing records from start. If non-null pagination token is returned in a result, then pass its value in here in another request to list more entries."]
    let context_ = "NextToken"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"NextToken" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "Pagination token. Set to null to start listing records from start. If non-null pagination token is returned in a result, then pass its value in here in another request to list more entries."]
module ProjectSummaries =
  struct
    type nonrec t = ProjectSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ProjectSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ProjectSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"ProjectSummaries" ~of_json:ProjectSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxResults =
  struct
    type nonrec t = int[@@ocaml.doc
                         "Maximum number of records to list in a single response."]
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for MaxResults" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "Maximum number of records to list in a single response."]
module BundleList =
  struct
    type nonrec t = BundleDetails.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:BundleDetails.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:BundleDetails.of_xml)
    let of_json j =
      list_of_json ~kind:"BundleList" ~of_json:BundleDetails.of_json j
    let to_json v = composed_to_json to_value v
  end
module DownloadUrl =
  struct
    type nonrec t = string[@@ocaml.doc "The download Url."]
    let context_ = "DownloadUrl"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DownloadUrl" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "The download Url."]
module ShareUrl =
  struct
    type nonrec t = string[@@ocaml.doc
                            "URL which can be shared to allow other AWS users to create their own project in AWS Mobile Hub with the same configuration as the specified project. This URL pertains to a snapshot in time of the project configuration that is created when this API is called. If you want to share additional changes to your project configuration, then you will need to create and share a new snapshot by calling this method again."]
    let context_ = "ShareUrl"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ShareUrl" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "URL which can be shared to allow other AWS users to create their own project in AWS Mobile Hub with the same configuration as the specified project. This URL pertains to a snapshot in time of the project configuration that is created when this API is called. If you want to share additional changes to your project configuration, then you will need to create and share a new snapshot by calling this method again."]
module SnapshotId =
  struct
    type nonrec t = string[@@ocaml.doc
                            "Unique identifier for the exported snapshot of the project configuration. This snapshot identifier is included in the share URL."]
    let context_ = "SnapshotId"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SnapshotId" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "Unique identifier for the exported snapshot of the project configuration. This snapshot identifier is included in the share URL."]
module Boolean =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module UpdateProjectResult =
  struct
    type nonrec t =
      {
      details: ProjectDetails.t option
        [@ocaml.doc
          "Detailed information about the updated AWS Mobile Hub project."]}
    type nonrec error =
      [ `AccountActionRequiredException of AccountActionRequiredException.t 
      | `BadRequestException of BadRequestException.t 
      | `InternalFailureException of InternalFailureException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?details = fun () -> { details }
    let error_of_json name json =
      match name with
      | "AccountActionRequiredException" ->
          `AccountActionRequiredException
            (AccountActionRequiredException.of_json json)
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "AccountActionRequiredException" ->
          `AccountActionRequiredException
            (AccountActionRequiredException.of_xml xml)
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `AccountActionRequiredException e ->
          `Assoc
            [("error", (`String "AccountActionRequiredException"));
            ("details", (AccountActionRequiredException.to_json e))]
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("details", (Option.map x.details ~f:ProjectDetails.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let details =
        (Option.map ~f:ProjectDetails.of_xml) (Xml.child xml_arg0 "details") in
      make ?details ()
    let of_json json =
      let details = field_map json "details" ProjectDetails.of_json in
      make ?details ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Result structure used for requests to updated project configuration."]
module UpdateProjectRequest =
  struct
    type nonrec t =
      {
      contents: Contents.t option
        [@ocaml.doc
          "ZIP or YAML file which contains project configuration to be updated. This should be the contents of the file downloaded from the URL provided in an export project operation."];
      projectId: ProjectId.t [@ocaml.doc "Unique project identifier."]}
    let context_ = "UpdateProjectRequest"
    let make ?contents = fun ~projectId -> fun () -> { contents; projectId }
    let to_value x =
      structure_to_value
        [("contents", (Option.map x.contents ~f:Contents.to_value));
        ("projectId", (Some (ProjectId.to_value x.projectId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let projectId =
        ProjectId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "projectId") in
      let contents =
        (Option.map ~f:Contents.of_xml) (Xml.child xml_arg0 "contents") in
      make ~projectId ?contents ()
    let of_json json =
      let projectId = field_map_exn json "projectId" ProjectId.of_json in
      let contents = field_map json "contents" Contents.of_json in
      make ~projectId ?contents ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Request structure used for requests to update project configuration."]
module ListProjectsResult =
  struct
    type nonrec t =
      {
      projects: ProjectSummaries.t option ;
      nextToken: NextToken.t option }
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `InternalFailureException of InternalFailureException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?projects = fun ?nextToken -> fun () -> { projects; nextToken }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("projects", (Option.map x.projects ~f:ProjectSummaries.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let projects =
        (Option.map ~f:ProjectSummaries.of_xml)
          (Xml.child xml_arg0 "projects") in
      make ?nextToken ?projects ()
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let projects = field_map json "projects" ProjectSummaries.of_json in
      make ?nextToken ?projects ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Result structure used for requests to list projects in AWS Mobile Hub."]
module ListProjectsRequest =
  struct
    type nonrec t =
      {
      maxResults: MaxResults.t option
        [@ocaml.doc
          "Maximum number of records to list in a single response."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "Pagination token. Set to null to start listing projects from start. If non-null pagination token is returned in a result, then pass its value in here in another request to list more projects."]}
    let make ?maxResults =
      fun ?nextToken -> fun () -> { maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      make ?nextToken ?maxResults ()
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      make ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Request structure used to request projects list in AWS Mobile Hub."]
module ListBundlesResult =
  struct
    type nonrec t =
      {
      bundleList: BundleList.t option [@ocaml.doc "A list of bundles."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "Pagination token. If non-null pagination token is returned in a result, then pass its value in another request to fetch more entries."]}
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `InternalFailureException of InternalFailureException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?bundleList =
      fun ?nextToken -> fun () -> { bundleList; nextToken }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("bundleList", (Option.map x.bundleList ~f:BundleList.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let bundleList =
        (Option.map ~f:BundleList.of_xml) (Xml.child xml_arg0 "bundleList") in
      make ?nextToken ?bundleList ()
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let bundleList = field_map json "bundleList" BundleList.of_json in
      make ?nextToken ?bundleList ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Result structure contains a list of all available bundles with details."]
module ListBundlesRequest =
  struct
    type nonrec t =
      {
      maxResults: MaxResults.t option
        [@ocaml.doc
          "Maximum number of records to list in a single response."];
      nextToken: NextToken.t option
        [@ocaml.doc
          "Pagination token. Set to null to start listing bundles from start. If non-null pagination token is returned in a result, then pass its value in here in another request to list more bundles."]}
    let make ?maxResults =
      fun ?nextToken -> fun () -> { maxResults; nextToken }
    let to_value x =
      structure_to_value
        [("maxResults", (Option.map x.maxResults ~f:MaxResults.to_value));
        ("nextToken", (Option.map x.nextToken ~f:NextToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:NextToken.of_xml) (Xml.child xml_arg0 "nextToken") in
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxResults") in
      make ?nextToken ?maxResults ()
    let of_json json =
      let nextToken = field_map json "nextToken" NextToken.of_json in
      let maxResults = field_map json "maxResults" MaxResults.of_json in
      make ?nextToken ?maxResults ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Request structure to request all available bundles."]
module ExportProjectResult =
  struct
    type nonrec t =
      {
      downloadUrl: DownloadUrl.t option
        [@ocaml.doc
          "URL which can be used to download the exported project configuation file(s)."];
      shareUrl: ShareUrl.t option
        [@ocaml.doc
          "URL which can be shared to allow other AWS users to create their own project in AWS Mobile Hub with the same configuration as the specified project. This URL pertains to a snapshot in time of the project configuration that is created when this API is called. If you want to share additional changes to your project configuration, then you will need to create and share a new snapshot by calling this method again."];
      snapshotId: SnapshotId.t option
        [@ocaml.doc
          "Unique identifier for the exported snapshot of the project configuration. This snapshot identifier is included in the share URL."]}
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `InternalFailureException of InternalFailureException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?downloadUrl =
      fun ?shareUrl ->
        fun ?snapshotId -> fun () -> { downloadUrl; shareUrl; snapshotId }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("downloadUrl", (Option.map x.downloadUrl ~f:DownloadUrl.to_value));
        ("shareUrl", (Option.map x.shareUrl ~f:ShareUrl.to_value));
        ("snapshotId", (Option.map x.snapshotId ~f:SnapshotId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let snapshotId =
        (Option.map ~f:SnapshotId.of_xml) (Xml.child xml_arg0 "snapshotId") in
      let shareUrl =
        (Option.map ~f:ShareUrl.of_xml) (Xml.child xml_arg0 "shareUrl") in
      let downloadUrl =
        (Option.map ~f:DownloadUrl.of_xml) (Xml.child xml_arg0 "downloadUrl") in
      make ?snapshotId ?shareUrl ?downloadUrl ()
    let of_json json =
      let snapshotId = field_map json "snapshotId" SnapshotId.of_json in
      let shareUrl = field_map json "shareUrl" ShareUrl.of_json in
      let downloadUrl = field_map json "downloadUrl" DownloadUrl.of_json in
      make ?snapshotId ?shareUrl ?downloadUrl ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Result structure used for requests to export project configuration details."]
module ExportProjectRequest =
  struct
    type nonrec t =
      {
      projectId: ProjectId.t [@ocaml.doc "Unique project identifier."]}
    let context_ = "ExportProjectRequest"
    let make ~projectId = fun () -> { projectId }
    let to_value x =
      structure_to_value
        [("projectId", (Some (ProjectId.to_value x.projectId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let projectId =
        ProjectId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "projectId") in
      make ~projectId ()
    let of_json json =
      let projectId = field_map_exn json "projectId" ProjectId.of_json in
      make ~projectId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Request structure used in requests to export project configuration details."]
module ExportBundleResult =
  struct
    type nonrec t =
      {
      downloadUrl: DownloadUrl.t option
        [@ocaml.doc
          "URL which contains the custom-generated SDK and tool packages used to integrate the client mobile app or web app with the AWS resources created by the AWS Mobile Hub project."]}
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `InternalFailureException of InternalFailureException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?downloadUrl = fun () -> { downloadUrl }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("downloadUrl", (Option.map x.downloadUrl ~f:DownloadUrl.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let downloadUrl =
        (Option.map ~f:DownloadUrl.of_xml) (Xml.child xml_arg0 "downloadUrl") in
      make ?downloadUrl ()
    let of_json json =
      let downloadUrl = field_map json "downloadUrl" DownloadUrl.of_json in
      make ?downloadUrl ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Result structure which contains link to download custom-generated SDK and tool packages used to integrate mobile web or app clients with backed AWS resources."]
module ExportBundleRequest =
  struct
    type nonrec t =
      {
      bundleId: BundleId.t [@ocaml.doc "Unique bundle identifier."];
      projectId: ProjectId.t option [@ocaml.doc "Unique project identifier."];
      platform: Platform.t option
        [@ocaml.doc "Developer desktop or target application platform."]}
    let context_ = "ExportBundleRequest"
    let make ?projectId =
      fun ?platform ->
        fun ~bundleId -> fun () -> { projectId; platform; bundleId }
    let to_value x =
      structure_to_value
        [("bundleId", (Some (BundleId.to_value x.bundleId)));
        ("projectId", (Option.map x.projectId ~f:ProjectId.to_value));
        ("platform", (Option.map x.platform ~f:Platform.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let platform =
        (Option.map ~f:Platform.of_xml) (Xml.child xml_arg0 "platform") in
      let projectId =
        (Option.map ~f:ProjectId.of_xml) (Xml.child xml_arg0 "projectId") in
      let bundleId =
        BundleId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "bundleId") in
      make ?platform ?projectId ~bundleId ()
    let of_json json =
      let platform = field_map json "platform" Platform.of_json in
      let projectId = field_map json "projectId" ProjectId.of_json in
      let bundleId = field_map_exn json "bundleId" BundleId.of_json in
      make ?platform ?projectId ~bundleId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Request structure used to request generation of custom SDK and tool packages required to integrate mobile web or app clients with backed AWS resources."]
module DescribeProjectResult =
  struct
    type nonrec t = {
      details: ProjectDetails.t option }
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `InternalFailureException of InternalFailureException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?details = fun () -> { details }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("details", (Option.map x.details ~f:ProjectDetails.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let details =
        (Option.map ~f:ProjectDetails.of_xml) (Xml.child xml_arg0 "details") in
      make ?details ()
    let of_json json =
      let details = field_map json "details" ProjectDetails.of_json in
      make ?details ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Result structure used for requests of project details."]
module DescribeProjectRequest =
  struct
    type nonrec t =
      {
      projectId: ProjectId.t [@ocaml.doc "Unique project identifier."];
      syncFromResources: Boolean.t option
        [@ocaml.doc
          "If set to true, causes AWS Mobile Hub to synchronize information from other services, e.g., update state of AWS CloudFormation stacks in the AWS Mobile Hub project."]}
    let context_ = "DescribeProjectRequest"
    let make ?syncFromResources =
      fun ~projectId -> fun () -> { syncFromResources; projectId }
    let to_value x =
      structure_to_value
        [("projectId", (Some (ProjectId.to_value x.projectId)));
        ("syncFromResources",
          (Option.map x.syncFromResources ~f:Boolean.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let syncFromResources =
        (Option.map ~f:Boolean.of_xml)
          (Xml.child xml_arg0 "syncFromResources") in
      let projectId =
        ProjectId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "projectId") in
      make ?syncFromResources ~projectId ()
    let of_json json =
      let syncFromResources =
        field_map json "syncFromResources" Boolean.of_json in
      let projectId = field_map_exn json "projectId" ProjectId.of_json in
      make ?syncFromResources ~projectId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Request structure used to request details about a project."]
module DescribeBundleResult =
  struct
    type nonrec t =
      {
      details: BundleDetails.t option
        [@ocaml.doc "The details of the bundle."]}
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `InternalFailureException of InternalFailureException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?details = fun () -> { details }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("details", (Option.map x.details ~f:BundleDetails.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let details =
        (Option.map ~f:BundleDetails.of_xml) (Xml.child xml_arg0 "details") in
      make ?details ()
    let of_json json =
      let details = field_map json "details" BundleDetails.of_json in
      make ?details ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Result structure contains the details of the bundle."]
module DescribeBundleRequest =
  struct
    type nonrec t =
      {
      bundleId: BundleId.t [@ocaml.doc "Unique bundle identifier."]}
    let context_ = "DescribeBundleRequest"
    let make ~bundleId = fun () -> { bundleId }
    let to_value x =
      structure_to_value
        [("bundleId", (Some (BundleId.to_value x.bundleId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let bundleId =
        BundleId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "bundleId") in
      make ~bundleId ()
    let of_json json =
      let bundleId = field_map_exn json "bundleId" BundleId.of_json in
      make ~bundleId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Request structure to request the details of a specific bundle."]
module DeleteProjectResult =
  struct
    type nonrec t =
      {
      deletedResources: Resources.t option
        [@ocaml.doc "Resources which were deleted."];
      orphanedResources: Resources.t option
        [@ocaml.doc
          "Resources which were not deleted, due to a risk of losing potentially important data or files."]}
    type nonrec error =
      [ `InternalFailureException of InternalFailureException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?deletedResources =
      fun ?orphanedResources ->
        fun () -> { deletedResources; orphanedResources }
    let error_of_json name json =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("deletedResources",
           (Option.map x.deletedResources ~f:Resources.to_value));
        ("orphanedResources",
          (Option.map x.orphanedResources ~f:Resources.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let orphanedResources =
        (Option.map ~f:Resources.of_xml)
          (Xml.child xml_arg0 "orphanedResources") in
      let deletedResources =
        (Option.map ~f:Resources.of_xml)
          (Xml.child xml_arg0 "deletedResources") in
      make ?orphanedResources ?deletedResources ()
    let of_json json =
      let orphanedResources =
        field_map json "orphanedResources" Resources.of_json in
      let deletedResources =
        field_map json "deletedResources" Resources.of_json in
      make ?orphanedResources ?deletedResources ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Result structure used in response to request to delete a project."]
module DeleteProjectRequest =
  struct
    type nonrec t =
      {
      projectId: ProjectId.t [@ocaml.doc "Unique project identifier."]}
    let context_ = "DeleteProjectRequest"
    let make ~projectId = fun () -> { projectId }
    let to_value x =
      structure_to_value
        [("projectId", (Some (ProjectId.to_value x.projectId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let projectId =
        ProjectId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "projectId") in
      make ~projectId ()
    let of_json json =
      let projectId = field_map_exn json "projectId" ProjectId.of_json in
      make ~projectId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Request structure used to request a project be deleted."]
module CreateProjectResult =
  struct
    type nonrec t =
      {
      details: ProjectDetails.t option
        [@ocaml.doc
          "Detailed information about the created AWS Mobile Hub project."]}
    type nonrec error =
      [ `BadRequestException of BadRequestException.t 
      | `InternalFailureException of InternalFailureException.t 
      | `LimitExceededException of LimitExceededException.t 
      | `NotFoundException of NotFoundException.t 
      | `ServiceUnavailableException of ServiceUnavailableException.t 
      | `TooManyRequestsException of TooManyRequestsException.t 
      | `UnauthorizedException of UnauthorizedException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make ?details = fun () -> { details }
    let error_of_json name json =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_json json)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_json json)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_json json)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_json json)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_json json)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_json json)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "BadRequestException" ->
          `BadRequestException (BadRequestException.of_xml xml)
      | "InternalFailureException" ->
          `InternalFailureException (InternalFailureException.of_xml xml)
      | "LimitExceededException" ->
          `LimitExceededException (LimitExceededException.of_xml xml)
      | "NotFoundException" ->
          `NotFoundException (NotFoundException.of_xml xml)
      | "ServiceUnavailableException" ->
          `ServiceUnavailableException
            (ServiceUnavailableException.of_xml xml)
      | "TooManyRequestsException" ->
          `TooManyRequestsException (TooManyRequestsException.of_xml xml)
      | "UnauthorizedException" ->
          `UnauthorizedException (UnauthorizedException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `BadRequestException e ->
          `Assoc
            [("error", (`String "BadRequestException"));
            ("details", (BadRequestException.to_json e))]
      | `InternalFailureException e ->
          `Assoc
            [("error", (`String "InternalFailureException"));
            ("details", (InternalFailureException.to_json e))]
      | `LimitExceededException e ->
          `Assoc
            [("error", (`String "LimitExceededException"));
            ("details", (LimitExceededException.to_json e))]
      | `NotFoundException e ->
          `Assoc
            [("error", (`String "NotFoundException"));
            ("details", (NotFoundException.to_json e))]
      | `ServiceUnavailableException e ->
          `Assoc
            [("error", (`String "ServiceUnavailableException"));
            ("details", (ServiceUnavailableException.to_json e))]
      | `TooManyRequestsException e ->
          `Assoc
            [("error", (`String "TooManyRequestsException"));
            ("details", (TooManyRequestsException.to_json e))]
      | `UnauthorizedException e ->
          `Assoc
            [("error", (`String "UnauthorizedException"));
            ("details", (UnauthorizedException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("details", (Option.map x.details ~f:ProjectDetails.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let details =
        (Option.map ~f:ProjectDetails.of_xml) (Xml.child xml_arg0 "details") in
      make ?details ()
    let of_json json =
      let details = field_map json "details" ProjectDetails.of_json in
      make ?details ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Result structure used in response to a request to create a project."]
module CreateProjectRequest =
  struct
    type nonrec t =
      {
      name: ProjectName.t option [@ocaml.doc "Name of the project."];
      region: ProjectRegion.t option
        [@ocaml.doc
          "Default region where project resources should be created."];
      contents: Contents.t option
        [@ocaml.doc
          "ZIP or YAML file which contains configuration settings to be used when creating the project. This may be the contents of the file downloaded from the URL provided in an export project operation."];
      snapshotId: SnapshotId.t option
        [@ocaml.doc
          "Unique identifier for an exported snapshot of project configuration. This snapshot identifier is included in the share URL when a project is exported."]}
    let make ?name =
      fun ?region ->
        fun ?contents ->
          fun ?snapshotId -> fun () -> { name; region; contents; snapshotId }
    let to_value x =
      structure_to_value
        [("name", (Option.map x.name ~f:ProjectName.to_value));
        ("region", (Option.map x.region ~f:ProjectRegion.to_value));
        ("contents", (Option.map x.contents ~f:Contents.to_value));
        ("snapshotId", (Option.map x.snapshotId ~f:SnapshotId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let snapshotId =
        (Option.map ~f:SnapshotId.of_xml) (Xml.child xml_arg0 "snapshotId") in
      let contents =
        (Option.map ~f:Contents.of_xml) (Xml.child xml_arg0 "contents") in
      let region =
        (Option.map ~f:ProjectRegion.of_xml) (Xml.child xml_arg0 "region") in
      let name =
        (Option.map ~f:ProjectName.of_xml) (Xml.child xml_arg0 "name") in
      make ?snapshotId ?contents ?region ?name ()
    let of_json json =
      let snapshotId = field_map json "snapshotId" SnapshotId.of_json in
      let contents = field_map json "contents" Contents.of_json in
      let region = field_map json "region" ProjectRegion.of_json in
      let name = field_map json "name" ProjectName.of_json in
      make ?snapshotId ?contents ?region ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Request structure used to request a project be created."]
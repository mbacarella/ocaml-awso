(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | AddListenerCertificates: (AddListenerCertificatesInput.t,
  AddListenerCertificatesOutput.t, AddListenerCertificatesOutput.error) t 
  | AddTags: (AddTagsInput.t, AddTagsOutput.t, AddTagsOutput.error) t 
  | CreateListener: (CreateListenerInput.t, CreateListenerOutput.t,
  CreateListenerOutput.error) t 
  | CreateLoadBalancer: (CreateLoadBalancerInput.t,
  CreateLoadBalancerOutput.t, CreateLoadBalancerOutput.error) t 
  | CreateRule: (CreateRuleInput.t, CreateRuleOutput.t,
  CreateRuleOutput.error) t 
  | CreateTargetGroup: (CreateTargetGroupInput.t, CreateTargetGroupOutput.t,
  CreateTargetGroupOutput.error) t 
  | DeleteListener: (DeleteListenerInput.t, DeleteListenerOutput.t,
  DeleteListenerOutput.error) t 
  | DeleteLoadBalancer: (DeleteLoadBalancerInput.t,
  DeleteLoadBalancerOutput.t, DeleteLoadBalancerOutput.error) t 
  | DeleteRule: (DeleteRuleInput.t, DeleteRuleOutput.t,
  DeleteRuleOutput.error) t 
  | DeleteTargetGroup: (DeleteTargetGroupInput.t, DeleteTargetGroupOutput.t,
  DeleteTargetGroupOutput.error) t 
  | DeregisterTargets: (DeregisterTargetsInput.t, DeregisterTargetsOutput.t,
  DeregisterTargetsOutput.error) t 
  | DescribeAccountLimits: (DescribeAccountLimitsInput.t,
  DescribeAccountLimitsOutput.t, DescribeAccountLimitsOutput.error) t 
  | DescribeListenerCertificates: (DescribeListenerCertificatesInput.t,
  DescribeListenerCertificatesOutput.t,
  DescribeListenerCertificatesOutput.error) t 
  | DescribeListeners: (DescribeListenersInput.t, DescribeListenersOutput.t,
  DescribeListenersOutput.error) t 
  | DescribeLoadBalancerAttributes: (DescribeLoadBalancerAttributesInput.t,
  DescribeLoadBalancerAttributesOutput.t,
  DescribeLoadBalancerAttributesOutput.error) t 
  | DescribeLoadBalancers: (DescribeLoadBalancersInput.t,
  DescribeLoadBalancersOutput.t, DescribeLoadBalancersOutput.error) t 
  | DescribeRules: (DescribeRulesInput.t, DescribeRulesOutput.t,
  DescribeRulesOutput.error) t 
  | DescribeSSLPolicies: (DescribeSSLPoliciesInput.t,
  DescribeSSLPoliciesOutput.t, DescribeSSLPoliciesOutput.error) t 
  | DescribeTags: (DescribeTagsInput.t, DescribeTagsOutput.t,
  DescribeTagsOutput.error) t 
  | DescribeTargetGroupAttributes: (DescribeTargetGroupAttributesInput.t,
  DescribeTargetGroupAttributesOutput.t,
  DescribeTargetGroupAttributesOutput.error) t 
  | DescribeTargetGroups: (DescribeTargetGroupsInput.t,
  DescribeTargetGroupsOutput.t, DescribeTargetGroupsOutput.error) t 
  | DescribeTargetHealth: (DescribeTargetHealthInput.t,
  DescribeTargetHealthOutput.t, DescribeTargetHealthOutput.error) t 
  | ModifyListener: (ModifyListenerInput.t, ModifyListenerOutput.t,
  ModifyListenerOutput.error) t 
  | ModifyLoadBalancerAttributes: (ModifyLoadBalancerAttributesInput.t,
  ModifyLoadBalancerAttributesOutput.t,
  ModifyLoadBalancerAttributesOutput.error) t 
  | ModifyRule: (ModifyRuleInput.t, ModifyRuleOutput.t,
  ModifyRuleOutput.error) t 
  | ModifyTargetGroup: (ModifyTargetGroupInput.t, ModifyTargetGroupOutput.t,
  ModifyTargetGroupOutput.error) t 
  | ModifyTargetGroupAttributes: (ModifyTargetGroupAttributesInput.t,
  ModifyTargetGroupAttributesOutput.t,
  ModifyTargetGroupAttributesOutput.error) t 
  | RegisterTargets: (RegisterTargetsInput.t, RegisterTargetsOutput.t,
  RegisterTargetsOutput.error) t 
  | RemoveListenerCertificates: (RemoveListenerCertificatesInput.t,
  RemoveListenerCertificatesOutput.t, RemoveListenerCertificatesOutput.error)
  t 
  | RemoveTags: (RemoveTagsInput.t, RemoveTagsOutput.t,
  RemoveTagsOutput.error) t 
  | SetIpAddressType: (SetIpAddressTypeInput.t, SetIpAddressTypeOutput.t,
  SetIpAddressTypeOutput.error) t 
  | SetRulePriorities: (SetRulePrioritiesInput.t, SetRulePrioritiesOutput.t,
  SetRulePrioritiesOutput.error) t 
  | SetSecurityGroups: (SetSecurityGroupsInput.t, SetSecurityGroupsOutput.t,
  SetSecurityGroupsOutput.error) t 
  | SetSubnets: (SetSubnetsInput.t, SetSubnetsOutput.t,
  SetSubnetsOutput.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | AddListenerCertificates -> `POST
  | AddTags -> `POST
  | CreateListener -> `POST
  | CreateLoadBalancer -> `POST
  | CreateRule -> `POST
  | CreateTargetGroup -> `POST
  | DeleteListener -> `POST
  | DeleteLoadBalancer -> `POST
  | DeleteRule -> `POST
  | DeleteTargetGroup -> `POST
  | DeregisterTargets -> `POST
  | DescribeAccountLimits -> `POST
  | DescribeListenerCertificates -> `POST
  | DescribeListeners -> `POST
  | DescribeLoadBalancerAttributes -> `POST
  | DescribeLoadBalancers -> `POST
  | DescribeRules -> `POST
  | DescribeSSLPolicies -> `POST
  | DescribeTags -> `POST
  | DescribeTargetGroupAttributes -> `POST
  | DescribeTargetGroups -> `POST
  | DescribeTargetHealth -> `POST
  | ModifyListener -> `POST
  | ModifyLoadBalancerAttributes -> `POST
  | ModifyRule -> `POST
  | ModifyTargetGroup -> `POST
  | ModifyTargetGroupAttributes -> `POST
  | RegisterTargets -> `POST
  | RemoveListenerCertificates -> `POST
  | RemoveTags -> `POST
  | SetIpAddressType -> `POST
  | SetRulePriorities -> `POST
  | SetSecurityGroups -> `POST
  | SetSubnets -> `POST
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | AddListenerCertificates -> (Format.kasprintf Uri.of_string) "/"
      | AddTags -> (Format.kasprintf Uri.of_string) "/"
      | CreateListener -> (Format.kasprintf Uri.of_string) "/"
      | CreateLoadBalancer -> (Format.kasprintf Uri.of_string) "/"
      | CreateRule -> (Format.kasprintf Uri.of_string) "/"
      | CreateTargetGroup -> (Format.kasprintf Uri.of_string) "/"
      | DeleteListener -> (Format.kasprintf Uri.of_string) "/"
      | DeleteLoadBalancer -> (Format.kasprintf Uri.of_string) "/"
      | DeleteRule -> (Format.kasprintf Uri.of_string) "/"
      | DeleteTargetGroup -> (Format.kasprintf Uri.of_string) "/"
      | DeregisterTargets -> (Format.kasprintf Uri.of_string) "/"
      | DescribeAccountLimits -> (Format.kasprintf Uri.of_string) "/"
      | DescribeListenerCertificates -> (Format.kasprintf Uri.of_string) "/"
      | DescribeListeners -> (Format.kasprintf Uri.of_string) "/"
      | DescribeLoadBalancerAttributes ->
          (Format.kasprintf Uri.of_string) "/"
      | DescribeLoadBalancers -> (Format.kasprintf Uri.of_string) "/"
      | DescribeRules -> (Format.kasprintf Uri.of_string) "/"
      | DescribeSSLPolicies -> (Format.kasprintf Uri.of_string) "/"
      | DescribeTags -> (Format.kasprintf Uri.of_string) "/"
      | DescribeTargetGroupAttributes -> (Format.kasprintf Uri.of_string) "/"
      | DescribeTargetGroups -> (Format.kasprintf Uri.of_string) "/"
      | DescribeTargetHealth -> (Format.kasprintf Uri.of_string) "/"
      | ModifyListener -> (Format.kasprintf Uri.of_string) "/"
      | ModifyLoadBalancerAttributes -> (Format.kasprintf Uri.of_string) "/"
      | ModifyRule -> (Format.kasprintf Uri.of_string) "/"
      | ModifyTargetGroup -> (Format.kasprintf Uri.of_string) "/"
      | ModifyTargetGroupAttributes -> (Format.kasprintf Uri.of_string) "/"
      | RegisterTargets -> (Format.kasprintf Uri.of_string) "/"
      | RemoveListenerCertificates -> (Format.kasprintf Uri.of_string) "/"
      | RemoveTags -> (Format.kasprintf Uri.of_string) "/"
      | SetIpAddressType -> (Format.kasprintf Uri.of_string) "/"
      | SetRulePriorities -> (Format.kasprintf Uri.of_string) "/"
      | SetSecurityGroups -> (Format.kasprintf Uri.of_string) "/"
      | SetSubnets -> (Format.kasprintf Uri.of_string) "/")
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | AddListenerCertificates ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["AddListenerCertificates"]);
          ("Version", [apiVersion])] in
        let query =
          (AddListenerCertificatesInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | AddTags ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["AddTags"]); ("Version", [apiVersion])] in
        let query = (AddTagsInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | CreateListener ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["CreateListener"]); ("Version", [apiVersion])] in
        let query =
          (CreateListenerInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | CreateLoadBalancer ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["CreateLoadBalancer"]); ("Version", [apiVersion])] in
        let query =
          (CreateLoadBalancerInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | CreateRule ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["CreateRule"]); ("Version", [apiVersion])] in
        let query =
          (CreateRuleInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | CreateTargetGroup ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["CreateTargetGroup"]); ("Version", [apiVersion])] in
        let query =
          (CreateTargetGroupInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DeleteListener ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DeleteListener"]); ("Version", [apiVersion])] in
        let query =
          (DeleteListenerInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DeleteLoadBalancer ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DeleteLoadBalancer"]); ("Version", [apiVersion])] in
        let query =
          (DeleteLoadBalancerInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DeleteRule ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["DeleteRule"]); ("Version", [apiVersion])] in
        let query =
          (DeleteRuleInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DeleteTargetGroup ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DeleteTargetGroup"]); ("Version", [apiVersion])] in
        let query =
          (DeleteTargetGroupInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DeregisterTargets ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DeregisterTargets"]); ("Version", [apiVersion])] in
        let query =
          (DeregisterTargetsInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeAccountLimits ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeAccountLimits"]); ("Version", [apiVersion])] in
        let query =
          (DescribeAccountLimitsInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeListenerCertificates ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeListenerCertificates"]);
          ("Version", [apiVersion])] in
        let query =
          (DescribeListenerCertificatesInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeListeners ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeListeners"]); ("Version", [apiVersion])] in
        let query =
          (DescribeListenersInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeLoadBalancerAttributes ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeLoadBalancerAttributes"]);
          ("Version", [apiVersion])] in
        let query =
          (DescribeLoadBalancerAttributesInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeLoadBalancers ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeLoadBalancers"]); ("Version", [apiVersion])] in
        let query =
          (DescribeLoadBalancersInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeRules ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["DescribeRules"]); ("Version", [apiVersion])] in
        let query =
          (DescribeRulesInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeSSLPolicies ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeSSLPolicies"]); ("Version", [apiVersion])] in
        let query =
          (DescribeSSLPoliciesInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeTags ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["DescribeTags"]); ("Version", [apiVersion])] in
        let query =
          (DescribeTagsInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeTargetGroupAttributes ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeTargetGroupAttributes"]);
          ("Version", [apiVersion])] in
        let query =
          (DescribeTargetGroupAttributesInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeTargetGroups ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeTargetGroups"]); ("Version", [apiVersion])] in
        let query =
          (DescribeTargetGroupsInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | DescribeTargetHealth ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["DescribeTargetHealth"]); ("Version", [apiVersion])] in
        let query =
          (DescribeTargetHealthInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | ModifyListener ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["ModifyListener"]); ("Version", [apiVersion])] in
        let query =
          (ModifyListenerInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | ModifyLoadBalancerAttributes ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["ModifyLoadBalancerAttributes"]);
          ("Version", [apiVersion])] in
        let query =
          (ModifyLoadBalancerAttributesInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | ModifyRule ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["ModifyRule"]); ("Version", [apiVersion])] in
        let query =
          (ModifyRuleInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | ModifyTargetGroup ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["ModifyTargetGroup"]); ("Version", [apiVersion])] in
        let query =
          (ModifyTargetGroupInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | ModifyTargetGroupAttributes ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["ModifyTargetGroupAttributes"]);
          ("Version", [apiVersion])] in
        let query =
          (ModifyTargetGroupAttributesInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | RegisterTargets ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["RegisterTargets"]); ("Version", [apiVersion])] in
        let query =
          (RegisterTargetsInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | RemoveListenerCertificates ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["RemoveListenerCertificates"]);
          ("Version", [apiVersion])] in
        let query =
          (RemoveListenerCertificatesInput.to_query req) |>
            Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | RemoveTags ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["RemoveTags"]); ("Version", [apiVersion])] in
        let query =
          (RemoveTagsInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | SetIpAddressType ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["SetIpAddressType"]); ("Version", [apiVersion])] in
        let query =
          (SetIpAddressTypeInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | SetRulePriorities ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["SetRulePriorities"]); ("Version", [apiVersion])] in
        let query =
          (SetRulePrioritiesInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | SetSecurityGroups ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta =
          [("Action", ["SetSecurityGroups"]); ("Version", [apiVersion])] in
        let query =
          (SetSecurityGroupsInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
  | SetSubnets ->
      let headers =
        Awso.Http.Headers.of_list
          [("content-type",
             "application/x-www-form-urlencoded; charset=utf-8")] in
      let body =
        let meta = [("Action", ["SetSubnets"]); ("Version", [apiVersion])] in
        let query =
          (SetSubnetsInput.to_query req) |> Awso.Client.Query.render in
        Some (Uri.encoded_of_query (meta @ query)) in
      Awso.Http.Request.make ?body ~headers (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : Awso.Http.Response.t) : (o, e) result=
  let code = Awso.Http.Status.to_code (Awso.Http.Response.status resp) in
  let is_success = (code >= 200) && (code < 300) in
  let parse_aws_error error_of_xml =
    let body = Awso.Http.Response.body resp in
    let bail () =
      raise
        (Awso.Http.Io.Error.Bad_response
           { Awso.Http.Io.Error.code = code; body; x_amzn_error_type = None }) in
    match (error_of_xml, ((code >= 400) && (code <= 599))) with
    | (None, _) | (_, false) -> bail ()
    | (Some error_of_xml, true) ->
        (match Awso.Xml.parse_response body with
         | `Data _ -> bail ()
         | `El (((_, "ErrorResponse"), _), _) as error_response_xml ->
             let error_xml = Awso.Xml.child_exn error_response_xml "Error" in
             (try
                let error_code =
                  match Awso.Xml.child_exn error_xml "Code" with
                  | `Data error_code -> error_code
                  | `El (_, children) ->
                      (List.map children
                         ~f:(function | `Data s -> s | `El _ -> ""))
                        |> (String.concat ~sep:"") in
                error_of_xml (String.strip error_code) error_xml
              with | Failure _ -> bail ())
         | `El _ -> bail ()) in
  let _ = parse_aws_error in
  let _ = resp in
  match endpoint with
  | AddListenerCertificates ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (AddListenerCertificatesOutput.of_xml xml)
      else
        Error
          (parse_aws_error (Some AddListenerCertificatesOutput.error_of_xml))
  | AddTags ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (AddTagsOutput.of_xml xml)
      else Error (parse_aws_error (Some AddTagsOutput.error_of_xml))
  | CreateListener ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (CreateListenerOutput.of_xml xml)
      else Error (parse_aws_error (Some CreateListenerOutput.error_of_xml))
  | CreateLoadBalancer ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (CreateLoadBalancerOutput.of_xml xml)
      else
        Error (parse_aws_error (Some CreateLoadBalancerOutput.error_of_xml))
  | CreateRule ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (CreateRuleOutput.of_xml xml)
      else Error (parse_aws_error (Some CreateRuleOutput.error_of_xml))
  | CreateTargetGroup ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (CreateTargetGroupOutput.of_xml xml)
      else
        Error (parse_aws_error (Some CreateTargetGroupOutput.error_of_xml))
  | DeleteListener ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DeleteListenerOutput.of_xml xml)
      else Error (parse_aws_error (Some DeleteListenerOutput.error_of_xml))
  | DeleteLoadBalancer ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DeleteLoadBalancerOutput.of_xml xml)
      else
        Error (parse_aws_error (Some DeleteLoadBalancerOutput.error_of_xml))
  | DeleteRule ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DeleteRuleOutput.of_xml xml)
      else Error (parse_aws_error (Some DeleteRuleOutput.error_of_xml))
  | DeleteTargetGroup ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DeleteTargetGroupOutput.of_xml xml)
      else
        Error (parse_aws_error (Some DeleteTargetGroupOutput.error_of_xml))
  | DeregisterTargets ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DeregisterTargetsOutput.of_xml xml)
      else
        Error (parse_aws_error (Some DeregisterTargetsOutput.error_of_xml))
  | DescribeAccountLimits ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeAccountLimitsOutput.of_xml xml)
      else Error (parse_aws_error None)
  | DescribeListenerCertificates ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeListenerCertificatesOutput.of_xml xml)
      else
        Error
          (parse_aws_error
             (Some DescribeListenerCertificatesOutput.error_of_xml))
  | DescribeListeners ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeListenersOutput.of_xml xml)
      else
        Error (parse_aws_error (Some DescribeListenersOutput.error_of_xml))
  | DescribeLoadBalancerAttributes ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeLoadBalancerAttributesOutput.of_xml xml)
      else
        Error
          (parse_aws_error
             (Some DescribeLoadBalancerAttributesOutput.error_of_xml))
  | DescribeLoadBalancers ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeLoadBalancersOutput.of_xml xml)
      else
        Error
          (parse_aws_error (Some DescribeLoadBalancersOutput.error_of_xml))
  | DescribeRules ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeRulesOutput.of_xml xml)
      else Error (parse_aws_error (Some DescribeRulesOutput.error_of_xml))
  | DescribeSSLPolicies ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeSSLPoliciesOutput.of_xml xml)
      else
        Error (parse_aws_error (Some DescribeSSLPoliciesOutput.error_of_xml))
  | DescribeTags ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeTagsOutput.of_xml xml)
      else Error (parse_aws_error (Some DescribeTagsOutput.error_of_xml))
  | DescribeTargetGroupAttributes ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeTargetGroupAttributesOutput.of_xml xml)
      else
        Error
          (parse_aws_error
             (Some DescribeTargetGroupAttributesOutput.error_of_xml))
  | DescribeTargetGroups ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeTargetGroupsOutput.of_xml xml)
      else
        Error
          (parse_aws_error (Some DescribeTargetGroupsOutput.error_of_xml))
  | DescribeTargetHealth ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (DescribeTargetHealthOutput.of_xml xml)
      else
        Error
          (parse_aws_error (Some DescribeTargetHealthOutput.error_of_xml))
  | ModifyListener ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (ModifyListenerOutput.of_xml xml)
      else Error (parse_aws_error (Some ModifyListenerOutput.error_of_xml))
  | ModifyLoadBalancerAttributes ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (ModifyLoadBalancerAttributesOutput.of_xml xml)
      else
        Error
          (parse_aws_error
             (Some ModifyLoadBalancerAttributesOutput.error_of_xml))
  | ModifyRule ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (ModifyRuleOutput.of_xml xml)
      else Error (parse_aws_error (Some ModifyRuleOutput.error_of_xml))
  | ModifyTargetGroup ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (ModifyTargetGroupOutput.of_xml xml)
      else
        Error (parse_aws_error (Some ModifyTargetGroupOutput.error_of_xml))
  | ModifyTargetGroupAttributes ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (ModifyTargetGroupAttributesOutput.of_xml xml)
      else
        Error
          (parse_aws_error
             (Some ModifyTargetGroupAttributesOutput.error_of_xml))
  | RegisterTargets ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (RegisterTargetsOutput.of_xml xml)
      else Error (parse_aws_error (Some RegisterTargetsOutput.error_of_xml))
  | RemoveListenerCertificates ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (RemoveListenerCertificatesOutput.of_xml xml)
      else
        Error
          (parse_aws_error
             (Some RemoveListenerCertificatesOutput.error_of_xml))
  | RemoveTags ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (RemoveTagsOutput.of_xml xml)
      else Error (parse_aws_error (Some RemoveTagsOutput.error_of_xml))
  | SetIpAddressType ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (SetIpAddressTypeOutput.of_xml xml)
      else Error (parse_aws_error (Some SetIpAddressTypeOutput.error_of_xml))
  | SetRulePriorities ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (SetRulePrioritiesOutput.of_xml xml)
      else
        Error (parse_aws_error (Some SetRulePrioritiesOutput.error_of_xml))
  | SetSecurityGroups ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (SetSecurityGroupsOutput.of_xml xml)
      else
        Error (parse_aws_error (Some SetSecurityGroupsOutput.error_of_xml))
  | SetSubnets ->
      if is_success
      then
        let xml = Awso.Xml.parse_response (Awso.Http.Response.body resp) in
        Ok (SetSubnetsOutput.of_xml xml)
      else Error (parse_aws_error (Some SetSubnetsOutput.error_of_xml))
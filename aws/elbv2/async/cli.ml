(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let add_listener_certificates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and listenerArn =
         flag "listener-arn" (required string) ~doc:"STRING ListenerArn"
       and certificates =
         flag "certificates" (required json_arg) ~doc:"JSON CertificateList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_listener_certificates
           (Values.AddListenerCertificatesInput.make ~listenerArn
              ~certificates:(Values.CertificateList.of_json certificates) ())
           (Some Values.AddListenerCertificatesOutput.to_json)
           (Some Values.AddListenerCertificatesOutput.error_to_json)])
let add_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArns =
         flag "resource-arns" (required json_arg) ~doc:"JSON ResourceArns"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.add_tags
           (Values.AddTagsInput.make
              ~resourceArns:(Values.ResourceArns.of_json resourceArns)
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.AddTagsOutput.to_json)
           (Some Values.AddTagsOutput.error_to_json)])
let create_listener =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and protocol =
         flag "protocol" (optional json_arg) ~doc:"JSON ProtocolEnum"
       and port = flag "port" (optional int) ~doc:"INT Port"
       and sslPolicy =
         flag "ssl-policy" (optional string) ~doc:"STRING SslPolicyName"
       and certificates =
         flag "certificates" (optional json_arg) ~doc:"JSON CertificateList"
       and alpnPolicy =
         flag "alpn-policy" (optional json_arg) ~doc:"JSON AlpnPolicyName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and loadBalancerArn =
         flag "load-balancer-arn" (required string)
           ~doc:"STRING LoadBalancerArn"
       and defaultActions =
         flag "default-actions" (required json_arg) ~doc:"JSON Actions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_listener
           (Values.CreateListenerInput.make
              ?protocol:(Option.map ~f:Values.ProtocolEnum.of_json protocol)
              ?port ?sslPolicy
              ?certificates:(Option.map ~f:Values.CertificateList.of_json
                               certificates)
              ?alpnPolicy:(Option.map ~f:Values.AlpnPolicyName.of_json
                             alpnPolicy)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ~loadBalancerArn
              ~defaultActions:(Values.Actions.of_json defaultActions) ())
           (Some Values.CreateListenerOutput.to_json)
           (Some Values.CreateListenerOutput.error_to_json)])
let create_load_balancer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subnets = flag "subnets" (optional json_arg) ~doc:"JSON Subnets"
       and subnetMappings =
         flag "subnet-mappings" (optional json_arg)
           ~doc:"JSON SubnetMappings"
       and securityGroups =
         flag "security-groups" (optional json_arg)
           ~doc:"JSON SecurityGroups"
       and scheme =
         flag "scheme" (optional json_arg) ~doc:"JSON LoadBalancerSchemeEnum"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and type_ =
         flag "type-" (optional json_arg) ~doc:"JSON LoadBalancerTypeEnum"
       and ipAddressType =
         flag "ip-address-type" (optional json_arg) ~doc:"JSON IpAddressType"
       and customerOwnedIpv4Pool =
         flag "customer-owned-ipv4-pool" (optional string)
           ~doc:"STRING CustomerOwnedIpv4Pool"
       and name =
         flag "name" (required string) ~doc:"STRING LoadBalancerName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_load_balancer
           (Values.CreateLoadBalancerInput.make
              ?subnets:(Option.map ~f:Values.Subnets.of_json subnets)
              ?subnetMappings:(Option.map ~f:Values.SubnetMappings.of_json
                                 subnetMappings)
              ?securityGroups:(Option.map ~f:Values.SecurityGroups.of_json
                                 securityGroups)
              ?scheme:(Option.map ~f:Values.LoadBalancerSchemeEnum.of_json
                         scheme)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?type_:(Option.map ~f:Values.LoadBalancerTypeEnum.of_json type_)
              ?ipAddressType:(Option.map ~f:Values.IpAddressType.of_json
                                ipAddressType) ?customerOwnedIpv4Pool ~name
              ()) (Some Values.CreateLoadBalancerOutput.to_json)
           (Some Values.CreateLoadBalancerOutput.error_to_json)])
let create_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and listenerArn =
         flag "listener-arn" (required string) ~doc:"STRING ListenerArn"
       and conditions =
         flag "conditions" (required json_arg) ~doc:"JSON RuleConditionList"
       and priority = flag "priority" (required int) ~doc:"INT RulePriority"
       and actions = flag "actions" (required json_arg) ~doc:"JSON Actions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_rule
           (Values.CreateRuleInput.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~listenerArn
              ~conditions:(Values.RuleConditionList.of_json conditions)
              ~priority ~actions:(Values.Actions.of_json actions) ())
           (Some Values.CreateRuleOutput.to_json)
           (Some Values.CreateRuleOutput.error_to_json)])
let create_target_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and protocol =
         flag "protocol" (optional json_arg) ~doc:"JSON ProtocolEnum"
       and protocolVersion =
         flag "protocol-version" (optional string)
           ~doc:"STRING ProtocolVersion"
       and port = flag "port" (optional int) ~doc:"INT Port"
       and vpcId = flag "vpc-id" (optional string) ~doc:"STRING VpcId"
       and healthCheckProtocol =
         flag "health-check-protocol" (optional json_arg)
           ~doc:"JSON ProtocolEnum"
       and healthCheckPort =
         flag "health-check-port" (optional string)
           ~doc:"STRING HealthCheckPort"
       and healthCheckEnabled =
         flag "health-check-enabled" (optional bool)
           ~doc:"BOOL HealthCheckEnabled"
       and healthCheckPath =
         flag "health-check-path" (optional string) ~doc:"STRING Path"
       and healthCheckIntervalSeconds =
         flag "health-check-interval-seconds" (optional int)
           ~doc:"INT HealthCheckIntervalSeconds"
       and healthCheckTimeoutSeconds =
         flag "health-check-timeout-seconds" (optional int)
           ~doc:"INT HealthCheckTimeoutSeconds"
       and healthyThresholdCount =
         flag "healthy-threshold-count" (optional int)
           ~doc:"INT HealthCheckThresholdCount"
       and unhealthyThresholdCount =
         flag "unhealthy-threshold-count" (optional int)
           ~doc:"INT HealthCheckThresholdCount"
       and matcher = flag "matcher" (optional json_arg) ~doc:"JSON Matcher"
       and targetType =
         flag "target-type" (optional json_arg) ~doc:"JSON TargetTypeEnum"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and ipAddressType =
         flag "ip-address-type" (optional json_arg)
           ~doc:"JSON TargetGroupIpAddressTypeEnum"
       and name = flag "name" (required string) ~doc:"STRING TargetGroupName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_target_group
           (Values.CreateTargetGroupInput.make
              ?protocol:(Option.map ~f:Values.ProtocolEnum.of_json protocol)
              ?protocolVersion ?port ?vpcId
              ?healthCheckProtocol:(Option.map ~f:Values.ProtocolEnum.of_json
                                      healthCheckProtocol) ?healthCheckPort
              ?healthCheckEnabled ?healthCheckPath
              ?healthCheckIntervalSeconds ?healthCheckTimeoutSeconds
              ?healthyThresholdCount ?unhealthyThresholdCount
              ?matcher:(Option.map ~f:Values.Matcher.of_json matcher)
              ?targetType:(Option.map ~f:Values.TargetTypeEnum.of_json
                             targetType)
              ?tags:(Option.map ~f:Values.TagList.of_json tags)
              ?ipAddressType:(Option.map
                                ~f:Values.TargetGroupIpAddressTypeEnum.of_json
                                ipAddressType) ~name ())
           (Some Values.CreateTargetGroupOutput.to_json)
           (Some Values.CreateTargetGroupOutput.error_to_json)])
let delete_listener =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and listenerArn =
         flag "listener-arn" (required string) ~doc:"STRING ListenerArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_listener
           (Values.DeleteListenerInput.make ~listenerArn ())
           (Some Values.DeleteListenerOutput.to_json)
           (Some Values.DeleteListenerOutput.error_to_json)])
let delete_load_balancer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and loadBalancerArn =
         flag "load-balancer-arn" (required string)
           ~doc:"STRING LoadBalancerArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_load_balancer
           (Values.DeleteLoadBalancerInput.make ~loadBalancerArn ())
           (Some Values.DeleteLoadBalancerOutput.to_json)
           (Some Values.DeleteLoadBalancerOutput.error_to_json)])
let delete_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleArn = flag "rule-arn" (required string) ~doc:"STRING RuleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_rule (Values.DeleteRuleInput.make ~ruleArn ())
           (Some Values.DeleteRuleOutput.to_json)
           (Some Values.DeleteRuleOutput.error_to_json)])
let delete_target_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetGroupArn =
         flag "target-group-arn" (required string)
           ~doc:"STRING TargetGroupArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_target_group
           (Values.DeleteTargetGroupInput.make ~targetGroupArn ())
           (Some Values.DeleteTargetGroupOutput.to_json)
           (Some Values.DeleteTargetGroupOutput.error_to_json)])
let deregister_targets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetGroupArn =
         flag "target-group-arn" (required string)
           ~doc:"STRING TargetGroupArn"
       and targets =
         flag "targets" (required json_arg) ~doc:"JSON TargetDescriptions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deregister_targets
           (Values.DeregisterTargetsInput.make ~targetGroupArn
              ~targets:(Values.TargetDescriptions.of_json targets) ())
           (Some Values.DeregisterTargetsOutput.to_json)
           (Some Values.DeregisterTargetsOutput.error_to_json)])
let describe_account_limits =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_account_limits
           (Values.DescribeAccountLimitsInput.make ?marker ?pageSize ())
           (Some Values.DescribeAccountLimitsOutput.to_json)
           (Some Values.DescribeAccountLimitsOutput.error_to_json)])
let describe_listener_certificates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and listenerArn =
         flag "listener-arn" (required string) ~doc:"STRING ListenerArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_listener_certificates
           (Values.DescribeListenerCertificatesInput.make ?marker ?pageSize
              ~listenerArn ())
           (Some Values.DescribeListenerCertificatesOutput.to_json)
           (Some Values.DescribeListenerCertificatesOutput.error_to_json)])
let describe_listeners =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and loadBalancerArn =
         flag "load-balancer-arn" (optional string)
           ~doc:"STRING LoadBalancerArn"
       and listenerArns =
         flag "listener-arns" (optional json_arg) ~doc:"JSON ListenerArns"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_listeners
           (Values.DescribeListenersInput.make ?loadBalancerArn
              ?listenerArns:(Option.map ~f:Values.ListenerArns.of_json
                               listenerArns) ?marker ?pageSize ())
           (Some Values.DescribeListenersOutput.to_json)
           (Some Values.DescribeListenersOutput.error_to_json)])
let describe_load_balancer_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and loadBalancerArn =
         flag "load-balancer-arn" (required string)
           ~doc:"STRING LoadBalancerArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_load_balancer_attributes
           (Values.DescribeLoadBalancerAttributesInput.make ~loadBalancerArn
              ()) (Some Values.DescribeLoadBalancerAttributesOutput.to_json)
           (Some Values.DescribeLoadBalancerAttributesOutput.error_to_json)])
let describe_load_balancers =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and loadBalancerArns =
         flag "load-balancer-arns" (optional json_arg)
           ~doc:"JSON LoadBalancerArns"
       and names =
         flag "names" (optional json_arg) ~doc:"JSON LoadBalancerNames"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_load_balancers
           (Values.DescribeLoadBalancersInput.make
              ?loadBalancerArns:(Option.map
                                   ~f:Values.LoadBalancerArns.of_json
                                   loadBalancerArns)
              ?names:(Option.map ~f:Values.LoadBalancerNames.of_json names)
              ?marker ?pageSize ())
           (Some Values.DescribeLoadBalancersOutput.to_json)
           (Some Values.DescribeLoadBalancersOutput.error_to_json)])
let describe_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and listenerArn =
         flag "listener-arn" (optional string) ~doc:"STRING ListenerArn"
       and ruleArns =
         flag "rule-arns" (optional json_arg) ~doc:"JSON RuleArns"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_rules
           (Values.DescribeRulesInput.make ?listenerArn
              ?ruleArns:(Option.map ~f:Values.RuleArns.of_json ruleArns)
              ?marker ?pageSize ()) (Some Values.DescribeRulesOutput.to_json)
           (Some Values.DescribeRulesOutput.error_to_json)])
let describe_s_s_l_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and names =
         flag "names" (optional json_arg) ~doc:"JSON SslPolicyNames"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize"
       and loadBalancerType =
         flag "load-balancer-type" (optional json_arg)
           ~doc:"JSON LoadBalancerTypeEnum" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_s_s_l_policies
           (Values.DescribeSSLPoliciesInput.make
              ?names:(Option.map ~f:Values.SslPolicyNames.of_json names)
              ?marker ?pageSize
              ?loadBalancerType:(Option.map
                                   ~f:Values.LoadBalancerTypeEnum.of_json
                                   loadBalancerType) ())
           (Some Values.DescribeSSLPoliciesOutput.to_json)
           (Some Values.DescribeSSLPoliciesOutput.error_to_json)])
let describe_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArns =
         flag "resource-arns" (required json_arg) ~doc:"JSON ResourceArns" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_tags
           (Values.DescribeTagsInput.make
              ~resourceArns:(Values.ResourceArns.of_json resourceArns) ())
           (Some Values.DescribeTagsOutput.to_json)
           (Some Values.DescribeTagsOutput.error_to_json)])
let describe_target_group_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetGroupArn =
         flag "target-group-arn" (required string)
           ~doc:"STRING TargetGroupArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_target_group_attributes
           (Values.DescribeTargetGroupAttributesInput.make ~targetGroupArn ())
           (Some Values.DescribeTargetGroupAttributesOutput.to_json)
           (Some Values.DescribeTargetGroupAttributesOutput.error_to_json)])
let describe_target_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and loadBalancerArn =
         flag "load-balancer-arn" (optional string)
           ~doc:"STRING LoadBalancerArn"
       and targetGroupArns =
         flag "target-group-arns" (optional json_arg)
           ~doc:"JSON TargetGroupArns"
       and names =
         flag "names" (optional json_arg) ~doc:"JSON TargetGroupNames"
       and marker = flag "marker" (optional string) ~doc:"STRING Marker"
       and pageSize = flag "page-size" (optional int) ~doc:"INT PageSize" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_target_groups
           (Values.DescribeTargetGroupsInput.make ?loadBalancerArn
              ?targetGroupArns:(Option.map ~f:Values.TargetGroupArns.of_json
                                  targetGroupArns)
              ?names:(Option.map ~f:Values.TargetGroupNames.of_json names)
              ?marker ?pageSize ())
           (Some Values.DescribeTargetGroupsOutput.to_json)
           (Some Values.DescribeTargetGroupsOutput.error_to_json)])
let describe_target_health =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targets =
         flag "targets" (optional json_arg) ~doc:"JSON TargetDescriptions"
       and targetGroupArn =
         flag "target-group-arn" (required string)
           ~doc:"STRING TargetGroupArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_target_health
           (Values.DescribeTargetHealthInput.make
              ?targets:(Option.map ~f:Values.TargetDescriptions.of_json
                          targets) ~targetGroupArn ())
           (Some Values.DescribeTargetHealthOutput.to_json)
           (Some Values.DescribeTargetHealthOutput.error_to_json)])
let modify_listener =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and port = flag "port" (optional int) ~doc:"INT Port"
       and protocol =
         flag "protocol" (optional json_arg) ~doc:"JSON ProtocolEnum"
       and sslPolicy =
         flag "ssl-policy" (optional string) ~doc:"STRING SslPolicyName"
       and certificates =
         flag "certificates" (optional json_arg) ~doc:"JSON CertificateList"
       and defaultActions =
         flag "default-actions" (optional json_arg) ~doc:"JSON Actions"
       and alpnPolicy =
         flag "alpn-policy" (optional json_arg) ~doc:"JSON AlpnPolicyName"
       and listenerArn =
         flag "listener-arn" (required string) ~doc:"STRING ListenerArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_listener
           (Values.ModifyListenerInput.make ?port
              ?protocol:(Option.map ~f:Values.ProtocolEnum.of_json protocol)
              ?sslPolicy
              ?certificates:(Option.map ~f:Values.CertificateList.of_json
                               certificates)
              ?defaultActions:(Option.map ~f:Values.Actions.of_json
                                 defaultActions)
              ?alpnPolicy:(Option.map ~f:Values.AlpnPolicyName.of_json
                             alpnPolicy) ~listenerArn ())
           (Some Values.ModifyListenerOutput.to_json)
           (Some Values.ModifyListenerOutput.error_to_json)])
let modify_load_balancer_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and loadBalancerArn =
         flag "load-balancer-arn" (required string)
           ~doc:"STRING LoadBalancerArn"
       and attributes =
         flag "attributes" (required json_arg)
           ~doc:"JSON LoadBalancerAttributes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_load_balancer_attributes
           (Values.ModifyLoadBalancerAttributesInput.make ~loadBalancerArn
              ~attributes:(Values.LoadBalancerAttributes.of_json attributes)
              ()) (Some Values.ModifyLoadBalancerAttributesOutput.to_json)
           (Some Values.ModifyLoadBalancerAttributesOutput.error_to_json)])
let modify_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and conditions =
         flag "conditions" (optional json_arg) ~doc:"JSON RuleConditionList"
       and actions = flag "actions" (optional json_arg) ~doc:"JSON Actions"
       and ruleArn = flag "rule-arn" (required string) ~doc:"STRING RuleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_rule
           (Values.ModifyRuleInput.make
              ?conditions:(Option.map ~f:Values.RuleConditionList.of_json
                             conditions)
              ?actions:(Option.map ~f:Values.Actions.of_json actions)
              ~ruleArn ()) (Some Values.ModifyRuleOutput.to_json)
           (Some Values.ModifyRuleOutput.error_to_json)])
let modify_target_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and healthCheckProtocol =
         flag "health-check-protocol" (optional json_arg)
           ~doc:"JSON ProtocolEnum"
       and healthCheckPort =
         flag "health-check-port" (optional string)
           ~doc:"STRING HealthCheckPort"
       and healthCheckPath =
         flag "health-check-path" (optional string) ~doc:"STRING Path"
       and healthCheckEnabled =
         flag "health-check-enabled" (optional bool)
           ~doc:"BOOL HealthCheckEnabled"
       and healthCheckIntervalSeconds =
         flag "health-check-interval-seconds" (optional int)
           ~doc:"INT HealthCheckIntervalSeconds"
       and healthCheckTimeoutSeconds =
         flag "health-check-timeout-seconds" (optional int)
           ~doc:"INT HealthCheckTimeoutSeconds"
       and healthyThresholdCount =
         flag "healthy-threshold-count" (optional int)
           ~doc:"INT HealthCheckThresholdCount"
       and unhealthyThresholdCount =
         flag "unhealthy-threshold-count" (optional int)
           ~doc:"INT HealthCheckThresholdCount"
       and matcher = flag "matcher" (optional json_arg) ~doc:"JSON Matcher"
       and targetGroupArn =
         flag "target-group-arn" (required string)
           ~doc:"STRING TargetGroupArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_target_group
           (Values.ModifyTargetGroupInput.make
              ?healthCheckProtocol:(Option.map ~f:Values.ProtocolEnum.of_json
                                      healthCheckProtocol) ?healthCheckPort
              ?healthCheckPath ?healthCheckEnabled
              ?healthCheckIntervalSeconds ?healthCheckTimeoutSeconds
              ?healthyThresholdCount ?unhealthyThresholdCount
              ?matcher:(Option.map ~f:Values.Matcher.of_json matcher)
              ~targetGroupArn ())
           (Some Values.ModifyTargetGroupOutput.to_json)
           (Some Values.ModifyTargetGroupOutput.error_to_json)])
let modify_target_group_attributes =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetGroupArn =
         flag "target-group-arn" (required string)
           ~doc:"STRING TargetGroupArn"
       and attributes =
         flag "attributes" (required json_arg)
           ~doc:"JSON TargetGroupAttributes" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.modify_target_group_attributes
           (Values.ModifyTargetGroupAttributesInput.make ~targetGroupArn
              ~attributes:(Values.TargetGroupAttributes.of_json attributes)
              ()) (Some Values.ModifyTargetGroupAttributesOutput.to_json)
           (Some Values.ModifyTargetGroupAttributesOutput.error_to_json)])
let register_targets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and targetGroupArn =
         flag "target-group-arn" (required string)
           ~doc:"STRING TargetGroupArn"
       and targets =
         flag "targets" (required json_arg) ~doc:"JSON TargetDescriptions" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.register_targets
           (Values.RegisterTargetsInput.make ~targetGroupArn
              ~targets:(Values.TargetDescriptions.of_json targets) ())
           (Some Values.RegisterTargetsOutput.to_json)
           (Some Values.RegisterTargetsOutput.error_to_json)])
let remove_listener_certificates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and listenerArn =
         flag "listener-arn" (required string) ~doc:"STRING ListenerArn"
       and certificates =
         flag "certificates" (required json_arg) ~doc:"JSON CertificateList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_listener_certificates
           (Values.RemoveListenerCertificatesInput.make ~listenerArn
              ~certificates:(Values.CertificateList.of_json certificates) ())
           (Some Values.RemoveListenerCertificatesOutput.to_json)
           (Some Values.RemoveListenerCertificatesOutput.error_to_json)])
let remove_tags =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArns =
         flag "resource-arns" (required json_arg) ~doc:"JSON ResourceArns"
       and tagKeys = flag "tag-keys" (required json_arg) ~doc:"JSON TagKeys" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.remove_tags
           (Values.RemoveTagsInput.make
              ~resourceArns:(Values.ResourceArns.of_json resourceArns)
              ~tagKeys:(Values.TagKeys.of_json tagKeys) ())
           (Some Values.RemoveTagsOutput.to_json)
           (Some Values.RemoveTagsOutput.error_to_json)])
let set_ip_address_type =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and loadBalancerArn =
         flag "load-balancer-arn" (required string)
           ~doc:"STRING LoadBalancerArn"
       and ipAddressType =
         flag "ip-address-type" (required json_arg) ~doc:"JSON IpAddressType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_ip_address_type
           (Values.SetIpAddressTypeInput.make ~loadBalancerArn
              ~ipAddressType:(Values.IpAddressType.of_json ipAddressType) ())
           (Some Values.SetIpAddressTypeOutput.to_json)
           (Some Values.SetIpAddressTypeOutput.error_to_json)])
let set_rule_priorities =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and rulePriorities =
         flag "rule-priorities" (required json_arg)
           ~doc:"JSON RulePriorityList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_rule_priorities
           (Values.SetRulePrioritiesInput.make
              ~rulePriorities:(Values.RulePriorityList.of_json rulePriorities)
              ()) (Some Values.SetRulePrioritiesOutput.to_json)
           (Some Values.SetRulePrioritiesOutput.error_to_json)])
let set_security_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and loadBalancerArn =
         flag "load-balancer-arn" (required string)
           ~doc:"STRING LoadBalancerArn"
       and securityGroups =
         flag "security-groups" (required json_arg)
           ~doc:"JSON SecurityGroups" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_security_groups
           (Values.SetSecurityGroupsInput.make ~loadBalancerArn
              ~securityGroups:(Values.SecurityGroups.of_json securityGroups)
              ()) (Some Values.SetSecurityGroupsOutput.to_json)
           (Some Values.SetSecurityGroupsOutput.error_to_json)])
let set_subnets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and subnets = flag "subnets" (optional json_arg) ~doc:"JSON Subnets"
       and subnetMappings =
         flag "subnet-mappings" (optional json_arg)
           ~doc:"JSON SubnetMappings"
       and ipAddressType =
         flag "ip-address-type" (optional json_arg) ~doc:"JSON IpAddressType"
       and loadBalancerArn =
         flag "load-balancer-arn" (required string)
           ~doc:"STRING LoadBalancerArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.set_subnets
           (Values.SetSubnetsInput.make
              ?subnets:(Option.map ~f:Values.Subnets.of_json subnets)
              ?subnetMappings:(Option.map ~f:Values.SubnetMappings.of_json
                                 subnetMappings)
              ?ipAddressType:(Option.map ~f:Values.IpAddressType.of_json
                                ipAddressType) ~loadBalancerArn ())
           (Some Values.SetSubnetsOutput.to_json)
           (Some Values.SetSubnetsOutput.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("add-listener-certificates", add_listener_certificates);
    ("add-tags", add_tags);
    ("create-listener", create_listener);
    ("create-load-balancer", create_load_balancer);
    ("create-rule", create_rule);
    ("create-target-group", create_target_group);
    ("delete-listener", delete_listener);
    ("delete-load-balancer", delete_load_balancer);
    ("delete-rule", delete_rule);
    ("delete-target-group", delete_target_group);
    ("deregister-targets", deregister_targets);
    ("describe-account-limits", describe_account_limits);
    ("describe-listener-certificates", describe_listener_certificates);
    ("describe-listeners", describe_listeners);
    ("describe-load-balancer-attributes", describe_load_balancer_attributes);
    ("describe-load-balancers", describe_load_balancers);
    ("describe-rules", describe_rules);
    ("describe-s-s-l-policies", describe_s_s_l_policies);
    ("describe-tags", describe_tags);
    ("describe-target-group-attributes", describe_target_group_attributes);
    ("describe-target-groups", describe_target_groups);
    ("describe-target-health", describe_target_health);
    ("modify-listener", modify_listener);
    ("modify-load-balancer-attributes", modify_load_balancer_attributes);
    ("modify-rule", modify_rule);
    ("modify-target-group", modify_target_group);
    ("modify-target-group-attributes", modify_target_group_attributes);
    ("register-targets", register_targets);
    ("remove-listener-certificates", remove_listener_certificates);
    ("remove-tags", remove_tags);
    ("set-ip-address-type", set_ip_address_type);
    ("set-rule-priorities", set_rule_priorities);
    ("set-security-groups", set_security_groups);
    ("set-subnets", set_subnets)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match err with
                 | `Transport err ->
                     let msg =
                       match err with
                       | `Bad_response (r : Awso.Http.Io.Error.bad_response)
                           ->
                           sprintf "Bad response: code=%d body=%s" r.code
                             r.body
                       | `Too_many_redirects -> "Too many redirects" in
                     failwithf "Transport error: %s" msg ()
                 | `AWS err ->
                     (match error_to_json with
                      | None ->
                          failwithf
                            "endpoint error, but no error values defined in boto"
                            ()
                      | Some to_json ->
                          let s = (err |> to_json) |> Yojson.Safe.to_string in
                          failwithf "AWS error: %s" s ()))
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let cancel_job_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ResourceIdString"
       and virtualClusterId =
         flag "virtual-cluster-id" (required string)
           ~doc:"STRING ResourceIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_job_run
           (Values.CancelJobRunRequest.make ~id ~virtualClusterId ())
           (Some Values.CancelJobRunResponse.to_json)
           (Some Values.CancelJobRunResponse.error_to_json)])
let create_managed_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and certificateArn =
         flag "certificate-arn" (optional string) ~doc:"STRING ACMCertArn"
       and configurationOverrides =
         flag "configuration-overrides" (optional json_arg)
           ~doc:"JSON ConfigurationOverrides"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name =
         flag "name" (required string) ~doc:"STRING ResourceNameString"
       and virtualClusterId =
         flag "virtual-cluster-id" (required string)
           ~doc:"STRING ResourceIdString"
       and type_ = flag "type-" (required string) ~doc:"STRING EndpointType"
       and releaseLabel =
         flag "release-label" (required string) ~doc:"STRING ReleaseLabel"
       and executionRoleArn =
         flag "execution-role-arn" (required string) ~doc:"STRING IAMRoleArn"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_managed_endpoint
           (Values.CreateManagedEndpointRequest.make ?certificateArn
              ?configurationOverrides:(Option.map
                                         ~f:Values.ConfigurationOverrides.of_json
                                         configurationOverrides)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name
              ~virtualClusterId ~type_ ~releaseLabel ~executionRoleArn
              ~clientToken ())
           (Some Values.CreateManagedEndpointResponse.to_json)
           (Some Values.CreateManagedEndpointResponse.error_to_json)])
let create_virtual_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and name =
         flag "name" (required string) ~doc:"STRING ResourceNameString"
       and containerProvider =
         flag "container-provider" (required json_arg)
           ~doc:"JSON ContainerProvider"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_virtual_cluster
           (Values.CreateVirtualClusterRequest.make
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ~name
              ~containerProvider:(Values.ContainerProvider.of_json
                                    containerProvider) ~clientToken ())
           (Some Values.CreateVirtualClusterResponse.to_json)
           (Some Values.CreateVirtualClusterResponse.error_to_json)])
let delete_managed_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ResourceIdString"
       and virtualClusterId =
         flag "virtual-cluster-id" (required string)
           ~doc:"STRING ResourceIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_managed_endpoint
           (Values.DeleteManagedEndpointRequest.make ~id ~virtualClusterId ())
           (Some Values.DeleteManagedEndpointResponse.to_json)
           (Some Values.DeleteManagedEndpointResponse.error_to_json)])
let delete_virtual_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ResourceIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_virtual_cluster
           (Values.DeleteVirtualClusterRequest.make ~id ())
           (Some Values.DeleteVirtualClusterResponse.to_json)
           (Some Values.DeleteVirtualClusterResponse.error_to_json)])
let describe_job_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ResourceIdString"
       and virtualClusterId =
         flag "virtual-cluster-id" (required string)
           ~doc:"STRING ResourceIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_job_run
           (Values.DescribeJobRunRequest.make ~id ~virtualClusterId ())
           (Some Values.DescribeJobRunResponse.to_json)
           (Some Values.DescribeJobRunResponse.error_to_json)])
let describe_managed_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ResourceIdString"
       and virtualClusterId =
         flag "virtual-cluster-id" (required string)
           ~doc:"STRING ResourceIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_managed_endpoint
           (Values.DescribeManagedEndpointRequest.make ~id ~virtualClusterId
              ()) (Some Values.DescribeManagedEndpointResponse.to_json)
           (Some Values.DescribeManagedEndpointResponse.error_to_json)])
let describe_virtual_cluster =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ResourceIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_virtual_cluster
           (Values.DescribeVirtualClusterRequest.make ~id ())
           (Some Values.DescribeVirtualClusterResponse.to_json)
           (Some Values.DescribeVirtualClusterResponse.error_to_json)])
let list_job_runs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and createdBefore =
         flag "created-before" (optional json_arg) ~doc:"JSON Date"
       and createdAfter =
         flag "created-after" (optional json_arg) ~doc:"JSON Date"
       and name =
         flag "name" (optional string) ~doc:"STRING ResourceNameString"
       and states =
         flag "states" (optional json_arg) ~doc:"JSON JobRunStates"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT JavaInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and virtualClusterId =
         flag "virtual-cluster-id" (required string)
           ~doc:"STRING ResourceIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_job_runs
           (Values.ListJobRunsRequest.make
              ?createdBefore:(Option.map ~f:Values.Date.of_json createdBefore)
              ?createdAfter:(Option.map ~f:Values.Date.of_json createdAfter)
              ?name
              ?states:(Option.map ~f:Values.JobRunStates.of_json states)
              ?maxResults ?nextToken ~virtualClusterId ())
           (Some Values.ListJobRunsResponse.to_json)
           (Some Values.ListJobRunsResponse.error_to_json)])
let list_managed_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and createdBefore =
         flag "created-before" (optional json_arg) ~doc:"JSON Date"
       and createdAfter =
         flag "created-after" (optional json_arg) ~doc:"JSON Date"
       and types = flag "types" (optional json_arg) ~doc:"JSON EndpointTypes"
       and states =
         flag "states" (optional json_arg) ~doc:"JSON EndpointStates"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT JavaInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and virtualClusterId =
         flag "virtual-cluster-id" (required string)
           ~doc:"STRING ResourceIdString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_managed_endpoints
           (Values.ListManagedEndpointsRequest.make
              ?createdBefore:(Option.map ~f:Values.Date.of_json createdBefore)
              ?createdAfter:(Option.map ~f:Values.Date.of_json createdAfter)
              ?types:(Option.map ~f:Values.EndpointTypes.of_json types)
              ?states:(Option.map ~f:Values.EndpointStates.of_json states)
              ?maxResults ?nextToken ~virtualClusterId ())
           (Some Values.ListManagedEndpointsResponse.to_json)
           (Some Values.ListManagedEndpointsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING RsiArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_virtual_clusters =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and containerProviderId =
         flag "container-provider-id" (optional string)
           ~doc:"STRING String1024"
       and containerProviderType =
         flag "container-provider-type" (optional json_arg)
           ~doc:"JSON ContainerProviderType"
       and createdAfter =
         flag "created-after" (optional json_arg) ~doc:"JSON Date"
       and createdBefore =
         flag "created-before" (optional json_arg) ~doc:"JSON Date"
       and states =
         flag "states" (optional json_arg) ~doc:"JSON VirtualClusterStates"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT JavaInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_virtual_clusters
           (Values.ListVirtualClustersRequest.make ?containerProviderId
              ?containerProviderType:(Option.map
                                        ~f:Values.ContainerProviderType.of_json
                                        containerProviderType)
              ?createdAfter:(Option.map ~f:Values.Date.of_json createdAfter)
              ?createdBefore:(Option.map ~f:Values.Date.of_json createdBefore)
              ?states:(Option.map ~f:Values.VirtualClusterStates.of_json
                         states) ?maxResults ?nextToken ())
           (Some Values.ListVirtualClustersResponse.to_json)
           (Some Values.ListVirtualClustersResponse.error_to_json)])
let start_job_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name =
         flag "name" (optional string) ~doc:"STRING ResourceNameString"
       and configurationOverrides =
         flag "configuration-overrides" (optional json_arg)
           ~doc:"JSON ConfigurationOverrides"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and virtualClusterId =
         flag "virtual-cluster-id" (required string)
           ~doc:"STRING ResourceIdString"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken"
       and executionRoleArn =
         flag "execution-role-arn" (required string) ~doc:"STRING IAMRoleArn"
       and releaseLabel =
         flag "release-label" (required string) ~doc:"STRING ReleaseLabel"
       and jobDriver =
         flag "job-driver" (required json_arg) ~doc:"JSON JobDriver" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_job_run
           (Values.StartJobRunRequest.make ?name
              ?configurationOverrides:(Option.map
                                         ~f:Values.ConfigurationOverrides.of_json
                                         configurationOverrides)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ~virtualClusterId ~clientToken ~executionRoleArn ~releaseLabel
              ~jobDriver:(Values.JobDriver.of_json jobDriver) ())
           (Some Values.StartJobRunResponse.to_json)
           (Some Values.StartJobRunResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING RsiArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING RsiArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso_codegen.Service.to_string Values.service) ^ " commands")
    [("cancel-job-run", cancel_job_run);
    ("create-managed-endpoint", create_managed_endpoint);
    ("create-virtual-cluster", create_virtual_cluster);
    ("delete-managed-endpoint", delete_managed_endpoint);
    ("delete-virtual-cluster", delete_virtual_cluster);
    ("describe-job-run", describe_job_run);
    ("describe-managed-endpoint", describe_managed_endpoint);
    ("describe-virtual-cluster", describe_virtual_cluster);
    ("list-job-runs", list_job_runs);
    ("list-managed-endpoints", list_managed_endpoints);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-virtual-clusters", list_virtual_clusters);
    ("start-job-run", start_job_run);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource)]
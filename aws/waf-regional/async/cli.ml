(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_web_a_c_l =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and webACLId =
         flag "web-a-c-l-id" (required string) ~doc:"STRING ResourceId"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_web_a_c_l
           (Values.AssociateWebACLRequest.make ~webACLId ~resourceArn ())
           (Some Values.AssociateWebACLResponse.to_json)
           (Some Values.AssociateWebACLResponse.error_to_json)])
let create_byte_match_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_byte_match_set
           (Values.CreateByteMatchSetRequest.make ~name ~changeToken ())
           (Some Values.CreateByteMatchSetResponse.to_json)
           (Some Values.CreateByteMatchSetResponse.error_to_json)])
let create_geo_match_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_geo_match_set
           (Values.CreateGeoMatchSetRequest.make ~name ~changeToken ())
           (Some Values.CreateGeoMatchSetResponse.to_json)
           (Some Values.CreateGeoMatchSetResponse.error_to_json)])
let create_i_p_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_i_p_set
           (Values.CreateIPSetRequest.make ~name ~changeToken ())
           (Some Values.CreateIPSetResponse.to_json)
           (Some Values.CreateIPSetResponse.error_to_json)])
let create_rate_based_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and metricName =
         flag "metric-name" (required string) ~doc:"STRING MetricName"
       and rateKey = flag "rate-key" (required json_arg) ~doc:"JSON RateKey"
       and rateLimit =
         flag "rate-limit" (required json_arg) ~doc:"JSON RateLimit"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_rate_based_rule
           (Values.CreateRateBasedRuleRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name
              ~metricName ~rateKey:(Values.RateKey.of_json rateKey)
              ~rateLimit:(Values.RateLimit.of_json rateLimit) ~changeToken ())
           (Some Values.CreateRateBasedRuleResponse.to_json)
           (Some Values.CreateRateBasedRuleResponse.error_to_json)])
let create_regex_match_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_regex_match_set
           (Values.CreateRegexMatchSetRequest.make ~name ~changeToken ())
           (Some Values.CreateRegexMatchSetResponse.to_json)
           (Some Values.CreateRegexMatchSetResponse.error_to_json)])
let create_regex_pattern_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_regex_pattern_set
           (Values.CreateRegexPatternSetRequest.make ~name ~changeToken ())
           (Some Values.CreateRegexPatternSetResponse.to_json)
           (Some Values.CreateRegexPatternSetResponse.error_to_json)])
let create_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and metricName =
         flag "metric-name" (required string) ~doc:"STRING MetricName"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_rule
           (Values.CreateRuleRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name
              ~metricName ~changeToken ())
           (Some Values.CreateRuleResponse.to_json)
           (Some Values.CreateRuleResponse.error_to_json)])
let create_rule_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and metricName =
         flag "metric-name" (required string) ~doc:"STRING MetricName"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_rule_group
           (Values.CreateRuleGroupRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name
              ~metricName ~changeToken ())
           (Some Values.CreateRuleGroupResponse.to_json)
           (Some Values.CreateRuleGroupResponse.error_to_json)])
let create_size_constraint_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_size_constraint_set
           (Values.CreateSizeConstraintSetRequest.make ~name ~changeToken ())
           (Some Values.CreateSizeConstraintSetResponse.to_json)
           (Some Values.CreateSizeConstraintSetResponse.error_to_json)])
let create_sql_injection_match_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_sql_injection_match_set
           (Values.CreateSqlInjectionMatchSetRequest.make ~name ~changeToken
              ()) (Some Values.CreateSqlInjectionMatchSetResponse.to_json)
           (Some Values.CreateSqlInjectionMatchSetResponse.error_to_json)])
let create_web_a_c_l =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagList"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and metricName =
         flag "metric-name" (required string) ~doc:"STRING MetricName"
       and defaultAction =
         flag "default-action" (required json_arg) ~doc:"JSON WafAction"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_web_a_c_l
           (Values.CreateWebACLRequest.make
              ?tags:(Option.map ~f:Values.TagList.of_json tags) ~name
              ~metricName
              ~defaultAction:(Values.WafAction.of_json defaultAction)
              ~changeToken ()) (Some Values.CreateWebACLResponse.to_json)
           (Some Values.CreateWebACLResponse.error_to_json)])
let create_web_a_c_l_migration_stack =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and webACLId =
         flag "web-a-c-l-id" (required string) ~doc:"STRING ResourceId"
       and s3BucketName =
         flag "s3-bucket-name" (required string) ~doc:"STRING S3BucketName"
       and ignoreUnsupportedType =
         flag "ignore-unsupported-type" (required bool)
           ~doc:"BOOL IgnoreUnsupportedType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_web_a_c_l_migration_stack
           (Values.CreateWebACLMigrationStackRequest.make ~webACLId
              ~s3BucketName ~ignoreUnsupportedType ())
           (Some Values.CreateWebACLMigrationStackResponse.to_json)
           (Some Values.CreateWebACLMigrationStackResponse.error_to_json)])
let create_xss_match_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING ResourceName"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_xss_match_set
           (Values.CreateXssMatchSetRequest.make ~name ~changeToken ())
           (Some Values.CreateXssMatchSetResponse.to_json)
           (Some Values.CreateXssMatchSetResponse.error_to_json)])
let delete_byte_match_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and byteMatchSetId =
         flag "byte-match-set-id" (required string) ~doc:"STRING ResourceId"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_byte_match_set
           (Values.DeleteByteMatchSetRequest.make ~byteMatchSetId
              ~changeToken ())
           (Some Values.DeleteByteMatchSetResponse.to_json)
           (Some Values.DeleteByteMatchSetResponse.error_to_json)])
let delete_geo_match_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and geoMatchSetId =
         flag "geo-match-set-id" (required string) ~doc:"STRING ResourceId"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_geo_match_set
           (Values.DeleteGeoMatchSetRequest.make ~geoMatchSetId ~changeToken
              ()) (Some Values.DeleteGeoMatchSetResponse.to_json)
           (Some Values.DeleteGeoMatchSetResponse.error_to_json)])
let delete_i_p_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and iPSetId =
         flag "i-p-set-id" (required string) ~doc:"STRING ResourceId"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_i_p_set
           (Values.DeleteIPSetRequest.make ~iPSetId ~changeToken ())
           (Some Values.DeleteIPSetResponse.to_json)
           (Some Values.DeleteIPSetResponse.error_to_json)])
let delete_logging_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_logging_configuration
           (Values.DeleteLoggingConfigurationRequest.make ~resourceArn ())
           (Some Values.DeleteLoggingConfigurationResponse.to_json)
           (Some Values.DeleteLoggingConfigurationResponse.error_to_json)])
let delete_permission_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_permission_policy
           (Values.DeletePermissionPolicyRequest.make ~resourceArn ())
           (Some Values.DeletePermissionPolicyResponse.to_json)
           (Some Values.DeletePermissionPolicyResponse.error_to_json)])
let delete_rate_based_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleId = flag "rule-id" (required string) ~doc:"STRING ResourceId"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_rate_based_rule
           (Values.DeleteRateBasedRuleRequest.make ~ruleId ~changeToken ())
           (Some Values.DeleteRateBasedRuleResponse.to_json)
           (Some Values.DeleteRateBasedRuleResponse.error_to_json)])
let delete_regex_match_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and regexMatchSetId =
         flag "regex-match-set-id" (required string) ~doc:"STRING ResourceId"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_regex_match_set
           (Values.DeleteRegexMatchSetRequest.make ~regexMatchSetId
              ~changeToken ())
           (Some Values.DeleteRegexMatchSetResponse.to_json)
           (Some Values.DeleteRegexMatchSetResponse.error_to_json)])
let delete_regex_pattern_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and regexPatternSetId =
         flag "regex-pattern-set-id" (required string)
           ~doc:"STRING ResourceId"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_regex_pattern_set
           (Values.DeleteRegexPatternSetRequest.make ~regexPatternSetId
              ~changeToken ())
           (Some Values.DeleteRegexPatternSetResponse.to_json)
           (Some Values.DeleteRegexPatternSetResponse.error_to_json)])
let delete_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleId = flag "rule-id" (required string) ~doc:"STRING ResourceId"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_rule
           (Values.DeleteRuleRequest.make ~ruleId ~changeToken ())
           (Some Values.DeleteRuleResponse.to_json)
           (Some Values.DeleteRuleResponse.error_to_json)])
let delete_rule_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleGroupId =
         flag "rule-group-id" (required string) ~doc:"STRING ResourceId"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_rule_group
           (Values.DeleteRuleGroupRequest.make ~ruleGroupId ~changeToken ())
           (Some Values.DeleteRuleGroupResponse.to_json)
           (Some Values.DeleteRuleGroupResponse.error_to_json)])
let delete_size_constraint_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sizeConstraintSetId =
         flag "size-constraint-set-id" (required string)
           ~doc:"STRING ResourceId"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_size_constraint_set
           (Values.DeleteSizeConstraintSetRequest.make ~sizeConstraintSetId
              ~changeToken ())
           (Some Values.DeleteSizeConstraintSetResponse.to_json)
           (Some Values.DeleteSizeConstraintSetResponse.error_to_json)])
let delete_sql_injection_match_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sqlInjectionMatchSetId =
         flag "sql-injection-match-set-id" (required string)
           ~doc:"STRING ResourceId"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_sql_injection_match_set
           (Values.DeleteSqlInjectionMatchSetRequest.make
              ~sqlInjectionMatchSetId ~changeToken ())
           (Some Values.DeleteSqlInjectionMatchSetResponse.to_json)
           (Some Values.DeleteSqlInjectionMatchSetResponse.error_to_json)])
let delete_web_a_c_l =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and webACLId =
         flag "web-a-c-l-id" (required string) ~doc:"STRING ResourceId"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_web_a_c_l
           (Values.DeleteWebACLRequest.make ~webACLId ~changeToken ())
           (Some Values.DeleteWebACLResponse.to_json)
           (Some Values.DeleteWebACLResponse.error_to_json)])
let delete_xss_match_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and xssMatchSetId =
         flag "xss-match-set-id" (required string) ~doc:"STRING ResourceId"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_xss_match_set
           (Values.DeleteXssMatchSetRequest.make ~xssMatchSetId ~changeToken
              ()) (Some Values.DeleteXssMatchSetResponse.to_json)
           (Some Values.DeleteXssMatchSetResponse.error_to_json)])
let disassociate_web_a_c_l =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_web_a_c_l
           (Values.DisassociateWebACLRequest.make ~resourceArn ())
           (Some Values.DisassociateWebACLResponse.to_json)
           (Some Values.DisassociateWebACLResponse.error_to_json)])
let get_byte_match_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and byteMatchSetId =
         flag "byte-match-set-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_byte_match_set
           (Values.GetByteMatchSetRequest.make ~byteMatchSetId ())
           (Some Values.GetByteMatchSetResponse.to_json)
           (Some Values.GetByteMatchSetResponse.error_to_json)])
let get_change_token =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_change_token (Values.GetChangeTokenRequest.make ())
           (Some Values.GetChangeTokenResponse.to_json)
           (Some Values.GetChangeTokenResponse.error_to_json)])
let get_change_token_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_change_token_status
           (Values.GetChangeTokenStatusRequest.make ~changeToken ())
           (Some Values.GetChangeTokenStatusResponse.to_json)
           (Some Values.GetChangeTokenStatusResponse.error_to_json)])
let get_geo_match_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and geoMatchSetId =
         flag "geo-match-set-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_geo_match_set
           (Values.GetGeoMatchSetRequest.make ~geoMatchSetId ())
           (Some Values.GetGeoMatchSetResponse.to_json)
           (Some Values.GetGeoMatchSetResponse.error_to_json)])
let get_i_p_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and iPSetId =
         flag "i-p-set-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_i_p_set (Values.GetIPSetRequest.make ~iPSetId ())
           (Some Values.GetIPSetResponse.to_json)
           (Some Values.GetIPSetResponse.error_to_json)])
let get_logging_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_logging_configuration
           (Values.GetLoggingConfigurationRequest.make ~resourceArn ())
           (Some Values.GetLoggingConfigurationResponse.to_json)
           (Some Values.GetLoggingConfigurationResponse.error_to_json)])
let get_permission_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_permission_policy
           (Values.GetPermissionPolicyRequest.make ~resourceArn ())
           (Some Values.GetPermissionPolicyResponse.to_json)
           (Some Values.GetPermissionPolicyResponse.error_to_json)])
let get_rate_based_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleId = flag "rule-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_rate_based_rule
           (Values.GetRateBasedRuleRequest.make ~ruleId ())
           (Some Values.GetRateBasedRuleResponse.to_json)
           (Some Values.GetRateBasedRuleResponse.error_to_json)])
let get_rate_based_rule_managed_keys =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextMarker =
         flag "next-marker" (optional string) ~doc:"STRING NextMarker"
       and ruleId = flag "rule-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_rate_based_rule_managed_keys
           (Values.GetRateBasedRuleManagedKeysRequest.make ?nextMarker
              ~ruleId ())
           (Some Values.GetRateBasedRuleManagedKeysResponse.to_json)
           (Some Values.GetRateBasedRuleManagedKeysResponse.error_to_json)])
let get_regex_match_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and regexMatchSetId =
         flag "regex-match-set-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_regex_match_set
           (Values.GetRegexMatchSetRequest.make ~regexMatchSetId ())
           (Some Values.GetRegexMatchSetResponse.to_json)
           (Some Values.GetRegexMatchSetResponse.error_to_json)])
let get_regex_pattern_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and regexPatternSetId =
         flag "regex-pattern-set-id" (required string)
           ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_regex_pattern_set
           (Values.GetRegexPatternSetRequest.make ~regexPatternSetId ())
           (Some Values.GetRegexPatternSetResponse.to_json)
           (Some Values.GetRegexPatternSetResponse.error_to_json)])
let get_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleId = flag "rule-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_rule (Values.GetRuleRequest.make ~ruleId ())
           (Some Values.GetRuleResponse.to_json)
           (Some Values.GetRuleResponse.error_to_json)])
let get_rule_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleGroupId =
         flag "rule-group-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_rule_group
           (Values.GetRuleGroupRequest.make ~ruleGroupId ())
           (Some Values.GetRuleGroupResponse.to_json)
           (Some Values.GetRuleGroupResponse.error_to_json)])
let get_sampled_requests =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and webAclId =
         flag "web-acl-id" (required string) ~doc:"STRING ResourceId"
       and ruleId = flag "rule-id" (required string) ~doc:"STRING ResourceId"
       and timeWindow =
         flag "time-window" (required json_arg) ~doc:"JSON TimeWindow"
       and maxItems =
         flag "max-items" (required json_arg)
           ~doc:"JSON GetSampledRequestsMaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sampled_requests
           (Values.GetSampledRequestsRequest.make ~webAclId ~ruleId
              ~timeWindow:(Values.TimeWindow.of_json timeWindow)
              ~maxItems:(Values.GetSampledRequestsMaxItems.of_json maxItems)
              ()) (Some Values.GetSampledRequestsResponse.to_json)
           (Some Values.GetSampledRequestsResponse.error_to_json)])
let get_size_constraint_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sizeConstraintSetId =
         flag "size-constraint-set-id" (required string)
           ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_size_constraint_set
           (Values.GetSizeConstraintSetRequest.make ~sizeConstraintSetId ())
           (Some Values.GetSizeConstraintSetResponse.to_json)
           (Some Values.GetSizeConstraintSetResponse.error_to_json)])
let get_sql_injection_match_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sqlInjectionMatchSetId =
         flag "sql-injection-match-set-id" (required string)
           ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_sql_injection_match_set
           (Values.GetSqlInjectionMatchSetRequest.make
              ~sqlInjectionMatchSetId ())
           (Some Values.GetSqlInjectionMatchSetResponse.to_json)
           (Some Values.GetSqlInjectionMatchSetResponse.error_to_json)])
let get_web_a_c_l =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and webACLId =
         flag "web-a-c-l-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_web_a_c_l (Values.GetWebACLRequest.make ~webACLId ())
           (Some Values.GetWebACLResponse.to_json)
           (Some Values.GetWebACLResponse.error_to_json)])
let get_web_a_c_l_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_web_a_c_l_for_resource
           (Values.GetWebACLForResourceRequest.make ~resourceArn ())
           (Some Values.GetWebACLForResourceResponse.to_json)
           (Some Values.GetWebACLForResourceResponse.error_to_json)])
let get_xss_match_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and xssMatchSetId =
         flag "xss-match-set-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_xss_match_set
           (Values.GetXssMatchSetRequest.make ~xssMatchSetId ())
           (Some Values.GetXssMatchSetResponse.to_json)
           (Some Values.GetXssMatchSetResponse.error_to_json)])
let list_activated_rules_in_rule_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleGroupId =
         flag "rule-group-id" (optional string) ~doc:"STRING ResourceId"
       and nextMarker =
         flag "next-marker" (optional string) ~doc:"STRING NextMarker"
       and limit = flag "limit" (optional int) ~doc:"INT PaginationLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_activated_rules_in_rule_group
           (Values.ListActivatedRulesInRuleGroupRequest.make ?ruleGroupId
              ?nextMarker ?limit ())
           (Some Values.ListActivatedRulesInRuleGroupResponse.to_json)
           (Some Values.ListActivatedRulesInRuleGroupResponse.error_to_json)])
let list_byte_match_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextMarker =
         flag "next-marker" (optional string) ~doc:"STRING NextMarker"
       and limit = flag "limit" (optional int) ~doc:"INT PaginationLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_byte_match_sets
           (Values.ListByteMatchSetsRequest.make ?nextMarker ?limit ())
           (Some Values.ListByteMatchSetsResponse.to_json)
           (Some Values.ListByteMatchSetsResponse.error_to_json)])
let list_geo_match_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextMarker =
         flag "next-marker" (optional string) ~doc:"STRING NextMarker"
       and limit = flag "limit" (optional int) ~doc:"INT PaginationLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_geo_match_sets
           (Values.ListGeoMatchSetsRequest.make ?nextMarker ?limit ())
           (Some Values.ListGeoMatchSetsResponse.to_json)
           (Some Values.ListGeoMatchSetsResponse.error_to_json)])
let list_i_p_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextMarker =
         flag "next-marker" (optional string) ~doc:"STRING NextMarker"
       and limit = flag "limit" (optional int) ~doc:"INT PaginationLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_i_p_sets
           (Values.ListIPSetsRequest.make ?nextMarker ?limit ())
           (Some Values.ListIPSetsResponse.to_json)
           (Some Values.ListIPSetsResponse.error_to_json)])
let list_logging_configurations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextMarker =
         flag "next-marker" (optional string) ~doc:"STRING NextMarker"
       and limit = flag "limit" (optional int) ~doc:"INT PaginationLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_logging_configurations
           (Values.ListLoggingConfigurationsRequest.make ?nextMarker ?limit
              ()) (Some Values.ListLoggingConfigurationsResponse.to_json)
           (Some Values.ListLoggingConfigurationsResponse.error_to_json)])
let list_rate_based_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextMarker =
         flag "next-marker" (optional string) ~doc:"STRING NextMarker"
       and limit = flag "limit" (optional int) ~doc:"INT PaginationLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_rate_based_rules
           (Values.ListRateBasedRulesRequest.make ?nextMarker ?limit ())
           (Some Values.ListRateBasedRulesResponse.to_json)
           (Some Values.ListRateBasedRulesResponse.error_to_json)])
let list_regex_match_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextMarker =
         flag "next-marker" (optional string) ~doc:"STRING NextMarker"
       and limit = flag "limit" (optional int) ~doc:"INT PaginationLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_regex_match_sets
           (Values.ListRegexMatchSetsRequest.make ?nextMarker ?limit ())
           (Some Values.ListRegexMatchSetsResponse.to_json)
           (Some Values.ListRegexMatchSetsResponse.error_to_json)])
let list_regex_pattern_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextMarker =
         flag "next-marker" (optional string) ~doc:"STRING NextMarker"
       and limit = flag "limit" (optional int) ~doc:"INT PaginationLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_regex_pattern_sets
           (Values.ListRegexPatternSetsRequest.make ?nextMarker ?limit ())
           (Some Values.ListRegexPatternSetsResponse.to_json)
           (Some Values.ListRegexPatternSetsResponse.error_to_json)])
let list_resources_for_web_a_c_l =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceType =
         flag "resource-type" (optional json_arg) ~doc:"JSON ResourceType"
       and webACLId =
         flag "web-a-c-l-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resources_for_web_a_c_l
           (Values.ListResourcesForWebACLRequest.make
              ?resourceType:(Option.map ~f:Values.ResourceType.of_json
                               resourceType) ~webACLId ())
           (Some Values.ListResourcesForWebACLResponse.to_json)
           (Some Values.ListResourcesForWebACLResponse.error_to_json)])
let list_rule_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextMarker =
         flag "next-marker" (optional string) ~doc:"STRING NextMarker"
       and limit = flag "limit" (optional int) ~doc:"INT PaginationLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_rule_groups
           (Values.ListRuleGroupsRequest.make ?nextMarker ?limit ())
           (Some Values.ListRuleGroupsResponse.to_json)
           (Some Values.ListRuleGroupsResponse.error_to_json)])
let list_rules =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextMarker =
         flag "next-marker" (optional string) ~doc:"STRING NextMarker"
       and limit = flag "limit" (optional int) ~doc:"INT PaginationLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_rules (Values.ListRulesRequest.make ?nextMarker ?limit ())
           (Some Values.ListRulesResponse.to_json)
           (Some Values.ListRulesResponse.error_to_json)])
let list_size_constraint_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextMarker =
         flag "next-marker" (optional string) ~doc:"STRING NextMarker"
       and limit = flag "limit" (optional int) ~doc:"INT PaginationLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_size_constraint_sets
           (Values.ListSizeConstraintSetsRequest.make ?nextMarker ?limit ())
           (Some Values.ListSizeConstraintSetsResponse.to_json)
           (Some Values.ListSizeConstraintSetsResponse.error_to_json)])
let list_sql_injection_match_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextMarker =
         flag "next-marker" (optional string) ~doc:"STRING NextMarker"
       and limit = flag "limit" (optional int) ~doc:"INT PaginationLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_sql_injection_match_sets
           (Values.ListSqlInjectionMatchSetsRequest.make ?nextMarker ?limit
              ()) (Some Values.ListSqlInjectionMatchSetsResponse.to_json)
           (Some Values.ListSqlInjectionMatchSetsResponse.error_to_json)])
let list_subscribed_rule_groups =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextMarker =
         flag "next-marker" (optional string) ~doc:"STRING NextMarker"
       and limit = flag "limit" (optional int) ~doc:"INT PaginationLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_subscribed_rule_groups
           (Values.ListSubscribedRuleGroupsRequest.make ?nextMarker ?limit ())
           (Some Values.ListSubscribedRuleGroupsResponse.to_json)
           (Some Values.ListSubscribedRuleGroupsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextMarker =
         flag "next-marker" (optional string) ~doc:"STRING NextMarker"
       and limit = flag "limit" (optional int) ~doc:"INT PaginationLimit"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ?nextMarker ?limit
              ~resourceARN ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_web_a_c_ls =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextMarker =
         flag "next-marker" (optional string) ~doc:"STRING NextMarker"
       and limit = flag "limit" (optional int) ~doc:"INT PaginationLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_web_a_c_ls
           (Values.ListWebACLsRequest.make ?nextMarker ?limit ())
           (Some Values.ListWebACLsResponse.to_json)
           (Some Values.ListWebACLsResponse.error_to_json)])
let list_xss_match_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextMarker =
         flag "next-marker" (optional string) ~doc:"STRING NextMarker"
       and limit = flag "limit" (optional int) ~doc:"INT PaginationLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_xss_match_sets
           (Values.ListXssMatchSetsRequest.make ?nextMarker ?limit ())
           (Some Values.ListXssMatchSetsResponse.to_json)
           (Some Values.ListXssMatchSetsResponse.error_to_json)])
let put_logging_configuration =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and loggingConfiguration =
         flag "logging-configuration" (required json_arg)
           ~doc:"JSON LoggingConfiguration" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_logging_configuration
           (Values.PutLoggingConfigurationRequest.make
              ~loggingConfiguration:(Values.LoggingConfiguration.of_json
                                       loggingConfiguration) ())
           (Some Values.PutLoggingConfigurationResponse.to_json)
           (Some Values.PutLoggingConfigurationResponse.error_to_json)])
let put_permission_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and policy =
         flag "policy" (required string) ~doc:"STRING PolicyString" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_permission_policy
           (Values.PutPermissionPolicyRequest.make ~resourceArn ~policy ())
           (Some Values.PutPermissionPolicyResponse.to_json)
           (Some Values.PutPermissionPolicyResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceARN
              ~tags:(Values.TagList.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceARN =
         flag "resource-a-r-n" (required string) ~doc:"STRING ResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceARN
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_byte_match_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and byteMatchSetId =
         flag "byte-match-set-id" (required string) ~doc:"STRING ResourceId"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken"
       and updates =
         flag "updates" (required json_arg) ~doc:"JSON ByteMatchSetUpdates" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_byte_match_set
           (Values.UpdateByteMatchSetRequest.make ~byteMatchSetId
              ~changeToken
              ~updates:(Values.ByteMatchSetUpdates.of_json updates) ())
           (Some Values.UpdateByteMatchSetResponse.to_json)
           (Some Values.UpdateByteMatchSetResponse.error_to_json)])
let update_geo_match_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and geoMatchSetId =
         flag "geo-match-set-id" (required string) ~doc:"STRING ResourceId"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken"
       and updates =
         flag "updates" (required json_arg) ~doc:"JSON GeoMatchSetUpdates" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_geo_match_set
           (Values.UpdateGeoMatchSetRequest.make ~geoMatchSetId ~changeToken
              ~updates:(Values.GeoMatchSetUpdates.of_json updates) ())
           (Some Values.UpdateGeoMatchSetResponse.to_json)
           (Some Values.UpdateGeoMatchSetResponse.error_to_json)])
let update_i_p_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and iPSetId =
         flag "i-p-set-id" (required string) ~doc:"STRING ResourceId"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken"
       and updates =
         flag "updates" (required json_arg) ~doc:"JSON IPSetUpdates" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_i_p_set
           (Values.UpdateIPSetRequest.make ~iPSetId ~changeToken
              ~updates:(Values.IPSetUpdates.of_json updates) ())
           (Some Values.UpdateIPSetResponse.to_json)
           (Some Values.UpdateIPSetResponse.error_to_json)])
let update_rate_based_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleId = flag "rule-id" (required string) ~doc:"STRING ResourceId"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken"
       and updates =
         flag "updates" (required json_arg) ~doc:"JSON RuleUpdates"
       and rateLimit =
         flag "rate-limit" (required json_arg) ~doc:"JSON RateLimit" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_rate_based_rule
           (Values.UpdateRateBasedRuleRequest.make ~ruleId ~changeToken
              ~updates:(Values.RuleUpdates.of_json updates)
              ~rateLimit:(Values.RateLimit.of_json rateLimit) ())
           (Some Values.UpdateRateBasedRuleResponse.to_json)
           (Some Values.UpdateRateBasedRuleResponse.error_to_json)])
let update_regex_match_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and regexMatchSetId =
         flag "regex-match-set-id" (required string) ~doc:"STRING ResourceId"
       and updates =
         flag "updates" (required json_arg) ~doc:"JSON RegexMatchSetUpdates"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_regex_match_set
           (Values.UpdateRegexMatchSetRequest.make ~regexMatchSetId
              ~updates:(Values.RegexMatchSetUpdates.of_json updates)
              ~changeToken ())
           (Some Values.UpdateRegexMatchSetResponse.to_json)
           (Some Values.UpdateRegexMatchSetResponse.error_to_json)])
let update_regex_pattern_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and regexPatternSetId =
         flag "regex-pattern-set-id" (required string)
           ~doc:"STRING ResourceId"
       and updates =
         flag "updates" (required json_arg)
           ~doc:"JSON RegexPatternSetUpdates"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_regex_pattern_set
           (Values.UpdateRegexPatternSetRequest.make ~regexPatternSetId
              ~updates:(Values.RegexPatternSetUpdates.of_json updates)
              ~changeToken ())
           (Some Values.UpdateRegexPatternSetResponse.to_json)
           (Some Values.UpdateRegexPatternSetResponse.error_to_json)])
let update_rule =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleId = flag "rule-id" (required string) ~doc:"STRING ResourceId"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken"
       and updates =
         flag "updates" (required json_arg) ~doc:"JSON RuleUpdates" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_rule
           (Values.UpdateRuleRequest.make ~ruleId ~changeToken
              ~updates:(Values.RuleUpdates.of_json updates) ())
           (Some Values.UpdateRuleResponse.to_json)
           (Some Values.UpdateRuleResponse.error_to_json)])
let update_rule_group =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and ruleGroupId =
         flag "rule-group-id" (required string) ~doc:"STRING ResourceId"
       and updates =
         flag "updates" (required json_arg) ~doc:"JSON RuleGroupUpdates"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_rule_group
           (Values.UpdateRuleGroupRequest.make ~ruleGroupId
              ~updates:(Values.RuleGroupUpdates.of_json updates) ~changeToken
              ()) (Some Values.UpdateRuleGroupResponse.to_json)
           (Some Values.UpdateRuleGroupResponse.error_to_json)])
let update_size_constraint_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sizeConstraintSetId =
         flag "size-constraint-set-id" (required string)
           ~doc:"STRING ResourceId"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken"
       and updates =
         flag "updates" (required json_arg)
           ~doc:"JSON SizeConstraintSetUpdates" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_size_constraint_set
           (Values.UpdateSizeConstraintSetRequest.make ~sizeConstraintSetId
              ~changeToken
              ~updates:(Values.SizeConstraintSetUpdates.of_json updates) ())
           (Some Values.UpdateSizeConstraintSetResponse.to_json)
           (Some Values.UpdateSizeConstraintSetResponse.error_to_json)])
let update_sql_injection_match_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and sqlInjectionMatchSetId =
         flag "sql-injection-match-set-id" (required string)
           ~doc:"STRING ResourceId"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken"
       and updates =
         flag "updates" (required json_arg)
           ~doc:"JSON SqlInjectionMatchSetUpdates" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_sql_injection_match_set
           (Values.UpdateSqlInjectionMatchSetRequest.make
              ~sqlInjectionMatchSetId ~changeToken
              ~updates:(Values.SqlInjectionMatchSetUpdates.of_json updates)
              ()) (Some Values.UpdateSqlInjectionMatchSetResponse.to_json)
           (Some Values.UpdateSqlInjectionMatchSetResponse.error_to_json)])
let update_web_a_c_l =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and updates =
         flag "updates" (optional json_arg) ~doc:"JSON WebACLUpdates"
       and defaultAction =
         flag "default-action" (optional json_arg) ~doc:"JSON WafAction"
       and webACLId =
         flag "web-a-c-l-id" (required string) ~doc:"STRING ResourceId"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_web_a_c_l
           (Values.UpdateWebACLRequest.make
              ?updates:(Option.map ~f:Values.WebACLUpdates.of_json updates)
              ?defaultAction:(Option.map ~f:Values.WafAction.of_json
                                defaultAction) ~webACLId ~changeToken ())
           (Some Values.UpdateWebACLResponse.to_json)
           (Some Values.UpdateWebACLResponse.error_to_json)])
let update_xss_match_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and xssMatchSetId =
         flag "xss-match-set-id" (required string) ~doc:"STRING ResourceId"
       and changeToken =
         flag "change-token" (required string) ~doc:"STRING ChangeToken"
       and updates =
         flag "updates" (required json_arg) ~doc:"JSON XssMatchSetUpdates" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_xss_match_set
           (Values.UpdateXssMatchSetRequest.make ~xssMatchSetId ~changeToken
              ~updates:(Values.XssMatchSetUpdates.of_json updates) ())
           (Some Values.UpdateXssMatchSetResponse.to_json)
           (Some Values.UpdateXssMatchSetResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-web-a-c-l", associate_web_a_c_l);
    ("create-byte-match-set", create_byte_match_set);
    ("create-geo-match-set", create_geo_match_set);
    ("create-i-p-set", create_i_p_set);
    ("create-rate-based-rule", create_rate_based_rule);
    ("create-regex-match-set", create_regex_match_set);
    ("create-regex-pattern-set", create_regex_pattern_set);
    ("create-rule", create_rule);
    ("create-rule-group", create_rule_group);
    ("create-size-constraint-set", create_size_constraint_set);
    ("create-sql-injection-match-set", create_sql_injection_match_set);
    ("create-web-a-c-l", create_web_a_c_l);
    ("create-web-a-c-l-migration-stack", create_web_a_c_l_migration_stack);
    ("create-xss-match-set", create_xss_match_set);
    ("delete-byte-match-set", delete_byte_match_set);
    ("delete-geo-match-set", delete_geo_match_set);
    ("delete-i-p-set", delete_i_p_set);
    ("delete-logging-configuration", delete_logging_configuration);
    ("delete-permission-policy", delete_permission_policy);
    ("delete-rate-based-rule", delete_rate_based_rule);
    ("delete-regex-match-set", delete_regex_match_set);
    ("delete-regex-pattern-set", delete_regex_pattern_set);
    ("delete-rule", delete_rule);
    ("delete-rule-group", delete_rule_group);
    ("delete-size-constraint-set", delete_size_constraint_set);
    ("delete-sql-injection-match-set", delete_sql_injection_match_set);
    ("delete-web-a-c-l", delete_web_a_c_l);
    ("delete-xss-match-set", delete_xss_match_set);
    ("disassociate-web-a-c-l", disassociate_web_a_c_l);
    ("get-byte-match-set", get_byte_match_set);
    ("get-change-token", get_change_token);
    ("get-change-token-status", get_change_token_status);
    ("get-geo-match-set", get_geo_match_set);
    ("get-i-p-set", get_i_p_set);
    ("get-logging-configuration", get_logging_configuration);
    ("get-permission-policy", get_permission_policy);
    ("get-rate-based-rule", get_rate_based_rule);
    ("get-rate-based-rule-managed-keys", get_rate_based_rule_managed_keys);
    ("get-regex-match-set", get_regex_match_set);
    ("get-regex-pattern-set", get_regex_pattern_set);
    ("get-rule", get_rule);
    ("get-rule-group", get_rule_group);
    ("get-sampled-requests", get_sampled_requests);
    ("get-size-constraint-set", get_size_constraint_set);
    ("get-sql-injection-match-set", get_sql_injection_match_set);
    ("get-web-a-c-l", get_web_a_c_l);
    ("get-web-a-c-l-for-resource", get_web_a_c_l_for_resource);
    ("get-xss-match-set", get_xss_match_set);
    ("list-activated-rules-in-rule-group",
      list_activated_rules_in_rule_group);
    ("list-byte-match-sets", list_byte_match_sets);
    ("list-geo-match-sets", list_geo_match_sets);
    ("list-i-p-sets", list_i_p_sets);
    ("list-logging-configurations", list_logging_configurations);
    ("list-rate-based-rules", list_rate_based_rules);
    ("list-regex-match-sets", list_regex_match_sets);
    ("list-regex-pattern-sets", list_regex_pattern_sets);
    ("list-resources-for-web-a-c-l", list_resources_for_web_a_c_l);
    ("list-rule-groups", list_rule_groups);
    ("list-rules", list_rules);
    ("list-size-constraint-sets", list_size_constraint_sets);
    ("list-sql-injection-match-sets", list_sql_injection_match_sets);
    ("list-subscribed-rule-groups", list_subscribed_rule_groups);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-web-a-c-ls", list_web_a_c_ls);
    ("list-xss-match-sets", list_xss_match_sets);
    ("put-logging-configuration", put_logging_configuration);
    ("put-permission-policy", put_permission_policy);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-byte-match-set", update_byte_match_set);
    ("update-geo-match-set", update_geo_match_set);
    ("update-i-p-set", update_i_p_set);
    ("update-rate-based-rule", update_rate_based_rule);
    ("update-regex-match-set", update_regex_match_set);
    ("update-regex-pattern-set", update_regex_pattern_set);
    ("update-rule", update_rule);
    ("update-rule-group", update_rule_group);
    ("update-size-constraint-set", update_size_constraint_set);
    ("update-sql-injection-match-set", update_sql_injection_match_set);
    ("update-web-a-c-l", update_web_a_c_l);
    ("update-xss-match-set", update_xss_match_set)]
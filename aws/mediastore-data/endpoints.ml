(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso *)
open! Awso_common.Jane_compat
open Values
type ('i, 'o, 'e) t =
  | DeleteObject: (DeleteObjectRequest.t, DeleteObjectResponse.t,
  DeleteObjectResponse.error) t 
  | DescribeObject: (DescribeObjectRequest.t, DescribeObjectResponse.t,
  DescribeObjectResponse.error) t 
  | GetObject: (GetObjectRequest.t, GetObjectResponse.t,
  GetObjectResponse.error) t 
  | ListItems: (ListItemsRequest.t, ListItemsResponse.t,
  ListItemsResponse.error) t 
  | PutObject: (PutObjectRequest.t, PutObjectResponse.t,
  PutObjectResponse.error) t 
let method_of_endpoint : type i o e. (i, o, e) t -> _ =
  function
  | DeleteObject -> `DELETE
  | DescribeObject -> `HEAD
  | GetObject -> `GET
  | ListItems -> `GET
  | PutObject -> `PUT
let uri_of_endpoint : type i o e. (i, o, e) t -> i -> Uri.t =
  ((fun endpoint x ->
      match endpoint with
      | DeleteObject ->
          (Format.kasprintf Uri.of_string) "/%s"
            (PathNaming.to_header x.DeleteObjectRequest.path)
      | DescribeObject ->
          (Format.kasprintf Uri.of_string) "/%s"
            (PathNaming.to_header x.DescribeObjectRequest.path)
      | GetObject ->
          (Format.kasprintf Uri.of_string) "/%s"
            (PathNaming.to_header x.GetObjectRequest.path)
      | ListItems ->
          Uri.add_query_params' ((Format.kasprintf Uri.of_string) "/")
            (List.filter_opt
               [Option.map
                  ~f:(fun v -> ("Path", (ListPathNaming.to_header v))) 
                  x.path;
               Option.map
                 ~f:(fun v -> ("MaxResults", (ListLimit.to_header v)))
                 x.maxResults;
               Option.map
                 ~f:(fun v -> ("NextToken", (PaginationToken.to_header v)))
                 x.nextToken])
      | PutObject ->
          (Format.kasprintf Uri.of_string) "/%s"
            (PathNaming.to_header x.PutObjectRequest.path))
  [@ocaml.warning "-27"])
let to_request (type i) (type o) (type e) (endp : (i, o, e) t) (req : i) =
  let _req = req in
  match endp with
  | DeleteObject -> Awso.Http.Request.make (method_of_endpoint endp)
  | DescribeObject -> Awso.Http.Request.make (method_of_endpoint endp)
  | GetObject ->
      let (headers, body) =
        let headers =
          Some
            ((List.filter_opt
                [Option.map req.GetObjectRequest.range
                   ~f:(fun x -> ("Range", (RangePattern.to_header x)))])
               |> Awso.Http.Headers.of_list) in
        let body =
          Some
            ((`Assoc
                (List.map (List.filter_opt [])
                   ~f:(fun (x, y) ->
                         let value =
                           Awso.Botodata.Json.value_to_json_scalar y in
                         (x, value))))
               |> Yojson.Safe.to_string) in
        (headers, body) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | ListItems ->
      let (headers, body) = (None, None) in
      Awso.Http.Request.make ?headers ?body (method_of_endpoint endp)
  | PutObject ->
      let headers =
        Some
          ((List.filter_opt
              [Option.map req.PutObjectRequest.contentType
                 ~f:(fun x -> ("Content-Type", (ContentType.to_header x)));
              Option.map req.PutObjectRequest.cacheControl
                ~f:(fun x -> ("Cache-Control", (StringPrimitive.to_header x)));
              Option.map req.PutObjectRequest.storageClass
                ~f:(fun x ->
                      ("x-amz-storage-class", (StorageClass.to_header x)));
              Option.map req.PutObjectRequest.uploadAvailability
                ~f:(fun x ->
                      ("x-amz-upload-availability",
                        (UploadAvailability.to_header x)))])
             |> Awso.Http.Headers.of_list) in
      let body = PayloadBlob.to_header req.body in
      Awso.Http.Request.make ?headers ~body (method_of_endpoint endp)
let of_response (type i) (type o) (type e) (endpoint : (i, o, e) t)
  (resp : (Awso.Http.Response.t, Awso.Http.Io.Error.call) result) :
  (o, [ `AWS of e  | `Transport of Awso.Http.Io.Error.call ]) result=
  let handle_error err error_of_json =
    match err with
    | `Too_many_redirects -> Error (`Transport `Too_many_redirects)
    | `Bad_response
        { Awso.Http.Io.Error.code = code; body; x_amzn_error_type } ->
        let generic_error () =
          Error
            (`Transport
               (`Bad_response
                  { Awso.Http.Io.Error.code = code; body; x_amzn_error_type })) in
        (match (x_amzn_error_type, error_of_json,
                 ((code >= 400) && (code <= 599)))
         with
         | (Some error_type, Some error_of_json, true) ->
             let json = Yojson.Safe.from_string body in
             Error (`AWS (error_of_json error_type json))
         | (None, Some error_of_json, true) ->
             (try
                let json = Yojson.Safe.from_string body in
                match json |> (Yojson.Safe.Util.member "__type") with
                | `String error_type ->
                    let error_type =
                      match String.lsplit2 error_type ~on:'#' with
                      | Some (_, s) -> s
                      | None -> error_type in
                    Error (`AWS (error_of_json error_type json))
                | `Null -> generic_error ()
                | _ ->
                    failwithf "Error '__type' did not have string type: %s"
                      body ()
              with | _ -> generic_error ())
         | (None, _, _) | (_, None, _) | (_, _, false) -> generic_error ()) in
  let response_to_json resp =
    Yojson.Safe.from_string (Awso.Http.Response.body resp) in
  let _ = resp in
  let _ = handle_error in
  let _ = response_to_json in
  match endpoint with
  | DeleteObject ->
      (match resp with
       | Error err ->
           handle_error err (Some DeleteObjectResponse.error_of_json)
       | Ok resp ->
           let headers =
             Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
           Ok (DeleteObjectResponse.of_header_and_body (headers, ())))
  | DescribeObject ->
      (match resp with
       | Error err ->
           handle_error err (Some DescribeObjectResponse.error_of_json)
       | Ok resp ->
           let headers =
             Awso.Http.Headers.to_list (Awso.Http.Response.headers resp) in
           Ok (DescribeObjectResponse.of_header_and_body (headers, ())))
  | GetObject ->
      (match resp with
       | Error err -> handle_error err (Some GetObjectResponse.error_of_json)
       | Ok resp -> Ok (GetObjectResponse.of_json (response_to_json resp)))
  | ListItems ->
      (match resp with
       | Error err -> handle_error err (Some ListItemsResponse.error_of_json)
       | Ok resp -> Ok (ListItemsResponse.of_json (response_to_json resp)))
  | PutObject ->
      (match resp with
       | Error err -> handle_error err (Some PutObjectResponse.error_of_json)
       | Ok resp -> Ok (PutObjectResponse.of_json (response_to_json resp)))
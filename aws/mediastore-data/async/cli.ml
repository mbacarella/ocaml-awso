(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let delete_object =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and path = flag "path" (required string) ~doc:"STRING PathNaming" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_object (Values.DeleteObjectRequest.make ~path ())
           (Some Values.DeleteObjectResponse.to_json)
           (Some Values.DeleteObjectResponse.error_to_json)])
let describe_object =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and path = flag "path" (required string) ~doc:"STRING PathNaming" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_object (Values.DescribeObjectRequest.make ~path ())
           (Some Values.DescribeObjectResponse.to_json)
           (Some Values.DescribeObjectResponse.error_to_json)])
let get_object =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and range = flag "range" (optional string) ~doc:"STRING RangePattern"
       and path = flag "path" (required string) ~doc:"STRING PathNaming" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_object (Values.GetObjectRequest.make ?range ~path ())
           (Some Values.GetObjectResponse.to_json)
           (Some Values.GetObjectResponse.error_to_json)])
let list_items =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and path = flag "path" (optional string) ~doc:"STRING ListPathNaming"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT ListLimit"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_items
           (Values.ListItemsRequest.make ?path ?maxResults ?nextToken ())
           (Some Values.ListItemsResponse.to_json)
           (Some Values.ListItemsResponse.error_to_json)])
let put_object =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and contentType =
         flag "content-type" (optional string) ~doc:"STRING ContentType"
       and cacheControl =
         flag "cache-control" (optional string) ~doc:"STRING StringPrimitive"
       and storageClass =
         flag "storage-class" (optional json_arg) ~doc:"JSON StorageClass"
       and uploadAvailability =
         flag "upload-availability" (optional json_arg)
           ~doc:"JSON UploadAvailability"
       and body = flag "body" (required json_arg) ~doc:"JSON PayloadBlob"
       and path = flag "path" (required string) ~doc:"STRING PathNaming" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_object
           (Values.PutObjectRequest.make ?contentType ?cacheControl
              ?storageClass:(Option.map ~f:Values.StorageClass.of_json
                               storageClass)
              ?uploadAvailability:(Option.map
                                     ~f:Values.UploadAvailability.of_json
                                     uploadAvailability)
              ~body:(Values.PayloadBlob.of_json body) ~path ())
           (Some Values.PutObjectResponse.to_json)
           (Some Values.PutObjectResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("delete-object", delete_object);
    ("describe-object", describe_object);
    ("get-object", get_object);
    ("list-items", list_items);
    ("put-object", put_object)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let cancel_job_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and shutdownGracePeriodInSeconds =
         flag "shutdown-grace-period-in-seconds" (optional int)
           ~doc:"INT ShutdownGracePeriodInSeconds"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and jobRunId =
         flag "job-run-id" (required string) ~doc:"STRING JobRunId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_job_run
           (Values.CancelJobRunRequest.make ?shutdownGracePeriodInSeconds
              ~applicationId ~jobRunId ())
           (Some Values.CancelJobRunResponse.to_json)
           (Some Values.CancelJobRunResponse.error_to_json)])
let create_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (optional string) ~doc:"STRING ApplicationName"
       and initialCapacity =
         flag "initial-capacity" (optional json_arg)
           ~doc:"JSON InitialCapacityConfigMap"
       and maximumCapacity =
         flag "maximum-capacity" (optional json_arg)
           ~doc:"JSON MaximumAllowedResources"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and autoStartConfiguration =
         flag "auto-start-configuration" (optional json_arg)
           ~doc:"JSON AutoStartConfig"
       and autoStopConfiguration =
         flag "auto-stop-configuration" (optional json_arg)
           ~doc:"JSON AutoStopConfig"
       and networkConfiguration =
         flag "network-configuration" (optional json_arg)
           ~doc:"JSON NetworkConfiguration"
       and architecture =
         flag "architecture" (optional json_arg) ~doc:"JSON Architecture"
       and imageConfiguration =
         flag "image-configuration" (optional json_arg)
           ~doc:"JSON ImageConfigurationInput"
       and workerTypeSpecifications =
         flag "worker-type-specifications" (optional json_arg)
           ~doc:"JSON WorkerTypeSpecificationInputMap"
       and runtimeConfiguration =
         flag "runtime-configuration" (optional json_arg)
           ~doc:"JSON ConfigurationList"
       and monitoringConfiguration =
         flag "monitoring-configuration" (optional json_arg)
           ~doc:"JSON MonitoringConfiguration"
       and diskEncryptionConfiguration =
         flag "disk-encryption-configuration" (optional json_arg)
           ~doc:"JSON DiskEncryptionConfiguration"
       and interactiveConfiguration =
         flag "interactive-configuration" (optional json_arg)
           ~doc:"JSON InteractiveConfiguration"
       and schedulerConfiguration =
         flag "scheduler-configuration" (optional json_arg)
           ~doc:"JSON SchedulerConfiguration"
       and identityCenterConfiguration =
         flag "identity-center-configuration" (optional json_arg)
           ~doc:"JSON IdentityCenterConfigurationInput"
       and jobLevelCostAllocationConfiguration =
         flag "job-level-cost-allocation-configuration" (optional json_arg)
           ~doc:"JSON JobLevelCostAllocationConfiguration"
       and releaseLabel =
         flag "release-label" (required string) ~doc:"STRING ReleaseLabel"
       and type_ = flag "type-" (required string) ~doc:"STRING EngineType"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_application
           (Values.CreateApplicationRequest.make ?name
              ?initialCapacity:(Option.map
                                  ~f:Values.InitialCapacityConfigMap.of_json
                                  initialCapacity)
              ?maximumCapacity:(Option.map
                                  ~f:Values.MaximumAllowedResources.of_json
                                  maximumCapacity)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?autoStartConfiguration:(Option.map
                                         ~f:Values.AutoStartConfig.of_json
                                         autoStartConfiguration)
              ?autoStopConfiguration:(Option.map
                                        ~f:Values.AutoStopConfig.of_json
                                        autoStopConfiguration)
              ?networkConfiguration:(Option.map
                                       ~f:Values.NetworkConfiguration.of_json
                                       networkConfiguration)
              ?architecture:(Option.map ~f:Values.Architecture.of_json
                               architecture)
              ?imageConfiguration:(Option.map
                                     ~f:Values.ImageConfigurationInput.of_json
                                     imageConfiguration)
              ?workerTypeSpecifications:(Option.map
                                           ~f:Values.WorkerTypeSpecificationInputMap.of_json
                                           workerTypeSpecifications)
              ?runtimeConfiguration:(Option.map
                                       ~f:Values.ConfigurationList.of_json
                                       runtimeConfiguration)
              ?monitoringConfiguration:(Option.map
                                          ~f:Values.MonitoringConfiguration.of_json
                                          monitoringConfiguration)
              ?diskEncryptionConfiguration:(Option.map
                                              ~f:Values.DiskEncryptionConfiguration.of_json
                                              diskEncryptionConfiguration)
              ?interactiveConfiguration:(Option.map
                                           ~f:Values.InteractiveConfiguration.of_json
                                           interactiveConfiguration)
              ?schedulerConfiguration:(Option.map
                                         ~f:Values.SchedulerConfiguration.of_json
                                         schedulerConfiguration)
              ?identityCenterConfiguration:(Option.map
                                              ~f:Values.IdentityCenterConfigurationInput.of_json
                                              identityCenterConfiguration)
              ?jobLevelCostAllocationConfiguration:(Option.map
                                                      ~f:Values.JobLevelCostAllocationConfiguration.of_json
                                                      jobLevelCostAllocationConfiguration)
              ~releaseLabel ~type_ ~clientToken ())
           (Some Values.CreateApplicationResponse.to_json)
           (Some Values.CreateApplicationResponse.error_to_json)])
let delete_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_application
           (Values.DeleteApplicationRequest.make ~applicationId ())
           (Some Values.DeleteApplicationResponse.to_json)
           (Some Values.DeleteApplicationResponse.error_to_json)])
let get_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_application
           (Values.GetApplicationRequest.make ~applicationId ())
           (Some Values.GetApplicationResponse.to_json)
           (Some Values.GetApplicationResponse.error_to_json)])
let get_dashboard_for_job_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attempt = flag "attempt" (optional int) ~doc:"INT AttemptNumber"
       and accessSystemProfileLogs =
         flag "access-system-profile-logs" (optional bool)
           ~doc:"BOOL Boolean"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and jobRunId =
         flag "job-run-id" (required string) ~doc:"STRING JobRunId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_dashboard_for_job_run
           (Values.GetDashboardForJobRunRequest.make ?attempt
              ?accessSystemProfileLogs ~applicationId ~jobRunId ())
           (Some Values.GetDashboardForJobRunResponse.to_json)
           (Some Values.GetDashboardForJobRunResponse.error_to_json)])
let get_job_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and attempt = flag "attempt" (optional int) ~doc:"INT AttemptNumber"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and jobRunId =
         flag "job-run-id" (required string) ~doc:"STRING JobRunId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_job_run
           (Values.GetJobRunRequest.make ?attempt ~applicationId ~jobRunId ())
           (Some Values.GetJobRunResponse.to_json)
           (Some Values.GetJobRunResponse.error_to_json)])
let get_resource_dashboard =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING ResourceId"
       and resourceType =
         flag "resource-type" (required json_arg) ~doc:"JSON ResourceType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_resource_dashboard
           (Values.GetResourceDashboardRequest.make ~applicationId
              ~resourceId
              ~resourceType:(Values.ResourceType.of_json resourceType) ())
           (Some Values.GetResourceDashboardResponse.to_json)
           (Some Values.GetResourceDashboardResponse.error_to_json)])
let get_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING SessionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_session
           (Values.GetSessionRequest.make ~applicationId ~sessionId ())
           (Some Values.GetSessionResponse.to_json)
           (Some Values.GetSessionResponse.error_to_json)])
let get_session_endpoint =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING SessionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_session_endpoint
           (Values.GetSessionEndpointRequest.make ~applicationId ~sessionId
              ()) (Some Values.GetSessionEndpointResponse.to_json)
           (Some Values.GetSessionEndpointResponse.error_to_json)])
let list_applications =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListApplicationsRequestMaxResultsInteger"
       and states =
         flag "states" (optional json_arg) ~doc:"JSON ApplicationStateSet" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_applications
           (Values.ListApplicationsRequest.make ?nextToken ?maxResults
              ?states:(Option.map ~f:Values.ApplicationStateSet.of_json
                         states) ())
           (Some Values.ListApplicationsResponse.to_json)
           (Some Values.ListApplicationsResponse.error_to_json)])
let list_job_run_attempts =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListJobRunAttemptsRequestMaxResultsInteger"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and jobRunId =
         flag "job-run-id" (required string) ~doc:"STRING JobRunId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_job_run_attempts
           (Values.ListJobRunAttemptsRequest.make ?nextToken ?maxResults
              ~applicationId ~jobRunId ())
           (Some Values.ListJobRunAttemptsResponse.to_json)
           (Some Values.ListJobRunAttemptsResponse.error_to_json)])
let list_job_runs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListJobRunsRequestMaxResultsInteger"
       and createdAtAfter =
         flag "created-at-after" (optional json_arg) ~doc:"JSON Date"
       and createdAtBefore =
         flag "created-at-before" (optional json_arg) ~doc:"JSON Date"
       and states =
         flag "states" (optional json_arg) ~doc:"JSON JobRunStateSet"
       and mode = flag "mode" (optional json_arg) ~doc:"JSON JobRunMode"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_job_runs
           (Values.ListJobRunsRequest.make ?nextToken ?maxResults
              ?createdAtAfter:(Option.map ~f:Values.Date.of_json
                                 createdAtAfter)
              ?createdAtBefore:(Option.map ~f:Values.Date.of_json
                                  createdAtBefore)
              ?states:(Option.map ~f:Values.JobRunStateSet.of_json states)
              ?mode:(Option.map ~f:Values.JobRunMode.of_json mode)
              ~applicationId ()) (Some Values.ListJobRunsResponse.to_json)
           (Some Values.ListJobRunsResponse.error_to_json)])
let list_sessions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListSessionsRequestMaxResultsInteger"
       and states =
         flag "states" (optional json_arg) ~doc:"JSON SessionStateSet"
       and createdAtAfter =
         flag "created-at-after" (optional json_arg) ~doc:"JSON Date"
       and createdAtBefore =
         flag "created-at-before" (optional json_arg) ~doc:"JSON Date"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_sessions
           (Values.ListSessionsRequest.make ?nextToken ?maxResults
              ?states:(Option.map ~f:Values.SessionStateSet.of_json states)
              ?createdAtAfter:(Option.map ~f:Values.Date.of_json
                                 createdAtAfter)
              ?createdAtBefore:(Option.map ~f:Values.Date.of_json
                                  createdAtBefore) ~applicationId ())
           (Some Values.ListSessionsResponse.to_json)
           (Some Values.ListSessionsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let start_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_application
           (Values.StartApplicationRequest.make ~applicationId ())
           (Some Values.StartApplicationResponse.to_json)
           (Some Values.StartApplicationResponse.error_to_json)])
let start_job_run =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and executionIamPolicy =
         flag "execution-iam-policy" (optional json_arg)
           ~doc:"JSON JobRunExecutionIamPolicy"
       and jobDriver =
         flag "job-driver" (optional json_arg) ~doc:"JSON JobDriver"
       and configurationOverrides =
         flag "configuration-overrides" (optional json_arg)
           ~doc:"JSON ConfigurationOverrides"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and executionTimeoutMinutes =
         flag "execution-timeout-minutes" (optional json_arg)
           ~doc:"JSON Duration"
       and name = flag "name" (optional string) ~doc:"STRING String256"
       and mode = flag "mode" (optional json_arg) ~doc:"JSON JobRunMode"
       and retryPolicy =
         flag "retry-policy" (optional json_arg) ~doc:"JSON RetryPolicy"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken"
       and executionRoleArn =
         flag "execution-role-arn" (required string) ~doc:"STRING IAMRoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_job_run
           (Values.StartJobRunRequest.make
              ?executionIamPolicy:(Option.map
                                     ~f:Values.JobRunExecutionIamPolicy.of_json
                                     executionIamPolicy)
              ?jobDriver:(Option.map ~f:Values.JobDriver.of_json jobDriver)
              ?configurationOverrides:(Option.map
                                         ~f:Values.ConfigurationOverrides.of_json
                                         configurationOverrides)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?executionTimeoutMinutes:(Option.map ~f:Values.Duration.of_json
                                          executionTimeoutMinutes) ?name
              ?mode:(Option.map ~f:Values.JobRunMode.of_json mode)
              ?retryPolicy:(Option.map ~f:Values.RetryPolicy.of_json
                              retryPolicy) ~applicationId ~clientToken
              ~executionRoleArn ()) (Some Values.StartJobRunResponse.to_json)
           (Some Values.StartJobRunResponse.error_to_json)])
let start_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and configurationOverrides =
         flag "configuration-overrides" (optional json_arg)
           ~doc:"JSON SessionConfigurationOverrides"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and idleTimeoutMinutes =
         flag "idle-timeout-minutes" (optional json_arg) ~doc:"JSON Duration"
       and name = flag "name" (optional string) ~doc:"STRING String256"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken"
       and executionRoleArn =
         flag "execution-role-arn" (required string) ~doc:"STRING IAMRoleArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_session
           (Values.StartSessionRequest.make
              ?configurationOverrides:(Option.map
                                         ~f:Values.SessionConfigurationOverrides.of_json
                                         configurationOverrides)
              ?tags:(Option.map ~f:Values.TagMap.of_json tags)
              ?idleTimeoutMinutes:(Option.map ~f:Values.Duration.of_json
                                     idleTimeoutMinutes) ?name ~applicationId
              ~clientToken ~executionRoleArn ())
           (Some Values.StartSessionResponse.to_json)
           (Some Values.StartSessionResponse.error_to_json)])
let stop_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.stop_application
           (Values.StopApplicationRequest.make ~applicationId ())
           (Some Values.StopApplicationResponse.to_json)
           (Some Values.StopApplicationResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let terminate_session =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and sessionId =
         flag "session-id" (required string) ~doc:"STRING SessionId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.terminate_session
           (Values.TerminateSessionRequest.make ~applicationId ~sessionId ())
           (Some Values.TerminateSessionResponse.to_json)
           (Some Values.TerminateSessionResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING ResourceArn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_application =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and initialCapacity =
         flag "initial-capacity" (optional json_arg)
           ~doc:"JSON InitialCapacityConfigMap"
       and maximumCapacity =
         flag "maximum-capacity" (optional json_arg)
           ~doc:"JSON MaximumAllowedResources"
       and autoStartConfiguration =
         flag "auto-start-configuration" (optional json_arg)
           ~doc:"JSON AutoStartConfig"
       and autoStopConfiguration =
         flag "auto-stop-configuration" (optional json_arg)
           ~doc:"JSON AutoStopConfig"
       and networkConfiguration =
         flag "network-configuration" (optional json_arg)
           ~doc:"JSON NetworkConfiguration"
       and architecture =
         flag "architecture" (optional json_arg) ~doc:"JSON Architecture"
       and imageConfiguration =
         flag "image-configuration" (optional json_arg)
           ~doc:"JSON ImageConfigurationInput"
       and workerTypeSpecifications =
         flag "worker-type-specifications" (optional json_arg)
           ~doc:"JSON WorkerTypeSpecificationInputMap"
       and interactiveConfiguration =
         flag "interactive-configuration" (optional json_arg)
           ~doc:"JSON InteractiveConfiguration"
       and releaseLabel =
         flag "release-label" (optional string) ~doc:"STRING ReleaseLabel"
       and runtimeConfiguration =
         flag "runtime-configuration" (optional json_arg)
           ~doc:"JSON ConfigurationList"
       and monitoringConfiguration =
         flag "monitoring-configuration" (optional json_arg)
           ~doc:"JSON MonitoringConfiguration"
       and diskEncryptionConfiguration =
         flag "disk-encryption-configuration" (optional json_arg)
           ~doc:"JSON DiskEncryptionConfiguration"
       and schedulerConfiguration =
         flag "scheduler-configuration" (optional json_arg)
           ~doc:"JSON SchedulerConfiguration"
       and identityCenterConfiguration =
         flag "identity-center-configuration" (optional json_arg)
           ~doc:"JSON IdentityCenterConfigurationInput"
       and jobLevelCostAllocationConfiguration =
         flag "job-level-cost-allocation-configuration" (optional json_arg)
           ~doc:"JSON JobLevelCostAllocationConfiguration"
       and applicationId =
         flag "application-id" (required string) ~doc:"STRING ApplicationId"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_application
           (Values.UpdateApplicationRequest.make
              ?initialCapacity:(Option.map
                                  ~f:Values.InitialCapacityConfigMap.of_json
                                  initialCapacity)
              ?maximumCapacity:(Option.map
                                  ~f:Values.MaximumAllowedResources.of_json
                                  maximumCapacity)
              ?autoStartConfiguration:(Option.map
                                         ~f:Values.AutoStartConfig.of_json
                                         autoStartConfiguration)
              ?autoStopConfiguration:(Option.map
                                        ~f:Values.AutoStopConfig.of_json
                                        autoStopConfiguration)
              ?networkConfiguration:(Option.map
                                       ~f:Values.NetworkConfiguration.of_json
                                       networkConfiguration)
              ?architecture:(Option.map ~f:Values.Architecture.of_json
                               architecture)
              ?imageConfiguration:(Option.map
                                     ~f:Values.ImageConfigurationInput.of_json
                                     imageConfiguration)
              ?workerTypeSpecifications:(Option.map
                                           ~f:Values.WorkerTypeSpecificationInputMap.of_json
                                           workerTypeSpecifications)
              ?interactiveConfiguration:(Option.map
                                           ~f:Values.InteractiveConfiguration.of_json
                                           interactiveConfiguration)
              ?releaseLabel
              ?runtimeConfiguration:(Option.map
                                       ~f:Values.ConfigurationList.of_json
                                       runtimeConfiguration)
              ?monitoringConfiguration:(Option.map
                                          ~f:Values.MonitoringConfiguration.of_json
                                          monitoringConfiguration)
              ?diskEncryptionConfiguration:(Option.map
                                              ~f:Values.DiskEncryptionConfiguration.of_json
                                              diskEncryptionConfiguration)
              ?schedulerConfiguration:(Option.map
                                         ~f:Values.SchedulerConfiguration.of_json
                                         schedulerConfiguration)
              ?identityCenterConfiguration:(Option.map
                                              ~f:Values.IdentityCenterConfigurationInput.of_json
                                              identityCenterConfiguration)
              ?jobLevelCostAllocationConfiguration:(Option.map
                                                      ~f:Values.JobLevelCostAllocationConfiguration.of_json
                                                      jobLevelCostAllocationConfiguration)
              ~applicationId ~clientToken ())
           (Some Values.UpdateApplicationResponse.to_json)
           (Some Values.UpdateApplicationResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("cancel-job-run", cancel_job_run);
    ("create-application", create_application);
    ("delete-application", delete_application);
    ("get-application", get_application);
    ("get-dashboard-for-job-run", get_dashboard_for_job_run);
    ("get-job-run", get_job_run);
    ("get-resource-dashboard", get_resource_dashboard);
    ("get-session", get_session);
    ("get-session-endpoint", get_session_endpoint);
    ("list-applications", list_applications);
    ("list-job-run-attempts", list_job_run_attempts);
    ("list-job-runs", list_job_runs);
    ("list-sessions", list_sessions);
    ("list-tags-for-resource", list_tags_for_resource);
    ("start-application", start_application);
    ("start-job-run", start_job_run);
    ("start-session", start_session);
    ("stop-application", stop_application);
    ("tag-resource", tag_resource);
    ("terminate-session", terminate_session);
    ("untag-resource", untag_resource);
    ("update-application", update_application)]
(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let associate_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_certificate
           (Values.AssociateCertificateRequest.make ~arn ())
           (Some Values.AssociateCertificateResponse.to_json)
           (Some Values.AssociateCertificateResponse.error_to_json)])
let cancel_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.cancel_job (Values.CancelJobRequest.make ~id ())
           (Some Values.CancelJobResponse.to_json)
           (Some Values.CancelJobResponse.error_to_json)])
let create_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accelerationSettings =
         flag "acceleration-settings" (optional json_arg)
           ~doc:"JSON AccelerationSettings"
       and billingTagsSource =
         flag "billing-tags-source" (optional json_arg)
           ~doc:"JSON BillingTagsSource"
       and clientRequestToken =
         flag "client-request-token" (optional string) ~doc:"STRING __string"
       and hopDestinations =
         flag "hop-destinations" (optional json_arg)
           ~doc:"JSON __listOfHopDestination"
       and jobEngineVersion =
         flag "job-engine-version" (optional string) ~doc:"STRING __string"
       and jobTemplate =
         flag "job-template" (optional string) ~doc:"STRING __string"
       and priority =
         flag "priority" (optional int)
           ~doc:"INT __integerMinNegative50Max50"
       and queue = flag "queue" (optional string) ~doc:"STRING __string"
       and simulateReservedQueue =
         flag "simulate-reserved-queue" (optional json_arg)
           ~doc:"JSON SimulateReservedQueue"
       and statusUpdateInterval =
         flag "status-update-interval" (optional json_arg)
           ~doc:"JSON StatusUpdateInterval"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON __mapOf__string"
       and userMetadata =
         flag "user-metadata" (optional json_arg) ~doc:"JSON __mapOf__string"
       and role = flag "role" (required string) ~doc:"STRING __string"
       and settings =
         flag "settings" (required json_arg) ~doc:"JSON JobSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_job
           (Values.CreateJobRequest.make
              ?accelerationSettings:(Option.map
                                       ~f:Values.AccelerationSettings.of_json
                                       accelerationSettings)
              ?billingTagsSource:(Option.map
                                    ~f:Values.BillingTagsSource.of_json
                                    billingTagsSource) ?clientRequestToken
              ?hopDestinations:(Option.map
                                  ~f:Values.Zz__listOfHopDestination.of_json
                                  hopDestinations) ?jobEngineVersion
              ?jobTemplate ?priority ?queue
              ?simulateReservedQueue:(Option.map
                                        ~f:Values.SimulateReservedQueue.of_json
                                        simulateReservedQueue)
              ?statusUpdateInterval:(Option.map
                                       ~f:Values.StatusUpdateInterval.of_json
                                       statusUpdateInterval)
              ?tags:(Option.map ~f:Values.Zz__mapOf__string.of_json tags)
              ?userMetadata:(Option.map ~f:Values.Zz__mapOf__string.of_json
                               userMetadata) ~role
              ~settings:(Values.JobSettings.of_json settings) ())
           (Some Values.CreateJobResponse.to_json)
           (Some Values.CreateJobResponse.error_to_json)])
let create_job_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accelerationSettings =
         flag "acceleration-settings" (optional json_arg)
           ~doc:"JSON AccelerationSettings"
       and category =
         flag "category" (optional string) ~doc:"STRING __string"
       and description =
         flag "description" (optional string) ~doc:"STRING __string"
       and hopDestinations =
         flag "hop-destinations" (optional json_arg)
           ~doc:"JSON __listOfHopDestination"
       and priority =
         flag "priority" (optional int)
           ~doc:"INT __integerMinNegative50Max50"
       and queue = flag "queue" (optional string) ~doc:"STRING __string"
       and statusUpdateInterval =
         flag "status-update-interval" (optional json_arg)
           ~doc:"JSON StatusUpdateInterval"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON __mapOf__string"
       and name = flag "name" (required string) ~doc:"STRING __string"
       and settings =
         flag "settings" (required json_arg) ~doc:"JSON JobTemplateSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_job_template
           (Values.CreateJobTemplateRequest.make
              ?accelerationSettings:(Option.map
                                       ~f:Values.AccelerationSettings.of_json
                                       accelerationSettings) ?category
              ?description
              ?hopDestinations:(Option.map
                                  ~f:Values.Zz__listOfHopDestination.of_json
                                  hopDestinations) ?priority ?queue
              ?statusUpdateInterval:(Option.map
                                       ~f:Values.StatusUpdateInterval.of_json
                                       statusUpdateInterval)
              ?tags:(Option.map ~f:Values.Zz__mapOf__string.of_json tags)
              ~name ~settings:(Values.JobTemplateSettings.of_json settings)
              ()) (Some Values.CreateJobTemplateResponse.to_json)
           (Some Values.CreateJobTemplateResponse.error_to_json)])
let create_preset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and category =
         flag "category" (optional string) ~doc:"STRING __string"
       and description =
         flag "description" (optional string) ~doc:"STRING __string"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON __mapOf__string"
       and name = flag "name" (required string) ~doc:"STRING __string"
       and settings =
         flag "settings" (required json_arg) ~doc:"JSON PresetSettings" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_preset
           (Values.CreatePresetRequest.make ?category ?description
              ?tags:(Option.map ~f:Values.Zz__mapOf__string.of_json tags)
              ~name ~settings:(Values.PresetSettings.of_json settings) ())
           (Some Values.CreatePresetResponse.to_json)
           (Some Values.CreatePresetResponse.error_to_json)])
let create_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and concurrentJobs =
         flag "concurrent-jobs" (optional int) ~doc:"INT __integer"
       and description =
         flag "description" (optional string) ~doc:"STRING __string"
       and maximumConcurrentFeeds =
         flag "maximum-concurrent-feeds" (optional int)
           ~doc:"INT __integerMin0"
       and pricingPlan =
         flag "pricing-plan" (optional json_arg) ~doc:"JSON PricingPlan"
       and reservationPlanSettings =
         flag "reservation-plan-settings" (optional json_arg)
           ~doc:"JSON ReservationPlanSettings"
       and status = flag "status" (optional json_arg) ~doc:"JSON QueueStatus"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON __mapOf__string"
       and name = flag "name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_queue
           (Values.CreateQueueRequest.make ?concurrentJobs ?description
              ?maximumConcurrentFeeds
              ?pricingPlan:(Option.map ~f:Values.PricingPlan.of_json
                              pricingPlan)
              ?reservationPlanSettings:(Option.map
                                          ~f:Values.ReservationPlanSettings.of_json
                                          reservationPlanSettings)
              ?status:(Option.map ~f:Values.QueueStatus.of_json status)
              ?tags:(Option.map ~f:Values.Zz__mapOf__string.of_json tags)
              ~name ()) (Some Values.CreateQueueResponse.to_json)
           (Some Values.CreateQueueResponse.error_to_json)])
let create_resource_share =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobId = flag "job-id" (required string) ~doc:"STRING __string"
       and supportCaseId =
         flag "support-case-id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_resource_share
           (Values.CreateResourceShareRequest.make ~jobId ~supportCaseId ())
           (Some Values.CreateResourceShareResponse.to_json)
           (Some Values.CreateResourceShareResponse.error_to_json)])
let delete_job_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_job_template
           (Values.DeleteJobTemplateRequest.make ~name ())
           (Some Values.DeleteJobTemplateResponse.to_json)
           (Some Values.DeleteJobTemplateResponse.error_to_json)])
let delete_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_policy (Values.DeletePolicyRequest.make ())
           (Some Values.DeletePolicyResponse.to_json)
           (Some Values.DeletePolicyResponse.error_to_json)])
let delete_preset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_preset (Values.DeletePresetRequest.make ~name ())
           (Some Values.DeletePresetResponse.to_json)
           (Some Values.DeletePresetResponse.error_to_json)])
let delete_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_queue (Values.DeleteQueueRequest.make ~name ())
           (Some Values.DeleteQueueResponse.to_json)
           (Some Values.DeleteQueueResponse.error_to_json)])
let describe_endpoints =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT __integer"
       and mode =
         flag "mode" (optional json_arg) ~doc:"JSON DescribeEndpointsMode"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.describe_endpoints
           (Values.DescribeEndpointsRequest.make ?maxResults
              ?mode:(Option.map ~f:Values.DescribeEndpointsMode.of_json mode)
              ?nextToken ()) (Some Values.DescribeEndpointsResponse.to_json)
           (Some Values.DescribeEndpointsResponse.error_to_json)])
let disassociate_certificate =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_certificate
           (Values.DisassociateCertificateRequest.make ~arn ())
           (Some Values.DisassociateCertificateResponse.to_json)
           (Some Values.DisassociateCertificateResponse.error_to_json)])
let get_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_job (Values.GetJobRequest.make ~id ())
           (Some Values.GetJobResponse.to_json)
           (Some Values.GetJobResponse.error_to_json)])
let get_job_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_job_template (Values.GetJobTemplateRequest.make ~name ())
           (Some Values.GetJobTemplateResponse.to_json)
           (Some Values.GetJobTemplateResponse.error_to_json)])
let get_jobs_query_results =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_jobs_query_results
           (Values.GetJobsQueryResultsRequest.make ~id ())
           (Some Values.GetJobsQueryResultsResponse.to_json)
           (Some Values.GetJobsQueryResultsResponse.error_to_json)])
let get_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_policy (Values.GetPolicyRequest.make ())
           (Some Values.GetPolicyResponse.to_json)
           (Some Values.GetPolicyResponse.error_to_json)])
let get_preset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_preset (Values.GetPresetRequest.make ~name ())
           (Some Values.GetPresetResponse.to_json)
           (Some Values.GetPresetResponse.error_to_json)])
let get_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and name = flag "name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_queue (Values.GetQueueRequest.make ~name ())
           (Some Values.GetQueueResponse.to_json)
           (Some Values.GetQueueResponse.error_to_json)])
let list_job_templates =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and category =
         flag "category" (optional string) ~doc:"STRING __string"
       and listBy =
         flag "list-by" (optional json_arg) ~doc:"JSON JobTemplateListBy"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT __integerMin1Max20"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and order = flag "order" (optional json_arg) ~doc:"JSON Order" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_job_templates
           (Values.ListJobTemplatesRequest.make ?category
              ?listBy:(Option.map ~f:Values.JobTemplateListBy.of_json listBy)
              ?maxResults ?nextToken
              ?order:(Option.map ~f:Values.Order.of_json order) ())
           (Some Values.ListJobTemplatesResponse.to_json)
           (Some Values.ListJobTemplatesResponse.error_to_json)])
let list_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT __integerMin1Max20"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and order = flag "order" (optional json_arg) ~doc:"JSON Order"
       and queue = flag "queue" (optional string) ~doc:"STRING __string"
       and status = flag "status" (optional json_arg) ~doc:"JSON JobStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_jobs
           (Values.ListJobsRequest.make ?maxResults ?nextToken
              ?order:(Option.map ~f:Values.Order.of_json order) ?queue
              ?status:(Option.map ~f:Values.JobStatus.of_json status) ())
           (Some Values.ListJobsResponse.to_json)
           (Some Values.ListJobsResponse.error_to_json)])
let list_presets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and category =
         flag "category" (optional string) ~doc:"STRING __string"
       and listBy =
         flag "list-by" (optional json_arg) ~doc:"JSON PresetListBy"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT __integerMin1Max20"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and order = flag "order" (optional json_arg) ~doc:"JSON Order" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_presets
           (Values.ListPresetsRequest.make ?category
              ?listBy:(Option.map ~f:Values.PresetListBy.of_json listBy)
              ?maxResults ?nextToken
              ?order:(Option.map ~f:Values.Order.of_json order) ())
           (Some Values.ListPresetsResponse.to_json)
           (Some Values.ListPresetsResponse.error_to_json)])
let list_queues =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and listBy =
         flag "list-by" (optional json_arg) ~doc:"JSON QueueListBy"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT __integerMin1Max20"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and order = flag "order" (optional json_arg) ~doc:"JSON Order" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_queues
           (Values.ListQueuesRequest.make
              ?listBy:(Option.map ~f:Values.QueueListBy.of_json listBy)
              ?maxResults ?nextToken
              ?order:(Option.map ~f:Values.Order.of_json order) ())
           (Some Values.ListQueuesResponse.to_json)
           (Some Values.ListQueuesResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~arn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT __integerMin1Max20"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_versions
           (Values.ListVersionsRequest.make ?maxResults ?nextToken ())
           (Some Values.ListVersionsResponse.to_json)
           (Some Values.ListVersionsResponse.error_to_json)])
let probe =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and inputFiles =
         flag "input-files" (optional json_arg)
           ~doc:"JSON __listOfProbeInputFile" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region Io.probe
           (Values.ProbeRequest.make
              ?inputFiles:(Option.map
                             ~f:Values.Zz__listOfProbeInputFile.of_json
                             inputFiles) ())
           (Some Values.ProbeResponse.to_json)
           (Some Values.ProbeResponse.error_to_json)])
let put_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and policy = flag "policy" (required json_arg) ~doc:"JSON Policy" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.put_policy
           (Values.PutPolicyRequest.make
              ~policy:(Values.Policy.of_json policy) ())
           (Some Values.PutPolicyResponse.to_json)
           (Some Values.PutPolicyResponse.error_to_json)])
let search_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and inputFile =
         flag "input-file" (optional string) ~doc:"STRING __string"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT __integerMin1Max20"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and order = flag "order" (optional json_arg) ~doc:"JSON Order"
       and queue = flag "queue" (optional string) ~doc:"STRING __string"
       and status = flag "status" (optional json_arg) ~doc:"JSON JobStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_jobs
           (Values.SearchJobsRequest.make ?inputFile ?maxResults ?nextToken
              ?order:(Option.map ~f:Values.Order.of_json order) ?queue
              ?status:(Option.map ~f:Values.JobStatus.of_json status) ())
           (Some Values.SearchJobsResponse.to_json)
           (Some Values.SearchJobsResponse.error_to_json)])
let start_jobs_query =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and filterList =
         flag "filter-list" (optional json_arg)
           ~doc:"JSON __listOfJobsQueryFilter"
       and maxResults =
         flag "max-results" (optional int) ~doc:"INT __integerMin1Max20"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING __string"
       and order = flag "order" (optional json_arg) ~doc:"JSON Order" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_jobs_query
           (Values.StartJobsQueryRequest.make
              ?filterList:(Option.map
                             ~f:Values.Zz__listOfJobsQueryFilter.of_json
                             filterList) ?maxResults ?nextToken
              ?order:(Option.map ~f:Values.Order.of_json order) ())
           (Some Values.StartJobsQueryResponse.to_json)
           (Some Values.StartJobsQueryResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and arn = flag "arn" (required string) ~doc:"STRING __string"
       and tags = flag "tags" (required json_arg) ~doc:"JSON __mapOf__string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~arn
              ~tags:(Values.Zz__mapOf__string.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and tagKeys =
         flag "tag-keys" (optional json_arg) ~doc:"JSON __listOf__string"
       and arn = flag "arn" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make
              ?tagKeys:(Option.map ~f:Values.Zz__listOf__string.of_json
                          tagKeys) ~arn ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_job_template =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and accelerationSettings =
         flag "acceleration-settings" (optional json_arg)
           ~doc:"JSON AccelerationSettings"
       and category =
         flag "category" (optional string) ~doc:"STRING __string"
       and description =
         flag "description" (optional string) ~doc:"STRING __string"
       and hopDestinations =
         flag "hop-destinations" (optional json_arg)
           ~doc:"JSON __listOfHopDestination"
       and priority =
         flag "priority" (optional int)
           ~doc:"INT __integerMinNegative50Max50"
       and queue = flag "queue" (optional string) ~doc:"STRING __string"
       and settings =
         flag "settings" (optional json_arg) ~doc:"JSON JobTemplateSettings"
       and statusUpdateInterval =
         flag "status-update-interval" (optional json_arg)
           ~doc:"JSON StatusUpdateInterval"
       and name = flag "name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_job_template
           (Values.UpdateJobTemplateRequest.make
              ?accelerationSettings:(Option.map
                                       ~f:Values.AccelerationSettings.of_json
                                       accelerationSettings) ?category
              ?description
              ?hopDestinations:(Option.map
                                  ~f:Values.Zz__listOfHopDestination.of_json
                                  hopDestinations) ?priority ?queue
              ?settings:(Option.map ~f:Values.JobTemplateSettings.of_json
                           settings)
              ?statusUpdateInterval:(Option.map
                                       ~f:Values.StatusUpdateInterval.of_json
                                       statusUpdateInterval) ~name ())
           (Some Values.UpdateJobTemplateResponse.to_json)
           (Some Values.UpdateJobTemplateResponse.error_to_json)])
let update_preset =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and category =
         flag "category" (optional string) ~doc:"STRING __string"
       and description =
         flag "description" (optional string) ~doc:"STRING __string"
       and settings =
         flag "settings" (optional json_arg) ~doc:"JSON PresetSettings"
       and name = flag "name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_preset
           (Values.UpdatePresetRequest.make ?category ?description
              ?settings:(Option.map ~f:Values.PresetSettings.of_json settings)
              ~name ()) (Some Values.UpdatePresetResponse.to_json)
           (Some Values.UpdatePresetResponse.error_to_json)])
let update_queue =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and concurrentJobs =
         flag "concurrent-jobs" (optional int) ~doc:"INT __integer"
       and description =
         flag "description" (optional string) ~doc:"STRING __string"
       and maximumConcurrentFeeds =
         flag "maximum-concurrent-feeds" (optional int)
           ~doc:"INT __integerMin0"
       and reservationPlanSettings =
         flag "reservation-plan-settings" (optional json_arg)
           ~doc:"JSON ReservationPlanSettings"
       and status = flag "status" (optional json_arg) ~doc:"JSON QueueStatus"
       and name = flag "name" (required string) ~doc:"STRING __string" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_queue
           (Values.UpdateQueueRequest.make ?concurrentJobs ?description
              ?maximumConcurrentFeeds
              ?reservationPlanSettings:(Option.map
                                          ~f:Values.ReservationPlanSettings.of_json
                                          reservationPlanSettings)
              ?status:(Option.map ~f:Values.QueueStatus.of_json status) ~name
              ()) (Some Values.UpdateQueueResponse.to_json)
           (Some Values.UpdateQueueResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("associate-certificate", associate_certificate);
    ("cancel-job", cancel_job);
    ("create-job", create_job);
    ("create-job-template", create_job_template);
    ("create-preset", create_preset);
    ("create-queue", create_queue);
    ("create-resource-share", create_resource_share);
    ("delete-job-template", delete_job_template);
    ("delete-policy", delete_policy);
    ("delete-preset", delete_preset);
    ("delete-queue", delete_queue);
    ("describe-endpoints", describe_endpoints);
    ("disassociate-certificate", disassociate_certificate);
    ("get-job", get_job);
    ("get-job-template", get_job_template);
    ("get-jobs-query-results", get_jobs_query_results);
    ("get-policy", get_policy);
    ("get-preset", get_preset);
    ("get-queue", get_queue);
    ("list-job-templates", list_job_templates);
    ("list-jobs", list_jobs);
    ("list-presets", list_presets);
    ("list-queues", list_queues);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-versions", list_versions);
    ("probe", probe);
    ("put-policy", put_policy);
    ("search-jobs", search_jobs);
    ("start-jobs-query", start_jobs_query);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-job-template", update_job_template);
    ("update-preset", update_preset);
    ("update-queue", update_queue)]
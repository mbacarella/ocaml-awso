(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let activate_key_signing_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hostedZoneId =
         flag "hosted-zone-id" (required string) ~doc:"STRING ResourceId"
       and name = flag "name" (required string) ~doc:"STRING SigningKeyName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.activate_key_signing_key
           (Values.ActivateKeySigningKeyRequest.make ~hostedZoneId ~name ())
           (Some Values.ActivateKeySigningKeyResponse.to_json)
           (Some Values.ActivateKeySigningKeyResponse.error_to_json)])
let associate_v_p_c_with_hosted_zone =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and comment =
         flag "comment" (optional string) ~doc:"STRING AssociateVPCComment"
       and hostedZoneId =
         flag "hosted-zone-id" (required string) ~doc:"STRING ResourceId"
       and vPC = flag "v-p-c" (required json_arg) ~doc:"JSON VPC" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.associate_v_p_c_with_hosted_zone
           (Values.AssociateVPCWithHostedZoneRequest.make ?comment
              ~hostedZoneId ~vPC:(Values.VPC.of_json vPC) ())
           (Some Values.AssociateVPCWithHostedZoneResponse.to_json)
           (Some Values.AssociateVPCWithHostedZoneResponse.error_to_json)])
let change_resource_record_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hostedZoneId =
         flag "hosted-zone-id" (required string) ~doc:"STRING ResourceId"
       and changeBatch =
         flag "change-batch" (required json_arg) ~doc:"JSON ChangeBatch" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.change_resource_record_sets
           (Values.ChangeResourceRecordSetsRequest.make ~hostedZoneId
              ~changeBatch:(Values.ChangeBatch.of_json changeBatch) ())
           (Some Values.ChangeResourceRecordSetsResponse.to_json)
           (Some Values.ChangeResourceRecordSetsResponse.error_to_json)])
let change_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and addTags = flag "add-tags" (optional json_arg) ~doc:"JSON TagList"
       and removeTagKeys =
         flag "remove-tag-keys" (optional json_arg) ~doc:"JSON TagKeyList"
       and resourceType =
         flag "resource-type" (required json_arg) ~doc:"JSON TagResourceType"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING TagResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.change_tags_for_resource
           (Values.ChangeTagsForResourceRequest.make
              ?addTags:(Option.map ~f:Values.TagList.of_json addTags)
              ?removeTagKeys:(Option.map ~f:Values.TagKeyList.of_json
                                removeTagKeys)
              ~resourceType:(Values.TagResourceType.of_json resourceType)
              ~resourceId ())
           (Some Values.ChangeTagsForResourceResponse.to_json)
           (Some Values.ChangeTagsForResourceResponse.error_to_json)])
let create_health_check =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and callerReference =
         flag "caller-reference" (required string)
           ~doc:"STRING HealthCheckNonce"
       and healthCheckConfig =
         flag "health-check-config" (required json_arg)
           ~doc:"JSON HealthCheckConfig" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_health_check
           (Values.CreateHealthCheckRequest.make ~callerReference
              ~healthCheckConfig:(Values.HealthCheckConfig.of_json
                                    healthCheckConfig) ())
           (Some Values.CreateHealthCheckResponse.to_json)
           (Some Values.CreateHealthCheckResponse.error_to_json)])
let create_hosted_zone =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and vPC = flag "v-p-c" (optional json_arg) ~doc:"JSON VPC"
       and hostedZoneConfig =
         flag "hosted-zone-config" (optional json_arg)
           ~doc:"JSON HostedZoneConfig"
       and delegationSetId =
         flag "delegation-set-id" (optional string) ~doc:"STRING ResourceId"
       and name = flag "name" (required string) ~doc:"STRING DNSName"
       and callerReference =
         flag "caller-reference" (required string) ~doc:"STRING Nonce" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_hosted_zone
           (Values.CreateHostedZoneRequest.make
              ?vPC:(Option.map ~f:Values.VPC.of_json vPC)
              ?hostedZoneConfig:(Option.map
                                   ~f:Values.HostedZoneConfig.of_json
                                   hostedZoneConfig) ?delegationSetId ~name
              ~callerReference ())
           (Some Values.CreateHostedZoneResponse.to_json)
           (Some Values.CreateHostedZoneResponse.error_to_json)])
let create_key_signing_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and callerReference =
         flag "caller-reference" (required string) ~doc:"STRING Nonce"
       and hostedZoneId =
         flag "hosted-zone-id" (required string) ~doc:"STRING ResourceId"
       and keyManagementServiceArn =
         flag "key-management-service-arn" (required string)
           ~doc:"STRING SigningKeyString"
       and name = flag "name" (required string) ~doc:"STRING SigningKeyName"
       and status =
         flag "status" (required string) ~doc:"STRING SigningKeyStatus" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_key_signing_key
           (Values.CreateKeySigningKeyRequest.make ~callerReference
              ~hostedZoneId ~keyManagementServiceArn ~name ~status ())
           (Some Values.CreateKeySigningKeyResponse.to_json)
           (Some Values.CreateKeySigningKeyResponse.error_to_json)])
let create_query_logging_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hostedZoneId =
         flag "hosted-zone-id" (required string) ~doc:"STRING ResourceId"
       and cloudWatchLogsLogGroupArn =
         flag "cloud-watch-logs-log-group-arn" (required string)
           ~doc:"STRING CloudWatchLogsLogGroupArn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_query_logging_config
           (Values.CreateQueryLoggingConfigRequest.make ~hostedZoneId
              ~cloudWatchLogsLogGroupArn ())
           (Some Values.CreateQueryLoggingConfigResponse.to_json)
           (Some Values.CreateQueryLoggingConfigResponse.error_to_json)])
let create_reusable_delegation_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hostedZoneId =
         flag "hosted-zone-id" (optional string) ~doc:"STRING ResourceId"
       and callerReference =
         flag "caller-reference" (required string) ~doc:"STRING Nonce" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_reusable_delegation_set
           (Values.CreateReusableDelegationSetRequest.make ?hostedZoneId
              ~callerReference ())
           (Some Values.CreateReusableDelegationSetResponse.to_json)
           (Some Values.CreateReusableDelegationSetResponse.error_to_json)])
let create_traffic_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and comment =
         flag "comment" (optional string) ~doc:"STRING TrafficPolicyComment"
       and name =
         flag "name" (required string) ~doc:"STRING TrafficPolicyName"
       and document =
         flag "document" (required string)
           ~doc:"STRING TrafficPolicyDocument" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_traffic_policy
           (Values.CreateTrafficPolicyRequest.make ?comment ~name ~document
              ()) (Some Values.CreateTrafficPolicyResponse.to_json)
           (Some Values.CreateTrafficPolicyResponse.error_to_json)])
let create_traffic_policy_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hostedZoneId =
         flag "hosted-zone-id" (required string) ~doc:"STRING ResourceId"
       and name = flag "name" (required string) ~doc:"STRING DNSName"
       and tTL = flag "t-t-l" (required json_arg) ~doc:"JSON TTL"
       and trafficPolicyId =
         flag "traffic-policy-id" (required string)
           ~doc:"STRING TrafficPolicyId"
       and trafficPolicyVersion =
         flag "traffic-policy-version" (required int)
           ~doc:"INT TrafficPolicyVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_traffic_policy_instance
           (Values.CreateTrafficPolicyInstanceRequest.make ~hostedZoneId
              ~name ~tTL:(Values.TTL.of_json tTL) ~trafficPolicyId
              ~trafficPolicyVersion ())
           (Some Values.CreateTrafficPolicyInstanceResponse.to_json)
           (Some Values.CreateTrafficPolicyInstanceResponse.error_to_json)])
let create_traffic_policy_version =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and comment =
         flag "comment" (optional string) ~doc:"STRING TrafficPolicyComment"
       and id = flag "id" (required string) ~doc:"STRING TrafficPolicyId"
       and document =
         flag "document" (required string)
           ~doc:"STRING TrafficPolicyDocument" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_traffic_policy_version
           (Values.CreateTrafficPolicyVersionRequest.make ?comment ~id
              ~document ())
           (Some Values.CreateTrafficPolicyVersionResponse.to_json)
           (Some Values.CreateTrafficPolicyVersionResponse.error_to_json)])
let create_v_p_c_association_authorization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hostedZoneId =
         flag "hosted-zone-id" (required string) ~doc:"STRING ResourceId"
       and vPC = flag "v-p-c" (required json_arg) ~doc:"JSON VPC" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_v_p_c_association_authorization
           (Values.CreateVPCAssociationAuthorizationRequest.make
              ~hostedZoneId ~vPC:(Values.VPC.of_json vPC) ())
           (Some Values.CreateVPCAssociationAuthorizationResponse.to_json)
           (Some
              Values.CreateVPCAssociationAuthorizationResponse.error_to_json)])
let deactivate_key_signing_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hostedZoneId =
         flag "hosted-zone-id" (required string) ~doc:"STRING ResourceId"
       and name = flag "name" (required string) ~doc:"STRING SigningKeyName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.deactivate_key_signing_key
           (Values.DeactivateKeySigningKeyRequest.make ~hostedZoneId ~name ())
           (Some Values.DeactivateKeySigningKeyResponse.to_json)
           (Some Values.DeactivateKeySigningKeyResponse.error_to_json)])
let delete_health_check =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and healthCheckId =
         flag "health-check-id" (required string) ~doc:"STRING HealthCheckId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_health_check
           (Values.DeleteHealthCheckRequest.make ~healthCheckId ())
           (Some Values.DeleteHealthCheckResponse.to_json)
           (Some Values.DeleteHealthCheckResponse.error_to_json)])
let delete_hosted_zone =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_hosted_zone (Values.DeleteHostedZoneRequest.make ~id ())
           (Some Values.DeleteHostedZoneResponse.to_json)
           (Some Values.DeleteHostedZoneResponse.error_to_json)])
let delete_key_signing_key =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hostedZoneId =
         flag "hosted-zone-id" (required string) ~doc:"STRING ResourceId"
       and name = flag "name" (required string) ~doc:"STRING SigningKeyName" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_key_signing_key
           (Values.DeleteKeySigningKeyRequest.make ~hostedZoneId ~name ())
           (Some Values.DeleteKeySigningKeyResponse.to_json)
           (Some Values.DeleteKeySigningKeyResponse.error_to_json)])
let delete_query_logging_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id =
         flag "id" (required string) ~doc:"STRING QueryLoggingConfigId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_query_logging_config
           (Values.DeleteQueryLoggingConfigRequest.make ~id ())
           (Some Values.DeleteQueryLoggingConfigResponse.to_json)
           (Some Values.DeleteQueryLoggingConfigResponse.error_to_json)])
let delete_reusable_delegation_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_reusable_delegation_set
           (Values.DeleteReusableDelegationSetRequest.make ~id ())
           (Some Values.DeleteReusableDelegationSetResponse.to_json)
           (Some Values.DeleteReusableDelegationSetResponse.error_to_json)])
let delete_traffic_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING TrafficPolicyId"
       and version =
         flag "version" (required int) ~doc:"INT TrafficPolicyVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_traffic_policy
           (Values.DeleteTrafficPolicyRequest.make ~id ~version ())
           (Some Values.DeleteTrafficPolicyResponse.to_json)
           (Some Values.DeleteTrafficPolicyResponse.error_to_json)])
let delete_traffic_policy_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id =
         flag "id" (required string) ~doc:"STRING TrafficPolicyInstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_traffic_policy_instance
           (Values.DeleteTrafficPolicyInstanceRequest.make ~id ())
           (Some Values.DeleteTrafficPolicyInstanceResponse.to_json)
           (Some Values.DeleteTrafficPolicyInstanceResponse.error_to_json)])
let delete_v_p_c_association_authorization =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hostedZoneId =
         flag "hosted-zone-id" (required string) ~doc:"STRING ResourceId"
       and vPC = flag "v-p-c" (required json_arg) ~doc:"JSON VPC" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_v_p_c_association_authorization
           (Values.DeleteVPCAssociationAuthorizationRequest.make
              ~hostedZoneId ~vPC:(Values.VPC.of_json vPC) ())
           (Some Values.DeleteVPCAssociationAuthorizationResponse.to_json)
           (Some
              Values.DeleteVPCAssociationAuthorizationResponse.error_to_json)])
let disable_hosted_zone_d_n_s_s_e_c =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hostedZoneId =
         flag "hosted-zone-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disable_hosted_zone_d_n_s_s_e_c
           (Values.DisableHostedZoneDNSSECRequest.make ~hostedZoneId ())
           (Some Values.DisableHostedZoneDNSSECResponse.to_json)
           (Some Values.DisableHostedZoneDNSSECResponse.error_to_json)])
let disassociate_v_p_c_from_hosted_zone =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and comment =
         flag "comment" (optional string)
           ~doc:"STRING DisassociateVPCComment"
       and hostedZoneId =
         flag "hosted-zone-id" (required string) ~doc:"STRING ResourceId"
       and vPC = flag "v-p-c" (required json_arg) ~doc:"JSON VPC" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.disassociate_v_p_c_from_hosted_zone
           (Values.DisassociateVPCFromHostedZoneRequest.make ?comment
              ~hostedZoneId ~vPC:(Values.VPC.of_json vPC) ())
           (Some Values.DisassociateVPCFromHostedZoneResponse.to_json)
           (Some Values.DisassociateVPCFromHostedZoneResponse.error_to_json)])
let enable_hosted_zone_d_n_s_s_e_c =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hostedZoneId =
         flag "hosted-zone-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.enable_hosted_zone_d_n_s_s_e_c
           (Values.EnableHostedZoneDNSSECRequest.make ~hostedZoneId ())
           (Some Values.EnableHostedZoneDNSSECResponse.to_json)
           (Some Values.EnableHostedZoneDNSSECResponse.error_to_json)])
let get_account_limit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON AccountLimitType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_account_limit
           (Values.GetAccountLimitRequest.make
              ~type_:(Values.AccountLimitType.of_json type_) ())
           (Some Values.GetAccountLimitResponse.to_json)
           (Some Values.GetAccountLimitResponse.error_to_json)])
let get_change =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_change (Values.GetChangeRequest.make ~id ())
           (Some Values.GetChangeResponse.to_json)
           (Some Values.GetChangeResponse.error_to_json)])
let get_checker_ip_ranges =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_checker_ip_ranges
           (Values.GetCheckerIpRangesRequest.make ())
           (Some Values.GetCheckerIpRangesResponse.to_json)
           (Some Values.GetCheckerIpRangesResponse.error_to_json)])
let get_d_n_s_s_e_c =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hostedZoneId =
         flag "hosted-zone-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_d_n_s_s_e_c (Values.GetDNSSECRequest.make ~hostedZoneId ())
           (Some Values.GetDNSSECResponse.to_json)
           (Some Values.GetDNSSECResponse.error_to_json)])
let get_geo_location =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and continentCode =
         flag "continent-code" (optional string)
           ~doc:"STRING GeoLocationContinentCode"
       and countryCode =
         flag "country-code" (optional string)
           ~doc:"STRING GeoLocationCountryCode"
       and subdivisionCode =
         flag "subdivision-code" (optional string)
           ~doc:"STRING GeoLocationSubdivisionCode" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_geo_location
           (Values.GetGeoLocationRequest.make ?continentCode ?countryCode
              ?subdivisionCode ())
           (Some Values.GetGeoLocationResponse.to_json)
           (Some Values.GetGeoLocationResponse.error_to_json)])
let get_health_check =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and healthCheckId =
         flag "health-check-id" (required string) ~doc:"STRING HealthCheckId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_health_check
           (Values.GetHealthCheckRequest.make ~healthCheckId ())
           (Some Values.GetHealthCheckResponse.to_json)
           (Some Values.GetHealthCheckResponse.error_to_json)])
let get_health_check_count =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_health_check_count
           (Values.GetHealthCheckCountRequest.make ())
           (Some Values.GetHealthCheckCountResponse.to_json)
           (Some Values.GetHealthCheckCountResponse.error_to_json)])
let get_health_check_last_failure_reason =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and healthCheckId =
         flag "health-check-id" (required string) ~doc:"STRING HealthCheckId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_health_check_last_failure_reason
           (Values.GetHealthCheckLastFailureReasonRequest.make ~healthCheckId
              ())
           (Some Values.GetHealthCheckLastFailureReasonResponse.to_json)
           (Some Values.GetHealthCheckLastFailureReasonResponse.error_to_json)])
let get_health_check_status =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and healthCheckId =
         flag "health-check-id" (required string) ~doc:"STRING HealthCheckId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_health_check_status
           (Values.GetHealthCheckStatusRequest.make ~healthCheckId ())
           (Some Values.GetHealthCheckStatusResponse.to_json)
           (Some Values.GetHealthCheckStatusResponse.error_to_json)])
let get_hosted_zone =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_hosted_zone (Values.GetHostedZoneRequest.make ~id ())
           (Some Values.GetHostedZoneResponse.to_json)
           (Some Values.GetHostedZoneResponse.error_to_json)])
let get_hosted_zone_count =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_hosted_zone_count
           (Values.GetHostedZoneCountRequest.make ())
           (Some Values.GetHostedZoneCountResponse.to_json)
           (Some Values.GetHostedZoneCountResponse.error_to_json)])
let get_hosted_zone_limit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ =
         flag "type-" (required json_arg) ~doc:"JSON HostedZoneLimitType"
       and hostedZoneId =
         flag "hosted-zone-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_hosted_zone_limit
           (Values.GetHostedZoneLimitRequest.make
              ~type_:(Values.HostedZoneLimitType.of_json type_) ~hostedZoneId
              ()) (Some Values.GetHostedZoneLimitResponse.to_json)
           (Some Values.GetHostedZoneLimitResponse.error_to_json)])
let get_query_logging_config =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id =
         flag "id" (required string) ~doc:"STRING QueryLoggingConfigId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_query_logging_config
           (Values.GetQueryLoggingConfigRequest.make ~id ())
           (Some Values.GetQueryLoggingConfigResponse.to_json)
           (Some Values.GetQueryLoggingConfigResponse.error_to_json)])
let get_reusable_delegation_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_reusable_delegation_set
           (Values.GetReusableDelegationSetRequest.make ~id ())
           (Some Values.GetReusableDelegationSetResponse.to_json)
           (Some Values.GetReusableDelegationSetResponse.error_to_json)])
let get_reusable_delegation_set_limit =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and type_ =
         flag "type-" (required json_arg)
           ~doc:"JSON ReusableDelegationSetLimitType"
       and delegationSetId =
         flag "delegation-set-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_reusable_delegation_set_limit
           (Values.GetReusableDelegationSetLimitRequest.make
              ~type_:(Values.ReusableDelegationSetLimitType.of_json type_)
              ~delegationSetId ())
           (Some Values.GetReusableDelegationSetLimitResponse.to_json)
           (Some Values.GetReusableDelegationSetLimitResponse.error_to_json)])
let get_traffic_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING TrafficPolicyId"
       and version =
         flag "version" (required int) ~doc:"INT TrafficPolicyVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_traffic_policy
           (Values.GetTrafficPolicyRequest.make ~id ~version ())
           (Some Values.GetTrafficPolicyResponse.to_json)
           (Some Values.GetTrafficPolicyResponse.error_to_json)])
let get_traffic_policy_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id =
         flag "id" (required string) ~doc:"STRING TrafficPolicyInstanceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_traffic_policy_instance
           (Values.GetTrafficPolicyInstanceRequest.make ~id ())
           (Some Values.GetTrafficPolicyInstanceResponse.to_json)
           (Some Values.GetTrafficPolicyInstanceResponse.error_to_json)])
let get_traffic_policy_instance_count =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and () = return () in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_traffic_policy_instance_count
           (Values.GetTrafficPolicyInstanceCountRequest.make ())
           (Some Values.GetTrafficPolicyInstanceCountResponse.to_json)
           (Some Values.GetTrafficPolicyInstanceCountResponse.error_to_json)])
let list_geo_locations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and startContinentCode =
         flag "start-continent-code" (optional string)
           ~doc:"STRING GeoLocationContinentCode"
       and startCountryCode =
         flag "start-country-code" (optional string)
           ~doc:"STRING GeoLocationCountryCode"
       and startSubdivisionCode =
         flag "start-subdivision-code" (optional string)
           ~doc:"STRING GeoLocationSubdivisionCode"
       and maxItems =
         flag "max-items" (optional string) ~doc:"STRING PageMaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_geo_locations
           (Values.ListGeoLocationsRequest.make ?startContinentCode
              ?startCountryCode ?startSubdivisionCode ?maxItems ())
           (Some Values.ListGeoLocationsResponse.to_json)
           (Some Values.ListGeoLocationsResponse.error_to_json)])
let list_health_checks =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING PageMarker"
       and maxItems =
         flag "max-items" (optional string) ~doc:"STRING PageMaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_health_checks
           (Values.ListHealthChecksRequest.make ?marker ?maxItems ())
           (Some Values.ListHealthChecksResponse.to_json)
           (Some Values.ListHealthChecksResponse.error_to_json)])
let list_hosted_zones =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING PageMarker"
       and maxItems =
         flag "max-items" (optional string) ~doc:"STRING PageMaxItems"
       and delegationSetId =
         flag "delegation-set-id" (optional string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_hosted_zones
           (Values.ListHostedZonesRequest.make ?marker ?maxItems
              ?delegationSetId ())
           (Some Values.ListHostedZonesResponse.to_json)
           (Some Values.ListHostedZonesResponse.error_to_json)])
let list_hosted_zones_by_name =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and dNSName =
         flag "d-n-s-name" (optional string) ~doc:"STRING DNSName"
       and hostedZoneId =
         flag "hosted-zone-id" (optional string) ~doc:"STRING ResourceId"
       and maxItems =
         flag "max-items" (optional string) ~doc:"STRING PageMaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_hosted_zones_by_name
           (Values.ListHostedZonesByNameRequest.make ?dNSName ?hostedZoneId
              ?maxItems ())
           (Some Values.ListHostedZonesByNameResponse.to_json)
           (Some Values.ListHostedZonesByNameResponse.error_to_json)])
let list_hosted_zones_by_v_p_c =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and maxItems =
         flag "max-items" (optional string) ~doc:"STRING PageMaxItems"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and vPCId = flag "v-p-c-id" (required string) ~doc:"STRING VPCId"
       and vPCRegion =
         flag "v-p-c-region" (required json_arg) ~doc:"JSON VPCRegion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_hosted_zones_by_v_p_c
           (Values.ListHostedZonesByVPCRequest.make ?maxItems ?nextToken
              ~vPCId ~vPCRegion:(Values.VPCRegion.of_json vPCRegion) ())
           (Some Values.ListHostedZonesByVPCResponse.to_json)
           (Some Values.ListHostedZonesByVPCResponse.error_to_json)])
let list_query_logging_configs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hostedZoneId =
         flag "hosted-zone-id" (optional string) ~doc:"STRING ResourceId"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING MaxResults" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_query_logging_configs
           (Values.ListQueryLoggingConfigsRequest.make ?hostedZoneId
              ?nextToken ?maxResults ())
           (Some Values.ListQueryLoggingConfigsResponse.to_json)
           (Some Values.ListQueryLoggingConfigsResponse.error_to_json)])
let list_resource_record_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and startRecordName =
         flag "start-record-name" (optional string) ~doc:"STRING DNSName"
       and startRecordType =
         flag "start-record-type" (optional json_arg) ~doc:"JSON RRType"
       and startRecordIdentifier =
         flag "start-record-identifier" (optional string)
           ~doc:"STRING ResourceRecordSetIdentifier"
       and maxItems =
         flag "max-items" (optional string) ~doc:"STRING PageMaxItems"
       and hostedZoneId =
         flag "hosted-zone-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_resource_record_sets
           (Values.ListResourceRecordSetsRequest.make ?startRecordName
              ?startRecordType:(Option.map ~f:Values.RRType.of_json
                                  startRecordType) ?startRecordIdentifier
              ?maxItems ~hostedZoneId ())
           (Some Values.ListResourceRecordSetsResponse.to_json)
           (Some Values.ListResourceRecordSetsResponse.error_to_json)])
let list_reusable_delegation_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and marker = flag "marker" (optional string) ~doc:"STRING PageMarker"
       and maxItems =
         flag "max-items" (optional string) ~doc:"STRING PageMaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_reusable_delegation_sets
           (Values.ListReusableDelegationSetsRequest.make ?marker ?maxItems
              ()) (Some Values.ListReusableDelegationSetsResponse.to_json)
           (Some Values.ListReusableDelegationSetsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceType =
         flag "resource-type" (required json_arg) ~doc:"JSON TagResourceType"
       and resourceId =
         flag "resource-id" (required string) ~doc:"STRING TagResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make
              ~resourceType:(Values.TagResourceType.of_json resourceType)
              ~resourceId ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let list_tags_for_resources =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceType =
         flag "resource-type" (required json_arg) ~doc:"JSON TagResourceType"
       and resourceIds =
         flag "resource-ids" (required json_arg)
           ~doc:"JSON TagResourceIdList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resources
           (Values.ListTagsForResourcesRequest.make
              ~resourceType:(Values.TagResourceType.of_json resourceType)
              ~resourceIds:(Values.TagResourceIdList.of_json resourceIds) ())
           (Some Values.ListTagsForResourcesResponse.to_json)
           (Some Values.ListTagsForResourcesResponse.error_to_json)])
let list_traffic_policies =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trafficPolicyIdMarker =
         flag "traffic-policy-id-marker" (optional string)
           ~doc:"STRING TrafficPolicyId"
       and maxItems =
         flag "max-items" (optional string) ~doc:"STRING PageMaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_traffic_policies
           (Values.ListTrafficPoliciesRequest.make ?trafficPolicyIdMarker
              ?maxItems ()) (Some Values.ListTrafficPoliciesResponse.to_json)
           (Some Values.ListTrafficPoliciesResponse.error_to_json)])
let list_traffic_policy_instances =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hostedZoneIdMarker =
         flag "hosted-zone-id-marker" (optional string)
           ~doc:"STRING ResourceId"
       and trafficPolicyInstanceNameMarker =
         flag "traffic-policy-instance-name-marker" (optional string)
           ~doc:"STRING DNSName"
       and trafficPolicyInstanceTypeMarker =
         flag "traffic-policy-instance-type-marker" (optional json_arg)
           ~doc:"JSON RRType"
       and maxItems =
         flag "max-items" (optional string) ~doc:"STRING PageMaxItems" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_traffic_policy_instances
           (Values.ListTrafficPolicyInstancesRequest.make ?hostedZoneIdMarker
              ?trafficPolicyInstanceNameMarker
              ?trafficPolicyInstanceTypeMarker:(Option.map
                                                  ~f:Values.RRType.of_json
                                                  trafficPolicyInstanceTypeMarker)
              ?maxItems ())
           (Some Values.ListTrafficPolicyInstancesResponse.to_json)
           (Some Values.ListTrafficPolicyInstancesResponse.error_to_json)])
let list_traffic_policy_instances_by_hosted_zone =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trafficPolicyInstanceNameMarker =
         flag "traffic-policy-instance-name-marker" (optional string)
           ~doc:"STRING DNSName"
       and trafficPolicyInstanceTypeMarker =
         flag "traffic-policy-instance-type-marker" (optional json_arg)
           ~doc:"JSON RRType"
       and maxItems =
         flag "max-items" (optional string) ~doc:"STRING PageMaxItems"
       and hostedZoneId =
         flag "hosted-zone-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_traffic_policy_instances_by_hosted_zone
           (Values.ListTrafficPolicyInstancesByHostedZoneRequest.make
              ?trafficPolicyInstanceNameMarker
              ?trafficPolicyInstanceTypeMarker:(Option.map
                                                  ~f:Values.RRType.of_json
                                                  trafficPolicyInstanceTypeMarker)
              ?maxItems ~hostedZoneId ())
           (Some
              Values.ListTrafficPolicyInstancesByHostedZoneResponse.to_json)
           (Some
              Values.ListTrafficPolicyInstancesByHostedZoneResponse.error_to_json)])
let list_traffic_policy_instances_by_policy =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and hostedZoneIdMarker =
         flag "hosted-zone-id-marker" (optional string)
           ~doc:"STRING ResourceId"
       and trafficPolicyInstanceNameMarker =
         flag "traffic-policy-instance-name-marker" (optional string)
           ~doc:"STRING DNSName"
       and trafficPolicyInstanceTypeMarker =
         flag "traffic-policy-instance-type-marker" (optional json_arg)
           ~doc:"JSON RRType"
       and maxItems =
         flag "max-items" (optional string) ~doc:"STRING PageMaxItems"
       and trafficPolicyId =
         flag "traffic-policy-id" (required string)
           ~doc:"STRING TrafficPolicyId"
       and trafficPolicyVersion =
         flag "traffic-policy-version" (required int)
           ~doc:"INT TrafficPolicyVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_traffic_policy_instances_by_policy
           (Values.ListTrafficPolicyInstancesByPolicyRequest.make
              ?hostedZoneIdMarker ?trafficPolicyInstanceNameMarker
              ?trafficPolicyInstanceTypeMarker:(Option.map
                                                  ~f:Values.RRType.of_json
                                                  trafficPolicyInstanceTypeMarker)
              ?maxItems ~trafficPolicyId ~trafficPolicyVersion ())
           (Some Values.ListTrafficPolicyInstancesByPolicyResponse.to_json)
           (Some
              Values.ListTrafficPolicyInstancesByPolicyResponse.error_to_json)])
let list_traffic_policy_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and trafficPolicyVersionMarker =
         flag "traffic-policy-version-marker" (optional string)
           ~doc:"STRING TrafficPolicyVersionMarker"
       and maxItems =
         flag "max-items" (optional string) ~doc:"STRING PageMaxItems"
       and id = flag "id" (required string) ~doc:"STRING TrafficPolicyId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_traffic_policy_versions
           (Values.ListTrafficPolicyVersionsRequest.make
              ?trafficPolicyVersionMarker ?maxItems ~id ())
           (Some Values.ListTrafficPolicyVersionsResponse.to_json)
           (Some Values.ListTrafficPolicyVersionsResponse.error_to_json)])
let list_v_p_c_association_authorizations =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING PaginationToken"
       and maxResults =
         flag "max-results" (optional string) ~doc:"STRING MaxResults"
       and hostedZoneId =
         flag "hosted-zone-id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_v_p_c_association_authorizations
           (Values.ListVPCAssociationAuthorizationsRequest.make ?nextToken
              ?maxResults ~hostedZoneId ())
           (Some Values.ListVPCAssociationAuthorizationsResponse.to_json)
           (Some
              Values.ListVPCAssociationAuthorizationsResponse.error_to_json)])
let test_d_n_s_answer =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resolverIP =
         flag "resolver-i-p" (optional string) ~doc:"STRING IPAddress"
       and eDNS0ClientSubnetIP =
         flag "e-d-n-s0-client-subnet-i-p" (optional string)
           ~doc:"STRING IPAddress"
       and eDNS0ClientSubnetMask =
         flag "e-d-n-s0-client-subnet-mask" (optional string)
           ~doc:"STRING SubnetMask"
       and hostedZoneId =
         flag "hosted-zone-id" (required string) ~doc:"STRING ResourceId"
       and recordName =
         flag "record-name" (required string) ~doc:"STRING DNSName"
       and recordType =
         flag "record-type" (required json_arg) ~doc:"JSON RRType" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.test_d_n_s_answer
           (Values.TestDNSAnswerRequest.make ?resolverIP ?eDNS0ClientSubnetIP
              ?eDNS0ClientSubnetMask ~hostedZoneId ~recordName
              ~recordType:(Values.RRType.of_json recordType) ())
           (Some Values.TestDNSAnswerResponse.to_json)
           (Some Values.TestDNSAnswerResponse.error_to_json)])
let update_health_check =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and healthCheckVersion =
         flag "health-check-version" (optional json_arg)
           ~doc:"JSON HealthCheckVersion"
       and iPAddress =
         flag "i-p-address" (optional string) ~doc:"STRING IPAddress"
       and port = flag "port" (optional int) ~doc:"INT Port"
       and resourcePath =
         flag "resource-path" (optional string) ~doc:"STRING ResourcePath"
       and fullyQualifiedDomainName =
         flag "fully-qualified-domain-name" (optional string)
           ~doc:"STRING FullyQualifiedDomainName"
       and searchString =
         flag "search-string" (optional string) ~doc:"STRING SearchString"
       and failureThreshold =
         flag "failure-threshold" (optional int) ~doc:"INT FailureThreshold"
       and inverted = flag "inverted" (optional bool) ~doc:"BOOL Inverted"
       and disabled = flag "disabled" (optional bool) ~doc:"BOOL Disabled"
       and healthThreshold =
         flag "health-threshold" (optional int) ~doc:"INT HealthThreshold"
       and childHealthChecks =
         flag "child-health-checks" (optional json_arg)
           ~doc:"JSON ChildHealthCheckList"
       and enableSNI =
         flag "enable-s-n-i" (optional bool) ~doc:"BOOL EnableSNI"
       and regions =
         flag "regions" (optional json_arg) ~doc:"JSON HealthCheckRegionList"
       and alarmIdentifier =
         flag "alarm-identifier" (optional json_arg)
           ~doc:"JSON AlarmIdentifier"
       and insufficientDataHealthStatus =
         flag "insufficient-data-health-status" (optional json_arg)
           ~doc:"JSON InsufficientDataHealthStatus"
       and resetElements =
         flag "reset-elements" (optional json_arg)
           ~doc:"JSON ResettableElementNameList"
       and healthCheckId =
         flag "health-check-id" (required string) ~doc:"STRING HealthCheckId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_health_check
           (Values.UpdateHealthCheckRequest.make
              ?healthCheckVersion:(Option.map
                                     ~f:Values.HealthCheckVersion.of_json
                                     healthCheckVersion) ?iPAddress ?port
              ?resourcePath ?fullyQualifiedDomainName ?searchString
              ?failureThreshold ?inverted ?disabled ?healthThreshold
              ?childHealthChecks:(Option.map
                                    ~f:Values.ChildHealthCheckList.of_json
                                    childHealthChecks) ?enableSNI
              ?regions:(Option.map ~f:Values.HealthCheckRegionList.of_json
                          regions)
              ?alarmIdentifier:(Option.map ~f:Values.AlarmIdentifier.of_json
                                  alarmIdentifier)
              ?insufficientDataHealthStatus:(Option.map
                                               ~f:Values.InsufficientDataHealthStatus.of_json
                                               insufficientDataHealthStatus)
              ?resetElements:(Option.map
                                ~f:Values.ResettableElementNameList.of_json
                                resetElements) ~healthCheckId ())
           (Some Values.UpdateHealthCheckResponse.to_json)
           (Some Values.UpdateHealthCheckResponse.error_to_json)])
let update_hosted_zone_comment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and comment =
         flag "comment" (optional string) ~doc:"STRING ResourceDescription"
       and id = flag "id" (required string) ~doc:"STRING ResourceId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_hosted_zone_comment
           (Values.UpdateHostedZoneCommentRequest.make ?comment ~id ())
           (Some Values.UpdateHostedZoneCommentResponse.to_json)
           (Some Values.UpdateHostedZoneCommentResponse.error_to_json)])
let update_traffic_policy_comment =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id = flag "id" (required string) ~doc:"STRING TrafficPolicyId"
       and version =
         flag "version" (required int) ~doc:"INT TrafficPolicyVersion"
       and comment =
         flag "comment" (required string) ~doc:"STRING TrafficPolicyComment" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_traffic_policy_comment
           (Values.UpdateTrafficPolicyCommentRequest.make ~id ~version
              ~comment ())
           (Some Values.UpdateTrafficPolicyCommentResponse.to_json)
           (Some Values.UpdateTrafficPolicyCommentResponse.error_to_json)])
let update_traffic_policy_instance =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and id =
         flag "id" (required string) ~doc:"STRING TrafficPolicyInstanceId"
       and tTL = flag "t-t-l" (required json_arg) ~doc:"JSON TTL"
       and trafficPolicyId =
         flag "traffic-policy-id" (required string)
           ~doc:"STRING TrafficPolicyId"
       and trafficPolicyVersion =
         flag "traffic-policy-version" (required int)
           ~doc:"INT TrafficPolicyVersion" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_traffic_policy_instance
           (Values.UpdateTrafficPolicyInstanceRequest.make ~id
              ~tTL:(Values.TTL.of_json tTL) ~trafficPolicyId
              ~trafficPolicyVersion ())
           (Some Values.UpdateTrafficPolicyInstanceResponse.to_json)
           (Some Values.UpdateTrafficPolicyInstanceResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("activate-key-signing-key", activate_key_signing_key);
    ("associate-v-p-c-with-hosted-zone", associate_v_p_c_with_hosted_zone);
    ("change-resource-record-sets", change_resource_record_sets);
    ("change-tags-for-resource", change_tags_for_resource);
    ("create-health-check", create_health_check);
    ("create-hosted-zone", create_hosted_zone);
    ("create-key-signing-key", create_key_signing_key);
    ("create-query-logging-config", create_query_logging_config);
    ("create-reusable-delegation-set", create_reusable_delegation_set);
    ("create-traffic-policy", create_traffic_policy);
    ("create-traffic-policy-instance", create_traffic_policy_instance);
    ("create-traffic-policy-version", create_traffic_policy_version);
    ("create-v-p-c-association-authorization",
      create_v_p_c_association_authorization);
    ("deactivate-key-signing-key", deactivate_key_signing_key);
    ("delete-health-check", delete_health_check);
    ("delete-hosted-zone", delete_hosted_zone);
    ("delete-key-signing-key", delete_key_signing_key);
    ("delete-query-logging-config", delete_query_logging_config);
    ("delete-reusable-delegation-set", delete_reusable_delegation_set);
    ("delete-traffic-policy", delete_traffic_policy);
    ("delete-traffic-policy-instance", delete_traffic_policy_instance);
    ("delete-v-p-c-association-authorization",
      delete_v_p_c_association_authorization);
    ("disable-hosted-zone-d-n-s-s-e-c", disable_hosted_zone_d_n_s_s_e_c);
    ("disassociate-v-p-c-from-hosted-zone",
      disassociate_v_p_c_from_hosted_zone);
    ("enable-hosted-zone-d-n-s-s-e-c", enable_hosted_zone_d_n_s_s_e_c);
    ("get-account-limit", get_account_limit);
    ("get-change", get_change);
    ("get-checker-ip-ranges", get_checker_ip_ranges);
    ("get-d-n-s-s-e-c", get_d_n_s_s_e_c);
    ("get-geo-location", get_geo_location);
    ("get-health-check", get_health_check);
    ("get-health-check-count", get_health_check_count);
    ("get-health-check-last-failure-reason",
      get_health_check_last_failure_reason);
    ("get-health-check-status", get_health_check_status);
    ("get-hosted-zone", get_hosted_zone);
    ("get-hosted-zone-count", get_hosted_zone_count);
    ("get-hosted-zone-limit", get_hosted_zone_limit);
    ("get-query-logging-config", get_query_logging_config);
    ("get-reusable-delegation-set", get_reusable_delegation_set);
    ("get-reusable-delegation-set-limit", get_reusable_delegation_set_limit);
    ("get-traffic-policy", get_traffic_policy);
    ("get-traffic-policy-instance", get_traffic_policy_instance);
    ("get-traffic-policy-instance-count", get_traffic_policy_instance_count);
    ("list-geo-locations", list_geo_locations);
    ("list-health-checks", list_health_checks);
    ("list-hosted-zones", list_hosted_zones);
    ("list-hosted-zones-by-name", list_hosted_zones_by_name);
    ("list-hosted-zones-by-v-p-c", list_hosted_zones_by_v_p_c);
    ("list-query-logging-configs", list_query_logging_configs);
    ("list-resource-record-sets", list_resource_record_sets);
    ("list-reusable-delegation-sets", list_reusable_delegation_sets);
    ("list-tags-for-resource", list_tags_for_resource);
    ("list-tags-for-resources", list_tags_for_resources);
    ("list-traffic-policies", list_traffic_policies);
    ("list-traffic-policy-instances", list_traffic_policy_instances);
    ("list-traffic-policy-instances-by-hosted-zone",
      list_traffic_policy_instances_by_hosted_zone);
    ("list-traffic-policy-instances-by-policy",
      list_traffic_policy_instances_by_policy);
    ("list-traffic-policy-versions", list_traffic_policy_versions);
    ("list-v-p-c-association-authorizations",
      list_v_p_c_association_authorizations);
    ("test-d-n-s-answer", test_d_n_s_answer);
    ("update-health-check", update_health_check);
    ("update-hosted-zone-comment", update_hosted_zone_comment);
    ("update-traffic-policy-comment", update_traffic_policy_comment);
    ("update-traffic-policy-instance", update_traffic_policy_instance)]
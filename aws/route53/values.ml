(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Awso
open! Import
[@@@warning "-32"]
let service = Service.route53
let apiVersion = "2013-04-01"
let endpointPrefix = "route53"
let serviceFullName = "Amazon Route 53"
let signatureVersion = "v4"
let protocol = "rest_xml"
let globalEndpoint = endpointPrefix ^ ".amazonaws.com"
let serviceAbbreviation = "Route 53"
let simple_to_json to_value x =
  Botodata.Json.value_to_json_scalar (to_value x)
let composed_to_json to_value x = Botodata.Json.value_to_json (to_value x)
let to_query to_value x = Client.Query.of_value (to_value x)
let structure_to_value_aux st ~f =
  let filter = function | (k, Some v) -> Some (k, v) | _ -> None in
  let pair k v = (k, v) in
  let defer_value (k, dv) = pair k dv in
  ((List.filter_map st ~f:filter) |> (List.map ~f:defer_value)) |>
    (fun x -> `Structure (f x))
let structure_to_value = structure_to_value_aux ~f:Fn.id
let structure_to_wrapped_value ~wrapper ~response =
  structure_to_value_aux
    ~f:(fun x -> [(wrapper, (`Structure x)); (response, (`Structure []))])
module RData =
  struct
    type nonrec t = string
    let context_ = "RData"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:4000); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RData" j
    let to_json = simple_to_json to_value
  end
module DimensionField =
  struct
    type nonrec t = string
    let context_ = "DimensionField"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:255) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DimensionField" j
    let to_json = simple_to_json to_value
  end
module AliasHealthEnabled =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module DNSName =
  struct
    type nonrec t = string
    let context_ = "DNSName"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:1024); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DNSName" j
    let to_json = simple_to_json to_value
  end
module ResourceId =
  struct
    type nonrec t = string
    let context_ = "ResourceId"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:32); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceId" j
    let to_json = simple_to_json to_value
  end
module GeoLocationContinentCode =
  struct
    type nonrec t = string
    let context_ = "GeoLocationContinentCode"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2) >>=
             (fun () -> check_string_min i ~min:2));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GeoLocationContinentCode" j
    let to_json = simple_to_json to_value
  end
module GeoLocationCountryCode =
  struct
    type nonrec t = string
    let context_ = "GeoLocationCountryCode"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:2) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GeoLocationCountryCode" j
    let to_json = simple_to_json to_value
  end
module GeoLocationSubdivisionCode =
  struct
    type nonrec t = string
    let context_ = "GeoLocationSubdivisionCode"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:3) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GeoLocationSubdivisionCode" j
    let to_json = simple_to_json to_value
  end
module ResourceRecord =
  struct
    type nonrec t =
      {
      value: RData.t
        [@ocaml.doc
          "The current or new DNS record value, not to exceed 4,000 characters. In the case of a DELETE action, if the current value does not match the actual value, an error is returned. For descriptions about how to format Value for different record types, see Supported DNS Resource Record Types in the Amazon Route 53 Developer Guide. You can specify more than one value for all record types except CNAME and SOA. If you're creating an alias resource record set, omit Value."]}
    let context_ = "ResourceRecord"
    let make ~value = fun () -> { value }
    let to_value x =
      structure_to_value [("Value", (Some (RData.to_value x.value)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        RData.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Value") in
      make ~value ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let value = field_map_exn json "Value" RData.of_json in make ~value ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information specific to the resource record. If you're creating an alias resource record set, omit ResourceRecord."]
module TagKey =
  struct
    type nonrec t = string
    let context_ = "TagKey"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:128); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagKey" j
    let to_json = simple_to_json to_value
  end
module TagValue =
  struct
    type nonrec t = string
    let context_ = "TagValue"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:256); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagValue" j
    let to_json = simple_to_json to_value
  end
module Dimension =
  struct
    type nonrec t =
      {
      name: DimensionField.t
        [@ocaml.doc
          "For the metric that the CloudWatch alarm is associated with, the name of one dimension."];
      value: DimensionField.t
        [@ocaml.doc
          "For the metric that the CloudWatch alarm is associated with, the value of one dimension."]}
    let context_ = "Dimension"
    let make ~name = fun ~value -> fun () -> { name; value }
    let to_value x =
      structure_to_value
        [("Name", (Some (DimensionField.to_value x.name)));
        ("Value", (Some (DimensionField.to_value x.value)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        DimensionField.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Value") in
      let name =
        DimensionField.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ~value ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let value = field_map_exn json "Value" DimensionField.of_json in
      let name = field_map_exn json "Name" DimensionField.of_json in
      make ~value ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "For the metric that the CloudWatch alarm is associated with, a complex type that contains information about one dimension."]
module AlarmName =
  struct
    type nonrec t = string
    let context_ = "AlarmName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:256) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AlarmName" j
    let to_json = simple_to_json to_value
  end
module CloudWatchRegion =
  struct
    type nonrec t =
      | Us_east_1 
      | Us_east_2 
      | Us_west_1 
      | Us_west_2 
      | Ca_central_1 
      | Eu_central_1 
      | Eu_west_1 
      | Eu_west_2 
      | Eu_west_3 
      | Ap_east_1 
      | Me_south_1 
      | Ap_south_1 
      | Ap_southeast_1 
      | Ap_southeast_2 
      | Ap_southeast_3 
      | Ap_northeast_1 
      | Ap_northeast_2 
      | Ap_northeast_3 
      | Eu_north_1 
      | Sa_east_1 
      | Cn_northwest_1 
      | Cn_north_1 
      | Af_south_1 
      | Eu_south_1 
      | Us_gov_west_1 
      | Us_gov_east_1 
      | Us_iso_east_1 
      | Us_iso_west_1 
      | Us_isob_east_1 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Us_east_1 -> "us-east-1"
      | Us_east_2 -> "us-east-2"
      | Us_west_1 -> "us-west-1"
      | Us_west_2 -> "us-west-2"
      | Ca_central_1 -> "ca-central-1"
      | Eu_central_1 -> "eu-central-1"
      | Eu_west_1 -> "eu-west-1"
      | Eu_west_2 -> "eu-west-2"
      | Eu_west_3 -> "eu-west-3"
      | Ap_east_1 -> "ap-east-1"
      | Me_south_1 -> "me-south-1"
      | Ap_south_1 -> "ap-south-1"
      | Ap_southeast_1 -> "ap-southeast-1"
      | Ap_southeast_2 -> "ap-southeast-2"
      | Ap_southeast_3 -> "ap-southeast-3"
      | Ap_northeast_1 -> "ap-northeast-1"
      | Ap_northeast_2 -> "ap-northeast-2"
      | Ap_northeast_3 -> "ap-northeast-3"
      | Eu_north_1 -> "eu-north-1"
      | Sa_east_1 -> "sa-east-1"
      | Cn_northwest_1 -> "cn-northwest-1"
      | Cn_north_1 -> "cn-north-1"
      | Af_south_1 -> "af-south-1"
      | Eu_south_1 -> "eu-south-1"
      | Us_gov_west_1 -> "us-gov-west-1"
      | Us_gov_east_1 -> "us-gov-east-1"
      | Us_iso_east_1 -> "us-iso-east-1"
      | Us_iso_west_1 -> "us-iso-west-1"
      | Us_isob_east_1 -> "us-isob-east-1"
      | Non_static_id s -> s
    let of_string =
      function
      | "us-east-1" -> Us_east_1
      | "us-east-2" -> Us_east_2
      | "us-west-1" -> Us_west_1
      | "us-west-2" -> Us_west_2
      | "ca-central-1" -> Ca_central_1
      | "eu-central-1" -> Eu_central_1
      | "eu-west-1" -> Eu_west_1
      | "eu-west-2" -> Eu_west_2
      | "eu-west-3" -> Eu_west_3
      | "ap-east-1" -> Ap_east_1
      | "me-south-1" -> Me_south_1
      | "ap-south-1" -> Ap_south_1
      | "ap-southeast-1" -> Ap_southeast_1
      | "ap-southeast-2" -> Ap_southeast_2
      | "ap-southeast-3" -> Ap_southeast_3
      | "ap-northeast-1" -> Ap_northeast_1
      | "ap-northeast-2" -> Ap_northeast_2
      | "ap-northeast-3" -> Ap_northeast_3
      | "eu-north-1" -> Eu_north_1
      | "sa-east-1" -> Sa_east_1
      | "cn-northwest-1" -> Cn_northwest_1
      | "cn-north-1" -> Cn_north_1
      | "af-south-1" -> Af_south_1
      | "eu-south-1" -> Eu_south_1
      | "us-gov-west-1" -> Us_gov_west_1
      | "us-gov-east-1" -> Us_gov_east_1
      | "us-iso-east-1" -> Us_iso_east_1
      | "us-iso-west-1" -> Us_iso_west_1
      | "us-isob-east-1" -> Us_isob_east_1
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration CloudWatchRegion" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"CloudWatchRegion" j)
    let to_json = simple_to_json to_value
  end
module HealthCheckId =
  struct
    type nonrec t = string
    let context_ = "HealthCheckId"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:64); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"HealthCheckId" j
    let to_json = simple_to_json to_value
  end
module HealthCheckRegion =
  struct
    type nonrec t =
      | Us_east_1 
      | Us_west_1 
      | Us_west_2 
      | Eu_west_1 
      | Ap_southeast_1 
      | Ap_southeast_2 
      | Ap_northeast_1 
      | Sa_east_1 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Us_east_1 -> "us-east-1"
      | Us_west_1 -> "us-west-1"
      | Us_west_2 -> "us-west-2"
      | Eu_west_1 -> "eu-west-1"
      | Ap_southeast_1 -> "ap-southeast-1"
      | Ap_southeast_2 -> "ap-southeast-2"
      | Ap_northeast_1 -> "ap-northeast-1"
      | Sa_east_1 -> "sa-east-1"
      | Non_static_id s -> s
    let of_string =
      function
      | "us-east-1" -> Us_east_1
      | "us-west-1" -> Us_west_1
      | "us-west-2" -> Us_west_2
      | "eu-west-1" -> Eu_west_1
      | "ap-southeast-1" -> Ap_southeast_1
      | "ap-southeast-2" -> Ap_southeast_2
      | "ap-northeast-1" -> Ap_northeast_1
      | "sa-east-1" -> Sa_east_1
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration HealthCheckRegion" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"HealthCheckRegion" j)
    let to_json = simple_to_json to_value
  end
module AliasTarget =
  struct
    type nonrec t =
      {
      hostedZoneId: ResourceId.t
        [@ocaml.doc
          "Alias resource records sets only: The value used depends on where you want to route traffic: Amazon API Gateway custom regional APIs and edge-optimized APIs Specify the hosted zone ID for your API. You can get the applicable value using the CLI command get-domain-names: For regional APIs, specify the value of regionalHostedZoneId. For edge-optimized APIs, specify the value of distributionHostedZoneId. Amazon Virtual Private Cloud interface VPC endpoint Specify the hosted zone ID for your interface endpoint. You can get the value of HostedZoneId using the CLI command describe-vpc-endpoints. CloudFront distribution Specify Z2FDTNDATAQYW2. Alias resource record sets for CloudFront can't be created in a private zone. Elastic Beanstalk environment Specify the hosted zone ID for the region that you created the environment in. The environment must have a regionalized subdomain. For a list of regions and the corresponding hosted zone IDs, see Elastic Beanstalk endpoints and quotas in the the Amazon Web Services General Reference. ELB load balancer Specify the value of the hosted zone ID for the load balancer. Use the following methods to get the hosted zone ID: Elastic Load Balancing endpoints and quotas topic in the Amazon Web Services General Reference: Use the value that corresponds with the region that you created your load balancer in. Note that there are separate columns for Application and Classic Load Balancers and for Network Load Balancers. Amazon Web Services Management Console: Go to the Amazon EC2 page, choose Load Balancers in the navigation pane, select the load balancer, and get the value of the Hosted zone field on the Description tab. Elastic Load Balancing API: Use DescribeLoadBalancers to get the applicable value. For more information, see the applicable guide: Classic Load Balancers: Use DescribeLoadBalancers to get the value of CanonicalHostedZoneNameId. Application and Network Load Balancers: Use DescribeLoadBalancers to get the value of CanonicalHostedZoneId. CLI: Use describe-load-balancers to get the applicable value. For more information, see the applicable guide: Classic Load Balancers: Use describe-load-balancers to get the value of CanonicalHostedZoneNameId. Application and Network Load Balancers: Use describe-load-balancers to get the value of CanonicalHostedZoneId. Global Accelerator accelerator Specify Z2BJ6XQ5FK7U4H. An Amazon S3 bucket configured as a static website Specify the hosted zone ID for the region that you created the bucket in. For more information about valid values, see the table Amazon S3 Website Endpoints in the Amazon Web Services General Reference. Another Route 53 resource record set in your hosted zone Specify the hosted zone ID of your hosted zone. (An alias resource record set can't reference a resource record set in a different hosted zone.)"];
      dNSName: DNSName.t
        [@ocaml.doc
          "Alias resource record sets only: The value that you specify depends on where you want to route queries: Amazon API Gateway custom regional APIs and edge-optimized APIs Specify the applicable domain name for your API. You can get the applicable value using the CLI command get-domain-names: For regional APIs, specify the value of regionalDomainName. For edge-optimized APIs, specify the value of distributionDomainName. This is the name of the associated CloudFront distribution, such as da1b2c3d4e5.cloudfront.net. The name of the record that you're creating must match a custom domain name for your API, such as api.example.com. Amazon Virtual Private Cloud interface VPC endpoint Enter the API endpoint for the interface endpoint, such as vpce-123456789abcdef01-example-us-east-1a.elasticloadbalancing.us-east-1.vpce.amazonaws.com. For edge-optimized APIs, this is the domain name for the corresponding CloudFront distribution. You can get the value of DnsName using the CLI command describe-vpc-endpoints. CloudFront distribution Specify the domain name that CloudFront assigned when you created your distribution. Your CloudFront distribution must include an alternate domain name that matches the name of the resource record set. For example, if the name of the resource record set is acme.example.com, your CloudFront distribution must include acme.example.com as one of the alternate domain names. For more information, see Using Alternate Domain Names (CNAMEs) in the Amazon CloudFront Developer Guide. You can't create a resource record set in a private hosted zone to route traffic to a CloudFront distribution. For failover alias records, you can't specify a CloudFront distribution for both the primary and secondary records. A distribution must include an alternate domain name that matches the name of the record. However, the primary and secondary records have the same name, and you can't include the same alternate domain name in more than one distribution. Elastic Beanstalk environment If the domain name for your Elastic Beanstalk environment includes the region that you deployed the environment in, you can create an alias record that routes traffic to the environment. For example, the domain name my-environment.us-west-2.elasticbeanstalk.com is a regionalized domain name. For environments that were created before early 2016, the domain name doesn't include the region. To route traffic to these environments, you must create a CNAME record instead of an alias record. Note that you can't create a CNAME record for the root domain name. For example, if your domain name is example.com, you can create a record that routes traffic for acme.example.com to your Elastic Beanstalk environment, but you can't create a record that routes traffic for example.com to your Elastic Beanstalk environment. For Elastic Beanstalk environments that have regionalized subdomains, specify the CNAME attribute for the environment. You can use the following methods to get the value of the CNAME attribute: Amazon Web Services Management Console: For information about how to get the value by using the console, see Using Custom Domains with Elastic Beanstalk in the Elastic Beanstalk Developer Guide. Elastic Beanstalk API: Use the DescribeEnvironments action to get the value of the CNAME attribute. For more information, see DescribeEnvironments in the Elastic Beanstalk API Reference. CLI: Use the describe-environments command to get the value of the CNAME attribute. For more information, see describe-environments in the CLI Command Reference. ELB load balancer Specify the DNS name that is associated with the load balancer. Get the DNS name by using the Amazon Web Services Management Console, the ELB API, or the CLI. Amazon Web Services Management Console: Go to the EC2 page, choose Load Balancers in the navigation pane, choose the load balancer, choose the Description tab, and get the value of the DNS name field. If you're routing traffic to a Classic Load Balancer, get the value that begins with dualstack. If you're routing traffic to another type of load balancer, get the value that applies to the record type, A or AAAA. Elastic Load Balancing API: Use DescribeLoadBalancers to get the value of DNSName. For more information, see the applicable guide: Classic Load Balancers: DescribeLoadBalancers Application and Network Load Balancers: DescribeLoadBalancers CLI: Use describe-load-balancers to get the value of DNSName. For more information, see the applicable guide: Classic Load Balancers: describe-load-balancers Application and Network Load Balancers: describe-load-balancers Global Accelerator accelerator Specify the DNS name for your accelerator: Global Accelerator API: To get the DNS name, use DescribeAccelerator. CLI: To get the DNS name, use describe-accelerator. Amazon S3 bucket that is configured as a static website Specify the domain name of the Amazon S3 website endpoint that you created the bucket in, for example, s3-website.us-east-2.amazonaws.com. For more information about valid values, see the table Amazon S3 Website Endpoints in the Amazon Web Services General Reference. For more information about using S3 buckets for websites, see Getting Started with Amazon Route 53 in the Amazon Route 53 Developer Guide. Another Route 53 resource record set Specify the value of the Name element for a resource record set in the current hosted zone. If you're creating an alias record that has the same name as the hosted zone (known as the zone apex), you can't specify the domain name for a record for which the value of Type is CNAME. This is because the alias record must have the same type as the record that you're routing traffic to, and creating a CNAME record for the zone apex isn't supported even for an alias record."];
      evaluateTargetHealth: AliasHealthEnabled.t
        [@ocaml.doc
          "Applies only to alias, failover alias, geolocation alias, latency alias, and weighted alias resource record sets: When EvaluateTargetHealth is true, an alias resource record set inherits the health of the referenced Amazon Web Services resource, such as an ELB load balancer or another resource record set in the hosted zone. Note the following: CloudFront distributions You can't set EvaluateTargetHealth to true when the alias target is a CloudFront distribution. Elastic Beanstalk environments that have regionalized subdomains If you specify an Elastic Beanstalk environment in DNSName and the environment contains an ELB load balancer, Elastic Load Balancing routes queries only to the healthy Amazon EC2 instances that are registered with the load balancer. (An environment automatically contains an ELB load balancer if it includes more than one Amazon EC2 instance.) If you set EvaluateTargetHealth to true and either no Amazon EC2 instances are healthy or the load balancer itself is unhealthy, Route 53 routes queries to other available resources that are healthy, if any. If the environment contains a single Amazon EC2 instance, there are no special requirements. ELB load balancers Health checking behavior depends on the type of load balancer: Classic Load Balancers: If you specify an ELB Classic Load Balancer in DNSName, Elastic Load Balancing routes queries only to the healthy Amazon EC2 instances that are registered with the load balancer. If you set EvaluateTargetHealth to true and either no EC2 instances are healthy or the load balancer itself is unhealthy, Route 53 routes queries to other resources. Application and Network Load Balancers: If you specify an ELB Application or Network Load Balancer and you set EvaluateTargetHealth to true, Route 53 routes queries to the load balancer based on the health of the target groups that are associated with the load balancer: For an Application or Network Load Balancer to be considered healthy, every target group that contains targets must contain at least one healthy target. If any target group contains only unhealthy targets, the load balancer is considered unhealthy, and Route 53 routes queries to other resources. A target group that has no registered targets is considered unhealthy. When you create a load balancer, you configure settings for Elastic Load Balancing health checks; they're not Route 53 health checks, but they perform a similar function. Do not create Route 53 health checks for the EC2 instances that you register with an ELB load balancer. S3 buckets There are no special requirements for setting EvaluateTargetHealth to true when the alias target is an S3 bucket. Other records in the same hosted zone If the Amazon Web Services resource that you specify in DNSName is a record or a group of records (for example, a group of weighted records) but is not another alias record, we recommend that you associate a health check with all of the records in the alias target. For more information, see What Happens When You Omit Health Checks? in the Amazon Route 53 Developer Guide. For more information and examples, see Amazon Route 53 Health Checks and DNS Failover in the Amazon Route 53 Developer Guide."]}
    let context_ = "AliasTarget"
    let make ~hostedZoneId =
      fun ~dNSName ->
        fun ~evaluateTargetHealth ->
          fun () -> { hostedZoneId; dNSName; evaluateTargetHealth }
    let to_value x =
      structure_to_value
        [("HostedZoneId", (Some (ResourceId.to_value x.hostedZoneId)));
        ("DNSName", (Some (DNSName.to_value x.dNSName)));
        ("EvaluateTargetHealth",
          (Some (AliasHealthEnabled.to_value x.evaluateTargetHealth)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let evaluateTargetHealth =
        AliasHealthEnabled.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "EvaluateTargetHealth") in
      let dNSName =
        DNSName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "DNSName") in
      let hostedZoneId =
        ResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HostedZoneId") in
      make ~evaluateTargetHealth ~dNSName ~hostedZoneId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let evaluateTargetHealth =
        field_map_exn json "EvaluateTargetHealth" AliasHealthEnabled.of_json in
      let dNSName = field_map_exn json "DNSName" DNSName.of_json in
      let hostedZoneId = field_map_exn json "HostedZoneId" ResourceId.of_json in
      make ~evaluateTargetHealth ~dNSName ~hostedZoneId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Alias resource record sets only: Information about the Amazon Web Services resource, such as a CloudFront distribution or an Amazon S3 bucket, that you want to route traffic to. When creating resource record sets for a private hosted zone, note the following: Creating geolocation alias resource record sets or latency alias resource record sets in a private hosted zone is unsupported. For information about creating failover resource record sets in a private hosted zone, see Configuring Failover in a Private Hosted Zone."]
module GeoLocation =
  struct
    type nonrec t =
      {
      continentCode: GeoLocationContinentCode.t option
        [@ocaml.doc
          "The two-letter code for the continent. Amazon Route 53 supports the following continent codes: AF: Africa AN: Antarctica AS: Asia EU: Europe OC: Oceania NA: North America SA: South America Constraint: Specifying ContinentCode with either CountryCode or SubdivisionCode returns an InvalidInput error."];
      countryCode: GeoLocationCountryCode.t option
        [@ocaml.doc
          "For geolocation resource record sets, the two-letter code for a country. Amazon Route 53 uses the two-letter country codes that are specified in ISO standard 3166-1 alpha-2."];
      subdivisionCode: GeoLocationSubdivisionCode.t option
        [@ocaml.doc
          "For geolocation resource record sets, the two-letter code for a state of the United States. Route 53 doesn't support any other values for SubdivisionCode. For a list of state abbreviations, see Appendix B: Two\226\128\147Letter State and Possession Abbreviations on the United States Postal Service website. If you specify subdivisioncode, you must also specify US for CountryCode."]}
    let make ?continentCode =
      fun ?countryCode ->
        fun ?subdivisionCode ->
          fun () -> { continentCode; countryCode; subdivisionCode }
    let to_value x =
      structure_to_value
        [("ContinentCode",
           (Option.map x.continentCode ~f:GeoLocationContinentCode.to_value));
        ("CountryCode",
          (Option.map x.countryCode ~f:GeoLocationCountryCode.to_value));
        ("SubdivisionCode",
          (Option.map x.subdivisionCode
             ~f:GeoLocationSubdivisionCode.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let subdivisionCode =
        (Option.map ~f:GeoLocationSubdivisionCode.of_xml)
          (Xml.child xml_arg0 "SubdivisionCode") in
      let countryCode =
        (Option.map ~f:GeoLocationCountryCode.of_xml)
          (Xml.child xml_arg0 "CountryCode") in
      let continentCode =
        (Option.map ~f:GeoLocationContinentCode.of_xml)
          (Xml.child xml_arg0 "ContinentCode") in
      make ?subdivisionCode ?countryCode ?continentCode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let subdivisionCode =
        field_map json "SubdivisionCode" GeoLocationSubdivisionCode.of_json in
      let countryCode =
        field_map json "CountryCode" GeoLocationCountryCode.of_json in
      let continentCode =
        field_map json "ContinentCode" GeoLocationContinentCode.of_json in
      make ?subdivisionCode ?countryCode ?continentCode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about a geographic location."]
module RRType =
  struct
    type nonrec t =
      | SOA 
      | A 
      | TXT 
      | NS 
      | CNAME 
      | MX 
      | NAPTR 
      | PTR 
      | SRV 
      | SPF 
      | AAAA 
      | CAA 
      | DS 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | SOA -> "SOA"
      | A -> "A"
      | TXT -> "TXT"
      | NS -> "NS"
      | CNAME -> "CNAME"
      | MX -> "MX"
      | NAPTR -> "NAPTR"
      | PTR -> "PTR"
      | SRV -> "SRV"
      | SPF -> "SPF"
      | AAAA -> "AAAA"
      | CAA -> "CAA"
      | DS -> "DS"
      | Non_static_id s -> s
    let of_string =
      function
      | "SOA" -> SOA
      | "A" -> A
      | "TXT" -> TXT
      | "NS" -> NS
      | "CNAME" -> CNAME
      | "MX" -> MX
      | "NAPTR" -> NAPTR
      | "PTR" -> PTR
      | "SRV" -> SRV
      | "SPF" -> SPF
      | "AAAA" -> AAAA
      | "CAA" -> CAA
      | "DS" -> DS
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration RRType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"RRType" j)
    let to_json = simple_to_json to_value
  end
module ResourceRecordSetFailover =
  struct
    type nonrec t =
      | PRIMARY 
      | SECONDARY 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PRIMARY -> "PRIMARY"
      | SECONDARY -> "SECONDARY"
      | Non_static_id s -> s
    let of_string =
      function
      | "PRIMARY" -> PRIMARY
      | "SECONDARY" -> SECONDARY
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ResourceRecordSetFailover" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ResourceRecordSetFailover" j)
    let to_json = simple_to_json to_value
  end
module ResourceRecordSetIdentifier =
  struct
    type nonrec t = string
    let context_ = "ResourceRecordSetIdentifier"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceRecordSetIdentifier" j
    let to_json = simple_to_json to_value
  end
module ResourceRecordSetMultiValueAnswer =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module ResourceRecordSetRegion =
  struct
    type nonrec t =
      | Us_east_1 
      | Us_east_2 
      | Us_west_1 
      | Us_west_2 
      | Ca_central_1 
      | Eu_west_1 
      | Eu_west_2 
      | Eu_west_3 
      | Eu_central_1 
      | Ap_southeast_1 
      | Ap_southeast_2 
      | Ap_southeast_3 
      | Ap_northeast_1 
      | Ap_northeast_2 
      | Ap_northeast_3 
      | Eu_north_1 
      | Sa_east_1 
      | Cn_north_1 
      | Cn_northwest_1 
      | Ap_east_1 
      | Me_south_1 
      | Ap_south_1 
      | Af_south_1 
      | Eu_south_1 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Us_east_1 -> "us-east-1"
      | Us_east_2 -> "us-east-2"
      | Us_west_1 -> "us-west-1"
      | Us_west_2 -> "us-west-2"
      | Ca_central_1 -> "ca-central-1"
      | Eu_west_1 -> "eu-west-1"
      | Eu_west_2 -> "eu-west-2"
      | Eu_west_3 -> "eu-west-3"
      | Eu_central_1 -> "eu-central-1"
      | Ap_southeast_1 -> "ap-southeast-1"
      | Ap_southeast_2 -> "ap-southeast-2"
      | Ap_southeast_3 -> "ap-southeast-3"
      | Ap_northeast_1 -> "ap-northeast-1"
      | Ap_northeast_2 -> "ap-northeast-2"
      | Ap_northeast_3 -> "ap-northeast-3"
      | Eu_north_1 -> "eu-north-1"
      | Sa_east_1 -> "sa-east-1"
      | Cn_north_1 -> "cn-north-1"
      | Cn_northwest_1 -> "cn-northwest-1"
      | Ap_east_1 -> "ap-east-1"
      | Me_south_1 -> "me-south-1"
      | Ap_south_1 -> "ap-south-1"
      | Af_south_1 -> "af-south-1"
      | Eu_south_1 -> "eu-south-1"
      | Non_static_id s -> s
    let of_string =
      function
      | "us-east-1" -> Us_east_1
      | "us-east-2" -> Us_east_2
      | "us-west-1" -> Us_west_1
      | "us-west-2" -> Us_west_2
      | "ca-central-1" -> Ca_central_1
      | "eu-west-1" -> Eu_west_1
      | "eu-west-2" -> Eu_west_2
      | "eu-west-3" -> Eu_west_3
      | "eu-central-1" -> Eu_central_1
      | "ap-southeast-1" -> Ap_southeast_1
      | "ap-southeast-2" -> Ap_southeast_2
      | "ap-southeast-3" -> Ap_southeast_3
      | "ap-northeast-1" -> Ap_northeast_1
      | "ap-northeast-2" -> Ap_northeast_2
      | "ap-northeast-3" -> Ap_northeast_3
      | "eu-north-1" -> Eu_north_1
      | "sa-east-1" -> Sa_east_1
      | "cn-north-1" -> Cn_north_1
      | "cn-northwest-1" -> Cn_northwest_1
      | "ap-east-1" -> Ap_east_1
      | "me-south-1" -> Me_south_1
      | "ap-south-1" -> Ap_south_1
      | "af-south-1" -> Af_south_1
      | "eu-south-1" -> Eu_south_1
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ResourceRecordSetRegion" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ResourceRecordSetRegion" j)
    let to_json = simple_to_json to_value
  end
module ResourceRecordSetWeight =
  struct
    type nonrec t = Int64.t
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int64_max i ~max:255L) >>=
             (fun () -> check_int64_min i ~min:0L));
        i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module ResourceRecords =
  struct
    type nonrec t = ResourceRecord.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let to_value xs =
      (xs |> (List.map ~f:ResourceRecord.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResourceRecord.of_xml)
    let of_json j =
      list_of_json ~kind:"ResourceRecords" ~of_json:ResourceRecord.of_json j
    let to_json v = composed_to_json to_value v
  end
module TTL =
  struct
    type nonrec t = Int64.t
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int64_max i ~max:2147483647L) >>=
             (fun () -> check_int64_min i ~min:0L));
        i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module TrafficPolicyInstanceId =
  struct
    type nonrec t = string
    let context_ = "TrafficPolicyInstanceId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:36) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TrafficPolicyInstanceId" j
    let to_json = simple_to_json to_value
  end
module Tag =
  struct
    type nonrec t =
      {
      key: TagKey.t option
        [@ocaml.doc
          "The value of Key depends on the operation that you want to perform: Add a tag to a health check or hosted zone: Key is the name that you want to give the new tag. Edit a tag: Key is the name of the tag that you want to change the Value for. Delete a key: Key is the name of the tag you want to remove. Give a name to a health check: Edit the default Name tag. In the Amazon Route 53 console, the list of your health checks includes a Name column that lets you see the name that you've given to each health check."];
      value: TagValue.t option
        [@ocaml.doc
          "The value of Value depends on the operation that you want to perform: Add a tag to a health check or hosted zone: Value is the value that you want to give the new tag. Edit a tag: Value is the new value that you want to assign the tag."]}
    let make ?key = fun ?value -> fun () -> { key; value }
    let to_value x =
      structure_to_value
        [("Key", (Option.map x.key ~f:TagKey.to_value));
        ("Value", (Option.map x.value ~f:TagValue.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        (Option.map ~f:TagValue.of_xml) (Xml.child xml_arg0 "Value") in
      let key = (Option.map ~f:TagKey.of_xml) (Xml.child xml_arg0 "Key") in
      make ?value ?key ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let value = field_map json "Value" TagValue.of_json in
      let key = field_map json "Key" TagKey.of_json in make ?value ?key ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about a tag that you want to add or edit for the specified health check or hosted zone."]
module IsPrivateZone =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module ResourceDescription =
  struct
    type nonrec t = string
    let context_ = "ResourceDescription"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:256); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceDescription" j
    let to_json = simple_to_json to_value
  end
module ServicePrincipal =
  struct
    type nonrec t = string
    let context_ = "ServicePrincipal"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:128); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ServicePrincipal" j
    let to_json = simple_to_json to_value
  end
module AWSAccountID =
  struct
    type nonrec t = string
    let context_ = "AWSAccountID"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AWSAccountID" j
    let to_json = simple_to_json to_value
  end
module HostedZoneOwningService =
  struct
    type nonrec t = string
    let context_ = "HostedZoneOwningService"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:128); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"HostedZoneOwningService" j
    let to_json = simple_to_json to_value
  end
module ComparisonOperator =
  struct
    type nonrec t =
      | GreaterThanOrEqualToThreshold 
      | GreaterThanThreshold 
      | LessThanThreshold 
      | LessThanOrEqualToThreshold 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | GreaterThanOrEqualToThreshold -> "GreaterThanOrEqualToThreshold"
      | GreaterThanThreshold -> "GreaterThanThreshold"
      | LessThanThreshold -> "LessThanThreshold"
      | LessThanOrEqualToThreshold -> "LessThanOrEqualToThreshold"
      | Non_static_id s -> s
    let of_string =
      function
      | "GreaterThanOrEqualToThreshold" -> GreaterThanOrEqualToThreshold
      | "GreaterThanThreshold" -> GreaterThanThreshold
      | "LessThanThreshold" -> LessThanThreshold
      | "LessThanOrEqualToThreshold" -> LessThanOrEqualToThreshold
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ComparisonOperator" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ComparisonOperator" j)
    let to_json = simple_to_json to_value
  end
module DimensionList =
  struct
    type nonrec t = Dimension.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:10); i
    let to_value xs =
      (xs |> (List.map ~f:Dimension.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Dimension.of_xml)
    let of_json j =
      list_of_json ~kind:"DimensionList" ~of_json:Dimension.of_json j
    let to_json v = composed_to_json to_value v
  end
module EvaluationPeriods =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:1); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for EvaluationPeriods" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module MetricName =
  struct
    type nonrec t = string
    let context_ = "MetricName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:255) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MetricName" j
    let to_json = simple_to_json to_value
  end
module Namespace =
  struct
    type nonrec t = string
    let context_ = "Namespace"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:255) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Namespace" j
    let to_json = simple_to_json to_value
  end
module Period =
  struct
    type nonrec t = int
    let make i =
      let open Result in ok_or_failwith (check_int_min i ~min:60); i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Period" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Statistic =
  struct
    type nonrec t =
      | Average 
      | Sum 
      | SampleCount 
      | Maximum 
      | Minimum 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Average -> "Average"
      | Sum -> "Sum"
      | SampleCount -> "SampleCount"
      | Maximum -> "Maximum"
      | Minimum -> "Minimum"
      | Non_static_id s -> s
    let of_string =
      function
      | "Average" -> Average
      | "Sum" -> Sum
      | "SampleCount" -> SampleCount
      | "Maximum" -> Maximum
      | "Minimum" -> Minimum
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration Statistic" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"Statistic" j)
    let to_json = simple_to_json to_value
  end
module Threshold =
  struct
    type nonrec t = float
    let make i = i
    let of_string = Float.of_string
    let to_value x = `Double x
    let to_query v = to_query to_value v
    let to_header x = Stdlib.Float.to_string x
    let of_xml xml_arg0 =
      Float.of_string (string_of_xml ~kind:"a double" xml_arg0)
    let of_json j = float_of_json ~kind:"a double" j
    let to_json = simple_to_json to_value
  end
module AlarmIdentifier =
  struct
    type nonrec t =
      {
      region: CloudWatchRegion.t
        [@ocaml.doc
          "For the CloudWatch alarm that you want Route 53 health checkers to use to determine whether this health check is healthy, the region that the alarm was created in. For the current list of CloudWatch regions, see Amazon CloudWatch endpoints and quotas in the Amazon Web Services General Reference."];
      name: AlarmName.t
        [@ocaml.doc
          "The name of the CloudWatch alarm that you want Amazon Route 53 health checkers to use to determine whether this health check is healthy. Route 53 supports CloudWatch alarms with the following features: Standard-resolution metrics. High-resolution metrics aren't supported. For more information, see High-Resolution Metrics in the Amazon CloudWatch User Guide. Statistics: Average, Minimum, Maximum, Sum, and SampleCount. Extended statistics aren't supported."]}
    let context_ = "AlarmIdentifier"
    let make ~region = fun ~name -> fun () -> { region; name }
    let to_value x =
      structure_to_value
        [("Region", (Some (CloudWatchRegion.to_value x.region)));
        ("Name", (Some (AlarmName.to_value x.name)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        AlarmName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      let region =
        CloudWatchRegion.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Region") in
      make ~name ~region ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let name = field_map_exn json "Name" AlarmName.of_json in
      let region = field_map_exn json "Region" CloudWatchRegion.of_json in
      make ~name ~region ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that identifies the CloudWatch alarm that you want Amazon Route 53 health checkers to use to determine whether the specified health check is healthy."]
module ChildHealthCheckList =
  struct
    type nonrec t = HealthCheckId.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:256); i
    let to_value xs =
      (xs |> (List.map ~f:HealthCheckId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:HealthCheckId.of_xml)
    let of_json j =
      list_of_json ~kind:"ChildHealthCheckList"
        ~of_json:HealthCheckId.of_json j
    let to_json v = composed_to_json to_value v
  end
module Disabled =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module EnableSNI =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module FailureThreshold =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:10) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for FailureThreshold" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module FullyQualifiedDomainName =
  struct
    type nonrec t = string
    let context_ = "FullyQualifiedDomainName"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:255); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"FullyQualifiedDomainName" j
    let to_json = simple_to_json to_value
  end
module HealthCheckRegionList =
  struct
    type nonrec t = HealthCheckRegion.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:64) >>= (fun () -> check_list_min i ~min:3));
        i
    let to_value xs =
      (xs |> (List.map ~f:HealthCheckRegion.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:HealthCheckRegion.of_xml)
    let of_json j =
      list_of_json ~kind:"HealthCheckRegionList"
        ~of_json:HealthCheckRegion.of_json j
    let to_json v = composed_to_json to_value v
  end
module HealthCheckType =
  struct
    type nonrec t =
      | HTTP 
      | HTTPS 
      | HTTP_STR_MATCH 
      | HTTPS_STR_MATCH 
      | TCP 
      | CALCULATED 
      | CLOUDWATCH_METRIC 
      | RECOVERY_CONTROL 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | HTTP -> "HTTP"
      | HTTPS -> "HTTPS"
      | HTTP_STR_MATCH -> "HTTP_STR_MATCH"
      | HTTPS_STR_MATCH -> "HTTPS_STR_MATCH"
      | TCP -> "TCP"
      | CALCULATED -> "CALCULATED"
      | CLOUDWATCH_METRIC -> "CLOUDWATCH_METRIC"
      | RECOVERY_CONTROL -> "RECOVERY_CONTROL"
      | Non_static_id s -> s
    let of_string =
      function
      | "HTTP" -> HTTP
      | "HTTPS" -> HTTPS
      | "HTTP_STR_MATCH" -> HTTP_STR_MATCH
      | "HTTPS_STR_MATCH" -> HTTPS_STR_MATCH
      | "TCP" -> TCP
      | "CALCULATED" -> CALCULATED
      | "CLOUDWATCH_METRIC" -> CLOUDWATCH_METRIC
      | "RECOVERY_CONTROL" -> RECOVERY_CONTROL
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration HealthCheckType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"HealthCheckType" j)
    let to_json = simple_to_json to_value
  end
module HealthThreshold =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:256) >>= (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for HealthThreshold" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module IPAddress =
  struct
    type nonrec t = string
    let context_ = "IPAddress"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:45) >>=
             (fun () ->
                check_pattern i
                  ~pattern:"(^((([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])\\.){3}([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5]))$|^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:)|fe80:(:[0-9a-fA-F]{0,4}){0,4}%[0-9a-zA-Z]{1,}|::(ffff(:0{1,4}){0,1}:){0,1}((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])|([0-9a-fA-F]{1,4}:){1,4}:((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9]))$)"));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"IPAddress" j
    let to_json = simple_to_json to_value
  end
module InsufficientDataHealthStatus =
  struct
    type nonrec t =
      | Healthy 
      | Unhealthy 
      | LastKnownStatus 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Healthy -> "Healthy"
      | Unhealthy -> "Unhealthy"
      | LastKnownStatus -> "LastKnownStatus"
      | Non_static_id s -> s
    let of_string =
      function
      | "Healthy" -> Healthy
      | "Unhealthy" -> Unhealthy
      | "LastKnownStatus" -> LastKnownStatus
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration InsufficientDataHealthStatus"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"InsufficientDataHealthStatus" j)
    let to_json = simple_to_json to_value
  end
module Inverted =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module MeasureLatency =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module Port =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:65535) >>=
             (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string (string_of_xml ~kind:"an integer for Port" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module RequestInterval =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:30) >>= (fun () -> check_int_min i ~min:10));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for RequestInterval" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ResourcePath =
  struct
    type nonrec t = string
    let context_ = "ResourcePath"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:255); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourcePath" j
    let to_json = simple_to_json to_value
  end
module RoutingControlArn =
  struct
    type nonrec t = string
    let context_ = "RoutingControlArn"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:255) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RoutingControlArn" j
    let to_json = simple_to_json to_value
  end
module SearchString =
  struct
    type nonrec t = string
    let context_ = "SearchString"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:255); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SearchString" j
    let to_json = simple_to_json to_value
  end
module Status =
  struct
    type nonrec t = string
    let context_ = "Status"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Status" j
    let to_json = simple_to_json to_value
  end
module TimeStamp =
  struct
    type nonrec t = string
    let make i = i
    let of_string x = x
    let to_value x = `Timestamp x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = string_of_xml ~kind:"a timestamp"
    let of_json = timestamp_of_json
    let to_json = simple_to_json to_value
  end
module ChangeAction =
  struct
    type nonrec t =
      | CREATE 
      | DELETE 
      | UPSERT 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | CREATE -> "CREATE"
      | DELETE -> "DELETE"
      | UPSERT -> "UPSERT"
      | Non_static_id s -> s
    let of_string =
      function
      | "CREATE" -> CREATE
      | "DELETE" -> DELETE
      | "UPSERT" -> UPSERT
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ChangeAction" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ChangeAction" j)
    let to_json = simple_to_json to_value
  end
module ResourceRecordSet =
  struct
    type nonrec t =
      {
      name: DNSName.t
        [@ocaml.doc
          "For ChangeResourceRecordSets requests, the name of the record that you want to create, update, or delete. For ListResourceRecordSets responses, the name of a record in the specified hosted zone. ChangeResourceRecordSets Only Enter a fully qualified domain name, for example, www.example.com. You can optionally include a trailing dot. If you omit the trailing dot, Amazon Route 53 assumes that the domain name that you specify is fully qualified. This means that Route 53 treats www.example.com (without a trailing dot) and www.example.com. (with a trailing dot) as identical. For information about how to specify characters other than a-z, 0-9, and - (hyphen) and how to specify internationalized domain names, see DNS Domain Name Format in the Amazon Route 53 Developer Guide. You can use the asterisk (*) wildcard to replace the leftmost label in a domain name, for example, *.example.com. Note the following: The * must replace the entire label. For example, you can't specify *prod.example.com or prod*.example.com. The * can't replace any of the middle labels, for example, marketing.*.example.com. If you include * in any position other than the leftmost label in a domain name, DNS treats it as an * character (ASCII 42), not as a wildcard. You can't use the * wildcard for resource records sets that have a type of NS. You can use the * wildcard as the leftmost label in a domain name, for example, *.example.com. You can't use an * for one of the middle labels, for example, marketing.*.example.com. In addition, the * must replace the entire label; for example, you can't specify prod*.example.com."];
      type_: RRType.t
        [@ocaml.doc
          "The DNS record type. For information about different record types and how data is encoded for them, see Supported DNS Resource Record Types in the Amazon Route 53 Developer Guide. Valid values for basic resource record sets: A | AAAA | CAA | CNAME | DS |MX | NAPTR | NS | PTR | SOA | SPF | SRV | TXT Values for weighted, latency, geolocation, and failover resource record sets: A | AAAA | CAA | CNAME | MX | NAPTR | PTR | SPF | SRV | TXT. When creating a group of weighted, latency, geolocation, or failover resource record sets, specify the same value for all of the resource record sets in the group. Valid values for multivalue answer resource record sets: A | AAAA | MX | NAPTR | PTR | SPF | SRV | TXT SPF records were formerly used to verify the identity of the sender of email messages. However, we no longer recommend that you create resource record sets for which the value of Type is SPF. RFC 7208, Sender Policy Framework (SPF) for Authorizing Use of Domains in Email, Version 1, has been updated to say, \"...\\[I\\]ts existence and mechanism defined in \\[RFC4408\\] have led to some interoperability issues. Accordingly, its use is no longer appropriate for SPF version 1; implementations are not to use it.\" In RFC 7208, see section 14.1, The SPF DNS Record Type. Values for alias resource record sets: Amazon API Gateway custom regional APIs and edge-optimized APIs: A CloudFront distributions: A If IPv6 is enabled for the distribution, create two resource record sets to route traffic to your distribution, one with a value of A and one with a value of AAAA. Amazon API Gateway environment that has a regionalized subdomain: A ELB load balancers: A | AAAA Amazon S3 buckets: A Amazon Virtual Private Cloud interface VPC endpoints A Another resource record set in this hosted zone: Specify the type of the resource record set that you're creating the alias for. All values are supported except NS and SOA. If you're creating an alias record that has the same name as the hosted zone (known as the zone apex), you can't route traffic to a record for which the value of Type is CNAME. This is because the alias record must have the same type as the record you're routing traffic to, and creating a CNAME record for the zone apex isn't supported even for an alias record."];
      setIdentifier: ResourceRecordSetIdentifier.t option
        [@ocaml.doc
          "Resource record sets that have a routing policy other than simple: An identifier that differentiates among multiple resource record sets that have the same combination of name and type, such as multiple weighted resource record sets named acme.example.com that have a type of A. In a group of resource record sets that have the same name and type, the value of SetIdentifier must be unique for each resource record set. For information about routing policies, see Choosing a Routing Policy in the Amazon Route 53 Developer Guide."];
      weight: ResourceRecordSetWeight.t option
        [@ocaml.doc
          "Weighted resource record sets only: Among resource record sets that have the same combination of DNS name and type, a value that determines the proportion of DNS queries that Amazon Route 53 responds to using the current resource record set. Route 53 calculates the sum of the weights for the resource record sets that have the same combination of DNS name and type. Route 53 then responds to queries based on the ratio of a resource's weight to the total. Note the following: You must specify a value for the Weight element for every weighted resource record set. You can only specify one ResourceRecord per weighted resource record set. You can't create latency, failover, or geolocation resource record sets that have the same values for the Name and Type elements as weighted resource record sets. You can create a maximum of 100 weighted resource record sets that have the same values for the Name and Type elements. For weighted (but not weighted alias) resource record sets, if you set Weight to 0 for a resource record set, Route 53 never responds to queries with the applicable value for that resource record set. However, if you set Weight to 0 for all resource record sets that have the same combination of DNS name and type, traffic is routed to all resources with equal probability. The effect of setting Weight to 0 is different when you associate health checks with weighted resource record sets. For more information, see Options for Configuring Route 53 Active-Active and Active-Passive Failover in the Amazon Route 53 Developer Guide."];
      region: ResourceRecordSetRegion.t option
        [@ocaml.doc
          "Latency-based resource record sets only: The Amazon EC2 Region where you created the resource that this resource record set refers to. The resource typically is an Amazon Web Services resource, such as an EC2 instance or an ELB load balancer, and is referred to by an IP address or a DNS domain name, depending on the record type. Although creating latency and latency alias resource record sets in a private hosted zone is allowed, it's not supported. When Amazon Route 53 receives a DNS query for a domain name and type for which you have created latency resource record sets, Route 53 selects the latency resource record set that has the lowest latency between the end user and the associated Amazon EC2 Region. Route 53 then returns the value that is associated with the selected resource record set. Note the following: You can only specify one ResourceRecord per latency resource record set. You can only create one latency resource record set for each Amazon EC2 Region. You aren't required to create latency resource record sets for all Amazon EC2 Regions. Route 53 will choose the region with the best latency from among the regions that you create latency resource record sets for. You can't create non-latency resource record sets that have the same values for the Name and Type elements as latency resource record sets."];
      geoLocation: GeoLocation.t option
        [@ocaml.doc
          "Geolocation resource record sets only: A complex type that lets you control how Amazon Route 53 responds to DNS queries based on the geographic origin of the query. For example, if you want all queries from Africa to be routed to a web server with an IP address of 192.0.2.111, create a resource record set with a Type of A and a ContinentCode of AF. Although creating geolocation and geolocation alias resource record sets in a private hosted zone is allowed, it's not supported. If you create separate resource record sets for overlapping geographic regions (for example, one resource record set for a continent and one for a country on the same continent), priority goes to the smallest geographic region. This allows you to route most queries for a continent to one resource and to route queries for a country on that continent to a different resource. You can't create two geolocation resource record sets that specify the same geographic location. The value * in the CountryCode element matches all geographic locations that aren't specified in other geolocation resource record sets that have the same values for the Name and Type elements. Geolocation works by mapping IP addresses to locations. However, some IP addresses aren't mapped to geographic locations, so even if you create geolocation resource record sets that cover all seven continents, Route 53 will receive some DNS queries from locations that it can't identify. We recommend that you create a resource record set for which the value of CountryCode is *. Two groups of queries are routed to the resource that you specify in this record: queries that come from locations for which you haven't created geolocation resource record sets and queries from IP addresses that aren't mapped to a location. If you don't create a * resource record set, Route 53 returns a \"no answer\" response for queries from those locations. You can't create non-geolocation resource record sets that have the same values for the Name and Type elements as geolocation resource record sets."];
      failover: ResourceRecordSetFailover.t option
        [@ocaml.doc
          "Failover resource record sets only: To configure failover, you add the Failover element to two resource record sets. For one resource record set, you specify PRIMARY as the value for Failover; for the other resource record set, you specify SECONDARY. In addition, you include the HealthCheckId element and specify the health check that you want Amazon Route 53 to perform for each resource record set. Except where noted, the following failover behaviors assume that you have included the HealthCheckId element in both resource record sets: When the primary resource record set is healthy, Route 53 responds to DNS queries with the applicable value from the primary resource record set regardless of the health of the secondary resource record set. When the primary resource record set is unhealthy and the secondary resource record set is healthy, Route 53 responds to DNS queries with the applicable value from the secondary resource record set. When the secondary resource record set is unhealthy, Route 53 responds to DNS queries with the applicable value from the primary resource record set regardless of the health of the primary resource record set. If you omit the HealthCheckId element for the secondary resource record set, and if the primary resource record set is unhealthy, Route 53 always responds to DNS queries with the applicable value from the secondary resource record set. This is true regardless of the health of the associated endpoint. You can't create non-failover resource record sets that have the same values for the Name and Type elements as failover resource record sets. For failover alias resource record sets, you must also include the EvaluateTargetHealth element and set the value to true. For more information about configuring failover for Route 53, see the following topics in the Amazon Route 53 Developer Guide: Route 53 Health Checks and DNS Failover Configuring Failover in a Private Hosted Zone"];
      multiValueAnswer: ResourceRecordSetMultiValueAnswer.t option
        [@ocaml.doc
          "Multivalue answer resource record sets only: To route traffic approximately randomly to multiple resources, such as web servers, create one multivalue answer record for each resource and specify true for MultiValueAnswer. Note the following: If you associate a health check with a multivalue answer resource record set, Amazon Route 53 responds to DNS queries with the corresponding IP address only when the health check is healthy. If you don't associate a health check with a multivalue answer record, Route 53 always considers the record to be healthy. Route 53 responds to DNS queries with up to eight healthy records; if you have eight or fewer healthy records, Route 53 responds to all DNS queries with all the healthy records. If you have more than eight healthy records, Route 53 responds to different DNS resolvers with different combinations of healthy records. When all records are unhealthy, Route 53 responds to DNS queries with up to eight unhealthy records. If a resource becomes unavailable after a resolver caches a response, client software typically tries another of the IP addresses in the response. You can't create multivalue answer alias records."];
      tTL: TTL.t option
        [@ocaml.doc
          "The resource record cache time to live (TTL), in seconds. Note the following: If you're creating or updating an alias resource record set, omit TTL. Amazon Route 53 uses the value of TTL for the alias target. If you're associating this resource record set with a health check (if you're adding a HealthCheckId element), we recommend that you specify a TTL of 60 seconds or less so clients respond quickly to changes in health status. All of the resource record sets in a group of weighted resource record sets must have the same value for TTL. If a group of weighted resource record sets includes one or more weighted alias resource record sets for which the alias target is an ELB load balancer, we recommend that you specify a TTL of 60 seconds for all of the non-alias weighted resource record sets that have the same name and type. Values other than 60 seconds (the TTL for load balancers) will change the effect of the values that you specify for Weight."];
      resourceRecords: ResourceRecords.t option
        [@ocaml.doc
          "Information about the resource records to act upon. If you're creating an alias resource record set, omit ResourceRecords."];
      aliasTarget: AliasTarget.t option
        [@ocaml.doc
          "Alias resource record sets only: Information about the Amazon Web Services resource, such as a CloudFront distribution or an Amazon S3 bucket, that you want to route traffic to. If you're creating resource records sets for a private hosted zone, note the following: You can't create an alias resource record set in a private hosted zone to route traffic to a CloudFront distribution. Creating geolocation alias resource record sets or latency alias resource record sets in a private hosted zone is unsupported. For information about creating failover resource record sets in a private hosted zone, see Configuring Failover in a Private Hosted Zone in the Amazon Route 53 Developer Guide."];
      healthCheckId: HealthCheckId.t option
        [@ocaml.doc
          "If you want Amazon Route 53 to return this resource record set in response to a DNS query only when the status of a health check is healthy, include the HealthCheckId element and specify the ID of the applicable health check. Route 53 determines whether a resource record set is healthy based on one of the following: By periodically sending a request to the endpoint that is specified in the health check By aggregating the status of a specified group of health checks (calculated health checks) By determining the current state of a CloudWatch alarm (CloudWatch metric health checks) Route 53 doesn't check the health of the endpoint that is specified in the resource record set, for example, the endpoint specified by the IP address in the Value element. When you add a HealthCheckId element to a resource record set, Route 53 checks the health of the endpoint that you specified in the health check. For more information, see the following topics in the Amazon Route 53 Developer Guide: How Amazon Route 53 Determines Whether an Endpoint Is Healthy Route 53 Health Checks and DNS Failover Configuring Failover in a Private Hosted Zone When to Specify HealthCheckId Specifying a value for HealthCheckId is useful only when Route 53 is choosing between two or more resource record sets to respond to a DNS query, and you want Route 53 to base the choice in part on the status of a health check. Configuring health checks makes sense only in the following configurations: Non-alias resource record sets: You're checking the health of a group of non-alias resource record sets that have the same routing policy, name, and type (such as multiple weighted records named www.example.com with a type of A) and you specify health check IDs for all the resource record sets. If the health check status for a resource record set is healthy, Route 53 includes the record among the records that it responds to DNS queries with. If the health check status for a resource record set is unhealthy, Route 53 stops responding to DNS queries using the value for that resource record set. If the health check status for all resource record sets in the group is unhealthy, Route 53 considers all resource record sets in the group healthy and responds to DNS queries accordingly. Alias resource record sets: You specify the following settings: You set EvaluateTargetHealth to true for an alias resource record set in a group of resource record sets that have the same routing policy, name, and type (such as multiple weighted records named www.example.com with a type of A). You configure the alias resource record set to route traffic to a non-alias resource record set in the same hosted zone. You specify a health check ID for the non-alias resource record set. If the health check status is healthy, Route 53 considers the alias resource record set to be healthy and includes the alias record among the records that it responds to DNS queries with. If the health check status is unhealthy, Route 53 stops responding to DNS queries using the alias resource record set. The alias resource record set can also route traffic to a group of non-alias resource record sets that have the same routing policy, name, and type. In that configuration, associate health checks with all of the resource record sets in the group of non-alias resource record sets. Geolocation Routing For geolocation resource record sets, if an endpoint is unhealthy, Route 53 looks for a resource record set for the larger, associated geographic region. For example, suppose you have resource record sets for a state in the United States, for the entire United States, for North America, and a resource record set that has * for CountryCode is *, which applies to all locations. If the endpoint for the state resource record set is unhealthy, Route 53 checks for healthy resource record sets in the following order until it finds a resource record set for which the endpoint is healthy: The United States North America The default resource record set Specifying the Health Check Endpoint by Domain Name If your health checks specify the endpoint only by domain name, we recommend that you create a separate health check for each endpoint. For example, create a health check for each HTTP server that is serving content for www.example.com. For the value of FullyQualifiedDomainName, specify the domain name of the server (such as us-east-2-www.example.com), not the name of the resource record sets (www.example.com). Health check results will be unpredictable if you do the following: Create a health check that has the same value for FullyQualifiedDomainName as the name of a resource record set. Associate that health check with the resource record set."];
      trafficPolicyInstanceId: TrafficPolicyInstanceId.t option
        [@ocaml.doc
          "When you create a traffic policy instance, Amazon Route 53 automatically creates a resource record set. TrafficPolicyInstanceId is the ID of the traffic policy instance that Route 53 created this resource record set for. To delete the resource record set that is associated with a traffic policy instance, use DeleteTrafficPolicyInstance. Route 53 will delete the resource record set automatically. If you delete the resource record set by using ChangeResourceRecordSets, Route 53 doesn't automatically delete the traffic policy instance, and you'll continue to be charged for it even though it's no longer in use."]}
    let context_ = "ResourceRecordSet"
    let make ?setIdentifier =
      fun ?weight ->
        fun ?region ->
          fun ?geoLocation ->
            fun ?failover ->
              fun ?multiValueAnswer ->
                fun ?tTL ->
                  fun ?resourceRecords ->
                    fun ?aliasTarget ->
                      fun ?healthCheckId ->
                        fun ?trafficPolicyInstanceId ->
                          fun ~name ->
                            fun ~type_ ->
                              fun () ->
                                {
                                  setIdentifier;
                                  weight;
                                  region;
                                  geoLocation;
                                  failover;
                                  multiValueAnswer;
                                  tTL;
                                  resourceRecords;
                                  aliasTarget;
                                  healthCheckId;
                                  trafficPolicyInstanceId;
                                  name;
                                  type_
                                }
    let to_value x =
      structure_to_value
        [("Name", (Some (DNSName.to_value x.name)));
        ("Type", (Some (RRType.to_value x.type_)));
        ("SetIdentifier",
          (Option.map x.setIdentifier ~f:ResourceRecordSetIdentifier.to_value));
        ("Weight", (Option.map x.weight ~f:ResourceRecordSetWeight.to_value));
        ("Region", (Option.map x.region ~f:ResourceRecordSetRegion.to_value));
        ("GeoLocation", (Option.map x.geoLocation ~f:GeoLocation.to_value));
        ("Failover",
          (Option.map x.failover ~f:ResourceRecordSetFailover.to_value));
        ("MultiValueAnswer",
          (Option.map x.multiValueAnswer
             ~f:ResourceRecordSetMultiValueAnswer.to_value));
        ("TTL", (Option.map x.tTL ~f:TTL.to_value));
        ("ResourceRecords",
          (Option.map x.resourceRecords ~f:ResourceRecords.to_value));
        ("AliasTarget", (Option.map x.aliasTarget ~f:AliasTarget.to_value));
        ("HealthCheckId",
          (Option.map x.healthCheckId ~f:HealthCheckId.to_value));
        ("TrafficPolicyInstanceId",
          (Option.map x.trafficPolicyInstanceId
             ~f:TrafficPolicyInstanceId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trafficPolicyInstanceId =
        (Option.map ~f:TrafficPolicyInstanceId.of_xml)
          (Xml.child xml_arg0 "TrafficPolicyInstanceId") in
      let healthCheckId =
        (Option.map ~f:HealthCheckId.of_xml)
          (Xml.child xml_arg0 "HealthCheckId") in
      let aliasTarget =
        (Option.map ~f:AliasTarget.of_xml) (Xml.child xml_arg0 "AliasTarget") in
      let resourceRecords =
        (Option.map ~f:ResourceRecords.of_xml)
          (Xml.child xml_arg0 "ResourceRecords") in
      let tTL = (Option.map ~f:TTL.of_xml) (Xml.child xml_arg0 "TTL") in
      let multiValueAnswer =
        (Option.map ~f:ResourceRecordSetMultiValueAnswer.of_xml)
          (Xml.child xml_arg0 "MultiValueAnswer") in
      let failover =
        (Option.map ~f:ResourceRecordSetFailover.of_xml)
          (Xml.child xml_arg0 "Failover") in
      let geoLocation =
        (Option.map ~f:GeoLocation.of_xml) (Xml.child xml_arg0 "GeoLocation") in
      let region =
        (Option.map ~f:ResourceRecordSetRegion.of_xml)
          (Xml.child xml_arg0 "Region") in
      let weight =
        (Option.map ~f:ResourceRecordSetWeight.of_xml)
          (Xml.child xml_arg0 "Weight") in
      let setIdentifier =
        (Option.map ~f:ResourceRecordSetIdentifier.of_xml)
          (Xml.child xml_arg0 "SetIdentifier") in
      let type_ =
        RRType.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Type") in
      let name =
        DNSName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ?trafficPolicyInstanceId ?healthCheckId ?aliasTarget
        ?resourceRecords ?tTL ?multiValueAnswer ?failover ?geoLocation
        ?region ?weight ?setIdentifier ~type_ ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let trafficPolicyInstanceId =
        field_map json "TrafficPolicyInstanceId"
          TrafficPolicyInstanceId.of_json in
      let healthCheckId =
        field_map json "HealthCheckId" HealthCheckId.of_json in
      let aliasTarget = field_map json "AliasTarget" AliasTarget.of_json in
      let resourceRecords =
        field_map json "ResourceRecords" ResourceRecords.of_json in
      let tTL = field_map json "TTL" TTL.of_json in
      let multiValueAnswer =
        field_map json "MultiValueAnswer"
          ResourceRecordSetMultiValueAnswer.of_json in
      let failover =
        field_map json "Failover" ResourceRecordSetFailover.of_json in
      let geoLocation = field_map json "GeoLocation" GeoLocation.of_json in
      let region = field_map json "Region" ResourceRecordSetRegion.of_json in
      let weight = field_map json "Weight" ResourceRecordSetWeight.of_json in
      let setIdentifier =
        field_map json "SetIdentifier" ResourceRecordSetIdentifier.of_json in
      let type_ = field_map_exn json "Type" RRType.of_json in
      let name = field_map_exn json "Name" DNSName.of_json in
      make ?trafficPolicyInstanceId ?healthCheckId ?aliasTarget
        ?resourceRecords ?tTL ?multiValueAnswer ?failover ?geoLocation
        ?region ?weight ?setIdentifier ~type_ ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Information about the resource record set to create or delete."]
module VPCId =
  struct
    type nonrec t = string[@@ocaml.doc
                            "(Private hosted zones only) The ID of an Amazon VPC."]
    let context_ = "VPCId"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:1024); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"VPCId" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc "(Private hosted zones only) The ID of an Amazon VPC."]
module VPCRegion =
  struct
    type nonrec t =
      | Us_east_1 
      | Us_east_2 
      | Us_west_1 
      | Us_west_2 
      | Eu_west_1 
      | Eu_west_2 
      | Eu_west_3 
      | Eu_central_1 
      | Ap_east_1 
      | Me_south_1 
      | Us_gov_west_1 
      | Us_gov_east_1 
      | Us_iso_east_1 
      | Us_iso_west_1 
      | Us_isob_east_1 
      | Ap_southeast_1 
      | Ap_southeast_2 
      | Ap_southeast_3 
      | Ap_south_1 
      | Ap_northeast_1 
      | Ap_northeast_2 
      | Ap_northeast_3 
      | Eu_north_1 
      | Sa_east_1 
      | Ca_central_1 
      | Cn_north_1 
      | Af_south_1 
      | Eu_south_1 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Us_east_1 -> "us-east-1"
      | Us_east_2 -> "us-east-2"
      | Us_west_1 -> "us-west-1"
      | Us_west_2 -> "us-west-2"
      | Eu_west_1 -> "eu-west-1"
      | Eu_west_2 -> "eu-west-2"
      | Eu_west_3 -> "eu-west-3"
      | Eu_central_1 -> "eu-central-1"
      | Ap_east_1 -> "ap-east-1"
      | Me_south_1 -> "me-south-1"
      | Us_gov_west_1 -> "us-gov-west-1"
      | Us_gov_east_1 -> "us-gov-east-1"
      | Us_iso_east_1 -> "us-iso-east-1"
      | Us_iso_west_1 -> "us-iso-west-1"
      | Us_isob_east_1 -> "us-isob-east-1"
      | Ap_southeast_1 -> "ap-southeast-1"
      | Ap_southeast_2 -> "ap-southeast-2"
      | Ap_southeast_3 -> "ap-southeast-3"
      | Ap_south_1 -> "ap-south-1"
      | Ap_northeast_1 -> "ap-northeast-1"
      | Ap_northeast_2 -> "ap-northeast-2"
      | Ap_northeast_3 -> "ap-northeast-3"
      | Eu_north_1 -> "eu-north-1"
      | Sa_east_1 -> "sa-east-1"
      | Ca_central_1 -> "ca-central-1"
      | Cn_north_1 -> "cn-north-1"
      | Af_south_1 -> "af-south-1"
      | Eu_south_1 -> "eu-south-1"
      | Non_static_id s -> s
    let of_string =
      function
      | "us-east-1" -> Us_east_1
      | "us-east-2" -> Us_east_2
      | "us-west-1" -> Us_west_1
      | "us-west-2" -> Us_west_2
      | "eu-west-1" -> Eu_west_1
      | "eu-west-2" -> Eu_west_2
      | "eu-west-3" -> Eu_west_3
      | "eu-central-1" -> Eu_central_1
      | "ap-east-1" -> Ap_east_1
      | "me-south-1" -> Me_south_1
      | "us-gov-west-1" -> Us_gov_west_1
      | "us-gov-east-1" -> Us_gov_east_1
      | "us-iso-east-1" -> Us_iso_east_1
      | "us-iso-west-1" -> Us_iso_west_1
      | "us-isob-east-1" -> Us_isob_east_1
      | "ap-southeast-1" -> Ap_southeast_1
      | "ap-southeast-2" -> Ap_southeast_2
      | "ap-southeast-3" -> Ap_southeast_3
      | "ap-south-1" -> Ap_south_1
      | "ap-northeast-1" -> Ap_northeast_1
      | "ap-northeast-2" -> Ap_northeast_2
      | "ap-northeast-3" -> Ap_northeast_3
      | "eu-north-1" -> Eu_north_1
      | "sa-east-1" -> Sa_east_1
      | "ca-central-1" -> Ca_central_1
      | "cn-north-1" -> Cn_north_1
      | "af-south-1" -> Af_south_1
      | "eu-south-1" -> Eu_south_1
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration VPCRegion" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"VPCRegion" j)
    let to_json = simple_to_json to_value
  end
module TrafficPolicyComment =
  struct
    type nonrec t = string
    let context_ = "TrafficPolicyComment"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:1024); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TrafficPolicyComment" j
    let to_json = simple_to_json to_value
  end
module TrafficPolicyDocument =
  struct
    type nonrec t = string
    let context_ = "TrafficPolicyDocument"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:102400); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TrafficPolicyDocument" j
    let to_json = simple_to_json to_value
  end
module TrafficPolicyId =
  struct
    type nonrec t = string
    let context_ = "TrafficPolicyId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:36) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TrafficPolicyId" j
    let to_json = simple_to_json to_value
  end
module TrafficPolicyName =
  struct
    type nonrec t = string
    let context_ = "TrafficPolicyName"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:512); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TrafficPolicyName" j
    let to_json = simple_to_json to_value
  end
module TrafficPolicyVersion =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:1000) >>= (fun () -> check_int_min i ~min:1));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for TrafficPolicyVersion" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module Message =
  struct
    type nonrec t = string
    let context_ = "Message"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:1024); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Message" j
    let to_json = simple_to_json to_value
  end
module TrafficPolicyInstanceState =
  struct
    type nonrec t = string
    let context_ = "TrafficPolicyInstanceState"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TrafficPolicyInstanceState" j
    let to_json = simple_to_json to_value
  end
module TagList =
  struct
    type nonrec t = Tag.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:Tag.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Tag.of_xml)
    let of_json j = list_of_json ~kind:"TagList" ~of_json:Tag.of_json j
    let to_json v = composed_to_json to_value v
  end
module TagResourceId =
  struct
    type nonrec t = string
    let context_ = "TagResourceId"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:64); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TagResourceId" j
    let to_json = simple_to_json to_value
  end
module TagResourceType =
  struct
    type nonrec t =
      | Healthcheck 
      | Hostedzone 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | Healthcheck -> "healthcheck"
      | Hostedzone -> "hostedzone"
      | Non_static_id s -> s
    let of_string =
      function
      | "healthcheck" -> Healthcheck
      | "hostedzone" -> Hostedzone
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration TagResourceType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"TagResourceType" j)
    let to_json = simple_to_json to_value
  end
module DelegationSetNameServers =
  struct
    type nonrec t = DNSName.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let to_value xs =
      (xs |> (List.map ~f:DNSName.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DNSName.of_xml)
    let of_json j =
      list_of_json ~kind:"DelegationSetNameServers" ~of_json:DNSName.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module Nonce =
  struct
    type nonrec t = string
    let context_ = "Nonce"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Nonce" j
    let to_json = simple_to_json to_value
  end
module CloudWatchLogsLogGroupArn =
  struct
    type nonrec t = string
    let context_ = "CloudWatchLogsLogGroupArn"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"CloudWatchLogsLogGroupArn" j
    let to_json = simple_to_json to_value
  end
module QueryLoggingConfigId =
  struct
    type nonrec t = string
    let context_ = "QueryLoggingConfigId"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:36) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"QueryLoggingConfigId" j
    let to_json = simple_to_json to_value
  end
module HostedZoneConfig =
  struct
    type nonrec t =
      {
      comment: ResourceDescription.t option
        [@ocaml.doc
          "Any comments that you want to include about the hosted zone."];
      privateZone: IsPrivateZone.t option
        [@ocaml.doc
          "A value that indicates whether this is a private hosted zone."]}
    let make ?comment =
      fun ?privateZone -> fun () -> { comment; privateZone }
    let to_value x =
      structure_to_value
        [("Comment", (Option.map x.comment ~f:ResourceDescription.to_value));
        ("PrivateZone", (Option.map x.privateZone ~f:IsPrivateZone.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let privateZone =
        (Option.map ~f:IsPrivateZone.of_xml)
          (Xml.child xml_arg0 "PrivateZone") in
      let comment =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Comment") in
      make ?privateZone ?comment ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let privateZone = field_map json "PrivateZone" IsPrivateZone.of_json in
      let comment = field_map json "Comment" ResourceDescription.of_json in
      make ?privateZone ?comment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains an optional comment about your hosted zone. If you don't want to specify a comment, omit both the HostedZoneConfig and Comment elements."]
module HostedZoneRRSetCount =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module LinkedService =
  struct
    type nonrec t =
      {
      servicePrincipal: ServicePrincipal.t option
        [@ocaml.doc
          "If the health check or hosted zone was created by another service, the service that created the resource. When a resource is created by another service, you can't edit or delete it using Amazon Route 53."];
      description: ResourceDescription.t option
        [@ocaml.doc
          "If the health check or hosted zone was created by another service, an optional description that can be provided by the other service. When a resource is created by another service, you can't edit or delete it using Amazon Route 53."]}
    let make ?servicePrincipal =
      fun ?description -> fun () -> { servicePrincipal; description }
    let to_value x =
      structure_to_value
        [("ServicePrincipal",
           (Option.map x.servicePrincipal ~f:ServicePrincipal.to_value));
        ("Description",
          (Option.map x.description ~f:ResourceDescription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let description =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Description") in
      let servicePrincipal =
        (Option.map ~f:ServicePrincipal.of_xml)
          (Xml.child xml_arg0 "ServicePrincipal") in
      make ?description ?servicePrincipal ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let description =
        field_map json "Description" ResourceDescription.of_json in
      let servicePrincipal =
        field_map json "ServicePrincipal" ServicePrincipal.of_json in
      make ?description ?servicePrincipal ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "If a health check or hosted zone was created by another service, LinkedService is a complex type that describes the service that created the resource. When a resource is created by another service, you can't edit or delete it using Amazon Route 53."]
module HostedZoneOwner =
  struct
    type nonrec t =
      {
      owningAccount: AWSAccountID.t option
        [@ocaml.doc
          "If the hosted zone was created by an Amazon Web Services account, or was created by an Amazon Web Services service that creates hosted zones using the current account, OwningAccount contains the account ID of that account. For example, when you use Cloud Map to create a hosted zone, Cloud Map creates the hosted zone using the current Amazon Web Services account."];
      owningService: HostedZoneOwningService.t option
        [@ocaml.doc
          "If an Amazon Web Services service uses its own account to create a hosted zone and associate the specified VPC with that hosted zone, OwningService contains an abbreviation that identifies the service. For example, if Amazon Elastic File System (Amazon EFS) created a hosted zone and associated a VPC with the hosted zone, the value of OwningService is efs.amazonaws.com."]}
    let make ?owningAccount =
      fun ?owningService -> fun () -> { owningAccount; owningService }
    let to_value x =
      structure_to_value
        [("OwningAccount",
           (Option.map x.owningAccount ~f:AWSAccountID.to_value));
        ("OwningService",
          (Option.map x.owningService ~f:HostedZoneOwningService.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let owningService =
        (Option.map ~f:HostedZoneOwningService.of_xml)
          (Xml.child xml_arg0 "OwningService") in
      let owningAccount =
        (Option.map ~f:AWSAccountID.of_xml)
          (Xml.child xml_arg0 "OwningAccount") in
      make ?owningService ?owningAccount ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let owningService =
        field_map json "OwningService" HostedZoneOwningService.of_json in
      let owningAccount = field_map json "OwningAccount" AWSAccountID.of_json in
      make ?owningService ?owningAccount ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that identifies a hosted zone that a specified Amazon VPC is associated with and the owner of the hosted zone. If there is a value for OwningAccount, there is no value for OwningService, and vice versa."]
module CloudWatchAlarmConfiguration =
  struct
    type nonrec t =
      {
      evaluationPeriods: EvaluationPeriods.t
        [@ocaml.doc
          "For the metric that the CloudWatch alarm is associated with, the number of periods that the metric is compared to the threshold."];
      threshold: Threshold.t
        [@ocaml.doc
          "For the metric that the CloudWatch alarm is associated with, the value the metric is compared with."];
      comparisonOperator: ComparisonOperator.t
        [@ocaml.doc
          "For the metric that the CloudWatch alarm is associated with, the arithmetic operation that is used for the comparison."];
      period: Period.t
        [@ocaml.doc
          "For the metric that the CloudWatch alarm is associated with, the duration of one evaluation period in seconds."];
      metricName: MetricName.t
        [@ocaml.doc
          "The name of the CloudWatch metric that the alarm is associated with."];
      namespace: Namespace.t
        [@ocaml.doc
          "The namespace of the metric that the alarm is associated with. For more information, see Amazon CloudWatch Namespaces, Dimensions, and Metrics Reference in the Amazon CloudWatch User Guide."];
      statistic: Statistic.t
        [@ocaml.doc
          "For the metric that the CloudWatch alarm is associated with, the statistic that is applied to the metric."];
      dimensions: DimensionList.t option
        [@ocaml.doc
          "For the metric that the CloudWatch alarm is associated with, a complex type that contains information about the dimensions for the metric. For information, see Amazon CloudWatch Namespaces, Dimensions, and Metrics Reference in the Amazon CloudWatch User Guide."]}
    let context_ = "CloudWatchAlarmConfiguration"
    let make ?dimensions =
      fun ~evaluationPeriods ->
        fun ~threshold ->
          fun ~comparisonOperator ->
            fun ~period ->
              fun ~metricName ->
                fun ~namespace ->
                  fun ~statistic ->
                    fun () ->
                      {
                        dimensions;
                        evaluationPeriods;
                        threshold;
                        comparisonOperator;
                        period;
                        metricName;
                        namespace;
                        statistic
                      }
    let to_value x =
      structure_to_value
        [("EvaluationPeriods",
           (Some (EvaluationPeriods.to_value x.evaluationPeriods)));
        ("Threshold", (Some (Threshold.to_value x.threshold)));
        ("ComparisonOperator",
          (Some (ComparisonOperator.to_value x.comparisonOperator)));
        ("Period", (Some (Period.to_value x.period)));
        ("MetricName", (Some (MetricName.to_value x.metricName)));
        ("Namespace", (Some (Namespace.to_value x.namespace)));
        ("Statistic", (Some (Statistic.to_value x.statistic)));
        ("Dimensions", (Option.map x.dimensions ~f:DimensionList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let dimensions =
        (Option.map ~f:DimensionList.of_xml)
          (Xml.child xml_arg0 "Dimensions") in
      let statistic =
        Statistic.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Statistic") in
      let namespace =
        Namespace.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Namespace") in
      let metricName =
        MetricName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MetricName") in
      let period =
        Period.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Period") in
      let comparisonOperator =
        ComparisonOperator.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ComparisonOperator") in
      let threshold =
        Threshold.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Threshold") in
      let evaluationPeriods =
        EvaluationPeriods.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "EvaluationPeriods") in
      make ?dimensions ~statistic ~namespace ~metricName ~period
        ~comparisonOperator ~threshold ~evaluationPeriods ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let dimensions = field_map json "Dimensions" DimensionList.of_json in
      let statistic = field_map_exn json "Statistic" Statistic.of_json in
      let namespace = field_map_exn json "Namespace" Namespace.of_json in
      let metricName = field_map_exn json "MetricName" MetricName.of_json in
      let period = field_map_exn json "Period" Period.of_json in
      let comparisonOperator =
        field_map_exn json "ComparisonOperator" ComparisonOperator.of_json in
      let threshold = field_map_exn json "Threshold" Threshold.of_json in
      let evaluationPeriods =
        field_map_exn json "EvaluationPeriods" EvaluationPeriods.of_json in
      make ?dimensions ~statistic ~namespace ~metricName ~period
        ~comparisonOperator ~threshold ~evaluationPeriods ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about the CloudWatch alarm that Amazon Route 53 is monitoring for this health check."]
module HealthCheckConfig =
  struct
    type nonrec t =
      {
      iPAddress: IPAddress.t option
        [@ocaml.doc
          "The IPv4 or IPv6 IP address of the endpoint that you want Amazon Route 53 to perform health checks on. If you don't specify a value for IPAddress, Route 53 sends a DNS request to resolve the domain name that you specify in FullyQualifiedDomainName at the interval that you specify in RequestInterval. Using an IP address returned by DNS, Route 53 then checks the health of the endpoint. Use one of the following formats for the value of IPAddress: IPv4 address: four values between 0 and 255, separated by periods (.), for example, 192.0.2.44. IPv6 address: eight groups of four hexadecimal values, separated by colons (:), for example, 2001:0db8:85a3:0000:0000:abcd:0001:2345. You can also shorten IPv6 addresses as described in RFC 5952, for example, 2001:db8:85a3::abcd:1:2345. If the endpoint is an EC2 instance, we recommend that you create an Elastic IP address, associate it with your EC2 instance, and specify the Elastic IP address for IPAddress. This ensures that the IP address of your instance will never change. For more information, see FullyQualifiedDomainName. Constraints: Route 53 can't check the health of endpoints for which the IP address is in local, private, non-routable, or multicast ranges. For more information about IP addresses for which you can't create health checks, see the following documents: RFC 5735, Special Use IPv4 Addresses RFC 6598, IANA-Reserved IPv4 Prefix for Shared Address Space RFC 5156, Special-Use IPv6 Addresses When the value of Type is CALCULATED or CLOUDWATCH_METRIC, omit IPAddress."];
      port: Port.t option
        [@ocaml.doc
          "The port on the endpoint that you want Amazon Route 53 to perform health checks on. Don't specify a value for Port when you specify a value for Type of CLOUDWATCH_METRIC or CALCULATED."];
      type_: HealthCheckType.t
        [@ocaml.doc
          "The type of health check that you want to create, which indicates how Amazon Route 53 determines whether an endpoint is healthy. You can't change the value of Type after you create a health check. You can create the following types of health checks: HTTP: Route 53 tries to establish a TCP connection. If successful, Route 53 submits an HTTP request and waits for an HTTP status code of 200 or greater and less than 400. HTTPS: Route 53 tries to establish a TCP connection. If successful, Route 53 submits an HTTPS request and waits for an HTTP status code of 200 or greater and less than 400. If you specify HTTPS for the value of Type, the endpoint must support TLS v1.0 or later. HTTP_STR_MATCH: Route 53 tries to establish a TCP connection. If successful, Route 53 submits an HTTP request and searches the first 5,120 bytes of the response body for the string that you specify in SearchString. HTTPS_STR_MATCH: Route 53 tries to establish a TCP connection. If successful, Route 53 submits an HTTPS request and searches the first 5,120 bytes of the response body for the string that you specify in SearchString. TCP: Route 53 tries to establish a TCP connection. CLOUDWATCH_METRIC: The health check is associated with a CloudWatch alarm. If the state of the alarm is OK, the health check is considered healthy. If the state is ALARM, the health check is considered unhealthy. If CloudWatch doesn't have sufficient data to determine whether the state is OK or ALARM, the health check status depends on the setting for InsufficientDataHealthStatus: Healthy, Unhealthy, or LastKnownStatus. CALCULATED: For health checks that monitor the status of other health checks, Route 53 adds up the number of health checks that Route 53 health checkers consider to be healthy and compares that number with the value of HealthThreshold. RECOVERY_CONTROL: The health check is assocated with a Route53 Application Recovery Controller routing control. If the routing control state is ON, the health check is considered healthy. If the state is OFF, the health check is considered unhealthy. For more information, see How Route 53 Determines Whether an Endpoint Is Healthy in the Amazon Route 53 Developer Guide."];
      resourcePath: ResourcePath.t option
        [@ocaml.doc
          "The path, if any, that you want Amazon Route 53 to request when performing health checks. The path can be any value for which your endpoint will return an HTTP status code of 2xx or 3xx when the endpoint is healthy, for example, the file /docs/route53-health-check.html. You can also include query string parameters, for example, /welcome.html?language=jp&login=y."];
      fullyQualifiedDomainName: FullyQualifiedDomainName.t option
        [@ocaml.doc
          "Amazon Route 53 behavior depends on whether you specify a value for IPAddress. If you specify a value for IPAddress: Amazon Route 53 sends health check requests to the specified IPv4 or IPv6 address and passes the value of FullyQualifiedDomainName in the Host header for all health checks except TCP health checks. This is typically the fully qualified DNS name of the endpoint on which you want Route 53 to perform health checks. When Route 53 checks the health of an endpoint, here is how it constructs the Host header: If you specify a value of 80 for Port and HTTP or HTTP_STR_MATCH for Type, Route 53 passes the value of FullyQualifiedDomainName to the endpoint in the Host header. If you specify a value of 443 for Port and HTTPS or HTTPS_STR_MATCH for Type, Route 53 passes the value of FullyQualifiedDomainName to the endpoint in the Host header. If you specify another value for Port and any value except TCP for Type, Route 53 passes FullyQualifiedDomainName:Port to the endpoint in the Host header. If you don't specify a value for FullyQualifiedDomainName, Route 53 substitutes the value of IPAddress in the Host header in each of the preceding cases. If you don't specify a value for IPAddress: Route 53 sends a DNS request to the domain that you specify for FullyQualifiedDomainName at the interval that you specify for RequestInterval. Using an IPv4 address that DNS returns, Route 53 then checks the health of the endpoint. If you don't specify a value for IPAddress, Route 53 uses only IPv4 to send health checks to the endpoint. If there's no resource record set with a type of A for the name that you specify for FullyQualifiedDomainName, the health check fails with a \"DNS resolution failed\" error. If you want to check the health of weighted, latency, or failover resource record sets and you choose to specify the endpoint only by FullyQualifiedDomainName, we recommend that you create a separate health check for each endpoint. For example, create a health check for each HTTP server that is serving content for www.example.com. For the value of FullyQualifiedDomainName, specify the domain name of the server (such as us-east-2-www.example.com), not the name of the resource record sets (www.example.com). In this configuration, if you create a health check for which the value of FullyQualifiedDomainName matches the name of the resource record sets and you then associate the health check with those resource record sets, health check results will be unpredictable. In addition, if the value that you specify for Type is HTTP, HTTPS, HTTP_STR_MATCH, or HTTPS_STR_MATCH, Route 53 passes the value of FullyQualifiedDomainName in the Host header, as it does when you specify a value for IPAddress. If the value of Type is TCP, Route 53 doesn't pass a Host header."];
      searchString: SearchString.t option
        [@ocaml.doc
          "If the value of Type is HTTP_STR_MATCH or HTTPS_STR_MATCH, the string that you want Amazon Route 53 to search for in the response body from the specified resource. If the string appears in the response body, Route 53 considers the resource healthy. Route 53 considers case when searching for SearchString in the response body."];
      requestInterval: RequestInterval.t option
        [@ocaml.doc
          "The number of seconds between the time that Amazon Route 53 gets a response from your endpoint and the time that it sends the next health check request. Each Route 53 health checker makes requests at this interval. You can't change the value of RequestInterval after you create a health check. If you don't specify a value for RequestInterval, the default value is 30 seconds."];
      failureThreshold: FailureThreshold.t option
        [@ocaml.doc
          "The number of consecutive health checks that an endpoint must pass or fail for Amazon Route 53 to change the current status of the endpoint from unhealthy to healthy or vice versa. For more information, see How Amazon Route 53 Determines Whether an Endpoint Is Healthy in the Amazon Route 53 Developer Guide. If you don't specify a value for FailureThreshold, the default value is three health checks."];
      measureLatency: MeasureLatency.t option
        [@ocaml.doc
          "Specify whether you want Amazon Route 53 to measure the latency between health checkers in multiple Amazon Web Services regions and your endpoint, and to display CloudWatch latency graphs on the Health Checks page in the Route 53 console. You can't change the value of MeasureLatency after you create a health check."];
      inverted: Inverted.t option
        [@ocaml.doc
          "Specify whether you want Amazon Route 53 to invert the status of a health check, for example, to consider a health check unhealthy when it otherwise would be considered healthy."];
      disabled: Disabled.t option
        [@ocaml.doc
          "Stops Route 53 from performing health checks. When you disable a health check, here's what happens: Health checks that check the health of endpoints: Route 53 stops submitting requests to your application, server, or other resource. Calculated health checks: Route 53 stops aggregating the status of the referenced health checks. Health checks that monitor CloudWatch alarms: Route 53 stops monitoring the corresponding CloudWatch metrics. After you disable a health check, Route 53 considers the status of the health check to always be healthy. If you configured DNS failover, Route 53 continues to route traffic to the corresponding resources. If you want to stop routing traffic to a resource, change the value of Inverted. Charges for a health check still apply when the health check is disabled. For more information, see Amazon Route 53 Pricing."];
      healthThreshold: HealthThreshold.t option
        [@ocaml.doc
          "The number of child health checks that are associated with a CALCULATED health check that Amazon Route 53 must consider healthy for the CALCULATED health check to be considered healthy. To specify the child health checks that you want to associate with a CALCULATED health check, use the ChildHealthChecks element. Note the following: If you specify a number greater than the number of child health checks, Route 53 always considers this health check to be unhealthy. If you specify 0, Route 53 always considers this health check to be healthy."];
      childHealthChecks: ChildHealthCheckList.t option
        [@ocaml.doc
          "(CALCULATED Health Checks Only) A complex type that contains one ChildHealthCheck element for each health check that you want to associate with a CALCULATED health check."];
      enableSNI: EnableSNI.t option
        [@ocaml.doc
          "Specify whether you want Amazon Route 53 to send the value of FullyQualifiedDomainName to the endpoint in the client_hello message during TLS negotiation. This allows the endpoint to respond to HTTPS health check requests with the applicable SSL/TLS certificate. Some endpoints require that HTTPS requests include the host name in the client_hello message. If you don't enable SNI, the status of the health check will be SSL alert handshake_failure. A health check can also have that status for other reasons. If SNI is enabled and you're still getting the error, check the SSL/TLS configuration on your endpoint and confirm that your certificate is valid. The SSL/TLS certificate on your endpoint includes a domain name in the Common Name field and possibly several more in the Subject Alternative Names field. One of the domain names in the certificate should match the value that you specify for FullyQualifiedDomainName. If the endpoint responds to the client_hello message with a certificate that does not include the domain name that you specified in FullyQualifiedDomainName, a health checker will retry the handshake. In the second attempt, the health checker will omit FullyQualifiedDomainName from the client_hello message."];
      regions: HealthCheckRegionList.t option
        [@ocaml.doc
          "A complex type that contains one Region element for each region from which you want Amazon Route 53 health checkers to check the specified endpoint. If you don't specify any regions, Route 53 health checkers automatically performs checks from all of the regions that are listed under Valid Values. If you update a health check to remove a region that has been performing health checks, Route 53 will briefly continue to perform checks from that region to ensure that some health checkers are always checking the endpoint (for example, if you replace three regions with four different regions)."];
      alarmIdentifier: AlarmIdentifier.t option
        [@ocaml.doc
          "A complex type that identifies the CloudWatch alarm that you want Amazon Route 53 health checkers to use to determine whether the specified health check is healthy."];
      insufficientDataHealthStatus: InsufficientDataHealthStatus.t option
        [@ocaml.doc
          "When CloudWatch has insufficient data about the metric to determine the alarm state, the status that you want Amazon Route 53 to assign to the health check: Healthy: Route 53 considers the health check to be healthy. Unhealthy: Route 53 considers the health check to be unhealthy. LastKnownStatus: Route 53 uses the status of the health check from the last time that CloudWatch had sufficient data to determine the alarm state. For new health checks that have no last known status, the default status for the health check is healthy."];
      routingControlArn: RoutingControlArn.t option
        [@ocaml.doc
          "The Amazon Resource Name (ARN) for the Route 53 Application Recovery Controller routing control. For more information about Route 53 Application Recovery Controller, see Route 53 Application Recovery Controller Developer Guide.."]}
    let context_ = "HealthCheckConfig"
    let make ?iPAddress =
      fun ?port ->
        fun ?resourcePath ->
          fun ?fullyQualifiedDomainName ->
            fun ?searchString ->
              fun ?requestInterval ->
                fun ?failureThreshold ->
                  fun ?measureLatency ->
                    fun ?inverted ->
                      fun ?disabled ->
                        fun ?healthThreshold ->
                          fun ?childHealthChecks ->
                            fun ?enableSNI ->
                              fun ?regions ->
                                fun ?alarmIdentifier ->
                                  fun ?insufficientDataHealthStatus ->
                                    fun ?routingControlArn ->
                                      fun ~type_ ->
                                        fun () ->
                                          {
                                            iPAddress;
                                            port;
                                            resourcePath;
                                            fullyQualifiedDomainName;
                                            searchString;
                                            requestInterval;
                                            failureThreshold;
                                            measureLatency;
                                            inverted;
                                            disabled;
                                            healthThreshold;
                                            childHealthChecks;
                                            enableSNI;
                                            regions;
                                            alarmIdentifier;
                                            insufficientDataHealthStatus;
                                            routingControlArn;
                                            type_
                                          }
    let to_value x =
      structure_to_value
        [("IPAddress", (Option.map x.iPAddress ~f:IPAddress.to_value));
        ("Port", (Option.map x.port ~f:Port.to_value));
        ("Type", (Some (HealthCheckType.to_value x.type_)));
        ("ResourcePath",
          (Option.map x.resourcePath ~f:ResourcePath.to_value));
        ("FullyQualifiedDomainName",
          (Option.map x.fullyQualifiedDomainName
             ~f:FullyQualifiedDomainName.to_value));
        ("SearchString",
          (Option.map x.searchString ~f:SearchString.to_value));
        ("RequestInterval",
          (Option.map x.requestInterval ~f:RequestInterval.to_value));
        ("FailureThreshold",
          (Option.map x.failureThreshold ~f:FailureThreshold.to_value));
        ("MeasureLatency",
          (Option.map x.measureLatency ~f:MeasureLatency.to_value));
        ("Inverted", (Option.map x.inverted ~f:Inverted.to_value));
        ("Disabled", (Option.map x.disabled ~f:Disabled.to_value));
        ("HealthThreshold",
          (Option.map x.healthThreshold ~f:HealthThreshold.to_value));
        ("ChildHealthChecks",
          (Option.map x.childHealthChecks ~f:ChildHealthCheckList.to_value));
        ("EnableSNI", (Option.map x.enableSNI ~f:EnableSNI.to_value));
        ("Regions", (Option.map x.regions ~f:HealthCheckRegionList.to_value));
        ("AlarmIdentifier",
          (Option.map x.alarmIdentifier ~f:AlarmIdentifier.to_value));
        ("InsufficientDataHealthStatus",
          (Option.map x.insufficientDataHealthStatus
             ~f:InsufficientDataHealthStatus.to_value));
        ("RoutingControlArn",
          (Option.map x.routingControlArn ~f:RoutingControlArn.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let routingControlArn =
        (Option.map ~f:RoutingControlArn.of_xml)
          (Xml.child xml_arg0 "RoutingControlArn") in
      let insufficientDataHealthStatus =
        (Option.map ~f:InsufficientDataHealthStatus.of_xml)
          (Xml.child xml_arg0 "InsufficientDataHealthStatus") in
      let alarmIdentifier =
        (Option.map ~f:AlarmIdentifier.of_xml)
          (Xml.child xml_arg0 "AlarmIdentifier") in
      let regions =
        (Option.map ~f:HealthCheckRegionList.of_xml)
          (Xml.child xml_arg0 "Regions") in
      let enableSNI =
        (Option.map ~f:EnableSNI.of_xml) (Xml.child xml_arg0 "EnableSNI") in
      let childHealthChecks =
        (Option.map ~f:ChildHealthCheckList.of_xml)
          (Xml.child xml_arg0 "ChildHealthChecks") in
      let healthThreshold =
        (Option.map ~f:HealthThreshold.of_xml)
          (Xml.child xml_arg0 "HealthThreshold") in
      let disabled =
        (Option.map ~f:Disabled.of_xml) (Xml.child xml_arg0 "Disabled") in
      let inverted =
        (Option.map ~f:Inverted.of_xml) (Xml.child xml_arg0 "Inverted") in
      let measureLatency =
        (Option.map ~f:MeasureLatency.of_xml)
          (Xml.child xml_arg0 "MeasureLatency") in
      let failureThreshold =
        (Option.map ~f:FailureThreshold.of_xml)
          (Xml.child xml_arg0 "FailureThreshold") in
      let requestInterval =
        (Option.map ~f:RequestInterval.of_xml)
          (Xml.child xml_arg0 "RequestInterval") in
      let searchString =
        (Option.map ~f:SearchString.of_xml)
          (Xml.child xml_arg0 "SearchString") in
      let fullyQualifiedDomainName =
        (Option.map ~f:FullyQualifiedDomainName.of_xml)
          (Xml.child xml_arg0 "FullyQualifiedDomainName") in
      let resourcePath =
        (Option.map ~f:ResourcePath.of_xml)
          (Xml.child xml_arg0 "ResourcePath") in
      let type_ =
        HealthCheckType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Type") in
      let port = (Option.map ~f:Port.of_xml) (Xml.child xml_arg0 "Port") in
      let iPAddress =
        (Option.map ~f:IPAddress.of_xml) (Xml.child xml_arg0 "IPAddress") in
      make ?routingControlArn ?insufficientDataHealthStatus ?alarmIdentifier
        ?regions ?enableSNI ?childHealthChecks ?healthThreshold ?disabled
        ?inverted ?measureLatency ?failureThreshold ?requestInterval
        ?searchString ?fullyQualifiedDomainName ?resourcePath ~type_ ?port
        ?iPAddress ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let routingControlArn =
        field_map json "RoutingControlArn" RoutingControlArn.of_json in
      let insufficientDataHealthStatus =
        field_map json "InsufficientDataHealthStatus"
          InsufficientDataHealthStatus.of_json in
      let alarmIdentifier =
        field_map json "AlarmIdentifier" AlarmIdentifier.of_json in
      let regions = field_map json "Regions" HealthCheckRegionList.of_json in
      let enableSNI = field_map json "EnableSNI" EnableSNI.of_json in
      let childHealthChecks =
        field_map json "ChildHealthChecks" ChildHealthCheckList.of_json in
      let healthThreshold =
        field_map json "HealthThreshold" HealthThreshold.of_json in
      let disabled = field_map json "Disabled" Disabled.of_json in
      let inverted = field_map json "Inverted" Inverted.of_json in
      let measureLatency =
        field_map json "MeasureLatency" MeasureLatency.of_json in
      let failureThreshold =
        field_map json "FailureThreshold" FailureThreshold.of_json in
      let requestInterval =
        field_map json "RequestInterval" RequestInterval.of_json in
      let searchString = field_map json "SearchString" SearchString.of_json in
      let fullyQualifiedDomainName =
        field_map json "FullyQualifiedDomainName"
          FullyQualifiedDomainName.of_json in
      let resourcePath = field_map json "ResourcePath" ResourcePath.of_json in
      let type_ = field_map_exn json "Type" HealthCheckType.of_json in
      let port = field_map json "Port" Port.of_json in
      let iPAddress = field_map json "IPAddress" IPAddress.of_json in
      make ?routingControlArn ?insufficientDataHealthStatus ?alarmIdentifier
        ?regions ?enableSNI ?childHealthChecks ?healthThreshold ?disabled
        ?inverted ?measureLatency ?failureThreshold ?requestInterval
        ?searchString ?fullyQualifiedDomainName ?resourcePath ~type_ ?port
        ?iPAddress ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about the health check."]
module HealthCheckNonce =
  struct
    type nonrec t = string
    let context_ = "HealthCheckNonce"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"HealthCheckNonce" j
    let to_json = simple_to_json to_value
  end
module HealthCheckVersion =
  struct
    type nonrec t = Int64.t
    let make i =
      let open Result in ok_or_failwith (check_int64_min i ~min:1L); i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module GeoLocationContinentName =
  struct
    type nonrec t = string
    let context_ = "GeoLocationContinentName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:32) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GeoLocationContinentName" j
    let to_json = simple_to_json to_value
  end
module GeoLocationCountryName =
  struct
    type nonrec t = string
    let context_ = "GeoLocationCountryName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GeoLocationCountryName" j
    let to_json = simple_to_json to_value
  end
module GeoLocationSubdivisionName =
  struct
    type nonrec t = string
    let context_ = "GeoLocationSubdivisionName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:64) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"GeoLocationSubdivisionName" j
    let to_json = simple_to_json to_value
  end
module StatusReport =
  struct
    type nonrec t =
      {
      status: Status.t option
        [@ocaml.doc
          "A description of the status of the health check endpoint as reported by one of the Amazon Route 53 health checkers."];
      checkedTime: TimeStamp.t option
        [@ocaml.doc
          "The date and time that the health checker performed the health check in ISO 8601 format and Coordinated Universal Time (UTC). For example, the value 2017-03-27T17:48:16.751Z represents March 27, 2017 at 17:48:16.751 UTC."]}
    let make ?status = fun ?checkedTime -> fun () -> { status; checkedTime }
    let to_value x =
      structure_to_value
        [("Status", (Option.map x.status ~f:Status.to_value));
        ("CheckedTime", (Option.map x.checkedTime ~f:TimeStamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let checkedTime =
        (Option.map ~f:TimeStamp.of_xml) (Xml.child xml_arg0 "CheckedTime") in
      let status =
        (Option.map ~f:Status.of_xml) (Xml.child xml_arg0 "Status") in
      make ?checkedTime ?status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let checkedTime = field_map json "CheckedTime" TimeStamp.of_json in
      let status = field_map json "Status" Status.of_json in
      make ?checkedTime ?status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the status that one Amazon Route 53 health checker reports and the time of the health check."]
module SigningKeyInteger =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for SigningKeyInteger" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module SigningKeyName =
  struct
    type nonrec t = string
    let context_ = "SigningKeyName"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:128) >>=
             (fun () -> check_string_min i ~min:3));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SigningKeyName" j
    let to_json = simple_to_json to_value
  end
module SigningKeyStatus =
  struct
    type nonrec t = string
    let context_ = "SigningKeyStatus"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:150) >>=
             (fun () -> check_string_min i ~min:5));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SigningKeyStatus" j
    let to_json = simple_to_json to_value
  end
module SigningKeyStatusMessage =
  struct
    type nonrec t = string
    let context_ = "SigningKeyStatusMessage"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:512) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SigningKeyStatusMessage" j
    let to_json = simple_to_json to_value
  end
module SigningKeyString =
  struct
    type nonrec t = string
    let context_ = "SigningKeyString"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SigningKeyString" j
    let to_json = simple_to_json to_value
  end
module SigningKeyTag =
  struct
    type nonrec t = int
    let make i =
      let open Result in
        ok_or_failwith
          ((check_int_max i ~max:65536) >>=
             (fun () -> check_int_min i ~min:0));
        i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for SigningKeyTag" xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ErrorMessage =
  struct
    type nonrec t = string
    let context_ = "ErrorMessage"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ErrorMessage" j
    let to_json = simple_to_json to_value
  end
module Change =
  struct
    type nonrec t =
      {
      action: ChangeAction.t
        [@ocaml.doc
          "The action to perform: CREATE: Creates a resource record set that has the specified values. DELETE: Deletes a existing resource record set. To delete the resource record set that is associated with a traffic policy instance, use DeleteTrafficPolicyInstance. Amazon Route 53 will delete the resource record set automatically. If you delete the resource record set by using ChangeResourceRecordSets, Route 53 doesn't automatically delete the traffic policy instance, and you'll continue to be charged for it even though it's no longer in use. UPSERT: If a resource record set doesn't already exist, Route 53 creates it. If a resource record set does exist, Route 53 updates it with the values in the request."];
      resourceRecordSet: ResourceRecordSet.t
        [@ocaml.doc
          "Information about the resource record set to create, delete, or update."]}
    let context_ = "Change"
    let make ~action =
      fun ~resourceRecordSet -> fun () -> { action; resourceRecordSet }
    let to_value x =
      structure_to_value
        [("Action", (Some (ChangeAction.to_value x.action)));
        ("ResourceRecordSet",
          (Some (ResourceRecordSet.to_value x.resourceRecordSet)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceRecordSet =
        ResourceRecordSet.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceRecordSet") in
      let action =
        ChangeAction.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Action") in
      make ~resourceRecordSet ~action ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let resourceRecordSet =
        field_map_exn json "ResourceRecordSet" ResourceRecordSet.of_json in
      let action = field_map_exn json "Action" ChangeAction.of_json in
      make ~resourceRecordSet ~action ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The information for each resource record set that you want to change."]
module ResettableElementName =
  struct
    type nonrec t =
      | FullyQualifiedDomainName 
      | Regions 
      | ResourcePath 
      | ChildHealthChecks 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | FullyQualifiedDomainName -> "FullyQualifiedDomainName"
      | Regions -> "Regions"
      | ResourcePath -> "ResourcePath"
      | ChildHealthChecks -> "ChildHealthChecks"
      | Non_static_id s -> s
    let of_string =
      function
      | "FullyQualifiedDomainName" -> FullyQualifiedDomainName
      | "Regions" -> Regions
      | "ResourcePath" -> ResourcePath
      | "ChildHealthChecks" -> ChildHealthChecks
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ResettableElementName" xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ResettableElementName" j)
    let to_json = simple_to_json to_value
  end
module RecordDataEntry =
  struct
    type nonrec t = string[@@ocaml.doc
                            "A value that Amazon Route 53 returned for this resource record set. A RecordDataEntry element is one of the following: For non-alias resource record sets, a RecordDataEntry element contains one value in the resource record set. If the resource record set contains multiple values, the response includes one RecordDataEntry element for each value. For multiple resource record sets that have the same name and type, which includes weighted, latency, geolocation, and failover, a RecordDataEntry element contains the value from the appropriate resource record set based on the request. For alias resource record sets that refer to Amazon Web Services resources other than another resource record set, the RecordDataEntry element contains an IP address or a domain name for the Amazon Web Services resource, depending on the type of resource. For alias resource record sets that refer to other resource record sets, a RecordDataEntry element contains one value from the referenced resource record set. If the referenced resource record set contains multiple values, the response includes one RecordDataEntry element for each value."]
    let context_ = "RecordDataEntry"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:512) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"RecordDataEntry" j
    let to_json = simple_to_json to_value
  end[@@ocaml.doc
       "A value that Amazon Route 53 returned for this resource record set. A RecordDataEntry element is one of the following: For non-alias resource record sets, a RecordDataEntry element contains one value in the resource record set. If the resource record set contains multiple values, the response includes one RecordDataEntry element for each value. For multiple resource record sets that have the same name and type, which includes weighted, latency, geolocation, and failover, a RecordDataEntry element contains the value from the appropriate resource record set based on the request. For alias resource record sets that refer to Amazon Web Services resources other than another resource record set, the RecordDataEntry element contains an IP address or a domain name for the Amazon Web Services resource, depending on the type of resource. For alias resource record sets that refer to other resource record sets, a RecordDataEntry element contains one value from the referenced resource record set. If the referenced resource record set contains multiple values, the response includes one RecordDataEntry element for each value."]
module VPC =
  struct
    type nonrec t =
      {
      vPCRegion: VPCRegion.t option
        [@ocaml.doc
          "(Private hosted zones only) The region that an Amazon VPC was created in."];
      vPCId: VPCId.t option }
    let make ?vPCRegion = fun ?vPCId -> fun () -> { vPCRegion; vPCId }
    let to_value x =
      structure_to_value
        [("VPCRegion", (Option.map x.vPCRegion ~f:VPCRegion.to_value));
        ("VPCId", (Option.map x.vPCId ~f:VPCId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vPCId = (Option.map ~f:VPCId.of_xml) (Xml.child xml_arg0 "VPCId") in
      let vPCRegion =
        (Option.map ~f:VPCRegion.of_xml) (Xml.child xml_arg0 "VPCRegion") in
      make ?vPCId ?vPCRegion ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let vPCId = field_map json "VPCId" VPCId.of_json in
      let vPCRegion = field_map json "VPCRegion" VPCRegion.of_json in
      make ?vPCId ?vPCRegion ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "(Private hosted zones only) A complex type that contains information about an Amazon VPC. If you associate a private hosted zone with an Amazon VPC when you make a CreateHostedZone request, the following parameters are also required."]
module TrafficPolicy =
  struct
    type nonrec t =
      {
      id: TrafficPolicyId.t
        [@ocaml.doc
          "The ID that Amazon Route 53 assigned to a traffic policy when you created it."];
      version: TrafficPolicyVersion.t
        [@ocaml.doc
          "The version number that Amazon Route 53 assigns to a traffic policy. For a new traffic policy, the value of Version is always 1."];
      name: TrafficPolicyName.t
        [@ocaml.doc
          "The name that you specified when you created the traffic policy."];
      type_: RRType.t
        [@ocaml.doc
          "The DNS type of the resource record sets that Amazon Route 53 creates when you use a traffic policy to create a traffic policy instance."];
      document: TrafficPolicyDocument.t
        [@ocaml.doc
          "The definition of a traffic policy in JSON format. You specify the JSON document to use for a new traffic policy in the CreateTrafficPolicy request. For more information about the JSON format, see Traffic Policy Document Format."];
      comment: TrafficPolicyComment.t option
        [@ocaml.doc
          "The comment that you specify in the CreateTrafficPolicy request, if any."]}
    let context_ = "TrafficPolicy"
    let make ?comment =
      fun ~id ->
        fun ~version ->
          fun ~name ->
            fun ~type_ ->
              fun ~document ->
                fun () -> { comment; id; version; name; type_; document }
    let to_value x =
      structure_to_value
        [("Id", (Some (TrafficPolicyId.to_value x.id)));
        ("Version", (Some (TrafficPolicyVersion.to_value x.version)));
        ("Name", (Some (TrafficPolicyName.to_value x.name)));
        ("Type", (Some (RRType.to_value x.type_)));
        ("Document", (Some (TrafficPolicyDocument.to_value x.document)));
        ("Comment", (Option.map x.comment ~f:TrafficPolicyComment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let comment =
        (Option.map ~f:TrafficPolicyComment.of_xml)
          (Xml.child xml_arg0 "Comment") in
      let document =
        TrafficPolicyDocument.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Document") in
      let type_ =
        RRType.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Type") in
      let name =
        TrafficPolicyName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      let version =
        TrafficPolicyVersion.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Version") in
      let id =
        TrafficPolicyId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ?comment ~document ~type_ ~name ~version ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let comment = field_map json "Comment" TrafficPolicyComment.of_json in
      let document =
        field_map_exn json "Document" TrafficPolicyDocument.of_json in
      let type_ = field_map_exn json "Type" RRType.of_json in
      let name = field_map_exn json "Name" TrafficPolicyName.of_json in
      let version = field_map_exn json "Version" TrafficPolicyVersion.of_json in
      let id = field_map_exn json "Id" TrafficPolicyId.of_json in
      make ?comment ~document ~type_ ~name ~version ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains settings for a traffic policy."]
module TrafficPolicyInstance =
  struct
    type nonrec t =
      {
      id: TrafficPolicyInstanceId.t
        [@ocaml.doc
          "The ID that Amazon Route 53 assigned to the new traffic policy instance."];
      hostedZoneId: ResourceId.t
        [@ocaml.doc
          "The ID of the hosted zone that Amazon Route 53 created resource record sets in."];
      name: DNSName.t
        [@ocaml.doc
          "The DNS name, such as www.example.com, for which Amazon Route 53 responds to queries by using the resource record sets that are associated with this traffic policy instance."];
      tTL: TTL.t
        [@ocaml.doc
          "The TTL that Amazon Route 53 assigned to all of the resource record sets that it created in the specified hosted zone."];
      state: TrafficPolicyInstanceState.t
        [@ocaml.doc
          "The value of State is one of the following values: Applied Amazon Route 53 has finished creating resource record sets, and changes have propagated to all Route 53 edge locations. Creating Route 53 is creating the resource record sets. Use GetTrafficPolicyInstance to confirm that the CreateTrafficPolicyInstance request completed successfully. Failed Route 53 wasn't able to create or update the resource record sets. When the value of State is Failed, see Message for an explanation of what caused the request to fail."];
      message: Message.t
        [@ocaml.doc
          "If State is Failed, an explanation of the reason for the failure. If State is another value, Message is empty."];
      trafficPolicyId: TrafficPolicyId.t
        [@ocaml.doc
          "The ID of the traffic policy that Amazon Route 53 used to create resource record sets in the specified hosted zone."];
      trafficPolicyVersion: TrafficPolicyVersion.t
        [@ocaml.doc
          "The version of the traffic policy that Amazon Route 53 used to create resource record sets in the specified hosted zone."];
      trafficPolicyType: RRType.t
        [@ocaml.doc
          "The DNS type that Amazon Route 53 assigned to all of the resource record sets that it created for this traffic policy instance."]}
    let context_ = "TrafficPolicyInstance"
    let make ~id =
      fun ~hostedZoneId ->
        fun ~name ->
          fun ~tTL ->
            fun ~state ->
              fun ~message ->
                fun ~trafficPolicyId ->
                  fun ~trafficPolicyVersion ->
                    fun ~trafficPolicyType ->
                      fun () ->
                        {
                          id;
                          hostedZoneId;
                          name;
                          tTL;
                          state;
                          message;
                          trafficPolicyId;
                          trafficPolicyVersion;
                          trafficPolicyType
                        }
    let to_value x =
      structure_to_value
        [("Id", (Some (TrafficPolicyInstanceId.to_value x.id)));
        ("HostedZoneId", (Some (ResourceId.to_value x.hostedZoneId)));
        ("Name", (Some (DNSName.to_value x.name)));
        ("TTL", (Some (TTL.to_value x.tTL)));
        ("State", (Some (TrafficPolicyInstanceState.to_value x.state)));
        ("Message", (Some (Message.to_value x.message)));
        ("TrafficPolicyId",
          (Some (TrafficPolicyId.to_value x.trafficPolicyId)));
        ("TrafficPolicyVersion",
          (Some (TrafficPolicyVersion.to_value x.trafficPolicyVersion)));
        ("TrafficPolicyType", (Some (RRType.to_value x.trafficPolicyType)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trafficPolicyType =
        RRType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficPolicyType") in
      let trafficPolicyVersion =
        TrafficPolicyVersion.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficPolicyVersion") in
      let trafficPolicyId =
        TrafficPolicyId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficPolicyId") in
      let message =
        Message.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Message") in
      let state =
        TrafficPolicyInstanceState.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "State") in
      let tTL = TTL.of_xml (Xml.child_exn ~context:context_ xml_arg0 "TTL") in
      let name =
        DNSName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      let hostedZoneId =
        ResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HostedZoneId") in
      let id =
        TrafficPolicyInstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ~trafficPolicyType ~trafficPolicyVersion ~trafficPolicyId ~message
        ~state ~tTL ~name ~hostedZoneId ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let trafficPolicyType =
        field_map_exn json "TrafficPolicyType" RRType.of_json in
      let trafficPolicyVersion =
        field_map_exn json "TrafficPolicyVersion"
          TrafficPolicyVersion.of_json in
      let trafficPolicyId =
        field_map_exn json "TrafficPolicyId" TrafficPolicyId.of_json in
      let message = field_map_exn json "Message" Message.of_json in
      let state =
        field_map_exn json "State" TrafficPolicyInstanceState.of_json in
      let tTL = field_map_exn json "TTL" TTL.of_json in
      let name = field_map_exn json "Name" DNSName.of_json in
      let hostedZoneId = field_map_exn json "HostedZoneId" ResourceId.of_json in
      let id = field_map_exn json "Id" TrafficPolicyInstanceId.of_json in
      make ~trafficPolicyType ~trafficPolicyVersion ~trafficPolicyId ~message
        ~state ~tTL ~name ~hostedZoneId ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains settings for the new traffic policy instance."]
module TrafficPolicySummary =
  struct
    type nonrec t =
      {
      id: TrafficPolicyId.t
        [@ocaml.doc
          "The ID that Amazon Route 53 assigned to the traffic policy when you created it."];
      name: TrafficPolicyName.t
        [@ocaml.doc
          "The name that you specified for the traffic policy when you created it."];
      type_: RRType.t
        [@ocaml.doc
          "The DNS type of the resource record sets that Amazon Route 53 creates when you use a traffic policy to create a traffic policy instance."];
      latestVersion: TrafficPolicyVersion.t
        [@ocaml.doc
          "The version number of the latest version of the traffic policy."];
      trafficPolicyCount: TrafficPolicyVersion.t
        [@ocaml.doc
          "The number of traffic policies that are associated with the current Amazon Web Services account."]}
    let context_ = "TrafficPolicySummary"
    let make ~id =
      fun ~name ->
        fun ~type_ ->
          fun ~latestVersion ->
            fun ~trafficPolicyCount ->
              fun () ->
                { id; name; type_; latestVersion; trafficPolicyCount }
    let to_value x =
      structure_to_value
        [("Id", (Some (TrafficPolicyId.to_value x.id)));
        ("Name", (Some (TrafficPolicyName.to_value x.name)));
        ("Type", (Some (RRType.to_value x.type_)));
        ("LatestVersion",
          (Some (TrafficPolicyVersion.to_value x.latestVersion)));
        ("TrafficPolicyCount",
          (Some (TrafficPolicyVersion.to_value x.trafficPolicyCount)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trafficPolicyCount =
        TrafficPolicyVersion.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficPolicyCount") in
      let latestVersion =
        TrafficPolicyVersion.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "LatestVersion") in
      let type_ =
        RRType.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Type") in
      let name =
        TrafficPolicyName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      let id =
        TrafficPolicyId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ~trafficPolicyCount ~latestVersion ~type_ ~name ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let trafficPolicyCount =
        field_map_exn json "TrafficPolicyCount" TrafficPolicyVersion.of_json in
      let latestVersion =
        field_map_exn json "LatestVersion" TrafficPolicyVersion.of_json in
      let type_ = field_map_exn json "Type" RRType.of_json in
      let name = field_map_exn json "Name" TrafficPolicyName.of_json in
      let id = field_map_exn json "Id" TrafficPolicyId.of_json in
      make ~trafficPolicyCount ~latestVersion ~type_ ~name ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about the latest version of one traffic policy that is associated with the current Amazon Web Services account."]
module ResourceTagSet =
  struct
    type nonrec t =
      {
      resourceType: TagResourceType.t option
        [@ocaml.doc
          "The type of the resource. The resource type for health checks is healthcheck. The resource type for hosted zones is hostedzone."];
      resourceId: TagResourceId.t option
        [@ocaml.doc "The ID for the specified resource."];
      tags: TagList.t option
        [@ocaml.doc "The tags associated with the specified resource."]}
    let make ?resourceType =
      fun ?resourceId ->
        fun ?tags -> fun () -> { resourceType; resourceId; tags }
    let to_value x =
      structure_to_value
        [("ResourceType",
           (Option.map x.resourceType ~f:TagResourceType.to_value));
        ("ResourceId", (Option.map x.resourceId ~f:TagResourceId.to_value));
        ("Tags", (Option.map x.tags ~f:TagList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let tags = (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "Tags") in
      let resourceId =
        (Option.map ~f:TagResourceId.of_xml)
          (Xml.child xml_arg0 "ResourceId") in
      let resourceType =
        (Option.map ~f:TagResourceType.of_xml)
          (Xml.child xml_arg0 "ResourceType") in
      make ?tags ?resourceId ?resourceType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let tags = field_map json "Tags" TagList.of_json in
      let resourceId = field_map json "ResourceId" TagResourceId.of_json in
      let resourceType =
        field_map json "ResourceType" TagResourceType.of_json in
      make ?tags ?resourceId ?resourceType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type containing a resource and its associated tags."]
module DelegationSet =
  struct
    type nonrec t =
      {
      id: ResourceId.t option
        [@ocaml.doc
          "The ID that Amazon Route 53 assigns to a reusable delegation set."];
      callerReference: Nonce.t option
        [@ocaml.doc
          "The value that you specified for CallerReference when you created the reusable delegation set."];
      nameServers: DelegationSetNameServers.t
        [@ocaml.doc
          "A complex type that contains a list of the authoritative name servers for a hosted zone or for a reusable delegation set."]}
    let context_ = "DelegationSet"
    let make ?id =
      fun ?callerReference ->
        fun ~nameServers -> fun () -> { id; callerReference; nameServers }
    let to_value x =
      structure_to_value
        [("Id", (Option.map x.id ~f:ResourceId.to_value));
        ("CallerReference", (Option.map x.callerReference ~f:Nonce.to_value));
        ("NameServers",
          (Some (DelegationSetNameServers.to_value x.nameServers)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nameServers =
        DelegationSetNameServers.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "NameServers") in
      let callerReference =
        (Option.map ~f:Nonce.of_xml) (Xml.child xml_arg0 "CallerReference") in
      let id = (Option.map ~f:ResourceId.of_xml) (Xml.child xml_arg0 "Id") in
      make ~nameServers ?callerReference ?id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nameServers =
        field_map_exn json "NameServers" DelegationSetNameServers.of_json in
      let callerReference = field_map json "CallerReference" Nonce.of_json in
      let id = field_map json "Id" ResourceId.of_json in
      make ~nameServers ?callerReference ?id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that lists the name servers in a delegation set, as well as the CallerReference and the ID for the delegation set."]
module QueryLoggingConfig =
  struct
    type nonrec t =
      {
      id: QueryLoggingConfigId.t
        [@ocaml.doc "The ID for a configuration for DNS query logging."];
      hostedZoneId: ResourceId.t
        [@ocaml.doc
          "The ID of the hosted zone that CloudWatch Logs is logging queries for."];
      cloudWatchLogsLogGroupArn: CloudWatchLogsLogGroupArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) of the CloudWatch Logs log group that Amazon Route 53 is publishing logs to."]}
    let context_ = "QueryLoggingConfig"
    let make ~id =
      fun ~hostedZoneId ->
        fun ~cloudWatchLogsLogGroupArn ->
          fun () -> { id; hostedZoneId; cloudWatchLogsLogGroupArn }
    let to_value x =
      structure_to_value
        [("Id", (Some (QueryLoggingConfigId.to_value x.id)));
        ("HostedZoneId", (Some (ResourceId.to_value x.hostedZoneId)));
        ("CloudWatchLogsLogGroupArn",
          (Some
             (CloudWatchLogsLogGroupArn.to_value x.cloudWatchLogsLogGroupArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cloudWatchLogsLogGroupArn =
        CloudWatchLogsLogGroupArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "CloudWatchLogsLogGroupArn") in
      let hostedZoneId =
        ResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HostedZoneId") in
      let id =
        QueryLoggingConfigId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ~cloudWatchLogsLogGroupArn ~hostedZoneId ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let cloudWatchLogsLogGroupArn =
        field_map_exn json "CloudWatchLogsLogGroupArn"
          CloudWatchLogsLogGroupArn.of_json in
      let hostedZoneId = field_map_exn json "HostedZoneId" ResourceId.of_json in
      let id = field_map_exn json "Id" QueryLoggingConfigId.of_json in
      make ~cloudWatchLogsLogGroupArn ~hostedZoneId ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about a configuration for DNS query logging."]
module HostedZone =
  struct
    type nonrec t =
      {
      id: ResourceId.t
        [@ocaml.doc
          "The ID that Amazon Route 53 assigned to the hosted zone when you created it."];
      name: DNSName.t
        [@ocaml.doc
          "The name of the domain. For public hosted zones, this is the name that you have registered with your DNS registrar. For information about how to specify characters other than a-z, 0-9, and - (hyphen) and how to specify internationalized domain names, see CreateHostedZone."];
      callerReference: Nonce.t
        [@ocaml.doc
          "The value that you specified for CallerReference when you created the hosted zone."];
      config: HostedZoneConfig.t option
        [@ocaml.doc
          "A complex type that includes the Comment and PrivateZone elements. If you omitted the HostedZoneConfig and Comment elements from the request, the Config and Comment elements don't appear in the response."];
      resourceRecordSetCount: HostedZoneRRSetCount.t option
        [@ocaml.doc "The number of resource record sets in the hosted zone."];
      linkedService: LinkedService.t option
        [@ocaml.doc
          "If the hosted zone was created by another service, the service that created the hosted zone. When a hosted zone is created by another service, you can't edit or delete it using Route 53."]}
    let context_ = "HostedZone"
    let make ?config =
      fun ?resourceRecordSetCount ->
        fun ?linkedService ->
          fun ~id ->
            fun ~name ->
              fun ~callerReference ->
                fun () ->
                  {
                    config;
                    resourceRecordSetCount;
                    linkedService;
                    id;
                    name;
                    callerReference
                  }
    let to_value x =
      structure_to_value
        [("Id", (Some (ResourceId.to_value x.id)));
        ("Name", (Some (DNSName.to_value x.name)));
        ("CallerReference", (Some (Nonce.to_value x.callerReference)));
        ("Config", (Option.map x.config ~f:HostedZoneConfig.to_value));
        ("ResourceRecordSetCount",
          (Option.map x.resourceRecordSetCount
             ~f:HostedZoneRRSetCount.to_value));
        ("LinkedService",
          (Option.map x.linkedService ~f:LinkedService.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let linkedService =
        (Option.map ~f:LinkedService.of_xml)
          (Xml.child xml_arg0 "LinkedService") in
      let resourceRecordSetCount =
        (Option.map ~f:HostedZoneRRSetCount.of_xml)
          (Xml.child xml_arg0 "ResourceRecordSetCount") in
      let config =
        (Option.map ~f:HostedZoneConfig.of_xml) (Xml.child xml_arg0 "Config") in
      let callerReference =
        Nonce.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CallerReference") in
      let name =
        DNSName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      let id =
        ResourceId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ?linkedService ?resourceRecordSetCount ?config ~callerReference
        ~name ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let linkedService =
        field_map json "LinkedService" LinkedService.of_json in
      let resourceRecordSetCount =
        field_map json "ResourceRecordSetCount" HostedZoneRRSetCount.of_json in
      let config = field_map json "Config" HostedZoneConfig.of_json in
      let callerReference =
        field_map_exn json "CallerReference" Nonce.of_json in
      let name = field_map_exn json "Name" DNSName.of_json in
      let id = field_map_exn json "Id" ResourceId.of_json in
      make ?linkedService ?resourceRecordSetCount ?config ~callerReference
        ~name ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains general information about the hosted zone."]
module HostedZoneSummary =
  struct
    type nonrec t =
      {
      hostedZoneId: ResourceId.t
        [@ocaml.doc
          "The Route 53 hosted zone ID of a private hosted zone that the specified VPC is associated with."];
      name: DNSName.t
        [@ocaml.doc
          "The name of the private hosted zone, such as example.com."];
      owner: HostedZoneOwner.t
        [@ocaml.doc
          "The owner of a private hosted zone that the specified VPC is associated with. The owner can be either an Amazon Web Services account or an Amazon Web Services service."]}
    let context_ = "HostedZoneSummary"
    let make ~hostedZoneId =
      fun ~name -> fun ~owner -> fun () -> { hostedZoneId; name; owner }
    let to_value x =
      structure_to_value
        [("HostedZoneId", (Some (ResourceId.to_value x.hostedZoneId)));
        ("Name", (Some (DNSName.to_value x.name)));
        ("Owner", (Some (HostedZoneOwner.to_value x.owner)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let owner =
        HostedZoneOwner.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Owner") in
      let name =
        DNSName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      let hostedZoneId =
        ResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HostedZoneId") in
      make ~owner ~name ~hostedZoneId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let owner = field_map_exn json "Owner" HostedZoneOwner.of_json in
      let name = field_map_exn json "Name" DNSName.of_json in
      let hostedZoneId = field_map_exn json "HostedZoneId" ResourceId.of_json in
      make ~owner ~name ~hostedZoneId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "In the response to a ListHostedZonesByVPC request, the HostedZoneSummaries element contains one HostedZoneSummary element for each hosted zone that the specified Amazon VPC is associated with. Each HostedZoneSummary element contains the hosted zone name and ID, and information about who owns the hosted zone."]
module HealthCheck =
  struct
    type nonrec t =
      {
      id: HealthCheckId.t
        [@ocaml.doc
          "The identifier that Amazon Route 53 assigned to the health check when you created it. When you add or update a resource record set, you use this value to specify which health check to use. The value can be up to 64 characters long."];
      callerReference: HealthCheckNonce.t
        [@ocaml.doc
          "A unique string that you specified when you created the health check."];
      linkedService: LinkedService.t option
        [@ocaml.doc
          "If the health check was created by another service, the service that created the health check. When a health check is created by another service, you can't edit or delete it using Amazon Route 53."];
      healthCheckConfig: HealthCheckConfig.t
        [@ocaml.doc
          "A complex type that contains detailed information about one health check."];
      healthCheckVersion: HealthCheckVersion.t
        [@ocaml.doc
          "The version of the health check. You can optionally pass this value in a call to UpdateHealthCheck to prevent overwriting another change to the health check."];
      cloudWatchAlarmConfiguration: CloudWatchAlarmConfiguration.t option
        [@ocaml.doc
          "A complex type that contains information about the CloudWatch alarm that Amazon Route 53 is monitoring for this health check."]}
    let context_ = "HealthCheck"
    let make ?linkedService =
      fun ?cloudWatchAlarmConfiguration ->
        fun ~id ->
          fun ~callerReference ->
            fun ~healthCheckConfig ->
              fun ~healthCheckVersion ->
                fun () ->
                  {
                    linkedService;
                    cloudWatchAlarmConfiguration;
                    id;
                    callerReference;
                    healthCheckConfig;
                    healthCheckVersion
                  }
    let to_value x =
      structure_to_value
        [("Id", (Some (HealthCheckId.to_value x.id)));
        ("CallerReference",
          (Some (HealthCheckNonce.to_value x.callerReference)));
        ("LinkedService",
          (Option.map x.linkedService ~f:LinkedService.to_value));
        ("HealthCheckConfig",
          (Some (HealthCheckConfig.to_value x.healthCheckConfig)));
        ("HealthCheckVersion",
          (Some (HealthCheckVersion.to_value x.healthCheckVersion)));
        ("CloudWatchAlarmConfiguration",
          (Option.map x.cloudWatchAlarmConfiguration
             ~f:CloudWatchAlarmConfiguration.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cloudWatchAlarmConfiguration =
        (Option.map ~f:CloudWatchAlarmConfiguration.of_xml)
          (Xml.child xml_arg0 "CloudWatchAlarmConfiguration") in
      let healthCheckVersion =
        HealthCheckVersion.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HealthCheckVersion") in
      let healthCheckConfig =
        HealthCheckConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HealthCheckConfig") in
      let linkedService =
        (Option.map ~f:LinkedService.of_xml)
          (Xml.child xml_arg0 "LinkedService") in
      let callerReference =
        HealthCheckNonce.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CallerReference") in
      let id =
        HealthCheckId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ?cloudWatchAlarmConfiguration ~healthCheckVersion
        ~healthCheckConfig ?linkedService ~callerReference ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let cloudWatchAlarmConfiguration =
        field_map json "CloudWatchAlarmConfiguration"
          CloudWatchAlarmConfiguration.of_json in
      let healthCheckVersion =
        field_map_exn json "HealthCheckVersion" HealthCheckVersion.of_json in
      let healthCheckConfig =
        field_map_exn json "HealthCheckConfig" HealthCheckConfig.of_json in
      let linkedService =
        field_map json "LinkedService" LinkedService.of_json in
      let callerReference =
        field_map_exn json "CallerReference" HealthCheckNonce.of_json in
      let id = field_map_exn json "Id" HealthCheckId.of_json in
      make ?cloudWatchAlarmConfiguration ~healthCheckVersion
        ~healthCheckConfig ?linkedService ~callerReference ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about one health check that is associated with the current Amazon Web Services account."]
module GeoLocationDetails =
  struct
    type nonrec t =
      {
      continentCode: GeoLocationContinentCode.t option
        [@ocaml.doc "The two-letter code for the continent."];
      continentName: GeoLocationContinentName.t option
        [@ocaml.doc "The full name of the continent."];
      countryCode: GeoLocationCountryCode.t option
        [@ocaml.doc "The two-letter code for the country."];
      countryName: GeoLocationCountryName.t option
        [@ocaml.doc "The name of the country."];
      subdivisionCode: GeoLocationSubdivisionCode.t option
        [@ocaml.doc
          "The code for the subdivision, such as a particular state within the United States. For a list of US state abbreviations, see Appendix B: Two\226\128\147Letter State and Possession Abbreviations on the United States Postal Service website. For a list of all supported subdivision codes, use the ListGeoLocations API."];
      subdivisionName: GeoLocationSubdivisionName.t option
        [@ocaml.doc
          "The full name of the subdivision. Route 53 currently supports only states in the United States."]}
    let make ?continentCode =
      fun ?continentName ->
        fun ?countryCode ->
          fun ?countryName ->
            fun ?subdivisionCode ->
              fun ?subdivisionName ->
                fun () ->
                  {
                    continentCode;
                    continentName;
                    countryCode;
                    countryName;
                    subdivisionCode;
                    subdivisionName
                  }
    let to_value x =
      structure_to_value
        [("ContinentCode",
           (Option.map x.continentCode ~f:GeoLocationContinentCode.to_value));
        ("ContinentName",
          (Option.map x.continentName ~f:GeoLocationContinentName.to_value));
        ("CountryCode",
          (Option.map x.countryCode ~f:GeoLocationCountryCode.to_value));
        ("CountryName",
          (Option.map x.countryName ~f:GeoLocationCountryName.to_value));
        ("SubdivisionCode",
          (Option.map x.subdivisionCode
             ~f:GeoLocationSubdivisionCode.to_value));
        ("SubdivisionName",
          (Option.map x.subdivisionName
             ~f:GeoLocationSubdivisionName.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let subdivisionName =
        (Option.map ~f:GeoLocationSubdivisionName.of_xml)
          (Xml.child xml_arg0 "SubdivisionName") in
      let subdivisionCode =
        (Option.map ~f:GeoLocationSubdivisionCode.of_xml)
          (Xml.child xml_arg0 "SubdivisionCode") in
      let countryName =
        (Option.map ~f:GeoLocationCountryName.of_xml)
          (Xml.child xml_arg0 "CountryName") in
      let countryCode =
        (Option.map ~f:GeoLocationCountryCode.of_xml)
          (Xml.child xml_arg0 "CountryCode") in
      let continentName =
        (Option.map ~f:GeoLocationContinentName.of_xml)
          (Xml.child xml_arg0 "ContinentName") in
      let continentCode =
        (Option.map ~f:GeoLocationContinentCode.of_xml)
          (Xml.child xml_arg0 "ContinentCode") in
      make ?subdivisionName ?subdivisionCode ?countryName ?countryCode
        ?continentName ?continentCode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let subdivisionName =
        field_map json "SubdivisionName" GeoLocationSubdivisionName.of_json in
      let subdivisionCode =
        field_map json "SubdivisionCode" GeoLocationSubdivisionCode.of_json in
      let countryName =
        field_map json "CountryName" GeoLocationCountryName.of_json in
      let countryCode =
        field_map json "CountryCode" GeoLocationCountryCode.of_json in
      let continentName =
        field_map json "ContinentName" GeoLocationContinentName.of_json in
      let continentCode =
        field_map json "ContinentCode" GeoLocationContinentCode.of_json in
      make ?subdivisionName ?subdivisionCode ?countryName ?countryCode
        ?continentName ?continentCode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the codes and full continent, country, and subdivision names for the specified geolocation code."]
module LimitValue =
  struct
    type nonrec t = Int64.t
    let make i =
      let open Result in ok_or_failwith (check_int64_min i ~min:1L); i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module ReusableDelegationSetLimitType =
  struct
    type nonrec t =
      | MAX_ZONES_BY_REUSABLE_DELEGATION_SET 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | MAX_ZONES_BY_REUSABLE_DELEGATION_SET ->
          "MAX_ZONES_BY_REUSABLE_DELEGATION_SET"
      | Non_static_id s -> s
    let of_string =
      function
      | "MAX_ZONES_BY_REUSABLE_DELEGATION_SET" ->
          MAX_ZONES_BY_REUSABLE_DELEGATION_SET
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration ReusableDelegationSetLimitType"
           xml_arg0)
    let of_json j =
      of_string (string_of_json ~kind:"ReusableDelegationSetLimitType" j)
    let to_json = simple_to_json to_value
  end
module HostedZoneLimitType =
  struct
    type nonrec t =
      | MAX_RRSETS_BY_ZONE 
      | MAX_VPCS_ASSOCIATED_BY_ZONE 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | MAX_RRSETS_BY_ZONE -> "MAX_RRSETS_BY_ZONE"
      | MAX_VPCS_ASSOCIATED_BY_ZONE -> "MAX_VPCS_ASSOCIATED_BY_ZONE"
      | Non_static_id s -> s
    let of_string =
      function
      | "MAX_RRSETS_BY_ZONE" -> MAX_RRSETS_BY_ZONE
      | "MAX_VPCS_ASSOCIATED_BY_ZONE" -> MAX_VPCS_ASSOCIATED_BY_ZONE
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string
        (string_of_xml ~kind:"enumeration HostedZoneLimitType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"HostedZoneLimitType" j)
    let to_json = simple_to_json to_value
  end
module HealthCheckObservation =
  struct
    type nonrec t =
      {
      region: HealthCheckRegion.t option
        [@ocaml.doc
          "The region of the Amazon Route 53 health checker that provided the status in StatusReport."];
      iPAddress: IPAddress.t option
        [@ocaml.doc
          "The IP address of the Amazon Route 53 health checker that provided the failure reason in StatusReport."];
      statusReport: StatusReport.t option
        [@ocaml.doc
          "A complex type that contains the last failure reason as reported by one Amazon Route 53 health checker and the time of the failed health check."]}
    let make ?region =
      fun ?iPAddress ->
        fun ?statusReport -> fun () -> { region; iPAddress; statusReport }
    let to_value x =
      structure_to_value
        [("Region", (Option.map x.region ~f:HealthCheckRegion.to_value));
        ("IPAddress", (Option.map x.iPAddress ~f:IPAddress.to_value));
        ("StatusReport",
          (Option.map x.statusReport ~f:StatusReport.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let statusReport =
        (Option.map ~f:StatusReport.of_xml)
          (Xml.child xml_arg0 "StatusReport") in
      let iPAddress =
        (Option.map ~f:IPAddress.of_xml) (Xml.child xml_arg0 "IPAddress") in
      let region =
        (Option.map ~f:HealthCheckRegion.of_xml)
          (Xml.child xml_arg0 "Region") in
      make ?statusReport ?iPAddress ?region ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let statusReport = field_map json "StatusReport" StatusReport.of_json in
      let iPAddress = field_map json "IPAddress" IPAddress.of_json in
      let region = field_map json "Region" HealthCheckRegion.of_json in
      make ?statusReport ?iPAddress ?region ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the last failure reason as reported by one Amazon Route 53 health checker."]
module ServeSignature =
  struct
    type nonrec t = string
    let context_ = "ServeSignature"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:1024) >>=
             (fun () -> check_string_min i ~min:1));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ServeSignature" j
    let to_json = simple_to_json to_value
  end
module KeySigningKey =
  struct
    type nonrec t =
      {
      name: SigningKeyName.t option
        [@ocaml.doc
          "A string used to identify a key-signing key (KSK). Name can include numbers, letters, and underscores (_). Name must be unique for each key-signing key in the same hosted zone."];
      kmsArn: SigningKeyString.t option
        [@ocaml.doc
          "The Amazon resource name (ARN) used to identify the customer managed key in Key Management Service (KMS). The KmsArn must be unique for each key-signing key (KSK) in a single hosted zone. You must configure the customer managed key as follows: Status Enabled Key spec ECC_NIST_P256 Key usage Sign and verify Key policy The key policy must give permission for the following actions: DescribeKey GetPublicKey Sign The key policy must also include the Amazon Route 53 service in the principal for your account. Specify the following: \"Service\": \"dnssec-route53.amazonaws.com\" For more information about working with the customer managed key in KMS, see Key Management Service concepts."];
      flag: SigningKeyInteger.t option
        [@ocaml.doc
          "An integer that specifies how the key is used. For key-signing key (KSK), this value is always 257."];
      signingAlgorithmMnemonic: SigningKeyString.t option
        [@ocaml.doc
          "A string used to represent the signing algorithm. This value must follow the guidelines provided by RFC-8624 Section 3.1."];
      signingAlgorithmType: SigningKeyInteger.t option
        [@ocaml.doc
          "An integer used to represent the signing algorithm. This value must follow the guidelines provided by RFC-8624 Section 3.1."];
      digestAlgorithmMnemonic: SigningKeyString.t option
        [@ocaml.doc
          "A string used to represent the delegation signer digest algorithm. This value must follow the guidelines provided by RFC-8624 Section 3.3."];
      digestAlgorithmType: SigningKeyInteger.t option
        [@ocaml.doc
          "An integer used to represent the delegation signer digest algorithm. This value must follow the guidelines provided by RFC-8624 Section 3.3."];
      keyTag: SigningKeyTag.t option
        [@ocaml.doc
          "An integer used to identify the DNSSEC record for the domain name. The process used to calculate the value is described in RFC-4034 Appendix B."];
      digestValue: SigningKeyString.t option
        [@ocaml.doc
          "A cryptographic digest of a DNSKEY resource record (RR). DNSKEY records are used to publish the public key that resolvers can use to verify DNSSEC signatures that are used to secure certain kinds of information provided by the DNS system."];
      publicKey: SigningKeyString.t option
        [@ocaml.doc
          "The public key, represented as a Base64 encoding, as required by RFC-4034 Page 5."];
      dSRecord: SigningKeyString.t option
        [@ocaml.doc
          "A string that represents a delegation signer (DS) record."];
      dNSKEYRecord: SigningKeyString.t option
        [@ocaml.doc "A string that represents a DNSKEY record."];
      status: SigningKeyStatus.t option
        [@ocaml.doc
          "A string that represents the current key-signing key (KSK) status. Status can have one of the following values: ACTIVE The KSK is being used for signing. INACTIVE The KSK is not being used for signing. DELETING The KSK is in the process of being deleted. ACTION_NEEDED There is a problem with the KSK that requires you to take action to resolve. For example, the customer managed key might have been deleted, or the permissions for the customer managed key might have been changed. INTERNAL_FAILURE There was an error during a request. Before you can continue to work with DNSSEC signing, including actions that involve this KSK, you must correct the problem. For example, you may need to activate or deactivate the KSK."];
      statusMessage: SigningKeyStatusMessage.t option
        [@ocaml.doc
          "The status message provided for the following key-signing key (KSK) statuses: ACTION_NEEDED or INTERNAL_FAILURE. The status message includes information about what the problem might be and steps that you can take to correct the issue."];
      createdDate: TimeStamp.t option
        [@ocaml.doc "The date when the key-signing key (KSK) was created."];
      lastModifiedDate: TimeStamp.t option
        [@ocaml.doc
          "The last time that the key-signing key (KSK) was changed."]}
    let make ?name =
      fun ?kmsArn ->
        fun ?flag ->
          fun ?signingAlgorithmMnemonic ->
            fun ?signingAlgorithmType ->
              fun ?digestAlgorithmMnemonic ->
                fun ?digestAlgorithmType ->
                  fun ?keyTag ->
                    fun ?digestValue ->
                      fun ?publicKey ->
                        fun ?dSRecord ->
                          fun ?dNSKEYRecord ->
                            fun ?status ->
                              fun ?statusMessage ->
                                fun ?createdDate ->
                                  fun ?lastModifiedDate ->
                                    fun () ->
                                      {
                                        name;
                                        kmsArn;
                                        flag;
                                        signingAlgorithmMnemonic;
                                        signingAlgorithmType;
                                        digestAlgorithmMnemonic;
                                        digestAlgorithmType;
                                        keyTag;
                                        digestValue;
                                        publicKey;
                                        dSRecord;
                                        dNSKEYRecord;
                                        status;
                                        statusMessage;
                                        createdDate;
                                        lastModifiedDate
                                      }
    let to_value x =
      structure_to_value
        [("Name", (Option.map x.name ~f:SigningKeyName.to_value));
        ("KmsArn", (Option.map x.kmsArn ~f:SigningKeyString.to_value));
        ("Flag", (Option.map x.flag ~f:SigningKeyInteger.to_value));
        ("SigningAlgorithmMnemonic",
          (Option.map x.signingAlgorithmMnemonic ~f:SigningKeyString.to_value));
        ("SigningAlgorithmType",
          (Option.map x.signingAlgorithmType ~f:SigningKeyInteger.to_value));
        ("DigestAlgorithmMnemonic",
          (Option.map x.digestAlgorithmMnemonic ~f:SigningKeyString.to_value));
        ("DigestAlgorithmType",
          (Option.map x.digestAlgorithmType ~f:SigningKeyInteger.to_value));
        ("KeyTag", (Option.map x.keyTag ~f:SigningKeyTag.to_value));
        ("DigestValue",
          (Option.map x.digestValue ~f:SigningKeyString.to_value));
        ("PublicKey", (Option.map x.publicKey ~f:SigningKeyString.to_value));
        ("DSRecord", (Option.map x.dSRecord ~f:SigningKeyString.to_value));
        ("DNSKEYRecord",
          (Option.map x.dNSKEYRecord ~f:SigningKeyString.to_value));
        ("Status", (Option.map x.status ~f:SigningKeyStatus.to_value));
        ("StatusMessage",
          (Option.map x.statusMessage ~f:SigningKeyStatusMessage.to_value));
        ("CreatedDate", (Option.map x.createdDate ~f:TimeStamp.to_value));
        ("LastModifiedDate",
          (Option.map x.lastModifiedDate ~f:TimeStamp.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let lastModifiedDate =
        (Option.map ~f:TimeStamp.of_xml)
          (Xml.child xml_arg0 "LastModifiedDate") in
      let createdDate =
        (Option.map ~f:TimeStamp.of_xml) (Xml.child xml_arg0 "CreatedDate") in
      let statusMessage =
        (Option.map ~f:SigningKeyStatusMessage.of_xml)
          (Xml.child xml_arg0 "StatusMessage") in
      let status =
        (Option.map ~f:SigningKeyStatus.of_xml) (Xml.child xml_arg0 "Status") in
      let dNSKEYRecord =
        (Option.map ~f:SigningKeyString.of_xml)
          (Xml.child xml_arg0 "DNSKEYRecord") in
      let dSRecord =
        (Option.map ~f:SigningKeyString.of_xml)
          (Xml.child xml_arg0 "DSRecord") in
      let publicKey =
        (Option.map ~f:SigningKeyString.of_xml)
          (Xml.child xml_arg0 "PublicKey") in
      let digestValue =
        (Option.map ~f:SigningKeyString.of_xml)
          (Xml.child xml_arg0 "DigestValue") in
      let keyTag =
        (Option.map ~f:SigningKeyTag.of_xml) (Xml.child xml_arg0 "KeyTag") in
      let digestAlgorithmType =
        (Option.map ~f:SigningKeyInteger.of_xml)
          (Xml.child xml_arg0 "DigestAlgorithmType") in
      let digestAlgorithmMnemonic =
        (Option.map ~f:SigningKeyString.of_xml)
          (Xml.child xml_arg0 "DigestAlgorithmMnemonic") in
      let signingAlgorithmType =
        (Option.map ~f:SigningKeyInteger.of_xml)
          (Xml.child xml_arg0 "SigningAlgorithmType") in
      let signingAlgorithmMnemonic =
        (Option.map ~f:SigningKeyString.of_xml)
          (Xml.child xml_arg0 "SigningAlgorithmMnemonic") in
      let flag =
        (Option.map ~f:SigningKeyInteger.of_xml) (Xml.child xml_arg0 "Flag") in
      let kmsArn =
        (Option.map ~f:SigningKeyString.of_xml) (Xml.child xml_arg0 "KmsArn") in
      let name =
        (Option.map ~f:SigningKeyName.of_xml) (Xml.child xml_arg0 "Name") in
      make ?lastModifiedDate ?createdDate ?statusMessage ?status
        ?dNSKEYRecord ?dSRecord ?publicKey ?digestValue ?keyTag
        ?digestAlgorithmType ?digestAlgorithmMnemonic ?signingAlgorithmType
        ?signingAlgorithmMnemonic ?flag ?kmsArn ?name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let lastModifiedDate =
        field_map json "LastModifiedDate" TimeStamp.of_json in
      let createdDate = field_map json "CreatedDate" TimeStamp.of_json in
      let statusMessage =
        field_map json "StatusMessage" SigningKeyStatusMessage.of_json in
      let status = field_map json "Status" SigningKeyStatus.of_json in
      let dNSKEYRecord =
        field_map json "DNSKEYRecord" SigningKeyString.of_json in
      let dSRecord = field_map json "DSRecord" SigningKeyString.of_json in
      let publicKey = field_map json "PublicKey" SigningKeyString.of_json in
      let digestValue = field_map json "DigestValue" SigningKeyString.of_json in
      let keyTag = field_map json "KeyTag" SigningKeyTag.of_json in
      let digestAlgorithmType =
        field_map json "DigestAlgorithmType" SigningKeyInteger.of_json in
      let digestAlgorithmMnemonic =
        field_map json "DigestAlgorithmMnemonic" SigningKeyString.of_json in
      let signingAlgorithmType =
        field_map json "SigningAlgorithmType" SigningKeyInteger.of_json in
      let signingAlgorithmMnemonic =
        field_map json "SigningAlgorithmMnemonic" SigningKeyString.of_json in
      let flag = field_map json "Flag" SigningKeyInteger.of_json in
      let kmsArn = field_map json "KmsArn" SigningKeyString.of_json in
      let name = field_map json "Name" SigningKeyName.of_json in
      make ?lastModifiedDate ?createdDate ?statusMessage ?status
        ?dNSKEYRecord ?dSRecord ?publicKey ?digestValue ?keyTag
        ?digestAlgorithmType ?digestAlgorithmMnemonic ?signingAlgorithmType
        ?signingAlgorithmMnemonic ?flag ?kmsArn ?name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A key-signing key (KSK) is a complex type that represents a public/private key pair. The private key is used to generate a digital signature for the zone signing key (ZSK). The public key is stored in the DNS and is used to authenticate the ZSK. A KSK is always associated with a hosted zone; it cannot exist by itself."]
module IPAddressCidr =
  struct
    type nonrec t = string
    let context_ = "IPAddressCidr"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"IPAddressCidr" j
    let to_json = simple_to_json to_value
  end
module ChangeStatus =
  struct
    type nonrec t =
      | PENDING 
      | INSYNC 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | PENDING -> "PENDING"
      | INSYNC -> "INSYNC"
      | Non_static_id s -> s
    let of_string =
      function
      | "PENDING" -> PENDING
      | "INSYNC" -> INSYNC
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration ChangeStatus" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"ChangeStatus" j)
    let to_json = simple_to_json to_value
  end
module AccountLimitType =
  struct
    type nonrec t =
      | MAX_HEALTH_CHECKS_BY_OWNER 
      | MAX_HOSTED_ZONES_BY_OWNER 
      | MAX_TRAFFIC_POLICY_INSTANCES_BY_OWNER 
      | MAX_REUSABLE_DELEGATION_SETS_BY_OWNER 
      | MAX_TRAFFIC_POLICIES_BY_OWNER 
      | Non_static_id of string 
    let make i = i
    let to_string =
      function
      | MAX_HEALTH_CHECKS_BY_OWNER -> "MAX_HEALTH_CHECKS_BY_OWNER"
      | MAX_HOSTED_ZONES_BY_OWNER -> "MAX_HOSTED_ZONES_BY_OWNER"
      | MAX_TRAFFIC_POLICY_INSTANCES_BY_OWNER ->
          "MAX_TRAFFIC_POLICY_INSTANCES_BY_OWNER"
      | MAX_REUSABLE_DELEGATION_SETS_BY_OWNER ->
          "MAX_REUSABLE_DELEGATION_SETS_BY_OWNER"
      | MAX_TRAFFIC_POLICIES_BY_OWNER -> "MAX_TRAFFIC_POLICIES_BY_OWNER"
      | Non_static_id s -> s
    let of_string =
      function
      | "MAX_HEALTH_CHECKS_BY_OWNER" -> MAX_HEALTH_CHECKS_BY_OWNER
      | "MAX_HOSTED_ZONES_BY_OWNER" -> MAX_HOSTED_ZONES_BY_OWNER
      | "MAX_TRAFFIC_POLICY_INSTANCES_BY_OWNER" ->
          MAX_TRAFFIC_POLICY_INSTANCES_BY_OWNER
      | "MAX_REUSABLE_DELEGATION_SETS_BY_OWNER" ->
          MAX_REUSABLE_DELEGATION_SETS_BY_OWNER
      | "MAX_TRAFFIC_POLICIES_BY_OWNER" -> MAX_TRAFFIC_POLICIES_BY_OWNER
      | x -> Non_static_id x
    let to_value x = `Enum (to_string x)
    let to_query v = to_query to_value v
    let to_header x = to_string x
    let of_xml xml_arg0 =
      of_string (string_of_xml ~kind:"enumeration AccountLimitType" xml_arg0)
    let of_json j = of_string (string_of_json ~kind:"AccountLimitType" j)
    let to_json = simple_to_json to_value
  end
module ErrorMessages =
  struct
    type nonrec t = ErrorMessage.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ErrorMessage.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ErrorMessage.of_xml)
    let of_json j =
      list_of_json ~kind:"ErrorMessages" ~of_json:ErrorMessage.of_json j
    let to_json v = composed_to_json to_value v
  end
module Changes =
  struct
    type nonrec t = Change.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let to_value xs =
      (xs |> (List.map ~f:Change.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:Change.of_xml)
    let of_json j = list_of_json ~kind:"Changes" ~of_json:Change.of_json j
    let to_json v = composed_to_json to_value v
  end
module ConflictingTypes =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You tried to update a traffic policy instance by using a traffic policy version that has a different DNS type than the current type for the instance. You specified the type in the JSON document in the CreateTrafficPolicy or CreateTrafficPolicyVersionrequest."]
module InvalidInput =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The input is not valid."]
module NoSuchTrafficPolicy =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "No traffic policy exists with the specified ID."]
module NoSuchTrafficPolicyInstance =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "No traffic policy instance exists with the specified ID."]
module PriorRequestNotComplete =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "If Amazon Route 53 can't process a request before the next request arrives, it will reject subsequent requests for the same hosted zone and return an HTTP 400 error (Bad request). If Route 53 returns this error repeatedly for the same request, we recommend that you wait, in intervals of increasing duration, before you try the request again."]
module ConcurrentModification =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Another user submitted a request to create, update, or delete the object at the same time that you did. Retry the request."]
module NoSuchHostedZone =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "No hosted zone exists with the ID that you specified."]
module HealthCheckVersionMismatch =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The value of HealthCheckVersion in the request doesn't match the value of HealthCheckVersion in the health check."]
module NoSuchHealthCheck =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "No health check exists with the specified ID."]
module ResettableElementNameList =
  struct
    type nonrec t = ResettableElementName.t list
    let make i =
      let open Result in ok_or_failwith (check_list_max i ~max:64); i
    let to_value xs =
      (xs |> (List.map ~f:ResettableElementName.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResettableElementName.of_xml)
    let of_json j =
      list_of_json ~kind:"ResettableElementNameList"
        ~of_json:ResettableElementName.of_json j
    let to_json v = composed_to_json to_value v
  end
module DNSRCode =
  struct
    type nonrec t = string
    let context_ = "DNSRCode"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DNSRCode" j
    let to_json = simple_to_json to_value
  end
module Nameserver =
  struct
    type nonrec t = string
    let context_ = "Nameserver"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:255) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"Nameserver" j
    let to_json = simple_to_json to_value
  end
module RecordData =
  struct
    type nonrec t = RecordDataEntry.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:RecordDataEntry.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:RecordDataEntry.of_xml)
    let of_json j =
      list_of_json ~kind:"RecordData" ~of_json:RecordDataEntry.of_json j
    let to_json v = composed_to_json to_value v
  end
module TransportProtocol =
  struct
    type nonrec t = string
    let context_ = "TransportProtocol"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TransportProtocol" j
    let to_json = simple_to_json to_value
  end
module SubnetMask =
  struct
    type nonrec t = string
    let context_ = "SubnetMask"
    let make i =
      let open Result in
        ok_or_failwith
          ((check_string_max i ~max:3) >>=
             (fun () -> check_string_min i ~min:0));
        i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"SubnetMask" j
    let to_json = simple_to_json to_value
  end
module InvalidPaginationToken =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The value that you specified to get the second or subsequent page of results is invalid."]
module PaginationToken =
  struct
    type nonrec t = string
    let context_ = "PaginationToken"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:1024); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PaginationToken" j
    let to_json = simple_to_json to_value
  end
module VPCs =
  struct
    type nonrec t = VPC.t list
    let make i =
      let open Result in ok_or_failwith (check_list_min i ~min:1); i
    let to_value xs =
      (xs |> (List.map ~f:VPC.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:VPC.of_xml)
    let of_json j = list_of_json ~kind:"VPCs" ~of_json:VPC.of_json j
    let to_json v = composed_to_json to_value v
  end
module MaxResults =
  struct
    type nonrec t = string
    let context_ = "MaxResults"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"MaxResults" j
    let to_json = simple_to_json to_value
  end
module PageMaxItems =
  struct
    type nonrec t = string
    let context_ = "PageMaxItems"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PageMaxItems" j
    let to_json = simple_to_json to_value
  end
module PageTruncated =
  struct
    type nonrec t = bool
    let make i = i
    let of_string = Bool.of_string
    let to_value x = `Boolean x
    let to_query v = to_query to_value v
    let to_header x = Bool.to_string x
    let of_xml xml_arg0 =
      Bool.of_string (string_of_xml ~kind:"a boolean" xml_arg0)
    let of_json = bool_of_json
    let to_json = simple_to_json to_value
  end
module TrafficPolicies =
  struct
    type nonrec t = TrafficPolicy.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TrafficPolicy.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TrafficPolicy.of_xml)
    let of_json j =
      list_of_json ~kind:"TrafficPolicies" ~of_json:TrafficPolicy.of_json j
    let to_json v = composed_to_json to_value v
  end
module TrafficPolicyVersionMarker =
  struct
    type nonrec t = string
    let context_ = "TrafficPolicyVersionMarker"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:4); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"TrafficPolicyVersionMarker" j
    let to_json = simple_to_json to_value
  end
module TrafficPolicyInstances =
  struct
    type nonrec t = TrafficPolicyInstance.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TrafficPolicyInstance.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TrafficPolicyInstance.of_xml)
    let of_json j =
      list_of_json ~kind:"TrafficPolicyInstances"
        ~of_json:TrafficPolicyInstance.of_json j
    let to_json v = composed_to_json to_value v
  end
module TrafficPolicySummaries =
  struct
    type nonrec t = TrafficPolicySummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:TrafficPolicySummary.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TrafficPolicySummary.of_xml)
    let of_json j =
      list_of_json ~kind:"TrafficPolicySummaries"
        ~of_json:TrafficPolicySummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module ResourceTagSetList =
  struct
    type nonrec t = ResourceTagSet.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ResourceTagSet.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResourceTagSet.of_xml)
    let of_json j =
      list_of_json ~kind:"ResourceTagSetList" ~of_json:ResourceTagSet.of_json
        j
    let to_json v = composed_to_json to_value v
  end
module ThrottlingException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The limit on the number of requests per second was exceeded."]
module TagResourceIdList =
  struct
    type nonrec t = TagResourceId.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:TagResourceId.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagResourceId.of_xml)
    let of_json j =
      list_of_json ~kind:"TagResourceIdList" ~of_json:TagResourceId.of_json j
    let to_json v = composed_to_json to_value v
  end
module DelegationSets =
  struct
    type nonrec t = DelegationSet.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:DelegationSet.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:DelegationSet.of_xml)
    let of_json j =
      list_of_json ~kind:"DelegationSets" ~of_json:DelegationSet.of_json j
    let to_json v = composed_to_json to_value v
  end
module PageMarker =
  struct
    type nonrec t = string
    let context_ = "PageMarker"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:64); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"PageMarker" j
    let to_json = simple_to_json to_value
  end
module ResourceRecordSets =
  struct
    type nonrec t = ResourceRecordSet.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:ResourceRecordSet.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:ResourceRecordSet.of_xml)
    let of_json j =
      list_of_json ~kind:"ResourceRecordSets"
        ~of_json:ResourceRecordSet.of_json j
    let to_json v = composed_to_json to_value v
  end
module QueryLoggingConfigs =
  struct
    type nonrec t = QueryLoggingConfig.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:QueryLoggingConfig.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:QueryLoggingConfig.of_xml)
    let of_json j =
      list_of_json ~kind:"QueryLoggingConfigs"
        ~of_json:QueryLoggingConfig.of_json j
    let to_json v = composed_to_json to_value v
  end
module DelegationSetNotReusable =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A reusable delegation set with the specified ID does not exist."]
module HostedZones =
  struct
    type nonrec t = HostedZone.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:HostedZone.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:HostedZone.of_xml)
    let of_json j =
      list_of_json ~kind:"HostedZones" ~of_json:HostedZone.of_json j
    let to_json v = composed_to_json to_value v
  end
module NoSuchDelegationSet =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A reusable delegation set with the specified ID does not exist."]
module HostedZoneSummaries =
  struct
    type nonrec t = HostedZoneSummary.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:HostedZoneSummary.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:HostedZoneSummary.of_xml)
    let of_json j =
      list_of_json ~kind:"HostedZoneSummaries"
        ~of_json:HostedZoneSummary.of_json j
    let to_json v = composed_to_json to_value v
  end
module InvalidDomainName =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The specified domain name is not valid."]
module HealthChecks =
  struct
    type nonrec t = HealthCheck.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:HealthCheck.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:HealthCheck.of_xml)
    let of_json j =
      list_of_json ~kind:"HealthChecks" ~of_json:HealthCheck.of_json j
    let to_json v = composed_to_json to_value v
  end
module IncompatibleVersion =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The resource you're trying to access is unsupported on this Amazon Route 53 endpoint."]
module GeoLocationDetailsList =
  struct
    type nonrec t = GeoLocationDetails.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:GeoLocationDetails.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:GeoLocationDetails.of_xml)
    let of_json j =
      list_of_json ~kind:"GeoLocationDetailsList"
        ~of_json:GeoLocationDetails.of_json j
    let to_json v = composed_to_json to_value v
  end
module TrafficPolicyInstanceCount =
  struct
    type nonrec t = int
    let make i = i
    let of_string = Int.of_string
    let to_value x = `Integer x
    let to_query v = to_query to_value v
    let to_header x = Int.to_string x
    let of_xml xml_arg0 =
      Int.of_string
        (string_of_xml ~kind:"an integer for TrafficPolicyInstanceCount"
           xml_arg0)
    let of_json j = Int.of_float (float_of_json ~kind:"an integer" j)
    let to_json = simple_to_json to_value
  end
module ReusableDelegationSetLimit =
  struct
    type nonrec t =
      {
      type_: ReusableDelegationSetLimitType.t
        [@ocaml.doc
          "The limit that you requested: MAX_ZONES_BY_REUSABLE_DELEGATION_SET, the maximum number of hosted zones that you can associate with the specified reusable delegation set."];
      value: LimitValue.t
        [@ocaml.doc
          "The current value for the MAX_ZONES_BY_REUSABLE_DELEGATION_SET limit."]}
    let context_ = "ReusableDelegationSetLimit"
    let make ~type_ = fun ~value -> fun () -> { type_; value }
    let to_value x =
      structure_to_value
        [("Type", (Some (ReusableDelegationSetLimitType.to_value x.type_)));
        ("Value", (Some (LimitValue.to_value x.value)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        LimitValue.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Value") in
      let type_ =
        ReusableDelegationSetLimitType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Type") in
      make ~value ~type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let value = field_map_exn json "Value" LimitValue.of_json in
      let type_ =
        field_map_exn json "Type" ReusableDelegationSetLimitType.of_json in
      make ~value ~type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the type of limit that you specified in the request and the current value for that limit."]
module UsageCount =
  struct
    type nonrec t = Int64.t
    let make i =
      let open Result in ok_or_failwith (check_int64_min i ~min:0L); i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module NoSuchQueryLoggingConfig =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "There is no DNS query logging configuration with the specified ID."]
module HostedZoneLimit =
  struct
    type nonrec t =
      {
      type_: HostedZoneLimitType.t
        [@ocaml.doc
          "The limit that you requested. Valid values include the following: MAX_RRSETS_BY_ZONE: The maximum number of records that you can create in the specified hosted zone. MAX_VPCS_ASSOCIATED_BY_ZONE: The maximum number of Amazon VPCs that you can associate with the specified private hosted zone."];
      value: LimitValue.t
        [@ocaml.doc
          "The current value for the limit that is specified by Type."]}
    let context_ = "HostedZoneLimit"
    let make ~type_ = fun ~value -> fun () -> { type_; value }
    let to_value x =
      structure_to_value
        [("Type", (Some (HostedZoneLimitType.to_value x.type_)));
        ("Value", (Some (LimitValue.to_value x.value)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        LimitValue.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Value") in
      let type_ =
        HostedZoneLimitType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Type") in
      make ~value ~type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let value = field_map_exn json "Value" LimitValue.of_json in
      let type_ = field_map_exn json "Type" HostedZoneLimitType.of_json in
      make ~value ~type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the type of limit that you specified in the request and the current value for that limit."]
module HostedZoneNotPrivate =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified hosted zone is a public hosted zone, not a private hosted zone."]
module HostedZoneCount =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module HealthCheckObservations =
  struct
    type nonrec t = HealthCheckObservation.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:HealthCheckObservation.to_value)) |>
        (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:HealthCheckObservation.of_xml)
    let of_json j =
      list_of_json ~kind:"HealthCheckObservations"
        ~of_json:HealthCheckObservation.of_json j
    let to_json v = composed_to_json to_value v
  end
module HealthCheckCount =
  struct
    type nonrec t = Int64.t
    let make i = i
    let of_string = Int64.of_string
    let to_value x = `Long x
    let to_query v = to_query to_value v
    let to_header x = Int64.to_string x
    let of_xml xml_arg0 =
      Int64.of_string (string_of_xml ~kind:"a long" xml_arg0)
    let of_json j = Int64.of_float (float_of_json ~kind:"a long" j)
    let to_json = simple_to_json to_value
  end
module NoSuchGeoLocation =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Amazon Route 53 doesn't support the specified geographic location. For a list of supported geolocation codes, see the GeoLocation data type."]
module DNSSECStatus =
  struct
    type nonrec t =
      {
      serveSignature: ServeSignature.t option
        [@ocaml.doc
          "A string that represents the current hosted zone signing status. Status can have one of the following values: SIGNING DNSSEC signing is enabled for the hosted zone. NOT_SIGNING DNSSEC signing is not enabled for the hosted zone. DELETING DNSSEC signing is in the process of being removed for the hosted zone. ACTION_NEEDED There is a problem with signing in the hosted zone that requires you to take action to resolve. For example, the customer managed key might have been deleted, or the permissions for the customer managed key might have been changed. INTERNAL_FAILURE There was an error during a request. Before you can continue to work with DNSSEC signing, including with key-signing keys (KSKs), you must correct the problem by enabling or disabling DNSSEC signing for the hosted zone."];
      statusMessage: SigningKeyStatusMessage.t option
        [@ocaml.doc
          "The status message provided for the following DNSSEC signing status: INTERNAL_FAILURE. The status message includes information about what the problem might be and steps that you can take to correct the issue."]}
    let make ?serveSignature =
      fun ?statusMessage -> fun () -> { serveSignature; statusMessage }
    let to_value x =
      structure_to_value
        [("ServeSignature",
           (Option.map x.serveSignature ~f:ServeSignature.to_value));
        ("StatusMessage",
          (Option.map x.statusMessage ~f:SigningKeyStatusMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let statusMessage =
        (Option.map ~f:SigningKeyStatusMessage.of_xml)
          (Xml.child xml_arg0 "StatusMessage") in
      let serveSignature =
        (Option.map ~f:ServeSignature.of_xml)
          (Xml.child xml_arg0 "ServeSignature") in
      make ?statusMessage ?serveSignature ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let statusMessage =
        field_map json "StatusMessage" SigningKeyStatusMessage.of_json in
      let serveSignature =
        field_map json "ServeSignature" ServeSignature.of_json in
      make ?statusMessage ?serveSignature ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A string repesenting the status of DNSSEC signing."]
module InvalidArgument =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Parameter name is not valid."]
module KeySigningKeys =
  struct
    type nonrec t = KeySigningKey.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:KeySigningKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:KeySigningKey.of_xml)
    let of_json j =
      list_of_json ~kind:"KeySigningKeys" ~of_json:KeySigningKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module CheckerIpRanges =
  struct
    type nonrec t = IPAddressCidr.t list
    let make i = i
    let to_value xs =
      (xs |> (List.map ~f:IPAddressCidr.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:IPAddressCidr.of_xml)
    let of_json j =
      list_of_json ~kind:"CheckerIpRanges" ~of_json:IPAddressCidr.of_json j
    let to_json v = composed_to_json to_value v
  end
module ChangeInfo =
  struct
    type nonrec t =
      {
      id: ResourceId.t
        [@ocaml.doc
          "This element contains an ID that you use when performing a GetChange action to get detailed information about the change."];
      status: ChangeStatus.t
        [@ocaml.doc
          "The current state of the request. PENDING indicates that this request has not yet been applied to all Amazon Route 53 DNS servers."];
      submittedAt: TimeStamp.t
        [@ocaml.doc
          "The date and time that the change request was submitted in ISO 8601 format and Coordinated Universal Time (UTC). For example, the value 2017-03-27T17:48:16.751Z represents March 27, 2017 at 17:48:16.751 UTC."];
      comment: ResourceDescription.t option
        [@ocaml.doc "A comment you can provide."]}
    let context_ = "ChangeInfo"
    let make ?comment =
      fun ~id ->
        fun ~status ->
          fun ~submittedAt -> fun () -> { comment; id; status; submittedAt }
    let to_value x =
      structure_to_value
        [("Id", (Some (ResourceId.to_value x.id)));
        ("Status", (Some (ChangeStatus.to_value x.status)));
        ("SubmittedAt", (Some (TimeStamp.to_value x.submittedAt)));
        ("Comment", (Option.map x.comment ~f:ResourceDescription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let comment =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Comment") in
      let submittedAt =
        TimeStamp.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "SubmittedAt") in
      let status =
        ChangeStatus.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Status") in
      let id =
        ResourceId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ?comment ~submittedAt ~status ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let comment = field_map json "Comment" ResourceDescription.of_json in
      let submittedAt = field_map_exn json "SubmittedAt" TimeStamp.of_json in
      let status = field_map_exn json "Status" ChangeStatus.of_json in
      let id = field_map_exn json "Id" ResourceId.of_json in
      make ?comment ~submittedAt ~status ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that describes change information about changes made to your hosted zone."]
module NoSuchChange =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A change with the specified change ID does not exist."]
module AccountLimit =
  struct
    type nonrec t =
      {
      type_: AccountLimitType.t
        [@ocaml.doc
          "The limit that you requested. Valid values include the following: MAX_HEALTH_CHECKS_BY_OWNER: The maximum number of health checks that you can create using the current account. MAX_HOSTED_ZONES_BY_OWNER: The maximum number of hosted zones that you can create using the current account. MAX_REUSABLE_DELEGATION_SETS_BY_OWNER: The maximum number of reusable delegation sets that you can create using the current account. MAX_TRAFFIC_POLICIES_BY_OWNER: The maximum number of traffic policies that you can create using the current account. MAX_TRAFFIC_POLICY_INSTANCES_BY_OWNER: The maximum number of traffic policy instances that you can create using the current account. (Traffic policy instances are referred to as traffic flow policy records in the Amazon Route 53 console.)"];
      value: LimitValue.t
        [@ocaml.doc
          "The current value for the limit that is specified by Type."]}
    let context_ = "AccountLimit"
    let make ~type_ = fun ~value -> fun () -> { type_; value }
    let to_value x =
      structure_to_value
        [("Type", (Some (AccountLimitType.to_value x.type_)));
        ("Value", (Some (LimitValue.to_value x.value)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let value =
        LimitValue.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Value") in
      let type_ =
        AccountLimitType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Type") in
      make ~value ~type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let value = field_map_exn json "Value" LimitValue.of_json in
      let type_ = field_map_exn json "Type" AccountLimitType.of_json in
      make ~value ~type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the type of limit that you specified in the request and the current value for that limit."]
module DNSSECNotFound =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The hosted zone doesn't have any DNSSEC resources."]
module HostedZonePartiallyDelegated =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The hosted zone nameservers don't match the parent nameservers. The hosted zone and parent must have the same nameservers."]
module InvalidKMSArn =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The KeyManagementServiceArn that you specified isn't valid to use with DNSSEC signing."]
module InvalidKeySigningKeyStatus =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The key-signing key (KSK) status isn't valid or another KSK has the status INTERNAL_FAILURE."]
module KeySigningKeyWithActiveStatusNotFound =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A key-signing key (KSK) with ACTIVE status wasn't found."]
module InvalidVPCId =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The VPC ID that you specified either isn't a valid ID or the current account is not authorized to access this VPC."]
module LastVPCAssociation =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The VPC that you're trying to disassociate from the private hosted zone is the last VPC that is associated with the hosted zone. Amazon Route 53 doesn't support disassociating the last VPC from a hosted zone."]
module VPCAssociationNotFound =
  struct
    type nonrec t =
      {
      message: ErrorMessage.t option
        [@ocaml.doc "The specified VPC or hosted zone weren't found."]}
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified VPC and hosted zone are not currently associated."]
module DisassociateVPCComment =
  struct
    type nonrec t = string
    let context_ = "DisassociateVPCComment"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"DisassociateVPCComment" j
    let to_json = simple_to_json to_value
  end
module KeySigningKeyInParentDSRecord =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The key-signing key (KSK) is specified in a parent DS record."]
module VPCAssociationAuthorizationNotFound =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The VPC that you specified is not authorized to be associated with the hosted zone."]
module TrafficPolicyInUse =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "One or more traffic policy instances were created by using the specified traffic policy."]
module DelegationSetInUse =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified delegation contains associated hosted zones which must be deleted before the reusable delegation set can be deleted."]
module InvalidSigningStatus =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Your hosted zone status isn't valid for this operation. In the hosted zone, change the status to enable DNSSEC or disable DNSSEC."]
module NoSuchKeySigningKey =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The specified key-signing key (KSK) doesn't exist."]
module HostedZoneNotEmpty =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The hosted zone contains resource records that are not SOA or NS records."]
module HealthCheckInUse =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "This error code is not in use."]
module KeySigningKeyInUse =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The key-signing key (KSK) that you specified can't be deactivated because it's the only KSK for a currently-enabled DNSSEC. Disable DNSSEC signing, or add or enable another KSK."]
module TooManyVPCAssociationAuthorizations =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You've created the maximum number of authorizations that can be created for the specified hosted zone. To authorize another VPC to be associated with the hosted zone, submit a DeleteVPCAssociationAuthorization request to remove an existing authorization. To get a list of existing authorizations, submit a ListVPCAssociationAuthorizations request."]
module InvalidTrafficPolicyDocument =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The format of the traffic policy document that you specified in the Document element is not valid."]
module ResourceURI =
  struct
    type nonrec t = string
    let context_ = "ResourceURI"
    let make i =
      let open Result in ok_or_failwith (check_string_max i ~max:1024); i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"ResourceURI" j
    let to_json = simple_to_json to_value
  end
module TooManyTrafficPolicyVersionsForCurrentPolicy =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This traffic policy version can't be created because you've reached the limit of 1000 on the number of versions that you can create for the current traffic policy. To create more traffic policy versions, you can use GetTrafficPolicy to get the traffic policy document for a specified traffic policy version, and then use CreateTrafficPolicy to create a new traffic policy using the traffic policy document."]
module TooManyTrafficPolicies =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This traffic policy can't be created because the current account has reached the limit on the number of traffic policies. For information about default limits, see Limits in the Amazon Route 53 Developer Guide. To get the current limit for an account, see GetAccountLimit. To request a higher limit, create a case with the Amazon Web Services Support Center."]
module TrafficPolicyAlreadyExists =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A traffic policy that has the same value for Name already exists."]
module TooManyTrafficPolicyInstances =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This traffic policy instance can't be created because the current account has reached the limit on the number of traffic policy instances. For information about default limits, see Limits in the Amazon Route 53 Developer Guide. For information about how to get the current limit for an account, see GetAccountLimit. To request a higher limit, create a case with the Amazon Web Services Support Center."]
module TrafficPolicyInstanceAlreadyExists =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "There is already a traffic policy instance with the specified ID."]
module DelegationSetAlreadyCreated =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A delegation set with the same owner and caller reference combination has already been created."]
module DelegationSetAlreadyReusable =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The specified delegation set has already been marked as reusable."]
module DelegationSetNotAvailable =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You can create a hosted zone that has the same name as an existing hosted zone (example.com is common), but there is a limit to the number of hosted zones that have the same name. If you get this error, Amazon Route 53 has reached that limit. If you own the domain name and Route 53 generates this error, contact Customer Support."]
module HostedZoneNotFound =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The specified HostedZone can't be found."]
module LimitsExceeded =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This operation can't be completed either because the current account has reached the limit on reusable delegation sets that it can create or because you've reached the limit on the number of Amazon VPCs that you can associate with a private hosted zone. To get the current limit on the number of reusable delegation sets, see GetAccountLimit. To get the current limit on the number of Amazon VPCs that you can associate with a private hosted zone, see GetHostedZoneLimit. To request a higher limit, create a case with the Amazon Web Services Support Center."]
module InsufficientCloudWatchLogsResourcePolicy =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Amazon Route 53 doesn't have the permissions required to create log streams and send query logs to log streams. Possible causes include the following: There is no resource policy that specifies the log group ARN in the value for Resource. The resource policy that includes the log group ARN in the value for Resource doesn't have the necessary permissions. The resource policy hasn't finished propagating yet. The Key management service (KMS) key you specified doesn\226\128\153t exist or it can\226\128\153t be used with the log group associated with query log. Update or provide a resource policy to grant permissions for the KMS key."]
module NoSuchCloudWatchLogsLogGroup =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "There is no CloudWatch Logs log group with the specified ARN."]
module QueryLoggingConfigAlreadyExists =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You can create only one query logging configuration for a hosted zone, and a query logging configuration already exists for this hosted zone."]
module InvalidKeySigningKeyName =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The key-signing key (KSK) name that you specified isn't a valid name."]
module KeySigningKeyAlreadyExists =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You've already created a key-signing key (KSK) with this name or with the same customer managed key ARN."]
module TooManyKeySigningKeys =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You've reached the limit for the number of key-signing keys (KSKs). Remove at least one KSK, and then try again."]
module ConflictingDomainExists =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The cause of this error depends on the operation that you're performing: Create a public hosted zone: Two hosted zones that have the same name or that have a parent/child relationship (example.com and test.example.com) can't have any common name servers. You tried to create a hosted zone that has the same name as an existing hosted zone or that's the parent or child of an existing hosted zone, and you specified a delegation set that shares one or more name servers with the existing hosted zone. For more information, see CreateReusableDelegationSet. Create a private hosted zone: A hosted zone with the specified name already exists and is already associated with the Amazon VPC that you specified. Associate VPCs with a private hosted zone: The VPC that you specified is already associated with another hosted zone that has the same name."]
module HostedZoneAlreadyExists =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The hosted zone you're trying to create already exists. Amazon Route 53 returns this error when a hosted zone has already been created with the specified CallerReference."]
module TooManyHostedZones =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This operation can't be completed either because the current account has reached the limit on the number of hosted zones or because you've reached the limit on the number of hosted zones that can be associated with a reusable delegation set. For information about default limits, see Limits in the Amazon Route 53 Developer Guide. To get the current limit on hosted zones that can be created by an account, see GetAccountLimit. To get the current limit on hosted zones that can be associated with a reusable delegation set, see GetReusableDelegationSetLimit. To request a higher limit, create a case with the Amazon Web Services Support Center."]
module HealthCheckAlreadyExists =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "The health check you're attempting to create already exists. Amazon Route 53 returns this error when you submit a request that has the following values: The same value for CallerReference as an existing health check, and one or more values that differ from the existing health check that has the same caller reference. The same value for CallerReference as a health check that you created and later deleted, regardless of the other settings in the request."]
module TooManyHealthChecks =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This health check can't be created because the current account has reached the limit on the number of active health checks. For information about default limits, see Limits in the Amazon Route 53 Developer Guide. For information about how to get the current limit for an account, see GetAccountLimit. To request a higher limit, create a case with the Amazon Web Services Support Center. You have reached the maximum number of active health checks for an Amazon Web Services account. To request a higher limit, create a case with the Amazon Web Services Support Center."]
module TagKeyList =
  struct
    type nonrec t = TagKey.t list
    let make i =
      let open Result in
        ok_or_failwith
          ((check_list_max i ~max:10) >>= (fun () -> check_list_min i ~min:1));
        i
    let to_value xs =
      (xs |> (List.map ~f:TagKey.to_value)) |> (fun x -> `List x)
    let to_query v = to_query to_value v
    let to_header _ =
      failwithf "to_header is not implemented for List_shape objects" ()
    let of_xml x =
      make
        (List.map
           ((Xml.all_children x) |>
              (List.filter
                 ~f:(function
                     | `Data s ->
                         (match Stdlib.String.trim s with
                          | "" -> false
                          | _ -> true)
                     | _ -> true))) ~f:TagKey.of_xml)
    let of_json j = list_of_json ~kind:"TagKeyList" ~of_json:TagKey.of_json j
    let to_json v = composed_to_json to_value v
  end
module InvalidChangeBatch =
  struct
    type nonrec t =
      {
      messages: ErrorMessages.t option ;
      message: ErrorMessage.t option }
    let make ?messages = fun ?message -> fun () -> { messages; message }
    let to_value x =
      structure_to_value
        [("messages", (Option.map x.messages ~f:ErrorMessages.to_value));
        ("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      let messages =
        (Option.map ~f:ErrorMessages.of_xml) (Xml.child xml_arg0 "messages") in
      make ?message ?messages ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      let messages = field_map json "messages" ErrorMessages.of_json in
      make ?message ?messages ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "This exception contains a list of messages that might contain one or more error messages. Each error message indicates one error in the change batch."]
module ChangeBatch =
  struct
    type nonrec t =
      {
      comment: ResourceDescription.t option
        [@ocaml.doc
          "Optional: Any comments you want to include about a change batch request."];
      changes: Changes.t
        [@ocaml.doc
          "Information about the changes to make to the record sets."]}
    let context_ = "ChangeBatch"
    let make ?comment = fun ~changes -> fun () -> { comment; changes }
    let to_value x =
      structure_to_value
        [("Comment", (Option.map x.comment ~f:ResourceDescription.to_value));
        ("Changes", (Some (Changes.to_value x.changes)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let changes =
        Changes.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Changes") in
      let comment =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Comment") in
      make ~changes ?comment ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let changes = field_map_exn json "Changes" Changes.of_json in
      let comment = field_map json "Comment" ResourceDescription.of_json in
      make ~changes ?comment ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The information for a change request."]
module NotAuthorizedException =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Associating the specified VPC with the specified hosted zone has not been authorized."]
module PublicZoneVPCAssociation =
  struct
    type nonrec t = {
      message: ErrorMessage.t option }
    let make ?message = fun () -> { message }
    let to_value x =
      structure_to_value
        [("message", (Option.map x.message ~f:ErrorMessage.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let message =
        (Option.map ~f:ErrorMessage.of_xml) (Xml.child xml_arg0 "message") in
      make ?message ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let message = field_map json "message" ErrorMessage.of_json in
      make ?message ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "You're trying to associate a VPC with a public hosted zone. Amazon Route 53 doesn't support associating a VPC with a public hosted zone."]
module AssociateVPCComment =
  struct
    type nonrec t = string
    let context_ = "AssociateVPCComment"
    let make i = i
    let of_string x = x
    let to_value x = `String x
    let to_query v = to_query to_value v
    let to_header x = x
    let of_xml = Xml.string_data_exn ~context:context_
    let of_json j = string_of_json ~kind:"AssociateVPCComment" j
    let to_json = simple_to_json to_value
  end
module UpdateTrafficPolicyInstanceResponse =
  struct
    type nonrec t =
      {
      trafficPolicyInstance: TrafficPolicyInstance.t
        [@ocaml.doc
          "A complex type that contains settings for the updated traffic policy instance."]}
    type nonrec error =
      [ `ConflictingTypes of ConflictingTypes.t 
      | `InvalidInput of InvalidInput.t 
      | `NoSuchTrafficPolicy of NoSuchTrafficPolicy.t 
      | `NoSuchTrafficPolicyInstance of NoSuchTrafficPolicyInstance.t 
      | `PriorRequestNotComplete of PriorRequestNotComplete.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "UpdateTrafficPolicyInstanceResponse"
    let make ~trafficPolicyInstance = fun () -> { trafficPolicyInstance }
    let error_of_json name json =
      match name with
      | "ConflictingTypes" ->
          `ConflictingTypes (ConflictingTypes.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchTrafficPolicy" ->
          `NoSuchTrafficPolicy (NoSuchTrafficPolicy.of_json json)
      | "NoSuchTrafficPolicyInstance" ->
          `NoSuchTrafficPolicyInstance
            (NoSuchTrafficPolicyInstance.of_json json)
      | "PriorRequestNotComplete" ->
          `PriorRequestNotComplete (PriorRequestNotComplete.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConflictingTypes" -> `ConflictingTypes (ConflictingTypes.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchTrafficPolicy" ->
          `NoSuchTrafficPolicy (NoSuchTrafficPolicy.of_xml xml)
      | "NoSuchTrafficPolicyInstance" ->
          `NoSuchTrafficPolicyInstance
            (NoSuchTrafficPolicyInstance.of_xml xml)
      | "PriorRequestNotComplete" ->
          `PriorRequestNotComplete (PriorRequestNotComplete.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConflictingTypes e ->
          `Assoc
            [("error", (`String "ConflictingTypes"));
            ("details", (ConflictingTypes.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchTrafficPolicy e ->
          `Assoc
            [("error", (`String "NoSuchTrafficPolicy"));
            ("details", (NoSuchTrafficPolicy.to_json e))]
      | `NoSuchTrafficPolicyInstance e ->
          `Assoc
            [("error", (`String "NoSuchTrafficPolicyInstance"));
            ("details", (NoSuchTrafficPolicyInstance.to_json e))]
      | `PriorRequestNotComplete e ->
          `Assoc
            [("error", (`String "PriorRequestNotComplete"));
            ("details", (PriorRequestNotComplete.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("TrafficPolicyInstance",
           (Some (TrafficPolicyInstance.to_value x.trafficPolicyInstance)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trafficPolicyInstance =
        TrafficPolicyInstance.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficPolicyInstance") in
      make ~trafficPolicyInstance ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let trafficPolicyInstance =
        field_map_exn json "TrafficPolicyInstance"
          TrafficPolicyInstance.of_json in
      make ~trafficPolicyInstance ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about the resource record sets that Amazon Route 53 created based on a specified traffic policy."]
module UpdateTrafficPolicyInstanceRequest =
  struct
    type nonrec t =
      {
      id: TrafficPolicyInstanceId.t
        [@ocaml.doc
          "The ID of the traffic policy instance that you want to update."];
      tTL: TTL.t
        [@ocaml.doc
          "The TTL that you want Amazon Route 53 to assign to all of the updated resource record sets."];
      trafficPolicyId: TrafficPolicyId.t
        [@ocaml.doc
          "The ID of the traffic policy that you want Amazon Route 53 to use to update resource record sets for the specified traffic policy instance."];
      trafficPolicyVersion: TrafficPolicyVersion.t
        [@ocaml.doc
          "The version of the traffic policy that you want Amazon Route 53 to use to update resource record sets for the specified traffic policy instance."]}
    let context_ = "UpdateTrafficPolicyInstanceRequest"
    let make ~id =
      fun ~tTL ->
        fun ~trafficPolicyId ->
          fun ~trafficPolicyVersion ->
            fun () -> { id; tTL; trafficPolicyId; trafficPolicyVersion }
    let to_value x =
      structure_to_value
        [("Id", (Some (TrafficPolicyInstanceId.to_value x.id)));
        ("TTL", (Some (TTL.to_value x.tTL)));
        ("TrafficPolicyId",
          (Some (TrafficPolicyId.to_value x.trafficPolicyId)));
        ("TrafficPolicyVersion",
          (Some (TrafficPolicyVersion.to_value x.trafficPolicyVersion)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trafficPolicyVersion =
        TrafficPolicyVersion.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficPolicyVersion") in
      let trafficPolicyId =
        TrafficPolicyId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficPolicyId") in
      let tTL = TTL.of_xml (Xml.child_exn ~context:context_ xml_arg0 "TTL") in
      let id =
        TrafficPolicyInstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ~trafficPolicyVersion ~trafficPolicyId ~tTL ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let trafficPolicyVersion =
        field_map_exn json "TrafficPolicyVersion"
          TrafficPolicyVersion.of_json in
      let trafficPolicyId =
        field_map_exn json "TrafficPolicyId" TrafficPolicyId.of_json in
      let tTL = field_map_exn json "TTL" TTL.of_json in
      let id = field_map_exn json "Id" TrafficPolicyInstanceId.of_json in
      make ~trafficPolicyVersion ~trafficPolicyId ~tTL ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about the resource record sets that you want to update based on a specified traffic policy instance."]
module UpdateTrafficPolicyCommentResponse =
  struct
    type nonrec t =
      {
      trafficPolicy: TrafficPolicy.t
        [@ocaml.doc
          "A complex type that contains settings for the specified traffic policy."]}
    type nonrec error =
      [ `ConcurrentModification of ConcurrentModification.t 
      | `InvalidInput of InvalidInput.t 
      | `NoSuchTrafficPolicy of NoSuchTrafficPolicy.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "UpdateTrafficPolicyCommentResponse"
    let make ~trafficPolicy = fun () -> { trafficPolicy }
    let error_of_json name json =
      match name with
      | "ConcurrentModification" ->
          `ConcurrentModification (ConcurrentModification.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchTrafficPolicy" ->
          `NoSuchTrafficPolicy (NoSuchTrafficPolicy.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConcurrentModification" ->
          `ConcurrentModification (ConcurrentModification.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchTrafficPolicy" ->
          `NoSuchTrafficPolicy (NoSuchTrafficPolicy.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConcurrentModification e ->
          `Assoc
            [("error", (`String "ConcurrentModification"));
            ("details", (ConcurrentModification.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchTrafficPolicy e ->
          `Assoc
            [("error", (`String "NoSuchTrafficPolicy"));
            ("details", (NoSuchTrafficPolicy.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("TrafficPolicy", (Some (TrafficPolicy.to_value x.trafficPolicy)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trafficPolicy =
        TrafficPolicy.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficPolicy") in
      make ~trafficPolicy ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let trafficPolicy =
        field_map_exn json "TrafficPolicy" TrafficPolicy.of_json in
      make ~trafficPolicy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response information for the traffic policy."]
module UpdateTrafficPolicyCommentRequest =
  struct
    type nonrec t =
      {
      id: TrafficPolicyId.t
        [@ocaml.doc
          "The value of Id for the traffic policy that you want to update the comment for."];
      version: TrafficPolicyVersion.t
        [@ocaml.doc
          "The value of Version for the traffic policy that you want to update the comment for."];
      comment: TrafficPolicyComment.t
        [@ocaml.doc
          "The new comment for the specified traffic policy and version."]}
    let context_ = "UpdateTrafficPolicyCommentRequest"
    let make ~id =
      fun ~version -> fun ~comment -> fun () -> { id; version; comment }
    let to_value x =
      structure_to_value
        [("Id", (Some (TrafficPolicyId.to_value x.id)));
        ("Version", (Some (TrafficPolicyVersion.to_value x.version)));
        ("Comment", (Some (TrafficPolicyComment.to_value x.comment)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let comment =
        TrafficPolicyComment.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Comment") in
      let version =
        TrafficPolicyVersion.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Version") in
      let id =
        TrafficPolicyId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ~comment ~version ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let comment = field_map_exn json "Comment" TrafficPolicyComment.of_json in
      let version = field_map_exn json "Version" TrafficPolicyVersion.of_json in
      let id = field_map_exn json "Id" TrafficPolicyId.of_json in
      make ~comment ~version ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about the traffic policy that you want to update the comment for."]
module UpdateHostedZoneCommentResponse =
  struct
    type nonrec t =
      {
      hostedZone: HostedZone.t
        [@ocaml.doc
          "A complex type that contains the response to the UpdateHostedZoneComment request."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `NoSuchHostedZone of NoSuchHostedZone.t 
      | `PriorRequestNotComplete of PriorRequestNotComplete.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "UpdateHostedZoneCommentResponse"
    let make ~hostedZone = fun () -> { hostedZone }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchHostedZone" ->
          `NoSuchHostedZone (NoSuchHostedZone.of_json json)
      | "PriorRequestNotComplete" ->
          `PriorRequestNotComplete (PriorRequestNotComplete.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchHostedZone" -> `NoSuchHostedZone (NoSuchHostedZone.of_xml xml)
      | "PriorRequestNotComplete" ->
          `PriorRequestNotComplete (PriorRequestNotComplete.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchHostedZone e ->
          `Assoc
            [("error", (`String "NoSuchHostedZone"));
            ("details", (NoSuchHostedZone.to_json e))]
      | `PriorRequestNotComplete e ->
          `Assoc
            [("error", (`String "PriorRequestNotComplete"));
            ("details", (PriorRequestNotComplete.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("HostedZone", (Some (HostedZone.to_value x.hostedZone)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let hostedZone =
        HostedZone.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HostedZone") in
      make ~hostedZone ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let hostedZone = field_map_exn json "HostedZone" HostedZone.of_json in
      make ~hostedZone ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response to the UpdateHostedZoneComment request."]
module UpdateHostedZoneCommentRequest =
  struct
    type nonrec t =
      {
      id: ResourceId.t
        [@ocaml.doc
          "The ID for the hosted zone that you want to update the comment for."];
      comment: ResourceDescription.t option
        [@ocaml.doc
          "The new comment for the hosted zone. If you don't specify a value for Comment, Amazon Route 53 deletes the existing value of the Comment element, if any."]}
    let context_ = "UpdateHostedZoneCommentRequest"
    let make ?comment = fun ~id -> fun () -> { comment; id }
    let to_value x =
      structure_to_value
        [("Id", (Some (ResourceId.to_value x.id)));
        ("Comment", (Option.map x.comment ~f:ResourceDescription.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let comment =
        (Option.map ~f:ResourceDescription.of_xml)
          (Xml.child xml_arg0 "Comment") in
      let id =
        ResourceId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ?comment ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let comment = field_map json "Comment" ResourceDescription.of_json in
      let id = field_map_exn json "Id" ResourceId.of_json in
      make ?comment ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A request to update the comment for a hosted zone."]
module UpdateHealthCheckResponse =
  struct
    type nonrec t =
      {
      healthCheck: HealthCheck.t
        [@ocaml.doc
          "A complex type that contains the response to an UpdateHealthCheck request."]}
    type nonrec error =
      [ `HealthCheckVersionMismatch of HealthCheckVersionMismatch.t 
      | `InvalidInput of InvalidInput.t 
      | `NoSuchHealthCheck of NoSuchHealthCheck.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "UpdateHealthCheckResponse"
    let make ~healthCheck = fun () -> { healthCheck }
    let error_of_json name json =
      match name with
      | "HealthCheckVersionMismatch" ->
          `HealthCheckVersionMismatch
            (HealthCheckVersionMismatch.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchHealthCheck" ->
          `NoSuchHealthCheck (NoSuchHealthCheck.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "HealthCheckVersionMismatch" ->
          `HealthCheckVersionMismatch (HealthCheckVersionMismatch.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchHealthCheck" ->
          `NoSuchHealthCheck (NoSuchHealthCheck.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `HealthCheckVersionMismatch e ->
          `Assoc
            [("error", (`String "HealthCheckVersionMismatch"));
            ("details", (HealthCheckVersionMismatch.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchHealthCheck e ->
          `Assoc
            [("error", (`String "NoSuchHealthCheck"));
            ("details", (NoSuchHealthCheck.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("HealthCheck", (Some (HealthCheck.to_value x.healthCheck)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let healthCheck =
        HealthCheck.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HealthCheck") in
      make ~healthCheck ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let healthCheck = field_map_exn json "HealthCheck" HealthCheck.of_json in
      make ~healthCheck ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response to the UpdateHealthCheck request."]
module UpdateHealthCheckRequest =
  struct
    type nonrec t =
      {
      healthCheckId: HealthCheckId.t
        [@ocaml.doc
          "The ID for the health check for which you want detailed information. When you created the health check, CreateHealthCheck returned the ID in the response, in the HealthCheckId element."];
      healthCheckVersion: HealthCheckVersion.t option
        [@ocaml.doc
          "A sequential counter that Amazon Route 53 sets to 1 when you create a health check and increments by 1 each time you update settings for the health check. We recommend that you use GetHealthCheck or ListHealthChecks to get the current value of HealthCheckVersion for the health check that you want to update, and that you include that value in your UpdateHealthCheck request. This prevents Route 53 from overwriting an intervening update: If the value in the UpdateHealthCheck request matches the value of HealthCheckVersion in the health check, Route 53 updates the health check with the new settings. If the value of HealthCheckVersion in the health check is greater, the health check was changed after you got the version number. Route 53 does not update the health check, and it returns a HealthCheckVersionMismatch error."];
      iPAddress: IPAddress.t option
        [@ocaml.doc
          "The IPv4 or IPv6 IP address for the endpoint that you want Amazon Route 53 to perform health checks on. If you don't specify a value for IPAddress, Route 53 sends a DNS request to resolve the domain name that you specify in FullyQualifiedDomainName at the interval that you specify in RequestInterval. Using an IP address that is returned by DNS, Route 53 then checks the health of the endpoint. Use one of the following formats for the value of IPAddress: IPv4 address: four values between 0 and 255, separated by periods (.), for example, 192.0.2.44. IPv6 address: eight groups of four hexadecimal values, separated by colons (:), for example, 2001:0db8:85a3:0000:0000:abcd:0001:2345. You can also shorten IPv6 addresses as described in RFC 5952, for example, 2001:db8:85a3::abcd:1:2345. If the endpoint is an EC2 instance, we recommend that you create an Elastic IP address, associate it with your EC2 instance, and specify the Elastic IP address for IPAddress. This ensures that the IP address of your instance never changes. For more information, see the applicable documentation: Linux: Elastic IP Addresses (EIP) in the Amazon EC2 User Guide for Linux Instances Windows: Elastic IP Addresses (EIP) in the Amazon EC2 User Guide for Windows Instances If a health check already has a value for IPAddress, you can change the value. However, you can't update an existing health check to add or remove the value of IPAddress. For more information, see FullyQualifiedDomainName. Constraints: Route 53 can't check the health of endpoints for which the IP address is in local, private, non-routable, or multicast ranges. For more information about IP addresses for which you can't create health checks, see the following documents: RFC 5735, Special Use IPv4 Addresses RFC 6598, IANA-Reserved IPv4 Prefix for Shared Address Space RFC 5156, Special-Use IPv6 Addresses"];
      port: Port.t option
        [@ocaml.doc
          "The port on the endpoint that you want Amazon Route 53 to perform health checks on. Don't specify a value for Port when you specify a value for Type of CLOUDWATCH_METRIC or CALCULATED."];
      resourcePath: ResourcePath.t option
        [@ocaml.doc
          "The path that you want Amazon Route 53 to request when performing health checks. The path can be any value for which your endpoint will return an HTTP status code of 2xx or 3xx when the endpoint is healthy, for example the file /docs/route53-health-check.html. You can also include query string parameters, for example, /welcome.html?language=jp&login=y. Specify this value only if you want to change it."];
      fullyQualifiedDomainName: FullyQualifiedDomainName.t option
        [@ocaml.doc
          "Amazon Route 53 behavior depends on whether you specify a value for IPAddress. If a health check already has a value for IPAddress, you can change the value. However, you can't update an existing health check to add or remove the value of IPAddress. If you specify a value for IPAddress: Route 53 sends health check requests to the specified IPv4 or IPv6 address and passes the value of FullyQualifiedDomainName in the Host header for all health checks except TCP health checks. This is typically the fully qualified DNS name of the endpoint on which you want Route 53 to perform health checks. When Route 53 checks the health of an endpoint, here is how it constructs the Host header: If you specify a value of 80 for Port and HTTP or HTTP_STR_MATCH for Type, Route 53 passes the value of FullyQualifiedDomainName to the endpoint in the Host header. If you specify a value of 443 for Port and HTTPS or HTTPS_STR_MATCH for Type, Route 53 passes the value of FullyQualifiedDomainName to the endpoint in the Host header. If you specify another value for Port and any value except TCP for Type, Route 53 passes FullyQualifiedDomainName:Port to the endpoint in the Host header. If you don't specify a value for FullyQualifiedDomainName, Route 53 substitutes the value of IPAddress in the Host header in each of the above cases. If you don't specify a value for IPAddress: If you don't specify a value for IPAddress, Route 53 sends a DNS request to the domain that you specify in FullyQualifiedDomainName at the interval you specify in RequestInterval. Using an IPv4 address that is returned by DNS, Route 53 then checks the health of the endpoint. If you don't specify a value for IPAddress, Route 53 uses only IPv4 to send health checks to the endpoint. If there's no resource record set with a type of A for the name that you specify for FullyQualifiedDomainName, the health check fails with a \"DNS resolution failed\" error. If you want to check the health of weighted, latency, or failover resource record sets and you choose to specify the endpoint only by FullyQualifiedDomainName, we recommend that you create a separate health check for each endpoint. For example, create a health check for each HTTP server that is serving content for www.example.com. For the value of FullyQualifiedDomainName, specify the domain name of the server (such as us-east-2-www.example.com), not the name of the resource record sets (www.example.com). In this configuration, if the value of FullyQualifiedDomainName matches the name of the resource record sets and you then associate the health check with those resource record sets, health check results will be unpredictable. In addition, if the value of Type is HTTP, HTTPS, HTTP_STR_MATCH, or HTTPS_STR_MATCH, Route 53 passes the value of FullyQualifiedDomainName in the Host header, as it does when you specify a value for IPAddress. If the value of Type is TCP, Route 53 doesn't pass a Host header."];
      searchString: SearchString.t option
        [@ocaml.doc
          "If the value of Type is HTTP_STR_MATCH or HTTPS_STR_MATCH, the string that you want Amazon Route 53 to search for in the response body from the specified resource. If the string appears in the response body, Route 53 considers the resource healthy. (You can't change the value of Type when you update a health check.)"];
      failureThreshold: FailureThreshold.t option
        [@ocaml.doc
          "The number of consecutive health checks that an endpoint must pass or fail for Amazon Route 53 to change the current status of the endpoint from unhealthy to healthy or vice versa. For more information, see How Amazon Route 53 Determines Whether an Endpoint Is Healthy in the Amazon Route 53 Developer Guide. If you don't specify a value for FailureThreshold, the default value is three health checks."];
      inverted: Inverted.t option
        [@ocaml.doc
          "Specify whether you want Amazon Route 53 to invert the status of a health check, for example, to consider a health check unhealthy when it otherwise would be considered healthy."];
      disabled: Disabled.t option
        [@ocaml.doc
          "Stops Route 53 from performing health checks. When you disable a health check, here's what happens: Health checks that check the health of endpoints: Route 53 stops submitting requests to your application, server, or other resource. Calculated health checks: Route 53 stops aggregating the status of the referenced health checks. Health checks that monitor CloudWatch alarms: Route 53 stops monitoring the corresponding CloudWatch metrics. After you disable a health check, Route 53 considers the status of the health check to always be healthy. If you configured DNS failover, Route 53 continues to route traffic to the corresponding resources. If you want to stop routing traffic to a resource, change the value of Inverted. Charges for a health check still apply when the health check is disabled. For more information, see Amazon Route 53 Pricing."];
      healthThreshold: HealthThreshold.t option
        [@ocaml.doc
          "The number of child health checks that are associated with a CALCULATED health that Amazon Route 53 must consider healthy for the CALCULATED health check to be considered healthy. To specify the child health checks that you want to associate with a CALCULATED health check, use the ChildHealthChecks and ChildHealthCheck elements. Note the following: If you specify a number greater than the number of child health checks, Route 53 always considers this health check to be unhealthy. If you specify 0, Route 53 always considers this health check to be healthy."];
      childHealthChecks: ChildHealthCheckList.t option
        [@ocaml.doc
          "A complex type that contains one ChildHealthCheck element for each health check that you want to associate with a CALCULATED health check."];
      enableSNI: EnableSNI.t option
        [@ocaml.doc
          "Specify whether you want Amazon Route 53 to send the value of FullyQualifiedDomainName to the endpoint in the client_hello message during TLS negotiation. This allows the endpoint to respond to HTTPS health check requests with the applicable SSL/TLS certificate. Some endpoints require that HTTPS requests include the host name in the client_hello message. If you don't enable SNI, the status of the health check will be SSL alert handshake_failure. A health check can also have that status for other reasons. If SNI is enabled and you're still getting the error, check the SSL/TLS configuration on your endpoint and confirm that your certificate is valid. The SSL/TLS certificate on your endpoint includes a domain name in the Common Name field and possibly several more in the Subject Alternative Names field. One of the domain names in the certificate should match the value that you specify for FullyQualifiedDomainName. If the endpoint responds to the client_hello message with a certificate that does not include the domain name that you specified in FullyQualifiedDomainName, a health checker will retry the handshake. In the second attempt, the health checker will omit FullyQualifiedDomainName from the client_hello message."];
      regions: HealthCheckRegionList.t option
        [@ocaml.doc
          "A complex type that contains one Region element for each region that you want Amazon Route 53 health checkers to check the specified endpoint from."];
      alarmIdentifier: AlarmIdentifier.t option
        [@ocaml.doc
          "A complex type that identifies the CloudWatch alarm that you want Amazon Route 53 health checkers to use to determine whether the specified health check is healthy."];
      insufficientDataHealthStatus: InsufficientDataHealthStatus.t option
        [@ocaml.doc
          "When CloudWatch has insufficient data about the metric to determine the alarm state, the status that you want Amazon Route 53 to assign to the health check: Healthy: Route 53 considers the health check to be healthy. Unhealthy: Route 53 considers the health check to be unhealthy. LastKnownStatus: By default, Route 53 uses the status of the health check from the last time CloudWatch had sufficient data to determine the alarm state. For new health checks that have no last known status, the status for the health check is healthy."];
      resetElements: ResettableElementNameList.t option
        [@ocaml.doc
          "A complex type that contains one ResettableElementName element for each element that you want to reset to the default value. Valid values for ResettableElementName include the following: ChildHealthChecks: Amazon Route 53 resets ChildHealthChecks to null. FullyQualifiedDomainName: Route 53 resets FullyQualifiedDomainName. to null. Regions: Route 53 resets the Regions list to the default set of regions. ResourcePath: Route 53 resets ResourcePath to null."]}
    let context_ = "UpdateHealthCheckRequest"
    let make ?healthCheckVersion =
      fun ?iPAddress ->
        fun ?port ->
          fun ?resourcePath ->
            fun ?fullyQualifiedDomainName ->
              fun ?searchString ->
                fun ?failureThreshold ->
                  fun ?inverted ->
                    fun ?disabled ->
                      fun ?healthThreshold ->
                        fun ?childHealthChecks ->
                          fun ?enableSNI ->
                            fun ?regions ->
                              fun ?alarmIdentifier ->
                                fun ?insufficientDataHealthStatus ->
                                  fun ?resetElements ->
                                    fun ~healthCheckId ->
                                      fun () ->
                                        {
                                          healthCheckVersion;
                                          iPAddress;
                                          port;
                                          resourcePath;
                                          fullyQualifiedDomainName;
                                          searchString;
                                          failureThreshold;
                                          inverted;
                                          disabled;
                                          healthThreshold;
                                          childHealthChecks;
                                          enableSNI;
                                          regions;
                                          alarmIdentifier;
                                          insufficientDataHealthStatus;
                                          resetElements;
                                          healthCheckId
                                        }
    let to_value x =
      structure_to_value
        [("HealthCheckId", (Some (HealthCheckId.to_value x.healthCheckId)));
        ("HealthCheckVersion",
          (Option.map x.healthCheckVersion ~f:HealthCheckVersion.to_value));
        ("IPAddress", (Option.map x.iPAddress ~f:IPAddress.to_value));
        ("Port", (Option.map x.port ~f:Port.to_value));
        ("ResourcePath",
          (Option.map x.resourcePath ~f:ResourcePath.to_value));
        ("FullyQualifiedDomainName",
          (Option.map x.fullyQualifiedDomainName
             ~f:FullyQualifiedDomainName.to_value));
        ("SearchString",
          (Option.map x.searchString ~f:SearchString.to_value));
        ("FailureThreshold",
          (Option.map x.failureThreshold ~f:FailureThreshold.to_value));
        ("Inverted", (Option.map x.inverted ~f:Inverted.to_value));
        ("Disabled", (Option.map x.disabled ~f:Disabled.to_value));
        ("HealthThreshold",
          (Option.map x.healthThreshold ~f:HealthThreshold.to_value));
        ("ChildHealthChecks",
          (Option.map x.childHealthChecks ~f:ChildHealthCheckList.to_value));
        ("EnableSNI", (Option.map x.enableSNI ~f:EnableSNI.to_value));
        ("Regions", (Option.map x.regions ~f:HealthCheckRegionList.to_value));
        ("AlarmIdentifier",
          (Option.map x.alarmIdentifier ~f:AlarmIdentifier.to_value));
        ("InsufficientDataHealthStatus",
          (Option.map x.insufficientDataHealthStatus
             ~f:InsufficientDataHealthStatus.to_value));
        ("ResetElements",
          (Option.map x.resetElements ~f:ResettableElementNameList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resetElements =
        (Option.map ~f:ResettableElementNameList.of_xml)
          (Xml.child xml_arg0 "ResetElements") in
      let insufficientDataHealthStatus =
        (Option.map ~f:InsufficientDataHealthStatus.of_xml)
          (Xml.child xml_arg0 "InsufficientDataHealthStatus") in
      let alarmIdentifier =
        (Option.map ~f:AlarmIdentifier.of_xml)
          (Xml.child xml_arg0 "AlarmIdentifier") in
      let regions =
        (Option.map ~f:HealthCheckRegionList.of_xml)
          (Xml.child xml_arg0 "Regions") in
      let enableSNI =
        (Option.map ~f:EnableSNI.of_xml) (Xml.child xml_arg0 "EnableSNI") in
      let childHealthChecks =
        (Option.map ~f:ChildHealthCheckList.of_xml)
          (Xml.child xml_arg0 "ChildHealthChecks") in
      let healthThreshold =
        (Option.map ~f:HealthThreshold.of_xml)
          (Xml.child xml_arg0 "HealthThreshold") in
      let disabled =
        (Option.map ~f:Disabled.of_xml) (Xml.child xml_arg0 "Disabled") in
      let inverted =
        (Option.map ~f:Inverted.of_xml) (Xml.child xml_arg0 "Inverted") in
      let failureThreshold =
        (Option.map ~f:FailureThreshold.of_xml)
          (Xml.child xml_arg0 "FailureThreshold") in
      let searchString =
        (Option.map ~f:SearchString.of_xml)
          (Xml.child xml_arg0 "SearchString") in
      let fullyQualifiedDomainName =
        (Option.map ~f:FullyQualifiedDomainName.of_xml)
          (Xml.child xml_arg0 "FullyQualifiedDomainName") in
      let resourcePath =
        (Option.map ~f:ResourcePath.of_xml)
          (Xml.child xml_arg0 "ResourcePath") in
      let port = (Option.map ~f:Port.of_xml) (Xml.child xml_arg0 "Port") in
      let iPAddress =
        (Option.map ~f:IPAddress.of_xml) (Xml.child xml_arg0 "IPAddress") in
      let healthCheckVersion =
        (Option.map ~f:HealthCheckVersion.of_xml)
          (Xml.child xml_arg0 "HealthCheckVersion") in
      let healthCheckId =
        HealthCheckId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HealthCheckId") in
      make ?resetElements ?insufficientDataHealthStatus ?alarmIdentifier
        ?regions ?enableSNI ?childHealthChecks ?healthThreshold ?disabled
        ?inverted ?failureThreshold ?searchString ?fullyQualifiedDomainName
        ?resourcePath ?port ?iPAddress ?healthCheckVersion ~healthCheckId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let resetElements =
        field_map json "ResetElements" ResettableElementNameList.of_json in
      let insufficientDataHealthStatus =
        field_map json "InsufficientDataHealthStatus"
          InsufficientDataHealthStatus.of_json in
      let alarmIdentifier =
        field_map json "AlarmIdentifier" AlarmIdentifier.of_json in
      let regions = field_map json "Regions" HealthCheckRegionList.of_json in
      let enableSNI = field_map json "EnableSNI" EnableSNI.of_json in
      let childHealthChecks =
        field_map json "ChildHealthChecks" ChildHealthCheckList.of_json in
      let healthThreshold =
        field_map json "HealthThreshold" HealthThreshold.of_json in
      let disabled = field_map json "Disabled" Disabled.of_json in
      let inverted = field_map json "Inverted" Inverted.of_json in
      let failureThreshold =
        field_map json "FailureThreshold" FailureThreshold.of_json in
      let searchString = field_map json "SearchString" SearchString.of_json in
      let fullyQualifiedDomainName =
        field_map json "FullyQualifiedDomainName"
          FullyQualifiedDomainName.of_json in
      let resourcePath = field_map json "ResourcePath" ResourcePath.of_json in
      let port = field_map json "Port" Port.of_json in
      let iPAddress = field_map json "IPAddress" IPAddress.of_json in
      let healthCheckVersion =
        field_map json "HealthCheckVersion" HealthCheckVersion.of_json in
      let healthCheckId =
        field_map_exn json "HealthCheckId" HealthCheckId.of_json in
      make ?resetElements ?insufficientDataHealthStatus ?alarmIdentifier
        ?regions ?enableSNI ?childHealthChecks ?healthThreshold ?disabled
        ?inverted ?failureThreshold ?searchString ?fullyQualifiedDomainName
        ?resourcePath ?port ?iPAddress ?healthCheckVersion ~healthCheckId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about a request to update a health check."]
module TestDNSAnswerResponse =
  struct
    type nonrec t =
      {
      nameserver: Nameserver.t
        [@ocaml.doc
          "The Amazon Route 53 name server used to respond to the request."];
      recordName: DNSName.t
        [@ocaml.doc
          "The name of the resource record set that you submitted a request for."];
      recordType: RRType.t
        [@ocaml.doc
          "The type of the resource record set that you submitted a request for."];
      recordData: RecordData.t
        [@ocaml.doc
          "A list that contains values that Amazon Route 53 returned for this resource record set."];
      responseCode: DNSRCode.t
        [@ocaml.doc
          "A code that indicates whether the request is valid or not. The most common response code is NOERROR, meaning that the request is valid. If the response is not valid, Amazon Route 53 returns a response code that describes the error. For a list of possible response codes, see DNS RCODES on the IANA website."];
      protocol: TransportProtocol.t
        [@ocaml.doc
          "The protocol that Amazon Route 53 used to respond to the request, either UDP or TCP."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `NoSuchHostedZone of NoSuchHostedZone.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "TestDNSAnswerResponse"
    let make ~nameserver =
      fun ~recordName ->
        fun ~recordType ->
          fun ~recordData ->
            fun ~responseCode ->
              fun ~protocol ->
                fun () ->
                  {
                    nameserver;
                    recordName;
                    recordType;
                    recordData;
                    responseCode;
                    protocol
                  }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchHostedZone" ->
          `NoSuchHostedZone (NoSuchHostedZone.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchHostedZone" -> `NoSuchHostedZone (NoSuchHostedZone.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchHostedZone e ->
          `Assoc
            [("error", (`String "NoSuchHostedZone"));
            ("details", (NoSuchHostedZone.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Nameserver", (Some (Nameserver.to_value x.nameserver)));
        ("RecordName", (Some (DNSName.to_value x.recordName)));
        ("RecordType", (Some (RRType.to_value x.recordType)));
        ("RecordData", (Some (RecordData.to_value x.recordData)));
        ("ResponseCode", (Some (DNSRCode.to_value x.responseCode)));
        ("Protocol", (Some (TransportProtocol.to_value x.protocol)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let protocol =
        TransportProtocol.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Protocol") in
      let responseCode =
        DNSRCode.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResponseCode") in
      let recordData =
        RecordData.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RecordData") in
      let recordType =
        RRType.of_xml (Xml.child_exn ~context:context_ xml_arg0 "RecordType") in
      let recordName =
        DNSName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "RecordName") in
      let nameserver =
        Nameserver.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Nameserver") in
      make ~protocol ~responseCode ~recordData ~recordType ~recordName
        ~nameserver ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let protocol = field_map_exn json "Protocol" TransportProtocol.of_json in
      let responseCode = field_map_exn json "ResponseCode" DNSRCode.of_json in
      let recordData = field_map_exn json "RecordData" RecordData.of_json in
      let recordType = field_map_exn json "RecordType" RRType.of_json in
      let recordName = field_map_exn json "RecordName" DNSName.of_json in
      let nameserver = field_map_exn json "Nameserver" Nameserver.of_json in
      make ~protocol ~responseCode ~recordData ~recordType ~recordName
        ~nameserver ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response to a TestDNSAnswer request."]
module TestDNSAnswerRequest =
  struct
    type nonrec t =
      {
      hostedZoneId: ResourceId.t
        [@ocaml.doc
          "The ID of the hosted zone that you want Amazon Route 53 to simulate a query for."];
      recordName: DNSName.t
        [@ocaml.doc
          "The name of the resource record set that you want Amazon Route 53 to simulate a query for."];
      recordType: RRType.t
        [@ocaml.doc "The type of the resource record set."];
      resolverIP: IPAddress.t option
        [@ocaml.doc
          "If you want to simulate a request from a specific DNS resolver, specify the IP address for that resolver. If you omit this value, TestDnsAnswer uses the IP address of a DNS resolver in the Amazon Web Services US East (N. Virginia) Region (us-east-1)."];
      eDNS0ClientSubnetIP: IPAddress.t option
        [@ocaml.doc
          "If the resolver that you specified for resolverip supports EDNS0, specify the IPv4 or IPv6 address of a client in the applicable location, for example, 192.0.2.44 or 2001:db8:85a3::8a2e:370:7334."];
      eDNS0ClientSubnetMask: SubnetMask.t option
        [@ocaml.doc
          "If you specify an IP address for edns0clientsubnetip, you can optionally specify the number of bits of the IP address that you want the checking tool to include in the DNS query. For example, if you specify 192.0.2.44 for edns0clientsubnetip and 24 for edns0clientsubnetmask, the checking tool will simulate a request from 192.0.2.0/24. The default value is 24 bits for IPv4 addresses and 64 bits for IPv6 addresses. The range of valid values depends on whether edns0clientsubnetip is an IPv4 or an IPv6 address: IPv4: Specify a value between 0 and 32 IPv6: Specify a value between 0 and 128"]}
    let context_ = "TestDNSAnswerRequest"
    let make ?resolverIP =
      fun ?eDNS0ClientSubnetIP ->
        fun ?eDNS0ClientSubnetMask ->
          fun ~hostedZoneId ->
            fun ~recordName ->
              fun ~recordType ->
                fun () ->
                  {
                    resolverIP;
                    eDNS0ClientSubnetIP;
                    eDNS0ClientSubnetMask;
                    hostedZoneId;
                    recordName;
                    recordType
                  }
    let to_value x =
      structure_to_value
        [("hostedzoneid", (Some (ResourceId.to_value x.hostedZoneId)));
        ("recordname", (Some (DNSName.to_value x.recordName)));
        ("recordtype", (Some (RRType.to_value x.recordType)));
        ("resolverip", (Option.map x.resolverIP ~f:IPAddress.to_value));
        ("edns0clientsubnetip",
          (Option.map x.eDNS0ClientSubnetIP ~f:IPAddress.to_value));
        ("edns0clientsubnetmask",
          (Option.map x.eDNS0ClientSubnetMask ~f:SubnetMask.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let eDNS0ClientSubnetMask =
        (Option.map ~f:SubnetMask.of_xml)
          (Xml.child xml_arg0 "edns0clientsubnetmask") in
      let eDNS0ClientSubnetIP =
        (Option.map ~f:IPAddress.of_xml)
          (Xml.child xml_arg0 "edns0clientsubnetip") in
      let resolverIP =
        (Option.map ~f:IPAddress.of_xml) (Xml.child xml_arg0 "resolverip") in
      let recordType =
        RRType.of_xml (Xml.child_exn ~context:context_ xml_arg0 "recordtype") in
      let recordName =
        DNSName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "recordname") in
      let hostedZoneId =
        ResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "hostedzoneid") in
      make ?eDNS0ClientSubnetMask ?eDNS0ClientSubnetIP ?resolverIP
        ~recordType ~recordName ~hostedZoneId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let eDNS0ClientSubnetMask =
        field_map json "EDNS0ClientSubnetMask" SubnetMask.of_json in
      let eDNS0ClientSubnetIP =
        field_map json "EDNS0ClientSubnetIP" IPAddress.of_json in
      let resolverIP = field_map json "ResolverIP" IPAddress.of_json in
      let recordType = field_map_exn json "RecordType" RRType.of_json in
      let recordName = field_map_exn json "RecordName" DNSName.of_json in
      let hostedZoneId = field_map_exn json "HostedZoneId" ResourceId.of_json in
      make ?eDNS0ClientSubnetMask ?eDNS0ClientSubnetIP ?resolverIP
        ~recordType ~recordName ~hostedZoneId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets the value that Amazon Route 53 returns in response to a DNS request for a specified record name and type. You can optionally specify the IP address of a DNS resolver, an EDNS0 client subnet IP address, and a subnet mask."]
module ListVPCAssociationAuthorizationsResponse =
  struct
    type nonrec t =
      {
      hostedZoneId: ResourceId.t
        [@ocaml.doc
          "The ID of the hosted zone that you can associate the listed VPCs with."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "When the response includes a NextToken element, there are more VPCs that can be associated with the specified hosted zone. To get the next page of VPCs, submit another ListVPCAssociationAuthorizations request, and include the value of the NextToken element from the response in the nexttoken request parameter."];
      vPCs: VPCs.t
        [@ocaml.doc
          "The list of VPCs that are authorized to be associated with the specified hosted zone."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `InvalidPaginationToken of InvalidPaginationToken.t 
      | `NoSuchHostedZone of NoSuchHostedZone.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListVPCAssociationAuthorizationsResponse"
    let make ?nextToken =
      fun ~hostedZoneId ->
        fun ~vPCs -> fun () -> { nextToken; hostedZoneId; vPCs }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "InvalidPaginationToken" ->
          `InvalidPaginationToken (InvalidPaginationToken.of_json json)
      | "NoSuchHostedZone" ->
          `NoSuchHostedZone (NoSuchHostedZone.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "InvalidPaginationToken" ->
          `InvalidPaginationToken (InvalidPaginationToken.of_xml xml)
      | "NoSuchHostedZone" -> `NoSuchHostedZone (NoSuchHostedZone.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `InvalidPaginationToken e ->
          `Assoc
            [("error", (`String "InvalidPaginationToken"));
            ("details", (InvalidPaginationToken.to_json e))]
      | `NoSuchHostedZone e ->
          `Assoc
            [("error", (`String "NoSuchHostedZone"));
            ("details", (NoSuchHostedZone.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("HostedZoneId", (Some (ResourceId.to_value x.hostedZoneId)));
        ("NextToken", (Option.map x.nextToken ~f:PaginationToken.to_value));
        ("VPCs", (Some (VPCs.to_value x.vPCs)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vPCs =
        VPCs.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VPCs") in
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let hostedZoneId =
        ResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HostedZoneId") in
      make ~vPCs ?nextToken ~hostedZoneId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let vPCs = field_map_exn json "VPCs" VPCs.of_json in
      let nextToken = field_map json "NextToken" PaginationToken.of_json in
      let hostedZoneId = field_map_exn json "HostedZoneId" ResourceId.of_json in
      make ~vPCs ?nextToken ~hostedZoneId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response information for the request."]
module ListVPCAssociationAuthorizationsRequest =
  struct
    type nonrec t =
      {
      hostedZoneId: ResourceId.t
        [@ocaml.doc
          "The ID of the hosted zone for which you want a list of VPCs that can be associated with the hosted zone."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "Optional: If a response includes a NextToken element, there are more VPCs that can be associated with the specified hosted zone. To get the next page of results, submit another request, and include the value of NextToken from the response in the nexttoken parameter in another ListVPCAssociationAuthorizations request."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "Optional: An integer that specifies the maximum number of VPCs that you want Amazon Route 53 to return. If you don't specify a value for MaxResults, Route 53 returns up to 50 VPCs per page."]}
    let context_ = "ListVPCAssociationAuthorizationsRequest"
    let make ?nextToken =
      fun ?maxResults ->
        fun ~hostedZoneId ->
          fun () -> { nextToken; maxResults; hostedZoneId }
    let to_value x =
      structure_to_value
        [("Id", (Some (ResourceId.to_value x.hostedZoneId)));
        ("nexttoken", (Option.map x.nextToken ~f:PaginationToken.to_value));
        ("maxresults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxresults") in
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nexttoken") in
      let hostedZoneId =
        ResourceId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ?maxResults ?nextToken ~hostedZoneId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" PaginationToken.of_json in
      let hostedZoneId = field_map_exn json "HostedZoneId" ResourceId.of_json in
      make ?maxResults ?nextToken ~hostedZoneId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about that can be associated with your hosted zone."]
module ListTrafficPolicyVersionsResponse =
  struct
    type nonrec t =
      {
      trafficPolicies: TrafficPolicies.t
        [@ocaml.doc
          "A list that contains one TrafficPolicy element for each traffic policy version that is associated with the specified traffic policy."];
      isTruncated: PageTruncated.t
        [@ocaml.doc
          "A flag that indicates whether there are more traffic policies to be listed. If the response was truncated, you can get the next group of traffic policies by submitting another ListTrafficPolicyVersions request and specifying the value of NextMarker in the marker parameter."];
      trafficPolicyVersionMarker: TrafficPolicyVersionMarker.t
        [@ocaml.doc
          "If IsTruncated is true, the value of TrafficPolicyVersionMarker identifies the first traffic policy that Amazon Route 53 will return if you submit another request. Call ListTrafficPolicyVersions again and specify the value of TrafficPolicyVersionMarker in the TrafficPolicyVersionMarker request parameter. This element is present only if IsTruncated is true."];
      maxItems: PageMaxItems.t
        [@ocaml.doc
          "The value that you specified for the maxitems parameter in the ListTrafficPolicyVersions request that produced the current response."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `NoSuchTrafficPolicy of NoSuchTrafficPolicy.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListTrafficPolicyVersionsResponse"
    let make ~trafficPolicies =
      fun ~isTruncated ->
        fun ~trafficPolicyVersionMarker ->
          fun ~maxItems ->
            fun () ->
              {
                trafficPolicies;
                isTruncated;
                trafficPolicyVersionMarker;
                maxItems
              }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchTrafficPolicy" ->
          `NoSuchTrafficPolicy (NoSuchTrafficPolicy.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchTrafficPolicy" ->
          `NoSuchTrafficPolicy (NoSuchTrafficPolicy.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchTrafficPolicy e ->
          `Assoc
            [("error", (`String "NoSuchTrafficPolicy"));
            ("details", (NoSuchTrafficPolicy.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("TrafficPolicies",
           (Some (TrafficPolicies.to_value x.trafficPolicies)));
        ("IsTruncated", (Some (PageTruncated.to_value x.isTruncated)));
        ("TrafficPolicyVersionMarker",
          (Some
             (TrafficPolicyVersionMarker.to_value
                x.trafficPolicyVersionMarker)));
        ("MaxItems", (Some (PageMaxItems.to_value x.maxItems)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxItems =
        PageMaxItems.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MaxItems") in
      let trafficPolicyVersionMarker =
        TrafficPolicyVersionMarker.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TrafficPolicyVersionMarker") in
      let isTruncated =
        PageTruncated.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IsTruncated") in
      let trafficPolicies =
        TrafficPolicies.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficPolicies") in
      make ~maxItems ~trafficPolicyVersionMarker ~isTruncated
        ~trafficPolicies ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxItems = field_map_exn json "MaxItems" PageMaxItems.of_json in
      let trafficPolicyVersionMarker =
        field_map_exn json "TrafficPolicyVersionMarker"
          TrafficPolicyVersionMarker.of_json in
      let isTruncated =
        field_map_exn json "IsTruncated" PageTruncated.of_json in
      let trafficPolicies =
        field_map_exn json "TrafficPolicies" TrafficPolicies.of_json in
      make ~maxItems ~trafficPolicyVersionMarker ~isTruncated
        ~trafficPolicies ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response information for the request."]
module ListTrafficPolicyVersionsRequest =
  struct
    type nonrec t =
      {
      id: TrafficPolicyId.t
        [@ocaml.doc
          "Specify the value of Id of the traffic policy for which you want to list all versions."];
      trafficPolicyVersionMarker: TrafficPolicyVersionMarker.t option
        [@ocaml.doc
          "For your first request to ListTrafficPolicyVersions, don't include the TrafficPolicyVersionMarker parameter. If you have more traffic policy versions than the value of MaxItems, ListTrafficPolicyVersions returns only the first group of MaxItems versions. To get more traffic policy versions, submit another ListTrafficPolicyVersions request. For the value of TrafficPolicyVersionMarker, specify the value of TrafficPolicyVersionMarker in the previous response."];
      maxItems: PageMaxItems.t option
        [@ocaml.doc
          "The maximum number of traffic policy versions that you want Amazon Route 53 to include in the response body for this request. If the specified traffic policy has more than MaxItems versions, the value of IsTruncated in the response is true, and the value of the TrafficPolicyVersionMarker element is the ID of the first version that Route 53 will return if you submit another request."]}
    let context_ = "ListTrafficPolicyVersionsRequest"
    let make ?trafficPolicyVersionMarker =
      fun ?maxItems ->
        fun ~id -> fun () -> { trafficPolicyVersionMarker; maxItems; id }
    let to_value x =
      structure_to_value
        [("Id", (Some (TrafficPolicyId.to_value x.id)));
        ("trafficpolicyversion",
          (Option.map x.trafficPolicyVersionMarker
             ~f:TrafficPolicyVersionMarker.to_value));
        ("maxitems", (Option.map x.maxItems ~f:PageMaxItems.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxItems =
        (Option.map ~f:PageMaxItems.of_xml) (Xml.child xml_arg0 "maxitems") in
      let trafficPolicyVersionMarker =
        (Option.map ~f:TrafficPolicyVersionMarker.of_xml)
          (Xml.child xml_arg0 "trafficpolicyversion") in
      let id =
        TrafficPolicyId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ?maxItems ?trafficPolicyVersionMarker ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxItems = field_map json "MaxItems" PageMaxItems.of_json in
      let trafficPolicyVersionMarker =
        field_map json "TrafficPolicyVersionMarker"
          TrafficPolicyVersionMarker.of_json in
      let id = field_map_exn json "Id" TrafficPolicyId.of_json in
      make ?maxItems ?trafficPolicyVersionMarker ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the information about the request to list your traffic policies."]
module ListTrafficPolicyInstancesResponse =
  struct
    type nonrec t =
      {
      trafficPolicyInstances: TrafficPolicyInstances.t
        [@ocaml.doc
          "A list that contains one TrafficPolicyInstance element for each traffic policy instance that matches the elements in the request."];
      hostedZoneIdMarker: ResourceId.t option
        [@ocaml.doc
          "If IsTruncated is true, HostedZoneIdMarker is the ID of the hosted zone of the first traffic policy instance that Route 53 will return if you submit another ListTrafficPolicyInstances request."];
      trafficPolicyInstanceNameMarker: DNSName.t option
        [@ocaml.doc
          "If IsTruncated is true, TrafficPolicyInstanceNameMarker is the name of the first traffic policy instance that Route 53 will return if you submit another ListTrafficPolicyInstances request."];
      trafficPolicyInstanceTypeMarker: RRType.t option
        [@ocaml.doc
          "If IsTruncated is true, TrafficPolicyInstanceTypeMarker is the DNS type of the resource record sets that are associated with the first traffic policy instance that Amazon Route 53 will return if you submit another ListTrafficPolicyInstances request."];
      isTruncated: PageTruncated.t
        [@ocaml.doc
          "A flag that indicates whether there are more traffic policy instances to be listed. If the response was truncated, you can get more traffic policy instances by calling ListTrafficPolicyInstances again and specifying the values of the HostedZoneIdMarker, TrafficPolicyInstanceNameMarker, and TrafficPolicyInstanceTypeMarker in the corresponding request parameters."];
      maxItems: PageMaxItems.t
        [@ocaml.doc
          "The value that you specified for the MaxItems parameter in the call to ListTrafficPolicyInstances that produced the current response."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `NoSuchTrafficPolicyInstance of NoSuchTrafficPolicyInstance.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListTrafficPolicyInstancesResponse"
    let make ?hostedZoneIdMarker =
      fun ?trafficPolicyInstanceNameMarker ->
        fun ?trafficPolicyInstanceTypeMarker ->
          fun ~trafficPolicyInstances ->
            fun ~isTruncated ->
              fun ~maxItems ->
                fun () ->
                  {
                    hostedZoneIdMarker;
                    trafficPolicyInstanceNameMarker;
                    trafficPolicyInstanceTypeMarker;
                    trafficPolicyInstances;
                    isTruncated;
                    maxItems
                  }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchTrafficPolicyInstance" ->
          `NoSuchTrafficPolicyInstance
            (NoSuchTrafficPolicyInstance.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchTrafficPolicyInstance" ->
          `NoSuchTrafficPolicyInstance
            (NoSuchTrafficPolicyInstance.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchTrafficPolicyInstance e ->
          `Assoc
            [("error", (`String "NoSuchTrafficPolicyInstance"));
            ("details", (NoSuchTrafficPolicyInstance.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("TrafficPolicyInstances",
           (Some (TrafficPolicyInstances.to_value x.trafficPolicyInstances)));
        ("HostedZoneIdMarker",
          (Option.map x.hostedZoneIdMarker ~f:ResourceId.to_value));
        ("TrafficPolicyInstanceNameMarker",
          (Option.map x.trafficPolicyInstanceNameMarker ~f:DNSName.to_value));
        ("TrafficPolicyInstanceTypeMarker",
          (Option.map x.trafficPolicyInstanceTypeMarker ~f:RRType.to_value));
        ("IsTruncated", (Some (PageTruncated.to_value x.isTruncated)));
        ("MaxItems", (Some (PageMaxItems.to_value x.maxItems)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxItems =
        PageMaxItems.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MaxItems") in
      let isTruncated =
        PageTruncated.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IsTruncated") in
      let trafficPolicyInstanceTypeMarker =
        (Option.map ~f:RRType.of_xml)
          (Xml.child xml_arg0 "TrafficPolicyInstanceTypeMarker") in
      let trafficPolicyInstanceNameMarker =
        (Option.map ~f:DNSName.of_xml)
          (Xml.child xml_arg0 "TrafficPolicyInstanceNameMarker") in
      let hostedZoneIdMarker =
        (Option.map ~f:ResourceId.of_xml)
          (Xml.child xml_arg0 "HostedZoneIdMarker") in
      let trafficPolicyInstances =
        TrafficPolicyInstances.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficPolicyInstances") in
      make ~maxItems ~isTruncated ?trafficPolicyInstanceTypeMarker
        ?trafficPolicyInstanceNameMarker ?hostedZoneIdMarker
        ~trafficPolicyInstances ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxItems = field_map_exn json "MaxItems" PageMaxItems.of_json in
      let isTruncated =
        field_map_exn json "IsTruncated" PageTruncated.of_json in
      let trafficPolicyInstanceTypeMarker =
        field_map json "TrafficPolicyInstanceTypeMarker" RRType.of_json in
      let trafficPolicyInstanceNameMarker =
        field_map json "TrafficPolicyInstanceNameMarker" DNSName.of_json in
      let hostedZoneIdMarker =
        field_map json "HostedZoneIdMarker" ResourceId.of_json in
      let trafficPolicyInstances =
        field_map_exn json "TrafficPolicyInstances"
          TrafficPolicyInstances.of_json in
      make ~maxItems ~isTruncated ?trafficPolicyInstanceTypeMarker
        ?trafficPolicyInstanceNameMarker ?hostedZoneIdMarker
        ~trafficPolicyInstances ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response information for the request."]
module ListTrafficPolicyInstancesRequest =
  struct
    type nonrec t =
      {
      hostedZoneIdMarker: ResourceId.t option
        [@ocaml.doc
          "If the value of IsTruncated in the previous response was true, you have more traffic policy instances. To get more traffic policy instances, submit another ListTrafficPolicyInstances request. For the value of HostedZoneId, specify the value of HostedZoneIdMarker from the previous response, which is the hosted zone ID of the first traffic policy instance in the next group of traffic policy instances. If the value of IsTruncated in the previous response was false, there are no more traffic policy instances to get."];
      trafficPolicyInstanceNameMarker: DNSName.t option
        [@ocaml.doc
          "If the value of IsTruncated in the previous response was true, you have more traffic policy instances. To get more traffic policy instances, submit another ListTrafficPolicyInstances request. For the value of trafficpolicyinstancename, specify the value of TrafficPolicyInstanceNameMarker from the previous response, which is the name of the first traffic policy instance in the next group of traffic policy instances. If the value of IsTruncated in the previous response was false, there are no more traffic policy instances to get."];
      trafficPolicyInstanceTypeMarker: RRType.t option
        [@ocaml.doc
          "If the value of IsTruncated in the previous response was true, you have more traffic policy instances. To get more traffic policy instances, submit another ListTrafficPolicyInstances request. For the value of trafficpolicyinstancetype, specify the value of TrafficPolicyInstanceTypeMarker from the previous response, which is the type of the first traffic policy instance in the next group of traffic policy instances. If the value of IsTruncated in the previous response was false, there are no more traffic policy instances to get."];
      maxItems: PageMaxItems.t option
        [@ocaml.doc
          "The maximum number of traffic policy instances that you want Amazon Route 53 to return in response to a ListTrafficPolicyInstances request. If you have more than MaxItems traffic policy instances, the value of the IsTruncated element in the response is true, and the values of HostedZoneIdMarker, TrafficPolicyInstanceNameMarker, and TrafficPolicyInstanceTypeMarker represent the first traffic policy instance in the next group of MaxItems traffic policy instances."]}
    let make ?hostedZoneIdMarker =
      fun ?trafficPolicyInstanceNameMarker ->
        fun ?trafficPolicyInstanceTypeMarker ->
          fun ?maxItems ->
            fun () ->
              {
                hostedZoneIdMarker;
                trafficPolicyInstanceNameMarker;
                trafficPolicyInstanceTypeMarker;
                maxItems
              }
    let to_value x =
      structure_to_value
        [("hostedzoneid",
           (Option.map x.hostedZoneIdMarker ~f:ResourceId.to_value));
        ("trafficpolicyinstancename",
          (Option.map x.trafficPolicyInstanceNameMarker ~f:DNSName.to_value));
        ("trafficpolicyinstancetype",
          (Option.map x.trafficPolicyInstanceTypeMarker ~f:RRType.to_value));
        ("maxitems", (Option.map x.maxItems ~f:PageMaxItems.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxItems =
        (Option.map ~f:PageMaxItems.of_xml) (Xml.child xml_arg0 "maxitems") in
      let trafficPolicyInstanceTypeMarker =
        (Option.map ~f:RRType.of_xml)
          (Xml.child xml_arg0 "trafficpolicyinstancetype") in
      let trafficPolicyInstanceNameMarker =
        (Option.map ~f:DNSName.of_xml)
          (Xml.child xml_arg0 "trafficpolicyinstancename") in
      let hostedZoneIdMarker =
        (Option.map ~f:ResourceId.of_xml) (Xml.child xml_arg0 "hostedzoneid") in
      make ?maxItems ?trafficPolicyInstanceTypeMarker
        ?trafficPolicyInstanceNameMarker ?hostedZoneIdMarker ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxItems = field_map json "MaxItems" PageMaxItems.of_json in
      let trafficPolicyInstanceTypeMarker =
        field_map json "TrafficPolicyInstanceTypeMarker" RRType.of_json in
      let trafficPolicyInstanceNameMarker =
        field_map json "TrafficPolicyInstanceNameMarker" DNSName.of_json in
      let hostedZoneIdMarker =
        field_map json "HostedZoneIdMarker" ResourceId.of_json in
      make ?maxItems ?trafficPolicyInstanceTypeMarker
        ?trafficPolicyInstanceNameMarker ?hostedZoneIdMarker ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A request to get information about the traffic policy instances that you created by using the current Amazon Web Services account."]
module ListTrafficPolicyInstancesByPolicyResponse =
  struct
    type nonrec t =
      {
      trafficPolicyInstances: TrafficPolicyInstances.t
        [@ocaml.doc
          "A list that contains one TrafficPolicyInstance element for each traffic policy instance that matches the elements in the request."];
      hostedZoneIdMarker: ResourceId.t option
        [@ocaml.doc
          "If IsTruncated is true, HostedZoneIdMarker is the ID of the hosted zone of the first traffic policy instance in the next group of traffic policy instances."];
      trafficPolicyInstanceNameMarker: DNSName.t option
        [@ocaml.doc
          "If IsTruncated is true, TrafficPolicyInstanceNameMarker is the name of the first traffic policy instance in the next group of MaxItems traffic policy instances."];
      trafficPolicyInstanceTypeMarker: RRType.t option
        [@ocaml.doc
          "If IsTruncated is true, TrafficPolicyInstanceTypeMarker is the DNS type of the resource record sets that are associated with the first traffic policy instance in the next group of MaxItems traffic policy instances."];
      isTruncated: PageTruncated.t
        [@ocaml.doc
          "A flag that indicates whether there are more traffic policy instances to be listed. If the response was truncated, you can get the next group of traffic policy instances by calling ListTrafficPolicyInstancesByPolicy again and specifying the values of the HostedZoneIdMarker, TrafficPolicyInstanceNameMarker, and TrafficPolicyInstanceTypeMarker elements in the corresponding request parameters."];
      maxItems: PageMaxItems.t
        [@ocaml.doc
          "The value that you specified for the MaxItems parameter in the call to ListTrafficPolicyInstancesByPolicy that produced the current response."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `NoSuchTrafficPolicy of NoSuchTrafficPolicy.t 
      | `NoSuchTrafficPolicyInstance of NoSuchTrafficPolicyInstance.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListTrafficPolicyInstancesByPolicyResponse"
    let make ?hostedZoneIdMarker =
      fun ?trafficPolicyInstanceNameMarker ->
        fun ?trafficPolicyInstanceTypeMarker ->
          fun ~trafficPolicyInstances ->
            fun ~isTruncated ->
              fun ~maxItems ->
                fun () ->
                  {
                    hostedZoneIdMarker;
                    trafficPolicyInstanceNameMarker;
                    trafficPolicyInstanceTypeMarker;
                    trafficPolicyInstances;
                    isTruncated;
                    maxItems
                  }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchTrafficPolicy" ->
          `NoSuchTrafficPolicy (NoSuchTrafficPolicy.of_json json)
      | "NoSuchTrafficPolicyInstance" ->
          `NoSuchTrafficPolicyInstance
            (NoSuchTrafficPolicyInstance.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchTrafficPolicy" ->
          `NoSuchTrafficPolicy (NoSuchTrafficPolicy.of_xml xml)
      | "NoSuchTrafficPolicyInstance" ->
          `NoSuchTrafficPolicyInstance
            (NoSuchTrafficPolicyInstance.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchTrafficPolicy e ->
          `Assoc
            [("error", (`String "NoSuchTrafficPolicy"));
            ("details", (NoSuchTrafficPolicy.to_json e))]
      | `NoSuchTrafficPolicyInstance e ->
          `Assoc
            [("error", (`String "NoSuchTrafficPolicyInstance"));
            ("details", (NoSuchTrafficPolicyInstance.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("TrafficPolicyInstances",
           (Some (TrafficPolicyInstances.to_value x.trafficPolicyInstances)));
        ("HostedZoneIdMarker",
          (Option.map x.hostedZoneIdMarker ~f:ResourceId.to_value));
        ("TrafficPolicyInstanceNameMarker",
          (Option.map x.trafficPolicyInstanceNameMarker ~f:DNSName.to_value));
        ("TrafficPolicyInstanceTypeMarker",
          (Option.map x.trafficPolicyInstanceTypeMarker ~f:RRType.to_value));
        ("IsTruncated", (Some (PageTruncated.to_value x.isTruncated)));
        ("MaxItems", (Some (PageMaxItems.to_value x.maxItems)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxItems =
        PageMaxItems.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MaxItems") in
      let isTruncated =
        PageTruncated.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IsTruncated") in
      let trafficPolicyInstanceTypeMarker =
        (Option.map ~f:RRType.of_xml)
          (Xml.child xml_arg0 "TrafficPolicyInstanceTypeMarker") in
      let trafficPolicyInstanceNameMarker =
        (Option.map ~f:DNSName.of_xml)
          (Xml.child xml_arg0 "TrafficPolicyInstanceNameMarker") in
      let hostedZoneIdMarker =
        (Option.map ~f:ResourceId.of_xml)
          (Xml.child xml_arg0 "HostedZoneIdMarker") in
      let trafficPolicyInstances =
        TrafficPolicyInstances.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficPolicyInstances") in
      make ~maxItems ~isTruncated ?trafficPolicyInstanceTypeMarker
        ?trafficPolicyInstanceNameMarker ?hostedZoneIdMarker
        ~trafficPolicyInstances ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxItems = field_map_exn json "MaxItems" PageMaxItems.of_json in
      let isTruncated =
        field_map_exn json "IsTruncated" PageTruncated.of_json in
      let trafficPolicyInstanceTypeMarker =
        field_map json "TrafficPolicyInstanceTypeMarker" RRType.of_json in
      let trafficPolicyInstanceNameMarker =
        field_map json "TrafficPolicyInstanceNameMarker" DNSName.of_json in
      let hostedZoneIdMarker =
        field_map json "HostedZoneIdMarker" ResourceId.of_json in
      let trafficPolicyInstances =
        field_map_exn json "TrafficPolicyInstances"
          TrafficPolicyInstances.of_json in
      make ~maxItems ~isTruncated ?trafficPolicyInstanceTypeMarker
        ?trafficPolicyInstanceNameMarker ?hostedZoneIdMarker
        ~trafficPolicyInstances ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response information for the request."]
module ListTrafficPolicyInstancesByPolicyRequest =
  struct
    type nonrec t =
      {
      trafficPolicyId: TrafficPolicyId.t
        [@ocaml.doc
          "The ID of the traffic policy for which you want to list traffic policy instances."];
      trafficPolicyVersion: TrafficPolicyVersion.t
        [@ocaml.doc
          "The version of the traffic policy for which you want to list traffic policy instances. The version must be associated with the traffic policy that is specified by TrafficPolicyId."];
      hostedZoneIdMarker: ResourceId.t option
        [@ocaml.doc
          "If the value of IsTruncated in the previous response was true, you have more traffic policy instances. To get more traffic policy instances, submit another ListTrafficPolicyInstancesByPolicy request. For the value of hostedzoneid, specify the value of HostedZoneIdMarker from the previous response, which is the hosted zone ID of the first traffic policy instance that Amazon Route 53 will return if you submit another request. If the value of IsTruncated in the previous response was false, there are no more traffic policy instances to get."];
      trafficPolicyInstanceNameMarker: DNSName.t option
        [@ocaml.doc
          "If the value of IsTruncated in the previous response was true, you have more traffic policy instances. To get more traffic policy instances, submit another ListTrafficPolicyInstancesByPolicy request. For the value of trafficpolicyinstancename, specify the value of TrafficPolicyInstanceNameMarker from the previous response, which is the name of the first traffic policy instance that Amazon Route 53 will return if you submit another request. If the value of IsTruncated in the previous response was false, there are no more traffic policy instances to get."];
      trafficPolicyInstanceTypeMarker: RRType.t option
        [@ocaml.doc
          "If the value of IsTruncated in the previous response was true, you have more traffic policy instances. To get more traffic policy instances, submit another ListTrafficPolicyInstancesByPolicy request. For the value of trafficpolicyinstancetype, specify the value of TrafficPolicyInstanceTypeMarker from the previous response, which is the name of the first traffic policy instance that Amazon Route 53 will return if you submit another request. If the value of IsTruncated in the previous response was false, there are no more traffic policy instances to get."];
      maxItems: PageMaxItems.t option
        [@ocaml.doc
          "The maximum number of traffic policy instances to be included in the response body for this request. If you have more than MaxItems traffic policy instances, the value of the IsTruncated element in the response is true, and the values of HostedZoneIdMarker, TrafficPolicyInstanceNameMarker, and TrafficPolicyInstanceTypeMarker represent the first traffic policy instance that Amazon Route 53 will return if you submit another request."]}
    let context_ = "ListTrafficPolicyInstancesByPolicyRequest"
    let make ?hostedZoneIdMarker =
      fun ?trafficPolicyInstanceNameMarker ->
        fun ?trafficPolicyInstanceTypeMarker ->
          fun ?maxItems ->
            fun ~trafficPolicyId ->
              fun ~trafficPolicyVersion ->
                fun () ->
                  {
                    hostedZoneIdMarker;
                    trafficPolicyInstanceNameMarker;
                    trafficPolicyInstanceTypeMarker;
                    maxItems;
                    trafficPolicyId;
                    trafficPolicyVersion
                  }
    let to_value x =
      structure_to_value
        [("id", (Some (TrafficPolicyId.to_value x.trafficPolicyId)));
        ("version",
          (Some (TrafficPolicyVersion.to_value x.trafficPolicyVersion)));
        ("hostedzoneid",
          (Option.map x.hostedZoneIdMarker ~f:ResourceId.to_value));
        ("trafficpolicyinstancename",
          (Option.map x.trafficPolicyInstanceNameMarker ~f:DNSName.to_value));
        ("trafficpolicyinstancetype",
          (Option.map x.trafficPolicyInstanceTypeMarker ~f:RRType.to_value));
        ("maxitems", (Option.map x.maxItems ~f:PageMaxItems.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxItems =
        (Option.map ~f:PageMaxItems.of_xml) (Xml.child xml_arg0 "maxitems") in
      let trafficPolicyInstanceTypeMarker =
        (Option.map ~f:RRType.of_xml)
          (Xml.child xml_arg0 "trafficpolicyinstancetype") in
      let trafficPolicyInstanceNameMarker =
        (Option.map ~f:DNSName.of_xml)
          (Xml.child xml_arg0 "trafficpolicyinstancename") in
      let hostedZoneIdMarker =
        (Option.map ~f:ResourceId.of_xml) (Xml.child xml_arg0 "hostedzoneid") in
      let trafficPolicyVersion =
        TrafficPolicyVersion.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "version") in
      let trafficPolicyId =
        TrafficPolicyId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ?maxItems ?trafficPolicyInstanceTypeMarker
        ?trafficPolicyInstanceNameMarker ?hostedZoneIdMarker
        ~trafficPolicyVersion ~trafficPolicyId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxItems = field_map json "MaxItems" PageMaxItems.of_json in
      let trafficPolicyInstanceTypeMarker =
        field_map json "TrafficPolicyInstanceTypeMarker" RRType.of_json in
      let trafficPolicyInstanceNameMarker =
        field_map json "TrafficPolicyInstanceNameMarker" DNSName.of_json in
      let hostedZoneIdMarker =
        field_map json "HostedZoneIdMarker" ResourceId.of_json in
      let trafficPolicyVersion =
        field_map_exn json "TrafficPolicyVersion"
          TrafficPolicyVersion.of_json in
      let trafficPolicyId =
        field_map_exn json "TrafficPolicyId" TrafficPolicyId.of_json in
      make ?maxItems ?trafficPolicyInstanceTypeMarker
        ?trafficPolicyInstanceNameMarker ?hostedZoneIdMarker
        ~trafficPolicyVersion ~trafficPolicyId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the information about the request to list your traffic policy instances."]
module ListTrafficPolicyInstancesByHostedZoneResponse =
  struct
    type nonrec t =
      {
      trafficPolicyInstances: TrafficPolicyInstances.t
        [@ocaml.doc
          "A list that contains one TrafficPolicyInstance element for each traffic policy instance that matches the elements in the request."];
      trafficPolicyInstanceNameMarker: DNSName.t option
        [@ocaml.doc
          "If IsTruncated is true, TrafficPolicyInstanceNameMarker is the name of the first traffic policy instance in the next group of traffic policy instances."];
      trafficPolicyInstanceTypeMarker: RRType.t option
        [@ocaml.doc
          "If IsTruncated is true, TrafficPolicyInstanceTypeMarker is the DNS type of the resource record sets that are associated with the first traffic policy instance in the next group of traffic policy instances."];
      isTruncated: PageTruncated.t
        [@ocaml.doc
          "A flag that indicates whether there are more traffic policy instances to be listed. If the response was truncated, you can get the next group of traffic policy instances by submitting another ListTrafficPolicyInstancesByHostedZone request and specifying the values of HostedZoneIdMarker, TrafficPolicyInstanceNameMarker, and TrafficPolicyInstanceTypeMarker in the corresponding request parameters."];
      maxItems: PageMaxItems.t
        [@ocaml.doc
          "The value that you specified for the MaxItems parameter in the ListTrafficPolicyInstancesByHostedZone request that produced the current response."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `NoSuchHostedZone of NoSuchHostedZone.t 
      | `NoSuchTrafficPolicyInstance of NoSuchTrafficPolicyInstance.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListTrafficPolicyInstancesByHostedZoneResponse"
    let make ?trafficPolicyInstanceNameMarker =
      fun ?trafficPolicyInstanceTypeMarker ->
        fun ~trafficPolicyInstances ->
          fun ~isTruncated ->
            fun ~maxItems ->
              fun () ->
                {
                  trafficPolicyInstanceNameMarker;
                  trafficPolicyInstanceTypeMarker;
                  trafficPolicyInstances;
                  isTruncated;
                  maxItems
                }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchHostedZone" ->
          `NoSuchHostedZone (NoSuchHostedZone.of_json json)
      | "NoSuchTrafficPolicyInstance" ->
          `NoSuchTrafficPolicyInstance
            (NoSuchTrafficPolicyInstance.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchHostedZone" -> `NoSuchHostedZone (NoSuchHostedZone.of_xml xml)
      | "NoSuchTrafficPolicyInstance" ->
          `NoSuchTrafficPolicyInstance
            (NoSuchTrafficPolicyInstance.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchHostedZone e ->
          `Assoc
            [("error", (`String "NoSuchHostedZone"));
            ("details", (NoSuchHostedZone.to_json e))]
      | `NoSuchTrafficPolicyInstance e ->
          `Assoc
            [("error", (`String "NoSuchTrafficPolicyInstance"));
            ("details", (NoSuchTrafficPolicyInstance.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("TrafficPolicyInstances",
           (Some (TrafficPolicyInstances.to_value x.trafficPolicyInstances)));
        ("TrafficPolicyInstanceNameMarker",
          (Option.map x.trafficPolicyInstanceNameMarker ~f:DNSName.to_value));
        ("TrafficPolicyInstanceTypeMarker",
          (Option.map x.trafficPolicyInstanceTypeMarker ~f:RRType.to_value));
        ("IsTruncated", (Some (PageTruncated.to_value x.isTruncated)));
        ("MaxItems", (Some (PageMaxItems.to_value x.maxItems)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxItems =
        PageMaxItems.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MaxItems") in
      let isTruncated =
        PageTruncated.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IsTruncated") in
      let trafficPolicyInstanceTypeMarker =
        (Option.map ~f:RRType.of_xml)
          (Xml.child xml_arg0 "TrafficPolicyInstanceTypeMarker") in
      let trafficPolicyInstanceNameMarker =
        (Option.map ~f:DNSName.of_xml)
          (Xml.child xml_arg0 "TrafficPolicyInstanceNameMarker") in
      let trafficPolicyInstances =
        TrafficPolicyInstances.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficPolicyInstances") in
      make ~maxItems ~isTruncated ?trafficPolicyInstanceTypeMarker
        ?trafficPolicyInstanceNameMarker ~trafficPolicyInstances ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxItems = field_map_exn json "MaxItems" PageMaxItems.of_json in
      let isTruncated =
        field_map_exn json "IsTruncated" PageTruncated.of_json in
      let trafficPolicyInstanceTypeMarker =
        field_map json "TrafficPolicyInstanceTypeMarker" RRType.of_json in
      let trafficPolicyInstanceNameMarker =
        field_map json "TrafficPolicyInstanceNameMarker" DNSName.of_json in
      let trafficPolicyInstances =
        field_map_exn json "TrafficPolicyInstances"
          TrafficPolicyInstances.of_json in
      make ~maxItems ~isTruncated ?trafficPolicyInstanceTypeMarker
        ?trafficPolicyInstanceNameMarker ~trafficPolicyInstances ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response information for the request."]
module ListTrafficPolicyInstancesByHostedZoneRequest =
  struct
    type nonrec t =
      {
      hostedZoneId: ResourceId.t
        [@ocaml.doc
          "The ID of the hosted zone that you want to list traffic policy instances for."];
      trafficPolicyInstanceNameMarker: DNSName.t option
        [@ocaml.doc
          "If the value of IsTruncated in the previous response is true, you have more traffic policy instances. To get more traffic policy instances, submit another ListTrafficPolicyInstances request. For the value of trafficpolicyinstancename, specify the value of TrafficPolicyInstanceNameMarker from the previous response, which is the name of the first traffic policy instance in the next group of traffic policy instances. If the value of IsTruncated in the previous response was false, there are no more traffic policy instances to get."];
      trafficPolicyInstanceTypeMarker: RRType.t option
        [@ocaml.doc
          "If the value of IsTruncated in the previous response is true, you have more traffic policy instances. To get more traffic policy instances, submit another ListTrafficPolicyInstances request. For the value of trafficpolicyinstancetype, specify the value of TrafficPolicyInstanceTypeMarker from the previous response, which is the type of the first traffic policy instance in the next group of traffic policy instances. If the value of IsTruncated in the previous response was false, there are no more traffic policy instances to get."];
      maxItems: PageMaxItems.t option
        [@ocaml.doc
          "The maximum number of traffic policy instances to be included in the response body for this request. If you have more than MaxItems traffic policy instances, the value of the IsTruncated element in the response is true, and the values of HostedZoneIdMarker, TrafficPolicyInstanceNameMarker, and TrafficPolicyInstanceTypeMarker represent the first traffic policy instance that Amazon Route 53 will return if you submit another request."]}
    let context_ = "ListTrafficPolicyInstancesByHostedZoneRequest"
    let make ?trafficPolicyInstanceNameMarker =
      fun ?trafficPolicyInstanceTypeMarker ->
        fun ?maxItems ->
          fun ~hostedZoneId ->
            fun () ->
              {
                trafficPolicyInstanceNameMarker;
                trafficPolicyInstanceTypeMarker;
                maxItems;
                hostedZoneId
              }
    let to_value x =
      structure_to_value
        [("id", (Some (ResourceId.to_value x.hostedZoneId)));
        ("trafficpolicyinstancename",
          (Option.map x.trafficPolicyInstanceNameMarker ~f:DNSName.to_value));
        ("trafficpolicyinstancetype",
          (Option.map x.trafficPolicyInstanceTypeMarker ~f:RRType.to_value));
        ("maxitems", (Option.map x.maxItems ~f:PageMaxItems.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxItems =
        (Option.map ~f:PageMaxItems.of_xml) (Xml.child xml_arg0 "maxitems") in
      let trafficPolicyInstanceTypeMarker =
        (Option.map ~f:RRType.of_xml)
          (Xml.child xml_arg0 "trafficpolicyinstancetype") in
      let trafficPolicyInstanceNameMarker =
        (Option.map ~f:DNSName.of_xml)
          (Xml.child xml_arg0 "trafficpolicyinstancename") in
      let hostedZoneId =
        ResourceId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "id") in
      make ?maxItems ?trafficPolicyInstanceTypeMarker
        ?trafficPolicyInstanceNameMarker ~hostedZoneId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxItems = field_map json "MaxItems" PageMaxItems.of_json in
      let trafficPolicyInstanceTypeMarker =
        field_map json "TrafficPolicyInstanceTypeMarker" RRType.of_json in
      let trafficPolicyInstanceNameMarker =
        field_map json "TrafficPolicyInstanceNameMarker" DNSName.of_json in
      let hostedZoneId = field_map_exn json "HostedZoneId" ResourceId.of_json in
      make ?maxItems ?trafficPolicyInstanceTypeMarker
        ?trafficPolicyInstanceNameMarker ~hostedZoneId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A request for the traffic policy instances that you created in a specified hosted zone."]
module ListTrafficPoliciesResponse =
  struct
    type nonrec t =
      {
      trafficPolicySummaries: TrafficPolicySummaries.t
        [@ocaml.doc
          "A list that contains one TrafficPolicySummary element for each traffic policy that was created by the current Amazon Web Services account."];
      isTruncated: PageTruncated.t
        [@ocaml.doc
          "A flag that indicates whether there are more traffic policies to be listed. If the response was truncated, you can get the next group of traffic policies by submitting another ListTrafficPolicies request and specifying the value of TrafficPolicyIdMarker in the TrafficPolicyIdMarker request parameter."];
      trafficPolicyIdMarker: TrafficPolicyId.t
        [@ocaml.doc
          "If the value of IsTruncated is true, TrafficPolicyIdMarker is the ID of the first traffic policy in the next group of MaxItems traffic policies."];
      maxItems: PageMaxItems.t
        [@ocaml.doc
          "The value that you specified for the MaxItems parameter in the ListTrafficPolicies request that produced the current response."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListTrafficPoliciesResponse"
    let make ~trafficPolicySummaries =
      fun ~isTruncated ->
        fun ~trafficPolicyIdMarker ->
          fun ~maxItems ->
            fun () ->
              {
                trafficPolicySummaries;
                isTruncated;
                trafficPolicyIdMarker;
                maxItems
              }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("TrafficPolicySummaries",
           (Some (TrafficPolicySummaries.to_value x.trafficPolicySummaries)));
        ("IsTruncated", (Some (PageTruncated.to_value x.isTruncated)));
        ("TrafficPolicyIdMarker",
          (Some (TrafficPolicyId.to_value x.trafficPolicyIdMarker)));
        ("MaxItems", (Some (PageMaxItems.to_value x.maxItems)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxItems =
        PageMaxItems.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MaxItems") in
      let trafficPolicyIdMarker =
        TrafficPolicyId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficPolicyIdMarker") in
      let isTruncated =
        PageTruncated.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IsTruncated") in
      let trafficPolicySummaries =
        TrafficPolicySummaries.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficPolicySummaries") in
      make ~maxItems ~trafficPolicyIdMarker ~isTruncated
        ~trafficPolicySummaries ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxItems = field_map_exn json "MaxItems" PageMaxItems.of_json in
      let trafficPolicyIdMarker =
        field_map_exn json "TrafficPolicyIdMarker" TrafficPolicyId.of_json in
      let isTruncated =
        field_map_exn json "IsTruncated" PageTruncated.of_json in
      let trafficPolicySummaries =
        field_map_exn json "TrafficPolicySummaries"
          TrafficPolicySummaries.of_json in
      make ~maxItems ~trafficPolicyIdMarker ~isTruncated
        ~trafficPolicySummaries ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response information for the request."]
module ListTrafficPoliciesRequest =
  struct
    type nonrec t =
      {
      trafficPolicyIdMarker: TrafficPolicyId.t option
        [@ocaml.doc
          "(Conditional) For your first request to ListTrafficPolicies, don't include the TrafficPolicyIdMarker parameter. If you have more traffic policies than the value of MaxItems, ListTrafficPolicies returns only the first MaxItems traffic policies. To get the next group of policies, submit another request to ListTrafficPolicies. For the value of TrafficPolicyIdMarker, specify the value of TrafficPolicyIdMarker that was returned in the previous response."];
      maxItems: PageMaxItems.t option
        [@ocaml.doc
          "(Optional) The maximum number of traffic policies that you want Amazon Route 53 to return in response to this request. If you have more than MaxItems traffic policies, the value of IsTruncated in the response is true, and the value of TrafficPolicyIdMarker is the ID of the first traffic policy that Route 53 will return if you submit another request."]}
    let make ?trafficPolicyIdMarker =
      fun ?maxItems -> fun () -> { trafficPolicyIdMarker; maxItems }
    let to_value x =
      structure_to_value
        [("trafficpolicyid",
           (Option.map x.trafficPolicyIdMarker ~f:TrafficPolicyId.to_value));
        ("maxitems", (Option.map x.maxItems ~f:PageMaxItems.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxItems =
        (Option.map ~f:PageMaxItems.of_xml) (Xml.child xml_arg0 "maxitems") in
      let trafficPolicyIdMarker =
        (Option.map ~f:TrafficPolicyId.of_xml)
          (Xml.child xml_arg0 "trafficpolicyid") in
      make ?maxItems ?trafficPolicyIdMarker ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxItems = field_map json "MaxItems" PageMaxItems.of_json in
      let trafficPolicyIdMarker =
        field_map json "TrafficPolicyIdMarker" TrafficPolicyId.of_json in
      make ?maxItems ?trafficPolicyIdMarker ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the information about the request to list the traffic policies that are associated with the current Amazon Web Services account."]
module ListTagsForResourcesResponse =
  struct
    type nonrec t =
      {
      resourceTagSets: ResourceTagSetList.t
        [@ocaml.doc
          "A list of ResourceTagSets containing tags associated with the specified resources."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `NoSuchHealthCheck of NoSuchHealthCheck.t 
      | `NoSuchHostedZone of NoSuchHostedZone.t 
      | `PriorRequestNotComplete of PriorRequestNotComplete.t 
      | `ThrottlingException of ThrottlingException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListTagsForResourcesResponse"
    let make ~resourceTagSets = fun () -> { resourceTagSets }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchHealthCheck" ->
          `NoSuchHealthCheck (NoSuchHealthCheck.of_json json)
      | "NoSuchHostedZone" ->
          `NoSuchHostedZone (NoSuchHostedZone.of_json json)
      | "PriorRequestNotComplete" ->
          `PriorRequestNotComplete (PriorRequestNotComplete.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchHealthCheck" ->
          `NoSuchHealthCheck (NoSuchHealthCheck.of_xml xml)
      | "NoSuchHostedZone" -> `NoSuchHostedZone (NoSuchHostedZone.of_xml xml)
      | "PriorRequestNotComplete" ->
          `PriorRequestNotComplete (PriorRequestNotComplete.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchHealthCheck e ->
          `Assoc
            [("error", (`String "NoSuchHealthCheck"));
            ("details", (NoSuchHealthCheck.to_json e))]
      | `NoSuchHostedZone e ->
          `Assoc
            [("error", (`String "NoSuchHostedZone"));
            ("details", (NoSuchHostedZone.to_json e))]
      | `PriorRequestNotComplete e ->
          `Assoc
            [("error", (`String "PriorRequestNotComplete"));
            ("details", (PriorRequestNotComplete.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ResourceTagSets",
           (Some (ResourceTagSetList.to_value x.resourceTagSets)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceTagSets =
        ResourceTagSetList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceTagSets") in
      make ~resourceTagSets ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let resourceTagSets =
        field_map_exn json "ResourceTagSets" ResourceTagSetList.of_json in
      make ~resourceTagSets ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type containing tags for the specified resources."]
module ListTagsForResourcesRequest =
  struct
    type nonrec t =
      {
      resourceType: TagResourceType.t
        [@ocaml.doc
          "The type of the resources. The resource type for health checks is healthcheck. The resource type for hosted zones is hostedzone."];
      resourceIds: TagResourceIdList.t
        [@ocaml.doc
          "A complex type that contains the ResourceId element for each resource for which you want to get a list of tags."]}
    let context_ = "ListTagsForResourcesRequest"
    let make ~resourceType =
      fun ~resourceIds -> fun () -> { resourceType; resourceIds }
    let to_value x =
      structure_to_value
        [("ResourceType", (Some (TagResourceType.to_value x.resourceType)));
        ("ResourceIds", (Some (TagResourceIdList.to_value x.resourceIds)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceIds =
        TagResourceIdList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceIds") in
      let resourceType =
        TagResourceType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceType") in
      make ~resourceIds ~resourceType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let resourceIds =
        field_map_exn json "ResourceIds" TagResourceIdList.of_json in
      let resourceType =
        field_map_exn json "ResourceType" TagResourceType.of_json in
      make ~resourceIds ~resourceType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about the health checks or hosted zones for which you want to list tags."]
module ListTagsForResourceResponse =
  struct
    type nonrec t =
      {
      resourceTagSet: ResourceTagSet.t
        [@ocaml.doc
          "A ResourceTagSet containing tags associated with the specified resource."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `NoSuchHealthCheck of NoSuchHealthCheck.t 
      | `NoSuchHostedZone of NoSuchHostedZone.t 
      | `PriorRequestNotComplete of PriorRequestNotComplete.t 
      | `ThrottlingException of ThrottlingException.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListTagsForResourceResponse"
    let make ~resourceTagSet = fun () -> { resourceTagSet }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchHealthCheck" ->
          `NoSuchHealthCheck (NoSuchHealthCheck.of_json json)
      | "NoSuchHostedZone" ->
          `NoSuchHostedZone (NoSuchHostedZone.of_json json)
      | "PriorRequestNotComplete" ->
          `PriorRequestNotComplete (PriorRequestNotComplete.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchHealthCheck" ->
          `NoSuchHealthCheck (NoSuchHealthCheck.of_xml xml)
      | "NoSuchHostedZone" -> `NoSuchHostedZone (NoSuchHostedZone.of_xml xml)
      | "PriorRequestNotComplete" ->
          `PriorRequestNotComplete (PriorRequestNotComplete.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchHealthCheck e ->
          `Assoc
            [("error", (`String "NoSuchHealthCheck"));
            ("details", (NoSuchHealthCheck.to_json e))]
      | `NoSuchHostedZone e ->
          `Assoc
            [("error", (`String "NoSuchHostedZone"));
            ("details", (NoSuchHostedZone.to_json e))]
      | `PriorRequestNotComplete e ->
          `Assoc
            [("error", (`String "PriorRequestNotComplete"));
            ("details", (PriorRequestNotComplete.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ResourceTagSet",
           (Some (ResourceTagSet.to_value x.resourceTagSet)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceTagSet =
        ResourceTagSet.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceTagSet") in
      make ~resourceTagSet ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let resourceTagSet =
        field_map_exn json "ResourceTagSet" ResourceTagSet.of_json in
      make ~resourceTagSet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about the health checks or hosted zones for which you want to list tags."]
module ListTagsForResourceRequest =
  struct
    type nonrec t =
      {
      resourceType: TagResourceType.t
        [@ocaml.doc
          "The type of the resource. The resource type for health checks is healthcheck. The resource type for hosted zones is hostedzone."];
      resourceId: TagResourceId.t
        [@ocaml.doc
          "The ID of the resource for which you want to retrieve tags."]}
    let context_ = "ListTagsForResourceRequest"
    let make ~resourceType =
      fun ~resourceId -> fun () -> { resourceType; resourceId }
    let to_value x =
      structure_to_value
        [("ResourceType", (Some (TagResourceType.to_value x.resourceType)));
        ("ResourceId", (Some (TagResourceId.to_value x.resourceId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let resourceId =
        TagResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceId") in
      let resourceType =
        TagResourceType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceType") in
      make ~resourceId ~resourceType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let resourceId = field_map_exn json "ResourceId" TagResourceId.of_json in
      let resourceType =
        field_map_exn json "ResourceType" TagResourceType.of_json in
      make ~resourceId ~resourceType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type containing information about a request for a list of the tags that are associated with an individual resource."]
module ListReusableDelegationSetsResponse =
  struct
    type nonrec t =
      {
      delegationSets: DelegationSets.t
        [@ocaml.doc
          "A complex type that contains one DelegationSet element for each reusable delegation set that was created by the current Amazon Web Services account."];
      marker: PageMarker.t option
        [@ocaml.doc
          "For the second and subsequent calls to ListReusableDelegationSets, Marker is the value that you specified for the marker parameter in the request that produced the current response."];
      isTruncated: PageTruncated.t
        [@ocaml.doc
          "A flag that indicates whether there are more reusable delegation sets to be listed."];
      nextMarker: PageMarker.t option
        [@ocaml.doc
          "If IsTruncated is true, the value of NextMarker identifies the next reusable delegation set that Amazon Route 53 will return if you submit another ListReusableDelegationSets request and specify the value of NextMarker in the marker parameter."];
      maxItems: PageMaxItems.t
        [@ocaml.doc
          "The value that you specified for the maxitems parameter in the call to ListReusableDelegationSets that produced the current response."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListReusableDelegationSetsResponse"
    let make ?marker =
      fun ?nextMarker ->
        fun ~delegationSets ->
          fun ~isTruncated ->
            fun ~maxItems ->
              fun () ->
                { marker; nextMarker; delegationSets; isTruncated; maxItems }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("DelegationSets",
           (Some (DelegationSets.to_value x.delegationSets)));
        ("Marker", (Option.map x.marker ~f:PageMarker.to_value));
        ("IsTruncated", (Some (PageTruncated.to_value x.isTruncated)));
        ("NextMarker", (Option.map x.nextMarker ~f:PageMarker.to_value));
        ("MaxItems", (Some (PageMaxItems.to_value x.maxItems)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxItems =
        PageMaxItems.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MaxItems") in
      let nextMarker =
        (Option.map ~f:PageMarker.of_xml) (Xml.child xml_arg0 "NextMarker") in
      let isTruncated =
        PageTruncated.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IsTruncated") in
      let marker =
        (Option.map ~f:PageMarker.of_xml) (Xml.child xml_arg0 "Marker") in
      let delegationSets =
        DelegationSets.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DelegationSets") in
      make ~maxItems ?nextMarker ~isTruncated ?marker ~delegationSets ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxItems = field_map_exn json "MaxItems" PageMaxItems.of_json in
      let nextMarker = field_map json "NextMarker" PageMarker.of_json in
      let isTruncated =
        field_map_exn json "IsTruncated" PageTruncated.of_json in
      let marker = field_map json "Marker" PageMarker.of_json in
      let delegationSets =
        field_map_exn json "DelegationSets" DelegationSets.of_json in
      make ~maxItems ?nextMarker ~isTruncated ?marker ~delegationSets ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about the reusable delegation sets that are associated with the current Amazon Web Services account."]
module ListReusableDelegationSetsRequest =
  struct
    type nonrec t =
      {
      marker: PageMarker.t option
        [@ocaml.doc
          "If the value of IsTruncated in the previous response was true, you have more reusable delegation sets. To get another group, submit another ListReusableDelegationSets request. For the value of marker, specify the value of NextMarker from the previous response, which is the ID of the first reusable delegation set that Amazon Route 53 will return if you submit another request. If the value of IsTruncated in the previous response was false, there are no more reusable delegation sets to get."];
      maxItems: PageMaxItems.t option
        [@ocaml.doc
          "The number of reusable delegation sets that you want Amazon Route 53 to return in the response to this request. If you specify a value greater than 100, Route 53 returns only the first 100 reusable delegation sets."]}
    let make ?marker = fun ?maxItems -> fun () -> { marker; maxItems }
    let to_value x =
      structure_to_value
        [("marker", (Option.map x.marker ~f:PageMarker.to_value));
        ("maxitems", (Option.map x.maxItems ~f:PageMaxItems.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxItems =
        (Option.map ~f:PageMaxItems.of_xml) (Xml.child xml_arg0 "maxitems") in
      let marker =
        (Option.map ~f:PageMarker.of_xml) (Xml.child xml_arg0 "marker") in
      make ?maxItems ?marker ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxItems = field_map json "MaxItems" PageMaxItems.of_json in
      let marker = field_map json "Marker" PageMarker.of_json in
      make ?maxItems ?marker ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A request to get a list of the reusable delegation sets that are associated with the current Amazon Web Services account."]
module ListResourceRecordSetsResponse =
  struct
    type nonrec t =
      {
      resourceRecordSets: ResourceRecordSets.t
        [@ocaml.doc "Information about multiple resource record sets."];
      isTruncated: PageTruncated.t
        [@ocaml.doc
          "A flag that indicates whether more resource record sets remain to be listed. If your results were truncated, you can make a follow-up pagination request by using the NextRecordName element."];
      nextRecordName: DNSName.t option
        [@ocaml.doc
          "If the results were truncated, the name of the next record in the list. This element is present only if IsTruncated is true."];
      nextRecordType: RRType.t option
        [@ocaml.doc
          "If the results were truncated, the type of the next record in the list. This element is present only if IsTruncated is true."];
      nextRecordIdentifier: ResourceRecordSetIdentifier.t option
        [@ocaml.doc
          "Resource record sets that have a routing policy other than simple: If results were truncated for a given DNS name and type, the value of SetIdentifier for the next resource record set that has the current DNS name and type. For information about routing policies, see Choosing a Routing Policy in the Amazon Route 53 Developer Guide."];
      maxItems: PageMaxItems.t
        [@ocaml.doc "The maximum number of records you requested."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `NoSuchHostedZone of NoSuchHostedZone.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListResourceRecordSetsResponse"
    let make ?nextRecordName =
      fun ?nextRecordType ->
        fun ?nextRecordIdentifier ->
          fun ~resourceRecordSets ->
            fun ~isTruncated ->
              fun ~maxItems ->
                fun () ->
                  {
                    nextRecordName;
                    nextRecordType;
                    nextRecordIdentifier;
                    resourceRecordSets;
                    isTruncated;
                    maxItems
                  }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchHostedZone" ->
          `NoSuchHostedZone (NoSuchHostedZone.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchHostedZone" -> `NoSuchHostedZone (NoSuchHostedZone.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchHostedZone e ->
          `Assoc
            [("error", (`String "NoSuchHostedZone"));
            ("details", (NoSuchHostedZone.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ResourceRecordSets",
           (Some (ResourceRecordSets.to_value x.resourceRecordSets)));
        ("IsTruncated", (Some (PageTruncated.to_value x.isTruncated)));
        ("NextRecordName", (Option.map x.nextRecordName ~f:DNSName.to_value));
        ("NextRecordType", (Option.map x.nextRecordType ~f:RRType.to_value));
        ("NextRecordIdentifier",
          (Option.map x.nextRecordIdentifier
             ~f:ResourceRecordSetIdentifier.to_value));
        ("MaxItems", (Some (PageMaxItems.to_value x.maxItems)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxItems =
        PageMaxItems.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MaxItems") in
      let nextRecordIdentifier =
        (Option.map ~f:ResourceRecordSetIdentifier.of_xml)
          (Xml.child xml_arg0 "NextRecordIdentifier") in
      let nextRecordType =
        (Option.map ~f:RRType.of_xml) (Xml.child xml_arg0 "NextRecordType") in
      let nextRecordName =
        (Option.map ~f:DNSName.of_xml) (Xml.child xml_arg0 "NextRecordName") in
      let isTruncated =
        PageTruncated.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IsTruncated") in
      let resourceRecordSets =
        ResourceRecordSets.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceRecordSets") in
      make ~maxItems ?nextRecordIdentifier ?nextRecordType ?nextRecordName
        ~isTruncated ~resourceRecordSets ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxItems = field_map_exn json "MaxItems" PageMaxItems.of_json in
      let nextRecordIdentifier =
        field_map json "NextRecordIdentifier"
          ResourceRecordSetIdentifier.of_json in
      let nextRecordType = field_map json "NextRecordType" RRType.of_json in
      let nextRecordName = field_map json "NextRecordName" DNSName.of_json in
      let isTruncated =
        field_map_exn json "IsTruncated" PageTruncated.of_json in
      let resourceRecordSets =
        field_map_exn json "ResourceRecordSets" ResourceRecordSets.of_json in
      make ~maxItems ?nextRecordIdentifier ?nextRecordType ?nextRecordName
        ~isTruncated ~resourceRecordSets ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains list information for the resource record set."]
module ListResourceRecordSetsRequest =
  struct
    type nonrec t =
      {
      hostedZoneId: ResourceId.t
        [@ocaml.doc
          "The ID of the hosted zone that contains the resource record sets that you want to list."];
      startRecordName: DNSName.t option
        [@ocaml.doc
          "The first name in the lexicographic ordering of resource record sets that you want to list. If the specified record name doesn't exist, the results begin with the first resource record set that has a name greater than the value of name."];
      startRecordType: RRType.t option
        [@ocaml.doc
          "The type of resource record set to begin the record listing from. Valid values for basic resource record sets: A | AAAA | CAA | CNAME | MX | NAPTR | NS | PTR | SOA | SPF | SRV | TXT Values for weighted, latency, geolocation, and failover resource record sets: A | AAAA | CAA | CNAME | MX | NAPTR | PTR | SPF | SRV | TXT Values for alias resource record sets: API Gateway custom regional API or edge-optimized API: A CloudFront distribution: A or AAAA Elastic Beanstalk environment that has a regionalized subdomain: A Elastic Load Balancing load balancer: A | AAAA S3 bucket: A VPC interface VPC endpoint: A Another resource record set in this hosted zone: The type of the resource record set that the alias references. Constraint: Specifying type without specifying name returns an InvalidInput error."];
      startRecordIdentifier: ResourceRecordSetIdentifier.t option
        [@ocaml.doc
          "Resource record sets that have a routing policy other than simple: If results were truncated for a given DNS name and type, specify the value of NextRecordIdentifier from the previous response to get the next resource record set that has the current DNS name and type."];
      maxItems: PageMaxItems.t option
        [@ocaml.doc
          "(Optional) The maximum number of resource records sets to include in the response body for this request. If the response includes more than maxitems resource record sets, the value of the IsTruncated element in the response is true, and the values of the NextRecordName and NextRecordType elements in the response identify the first resource record set in the next group of maxitems resource record sets."]}
    let context_ = "ListResourceRecordSetsRequest"
    let make ?startRecordName =
      fun ?startRecordType ->
        fun ?startRecordIdentifier ->
          fun ?maxItems ->
            fun ~hostedZoneId ->
              fun () ->
                {
                  startRecordName;
                  startRecordType;
                  startRecordIdentifier;
                  maxItems;
                  hostedZoneId
                }
    let to_value x =
      structure_to_value
        [("Id", (Some (ResourceId.to_value x.hostedZoneId)));
        ("name", (Option.map x.startRecordName ~f:DNSName.to_value));
        ("type", (Option.map x.startRecordType ~f:RRType.to_value));
        ("identifier",
          (Option.map x.startRecordIdentifier
             ~f:ResourceRecordSetIdentifier.to_value));
        ("maxitems", (Option.map x.maxItems ~f:PageMaxItems.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxItems =
        (Option.map ~f:PageMaxItems.of_xml) (Xml.child xml_arg0 "maxitems") in
      let startRecordIdentifier =
        (Option.map ~f:ResourceRecordSetIdentifier.of_xml)
          (Xml.child xml_arg0 "identifier") in
      let startRecordType =
        (Option.map ~f:RRType.of_xml) (Xml.child xml_arg0 "type") in
      let startRecordName =
        (Option.map ~f:DNSName.of_xml) (Xml.child xml_arg0 "name") in
      let hostedZoneId =
        ResourceId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ?maxItems ?startRecordIdentifier ?startRecordType ?startRecordName
        ~hostedZoneId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxItems = field_map json "MaxItems" PageMaxItems.of_json in
      let startRecordIdentifier =
        field_map json "StartRecordIdentifier"
          ResourceRecordSetIdentifier.of_json in
      let startRecordType = field_map json "StartRecordType" RRType.of_json in
      let startRecordName = field_map json "StartRecordName" DNSName.of_json in
      let hostedZoneId = field_map_exn json "HostedZoneId" ResourceId.of_json in
      make ?maxItems ?startRecordIdentifier ?startRecordType ?startRecordName
        ~hostedZoneId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A request for the resource record sets that are associated with a specified hosted zone."]
module ListQueryLoggingConfigsResponse =
  struct
    type nonrec t =
      {
      queryLoggingConfigs: QueryLoggingConfigs.t
        [@ocaml.doc
          "An array that contains one QueryLoggingConfig element for each configuration for DNS query logging that is associated with the current Amazon Web Services account."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "If a response includes the last of the query logging configurations that are associated with the current Amazon Web Services account, NextToken doesn't appear in the response. If a response doesn't include the last of the configurations, you can get more configurations by submitting another ListQueryLoggingConfigs request. Get the value of NextToken that Amazon Route 53 returned in the previous response and include it in NextToken in the next request."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `InvalidPaginationToken of InvalidPaginationToken.t 
      | `NoSuchHostedZone of NoSuchHostedZone.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListQueryLoggingConfigsResponse"
    let make ?nextToken =
      fun ~queryLoggingConfigs ->
        fun () -> { nextToken; queryLoggingConfigs }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "InvalidPaginationToken" ->
          `InvalidPaginationToken (InvalidPaginationToken.of_json json)
      | "NoSuchHostedZone" ->
          `NoSuchHostedZone (NoSuchHostedZone.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "InvalidPaginationToken" ->
          `InvalidPaginationToken (InvalidPaginationToken.of_xml xml)
      | "NoSuchHostedZone" -> `NoSuchHostedZone (NoSuchHostedZone.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `InvalidPaginationToken e ->
          `Assoc
            [("error", (`String "InvalidPaginationToken"));
            ("details", (InvalidPaginationToken.to_json e))]
      | `NoSuchHostedZone e ->
          `Assoc
            [("error", (`String "NoSuchHostedZone"));
            ("details", (NoSuchHostedZone.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("QueryLoggingConfigs",
           (Some (QueryLoggingConfigs.to_value x.queryLoggingConfigs)));
        ("NextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let queryLoggingConfigs =
        QueryLoggingConfigs.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "QueryLoggingConfigs") in
      make ?nextToken ~queryLoggingConfigs ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" PaginationToken.of_json in
      let queryLoggingConfigs =
        field_map_exn json "QueryLoggingConfigs" QueryLoggingConfigs.of_json in
      make ?nextToken ~queryLoggingConfigs ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the configurations for DNS query logging that are associated with the current Amazon Web Services account or the configuration that is associated with a specified hosted zone. For more information about DNS query logs, see CreateQueryLoggingConfig. Additional information, including the format of DNS query logs, appears in Logging DNS Queries in the Amazon Route 53 Developer Guide."]
module ListQueryLoggingConfigsRequest =
  struct
    type nonrec t =
      {
      hostedZoneId: ResourceId.t option
        [@ocaml.doc
          "(Optional) If you want to list the query logging configuration that is associated with a hosted zone, specify the ID in HostedZoneId. If you don't specify a hosted zone ID, ListQueryLoggingConfigs returns all of the configurations that are associated with the current Amazon Web Services account."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "(Optional) If the current Amazon Web Services account has more than MaxResults query logging configurations, use NextToken to get the second and subsequent pages of results. For the first ListQueryLoggingConfigs request, omit this value. For the second and subsequent requests, get the value of NextToken from the previous response and specify that value for NextToken in the request."];
      maxResults: MaxResults.t option
        [@ocaml.doc
          "(Optional) The maximum number of query logging configurations that you want Amazon Route 53 to return in response to the current request. If the current Amazon Web Services account has more than MaxResults configurations, use the value of NextToken in the response to get the next page of results. If you don't specify a value for MaxResults, Route 53 returns up to 100 configurations."]}
    let make ?hostedZoneId =
      fun ?nextToken ->
        fun ?maxResults -> fun () -> { hostedZoneId; nextToken; maxResults }
    let to_value x =
      structure_to_value
        [("hostedzoneid", (Option.map x.hostedZoneId ~f:ResourceId.to_value));
        ("nexttoken", (Option.map x.nextToken ~f:PaginationToken.to_value));
        ("maxresults", (Option.map x.maxResults ~f:MaxResults.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxResults =
        (Option.map ~f:MaxResults.of_xml) (Xml.child xml_arg0 "maxresults") in
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nexttoken") in
      let hostedZoneId =
        (Option.map ~f:ResourceId.of_xml) (Xml.child xml_arg0 "hostedzoneid") in
      make ?maxResults ?nextToken ?hostedZoneId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxResults = field_map json "MaxResults" MaxResults.of_json in
      let nextToken = field_map json "NextToken" PaginationToken.of_json in
      let hostedZoneId = field_map json "HostedZoneId" ResourceId.of_json in
      make ?maxResults ?nextToken ?hostedZoneId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists the configurations for DNS query logging that are associated with the current Amazon Web Services account or the configuration that is associated with a specified hosted zone. For more information about DNS query logs, see CreateQueryLoggingConfig. Additional information, including the format of DNS query logs, appears in Logging DNS Queries in the Amazon Route 53 Developer Guide."]
module ListHostedZonesResponse =
  struct
    type nonrec t =
      {
      hostedZones: HostedZones.t
        [@ocaml.doc
          "A complex type that contains general information about the hosted zone."];
      marker: PageMarker.t option
        [@ocaml.doc
          "For the second and subsequent calls to ListHostedZones, Marker is the value that you specified for the marker parameter in the request that produced the current response."];
      isTruncated: PageTruncated.t
        [@ocaml.doc
          "A flag indicating whether there are more hosted zones to be listed. If the response was truncated, you can get more hosted zones by submitting another ListHostedZones request and specifying the value of NextMarker in the marker parameter."];
      nextMarker: PageMarker.t option
        [@ocaml.doc
          "If IsTruncated is true, the value of NextMarker identifies the first hosted zone in the next group of hosted zones. Submit another ListHostedZones request, and specify the value of NextMarker from the response in the marker parameter. This element is present only if IsTruncated is true."];
      maxItems: PageMaxItems.t
        [@ocaml.doc
          "The value that you specified for the maxitems parameter in the call to ListHostedZones that produced the current response."]}
    type nonrec error =
      [ `DelegationSetNotReusable of DelegationSetNotReusable.t 
      | `InvalidInput of InvalidInput.t 
      | `NoSuchDelegationSet of NoSuchDelegationSet.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListHostedZonesResponse"
    let make ?marker =
      fun ?nextMarker ->
        fun ~hostedZones ->
          fun ~isTruncated ->
            fun ~maxItems ->
              fun () ->
                { marker; nextMarker; hostedZones; isTruncated; maxItems }
    let error_of_json name json =
      match name with
      | "DelegationSetNotReusable" ->
          `DelegationSetNotReusable (DelegationSetNotReusable.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchDelegationSet" ->
          `NoSuchDelegationSet (NoSuchDelegationSet.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "DelegationSetNotReusable" ->
          `DelegationSetNotReusable (DelegationSetNotReusable.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchDelegationSet" ->
          `NoSuchDelegationSet (NoSuchDelegationSet.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `DelegationSetNotReusable e ->
          `Assoc
            [("error", (`String "DelegationSetNotReusable"));
            ("details", (DelegationSetNotReusable.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchDelegationSet e ->
          `Assoc
            [("error", (`String "NoSuchDelegationSet"));
            ("details", (NoSuchDelegationSet.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("HostedZones", (Some (HostedZones.to_value x.hostedZones)));
        ("Marker", (Option.map x.marker ~f:PageMarker.to_value));
        ("IsTruncated", (Some (PageTruncated.to_value x.isTruncated)));
        ("NextMarker", (Option.map x.nextMarker ~f:PageMarker.to_value));
        ("MaxItems", (Some (PageMaxItems.to_value x.maxItems)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxItems =
        PageMaxItems.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MaxItems") in
      let nextMarker =
        (Option.map ~f:PageMarker.of_xml) (Xml.child xml_arg0 "NextMarker") in
      let isTruncated =
        PageTruncated.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IsTruncated") in
      let marker =
        (Option.map ~f:PageMarker.of_xml) (Xml.child xml_arg0 "Marker") in
      let hostedZones =
        HostedZones.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HostedZones") in
      make ~maxItems ?nextMarker ~isTruncated ?marker ~hostedZones ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxItems = field_map_exn json "MaxItems" PageMaxItems.of_json in
      let nextMarker = field_map json "NextMarker" PageMarker.of_json in
      let isTruncated =
        field_map_exn json "IsTruncated" PageTruncated.of_json in
      let marker = field_map json "Marker" PageMarker.of_json in
      let hostedZones = field_map_exn json "HostedZones" HostedZones.of_json in
      make ~maxItems ?nextMarker ~isTruncated ?marker ~hostedZones ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a list of the public and private hosted zones that are associated with the current Amazon Web Services account. The response includes a HostedZones child element for each hosted zone. Amazon Route 53 returns a maximum of 100 items in each response. If you have a lot of hosted zones, you can use the maxitems parameter to list them in groups of up to 100."]
module ListHostedZonesRequest =
  struct
    type nonrec t =
      {
      marker: PageMarker.t option
        [@ocaml.doc
          "If the value of IsTruncated in the previous response was true, you have more hosted zones. To get more hosted zones, submit another ListHostedZones request. For the value of marker, specify the value of NextMarker from the previous response, which is the ID of the first hosted zone that Amazon Route 53 will return if you submit another request. If the value of IsTruncated in the previous response was false, there are no more hosted zones to get."];
      maxItems: PageMaxItems.t option
        [@ocaml.doc
          "(Optional) The maximum number of hosted zones that you want Amazon Route 53 to return. If you have more than maxitems hosted zones, the value of IsTruncated in the response is true, and the value of NextMarker is the hosted zone ID of the first hosted zone that Route 53 will return if you submit another request."];
      delegationSetId: ResourceId.t option
        [@ocaml.doc
          "If you're using reusable delegation sets and you want to list all of the hosted zones that are associated with a reusable delegation set, specify the ID of that reusable delegation set."]}
    let make ?marker =
      fun ?maxItems ->
        fun ?delegationSetId ->
          fun () -> { marker; maxItems; delegationSetId }
    let to_value x =
      structure_to_value
        [("marker", (Option.map x.marker ~f:PageMarker.to_value));
        ("maxitems", (Option.map x.maxItems ~f:PageMaxItems.to_value));
        ("delegationsetid",
          (Option.map x.delegationSetId ~f:ResourceId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let delegationSetId =
        (Option.map ~f:ResourceId.of_xml)
          (Xml.child xml_arg0 "delegationsetid") in
      let maxItems =
        (Option.map ~f:PageMaxItems.of_xml) (Xml.child xml_arg0 "maxitems") in
      let marker =
        (Option.map ~f:PageMarker.of_xml) (Xml.child xml_arg0 "marker") in
      make ?delegationSetId ?maxItems ?marker ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let delegationSetId =
        field_map json "DelegationSetId" ResourceId.of_json in
      let maxItems = field_map json "MaxItems" PageMaxItems.of_json in
      let marker = field_map json "Marker" PageMarker.of_json in
      make ?delegationSetId ?maxItems ?marker ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A request to retrieve a list of the public and private hosted zones that are associated with the current Amazon Web Services account."]
module ListHostedZonesByVPCResponse =
  struct
    type nonrec t =
      {
      hostedZoneSummaries: HostedZoneSummaries.t
        [@ocaml.doc
          "A list that contains one HostedZoneSummary element for each hosted zone that the specified Amazon VPC is associated with. Each HostedZoneSummary element contains the hosted zone name and ID, and information about who owns the hosted zone."];
      maxItems: PageMaxItems.t
        [@ocaml.doc
          "The value that you specified for MaxItems in the most recent ListHostedZonesByVPC request."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "The value that you will use for NextToken in the next ListHostedZonesByVPC request."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `InvalidPaginationToken of InvalidPaginationToken.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListHostedZonesByVPCResponse"
    let make ?nextToken =
      fun ~hostedZoneSummaries ->
        fun ~maxItems ->
          fun () -> { nextToken; hostedZoneSummaries; maxItems }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "InvalidPaginationToken" ->
          `InvalidPaginationToken (InvalidPaginationToken.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "InvalidPaginationToken" ->
          `InvalidPaginationToken (InvalidPaginationToken.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `InvalidPaginationToken e ->
          `Assoc
            [("error", (`String "InvalidPaginationToken"));
            ("details", (InvalidPaginationToken.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("HostedZoneSummaries",
           (Some (HostedZoneSummaries.to_value x.hostedZoneSummaries)));
        ("MaxItems", (Some (PageMaxItems.to_value x.maxItems)));
        ("NextToken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "NextToken") in
      let maxItems =
        PageMaxItems.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MaxItems") in
      let hostedZoneSummaries =
        HostedZoneSummaries.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HostedZoneSummaries") in
      make ?nextToken ~maxItems ~hostedZoneSummaries ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" PaginationToken.of_json in
      let maxItems = field_map_exn json "MaxItems" PageMaxItems.of_json in
      let hostedZoneSummaries =
        field_map_exn json "HostedZoneSummaries" HostedZoneSummaries.of_json in
      make ?nextToken ~maxItems ~hostedZoneSummaries ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all the private hosted zones that a specified VPC is associated with, regardless of which Amazon Web Services account or Amazon Web Services service owns the hosted zones. The HostedZoneOwner structure in the response contains one of the following values: An OwningAccount element, which contains the account number of either the current Amazon Web Services account or another Amazon Web Services account. Some services, such as Cloud Map, create hosted zones using the current account. An OwningService element, which identifies the Amazon Web Services service that created and owns the hosted zone. For example, if a hosted zone was created by Amazon Elastic File System (Amazon EFS), the value of Owner is efs.amazonaws.com. When listing private hosted zones, the hosted zone and the Amazon VPC must belong to the same partition where the hosted zones were created. A partition is a group of Amazon Web Services Regions. Each Amazon Web Services account is scoped to one partition. The following are the supported partitions: aws - Amazon Web Services Regions aws-cn - China Regions aws-us-gov - Amazon Web Services GovCloud (US) Region For more information, see Access Management in the Amazon Web Services General Reference."]
module ListHostedZonesByVPCRequest =
  struct
    type nonrec t =
      {
      vPCId: VPCId.t
        [@ocaml.doc
          "The ID of the Amazon VPC that you want to list hosted zones for."];
      vPCRegion: VPCRegion.t
        [@ocaml.doc
          "For the Amazon VPC that you specified for VPCId, the Amazon Web Services Region that you created the VPC in."];
      maxItems: PageMaxItems.t option
        [@ocaml.doc
          "(Optional) The maximum number of hosted zones that you want Amazon Route 53 to return. If the specified VPC is associated with more than MaxItems hosted zones, the response includes a NextToken element. NextToken contains an encrypted token that identifies the first hosted zone that Route 53 will return if you submit another request."];
      nextToken: PaginationToken.t option
        [@ocaml.doc
          "If the previous response included a NextToken element, the specified VPC is associated with more hosted zones. To get more hosted zones, submit another ListHostedZonesByVPC request. For the value of NextToken, specify the value of NextToken from the previous response. If the previous response didn't include a NextToken element, there are no more hosted zones to get."]}
    let context_ = "ListHostedZonesByVPCRequest"
    let make ?maxItems =
      fun ?nextToken ->
        fun ~vPCId ->
          fun ~vPCRegion ->
            fun () -> { maxItems; nextToken; vPCId; vPCRegion }
    let to_value x =
      structure_to_value
        [("vpcid", (Some (VPCId.to_value x.vPCId)));
        ("vpcregion", (Some (VPCRegion.to_value x.vPCRegion)));
        ("maxitems", (Option.map x.maxItems ~f:PageMaxItems.to_value));
        ("nexttoken", (Option.map x.nextToken ~f:PaginationToken.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let nextToken =
        (Option.map ~f:PaginationToken.of_xml)
          (Xml.child xml_arg0 "nexttoken") in
      let maxItems =
        (Option.map ~f:PageMaxItems.of_xml) (Xml.child xml_arg0 "maxitems") in
      let vPCRegion =
        VPCRegion.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "vpcregion") in
      let vPCId =
        VPCId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "vpcid") in
      make ?nextToken ?maxItems ~vPCRegion ~vPCId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let nextToken = field_map json "NextToken" PaginationToken.of_json in
      let maxItems = field_map json "MaxItems" PageMaxItems.of_json in
      let vPCRegion = field_map_exn json "VPCRegion" VPCRegion.of_json in
      let vPCId = field_map_exn json "VPCId" VPCId.of_json in
      make ?nextToken ?maxItems ~vPCRegion ~vPCId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Lists all the private hosted zones that a specified VPC is associated with, regardless of which Amazon Web Services account created the hosted zones."]
module ListHostedZonesByNameResponse =
  struct
    type nonrec t =
      {
      hostedZones: HostedZones.t
        [@ocaml.doc
          "A complex type that contains general information about the hosted zone."];
      dNSName: DNSName.t option
        [@ocaml.doc
          "For the second and subsequent calls to ListHostedZonesByName, DNSName is the value that you specified for the dnsname parameter in the request that produced the current response."];
      hostedZoneId: ResourceId.t option
        [@ocaml.doc
          "The ID that Amazon Route 53 assigned to the hosted zone when you created it."];
      isTruncated: PageTruncated.t
        [@ocaml.doc
          "A flag that indicates whether there are more hosted zones to be listed. If the response was truncated, you can get the next group of maxitems hosted zones by calling ListHostedZonesByName again and specifying the values of NextDNSName and NextHostedZoneId elements in the dnsname and hostedzoneid parameters."];
      nextDNSName: DNSName.t option
        [@ocaml.doc
          "If IsTruncated is true, the value of NextDNSName is the name of the first hosted zone in the next group of maxitems hosted zones. Call ListHostedZonesByName again and specify the value of NextDNSName and NextHostedZoneId in the dnsname and hostedzoneid parameters, respectively. This element is present only if IsTruncated is true."];
      nextHostedZoneId: ResourceId.t option
        [@ocaml.doc
          "If IsTruncated is true, the value of NextHostedZoneId identifies the first hosted zone in the next group of maxitems hosted zones. Call ListHostedZonesByName again and specify the value of NextDNSName and NextHostedZoneId in the dnsname and hostedzoneid parameters, respectively. This element is present only if IsTruncated is true."];
      maxItems: PageMaxItems.t
        [@ocaml.doc
          "The value that you specified for the maxitems parameter in the call to ListHostedZonesByName that produced the current response."]}
    type nonrec error =
      [ `InvalidDomainName of InvalidDomainName.t 
      | `InvalidInput of InvalidInput.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListHostedZonesByNameResponse"
    let make ?dNSName =
      fun ?hostedZoneId ->
        fun ?nextDNSName ->
          fun ?nextHostedZoneId ->
            fun ~hostedZones ->
              fun ~isTruncated ->
                fun ~maxItems ->
                  fun () ->
                    {
                      dNSName;
                      hostedZoneId;
                      nextDNSName;
                      nextHostedZoneId;
                      hostedZones;
                      isTruncated;
                      maxItems
                    }
    let error_of_json name json =
      match name with
      | "InvalidDomainName" ->
          `InvalidDomainName (InvalidDomainName.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidDomainName" ->
          `InvalidDomainName (InvalidDomainName.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidDomainName e ->
          `Assoc
            [("error", (`String "InvalidDomainName"));
            ("details", (InvalidDomainName.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("HostedZones", (Some (HostedZones.to_value x.hostedZones)));
        ("DNSName", (Option.map x.dNSName ~f:DNSName.to_value));
        ("HostedZoneId", (Option.map x.hostedZoneId ~f:ResourceId.to_value));
        ("IsTruncated", (Some (PageTruncated.to_value x.isTruncated)));
        ("NextDNSName", (Option.map x.nextDNSName ~f:DNSName.to_value));
        ("NextHostedZoneId",
          (Option.map x.nextHostedZoneId ~f:ResourceId.to_value));
        ("MaxItems", (Some (PageMaxItems.to_value x.maxItems)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxItems =
        PageMaxItems.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MaxItems") in
      let nextHostedZoneId =
        (Option.map ~f:ResourceId.of_xml)
          (Xml.child xml_arg0 "NextHostedZoneId") in
      let nextDNSName =
        (Option.map ~f:DNSName.of_xml) (Xml.child xml_arg0 "NextDNSName") in
      let isTruncated =
        PageTruncated.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IsTruncated") in
      let hostedZoneId =
        (Option.map ~f:ResourceId.of_xml) (Xml.child xml_arg0 "HostedZoneId") in
      let dNSName =
        (Option.map ~f:DNSName.of_xml) (Xml.child xml_arg0 "DNSName") in
      let hostedZones =
        HostedZones.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HostedZones") in
      make ~maxItems ?nextHostedZoneId ?nextDNSName ~isTruncated
        ?hostedZoneId ?dNSName ~hostedZones ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxItems = field_map_exn json "MaxItems" PageMaxItems.of_json in
      let nextHostedZoneId =
        field_map json "NextHostedZoneId" ResourceId.of_json in
      let nextDNSName = field_map json "NextDNSName" DNSName.of_json in
      let isTruncated =
        field_map_exn json "IsTruncated" PageTruncated.of_json in
      let hostedZoneId = field_map json "HostedZoneId" ResourceId.of_json in
      let dNSName = field_map json "DNSName" DNSName.of_json in
      let hostedZones = field_map_exn json "HostedZones" HostedZones.of_json in
      make ~maxItems ?nextHostedZoneId ?nextDNSName ~isTruncated
        ?hostedZoneId ?dNSName ~hostedZones ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response information for the request."]
module ListHostedZonesByNameRequest =
  struct
    type nonrec t =
      {
      dNSName: DNSName.t option
        [@ocaml.doc
          "(Optional) For your first request to ListHostedZonesByName, include the dnsname parameter only if you want to specify the name of the first hosted zone in the response. If you don't include the dnsname parameter, Amazon Route 53 returns all of the hosted zones that were created by the current Amazon Web Services account, in ASCII order. For subsequent requests, include both dnsname and hostedzoneid parameters. For dnsname, specify the value of NextDNSName from the previous response."];
      hostedZoneId: ResourceId.t option
        [@ocaml.doc
          "(Optional) For your first request to ListHostedZonesByName, do not include the hostedzoneid parameter. If you have more hosted zones than the value of maxitems, ListHostedZonesByName returns only the first maxitems hosted zones. To get the next group of maxitems hosted zones, submit another request to ListHostedZonesByName and include both dnsname and hostedzoneid parameters. For the value of hostedzoneid, specify the value of the NextHostedZoneId element from the previous response."];
      maxItems: PageMaxItems.t option
        [@ocaml.doc
          "The maximum number of hosted zones to be included in the response body for this request. If you have more than maxitems hosted zones, then the value of the IsTruncated element in the response is true, and the values of NextDNSName and NextHostedZoneId specify the first hosted zone in the next group of maxitems hosted zones."]}
    let make ?dNSName =
      fun ?hostedZoneId ->
        fun ?maxItems -> fun () -> { dNSName; hostedZoneId; maxItems }
    let to_value x =
      structure_to_value
        [("dnsname", (Option.map x.dNSName ~f:DNSName.to_value));
        ("hostedzoneid", (Option.map x.hostedZoneId ~f:ResourceId.to_value));
        ("maxitems", (Option.map x.maxItems ~f:PageMaxItems.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxItems =
        (Option.map ~f:PageMaxItems.of_xml) (Xml.child xml_arg0 "maxitems") in
      let hostedZoneId =
        (Option.map ~f:ResourceId.of_xml) (Xml.child xml_arg0 "hostedzoneid") in
      let dNSName =
        (Option.map ~f:DNSName.of_xml) (Xml.child xml_arg0 "dnsname") in
      make ?maxItems ?hostedZoneId ?dNSName ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxItems = field_map json "MaxItems" PageMaxItems.of_json in
      let hostedZoneId = field_map json "HostedZoneId" ResourceId.of_json in
      let dNSName = field_map json "DNSName" DNSName.of_json in
      make ?maxItems ?hostedZoneId ?dNSName ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Retrieves a list of the public and private hosted zones that are associated with the current Amazon Web Services account in ASCII order by domain name."]
module ListHealthChecksResponse =
  struct
    type nonrec t =
      {
      healthChecks: HealthChecks.t
        [@ocaml.doc
          "A complex type that contains one HealthCheck element for each health check that is associated with the current Amazon Web Services account."];
      marker: PageMarker.t option
        [@ocaml.doc
          "For the second and subsequent calls to ListHealthChecks, Marker is the value that you specified for the marker parameter in the previous request."];
      isTruncated: PageTruncated.t
        [@ocaml.doc
          "A flag that indicates whether there are more health checks to be listed. If the response was truncated, you can get the next group of health checks by submitting another ListHealthChecks request and specifying the value of NextMarker in the marker parameter."];
      nextMarker: PageMarker.t option
        [@ocaml.doc
          "If IsTruncated is true, the value of NextMarker identifies the first health check that Amazon Route 53 returns if you submit another ListHealthChecks request and specify the value of NextMarker in the marker parameter."];
      maxItems: PageMaxItems.t
        [@ocaml.doc
          "The value that you specified for the maxitems parameter in the call to ListHealthChecks that produced the current response."]}
    type nonrec error =
      [ `IncompatibleVersion of IncompatibleVersion.t 
      | `InvalidInput of InvalidInput.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListHealthChecksResponse"
    let make ?marker =
      fun ?nextMarker ->
        fun ~healthChecks ->
          fun ~isTruncated ->
            fun ~maxItems ->
              fun () ->
                { marker; nextMarker; healthChecks; isTruncated; maxItems }
    let error_of_json name json =
      match name with
      | "IncompatibleVersion" ->
          `IncompatibleVersion (IncompatibleVersion.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "IncompatibleVersion" ->
          `IncompatibleVersion (IncompatibleVersion.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `IncompatibleVersion e ->
          `Assoc
            [("error", (`String "IncompatibleVersion"));
            ("details", (IncompatibleVersion.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("HealthChecks", (Some (HealthChecks.to_value x.healthChecks)));
        ("Marker", (Option.map x.marker ~f:PageMarker.to_value));
        ("IsTruncated", (Some (PageTruncated.to_value x.isTruncated)));
        ("NextMarker", (Option.map x.nextMarker ~f:PageMarker.to_value));
        ("MaxItems", (Some (PageMaxItems.to_value x.maxItems)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxItems =
        PageMaxItems.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MaxItems") in
      let nextMarker =
        (Option.map ~f:PageMarker.of_xml) (Xml.child xml_arg0 "NextMarker") in
      let isTruncated =
        PageTruncated.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IsTruncated") in
      let marker =
        (Option.map ~f:PageMarker.of_xml) (Xml.child xml_arg0 "Marker") in
      let healthChecks =
        HealthChecks.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HealthChecks") in
      make ~maxItems ?nextMarker ~isTruncated ?marker ~healthChecks ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxItems = field_map_exn json "MaxItems" PageMaxItems.of_json in
      let nextMarker = field_map json "NextMarker" PageMarker.of_json in
      let isTruncated =
        field_map_exn json "IsTruncated" PageTruncated.of_json in
      let marker = field_map json "Marker" PageMarker.of_json in
      let healthChecks =
        field_map_exn json "HealthChecks" HealthChecks.of_json in
      make ~maxItems ?nextMarker ~isTruncated ?marker ~healthChecks ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response to a ListHealthChecks request."]
module ListHealthChecksRequest =
  struct
    type nonrec t =
      {
      marker: PageMarker.t option
        [@ocaml.doc
          "If the value of IsTruncated in the previous response was true, you have more health checks. To get another group, submit another ListHealthChecks request. For the value of marker, specify the value of NextMarker from the previous response, which is the ID of the first health check that Amazon Route 53 will return if you submit another request. If the value of IsTruncated in the previous response was false, there are no more health checks to get."];
      maxItems: PageMaxItems.t option
        [@ocaml.doc
          "The maximum number of health checks that you want ListHealthChecks to return in response to the current request. Amazon Route 53 returns a maximum of 100 items. If you set MaxItems to a value greater than 100, Route 53 returns only the first 100 health checks."]}
    let make ?marker = fun ?maxItems -> fun () -> { marker; maxItems }
    let to_value x =
      structure_to_value
        [("marker", (Option.map x.marker ~f:PageMarker.to_value));
        ("maxitems", (Option.map x.maxItems ~f:PageMaxItems.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxItems =
        (Option.map ~f:PageMaxItems.of_xml) (Xml.child xml_arg0 "maxitems") in
      let marker =
        (Option.map ~f:PageMarker.of_xml) (Xml.child xml_arg0 "marker") in
      make ?maxItems ?marker ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxItems = field_map json "MaxItems" PageMaxItems.of_json in
      let marker = field_map json "Marker" PageMarker.of_json in
      make ?maxItems ?marker ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A request to retrieve a list of the health checks that are associated with the current Amazon Web Services account."]
module ListGeoLocationsResponse =
  struct
    type nonrec t =
      {
      geoLocationDetailsList: GeoLocationDetailsList.t
        [@ocaml.doc
          "A complex type that contains one GeoLocationDetails element for each location that Amazon Route 53 supports for geolocation."];
      isTruncated: PageTruncated.t
        [@ocaml.doc
          "A value that indicates whether more locations remain to be listed after the last location in this response. If so, the value of IsTruncated is true. To get more values, submit another request and include the values of NextContinentCode, NextCountryCode, and NextSubdivisionCode in the startcontinentcode, startcountrycode, and startsubdivisioncode, as applicable."];
      nextContinentCode: GeoLocationContinentCode.t option
        [@ocaml.doc
          "If IsTruncated is true, you can make a follow-up request to display more locations. Enter the value of NextContinentCode in the startcontinentcode parameter in another ListGeoLocations request."];
      nextCountryCode: GeoLocationCountryCode.t option
        [@ocaml.doc
          "If IsTruncated is true, you can make a follow-up request to display more locations. Enter the value of NextCountryCode in the startcountrycode parameter in another ListGeoLocations request."];
      nextSubdivisionCode: GeoLocationSubdivisionCode.t option
        [@ocaml.doc
          "If IsTruncated is true, you can make a follow-up request to display more locations. Enter the value of NextSubdivisionCode in the startsubdivisioncode parameter in another ListGeoLocations request."];
      maxItems: PageMaxItems.t
        [@ocaml.doc
          "The value that you specified for MaxItems in the request."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ListGeoLocationsResponse"
    let make ?nextContinentCode =
      fun ?nextCountryCode ->
        fun ?nextSubdivisionCode ->
          fun ~geoLocationDetailsList ->
            fun ~isTruncated ->
              fun ~maxItems ->
                fun () ->
                  {
                    nextContinentCode;
                    nextCountryCode;
                    nextSubdivisionCode;
                    geoLocationDetailsList;
                    isTruncated;
                    maxItems
                  }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GeoLocationDetailsList",
           (Some (GeoLocationDetailsList.to_value x.geoLocationDetailsList)));
        ("IsTruncated", (Some (PageTruncated.to_value x.isTruncated)));
        ("NextContinentCode",
          (Option.map x.nextContinentCode
             ~f:GeoLocationContinentCode.to_value));
        ("NextCountryCode",
          (Option.map x.nextCountryCode ~f:GeoLocationCountryCode.to_value));
        ("NextSubdivisionCode",
          (Option.map x.nextSubdivisionCode
             ~f:GeoLocationSubdivisionCode.to_value));
        ("MaxItems", (Some (PageMaxItems.to_value x.maxItems)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxItems =
        PageMaxItems.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "MaxItems") in
      let nextSubdivisionCode =
        (Option.map ~f:GeoLocationSubdivisionCode.of_xml)
          (Xml.child xml_arg0 "NextSubdivisionCode") in
      let nextCountryCode =
        (Option.map ~f:GeoLocationCountryCode.of_xml)
          (Xml.child xml_arg0 "NextCountryCode") in
      let nextContinentCode =
        (Option.map ~f:GeoLocationContinentCode.of_xml)
          (Xml.child xml_arg0 "NextContinentCode") in
      let isTruncated =
        PageTruncated.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "IsTruncated") in
      let geoLocationDetailsList =
        GeoLocationDetailsList.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GeoLocationDetailsList") in
      make ~maxItems ?nextSubdivisionCode ?nextCountryCode ?nextContinentCode
        ~isTruncated ~geoLocationDetailsList ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxItems = field_map_exn json "MaxItems" PageMaxItems.of_json in
      let nextSubdivisionCode =
        field_map json "NextSubdivisionCode"
          GeoLocationSubdivisionCode.of_json in
      let nextCountryCode =
        field_map json "NextCountryCode" GeoLocationCountryCode.of_json in
      let nextContinentCode =
        field_map json "NextContinentCode" GeoLocationContinentCode.of_json in
      let isTruncated =
        field_map_exn json "IsTruncated" PageTruncated.of_json in
      let geoLocationDetailsList =
        field_map_exn json "GeoLocationDetailsList"
          GeoLocationDetailsList.of_json in
      make ~maxItems ?nextSubdivisionCode ?nextCountryCode ?nextContinentCode
        ~isTruncated ~geoLocationDetailsList ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type containing the response information for the request."]
module ListGeoLocationsRequest =
  struct
    type nonrec t =
      {
      startContinentCode: GeoLocationContinentCode.t option
        [@ocaml.doc
          "The code for the continent with which you want to start listing locations that Amazon Route 53 supports for geolocation. If Route 53 has already returned a page or more of results, if IsTruncated is true, and if NextContinentCode from the previous response has a value, enter that value in startcontinentcode to return the next page of results. Include startcontinentcode only if you want to list continents. Don't include startcontinentcode when you're listing countries or countries with their subdivisions."];
      startCountryCode: GeoLocationCountryCode.t option
        [@ocaml.doc
          "The code for the country with which you want to start listing locations that Amazon Route 53 supports for geolocation. If Route 53 has already returned a page or more of results, if IsTruncated is true, and if NextCountryCode from the previous response has a value, enter that value in startcountrycode to return the next page of results."];
      startSubdivisionCode: GeoLocationSubdivisionCode.t option
        [@ocaml.doc
          "The code for the state of the United States with which you want to start listing locations that Amazon Route 53 supports for geolocation. If Route 53 has already returned a page or more of results, if IsTruncated is true, and if NextSubdivisionCode from the previous response has a value, enter that value in startsubdivisioncode to return the next page of results. To list subdivisions (U.S. states), you must include both startcountrycode and startsubdivisioncode."];
      maxItems: PageMaxItems.t option
        [@ocaml.doc
          "(Optional) The maximum number of geolocations to be included in the response body for this request. If more than maxitems geolocations remain to be listed, then the value of the IsTruncated element in the response is true."]}
    let make ?startContinentCode =
      fun ?startCountryCode ->
        fun ?startSubdivisionCode ->
          fun ?maxItems ->
            fun () ->
              {
                startContinentCode;
                startCountryCode;
                startSubdivisionCode;
                maxItems
              }
    let to_value x =
      structure_to_value
        [("startcontinentcode",
           (Option.map x.startContinentCode
              ~f:GeoLocationContinentCode.to_value));
        ("startcountrycode",
          (Option.map x.startCountryCode ~f:GeoLocationCountryCode.to_value));
        ("startsubdivisioncode",
          (Option.map x.startSubdivisionCode
             ~f:GeoLocationSubdivisionCode.to_value));
        ("maxitems", (Option.map x.maxItems ~f:PageMaxItems.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let maxItems =
        (Option.map ~f:PageMaxItems.of_xml) (Xml.child xml_arg0 "maxitems") in
      let startSubdivisionCode =
        (Option.map ~f:GeoLocationSubdivisionCode.of_xml)
          (Xml.child xml_arg0 "startsubdivisioncode") in
      let startCountryCode =
        (Option.map ~f:GeoLocationCountryCode.of_xml)
          (Xml.child xml_arg0 "startcountrycode") in
      let startContinentCode =
        (Option.map ~f:GeoLocationContinentCode.of_xml)
          (Xml.child xml_arg0 "startcontinentcode") in
      make ?maxItems ?startSubdivisionCode ?startCountryCode
        ?startContinentCode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let maxItems = field_map json "MaxItems" PageMaxItems.of_json in
      let startSubdivisionCode =
        field_map json "StartSubdivisionCode"
          GeoLocationSubdivisionCode.of_json in
      let startCountryCode =
        field_map json "StartCountryCode" GeoLocationCountryCode.of_json in
      let startContinentCode =
        field_map json "StartContinentCode" GeoLocationContinentCode.of_json in
      make ?maxItems ?startSubdivisionCode ?startCountryCode
        ?startContinentCode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A request to get a list of geographic locations that Amazon Route 53 supports for geolocation resource record sets."]
module GetTrafficPolicyResponse =
  struct
    type nonrec t =
      {
      trafficPolicy: TrafficPolicy.t
        [@ocaml.doc
          "A complex type that contains settings for the specified traffic policy."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `NoSuchTrafficPolicy of NoSuchTrafficPolicy.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetTrafficPolicyResponse"
    let make ~trafficPolicy = fun () -> { trafficPolicy }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchTrafficPolicy" ->
          `NoSuchTrafficPolicy (NoSuchTrafficPolicy.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchTrafficPolicy" ->
          `NoSuchTrafficPolicy (NoSuchTrafficPolicy.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchTrafficPolicy e ->
          `Assoc
            [("error", (`String "NoSuchTrafficPolicy"));
            ("details", (NoSuchTrafficPolicy.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("TrafficPolicy", (Some (TrafficPolicy.to_value x.trafficPolicy)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trafficPolicy =
        TrafficPolicy.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficPolicy") in
      make ~trafficPolicy ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let trafficPolicy =
        field_map_exn json "TrafficPolicy" TrafficPolicy.of_json in
      make ~trafficPolicy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response information for the request."]
module GetTrafficPolicyRequest =
  struct
    type nonrec t =
      {
      id: TrafficPolicyId.t
        [@ocaml.doc
          "The ID of the traffic policy that you want to get information about."];
      version: TrafficPolicyVersion.t
        [@ocaml.doc
          "The version number of the traffic policy that you want to get information about."]}
    let context_ = "GetTrafficPolicyRequest"
    let make ~id = fun ~version -> fun () -> { id; version }
    let to_value x =
      structure_to_value
        [("Id", (Some (TrafficPolicyId.to_value x.id)));
        ("Version", (Some (TrafficPolicyVersion.to_value x.version)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let version =
        TrafficPolicyVersion.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Version") in
      let id =
        TrafficPolicyId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ~version ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let version = field_map_exn json "Version" TrafficPolicyVersion.of_json in
      let id = field_map_exn json "Id" TrafficPolicyId.of_json in
      make ~version ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about a specific traffic policy version."]
module GetTrafficPolicyInstanceResponse =
  struct
    type nonrec t =
      {
      trafficPolicyInstance: TrafficPolicyInstance.t
        [@ocaml.doc
          "A complex type that contains settings for the traffic policy instance."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `NoSuchTrafficPolicyInstance of NoSuchTrafficPolicyInstance.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetTrafficPolicyInstanceResponse"
    let make ~trafficPolicyInstance = fun () -> { trafficPolicyInstance }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchTrafficPolicyInstance" ->
          `NoSuchTrafficPolicyInstance
            (NoSuchTrafficPolicyInstance.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchTrafficPolicyInstance" ->
          `NoSuchTrafficPolicyInstance
            (NoSuchTrafficPolicyInstance.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchTrafficPolicyInstance e ->
          `Assoc
            [("error", (`String "NoSuchTrafficPolicyInstance"));
            ("details", (NoSuchTrafficPolicyInstance.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("TrafficPolicyInstance",
           (Some (TrafficPolicyInstance.to_value x.trafficPolicyInstance)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trafficPolicyInstance =
        TrafficPolicyInstance.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficPolicyInstance") in
      make ~trafficPolicyInstance ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let trafficPolicyInstance =
        field_map_exn json "TrafficPolicyInstance"
          TrafficPolicyInstance.of_json in
      make ~trafficPolicyInstance ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about the resource record sets that Amazon Route 53 created based on a specified traffic policy."]
module GetTrafficPolicyInstanceRequest =
  struct
    type nonrec t =
      {
      id: TrafficPolicyInstanceId.t
        [@ocaml.doc
          "The ID of the traffic policy instance that you want to get information about."]}
    let context_ = "GetTrafficPolicyInstanceRequest"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value
        [("Id", (Some (TrafficPolicyInstanceId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        TrafficPolicyInstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let id = field_map_exn json "Id" TrafficPolicyInstanceId.of_json in
      make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about a specified traffic policy instance."]
module GetTrafficPolicyInstanceCountResponse =
  struct
    type nonrec t =
      {
      trafficPolicyInstanceCount: TrafficPolicyInstanceCount.t
        [@ocaml.doc
          "The number of traffic policy instances that are associated with the current Amazon Web Services account."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let context_ = "GetTrafficPolicyInstanceCountResponse"
    let make ~trafficPolicyInstanceCount =
      fun () -> { trafficPolicyInstanceCount }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("TrafficPolicyInstanceCount",
           (Some
              (TrafficPolicyInstanceCount.to_value
                 x.trafficPolicyInstanceCount)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trafficPolicyInstanceCount =
        TrafficPolicyInstanceCount.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "TrafficPolicyInstanceCount") in
      make ~trafficPolicyInstanceCount ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let trafficPolicyInstanceCount =
        field_map_exn json "TrafficPolicyInstanceCount"
          TrafficPolicyInstanceCount.of_json in
      make ~trafficPolicyInstanceCount ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about the resource record sets that Amazon Route 53 created based on a specified traffic policy."]
module GetTrafficPolicyInstanceCountRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Request to get the number of traffic policy instances that are associated with the current Amazon Web Services account."]
module GetReusableDelegationSetResponse =
  struct
    type nonrec t =
      {
      delegationSet: DelegationSet.t
        [@ocaml.doc
          "A complex type that contains information about the reusable delegation set."]}
    type nonrec error =
      [ `DelegationSetNotReusable of DelegationSetNotReusable.t 
      | `InvalidInput of InvalidInput.t 
      | `NoSuchDelegationSet of NoSuchDelegationSet.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetReusableDelegationSetResponse"
    let make ~delegationSet = fun () -> { delegationSet }
    let error_of_json name json =
      match name with
      | "DelegationSetNotReusable" ->
          `DelegationSetNotReusable (DelegationSetNotReusable.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchDelegationSet" ->
          `NoSuchDelegationSet (NoSuchDelegationSet.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "DelegationSetNotReusable" ->
          `DelegationSetNotReusable (DelegationSetNotReusable.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchDelegationSet" ->
          `NoSuchDelegationSet (NoSuchDelegationSet.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `DelegationSetNotReusable e ->
          `Assoc
            [("error", (`String "DelegationSetNotReusable"));
            ("details", (DelegationSetNotReusable.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchDelegationSet e ->
          `Assoc
            [("error", (`String "NoSuchDelegationSet"));
            ("details", (NoSuchDelegationSet.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("DelegationSet", (Some (DelegationSet.to_value x.delegationSet)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let delegationSet =
        DelegationSet.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DelegationSet") in
      make ~delegationSet ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let delegationSet =
        field_map_exn json "DelegationSet" DelegationSet.of_json in
      make ~delegationSet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response to the GetReusableDelegationSet request."]
module GetReusableDelegationSetRequest =
  struct
    type nonrec t =
      {
      id: ResourceId.t
        [@ocaml.doc
          "The ID of the reusable delegation set that you want to get a list of name servers for."]}
    let context_ = "GetReusableDelegationSetRequest"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value [("Id", (Some (ResourceId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        ResourceId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let id = field_map_exn json "Id" ResourceId.of_json in make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A request to get information about a specified reusable delegation set."]
module GetReusableDelegationSetLimitResponse =
  struct
    type nonrec t =
      {
      limit: ReusableDelegationSetLimit.t
        [@ocaml.doc
          "The current setting for the limit on hosted zones that you can associate with the specified reusable delegation set."];
      count: UsageCount.t
        [@ocaml.doc
          "The current number of hosted zones that you can associate with the specified reusable delegation set."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `NoSuchDelegationSet of NoSuchDelegationSet.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetReusableDelegationSetLimitResponse"
    let make ~limit = fun ~count -> fun () -> { limit; count }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchDelegationSet" ->
          `NoSuchDelegationSet (NoSuchDelegationSet.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchDelegationSet" ->
          `NoSuchDelegationSet (NoSuchDelegationSet.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchDelegationSet e ->
          `Assoc
            [("error", (`String "NoSuchDelegationSet"));
            ("details", (NoSuchDelegationSet.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Limit", (Some (ReusableDelegationSetLimit.to_value x.limit)));
        ("Count", (Some (UsageCount.to_value x.count)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let count =
        UsageCount.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Count") in
      let limit =
        ReusableDelegationSetLimit.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Limit") in
      make ~count ~limit ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let count = field_map_exn json "Count" UsageCount.of_json in
      let limit =
        field_map_exn json "Limit" ReusableDelegationSetLimit.of_json in
      make ~count ~limit ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A complex type that contains the requested limit."]
module GetReusableDelegationSetLimitRequest =
  struct
    type nonrec t =
      {
      type_: ReusableDelegationSetLimitType.t
        [@ocaml.doc
          "Specify MAX_ZONES_BY_REUSABLE_DELEGATION_SET to get the maximum number of hosted zones that you can associate with the specified reusable delegation set."];
      delegationSetId: ResourceId.t
        [@ocaml.doc
          "The ID of the delegation set that you want to get the limit for."]}
    let context_ = "GetReusableDelegationSetLimitRequest"
    let make ~type_ =
      fun ~delegationSetId -> fun () -> { type_; delegationSetId }
    let to_value x =
      structure_to_value
        [("Type", (Some (ReusableDelegationSetLimitType.to_value x.type_)));
        ("Id", (Some (ResourceId.to_value x.delegationSetId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let delegationSetId =
        ResourceId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      let type_ =
        ReusableDelegationSetLimitType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Type") in
      make ~delegationSetId ~type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let delegationSetId =
        field_map_exn json "DelegationSetId" ResourceId.of_json in
      let type_ =
        field_map_exn json "Type" ReusableDelegationSetLimitType.of_json in
      make ~delegationSetId ~type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about the request to create a hosted zone."]
module GetQueryLoggingConfigResponse =
  struct
    type nonrec t =
      {
      queryLoggingConfig: QueryLoggingConfig.t
        [@ocaml.doc
          "A complex type that contains information about the query logging configuration that you specified in a GetQueryLoggingConfig request."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `NoSuchQueryLoggingConfig of NoSuchQueryLoggingConfig.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetQueryLoggingConfigResponse"
    let make ~queryLoggingConfig = fun () -> { queryLoggingConfig }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchQueryLoggingConfig" ->
          `NoSuchQueryLoggingConfig (NoSuchQueryLoggingConfig.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchQueryLoggingConfig" ->
          `NoSuchQueryLoggingConfig (NoSuchQueryLoggingConfig.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchQueryLoggingConfig e ->
          `Assoc
            [("error", (`String "NoSuchQueryLoggingConfig"));
            ("details", (NoSuchQueryLoggingConfig.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("QueryLoggingConfig",
           (Some (QueryLoggingConfig.to_value x.queryLoggingConfig)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let queryLoggingConfig =
        QueryLoggingConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "QueryLoggingConfig") in
      make ~queryLoggingConfig ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let queryLoggingConfig =
        field_map_exn json "QueryLoggingConfig" QueryLoggingConfig.of_json in
      make ~queryLoggingConfig ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about a specified configuration for DNS query logging. For more information about DNS query logs, see CreateQueryLoggingConfig and Logging DNS Queries."]
module GetQueryLoggingConfigRequest =
  struct
    type nonrec t =
      {
      id: QueryLoggingConfigId.t
        [@ocaml.doc
          "The ID of the configuration for DNS query logging that you want to get information about."]}
    let context_ = "GetQueryLoggingConfigRequest"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value
        [("Id", (Some (QueryLoggingConfigId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        QueryLoggingConfigId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let id = field_map_exn json "Id" QueryLoggingConfigId.of_json in
      make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Gets information about a specified configuration for DNS query logging. For more information about DNS query logs, see CreateQueryLoggingConfig and Logging DNS Queries."]
module GetHostedZoneResponse =
  struct
    type nonrec t =
      {
      hostedZone: HostedZone.t
        [@ocaml.doc
          "A complex type that contains general information about the specified hosted zone."];
      delegationSet: DelegationSet.t option
        [@ocaml.doc
          "A complex type that lists the Amazon Route 53 name servers for the specified hosted zone."];
      vPCs: VPCs.t option
        [@ocaml.doc
          "A complex type that contains information about the VPCs that are associated with the specified hosted zone."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `NoSuchHostedZone of NoSuchHostedZone.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetHostedZoneResponse"
    let make ?delegationSet =
      fun ?vPCs ->
        fun ~hostedZone -> fun () -> { delegationSet; vPCs; hostedZone }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchHostedZone" ->
          `NoSuchHostedZone (NoSuchHostedZone.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchHostedZone" -> `NoSuchHostedZone (NoSuchHostedZone.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchHostedZone e ->
          `Assoc
            [("error", (`String "NoSuchHostedZone"));
            ("details", (NoSuchHostedZone.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("HostedZone", (Some (HostedZone.to_value x.hostedZone)));
        ("DelegationSet",
          (Option.map x.delegationSet ~f:DelegationSet.to_value));
        ("VPCs", (Option.map x.vPCs ~f:VPCs.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vPCs = (Option.map ~f:VPCs.of_xml) (Xml.child xml_arg0 "VPCs") in
      let delegationSet =
        (Option.map ~f:DelegationSet.of_xml)
          (Xml.child xml_arg0 "DelegationSet") in
      let hostedZone =
        HostedZone.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HostedZone") in
      make ?vPCs ?delegationSet ~hostedZone ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let vPCs = field_map json "VPCs" VPCs.of_json in
      let delegationSet =
        field_map json "DelegationSet" DelegationSet.of_json in
      let hostedZone = field_map_exn json "HostedZone" HostedZone.of_json in
      make ?vPCs ?delegationSet ~hostedZone ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contain the response to a GetHostedZone request."]
module GetHostedZoneRequest =
  struct
    type nonrec t =
      {
      id: ResourceId.t
        [@ocaml.doc
          "The ID of the hosted zone that you want to get information about."]}
    let context_ = "GetHostedZoneRequest"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value [("Id", (Some (ResourceId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        ResourceId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let id = field_map_exn json "Id" ResourceId.of_json in make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A request to get information about a specified hosted zone."]
module GetHostedZoneLimitResponse =
  struct
    type nonrec t =
      {
      limit: HostedZoneLimit.t
        [@ocaml.doc
          "The current setting for the specified limit. For example, if you specified MAX_RRSETS_BY_ZONE for the value of Type in the request, the value of Limit is the maximum number of records that you can create in the specified hosted zone."];
      count: UsageCount.t
        [@ocaml.doc
          "The current number of entities that you have created of the specified type. For example, if you specified MAX_RRSETS_BY_ZONE for the value of Type in the request, the value of Count is the current number of records that you have created in the specified hosted zone."]}
    type nonrec error =
      [ `HostedZoneNotPrivate of HostedZoneNotPrivate.t 
      | `InvalidInput of InvalidInput.t 
      | `NoSuchHostedZone of NoSuchHostedZone.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetHostedZoneLimitResponse"
    let make ~limit = fun ~count -> fun () -> { limit; count }
    let error_of_json name json =
      match name with
      | "HostedZoneNotPrivate" ->
          `HostedZoneNotPrivate (HostedZoneNotPrivate.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchHostedZone" ->
          `NoSuchHostedZone (NoSuchHostedZone.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "HostedZoneNotPrivate" ->
          `HostedZoneNotPrivate (HostedZoneNotPrivate.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchHostedZone" -> `NoSuchHostedZone (NoSuchHostedZone.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `HostedZoneNotPrivate e ->
          `Assoc
            [("error", (`String "HostedZoneNotPrivate"));
            ("details", (HostedZoneNotPrivate.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchHostedZone e ->
          `Assoc
            [("error", (`String "NoSuchHostedZone"));
            ("details", (NoSuchHostedZone.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Limit", (Some (HostedZoneLimit.to_value x.limit)));
        ("Count", (Some (UsageCount.to_value x.count)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let count =
        UsageCount.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Count") in
      let limit =
        HostedZoneLimit.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Limit") in
      make ~count ~limit ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let count = field_map_exn json "Count" UsageCount.of_json in
      let limit = field_map_exn json "Limit" HostedZoneLimit.of_json in
      make ~count ~limit ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A complex type that contains the requested limit."]
module GetHostedZoneLimitRequest =
  struct
    type nonrec t =
      {
      type_: HostedZoneLimitType.t
        [@ocaml.doc
          "The limit that you want to get. Valid values include the following: MAX_RRSETS_BY_ZONE: The maximum number of records that you can create in the specified hosted zone. MAX_VPCS_ASSOCIATED_BY_ZONE: The maximum number of Amazon VPCs that you can associate with the specified private hosted zone."];
      hostedZoneId: ResourceId.t
        [@ocaml.doc
          "The ID of the hosted zone that you want to get a limit for."]}
    let context_ = "GetHostedZoneLimitRequest"
    let make ~type_ = fun ~hostedZoneId -> fun () -> { type_; hostedZoneId }
    let to_value x =
      structure_to_value
        [("Type", (Some (HostedZoneLimitType.to_value x.type_)));
        ("Id", (Some (ResourceId.to_value x.hostedZoneId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let hostedZoneId =
        ResourceId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      let type_ =
        HostedZoneLimitType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Type") in
      make ~hostedZoneId ~type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let hostedZoneId = field_map_exn json "HostedZoneId" ResourceId.of_json in
      let type_ = field_map_exn json "Type" HostedZoneLimitType.of_json in
      make ~hostedZoneId ~type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about the request to create a hosted zone."]
module GetHostedZoneCountResponse =
  struct
    type nonrec t =
      {
      hostedZoneCount: HostedZoneCount.t
        [@ocaml.doc
          "The total number of public and private hosted zones that are associated with the current Amazon Web Services account."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetHostedZoneCountResponse"
    let make ~hostedZoneCount = fun () -> { hostedZoneCount }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("HostedZoneCount",
           (Some (HostedZoneCount.to_value x.hostedZoneCount)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let hostedZoneCount =
        HostedZoneCount.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HostedZoneCount") in
      make ~hostedZoneCount ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let hostedZoneCount =
        field_map_exn json "HostedZoneCount" HostedZoneCount.of_json in
      make ~hostedZoneCount ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response to a GetHostedZoneCount request."]
module GetHostedZoneCountRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A request to retrieve a count of all the hosted zones that are associated with the current Amazon Web Services account."]
module GetHealthCheckStatusResponse =
  struct
    type nonrec t =
      {
      healthCheckObservations: HealthCheckObservations.t
        [@ocaml.doc
          "A list that contains one HealthCheckObservation element for each Amazon Route 53 health checker that is reporting a status about the health check endpoint."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `NoSuchHealthCheck of NoSuchHealthCheck.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetHealthCheckStatusResponse"
    let make ~healthCheckObservations = fun () -> { healthCheckObservations }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchHealthCheck" ->
          `NoSuchHealthCheck (NoSuchHealthCheck.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchHealthCheck" ->
          `NoSuchHealthCheck (NoSuchHealthCheck.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchHealthCheck e ->
          `Assoc
            [("error", (`String "NoSuchHealthCheck"));
            ("details", (NoSuchHealthCheck.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("HealthCheckObservations",
           (Some (HealthCheckObservations.to_value x.healthCheckObservations)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let healthCheckObservations =
        HealthCheckObservations.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HealthCheckObservations") in
      make ~healthCheckObservations ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let healthCheckObservations =
        field_map_exn json "HealthCheckObservations"
          HealthCheckObservations.of_json in
      make ~healthCheckObservations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response to a GetHealthCheck request."]
module GetHealthCheckStatusRequest =
  struct
    type nonrec t =
      {
      healthCheckId: HealthCheckId.t
        [@ocaml.doc
          "The ID for the health check that you want the current status for. When you created the health check, CreateHealthCheck returned the ID in the response, in the HealthCheckId element. If you want to check the status of a calculated health check, you must use the Amazon Route 53 console or the CloudWatch console. You can't use GetHealthCheckStatus to get the status of a calculated health check."]}
    let context_ = "GetHealthCheckStatusRequest"
    let make ~healthCheckId = fun () -> { healthCheckId }
    let to_value x =
      structure_to_value
        [("HealthCheckId", (Some (HealthCheckId.to_value x.healthCheckId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let healthCheckId =
        HealthCheckId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HealthCheckId") in
      make ~healthCheckId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let healthCheckId =
        field_map_exn json "HealthCheckId" HealthCheckId.of_json in
      make ~healthCheckId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A request to get the status for a health check."]
module GetHealthCheckResponse =
  struct
    type nonrec t =
      {
      healthCheck: HealthCheck.t
        [@ocaml.doc
          "A complex type that contains information about one health check that is associated with the current Amazon Web Services account."]}
    type nonrec error =
      [ `IncompatibleVersion of IncompatibleVersion.t 
      | `InvalidInput of InvalidInput.t 
      | `NoSuchHealthCheck of NoSuchHealthCheck.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetHealthCheckResponse"
    let make ~healthCheck = fun () -> { healthCheck }
    let error_of_json name json =
      match name with
      | "IncompatibleVersion" ->
          `IncompatibleVersion (IncompatibleVersion.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchHealthCheck" ->
          `NoSuchHealthCheck (NoSuchHealthCheck.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "IncompatibleVersion" ->
          `IncompatibleVersion (IncompatibleVersion.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchHealthCheck" ->
          `NoSuchHealthCheck (NoSuchHealthCheck.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `IncompatibleVersion e ->
          `Assoc
            [("error", (`String "IncompatibleVersion"));
            ("details", (IncompatibleVersion.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchHealthCheck e ->
          `Assoc
            [("error", (`String "NoSuchHealthCheck"));
            ("details", (NoSuchHealthCheck.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("HealthCheck", (Some (HealthCheck.to_value x.healthCheck)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let healthCheck =
        HealthCheck.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HealthCheck") in
      make ~healthCheck ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let healthCheck = field_map_exn json "HealthCheck" HealthCheck.of_json in
      make ~healthCheck ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response to a GetHealthCheck request."]
module GetHealthCheckRequest =
  struct
    type nonrec t =
      {
      healthCheckId: HealthCheckId.t
        [@ocaml.doc
          "The identifier that Amazon Route 53 assigned to the health check when you created it. When you add or update a resource record set, you use this value to specify which health check to use. The value can be up to 64 characters long."]}
    let context_ = "GetHealthCheckRequest"
    let make ~healthCheckId = fun () -> { healthCheckId }
    let to_value x =
      structure_to_value
        [("HealthCheckId", (Some (HealthCheckId.to_value x.healthCheckId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let healthCheckId =
        HealthCheckId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HealthCheckId") in
      make ~healthCheckId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let healthCheckId =
        field_map_exn json "HealthCheckId" HealthCheckId.of_json in
      make ~healthCheckId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A request to get information about a specified health check."]
module GetHealthCheckLastFailureReasonResponse =
  struct
    type nonrec t =
      {
      healthCheckObservations: HealthCheckObservations.t
        [@ocaml.doc
          "A list that contains one Observation element for each Amazon Route 53 health checker that is reporting a last failure reason."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `NoSuchHealthCheck of NoSuchHealthCheck.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetHealthCheckLastFailureReasonResponse"
    let make ~healthCheckObservations = fun () -> { healthCheckObservations }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchHealthCheck" ->
          `NoSuchHealthCheck (NoSuchHealthCheck.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchHealthCheck" ->
          `NoSuchHealthCheck (NoSuchHealthCheck.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchHealthCheck e ->
          `Assoc
            [("error", (`String "NoSuchHealthCheck"));
            ("details", (NoSuchHealthCheck.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("HealthCheckObservations",
           (Some (HealthCheckObservations.to_value x.healthCheckObservations)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let healthCheckObservations =
        HealthCheckObservations.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HealthCheckObservations") in
      make ~healthCheckObservations ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let healthCheckObservations =
        field_map_exn json "HealthCheckObservations"
          HealthCheckObservations.of_json in
      make ~healthCheckObservations ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response to a GetHealthCheckLastFailureReason request."]
module GetHealthCheckLastFailureReasonRequest =
  struct
    type nonrec t =
      {
      healthCheckId: HealthCheckId.t
        [@ocaml.doc
          "The ID for the health check for which you want the last failure reason. When you created the health check, CreateHealthCheck returned the ID in the response, in the HealthCheckId element. If you want to get the last failure reason for a calculated health check, you must use the Amazon Route 53 console or the CloudWatch console. You can't use GetHealthCheckLastFailureReason for a calculated health check."]}
    let context_ = "GetHealthCheckLastFailureReasonRequest"
    let make ~healthCheckId = fun () -> { healthCheckId }
    let to_value x =
      structure_to_value
        [("HealthCheckId", (Some (HealthCheckId.to_value x.healthCheckId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let healthCheckId =
        HealthCheckId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HealthCheckId") in
      make ~healthCheckId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let healthCheckId =
        field_map_exn json "HealthCheckId" HealthCheckId.of_json in
      make ~healthCheckId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A request for the reason that a health check failed most recently."]
module GetHealthCheckCountResponse =
  struct
    type nonrec t =
      {
      healthCheckCount: HealthCheckCount.t
        [@ocaml.doc
          "The number of health checks associated with the current Amazon Web Services account."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let context_ = "GetHealthCheckCountResponse"
    let make ~healthCheckCount = fun () -> { healthCheckCount }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("HealthCheckCount",
           (Some (HealthCheckCount.to_value x.healthCheckCount)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let healthCheckCount =
        HealthCheckCount.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HealthCheckCount") in
      make ~healthCheckCount ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let healthCheckCount =
        field_map_exn json "HealthCheckCount" HealthCheckCount.of_json in
      make ~healthCheckCount ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response to a GetHealthCheckCount request."]
module GetHealthCheckCountRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A request for the number of health checks that are associated with the current Amazon Web Services account."]
module GetGeoLocationResponse =
  struct
    type nonrec t =
      {
      geoLocationDetails: GeoLocationDetails.t
        [@ocaml.doc
          "A complex type that contains the codes and full continent, country, and subdivision names for the specified geolocation code."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `NoSuchGeoLocation of NoSuchGeoLocation.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetGeoLocationResponse"
    let make ~geoLocationDetails = fun () -> { geoLocationDetails }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchGeoLocation" ->
          `NoSuchGeoLocation (NoSuchGeoLocation.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchGeoLocation" ->
          `NoSuchGeoLocation (NoSuchGeoLocation.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchGeoLocation e ->
          `Assoc
            [("error", (`String "NoSuchGeoLocation"));
            ("details", (NoSuchGeoLocation.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("GeoLocationDetails",
           (Some (GeoLocationDetails.to_value x.geoLocationDetails)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let geoLocationDetails =
        GeoLocationDetails.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "GeoLocationDetails") in
      make ~geoLocationDetails ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let geoLocationDetails =
        field_map_exn json "GeoLocationDetails" GeoLocationDetails.of_json in
      make ~geoLocationDetails ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response information for the specified geolocation code."]
module GetGeoLocationRequest =
  struct
    type nonrec t =
      {
      continentCode: GeoLocationContinentCode.t option
        [@ocaml.doc
          "For geolocation resource record sets, a two-letter abbreviation that identifies a continent. Amazon Route 53 supports the following continent codes: AF: Africa AN: Antarctica AS: Asia EU: Europe OC: Oceania NA: North America SA: South America"];
      countryCode: GeoLocationCountryCode.t option
        [@ocaml.doc
          "Amazon Route 53 uses the two-letter country codes that are specified in ISO standard 3166-1 alpha-2."];
      subdivisionCode: GeoLocationSubdivisionCode.t option
        [@ocaml.doc
          "The code for the subdivision, such as a particular state within the United States. For a list of US state abbreviations, see Appendix B: Two\226\128\147Letter State and Possession Abbreviations on the United States Postal Service website. For a list of all supported subdivision codes, use the ListGeoLocations API."]}
    let make ?continentCode =
      fun ?countryCode ->
        fun ?subdivisionCode ->
          fun () -> { continentCode; countryCode; subdivisionCode }
    let to_value x =
      structure_to_value
        [("continentcode",
           (Option.map x.continentCode ~f:GeoLocationContinentCode.to_value));
        ("countrycode",
          (Option.map x.countryCode ~f:GeoLocationCountryCode.to_value));
        ("subdivisioncode",
          (Option.map x.subdivisionCode
             ~f:GeoLocationSubdivisionCode.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let subdivisionCode =
        (Option.map ~f:GeoLocationSubdivisionCode.of_xml)
          (Xml.child xml_arg0 "subdivisioncode") in
      let countryCode =
        (Option.map ~f:GeoLocationCountryCode.of_xml)
          (Xml.child xml_arg0 "countrycode") in
      let continentCode =
        (Option.map ~f:GeoLocationContinentCode.of_xml)
          (Xml.child xml_arg0 "continentcode") in
      make ?subdivisionCode ?countryCode ?continentCode ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let subdivisionCode =
        field_map json "SubdivisionCode" GeoLocationSubdivisionCode.of_json in
      let countryCode =
        field_map json "CountryCode" GeoLocationCountryCode.of_json in
      let continentCode =
        field_map json "ContinentCode" GeoLocationContinentCode.of_json in
      make ?subdivisionCode ?countryCode ?continentCode ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A request for information about whether a specified geographic location is supported for Amazon Route 53 geolocation resource record sets."]
module GetDNSSECResponse =
  struct
    type nonrec t =
      {
      status: DNSSECStatus.t
        [@ocaml.doc "A string repesenting the status of DNSSEC."];
      keySigningKeys: KeySigningKeys.t
        [@ocaml.doc "The key-signing keys (KSKs) in your account."]}
    type nonrec error =
      [ `InvalidArgument of InvalidArgument.t 
      | `InvalidInput of InvalidInput.t 
      | `NoSuchHostedZone of NoSuchHostedZone.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetDNSSECResponse"
    let make ~status =
      fun ~keySigningKeys -> fun () -> { status; keySigningKeys }
    let error_of_json name json =
      match name with
      | "InvalidArgument" -> `InvalidArgument (InvalidArgument.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchHostedZone" ->
          `NoSuchHostedZone (NoSuchHostedZone.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidArgument" -> `InvalidArgument (InvalidArgument.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchHostedZone" -> `NoSuchHostedZone (NoSuchHostedZone.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidArgument e ->
          `Assoc
            [("error", (`String "InvalidArgument"));
            ("details", (InvalidArgument.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchHostedZone e ->
          `Assoc
            [("error", (`String "NoSuchHostedZone"));
            ("details", (NoSuchHostedZone.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Status", (Some (DNSSECStatus.to_value x.status)));
        ("KeySigningKeys", (Some (KeySigningKeys.to_value x.keySigningKeys)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let keySigningKeys =
        KeySigningKeys.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "KeySigningKeys") in
      let status =
        DNSSECStatus.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Status") in
      make ~keySigningKeys ~status ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let keySigningKeys =
        field_map_exn json "KeySigningKeys" KeySigningKeys.of_json in
      let status = field_map_exn json "Status" DNSSECStatus.of_json in
      make ~keySigningKeys ~status ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about DNSSEC for a specific hosted zone, including the key-signing keys (KSKs) in the hosted zone."]
module GetDNSSECRequest =
  struct
    type nonrec t =
      {
      hostedZoneId: ResourceId.t
        [@ocaml.doc "A unique string used to identify a hosted zone."]}
    let context_ = "GetDNSSECRequest"
    let make ~hostedZoneId = fun () -> { hostedZoneId }
    let to_value x =
      structure_to_value
        [("Id", (Some (ResourceId.to_value x.hostedZoneId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let hostedZoneId =
        ResourceId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ~hostedZoneId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let hostedZoneId = field_map_exn json "HostedZoneId" ResourceId.of_json in
      make ~hostedZoneId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Returns information about DNSSEC for a specific hosted zone, including the key-signing keys (KSKs) in the hosted zone."]
module GetCheckerIpRangesResponse =
  struct
    type nonrec t =
      {
      checkerIpRanges: CheckerIpRanges.t
        [@ocaml.doc
          "A complex type that contains sorted list of IP ranges in CIDR format for Amazon Route 53 health checkers."]}
    type nonrec error =
      [ `Unknown_operation_error of (string * string option) ]
    let context_ = "GetCheckerIpRangesResponse"
    let make ~checkerIpRanges = fun () -> { checkerIpRanges }
    let error_of_json name json =
      match name with
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("CheckerIpRanges",
           (Some (CheckerIpRanges.to_value x.checkerIpRanges)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let checkerIpRanges =
        CheckerIpRanges.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CheckerIpRanges") in
      make ~checkerIpRanges ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let checkerIpRanges =
        field_map_exn json "CheckerIpRanges" CheckerIpRanges.of_json in
      make ~checkerIpRanges ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the CheckerIpRanges element."]
module GetCheckerIpRangesRequest =
  struct
    type nonrec t = unit
    let make () = ()
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Empty request."]
module GetChangeResponse =
  struct
    type nonrec t =
      {
      changeInfo: ChangeInfo.t
        [@ocaml.doc
          "A complex type that contains information about the specified change batch."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t  | `NoSuchChange of NoSuchChange.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetChangeResponse"
    let make ~changeInfo = fun () -> { changeInfo }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchChange" -> `NoSuchChange (NoSuchChange.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchChange" -> `NoSuchChange (NoSuchChange.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchChange e ->
          `Assoc
            [("error", (`String "NoSuchChange"));
            ("details", (NoSuchChange.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ChangeInfo", (Some (ChangeInfo.to_value x.changeInfo)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let changeInfo =
        ChangeInfo.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChangeInfo") in
      make ~changeInfo ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let changeInfo = field_map_exn json "ChangeInfo" ChangeInfo.of_json in
      make ~changeInfo ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A complex type that contains the ChangeInfo element."]
module GetChangeRequest =
  struct
    type nonrec t =
      {
      id: ResourceId.t
        [@ocaml.doc
          "The ID of the change batch request. The value that you specify here is the value that ChangeResourceRecordSets returned in the Id element when you submitted the request."]}
    let context_ = "GetChangeRequest"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value [("Id", (Some (ResourceId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        ResourceId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let id = field_map_exn json "Id" ResourceId.of_json in make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "The input for a GetChange request."]
module GetAccountLimitResponse =
  struct
    type nonrec t =
      {
      limit: AccountLimit.t
        [@ocaml.doc
          "The current setting for the specified limit. For example, if you specified MAX_HEALTH_CHECKS_BY_OWNER for the value of Type in the request, the value of Limit is the maximum number of health checks that you can create using the current account."];
      count: UsageCount.t
        [@ocaml.doc
          "The current number of entities that you have created of the specified type. For example, if you specified MAX_HEALTH_CHECKS_BY_OWNER for the value of Type in the request, the value of Count is the current number of health checks that you have created using the current account."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "GetAccountLimitResponse"
    let make ~limit = fun ~count -> fun () -> { limit; count }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("Limit", (Some (AccountLimit.to_value x.limit)));
        ("Count", (Some (UsageCount.to_value x.count)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let count =
        UsageCount.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Count") in
      let limit =
        AccountLimit.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Limit") in
      make ~count ~limit ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let count = field_map_exn json "Count" UsageCount.of_json in
      let limit = field_map_exn json "Limit" AccountLimit.of_json in
      make ~count ~limit ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A complex type that contains the requested limit."]
module GetAccountLimitRequest =
  struct
    type nonrec t =
      {
      type_: AccountLimitType.t
        [@ocaml.doc
          "The limit that you want to get. Valid values include the following: MAX_HEALTH_CHECKS_BY_OWNER: The maximum number of health checks that you can create using the current account. MAX_HOSTED_ZONES_BY_OWNER: The maximum number of hosted zones that you can create using the current account. MAX_REUSABLE_DELEGATION_SETS_BY_OWNER: The maximum number of reusable delegation sets that you can create using the current account. MAX_TRAFFIC_POLICIES_BY_OWNER: The maximum number of traffic policies that you can create using the current account. MAX_TRAFFIC_POLICY_INSTANCES_BY_OWNER: The maximum number of traffic policy instances that you can create using the current account. (Traffic policy instances are referred to as traffic flow policy records in the Amazon Route 53 console.)"]}
    let context_ = "GetAccountLimitRequest"
    let make ~type_ = fun () -> { type_ }
    let to_value x =
      structure_to_value
        [("Type", (Some (AccountLimitType.to_value x.type_)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let type_ =
        AccountLimitType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Type") in
      make ~type_ ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let type_ = field_map_exn json "Type" AccountLimitType.of_json in
      make ~type_ ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about the request to create a hosted zone."]
module EnableHostedZoneDNSSECResponse =
  struct
    type nonrec t = {
      changeInfo: ChangeInfo.t }
    type nonrec error =
      [ `ConcurrentModification of ConcurrentModification.t 
      | `DNSSECNotFound of DNSSECNotFound.t 
      | `HostedZonePartiallyDelegated of HostedZonePartiallyDelegated.t 
      | `InvalidArgument of InvalidArgument.t 
      | `InvalidInput of InvalidInput.t  | `InvalidKMSArn of InvalidKMSArn.t 
      | `InvalidKeySigningKeyStatus of InvalidKeySigningKeyStatus.t 
      | `KeySigningKeyWithActiveStatusNotFound of
          KeySigningKeyWithActiveStatusNotFound.t 
      | `NoSuchHostedZone of NoSuchHostedZone.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "EnableHostedZoneDNSSECResponse"
    let make ~changeInfo = fun () -> { changeInfo }
    let error_of_json name json =
      match name with
      | "ConcurrentModification" ->
          `ConcurrentModification (ConcurrentModification.of_json json)
      | "DNSSECNotFound" -> `DNSSECNotFound (DNSSECNotFound.of_json json)
      | "HostedZonePartiallyDelegated" ->
          `HostedZonePartiallyDelegated
            (HostedZonePartiallyDelegated.of_json json)
      | "InvalidArgument" -> `InvalidArgument (InvalidArgument.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "InvalidKMSArn" -> `InvalidKMSArn (InvalidKMSArn.of_json json)
      | "InvalidKeySigningKeyStatus" ->
          `InvalidKeySigningKeyStatus
            (InvalidKeySigningKeyStatus.of_json json)
      | "KeySigningKeyWithActiveStatusNotFound" ->
          `KeySigningKeyWithActiveStatusNotFound
            (KeySigningKeyWithActiveStatusNotFound.of_json json)
      | "NoSuchHostedZone" ->
          `NoSuchHostedZone (NoSuchHostedZone.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConcurrentModification" ->
          `ConcurrentModification (ConcurrentModification.of_xml xml)
      | "DNSSECNotFound" -> `DNSSECNotFound (DNSSECNotFound.of_xml xml)
      | "HostedZonePartiallyDelegated" ->
          `HostedZonePartiallyDelegated
            (HostedZonePartiallyDelegated.of_xml xml)
      | "InvalidArgument" -> `InvalidArgument (InvalidArgument.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "InvalidKMSArn" -> `InvalidKMSArn (InvalidKMSArn.of_xml xml)
      | "InvalidKeySigningKeyStatus" ->
          `InvalidKeySigningKeyStatus (InvalidKeySigningKeyStatus.of_xml xml)
      | "KeySigningKeyWithActiveStatusNotFound" ->
          `KeySigningKeyWithActiveStatusNotFound
            (KeySigningKeyWithActiveStatusNotFound.of_xml xml)
      | "NoSuchHostedZone" -> `NoSuchHostedZone (NoSuchHostedZone.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConcurrentModification e ->
          `Assoc
            [("error", (`String "ConcurrentModification"));
            ("details", (ConcurrentModification.to_json e))]
      | `DNSSECNotFound e ->
          `Assoc
            [("error", (`String "DNSSECNotFound"));
            ("details", (DNSSECNotFound.to_json e))]
      | `HostedZonePartiallyDelegated e ->
          `Assoc
            [("error", (`String "HostedZonePartiallyDelegated"));
            ("details", (HostedZonePartiallyDelegated.to_json e))]
      | `InvalidArgument e ->
          `Assoc
            [("error", (`String "InvalidArgument"));
            ("details", (InvalidArgument.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `InvalidKMSArn e ->
          `Assoc
            [("error", (`String "InvalidKMSArn"));
            ("details", (InvalidKMSArn.to_json e))]
      | `InvalidKeySigningKeyStatus e ->
          `Assoc
            [("error", (`String "InvalidKeySigningKeyStatus"));
            ("details", (InvalidKeySigningKeyStatus.to_json e))]
      | `KeySigningKeyWithActiveStatusNotFound e ->
          `Assoc
            [("error", (`String "KeySigningKeyWithActiveStatusNotFound"));
            ("details", (KeySigningKeyWithActiveStatusNotFound.to_json e))]
      | `NoSuchHostedZone e ->
          `Assoc
            [("error", (`String "NoSuchHostedZone"));
            ("details", (NoSuchHostedZone.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ChangeInfo", (Some (ChangeInfo.to_value x.changeInfo)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let changeInfo =
        ChangeInfo.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChangeInfo") in
      make ~changeInfo ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let changeInfo = field_map_exn json "ChangeInfo" ChangeInfo.of_json in
      make ~changeInfo ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Enables DNSSEC signing in a specific hosted zone."]
module EnableHostedZoneDNSSECRequest =
  struct
    type nonrec t =
      {
      hostedZoneId: ResourceId.t
        [@ocaml.doc "A unique string used to identify a hosted zone."]}
    let context_ = "EnableHostedZoneDNSSECRequest"
    let make ~hostedZoneId = fun () -> { hostedZoneId }
    let to_value x =
      structure_to_value
        [("Id", (Some (ResourceId.to_value x.hostedZoneId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let hostedZoneId =
        ResourceId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ~hostedZoneId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let hostedZoneId = field_map_exn json "HostedZoneId" ResourceId.of_json in
      make ~hostedZoneId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Enables DNSSEC signing in a specific hosted zone."]
module DisassociateVPCFromHostedZoneResponse =
  struct
    type nonrec t =
      {
      changeInfo: ChangeInfo.t
        [@ocaml.doc
          "A complex type that describes the changes made to the specified private hosted zone."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t  | `InvalidVPCId of InvalidVPCId.t 
      | `LastVPCAssociation of LastVPCAssociation.t 
      | `NoSuchHostedZone of NoSuchHostedZone.t 
      | `VPCAssociationNotFound of VPCAssociationNotFound.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DisassociateVPCFromHostedZoneResponse"
    let make ~changeInfo = fun () -> { changeInfo }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "InvalidVPCId" -> `InvalidVPCId (InvalidVPCId.of_json json)
      | "LastVPCAssociation" ->
          `LastVPCAssociation (LastVPCAssociation.of_json json)
      | "NoSuchHostedZone" ->
          `NoSuchHostedZone (NoSuchHostedZone.of_json json)
      | "VPCAssociationNotFound" ->
          `VPCAssociationNotFound (VPCAssociationNotFound.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "InvalidVPCId" -> `InvalidVPCId (InvalidVPCId.of_xml xml)
      | "LastVPCAssociation" ->
          `LastVPCAssociation (LastVPCAssociation.of_xml xml)
      | "NoSuchHostedZone" -> `NoSuchHostedZone (NoSuchHostedZone.of_xml xml)
      | "VPCAssociationNotFound" ->
          `VPCAssociationNotFound (VPCAssociationNotFound.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `InvalidVPCId e ->
          `Assoc
            [("error", (`String "InvalidVPCId"));
            ("details", (InvalidVPCId.to_json e))]
      | `LastVPCAssociation e ->
          `Assoc
            [("error", (`String "LastVPCAssociation"));
            ("details", (LastVPCAssociation.to_json e))]
      | `NoSuchHostedZone e ->
          `Assoc
            [("error", (`String "NoSuchHostedZone"));
            ("details", (NoSuchHostedZone.to_json e))]
      | `VPCAssociationNotFound e ->
          `Assoc
            [("error", (`String "VPCAssociationNotFound"));
            ("details", (VPCAssociationNotFound.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ChangeInfo", (Some (ChangeInfo.to_value x.changeInfo)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let changeInfo =
        ChangeInfo.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChangeInfo") in
      make ~changeInfo ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let changeInfo = field_map_exn json "ChangeInfo" ChangeInfo.of_json in
      make ~changeInfo ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response information for the disassociate request."]
module DisassociateVPCFromHostedZoneRequest =
  struct
    type nonrec t =
      {
      hostedZoneId: ResourceId.t
        [@ocaml.doc
          "The ID of the private hosted zone that you want to disassociate a VPC from."];
      vPC: VPC.t
        [@ocaml.doc
          "A complex type that contains information about the VPC that you're disassociating from the specified hosted zone."];
      comment: DisassociateVPCComment.t option
        [@ocaml.doc "Optional: A comment about the disassociation request."]}
    let context_ = "DisassociateVPCFromHostedZoneRequest"
    let make ?comment =
      fun ~hostedZoneId ->
        fun ~vPC -> fun () -> { comment; hostedZoneId; vPC }
    let to_value x =
      structure_to_value
        [("Id", (Some (ResourceId.to_value x.hostedZoneId)));
        ("VPC", (Some (VPC.to_value x.vPC)));
        ("Comment",
          (Option.map x.comment ~f:DisassociateVPCComment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let comment =
        (Option.map ~f:DisassociateVPCComment.of_xml)
          (Xml.child xml_arg0 "Comment") in
      let vPC = VPC.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VPC") in
      let hostedZoneId =
        ResourceId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ?comment ~vPC ~hostedZoneId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let comment = field_map json "Comment" DisassociateVPCComment.of_json in
      let vPC = field_map_exn json "VPC" VPC.of_json in
      let hostedZoneId = field_map_exn json "HostedZoneId" ResourceId.of_json in
      make ?comment ~vPC ~hostedZoneId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about the VPC that you want to disassociate from a specified private hosted zone."]
module DisableHostedZoneDNSSECResponse =
  struct
    type nonrec t = {
      changeInfo: ChangeInfo.t }
    type nonrec error =
      [ `ConcurrentModification of ConcurrentModification.t 
      | `DNSSECNotFound of DNSSECNotFound.t 
      | `InvalidArgument of InvalidArgument.t 
      | `InvalidInput of InvalidInput.t  | `InvalidKMSArn of InvalidKMSArn.t 
      | `InvalidKeySigningKeyStatus of InvalidKeySigningKeyStatus.t 
      | `KeySigningKeyInParentDSRecord of KeySigningKeyInParentDSRecord.t 
      | `NoSuchHostedZone of NoSuchHostedZone.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DisableHostedZoneDNSSECResponse"
    let make ~changeInfo = fun () -> { changeInfo }
    let error_of_json name json =
      match name with
      | "ConcurrentModification" ->
          `ConcurrentModification (ConcurrentModification.of_json json)
      | "DNSSECNotFound" -> `DNSSECNotFound (DNSSECNotFound.of_json json)
      | "InvalidArgument" -> `InvalidArgument (InvalidArgument.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "InvalidKMSArn" -> `InvalidKMSArn (InvalidKMSArn.of_json json)
      | "InvalidKeySigningKeyStatus" ->
          `InvalidKeySigningKeyStatus
            (InvalidKeySigningKeyStatus.of_json json)
      | "KeySigningKeyInParentDSRecord" ->
          `KeySigningKeyInParentDSRecord
            (KeySigningKeyInParentDSRecord.of_json json)
      | "NoSuchHostedZone" ->
          `NoSuchHostedZone (NoSuchHostedZone.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConcurrentModification" ->
          `ConcurrentModification (ConcurrentModification.of_xml xml)
      | "DNSSECNotFound" -> `DNSSECNotFound (DNSSECNotFound.of_xml xml)
      | "InvalidArgument" -> `InvalidArgument (InvalidArgument.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "InvalidKMSArn" -> `InvalidKMSArn (InvalidKMSArn.of_xml xml)
      | "InvalidKeySigningKeyStatus" ->
          `InvalidKeySigningKeyStatus (InvalidKeySigningKeyStatus.of_xml xml)
      | "KeySigningKeyInParentDSRecord" ->
          `KeySigningKeyInParentDSRecord
            (KeySigningKeyInParentDSRecord.of_xml xml)
      | "NoSuchHostedZone" -> `NoSuchHostedZone (NoSuchHostedZone.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConcurrentModification e ->
          `Assoc
            [("error", (`String "ConcurrentModification"));
            ("details", (ConcurrentModification.to_json e))]
      | `DNSSECNotFound e ->
          `Assoc
            [("error", (`String "DNSSECNotFound"));
            ("details", (DNSSECNotFound.to_json e))]
      | `InvalidArgument e ->
          `Assoc
            [("error", (`String "InvalidArgument"));
            ("details", (InvalidArgument.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `InvalidKMSArn e ->
          `Assoc
            [("error", (`String "InvalidKMSArn"));
            ("details", (InvalidKMSArn.to_json e))]
      | `InvalidKeySigningKeyStatus e ->
          `Assoc
            [("error", (`String "InvalidKeySigningKeyStatus"));
            ("details", (InvalidKeySigningKeyStatus.to_json e))]
      | `KeySigningKeyInParentDSRecord e ->
          `Assoc
            [("error", (`String "KeySigningKeyInParentDSRecord"));
            ("details", (KeySigningKeyInParentDSRecord.to_json e))]
      | `NoSuchHostedZone e ->
          `Assoc
            [("error", (`String "NoSuchHostedZone"));
            ("details", (NoSuchHostedZone.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ChangeInfo", (Some (ChangeInfo.to_value x.changeInfo)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let changeInfo =
        ChangeInfo.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChangeInfo") in
      make ~changeInfo ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let changeInfo = field_map_exn json "ChangeInfo" ChangeInfo.of_json in
      make ~changeInfo ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Disables DNSSEC signing in a specific hosted zone. This action does not deactivate any key-signing keys (KSKs) that are active in the hosted zone."]
module DisableHostedZoneDNSSECRequest =
  struct
    type nonrec t =
      {
      hostedZoneId: ResourceId.t
        [@ocaml.doc "A unique string used to identify a hosted zone."]}
    let context_ = "DisableHostedZoneDNSSECRequest"
    let make ~hostedZoneId = fun () -> { hostedZoneId }
    let to_value x =
      structure_to_value
        [("Id", (Some (ResourceId.to_value x.hostedZoneId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let hostedZoneId =
        ResourceId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ~hostedZoneId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let hostedZoneId = field_map_exn json "HostedZoneId" ResourceId.of_json in
      make ~hostedZoneId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Disables DNSSEC signing in a specific hosted zone. This action does not deactivate any key-signing keys (KSKs) that are active in the hosted zone."]
module DeleteVPCAssociationAuthorizationResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ConcurrentModification of ConcurrentModification.t 
      | `InvalidInput of InvalidInput.t  | `InvalidVPCId of InvalidVPCId.t 
      | `NoSuchHostedZone of NoSuchHostedZone.t 
      | `VPCAssociationAuthorizationNotFound of
          VPCAssociationAuthorizationNotFound.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ConcurrentModification" ->
          `ConcurrentModification (ConcurrentModification.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "InvalidVPCId" -> `InvalidVPCId (InvalidVPCId.of_json json)
      | "NoSuchHostedZone" ->
          `NoSuchHostedZone (NoSuchHostedZone.of_json json)
      | "VPCAssociationAuthorizationNotFound" ->
          `VPCAssociationAuthorizationNotFound
            (VPCAssociationAuthorizationNotFound.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConcurrentModification" ->
          `ConcurrentModification (ConcurrentModification.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "InvalidVPCId" -> `InvalidVPCId (InvalidVPCId.of_xml xml)
      | "NoSuchHostedZone" -> `NoSuchHostedZone (NoSuchHostedZone.of_xml xml)
      | "VPCAssociationAuthorizationNotFound" ->
          `VPCAssociationAuthorizationNotFound
            (VPCAssociationAuthorizationNotFound.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConcurrentModification e ->
          `Assoc
            [("error", (`String "ConcurrentModification"));
            ("details", (ConcurrentModification.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `InvalidVPCId e ->
          `Assoc
            [("error", (`String "InvalidVPCId"));
            ("details", (InvalidVPCId.to_json e))]
      | `NoSuchHostedZone e ->
          `Assoc
            [("error", (`String "NoSuchHostedZone"));
            ("details", (NoSuchHostedZone.to_json e))]
      | `VPCAssociationAuthorizationNotFound e ->
          `Assoc
            [("error", (`String "VPCAssociationAuthorizationNotFound"));
            ("details", (VPCAssociationAuthorizationNotFound.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Empty response for the request."]
module DeleteVPCAssociationAuthorizationRequest =
  struct
    type nonrec t =
      {
      hostedZoneId: ResourceId.t
        [@ocaml.doc
          "When removing authorization to associate a VPC that was created by one Amazon Web Services account with a hosted zone that was created with a different Amazon Web Services account, the ID of the hosted zone."];
      vPC: VPC.t
        [@ocaml.doc
          "When removing authorization to associate a VPC that was created by one Amazon Web Services account with a hosted zone that was created with a different Amazon Web Services account, a complex type that includes the ID and region of the VPC."]}
    let context_ = "DeleteVPCAssociationAuthorizationRequest"
    let make ~hostedZoneId = fun ~vPC -> fun () -> { hostedZoneId; vPC }
    let to_value x =
      structure_to_value
        [("Id", (Some (ResourceId.to_value x.hostedZoneId)));
        ("VPC", (Some (VPC.to_value x.vPC)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vPC = VPC.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VPC") in
      let hostedZoneId =
        ResourceId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ~vPC ~hostedZoneId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let vPC = field_map_exn json "VPC" VPC.of_json in
      let hostedZoneId = field_map_exn json "HostedZoneId" ResourceId.of_json in
      make ~vPC ~hostedZoneId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about the request to remove authorization to associate a VPC that was created by one Amazon Web Services account with a hosted zone that was created with a different Amazon Web Services account."]
module DeleteTrafficPolicyResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ConcurrentModification of ConcurrentModification.t 
      | `InvalidInput of InvalidInput.t 
      | `NoSuchTrafficPolicy of NoSuchTrafficPolicy.t 
      | `TrafficPolicyInUse of TrafficPolicyInUse.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ConcurrentModification" ->
          `ConcurrentModification (ConcurrentModification.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchTrafficPolicy" ->
          `NoSuchTrafficPolicy (NoSuchTrafficPolicy.of_json json)
      | "TrafficPolicyInUse" ->
          `TrafficPolicyInUse (TrafficPolicyInUse.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConcurrentModification" ->
          `ConcurrentModification (ConcurrentModification.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchTrafficPolicy" ->
          `NoSuchTrafficPolicy (NoSuchTrafficPolicy.of_xml xml)
      | "TrafficPolicyInUse" ->
          `TrafficPolicyInUse (TrafficPolicyInUse.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConcurrentModification e ->
          `Assoc
            [("error", (`String "ConcurrentModification"));
            ("details", (ConcurrentModification.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchTrafficPolicy e ->
          `Assoc
            [("error", (`String "NoSuchTrafficPolicy"));
            ("details", (NoSuchTrafficPolicy.to_json e))]
      | `TrafficPolicyInUse e ->
          `Assoc
            [("error", (`String "TrafficPolicyInUse"));
            ("details", (TrafficPolicyInUse.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element."]
module DeleteTrafficPolicyRequest =
  struct
    type nonrec t =
      {
      id: TrafficPolicyId.t
        [@ocaml.doc "The ID of the traffic policy that you want to delete."];
      version: TrafficPolicyVersion.t
        [@ocaml.doc
          "The version number of the traffic policy that you want to delete."]}
    let context_ = "DeleteTrafficPolicyRequest"
    let make ~id = fun ~version -> fun () -> { id; version }
    let to_value x =
      structure_to_value
        [("Id", (Some (TrafficPolicyId.to_value x.id)));
        ("Version", (Some (TrafficPolicyVersion.to_value x.version)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let version =
        TrafficPolicyVersion.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Version") in
      let id =
        TrafficPolicyId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ~version ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let version = field_map_exn json "Version" TrafficPolicyVersion.of_json in
      let id = field_map_exn json "Id" TrafficPolicyId.of_json in
      make ~version ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A request to delete a specified traffic policy version."]
module DeleteTrafficPolicyInstanceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `NoSuchTrafficPolicyInstance of NoSuchTrafficPolicyInstance.t 
      | `PriorRequestNotComplete of PriorRequestNotComplete.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchTrafficPolicyInstance" ->
          `NoSuchTrafficPolicyInstance
            (NoSuchTrafficPolicyInstance.of_json json)
      | "PriorRequestNotComplete" ->
          `PriorRequestNotComplete (PriorRequestNotComplete.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchTrafficPolicyInstance" ->
          `NoSuchTrafficPolicyInstance
            (NoSuchTrafficPolicyInstance.of_xml xml)
      | "PriorRequestNotComplete" ->
          `PriorRequestNotComplete (PriorRequestNotComplete.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchTrafficPolicyInstance e ->
          `Assoc
            [("error", (`String "NoSuchTrafficPolicyInstance"));
            ("details", (NoSuchTrafficPolicyInstance.to_json e))]
      | `PriorRequestNotComplete e ->
          `Assoc
            [("error", (`String "PriorRequestNotComplete"));
            ("details", (PriorRequestNotComplete.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element."]
module DeleteTrafficPolicyInstanceRequest =
  struct
    type nonrec t =
      {
      id: TrafficPolicyInstanceId.t
        [@ocaml.doc
          "The ID of the traffic policy instance that you want to delete. When you delete a traffic policy instance, Amazon Route 53 also deletes all of the resource record sets that were created when you created the traffic policy instance."]}
    let context_ = "DeleteTrafficPolicyInstanceRequest"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value
        [("Id", (Some (TrafficPolicyInstanceId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        TrafficPolicyInstanceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let id = field_map_exn json "Id" TrafficPolicyInstanceId.of_json in
      make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A request to delete a specified traffic policy instance."]
module DeleteReusableDelegationSetResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `DelegationSetInUse of DelegationSetInUse.t 
      | `DelegationSetNotReusable of DelegationSetNotReusable.t 
      | `InvalidInput of InvalidInput.t 
      | `NoSuchDelegationSet of NoSuchDelegationSet.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "DelegationSetInUse" ->
          `DelegationSetInUse (DelegationSetInUse.of_json json)
      | "DelegationSetNotReusable" ->
          `DelegationSetNotReusable (DelegationSetNotReusable.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchDelegationSet" ->
          `NoSuchDelegationSet (NoSuchDelegationSet.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "DelegationSetInUse" ->
          `DelegationSetInUse (DelegationSetInUse.of_xml xml)
      | "DelegationSetNotReusable" ->
          `DelegationSetNotReusable (DelegationSetNotReusable.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchDelegationSet" ->
          `NoSuchDelegationSet (NoSuchDelegationSet.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `DelegationSetInUse e ->
          `Assoc
            [("error", (`String "DelegationSetInUse"));
            ("details", (DelegationSetInUse.to_json e))]
      | `DelegationSetNotReusable e ->
          `Assoc
            [("error", (`String "DelegationSetNotReusable"));
            ("details", (DelegationSetNotReusable.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchDelegationSet e ->
          `Assoc
            [("error", (`String "NoSuchDelegationSet"));
            ("details", (NoSuchDelegationSet.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element."]
module DeleteReusableDelegationSetRequest =
  struct
    type nonrec t =
      {
      id: ResourceId.t
        [@ocaml.doc
          "The ID of the reusable delegation set that you want to delete."]}
    let context_ = "DeleteReusableDelegationSetRequest"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value [("Id", (Some (ResourceId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        ResourceId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let id = field_map_exn json "Id" ResourceId.of_json in make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A request to delete a reusable delegation set."]
module DeleteQueryLoggingConfigResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `ConcurrentModification of ConcurrentModification.t 
      | `InvalidInput of InvalidInput.t 
      | `NoSuchQueryLoggingConfig of NoSuchQueryLoggingConfig.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "ConcurrentModification" ->
          `ConcurrentModification (ConcurrentModification.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchQueryLoggingConfig" ->
          `NoSuchQueryLoggingConfig (NoSuchQueryLoggingConfig.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConcurrentModification" ->
          `ConcurrentModification (ConcurrentModification.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchQueryLoggingConfig" ->
          `NoSuchQueryLoggingConfig (NoSuchQueryLoggingConfig.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConcurrentModification e ->
          `Assoc
            [("error", (`String "ConcurrentModification"));
            ("details", (ConcurrentModification.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchQueryLoggingConfig e ->
          `Assoc
            [("error", (`String "NoSuchQueryLoggingConfig"));
            ("details", (NoSuchQueryLoggingConfig.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a configuration for DNS query logging. If you delete a configuration, Amazon Route 53 stops sending query logs to CloudWatch Logs. Route 53 doesn't delete any logs that are already in CloudWatch Logs. For more information about DNS query logs, see CreateQueryLoggingConfig."]
module DeleteQueryLoggingConfigRequest =
  struct
    type nonrec t =
      {
      id: QueryLoggingConfigId.t
        [@ocaml.doc "The ID of the configuration that you want to delete."]}
    let context_ = "DeleteQueryLoggingConfigRequest"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value
        [("Id", (Some (QueryLoggingConfigId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        QueryLoggingConfigId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let id = field_map_exn json "Id" QueryLoggingConfigId.of_json in
      make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a configuration for DNS query logging. If you delete a configuration, Amazon Route 53 stops sending query logs to CloudWatch Logs. Route 53 doesn't delete any logs that are already in CloudWatch Logs. For more information about DNS query logs, see CreateQueryLoggingConfig."]
module DeleteKeySigningKeyResponse =
  struct
    type nonrec t = {
      changeInfo: ChangeInfo.t }
    type nonrec error =
      [ `ConcurrentModification of ConcurrentModification.t 
      | `InvalidInput of InvalidInput.t  | `InvalidKMSArn of InvalidKMSArn.t 
      | `InvalidKeySigningKeyStatus of InvalidKeySigningKeyStatus.t 
      | `InvalidSigningStatus of InvalidSigningStatus.t 
      | `NoSuchKeySigningKey of NoSuchKeySigningKey.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DeleteKeySigningKeyResponse"
    let make ~changeInfo = fun () -> { changeInfo }
    let error_of_json name json =
      match name with
      | "ConcurrentModification" ->
          `ConcurrentModification (ConcurrentModification.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "InvalidKMSArn" -> `InvalidKMSArn (InvalidKMSArn.of_json json)
      | "InvalidKeySigningKeyStatus" ->
          `InvalidKeySigningKeyStatus
            (InvalidKeySigningKeyStatus.of_json json)
      | "InvalidSigningStatus" ->
          `InvalidSigningStatus (InvalidSigningStatus.of_json json)
      | "NoSuchKeySigningKey" ->
          `NoSuchKeySigningKey (NoSuchKeySigningKey.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConcurrentModification" ->
          `ConcurrentModification (ConcurrentModification.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "InvalidKMSArn" -> `InvalidKMSArn (InvalidKMSArn.of_xml xml)
      | "InvalidKeySigningKeyStatus" ->
          `InvalidKeySigningKeyStatus (InvalidKeySigningKeyStatus.of_xml xml)
      | "InvalidSigningStatus" ->
          `InvalidSigningStatus (InvalidSigningStatus.of_xml xml)
      | "NoSuchKeySigningKey" ->
          `NoSuchKeySigningKey (NoSuchKeySigningKey.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConcurrentModification e ->
          `Assoc
            [("error", (`String "ConcurrentModification"));
            ("details", (ConcurrentModification.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `InvalidKMSArn e ->
          `Assoc
            [("error", (`String "InvalidKMSArn"));
            ("details", (InvalidKMSArn.to_json e))]
      | `InvalidKeySigningKeyStatus e ->
          `Assoc
            [("error", (`String "InvalidKeySigningKeyStatus"));
            ("details", (InvalidKeySigningKeyStatus.to_json e))]
      | `InvalidSigningStatus e ->
          `Assoc
            [("error", (`String "InvalidSigningStatus"));
            ("details", (InvalidSigningStatus.to_json e))]
      | `NoSuchKeySigningKey e ->
          `Assoc
            [("error", (`String "NoSuchKeySigningKey"));
            ("details", (NoSuchKeySigningKey.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ChangeInfo", (Some (ChangeInfo.to_value x.changeInfo)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let changeInfo =
        ChangeInfo.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChangeInfo") in
      make ~changeInfo ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let changeInfo = field_map_exn json "ChangeInfo" ChangeInfo.of_json in
      make ~changeInfo ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a key-signing key (KSK). Before you can delete a KSK, you must deactivate it. The KSK must be deactivated before you can delete it regardless of whether the hosted zone is enabled for DNSSEC signing. You can use DeactivateKeySigningKey to deactivate the key before you delete it. Use GetDNSSEC to verify that the KSK is in an INACTIVE status."]
module DeleteKeySigningKeyRequest =
  struct
    type nonrec t =
      {
      hostedZoneId: ResourceId.t
        [@ocaml.doc "A unique string used to identify a hosted zone."];
      name: SigningKeyName.t
        [@ocaml.doc "A string used to identify a key-signing key (KSK)."]}
    let context_ = "DeleteKeySigningKeyRequest"
    let make ~hostedZoneId = fun ~name -> fun () -> { hostedZoneId; name }
    let to_value x =
      structure_to_value
        [("HostedZoneId", (Some (ResourceId.to_value x.hostedZoneId)));
        ("Name", (Some (SigningKeyName.to_value x.name)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        SigningKeyName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      let hostedZoneId =
        ResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HostedZoneId") in
      make ~name ~hostedZoneId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let name = field_map_exn json "Name" SigningKeyName.of_json in
      let hostedZoneId = field_map_exn json "HostedZoneId" ResourceId.of_json in
      make ~name ~hostedZoneId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deletes a key-signing key (KSK). Before you can delete a KSK, you must deactivate it. The KSK must be deactivated before you can delete it regardless of whether the hosted zone is enabled for DNSSEC signing. You can use DeactivateKeySigningKey to deactivate the key before you delete it. Use GetDNSSEC to verify that the KSK is in an INACTIVE status."]
module DeleteHostedZoneResponse =
  struct
    type nonrec t =
      {
      changeInfo: ChangeInfo.t
        [@ocaml.doc
          "A complex type that contains the ID, the status, and the date and time of a request to delete a hosted zone."]}
    type nonrec error =
      [ `HostedZoneNotEmpty of HostedZoneNotEmpty.t 
      | `InvalidDomainName of InvalidDomainName.t 
      | `InvalidInput of InvalidInput.t 
      | `NoSuchHostedZone of NoSuchHostedZone.t 
      | `PriorRequestNotComplete of PriorRequestNotComplete.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DeleteHostedZoneResponse"
    let make ~changeInfo = fun () -> { changeInfo }
    let error_of_json name json =
      match name with
      | "HostedZoneNotEmpty" ->
          `HostedZoneNotEmpty (HostedZoneNotEmpty.of_json json)
      | "InvalidDomainName" ->
          `InvalidDomainName (InvalidDomainName.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchHostedZone" ->
          `NoSuchHostedZone (NoSuchHostedZone.of_json json)
      | "PriorRequestNotComplete" ->
          `PriorRequestNotComplete (PriorRequestNotComplete.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "HostedZoneNotEmpty" ->
          `HostedZoneNotEmpty (HostedZoneNotEmpty.of_xml xml)
      | "InvalidDomainName" ->
          `InvalidDomainName (InvalidDomainName.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchHostedZone" -> `NoSuchHostedZone (NoSuchHostedZone.of_xml xml)
      | "PriorRequestNotComplete" ->
          `PriorRequestNotComplete (PriorRequestNotComplete.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `HostedZoneNotEmpty e ->
          `Assoc
            [("error", (`String "HostedZoneNotEmpty"));
            ("details", (HostedZoneNotEmpty.to_json e))]
      | `InvalidDomainName e ->
          `Assoc
            [("error", (`String "InvalidDomainName"));
            ("details", (InvalidDomainName.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchHostedZone e ->
          `Assoc
            [("error", (`String "NoSuchHostedZone"));
            ("details", (NoSuchHostedZone.to_json e))]
      | `PriorRequestNotComplete e ->
          `Assoc
            [("error", (`String "PriorRequestNotComplete"));
            ("details", (PriorRequestNotComplete.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ChangeInfo", (Some (ChangeInfo.to_value x.changeInfo)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let changeInfo =
        ChangeInfo.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChangeInfo") in
      make ~changeInfo ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let changeInfo = field_map_exn json "ChangeInfo" ChangeInfo.of_json in
      make ~changeInfo ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response to a DeleteHostedZone request."]
module DeleteHostedZoneRequest =
  struct
    type nonrec t =
      {
      id: ResourceId.t
        [@ocaml.doc "The ID of the hosted zone you want to delete."]}
    let context_ = "DeleteHostedZoneRequest"
    let make ~id = fun () -> { id }
    let to_value x =
      structure_to_value [("Id", (Some (ResourceId.to_value x.id)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let id =
        ResourceId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let id = field_map_exn json "Id" ResourceId.of_json in make ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A request to delete a hosted zone."]
module DeleteHealthCheckResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `HealthCheckInUse of HealthCheckInUse.t 
      | `InvalidInput of InvalidInput.t 
      | `NoSuchHealthCheck of NoSuchHealthCheck.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "HealthCheckInUse" ->
          `HealthCheckInUse (HealthCheckInUse.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchHealthCheck" ->
          `NoSuchHealthCheck (NoSuchHealthCheck.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "HealthCheckInUse" -> `HealthCheckInUse (HealthCheckInUse.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchHealthCheck" ->
          `NoSuchHealthCheck (NoSuchHealthCheck.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `HealthCheckInUse e ->
          `Assoc
            [("error", (`String "HealthCheckInUse"));
            ("details", (HealthCheckInUse.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchHealthCheck e ->
          `Assoc
            [("error", (`String "NoSuchHealthCheck"));
            ("details", (NoSuchHealthCheck.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "An empty element."]
module DeleteHealthCheckRequest =
  struct
    type nonrec t =
      {
      healthCheckId: HealthCheckId.t
        [@ocaml.doc "The ID of the health check that you want to delete."]}
    let context_ = "DeleteHealthCheckRequest"
    let make ~healthCheckId = fun () -> { healthCheckId }
    let to_value x =
      structure_to_value
        [("HealthCheckId", (Some (HealthCheckId.to_value x.healthCheckId)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let healthCheckId =
        HealthCheckId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HealthCheckId") in
      make ~healthCheckId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let healthCheckId =
        field_map_exn json "HealthCheckId" HealthCheckId.of_json in
      make ~healthCheckId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "This action deletes a health check."]
module DeactivateKeySigningKeyResponse =
  struct
    type nonrec t = {
      changeInfo: ChangeInfo.t }
    type nonrec error =
      [ `ConcurrentModification of ConcurrentModification.t 
      | `InvalidInput of InvalidInput.t 
      | `InvalidKeySigningKeyStatus of InvalidKeySigningKeyStatus.t 
      | `InvalidSigningStatus of InvalidSigningStatus.t 
      | `KeySigningKeyInParentDSRecord of KeySigningKeyInParentDSRecord.t 
      | `KeySigningKeyInUse of KeySigningKeyInUse.t 
      | `NoSuchKeySigningKey of NoSuchKeySigningKey.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "DeactivateKeySigningKeyResponse"
    let make ~changeInfo = fun () -> { changeInfo }
    let error_of_json name json =
      match name with
      | "ConcurrentModification" ->
          `ConcurrentModification (ConcurrentModification.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "InvalidKeySigningKeyStatus" ->
          `InvalidKeySigningKeyStatus
            (InvalidKeySigningKeyStatus.of_json json)
      | "InvalidSigningStatus" ->
          `InvalidSigningStatus (InvalidSigningStatus.of_json json)
      | "KeySigningKeyInParentDSRecord" ->
          `KeySigningKeyInParentDSRecord
            (KeySigningKeyInParentDSRecord.of_json json)
      | "KeySigningKeyInUse" ->
          `KeySigningKeyInUse (KeySigningKeyInUse.of_json json)
      | "NoSuchKeySigningKey" ->
          `NoSuchKeySigningKey (NoSuchKeySigningKey.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConcurrentModification" ->
          `ConcurrentModification (ConcurrentModification.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "InvalidKeySigningKeyStatus" ->
          `InvalidKeySigningKeyStatus (InvalidKeySigningKeyStatus.of_xml xml)
      | "InvalidSigningStatus" ->
          `InvalidSigningStatus (InvalidSigningStatus.of_xml xml)
      | "KeySigningKeyInParentDSRecord" ->
          `KeySigningKeyInParentDSRecord
            (KeySigningKeyInParentDSRecord.of_xml xml)
      | "KeySigningKeyInUse" ->
          `KeySigningKeyInUse (KeySigningKeyInUse.of_xml xml)
      | "NoSuchKeySigningKey" ->
          `NoSuchKeySigningKey (NoSuchKeySigningKey.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConcurrentModification e ->
          `Assoc
            [("error", (`String "ConcurrentModification"));
            ("details", (ConcurrentModification.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `InvalidKeySigningKeyStatus e ->
          `Assoc
            [("error", (`String "InvalidKeySigningKeyStatus"));
            ("details", (InvalidKeySigningKeyStatus.to_json e))]
      | `InvalidSigningStatus e ->
          `Assoc
            [("error", (`String "InvalidSigningStatus"));
            ("details", (InvalidSigningStatus.to_json e))]
      | `KeySigningKeyInParentDSRecord e ->
          `Assoc
            [("error", (`String "KeySigningKeyInParentDSRecord"));
            ("details", (KeySigningKeyInParentDSRecord.to_json e))]
      | `KeySigningKeyInUse e ->
          `Assoc
            [("error", (`String "KeySigningKeyInUse"));
            ("details", (KeySigningKeyInUse.to_json e))]
      | `NoSuchKeySigningKey e ->
          `Assoc
            [("error", (`String "NoSuchKeySigningKey"));
            ("details", (NoSuchKeySigningKey.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ChangeInfo", (Some (ChangeInfo.to_value x.changeInfo)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let changeInfo =
        ChangeInfo.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChangeInfo") in
      make ~changeInfo ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let changeInfo = field_map_exn json "ChangeInfo" ChangeInfo.of_json in
      make ~changeInfo ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deactivates a key-signing key (KSK) so that it will not be used for signing by DNSSEC. This operation changes the KSK status to INACTIVE."]
module DeactivateKeySigningKeyRequest =
  struct
    type nonrec t =
      {
      hostedZoneId: ResourceId.t
        [@ocaml.doc "A unique string used to identify a hosted zone."];
      name: SigningKeyName.t
        [@ocaml.doc "A string used to identify a key-signing key (KSK)."]}
    let context_ = "DeactivateKeySigningKeyRequest"
    let make ~hostedZoneId = fun ~name -> fun () -> { hostedZoneId; name }
    let to_value x =
      structure_to_value
        [("HostedZoneId", (Some (ResourceId.to_value x.hostedZoneId)));
        ("Name", (Some (SigningKeyName.to_value x.name)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        SigningKeyName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      let hostedZoneId =
        ResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HostedZoneId") in
      make ~name ~hostedZoneId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let name = field_map_exn json "Name" SigningKeyName.of_json in
      let hostedZoneId = field_map_exn json "HostedZoneId" ResourceId.of_json in
      make ~name ~hostedZoneId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Deactivates a key-signing key (KSK) so that it will not be used for signing by DNSSEC. This operation changes the KSK status to INACTIVE."]
module CreateVPCAssociationAuthorizationResponse =
  struct
    type nonrec t =
      {
      hostedZoneId: ResourceId.t
        [@ocaml.doc
          "The ID of the hosted zone that you authorized associating a VPC with."];
      vPC: VPC.t
        [@ocaml.doc
          "The VPC that you authorized associating with a hosted zone."]}
    type nonrec error =
      [ `ConcurrentModification of ConcurrentModification.t 
      | `InvalidInput of InvalidInput.t  | `InvalidVPCId of InvalidVPCId.t 
      | `NoSuchHostedZone of NoSuchHostedZone.t 
      | `TooManyVPCAssociationAuthorizations of
          TooManyVPCAssociationAuthorizations.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "CreateVPCAssociationAuthorizationResponse"
    let make ~hostedZoneId = fun ~vPC -> fun () -> { hostedZoneId; vPC }
    let error_of_json name json =
      match name with
      | "ConcurrentModification" ->
          `ConcurrentModification (ConcurrentModification.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "InvalidVPCId" -> `InvalidVPCId (InvalidVPCId.of_json json)
      | "NoSuchHostedZone" ->
          `NoSuchHostedZone (NoSuchHostedZone.of_json json)
      | "TooManyVPCAssociationAuthorizations" ->
          `TooManyVPCAssociationAuthorizations
            (TooManyVPCAssociationAuthorizations.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConcurrentModification" ->
          `ConcurrentModification (ConcurrentModification.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "InvalidVPCId" -> `InvalidVPCId (InvalidVPCId.of_xml xml)
      | "NoSuchHostedZone" -> `NoSuchHostedZone (NoSuchHostedZone.of_xml xml)
      | "TooManyVPCAssociationAuthorizations" ->
          `TooManyVPCAssociationAuthorizations
            (TooManyVPCAssociationAuthorizations.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConcurrentModification e ->
          `Assoc
            [("error", (`String "ConcurrentModification"));
            ("details", (ConcurrentModification.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `InvalidVPCId e ->
          `Assoc
            [("error", (`String "InvalidVPCId"));
            ("details", (InvalidVPCId.to_json e))]
      | `NoSuchHostedZone e ->
          `Assoc
            [("error", (`String "NoSuchHostedZone"));
            ("details", (NoSuchHostedZone.to_json e))]
      | `TooManyVPCAssociationAuthorizations e ->
          `Assoc
            [("error", (`String "TooManyVPCAssociationAuthorizations"));
            ("details", (TooManyVPCAssociationAuthorizations.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("HostedZoneId", (Some (ResourceId.to_value x.hostedZoneId)));
        ("VPC", (Some (VPC.to_value x.vPC)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vPC = VPC.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VPC") in
      let hostedZoneId =
        ResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HostedZoneId") in
      make ~vPC ~hostedZoneId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let vPC = field_map_exn json "VPC" VPC.of_json in
      let hostedZoneId = field_map_exn json "HostedZoneId" ResourceId.of_json in
      make ~vPC ~hostedZoneId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response information from a CreateVPCAssociationAuthorization request."]
module CreateVPCAssociationAuthorizationRequest =
  struct
    type nonrec t =
      {
      hostedZoneId: ResourceId.t
        [@ocaml.doc
          "The ID of the private hosted zone that you want to authorize associating a VPC with."];
      vPC: VPC.t
        [@ocaml.doc
          "A complex type that contains the VPC ID and region for the VPC that you want to authorize associating with your hosted zone."]}
    let context_ = "CreateVPCAssociationAuthorizationRequest"
    let make ~hostedZoneId = fun ~vPC -> fun () -> { hostedZoneId; vPC }
    let to_value x =
      structure_to_value
        [("Id", (Some (ResourceId.to_value x.hostedZoneId)));
        ("VPC", (Some (VPC.to_value x.vPC)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let vPC = VPC.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VPC") in
      let hostedZoneId =
        ResourceId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ~vPC ~hostedZoneId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let vPC = field_map_exn json "VPC" VPC.of_json in
      let hostedZoneId = field_map_exn json "HostedZoneId" ResourceId.of_json in
      make ~vPC ~hostedZoneId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about the request to authorize associating a VPC with your private hosted zone. Authorization is only required when a private hosted zone and a VPC were created by using different accounts."]
module CreateTrafficPolicyVersionResponse =
  struct
    type nonrec t =
      {
      trafficPolicy: TrafficPolicy.t
        [@ocaml.doc
          "A complex type that contains settings for the new version of the traffic policy."];
      location: ResourceURI.t
        [@ocaml.doc
          "A unique URL that represents a new traffic policy version."]}
    type nonrec error =
      [ `ConcurrentModification of ConcurrentModification.t 
      | `InvalidInput of InvalidInput.t 
      | `InvalidTrafficPolicyDocument of InvalidTrafficPolicyDocument.t 
      | `NoSuchTrafficPolicy of NoSuchTrafficPolicy.t 
      | `TooManyTrafficPolicyVersionsForCurrentPolicy of
          TooManyTrafficPolicyVersionsForCurrentPolicy.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "CreateTrafficPolicyVersionResponse"
    let make ~trafficPolicy =
      fun ~location -> fun () -> { trafficPolicy; location }
    let error_of_json name json =
      match name with
      | "ConcurrentModification" ->
          `ConcurrentModification (ConcurrentModification.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "InvalidTrafficPolicyDocument" ->
          `InvalidTrafficPolicyDocument
            (InvalidTrafficPolicyDocument.of_json json)
      | "NoSuchTrafficPolicy" ->
          `NoSuchTrafficPolicy (NoSuchTrafficPolicy.of_json json)
      | "TooManyTrafficPolicyVersionsForCurrentPolicy" ->
          `TooManyTrafficPolicyVersionsForCurrentPolicy
            (TooManyTrafficPolicyVersionsForCurrentPolicy.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConcurrentModification" ->
          `ConcurrentModification (ConcurrentModification.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "InvalidTrafficPolicyDocument" ->
          `InvalidTrafficPolicyDocument
            (InvalidTrafficPolicyDocument.of_xml xml)
      | "NoSuchTrafficPolicy" ->
          `NoSuchTrafficPolicy (NoSuchTrafficPolicy.of_xml xml)
      | "TooManyTrafficPolicyVersionsForCurrentPolicy" ->
          `TooManyTrafficPolicyVersionsForCurrentPolicy
            (TooManyTrafficPolicyVersionsForCurrentPolicy.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConcurrentModification e ->
          `Assoc
            [("error", (`String "ConcurrentModification"));
            ("details", (ConcurrentModification.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `InvalidTrafficPolicyDocument e ->
          `Assoc
            [("error", (`String "InvalidTrafficPolicyDocument"));
            ("details", (InvalidTrafficPolicyDocument.to_json e))]
      | `NoSuchTrafficPolicy e ->
          `Assoc
            [("error", (`String "NoSuchTrafficPolicy"));
            ("details", (NoSuchTrafficPolicy.to_json e))]
      | `TooManyTrafficPolicyVersionsForCurrentPolicy e ->
          `Assoc
            [("error",
               (`String "TooManyTrafficPolicyVersionsForCurrentPolicy"));
            ("details",
              (TooManyTrafficPolicyVersionsForCurrentPolicy.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("TrafficPolicy", (Some (TrafficPolicy.to_value x.trafficPolicy)));
        ("Location", (Some (ResourceURI.to_value x.location)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        ResourceURI.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Location") in
      let trafficPolicy =
        TrafficPolicy.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficPolicy") in
      make ~location ~trafficPolicy ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let location = field_map_exn json "Location" ResourceURI.of_json in
      let trafficPolicy =
        field_map_exn json "TrafficPolicy" TrafficPolicy.of_json in
      make ~location ~trafficPolicy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response information for the CreateTrafficPolicyVersion request."]
module CreateTrafficPolicyVersionRequest =
  struct
    type nonrec t =
      {
      id: TrafficPolicyId.t
        [@ocaml.doc
          "The ID of the traffic policy for which you want to create a new version."];
      document: TrafficPolicyDocument.t
        [@ocaml.doc
          "The definition of this version of the traffic policy, in JSON format. You specified the JSON in the CreateTrafficPolicyVersion request. For more information about the JSON format, see CreateTrafficPolicy."];
      comment: TrafficPolicyComment.t option
        [@ocaml.doc
          "The comment that you specified in the CreateTrafficPolicyVersion request, if any."]}
    let context_ = "CreateTrafficPolicyVersionRequest"
    let make ?comment =
      fun ~id -> fun ~document -> fun () -> { comment; id; document }
    let to_value x =
      structure_to_value
        [("Id", (Some (TrafficPolicyId.to_value x.id)));
        ("Document", (Some (TrafficPolicyDocument.to_value x.document)));
        ("Comment", (Option.map x.comment ~f:TrafficPolicyComment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let comment =
        (Option.map ~f:TrafficPolicyComment.of_xml)
          (Xml.child xml_arg0 "Comment") in
      let document =
        TrafficPolicyDocument.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Document") in
      let id =
        TrafficPolicyId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ?comment ~document ~id ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let comment = field_map json "Comment" TrafficPolicyComment.of_json in
      let document =
        field_map_exn json "Document" TrafficPolicyDocument.of_json in
      let id = field_map_exn json "Id" TrafficPolicyId.of_json in
      make ?comment ~document ~id ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about the traffic policy that you want to create a new version for."]
module CreateTrafficPolicyResponse =
  struct
    type nonrec t =
      {
      trafficPolicy: TrafficPolicy.t
        [@ocaml.doc
          "A complex type that contains settings for the new traffic policy."];
      location: ResourceURI.t
        [@ocaml.doc "A unique URL that represents a new traffic policy."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `InvalidTrafficPolicyDocument of InvalidTrafficPolicyDocument.t 
      | `TooManyTrafficPolicies of TooManyTrafficPolicies.t 
      | `TrafficPolicyAlreadyExists of TrafficPolicyAlreadyExists.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "CreateTrafficPolicyResponse"
    let make ~trafficPolicy =
      fun ~location -> fun () -> { trafficPolicy; location }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "InvalidTrafficPolicyDocument" ->
          `InvalidTrafficPolicyDocument
            (InvalidTrafficPolicyDocument.of_json json)
      | "TooManyTrafficPolicies" ->
          `TooManyTrafficPolicies (TooManyTrafficPolicies.of_json json)
      | "TrafficPolicyAlreadyExists" ->
          `TrafficPolicyAlreadyExists
            (TrafficPolicyAlreadyExists.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "InvalidTrafficPolicyDocument" ->
          `InvalidTrafficPolicyDocument
            (InvalidTrafficPolicyDocument.of_xml xml)
      | "TooManyTrafficPolicies" ->
          `TooManyTrafficPolicies (TooManyTrafficPolicies.of_xml xml)
      | "TrafficPolicyAlreadyExists" ->
          `TrafficPolicyAlreadyExists (TrafficPolicyAlreadyExists.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `InvalidTrafficPolicyDocument e ->
          `Assoc
            [("error", (`String "InvalidTrafficPolicyDocument"));
            ("details", (InvalidTrafficPolicyDocument.to_json e))]
      | `TooManyTrafficPolicies e ->
          `Assoc
            [("error", (`String "TooManyTrafficPolicies"));
            ("details", (TooManyTrafficPolicies.to_json e))]
      | `TrafficPolicyAlreadyExists e ->
          `Assoc
            [("error", (`String "TrafficPolicyAlreadyExists"));
            ("details", (TrafficPolicyAlreadyExists.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("TrafficPolicy", (Some (TrafficPolicy.to_value x.trafficPolicy)));
        ("Location", (Some (ResourceURI.to_value x.location)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        ResourceURI.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Location") in
      let trafficPolicy =
        TrafficPolicy.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficPolicy") in
      make ~location ~trafficPolicy ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let location = field_map_exn json "Location" ResourceURI.of_json in
      let trafficPolicy =
        field_map_exn json "TrafficPolicy" TrafficPolicy.of_json in
      make ~location ~trafficPolicy ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response information for the CreateTrafficPolicy request."]
module CreateTrafficPolicyRequest =
  struct
    type nonrec t =
      {
      name: TrafficPolicyName.t
        [@ocaml.doc "The name of the traffic policy."];
      document: TrafficPolicyDocument.t
        [@ocaml.doc
          "The definition of this traffic policy in JSON format. For more information, see Traffic Policy Document Format."];
      comment: TrafficPolicyComment.t option
        [@ocaml.doc
          "(Optional) Any comments that you want to include about the traffic policy."]}
    let context_ = "CreateTrafficPolicyRequest"
    let make ?comment =
      fun ~name -> fun ~document -> fun () -> { comment; name; document }
    let to_value x =
      structure_to_value
        [("Name", (Some (TrafficPolicyName.to_value x.name)));
        ("Document", (Some (TrafficPolicyDocument.to_value x.document)));
        ("Comment", (Option.map x.comment ~f:TrafficPolicyComment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let comment =
        (Option.map ~f:TrafficPolicyComment.of_xml)
          (Xml.child xml_arg0 "Comment") in
      let document =
        TrafficPolicyDocument.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Document") in
      let name =
        TrafficPolicyName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ?comment ~document ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let comment = field_map json "Comment" TrafficPolicyComment.of_json in
      let document =
        field_map_exn json "Document" TrafficPolicyDocument.of_json in
      let name = field_map_exn json "Name" TrafficPolicyName.of_json in
      make ?comment ~document ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about the traffic policy that you want to create."]
module CreateTrafficPolicyInstanceResponse =
  struct
    type nonrec t =
      {
      trafficPolicyInstance: TrafficPolicyInstance.t
        [@ocaml.doc
          "A complex type that contains settings for the new traffic policy instance."];
      location: ResourceURI.t
        [@ocaml.doc
          "A unique URL that represents a new traffic policy instance."]}
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `NoSuchHostedZone of NoSuchHostedZone.t 
      | `NoSuchTrafficPolicy of NoSuchTrafficPolicy.t 
      | `TooManyTrafficPolicyInstances of TooManyTrafficPolicyInstances.t 
      | `TrafficPolicyInstanceAlreadyExists of
          TrafficPolicyInstanceAlreadyExists.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "CreateTrafficPolicyInstanceResponse"
    let make ~trafficPolicyInstance =
      fun ~location -> fun () -> { trafficPolicyInstance; location }
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchHostedZone" ->
          `NoSuchHostedZone (NoSuchHostedZone.of_json json)
      | "NoSuchTrafficPolicy" ->
          `NoSuchTrafficPolicy (NoSuchTrafficPolicy.of_json json)
      | "TooManyTrafficPolicyInstances" ->
          `TooManyTrafficPolicyInstances
            (TooManyTrafficPolicyInstances.of_json json)
      | "TrafficPolicyInstanceAlreadyExists" ->
          `TrafficPolicyInstanceAlreadyExists
            (TrafficPolicyInstanceAlreadyExists.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchHostedZone" -> `NoSuchHostedZone (NoSuchHostedZone.of_xml xml)
      | "NoSuchTrafficPolicy" ->
          `NoSuchTrafficPolicy (NoSuchTrafficPolicy.of_xml xml)
      | "TooManyTrafficPolicyInstances" ->
          `TooManyTrafficPolicyInstances
            (TooManyTrafficPolicyInstances.of_xml xml)
      | "TrafficPolicyInstanceAlreadyExists" ->
          `TrafficPolicyInstanceAlreadyExists
            (TrafficPolicyInstanceAlreadyExists.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchHostedZone e ->
          `Assoc
            [("error", (`String "NoSuchHostedZone"));
            ("details", (NoSuchHostedZone.to_json e))]
      | `NoSuchTrafficPolicy e ->
          `Assoc
            [("error", (`String "NoSuchTrafficPolicy"));
            ("details", (NoSuchTrafficPolicy.to_json e))]
      | `TooManyTrafficPolicyInstances e ->
          `Assoc
            [("error", (`String "TooManyTrafficPolicyInstances"));
            ("details", (TooManyTrafficPolicyInstances.to_json e))]
      | `TrafficPolicyInstanceAlreadyExists e ->
          `Assoc
            [("error", (`String "TrafficPolicyInstanceAlreadyExists"));
            ("details", (TrafficPolicyInstanceAlreadyExists.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("TrafficPolicyInstance",
           (Some (TrafficPolicyInstance.to_value x.trafficPolicyInstance)));
        ("Location", (Some (ResourceURI.to_value x.location)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        ResourceURI.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Location") in
      let trafficPolicyInstance =
        TrafficPolicyInstance.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficPolicyInstance") in
      make ~location ~trafficPolicyInstance ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let location = field_map_exn json "Location" ResourceURI.of_json in
      let trafficPolicyInstance =
        field_map_exn json "TrafficPolicyInstance"
          TrafficPolicyInstance.of_json in
      make ~location ~trafficPolicyInstance ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response information for the CreateTrafficPolicyInstance request."]
module CreateTrafficPolicyInstanceRequest =
  struct
    type nonrec t =
      {
      hostedZoneId: ResourceId.t
        [@ocaml.doc
          "The ID of the hosted zone that you want Amazon Route 53 to create resource record sets in by using the configuration in a traffic policy."];
      name: DNSName.t
        [@ocaml.doc
          "The domain name (such as example.com) or subdomain name (such as www.example.com) for which Amazon Route 53 responds to DNS queries by using the resource record sets that Route 53 creates for this traffic policy instance."];
      tTL: TTL.t
        [@ocaml.doc
          "(Optional) The TTL that you want Amazon Route 53 to assign to all of the resource record sets that it creates in the specified hosted zone."];
      trafficPolicyId: TrafficPolicyId.t
        [@ocaml.doc
          "The ID of the traffic policy that you want to use to create resource record sets in the specified hosted zone."];
      trafficPolicyVersion: TrafficPolicyVersion.t
        [@ocaml.doc
          "The version of the traffic policy that you want to use to create resource record sets in the specified hosted zone."]}
    let context_ = "CreateTrafficPolicyInstanceRequest"
    let make ~hostedZoneId =
      fun ~name ->
        fun ~tTL ->
          fun ~trafficPolicyId ->
            fun ~trafficPolicyVersion ->
              fun () ->
                {
                  hostedZoneId;
                  name;
                  tTL;
                  trafficPolicyId;
                  trafficPolicyVersion
                }
    let to_value x =
      structure_to_value
        [("HostedZoneId", (Some (ResourceId.to_value x.hostedZoneId)));
        ("Name", (Some (DNSName.to_value x.name)));
        ("TTL", (Some (TTL.to_value x.tTL)));
        ("TrafficPolicyId",
          (Some (TrafficPolicyId.to_value x.trafficPolicyId)));
        ("TrafficPolicyVersion",
          (Some (TrafficPolicyVersion.to_value x.trafficPolicyVersion)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let trafficPolicyVersion =
        TrafficPolicyVersion.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficPolicyVersion") in
      let trafficPolicyId =
        TrafficPolicyId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "TrafficPolicyId") in
      let tTL = TTL.of_xml (Xml.child_exn ~context:context_ xml_arg0 "TTL") in
      let name =
        DNSName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      let hostedZoneId =
        ResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HostedZoneId") in
      make ~trafficPolicyVersion ~trafficPolicyId ~tTL ~name ~hostedZoneId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let trafficPolicyVersion =
        field_map_exn json "TrafficPolicyVersion"
          TrafficPolicyVersion.of_json in
      let trafficPolicyId =
        field_map_exn json "TrafficPolicyId" TrafficPolicyId.of_json in
      let tTL = field_map_exn json "TTL" TTL.of_json in
      let name = field_map_exn json "Name" DNSName.of_json in
      let hostedZoneId = field_map_exn json "HostedZoneId" ResourceId.of_json in
      make ~trafficPolicyVersion ~trafficPolicyId ~tTL ~name ~hostedZoneId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about the resource record sets that you want to create based on a specified traffic policy."]
module CreateReusableDelegationSetResponse =
  struct
    type nonrec t =
      {
      delegationSet: DelegationSet.t
        [@ocaml.doc "A complex type that contains name server information."];
      location: ResourceURI.t
        [@ocaml.doc
          "The unique URL representing the new reusable delegation set."]}
    type nonrec error =
      [ `DelegationSetAlreadyCreated of DelegationSetAlreadyCreated.t 
      | `DelegationSetAlreadyReusable of DelegationSetAlreadyReusable.t 
      | `DelegationSetNotAvailable of DelegationSetNotAvailable.t 
      | `HostedZoneNotFound of HostedZoneNotFound.t 
      | `InvalidArgument of InvalidArgument.t 
      | `InvalidInput of InvalidInput.t 
      | `LimitsExceeded of LimitsExceeded.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "CreateReusableDelegationSetResponse"
    let make ~delegationSet =
      fun ~location -> fun () -> { delegationSet; location }
    let error_of_json name json =
      match name with
      | "DelegationSetAlreadyCreated" ->
          `DelegationSetAlreadyCreated
            (DelegationSetAlreadyCreated.of_json json)
      | "DelegationSetAlreadyReusable" ->
          `DelegationSetAlreadyReusable
            (DelegationSetAlreadyReusable.of_json json)
      | "DelegationSetNotAvailable" ->
          `DelegationSetNotAvailable (DelegationSetNotAvailable.of_json json)
      | "HostedZoneNotFound" ->
          `HostedZoneNotFound (HostedZoneNotFound.of_json json)
      | "InvalidArgument" -> `InvalidArgument (InvalidArgument.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "LimitsExceeded" -> `LimitsExceeded (LimitsExceeded.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "DelegationSetAlreadyCreated" ->
          `DelegationSetAlreadyCreated
            (DelegationSetAlreadyCreated.of_xml xml)
      | "DelegationSetAlreadyReusable" ->
          `DelegationSetAlreadyReusable
            (DelegationSetAlreadyReusable.of_xml xml)
      | "DelegationSetNotAvailable" ->
          `DelegationSetNotAvailable (DelegationSetNotAvailable.of_xml xml)
      | "HostedZoneNotFound" ->
          `HostedZoneNotFound (HostedZoneNotFound.of_xml xml)
      | "InvalidArgument" -> `InvalidArgument (InvalidArgument.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "LimitsExceeded" -> `LimitsExceeded (LimitsExceeded.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `DelegationSetAlreadyCreated e ->
          `Assoc
            [("error", (`String "DelegationSetAlreadyCreated"));
            ("details", (DelegationSetAlreadyCreated.to_json e))]
      | `DelegationSetAlreadyReusable e ->
          `Assoc
            [("error", (`String "DelegationSetAlreadyReusable"));
            ("details", (DelegationSetAlreadyReusable.to_json e))]
      | `DelegationSetNotAvailable e ->
          `Assoc
            [("error", (`String "DelegationSetNotAvailable"));
            ("details", (DelegationSetNotAvailable.to_json e))]
      | `HostedZoneNotFound e ->
          `Assoc
            [("error", (`String "HostedZoneNotFound"));
            ("details", (HostedZoneNotFound.to_json e))]
      | `InvalidArgument e ->
          `Assoc
            [("error", (`String "InvalidArgument"));
            ("details", (InvalidArgument.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `LimitsExceeded e ->
          `Assoc
            [("error", (`String "LimitsExceeded"));
            ("details", (LimitsExceeded.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("DelegationSet", (Some (DelegationSet.to_value x.delegationSet)));
        ("Location", (Some (ResourceURI.to_value x.location)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        ResourceURI.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Location") in
      let delegationSet =
        DelegationSet.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DelegationSet") in
      make ~location ~delegationSet ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let location = field_map_exn json "Location" ResourceURI.of_json in
      let delegationSet =
        field_map_exn json "DelegationSet" DelegationSet.of_json in
      make ~location ~delegationSet ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a delegation set (a group of four name servers) that can be reused by multiple hosted zones that were created by the same Amazon Web Services account. You can also create a reusable delegation set that uses the four name servers that are associated with an existing hosted zone. Specify the hosted zone ID in the CreateReusableDelegationSet request. You can't associate a reusable delegation set with a private hosted zone. For information about using a reusable delegation set to configure white label name servers, see Configuring White Label Name Servers. The process for migrating existing hosted zones to use a reusable delegation set is comparable to the process for configuring white label name servers. You need to perform the following steps: Create a reusable delegation set. Recreate hosted zones, and reduce the TTL to 60 seconds or less. Recreate resource record sets in the new hosted zones. Change the registrar's name servers to use the name servers for the new hosted zones. Monitor traffic for the website or application. Change TTLs back to their original values. If you want to migrate existing hosted zones to use a reusable delegation set, the existing hosted zones can't use any of the name servers that are assigned to the reusable delegation set. If one or more hosted zones do use one or more name servers that are assigned to the reusable delegation set, you can do one of the following: For small numbers of hosted zones\226\128\148up to a few hundred\226\128\148it's relatively easy to create reusable delegation sets until you get one that has four name servers that don't overlap with any of the name servers in your hosted zones. For larger numbers of hosted zones, the easiest solution is to use more than one reusable delegation set. For larger numbers of hosted zones, you can also migrate hosted zones that have overlapping name servers to hosted zones that don't have overlapping name servers, then migrate the hosted zones again to use the reusable delegation set."]
module CreateReusableDelegationSetRequest =
  struct
    type nonrec t =
      {
      callerReference: Nonce.t
        [@ocaml.doc
          "A unique string that identifies the request, and that allows you to retry failed CreateReusableDelegationSet requests without the risk of executing the operation twice. You must use a unique CallerReference string every time you submit a CreateReusableDelegationSet request. CallerReference can be any unique string, for example a date/time stamp."];
      hostedZoneId: ResourceId.t option
        [@ocaml.doc
          "If you want to mark the delegation set for an existing hosted zone as reusable, the ID for that hosted zone."]}
    let context_ = "CreateReusableDelegationSetRequest"
    let make ?hostedZoneId =
      fun ~callerReference -> fun () -> { hostedZoneId; callerReference }
    let to_value x =
      structure_to_value
        [("CallerReference", (Some (Nonce.to_value x.callerReference)));
        ("HostedZoneId", (Option.map x.hostedZoneId ~f:ResourceId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let hostedZoneId =
        (Option.map ~f:ResourceId.of_xml) (Xml.child xml_arg0 "HostedZoneId") in
      let callerReference =
        Nonce.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CallerReference") in
      make ?hostedZoneId ~callerReference ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let hostedZoneId = field_map json "HostedZoneId" ResourceId.of_json in
      let callerReference =
        field_map_exn json "CallerReference" Nonce.of_json in
      make ?hostedZoneId ~callerReference ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a delegation set (a group of four name servers) that can be reused by multiple hosted zones that were created by the same Amazon Web Services account. You can also create a reusable delegation set that uses the four name servers that are associated with an existing hosted zone. Specify the hosted zone ID in the CreateReusableDelegationSet request. You can't associate a reusable delegation set with a private hosted zone. For information about using a reusable delegation set to configure white label name servers, see Configuring White Label Name Servers. The process for migrating existing hosted zones to use a reusable delegation set is comparable to the process for configuring white label name servers. You need to perform the following steps: Create a reusable delegation set. Recreate hosted zones, and reduce the TTL to 60 seconds or less. Recreate resource record sets in the new hosted zones. Change the registrar's name servers to use the name servers for the new hosted zones. Monitor traffic for the website or application. Change TTLs back to their original values. If you want to migrate existing hosted zones to use a reusable delegation set, the existing hosted zones can't use any of the name servers that are assigned to the reusable delegation set. If one or more hosted zones do use one or more name servers that are assigned to the reusable delegation set, you can do one of the following: For small numbers of hosted zones\226\128\148up to a few hundred\226\128\148it's relatively easy to create reusable delegation sets until you get one that has four name servers that don't overlap with any of the name servers in your hosted zones. For larger numbers of hosted zones, the easiest solution is to use more than one reusable delegation set. For larger numbers of hosted zones, you can also migrate hosted zones that have overlapping name servers to hosted zones that don't have overlapping name servers, then migrate the hosted zones again to use the reusable delegation set."]
module CreateQueryLoggingConfigResponse =
  struct
    type nonrec t =
      {
      queryLoggingConfig: QueryLoggingConfig.t
        [@ocaml.doc
          "A complex type that contains the ID for a query logging configuration, the ID of the hosted zone that you want to log queries for, and the ARN for the log group that you want Amazon Route 53 to send query logs to."];
      location: ResourceURI.t
        [@ocaml.doc
          "The unique URL representing the new query logging configuration."]}
    type nonrec error =
      [ `ConcurrentModification of ConcurrentModification.t 
      | `InsufficientCloudWatchLogsResourcePolicy of
          InsufficientCloudWatchLogsResourcePolicy.t 
      | `InvalidInput of InvalidInput.t 
      | `NoSuchCloudWatchLogsLogGroup of NoSuchCloudWatchLogsLogGroup.t 
      | `NoSuchHostedZone of NoSuchHostedZone.t 
      | `QueryLoggingConfigAlreadyExists of QueryLoggingConfigAlreadyExists.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "CreateQueryLoggingConfigResponse"
    let make ~queryLoggingConfig =
      fun ~location -> fun () -> { queryLoggingConfig; location }
    let error_of_json name json =
      match name with
      | "ConcurrentModification" ->
          `ConcurrentModification (ConcurrentModification.of_json json)
      | "InsufficientCloudWatchLogsResourcePolicy" ->
          `InsufficientCloudWatchLogsResourcePolicy
            (InsufficientCloudWatchLogsResourcePolicy.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchCloudWatchLogsLogGroup" ->
          `NoSuchCloudWatchLogsLogGroup
            (NoSuchCloudWatchLogsLogGroup.of_json json)
      | "NoSuchHostedZone" ->
          `NoSuchHostedZone (NoSuchHostedZone.of_json json)
      | "QueryLoggingConfigAlreadyExists" ->
          `QueryLoggingConfigAlreadyExists
            (QueryLoggingConfigAlreadyExists.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConcurrentModification" ->
          `ConcurrentModification (ConcurrentModification.of_xml xml)
      | "InsufficientCloudWatchLogsResourcePolicy" ->
          `InsufficientCloudWatchLogsResourcePolicy
            (InsufficientCloudWatchLogsResourcePolicy.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchCloudWatchLogsLogGroup" ->
          `NoSuchCloudWatchLogsLogGroup
            (NoSuchCloudWatchLogsLogGroup.of_xml xml)
      | "NoSuchHostedZone" -> `NoSuchHostedZone (NoSuchHostedZone.of_xml xml)
      | "QueryLoggingConfigAlreadyExists" ->
          `QueryLoggingConfigAlreadyExists
            (QueryLoggingConfigAlreadyExists.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConcurrentModification e ->
          `Assoc
            [("error", (`String "ConcurrentModification"));
            ("details", (ConcurrentModification.to_json e))]
      | `InsufficientCloudWatchLogsResourcePolicy e ->
          `Assoc
            [("error", (`String "InsufficientCloudWatchLogsResourcePolicy"));
            ("details", (InsufficientCloudWatchLogsResourcePolicy.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchCloudWatchLogsLogGroup e ->
          `Assoc
            [("error", (`String "NoSuchCloudWatchLogsLogGroup"));
            ("details", (NoSuchCloudWatchLogsLogGroup.to_json e))]
      | `NoSuchHostedZone e ->
          `Assoc
            [("error", (`String "NoSuchHostedZone"));
            ("details", (NoSuchHostedZone.to_json e))]
      | `QueryLoggingConfigAlreadyExists e ->
          `Assoc
            [("error", (`String "QueryLoggingConfigAlreadyExists"));
            ("details", (QueryLoggingConfigAlreadyExists.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("QueryLoggingConfig",
           (Some (QueryLoggingConfig.to_value x.queryLoggingConfig)));
        ("Location", (Some (ResourceURI.to_value x.location)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        ResourceURI.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Location") in
      let queryLoggingConfig =
        QueryLoggingConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "QueryLoggingConfig") in
      make ~location ~queryLoggingConfig ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let location = field_map_exn json "Location" ResourceURI.of_json in
      let queryLoggingConfig =
        field_map_exn json "QueryLoggingConfig" QueryLoggingConfig.of_json in
      make ~location ~queryLoggingConfig ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a configuration for DNS query logging. After you create a query logging configuration, Amazon Route 53 begins to publish log data to an Amazon CloudWatch Logs log group. DNS query logs contain information about the queries that Route 53 receives for a specified public hosted zone, such as the following: Route 53 edge location that responded to the DNS query Domain or subdomain that was requested DNS record type, such as A or AAAA DNS response code, such as NoError or ServFail Log Group and Resource Policy Before you create a query logging configuration, perform the following operations. If you create a query logging configuration using the Route 53 console, Route 53 performs these operations automatically. Create a CloudWatch Logs log group, and make note of the ARN, which you specify when you create a query logging configuration. Note the following: You must create the log group in the us-east-1 region. You must use the same Amazon Web Services account to create the log group and the hosted zone that you want to configure query logging for. When you create log groups for query logging, we recommend that you use a consistent prefix, for example: /aws/route53/hosted zone name In the next step, you'll create a resource policy, which controls access to one or more log groups and the associated Amazon Web Services resources, such as Route 53 hosted zones. There's a limit on the number of resource policies that you can create, so we recommend that you use a consistent prefix so you can use the same resource policy for all the log groups that you create for query logging. Create a CloudWatch Logs resource policy, and give it the permissions that Route 53 needs to create log streams and to send query logs to log streams. For the value of Resource, specify the ARN for the log group that you created in the previous step. To use the same resource policy for all the CloudWatch Logs log groups that you created for query logging configurations, replace the hosted zone name with *, for example: arn:aws:logs:us-east-1:123412341234:log-group:/aws/route53/* To avoid the confused deputy problem, a security issue where an entity without a permission for an action can coerce a more-privileged entity to perform it, you can optionally limit the permissions that a service has to a resource in a resource-based policy by supplying the following values: For aws:SourceArn, supply the hosted zone ARN used in creating the query logging configuration. For example, aws:SourceArn: arn:aws:route53:::hostedzone/hosted zone ID. For aws:SourceAccount, supply the account ID for the account that creates the query logging configuration. For example, aws:SourceAccount:111111111111. For more information, see The confused deputy problem in the Amazon Web Services IAM User Guide. You can't use the CloudWatch console to create or edit a resource policy. You must use the CloudWatch API, one of the Amazon Web Services SDKs, or the CLI. Log Streams and Edge Locations When Route 53 finishes creating the configuration for DNS query logging, it does the following: Creates a log stream for an edge location the first time that the edge location responds to DNS queries for the specified hosted zone. That log stream is used to log all queries that Route 53 responds to for that edge location. Begins to send query logs to the applicable log stream. The name of each log stream is in the following format: hosted zone ID/edge location code The edge location code is a three-letter code and an arbitrarily assigned number, for example, DFW3. The three-letter code typically corresponds with the International Air Transport Association airport code for an airport near the edge location. (These abbreviations might change in the future.) For a list of edge locations, see \"The Route 53 Global Network\" on the Route 53 Product Details page. Queries That Are Logged Query logs contain only the queries that DNS resolvers forward to Route 53. If a DNS resolver has already cached the response to a query (such as the IP address for a load balancer for example.com), the resolver will continue to return the cached response. It doesn't forward another query to Route 53 until the TTL for the corresponding resource record set expires. Depending on how many DNS queries are submitted for a resource record set, and depending on the TTL for that resource record set, query logs might contain information about only one query out of every several thousand queries that are submitted to DNS. For more information about how DNS works, see Routing Internet Traffic to Your Website or Web Application in the Amazon Route 53 Developer Guide. Log File Format For a list of the values in each query log and the format of each value, see Logging DNS Queries in the Amazon Route 53 Developer Guide. Pricing For information about charges for query logs, see Amazon CloudWatch Pricing. How to Stop Logging If you want Route 53 to stop sending query logs to CloudWatch Logs, delete the query logging configuration. For more information, see DeleteQueryLoggingConfig."]
module CreateQueryLoggingConfigRequest =
  struct
    type nonrec t =
      {
      hostedZoneId: ResourceId.t
        [@ocaml.doc
          "The ID of the hosted zone that you want to log queries for. You can log queries only for public hosted zones."];
      cloudWatchLogsLogGroupArn: CloudWatchLogsLogGroupArn.t
        [@ocaml.doc
          "The Amazon Resource Name (ARN) for the log group that you want to Amazon Route 53 to send query logs to. This is the format of the ARN: arn:aws:logs:region:account-id:log-group:log_group_name To get the ARN for a log group, you can use the CloudWatch console, the DescribeLogGroups API action, the describe-log-groups command, or the applicable command in one of the Amazon Web Services SDKs."]}
    let context_ = "CreateQueryLoggingConfigRequest"
    let make ~hostedZoneId =
      fun ~cloudWatchLogsLogGroupArn ->
        fun () -> { hostedZoneId; cloudWatchLogsLogGroupArn }
    let to_value x =
      structure_to_value
        [("HostedZoneId", (Some (ResourceId.to_value x.hostedZoneId)));
        ("CloudWatchLogsLogGroupArn",
          (Some
             (CloudWatchLogsLogGroupArn.to_value x.cloudWatchLogsLogGroupArn)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let cloudWatchLogsLogGroupArn =
        CloudWatchLogsLogGroupArn.of_xml
          (Xml.child_exn ~context:context_ xml_arg0
             "CloudWatchLogsLogGroupArn") in
      let hostedZoneId =
        ResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HostedZoneId") in
      make ~cloudWatchLogsLogGroupArn ~hostedZoneId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let cloudWatchLogsLogGroupArn =
        field_map_exn json "CloudWatchLogsLogGroupArn"
          CloudWatchLogsLogGroupArn.of_json in
      let hostedZoneId = field_map_exn json "HostedZoneId" ResourceId.of_json in
      make ~cloudWatchLogsLogGroupArn ~hostedZoneId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a configuration for DNS query logging. After you create a query logging configuration, Amazon Route 53 begins to publish log data to an Amazon CloudWatch Logs log group. DNS query logs contain information about the queries that Route 53 receives for a specified public hosted zone, such as the following: Route 53 edge location that responded to the DNS query Domain or subdomain that was requested DNS record type, such as A or AAAA DNS response code, such as NoError or ServFail Log Group and Resource Policy Before you create a query logging configuration, perform the following operations. If you create a query logging configuration using the Route 53 console, Route 53 performs these operations automatically. Create a CloudWatch Logs log group, and make note of the ARN, which you specify when you create a query logging configuration. Note the following: You must create the log group in the us-east-1 region. You must use the same Amazon Web Services account to create the log group and the hosted zone that you want to configure query logging for. When you create log groups for query logging, we recommend that you use a consistent prefix, for example: /aws/route53/hosted zone name In the next step, you'll create a resource policy, which controls access to one or more log groups and the associated Amazon Web Services resources, such as Route 53 hosted zones. There's a limit on the number of resource policies that you can create, so we recommend that you use a consistent prefix so you can use the same resource policy for all the log groups that you create for query logging. Create a CloudWatch Logs resource policy, and give it the permissions that Route 53 needs to create log streams and to send query logs to log streams. For the value of Resource, specify the ARN for the log group that you created in the previous step. To use the same resource policy for all the CloudWatch Logs log groups that you created for query logging configurations, replace the hosted zone name with *, for example: arn:aws:logs:us-east-1:123412341234:log-group:/aws/route53/* To avoid the confused deputy problem, a security issue where an entity without a permission for an action can coerce a more-privileged entity to perform it, you can optionally limit the permissions that a service has to a resource in a resource-based policy by supplying the following values: For aws:SourceArn, supply the hosted zone ARN used in creating the query logging configuration. For example, aws:SourceArn: arn:aws:route53:::hostedzone/hosted zone ID. For aws:SourceAccount, supply the account ID for the account that creates the query logging configuration. For example, aws:SourceAccount:111111111111. For more information, see The confused deputy problem in the Amazon Web Services IAM User Guide. You can't use the CloudWatch console to create or edit a resource policy. You must use the CloudWatch API, one of the Amazon Web Services SDKs, or the CLI. Log Streams and Edge Locations When Route 53 finishes creating the configuration for DNS query logging, it does the following: Creates a log stream for an edge location the first time that the edge location responds to DNS queries for the specified hosted zone. That log stream is used to log all queries that Route 53 responds to for that edge location. Begins to send query logs to the applicable log stream. The name of each log stream is in the following format: hosted zone ID/edge location code The edge location code is a three-letter code and an arbitrarily assigned number, for example, DFW3. The three-letter code typically corresponds with the International Air Transport Association airport code for an airport near the edge location. (These abbreviations might change in the future.) For a list of edge locations, see \"The Route 53 Global Network\" on the Route 53 Product Details page. Queries That Are Logged Query logs contain only the queries that DNS resolvers forward to Route 53. If a DNS resolver has already cached the response to a query (such as the IP address for a load balancer for example.com), the resolver will continue to return the cached response. It doesn't forward another query to Route 53 until the TTL for the corresponding resource record set expires. Depending on how many DNS queries are submitted for a resource record set, and depending on the TTL for that resource record set, query logs might contain information about only one query out of every several thousand queries that are submitted to DNS. For more information about how DNS works, see Routing Internet Traffic to Your Website or Web Application in the Amazon Route 53 Developer Guide. Log File Format For a list of the values in each query log and the format of each value, see Logging DNS Queries in the Amazon Route 53 Developer Guide. Pricing For information about charges for query logs, see Amazon CloudWatch Pricing. How to Stop Logging If you want Route 53 to stop sending query logs to CloudWatch Logs, delete the query logging configuration. For more information, see DeleteQueryLoggingConfig."]
module CreateKeySigningKeyResponse =
  struct
    type nonrec t =
      {
      changeInfo: ChangeInfo.t ;
      keySigningKey: KeySigningKey.t
        [@ocaml.doc "The key-signing key (KSK) that the request creates."];
      location: ResourceURI.t
        [@ocaml.doc
          "The unique URL representing the new key-signing key (KSK)."]}
    type nonrec error =
      [ `ConcurrentModification of ConcurrentModification.t 
      | `InvalidArgument of InvalidArgument.t 
      | `InvalidInput of InvalidInput.t  | `InvalidKMSArn of InvalidKMSArn.t 
      | `InvalidKeySigningKeyName of InvalidKeySigningKeyName.t 
      | `InvalidKeySigningKeyStatus of InvalidKeySigningKeyStatus.t 
      | `InvalidSigningStatus of InvalidSigningStatus.t 
      | `KeySigningKeyAlreadyExists of KeySigningKeyAlreadyExists.t 
      | `NoSuchHostedZone of NoSuchHostedZone.t 
      | `TooManyKeySigningKeys of TooManyKeySigningKeys.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "CreateKeySigningKeyResponse"
    let make ~changeInfo =
      fun ~keySigningKey ->
        fun ~location -> fun () -> { changeInfo; keySigningKey; location }
    let error_of_json name json =
      match name with
      | "ConcurrentModification" ->
          `ConcurrentModification (ConcurrentModification.of_json json)
      | "InvalidArgument" -> `InvalidArgument (InvalidArgument.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "InvalidKMSArn" -> `InvalidKMSArn (InvalidKMSArn.of_json json)
      | "InvalidKeySigningKeyName" ->
          `InvalidKeySigningKeyName (InvalidKeySigningKeyName.of_json json)
      | "InvalidKeySigningKeyStatus" ->
          `InvalidKeySigningKeyStatus
            (InvalidKeySigningKeyStatus.of_json json)
      | "InvalidSigningStatus" ->
          `InvalidSigningStatus (InvalidSigningStatus.of_json json)
      | "KeySigningKeyAlreadyExists" ->
          `KeySigningKeyAlreadyExists
            (KeySigningKeyAlreadyExists.of_json json)
      | "NoSuchHostedZone" ->
          `NoSuchHostedZone (NoSuchHostedZone.of_json json)
      | "TooManyKeySigningKeys" ->
          `TooManyKeySigningKeys (TooManyKeySigningKeys.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConcurrentModification" ->
          `ConcurrentModification (ConcurrentModification.of_xml xml)
      | "InvalidArgument" -> `InvalidArgument (InvalidArgument.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "InvalidKMSArn" -> `InvalidKMSArn (InvalidKMSArn.of_xml xml)
      | "InvalidKeySigningKeyName" ->
          `InvalidKeySigningKeyName (InvalidKeySigningKeyName.of_xml xml)
      | "InvalidKeySigningKeyStatus" ->
          `InvalidKeySigningKeyStatus (InvalidKeySigningKeyStatus.of_xml xml)
      | "InvalidSigningStatus" ->
          `InvalidSigningStatus (InvalidSigningStatus.of_xml xml)
      | "KeySigningKeyAlreadyExists" ->
          `KeySigningKeyAlreadyExists (KeySigningKeyAlreadyExists.of_xml xml)
      | "NoSuchHostedZone" -> `NoSuchHostedZone (NoSuchHostedZone.of_xml xml)
      | "TooManyKeySigningKeys" ->
          `TooManyKeySigningKeys (TooManyKeySigningKeys.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConcurrentModification e ->
          `Assoc
            [("error", (`String "ConcurrentModification"));
            ("details", (ConcurrentModification.to_json e))]
      | `InvalidArgument e ->
          `Assoc
            [("error", (`String "InvalidArgument"));
            ("details", (InvalidArgument.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `InvalidKMSArn e ->
          `Assoc
            [("error", (`String "InvalidKMSArn"));
            ("details", (InvalidKMSArn.to_json e))]
      | `InvalidKeySigningKeyName e ->
          `Assoc
            [("error", (`String "InvalidKeySigningKeyName"));
            ("details", (InvalidKeySigningKeyName.to_json e))]
      | `InvalidKeySigningKeyStatus e ->
          `Assoc
            [("error", (`String "InvalidKeySigningKeyStatus"));
            ("details", (InvalidKeySigningKeyStatus.to_json e))]
      | `InvalidSigningStatus e ->
          `Assoc
            [("error", (`String "InvalidSigningStatus"));
            ("details", (InvalidSigningStatus.to_json e))]
      | `KeySigningKeyAlreadyExists e ->
          `Assoc
            [("error", (`String "KeySigningKeyAlreadyExists"));
            ("details", (KeySigningKeyAlreadyExists.to_json e))]
      | `NoSuchHostedZone e ->
          `Assoc
            [("error", (`String "NoSuchHostedZone"));
            ("details", (NoSuchHostedZone.to_json e))]
      | `TooManyKeySigningKeys e ->
          `Assoc
            [("error", (`String "TooManyKeySigningKeys"));
            ("details", (TooManyKeySigningKeys.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ChangeInfo", (Some (ChangeInfo.to_value x.changeInfo)));
        ("KeySigningKey", (Some (KeySigningKey.to_value x.keySigningKey)));
        ("Location", (Some (ResourceURI.to_value x.location)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        ResourceURI.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Location") in
      let keySigningKey =
        KeySigningKey.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "KeySigningKey") in
      let changeInfo =
        ChangeInfo.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChangeInfo") in
      make ~location ~keySigningKey ~changeInfo ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let location = field_map_exn json "Location" ResourceURI.of_json in
      let keySigningKey =
        field_map_exn json "KeySigningKey" KeySigningKey.of_json in
      let changeInfo = field_map_exn json "ChangeInfo" ChangeInfo.of_json in
      make ~location ~keySigningKey ~changeInfo ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new key-signing key (KSK) associated with a hosted zone. You can only have two KSKs per hosted zone."]
module CreateKeySigningKeyRequest =
  struct
    type nonrec t =
      {
      callerReference: Nonce.t
        [@ocaml.doc "A unique string that identifies the request."];
      hostedZoneId: ResourceId.t
        [@ocaml.doc "The unique string (ID) used to identify a hosted zone."];
      keyManagementServiceArn: SigningKeyString.t
        [@ocaml.doc
          "The Amazon resource name (ARN) for a customer managed key in Key Management Service (KMS). The KeyManagementServiceArn must be unique for each key-signing key (KSK) in a single hosted zone. To see an example of KeyManagementServiceArn that grants the correct permissions for DNSSEC, scroll down to Example. You must configure the customer managed customer managed key as follows: Status Enabled Key spec ECC_NIST_P256 Key usage Sign and verify Key policy The key policy must give permission for the following actions: DescribeKey GetPublicKey Sign The key policy must also include the Amazon Route 53 service in the principal for your account. Specify the following: \"Service\": \"dnssec-route53.amazonaws.com\" For more information about working with a customer managed key in KMS, see Key Management Service concepts."];
      name: SigningKeyName.t
        [@ocaml.doc
          "A string used to identify a key-signing key (KSK). Name can include numbers, letters, and underscores (_). Name must be unique for each key-signing key in the same hosted zone."];
      status: SigningKeyStatus.t
        [@ocaml.doc
          "A string specifying the initial status of the key-signing key (KSK). You can set the value to ACTIVE or INACTIVE."]}
    let context_ = "CreateKeySigningKeyRequest"
    let make ~callerReference =
      fun ~hostedZoneId ->
        fun ~keyManagementServiceArn ->
          fun ~name ->
            fun ~status ->
              fun () ->
                {
                  callerReference;
                  hostedZoneId;
                  keyManagementServiceArn;
                  name;
                  status
                }
    let to_value x =
      structure_to_value
        [("CallerReference", (Some (Nonce.to_value x.callerReference)));
        ("HostedZoneId", (Some (ResourceId.to_value x.hostedZoneId)));
        ("KeyManagementServiceArn",
          (Some (SigningKeyString.to_value x.keyManagementServiceArn)));
        ("Name", (Some (SigningKeyName.to_value x.name)));
        ("Status", (Some (SigningKeyStatus.to_value x.status)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let status =
        SigningKeyStatus.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Status") in
      let name =
        SigningKeyName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      let keyManagementServiceArn =
        SigningKeyString.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "KeyManagementServiceArn") in
      let hostedZoneId =
        ResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HostedZoneId") in
      let callerReference =
        Nonce.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CallerReference") in
      make ~status ~name ~keyManagementServiceArn ~hostedZoneId
        ~callerReference ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let status = field_map_exn json "Status" SigningKeyStatus.of_json in
      let name = field_map_exn json "Name" SigningKeyName.of_json in
      let keyManagementServiceArn =
        field_map_exn json "KeyManagementServiceArn" SigningKeyString.of_json in
      let hostedZoneId = field_map_exn json "HostedZoneId" ResourceId.of_json in
      let callerReference =
        field_map_exn json "CallerReference" Nonce.of_json in
      make ~status ~name ~keyManagementServiceArn ~hostedZoneId
        ~callerReference ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Creates a new key-signing key (KSK) associated with a hosted zone. You can only have two KSKs per hosted zone."]
module CreateHostedZoneResponse =
  struct
    type nonrec t =
      {
      hostedZone: HostedZone.t
        [@ocaml.doc
          "A complex type that contains general information about the hosted zone."];
      changeInfo: ChangeInfo.t
        [@ocaml.doc
          "A complex type that contains information about the CreateHostedZone request."];
      delegationSet: DelegationSet.t
        [@ocaml.doc
          "A complex type that describes the name servers for this hosted zone."];
      vPC: VPC.t option
        [@ocaml.doc
          "A complex type that contains information about an Amazon VPC that you associated with this hosted zone."];
      location: ResourceURI.t
        [@ocaml.doc "The unique URL representing the new hosted zone."]}
    type nonrec error =
      [ `ConflictingDomainExists of ConflictingDomainExists.t 
      | `DelegationSetNotAvailable of DelegationSetNotAvailable.t 
      | `DelegationSetNotReusable of DelegationSetNotReusable.t 
      | `HostedZoneAlreadyExists of HostedZoneAlreadyExists.t 
      | `InvalidDomainName of InvalidDomainName.t 
      | `InvalidInput of InvalidInput.t  | `InvalidVPCId of InvalidVPCId.t 
      | `NoSuchDelegationSet of NoSuchDelegationSet.t 
      | `TooManyHostedZones of TooManyHostedZones.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "CreateHostedZoneResponse"
    let make ?vPC =
      fun ~hostedZone ->
        fun ~changeInfo ->
          fun ~delegationSet ->
            fun ~location ->
              fun () ->
                { vPC; hostedZone; changeInfo; delegationSet; location }
    let error_of_json name json =
      match name with
      | "ConflictingDomainExists" ->
          `ConflictingDomainExists (ConflictingDomainExists.of_json json)
      | "DelegationSetNotAvailable" ->
          `DelegationSetNotAvailable (DelegationSetNotAvailable.of_json json)
      | "DelegationSetNotReusable" ->
          `DelegationSetNotReusable (DelegationSetNotReusable.of_json json)
      | "HostedZoneAlreadyExists" ->
          `HostedZoneAlreadyExists (HostedZoneAlreadyExists.of_json json)
      | "InvalidDomainName" ->
          `InvalidDomainName (InvalidDomainName.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "InvalidVPCId" -> `InvalidVPCId (InvalidVPCId.of_json json)
      | "NoSuchDelegationSet" ->
          `NoSuchDelegationSet (NoSuchDelegationSet.of_json json)
      | "TooManyHostedZones" ->
          `TooManyHostedZones (TooManyHostedZones.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConflictingDomainExists" ->
          `ConflictingDomainExists (ConflictingDomainExists.of_xml xml)
      | "DelegationSetNotAvailable" ->
          `DelegationSetNotAvailable (DelegationSetNotAvailable.of_xml xml)
      | "DelegationSetNotReusable" ->
          `DelegationSetNotReusable (DelegationSetNotReusable.of_xml xml)
      | "HostedZoneAlreadyExists" ->
          `HostedZoneAlreadyExists (HostedZoneAlreadyExists.of_xml xml)
      | "InvalidDomainName" ->
          `InvalidDomainName (InvalidDomainName.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "InvalidVPCId" -> `InvalidVPCId (InvalidVPCId.of_xml xml)
      | "NoSuchDelegationSet" ->
          `NoSuchDelegationSet (NoSuchDelegationSet.of_xml xml)
      | "TooManyHostedZones" ->
          `TooManyHostedZones (TooManyHostedZones.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConflictingDomainExists e ->
          `Assoc
            [("error", (`String "ConflictingDomainExists"));
            ("details", (ConflictingDomainExists.to_json e))]
      | `DelegationSetNotAvailable e ->
          `Assoc
            [("error", (`String "DelegationSetNotAvailable"));
            ("details", (DelegationSetNotAvailable.to_json e))]
      | `DelegationSetNotReusable e ->
          `Assoc
            [("error", (`String "DelegationSetNotReusable"));
            ("details", (DelegationSetNotReusable.to_json e))]
      | `HostedZoneAlreadyExists e ->
          `Assoc
            [("error", (`String "HostedZoneAlreadyExists"));
            ("details", (HostedZoneAlreadyExists.to_json e))]
      | `InvalidDomainName e ->
          `Assoc
            [("error", (`String "InvalidDomainName"));
            ("details", (InvalidDomainName.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `InvalidVPCId e ->
          `Assoc
            [("error", (`String "InvalidVPCId"));
            ("details", (InvalidVPCId.to_json e))]
      | `NoSuchDelegationSet e ->
          `Assoc
            [("error", (`String "NoSuchDelegationSet"));
            ("details", (NoSuchDelegationSet.to_json e))]
      | `TooManyHostedZones e ->
          `Assoc
            [("error", (`String "TooManyHostedZones"));
            ("details", (TooManyHostedZones.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("HostedZone", (Some (HostedZone.to_value x.hostedZone)));
        ("ChangeInfo", (Some (ChangeInfo.to_value x.changeInfo)));
        ("DelegationSet", (Some (DelegationSet.to_value x.delegationSet)));
        ("VPC", (Option.map x.vPC ~f:VPC.to_value));
        ("Location", (Some (ResourceURI.to_value x.location)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        ResourceURI.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Location") in
      let vPC = (Option.map ~f:VPC.of_xml) (Xml.child xml_arg0 "VPC") in
      let delegationSet =
        DelegationSet.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "DelegationSet") in
      let changeInfo =
        ChangeInfo.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChangeInfo") in
      let hostedZone =
        HostedZone.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HostedZone") in
      make ~location ?vPC ~delegationSet ~changeInfo ~hostedZone ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let location = field_map_exn json "Location" ResourceURI.of_json in
      let vPC = field_map json "VPC" VPC.of_json in
      let delegationSet =
        field_map_exn json "DelegationSet" DelegationSet.of_json in
      let changeInfo = field_map_exn json "ChangeInfo" ChangeInfo.of_json in
      let hostedZone = field_map_exn json "HostedZone" HostedZone.of_json in
      make ~location ?vPC ~delegationSet ~changeInfo ~hostedZone ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type containing the response information for the hosted zone."]
module CreateHostedZoneRequest =
  struct
    type nonrec t =
      {
      name: DNSName.t
        [@ocaml.doc
          "The name of the domain. Specify a fully qualified domain name, for example, www.example.com. The trailing dot is optional; Amazon Route 53 assumes that the domain name is fully qualified. This means that Route 53 treats www.example.com (without a trailing dot) and www.example.com. (with a trailing dot) as identical. If you're creating a public hosted zone, this is the name you have registered with your DNS registrar. If your domain name is registered with a registrar other than Route 53, change the name servers for your domain to the set of NameServers that CreateHostedZone returns in DelegationSet."];
      vPC: VPC.t option
        [@ocaml.doc
          "(Private hosted zones only) A complex type that contains information about the Amazon VPC that you're associating with this hosted zone. You can specify only one Amazon VPC when you create a private hosted zone. If you are associating a VPC with a hosted zone with this request, the paramaters VPCId and VPCRegion are also required. To associate additional Amazon VPCs with the hosted zone, use AssociateVPCWithHostedZone after you create a hosted zone."];
      callerReference: Nonce.t
        [@ocaml.doc
          "A unique string that identifies the request and that allows failed CreateHostedZone requests to be retried without the risk of executing the operation twice. You must use a unique CallerReference string every time you submit a CreateHostedZone request. CallerReference can be any unique string, for example, a date/time stamp."];
      hostedZoneConfig: HostedZoneConfig.t option
        [@ocaml.doc
          "(Optional) A complex type that contains the following optional values: For public and private hosted zones, an optional comment For private hosted zones, an optional PrivateZone element If you don't specify a comment or the PrivateZone element, omit HostedZoneConfig and the other elements."];
      delegationSetId: ResourceId.t option
        [@ocaml.doc
          "If you want to associate a reusable delegation set with this hosted zone, the ID that Amazon Route 53 assigned to the reusable delegation set when you created it. For more information about reusable delegation sets, see CreateReusableDelegationSet."]}
    let context_ = "CreateHostedZoneRequest"
    let make ?vPC =
      fun ?hostedZoneConfig ->
        fun ?delegationSetId ->
          fun ~name ->
            fun ~callerReference ->
              fun () ->
                {
                  vPC;
                  hostedZoneConfig;
                  delegationSetId;
                  name;
                  callerReference
                }
    let to_value x =
      structure_to_value
        [("Name", (Some (DNSName.to_value x.name)));
        ("VPC", (Option.map x.vPC ~f:VPC.to_value));
        ("CallerReference", (Some (Nonce.to_value x.callerReference)));
        ("HostedZoneConfig",
          (Option.map x.hostedZoneConfig ~f:HostedZoneConfig.to_value));
        ("DelegationSetId",
          (Option.map x.delegationSetId ~f:ResourceId.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let delegationSetId =
        (Option.map ~f:ResourceId.of_xml)
          (Xml.child xml_arg0 "DelegationSetId") in
      let hostedZoneConfig =
        (Option.map ~f:HostedZoneConfig.of_xml)
          (Xml.child xml_arg0 "HostedZoneConfig") in
      let callerReference =
        Nonce.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CallerReference") in
      let vPC = (Option.map ~f:VPC.of_xml) (Xml.child xml_arg0 "VPC") in
      let name =
        DNSName.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      make ?delegationSetId ?hostedZoneConfig ~callerReference ?vPC ~name ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let delegationSetId =
        field_map json "DelegationSetId" ResourceId.of_json in
      let hostedZoneConfig =
        field_map json "HostedZoneConfig" HostedZoneConfig.of_json in
      let callerReference =
        field_map_exn json "CallerReference" Nonce.of_json in
      let vPC = field_map json "VPC" VPC.of_json in
      let name = field_map_exn json "Name" DNSName.of_json in
      make ?delegationSetId ?hostedZoneConfig ~callerReference ?vPC ~name ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about the request to create a public or private hosted zone."]
module CreateHealthCheckResponse =
  struct
    type nonrec t =
      {
      healthCheck: HealthCheck.t
        [@ocaml.doc
          "A complex type that contains identifying information about the health check."];
      location: ResourceURI.t
        [@ocaml.doc "The unique URL representing the new health check."]}
    type nonrec error =
      [ `HealthCheckAlreadyExists of HealthCheckAlreadyExists.t 
      | `InvalidInput of InvalidInput.t 
      | `TooManyHealthChecks of TooManyHealthChecks.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "CreateHealthCheckResponse"
    let make ~healthCheck =
      fun ~location -> fun () -> { healthCheck; location }
    let error_of_json name json =
      match name with
      | "HealthCheckAlreadyExists" ->
          `HealthCheckAlreadyExists (HealthCheckAlreadyExists.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "TooManyHealthChecks" ->
          `TooManyHealthChecks (TooManyHealthChecks.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "HealthCheckAlreadyExists" ->
          `HealthCheckAlreadyExists (HealthCheckAlreadyExists.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "TooManyHealthChecks" ->
          `TooManyHealthChecks (TooManyHealthChecks.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `HealthCheckAlreadyExists e ->
          `Assoc
            [("error", (`String "HealthCheckAlreadyExists"));
            ("details", (HealthCheckAlreadyExists.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `TooManyHealthChecks e ->
          `Assoc
            [("error", (`String "TooManyHealthChecks"));
            ("details", (TooManyHealthChecks.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("HealthCheck", (Some (HealthCheck.to_value x.healthCheck)));
        ("Location", (Some (ResourceURI.to_value x.location)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let location =
        ResourceURI.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Location") in
      let healthCheck =
        HealthCheck.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HealthCheck") in
      make ~location ~healthCheck ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let location = field_map_exn json "Location" ResourceURI.of_json in
      let healthCheck = field_map_exn json "HealthCheck" HealthCheck.of_json in
      make ~location ~healthCheck ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type containing the response information for the new health check."]
module CreateHealthCheckRequest =
  struct
    type nonrec t =
      {
      callerReference: HealthCheckNonce.t
        [@ocaml.doc
          "A unique string that identifies the request and that allows you to retry a failed CreateHealthCheck request without the risk of creating two identical health checks: If you send a CreateHealthCheck request with the same CallerReference and settings as a previous request, and if the health check doesn't exist, Amazon Route 53 creates the health check. If the health check does exist, Route 53 returns the settings for the existing health check. If you send a CreateHealthCheck request with the same CallerReference as a deleted health check, regardless of the settings, Route 53 returns a HealthCheckAlreadyExists error. If you send a CreateHealthCheck request with the same CallerReference as an existing health check but with different settings, Route 53 returns a HealthCheckAlreadyExists error. If you send a CreateHealthCheck request with a unique CallerReference but settings identical to an existing health check, Route 53 creates the health check."];
      healthCheckConfig: HealthCheckConfig.t
        [@ocaml.doc
          "A complex type that contains settings for a new health check."]}
    let context_ = "CreateHealthCheckRequest"
    let make ~callerReference =
      fun ~healthCheckConfig ->
        fun () -> { callerReference; healthCheckConfig }
    let to_value x =
      structure_to_value
        [("CallerReference",
           (Some (HealthCheckNonce.to_value x.callerReference)));
        ("HealthCheckConfig",
          (Some (HealthCheckConfig.to_value x.healthCheckConfig)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let healthCheckConfig =
        HealthCheckConfig.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HealthCheckConfig") in
      let callerReference =
        HealthCheckNonce.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "CallerReference") in
      make ~healthCheckConfig ~callerReference ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let healthCheckConfig =
        field_map_exn json "HealthCheckConfig" HealthCheckConfig.of_json in
      let callerReference =
        field_map_exn json "CallerReference" HealthCheckNonce.of_json in
      make ~healthCheckConfig ~callerReference ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the health check request information."]
module ChangeTagsForResourceResponse =
  struct
    type nonrec t = unit
    type nonrec error =
      [ `InvalidInput of InvalidInput.t 
      | `NoSuchHealthCheck of NoSuchHealthCheck.t 
      | `NoSuchHostedZone of NoSuchHostedZone.t 
      | `PriorRequestNotComplete of PriorRequestNotComplete.t 
      | `ThrottlingException of ThrottlingException.t 
      | `Unknown_operation_error of (string * string option) ]
    let make () = ()
    let error_of_json name json =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchHealthCheck" ->
          `NoSuchHealthCheck (NoSuchHealthCheck.of_json json)
      | "NoSuchHostedZone" ->
          `NoSuchHostedZone (NoSuchHostedZone.of_json json)
      | "PriorRequestNotComplete" ->
          `PriorRequestNotComplete (PriorRequestNotComplete.of_json json)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchHealthCheck" ->
          `NoSuchHealthCheck (NoSuchHealthCheck.of_xml xml)
      | "NoSuchHostedZone" -> `NoSuchHostedZone (NoSuchHostedZone.of_xml xml)
      | "PriorRequestNotComplete" ->
          `PriorRequestNotComplete (PriorRequestNotComplete.of_xml xml)
      | "ThrottlingException" ->
          `ThrottlingException (ThrottlingException.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchHealthCheck e ->
          `Assoc
            [("error", (`String "NoSuchHealthCheck"));
            ("details", (NoSuchHealthCheck.to_json e))]
      | `NoSuchHostedZone e ->
          `Assoc
            [("error", (`String "NoSuchHostedZone"));
            ("details", (NoSuchHostedZone.to_json e))]
      | `PriorRequestNotComplete e ->
          `Assoc
            [("error", (`String "PriorRequestNotComplete"));
            ("details", (PriorRequestNotComplete.to_json e))]
      | `ThrottlingException e ->
          `Assoc
            [("error", (`String "ThrottlingException"));
            ("details", (ThrottlingException.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let of_header_and_body = ((fun (xs, pipe) -> make ())[@warning "-27"])
    let to_value _ = `Structure []
    let to_query v = to_query to_value v
    let of_xml _ = make ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json _ = make ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "Empty response for the request."]
module ChangeTagsForResourceRequest =
  struct
    type nonrec t =
      {
      resourceType: TagResourceType.t
        [@ocaml.doc
          "The type of the resource. The resource type for health checks is healthcheck. The resource type for hosted zones is hostedzone."];
      resourceId: TagResourceId.t
        [@ocaml.doc
          "The ID of the resource for which you want to add, change, or delete tags."];
      addTags: TagList.t option
        [@ocaml.doc
          "A complex type that contains a list of the tags that you want to add to the specified health check or hosted zone and/or the tags that you want to edit Value for. You can add a maximum of 10 tags to a health check or a hosted zone."];
      removeTagKeys: TagKeyList.t option
        [@ocaml.doc
          "A complex type that contains a list of the tags that you want to delete from the specified health check or hosted zone. You can specify up to 10 keys."]}
    let context_ = "ChangeTagsForResourceRequest"
    let make ?addTags =
      fun ?removeTagKeys ->
        fun ~resourceType ->
          fun ~resourceId ->
            fun () -> { addTags; removeTagKeys; resourceType; resourceId }
    let to_value x =
      structure_to_value
        [("ResourceType", (Some (TagResourceType.to_value x.resourceType)));
        ("ResourceId", (Some (TagResourceId.to_value x.resourceId)));
        ("AddTags", (Option.map x.addTags ~f:TagList.to_value));
        ("RemoveTagKeys",
          (Option.map x.removeTagKeys ~f:TagKeyList.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let removeTagKeys =
        (Option.map ~f:TagKeyList.of_xml)
          (Xml.child xml_arg0 "RemoveTagKeys") in
      let addTags =
        (Option.map ~f:TagList.of_xml) (Xml.child xml_arg0 "AddTags") in
      let resourceId =
        TagResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceId") in
      let resourceType =
        TagResourceType.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ResourceType") in
      make ?removeTagKeys ?addTags ~resourceId ~resourceType ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let removeTagKeys = field_map json "RemoveTagKeys" TagKeyList.of_json in
      let addTags = field_map json "AddTags" TagList.of_json in
      let resourceId = field_map_exn json "ResourceId" TagResourceId.of_json in
      let resourceType =
        field_map_exn json "ResourceType" TagResourceType.of_json in
      make ?removeTagKeys ?addTags ~resourceId ~resourceType ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about the tags that you want to add, edit, or delete."]
module ChangeResourceRecordSetsResponse =
  struct
    type nonrec t =
      {
      changeInfo: ChangeInfo.t
        [@ocaml.doc
          "A complex type that contains information about changes made to your hosted zone. This element contains an ID that you use when performing a GetChange action to get detailed information about the change."]}
    type nonrec error =
      [ `InvalidChangeBatch of InvalidChangeBatch.t 
      | `InvalidInput of InvalidInput.t 
      | `NoSuchHealthCheck of NoSuchHealthCheck.t 
      | `NoSuchHostedZone of NoSuchHostedZone.t 
      | `PriorRequestNotComplete of PriorRequestNotComplete.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ChangeResourceRecordSetsResponse"
    let make ~changeInfo = fun () -> { changeInfo }
    let error_of_json name json =
      match name with
      | "InvalidChangeBatch" ->
          `InvalidChangeBatch (InvalidChangeBatch.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "NoSuchHealthCheck" ->
          `NoSuchHealthCheck (NoSuchHealthCheck.of_json json)
      | "NoSuchHostedZone" ->
          `NoSuchHostedZone (NoSuchHostedZone.of_json json)
      | "PriorRequestNotComplete" ->
          `PriorRequestNotComplete (PriorRequestNotComplete.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "InvalidChangeBatch" ->
          `InvalidChangeBatch (InvalidChangeBatch.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "NoSuchHealthCheck" ->
          `NoSuchHealthCheck (NoSuchHealthCheck.of_xml xml)
      | "NoSuchHostedZone" -> `NoSuchHostedZone (NoSuchHostedZone.of_xml xml)
      | "PriorRequestNotComplete" ->
          `PriorRequestNotComplete (PriorRequestNotComplete.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `InvalidChangeBatch e ->
          `Assoc
            [("error", (`String "InvalidChangeBatch"));
            ("details", (InvalidChangeBatch.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `NoSuchHealthCheck e ->
          `Assoc
            [("error", (`String "NoSuchHealthCheck"));
            ("details", (NoSuchHealthCheck.to_json e))]
      | `NoSuchHostedZone e ->
          `Assoc
            [("error", (`String "NoSuchHostedZone"));
            ("details", (NoSuchHostedZone.to_json e))]
      | `PriorRequestNotComplete e ->
          `Assoc
            [("error", (`String "PriorRequestNotComplete"));
            ("details", (PriorRequestNotComplete.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ChangeInfo", (Some (ChangeInfo.to_value x.changeInfo)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let changeInfo =
        ChangeInfo.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChangeInfo") in
      make ~changeInfo ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let changeInfo = field_map_exn json "ChangeInfo" ChangeInfo.of_json in
      make ~changeInfo ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc "A complex type containing the response for the request."]
module ChangeResourceRecordSetsRequest =
  struct
    type nonrec t =
      {
      hostedZoneId: ResourceId.t
        [@ocaml.doc
          "The ID of the hosted zone that contains the resource record sets that you want to change."];
      changeBatch: ChangeBatch.t
        [@ocaml.doc
          "A complex type that contains an optional comment and the Changes element."]}
    let context_ = "ChangeResourceRecordSetsRequest"
    let make ~hostedZoneId =
      fun ~changeBatch -> fun () -> { hostedZoneId; changeBatch }
    let to_value x =
      structure_to_value
        [("Id", (Some (ResourceId.to_value x.hostedZoneId)));
        ("ChangeBatch", (Some (ChangeBatch.to_value x.changeBatch)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let changeBatch =
        ChangeBatch.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChangeBatch") in
      let hostedZoneId =
        ResourceId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ~changeBatch ~hostedZoneId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let changeBatch = field_map_exn json "ChangeBatch" ChangeBatch.of_json in
      let hostedZoneId = field_map_exn json "HostedZoneId" ResourceId.of_json in
      make ~changeBatch ~hostedZoneId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains change information for the resource record set."]
module AssociateVPCWithHostedZoneResponse =
  struct
    type nonrec t =
      {
      changeInfo: ChangeInfo.t
        [@ocaml.doc
          "A complex type that describes the changes made to your hosted zone."]}
    type nonrec error =
      [ `ConflictingDomainExists of ConflictingDomainExists.t 
      | `InvalidInput of InvalidInput.t  | `InvalidVPCId of InvalidVPCId.t 
      | `LimitsExceeded of LimitsExceeded.t 
      | `NoSuchHostedZone of NoSuchHostedZone.t 
      | `NotAuthorizedException of NotAuthorizedException.t 
      | `PriorRequestNotComplete of PriorRequestNotComplete.t 
      | `PublicZoneVPCAssociation of PublicZoneVPCAssociation.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "AssociateVPCWithHostedZoneResponse"
    let make ~changeInfo = fun () -> { changeInfo }
    let error_of_json name json =
      match name with
      | "ConflictingDomainExists" ->
          `ConflictingDomainExists (ConflictingDomainExists.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "InvalidVPCId" -> `InvalidVPCId (InvalidVPCId.of_json json)
      | "LimitsExceeded" -> `LimitsExceeded (LimitsExceeded.of_json json)
      | "NoSuchHostedZone" ->
          `NoSuchHostedZone (NoSuchHostedZone.of_json json)
      | "NotAuthorizedException" ->
          `NotAuthorizedException (NotAuthorizedException.of_json json)
      | "PriorRequestNotComplete" ->
          `PriorRequestNotComplete (PriorRequestNotComplete.of_json json)
      | "PublicZoneVPCAssociation" ->
          `PublicZoneVPCAssociation (PublicZoneVPCAssociation.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConflictingDomainExists" ->
          `ConflictingDomainExists (ConflictingDomainExists.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "InvalidVPCId" -> `InvalidVPCId (InvalidVPCId.of_xml xml)
      | "LimitsExceeded" -> `LimitsExceeded (LimitsExceeded.of_xml xml)
      | "NoSuchHostedZone" -> `NoSuchHostedZone (NoSuchHostedZone.of_xml xml)
      | "NotAuthorizedException" ->
          `NotAuthorizedException (NotAuthorizedException.of_xml xml)
      | "PriorRequestNotComplete" ->
          `PriorRequestNotComplete (PriorRequestNotComplete.of_xml xml)
      | "PublicZoneVPCAssociation" ->
          `PublicZoneVPCAssociation (PublicZoneVPCAssociation.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConflictingDomainExists e ->
          `Assoc
            [("error", (`String "ConflictingDomainExists"));
            ("details", (ConflictingDomainExists.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `InvalidVPCId e ->
          `Assoc
            [("error", (`String "InvalidVPCId"));
            ("details", (InvalidVPCId.to_json e))]
      | `LimitsExceeded e ->
          `Assoc
            [("error", (`String "LimitsExceeded"));
            ("details", (LimitsExceeded.to_json e))]
      | `NoSuchHostedZone e ->
          `Assoc
            [("error", (`String "NoSuchHostedZone"));
            ("details", (NoSuchHostedZone.to_json e))]
      | `NotAuthorizedException e ->
          `Assoc
            [("error", (`String "NotAuthorizedException"));
            ("details", (NotAuthorizedException.to_json e))]
      | `PriorRequestNotComplete e ->
          `Assoc
            [("error", (`String "PriorRequestNotComplete"));
            ("details", (PriorRequestNotComplete.to_json e))]
      | `PublicZoneVPCAssociation e ->
          `Assoc
            [("error", (`String "PublicZoneVPCAssociation"));
            ("details", (PublicZoneVPCAssociation.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ChangeInfo", (Some (ChangeInfo.to_value x.changeInfo)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let changeInfo =
        ChangeInfo.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChangeInfo") in
      make ~changeInfo ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let changeInfo = field_map_exn json "ChangeInfo" ChangeInfo.of_json in
      make ~changeInfo ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains the response information for the AssociateVPCWithHostedZone request."]
module AssociateVPCWithHostedZoneRequest =
  struct
    type nonrec t =
      {
      hostedZoneId: ResourceId.t
        [@ocaml.doc
          "The ID of the private hosted zone that you want to associate an Amazon VPC with. Note that you can't associate a VPC with a hosted zone that doesn't have an existing VPC association."];
      vPC: VPC.t
        [@ocaml.doc
          "A complex type that contains information about the VPC that you want to associate with a private hosted zone."];
      comment: AssociateVPCComment.t option
        [@ocaml.doc "Optional: A comment about the association request."]}
    let context_ = "AssociateVPCWithHostedZoneRequest"
    let make ?comment =
      fun ~hostedZoneId ->
        fun ~vPC -> fun () -> { comment; hostedZoneId; vPC }
    let to_value x =
      structure_to_value
        [("Id", (Some (ResourceId.to_value x.hostedZoneId)));
        ("VPC", (Some (VPC.to_value x.vPC)));
        ("Comment", (Option.map x.comment ~f:AssociateVPCComment.to_value))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let comment =
        (Option.map ~f:AssociateVPCComment.of_xml)
          (Xml.child xml_arg0 "Comment") in
      let vPC = VPC.of_xml (Xml.child_exn ~context:context_ xml_arg0 "VPC") in
      let hostedZoneId =
        ResourceId.of_xml (Xml.child_exn ~context:context_ xml_arg0 "Id") in
      make ?comment ~vPC ~hostedZoneId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let comment = field_map json "Comment" AssociateVPCComment.of_json in
      let vPC = field_map_exn json "VPC" VPC.of_json in
      let hostedZoneId = field_map_exn json "HostedZoneId" ResourceId.of_json in
      make ?comment ~vPC ~hostedZoneId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "A complex type that contains information about the request to associate a VPC with a private hosted zone."]
module ActivateKeySigningKeyResponse =
  struct
    type nonrec t = {
      changeInfo: ChangeInfo.t }
    type nonrec error =
      [ `ConcurrentModification of ConcurrentModification.t 
      | `InvalidInput of InvalidInput.t  | `InvalidKMSArn of InvalidKMSArn.t 
      | `InvalidKeySigningKeyStatus of InvalidKeySigningKeyStatus.t 
      | `InvalidSigningStatus of InvalidSigningStatus.t 
      | `NoSuchKeySigningKey of NoSuchKeySigningKey.t 
      | `Unknown_operation_error of (string * string option) ]
    let context_ = "ActivateKeySigningKeyResponse"
    let make ~changeInfo = fun () -> { changeInfo }
    let error_of_json name json =
      match name with
      | "ConcurrentModification" ->
          `ConcurrentModification (ConcurrentModification.of_json json)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_json json)
      | "InvalidKMSArn" -> `InvalidKMSArn (InvalidKMSArn.of_json json)
      | "InvalidKeySigningKeyStatus" ->
          `InvalidKeySigningKeyStatus
            (InvalidKeySigningKeyStatus.of_json json)
      | "InvalidSigningStatus" ->
          `InvalidSigningStatus (InvalidSigningStatus.of_json json)
      | "NoSuchKeySigningKey" ->
          `NoSuchKeySigningKey (NoSuchKeySigningKey.of_json json)
      | name ->
          `Unknown_operation_error
            (name, (Some (Yojson.Safe.to_string json)))
    let error_of_xml name xml =
      match name with
      | "ConcurrentModification" ->
          `ConcurrentModification (ConcurrentModification.of_xml xml)
      | "InvalidInput" -> `InvalidInput (InvalidInput.of_xml xml)
      | "InvalidKMSArn" -> `InvalidKMSArn (InvalidKMSArn.of_xml xml)
      | "InvalidKeySigningKeyStatus" ->
          `InvalidKeySigningKeyStatus (InvalidKeySigningKeyStatus.of_xml xml)
      | "InvalidSigningStatus" ->
          `InvalidSigningStatus (InvalidSigningStatus.of_xml xml)
      | "NoSuchKeySigningKey" ->
          `NoSuchKeySigningKey (NoSuchKeySigningKey.of_xml xml)
      | name ->
          `Unknown_operation_error (name, (Some (Awso.Xml.to_string xml)))
    let error_to_json : error -> Yojson.Safe.t =
      function
      | `ConcurrentModification e ->
          `Assoc
            [("error", (`String "ConcurrentModification"));
            ("details", (ConcurrentModification.to_json e))]
      | `InvalidInput e ->
          `Assoc
            [("error", (`String "InvalidInput"));
            ("details", (InvalidInput.to_json e))]
      | `InvalidKMSArn e ->
          `Assoc
            [("error", (`String "InvalidKMSArn"));
            ("details", (InvalidKMSArn.to_json e))]
      | `InvalidKeySigningKeyStatus e ->
          `Assoc
            [("error", (`String "InvalidKeySigningKeyStatus"));
            ("details", (InvalidKeySigningKeyStatus.to_json e))]
      | `InvalidSigningStatus e ->
          `Assoc
            [("error", (`String "InvalidSigningStatus"));
            ("details", (InvalidSigningStatus.to_json e))]
      | `NoSuchKeySigningKey e ->
          `Assoc
            [("error", (`String "NoSuchKeySigningKey"));
            ("details", (NoSuchKeySigningKey.to_json e))]
      | `Unknown_operation_error (code, msg) ->
          `Assoc (("error", (`String code)) ::
            ((match msg with
              | None -> []
              | Some m -> [("message", (`String m))])))
    let to_value x =
      structure_to_value
        [("ChangeInfo", (Some (ChangeInfo.to_value x.changeInfo)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let changeInfo =
        ChangeInfo.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "ChangeInfo") in
      make ~changeInfo ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let changeInfo = field_map_exn json "ChangeInfo" ChangeInfo.of_json in
      make ~changeInfo ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Activates a key-signing key (KSK) so that it can be used for signing by DNSSEC. This operation changes the KSK status to ACTIVE."]
module ActivateKeySigningKeyRequest =
  struct
    type nonrec t =
      {
      hostedZoneId: ResourceId.t
        [@ocaml.doc "A unique string used to identify a hosted zone."];
      name: SigningKeyName.t
        [@ocaml.doc
          "A string used to identify a key-signing key (KSK). Name can include numbers, letters, and underscores (_). Name must be unique for each key-signing key in the same hosted zone."]}
    let context_ = "ActivateKeySigningKeyRequest"
    let make ~hostedZoneId = fun ~name -> fun () -> { hostedZoneId; name }
    let to_value x =
      structure_to_value
        [("HostedZoneId", (Some (ResourceId.to_value x.hostedZoneId)));
        ("Name", (Some (SigningKeyName.to_value x.name)))]
    let to_query v = to_query to_value v
    let of_xml xml_arg0 =
      let name =
        SigningKeyName.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "Name") in
      let hostedZoneId =
        ResourceId.of_xml
          (Xml.child_exn ~context:context_ xml_arg0 "HostedZoneId") in
      make ~name ~hostedZoneId ()
    let of_string s = of_xml (Awso.Xml.parse_response s)[@@warning "-32"]
    let of_json json =
      let name = field_map_exn json "Name" SigningKeyName.of_json in
      let hostedZoneId = field_map_exn json "HostedZoneId" ResourceId.of_json in
      make ~name ~hostedZoneId ()
    let to_json v = composed_to_json to_value v
  end[@@ocaml.doc
       "Activates a key-signing key (KSK) so that it can be used for signing by DNSSEC. This operation changes the KSK status to ACTIVE."]
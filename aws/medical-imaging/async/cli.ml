(* generated by: awso-codegen generate-all --botocore-data vendor/botocore/botocore/data -o aws --runtime-dir lib/runtime/awso --cli-dir awso-cli *)
open Core
open Async
let json_arg = Command.Arg_type.create Yojson.Safe.from_string
let call ?endpoint_url ?profile ?region f m result_to_json error_to_json =
  let region =
    match region with
    | Some region -> Some (Awso.Region.of_string region)
    | None -> None in
  (Awso_async.Cfg.get_exn ?profile ?region ()) >>=
    (fun cfg ->
       (f ?endpoint_url ?cfg:(Some cfg) m) >>=
         (fun result ->
            match result with
            | Error err ->
                (match error_to_json with
                 | None ->
                     failwithf
                       "endpoint error, but no error values defined in boto"
                       ()
                 | Some to_json ->
                     let s = (err |> to_json) |> Yojson.Safe.to_string in
                     failwithf "AWS error: %s" s ())
            | Ok result ->
                ((match result_to_json with
                  | None -> print_endline "ok response from endpoint"
                  | Some to_json ->
                      ((result |> to_json) |> Yojson.Safe.to_string) |>
                        print_endline);
                 return ())))
let copy_image_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and force = flag "force" (optional bool) ~doc:"BOOL Boolean"
       and promoteToPrimary =
         flag "promote-to-primary" (optional bool) ~doc:"BOOL Boolean"
       and datastoreId =
         flag "datastore-id" (required string) ~doc:"STRING DatastoreId"
       and sourceImageSetId =
         flag "source-image-set-id" (required string)
           ~doc:"STRING ImageSetId"
       and copyImageSetInformation =
         flag "copy-image-set-information" (required json_arg)
           ~doc:"JSON CopyImageSetInformation" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.copy_image_set
           (Values.CopyImageSetRequest.make ?force ?promoteToPrimary
              ~datastoreId ~sourceImageSetId
              ~copyImageSetInformation:(Values.CopyImageSetInformation.of_json
                                          copyImageSetInformation) ())
           (Some Values.CopyImageSetResponse.to_json)
           (Some Values.CopyImageSetResponse.error_to_json)])
let create_datastore =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datastoreName =
         flag "datastore-name" (optional string) ~doc:"STRING DatastoreName"
       and tags = flag "tags" (optional json_arg) ~doc:"JSON TagMap"
       and kmsKeyArn =
         flag "kms-key-arn" (optional string) ~doc:"STRING KmsKeyArn"
       and lambdaAuthorizerArn =
         flag "lambda-authorizer-arn" (optional string)
           ~doc:"STRING LambdaArn"
       and losslessStorageFormat =
         flag "lossless-storage-format" (optional json_arg)
           ~doc:"JSON LosslessStorageFormat"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.create_datastore
           (Values.CreateDatastoreRequest.make ?datastoreName
              ?tags:(Option.map ~f:Values.TagMap.of_json tags) ?kmsKeyArn
              ?lambdaAuthorizerArn
              ?losslessStorageFormat:(Option.map
                                        ~f:Values.LosslessStorageFormat.of_json
                                        losslessStorageFormat) ~clientToken
              ()) (Some Values.CreateDatastoreResponse.to_json)
           (Some Values.CreateDatastoreResponse.error_to_json)])
let delete_datastore =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datastoreId =
         flag "datastore-id" (required string) ~doc:"STRING DatastoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_datastore
           (Values.DeleteDatastoreRequest.make ~datastoreId ())
           (Some Values.DeleteDatastoreResponse.to_json)
           (Some Values.DeleteDatastoreResponse.error_to_json)])
let delete_image_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datastoreId =
         flag "datastore-id" (required string) ~doc:"STRING DatastoreId"
       and imageSetId =
         flag "image-set-id" (required string) ~doc:"STRING ImageSetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.delete_image_set
           (Values.DeleteImageSetRequest.make ~datastoreId ~imageSetId ())
           (Some Values.DeleteImageSetResponse.to_json)
           (Some Values.DeleteImageSetResponse.error_to_json)])
let get_d_i_c_o_m_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datastoreId =
         flag "datastore-id" (required string) ~doc:"STRING DatastoreId"
       and jobId = flag "job-id" (required string) ~doc:"STRING JobId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_d_i_c_o_m_import_job
           (Values.GetDICOMImportJobRequest.make ~datastoreId ~jobId ())
           (Some Values.GetDICOMImportJobResponse.to_json)
           (Some Values.GetDICOMImportJobResponse.error_to_json)])
let get_datastore =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datastoreId =
         flag "datastore-id" (required string) ~doc:"STRING DatastoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_datastore (Values.GetDatastoreRequest.make ~datastoreId ())
           (Some Values.GetDatastoreResponse.to_json)
           (Some Values.GetDatastoreResponse.error_to_json)])
let get_image_frame =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datastoreId =
         flag "datastore-id" (required string) ~doc:"STRING DatastoreId"
       and imageSetId =
         flag "image-set-id" (required string) ~doc:"STRING ImageSetId"
       and imageFrameInformation =
         flag "image-frame-information" (required json_arg)
           ~doc:"JSON ImageFrameInformation" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_image_frame
           (Values.GetImageFrameRequest.make ~datastoreId ~imageSetId
              ~imageFrameInformation:(Values.ImageFrameInformation.of_json
                                        imageFrameInformation) ())
           (Some Values.GetImageFrameResponse.to_json)
           (Some Values.GetImageFrameResponse.error_to_json)])
let get_image_set =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionId =
         flag "version-id" (optional string)
           ~doc:"STRING ImageSetExternalVersionId"
       and datastoreId =
         flag "datastore-id" (required string) ~doc:"STRING DatastoreId"
       and imageSetId =
         flag "image-set-id" (required string) ~doc:"STRING ImageSetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_image_set
           (Values.GetImageSetRequest.make ?versionId ~datastoreId
              ~imageSetId ()) (Some Values.GetImageSetResponse.to_json)
           (Some Values.GetImageSetResponse.error_to_json)])
let get_image_set_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and versionId =
         flag "version-id" (optional string)
           ~doc:"STRING ImageSetExternalVersionId"
       and datastoreId =
         flag "datastore-id" (required string) ~doc:"STRING DatastoreId"
       and imageSetId =
         flag "image-set-id" (required string) ~doc:"STRING ImageSetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.get_image_set_metadata
           (Values.GetImageSetMetadataRequest.make ?versionId ~datastoreId
              ~imageSetId ())
           (Some Values.GetImageSetMetadataResponse.to_json)
           (Some Values.GetImageSetMetadataResponse.error_to_json)])
let list_d_i_c_o_m_import_jobs =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobStatus =
         flag "job-status" (optional json_arg) ~doc:"JSON JobStatus"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListDICOMImportJobsRequestMaxResultsInteger"
       and datastoreId =
         flag "datastore-id" (required string) ~doc:"STRING DatastoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_d_i_c_o_m_import_jobs
           (Values.ListDICOMImportJobsRequest.make
              ?jobStatus:(Option.map ~f:Values.JobStatus.of_json jobStatus)
              ?nextToken ?maxResults ~datastoreId ())
           (Some Values.ListDICOMImportJobsResponse.to_json)
           (Some Values.ListDICOMImportJobsResponse.error_to_json)])
let list_datastores =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and datastoreStatus =
         flag "datastore-status" (optional json_arg)
           ~doc:"JSON DatastoreStatus"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListDatastoresRequestMaxResultsInteger" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_datastores
           (Values.ListDatastoresRequest.make
              ?datastoreStatus:(Option.map ~f:Values.DatastoreStatus.of_json
                                  datastoreStatus) ?nextToken ?maxResults ())
           (Some Values.ListDatastoresResponse.to_json)
           (Some Values.ListDatastoresResponse.error_to_json)])
let list_image_set_versions =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT ListImageSetVersionsRequestMaxResultsInteger"
       and datastoreId =
         flag "datastore-id" (required string) ~doc:"STRING DatastoreId"
       and imageSetId =
         flag "image-set-id" (required string) ~doc:"STRING ImageSetId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_image_set_versions
           (Values.ListImageSetVersionsRequest.make ?nextToken ?maxResults
              ~datastoreId ~imageSetId ())
           (Some Values.ListImageSetVersionsResponse.to_json)
           (Some Values.ListImageSetVersionsResponse.error_to_json)])
let list_tags_for_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.list_tags_for_resource
           (Values.ListTagsForResourceRequest.make ~resourceArn ())
           (Some Values.ListTagsForResourceResponse.to_json)
           (Some Values.ListTagsForResourceResponse.error_to_json)])
let search_image_sets =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and searchCriteria =
         flag "search-criteria" (optional json_arg)
           ~doc:"JSON SearchCriteria"
       and maxResults =
         flag "max-results" (optional int)
           ~doc:"INT SearchImageSetsRequestMaxResultsInteger"
       and nextToken =
         flag "next-token" (optional string) ~doc:"STRING NextToken"
       and datastoreId =
         flag "datastore-id" (required string) ~doc:"STRING DatastoreId" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.search_image_sets
           (Values.SearchImageSetsRequest.make
              ?searchCriteria:(Option.map ~f:Values.SearchCriteria.of_json
                                 searchCriteria) ?maxResults ?nextToken
              ~datastoreId ()) (Some Values.SearchImageSetsResponse.to_json)
           (Some Values.SearchImageSetsResponse.error_to_json)])
let start_d_i_c_o_m_import_job =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and jobName = flag "job-name" (optional string) ~doc:"STRING JobName"
       and inputOwnerAccountId =
         flag "input-owner-account-id" (optional string)
           ~doc:"STRING AwsAccountId"
       and importConfiguration =
         flag "import-configuration" (optional json_arg)
           ~doc:"JSON ImportConfiguration"
       and dataAccessRoleArn =
         flag "data-access-role-arn" (required string) ~doc:"STRING RoleArn"
       and clientToken =
         flag "client-token" (required string) ~doc:"STRING ClientToken"
       and datastoreId =
         flag "datastore-id" (required string) ~doc:"STRING DatastoreId"
       and inputS3Uri =
         flag "input-s3-uri" (required string) ~doc:"STRING S3Uri"
       and outputS3Uri =
         flag "output-s3-uri" (required string) ~doc:"STRING S3Uri" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.start_d_i_c_o_m_import_job
           (Values.StartDICOMImportJobRequest.make ?jobName
              ?inputOwnerAccountId
              ?importConfiguration:(Option.map
                                      ~f:Values.ImportConfiguration.of_json
                                      importConfiguration) ~dataAccessRoleArn
              ~clientToken ~datastoreId ~inputS3Uri ~outputS3Uri ())
           (Some Values.StartDICOMImportJobResponse.to_json)
           (Some Values.StartDICOMImportJobResponse.error_to_json)])
let tag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tags = flag "tags" (required json_arg) ~doc:"JSON TagMap" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.tag_resource
           (Values.TagResourceRequest.make ~resourceArn
              ~tags:(Values.TagMap.of_json tags) ())
           (Some Values.TagResourceResponse.to_json)
           (Some Values.TagResourceResponse.error_to_json)])
let untag_resource =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and resourceArn =
         flag "resource-arn" (required string) ~doc:"STRING Arn"
       and tagKeys =
         flag "tag-keys" (required json_arg) ~doc:"JSON TagKeyList" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.untag_resource
           (Values.UntagResourceRequest.make ~resourceArn
              ~tagKeys:(Values.TagKeyList.of_json tagKeys) ())
           (Some Values.UntagResourceResponse.to_json)
           (Some Values.UntagResourceResponse.error_to_json)])
let update_image_set_metadata =
  Command.async ~summary:""
    ([%map_open.Command
       let cli_profile =
         flag "-cli-profile" (optional string) ~doc:"NAME aws profile to use"
       and cli_region =
         flag "-cli-region" (optional string) ~doc:"REGION override region"
       and endpoint_url =
         flag "-endpoint-url" (optional string)
           ~doc:"URL override endpoint url"
       and force = flag "force" (optional bool) ~doc:"BOOL Boolean"
       and includeStudyImageSets =
         flag "include-study-image-sets" (optional bool) ~doc:"BOOL Boolean"
       and datastoreId =
         flag "datastore-id" (required string) ~doc:"STRING DatastoreId"
       and imageSetId =
         flag "image-set-id" (required string) ~doc:"STRING ImageSetId"
       and latestVersionId =
         flag "latest-version-id" (required string)
           ~doc:"STRING ImageSetExternalVersionId"
       and updateImageSetMetadataUpdates =
         flag "update-image-set-metadata-updates" (required json_arg)
           ~doc:"JSON MetadataUpdates" in
       fun () ->
         call ?endpoint_url ?profile:cli_profile ?region:cli_region
           Io.update_image_set_metadata
           (Values.UpdateImageSetMetadataRequest.make ?force
              ?includeStudyImageSets ~datastoreId ~imageSetId
              ~latestVersionId
              ~updateImageSetMetadataUpdates:(Values.MetadataUpdates.of_json
                                                updateImageSetMetadataUpdates)
              ()) (Some Values.UpdateImageSetMetadataResponse.to_json)
           (Some Values.UpdateImageSetMetadataResponse.error_to_json)])
let main =
  Command.group
    ~summary:((Awso.Service.to_string Values.service) ^ " commands")
    [("copy-image-set", copy_image_set);
    ("create-datastore", create_datastore);
    ("delete-datastore", delete_datastore);
    ("delete-image-set", delete_image_set);
    ("get-d-i-c-o-m-import-job", get_d_i_c_o_m_import_job);
    ("get-datastore", get_datastore);
    ("get-image-frame", get_image_frame);
    ("get-image-set", get_image_set);
    ("get-image-set-metadata", get_image_set_metadata);
    ("list-d-i-c-o-m-import-jobs", list_d_i_c_o_m_import_jobs);
    ("list-datastores", list_datastores);
    ("list-image-set-versions", list_image_set_versions);
    ("list-tags-for-resource", list_tags_for_resource);
    ("search-image-sets", search_image_sets);
    ("start-d-i-c-o-m-import-job", start_d_i_c_o_m_import_job);
    ("tag-resource", tag_resource);
    ("untag-resource", untag_resource);
    ("update-image-set-metadata", update_image_set_metadata)]